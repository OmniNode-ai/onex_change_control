---
schema_version: 1.0.0
ticket_id: OMN-14201
evidence_item_id: dod-omn14201-grant-schema-validation
check_type: command
check_value: uv run python src/onex_change_control/scripts/validate_prod_promotion_grants.py --file grants/prod_promotion_grants.yaml
contract_sha256: sha256:68a91182493296550b046bd04b5690edb77b3aa55dfa16435603ea60cc692e06
contract_entry_sha256: sha256:7d128f9f265fbc54ef78d08b55c06523c7b675f24ef505fbaea3a422766a607b
status: PASS
run_timestamp: '2026-09-10T17:19:55Z'
commit_sha: __COMMIT_SHA__
runner: prod-scale-down-plan-lane
verifier: prod-scale-down-plan-lane
probe_command: uv run python src/onex_change_control/scripts/validate_prod_promotion_grants.py --file <rendered grants file>
probe_stdout: 'PASS: 1 grant(s) validated successfully'
actual_output: |-
  PASS: the repo's own prod-promotion grant validator accepts the rendered OMN-14201 entry --
  "1 grant(s) validated successfully", exit 0. Executed hermetically from the repo checkout against the
  rendered file with placeholders substituted (approved_by jonahgabriel, expires_at 2026-09-17T17:00:00Z,
  created_at 2026-09-10T17:20:00Z); no network.
  NEGATIVE CONTROL run in the same session against a byte-identical copy with runtime_lane blanked:
  "FAIL: 1 violation(s): Entry[0]: runtime_lane must be a non-empty string, got: ''", exit 1.
  The PASS is therefore a real verdict from a validator that does reject, not a no-op.
  The repo's pytest suite tests/test_prod_promotion_grants.py also passed, 40 passed in 0.15s, but that
  suite resolves the grants file from the repo root rather than from the rendered copy, so it is reported
  here as a companion signal and NOT as the proof of this evidence item. The --file invocation above is
  the proof, because its target is unambiguous.
exit_code: 0
pr_number: __PR_NUMBER__
branch: __BRANCH__
