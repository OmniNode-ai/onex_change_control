---
schema_version: "1.0.0"
ticket_id: OMN-9614
title: "Add onex hooks CLI subcommands"
summary: "Implement onex hooks {list,mask,enable,disable} CLI for user-facing hook bitmask management"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "CLI module cli_hooks.py exists with list/mask/enable/disable subcommands"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PYTHONPATH=src:$PYTHONPATH uv run python -m pytest tests/unit/cli/test_cli_hooks.py
          -v"
  - id: "dod-002"
    description: "onex hooks list shows all 58 enum members"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PYTHONPATH=src:$PYTHONPATH uv run onex hooks list | wc -l"
  - id: "dod-003"
    description: "onex hooks disable writes ONEX_HOOKS_MASK to ~/.omnibase/.env atomically"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PYTHONPATH=src:$PYTHONPATH uv run python -m pytest tests/unit/cli/test_cli_hooks.py::test_disable_writes_mask
          -v"
  - id: "dod-004"
    description: "mypy --strict passes on cli_hooks module"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run mypy src/omnibase_core/cli/cli_hooks.py --strict"
