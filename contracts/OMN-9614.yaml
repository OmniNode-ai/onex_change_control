---
schema_version: "1.0.0"
ticket_id: OMN-9614
title: "Add onex hooks CLI subcommands"
summary: "Implement onex hooks {list,mask,enable,disable} CLI for user-facing hook bitmask management"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Durable OCC DoD receipts exist for the OMN-9614 merge PR"
    command: "test -s drift/dod_receipts/OMN-9614/dod-001/command.yaml && test -s drift/dod_receipts/OMN-9614/dod-002/command.yaml
      && test -s drift/dod_receipts/OMN-9614/dod-003/command.yaml && test -s drift/dod_receipts/OMN-9614/dod-004/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "CLI module cli_hooks.py exists with list/mask/enable/disable subcommands"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-9614/dod-001/command.yaml && grep
          -q 'pr_number: 1013' drift/dod_receipts/OMN-9614/dod-001/command.yaml"
  - id: "dod-002"
    description: "onex hooks list shows all 58 enum members"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-9614/dod-002/command.yaml && grep
          -q '58 enum entries' drift/dod_receipts/OMN-9614/dod-002/command.yaml"
  - id: "dod-003"
    description: "onex hooks disable writes ONEX_HOOKS_MASK to ~/.omnibase/.env atomically"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-9614/dod-003/command.yaml && grep
          -q 'ONEX_HOOKS_MASK' drift/dod_receipts/OMN-9614/dod-003/command.yaml"
  - id: "dod-004"
    description: "mypy --strict passes on cli_hooks module"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-9614/dod-004/command.yaml && grep
          -q 'mypy --strict' drift/dod_receipts/OMN-9614/dod-004/command.yaml"
