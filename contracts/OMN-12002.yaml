---
schema_version: "1.0.0"
ticket_id: "OMN-12002"
title: "Allow auto-wired handlers to publish from handle_async"
summary: >
  Central OCC evidence for omnibase_infra PR #1740, which fixes auto-wired handler dispatch so handlers
  that explicitly declare handle_async are dispatched through that async runtime entry point. This preserves
  side-effect publishes performed by orchestrator handlers while keeping sync-only handlers on handle.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1740"
    description: "omnibase_infra PR #1740 carries the OMN-12002 handle_async dispatch fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=1740 REPO=OmniNode-ai/omnibase_infra gh pr view ${PR_NUMBER} --repo ${REPO}
          --json number,state,headRefOid,headRefName,title"
  - id: "dod-local-validation"
    description: "Focused handler-wiring unit tests and strict mypy validation passed locally for the
      dispatch-target typing fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12002/dod-local-validation/command.yaml
          && grep -q '6 passed' drift/dod_receipts/OMN-12002/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      No live deploy was performed for this runtime auto-wiring fix. The code path takes effect on the
      next normal runtime restart and does not add topics, schemas, migrations, or configuration.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy: no live deploy required; handler_wiring.py change applies on next
          normal runtime restart'"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC evidence for OMN-12002."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12002/dod-occ-pr/command.yaml"
