---
schema_version: "1.0.0"
ticket_id: "OMN-6664"
title: "Clean up non-standard noqa suppressions in omnibase_infra"
summary: >
  Rebase and validate omnibase_infra PR #1778, which registers the repo's custom lint suppression code
  prefixes, removes redundant quoted annotations from remote task state persistence, and normalizes malformed
  snapshot cache noqa comments.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #1778 carries the OMN-6664 lint suppression cleanup branch at the
      recorded head."
    command: "gh pr view 1778 --repo OmniNode-ai/omnibase_infra --json number,url,headRefOid,headRefName,title,state,baseRefName"
  - kind: "tests"
    description: "Focused formatting, lint, type, and unit verification pass against the PR worktree."
    command: "uv run ruff format --check src/ tests/ && uv run ruff check src/ tests/ && uv run mypy src/omnibase_infra
      && uv run python -m pytest tests/unit/runtime/auto_wiring/test_handler_wiring_handle_async_dispatch.py
      -q"
  - kind: "manual"
    description: "Runtime-path deploy impact is reviewed for the lint-only persistence and snapshot cache
      edits."
    command: "deploy impact review for omnibase_infra PR #1778"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1778-local-verification"
    description: "omnibase_infra PR #1778 lint suppression cleanup passes focused local verification at
      the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-6664/dod-infra-pr-1778-local-verification/command.yaml"
          && grep -q '^commit_sha: "6790648c6e51a3109bfe8fe30206025e209f635f"$' "drift/dod_receipts/OMN-6664/dod-infra-pr-1778-local-verification/command.yaml"
          && grep -q '3955 files already formatted' "drift/dod_receipts/OMN-6664/dod-infra-pr-1778-local-verification/command.yaml"
          && grep -q 'All checks passed!' "drift/dod_receipts/OMN-6664/dod-infra-pr-1778-local-verification/command.yaml"
          && grep -q 'Success: no issues found in 2372 source files' "drift/dod_receipts/OMN-6664/dod-infra-pr-1778-local-verification/command.yaml"
          && grep -q '7 passed' "drift/dod_receipts/OMN-6664/dod-infra-pr-1778-local-verification/command.yaml"
  - id: "dod-infra-pr-1778-deploy-impact"
    description: "omnibase_infra PR #1778 runtime-path edits are deploy-reviewed as lint-only changes
      with no runtime contract, topic, schema, or migration changes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-6664/dod-infra-pr-1778-deploy-impact/command.yaml"
          && grep -q '^commit_sha: "6790648c6e51a3109bfe8fe30206025e209f635f"$' "drift/dod_receipts/OMN-6664/dod-infra-pr-1778-deploy-impact/command.yaml"
          && grep -q 'no runtime deploy required' "drift/dod_receipts/OMN-6664/dod-infra-pr-1778-deploy-impact/command.yaml"
          && echo 'deploy impact review passed'
