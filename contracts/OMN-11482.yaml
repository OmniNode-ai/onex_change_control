---
schema_version: "1.0.0"
ticket_id: "OMN-11482"
title: "Fix _InputProxy invoke crash"
summary: >-
  Bind central OCC evidence for onex-self-extending-agent PR #105, which wraps tool invoke input in _InputProxy
  so generated handlers can use attribute-style, dict-style, get(), and unpacking access without crashing
  agent mode.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Product PR #105 test, lint, typecheck, security, title, skip-token, and review-thread
      checks pass at the observed head."
    command: "gh pr checks 105 --repo OmniNode-ai/onex-self-extending-agent"
  - kind: "manual"
    description: "Product PR #105 is bound to OMN-11482 and head 1722949b26d3b8e74ac690a7f7be467be83c4f3a."
    command: "gh pr view 105 --repo OmniNode-ai/onex-self-extending-agent --json number,title,headRefOid,headRefName,state,url"
  - kind: "ci"
    description: "onex_change_control PR #1324 carries the central OCC contract and receipt evidence."
    command: "See drift/dod_receipts/OMN-11482/dod-occ-pr-1324/command.yaml"
dod_evidence:
  - id: "dod-product-pr-head"
    description: "Bind OMN-11482 evidence to onex-self-extending-agent PR #105 at the observed product
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11482/dod-product-pr-head/command.yaml\"
          && grep -q '^pr_number: 105$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11482/dod-product-pr-head/command.yaml\"
          && grep -q '^commit_sha: \"1722949b26d3b8e74ac690a7f7be467be83c4f3a\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11482/dod-product-pr-head/command.yaml\""
  - id: "dod-product-checks"
    description: "Product PR #105 has passing non-receipt checks; Receipt Gate is the expected missing-OCC-evidence
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11482/dod-product-checks/command.yaml\""
  - id: "dod-occ-pr-1324"
    description: "onex_change_control PR #1324 is open and binds this central OCC evidence to OMN-11482."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11482/dod-occ-pr-1324/command.yaml\"
          && grep -q '^pr_number: 1324$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11482/dod-occ-pr-1324/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
