---
schema_version: "1.0.0"
ticket_id: "OMN-15200"
title: "Slim omniclaude CLAUDE.md — org-wide repo CLAUDE.md refresh"
summary: >
  OCC companion evidence for omniclaude PR #1948: slims CLAUDE.md from 40791 to 18980 bytes per context-engineering
  doctrine (drift-prone factual state replaced with regenerating probes; Headless Mode reference moved
  to docs/runbooks/headless-mode.md; stale TeamCreate automation guidance replaced with Workflow-tool
  doctrine; live-false claims corrected against source: OMNICLAUDE_PROJECT_ROOT is live in find_python(),
  hooks.json carries three registered guards, emit socket is ~/.claude/emit.sock, onex hooks CLI exists).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Product PR binding and content probes for omniclaude#1948."
    command: "gh pr view 1948 --repo OmniNode-ai/omniclaude --json number,state,headRefName,headRefOid,files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1948-head-a4f46a64"
    description: >
      Consuming-PR binding: omniclaude PR #1948 head a4f46a6406441a3f6751c8e5f2e06ad4a5376762 carries
      the slimmed CLAUDE.md plus the new docs/runbooks/headless-mode.md for OMN-15200.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1948 --repo OmniNode-ai/omniclaude --json number,state"
  - id: "dod-omniclaude1948-claude-slim-a4f46a64"
    description: >
      Falsifiable product-head probe for omniclaude PR #1948: the new docs/runbooks/headless-mode.md exists
      at the pinned head, CLAUDE.md at the same head points to it, and CLAUDE.md still carries the api_timeout_ms
      keep-contract trap (do not raise to 1000ms or higher).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omniclaude/contents/docs/runbooks/headless-mode.md?ref=a4f46a6406441a3f6751c8e5f2e06ad4a5376762'
          --jq .content | base64 -d | grep -q 'Headless Mode' && gh api 'repos/OmniNode-ai/omniclaude/contents/CLAUDE.md?ref=a4f46a6406441a3f6751c8e5f2e06ad4a5376762'
          --jq .content | base64 -d | grep -q 'docs/runbooks/headless-mode.md' && gh api 'repos/OmniNode-ai/omniclaude/contents/CLAUDE.md?ref=a4f46a6406441a3f6751c8e5f2e06ad4a5376762'
          --jq .content | base64 -d | grep -q 'api_timeout_ms'
  - id: "dod-occ-self"
    description: >
      OCC companion self-bind for OMN-15200 (deterministic commit-sha bind, no PR-number prediction).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15200/dod-occ-self/command.yaml"
