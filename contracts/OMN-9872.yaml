---
schema_version: "1.0.0"
ticket_id: "OMN-9872"
title: "Wire merge-sweep conflict resolution through conflict-hunk effect worktrees"
summary: >
  Adds push step to HandlerConflictHunk: after LLM resolves conflict hunks and commits in an ephemeral
  worktree, push the resolution to origin via git push --force-with-lease. Refactors _resolve into helper
  methods (_apply_llm_resolutions, _commit_resolution, _push_resolution) to satisfy SonarCloud C901 cognitive
  complexity gate. Adds docs/work-tracking/contracts/OMN-9872.yaml deploy-gate contract.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #684 merged to main with CI green."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,mergeCommit"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-684"
    description: "omnimarket PR #684 (conflict-hunk push + refactor) merged to main."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state --jq .state | grep -q MERGED"
