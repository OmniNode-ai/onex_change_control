---
schema_version: "1.0.0"
ticket_id: "OMN-11248"
title: "feat(OMN-11248): integration test - dynamic contract registration E2E"
summary: >
  Adds E2E integration coverage for the dynamic contract registration path in omnibase_infra. The tests
  exercise register -> materialize -> dispatcher/topic wiring with a real MessageDispatchEngine and EventBusInmemory
  plus a no-op in-process handler fixture. This is follow-on proof for OMN-11244 runtime contract materialization
  and does not add runtime service wiring.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Dynamic registration E2E integration tests pass locally."
    command: "cd omnibase_infra && uv run pytest tests/integration/runtime/test_dynamic_contract_registration_e2e.py
      -q"
  - kind: "ci"
    description: "omnibase_infra PR #1656 carries the E2E test implementation."
    command: "gh pr view 1656 --repo OmniNode-ai/omnibase_infra"
  - kind: "manual"
    description: "No runtime deploy required; PR adds integration tests and a no-op test fixture only."
    command: "See drift/dod_receipts/OMN-11248/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1656"
    description: "omnibase_infra PR #1656 carries the dynamic registration E2E test implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11248/dod-infra-pr-1656/command.yaml
          && grep -q '^pr_number: 1656$' drift/dod_receipts/OMN-11248/dod-infra-pr-1656/command.yaml"
  - id: "dod-integration-tests"
    description: "Five focused dynamic registration integration tests passed locally after rebase."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11248/dod-integration-tests/command.yaml
          && grep -q '5 passed' drift/dod_receipts/OMN-11248/dod-integration-tests/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy, runtime restart, .201 mutation, or event-bus write performed or required
      for this test-only PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11248/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-11248/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carrying this contract and binding receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11248/dod-occ-pr/command.yaml"
