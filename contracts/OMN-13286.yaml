---
schema_version: "1.0.0"
ticket_id: "OMN-13286"
title: "feat(OMN-13286): url-authority fleet wiring — seed baseline + wire check-url-authority (W1)"
summary: >-
  W1 of the validator-standardization remediation (child of OMN-12803). Seeds the shared url-authority
  ratchet baseline (omnibase_core/src/omnibase_core/validation/baselines/url_authority_baseline.json)
  for omnibase_infra (49), omniclaude (50), and omniintelligence (33) so the already-exposed check-url-authority
  remote hook can be wired into those repos without failing on pre-existing literals. The seed is the
  documented one-time grandfather (burn-down only); existing literals remain tracked debt under OMN-12807/12808.
  Build-time validation tooling only — no runtime node/handler path is touched.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1271"
    description: >-
      Binding receipt for omnibase_core PR #1271 (jonah/omn-13286-url-authority-fleet, base=dev): seeds
      the url-authority baseline for infra/clde/intel. Fail-closed proof captured: a planted https://
      literal makes the --all gate exit 1 naming the file; revert returns exit 0 (0 new violations, N
      grandfathered) per repo.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13286/dod-core-pr-1271/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13286 contract and the core
      PR #1271 binding, so the OCC receipt-gate eligibility check can resolve a PASS receipt for the evidence
      PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13286/dod-occ-pr-self/command.yaml
