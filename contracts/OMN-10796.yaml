---
schema_version: "1.0.0"
ticket_id: "OMN-10796"
title: "test(OMN-10796): proof-of-life E2E for delegation compliance loop"
summary: >
  Integration test that drives the full delegation pipeline with output_schema_key="delegation_result".
  Simulates LLM that returns malformed JSON on attempt 1 and valid JSON on attempt 2; asserts tokens_to_compliance
  equals sum of both attempts and compliance_attempts equals 2 on both ModelDelegationResult and ModelTaskDelegatedEvent
  (the omnidash projection-facing event). Test-only change in product repo — no source modifications.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1561 carries the OMN-10796 PoL E2E test."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10796/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "PoL E2E test passes — full pipeline through terminal event publication."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10796/dod-local-tests/command.yaml\""
  - id: "dod-occ-contract"
    description: "OCC contract exists for OMN-10796."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10796/dod-occ-contract/command.yaml\""
