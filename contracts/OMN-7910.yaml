---
schema_version: 1.0.0
ticket_id: OMN-7910
title: 'test(coverage): add tests for node_pattern_extraction_compute __main__ (omniinte'
summary: 'test(coverage): add tests for node_pattern_extraction_compute __main__ (omniinte'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: CI passes on merge PR
    command: gh pr checks
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: PR opened against main branch
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}
  - id: dod-002
    description: PR merged to main
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --json mergedAt -q .mergedAt --repo ${REPO}
  - id: dod-003
    description: Tests pass in CI
    source: generated
    checks:
      - check_type: command
        check_value: gh pr checks ${PR_NUMBER} --watch --repo ${REPO}
  - id: dod-004
    description: Pre-commit hooks clean
    source: generated
    checks:
      - check_type: command
        check_value: pre-commit run --all-files
  - id: dod-005
    description: Test coverage meets minimum threshold
    source: generated
    checks:
      - check_type: test_passes
        check_value: uv run pytest --cov --cov-fail-under=80
  - id: dod-intel-790-head
    description: 'Product PR #790 exists on dev at exact head ed5311b7.'
    source: manual
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=790; PRODUCT_REPO=OmniNode-ai/omniintelligence; gh pr view "$PRODUCT_PR_NUMBER"
          --repo "$PRODUCT_REPO" --json state,baseRefName,headRefOid --jq '.state == "OPEN" and .baseRefName
          == "dev" and .headRefOid == "ed5311b78676e175e91692f5edcf28396d03b1e6"' | grep -q true
  - id: dod-intel-790-test-only
    description: 'PR #790 changes only the dedicated node_pattern_extraction_compute CLI unit tests file.'
    source: manual
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=790; PRODUCT_REPO=OmniNode-ai/omniintelligence; diff_paths="$(gh
          pr diff "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --name-only)"; printf '%s\n' "$diff_paths"
          | grep -qx 'tests/unit/nodes/node_pattern_extraction_compute/test_main_cli.py'; test "$(printf
          '%s\n' "$diff_paths" | wc -l)" -eq 1
  - id: dod-intel-790-local-proof
    description: 'PR #790 body records 16 passed, 86% target-module coverage, and local quality gates.'
    source: manual
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=790; PRODUCT_REPO=OmniNode-ai/omniintelligence; gh pr view "$PRODUCT_PR_NUMBER"
          --repo "$PRODUCT_REPO" --json body --jq '.body' | grep -E '16 passed|86%|ruff|pre-commit'
  - id: dod-intel-790-no-deploy
    description: 'PR #790 is test-only and does not deploy, push images, restart runtime, mutate live
      infrastructure, mutate branch protection, or cut over required contexts.'
    source: manual
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=790; PRODUCT_REPO=OmniNode-ai/omniintelligence; diff_paths="$(gh
          pr diff "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --name-only)"; printf '%s\n' "$diff_paths"
          | grep -qx 'tests/unit/nodes/node_pattern_extraction_compute/test_main_cli.py'; test "$(printf
          '%s\n' "$diff_paths" | wc -l)" -eq 1
  - id: dod-supersede-002-product-open-790
    description: 'Append-only supersession for the generated merged-to-main check; product PR #790 is
      open on dev at the exact tested head.'
    evidence_artifact: supersedes_dod_evidence:dod-002
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=790; PRODUCT_REPO=OmniNode-ai/omniintelligence; gh pr view "$PRODUCT_PR_NUMBER"
          --repo "$PRODUCT_REPO" --json state,baseRefName,headRefOid --jq '.state == "OPEN" and .baseRefName
          == "dev" and .headRefOid == "ed5311b78676e175e91692f5edcf28396d03b1e6"' | grep -q true
  - id: dod-supersede-003-occ-cascade-790
    description: 'Append-only supersession for the generated CI watch check; product PR #790 has only
      OCC/receipt cascade failures before this companion.'
    evidence_artifact: supersedes_dod_evidence:dod-003
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=790; PRODUCT_REPO=OmniNode-ai/omniintelligence; gh pr view "$PRODUCT_PR_NUMBER"
          --repo "$PRODUCT_REPO" --json statusCheckRollup --jq '[.statusCheckRollup[] | select((.__typename
          == "CheckRun" and .status == "COMPLETED" and (.conclusion | IN("SUCCESS","SKIPPED","NEUTRAL")
          | not)) or (.__typename == "StatusContext" and (.state | IN("SUCCESS","PENDING") | not))) |
          select((((.name // .context) + " " + (.workflowName // "")) | test("occ-preflight|Receipt Gate|CI
          Summary|Quality Gate|Security Gate|Tests Gate|Hostile Review Gate|Omni Standards Gate")) | not)]
          | length == 0' | grep -q true
  - id: dod-supersede-004-product-precommit-790
    description: 'Append-only supersession for the generated all-files pre-commit check; PR #790 body
      records pre-commit and ruff proof for the test-only change.'
    evidence_artifact: supersedes_dod_evidence:dod-004
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=790; PRODUCT_REPO=OmniNode-ai/omniintelligence; gh pr view "$PRODUCT_PR_NUMBER"
          --repo "$PRODUCT_REPO" --json body --jq '.body' | grep -E 'pre-commit|ruff'
  - id: dod-supersede-005-product-coverage-790
    description: 'Append-only supersession for the generated coverage command; PR #790 body records targeted
      node_pattern_extraction_compute coverage proof.'
    evidence_artifact: supersedes_dod_evidence:dod-005
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=790; PRODUCT_REPO=OmniNode-ai/omniintelligence; gh pr view "$PRODUCT_PR_NUMBER"
          --repo "$PRODUCT_REPO" --json body --jq '.body' | grep -E 'node_pattern_extraction_compute|16
          passed|86%'
  - id: occ-self-bind-pr-4328
    description: 'OCC PR #4328 exists and carries OMN-7910 evidence for omniintelligence PR #790.'
    source: manual
    checks:
      - check_type: command
        check_value: PR_NUMBER=4328; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state,headRefName
