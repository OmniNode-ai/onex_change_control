---
schema_version: "1.0.0"
ticket_id: "OMN-13126"
title: "docs(OMN-13126): doc-vs-impl reconciliation batch — skill docs, stale stub markers, phantom flags,
  eval head topic, node/run-node verbs"
summary: >-
  Docs-only reconciliation batch for epic OMN-13119 (drift-rem-1). Fixes inaccurate SKILL.md descriptions
  vs actual node implementations: trims integration_sweep surface table to 3 implemented surfaces, removes
  phantom CLI flags from integration_sweep, replaces golden_chain_sweep publish/poll harness with pure-validator
  description matching the actual COMPUTE node, fixes evaluation chain head-topic (run-evaluated.v1 ->
  session-outcome.v1), drops stale (stub)/migration_status markers on 4 implemented skills, and standardizes
  node-invocation verbs (onex node vs onex run-node).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "docs"
    description: "All changes are docs-only (SKILL.md, prompt.md, contract.yaml, metadata.yaml); no Python
      source changed"
    command: "git diff --name-only HEAD~1"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-docs-verified"
    description: >-
      Each doc fix was verified against actual implementation before editing: surface_probes.py confirmed
      RUNTIME_HEALTH/CONTAINER_HEALTH/GITHUB_CI only; handler_golden_chain_sweep.py confirmed pure compute
      on pre-collected projected_rows; all 4 backing nodes (node_dod_sweep_orchestrator, node_env_parity_compute,
      node_pr_watch_orchestrator, node_integration_sweep_orchestrator) confirmed present in omnimarket/src/omnimarket/nodes/.
      Pre-commit on both worktrees passes all checks (only pre-existing infrastructure failures on dev
      branch).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-13126/dod-docs-verified/command.yaml" && grep
          -q '^ticket_id: "OMN-13126"$' "drift/dod_receipts/OMN-13126/dod-docs-verified/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      This onex_change_control OCC PR carries the OMN-13126 contract and receipt. Evidence-Source: OCC#<this-pr>
      / Evidence-Ticket: OMN-13126 (self-binding eligibility).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-13126/dod-occ-pr-self/command.yaml" && grep
          -q '^ticket_id: "OMN-13126"$' "drift/dod_receipts/OMN-13126/dod-occ-pr-self/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for the omnimarket node contract/metadata edits. The PR touches runtime-gated
      node contract paths, but the change is declarative documentation only: no handler code, emitter,
      topic, envelope shape, runtime service, container image, or .201 state was changed. No live deploy
      or restart was performed; normal post-merge rebuild/promotion can carry the metadata.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'No live deploy' "drift/dod_receipts/OMN-13126/dod-deploy-assessment/command.yaml" &&
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-13126/dod-deploy-assessment/command.yaml"
