---
schema_version: "1.0.0"
ticket_id: "OMN-13306"
title: "Port routing authority and delegation profile validators into omnibase_core"
summary: >-
  Bind omnibase_core PR #1267 to central OCC evidence for porting the routing-authority validator into
  a canonical COMPUTE node and wiring the delegation-profile validator as an exported pre-commit hook.
  The core PR adds node_routing_authority_check_compute, an EFFECT-boundary CLI wrapper, exported hook
  metadata, and focused unit coverage including fail-closed planted violation proof and a clean-tree complement.
  The implementation is static validation code only and does not require a live deploy, runtime restart,
  or service mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1267"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1267 (jonah/omn-13306-routing-authority-compute-handler,
      base=dev). The PR carries the OMN-13306 routing-authority COMPUTE handler, validator CLI wrapper,
      exported pre-commit hooks, and validator_delegation_profile CLI wiring at the cited head commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13306/dod-omnibase-core-pr-1267/command.yaml
  - id: "dod-routing-authority-tests"
    description: >-
      Focused local proof for the routing-authority validator port. The omnibase_core PR branch passed
      tests/validation/test_validator_routing_authority.py with 22 tests covering the COMPUTE handler,
      fail-closed planted violation behavior, and the green-on-clean-tree complement.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13306/dod-routing-authority-tests/command.yaml
  - id: "dod-static-validator-deploy-assessment"
    description: >-
      Deploy-gate evidence for omnibase_core PR #1267. The touched runtime paths add a pure static COMPUTE
      validator and hook export only; no live service deploy, runtime restart, Kafka topic mutation, or
      database migration is required before merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13306/dod-static-validator-deploy-assessment/command.yaml
          # deploy: static validator only; no live deploy or runtime restart required
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13306 contract and omnibase_core
      PR #1267 binding. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check
      can resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13306/dod-occ-pr-self/command.yaml
