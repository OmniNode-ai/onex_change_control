---
schema_version: "1.0.0"
ticket_id: "OMN-15790"
title: "Autobind OCC evidence for OMN-15790"
summary: >
  OCC contract for OmniNode-ai/omninode_infra PR #839 — codifies a real destination (auto-created SNS
  topic + EventBridge target) for the OMN-15690 MSK security-group drift alarm, which was live-applied
  with Targets: [].
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #839 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-839"
    description: "PR #839 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 839 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 839 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-deploy-assessment"
    description: >
      Deploy-scope DoD: PR #839 is Terraform-only, plan/validate-verified, and explicitly does NOT apply
      — no runtime deploy surface is touched.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 839 --repo OmniNode-ai/omninode_infra --name-only | grep -ciE '\\.tf$|\\.tfvars$'"
