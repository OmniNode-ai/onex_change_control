---
schema_version: "1.0.0"
ticket_id: "OMN-14805"
title: "Autobind OCC evidence for OMN-14805"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2349."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2349 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2349"
    description: "PR #2349 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2349-ci"
    description: "PR #2349 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4446"
    description: "OCC companion PR #4446 — self-bind for OMN-14805 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnibase-infra-pr-2349-deploy-scope"
    description: >-
      Deploy-gate scope for omnibase_infra PR #2349. The change touches src/omnibase_infra/nodes/node_runtime_source_attestor_effect/handlers/handler_source_attestation.py
      and src/omnibase_infra/nodes/node_runtime_source_attestor_effect/node.py, runtime node paths, but
      it only restores canonical dispatch entrypoints for the existing source attestor effect. No live
      runtime deployment is performed by this PR; the required deploy evidence is the merge-time CI/OCC
      gate suite plus post-merge availability of the handler path on dev.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          printf '%s\n' 'deploy scope assessed for omnibase_infra#2349 runtime source attestor paths;
          no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'
  - id: "occ-self-bind-pr-4447"
    description: "OCC deploy-scope repair PR #4447 — self-bind for OMN-14805."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2356"
    description: "PR #2356 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName,headRefOid"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2356-ci"
    description: "PR #2356 on OmniNode-ai/omnibase_infra — product diff scope check."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
