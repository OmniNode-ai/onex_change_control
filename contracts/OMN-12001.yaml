---
schema_version: "1.0.0"
ticket_id: "OMN-12001"
title: "Add event_model block to node_delegate_skill_orchestrator handler_routing"
summary: >
  Central OCC evidence for omnimarket PR #848, which adds the event_model block to the handler_routing.handlers[]
  entry in node_delegate_skill_orchestrator/contract.yaml. Without this block _parse_handler_routing()
  sets event_model=None and Pydantic deserialization is skipped, causing HandlerDelegateSkill to receive
  a raw dict and crash with AttributeError.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-848"
    description: "omnimarket PR #848 adds the event_model block to node_delegate_skill_orchestrator contract.yaml."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'ModelDelegateSkillRequest' /Users/jonah/Code/omni_worktrees/OMN-12001/omnimarket/src/omnimarket/nodes/node_delegate_skill_orchestrator/contract.yaml
          && echo 'deploy event_model present'"
  - id: "dod-deploy-assessment"
    description: >
      No live deploy required for the contract.yaml change. The fix updates the auto-wiring discovery
      configuration; the runtime will pick it up on next restart.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy: no live deploy required; auto-wiring reads contract.yaml at startup'"
