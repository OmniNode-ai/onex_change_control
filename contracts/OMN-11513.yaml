---
schema_version: "1.0.0"
ticket_id: "OMN-11513"
title: "May Monthly Integration Testing Plan (May 1-22)"
summary: >
  Captures the May 1-22 monthly integration testing plan and bounded evidence bundle, including runtime
  lane identity, stability runtime delegation proof, command-topic ownership classification, SEA demo
  acceptance, and known blocked implementation surfaces.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omni_home PR #145 carries the plan and evidence bundle."
    command: "See drift/dod_receipts/OMN-11513/dod-omni-home-pr-145/command.yaml"
  - kind: "tests"
    description: "onex_change_control cross-repo contract introspection test no longer fails on missing
      asyncpg."
    command: "See drift/dod_receipts/OMN-11513/dod-occ-cross-repo-test/command.yaml"
  - kind: "manual"
    description: ".201 stability runtime exposes the introspection manifest and completed the synthetic
      delegation proof."
    command: "See drift/dod_receipts/OMN-11513/dod-stability-runtime-proof/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omni-home-pr-145"
    description: "omni_home PR #145 carries the OMN-11513 plan and evidence bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11513/dod-omni-home-pr-145/command.yaml"
  - id: "dod-occ-cross-repo-test"
    description: "OCC cross-repo contract introspection test no longer fails on the missing asyncpg dependency."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11513/dod-occ-cross-repo-test/command.yaml"
  - id: "dod-stability-runtime-proof"
    description: "Stability runtime manifest and synthetic delegation proof were captured for OMN-11513."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11513/dod-stability-runtime-proof/command.yaml"
