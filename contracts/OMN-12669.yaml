---
schema_version: "1.0.0"
ticket_id: "OMN-12669"
title: "Bind OCC evidence for omnibase_infra checkout action bump"
summary: >
  Central OCC evidence for omnibase_infra PR #1862, a ticketed replacement for Dependabot PR #1851. The
  product PR bumps actions/checkout from v4 or an older pinned v6 SHA to v6/current pinned v6 SHA in
  omnibase_infra workflow files.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #1862 is open against dev at the recorded ticketed head."
    command: "gh pr view 1862 --repo OmniNode-ai/omnibase_infra --json state,headRefOid,baseRefName"
  - kind: "ci"
    description: "All visible non-Receipt-Gate checks on omnibase_infra PR #1862 are SUCCESS or SKIPPED."
    command: "gh pr checks 1862 --repo OmniNode-ai/omnibase_infra --json name,state"
  - kind: "manual"
    description: "The OCC PR carrying this central evidence is open and bound to OMN-12669."
    command: "gh pr view 2146 --repo OmniNode-ai/onex_change_control --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1862-head"
    description: "omnibase_infra PR #1862 is open against dev at the recorded ticketed checkout head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12669/dod-infra-pr-1862-head/command.yaml"
          && grep -q '^pr_number: 1862$' "drift/dod_receipts/OMN-12669/dod-infra-pr-1862-head/command.yaml"
          && grep -q '^commit_sha: "23ea3bc0bee46f26f6c2b8fffe0040f7dbe9a5d1"$' "drift/dod_receipts/OMN-12669/dod-infra-pr-1862-head/command.yaml"
  - id: "dod-infra-pr-1862-non-receipt-checks"
    description: "All visible non-Receipt-Gate checks on omnibase_infra PR #1862 are green or skipped."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12669/dod-infra-pr-1862-non-receipt-checks/command.yaml"
          && grep -q 'all visible non-Receipt-Gate checks are SUCCESS or SKIPPED' "drift/dod_receipts/OMN-12669/dod-infra-pr-1862-non-receipt-checks/command.yaml"
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR #2146 carries the OMN-12669 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12669/dod-occ-pr-self/command.yaml" && grep
          -q '^pr_number: 2146$' "drift/dod_receipts/OMN-12669/dod-occ-pr-self/command.yaml"
