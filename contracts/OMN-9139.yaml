---
schema_version: 1.0.0
ticket_id: OMN-9139
title: Deployed artifact source hash attestation
summary: Build-time source hash attestation added for runtime artifacts and release wheel stamping.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - events
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Wave worker local verification evidence is captured in central OCC receipt.
    command: uv run pytest tests/unit/scripts/test_attest_source_hash.py tests/unit/docker/test_source_hash_attestation.py
      -q
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-wave-local-evidence
    description: Wave worker local verification or readiness evidence is captured as a canonical PASS
      receipt.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-wave-local-evidence/command.yaml"
          && grep -q '^ticket_id: OMN-9139$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-wave-local-evidence/command.yaml"
          && grep -q 'RUNTIME_SOURCE_HASH validation' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-wave-local-evidence/command.yaml"
  - id: dod-omnibase-infra-deploy-attestation
    description: Runtime image built with RUNTIME_SOURCE_HASH baked in via Docker build-arg. Verifiable
      via docker exec on the deployed container reading the env var stamped at image build time (ARG RUNTIME_SOURCE_HASH
      → ENV RUNTIME_SOURCE_HASH).
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-omnibase-infra-deploy-attestation/command.yaml"
          && grep -q '^pr_number: 1662$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-omnibase-infra-deploy-attestation/command.yaml"
          && grep -q 'docker exec omnibase-runtime printenv RUNTIME_SOURCE_HASH' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-omnibase-infra-deploy-attestation/command.yaml"
  - id: dod-pr-1662-head-888616fc
    description: omnibase_infra PR 1662 head refresh after merge-sweep repair for the published omnibase_core
      0.41.0 projection contract compatibility break.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-pr-1662-head-888616fc/command.yaml"
          && grep -q '^pr_number: 1662$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-pr-1662-head-888616fc/command.yaml"
          && grep -q '^commit_sha: 888616fc95df739014d571cb6cd985a00b7fc7a0$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-pr-1662-head-888616fc/command.yaml"
  - id: dod-pr-1662-head-ae94f517
    description: omnibase_infra PR 1662 final head after CodeRabbit source-hash exact-match repair.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-pr-1662-head-ae94f517/command.yaml"
          && grep -q '^pr_number: 1662$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-pr-1662-head-ae94f517/command.yaml"
          && grep -q '^commit_sha: ae94f5179d54b913eb44414cc2bd23f4de094f16$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-pr-1662-head-ae94f517/command.yaml"
  - id: dod-pr-1662-head-efc3f6f1
    description: omnibase_infra PR 1662 head after entry point and runtime-boot publisher contract repair.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-pr-1662-head-efc3f6f1/command.yaml"
          && grep -q '^pr_number: 1662$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-pr-1662-head-efc3f6f1/command.yaml"
          && grep -q '^commit_sha: efc3f6f1f2ba52de581a008e6851c4a5df97110f$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-pr-1662-head-efc3f6f1/command.yaml"
  - id: dod-occ-pr
    description: onex_change_control PR 1158 carries the central OMN-9139 receipt and contract repair
      bundle.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 1158$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-occ-pr/command.yaml"
          && grep -q 'OMN-9139 central receipt and contract repair' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-occ-pr/command.yaml"
  - id: dod-occ-pr-1191
    description: onex_change_control PR 1191 carries the refreshed OMN-9139 PR-head receipt for downstream
      omnibase_infra PR 1662.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-occ-pr-1191/command.yaml"
          && grep -q '^pr_number: 1191$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-occ-pr-1191/command.yaml"
  - id: dod-occ-pr-1194
    description: onex_change_control PR 1194 carries the refreshed OMN-9139 PR-head receipt for downstream
      omnibase_infra PR 1662 head efc3f6f1.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-occ-pr-1194/command.yaml"
          && grep -q '^pr_number: 1194$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9139/dod-occ-pr-1194/command.yaml"
