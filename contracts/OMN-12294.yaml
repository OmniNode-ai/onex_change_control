---
schema_version: "1.0.0"
ticket_id: "OMN-12294"
title: "Eliminate DelegationIntentBridge and route delegation intents through Kafka"
summary: >
  Bind central evidence for the OMN-12294 stage 2 cross-repo delegation refactor: omnimarket PR #977 removed
  the in-process DelegationIntentBridge and omnibase_infra PR #1779 removes bridge-backed runtime registration
  and injects the Kafka delegation dispatch port directly.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
  - protocols
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-977"
    description: "omnimarket PR #977 merged the stage 2 DelegationIntentBridge removal."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-omnimarket-pr-977/command.yaml\"
          && grep -q '^pr_number: 977$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-omnimarket-pr-977/command.yaml\""
  - id: "dod-omnibase-infra-pr-1779"
    description: "omnibase_infra PR #1779 carries the runtime wiring half of the stage 2 bridge removal."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-omnibase-infra-pr-1779/command.yaml\"
          && grep -q '^pr_number: 1779$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-omnibase-infra-pr-1779/command.yaml\""
  - id: "dod-infra-allowlist-validation"
    description: "Infra node handler ownership allowlist passes after removing the stale deleted-handler
      entry."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-infra-allowlist-validation/command.yaml\"
          && grep -q 'allowlist is satisfied' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-infra-allowlist-validation/command.yaml\""
  - id: "dod-bridge-symbol-grep"
    description: "Bridge-backed delegation symbols are absent from omnibase_infra src after #1779."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-bridge-symbol-grep/command.yaml\"
          && grep -q 'bridge grep clean' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-bridge-symbol-grep/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Deploy gate assessment for runtime wiring paths touched by omnibase_infra PR #1779."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-deploy-assessment/command.yaml\"
          && grep -qi 'deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-1833"
    description: "onex_change_control PR #1833 carries the central OMN-12294 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-occ-pr-1833/command.yaml\"
          && grep -q '^pr_number: 1833$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-occ-pr-1833/command.yaml\""
  - id: "dod-omnimarket-pr-978"
    description: "omnimarket PR #978 promotes the legacy follow-up branch into a compliant worktree and
      PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-omnimarket-pr-978/command.yaml\"
          && grep -q '^pr_number: 978$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-omnimarket-pr-978/command.yaml\""
  - id: "dod-omnimarket-pr-978-validation"
    description: "omnimarket PR #978 focused delegation tests, lint, diff check, and dep-health baseline
      validation passed locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-omnimarket-pr-978-validation/command.yaml\"
          && grep -q 'baseline_delta: 0' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-omnimarket-pr-978-validation/command.yaml\""
  - id: "dod-occ-pr-1834"
    description: "onex_change_control PR #1834 carries the central receipts for omnimarket PR #978."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-occ-pr-1834/command.yaml\"
          && grep -q '^pr_number: 1834$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-occ-pr-1834/command.yaml\""
  - id: "dod-omnimarket-pr-979"
    description: "omnimarket PR #979 promotes the remaining event_model and published_events runtime autopublish
      repair from the legacy branch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-omnimarket-pr-979/command.yaml\"
          && grep -q '^pr_number: 979$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-omnimarket-pr-979/command.yaml\""
  - id: "dod-omnimarket-pr-979-validation"
    description: "omnimarket PR #979 focused delegation tests, contract tests, strict typecheck, lint,
      and diff check passed locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-omnimarket-pr-979-validation/command.yaml\"
          && grep -q 'Success: no issues found' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-omnimarket-pr-979-validation/command.yaml\""
  - id: "dod-occ-pr-1835"
    description: "onex_change_control PR #1835 carries the central receipts for omnimarket PR #979."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-occ-pr-1835/command.yaml\"
          && grep -q '^pr_number: 1835$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12294/dod-occ-pr-1835/command.yaml\""
