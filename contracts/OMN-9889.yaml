---
schema_version: "1.0.0"
ticket_id: "OMN-9889"
title: "feat(OMN-9889): backfill runtime ownership profiles"
summary: >
  OCC evidence for omnimarket PR #649. Backfills runtime_profiles declarations across omnimarket command/effect
  contracts. Adds validation test (test_effectful_command_consumers_declare_effects_runtime_profile) to
  assert all EFFECT nodes subscribing to cmd topics declare the effects runtime profile. Also adds handler
  block to node_build_dispatch_effect contract (required by strict drift gate). Internal contract metadata
  change — no new Kafka topics, no schema changes, no API surface changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Runtime profile validation tests pass"
    command: "See drift/dod_receipts/OMN-9889/dod-unit-tests/command.yaml"
  - kind: "ci"
    description: "Deploy assessment — contract metadata backfill, no live deploy required"
    command: "See drift/dod_receipts/OMN-9889/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "Runtime profile validation tests pass"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'PASS' drift/dod_receipts/OMN-9889/dod-unit-tests/command.yaml && echo 'OMN-9889 unit-tests
          receipt present'
  - id: "dod-deploy"
    description: >-
      Deploy assessment recorded — contract metadata backfill only, no live deploy or runtime restart
      performed.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'status: PASS' drift/dod_receipts/OMN-9889/dod-deploy/command.yaml && grep -q 'evidence_item_id:
          "dod-deploy"' drift/dod_receipts/OMN-9889/dod-deploy/command.yaml && echo 'OMN-9889 deploy receipt
          present and PASS'
  - id: "dod-omnimarket-pr-649"
    description: "omnimarket PR #649 carries all runtime ownership profile backfill changes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'PASS' drift/dod_receipts/OMN-9889/dod-omnimarket-pr-649/command.yaml && grep -q 'pr_number:
          649' drift/dod_receipts/OMN-9889/dod-omnimarket-pr-649/command.yaml
