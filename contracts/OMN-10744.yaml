---
schema_version: "1.0.0"
ticket_id: "OMN-10744"
title: "Extract runtime lifecycle EventBusKafka construction to lifecycle_bootstrapper factory"
summary: >
  Binds omniclaude PR #1553, which removes the direct EventBusKafka() instantiation (stopgap bus-ok annotation)
  from src/omniclaude/runtime/lifecycle.py. A new lifecycle_bootstrapper.py factory is introduced, allowlisted
  by the _bootstrapper.py suffix pattern in the AST validator. The change is non-deploy: it refactors
  the object construction site; no runtime compose files, Dockerfiles, or deploy scripts are changed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1553-head"
    description: "omniclaude PR #1553 carries the runtime lifecycle bus DI refactor at the recorded head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10744/dod-pr-1553-head/command.yaml\""
  - id: "dod-no-direct-bus"
    description: "Direct EventBus instantiation validator passes after refactor."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10744/dod-no-direct-bus/command.yaml\""
  - id: "dod-deploy-scope"
    description: "Confirm no deploy action required: refactor only changes Python object construction,
      not runtime compose or Dockerfile. No deploy triggered."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10744/dod-deploy-scope/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10744/dod-occ-pr/command.yaml\""
