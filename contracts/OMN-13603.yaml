---
schema_version: "1.0.0"
ticket_id: "OMN-13603"
title: "wire remaining protocol-typed handler dependencies after handler-prep quarantine sweep"
summary: >-
  Follow-up ticket for the three remaining handler-prep quarantine entries intentionally carved out of
  OMN-13551. OMN-13551 removes helper functions/classes that were incorrectly declared as runtime message
  handlers and leaves only genuinely shaped handlers with unwired protocol-typed external dependencies
  (project tracker, generated-tool registry, and task dispatcher). This contract exists so PRs that document
  the OMN-13603 carve-out have central OCC coverage without claiming that OMN-13603 is implemented by
  the OMN-13551 product PR. The actual adapter wiring and live dev-lane readback remain owned by the OMN-13603
  implementation work.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for the OMN-13603 reference in omnimarket PR #1429. That PR does not implement
      OMN-13603; it only documents the remaining three unwired-dependency handler entries as a guarded
      carve-out while fixing the helper-misclassification cohort under OMN-13551. No live deploy, runtime
      restart, topic mutation, or .201 mutation is performed for OMN-13603 by PR #1429.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13603/dod-deploy-assessment/command.yaml &&
          grep -q '^pr_number: 1429$' drift/dod_receipts/OMN-13603/dod-deploy-assessment/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-13603/dod-deploy-assessment/command.yaml
