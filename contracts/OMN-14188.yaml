---
schema_version: "1.0.0"
ticket_id: "OMN-14188"
title: "feat(OMN-14188): add OCC PR stamp parser renderer"
summary: >-
  omnibase_core code-only change (Piece 2/5 of epic OMN-14180). Adds a pure parser/renderer for canonical
  PR OCC metadata stamps on top of the OMN-14187 typed models. The parser decomposes PR bodies into ModelPrOccMetadataStamp,
  preserving human-authored sections while marking renderer-owned Evidence metadata sections. The renderer
  emits one canonical Evidence-Ticket/Evidence-Source block and replaces stale owned blocks idempotently.
  It covers OCC PR evidence sources, commit-SHA sources, Evidence-Ticket lines, skip-token allowlist markers,
  and no-I/O round-trips. No runtime services, live deploy, restart, branch-protection mutation, or bypass.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1408"
    description: >-
      omnibase_core PR #1408 targets dev at head 1f398163c5c017ec0c148e269bab64a74f10678b and adds src/omnibase_core/validation/pr_occ_metadata_stamp.py
      plus focused unit tests. Local verification passed ruff on the touched files and 50 pytest cases
      across the new parser/renderer tests plus the existing receipt-gate evidence-source regression suite.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14188/dod-omnibase_core-pr-1408/command.yaml
          && grep -q '^pr_number: 1408$' drift/dod_receipts/OMN-14188/dod-omnibase_core-pr-1408/command.yaml
          && grep -q '^commit_sha: 1f398163c5c017ec0c148e269bab64a74f10678b$' drift/dod_receipts/OMN-14188/dod-omnibase_core-pr-1408/command.yaml
  - id: "dod-no-live-deploy"
    description: >-
      Scope evidence: OMN-14188 adds pure parser/renderer code and unit tests to omnibase_core. It performs
      no live deploy, no runtime restart, no branch-protection mutation, and no bypass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14188/dod-no-live-deploy/command.yaml && grep
          -qi 'no live deploy' drift/dod_receipts/OMN-14188/dod-no-live-deploy/command.yaml
