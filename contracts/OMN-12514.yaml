---
schema_version: "1.0.0"
ticket_id: "OMN-12514"
title: "SEA: route GenerationConsumer inference through contract-native bus"
summary: >-
  Bind central evidence for onex-self-extending-agent PR #184, which removes the remaining unwired live
  GenerationConsumer inference surface by requiring construction paths to inject or load a contract-native
  inference bus before _call_llm can publish model inference intent.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-184"
    description: "Bind onex-self-extending-agent PR #184 head and branch for the OMN-12514 fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-sea-pr-184/command.yaml"
          && grep -q '^pr_number: 184$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-sea-pr-184/command.yaml"
          && grep -q '^commit_sha: "3aeda2f3ebc7dcb5bcfaa9eef64632b169013a43"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-sea-pr-184/command.yaml"
  - id: "dod-live-inference-wiring"
    description: "Prove no src httpx.post surface remains and live GenerationConsumer paths load or inject
      the inference bus."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-live-inference-wiring/command.yaml"
          && grep -q 'httpx.post matches: 0' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-live-inference-wiring/command.yaml"
          && grep -q 'load_required_inference_bus_from_env' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-live-inference-wiring/command.yaml"
  - id: "dod-local-validation"
    description: "Focused unit, full unit, lint, format, mypy, and security grep validation passed locally."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-local-validation/command.yaml"
          && grep -q '1189 passed, 1 skipped' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-local-validation/command.yaml"
          && grep -q '56 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-local-validation/command.yaml"
          && grep -q 'Success: no issues found in 156 source files' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-local-validation/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12514 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-occ-pr-self/command.yaml"
          && grep -q '^pr_number: 1985$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-occ-pr-self/command.yaml"
  - id: "dod-occ-pr-yamlfmt"
    description: "Self-binding receipt for the OCC follow-up PR carrying OMN-12514 yamlfmt repair."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-occ-pr-yamlfmt/command.yaml"
          && grep -q '^pr_number: 1987$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12514/dod-occ-pr-yamlfmt/command.yaml"
