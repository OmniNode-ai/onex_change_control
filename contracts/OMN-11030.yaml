---
schema_version: "1.0.0"
ticket_id: "OMN-11030"
title: "Complete dependency health sweep epic"
summary: >-
  Binds omnimarket PR #644, which implements the dependency health sweep node, analysis engine, CI/pre-commit
  enforcement, projection node, and proof-of-life evidence bundle for the OMN-11030 epic.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnimarket PR #644 carries the dependency health sweep epic implementation."
    command: "gh pr view 644 --repo OmniNode-ai/omnimarket --json number,state,headRefOid,title"
  - kind: "tests"
    description: "Focused regression checks for the CI failures introduced by the epic pass locally."
    command: "uv run pytest tests/nodes/test_dep_health_handler_stub.py::test_handler_handle_returns_clean_status_for_empty_roots
      tests/unit/structure/test_no_hardcoded_literals.py::test_no_hardcoded_literals_in_tests -q"
  - kind: "manual"
    description: "The omnimarket PR includes the proof-of-life evidence bundle for the dependency health
      sweep."
    command: "test -f docs/evidence/dep-health-sweep/proof-of-life-001/proof_summary.md"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-644"
    description: "omnimarket PR #644 is open with the final OMN-11030 dependency health sweep implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11030/dod-omnimarket-pr-644/command.yaml
          && grep -q '^pr_number: 644$' drift/dod_receipts/OMN-11030/dod-omnimarket-pr-644/command.yaml
          && grep -q '^commit_sha: \"aae8dfedb799c6d642fb423f746914888f0de158\"$' drift/dod_receipts/OMN-11030/dod-omnimarket-pr-644/command.yaml"
  - id: "dod-focused-ci-fixes"
    description: "Focused tests for the observed CI failures pass after the stabilization patch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11030/dod-focused-ci-fixes/command.yaml
          && grep -q '2 passed' drift/dod_receipts/OMN-11030/dod-focused-ci-fixes/command.yaml"
  - id: "dod-proof-evidence-bundle"
    description: "The dependency health proof-of-life evidence bundle is present in PR #644."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11030/dod-proof-evidence-bundle/command.yaml
          && grep -q 'proof-of-life-001' drift/dod_receipts/OMN-11030/dod-proof-evidence-bundle/command.yaml"
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carries the central OMN-11030 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11030/dod-occ-pr-self/command.yaml
          && grep -q '^pr_number: 1028$' drift/dod_receipts/OMN-11030/dod-occ-pr-self/command.yaml"
