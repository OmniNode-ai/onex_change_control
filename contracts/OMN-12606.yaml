---
schema_version: "1.0.0"
ticket_id: "OMN-12606"
title: "Materialize fresh terminal delegation event into delegation_events without operator backfill"
summary: >-
  Bind central evidence for omnimarket PR #1020. The reducer/orchestrator delegation completion path now
  materializes a fresh terminal delegation event into delegation_events directly — a single reducer upsert,
  no manual operator backfill (May 31 finding: a fresh stability row had required operator backfill from
  observed runtime bus evidence because the reducer did not materialize the terminal row itself). Every
  reducer-materialized row is stamped with projection_version and reducer_version provenance (OMN-12488
  acceptance-extension) via both the sync handler (HandlerProjectionDelegation.project_delegate_skill_terminal)
  and the async runner (DelegationProjectionRunner._upsert_delegate_skill_projection_row). Migration 0011_delegation_event_projection_versions.sql
  adds the two TEXT columns to delegation_events. Relates to OMN-12026 (terminal-event-materializes-row)
  and OMN-11187 (reducer emits terminal event).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-no-backfill-materialization-tests"
    description: "The node_projection_delegation no-backfill materialization suite passes locally on the
      implementation branch: a fresh terminal delegation event materializes into delegation_events via
      a single reducer upsert with no operator backfill, the materialized row carries projection_version
      and reducer_version (sync handler path), the async runner upsert emits the version columns and values
      with a single DB write, and migration 0011 declares the columns."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12606/dod-no-backfill-materialization-tests/command.yaml"
          && grep -q '62 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12606/dod-no-backfill-materialization-tests/command.yaml"
          && grep -q 'TestNoBackfillMaterialization' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12606/dod-no-backfill-materialization-tests/command.yaml"
  - id: "dod-lint-typecheck"
    description: "ruff check/format and mypy --strict pass for the changed node_projection_delegation
      handlers and the delegation terminal projection model on the implementation branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12606/dod-lint-typecheck/command.yaml"
          && grep -q 'no issues found' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12606/dod-lint-typecheck/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12606 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12606/dod-occ-pr-self/command.yaml"
