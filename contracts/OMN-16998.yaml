---
schema_version: "1.0.0"
ticket_id: "OMN-16998"
title: "Autobind OCC evidence for OMN-16998"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_core PR #1632.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1632 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1632 on OmniNode-ai/omnibase_core, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_core`.
    command: "uv run pytest tests/unit/enums/test_enum_delegation_terminal_failure_cause.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_core-pr-1632"
    description: "PR #1632 on OmniNode-ai/omnibase_core — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_core/contents/tests/unit/enums/test_enum_delegation_terminal_failure_cause.py?ref=96878986690436d3c39ea6f8b8af3b91263647a2 --jq '.content' | base64 -d | grep -c 'class it'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_core/pulls/1632/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_core/enums/enum_delegation_terminal_failure_cause.py'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #1632 on OmniNode-ai/omnibase_core — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/enums/test_enum_delegation_terminal_failure_cause.py -q"
        cwd: "${OMNI_HOME}/omnibase_core"
  - id: "occ-self-bind-pr-7923"
    description: "Binds OMN-16998 to OCC companion PR #7923 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7923 --repo OmniNode-ai/onex_change_control --json number,state"
