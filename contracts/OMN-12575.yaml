---
schema_version: "1.0.0"
ticket_id: "OMN-12575"
title: "Phase 1: Confirm live deployment baseline and capture evidence"
summary: >-
  Bind central OCC evidence for omnibase_infra PR #1828, which captures the read-only live runtime deployment
  baseline for OMN-12575 under docs/tracking/ without source, runtime, deploy, restart, or .201 mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1828"
    description: "Bind omnibase_infra PR #1828 head and the tracked baseline evidence document."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12575/dod-omnibase-infra-pr-1828/command.yaml"
          && grep -q '^pr_number: 1828$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12575/dod-omnibase-infra-pr-1828/command.yaml"
          && grep -q '^commit_sha: "d7151fd27bb27041cc9746593faf60793f66232f"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12575/dod-omnibase-infra-pr-1828/command.yaml"
          && grep -q 'docs/tracking/2026-06-01-omn-12575-runtime-deployment-baseline.md' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12575/dod-omnibase-infra-pr-1828/command.yaml"
  - id: "dod-docs-validation"
    description: "Docs-only local validation for the OMN-12575 baseline evidence passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12575/dod-docs-validation/command.yaml"
          && grep -q 'pre-commit run --files docs/tracking/2026-06-01-omn-12575-runtime-deployment-baseline.md'
          "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12575/dod-docs-validation/command.yaml" && grep -q
          'ONEX Markdown Link Validation.*Passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12575/dod-docs-validation/command.yaml"
          && grep -q 'Reject skip-gate bypass tokens.*Passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12575/dod-docs-validation/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12575 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12575/dod-occ-pr-self/command.yaml"
