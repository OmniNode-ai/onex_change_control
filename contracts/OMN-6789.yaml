---
schema_version: "1.0.0"
ticket_id: OMN-6789
title: Add data_flow_sweep coverage for LLM event paths
summary: "Add data_flow_sweep coverage for LLM event paths"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "OCC PR #1175 is bound to the OMN-6789 delivery PR evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6789/dod-001/command.yaml\"
          && grep -q '^pr_number: 1175$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6789/dod-001/command.yaml\""
  - id: "dod-002"
    description: "Delivery PR #1672 targets main and is queued for merge."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6789/dod-002/command.yaml\"
          && grep -q '^pr_number: 1175$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6789/dod-002/command.yaml\""
  - id: "dod-003"
    description: "LLM dispatch wiring integration tests pass for the delivery branch."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6789/dod-003/command.yaml\"
          && grep -q '^pr_number: 1175$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6789/dod-003/command.yaml\""
  - id: "dod-004"
    description: "Pre-commit hooks pass for the LLM dispatch wiring test file."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6789/dod-004/command.yaml\"
          && grep -q '^pr_number: 1175$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6789/dod-004/command.yaml\""
