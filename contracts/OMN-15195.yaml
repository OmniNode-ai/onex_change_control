---
schema_version: "1.0.0"
ticket_id: "OMN-15195"
title: "Autobind OCC evidence for OMN-15195"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2477."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2477 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2477"
    description: "PR #2477 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2477-ci"
    description: "PR #2477 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4999"
    description: "OCC companion PR #4999 — self-bind for OMN-15195 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnibase-infra-pr-2479-bridge-3b67018b"
    description: "Consuming-PR binding: omnibase_infra PR #2479 (head 3b67018b, hotfix/omn-15195-devwins-bridge)
      is the operator-ruled dev->main commit-tree bridge that resolves the OMN-15195 structural blocker
      (main-target-guard rename supersedes closed PRs #2475/#2477, same bridge SHA)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15195/dod-omnibase-infra-pr-2479-bridge-3b67018b/command.yaml"
  - id: "dod-omnibase-infra-pr-2481-bridge-a09a48a6"
    description: "Consuming-PR binding: omnibase_infra PR #2481 (head a09a48a6, hotfix/omn-15195-devwins-bridge-v2)
      is the active replacement dev->main ancestry bridge for OMN-15195 after #2479/#2480 closed unmerged;
      it preserves the dev-wins bridge proof and adds a zero-tree-diff disambiguating commit."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15195/dod-omnibase-infra-pr-2481-bridge-a09a48a6/command.yaml"
