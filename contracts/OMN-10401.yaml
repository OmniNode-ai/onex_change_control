---
schema_version: "1.0.0"
ticket_id: OMN-10401
title: "Add shared rendering test fixtures and golden output tests"
summary: "Add deterministic golden output coverage for the shared market CLI report renderers across JSON,
  YAML, text, and markdown."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Golden output renderer tests pass"
    command: "uv run pytest tests/unit/cli/test_golden_outputs.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Golden output tests pass for text, JSON, YAML, and markdown renderers"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/cli/test_golden_outputs.py -q"
  - id: "dod-002"
    description: "No live dependencies used; tests render deterministic in-process fixtures only"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no live GitHub, Linear, Kafka, Docker, or runtime access required for OMN-10401'"
