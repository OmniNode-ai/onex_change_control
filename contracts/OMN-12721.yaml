---
schema_version: "1.0.0"
ticket_id: "OMN-12721"
title: "Route local delegation to served model ids"
summary: >-
  Central evidence for omnimarket PR #1057. Delegation routing now keeps local
  .201 route selections aligned with the model ids actually served by the
  compatible inference endpoints, while preserving backend model-name overlays
  for cloud and CLI providers.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "protocols"
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-routing-served-model-regression"
    description: >-
      Focused delegation routing tests prove local routes emit served routing-tier ids
      and do not regress OpenRouter or task-class contract routing behavior.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-routing-served-model-regression/command.yaml"
          && grep -q '38 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-routing-served-model-regression/command.yaml"
          && grep -q 'Qwen3.6-27B-MTP-IQ4_XS.gguf' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-routing-served-model-regression/command.yaml"
  - id: "dod-golden-chain-served-model-parity"
    description: >-
      SEA/delegation golden-chain tests pass with the local served 35B model id
      expected in the route-selection evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-golden-chain-served-model-parity/command.yaml"
          && grep -q '39 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-golden-chain-served-model-parity/command.yaml"
          && grep -q 'Qwen3.6-35B-A3B' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-golden-chain-served-model-parity/command.yaml"
  - id: "dod-live-served-model-probe"
    description: >-
      Live .201 OpenAI-compatible endpoints report the model ids encoded in the
      local routing tiers.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-live-served-model-probe/command.yaml"
          && grep -q 'port 8000: Qwen3.6-35B-A3B' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-live-served-model-probe/command.yaml"
          && grep -q 'port 8001: Qwen3.6-27B-MTP-IQ4_XS.gguf' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-live-served-model-probe/command.yaml"
  - id: "dod-omnimarket-pr-1057"
    description: "omnimarket PR #1057 carries the local served-model routing fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-omnimarket-pr-1057/command.yaml"
          && grep -q '^pr_number: 1057$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-omnimarket-pr-1057/command.yaml"
          && grep -q 'c3f8ae87a4f93d5d23a81378504f6339c4c4e590' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-omnimarket-pr-1057/command.yaml"
  - id: "dod-deploy-gate-post-merge"
    description: >-
      Routing configuration changed, but no live deploy or .201 restart was performed
      in this PR. Post-merge dev-lane validation must publish a delegation unit-test
      prompt and observe useful pytest-file output from the served local model id.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-deploy-gate-post-merge/command.yaml"
          && grep -q 'No live deploy was performed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-deploy-gate-post-merge/command.yaml"
          && grep -q 'delegation unit-test prompt' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12721/dod-deploy-gate-post-merge/command.yaml"
