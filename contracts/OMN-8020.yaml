---
schema_version: "1.0.0"
ticket_id: "OMN-8020"
title: "Add Claude Code session cost emitter hook"
summary: >
  Add handler_session_cost_emitter.py Python handler that wraps normalize_session_cost_payload() and routes
  llm.cost.completed events through emit_client_wrapper -> node_emit_daemon. Provides testable Python-layer
  equivalent of the existing session-end.sh shell emitter.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "tests"
    description: "Unit tests for handler_session_cost_emitter pass (8 tests)."
    command: "uv run pytest tests/unit/hooks/test_handler_session_cost_emitter.py -q"
  - kind: "ci"
    description: "omniclaude PR #1631 CI green at recorded head SHA."
    command: "gh pr checks 1631 --repo OmniNode-ai/omniclaude"
  - kind: "manual"
    description: "omniclaude PR #1631 is open against main at the recorded head SHA."
    command: "gh pr view 1631 --repo OmniNode-ai/omniclaude --json number,url,headRefOid,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1631-open"
    description: "omniclaude PR #1631 carries the session cost hook branch at the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8020/dod-pr-1631-open/command.yaml"
          && grep -q '^pr_number: 1631$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8020/dod-pr-1631-open/command.yaml"
          && grep -q '^commit_sha: "683192636295f6527b06265564d878a3503dfaa1"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8020/dod-pr-1631-open/command.yaml"
  - id: "dod-focused-tests"
    description: "8 unit tests for handler_session_cost_emitter passed at the recorded head SHA."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8020/dod-focused-tests/command.yaml"
          && grep -q '^commit_sha: "683192636295f6527b06265564d878a3503dfaa1"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8020/dod-focused-tests/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-8020 contract evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8020/dod-occ-self/command.yaml"
