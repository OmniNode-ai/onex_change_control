---
schema_version: "1.0.0"
ticket_id: "OMN-14394"
title: "fix(OMN-14394): close pr_state is_draft seam gap + dict-envelope regression test"
summary: >-
  omnibase_infra PR #2266 closes a seam gap found during independent verification of #2262 (OMN-14375):
  ModelOpenPrSummary.is_draft (the OMN-14374 read skill, omnimarket's node_github_repo_gateway_effect)
  had zero counterpart in pr_state's schema. This PR threads is_draft end-to-end on the producer side
  (migration 092, ModelPayloadPrStateUpsert, HandlerPrStateProjection extraction, poller emission, write-effect
  SQL $15) plus a separate test-gap item: a regression test proving HandlerPrStateProjection.handle()
  accepts a raw DICT-shaped envelope, not just an already-typed ModelEventMessage. STACKED PR — base is
  jonah/omn-14375-github-state-projection-pr-state (#2262), not dev; #2262's pr_state table/models/handlers
  do not exist on dev yet, and #2262 is itself OCC-cascade-blocked. #2266 cannot merge until #2262 lands
  and GitHub retargets its base to dev. This companion clears #2266's own occ-preflight so it is merge-ready
  the moment that happens.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2266-head-daa28e5c"
    description: >-
      Independent adversarial verification of omnibase_infra PR #2266 at head daa28e5cd1af226af79b3641de1637915510539a:
      confirmed the diff is a clean stacked delta (no bleed from #2262's own content), confirmed migration
      092 is collision-free across dev/#2262-branch/#2266-branch, confirmed the is_draft field matches
      the omnimarket reader (ModelOpenPrSummary.is_draft: bool) field-for-field with no nullability/type
      mismatch, and independently reproduced RED (9 failed against pre-fix source) then GREEN (56 passed
      with the fix restored) in a fresh worktree, plus confirmed the HandlerPrStateProjection god-class
      ratchet holds at exactly 10 sync methods.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14394/dod-omnibase-infra-pr-2266-head-daa28e5c/command.yaml
          && grep -q '^pr_number: 2266$' drift/dod_receipts/OMN-14394/dod-omnibase-infra-pr-2266-head-daa28e5c/command.yaml
          && grep -q 'daa28e5cd1af226af79b3641de1637915510539a' drift/dod_receipts/OMN-14394/dod-omnibase-infra-pr-2266-head-daa28e5c/command.yaml
  - id: "dod-deploy-assessment-head-daa28e5c"
    description: >-
      Deploy-surface assessment for omnibase_infra PR #2266 head daa28e5cd1af226af79b3641de1637915510539a:
      the diff adds one additive forward+rollback migration (092_pr_state_add_is_draft.sql, idempotent
      ALTER TABLE ... ADD COLUMN IF NOT EXISTS) plus four handler/model source files and four test files.
      It does not perform a live deploy, service restart, or .201 mutation itself, and the PR is merge-blocked
      (stacked on #2262) so nothing in this diff can reach any runtime environment until #2262 lands first.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14394/dod-deploy-assessment-head-daa28e5c/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14394/dod-deploy-assessment-head-daa28e5c/command.yaml
  - id: "dod-occ-self-commit"
    description: >-
      OCC self-binding: this onex_change_control branch carries the OMN-14394 central contract and PASS
      receipts for omnibase_infra PR #2266 head daa28e5cd1af226af79b3641de1637915510539a. The receipt
      binds the first evidence commit for occ-preflight eligibility before the OCC PR number exists.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14394/dod-occ-self-commit/command.yaml && grep
          -q 'jonah/omn-14394-occ-pr2266' drift/dod_receipts/OMN-14394/dod-occ-self-commit/command.yaml
