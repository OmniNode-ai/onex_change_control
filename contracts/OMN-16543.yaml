---
schema_version: "1.0.0"
ticket_id: "OMN-16543"
title: "Autobind OCC evidence for OMN-16543"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2905.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2905 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2905"
    description: "PR #2905 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2905 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2905 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: >
      Deploy-scope DoD so PR #2905 clears the deploy-gate (F-05). PR #2905 is a docs/comment-only change
      to two deploy-scoped compose files (docker/docker-compose.infra.yml, docker/docker-compose.e2e.yml)
      -- it corrects stale header-comment claims (fabricated --profile consul, misdescribed --profile
      secrets) with zero change to any service definition, image, port, env var, or profile assignment.
      No live redeploy is required to prove this fix; the hermetic compute-only pattern (OMN-14051) applies
      -- the check fetches the actual merged file content by SHA and greps for the corrected text the
      fix introduces, so its exit status depends on the real state of the file at that commit, not on
      anything this PR's author wrote about itself.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "f=\"$(mktemp)\" && gh api repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.infra.yml?ref=ce10b54a192a89898ad86814f5fc92ec41bf2516
          --jq .content | base64 -d > \"$f\" && grep -qF 'omnimemory: Memgraph (OMN-4310)' \"$f\""
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2905"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7166"
    description: "Binds OMN-16543 to OCC companion PR #7166 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7166 --repo OmniNode-ai/onex_change_control --json number,state"
