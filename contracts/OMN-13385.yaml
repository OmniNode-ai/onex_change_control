---
schema_version: "1.0.0"
ticket_id: "OMN-13385"
title: "Thin GET surface exposing the Contract Graph IR (read-only)"
summary: >-
  Implements a read-only COMPUTE node (node_contract_graph_ir_compute) that returns the deterministic
  Contract Graph IR (ModelContractGraphIr) + stable per-source/per-adapter sha256 hash manifest via manifest-driven
  discovery. Reuses the Phase-2 IR adapters from omnibase_core.contract_graph (OMN-13132, already merged).
  No mutation, no write path, no authoring logic. Does not violate bus-native action-path invariant —
  this is an authoring read surface for a derived artifact, not a runtime command path.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1322"
    description: >-
      omnimarket PR #1322 adds node_contract_graph_ir_compute with 17 unit tests (including explicit byte-stability
      determinism proofs), mypy --strict clean, ruff + pre-commit clean, 4862 unit tests pass. Acceptance:
      GET returns IR + hash manifest for >=1 EFFECT + >=1 COMPUTE backend contract; repeated request is
      byte-stable. Evidence anchor: omnimarket head commit c71479c2279c40182ed9f5ea9dbd49e90d6d05e0.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13385/dod-omnimarket-pr-1322/command.yaml"
          && grep -q '^ticket_id: "OMN-13385"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13385/dod-omnimarket-pr-1322/command.yaml"
          && grep -q '^pr_number: 1322$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13385/dod-omnimarket-pr-1322/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: new COMPUTE node node_contract_graph_ir_compute added to omnimarket. No Kafka
      topic creation (topics declared in contract.yaml, provisioned on standard deploy). No Postgres migration.
      No contract API change. The new node rides a standard omnimarket image rebuild as a pure compute
      handler with no side effects. No live runtime deploy is performed by this PR; dev/stability/prod
      lanes are untouched. Invoked on-demand via the bus; no separate deploy action beyond image rebuild.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13385/dod-deploy-assessment/command.yaml"
          && grep -q '^ticket_id: "OMN-13385"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13385/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR (#2863), which carries the OMN-13385 contract and the dod-omnimarket-pr-1322
      + dod-deploy-assessment receipts that omnimarket PR #1322's Receipt-Gate resolves via Evidence-Source:
      OCC#2863. Binds by pr_number so the OCC eligibility validator finds a PASS receipt for OMN-13385
      on this OCC PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13385/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-13385"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13385/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2863$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13385/dod-occ-pr/command.yaml"
