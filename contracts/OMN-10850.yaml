---
schema_version: "1.0.0"
ticket_id: OMN-10850
title: "SQLite projection adapter includes prompt_text and response_text"
summary: "Fix delegation projection rows showing empty content in dashboard — prompt_text and response_text
  were never written to SQLite. Adds both fields to ModelTaskDelegatedEvent, HandlerProjectionDelegation.project()
  upsert, and DelegationProjectionRunner Kafka consumer write path. Also fixes empty-string coercion bug
  (CodeRabbit): uses None-check rather than truthy check for camelCase fallback."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "Implementation PR green on omnimarket"
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket"
  - kind: manual
    description: "All projection delegation tests pass including new prompt/response text coverage"
    command: "uv run pytest tests/nodes/node_projection_delegation/ -v --tb=short"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-omnimarket-pr-601
    description: "Implementation PR #601 is open against omnimarket main. No live deploy required — SQLite-only
      column addition, projection runner reads new fields on next delegation event."
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json url,state,baseRefName,headRefName,headRefOid"
  - id: dod-occ-pr
    description: "Self-binding receipt for this OCC PR #929."
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json state,baseRefName
          --jq '.state + \":\" + .baseRefName' | grep -q 'OPEN:main'"
