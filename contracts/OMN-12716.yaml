---
schema_version: "1.0.0"
ticket_id: "OMN-12716"
title: "Codify Qwen coder vLLM tool-call parser config"
summary: >-
  Central evidence for omnibase_infra PR #1871. The .201 Qwen coder vLLM
  service must preserve the ADK-required `--enable-auto-tool-choice` and
  `--tool-call-parser qwen3_coder` flags so SEA local OpenAI-compatible ADK
  runs do not regress to HTTP 400 while plain chat completions still pass.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-vllm-unit-tests"
    description: "Focused omnibase_infra test and lint guard for the Qwen coder vLLM systemd unit passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12716/dod-focused-vllm-unit-tests/command.yaml"
          && grep -q '2 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12716/dod-focused-vllm-unit-tests/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12716/dod-focused-vllm-unit-tests/command.yaml"
  - id: "dod-live-hotpatch-proof"
    description: "The .201 hotpatched vLLM service with qwen3_coder tool parser passed full SEA and direct delegation proofs."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12716/dod-live-hotpatch-proof/command.yaml"
          && grep -q 'f6859ca4-e2ad-46ea-bd14-4eaab5ae81f2' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12716/dod-live-hotpatch-proof/command.yaml"
          && grep -q '39cba6ae-68d0-4690-8238-fcdda82f3890' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12716/dod-live-hotpatch-proof/command.yaml"
          && grep -q 'tool-call-parser qwen3_coder' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12716/dod-live-hotpatch-proof/command.yaml"
  - id: "dod-infra-pr-1871"
    description: "omnibase_infra PR #1871 carries the durable service-unit artifact against dev."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12716/dod-infra-pr-1871/command.yaml"
          && grep -q '^pr_number: 1871$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12716/dod-infra-pr-1871/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR #2199 carries the OMN-12716 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12716/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2199$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12716/dod-occ-pr/command.yaml"
