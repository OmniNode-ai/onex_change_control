---
schema_version: "1.0.0"
ticket_id: "OMN-11003"
title: "Classify duplicate model collisions and burn down delegation event wrapper"
summary: >
  Binds omnimarket PR #664, which classifies high-risk delegation/routing/task-contract D4 model-name
  collisions and removes omnimarket's local ModelDelegationEvent class definition by re-exporting the
  canonical omnibase_compat delegation event envelope.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #664 carries the OMN-11003 classification and delegation event wrapper
      burn-down."
    command: "gh pr view 664 --repo OmniNode-ai/omnimarket --json state,headRefOid"
  - kind: "tests"
    description: "Focused delegation DTO compatibility and workflow tests pass."
    command: "uv run pytest tests/test_delegation_dto_omnibase_compat_imports.py tests/unit/delegation/test_delegation_orchestrator.py
      tests/unit/delegation/test_dispatcher_event_bus.py tests/unit/nodes/node_delegate_skill_orchestrator/test_runtime_dispatch_port.py
      -q"
  - kind: "manual"
    description: "D4 overlay scan removes the targeted ModelDelegationEvent collision while leaving scoped
      residual families."
    command: "D4 overlay targeted assertion: ModelDelegationEvent absent=True finding_count=182"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-664"
    description: "omnimarket PR #664 carries the OMN-11003 model-collision slice."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11003/dod-omnimarket-pr-664/command.yaml
          && grep -q '^pr_number: 664$' drift/dod_receipts/OMN-11003/dod-omnimarket-pr-664/command.yaml
          && grep -q '^commit_sha: 4d04988896a2f3ee4d20c4a353fc707c30609653$' drift/dod_receipts/OMN-11003/dod-omnimarket-pr-664/command.yaml"
  - id: "dod-classification-evidence"
    description: "Classification evidence covers the high-risk delegation/routing/task-contract/dispatch-record
      families."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11003/dod-classification-evidence/command.yaml
          && grep -q 'ModelDispatchRecord' drift/dod_receipts/OMN-11003/dod-classification-evidence/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused omnimarket delegation compatibility and workflow tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11003/dod-focused-tests/command.yaml
          && grep -q '64 passed' drift/dod_receipts/OMN-11003/dod-focused-tests/command.yaml"
  - id: "dod-ruff"
    description: "Touched omnimarket compatibility shim and tests pass ruff."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11003/dod-ruff/command.yaml && grep
          -q 'All checks passed' drift/dod_receipts/OMN-11003/dod-ruff/command.yaml"
  - id: "dod-contract-parse"
    description: "Repo-local omnimarket OMN-11003 work-tracking contract parses and evidence file exists."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11003/dod-contract-parse/command.yaml
          && grep -q 'contract ok' drift/dod_receipts/OMN-11003/dod-contract-parse/command.yaml"
  - id: "dod-d4-targeted"
    description: "D4 overlay removes ModelDelegationEvent from residual findings."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11003/dod-d4-targeted/command.yaml
          && grep -q 'ModelDelegationEvent absent=True' drift/dod_receipts/OMN-11003/dod-d4-targeted/command.yaml"
  - id: "dod-deploy"
    description: "No live deploy or runtime restart is required for the omnimarket DTO compatibility import."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11003/dod-deploy/command.yaml &&
          grep -q 'No live deploy' drift/dod_receipts/OMN-11003/dod-deploy/command.yaml"
