---
schema_version: "1.0.0"
ticket_id: "OMN-10337"
title: "Task 6: Build Codex session cost emitter"
summary: "Scan Codex JSONL session logs into normalized, idempotent session cost events with attribution
  and absence handling."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "protocols"
evidence_requirements:
  - kind: "tests"
    description: "Codex emitter unit and scanner integration tests pass"
    command: "cd omniclaude && uv run pytest tests/unit/hooks/test_codex_emitter.py -q && KAFKA_BOOTSTRAP_SERVERS=localhost:19092
      uv run pytest tests/integration/hooks/test_codex_session_emission.py -m 'not integration' -q"
  - kind: "ci"
    description: "Focused lint, topic naming, secret, ONEX, and shell checks pass"
    command: "cd omniclaude && uv run ruff check src/omniclaude/hooks/codex_emitter.py tests/unit/hooks/test_codex_emitter.py
      tests/integration/hooks/test_codex_session_emission.py && uv run python scripts/validation/validate_topic_naming.py
      && uv run python scripts/validation/validate_secrets.py src/omniclaude/hooks/codex_emitter.py &&
      bash scripts/uv-run-worktree-safe.sh python scripts/validate_onex.py && bash -n scripts/codex-session-scanner.sh"
  - kind: "ci"
    description: "Implementation PR created for OMN-10337"
    command: "gh pr view 1468 --repo OmniNode-ai/omniclaude --json number,state,url"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Codex emitter unit and scanner integration tests passed"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10337/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-10337$' drift/dod_receipts/OMN-10337/dod-001/command.yaml"
  - id: "dod-002"
    description: "Focused lint, topic naming, secret, ONEX, and shell checks passed"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10337/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-10337$' drift/dod_receipts/OMN-10337/dod-002/command.yaml"
  - id: "dod-003"
    description: "Implementation PR was created and linked"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10337/dod-003/command.yaml && grep
          -q 'pr_number: 1468' drift/dod_receipts/OMN-10337/dod-003/command.yaml"
