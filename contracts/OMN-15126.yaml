---
schema_version: "1.0.0"
ticket_id: "OMN-15126"
title: "Autobind OCC evidence for OMN-15126"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_core PR #1507.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1507 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_core-pr-1507"
    description: "PR #1507 on OmniNode-ai/omnibase_core — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4852"
    description: "Binds OMN-15126 to OCC companion PR #4852 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1896"
    description: "PR #1896 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1896-ci"
    description: "PR #1896 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4885"
    description: "OCC companion PR #4885 — self-bind for OMN-15126 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-omn15126-hermetic-symbol-check"
    description: >-
      Compute-only ticket: the OMN-15126 A9 liveness evaluator (node_liveness_demand_query_effect + node_liveness_evaluate_compute,
      OmniNode-ai/omnimarket#1896) has no live runtime deploy yet (design OMN-14845 §7 OPEN-3/OPEN-4 residual
      — registry-catalog placement and the live orchestrator wiring are explicit, undecided follow-on
      work, not invented in that PR). Per the deploy-gate falsifiability guidance (OMN-14505) for compute-only/no-live-deploy
      tickets, this is a hermetic GitHub-content check (OMN-14051 exemption): it reads the actual file
      content at a real commit on OmniNode-ai/omnimarket and asserts the class the PR introduces is genuinely
      present. It is NOT self-referential (does not read onex_change_control's own dod_receipts/contracts
      tree) and goes RED if the class is ever removed. Pinned to PR #1896's own head commit (c9920fe585fe4e8e444ab023831a0c5fe818b3ec)
      rather than @dev because this repo's own Contract Compliance Check EXECUTES the command at authoring
      time (unlike the deploy-gate consumer, which only classifies check_value shape) — pinning to @dev
      here would 404 until after #1896 merges, creating a circular dependency between this OCC PR and
      the PR it backfills evidence for. Follow-up: once #1896 merges, re-pin both checks to the merged
      squash-commit SHA on omnimarket@dev (a stable, permanent reference per reference_never_pin_a_feature_branch_head)
      instead of the PR head, which is only reachable until GitHub GCs the closed branch's commits.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_liveness_evaluate_compute/handlers/handler_liveness_evaluate_compute.py?ref=c9920fe585fe4e8e444ab023831a0c5fe818b3ec
          --jq .content | base64 -d | grep -q 'class HandlerLivenessEvaluateCompute'"
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_liveness_demand_query_effect/handlers/handler_liveness_demand_query_effect.py?ref=c9920fe585fe4e8e444ab023831a0c5fe818b3ec
          --jq .content | base64 -d | grep -q 'class HandlerLivenessDemandQueryEffect'"
