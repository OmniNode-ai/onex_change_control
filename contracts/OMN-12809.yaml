---
schema_version: "1.0.0"
ticket_id: "OMN-12809"
title: "Retire /api/dispatch + node_dispatch_request_handler (router-hop anti-pattern)"
summary: >-
  Removes the generic /api/dispatch endpoint (omnidash) and node_dispatch_request_handler node (omnimarket)
  identified as the router-hop anti-pattern in audit F3 (docs/audits/2026-06-07-demo-path-contract-native-audit.md).
  Migrates all UI callers (DispatchButton, CommandPalette) to the canonical /api/renderer/emit thin-publisher
  path. #magic___^_^___line
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
  - "public_api"

evidence_requirements:
  - kind: ci
    description: "omnimarket PR #1485 all checks pass"
    command: "gh pr checks 1485 --repo OmniNode-ai/omnimarket"
  - kind: ci
    description: "omnidash PR #245 all checks pass"
    command: "gh pr checks 245 --repo OmniNode-ai/omnidash"

emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""

dod_evidence:
  - id: "dod-001"
    description: >-
      omnimarket: node_dispatch_request_handler directory deleted, entry-point removed from pyproject.toml,
      all 4618 unit tests pass, ruff/mypy/pre-commit clean.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12809/dod-001/command.yaml && grep -q '^pr_number:
          1485$' drift/dod_receipts/OMN-12809/dod-001/command.yaml
  - id: "dod-002"
    description: >-
      omnidash: POST /api/dispatch removed from server/routes.ts, dispatch-api.ts deleted, useDispatch.ts
      migrated to emitRendererAction, all 1472 tests pass, TypeScript/ESLint clean.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12809/dod-002/command.yaml && grep -q '^pr_number:
          245$' drift/dod_receipts/OMN-12809/dod-002/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding OCC eligibility receipt for onex_change_control OCC PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12809/dod-occ-pr-self/command.yaml
