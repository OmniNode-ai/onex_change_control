---
schema_version: "1.0.0"
ticket_id: OMN-8537
title: "Implement model escalation chain in dispatch engine (local → cheap frontier → ex"
summary: "Implement model escalation chain in dispatch engine (local → cheap frontier → ex"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on omnimarket PR #683"
    command: "gh pr checks 683 --repo OmniNode-ai/omnimarket --watch"
  - kind: "tests"
    description: "14 TDD escalation chain tests pass; 11 existing model_router regression tests pass"
    command: "uv run pytest tests/nodes/node_model_router/test_escalation_chain.py tests/test_model_router_*.py
      -v"
  - kind: "manual"
    description: "Post-merge deploy readiness smoke: escalation chain imports cleanly from runtime container"
    command: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_model_router.models.model_escalation_chain
      import ModelEscalationChain, EscalationTier; print('escalation chain deploy readiness smoke ok')\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}"
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json mergedAt -q .mergedAt --repo ${REPO}"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --watch --repo ${REPO}"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-005"
    description: "Post-merge deploy readiness smoke: escalation chain imports cleanly from runtime container.
      This is a compute-only node change (no runtime restart or deploy required); smoke verifies the module
      is importable in the deployed container image."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_model_router.models.model_escalation_chain
          import ModelEscalationChain, EscalationTier; print('escalation chain deploy readiness smoke
          ok')\""
