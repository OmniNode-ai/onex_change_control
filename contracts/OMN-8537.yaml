---
schema_version: "1.0.0"
ticket_id: OMN-8537
title: "Implement model escalation chain in dispatch engine (local → cheap frontier → ex"
summary: "Implement model escalation chain in dispatch engine (local → cheap frontier → ex"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on omnimarket PR #683"
    command: "gh pr checks 683 --repo OmniNode-ai/omnimarket"
  - kind: "tests"
    description: "31 TDD tests pass (14 escalation chain + 11 existing model_router)"
    command: "uv run pytest tests/nodes/node_model_router/ tests/test_model_router_*.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ci"
    description: "CI passes on omnimarket PR (tests, lint, mypy, hooks all green)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-8537/dod-ci/command.yaml"
  - id: "dod-tests"
    description: "31 TDD tests pass: 14 escalation chain unit/integration + 11 existing model_router regression"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-8537/dod-tests/command.yaml"
  - id: "dod-deploy"
    description: "Post-merge deploy readiness smoke: escalation chain imports cleanly. Compute-only change;
      no runtime restart required."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-8537/dod-deploy/command.yaml"
