---
schema_version: "1.0.0"
ticket_id: "OMN-17836"
title: "Group auto-merge GitHub output writes"
summary: >-
  Repairs the OCC auto-merge workflow by grouping the pr, actor, and skip writes to GITHUB_OUTPUT. This
  is a workflow-only change and does not authorize deployment, runtime mutation, or bypass.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "The PR diff is limited to the auto-merge workflow and preserves explicit output keys."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-auto-merge-output-grouping"
    description: "PR #8156 changes only .github/workflows/auto-merge.yml and groups the GITHUB_OUTPUT
      writes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr diff 8156 --repo OmniNode-ai/onex_change_control --patch | grep -F '} >> "$GITHUB_OUTPUT"'
  - id: "occ-self-bind-pr-8156"
    description: >-
      OCC self-binding receipt for PR #8156, the in-repo evidence PR carrying the OMN-17836 contract.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8156 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
