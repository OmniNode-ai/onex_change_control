---
schema_version: "1.0.0"
ticket_id: "OMN-11151"
title: "Block skill-node boundary validation suppressions"
summary: >
  Removes advisory continuation from the omniclaude skill-node boundary workflow path so changed ONEX
  skill files are gated, and adds workflow regression coverage to prevent suppression drift.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Blocking validation workflow tests pass on omniclaude PR #1613."
    command: "uv run pytest tests/workflows/test_blocking_validation_workflows.py tests/validation/test_validate_skill_node_boundary.py
      tests/unit/skills/generate_ticket_contract/test_proof_validation.py -q"
  - kind: "ci"
    description: "omniclaude PR #1613 is bound to OMN-11151 at the recorded head SHA."
    command: "gh pr view 1613 --repo OmniNode-ai/omniclaude --json headRefOid,title"
  - kind: "ci"
    description: "omniclaude PR #1613 non-receipt checks are green; Receipt Gate is the only blocker."
    command: "gh pr checks 1613 --repo OmniNode-ai/omniclaude"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1613"
    description: "omniclaude PR #1613 is open against main at the recorded OMN-11151 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11151/dod-omniclaude-pr-1613/command.yaml\"
          && grep -q '^pr_number: 1613$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11151/dod-omniclaude-pr-1613/command.yaml\"
          && grep -q '^commit_sha: \"88d811947333d1b5b2dba7536d16adf90bb3cf8c\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11151/dod-omniclaude-pr-1613/command.yaml\""
  - id: "dod-product-ci-gates"
    description: "omniclaude PR #1613 product CI is green except Receipt Gate pending this central evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11151/dod-product-ci-gates/command.yaml\"
          && grep -q 'Receipt Gate is the only failing check' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11151/dod-product-ci-gates/command.yaml\""
