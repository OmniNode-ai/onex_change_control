---
schema_version: "1.0.0"
ticket_id: "OMN-10572"
title: "Task 5: Hatchling wheel-content verification"
summary: "Add tests/unit/structure/test_wheel_content.py that builds the hatchling wheel and asserts non-production
  content (experiments/, .onex_state/, OMN-*.yaml work-tracking files, machine-specific /Users/ or /Volumes/
  paths) is absent, and omnimarket/ package files are present."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Wheel content unit tests pass."
    command: "uv run pytest tests/unit/structure/test_wheel_content.py -v"
  - kind: "ci"
    description: "CI pipeline green on omnimarket PR #542."
    command: "gh pr checks 542 --repo OmniNode-ai/omnimarket --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Wheel-content tests pass: 4 passed (experiments test skipped pending OMN-10570/PR#541
      merge)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10572/dod-001/command.yaml\"; else uv run pytest
          tests/unit/structure/test_wheel_content.py -v; fi"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10572 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10572/dod-occ-pr/command.yaml\""
