---
schema_version: "1.0.0"
ticket_id: "OMN-11563"
title: "refactor(OMN-11563): rename debug_utils.py → diagnostics.py [omniclaude]"
summary: >
  Pure mechanical rename in omniclaude lib/utils/: debug_utils.py → diagnostics.py. All internal imports,
  lazy-load map, and affected test files updated. Covered by 46 unit tests in omniclaude PR #1658. No
  deployable artifact — rename only.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: 46 unit tests pass covering all renamed modules and their consumers.
    command: >
      cd omniclaude && uv run pytest tests/unit/lib/utils/test_circular_import_regression.py -v
  - kind: manual
    description: omniclaude PR  #1658 carries the rename implementation.
    command: gh pr checks 1658 --repo OmniNode-ai/omniclaude
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "46 unit tests pass — all renamed module consumers verified clean."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11563/dod-unit-tests/command.yaml"
  - id: "dod-no-deploy"
    description: >
      No deployable artifact. Pure file rename with no runtime behavior change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11563/dod-no-deploy/command.yaml"
