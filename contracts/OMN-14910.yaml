---
schema_version: "1.0.0"
ticket_id: "OMN-14910"
title: "Narrow omnibase_core smart-test selector escalation"
summary: "OCC evidence for omnibase_core#1477, the OMN-14910 change that narrows smart-test selector escalation
  while retaining fail-closed behavior and CI full-suite coverage for selector self-changes."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_core#1477 product PR is open on the expected OMN-14910 smart-test selector
      branch."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - kind: "ci"
    description: "omnibase_core#1477 changes only CI smart-test selector workflow, selector models/runtime,
      adjacency data, and focused selector tests."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1477"
    description: "PR #1477 on OmniNode-ai/omnibase_core is the OMN-14910 smart-test selector narrowing
      change."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - id: "dod-core-pr-1477-files"
    description: "PR #1477 on OmniNode-ai/omnibase_core is scoped to CI smart-test selector workflow,
      selector models/runtime, adjacency data, and focused selector tests."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-occ-self-bind-pr-4573"
    description: "OCC evidence PR #4573 carries central OMN-14910 evidence for omnibase_core#1477."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - id: "dod-deploy-assessment"
    description: >
      Deploy assessment for omnibase_core#1477. The product PR touches runtime selector paths (`runtime_test_selector.py`
      and `selector_core.py`) but changes CI test-selection behavior only: no live deploy, docker exec,
      rpk topic produce, runtime restart, ECR push, migration, or cluster mutation was performed as part
      of this pre-merge evidence. Runtime rollout remains under the normal omnibase_core merge/deploy
      pipeline and deploy gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14910/dod-deploy-assessment/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-14910/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self-bind-pr-4579"
    description: "OCC evidence PR #4579 carries central OMN-14910 deploy-scope evidence for omnibase_core#1477."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
