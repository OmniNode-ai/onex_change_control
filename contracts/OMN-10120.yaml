---
schema_version: "1.0.0"
ticket_id: OMN-10120
title: "Delete legacy omniclaude publisher source tree"
summary: "Central receipt contract for deleting the legacy omniclaude.publisher package and routing PluginClaude
  startup through the omnimarket emit daemon in omniclaude PR #1502."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: "manual"
    description: "Legacy publisher source tree, tests, and module references are absent"
    command: "test -f drift/dod_receipts/OMN-10120/dod-static-removal/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-10120/dod-static-removal/command.yaml && grep -q 'Legacy publisher
      package and tests are absent' drift/dod_receipts/OMN-10120/dod-static-removal/command.yaml"
  - kind: "manual"
    description: "Runtime lifecycle and launcher cutover tests pass"
    command: "test -f drift/dod_receipts/OMN-10120/dod-focused-tests/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-10120/dod-focused-tests/command.yaml && grep -q '70 passed' drift/dod_receipts/OMN-10120/dod-focused-tests/command.yaml"
  - kind: "manual"
    description: "Full unit suite passes in isolated local environment"
    command: "test -f drift/dod_receipts/OMN-10120/dod-unit-suite/command.yaml && grep -q '^status: PASS$'
      drift/dod_receipts/OMN-10120/dod-unit-suite/command.yaml && grep -q '4738 passed' drift/dod_receipts/OMN-10120/dod-unit-suite/command.yaml"
  - kind: "manual"
    description: "Lint, format, mypy, pyright, ONEX validation, and clean-root gates pass"
    command: "test -f drift/dod_receipts/OMN-10120/dod-local-gates/command.yaml && grep -q '^status: PASS$'
      drift/dod_receipts/OMN-10120/dod-local-gates/command.yaml && grep -q 'mypy: Success' drift/dod_receipts/OMN-10120/dod-local-gates/command.yaml
      && grep -q 'pyright: 0 errors' drift/dod_receipts/OMN-10120/dod-local-gates/command.yaml && grep
      -q 'ONEX validation: 0 errors' drift/dod_receipts/OMN-10120/dod-local-gates/command.yaml"
  - kind: "manual"
    description: "Product branch pre-push hooks pass"
    command: "test -f drift/dod_receipts/OMN-10120/dod-pre-push/command.yaml && grep -q '^status: PASS$'
      drift/dod_receipts/OMN-10120/dod-pre-push/command.yaml && grep -q 'pre-push hooks passed' drift/dod_receipts/OMN-10120/dod-pre-push/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-static-removal"
    description: "Legacy publisher source tree, tests, and module references are absent"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10120/dod-static-removal/command.yaml && grep -q
          '^status: PASS$' drift/dod_receipts/OMN-10120/dod-static-removal/command.yaml && grep -q 'Legacy
          publisher package and tests are absent' drift/dod_receipts/OMN-10120/dod-static-removal/command.yaml"
  - id: "dod-focused-tests"
    description: "Runtime lifecycle and launcher cutover tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10120/dod-focused-tests/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10120/dod-focused-tests/command.yaml && grep -q '70 passed' drift/dod_receipts/OMN-10120/dod-focused-tests/command.yaml"
  - id: "dod-unit-suite"
    description: "Full unit suite passes in isolated local environment"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10120/dod-unit-suite/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10120/dod-unit-suite/command.yaml && grep -q '4738 passed' drift/dod_receipts/OMN-10120/dod-unit-suite/command.yaml"
  - id: "dod-local-gates"
    description: "Lint, format, mypy, pyright, ONEX validation, and clean-root gates pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10120/dod-local-gates/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10120/dod-local-gates/command.yaml && grep -q 'mypy: Success'
          drift/dod_receipts/OMN-10120/dod-local-gates/command.yaml && grep -q 'pyright: 0 errors' drift/dod_receipts/OMN-10120/dod-local-gates/command.yaml
          && grep -q 'ONEX validation: 0 errors' drift/dod_receipts/OMN-10120/dod-local-gates/command.yaml"
  - id: "dod-pre-push"
    description: "Product branch pre-push hooks pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10120/dod-pre-push/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10120/dod-pre-push/command.yaml && grep -q 'pre-push hooks passed'
          drift/dod_receipts/OMN-10120/dod-pre-push/command.yaml"
