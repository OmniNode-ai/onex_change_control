---
schema_version: "1.0.0"
ticket_id: "OMN-10966"
title: "Fix remaining SonarCloud findings + review security hotspots on #1581"
summary: >-
  Fixes 2 SonarCloud S7503 (unnecessary async) findings in omnimarket's delegation plugin by adding #
  NOSONAR S7503 suppressions to protocol-required async methods that satisfy ProtocolDomainPlugin. Also
  provides written security hotspot review document for all 5 omnimarket hotspots (S7503 x2, S2245 x2,
  S2076 x1) with per-hotspot decision and rationale. Pure code-smell + documentation; no behavior change,
  no deploy required.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnimarket PR #680 is bound to OMN-10966 at observed head."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10966/dod-omnimarket-pr-680/command.yaml"
  - kind: "manual"
    description: "No deploy required; pure code-smell suppression + security review doc."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10966/dod-deploy/command.yaml"
  - kind: "manual"
    description: "OCC PR carries OMN-10966 evidence."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10966/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-680"
    description: "Bind omnimarket PR #680 to OMN-10966 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10966/dod-omnimarket-pr-680/command.yaml\""
  - id: "dod-deploy"
    description: "No live deploy or runtime restart required; pure code-smell suppression with no behavior
      change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\n' 'deploy gate: OMN-10966 is a pure code-smell suppression; no live
          deploy or runtime restart required'"
  - id: "dod-occ-self"
    description: "Self-binding receipt for OCC PR carrying OMN-10966 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\n' 'OCC self-bind: this OCC PR carries OMN-10966 evidence'"
