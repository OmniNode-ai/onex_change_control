---
schema_version: "1.0.0"
ticket_id: OMN-10274
title: "Queue manager Track A must use live merge-queue adapter"
summary: "Queue manager Track A must use live merge-queue adapter"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #442 checks pass"
    command: "gh pr checks 442 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "Post-merge runtime smoke confirms PR lifecycle merge handler imports"
    command: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_pr_lifecycle_merge_effect.handlers.adapter_github_merge_queue
      import GitHubMergeQueueAdapter; print('deploy smoke ok')\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused queue-manager merge adapter and orchestrator wiring tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_pr_lifecycle_merge_effect/test_adapter_github_merge_queue.py
          tests/unit/nodes/node_pr_lifecycle_merge_effect/test_orchestrator_wires_live_merge_adapter.py
          tests/unit/nodes/node_pr_lifecycle_fix_effect/test_orchestrator_wires_live_adapters.py tests/test_golden_chain_pr_lifecycle_merge_effect.py
          tests/test_golden_chain_pr_lifecycle_orchestrator.py -q"
  - id: "dod-002"
    description: "Touched queue-manager source passes lint and type checks"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_pr_lifecycle_merge_effect/handlers/adapter_github_merge_queue.py
          src/omnimarket/nodes/node_pr_lifecycle_orchestrator/handlers/handler_pr_lifecycle_orchestrator.py
          tests/unit/nodes/node_pr_lifecycle_merge_effect && uv run mypy src/omnimarket/nodes/node_pr_lifecycle_merge_effect/handlers/adapter_github_merge_queue.py
          src/omnimarket/nodes/node_pr_lifecycle_orchestrator/handlers/handler_pr_lifecycle_orchestrator.py"
  - id: "dod-003"
    description: "Post-merge deploy smoke plan for runtime import of live merge adapter"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\n' 'post-merge deploy smoke: docker exec ${RUNTIME_CONTAINER:-omninode-runtime}
          python -c \"from omnimarket.nodes.node_pr_lifecycle_merge_effect.handlers.adapter_github_merge_queue
          import GitHubMergeQueueAdapter; print(GitHubMergeQueueAdapter.__name__)\"'"
