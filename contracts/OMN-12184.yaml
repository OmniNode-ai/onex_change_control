---
# OMN-12184 contract file for omnibase_infra deploy-gate.
schema_version: "1.0.0"
ticket_id: OMN-12184
title: "Convert boundary dataclasses to Pydantic BaseModel"
summary: >
  Converts boundary-crossing dataclasses to Pydantic BaseModel and keeps internal-only runtime containers
  explicitly annotated. The change updates model construction/copy semantics but introduces no new topics,
  services, migrations, or runtime deployment step before merge. #magic___^_^___line
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
  - protocols
evidence_requirements:
  - kind: tests
    description: "Focused CLI/runtime/adapters unit coverage passes for converted boundary models."
    command: "uv run pytest tests/unit/cli/ tests/unit/adapters/ tests/unit/runtime/ -q"
  - kind: ci
    description: "Raw topic literal invariant remains clean after deriving demo reset prefixes from topic
      constants."
    command: "uv run python scripts/validation/check_topic_literals.py --baseline scripts/validation/topic_literal_baseline.txt"
  - kind: manual
    description: "Deploy-gate proof: model-boundary refactor only; no live deploy is performed pre-merge."
    command: "deploy-gate: no live deploy performed pre-merge; next runtime image build carries the boundary
      model refactor"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-targeted-tests
    description: "Targeted CLI/runtime/adapters tests pass for the converted boundary models."
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12184/dod-targeted-tests/command.yaml"
  - id: dod-topic-literals
    description: "Topic literal invariant passes with demo reset prefixes derived from canonical topic
      constants."
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12184/dod-topic-literals/command.yaml"
  - id: dod-deploy-gate
    description: "No pre-merge live deploy required; next runtime image build carries the boundary model
      refactor."
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12184/dod-deploy-gate/command.yaml"
  - id: dod-occ-pr-self
    description: "OCC PR #2968 carries the central OMN-12184 contract and PASS receipts for omnibase_infra#2078."
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12184/dod-occ-pr-self/command.yaml"
