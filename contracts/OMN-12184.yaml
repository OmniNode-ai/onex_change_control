---
schema_version: "1.0.0"
ticket_id: "OMN-12184"
title: "Convert omnibase_infra boundary dataclasses to Pydantic BaseModel"
summary: >
  Central OCC evidence for omnibase_infra PR #1757, which converts boundary-crossing dataclasses to Pydantic
  BaseModel, annotates internal-only dataclasses, and derives demo reset topic prefixes from canonical
  topic constants. No new topics, services, migrations, or live runtime mutation are introduced before
  merge.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
  - "protocols"
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: "Focused demo reset tests pass after deriving prefixes from canonical topic constants."
    command: "cd omnibase_infra && uv run pytest tests/unit/cli/test_service_demo_reset.py -q"
  - kind: ci
    description: "Raw topic literal invariant passes for the PR head."
    command: "cd omnibase_infra && uv run python scripts/validation/check_topic_literals.py --baseline
      scripts/validation/topic_literal_baseline.txt"
  - kind: manual
    description: "omnibase_infra PR #1757 carries the OMN-12184 implementation and repo-local deploy-gate
      contract."
    command: "gh pr view 1757 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1757"
    description: "omnibase_infra PR #1757 carries the boundary dataclass to Pydantic BaseModel implementation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12184/dod-omnibase-infra-pr-1757/command.yaml
          && grep -q '^pr_number: 1757$' drift/dod_receipts/OMN-12184/dod-omnibase-infra-pr-1757/command.yaml"
  - id: "dod-topic-literals"
    description: "Raw topic literal invariant passes after deriving demo reset prefixes from canonical
      topic constants."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12184/dod-topic-literals/command.yaml"
  - id: "dod-demo-reset-tests"
    description: "Focused demo reset unit tests pass after the prefix derivation fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12184/dod-demo-reset-tests/command.yaml"
  - id: "dod-deploy-gate"
    description: "Deploy-gate evidence: no pre-merge live deploy required; next runtime image build carries
      the boundary model refactor."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12184/dod-deploy-gate/command.yaml
          && grep -q 'deploy-gate' drift/dod_receipts/OMN-12184/dod-deploy-gate/command.yaml"
