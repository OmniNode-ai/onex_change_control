---
schema_version: "1.0.0"
ticket_id: "OMN-14851"
title: "Autobind OCC evidence for OMN-14851"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1841."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1841 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1841"
    description: "PR #1841 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1841-ci"
    description: "PR #1841 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-omnimarket-pr-1841-deploy-scope"
    description: "Deploy-scope assessment for omnimarket#1841 runtime projection scaffold."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy scope assessed for omnimarket#1841 runtime projection scaffold;
          no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'"
  - id: "occ-self-bind-pr-4505"
    description: "OCC companion PR #4505 — self-bind for OMN-14851 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4507"
    description: "OCC deploy evidence PR #4507 — self-bind for OMN-14851."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
