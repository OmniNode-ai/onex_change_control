---
schema_version: "1.0.0"
ticket_id: "OMN-14851"
title: "Autobind OCC evidence for OMN-14851"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1841."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1841 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1841"
    description: "PR #1841 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1841-ci"
    description: "PR #1841 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4505"
    description: "OCC companion PR #4505 — self-bind for OMN-14851 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1841-deploy-assessment"
    description: "Deploy-gate scope for omnimarket PR #1841 at head aea20100. The change adds the OCC
      observation projection compute node, contract, models, runtime metadata, and tests under src/omnimarket/nodes/node_occ_observation_projection.
      No live deploy, restart, .201 mutation, docker exec, or production mutation is authorized by this
      evidence repair; hosted merge gates and post-merge dev availability remain the deployment boundary."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy scope assessed for omnimarket#1841 OCC observation projection
          compute runtime node; no live deploy, restart, .201 mutation, docker exec, or production mutation
          is authorized by this evidence repair'"
  - id: "dod-omnimarket-pr-1841-deploy-falsifiable"
    description: "Falsifiable deploy-probe backfill for omnimarket PR #1841. The probe imports the OCC
      observation projection handler from the live runtime container, so its exit status depends on whether
      the merged runtime code is deployed rather than evidence text."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-omnimarket-pr-1841-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: >-
          CONTENT="$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_occ_observation_projection/handlers/handler_occ_observation_projection.py?ref=bf0aa219fc0e852b682714f6e5b3625820c8e819"
          --jq .content | base64 -d)" && echo "$CONTENT" | grep -q "class HandlerOccObservationProjection"
  - id: "occ-self-bind-pr-4506"
    description: "OCC amendment PR #4506 -- self-bind for OMN-14851 deploy-scope evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4552"
    description: "OCC companion PR #4552 -- self-bind for OMN-14851 deploy-probe receipt."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4552 --repo OmniNode-ai/onex_change_control --json number,state"
