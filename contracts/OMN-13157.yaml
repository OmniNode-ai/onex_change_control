---
schema_version: "1.0.0"
ticket_id: "OMN-13157"
title: "test(OMN-13157): next_eligible_tier honors task-class tier_order (closed-set regression tests)"
summary: >-
  OCC evidence for omnimarket PR #1339. Adds TestClosedSetTierOrderOMN13157 to test_delegation_tier_escalation.py
  — four focused regression tests that directly assert the three closed-set acceptance criteria for next_eligible_tier:
  (1) after cheap_cloud fails, code_generation escalates to local (contract order respected over declaration
  order); (2) task_type=None preserves routing_tiers.yaml declaration order (backward compat); (3) tiers
  absent from tier_order are excluded without needing them in excluded_tiers. The handler fix (removing
  the append-unlisted code from _tier_order_from_contract) was already present in dev; this PR adds the
  regression guard so the bug cannot reopen undetected.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-closed-set-tests"
    description: >-
      Four new tests in TestClosedSetTierOrderOMN13157 pass green: (1) test_code_generation_after_cheap_cloud_escalates_to_local
      — after cheap_cloud, code_generation returns local (not cheap_frontier, which is absent from the
      closed set); (2) test_code_generation_after_local_no_routable_ceiling — after local, claude tier
      is unconfigured so returns None (ceiling honored); (3) test_cheap_frontier_excluded_from_code_generation_by_closed_set
      — proves cheap_frontier is reachable in declaration order but excluded by closed-set; (4) test_task_type_none_preserves_declaration_order
      — full declaration order chain confirmed for backward compat. Full suite: 7919 passed, 64 skipped
      (not kafka). mypy strict: 0 issues. pre-commit: all 25 hooks passed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'PASS' drift/dod_receipts/OMN-13157/dod-closed-set-tests/command.yaml
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13157.yaml consumed
      by the omnimarket PR #1339 Receipt Gate via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13157\"$' contracts/OMN-13157.yaml"
  - id: "dod-deploy-gate"
    description: >-
      Deploy-gate evidence: no live deploy, restart, docker exec, rpk topic produce, or runtime lane mutation
      was performed. This PR adds regression tests only — no deployment surface is touched.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c 'deploy-gate evidence' contracts/OMN-13157.yaml"
