---
schema_version: "1.0.0"
ticket_id: "OMN-12188"
title: "Migrate EventEnvelopeV1Minimal from compat to core"
summary: >
  Central OCC evidence for omnibase_core PR #1161. The change adds the minimal event envelope to omnibase_core,
  preserves the EventEnvelopeV1Minimal import alias, and proves focused model tests plus naming, typing,
  and lint gates.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Event envelope unit tests pass on the PR head."
    command: "See drift/dod_receipts/OMN-12188/dod-focused-model-tests/command.yaml"
  - kind: "tests"
    description: "Class naming validation passes after adding the Model-prefixed envelope class."
    command: "See drift/dod_receipts/OMN-12188/dod-class-naming/command.yaml"
  - kind: "tests"
    description: "Strict mypy validation passes for the new envelope model source file."
    command: "See drift/dod_receipts/OMN-12188/dod-strict-mypy/command.yaml"
  - kind: "tests"
    description: "Targeted ruff format and lint checks pass for touched envelope files and tests."
    command: "See drift/dod_receipts/OMN-12188/dod-targeted-ruff/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carries the central OMN-12188 contract and receipts."
    command: "See drift/dod_receipts/OMN-12188/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-model-tests"
    description: "Focused event envelope model tests passed on the omnibase_core PR #1161 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12188/dod-focused-model-tests/command.yaml
          && grep -q '6 passed' drift/dod_receipts/OMN-12188/dod-focused-model-tests/command.yaml"
  - id: "dod-class-naming"
    description: "Class naming validation passed after exposing ModelEventEnvelopeV1Minimal."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12188/dod-class-naming/command.yaml
          && grep -q 'conform to naming conventions' drift/dod_receipts/OMN-12188/dod-class-naming/command.yaml"
  - id: "dod-strict-mypy"
    description: "Strict mypy validation passed for the new envelope model source file."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12188/dod-strict-mypy/command.yaml
          && grep -q 'Success: no issues found in 1 source file' drift/dod_receipts/OMN-12188/dod-strict-mypy/command.yaml"
  - id: "dod-targeted-ruff"
    description: "Targeted ruff checks passed for touched envelope files and tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12188/dod-targeted-ruff/command.yaml
          && grep -q 'All checks passed' drift/dod_receipts/OMN-12188/dod-targeted-ruff/command.yaml"
  - id: "dod-occ-pr"
    description: "This onex_change_control PR carries the central OMN-12188 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12188/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: \"OMN-12188\"$' drift/dod_receipts/OMN-12188/dod-occ-pr/command.yaml"
