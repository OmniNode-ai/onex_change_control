---
schema_version: "1.0.0"
ticket_id: "OMN-11222"
title: "feat(OMN-11222): add demo_readiness_team skill shim"
summary: >
  Adds the omniclaude demo_readiness_team skill shim and generated node_skill_demo_readiness_team_orchestrator
  shell for omniclaude PR #1605. The skill delegates demo rehearsal, drift detection, bounded fix dispatch,
  and morning handoff generation to existing omnimarket demo readiness nodes. The skill document explicitly
  blocks live deploys, production lane restarts, topology-affecting merges, and production data mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omniclaude PR #1605 is open at the recorded head commit."
    command: "gh pr view 1605 --repo OmniNode-ai/omniclaude --json state,headRefOid,url"
  - kind: "manual"
    description: "Generated orchestrator contract and skill authority boundary are present at the PR head."
    command: "gh api repos/OmniNode-ai/omniclaude/contents/src/omniclaude/nodes/node_skill_demo_readiness_team_orchestrator/contract.yaml?ref=c8af1f536ff2a8cd0f67088d0bb07f704e21693f"
  - kind: "ci"
    description: "Current omniclaude PR #1605 non-evidence checks pass; deploy-gate and Receipt Gate remain
      blocked pending this OCC evidence."
    command: "gh pr checks 1605 --repo OmniNode-ai/omniclaude"
  - kind: "manual"
    description: "No live deploy or restart was performed for this evidence PR; post-merge deploy validation
      is by rerunning the downstream deploy-gate."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11222/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1605"
    description: "omniclaude PR #1605 carries OMN-11222 at head commit c8af1f536ff2a8cd0f67088d0bb07f704e21693f."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11222/dod-omniclaude-pr-1605/command.yaml
          && grep -q '^pr_number: 1605$' drift/dod_receipts/OMN-11222/dod-omniclaude-pr-1605/command.yaml
          && grep -q '^commit_sha: \"c8af1f536ff2a8cd0f67088d0bb07f704e21693f\"$' drift/dod_receipts/OMN-11222/dod-omniclaude-pr-1605/command.yaml"
  - id: "dod-skill-boundary"
    description: "The skill and generated orchestrator shell preserve the runtime authority boundary:
      dispatch-only skill shell, generated ORCHESTRATOR_GENERIC contract, no claimed live deploy."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11222/dod-skill-boundary/command.yaml
          && grep -q 'Deploy runtime changes | NEVER' drift/dod_receipts/OMN-11222/dod-skill-boundary/command.yaml
          && grep -q 'node_type: ORCHESTRATOR_GENERIC' drift/dod_receipts/OMN-11222/dod-skill-boundary/command.yaml"
  - id: "dod-product-ci-gates"
    description: "omniclaude PR #1605 has passing build, tests, skill validation, CodeRabbit, and DoD
      checks; deploy-gate and Receipt Gate are the known evidence blockers."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11222/dod-product-ci-gates/command.yaml
          && grep -q 'Tests (Split 1/1).*pass' drift/dod_receipts/OMN-11222/dod-product-ci-gates/command.yaml
          && grep -q 'deploy-gate.*fail' drift/dod_receipts/OMN-11222/dod-product-ci-gates/command.yaml
          && grep -q 'verify / verify.*fail' drift/dod_receipts/OMN-11222/dod-product-ci-gates/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy, runtime restart, .201 mutation, or production data mutation was performed.
      Downstream deploy-gate should be rerun after this OCC Evidence-Source is cited."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11222/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-11222/dod-deploy-assessment/command.yaml
          && grep -q 'post-merge validation command' drift/dod_receipts/OMN-11222/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carrying the central OMN-11222 contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11222/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: \"OMN-11222\"$' drift/dod_receipts/OMN-11222/dod-occ-pr/command.yaml"
