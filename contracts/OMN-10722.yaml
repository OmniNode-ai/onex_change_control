---
schema_version: "1.0.0"
ticket_id: "OMN-10722"
title: "Track omniintelligence python-multipart dependabot bump"
summary: >
  Binds omniintelligence Dependabot PR #643, which bumps python-multipart from 0.0.26 to 0.0.27, to central
  OCC evidence so Receipt Gate can verify the dependency maintenance update without bypassing branch protection.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-643-head"
    description: "omniintelligence PR #643 carries the python-multipart dependency bump head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10722/dod-pr-643-head/command.yaml\""
  - id: "dod-ci-643-green"
    description: "All non-receipt CI checks on omniintelligence PR #643 are passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10722/dod-ci-643-green/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10722/dod-occ-pr/command.yaml\""
