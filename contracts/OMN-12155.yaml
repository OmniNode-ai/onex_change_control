---
schema_version: "1.0.0"
ticket_id: "OMN-12155"
title: "Pin prod k8s image tags"
summary: >
  Removes :latest from omniintelligence prod deployment (replaced with :placeholder consistent
  with dev pattern and OMN-10856 TODO). Adds Kustomize images transformer to omniintelligence
  prod kustomization so the tag is overridden at deploy time. Documents the CI mechanism for
  the 9 omninode-runtime :placeholder tags already covered by an existing digest override in
  runtime/kustomization.yaml.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-471"
    description: >
      omninode_infra PR #471 merged — removes :latest from omniintelligence prod deployment,
      adds Kustomize images transformer, documents runtime :placeholder CI mechanism.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12155/dod-infra-pr-471/command.yaml"
  - id: "dod-no-latest-in-prod"
    description: >
      No :latest image tags remain in k8s/onex-prod/ after this change. Verified via grep
      against the merged PR commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12155/dod-no-latest-in-prod/command.yaml"
