---
schema_version: "1.0.0"
ticket_id: "OMN-12155"
title: "Pin prod k8s image tags"
summary: >
  Removes :latest from omniintelligence prod deployment (replaced with :placeholder consistent
  with dev pattern and OMN-10856 TODO). Adds Kustomize images transformer to omniintelligence
  prod kustomization so the tag is overridden at deploy time. Documents the CI mechanism for
  the 9 omninode-runtime :placeholder tags already covered by an existing digest override in
  runtime/kustomization.yaml.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-471"
    description: >
      omninode_infra PR #471 — remove :latest from omniintelligence prod deployment, add
      Kustomize images transformer, document runtime :placeholder CI mechanism.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "pr_merged"
        check_value: "OmniNode-ai/omninode_infra#471"
  - id: "dod-kustomize-verified"
    description: >
      kubectl kustomize k8s/onex-prod/runtime resolves all omninode-runtime images to pinned
      SHA256 digest. kubectl kustomize k8s/onex-prod/omniintelligence shows :placeholder
      (no :latest). Verified locally before push.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12155/dod-kustomize-verified/command.yaml"
  - id: "dod-no-latest-in-prod"
    description: >
      No :latest image tags remain in k8s/onex-prod/ after this change. Verified via grep.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "! grep -rn ':latest' k8s/onex-prod/ --include='*.yaml' | grep 'image:'"
