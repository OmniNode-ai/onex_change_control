---
schema_version: "1.0.0"
ticket_id: "OMN-11591"
title: "Implement generate_reverse_patch decision gate"
summary: >
  Central OCC evidence for omnibase_core PR #1154, which implements the previously-stubbed generate_reverse_patch
  method in ContractDiffComputer. The stub raised NotImplementedError (orphaned after OMN-1148 Phase 4
  was never assigned to OMN-1149). The implementation inverts list-operation diffs into a ModelContractPatch,
  raises ValueError for scalar changes that cannot be expressed as patch operations, and skips object-typed
  removed list items with a debug log.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1154"
    description: "omnibase_core PR #1154 carries the OMN-11591 generate_reverse_patch implementation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=1154; REPO=OmniNode-ai/omnibase_core; gh pr view \"$PR_NUMBER\" --repo
          \"$REPO\" --json number,state,headRefOid,headRefName,title"
  - id: "dod-local-validation"
    description: "10 new TestGenerateReversePatch unit tests pass; 830/830 contracts tests pass; mypy
      --strict clean on modified file; ruff format and check clean."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11591/dod-local-validation/command.yaml
          && grep -q '830/830 contracts' drift/dod_receipts/OMN-11591/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      No live deploy required. generate_reverse_patch is a library function with no topic declarations,
      schema changes, migrations, or configuration updates. The fix applies on next import.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy: no live deploy required; generate_reverse_patch is a pure library
          function with no runtime topology changes'"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC evidence for OMN-11591."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11591/dod-occ-pr/command.yaml"
