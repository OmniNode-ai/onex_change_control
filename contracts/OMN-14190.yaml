---
schema_version: "1.0.0"
ticket_id: "OMN-14190"
title: "feat(OMN-14190): stamp and validate OCC PR metadata locally"
summary: >-
  OMN-14190 adds a paired shift-left OCC stamp validation path. omnibase_infra PR #2245 adds the `onex
  occ` CLI group for canonical Evidence-Source and Evidence-Ticket stamp validation/rendering through
  the shared omnibase_compat PR OCC stamp schema. omniclaude PR #1875 adds a thin pre-commit hook that
  shells out to `onex occ validate` for staged markdown/text artifacts, without duplicating stamp parsing
  logic or adding a direct compat dependency in omniclaude.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2245"
    description: >-
      Live GitHub verification shows omnibase_infra PR #2245 targets dev from branch jonah/omn-14190-occ-stamp-validate-cli
      at head fd90ac0258a9daef00b15ce485c84f7732080cdb. The diff adds src/omnibase_infra/cli/cli_occ.py,
      tests/unit/cli/test_cli_occ.py, and dependency/lock updates for omnibase-compat 0.5.6.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14190/dod-infra-pr-2245/command.yaml && grep
          -q '^pr_number: 2245$' drift/dod_receipts/OMN-14190/dod-infra-pr-2245/command.yaml
  - id: "dod-infra-verification"
    description: >-
      Independent verification in the clean OMN-14190 omnibase_infra worktree resolved published omnibase-compat
      0.5.6, ran the focused OCC CLI tests plus version compatibility tests, and passed Ruff format/check
      over the changed CLI source and tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14190/dod-infra-verification/command.yaml &&
          grep -q '49 passed' drift/dod_receipts/OMN-14190/dod-infra-verification/command.yaml
  - id: "dod-omniclaude-pr-1875"
    description: >-
      Live GitHub verification shows omniclaude PR #1875 targets dev from branch jonah/omn-14190-occ-stamp-hook
      at head 3ccd27833b274cedaf69c83e9c809c8a073f296b. The diff adds a validate-occ-pr-stamp shell hook,
      wires it into pre-commit, and adds focused hook tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14190/dod-omniclaude-pr-1875/command.yaml &&
          grep -q '^pr_number: 1875$' drift/dod_receipts/OMN-14190/dod-omniclaude-pr-1875/command.yaml
  - id: "dod-omniclaude-verification"
    description: >-
      Independent verification in the clean OMN-14190 omniclaude worktree passed the focused hook tests,
      the hook's all-files no-artifact path, and the changed-file pre-commit suite for the hook, pre-commit
      config, and test file.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14190/dod-omniclaude-verification/command.yaml
          && grep -q '4 passed, 2 skipped' drift/dod_receipts/OMN-14190/dod-omniclaude-verification/command.yaml
  - id: "dod-occ-companion-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14190 contract and
      PASS receipts consumed by omnibase_infra PR #2245 and omniclaude PR #1875 through their Evidence-Source
      lines.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14190/dod-occ-companion-self/command.yaml
