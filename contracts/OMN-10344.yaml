---
schema_version: "1.0.0"
ticket_id: "OMN-10344"
title: "Task 13: Add repeatable cost observability proof-of-life e2e"
summary: "Add a repeatable proof-of-life/e2e test for cost observability snapshot topics, proving exact
  fixture/golden payloads for cost.summary.v1, cost.by_repo.v1, and cost.token_usage.v1 while preserving
  a deterministic skip when live Kafka is not enabled."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
evidence_requirements:
  - kind: "tests"
    description: "Repeatable gated pytest path passes, including the expected Kafka e2e skip when KAFKA_INTEGRATION_TESTS
      and KAFKA_BOOTSTRAP_SERVERS are not set"
    command: "cd omnibase_infra && PYTHONPATH=/Users/jonah/Code/omni_home/omni_worktrees/OMN-10342/omniintelligence/src
      uv run pytest tests/proof_of_life/test_cost_observability_e2e.py tests/integration/nodes/test_node_projection_cost_snapshots.py
      tests/integration/nodes/test_node_runner_usage_effect.py tests/integration/api/test_cost_api_routes.py
      -q"
  - kind: "tests"
    description: "Live Kafka e2e was attempted with docker/docker-compose.e2e.yml and documented as blocked
      by Docker daemon availability on the local runner"
    command: "cd omnibase_infra && docker compose -f docker/docker-compose.e2e.yml up -d"
  - kind: "ci"
    description: "Implementation PR for OMN-10344 was merged"
    command: "gh pr view 1466 --repo OmniNode-ai/omnibase_infra --json number,state,mergeCommit,url"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Repeatable gated pytest path passed with the expected Kafka e2e skip"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10344/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-10344$' drift/dod_receipts/OMN-10344/dod-001/command.yaml && grep -q 'expected
          Kafka e2e skip' drift/dod_receipts/OMN-10344/dod-001/command.yaml"
  - id: "dod-002"
    description: "Live Kafka e2e attempt was made and blocked by unavailable Docker daemon"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10344/dod-002/command.yaml && grep
          -q 'Cannot connect to the Docker daemon' drift/dod_receipts/OMN-10344/dod-002/command.yaml"
  - id: "dod-003"
    description: "omnibase_infra implementation PR #1466 was merged"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10344/dod-003/command.yaml && grep
          -q '^pr_number: 1466$' drift/dod_receipts/OMN-10344/dod-003/command.yaml && grep -q 'MERGED'
          drift/dod_receipts/OMN-10344/dod-003/command.yaml"
