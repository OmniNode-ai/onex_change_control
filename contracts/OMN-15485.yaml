---
schema_version: "1.0.0"
ticket_id: "OMN-15485"
title: "Autobind OCC evidence for OMN-15485"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #1975.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1975 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1975"
    description: "PR #1975 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1975 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: "gh pr view 1975 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #1975 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 1975 --repo OmniNode-ai/omnimarket --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-1975"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5626"
    description: "Binds OMN-15485 to OCC companion PR #5626 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5626 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2035"
    description: >
      PR #2035 on OmniNode-ai/omnimarket — sibling-producer port of this ticket's fix. Root cause: `occ_companion_emitter.py`
      (the live node_pr_lifecycle_fix_effect producer) unconditionally write_text()'d the admissibility-validator
      receipt at the fixed, ticket-scoped ADMISSIBILITY_VALIDATOR_EVIDENCE_ID path on every companion
      mint, colliding whenever a ticket got more than one companion — the same defect class this ticket
      fixed on node_occ_companion_compute, never ported to the sibling live-effect producer. Fix: never-overwrite
      guard added at 4 sites (skip the write; exclude from both _rebind_receipts passes; exclude from
      both _assert_append_only allow-lists). Mutation-proven: RED-first test reproduces the live OCC#6264->#6276
      collision against pre-fix code, GREEN after. Residual: this ticket's own AC2 (generalized producer-side
      append-only invariant that fails loudly rather than silently on any merged-path collision, across
      ALL emission sites) is scoped to the compute-node ModelCompanionFile-return architecture and remains
      open — #2035 is a point-fix guard on the effect producer, not that invariant.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2035 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2035 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-6291"
    description: "Binds OMN-15485 to OCC companion PR #6291 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6291 --repo OmniNode-ai/onex_change_control --json number,state"
