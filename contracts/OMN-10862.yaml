---
schema_version: "1.0.0"
ticket_id: "OMN-10862"
title: "Prove useful delegation test artifact chain"
summary: >
  Central OCC evidence for omnimarket PR #1058, which proves delegation can route a unit-test request
  through the live inference workflow and produce a useful pytest artifact, not just any model response.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1058"
    description: "omnimarket PR #1058 carries the durable useful delegation artifact-chain implementation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10862/dod-omnimarket-pr-1058/command.yaml
          && grep -q '^pr_number: 1058$' drift/dod_receipts/OMN-10862/dod-omnimarket-pr-1058/command.yaml"
  - id: "dod-local-validation"
    description: "Focused unit and golden-chain checks prove useful pytest artifact semantics."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10862/dod-local-validation/command.yaml
          && grep -q '140 passed' drift/dod_receipts/OMN-10862/dod-local-validation/command.yaml"
  - id: "dod-live-dev-proof"
    description: "Hotpatched dev run produced a useful pytest artifact with passing quality gates."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10862/dod-live-dev-proof/command.yaml
          && grep -q 'b29b9532-df6a-45e4-8024-04461604d3cc' drift/dod_receipts/OMN-10862/dod-live-dev-proof/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Runtime-path deploy gate evidence: merge requires a post-merge dev redeploy and fresh
      useful delegation proof before release."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10862/dod-deploy-assessment/command.yaml
          && grep -q 'post-merge dev deploy' drift/dod_receipts/OMN-10862/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2209"
    description: "onex_change_control PR #2209 carries the central delegation integration evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10862/dod-occ-pr-2209/command.yaml
          && grep -q '^pr_number: 2209$' drift/dod_receipts/OMN-10862/dod-occ-pr-2209/command.yaml"
  - id: "dod-occ-pr-2210"
    description: "onex_change_control PR #2210 carries deploy-gate evidence for delegation release readiness."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10862/dod-occ-pr-2210/command.yaml
          && grep -q '^pr_number: 2210$' drift/dod_receipts/OMN-10862/dod-occ-pr-2210/command.yaml"
