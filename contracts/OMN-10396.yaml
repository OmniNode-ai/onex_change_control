---
schema_version: "1.0.0"
ticket_id: OMN-10396
title: "Implement text handler"
summary: "Add the shared text output handler for typed market CLI reports."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Text output handler tests pass"
    command: "uv run pytest tests/unit/cli/output/handlers/test_handler_text.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Text output handler tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/cli/output/handlers/test_handler_text.py -q"
  - id: "dod-002"
    description: "No deploy required; text rendering is local CLI presentation logic"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for OMN-10396 - local CLI renderer only'"
