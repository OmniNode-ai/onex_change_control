---
schema_version: "1.0.0"
ticket_id: OMN-10759
title: "feat(OMN-10759): port change-aware smart test selection to omnimarket"
summary: >
  Ports the change-aware selective test execution system from omnibase_core to omnimarket. Adds scripts/ci
  detector, loader, models, adjacency map, pytest-split dependency, and a dynamic detect-changes/test
  matrix in CI.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnimarket PR #589 carries the smart test selection implementation"
    command: "gh pr view 589 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "scripts/ci/detect_test_paths.py is present on omnimarket PR #589"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10759/dod-001/command.yaml"
  - id: dod-002
    description: "The omnimarket detector runs locally using direct script execution"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10759/dod-002/command.yaml"
  - id: dod-003
    description: "omnimarket PR #589 title/body cites OMN-10759"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10759/dod-003/command.yaml"
