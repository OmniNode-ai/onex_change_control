---
schema_version: "1.0.0"
ticket_id: "OMN-11468"
title: "Evidence Pipeline Live Dashboard — Projection-Backed Event Flow Visualization"
summary: >-
  Parent evidence for the OMN-11468 dashboard cleanup wave across omnibase_compat, omnimarket, and omnidash.
  The dashboard remains a rendering surface; reducer-owned projections and OCC receipts remain the authoritative
  proof surfaces.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
  - topics
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "Open child PRs are recorded at their current heads."
    command: "See drift/dod_receipts/OMN-11468/dod-*-pr-*/command.yaml"
  - kind: "manual"
    description: "No live deploy is required for this OCC evidence-only PR."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11468/dod-deploy-assessment/command.yaml"
  - kind: "manual"
    description: "The central OCC PR is bound to this parent ticket."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11468/dod-occ-pr-1305/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-compat-pr-99"
    description: "omnibase_compat PR #99 carries the Wave 1 dashboard DTO branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11468/dod-omnibase-compat-pr-99/command.yaml\""
  - id: "dod-omnimarket-pr-739"
    description: "omnimarket PR #739 carries the Wave 2 and Wave 3 projection/API branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11468/dod-omnimarket-pr-739/command.yaml\""
  - id: "dod-omnidash-pr-104"
    description: "omnidash PR #104 carries the Wave 3 dashboard rendering branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11468/dod-omnidash-pr-104/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Central OCC receipt evidence only; no runtime deploy or restart is required before merging
      this OCC PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11468/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-1305"
    description: "onex_change_control PR #1305 carries the parent OMN-11468 central OCC evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11468/dod-occ-pr-1305/command.yaml\""
