---
schema_version: "1.0.0"
ticket_id: "OMN-12222"
title: "Wave 5 new nodes — contract drift compute + verification sweep orchestrator"
summary: >
  OCC evidence for omnimarket PR #910. The change adds two Wave 5 stub nodes: node_contract_drift_compute
  (COMPUTE, pure, idempotent) and node_verification_sweep_orchestrator (ORCHESTRATOR, impure, idempotent).
  Both are contract-only stubs with node_not_implemented: true. 42 golden-chain tests total.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain"
    description: "42 golden-chain tests pass for both Wave 5 nodes on omnimarket PR #910 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12222/dod-golden-chain/command.yaml
          && grep -q '42 passed' drift/dod_receipts/OMN-12222/dod-golden-chain/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy or runtime restart is required before merge for this contract + handler-stub
      slice."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12222/dod-deploy-assessment/command.yaml
          && grep -q 'no live deploy or runtime restart required before merge' drift/dod_receipts/OMN-12222/dod-deploy-assessment/command.yaml"
