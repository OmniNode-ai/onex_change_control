---
schema_version: "1.0.0"
ticket_id: "OMN-16689"
title: "Autobind OCC evidence for OMN-16689"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #1074.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1074 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #1074 on OmniNode-ai/omninode_infra changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (k8s/onex-dev/api/deployment.yaml, k8s/onex-dev/runtime/configmap.yaml, k8s/onex-dev/runtime/deployment-omnimarket-projection-api.yaml, k8s/onex-dev/runtime/deployment-omnimarket-projection-tenant-credentials-writer.yaml) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1074"
    description: "PR #1074 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1074 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1074/files --paginate --jq '.[].filename' | grep -cFx 'k8s/onex-dev/api/deployment.yaml'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1074"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7632"
    description: "Binds OMN-16689 to OCC companion PR #7632 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7632 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-7648"
    description: "Binds OMN-16689 to OCC companion PR #7648 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7648 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omninode-infra-pr-1075-cred-scope"
    description: >-
      Behavior proof for PR #1075 on OmniNode-ai/omninode_infra — credential-scope hygiene. #1075 removes
      the org-admin `operator-k8s` Universal Auth pair from Infisical /dev/onex-runtime/ (the one path
      the least-privilege `onex-runtime-store-resolver` identity is granted READ on, so the low-privilege
      token could read the pair and log in as org admin), repoints the four runtime Deployments at onex-store-resolver-credentials
      with optional:false, retires the three C2 masking waivers those keys carried and tightens the waiver
      ratchet 25 -> 22. This is the hand-authored `source: manual` behavior proof the contract's `evidence_requirements`
      recorded as OWED, discharged for the successor PR. The gates that prove it (scripts/validate_secret_ownership.py
      C1-C7 and tests/k8s/test_secret_ownership_manifest.py, test_runtime_secret_key_coverage.py, test_omn_16558_required_secret_preflight.py)
      live in omninode_infra, not in this repo; the receipt carries their recorded output and the checks
      below assert the diff those gates ran against.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1075/files --paginate --jq '.[].filename' | grep -cFx 'k8s/onex-dev/secret-ownership-manifest.yaml'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1075/files --paginate --jq '.[] | select(.filename == "scripts/secret-ownership-waiver-baseline.txt") | .patch' | grep -cE '^\+22$'
  - id: "dod-OmniNode-ai-omninode_infra-pr-1075"
    description: "PR #1075 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1075 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1075-ci"
    description: "PR #1075 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1075 --repo OmniNode-ai/omninode_infra --json files"
