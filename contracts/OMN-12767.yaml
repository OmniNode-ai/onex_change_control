---
schema_version: "1.0.0"
ticket_id: "OMN-12767"
title: ".201 dev runtime lane torn down unattended — demo outage RCA and guard"
summary: >-
  The omnibase-infra Docker Compose project (dev lane, ports 8085/3002/5436/19092) was fully torn down
  at ~03:52 UTC 2026-06-07 after a healthy deploy chain. All dev lane containers were absent; prod and
  stability-test lanes were unaffected. Root cause is unknown (possible: OMN-12765 release-train automation
  issued compose down on dev lane). Recovery completed at ~06:55 UTC by syncing sibling clones to dev
  HEAD, baking in two demo contract fixes, and running deploy-runtime.sh --execute --force --restart.
  Closeout requires: identifying which process or script issued 'docker compose -p omnibase-infra down'
  at ~03:52 UTC and adding a guard to prevent unattended dev lane teardown.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-rca-cause-identified"
    description: >-
      Root cause of unattended 'docker compose -p omnibase-infra down' at ~03:52 UTC 2026-06-07 identified
      with evidence: specific script/process/automation actor named.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12767/dod-rca-cause-identified/command.yaml
  - id: "dod-teardown-guard-added"
    description: >-
      A mechanical guard is in place preventing unattended dev lane teardown. Guard wired as a code change
      (not advisory) with a test or pre-flight check.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12767/dod-teardown-guard-added/command.yaml
  - id: "dod-dev-lane-healthy-post-restore"
    description: >-
      Dev lane restored with all containers healthy: omninode-runtime (8085), omnimarket-projection-api
      (3002), omninode-runtime-effects, omninode-contract-resolver, postgres, redpanda, valkey. Restore
      evidence captured in ticket description.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12767/dod-dev-lane-healthy-post-restore/command.yaml
          && grep -q 'omninode-runtime' drift/dod_receipts/OMN-12767/dod-dev-lane-healthy-post-restore/command.yaml
  - id: "dod-occ-evidence"
    description: "onex_change_control OCC contract + receipts for OMN-12767."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12767/dod-occ-evidence/command.yaml && grep
          -q '^ticket_id: "OMN-12767"$' contracts/OMN-12767.yaml
