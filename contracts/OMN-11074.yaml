---
schema_version: "1.0.0"
ticket_id: "OMN-11074"
title: "Publish delegate skill direct payload"
summary: >
  Central OCC evidence for omnimarket PR #658. Aligns the Codex runtime adapter so direct delegate-skill
  dispatches publish a ModelDelegateSkillRequest envelope with event_type omnimarket.delegate-skill while
  keeping the existing Pattern B command wrapping for all other runtime requests, and forces the canonical
  command correlation_id onto the delegate payload so terminal envelope matching cannot silently time
  out on a mismatched payload-level id.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused delegate-skill adapter, orchestrator, and Codex runtime client tests pass."
    command: "See drift/dod_receipts/OMN-11074/dod-focused-tests/command.yaml"
  - kind: "ci"
    description: "Touched delegate runtime client and adapter test files pass ruff."
    command: "See drift/dod_receipts/OMN-11074/dod-lint/command.yaml"
  - kind: "ci"
    description: "Central OCC PR carries the OMN-11074 contract and receipt bundle."
    command: "See drift/dod_receipts/OMN-11074/dod-occ-pr-1054/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused delegate-skill adapter, orchestrator, and Codex runtime client tests passed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '78 passed' drift/dod_receipts/OMN-11074/dod-focused-tests/command.yaml"
  - id: "dod-lint"
    description: "Ruff check passed for touched delegate runtime client and adapter files."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'All checks passed' drift/dod_receipts/OMN-11074/dod-lint/command.yaml"
  - id: "dod-occ-pr-1054"
    description: "onex_change_control PR #1054 carries the central OMN-11074 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11074/dod-occ-pr-1054/command.yaml
          && grep -q '^pr_number: 1054$' drift/dod_receipts/OMN-11074/dod-occ-pr-1054/command.yaml"
