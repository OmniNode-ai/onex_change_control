---
schema_version: "1.0.0"
ticket_id: "OMN-18921"
title: "Hand-authored OCC evidence for OMN-18921"
summary: |-
  OCC contract hand-authored for onex_change_control PR #10602, which registers the OCC self-bind
  minter as a reviewed scripts exception. Hand-authored rather than autobound because the change is
  confined to allowlists/, which the autobind producer does not mint for.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #10602 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Content proof for PR #10602 on OmniNode-ai/onex_change_control, derived from the PR's own diff and
      measured in both directions before authoring: the registered exception path is present in
      allowlists/scripts_exceptions.yaml at this PR's commit (count 1, exit 0) and absent at the merge base
      de30484d9b70bf55e75bdebe2269e3ce4055bc77 (count 0, exit 1), so the check is falsifiable rather than a
      tautology pinned to a head SHA.
    command: |-
      gh api repos/OmniNode-ai/onex_change_control/contents/allowlists/scripts_exceptions.yaml?ref=4214b3c9b51065dd28330a64027d0b3956d6ec31 --jq '.content' | base64 -d | grep -c 'path: scripts/ci/mint_occ_self_bind.py'
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-onex_change_control-pr-10602"
    description: "PR #10602 on OmniNode-ai/onex_change_control — hand-authored evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/contents/allowlists/scripts_exceptions.yaml?ref=4214b3c9b51065dd28330a64027d0b3956d6ec31 --jq '.content' | base64 -d | grep -c 'path: scripts/ci/mint_occ_self_bind.py'
