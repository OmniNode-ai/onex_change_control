---
schema_version: "1.0.0"
ticket_id: "OMN-10723"
title: "Remove silent inprocess fallback from delegation skill and add no-inprocess-fallback-in-skills
  pre-commit gate"
summary: "Delete _inprocess_fallback and InProcessDelegationRunner import from delegate skill; force_local=True
  now returns explicit error; add pre-commit gate blocking reintroduction."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Unit and delegation tests verifying force_local=True returns explicit error and no in-process
      execution occurs."
    command: "uv run pytest tests/delegation/test_e2e_inprocess_wiring.py tests/unit/hooks/lib/test_delegate_inprocess_fallback.py
      -v"
  - kind: "ci"
    description: "All CI checks pass on omniclaude PR #1544."
    command: "gh pr checks 1544 --repo OmniNode-ai/omniclaude --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "omniclaude PR #1544 CI all checks pass with inprocess fallback removed and tests updated."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10723/dod-001/command.yaml\"
          && grep -q '^ticket_id: OMN-10723$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10723/dod-001/command.yaml\"
          && grep -q '^pr_number: 852$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10723/dod-001/command.yaml\""
