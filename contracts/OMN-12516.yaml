---
schema_version: "1.0.0"
ticket_id: "OMN-12516"
title: "Fix Infisical catalog Redis auth in generated compose"
summary: >-
  Require the catalog-generated Infisical service to source REDIS_URL from the authenticated
  INFISICAL_REDIS_URL operator secret instead of hardcoding an unauthenticated Valkey URL. This
  addresses the .201 Infisical NOAUTH restart loop without hand-editing generated runtime state.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1803"
    description: "omnibase_infra PR #1803 carries the Infisical catalog Redis auth fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12516/dod-infra-pr-1803/command.yaml &&
          grep -q '^pr_number: 1803$' drift/dod_receipts/OMN-12516/dod-infra-pr-1803/command.yaml &&
          grep -q '^commit_sha: "7e973884368600a0a260e5c42f77e32ee8ac762f"$'
          drift/dod_receipts/OMN-12516/dod-infra-pr-1803/command.yaml
  - id: "dod-targeted-tests"
    description: "Focused Infisical catalog and stability compose render regression tests pass locally."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12516/dod-targeted-tests/command.yaml &&
          grep -q '9 passed' drift/dod_receipts/OMN-12516/dod-targeted-tests/command.yaml &&
          grep -q '6 passed' drift/dod_receipts/OMN-12516/dod-targeted-tests/command.yaml
  - id: "dod-generated-compose"
    description: "Catalog validation and generated compose prove Infisical REDIS_URL comes from INFISICAL_REDIS_URL."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12516/dod-generated-compose/command.yaml &&
          grep -q 'INFISICAL_REDIS_URL' drift/dod_receipts/OMN-12516/dod-generated-compose/command.yaml &&
          grep -q 'REDIS_URL' drift/dod_receipts/OMN-12516/dod-generated-compose/command.yaml
  - id: "dod-ruff-yaml"
    description: "Touched tests pass ruff checks and Infisical catalog YAML parses."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12516/dod-ruff-yaml/command.yaml &&
          grep -q 'All checks passed' drift/dod_receipts/OMN-12516/dod-ruff-yaml/command.yaml &&
          grep -q 'yaml ok' drift/dod_receipts/OMN-12516/dod-ruff-yaml/command.yaml
