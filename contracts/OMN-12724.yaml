---
schema_version: "1.0.0"
ticket_id: "OMN-12724"
title: "Fix duplicate delegation workflow consumption and terminal payload projection"
summary: >-
  Central OCC evidence for the complete OMN-12724 delegation runtime fix. The
  first infra PR #1874 prevents main and effects runtime processes from both
  consuming delegation workflow topics. omnimarket PR #1059 makes delegation
  dispatch contract-managed, removes the catch-all compliance bus route, and
  keeps workflow consumers owned by the intended runtime profile.
  omnibase_infra PR #1878 flattens topic-bearing delegation terminal envelopes
  so projections receive ModelDelegationResult payloads. Hotpatched .201 proof
  correlation ead5475c-be5d-415c-a1ab-e526a0faf3d9 completed a task_type=test
  delegation request through routing, local inference, quality gate, and
  delegation-completed with useful pytest content.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
  - "envelopes"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1874"
    description: "omnibase_infra PR #1874 carries the runtime profile ownership subscription fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12724/dod-omnibase-infra-pr-1874/command.yaml
          && grep -q '^pr_number: 1874$' drift/dod_receipts/OMN-12724/dod-omnibase-infra-pr-1874/command.yaml
  - id: "dod-local-validation"
    description: "Runtime auto-wiring and subscription wiring regression tests passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12724/dod-local-validation/command.yaml
          && grep -q '88 passed' drift/dod_receipts/OMN-12724/dod-local-validation/command.yaml
  - id: "dod-deploy-assessment"
    description: "Runtime ownership change requires post-merge dev deploy validation that only the owning profile consumes each delegation workflow topic."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12724/dod-deploy-assessment/command.yaml
          && grep -q 'post-merge dev deploy' drift/dod_receipts/OMN-12724/dod-deploy-assessment/command.yaml
  - id: "dod-occ-pr-2210"
    description: "onex_change_control PR #2210 carries deploy-gate evidence for the duplicate-consumer fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12724/dod-occ-pr-2210/command.yaml
          && grep -q '^pr_number: 2210$' drift/dod_receipts/OMN-12724/dod-occ-pr-2210/command.yaml
  - id: "dod-omnimarket-pr-1059"
    description: "omnimarket PR #1059 carries the contract-managed delegation workflow routing fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12724/dod-omnimarket-pr-1059/command.yaml
          && grep -q '^pr_number: 1059$' drift/dod_receipts/OMN-12724/dod-omnimarket-pr-1059/command.yaml
  - id: "dod-infra-pr-1878"
    description: "omnibase_infra PR #1878 carries the terminal delegation payload flattening fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12724/dod-infra-pr-1878/command.yaml
          && grep -q '^pr_number: 1878$' drift/dod_receipts/OMN-12724/dod-infra-pr-1878/command.yaml
  - id: "dod-dev-runtime-useful-delegation"
    description: "Hotpatched .201 dev runtime produced a useful pytest artifact through the full delegation chain."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12724/dod-dev-runtime-useful-delegation/command.yaml
          && grep -q 'ead5475c-be5d-415c-a1ab-e526a0faf3d9' drift/dod_receipts/OMN-12724/dod-dev-runtime-useful-delegation/command.yaml
  - id: "dod-occ-pr"
    description: "onex_change_control PR #2212 carries the expanded OMN-12724 contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12724/dod-occ-pr/command.yaml
