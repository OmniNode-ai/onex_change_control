---
schema_version: "1.0.0"
ticket_id: "OMN-12724"
title: "Respect runtime profile ownership for subscriptions"
summary: >
  Central OCC evidence for omnibase_infra PR #1874, which prevents main and effects runtime processes
  from both consuming the delegation inference workflow and duplicating terminal events.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1874"
    description: "omnibase_infra PR #1874 carries the runtime profile ownership subscription fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12724/dod-omnibase-infra-pr-1874/command.yaml
          && grep -q '^pr_number: 1874$' drift/dod_receipts/OMN-12724/dod-omnibase-infra-pr-1874/command.yaml"
  - id: "dod-local-validation"
    description: "Runtime auto-wiring and subscription wiring regression tests passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12724/dod-local-validation/command.yaml
          && grep -q '88 passed' drift/dod_receipts/OMN-12724/dod-local-validation/command.yaml"
