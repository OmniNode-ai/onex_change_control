---
schema_version: "1.0.0"
ticket_id: "OMN-12724"
title: "Fix duplicate delegation workflow consumption and terminal payload projection"
summary: >-
  Central evidence for the dev-runtime delegation fix. omnimarket PR #1059
  makes delegation dispatch contract-managed, removes the catch-all compliance
  bus route, and prevents the effects profile from owning workflow consumers.
  omnibase_infra PR #1878 flattens topic-bearing delegation terminal envelopes
  so projections receive ModelDelegationResult payloads. Hotpatched .201 proof
  correlation ead5475c-be5d-415c-a1ab-e526a0faf3d9 completed a task_type=test
  delegation request through routing, local inference, quality gate, and
  delegation-completed with useful pytest content.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
  - "envelopes"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1059"
    description: "omnimarket PR #1059 carries the contract-managed delegation workflow routing fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12724/dod-omnimarket-pr-1059/command.yaml"
          && grep -q '^pr_number: 1059$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12724/dod-omnimarket-pr-1059/command.yaml"
  - id: "dod-infra-pr-1878"
    description: "omnibase_infra PR #1878 carries the terminal delegation payload flattening fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12724/dod-infra-pr-1878/command.yaml"
          && grep -q '^pr_number: 1878$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12724/dod-infra-pr-1878/command.yaml"
  - id: "dod-dev-runtime-useful-delegation"
    description: "Hotpatched .201 dev runtime produced a useful pytest artifact through the full delegation chain."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12724/dod-dev-runtime-useful-delegation/command.yaml"
          && grep -q 'ead5475c-be5d-415c-a1ab-e526a0faf3d9' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12724/dod-dev-runtime-useful-delegation/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-12724 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12724/dod-occ-pr/command.yaml"
