---
schema_version: "1.0.0"
ticket_id: "OMN-15170"
title: "OCC evidence for OMN-15170 title-citation on omnimarket#1906"
summary: >-
  OMN-15170 (steel_onslaught live driver test) was implemented and merged in jonahgabriel/steel_onslaught#220
  (mergeCommit 24f3f5174ee47d26e0c9abe564c6da58e23497f2), a private personal repo not owned by this OCC
  evidence chain. This contract exists solely because omnimarket PR #1906's TITLE ("fix(OMN-15187): ...
  (unblocks OMN-15170 live driver)") mentions OMN-15170 parenthetically, and the OCC eligibility gate's
  title-fallback ticket extractor (validator_occ_merge_eligibility._extract_ticket_ids, when the PR body
  has no Closes/Fixes/Resolves/Implements keyword) treats EVERY OMN-XXXX token in the title as an equally-required
  ticket, not just the primary one. Without this contract, PR #1906 (the actual target: OMN-15187) hard-fails
  occ-preflight/eligibility with reason=missing_contract for the incidentally -mentioned OMN-15170, even
  though #1906 makes no code change related to OMN-15170. Both dod_evidence items below are bound to omnimarket
  PR #1906 (pr_number) to satisfy the gate's PR-binding requirement; the substantive check independently
  verifies OMN-15170's real, merged deliverable.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "steel_onslaught#220 (OMN-15170) merged, live-driver test file present at merge head"
    command: "gh api repos/jonahgabriel/steel_onslaught/contents/tests/live/test_omn15170_live_driver.py?ref=24f3f5174ee47d26e0c9abe564c6da58e23497f2
      --jq .content"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1906-omn15170-title-citation"
    description: >-
      Title-citation binding: omnimarket PR #1906's title mentions OMN-15170 parenthetically ("unblocks
      OMN-15170 live driver"); this receipt binds that citation to PR #1906 itself so the OCC eligibility
      gate's per-ticket PR-binding requirement is satisfied for the incidentally -cited ticket.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1906 --repo OmniNode-ai/omnimarket --json number,state,headRefName"
  - id: "dod-steel220-omn15170-live-driver-content"
    description: >-
      Falsifiable content probe: OMN-15170's real deliverable (jonahgabriel/steel_onslaught#220, merged
      24f3f5174ee47d26e0c9abe564c6da58e23497f2) added tests/live/test_omn15170_live_driver.py containing
      the real Kafka-producer/consumer live-driver test function. The file did not exist before this PR
      (net-new path), so the check is falsifiable by construction.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/jonahgabriel/steel_onslaught/contents/tests/live/test_omn15170_live_driver.py?ref=24f3f5174ee47d26e0c9abe564c6da58e23497f2 --jq .content | base64 -d | grep -q 'def test_live_match_produces_real_kafka_terminal_event_via_delegation_pin'
  - id: "occ-self-bind-pr-4985"
    description: "OCC companion PR #4985 — self-bind for OMN-15170 (title-citation companion for omnimarket#1906)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4985 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
  - id: "occ-self-bind-pr-5498"
    description: "Binds OMN-15170 to OCC companion PR #5498 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5498 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-steel220-omn15170-sigpipe-safe"
    description: >-
      Append-only replacement for dod-steel220-omn15170-live-driver-content (OMN-15411). The historical
      entry piped `gh api ... | base64 -d | grep -q '<marker>'` -- `grep -q` closes stdin at the first
      match, so the upstream `base64 -d` (and, transitively, `gh api`'s curl) is killed by SIGPIPE once
      the pipe drains; under `pipefail` (the dod_verify runner's shell mode) that SIGPIPE exit (128+13=141)
      propagates as the pipeline's exit code even though the substantive grep matched, producing a reproducible
      false RED inside the runner while the same command exits 0 when run directly (no pipefail, or an
      interactive shell that doesn't propagate the killed writer's status). This entry binds the same
      falsifiable content probe -- same gh api target, same marker -- but replaces the short-circuiting
      `grep -q` pipeline with a fail-closed buffered read: the decoded file body is assigned first, proving
      the producer completed, and `grep -qF` then checks the marker from that buffered body. Verified:
      repeated runs under `bash -o pipefail -c` exit 0 against the real marker; the same form against
      a mutated, absent marker string exits 1 (RED), proving the check stays falsifiable.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-steel220-omn15170-live-driver-content"
    checks:
      - check_type: "command"
        check_value: |-
          body="$(gh api repos/jonahgabriel/steel_onslaught/contents/tests/live/test_omn15170_live_driver.py?ref=24f3f5174ee47d26e0c9abe564c6da58e23497f2 --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF 'def test_live_match_produces_real_kafka_terminal_event_via_delegation_pin'
