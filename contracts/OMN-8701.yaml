---
schema_version: 1.0.0
ticket_id: OMN-8701
title: Local runtime fallback when Kafka runtime is down
summary: Runtime client gained explicit deployed/local/deployed_or_local selection and local in-memory
  bus fallback evidence.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - events
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Wave worker local verification evidence is captured in central OCC receipt.
    command: verify central PASS receipt and DoD report for OMN-8701
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-wave-local-evidence
    description: Wave worker local verification or readiness evidence is captured as a canonical PASS
      receipt.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8701/dod-wave-local-evidence/command.yaml"
          && grep -q '^pr_number: 1664$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8701/dod-wave-local-evidence/command.yaml"
          && grep -q '^verifier: codex$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8701/dod-wave-local-evidence/command.yaml"
          && grep -q '"verdict": "passed"' "$CONTRACT_REPO_DIR/evidence/OMN-8701/dod_report.json"
