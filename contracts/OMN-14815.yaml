---
schema_version: 1.0.0
ticket_id: OMN-14815
title: Autobind OCC evidence for OMN-14815
summary: OCC Evidence-Source autobind companion for OmniNode-ai/omnibase_infra#2367.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #2367 product diff scope present'
    command: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-OmniNode-ai-omnibase_infra-pr-2367
    description: 'PR #2367 on OmniNode-ai/omnibase_infra -- Evidence-Source autobind.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-OmniNode-ai-omnibase_infra-pr-2367-ci
    description: 'PR #2367 on OmniNode-ai/omnibase_infra -- product diff scope check.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
  - id: dod-omnibase-infra-pr-2367-deploy-scope
    description: 'Deploy-gate scope for omnibase_infra PR #2367. The change touches src/omnibase_infra/nodes/node_chain_replay_compute/handlers/handler_chain_replay.py
      runtime handler path, but only flips the existing chain replay compute node to canonical def-B dispatch
      shape with equivalence proof while preserving deterministic chain replay/adaptation semantics and
      golden outputs. No live runtime deployment, .201 mutation, docker exec, or rpk topic produce is
      authorized by this evidence repair; hosted merge gates and post-merge dev availability remain the
      deployment boundary.'
    source: generated
    checks:
      - check_type: command
        check_value: printf '%s\n' 'deploy scope assessed for omnibase_infra#2367 runtime handler path;
          no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'
  - id: occ-self-bind-pr-4490
    description: OCC companion PR  #4490 -- self-bind for OMN-14815 deploy evidence repair.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-infra-2367-head-5c2a674d
    description: 'PR #2367 on OmniNode-ai/omnibase_infra -- refreshed product head 5c2a674d.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName
    evidence_artifact: supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2367
  - id: dod-infra-2367-diff-5c2a674d
    description: 'PR #2367 on OmniNode-ai/omnibase_infra -- refreshed product diff scope at head 5c2a674d.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
    evidence_artifact: supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2367-ci
  - id: dod-infra-2367-deploy-5c2a674d
    description: 'Deploy-gate scope for omnibase_infra PR #2367 at head 5c2a674d. The change touches src/omnibase_infra/nodes/node_chain_replay_compute/contract.yaml
      and src/omnibase_infra/nodes/node_chain_replay_compute/handlers/handler_chain_replay.py runtime
      contract/handler paths, but only records the existing canonical def-B chain replay dispatch shape
      and preserves deterministic chain replay/adaptation semantics and golden outputs. No live runtime
      deployment, .201 mutation, docker exec, or rpk topic produce is authorized by this evidence repair;
      hosted merge gates and post-merge dev availability remain the deployment boundary.'
    source: generated
    checks:
      - check_type: command
        check_value: printf '%s\n' 'deploy scope assessed for omnibase_infra#2367 refreshed runtime contract/handler
          paths; no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'
    evidence_artifact: supersedes_dod_evidence:dod-omnibase-infra-pr-2367-deploy-scope
  - id: occ-self-bind-pr-4492
    description: OCC follow-up PR  #4492 -- self-bind for OMN-14815 refreshed head evidence repair.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
    evidence_artifact: supersedes_dod_evidence:occ-self-bind-pr-4490
