---
schema_version: "1.0.0"
ticket_id: "OMN-18024"
title: "Retire the published cloud identity chain from the public tree (omnibase_infra)"
summary: >
  OCC companion for OmniNode-ai/omnibase_infra PR #3272. Hand-authored: the autobind producer declined
  with occ-autobind-no-red-derivable (OMN-15247) because it could not prove a changed-file candidate RED
  against the merge base. The behavior proof below is derived from this PR's own diff and is falsifiable
  at its first parent, which is recorded rather than asserted.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3272 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3272 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: every test module the PR changes, plus the two whose pinned fixture digests it moves. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/ci/test_exposed_identifier_gate.py tests/ci/test_incident_replay_omn17534.py tests/ci/test_incident_replay_omn17888.py tests/unit/topics/test_topic_provisioning_policy.py tests/unit/topology/test_validator_ro_principal_omn17792.py tests/unit/db/test_migration_104_retired_omn17923.py -q
  - kind: "tests"
    description: |-
      The exposure gate itself, with its positive control. `check_exposed_identifiers.py --mode blocking --scope all` must exit 0 on the PR head AND non-zero at its first parent with the SAME denylist file. A zero-finding run alone is not evidence of absence; the red parent run is what makes it evidence.
    command: |-
      python3 scripts/validation/check_exposed_identifiers.py --mode blocking --scope all
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3272"
    description: "PR #3272 on OmniNode-ai/omnibase_infra — hand-authored OCC companion."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3272 --repo OmniNode-ai/omnibase_infra --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3272-ci"
    description: "PR #3272 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3272 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3272 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-3272"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/ci/test_exposed_identifier_gate.py tests/ci/test_incident_replay_omn17534.py tests/ci/test_incident_replay_omn17888.py tests/unit/topics/test_topic_provisioning_policy.py tests/unit/topology/test_validator_ro_principal_omn17792.py tests/unit/db/test_migration_104_retired_omn17923.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-exposure-gate-with-positive-control"
    description: "PR #3272 — the exposed-identifier gate green on the head and RED at the first parent
      with the same denylist."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "python3 scripts/validation/check_exposed_identifiers.py --mode blocking --scope
          all"
        cwd: "${OMNI_HOME}/omnibase_infra"
