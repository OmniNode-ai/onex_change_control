---
schema_version: "1.0.0"
ticket_id: "OMN-14948"
title: "Autobind OCC evidence for OMN-14948"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1868."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1868 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1868"
    description: "PR #1868 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1868-ci"
    description: "PR #1868 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4655"
    description: "OCC companion PR #4655 — self-bind for OMN-14948 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4663-omn14948-deploy-scope"
    description: "OCC deploy-scope companion PR #4663 — self-bind for OMN-14948."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4663 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
  - id: "dod-omnimarket-pr-1868-deploy-scope"
    description: >-
      Deploy-scope evidence for runtime-path changes in omnimarket PR #1868. The PR wires typed canary
      monitoring threshold config only; it does not deploy, restart, mutate live runtime lanes, or execute
      abort actions.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1868 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -F 'No
          AWS/IAM/network/secret/DNS mutation' && gh pr view 1868 --repo OmniNode-ai/omnimarket --json
          body --jq .body | grep -F 'no deploy, no runtime restart' && gh pr view 1868 --repo OmniNode-ai/omnimarket
          --json body --jq .body | grep -F 'abort action' && test 'deploy scope acknowledged; no live
          deploy performed by PR #1868' = 'deploy scope acknowledged; no live deploy performed by PR #1868'"
