---
schema_version: "1.0.0"
ticket_id: "OMN-15744"
title: "Autobind OCC evidence for OMN-15744"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2686.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2686 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2686"
    description: "PR #2686 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2686 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2686 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2686 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2686 --repo OmniNode-ai/omnibase_infra --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2686"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6204"
    description: "Binds OMN-15744 to OCC companion PR #6204 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6204 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dns-tls-broker-connectivity-proof"
    description: >-
      Live proof that the gateway-forwarder container on .201 resolves all 6 MSK broker FQDNs to the SNI-routing
      bastion (extra_hosts stopgap, OMN-15744) and completes a full TLS handshake with cert verification
      against the bastion. Commands executed live inside the running omninode-gateway-forwarder container,
      not simulated.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15744/dns-tls-broker-connectivity-proof/command.yaml"
  - id: "outbound-forwarding-live-proof"
    description: >-
      Live proof of a local-to-cloud outbound forward: one synthetic ModelEventEnvelope-wrapped record
      was produced to the local redpanda mirror topic onex.evt.omniintelligence.llm-call-completed.v1
      on omnibase-infra-redpanda (.201 dev lane), and the gateway-forwarder logged a direction=outbound
      delivery-acknowledged line for that exact envelope_id within seconds. This closes the OMN-15744
      stopgap's previously-unproven outbound leg (inbound was already flowing).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15744/outbound-forwarding-live-proof/command.yaml"
