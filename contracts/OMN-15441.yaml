---
schema_version: "1.0.0"
ticket_id: "OMN-15441"
title: "Autobind OCC evidence for OMN-15441"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #1963.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1963 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1963"
    description: "PR #1963 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1963 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: "gh pr view 1963 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #1963 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} --repo ${REPO} --name-only | grep -qiE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-deploy-assessment-pr-1968"
    description: "Falsifiable deploy-gate probe for omnimarket PR #1968 compute-only producer remediation."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: |-
          tmp="$(mktemp)"
          gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_pr_lifecycle_fix_effect/handlers/occ_companion_emitter.py?ref=53d22f66132f795a73cbfe42f92dc2646a21c375' --jq .content > "${tmp}.b64"
          python3 -c 'import base64, pathlib, sys; sys.stdout.buffer.write(base64.b64decode(pathlib.Path(sys.argv[1]).read_bytes()))' "${tmp}.b64" > "$tmp"
          grep -qF 'contract_secret_ref(_CONTRACT_PATH, _PRODUCT_TOKEN_ENV_VAR)' "$tmp"
          grep -qF 'rest_json_array(' "$tmp"
          grep -qF 'product-repo-scoped credential via {_PRODUCT_TOKEN_ENV_VAR}' "$tmp"
          rm -f "$tmp" "${tmp}.b64"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-1963"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5545"
    description: "Binds OMN-15441 to OCC companion PR #5545 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5545 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-5563"
    description: "Binds OMN-15441 to OCC companion PR #5563 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5563 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-5565"
    description: "Binds OMN-15441 to OCC repair PR #5565 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5565 --repo OmniNode-ai/onex_change_control --json number,state"
