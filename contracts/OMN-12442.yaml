---
schema_version: "1.0.0"
ticket_id: "OMN-12442"
title: "Fix Codex Pattern B runtime client terminal envelope parsing"
summary: >
  Harden the omnimarket Codex runtime client so deployed Pattern B terminal envelopes whose terminal
  result is nested as a plain payload mapping are parsed into ModelDispatchBusTerminalResult instead of
  timing out at the client boundary.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnimarket PR #995 carries the OMN-12442 runtime-client parser fix at the recorded head SHA."
    command: "gh pr view 995 --repo OmniNode-ai/omnimarket --json number,url,state,headRefOid,headRefName,title,baseRefName"
  - kind: "tests"
    description: "Focused Codex runtime client regression suite passes locally at the recorded head SHA."
    command: "uv run pytest tests/test_codex_runtime_client.py -q"
  - kind: "tests"
    description: "Strict omnimarket mypy passes at the recorded head SHA."
    command: "uv run mypy src/omnimarket/ --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-995"
    description: "omnimarket PR #995 is open against dev at the recorded OMN-12442 parser-fix head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12442/dod-omnimarket-pr-995/command.yaml" &&
          grep -q '^pr_number: 995$' "drift/dod_receipts/OMN-12442/dod-omnimarket-pr-995/command.yaml" &&
          grep -q '^commit_sha: "a916152b7a91ad735fc06fd319bc38abceaba4ba"$' "drift/dod_receipts/OMN-12442/dod-omnimarket-pr-995/command.yaml"
  - id: "dod-runtime-client-tests"
    description: "Focused Codex runtime client regression tests pass at the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12442/dod-runtime-client-tests/command.yaml" &&
          grep -q '^commit_sha: "a916152b7a91ad735fc06fd319bc38abceaba4ba"$' "drift/dod_receipts/OMN-12442/dod-runtime-client-tests/command.yaml"
  - id: "dod-strict-mypy"
    description: "Strict omnimarket mypy passes at the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12442/dod-strict-mypy/command.yaml" &&
          grep -q '^commit_sha: "a916152b7a91ad735fc06fd319bc38abceaba4ba"$' "drift/dod_receipts/OMN-12442/dod-strict-mypy/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR #1889 carries the central OMN-12442 contract and receipt evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12442/dod-occ-pr/command.yaml" &&
          grep -q '^pr_number: 1889$' "drift/dod_receipts/OMN-12442/dod-occ-pr/command.yaml" &&
          grep -q '^commit_sha: "28d5bf2ec16351563d1ebd297a5a33f1f25730b3"$' "drift/dod_receipts/OMN-12442/dod-occ-pr/command.yaml"
