---
schema_version: "1.0.0"
ticket_id: "OMN-14783"
title: "Autobind OCC evidence for OMN-14783"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1819."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1819 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1819"
    description: "PR #1819 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1819-ci"
    description: "PR #1819 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4413"
    description: "OCC companion PR #4413 — self-bind for OMN-14783 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-1819-deploy"
    description: >-
      Deploy gate assessment for omnimarket#1819 runtime OCC companion emitter/state-effect changes. No
      live deploy, restart, Kafka mutation, database mutation, or .201 mutation is performed by this merge
      sweep.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          DEPLOY_ASSESSMENT=deploy-required PRODUCT_PR_NUMBER=1819 PRODUCT_REPO=OmniNode-ai/omnimarket
          PRODUCT_HEAD=998caa38c1f7b227feedcf54c3a8143593244530 gh pr view 1819 --repo OmniNode-ai/omnimarket
          --json headRefOid,files --jq '.headRefOid == "998caa38c1f7b227feedcf54c3a8143593244530" and
          ([.files[].path] | index("src/omnimarket/nodes/node_occ_companion_compute/handlers/handler_occ_companion_compute.py")
          != null) and ([.files[].path] | index("src/omnimarket/nodes/node_occ_state_effect/handlers/handler_occ_state_effect.py")
          != null) and ([.files[].path] | index("src/omnimarket/nodes/node_pr_lifecycle_fix_effect/handlers/occ_evidence_stamp.py")
          != null)' | grep -qx true
  - id: "occ-self-bind-pr-4415"
    description: "OCC companion PR #4415 - self-bind for OMN-14783 deploy evidence."
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PR_NUMBER=4415; REPO=OmniNode-ai/onex_change_control; HEAD_BRANCH=codex-letter/omn-14783-deploy-occ;
          gh pr view "$PR_NUMBER" --repo "$REPO" --json number,state,headRefName --jq '.number == 4415
          and .state == "OPEN" and .headRefName == "'$HEAD_BRANCH'"' | grep -qx true
