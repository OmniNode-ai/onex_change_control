---
schema_version: 1.0.0
ticket_id: OMN-11174
title: Publish proof of life seam through bus
summary: Wire seam proof-of-life event publication through the configured event bus so seam liveness events
  are observable at the Kafka/bus layer.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - protocols
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Seam bus publisher tests pass verifying event publication wiring.
    command: uv run pytest tests/ -q -k "seam or proof_of_life or bus"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-seam-bus-publisher
    description: Seam proof-of-life events are published through the configured bus — verified by omnimarket
      PR  #741.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' drift/dod_receipts/OMN-11174/dod-seam-bus-publisher/command.yaml &&
          grep -q '^pr_number: 741$' drift/dod_receipts/OMN-11174/dod-seam-bus-publisher/command.yaml
