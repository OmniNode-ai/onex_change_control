---
schema_version: "1.0.0"
ticket_id: "OMN-10656"
title: "Emit delegated event from local delegation fallback"
summary: >
  Repairs the local /onex:delegate fallback so a successful in-process delegation emits the canonical
  task.delegated event for projection consumers instead of completing silently with only local return
  data.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-delegate-fallback-tests"
    description: "Focused delegate fallback tests cover canonical task.delegated emission."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/hooks/lib/test_delegate_inprocess_fallback.py -q"
  - id: "dod-precommit"
    description: "Pre-commit hooks pass for touched delegate fallback files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --files plugins/onex/skills/delegate/_lib/run.py tests/unit/hooks/lib/test_delegate_inprocess_fallback.py"
  - id: "dod-pr-1537-head"
    description: "Bind OMN-10656 evidence to omniclaude PR #1537 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1537 --repo OmniNode-ai/omniclaude --json headRefOid,title"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10656 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10656/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number: 795$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10656/dod-occ-pr/command.yaml\""
