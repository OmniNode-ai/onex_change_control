---
schema_version: '1.0.0'
ticket_id: OMN-10414
title: "feat(OMN-10414): extend pre-commit hook to explicitly reject [skip-receipt-gate:] tokens"
summary: >
  Update reject-deploy-gate-skip-token.sh to explicitly document and reject [skip-receipt-gate:] tokens
  alongside [skip-deploy-gate:]. Hook marked as advisory only; receipt-gate (omnibase_core) is sole enforcement
  authority.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: Hook self-test passes all 9 cases including skip-receipt-gate variants
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10414/dod-001/command.yaml && grep -q '9
      passed, 0 failed' drift/dod_receipts/OMN-10414/dod-001/command.yaml"
  - kind: tests
    description: pytest test_reject_skip_token_hook.py covers all 4 golden fixtures + self-test
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10414/dod-002/command.yaml && grep -q '5
      passed' drift/dod_receipts/OMN-10414/dod-002/command.yaml"
  - kind: ci
    description: shellcheck reports no issues on the hook script
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10414/dod-003/command.yaml && grep -q 'shellcheck
      clean' drift/dod_receipts/OMN-10414/dod-003/command.yaml"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Hook self-test exits 0 with 9 passed, 0 failed
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10414/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-10414$' drift/dod_receipts/OMN-10414/dod-001/command.yaml && grep -q '^evidence_item_id:
          dod-001$' drift/dod_receipts/OMN-10414/dod-001/command.yaml && grep -q '9 passed, 0 failed'
          drift/dod_receipts/OMN-10414/dod-001/command.yaml"
  - id: dod-002
    description: pytest test_reject_skip_token_hook.py passes all 5 cases
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10414/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-10414$' drift/dod_receipts/OMN-10414/dod-002/command.yaml && grep -q '^evidence_item_id:
          dod-002$' drift/dod_receipts/OMN-10414/dod-002/command.yaml && grep -q '5 passed' drift/dod_receipts/OMN-10414/dod-002/command.yaml"
  - id: dod-003
    description: shellcheck reports no issues on the hook script
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10414/dod-003/command.yaml && grep
          -q '^ticket_id: OMN-10414$' drift/dod_receipts/OMN-10414/dod-003/command.yaml && grep -q '^evidence_item_id:
          dod-003$' drift/dod_receipts/OMN-10414/dod-003/command.yaml && grep -q 'shellcheck clean' drift/dod_receipts/OMN-10414/dod-003/command.yaml"
  - id: dod-004
    description: PR cites this ticket
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view 1481 --repo OmniNode-ai/omniclaude --json title,body --jq '.title + \"
          \" + (.body // \"\")' | grep -q 'OMN-10414'"
