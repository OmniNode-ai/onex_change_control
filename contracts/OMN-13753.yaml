---
schema_version: "1.0.0"
ticket_id: "OMN-13753"
title: "feat(OMN-13753): provision missing prod secrets — omnidash-tls Certificate + umami-credentials
  InfisicalSecret"
summary: >-
  Declare two missing onex-prod Kubernetes secret sources whose absence drives live prod alerts: a cert-manager
  Certificate for omnidash-tls (dash.omninode.ai via the letsencrypt-prod ClusterIssuer) and an InfisicalSecret
  CRD that syncs umami-credentials from Infisical path /prod/umami/. This change adds manifest declarations
  only; no literal secret values are committed (all references resolve at the cluster boundary). Applying
  to the cluster requires a fresh operator approval (OMN-13418) — no pre-merge cluster mutation is performed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "The Certificate and InfisicalSecret manifests on omnibase_infra PR #2158 are valid YAML
      and commit no literal secret values; no pre-merge cluster mutation was performed."
    command: >-
      grep -q '^status: PASS$' drift/dod_receipts/OMN-13753/dod-manifest-lint/command.yaml && grep -q
      '^status: PASS$' drift/dod_receipts/OMN-13753/dod-no-pre-merge-deploy/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-13753/dod-occ-self/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-manifest-lint"
    description: >-
      The omnidash-tls Certificate and umami-credentials InfisicalSecret manifests parse as valid YAML
      and contain no literal secret values (no CHANGEME) on omnibase_infra PR #2158.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13753/dod-manifest-lint/command.yaml"
  - id: "dod-no-pre-merge-deploy"
    description: >-
      OMN-13753 ships secret-source manifests only. No live deploy, restart, .201 mutation, production
      data mutation, new REST endpoint, event-bus topic change, or cluster apply was performed pre-merge;
      activation is operator-gated by OMN-13418.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13753/dod-no-pre-merge-deploy/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13753 contract and receipts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13753/dod-occ-self/command.yaml"
