---
schema_version: "1.0.0"
ticket_id: "OMN-13877"
title: "feat(OMN-13877): dep-provenance gate — onex_change_control"
summary: >-
  Ports the OMN-13873 offline dependency-provenance gate to onex_change_control: rejects first-party git-source
  overrides for core/spi/compat packages in [tool.uv.sources] on protected branches, with CI and pre-commit
  wiring plus focused unit coverage. The onex-change-control self-pin remains exempt; the repo's pre-existing
  omnibase-core override is escape-tokened to OMN-13878.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-gate"
    description: >-
      onex_change_control PR #3518 carries the OMN-13877 dep-provenance gate (scripts/check_dep_provenance.py,
      .github/workflows/dep-provenance-gate.yml, tests/unit/scripts/test_check_dep_provenance.py, and
      the check-dep-provenance pre-commit hook). The checker exits 0 after the existing omnibase-core
      override is escape-tokened to OMN-13878, and the 11 focused unit tests pass.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13877/dod-occ-gate/command.yaml"
