---
schema_version: "1.0.0"
ticket_id: "OMN-12879"
title: "[build] Guardrail: require dispatcher coverage for changed command topics"
summary: >-
  Tracks contract/CI guard coverage proving changed command topics have live MessageDispatchEngine dispatcher
  routes and do not fall through default_handler to NO_DISPATCHER plus DLQ.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-dispatcher-coverage-guard"
    description: >-
      Failing changed-topic fixture without dispatcher coverage is rejected; valid dispatcher fixture passes
      and output identifies contract path plus route source.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12879/dod-dispatcher-coverage-guard/command.yaml"
