---
schema_version: "1.0.0"
ticket_id: "OMN-11382"
title: "Rich terminal UI with correlation and hash visibility"
summary: >-
  onex-self-extending-agent PR #71 adds a rich-based terminal UI renderer for the agent demo pipeline,
  including generation status, validation, registration, invocation, and trend-table surfaces with a plain-print
  fallback when rich is unavailable. This evidence binds the rebased PR head to focused unit, lint, and
  security-fixture verification.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: tests
    description: "Demo UI unit tests pass locally for onex-self-extending-agent PR #71."
    command: "uv run pytest tests/unit/test_demo_ui.py -q"
  - kind: manual
    description: "Demo UI touched files pass ruff and the hardcoded-path security fixture is annotated."
    command: "uv run ruff check src/demo_ui.py tests/unit/test_demo_ui.py"
  - kind: manual
    description: "No live deploy required; change is a local demo UI and dependency lock update."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11382/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-71"
    description: "onex-self-extending-agent PR #71 carries the rich terminal UI implementation at the
      rebased head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11382/dod-product-pr-71/command.yaml"
          && grep -q '^pr_number: 71$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11382/dod-product-pr-71/command.yaml"
          && grep -q '^commit_sha: "8cf718e12fc19e565101cc612c0738e792900113"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11382/dod-product-pr-71/command.yaml"
  - id: "dod-local-proof"
    description: "Focused local demo UI tests, ruff checks, and hardcoded-path security scan pass after
      the merge-sweep repair."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11382/dod-local-proof/command.yaml"
          && grep -q '10 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11382/dod-local-proof/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11382/dod-local-proof/command.yaml"
          && grep -q 'hardcoded path grep: no matches' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11382/dod-local-proof/command.yaml"
  - id: "dod-deploy"
    description: "No live deploy required; rich terminal UI is a local CLI/demo presentation path with
      no runtime service mutation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11382/dod-deploy/command.yaml"
