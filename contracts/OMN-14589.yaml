---
schema_version: "1.0.0"
ticket_id: "OMN-14589"
title: "feat(OMN-14589): canonicalize node_coding_agent_invoke_effect to def-B"
summary: >-
  omnibase_infra PR #2302 canonicalizes node_coding_agent_invoke_effect from the envelope-wrapping def-A
  handler shape to the typed def-B command handler shape. The PR preserves real dispatch behavior with
  golden equivalence fixtures, adequacy receipts, and the node_coding_agent focused unit suite.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
  - protocols
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2302-coding-agent-defb"
    description: >-
      Product-observing verification that omnibase_infra PR #2302 head 1011fcb308ebca2b29ec3c705cc3c062386d0358
      carries the coding-agent invoke def-B handler conversion, adequacy receipt artifacts, golden equivalence
      fixtures, and the focused node_coding_agent test proof described in the product PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=2302; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=1011fcb308ebca2b29ec3c705cc3c062386d0358;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,files,body
          --jq '.headRefOid + "\n" + ((.files | map(.path)) | join("\n")) + "\n" + .body' > "$d/pr.txt"
          && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fq 'src/omnibase_infra/nodes/node_coding_agent_invoke_effect/handlers/handler_coding_agent_invoke.py'
          "$d/pr.txt" && grep -Fq 'tests/unit/nodes/node_coding_agent/test_invoke_effect_golden_equivalence.py'
          "$d/pr.txt" && grep -Fq 'scripts/ci/adequacy_receipts/omnibase_infra.nodes.node_coding_agent_invoke_effect.json'
          "$d/pr.txt" && grep -Fq 'scripts/ci/adequacy_receipts/omnibase_infra.nodes.node_coding_agent_invoke_effect.equivalence.json'
          "$d/pr.txt" && for p in src/omnibase_infra/nodes/node_coding_agent_invoke_effect/handlers/handler_coding_agent_invoke.py
          scripts/ci/adequacy_receipts/omnibase_infra.nodes.node_coding_agent_invoke_effect.json scripts/ci/adequacy_receipts/omnibase_infra.nodes.node_coding_agent_invoke_effect.equivalence.json
          tests/unit/nodes/node_coding_agent/test_invoke_effect_golden_equivalence.py; do mkdir -p "$d/$(dirname
          "$p")"; gh api "repos/OmniNode-ai/omnibase_infra/contents/${p}?ref=${PRODUCT_HEAD}" --jq .content
          | base64 --decode > "$d/$p"; done && grep -Fq 'ModelCodingAgentInvokeCommand' "$d/src/omnibase_infra/nodes/node_coding_agent_invoke_effect/handlers/handler_coding_agent_invoke.py"
          && grep -Fq 'ModelCodingAgentResult' "$d/src/omnibase_infra/nodes/node_coding_agent_invoke_effect/handlers/handler_coding_agent_invoke.py"
          && grep -Fq 'branch_coverage' "$d/scripts/ci/adequacy_receipts/omnibase_infra.nodes.node_coding_agent_invoke_effect.json"
          && grep -Fq 'selected_input_hashes' "$d/scripts/ci/adequacy_receipts/omnibase_infra.nodes.node_coding_agent_invoke_effect.equivalence.json"
          && grep -Fq 'test_handle_reproduces_recorded_golden' "$d/tests/unit/nodes/node_coding_agent/test_invoke_effect_golden_equivalence.py"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment for omnibase_infra PR #2302. The PR changes a node effect handler implementation
      and its golden-chain behavior, but this merge sweep does not perform a live deploy, restart, docker
      exec, rpk topic produce, database mutation, or .201 runtime mutation. Product proof is the focused
      local 57-test node_coding_agent suite plus golden equivalence and adequacy artifacts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=2302; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=1011fcb308ebca2b29ec3c705cc3c062386d0358;
          DEPLOY_SCOPE=coding-agent-effect-handler-deploy-assessment; d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER"
          --repo "$PRODUCT_REPO" --json headRefOid,files,body --jq '.headRefOid + "\n" + ((.files | map(.path))
          | join("\n")) + "\n" + .body' > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep
          -Fq 'src/omnibase_infra/nodes/node_coding_agent_invoke_effect/handlers/handler_coding_agent_invoke.py'
          "$d/pr.txt" && grep -Fq 'tests/unit/nodes/node_coding_agent/' "$d/pr.txt" && grep -Fq 'Golden-equivalence'
          "$d/pr.txt" && test "$DEPLOY_SCOPE" = "coding-agent-effect-handler-deploy-assessment"
  - id: "dod-infra-pr-2302-contract-repair-rebound"
    description: >-
      Product-observing rebound verification for omnibase_infra PR #2302 head cc51e4874c04d463af2add82096f9b02d436d931.
      The rebound adds the node_coding_agent_invoke_effect contract update required by the Wave C contract-sync
      gate while preserving the def-B handler conversion, adequacy receipts, golden equivalence fixtures,
      and focused node_coding_agent test proof from the original evidence item.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=2302; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=cc51e4874c04d463af2add82096f9b02d436d931;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,files,body
          --jq '.headRefOid + "\n" + ((.files | map(.path)) | join("\n")) + "\n" + .body' > "$d/pr.txt"
          && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fq 'src/omnibase_infra/nodes/node_coding_agent_invoke_effect/contract.yaml'
          "$d/pr.txt" && grep -Fq 'src/omnibase_infra/nodes/node_coding_agent_invoke_effect/handlers/handler_coding_agent_invoke.py'
          "$d/pr.txt" && grep -Fq 'tests/unit/nodes/node_coding_agent/test_invoke_effect_golden_equivalence.py'
          "$d/pr.txt" && grep -Fq 'scripts/ci/adequacy_receipts/omnibase_infra.nodes.node_coding_agent_invoke_effect.json'
          "$d/pr.txt" && grep -Fq 'scripts/ci/adequacy_receipts/omnibase_infra.nodes.node_coding_agent_invoke_effect.equivalence.json'
          "$d/pr.txt" && for p in src/omnibase_infra/nodes/node_coding_agent_invoke_effect/contract.yaml
          src/omnibase_infra/nodes/node_coding_agent_invoke_effect/handlers/handler_coding_agent_invoke.py
          tests/unit/nodes/node_coding_agent/test_invoke_effect_golden_equivalence.py; do mkdir -p "$d/$(dirname
          "$p")"; gh api "repos/OmniNode-ai/omnibase_infra/contents/${p}?ref=${PRODUCT_HEAD}" --jq .content
          | base64 --decode > "$d/$p"; done && grep -Fq 'DEF-B CANONICAL HANDLER SHAPE (OMN-14589)' "$d/src/omnibase_infra/nodes/node_coding_agent_invoke_effect/contract.yaml"
          && grep -Fq 'patch: 1' "$d/src/omnibase_infra/nodes/node_coding_agent_invoke_effect/contract.yaml"
          && grep -Fq 'ModelCodingAgentInvokeCommand' "$d/src/omnibase_infra/nodes/node_coding_agent_invoke_effect/handlers/handler_coding_agent_invoke.py"
  - id: "dod-infra-pr-2302-review-thread-rebound"
    description: >-
      Product-observing rebound verification for omnibase_infra PR #2302 head 6477d6a175c9c73068f10c687a9c81e2c054a3e3.
      The rebound resolves the required CodeRabbit review thread by recording coverage.py's actual missing_branches
      total as uncovered_branch_count in adequacy receipt waivers, while preserving the def-B handler
      conversion, node contract update, adequacy artifacts, and focused node_coding_agent test proof.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=2302; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=6477d6a175c9c73068f10c687a9c81e2c054a3e3;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,files,body
          --jq '.headRefOid + "\n" + ((.files | map(.path)) | join("\n")) + "\n" + .body' > "$d/pr.txt"
          && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fq 'scripts/ci/adequacy_receipt.py' "$d/pr.txt"
          && grep -Fq 'src/omnibase_infra/nodes/node_coding_agent_invoke_effect/contract.yaml' "$d/pr.txt"
          && grep -Fq 'src/omnibase_infra/nodes/node_coding_agent_invoke_effect/handlers/handler_coding_agent_invoke.py'
          "$d/pr.txt" && for p in scripts/ci/adequacy_receipt.py; do mkdir -p "$d/$(dirname "$p")"; gh
          api "repos/OmniNode-ai/omnibase_infra/contents/${p}?ref=${PRODUCT_HEAD}" --jq .content | base64
          --decode > "$d/$p"; done && grep -Fq 'missing_branches' "$d/scripts/ci/adequacy_receipt.py"
          && grep -Fq 'uncovered_branch_count=missing_branches' "$d/scripts/ci/adequacy_receipt.py"
  - id: "dod-occ-self-4128"
    description: >-
      OCC companion self-binding for onex_change_control PR #4128. This binding item asserts that the
      OCC PR carries the central OMN-14589 contract and receipts consumed by omnibase_infra PR #2302 through
      its Evidence-Source line; substantive product evidence is carried by the preceding DoD items.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          OCC_PR_NUMBER=4128; OCC_REPO=OmniNode-ai/onex_change_control; test "$(gh pr view "$OCC_PR_NUMBER"
          --repo "$OCC_REPO" --json headRefName --jq .headRefName)" = "jonah/omn-14589-occ-coding-agent-defb"
  - id: "dod-occ-self-4130"
    description: >-
      OCC companion self-binding for onex_change_control PR #4130. This binding item asserts that the
      rebound OCC PR carries the central OMN-14589 contract and receipt for omnibase_infra PR #2302 product
      head cc51e4874c04d463af2add82096f9b02d436d931.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          OCC_PR_NUMBER=4130; OCC_REPO=OmniNode-ai/onex_change_control; test "$(gh pr view "$OCC_PR_NUMBER"
          --repo "$OCC_REPO" --json headRefName --jq .headRefName)" = "jonah/omn-14589-occ-rebind-infra-pr-2302-cc51e487"
  - id: "dod-occ-self-4132"
    description: >-
      OCC companion self-binding for onex_change_control PR #4132. This binding item asserts that the
      rebound OCC PR carries the central OMN-14589 contract and receipt for omnibase_infra PR #2302 product
      head 6477d6a175c9c73068f10c687a9c81e2c054a3e3.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          OCC_PR_NUMBER=4132; OCC_REPO=OmniNode-ai/onex_change_control; test "$(gh pr view "$OCC_PR_NUMBER"
          --repo "$OCC_REPO" --json headRefName --jq .headRefName)" = "jonah/omn-14589-occ-rebind-infra-pr-2302-6477d6a1"
