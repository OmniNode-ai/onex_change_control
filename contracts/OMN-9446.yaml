schema_version: "1.0.0"
ticket_id: OMN-9446
summary: "feat(omnibase_infra): canonical GitHubHttpClient — single adapter for all GitHub API access"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Integration test with fake HTTP server covering all public methods
    command: "uv run pytest tests/integration/adapters/test_github_http_client_integration.py -v"
  - kind: ci
    description: CI passes on PR branch
    command: "gh pr checks 1369 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: Integration test exists and passes
    source: generated
    checks:
      - check_type: command
        check_value: "uv run pytest tests/integration/adapters/test_github_http_client_integration.py -v"
  - id: dod-002
    description: Lint clean (ruff check + ruff format)
    source: generated
    checks:
      - check_type: command
        check_value: "uv run ruff check src/omnibase_infra/adapters/github/ && uv run ruff format --check src/omnibase_infra/adapters/github/"
