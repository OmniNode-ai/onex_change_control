---
schema_version: "1.0.0"
ticket_id: "OMN-15362"
title: "Autobind OCC evidence for OMN-15362"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omniclaude PR #1961.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1961 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniclaude-pr-1961"
    description: "PR #1961 on OmniNode-ai/omniclaude — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omniclaude-pr-1961"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5473"
    description: "Binds OMN-15362 to OCC companion PR #5473 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: dod-15362-omniclaude1961-stamp-rb15391
    description: 'Append-only replacement for dod-occ-evidence-admissibility-validator (OMN-15391), whose
      one-line prose (''Hosted OCC evidence admissibility validator'') was filed as the supersession of
      the omniclaude#1961 product binding. The superseded entry''s only check was ''uv run pytest tests/test_evidence_admissibility.py
      -q''. node_dod_verify''s EvidenceCollector._infer_occ_cwd resolves that command into the OCC checkout,
      where it runs OCC''s own OMN-15309 admissibility-predicate suite (76 tests, ~0.1s) and passes identically
      whether or not the product fact it is filed under is true. This entry binds the OMN-15362 product
      fact on a PUBLIC repo, so it is executable by every runner: the reusable verifier-stamp workflow
      the ticket ships exists at omniclaude#1961''s merge commit 7009244192111177d172825bb8374308797a0413
      and did not exist at its first parent 5703f123fe272a1214cca50d0fdd66e34180ed14.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-occ-evidence-admissibility-validator
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omniclaude/contents/.github/workflows/verifier-stamp-reusable.yml?ref=7009244192111177d172825bb8374308797a0413''
          | grep -q ''name: Verifier Stamp (reusable)'''
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omniclaude/contents/.github/workflows/verifier-stamp-reusable.yml?ref=5703f123fe272a1214cca50d0fdd66e34180ed14''
          | grep -c ''name: Verifier Stamp (reusable)'' | grep -qx 0'
  - id: occ-self-bind-5473-lit-rb15391
    description: 'Append-only replacement for occ-self-bind-pr-5473 (OMN-15391 collateral repair). The
      superseded stamp''s id pins OCC companion PR #5473, but its check_value is the bare ${PR_NUMBER}
      runner placeholder, which the compliance runner pre-substitutes with whatever PR it is currently
      evaluating -- the OMN-15382 class-3 mis-binding, frozen in .onex_ratchets/omn_15382_rule_b_baseline.yaml.
      This entry pins the literal number so the stamp can only ever be true of the PR it names. Stated
      plainly: this is a binding/stamp item, not product proof -- it does not count toward the substance
      floor, and the product fact for this ticket is carried by the -rb15391 rebind item above.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:occ-self-bind-pr-5473
    checks:
      - check_type: command
        check_value: gh pr view 5473 --repo OmniNode-ai/onex_change_control --json number --jq '.number
          == 5473' | grep -qx true
  - id: occ-self-bind-5473-unprovable-rb2
    description: 'Append-only replacement for occ-self-bind-5473-lit-rb15391 (OMN-15391 round 2, Rule
      C). The superseded stamp''s only check was "gh pr view 5473 --repo OmniNode-ai/onex_change_control
      --json number --jq ''.number == 5473'' | grep -qx true" -- it hands PR #5473 to the API and then
      asserts the returned number equals 5473. That is `N == N`: true by construction for every PR that
      exists, and incapable of going RED for any product reason. It was EXECUTED at review time and returned
      rc=0, as it always will. THIS ENTRY DOES NOT REPLACE IT WITH A BETTER BINDING CHECK, BECAUSE NO
      ADMISSIBLE ONE EXISTS. The two exhaustive candidate shapes are both refused by the live OMN-14505/OMN-15309
      predicate, for two different and individually correct reasons: reading PR #5473''s file list to
      assert it carries contracts/OMN-15362.yaml is INSIDE_OWN_DIFF (it reads back the contract tree the
      evidence author writes), and reading /pulls/5473 metadata to assert it merged is also INSIDE_OWN_DIFF
      (PR metadata proves the change exists, not that anything it claims is true). An OCC self-bind stamp
      is by construction a statement about OCC''s own tree and a PR''s own metadata, so both halves are
      exactly what the predicate refuses. THE BINDING OF OMN-15362 TO OCC COMPANION PR #5473 IS THEREFORE
      RECORDED AS UNPROVABLE-ON-RECORD, mirroring the operator''s OMN-15376 amend-on-record ruling: state
      the limit rather than manufacture a vacuous command that passes. The check below does not assert
      the binding. It asserts the UNPROVABILITY -- it executes the predicate against both candidate shapes
      and goes RED if either is ever admitted, which would mean this recorded limit no longer holds and
      the stamp must be revisited. The product fact for this ticket is carried by the sibling -rb2 item,
      not here; this item counts toward no substance floor.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:occ-self-bind-5473-lit-rb15391
    checks:
      - check_type: command
        check_value: uv run pytest 'tests/unit/test_occ_self_bind_unprovable_on_record_omn15391.py::test_pr_5473_omn_15362_binding_unprovable'
          -q
  - id: dod-15362-omniclaude1961-stamp-rb2
    description: 'Append-only replacement for dod-15362-omniclaude1961-stamp-rb15391 (OMN-15391 round
      2, Rule D). The superseded item''s GREEN leg is correct and is carried forward here unchanged; its
      ABSENCE leg was ''... | grep -c ''name: Verifier Stamp (reusable)'' | grep -qx 0'', which FAILS
      OPEN. check_values are handed to `sh -c` without pipefail, so a producer that 404s, is denied, or
      hits a network error emits nothing, `grep -c` prints 0, and `grep -qx 0` exits 0 -- GREEN, having
      read nothing. Executed proof of the defect at authoring time: the superseded shape returns rc=0
      against OmniNode-ai/no_such_repo_xyz, a repository that does not exist. This file did not exist
      AT ALL at the parent 5703f123fe272a1214cca50d0fdd66e34180ed14, so the superseded leg passed on a
      404 -- it never read anything, and would have passed identically had the repo been deleted. The
      replacement pairs a REACHABILITY CONTROL with the absence assertion: it first proves the token can
      resolve 5703f123fe27 in this repo (`gh api .../commits/5703f123fe27 --jq .sha` must echo the sha
      back), and only then requires the contents fetch for the path to FAIL. A 404 is therefore admissible
      as evidence of absence only after the same token has demonstrably read the same ref -- an auth or
      network failure fails the control leg first and the check goes RED. Claim bound, unchanged: the
      reusable verifier-stamp workflow exists at omniclaude#1961''s merge commit 7009244192111177d172825bb8374308797a0413,
      and the path did not exist at its first parent 5703f123fe272a1214cca50d0fdd66e34180ed14. Executed
      at authoring time: green leg rc=0, absence leg rc=0; and the absence leg goes RED under all three
      mutations -- invalid token, nonexistent repo, and pointed at the merge commit where the path does
      exist.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-15362-omniclaude1961-stamp-rb15391
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omniclaude/contents/.github/workflows/verifier-stamp-reusable.yml?ref=7009244192111177d172825bb8374308797a0413''
          | grep -q ''name: Verifier Stamp (reusable)'''
      - check_type: command
        check_value: gh api 'repos/OmniNode-ai/omniclaude/commits/5703f123fe272a1214cca50d0fdd66e34180ed14'
          --jq '.sha' | grep -qx '5703f123fe272a1214cca50d0fdd66e34180ed14' && ! gh api 'repos/OmniNode-ai/omniclaude/contents/.github/workflows/verifier-stamp-reusable.yml?ref=5703f123fe272a1214cca50d0fdd66e34180ed14'
          --silent
  - id: "occ-self-bind-pr-5644"
    description: "Binds OMN-15362 to OCC companion PR #5644 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5644 --repo OmniNode-ai/onex_change_control --json number,state"
