---
schema_version: "1.0.0"
ticket_id: "OMN-16549"
title: "Autobind OCC evidence for OMN-16549"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_compat PR #192.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #192 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_compat-pr-192"
    description: "PR #192 on OmniNode-ai/omnibase_compat — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 192 --repo OmniNode-ai/omnibase_compat --json number,state"
      - check_type: "command"
        check_value: "gh pr view 192 --repo OmniNode-ai/omnibase_compat --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_compat-pr-192"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7156"
    description: "Binds OMN-16549 to OCC companion PR #7156 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7156 --repo OmniNode-ai/onex_change_control --json number,state"
  # ---------------------------------------------------------------------------
  # APPEND-ONLY CORRECTION: dod-OmniNode-ai-omnibase_compat-pr-192's commit_sha
  # is stale. Per a 2026-08-26 Wave-3 audit, occ#7156 (the OCC companion) merged
  # AFTER omnibase_compat#192 (07:07:34Z vs 06:39:30Z, 28min later), inverting
  # companions-merge-first. The node_occ_companion_compute autobind that wrote
  # the original entry's receipt ran its probe while #192 was still OPEN
  # (probe_stdout: {"state":"OPEN"}), pinning commit_sha to the PR's then-current
  # head 404e9a59447a7530269604c112a9e79c5fd21727. Squash-merge rewrote that
  # history: 404e9a5... exists as a loose commit object but is NOT an ancestor
  # of omnibase_compat origin/dev (git merge-base --is-ancestor exit 1, verified
  # live). The real landed identity is squash commit abe9095f3b55e87550e092d5e51f145739405d10,
  # which IS an ancestor of origin/dev (verified live). This is the documented
  # squash-invalidates-Evidence-Source failure mode (memory
  # reference_occ_squash_invalidates_evidence_source.md), applied to a receipt's
  # commit_sha field rather than a PR body's Evidence-Source line. Does NOT edit
  # dod-OmniNode-ai-omnibase_compat-pr-192's bytes — this entry only records
  # what actually happened (product merged first, real SHA abe9095f) as a
  # separate, falsifiable, append-only fact. It does not fabricate a compliant
  # merge ordering.
  # ---------------------------------------------------------------------------
  - id: "dod-compat-pr-192-rebind-abe9095f"
    description: >-
      Correction: omnibase_compat#192's real landed identity is squash commit abe9095f3b55e87550e092d5e51f145739405d10
      (an ancestor of origin/dev), not the pre-squash head 404e9a59447a7530269604c112a9e79c5fd21727 the
      original autobind receipt recorded. Also records, as a second falsifiable fact, that OCC companion
      #7156 merged 28 minutes after #192 -- the companions-merge-first inversion this correction exists
      to document.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_compat-pr-192"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/omnibase_compat/pulls/192 --jq '(.merged == true) and (.merge_commit_sha
          == "abe9095f3b55e87550e092d5e51f145739405d10") and (.merged_at == "2026-08-26T06:39:30Z")' |
          grep -qx true
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/7156 --jq '(.merged == true) and (.merged_at
          == "2026-08-26T07:07:34Z")' | grep -qx true
