---
schema_version: "1.0.0"
ticket_id: "OMN-14249"
title: "chore: promote omniclaude dev→main (OMN-14244 deploy-gate matcher fix + routine, unblocks OMN-14190
  Piece-4)"
summary: >-
  Promotion-train contract for omniclaude dev->main promotion PR #1878 (promotion branch tree equals origin/dev,
  dev-head 5fa434d06). Promotes 9 routine commits plus the load-bearing OMN-14244 deploy-gate `RUNTIME_PATH_PATTERNS`
  matcher fix, which all caller repos (omnibase_core/omnibase_infra/omnimarket) resolve from omniclaude
  @main — required to unblock omnibase_infra#2245 (OMN-14190 Piece-4). Dedicated ticket per the OMN-13828
  promotion-ticket precedent, so its OCC receipts bind to the promotion PR number without superseding
  the already-merged OMN-14244/OMN-14190 contracts.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-promotion-pr"
    description: >-
      omniclaude dev->main promotion PR #1878 (promotion branch promotion/omn-14244-omniclaude-dev-to-main,
      dev-head 5fa434d06) promotes the OMN-14244 deploy-gate matcher fix and 8 other routine merged commits
      to main.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14249/dod-omniclaude-promotion-pr/command.yaml"
  - id: "dod-omniclaude-deploy-evidence"
    description: >-
      Deploy-gate evidence for omniclaude dev->main promotion PR #1878. The promotion may touch runtime
      paths (deploy-gate action source, CI workflows); this central OCC deploy receipt satisfies the downstream
      deploy evidence gate without a repository-local bypass.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14249/dod-omniclaude-deploy-evidence/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-14249 promotion contract and
      its PASS receipts for the omniclaude dev->main promotion.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14249/dod-occ-self/command.yaml"
