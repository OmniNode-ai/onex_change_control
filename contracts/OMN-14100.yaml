---
schema_version: "1.0.0"
ticket_id: "OMN-14100"
title: "Drop duplicate omnibase_core standards workflow jobs"
summary: >-
  Central OCC evidence for omnibase_core PR #1395, which removes duplicate or superseded
  jobs from .github/workflows/omni-standards-compliance.yml while preserving the
  required ci.yml-backed enforcement surface.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "omnibase_core PR #1395 is scoped to omni-standards-compliance.yml cleanup."
    command: "gh pr view 1395 --repo OmniNode-ai/omnibase_core --json files,headRefOid"
  - kind: "manual"
    description: "PR #1395 body records coverage-preserved proof and local workflow validation."
    command: "gh pr view 1395 --repo OmniNode-ai/omnibase_core --json body"
  - kind: "manual"
    description: "No live deploy or runtime restart is required for this CI workflow-only change."
    command: "gh pr diff 1395 --repo OmniNode-ai/omnibase_core --name-only"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1395-scope"
    description: >-
      omnibase_core PR #1395 is open at the recorded head and changes only
      .github/workflows/omni-standards-compliance.yml.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14100/dod-core-pr-1395-scope/command.yaml"
          && grep -q '^pr_number: 1395$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14100/dod-core-pr-1395-scope/command.yaml"
          && grep -q '^commit_sha: "11bb232f432166c767f15a0d32eed6dfd1164fcf"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14100/dod-core-pr-1395-scope/command.yaml"
  - id: "dod-core-pr-1395-proof"
    description: >-
      PR #1395 body records the removed jobs, kept jobs, required-check coverage proof,
      no-dangling-reference grep, and local actionlint/YAML/pre-commit validation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14100/dod-core-pr-1395-proof/command.yaml"
          && grep -q 'coverage-preserved' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14100/dod-core-pr-1395-proof/command.yaml"
          && grep -q 'actionlint' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14100/dod-core-pr-1395-proof/command.yaml"
  - id: "dod-deploy-plan"
    description: "No live deploy is required; this is CI workflow configuration cleanup only."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14100/dod-deploy-plan/command.yaml"
          && grep -q 'No live deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14100/dod-deploy-plan/command.yaml"
