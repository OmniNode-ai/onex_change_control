---
schema_version: '1.0.0'
ticket_id: OMN-13699
title: 'fix(OMN-13699): replace os.getenv pattern with contract-sourced factory in ServiceLlmEndpointHealth'
summary: >-
  Removes os.getenv calls from ServiceLlmEndpointHealth and ModelLlmEndpointHealthConfig docstrings; adds
  from_model_registry() factory that resolves LLM endpoint URLs from the routing contract YAML via an
  injected env_resolver callable; fixes stale model aliases and adds 8 unit tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >-
      8 new unit tests in TestModelLlmEndpointHealthConfigFactory covering happy path, non-http transport
      skip, unset env skip, empty env raises, missing registry raises, invalid YAML raises, field defaults,
      and override pattern. All 73 existing service tests pass.
    command: 'uv run pytest tests/ -m unit -q'
  - kind: manual
    description: >-
      mypy --strict passes on both changed source files with 0 issues.
    command: 'uv run mypy src/omnibase_infra/models/health/ --strict'
  - kind: ci
    description: CI pipeline green on omnibase_infra PR.
    command: 'gh pr checks 2142 --repo OmniNode-ai/omnibase_infra'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-factory-implemented
    description: >-
      ModelLlmEndpointHealthConfig.from_model_registry() factory implemented in src/omnibase_infra/models/health/model_llm_endpoint_health_config.py;
      reads model_key/base_url_env from routing contract YAML; resolves via injected env_resolver; raises
      clearly on empty URL strings.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13699/dod-factory-implemented/command.yaml"
  - id: dod-unit-tests-pass
    description: >-
      8 new unit tests in TestModelLlmEndpointHealthConfigFactory all pass; 73 existing service tests
      unchanged and passing.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13699/dod-unit-tests-pass/command.yaml"
  - id: dod-mypy-clean
    description: >-
      mypy --strict clean on all changed source files; 0 issues reported.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13699/dod-mypy-clean/command.yaml"
  - id: dod-no-os-getenv
    description: >-
      No direct os.getenv( calls in model_llm_endpoint_health_config.py implementation; remaining references
      are in docstrings (showing os.getenv as the env_resolver callable) and error messages.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13699/dod-no-os-getenv/command.yaml"
  - id: dod-occ-pr-self
    description: >-
      onex_change_control PR #3323 carries the OMN-13699 central contract and receipt evidence on base
      dev, binding this OCC evidence update to the PR reviewed by Receipt Gate.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13699/dod-occ-pr-self/command.yaml
          && grep -q '^pr_number: 3323$' drift/dod_receipts/OMN-13699/dod-occ-pr-self/command.yaml"
  - id: dod-deploy-assessment
    description: >-
      Deploy assessment for omnibase_infra PR #2142. The deploy-gate runtime-path hit is src/omnibase_infra/services/service_llm_endpoint_health.py;
      the PR also changes the paired health config model, focused unit tests, and migration files shown
      in the PR diff. No live deploy, runtime restart, .201 mutation, topic mutation, or container mutation
      was performed by this evidence repair. Runtime rollout is outside this OCC evidence repair and remains
      governed by the normal omnibase_infra merge/deploy process.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13699/dod-deploy-assessment/command.yaml
          && grep -q '^pr_number: 2142$' drift/dod_receipts/OMN-13699/dod-deploy-assessment/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-13699/dod-deploy-assessment/command.yaml"
