---
schema_version: "1.0.0"
ticket_id: "OMN-11177"
title: "Publish baselines batch through bus"
summary: >
  Refactor scripts/run_baselines_batch_compute.py to publish a typed ModelEventEnvelope to the ONEX event
  bus (onex.cmd.omnibase-infra.baselines-batch-compute.v1) instead of invoking the node handler directly.
  Adds dry-run support, integration tests, and wiring health allowlist entry for the CLI-triggered command
  topic.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1696 CI checks pass."
    command: "gh pr checks 1696 --repo OmniNode-ai/omnibase_infra --watch"
  - kind: "tests"
    description: "Unit tests and integration tests for run_baselines_batch_compute.py pass."
    command: "uv run pytest tests/unit/scripts/test_run_baselines_batch_compute.py tests/integration/scripts/test_run_baselines_batch_compute.py
      -v"
  - kind: "manual"
    description: "node_baselines_batch_compute contract.yaml touches a runtime path; deploy evidence confirms
      contract.yaml is a pure metadata declaration with no handler/DI changes required."
    command: "gh pr diff 1696 --repo OmniNode-ai/omnibase_infra | grep -q 'contract.yaml'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1696-open"
    description: "omnibase_infra PR #1696 carries the bus-routed baselines batch publish branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11177/dod-pr-1696-open/command.yaml"
          && grep -q '^pr_number: 1696$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11177/dod-pr-1696-open/command.yaml"
          && grep -q '^commit_sha: "f94dee840a78b508624d3d635080ad484f421a90"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11177/dod-pr-1696-open/command.yaml"
  - id: "dod-deploy-evidence"
    description: >
      deploy-verified: node_baselines_batch_compute/contract.yaml change adds subscribe_topics and publish_topics
      declarations. No new container, no handler signature change, no DI registration change. The contract.yaml
      update is a pure metadata declaration that the existing deployed node already satisfies at runtime.
      No Docker restart required.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11177/dod-deploy-evidence/command.yaml"
          && echo "deploy-verified: contract.yaml is pure metadata, no docker restart required"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11177 contract evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11177/dod-occ-self/command.yaml"
