---
schema_version: "1.0.0"
ticket_id: "OMN-11814"
title: "node_swarm_dispatch_orchestrator"
summary: >
  ORCHESTRATOR node that coordinates the full swarm dispatch FSM lifecycle.
  Receives a dispatch request, probes endpoint health, decomposes the task,
  selects endpoints, fans out subtasks, aggregates results, and publishes a
  completed or failed terminal event. Subscribes to onex.cmd.omnimarket.swarm-dispatch.v1
  and publishes onex.evt.omnimarket.swarm-dispatch-completed.v1 or
  onex.evt.omnimarket.swarm-dispatch-failed.v1.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-809"
    description: "omnimarket PR #809 implements node_swarm_dispatch_orchestrator with 48 unit tests covering FSM happy path, failure paths, and invalid transition guards"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11814/dod-omnimarket-pr-809/command.yaml\"
          && grep -q '^pr_number: 809$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11814/dod-omnimarket-pr-809/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR binds this contract to onex_change_control as the evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11814/dod-occ-pr/command.yaml\""
  - id: "dod-deploy"
    description: "Deploy assessment — new ORCHESTRATOR node, no live deploy required for initial merge; node is invoked on-demand via contract topic"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11814/dod-deploy/command.yaml\""
