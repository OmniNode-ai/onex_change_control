---
schema_version: "1.0.0"
ticket_id: "OMN-11814"
title: "Swarm dispatch orchestrator node"
summary: >
  Bind central evidence for omnimarket PR #809, which adds the node_swarm_dispatch_orchestrator FSM coordinator
  and repairs merge-gate findings around metadata schema, leaked literals, and architectural lint.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-809"
    description: "Bind OMN-11814 evidence to omnimarket PR #809."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11814/dod-omnimarket-pr-809/command.yaml
          && grep -q '^pr_number: 809$' drift/dod_receipts/OMN-11814/dod-omnimarket-pr-809/command.yaml"
  - id: "dod-swarm-dispatch-focused-tests"
    description: "Swarm dispatch orchestrator focused tests and metadata schema pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11814/dod-swarm-dispatch-focused-tests/command.yaml
          && grep -q '71 passed' drift/dod_receipts/OMN-11814/dod-swarm-dispatch-focused-tests/command.yaml"
  - id: "dod-split-12-regression"
    description: "CI split 12 regression shard passes after metadata repairs."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11814/dod-split-12-regression/command.yaml
          && grep -q '238 passed' drift/dod_receipts/OMN-11814/dod-split-12-regression/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence for swarm dispatch orchestrator merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11814/dod-deploy-readiness/command.yaml
          && grep -q 'swarm dispatch orchestrator has focused test, split, leak, and arch proof' drift/dod_receipts/OMN-11814/dod-deploy-readiness/command.yaml"
