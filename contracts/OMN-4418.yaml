---
schema_version: "1.0.0"
ticket_id: "OMN-4418"
title: "@shim decorator in omnibase_core"
summary: >
  Add the temporary compatibility @shim decorator and typed shim metadata model in omnibase_core, with
  focused unit coverage for marker behavior, metadata validation, and decorator export wiring.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_core PR #1175 carries the shim decorator branch at the recorded head."
    command: "gh pr view 1175 --repo OmniNode-ai/omnibase_core --json number,url,headRefOid,headRefName,title,state,baseRefName"
  - kind: "tests"
    description: "Focused shim decorator unit tests pass against the PR worktree."
    command: "PYTHONPATH=/Users/jonah/Code/omni_home/omni_worktrees/OMN-4418/omnibase_core/src:/Users/jonah/Code/omni_home/omnibase_infra/src
      uv run python -m pytest tests/unit/decorators/test_decorator_shim.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-shim-decorator-pr-1175"
    description: "omnibase_core PR #1175 shim decorator implementation passes focused local unit tests
      at the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-4418/dod-shim-decorator-pr-1175/command.yaml"
          && grep -q '^commit_sha: "3f062807a370bb3d15e24787d9d72c057275b842"$' "drift/dod_receipts/OMN-4418/dod-shim-decorator-pr-1175/command.yaml"
          && grep -q '10 passed' "drift/dod_receipts/OMN-4418/dod-shim-decorator-pr-1175/command.yaml"
