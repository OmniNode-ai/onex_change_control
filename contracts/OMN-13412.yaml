---
schema_version: "1.0.0"
ticket_id: "OMN-13412"
title: "feat(OMN-13412): wire 7 fresh-deploy fitness validators as required CI + pre-commit gates"
summary: >-
  OCC evidence for omnibase_infra PR #2067 (Wave E). Wires 7 fresh-deploy fitness validators as enforcement
  (required CI + pre-commit), not detection. Three NEW static validators land (terminal cost completeness,
  context-ROI field presence, release identity), two existing validators get promoted to CI gates (sibling-pin
  recurrence ratchet logic-regression gate, build-provenance version-skew guard), and two were already
  required (scratch-Postgres cold-apply in ci.yml migration-integration, vendored-tree byte-equality in
  node-migration-sync.yml). A deliberately broken input (hardcoded cost_usd=0.0, context-ROI claim with
  no context_pack_hash, packaged src change on an already-published version) fails CI with a non-zero
  exit. pyproject version bumped 0.38.3 -> 0.38.4 to satisfy the new release-identity gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-fresh-deploy-fitness-gates"
    description: >-
      Product PR #2067 adds scripts/check_terminal_cost_completeness.py, scripts/check_context_field_presence.py,
      scripts/check_release_identity.py, the .github/workflows/fresh-deploy-fitness.yml required-CI workflow
      (release-identity, terminal-cost-completeness, context-field-presence, sibling-lock-pins, pinned-wheel-skew),
      and 3 new pre-commit hooks. Each broken input fails CI non-zero; correct inputs pass. tests/scripts/test_fresh_deploy_fitness_gates.py
      (8 passed) proves both directions.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c 'fresh-deploy fitness' contracts/OMN-13412.yaml"
  - id: "dod-deploy-gate"
    description: >-
      Deploy-gate evidence for omnibase_infra PR #2067. No live deploy, restart, docker exec, rpk topic
      produce, or runtime lane mutation was performed. The change is CI/pre-commit wiring plus static
      validators and comment-only annotations to one service file; no runtime behavior change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c 'deploy-gate evidence' contracts/OMN-13412.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13412.yaml consumed
      by omnibase_infra PR #2067 Receipt Gate and Deploy Gate via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13412\"$' contracts/OMN-13412.yaml"
