---
schema_version: "1.0.0"
ticket_id: "OMN-11736"
title: "Update receipt gate for dev/main split"
summary: >-
  Central OCC evidence for the branch-aware receipt gate mode that keeps dev PR evidence on the existing
  per-PR path while allowing main release enforcement to require merged OCC evidence and promotion/hotfix
  receipts.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "omnibase_core PR #1143 carries the branch-aware receipt gate implementation."
    command: "See drift/dod_receipts/OMN-11736/dod-omnibase-core-pr-1143/command.yaml"
  - kind: "manual"
    description: "Targeted branch-policy receipt gate tests passed."
    command: "See drift/dod_receipts/OMN-11736/dod-branch-policy-tests/command.yaml"
  - kind: "manual"
    description: "Focused ruff validation passed."
    command: "See drift/dod_receipts/OMN-11736/dod-ruff/command.yaml"
  - kind: "manual"
    description: "File-scoped pre-commit validation passed."
    command: "See drift/dod_receipts/OMN-11736/dod-precommit/command.yaml"
  - kind: "manual"
    description: "No live deploy is required for reusable receipt-gate policy code."
    command: "See drift/dod_receipts/OMN-11736/dod-deploy/command.yaml"
  - kind: "manual"
    description: "The OCC PR carrying this contract and receipts is self-bound."
    command: "See drift/dod_receipts/OMN-11736/dod-occ-pr-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1143"
    description: "omnibase_core PR #1143 carries the branch-aware receipt gate implementation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11736/dod-omnibase-core-pr-1143/command.yaml"
          && grep -q '^pr_number: 1143$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11736/dod-omnibase-core-pr-1143/command.yaml"
          && grep -q '^commit_sha: "9d726d7671c2986e5ac5bde0be40191b681a9f33"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11736/dod-omnibase-core-pr-1143/command.yaml"
  - id: "dod-branch-policy-tests"
    description: "Targeted receipt gate branch-policy tests passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11736/dod-branch-policy-tests/command.yaml"
          && grep -q '52 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11736/dod-branch-policy-tests/command.yaml"
  - id: "dod-ruff"
    description: "Focused ruff validation passed on modified Python files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11736/dod-ruff/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11736/dod-ruff/command.yaml"
  - id: "dod-precommit"
    description: "File-scoped pre-commit validation passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11736/dod-precommit/command.yaml"
          && grep -q 'No hardcoded ONEX topic strings.*Passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11736/dod-precommit/command.yaml"
  - id: "dod-deploy"
    description: "No live deploy required; this changes reusable CI gate policy and tests only."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11736/dod-deploy/command.yaml"
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carrying OMN-11736 contract and receipts is self-bound."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11736/dod-occ-pr-self/command.yaml"
          && grep -q '^pr_number:' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11736/dod-occ-pr-self/command.yaml"
