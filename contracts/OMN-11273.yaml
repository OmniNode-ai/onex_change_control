---
schema_version: 1.0.0
ticket_id: OMN-11273
title: Create runner health collector handler with GitHub API and SSH sources
summary: >
  Bind omnibase_infra#1674 to central OCC evidence for HandlerRunnerHealthCollector, including the focused
  unit and integration proof for the GitHub Actions API and SSH Docker source reconciliation path.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: omnibase_infra#1674 focused runner-health collector unit and integration tests pass on
      the PR head.
    command: uv run pytest tests/unit/observability/runner_health/test_handler_runner_health_collector.py
      tests/integration/test_runner_health_collector_integration.py -q
  - kind: ci
    description: onex_change_control#1182 binds OMN-11273 central evidence to an OCC PR receipt.
    command: gh pr checks 1182 --repo OmniNode-ai/onex_change_control
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-runner-health-collector-tests
    description: omnibase_infra#1674 runner health collector unit and integration tests are bound to OMN-11273.
    source: generated
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11273/dod-runner-health-collector-tests/command.yaml"
          && grep -q '^ticket_id: OMN-11273$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11273/dod-runner-health-collector-tests/command.yaml"
          && grep -q '^pr_number: 1674$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11273/dod-runner-health-collector-tests/command.yaml"
  - id: dod-onex-change-control-pr-1182
    description: onex_change_control#1182 OCC evidence PR is bound to OMN-11273.
    source: generated
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11273/dod-onex-change-control-pr-1182/command.yaml"
          && grep -q '^ticket_id: OMN-11273$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11273/dod-onex-change-control-pr-1182/command.yaml"
          && grep -q '^pr_number: 1182$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11273/dod-onex-change-control-pr-1182/command.yaml"
