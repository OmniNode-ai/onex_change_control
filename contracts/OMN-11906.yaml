---
schema_version: "1.0.0"
ticket_id: "OMN-11906"
title: "Wave 6 infrastructure and monitoring declarative contract migration"
summary: >
  Bind central evidence for omnimarket PR #804, which migrates infrastructure and monitoring nodes to
  declarative handler_routing contracts, fixes contract topic naming, and proves the state-persist effect
  golden chain.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-804"
    description: "Bind OMN-11906 evidence to omnimarket PR #804."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11906/dod-omnimarket-pr-804/command.yaml
          && grep -q '^pr_number: 804$' drift/dod_receipts/OMN-11906/dod-omnimarket-pr-804/command.yaml"
  - id: "dod-state-persist-golden-chain"
    description: "State-persist golden-chain proof passes after command-topic migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11906/dod-state-persist-golden-chain/command.yaml
          && grep -q '9 passed' drift/dod_receipts/OMN-11906/dod-state-persist-golden-chain/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence for infrastructure and monitoring contract migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11906/dod-deploy-readiness/command.yaml
          && grep -q 'contract migration has focused golden-chain and lint proof' drift/dod_receipts/OMN-11906/dod-deploy-readiness/command.yaml"
