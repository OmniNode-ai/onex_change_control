---
schema_version: "1.0.0"
ticket_id: OMN-7944
title: "Add schema repair compute node in omnimarket"
summary: "Add node_schema_repair_compute in omnimarket as a pure COMPUTE node that builds deterministic
  repair prompts from malformed model output and schema validation errors."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
evidence_requirements:
  - kind: "ci"
    description: "omnimarket#570 checks pass for node_schema_repair_compute"
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001-omnimarket-570"
    description: "omnimarket#570 carries node_schema_repair_compute implementation, CodeRabbit fence fix,
      and deploy-gate contract"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json headRefOid -q .headRefOid"
  - id: "dod-occ-pr-838"
    description: "onex_change_control#838 carries OMN-7944 central contract and receipt evidence"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json state,baseRefName
          --jq '.state + \":\" + .baseRefName'"
  - id: "dod-occ-pr-839"
    description: "onex_change_control#839 updates OMN-7944 receipt evidence for the rebased omnimarket#570
      head"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json state,baseRefName
          --jq '.state + \":\" + .baseRefName'"
