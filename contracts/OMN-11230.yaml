---
schema_version: "1.0.0"
ticket_id: "OMN-11230"
title: "feat(OMN-11230): create node_session_phase_reducer REDUCER node"
summary: >
  Creates node_session_phase_reducer — a pure REDUCER node implementing delta(state, event) -> new_state.
  Materializes canonical session phase state from session-started, session-ended, and session-phase-state
  events. Writes .onex_state/session/phase_state.yaml for hook consumption. Returns projections[] only
  (strict REDUCER contract). Part of epic OMN-11224.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "14 unit tests pass covering delta function, file projection, and field completeness"
    command: "cd omnimarket && uv run pytest tests/unit/nodes/node_session_phase_reducer/ -v"
  - kind: "ci"
    description: "omnimarket PR #703 carries the reducer implementation with CI green"
    command: "gh pr view 703 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-703"
    description: "omnimarket PR #703 carries node_session_phase_reducer with 14 passing unit tests, mypy
      strict clean, and all pre-commit hooks passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11230/dod-omnimarket-pr-703/command.yaml
          && grep -q '^pr_number: 703$' drift/dod_receipts/OMN-11230/dod-omnimarket-pr-703/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carrying this contract and binding receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11230/dod-occ-pr/command.yaml"
