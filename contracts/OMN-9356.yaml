---
schema_version: '1.0.0'
ticket_id: OMN-9356
summary: 'feat(core): runtime_sha_match DoD gate — hard-block Done if deployed SHA != merge SHA'
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: ci
    description: CI passes on merge PR
    command: gh pr checks
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: PR opened against main branch in omnibase_core
    source: generated
    checks:
      - check_type: command
        check_value: 'gh pr view 872 --repo OmniNode-ai/omnibase_core --json state,baseRefName -q ''[.state,
          .baseRefName] | @tsv'''
  - id: dod-002
    description: 20 unit tests pass on omnibase_core PR  #872
    source: generated
    checks:
      - check_type: command
        check_value: 'result=$(gh pr view 872 --repo OmniNode-ai/omnibase_core --json statusCheckRollup
          -q ''[.statusCheckRollup[] | select(.name == "Tests Gate") | .conclusion] | first // empty'');
          [ "$result" = "SUCCESS" ]'
  - id: dod-003
    description: Security scan (detect-secrets) passes on omnibase_core PR  #872
    source: generated
    checks:
      - check_type: command
        check_value: 'result=$(gh pr view 872 --repo OmniNode-ai/omnibase_core --json statusCheckRollup
          -q ''[.statusCheckRollup[] | select(.name == "Code Quality") | .conclusion] | first // empty'');
          [ "$result" = "SUCCESS" ]'
