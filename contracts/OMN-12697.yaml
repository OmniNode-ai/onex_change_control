---
schema_version: "1.0.0"
ticket_id: "OMN-12697"
title: "Dead-code cleanup: 5 R8 delegation-wire re-export stubs (omnimarket)"
summary: >
  Deletes the 5 dead R8 re-export stubs in omnimarket (adapters/database.py + models/delegation/wire/{model_event_envelope,
  model_orchestrator_intents, model_routing_config, model_task_delegated_event}.py). They duplicated classes
  now canonical in omnibase_core (graduated under OMN-12659/OMN-12126) and had zero importers in the omnimarket
  tree — the wire package __init__ already re-exports the live symbols from omnibase_core. Covers the
  5-stub sub-item of the OMN-12697 12-item WS-B dead-code cleanup.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-stubs-removed"
    description: "omnimarket PR #1486 deletes the 5 dead re-export stubs and adds a regression guard."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12697/dod-stubs-removed/command.yaml\""
  - id: "dod-full-suite-green"
    description: "omnimarket full non-kafka suite passes after the deletions."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12697/dod-full-suite-green/command.yaml\""
  - id: "dod-occ-self"
    description: "onex_change_control OCC PR carries the OMN-12697 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12697/dod-occ-self/command.yaml\""
