---
schema_version: "1.0.0"
ticket_id: "OMN-7464"
title: "Runtime cold start Kafka subscription parallelization"
summary: >-
  Bind central OCC evidence for omnibase_infra PR #1706, which removes cold-start serialization by starting
  Kafka topic consumers outside the subscription lock and adds focused concurrent subscription coverage.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused local coverage proves concurrent distinct-topic startup and duplicate-key coalescing."
    command: "uv run pytest tests/integration/event_bus/test_kafka_concurrent_subscribe_startup.py tests/unit/event_bus/test_kafka_event_bus.py::TestKafkaEventBusSubscribe::test_concurrent_subscribe_no_duplicate_consumers
      tests/unit/event_bus/test_kafka_event_bus.py::TestKafkaEventBusSubscribe::test_concurrent_subscribe_same_key_starts_one_consumer
      -q"
  - kind: "ci"
    description: "omnibase_infra PR #1706 checks pass after central OCC evidence is attached."
    command: "gh pr checks 1706 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1706"
    description: "Bind OMN-7464 evidence to omnibase_infra PR #1706 at the observed PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7464/dod-omnibase-infra-pr-1706/command.yaml\"
          && grep -q '^pr_number: 1706$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7464/dod-omnibase-infra-pr-1706/command.yaml\"
          && grep -q '^commit_sha: \"1801c4cd48c0fea098ad5d9fd8bf808b24db7f3c\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7464/dod-omnibase-infra-pr-1706/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Bind OMN-7464 central evidence to the OCC PR that carries this bundle."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7464/dod-occ-pr-self/command.yaml\"
          && grep -q '^ticket_id: \"OMN-7464\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7464/dod-occ-pr-self/command.yaml\"
          && grep -q '^pr_number: 1352$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7464/dod-occ-pr-self/command.yaml\""
