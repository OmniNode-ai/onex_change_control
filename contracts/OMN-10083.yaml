---
schema_version: "1.0.0"
ticket_id: "OMN-10083"
summary: "Cut current market skills to the local runtime client path"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused omnimarket client-path and skill-shim tests pass locally"
    command: "cd ../omnimarket && uv run pytest tests/test_codex_plugin_shims.py tests/unit/test_generate_adapters.py
      -q"
  - kind: "docs"
    description: "Skill docs and examples reflect the runtime-ingress path instead of direct Kafka or
      direct CLI as the default"
    command: "cd ../omnimarket && rg -n 'runtime ingress|local runtime client|Unix socket' plugins/onex/skills
      src/omnimarket/adapters/codex"
  - kind: "ci"
    description: "omnimarket PR checks pass for the skill client-path cutover"
    command: "gh pr checks --repo OmniNode-ai/omnimarket --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "A supported omnimarket local-runtime client path exists and the current skill shims
      point at it"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10083/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "The default market-skill execution path no longer requires direct Kafka publish access
      or direct CLI fallback"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10083/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "Focused client-path tests and dry-run degradation checks are captured canonically"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10083/dod-003 -type f -name '*.yaml' | grep -q ."
