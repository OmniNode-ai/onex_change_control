---
schema_version: "1.0.0"
ticket_id: "OMN-16821"
title: "OCC evidence for OMN-16821 — autoclose flip predicate uses a verdict-bearing denominator"
summary: >
  OCC evidence companion for OmniNode-ai/omnibase_infra PR #2957: the evidence-autoclose sweep's flip
  predicate compared verified_count against total_checks, a denominator that includes non_probative verdicts,
  making it unsatisfiable for most of the OCC corpus and leaving the flip path never once exercised. The
  predicate now compares verified_count + non_probative_count against total_checks, with failed_count
  == 0 and verified_count > 0 so the merge-state-only corpus stays un-flippable, and the gap comment names
  the shortfall it actually found instead of asserting the ACs are unproven.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2957 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2957-predicate"
    description: >
      PR #2957 on OmniNode-ai/omnibase_infra — the flip equality on the merged handler is the verdict-bearing
      one: verified_count + non_probative_count == total_checks. The old `verified_count == total_checks`
      conjunct is gone from the predicate.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py?ref=2f131bc347599104411f84de2dfead45c050b3b9 --jq '.content' | base64 -d | grep -c 'verified_count + non_probative_count == total_checks'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py?ref=2f131bc347599104411f84de2dfead45c050b3b9 --jq '.content' | base64 -d | grep -c '_DOD_VERIFY_NON_PROBATIVE_KEY = "non_probative_count"'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2957-unflippable"
    description: >
      The merge-state-only corpus stays un-flippable by arithmetic and not only by dod_verify's own terminal
      status: `verified_count > 0` is a conjunct of the predicate, so a denominator made entirely of non-probative
      entries can never satisfy the equality.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py?ref=2f131bc347599104411f84de2dfead45c050b3b9 --jq '.content' | base64 -d | grep -c 'and verified_count > 0'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2957-gap-wording"
    description: >
      AC4 — the gap comment names the real shortfall. `_gap_shortfall` exists on the merged handler and
      the single hardcoded "not all ACs are receipt-proven" clause is no longer the only wording a gap
      can carry.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py?ref=2f131bc347599104411f84de2dfead45c050b3b9 --jq '.content' | base64 -d | grep -c 'def _gap_shortfall'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2957-tests"
    description: >
      AC3 — both directions are pinned. The OMN-16821 suite exists at the merged ref and carries the headline
      flip case, the all-non-probative refusal, and the forged-status refusal that goes red if the `verified_count
      > 0` conjunct is ever dropped as redundant.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_16821_non_probative_denominator.py?ref=2f131bc347599104411f84de2dfead45c050b3b9 --jq '.content' | base64 -d | grep -c 'def test_omn_16260_shape_reaches_a_flip'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_16821_non_probative_denominator.py?ref=2f131bc347599104411f84de2dfead45c050b3b9 --jq '.content' | base64 -d | grep -c 'def test_a_forged_verified_status_over_all_non_probative_still_gaps'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_16821_non_probative_denominator.py?ref=2f131bc347599104411f84de2dfead45c050b3b9 --jq '.content' | base64 -d | grep -c 'def test_an_all_non_probative_contract_still_gaps'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2957 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/models/model_evidence_autoclose_outcome.py?ref=2f131bc347599104411f84de2dfead45c050b3b9 --jq '.content' | base64 -d | grep -c 'dod_verify_non_probative_count'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7377"
    description: "OCC companion PR #7377 — self-bind for OMN-16821 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7377 --repo OmniNode-ai/onex_change_control --json number,state"
