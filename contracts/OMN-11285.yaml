---
schema_version: "1.0.0"
ticket_id: "OMN-11285"
title: "Track omnibase_infra idna Dependabot update"
summary: "Bind omnibase_infra PR #1675 idna Dependabot lockfile update evidence."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Root and deploy-agent uv lockfiles are already resolved after the idna update."
    command: "uv lock --check && (cd scripts/deploy-agent && uv lock --check)"
  - kind: "ci"
    description: "onex_change_control PR #1190 binds the OMN-11285 central contract and PR #1675 receipt."
    command: "gh pr checks 1190 --repo OmniNode-ai/onex_change_control"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Bind omnibase_infra PR #1675 idna lockfile evidence to its Dependabot head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11285/dod-001/command.yaml\"; else uv lock --check
          && (cd scripts/deploy-agent && uv lock --check); fi"
  - id: "dod-occ-pr-1190"
    description: "OCC PR #1190 carries the OMN-11285 central contract and PR #1675 receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11285/dod-occ-pr-1190/command.yaml\"
          && grep -q '^pr_number: 1190$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11285/dod-occ-pr-1190/command.yaml\""
