---
schema_version: "1.0.0"
ticket_id: "OMN-17852"
title: "Autobind OCC evidence for OMN-17852"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3171.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3171 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3171 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/unit/models/test_model_serialization_roundtrip.py tests/unit/runtime/test_rsd_postgres_acceptance_overlay.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3171"
    description: "PR #3171 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/models/model_rsd_postgres_acceptance_overlay.py?ref=f4597186ceadf00064e13868667b2767afefa161 --jq '.content' | base64 -d | grep -c 'class ModelRsdPostgresAcceptanceOverlay'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3171/files --paginate --jq '.[].filename' | grep -cFx 'docker/lane-overlays/dev.rsd-postgres-acceptance.yaml'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #3171 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3171/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3171 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/models/test_model_serialization_roundtrip.py tests/unit/runtime/test_rsd_postgres_acceptance_overlay.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
