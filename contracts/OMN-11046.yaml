---
schema_version: "1.0.0"
ticket_id: "OMN-11046"
title: "Fix dependency health AST import scanner edge explosion"
summary: >
  Central OCC evidence for omnimarket PR #648. Replaces prefix-based import matching with exact path-based
  module resolution so dependency health scans do not emit quadratic false-positive edges.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused dependency health scanner tests pass."
    command: "See drift/dod_receipts/OMN-11046/dod-focused-tests/command.yaml"
  - kind: "ci"
    description: "Deploy/import smoke for the affected scanner passes without live deploy or runtime restart."
    command: "See drift/dod_receipts/OMN-11046/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused dependency health scanner tests cover exact import resolution and bounded edge
      counts."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '13 passed' drift/dod_receipts/OMN-11046/dod-focused-tests/command.yaml
          && echo 'OMN-11046 focused-test receipt present'"
  - id: "dod-deploy"
    description: "Deploy/import smoke for ASTImportScanner is recorded; no live deploy or runtime restart
      was performed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'ASTImportScanner' drift/dod_receipts/OMN-11046/dod-deploy/command.yaml
          && grep -q 'deploy' drift/dod_receipts/OMN-11046/dod-deploy/command.yaml && echo 'OMN-11046
          deploy receipt present'"
