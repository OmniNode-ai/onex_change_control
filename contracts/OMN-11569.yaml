---
schema_version: "1.0.0"
ticket_id: "OMN-11569"
title: "Derive overnight handler topics from contract"
summary: >-
  Bind central OCC evidence for omnimarket PR #756, which derives node_overnight handler topic exports
  from contract.yaml and removes source-owned topic literals from the runtime handler.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omnimarket PR #756 overnight topic binding tests pass in CI."
    command: "gh pr checks 756 --repo OmniNode-ai/omnimarket"
  - kind: "manual"
    description: "Deploy-gate assessment confirms the runtime handler topic-binding change needs post-merge
      validation, not live pre-merge deployment."
    command: "deploy assessment: verify contract-derived topic bindings in CI and rerun downstream deploy
      gate after OCC merge"
dod_evidence:
  - id: "dod-omnimarket-pr-756"
    description: "Bind OMN-11569 evidence to omnimarket PR #756 at the observed PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11569/dod-omnimarket-pr-756/command.yaml\"
          && grep -q '^pr_number: 756$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11569/dod-omnimarket-pr-756/command.yaml\"
          && grep -q '^commit_sha: \"e527e77fe8520c8ea59261b6412466972939c732\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11569/dod-omnimarket-pr-756/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Record deploy-gate assessment for the runtime handler topic-binding path."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11569/dod-deploy-assessment/command.yaml\"
          && grep -qi 'deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11569/dod-deploy-assessment/command.yaml\"
          && grep -q '^pr_number: 756$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11569/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Bind OMN-11569 central evidence to the OCC PR that carries this bundle."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11569/dod-occ-pr-self/command.yaml\"
          && grep -q '^ticket_id: \"OMN-11569\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11569/dod-occ-pr-self/command.yaml\"
          && grep -q '^pr_number: 1363$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11569/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
