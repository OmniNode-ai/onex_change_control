---
schema_version: "1.0.0"
ticket_id: "OMN-11941"
title: "Ticket work research enrichment evidence"
summary: >
  Central OCC evidence for OMN-11941 across the ticket-work research enrichment flow. It covers the omniclaude
  ticket-work skill documentation and the omnimarket node_ticket_research_enrichment_compute implementation,
  which deterministically enriches RESEARCH phase ticket work with auditable knowledge context while preserving
  non-blocking timeout and error behavior.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1652"
    description: "omniclaude PR #1652 documents the ticket-work research enrichment flow."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11941/dod-omniclaude-pr-1652/command.yaml
          && grep -q '^pr_number: 1652$' drift/dod_receipts/OMN-11941/dod-omniclaude-pr-1652/command.yaml"
  - id: "dod-omnimarket-pr-837"
    description: "Bind OMN-11941 evidence to omnimarket PR #837."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11941/dod-omnimarket-pr-837/command.yaml
          && grep -q '^pr_number: 837$' drift/dod_receipts/OMN-11941/dod-omnimarket-pr-837/command.yaml"
  - id: "dod-ticket-research-enrichment-local-validation"
    description: "Focused ticket research enrichment tests, cross-node reach-in guard, ruff, leaked-literals,
      dep-health, and imperative contract guard pass for the current omnimarket PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11941/dod-ticket-research-enrichment-local-validation/command.yaml
          && grep -q '8 passed' drift/dod_receipts/OMN-11941/dod-ticket-research-enrichment-local-validation/command.yaml
          && grep -q 'baseline_delta: 0' drift/dod_receipts/OMN-11941/dod-ticket-research-enrichment-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: the new ticket research enrichment compute node is locally validated
      and requires no live runtime deployment before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11941/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11941/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11941 contract, deploy evidence, and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11941/dod-occ-pr/command.yaml"
