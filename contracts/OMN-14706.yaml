---
schema_version: "1.0.0"
ticket_id: "OMN-14706"
title: "Autobind OCC evidence for OMN-14706"
summary: >
  OCC contract bound by C3 manual merge lane for omniclaude#1906, proving the Phase 3 shadow Product Readiness
  diff before downstream receipt gates are rerun.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1906 product diff scope present"
    command: "PRODUCT_PR_NUMBER=1906; PRODUCT_REPO=OmniNode-ai/omniclaude; gh pr diff ${PRODUCT_PR_NUMBER}
      --repo ${PRODUCT_REPO} --name-only | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniclaude-pr-1906"
    description: "Product PR exists for OMN-14706"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1906; PRODUCT_REPO=OmniNode-ai/omniclaude; gh pr view ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omniclaude-pr-1906-ci"
    description: "Product PR diff is non-empty for OMN-14706"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1906; PRODUCT_REPO=OmniNode-ai/omniclaude; gh pr diff ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --name-only | grep -q ."
