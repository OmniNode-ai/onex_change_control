---
schema_version: "1.0.0"
ticket_id: "OMN-11086"
title: "ModelNodeGenerationRequest + ModelNodeGenerationResult in omnibase_core"
summary: >
  Adds frozen Pydantic models for the hackathon MVP node generation pipeline. ModelNodeGenerationRequest
  enforces compute-only node type via field_validator. ModelNodeGenerationResult captures node name, YAML/source
  artifacts, sha256 hashes, and subprocess stdout/stderr/returncode. Both models use ConfigDict(frozen=True,
  extra="forbid"). Exported from omnibase_core.models.generation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "8 unit tests in tests/unit/models/generation/test_model_node_generation.py pass."
    command: "See drift/dod_receipts/OMN-11086/dod-tests/command.yaml"
  - kind: "ci"
    description: "Central OCC PR carries the OMN-11086 contract and receipt bundle."
    command: "See drift/dod_receipts/OMN-11086/dod-occ-pr-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "8 unit tests covering request/result model validation, frozen enforcement, and extra-fields
      rejection pass."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11086/dod-tests/command.yaml &&
          grep -q '^pr_number: 1085$' drift/dod_receipts/OMN-11086/dod-tests/command.yaml"
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carrying the central OMN-11086 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11086/dod-occ-pr-self/command.yaml"
