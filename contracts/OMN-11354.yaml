---
schema_version: "1.0.0"
ticket_id: "OMN-11354"
title: "Entropy Experiment Node-Based Rewrite"
summary: >-
  Bind central OCC evidence for the onex-self-extending-agent entropy experiment rewrite, which moves
  the experiment from standalone research scaffolding to typed contracts, event-native orchestration,
  reducer/effect evidence boundaries, runtime topology proof, and deterministic reporting.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
  - "protocols"
  - "envelopes"
evidence_requirements:
  - kind: "tests"
    description: "The integrated entropy rewrite unit suite, typecheck, lint, formatting check, wheel
      build, and package-data proof pass locally."
    command: "See drift/dod_receipts/OMN-11354/dod-local-verification/command.yaml"
  - kind: "manual"
    description: "The onex-self-extending-agent PR stack is bound with PR numbers, branch names, and head
      commits."
    command: "See drift/dod_receipts/OMN-11354/dod-onex-self-extending-agent-pr-stack/command.yaml"
dod_evidence:
  - id: "dod-onex-self-extending-agent-pr-stack"
    description: "The onex-self-extending-agent PR stack carries the six-wave entropy node rewrite."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11354/dod-onex-self-extending-agent-pr-stack/command.yaml"
          && grep -q 'pr_61_head_sha=60687b03449f92125c30dca3bdcdd2de9ed646ee' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11354/dod-onex-self-extending-agent-pr-stack/command.yaml"
          && grep -q 'pr_69_head_sha=61cb2836dc98b2fee327cf498e314eb9fd9a0cc8' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11354/dod-onex-self-extending-agent-pr-stack/command.yaml"
  - id: "dod-local-verification"
    description: "Focused local verification passes for the integrated entropy rewrite."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11354/dod-local-verification/command.yaml"
          && grep -q '558 passed, 1 skipped' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11354/dod-local-verification/command.yaml"
          && grep -q 'Success: no issues found in 82 source files' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11354/dod-local-verification/command.yaml"
          && grep -q 'wheel package-data check: PASS' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11354/dod-local-verification/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11354 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11354/dod-occ-pr-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
