---
schema_version: "1.0.0"
ticket_id: OMN-11178
title: "Move LLM endpoint configuration into node contracts"
summary: "Bind omnimarket PR #701 to central evidence for migrating LLM endpoint configuration to node
  contract config declarations."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - protocols
  - public_api
evidence_requirements:
  - kind: ci
    description: "omnimarket#701 carries the LLM endpoint contract config migration."
    command: "gh pr checks 701 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-omnimarket-pr-701
    description: "omnimarket PR #701 is bound to OMN-11178."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11178/dod-omnimarket-pr-701/command.yaml
          && grep -q '^ticket_id: OMN-11178$' drift/dod_receipts/OMN-11178/dod-omnimarket-pr-701/command.yaml
          && grep -q '^pr_number: 701$' drift/dod_receipts/OMN-11178/dod-omnimarket-pr-701/command.yaml"
  - id: dod-deploy-assessment
    description: "Runtime config wiring changed, but no live deploy, restart, or .201 mutation was performed
      before merge."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11178/dod-deploy-assessment/command.yaml
          && grep -q '^ticket_id: OMN-11178$' drift/dod_receipts/OMN-11178/dod-deploy-assessment/command.yaml
          && grep -q '^pr_number: 701$' drift/dod_receipts/OMN-11178/dod-deploy-assessment/command.yaml"
