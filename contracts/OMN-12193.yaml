---
schema_version: "1.0.0"
ticket_id: "OMN-12193"
title: "Create AdapterProjectTrackerLinear migration target"
summary: >
  Central OCC evidence for omnibase_infra PR #1756. The infra change adds
  AdapterProjectTrackerLinear as the canonical migration target for the compat
  Linear project tracker shim, with typed models for teams, labels, and issue
  statuses.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused AdapterProjectTrackerLinear unit tests pass."
    command: "See drift/dod_receipts/OMN-12193/dod-adapter-tests/command.yaml"
  - kind: "ci"
    description: "Ruff check passes for the project tracker adapter package and focused tests."
    command: "See drift/dod_receipts/OMN-12193/dod-ruff/command.yaml"
  - kind: "ci"
    description: "ONEX validators pass after splitting models and typing the Linear workflow status."
    command: "See drift/dod_receipts/OMN-12193/dod-onex-validators/command.yaml"
  - kind: "ci"
    description: "Deploy-gate posture is recorded for PR #1756."
    command: "See drift/dod_receipts/OMN-12193/dod-deploy-gate/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-adapter-tests"
    description: "Focused AdapterProjectTrackerLinear unit tests passed in the omnibase_infra worktree."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12193/dod-adapter-tests/command.yaml && grep -q '15 passed' drift/dod_receipts/OMN-12193/dod-adapter-tests/command.yaml"
  - id: "dod-ruff"
    description: "Ruff check passed for the touched project tracker adapter package and focused tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12193/dod-ruff/command.yaml && grep -q 'All checks passed' drift/dod_receipts/OMN-12193/dod-ruff/command.yaml"
  - id: "dod-onex-validators"
    description: "ONEX validators passed locally with Architecture, Patterns, and Any Types clean."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12193/dod-onex-validators/command.yaml && grep -q 'Summary: 15/15 passed' drift/dod_receipts/OMN-12193/dod-onex-validators/command.yaml"
  - id: "dod-deploy-gate"
    description: "Deploy-gate posture for the adapter migration PR is recorded; no live pre-merge deploy was required."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12193/dod-deploy-gate/command.yaml && grep -q 'deploy-gate:' drift/dod_receipts/OMN-12193/dod-deploy-gate/command.yaml"
