---
schema_version: "1.0.0"
ticket_id: OMN-10794
title: "feat(OMN-10794): wire HandlerComplianceLoop into HandlerDelegationWorkflow"
summary: >
  Wave 3 / Task 5 of the tokens-to-compliance epic. Adds output_schema_key and compliance_budget to ModelDelegationRequest,
  threads HandlerComplianceLoop into HandlerDelegationWorkflow.handle_inference_response, allows ROUTED
  -> ROUTED self-loop for repair re-prompts in the FSM, and accumulates tokens_to_compliance + compliance_attempts
  onto the terminal ModelDelegationResult and ModelTaskDelegatedEvent. Backwards compatible: legacy callers
  that omit output_schema_key get the unchanged single-attempt path. Bumps node_delegation_orchestrator
  contract to 0.3.0.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-unit-tests
    description: "All 41 tests pass on omnibase_infra branch (9 new compliance-loop wiring tests + 32
      existing orchestrator tests proving legacy path is unchanged)."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10794/dod-unit-tests/command.yaml
          && grep -q '^ticket_id: OMN-10794$' drift/dod_receipts/OMN-10794/dod-unit-tests/command.yaml"
  - id: dod-occ-pr
    description: "Self-binding receipt for this OCC PR."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10794/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: OMN-10794$' drift/dod_receipts/OMN-10794/dod-occ-pr/command.yaml"
