---
schema_version: "1.0.0"
ticket_id: "OMN-13829"
title: "fix(OMN-13829): install node-skill package into infra venv"
summary: >-
  omnibase_infra PR #2174 adds an operator-gated install script for refreshing the omnimarket node-skill
  package in an infra virtualenv without mutating the canonical operator venv during CI. The change is
  intentionally additive: a dry-run-by-default script, a runbook, and focused script tests. Runtime mutation
  remains behind the script's --execute flag and operator action.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2174"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2174 carries the node-skill install script, runbook, and
      tests at the recorded head commit.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13829/dod-infra-pr-2174/command.yaml"
  - id: "dod-local-validation"
    description: >-
      Local validation recorded by the PR: focused script tests pass, changed-file ruff/pre-commit gates
      pass, and full unit-suite residual failures are pre-existing environmental INFISICAL_ADDR failures.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13829/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy disposition for the operator-gated venv install script: no live venv mutation is performed
      by this PR or its CI. Execution remains an explicit post-merge operator action using --execute.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13829/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      This onex_change_control PR carries the central OMN-13829 contract and PASS receipts consumed by
      omnibase_infra PR #2174 through OCC preflight and Receipt Gate.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13829/dod-occ-self/command.yaml"
