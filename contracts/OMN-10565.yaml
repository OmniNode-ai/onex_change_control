---
schema_version: "1.0.0"
ticket_id: "OMN-10565"
title: "chore: contract config audit baseline"
summary: "Generate the omnimarket contract config audit CSV + summary classifying every contract as
  config_free, config_required, or needs_review. Input for Task 18 (OMN-10567) which will apply scoped
  contract changes only to config_required rows."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Receipt Gate passes on omnimarket PR #533 after this OCC evidence lands."
    command: "gh pr checks 533 --repo OmniNode-ai/omnimarket --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Audit script runs and writes one CSV row per contract.yaml under src/omnimarket/nodes/."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/audit/contract_config_audit.py --print-summary"
  - id: "dod-002"
    description: "8 unit tests cover positive + drift-detection + CSV/MD emission."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/scripts/test_contract_config_audit.py -v"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10565 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10565/dod-occ-pr/command.yaml\""
