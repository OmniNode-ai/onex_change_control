---
schema_version: "1.0.0"
ticket_id: "OMN-11529"
title: "feat(OMN-11529): add demo recordings manifest"
summary: >-
  Add asciinema v2 demo recordings and a manifest for the OMN-11529 judge flow, covering the agent-stage
  replay, full declared demo, and deterministic golden replay paths with mode banners, canonical task
  metadata, hashes, model identity, and uploaded asciinema.org links.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "onex-self-extending-agent PR #111 carries the OMN-11529 demo recordings and manifest."
    command: "gh pr view 111 --repo OmniNode-ai/onex-self-extending-agent --json number,state,headRefName,headRefOid,url,title"
  - kind: "tests"
    description: "Demo manifest is valid JSON and all cast files are asciinema v2 files with mode banners
      and the canonical task."
    command: "python3 -m json.tool docs/demo/demo_manifest.json && python3 <cast validation snippet>"
  - kind: "manual"
    description: "asciinema.org upload links were produced for all three recordings; unauthenticated 7-day
      retention is recorded in the manifest."
    command: "grep -q 'asciinema.org/a/' docs/demo/demo_manifest.json"
  - kind: "manual"
    description: "onex-self-extending-agent PR #112 sanitizes the recording path leak and makes committed
      cast files the durable source of truth."
    command: "gh pr view 112 --repo OmniNode-ai/onex-self-extending-agent --json number,state,headRefName,headRefOid,url,title"
  - kind: "ci"
    description: "onex_change_control PR carrying this central evidence is bound to OMN-11529."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11529/dod-occ-pr/command.yaml"
  - kind: "ci"
    description: "onex_change_control PR #1331 carries the sanitized recording follow-up receipts."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11529/dod-occ-pr-sanitize/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-111-open"
    description: "onex-self-extending-agent PR #111 carries OMN-11529 at the recorded head SHA."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-product-pr-111-open/command.yaml\"
          && grep -q '^pr_number: 111$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-product-pr-111-open/command.yaml\"
          && grep -q '^commit_sha: \"a7af83284eb130cdcf76b5d550e1082253dc41da\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-product-pr-111-open/command.yaml\""
  - id: "dod-recording-validation"
    description: "Manifest and cast contents validate locally."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-recording-validation/command.yaml\"
          && grep -q 'manifest and casts valid' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-recording-validation/command.yaml\""
  - id: "dod-asciinema-uploads"
    description: "asciinema.org returned public links for all three cast uploads."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-asciinema-uploads/command.yaml\"
          && grep -q 'https://asciinema.org/a/oHvzsjdiq8PjyUsV' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-asciinema-uploads/command.yaml\""
  - id: "dod-product-pr-112-open"
    description: "onex-self-extending-agent PR #112 carries the sanitized recording follow-up at the recorded
      head SHA."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-product-pr-112-open/command.yaml\"
          && grep -q '^pr_number: 112$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-product-pr-112-open/command.yaml\"
          && grep -q '^commit_sha: \"e57201413925b83c4c2f7e9a404b285ca2aa98ac\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-product-pr-112-open/command.yaml\""
  - id: "dod-sanitized-recording-validation"
    description: "Follow-up recording validation proves no local path leak remains and committed casts
      are durable playback artifacts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-sanitized-recording-validation/command.yaml\"
          && grep -q 'no /Users or omni_worktrees paths' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-sanitized-recording-validation/command.yaml\""
  - id: "dod-occ-pr-sanitize"
    description: "onex_change_control PR #1331 carries the sanitized recording follow-up receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-occ-pr-sanitize/command.yaml\"
          && grep -q '^pr_number: 1331$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-occ-pr-sanitize/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OMN-11529 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number:' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11529/dod-occ-pr/command.yaml\""
