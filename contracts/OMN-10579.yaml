---
schema_version: "1.0.0"
ticket_id: "OMN-10579"
title: "Build canonical test conftest + scrub test fixtures"
summary: "Add canonical portable fixtures to tests/conftest.py (fake_postgres_dsn, fake_kafka_bootstrap,
  fake_omni_home, fake_lan_ip, fake_llm_url, mock_settings). Replace all hardcoded 192.168.86.x LAN IPs
  across 14 unit test files. Add regression gate test_no_hardcoded_literals.py that greps all test .py
  files and fails on unannotated literals."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "14 unit/structure tests pass including the new regression gate."
    command: "uv run pytest tests/unit/structure/test_no_hardcoded_literals.py tests/unit/config/test_settings.py
      -v"
  - kind: "ci"
    description: "CI pipeline green on omnimarket PR #545."
    command: "gh pr checks 545 --repo OmniNode-ai/omnimarket --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "14 structure/config tests pass including test_no_hardcoded_literals regression gate."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10579/dod-001/command.yaml\"; else uv run pytest
          tests/unit/structure/test_no_hardcoded_literals.py tests/unit/config/test_settings.py -v; fi"
  - id: "dod-002"
    description: "Full unit+non-kafka test suite passes on the conftest branch (3416 passed)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10579/dod-002/command.yaml\"; else uv run pytest
          tests/ -m \"not kafka and not integration\" -q; fi"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10579 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10579/dod-occ-pr/command.yaml\""
