---
schema_version: "1.0.0"
ticket_id: "OMN-13310"
title: "Port GitHub token env-read validator to omnibase_core"
summary: >-
  Bind omnibase_core PR #1269 to central OCC evidence for porting the GitHub-token environment read ban
  into omnibase_core as a canonical static validator with Pydantic scan models, a COMPUTE handler, CLI
  entry point, exported pre-commit hook, and focused unit coverage. The PR changes validator, model, hook,
  and packaging metadata only; no live service deploy, runtime restart, Kafka topic mutation, or database
  migration is required before merge.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr-2800"
    description: >-
      OCC self-binding receipt for onex_change_control PR #2800. The PR replaces #2799 with a branch name
      that satisfies identity binding for OMN-13291 and carries the central evidence update for OMN-13310
      (github-token environment-read COMPUTE validator and hook export), including active omnibase_core
      PR #1280 binding receipts. This receipt binds the OCC evidence PR itself by pr_number so its own
      OCC preflight and Receipt Gate can pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13310/dod-occ-pr-2800/command.yaml
  - id: "dod-occ-pr-2799"
    description: >-
      OCC self-binding receipt for onex_change_control PR #2799. The PR carries the central evidence update
      for OMN-13310 (github-token environment-read COMPUTE validator and hook export), including active
      omnibase_core PR #1280 binding receipts. This receipt binds the OCC evidence PR itself by pr_number
      so its own OCC preflight and Receipt Gate can pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13310/dod-occ-pr-2799/command.yaml
  - id: "dod-omnibase_core-pr-1280"
    description: >-
      Consolidation binding receipt for omnibase_core PR #1280. The PR carries the active validator-fleet
      consolidation for OMN-13310 (github-token environment-read COMPUTE validator and hook export) after
      superseding closed PR #1279. This receipt binds the central OCC contract to the active consolidated
      citing PR by pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13310/dod-omnibase_core-pr-1280/command.yaml
  - id: "dod-omnibase_core-pr-1279"
    description: >-
      Consolidation binding receipt for omnibase_core PR #1279. The PR unions the conflict-coupled validator-fleet
      additions for OMN-13310 (github-token environment-read COMPUTE validator and hook export) into a
      single queue-safe branch. This receipt binds the central OCC contract to the consolidated citing
      PR by pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13310/dod-omnibase_core-pr-1279/command.yaml
  - id: "dod-omnibase-core-pr-1269"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1269 (jonahgabriel/omn-13310-github-token-ban-core-handler,
      base=dev). The PR carries the OMN-13310 GitHub-token env-read static validator, Pydantic scan models,
      COMPUTE handler, CLI entry point, exported hook metadata, and focused unit tests at the cited head
      commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13310/dod-omnibase-core-pr-1269/command.yaml
  - id: "dod-github-token-tests"
    description: >-
      Focused local proof for the GitHub-token env-read validator port. The omnibase_core PR branch passed
      tests/unit/validation/test_validator_github_token_env_reads.py with 25 tests covering violations,
      clean fixtures, scan models, handler behavior, CLI behavior, and suppression annotations.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13310/dod-github-token-tests/command.yaml
  - id: "dod-static-validator-deploy-assessment"
    description: >-
      Deploy-gate evidence for omnibase_core PR #1269. The touched files add a pure static validator,
      models, tests, package entry point, and pre-commit hook export only; no live service deploy, runtime
      restart, Kafka topic mutation, or database migration is required before merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13310/dod-static-validator-deploy-assessment/command.yaml
          # deploy: static validator only; no live deploy or runtime restart required
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13310 contract and omnibase_core
      PR #1269 binding. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check
      can resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13310/dod-occ-pr-self/command.yaml
