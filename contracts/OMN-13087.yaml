---
schema_version: '1.0.0'
ticket_id: OMN-13087
title: 'feat(omnimarket): projection contract for session.replay (NC-14)'
summary: >-
  Creates node_projection_session_replay — a reducer that projects session lifecycle events (started,
  prompt-submitted, tool-executed, outcome, ended) into session_replay_snapshots table, exposing topic
  onex.snapshot.projection.session.replay.v1 for the omnidash Session Replay widget.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - topics
  - events
evidence_requirements:
  - kind: tests
    description: >-
      28 unit tests for HandlerProjectionSessionReplay (accumulate, classify, state_delta, project, handle
      shim) pass. Hardcoded-topics gate and runtime dogfood inventory gate both green.
    command: 'uv run pytest src/omnimarket/nodes/node_projection_session_replay/tests/ tests/ci/test_check_no_hardcoded_topics.py::test_main_passes_on_current_repo
      tests/test_market_node_runtime_dogfood.py::test_market_node_runtime_dogfood_inventory_classifies_all_entry_points
      -v'
  - kind: ci
    description: CI pipeline green on PR
    command: 'gh pr checks --repo OmniNode-ai/omnimarket'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >-
      node_projection_session_replay directory exists with contract.yaml (node_type: reducer), handler,
      models, migration, and tests.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13087/dod-001/command.yaml"
  - id: dod-002
    description: >-
      contract.yaml declares node_type: reducer, subscribes to 5 session lifecycle topics, and exposes
      projection_api on onex.snapshot.projection.session.replay.v1.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13087/dod-002/command.yaml"
  - id: dod-003
    description: >-
      28 unit tests for HandlerProjectionSessionReplay pass including accumulate/classify/project/handle
      shim coverage.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13087/dod-003/command.yaml"
  - id: dod-004
    description: >-
      Hardcoded topic literals gate passes — all topic strings declared as annotated constants with onex-topic-allow.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13087/dod-004/command.yaml"
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt — this onex_change_control PR carries the OMN-13087 contract consumed by
      the omnimarket PR via Evidence-Source.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13087/dod-occ-self/command.yaml"
