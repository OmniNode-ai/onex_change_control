---
schema_version: "1.0.0"
ticket_id: "OMN-13086"
title: "feat(OMN-13086): projection contract for voice.sessions (NC-13)"
summary: >
  Creates node_projection_voice_sessions in omnimarket — a reducer node that subscribes to voice-session-started,
  voice-session-turn, and voice-session-ended events from omniclaude, UPSERTs into the voice_sessions
  table keyed on session_id, and exposes the onex.snapshot.projection.voice.sessions.v1 projection topic.
  Closes the NC-13 dashboard-declared-no-contract gap identified in the P2 integration inventory.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "25 golden-chain unit tests pass covering session_started, session_turn, session_ended,
      full lifecycle, batch projection, and contract wiring assertions"
    command: "cd omni_worktrees/OMN-13086/omnimarket && uv run pytest tests/test_golden_chain_projection_voice_sessions.py
      -v"
  - kind: "ci"
    description: "mypy --strict passes on all node source files"
    command: "cd omni_worktrees/OMN-13086/omnimarket && uv run mypy src/omnimarket/nodes/node_projection_voice_sessions/
      --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-voice-sessions-node"
    description: >
      omnimarket PR implements node_projection_voice_sessions with contract.yaml (node_type: reducer,
      subscribe_topics for all 3 voice event types, projection_api for onex.snapshot.projection.voice.sessions.v1),
      handler (UPSERT by session_id, turn dedup by turn_id, lifecycle routing), migration 0001_create_voice_sessions.sql,
      and 25 passing golden-chain tests. ruff, mypy --strict, and pre-commit all green.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'node_projection_voice_sessions' contracts/OMN-13086.yaml"
      - check_type: "command"
        check_value: "test -f contracts/OMN-13086.yaml"
      - check_type: "command"
        check_value: "grep -q 'onex.snapshot.projection.voice.sessions.v1' contracts/OMN-13086.yaml"
