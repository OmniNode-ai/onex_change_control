---
schema_version: "1.0.0"
ticket_id: "OMN-13935"
title: "feat(OMN-13935): escalating-complexity graded ladder benchmark — real per-rung floor<ceiling separation"
summary: >-
  omnimarket PR #1607 lands the operator §3.6 re-cut of the delegation benchmark: an escalating-complexity
  GRADED benchmark run across the EXISTING local delegation ladder (floor RTX-4090 Qwen3.6-27B, mid RTX-5090
  Qwen3.6-35B, ceiling DeepSeek-V4-Flash 284B), superseding the fixture-content smoke landed under OMN-13369
  (#1295) which scored hand-authored attempt text through the quality gate (a tautological gradient with
  zero AI-PC rungs). The new harness (src/omnimarket/delegation/graded_ladder) uses objective deterministic
  graders (numeric equality, substring, HumanEval-style code execution in an isolated timeout-bounded
  subprocess) over a 13-task easy/medium/hard corpus, grades GENUINE recorded per-rung outputs captured
  live from the local ladder, and asserts the acceptance criterion floor_weighted < ceiling_weighted by
  a required margin. Recorded result: floor 0.719 -> ceiling 1.0, margin 0.281 >= 0.15, monotonic non-decreasing.
  This is a proof/benchmark harness plus config + recorded fixtures + a live-capture recorder script;
  it adds NO node contract, NO topic, and performs NO live deploy. Live model capture is the separate
  scripts/ci recorder gated behind the live_model marker; CI grades the committed fixtures hermetically.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1607"
    description: >-
      Consuming-PR binding: omnimarket PR #1607 carries the OMN-13935 graded ladder benchmark harness,
      the escalating corpus + rung roster (incl. the 5090/4090 AI-PC rungs), the genuine recorded per-rung
      fixtures, and the floor<ceiling separation assertion test. Real probe: gh pr view 1607 --repo OmniNode-ai/omnimarket
      --json number,state,headRefOid,headRefName,title.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13935/dod-omnimarket-pr-1607/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment: this PR adds a new non-node support module (omnimarket.delegation.graded_ladder),
      a CI benchmark report + live-capture recorder script, an escalating corpus + rung-roster config,
      recorded evidence fixtures, and unit tests. It adds NO node contract.yaml, NO topic registration,
      and NO handler dispatch wiring; it performs no live deploy, no docker exec, and no rpk topic produce.
      Deploy-neutral.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13935/dod-deploy-assessment/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-13935/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying this OMN-13935 contract and the consuming-PR
      evidence for omnimarket PR #1607.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13935/dod-occ-self/command.yaml"
