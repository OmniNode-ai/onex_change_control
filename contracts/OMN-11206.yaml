---
schema_version: "1.0.0"
ticket_id: "OMN-11206"
title: "Add R11 doctrine check to design_to_plan"
summary: >
  Add an advisory R11 doctrine compliance check to the design_to_plan skill specification. R11 flags plan
  tasks that touch doctrine-governed surfaces such as Kafka topics, projections, API endpoints, and new
  data provenance, while leaving CI and runtime gates as the authoritative enforcement layer. Delivery
  via omniclaude PR #1600.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omniclaude PR #1600 checks pass before merge"
    command: "gh pr checks 1600 --repo OmniNode-ai/omniclaude --watch=false"
  - kind: "manual"
    description: "R11 output format documents separate advisory and clean alternatives"
    command: "grep -n \"Emit exactly one of the following forms\" plugins/onex/skills/design_to_plan/SKILL.md"
  - kind: "manual"
    description: "Focused pre-commit hooks passed for the changed SKILL.md"
    command: "uv run pre-commit run --files plugins/onex/skills/design_to_plan/SKILL.md"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-r11-output-format"
    description: "R11 output alternatives are shown separately and do not instruct agents to emit literal
      OR text."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11206/dod-r11-output-format/command.yaml\""
  - id: "dod-precommit-clean"
    description: "Focused pre-commit run passed for plugins/onex/skills/design_to_plan/SKILL.md."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11206/dod-precommit-clean/command.yaml\""
  - id: "dod-occ-pr"
    description: "Self-binding receipt for the OCC PR carrying the OMN-11206 contract and receipt bundle."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11206/dod-occ-pr/command.yaml\""
