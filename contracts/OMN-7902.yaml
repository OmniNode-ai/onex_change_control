---
schema_version: "1.0.0"
ticket_id: OMN-7902
title: 'test(coverage): add tests for tool_dict_any_checker (omnibase_core)'
summary: "test(coverage): add tests for tool_dict_any_checker (omnibase_core)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}"
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json mergedAt -q .mergedAt --repo ${REPO}"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --watch --repo ${REPO}"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-005"
    description: "Test coverage meets minimum threshold"
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest --cov --cov-fail-under=80"
  - id: "dod-core-1456-head"
    description: "Product PR #1456 exists on dev at exact head 3d43ff58."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=1456; REPO=OmniNode-ai/omnibase_core; gh pr view ${PR_NUMBER} --repo ${REPO}
          --json number,headRefOid,headRefName,baseRefName,title"
  - id: "dod-core-1456-test-only"
    description: "PR #1456 changes only unit test files for tool_dict_any_checker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=1456; REPO=OmniNode-ai/omnibase_core; gh pr view ${PR_NUMBER} --repo ${REPO}
          --json files"
  - id: "dod-core-1456-local-proof"
    description: "PR #1456 body records local unit coverage, mypy, ruff, and pre-commit proof for the
      exact test-only change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-7902/dod-core-1456-local-proof/command.yaml"
  - id: "dod-core-1456-no-deploy"
    description: "PR #1456 is test-only and does not deploy, push images, restart runtime, mutate live
      infrastructure, mutate branch protection, or cut over required contexts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-7902/dod-core-1456-no-deploy/command.yaml"
  - id: "dod-supersede-002-product-open-1456"
    description: "Append-only supersession for the generated merged-to-main check; product PR #1456 is
      open on dev at the exact tested head and will be merged only after this OCC companion lands."
    evidence_artifact: "supersedes_dod_evidence:dod-002"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1456; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr view \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json state,baseRefName,headRefOid --jq '.state == \"OPEN\" and .baseRefName
          == \"dev\" and .headRefOid == \"3d43ff585b9e0c704e9665c481022a76a46b284c\"' | grep -q true"
  - id: "dod-supersede-003-occ-cascade-1456"
    description: "Append-only supersession for the generated CI watch check; product PR #1456 has only
      OCC-preflight cascade failures pending this companion."
    evidence_artifact: "supersedes_dod_evidence:dod-003"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1456; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr view \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json statusCheckRollup --jq '[.statusCheckRollup[] | select((.__typename
          == \"CheckRun\" and .status == \"COMPLETED\" and (.conclusion | IN(\"SUCCESS\",\"SKIPPED\",\"NEUTRAL\")
          | not)) or (.__typename == \"StatusContext\" and (.state | IN(\"SUCCESS\",\"PENDING\") | not)))
          | select(((.name // .context) | test(\"occ-preflight|Canonical Inference Gate|receipt-honesty|call-reject-skip-token\"))
          | not)] | length == 0' | grep -q true"
  - id: "dod-supersede-004-product-precommit-1456"
    description: "Append-only supersession for the generated all-files pre-commit check; PR #1456 body
      records pre-commit and ruff proof for the test-only change."
    evidence_artifact: "supersedes_dod_evidence:dod-004"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1456; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr view \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json body --jq '.body' | grep -E 'pre-commit|ruff'"
  - id: "dod-supersede-005-product-coverage-1456"
    description: "Append-only supersession for the generated coverage command; PR #1456 body records the
      focused unit coverage proof for tool_dict_any_checker."
    evidence_artifact: "supersedes_dod_evidence:dod-005"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1456; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr view \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json body --jq '.body' | grep -E 'tool_dict_any_checker|53 passed|57.33%'"
  - id: "occ-self-bind-pr-4324"
    description: "OCC PR #4324 exists and carries OMN-7902 evidence for omnibase_core PR #1456 head 3d43ff58."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4324; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state,headRefName"
