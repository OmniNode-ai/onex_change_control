---
schema_version: "1.0.0"
ticket_id: OMN-9890
title: "chore(OMN-9890): migrate stale file_exists+PASS receipts to OMN-9788 schema"
summary: "Migrate stale file_exists+PASS receipts so they satisfy the post-OMN-9788 receipt schema while
  preserving weak-proof downgrade behavior for file_exists evidence."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: onex_change_control PR  #710 receipt gate and CI checks pass
    command: gh pr checks 710 --repo OmniNode-ai/onex_change_control --watch
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Migration script exists and records the schema_version field required by OMN-9788 receipts."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9890/dod-001/file_exists.yaml &&
          grep -q '^evidence_item_id: dod-001$' drift/dod_receipts/OMN-9890/dod-001/file_exists.yaml &&
          grep -q '^ticket_id: OMN-9890$' drift/dod_receipts/OMN-9890/dod-001/file_exists.yaml && grep
          -q 'schema_version' scripts/migrate_file_exists_receipts.py"
  - id: dod-002
    description: "Migration script references schema_version so rewritten receipts include the required
      OMN-9788 schema field."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9890/dod-002/command.yaml && grep
          -q '^evidence_item_id: dod-002$' drift/dod_receipts/OMN-9890/dod-002/command.yaml && grep -q
          '^ticket_id: OMN-9890$' drift/dod_receipts/OMN-9890/dod-002/command.yaml && grep -q 'schema_version'
          scripts/migrate_file_exists_receipts.py"
  - id: dod-003
    description: "At least one migrated legacy receipt contains schema_version 1.0.0 after the migration."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9890/dod-003/command.yaml && grep
          -q '^evidence_item_id: dod-003$' drift/dod_receipts/OMN-9890/dod-003/command.yaml && grep -q
          '^ticket_id: OMN-9890$' drift/dod_receipts/OMN-9890/dod-003/command.yaml && grep -q 'schema_version:'
          drift/dod_receipts/OMN-9647/dod-001/file_exists.yaml"
