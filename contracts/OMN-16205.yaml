---
schema_version: "1.0.0"
ticket_id: "OMN-16205"
title: "Public omniweb ingress swap onex-dev -> onex-prod + AUTH_URL contract overlay"
summary: >
  Prep-only fix for OmniNode-ai/omninode_infra PR #947 (draft, operator-gated): moves the public app.omninode.ai
  / omninode.ai / www.omninode.ai Ingress from the unmanaged onex-dev namespace to the pipeline-managed
  onex-prod namespace, and moves omniweb's AUTH_URL from a bare Deployment-manifest literal to the onex-runtime-config
  ConfigMap (contract-overlay sourced, per operator ruling 2026-08-18). No live cluster mutation performed
  by this PR or this contract; the live cutover is a separate operator-gated runbook documented in the
  PR body.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #947 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-947"
    description: "PR #947 on OmniNode-ai/omninode_infra — Evidence-Source binding for the OMN-16205 ingress-swap
      prep."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 947 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-947-ci"
    description: >
      PR #947 diff scope: k8s/onex-prod/omniweb/ingress.yaml (new), k8s/onex-dev/omniweb/ingress.yaml,
      k8s/onex-prod/omniweb/{deployment,kustomization}.yaml, k8s/onex-dev/omniweb/deployment.yaml, k8s/onex-{dev,prod}/runtime/configmap.yaml,
      k8s/onex-dev-overlay-dev/kustomization.yaml (+ deleted patches/omniweb-ingress.yaml), k8s/onex-prod/{api/kustomization.yaml,CANONICAL_INGRESS.md},
      scripts/check-auth-url-manifest.py, tests/k8s/test_check_auth_url_manifest.py, .secrets.baseline
      — matches the PR's stated single-purpose scope (ingress ownership + AUTH_URL contract-overlay migration),
      no unrelated files.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 947 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-6717"
    description: "Binds OMN-16205 to OCC companion PR #6717 (self-bind)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6717 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-947-content"
    description: >-
      Repairs dod-OmniNode-ai-omninode_infra-pr-947 and -pr-947-ci: both original checks are bare `gh
      pr view 947 ... --json number,state`/`--json files`, PR-existence/metadata probes (INADMISSIBLE
      per OMN-15309/OMN-15247/OMN-16410 -- they prove the PR exists and lists files, not that the AUTH_URL
      ConfigMap migration the PR claims actually landed in the source). APPEND-ONLY: both original entries
      stay verbatim. Content-bound replacement: reads the actual omninode_infra PR #947 source at its
      pinned head commit (110fc3251dd90a3d34bf8cb8b7b7d0271b03515d, still draft/unmerged since this is
      operator-gated prep work) -- scripts/check-auth-url-manifest.py defines _resolve_configmap_value,
      the function that reads AUTH_URL out of the onex-runtime-config ConfigMap the ticket describes.
      Falsifiable: confirmed ABSENT on omninode_infra@main (the PR's base) via direct gh api call before
      authoring this receipt, so the probe distinguishes base-vs-head.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-947,dod-OmniNode-ai-omninode_infra-pr-947-ci"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api 'repos/OmniNode-ai/omninode_infra/contents/scripts/check-auth-url-manifest.py?ref=110fc3251dd90a3d34bf8cb8b7b7d0271b03515d'
          --jq .content | base64 -d)" && printf '%s' "$body" | grep -F 'def _resolve_configmap_value'
  - id: "dod-OmniNode-ai-omninode_infra-pr-992"
    description: >-
      PR #992 on OmniNode-ai/omninode_infra -- SSA deadlock fix for OMN-16205 Gate 4: switches
      NEXT_PUBLIC_POSTHOG_KEY from valueFrom to value: "" so SSA takes ownership of the stale
      value field set by a past ad-hoc kubectl mutation, allowing every subsequent kustomize apply
      to succeed.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 992 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-992-content"
    description: >-
      Content-bound check for PR #992: reads k8s/onex-dev/omniweb/deployment.yaml at head commit
      07197611399c9b5e42d352871b5c5c6207e8b4e1 and confirms NEXT_PUBLIC_POSTHOG_KEY uses
      value: "" (not valueFrom). Falsifiable: the valueFrom form was present on dev before this PR
      and the grep would fail against the base branch.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api 'repos/OmniNode-ai/omninode_infra/contents/k8s/onex-dev/omniweb/deployment.yaml?ref=07197611399c9b5e42d352871b5c5c6207e8b4e1'
          --jq .content | base64 -d)" && printf '%s' "$body" | grep -A1 'NEXT_PUBLIC_POSTHOG_KEY' | grep -F 'value: ""'
  - id: "occ-self-bind-pr-7104"
    description: "Binds OMN-16205 to OCC companion PR #7104 (self-bind)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7104 --repo OmniNode-ai/onex_change_control --json number,state"
