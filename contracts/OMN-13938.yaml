---
schema_version: "1.0.0"
ticket_id: "OMN-13938"
title: "feat(OMN-13938): add rolling_plan_governor skill"
summary: >-
  omniclaude PR #1858 adds the rolling_plan_governor ONEX skill and its orchestrator node. The skill keeps
  the canonical rolling seven-day plan aligned with live evidence using minimal, ledger-backed diffs rather
  than broad prose rewrites.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1858"
    description: >-
      Consuming-PR binding: omniclaude PR #1858 targets dev from jonah/omn-13938-rolling-plan-governor-skill
      and adds the rolling_plan_governor skill, prompt, orchestrator contract, and node.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13938/dod-omniclaude-pr-1858/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment: PR #1858 touches runtime node paths for an executable ONEX skill. Merge
      does not deploy, reload plugins, restart services, docker exec, kubectl apply, or publish runtime
      events. Post-merge deployment and plugin reload remain an operator-controlled action.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13938/dod-deploy-assessment/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-13938/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying this OMN-13938 contract and the consuming-PR
      evidence for omniclaude PR #1858.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13938/dod-occ-self/command.yaml"
