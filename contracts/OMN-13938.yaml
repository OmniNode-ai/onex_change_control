---
schema_version: "1.0.0"
ticket_id: "OMN-13938"
title: "Frontier tier for delegation graded ladder benchmark"
summary: >-
  Evidence for omnimarket PR #1613. The escalating-complexity graded ladder benchmark's `hard` tier saturated
  at 1.0 across the local 35B, local 284B, and paid-cloud GLM rungs (OMN-13937 follow-up) and no longer
  separated the frontier of the ladder. PR #1613 adds a FRONTIER tier (weight 4) with two register-machine
  trace tasks, an efficiency-gated code_exec task, and a long-context faithfulness task — all objective-graded,
  no LLM-judge — plus a `blocked` cell-exclusion mechanism so an infra-unavailable cell (this week's GLM
  quota exhaustion, the 4090 reasoner's unresolved endpoint, and the 284B rung going unreachable mid-session)
  is excluded from rung scoring instead of manufacturing a false capability regression. Live-recorded
  this session: rung_openrouter_free 4/4, rung_5090_coder 3/4, rung_ds_v4_flash 2/4 (2/4 honestly blocked).
  rung_cloud_glm and rung_4090_reasoner are honestly blocked (0/4 each).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1613"
    description: >-
      omnimarket PR #1613 adds the FRONTIER benchmark tier, the `blocked` cell-exclusion harness change,
      and a ground-truth test that independently re-derives the VM tasks' expected answers via a from-scratch
      reference interpreter. Full local gates pass: ruff format/check clean, mypy --strict clean (2686
      files), delegation test suite 472 passed/2 skipped, full repo suite (no -k filter) 11949 passed/53
      skipped/5 failed (all 5 pre-existing and unrelated — Postgres integration tests requiring a local
      DB not running this session, and one unrelated delegate_skill_orchestrator test; neither references
      graded_ladder).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13938/dod-omnimarket-pr-1613/command.yaml &&
          grep -q '^pr_number: 1613$' drift/dod_receipts/OMN-13938/dod-omnimarket-pr-1613/command.yaml
  - id: "dod-occ-self"
    description: >-
      This onex_change_control PR carries the OMN-13938 companion receipt for omnimarket PR #1613 and
      binds this OCC evidence update to the ticket, per the receipt-gate Evidence-Source contract requirement
      (OMN-10419).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13938/dod-occ-self/command.yaml && grep -q '^ticket_id:
          "OMN-13938"$' drift/dod_receipts/OMN-13938/dod-occ-self/command.yaml
