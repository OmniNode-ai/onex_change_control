---
schema_version: "1.0.0"
ticket_id: "OMN-10948"
title: "Versioned pricing contract model in omnibase_compat"
summary: "Adds frozen Pydantic v2 pricing contract wire DTOs to omnibase_compat.contracts.pricing, declaring
  per-model token costs, baseline model, and savings method for auditable savings replay."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Pricing contract unit tests and strict typing checks pass in omnibase_compat PR #88."
    command: "gh pr checks --repo OmniNode-ai/omnibase_compat 88"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-compat-pr-88"
    description: "omnibase_compat PR #88 adds ModelPricingContract and ModelLlmPricing to omnibase_compat.contracts.pricing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10948/dod-compat-pr-88/command.yaml
          && grep -q '^pr_number: 88$' drift/dod_receipts/OMN-10948/dod-compat-pr-88/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR for OMN-10948 is open and binds this receipt set to the central evidence PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10948/dod-occ-pr/command.yaml &&
          grep -q '^pr_number: 996$' drift/dod_receipts/OMN-10948/dod-occ-pr/command.yaml"
