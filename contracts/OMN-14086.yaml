---
schema_version: "1.0.0"
ticket_id: "OMN-14086"
title: "fix(OMN-14086): diff-scope omniclaude aislop sweep"
summary: >-
  Bind omniclaude PR #1867 to central OCC evidence for the OMN-14086 canary that narrows the CI aislop-sweep
  validator to changed files on pull requests, while failing closed to a full-tree scan when diff scope
  is unavailable or validator infrastructure changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omniclaude PR #1867 carries the diff-scoped aislop sweep implementation at the recorded
      head SHA."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-omniclaude-pr-1867/command.yaml"
  - kind: "tests"
    description: "Focused local tests validate diff-scoping, fail-closed escalation, and CLI behavior."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-local-validation/command.yaml"
  - kind: "manual"
    description: "No runtime deploy is in scope for this CI validator-scope change."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-deploy-scope/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR #3679 carries the central OCC evidence for OMN-14086."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-occ-pr-3679/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1867"
    description: >-
      Consuming-PR binding: omniclaude PR #1867 at head 3f6b1d43ed584c26d5b33677c8972ad45dd29775 carries
      the OMN-14086 aislop-sweep diff-scoping canary and its focused tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-omniclaude-pr-1867/command.yaml &&
          grep -q '^ticket_id: OMN-14086$' drift/dod_receipts/OMN-14086/dod-omniclaude-pr-1867/command.yaml
          && grep -q '^pr_number: 1867$' drift/dod_receipts/OMN-14086/dod-omniclaude-pr-1867/command.yaml
          && grep -q '3f6b1d43ed584c26d5b33677c8972ad45dd29775' drift/dod_receipts/OMN-14086/dod-omniclaude-pr-1867/command.yaml
  - id: "dod-local-validation"
    description: >-
      Focused local validation for the CodeRabbit follow-up confirms the diff-scoped sweep test suite
      and Python formatting/linting pass after resolving review feedback.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-local-validation/command.yaml"
  - id: "dod-deploy-scope"
    description: >-
      Deploy-scope binding: OMN-14086 changes CI workflow wiring, a CI validation script, and tests only;
      no live runtime, host, runner, Docker, Redpanda, AWS, SSH, or production deployment mutation is
      required.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-deploy-scope/command.yaml"
  - id: "dod-occ-pr-3679"
    description: >-
      OCC self-binding: onex_change_control PR #3679 carries the central OMN-14086 contract and receipts
      consumed by OCC preflight and receipt-gate eligibility.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-occ-pr-3679/command.yaml && grep -q
          '^pr_number: 3679$' drift/dod_receipts/OMN-14086/dod-occ-pr-3679/command.yaml && grep -q 'f9866385d0570b5eceefd07d9180d4f3e2009847'
          drift/dod_receipts/OMN-14086/dod-occ-pr-3679/command.yaml
  - id: "dod-omnibase-infra-pr-2263-head-c7b8ee81"
    description: >-
      Independent adversarial verification of retrofit item #5 (this ticket bundles 5 independent zero-risk
      validator diff-scope/wiring retrofits; item #3 is the omniclaude#1867/OCC#3679 evidence above, item
      #5 is this entry): omnibase_infra PR #2263 at head c7b8ee8137c8d7eebf3bc9791b745ad3b48c3f5a wires
      the pre-existing scripts/ci/run_duplication_sweep.py (D1 Drizzle-table dupes, D2 topic-producer
      conflicts) as an ENFORCED required CI gate + always_run pre-commit hook -- the script had a full
      pytest fixture suite but zero CI/pre-commit caller before this PR. Read the full diff (4 files).
      Checked out the branch in an isolated worktree, ran the full focused suite (12/12 TestDuplicationSweep
      tests) green. Went beyond the test suite with an INDEPENDENT fires-on-violation proof in a CI-shaped
      sparse-checkout layout (not the pytest fixture): wrote two schema-*.ts files with a real duplicate
      pgTable("users") declaration and ran run_duplication_sweep.py --omni-home against that layout directly
      -- FAIL/exit 1 with the exact duplicate-table finding; renamed the duplicate and reran -- PASS/exit
      0. Also performed a RED->GREEN mutation proof on the new --changed-files narrowing logic: forced
      _check_is_relevant to always return True (never skip) -- 3 of 12 dedicated tests failed as expected,
      confirming the narrowing logic is real and test-covered. Both changes were reverted and the suite
      reconfirmed green (12/12) with a clean worktree. ruff check/format and actionlint on the new workflow
      YAML all passed. CI on the live PR shows all reds classify as occ-preflight dependency cascade (missing
      Evidence-Source), not real product failures.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-omnibase-infra-pr-2263-head-c7b8ee81/command.yaml
          && grep -q '^pr_number: 2263$' drift/dod_receipts/OMN-14086/dod-omnibase-infra-pr-2263-head-c7b8ee81/command.yaml
          && grep -q 'c7b8ee8137c8d7eebf3bc9791b745ad3b48c3f5a' drift/dod_receipts/OMN-14086/dod-omnibase-infra-pr-2263-head-c7b8ee81/command.yaml
  - id: "dod-deploy-assessment-head-c7b8ee81"
    description: >-
      Deploy-surface assessment for omnibase_infra PR #2263 head c7b8ee8137c8d7eebf3bc9791b745ad3b48c3f5a:
      the diff adds a new GitHub Actions workflow, a new pre-commit hook entry, and extends an existing
      CI script plus its test file. It does not deploy or restart a running service, does not mutate runtime
      configuration, does not touch .201, and does not add a migration. The new workflow only takes effect
      on the NEXT PR's CI run through the existing branch-protection required-status-check mechanism.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-deploy-assessment-head-c7b8ee81/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14086/dod-deploy-assessment-head-c7b8ee81/command.yaml
  - id: "dod-occ-self-commit-infra-2263"
    description: >-
      OCC self-binding: this onex_change_control branch carries the OMN-14086 contract append (retrofit
      item #5) and PASS receipts for omnibase_infra PR #2263 head c7b8ee8137c8d7eebf3bc9791b745ad3b48c3f5a.
      The receipt binds the first evidence commit for occ-preflight eligibility before the OCC PR number
      exists. A distinct evidence_item_id from the existing "dod-occ-pr-3679" entry above (different OCC
      PR, additive, does not touch it).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-occ-self-commit-infra-2263/command.yaml
          && grep -q 'jonah/omn-14086-occ-infra-2263' drift/dod_receipts/OMN-14086/dod-occ-self-commit-infra-2263/command.yaml
  - id: "dod-omnibase-infra-pr-2263-head-2c0459f7"
    description: >-
      Current-head verification refresh for omnibase_infra PR #2263 after the branch advanced to 2c0459f7f4e8d5dfb5745f9f09c89a1e9ee924fc.
      The current head is a rebase of the duplication-sweep gate wiring onto current dev; the stale auto_merge
      mapping is absent because omnimarket OMN-14151 deliberately deregistered node_auto_merge_effect
      and current dev already carries the matching skill_mapping cleanup. Focused proof: duplication-sweep
      tests, actionlint, ruff check, and ruff format pass on the product head, and an independent verifier
      confirmed the registry/catalog state against current omnimarket dev.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-omnibase-infra-pr-2263-head-2c0459f7/command.yaml
          && grep -q '^pr_number: 2263$' drift/dod_receipts/OMN-14086/dod-omnibase-infra-pr-2263-head-2c0459f7/command.yaml
          && grep -q '2c0459f7f4e8d5dfb5745f9f09c89a1e9ee924fc' drift/dod_receipts/OMN-14086/dod-omnibase-infra-pr-2263-head-2c0459f7/command.yaml
  - id: "dod-deploy-assessment-head-2c0459f7"
    description: >-
      Deploy-surface refresh for omnibase_infra PR #2263 head 2c0459f7f4e8d5dfb5745f9f09c89a1e9ee924fc:
      the current PR diff adds the duplication-sweep CI/pre-commit wiring, extends the CI validation script,
      and adds focused tests. It does not deploy or restart a running service, does not mutate runtime
      configuration, does not touch .201, and does not add a migration.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14086/dod-deploy-assessment-head-2c0459f7/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14086/dod-deploy-assessment-head-2c0459f7/command.yaml
  - id: "dod-occ-self-pr-4034"
    description: >-
      OCC self-binding: onex_change_control PR #4034 carries the append-only OMN-14086 current-head evidence
      refresh for omnibase_infra PR #2263 head 2c0459f7f4e8d5dfb5745f9f09c89a1e9ee924fc.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/4034 --jq '.number == 4034 and .head.ref
          == "jonah/omn-14086-refresh-9762" and .base.ref == "dev"' | grep -q '^true$'
