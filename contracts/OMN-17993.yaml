---
schema_version: "1.0.0"
ticket_id: "OMN-17993"
title: "Autobind OCC evidence for OMN-17993"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_compat PR #202.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #202 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #202 on OmniNode-ai/omnibase_compat, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_compat`.
    command: |-
      uv run pytest tests/ci/test_ban_hardcoded_runner_ip_scope.py tests/unit/contracts/delegation/test_model_delegation_runtime_profile.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_compat-pr-202"
    description: "PR #202 on OmniNode-ai/omnibase_compat — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_compat/contents/tests/ci/test_ban_hardcoded_runner_ip_scope.py?ref=d8a7d65c51fed795a683850802619e3a985cca85 --jq '.content' | base64 -d | grep -c 'def test_caller_paths_filter_includes_tests'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_compat/pulls/202/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/ban-hardcoded-runner-ip.yml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #202 on OmniNode-ai/omnibase_compat — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/ci/test_ban_hardcoded_runner_ip_scope.py tests/unit/contracts/delegation/test_model_delegation_runtime_profile.py -q
        cwd: "${OMNI_HOME}/omnibase_compat"
