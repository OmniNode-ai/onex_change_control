---
schema_version: "1.0.0"
ticket_id: "OMN-16296"
title: "Autobind OCC evidence for OMN-16296"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2815.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2815 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2815"
    description: "PR #2815 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2815 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2815 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2815 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2815 --repo OmniNode-ai/omnibase_infra --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2815"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6782"
    description: |-
      Binds OMN-16296 to OCC companion PR #6782 (self-bind). The autobind writer minted
      this companion's receipts bound to the product PR (omnibase_infra#2815) but not to
      the companion PR itself, so occ-preflight returns pr_ticket_mismatch. Asserts only
      the PR's identity via the admissible `gh api` verb, which is stable in every PR
      state -- a state-dependent form (e.g. requiring "open") goes permanently false the
      moment the PR merges.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api 'repos/OmniNode-ai/onex_change_control/pulls/6782' --jq '.number' | grep -x '6782'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2822"
    description: "PR #2822 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/scripts/test_ensure_runner_clones.py?ref=688751f94b78a4130d0d8ca68d49f610a6abe45e --jq '.content' | base64 -d | grep -c 'def test_manifest_declares_all_canonical_siblings'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2822-ci"
    description: "PR #2822 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2822 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "occ-self-bind-pr-6804"
    description: "OCC companion PR #6804 — self-bind for OMN-16296 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6804 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-6861"
    description: "Binds OMN-16296 to OCC companion PR #6861 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6861 --repo OmniNode-ai/onex_change_control --json number,state"
