---
schema_version: "1.0.0"
ticket_id: OMN-12453
title: "feat(OMN-12453): wire gitignore-baseline as pre-commit hook + CI gate"
summary: >
  Wires the gitignore_baseline validator (OMN-12452, already on dev) as an enforced gate: adds validate-gitignore-baseline
  hook entry to .pre-commit-hooks.yaml and .pre-commit-config.yaml, adds .github/workflows/validator-gitignore-baseline.yml
  (status check: "Gitignore Baseline Validator / validate"), and adds the two managed blocks (universal
  + python) to omnibase_core's .gitignore so the repo passes its own gate on first enforcement. Depends
  on OMN-12452 (T2 validator, merged to dev).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "8/8 unit tests pass for gitignore_baseline validator in omnibase_core PR #1184."
    command: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12453/dod-001/command.yaml\""
  - kind: "manual"
    description: "279/279 full validation suite passes with the new hook wired in omnibase_core PR #1184."
    command: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12453/dod-002/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "8 unit tests pass for the gitignore_baseline validator (test_gitignore_baseline.py)
      in omnibase_core PR #1184."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12453/dod-001/command.yaml\" &&
          grep -q '^pr_number: 1184$' \"drift/dod_receipts/OMN-12453/dod-001/command.yaml\""
  - id: "dod-002"
    description: "Full validation test suite (279 tests) passes with the new hook wired in omnibase_core
      PR #1184."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12453/dod-002/command.yaml\" &&
          grep -q '^pr_number: 1184$' \"drift/dod_receipts/OMN-12453/dod-002/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-12453 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12453/dod-occ-pr/command.yaml\"
          && grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12453/dod-occ-pr/command.yaml\""
