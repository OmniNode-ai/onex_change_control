---
schema_version: "1.0.0"
ticket_id: "OMN-17358"
title: "OCC evidence for OMN-17358 -- D-2: every served task class resolves a live tier, cheapest-first,
  bound to the route that answered"
summary: >-
  Requirement ticket D-2 (beta requirements Annex A.2). Zero product PR of its own: its folded members
  and children carry the code, and the requirement closes when the property holds. The property is proven
  by two scheduled probe verdicts, so this contract declares proof_class receipt-bound (OMN-15817 shape
  5), as OMN-15956 does, rather than the merged-PR class. Leg L2 (routed) is M4 criterion C14's producer
  on a no-checkout customer install, run 35823801847 on omninode_infra. Leg L4 (quality-checked, SKIP
  never PASS) and route-identity honesty are M4 criterion C16's producer on the .201 dev lane, run 35832008413
  on omnibase_infra. Each check pins the run id, its workflow name, its conclusion and its head sha, so
  a re-run, a different workflow or a moved head fails closed.
proof_class: "receipt-bound"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "L2 routed: C14 customer-local routing verdict on a no-checkout install."
    command: "See dod-c14-routing-verdict below."
  - kind: "manual"
    description: "L4 quality-checked and route identity: C16 receipt identity verdict on the dev lane."
    command: "See dod-c16-receipt-identity-verdict below."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-c14-routing-verdict"
    description: >-
      Leg L2 (routed). The C14 producer installed omnibase-core 0.47.20, omnibase-infra 0.38.51 and omnimarket
      0.4.205 from the package index into an isolated interpreter with no checkout and every bifrost path
      binding unset, then graded three rows, all PASS with no findings. Row 1: a real delegation (correlation
      ccb13a69-d355-41dc-9a5b-be02e2eec0e2) resolved tier local, backend local-heavy-reasoning, model
      Qwen3.8-27B, accepted on the first rung with quality_bar_met and escalation_count 0, and its only
      egress was the private local endpoint. Row 2: all 11 public task classes in the shipped routing
      contract walked with no tier naming an unbindable backend. Row 4: the reviewer leg resolved to the
      unmetered local provider. Positive controls (a deleted backend and an unreachable endpoint) were
      classified as such in the same record.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh run view 35823801847 --repo OmniNode-ai/omninode_infra --json workflowName,conclusion,headSha
          --jq '.workflowName + "|" + .conclusion + "|" + .headSha' | grep -qFx 'C14 customer-local routing
          (no-checkout machine)|success|f278383871737e3e0f872bf53aa9c9a445822555'
  - id: "dod-c16-receipt-identity-verdict"
    description: >-
      Leg L4 (quality-checked, SKIP never PASS) and the route-identity half of the requirement. The C16
      producer drove a healthy and a dying delegation through the dev lane's gateway on merged code and
      graded R-DELEG-11 PASS (the completed run's four quality rules each reached a boolean verdict, so
      no skipped verifier read as a pass), R-DELEG-12 PASS (the receipt's route local-coder, provider
      local and model Qwen3.8-27B equal what the bus terminal shows answered) and R-DELEG-26 PASS (the
      dying run ended failed with a typed class and code on both the status endpoint and the receipt).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh run view 35832008413 --repo OmniNode-ai/omnibase_infra --json workflowName,conclusion,headSha
          --jq '.workflowName + "|" + .conclusion + "|" + .headSha' | grep -qFx 'Chain Canary — C16 receipt
          identity|success|4e4baf2f4335c944ecba3f98765dabc8980b53e6'
