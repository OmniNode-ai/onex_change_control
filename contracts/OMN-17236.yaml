---
schema_version: "1.0.0"
ticket_id: "OMN-17236"
title: "Autobind OCC evidence for OMN-17236"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimemory PR #466.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #466 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #466 on OmniNode-ai/omnimemory, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimemory`.
    command: |-
      uv run pytest tests/unit/adapters/test_adapter_pii_detector.py tests/unit/adapters/test_pii_live_call_paths.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimemory-pr-466"
    description: "PR #466 on OmniNode-ai/omnimemory — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimemory/contents/tests/unit/adapters/test_adapter_pii_detector.py?ref=9d5653ecb18d8e0b95f5ab35a4e188c0ba1e6ab1 --jq '.content' | base64 -d | grep -c 'def detector'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimemory/pulls/466/files --paginate --jq '.[].filename' | grep -cFx 'scripts/ci/test_selection_adjacency.yaml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #466 on OmniNode-ai/omnimemory — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/adapters/test_adapter_pii_detector.py tests/unit/adapters/test_pii_live_call_paths.py -q
        cwd: "${OMNI_HOME}/omnimemory"
