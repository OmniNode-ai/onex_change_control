---
schema_version: "1.0.0"
ticket_id: "OMN-11202"
title: "Task 14: Gateway routes declaration and contract binding"
summary: >
  Creates gateway-routes.yaml declaring route-to-contract bindings for all 22 onex-api endpoints. Adds
  ModelGatewayRoutes Pydantic loader in models/model_gateway_routes.py with contract_id semver validation
  and path_pattern leading-slash enforcement. Includes 20 unit tests covering YAML structure, completeness,
  exact (method, path, contract_id) tuple bindings, and model validation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "20 unit tests in test_gateway_routes.py pass."
    command: "See drift/dod_receipts/OMN-11202/dod-tests/command.yaml"
  - kind: "ci"
    description: "ruff format + ruff check + yamllint pass for new files."
    command: "See drift/dod_receipts/OMN-11202/dod-lint/command.yaml"
  - kind: "ci"
    description: "omninode_infra PR #431 carries the implementation and tests."
    command: "See drift/dod_receipts/OMN-11202/dod-infra-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "20 gateway routes unit tests passed in omninode_infra."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -qE '(16|20) passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11202/dod-tests/command.yaml"
  - id: "dod-lint"
    description: "ruff format and ruff check passed for new files."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11202/dod-lint/command.yaml"
  - id: "dod-infra-pr"
    description: "omninode_infra PR #431 carries the OMN-11202 implementation and tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11202/dod-infra-pr/command.yaml"
          && grep -q '^pr_number: 431$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11202/dod-infra-pr/command.yaml"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for OCC PR — anchors evidence chain."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'status: PASS' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11202/dod-occ-pr/command.yaml"
          && grep -q 'contracts/OMN-11202.yaml' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11202/dod-occ-pr/command.yaml"
