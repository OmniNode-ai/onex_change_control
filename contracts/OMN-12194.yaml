---
schema_version: "1.0.0"
ticket_id: "OMN-12194"
title: "Fix compat retention CI failure"
summary: >
  Central OCC evidence for omnibase_compat PR #111. The change marks two test helper modules as retention-exempt
  so the compat retention scanner does not treat unit-test helper classes as stale compatibility shims.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "The compat retention scanner passes after the two test helper exemptions."
    command: "See drift/dod_receipts/OMN-12194/dod-compat-retention/command.yaml"
  - kind: "tests"
    description: "Focused compat registration/concurrency tests pass on the rebased PR head."
    command: "See drift/dod_receipts/OMN-12194/dod-focused-tests/command.yaml"
  - kind: "tests"
    description: "The full omnibase_compat pytest suite passes on the rebased PR head."
    command: "See drift/dod_receipts/OMN-12194/dod-full-tests/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carries the central OMN-12194 contract and receipts."
    command: "See drift/dod_receipts/OMN-12194/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-compat-retention"
    description: "Compat retention scanner passed on the rebased omnibase_compat PR #111 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12194/dod-compat-retention/command.yaml
          && grep -q 'no stale compat modules' drift/dod_receipts/OMN-12194/dod-compat-retention/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused registration and concurrency tests passed on the rebased PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12194/dod-focused-tests/command.yaml
          && grep -q '17 passed' drift/dod_receipts/OMN-12194/dod-focused-tests/command.yaml"
  - id: "dod-full-tests"
    description: "Full omnibase_compat pytest suite passed on the rebased PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12194/dod-full-tests/command.yaml
          && grep -q '311 passed' drift/dod_receipts/OMN-12194/dod-full-tests/command.yaml"
  - id: "dod-occ-pr"
    description: "This onex_change_control PR carries the central OMN-12194 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12194/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: \"OMN-12194\"$' drift/dod_receipts/OMN-12194/dod-occ-pr/command.yaml"
