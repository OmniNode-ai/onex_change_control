---
schema_version: "1.0.0"
ticket_id: OMN-9648
title: "fix(runtime): stabilize registration health verification [OMN-9648]"
summary: "route auto-wired heartbeat events by topic-derived type and filter runtime health monitor coverage
  by active runtime profile"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Merged PR confirms work shipped"
    command: "gh pr view {pr} --repo {repo} --json state,mergedAt"
  - kind: "tests"
    description: "Focused runtime health monitor tests cover profile-filtered expected consumer groups"
    command: "uv run pytest tests/unit/services/test_service_runtime_health_monitor.py tests/integration/test_runtime_health_monitor_exact_groups.py
      tests/integration/test_runtime_health_monitor_profile_filter.py -q"
  - kind: "manual"
    description: "Stability runtime hotfix was deployed and validated healthy on .201"
    command: "docker exec omninode-stability-test-runtime python -c 'from omnibase_infra.services.service_runtime_health_monitor
      import ServiceRuntimeHealthMonitor; print(ServiceRuntimeHealthMonitor.__name__)'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR merged to main with verified merge commit"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json state,mergedAt"
  - id: "dod-pr-1527-head"
    description: "Runtime health monitor profile-filter PR head is pinned."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1527 --repo OmniNode-ai/omnibase_infra --json headRefOid --jq '.headRefOid'"
  - id: "dod-profile-filter-tests"
    description: "Focused runtime health monitor profile-filter tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "bash -lc \"grep -q '^status: PASS$' drift/dod_receipts/OMN-9648/dod-profile-filter-tests/command.yaml
          && printf PASS\""
  - id: "dod-stability-runtime-deploy"
    description: "Stability runtime deploy proof imports the runtime health monitor from the deployed
      container."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "bash -lc \"grep -q '^status: PASS$' drift/dod_receipts/OMN-9648/dod-stability-runtime-deploy/command.yaml
          && printf PASS\""
  - id: "dod-occ-pr"
    description: "OCC evidence PR for OMN-9648 is self-bound."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "bash -lc \"grep -q '^status: PASS$' drift/dod_receipts/OMN-9648/dod-occ-pr/command.yaml
          && printf PASS\""
  - id: "dod-pr-1529-runtime-live-groups"
    description: "Runtime live-groups PR is pinned and live stability runtime reports HEALTHY."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1529 --repo OmniNode-ai/omnibase_infra --json headRefOid --jq '.headRefOid'"
