---
schema_version: "1.0.0"
ticket_id: "OMN-15570"
title: "Deploy-scope OCC evidence for OMN-15570"
summary: "OCC contract for OmniNode-ai/omnibase_infra PR #2692 (G3 of the gateway node-architecture-lift
  Phase 0): gateway link-health projection -- node_gateway_link_health_projection_compute / node_gateway_link_health_write_effect
  materialize a per-tenant-edge gateway_link_health table plus a read-time gateway_link_health_status
  view that alerts on absence of heartbeat progress."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2692 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2692"
    description: "PR #2692 on OmniNode-ai/omnibase_infra -- Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2692 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2692 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2692 clears the deploy-gate. Falsifiable live-surface probe:
      fetches the actual migration SQL, write-effect handler, and pyproject.toml entry-point registration
      from GitHub at head commit bfa0b093646471667a265e4d884af53857fa2e10 and asserts the new gateway_link_health_status
      view, HandlerGatewayLinkHealthUpsert class, and node entry-point registration this PR introduces
      are all present -- exit status depends on the real state of the pushed commit, not a self-authored
      receipt."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "body=\"$(gh api repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/098_create_gateway_link_health.sql?ref=bfa0b093646471667a265e4d884af53857fa2e10
          --jq .content | base64 -d)\" && printf '%s' \"$body\" | grep -qF 'CREATE OR REPLACE VIEW public.gateway_link_health_status'"
      - check_type: "command"
        check_value: "body=\"$(gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_gateway_link_health_write_effect/handlers/handler_gateway_link_health_upsert.py?ref=bfa0b093646471667a265e4d884af53857fa2e10
          --jq .content | base64 -d)\" && printf '%s' \"$body\" | grep -qF 'class HandlerGatewayLinkHealthUpsert'"
      - check_type: "command"
        check_value: "body=\"$(gh api repos/OmniNode-ai/omnibase_infra/contents/pyproject.toml?ref=bfa0b093646471667a265e4d884af53857fa2e10
          --jq .content | base64 -d)\" && printf '%s' \"$body\" | grep -qF 'node_gateway_link_health_write_effect
          = \"omnibase_infra.nodes.node_gateway_link_health_write_effect\"'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2692"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
