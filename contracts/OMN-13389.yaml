---
schema_version: "1.0.0"
ticket_id: "OMN-13389"
title: "feat(OMN-13389): add ModelRendererThemeContract — versioned design-token contract"
summary: >-
  Adds ModelRendererThemeContract to omnibase_core/models/dashboard: a frozen Pydantic model (extra="forbid")
  that declares design tokens (colors, spacing, typography, border radii) as typed, versioned contract
  fields. The explicit contract_version: ModelSemVer field makes schema drift detectable. Registered in
  scripts/emit_ts_types.py so the existing TS drift gate in CI mirrors ModelRendererThemeContract to TypeScript
  automatically. Exported from models.dashboard.__all__. A consumer fixture (TestRendererThemingFromContract)
  demonstrates theming purely from the contract with zero hardcoded token values.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1292"
    description: >-
      omnibase_core PR #1292 adds ModelRendererThemeContract model, 16 new unit tests, emit_ts_types registration,
      and dashboard export surface update. 271/271 dashboard tests pass. mypy strict clean on changed
      files. ruff format + check clean.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13389/dod-core-pr-1292/command.yaml"
          && grep -q '^ticket_id: "OMN-13389"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13389/dod-core-pr-1292/command.yaml"
          && grep -q '^pr_number: 1292$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13389/dod-core-pr-1292/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the OMN-13389 central contract and receipts
      used by omnibase_core PR #1292 Receipt Gate via Evidence-Source: OCC PR head SHA.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13389/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-13389"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13389/dod-occ-pr/command.yaml"
