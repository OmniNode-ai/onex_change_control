---
schema_version: "1.0.0"
ticket_id: "OMN-18890"
title: "Autobind OCC evidence for OMN-18890"
summary: |-
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3880.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3880 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3880 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/unit/scripts/test_generate_claude_lane_block.py tests/unit/scripts/test_prepr_pool_lane_census_omn18890.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3880"
    description: "PR #3880 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/lane_census_plan.py?ref=30bb7cf3d5a8cb7532b4a50c06184d3166204ebc --jq '.content' | base64 -d | grep -c 'def _is_optional_and_entirely_down'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3880/files --paginate --jq '.[].filename' | grep -cFx 'deploy/lane-census/lane-manifest.yaml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3880 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/scripts/test_generate_claude_lane_block.py tests/unit/scripts/test_prepr_pool_lane_census_omn18890.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
