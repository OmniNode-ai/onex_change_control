---
schema_version: "1.0.0"
ticket_id: "OMN-13172"
title: "Repository documentation refresh wave"
summary: >-
  Parent evidence for the repository documentation refresh wave. The wave updates stale repository documentation
  across omnibase, knowledge-base, onex-self-extending-agent, omnibase_compat, onex_change_control, omnimarket,
  omniintelligence, omnibase_core, omniclaude, omnibase_spi, and omnibase_infra. Each downstream PR is
  branch-named for this parent ticket and is documented in the inventory receipt.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-doc-pr-inventory"
    description: >-
      Live GitHub inventory confirms the documentation refresh PR set and branch names for the wave. The
      evidence records the affected repositories, PR numbers, branch names, and head commits used by the
      downstream receipt gates.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-doc-pr-inventory/command.yaml"
          && grep -q 'omnimarket#1234' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-doc-pr-inventory/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      onex_change_control PR #2660 carries the parent OMN-13172 central evidence bundle and therefore
      serves as the Evidence-Source for the downstream docs-refresh PRs.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-occ-pr-self/command.yaml"
          && grep -q '^pr_number: 2660$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-occ-pr-self/command.yaml"
  - id: "dod-omnibase-pr-3"
    description: "Bound receipt for omnibase PR #3."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 3$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-omnibase-pr-3/command.yaml\""
  - id: "dod-knowledge-base-pr-16"
    description: "Bound receipt for knowledge-base PR #16."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 16$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-knowledge-base-pr-16/command.yaml\""
  - id: "dod-sea-pr-235"
    description: "Bound receipt for onex-self-extending-agent PR #235."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 235$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-sea-pr-235/command.yaml\""
  - id: "dod-compat-pr-155"
    description: "Bound receipt for omnibase_compat PR #155."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 155$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-compat-pr-155/command.yaml\""
  - id: "dod-omnimarket-pr-1234"
    description: "Bound receipt for omnimarket PR #1234."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 1234$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-omnimarket-pr-1234/command.yaml\""
  - id: "dod-omniintelligence-pr-711"
    description: "Bound receipt for omniintelligence PR #711."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 711$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-omniintelligence-pr-711/command.yaml\""
  - id: "dod-core-pr-1241"
    description: "Bound receipt for omnibase_core PR #1241."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 1241$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-core-pr-1241/command.yaml\""
  - id: "dod-omniclaude-pr-1765"
    description: "Bound receipt for omniclaude PR #1765."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 1765$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-omniclaude-pr-1765/command.yaml\""
  - id: "dod-spi-pr-234"
    description: "Bound receipt for omnibase_spi PR #234."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 234$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-spi-pr-234/command.yaml\""
  - id: "dod-infra-pr-1997"
    description: "Bound receipt for omnibase_infra PR #1997."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 1997$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13172/dod-infra-pr-1997/command.yaml\""
