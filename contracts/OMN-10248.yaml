---
schema_version: '1.0.0'
ticket_id: OMN-10248
title: Governance models batch D — day open/close, dogfood, doc freshness, dod sweep, integration
summary: 'Migrate governance models Batch D into omnibase_core/src/omnibase_core/models/governance/'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Batch D plus current main canary governance tests pass, covering 42 focused governance
      tests
    command: uv run pytest tests/unit/models/governance/test_governance_model_batch_d.py tests/unit/models/governance/test_canary_tier.py
      -q
  - kind: ci
    description: CI pipeline green on the omnibase_core PR
    command: gh pr checks 976 --repo OmniNode-ai/omnibase_core
dod_evidence:
  - id: dod-001
    description: PR 976 diff contains at least one added Model class (non-zero count of +class Model lines)
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 976 --repo OmniNode-ai/omnibase_core | grep -c '^+class Model' | grep
          -qv '^0$'
  - id: dod-002
    description: PR 976 CI has at least one passing check (governance tests included in CI gate)
    source: manual
    checks:
      - check_type: command
        check_value: gh pr checks 976 --repo OmniNode-ai/omnibase_core | grep -c 'pass' | grep -qv '^0$'
  - id: dod-003
    description: PR 976 diff contains model_day_close confirming batch D day-close files are present
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 976 --repo OmniNode-ai/omnibase_core | grep -q 'model_day_close'
  - id: dod-004
    description: PR 976 diff contains model_integration_record confirming batch D integration files are
      present
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 976 --repo OmniNode-ai/omnibase_core | grep -q 'model_integration_record'
  - id: dod-005
    description: omnibase_core PR 976 open and targeting main
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 976 --repo OmniNode-ai/omnibase_core --json state,baseRefName --jq 'select(.state=="OPEN"
          and .baseRefName=="main") | "ok"' | grep -q ok
  - id: dod-007
    description: PR title or body cites this ticket
    source: generated
    checks:
      - check_type: command
        check_value: 'gh pr view ${PR_NUMBER} --repo ${REPO} --json title,body --jq ''.title + " " + (.body
          // "")'' | grep -q ''${TICKET_ID}'''
