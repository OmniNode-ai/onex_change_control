---
schema_version: '1.0.0'
ticket_id: OMN-10248
title: Governance models batch D — day open/close, dogfood, doc freshness, dod sweep, integration
summary: 'Migrate governance models Batch D into omnibase_core/src/omnibase_core/models/governance/'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: All 35 governance unit tests pass including 28 new batch D tests
    command: uv run pytest tests/unit/models/governance/ -v
  - kind: ci
    description: CI pipeline green on the omnibase_core PR
    command: gh pr checks 976 --repo OmniNode-ai/omnibase_core
dod_evidence:
  - id: dod-001
    description: 28 new Batch D model classes present in omnibase_core PR 976 diff
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 976 --repo OmniNode-ai/omnibase_core | grep -c '^+class Model' | grep
          -qv '^0$'
  - id: dod-002
    description: All 35 governance unit tests pass (28 batch D + 7 batch A)
    source: manual
    checks:
      - check_type: command
        check_value: gh pr checks 976 --repo OmniNode-ai/omnibase_core | grep -c 'pass' | grep -qv '^0$'
  - id: dod-003
    description: mypy --strict clean on all governance source files
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 976 --repo OmniNode-ai/omnibase_core | grep -q 'model_day_close'
  - id: dod-004
    description: pre-commit run --all-files passes including ONEX Single Class Per File hook
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 976 --repo OmniNode-ai/omnibase_core | grep -q 'model_integration_record'
  - id: dod-005
    description: omnibase_core PR 976 exists, is open or merged, and targets main
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 976 --repo OmniNode-ai/omnibase_core --json state,baseRefName --jq 'select((.state=="OPEN"
          or .state=="MERGED") and .baseRefName=="main") | "ok"' | grep -q ok
  - id: dod-007
    description: PR title or body cites this ticket
    source: generated
    checks:
      - check_type: command
        check_value: 'gh pr view ${PR_NUMBER} --repo ${REPO} --json title,body --jq ''.title + " " + (.body
          // "")'' | grep -q ''${TICKET_ID}'''
