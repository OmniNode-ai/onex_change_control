---
schema_version: "1.0.0"
ticket_id: "OMN-9745"
title: "Add hook activations YAML contract to omniclaude package"
summary: >
  Adds the omniclaude hook_activations.yaml contract with ModelHookActivation-backed entries for every
  EnumHookBit member, plus unit coverage proving schema and enum integrity.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-hook-activation-tests"
    description: "Focused hook activation contract tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9745/dod-hook-activation-tests/command.yaml\""
  - id: "dod-unit-suite"
    description: "omniclaude unit suite passed for the PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9745/dod-unit-suite/command.yaml\""
  - id: "dod-precommit"
    description: "Pre-commit hooks passed for the contract and unit test files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9745/dod-precommit/command.yaml\""
  - id: "dod-pr-1542-head"
    description: "omniclaude PR #1542 carries the OMN-9745 implementation head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9745/dod-pr-1542-head/command.yaml\""
  - id: "dod-occ-pr-827"
    description: "Bind this central OCC evidence update to onex_change_control PR #827."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9745/dod-occ-pr-827/command.yaml\""
