---
schema_version: "1.0.0"
ticket_id: "OMN-15696"
title: "Autobind OCC evidence for OMN-15696"
summary: >
  OCC contract for OmniNode-ai/omnibase_infra PR #2655: sync config/ into the deployed build context in
  scripts/deploy-runtime.sh's sync_files(), so a --force redeploy or cold bring-up recreating deployed/<version>/
  no longer fails the config/runner_fleet.yaml COPY in docker/Dockerfile.runtime (OMN-15676, same class
  as OMN-12987).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2655 CI checks green"
    command: "gh pr checks 2655 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2655"
    description: >-
      Exact-head source binding for OmniNode-ai/omnibase_infra#2655, which adds an rsync of config/ into
      deploy-runtime.sh's sync_files() and extends tests/scripts/test_deploy_runtime_build_context.py
      with the OMN-15696 regression guard. Probe pins the PR head commit and greps for the OMN-15696 marker
      comment introduced by the fix, which appears exactly once in the file.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/deploy-runtime.sh?ref=399bf99474625f5b7467c01e570adf3510050916 --jq '.content' | base64 -d | grep -c 'OMN-15696'
  - id: "occ-self-bind-pr-6071"
    description: "Binds OMN-15696 to OCC companion PR #6071 (self-bind)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6071 --repo OmniNode-ai/onex_change_control --json number,state"
