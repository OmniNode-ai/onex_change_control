---
schema_version: "1.0.0"
ticket_id: "OMN-10815"
title: "feat(OMN-10815): expose contract config on node base class"
summary: >
  omnibase_core PR #1123 adds a contract_config property to NodeCoreBase so all node types (COMPUTE, EFFECT,
  REDUCER, ORCHESTRATOR) can read config values declared in their contract.yaml at construction time,
  without env var fallbacks. Also adds ModelYamlContract.config field with default_factory=dict.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_core PR #1123 carries the OMN-10815 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10815/dod-product-pr/command.yaml\""
  - id: "dod-unit-tests"
    description: "6 unit tests in test_node_core_base_contract_config.py pass (infrastructure suite: 278
      passed)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10815/dod-unit-tests/command.yaml\""
  - id: "dod-api-snapshot"
    description: "192 API snapshot tests pass — no regressions introduced."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10815/dod-api-snapshot/command.yaml\""
  - id: "dod-occ-self-binding"
    description: "Self-binding receipt for OCC PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10815/dod-occ-self-binding/command.yaml\""
