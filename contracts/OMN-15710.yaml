---
schema_version: "1.0.0"
ticket_id: "OMN-15710"
title: "Receipt Honesty Gate extension — ABS_PATH + STDOUT_EMIT rules"
summary: "Extends scripts/validation/check_receipt_hardening.py with two new rules (ABS_PATH, STDOUT_EMIT)
  per operator ruling R-c, closing the gap the OCC#6084 merged fabrications exposed. Self-authored by
  the implementer for this ticket's OWN carrier PR #6107 (not a companion pattern) — a bounded, gate-mechanical
  change where the implementer and the falsifiable-check author are the same session per repo convention
  for self-contained gate PRs."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "OCC PR CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omn15710-cutoff-constant-shipped-pr-6107"
    description: "PR #6107 actually introduces the OMN_15710_CUTOFF constant (2026-08-01), not merely
      describes it. Asserted against the PR's own diff hunk for scripts/validation/check_receipt_hardening.py:
      exactly one ADDED line declares OMN_15710_CUTOFF = datetime(2026, 8, 1, ...). Reading the ADDED
      lines (the ^+ anchor) proves introduction rather than mere presence. Falsifiable: asserting any
      count other than 1 exits non-zero."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/onex_change_control/pulls/6107/files --paginate --jq '[.[]
          | select(.filename == \"scripts/validation/check_receipt_hardening.py\") | .patch] | join(\"\")'
          | grep -c 'OMN_15710_CUTOFF = datetime(2026, 8, 1'"
  - id: "dod-omn15710-new-rule-tests-pass"
    description: "The 45-test suite for check_receipt_hardening.py (24 new tests covering ABS_PATH, STDOUT_EMIT,
      the OMN_15710_CUTOFF ratchet, the contract baseline, and a real-CLI reproduction of the OCC#6084(a)
      fabricated receipt) passes. Falsifiable: reverting the ABS_PATH/STDOUT_EMIT implementation while
      keeping the tests fails this exact command."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_check_receipt_hardening.py -q"
  - id: "occ-self-bind-pr-6107"
    description: "Self-bind: OCC PR 6107 is this ticket's own carrier PR, proven by its own file list
      containing scripts/validation/check_receipt_hardening.py."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6107 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'scripts/validation/check_receipt_hardening.py'"
  - id: "dod-OmniNode-ai-onex_change_control-pr-6097"
    description: "PR #6097 on OmniNode-ai/onex_change_control — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6097 --repo OmniNode-ai/onex_change_control --json number,state"
      - check_type: "command"
        check_value: "gh pr view 6097 --repo OmniNode-ai/onex_change_control --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-onex_change_control-pr-6097"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6098"
    description: "Binds OMN-15710 to OCC companion PR #6098 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6098 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-6108"
    description: "Binds OMN-15710 to OCC companion PR #6108 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6108 --repo OmniNode-ai/onex_change_control --json number,state"
