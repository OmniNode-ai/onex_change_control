---
schema_version: "1.0.0"
ticket_id: "OMN-13485"
title: "fix(OMN-13485): re-point ARCH-004 baseline owner tickets + accept risk-2 thin handlers"
summary: >-
  OCC evidence for omnibase_infra PR #2069 (Track B · B0 + risk-2 triage). The ARCH-004 imperative-orchestrator
  ratchet baseline previously attributed all 9 hard-fails to the catch-all owner_ticket OMN-13471. Each
  hard-fail is re-pointed to its own decomposition ticket: delegation stays OMN-13471; pr_lifecycle->OMN-13487,
  autopilot->OMN-13488, chain->OMN-13489, merge_sweep->OMN-13490, registration->OMN-13491, routing->OMN-13492,
  rsd->OMN-13493, scope->OMN-13494. An optional accepted_rationale baseline field is added minimally to
  BaselineEntry (defaults to "", only serialized when present) and a one-line rationale is recorded for
  each of the six risk-2 thin handlers (closes OMN-13489-13494 triage). All six are H2-only thin decision
  handlers that emit a next-step action and do NOT drive a decorative FSM via handler-side _transition;
  registration (435L) is I/O-bound and delegates the ack state decision to the pure-function RegistrationReducerService.
  None is Class A. Signal B is out of scope (lands as OMN-13486/B2). The ARCH-004 ratchet stays green.
  YAML + one optional model field + a unit test; no runtime/contract/topic change, no live-lane mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ratchet-baseline-repoint"
    description: >-
      Product PR #2069 re-points the 8 mis-attributed owner_tickets in architecture-handshakes/imperative-orchestrator-baseline.yaml,
      adds the optional accepted_rationale field to BaselineEntry, and adds tests/unit/.../test_imperative_orchestrator_ratchet_baseline.py
      (5 passed) asserting the owner-ticket map, the risk-2 accepted_rationale presence, and field round-trip.
      The scanner --check-all --report exits 0 (ratchet stays green). Full unit suite: 21247 passed, 1
      pre-existing unrelated failure (verification CLI, tracked OMN-13481).
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c 'ARCH-004' contracts/OMN-13485.yaml"
  - id: "dod-deploy-gate"
    description: >-
      Deploy-gate evidence for omnibase_infra PR #2069. No live deploy, restart, docker exec, rpk topic
      produce, or runtime lane mutation was performed. The change is a baseline YAML re-point plus one
      optional model field and a unit test; no runtime behavior change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c 'deploy-gate evidence' contracts/OMN-13485.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13485.yaml consumed
      by omnibase_infra PR #2069 Receipt Gate and Deploy Gate via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13485\"$' contracts/OMN-13485.yaml"
