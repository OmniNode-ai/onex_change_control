---
schema_version: "1.0.0"
ticket_id: "OMN-10569"
title: "Strip baked LAN/path/identity defaults from omnimarket production source"
summary: "Replace all hardcoded 192.168.x.x IPs, /Users/jonah paths, /Volumes/PRO-G40 paths, localhost:9092
  Kafka defaults, and model ID literals in src/omnimarket/ with empty-string defaults. 36 files changed
  across probe, consumer, sweep, and handler surfaces."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Full unit + non-kafka test suite passes (3425 passed)."
    command: "uv run pytest tests/ -m 'not kafka' --ignore=tests/test_datasource_postgres.py -q"
  - kind: "ci"
    description: "CI pipeline green on omnimarket PR #546."
    command: "gh pr checks 546 --repo OmniNode-ai/omnimarket --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "3425 unit tests pass; zero new failures vs main. Pre-existing postgres integration test
      failure confirmed on main (missing DB table, unrelated)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10569/dod-001/command.yaml\""
  - id: "dod-002"
    description: "grep confirms no 192.168.x.x IPs remain as os.environ.get defaults in src/omnimarket/."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10569/dod-002/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10569 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10569/dod-occ-pr/command.yaml\""
