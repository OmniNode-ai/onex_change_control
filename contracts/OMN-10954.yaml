---
schema_version: "1.0.0"
ticket_id: "OMN-10954"
title: "Make savings consumer non-fatal + fix dsn-mode POSTGRES_HOST crash on runtime boot"
summary: "Binds omnibase_infra PR #1590, which (1) wraps the savings estimation consumer loop in a task-level
  safety net so InfraTimeoutError on a fresh-broker subscribe no longer surfaces as a kernel crash, and
  (2) makes HandlerRegistrationStoragePostgres skip the os.environ['POSTGRES_HOST'] lookup when a dsn
  is provided. Unblocks the .201 runtime deploy that was crash-looping after auto-wiring completed successfully
  (0 failures, 166 handlers wired)."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Downstream PR #1590 ships 4 new unit tests covering the dsn-bypass-env, explicit-host-precedence,
      env-fallback, and fail-fast paths of HandlerRegistrationStoragePostgres construction, plus the existing
      integration guard test_savings_estimator_subscription_group.py continues to pass."
    command: "gh pr view 1590 --repo OmniNode-ai/omnibase_infra --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1590"
    description: "omnibase_infra PR #1590 carries the OMN-10954 savings consumer non-fatal + dsn-mode
      POSTGRES_HOST fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10954/dod-omnibase-infra-pr-1590/command.yaml
          && grep -q '^pr_number: 1590$' drift/dod_receipts/OMN-10954/dod-omnibase-infra-pr-1590/command.yaml"
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carries the OMN-10954 contract and downstream receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10954/dod-occ-pr-self/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10954/dod-occ-pr-self/command.yaml"
