---
schema_version: '1.0.0'
ticket_id: OMN-10063
summary: 'feat(omnibase_core): add EnumContractCompleteness enum (Task 1.5 of OMN-9582)'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Enum unit tests pass (8 cases)
    command: uv run pytest tests/unit/enums/test_enum_contract_completeness.py -v
  - kind: ci
    description: CI pipeline green on PR
    command: gh pr checks 951 --repo OmniNode-ai/omnibase_core
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: EnumContractCompleteness enum created with STUB/ENRICHED/FULL members
    source: manual
    checks:
      - check_type: command
        check_value: grep -q 'STUB\|ENRICHED\|FULL' src/omnibase_core/enums/enum_contract_completeness.py
  - id: dod-002
    description: Exported from package __init__.py
    source: manual
    checks:
      - check_type: command
        check_value: grep -q 'EnumContractCompleteness' src/omnibase_core/enums/__init__.py
  - id: dod-003
    description: 8 unit tests pass
    source: manual
    checks:
      - check_type: command
        check_value: uv run pytest tests/unit/enums/test_enum_contract_completeness.py -q --tb=no
  - id: dod-004
    description: mypy --strict passes on new file
    source: manual
    checks:
      - check_type: command
        check_value: uv run mypy src/omnibase_core/enums/enum_contract_completeness.py --strict
  - id: dod-005
    description: PR 951 open with all CI checks passing
    source: manual
    checks:
      - check_type: command
        check_value: gh pr checks 951 --repo OmniNode-ai/omnibase_core | grep -v fail
