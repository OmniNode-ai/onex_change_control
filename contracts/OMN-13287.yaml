---
schema_version: "1.0.0"
ticket_id: "OMN-13287"
title: "W10B: expose enum-governance + no-backward-compat as omnibase_core remote pre-commit hooks"
summary: >-
  Exposes two canonical fail-closed validators as remote pre-commit hooks in omnibase_core/.pre-commit-hooks.yaml
  (check-enum-governance via python -m omnibase_core.validation.checker_enum_governance; check-no-backward-compat
  via the scripts/validation/validate-no-backward-compatibility.py script under language: script) so downstream
  repos consume one implementation instead of divergent local copies (validator-standardization-remediation-plan
  §3 W10B). pydantic-conventions is intentionally NOT exposed: its core default contract ships fail_on_error:false
  (577 grandfathered violations) so it detects but cannot block, and a warn-only gate is forbidden. Consumer-side
  local-copy deletion (omnimemory/omniclaude) is a gated follow-up: consumers pin core remote hooks by
  commit SHA and the stricter core no-backward-compat validator surfaces 8 (mem) + 43 (clde) pre-existing
  violations needing real refactors. Config + test only; no runtime code touched.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1274"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1274 (jonah/omn-13287-omnibase_core-w10b, base=dev).
      Adds the check-enum-governance and check-no-backward-compat remote hooks to .pre-commit-hooks.yaml
      plus tests/validation/test_remote_hook_exposure_omn13287.py. Binds OMN-13287 to omnibase_core PR
      #1274 by pr_number and commit_sha so downstream Receipt Gate can resolve central OCC evidence for
      the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13287/dod-omnibase_core-pr-1274/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13287 contract and the omnibase_core
      PR #1274 binding. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check
      can resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13287/dod-occ-pr-self/command.yaml
  - id: "dod-deploy-no-runtime-impact"
    description: >-
      Deploy evidence for OMN-13287. The change touches only .pre-commit-hooks.yaml (build-time gate config)
      and a tests/ file; it adds no runtime code and no entry-point/handler registration. No image rebuild
      or redeploy is required. This receipt records that no deploy action is needed and proves zero runtime-path
      footprint (no src/*/nodes/*, handlers, or contracts changed).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13287/dod-deploy-no-runtime-impact/command.yaml
          # deploy: build-time gate config + test only; no runtime deploy required
