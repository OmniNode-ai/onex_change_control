---
schema_version: "1.0.0"
ticket_id: "OMN-11435"
title: "Ledger stats dashboard endpoint evidence"
summary: >
  Central OCC evidence for the onex-self-extending-agent ledger stats endpoint. The implementation exposes
  GET /api/ledger-stats over the existing event-chain projection and the PR adds durable DoD evidence
  for ticket closeout.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-143"
    description: "onex-self-extending-agent PR #143 carries the OMN-11435 ledger-stats DoD evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11435/dod-sea-pr-143/command.yaml"
  - id: "dod-sea-unit-tests"
    description: "Focused ledger-stats tests, lint, mypy, and full unit suite passed on the cleaned dev-based
      branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11435/dod-sea-unit-tests/command.yaml"
  - id: "dod-occ-pr-1766"
    description: "OCC PR #1766 binds the central OMN-11435 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11435/dod-occ-pr-1766/command.yaml"
