---
schema_version: "1.0.0"
ticket_id: "OMN-16813"
title: "Autobind OCC evidence for OMN-16813"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2952.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2952 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2952"
    description: "PR #2952 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/integration/chains/test_intent_arm_chain_gate.py?ref=652f7e3145d8a6de564ffd673c38cfb4aec5e790 --jq '.content' | base64 -d | grep -c 'class ModelMirrorLinkHealthUpsert'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/integration/chains/test_intent_arm_chain_gate.py?ref=652f7e3145d8a6de564ffd673c38cfb4aec5e790 --jq '.content' | base64 -d | grep -c 'class ModelMirrorLinkHealthUpsert'
  - id: "dod-behavior-intent-arm-chain-gate"
    description: >
      BEHAVIOR-PROVING. The three autobind items on this contract are surrogates: two byte-identical file-content
      greps (static inspection, and they grep a class name rather than an assertion), one foreign suite,
      one PR-state read. None of them executes the chain this ticket is about, so behavior_proving_count
      would be 0. This item runs the actual gate suite added by omnibase_infra#2952 — raw wire bytes through
      the real dispatch seam into a real ProtocolIntentEffect, plus the two refusal controls and the DLQ/quarantine
      invisibility measurement. It is the same command the required `Event Chain Gate` CI job runs.
    source: generated
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/integration/chains/test_intent_arm_chain_gate.py -q"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7353"
    description: "Binds OMN-16813 to OCC companion PR #7353 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7353 --repo OmniNode-ai/onex_change_control --json number,state"
