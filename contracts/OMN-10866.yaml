---
schema_version: "1.0.0"
ticket_id: "OMN-10866"
title: "Bind May 12 dependency PR batch to OCC evidence"
summary: >
  Bind the May 12 dependency maintenance PR batch to central OCC evidence so Receipt Gate can evaluate
  each PR from a pinned Evidence-Source without bypass tokens. This batch covers omniclaude#1567, omnibase_infra#1567,
  omnibase_infra#1565, omniintelligence#648, and omninode_infra#419/#410/#408/#407/#406/#405/#404.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-1567-head"
    description: "omniclaude PR #1567 carries the GitHub Actions dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omniclaude-1567-head/command.yaml\""
  - id: "dod-omniclaude-1567-ci"
    description: "omniclaude PR #1567 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omniclaude-1567-ci/command.yaml\""
  - id: "dod-infra-1567-head"
    description: "omnibase_infra PR #1567 carries the mypy development dependency range update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-infra-1567-head/command.yaml\""
  - id: "dod-infra-1567-ci"
    description: "omnibase_infra PR #1567 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-infra-1567-ci/command.yaml\""
  - id: "dod-infra-1565-head"
    description: "omnibase_infra PR #1565 carries the urllib3 dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-infra-1565-head/command.yaml\""
  - id: "dod-infra-1565-ci"
    description: "omnibase_infra PR #1565 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-infra-1565-ci/command.yaml\""
  - id: "dod-omniintelligence-648-head"
    description: "omniintelligence PR #648 carries the urllib3 dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omniintelligence-648-head/command.yaml\""
  - id: "dod-omniintelligence-648-blocker"
    description: "omniintelligence PR #648 still has a unit-test blocker after Receipt Gate is supplied
      with evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omniintelligence-648-blocker/command.yaml\""
  - id: "dod-omninode-infra-421-head"
    description: "omninode_infra PR #421 carries the ticket-bound pydantic-settings replacement update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-421-head/command.yaml\""
  - id: "dod-omninode-infra-421-ci"
    description: "omninode_infra PR #421 replacement checks have no failing or pending non-receipt checks."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-421-ci/command.yaml\""
  - id: "dod-omninode-infra-422-head"
    description: "omninode_infra PR #422 carries the ticket-bound types-requests replacement update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-422-head/command.yaml\""
  - id: "dod-omninode-infra-422-ci"
    description: "omninode_infra PR #422 replacement checks have no failing or pending non-receipt checks."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-422-ci/command.yaml\""
  - id: "dod-omninode-infra-423-head"
    description: "omninode_infra PR #423 carries the ticket-bound mypy replacement update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-423-head/command.yaml\""
  - id: "dod-omninode-infra-423-ci"
    description: "omninode_infra PR #423 replacement checks have no failing or pending non-receipt checks."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-423-ci/command.yaml\""
  - id: "dod-omninode-infra-424-head"
    description: "omninode_infra PR #424 carries the ticket-bound aws/network provider replacement update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-424-head/command.yaml\""
  - id: "dod-omninode-infra-424-ci"
    description: "omninode_infra PR #424 replacement checks completed before merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-424-ci/command.yaml\""
  - id: "dod-omninode-infra-425-head"
    description: "omninode_infra PR #425 carries the ticket-bound aws/cluster provider replacement update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-425-head/command.yaml\""
  - id: "dod-omninode-infra-425-ci"
    description: "omninode_infra PR #425 replacement checks completed before merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-425-ci/command.yaml\""
  - id: "dod-omninode-infra-426-head"
    description: "omninode_infra PR #426 carries the ticket-bound actions/upload-artifact replacement
      update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-426-head/command.yaml\""
  - id: "dod-omninode-infra-426-ci"
    description: "omninode_infra PR #426 replacement checks have no failing or pending non-receipt checks."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-426-ci/command.yaml\""
  - id: "dod-omninode-infra-427-head"
    description: "omninode_infra PR #427 carries the ticket-bound GitHub Actions group replacement update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-427-head/command.yaml\""
  - id: "dod-omninode-infra-427-ci"
    description: "omninode_infra PR #427 replacement checks have no failing or pending non-receipt checks."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-427-ci/command.yaml\""
  - id: "dod-occ-contract"
    description: "OCC contract exists for OMN-10866."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-occ-contract/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR #946 carries the OMN-10866 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-occ-pr/command.yaml\""
  - id: "dod-occ-refresh-pr"
    description: "onex_change_control PR #956 carries the OMN-10866 omninode_infra refresh receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-occ-refresh-pr/command.yaml\""
  - id: "dod-occ-replacement-pr"
    description: "onex_change_control PR #963 carries the OMN-10866 replacement PR receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-occ-replacement-pr/command.yaml\""
  - id: "dod-omninode-infra-434-head"
    description: "omninode_infra PR #434 carries the python-production group dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-434-head/command.yaml\""
  - id: "dod-omninode-infra-434-ci"
    description: "omninode_infra PR #434 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-434-ci/command.yaml\""
  - id: "dod-omninode-infra-435-head"
    description: "omninode_infra PR #435 carries the types-requests dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-435-head/command.yaml\""
  - id: "dod-omninode-infra-435-ci"
    description: "omninode_infra PR #435 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-435-ci/command.yaml\""
  - id: "dod-omninode-infra-436-head"
    description: "omninode_infra PR #436 carries the types-pyyaml dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-436-head/command.yaml\""
  - id: "dod-omninode-infra-436-ci"
    description: "omninode_infra PR #436 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-436-ci/command.yaml\""
  - id: "dod-omninode-infra-437-head"
    description: "omninode_infra PR #437 carries the hashicorp/aws aws/network provider dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-437-head/command.yaml\""
  - id: "dod-omninode-infra-437-ci"
    description: "omninode_infra PR #437 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-437-ci/command.yaml\""
  - id: "dod-omninode-infra-438-head"
    description: "omninode_infra PR #438 carries the hashicorp/aws aws/cluster provider dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-438-head/command.yaml\""
  - id: "dod-omninode-infra-438-ci"
    description: "omninode_infra PR #438 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-438-ci/command.yaml\""
  - id: "dod-omninode-infra-441-head"
    description: "omninode_infra PR #441 carries the github-actions group dependency update (supersedes
      closed #439)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-441-head/command.yaml\""
  - id: "dod-omninode-infra-441-ci"
    description: "omninode_infra PR #441 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-441-ci/command.yaml\""
  - id: "dod-occ-batch-2026-05-20-pr"
    description: "onex_change_control PR carrying the OMN-10866 2026-05-20 dependency batch receipts is
      open."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-occ-batch-2026-05-20-pr/command.yaml\""
