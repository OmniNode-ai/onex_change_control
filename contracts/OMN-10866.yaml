---
schema_version: "1.0.0"
ticket_id: "OMN-10866"
title: "Bind May 12 dependency PR batch to OCC evidence"
summary: >
  Bind the May 12 dependency maintenance PR batch to central OCC evidence so Receipt Gate can evaluate
  each PR from a pinned Evidence-Source without bypass tokens. This batch covers omniclaude#1567, omnibase_infra#1567,
  omnibase_infra#1565, omniintelligence#648, and omninode_infra#419/#410/#408/#407/#406/#405/#404.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-1567-head"
    description: "omniclaude PR #1567 carries the GitHub Actions dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omniclaude-1567-head/command.yaml\""
  - id: "dod-omniclaude-1567-ci"
    description: "omniclaude PR #1567 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omniclaude-1567-ci/command.yaml\""
  - id: "dod-infra-1567-head"
    description: "omnibase_infra PR #1567 carries the mypy development dependency range update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-infra-1567-head/command.yaml\""
  - id: "dod-infra-1567-ci"
    description: "omnibase_infra PR #1567 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-infra-1567-ci/command.yaml\""
  - id: "dod-infra-1565-head"
    description: "omnibase_infra PR #1565 carries the urllib3 dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-infra-1565-head/command.yaml\""
  - id: "dod-infra-1565-ci"
    description: "omnibase_infra PR #1565 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-infra-1565-ci/command.yaml\""
  - id: "dod-omniintelligence-648-head"
    description: "omniintelligence PR #648 carries the urllib3 dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omniintelligence-648-head/command.yaml\""
  - id: "dod-omniintelligence-648-blocker"
    description: "omniintelligence PR #648 still has a unit-test blocker after Receipt Gate is supplied
      with evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omniintelligence-648-blocker/command.yaml\""
  - id: "dod-omninode-infra-419-head"
    description: "omninode_infra PR #419 carries the GitHub Actions dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-419-head/command.yaml\""
  - id: "dod-omninode-infra-419-ci"
    description: "omninode_infra PR #419 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-419-ci/command.yaml\""
  - id: "dod-omninode-infra-410-head"
    description: "omninode_infra PR #410 carries the actions/upload-artifact dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-410-head/command.yaml\""
  - id: "dod-omninode-infra-410-ci"
    description: "omninode_infra PR #410 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-410-ci/command.yaml\""
  - id: "dod-omninode-infra-408-head"
    description: "omninode_infra PR #408 carries the aws/cluster Terraform provider lock update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-408-head/command.yaml\""
  - id: "dod-omninode-infra-408-ci"
    description: "omninode_infra PR #408 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-408-ci/command.yaml\""
  - id: "dod-omninode-infra-407-head"
    description: "omninode_infra PR #407 carries the aws/network Terraform provider lock update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-407-head/command.yaml\""
  - id: "dod-omninode-infra-407-ci"
    description: "omninode_infra PR #407 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-407-ci/command.yaml\""
  - id: "dod-omninode-infra-406-head"
    description: "omninode_infra PR #406 carries the mypy test dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-406-head/command.yaml\""
  - id: "dod-omninode-infra-406-ci"
    description: "omninode_infra PR #406 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-406-ci/command.yaml\""
  - id: "dod-omninode-infra-405-head"
    description: "omninode_infra PR #405 carries the types-requests test dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-405-head/command.yaml\""
  - id: "dod-omninode-infra-405-ci"
    description: "omninode_infra PR #405 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-405-ci/command.yaml\""
  - id: "dod-omninode-infra-404-head"
    description: "omninode_infra PR #404 carries the pydantic-settings production dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-404-head/command.yaml\""
  - id: "dod-omninode-infra-404-ci"
    description: "omninode_infra PR #404 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-404-ci/command.yaml\""
  - id: "dod-omninode-infra-421-head"
    description: "omninode_infra PR #421 carries the ticket-bound pydantic-settings replacement update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-421-head/command.yaml\""
  - id: "dod-omninode-infra-422-head"
    description: "omninode_infra PR #422 carries the ticket-bound types-requests replacement update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-422-head/command.yaml\""
  - id: "dod-omninode-infra-423-head"
    description: "omninode_infra PR #423 carries the ticket-bound mypy replacement update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-423-head/command.yaml\""
  - id: "dod-omninode-infra-424-head"
    description: "omninode_infra PR #424 carries the ticket-bound aws/network provider replacement update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-424-head/command.yaml\""
  - id: "dod-omninode-infra-425-head"
    description: "omninode_infra PR #425 carries the ticket-bound aws/cluster provider replacement update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-425-head/command.yaml\""
  - id: "dod-omninode-infra-426-head"
    description: "omninode_infra PR #426 carries the ticket-bound actions/upload-artifact replacement
      update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-426-head/command.yaml\""
  - id: "dod-omninode-infra-427-head"
    description: "omninode_infra PR #427 carries the ticket-bound GitHub Actions group replacement update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-omninode-infra-427-head/command.yaml\""
  - id: "dod-occ-contract"
    description: "OCC contract exists for OMN-10866."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-occ-contract/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR #946 carries the OMN-10866 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-occ-pr/command.yaml\""
  - id: "dod-occ-refresh-pr"
    description: "onex_change_control PR #956 carries the OMN-10866 omninode_infra refresh receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-occ-refresh-pr/command.yaml\""
  - id: "dod-occ-replacement-pr"
    description: "onex_change_control PR #963 carries the OMN-10866 replacement PR receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10866/dod-occ-replacement-pr/command.yaml\""
