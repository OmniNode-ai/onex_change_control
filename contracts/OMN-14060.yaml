---
schema_version: "1.0.0"
ticket_id: OMN-14060
title: "infra: omnimarket venv drift guard and repair tick"
summary: >
  Adds a dynamic omnimarket ref resolver, a local-only onex skill pre-flight drift guard, and a repair
  script so stale omnimarket installs in the omnibase_infra venv are detected and repaired instead of
  silently missing current omnimarket fixes.
is_seam_ticket: false
interface_change: false
evidence_requirements:
  - kind: "tests"
    description: "omnibase_infra CLI and script tests for omnimarket venv drift pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14060/dod-tests/command.yaml"
  - kind: "ci"
    description: "ruff, mypy, and pre-commit validation pass for touched infra files"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14060/dod-lint-typecheck-precommit/command.yaml"
  - kind: "manual"
    description: "omnibase_infra PR #2223 carries the OMN-14060 venv drift guard"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14060/dod-omnibase-infra-pr-2223/command.yaml"
  - kind: "manual"
    description: "Runtime deploy gate reviewed: OMN-14060 is a local guard/repair change with no live
      deploy required"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14060/dod-no-runtime-deploy/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR #3657 carries the central OMN-14060 contract and PASS receipts"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14060/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "omnibase_infra CLI and script tests for omnimarket venv drift pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14060/dod-tests/command.yaml"
  - id: "dod-lint-typecheck-precommit"
    description: "ruff, mypy, and pre-commit validation pass for touched infra files"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14060/dod-lint-typecheck-precommit/command.yaml"
  - id: "dod-omnibase-infra-pr-2223"
    description: "omnibase_infra PR #2223 carries the OMN-14060 venv drift guard"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14060/dod-omnibase-infra-pr-2223/command.yaml
          && grep -q '^pr_number: 2223$' drift/dod_receipts/OMN-14060/dod-omnibase-infra-pr-2223/command.yaml"
  - id: "dod-no-runtime-deploy"
    description: "Runtime deploy gate reviewed: local CLI/script guard only; no live deploy required for
      OMN-14060"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14060/dod-no-runtime-deploy/command.yaml
          && grep -q 'no live deploy required' drift/dod_receipts/OMN-14060/dod-no-runtime-deploy/command.yaml"
  - id: "dod-occ-self"
    description: "onex_change_control PR #3657 carries the central OMN-14060 contract and PASS receipts"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14060/dod-occ-self/command.yaml
          && grep -q '^pr_number: 3657$' drift/dod_receipts/OMN-14060/dod-occ-self/command.yaml"
