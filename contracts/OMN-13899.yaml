---
schema_version: "1.0.0"
ticket_id: "OMN-13899"
title: "Wire no-faked-boundary gate report-only in omniclaude"
summary: >-
  OCC evidence for omniclaude PR #1845, which wires the no-faked-boundary detector into omniclaude in
  REPORT-ONLY mode (the omniclaude counterpart of omnimarket #1590 / OMN-13501 and omnibase_core's blocking
  gate / OMN-13895): a `.github/workflows/no-faked-boundary.yml` job (merge_group + pull_request, no paths
  filter, fail-propagating, --report-only) plus a `check-no-faked-boundary --report-only` pre-commit hook.
  The detector ships in omnibase_core 0.46.4, already on omniclaude dev via #1843 (OMN-13898/OMN-13894).
  Advisory only; the blocking flip + 61-site burn-down are deferred to OMN-13501/13502 Phase 3 (22/61
  sites hard-blocked by the OMN-13499 recorded-replay harness). CI + pre-commit config only; no runtime
  change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-self"
    description: "This onex_change_control PR carries the net-new OMN-13899 contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13899/dod-occ-self/command.yaml && grep -q '^ticket_id:
          "OMN-13899"$' drift/dod_receipts/OMN-13899/dod-occ-self/command.yaml
  - id: "dod-omniclaude-pr-1845"
    description: "omniclaude PR #1845 wires the report-only no-faked-boundary gate (CI workflow + pre-commit
      hook)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13899/dod-omniclaude-pr-1845/command.yaml &&
          grep -q '^pr_number: 1845$' drift/dod_receipts/OMN-13899/dod-omniclaude-pr-1845/command.yaml
