---
schema_version: "1.0.0"
ticket_id: "OMN-12176"
title: "Annotate hardcoded internal IPs in tests"
summary: >
  Central OCC evidence for OMN-12176 across omnibase_core PR #1165 and onex-self-extending-agent PR #135.
  The downstream changes annotate test-only 192.168.86.* references with the explicit onex internal-IP
  allow marker required by aislop-sweep enforcement.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omnibase_core local test coverage passes for the touched dispatch, delegation, and contract-model
      tests."
    command: "See drift/dod_receipts/OMN-12176/dod-core-tests/command.yaml"
  - kind: "manual"
    description: "omnibase_core test files contain no unannotated 192.168.86.* references."
    command: "See drift/dod_receipts/OMN-12176/dod-core-ip-scan/command.yaml"
  - kind: "tests"
    description: "onex-self-extending-agent local unit suite passes for the touched test files."
    command: "See drift/dod_receipts/OMN-12176/dod-self-agent-tests/command.yaml"
  - kind: "manual"
    description: "onex-self-extending-agent test files contain no unannotated 192.168.86.* references."
    command: "See drift/dod_receipts/OMN-12176/dod-self-agent-ip-scan/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-tests"
    description: "omnibase_core local tests passed for the OMN-12176 touched areas."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12176/dod-core-tests/command.yaml
          && grep -q '3101 passed' drift/dod_receipts/OMN-12176/dod-core-tests/command.yaml"
  - id: "dod-core-ip-scan"
    description: "omnibase_core test IP references are explicitly annotated."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12176/dod-core-ip-scan/command.yaml
          && grep -q 'no unannotated' drift/dod_receipts/OMN-12176/dod-core-ip-scan/command.yaml"
  - id: "dod-self-agent-tests"
    description: "onex-self-extending-agent unit tests passed for OMN-12176."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12176/dod-self-agent-tests/command.yaml
          && grep -q '226 passed' drift/dod_receipts/OMN-12176/dod-self-agent-tests/command.yaml"
  - id: "dod-self-agent-ip-scan"
    description: "onex-self-extending-agent test IP references are explicitly annotated."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12176/dod-self-agent-ip-scan/command.yaml
          && grep -q 'no unannotated' drift/dod_receipts/OMN-12176/dod-self-agent-ip-scan/command.yaml"
