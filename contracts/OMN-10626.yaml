---
schema_version: "1.0.0"
ticket_id: "OMN-10626"
title: "Add delegation installation script"
summary: >
  Adds the customer-facing ONEX delegation installer, including dry-run mode, rollback manifest handling,
  settings merge preservation, and package install hardening for the Claude Code delegation feature.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-installer-tests"
    description: "Installer shell and pytest wrapper suites pass after option and package-install hardening."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10626/dod-installer-tests/command.yaml\""
  - id: "dod-precommit"
    description: "Pre-commit hooks pass for the installer and installer tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10626/dod-precommit/command.yaml\""
  - id: "dod-pr-1538-head"
    description: "Bind OMN-10626 evidence to omniclaude PR #1538 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omniclaude --json headRefOid,title"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10626 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10626/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number: 802$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10626/dod-occ-pr/command.yaml\""
