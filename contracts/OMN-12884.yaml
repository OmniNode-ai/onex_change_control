---
schema_version: "1.0.0"
ticket_id: "OMN-12884"
title: "[build] Guardrail: replay/idempotence projection dedupe checks"
summary: >-
  Tracks replay and idempotence checks that protect projection dedupe and classify closure evidence as
  replay-proven, runtime-observed, dashboard-rendered, blocked, or superseded.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused replay/idempotence projection dedupe node tests pass."
    command: "uv run pytest src/omnimarket/nodes/node_projection_replay_check_compute/tests/ -q"
  - kind: "tests"
    description: "Unit suite, strict mypy, ruff, and pre-commit proof is recorded in the product PR body."
    command: "uv run pytest tests/ -q -m unit && uv run ruff check src/omnimarket/nodes/node_projection_replay_check_compute/
      && uv run mypy src/omnimarket/nodes/node_projection_replay_check_compute/ --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-replay-idempotence-dedupe-guard"
    description: >-
      Duplicate/replay fixture proves idempotent projection behavior and classification output is attached
      to dod_evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12884/dod-replay-idempotence-dedupe-guard/command.yaml"
  - id: "dod-omnimarket-pr-1367"
    description: >-
      omnimarket PR #1367 carries the replay/idempotence projection dedupe check node and proof fixtures.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12884/dod-omnimarket-pr-1367/command.yaml"
  - id: "dod-deploy-no-live-mutation"
    description: >-
      Pure compute node, contract, and tests only; no live deploy, runtime restart, broker mutation, migration,
      or runtime state mutation is required before merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12884/dod-deploy-no-live-mutation/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-12884 contract and receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12884/dod-occ-pr-self/command.yaml"
