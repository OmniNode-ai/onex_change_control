---
schema_version: "1.0.0"
ticket_id: "OMN-15800"
title: "OCC evidence for OMN-15800"
summary: >
  Manual OCC companion for OmniNode-ai/omnimarket PR #2032 (projection serving layer reads from the bus,
  not Postgres -- 2026-08-09 operator ruling). Records a falsifiable content probe against the PR's exact
  head confirming publish_snapshot_delta / SnapshotCache exist in the pushed diff, plus a behavioral (L1+)
  check that the PR's live typecheck (shadow) job -- a real mypy --strict run over the changed source,
  which fails on a genuine type error -- concluded SUCCESS on that same head. (Corrected 2026-08-10, dash-busread-verify
  corrective round: the original entries below pinned PR #2032's head at f90b947d8685a9296a44312613c7b8f7fde7beb7;
  two further substantive pushes (581aa680, ecbd7952) landed before this correction, staling both pinned
  probes -- see dod-deploy-live-probe-pr-2032-rebind-ecbd7952 and dod-typecheck-shadow-pr-2032-rebind-ecbd7952
  below, which supersede the originals at the new exact head. Do not read the two original entries as
  current -- read the tip rebind entries. (Further corrected 2026-08-10, busread-corrective round 3: two
  more substantive pushes (2926c357, f2958714 -- the round-3 CodeRabbit offset-ordering fix) landed after
  the ecbd7952 rebind, staling it in turn -- see dod-deploy-live-probe-pr-2032-rebind-f2958714 and dod-typecheck-shadow-pr-2032-rebind-f2958714,
  which supersede the ecbd7952 pair. Read those two as the current tip; the ecbd7952 pair is itself now
  superseded, not current.)
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-deploy-live-probe-pr-2032"
    description: >
      Falsifiable deploy-scope probe for OmniNode-ai/omnimarket PR #2032. Reads the ACTUAL content at
      the PR's exact head via the GitHub contents API and asserts publish_snapshot_delta (runner.py) is
      present -- genuinely falsifiable: re-verified 0 occurrences of publish_snapshot_delta on dev at
      the same moment (exit 1), 6 occurrences at the PR head (exit 0).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/projection/runner.py?ref=f90b947d8685a9296a44312613c7b8f7fde7beb7"
          --jq '.content' | base64 -d)" && count="$(printf '%s' "$body" | grep -c 'publish_snapshot_delta')"
          && [ "$count" -ge 1 ] && gh pr view 2032 --repo OmniNode-ai/omnimarket --json headRefOid --jq
          'if (.headRefOid == "f90b947d8685a9296a44312613c7b8f7fde7beb7") then "true" else error("PR #2032
          head moved off the pinned SHA") end'
  - id: "dod-typecheck-shadow-pr-2032"
    description: >
      L1+ behavioral check (OMN-14409 substance floor): the "typecheck (shadow)" job on PR #2032 is a
      real `mypy --strict` run over the changed source and genuinely fails on a type error (distinct from
      a mere existence/binding probe). Queried live by check-run NAME (not a pinned, ephemeral job id)
      so a re-run on the same head re-validates the same assertion. Local corroboration (same head, same
      command): `uv run mypy src/omnimarket/ --strict` -> "Success: no issues found in 3011 source files".
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          conclusion="$(gh pr view 2032 --repo OmniNode-ai/omnimarket --json statusCheckRollup --jq '.statusCheckRollup[]
          | select(.name == "typecheck (shadow)") | .conclusion')" && [ "$conclusion" = "SUCCESS" ]
  - id: "dod-deploy-live-probe-pr-2032-rebind-ecbd7952"
    description: >
      dash-busread-verify corrective-round rebind: PR #2032's head moved f90b947d8685a9296a44312613c7b8f7fde7beb7
      -> ecbd7952622ab7e50e57186dfb83810c6370a799 across two further substantive pushes (581aa680 CodeRabbit
      SnapshotCache annotation; ecbd7952 CodeRabbit findings batch -- consumer-group split, eviction,
      bootstrap, ordering, cursor, key delimiter, monotonicity, savings key identity). Every push stales
      the exact-head probe pinned by dod-deploy-live-probe-pr-2032, so this entry supersedes it and re-pins
      the SAME falsifiable probe at the new head. Re-verified 2026-08-10 that falsifiability survives
      the rebind: publish_snapshot_delta still present (6 occurrences, unchanged) at ref=ecbd7952622ab7e50e57186dfb83810c6370a799,
      still 0 occurrences at ref=dev.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-live-probe-pr-2032"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/projection/runner.py?ref=ecbd7952622ab7e50e57186dfb83810c6370a799"
          --jq '.content' | base64 -d)" && count="$(printf '%s' "$body" | grep -c 'publish_snapshot_delta')"
          && [ "$count" -ge 1 ] && gh pr view 2032 --repo OmniNode-ai/omnimarket --json headRefOid --jq
          'if (.headRefOid == "ecbd7952622ab7e50e57186dfb83810c6370a799") then "true" else error("PR #2032
          head moved off the pinned SHA") end'
  - id: "dod-typecheck-shadow-pr-2032-rebind-ecbd7952"
    description: >
      dash-busread-verify corrective-round rebind, paired with dod-deploy-live-probe-pr-2032-rebind-ecbd7952:
      the "typecheck (shadow)" job on PR #2032's new head (ecbd7952622ab7e50e57186dfb83810c6370a799) concluded
      SUCCESS live (2026-08-10T01:06:57Z, run 31346143515). Local corroboration on the same head with
      the same command (`uv run mypy src/omnimarket/ --strict`): "Success: no issues found in 3011 source
      files" -- unchanged from the original entry's corroboration.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-typecheck-shadow-pr-2032"
    checks:
      - check_type: "command"
        check_value: >-
          conclusion="$(gh pr view 2032 --repo OmniNode-ai/omnimarket --json statusCheckRollup --jq '.statusCheckRollup[]
          | select(.name == "typecheck (shadow)") | .conclusion')" && [ "$conclusion" = "SUCCESS" ] &&
          gh pr view 2032 --repo OmniNode-ai/omnimarket --json headRefOid --jq 'if (.headRefOid == "ecbd7952622ab7e50e57186dfb83810c6370a799")
          then "true" else error("PR #2032 head moved off the pinned SHA") end'
  - id: "occ-self-bind-pr-6285"
    description: >
      Binds OMN-15800 to this OCC companion PR #6285 (self-bind). occ-preflight's merge-eligibility validator
      requires at least one PASS receipt whose pr_number/commit_sha binds to the OCC PR carrying the receipt
      itself (validator_occ_merge_eligibility._receipt_bound_to_pr); every other entry in this contract
      intentionally binds to omnimarket#2032, not this OCC PR, so this entry supplies the missing self-bind
      (established pattern: occ-self-bind-pr-6271/6274/6282). Deliberately does NOT supersede either rebind
      entry above -- a self-bind PR-existence attestation is weaker evidence than a falsifiable content
      probe and must never retire one.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 6285 --repo OmniNode-ai/onex_change_control --json number,state --jq 'if (.number
          == 6285 and .state == "OPEN") then "true" else error("unexpected OCC self-bind PR state") end'
  - id: "dod-deploy-live-probe-pr-2032-rebind-f2958714"
    description: >
      busread-corrective round 3 rebind: PR #2032's head moved ecbd7952622ab7e50e57186dfb83810c6370a799
      -> f2958714f54a3372b09e6f96d0e4597610c2ecb7 across two further substantive pushes (2926c357 regression
      coverage for defects A/B; f2958714 the round-3 CodeRabbit fix -- SnapshotCache staleness keyed on
      Kafka (source_topic, source_partition, source_offset) instead of time.time_ns(), discussion r3745850632).
      Every push stales the exact-head probe pinned by the prior rebind (dod-deploy-live-probe-pr-2032-rebind-ecbd7952),
      so this entry supersedes it and re-pins the SAME falsifiable probe at the new head. Re-verified
      2026-08-10 that falsifiability survives the rebind: publish_snapshot_delta still present (7 occurrences
      -- one more than the ecbd7952 pin, from the new docstring reference to the removed source_topic/partition/offset
      kwargs) at ref=f2958714f54a3372b09e6f96d0e4597610c2ecb7, still 0 occurrences at ref=dev.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-live-probe-pr-2032-rebind-ecbd7952"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/projection/runner.py?ref=f2958714f54a3372b09e6f96d0e4597610c2ecb7"
          --jq '.content' | base64 -d)" && count="$(printf '%s' "$body" | grep -c 'publish_snapshot_delta')"
          && [ "$count" -ge 1 ] && gh pr view 2032 --repo OmniNode-ai/omnimarket --json headRefOid --jq
          'if (.headRefOid == "f2958714f54a3372b09e6f96d0e4597610c2ecb7") then "true" else error("PR #2032
          head moved off the pinned SHA") end'
  - id: "dod-typecheck-shadow-pr-2032-rebind-f2958714"
    description: >
      busread-corrective round 3 rebind, paired with dod-deploy-live-probe-pr-2032-rebind-f2958714: the
      "typecheck (shadow)" job on PR #2032's new head (f2958714f54a3372b09e6f96d0e4597610c2ecb7) concluded
      SUCCESS live (run 31354171982, job 93350569533). Local corroboration on the same head with the same
      command (`uv run mypy src/omnimarket/ --strict`): "Success: no issues found in 3011 source files"
      -- unchanged from the prior entry's corroboration.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-typecheck-shadow-pr-2032-rebind-ecbd7952"
    checks:
      - check_type: "command"
        check_value: >-
          conclusion="$(gh pr view 2032 --repo OmniNode-ai/omnimarket --json statusCheckRollup --jq '.statusCheckRollup[]
          | select(.name == "typecheck (shadow)") | .conclusion')" && [ "$conclusion" = "SUCCESS" ] &&
          gh pr view 2032 --repo OmniNode-ai/omnimarket --json headRefOid --jq 'if (.headRefOid == "f2958714f54a3372b09e6f96d0e4597610c2ecb7")
          then "true" else error("PR #2032 head moved off the pinned SHA") end'
  # ---------------------------------------------------------------------------
  # APPEND-ONLY FOLLOW-UP: RECEIPT-LABEL SUPERSESSION (2026-08-10).
  #
  # Merged occ#6285 (0c06258c) is NOT edited by this block. The entries above
  # keep their bytes and their OMN-13888 per-entry hashes unchanged. This is a
  # net-new dod_evidence item that supersedes ONLY the evidence_item_id label
  # of dod-deploy-live-probe-pr-2032-rebind-f2958714, per the OMN-15800
  # round-3 verification terminal comment (2026-08-10T05:17:14Z, wf_056f187e,
  # comment 2898f48e-6cbd-4991-b45c-eb4650e40f35): "Honesty defect for the
  # next OCC repair batch: occ#6285 receipt
  # dod-deploy-live-probe-pr-2032-rebind-f2958714 is a source-presence grep
  # mislabeled as a live probe (body text honest, evidence_item_id not).
  # Append-only superseding correction queued." This entry is that queued
  # correction.
  # ---------------------------------------------------------------------------
  - id: "dod-source-presence-probe-pr-2032-rebind-f2958714"
    description: >
      RECEIPT-LABEL SUPERSESSION, not a re-litigation of content. The merged occ#6285 (0c06258c) receipt
      at drift/dod_receipts/OMN-15800/dod-deploy-live-probe-pr-2032-rebind-f2958714/command.yaml names
      itself a "deploy-live-probe". Its check_value is a `gh api .../contents/...?ref=<sha>` GitHub Contents
      API read of committed source text at a pinned git ref, base64-decoded and grepped for a string count,
      plus a headRefOid pin -- a static SOURCE-PRESENCE probe over what was committed, not a probe of
      any deployed or running system. The receipt's body text never claims deployment; only the evidence_item_id
      mislabels the check class as "deploy-live". This entry corrects the LABEL ONLY -- same check_value,
      same probe, same PR binding, re-executed live here and reconfirmed PASS: 7 occurrences of publish_snapshot_delta
      at ref=f2958714f54a3372b09e6f96d0e4597610c2ecb7, headRefOid still pinned to that SHA (PR #2032 merged
      2026-08-10T04:22:53Z at that same head, per `gh pr view 2032 --json headRefOid,state,mergedAt`).
      STATED PLAINLY, per the same terminal comment: no deploy/live probe has ever occurred for omnimarket#2032.
      proof_class for that PR's runtime behavior is code-only, NOT live-readback or deployed, notwithstanding
      OMN-15800 AC2/AC3/AC4/AC6 declaring a live-readback proof class for the ticket as a whole. Live
      proof is frozen pending two independently-confirmed blockers: (1) OMN-15717 -- the .201 dev-lane
      forward-migration fix (commit c09717865) needed before any workspace-mode redeploy of that lane
      will complete; omnibase_infra#2678 is CLOSED with mergedAt=null, so the fix has not landed, and
      re-land is being dispatched separately under OMN-15717; (2) topic provisioning -- onex.snapshot.projection.registration.v1
      does not exist on the dev broker (UNKNOWN_TOPIC_OR_PARTITION), so zero snapshot deltas have ever
      been published, and deploying the merged code alone would not green the end-to-end read path. Cited
      verbatim from the OMN-15800 round-3 verification terminal comment (2026-08-10T05:17:14Z, wf_056f187e,
      Linear comment id 2898f48e-6cbd-4991-b45c-eb4650e40f35): "Proof axis: FAIL, strike 3 -- proof_class
      remains code-only. No dashboard feed has ever been observed serving from the bus."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-live-probe-pr-2032-rebind-f2958714"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/projection/runner.py?ref=f2958714f54a3372b09e6f96d0e4597610c2ecb7"
          --jq '.content' | base64 -d)" && count="$(printf '%s' "$body" | grep -c 'publish_snapshot_delta')"
          && [ "$count" -ge 1 ] && gh pr view 2032 --repo OmniNode-ai/omnimarket --json headRefOid --jq
          'if (.headRefOid == "f2958714f54a3372b09e6f96d0e4597610c2ecb7") then "true" else error("PR #2032
          head moved off the pinned SHA") end'
  - id: "occ-self-bind-pr-6304"
    description: >
      Binds OMN-15800 to this receipt-relabel OCC companion PR #6304 (self-bind). occ-preflight's merge-eligibility
      validator requires at least one PASS receipt whose pr_number/commit_sha binds to the OCC PR carrying
      the receipt itself (validator_occ_merge_eligibility._receipt_bound_to_pr); the new relabel entry
      above intentionally binds to omnimarket#2032, not this OCC PR, so this entry supplies the missing
      self-bind (established pattern: occ-self-bind-pr-6271/6274/6282/6285).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 6304 --repo OmniNode-ai/onex_change_control --json number,state --jq 'if (.number
          == 6304 and .state == "OPEN") then "true" else error("unexpected OCC self-bind PR state") end'
  - id: "dod-OmniNode-ai-omninode_infra-pr-843"
    description: "PR #843 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 843 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-843-ci"
    description: "PR #843 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 843 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-6309"
    description: "OCC companion PR #6309 — self-bind for OMN-15800 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6309 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2044"
    description: "PR #2044 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_live_events/handlers/handler_live_events.py?ref=25270dd5bb5f74089600da1665e4a857bbd4daf9 --jq '.content' | base64 -d | grep -c 'class is'
  - id: "dod-OmniNode-ai-omnimarket-pr-2044-ci"
    description: "PR #2044 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2044 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-6340"
    description: "OCC companion PR #6340 — self-bind for OMN-15800 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6340 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-855"
    description: "PR #855 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 855 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-855-ci"
    description: "PR #855 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 855 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-6344"
    description: "OCC companion PR #6344 — self-bind for OMN-15800 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6344 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2718"
    description: "PR #2718 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/unit/infra/test_omn8738_compose_migration_manifests.py?ref=f878e66332d773b245c3d8ad9d0cd0ef5d20a68c --jq '.content' | base64 -d | grep -c 'def test_omnimarket_projections_bundle_resolves_all_seven_consumers'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2718-ci"
    description: "PR #2718 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2718 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "occ-self-bind-pr-6343"
    description: "OCC companion PR #6343 — self-bind for OMN-15800 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6343 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2048"
    description: "PR #2048 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/projection/runner.py?ref=08c7abe753612ca63b68fee0f4cafff507c85fa2 --jq '.content' | base64 -d | grep -c 'class _ProjectionHealthServer'
  - id: "dod-OmniNode-ai-omnimarket-pr-2048-ci"
    description: "PR #2048 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2048 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-6353"
    description: "OCC companion PR #6353 — self-bind for OMN-15800 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6353 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-2048"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-OmniNode-ai-omninode_infra-pr-858"
    description: "PR #858 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 858 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-858-ci"
    description: "PR #858 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 858 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-6352"
    description: "OCC companion PR #6352 — self-bind for OMN-15800 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6352 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-859"
    description: "PR #859 on OmniNode-ai/omninode_infra — Evidence-Source (hand-authored: the occ-autobind
      bus consumer did not produce a companion for this PR within a reasonable window after two publish
      attempts, see command.yaml reason)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 859 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-859-ci"
    description: "PR #859 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 859 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-6354"
    description: "OCC companion PR #6354 — self-bind for OMN-15800 (OMN-14650)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6354 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-860"
    description: "PR #860 on OmniNode-ai/omninode_infra — Evidence-Source (hand-authored: occ-autobind
      bus consumer did not produce a companion within a reasonable window, same class as PR#859)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 860 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-860-ci"
    description: "PR #860 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 860 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-6356"
    description: "OCC companion PR #6356 — self-bind for OMN-15800 (OMN-14650)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6356 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-861"
    description: "PR #861 on OmniNode-ai/omninode_infra — Evidence-Source (hand-authored: occ-autobind
      bus consumer did not produce a companion within a reasonable window, same class as PR#859/PR#860)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 861 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-861-ci"
    description: "PR #861 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 861 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-6357"
    description: "OCC companion PR #6357 — self-bind for OMN-15800 (OMN-14650)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6357 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2087"
    description: "PR #2087 on OmniNode-ai/omnimarket — Evidence-Source (hand-authored: the occ_companion_emitter
      live consumer on .201 explicitly skipped this PR with skip:NO_RED_DERIVABLE_CHECK — the only changed
      file, Dockerfile.projection, has no RED-derivable static check, so hand-authored evidence is required
      per OMN-15247)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2087 --repo OmniNode-ai/omnimarket --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2087-ci"
    description: "PR #2087 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425): confirms
      the PR's sole changed file is Dockerfile.projection (installs git + copies README.md into the build
      context, fixing the omnimarket-projections writer bundle's unconditional build failure)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2087 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-6621"
    description: "OCC companion PR #6621 — self-bind for OMN-15800 (OMN-14650)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6621 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2089"
    description: "PR #2089 on OmniNode-ai/omnimarket — Evidence-Source (hand-authored: the occ_companion_emitter
      live consumer's compute_companion_plan raised SupersessionCheckBindingError attempting to author
      this ticket's supersession cohort -- dod-deploy-live-probe-pr-2032-rebind-f2958714 and dod-source-presence-probe-pr-2032-rebind-f2958714
      would both rebind to the identical check_value, which the required supersession-binding gate rejects
      as an OMN-15459-class wrong-item rebind, so hand-authored evidence is required)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2089 --repo OmniNode-ai/omnimarket --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2089-ci"
    description: "PR #2089 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425): AC6 fix --
      defers omnibase_infra imports (AsyncpgAdapter, error_classification, EventBusKafka, kafka_auth,
      consumer-group derivation) in runner.py / generation_publisher.py / snapshot_cache.py to their call
      sites so the projection-api process's import graph never loads asyncpg, plus the strengthened subprocess-based
      sys.modules guard test and an OMN-15909-style real-Postgres regression sentinel."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2089 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-6633"
    description: "OCC companion PR #6633 — self-bind for OMN-15800: every other entry in this contract
      binds to omnimarket PRs, not this OCC PR, so occ-preflight's merge-eligibility validator (validator_occ_merge_eligibility._receipt_bound_to_pr)
      needs this entry to find a PASS receipt bound to the OCC PR carrying it (established pattern: occ-self-bind-pr-6621)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6633 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2090"
    description: "PR #2090 on OmniNode-ai/omnimarket — Evidence-Source (hand-authored: same class as PR#2087/PR#2089
      -- the occ_companion_emitter live consumer's compute_companion_plan raises SupersessionCheckBindingError
      on this ticket's PR #2032 rebind cohort, so hand-authored evidence is required per the same OMN-15247
      fallback policy)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2090 --repo OmniNode-ai/omnimarket --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2090-ci"
    description: "PR #2090 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425): AC5 fix --
      test_registration_event_to_http_readback (tests/integration/test_projection_bus_seam.py) now drives
      EphemeralPostgres (hermetic initdb/pg_ctl cluster migrated with the real node_projection_registration
      migrations) and a real AIOKafkaProducer/AIOKafkaConsumer round trip against the dev-lane Redpanda
      broker in place of a MagicMock AsyncpgAdapter and a fake AIOKafkaProducer, with the HTTP readback
      asserted field-by-field against the row read back directly from Postgres."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2090 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-6634"
    description: "OCC companion PR #6634 — self-bind for OMN-15800: every other entry in this contract
      binds to omnimarket PRs, not this OCC PR, so occ-preflight's merge-eligibility validator (validator_occ_merge_eligibility._receipt_bound_to_pr)
      needs this entry to find a PASS receipt bound to the OCC PR carrying it (established pattern: occ-self-bind-pr-6633)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6634 --repo OmniNode-ai/onex_change_control --json number,state"
