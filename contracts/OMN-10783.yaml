---
schema_version: "1.0.0"
ticket_id: "OMN-10783"
title: "feat(OMN-10783): ConfigWriter merge-preserve env file writer with atomic write"
summary: >
  Adds a merge-preserving ConfigWriter for onboarding env files with atomic writes and validation that
  prevents malformed keys or values from corrupting KEY=value output.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1545 carries the OMN-10783 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10783/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Focused unit, integration, ruff, and pre-commit checks passed for ConfigWriter validation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10783/dod-local-tests/command.yaml\""
