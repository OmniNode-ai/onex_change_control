---
schema_version: '1.0.0'
ticket_id: OMN-10367
title: "feat(OMN-10367): add TrajectoryStore with JSONL append and bounded cache"
summary: >
  Wave 4 PRM trajectory store: JSONL-backed per-session file under .onex_state/trajectory/{session_id}.jsonl,
  bounded in-memory deque (cap 1000, evict 500), read_recent(n), and threading.Lock for concurrent-append
  safety.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: 6 unit tests covering all 4 acceptance criteria pass
    command: "env -u PYTHONPATH uv run pytest tests/agents/test_trajectory_store.py -v --tb=short | grep
      -E '6 passed'"
  - kind: ci
    description: PR  #996 on omnibase_core passes all required status checks
    command: "gh pr checks 996 --repo OmniNode-ai/omnibase_core | grep -v FAIL"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: TrajectoryStore module exists at expected path in omnibase_core PR 996 diff
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr diff 996 --repo OmniNode-ai/omnibase_core --name-only | grep -qx 'src/omnibase_core/agents/trajectory_store.py'"
  - id: dod-002
    description: Unit test file exists at expected path in omnibase_core PR 996 diff
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr diff 996 --repo OmniNode-ai/omnibase_core --name-only | grep -qx 'tests/agents/test_trajectory_store.py'"
  - id: dod-003
    description: Six focused TrajectoryStore unit tests are present and PR evidence records them passing
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr diff 996 --repo OmniNode-ai/omnibase_core | awk '/^diff --git a\\/tests\\/agents\\/test_trajectory_store.py
          /{infile=1; next} /^diff --git /{infile=0} infile && /^\\+def test_/{count++} END{exit !(count==6)}'
          && gh pr view 996 --repo OmniNode-ai/omnibase_core --json body --jq '.body' | grep -q '6/6 unit
          tests passing'"
  - id: dod-004
    description: PR cites this ticket
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr view 996 --repo OmniNode-ai/omnibase_core --json title,body --jq '.title +
          \" \" + (.body // \"\")' | grep -q 'OMN-10367'"
