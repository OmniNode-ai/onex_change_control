---
schema_version: "1.0.0"
ticket_id: "OMN-10563"
title: "feat: register omnimarket service keys; schema v1.2"
summary: "Add top-level services.omnimarket section to omnibase_infra/config/shared_key_registry.yaml,
  bumping the registry schema 1.1 -> 1.2. Adds a _services_keys() loader to scripts/register-repo.py.
  Each entry maps to /services/<repo>/<transport>/<KEY> in Infisical."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "Receipt Gate passes on omnibase_infra PR #1505 after this OCC evidence lands."
    command: "gh pr checks 1505 --repo OmniNode-ai/omnibase_infra --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Registry parses with version 1.2 and exposes services.omnimarket.{kafka,llm,db}."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python -c 'import yaml; d=yaml.safe_load(open(\"config/shared_key_registry.yaml\"));
          assert d[\"version\"]==\"1.2\" and \"omnimarket\" in d[\"services\"]'"
  - id: "dod-002"
    description: "16 unit tests pass: schema bump + omnimarket section content + new loader + regression
      of four existing loaders."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/config/test_shared_key_registry_omnimarket.py -v"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10563 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10563/dod-occ-pr/command.yaml\""
