---
schema_version: "1.0.0"
ticket_id: "OMN-7465"
title: "infra-up shell functions delegate to remote host when .env points to .201"
summary: >
  Central OCC evidence for omnibase_infra PR #1658. The PR adds remote-host guard logic to scripts/onex-cli.sh
  and regression coverage for remote delegation behavior.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1658 is open against main at the recorded remote delegation head."
    command: "gh pr view 1658 --repo OmniNode-ai/omnibase_infra --json headRefOid,state,url,headRefName,baseRefName"
  - kind: "tests"
    description: "Remote delegation regression tests pass locally in the OMN-7465 worktree."
    command: "uv run pytest tests/scripts/test_onex_cli_remote_delegation.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1658"
    description: "omnibase_infra PR #1658 is open against main at the recorded remote delegation head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7465/dod-omnibase-infra-pr-1658/command.yaml\"
          && grep -q '^commit_sha: \"592a5016dc212424344255309ed1b731cf93987b\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7465/dod-omnibase-infra-pr-1658/command.yaml\""
  - id: "dod-remote-delegation-artifacts"
    description: "The PR carries the remote delegation shell changes and regression test file."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7465/dod-remote-delegation-artifacts/command.yaml\"
          && grep -q 'scripts/onex-cli.sh' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7465/dod-remote-delegation-artifacts/command.yaml\"
          && grep -q 'tests/scripts/test_onex_cli_remote_delegation.py' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7465/dod-remote-delegation-artifacts/command.yaml\""
  - id: "dod-local-targeted-test"
    description: "The targeted remote delegation regression test passes in the product worktree."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7465/dod-local-targeted-test/command.yaml\"
          && grep -q '5 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7465/dod-local-targeted-test/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-7465 central contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7465/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: \"OMN-7465\"' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7465/dod-occ-pr/command.yaml\""
