---
schema_version: 1.0.0
ticket_id: OMN-14780
title: Imperative Contract Guard fails fast on deterministic uv.lock drift
summary: Central OCC evidence for onex_change_control#4423. The PR changes the reusable Imperative Contract
  Guard workflow so deterministic `uv sync --locked` lockfile drift is classified and failed fast instead
  of retried as a transient failure, with workflow-script tests proving stale-lock no-retry, transient
  retry, and successful guard execution at exact product head e11cd0e80fc22140c68e1bff76b98aa707d0618e.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #4423 product head is exact for OMN-14780 evidence.'
    command: 'PRODUCT_PR_NUMBER=4423; PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=e11cd0e80fc22140c68e1bff76b98aa707d0618e;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-onex_change_control-pr-4423
    description: 'PR #4423 on OmniNode-ai/onex_change_control exists and is bound to OMN-14780 at exact
      product head e11cd0e80fc22140c68e1bff76b98aa707d0618e.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=4423; PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=e11cd0e80fc22140c68e1bff76b98aa707d0618e;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-onex_change_control-pr-4423-diff
    description: 'PR #4423 changes the Imperative Contract Guard reusable workflow and its CI tests.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=4423; PRODUCT_REPO=OmniNode-ai/onex_change_control; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx ".github/workflows/imperative-contract-guard.yml"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "tests/ci/test_imperative_contract_guard_workflow.py"'
  - id: dod-onex_change_control-pr-4423-local-verification
    description: 'Product PR body records local verification for OMN-14780: workflow-script tests execute
      the actual YAML run script, stale-lock no-retry and transient retry cases are proven, 96 CI tests
      pass, ruff/mypy are clean, changed-file pre-commit passes, and the exact uv 0.8.3 stale-lock string
      is captured.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=4423; PRODUCT_REPO=OmniNode-ai/onex_change_control; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "96 passed"
          && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "test_locked_drift_is_not_retried"
          && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "uv 0.8.3
          stale-lock string"'
  - id: dod-onex_change_control-pr-4423-deploy
    description: Deploy-readiness assessment for OMN-14780. The PR changes CI workflow logic and tests
      only; no live deploy, restart, .201, Kafka, database, or production mutation was performed by this
      OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14780/dod-onex_change_control-pr-4423-deploy/command.yaml
          && grep -qi ''no live deploy, restart, .201, Kafka, database, or production mutation'' drift/dod_receipts/OMN-14780/dod-onex_change_control-pr-4423-deploy/command.yaml'
  - id: occ-self-bind-pr-4424
    description: 'OCC PR #4424 on OmniNode-ai/onex_change_control self-binding proof for OMN-14780 evidence.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
