---
schema_version: "1.0.0"
ticket_id: "OMN-14781"
title: "Canonical-shape gate git-base baseline hardening"
summary: >-
  Central OCC evidence for omnibase_core PR #1469. The product PR hardens scripts/ci/canonical_handler_shape.py
  so in-PR baseline removals are checked against the git base baseline, adds a sanctioned hand-flip proof
  path, and fixes receipt-path display for sibling receipt directories.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1469-product-head"
    description: >-
      Product-observing verification for omnibase_core PR #1469 at head d07e5f7e70807a949efced1ea6e2db8df7b4813f.
      Verifies the PR targets dev and touches only the canonical-shape gate implementation plus its focused
      unit tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1469; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=d07e5f7e70807a949efced1ea6e2db8df7b4813f;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,baseRefName,files --jq
          '.headRefOid == "'$PRODUCT_HEAD'" and .baseRefName == "dev" and ([.files[].path] | index("scripts/ci/canonical_handler_shape.py")
          != null) and ([.files[].path] | index("tests/unit/scripts/ci/test_canonical_handler_shape.py")
          != null)' | grep -qx true
  - id: "dod-core-pr-1469-local-verification"
    description: >-
      Verification evidence for omnibase_core PR #1469. The PR body records the focused canonical-handler-shape
      unit suite, formatter/linter checks, and the hard constraints that no baseline revert or fabricated
      equivalence artifact was used.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1469; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=d07e5f7e70807a949efced1ea6e2db8df7b4813f;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,body --jq '.headRefOid
          == "'$PRODUCT_HEAD'" and (.body | contains("uv run pytest tests/unit/scripts/ci/test_canonical_handler_shape.py"))
          and (.body | contains("53 passed")) and (.body | contains("uv run ruff format")) and (.body
          | contains("ruff check")) and (.body | contains("mypy --strict")) and (.body | contains("baseline
          NOT reverted")) and (.body | contains("no `.equivalence.json`/replay artifact fabricated"))'
          | grep -qx true
  - id: "dod-occ-self-bind-pr-4411"
    description: >-
      OCC companion self-binding for onex_change_control PR #4411. Verifies this evidence PR is open on
      the expected codex-letter branch.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName --jq '.number == 4411
          and .state == "OPEN" and .headRefName == "codex-letter/omn-14781-occ-evidence"' | grep -qx true
