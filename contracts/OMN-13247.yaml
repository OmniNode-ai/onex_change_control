---
schema_version: "1.0.0"
ticket_id: "OMN-13247"
title: "Credential-independent coding-agent node skeleton"
summary: >-
  OMN-13247 adds the credential-independent skeleton for four canonical coding-agent nodes in omnibase_infra:
  an orchestrator, a pure FSM reducer, an invoke effect with injected subprocess/git seams, and a workspace
  validation compute node. The PR intentionally does not execute live claude/codex subprocesses before
  the Phase 0 credential proof. It registers the nodes, adds typed models, and proves the mocked dispatch/FSM
  behavior with focused local tests while keeping live execution fail-closed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2031"
    description: >-
      Per-code-PR binding receipt for omnibase_infra PR #2031 (jonahgabriel/omn-13247-phase-a-build-the-four-coding-agent-nodes-proven-standalone,
      base=dev). The PR adds the four coding-agent node skeletons, typed models, topic enum entries, and
      focused mocked tests. This receipt binds OMN-13247 to the infra PR by pr_number and commit_sha so
      downstream Receipt Gate can resolve central OCC evidence for the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13247/dod-infra-pr-2031/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate evidence for new node packages, runtime entry points, runner image metadata, and vendored
      migration paths touched by OMN-13247. No live deploy, runtime restart, or real CLI subprocess proof
      was performed pre-merge. The required operational step is a normal post-merge dev deploy/redeploy
      after the Phase 0 credential proof, followed by validation that the registered nodes load and live
      execution remains fail-closed until credentials are proven.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13247/dod-deploy-assessment/command.yaml &&
          grep -q 'post-merge dev deploy' drift/dod_receipts/OMN-13247/dod-deploy-assessment/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13247 contract and omnibase_infra
      PR #2031 binding.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13247/dod-occ-pr-self/command.yaml
