---
schema_version: "1.0.0"
ticket_id: "OMN-11186"
title: "feat: add pre-commit + CI validator blocking new os.environ/os.getenv additions"
summary: "Adds mechanical enforcement of the contracts-only config policy by shipping a no-new-env-vars
  pre-commit hook and CI job. Blocks any new os.environ[...], os.environ.get(...), or os.getenv(...) call
  with a static string key outside an established allowlist. Exempts tests/, scripts/, examples/, dynamic-key
  patterns, and lines annotated with # env-var-ok."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Unit tests for validate_no_new_env_vars.py all pass."
    command: "cd omnibase_core && uv run pytest tests/unit/validation/test_validate_no_new_env_vars.py
      -v -q"
  - kind: "ci"
    description: "no-new-env-vars hook passes on full omnibase_core codebase."
    command: "cd omnibase_core && pre-commit run no-new-env-vars --all-files"
  - kind: "ci"
    description: "OCC PR #1098 carries the central OMN-11186 contract and receipt evidence."
    command: "gh pr view 1098 --repo OmniNode-ai/onex_change_control --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "omnibase_core PR #1096 carries the validator, tests, hook registration, and CI job.
      17 unit tests pass, hook passes on all files with zero false positives on existing src/."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11186/dod-001/command.yaml && grep
          -q '^ticket_id: \"OMN-11186\"$' drift/dod_receipts/OMN-11186/dod-001/command.yaml && grep -q
          '^pr_number: 1096$' drift/dod_receipts/OMN-11186/dod-001/command.yaml"
  - id: "dod-occ-pr-1098"
    description: "OCC PR #1098 carries the OMN-11186 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11186/dod-occ-pr-1098/command.yaml
          && grep -q '^pr_number: 1098$' drift/dod_receipts/OMN-11186/dod-occ-pr-1098/command.yaml"
