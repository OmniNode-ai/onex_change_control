---
schema_version: "1.0.0"
ticket_id: "OMN-10737"
title: "Fix 7 silent fallback violations in onex_change_control (OMN-10658 follow-up)"
summary: >
  Removes localhost:19092 Kafka default from governance_emitter.py (P0), makes POSTGRES_HOST / OMNIDASH_DB_NAME
  / POSTGRES_USER required in check_omnidash_health.py, and adds validate_no_env_fallbacks.py validator
  wired as pre-commit hook and CI gate so the pattern cannot regress. Part of OMN-10658 fallback enforcement
  sweep.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-governance-emitter-fixed"
    description: "governance_emitter.py has no localhost:19092 default — validator passes clean."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10737/dod-governance-emitter-fixed/command.yaml\""
  - id: "dod-validator-wired"
    description: "validate_no_env_fallbacks.py passes clean and is wired as CI gate."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10737/dod-validator-wired/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR #863 (jonah/omn-10737-fallback-sweep-occ) open against main with contract and
      receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10737/dod-occ-pr/command.yaml\""
