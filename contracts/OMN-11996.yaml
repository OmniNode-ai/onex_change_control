---
schema_version: "1.0.0"
ticket_id: "OMN-11996"
title: "Add missing DispatchResultApplier for node_delegate_skill_orchestrator"
summary: >
  Central OCC evidence for omnibase_infra PR #1736, which registers the missing DispatchResultApplier
  for node_delegate_skill_orchestrator in service_kernel.py. Without this registration the terminal completion
  event was silently discarded, causing B9 quality gate CLI tests to time out waiting for the event.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1736"
    description: "omnibase_infra PR #1736 adds the DispatchResultApplier registration for node_delegate_skill_orchestrator."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/pulls/1736 --jq '.merged == true and .merge_commit_sha
          == \"22beef8aed810b82e3c787631616d722de4d8b01\"' | grep -q true && echo 'deploy applier registered'"
  - id: "dod-deploy-assessment"
    description: >
      No live deploy required for this change. The fix is a registration in service_kernel.py that takes
      effect on next runtime restart. Stability-test lane restart will validate E2E.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy: no live deploy required; runtime restart picks up applier registration
          on next launch'"
  - id: "dod-occ-pr"
    description: "onex_change_control PR #1610 carries the central OCC evidence for OMN-11996."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11996/dod-occ-pr/command.yaml"
