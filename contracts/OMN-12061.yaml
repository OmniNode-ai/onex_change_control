---
schema_version: "1.0.0"
ticket_id: "OMN-12061"
title: "Contract-first model registry refactor"
summary: >
  Central OCC evidence for onex-self-extending-agent PR #130, which removes endpoint/model environment-variable
  reads from generation and judge setup and makes the committed model registry contract the source of
  truth.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-130"
    description: "onex-self-extending-agent PR #130 carries the OMN-12061 model registry refactor."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12061/dod-product-pr-130/command.yaml"
  - id: "dod-ci-validation"
    description: "GitHub checks for PR #130 are green except the expected missing central OCC receipt
      gate."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12061/dod-ci-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      No live deploy was performed during the merge sweep. The contract-first registry refactor applies
      when the application is next deployed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12061/dod-deploy-assessment/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-12061/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC evidence for OMN-12061."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12061/dod-occ-pr/command.yaml"
