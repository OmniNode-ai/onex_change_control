---
schema_version: "1.0.0"
ticket_id: "OMN-12738"
title: "Delegation Qwen prompts can truncate unless reasoning is disabled"
summary: >
  Add typed inference protocol request shaping so Qwen test and code-generation
  delegation requests suppress excessive reasoning at the provider boundary
  without requiring callers to include /no_think, while preserving original
  prompt, response, and token evidence.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1065"
    description: "omnimarket PR #1065 implements typed inference protocol directives for Qwen code and test delegation"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12738/dod-omnimarket-pr-1065/command.yaml\"
          && grep -q '^pr_number: 1065$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12738/dod-omnimarket-pr-1065/command.yaml\"
          && grep -q '^commit_sha: \"4b9546e150058b16c96d66ffbd51b3c28780df54\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12738/dod-omnimarket-pr-1065/command.yaml\""
  - id: "dod-focused-delegation-tests"
    description: "Focused protocol, inference, dispatcher, direct-curl, and delegate-skill golden-chain tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12738/dod-focused-delegation-tests/command.yaml\"
          && grep -q '89 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12738/dod-focused-delegation-tests/command.yaml\"
          && grep -q 'prompt/response/token evidence' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12738/dod-focused-delegation-tests/command.yaml\""
  - id: "dod-deploy-gate-assessment"
    description: "Deploy-gate assessment for runtime path touches and post-merge runtime validation"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12738/dod-deploy-gate-assessment/command.yaml\"
          && grep -q 'deploy assessment' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12738/dod-deploy-gate-assessment/command.yaml\"
          && grep -q 'docker exec omninode-runtime-effects' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12738/dod-deploy-gate-assessment/command.yaml\""
