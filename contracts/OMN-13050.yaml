---
schema_version: "1.0.0"
ticket_id: "OMN-13050"
title: "feat(OMN-13050): OCC recipe as tooling — tool-generate receipts (incl. contract_sha256), default
  --base dev, pair prohibitions (D-4)"
summary: >-
  Retro D-4 (PROCESS_FAILURE_RETRO.md §3.D). Hand-authored OCC receipts wedged OCC PR #2530 four ways
  and blocked three code PRs overnight. Makes the receipt-creation path tool-generated so each of the
  four #2530 wedges is structurally unrepresentable or self-reporting. Code-only in omniclaude (skill/tooling)
  + memory/docs; NO runtime dependency. New scripts/scaffold_occ_receipt.py tool-generates the full ModelDodReceipt
  schema INCLUDING contract_sha256 (via core compute_contract_sha256 — the field whose omission wedged
  #2530), defaults --base dev mechanically, and self-reports the four wedges (base!=dev, bracketed skip-token,
  armed-blind) while contract_sha256 omission is closed structurally; it refuses to emit while any wedge
  is present. The dispatch_worker skill embeds the recipe verbatim with every prohibition paired to a
  failure-mode + alternative. Stale feedback_dispatch_must_embed_occ_recipe memory updated to the OMN-10421
  reality; new policy memory requires reference_* live-state workarounds to cite an open root-fix ticket.
  No envelope/contract-API change, no live-lane mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >-
      The 16 TDD contract-derived scaffold tests and the 4 new dispatch_worker injection assertions pass,
      and the four OCC #2530 wedges are each demonstrated unrepresentable (W1) or self-reported (W2/W3/W4).
    command: "env -u PYTHONPATH uv run pytest tests/scripts/test_scaffold_occ_receipt.py tests/skills/test_dispatch_worker_operating_rules_injection.py
      -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr"
    description: >-
      omniclaude product PR #1806 adds scripts/scaffold_occ_receipt.py (tool-generates the full receipt
      schema incl. contract_sha256, defaults --base dev, self-reports the four #2530 wedges), embeds the
      tool-generated OCC recipe verbatim into the dispatch_worker skill with failure-mode + alternative
      prohibitions, and adds 16 scaffold tests + 4 dispatch_worker injection assertions. Full local verify:
      26 focused tests pass, tests/scripts + tests/skills 577 passed / 10 skipped, ruff + mypy --strict
      + pyright clean on changed files, pre-commit run on changed files all gates PASS.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13050/dod-omniclaude-pr/command.yaml && grep
          -Eq '^pr_number: 1806$' drift/dod_receipts/OMN-13050/dod-omniclaude-pr/command.yaml && grep
          -q 'passed' drift/dod_receipts/OMN-13050/dod-omniclaude-pr/command.yaml
  - id: "dod-no-runtime-deploy"
    description: >-
      Deploy assessment for omniclaude PR #1806: the change is code-only (a Python authoring helper, skill
      markdown, tests) plus memory/docs. It performs no live deploy, runtime restart, topic mutation,
      docker exec, rpk produce, or .201 mutation — the ticket explicitly states no runtime dependency.
      The scaffold tool does no git/gh I/O. No deployable runtime artifact changes; no deploy step is
      required.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13050/dod-no-runtime-deploy/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      This onex_change_control PR carries the OMN-13050 contract + DoD receipts onto dev. The self-binding
      receipt binds the OCC PR by its own pr_number, satisfying validator_occ_merge_eligibility self-preflight.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^status: PASS$' drift/dod_receipts/OMN-13050/dod-occ-pr-self/command.yaml
