---
schema_version: "1.0.0"
ticket_id: "OMN-15683"
title: "Autobind OCC evidence for OMN-15683"
summary: >
  OCC contract for OmniNode-ai/omnimarket PR #2106 (convert delegation_events.tenant_id from a legacy
  TEXT slug to the canonical UUID identity). Local-CLI mint fallback: occ-autobind/occ-companion-effect
  never produced a companion for this PR (PR body carried no Evidence-Source line at all), same class
  as the OMN-16106/OMN-16191 silent-drop symptoms already documented on other tickets tonight.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2106"
    description: >
      PR #2106 on OmniNode-ai/omnimarket -- adds migration 0031_delegation_events_tenant_id_to_uuid.sql
      converting delegation_events.tenant_id from TEXT slug to canonical UUID. Content-pinned RED-before/GREEN-after
      differential: fetches the migration file path via the GitHub contents API at the PR's base and head
      SHAs and confirms it is absent (404) at base and present with tenant_id/UUID conversion content
      at head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_check=$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_delegation/migrations/0031_delegation_events_tenant_id_to_uuid.sql?ref=f03ed4125d235069cb1e2472b3b5a71684d9b5ed"
          2>&1); head_body="$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_delegation/migrations/0031_delegation_events_tenant_id_to_uuid.sql?ref=1ac4fc4059fbf5963a630ebd2b3deabdc155da7d"
          --jq .content | base64 -d)"; echo "$base_check" | grep -q "Not Found" && printf '%s' "$head_body"
          | grep -qF "tenant_id" && printf '%s' "$head_body" | grep -qiF "uuid" && echo "OMN-15683-MKT-2106-DIFFERENTIAL-PROBE-PASS:
          migration 0031 absent at base (404), present at head with tenant_id/UUID conversion content"
  - id: "occ-self-bind-pr-6766"
    description: "Binds OMN-15683 to OCC companion PR #6766 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6766 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-7009"
    description: "Binds OMN-15683 to OCC companion PR #7009 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7009 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2106-ci"
    description: "PR #2106 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2106 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-7018"
    description: "OCC companion PR #7018 — self-bind for OMN-15683 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7018 --repo OmniNode-ai/onex_change_control --json number,state"
