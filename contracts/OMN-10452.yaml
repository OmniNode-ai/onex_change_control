---
schema_version: "1.0.0"
ticket_id: OMN-10452
title: "Fix intelligence orchestrator empty handler name in auto-wiring"
summary: "Add resolver-compatible handler names to node_intelligence_orchestrator function routing entries
  and test that the names are non-empty, profile-restricted, and import-resolve to the declared functions."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Intelligence orchestrator runtime readiness tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10452/dod-runtime-readiness-tests/command.yaml"
  - kind: "manual"
    description: "Touched intelligence orchestrator runtime readiness test passes lint"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10452/dod-lint/command.yaml"
  - kind: "manual"
    description: "Contract handler names resolve to non-empty auto-wiring identifiers"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10452/dod-handler-names/command.yaml"
  - kind: "manual"
    description: "Repo-local ticket contract validates"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10452/dod-contract-validate/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-runtime-readiness-tests"
    description: "Intelligence orchestrator runtime readiness tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10452/dod-runtime-readiness-tests/command.yaml
          && grep -q '^ticket_id: OMN-10452$' drift/dod_receipts/OMN-10452/dod-runtime-readiness-tests/command.yaml
          && grep -q '^evidence_item_id: dod-runtime-readiness-tests$' drift/dod_receipts/OMN-10452/dod-runtime-readiness-tests/command.yaml"
  - id: "dod-lint"
    description: "Touched intelligence orchestrator runtime readiness test passes lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10452/dod-lint/command.yaml && grep
          -q '^ticket_id: OMN-10452$' drift/dod_receipts/OMN-10452/dod-lint/command.yaml && grep -q '^evidence_item_id:
          dod-lint$' drift/dod_receipts/OMN-10452/dod-lint/command.yaml"
  - id: "dod-handler-names"
    description: "Contract handler names resolve to non-empty auto-wiring identifiers"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10452/dod-handler-names/command.yaml
          && grep -q '^ticket_id: OMN-10452$' drift/dod_receipts/OMN-10452/dod-handler-names/command.yaml
          && grep -q '^evidence_item_id: dod-handler-names$' drift/dod_receipts/OMN-10452/dod-handler-names/command.yaml"
  - id: "dod-contract-validate"
    description: "Repo-local ticket contract validates"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10452/dod-contract-validate/command.yaml
          && grep -q '^ticket_id: OMN-10452$' drift/dod_receipts/OMN-10452/dod-contract-validate/command.yaml
          && grep -q '^evidence_item_id: dod-contract-validate$' drift/dod_receipts/OMN-10452/dod-contract-validate/command.yaml"
