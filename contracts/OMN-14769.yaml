---
schema_version: "1.0.0"
ticket_id: "OMN-14769"
title: "Autobind OCC evidence for OMN-14769"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1811."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1811 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1811"
    description: "PR #1811 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1811-ci"
    description: "PR #1811 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4385"
    description: "OCC companion PR #4385 — self-bind for OMN-14769 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-1811-deploy"
    description: "Deploy-readiness assessment for OMN-14769. The omnimarket PR touches src/omnimarket/nodes/node_pr_lifecycle_inventory_compute/handlers/handler_pr_lifecycle_inventory.py;
      this evidence lane performed no live deploy, restart, .201, or production mutation."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14769/dod-omnimarket-1811-deploy/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-14769/dod-omnimarket-1811-deploy/command.yaml"
  - id: "occ-self-bind-pr-4389"
    description: "OCC companion PR #4389 self-binding proof for OMN-14769 deploy evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
