---
schema_version: "1.0.0"
ticket_id: "OMN-13567"
title: "Foundational doc_content_scan validator — corpus + generated COMPUTE node (identification/build,
  not yet gated)"
summary: >-
  Builds the foundational doc_content_scan validator. Lands the deterministic acceptance corpus + generation-driver
  task in omnimarket (PR #1386, OMN-13568) and the generated, corpus-accepted doc_content_scan COMPUTE
  validator in omnibase_core (PR #1326, OMN-13569). The scanning logic was generated by node_generation_consumer
  local-model-first (Qwen3.6-35B-A3B on the local-coder backend) and accepted only because it passed the
  deterministic acceptance corpus in the hardened sandbox on the first attempt (corpus is the authority,
  not the LLM). This central OCC evidence binds OMN-13567 to omnimarket PR #1386 and omnibase_core PR
  #1326 and records that the PRs add no live deploy mutation. Gate wiring (pre-commit / CI) is explicitly
  deferred to OMN-13572.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1386"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1386 (OMN-13568) at the current head. Lands the doc_content_scan
      acceptance corpus (15 violation + 13 clean fixtures, 17 adversarial mutations) and the generation-driver
      task description; corpus consistency proven by the parametrized validator_corpora tests (well-formed
      + rejects flag-nothing/flag-everything + accepts a correct reference scanner). The receipt binds
      by pr_number and commit_sha for downstream Receipt Gate resolution.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13567/dod-omnimarket-pr-1386/command.yaml
  - id: "dod-omnibase-core-pr-1326"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1326 (OMN-13569) at the current head. Lands the
      generated, corpus-accepted doc_content_scan COMPUTE validator mirroring the private_ip/ package
      (handler + models + two-transport runtime + provenance), with 45 unit tests, mypy --strict clean,
      and a fail-closed runtime proof. NOT wired as a gate (deferred to OMN-13572). The receipt binds
      by pr_number and commit_sha for downstream Receipt Gate resolution.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13567/dod-omnibase-core-pr-1326/command.yaml
  - id: "dod-deploy-no-live-mutation"
    description: >-
      Deploy-path evidence for OMN-13567. The PRs add a repository validation gate (a COMPUTE node executed
      over the in-memory bus) plus its generation producer corpus + provenance only. They add no runtime
      node deploy, no Kafka topic in a live broker, no migration, no image-build input, no .201 mutation,
      and no live deploy. The new doc-content scan topic constants are pre-commit/in-memory transport
      names, not live-broker topics, and the validator is NOT wired as a blocking gate (deferred to OMN-13572).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13567/dod-deploy-no-live-mutation/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13567 contract and receipts.
      Binds the OCC PR by pr_number so the OCC receipt-gate eligibility check can resolve a PASS receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13567/dod-occ-pr-self/command.yaml
