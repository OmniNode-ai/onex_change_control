---
schema_version: 1.0.0
ticket_id: OMN-16574
title: deploy-onex-prod.yml rollback step can report GREEN after a FAILED new-object rollback
summary: deploy-onex-prod.yml rollback step can report GREEN after a FAILED new-object rollback
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Rollback-step-swallow regression tests pass
    command: uv run pytest tests/k8s/test_deploy_onex_prod_workflow.py -q
  - kind: ci
    description: pre-commit clean on the changed workflow + test files
    command: pre-commit run --files .github/workflows/deploy-onex-prod.yml tests/k8s/test_deploy_onex_prod_workflow.py
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Tests exist and pass
    source: generated
    checks:
      - check_type: test_passes
        check_value: uv run pytest tests/k8s/test_deploy_onex_prod_workflow.py -q
  - id: dod-002
    description: Pre-commit hooks pass
    source: generated
    checks:
      - check_type: command
        check_value: pre-commit run --files .github/workflows/deploy-onex-prod.yml tests/k8s/test_deploy_onex_prod_workflow.py
  - id: occ-self-bind-pr-7150
    description: >-
      OCC self-binding receipt for PR #7150, the in-repo evidence PR carrying this contract. Binds OMN-16574
      to the OCC PR by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          gh pr view 7150 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
