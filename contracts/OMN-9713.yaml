---
schema_version: "1.0.0"
ticket_id: "OMN-9713"
summary: "Fix deploy-agent split-brain Kafka bus configuration"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused tests prove deploy-agent Kafka config has no localhost fallback and completion
      publishing uses the configured bus"
    command: "cd ../omnibase_infra/scripts/deploy-agent && uv run --extra dev pytest tests/unit/test_kafka_config.py
      tests/unit/test_publisher_kafka_bus.py -q"
  - kind: "tests"
    description: "Deploy-agent unit suite remains green"
    command: "cd ../omnibase_infra/scripts/deploy-agent && uv run --extra dev pytest tests/unit -q"
  - kind: "ci"
    description: "Ruff check passes for touched deploy-agent Kafka config and publisher files"
    command: "cd ../omnibase_infra/scripts/deploy-agent && uv run ruff check deploy_agent/agent.py deploy_agent/consumer.py
      deploy_agent/publisher.py deploy_agent/kafka_config.py tests/unit/test_kafka_config.py tests/unit/test_publisher_kafka_bus.py
      tests/unit/test_llm_endpoint_env_contract_guard.py"
  - kind: "tests"
    description: "Focused tests prove deploy-agent self-update and service source both point at the canonical
      repo copy"
    command: "cd ../omnibase_infra/scripts/deploy-agent && uv run --extra dev pytest tests/unit/test_executor_self_update.py
      tests/unit/test_deploy_agent_service_source.py tests/unit/test_kafka_config.py tests/unit/test_publisher_kafka_bus.py
      -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused deploy-agent Kafka bus tests passed"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-9713/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "Deploy-agent unit suite passed after Kafka bus consolidation"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-9713/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "Ruff check passed for deploy-agent Kafka bus consolidation files"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-9713/dod-003 -type f -name '*.yaml' | grep -q ."
  - id: "dod-004"
    description: "Deploy-agent self-update and service source use the canonical repo copy"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-9713/dod-004 -type f -name '*.yaml' | grep -q ."
