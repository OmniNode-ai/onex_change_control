---
schema_version: "1.0.0"
ticket_id: "OMN-12609"
title: "SEA adversarial architecture verdict gate and stale-stack cleanup evidence"
summary: >-
  Central evidence for onex-self-extending-agent PR #197. The PR adds a deterministic architecture verdict
  COMPUTE node, CLI, CI workflow, and manual pre-commit hook so adversarial architecture findings can
  be evaluated as a named pre-dev-merge gate. It also records durable cleanup evidence for redundant stale
  SEA stack PRs and abandoned branches, without performing destructive branch deletion.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-197-binding"
    description: "Bind OMN-12609 central OCC evidence to onex-self-extending-agent PR #197 and its current
      head commit."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12609/dod-sea-pr-197-binding/command.yaml"
          && grep -q '^pr_number: 197$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12609/dod-sea-pr-197-binding/command.yaml"
          && grep -q '^commit_sha: "f89efac2343c6e480e39a263b32561c1eda8af90"$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12609/dod-sea-pr-197-binding/command.yaml"
  - id: "dod-sea-pr-197-ci-validation"
    description: "The source PR reports successful architecture verdict, lint, typecheck, test, security,
      PR title, CodeRabbit thread, and skip-token checks; only Receipt Gate remains blocked awaiting central
      OCC evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12609/dod-sea-pr-197-ci-validation/command.yaml"
          && grep -q 'architecture-verdict.*SUCCESS' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12609/dod-sea-pr-197-ci-validation/command.yaml"
          && grep -q 'test.*SUCCESS' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12609/dod-sea-pr-197-ci-validation/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12609 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12609/dod-occ-pr-self/command.yaml"
