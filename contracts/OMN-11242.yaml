---
schema_version: "1.0.0"
ticket_id: "OMN-11242"
title: "Wire TopicProvisioner into runtime kernel startup"
summary: >
  Central OCC evidence for omnibase_infra PR #1652. The runtime now pre-provisions Kafka topics during
  live contract materialization without blocking subscription wiring when one topic provision attempt
  fails.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - topics
  - protocols
evidence_requirements:
  - kind: "tests"
    description: "Focused unit and integration tests pass for live topic provisioning."
    command: "See drift/dod_receipts/OMN-11242/dod-tests/command.yaml"
  - kind: "manual"
    description: "Runtime deploy-gate evidence is carried by omnibase_infra contracts/OMN-11242.yaml."
    command: "See drift/dod_receipts/OMN-11242/dod-deploy/command.yaml"
  - kind: "ci"
    description: "Central OCC PR carries the OMN-11242 contract and receipts."
    command: "See drift/dod_receipts/OMN-11242/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Focused runtime live-materialization tests passed on the patched omnibase_infra branch."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '11 passed' drift/dod_receipts/OMN-11242/dod-tests/command.yaml"
  - id: "dod-deploy"
    description: "Repo-local deploy-gate contract exists for the runtime-path change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'deploy: docker exec omninode-runtime' drift/dod_receipts/OMN-11242/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OMN-11242 contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11242/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: \"OMN-11242\"' drift/dod_receipts/OMN-11242/dod-occ-pr/command.yaml"
