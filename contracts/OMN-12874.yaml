---
schema_version: "1.0.0"
ticket_id: "OMN-12874"
title: "Dev/stability closure: SEA final proof packet"
summary: >-
  Tracks the final SEA closure pass through dashboard/API and direct bus generation, non-empty artifacts,
  materialization/deploy, synchronous registration, asynchronous invocation, projection, dashboard render,
  and replay classification.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-final-proof"
    description: >-
      Fresh same-day SEA proof packet includes artifact hashes, generated source and contract fields,
      registration event, invocation result, projection row, dashboard capture, runtime identity, and
      replay status.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12874/dod-sea-final-proof/command.yaml"
