---
schema_version: 1.0.0
ticket_id: OMN-14711
title: 'Evidence for OmniNode-ai/omnibase_core#1460: arch-no-raw-sqlite3 submodule import detection'
summary: OCC evidence for omnibase_core#1460 validating arch-no-raw-sqlite3 sqlite3 submodule import detection.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #1460 product diff scope present'
    command: 'PRODUCT_PR_NUMBER=1460; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER}; gh pr diff
      $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q .'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnibase_core-pr-1460
    description: 'PR #1460 on OmniNode-ai/omnibase_core exists and is bound to OMN-14711.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1460; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json number,state,headRefOid'
  - id: dod-omnibase_core-pr-1460-diff
    description: 'PR #1460 on OmniNode-ai/omnibase_core has a non-empty reviewed product diff for OMN-14711.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1460; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q .'
  - id: occ-self-bind-pr-4336
    description: 'OCC batch PR #4336 on OmniNode-ai/onex_change_control self-binding proof for OMN-14711.'
    source: generated
    checks:
      - check_type: command
        check_value: 'SELF_PR_NUMBER=4336; SELF_REPO=OmniNode-ai/onex_change_control; : ${PR_NUMBER};
          gh pr view $SELF_PR_NUMBER --repo $SELF_REPO --json number,state'
  - id: dod-omnibase_core-pr-1460-deploy
    description: 'Deploy-readiness assessment for omnibase_core PR #1460 runtime-path edits. No live deploy,
      restart, docker exec, rpk topic produce, or .201 mutation was performed by this OCC PR; post-merge
      validation remains in the normal dev deploy pipeline.'
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14711/dod-omnibase_core-pr-1460-deploy/command.yaml
          && grep -qi ''no live deploy'' drift/dod_receipts/OMN-14711/dod-omnibase_core-pr-1460-deploy/command.yaml'
  - id: occ-self-bind-pr-4337
    description: 'OCC follow-up PR #4337 on OmniNode-ai/onex_change_control self-binding proof for OMN-14711
      deploy-gate evidence.'
    source: generated
    checks:
      - check_type: command
        check_value: 'SELF_PR_NUMBER=4337; SELF_REPO=OmniNode-ai/onex_change_control; : ${PR_NUMBER};
          gh pr view $SELF_PR_NUMBER --repo $SELF_REPO --json number,state'
