---
schema_version: "1.0.0"
ticket_id: "OMN-10741"
title: "Create unified validate_no_env_fallbacks.py and share across all repos (OMN-10658 follow-up)"
summary: >
  Binds omnibase_infra PR #1544, which ships the canonical unified validate_no_env_fallbacks.py covering
  all 6 pattern types from the OMN-10658 enforcement sweep, to central OCC evidence so the Receipt Gate
  can verify the canonical validator shipped correctly.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1544-head"
    description: "omnibase_infra PR #1544 carries the unified validator at the recorded implementation
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10741/dod-pr-1544-head/command.yaml\""
  - id: "dod-ci-1544-green"
    description: "All non-receipt CI checks on omnibase_infra PR #1544 are passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10741/dod-ci-1544-green/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10741/dod-occ-pr/command.yaml\""
