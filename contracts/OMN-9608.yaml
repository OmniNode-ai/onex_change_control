---
schema_version: "1.0.0"
ticket_id: "OMN-9608"
title: "Fix GitHub markdown anchor slug mismatches in architecture docs"
summary: "Align markdown link validation and architecture-guide ToC anchors with GitHub heading slug behavior
  for em-dash headings."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused markdown-link validator tests pass on the omnibase_infra PR head."
    command: "uv run pytest tests/unit/validation/test_validate_markdown_links.py -v"
  - kind: "manual"
    description: "Markdown link validation passes across the documentation corpus."
    command: "uv run python scripts/validate.py markdown_links && uv run onex-validate-links"
  - kind: "ci"
    description: "Required GitHub checks pass for omnibase_infra PR #1459."
    command: "gh pr checks 1459 --repo OmniNode-ai/omnibase_infra --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "The validator change preserves GitHub-style double hyphens generated by em-dash headings."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 1459 --repo OmniNode-ai/omnibase_infra | grep -E 'double hyphen|handlermcp--server-lifecycle'"
  - id: "dod-002"
    description: "Focused markdown link validator tests passed on the PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1459 --repo OmniNode-ai/omnibase_infra --json body --jq '.body' | grep
          -q '71 passed'"
  - id: "dod-003"
    description: "Full markdown link validation and required PR checks passed for PR #1459."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr checks 1459 --repo OmniNode-ai/omnibase_infra --required"
