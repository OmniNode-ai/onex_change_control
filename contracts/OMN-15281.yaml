---
schema_version: "1.0.0"
ticket_id: "OMN-15281"
title: "Autobind OCC evidence for OMN-15281"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #716."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #716 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-716"
    description: "PR #716 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15281/dod-OmniNode-ai-omninode_infra-pr-716/command.yaml"
  - id: "dod-OmniNode-ai-omninode_infra-pr-716-ci"
    description: "PR #716 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15281/dod-OmniNode-ai-omninode_infra-pr-716-ci/command.yaml"
  - id: "occ-self-bind-pr-5227"
    description: "OCC companion PR #5227 — self-bind for OMN-15281 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omn15281-verify-env-driven-rds-content"
    description: >
      Independent adversarial verification (verifier != implementer): content-bound assertions against
      PR #716's ACTUAL diff at head a467cc69 (gh pr diff, not gh pr view existence), plus an independent
      re-run of the full repo-root suite and the new test modules on .200. Supersedes the prior dod-OmniNode-ai-omninode_infra-pr-716
      / -pr-716-ci autobind entries' bare existence checks with falsifiable content assertions. PR #716
      was OPEN, not yet merged, at verification time.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -qx 'ENV_DRIVEN_DB_HOST_COUNT=6' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
          && grep -qx 'MANIFEST_RENDER_ERROR_DEFINED=1' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
          && grep -qx 'RDS_FAILFAST_PATTERNS=2' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
          && grep -qx 'NEW_TEST_MODULES_PASS=20/20' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
          && grep -qx 'FULL_SUITE_ON_PR_716=446 passed' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
  - id: "occ-self-bind-pr-5235"
    description: "Binds OMN-15281 to the adversarial-verification OCC companion PR #5235 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5235 --repo OmniNode-ai/onex_change_control --json number,state"
