---
schema_version: "1.0.0"
ticket_id: "OMN-12574"
title: "Node-based runtime deployment and OCC automation durability"
summary: >-
  Bind durable process evidence for the node-based runtime deployment/OCC automation lane. This receipt
  covers the boundary-clone retry hardening added after omnibase_infra merge-group validation failed on
  a one-shot peer repository clone while the requested migration-conflicts boundary check itself passed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
dod_evidence:
  - id: "dod-boundary-clone-retry-pr"
    description: "OCC PR #2083 retries validate-boundaries peer repository clones before marking a peer
      clone degraded."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12574/dod-boundary-clone-retry-pr/command.yaml"
          && grep -q '^pr_number: 2083$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12574/dod-boundary-clone-retry-pr/command.yaml"
          && grep -q 'validate-boundaries' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12574/dod-boundary-clone-retry-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
