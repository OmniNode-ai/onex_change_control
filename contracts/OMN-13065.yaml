---
schema_version: "1.0.0"
ticket_id: "OMN-13065"
title: "Route-seam baseline migration — migrate /api/ literals behind src/services/ seams"
summary: >-
  Migrates the three eslint-disable-annotated /api/ path literals (useDispatch.ts:31,45, ControlPlanePage.tsx:24,
  TraceExplorerWidget.tsx:226) behind src/services/ seams and removes all eslint-disable annotations.
  Creates src/services/dispatch-api.ts (dispatch seam) and src/services/feature-flag-service.ts (feature-flags
  seam). Extends event-dash-api.ts with submitGeneration(baseUrl?) and fetchLogEntries(). Migrates FeatureFlagDashboard.tsx:218
  mid-template literal behind the new feature-flag seam. Extends eslint.config.js with v2 mid-template
  TemplateLiteral:has() selectors to catch the ${baseUrl}/api/... pattern the v1 leading-quasi selectors
  missed (OMN-13065). All 1307 tests pass; ESLint 0 violations; pre-commit all green.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-seam-migration"
    description: "eslint-disable annotations removed from useDispatch.ts, ControlPlanePage.tsx, TraceExplorerWidget.tsx;
      all /api/ literals behind src/services/ seams."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13065/dod-seam-migration/command.yaml"
