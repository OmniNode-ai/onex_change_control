---
schema_version: "1.0.0"
ticket_id: "OMN-12428"
title: "node_ci_watch fails loud on gh query error instead of false PASSED"
summary: >-
  Central evidence for omnimarket PR #1399, which fixes a false-positive CI verdict in node_ci_watch:
  a gh CLI / transport / parse error was coerced into terminal_status=passed. The fix introduces a structured
  fetch result that distinguishes a true green from a query error, maps query errors to the previously-dead
  terminal_status=error, and switches the gh JSON field from the version-specific 'conclusion' to the
  gh-recommended 'bucket'. The change is runtime-path touching and build/PR scoped; this contract supplies
  the central Receipt-Gate and deploy-gate evidence cited by the product PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1399"
    description: >-
      omnimarket PR #1399 introduces ModelCiStatusFetch (failed_checks + failure_summary + query_error),
      maps query errors to terminal_status=error in handle() and the auto-fix re-poll path, switches the
      gh field to 'bucket', and adds 7 fail-loud unit tests plus updates the existing 11 to the structured
      fetch. Full local lint + ruff + mypy --strict clean; runtime sweep 325/325 OK.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid,baseRefName"
  - id: "dod-occ-evidence-source"
    description: >-
      This OCC contract is the central Evidence-Source for OMN-12428 and is cited by omnimarket PR #1399.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-12428.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: omnimarket PR #1399 changes the runtime node_ci_watch handler classification
      path. The PR scope is code, tests, and contract evidence only; post-merge runtime rollout follows
      the normal deployment lane. No runtime lane is mutated by this PR.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12428/dod-deploy-assessment/command.yaml"
