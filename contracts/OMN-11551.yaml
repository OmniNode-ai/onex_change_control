---
schema_version: "1.0.0"
ticket_id: "OMN-11551"
title: "Wire NodeContractEvalCompute into runtime"
summary: >-
  Bind central OCC evidence for wiring NodeContractEvalCompute into the Bloom runtime in PR #664.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "OMN-11551 PR checks pass in omniintelligence."
    command: "gh pr checks 664 --repo OmniNode-ai/omniintelligence"
dod_evidence:
  - id: "dod-omniintelligence-pr-664"
    description: "omniintelligence PR #664 carries the OMN-11551 change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11551/dod-omniintelligence-pr-664/command.yaml\"
          && grep -q '^pr_number: 664$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11551/dod-omniintelligence-pr-664/command.yaml\""
  - id: "dod-occ-pr-9999"
    description: "OCC PR #9999 carries the central OMN-11551 contract and bound receipt."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11551/dod-occ-pr-9999/command.yaml\"
          && grep -q '^pr_number: 9999$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11551/dod-occ-pr-9999/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
