---
schema_version: "1.0.0"
ticket_id: OMN-10054
title: "Align delegate skill publisher to consumer topic"
summary: "Align delegate skill registry and node contract on the delegate-task command topic"
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - topics
  - events
evidence_requirements:
  - kind: ci
    description: "omniclaude PR CI green across required gates"
    command: "gh pr checks --repo OmniNode-ai/omniclaude 1501"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Delegate event registry maps delegate.task to the consumer topic"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10054/dod-001/command.yaml && grep
          -q '^evidence_item_id: \"dod-001\"$' drift/dod_receipts/OMN-10054/dod-001/command.yaml"
  - id: dod-002
    description: "Delegate orchestrator contract subscribes to the delegate-task command topic"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10054/dod-002/command.yaml && grep
          -q '^evidence_item_id: \"dod-002\"$' drift/dod_receipts/OMN-10054/dod-002/command.yaml"
  - id: dod-003
    description: "Repo-local deploy gate accepts the OMN-10054 contract evidence"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10054/dod-003/command.yaml && grep
          -q '^evidence_item_id: \"dod-003\"$' drift/dod_receipts/OMN-10054/dod-003/command.yaml"
