---
schema_version: "1.0.0"
ticket_id: "OMN-11512"
title: "Add stretch-goal evidence bundle and demo flag"
summary: >
  Add the onex-self-extending-agent --demo CLI path, evidence bundle emission, and mixed-provenance display
  warnings tying together the hackathon stretch goal workflow.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "tests"
    description: "Evidence bundle unit tests pass at the recorded PR head."
    command: "uv run pytest tests/unit/test_evidence_bundle.py -q"
  - kind: "manual"
    description: "onex-self-extending-agent PR #98 is open against main at the recorded head SHA."
    command: "gh pr view 98 --repo OmniNode-ai/onex-self-extending-agent --json number,url,headRefOid,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-98"
    description: "onex-self-extending-agent PR #98 carries OMN-11512 evidence bundle and demo flag support
      at the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11512/dod-product-pr-98/command.yaml"
          && grep -q '^pr_number: 98$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11512/dod-product-pr-98/command.yaml"
          && grep -q '^commit_sha: "74c1a75525b6b1b417f4eb5e7786dee916062ac7"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11512/dod-product-pr-98/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11512 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11512/dod-occ-self/command.yaml"
          && grep -q '^ticket_id: "OMN-11512"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11512/dod-occ-self/command.yaml"
