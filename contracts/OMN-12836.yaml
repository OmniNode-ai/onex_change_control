---
schema_version: "1.0.0"
ticket_id: "OMN-12836"
title: "Projection-backed SEA contractor --agent generation evidence"
summary: >-
  Central evidence for onex-self-extending-agent PR #220 and follow-on PR #223. The sanctioned-live SEA
  --agent path now publishes its node-generation requested/completed envelopes to the contract-declared
  generation event topics after local event-chain capture, so OmniMarket node_projection_delegation can
  materialize contractor runs into generation_events. The stability overlay owns the projection publisher
  factory, generation topics, and contractor-routable Tailscale broker address.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-220-source-checks"
    description: >-
      onex-self-extending-agent PR #220 carries the OMN-12836 source fix and focused tests proving sanctioned-live
      --agent publishes generation requested/completed projection events.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-sea-pr-220-source-checks/command.yaml"
          && grep -q '^pr_number: 220$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-sea-pr-220-source-checks/command.yaml"
          && grep -q '62 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-sea-pr-220-source-checks/command.yaml"
          && grep -q '14939be24710a018e60ed217d65c2fe46557a980' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-sea-pr-220-source-checks/command.yaml"
  - id: "dod-sea-pr-223-hotfix-checks"
    description: >-
      onex-self-extending-agent PR #223 carries the contractor-unblock hotfixes for the stability projection
      publisher config path and generated-node invoke JSON serialization.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-sea-pr-223-hotfix-checks/command.yaml"
          && grep -q '^pr_number: 223$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-sea-pr-223-hotfix-checks/command.yaml"
          && grep -q 'cbb9c6597e973ee38b11d77545fb64553585a2d7' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-sea-pr-223-hotfix-checks/command.yaml"
          && grep -q 'Receipt Gate OCC Eligibility target' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-sea-pr-223-hotfix-checks/command.yaml"
  - id: "dod-stability-overlay-readback"
    description: >-
      The SEA stability overlay resolves the generation completion topic and contractor-routable broker
      address from contract fields, not an ad-hoc environment variable.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-stability-overlay-readback/command.yaml"
          && grep -q '100.109.203.94:39092' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-stability-overlay-readback/command.yaml"
          && grep -q 'onex.evt.omnimarket.node-generation-completed.v1' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-stability-overlay-readback/command.yaml"
  - id: "dod-bus-audit-compile-only"
    description: >-
      OmniMarket bus-audit adapter compile-only accepted the SEA stability overlay, SEA pipeline topics,
      and OmniMarket projection contract roots without publishing to Kafka.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-bus-audit-compile-only/command.yaml"
          && grep -q 'status=compiled' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-bus-audit-compile-only/command.yaml"
          && grep -q 'bus_audit_compute' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-bus-audit-compile-only/command.yaml"
  - id: "dod-occ-pr-2399"
    description: >-
      OCC PR #2399 binds the OMN-12836 SEA projection contract and receipts to a mergeable onex_change_control
      evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12836/dod-occ-pr-2399/command.yaml"
