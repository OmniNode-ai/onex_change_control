---
schema_version: '1.0.0'
ticket_id: OMN-10376
title: "Task 24: [PoL] Proof of Life — end-to-end verification for all five swarm patterns"
summary: "Integration tests exercising all 5 selective swarm patterns added to omnibase_core (zone classifier,
  completion-verify, co-change dark matter, PRM detectors, AST semantic diff)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "5 integration tests pass (run in omnibase_core repo)"
    command: "gh pr checks 1011 --repo OmniNode-ai/omnibase_core"
  - kind: ci
    description: "mypy --strict and ruff check pass (verified via receipts)"
    command: "grep -q 'status: PASS' drift/dod_receipts/OMN-10376/dod-002/command.yaml && grep -q 'status:
      PASS' drift/dod_receipts/OMN-10376/dod-003/command.yaml"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "5 integration tests pass — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS' drift/dod_receipts/OMN-10376/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-10376' drift/dod_receipts/OMN-10376/dod-001/command.yaml && grep -q '^evidence_item_id:
          dod-001' drift/dod_receipts/OMN-10376/dod-001/command.yaml"
  - id: dod-002
    description: "mypy --strict clean — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS' drift/dod_receipts/OMN-10376/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-10376' drift/dod_receipts/OMN-10376/dod-002/command.yaml && grep -q '^evidence_item_id:
          dod-002' drift/dod_receipts/OMN-10376/dod-002/command.yaml"
  - id: dod-003
    description: "ruff check passes — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS' drift/dod_receipts/OMN-10376/dod-003/command.yaml && grep
          -q '^ticket_id: OMN-10376' drift/dod_receipts/OMN-10376/dod-003/command.yaml && grep -q '^evidence_item_id:
          dod-003' drift/dod_receipts/OMN-10376/dod-003/command.yaml"
