---
schema_version: "1.0.0"
ticket_id: "OMN-13532"
title: "Add live-bus target to onex delegate"
summary: >
  Adds an explicit onex delegate --bus kafka path, with bootstrap validation, so CLI-initiated delegation
  can publish typed ModelDelegateSkillRequest messages to the live dev broker instead of being forced
  through an in-process EventBusInmemory path. The default remains inmemory for self-contained local use.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "CLI delegate unit tests and focused lint/type checks pass."
    command: "uv run pytest tests/unit/cli/test_cli_delegate.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2079"
    description: "omnibase_infra PR #2079 adds --bus/--kafka-bootstrap handling and tests for onex delegate."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid -q .headRefOid"
  - id: "dod-deploy-no-runtime-impact"
    description: "CLI source and unit tests only; no live deploy or runtime mutation required before merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy-gate: OMN-13532 changes CLI option wiring and tests only; no live deploy
          required before merge'"
  - id: "dod-occ-pr-self"
    description: "OCC self-binding receipt for the central evidence PR carrying the OMN-13532 contract
      and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid -q .headRefOid"
