---
schema_version: "1.0.0"
ticket_id: "OMN-16612"
title: "Autobind OCC evidence for OMN-16612"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omniintelligence PR #877.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #877 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #877 on OmniNode-ai/omniintelligence, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omniintelligence`.
    command: "uv run pytest tests/unit/migrations/test_code_entity_ddl_ownership.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniintelligence-pr-877"
    description: "PR #877 on OmniNode-ai/omniintelligence — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 877 --repo OmniNode-ai/omniintelligence --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniintelligence/pulls/877/files --paginate --jq '.[].filename' | grep -cFx '.github/CODE_OF_CONDUCT.md'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #877 on OmniNode-ai/omniintelligence — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omniintelligence-pr-877"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/migrations/test_code_entity_ddl_ownership.py -q"
        cwd: "${OMNI_HOME}/omniintelligence"
  - id: "occ-self-bind-pr-8033"
    description: "Binds OMN-16612 to OCC companion PR #8033 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8033 --repo OmniNode-ai/onex_change_control --json number,state"
