---
schema_version: "1.0.0"
ticket_id: OMN-4152
title: 'Task 6: Retire ModelOnexEnvelopeV1 from active code paths'
summary: >-
  Task 6: Retire ModelOnexEnvelopeV1 from active code paths. Completed by OMN-13189 (B1). Verification
  confirmed ModelOnexEnvelopeV1 has zero production callers and zero test imports across omnibase_core
  / omnibase_infra / omnimarket (src + tests), so the canonical retirement is outright deletion rather
  than the originally-planned migrate-then-retain. The implementation PR omnibase_core#1242 deletes the
  class file, both models/core/__init__.py exports, the 3 V1 predecessor mentions in model_onex_envelope.py,
  and the incidental test-file docstring mention, leaving grep -rn "ModelOnexEnvelopeV1" src/ tests/ at
  zero. This contract's dod_evidence is satisfied by the same paired OCC receipt that proves OMN-13189.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on the omnibase_core implementation PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "pr-omnibase_core"
    description: >-
      ModelOnexEnvelopeV1 retired from active code paths via deletion (impl PR omnibase_core#1242, branch
      jonah/omn-13189-retire-modelonexenvelopev1). The paired OCC receipt drift/dod_receipts/OMN-4152/pr-omnibase_core/command.yaml
      binds the code PR head sha (commit_sha) and OCC PR number (pr_number) with status PASS and records
      the zero-reference grep proof plus green gate results.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-4152/pr-omnibase_core/command.yaml"
