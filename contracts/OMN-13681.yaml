---
schema_version: "1.0.0"
ticket_id: "OMN-13681"
title: "WS5 Wave 7 session dispatch and authorization multi-param integration"
summary: >-
  omnimarket PR #1476 adds real multi-parameter integration coverage for OMN-13681 as part of the WS5
  market-skill integration wave. The change is test-only evidence coverage and does not authorize or perform
  a live deploy, runtime restart, topic mutation, schema migration, database migration, or .201/.200 mutation
  during merge.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >-
      Wave-specific integration tests described in omnimarket PR #1476 passed locally with typed assertions,
      negative controls, and no live Kafka or .201 dependency.
    command: "see omnimarket PR #1476 dod_evidence"
  - kind: "tests"
    description: >-
      Full omnimarket non-Kafka suite passed locally for this wave PR before opening.
    command: "uv run pytest tests/ -m 'not kafka' -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1476"
    description: >-
      omnimarket PR #1476 carries the WS5 integration test coverage for OMN-13681 at head commit bb03e3c3f4e9d86013e129326dd3e65fed64d566.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13681/dod-omnimarket-pr-1476/command.yaml
          && grep -q '^pr_number: 1476$' drift/dod_receipts/OMN-13681/dod-omnimarket-pr-1476/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: PR #1476 is test-only WS5 integration coverage. It does not perform or authorize
      a live deploy, runtime restart, topic mutation, schema migration, database migration, or .201/.200
      mutation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13681/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-13681/dod-deploy-assessment/command.yaml"
