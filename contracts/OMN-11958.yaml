---
schema_version: "1.0.0"
ticket_id: "OMN-11958"
title: "Deduplicate auto-wiring contract names during discovery"
summary: >
  Central OCC evidence for omnibase_infra PR #1732, which prevents runtime duplicate dispatcher registration
  by surfacing duplicate auto-wiring contract names as discovery errors before dispatcher wiring.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1732"
    description: "omnibase_infra PR #1732 carries the OMN-11958 duplicate contract discovery fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11958/dod-omnibase-infra-pr-1732/command.yaml
          && grep -q '^pr_number: 1732$' drift/dod_receipts/OMN-11958/dod-omnibase-infra-pr-1732/command.yaml"
  - id: "dod-local-validation"
    description: "Focused unit and integration coverage plus lint passed for duplicate contract discovery."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11958/dod-local-validation/command.yaml
          && grep -q 'test_auto_wiring_duplicate_contract_discovery.py' drift/dod_receipts/OMN-11958/dod-local-validation/command.yaml
          && grep -q 'ruff check' drift/dod_receipts/OMN-11958/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy was performed; runtime discovery behavior remains gated by deploy-gate
      and runtime boot CI before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11958/dod-deploy-assessment/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-11958/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-1603"
    description: "onex_change_control PR #1603 carries the central OCC evidence for OMN-11958."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11958/dod-occ-pr-1603/command.yaml
          && grep -q '^pr_number: 1603$' drift/dod_receipts/OMN-11958/dod-occ-pr-1603/command.yaml"
