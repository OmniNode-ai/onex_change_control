---
schema_version: "1.0.0"
ticket_id: "OMN-14905"
title: "Deterministic flip-bundle byte-compare assertion"
summary: "OCC evidence for omnibase_core#1476, the OMN-14905 change that replaces flip-bundle assertion
  6 author-identity trust with deterministic replay and byte-compare verification."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_core#1476 product PR is open on the expected OMN-14905 deterministic byte-compare
      branch."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - kind: "ci"
    description: "omnibase_core#1476 changes only flip-bundle/equivalence producer tooling, tests, pre-commit
      wiring, and lint configuration."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1476"
    description: "PR #1476 on OmniNode-ai/omnibase_core is the OMN-14905 deterministic flip-bundle byte-compare
      change."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - id: "dod-core-pr-1476-files"
    description: "PR #1476 on OmniNode-ai/omnibase_core is scoped to flip-bundle/equivalence producer
      tooling, tests, pre-commit wiring, and lint configuration."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
