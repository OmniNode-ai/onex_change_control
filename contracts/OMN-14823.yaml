---
schema_version: "1.0.0"
ticket_id: "OMN-14823"
title: "Autobind OCC evidence for OMN-14823"
summary: "OCC Evidence-Source autobind companion for OmniNode-ai/omnibase_infra#2358."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2358 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2358"
    description: "PR #2358 on OmniNode-ai/omnibase_infra -- Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2358-ci"
    description: "PR #2358 on OmniNode-ai/omnibase_infra -- product diff scope check."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-omnibase-infra-pr-2358-deploy-scope"
    description: >-
      Deploy-gate scope for omnibase_infra PR #2358. The change touches src/omnibase_infra/nodes/node_ledger_projection_compute/handlers/handler_ledger_projection.py,
      a runtime handler path, but only flips the existing ledger projection compute node to canonical
      def-B dispatch shape with hand-flip proof while preserving ledger.append intent routing. No live
      runtime deployment, .201 mutation, docker exec, or rpk topic produce is authorized by this evidence
      repair; hosted merge gates and post-merge dev availability remain the deployment boundary.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          printf '%s\n' 'deploy scope assessed for omnibase_infra#2358 runtime handler path; no live deploy,
          docker exec, or rpk topic produce is authorized by this evidence repair'
  - id: "occ-self-bind-pr-4482"
    description: "OCC companion PR #4482 -- self-bind for OMN-14823 deploy evidence repair."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
