---
schema_version: "1.0.0"
ticket_id: OMN-10438
title: "Define Pattern B broker contracts and envelope models"
summary: "Add the typed contract/model boundary for the full Pattern B cross-CLI broker without introducing
  a canonical-looking PatternBrokerClient."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Pattern B broker contract/model tests pass"
    command: "uv run pytest tests/unit/nodes/node_pattern_b_broker -q"
  - kind: "manual"
    description: "Touched Pattern B broker files pass lint"
    command: "uv run ruff check src/omnimarket/nodes/node_pattern_b_broker tests/unit/nodes/node_pattern_b_broker"
  - kind: "manual"
    description: "Ticket contract validates"
    command: "uv run validate-yaml contracts/OMN-10438.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Pattern B broker contract/model tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_pattern_b_broker -q"
  - id: "dod-002"
    description: "Touched Pattern B broker files pass lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_pattern_b_broker tests/unit/nodes/node_pattern_b_broker"
  - id: "dod-003"
    description: "Ticket contract validates"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run validate-yaml contracts/OMN-10438.yaml"
  - id: "dod-deploy"
    description: |
      Deploy posture for this slice. OMN-10438 adds typed Pydantic envelope
      models, a contract.yaml declaration, and a contract-reading config
      adapter only. It does not add a long-running broker process, event-bus
      consumer registration, subprocess execution, or runtime deployment hook.
      The follow-up implementation tickets carry deploy/runtime smoke evidence
      when publish/consume/quality-gate behavior lands.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for OMN-10438 - typed broker contract and adapter boundary
          only; follow-up tickets carry runtime deploy smoke evidence'"
