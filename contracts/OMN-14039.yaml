---
schema_version: "1.0.0"
ticket_id: OMN-14039
title: "learning-loop: ModelLearningRecord DTO + failure-class enum"
summary: >
  Adds the shared capture substrate for the cross-process learning loop (spec docs/plans/2026-07-06-learning-loop-generalization-spec.md
  §3.1, §3.4): EnumLearningProcessId, EnumFailureClass, EnumLearningOutcome, and ModelLearningRecord in
  omnibase_compat. Pure substrate — unblocks the merge-sweep, recall-backend, and dispatch-worker learning-loop
  closures.
is_seam_ticket: false
interface_change: false
evidence_requirements:
  - kind: "ci"
    description: "ruff format/check and mypy pass on the new learning module"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14039/dod-lint-typecheck/command.yaml"
  - kind: "tests"
    description: "Full local omnibase_compat pytest suite passes, including new learning tests"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14039/dod-tests/command.yaml"
  - kind: "manual"
    description: "omnibase_compat PR #172 carries the OMN-14039 ModelLearningRecord DTO"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14039/dod-omnibase-compat-pr-172/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-lint-typecheck"
    description: "ruff format/check and mypy pass on the new learning module"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14039/dod-lint-typecheck/command.yaml"
  - id: "dod-tests"
    description: "Full local omnibase_compat pytest suite passes, including new learning tests"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14039/dod-tests/command.yaml"
  - id: "dod-omnibase-compat-pr-172"
    description: "omnibase_compat PR #172 carries the OMN-14039 ModelLearningRecord DTO"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14039/dod-omnibase-compat-pr-172/command.yaml
          && grep -q '^pr_number: 172$' drift/dod_receipts/OMN-14039/dod-omnibase-compat-pr-172/command.yaml"
