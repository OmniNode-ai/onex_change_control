---
schema_version: "1.0.0"
ticket_id: "OMN-10743"
title: "Extract hooks EventBusKafka construction to emit_bus_bootstrapper factory"
summary: >
  Binds omniclaude PR #1551, which removes 4 direct EventBusKafka() instantiations (stopgap bus-ok annotations)
  from handler_event_emitter.py and cli_emit.py. A new emit_bus_bootstrapper.py factory is introduced,
  allowlisted by the _bootstrapper.py suffix pattern in the AST validator. EventBusKafka is moved to TYPE_CHECKING
  blocks in both callers.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1551-head"
    description: "omniclaude PR #1551 carries the hooks emit bus DI refactor at the recorded head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10743/dod-pr-1551-head/command.yaml\""
  - id: "dod-no-direct-bus"
    description: "Direct EventBus instantiation validator passes after refactor."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10743/dod-no-direct-bus/command.yaml\""
  - id: "dod-tests-pass"
    description: "All three affected test files pass with updated patch targets."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10743/dod-tests-pass/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10743/dod-occ-pr/command.yaml\""
