---
schema_version: "1.0.0"
ticket_id: "OMN-14282"
title: "refactor(OMN-14282): canonicalize node_deep_dive_report_effect to thin contract+handler"
summary: >-
  omnimarket product PR #1685 (branch jonah/omn-14282-canonicalize-node_deep_dive_report_effect-to-thin,
  head 3675460a4e966ee0b940ab4723379815d60f694d) canonicalizes HandlerDeepDiveReportEffect (node_deep_dive_report_effect)
  to the thin contract+handler shape: handle() returns the typed ModelDeepDiveReportResultEvent directly
  instead of wrapping it in ModelHandlerOutput[None]; the _wrap envelope helper, the _HANDLER_ID constant,
  and the now-unused uuid4 / ModelHandlerOutput imports are removed. Public-safe generic architecture-hygiene
  work under epic OMN-14242.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      Independent landing verification at product head 3675460a4e966ee0b940ab4723379815d60f694d: node
      golden-chain + contract tests, mypy --strict on the handler, the strict state-coverage gate, and
      the golden-chain-coverage gate all pass. Runner (implementer) is distinct from the verifier (landing
      verification), satisfying the receipt-honesty gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14282/dod-001/command.yaml && grep -q '3675460a4e966ee0b940ab4723379815d60f694d'
          drift/dod_receipts/OMN-14282/dod-001/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Honest deploy-surface assessment: HandlerDeepDiveReportEffect has no production callers today outside
      its node contract.yaml and tests. Pure in-module return-shape change; no live deploy required or
      performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14282/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-14282/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14282 contract and
      PASS receipts consumed by the product PR through its Evidence-Source line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14282/dod-occ-self/command.yaml
