---
schema_version: "1.0.0"
ticket_id: "OMN-12768"
title: "OMN-12765 main->dev backmerge (infra #1892/#1894) regressed dev delegation dispatch"
summary: >-
  Two OMN-12765 main->dev backmerges (infra PRs #1892 and #1894) regressed dev lane delegation dispatch
  after the last-known-good build window at infra SHA 8a684c16. Post-backmerge dev HEAD (8a14d61b) yields
  'No dispatcher found for omnibase-infra.delegation-request' -> DLQ -> 0 delegation_events rows. Recovery:
  dev lane pinned to 8a684c16 (infra) + 39ab7e9a (omnimarket) with savings/registration contract fixes
  baked in; full delegation chain verified green (correlation 2c290645). Closeout requires: identify exact
  change in #1892/#1894 that broke dispatch, reconcile in a targeted PR or forward-port the working wiring,
  then confirm clean dev HEAD build runs delegation chain without SHA pinning.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-regression-change-identified"
    description: >-
      Specific commit/change in infra PR #1892 or #1894 that broke 'omnibase-infra.delegation-request'
      dispatch identified with code reference.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12768/dod-regression-change-identified/command.yaml
  - id: "dod-dispatch-reconciled-in-dev-head"
    description: >-
      A clean build from current dev HEAD (post-reconciliation) runs the full delegation chain without
      SHA pinning. Verified by fresh correlation ID through delegation_events row with quality_gate_passed,
      prompt+response present.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12768/dod-dispatch-reconciled-in-dev-head/command.yaml
          && grep -q 'quality_gate_passed' drift/dod_receipts/OMN-12768/dod-dispatch-reconciled-in-dev-head/command.yaml
  - id: "dod-occ-evidence"
    description: "onex_change_control OCC contract + receipts for OMN-12768."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12768/dod-occ-evidence/command.yaml && grep
          -q '^ticket_id: "OMN-12768"$' contracts/OMN-12768.yaml
