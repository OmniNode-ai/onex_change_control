---
schema_version: "1.0.0"
ticket_id: "OMN-13190"
title: "Remove 3 zero-consumer TOPIC_* constants from topic_constants.py"
summary: >-
  Platform-debt cleanup (epic OMN-13188). Removes the 3 TOPIC_* constants in omnibase_infra event_bus/topic_constants.py
  that verification AND the build confirmed dead (zero production and zero test consumers): TOPIC_DLQ_QUARANTINE
  (constant-only; the quarantine topic stays live via build_dlq_topic("quarantine")), TOPIC_EVAL_COMPLETED,
  and TOPIC_SESSION_STATUS_CHANGED. Three of the six plan candidates were refuted as NOT dead and kept:
  TOPIC_SESSION_COORDINATION_SIGNAL (live omnimarket emit_daemon consumer) and the TOPIC_DELEGATE_SKILL_COMPLETED
  / TOPIC_DELEGATE_SKILL_FAILED pair, which are the TopicEnumGenerator (OMN-2964) source for EnumOmnimarketTopic.EVT_DELEGATE_SKILL_*_V1
  consumed by runtime/service_kernel.py to wire the delegate-skill terminal result applier (OMN-11996).
  The 16 DLQ builder/format helpers are untouched.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2004"
    description: >-
      omnibase_infra PR #2004 carries the OMN-13190 dead-constant removal on a ticketed branch, with both
      topic gates and the Topic Enum Drift Check green and zero remaining references to the removed constants.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13190/dod-infra-pr-2004/command.yaml"
          && grep -q '^pr_number: 2676$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13190/dod-infra-pr-2004/command.yaml"
          && grep -q '^commit_sha: "a54dba7d5db0b264c49cc5cad3494b72363eccc2"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13190/dod-infra-pr-2004/command.yaml"
