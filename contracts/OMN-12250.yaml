---
schema_version: "1.0.0"
ticket_id: "OMN-12250"
title: "Use EnumUsageSource in GPU event model test"
summary: >
  Rebuilds omniintelligence PR #688 on current dev and updates the GPU event test to pass the typed EnumUsageSource.MEASURED
  value instead of the stale legacy "API" string literal.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-pr-688"
    description: "omniintelligence PR #688 carries the one-line GPU event test update on current dev."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12250/dod-omniintelligence-pr-688/command.yaml\"
          && grep -q '^pr_number: 688$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12250/dod-omniintelligence-pr-688/command.yaml\""
  - id: "dod-syntax-compile-pr-688"
    description: "The changed GPU event test file compiles after the EnumUsageSource test-input update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12250/dod-syntax-compile-pr-688/command.yaml\""
