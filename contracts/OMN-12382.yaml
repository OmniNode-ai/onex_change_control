---
schema_version: "1.0.0"
ticket_id: OMN-12382
title: "Resolve package-level zombie findings as integration tickets, not deletion tickets"
summary: >
  Classified all 22 Repowise package-level zombie/orphan findings as product
  surface, tooling surface, deployment manifest, eval harness, or scanner false
  positive. Decision table attached as a tracked evidence doc. No deletions —
  package isolation is by design for entrypoints/tooling/manifests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "docs"
    description: "Zombie package classification decision table is tracked in omni_home"
    command: "test -f docs/evidence/OMN-12368-verification/zombie-packages.md"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-classification-table"
    description: "Zombie package classification decision table is tracked in omni_home"
    source: "docs"
    checks:
      - check_type: "command"
        check_value: "git -C \"$OMNI_HOME\" ls-files --error-unmatch docs/evidence/OMN-12368-verification/zombie-packages.md"
  - id: "dod-table-content"
    description: "Decision table classifies every package with a verdict column"
    source: "docs"
    checks:
      - check_type: "command"
        check_value: "grep -q 'Classification Table' \"$OMNI_HOME/docs/evidence/OMN-12368-verification/zombie-packages.md\" && grep -qE 'TOOLING_SURFACE|PRODUCT_SURFACE|DEPLOYMENT_MANIFEST|EVAL_HARNESS' \"$OMNI_HOME/docs/evidence/OMN-12368-verification/zombie-packages.md\""
