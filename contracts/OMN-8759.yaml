---
schema_version: "1.0.0"
ticket_id: OMN-8759
title: "Create missing node - node_dod_sweep_orchestrator"
summary: "Record central OCC evidence that OmniMarket PR #524 proves the existing DoD sweep orchestrator node is implemented, registered, and discoverable."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused DoD sweep orchestrator tests pass for OmniMarket PR #524."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8759/dod-focused-tests/command.yaml"
  - kind: "tests"
    description: "Adjacent stub-node policy regression tests pass for OmniMarket PR #524."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8759/dod-stub-policy-tests/command.yaml"
  - kind: "ci"
    description: "Touched DoD sweep orchestrator code and tests pass ruff for OmniMarket PR #524."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8759/dod-lint/command.yaml"
  - kind: "manual"
    description: "Current ONEX discovery output includes node_dod_sweep_orchestrator for OmniMarket PR #524."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8759/dod-node-discovery/command.yaml"
  - kind: "ci"
    description: "OCC evidence contract validates and local receipt gate passes for OCC PR #714."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8759/dod-occ-validation/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused DoD sweep orchestrator tests pass for OmniMarket PR #524."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8759/dod-focused-tests/command.yaml && grep -q '^pr_number: 524$' drift/dod_receipts/OMN-8759/dod-focused-tests/command.yaml"
  - id: "dod-stub-policy-tests"
    description: "Adjacent stub-node policy regression tests pass for OmniMarket PR #524."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8759/dod-stub-policy-tests/command.yaml && grep -q '^pr_number: 524$' drift/dod_receipts/OMN-8759/dod-stub-policy-tests/command.yaml"
  - id: "dod-lint"
    description: "Touched DoD sweep orchestrator code and tests pass ruff for OmniMarket PR #524."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8759/dod-lint/command.yaml && grep -q '^pr_number: 524$' drift/dod_receipts/OMN-8759/dod-lint/command.yaml"
  - id: "dod-node-discovery"
    description: "Current ONEX discovery output includes node_dod_sweep_orchestrator for OmniMarket PR #524."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8759/dod-node-discovery/command.yaml && grep -q '^pr_number: 524$' drift/dod_receipts/OMN-8759/dod-node-discovery/command.yaml"
  - id: "dod-occ-validation"
    description: "OCC evidence contract validates and local receipt gate passes for OCC PR #714."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8759/dod-occ-validation/command.yaml && grep -q '^pr_number: 714$' drift/dod_receipts/OMN-8759/dod-occ-validation/command.yaml"
