---
schema_version: "1.0.0"
ticket_id: "OMN-10547"
title: "chore: raw env usage audit baseline"
summary: "Add deterministic omnimarket raw environment variable usage audit artifacts for public-shippable
  settings work."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Receipt Gate passes on omnimarket PR #532 after this OCC evidence lands."
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Audit script deterministically writes 259 raw env usage rows."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python scripts/audit/raw_env_usage_audit.py"
  - id: "dod-002"
    description: "Regex cross-check count and documented row-count delta are recorded."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "rg -nP 'os\\.environ\\.get|os\\.getenv|os\\.environ\\[' src/omnimarket/ | wc -l
          && grep -q 'Row count:** 259' docs/audits/2026-05-05-raw-env-usage.md && grep -q '9-row delta'
          docs/audits/2026-05-05-raw-env-usage.md"
  - id: "dod-003"
    description: "Audit artifacts and downstream-use documentation are present on omnimarket PR #532."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json files --jq '.files[].path'
          | grep -Fx 'docs/audits/2026-05-05-raw-env-usage.csv' && gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket
          --json files --jq '.files[].path' | grep -Fx 'docs/audits/2026-05-05-raw-env-usage.md' && gh
          pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json files --jq '.files[].path' | grep
          -Fx 'scripts/audit/raw_env_usage_audit.py'"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10547 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10547/dod-occ-pr/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-occ-pr\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10547/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10547\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10547/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number: 736$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10547/dod-occ-pr/command.yaml\""
