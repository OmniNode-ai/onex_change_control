---
schema_version: 1.0.0
ticket_id: OMN-14763
title: omninode_infra ready-for-review auto-merge guard evidence
summary: OCC evidence for omninode_infra#607 F-24 auto-merge workflow hardening.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: OmniNode-ai/omninode_infra#607 CI succeeded on exact product head.
    command: gh run view ${RUN_ID} --repo ${REPO} --json headSha,conclusion
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omninode-infra-607-head-b588a2cb
    description: OmniNode-ai/omninode_infra#607 is open at exact product head b588a2cbc1c22171178fdb4854ae8af1a9bb4ead.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid,body
  - id: dod-omninode-infra-607-diff-b588a2cb
    description: OmniNode-ai/omninode_infra#607 has a non-empty reviewed product diff for OMN-14763.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr diff ${PR_NUMBER} --repo ${REPO} --name-only
  - id: dod-omninode-infra-607-ci-b588a2cb
    description: CI run 29637149541 completed successfully for OmniNode-ai/omninode_infra#607 at exact
      head b588a2cbc1c22171178fdb4854ae8af1a9bb4ead.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29637149541; PRODUCT_REPO=OmniNode-ai/omninode_infra; PRODUCT_HEAD=b588a2cbc1c22171178fdb4854ae8af1a9bb4ead;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-omn-14763-deploy-scope
    description: 'Deploy-scope proof for OMN-14763: product diff is limited to auto-merge workflow change;
      this OCC lane performed no live deploy, restart, docker exec, rpk topic produce, or production mutation.'
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14763/dod-omn-14763-deploy-scope/command.yaml
          && grep -qi ''no live deploy, restart, docker exec, rpk topic produce, or production mutation''
          drift/dod_receipts/OMN-14763/dod-omn-14763-deploy-scope/command.yaml'
  - id: occ-self-bind-pr-4383-omn-14763
    description: 'OCC PR #4383 self-binding proof for OMN-14763 evidence.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
