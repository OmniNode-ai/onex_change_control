---
schema_version: "1.0.0"
ticket_id: "OMN-10786"
title: "test(OMN-10786): proof-of-life E2E for interactive onboarding"
summary: >
  Three parametrized integration tests (local, cloud, hybrid) traversing the full interactive_onboarding.yaml
  policy via InteractiveExecutor + AdapterFakeInput. Each test verifies env_dict, step ordering, provenance,
  and round-trips the env through ConfigWriter to a tmp_path. Pure test addition — no source modifications.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1559 carries the OMN-10786 PoL E2E tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10786/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Three parametrized integration tests (local, cloud, hybrid) pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10786/dod-local-tests/command.yaml\""
  - id: "dod-occ-contract"
    description: "OCC contract exists for OMN-10786."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10786/dod-occ-contract/command.yaml\""
  - id: "dod-occ-pr-906"
    description: "Self-binding receipt for OCC PR #906."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10786/dod-occ-pr-906/command.yaml\""
