---
schema_version: "1.0.0"
ticket_id: "OMN-12664"
title: "fix(OMN-12664): route full Gemini endpoint via endpoint_url to preserve /v1beta/openai/chat/completions"
summary: >-
  Central OCC evidence for omnimarket PR #1031. The generation consumer passed the contract-provided LLM
  endpoint through ModelLlmInferenceRequest.base_url, which makes the omnibase_infra adapter append
  /v1/chat/completions and produce a 404 against Gemini's OpenAI-compatible endpoint. The fix routes the
  complete contract-provided endpoint through endpoint_url (post-as-is) using a provider-neutral, path-based
  predicate; origin-only bases keep the legacy append. No new helper module, router, or adapter.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1031-final-url"
    description: >-
      omnimarket PR #1031 regression tests build the real ModelLlmInferenceRequest the handler emits and run
      it through the canonical infra HandlerLlmOpenaiCompatible._build_url, asserting the final POST URL is
      https://generativelanguage.googleapis.com/v1beta/openai/chat/completions and is neither the
      single-versioned nor double-versioned 404 variant.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12664/dod-omnimarket-pr-1031-final-url/command.yaml"
          && grep -q '^pr_number: 1031$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12664/dod-omnimarket-pr-1031-final-url/command.yaml"
  - id: "dod-omnimarket-pr-1031-typed-failure"
    description: >-
      omnimarket PR #1031 keeps provider 404 a typed failure (contract_passed=False, generation-failed
      emitted, no deploy/registration) — never blank success.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12664/dod-omnimarket-pr-1031-typed-failure/command.yaml"
          && grep -q '^pr_number: 1031$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12664/dod-omnimarket-pr-1031-typed-failure/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-12664 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12664/dod-occ-pr-self/command.yaml"
