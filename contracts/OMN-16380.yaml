---
schema_version: "1.0.0"
ticket_id: "OMN-16380"
title: "bump npm-bundled tar 7.5.19->7.5.21 in Dockerfile.runtime — CVE-2026-73566"
summary: >
  Manual OCC companion for OmniNode-ai/omnibase_infra PR #2838. Bumps the tar@7.5.19 spec in docker/Dockerfile.runtime's
  npm-bundle CVE-patch RUN step (OMN-15688) to tar@7.5.21, clearing CVE-2026-73566 (GHSA-r292-9mhp-454m,
  HIGH — uncontrolled recursion in mapHas/filesFilter allows an uncatchable stack-overflow DoS via a crafted
  long-path tar with member selection) that reds Trivy's image CVE gate (run 32547888151). One-line Dockerfile
  diff; no runtime behavior change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2838 CI checks green; scripts/check_dockerfile_pins.py passes locally."
    command: "gh pr checks 2838 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2838"
    description: "PR #2838 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2838 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2838 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: >
      Deploy-scope DoD so PR #2838 clears the deploy-gate falsifiability ratchet (OMN-14443). Reads the
      actual CONTENT of docker/Dockerfile.runtime at PR #2838's pinned head commit 02e3b45f6aa4be2efc630e3684134fc30f5f66c1
      (a real GitHub content-API read, not a self-referential grep of this PR's own diff/receipt text)
      and asserts the patched pin tar@7.5.21 is present — falsifiable: would go RED if the commit lacked
      the bump or pinned a different tar version (OMN-15309 content-read-probe admissibility).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh api "repos/OmniNode-ai/omnibase_infra/contents/docker/Dockerfile.runtime?ref=02e3b45f6aa4be2efc630e3684134fc30f5f66c1"
          --jq .content | base64 -d | grep 'tar@7.5.21'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2838"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6871"
    description: "Binds OMN-16380 to this OCC companion PR #6871 (self-bind)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6871 --repo OmniNode-ai/onex_change_control --json number,state"
