---
schema_version: "1.0.0"
ticket_id: "OMN-10688"
title: "AST-based contract-config compliance validator"
summary: >
  Adds omnibase_core.validators.contract_config_compliance: an AST-based static validator that catches
  three anti-patterns (bare_env_read, bus_bypass_import, missing_contract_config) in handler and script
  files. Wires as pre-commit hook and CI gate feeding the quality-gate required status check.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "25 unit tests in tests/unit/validators/test_contract_config_compliance.py pass on omnibase_core
      PR #1053"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10688/dod-unit-tests/command.yaml\""
  - id: "dod-pr-1053-head"
    description: "Bind OMN-10688 evidence to omnibase_core PR #1053 head commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1053 --repo OmniNode-ai/omnibase_core --json headRefOid,title"
  - id: "dod-deploy"
    description: "Deploy-gate evidence: pure static validator, no runtime process or service deploy required."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10688/dod-deploy/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10688 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10688/dod-occ-pr/command.yaml\""
