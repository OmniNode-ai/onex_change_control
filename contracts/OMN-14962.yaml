---
schema_version: "1.0.0"
ticket_id: "OMN-14962"
title: "Kafka publish header correlation_id parity"
summary: >-
  OCC Evidence-Source companion for omnibase_infra PR #2401. The product PR fixes AdapterProtocolEventPublisherKafka
  so the Kafka message header correlation_id is the same UUID carried in the serialized ModelEventEnvelope,
  preventing an independent header UUID mint in EventBusKafka.publish().
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #2401 is open at the expected product head."
    command: "gh pr view 2401 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid"
  - kind: "ci"
    description: "omnibase_infra PR #2401 diff is scoped to the Kafka publisher adapter and its integration
      regression test."
    command: "gh pr view 2401 --repo OmniNode-ai/omnibase_infra --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2401-head"
    description: "Binds omnibase_infra PR #2401 at head 8c88d35b80e7fe636d925a64d53266871d2815fd."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2401 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid,headRefName"
  - id: "dod-omnibase-infra-pr-2401-diff-scope"
    description: >-
      PR #2401 product diff touches only src/omnibase_infra/event_bus/adapters/adapter_protocol_event_publisher_kafka.py
      and tests/integration/event_bus/test_adapter_protocol_event_publisher_kafka.py. This is a Kafka
      adapter correctness fix plus regression coverage; no live runtime deploy, .201 mutation, docker
      exec, or rpk produce is authorized by this evidence repair.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2401 --repo OmniNode-ai/omnibase_infra --json files"
