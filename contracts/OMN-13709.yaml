---
schema_version: "1.0.0"
ticket_id: "OMN-13709"
title: "fix(OMN-13709): remove duplicate EnumBuildability from node_ticket_classify_compute"
summary: >
  Delete the node-local copy of EnumBuildability (omnimarket/src/omnimarket/nodes/node_ticket_classify_compute/models/enum_buildability.py)
  and update the 3 importers (model_ticket_classification.py, handler_ticket_classify.py, models/__init__.py,
  plus the golden-chain test) to import from the canonical shared location omnimarket.enums.enum_buildability.
  The local copy was created to avoid omnibase_infra imports, but the shared enum already has zero infra
  deps.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Full pytest suite passes; golden-chain ticket-classify tests pass"
    command: "cd omnimarket && uv run pytest tests/ -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >
      omnimarket PR carries the deletion of the duplicate enum and the import redirects. uv run pytest
      tests/ -v: 7414 passed, 73 skipped, 2 Kafka-infra failures (pre-existing, require live broker at
      192.168.86.201). mypy --strict: no issues in 2348 source files. pre-commit run --all-files: all
      hooks passed. All 29+ node_ticket_classify_compute tests pass.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "true"
