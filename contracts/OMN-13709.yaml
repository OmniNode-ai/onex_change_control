---
schema_version: "1.0.0"
ticket_id: "OMN-13709"
title: "fix(OMN-13709): remove duplicate EnumBuildability from node_ticket_classify_compute"
summary: >
  Delete the node-local copy of EnumBuildability (omnimarket/src/omnimarket/nodes/node_ticket_classify_compute/models/enum_buildability.py)
  and update the 3 importers (model_ticket_classification.py, handler_ticket_classify.py, models/__init__.py,
  plus the golden-chain test) to import from the canonical shared location omnimarket.enums.enum_buildability.
  The local copy was created to avoid omnibase_infra imports, but the shared enum already has zero infra
  deps.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "PASS receipts bind targeted omnimarket evidence for the duplicate EnumBuildability removal."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13709/dod-001/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-13709/dod-mypy-imports/command.yaml && grep -q '^status: PASS$' drift/dod_receipts/OMN-13709/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >
      Targeted node_ticket_classify_compute unit and golden-chain tests pass after the duplicate enum
      deletion and canonical import redirects.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13709/dod-001/command.yaml"
  - id: "dod-mypy-imports"
    description: >
      Targeted strict mypy passes for node_ticket_classify_compute, and git grep finds no remaining imports
      of the deleted node-local enum path.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13709/dod-mypy-imports/command.yaml"
  - id: "dod-occ-self"
    description: >
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13709 contract and receipts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13709/dod-occ-self/command.yaml"
