---
schema_version: "1.0.0"
ticket_id: "OMN-16126"
title: "Autobind OCC evidence for OMN-16126"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omniclaude PR #1995
  and OmniNode-ai/omnibase_infra PR #2770.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "OMN-16126 product PR CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniclaude-pr-1995"
    description: "PR #1995 on OmniNode-ai/omniclaude — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1995 --repo OmniNode-ai/omniclaude --json number,state"
      - check_type: "command"
        check_value: "gh pr view 1995 --repo OmniNode-ai/omniclaude --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omniclaude-pr-1995"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6604"
    description: "Binds OMN-16126 to OCC companion PR #6604 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6604 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-eval-framework-path-scrub"
    description: "docs/EVAL_FRAMEWORK.md:180 replaced the hardcoded /Volumes/PRO-G40/Code/omni_home path
      in a public Python code example with os.environ[\"OMNI_HOME\"], matching the org's own path-discipline
      rule. Verified by counting added OMNI_HOME lines in the PR diff."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 6607 --repo OmniNode-ai/onex_change_control | grep -c '^+.*OMNI_HOME'"
  - id: "dod-omn12872-runtime-bundle-scrub"
    description: "docs/evidence/OMN-12872/runtime-identity-bundle-2026-06-09.md had the leaked 100.109.203.94
      broker host:port and runtime://omninode-pc/... runtime address redacted to <onex-host>-style placeholders.
      Verified by counting added placeholder lines in the PR diff."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 6607 --repo OmniNode-ai/onex_change_control | grep -c '^+.*<onex-host>'"
  - id: "dod-omn11513-evidence-scrub"
    description: "evidence/OMN-11513/** (delegation-golden-chain-proof-2026-05-28 bundle, 30 touched files)
      had LAN/Tailscale IPs and MagicDNS hostnames redacted to placeholders. evidence/OMN-11513/delegation-golden-chain-proof-2026-05-28/run_proof.py
      is exempt (carries an explicit # local-path-ok allowlist annotation for lab-connectivity literals)
      and was left untouched. Verified by counting touched files under evidence/OMN-11513/ in the PR diff."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 6607 --repo OmniNode-ai/onex_change_control | grep -c '^diff --git a/evidence/OMN-11513/'"
  - id: "dod-omn8376-dogfood-scrub"
    description: "evidence/OMN-8376/dogfood-2026-04-11.md had 100.109.203.94, 100.106.37.48 (bret-primary-home),
      the omninode-pc host labeled \"Jonah's Linux lab server\", and /Volumes/PRO-G40/Code/omni_home redacted
      to placeholders / $OMNI_HOME. Verified against the PR diff for this file."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 6607 --repo OmniNode-ai/onex_change_control | grep -c 'evidence/OMN-8376/dogfood-2026-04-11.md'"
  - id: "dod-no-residual-leaks-in-scope"
    description: "No occurrence of the leaked literals (100.109.203.94, 100.106.37.48, /Volumes/PRO-G40,
      /Users/jonah) survives in any added line of the PR diff, OUTSIDE this ticket's own OCC contract/receipt
      files — i.e. the scrub did not itself reintroduce or leave behind any in-scope leak. contracts/OMN-16126.yaml
      and drift/dod_receipts/OMN-16126/** are excluded from the scan because they legitimately name these
      same literals in prose (this entry's own description, and receipt probe_stdout/actual_output) as
      the documented record of what was found and redacted -- that self-reference is not a leak."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "body=\"$(gh pr diff 6607 --repo OmniNode-ai/onex_change_control | awk '/^diff --git/{skip=($0
          ~ /contracts\\/|drift\\/dod_receipts\\//)} !skip')\" && printf '%s' \"$body\" | grep -q '^diff
          --git' && ! printf '%s' \"$body\" | grep '^+' | grep -qE '100\\.109\\.203\\.94|100\\.106\\.37\\.48|/Volume[s]/PRO-G40|/User[s]/jonah'"
  - id: "occ-self-bind-pr-6607"
    description: "This PR itself, binding the evidence set to the governed change-control record."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6607 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2770"
    description: "PR #2770 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2770 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2770 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator-pr-2770"
    description: "Hosted OCC evidence admissibility validator for OMN-16126 / PR #2770."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2770"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6610"
    description: "Binds OMN-16126 to OCC companion PR #6610 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6610 --repo OmniNode-ai/onex_change_control --json number,state"
