---
schema_version: "1.0.0"
ticket_id: "OMN-13655"
title: "feat(OMN-13655): route cloud build/deploy through canonical event-bus node path; retire imperative
  skill"
summary: >
  Scope (b) safe bus-event + thin-shim refactor. Adds ModelRuntimeImageBuilt contract-declared event and
  wires it through node_redeploy_orchestrator -> node_prod_promotion_gate_compute -> node_redeploy_deploy_effect.
  Retires the 47KB imperative cloud-redeploy skill (git/gh/docker driven) with a thin shim that emits
  onex.evt.omnimarket.runtime-image-built.v1 onto the bus.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain-tests"
    description: >
      4 golden-chain unit tests prove: dev build-complete -> gate-evaluate; prod build-complete -> grant-resolve;
      gate-denied -> completed:BLOCKED; no direct deploy-publish emitted for build-complete event (gate
      mediated).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13655/dod-golden-chain-tests/command.yaml\""
  - id: "dod-thin-shim-no-imperative"
    description: >
      grep confirms the thin shim prompt.md has no executable git/gh/docker/kubectl commands (only explanatory
      text about what is NOT done).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13655/dod-thin-shim-no-imperative/command.yaml\""
  - id: "dod-omnimarket-full-gates"
    description: >
      omnimarket: 6688 unit tests pass, mypy --strict clean (0 errors), all 32 pre-commit hooks pass.
      omninode_infra: all pre-commit hooks pass.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13655/dod-omnimarket-full-gates/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carries the OMN-13655 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13655/dod-occ-pr-self/command.yaml\""
