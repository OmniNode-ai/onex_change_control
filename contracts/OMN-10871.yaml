---
schema_version: "1.0.0"
ticket_id: "OMN-10871"
title: "DI refactor: HandlerSegmentation AdapterInferenceBridge fallback annotated"
summary: >
  HandlerSegmentation.inference_bridge was already typed as ModelInferenceAdapter | None (ABC). Annotates
  the AdapterInferenceBridge fallback construction with lifecycle-ok: optional-di-fallback to make the
  DI pattern explicit. Full removal of env-loading fallback is a follow-on; dispatch instructions specified
  optional-DI-fallback pattern only. Part of OMN-10841 DI Wave 2 refactor.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-annotation-test"
    description: "test_handler_segmentation_init_type_annotation_is_typed passes; ModelInferenceAdapter
      in annotation, no Any."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10871/dod-annotation-test/command.yaml\""
  - id: "dod-omnimarket-pr-691"
    description: "omnimarket PR #691 open with CI green at commit 382674e2b5b62ba6dd826e6a8e45b78026adeba8."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10871/dod-omnimarket-pr-691/command.yaml\"
          && grep -q '^pr_number: 691$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10871/dod-omnimarket-pr-691/command.yaml\""
