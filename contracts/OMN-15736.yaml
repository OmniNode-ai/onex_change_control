---
schema_version: "1.0.0"
ticket_id: "OMN-15736"
title: "Autobind OCC evidence for OMN-15736"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2685.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2685 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2685"
    description: "PR #2685 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2685 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2685 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2685 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2685 --repo OmniNode-ai/omnibase_infra --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2685"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6199"
    description: "Binds OMN-15736 to OCC companion PR #6199 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6199 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "deploy-scope-falsifiable-probe"
    description: >-
      Falsifiable deploy-gate probe (OMN-14443/OMN-14505) for PR #2685's actual change: reads docker/docker-compose.dns-cache.yml
      from OmniNode-ai/omnibase_infra at the PR head SHA via the GitHub contents API and asserts the file's
      own "SHOVEL-READY / INERT" status banner and its "53:53/udp" port publish are both present — a real
      live-surface read of the product repo (not the OCC receipt tree) whose result depends on the actual
      diff: the identical probe against `dev` HEAD (pre-PR) 404s (the file does not exist there yet),
      and against the PR head SHA it returns 1/1 matches. A second check reads the live .201 runner host
      over ssh (read-only `docker ps -a`) and asserts zero containers named `omninode-dns-cache` are running
      there, corroborating the file's own claim that this is a net-new, not-yet-brought-up service — no
      deploy-scope daemon mutation accompanies this PR.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.dns-cache.yml?ref=0ce520e930b86ee83507c112fdd63385fae1ce40"
          --jq .content)" && decoded="$(printf '%s' "$body" | base64 -d)" && printf '%s' "$decoded" |
          grep -qF 'SHOVEL-READY / INERT' && printf '%s' "$decoded" | grep -qF '53:53/udp'
      - check_type: "command"
        check_value: >-
          [ "$(ssh jonah@192.168.86.201 "docker ps -a --filter name=omninode-dns-cache --format '{{.Names}}'
          | wc -l" | tr -d '[:space:]')" = "0" ]
  - id: "occ-self-bind-pr-6218"
    description: "Binds OMN-15736 to OCC companion PR #6218 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6218 --repo OmniNode-ai/onex_change_control --json number,state"
