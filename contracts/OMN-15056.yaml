---
schema_version: "1.0.0"
ticket_id: "OMN-15056"
title: "public-evidence scrub: redact leaked POSTGRES_PASSWORD literal from evidence tree"
summary: "Operator ruling (2026-07-26): onex_change_control stays public; scrub + rotate is the remedy
  for secret-shaped content already published. This is the redact-tip half — replacing the one confirmed
  real credential (evidence/OMN-11513/delegation-golden-chain-proof-2026-05-28/run_proof.py lines 36-38,
  POSTGRES_PASSWORD os.environ.get() default literal) with a masked placeholder, preserving file structure.
  Rotation of the live credential is tracked separately on OMN-15047 (operator/infra action, not performed
  here)."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "This PR's own diff redacts the credential; local pre-commit battery run clean before
      push (evidence/ is outside the OCC Append-Only Gate's contracts/+drift/dod_receipts/ diff scope,
      so no append-only conflict applies to this file)."
    command: "gh pr view 4929 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid,files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-redact-run-proof-credential"
    description: "run_proof.py:36-38 POSTGRES_PASSWORD os.environ.get() default literal replaced with
      the REDACTED-POSTGRES_PASSWORD-Piyr placeholder (masked prefix only, per OMN-15056 never-paste-secret-values
      discipline — the raw value is deliberately not reproduced in this contract or any tracked artifact)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'REDACTED-POSTGRES_PASSWORD-Piyr' evidence/OMN-11513/delegation-golden-chain-proof-2026-05-28/run_proof.py
          && ! grep -qE '\"POSTGRES_PASSWORD\", \"[A-Za-z0-9]{20,}\"' evidence/OMN-11513/delegation-golden-chain-proof-2026-05-28/run_proof.py"
  - id: "occ-self-bind-pr-4929-omn-15056"
    description: "OCC PR #4929 carries the OMN-15056 redaction evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4929 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid"
