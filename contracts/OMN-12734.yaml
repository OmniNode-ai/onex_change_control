---
schema_version: "1.0.0"
ticket_id: "OMN-12734"
title: "Contract resolver runtime service inherits BIFROST_CONTRACT_PATH without data volume"
summary: >-
  Central evidence for omnibase_infra PR #1881. The PR preserves the live dev-lane hotpatch by clearing
  BIFROST_CONTRACT_PATH for omninode-contract-resolver in the durable compose source and the runtime catalog,
  preventing the HTTP bridge from rendering Bifrost delegation config into /app/data when it has no data
  volume.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1881"
    description: "omnibase_infra PR #1881 carries the durable compose and catalog fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12734/dod-omnibase-infra-pr-1881/command.yaml"
          && grep -q '^pr_number: 1881$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12734/dod-omnibase-infra-pr-1881/command.yaml"
  - id: "dod-local-validation"
    description: "Focused catalog, compose-render, lint, and whitespace checks passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12734/dod-local-validation/command.yaml"
          && grep -q '11 passed, 1 xpassed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12734/dod-local-validation/command.yaml"
          && grep -q 'BIFROST_CONTRACT_PATH == ""' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12734/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Runtime impact is bounded to post-merge dev-lane redeploy and contract-resolver health
      validation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12734/dod-deploy-assessment/command.yaml"
          && grep -q 'post-merge dev-lane redeploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12734/dod-deploy-assessment/command.yaml"
          && grep -q 'http://127.0.0.1:8091/health' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12734/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2218"
    description: "onex_change_control PR #2218 carries the central OMN-12734 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12734/dod-occ-pr-2218/command.yaml"
          && grep -q '^pr_number: 2218$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12734/dod-occ-pr-2218/command.yaml"
