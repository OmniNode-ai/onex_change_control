---
schema_version: "1.0.0"
ticket_id: "OMN-14292"
title: "refactor(OMN-14292): canonicalize node_slack_publish_effect to thin contract+handler"
summary: >-
  omnimarket product PR #1687 (branch jonah/omn-14292-canonicalize-node_slack_publish_effect-to-thin,
  head 5152b974318838aa93b8a34322ab1ae65f327bcb) canonicalizes HandlerSlackPublishEffect (node_slack_publish_effect)
  to the thin contract+handler shape: handle() takes a single typed ModelSlackPublish payload (parameter
  renamed command to payload) and returns ModelSlackPublishResult directly instead of wrapping it in ModelHandlerOutput[None];
  the _HANDLER_ID constant and the now-unused uuid4 / ModelHandlerOutput imports are removed. The node
  Kafka consumer is updated to route topics off the direct-return result. Public-safe generic architecture-hygiene
  work under epic OMN-14242.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      Independent landing verification at product head 5152b974318838aa93b8a34322ab1ae65f327bcb: node
      golden-chain tests, mypy --strict on the handler and consumer, the strict state-coverage gate, and
      the golden-chain-coverage gate all pass. Runner (implementer) is distinct from the verifier (landing
      verification), satisfying the receipt-honesty gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14292/dod-001/command.yaml && grep -q '5152b974318838aa93b8a34322ab1ae65f327bcb'
          drift/dod_receipts/OMN-14292/dod-001/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Honest deploy-surface assessment: HandlerSlackPublishEffect has no production callers today outside
      its node contract.yaml, its own consumer, and tests. Pure in-module return-shape change threaded
      through the node consumer; no live deploy required or performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14292/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-14292/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14292 contract and
      PASS receipts consumed by the product PR through its Evidence-Source line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14292/dod-occ-self/command.yaml
