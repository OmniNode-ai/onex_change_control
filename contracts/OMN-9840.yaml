---
schema_version: 1.0.0
ticket_id: OMN-9840
summary: 'Task 15: [Epic A] Update omnidash-v2/CLAUDE.md to reference OmniNode-ai org'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Unit tests pass (where applicable)
    command: uv run pytest tests/ -m unit -x
  - kind: ci
    description: CI pipeline green on the PR
    command: gh pr checks
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Contract YAML for this ticket parses as valid YAML
    source: generated
    checks:
      - check_type: command
        check_value: uv run python -c "import yaml; yaml.safe_load(open('contracts/${TICKET_ID}.yaml').read());
          print('ok')"
  - id: dod-002
    description: PR title or body cites this ticket
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json title,body --jq '.title + " " + (.body
          // "")' | grep -q '${TICKET_ID}'
