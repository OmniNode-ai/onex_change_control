---
schema_version: "1.0.0"
ticket_id: "OMN-15241"
title: "Autobind OCC evidence for OMN-15241"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_spi PR #269.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #269 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_spi-pr-269"
    description: "PR #269 on OmniNode-ai/omnibase_spi — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5146"
    description: "Binds OMN-15241 to OCC companion PR #5146 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1919-hook-wired-92986d89"
    description: >-
      Independent content probe for omnimarket PR #1919 at pinned head 92986d89: the reject-required-check-skip-vector
      wrapper script is present in the product tree AND the .pre-commit-config.yaml hook entry that actually
      invokes it is present. Both clauses matter -- a script with no config entry is dead code and a config
      entry with no script is a broken hook, and either alone would satisfy a one-clause probe while leaving
      the CI-only-enforcement gap OMN-15241 exists to close. Read over the live GitHub contents API against
      the product repo -- it reads product source, not this receipt or this contract, so it is falsifiable
      by construction. Falsifiability PROVEN by execution, not asserted: the identical chain exits 0 at
      92986d89 and exits 1 at omnimarket@dev (the merge base), where neither artifact exists. Authored
      by an independent verifier who did not build this PR (no-self-evidence rule); read-only, no mutation
      of any kind.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnimarket/contents/.pre-commit-hooks/reject-required-check-skip-vector.sh?ref=92986d89f10aafadfc88cf180fc6f72ff4b69101'
          --jq .content | base64 -d | grep -q 'validate_no_required_check_skip_vectors.py' && gh api 'repos/OmniNode-ai/omnimarket/contents/.pre-commit-config.yaml?ref=92986d89f10aafadfc88cf180fc6f72ff4b69101'
          --jq .content | base64 -d | grep -q 'id: reject-required-check-skip-vector'
  - id: "dod-omnibase-compat-pr-183-hook-wired-78ad16e4"
    description: >-
      Independent content probe for omnibase_compat PR #183 at pinned head 78ad16e4: the reject-required-check-skip-vector
      wrapper script is present in the product tree AND the .pre-commit-config.yaml hook entry that actually
      invokes it is present. Both clauses matter -- a script with no config entry is dead code and a config
      entry with no script is a broken hook, and either alone would satisfy a one-clause probe while leaving
      the CI-only-enforcement gap OMN-15241 exists to close. Read over the live GitHub contents API against
      the product repo -- it reads product source, not this receipt or this contract, so it is falsifiable
      by construction. Falsifiability PROVEN by execution, not asserted: the identical chain exits 0 at
      78ad16e4 and exits 1 at omnibase_compat@dev (the merge base), where neither artifact exists. Authored
      by an independent verifier who did not build this PR (no-self-evidence rule); read-only, no mutation
      of any kind.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnibase_compat/contents/.pre-commit-hooks/reject-required-check-skip-vector.sh?ref=78ad16e47208d0ad1967d183677d1262a6f8802f'
          --jq .content | base64 -d | grep -q 'validate_no_required_check_skip_vectors.py' && gh api 'repos/OmniNode-ai/omnibase_compat/contents/.pre-commit-config.yaml?ref=78ad16e47208d0ad1967d183677d1262a6f8802f'
          --jq .content | base64 -d | grep -q 'id: reject-required-check-skip-vector'
  - id: "dod-omnidash-pr-273-hook-wired-d461b57c"
    description: >-
      Independent content probe for omnidash PR #273 at pinned head d461b57c: the reject-required-check-skip-vector
      wrapper script is present in the product tree AND the .pre-commit-config.yaml hook entry that actually
      invokes it is present. Both clauses matter -- a script with no config entry is dead code and a config
      entry with no script is a broken hook, and either alone would satisfy a one-clause probe while leaving
      the CI-only-enforcement gap OMN-15241 exists to close. Read over the live GitHub contents API against
      the product repo -- it reads product source, not this receipt or this contract, so it is falsifiable
      by construction. Falsifiability PROVEN by execution, not asserted: the identical chain exits 0 at
      d461b57c and exits 1 at omnidash@dev (the merge base), where neither artifact exists. Authored by
      an independent verifier who did not build this PR (no-self-evidence rule); read-only, no mutation
      of any kind.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnidash/contents/.pre-commit-hooks/reject-required-check-skip-vector.sh?ref=d461b57c6cbaddf5f3e05c67c7a8bfc2ec976ad5'
          --jq .content | base64 -d | grep -q 'validate_no_required_check_skip_vectors.py' && gh api 'repos/OmniNode-ai/omnidash/contents/.pre-commit-config.yaml?ref=d461b57c6cbaddf5f3e05c67c7a8bfc2ec976ad5'
          --jq .content | base64 -d | grep -q 'id: reject-required-check-skip-vector'
  - id: "dod-occ-pr-5143-hook-wired-9e9ae927"
    description: >-
      Independent content probe for onex_change_control PR #5143 at pinned head 9e9ae927: the reject-required-check-skip-vector
      wrapper script is present in the product tree AND the .pre-commit-config.yaml hook entry that actually
      invokes it is present. Both clauses matter -- a script with no config entry is dead code and a config
      entry with no script is a broken hook, and either alone would satisfy a one-clause probe while leaving
      the CI-only-enforcement gap OMN-15241 exists to close. Third clause asserts the ci.yml SKIP list
      carries reject-required-check-skip-vector, the duplicate-invocation removal specific to this repo's
      PR. Read over the live GitHub contents API against the product repo -- it reads product source,
      not this receipt or this contract, so it is falsifiable by construction. Falsifiability PROVEN by
      execution, not asserted: the identical chain exits 0 at 9e9ae927 and exits 1 at onex_change_control@dev
      (the merge base), where neither artifact exists. Authored by an independent verifier who did not
      build this PR (no-self-evidence rule); read-only, no mutation of any kind.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/onex_change_control/contents/.pre-commit-hooks/reject-required-check-skip-vector.sh?ref=9e9ae927c83ea9cb2d16600358a47dadc9e2ae16'
          --jq .content | base64 -d | grep -q 'validate_no_required_check_skip_vectors.py' && gh api 'repos/OmniNode-ai/onex_change_control/contents/.pre-commit-config.yaml?ref=9e9ae927c83ea9cb2d16600358a47dadc9e2ae16'
          --jq .content | base64 -d | grep -q 'id: reject-required-check-skip-vector' && gh api 'repos/OmniNode-ai/onex_change_control/contents/.github/workflows/ci.yml?ref=9e9ae927c83ea9cb2d16600358a47dadc9e2ae16'
          --jq .content | base64 -d | grep -q 'SKIP=check-receipt-honesty,check-receipt-hardening,reject-required-check-skip-vector'
  - id: "dod-omn-15241-seeded-red-run-30269831694"
    description: >-
      Independent re-verification of the seeded-RED proof OMN-15241 cites as the finding's ground truth.
      Asserts, against the live GitHub Actions API, that omnimarket run 30269831694 (throwaway PR #1914,
      'Required-Check Skip-Vector Guard' workflow, job 'required-check-skip-guard / check-skip-vectors',
      head e8235fba) concluded FAILURE on the seeded vector-5 violation, and that the paired clean-tree
      control run 30264429868 concluded SUCCESS. The conjunction is what makes it evidence: a RED alone
      could be an unrelated breakage, and a GREEN alone proves nothing. Exit status comes from `test`
      comparisons on the API-returned conclusions, not from a successful API read, so an API that returns
      anything other than the asserted conclusions goes RED. Read-only; no mutation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api repos/OmniNode-ai/omnimarket/actions/runs/30269831694 --jq .conclusion)" = "failure"
          && test "$(gh api repos/OmniNode-ai/omnimarket/actions/runs/30264429868 --jq .conclusion)" =
          "success"
  - id: "occ-self-bind-pr-5171"
    description: >-
      OCC PR #5171 -- self-bind for the OMN-15241 independent evidence mint (OMN-14650). Binds the four
      product-PR content probes and the seeded-RED re-verification recorded in this contract to the OCC
      PR that carries them.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 5171 --repo OmniNode-ai/onex_change_control --json number,state
  - id: "dod-OmniNode-ai-omnidash-pr-273"
    description: "PR #273 on OmniNode-ai/omnidash — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnidash-pr-273-ci"
    description: "PR #273 on OmniNode-ai/omnidash — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5175"
    description: "OCC companion PR #5175 — self-bind for OMN-15241 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
