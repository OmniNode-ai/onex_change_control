---
schema_version: "1.0.0"
ticket_id: "OMN-10839"
title: "Fix prune-worktrees.sh bash 3.2 compatibility"
summary: >
  Binds omniclaude PR #1563 to central OCC evidence. prune-worktrees.sh used mapfile and BASH_REMATCH
  (bash 4+ only); replaced with bash 3.2-compatible equivalents so the script works on macOS.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omniclaude PR #1563 is open with bash 3.2-compatible prune-worktrees.sh"
    command: "gh pr view 1563 --repo OmniNode-ai/omniclaude"
  - kind: "manual"
    description: "/bin/bash -n scripts/prune-worktrees.sh passes on macOS bash 3.2.57"
    command: "/bin/bash -n scripts/prune-worktrees.sh"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-open"
    description: "omniclaude PR #1563 is open for OMN-10839 bash 3.2 compat fix"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1563 --repo OmniNode-ai/omniclaude --json state -q '.state' | grep -q
          'OPEN'"
  - id: "dod-syntax-check"
    description: "/bin/bash -n scripts/prune-worktrees.sh passes on macOS bash 3.2"
    source: "manual"
    checks:
      - check_type: "behavior_proven"
        check_value: "Verified: /bin/bash -n scripts/prune-worktrees.sh exits 0 on macOS bash 3.2.57"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for this OCC PR #931."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 931 --repo OmniNode-ai/onex_change_control --json state,baseRefName --jq
          '.state + \":\" + .baseRefName' | grep -q 'OPEN:main'"
