---
schema_version: "1.0.0"
ticket_id: "OMN-16999"
title: "Autobind OCC evidence for OMN-16999"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3125.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3125 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3125 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. The original generated proof remains bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`; the ticket-worktree correction is appended as `dod-occ-diff-derived-behavior-proof-worktree-cwd`, executed at cwd `${OMNI_HOME}/omni_worktrees/OMN-16999/omnibase_infra`.
    command: "uv run pytest tests/unit/cli/test_cli_delegate.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3125"
    description: "PR #3125 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/cli/cli_delegate.py?ref=0358d193d58dd13a59aa3ce28fb8f914812da04f --jq '.content' | base64 -d | grep -c 'def _atomic_write_text'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3125/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_infra/cli/cli_delegate.py'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3125 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/cli/test_cli_delegate.py -q"
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-occ-diff-derived-behavior-proof-worktree-cwd"
    description: "Supersedes the proof cwd for OMN-16999 by binding the behavior proof to the ticket worktree,
      not the composed canonical CLI venv."
    source: "manual"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/cli/test_cli_delegate.py -q"
        cwd: "${OMNI_HOME}/omni_worktrees/OMN-16999/omnibase_infra"
  - id: "occ-self-bind-pr-7989"
    description: "Binds OMN-16999 to OCC companion PR #7989 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7989 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-8334"
    description: "Binds OMN-16999 to OCC companion PR #8334 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8334 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-8332"
    description: >-
      OCC self-binding receipt for PR #8332, the in-repo evidence PR carrying this contract. Binds OMN-16999
      to the OCC PR by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8332 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3229"
    description: "PR #3229 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/cli/cli_delegate.py?ref=ef660d30122c1e4c5913326eab3a957fc6317dd6 --jq '.content' | base64 -d | grep -c 'def _delegation_result'
  - id: "occ-self-bind-pr-8339"
    description: "Binds OMN-16999 to OCC companion PR #8339 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8339 --repo OmniNode-ai/onex_change_control --json number,state"
