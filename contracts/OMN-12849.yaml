---
schema_version: "1.0.0"
ticket_id: "OMN-12849"
title: "Preserve SEA inference endpoint authority"
summary: >-
  Fixes SEA inference intent publishing so the model-registry / routing-authority chat-completions endpoint
  is passed through unchanged. This prevents coder-tier requests from being stripped to a bare origin
  that the delegation inference effect posts verbatim, producing HTTP 404s.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr-2375"
    description: >-
      onex_change_control PR for OMN-12849 carries the central contract and receipt evidence for the SEA
      endpoint-authority fix.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12849/dod-occ-pr-2375/command.yaml"
  - id: "dod-source-pr-222"
    description: >-
      onex-self-extending-agent PR #222 carries the SEA endpoint-authority fix for OMN-12849.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12849/dod-source-pr-222/command.yaml"
  - id: "dod-focused-verification"
    description: >-
      Focused local lint, typecheck, and SEA inference/consumer tests passed for the changed endpoint-authority
      files.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12849/dod-focused-verification/command.yaml"
  - id: "dod-endpoint-authority-regression"
    description: >-
      Regression evidence proves SEA preserves complete local and cloud chat-completions URLs in ModelInferenceIntent.base_url
      instead of stripping them to provider origins.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12849/dod-endpoint-authority-regression/command.yaml"
