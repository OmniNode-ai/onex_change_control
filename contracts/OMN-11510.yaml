---
schema_version: "1.0.0"
ticket_id: "OMN-11510"
title: "Add regression fixture baseline"
summary: >
  Add deterministic regression fixture generation, display reporting, and a --regression CLI path for
  the onex-self-extending-agent hackathon stretch goal workflow.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "tests"
    description: "Regression fixture unit tests pass at the recorded PR head."
    command: "uv run pytest tests/unit/test_regression_fixtures.py -q"
  - kind: "manual"
    description: "onex-self-extending-agent PR #93 is open against main at the recorded head SHA."
    command: "gh pr view 93 --repo OmniNode-ai/onex-self-extending-agent --json number,url,headRefOid,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-93"
    description: "onex-self-extending-agent PR #93 carries OMN-11510 regression fixture support at the
      recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11510/dod-product-pr-93/command.yaml"
          && grep -q '^pr_number: 93$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11510/dod-product-pr-93/command.yaml"
          && grep -q '^commit_sha: "e49554d72130ec899f56cf994a3335c3bf1e08cc"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11510/dod-product-pr-93/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11510 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11510/dod-occ-self/command.yaml"
          && grep -q '^ticket_id: "OMN-11510"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11510/dod-occ-self/command.yaml"
