---
schema_version: 1.0.0
ticket_id: OMN-14681
title: Shift deploy-gate deploy-scope DoD left into pre-push parity
summary: Central OCC evidence for omnibase_infra#2345. The product PR adds a pre-push deploy-scope DoD
  parity hook for omnibase_infra, with tests proving deploy-scope detection imports the canonical hosted
  deploy-gate validator and catches missing deploy evidence locally. Exact product head af269f85cfb7302067260546c7376d4abaf25a9e.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #2345 product head is exact for OMN-14681 evidence.'
    command: 'PRODUCT_PR_NUMBER=2345; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=af269f85cfb7302067260546c7376d4abaf25a9e;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnibase_infra-pr-2345
    description: 'PR #2345 on OmniNode-ai/omnibase_infra exists and is bound to OMN-14681 at exact product
      head af269f85cfb7302067260546c7376d4abaf25a9e.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2345; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=af269f85cfb7302067260546c7376d4abaf25a9e;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnibase_infra-pr-2345-diff
    description: 'PR #2345 on OmniNode-ai/omnibase_infra touches the expected implementation and regression
      test files for OMN-14681.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2345; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx ".pre-commit-config.yaml"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "scripts/ci/check_deploy_scope_dod.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "tests/ci/test_check_deploy_scope_dod.py"'
  - id: dod-omnibase_infra-pr-2345-local-verification
    description: Product PR body records local verification evidence for OMN-14681.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2345; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "prepush-deploy-scope-dod"
          && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "840
          passed / 1 skipped"'
  - id: dod-omnibase_infra-pr-2345-deploy
    description: Deploy-readiness assessment for OMN-14681. The PR adds local CI/pre-push validation and
      tests only; the product PR body states its own diff touches no runtime paths and hosted deploy-gate
      is SKIP for this PR. No live deploy, restart, .201, Kafka, database, or production mutation was
      performed by this OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14681/dod-omnibase_infra-pr-2345-deploy/command.yaml
          && grep -qi ''no live deploy, restart, .201, Kafka, database, or production mutation'' drift/dod_receipts/OMN-14681/dod-omnibase_infra-pr-2345-deploy/command.yaml'
  - id: occ-self-bind-pr-4425
    description: 'OCC PR #4425 on OmniNode-ai/onex_change_control self-binding proof for OMN-14681 evidence.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
