---
schema_version: "1.0.0"
ticket_id: OMN-10135
title: "Cross-CLI Bridge Phase 1"
summary: "omniclaude PR #1607 implements explicit Cross-CLI delegation dispatch for Claude, OpenCode,
  and Codex."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: tests
    description: "omniclaude PR #1607 carries focused Cross-CLI invoker, dispatch, outcome, and sandbox
      config tests."
    command: "gh pr view 1607 --repo OmniNode-ai/omniclaude --json state,headRefOid"
  - kind: manual
    description: "Runtime-adjacent delegation orchestrator changes have a pre-merge deploy verification
      surface without live runtime mutation."
    command: "find drift/dod_receipts/OMN-10135 -type f -name command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-omniclaude-pr-1607
    description: "omniclaude PR #1607 carries the Cross-CLI invoker, explicit dispatch entry point, CLI
      result fields, and Codex sandbox contract config."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10135/dod-omniclaude-pr-1607/command.yaml
          && grep -q '^pr_number: 1607$' drift/dod_receipts/OMN-10135/dod-omniclaude-pr-1607/command.yaml
          && grep -q '^commit_sha: b4891efc155f5ef094042cc6bd2e20614403da73$' drift/dod_receipts/OMN-10135/dod-omniclaude-pr-1607/command.yaml"
  - id: dod-deploy-assessment
    description: "Deploy DoD evidence: runtime-adjacent delegation orchestrator changes are pre-merge
      test verified; no live deploy, restart, Kafka mutation, database mutation, or .201 mutation was
      performed."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10135/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-10135/dod-deploy-assessment/command.yaml"
  - id: dod-occ-pr-1220
    description: "onex_change_control PR #1220 carries the central OMN-10135 contract and receipts."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10135/dod-occ-pr-1220/command.yaml
          && grep -q '^pr_number: 1220$' drift/dod_receipts/OMN-10135/dod-occ-pr-1220/command.yaml"
