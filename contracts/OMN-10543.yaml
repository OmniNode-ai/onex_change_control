---
schema_version: "1.0.0"
ticket_id: "OMN-10543"
title: "ticketing_triage emits complete orphan and stale ticket enumerations"
summary: "Bind the omniclaude ticketing_triage skill fix that removes the self-imposed 100-record orphan
  sample cap and requires emitted report lists to match their summary counters."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "OMN-10543 regression tests pass on omniclaude PR #1513."
    command: "cd /Users/jonah/Code/omni_worktrees/OMN-10543/omniclaude && uv run pytest tests/skills/test_ticketing_triage_orphan_enumeration.py
      -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "ticketing_triage orphan enumeration regression tests pass on omniclaude PR #1513 at
      commit d09fdb56."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10543/dod-001/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-001\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10543/dod-001/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10543\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10543/dod-001/command.yaml\"
          && grep -q '^commit_sha: \"d09fdb56824c986a815e0f70e286ad1d4fdb4914\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10543/dod-001/command.yaml\"
          && grep -q '^pr_number: 1513$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10543/dod-001/command.yaml\""
  - id: "dod-deploy"
    description: "No runtime deploy required - change is skill guidance plus regression tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10543/dod-deploy/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-deploy\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10543/dod-deploy/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10543\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10543/dod-deploy/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carrying the OMN-10543 contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10543/dod-occ-pr/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-occ-pr\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10543/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10543\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10543/dod-occ-pr/command.yaml\""
