---
schema_version: "1.0.0"
ticket_id: "OMN-11158"
title: "Flip enum governance hook to blocking in omnibase_core"
summary: >
  Central OCC evidence for the OMN-11158 slice of omnibase_core PR #1106, which makes enum governance
  blocking and scopes validation to source files.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Enum governance validation passes for src/."
    command: "uv run python -m omnibase_core.validation.checker_enum_governance src/"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-enum-governance-blocking"
    description: "Enum governance validation passes after the hook is made blocking."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11158/dod-enum-governance-blocking/command.yaml\"
          && grep -q '^exit_code: 0$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11158/dod-enum-governance-blocking/command.yaml\""
