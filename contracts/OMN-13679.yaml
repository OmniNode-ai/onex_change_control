---
schema_version: "1.0.0"
ticket_id: "OMN-13679"
title: "WS-5 Wave 5 multi-parameter integration tests — Linear/ticketing/DoD COMPUTE"
summary: >-
  omnimarket PR #1478 adds real multi-parameter integration tests (full in-process handler passes, not
  smoke and not SKILL.md-shape grep) for six Wave 5 nodes: node_create_ticket, node_plan_to_tickets, node_linear_triage,
  node_dod_verify, node_dod_sweep_orchestrator, and node_ticket_research_enrichment_compute. Each test
  parametrizes at least three distinct param sets and carries at least one negative control that must
  produce a finding. All external I/O (Linear, GitHub, the knowledge-context assembler, the gh subprocess
  boundary) is satisfied by constructor-injected mock collaborators; nothing is monkeypatched. The change
  is test-only — no runtime handler, model, contract, topic, schema, migration, or .201/.200 runtime state
  is mutated.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >-
      The six Wave 5 node integration test directories pass in-process on the EventBusInmemory / direct
      handler path with no live Kafka, Linear, or .201.
    command: "uv run pytest tests/integration/node_create_ticket tests/integration/node_plan_to_tickets
      tests/integration/node_linear_triage tests/integration/node_dod_verify tests/integration/node_dod_sweep_orchestrator
      tests/integration/node_ticket_research_enrichment_compute -q"
  - kind: "tests"
    description: >-
      Full non-Kafka omnimarket suite passed locally before opening PR #1478 (9927 passed, 46 skipped).
    command: "uv run pytest -m 'not kafka'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1478"
    description: >-
      omnimarket PR #1478 carries the Wave 5 multi-parameter integration tests for OMN-13679 at head commit
      ec8403c711cebf4217138ddcea6927aa4c7b5ea8.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13679/dod-omnimarket-pr-1478/command.yaml
          && grep -q '^pr_number: 1478$' drift/dod_receipts/OMN-13679/dod-omnimarket-pr-1478/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: PR #1478 is test-only and adds no runtime behavior, so no live deploy, runtime
      restart, topic mutation, schema migration, database migration, or .201/.200 mutation is performed
      by this evidence PR or by merging #1478.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13679/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-13679/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-3229"
    description: >-
      Self-binding OCC eligibility receipt for onex_change_control PR #3229, which carries this contract
      and receipts onto dev so downstream omnimarket PR #1478 can cite Evidence-Source: OCC#3229.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13679/dod-occ-pr-3229/command.yaml
          && grep -q '^pr_number: 3229$' drift/dod_receipts/OMN-13679/dod-occ-pr-3229/command.yaml"
