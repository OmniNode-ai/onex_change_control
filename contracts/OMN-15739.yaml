---
schema_version: "1.0.0"
ticket_id: "OMN-15739"
title: "[DESIGN-PROPOSED] Customer cloud connectivity architecture — attach ingress replaces the bastion
  path"
summary: >-
  DoD contract for OMN-15739, a DESIGN-DECISION ticket with no product PR of its own. Its own dod_evidence
  section fixes proof_class as code-only and states the ticket must never be closed on the basis of the
  ingress being built — that is OMN-12908's evidence, not this one's — so nothing below binds ingress
  runtime behaviour. Two of the five acceptance criteria are not evidence items here and are recorded
  rather than faked. AC (a), the operator reviewing the brief and ruling on the four §6.4 open questions,
  is not independently observable from any machine surface and is subsumed by AC (b): a recorded ADOPT
  ruling presupposes the review. AC (e), that no build work started from this ticket before the ruling,
  is a negative process constraint with no falsifiable probe; verified by hand 2026-08-16 — every PR a
  search surfaces for this ticket id is an OMN-15740 / OMN-15743 / OMN-15750 PR that merely cites the
  brief, none is implemented against OMN-15739. Of the three remaining criteria, AC (c) and the first
  half of AC (d) are satisfied and bound below to merged omnibase_infra PRs; AC (b) and the second half
  of AC (d) are NOT done and are declared pending with their blocking conditions.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "docs"
    description: "AC (b) second half: the brief at docs/design/2026-08-08-customer-cloud-connectivity-design.md
      on omni_home branch jonah/docs-omni-home-refresh-20260630 flips its status line from DESIGN-PROPOSED
      to APPROVED in place. Read live 2026-08-16 — still DESIGN-PROPOSED, so this is outstanding."
    command: "git show origin/jonah/docs-omni-home-refresh-20260630:docs/design/2026-08-08-customer-cloud-connectivity-design.md
      | grep -m1 'Status:'"
  - kind: "manual"
    description: "AC (c) and AC (d) first half: the Phase 0 lane (OMN-15740..15744) and the Phase 1 attach-ingress
      slice (OMN-15750..15754) exist as tickets under the OMN-15690 remediation lane and epic OMN-12908
      respectively"
    command: "gh pr list --repo OmniNode-ai/omnibase_infra --state merged --search 'OMN-1574 OMN-1575'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-15739-ac-c-phase0-lane-real"
    description: >-
      AC (c). Phase 0 (P1 path-verifying healthcheck, P2 reconnect supervision, P3 link-health, plus the
      IaC-owned SG rule) had to be ticketed against OMN-15690's remediation lane and ship regardless of
      topology. Asserted in a strictly stronger form than "tickets exist": four of the five Phase-0 gateway
      tickets have MERGED omnibase_infra PRs, each pinned at its exact merge commit — G0 OMN-15740 (#2689),
      G1 OMN-15741 (#2690, the P1 canary healthcheck), G2 OMN-15742 (#2688, the P2 bounded-backoff DEGRADED
      state), G4 OMN-15743 (#2687). Honest residual, stated rather than hidden: the fifth, OMN-15744,
      was an avowed stopgap (extend bastion extra_hosts to b-3..b-6) whose PR #2686 was CLOSED unmerged
      because G4/OMN-15743 — which did land — supersedes it outright. AC (c) asks that Phase 0 be ticketed
      and not wait on the ingress; that holds.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 2690 --repo OmniNode-ai/omnibase_infra --json state,mergeCommit,title,files --jq 'if (.state == "MERGED" and .mergeCommit.oid == "9ee313b69f705c1bf6d2e61657e2f452df781a32" and (.title | test("OMN-15741")) and ((.files | length) > 0)) then "true" else error("unexpected #2690 state, merge commit, or title") end'
      - check_type: "command"
        check_value: |-
          gh pr view 2688 --repo OmniNode-ai/omnibase_infra --json state,mergeCommit,title,files --jq 'if (.state == "MERGED" and .mergeCommit.oid == "315de4d5276cad20c5f0f17d29fe214c6e1fa89f" and (.title | test("OMN-15742")) and ((.files | length) > 0)) then "true" else error("unexpected #2688 state, merge commit, or title") end'
      - check_type: "command"
        check_value: |-
          gh pr view 2689 --repo OmniNode-ai/omnibase_infra --json state,mergeCommit,title,files --jq 'if (.state == "MERGED" and .mergeCommit.oid == "642e60a87b2d065012940fc51e469afb65014334" and (.title | test("OMN-15740")) and ((.files | length) > 0)) then "true" else error("unexpected #2689 state, merge commit, or title") end'
      - check_type: "command"
        check_value: |-
          gh pr view 2687 --repo OmniNode-ai/omnibase_infra --json state,mergeCommit,title,files --jq 'if (.state == "MERGED" and .mergeCommit.oid == "4ca5ea10d426ecb5062d159e8fdbe363104ca503" and (.title | test("OMN-15743")) and ((.files | length) > 0)) then "true" else error("unexpected #2687 state, merge commit, or title") end'
  - id: "dod-15739-ac-d-phase1-ticketed"
    description: >-
      AC (d), first half. Phase 1 of the adopted recommendation had to be ticketed under epic OMN-12908.
      Bound to the slice's load-bearing ticket OMN-15750 (attach ingress node node_gateway_attach_effect,
      G6) via its MERGED omnibase_infra PR #2694 at merge commit 52d8b601c7fadb828940ac874232c2544d47ff34.
      This is the only Phase-1 ticket with landed work; the remaining slice tickets (OMN-15751 house-tenant
      provisioning, OMN-15752 edge-side .201 config, OMN-15753 end-to-end slice proof, OMN-15754 onex-dev
      deploy) exist and are open. AC (d) requires them ticketed, not landed — landing them is OMN-12908's
      evidence, which this ticket's own dod_evidence section explicitly forbids claiming here.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 2694 --repo OmniNode-ai/omnibase_infra --json state,mergeCommit,title,files --jq 'if (.state == "MERGED" and .mergeCommit.oid == "52d8b601c7fadb828940ac874232c2544d47ff34" and (.title | test("OMN-15750")) and ((.files | length) > 0)) then "true" else error("unexpected #2694 state, merge commit, or title") end'
  - id: "dod-15739-ac-b-brief-status-flip"
    description: >-
      AC (b), PENDING and NOT DONE. On ADOPT the brief's status line must flip from DESIGN-PROPOSED to
      APPROVED in place on the docs branch. Read live 2026-08-16 at origin/jonah/docs-omni-home-refresh-20260630:
      line 3 of docs/design/2026-08-08-customer-cloud-connectivity-design.md still reads "Status: DESIGN-PROPOSED
      (build gated on operator architecture review)". The ruling itself was recorded on the Linear ticket
      on 2026-08-08 (ADOPT, first slice of recommendation A), so the ruling half of AC (b) holds and only
      the in-place doc flip is outstanding. BLOCKING CONDITION: a one-line edit on that branch; nothing
      external gates it. Declared receipt-backed rather than as a gh api contents probe because omni_home
      is a PRIVATE repo the OCC compliance runner's token cannot resolve — that shape 404s hard and leaves
      a permanent red advisory (the OMN-15655 / OCC#6056 class), which is worse than no check at all.
    source: "manual"
    status: "pending"
    evidence_artifact: "drift/dod_receipts/OMN-15739/dod-15739-ac-b-brief-status-flip/command.yaml"
    checks:
      - check_type: "command"
        check_value: |-
          grep -qF 'brief_status_line=APPROVED' drift/dod_receipts/OMN-15739/dod-15739-ac-b-brief-status-flip/command.yaml
  - id: "dod-15739-ac-d-14323-reconciliation"
    description: >-
      AC (d), second half, PENDING and NOT DONE. AC (d) requires OMN-14323's scope to be reconciled against
      the decision that the AWS_MSK_IAM leg moves server-side to the ingress. The ticket's own acceptance
      section already records the honest state: that reconciliation was deferred to Phase 2, is flagged
      in OMN-15750's out-of-scope section, and is "not yet separately ticketed". Verified 2026-08-16 —
      the body of #2694, the PR that landed OMN-15750, mentions OMN-14323 zero times, so the flag is not
      even carried on the landed artefact. BLOCKING CONDITION: a Phase-2 scope decision on the server-side
      MSK-IAM leg, plus either an OMN-14323 scope edit or a successor ticket. Declared separately from
      the first half of AC (d) so a satisfied Phase-1-ticketed leg cannot carry an unsatisfied reconciliation
      leg through on one green item.
    source: "manual"
    status: "pending"
    evidence_artifact: "drift/dod_receipts/OMN-15739/dod-15739-ac-d-14323-reconciliation/command.yaml"
    checks:
      - check_type: "command"
        check_value: |-
          grep -qF 'omn_14323_scope_reconciled=true' drift/dod_receipts/OMN-15739/dod-15739-ac-d-14323-reconciliation/command.yaml
