---
schema_version: "1.0.0"
ticket_id: OMN-2863
title: 'Stage 4c: Migrate rate limiter to Valkey'
summary: "Stage 4c: Migrate rate limiter to Valkey"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}"
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json mergedAt -q .mergedAt --repo ${REPO}"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --watch --repo ${REPO}"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-omninode-infra-pr-633-head"
    description: "Binds omninode_infra PR #633 to exact reviewed head 4a9ee5578e47973155bbc5bcd84cfc524b150273."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-003"
    checks:
      - check_type: "command"
        check_value: "grep -F '4a9ee5578e47973155bbc5bcd84cfc524b150273' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-head/command.yaml
          && grep -F 'status: PASS' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-head/command.yaml"
  - id: "dod-omninode-infra-pr-633-scope"
    description: >-
      PR #633 scope is the Valkey-backed workflow rate limiter migration: workflow limiter/router code,
      onex-api tests/mocks, deployment manifest, manifest validation workflow, and test requirement update.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -F 'docker/onex-api/workflow_rate_limit.py' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-scope/command.yaml
          && grep -F 'docker/onex-api/routers/workflows.py' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-scope/command.yaml
          && grep -F 'k8s/onex-dev/api/deployment.yaml' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-scope/command.yaml
          && grep -F '.github/workflows/validate-deployment-manifests.yml' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-scope/command.yaml
          && grep -F 'docker/onex-api/tests/test_workflow_rate_limit.py' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-scope/command.yaml
          && grep -F 'docker/onex-api/tests/test_workflow_rate_limit_replica_seam.py' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-scope/command.yaml
          && grep -F 'status: PASS' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-scope/command.yaml"
  - id: "dod-omninode-infra-pr-633-body-gates"
    description: >-
      PR #633 body records RED->GREEN tests, hermetic Valkey/fakeredis proof, full onex-api unit suite,
      ruff, mypy, changed-file pre-commit, no live deploy/secret mutation, and Closes OMN-2863.
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-004"
    checks:
      - check_type: "command"
        check_value: "grep -F '33 passed' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-body-gates/command.yaml
          && grep -F '1494 passed, 5 skipped, 94 deselected' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-body-gates/command.yaml
          && grep -F 'pre-commit run --files <changed>' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-body-gates/command.yaml
          && grep -F 'no live mutation' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-body-gates/command.yaml
          && grep -F 'status: PASS' drift/dod_receipts/OMN-2863/dod-omninode-infra-pr-633-body-gates/command.yaml"
  - id: "occ-self-bind-pr-4697-omn2863"
    description: "OCC companion PR #4697 self-bind for the OMN-2863 omninode_infra#633 evidence companion."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4697 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
