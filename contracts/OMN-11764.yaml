---
schema_version: "1.0.0"
ticket_id: "OMN-11764"
title: "Per-table staleness thresholds for node_database_sweep"
summary: >
  Add per-table database freshness thresholds to omnimarket node_database_sweep, classify tables without
  freshness metadata as UNKNOWN, and expose tables_unknown in the sweep result.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-764"
    description: "omnimarket PR #764 implements per-table staleness thresholds, UNKNOWN table status,
      and focused database-sweep tests"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11764/dod-omnimarket-pr-764/command.yaml\"
          && grep -q '^pr_number: 764$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11764/dod-omnimarket-pr-764/command.yaml\"
          && grep -q '^commit_sha: \"13175ffceef5c88cb6d27b6252015e5d9c5c273c\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11764/dod-omnimarket-pr-764/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Record deploy-gate assessment for node_database_sweep runtime contract and handler changes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11764/dod-deploy-assessment/command.yaml\"
          && grep -qi 'deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11764/dod-deploy-assessment/command.yaml\"
          && grep -q '^pr_number: 764$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11764/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR binds OMN-11764 contract and receipts as the central evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11764/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: \"OMN-11764\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11764/dod-occ-pr/command.yaml\""
