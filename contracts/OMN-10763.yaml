---
schema_version: '1.0.0'
ticket_id: OMN-10763
title: "feat(OMN-10763): port change-aware test selection to onex_change_control"
summary: Port change-aware selective test execution from omnibase_core to onex_change_control
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: Smart test selection job passes in CI on PR
    command: "test -f scripts/ci/detect_test_paths.py"
  - kind: ci
    description: Unit tests for selection logic pass
    command: "uv run pytest tests/ci/test_detect_test_paths.py -v"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: scripts/ci/detect_test_paths.py exists in repo
    source: generated
    checks:
      - check_type: file_exists
        check_value: scripts/ci/detect_test_paths.py
  - id: dod-002
    description: scripts/ci/test_selection_adjacency.yaml exists in repo
    source: generated
    checks:
      - check_type: file_exists
        check_value: scripts/ci/test_selection_adjacency.yaml
  - id: dod-003
    description: Unit tests for test selection logic exist
    source: generated
    checks:
      - check_type: file_exists
        check_value: tests/ci/test_detect_test_paths.py
  - id: dod-004
    description: PR cites this ticket
    source: generated
    checks:
      - check_type: command
        check_value: 'gh pr view ${PR_NUMBER} --repo ${REPO} --json title,body --jq ''.title + " " + (.body
          // "")'' | grep -q ''${TICKET_ID}'''
