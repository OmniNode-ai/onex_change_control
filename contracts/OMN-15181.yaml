---
schema_version: 1.0.0
ticket_id: OMN-15181
title: Central OCC evidence for OMN-15181 prod-promotion grant landing on main
summary: Central OCC evidence for the hotfix/* PR that lands the main-built, stability-proven omnibase_infra
  prod-promotion grant (grant-68c191b0) on onex_change_control@main and removes the superseded grant-6dbeae94
  entry. This is a fresh, self-contained contract scoped to this grant landing only — the fuller OMN-15181
  deploy-agent/prod-dispatch-locality evidence chain lives in the separate contracts/OMN-15181.yaml history
  already merged on dev.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: manual
    description: Prod-promotion grant registry values, local validation, and no-raw-deploy disclosure
      for the OMN-15181 grant landing on main.
    command: test -f grants/prod_promotion_grants.yaml && grep -Fq 'grant-68c191b0-98a8-4f10-aef9-8a5479fbf230'
      grants/prod_promotion_grants.yaml
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: "dod-prod-promotion-grant-values-main-5bb0c25d"
    description: "Prod-promotion grant registry on main carries the new OMN-15181 grant (grant id, main-built
      stability-proven digest, sole platform-lead approver, absolute UTC expiry) landed from omnibase_infra
      main HEAD 5bb0c25d9b71f97796957e63ffb75d4855714ef7 (merge of PR #2481), and the superseded grant-6dbeae94-0dac-420b-94d4-66f86c433500
      entry is removed in the same PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15181/dod-prod-promotion-grant-values-main-5bb0c25d/command.yaml"
  - id: "dod-prod-promotion-grant-validation-main-5bb0c25d"
    description: "Local validation (validate-prod-promotion-grants schema logic + tests/test_prod_promotion_grants.py)
      passed against the new grant entry after removing the superseded grant-6dbeae94 entry."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15181/dod-prod-promotion-grant-validation-main-5bb0c25d/command.yaml"
  - id: "dod-prod-promotion-no-raw-deploy-main-5bb0c25d"
    description: "This grant-authoring PR states plainly that no raw .201 SSH/docker retag/recreate path
      is used, no prod mutation is performed or authorized by this PR itself, and the actual promotion
      remains gated through node_redeploy_orchestrator as a separate subsequent operator-approved action."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15181/dod-prod-promotion-no-raw-deploy-main-5bb0c25d/command.yaml"
  - id: "dod-prod-promotion-grant-companion-values-5bb0c25d"
    description: "Prod-promotion grant registry on main carries the two OMN-15181 per-service companion
      grants (grant-6cea9b85 for omninode-runtime-effects, grant-80624116 for omninode-projection-api),
      each batched under the same promotion_batch_id as grant-68c191b0 (omninode-runtime), fixing the
      single-digest-key vs per-service-command defect where the runtime-effects and projection-api ModelRebuildRequested
      commands would otherwise resolve ABSENT against the grant resolver's exact lane+digest+batch match."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15181/dod-prod-promotion-grant-companion-values-5bb0c25d/command.yaml"
  - id: "dod-prod-promotion-grant-companion-valid-5bb0c25d"
    description: "Local validation (validate-prod-promotion-grants schema logic + tests/test_prod_promotion_grants.py)
      passed against the file with the two new companion grant entries added and the existing grant-68c191b0
      entry left unmodified."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15181/dod-prod-promotion-grant-companion-valid-5bb0c25d/command.yaml"
  - id: "occ-self-bind-pr-6579"
    description: "OCC companion PR #6579 — self-bind for OMN-15181 supervised same-digest prod-promotion
      grant restart (4 grant entries under batch-be4d828d for omninode-runtime, omninode-runtime-effects,
      omnimarket-projection-api, and the prod redpanda broker, all pinned to their currently-running digests)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName,headRefOid"
  - id: "occ-self-bind-pr-6599"
    description: "OCC companion PR #6599 — self-bind for OMN-15181 supervised same-digest prod-promotion
      grant restart, replacing closed PR #6579 (identical grant content, re-authored expires_at 2026-08-17T20:00:00Z,
      hotfix/* branch shape to satisfy main-target-guard)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName,headRefOid"
