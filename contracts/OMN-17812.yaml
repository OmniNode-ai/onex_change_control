---
schema_version: "1.0.0"
ticket_id: "OMN-17812"
title: "Autobind OCC evidence for OMN-17812"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for sibling OMN-17812 product PRs.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3420 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3420 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: "uv run pytest scripts/tests/test_desired_user_profile_contract.py -q"
  - kind: "ci"
    description: "PR #1315 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1315 on OmniNode-ai/omninode_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as appended dod_evidence item `dod-occ-proof-omninode-infra-pr-1315`, executed at cwd `${OMNI_HOME}/omninode_infra`.
    command: "uv run pytest docker/onex-api/tests/test_auth_oidc.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3420"
    description: "PR #3420 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/tests/test_desired_user_profile_contract.py?ref=d7c1a9be95c824be6a6fdccf9852aeefa14389f8 --jq '.content' | base64 -d | grep -c 'def _attr'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3420/files --paginate --jq '.[].filename' | grep -cFx 'docker/keycloak/desired-user-profile.json'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3420 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest scripts/tests/test_desired_user_profile_contract.py -q"
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-9004"
    description: "Binds OMN-17812 to OCC companion PR #9004 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9004 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1315"
    description: "PR #1315 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1315 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1315/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/desired-user-profile-parity.yml'
  - id: "dod-occ-proof-omninode-infra-pr-1315"
    description: "PR #1315 on OmniNode-ai/omninode_infra — appended diff-derived behavior proof (OMN-16434)
      for the sibling OMN-17812 product PR."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest docker/onex-api/tests/test_auth_oidc.py -q"
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "occ-self-bind-pr-9003"
    description: "Binds OMN-17812 to OCC companion PR #9003 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9003 --repo OmniNode-ai/onex_change_control --json number,state"
