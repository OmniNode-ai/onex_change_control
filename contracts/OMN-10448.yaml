---
schema_version: "1.0.0"
ticket_id: OMN-10448
title: "Defer registration dispatcher wiring to generic auto-wiring"
summary: "Simplify ServiceRegistration.wire_dispatchers so the registration plugin no longer invokes explicit
  dispatcher wiring, and pin the ownership invariant with unit, integration, and AST guards."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "topics"
evidence_requirements:
  - kind: "manual"
    description: "Registration dispatcher deferral tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10448/dod-registration-dispatcher-tests/command.yaml"
  - kind: "manual"
    description: "Touched plugin, auto-wiring, and tests pass ruff"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10448/dod-lint/command.yaml"
  - kind: "manual"
    description: "Repo-local ticket contract validates"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10448/dod-contract-validate/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-registration-dispatcher-tests"
    description: "Registration dispatcher deferral tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10448/dod-registration-dispatcher-tests/command.yaml
          && grep -q '^ticket_id: OMN-10448$' drift/dod_receipts/OMN-10448/dod-registration-dispatcher-tests/command.yaml
          && grep -q '^evidence_item_id: dod-registration-dispatcher-tests$' drift/dod_receipts/OMN-10448/dod-registration-dispatcher-tests/command.yaml"
  - id: "dod-lint"
    description: "Touched plugin, auto-wiring, and tests pass ruff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10448/dod-lint/command.yaml && grep
          -q '^ticket_id: OMN-10448$' drift/dod_receipts/OMN-10448/dod-lint/command.yaml && grep -q '^evidence_item_id:
          dod-lint$' drift/dod_receipts/OMN-10448/dod-lint/command.yaml"
  - id: "dod-contract-validate"
    description: "Repo-local ticket contract validates"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10448/dod-contract-validate/command.yaml
          && grep -q '^ticket_id: OMN-10448$' drift/dod_receipts/OMN-10448/dod-contract-validate/command.yaml
          && grep -q '^evidence_item_id: dod-contract-validate$' drift/dod_receipts/OMN-10448/dod-contract-validate/command.yaml"
