---
schema_version: "1.0.0"
ticket_id: "OMN-14265"
title: "chore: promote omniclaude dev→main (OMN-14260 deploy-gate revert, unblocks org-wide deploy-gate)"
summary: >-
  Promotion-train contract for omniclaude dev->main promotion PR #1885 (promotion branch tree equals origin/dev,
  dev-head 1febe5e7b). Second promotion in this chain (follows OMN-14249). Promotes 3 new commits on top
  of the already-promoted main tip (3650658b3): #1883 (contracts/ records) and #1884 (OMN-14260, reverts
  the nested occ-preflight job that #1879 introduced into deploy-gate-reusable.yml, which caused startup_failure
  on the required deploy-gate check for every caller repo org-wide). Urgent — callers resolve deploy-gate-reusable.yml@main,
  so this promotion is required to actually restore caller-side deploy-gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-promotion-pr"
    description: >-
      omniclaude dev->main promotion PR #1885 (promotion branch promotion/omn-14265-omniclaude-dev-to-main,
      dev-head 1febe5e7b) promotes the OMN-14260 deploy-gate-reusable revert and the OMN-14249 contracts-records
      commit to main.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14265/dod-omniclaude-promotion-pr/command.yaml"
  - id: "dod-omniclaude-deploy-evidence"
    description: >-
      Deploy-gate evidence for omniclaude dev->main promotion PR #1885. The promotion touches .github/workflows/deploy-gate-reusable.yml
      (a CI workflow file, not a runtime src path); this central OCC deploy receipt satisfies the downstream
      deploy evidence gate without a repository-local bypass.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14265/dod-omniclaude-deploy-evidence/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-14265 promotion contract and
      its PASS receipts for the omniclaude dev->main promotion.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14265/dod-occ-self/command.yaml"
