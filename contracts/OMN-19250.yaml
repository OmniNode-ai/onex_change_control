---
schema_version: "1.0.0"
ticket_id: "OMN-19250"
title: "Hand-authored OCC evidence for OMN-19250"
summary: >
  OCC contract hand-authored (occ-autobind DECLINED: skip:NO_RED_DERIVABLE_CHECK, OMN-15247) for OmniNode-ai/omnibase_infra
  PR #4003 -- repo copy of vllm-gpu0-qwen38-27b.service adds the live .201 --speculative-config MTP flag
  to its ExecStart so a reinstall from the repo no longer silently disables MTP.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #4003 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "manual"
    description: |-
      Content-bound proof for PR #4003 on OmniNode-ai/omnibase_infra: the repo copy of
      deploy/systemd/vllm-gpu0-qwen38-27b.service carries the --speculative-config MTP flag
      at the PR head sha, and did not at the PR base sha (negative control).
    command: |-
      gh api "repos/OmniNode-ai/omnibase_infra/contents/deploy/systemd/vllm-gpu0-qwen38-27b.service?ref=d296c9f6ed9bcc2a039550a5891673a21bd82234" --jq '.content' | base64 -d | grep -c '"method":"mtp","num_speculative_tokens":2'
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-4003"
    description: "PR #4003 on OmniNode-ai/omnibase_infra -- hand-authored content-bound evidence (OMN-15247)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnibase_infra/contents/deploy/systemd/vllm-gpu0-qwen38-27b.service?ref=d296c9f6ed9bcc2a039550a5891673a21bd82234" --jq '.content' | base64 -d | grep -c '"method":"mtp","num_speculative_tokens":2'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/4003/files --paginate --jq '.[].filename' | grep -cFx 'deploy/systemd/vllm-gpu0-qwen38-27b.service'
  - id: "occ-self-bind-pr-10931"
    description: "Binds OMN-19250 to OCC companion PR #10931 (self-bind)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 10931 --repo OmniNode-ai/onex_change_control --json number,state"
