---
schema_version: "1.0.0"
ticket_id: "OMN-13162"
title: "Bind omniintelligence Dependabot actions-group bumps (#705, #739) to OCC evidence"
summary: >
  Bind omniintelligence Dependabot PR #705 (chore(deps): bump the actions group with 2 updates — actions/checkout
  4->6 and astral-sh/setup-uv) to central OCC evidence so the Receipt Gate can evaluate it from a pinned
  Evidence-Source without any [skip-*] bypass token. The PR touches exactly one file, .github/workflows/receipt-honesty.yml
  (the last stray actions/checkout@v4 pin; the other 33 workflow uses already pin @v6, so node24 is already
  proven on this repo's runners). It is a CI-config-only change: no source, no DDL, no migration, no runtime/deploy/.201
  mutation. All functional CI is green; verify / verify (Receipt Gate) is the only blocker, which fires
  because Dependabot PRs are genuinely ticketless. Follow-up PR #739 bumps actions/cache 5->6 in .github/workflows/ci.yml
  and has the same ticketless Receipt Gate metadata failure shape.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-705-head"
    description: >-
      omniintelligence PR #705 (branch dependabot/github_actions/actions-51f4226e04, base=dev) carries
      the actions-group Dependabot bump (actions/checkout 4->6, astral-sh/setup-uv) at head 73426be0aadab661e0bad150f3e09843d4982b3d,
      touching only .github/workflows/receipt-honesty.yml.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13162/dod-omniintelligence-705-head/command.yaml\""
  - id: "dod-omniintelligence-705-nonreceipt-checks"
    description: >-
      omniintelligence PR #705 has no failing, queued, or running non-Receipt-Gate checks at the captured
      head: 30 SUCCESS, 5 SKIPPED, 0 failing/pending. verify / verify (Receipt Gate) is the only blocker.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13162/dod-omniintelligence-705-nonreceipt-checks/command.yaml\""
  - id: "dod-omniintelligence-739-head"
    description: >-
      omniintelligence PR #739 (branch dependabot/github_actions/actions-6d1c06d137, base=dev) carries
      the actions/cache Dependabot bump 5->6 at head 7c3b3fb4e17a55bd8258ea69180e2af134bc420a, touching
      only .github/workflows/ci.yml.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13162/dod-omniintelligence-739-head/command.yaml\""
  - id: "dod-omniintelligence-739-check-snapshot"
    description: >-
      omniintelligence PR #739 check snapshot at capture time: 35 SUCCESS, 1 SKIPPED, 1 FAILURE (verify
      / verify Receipt Gate), and 2 queued or running checks (pr-title / check-title, CodeQL / CodeQL
      Analysis (python)). The captured failure is the missing Evidence-Source metadata this OCC evidence
      resolves.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13162/dod-omniintelligence-739-check-snapshot/command.yaml\""
  - id: "dod-occ-pr-self"
    description: >-
      This OCC PR carries the central OMN-13162 contract and receipts that the omniintelligence PR #705
      and PR #739 Receipt Gates resolve via Evidence-Source: OCC#<pr> and Evidence-Ticket: OMN-13162.
      Contract validates against ModelTicketContract.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13162/dod-occ-pr-self/command.yaml\""
