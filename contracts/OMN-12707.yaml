---
schema_version: "1.0.0"
ticket_id: "OMN-12707"
title: "Wire sanctioned SEA delegation bus through --agent scaffold path"
summary: >-
  Central evidence for onex-self-extending-agent PR #209. The sanctioned live `--agent` path now loads
  the required delegation bus and threads it through the ADK runtime, scaffold tool closure, and DelegationExecutor,
  preventing the former fail-closed placeholder bus from being constructed after readiness. Local regression
  coverage proves the injected bus is used, and a live dev-lane proof moved beyond the placeholder-bus
  failure into downstream routing checks.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-bus-injected-through-agent-runtime"
    description: "The sanctioned live bus is loaded once and passed into the ADK runtime, scaffold tool,
      and DelegationExecutor."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12707/dod-bus-injected-through-agent-runtime/command.yaml"
          && grep -q '^pr_number: 209$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12707/dod-bus-injected-through-agent-runtime/command.yaml"
  - id: "dod-placeholder-bus-regression"
    description: "Regression coverage prevents the scaffold path from constructing a placeholder bus after
      readiness succeeds."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12707/dod-placeholder-bus-regression/command.yaml"
          && grep -q '^pr_number: 209$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12707/dod-placeholder-bus-regression/command.yaml"
  - id: "dod-local-tests"
    description: "Focused SEA regression tests, lint, and formatting passed locally."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12707/dod-local-tests/command.yaml"
          && grep -q '^pr_number: 209$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12707/dod-local-tests/command.yaml"
  - id: "dod-live-proof-progressed-downstream"
    description: "Live dev-lane SEA proof with the delegation bootstrap overlay moved past the placeholder-bus
      failure."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12707/dod-live-proof-progressed-downstream/command.yaml"
          && grep -q '^pr_number: 209$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12707/dod-live-proof-progressed-downstream/command.yaml"
  - id: "dod-dev-pr"
    description: "onex-self-extending-agent PR #209 carries the product change against dev."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12707/dod-dev-pr/command.yaml"
          && grep -q '^pr_number: 209$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12707/dod-dev-pr/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR #2195 carries the OMN-12707 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12707/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2195$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12707/dod-occ-pr/command.yaml"
