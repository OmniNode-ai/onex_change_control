---
schema_version: "1.0.0"
ticket_id: OMN-9787
summary: "Demote file_exists as sole-proof check_type in ModelDodEvidenceItem — a sole file_exists check
  is tautological (the receipt is its own evidence), so the validator must reject evidence items whose
  only check_type is file_exists with the typed error DOD_EVIDENCE_FILE_EXISTS_SOLE_CHECK"
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #935 for OMN-9787 CI checks pass (canonical ModelDodEvidenceItem lives
      in omnibase_core; validator lands there)"
    command: "gh pr checks 935 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "ModelDodEvidenceItem rejects an evidence item whose sole check_type is file_exists with
      a ValueError mentioning file_exists and the DOD_EVIDENCE_FILE_EXISTS_SOLE_CHECK token (3 unit tests:
      sole file_exists rejected, file_exists+command accepted, command-only accepted) — receipt at drift/dod_receipts/OMN-9787/dod-001/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9787/dod-001/file_exists.yaml"
      - check_type: command
        check_value: "uv run python -c \"import yaml,sys; d=yaml.safe_load(open('drift/dod_receipts/OMN-9787/dod-001/file_exists.yaml'));
          assert d['status']=='PASS' and d['exit_code']==0 and d['ticket_id']=='OMN-9787' and d['evidence_item_id']=='dod-001',
          d; print('receipt valid')\""
  - id: dod-002
    description: "Validator code path exists in canonical ModelDodEvidenceItem (src/omnibase_core/models/contracts/ticket/model_dod_evidence_item.py)
      — the reject_sole_file_exists_check @model_validator(mode='after') is present and raises DOD_EVIDENCE_FILE_EXISTS_SOLE_CHECK
      — receipt at drift/dod_receipts/OMN-9787/dod-002/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9787/dod-002/file_exists.yaml"
      - check_type: command
        check_value: "uv run python -c \"import yaml,sys; d=yaml.safe_load(open('drift/dod_receipts/OMN-9787/dod-002/file_exists.yaml'));
          assert d['status']=='PASS' and d['exit_code']==0 and 'DOD_EVIDENCE_FILE_EXISTS_SOLE_CHECK' in
          d['probe_stdout'], d; print('receipt valid')\""
  - id: dod-003
    description: "Schema-purity respected: the validator performs no I/O — no open(), Path reads, yaml.safe_load,
      requests, or os.path lookups inside the validator file. The legacy-allowlist consultation is the
      gate's job, not the model's. Receipt records the negative grep at drift/dod_receipts/OMN-9787/dod-003/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9787/dod-003/file_exists.yaml"
      - check_type: command
        check_value: "uv run python -c \"import yaml,sys; d=yaml.safe_load(open('drift/dod_receipts/OMN-9787/dod-003/file_exists.yaml'));
          assert d['status']=='PASS' and d['exit_code']==1 and d['probe_stdout']=='', d; print('receipt
          valid')\""
  - id: dod-004
    description: "Existing ModelDodEvidenceItem and ModelTicketContract test suites still pass with the
      new validator (regression guard for downstream consumers) — receipt at drift/dod_receipts/OMN-9787/dod-004/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9787/dod-004/file_exists.yaml"
      - check_type: command
        check_value: "uv run python -c \"import yaml,sys; d=yaml.safe_load(open('drift/dod_receipts/OMN-9787/dod-004/file_exists.yaml'));
          assert d['status']=='PASS' and d['exit_code']==0 and d['evidence_item_id']=='dod-004', d; print('receipt
          valid')\""
  - id: dod-005
    description: "Sweep of existing onex_change_control contracts under contracts/ produces a violation
      candidate list at docs/tracking/2026-04-26-file_exists-violation-sweep.txt so the Task 8 gate (check_dod_compliance.py)
      can seed dod_evidence_legacy_file_exists.yaml — receipt at drift/dod_receipts/OMN-9787/dod-005/"
    source: manual
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9787/dod-005/file_exists.yaml"
      - check_type: command
        check_value: "test -s docs/tracking/2026-04-26-file_exists-violation-sweep.txt && uv run python
          -c \"import yaml; d=yaml.safe_load(open('drift/dod_receipts/OMN-9787/dod-005/file_exists.yaml'));
          assert d['status']=='PASS' and d['exit_code']==0, d; print('receipt valid')\""
