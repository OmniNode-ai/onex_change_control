---
schema_version: "1.0.0"
ticket_id: "OMN-12736"
title: "ci(OMN-12736): harden migration freeze checkout timeout"
summary: >-
  Central OCC evidence for omnibase_infra PR #1882. The Migration Freeze Check merge_group path is hardened
  after run 27058639824 spent the full 5m budget in actions/checkout fetching the gh-readonly-queue ref
  and skipped the freeze script. The migration-freeze job timeout is raised from 5m to 15m, the checkout
  step is bounded at 10m with explicit shallow fetch (fetch-depth 2, fetch-tags false), and focused CI
  workflow-resilience tests cover the new settings. CI-only change: no runtime contract, node, handler,
  topic, or migration ships.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ci-workflow-resilience-tests"
    description: "Focused omnibase_infra CI workflow-resilience tests prove the migration-freeze timeout
      and shallow-checkout settings are enforced in .github/workflows."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12736/dod-ci-workflow-resilience-tests/command.yaml"
          && grep -q 'test_ci_workflow_resilience' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12736/dod-ci-workflow-resilience-tests/command.yaml"
  - id: "dod-omnibase-infra-pr-1882"
    description: "omnibase_infra PR #1882 carries the migration-freeze checkout-timeout hardening."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12736/dod-omnibase-infra-pr-1882/command.yaml"
          && grep -q '^pr_number: 1882$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12736/dod-omnibase-infra-pr-1882/command.yaml"
          && grep -q 'be6bc6e2414b90dc6922a777bbccf9cd07790f06' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12736/dod-omnibase-infra-pr-1882/command.yaml"
  - id: "dod-occ-pr-evidence"
    description: "onex_change_control PR carries the OMN-12736 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12736/dod-occ-pr-evidence/command.yaml"
