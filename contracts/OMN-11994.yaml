---
schema_version: "1.0.0"
ticket_id: "OMN-11994"
title: "Fix handler wiring text array adaptation"
summary: >
  Central OCC evidence for omnibase_infra PR #1730, which fixes the synchronous psycopg2 handler-wiring
  adapter so Postgres text[] list values pass through as native list parameters while dict values remain
  JSON-adapted.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1730"
    description: "omnibase_infra PR #1730 carries the OMN-11994 handler_wiring text[] adaptation fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11994/dod-omnibase-infra-pr-1730/command.yaml
          && grep -q '^pr_number: 1730$' drift/dod_receipts/OMN-11994/dod-omnibase-infra-pr-1730/command.yaml"
  - id: "dod-local-validation"
    description: "Focused integration coverage and lint passed for psycopg2 text[] list adaptation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11994/dod-local-validation/command.yaml
          && grep -q 'test_projection_handler_db_injection_integration.py' drift/dod_receipts/OMN-11994/dod-local-validation/command.yaml
          && grep -q 'ruff check' drift/dod_receipts/OMN-11994/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy was performed; change is source-level adapter behavior with CI/deploy
      gates expected to validate before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11994/dod-deploy-assessment/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-11994/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-1599"
    description: "onex_change_control PR #1599 carries the central OCC evidence for OMN-11994."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11994/dod-occ-pr-1599/command.yaml
          && grep -q '^pr_number: 1599$' drift/dod_receipts/OMN-11994/dod-occ-pr-1599/command.yaml"
