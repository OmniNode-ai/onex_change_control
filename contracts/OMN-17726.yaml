---
schema_version: "1.0.0"
ticket_id: "OMN-17726"
title: "Autobind OCC evidence for OMN-17726"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2280.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2280 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2280 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: "uv run pytest tests/scripts/test_prepush_host_table.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2280"
    description: "PR #2280 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/tests/scripts/test_prepush_host_table.py?ref=4cbac6cfca237a6461fcdbdec9485f3b621ffa8e --jq '.content' | base64 -d | grep -c 'def _path_entries'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2280/files --paginate --jq '.[].filename' | grep -cFx 'scripts/hooks/prepush_dispatch.sh'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2280 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/scripts/test_prepush_host_table.py -q"
        cwd: "${OMNI_HOME}/omnimarket"
