---
schema_version: "1.0.0"
ticket_id: "OMN-12869"
title: "node_linear_triage auto-close safety: flag-only mode by default"
summary: >-
  Auto-close precision on node_linear_triage is ~17%. The node must never execute closes autonomously.
  Add flag_only=True default that blocks all Linear state mutations and populates result.suppressed_closes
  with candidate-close descriptions for human review.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr"
    description: >-
      omnimarket PR carries the OMN-12869 implementation: flag_only=True default on ModelLinearTriageStartCommand,
      suppressed_closes output field, zero save_issue/save_comment calls when flag_only=True.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12869/dod-omnimarket-pr/command.yaml"
  - id: "dod-red-case-tests"
    description: >-
      4 new TestFlagOnlySafety tests prove zero Linear mutations execute with flag_only=True (default).
      All 21 linear-triage tests pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12869/dod-red-case-tests/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      OMN-12869 changes handler logic only — no new topics, no new consumers, no schema changes. The flag_only
      gate is purely additive (new parameter with True default). No runtime deploy or restart is required;
      the change takes effect on next node invocation after merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12869/dod-deploy-assessment/command.yaml"
          && grep -q 'no deploy required' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12869/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding receipt for the OCC PR carrying the OMN-12869 central contract and DoD receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12869/dod-occ-pr-self/command.yaml"
