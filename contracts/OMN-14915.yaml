---
schema_version: "1.0.0"
ticket_id: "OMN-14915"
title: "Autobind OCC evidence for OMN-14915"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1854."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1854 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1854"
    description: "PR #1854 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1854-ci"
    description: "PR #1854 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4563"
    description: "OCC companion PR #4563 — self-bind for OMN-14915 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1854-deploy-scope"
    description: "PR #1854 runtime-path deploy scope is declared for OMN-14915."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files # deploy"
  - id: "occ-self-bind-pr-4564-omn14915"
    description: "OCC companion PR #4564 — self-bind for OMN-14915 deploy evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
