---
schema_version: "1.0.0"
ticket_id: "OMN-13300"
title: "Per-topic topic_config carry at event-bus contract declaration site"
summary: >-
  Adds an additive omnibase_core contract model surface so per-topic provisioning config can be carried
  at the event_bus publish_topics / subscribe_topics declaration site. The change introduces ModelTopicProvisioningConfig,
  adds optional ModelTopicMeta.topic_config, and exports the new model without changing runtime topic
  provisioning behavior. This central OCC evidence binds OMN-13300 to omnibase_core PR #1283 and records
  that the PR has no live deploy mutation.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1283"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1283 at the current head. The PR adds ModelTopicProvisioningConfig
      and optional ModelTopicMeta.topic_config so topic provisioning configuration can be loaded from
      event_bus publish/subscribe metadata. The receipt binds by pr_number and commit_sha for downstream
      Receipt Gate resolution.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13300/dod-omnibase-core-pr-1283/command.yaml
  - id: "dod-deploy-no-live-mutation"
    description: >-
      Deploy-path evidence for OMN-13300. This is an additive Pydantic model/schema surface in omnibase_core.
      It adds no node, no Kafka topic provisioning action, no migration, no Dockerfile, and performs no
      .201 mutation or live deploy. Follow-up tickets own any runtime provisioning use.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13300/dod-deploy-no-live-mutation/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying OMN-13300 contract and receipts. Binds
      the OCC PR by pr_number so the OCC receipt-gate eligibility check can resolve a PASS receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13300/dod-occ-pr-self/command.yaml
