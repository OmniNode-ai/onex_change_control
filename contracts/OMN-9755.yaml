---
schema_version: "1.0.0"
ticket_id: OMN-9755
summary: "feat(infra): wire model_validate() in SPI default template loader — move concrete loader to
  infra layer"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "8 unit tests for load_default_handler_contract pass"
    command: "uv run pytest tests/unit/contracts/defaults/test_loader.py -v"
  - kind: ci
    description: "omnibase_infra PR #1418 CI checks pass"
    command: "gh pr checks 1418 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "src/omnibase_infra/contracts/defaults/loader.py created — concrete loader in correct
      infra layer per CLAUDE.md rule #7"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9755/dod-001/file_exists.yaml"
  - id: dod-002
    description: "load_default_handler_contract() returns ModelHandlerContract (not raw dict); 8 unit
      tests pass"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9755/dod-002/file_exists.yaml"
