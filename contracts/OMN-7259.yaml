---
schema_version: "1.0.0"
ticket_id: OMN-7259
title: 'feat: self-healing orchestration wrapper (Workstream B Phase 3)'
summary: "feat: self-healing orchestration wrapper (Workstream B Phase 3)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view --json state -q .state"
  - id: "dod-002"
    description: "PR merge vehicle is registered"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view --json mergeStateStatus,autoMergeRequest"
  - id: "dod-003"
    description: "Required checks were observed after review-fix push"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks --watch=false --required"
  - id: "dod-004"
    description: "Focused local verification passes after review fixes"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7259/dod-004/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-004\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7259/dod-004/command.yaml\"
          && grep -q 'focused tests, lint, and mypy passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7259/dod-004/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-7259 central contract and omniclaude PR #1516 receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7259/dod-occ-pr/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-occ-pr\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7259/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: \"OMN-7259\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7259/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number: 733$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7259/dod-occ-pr/command.yaml\""
