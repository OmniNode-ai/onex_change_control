---
schema_version: "1.0.0"
ticket_id: "OMN-14173"
title: "fix(OMN-14173): fail-closed prs_fixed accounting for OCC autobind"
summary: >-
  Central OCC evidence for omnimarket PR #1653. The downstream PR fixes the merge_sweep false-green defect
  where the fix-only path reported prs_fixed after routing to OCC autobind even when no pushed OCC companion
  or product Evidence-Source patch existed. The fix adds effect read-back through OccCompanionVerifier
  and gates RECEIPT_EVIDENCE_SOURCE_AUTOBIND accounting on an actual pushed companion, not on a handler
  call returning without an exception. This evidence PR performs no live deploy, runtime restart, branch-protection
  mutation, merge-queue bypass, or runner restart.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1653"
    description: >-
      omnimarket PR #1653 is open against dev at head 4083a2360e3481a08ecb70fb0449eff60d9be1d8 and carries
      the OMN-14173 merge_sweep false-green accounting fix.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14173/dod-omnimarket-pr-1653/command.yaml &&
          grep -q '^pr_number: 1653$' drift/dod_receipts/OMN-14173/dod-omnimarket-pr-1653/command.yaml
          && grep -q '^commit_sha: 4083a2360e3481a08ecb70fb0449eff60d9be1d8$' drift/dod_receipts/OMN-14173/dod-omnimarket-pr-1653/command.yaml
  - id: "dod-fail-closed-accounting-proof"
    description: >-
      The downstream PR body records local proof that the autobind arm now verifies product Evidence-Source,
      the pushed OCC PR, and the remote companion branch before incrementing prs_fixed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14173/dod-fail-closed-accounting-proof/command.yaml
          && grep -q '12271 passed' drift/dod_receipts/OMN-14173/dod-fail-closed-accounting-proof/command.yaml
          && grep -q 'occ_companion_verified' drift/dod_receipts/OMN-14173/dod-fail-closed-accounting-proof/command.yaml
  - id: "dod-change-scope"
    description: >-
      Change scope is limited to merge-sweep lifecycle handlers, the OCC companion verifier, fix-result
      model accounting, and focused tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14173/dod-change-scope/command.yaml && grep
          -q 'occ_companion_verifier.py' drift/dod_receipts/OMN-14173/dod-change-scope/command.yaml
  - id: "dod-no-live-deploy"
    description: >-
      Scope evidence: OMN-14173 is merge-sweep process code plus central evidence only. No live deploy,
      runtime restart, branch-protection mutation, merge-queue bypass, or runner restart is performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14173/dod-no-live-deploy/command.yaml && grep
          -qi 'no live deploy' drift/dod_receipts/OMN-14173/dod-no-live-deploy/command.yaml
  - id: "dod-occ-self"
    description: >-
      Self-binding receipt: this onex_change_control companion PR carries the central OMN-14173 contract
      and PASS receipts consumed by omnimarket PR #1653 via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14173/dod-occ-self/command.yaml && grep -q '^ticket_id:
          OMN-14173$' drift/dod_receipts/OMN-14173/dod-occ-self/command.yaml
