---
schema_version: "1.0.0"
ticket_id: "OMN-13747"
title: "fix(OMN-13747): move runtime build and push to hosted runner"
summary: >-
  Central OCC evidence for omnibase_infra PR #2156, which moves the runtime image build-and-push job in
  .github/workflows/build-and-push-runtime.yml from the .201 self-hosted runner selector to ubuntu-latest.
  The change targets repeated ECR docker push EOFs observed on the self-hosted runner while leaving the
  lineage guard on the existing self-hosted path.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-workflow-hosted-runner"
    description: >-
      omnibase_infra PR #2156 changes only .github/workflows/build-and-push-runtime.yml, sets the build-and-push
      job to runs-on: ubuntu-latest, adds a hosted-runner disk cleanup step before the local runtime image
      build, and leaves the lineage-guard job on the existing self-hosted selector.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13747/dod-workflow-hosted-runner/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for omnibase_infra PR #2156. The PR changes a GitHub Actions workflow used for
      runtime image build and push, but this OCC evidence repair performs no live deploy, ECR retag, runtime
      restart, .201 mutation, topic mutation, or container mutation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13747/dod-deploy-assessment/command.yaml
          && grep -q '^pr_number: 2156$' drift/dod_receipts/OMN-13747/dod-deploy-assessment/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-13747/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      onex_change_control PR carrying this OMN-13747 central contract and receipt bundle on base dev,
      binding the evidence consumed by omnibase_infra PR #2156.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13747/dod-occ-pr-self/command.yaml"
