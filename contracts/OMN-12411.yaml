---
schema_version: "1.0.0"
ticket_id: "OMN-12411"
title: "Fix omnimarket all-node native runtime dogfood routing"
summary: >
  Central OCC evidence for omnimarket PR #980. The PR adds a direct market-node runtime dogfood inventory
  and repairs local runtime routing so nested handler-routing contracts, top-level terminal events, and
  node-local model references resolve through the Onex-native runtime adapter. Final stacked remediation
  leaves no unresolved route-resolution failures: every omnimarket entry point is either routable through
  the native dispatch contract path or explicitly classified as a non-command runtime surface.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-980"
    description: "omnimarket PR #980 carries the OMN-12411 market-node dogfood routing fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12411/dod-omnimarket-pr-980/command.yaml"
  - id: "dod-runtime-dogfood"
    description: "The all-node market runtime dogfood inventory routes 271 of 290 entry points, classifies
      19 native non-command surfaces, and leaves zero failures."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12411/dod-runtime-dogfood/command.yaml"
  - id: "dod-local-checks"
    description: "Focused local runtime tests, strict mypy, and ruff checks pass on the omnimarket PR
      head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12411/dod-local-checks/command.yaml"
  - id: "dod-current-pr-head"
    description: "The central receipt was refreshed against the current omnimarket PR #980 head after
      the final OMN-12415 review-edge commit landed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12411/dod-current-pr-head/command.yaml"
  - id: "dod-deploy-gate"
    description: "Runtime-path changes in omnimarket PR #980 have an explicit deploy DoD receipt for post-merge
      runtime validation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12411/dod-deploy-gate/command.yaml
          && grep -qi deploy drift/dod_receipts/OMN-12411/dod-deploy-gate/command.yaml"
  - id: "dod-occ-pr-1836"
    description: "onex_change_control PR #1836 carries the central OMN-12411 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12411/dod-occ-pr-1836/command.yaml
          && grep -q '^pr_number: 1836$' drift/dod_receipts/OMN-12411/dod-occ-pr-1836/command.yaml"
