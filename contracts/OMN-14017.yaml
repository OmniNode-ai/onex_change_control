---
schema_version: "1.0.0"
ticket_id: "OMN-14017"
title: "Set UV_HTTP_TIMEOUT on merge_group validator gates"
summary: >-
  omnibase_infra PR #2219 fixes merge_group validator gates that were still using uv's 30-second default
  download timeout under fleet egress saturation. The code PR sets UV_HTTP_TIMEOUT=600 on the queue-blocking
  validator-runtime-profiles, url-authority-gate, and validate-validator-requirements workflows. This is
  CI workflow configuration only: no runtime deploy, restart, docker exec, rpk topic produce, AWS mutation,
  or live cluster mutation was performed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2219"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2219 sets UV_HTTP_TIMEOUT=600 on the three merge_group
      validator workflows that previously used raw uv sync paths with the 30-second default timeout.
      The PR body records the failing run signature and focused local verification.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14017/dod-omnibase-infra-pr-2219/command.yaml"
  - id: "dod-no-runtime-deploy"
    description: >-
      Deploy-gate evidence: the change is CI-workflow YAML configuration only. No live deploy, restart,
      docker exec, rpk topic produce, AWS mutation, or .201/cluster mutation was performed.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'No runtime deploy, restart, docker exec, rpk topic produce, AWS mutation'
          drift/dod_receipts/OMN-14017/dod-no-runtime-deploy/command.yaml"
  - id: "dod-occ-self"
    description: >-
      OCC self-binding: this onex_change_control PR carries the OMN-14017 central contract and PASS receipts
      that omnibase_infra PR #2219 cites via Evidence-Source.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14017/dod-occ-self/command.yaml"
