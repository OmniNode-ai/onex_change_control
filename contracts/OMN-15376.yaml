---
schema_version: "1.0.0"
ticket_id: "OMN-15376"
title: "Autobind OCC evidence for OMN-15376"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2537.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2537 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "manual"
    description: >-
      AC1 IS NOT MET AND IS NOT CLAIMED BY ANY CHECK IN THIS CONTRACT. AC1 requires reproducing the failure
      against a scratch Postgres seeded with the LIVE RDS SHAPE of llm_cost_aggregates, read back ("read
      it back; do not assume what it looks like"). No RDS shape census was ever performed: the drifted
      shape exercised by the omnibase_infra#2537 tests was RECONSTRUCTED from the live error signature
      of run 30418878385, which is inference, not a readback — the ticket's own R19b comment records exactly
      this. Compounding it, the readback AC1 asks for is no longer performable in its original form: the
      reconciling ALTER has since executed against that table, so the pre-fix drifted shape no longer
      exists in RDS to be read. Two honest resolutions, both needing an operator ruling and neither taken
      unilaterally here: (a) read back the CURRENT live shape and subtract the migration's declared columns
      — the residue is the surviving fingerprint of the pre-existing shape, which confirms or refutes
      the reconstruction; or (b) amend AC1 on the record to accept error-signature reconstruction. Until
      one lands, OMN-15376 stays In Review. Recorded 2026-07-29 by session fable-relay-0729, lane R30b.
  - kind: "manual"
    description: >-
      AC3 — EXECUTED AND PASSING, BUT NOT BINDABLE AS A dod_evidence CHECK. The node loop runs to completion:
      deploy-onex-dev run 30454432188, job 90587035530 (deploy-dev, conclusion success), prints '== Migration
      complete: applied=0 skipped=79 node_applied=0 node_skipped=83 ==' followed by '== All migrations
      complete (6/6 succeeded) =='. Probe executed 2026-07-29, rc=0 both greps: `gh api repos/OmniNode-ai/omninode_infra/actions/jobs/90587035530/logs
      | grep -q '<terminal line>'`. Why it is recorded here instead of in dod_evidence: OmniNode-ai/omninode_infra
      is a PRIVATE repository, and hosted contract compliance runs with a repo-scoped GITHUB_TOKEN that
      404s on it (proven live — OCC PR #5470 run 30465894334 job 90623400313: 'gh: Not Found (HTTP 404)'
      on exactly these probes, while every omnibase_infra/omnimarket probe in the same run succeeded).
      A check that 404s in CI is a hard merge block with no hosted-skip mechanism in contract_compliance_check.py,
      so binding AC3 as an executable check would make this repair unlandable. The gap is structural and
      affects every deploy-chain ticket whose evidence lives in omninode_infra; filed separately.
  - kind: "manual"
    description: >-
      AC4 — EXECUTED AND PASSING, BUT NOT BINDABLE AS A dod_evidence CHECK, same private-repo reason as
      the AC3 entry above. Live readback on onex-dev: run 30454432188 @c5e61311276829c7cf4fe5bb057713f6a35545bc,
      workflow 'Deploy onex-dev (SSM port-forward)', conclusion=success, created 2026-07-29T13:05:26Z.
      Probe executed 2026-07-29, rc=0: `gh api repos/OmniNode-ai/omninode_infra/actions/runs/30454432188
      --jq '.conclusion' | grep -qx success`. Separate honest limit that survives regardless of the token:
      nothing anywhere binds the migrate IMAGE used by that run back to 78b8731103a019ea90eee3164b53e882a064fe34,
      because the workflow log does not name the image tag. AC4 is proven as 'a green onex-dev run exists',
      not as 'a green onex-dev run of this fix'.
  - kind: "manual"
    description: >-
      LIVE-RDS EXECUTION EVIDENCE — executed, passing, not bindable, same private-repo reason. The reconciliation
      ran against the live RDS drifted table, not only against a seeded local scratch database: run 30452468047
      (job 90581443570, 12:38Z) prints '== Migration complete: applied=0 skipped=79 node_applied=2 node_skipped=81
      ==' — two node migrations newly applied — and the next run 30454432188 prints node_applied=0 node_skipped=83
      against the same total of 83, i.e. the node ledger advanced from 81 to 83 and then held. Probe executed
      2026-07-29, rc=0: `gh api repos/OmniNode-ai/omninode_infra/actions/jobs/90581443570/logs | grep
      -q '<terminal line>'`. Honest limit: the workflow log truncates the Job output to its last 5 lines,
      so the per-file APPLY list is NOT in this surface — this does not prove WHICH two files applied,
      only that the ledger advanced by exactly two in the window in which the shape wall cleared.
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2537"
    description: "PR #2537 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-OmniNode-ai-omnimarket-pr-1946"
    description: "PR #1946 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5421"
    description: "Binds OMN-15376 to OCC companion PR #5421 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-5425"
    description: "Binds OMN-15376 to OCC companion PR #5425 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-5424"
    description: "Binds OMN-15376 to OCC companion PR #5424 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omn-15376-execution-proof"
    description: >
      Executed on real ephemeral Postgres 16 — 7 tests covering RED on both live signatures (llm_cost_aggregates.aggregation_key
      line 64, baselines_comparisons.snapshot_id line 76) against the reconciliation-stripped variant
      of the real file, GREEN on the same seeded drifted shapes, whole-corpus fresh-vs-drifted schema
      equality (fresh-only=0 drift-only=0 over 74 unfenced migrations and 57 seeded drifted tables), fresh-path
      no-op identity, and row preservation with a named OMN-15376 data-ruling refusal on a populated drifted
      table.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-omn-15376-static-gate"
    description: >
      Always-on static gate (no database) — 122 cases asserting every declared column of every guarded
      CREATE TABLE is reconciled in the same file, with the operator fence READ FROM scripts/run-forward-migrations.sh
      rather than restated, plus a no-DROP/TRUNCATE/DELETE assertion over every reconciliation region.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-omn-15376-vendor-parity"
    description: >
      The vendored tree in omnibase_infra is byte-identical to the paired omnimarket source branch, verified
      by the canonical sync checker against the pairing declared in the PR body (Omnimarket-Source-Ref).
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-occ-evidence-admissibility-validator"
    description: >
      Hosted-accessible OCC evidence admissibility validator suite for this companion. The generated Evidence-Source
      checks are preserved for audit, but hosted contract compliance must also execute a non-self-referential
      validator probe.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  # ---------------------------------------------------------------------------
  # OMN-15376 R30b remediation — append-only (session fable-relay-0729, lane R30b).
  #
  # Why these exist: an adversarial verifier REFUTED the 2026-07-29T14:46Z Done
  # flip. None of the 11 checks above binds ANY of the ticket's four acceptance
  # criteria — 7 are `gh pr view ${PR_NUMBER}` PR-existence probes (the shape the
  # OMN-15309 predicate refuses as INSIDE_OWN_DIFF/metadata-only), and the other 4
  # all carry the byte-identical command `uv run pytest tests/test_evidence_admissibility.py -q`,
  # which node_dod_verify resolves into the OCC clone (76 OCC predicate tests,
  # ~0.13s, no database) and which passes with the entire OMN-15376 fix reverted.
  # The ticket was moved Done -> In Review at 2026-07-29T15:11Z before these were
  # authored. Every check below was EXECUTED with a real exit code (all rc=0,
  # 2026-07-29) and classified ADMISSIBLE by
  # onex_change_control.validation.evidence_admissibility.classify_evidence_item
  # under BOTH the LIVE_PROBE and LIVE_PROBE|EXECUTED_HERMETIC vocabularies before
  # being written here.
  #
  # AC1, AC3, AC4 and the live-RDS execution proof are recorded under
  # evidence_requirements, NOT here. AC1 is genuinely unmet. AC3/AC4 are executed
  # and passing but CANNOT be bound as dod_evidence checks: their evidence lives in
  # OmniNode-ai/omninode_infra, a PRIVATE repo that hosted contract compliance 404s
  # on (proven live, OCC#5470 run 30465894334). Binding them would make this repair
  # unlandable. That structural gap is filed, not papered over.
  # ---------------------------------------------------------------------------
  - id: "dod-omn-15376-ac2-reconciling-alter"
    description: >-
      AC2 — which shape is authoritative, and the fix that encodes the answer. The MIGRATION shape wins,
      materialised as an explicit reconciling ALTER rather than by dropping the guard. Read back from
      the squash-merge commit of omnibase_infra#2537 (78b8731103a019ea90eee3164b53e882a064fe34; merged=true,
      base=dev) through the GitHub contents API at a pinned ref — a repository this OCC companion does
      not touch, so the verdict cannot be decided by text this PR authors. FIX-DISCRIMINATING: the second
      check is the negative control, running the identical probe against that commit's first parent 11b460db23d40dd2ca7704d0c3e9fd89851af60c,
      where the match count is 0. Strip the reconciliation and check 1 goes RED; leave it in place and
      check 2 goes RED. This is the check the pre-remediation contract did not have.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh api -H 'Accept: application/vnd.github.raw' 'repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_cost_summary/0001_create_llm_cost_aggregates.sql?ref=78b8731103a019ea90eee3164b53e882a064fe34'
          | grep -q 'ALTER TABLE llm_cost_aggregates ADD COLUMN IF NOT EXISTS aggregation_key'"
      - check_type: "command"
        check_value: "gh api -H 'Accept: application/vnd.github.raw' 'repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_cost_summary/0001_create_llm_cost_aggregates.sql?ref=11b460db23d40dd2ca7704d0c3e9fd89851af60c'
          | grep -c 'ADD COLUMN IF NOT EXISTS aggregation_key' | grep -qx 0"
  - id: "dod-omn-15376-ac2-fix-live-on-mainline"
    description: >-
      The reconciliation is live on omnibase_infra dev now, not merely present in a historical commit
      — the same probe at ref=dev, which goes RED the moment anyone reverts it on the mainline, plus a
      lineage assertion that 78b8731103a019ea90eee3164b53e882a064fe34 is an ancestor of dev (ahead_by=0
      on the compare endpoint). Honest limit: the lineage check alone is NOT fix-discriminating (the pre-fix
      parent is also an ancestor of dev); its job is to refuse a fix that only ever existed on a dangling
      branch. The discriminating pair is the item above.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh api -H 'Accept: application/vnd.github.raw' 'repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_cost_summary/0001_create_llm_cost_aggregates.sql?ref=dev'
          | grep -q 'ALTER TABLE llm_cost_aggregates ADD COLUMN IF NOT EXISTS aggregation_key'"
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/compare/dev...78b8731103a019ea90eee3164b53e882a064fe34
          --jq '.ahead_by' | grep -qx 0"
  - id: "dod-omn-15376-execution-proof-landed"
    description: >-
      Append-only replacement for dod-omn-15376-execution-proof. The superseded entry's PROSE claims an
      ephemeral-Postgres-16 experiment (7 tests, RED on both live signatures) that its COMMAND — 'uv run
      pytest tests/test_evidence_admissibility.py -q', which node_dod_verify runs in the OCC clone — cannot
      produce; that command runs OCC's 76 admissibility-predicate tests in ~0.13s with no database, and
      passes with the whole OMN-15376 fix reverted. This entry binds what is actually provable from OCC:
      the integration module named by that prose LANDED in the merged product commit. It does NOT execute
      it — no check in this contract can, because dod_verify resolves cwd to the OCC checkout and the
      module lives in omnibase_infra. The description/command mismatch class is filed separately.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-omn-15376-execution-proof"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/commits/78b8731103a019ea90eee3164b53e882a064fe34
          --jq '.files[].filename' | grep -qx 'tests/integration/migrations/test_node_migration_shape_drift_omn15376.py'"
  - id: "dod-omn-15376-static-gate-landed"
    description: >-
      Append-only replacement for dod-omn-15376-static-gate, same defect as the item above (prose asserts
      a 122-case always-on static gate; command runs OCC's admissibility suite). Binds the always-on gate
      module's presence in the merged product commit. Execution of that gate is proven by omnibase_infra
      CI on #2537, not by this contract.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-omn-15376-static-gate"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/commits/78b8731103a019ea90eee3164b53e882a064fe34
          --jq '.files[].filename' | grep -qx 'tests/ci/test_node_migration_shape_reconciliation.py'"
  - id: "dod-omn-15376-vendor-parity-digest"
    description: >-
      Append-only replacement for dod-omn-15376-vendor-parity. The superseded entry claims byte-identity
      between the vendored omnibase_infra tree and the paired omnimarket source; its command ran OCC's
      admissibility suite instead. This entry proves the identity directly and narrowly, for the file
      this ticket is about: both the omnimarket source at merge commit 708dadaa966bbbb584e806872f1c088027285743
      (omnimarket#1946) and the omnibase_infra vendored copy at 78b8731… hash to sha256 60cad6737c615c1cd6275d5818f0b45e9d8e57bbf1d2f265b1ebba3f6dc2ea4d,
      executed 2026-07-29. Scope stated honestly: ONE file, not the whole vendored tree — the superseded
      entry's whole-tree claim remains unproven and is not re-asserted here.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-omn-15376-vendor-parity"
    checks:
      - check_type: "command"
        check_value: "gh api -H 'Accept: application/vnd.github.raw' 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_cost_summary/migrations/0001_create_llm_cost_aggregates.sql?ref=708dadaa966bbbb584e806872f1c088027285743'
          | shasum -a 256 | grep -q 60cad6737c615c1cd6275d5818f0b45e9d8e57bbf1d2f265b1ebba3f6dc2ea4d"
      - check_type: "command"
        check_value: "gh api -H 'Accept: application/vnd.github.raw' 'repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_cost_summary/0001_create_llm_cost_aggregates.sql?ref=78b8731103a019ea90eee3164b53e882a064fe34'
          | shasum -a 256 | grep -q 60cad6737c615c1cd6275d5818f0b45e9d8e57bbf1d2f265b1ebba3f6dc2ea4d"
  - id: "dod-omn-15376-pr-2537-merged-commit"
    description: >-
      Append-only replacement for dod-OmniNode-ai-omnibase_infra-pr-2537, which used the machine-generated
      'gh pr view ${PR_NUMBER}' shape. That shape is a PR-existence probe: the OMN-15309 predicate refuses
      it as metadata-only (it proves the change exists, not that anything it claims is true), and it passes
      here only because the placeholder happens to resolve. Binds the same fact through the merge COMMIT
      instead.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2537"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/commits/78b8731103a019ea90eee3164b53e882a064fe34
          --jq '.commit.message' | grep -q '(#2537)'"
  - id: "dod-omn-15376-pr-1946-merged-commit"
    description: >-
      Append-only replacement for dod-OmniNode-ai-omnimarket-pr-1946 (same PR-existence defect). Binds
      omnimarket#1946 through its merge commit 708dadaa966bbbb584e806872f1c088027285743.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-1946"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/commits/708dadaa966bbbb584e806872f1c088027285743
          --jq '.commit.message' | grep -q '(#1946)'"
  - id: "dod-omn-15376-occ-5421-merged-commit"
    description: >-
      Append-only replacement for occ-self-bind-pr-5421 (same PR-existence defect). Binds the OCC companion
      through its merge commit 2284761dedc18b26f91d8d1d09e2ae41255226bb.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-5421"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/onex_change_control/commits/2284761dedc18b26f91d8d1d09e2ae41255226bb
          --jq '.commit.message' | grep -q '(#5421)'"
  - id: "dod-omn-15376-occ-5424-merged-commit"
    description: >-
      Append-only replacement for occ-self-bind-pr-5424 (same PR-existence defect). Binds the OCC companion
      through its merge commit 9fca83aeb9c17055a3504207cbc24d6075872af8.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-5424"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/onex_change_control/commits/9fca83aeb9c17055a3504207cbc24d6075872af8
          --jq '.commit.message' | grep -q '(#5424)'"
  - id: "dod-omn-15376-occ-5425-merged-commit"
    description: >-
      Append-only replacement for occ-self-bind-pr-5425 (same PR-existence defect). Binds the OCC companion
      through its merge commit 7d04f3424cc789b42269ef59f31a0d5aed34d8ee.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-5425"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/onex_change_control/commits/7d04f3424cc789b42269ef59f31a0d5aed34d8ee
          --jq '.commit.message' | grep -q '(#5425)'"
  - id: dod-15376-infra2537-baselines-rb15391
    description: 'Append-only replacement for dod-occ-evidence-admissibility-validator (OMN-15391) --
      the fourth carrier of the surrogate command in this contract, left standing when OCC#5470 rebound
      the other three. The superseded entry''s only check was ''uv run pytest tests/test_evidence_admissibility.py
      -q''. node_dod_verify''s EvidenceCollector._infer_occ_cwd resolves that command into the OCC checkout,
      where it runs OCC''s own OMN-15309 admissibility-predicate suite (76 tests, ~0.1s) and passes identically
      whether or not the product fact it is filed under is true. This entry binds the second live signature
      named in the OMN-15376 finding, on a PUBLIC repo so every runner can execute it: the baselines_snapshots
      reconciliation is present in the guarded CREATE migration at omnibase_infra#2537''s merge commit
      78b8731103a019ea90eee3164b53e882a064fe34 and absent at its first parent 11b460db23d40dd2ca7704d0c3e9fd89851af60c.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-occ-evidence-admissibility-validator
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_baselines/0001_create_baselines_tables.sql?ref=78b8731103a019ea90eee3164b53e882a064fe34''
          | grep -q ''ALTER TABLE baselines_snapshots ADD COLUMN IF NOT EXISTS snapshot_id'''
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_baselines/0001_create_baselines_tables.sql?ref=11b460db23d40dd2ca7704d0c3e9fd89851af60c''
          | grep -c ''ALTER TABLE baselines_snapshots ADD COLUMN IF NOT EXISTS snapshot_id'' | grep -qx
          0'
  - id: dod-15376-infra2537-baselines-rb2
    description: 'Append-only replacement for dod-15376-infra2537-baselines-rb15391 (OMN-15391 round 2,
      Rule D). The superseded item''s GREEN leg is correct and is carried forward here unchanged; its
      ABSENCE leg was ''... | grep -c ''ALTER TABLE baselines_snapshots ADD COLUMN IF NOT EXISTS snapshot_id''
      | grep -qx 0'', which FAILS OPEN. check_values are handed to `sh -c` without pipefail, so a producer
      that 404s, is denied, or hits a network error emits nothing, `grep -c` prints 0, and `grep -qx 0`
      exits 0 -- GREEN, having read nothing. Executed proof of the defect at authoring time: the superseded
      shape returns rc=0 against OmniNode-ai/no_such_repo_xyz, a repository that does not exist. The replacement
      absence leg reads the parent revision ONCE into a variable, PROVES the read landed by requiring
      the structural anchor ''OMN-11887: node-owned projection migration'' (present in this file at both
      11b460db23d4 and 78b8731103a0, so it tracks the read rather than the fix), and only then asserts
      the marker is absent. A failed fetch now yields an empty body, the anchor leg fails, and the check
      goes RED. Claim bound, unchanged: the baselines_snapshots reconciling ALTER exists at omnibase_infra#2537''s
      merge commit 78b8731103a019ea90eee3164b53e882a064fe34, and was absent at its first parent 11b460db23d40dd2ca7704d0c3e9fd89851af60c.
      Executed at authoring time: green leg rc=0, absence leg rc=0; and the absence leg goes RED under
      all four mutations -- invalid token, nonexistent repo, pointed at the merge commit instead of the
      parent, and anchor mutated.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-15376-infra2537-baselines-rb15391
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_baselines/0001_create_baselines_tables.sql?ref=78b8731103a019ea90eee3164b53e882a064fe34''
          | grep -q ''ALTER TABLE baselines_snapshots ADD COLUMN IF NOT EXISTS snapshot_id'''
      - check_type: command
        check_value: 'body=$(gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_baselines/0001_create_baselines_tables.sql?ref=11b460db23d40dd2ca7704d0c3e9fd89851af60c'')
          && printf ''%s'' "$body" | grep -qF ''OMN-11887: node-owned projection migration'' && ! printf
          ''%s'' "$body" | grep -qF ''ALTER TABLE baselines_snapshots ADD COLUMN IF NOT EXISTS snapshot_id'''
  - id: dod-omn-15376-ac2-reconciling-rb2
    description: 'Append-only replacement for dod-omn-15376-ac2-reconciling-alter (OMN-15391 round 2,
      Rule D). The superseded item''s GREEN leg is correct and is carried forward here unchanged; its
      ABSENCE leg was ''... | grep -c ''ADD COLUMN IF NOT EXISTS aggregation_key'' | grep -qx 0'', which
      FAILS OPEN. check_values are handed to `sh -c` without pipefail, so a producer that 404s, is denied,
      or hits a network error emits nothing, `grep -c` prints 0, and `grep -qx 0` exits 0 -- GREEN, having
      read nothing. Executed proof of the defect at authoring time: the superseded shape returns rc=0
      against OmniNode-ai/no_such_repo_xyz, a repository that does not exist. The replacement absence
      leg reads the parent revision ONCE into a variable, PROVES the read landed by requiring the structural
      anchor ''node-owned projection migration'' (present in this file at both 11b460db23d4 and 78b8731103a0,
      so it tracks the read rather than the fix), and only then asserts the marker is absent. A failed
      fetch now yields an empty body, the anchor leg fails, and the check goes RED. Claim bound, unchanged:
      the llm_cost_aggregates reconciling ALTER exists at omnibase_infra#2537''s merge commit 78b8731103a019ea90eee3164b53e882a064fe34,
      and was absent at its first parent 11b460db23d40dd2ca7704d0c3e9fd89851af60c. Executed at authoring
      time: green leg rc=0, absence leg rc=0; and the absence leg goes RED under all four mutations --
      invalid token, nonexistent repo, pointed at the merge commit instead of the parent, and anchor mutated.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-omn-15376-ac2-reconciling-alter
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_cost_summary/0001_create_llm_cost_aggregates.sql?ref=78b8731103a019ea90eee3164b53e882a064fe34''
          | grep -q ''ADD COLUMN IF NOT EXISTS aggregation_key'''
      - check_type: command
        check_value: 'body=$(gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_cost_summary/0001_create_llm_cost_aggregates.sql?ref=11b460db23d40dd2ca7704d0c3e9fd89851af60c'')
          && printf ''%s'' "$body" | grep -qF ''node-owned projection migration'' && ! printf ''%s'' "$body"
          | grep -qF ''ADD COLUMN IF NOT EXISTS aggregation_key'''
