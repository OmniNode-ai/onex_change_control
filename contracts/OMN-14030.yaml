---
schema_version: "1.0.0"
ticket_id: "OMN-14030"
title: "Consolidate host cleanup into reusable disk-GC/worktree maintenance profile"
summary: >-
  Bind OCC evidence for omnibase_infra PR #2222, which adds the reusable host maintenance installer/status
  wrapper for disk-GC and worktree-reaper timers and removes the legacy deploy-runners root docker-prune
  cron installation path. The source PR does not install timers or perform cleanup on .201.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #2222 carries the host-maintenance source change."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14030/dod-infra-pr-2222/command.yaml"
  - kind: "tests"
    description: "Host maintenance installer tests, related guards, lint, and shell syntax checks passed
      locally."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14030/dod-local-validation/command.yaml"
  - kind: "manual"
    description: "Deploy gate is recorded: no live timer install/restart/cleanup before merge."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14030/dod-deploy-gate/command.yaml"
  - kind: "manual"
    description: "OCC PR #3649 binds this evidence bundle to the current change-control PR."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14030/dod-occ-self/command.yaml"
dod_evidence:
  - id: "dod-infra-pr-2222"
    description: "Bind omnibase_infra PR #2222 at the observed head commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14030/dod-infra-pr-2222/command.yaml && grep
          -q '^ticket_id: OMN-14030$' drift/dod_receipts/OMN-14030/dod-infra-pr-2222/command.yaml && grep
          -q '^pr_number: 2222$' drift/dod_receipts/OMN-14030/dod-infra-pr-2222/command.yaml && grep -q
          'e29dd7792596caf9577ddd63e683b2c3b9eef632' drift/dod_receipts/OMN-14030/dod-infra-pr-2222/command.yaml
  - id: "dod-local-validation"
    description: "Targeted host maintenance tests, ruff checks, shell syntax checks, and status JSON probe
      passed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14030/dod-local-validation/command.yaml && grep
          -q '31 passed' drift/dod_receipts/OMN-14030/dod-local-validation/command.yaml && grep -q 'All
          checks passed' drift/dod_receipts/OMN-14030/dod-local-validation/command.yaml
  - id: "dod-deploy-gate"
    description: "No live .201 timer install, restart, or cleanup was performed before merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14030/dod-deploy-gate/command.yaml && grep -qi
          'no live install' drift/dod_receipts/OMN-14030/dod-deploy-gate/command.yaml
  - id: "dod-occ-self"
    description: "Bind OCC PR #3649 at the observed head commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14030/dod-occ-self/command.yaml && grep -q '^ticket_id:
          OMN-14030$' drift/dod_receipts/OMN-14030/dod-occ-self/command.yaml && grep -q '^pr_number: 3649$'
          drift/dod_receipts/OMN-14030/dod-occ-self/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
