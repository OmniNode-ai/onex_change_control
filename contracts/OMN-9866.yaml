---
schema_version: "1.0.0"
ticket_id: OMN-9866
title: "Add direct module-to-test path mapping (scripts/ci/detect_test_paths.py) with resolve_test_paths
  and _resolve; src/omnibase_core/<module> maps to tests/unit/<module>/, tests/unit/<dir> maps to itself,
  tests/integration/* and unmapped paths return []."
summary: "Add direct module-to-test path mapping (scripts/ci/detect_test_paths.py) with resolve_test_paths
  and _resolve; src/omnibase_core/<module> maps to tests/unit/<module>/, tests/unit/<dir> maps to itself,
  tests/integration/* and unmapped paths return []."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core bundle PR #940 merged the change-aware CI stack that superseded PR #929"
    command: "gh pr view 940 --repo OmniNode-ai/omnibase_core --json state,mergeCommit --jq 'select(.state==\"MERGED\"
      and .mergeCommit.oid==\"a80fe01dc32f2abc21e5ed4bcee2871f64db9cca\")'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "scripts/ci/detect_test_paths.py exposes resolve_test_paths and _resolve callables importable
      from the omnibase_core PR #929 worktree"
    source: generated
    checks:
      - check_type: command
        check_value: "cd ${OMNI_HOME:-/Users/jonah/Code/omni_home}/omnibase_core && uv run python -c \"from
          scripts.ci.detect_test_paths import resolve_test_paths, _resolve; print('ok', resolve_test_paths.__name__,
          _resolve.__name__)\""
  - id: dod-002
    description: "All 4 module-to-test path mapping tests pass: src->tests/unit/<module>/, tests/unit/<dir>->itself,
      tests/integration->[], unknown paths->[]"
    source: generated
    checks:
      - check_type: test_passes
        check_value: "cd ${OMNI_HOME:-/Users/jonah/Code/omni_home}/omnibase_core && uv run pytest tests/unit/scripts/ci/test_detect_test_paths.py
          -v"
  - id: dod-003
    description: "omnibase_core bundle PR #940 merged the superseding change-aware CI implementation"
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view 940 --repo OmniNode-ai/omnibase_core --json state,mergeCommit --jq 'select(.state==\"MERGED\"
          and .mergeCommit.oid==\"a80fe01dc32f2abc21e5ed4bcee2871f64db9cca\")'"
