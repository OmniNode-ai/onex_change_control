---
schema_version: "1.0.0"
ticket_id: "OMN-15064"
title: "Autobind OCC evidence for OMN-15064"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1889."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1889 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1889"
    description: "PR #1889 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1889-ci"
    description: "PR #1889 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4767"
    description: "OCC companion PR #4767 — self-bind for OMN-15064 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1889-deploy-gate"
    description: >-
      deploy-gate (OMN-8912) evidence for PR #1889. This is a compute-only handler change — HandlerAdminMerge
      now raises AdminMergeGateClosedError in-process on a real (non-dry-run) pass when the OMN-14151
      arm-gate choke point (ModelArmGatePolicy: action_mode + kill_switch) is closed, instead of silently
      skipping; no new runtime surface, container, or topic is introduced, so no live redeploy is required.
      The check below is a live, falsifiable probe (gh api against GitHub content, not a self-referential
      read of this repo's own dod_receipts/contract tree): it fetches both changed files at the PR's pinned
      head SHA (39f267b7dd32c42747ac13b0197cfee2a5264ba5) and greps for the guard symbols the fix introduces.
      It goes RED if the code does not exist at that SHA.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_pr_lifecycle_fix_effect/handlers/handler_admin_merge.py?ref=39f267b7dd32c42747ac13b0197cfee2a5264ba5
          --jq .content | base64 -d | grep -q 'AdminMergeGateClosedError' && gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_pr_lifecycle_fix_effect/models/model_admin_merge_request.py?ref=39f267b7dd32c42747ac13b0197cfee2a5264ba5
          --jq .content | base64 -d | grep -q 'ModelArmGatePolicy'  # deploy-gate: no-live-redeploy-required,
          compute-only change proven at pinned PR head SHA"
  - id: "occ-self-bind-pr-4793"
    description: "OCC companion PR #4793 — self-bind for OMN-15064 deploy-gate evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4793 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
  - id: "dod-omn15064-pr1889-head-02925864"
    description: "PR #1889 current product head after required-check skip-vector repair."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1889 --repo OmniNode-ai/omnimarket --json headRefOid,state --jq 'if .state
          == \"OPEN\" and .headRefOid == \"02925864d96a0c699fec0d48baa95cef19179771\" then \"OMN-15064
          PR #1889 current head verified\" else error(\"OMN-15064 PR #1889 head/state mismatch\") end'"
  - id: "occ-self-bind-pr-4803"
    description: "OCC companion PR #4803 — self-bind for OMN-15064 current-head evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4803 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
  - id: "dod-omnimarket-pr-1889-deploy-gate-a5528de2"
    description: >-
      Repair of drifted deploy-gate (OMN-8912) evidence for PR #1889 (OMN-15111 drift-repair sweep, 2026-07-25).
      The predecessor entries dod-omnimarket-pr-1889-deploy-gate (pinned SHA 39f267b7dd32c42747ac13b0197cfee2a5264ba5,
      occ-self-bind-pr-4793) and dod-omn15064-pr1889-head-02925864 (pinned SHA 02925864d96a0c699fec0d48baa95cef19179771,
      occ-self-bind-pr-4803) both cite commits verified via `gh api repos/OmniNode-ai/omnimarket/compare/<sha>...a5528de2a2b4ba68427c50de7947bfd9c96246f7`
      to be non-ancestors ("diverged") of PR #1889's live head as of this repair — they are left byte-identical,
      not edited or dropped, per the append-only receipt model. This entry re-runs the same falsifiable
      probe (gh api against live GitHub content, not a self-referential read of this repo's own dod_receipts/contract
      tree) pinned to the current head a5528de2a2b4ba68427c50de7947bfd9c96246f7, confirmed OPEN and current
      via `gh pr view 1889 --repo OmniNode-ai/omnimarket --json headRefOid,state` immediately before probing.
      Goes RED if the guard symbols do not exist at that SHA, or if the SHA is no longer the PR's head.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1889 --repo OmniNode-ai/omnimarket --json headRefOid,state --jq 'if .state
          == \"OPEN\" and .headRefOid == \"a5528de2a2b4ba68427c50de7947bfd9c96246f7\" then \"head verified\"
          else error(\"OMN-15064 PR #1889 head/state mismatch\") end' && gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_pr_lifecycle_fix_effect/handlers/handler_admin_merge.py?ref=a5528de2a2b4ba68427c50de7947bfd9c96246f7
          --jq .content | base64 -d | grep -q 'AdminMergeGateClosedError' && gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_pr_lifecycle_fix_effect/models/model_admin_merge_request.py?ref=a5528de2a2b4ba68427c50de7947bfd9c96246f7
          --jq .content | base64 -d | grep -q 'ModelArmGatePolicy'  # deploy-gate: no-live-redeploy-required,
          compute-only change reproven at CURRENT PR head SHA after prior receipts drifted"
