---
schema_version: "1.0.0"
ticket_id: "OMN-11131"
title: "Phase 2: Delegation Projection Activation"
summary: >
  Fix _derive_projection_event_type to allow non-platform topics (onex.evt.omniclaude.task-delegated.v1)
  through projection dispatch without raising ValueError. This unblocks HandlerProjectionDelegation from
  processing delegation events and writing rows to delegation_events table. PR #1625 in omnibase_infra.
  Deploy: rebuild omninode-runtime image on .201 after merge, rolling restart.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #1625 merged with fix to _derive_projection_event_type"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11131/dod-infra-pr-1625/command.yaml"
  - kind: "manual"
    description: "All 3 projection dispatch integration tests pass locally"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11131/dod-tests/command.yaml"
  - kind: "manual"
    description: "OCC PR carries OMN-11131 contract and receipts"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11131/dod-occ-self/command.yaml"
  - kind: "manual"
    description: "Deploy assessment documented for runtime path change in handler_wiring.py"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11131/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1625"
    description: "Bind OCC evidence to omnibase_infra PR #1625 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11131/dod-infra-pr-1625/command.yaml\""
  - id: "dod-tests"
    description: "All 3 projection dispatch integration tests pass on the PR branch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11131/dod-tests/command.yaml\""
  - id: "dod-occ-self"
    description: "OCC PR carries OMN-11131 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11131/dod-occ-self/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Deploy assessment for runtime path change: docker exec verification plan recorded."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11131/dod-deploy-assessment/command.yaml\""
