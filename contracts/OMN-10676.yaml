---
schema_version: "1.0.0"
ticket_id: "OMN-10676"
title: "Rewire deploy-onex-dev.yml to target the dev cluster"
summary: >
  Repoint the dev deploy workflow to the real dev k3s cluster, SSM Parameter Store kubeconfig, and dev
  hostname overlay. Live workflow dispatch remains a follow-up operational validation step and was not
  run as part of this code-prep PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-workflow-rewired"
    description: "omninode_infra PR #413 rewires deploy-onex-dev.yml for the dev cluster target."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10676/dod-workflow-rewired/command.yaml"
  - id: "dod-local-validation"
    description: "Workflow YAML, actionlint, whitespace, and dev overlay render validation passed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10676/dod-local-validation/command.yaml"
  - id: "dod-aws-preflight"
    description: "Read-only AWS checks confirmed the dev kubeconfig parameter and dev SSM target exist."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10676/dod-aws-preflight/command.yaml"
  - id: "dod-pr-open"
    description: "omninode_infra PR #414 is open for the OMN-10676 follow-up gate fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10676/dod-pr-open/command.yaml"
  - id: "dod-occ-pr"
    description: "Bind the central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10676/dod-occ-pr/command.yaml"
