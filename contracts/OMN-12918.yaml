---
schema_version: "1.0.0"
ticket_id: "OMN-12918"
title: "Move projection API database binding and database creation to contracts"
summary: >-
  Add typed overlay binding for the omnimarket projection API database DSN so dashboard projection serving
  can resolve its database from contract/runtime overlay authority instead of ambient OMNIDASH_ANALYTICS_DB_URL
  or OMNIBASE_INFRA_DB_URL environment variables.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1148"
    description: >-
      omnimarket PR #1148 carries the OMN-12918 projection API overlay database binding implementation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12918/dod-omnimarket-pr-1148/command.yaml"
  - id: "dod-focused-tests"
    description: >-
      Focused projection API tests, lint, format, and strict mypy checks pass for the OMN-12918 implementation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12918/dod-focused-tests/command.yaml"
  - id: "dod-no-env-overlay-proof"
    description: >-
      With OMNIDASH_ANALYTICS_DB_URL and OMNIBASE_INFRA_DB_URL unset, the projection API resolves its
      DB DSN from OMNIMARKET_PROJECTION_API_BINDING_OVERLAY.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12918/dod-no-env-overlay-proof/command.yaml"
          && grep -q 'db.noenv' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12918/dod-no-env-overlay-proof/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      OMN-12918 changes projection API runtime configuration. No live `.201` deployment or restart was
      performed during PR validation; hosted runtime validation remains a post-merge deploy step.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12918/dod-deploy-assessment/command.yaml"
          && grep -q 'No live deployment or restart was performed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12918/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      OCC PR carries the central OMN-12918 contract and DoD receipts used by downstream receipt gates.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12918/dod-occ-pr/command.yaml"
