---
schema_version: "1.0.0"
ticket_id: "OMN-13687"
title: "fix(OMN-13687): bounded retry on docker push to ECR"
summary: >-
  OCC evidence for omnibase_infra PR #2134, which wraps the runtime image docker push step in .github/workflows/build-and-push-runtime.yml
  with a bounded retry loop. The change is additive: image build, Trivy scan, tag generation, and digest
  handling remain unchanged; only the network push is retried to absorb the transient runner-to-ECR blob
  HEAD EOF observed during emergency prod recovery.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-workflow-retry-loop"
    description: >-
      The runtime image publish workflow parses as valid YAML at omnibase_infra PR #2134 head dfa4aba8b0a0acf1ca91e6506b4dfa175499b065
      and contains the bounded push_with_retry helper with PUSH_MAX_ATTEMPTS=5, initial delay 10s, max
      delay 120s, exponential backoff factor 2, docker push retry on each tag, and failure after the bounded
      attempts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13687/dod-workflow-retry-loop/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13687.yaml consumed
      by omnibase_infra PR #2134 via Evidence-Source and hotfix-evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13687/dod-occ-pr/command.yaml"
