---
schema_version: "1.0.0"
ticket_id: "OMN-13002"
title: "Sanctioned judge-direct overlay — key-only no-Kafka live SEA path; fix Gemini 400/429"
summary: >
  Ship a sanctioned judge-direct delegation overlay (transport_mode: direct) so an outside judge with
  only an API key can run the documented live SEA command end-to-end with no Kafka, no local models, and
  no lane overlays. The repo-default overlay stays fail-closed. A new in-process JudgeDirectInferenceBus
  POSTs the OpenAI-compatible request to the registry endpoint verbatim (fixing the doubled chat/completions
  path behind the OMN-12890 provider 4xx) and resolves the api key at the effect boundary. Provider-agnostic:
  verified live against Gemini (gemini-2.5-flash-lite) and OpenRouter (openai/gpt-4o-mini). JUDGE_REPRODUCE.md
  documents the per-provider copy-paste blocks; the no-key replay tier is untouched.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
dod_evidence:
  - id: "dod-sea-pr-231"
    description: >
      onex-self-extending-agent PR #231 ships the judge-direct overlay, JudgeDirectInferenceBus, the direct
      readiness gate, and unit tests proving the default still fail-closes. Full pre-commit + unit suite
      pass; live e2e PASS against Gemini and OpenRouter.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13002/dod-sea-pr-231/command.yaml"
          && grep -q '^ticket_id: "OMN-13002"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13002/dod-sea-pr-231/command.yaml"
          && grep -q '^pr_number: 231$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13002/dod-sea-pr-231/command.yaml"
  - id: "dod-occ-pr"
    description: >
      This OCC PR carries the OMN-13002 central contract and receipts used by the onex-self-extending-agent
      PR Receipt Gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13002/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-13002"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13002/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2524$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13002/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
