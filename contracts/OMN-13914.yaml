---
schema_version: "1.0.0"
ticket_id: "OMN-13914"
title: "fan-out: add synchronize/reopened to pr-title caller triggers (9 repos)"
summary: >-
  Fan-out of the OMN-13909 canary fix (onex_change_control PR #3542, merged) to the remaining repos that
  call the shared pr-title-check-reusable.yml workflow with the identical [opened, edited]-only trigger
  gap in their own caller .github/workflows/pr-title-check.yml. This contract covers the 7 consuming PRs
  whose repos run the Receipt Gate (verify / verify) and therefore need Evidence-Source resolution: omnibase_core
  #1385, omnibase_infra #2191, omniclaude #1850, omnibase_compat #169, omnimarket #1595, omniintelligence
  #752, and omninode_infra #561. (omnimemory and omnibase_spi have no Receipt Gate wired and omnidash
  is parked — not part of this evidence set.) Net-new contract per the sanctioned child-ticket pattern
  since contracts/OMN-13909.yaml is already consumed by the canary PR's self-referencing receipt.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1385"
    description: >-
      Consuming-PR binding: omnibase_core PR #1385 adds synchronize+reopened to pr-title-check.yml's trigger
      types at head ace398999122b47d5536f3236ed43f99a9b02bae.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13914/dod-omnibase_core-pr-1385/command.yaml"
  - id: "dod-omnibase_infra-pr-2191"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2191 adds synchronize+reopened to pr-title-check.yml's
      trigger types at head a3ab717d07e5379971621d76568e9d3c537f31c3.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13914/dod-omnibase_infra-pr-2191/command.yaml"
  - id: "dod-omniclaude-pr-1850"
    description: >-
      Consuming-PR binding: omniclaude PR #1850 adds synchronize+reopened to pr-title-check.yml's trigger
      types at head b51e6d2c7771ffd9acde2c22fe931f9db1da0a39.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13914/dod-omniclaude-pr-1850/command.yaml"
  - id: "dod-omnibase_compat-pr-169"
    description: >-
      Consuming-PR binding: omnibase_compat PR #169 adds synchronize+reopened to pr-title-check.yml's
      trigger types at head b1d7dcc479fbe2977b6a62e2485db50772266902.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13914/dod-omnibase_compat-pr-169/command.yaml"
  - id: "dod-omnimarket-pr-1595"
    description: >-
      Consuming-PR binding: omnimarket PR #1595 adds synchronize+reopened to pr-title-check.yml's trigger
      types at head a12f652d5ff2f0d4ae2c587d01431d16802d6c87.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13914/dod-omnimarket-pr-1595/command.yaml"
  - id: "dod-omniintelligence-pr-752"
    description: >-
      Consuming-PR binding: omniintelligence PR #752 adds synchronize+reopened to pr-title-check.yml's
      trigger types at head 2da66985f01e082577cc5699cc907cf24557ee09.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13914/dod-omniintelligence-pr-752/command.yaml"
  - id: "dod-omninode_infra-pr-561"
    description: >-
      Consuming-PR binding: omninode_infra PR #561 adds synchronize+reopened to pr-title-check.yml's trigger
      types at head e9a99883cd05c90ab052b4f79441dc42b1b1ff62.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13914/dod-omninode_infra-pr-561/command.yaml"
  - id: "dod-omniclaude-pr-1853"
    description: >-
      Consuming-PR binding: omniclaude PR #1853 commits the .onex_state/evidence/OMN-13914/dod_report.json
      DoD receipt that the omniclaude DoD Evidence Check reads to permit the OMN-13914 Done-flip; the
      pr-title-check.yml synchronize+reopened trigger change itself already merged as omniclaude #1850.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13914/dod-omniclaude-pr-1853/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13914 contract and receipts consumed
      by the 7 fan-out PRs above.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13914/dod-occ-self/command.yaml"
