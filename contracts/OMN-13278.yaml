---
schema_version: "1.0.0"
ticket_id: "OMN-13278"
title: "Hook measurement harness and tiered reintroduction design"
summary: >-
  OMN-13278 adds a read-only hook measurement harness and the design for tiered hook reintroduction after
  the hooks-off baseline. The PR reads existing cost accounting and PRM trajectory telemetry surfaces,
  compares hooks-off and hooks-on windows, and documents the Tier 0-3 reintroduction plan. It does not
  re-enable hooks.json or change live hook registration behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1774"
    description: >-
      Per-code-PR binding receipt for omniclaude PR #1774 (jonahgabriel/omn-13278-hook-measurement-tiered-plan,
      base=dev). The PR adds the hook measurement package, tests, README, and design document, while leaving
      hooks.json untouched. This receipt binds OMN-13278 to the omniclaude PR by pr_number and commit_sha
      so downstream Receipt Gate can resolve central OCC evidence for the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13278/dod-omniclaude-pr-1774/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate evidence for the design-first hook measurement package and documentation touched by
      OMN-13278. No live deploy, runtime restart, or hook re-registration was performed pre-merge. The
      required operational step is review of the design and a future explicit hook tier reintroduction
      before any runtime behavior is changed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13278/dod-deploy-assessment/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13278 contract and omniclaude
      PR #1774 binding.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13278/dod-occ-pr-self/command.yaml
