---
schema_version: "1.0.0"
ticket_id: "OMN-9121"
title: "Add Prometheus crash-loop alerting rules for runtime containers"
summary: >
  Bind omnibase_infra PR #1650 to central OCC evidence for Prometheus runtime crash-loop alerting. The
  implementation adds Prometheus rules for omninode container restart storms, high restart counts, and
  runtime metrics scrape failures, plus a minimal Prometheus config and root-validator allowlist update.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1650 is open at the recorded implementation head"
    command: "See drift/dod_receipts/OMN-9121/dod-omnibase-infra-pr-1650/command.yaml"
  - kind: "tests"
    description: "Prometheus rules and configuration validate with promtool"
    command: "See drift/dod_receipts/OMN-9121/dod-promtool-rules/command.yaml and dod-promtool-config/command.yaml"
  - kind: "tests"
    description: "Root directory validator tests pass after allowing observability/"
    command: "See drift/dod_receipts/OMN-9121/dod-clean-root-tests/command.yaml"
  - kind: "ci"
    description: "OCC evidence PR #1226 is open and self-bound"
    command: "See drift/dod_receipts/OMN-9121/dod-occ-pr-1226/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-1650-head"
    description: "omnibase_infra PR #1650 carries the Prometheus crash-loop alerting rules at the recorded
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9121/dod-omnibase-infra-1650-head/command.yaml\""
  - id: "dod-omnibase-infra-1650-ci"
    description: "omnibase_infra PR #1650 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9121/dod-omnibase-infra-1650-ci/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carrying the OMN-9121 central contract and receipts is open."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9121/dod-occ-pr/command.yaml\""
  - id: "dod-omnibase-infra-pr-1650"
    description: "omnibase_infra PR #1650 carries the OMN-9121 Prometheus alerting changes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-9121/dod-omnibase-infra-pr-1650/command.yaml
          && grep -q '^pr_number: 1650$' drift/dod_receipts/OMN-9121/dod-omnibase-infra-pr-1650/command.yaml
  - id: "dod-promtool-rules"
    description: "Prometheus runtime-health alert rules parse and contain three rules"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-9121/dod-promtool-rules/command.yaml && grep
          -q 'SUCCESS: 3 rules found' drift/dod_receipts/OMN-9121/dod-promtool-rules/command.yaml
  - id: "dod-promtool-config"
    description: "Prometheus config parses and loads the runtime-health rule file"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-9121/dod-promtool-config/command.yaml && grep
          -q 'valid prometheus config file syntax' drift/dod_receipts/OMN-9121/dod-promtool-config/command.yaml
  - id: "dod-clean-root-tests"
    description: "Root directory validator tests pass with observability/ allowed"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-9121/dod-clean-root-tests/command.yaml && grep
          -q '69 passed' drift/dod_receipts/OMN-9121/dod-clean-root-tests/command.yaml
  - id: "dod-occ-pr-1226"
    description: "Self-binding receipt for OCC PR #1226"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-9121/dod-occ-pr-1226/command.yaml && grep -q
          '^pr_number: 1226$' drift/dod_receipts/OMN-9121/dod-occ-pr-1226/command.yaml
