---
schema_version: "1.0.0"
ticket_id: "OMN-9121"
title: "Bind omnibase_infra Prometheus crash-loop alerting PR to OCC evidence"
summary: >
  Central OCC evidence for omnibase_infra PR #1650, which adds Prometheus crash-loop alerting rules for
  omninode-* containers (the 490-restart incident gap). This contract binds the exact product PR head
  and records that all visible non-Receipt-Gate checks are green or skipped, so Receipt Gate can evaluate
  the PR from a pinned Evidence-Source without bypass tokens.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-1650-head"
    description: "omnibase_infra PR #1650 carries the Prometheus crash-loop alerting rules at the recorded
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9121/dod-omnibase-infra-1650-head/command.yaml\""
  - id: "dod-omnibase-infra-1650-ci"
    description: "omnibase_infra PR #1650 non-receipt checks are green; Receipt Gate is the known evidence-source
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9121/dod-omnibase-infra-1650-ci/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carrying the OMN-9121 central contract and receipts is open."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9121/dod-occ-pr/command.yaml\""
