---
schema_version: "1.0.0"
ticket_id: "OMN-13828"
title: "OMN-13828 dogfood adoption — dev→main promotion train"
summary: >-
  Promote the dogfood adoption-plumbing work (node-backed skills plus the OMN-13877/OMN-13878/OMN-13887
  dep-provenance PyPI repins and dispatch migration) from dev to main across the adoption repos. This
  contract carries the per-repo promotion-PR bindings and the self-binding OCC receipt for the promotion
  train.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-promotion-pr"
    description: >-
      omniintelligence dev->main promotion PR #749 (head 6e4fd534) promotes the adoption plumbing, the
      OMN-13887 dispatch migration onto the canonical omnibase_infra MessageDispatchEngine seam, and the
      OMN-13878 PyPI repin (omnibase-core 0.46.3 / omnibase-spi 0.23.1) to main. Publishes omniintelligence
      0.24.0.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13828/dod-omniintelligence-promotion-pr/command.yaml"
  - id: "dod-omniclaude-promotion-pr"
    description: >-
      omniclaude dev->main promotion (dev-head 3056e495) promotes the adoption plumbing and the OMN-13877
      dep-provenance gate + omnibase-core PyPI repin to main, advancing omniclaude main 0.25.0 -> 0.25.1
      (0.25.1 already published to PyPI).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13828/dod-omniclaude-promotion-pr/command.yaml"
  - id: "dod-omniclaude-deploy-evidence"
    description: >-
      Deploy-gate evidence for omniclaude dev->main promotion PR #1841. The promotion touches runtime
      paths, so the downstream deploy gate is intentionally bound to this central OCC deploy receipt instead
      of a repository-local bypass.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13828/dod-omniclaude-deploy-evidence/command.yaml"
  - id: "dod-omnimarket-promotion-pr"
    description: >-
      omnimarket dev->main promotion (dev-head 1b326ec2) promotes the adoption plumbing and the OMN-13878
      omnibase-core/compat PyPI repin to main (omnimarket 0.4.3, already published).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13828/dod-omnimarket-promotion-pr/command.yaml"
  - id: "dod-omnibase-compat-promotion-pr"
    description: >-
      omnibase_compat dev->main promotion (dev-head 52083fda) promotes the adoption plumbing and OMN-13879
      pre-expiry retention work to main (omnibase-compat 0.5.5, already published).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13828/dod-omnibase-compat-promotion-pr/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13828 promotion contract and
      its PASS receipts for the adoption dev->main promotion train.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13828/dod-occ-self/command.yaml"
