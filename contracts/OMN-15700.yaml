---
schema_version: "1.0.0"
ticket_id: "OMN-15700"
title: "OCC evidence for OMN-15700"
summary: >
  Manual OCC companion for OmniNode-ai/omnibase_infra PR #2681, which derives the savings estimator Kafka
  consumer group identity canonically. This contract binds the product PR to its exact live head and records
  deploy scope honestly: runtime code changed, so post-merge deployment/readback remains a residual and
  is not claimed here.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2681 exact head and savings consumer-group proof are live."
    command: "gh pr view 2681 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2681"
    description: "PR #2681 on OmniNode-ai/omnibase_infra - exact-head savings consumer-group diff."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2681 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,files
          --jq 'if (.number == 2681 and .state == "OPEN" and .headRefName == "daniyal/omn-15700-savings-consumer-group"
          and .headRefOid == "bc14b302029c081872cbf9a6059133edbbea0ce4" and ([.files[].path] | sort) ==
          ["src/omnibase_infra/runtime/service_kernel.py","tests/integration/runtime/test_savings_estimator_subscription_group.py","tests/unit/services/observability/savings_estimation/test_consumer.py"])
          then "true" else error("unexpected #2681 state, head, or file set") end'
  - id: "dod-deploy-assessment"
    description: >
      Deploy-scope DoD for PR #2681. Runtime/services files are touched, so this item classifies deploy
      scope without claiming live deployment or post-deploy projection readback.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2681 --repo OmniNode-ai/omnibase_infra --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-validator-pr-2681"
    description: "Hosted OCC evidence admissibility validator for OMN-15700 / PR #2681."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2681"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6189"
    description: "Binds OMN-15700 to OCC companion PR #6189."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-evidence-validator-pr-2681"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 6189 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid
          --jq 'if (.number == 6189 and .state == "OPEN" and .headRefName == "codex/omn-15700-occ-pr2681")
          then "true" else error("unexpected OCC self-bind PR state") end'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2681-rebind-6192"
    description: >
      Rebind of dod-OmniNode-ai-omnibase_infra-pr-2681 to PR #2681's current head after new commits landed
      post-#6189 merge. Append-only: does not edit the original entry's bytes.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2681"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2681 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,files
          --jq 'if (.number == 2681 and .state == "OPEN" and .headRefName == "daniyal/omn-15700-savings-consumer-group"
          and .headRefOid == "0e21e0c4f055d3bd6b820b0c9afe1f1cbb60c00f" and ([.files[].path] | sort) ==
          ["src/omnibase_infra/runtime/service_kernel.py","tests/integration/runtime/test_savings_estimator_subscription_group.py","tests/unit/services/observability/savings_estimation/test_consumer.py"])
          then "true" else error("unexpected #2681 state, head, or file set") end'
  - id: "dod-deploy-assessment-rebind-6192"
    description: >
      Pre-deploy falsifiable content probe for PR #2681's current head. Reads the exact product head through
      the GitHub contents API and fails unless the canonical savings subscription symbol exists. This
      does not claim live deployment or post-deploy projection readback. Append-only: does not edit the
      original dod-deploy-assessment entry's bytes.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/service_kernel.py?ref=0e21e0c4f055d3bd6b820b0c9afe1f1cbb60c00f'
          --jq .content | base64 -d | grep -F 'node_identity=_savings_node_identity' >/dev/null
  - id: "occ-self-bind-pr-6261"
    description: "Binds OMN-15700 to replacement OCC companion PR #6261, rebinding infra PR #2681 to its
      current head after #6189 merged against a stale head. Supersedes the never-merged #6192 (same content,
      but #6192's head lived on a fork this session could not push a conflict-resolution commit to)."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-6189"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 6261 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRepositoryOwner
          --jq 'if (.number == 6261 and (.state == "OPEN" or .state == "MERGED") and .headRefName == "codex/omn-15700-occ-falsifiable"
          and .headRepositoryOwner.login == "OmniNode-ai") then "true" else error("unexpected OCC self-bind
          PR state") end'
