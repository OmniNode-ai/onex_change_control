---
schema_version: "1.0.0"
ticket_id: "OMN-13254"
title: "Classify omniclaude dead-code re-audit"
summary: >-
  Adds a documentation evidence artifact classifying omniclaude Repowise dead-code re-audit findings after
  reindex. The PR records focused hook/skill findings, updates the docs index, and recommends child-ticket
  splits instead of deleting runtime/plugin/hook code in this lane. This central OCC evidence binds OMN-13254
  to omniclaude PR #1784 and records that the PR has no live deploy mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1784"
    description: >-
      Per-code-PR binding receipt for omniclaude PR #1784 at the current head. The PR adds an OMN-13254
      dead-code re-audit evidence document and updates docs/INDEX.md. The receipt binds by pr_number and
      commit_sha for downstream Receipt Gate resolution.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13254/dod-omniclaude-pr-1784/command.yaml
  - id: "dod-deploy-no-live-mutation"
    description: >-
      Deploy-path evidence for OMN-13254. The PR changes documentation/evidence artifacts only. It adds
      no runtime node, no Kafka topic, no contract, no migration, no image-build input, and performs no
      .201 mutation or live deploy.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13254/dod-deploy-no-live-mutation/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying OMN-13254 contract and receipts. Binds
      the OCC PR by pr_number so the OCC receipt-gate eligibility check can resolve a PASS receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13254/dod-occ-pr-self/command.yaml
