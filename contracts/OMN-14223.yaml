---
schema_version: "1.0.0"
ticket_id: "OMN-14223"
title: "feat(OMN-14223): relocate canonical OCC stamp schema into omnibase_compat"
summary: >-
  omnibase_compat PR #173 relocates the canonical OCC PR metadata-stamp schema from omnibase_core into
  omnibase_compat, the lowest shared layer. The PR ports the evidence-source enum, four metadata stamp
  models, and the parse/render_pr_occ_metadata_stamp helpers with only import-path changes from omnibase_core.*
  to omnibase_compat.*. It also exports the public omnibase_compat.contracts.pr_occ_stamp import path,
  ports the full round-trip test suite, and bumps omnibase-compat from 0.5.5 to 0.5.6. This is the operator
  placement correction for the OMN-14180 OCC stamp-model epic; the core-side deletion remains a separate
  follow-up and this PR does not touch omnibase_core.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-compat-pr-173"
    description: >-
      Live GitHub verification shows omnibase_compat PR #173 targets dev from jonah/omn-14223-relocate-occ-stamp-schema
      at head 3b1817d79b9aa2048c50c218cbe6ba9a064cd56d. The diff is focused on the pr_occ_stamp package,
      the evidence-source enum, unit tests, pyproject version bump, and uv.lock. The PR body documents
      the canonical public import path and explicitly scopes core-side deletion to a separate follow-up.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14223/dod-omnibase-compat-pr-173/command.yaml
          && grep -q '^pr_number: 173$' drift/dod_receipts/OMN-14223/dod-omnibase-compat-pr-173/command.yaml
          && grep -q '^commit_sha: "3b1817d79b9aa2048c50c218cbe6ba9a064cd56d"$' drift/dod_receipts/OMN-14223/dod-omnibase-compat-pr-173/command.yaml
  - id: "dod-local-verification"
    description: >-
      Local verification in the clean omnibase_compat PR worktree passed ruff on the touched source/tests,
      the focused OCC-stamp suite, and the full test suite. Focused tests: 75 passed, 1 known pydantic
      serializer warning. Full tests: 261 passed, 1 same warning.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14223/dod-local-verification/command.yaml &&
          grep -q '261 passed' drift/dod_receipts/OMN-14223/dod-local-verification/command.yaml
  - id: "dod-occ-companion-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14223 contract and
      PASS receipts consumed by omnibase_compat PR #173 through its Evidence-Source line. The receipt
      binds the OCC companion by a commit in this PR's commit set so occ-preflight can validate the companion
      without relying on predicted PR numbers.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14223/dod-occ-companion-self/command.yaml
