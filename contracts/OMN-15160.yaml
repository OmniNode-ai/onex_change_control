---
schema_version: "1.0.0"
ticket_id: "OMN-15160"
title: "Migrate untracked pilots/ into tracked contracts_data provider references — receipt contract for
  already-merged work, with the retirement clause honestly scoped OUT"
summary: >-
  OMN-15160's migration deliverable is merged; one clause of its ticket text is NOT, and this contract
  says so rather than papering over it. MERGED: jonahgabriel/steel_onslaught PR #216, merge commit 965d574558de2164e2f573014eb8cad6bef6c8a0,
  merged 2026-07-26T05:29:31Z onto `main` (steel's canonical branch is `main`, NOT `dev`). The tracked
  provider-reference shape landed — `contracts_data/pilots/local_200/llm_local_200.yaml` is a new pilot
  spec in a DEDICATED registry dir (so it cannot collide with the flat `contracts_data/pilots/` glob used
  by `live_glm_cards.yaml`), paired with the provider-reference overlay `contracts_data/overlays/local_200_v1.yaml`
  that references by `provider_id` and carries endpoint/model on the overlay rather than inline in the
  pilot spec. NOT DELIVERED, and deliberately NOT claimed here: the ticket's "Retires the orphaned top-level
  pilots/ directory" clause. Merge commit 965d5745 deferred it in its own body, and that deferral is the
  ONLY thing the corresponding dod_evidence item below proves. The deferred clause is now carried by follow-up
  ticket OMN-15448 (https://linear.app/omninode/issue/OMN-15448), filed 2026-07-30 and recorded in an
  OMN-15160 Linear comment; per that comment the retirement is PARTIAL — 5 of 6 untracked files removed,
  `pilots/llm_local.yaml` RETAINED pending adjudication because its `provider: local` has zero tracked
  representation, which is itself a correction to 965d5745's own commit-body claim that all five stubs
  carried no unrepresented facts. A dod_verify PASS on this contract therefore MUST NOT be read as evidence
  that the top-level pilots/ directory was retired; cite OMN-15448 for that. Also disclosed by the merged
  work: a persona `operator` → `berserker` substitution because `operator` is unregistered. Every check
  is pinned to the merge SHA and was executed GREEN at authoring time.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "steel PR #216 is MERGED on jonahgabriel/steel_onslaught main at merge commit 965d5745"
    command: "gh pr view 216 --repo jonahgabriel/steel_onslaught --json state,mergeCommit"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omn-15160-pr-216-merged"
    description: >-
      steel PR #216 ("OMN-15160 — migrate orphaned pilots/ into tracked contracts_data provider references")
      is MERGED with merge commit exactly 965d574558de2164e2f573014eb8cad6bef6c8a0. State and SHA asserted
      together, so a closed-unmerged PR ("CLOSED none"), an open PR ("OPEN none"), or a re-land under
      another SHA all yield count 0 and fail.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 216 --repo jonahgabriel/steel_onslaught --json state,mergeCommit --jq '.state + " " + (.mergeCommit.oid // "none")' | grep -cFx 'MERGED 965d574558de2164e2f573014eb8cad6bef6c8a0' | grep -qx 1
  - id: "dod-omn-15160-tracked-pilot-spec"
    description: >-
      The new pilot spec exists as a TRACKED file at the merge SHA under a dedicated registry dir — `contracts_data/pilots/local_200/llm_local_200.yaml`
      — and declares the provider it exists to carry (`provider: local_200`). Reading it through the GitHub
      contents API at a pinned ref is what makes this falsifiable: the pre-migration state was an UNTRACKED
      working-directory file, which is not in git and therefore returns a 404 → empty body → count 0 →
      RED.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: |-
          gh api 'repos/jonahgabriel/steel_onslaught/contents/contracts_data/pilots/local_200/llm_local_200.yaml?ref=965d574558de2164e2f573014eb8cad6bef6c8a0' --jq '.content' | base64 -d | grep -cF 'provider: local_200' | grep -qx 1
  - id: "dod-omn-15160-provider-ref-overlay"
    description: >-
      The paired overlay `contracts_data/overlays/local_200_v1.yaml` implements the provider-REFERENCE
      pattern the ticket specifies: it references the provider by id (`provider_id: local_200`) under
      an `openai_compatible` binding kind, with endpoint/model carried on the overlay rather than inlined
      into the pilot spec. Asserting the `provider_id:` line (not merely the string `local_200` anywhere
      in the file) is what discriminates the reference pattern from an inline-endpoint regression.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: |-
          gh api 'repos/jonahgabriel/steel_onslaught/contents/contracts_data/overlays/local_200_v1.yaml?ref=965d574558de2164e2f573014eb8cad6bef6c8a0' --jq '.content' | base64 -d | grep -cF 'provider_id: local_200' | grep -qx 1
  - id: "dod-omn-15160-clause-superseded-15448"
    description: >-
      SCOPE EXCLUSION — recorded as an unmet clause, NOT claimed as satisfied. This item proves ONLY the
      DEFERRAL FACT: that merge commit 965d5745's own commit message defers the ticket's "Retires the
      orphaned top-level pilots/ directory" clause, in the words "a follow-up ticket removes it once this
      lands." It proves NOTHING about the directory having been retired. SUPERSEDED-BY-FOLLOW-UP: OMN-15448
      (https://linear.app/omninode/issue/OMN-15448), a child of epic OMN-15154 filed 2026-07-30 by the
      cleanup lane and recorded in an OMN-15160 Linear comment, which reports the retirement as PARTIAL
      (5/6 untracked files removed; `pilots/llm_local.yaml` RETAINED because `provider: local` has zero
      tracked representation). The residual is not deliverable as a PR at all — the files are UNTRACKED,
      so they are not in git and no worktree/PR can delete them — which is precisely why it is scoped
      out here instead of being turned into a check that could never pass honestly.
    source: "manual"
    status: "pending"
    evidence_artifact: "superseded_by_follow_up_ticket:OMN-15448"
    checks:
      - check_type: "command"
        check_value: |-
          gh api 'repos/jonahgabriel/steel_onslaught/commits/965d574558de2164e2f573014eb8cad6bef6c8a0' --jq '.commit.message' | grep -cF 'a follow-up ticket removes it once this lands.' | grep -qx 1
