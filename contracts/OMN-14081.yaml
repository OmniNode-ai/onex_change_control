---
schema_version: "1.0.0"
ticket_id: "OMN-14081"
title: "fix(OMN-14081): make pyproject.toml test-selector trigger content-aware"
summary: >-
  Bind omnibase_core PR #1393 to central OCC evidence for the change-aware test selector fix that
  classifies pyproject.toml changes by content instead of escalating every pyproject edit to the full
  40-shard test suite.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omnibase_core PR #1393 CI, type checks, and the 40-shard test matrix are green at the recorded head SHA."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14081/dod-omnibase-core-pr-1393/command.yaml"
  - kind: "tests"
    description: "Local focused selector verification was run for the OMN-14081 implementation."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14081/dod-local-validation/command.yaml"
  - kind: "manual"
    description: "No live runtime deploy is in scope for this CI selector change."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14081/dod-deploy-scope/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1393"
    description: >-
      Consuming-PR binding: omnibase_core PR #1393 at head 08d998bb39b69a89daadb076a4664800dc55af4d
      carries the OMN-14081 pyproject.toml content-aware selector implementation and has green CI/test
      evidence apart from the missing OCC metadata gates being repaired here.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14081/dod-omnibase-core-pr-1393/command.yaml
          && grep -q '^ticket_id: OMN-14081$' drift/dod_receipts/OMN-14081/dod-omnibase-core-pr-1393/command.yaml
          && grep -q '^pr_number: 1393$' drift/dod_receipts/OMN-14081/dod-omnibase-core-pr-1393/command.yaml
          && grep -q '08d998bb39b69a89daadb076a4664800dc55af4d' drift/dod_receipts/OMN-14081/dod-omnibase-core-pr-1393/command.yaml
  - id: "dod-local-validation"
    description: >-
      Local validation receipt records the focused selector test command and formatting/lint/type-check evidence
      reported by the implementing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14081/dod-local-validation/command.yaml"
  - id: "dod-deploy-scope"
    description: >-
      Deploy-scope binding: OMN-14081 changes CI selector logic and tests only; no live runtime, host,
      runner, Docker, Redpanda, AWS, SSH, or production deployment mutation is required.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14081/dod-deploy-scope/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-14081 central contract and
      PASS receipts consumed by omnibase_core PR #1393 via its Evidence-Source line.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14081/dod-occ-self/command.yaml"
