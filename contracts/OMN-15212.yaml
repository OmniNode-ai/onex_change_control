---
schema_version: "1.0.0"
ticket_id: "OMN-15212"
title: "Correct live-false ModelIntentPayloadBase-removed-in-0.6.2 claim in omnibase_infra docs"
summary: >
  OCC companion evidence for omnibase_infra PR #2483: corrects the live-false claim that ModelIntentPayloadBase
  was removed in omnibase_core 0.6.2. The class was never removed at any version (added at core v0.5.6
  in f79f0214/OMN-1008; present and exported at the v0.6.2 tag; live dev is 0.46.8 with ~12 core payloads
  subclassing it). Fix spans CLAUDE.md (2 bullets), docs/standards/ONEX_TERMINOLOGY.md, and three stale
  src NOTE comments. The claim was inherited from the pre-slim CLAUDE.md and preserved verbatim by the
  OMN-15198 KEEP contract.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Product PR binding and content probes for omnibase_infra#2483."
    command: "gh pr view 2483 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2483-head-cc97c657"
    description: >
      Consuming-PR binding: omnibase_infra PR #2483 head cc97c65790e70d6eb77ef04192145807ebdb05a4 carries
      the ModelIntentPayloadBase doc/comment corrections for OMN-15212.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2483 --repo OmniNode-ai/omnibase_infra --json number,state"
  - id: "dod-infra2483-claim-fix-cc97c657"
    description: >
      Falsifiable product-head probe for omnibase_infra PR #2483: CLAUDE.md and docs/standards/ONEX_TERMINOLOGY.md
      at the pinned head both carry the corrected never-removed statement introduced by this PR (absent
      on the dev base, so this check is RED against any ref without the fix).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/CLAUDE.md?ref=cc97c65790e70d6eb77ef04192145807ebdb05a4'
          --jq .content | base64 -d | grep -q 'The class itself was never removed' && gh api 'repos/OmniNode-ai/omnibase_infra/contents/docs/standards/ONEX_TERMINOLOGY.md?ref=cc97c65790e70d6eb77ef04192145807ebdb05a4'
          --jq .content | base64 -d | grep -q 'the class itself was never removed'
  - id: "dod-occ-self"
    description: >
      OCC companion self-bind for OMN-15212 (deterministic commit-sha bind, no PR-number prediction).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15212/dod-occ-self/command.yaml"
