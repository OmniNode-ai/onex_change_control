---
schema_version: "1.0.0"
ticket_id: "OMN-16086"
title: "Bind the omnimarket omnibase-infra 0.38.7 (post-#2736) re-bump for the onex-dev runtime deploy"
summary: >-
  OCC companion for OmniNode-ai/omnimarket#2079. Re-bumps the omnibase-infra git rev in omnimarket [tool.uv.sources]
  + uv.lock from 94247acf (v0.38.6, the OMN-16077 deploy lineage, pre-#2736) to 254c3672471261c596c565245fb51ac9d951c494
  — the OmniNode-ai/omnibase_infra#2736 merge commit on dev (gateway session expiry enforcement; node_gateway_attach_effect
  contract 0.3 -> 0.4, ModelGatewayHeartbeatResponse.revoked -> termination_reason; resolved version 0.38.6
  -> 0.38.7). This is the lineage the post-#2736 onex-dev runtime image rebuild and deploy consume. Deploy-order
  safety at bind time: the onex-api edge tolerant window (omninode_infra#907) is merged and deployed to
  onex-dev (edge accepts both heartbeat shapes), and the gateway_sessions terminal-states migration (EXPIRED/QUARANTINED
  CHECK extensions) is applied (deploy run 31915842120, applied=1). Topic surface is unchanged at this
  rev: the contract-topic-graph gate re-ran clean (exit 0, 737 contracts / 1547 topics) on the full corpus
  at the new pin — the OMN-16077 external_producers/external_consumers declarations already cover node_gateway_attach_effect.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2079 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2079"
    description: >-
      PR #2079 on OmniNode-ai/omnimarket carries the 0.38.7 (post-#2736) re-bump.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2079 --repo OmniNode-ai/omnimarket --json number,state"
  - id: "dod-market2079-rebump-exact-head-and-scope"
    description: >-
      omnimarket PR #2079 is bound at exact reviewed head 91bdff4a4cbf57d6825b1c8a4f9f6ede15ba94a0 with
      its complete two-file scope: the dependency-pin files pyproject.toml and uv.lock, nothing else —
      the pin move surfaced no gate or test adaptations (topic-graph gate clean at the new rev).
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 2079 --repo OmniNode-ai/omnimarket --json headRefOid,files --jq '(.headRefOid == "91bdff4a4cbf57d6825b1c8a4f9f6ede15ba94a0") and ([.files[].path] | sort == ["pyproject.toml", "uv.lock"])' | grep -x true
  - id: "dod-pin-is-2736-merge-commit"
    description: >-
      The pyproject [tool.uv.sources] rev at the PR head is exactly 254c3672471261c596c565245fb51ac9d951c494,
      and that SHA is the merge commit of OmniNode-ai/omnibase_infra#2736 (MERGED) — so the pinned lineage
      contains the revoked -> termination_reason contract rev by construction.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/pyproject.toml?ref=91bdff4a4cbf57d6825b1c8a4f9f6ede15ba94a0 --jq '.content' | base64 -d | grep -c 'rev = "254c3672471261c596c565245fb51ac9d951c494"' | grep -qx 1 && gh pr view 2736 --repo OmniNode-ai/omnibase_infra --json state,mergeCommit --jq '(.state == "MERGED") and (.mergeCommit.oid == "254c3672471261c596c565245fb51ac9d951c494")' | grep -x true
