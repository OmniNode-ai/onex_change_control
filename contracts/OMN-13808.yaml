---
schema_version: "1.0.0"
ticket_id: "OMN-13808"
title: "fix(OMN-13808): provision omnimarket generation command topic on runtime lane"
summary: >-
  omnibase_infra PR #2169 adds the consumer-only omnimarket generation command topic to the runtime lane's
  explicit services/topics.yaml provisioning manifest. The topic is not produced on that lane before subscription,
  so the broker does not auto-create it and node_generation_consumer cannot bind.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-topic-manifest"
    description: >-
      services/topics.yaml lists onex.cmd.omnimarket.node-generation-requested.v1 as an explicit runtime
      provisioning target.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13808/dod-topic-manifest/command.yaml"
  - id: "dod-product-tests"
    description: >-
      PR #2169 records focused generation-topic tests and topic config parity checks passing for the manifest
      change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13808/dod-product-tests/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for the runtime lane topic provisioning change. No live deployment is performed
      in this PR; post-merge operator validation must prove the topic exists and the effects-profile consumer
      binds.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13808/dod-deploy-assessment/command.yaml"
  - id: "dod-pr-2169-binding"
    description: >-
      OmniNode-ai/omnibase_infra PR #2169 carries the OMN-13808 fix at feature commit 595471d6dbd3c0877480dfab6ca2876536ef68c2.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13808/dod-pr-2169-binding/command.yaml"
  - id: "dod-occ-self"
    description: >-
      onex_change_control PR carries this central OMN-13808 contract and PASS receipts for downstream
      omnibase_infra PR #2169.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13808/dod-occ-self/command.yaml"
