---
schema_version: "1.0.0"
ticket_id: "OMN-17800"
title: "Autobind OCC evidence for OMN-17800"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3165.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3165 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3165 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: "uv run pytest tests/scripts/test_reconcile_cwd_and_receipt_omn17800.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3165"
    description: "PR #3165 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/scripts/test_reconcile_cwd_and_receipt_omn17800.py?ref=d2771b641e3fea16db1ad0905c3d41ff3a1f7b7a --jq '.content' | base64 -d | grep -c 'def _install_shim_recording_cwd'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3165/files --paginate --jq '.[].filename' | grep -cFx 'scripts/check_reconciler_privilege.py'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3165 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/scripts/test_reconcile_cwd_and_receipt_omn17800.py -q"
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-8157"
    description: "Binds OMN-17800 to OCC companion PR #8157 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8157 --repo OmniNode-ai/onex_change_control --json number,state"
