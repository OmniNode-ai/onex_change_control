---
schema_version: "1.0.0"
ticket_id: "OMN-12833"
title: "Baseline omniintelligence freestanding imperative findings for dependency PR"
summary: >-
  Adds the existing live freestanding imperative findings in omniintelligence to the central imperative-contract
  allowlist so dependency-only PRs can be evaluated against a stable baseline while still reporting non-live
  findings.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-freestanding-allowlist-baseline"
    description: >-
      The omniintelligence freestanding imperative guard reports zero live findings after applying the
      central allowlist baseline.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12833/dod-freestanding-allowlist-baseline/command.yaml"
  - id: "dod-dependabot-pr-green-code-checks"
    description: >-
      omniintelligence PR #701 is dependency-only and its repository code checks are green; the remaining
      blockers are central evidence and imperative baseline gates.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12833/dod-dependabot-pr-green-code-checks/command.yaml"
  - id: "dod-replacement-pr-703-bound"
    description: >-
      omniintelligence PR #703 carries the same dependency-only omnibase-core bound update as superseded
      Dependabot PR #701 on an OMN-named branch, with local metadata validation passing.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12833/dod-replacement-pr-703-bound/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-12833 contract, receipt, and allowlist
      baseline."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12833/dod-occ-pr-self/command.yaml"
  - id: "dod-occ-pr-703-followup-self"
    description: "Self-binding receipt for the OCC follow-up PR carrying the #703-bound replacement evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12833/dod-occ-pr-703-followup-self/command.yaml"
  # --- A2.5 dashboard one-backend sweep (close-the-loop plan track A2.5) ---
  # Same ticket id, separate work scope: omnidash A2.5 component correctness sweep
  # through the single standard stability-test projection backend.
  - id: "dod-omnidash-pr"
    description: >-
      omnidash A2.5 PR classifies all 32 registry components through the single standard projection backend
      (8 visible/projection-backed, 2 visible/degraded, 22 hidden/disabled), adds paletteVisibility +
      authorityLabel + the one-backend base resolver. Full vitest suite passing (130 files / 1239 tests),
      tsc --noEmit clean, eslint --max-warnings=0 clean.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12833/dod-omnidash-pr/command.yaml"
  - id: "dod-occ-evidence"
    description: >-
      Playwright network proof for the A2.5 sweep: every /projection/{topic} request hits the single backend
      origin; ZERO requests to :8765, :3010, :3002, committed fixtures, or bespoke routes.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12833/dod-occ-evidence/command.yaml"
