---
schema_version: "1.0.0"
ticket_id: OMN-11315
title: "Stability delegate-skill smoke loses consumer membership before terminal event"
summary: "Extend the stability-test runtime Kafka poll budget so long delegate-skill LLM handlers do not
  lose consumer group membership before emitting terminal events."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Kafka timeout config and stability lane regression tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11315/dod-targeted-tests/command.yaml"
  - kind: "ci"
    description: "Touched runtime config and stability lane files pass ruff checks"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11315/dod-ruff-check/command.yaml"
  - kind: "manual"
    description: "omnibase_infra PR carrying the stability poll interval fix is open"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11315/dod-infra-pr/command.yaml"
  - kind: "manual"
    description: "OCC PR carrying this contract and receipt set is open"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11315/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-targeted-tests"
    description: "Kafka timeout config and stability lane regression tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11315/dod-targeted-tests/command.yaml
          && grep -q '^ticket_id: OMN-11315$' drift/dod_receipts/OMN-11315/dod-targeted-tests/command.yaml
          && grep -q '^evidence_item_id: dod-targeted-tests$' drift/dod_receipts/OMN-11315/dod-targeted-tests/command.yaml"
  - id: "dod-ruff-check"
    description: "Touched runtime config and stability lane files pass ruff checks"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11315/dod-ruff-check/command.yaml
          && grep -q '^ticket_id: OMN-11315$' drift/dod_receipts/OMN-11315/dod-ruff-check/command.yaml
          && grep -q '^evidence_item_id: dod-ruff-check$' drift/dod_receipts/OMN-11315/dod-ruff-check/command.yaml"
  - id: "dod-infra-pr"
    description: "omnibase_infra PR carrying the stability poll interval fix is open"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11315/dod-infra-pr/command.yaml
          && grep -q '^ticket_id: OMN-11315$' drift/dod_receipts/OMN-11315/dod-infra-pr/command.yaml &&
          grep -q '^evidence_item_id: dod-infra-pr$' drift/dod_receipts/OMN-11315/dod-infra-pr/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carrying this contract and receipt set is open"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11315/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: OMN-11315$' drift/dod_receipts/OMN-11315/dod-occ-pr/command.yaml && grep
          -q '^evidence_item_id: dod-occ-pr$' drift/dod_receipts/OMN-11315/dod-occ-pr/command.yaml"
