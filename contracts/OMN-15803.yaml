---
schema_version: "1.0.0"
ticket_id: "OMN-15803"
title: "Autobind OCC evidence for OMN-15803"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2033.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2033 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2033"
    description: "PR #2033 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2033 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2033 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2033 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2033 --repo OmniNode-ai/omnimarket --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-2033"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6295"
    description: "Binds OMN-15803 to OCC companion PR #6295 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6295 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2033-merged"
    description: >-
      omnimarket PR #2033 (OMN-15803) MERGED via squash to dev, merge_commit f00d2e451d8941adcd57bb4020d3cfb8f3eb8c21
      (single-parent onto 362e8ae1211ee20c16b86988510934ded8c17111). Squash changes the SHA that OCC#6295's
      pre-merge self-bind cited (per reference_occ_squash_invalidates_evidence_source), so this entry
      rebinds Evidence-Source to the actual merged commit. RED-first proof re-derived independently in
      this pass, not taken from the PR body: cherry-picked the new test file tests/unit/nodes/node_delegate_skill_orchestrator/test_local_dispatch_tier_skip_omn15803.py
      onto the pre-fix parent 362e8ae1 (source unchanged) — both test_attempt_tier_labels_are_routing_authority_members_not_bifrost_labels
      and test_escalation_never_reattempts_the_identical_backend FAILED there (2 failed), the second failing
      with the exact identical-(backend_id, model_id) re-dispatch assertion the fix targets. Ran the same
      file unmodified at the merged head f00d2e45 in a separate worktree — 2 passed, 0 failed. Both runs
      used the omnimarket canonical .venv interpreter, zero source edits beyond the checkout itself.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2033 --repo OmniNode-ai/omnimarket --json state,mergedAt,mergeCommit --jq 'if (.state
          == "MERGED" and .mergeCommit.oid == "f00d2e451d8941adcd57bb4020d3cfb8f3eb8c21") then "true"
          else error("expected #2033 MERGED at f00d2e451d8941adcd57bb4020d3cfb8f3eb8c21, got " + (.state
          // "null") + "/" + (.mergeCommit.oid // "null")) end'
  - id: "occ-self-bind-pr-2033-rebind"
    description: "Binds OMN-15803 to the current OCC companion PR for the merged-sha (f00d2e45) rebind
      of omnimarket PR #2033."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-6295"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr list --repo OmniNode-ai/onex_change_control --head jonahgabriel/omn-15803-occ-pr2033-rebind-merged
          --state all --json number,state,headRefName --jq '.[0] | if (.state == "OPEN" or .state == "MERGED")
          then "true" else error("no OCC companion PR found for branch jonahgabriel/omn-15803-occ-pr2033-rebind-merged")
          end'
