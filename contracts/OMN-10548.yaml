---
schema_version: "1.0.0"
ticket_id: "OMN-10548"
title: "feat: omnimarket config settings with empty defaults"
summary: "Add the canonical omnimarket Settings layer with empty defaults, per-service enable flags, structured
  required-service validation, cached access, and tests covering forbidden literal absence."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "Receipt Gate passes on omnimarket PR #537 after this OCC evidence lands."
    command: "gh pr checks 537 --repo OmniNode-ai/omnimarket --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Settings unit tests cover empty defaults, per-service validation, aliases, DSN precedence,
      forbidden literals, and cache behavior."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10548/dod-001/command.yaml\"; else uv run pytest
          tests/unit/config/ -v; fi"
  - id: "dod-002"
    description: "Settings module exists and does not hard-code known dev-machine, internal host, or vendor
      literal classes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10548/dod-002/command.yaml\"; else test -f src/omnimarket/config/settings.py
          && ! grep -R -E '(/Users/jonah|/Volumes/PRO-G40|192\\.168\\.|10\\.|172\\.16\\.|dash\\.dev\\.omninode\\.ai|onreviewbot@gmail\\.com|jonahgabriel|cyankiwi|Corianas)'
          src/omnimarket/config/settings.py; fi"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10548 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10548/dod-occ-pr/command.yaml\""
