---
schema_version: "1.0.0"
ticket_id: "OMN-13502"
title: "no-faked-boundary detector — exempt canonical recorded-replay seam + honor file-level marker"
summary: >-
  Evidence for the WS-F boundary-fake burn-down F-ENABLE step 1. The
  check-no-faked-boundary detector (OMN-13497) false-flagged the canonical
  golden-chain replay seam (patch("httpx.Client", return_value=<RecordedReplayInferenceTransport>),
  OMN-13499) as a boundary fake and flagged its own detector source and the
  acceptance-corpus test. The fix makes scan_source honor the file-level
  onex-allow-file-faked-boundary marker and exempt the canonical recorded-replay
  patch seam, so fully-migrated golden-chain files stop tripping the gate and
  omnibase_core is clean under the detector — the prerequisite for the wedge-safe
  required-gate flip in F-ENABLE step 2.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1377"
    description: >-
      omnibase_core PR #1377 carries the no-faked-boundary detector fix (canonical
      recorded-replay seam exemption + file-level marker) for OMN-13502.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13502/dod-omnibase-core-pr-1377/command.yaml
          && grep -q '^pr_number: 1377$' drift/dod_receipts/OMN-13502/dod-omnibase-core-pr-1377/command.yaml
          && grep -q '0fa9598bbad7dc59c8e71e3a7297b22675e2d4b6' drift/dod_receipts/OMN-13502/dod-omnibase-core-pr-1377/command.yaml
  - id: "dod-occ-self"
    description: >-
      This onex_change_control PR carries the OMN-13502 contract and PASS receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13502/dod-occ-self/command.yaml
          && grep -q '^ticket_id: "OMN-13502"$' drift/dod_receipts/OMN-13502/dod-occ-self/command.yaml
