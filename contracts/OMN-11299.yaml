---
schema_version: "1.0.0"
ticket_id: "OMN-11299"
title: "Project delegate-skill terminal metrics into delegation and savings projections"
summary: >
  Adds delegate-skill-completed and delegate-skill-failed topic subscriptions to the delegation and savings
  projection handlers. Materializes typed terminal event metrics (token counts, cost, quality gates) into
  delegation_events and savings_estimates projections. Bound to omnimarket PR #721.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-721-head"
    description: "omnimarket PR #721 head commit carries the projection handler implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11299/dod-omnimarket-721-head/command.yaml\""
  - id: "dod-tests"
    description: "Projection handler tests pass including delegate-skill terminal event cases."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11299/dod-tests/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carrying this contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11299/dod-occ-pr/command.yaml\""
  - id: "dod-infra-1678"
    description: "omnibase_infra PR #1678 — composite projection upsert key fix and integration test."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnibase_infra --json headRefOid,title,url"
