---
schema_version: '1.0.0'
ticket_id: OMN-13689
title: 'Delete PluginJsonNormalizer: remove PluginComputeBase migration debt from omnibase_infra'
summary: >-
  Deletes omnibase_infra/src/omnibase_infra/plugins/examples/plugin_json_normalizer.py (PluginJsonNormalizer
  extending PluginComputeBase) — documented canonical architecture migration debt (CLAUDE.md, OMN-12525).
  PluginComputeBase is legacy; no new subclasses are permitted. PluginJsonNormalizer has no callers outside
  its own package. Removes the export from plugins/examples/__init__.py and deletes all tests that exclusively
  tested the deleted plugin. No deploy, runtime mutation, or live infra change is performed by this PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: manual
    description: >-
      plugin_json_normalizer.py deleted; PluginJsonNormalizer removed from plugins/examples/__init__.py;
      grep confirms no external callers across all $OMNI_HOME/*/src repos.
    command: 'test ! -f src/omnibase_infra/plugins/examples/plugin_json_normalizer.py && (grep -r PluginJsonNormalizer
      $OMNI_HOME/*/src 2>/dev/null | grep -v plugin_json_normalizer || echo EMPTY)'
  - kind: tests
    description: >-
      27 plugin unit tests + integration dead-subsystem test pass. 20436 unit tests pass.
    command: 'uv run pytest tests/unit/plugins/ tests/integration/test_omn_11564_dead_subsystem_removal.py
      -q'
  - kind: ci
    description: CI pipeline green on PR
    command: 'gh pr checks --repo OmniNode-ai/omnibase_infra'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >-
      plugin_json_normalizer.py deleted from src; PluginJsonNormalizer export removed from plugins/examples/__init__.py;
      package module is empty.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13689/dod-001/command.yaml"
  - id: dod-002
    description: >-
      No external repo imports PluginJsonNormalizer (grep -r PluginJsonNormalizer $OMNI_HOME/*/src returns
      empty).
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13689/dod-002/command.yaml"
  - id: dod-003
    description: >-
      27 plugin unit + integration tests pass; 20436 unit tests pass with no regressions introduced by
      this deletion.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13689/dod-003/command.yaml"
  - id: dod-deploy-assessment
    description: >-
      Static deploy assessment: no live deploy, runtime restart, ECR push, or cluster mutation performed
      by OMN-13689. Pure source deletion + test cleanup.
    source: generated
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13689/dod-deploy-assessment/command.yaml &&
          grep -qi 'deploy' drift/dod_receipts/OMN-13689/dod-deploy-assessment/command.yaml && grep -qi
          'docker exec' drift/dod_receipts/OMN-13689/dod-deploy-assessment/command.yaml
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt — this onex_change_control PR carries the OMN-13689 contract + receipts
      consumed by omnibase_infra PR via Evidence-Source.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13689/dod-occ-self/command.yaml"
