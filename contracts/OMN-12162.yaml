---
schema_version: "1.0.0"
ticket_id: "OMN-12162"
title: "fix(OMN-12162): Resolve stub handler_registry.register_handlers_from_config"
summary: >
  Remove the no-op stub register_handlers_from_config from handler_registry.py and its re-export from
  runtime/__init__.py. The stub had zero actual callers — only defined and re-exported, never invoked.
  Real handler registration is fully implemented via wire_from_manifest in auto_wiring/handler_wiring.py.
  No new nodes, topics, or runtime services introduced. Pure removal with 3 confirming unit tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: "43 handler_registry tests pass + 3 new stub-removal tests."
    command: >
      cd omnibase_infra && uv run pytest tests/unit/runtime/test_handler_registry_stub_removed.py tests/unit/runtime/test_registry_race_conditions.py
      -v 2>&1 | tail -5
  - kind: manual
    description: "omnibase_infra PR #1753 carries the stub removal implementation."
    command: gh pr checks 1753 --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "43 handler_registry tests + 3 new stub-removal assertion tests pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12162/dod-unit-tests/command.yaml\""
  - id: "dod-no-deploy"
    description: >
      No deployable artifact. This ticket removes a no-op stub with zero callers. No new nodes, topics,
      schemas, or runtime services introduced. pre-commit hooks pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12162/dod-no-deploy/command.yaml\""
