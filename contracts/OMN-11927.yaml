---
schema_version: "1.0.0"
ticket_id: "OMN-11927"
title: "node_model_router and shared routing DTOs"
summary: >
  Bind central evidence for shared LLM routing DTOs and downstream node_model_router route evidence, including
  route-resolved/rejected topic additions.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
  - "events"
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1145"
    description: "Bind OMN-11927 evidence to omnibase_core PR #1145."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11927/dod-omnibase-core-pr-1145/command.yaml
          && grep -q '^pr_number: 1145$' drift/dod_receipts/OMN-11927/dod-omnibase-core-pr-1145/command.yaml"
  - id: "dod-routing-dto-local-validation"
    description: "Focused routing DTO validation passes for the current omnibase_core PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11927/dod-routing-dto-local-validation/command.yaml
          && grep -q '4 passed' drift/dod_receipts/OMN-11927/dod-routing-dto-local-validation/command.yaml"
  - id: "dod-routing-dto-ci-validation"
    description: "GitHub naming and deploy gates pass on the current omnibase_core PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11927/dod-routing-dto-ci-validation/command.yaml
          && grep -q 'Naming Convention Validation.*SUCCESS' drift/dod_receipts/OMN-11927/dod-routing-dto-ci-validation/command.yaml"
  - id: "dod-omnimarket-pr-814"
    description: "omnimarket PR #814 emits node_model_router route evidence with shared DTO dependency
      alignment."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11927/dod-omnimarket-pr-814/command.yaml
          && grep -q '^pr_number: 814$' drift/dod_receipts/OMN-11927/dod-omnimarket-pr-814/command.yaml"
  - id: "dod-occ-pr-1523"
    description: "OCC PR #1523 carries the central OMN-11927 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11927/dod-occ-pr-1523/command.yaml
          && grep -q '^pr_number: 1523$' drift/dod_receipts/OMN-11927/dod-occ-pr-1523/command.yaml"
  - id: "dod-deploy-runtime-proof"
    description: "Deploy-style runtime proof exercises the omnimarket local runtime CLI after routing
      DTO dependency alignment."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11927/dod-deploy-runtime-proof/command.yaml
          && grep -q 'deploy runtime proof' drift/dod_receipts/OMN-11927/dod-deploy-runtime-proof/command.yaml"
  - id: "dod-omnimarket-pr-830"
    description: "omnimarket PR #830 adds route-resolved/rejected topics to node_model_router contract."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11927/dod-omnimarket-pr-830/command.yaml
          && grep -q '^pr_number: 830$' drift/dod_receipts/OMN-11927/dod-omnimarket-pr-830/command.yaml"
  - id: "dod-omnibase-core-pr-1150"
    description: "omnibase_core PR #1150 adds shared LLM routing DTOs."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11927/dod-omnibase-core-pr-1150/command.yaml
          && grep -q '^pr_number: 1150$' drift/dod_receipts/OMN-11927/dod-omnibase-core-pr-1150/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness for node_model_router contract topic additions."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11927/dod-deploy-readiness/command.yaml
          && grep -q 'node_model_router contract topic additions have focused unit test coverage' drift/dod_receipts/OMN-11927/dod-deploy-readiness/command.yaml"
