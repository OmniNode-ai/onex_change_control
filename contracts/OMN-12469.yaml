---
schema_version: "1.0.0"
ticket_id: "OMN-12469"
title: "SEA routing contract + node_model_routing_compute (foundation PR)"
summary: >
  Add the SEA route-construction contract and node_model_routing_compute (Phase 0 + Phase 1 of the SEA
  node-ization plan, epic OMN-12468). The routing contract owns route construction only: the local-first
  model ladder with cloud_opt_in=false, the three verbatim prompt blocks (node_generator_v1 with sandbox
  lines, node_generator_eval_v1 without them, self_extending_agent_v1), and generation params (max_tokens=2048
  moved out of consumer.py and eval_runner.py). The YAML loader rejects unknown keys; route resolution
  fails closed (ROUTE_NOT_FOUND, CLOUD_NOT_OPTED_IN) with no silent downgrade.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-routing-pr-167"
    description: >
      onex-self-extending-agent PR #167 adds node_model_routing_compute, the routing contract YAML + typed
      loader (extra=forbid, unknown keys rejected), the sea-routing topics, ModelModelRouteRequested and
      ModelResolvedRoute, the typed RoutingError taxonomy, and 14 passing unit tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12469/dod-sea-routing-pr-167/command.yaml"
