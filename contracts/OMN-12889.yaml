---
schema_version: "1.0.0"
ticket_id: "OMN-12889"
title: "Delegation quality gate accepts source-free accurate summaries"
summary: >-
  Central evidence for omnimarket PR #1141. The fix replaces marker-word based `accurate` gating for source-free
  summarization tasks with explicit accuracy-disclaimer detection, preserving honest failures while preventing
  matching summaries from failing only because they omit words like "evidence", "verified", "based on",
  or "line".
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-quality-tests"
    description: "Focused delegation quality-gate tests pass locally for the OMN-12889 patch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-focused-quality-tests/command.yaml"
          && grep -q '32 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-focused-quality-tests/command.yaml"
  - id: "dod-ruff-quality-files"
    description: "Changed quality-gate files pass ruff check and format verification."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-ruff-quality-files/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-ruff-quality-files/command.yaml"
          && grep -q '2 files already formatted' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-ruff-quality-files/command.yaml"
  - id: "dod-omnimarket-pr-1141"
    description: "omnimarket PR #1141 carries the quality-gate fix and repo-local deploy contract."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-omnimarket-pr-1141/command.yaml"
          && grep -q '^pr_number: 1141$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-omnimarket-pr-1141/command.yaml"
          && grep -q 'bb06030452e99446757b6d412d89315f346397c3' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-omnimarket-pr-1141/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for the runtime-path handler change: deterministic handler-only quality-gate logic,
      no Kafka topic creation, database migration, secret mutation, or live runtime restart before merge;
      stability-test rerun is required after deploy because historical projection rows do not change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-deploy-assessment/command.yaml"
          && grep -q 'deploy-assessment' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2408"
    description: "onex_change_control PR #2408 carries the central OMN-12889 contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-occ-pr-2408/command.yaml"
          && grep -q '^pr_number: 2408$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-occ-pr-2408/command.yaml"
          && grep -q '7a18bf533eb9d8e9305a9d699f343d1707916a81' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12889/dod-occ-pr-2408/command.yaml"
