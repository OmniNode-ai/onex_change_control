---
schema_version: "1.0.0"
ticket_id: OMN-14000
title: "fix(omnimarket): ratchet burn-down handler-routing and stale allowlist entries"
summary: >
  OMN-14000 local-first ratchet evidence combines the gate-verified batch-1 handler_routing schema migration
  with the phase-3 stale imperative-contract allowlist sweep already merged to dev. The handler-routing
  batch fixes node_kb_adr_publisher, node_kreuzberg_parse_effect, and the follow-up node_agent_learning_retrieval_effect
  evidence. The stale-sweep evidence preserves the freestanding imperative-contract guard ratchet: compliant
  coverage increased, allowlisted coverage decreased, and zero new violations were introduced.
is_seam_ticket: false
interface_change: false
evidence_requirements:
  - kind: "tests"
    description: "focused node tests pass for the batch-1 fixed nodes"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-gate-verified-batch1/command.yaml"
  - kind: "manual"
    description: "omnimarket PR #1631 carries the batch-1 handler_routing fix"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-omnimarket-pr-1631/command.yaml"
  - kind: "manual"
    description: "omnimarket PR #1641 carries the follow-up local-delegate handler_routing fix"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-omnimarket-pr-1641/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR #3643 carries the batch-1 contract and receipt set"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-occ-pr-3643/command.yaml"
  - kind: "tests"
    description: "phase-3 stale allowlist removal is gate-verified with zero new violations"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-gate-verified-phase3-stale-sweep/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR #3666 carries the phase-3 stale-sweep contract and receipts"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-occ-pr-3666/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-gate-verified-batch1"
    description: >-
      arch_handler_contract_compliance gate: Compliant 336 -> 338 (+2), New violations 95 -> 93 (-2),
      zero new violations introduced; focused node tests (tests/nodes/node_kreuzberg_parse_effect/, tests/integration/node_kb_adr_publisher/)
      15 passed; full local gates (ruff, mypy --strict, full pytest) clean apart from pre-existing infra-dependent
      failures.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-gate-verified-batch1/command.yaml
          && grep -q '^ticket_id: OMN-14000$' drift/dod_receipts/OMN-14000/dod-gate-verified-batch1/command.yaml"
  - id: "dod-omnimarket-pr-1631"
    description: "omnimarket PR #1631 carries the batch-1 handler_routing fix (node_kb_adr_publisher,
      node_kreuzberg_parse_effect)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-omnimarket-pr-1631/command.yaml
          && grep -q '^pr_number: 1631$' drift/dod_receipts/OMN-14000/dod-omnimarket-pr-1631/command.yaml"
  - id: "dod-omnimarket-pr-1641"
    description: "omnimarket PR #1641 carries the follow-up local-delegate handler_routing fix for node_agent_learning_retrieval_effect"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-omnimarket-pr-1641/command.yaml
          && grep -q '^pr_number: 1641$' drift/dod_receipts/OMN-14000/dod-omnimarket-pr-1641/command.yaml"
  - id: "dod-deploy-scope"
    description: >-
      Deploy-scope binding: this runtime-path contract schema migration rides the normal post-merge dev
      and stability redeploy cadence; no out-of-band production deploy is in scope. The .201 dev-lane
      runtime containers were observed healthy for post-merge validation.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-deploy-scope/command.yaml
          && grep -q '^evidence_item_id: dod-deploy-scope$' drift/dod_receipts/OMN-14000/dod-deploy-scope/command.yaml"
  - id: "dod-occ-pr-3643"
    description: "onex_change_control PR #3643 carries this OMN-14000 batch-1 contract and receipt set"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-occ-pr-3643/command.yaml
          && grep -q '^pr_number: 3643$' drift/dod_receipts/OMN-14000/dod-occ-pr-3643/command.yaml"
  - id: "dod-gate-verified-phase3-stale-sweep"
    description: >-
      Imperative contract guard verified this stale-sweep as a pure ratchet: compliant_count 180 -> 209
      (+29), allowlisted_count 256 -> 227 (-29), new_violation_count 0 -> 0, exit 0.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-gate-verified-phase3-stale-sweep/command.yaml
          && grep -q '^evidence_item_id: dod-gate-verified-phase3-stale-sweep$' drift/dod_receipts/OMN-14000/dod-gate-verified-phase3-stale-sweep/command.yaml"
  - id: "dod-occ-pr-3666"
    description: "onex_change_control PR #3666 carries this OMN-14000 phase-3 stale-sweep contract and
      receipt set"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-occ-pr-3666/command.yaml
          && grep -q '^pr_number: 3666$' drift/dod_receipts/OMN-14000/dod-occ-pr-3666/command.yaml"
