---
schema_version: "1.0.0"
ticket_id: OMN-14000
title: "fix(omnimarket): ratchet burn-down batch 1 — missing_handler_routing schema migration (2 nodes)"
summary: >
  First gate-verified batch of the local-first ratchet burn-down lane (OMN-14000). The
  arch_handler_contract_compliance gate only recognizes the nested handler_routing.handlers[].handler
  schema; node_kb_adr_publisher and node_kreuzberg_parse_effect were still on the legacy flat
  handler_class/handler_module string schema and false-flagged as missing_handler_routing. Both fixes
  were generated by the LOCAL delegate tier (Qwen3.6-35B-A3B via .201, onex delegate --task-type
  document) — 2/2 first-try, gate-verified, no cloud escalation — the flagship local-first proof for
  this lane. A larger 6-node batch was narrowed to these 2 after 4 other candidates were found coupled
  to an unrelated state-coverage-gate baseline grandfather clause (tracked separately as OMN-14009).
is_seam_ticket: false
interface_change: false
evidence_requirements:
  - kind: "tests"
    description: "focused node tests pass for both fixed nodes"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-gate-verified-batch1/command.yaml"
  - kind: "manual"
    description: "omnimarket PR #1631 carries the batch-1 handler_routing fix"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-omnimarket-pr-1631/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR #3643 carries this OMN-14000 contract and receipt set"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-occ-pr-3643/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-gate-verified-batch1"
    description: >-
      arch_handler_contract_compliance gate: Compliant 336 -> 338 (+2), New violations 95 -> 93 (-2),
      zero new violations introduced; focused node tests (tests/nodes/node_kreuzberg_parse_effect/,
      tests/integration/node_kb_adr_publisher/) 15 passed; full local gates (ruff, mypy --strict,
      full pytest) clean apart from pre-existing infra-dependent failures.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-gate-verified-batch1/command.yaml
          && grep -q '^ticket_id: OMN-14000$' drift/dod_receipts/OMN-14000/dod-gate-verified-batch1/command.yaml"
  - id: "dod-omnimarket-pr-1631"
    description: "omnimarket PR #1631 carries the batch-1 handler_routing fix (node_kb_adr_publisher, node_kreuzberg_parse_effect)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-omnimarket-pr-1631/command.yaml
          && grep -q '^pr_number: 1631$' drift/dod_receipts/OMN-14000/dod-omnimarket-pr-1631/command.yaml"
  - id: "dod-occ-pr-3643"
    description: "onex_change_control PR #3643 carries this OMN-14000 contract and receipt set"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-occ-pr-3643/command.yaml
          && grep -q '^pr_number: 3643$' drift/dod_receipts/OMN-14000/dod-occ-pr-3643/command.yaml"
