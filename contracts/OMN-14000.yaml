---
schema_version: "1.0.0"
ticket_id: OMN-14000
title: "fix(onex_change_control): ratchet stale omnimarket imperative-contract allowlist entries"
summary: >
  Phase-3 stale-sweep for the OMN-14000 local-first ratchet lane. This OCC-only change removes 29 omnimarket
  imperative-contract allowlist entries whose underlying handlers are already compliant. The freestanding
  imperative-contract guard is the source of truth: removing these entries increases compliant coverage,
  decreases allowlisted coverage, and introduces zero new violations.
is_seam_ticket: false
interface_change: false
evidence_requirements:
  - kind: "tests"
    description: "phase-3 stale allowlist removal is gate-verified with zero new violations"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-gate-verified-phase3-stale-sweep/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR #3666 carries the phase-3 stale-sweep contract and receipts"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-occ-pr-3666/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-gate-verified-phase3-stale-sweep"
    description: >-
      Imperative contract guard verified this stale-sweep as a pure ratchet: compliant_count 180 -> 209
      (+29), allowlisted_count 256 -> 227 (-29), new_violation_count 0 -> 0, exit 0.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-gate-verified-phase3-stale-sweep/command.yaml
          && grep -q '^evidence_item_id: dod-gate-verified-phase3-stale-sweep$' drift/dod_receipts/OMN-14000/dod-gate-verified-phase3-stale-sweep/command.yaml"
  - id: "dod-occ-pr-3666"
    description: "onex_change_control PR #3666 carries this OMN-14000 phase-3 stale-sweep contract and
      receipt set"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14000/dod-occ-pr-3666/command.yaml
          && grep -q '^pr_number: 3666$' drift/dod_receipts/OMN-14000/dod-occ-pr-3666/command.yaml"
