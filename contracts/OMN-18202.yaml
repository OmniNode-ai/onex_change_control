---
schema_version: "1.0.0"
ticket_id: "OMN-18202"
title: "Autobind OCC evidence for OMN-18202"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #3448."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3448 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #3448 on OmniNode-ai/omnibase_infra changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (docker/runners/runner-image.lock.json, pyproject.toml, src/omnibase_infra/runtime/version_compatibility.py, tests/fixtures/seams/core_release/0.46.11_expected_symbols.json) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3448"
    description: "PR #3448 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/uv.lock?ref=f85fbdebb62aedc9d0725dce5e933934038c32de --jq '.content' | base64 -d | grep -cF 'https://files.pythonhosted.org/packages/04/71/41250140db5e8558f2ebc91007dfcad80d3047da1923b000fb1c9a36ee0e/omnibase_core-0.47.12.tar.gz'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3448-ci"
    description: "PR #3448 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3448 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-9184"
    description: "OCC companion PR #9184 — self-bind for OMN-18202 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9184 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3998"
    description: "PR #3998 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/ci/test_dependency_cascade_no_self_stamp_omn18202.py?ref=473dafbb43f60206d5fff0811d0424d085f04e1f --jq '.content' | base64 -d | grep -c 'def _body_block'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3998/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/dependency-cascade.yml'
  - id: "occ-self-bind-pr-10917"
    description: "Binds OMN-18202 to OCC companion PR #10917 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 10917 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_core-pr-1768"
    description: "PR #1768 on OmniNode-ai/omnibase_core — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_core/contents/tests/scripts/test_dependency_cascade_matrix.py?ref=f004f2957d9fe833fa9ee48b48059b3d64ae3be5 --jq '.content' | base64 -d | grep -c 'def test_pr_body_does_not_recite_the_upstream_evidence_pair'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_core/pulls/1768/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/dependency-cascade.yml'
  - id: "occ-self-bind-pr-11231"
    description: "Binds OMN-18202 to OCC companion PR #11231 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 11231 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-occ-admissibility-local-rb15392"
    description: |-
      Append-only replacement for dod-occ-evidence-admissibility-validator (OMN-15391 pattern). The
      superseded item's only check is 'uv run pytest tests/test_evidence_admissibility.py -q', a suite
      that exists only in onex_change_control. It declares no cwd, so since OMN-16824 the hosted gate
      runs it in the product checkout the PR changes. For omnibase_core#1768 that checkout has no such
      file and pytest exits 5 (no tests ran), which blocked Contract Compliance Check job 107965360503
      on 1 of 10 items. The failure says nothing about the product. This entry carries the same command
      unchanged and scopes it local_done_gate (OMN-15392), as contracts/OMN-15468.yaml does for a suite
      that lives in another repo: the hosted gate records it NOT-EVALUATED instead of guessing a
      directory, and the local Done gate runs it in the OCC checkout, where it passed 76 tests on
      2026-09-25. The product facts for this ticket stay on the hosted gate through the per-PR items
      above (the #3998 and #1768 content and file-list checks).
    source: "manual"
    execution_scope: "local_done_gate"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-evidence-admissibility-validator"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
