---
schema_version: "1.0.0"
ticket_id: OMN-10265
title: "Market-only skill baseline gate for omnimarket node contracts"
summary: "Market-only skill baseline gate for omnimarket node contracts"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Market skill smoke tests and strict baseline pass"
    command: "gh pr checks 440 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "Fast strict baseline reports all covered market skill surfaces working"
    command: "uv run python scripts/run_market_skill_baseline.py --skip-pytest --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Market skill direct-entrypoint smoke tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_market_skill_smokes.py -q"
  - id: "dod-002"
    description: "Strict market skill baseline reports zero degraded and zero failing surfaces"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/run_market_skill_baseline.py --skip-pytest --strict"
  - id: "dod-003"
    description: "Deploy-readiness smoke: changed node contracts import and strict baseline runner is
      CI-runnable before post-merge runtime deploy"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python -c \"from omnimarket.market_skill_baseline import iter_market_skill_specs;
          from omnimarket.nodes.node_aislop_sweep.handlers.handler_aislop_sweep import NodeAislopSweep;
          from omnimarket.nodes.node_pr_lifecycle_orchestrator.handlers.handler_pr_lifecycle_orchestrator
          import HandlerPrLifecycleOrchestrator; from omnimarket.nodes.node_pr_polish.workflow_runner
          import run_live_pr_polish; assert len(iter_market_skill_specs()) >= 7; print('deploy readiness
          smoke ok')\""
