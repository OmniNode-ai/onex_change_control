---
schema_version: "1.0.0"
ticket_id: "OMN-11914"
title: "Knowledge-base Repowise index effect"
summary: >
  Bind central evidence for omnimarket PR #810, which adds the node_kb_repowise_index_effect node for
  cloning the knowledge-base repository and invoking the Repowise index CLI with deterministic completion-event
  evidence.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-810"
    description: "Bind OMN-11914 evidence to omnimarket PR #810."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11914/dod-omnimarket-pr-810/command.yaml
          && grep -q '^pr_number: 810$' drift/dod_receipts/OMN-11914/dod-omnimarket-pr-810/command.yaml"
  - id: "dod-kb-repowise-local-validation"
    description: "Focused node unit tests, strict mypy, and metadata dependency scope checks pass locally."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11914/dod-kb-repowise-local-validation/command.yaml
          && grep -q '9 passed' drift/dod_receipts/OMN-11914/dod-kb-repowise-local-validation/command.yaml"
  - id: "dod-kb-repowise-precommit"
    description: "Changed-file pre-commit slice passes for the Repowise index effect node files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11914/dod-kb-repowise-precommit/command.yaml
          && grep -q 'Dependency health gate.*Passed' drift/dod_receipts/OMN-11914/dod-kb-repowise-precommit/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness assessment for the effectful Repowise indexing node."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11914/dod-deploy-readiness/command.yaml
          && grep -q 'dry_run=true exits without subprocess calls' drift/dod_receipts/OMN-11914/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr-1526"
    description: "OCC PR for OMN-11914 carries the central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11914/dod-occ-pr-1526/command.yaml
          && grep -q '^pr_number: 1526$' drift/dod_receipts/OMN-11914/dod-occ-pr-1526/command.yaml"
