---
schema_version: "1.0.0"
ticket_id: "OMN-11139"
title: "Task 3: YAML Config Loader"
summary: >
  Central OCC evidence for omnibase_core PR #1091, which adds pure YAML discovery and loading helpers
  for trusted OmniGate repository configuration files.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_core PR #1091 carries the OMN-11139 config loader change."
    command: "gh pr view 1091 --repo OmniNode-ai/omnibase_core --json state,headRefOid"
  - kind: "tests"
    description: "Focused config loader tests, lint, and mypy pass locally."
    command: "PYTHONPATH=$PWD/src uv run pytest tests/unit/gate/test_config_loader.py tests/unit/models/gate/test_model_omnigate_config.py
      -q && PYTHONPATH=$PWD/src uv run ruff check src/omnibase_core/gate tests/unit/gate/test_config_loader.py
      && PYTHONPATH=$PWD/src uv run mypy src/omnibase_core/gate/config_loader.py"
  - kind: "manual"
    description: "No runtime deploy is required for pure config loader changes."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11139/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1091"
    description: "Bind OMN-11139 evidence to omnibase_core PR #1091 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11139/dod-omnibase-core-pr-1091/command.yaml\"
          && grep -q '^pr_number: 1091$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11139/dod-omnibase-core-pr-1091/command.yaml\"
          && grep -q '^commit_sha: b83a553dc0744729e225462154c5c555eae06398$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11139/dod-omnibase-core-pr-1091/command.yaml\""
  - id: "dod-focused-checks"
    description: "Focused config loader tests, lint, and mypy pass locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11139/dod-focused-checks/command.yaml\"
          && grep -q '17 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11139/dod-focused-checks/command.yaml\"
          && grep -q 'Success: no issues found' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11139/dod-focused-checks/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "No live deploy, runtime restart, .201 mutation, or event-bus write was performed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11139/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11139/dod-deploy-assessment/command.yaml\""
