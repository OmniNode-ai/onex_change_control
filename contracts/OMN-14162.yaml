---
schema_version: "1.0.0"
ticket_id: "OMN-14162"
title: "fix(OMN-14162): remove hardcoded LAN Kafka default from env template"
summary: >-
  Central OCC evidence for omnibase_infra PR #2239. The downstream PR updates docs/env-master-template.env
  so local skill-dispatch users must choose the correct Kafka bootstrap address instead of copying a LAN-only
  Redpanda default that silently fails off-network. This evidence PR performs no live deploy, runtime
  restart, branch-protection mutation, or bypass.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2239"
    description: >-
      omnibase_infra PR #2239 is open against dev at head 8318e1d7cf1671feaa9d0f341e0e05f6b8b39267 and
      changes only docs/env-master-template.env.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14162/dod-omnibase-infra-pr-2239/command.yaml
          && grep -q '^pr_number: 2239$' drift/dod_receipts/OMN-14162/dod-omnibase-infra-pr-2239/command.yaml
          && grep -q '^commit_sha: 8318e1d7cf1671feaa9d0f341e0e05f6b8b39267$' drift/dod_receipts/OMN-14162/dod-omnibase-infra-pr-2239/command.yaml
  - id: "dod-template-validation"
    description: >-
      The downstream PR's targeted validation evidence confirms the Kafka no-hardcoded-fallback guard
      and applicable pre-commit hooks pass for the env template update.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14162/dod-template-validation/command.yaml &&
          grep -qi 'check_kafka_no_hardcoded_fallback' drift/dod_receipts/OMN-14162/dod-template-validation/command.yaml
          && grep -qi 'pre-commit run --files docs/env-master-template.env' drift/dod_receipts/OMN-14162/dod-template-validation/command.yaml
  - id: "dod-no-live-deploy"
    description: >-
      Scope evidence: OMN-14162 is a documentation/template correction plus central evidence only. The
      OCC PR performs no live deploy, no runtime restart, no branch-protection mutation, and no bypass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14162/dod-no-live-deploy/command.yaml && grep
          -qi 'no live deploy' drift/dod_receipts/OMN-14162/dod-no-live-deploy/command.yaml
