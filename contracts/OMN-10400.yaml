---
schema_version: "1.0.0"
ticket_id: OMN-10400
title: "Dogfood orchestrated merge sweep workflow"
summary: "Add and verify the market-owned orchestrated merge sweep workflow proof surfaces and live CI
  rerun handling."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Merge sweep triage and outcome classifier tests pass"
    command: "uv run pytest tests/test_triage_orchestrator.py tests/test_triage_phase2_emit_rules.py tests/test_sweep_outcome_classify.py
      -q"
  - kind: "manual"
    description: "Merge sweep runtime CLI dogfood completes against omnimarket open PRs"
    command: "uv run python scripts/run_orchestrated_merge_sweep_workflow.py --repos OmniNode-ai/omnimarket
      --execute"
  - kind: "manual"
    description: "Post-merge deploy smoke imports the orchestrated merge sweep workflow CLI in the runtime
      container"
    command: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"import scripts.run_orchestrated_merge_sweep_workflow
      as workflow; print(workflow.main.__name__)\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Merge sweep triage and outcome classifier tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_triage_orchestrator.py tests/test_triage_phase2_emit_rules.py
          tests/test_sweep_outcome_classify.py -q"
  - id: "dod-002"
    description: "Live orchestrated merge sweep dogfood completed against omnimarket PRs"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/run_orchestrated_merge_sweep_workflow.py --repos OmniNode-ai/omnimarket
          --execute"
  - id: "dod-003"
    description: "Post-merge deploy smoke imports the orchestrated merge sweep workflow CLI in the runtime
      container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"import scripts.run_orchestrated_merge_sweep_workflow
          as workflow; print(workflow.main.__name__)\""
