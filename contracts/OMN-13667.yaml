---
schema_version: "1.0.0"
ticket_id: "OMN-13667"
title: "Repoint delegation escalation ceiling to GLM-5.2 z.ai direct + resilience hardening"
summary: >
  Move the delegation escalation ceiling tier (claude slot) off free-tier AI Studio Gemini (cloud-gemini-pro,
  gemini-2.5-flash) which 503s on every escalation attempt, onto GLM-5.2 z.ai direct (cloud-glm, secret_ref
  llm.glm.api_key) — the proven backend already used by the judge lane and cheap_cloud tier. Adds a fallback
  model entry (openrouter-qwen3-coder-480b, separate provider, free tier) to eliminate single point of
  failure. Raises max_retries from 0 to 2 for resilience against transient z.ai errors. Updates cost model
  from free_local to metered (0.002/1k tokens). Contract-config-only change; no handler code changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1469-head"
    description: "omnimarket PR #1469 carries the ceiling tier repoint to cloud-glm + fallback + max_retries
      raise."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13667/dod-omnimarket-pr-1469-head/command.yaml\""
  - id: "dod-omnimarket-pr-1469-local-gates"
    description: "Full omnimarket delegation/routing test suite (472/472 pass) plus pre-commit clean."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13667/dod-omnimarket-pr-1469-local-gates/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carries the OMN-13667 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13667/dod-occ-pr-self/command.yaml\""
