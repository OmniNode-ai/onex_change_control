---
schema_version: "1.0.0"
ticket_id: "OMN-15230"
title: "Autobind OCC evidence for OMN-15230"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1923."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1923 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1923"
    description: "PR #1923 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1923-ci"
    description: "PR #1923 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5191"
    description: "OCC companion PR #5191 — self-bind for OMN-15230 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1923-compute-deploy-probe"
    description: >-
      Compute-only deploy-gate probe for OMN-15230. The product PR touches runtime handler modules but
      does not deploy a live service in this PR. The falsifiable probe reads the product head from GitHub
      and asserts the runtime-facing repository provider resolves DB pools through the canonical AsyncpgAdapter
      boundary instead of constructing asyncpg pools directly.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/repositories/repository_code_entity_postgres.py?ref=98d962bba1db8b029f530a95b71411e3afa339a2'
          --jq .content | base64 -d | grep -q 'AsyncpgAdapter('
  - id: "occ-self-bind-pr-5197"
    description: "OCC companion PR #5197 — self-bind for OMN-15230 deploy-probe append (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
