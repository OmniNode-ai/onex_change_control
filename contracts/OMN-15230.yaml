---
schema_version: "1.0.0"
ticket_id: "OMN-15230"
title: "Autobind OCC evidence for OMN-15230"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1923."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1923 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1923"
    description: "PR #1923 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1923-ci"
    description: "PR #1923 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5191"
    description: "OCC companion PR #5191 — self-bind for OMN-15230 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1923-deploy-head-98d962bb"
    description: "Falsifiable deploy probe for omnimarket PR #1923 at head 98d962bba1db8b029f530a95b71411e3afa339a2.
      OMN-15230 ships the first production ProtocolCodeEntityRepository implementation plus the domain
      plugin that registers it in the boot container, and consolidates the two same-named protocol declarations
      that lived inside the two handler modules. This evidence reads product-repo content at the pinned
      PR head and asserts the provider class, the repository class and the onex.domain_plugins entry point
      are all present, and that the handler-local protocol declaration is gone. Exit status derives from
      GitHub content comparisons, not from reading any OCC receipt or this contract. Verified falsifiable:
      exit 0 at head 98d962bba, exit 1 at the pre-merge base c73ec6df9 (where the plugin/repository files
      do not exist and the handler still declares its own protocol)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/plugins/plugin_code_entity_repository.py?ref=98d962bba1db8b029f530a95b71411e3afa339a2'
          --jq .content | base64 -d | grep -q 'class PluginCodeEntityRepository' && gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/repositories/repository_code_entity_postgres.py?ref=98d962bba1db8b029f530a95b71411e3afa339a2'
          --jq .content | base64 -d | grep -q 'class RepositoryCodeEntityPostgres' && gh api 'repos/OmniNode-ai/omnimarket/contents/pyproject.toml?ref=98d962bba1db8b029f530a95b71411e3afa339a2'
          --jq .content | base64 -d | grep -q 'code_entity_repository = "omnimarket.plugins.plugin_code_entity_repository:PluginCodeEntityRepository"'
          && ! gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_code_enrichment_effect/handlers/handler_code_enrichment_effect.py?ref=98d962bba1db8b029f530a95b71411e3afa339a2'
          --jq .content | base64 -d | grep -q 'class ProtocolCodeEntityRepository(Protocol)'
  - id: "occ-self-bind-pr-OMN15230-deploy-probe"
    description: "OCC deploy-evidence companion for OMN-15230 — self-bind (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr list --repo OmniNode-ai/onex_change_control --head jonah/omn-15230-occ-deploy-probe
          --state all --json number,state"
