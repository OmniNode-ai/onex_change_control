---
schema_version: "1.0.0"
ticket_id: OMN-7946
title: "Add candidate pool COMPUTE node in omnimarket"
summary: "Add node_candidate_pool_compute in omnimarket as a pure COMPUTE node for deterministic candidate
  validation, scoring, and ranking."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
evidence_requirements:
  - kind: "ci"
    description: "omnimarket#574 checks pass for node_candidate_pool_compute"
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001-omnimarket-574"
    description: "omnimarket#574 carries node_candidate_pool_compute implementation, CodeRabbit hardening
      fixes, and deploy-gate contract"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json headRefOid -q .headRefOid"
  - id: "dod-occ-pr-843"
    description: "onex_change_control#843 carries OMN-7946 central contract and receipt evidence"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json state,baseRefName
          --jq '.state + \":\" + .baseRefName'"
