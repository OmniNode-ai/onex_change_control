---
schema_version: "1.0.0"
ticket_id: OMN-10789
title: "Add tokens_to_compliance + compliance_attempts columns to delegation_events table"
summary: >
  Adds migration 003 with two new INTEGER columns on delegation_events: tokens_to_compliance (default
  0) and compliance_attempts (default 1). Updates sqlite_adapter.py model and INSERT/UPSERT statements.
  7 unit tests cover fresh-DB migration, upgrade-from-002, PRAGMA metadata, write round-trip, defaults,
  and upsert behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-unit-tests
    description: "Unit tests for compliance migration pass — 7 tests covering schema, round-trip, defaults,
      and upsert."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10789/dod-unit-tests/command.yaml
          && grep -q '^ticket_id: OMN-10789$' drift/dod_receipts/OMN-10789/dod-unit-tests/command.yaml"
