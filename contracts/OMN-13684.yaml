---
schema_version: "1.0.0"
ticket_id: "OMN-13684"
title: "WS5 Wave 10 demo delegation and remaining COMPUTE multi-param integration"
summary: >-
  omnimarket PR #1475 adds real multi-parameter integration coverage for OMN-13684 as part of the WS5
  market-skill integration wave. The change is test-only evidence coverage and does not authorize or perform
  a live deploy, runtime restart, topic mutation, schema migration, database migration, or .201/.200 mutation
  during merge.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >-
      Wave-specific integration tests described in omnimarket PR #1475 passed locally with typed assertions,
      negative controls, and no live Kafka or .201 dependency.
    command: "see omnimarket PR #1475 dod_evidence"
  - kind: "tests"
    description: >-
      Full omnimarket non-Kafka suite passed locally for this wave PR before opening.
    command: "uv run pytest tests/ -m 'not kafka' -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1475"
    description: >-
      omnimarket PR #1475 carries the WS5 integration test coverage for OMN-13684 at head commit a0cf3c763260d617054e08fe6360457eed9b16b7.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13684/dod-omnimarket-pr-1475/command.yaml
          && grep -q '^pr_number: 1475$' drift/dod_receipts/OMN-13684/dod-omnimarket-pr-1475/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: PR #1475 is test-only WS5 integration coverage. It does not perform or authorize
      a live deploy, runtime restart, topic mutation, schema migration, database migration, or .201/.200
      mutation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13684/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-13684/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-3228"
    description: >-
      Self-binding OCC eligibility receipt for onex_change_control PR #3228, which carries this WS5 evidence
      contract and receipts onto dev so downstream omnimarket PR #1475 can cite Evidence-Source: OCC#3228.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13684/dod-occ-pr-3228/command.yaml
          && grep -q '^pr_number: 3228$' drift/dod_receipts/OMN-13684/dod-occ-pr-3228/command.yaml"
