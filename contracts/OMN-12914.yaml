---
schema_version: "1.0.0"
ticket_id: "OMN-12914"
title: "P3 tenant-isolation lab suite"
summary: "OCC evidence for omninode_infra#623, a test-only PR that lands the opt-in P3 tenant-isolation
  lab suite with strict xfail coverage for the known broker ACL gap and assertions for same-correlation
  tenant separation, spoofing rejection, and quota behavior."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omninode_infra PR #623 is open at head b84526581421888184985c095dad24e34630bf16."
    command: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-12914/dod-omninode_infra-pr-623-head/command.yaml"
  - kind: "manual"
    description: "omninode_infra PR #623 product diff scope is the opt-in P3 tenant-isolation lab test
      file."
    command: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-12914/dod-omninode_infra-pr-623-product-diff-scope/command.yaml"
  - kind: "manual"
    description: "omninode_infra PR #623 records independent verifier provenance for the live dev-lane
      run in the PR body."
    command: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-12914/dod-omninode_infra-pr-623-verifier-provenance/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode_infra-pr-623-head"
    description: "Binds omninode_infra PR #623 at head b84526581421888184985c095dad24e34630bf16."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-12914/dod-omninode_infra-pr-623-head/command.yaml
          && grep -q 'b84526581421888184985c095dad24e34630bf16' drift/dod_receipts/OMN-12914/dod-omninode_infra-pr-623-head/command.yaml"
  - id: "dod-omninode_infra-pr-623-product-diff-scope"
    description: "PR #623 product diff scope is limited to the P3 tenant-isolation lab test file."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-12914/dod-omninode_infra-pr-623-product-diff-scope/command.yaml
          && grep -q 'docker/onex-api/tests/test_p3_tenant_isolation_lab.py' drift/dod_receipts/OMN-12914/dod-omninode_infra-pr-623-product-diff-scope/command.yaml"
  - id: "dod-omninode_infra-pr-623-verifier-provenance"
    description: "PR #623 body records the live run result and independent verifier comment used as provenance
      for OMN-12914."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-12914/dod-omninode_infra-pr-623-verifier-provenance/command.yaml
          && grep -q 'wf_24e9085a-246' drift/dod_receipts/OMN-12914/dod-omninode_infra-pr-623-verifier-provenance/command.yaml
          && grep -q 'e98192f1' drift/dod_receipts/OMN-12914/dod-omninode_infra-pr-623-verifier-provenance/command.yaml"
