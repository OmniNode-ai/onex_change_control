---
schema_version: "1.0.0"
ticket_id: OMN-16702
title: Central OCC evidence for the OMN-16702 prod-promotion grant landing on main
summary: >-
  Central OCC evidence for the hotfix/* PR that lands four OMN-16702 / OMN-16685 prod-promotion grant
  entries on onex_change_control@main under promotion_batch_id batch-af8142c0-f2bf-4d79-887a-e65d964c9da8:
  grant-6219b88c (the runtime same-digest authorization sha256:13c96028…, the ONLY entry any gate reads),
  plus three DOCUMENTARY entries — grant-a24c94a4 (onex-dev/omnidash sha256:50601f9e…), grant-68783635
  (onex-dev/onex-api sha256:b4c32554…) and grant-6627c1d1 (the dev/dev-valkey requirepass roll, sha256:53ae5378…)
  — which no pipeline resolves and which the file's own header says so in as many words. PREP ONLY: this
  contract proves the PR is well-formed, locally validated against the producer-side schema suite, and
  that the manifest tree it authorizes exists on the branch the consumer dispatches from. It does NOT
  authorize prod mutation and it is NOT the approval act. The PR still requires a human CODEOWNERS operator's
  explicit merge per OMN-13418. This contract exists on the PR branch itself because occ-preflight pins
  OCC evidence at the PR head SHA for onex_change_control's own PRs (invariant I3, in-tree checkout) —
  the same shape OCC#7418 (OMN-16799), OCC#7213 (OMN-16626) and OCC#7084 (OMN-16205) used on this base.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: manual
    description: >-
      Prod-promotion grant registry values, producer-side schema validation, and existence of the k8s/onex-public
      manifest tree on omninode_infra@main that the grant authorizes an apply of.
    command: >-
      grep -Fq 'grant-6219b88c-72a7-40ec-9fef-a4e95729021c' grants/prod_promotion_grants.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-prod-promotion-grant-values-omn16702"
    description: >-
      SUPERSEDED CONTENT, LIVE-RE-EXECUTED: this item originally asserted batch-af8142c0's four grant_ids.
      That batch expired 2026-08-29T09:00:00Z unconsumed and was pruned by OCC#7554 (OMN-15010). PR #7561
      re-stages the same four authorizations under a NEW promotion_batch_id batch-48e48b4c-68f5-4e1c-b12e-818dcf062dfd
      with NEW grant_ids, per the operator's 2026-08-29 re-stage ruling — so this check now asserts the
      CURRENT batch is present, not the expired one: grant-30d406bb-d6c7-455a-a371-2a706b892346 (omninode-runtime
      sha256:13c96028…, the same-digest authorization the UNCONDITIONAL OMN-14209 runtime grant gate resolves
      from k8s/onex-prod/runtime/kustomization.yaml — the only mechanically-required entry in the batch),
      grant-7f32b169-c681-47a5-9380-8e04135842a4 (omnidash sha256:50601f9e…), grant-0bb17fe1-dcf7-4cfc-b41e-3a1b499762b1
      (omnicloud/core sha256:b4c32554…) and grant-25d6751a-1188-47a3-9590-62bb48f15327 (valkey/valkey:8.0
      sha256:53ae5378…, the dev/dev-valkey requirepass roll). Falsifiable against the repo checkout: the
      grep chain fails on any base that lacks these entries, including onex_change_control@main pre-#7561.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Fq 'grant-30d406bb-d6c7-455a-a371-2a706b892346' grants/prod_promotion_grants.yaml && grep
          -Fq 'grant-7f32b169-c681-47a5-9380-8e04135842a4' grants/prod_promotion_grants.yaml && grep -Fq
          'grant-0bb17fe1-dcf7-4cfc-b41e-3a1b499762b1' grants/prod_promotion_grants.yaml && grep -Fq 'grant-25d6751a-1188-47a3-9590-62bb48f15327'
          grants/prod_promotion_grants.yaml && grep -Fq 'promotion_batch_id: batch-48e48b4c-68f5-4e1c-b12e-818dcf062dfd'
          grants/prod_promotion_grants.yaml
  - id: "dod-prod-promotion-grant-validation-omn16702"
    description: >-
      The repo's own grant-schema suite passes against the grant file carrying the four new OMN-16702
      entries alongside the two carried-forward OMN-16799 entries — including the OMN-13424 at-rest invariant
      test that refuses any expired entry, which is why the OMN-16799 pair (expires_at 2026-08-30T16:00:00Z)
      can be carried forward untouched today but must be pruned if this PR is still open after that timestamp.
      Executed hermetically from the repo checkout.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_prod_promotion_grants.py -q"
  - id: "dod-onex-public-tree-on-main-omn16702"
    description: >-
      The grant NAMES the exact manifest tree it authorizes an apply of, so a reviewer can bind the authorization
      to a tree rather than to a directory path that could mean anything. deploy-onex-prod.yml refuses
      any dispatch whose GITHUB_REF is not refs/heads/main, and `git rev-parse origin/main:k8s/onex-public`
      == `origin/dev:k8s/onex-public` == 04c2fe17e6a7478271e1358b78ee9afda6aa544b, so that one SHA pins
      what will actually be applied. This check is deliberately HERMETIC to the OCC checkout: it asserts
      the tree SHA is recorded in the grant file itself. STATED LIMIT — it does NOT re-prove cross-repo
      existence. The first version of this item did, via `gh api repos/OmniNode-ai/omninode_infra/contents/k8s/onex-public?ref=main`,
      and the hosted Contract Compliance runner returned `gh: Not Found (HTTP 404)` because its token
      carries no read scope on the private omninode_infra repo. That cross-repo probe WAS executed successfully
      from the staging lane (exit 0, listing recorded in this item's receipt); the hosted gate simply
      cannot re-run it, and weakening the assertion is more honest than leaving a check that fails for
      lack of scope rather than lack of truth. Same class of hosted-scope defect OCC#7418 hit with an
      ${OMNINODE_INFRA}-rooted command, resolved the other way.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Fq '04c2fe17e6a7478271e1358b78ee9afda6aa544b' grants/prod_promotion_grants.yaml
  - id: "occ-self-bind-pr-7464"
    description: >-
      OCC PR #7464 — self-bind for the OMN-16702 / OMN-16685 prod-promotion grant PREP (batch-af8142c0).
      OPEN against onex_change_control@main and NOT to be merged by any agent per the PR's own operator-approval-act
      scope statement; the CODEOWNERS merge is the approval act (OMN-13418).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json number,state,baseRefName,headRefName,headRefOid
  - id: "occ-self-bind-pr-7561"
    description: >-
      OCC self-binding receipt for PR #7561, the in-repo evidence PR carrying this contract. Binds OMN-16702
      to the OCC PR by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself.
      PR #7561 re-stages batch-af8142c0 (grant-6219b88c etc., pruned by OCC#7554/OMN-15010 after lapsing
      unconsumed) as a new batch-48e48b4c under the same ticket, per the operator's 2026-08-29 re-stage
      ruling.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json number,state,baseRefName,headRefName,headRefOid
  - id: "occ-self-bind-pr-7662"
    description: >-
      OCC self-binding receipt for PR #7662, the in-repo evidence PR carrying this contract. Binds OMN-16702
      to the OCC PR by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json number,state,headRefName
