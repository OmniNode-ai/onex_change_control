---
schema_version: "1.0.0"
ticket_id: "OMN-14668"
title: "Autobind OCC evidence for OMN-14668"
summary: >
  OCC contract bound by node_pr_lifecycle_fix_effect Evidence-Source autobind (OMN-13317 F1) when OmniNode-ai/omnimarket
  PR #1787 carried a product-SHA Evidence-Source or was missing an OCC contract, and failed the gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1787 product diff scope present"
    command: "PRODUCT_PR_NUMBER=1787; PRODUCT_REPO=OmniNode-ai/omnimarket; gh pr diff ${PRODUCT_PR_NUMBER}
      --repo ${PRODUCT_REPO} --name-only | grep -q ."
  - kind: "ci"
    description: "PR #2320 product diff scope present"
    command: "PRODUCT_PR_NUMBER=2320; PRODUCT_REPO=OmniNode-ai/omnibase_infra; gh pr diff ${PRODUCT_PR_NUMBER}
      --repo ${PRODUCT_REPO} --name-only | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1787"
    description: "PR #1787 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1787; PRODUCT_REPO=OmniNode-ai/omnimarket; gh pr view ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1787-ci"
    description: "PR #1787 on OmniNode-ai/omnimarket — product diff scope (OMN-14425 substance check;
      OMN-14650: no longer gated on source-PR CI being green)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1787; PRODUCT_REPO=OmniNode-ai/omnimarket; gh pr diff ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --name-only | grep -q ."
  - id: "occ-self-bind-pr-4282"
    description: "OCC companion PR #4282 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14668
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4282; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4283"
    description: "OCC PR #4283 carries the OMN-14668 hosted-runner parity gate update."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4283; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state,headRefOid"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2320"
    description: "PR #2320 on OmniNode-ai/omnibase_infra carries the hosted-runner parity gate fan-out."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=2320; PRODUCT_REPO=OmniNode-ai/omnibase_infra; gh pr view ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2320-ci"
    description: "PR #2320 on OmniNode-ai/omnibase_infra product diff scope for the hosted-runner parity
      gate."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=2320; PRODUCT_REPO=OmniNode-ai/omnibase_infra; gh pr diff ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --name-only | grep -q ."
  - id: "occ-self-bind-pr-4300"
    description: "OCC PR #4300 carries the OMN-14668 omnibase_infra#2320 evidence repair."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4300; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state,headRefOid"
  - id: "dod-OmniNode-ai-omnibase_core-pr-1450"
    description: "PR #1450 on OmniNode-ai/omnibase_core — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1450; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr view ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omnibase_core-pr-1450-ci"
    description: "PR #1450 on OmniNode-ai/omnibase_core has changed-file product scope for OCC autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1450; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr diff ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --name-only | grep -q ."
  - id: "occ-self-bind-pr-4304"
    description: "OCC companion PR #4304 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14668
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4304; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
