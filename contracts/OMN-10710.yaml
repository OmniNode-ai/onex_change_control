---
schema_version: "1.0.0"
ticket_id: OMN-10710
title: "Declare LLM URL env vars in node contract.yaml files"
summary: "Add env_dependencies blocks to 7 ONEX nodes that read LLM endpoint env vars without contract-level
  declaration."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #597 passes node-drift-gate and arch-lint CI checks"
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket"
  - kind: "manual"
    description: "All 7 changed contract.yaml nodes pass strict drift validation"
    command: "uv run python scripts/validate_node_drift.py --check-changed origin/main --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-contract-compliance"
    description: "Node drift gate passes for all 7 changed nodes (handler block, topic naming, env_dependencies)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket 2>&1 | grep 'node-drift-gate'
          | grep -q pass"
  - id: "dod-arch-lint"
    description: "Architectural lint passes — ARCH-TOPIC-002 resolved and ARCH-DI checks clean"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket 2>&1 | grep 'Architectural
          Compliance Lint' | grep -q pass"
  - id: "dod-tests"
    description: "Unit test suite passes (contract-only change)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket 2>&1 | grep 'Tests' | grep
          -q pass"
  - id: "dod-deploy-smoke"
    description: "Post-merge deploy smoke: contract-only change, no runtime restart required; all 7 modified
      contracts parse cleanly."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket 2>&1 | grep 'Contract Compliance'
          | grep -q pass"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for this OCC PR #934."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json state,baseRefName
          --jq '.state + \":\" + .baseRefName' | grep -q '^OPEN:main$'"
