---
schema_version: '1.0.0'
ticket_id: OMN-10557
title: 'Task 11: InfisicalSecretStore wrapper'
summary: 'Wrap AdapterInfisical to satisfy ProtocolSecretStore without mutating the SDK-shaped adapter'
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: New unit tests cover hit/miss/update-fallback/delete-RuntimeError/list-prefix/health/close
    command: uv run pytest tests/unit/secret_stores/test_infisical_secret_store.py tests/unit/validators/test_rule_no_direct_adapter.py
      -v
  - kind: ci
    description: omnibase_infra PR 1506 CI green
    command: gh pr checks 1506 --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: New InfisicalSecretStore wrapper module present in PR 1506 diff
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 1506 --repo OmniNode-ai/omnibase_infra | grep -q 'src/omnibase_infra/secret_stores/infisical_secret_store.py'
  - id: dod-002
    description: AdapterInfisical source NOT modified by PR 1506 (file outside the diff)
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 1506 --repo OmniNode-ai/omnibase_infra | grep -E '^\+\+\+ b/src/omnibase_infra/adapters/_internal/adapter_infisical\.py'
          | wc -l | grep -q '^0$'
  - id: dod-003
    description: validator_no_direct_adapter allowlist amended to include omnibase_infra.secret_stores.
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 1506 --repo OmniNode-ai/omnibase_infra | grep -q 'omnibase_infra.secret_stores.'
  - id: dod-004
    description: omnibase_infra PR 1506 has CI passing
    source: manual
    checks:
      - check_type: command
        check_value: gh pr checks 1506 --repo OmniNode-ai/omnibase_infra | grep -c 'pass' | grep -qv '^0$'
  - id: dod-005
    description: PR 1506 open against main branch
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 1506 --repo OmniNode-ai/omnibase_infra --json state,baseRefName --jq 'select(.state=="OPEN"
          and .baseRefName=="main") | "ok"' | grep -q ok
