---
schema_version: "1.0.0"
ticket_id: "OMN-15870"
title: "OCC companion for OMN-15870 bootstrap circularity fix"
summary: "Evidence-Source companion for OmniNode-ai/omninode_infra#869 (admin-gated POST /v1/tenants/bootstrap
  route solves first-tenant circularity)."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "PR #869 diff scope present (content endpoint, not PR-existence metadata)"
    command: "gh api repos/${REPO}/pulls/${PR_NUMBER}/files --jq '.[].filename'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-869"
    description: "PR #869 on OmniNode-ai/omninode_infra — product diff scope (files endpoint, OMN-14425/OMN-15309
      admissible form)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omninode_infra/pulls/869/files --jq '.[].filename'"
  - id: "dod-OmniNode-ai-omninode_infra-pr-869-ci"
    description: "PR #869 on OmniNode-ai/omninode_infra — content-bound proof that TENANT_BOOTSTRAP_PATH
      exists at the fix commit (falsifiable: absent at pre-fix merge-base 032820cf, the OMN-15871 merge
      commit)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omninode_infra/contents/docker/onex-api/main.py?ref=5efadef8929855cc0dfd736780e48ab20014eeab'
          --jq '.content' | base64 -d | grep -c 'TENANT_BOOTSTRAP_PATH'"
