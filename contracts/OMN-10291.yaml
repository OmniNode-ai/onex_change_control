---
schema_version: 1.0.0
ticket_id: OMN-10291
title: 'Migrate cost-by-model widget to manifest-driven adapter dispatch (IBarChartAdapter + IDoughnutChartAdapter)'
summary: 'Migrated cost-by-model from bespoke router dispatch to manifest-driven IBarChartAdapter (2D)
  and IDoughnutChartAdapter (3D). Deleted CostByModel.tsx + CostByModelBars.tsx. Added DoughnutChartAdapter.tsx.
  Registry now contains 9 MVP components.'
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
contract_completeness: FULL
evidence_requirements:
  - kind: tests
    description: Integration tests verify 9 manifest entries and full adapter resolution
    command: cd omnidash && npm run test -- --run src/integration.part2.test.tsx src/integration.part3.test.tsx
  - kind: ci
    description: omnidash PR  #51 all CI checks pass
    command: gh pr checks 51 --repo OmniNode-ai/omnidash
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: All 14 CI checks pass on omnidash PR  #51 (Typecheck & Tests, contract-validation, golden-chain, gate/CodeRabbit all green)
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr checks 51 --repo OmniNode-ai/omnidash 2>&1 | grep -E '\\bfail\\b' | wc -l
          | grep -q '^0$'"
  - id: dod-002
    description: Registry manifest contains 9 components including cost-by-model-3d (IDoughnutChartAdapter)
    source: generated
    checks:
      - check_type: command
        check_value: "grep -c 'IDoughnutChartAdapter\\|IBarChartAdapter' /Users/jonah/Code/omni_worktrees/OMN-10291/omnidash/src/registry/component-registry.json
          | grep -qE '^[2-9]'"
  - id: dod-003
    description: Bespoke router files deleted — CostByModel.tsx and CostByModelBars.tsx absent from worktree
    source: generated
    checks:
      - check_type: command
        check_value: "test ! -f /Users/jonah/Code/omni_worktrees/OMN-10291/omnidash/src/components/dashboard/cost-by-model/CostByModel.tsx
          && test ! -f /Users/jonah/Code/omni_worktrees/OMN-10291/omnidash/src/components/dashboard/cost-by-model/CostByModelBars.tsx"
