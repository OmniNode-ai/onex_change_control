---
schema_version: "1.0.0"
ticket_id: "OMN-11025"
title: "Migrate omnimarket delegation DTO imports to omnibase_compat"
summary: >
  Binds omnimarket PR #629, which replaces duplicate delegation DTO definitions with compatibility imports
  from canonical omnibase_compat.contracts.delegation.wire models while preserving existing omnimarket
  import paths and contract refs.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #629 carries the OMN-11025 delegation DTO migration."
    command: "gh pr view 629 --repo OmniNode-ai/omnimarket --json state,headRefOid"
  - kind: "tests"
    description: "Focused delegation DTO import, schema-ref, routing, projection, and budget tests pass."
    command: "uv run pytest tests/test_delegation_dto_omnibase_compat_imports.py tests/test_memory_dto_omnimemory_imports.py
      tests/unit/delegation tests/nodes/node_delegation_routing_reducer/test_contract_routing_policy.py
      tests/test_golden_chain_projection_delegation.py tests/unit/nodes/node_build_loop_orchestrator/test_dispatch_trace.py
      src/omnimarket/nodes/node_budget_policy_compute/tests/test_handler_budget_policy.py -q"
  - kind: "manual"
    description: "Touched delegation compatibility files and node metadata dependency scope pass local
      checks."
    command: "uv run ruff check <touched files> && uv run python scripts/ci/check_node_metadata_dependencies.py"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-629"
    description: "omnimarket PR #629 carries the OMN-11025 delegation DTO import migration."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11025/dod-omnimarket-pr-629/command.yaml
          && grep -q '^pr_number: 629$' drift/dod_receipts/OMN-11025/dod-omnimarket-pr-629/command.yaml
          && grep -q '^commit_sha: c05f2e67ffc869abad9f194c7fe01e3cce8ba9b0$' drift/dod_receipts/OMN-11025/dod-omnimarket-pr-629/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused omnimarket delegation DTO import and compatibility tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11025/dod-focused-tests/command.yaml
          && grep -q '185 passed' drift/dod_receipts/OMN-11025/dod-focused-tests/command.yaml"
  - id: "dod-ruff-metadata"
    description: "Ruff and ONCP node metadata dependency scope checks pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11025/dod-ruff-metadata/command.yaml
          && grep -q 'ONCP node metadata dependency scope: PASS' drift/dod_receipts/OMN-11025/dod-ruff-metadata/command.yaml"
  - id: "dod-d4-delegation-wire"
    description: "D4 overlay shows zero targeted delegation wire DTO collisions, with ModelRoutingDecision
      untouched."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11025/dod-d4-delegation-wire/command.yaml
          && grep -q 'D4-delegation-wire-targeted' drift/dod_receipts/OMN-11025/dod-d4-delegation-wire/command.yaml"
  - id: "dod-deploy"
    description: >
      Deploy DoD evidence for PR #629: import compatibility only; no live deploy, restart, or .201 mutation
      required before merge.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11025/dod-deploy/command.yaml &&
          grep -q '^pr_number: 629$' drift/dod_receipts/OMN-11025/dod-deploy/command.yaml && grep -q 'No
          live deploy' drift/dod_receipts/OMN-11025/dod-deploy/command.yaml"
  - id: "dod-occ-pr-1019"
    description: "onex_change_control PR #1019 carries the central OMN-11025 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11025/dod-occ-pr-1019/command.yaml
          && grep -q '^pr_number: 1019$' drift/dod_receipts/OMN-11025/dod-occ-pr-1019/command.yaml"
