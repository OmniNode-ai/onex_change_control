---
schema_version: 1.0.0
ticket_id: OMN-14820
title: Autobind OCC evidence for OMN-14820
summary: OCC Evidence-Source autobind companion for OmniNode-ai/omnibase_infra#2368.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #2368 product diff scope present'
    command: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-infra-2368-head-dbe890cb
    description: 'PR #2368 on OmniNode-ai/omnibase_infra -- product head dbe890cb.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName
  - id: dod-infra-2368-diff-dbe890cb
    description: 'PR #2368 on OmniNode-ai/omnibase_infra -- product diff scope at head dbe890cb.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
  - id: dod-infra-2368-deploy-dbe890cb
    description: 'Deploy-gate scope for omnibase_infra PR #2368 at head dbe890cb. The change touches src/omnibase_infra/nodes/node_checkpoint_validate_compute/contract.yaml
      and src/omnibase_infra/nodes/node_checkpoint_validate_compute/handlers/handler_checkpoint_validate.py
      runtime contract/handler paths, but only records the existing canonical def-B checkpoint validation
      dispatch shape and preserves deterministic checkpoint structural validation semantics. No live runtime
      deployment, .201 mutation, docker exec, or rpk topic produce is authorized by this evidence repair;
      hosted merge gates and post-merge dev availability remain the deployment boundary.'
    source: generated
    checks:
      - check_type: command
        check_value: printf '%s\n' 'deploy scope assessed for omnibase_infra#2368 runtime contract/handler
          paths; no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'
  - id: occ-self-bind-pr-4493
    description: OCC companion PR  #4493 -- self-bind for OMN-14820 deploy evidence repair.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-infra-2368-head-137fd80b
    description: 'PR #2368 on OmniNode-ai/omnibase_infra -- refreshed product head 137fd80b.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName
    evidence_artifact: supersedes_dod_evidence:dod-infra-2368-head-dbe890cb
  - id: dod-infra-2368-diff-137fd80b
    description: 'PR #2368 on OmniNode-ai/omnibase_infra -- refreshed product diff scope at head 137fd80b.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
    evidence_artifact: supersedes_dod_evidence:dod-infra-2368-diff-dbe890cb
  - id: dod-infra-2368-deploy-137fd80b
    description: 'Deploy-gate scope for omnibase_infra PR #2368 at head 137fd80b. The change touches src/omnibase_infra/nodes/node_checkpoint_validate_compute/contract.yaml
      and src/omnibase_infra/nodes/node_checkpoint_validate_compute/handlers/handler_checkpoint_validate.py
      runtime contract/handler paths, plus a test-only topic-gating expectation repair in tests/unit/topics/test_platform_topic_suffixes.py
      after dev merge-forward. No live runtime deployment, .201 mutation, docker exec, or rpk topic produce
      is authorized by this evidence repair; hosted merge gates and post-merge dev availability remain
      the deployment boundary.'
    source: generated
    checks:
      - check_type: command
        check_value: printf '%s\n' 'deploy scope assessed for omnibase_infra#2368 refreshed runtime contract/handler
          paths plus test-only topic-gating repair; no live deploy, docker exec, or rpk topic produce
          is authorized by this evidence repair'
    evidence_artifact: supersedes_dod_evidence:dod-infra-2368-deploy-dbe890cb
  - id: occ-self-bind-pr-4495
    description: OCC follow-up PR  #4495 -- self-bind for OMN-14820 refreshed head evidence repair.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
    evidence_artifact: supersedes_dod_evidence:occ-self-bind-pr-4493
