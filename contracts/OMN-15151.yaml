---
schema_version: "1.0.0"
ticket_id: "OMN-15151"
title: "Runbook: LAB deploy operational procedure (stability tagged-deploy + preflights + dev refresh)"
summary: "Extends docs/runbooks/release-train-lab.md in omnibase_infra with the operator-facing procedure
  for the now-proven stability-lane tagged-deploy path (run 30180376657), the six preflight fixes that
  unblocked it, T0/T1 readback + rollback discipline, dev-lane gotchas, and a hard prod pointer."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2454 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "ci"
    description: "omnibase_infra PR #2454 head is bound to the reviewed commit."
    command: "test \"$(gh pr view 2454 --repo OmniNode-ai/omnibase_infra --json headRefOid --jq .headRefOid)\"
      = \"067713363ca58f2acab7726f73cb362e6fcb0c20\""
  - kind: "ci"
    description: "docs/runbooks/release-train-lab.md at the reviewed commit contains the new operational
      procedure section this ticket adds."
    command: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/docs/runbooks/release-train-lab.md?ref=067713363ca58f2acab7726f73cb362e6fcb0c20'
      --jq .content | base64 -d | grep -q '## What the preflights catch'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra2454-product-head"
    description: "omnibase_infra PR #2454 remains bound to the reviewed commit."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test \"$(gh pr view 2454 --repo OmniNode-ai/omnibase_infra --json headRefOid --jq
          .headRefOid)\" = \"067713363ca58f2acab7726f73cb362e6fcb0c20\""
  - id: "dod-infra2454-runbook-preflight-section"
    description: "The runbook at the reviewed commit contains the new '## What the preflights catch' section
      this ticket adds -- a real, falsifiable content read pinned to the source PR's head SHA, not a self-referential
      check."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/docs/runbooks/release-train-lab.md?ref=067713363ca58f2acab7726f73cb362e6fcb0c20'
          --jq .content | base64 -d | grep -q '## What the preflights catch'"
  - id: "dod-infra2454-runbook-run-citation"
    description: "The runbook at the reviewed commit cites the proven terminal-success run (30180376657)
      that grounds the whole procedure -- proves the doc content is sourced from the real evidence, not
      invented."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/docs/runbooks/release-train-lab.md?ref=067713363ca58f2acab7726f73cb362e6fcb0c20'
          --jq .content | base64 -d | grep -q '30180376657'"
