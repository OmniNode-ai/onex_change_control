---
schema_version: "1.0.0"
ticket_id: "OMN-12618"
title: "Producer-edge durability epic"
summary: >
  Epic-level central OCC evidence for the producer-edge durability work cited by omnimarket PR #1022.
  OMN-12620 implements the Section 5 canary: topic-tiered node_emit_daemon durability, with duty-critical
  topics routed through a durable outbox and telemetry retaining bounded drop-on-overflow behavior.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-epic-section-5-pr-1022"
    description: "omnimarket PR #1022 is the OMN-12618 Section 5 durability canary, implemented under
      OMN-12620."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-epic-section-5-pr-1022/command.yaml"
          && grep -q '^pr_number: 1022$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-epic-section-5-pr-1022/command.yaml"
  - id: "dod-epic-deploy-assessment"
    description: >
      Epic deploy stance for this canary: no live .201 mutation was performed pre-merge; runtime rollout
      is operator-approved post-merge work, while the PR-local quality bar is the durable-outbox test
      and replay proof.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-epic-deploy-assessment/command.yaml"
          && grep -qi 'deploy assessment' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-epic-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2104"
    description: "onex_change_control PR #2104 carries the epic-level central evidence for OMN-12618."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2104/command.yaml"
          && grep -q '^pr_number: 2104$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2104/command.yaml"
  - id: "dod-redeploy-lane-payload-pr-1024"
    description: >
      omnimarket PR #1024 carries the node_redeploy deploy-agent wire-contract fix required by the 2026-06-03
      three-runtime sync plan: runtime_lane, build_source, image_ref, and image_digest now survive from
      the workflow input through the Kafka rebuild-requested command.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-lane-payload-pr-1024/command.yaml"
          && grep -q '^pr_number: 1024$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-lane-payload-pr-1024/command.yaml"
  - id: "dod-redeploy-lane-payload-tests"
    description: >
      node_redeploy focused lint, model, handler, workflow, rollback, input-boundary, and prod-promotion
      regression tests pass locally for the lane-payload propagation change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-lane-payload-tests/command.yaml"
          && grep -q '85 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-lane-payload-tests/command.yaml"
  - id: "dod-redeploy-lane-payload-deploy-assessment"
    description: >
      Deploy stance for omnimarket PR #1024: no .201 runtime mutation is performed by this PR. It only
      repairs the node_redeploy publish contract so the approved runtime redeploy can be owned by node_redeploy
      and carry lane/build-source/digest fields safely.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-lane-payload-deploy-assessment/command.yaml"
          && grep -qi 'no .201 runtime mutation' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-lane-payload-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2121"
    description: "onex_change_control PR #2121 carries the central evidence for the OMN-12618 redeploy
      lane-payload fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2121/command.yaml"
          && grep -q '^pr_number: 2121$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2121/command.yaml"
