---
schema_version: "1.0.0"
ticket_id: "OMN-12618"
title: "Producer-edge durability epic"
summary: >
  Epic-level central OCC evidence for the producer-edge durability work cited by omnimarket PR #1022.
  OMN-12620 implements the Section 5 canary: topic-tiered node_emit_daemon durability, with duty-critical
  topics routed through a durable outbox and telemetry retaining bounded drop-on-overflow behavior.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-epic-section-5-pr-1022"
    description: "omnimarket PR #1022 is the OMN-12618 Section 5 durability canary, implemented under
      OMN-12620."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-epic-section-5-pr-1022/command.yaml"
          && grep -q '^pr_number: 1022$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-epic-section-5-pr-1022/command.yaml"
  - id: "dod-epic-deploy-assessment"
    description: >
      Epic deploy stance for this canary: no live .201 mutation was performed pre-merge; runtime rollout
      is operator-approved post-merge work, while the PR-local quality bar is the durable-outbox test
      and replay proof.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-epic-deploy-assessment/command.yaml"
          && grep -qi 'deploy assessment' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-epic-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2104"
    description: "onex_change_control PR #2104 carries the epic-level central evidence for OMN-12618."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2104/command.yaml"
          && grep -q '^pr_number: 2104$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2104/command.yaml"
  - id: "dod-redeploy-lane-payload-pr-1024"
    description: >
      omnimarket PR #1024 carries the node_redeploy deploy-agent wire-contract fix required by the 2026-06-03
      three-runtime sync plan: runtime_lane, build_source, image_ref, and image_digest now survive from
      the workflow input through the Kafka rebuild-requested command.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-lane-payload-pr-1024/command.yaml"
          && grep -q '^pr_number: 1024$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-lane-payload-pr-1024/command.yaml"
  - id: "dod-redeploy-lane-payload-tests"
    description: >
      node_redeploy focused lint, model, handler, workflow, rollback, input-boundary, and prod-promotion
      regression tests pass locally for the lane-payload propagation change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-lane-payload-tests/command.yaml"
          && grep -q '85 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-lane-payload-tests/command.yaml"
  - id: "dod-redeploy-lane-payload-deploy-assessment"
    description: >
      Deploy stance for omnimarket PR #1024: no .201 runtime mutation is performed by this PR. It only
      repairs the node_redeploy publish contract so the approved runtime redeploy can be owned by node_redeploy
      and carry lane/build-source/digest fields safely.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-lane-payload-deploy-assessment/command.yaml"
          && grep -qi 'no .201 runtime mutation' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-lane-payload-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2121"
    description: "onex_change_control PR #2121 carries the central evidence for the OMN-12618 redeploy
      lane-payload fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2121/command.yaml"
          && grep -q '^pr_number: 2121$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2121/command.yaml"
  - id: "dod-redeploy-hmac-envelope-pr-1026"
    description: >
      omnimarket PR #1026 fixes the node_redeploy to deploy-agent authentication boundary: deploy-agent
      rebuild-requested commands now carry the HMAC _signature field when DEPLOY_AGENT_HMAC_SECRET is
      available.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-hmac-envelope-pr-1026/command.yaml"
          && grep -q '^pr_number: 1026$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-hmac-envelope-pr-1026/command.yaml"
  - id: "dod-redeploy-hmac-envelope-tests"
    description: >
      node_redeploy focused lint plus redeploy golden-chain, model, lane FSM, rollback, input-boundary,
      and prod-promotion regression tests pass locally for the signed deploy-agent envelope change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-hmac-envelope-tests/command.yaml"
          && grep -q '85 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-hmac-envelope-tests/command.yaml"
  - id: "dod-redeploy-hmac-envelope-deploy-assessment"
    description: >
      Deploy stance for omnimarket PR #1026: no .201 runtime mutation is performed by this PR. It only
      repairs the command authentication boundary so node_redeploy-owned runtime rollout can be accepted
      by deploy-agent.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-hmac-envelope-deploy-assessment/command.yaml"
          && grep -qi 'no .201 runtime mutation' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-hmac-envelope-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2123"
    description: "onex_change_control PR #2123 carries the central evidence for the OMN-12618 signed node_redeploy
      deploy-agent envelope fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2123/command.yaml"
          && grep -q '^pr_number: 2123$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2123/command.yaml"
  - id: "dod-deploy-agent-lane-verify-pr-1847"
    description: >
      omnibase_infra PR #1847 fixes deploy-agent verification for the live lane topology: runtime health
      with config_prefetch_status=skipped is accepted as healthy, Postgres verification uses each lane's
      named container, and DB readiness checks the authoritative node_service_registry table.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-deploy-agent-lane-verify-pr-1847/command.yaml"
          && grep -q '^pr_number: 1847$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-deploy-agent-lane-verify-pr-1847/command.yaml"
  - id: "dod-deploy-agent-lane-verify-tests"
    description: >
      deploy-agent focused lint and unit tests pass for lane-specific Postgres verification, skipped-prefetch
      runtime health readiness, and the node_service_registry DB readiness probe.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-deploy-agent-lane-verify-tests/command.yaml"
          && grep -q '14 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-deploy-agent-lane-verify-tests/command.yaml"
  - id: "dod-deploy-agent-lane-verify-deploy-assessment"
    description: >
      Deploy stance for omnibase_infra PR #1847: no .201 runtime mutation is performed by the PR. It prevents
      false deploy-agent verification failures before the approved node_redeploy-owned runtime redeploy.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-deploy-agent-lane-verify-deploy-assessment/command.yaml"
          && grep -qi 'no .201 runtime mutation' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-deploy-agent-lane-verify-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2124"
    description: "onex_change_control PR #2124 carries the central evidence for the OMN-12618 deploy-agent
      lane verification fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2124/command.yaml"
          && grep -q '^pr_number: 2124$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2124/command.yaml"
  - id: "dod-occ-pr-2125"
    description: "onex_change_control PR #2125 refreshes central evidence for the amended OMN-12618 deploy-agent
      lane verification fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2125/command.yaml"
          && grep -q '^pr_number: 2125$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2125/command.yaml"
  - id: "dod-redeploy-kafka-config-pr-1027"
    description: >
      omnimarket PR #1027 fixes the node_redeploy Kafka adapter drift found during the approved 2026-06-03
      dev redeploy attempt: HandlerRedeployKafka.from_env now constructs EventBusKafka through the current
      typed ModelKafkaEventBusConfig and preserves KAFKA_API_VERSION for lane-safe publish flows.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-kafka-config-pr-1027/command.yaml"
          && grep -q '^pr_number: 1027$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-kafka-config-pr-1027/command.yaml"
  - id: "dod-redeploy-kafka-config-tests"
    description: >
      node_redeploy focused lint plus redeploy golden-chain, runtime deployment model, and lane FSM tests
      pass locally for the typed Kafka EventBus configuration fix.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-kafka-config-tests/command.yaml"
          && grep -q '65 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-kafka-config-tests/command.yaml"
  - id: "dod-redeploy-kafka-config-deploy-assessment"
    description: >
      Deploy stance for omnimarket PR #1027: the first node_redeploy attempt failed before publishing
      a rebuild-requested event, so no .201 runtime mutation occurred. This PR only restores the typed
      Kafka adapter needed before retrying the approved node_redeploy-owned dev redeploy.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-kafka-config-deploy-assessment/command.yaml"
          && grep -qi 'no .201 runtime mutation' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-kafka-config-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2126"
    description: "onex_change_control PR #2126 carries the central evidence for the OMN-12618 node_redeploy
      typed Kafka EventBus configuration fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2126/command.yaml"
          && grep -q '^pr_number: 2126$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2126/command.yaml"
  - id: "dod-redeploy-completion-pr-1028"
    description: >
      omnimarket PR #1028 fixes the second node_redeploy adapter drift found during the approved 2026-06-03
      dev redeploy attempt: deploy-agent completion payloads without computed status, and with final in_progress
      phase markers, are normalized before ModelDeployRebuildCompleted validation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-completion-pr-1028/command.yaml"
          && grep -q '^pr_number: 1028$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-completion-pr-1028/command.yaml"
  - id: "dod-redeploy-completion-tests"
    description: >
      node_redeploy focused lint plus redeploy golden-chain, runtime deployment model, and lane FSM tests
      pass locally for the deploy-agent completion payload normalization fix.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-completion-tests/command.yaml"
          && grep -q '66 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-completion-tests/command.yaml"
  - id: "dod-redeploy-completion-assessment"
    description: >
      Deploy stance for omnimarket PR #1028: the signed dev node_redeploy command was accepted, but deploy-agent
      failed before compose generation because its subprocess PATH lacked uv; no runtime containers were
      restarted. This PR only repairs completion-event parsing before retry.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-completion-assessment/command.yaml"
          && grep -qi 'no runtime containers were restarted' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-redeploy-completion-assessment/command.yaml"
  - id: "dod-occ-pr-2127"
    description: "onex_change_control PR #2127 carries the central evidence for the OMN-12618 node_redeploy
      deploy-agent completion payload normalization fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2127/command.yaml"
          && grep -q '^pr_number: 2127$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2127/command.yaml"
  - id: "dod-runtime-error-triage-index-pr-1848"
    description: >
      omnibase_infra PR #1848 adds migration 084, a partial unique index matching the runtime_error_triage
      handler's active incident upsert conflict target. The fix closes the dev redeploy failure where
      asyncpg raised InvalidColumnReferenceError after runtime/effects restarted.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-error-triage-index-pr-1848/command.yaml"
          && grep -q '^pr_number: 1848$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-error-triage-index-pr-1848/command.yaml"
  - id: "dod-runtime-error-triage-index-tests"
    description: >
      omnibase_infra schema fingerprint, migration sentinel, and focused runtime_error_triage handler
      tests pass for the additive migration 084 index fix.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-error-triage-index-tests/command.yaml"
          && grep -q '17 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-error-triage-index-tests/command.yaml"
          && grep -q '14 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-error-triage-index-tests/command.yaml"
  - id: "dod-runtime-error-triage-index-deploy-assessment"
    description: >
      Deploy assessment for omnibase_infra PR #1848: the approved dev node_redeploy attempt restarted
      runtime/effects with correlation_id 048ea264-c43f-478f-898f-3b2f5289cc60, then exposed the missing
      runtime_error_triage partial unique index. Stability and prod were not touched.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-error-triage-index-deploy-assessment/command.yaml"
          && grep -q '048ea264-c43f-478f-898f-3b2f5289cc60' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-error-triage-index-deploy-assessment/command.yaml"
          && grep -qi 'stability and prod were not touched' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-error-triage-index-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2128"
    description: "onex_change_control PR #2128 carries the central evidence for the OMN-12618 runtime_error_triage
      migration 084 prerequisite."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2128/command.yaml"
          && grep -q '^pr_number: 2128$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2128/command.yaml"
  - id: "dod-runtime-compose-build-pr-1849"
    description: >
      omnibase_infra PR #1849 restores the generated runtime compose build stanza and forces deploy-agent
      compose generation to import the staged repo source before ambient PYTHONPATH clones. This closes
      the dev node_redeploy no-op build where runtime:latest kept the May 25 image and blank git revision
      label.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-compose-build-pr-1849/command.yaml"
          && grep -q '^pr_number: 1849$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-compose-build-pr-1849/command.yaml"
  - id: "dod-runtime-compose-build-tests"
    description: >
      Focused catalog generator and deploy-agent tests pass for the runtime compose build/provenance fix,
      including generated Dockerfile.runtime build metadata and stale PYTHONPATH protection.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-compose-build-tests/command.yaml"
          && grep -q '13 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-compose-build-tests/command.yaml"
          && grep -q '31 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-compose-build-tests/command.yaml"
          && grep -q 'Dockerfile.runtime' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-compose-build-tests/command.yaml"
  - id: "dod-runtime-compose-build-deploy-assessment"
    description: >
      Deploy assessment for omnibase_infra PR #1849: dev correlation_id d89f9a03-5229-4377-9e91-8ddbc0aa4501
      proved node_redeploy ownership and post-migration health, but also proved catalog-generated compose
      omitted a runtime build stanza, leaving runtime:latest on the old May 25 image with blank provenance.
      Stability and prod were not touched.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-compose-build-deploy-assessment/command.yaml"
          && grep -q 'd89f9a03-5229-4377-9e91-8ddbc0aa4501' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-compose-build-deploy-assessment/command.yaml"
          && grep -q 'runtime:latest kept the May 25 image' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-compose-build-deploy-assessment/command.yaml"
          && grep -qi 'stability and prod were not touched' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-compose-build-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2130"
    description: "onex_change_control PR #2130 carries the central evidence for the OMN-12618 generated
      runtime compose build/provenance fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2130/command.yaml"
          && grep -q '^pr_number: 2130$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2130/command.yaml"
  - id: "dod-runtime-provenance-direct-url-pr-1850"
    description: >
      omnibase_infra PR #1850 fixes workspace provenance to read PEP 610 direct_url.json from distribution
      metadata so workspace-mode runtime image builds can verify sibling repos installed from local staged
      paths.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-provenance-direct-url-pr-1850/command.yaml"
          && grep -q '^pr_number: 1850$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-provenance-direct-url-pr-1850/command.yaml"
  - id: "dod-runtime-provenance-direct-url-tests"
    description: >
      Focused workspace provenance tests and ruff checks pass for the direct_url metadata reader fix.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-provenance-direct-url-tests/command.yaml"
          && grep -q '13 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-provenance-direct-url-tests/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-runtime-provenance-direct-url-tests/command.yaml"
  - id: "dod-occ-pr-2135"
    description: "onex_change_control PR #2135 carries the central evidence for the OMN-12618 runtime
      provenance direct_url metadata fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2135/command.yaml"
          && grep -q '^pr_number: 2135$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12618/dod-occ-pr-2135/command.yaml"
