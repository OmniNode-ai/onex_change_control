---
schema_version: "1.0.0"
ticket_id: "OMN-13690"
title: "fix(OMN-13690): remove infra direct omnimarket import for delegation plugin"
summary: >-
  Replace the omnibase_infra runtime service_kernel direct import of omnimarket's PluginDelegation with
  onex.domain_plugins entry-point discovery, and add the matching omnimarket entry point. The infra PR
  also vendors the current required node migration sync delta reported by pre-commit. No live deploy,
  restart, .201 mutation, or runtime lane mutation was performed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PASS receipts bind the paired omnimarket and omnibase_infra OMN-13690 PRs."
    command: >-
      grep -q '^status: PASS$' drift/dod_receipts/OMN-13690/dod-market-entrypoint/command.yaml && grep
      -q '^status: PASS$' drift/dod_receipts/OMN-13690/dod-infra-service-kernel/command.yaml && grep -q
      '^status: PASS$' drift/dod_receipts/OMN-13690/dod-migration-sync/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-13690/dod-deploy-assessment/command.yaml && grep -q '^status: PASS$'
      drift/dod_receipts/OMN-13690/dod-occ-self/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-market-entrypoint"
    description: >-
      omnimarket PR #1491 adds the delegation entry point under onex.domain_plugins so infra can discover
      PluginDelegation without importing omnimarket directly.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13690/dod-market-entrypoint/command.yaml"
  - id: "dod-infra-service-kernel"
    description: >-
      omnibase_infra PR #2138 replaces the direct service_kernel omnimarket import with entry-point discovery
      guarded by is_runtime_package_active('omnimarket').
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13690/dod-infra-service-kernel/command.yaml"
  - id: "dod-migration-sync"
    description: >-
      omnibase_infra PR #2138 includes the node migration vendor sync delta required by the repo pre-commit
      gate after the clean dev rebuild.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13690/dod-migration-sync/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: this is an entry-point registration, runtime import-boundary fix, and migration
      vendor-sync PR pair only. No live deploy, restart, .201 mutation, or runtime lane mutation was performed.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13690/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13690 central contract and
      receipt files.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13690/dod-occ-self/command.yaml"
