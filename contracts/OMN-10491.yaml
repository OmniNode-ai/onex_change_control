---
schema_version: "1.0.0"
ticket_id: "OMN-10491"
title: "Populate all llm_call_metrics columns in projection consumer"
summary: "Align the LLM cost projection consumer with the llm_call_metrics schema by populating deterministic
  input hashes, source metadata, raw usage payloads, run identifiers, and an explicit conflict target
  for idempotent inserts."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused LLM cost projection consumer tests pass on omnimarket PR #517"
    command: "cd omni_worktrees/OMN-10491/omnimarket && uv run pytest src/omnimarket/nodes/node_projection_llm_cost/tests/
      -q"
  - kind: "manual"
    description: "Post-merge deploy validation command is recorded; live deploy/restart requires explicit
      approval"
    command: "deploy validation: docker exec omninode-runtime python -m omnimarket.nodes.node_projection_llm_cost.consumer
      --help"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused LLM cost projection consumer tests pass on omnimarket PR #517"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10491/dod-001/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-001\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10491/dod-001/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10491\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10491/dod-001/command.yaml\"
          && grep -q '^commit_sha: \"b11e877af3babbfbf3a69957b390b9b285a992fb\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10491/dod-001/command.yaml\"
          && grep -q '^pr_number: 517$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10491/dod-001/command.yaml\"
          && grep -q '51 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10491/dod-001/command.yaml\""
  - id: "dod-deploy"
    description: "Post-merge deploy smoke plan covers the LLM cost projection consumer without live deploy
      in this PR"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10491/dod-deploy/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-deploy\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10491/dod-deploy/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10491\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10491/dod-deploy/command.yaml\"
          && grep -q '^pr_number: 517$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10491/dod-deploy/command.yaml\"
          && grep -q 'deploy validation' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10491/dod-deploy/command.yaml\""
