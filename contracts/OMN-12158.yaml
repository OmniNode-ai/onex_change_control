---
schema_version: "1.0.0"
ticket_id: "OMN-12158"
title: "Remove core to infra layer inversion in HTTP protocol"
summary: >
  Central OCC evidence for omnibase_core PR #1156. The change removes infra-import-looking docstring examples
  from core HTTP protocol files and adds a regression test proving the HTTP protocol package has no runtime
  infra imports.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "The no-infra-import validator passes for omnibase_core."
    command: "See drift/dod_receipts/OMN-12158/dod-no-infra-imports/command.yaml"
  - kind: "tests"
    description: "Focused HTTP/protocol regression tests pass on the PR head."
    command: "See drift/dod_receipts/OMN-12158/dod-focused-tests/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carries the central OMN-12158 contract and receipts."
    command: "See drift/dod_receipts/OMN-12158/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-no-infra-imports"
    description: "No-infra-import validator reported zero omnibase_infra imports."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12158/dod-no-infra-imports/command.yaml
          && grep -q 'No omnibase_infra imports found' drift/dod_receipts/OMN-12158/dod-no-infra-imports/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused HTTP/protocol regression tests passed on the PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12158/dod-focused-tests/command.yaml
          && grep -q '329 passed' drift/dod_receipts/OMN-12158/dod-focused-tests/command.yaml"
  - id: "dod-occ-pr"
    description: "This onex_change_control PR carries the central OMN-12158 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12158/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: \"OMN-12158\"$' drift/dod_receipts/OMN-12158/dod-occ-pr/command.yaml"
