---
schema_version: "1.0.0"
ticket_id: "OMN-12815"
title: "A1 — COMPLETE endpoints in contracts, posted VERBATIM; delete all in-code URL construction"
summary: >
  Close-the-loop plan track A1. Endpoints are specified COMPLETELY in the contract (incl. the full chat
  path) and posted VERBATIM at every demo-path call site. Every in-code URL constructor/derivation is
  deleted: omnibase_infra HandlerLlmOpenaiCompatible._build_url append branch + _OPERATION_PATHS map;
  omnimarket _split_resolved_endpoint (handler_generation_consumer), _chat_completions_url + _CHAT_COMPLETIONS_SUFFIX
  (handler_llm_delegation_call), the inline /v1/chat/completions append (handler_inference_intent), and
  the /paas/v4-vs-/v1 chat_path branches (adapter_llm_dispatch, handler_local_supervisor). Every POST
  site sends the contract endpoint_url unchanged and fails closed when it is empty/missing. bifrost_delegation.yaml
  cloud demo backends carry the COMPLETE endpoint URL; site-specific locals remain null in the repo default
  and MUST be supplied COMPLETE by the deploy overlay (documented in the contract header).
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: "dod-001-no-url-construction-market"
    description: >-
      omnimarket URL-construction helpers (_split_resolved_endpoint, _chat_completions_url, _CHAT_COMPLETIONS_SUFFIX)
      are deleted; receipt records the grep-exit-1 (no-match) probe run by a verifier != author.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12815/dod-001-no-url-construction-market/command.yaml
  - id: "dod-002-no-operation-paths-infra"
    description: >-
      omnibase_infra _OPERATION_PATHS map is deleted from src; receipt records the grep-exit-1 probe.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12815/dod-002-no-operation-paths-infra/command.yaml
  - id: "dod-003-verbatim-post-tests"
    description: >-
      Demo-path verbatim-POST tests pass (infra _build_url TestBuildUrl; market inference-intent verbatim
      POST; bifrost contract endpoint invariant).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12815/dod-003-verbatim-post-tests/command.yaml
  - id: "dod-004-quality-gates"
    description: >-
      mypy --strict clean on both repos; ruff clean; full omnimarket suite green (not kafka); infra inference-effect
      + adapter/handler/service/model unit suites green.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12815/dod-004-quality-gates/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment for the runtime-path touches in omnimarket and omnibase_infra (LLM inference/delegation
      handlers). Live stability-test deploy validation is intentionally post-merge Phase D and requires
      explicit runtime approval; no live deploy was performed in this PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12815/dod-deploy-assessment/command.yaml &&
          grep -q 'post-merge' drift/dod_receipts/OMN-12815/dod-deploy-assessment/command.yaml
  - id: "dod-occ-pr"
    description: "onex_change_control PR carrying this OMN-12815 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12815/dod-occ-pr/command.yaml
