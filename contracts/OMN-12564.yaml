---
schema_version: "1.0.0"
ticket_id: "OMN-12564"
title: "ci: harden OMN-12564 CI environment and OCC checkout lanes"
summary: >-
  Adds the omnibase_infra canary for immutable host-local CI dependency environments under /home/runner/.cache/omni/ci-envs/omnibase_infra/<digest>.
  Trusted read-only CI jobs build/reuse the digest environment under a lock, expose it through a checkout-local
  .venv symlink and runner-temp uv wrapper for existing uv run commands, consume it with UV_NO_SYNC=1
  and PYTHONPATH=<checkout>/src, and leave public fork PRs plus env-mutating jobs on isolated setup. This
  removes repeated dependency resolution from ordinary CI jobs while keeping checked-out PR source authoritative.
  Shared env builds use bounded retries for transient git and wheel fetch failures, and runner-temp package
  metadata exposes checkout entry points without mutating the shared venv. The follow-up cache-save fix
  disables setup-uv cache upload for the direct OCC contract-compliance job because that job runs uv sync
  --no-cache and merge-group CI proved the post-job cache upload can cancel after the actual validation
  work succeeds. Also binds the omniclaude deploy-gate reusable workflow hardening that removes the blobless
  OCC partial-clone checkout path and adds bounded fetch/checkout diagnostics for PR #1718, then promotes
  that reusable workflow fix to omniclaude main through PR #1735 so downstream @main callers receive it.
  The main hotfix also resolves central OCC DoD evidence from the live open PR on push events, keeping
  push and pull_request CI lanes bound to the same authoritative evidence source.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1824"
    description: "omnibase_infra PR #1824 carries the versioned CI environment canary implementation and
      docs."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-infra-pr-1824/command.yaml"
          && grep -q '^pr_number: 1824$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-infra-pr-1824/command.yaml"
          && grep -q '^commit_sha: "5edfa569de568055427f1a5ead462ce3faa4bd94"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-infra-pr-1824/command.yaml"
  - id: "dod-local-env-proof"
    description: "Local temp-root proof built and then reused the immutable CI env without touching privileged
      paths."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-local-env-proof/command.yaml"
  - id: "dod-infra-pr-1825"
    description: "omnibase_infra PR #1825 disables setup-uv cache save for the direct OCC contract-compliance
      job after merge-group CI showed the post-job cache upload can cancel the queue run."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-infra-pr-1825/command.yaml"
          && grep -q '^pr_number: 1825$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-infra-pr-1825/command.yaml"
          && grep -q '^commit_sha: "dde9ee2b502de4507a5ab4f1d3b5c9a6860e8185"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-infra-pr-1825/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12564 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-occ-pr-self/command.yaml"
          && grep -q '^pr_number: 2020$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-occ-pr-self/command.yaml"
  - id: "dod-occ-cache-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the cache-save follow-up evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-occ-cache-pr-self/command.yaml"
          && grep -q '^pr_number: 2021$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-occ-cache-pr-self/command.yaml"
  - id: "dod-omniclaude-pr-1718-checkout-hardening"
    description: >-
      omniclaude PR #1718 extracts the OCC deploy-gate checkout into a bounded script, removes the blobless
      partial-clone checkout path, and proves the stall case terminates with diagnostics instead of hanging
      the deploy-gate job.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-omniclaude-pr-1718-checkout-hardening/command.yaml"
          && grep -q '^pr_number: 1718$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-omniclaude-pr-1718-checkout-hardening/command.yaml"
          && grep -q '^commit_sha: "2850f546b0ee8913f8eae844f18455fefa1c1a32"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-omniclaude-pr-1718-checkout-hardening/command.yaml"
  - id: "dod-occ-pr-2039-omniclaude-binding"
    description: "Self-binding receipt for OCC PR #2039 carrying the omniclaude PR #1718 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-occ-pr-2039-omniclaude-binding/command.yaml"
          && grep -q '^pr_number: 2039$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-occ-pr-2039-omniclaude-binding/command.yaml"
  - id: "dod-omniclaude-pr-1735-main-hotfix"
    description: >-
      omniclaude PR #1735 carries the already-proven OMN-12564 bounded OCC deploy-gate checkout fix onto
      main, where downstream reusable workflow callers resolve it via omniclaude@main.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-omniclaude-pr-1735-main-hotfix/command.yaml"
          && grep -q '^pr_number: 1735$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-omniclaude-pr-1735-main-hotfix/command.yaml"
          && grep -q '^commit_sha: "b58e2c87d5912dea3cc5bb957462fa0358e529c7"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-omniclaude-pr-1735-main-hotfix/command.yaml"
  - id: "dod-occ-pr-2326-omniclaude-main-hotfix-binding"
    description: "Self-binding receipt for OCC PR #2326 carrying the omniclaude PR #1735 main-hotfix evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-occ-pr-2326-omniclaude-main-hotfix-binding/command.yaml"
          && grep -q '^pr_number: 2326$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-occ-pr-2326-omniclaude-main-hotfix-binding/command.yaml"
  - id: "dod-occ-pr-2329-omniclaude-push-dod-binding"
    description: "Self-binding receipt for OCC PR #2329 carrying the omniclaude PR #1735 push-event DoD
      resolver evidence refresh."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-occ-pr-2329-omniclaude-push-dod-binding/command.yaml"
          && grep -q '^pr_number: 2329$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12564/dod-occ-pr-2329-omniclaude-push-dod-binding/command.yaml"
