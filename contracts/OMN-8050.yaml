---
schema_version: "1.0.0"
ticket_id: OMN-8050
title: 'Task 3: node_local_supervisor in omnimarket'
summary: "Task 3: node_local_supervisor in omnimarket"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8050/dod-001/command.yaml\""
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8050/dod-002/command.yaml\""
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8050/dod-003/command.yaml\""
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8050/dod-004/command.yaml\""
  - id: "dod-deploy"
    description: "Post-merge deploy validation command is recorded for node_local_supervisor runtime paths"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "echo deploy: docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c 'from
          omnimarket.nodes.node_local_supervisor.handlers.handler_local_supervisor import HandlerLocalSupervisor;
          print(HandlerLocalSupervisor.__name__)'"
  - id: "dod-occ-validation"
    description: "OCC evidence PR validates against receipt gate"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8050/dod-occ-validation/command.yaml\""
