---
schema_version: "1.0.0"
ticket_id: "OMN-13542"
title: "Acceptance criteria validation returns actionable error naming allowed slugs"
summary: >-
  Central OCC evidence for omnibase_core PR #1319. The PR improves validate_acceptance_criteria so unsupported
  free-text criteria are quoted and the error names the allowed slug set plus the max_words_per_sentence_N
  pattern.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Delegation wire model unit tests pass with OMN-13542 actionable-error coverage."
    command: "uv run pytest tests/unit/models/delegation/wire/test_delegation_wire_models.py -q"
  - kind: "ci"
    description: "omnibase_core PR #1319 checks pass."
    command: "gh pr checks 1319 -R OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1319"
    description: "omnibase_core PR #1319 carries the actionable acceptance_criteria validation error fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13542/dod-core-pr-1319/command.yaml"
  - id: "dod-focused-wire-tests"
    description: "Focused delegation wire model tests pass on the PR worktree."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13542/dod-focused-wire-tests/command.yaml"
  - id: "dod-occ-pr-self"
    description: "This OCC evidence PR is bound by a PASS receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13542/dod-occ-pr-self/command.yaml"
