---
schema_version: "1.0.0"
ticket_id: "OMN-12079"
title: "B3 convergence fixture — cost curve visualization with provenance labels"
summary: >
  Central OCC evidence for onex-self-extending-agent PR #132, which adds the committed convergence baseline
  fixture (docs/evidence/convergence_baseline.json), --convergence CLI flag, ledger_examples column in
  display_convergence_chart, and FIXTURE/LIVE_MEASURED/ESTIMATED provenance labels. Covers OMN-12078,
  OMN-12079, OMN-12080, OMN-12081.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-132"
    description: "onex-self-extending-agent PR #132 carries the B3 convergence fixture."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12079/dod-product-pr-132/command.yaml"
  - id: "dod-ci-validation"
    description: "GitHub checks for PR #132 pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12079/dod-ci-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      No live deploy required. The convergence baseline is a committed evidence file and display-only
      CLI flag; no runtime services are affected.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12079/dod-deploy-assessment/command.yaml"
