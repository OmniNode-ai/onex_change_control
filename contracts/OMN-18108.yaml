---
schema_version: "1.0.0"
ticket_id: "OMN-18108"
title: "Autobind OCC evidence for OMN-18108"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #3378."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3378 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3378 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest scripts/deploy-agent/tests/unit/test_compose_budget_omn18057.py scripts/deploy-agent/tests/unit/test_dev_lane_only_scope_omn18108.py scripts/deploy-agent/tests/unit/test_executor_build_ceiling_omn18072.py scripts/deploy-agent/tests/unit/test_executor_build_path_omn16442.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3378"
    description: "PR #3378 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/deploy-agent/deploy_agent/executor.py?ref=9b512d84982f08d1264556633b827e5b640edf93 --jq '.content' | base64 -d | grep -c 'def _build_dev_lane_only_services'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3378-ci"
    description: "PR #3378 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3378 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3378 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest scripts/deploy-agent/tests/unit/test_compose_budget_omn18057.py scripts/deploy-agent/tests/unit/test_dev_lane_only_scope_omn18108.py scripts/deploy-agent/tests/unit/test_executor_build_ceiling_omn18072.py scripts/deploy-agent/tests/unit/test_executor_build_path_omn16442.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-8866"
    description: "OCC companion PR #8866 — self-bind for OMN-18108 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8866 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3378-ac-binding"
    description: |-
      PR #3378 on OmniNode-ai/omnibase_infra — the AC binding for OMN-18108, appended by hand because
      the autobind entries above carry no binds_ac. APPENDED, NOT EDITED: every entry above is
      byte-unchanged, so each keeps the contract_entry_sha256 its already-PASSing receipt under
      drift/dod_receipts/OMN-18108/ was minted against (OMN-13888). The self-bind entry deliberately
      carries no binds_ac either — it is an infrastructure binding to the evidence PR, not evidence for
      an acceptance criterion.

      WHAT THE PR PROVES, PER AC. AC1: the RED-first test. At the parent commit 17047498145d the new
      suite fails on the missing declaration, and the substantive gap probes as
      "services_for_scope(RUNTIME) = 10 services" with all eight DEV_LANE_ONLY_RUNTIME_SERVICES missing
      from every agent-path dev deploy. AC2: services_for_scope() takes an optional lane and resolves the
      dev-lane addendum for DEV, and the BUILD half is covered by a second additive build carrying the
      lane overlay — without it the up phase would recreate the eight from their existing stale images
      and report success. AC3: prod and stability-test scope is byte-unchanged; every prod/stability
      scope test in the repo is untouched, and a new parametrised class asserts both lanes still resolve
      the base list and share no service name with the dev-lane set. AC4: the anti-drift test parses
      DEV_LANE_ONLY_RUNTIME_SERVICES out of scripts/deploy-runtime.sh and asserts set equality with the
      Python tuple in BOTH directions, with a positive control so an empty parse cannot read as
      agreement. AC5: onex-api is declared tag-referenced and excluded from the build set while staying
      in the recreate set, with a test stating that a governed deploy can recreate it but cannot advance
      its tag.

      THE DEFECT, MEASURED. On the .201 dev lane 2026-09-10T00:45Z the runtime family carried the deploy
      agent's own build 4598a4358bd9f59528875b8b320b6cde54383fb1 while all eight dev-lane-only services
      carried 3461e4b0aeae from the previous day, ~35 infra commits behind, with restart: unless-stopped
      keeping each stale image running and healthy so nothing reported it.

      RULE 24 STATED RATHER THAN IMPLIED. This lane held the .201 dev lane READ-ONLY and mutated nothing
      — no compose up, no recreate, no restart, no --cold, no docker tag. The change reaches the lane
      through the deploy agent's own clone after this merges. NO LAB PASS IS CLAIMED HERE; the
      post-merge readback of the eight services' image revision after the first agent-path deploy
      carrying this code is recorded as a PROGRESS row by lane devlane-agent-scope-and-writer-role in
      omni_home docs/tracking/ROLLING_WORK_LEDGER.md.

      RULE 22: no credential was rotated, re-issued, revoked or minted, and no secret value was read,
      printed or decoded.
    source: "manual"
    binds_ac:
      - "AC1"
      - "AC2"
      - "AC3"
      - "AC4"
      - "AC5"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3378 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "test_passes"
        check_value: |-
          uv run --project scripts/deploy-agent --extra dev pytest scripts/deploy-agent/tests -q
        cwd: "${OMNI_HOME}/omnibase_infra"
