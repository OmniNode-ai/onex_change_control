---
schema_version: "1.0.0"
ticket_id: "OMN-14812"
title: "Autobind OCC evidence for OMN-14812"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1907."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1907 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1907"
    description: "PR #1907 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1907-ci"
    description: "PR #1907 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4981"
    description: "OCC companion PR #4981 — self-bind for OMN-14812 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-promotion-head-readback"
    description: >-
      Falsifiable deploy-gate probe for omnimarket PR #1907: read promoted repository content at the exact
      product PR head through the GitHub API. This proves the promotion head is addressable on the remote
      product repository without requiring a live deploy, runtime restart, .201 mutation, or production
      data mutation.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket?ref=c5b7af1160c4453b3aeee828fbe3e5d64da69195
  - id: "occ-self-bind-pr-4996"
    description: "OCC companion PR #4996 — deploy-gate evidence repair for OMN-14812."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4996 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid"
  - id: "dod-OmniNode-ai-omnimarket-pr-1910"
    description: "PR #1910 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1910-ci"
    description: "PR #1910 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5022"
    description: "OCC companion PR #5022 — self-bind for OMN-14812 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
