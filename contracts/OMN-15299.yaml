---
schema_version: "1.0.0"
ticket_id: "OMN-15299"
title: "Autobind OCC evidence for OMN-15299"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #750."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #750 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-750"
    description: "PR #750 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15299/dod-OmniNode-ai-omninode_infra-pr-750/command.yaml"
  - id: "dod-OmniNode-ai-omninode_infra-pr-750-ci"
    description: "PR #750 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15299/dod-OmniNode-ai-omninode_infra-pr-750-ci/command.yaml"
  - id: "occ-self-bind-pr-5414"
    description: "OCC companion PR #5414 — self-bind for OMN-15299 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "omn-15299-plan-oidc-red"
    description: >-
      The terraform plan workflow at omninode_infra#750 head b93a644f now fails the job when AWS credentials
      cannot be assumed, instead of reporting a green plan that never ran.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omninode_infra/contents/.github/workflows/terraform-plan.yml?ref=b93a644fb3c4039b3bc6d4727be73e920417753d
          --jq .content | base64 -d | grep -q 'Fail job if AWS credentials could not be assumed (OMN-15299)'"
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omninode_infra/contents/.github/workflows/terraform-plan.yml?ref=b93a644fb3c4039b3bc6d4727be73e920417753d
          --jq .content | base64 -d | grep -q 'is not a passed plan (OMN-15299)'"
  - id: "omn-15299-regression-harness"
    description: >-
      The product PR includes an executable regression harness with explicit tests for the credential-failure,
      empty-root, and plan-error fail-open paths that OMN-15299 closes.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omninode_infra/contents/tests/k8s/test_terraform_plan_fail_closed.py?ref=b93a644fb3c4039b3bc6d4727be73e920417753d
          --jq .content | base64 -d | grep -q 'test_fixed_workflow_fails_with_failed_credentials'"
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omninode_infra/contents/tests/k8s/test_terraform_plan_fail_closed.py?ref=b93a644fb3c4039b3bc6d4727be73e920417753d
          --jq .content | base64 -d | grep -q 'test_plan_job_fails_on_a_root_with_no_terraform'"
