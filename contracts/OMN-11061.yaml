---
schema_version: "1.0.0"
ticket_id: "OMN-11061"
title: "Wire undeclared topic literals to contract constants"
summary: >
  Central OCC evidence for omnimarket PR #657. Reduces UNDECLARED_TOPIC findings from 75 to 0 in omnimarket
  by wiring 49 omniclaude/omniintelligence fan-out topics into node_emit_daemon contract.yaml, declaring
  missing topics in 11 additional node contract.yaml files, extending ContractTopologyParser to skip test
  files and respect the allowlist for UNDECLARED_TOPIC suppression, and fixing the bare onex-allow-internal-ip
  annotations in bifrost_delegation.yaml to include required ticket+reason. Also adds handler block to
  node_monitor_alert_responder contract.yaml (removes it from KNOWN_MAIN_VIOLATIONS strict-mode failure).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Test suite passes (3967 passed); UNDECLARED_TOPIC findings reduced to 0."
    command: "See drift/dod_receipts/OMN-11061/dod-sweep-clean/command.yaml"
  - kind: "ci"
    description: "Deploy assessment — contract YAML + sweep engine change; no live deploy required."
    command: "See drift/dod_receipts/OMN-11061/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sweep-clean"
    description: "Test suite passes with 0 UNDECLARED_TOPIC findings after wiring node contracts and allowlist
      propagation."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-11061/dod-sweep-clean/command.yaml && echo
          'OMN-11061 sweep-clean receipt present'"
  - id: "dod-deploy"
    description: "Deploy assessment recorded — contract YAML + sweep engine patch; no live deploy required."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-11061/dod-deploy/command.yaml && grep
          -q 'evidence_item_id: \"dod-deploy\"' drift/dod_receipts/OMN-11061/dod-deploy/command.yaml &&
          echo 'OMN-11061 deploy receipt present and PASS'"
  - id: "dod-occ-pr-1044"
    description: "Self-binding receipt for OCC PR #1044 — anchors evidence chain to this OCC PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'status: PASS' drift/dod_receipts/OMN-11061/dod-occ-pr-1044/command.yaml && grep -q
          'pr_number: 1044' drift/dod_receipts/OMN-11061/dod-occ-pr-1044/command.yaml
