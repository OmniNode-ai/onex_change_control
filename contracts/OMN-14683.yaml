---
schema_version: "1.0.0"
ticket_id: "OMN-14683"
title: "Audit branch protection on dev and main"
summary: >
  Extend the onex_change_control branch-protection audit so it inspects both dev and main, uses the authoritative
  GraphQL reviews signal, and reports branch-attributed evidence for branch-protection drift.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "OCC PR #4281 self-binding is present"
    command: "PR_NUMBER=4281; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo ${REPO}
      --json number,state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-branch-protection-dev-audit"
    description: "Branch-protection audit covers dev and uses the GraphQL reviews enforcement signal."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'BRANCHES=(main dev)' scripts/audit_branch_protection.sh && grep -q 'requiresApprovingReviews'
          scripts/audit_branch_protection.sh && grep -q 'required_pull_request_reviews phantom' scripts/audit_branch_protection.sh"
  - id: "occ-self-bind-pr-4281"
    description: "OCC PR #4281 carries the OMN-14683 branch-protection audit update."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4281; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state,headRefOid"
