---
schema_version: "1.0.0"
ticket_id: "OMN-10919"
title: "Typed delegation runtime profile contract models"
summary: "Adds frozen Pydantic v2 contract models for delegation runtime profile configuration, including
  LLM backend, security, projection, pricing, datastore, and secret reference sections."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Contract model unit tests and strict typing checks pass in omnibase_core PR #1072."
    command: "gh pr checks --repo OmniNode-ai/omnibase_core 1072"
  - kind: "tests"
    description: "Contract model unit tests pass in omnibase_compat PR #87 (wire DTOs moved from core
      to compat)."
    command: "gh pr checks --repo OmniNode-ai/omnibase_compat 87"
  - kind: "manual"
    description: "Central OCC receipt binds OMN-10919 to omnibase_core PRs #1072/#1075 and omnibase_compat
      PR #87."
    command: "find drift/dod_receipts/OMN-10919 -type f -name command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1072"
    description: "omnibase_core PR #1072 adds typed delegation runtime profile contract models."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10919/dod-core-pr-1072/command.yaml
          && grep -q '^pr_number: 1072$' drift/dod_receipts/OMN-10919/dod-core-pr-1072/command.yaml"
  - id: "dod-compat-pr-87"
    description: "omnibase_compat PR #87 moves delegation wire DTOs from core to compat layer."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10919/dod-compat-pr-87/command.yaml
          && grep -q '^pr_number: 87$' drift/dod_receipts/OMN-10919/dod-compat-pr-87/command.yaml"
  - id: "dod-core-pr-1075"
    description: "omnibase_core PR #1075 removes duplicated delegation models and lazily re-exports compat
      DTOs from the existing public API."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10919/dod-core-pr-1075/command.yaml
          && grep -q '^pr_number: 1075$' drift/dod_receipts/OMN-10919/dod-core-pr-1075/command.yaml"
  - id: "dod-occ-pr-992"
    description: "OCC PR #992 carries the OMN-10919 compat receipt update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10919/dod-occ-pr-992/command.yaml
          && grep -q '^pr_number: 992$' drift/dod_receipts/OMN-10919/dod-occ-pr-992/command.yaml"
