---
schema_version: "1.0.0"
ticket_id: "OMN-11808"
title: "KB ADR publisher node"
summary: >
  EFFECT node node_kb_adr_publisher with full contract.yaml declaring topics.
  Subscribes to onex.cmd.omnimarket.kb-adr-publish-requested.v1 and publishes
  onex.evt.omnimarket.kb-adr-publish-completed.v1. Handler reads extracted_decisions.json,
  renders via kb_adr_renderer adapter, clones KB repo, opens PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-775"
    description: "omnimarket PR #775 implements KB ADR publisher node with 9 unit tests"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11808/dod-omnimarket-pr-775/command.yaml\" && grep -q '^pr_number: 775$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11808/dod-omnimarket-pr-775/command.yaml\""
  - id: "dod-occ-pr-1425"
    description: "OCC PR #1425 binds this contract to onex_change_control as the evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11808/dod-occ-pr-1425/command.yaml\" && grep -q '^pr_number: 1425$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11808/dod-occ-pr-1425/command.yaml\""
  - id: "dod-deploy"
    description: "Deploy assessment — new EFFECT node, no live deploy required for initial merge; node is invoked on-demand via contract topic"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11808/dod-deploy/command.yaml\""
