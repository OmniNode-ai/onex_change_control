---
schema_version: "1.0.0"
ticket_id: "OMN-13677"
title: "WS-5 Wave 3 multi-param integration — PR-lifecycle orchestrator + effects"
summary: >-
  omnimarket PR #1480 adds real multi-parameter integration tests (>=3 param sets and >=1 negative control
  each) for the Wave 3 PR-lifecycle market-skill backing nodes: node_pr_lifecycle_orchestrator (parametrized
  Variant B bus round-trip incl. the verify=True VERIFYING path), node_auto_merge_effect, node_coderabbit_triage,
  node_ci_watch, node_pr_polish, and node_pr_watch_orchestrator. Tests assert typed result fields and
  terminal-event payloads; I/O boundaries are mocked via injected collaborators (no subprocess/asyncpg
  monkeypatch). This is test-only: no runtime handler/source change, no topic, schema, migration, or .201/.200
  runtime mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >-
      Wave 3 multi-param integration suite passes in-process on EventBusInmemory with no external services.
    command: "uv run pytest tests/integration/node_auto_merge_effect tests/integration/node_coderabbit_triage
      tests/integration/node_ci_watch tests/integration/node_pr_polish tests/integration/node_pr_watch_orchestrator
      tests/integration/test_pr_lifecycle_orchestrator_round_trip.py -q"
  - kind: "tests"
    description: >-
      Full non-Kafka omnimarket suite passed locally before opening PR #1480.
    command: "uv run pytest tests/ -m 'not kafka' -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1480"
    description: >-
      omnimarket PR #1480 carries the Wave 3 multi-param integration tests for OMN-13677 at head commit
      e287e4e62027dd744d71e69c75e303f87779fc3c. Wave 3 suite = 39 passed; full non-Kafka suite = 9927
      passed, 46 skipped.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13677/dod-omnimarket-pr-1480/command.yaml
          && grep -q '^pr_number: 1480$' drift/dod_receipts/OMN-13677/dod-omnimarket-pr-1480/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: PR #1480 is test-only (tests/integration only, no src change). No live deploy,
      runtime restart, topic mutation, schema migration, database migration, or .201/.200 mutation is
      performed by this evidence PR or by merging #1480.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13677/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-13677/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-3231"
    description: >-
      Self-binding OCC eligibility receipt for onex_change_control PR #3231, which carries this contract
      and receipts onto dev so downstream omnimarket PR #1480 can cite Evidence-Source: OCC#3231.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13677/dod-occ-pr-3231/command.yaml
          && grep -q '^pr_number: 3231$' drift/dod_receipts/OMN-13677/dod-occ-pr-3231/command.yaml"
