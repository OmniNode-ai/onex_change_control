---
schema_version: "1.0.0"
ticket_id: "OMN-10087"
title: Split Keycloak desired client state out of the realm import file
summary: "Split Keycloak desired client state out of the realm import file"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Realm file guard passes and proves clients/clientScopes are absent"
    command: "cd ../omnibase_infra && bash scripts/check-realm-no-clients.sh && jq '.clients,.clientScopes'
      docker/keycloak/omninode-realm.json"
  - kind: "ci"
    description: "omnibase_infra PR checks pass for the Keycloak realm split"
    command: "gh pr checks 1431 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "The Keycloak realm import no longer owns clients or client scopes"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10087/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "The dedicated desired clients file is present for the reconciler follow-up"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10087/dod-002 -type f -name '*.yaml' | grep -q ."
