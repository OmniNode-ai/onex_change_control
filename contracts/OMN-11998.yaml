---
schema_version: "1.0.0"
ticket_id: "OMN-11998"
title: "Add swarm_runs migration for swarm dispatch projection"
summary: >
  Central OCC evidence for omnibase_infra PR #1737, which adds forward migration 082_swarm_runs.sql so
  the swarm_runs projection table is created by the migration-gate mechanism instead of dev-lane direct
  SQL.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1737"
    description: "omnibase_infra PR #1737 carries the OMN-11998 swarm_runs forward migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11998/dod-omnibase-infra-pr-1737/command.yaml
          && grep -q '^pr_number: 1737$' drift/dod_receipts/OMN-11998/dod-omnibase-infra-pr-1737/command.yaml"
  - id: "dod-migration-file-validation"
    description: "The forward migration file exists and defines the swarm_runs projection table shape."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11998/dod-migration-file-validation/command.yaml
          && grep -q 'CREATE TABLE IF NOT EXISTS swarm_runs' drift/dod_receipts/OMN-11998/dod-migration-file-validation/command.yaml
          && grep -q 'models_used' drift/dod_receipts/OMN-11998/dod-migration-file-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy was performed; the migration remains gated by CI, deploy-gate, and the
      migration-gate rollout."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11998/dod-deploy-assessment/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-11998/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-1606"
    description: "onex_change_control PR #1606 carries the central OCC evidence for OMN-11998."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11998/dod-occ-pr-1606/command.yaml
          && grep -q '^pr_number: 1606$' drift/dod_receipts/OMN-11998/dod-occ-pr-1606/command.yaml"
