---
schema_version: "1.0.0"
ticket_id: "OMN-10967"
title: "Remove free-text skip bypass from integration-test-check.yml"
summary: "Removes the [skip-test-removal-gate:] free-text override mechanism from the check-test-removal
  job in omnibase_infra's integration-test-check.yml. PRs that delete integration tests must now include
  a replacement test in the same PR — no bypass token is accepted. This closes an enforcement gap where
  any PR body text could bypass the gate."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1592 removes the grep-qF skip-test-removal-gate block and ships with
      all CI gates green."
    command: "gh pr view 1592 --repo OmniNode-ai/omnibase_infra --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1592"
    description: "omnibase_infra PR #1592 removes the skip-test-removal-gate bypass block."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10967/dod-omnibase-infra-pr-1592/command.yaml
          && grep -q '^pr_number: 1592$' drift/dod_receipts/OMN-10967/dod-omnibase-infra-pr-1592/command.yaml"
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carries the OMN-10967 contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10967/dod-occ-pr-self/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10967/dod-occ-pr-self/command.yaml"
