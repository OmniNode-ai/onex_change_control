---
schema_version: "1.0.0"
ticket_id: "OMN-11080"
title: "Stop-Phase Quality Gate — Changed-File Lint and Test"
summary: >
  Central OCC evidence for omniclaude PR #1592. Adds a Stop hook quality gate that detects changed Python
  files, runs bounded ruff, mypy, and unit-test checks, blocks actual violations with exit 2, skips outside
  git worktrees, and preserves the shared hook-bit/default-mask ownership boundary for OMN-11083.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused Stop quality gate tests pass."
    command: "See drift/dod_receipts/OMN-11080/dod-stop-quality-tests/command.yaml"
  - kind: "ci"
    description: "Changed Stop quality gate Python files pass ruff, hooks.json parses, and shell wrapper
      parses."
    command: "See drift/dod_receipts/OMN-11080/dod-stop-quality-lint/command.yaml"
  - kind: "tests"
    description: "Hook registration path tests and adjacent Stop hook tests pass."
    command: "See drift/dod_receipts/OMN-11080/dod-hook-registration/command.yaml"
  - kind: "ci"
    description: "omniclaude pre-push hooks pass for the implementation branch."
    command: "See drift/dod_receipts/OMN-11080/dod-pre-push/command.yaml"
  - kind: "ci"
    description: "omniclaude PR #1592 is bound to the observed implementation head."
    command: "See drift/dod_receipts/OMN-11080/dod-omniclaude-pr-1592/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-stop-quality-tests"
    description: "Focused Stop quality gate subprocess tests passed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11080/dod-stop-quality-tests/command.yaml\"
          && grep -q '8 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11080/dod-stop-quality-tests/command.yaml\""
  - id: "dod-stop-quality-lint"
    description: "Ruff format/check, hooks.json parsing, and shell syntax checks passed for Stop quality
      gate files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11080/dod-stop-quality-lint/command.yaml\"
          && grep -q 'All checks passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11080/dod-stop-quality-lint/command.yaml\""
  - id: "dod-hook-registration"
    description: "hooks.json registration tests and adjacent Stop hook regression tests passed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11080/dod-hook-registration/command.yaml\"
          && grep -q '149 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11080/dod-hook-registration/command.yaml\"
          && grep -q '10 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11080/dod-hook-registration/command.yaml\""
  - id: "dod-pre-push"
    description: "omniclaude branch push passed pre-push hooks after generated root cache cleanup."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11080/dod-pre-push/command.yaml\"
          && grep -q 'Validate clean root directory.*Passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11080/dod-pre-push/command.yaml\""
  - id: "dod-omniclaude-pr-1592"
    description: "omniclaude PR #1592 carries the OMN-11080 implementation at the observed head SHA."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11080/dod-omniclaude-pr-1592/command.yaml\"
          && grep -q '^pr_number: 1592$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11080/dod-omniclaude-pr-1592/command.yaml\"
          && grep -q '^commit_sha: \"c416b71c5d35e5223f9535b7d6836b6d7750e4fd\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11080/dod-omniclaude-pr-1592/command.yaml\""
