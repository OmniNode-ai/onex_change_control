---
schema_version: 1.0.0
ticket_id: OMN-910
title: Add percentile-based thresholds for performance logging
summary: Add percentile-based thresholds for performance logging
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
- kind: ci
  description: CI passes on merge PR
  command: gh pr checks
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
- id: dod-001
  description: Contract YAML for this ticket parses as valid YAML
  source: generated
  checks:
  - check_type: command
    check_value: uv run python -c "import yaml; yaml.safe_load(open('contracts/${TICKET_ID}.yaml').read());
      print('ok')"
- id: dod-002
  description: PR title or body cites this ticket
  source: generated
  checks:
  - check_type: command
    check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json title,body --jq '.title
      + " " + (.body // "")' | grep -q '${TICKET_ID}'
