---
schema_version: "1.0.0"
ticket_id: OMN-9751
summary: "feat(infra): typed ModelFeatureFlagContract + load_service_contract loader"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "10 unit tests for ModelFeatureFlagContract and load_service_contract pass"
    command: "uv run pytest tests/unit/runtime/test_service_contract_loader.py -v"
  - kind: ci
    description: "omnibase_infra PR #1417 CI checks pass"
    command: "gh pr checks 1417 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "ModelFeatureFlagContract model_validate() parses both runtime.contract.yaml and event_bus.contract.yaml
      correctly"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9751/dod-001/file_exists.yaml"
  - id: dod-002
    description: "10 unit tests pass — all pre-commit hooks pass; mypy clean on push"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9751/dod-002/file_exists.yaml"
