---
schema_version: "1.0.0"
ticket_id: "OMN-10695"
title: "Build decision extraction LLM effect node"
summary: >
  Adds node_adr_decision_extraction_llm_effect to omnimarket — the core intelligence EFFECT node for the
  ADR canary pipeline. Accepts segmented document content and a caller-specified model key, identifies
  architectural decisions, pivots, doctrine, operational lessons, supersessions, and rejected approaches
  via a six-type taxonomy. Called 19 times per document in the canary (once per model). Extraction IDs
  are deterministically computed from extraction_version, model_id, sorted segment IDs, and sorted segment
  content hashes. This is a new omnimarket node — no runtime deploy required.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests-pass"
    description: "38/38 unit tests pass for node_adr_decision_extraction_llm_effect with mocked LLM responses."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10695/dod-tests-pass/command.yaml\""
  - id: "dod-deploy"
    description: "No runtime deploy required — node_adr_decision_extraction_llm_effect is a new omnimarket
      node loaded dynamically from the package. No container rebuild or daemon restart needed. deploy
      evidence: omnimarket PR #564 CI passes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10695/dod-deploy/command.yaml\""
  - id: "dod-pr-564-head"
    description: "omnimarket PR #564 carries the node_adr_decision_extraction_llm_effect implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10695/dod-pr-564-head/command.yaml\""
