---
schema_version: "1.0.0"
ticket_id: "OMN-10203"
summary: "Add Pattern B broker command/result models and publish client"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "envelopes"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused Pattern B broker client and model tests pass in omnibase_core"
    command: "uv run pytest tests/unit/dispatch/test_dispatch_bus_client.py -q"
  - kind: "manual"
    description: "Typed broker publish/client surface is documented and does not depend on Agent()"
    command: "See canonical receipts under drift/dod_receipts/OMN-10203/"
  - kind: "ci"
    description: "Downstream omnibase_core PR checks are green"
    command: "gh pr checks --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Typed broker command and terminal-result models exist and validate"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10203/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "Dispatch bus client publishes via contract-driven topic/config lookup"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10203/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "Client remains safe for non-foreground contexts and imports no Agent() surface"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10203/dod-003 -type f -name '*.yaml' | grep -q ."
