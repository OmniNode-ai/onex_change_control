---
schema_version: "1.0.0"
ticket_id: "OMN-12544"
title: "ci: isolate required merge-queue runners from advisory and heavy jobs"
summary: >
  Add runner selector overrides so required merge_group CI and Docker-heavy jobs can be routed to separate
  self-hosted runner pools when repo variables are configured.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Workflow resilience tests prove merge_group and Docker runner override variables are
      present."
    command: "See drift/dod_receipts/OMN-12544/dod-runner-selector-tests/command.yaml"
  - kind: "ci"
    description: "omnibase_infra PR #1819 carries the runner selector override changes."
    command: "See drift/dod_receipts/OMN-12544/dod-infra-pr-1819/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-runner-selector-tests"
    description: "Focused CI workflow resilience tests and ruff checks pass for the PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12544/dod-runner-selector-tests/command.yaml
          && grep -q '8 passed' drift/dod_receipts/OMN-12544/dod-runner-selector-tests/command.yaml"
  - id: "dod-infra-pr-1819"
    description: "omnibase_infra PR #1819 is open at the runner selector override commit."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12544/dod-infra-pr-1819/command.yaml
          && grep -q '^pr_number: 1819$' drift/dod_receipts/OMN-12544/dod-infra-pr-1819/command.yaml"
