---
schema_version: "1.0.0"
ticket_id: "OMN-13669"
title: "fix: deploy-runtime.sh passes PROMOTION_CLASS + NON_MAIN_LINEAGE build-args"
summary: >
  Add resolve_promotion_class() and resolve_non_main_lineage() helpers to deploy-runtime.sh; thread both
  into the build_images() --build-arg array and print_compose_commands() echo path. Workspace builds now
  correctly stamp PROMOTION_CLASS=stability-candidate + NON_MAIN_LINEAGE=true, satisfying the Dockerfile.runtime
  guard added in OMN-13656 (#2116) without manual build-args.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "10 new unit tests in tests/scripts/test_deploy_runtime_promotion_class.py covering static
      text assertions and --print-compose-cmd subprocess dry-run (no live docker build); 201 scripts tests
      pass total."
    command: "uv run pytest tests/scripts/ -q"
  - kind: "ci"
    description: "pre-commit run --files scripts/deploy-runtime.sh tests/scripts/test_deploy_runtime_promotion_class.py
      -- all hooks pass including shellcheck, ruff, SPDX, aislop-sweep."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-scripts-tests-pass"
    description: "201 scripts tests pass including 10 new promotion-class assertions."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13669/dod-scripts-tests-pass/command.yaml\""
  - id: "dod-pre-commit-clean"
    description: "pre-commit run on changed files passes all hooks (shellcheck, ruff, SPDX, aislop-sweep,
      reject-skip-gate-bypass)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13669/dod-pre-commit-clean/command.yaml\""
  - id: "dod-promotion-class-args-present"
    description: "resolve_promotion_class(), resolve_non_main_lineage(), PROMOTION_CLASS and NON_MAIN_LINEAGE
      build-args present in deploy-runtime.sh."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13669/dod-promotion-class-args-present/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "onex_change_control follow-up PR carries OMN-13669 receipts bound to omnibase_infra
      PR #2125."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13669/dod-occ-pr-self/command.yaml\""
