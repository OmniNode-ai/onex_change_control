---
schema_version: "1.0.0"
ticket_id: OMN-7873
title: "fix(omnimarket): unified ingestion contract.yaml uses wrong format — must match omnimarket schema"
summary: "fix(omnimarket): unified ingestion contract.yaml uses wrong format — must match omnimarket schema"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR #579 open and CI green — contract format tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-001/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-001\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-001/command.yaml\"
          && grep -q '^ticket_id: \"OMN-7873\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-001/command.yaml\""
  - id: "dod-002"
    description: "Pre-commit hooks clean on branch jonah/omn-7873-fix-ingestion-contract-format"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-002/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-002\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-002/command.yaml\"
          && grep -q '^ticket_id: \"OMN-7873\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-002/command.yaml\""
  - id: "dod-003"
    description: "12 contract format tests pass (guards against regression to omniintelligence-style format)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-003/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-003\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-003/command.yaml\"
          && grep -q '^ticket_id: \"OMN-7873\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-003/command.yaml\""
  - id: "dod-deploy"
    description: "No runtime deploy required — new skeleton node. deploy evidence: HandlerContentIngestion
      importable from installed package; no container restart needed for a new entry-point registration."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-deploy/command.yaml\"
          && grep -q 'deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-deploy/command.yaml\"
          && grep -q '^ticket_id: \"OMN-7873\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-deploy/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR #854 merged to main with corrected receipt bindings for OMN-7873."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 854 --repo OmniNode-ai/onex_change_control --json state --jq '.state'
          | grep -Eq 'OPEN|MERGED'"
  - id: "dod-metrics-fix"
    description: "PR #581 fixes content ingestion false-success accounting and proves the regression tests
      pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-metrics-fix/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-metrics-fix\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-metrics-fix/command.yaml\"
          && grep -q '^pr_number: 581$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7873/dod-metrics-fix/command.yaml\""
  - id: "dod-occ-metrics-pr"
    description: "OCC PR #855 carries the metrics follow-up receipt for OMN-7873."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 855 --repo OmniNode-ai/onex_change_control --json state --jq '.state'
          | grep -Eq 'OPEN|MERGED'"
