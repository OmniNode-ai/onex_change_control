---
schema_version: "1.0.0"
ticket_id: OMN-7873
title: "fix(omnimarket): unified ingestion contract.yaml uses wrong format — must match "
summary: "fix(omnimarket): unified ingestion contract.yaml uses wrong format — must match "
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR #579 open and CI green — contract format tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr checks 579 --repo OmniNode-ai/omnimarket"
  - id: "dod-002"
    description: "Pre-commit hooks clean on branch jonah/omn-7873-fix-ingestion-contract-format"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-003"
    description: "12 contract format tests pass (guards against regression to omniintelligence-style format)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/nodes/node_content_ingestion_effect/ -v"
  - id: "dod-deploy"
    description: "No runtime deploy required — node_content_ingestion_effect is a new skeleton node. Handler
      and model stubs are importable; no container restart needed for a new node registration."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "deploy: uv run python -c 'from omnimarket.nodes.node_content_ingestion_effect.handlers.handler_content_ingestion
          import HandlerContentIngestion; print(HandlerContentIngestion)'"
