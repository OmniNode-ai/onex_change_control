---
schema_version: "1.0.0"
ticket_id: "OMN-13848"
title: "fix(OMN-13848): rehab fail-open enforcement gates (workflow + changeset guards)"
summary: >-
  omniclaude PR #1833 fixes two inert enforcement hooks in the fail-open-enforcement-gates cluster. pre_tool_use_workflow_guard.sh
  now binds PROJECT_ROOT before sourcing common.sh, so the canonical-clone write block reaches its Python
  guard and fails CLOSED instead of crashing on an unbound-variable and being swallowed to exit 0 by the
  error-guard trap; common.sh's ${PROJECT_ROOT} .env references are hardened to ${PROJECT_ROOT:-} to close
  the same fail-open landmine fleet-wide. pre_tool_use_changeset_guard.sh anchors its broad-staging regex
  with ([[:space:]]|$) to drop ~72% dotfile false positives and relocates its dead ~/.claude event sink
  to the observable friction registry ${ONEX_STATE_DIR}/friction/changeset_guard/. Hooks remain UNREGISTERED
  (hooks.json stays at the OMN-13244 empty baseline); re-enablement is a separate operator-gated step
  and is explicitly out of scope for this PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1833"
    description: >-
      Consuming-PR binding: omniclaude PR #1833 carries the OMN-13848 workflow + changeset guard fixes
      and the new shell-driven unit tests that prove them (canonical-clone Edit blocks with exit 2, worktree
      Edit allowed, common.sh sources cleanly under set -u without PROJECT_ROOT; dotfile stages no longer
      warn, broad staging warns and records friction under ONEX_STATE_DIR with no ~/.claude write). Real
      probe: gh pr view 1833 --repo OmniNode-ai/omniclaude --json number,state,headRefOid,headRefName,title,files.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13848/dod-omniclaude-pr-1833/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying this OMN-13848 contract and the dod-omniclaude-pr-1833
      receipt consumed by omniclaude PR #1833 through Receipt Gate and OCC eligibility.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13848/dod-occ-self/command.yaml"
