---
schema_version: "1.0.0"
ticket_id: "OMN-12830"
title: "D1 — full-loop E2E proof on stability-test (deploy-proven, loop-blocked)"
summary: >-
  D1 of the close-the-loop plan (Phase D). Built the stability-test lane on .201 in workspace mode from
  an integration branch = omnimarket origin/dev + C1 (OMN-12829 escalation cherry-picked); B3 (OMN-12828)
  was already merged to dev so it was not stacked on top. All 3 images built with --build-arg OMNI_HOME,
  migration 0013 vendored + applied. The build deployed and wired correctly (consumers Stable, escalation
  + node-deploy executor present, projection-api healthy). However the generate->register->invoke loop
  does NOT execute on this build: fresh generation commands are consumed (offsets advance) but the runtime
  service_kernel reports 'No dispatcher found for category command, message type omnimarket.node-generation-requested'
  and routes to DLQ — no generation_events row, no registration, no invocation. Per the Two-Strike Diagnosis
  Protocol the diagnosis was recorded instead of trial-and-error live mutations. The loop is honestly
  NOT closed today; fresh delegation row, escalation proof, and dashboard one-backend render are gated
  by this blocker and were not run.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-stability-build-deployed"
    description: >-
      The stability-test integration build (omnimarket integration SHA 95769ba2, compat 4d887307, occ
      4877d3c2, infra e2dbdc95) deployed in workspace mode: 3 images running with new digests, migration
      0013 proof columns present, generation + node-deploy + escalation consumers Stable with zero lag,
      projection-api healthy. Verified by an independent verifier via real docker / rpk / psql probes.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12830/dod-stability-build-deployed/command.yaml"
  - id: "dod-loop-blocked-diagnosed"
    description: >-
      The generate->register->invoke loop does NOT execute on this build; two fresh generation correlations
      were consumed but produced no generation_events row and no handler log, with the runtime service_kernel
      routing the command to DLQ ('No dispatcher found'). Recorded honestly as PENDING — the loop is not
      closed. Verified by an independent verifier via a real runtime-log + DB probe.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PENDING$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12830/dod-loop-blocked-diagnosed/command.yaml"
