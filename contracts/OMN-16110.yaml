---
schema_version: "1.0.0"
ticket_id: "OMN-16110"
title: "Autobind OCC evidence for OMN-16110"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2764.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2764 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2764"
    description: "PR #2764 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2764 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2764 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2764"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6575"
    description: "Binds OMN-16110 to OCC companion PR #6575 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6575 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-deploy-falsifiable-probe"
    description: >
      Falsifiable deploy probe (OMN-14505/OMN-8912) for the OMN-16110 fix: extends the contract-declared
      gateway.attach.keycloak.* secret-resolver mappings from the dev-only OMN-15918/#2845 fix to the
      stability-test and judge profiles, plus a new regression test that drives the real service_kernel
      boot-time dependency check. Reads the actual merged source content live from GitHub (never this
      ticket's own receipts) at the PR's own head commit and asserts the introduced test function is genuinely
      present.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/tests/ci/test_runtime_policy_contract.py?ref=e7b1db6701eea924351de8f2510a7128219815ee"
          --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF "def test_secret_resolver_mappings_satisfy_gateway_boot_check"
