---
schema_version: "1.0.0"
ticket_id: "OMN-13518"
title: "Pin OMN-13408 DoD evidence check cwd to omnimarket"
summary: >-
  The OCC-resident OMN-13408 contract declared a dod-001 check against an omnimarket-relative test path,
  but did not declare the owning repository cwd. node_dod_verify therefore inferred the onex_change_control
  repo root for the OCC-resident contract and false-failed with file-not-found when grepping tests/unit/delegation/test_cumulative_cost_accumulation_omn13408.py.
  The downstream OCC PR pins the dod-001 check cwd to ${OMNI_HOME}/omnimarket and verifies the real omnimarket
  test path instead of a self-referential grep. This is a contract evidence fix only; no runtime code,
  deployment, schema, endpoint, topic, or live lane behavior changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      The real node_dod_verify EvidenceCollector path was reproduced with OMNI_HOME set: before the cwd
      declaration, dod-001 failed because the omnimarket test path was evaluated from onex_change_control;
      after adding cwd: ${OMNI_HOME}/omnimarket, dod-001 verifies OK with match count 1 against the real
      omnimarket test file.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13518/dod-001/command.yaml"
  - id: "dod-002"
    description: >-
      The changed OMN-13408 contract validates with validate-yaml, the focused OCC dod/ticket-contract
      suite passes, and all OMN-13408 receipts touched by the OMN-13408 contract mutation are rebound
      to the normalized contract hash.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13518/dod-002/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      The paired OCC PR carries contracts/OMN-13518.yaml plus PASS receipts under drift/dod_receipts/OMN-13518/,
      and also updates the affected OMN-13408 receipt contract hashes after mutating contracts/OMN-13408.yaml.
      No deploy or live runtime mutation is included.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13518/dod-occ-pr/command.yaml"
