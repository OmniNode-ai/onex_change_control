---
schema_version: "1.0.0"
ticket_id: "OMN-15207"
title: "Slim omnidash CLAUDE.md — org-wide repo CLAUDE.md refresh"
summary: >
  OCC companion evidence for omnidash PR #272: slims CLAUDE.md from 8,856 to 4,784 bytes per context-engineering
  doctrine, corrects live-verified false claims (data-source default is http not file per OMN-14642; no
  local/no-api-literal ESLint rule exists at HEAD; fixtures/registry.json is not required; DashboardBuilder.tsx
  does not exist; fixtures/ is committed via gitignore carve-out), deletes the stale Dashboard lineage
  table (referenced directories no longer exist), and adds the dated deployed-instance-is-legacy-rest-express
  trap. Companion binds to the product PR head commit 0a210889af241178a516e73d819ee4a5d240ddd9 on branch
  jonah/omn-15207-slim-claude-md (deterministic commit-sha bind).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Product-PR head binding and content probes for omnidash#272."
    command: "gh pr view 272 --repo OmniNode-ai/omnidash --json number,state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-dash-pr-272-head-0a210889"
    description: >
      Consuming-PR binding: omnidash PR #272 head is exactly commit 0a210889af241178a516e73d819ee4a5d240ddd9,
      which carries the slimmed CLAUDE.md for OMN-15207 (deterministic head-commit bind, not a branch-name
      bind).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 272 --repo OmniNode-ai/omnidash --json headRefOid --jq .headRefOid |
          grep -qx 0a210889af241178a516e73d819ee4a5d240ddd9"
  - id: "dod-dash272-claude-slim-0a210889"
    description: >
      Falsifiable product probe at the bound commit: CLAUDE.md at 0a210889af241178a516e73d819ee4a5d240ddd9
      is exactly 4,784 bytes (slimmed from 8,856) and carries the deployed-instance-is-legacy trap section
      heading that heads the refreshed file.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          [ "$(gh api 'repos/OmniNode-ai/omnidash/contents/CLAUDE.md?ref=0a210889af241178a516e73d819ee4a5d240ddd9'
          --jq .size)" = "4784" ] && gh api 'repos/OmniNode-ai/omnidash/contents/CLAUDE.md?ref=0a210889af241178a516e73d819ee4a5d240ddd9'
          --jq .content | base64 -d | grep -q 'Deployed instance is NOT this app'
  - id: "dod-occ-self"
    description: >
      OCC companion self-bind for OMN-15207 (deterministic commit-sha bind, no PR-number prediction).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15207/dod-occ-self/command.yaml"
  - id: "occ-self-bind-pr-5054"
    description: "Binds OMN-15207 to OCC durability-rebind PR #5054 (self-bind for the merge-commit receipt
      supersession)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5054 --repo OmniNode-ai/onex_change_control --json number,state"
