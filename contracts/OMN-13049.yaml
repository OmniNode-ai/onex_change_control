---
schema_version: "1.0.0"
ticket_id: "OMN-13049"
title: "feat(OMN-13049): anchor-first ordering with persisted resume manifests"
summary: >-
  Implements D-3 retro action: persist a typed resume manifest at each worker phase boundary and update
  dispatch_worker Rule 6 so phase 0 anchors the work before long implementation legs. Survivor notes preserve
  defect identity when auth or usage-limit failures interrupt work.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Resume manifest and dispatch_worker prompt tests pass."
    command: "uv run pytest tests/hooks/test_resume_manifest.py tests/skills/test_dispatch_worker_operating_rules_injection.py
      -q"
  - kind: ci
    description: "Ruff and pre-commit pass on the changed product files."
    command: "uv run ruff check <touched-files>"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "23 focused tests pass for resume manifest model/writer behavior and dispatch_worker
      rule injection."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13049/dod-001/command.yaml"
  - id: dod-002
    description: "Product PR #1817 is open on dev at the expected OMN-13049 head."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13049/dod-002/command.yaml"
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13049 contract and receipts
      consumed by omniclaude PR #1817.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13049/dod-occ-self/command.yaml"
