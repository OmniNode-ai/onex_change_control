---
schema_version: "1.0.0"
ticket_id: "OMN-17185"
title: "FILE_PATH plan references resolve against the PR head tree"
summary: >-
  The plan-vs-live checker resolved every FILE_PATH reference against the base branch, so a PR landing
  two mutually-referencing plan docs failed by construction ("path missing on target branch"). For the
  repo the changeset edits, references now resolve against the PR head tree (--head-ref, default HEAD);
  sibling repos still resolve against --base-ref.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Same-PR mutually-referencing plan docs resolve as pass."
    command: "uv run pytest tests/unit/scripts/test_check_plan_vs_live.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-plan-vs-live-head-ref-tests"
    description: >-
      RED-first proof for OMN-17185 AC1: two mutually-referencing plan docs added on the head branch resolve
      as pass, the same references still fail when head-ref resolution is disabled (negative control),
      sibling-repo paths keep resolving against base-ref, and the CLI default is HEAD.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          uv run pytest tests/unit/scripts/test_check_plan_vs_live.py -q
  - id: "dod-plan-vs-live-head-ref-behaviour"
    description: >-
      Behavioural check: the checker exits 0 for a plan doc citing a path that exists only on the head
      tree, and the flag is surfaced on the CLI.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          uv run python src/onex_change_control/scripts/check_plan_vs_live.py --help | grep -c -- '--head-ref'
