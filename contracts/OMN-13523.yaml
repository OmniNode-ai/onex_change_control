---
schema_version: "1.0.0"
ticket_id: "OMN-13523"
title: "Bind June 23 omnibase_infra Dependabot dependency PR batch to OCC evidence"
summary: >
  Bind the June 23 omnibase_infra Dependabot dependency maintenance replacement PR to central OCC evidence
  so Receipt Gate can evaluate the OMN-named batch PR from a pinned Evidence-Source without bypass tokens.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-2077-head"
    description: "omnibase_infra PR #2077 carries the OMN-13523 Dependabot batch replacement head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13523/dod-infra-2077-head/command.yaml\""
  - id: "dod-infra-2077-original-heads-ancestors"
    description: "omnibase_infra PR #2077 preserves the original Dependabot heads from PRs #2074, #2075,
      and #2076 in branch history."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13523/dod-infra-2077-original-heads-ancestors/command.yaml\""
  - id: "dod-infra-2077-local-gates"
    description: "omnibase_infra PR #2077 passed focused local gates for runner identity, release identity,
      dispatcher coverage, sibling pins, and workflow assertions."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13523/dod-infra-2077-local-gates/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carries the OMN-13523 contract and receipts for the omnibase_infra
      dependency batch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13523/dod-occ-pr-self/command.yaml\""
