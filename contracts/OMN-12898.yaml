---
schema_version: "1.0.0"
ticket_id: "OMN-12898"
title: "Fix stability dashboard DB contract drift and topic materialization"
summary: >-
  Remove static node/topic allowlist gates so marketplace contract-declared migrations and Kafka topics
  are materialized by the infra runtime path.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1929"
    description: >-
      omnibase_infra PR #1929 carries the OMN-12898 implementation that removes the node migration allowlist
      and routes startup topic validation through contract extraction.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12898/dod-infra-pr-1929/command.yaml"
  - id: "dod-focused-tests"
    description: >-
      Focused migration, topic validator, provisioner, runtime wiring, lint, and syntax checks pass for
      the OMN-12898 implementation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12898/dod-focused-tests/command.yaml"
  - id: "dod-marketplace-migration-drift-check"
    description: >-
      The infra vendored node migration tree is in sync with the canonical omnimarket marketplace node
      migrations, with no `.synced-nodes` allowlist.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12898/dod-marketplace-migration-drift-check/command.yaml"
  - id: "dod-contract-topic-validator"
    description: >-
      Topic startup validation accepts a contract-only topic without requiring that topic to be present
      in the static `ALL_PROVISIONED_SUFFIXES` registry.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12898/dod-contract-topic-validator/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      OMN-12898 changes runtime startup/topic validation and vendored migrations. No live deployment or
      restart was performed during PR validation; post-merge deployment remains required before hosted
      runtime claims can be made.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12898/dod-deploy-assessment/command.yaml"
          && grep -q 'No live deployment or restart was performed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12898/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding receipt for the OCC PR that carries the OMN-12898 central contract and DoD receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12898/dod-occ-pr-self/command.yaml"
