---
schema_version: "1.0.0"
ticket_id: OMN-7904
title: 'test(coverage): add tests for consumer health consumer (omnibase_infra)'
summary: "test(coverage): add tests for consumer health consumer (omnibase_infra)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}"
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json mergedAt -q .mergedAt --repo ${REPO}"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --watch --repo ${REPO}"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-005"
    description: "Test coverage meets minimum threshold"
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest --cov --cov-fail-under=80"
  - id: "dod-infra-2327-head"
    description: "Product PR #2327 exists on dev at exact head 91844c49."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=2327; PRODUCT_REPO=OmniNode-ai/omnibase_infra; gh pr view \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json number,headRefOid,headRefName,baseRefName,title"
  - id: "dod-infra-2327-test-only"
    description: "PR #2327 changes only dedicated consumer health projection consumer unit tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=2327; PRODUCT_REPO=OmniNode-ai/omnibase_infra; gh pr view \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json files"
  - id: "dod-infra-2327-local-proof"
    description: "PR #2327 body records 24 passing tests, 87.18 percent target-module coverage, and local
      quality gates."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=2327; PRODUCT_REPO=OmniNode-ai/omnibase_infra; gh pr view \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json body --jq '.body' | grep -E '24 passed|87.18%|ruff|pre-commit'"
  - id: "dod-infra-2327-no-deploy"
    description: "PR #2327 is test-only and does not deploy, push images, restart runtime, mutate live
      infrastructure, mutate branch protection, or cut over required contexts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=2327; PRODUCT_REPO=OmniNode-ai/omnibase_infra; diff_paths=\"$(gh
          pr diff \"$PRODUCT_PR_NUMBER\" --repo \"$PRODUCT_REPO\" --name-only)\"; printf '%s\\n' \"$diff_paths\"
          | grep -qx 'tests/unit/services/observability/consumer_health/__init__.py'; printf '%s\\n' \"$diff_paths\"
          | grep -qx 'tests/unit/services/observability/consumer_health/test_consumer_health_projection_consumer.py';
          test \"$(printf '%s\\n' \"$diff_paths\" | wc -l)\" -eq 2"
  - id: "dod-supersede-002-product-open-2327"
    description: "Append-only supersession for the generated merged-to-main check; product PR #2327 is
      open on dev at the exact tested head."
    evidence_artifact: "supersedes_dod_evidence:dod-002"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=2327; PRODUCT_REPO=OmniNode-ai/omnibase_infra; gh pr view \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json state,baseRefName,headRefOid --jq '.state == \"OPEN\" and .baseRefName
          == \"dev\" and .headRefOid == \"91844c498c081b29b306c500dd779acd1f90e762\"' | grep -q true"
  - id: "dod-supersede-003-occ-cascade-2327"
    description: "Append-only supersession for the generated CI watch check; product PR #2327 has only
      OCC-preflight cascade failures before this companion."
    evidence_artifact: "supersedes_dod_evidence:dod-003"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=2327; PRODUCT_REPO=OmniNode-ai/omnibase_infra; gh pr view \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json statusCheckRollup --jq '[.statusCheckRollup[] | select((.__typename
          == \"CheckRun\" and .status == \"COMPLETED\" and (.conclusion | IN(\"SUCCESS\",\"SKIPPED\",\"NEUTRAL\")
          | not)) or (.__typename == \"StatusContext\" and (.state | IN(\"SUCCESS\",\"PENDING\") | not)))
          | select(((.name // .context) | test(\"occ-preflight|CI Summary\")) | not)] | length == 0' |
          grep -q true"
  - id: "dod-supersede-004-product-precommit-2327"
    description: "Append-only supersession for the generated all-files pre-commit check; PR #2327 body
      records pre-commit and ruff proof for the test-only change."
    evidence_artifact: "supersedes_dod_evidence:dod-004"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=2327; PRODUCT_REPO=OmniNode-ai/omnibase_infra; gh pr view \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json body --jq '.body' | grep -E 'pre-commit|ruff'"
  - id: "dod-supersede-005-product-coverage-2327"
    description: "Append-only supersession for the generated coverage command; PR #2327 body records the
      targeted ConsumerHealthProjectionConsumer coverage proof."
    evidence_artifact: "supersedes_dod_evidence:dod-005"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=2327; PRODUCT_REPO=OmniNode-ai/omnibase_infra; gh pr view \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json body --jq '.body' | grep -E 'ConsumerHealthProjectionConsumer|24
          passed|87.18%'"
  - id: "occ-self-bind-pr-4327"
    description: "OCC PR #4327 exists and carries OMN-7904 evidence for omnibase_infra PR #2327 head 91844c49."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4327; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state,headRefName"
