---
schema_version: "1.0.0"
ticket_id: "OMN-12313"
title: "test(OMN-12313): rename linear_* skills to ticketing_* (parent OMN-8771/OMN-8776)"
summary: >-
  Wave G skill->ticketing rename. The three skills named in OMN-12313 (linear_epic_org, linear_triage,
  linear_insights) were already renamed to ticketing_epic_org, ticketing_triage, and ticketing_insights
  under the parent epic OMN-8771/OMN-8776 (omniclaude PRs #1399 and #1408) and are merged on origin/dev:
  the old skill directories are absent, the ticketing_* directories are present and registered, SKILL.md
  frontmatter/prompt content reference the new names, and the Skill MCP Reference Lint gate (OMN-8776)
  passes clean. The residual linear_* strings in the tree are NOT skill identifiers: node_linear_triage
  is a cross-repo backing node in omnimarket, LINEAR_INSIGHTS_* are env var names, docs/plans/2026-04-09-skill-rename-mapping.md
  is the historical OLD->NEW mapping record, and docs/feature-dashboard/feature-dashboard.stable.json
  is a machine-generated baseline snapshot. The one in-scope alignment edit lands in omniclaude PR #1787:
  tests/scripts/test_lint_skill_mcp_refs.py used a stale linear_triage fixture skill-dir name; it is updated
  to ticketing_triage so the lint-test fixture matches the renamed skill. No runtime node, contract topic,
  or image build input changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1787"
    description: >-
      Per-code-PR binding receipt for omniclaude PR #1787 (jonahgabriel/omn-12313-omn-8771-t2-rename-3-linear_-skills-to-ticketing_-naming,
      base=dev). Confirms the linear_*->ticketing_* skill rename is complete on origin/dev (old skill
      dirs absent, ticketing_* dirs present, Skill MCP Reference Lint exit 0) and lands the lint-test
      fixture alignment (linear_triage -> ticketing_triage). Binds OMN-12313 to omniclaude PR #1787 by
      pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12313/dod-omniclaude-pr-1787/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-12313 contract and the omniclaude
      PR #1787 binding. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check
      can resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12313/dod-occ-pr-self/command.yaml
