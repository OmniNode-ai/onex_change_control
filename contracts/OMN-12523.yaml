---
schema_version: "1.0.0"
ticket_id: "OMN-12523"
title: "Configure Kafka client api_version for EventBusKafka"
summary: >
  Stabilize EventBusKafka bootstrap against the stability Redpanda runtime by adding a typed
  KAFKA_API_VERSION override and applying it only when the installed aiokafka client constructor
  supports the api_version keyword. This keeps default behavior unchanged while allowing the
  stability runtime to avoid aiokafka metadata-version auto-detection timeouts.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1805-local-proof"
    description: >-
      omnibase_infra PR #1805 adds the typed Kafka api_version configuration and conditional aiokafka
      constructor wiring. Focused unit tests, ruff validation, and live EventBusKafka bootstrap proof
      passed against PR head 1607fca7fd5bf6f18e8b7348006046cd861cd231.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12523/dod-infra-pr-1805-local-proof/command.yaml
          && grep -q '^pr_number: 1805$' drift/dod_receipts/OMN-12523/dod-infra-pr-1805-local-proof/command.yaml"
