---
schema_version: "1.0.0"
ticket_id: "OMN-13859"
title: "feat(OMN-13859): event-driven worktree prune-on-PR-close"
summary: >-
  omnimarket PR #1586 replaces polling worktree cleanup with an event-driven pr_lifecycle_orchestrator
  prune effect, with tests covering the safety rails and the declared output envelope for state-coverage.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-market-pr-1586"
    description: >-
      Consuming-PR binding: omnimarket PR #1586 carries the OMN-13859 worktree prune implementation at
      head 2e8b8d1940e2a7bb3f273f8777bf96d7a0f4b0ce.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13859/dod-market-pr-1586/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment for omnimarket PR #1586: the runtime path changes add an event-driven worktree
      prune effect behind the existing PR lifecycle orchestrator. No live deploy is performed by this
      PR; post-merge validation is covered by the focused handler and state-coverage gates plus deploy-gate
      review of the changed runtime paths.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13859/dod-deploy-assessment/command.yaml
          && grep -qi deploy drift/dod_receipts/OMN-13859/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13859 contract and receipt consumed
      by omnimarket PR #1586.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13859/dod-occ-self/command.yaml"
