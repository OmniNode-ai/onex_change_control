---
schema_version: "1.0.0"
ticket_id: "OMN-13757"
title: "fix(OMN-13757): enumerate orphaned and stale tickets in linear triage"
summary: >-
  ModelLinearTriageResult reported orphaned and stale_count as bare integer counts with no underlying
  ticket list. The omnimarket fix adds orphaned_tickets and stale_tickets list fields populated with the
  full ticket lists, with len(orphaned_tickets) == orphaned and len(stale_tickets) == stale_count invariants
  enforced before return.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "27 node_linear_triage unit tests pass including 5 new TDD tests for OMN-13757 list enumeration."
    command: >-
      grep -q '^status: PASS$' drift/dod_receipts/OMN-13757/dod-unit-tests/command.yaml
  - kind: "ci"
    description: "Implementation commit present in omnimarket branch."
    command: >-
      grep -q '^status: PASS$' drift/dod_receipts/OMN-13757/dod-implementation/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: >-
      27 unit tests pass in tests/nodes/node_linear_triage/ including 5 new TDD tests in TestOmn13757OrphanedStaleLists.
      The tests verify orphaned_tickets and stale_tickets fields exist, are populated with all matching
      tickets without truncation, and satisfy len==count invariants. Probe: cd omni_worktrees/OMN-13756/omnimarket
      && uv run pytest tests/nodes/node_linear_triage/ -q Result: 27 passed in 0.22s. Evidence-Source
      SHA: f4ae096f4c44299bb5525de62565c2ab747ca19c
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13757/dod-unit-tests/command.yaml"
  - id: "dod-implementation"
    description: >-
      Implementation in omnimarket branch jonahgabriel/omn-13756-fixomnimarket-node_linear_triage-pr-check-skips-backlog
      commit f4ae096f4c44299bb5525de62565c2ab747ca19c. Changes for OMN-13757: orphaned_tickets_list is
      built from all tickets where not parent_id and state is not done; stale_tickets is populated from
      the stale ticket scan; ModelLinearTriageResult exposes both lists for ticketing_triage and ticketing-epic-org
      consumers.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13757/dod-implementation/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13757 contract and receipts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf 'PASS\n'"
