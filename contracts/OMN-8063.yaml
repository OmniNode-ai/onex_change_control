---
schema_version: "1.0.0"
ticket_id: OMN-8063
title: "Task 5: InvariantViolation exception in omnibase_infra"
summary: "Add the InvariantViolation runtime contract error to omnibase_infra, export it from the errors package, and cover action/domain context with focused unit tests."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused InvariantViolation unit and integration tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8063/dod-tests/command.yaml"
  - kind: "manual"
    description: "Touched InvariantViolation files pass ruff"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8063/dod-lint/command.yaml"
  - kind: "manual"
    description: "Central OCC ticket contract validates"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8063/dod-occ-contract/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Focused InvariantViolation unit and integration tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8063/dod-tests/command.yaml && grep -q '^ticket_id: OMN-8063$' drift/dod_receipts/OMN-8063/dod-tests/command.yaml && grep -q '^evidence_item_id: dod-tests$' drift/dod_receipts/OMN-8063/dod-tests/command.yaml"
  - id: "dod-lint"
    description: "Touched InvariantViolation files pass ruff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8063/dod-lint/command.yaml && grep -q '^ticket_id: OMN-8063$' drift/dod_receipts/OMN-8063/dod-lint/command.yaml && grep -q '^evidence_item_id: dod-lint$' drift/dod_receipts/OMN-8063/dod-lint/command.yaml"
  - id: "dod-occ-contract"
    description: "Central OCC ticket contract validates"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8063/dod-occ-contract/command.yaml && grep -q '^ticket_id: OMN-8063$' drift/dod_receipts/OMN-8063/dod-occ-contract/command.yaml && grep -q '^evidence_item_id: dod-occ-contract$' drift/dod_receipts/OMN-8063/dod-occ-contract/command.yaml"
