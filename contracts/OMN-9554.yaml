---
schema_version: "1.0.0"
ticket_id: OMN-9554
title: "Wire runtime health boot grace configuration through service kernel"
summary: "Expose RUNTIME_HEALTH_BOOT_GRACE_SECONDS as the runtime health monitor boot-grace selector in
  ServiceKernel."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Focused health-monitor tests and PR diff prove the environment selector is wired into
      ServiceRuntimeHealthMonitor."
    command: "uv run pytest tests/unit/services/test_service_runtime_health_monitor.py -q"
  - kind: ci
    description: "omnibase_infra PR #1393 checks pass after OCC evidence is merged."
    command: "gh pr checks 1393 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "PR #1393 diff reads RUNTIME_HEALTH_BOOT_GRACE_SECONDS and passes boot_grace_seconds
      into ServiceRuntimeHealthMonitor."
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr diff ${PR_NUMBER} --repo ${REPO} | grep -q 'RUNTIME_HEALTH_BOOT_GRACE_SECONDS'"
  - id: dod-002
    description: "Focused health-monitor tests pass locally on PR #1393 head."
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/services/test_service_runtime_health_monitor.py -q"
  - id: dod-deploy
    description: "deploy gate evidence: PR #1393 records the post-merge runtime validation command; pre-merge
      live RestartCount proof is intentionally not claimed."
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json body --jq '.body | if contains(\"Post-merge
          runtime validation\") and contains(\"deploy: docker exec omninode-runtime\") then \"deploy validation
          recorded\" else error(\"missing deploy validation\") end'"
