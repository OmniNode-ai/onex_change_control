---
schema_version: "1.0.0"
ticket_id: OMN-9581
title: "Real CheckTarget classes for node_process_watchdog Phase 2"
summary: "Real CheckTarget classes for node_process_watchdog Phase 2"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #439 checks pass (lint, typecheck, test, deploy-gate)"
    command: "gh pr checks 439 --repo OmniNode-ai/omnimarket --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "All 46 unit tests for the four CheckTarget classes pass (mocked, no network)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_process_watchdog/ -q"
  - id: "dod-002"
    description: "Node metadata dependency scope check passes with docker declared"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/ci/check_node_metadata_dependencies.py"
  - id: "dod-003"
    description: "Post-merge runtime smoke: target classes importable in deployed runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_process_watchdog.targets.http_check_target
          import TargetHttp; from omnimarket.nodes.node_process_watchdog.targets.docker_api_check_target
          import TargetDockerApi; from omnimarket.nodes.node_process_watchdog.targets.rpk_check_target
          import TargetRpk; from omnimarket.nodes.node_process_watchdog.targets.socket_connect_check_target
          import TargetSocketConnect; print('deploy smoke ok')\""
