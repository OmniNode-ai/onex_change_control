---
schema_version: "1.0.0"
ticket_id: OMN-10402
title: "Add demo script for screen recording"
summary: "Add a direct omnimarket CLI demo script that renders the ticket_pipeline market skill in text,
  JSON, YAML, and markdown dry-run formats for screen recording."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Demo script test passes"
    command: "uv run pytest tests/test_demo_script.py -q"
  - kind: "manual"
    description: "Demo script renders all four market CLI output formats"
    command: "bash scripts/demo_market_cli_outputs.sh --dry-run-only"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Demo script test passes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_demo_script.py -q"
  - id: "dod-002"
    description: "Demo script renders text, JSON, YAML, and markdown using direct omnimarket package CLI
      invocation"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "bash scripts/demo_market_cli_outputs.sh --dry-run-only"
  - id: "dod-003"
    description: "No deploy required; this is a local dry-run demo script for screen recording and does
      not mutate runtime state"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for OMN-10402 - local dry-run screen-recording script only'"
