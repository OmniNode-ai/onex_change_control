---
schema_version: "1.0.0"
ticket_id: "OMN-18326"
title: "Autobind OCC evidence for OMN-18326"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2523."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2523 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2523 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: |-
      uv run pytest tests/test_omn15583_quality_gate_result_timestamp.py tests/test_omn16249_projection_injected_key_seam.py -q
  - kind: "ci"
    description: "PR #1438 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1438 on OmniNode-ai/omninode_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omninode_infra`.
    command: |-
      uv run pytest tests/k8s/test_onex_lab_overlay_omn17533.py tests/k8s/test_onex_lab_topology_logins_omn18326.py -q
  - kind: "ci"
    description: "PR #3496 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3496 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: "uv run pytest tests/unit/runtime/auto_wiring/test_handler_wiring_db_injection.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2523"
    description: "PR #2523 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/tests/test_omn15583_quality_gate_result_timestamp.py?ref=7367fad851fd043ec40994d6caa5fa290b45a2ca --jq '.content' | base64 -d | grep -c 'def test_kernel_seam_envelope_timestamp_is_read'
  - id: "dod-OmniNode-ai-omnimarket-pr-2523-ci"
    description: "PR #2523 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2523 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2523 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/test_omn15583_quality_gate_result_timestamp.py tests/test_omn16249_projection_injected_key_seam.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-9391"
    description: "OCC companion PR #9391 — self-bind for OMN-18326 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9391 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1438"
    description: "PR #1438 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1438 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1438/files --paginate --jq '.[].filename' | grep -cFx '.secrets.baseline'
  - id: "occ-self-bind-pr-9393"
    description: "Binds OMN-18326 to OCC companion PR #9393 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9393 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3496"
    description: "PR #3496 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=685e9ae41e92496bdada804f4a35065de64bbecb --jq '.content' | base64 -d | grep -c 'def _extract_projection_envelope_timestamp'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3496/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_infra/runtime/auto_wiring/handler_wiring.py'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #3496 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3496/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "occ-self-bind-pr-9395"
    description: "Binds OMN-18326 to OCC companion PR #9395 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9395 --repo OmniNode-ai/onex_change_control --json number,state"
