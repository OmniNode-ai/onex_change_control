---
schema_version: 1.0.0
ticket_id: OMN-10699
title: "ADR Canary Proof of Life — End-to-End Verification"
summary: Proof-of-life integration tests for the ADR canary orchestrator pipeline using deterministic
  stub protocol adapters
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'omnimarket PR #600 CI checks pass'
    command: gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket --watch
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Proof-of-life test suite passes (3 tests) — verified via omnimarket PR  #600 CI
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket 2>&1 | grep 'Tests' | grep
          -q pass"
  - id: dod-002
    description: Full pipeline test (entries_total == 3) — verified via omnimarket PR  #600 CI
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket 2>&1 | grep 'Tests' | grep
          -q pass"
  - id: dod-003
    description: Pre-commit hooks pass on added test files
    source: generated
    checks:
      - check_type: command
        check_value: uv run pre-commit run --files tests/integration/nodes/node_adr_canary_orchestrator/test_proof_of_life.py
  - id: dod-004
    description: omnimarket PR `#600` CI green
    source: generated
    checks:
      - check_type: command
        check_value: gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket
  - id: dod-occ-pr
    description: "Self-binding receipt for this OCC PR #927."
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json state,baseRefName
          --jq '.state + \":\" + .baseRefName' | grep -q '^OPEN:main$'"
