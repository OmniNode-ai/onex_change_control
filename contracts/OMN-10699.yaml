---
schema_version: 1.0.0
ticket_id: OMN-10699
title: "ADR Canary Proof of Life — End-to-End Verification"
summary: Proof-of-life integration tests for the ADR canary orchestrator pipeline using deterministic
  stub protocol adapters
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'omnimarket PR #600 CI checks pass'
    command: gh pr checks 600 --repo OmniNode-ai/omnimarket --watch
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Proof-of-life test suite passes (3 tests)
    source: generated
    checks:
      - check_type: test_passes
        check_value: uv run pytest tests/integration/nodes/node_adr_canary_orchestrator/test_proof_of_life.py
          -v
  - id: dod-002
    description: Full pipeline test asserts entries_total == 3, evidence JSON written, scorecard written
    source: generated
    checks:
      - check_type: test_passes
        check_value: uv run pytest tests/integration/nodes/node_adr_canary_orchestrator/test_proof_of_life.py::test_proof_of_life_full_pipeline
          -v
  - id: dod-003
    description: Pre-commit hooks pass on added test files
    source: generated
    checks:
      - check_type: command
        check_value: uv run pre-commit run --files tests/integration/nodes/node_adr_canary_orchestrator/test_proof_of_life.py
  - id: dod-004
    description: omnimarket PR  #600 CI green
    source: generated
    checks:
      - check_type: command
        check_value: gh pr checks 600 --repo OmniNode-ai/omnimarket
