---
schema_version: "1.0.0"
ticket_id: "OMN-15395"
title: "Autobind OCC evidence for OMN-15395"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2543.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2543 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2543"
    description: "PR #2543 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-deploy-assessment"
    description: >
      Falsifiable deploy probe (OMN-14443/OMN-14505). The autobind producer emitted a
      vacuous `gh pr diff | grep` check that reads back this PR's own file list and can
      never go RED; replaced with a real remote content read pinned to dev. RED until the
      fix lands on dev, GREEN after — its exit status depends on the deployed source, not
      on text authored in this companion.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/topics/model_topic_provisioning_policy.py?ref=dev --jq '.content' | base64 -d | grep -q 'MANAGED_MINIMUM_REPLICATION_FACTOR'"
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/event_bus/service_topic_manager.py?ref=dev --jq '.content' | base64 -d | grep -q '_resolve_specs_for_creation'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2543"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5488"
    description: "Binds OMN-15395 to OCC companion PR #5488 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5488 --repo OmniNode-ai/onex_change_control --json number,state"
