---
schema_version: "1.0.0"
ticket_id: "OMN-15065"
title: "Autobind OCC evidence for OMN-15065"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1936."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1936 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniclaude-pr-1936"
    description: "PR #1936 on OmniNode-ai/omniclaude — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omniclaude-pr-1936-ci"
    description: "PR #1936 on OmniNode-ai/omniclaude — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4764"
    description: "OCC companion PR #4764 — self-bind for OMN-15065 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omniclaude-pr-1938"
    description: "Promotion PR #1938 on OmniNode-ai/omniclaude — OMN-15065 dev-to-main release binding."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName,headRefOid"
  - id: "occ-self-bind-pr-4777"
    description: "OCC companion PR #4777 — self-bind for OMN-15065 promotion receipt."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omniclaude-pr-1941"
    description: "Promotion PR #1941 on OmniNode-ai/omniclaude — OMN-15065 dev-to-main release binding."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName,headRefOid"
  - id: "occ-self-bind-pr-4778"
    description: "OCC companion PR #4778 — self-bind for OMN-15065 promotion receipt."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName"
  - id: "deploy-omniclaude-1941-runtime"
    description: "Promotion PR #1941 on OmniNode-ai/omniclaude — falsifiable readback that runtime_profiles
      is present on the promoted delegation orchestrator contract."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omniclaude/contents/src/omniclaude/nodes/node_delegation_orchestrator/contract.yaml?ref=0c61b2cc1a65ab71399ddb442e035f5e0f758676'
          --jq .content | base64 -d | grep -q 'runtime_profiles: \\[main\\]'"
  - id: "occ-self-bind-pr-4779"
    description: "OCC companion PR #4779 — self-bind for OMN-15065 deploy proof."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName"
  - id: "dod-omniclaude-1941-final"
    description: "Promotion PR #1941 on OmniNode-ai/omniclaude — final OMN-15065 head binding after canonical
      handler repair."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName,headRefOid"
  - id: "deploy-omniclaude-1941-final"
    description: "Promotion PR #1941 on OmniNode-ai/omniclaude — falsifiable readback for final runtime-profile
      and canonical handler repair."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omniclaude/contents/src/omniclaude/nodes/node_delegation_orchestrator/handlers/handler_delegation_dispatch.py?ref=c9e057ca49dacdca008fc796f3e1f29c7eff5691'
          --jq .content | base64 -d | grep -q 'class HandlerDelegationDispatch' && gh api 'repos/OmniNode-ai/omniclaude/contents/src/omniclaude/nodes/node_delegation_orchestrator/contract.yaml?ref=c9e057ca49dacdca008fc796f3e1f29c7eff5691'
          --jq .content | base64 -d | grep -q 'runtime_profiles: \\[main\\]'"
  - id: "occ-self-bind-pr-4781"
    description: "OCC companion PR #4781 — self-bind for OMN-15065 final promotion evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName"
