---
schema_version: "1.0.0"
ticket_id: "OMN-10580"
title: "Add CI grep gate for leaked literals with allowlist governance"
summary: >
  Harden check_leaked_literals.sh from advisory to blocking mode. Add pre-commit hook (leaked-literals-gate)
  and required GHA status check (Leaked Literals Gate). Add inline annotation escape hatches (onex-allow-*)
  and file-level exemptions for test fixtures. Annotate all existing src/ env-var fallbacks. OMN-10580.
  Wave 1 / Task 8 of the Public-Shippable plan.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-gate-clean"
    description: "Blocking scan exits 0 with findings=0 across all files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          if [ "$REPO" = "OmniNode-ai/onex_change_control" ]; then grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10580/dod-gate-clean/command.yaml";
          else bash scripts/validation/check_leaked_literals.sh blocking all; fi
  - id: "dod-unit-tests"
    description: "Leak gate unit tests pass (12 tests in tests/unit/scripts/test_leak_gate.py)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          if [ "$REPO" = "OmniNode-ai/onex_change_control" ]; then grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10580/dod-unit-tests/command.yaml";
          else uv run pytest tests/unit/scripts/test_leak_gate.py -v; fi
  - id: "dod-deploy"
    description: >
      Source changes are annotation-only (onex-allow-* comments added to existing env-var fallback lines);
      no runtime logic was altered. No deploy or runtime restart required.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10580/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10580 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10580/dod-occ-pr/command.yaml"
  - id: "dod-concession-fix"
    description: "CR concession detection in check-unresolved-threads.sh updated (omniclaude PR #1534)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10580/dod-concession-fix/command.yaml"
