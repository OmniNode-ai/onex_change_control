---
schema_version: "1.0.0"
ticket_id: OMN-10450
title: "Stability overlay uses same image as production and allows restart"
summary: "Remove workspace-specific stability runtime image/build overrides, inherit release build args
  from the base compose file, and set runtime services to restart unless stopped."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
evidence_requirements:
  - kind: "manual"
    description: "Stability compose render tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10450/dod-stability-render-tests/command.yaml"
  - kind: "manual"
    description: "Stability overlay compose config renders successfully"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10450/dod-overlay-compose-config/command.yaml"
  - kind: "manual"
    description: "Touched stability compose render test passes ruff"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10450/dod-lint/command.yaml"
  - kind: "manual"
    description: "Repo-local ticket contract validates"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10450/dod-contract-validate/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-stability-render-tests"
    description: "Stability compose render tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10450/dod-stability-render-tests/command.yaml
          && grep -q '^ticket_id: OMN-10450$' drift/dod_receipts/OMN-10450/dod-stability-render-tests/command.yaml
          && grep -q '^evidence_item_id: dod-stability-render-tests$' drift/dod_receipts/OMN-10450/dod-stability-render-tests/command.yaml"
  - id: "dod-overlay-compose-config"
    description: "Stability overlay compose config renders successfully"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10450/dod-overlay-compose-config/command.yaml
          && grep -q '^ticket_id: OMN-10450$' drift/dod_receipts/OMN-10450/dod-overlay-compose-config/command.yaml
          && grep -q '^evidence_item_id: dod-overlay-compose-config$' drift/dod_receipts/OMN-10450/dod-overlay-compose-config/command.yaml"
  - id: "dod-lint"
    description: "Touched stability compose render test passes ruff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10450/dod-lint/command.yaml && grep
          -q '^ticket_id: OMN-10450$' drift/dod_receipts/OMN-10450/dod-lint/command.yaml && grep -q '^evidence_item_id:
          dod-lint$' drift/dod_receipts/OMN-10450/dod-lint/command.yaml"
  - id: "dod-contract-validate"
    description: "Repo-local ticket contract validates"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10450/dod-contract-validate/command.yaml
          && grep -q '^ticket_id: OMN-10450$' drift/dod_receipts/OMN-10450/dod-contract-validate/command.yaml
          && grep -q '^evidence_item_id: dod-contract-validate$' drift/dod_receipts/OMN-10450/dod-contract-validate/command.yaml"
