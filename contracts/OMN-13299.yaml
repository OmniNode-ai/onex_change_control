---
schema_version: "1.0.0"
ticket_id: "OMN-13299"
title: "Validator-requirements fleet gate and deterministic skill routing cleanup"
summary: >-
  Adds validator-requirements baseline/gate coverage for onex_change_control and binds the omniclaude
  deterministic skill routing cleanup that unblocks omnibase_core's OMN-8765 sibling-skill gate. The omniclaude
  PR documents receipt-mode topic dispatch and SkillRoutingError handling for dispatch-only ONEX skills
  without reintroducing bare node dispatch commands.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1778"
    description: >-
      Per-code-PR binding receipt for omniclaude PR #1778 (jonah/omn-13299-validate-validator-requirements-fleet,
      base=dev). The PR satisfies the deterministic skill routing gate for eight dispatch-only ONEX skills
      and adds the omniclaude validator-requirements baseline for OMN-13299. This receipt binds OMN-13299
      to omniclaude PR #1778 by pr_number and commit_sha so downstream Receipt Gate can resolve central
      OCC evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13299/dod-omniclaude-pr-1778/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13299 contract, onex_change_control
      validator-requirements gate changes, and the omniclaude PR #1778 binding. Binds the OCC PR itself
      by pr_number so the OCC receipt-gate eligibility check can resolve a PASS receipt for the evidence
      PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13299/dod-occ-pr-self/command.yaml
