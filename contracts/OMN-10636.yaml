---
schema_version: '1.0.0'
ticket_id: OMN-10636
title: Wire DelegationRunner to Bifrost gateway
summary: In-process adapter routing delegation inference calls through HandlerBifrostGateway instead of
  the legacy direct-HTTP select_backend() approach
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Unit tests for DelegationRunner covering success path, failure paths, audit event emission,
      env config building, and response text extraction
    command: uv run pytest tests/delegation/test_runner.py -v -m unit
  - kind: ci
    description: PR CI green on omniclaude PR  #1530
    command: gh pr checks 1530 --repo OmniNode-ai/omniclaude
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: DelegationRunner, ModelBifrostRunnerResult, ModelDelegationAuditEvent created in src/omniclaude/delegation/runner.py
    source: generated
    checks:
      - check_type: file_exists
        check_value: src/omniclaude/delegation/runner.py
  - id: dod-002
    description: 24 unit tests passing in tests/delegation/test_runner.py
    source: generated
    checks:
      - check_type: command
        check_value: uv run pytest tests/delegation/test_runner.py -v -m unit -q --tb=no | grep -E '24
          passed|passed'
  - id: dod-003
    description: PR cites this ticket
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view 1530 --repo OmniNode-ai/omniclaude --json title,body --jq '.title + "
          " + (.body // "")' | grep -q 'OMN-10636'
