---
schema_version: "1.0.0"
ticket_id: "OMN-9272"
title: "Correct pr_review_bot contract endpoint_env registry drift"
summary: >-
  Bind central OCC evidence for omnimarket PR #735, which aligns node_pr_review_bot/contract.yaml model_routing
  endpoint_env with the bridge_config_loader registry by replacing the unregistered LLM_QWEN3_CODER_URL
  with canonical LLM_CODER_URL, and adds a regression test that all contract model_routing endpoint_env
  values are registered.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "protocols"
evidence_requirements:
  - kind: "manual"
    description: "omnimarket PR #735 carries the OMN-9272 endpoint_env contract fix."
    command: "gh pr view 735 --repo OmniNode-ai/omnimarket --json headRefOid,state,url"
  - kind: "tests"
    description: "Focused pr_review_bot model_routing endpoint_env regression tests pass."
    command: "uv run pytest tests/nodes/node_pr_review_bot/test_contract_model_routing_env_alignment.py
      tests/nodes/node_pr_review_bot/ -q -m unit"
  - kind: "manual"
    description: "Deploy Gate evidence records that this contract-only runtime config fix rides the normal
      post-merge deployment path."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9272/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-735"
    description: "Bind OMN-9272 evidence to omnimarket PR #735 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9272/dod-product-pr-735/command.yaml\"
          && grep -q '^pr_number: 735$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9272/dod-product-pr-735/command.yaml\"
          && grep -q '^commit_sha: \"11b26a84216b7490f8a734a03552ba20f3dc6975\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9272/dod-product-pr-735/command.yaml\""
  - id: "dod-focused-tests"
    description: "Focused pr_review_bot unit tests pass locally for endpoint_env registry alignment."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9272/dod-focused-tests/command.yaml\"
          && grep -q '19 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9272/dod-focused-tests/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Deploy assessment for contract-only runtime configuration fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9272/dod-deploy-assessment/command.yaml\"
          && grep -q '^pr_number: 735$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9272/dod-deploy-assessment/command.yaml\"
          && grep -q 'normal post-merge deployment' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9272/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-9272 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9272/dod-occ-pr-self/command.yaml\""
