---
schema_version: "1.0.0"
ticket_id: "OMN-12525"
title: "Epic: Unify ONEX handler-invocation / dispatch chain onto the five primitives"
summary: >-
  Central OCC pairing for the routing reconciliation epic as it relates to the June 9 dev/stability loop
  closure plan. The Phase 0 routing impact record verifies that the specific delegation and SEA command
  topics in scope have live MessageDispatchEngine dispatcher coverage and are not gated by the known default_handler
  NO_DISPATCHER plus DLQ failure mode.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-phase0-routing-impact-record"
    description: >-
      Independent routing audit records contract path, dispatcher route presence, default_handler risk,
      and closure gating decision for the three in-scope command topics.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12525/dod-phase0-routing-impact-record/command.yaml"
  - id: "dod-core-pr-1373"
    description: >-
      Consuming-PR binding: omnibase_core PR #1373 carries the OMN-12549 S0 dispatch-selection seam under
      the OMN-12525 dispatch-chain unification epic.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12525/dod-core-pr-1373/command.yaml"
  - id: "dod-spi-pr-252"
    description: >-
      Consuming-PR binding: omnibase_spi PR #252 carries the ProtocolDispatchEngine relocation under the
      OMN-12525 dispatch-chain unification epic.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12525/dod-spi-pr-252/command.yaml"
  - id: "dod-infra-pr-2184"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2184 carries the infra-side Mode B dispatch parity flip
      under the OMN-12525 dispatch-chain unification epic.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12525/dod-infra-pr-2184/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment: the OMN-12549 S0 infra-side PR touches runtime dispatch wiring paths, but
      performs no live deploy, restart, docker exec, rpk topic produce, broker mutation, database mutation,
      or .201 mutation before merge. Runtime pickup is via the standard post-merge build/deploy path.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12525/dod-deploy-assessment/command.yaml"
          && grep -qi 'deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12525/dod-deploy-assessment/command.yaml"
