---
schema_version: "1.0.0"
ticket_id: "OMN-12525"
title: "Epic: Unify ONEX handler-invocation / dispatch chain onto the five primitives"
summary: >-
  Central OCC pairing for the routing reconciliation epic as it relates to the June 9 dev/stability loop
  closure plan. The Phase 0 routing impact record verifies that the specific delegation and SEA command
  topics in scope have live MessageDispatchEngine dispatcher coverage and are not gated by the known default_handler
  NO_DISPATCHER plus DLQ failure mode.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-phase0-routing-impact-record"
    description: >-
      Independent routing audit records contract path, dispatcher route presence, default_handler risk,
      and closure gating decision for the three in-scope command topics.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12525/dod-phase0-routing-impact-record/command.yaml"
