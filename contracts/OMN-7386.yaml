---
schema_version: "1.0.0"
ticket_id: "OMN-7386"
title: "Add infrastructure consistency DoD check for cron scripts"
summary: >
  Adds the central DoD sweep INFRA_CONSISTENCY check for cron script changes. The check is cron-only,
  fails mixed INFRA_HOST/localhost script references, and requires canonical PASS receipt evidence before
  cron-script tickets can pass the structured DoD sweep.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-enforcement"
    description: >
      Central DoD sweep enum, model, and handler enforce INFRA_CONSISTENCY for cron-closeout.sh and cron-buildloop.sh
      tickets and catch mixed localhost / INFRA_HOST references.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7386/dod-infra-enforcement/command.yaml\""
  - id: "dod-focused-tests"
    description: >
      Focused handler/model tests and DoD sweep integration tests pass locally for the INFRA_CONSISTENCY
      enforcement path.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7386/dod-focused-tests/command.yaml\""
  - id: "dod-lint"
    description: "Ruff formatting/lint checks pass on the touched enforcement files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7386/dod-lint/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-7386 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7386/dod-occ-pr/command.yaml\""
