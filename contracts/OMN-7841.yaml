---
schema_version: "1.0.0"
ticket_id: OMN-7841
title: "Remove all seed values from model registry — replace with measured eval data"
summary: "Remove seed cost/speed values from the model registry and route unmeasured models with explicit
  lowest-priority scoring until eval metrics are available."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Seed cost/speed fields are absent from registry, model entry, and routing API surfaces
      in omnibase_infra PR #1480."
    command: "! rg -q \"seed_cost_per_1k_tokens|seed_tokens_per_sec\" docker/catalog/model_registry.yaml
      src/omnibase_infra/nodes/node_model_router_compute/models/model_registry_entry.py src/omnibase_infra/services/routing_api/routes.py"
  - kind: "tests"
    description: "Focused model router scoring tests pass at omnibase_infra PR #1480 head."
    command: "uv run pytest tests/unit/nodes/test_model_router/test_score_models.py -q"
  - kind: "ci"
    description: "Strict typing and ruff checks pass at omnibase_infra PR #1480 head."
    command: "uv run mypy src/ --strict && uv run ruff check src/ tests/"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Seed cost and speed fields are removed from model_registry.yaml, ModelRegistryEntry,
      and routing API response surfaces."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-7841/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-7841$' drift/dod_receipts/OMN-7841/dod-001/command.yaml && grep -q '^evidence_item_id:
          dod-001$' drift/dod_receipts/OMN-7841/dod-001/command.yaml && grep -q '^commit_sha: 3270a198351a42d3b5ddd540855c0e3efb19baf4$'
          drift/dod_receipts/OMN-7841/dod-001/command.yaml"
  - id: "dod-002"
    description: "Router scoring uses measured eval policy: 20-sample live metric threshold, tier-based
      cost proxy, and zero speed score for unmeasured models."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-7841/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-7841$' drift/dod_receipts/OMN-7841/dod-002/command.yaml && grep -q '^evidence_item_id:
          dod-002$' drift/dod_receipts/OMN-7841/dod-002/command.yaml && grep -q '^commit_sha: 3270a198351a42d3b5ddd540855c0e3efb19baf4$'
          drift/dod_receipts/OMN-7841/dod-002/command.yaml"
  - id: "dod-003"
    description: "Focused model router scoring tests pass locally at omnibase_infra PR #1480 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-7841/dod-003/command.yaml && grep
          -q '^ticket_id: OMN-7841$' drift/dod_receipts/OMN-7841/dod-003/command.yaml && grep -q '^evidence_item_id:
          dod-003$' drift/dod_receipts/OMN-7841/dod-003/command.yaml && grep -q '^exit_code: 0$' drift/dod_receipts/OMN-7841/dod-003/command.yaml"
  - id: "dod-004"
    description: "Strict mypy and ruff checks pass locally at omnibase_infra PR #1480 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-7841/dod-004/command.yaml && grep
          -q '^ticket_id: OMN-7841$' drift/dod_receipts/OMN-7841/dod-004/command.yaml && grep -q '^evidence_item_id:
          dod-004$' drift/dod_receipts/OMN-7841/dod-004/command.yaml && grep -q '^commit_sha: 3270a198351a42d3b5ddd540855c0e3efb19baf4$'
          drift/dod_receipts/OMN-7841/dod-004/command.yaml"
