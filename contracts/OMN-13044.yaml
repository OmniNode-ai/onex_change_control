---
schema_version: "1.0.0"
ticket_id: "OMN-13044"
title: "C-4: Worktree durability sweep - NO-TICKET + dirty-plan-file flags, rescue refs, off-volume backups"
summary: >-
  Worktree durability sweep for the worktree skill and audit logic: flag NO-TICKET worktrees, flag dirty
  worktrees touching files referenced by docs/plans/ or docs/handoffs/ documents, auto-create a rescue
  ref over git stash create before a handoff block, and require an off-volume backup copy for demo-critical
  .onex_state backups to count toward DoD. Retains the reaper boundary: the OMN-13228 Mac worktree reaper
  delegates dirty-worktree salvage to OMN-13044 and preserves the existing prune-worktrees.sh safety behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-salvage-out-of-scope-reference"
    description: "The reaper implementation documents salvage as out of scope and delegates cleanup safety."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13044/dod-salvage-out-of-scope-reference/command.yaml"
  - id: "dod-pr-contract"
    description: "Repo-local PR contract exists for the worktree durability sweep update."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13044/dod-pr-contract/command.yaml"
  - id: "dod-skill-durability-section"
    description: "SKILL.md worktree audit lists NO-TICKET, dirty-plan-file, rescue-ref, and off-volume
      backup steps."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13044/dod-skill-durability-section/command.yaml"
  - id: "dod-durability-helpers"
    description: "Pure durability helpers and flags model exist and are unit-tested."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13044/dod-durability-helpers/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding OCC receipt for the OMN-13044 central evidence PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13044/dod-occ-self/command.yaml"
