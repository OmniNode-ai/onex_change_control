---
schema_version: "1.0.0"
ticket_id: "OMN-11262"
title: "Migrate contract evidence storage model from omnibase_compat to omnibase_core"
summary: >
  Adds the canonical omnibase_core contract evidence storage models and binds completion proof to stable
  model artifacts, focused tests, and public exports instead of PR-number identity.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: "tests"
    description: "Focused core contract evidence model tests pass."
    command: "PYTHONPATH=src uv run pytest tests/unit/models/contracts/evidence/test_contract_evidence_spec.py
      -v"
  - kind: "manual"
    description: "Canonical model files exist under omnibase_core.models.contracts.evidence."
    command: "git show --name-only --format=oneline 7df84e7c02a466a00b5758e0cdca3b6948786f1c -- src/omnibase_core/models/contracts/evidence"
  - kind: "manual"
    description: "Core public contract package exports the evidence models."
    command: "PYTHONPATH=src uv run python -c 'from omnibase_core.models.contracts import ModelContractEvidenceSpec,
      ModelContractEvidenceProof, ModelEvidenceProvenance; print(\"import ok\")'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-model-artifact"
    description: "Canonical core evidence model artifacts are present in the OMN-11262 core commit."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11262/dod-core-model-artifact/command.yaml\""
  - id: "dod-core-focused-tests"
    description: "Focused core evidence model tests passed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11262/dod-core-focused-tests/command.yaml\""
  - id: "dod-core-public-export"
    description: "Core public contract package exports the evidence models."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11262/dod-core-public-export/command.yaml\""
