---
schema_version: "1.0.0"
ticket_id: "OMN-14288"
title: "feat(OMN-14288): enforcement + merge-policy parity ratchet (report-only) + ARCH-006 governance-of-record"
summary: >-
  Shared OCC companion for the two OMN-14288 parity-ratchet work PRs (both cite OMN-14288 in their titles,
  so a single central contract carries a pr-bound receipt for each audience). omnibase_infra PR #2250
  (jonah/omn-14288-required-context-parity-ratchet, head c0916ed29ca0d90fb86d62dd5b48d517def85a34) adds
  a REPORT-ONLY branch-protection parity ratchet (config-as-data manifest + pure eval lib + thin gh/YAML
  CLI + non-blocking workflow) that flags MISSING load-bearing gates and merge-policy drift without mutating
  any branch protection and always exits 0. omnimarket PR #1693 (jonah/omn-14288-arch006-parity-declaration,
  head e73a2ad4b04e5445a482fc1daeec0c775a709ea7) registers ARCH-006 required_context_parity in the architectural-invariant
  catalogue as a declaration-only entry (loaded + counted, no checker in _CHECKER_MAP, never emits a violation)
  that points at the omnibase_infra EFFECT surface for actual enforcement.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2250"
    description: >-
      Independent verification of omnibase_infra PR #2250 at head c0916ed29ca0d90fb86d62dd5b48d517def85a34.
      REPORT-ONLY confirmed: audit_required_context_parity_cli.py is store_true `--fail-on-findings` (default
      OFF) and always `return 0` on the default path even with findings; it only issues `gh api` GET reads
      of branch protection and NEVER mutates. Live report-only run reproduces the four MISSING findings
      (omnimarket deploy-gate M1 + reject-skip M2; omniclaude deploy-gate M3 + reject-skip M4) and exits
      0. CI: the only real red is the occ-preflight cascade (CI Summary fail-closes on it); the four non-occ
      items that surfaced as `fail` (Canonical Inference Gate, main-target-guard, non-dev-base-guard,
      URL Authority Gate) are `conclusion=cancelled` (concurrency churn), not failures - verified via
      the check-runs API (zero non-occ `conclusion=failure`).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14288/dod-infra-pr-2250/command.yaml && grep
          -q '^pr_number: 2250$' drift/dod_receipts/OMN-14288/dod-infra-pr-2250/command.yaml && grep -q
          'c0916ed29ca0d90fb86d62dd5b48d517def85a34' drift/dod_receipts/OMN-14288/dod-infra-pr-2250/command.yaml
  - id: "dod-market-pr-1693"
    description: >-
      Independent verification of omnimarket PR #1693 at head e73a2ad4b04e5445a482fc1daeec0c775a709ea7.
      Governance-of-record confirmed: at runtime `_load_invariant_contracts(None)` loads ARCH-006 (invariants_evaluated
      5 -> 6) while `'ARCH-006' in _CHECKER_MAP` is False (map keys ARCH-001..ARCH-005 only), so the pure
      node counts the invariant but never emits a violation for it - enforcement lives on the omnibase_infra
      branch-protection EFFECT surface (PR #2250). Tests: the two catalogue test files pass 29/29.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14288/dod-market-pr-1693/command.yaml && grep
          -q '^pr_number: 1693$' drift/dod_receipts/OMN-14288/dod-market-pr-1693/command.yaml && grep
          -q 'e73a2ad4b04e5445a482fc1daeec0c775a709ea7' drift/dod_receipts/OMN-14288/dod-market-pr-1693/command.yaml
  - id: "dod-market-pr-1693-head-635d5a08"
    description: >-
      Follow-up verification of omnimarket PR #1693 at head 635d5a0840eea6866fde36ffbbee16e3d778345f after
      adding the missing golden-chain coverage proof for the changed live-path node node_architectural_invariant_loop.
      The new test proves ARCH-006 governance-of-record behavior is loaded and counted without pure-node
      violation emission, and the targeted golden-chain coverage gate passes against the CI base.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14288/dod-market-pr-1693-head-635d5a08/command.yaml
          && grep -q '^pr_number: 1693$' drift/dod_receipts/OMN-14288/dod-market-pr-1693-head-635d5a08/command.yaml
          && grep -q '635d5a0840eea6866fde36ffbbee16e3d778345f' drift/dod_receipts/OMN-14288/dod-market-pr-1693-head-635d5a08/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the net-new OMN-14288 central contract
      and PASS receipts consumed by BOTH omnibase_infra#2250 and omnimarket#1693 through their Evidence-Source
      lines. Self-binds via the first commit's SHA for occ-preflight eligibility (no PR-number prediction).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14288/dod-occ-self/command.yaml
  - id: "dod-occ-pr-3854"
    description: >-
      OCC follow-up self-binding: onex_change_control PR #3854 carries the add-only OMN-14288 evidence
      refresh for omnimarket#1693 head 635d5a0840eea6866fde36ffbbee16e3d778345f after the golden-chain
      coverage repair. The receipt binds this OCC PR number and the initial follow-up evidence commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14288/dod-occ-pr-3854/command.yaml && grep -q
          '^pr_number: 3854$' drift/dod_receipts/OMN-14288/dod-occ-pr-3854/command.yaml
