---
schema_version: "1.0.0"
ticket_id: "OMN-11975"
title: "Start Kafka consumers concurrently during startup"
summary: >
  Central OCC evidence for omnibase_infra PR #1731, which changes Kafka EventBus start_consuming to reserve
  consumer keys and start distinct consumers concurrently instead of serializing group joins.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1731"
    description: "omnibase_infra PR #1731 carries the OMN-11975 concurrent consumer startup fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11975/dod-omnibase-infra-pr-1731/command.yaml
          && grep -q '^pr_number: 1731$' drift/dod_receipts/OMN-11975/dod-omnibase-infra-pr-1731/command.yaml"
  - id: "dod-local-validation"
    description: "Focused unit and integration coverage plus lint passed for concurrent consumer startup."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11975/dod-local-validation/command.yaml
          && grep -q 'test_kafka_concurrent_subscribe_startup.py' drift/dod_receipts/OMN-11975/dod-local-validation/command.yaml
          && grep -q 'ruff check' drift/dod_receipts/OMN-11975/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy was performed; runtime event-bus startup behavior remains gated by deploy-gate
      and runtime boot CI before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11975/dod-deploy-assessment/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-11975/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-1604"
    description: "onex_change_control PR #1604 carries the central OCC evidence for OMN-11975."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11975/dod-occ-pr-1604/command.yaml
          && grep -q '^pr_number: 1604$' drift/dod_receipts/OMN-11975/dod-occ-pr-1604/command.yaml"
