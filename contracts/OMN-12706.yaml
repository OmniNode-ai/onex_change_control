---
schema_version: "1.0.0"
ticket_id: "OMN-12706"
title: "Pattern-B runtime integration + prompt/contract parity tests for task.execute"
summary: >-
  Central evidence that omnimarket PR #1051 lands Phase 6 of the generic task.execute route as a dev-based
  PR (cherry-picked verified commit 867802ce0 onto dev after OMN-12705 landed). Test-only: adds end-to-end
  Pattern-B integration tests that drive node_task_execution_orchestrator through a real event bus (command
  topic -> subscribed consumer.process -> terminal result on the response topic), proving topic-based
  dispatch rather than command_name routing. task.execute COMPOSES existing authorities; no new envelope and
  no new DoD model is introduced. Redpanda variants are gated behind the opt-in RUN_REDPANDA_TASK_EXECUTE
  flag and skipped when the broker is unreachable.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-inmemory-bus-flows"
    description: "Test-only PR adds in-memory bus tests: raw prompt -> terminal contract/route plan; mechanical
      pytest check -> verification evidence (real COMMAND_EXIT_0 pytest executed by the verification authority);
      dry-run PR action -> PR plan (no side effects); prompt-vs-supplied-contract parity (same normalized
      contract + fingerprint + route plan)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12706/dod-inmemory-bus-flows/command.yaml"
          && grep -q '^pr_number: 1051$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12706/dod-inmemory-bus-flows/command.yaml"
  - id: "dod-true-topic-pattern-b"
    description: "In-memory tests wire handler.process as a real bus topic subscriber, so dispatch is
      topic-based Pattern-B (command topic -> subscribed consumer), not command_name routing."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12706/dod-true-topic-pattern-b/command.yaml"
          && grep -q '^pr_number: 1051$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12706/dod-true-topic-pattern-b/command.yaml"
  - id: "dod-redpanda-opt-in-skip"
    description: "Redpanda variants of the same core flows over aiokafka are gated behind the opt-in
      RUN_REDPANDA_TASK_EXECUTE flag and skipped when the broker is unreachable."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12706/dod-redpanda-opt-in-skip/command.yaml"
          && grep -q '^pr_number: 1051$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12706/dod-redpanda-opt-in-skip/command.yaml"
  - id: "dod-deploy-gate-no-runtime-mutation"
    description: "Deploy-gate evidence for omnimarket PR #1051: the change adds integration tests only. No
      production source change, no live deploy, restart, or .201 mutation is performed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12706/dod-deploy-gate-no-runtime-mutation/command.yaml"
          && grep -q '^pr_number: 1051$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12706/dod-deploy-gate-no-runtime-mutation/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-12706 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12706/dod-occ-pr/command.yaml"
