---
schema_version: "1.0.0"
ticket_id: OMN-10646
title: "Replace silent OMNI_HOME fallbacks with fail-fast reads"
summary: "Remove silent OMNI_HOME fallback behavior from omnimarket runtime sweep, platform checks, PR
  snapshot, and build-loop surfaces so workspace state is read from explicit operator configuration."
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket#569 checks pass for OMNI_HOME fail-fast behavior"
    command: "gh pr checks 569 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001-omnimarket-569"
    description: "omnimarket#569 carries OMNI_HOME fail-fast changes and repo-local deploy-gate contract"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 569 --repo OmniNode-ai/omnimarket --json headRefOid -q .headRefOid"
  - id: "dod-occ-pr-844"
    description: "onex_change_control#844 carries OMN-10646 central contract and receipt evidence"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 844 --repo OmniNode-ai/onex_change_control --json state,baseRefName --jq
          '.state + \":\" + .baseRefName'"
