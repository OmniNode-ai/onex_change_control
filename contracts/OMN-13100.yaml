---
schema_version: "1.0.0"
ticket_id: OMN-13100
title: "OMN-13026 drain: omnimarket transport-mock baseline — 101 violations across 26 files"
summary: "Drain all bare AsyncMock/MagicMock transport surface violations from omnimarket baseline to
  zero"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnimarket PR #1238 passes all CI checks including transport-mock-lint gate"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13100/dod-003/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "omnimarket PR #1238 is open with passing CI (verify gate green)"
    source: generated
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13100/dod-001/command.yaml"
          && grep -q '^pr_number: 1238$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13100/dod-001/command.yaml"
  - id: dod-002
    description: "transport-mock-lint check passes in omnimarket CI"
    source: generated
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13100/dod-002/command.yaml"
          && grep -q '^pr_number: 1238$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13100/dod-002/command.yaml"
  - id: dod-003
    description: "All CI checks pass on omnimarket PR #1238"
    source: generated
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13100/dod-003/command.yaml"
          && grep -q '^pr_number: 1238$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13100/dod-003/command.yaml"
  - id: dod-occ-pr
    description: "This OCC PR carries the central OMN-13100 contract and PASS receipts for omnimarket
      PR #1238"
    source: generated
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13100/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2672$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13100/dod-occ-pr/command.yaml"
