---
schema_version: "1.0.0"
ticket_id: OMN-13100
title: "OMN-13026 drain: omnimarket transport-mock baseline — 101 violations across 26 files"
summary: "Drain all bare AsyncMock/MagicMock transport surface violations from omnimarket baseline to
  zero"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "All 26 baseline files fixed; transport_mock_baseline.yaml empty; lint gate green"
    command: "uv run pre-commit run transport-mock-lint --all-files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "validation/transport_mock_baseline.yaml drained to empty mapping"
    source: generated
    checks:
      - check_type: command
        check_value: "python -c \"import yaml; d=yaml.safe_load(open('validation/transport_mock_baseline.yaml'));
          assert d == {} or d is None, f'baseline not empty: {d}'\""
  - id: dod-002
    description: "Transport Mock Lint gate passes with zero violations"
    source: generated
    checks:
      - check_type: command
        check_value: "uv run pre-commit run transport-mock-lint --all-files"
  - id: dod-003
    description: "Full unit test suite passes"
    source: generated
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/ -q --tb=no"
