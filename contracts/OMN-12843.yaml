---
schema_version: "1.0.0"
ticket_id: "OMN-12843"
title: "feat(OMN-12843): add EnumSelectionReason for Context Authority Rule"
summary: >-
  Central OCC evidence for omnibase_core PR #1289. The PR adds EnumSelectionReason as a StrEnum next to
  EnumContextFactor so context selection can stamp an explicit selection_reason in the Context Authority
  tuple. The enum contains exactly five values: policy_effectiveness, policy_required_factor, experiment_assignment,
  exploration, and fallback_no_score. This core slice is the typed authority field used by downstream
  policy selection work; no runtime deploy or live service mutation is required for this enum-only PR.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
evidence_requirements:
  - kind: "tests"
    description: >-
      EnumSelectionReason unit tests prove the exact member set, StrEnum inheritance, JSON round-trip,
      and rejection of unknown values.
    command: "uv run pytest tests/unit/enums/test_enum_selection_reason.py"
dod_evidence:
  - id: "dod-core-pr-1289"
    description: >-
      omnibase_core PR #1289 adds EnumSelectionReason at head 2b76542a477ae34de325a4fe01a5436f1650ced3.
      The focused enum tests pass, mypy --strict on the new enum module succeeds, and pre-commit hooks
      for the changed files pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12843/dod-core-pr-1289/command.yaml"
          && grep -q '^pr_number: 1289$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12843/dod-core-pr-1289/command.yaml"
          && grep -q '2b76542a477ae34de325a4fe01a5436f1650ced3' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12843/dod-core-pr-1289/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the onex_change_control PR carrying this OMN-12843 contract and the
      core PR receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12843/dod-occ-pr-self/command.yaml"
          && grep -q '^ticket_id: "OMN-12843"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12843/dod-occ-pr-self/command.yaml"
