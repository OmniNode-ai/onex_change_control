---
schema_version: "1.0.0"
ticket_id: "OMN-10665"
title: "Deploy k8s/data-plane to dev cluster"
summary: >
  Correct the non-live data-plane bootstrap script so it renders and applies the current k8s/data-plane
  manifest root. Live dev data-plane deployment remains gated on OMN-10661 live addon bootstrap, AWS credentials,
  kubeconfig access, and explicit approval.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-bootstrap-script-updated"
    description: "omninode_infra PR #412 updates scripts/bootstrap-data-plane.sh for current manifests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10665/dod-bootstrap-script-updated/command.yaml"
  - id: "dod-local-validation"
    description: "Local script, Terraform format, and kustomize render validation passed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10665/dod-local-validation/command.yaml"
  - id: "dod-pr-open"
    description: "omninode_infra PR #412 is open for OMN-10665 code review."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10665/dod-pr-open/command.yaml"
  - id: "dod-occ-pr"
    description: "Bind the central OCC evidence update to onex_change_control PR #932."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10665/dod-occ-pr/command.yaml"
