---
schema_version: "1.0.0"
ticket_id: "OMN-10718"
title: "Wire delegation projection pipeline for in-process mode"
summary: >
  Binds omniclaude PR #1545, which wires SQLiteProjectionAdapter into the hook runtime bus bootstrap so
  delegation events emitted during in-process delegation are persisted to SQLite for the delegation dashboard
  pipeline.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1545-head"
    description: "omniclaude PR #1545 carries the delegation projection wiring at the recorded implementation
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10718/dod-pr-1545-head/command.yaml\""
  - id: "dod-ci-1545-green"
    description: "All non-receipt CI checks on omniclaude PR #1545 are passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10718/dod-ci-1545-green/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10718/dod-occ-pr/command.yaml\""
