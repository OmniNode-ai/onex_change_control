---
schema_version: "1.0.0"
ticket_id: "OMN-11331"
title: "Add delegation event recording with causation chain in onex-self-extending-agent"
summary: >-
  Bind OCC evidence for onex-self-extending-agent PR #49 (OMN-11331), which adds contract-derived topic
  constants and event builders (build_attempt_started_event, build_escalation_event, build_delegation_completed_event),
  wires emission into DelegationExecutor.execute() with correct causation linkage, extends ModelDelegationResult
  with an events tuple, and adds 7 unit tests for the wiring. Receipts reference the observed downstream
  PR head and the OCC self-binding PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Downstream PR test suite passes: delegation event builders emit contract-derived topics
      and DelegationExecutor wires causation correctly."
    command: "gh pr checks 49 --repo OmniNode-ai/onex-self-extending-agent"
dod_evidence:
  - id: "dod-tests"
    description: "Bind OMN-11331 evidence to onex-self-extending-agent PR #49 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11331/dod-tests/command.yaml\"
          && grep -q '^pr_number: 49$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11331/dod-tests/command.yaml\"
          && grep -q '^commit_sha: \"d7af3dc3a3a411e9a61ecb2a01a65a3e2e6d4230\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11331/dod-tests/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-11331 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11331/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
