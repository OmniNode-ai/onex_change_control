---
schema_version: "1.0.0"
ticket_id: "OMN-14167"
title: "ci(OMN-14167): run omnimarket real-Postgres integration tests in CI"
summary: >-
  Central OCC evidence for omnimarket PR #1650. The PR adds a Postgres service to the omnimarket Tests
  workflow so real-DB integration tests execute instead of self-skipping, and fixes the DSN options encoding
  defect exposed by running the proof against real Postgres. This evidence PR performs no live deploy,
  runtime restart, branch-protection mutation, or bypass.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1650"
    description: >-
      omnimarket PR #1650 is open against dev at head e6c6f829a65be3954fb0b27cd5bbf60f6b47f6bd and carries
      the CI Postgres service plus DSN encoding fix needed to execute the real-DB tenant_id proof.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14167/dod-omnimarket-pr-1650/command.yaml &&
          grep -q '^pr_number: 1650$' drift/dod_receipts/OMN-14167/dod-omnimarket-pr-1650/command.yaml
          && grep -q '^commit_sha: e6c6f829a65be3954fb0b27cd5bbf60f6b47f6bd$' drift/dod_receipts/OMN-14167/dod-omnimarket-pr-1650/command.yaml
  - id: "dod-real-postgres-proof"
    description: >-
      PR #1650 records a real-Postgres proof that the target integration test self-skips without POSTGRES_PASSWORD
      and executes/passes when the CI-exported Postgres environment is present.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14167/dod-real-postgres-proof/command.yaml &&
          grep -qi 'real Postgres' drift/dod_receipts/OMN-14167/dod-real-postgres-proof/command.yaml
  - id: "dod-no-live-deploy"
    description: >-
      Scope evidence: this is CI/test-process work only. The OCC PR performs no live deploy, no runtime
      restart, no branch-protection change, and no bypass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14167/dod-no-live-deploy/command.yaml && grep
          -qi 'no live deploy' drift/dod_receipts/OMN-14167/dod-no-live-deploy/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding receipt: this onex_change_control PR carries the central OMN-14167 contract and PASS
      receipts consumed by omnimarket PR #1650 via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14167/dod-occ-pr-self/command.yaml && grep -q
          '^ticket_id: "OMN-14167"$' drift/dod_receipts/OMN-14167/dod-occ-pr-self/command.yaml
