---
schema_version: "1.0.0"
ticket_id: OMN-13705
title: "Add unit tests for gate/receipt_canonical.py (35% → 100% coverage)"
summary: >-
  Creates tests/unit/gate/test_receipt_canonical.py with 21 unit tests covering all three previously uncovered
  functions in omnibase_core/gate/receipt_canonical.py: _normalize_json (NFC passthrough, list recursion,
  dict key-sort, scalar passthroughs), canonical_receipt_payload (deterministic bytes, exclude_signature
  True/False, valid JSON output), and compute_receipt_schema_fingerprint (sha256: prefix, 64-hex format,
  determinism). Coverage rises from 35.48% to 100%. Pure computation — no subprocess, network, or live
  infra dependency. No source code changed; test-only addition.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >-
      21 unit tests for gate/receipt_canonical.py pass with 100% module coverage, no live services.
    command: "uv run pytest tests/unit/gate/test_receipt_canonical.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: >-
      21 unit tests for _normalize_json, canonical_receipt_payload, and compute_receipt_schema_fingerprint
      pass with 100% module coverage on omnibase_core PR #1347 (branch jonah/omn-13705-receipt-canonical-unit-tests,
      commit 27e459808a2b9c1cffaca8b7de02c39102088f60).
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13705/dod-001/command.yaml"
  - id: dod-deploy
    description: >-
      Deploy-gate assessment: test-only addition, no runtime process, Docker image, broker topic, daemon,
      or service deploy required. Static echo proof.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13705/dod-deploy/command.yaml"
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt — this onex_change_control PR carries the OMN-13705 contract + receipts
      consumed by the omnibase_core test PR via Evidence-Source.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13705/dod-occ-self/command.yaml"
