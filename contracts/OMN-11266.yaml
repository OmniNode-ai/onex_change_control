---
schema_version: "1.0.0"
ticket_id: "OMN-11266"
title: "feat(OMN-11266): fail handoff generation on unsourceable .201 health data"
summary: >
  omnimarket#730 adds a deterministic pre-generation validation gate to node_handoff_effect. The gate
  checks env-sync.log readability, configured .201 SSH target, and service catalog health before any handoff
  artifact is written, surfacing failures through HandoffGateError with exit code 2 and structured HANDOFF_GATE_FAILURE
  stderr output.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Focused handoff gate and failure-mode tests pass at the current omnimarket#730 head.
    command: uv run pytest tests/test_handoff_failure_modes.py::TestEnvSyncLogNoSuccessLine::test_log_with_no_success_written_to_artifact
      tests/test_handoff_failure_modes.py tests/test_handoff_validation_gate.py -q
  - kind: manual
    description: omnimarket PR  #730 carries the handoff validation gate implementation and test repair.
    command: gh pr view 730 --repo OmniNode-ai/omnimarket --json headRefOid,state,url
  - kind: manual
    description: OCC PR self-binds the OMN-11266 contract and receipts.
    command: gh pr view <OCC-PR-NUM> --repo OmniNode-ai/onex_change_control --json number,state
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused handoff failure-mode and validation gate tests pass locally for omnimarket#730."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -Eq '^status: "?PASS"?$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11266/dod-focused-tests/command.yaml"
          && grep -q '^pr_number: 730$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11266/dod-focused-tests/command.yaml"
          && grep -q '^commit_sha: "a4d4cfb4f776608a97ed4e9262597b7a8e61617e"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11266/dod-focused-tests/command.yaml"
  - id: "dod-product-pr"
    description: "omnimarket#730 carries the handoff validation gate implementation and current test-signature
      repair."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -Eq '^status: "?PASS"?$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11266/dod-product-pr/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11266 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -Eq '^status: "?PASS"?$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11266/dod-occ-pr-self/command.yaml"
