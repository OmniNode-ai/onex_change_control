---
schema_version: "1.0.0"
ticket_id: "OMN-11266"
title: "Handoff validation gate — fail on unsourceable .201 health data"
summary: >-
  OCC evidence binder for omnimarket PR #730 (OMN-11266). Adds HandoffGateError (exit code 2) plus validate_infra_sources()
  as a hard pre-generation gate in node_handoff_effect that checks env-sync.log exists+readable, SSH target
  configured, and all 6 service catalog probes healthy before any artifact I/O. Builds on ServiceCatalogSnapshot
  from OMN-11267 and existing _gather_infra_health() from OMN-8867.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #730 CI passes (architectural compliance, contract compliance, golden
      chain, hostile reviewer, runtime sweep, full pytest)."
    command: "gh pr checks 730 --repo OmniNode-ai/omnimarket"
dod_evidence:
  - id: "dod-handoff-gate-product"
    description: "Bind OMN-11266 evidence to omnimarket PR #730 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11266/dod-handoff-gate-product/command.yaml\"
          && grep -q '^pr_number: 730$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11266/dod-handoff-gate-product/command.yaml\"
          && grep -q '^commit_sha: \"a4d4cfb4f776608a97ed4e9262597b7a8e61617e\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11266/dod-handoff-gate-product/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-11266 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11266/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
