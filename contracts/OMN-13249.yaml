---
schema_version: "1.0.0"
ticket_id: "OMN-13249"
title: "Structural agent_invocation carve-out for canonical inference gate"
summary: >-
  OMN-13249 adds the Phase C validator carve-out for canonical inference enforcement. ValidatorCanonicalInference
  continues to fail closed for model CLI subprocess usage unless the enclosing node contract.yaml declares
  descriptor.agent_invocation: true as a strict YAML boolean. The permission is structural and contract-owned,
  not a widened free-text suppression comment. scan_source now accepts an optional source_path so scan_tree,
  _validate_file, and the staged-files CLI path can locate the enclosing contract. Text-only scans without
  a source path still fail closed. The carve-out covers only the shell-out surface for codex, claude,
  gemini, or opencode command invocation; model-id environment reads are never licensed by the flag. The
  PR also adds fail-closed tests for no-flag, strict-flag, and inference-tier cases, plus a YAML validation
  allowlist entry for the validator that reads generic node contracts.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1262"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1262 (jonahgabriel/omn-13249-phase-c-gate-carve-out-agent_invocation-fleet-wide-rollout,
      base=dev). The PR updates ValidatorCanonicalInference so model CLI shell-out is permitted only when
      the enclosing node contract declares descriptor.agent_invocation: true as a strict boolean, keeps
      text-only scans fail-closed, and adds fail-closed regression tests for missing flag, valid flag,
      sibling modules, and inference-tier misuse. This receipt binds OMN-13249 to omnibase_core PR #1262
      by pr_number and commit_sha so downstream Receipt Gate can resolve central OCC evidence for the
      citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13249/dod-core-pr-1262/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13249 contract and omnibase_core
      PR #1262 binding. This binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility
      check can resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13249/dod-occ-pr-self/command.yaml
