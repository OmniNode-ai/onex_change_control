---
schema_version: "1.0.0"
ticket_id: "OMN-10570"
title: "Move experiments out of src/omnimarket entirely"
summary: "Move src/omnimarket/experiments/ to top-level experiments/ so it no longer ships in the wheel."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #541 test and typecheck checks pass."
    command: "gh pr checks 541 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "omnimarket PR #541 head is bound to OMN-10570."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10570/dod-001/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10570\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10570/dod-001/command.yaml\"
          && grep -q '^pr_number: 541$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10570/dod-001/command.yaml\"
          && grep -q '^commit_sha: \"0b3a7f1c3a2109a69ce50b653b2ae583d829ae96\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10570/dod-001/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR #766 carries the OMN-10570 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10570/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number: 766$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10570/dod-occ-pr/command.yaml\""
