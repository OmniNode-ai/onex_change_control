---
schema_version: "1.0.0"
ticket_id: OMN-10705
title: "fix(OMN-10705): Remove bus bypass handler imports from onex-setup.py"
summary: >
  Replaces 5 direct nodes.*.handlers.<module> imports in scripts/onex-setup.py with imports from each
  node's handlers/__init__.py public API. Adds HandlerSetupOrchestrator, HandlerInfisicalFullSetup, and
  HandlerServiceValidate to their respective handlers/__init__.py __all__ exports. No behavior change
  — same handler classes are used, now accessed via the package public surface. Bumps contract_version
  patch for 3 affected nodes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-unit-tests
    description: "33 unit tests pass across the 5 affected node test suites."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10705/dod-unit-tests/command.yaml\"
          && grep -q '^ticket_id: OMN-10705$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10705/dod-unit-tests/command.yaml\""
  - id: dod-integration-tests
    description: "9 integration tests in test_setup_handlers_public_api.py confirm all 3 handlers are
      importable via public __init__.py and match direct module imports."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10705/dod-integration-tests/command.yaml\"
          && grep -q '^ticket_id: OMN-10705$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10705/dod-integration-tests/command.yaml\""
  - id: dod-deploy
    description: "No runtime deploy required — change only exposes existing handler classes via __init__.py
      public API. No new imports in runtime kernel, no new Kafka wiring, no new container bindings. Handler
      classes were already reachable via direct sub-module import; this change only adds them to the package-level
      __all__."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10705/dod-deploy/command.yaml\"
          && grep -q '^ticket_id: OMN-10705$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10705/dod-deploy/command.yaml\""
  - id: dod-occ-pr
    description: "Self-binding receipt for this OCC PR #935."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10705/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: OMN-10705$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10705/dod-occ-pr/command.yaml\""
