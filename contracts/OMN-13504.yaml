---
schema_version: "1.0.0"
ticket_id: "OMN-13504"
title: "build(OMN-13504): re-lock omnimarket dev omnibase-core pin to merged dev SHA 81c865f6"
summary: >-
  OCC evidence for the omnimarket dependency re-lock that repoints omnibase-core from the unmerged OMN-13408
  feature-branch SHA 79ec3ad9d801b8da38b4d9099852cc8bfffad00e to the merged omnibase_core dev SHA 81c865f69a15781d526125ee13f39834a3ce69fd.
  The two SHAs are byte-identical for the relevant content, but only 81c865f6 is an ancestor of omnibase_core
  origin/dev, satisfying the OMN-12977 sibling-pin guard and unblocking the subsequent gated dev redeploy.
  This contract exists so the omnimarket product PR Receipt Gate binds via Evidence-Source.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      The new omnibase-core pin 81c865f69a15781d526125ee13f39834a3ce69fd is an ancestor of omnibase_core
      origin/dev, and its content diff from the prior unmerged pin is empty.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: \"PASS\"$' drift/dod_receipts/OMN-13504/dod-001/command.yaml"
  - id: "dod-002"
    description: >-
      The OMN-12977 sibling-pin guard exits 0 and reports omnibase-core [OK] against the canonical clones
      with the product PR lockfile.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: \"PASS\"$' drift/dod_receipts/OMN-13504/dod-002/command.yaml"
  - id: "dod-003"
    description: >-
      The lock resolves, omnibase_core imports as version 0.46.0, and the old unmerged SHA is absent from
      pyproject.toml and uv.lock.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: \"PASS\"$' drift/dod_receipts/OMN-13504/dod-003/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13504.yaml consumed
      by the omnimarket product PR Receipt Gate via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: \"PASS\"$' drift/dod_receipts/OMN-13504/dod-occ-pr/command.yaml"
