---
schema_version: "1.0.0"
ticket_id: "OMN-18048"
title: "A qualifier must not hide an acceptance-criteria heading"
summary: >
  OCC evidence companion for OmniNode-ai/omnibase_infra PR #3351. The evidence-autoclose sweep reported
  "2 acceptance criteria" on a ticket declaring four: a trailing qualifier on the heading skipped the
  whole section, and a bolded item was not matched inside a section that was found. Authored by hand:
  the OMN-16434 autobinder has minted no Evidence-Source companion since 02:57:22Z today, the same consumer
  inaction recorded in OCC#8753 and OCC#8756.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3351 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3351: the test module the PR adds, which asserts every measured
      AC-heading spelling and the bolded-item forms are recognised. Executed at cwd
      `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18048_ac_heading_qualifiers.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omn-18048-behavior-proof"
    description: "PR #3351 — the sweep-effect package passes with the parser fix, including the new OMN-18048
      module."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/ -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-omn-18048-behavior-proof-budget"
    description: "PR #3351 — budget-bounded behavior proof for this ticket's AC heading parser module."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-omn-18048-behavior-proof"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18048_ac_heading_qualifiers.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-omn-18048-qualifier-and-emphasis"
    description: "Both parser gaps are closed on the merged head: the heading comparison strips a trailing
      qualifier, and the item regex tolerates leading emphasis markers."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py?ref=546d66164e84fb277c5302dbc00d83659647f35f --jq '.content' | base64 -d | grep -c '_TRAILING_QUALIFIER_RE'
  - id: "dod-omn-18048-red-first-module-exists"
    description: "The RED-first test module the fix was written against is present on the merged head."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3351/files --paginate --jq '.[].filename' | grep -c 'test_omn_18048_ac_heading_qualifiers.py'
  - id: "occ-self-bind-pr-8762"
    description: |-
      OCC self-binding receipt for PR #8762, the in-repo evidence PR carrying this contract.
      Binds OMN-18048 to the OCC PR by pr_number so eligibility can resolve a PASS receipt
      for the evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 8762 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "occ-self-bind-pr-8774"
    description: |-
      OCC self-binding receipt for PR #8774, the in-repo evidence PR carrying this contract.
      Binds OMN-18048 to the OCC PR by pr_number so eligibility can resolve a PASS receipt
      for the evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 8774 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "occ-self-bind-pr-8783"
    description: |-
      OCC self-binding receipt for PR #8783, the in-repo evidence PR carrying the
      second-consumer rebind of OMN-18048 onto omnibase_infra#3356. Binds OMN-18048 to
      this OCC PR by pr_number so eligibility can resolve a PASS receipt for the
      evidence PR itself. Appended, not edited: every existing entry is untouched, so
      the merged receipts stay bound by their contract_entry_sha256 (OMN-13888).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 8783 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "ac1-qualified-headings-are-recognised"
    description: >-
      AC1 binding. Runs the class whose own docstring states the criterion — "AC1 - a qualifier must not
      hide an otherwise-recognised heading" — so the mapping is transcribed from the test module, not
      inferred from a class name. Covers the trailing parenthetical AND the PREFIX form (## Falsifiable
      acceptance criteria) that a trailing-only fix misses. Net-new entry: every pre-existing entry on
      this contract is merged and the Append-Only Gate holds them immutable (ci.yml:1446).
    source: "manual"
    status: "verified"
    binds_ac: ["AC1"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18048_ac_heading_qualifiers.py::TestQualifiedHeadingsAreRecognised -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "ac2-items-parse-identically-across-styles"
    description: >-
      AC2 binding. Runs the class whose docstring states the criterion — "AC2 - the same criteria must
      parse identically however they are written" — including test_the_real_omn_18035_shape_yields_four_not_two,
      which is the exact miscount that opened this ticket: four declared criteria, the sweep reported
      two.
    source: "manual"
    status: "verified"
    binds_ac: ["AC2"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18048_ac_heading_qualifiers.py::TestBoldPrefixedItemsAreSeen -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "ac5-widening-does-not-over-match"
    description: >-
      AC5 binding. Runs the class whose docstring states the criterion — "AC5 - recognising more headings
      must not start inventing criteria" — the negative half of AC1. Without it AC1 could be satisfied
      by matching everything, which is the same defect in the other direction.
    source: "manual"
    status: "verified"
    binds_ac: ["AC5"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18048_ac_heading_qualifiers.py::TestWideningDoesNotBecomeOverMatching -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "ac4-regression-mutation-proof"
    description: >-
      AC4 binding. Runs the complete regression module covering all three heading strings, all three item
      styles, section bounding, and the two independent mutation targets. The merged product PR recorded
      RED-first mutation runs; this net-new entry binds the executable regression suite without editing
      any merged evidence item.
    source: "manual"
    status: "verified"
    binds_ac: ["AC4"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18048_ac_heading_qualifiers.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-8940"
    description: >-
      OCC self-binding receipt for PR #8940, the in-repo evidence PR carrying this contract. Binds OMN-18048
      to the OCC PR by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8940 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "ac3-corpus-count-is-recorded-with-its-command"
    description: >-
      AC3 binding. AC3 asks that the corpus measurement be recorded as a number with the command behind
      it. The measurement IS recorded, on merged origin/dev, in the parser's own docstring at handler_evidence_autoclose_sweep.py:
      "Measured (OMN-18048 AC3, 110 ticket descriptions read at full text): **14.5%** carry an AC-looking
      heading this set does not recognise, across **12 distinct spellings**." The check below reads that
      file from the merged head over the GitHub contents API and asserts all three figures together —
      corpus size 110, rate 14.5%, and 12 distinct spellings — so a silent edit to any one of them turns
      the check red rather than passing on a partial match. Verified with a positive control (exits 0
      against the live merged head) and a negative control (the same command with corpus size 999 exits
      1), so a zero here is a real zero and not a swallowed error; stderr is not suppressed. This entry
      is net-new and append-only: every pre-existing entry on this contract is merged and held immutable
      by the Append-Only Gate, and nothing above is edited. The assignee's stated reason for leaving AC3
      unbound (2026-09-10T12:23Z) is that the historical count has no reproducible committed command.
      That concerns RE-RUNNING the census, which AC3 does not ask for; AC3 asks that the number be recorded
      with its command, and this binds the record.
    source: "manual"
    status: "verified"
    binds_ac: ["AC3"]
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py?ref=dev" --jq ".content" | base64 -d | tr "\n" " " | grep -qE "Measured \(OMN-18048 AC3, 110 ticket descriptions read at full text\): +\*\*14\.5%\*\* carry an AC-looking heading this set does not recognise, across +\*\*12 distinct spellings\*\*"
