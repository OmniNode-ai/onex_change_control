---
schema_version: "1.0.0"
ticket_id: "OMN-12624"
title: "docs(OMN-12624): full doc rewrite to canonical state after proof (Section 7B)"
summary: >-
  T6 (Section 7B) of the Divergent Transport & Runtime Convergence epic (OMN-12618): rewrite the architecture
  docs in omni_home from the OMN-12622 stale-banner state to canonical proven state, now that T1/T2/T4
  exist. Five docs were rewritten in omni_home (docs-only, no code/runtime paths touched): docs/architecture/OMNINODE_ARCHITECTURE_GAPS_V2.md
  (§4 DLQ split into "routing exists" vs "replay now contract-native" with onex.dlq.<category>.v1 naming;
  §5 RPO split into "bounded spool exists (telemetry)" vs "duty-critical tier RPO-zero via durable outbox");
  docs/architecture/OMNINODE_ARCHITECTURE_GRANULAR.md (canonical onex.dlq.<category>.v1 naming + contract-native
  replay node, tiered RPO posture, §11 topic migration now contract-native via NodeTopicMigrationExecutorEffect);
  docs/architecture/omniclaw-system-architecture.md (Failure Model + versioning rewritten); docs/handoffs/2026-05-31-sea-canonical-architecture-handoff.md
  (verification dates + superseded note reconciled to proven convergence); docs/architecture/README.md
  (index moved the three docs from "stale" to "canonical — rewritten to proven state" with a source-PR
  table). Every rewritten doc carries BOTH a source-verification date (2026-06-03) and a runtime-verification
  date ("PENDING operator-approved .201 proof", since live .201 runtime is held behind the OMN-12574/12588
  deploy hold). Every canonical claim is grounded in merged source: OMN-12619 (omnibase_infra PR 1839,
  merged 2026-06-02, dev 5577e9fc) for onex.dlq.<category>.v1 naming + node_dlq_replay_effect + quarantine-not-drop;
  OMN-12620 (omnimarket PR 1022, merged 2026-06-02, 5f11dfdf) for tiered producer-edge durability with
  the RPO-zero duty-critical durable outbox; OMN-12623 (omnibase_infra PR 1841) for the contract-native
  topic-migration executor. This contract covers the landable docs-only change. No source code or runtime
  path is modified by OMN-12624; no live .201 runtime step is taken — runtime proof of the underlying
  nodes is operator-gated and PENDING.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-docs-precommit"
    description: "The five rewritten omni_home docs pass pre-commit on the changed-file scope (no functional
      code, no hardcoded paths, no aislop violations introduced). This is a docs-only change: no .py/.sh
      and no runtime path is touched, so no deploy/runtime verification is in scope for OMN-12624 itself.
      The underlying runtime nodes' live .201 proof is operator-gated (OMN-12574/12588 deploy hold) and
      is documented in each doc as 'PENDING operator-approved .201 proof'."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12624/dod-docs-precommit/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-12624 central contract and receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12624/dod-occ-pr-self/command.yaml"
