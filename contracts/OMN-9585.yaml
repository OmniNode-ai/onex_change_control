---
schema_version: "1.0.0"
ticket_id: OMN-9585
title: "Restore merge-sweep runtime handler registration and deploy validation"
summary: "Restore merge-sweep runtime handler registration and deploy validation"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "protocols"
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR checks pass, including deploy gate"
    command: "gh pr checks 401 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "PR lifecycle contract wiring tests pass"
    command: "uv run pytest tests/test_pr_lifecycle_contract_autowiring.py tests/test_golden_chain_merge_sweep.py
      -q"
  - kind: "manual"
    description: "Post-merge .201 runtime deploy and Kafka consumer smoke validate pr_lifecycle handler
      registration"
    command: "deploy affected runtime to .201, then verify pr_lifecycle consumer groups have live members
      and registered handlers after restart"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused PR lifecycle contract and merge-sweep golden tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_pr_lifecycle_contract_autowiring.py tests/test_golden_chain_merge_sweep.py
          -q"
  - id: "dod-002"
    description: "Touched test surface passes lint and format"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff format tests/test_pr_lifecycle_contract_autowiring.py && uv run ruff
          check tests/test_pr_lifecycle_contract_autowiring.py"
  - id: "dod-003"
    description: "Post-merge deploy validates pr_lifecycle runtime consumer registration on .201"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "deploy runtime on .201 after merge; verify with docker exec and rpk group describe
          that pr_lifecycle consumer groups have live members and registered handlers"
