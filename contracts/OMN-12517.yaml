---
schema_version: "1.0.0"
ticket_id: "OMN-12517"
title: "Contract-bind omnimarket delegation max_tokens defaults and hard limit"
summary: >
  Bind central OCC evidence for omnimarket PR #1003, which sets delegation request max_tokens defaults
  and hard limits to the demo-safe 8192 contract value across production request models, adapter payload
  construction, and CLI defaults.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
  - protocols
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1003"
    description: "Bind OMN-12517 evidence to omnimarket PR #1003."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12517/dod-omnimarket-pr-1003/command.yaml
          && grep -q '^pr_number: 1003$' drift/dod_receipts/OMN-12517/dod-omnimarket-pr-1003/command.yaml"
  - id: "dod-focused-validation"
    description: "Focused ruff and pytest validation pass for the current omnimarket PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12517/dod-focused-validation/command.yaml
          && grep -q '44 passed' drift/dod_receipts/OMN-12517/dod-focused-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: delegation token-limit defaults are contract/model validation
      changes and require normal post-merge runtime rollout, not an ad hoc live deploy before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12517/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-12517/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr-1950"
    description: "OCC PR #1950 publishes the central OMN-12517 contract and receipt bundle."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12517/dod-occ-pr-1950/command.yaml
          && grep -q '^pr_number: 1950$' drift/dod_receipts/OMN-12517/dod-occ-pr-1950/command.yaml"
