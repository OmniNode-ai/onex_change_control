---
schema_version: "1.0.0"
ticket_id: "OMN-13240"
title: "fix(OMN-13240): detect qualified Pydantic BaseModel contracts"
summary: >
  Adds a focused ServiceContractValidator extraction in omnibase_core so model compliance detection handles
  qualified Pydantic base classes such as pydantic.BaseModel. The product PR is limited to validator logic
  and unit coverage; no runtime process, broker topic, Docker image, schema migration, or deployed service
  behavior is changed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused validator coverage proves qualified pydantic.BaseModel subclasses are detected."
    command: "uv run pytest tests/unit/services/test_service_contract_validator_coverage.py -q"
  - kind: "ci"
    description: "Changed validator file passes strict type checking."
    command: "uv run mypy src/omnibase_core/services/service_contract_validator.py --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1263"
    description: "omnibase_core PR #1263 contains the scoped validator extraction and qualified BaseModel
      test."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13240/dod-omnibase-core-pr-1263/command.yaml"
  - id: "dod-focused-verification"
    description: "Focused ruff, pytest, mypy, and diff checks passed on the changed files before push."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13240/dod-focused-verification/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Deploy assessment: no live deploy is required. The change is validator-only Python plus unit tests;
      it changes no runtime process, Docker image, broker topic, database migration, or service deployment.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13240/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy required' drift/dod_receipts/OMN-13240/dod-deploy-assessment/command.yaml"
  - id: "pr-occ-2768"
    description: "OCC PR #2768 carries the central OMN-13240 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13240/pr-occ-2768/command.yaml &&
          grep -q '^pr_number: 2768$' drift/dod_receipts/OMN-13240/pr-occ-2768/command.yaml"
