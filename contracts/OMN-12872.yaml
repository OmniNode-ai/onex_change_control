---
schema_version: "1.0.0"
ticket_id: "OMN-12872"
title: "Dev/stability closure: runtime identity bundle capture"
summary: >-
  Tracks runtime identity capture for dev/stability closure proofs: runtime image source SHAs, SEA client
  SHA, image digest, overlay and contract hashes, broker/projection/effects health, consumer groups, and
  DLQ status. The current observed bundle is evidence of runtime state, not final lane closure if source
  provenance is behind the accepted dev baseline.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-runtime-identity-bundle"
    description: >-
      Current stability-test runtime identity bundle is captured from read-only Docker, health, provenance,
      broker, and projection surfaces, including an explicit closure-gate result for whether it can be
      used as the accepted final delegation/SEA/escalation baseline.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12872/dod-runtime-identity-bundle/command.yaml"
