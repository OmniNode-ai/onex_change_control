---
schema_version: "1.0.0"
ticket_id: "OMN-12872"
title: "Dev/stability closure: runtime identity bundle capture"
summary: >-
  Tracks runtime identity capture for accepted dev/stability closure proofs: runtime image source SHAs,
  SEA client SHA, image digest, overlay and contract hashes, broker/projection/effects health, consumer
  groups, and DLQ status.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-runtime-identity-bundle"
    description: >-
      Runtime identity bundle is captured and attached by correlation ID to accepted delegation, SEA, and
      escalation proof packets.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12872/dod-runtime-identity-bundle/command.yaml"
