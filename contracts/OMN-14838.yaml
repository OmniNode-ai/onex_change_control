---
schema_version: "1.0.0"
ticket_id: "OMN-14838"
title: "Autobind OCC evidence for OMN-14838"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1833."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1833 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1833"
    description: "PR #1833 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1833-ci"
    description: "PR #1833 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4463"
    description: "OCC companion PR #4463 — self-bind for OMN-14838 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1833-deploy-scope"
    description: >-
      Deploy-gate scope for omnimarket PR #1833. The change touches src/omnimarket/nodes/node_prod_promotion_gate_compute/handlers/handler_prod_promotion_gate.py,
      a runtime node handler path, but only regenerates the existing prod-promotion gate handler to the
      canonical typed def-B dispatch shape with equivalence receipts. No live runtime deployment is performed
      by this PR; the required deploy evidence is the merge-time CI/OCC gate suite plus post-merge availability
      of the handler path on dev.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          printf '%s\n' 'deploy scope assessed for omnimarket#1833 runtime prod-promotion gate handler
          path; no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'
  - id: "occ-self-bind-pr-4466"
    description: "OCC deploy-scope repair PR #4466 — self-bind for OMN-14838."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
