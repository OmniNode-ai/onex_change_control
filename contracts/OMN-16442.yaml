---
schema_version: "1.0.0"
ticket_id: "OMN-16442"
title: "Autobind OCC evidence for OMN-16442"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2975.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2975 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2975 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/unit/contracts/test_llm_endpoints_contract.py tests/unit/docker/test_model_registry_health_paths.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2975"
    description: "PR #2975 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/unit/contracts/test_llm_endpoints_contract.py?ref=2b083664d7ffd0efc60dade090ebb4c9a9ea327f --jq '.content' | base64 -d | grep -c 'def test_reasoning_moe_35b_is_disabled_after_endpoint_loss'
      - check_type: "command"
        check_value: "gh pr view 2975 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2975 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    execution_scope: "local_done_gate"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/contracts/test_llm_endpoints_contract.py tests/unit/docker/test_model_registry_health_paths.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-7453"
    description: "Binds OMN-16442 to OCC companion PR #7453 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7453 --repo OmniNode-ai/onex_change_control --json number,state"
