---
schema_version: "1.0.0"
ticket_id: "OMN-14088"
title: "fix(OMN-14088): wire omniweb Keycloak auth into live onex-dev manifest"
summary: >-
  Central OCC evidence for omninode_infra PR #578. The PR wires the live onex-dev omniweb manifest for
  NextAuth v5 AUTH_SECRET and Keycloak OIDC configuration, reusing the existing Infisical secret source
  for AUTH_SECRET and adding the required Keycloak client secret reference. This OCC evidence update performs
  no live deploy, restart, branch-protection mutation, or bypass.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-578"
    description: >-
      omninode_infra PR #578 is open against dev at head 76ca761d4f4851fe581acf72784dc375a6a8a5a5 and
      carries the omniweb AUTH_SECRET and Keycloak OIDC manifest wiring for OMN-14088.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14088/dod-omninode-infra-pr-578/command.yaml
          && grep -q '^pr_number: 578$' drift/dod_receipts/OMN-14088/dod-omninode-infra-pr-578/command.yaml
          && grep -q '^commit_sha: 76ca761d4f4851fe581acf72784dc375a6a8a5a5$' drift/dod_receipts/OMN-14088/dod-omninode-infra-pr-578/command.yaml
  - id: "dod-no-live-deploy"
    description: >-
      Deploy-scope evidence: this OCC PR only records central evidence for a source manifest PR. It performs
      no live deploy, no runtime restart, no branch-protection change, and no bypass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14088/dod-no-live-deploy/command.yaml && grep
          -qi 'no live deploy' drift/dod_receipts/OMN-14088/dod-no-live-deploy/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding receipt: this onex_change_control PR carries the central OMN-14088 contract and PASS
      receipts consumed by omninode_infra PR #578 via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14088/dod-occ-pr-self/command.yaml && grep -q
          '^pr_number: 3728$' drift/dod_receipts/OMN-14088/dod-occ-pr-self/command.yaml
