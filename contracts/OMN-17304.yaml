---
schema_version: "1.0.0"
ticket_id: "OMN-17304"
title: "Autobind OCC evidence for OMN-17304"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3065.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3065 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3065 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: "uv run pytest tests/unit/cli/test_cli_delegate.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3065"
    description: "PR #3065 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/unit/cli/test_cli_delegate.py?ref=05d8bbc141391285764cbcbf287e6b8b66b8167d --jq '.content' | base64 -d | grep -c 'class TestExplicitOverrideProvenance'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3065/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_infra/cli/cli_delegate.py'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3065 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/cli/test_cli_delegate.py -q"
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-7826"
    description: "Binds OMN-17304 to OCC companion PR #7826 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7826 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-7925"
    description: "Binds OMN-17304 to OCC companion PR #7925 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7925 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-7984"
    description: "Binds OMN-17304 to OCC companion PR #7984 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7984 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3361"
    description: >
      PR #3361 on OmniNode-ai/omnibase_infra — the AC4 client-mode caller: `--locus {auto,in-process,deployed-lane}`,
      the fail-closed dispatch gate that requires a live STABLE consumer group on the contract-declared
      command topic before publishing, tri-state `live_consumer_groups()` in backend_probe, and locus
      provenance in the receipt and capture log. Bound by hand (union-resolve) because autobind mints
      no companion in this window (OMN-18073) — the same posture OCC#8760, OCC#8779 and OCC#8786 took.
    source: "generated"
    binds_ac:
      - "AC4"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3361 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3361/files --paginate --jq '.[].filename' | grep -cFx 'tests/unit/cli/test_delegate_locus.py'
  - id: "occ-self-bind-pr-8792"
    description: |-
      OCC self-binding receipt for PR #8792, the in-repo evidence PR carrying the
      union-resolve companion for omnibase_infra#3361. Binds OMN-17304 to this OCC PR
      by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself.
    source: "manual"
    status: "verified"
    binds_ac:
      - "AC4"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 8792 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3366"
    description: >
      PR #3366 on OmniNode-ai/omnibase_infra — the AC4 locus gate authenticates to a SASL lane. #3361
      landed `live_consumer_groups()` as the fail-closed liveness gate in front of every dispatched delegation,
      but built its confluent AdminClient implicitly PLAINTEXT — the shape OMN-18012 had already corrected
      in the two sibling synchronous admin clients in the same repo. Because this gate RAISES on an unanswerable
      broker and `resolve_delegate_locus` fails closed on UNKNOWN by design, the omission made the gate
      refuse EVERY dispatched run on the SASL-enabled `.201` dev lane, reported in the language of the
      lane rather than of the missing credential. Measured 2026-09-09 against that lane: unauthenticated
      raised `_TRANSPORT ... Failed to get metadata`; the same call with credentials returned 1659 topics,
      762 consumer groups, and the one STABLE orchestrator group bound to the delegate command topic.
      This is the change that makes the AC4 in-lane locus readback obtainable at all. Bound by hand (union-resolve)
      because autobind mints no companion in this window (OMN-18073).
    source: "generated"
    binds_ac:
      - "AC4"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3366 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3366/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_infra/backends/backend_probe.py'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3366/files --paginate --jq '.[] | select(.filename == "tests/unit/event_bus/test_omn18012_confluent_admin_transport.py") | .patch' | grep -c 'test_locus_gate_liveness_admin_client_carries_the_sasl_entries'
