---
schema_version: "1.0.0"
ticket_id: "OMN-10963"
title: "Fix unused config params and logging in delegation plugin (SonarCloud S1172 + S8572)"
summary: >-
  Fixes two SonarCloud code-smell findings in omnimarket's delegation plugin. OMN-10963 (S1172): Rename
  4 unused config params to _config in protocol-required method signatures (should_activate, initialize,
  validate_handshake, shutdown). OMN-10964 (S8572): Replace 3 logger.error() calls in except blocks with
  logger.exception() in wire_handlers, wire_dispatchers, start_consumers. Pure code-smell cleanup - no
  behavior change, no new runtime paths, no deploy required.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnimarket PR #676 is bound to OMN-10963 at observed head."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10963/dod-omnimarket-pr-676/command.yaml"
  - kind: "manual"
    description: "No deploy required; pure code-smell cleanup."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10963/dod-deploy/command.yaml"
  - kind: "manual"
    description: "OCC PR carries OMN-10963 evidence."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10963/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-676"
    description: "Bind omnimarket PR #676 to OMN-10963 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10963/dod-omnimarket-pr-676/command.yaml\""
  - id: "dod-deploy"
    description: "No live deploy or runtime restart required; pure code-smell cleanup with no behavior
      change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10963/dod-deploy/command.yaml\""
  - id: "dod-occ-self"
    description: "Self-binding receipt for OCC PR carrying OMN-10963 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10963/dod-occ-self/command.yaml\""
