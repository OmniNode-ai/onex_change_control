---
schema_version: "1.0.0"
ticket_id: "OMN-12178"
title: "Test worst-health files in omnibase_core"
summary: >
  Central OCC evidence for omnibase_core PR #1166. The core change adds regression coverage for NodeReducer
  snapshot deep-copy behavior and receipt-gate internal guard behavior without changing runtime contracts.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused tests for the newly added NodeReducer snapshot and receipt-gate guard coverage
      pass."
    command: "See drift/dod_receipts/OMN-12178/dod-focused-tests/command.yaml"
  - kind: "tests"
    description: "The broader omnibase_core unit node suite passes with the new regression tests included."
    command: "See drift/dod_receipts/OMN-12178/dod-nodes-suite/command.yaml"
  - kind: "ci"
    description: "Ruff check passes for the new omnibase_core test files."
    command: "See drift/dod_receipts/OMN-12178/dod-ruff/command.yaml"
  - kind: "ci"
    description: "Deploy-gate posture is recorded for omnibase_core PR #1166."
    command: "See drift/dod_receipts/OMN-12178/dod-deploy-gate/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR #1672 carries the central OMN-12178 contract and receipts."
    command: "See drift/dod_receipts/OMN-12178/dod-occ-pr-1672/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused tests passed for the new OMN-12178 coverage files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12178/dod-focused-tests/command.yaml
          && grep -q '69 passed' drift/dod_receipts/OMN-12178/dod-focused-tests/command.yaml"
  - id: "dod-nodes-suite"
    description: "The omnibase_core unit node suite passed after rebasing PR #1166 onto current dev."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12178/dod-nodes-suite/command.yaml
          && grep -q '421 passed' drift/dod_receipts/OMN-12178/dod-nodes-suite/command.yaml"
  - id: "dod-ruff"
    description: "Ruff check passed for the new OMN-12178 test files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12178/dod-ruff/command.yaml && grep
          -q 'All checks passed' drift/dod_receipts/OMN-12178/dod-ruff/command.yaml"
  - id: "dod-deploy-gate"
    description: "Deploy Gate for PR #1166 passed; no live pre-merge deploy was required for this test-only
      PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12178/dod-deploy-gate/command.yaml
          && grep -q 'deploy-gate / deploy-gate' drift/dod_receipts/OMN-12178/dod-deploy-gate/command.yaml"
  - id: "dod-occ-pr-1672"
    description: "onex_change_control PR #1672 carries the central OMN-12178 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12178/dod-occ-pr-1672/command.yaml
          && grep -q '^pr_number: 1672$' drift/dod_receipts/OMN-12178/dod-occ-pr-1672/command.yaml"
