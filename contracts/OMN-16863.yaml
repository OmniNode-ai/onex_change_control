---
schema_version: "1.0.0"
ticket_id: "OMN-16863"
title: "Comment-only correction for the credential-query consumer state"
summary: "Central evidence contract for omninode_infra PR #1156."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1156 contains only the scoped credential-query docstring correction."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,files"
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1156"
    description: "PR #1156 on OmniNode-ai/omninode_infra — comment-only credential-query correction."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omninode_infra/contents/docker/onex-api/inference_credentials_query.py?ref=d10c4c8d'
          --jq .content | base64 --decode | grep -F 'consumer is deployed on staging'"
