---
schema_version: "1.0.0"
ticket_id: "OMN-11724"
title: "Update pull-all.sh for dual-branch pull with dirty guard"
summary: >-
  Updates omnibase_infra/scripts/pull-all.sh for the dev/main transition so canonical repos fetch and
  fast-forward both main and dev, refuse dirty worktrees before branch switching, and leave processed
  repos on dev.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "tests"
    description: "Focused pull-all unit tests and script checks pass."
    command: "See drift/dod_receipts/OMN-11724/dod-omnibase-infra-pr-1717/command.yaml"
  - kind: "manual"
    description: "No live deploy is required for workspace pull script behavior."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11724/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1717"
    description: "omnibase_infra PR #1717 carries dual-branch pull-all.sh behavior and focused tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11724/dod-omnibase-infra-pr-1717/command.yaml"
          && grep -q '^pr_number: 1717$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11724/dod-omnibase-infra-pr-1717/command.yaml"
          && grep -q '^commit_sha: "c6390ab21ea650dd9bd8c5eed3a1a477c8450bb6"' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11724/dod-omnibase-infra-pr-1717/command.yaml"
  - id: "dod-deploy"
    description: "No live deploy required; this changes workspace pull automation only."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11724/dod-deploy/command.yaml"
