---
schema_version: "1.0.0"
ticket_id: "OMN-12723"
title: "Bootstrap runtime volume ownership"
summary: >
  Central OCC evidence for omnibase_infra PR #1876, which starts the runtime
  image with a bounded root bootstrap phase that repairs fresh Docker named
  volume ownership for /app/data, /app/data/delegation, /app/logs, and /app/tmp,
  then re-execs the entrypoint as omniinfra with gosu before schema
  fingerprinting, Bifrost rendering, or runtime launch.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1876"
    description: "omnibase_infra PR #1876 carries the runtime fresh-volume ownership bootstrap fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12723/dod-omnibase-infra-pr-1876/command.yaml
          && grep -q '^pr_number: 1876$' drift/dod_receipts/OMN-12723/dod-omnibase-infra-pr-1876/command.yaml"
  - id: "dod-local-validation"
    description: "Focused entrypoint syntax, contract validation, ruff, and Docker bootstrap unit tests passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12723/dod-local-validation/command.yaml
          && grep -q '39 passed' drift/dod_receipts/OMN-12723/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Fresh-volume ownership bootstrap requires the next runtime image build and post-merge dev deploy validation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12723/dod-deploy-assessment/command.yaml
          && grep -q 'post-merge dev deploy' drift/dod_receipts/OMN-12723/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2211"
    description: "onex_change_control PR #2211 carries central deploy-gate evidence for runtime volume bootstrap."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12723/dod-occ-pr-2211/command.yaml
          && grep -q '^pr_number: 2211$' drift/dod_receipts/OMN-12723/dod-occ-pr-2211/command.yaml"
