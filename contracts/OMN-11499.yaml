---
schema_version: "1.0.0"
ticket_id: "OMN-11499"
title: "Update agent definition with 4-tool system prompt"
summary: >-
  onex-self-extending-agent PR #83 updates the ADK agent definition to include register_generated_tool
  and invoke_generated_tool in its tool list and updates the system prompt to describe the 4-tool self-extension
  capability.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: tests
    description: "Unit tests pass with 4-tool agent definition."
    command: "uv run pytest tests/unit -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-83"
    description: "onex-self-extending-agent PR #83 carries the 4-tool agent definition."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11499/dod-product-pr-83/command.yaml"
          && grep -q '^pr_number: 83$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11499/dod-product-pr-83/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11499 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11499/dod-occ-self/command.yaml"
