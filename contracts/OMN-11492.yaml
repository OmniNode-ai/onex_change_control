---
schema_version: "1.0.0"
ticket_id: "OMN-11492"
title: "Fix stale onex-self-extending-agent README project structure tree"
summary: >
  onex-self-extending-agent PR #242 removes the stale README Project Structure reference to src/deploy
  after that directory was deleted by OMN-13618, and adds a regression test that asserts concrete README
  tree paths exist on disk.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "onex-self-extending-agent PR #242 is open at the recorded head with README/test changes."
    command: "gh pr view 242 --repo OmniNode-ai/onex-self-extending-agent --json headRefOid,files,body"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-242-readme-project-structure"
    description: >
      Binds onex-self-extending-agent PR #242 at head 50ec692791f4c97f701d4c57d5d9a153b439bf71. The PR
      changes README.md and tests/unit/test_readme_project_structure.py, and its PR body records RED/GREEN
      evidence for the stale src/deploy README tree entry.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 242 --repo OmniNode-ai/onex-self-extending-agent --json headRefOid,files,body --jq
          '.headRefOid == "50ec692791f4c97f701d4c57d5d9a153b439bf71" and ([.files[].path] | index("README.md")
          != null and index("tests/unit/test_readme_project_structure.py") != null) and (.body | contains("test_project_structure_paths_exist_on_disk")
          and contains("src/deploy") and contains("1214 passed, 1 skipped"))' | grep -qx true
  - id: "occ-self-bind-pr-4543"
    description: >
      Self-binding OCC eligibility receipt for onex_change_control PR #4543, which carries the OMN-11492
      contract and SEA #242 evidence receipt onto dev.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 4543 --repo OmniNode-ai/onex_change_control --json headRefOid,state,baseRefName --jq
          '.state == "OPEN" and .baseRefName == "dev" and .headRefOid == "05102fbe713d20b5f417b462a27571b0e36a44e9"'
          | grep -qx true
