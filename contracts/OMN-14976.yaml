---
schema_version: "1.0.0"
ticket_id: "OMN-14976"
title: "Autobind OCC evidence for OMN-14976"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1873."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1873 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1873"
    description: "PR #1873 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1873-ci"
    description: "PR #1873 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4712"
    description: "OCC companion PR #4712 — self-bind for OMN-14976 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omn14976-pr1873-deploy"
    description: "PR #1873 deploy disposition — omnimarket acceptor half carries explicit post-merge dev
      deploy validation bar."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1873 --repo OmniNode-ai/omnimarket --json body,files --jq 'if ((.body
          | test(\"Post-merge dev deploy validation\")) and (.body | test(\"gateway-side advertisement
          leg\")) and any(.files[].path; . == \"src/omnimarket/nodes/node_push_validation_effect/contract.yaml\")
          and any(.files[].path; . == \"src/omnimarket/nodes/node_push_validation_effect/handlers/handler_push_validation_effect.py\")
          and any(.files[].path; . == \"src/omnimarket/nodes/node_push_validation_effect/models/model_push_validation_request.py\"))
          then \"deploy disposition verified for OMN-14976 omnimarket acceptor half\" else error(\"OMN-14976
          deploy disposition missing PR body bar or runtime paths\") end'"
  - id: "occ-self-bind-pr-4715-omn14976"
    description: "OCC companion PR #4715 — self-bind for OMN-14976 deploy evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4715 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "dod-OmniNode-ai-omninode_infra-pr-640"
    description: "PR #640 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14976/dod-OmniNode-ai-omninode_infra-pr-640/command.yaml"
  - id: "dod-OmniNode-ai-omninode_infra-pr-640-ci"
    description: "PR #640 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14976/dod-OmniNode-ai-omninode_infra-pr-640-ci/command.yaml"
  - id: "occ-self-bind-pr-4724"
    description: "OCC companion PR #4724 — self-bind for OMN-14976 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
