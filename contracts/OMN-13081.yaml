---
schema_version: '1.0.0'
ticket_id: OMN-13081
title: 'feat(omnimarket): projection contract for receipt-gate (NC-08)'
summary: >-
  Creates node_projection_receipt_gate — a pure reducer that projects OCC/DoD receipt-gate outcomes (verification-receipt-completed.v1
  and evidence-validated.v1 events) into the onex.snapshot.projection.receipt-gate.v1 snapshot consumed
  by the omnidash receipt-gate widget (NC-08). Includes contract.yaml, handler, pure reducer, migration,
  focused reducer tests, and golden-chain coverage. Entry-point registered in pyproject.toml. Focused
  pytest, ruff, mypy --strict, golden-chain coverage, projection guards, and pre-commit pass locally.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >-
      Focused tests for the new node pass (reducer event dispatch, state accumulation, contract structure,
      golden-chain coverage, handler round-trip, dogfood registration, and projection guards).
    command: >-
      uv run pytest tests/nodes/node_projection_receipt_gate src/omnimarket/nodes/node_projection_receipt_gate/tests
      tests/test_market_node_runtime_dogfood.py tests/unit/projection/test_projection_table_migration_coverage.py
      tests/unit/projection/test_projection_materialization_validation.py tests/test_projection_api_server.py
      -q
  - kind: ci
    description: CI pipeline green on omnimarket PR
    command: 'env -u GITHUB_TOKEN gh pr checks --repo OmniNode-ai/omnimarket'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >-
      node_projection_receipt_gate directory present in src/omnimarket/nodes/ with contract.yaml (node_type:
      reducer), handler, pure reducer, models, migrations, and tests.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13081/dod-001/command.yaml"
  - id: dod-002
    description: >-
      contract.yaml declares node_type: reducer, subscribes to onex.evt.omnimarket.verification-receipt-completed.v1
      and onex.evt.omnimarket.evidence-validated.v1, and exposes onex.snapshot.projection.receipt-gate.v1
      as externally_consumed_topic.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13081/dod-002/command.yaml"
  - id: dod-003
    description: >-
      Focused reducer tests pass for contract structure, event dispatch, state accumulation, handler round-trip,
      golden-chain coverage, and dogfood inventory updated to the current dev count.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13081/dod-003/command.yaml"
  - id: dod-004
    description: >-
      ruff format + check, mypy --strict, and pre-commit --all-files all pass with no errors on the new
      node files.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13081/dod-004/command.yaml"
  - id: dod-deploy-assessment
    description: >-
      Static deploy assessment: no live deploy, runtime restart, Kafka mutation, database mutation, .201
      mutation, or cluster mutation was performed by OMN-13081. The node_projection_receipt_gate reducer
      node and receipt_gate_rows table DDL are dev-lane changes; runtime deployment follows the standard
      post-merge deploy process.
    source: generated
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13081/dod-deploy-assessment/command.yaml &&
          grep -q 'No live deploy' drift/dod_receipts/OMN-13081/dod-deploy-assessment/command.yaml
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt — this onex_change_control PR carries the OMN-13081 contract consumed by
      the omnimarket PR via Evidence-Source OCC SHA.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13081/dod-occ-self/command.yaml"
