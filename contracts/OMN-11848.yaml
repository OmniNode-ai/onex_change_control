---
schema_version: "1.0.0"
ticket_id: "OMN-11848"
title: "Workflow Projection Migration (021_workflow_results.sql)"
summary: >
  Creates the workflow_results projection table in omninode_infra (migration 021).
  Reducer-owned, append-driven, replayable. DT-003 freshness SLA (30s) enforced via
  projection_cursor. No runtime code — SQL migration only.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-454"
    description: "omninode_infra PR #454 adds 021_workflow_results.sql migration and rollback"
    source: "manual"
    checks:
      - check_type: "file_exists"
        check_value: "db/migrations/021_workflow_results.sql"
