---
schema_version: "1.0.0"
ticket_id: "OMN-14766"
title: "Autobind OCC evidence for OMN-14766"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1807."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1807 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1807"
    description: "PR #1807 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1807-ci"
    description: "PR #1807 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4374"
    description: "OCC companion PR #4374 — self-bind for OMN-14766 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-1807-deploy"
    description: "Deploy-readiness assessment for OMN-14766. The omnimarket PR touches runtime OCC emitter
      and evidence stamp paths; this evidence lane performed no live deploy, restart, .201, or production
      mutation."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14766/dod-omnimarket-1807-deploy/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-14766/dod-omnimarket-1807-deploy/command.yaml"
  - id: "occ-self-bind-pr-4378-14766"
    description: "OCC companion PR #4378 self-binding proof for OMN-14766 deploy evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
