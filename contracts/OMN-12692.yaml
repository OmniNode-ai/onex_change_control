---
schema_version: "1.0.0"
ticket_id: "OMN-12692"
title: "Workstream I-D: golden-chain registry parity"
summary: >-
  Central evidence that omniclaude PR #1732 reconciles the live golden-chain registry with omnimarket's
  canonical golden_chains.yaml metadata. omniclaude now exposes typed metadata for every canonical chain,
  preserves the explicit five-chain runnable subset, aligns the evaluation topic, and enforces cross-repo
  registry parity in the Golden Chain Integrity CI gate.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-golden-chain-registry-parity"
    description: "Focused omniclaude tests, type checks, pre-commit, and parity validation prove the registry
      reconciliation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12692/dod-omniclaude-golden-chain-registry-parity/command.yaml"
          && grep -q '^pr_number: 1732$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12692/dod-omniclaude-golden-chain-registry-parity/command.yaml"
  - id: "dod-deploy-gate-no-runtime-mutation"
    description: "Deploy-gate evidence for omniclaude registry metadata, validator, CI, tests, and contract
      changes in PR #1732."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12692/dod-deploy-gate-no-runtime-mutation/command.yaml"
          && grep -q '^pr_number: 1732$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12692/dod-deploy-gate-no-runtime-mutation/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-12692 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12692/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2178$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12692/dod-occ-pr/command.yaml"
