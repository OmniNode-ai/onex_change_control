---
schema_version: "1.0.0"
ticket_id: OMN-10166
title: "Add module CLI for node_pr_lifecycle_orchestrator so merge_sweep can dispatch the contract-canonical
  start envelope"
summary: "Add module CLI for node_pr_lifecycle_orchestrator so merge_sweep can dispatch the contract-canonical
  start envelope"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR checks pass, including deploy gate"
    command: "gh pr checks 431 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "PR lifecycle orchestrator CLI test and golden-chain tests pass"
    command: "uv run pytest tests/unit/nodes/node_pr_lifecycle_orchestrator/test_main_cli.py tests/test_golden_chain_pr_lifecycle_orchestrator.py
      -q"
  - kind: "manual"
    description: "Post-merge deploy smoke imports the module CLI in the deployed runtime container"
    command: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_pr_lifecycle_orchestrator.__main__
      import main; print('pr lifecycle cli deploy smoke ok')\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "CLI unit test and PR lifecycle golden-chain tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_pr_lifecycle_orchestrator/test_main_cli.py tests/test_golden_chain_pr_lifecycle_orchestrator.py
          -q"
  - id: "dod-002"
    description: "Focused lint, format, and pre-commit checks pass for the CLI files"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff format --check src/omnimarket/nodes/node_pr_lifecycle_orchestrator/__main__.py
          tests/unit/nodes/node_pr_lifecycle_orchestrator/test_main_cli.py && uv run ruff check src/omnimarket/nodes/node_pr_lifecycle_orchestrator/__main__.py
          tests/unit/nodes/node_pr_lifecycle_orchestrator/test_main_cli.py && OMNIBASE_INFRA_PATH=${OMNI_HOME}/omnibase_infra
          pre-commit run --files src/omnimarket/nodes/node_pr_lifecycle_orchestrator/__main__.py tests/unit/nodes/node_pr_lifecycle_orchestrator/test_main_cli.py"
  - id: "dod-003"
    description: "Post-merge deploy smoke imports node_pr_lifecycle_orchestrator module CLI in the deployed
      runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_pr_lifecycle_orchestrator.__main__
          import main; print('pr lifecycle cli deploy smoke ok')\""
