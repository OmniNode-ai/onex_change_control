---
schema_version: "1.0.0"
ticket_id: "OMN-10790"
title: "feat(OMN-10790): add tokens_to_compliance + compliance_attempts to delegation models"
summary: >
  Adds tokens_to_compliance (default 0) and compliance_attempts (default 1) fields to ModelDelegationResult
  and ModelTaskDelegatedEvent. Both models are frozen=True, extra=forbid. New fields have defaults so
  existing callers and Kafka consumers are unaffected. 8 unit tests + 5 integration tests covering defaults,
  explicit values, wire-format roundtrip, and backward compatibility.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "8 unit tests pass covering defaults, explicit values, serialization roundtrip, extra=forbid
      rejection."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10790/dod-unit-tests/command.yaml\""
  - id: "dod-integration-tests"
    description: "5 integration tests pass covering cross-model field mapping, Kafka wire-format roundtrip,
      backward compatibility with legacy payloads, and full pipeline roundtrip."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10790/dod-integration-tests/command.yaml\""
  - id: "dod-deploy-compat"
    description: "Models deploy backward-compatible: existing Kafka payloads without compliance fields
      deserialize with correct defaults (0 tokens, 1 attempt). No runtime restart required."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10790/dod-deploy-compat/command.yaml\""
