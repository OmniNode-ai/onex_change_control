---
schema_version: "1.0.0"
ticket_id: "OMN-12549"
title: "MixinNodeDispatch seam — omnibase_core 0.46.3 dev->main promotion"
summary: >-
  Binds OCC evidence for omnibase_core promotion PR #1376 (promotion/omn-12549-core-dev-to-main, base=main,
  v0.46.3). The PR promotes origin/dev (@022bb1c8, already CI-green) to main, carrying the MixinNodeDispatch
  seam (src/omnibase_core/runtime/mixin_node_dispatch.py) which relocates node dispatch into a reusable
  runtime mixin; the dispatch-parity test flips to Mode B. Also carries the dep-provenance gate (OMN-13877),
  cursor-hook enums/models, and run-node bootstrap determinism (OMN-13857). Version bumped 0.46.2->0.46.3
  past the burned 0.46.2 (promoted to main 2026-07-01 but never tagged/published due to auto-tag->release.yml
  anti-recursion; PyPI still at 0.46.1). The promotion branch is byte-identical to origin/dev except the
  pyproject.toml / uv.lock version bump (main is strict:false, so no main-merge is required).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-self"
    description: >-
      onex_change_control PR (jonah/omn-12549-occ-core-promotion-evidence, base=dev) carries this OMN-12549
      contract plus the PASS receipt binding omnibase_core promotion PR #1376. This self-binding receipt
      carries the OCC PR number so the OCC PR's own occ-preflight eligibility binds on pr_number.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12549/dod-occ-self/command.yaml
  - id: "dod-core-pr-1376"
    description: >-
      omnibase_core PR #1376 (promotion/omn-12549-core-dev-to-main, base=main) promotes origin/dev to
      main as omnibase_core v0.46.3, carrying the MixinNodeDispatch seam. This PASS receipt binds OMN-12549
      to core PR #1376 (pr_number + commit_sha) so the verify/verify OCC merge-eligibility gate finds
      a PASS receipt bound to the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12549/dod-core-pr-1376/command.yaml
  - id: "dod-infra-pr-2189"
    description: >-
      omnibase_infra PR #2189 (jonah/omn-12549-infra-repin, base=dev) removes the temporary
      omnibase-core/omnibase-spi git-rev overrides (and their raw-override-ok:OMN-12549 tokens) from
      pyproject.toml [tool.uv.sources], repins omnibase-core==0.46.3 / omnibase-spi==0.23.1 (the released
      MixinNodeDispatch seam + ProtocolDispatchEngine relocation), and re-locks. It also regenerates the
      three release-sensitive artifacts (dispatch-parity baseline provenance header, runner-image identity
      lock, and the _FALLBACK_MATRIX version-compatibility min-versions). This PASS receipt binds OMN-12549
      to infra PR #2189 (pr_number + commit_sha) so the citing PR's verify/verify receipt gate finds a
      PASS receipt for the reproducible-build closure step of the release train.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12549/dod-infra-pr-2189/command.yaml
  - id: "dod-infra-deploy-assessment"
    description: >-
      Deploy assessment for infra PR #2189's runtime-path touch. The only runtime-path change is
      src/omnibase_infra/runtime/version_compatibility.py, whose _FALLBACK_MATRIX carries only a
      compatibility-floor min-version data bump (omnibase-core 0.46.3 / omnibase-spi 0.23.1) with no
      behavioral runtime delta — it is regenerated deterministically from pyproject by
      scripts/update_version_matrix.py. The deployable surface (the omnibase_infra runtime image
      carrying the repinned 0.46.3/0.23.1 dependency closure) is deployed only through the gated
      redeploy lane (node_redeploy_orchestrator, OMN-13418 prod-promotion gate); this PR performs no
      live deploy, restart, or prod mutation. Satisfies the Wave-D deploy-gate (OMN-8912) runtime-change
      deploy-evidence requirement.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'FALLBACK_MATRIX' src/omnibase_infra/runtime/version_compatibility.py  # deploy-assessment: compatibility-floor data only; image deploy routed via gated R3 redeploy lane
