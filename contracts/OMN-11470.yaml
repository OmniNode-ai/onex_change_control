---
schema_version: "1.0.0"
ticket_id: "OMN-11470"
title: "feat(OMN-11470): projection models — Dashboard, CorrelationTrace, ReadinessAggregate"
summary: "Wave 1 omnibase_compat projection model evidence for dashboard, correlation trace, and readiness
  aggregate DTOs."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "omnibase_compat PR #99 is open against main at the recorded head SHA."
    command: "gh pr view 99 --repo OmniNode-ai/omnibase_compat --json headRefOid,state,url,headRefName,baseRefName"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-99-open"
    description: "omnibase_compat PR #99 carries OMN-11470 projection model work at the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11470/dod-pr-99-open/command.yaml\""
