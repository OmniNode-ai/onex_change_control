---
schema_version: "1.0.0"
ticket_id: OMN-9799
title: "Wire persona rebuild handler"
summary: "Wire persona lifecycle orchestrator exports and handler routing, add HandlerPersonaRebuild for
  on-demand and tick rebuild flows, and bind omnimarket PR #712 to central receipt evidence."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
  - envelopes
evidence_requirements:
  - kind: tests
    description: "Persona lifecycle rebuild handler tests pass."
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-9799/omnimarket-712-polish
      && uv run pytest tests/unit/nodes/node_persona_lifecycle_orchestrator/test_handler_persona_rebuild.py
      -q"
  - kind: manual
    description: "Touched persona lifecycle node files pass ruff."
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-9799/omnimarket-712-polish
      && uv run ruff check src/omnimarket/nodes/node_persona_lifecycle_orchestrator tests/unit/nodes/node_persona_lifecycle_orchestrator"
  - kind: ci
    description: "omnimarket#712 carries the OMN-9799 persona rebuild handler wiring."
    command: "gh pr checks 712 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-local-validation
    description: "Focused persona lifecycle ruff, pytest, and diff checks pass locally."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9799/dod-local-validation/command.yaml
          && grep -q '^ticket_id: OMN-9799$' drift/dod_receipts/OMN-9799/dod-local-validation/command.yaml"
  - id: dod-omnimarket-pr-712
    description: "omnimarket PR #712 is bound to OMN-9799 for persona rebuild handler verification."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9799/dod-omnimarket-pr-712/command.yaml
          && grep -q '^ticket_id: OMN-9799$' drift/dod_receipts/OMN-9799/dod-omnimarket-pr-712/command.yaml
          && grep -q '^pr_number: 712$' drift/dod_receipts/OMN-9799/dod-omnimarket-pr-712/command.yaml"
