---
schema_version: "1.0.0"
ticket_id: "OMN-10971"
title: "Harden skip-token-allowed escape hatch with central registry validation"
summary: "Updates the skip_token_approvals.yaml schema for GHA registry validation and adds central registry
  validation to the omniclaude reject-deploy-gate-skip workflow so the escape hatch is no longer free-text."
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "OCC schema update merges cleanly; omniclaude workflow validates against registry."
    command: "test -f allowlists/skip_token_approvals.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carries the OMN-10971 contract and schema update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10971/dod-occ-pr-self/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10971/dod-occ-pr-self/command.yaml"
  - id: "dod-omniclaude-pr-1586"
    description: "omniclaude PR #1586 adds central registry validation to the escape hatch workflow."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10971/dod-omniclaude-pr-1586/command.yaml
          && grep -q '^pr_number: 1586$' drift/dod_receipts/OMN-10971/dod-omniclaude-pr-1586/command.yaml"
