---
schema_version: "1.0.0"
ticket_id: "OMN-12932"
title: "OCC evidence: OMN-12898 node-based projection readiness preflight"
summary: >-
  Bind OMN-12898's omnimarket node_projection_query readiness preflight to
  central OCC evidence so downstream Receipt Gate can verify product PR #1149.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "protocols"
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1149"
    description: >-
      omnimarket PR #1149 carries the node_projection_query implementation for
      projection-topic-readiness and targets the normal dev branch.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12932/dod-omnimarket-pr-1149/command.yaml"
  - id: "dod-focused-node-tests"
    description: >-
      Focused golden-chain tests and ruff checks pass for the node-owned
      projection query readiness behavior.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12932/dod-focused-node-tests/command.yaml"
  - id: "dod-node-owned-boundary"
    description: >-
      Projection readiness acceptance is implemented in the market node handler
      and calls the projection discovery/database adapter directly; HTTP remains
      only an edge transport for dashboard compatibility.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12932/dod-node-owned-boundary/command.yaml"
  - id: "dod-no-runtime-mutation"
    description: >-
      The product and OCC work did not start containers locally or mutate the
      .201 stability runtime; live runtime changes remain gated by explicit
      approval.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12932/dod-no-runtime-mutation/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding receipt for the OCC PR that carries the OMN-12932 central
      contract and DoD receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12932/dod-occ-pr-self/command.yaml"
