---
schema_version: '1.0.0'
ticket_id: OMN-13725
title: 'Lift generate_deep_dive.py pure + scan logic into importable omnibase_infra.deep_dive module'
summary: >-
  Extract the portable, deterministic deep-dive logic (PR categorisation, drift score, velocity/effectiveness
  V2 scoring, family-dedup, runtime-state parsing) plus the git/gh scan helpers out of the standalone
  scripts/generate_deep_dive.py script into an importable package src/omnibase_infra/deep_dive/. The script
  now imports from the module so there is one source of truth (no copy-paste drift). node_deep_dive_report_effect
  (omnimarket) imports the same module via its injected ProtocolReportDataSource adapter. Script output
  is byte-identical (modulo the volatile runtime-state timestamp line).
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: tests
    description: PASS receipts bind the omnibase_infra deep-dive extraction evidence for OMN-13725.
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13725/dod-001/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-13725/dod-002/command.yaml && grep -q '^status: PASS$' drift/dod_receipts/OMN-13725/dod-003/command.yaml
      && grep -q '^status: PASS$' drift/dod_receipts/OMN-13725/dod-occ-self/command.yaml"
  - kind: ci
    description: CI pipeline green on PR
    command: gh pr checks <pr> --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: New shared module imports cleanly and exposes the pure + scan public surface
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13725/dod-001/command.yaml"
  - id: dod-002
    description: Migrated script + new module unit tests pass (pure fns now sourced from the module)
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13725/dod-002/command.yaml"
  - id: dod-003
    description: Script output is identical to the pre-extraction script (one source of truth, no logic
      fork)
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13725/dod-003/command.yaml"
  - id: dod-occ-self
    description: "Self-binding OCC receipt: this onex_change_control PR carries the OMN-13725 contract
      and receipts."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13725/dod-occ-self/command.yaml"
