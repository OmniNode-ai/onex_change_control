---
schema_version: "1.0.0"
ticket_id: "OMN-11503"
title: "Wire rich display into --agent and --progressive paths"
summary: >-
  onex-self-extending-agent PR #85 wires the rich terminal display module into the --agent and --progressive
  execution paths so all runs produce structured, colorized console output with progress panels and result
  summaries.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: tests
    description: "Unit tests pass with rich display wiring."
    command: "uv run pytest tests/unit -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-85"
    description: "onex-self-extending-agent PR #85 carries the rich display wiring."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11503/dod-product-pr-85/command.yaml"
          && grep -q '^pr_number: 85$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11503/dod-product-pr-85/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11503 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11503/dod-occ-self/command.yaml"
