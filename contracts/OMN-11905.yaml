---
schema_version: "1.0.0"
ticket_id: "OMN-11905"
title: "Wave 5 sweeps and verification declarative contract migration"
summary: >
  Central OCC evidence for omnimarket PR #802, which migrates sweep, verification, chain, and registry
  nodes to canonical declarative handler routing and includes the contract prose lint fix required by
  the architectural gate.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-802"
    description: "Bind OMN-11905 evidence to omnimarket PR #802."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11905/dod-omnimarket-pr-802/command.yaml
          && grep -q '^pr_number: 802$' drift/dod_receipts/OMN-11905/dod-omnimarket-pr-802/command.yaml"
  - id: "dod-local-validation"
    description: "PR-local validation and architectural lint trigger check passed for the Wave 5 migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11905/dod-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: declarative contract routing changes require no live runtime deploy
      before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11905/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11905/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11905 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11905/dod-occ-pr/command.yaml"
