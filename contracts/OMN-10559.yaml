---
schema_version: '1.0.0'
ticket_id: OMN-10559
title: 'Task 13: FakeSecretStore for tests'
summary: 'Deterministic in-memory ProtocolSecretStore for the canonical conftest'
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: 13 unit tests cover protocol conformance, copy-not-reference initial data, set/delete
      round-trips, sorted prefix list, health-after-close
    command: uv run pytest tests/unit/test_utils/test_fake_secret_store.py -v
  - kind: ci
    description: omnibase_infra PR 1508 CI green
    command: gh pr checks 1508 --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: New FakeSecretStore module present in PR 1508 diff
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 1508 --repo OmniNode-ai/omnibase_infra | grep -q 'src/omnibase_infra/test_utils/fake_secret_store.py'
  - id: dod-002
    description: Constructor accepts optional initial Mapping[str, str] and copies (not references) the
      source
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 1508 --repo OmniNode-ai/omnibase_infra | grep -q 'dict(initial)'
  - id: dod-003
    description: All seven ProtocolSecretStore methods implemented
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 1508 --repo OmniNode-ai/omnibase_infra | grep -E 'async def (get_secret|set_secret|delete_secret|list_keys|health_check|close)'
          | wc -l | grep -q '6'
  - id: dod-004
    description: omnibase_infra PR 1508 has CI passing
    source: manual
    checks:
      - check_type: command
        check_value: gh pr checks 1508 --repo OmniNode-ai/omnibase_infra | grep -c 'pass' | grep -qv '^0$'
  - id: dod-005
    description: PR 1508 open against main branch
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 1508 --repo OmniNode-ai/omnibase_infra --json state,baseRefName --jq 'select(.state=="OPEN"
          and .baseRefName=="main") | "ok"' | grep -q ok
