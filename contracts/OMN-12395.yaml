---
schema_version: "1.0.0"
ticket_id: "OMN-12395"
title: "Implement native evidence/readiness pipeline market nodes"
summary: >
  Replace node_not_implemented evidence/readiness pipeline contracts in omnimarket with native typed Onex
  node handlers. The local deterministic chain covers collection, extraction, deterministic contract matching,
  advisory publish/update effects, gap analysis, readiness scoring/gating, and reducer projection materialization
  for OMN-12395.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused evidence/readiness contract, native chain, and gap compute regression tests
      pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12395/dod-focused-tests/command.yaml" && grep
          -q '17 passed' "drift/dod_receipts/OMN-12395/dod-focused-tests/command.yaml"
  - id: "dod-node-scan"
    description: "Deterministic node scan reports zero node_not_implemented findings for the scoped nodes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12395/dod-node-scan/command.yaml" && grep -q
          'scoped_node_not_implemented_findings: 0' "drift/dod_receipts/OMN-12395/dod-node-scan/command.yaml"
  - id: "dod-gap-compile"
    description: "gap_compute runtime adapter compile-only proof accepts the OMN-12395 scan request."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12395/dod-gap-compile/command.yaml" && grep
          -q 'runtime_mode: compiled' "drift/dod_receipts/OMN-12395/dod-gap-compile/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: native omnimarket node handlers are locally validated and require
      no live runtime deployment before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12395/dod-deploy-readiness/command.yaml" &&
          grep -qi 'deploy' "drift/dod_receipts/OMN-12395/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR #1813 carries the central OMN-12395 contract and receipt evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12395/dod-occ-pr/command.yaml" && grep -q '^pr_number:
          1813$' "drift/dod_receipts/OMN-12395/dod-occ-pr/command.yaml"
  - id: "dod-ci-cleanup-pr"
    description: "Follow-up PR #967 binds OMN-12395 CI metadata and strict typing cleanup evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12395/dod-ci-cleanup-pr/command.yaml" && grep
          -q '^pr_number: 967$' "drift/dod_receipts/OMN-12395/dod-ci-cleanup-pr/command.yaml"
  - id: "dod-receipt-schema-fix-pr"
    description: "OCC PR #1819 removes the unsupported base_branch field from the cleanup receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12395/dod-receipt-schema-fix-pr/command.yaml"
          && grep -q '^pr_number: 1819$' "drift/dod_receipts/OMN-12395/dod-receipt-schema-fix-pr/command.yaml"
