---
schema_version: "1.0.0"
ticket_id: "OMN-18026"
title: "Autobind OCC evidence for OMN-18026"
summary: >
  OCC evidence for OMN-18026, preserving the existing omnimarket PR #2375 and omnibase_infra PR #3275
  Evidence-Source autobinds, and adding the omninode_infra PR #1220 companion evidence.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2375 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2375 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: |-
      uv run pytest src/omnimarket/nodes/node_adr_canary_orchestrator/tests/test_handler_canary_orchestrator.py tests/integration/nodes/node_adr_canary_orchestrator/test_proof_of_life.py tests/test_adr_discovery_manifest_load.py tests/test_omn18026_restricted_corpus_absent.py -q
  - kind: "ci"
    description: "PR #3275 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #3275 on OmniNode-ai/omnibase_infra changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (.github/workflows/contractor-integration-note.yml) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
  - kind: "ci"
    description: "PR #1220 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #1220 on OmniNode-ai/omninode_infra changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (config/contractor_roster.yaml) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
  - kind: "ci"
    description: "PR #2426 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2426 on OmniNode-ai/omnimarket, derived from the PR's own diff: the two test target(s) it changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof-pr-2426`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: |-
      uv run pytest tests/unit/nodes/node_contractor_integration_note_effect/test_contract.py tests/nodes/node_contractor_integration_note_effect/test_golden_chain_contractor_integration_note_effect.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2375"
    description: "PR #2375 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_adr_canary_orchestrator/tests/test_handler_canary_orchestrator.py?ref=95fadedfb1deeba832b83841dee2da054b57b487 --jq '.content' | base64 -d | grep -c 'def test_manifest_path_is_required_with_no_default'
  - id: "dod-OmniNode-ai-omnimarket-pr-2375-ci"
    description: "PR #2375 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2375 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2375 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest src/omnimarket/nodes/node_adr_canary_orchestrator/tests/test_handler_canary_orchestrator.py tests/integration/nodes/node_adr_canary_orchestrator/test_proof_of_life.py tests/test_adr_discovery_manifest_load.py tests/test_omn18026_restricted_corpus_absent.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-8548"
    description: "OCC companion PR #8548 — self-bind for OMN-18026 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8548 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3275"
    description: "PR #3275 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3275 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3275/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/contractor-integration-note.yml'
  - id: "occ-self-bind-pr-8554"
    description: "Binds OMN-18026 to OCC companion PR #8554 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8554 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1220"
    description: "PR #1220 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1220 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1220-ci"
    description: "PR #1220 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1220 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-3275"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-occ-evidence-admissibility-validator-pr-1220"
    description: "Hosted OCC evidence admissibility validator for omninode_infra PR #1220 (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1220"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-8545"
    description: "OCC companion PR #8545 — self-bind for OMN-18026 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8545 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2426"
    description: "PR #2426 on OmniNode-ai/omnimarket — hand-bound union-resolve companion (phase 2 link
      3: the contractor roster leaves the public tree)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2426 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2426/files --paginate --jq '.[] | select(.status == "removed") | .filename' | grep -cFx 'config/contractor_roster.yaml'
  - id: "dod-occ-diff-derived-behavior-proof-pr-2426"
    description: "PR #2426 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_contractor_integration_note_effect/test_contract.py tests/nodes/node_contractor_integration_note_effect/test_golden_chain_contractor_integration_note_effect.py -q
        cwd: "${OMNI_HOME}/omnimarket"
