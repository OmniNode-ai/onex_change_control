---
schema_version: "1.0.0"
ticket_id: "OMN-11940"
title: "Design plan context compute and preload evidence"
summary: >
  Central OCC evidence for OMN-11940 across the design-to-plan knowledge preload work. It covers the omniclaude
  Phase 0 design_to_plan skill preload and the omnimarket node_design_plan_context_compute implementation,
  which deterministically assembles architecture context from injected Repowise, antipattern, and dependency-impact
  results without I/O.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1651"
    description: "omniclaude PR #1651 adds Phase 0 knowledge preload to the design_to_plan skill."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11940/dod-omniclaude-pr-1651/command.yaml
          && grep -q '^pr_number: 1651$' drift/dod_receipts/OMN-11940/dod-omniclaude-pr-1651/command.yaml"
  - id: "dod-omnimarket-pr-836"
    description: "Bind OMN-11940 evidence to omnimarket PR #836."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11940/dod-omnimarket-pr-836/command.yaml
          && grep -q '^pr_number: 836$' drift/dod_receipts/OMN-11940/dod-omnimarket-pr-836/command.yaml"
  - id: "dod-design-plan-context-local-validation"
    description: "Focused design plan context tests, cross-node reach-in guard, ruff, leaked-literals,
      dep-health, and imperative contract guard pass for the current omnimarket PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11940/dod-design-plan-context-local-validation/command.yaml
          && grep -q '11 passed' drift/dod_receipts/OMN-11940/dod-design-plan-context-local-validation/command.yaml
          && grep -q 'baseline_delta: 0' drift/dod_receipts/OMN-11940/dod-design-plan-context-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: the new design plan context compute node is locally validated
      and requires no live runtime deployment before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11940/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11940/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11940 contract, deploy evidence, and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11940/dod-occ-pr/command.yaml"
