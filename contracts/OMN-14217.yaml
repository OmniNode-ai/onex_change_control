---
schema_version: "1.0.0"
ticket_id: "OMN-14217"
title: "refactor(OMN-14217): retire types→validation import-layering back-edge in omnibase_core"
summary: >-
  omnibase_core PR #1409 retires the frozen `types.** -> validation.**` import-layering back-edge by repointing
  a TYPE_CHECKING-only import in types/typed_dict_validator_info.py to the canonical definition site (models.common.model_validation_result)
  and dropping the now-unmatched .importlinter ignore line. Verified by the import-linter oracle: the
  core-foundation-no-upward contract stays KEPT with the allowlist entry removed, proving the edge is
  genuinely gone (no runtime behavior change — the import is TYPE_CHECKING-only, used solely in a string
  forward-reference annotation).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1409"
    description: >-
      Independent verification (a separate verifier, NOT the implementing agent) reproduced the import-linter
      oracle end-to-end against omnibase_core PR #1409 head f2690256 in an isolated worktree with PYTHONPATH
      cleared (grimp analyzed the worktree src, not the canonical clone). lint-imports kept all 4 contracts
      (Analyzed 4078 files, 10327 dependencies; 4 kept, 0 broken) with the types->validation ignore line
      removed. Negative control: restoring the old back-edge import (ignore line still removed) turned
      core-foundation-no-upward BROKEN (3 kept, 1 broken: types.typed_dict_validator_info -> validation.validator_utils
      l.14), proving the edge is real and genuinely removed by the repoint, not wildcard-masked. grep
      proof: no omnibase_core.validation import remains under src/omnibase_core/types/. mypy clean (4079
      files); 172 affected tests passed; ruff clean; pre-commit on changed files 0 failed (import-linter
      Passed). No runtime or deploy scope.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14217/dod-omnibase_core-pr-1409/command.yaml
          && grep -q '^pr_number: 1409$' drift/dod_receipts/OMN-14217/dod-omnibase_core-pr-1409/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the OMN-14217 contract and PASS
      receipt for omnibase_core PR #1409, binding itself to occ-preflight by commit_sha (contract commit
      C1), with no runtime or deploy scope.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14217/dod-occ-self/command.yaml
