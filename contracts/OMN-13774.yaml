---
schema_version: "1.0.0"
ticket_id: "OMN-13774"
title: "fix(OMN-13774): recreate spool dir before every _spool_event write"
summary: >-
  omnimarket PR #1544 fixes the live node_emit_daemon spool path so BoundedEventQueue._spool_event recreates
  the event-spool directory immediately before writing each spooled event. The PR also records regression
  tests that fail without the fix and pass with it, plus full-suite, ruff, mypy, and pre-commit evidence.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-spool-dir-regression"
    description: >-
      node_emit_daemon spool writes recreate a missing event-spool directory and drain_to_spool preserves
      buffered events when the directory is removed after queue initialization.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13774/dod-spool-dir-regression/command.yaml"
  - id: "dod-product-verification"
    description: >-
      PR #1544 records focused regression tests, full non-kafka pytest results, ruff, strict mypy for
      event_queue.py, and pre-commit evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13774/dod-product-verification/command.yaml"
  - id: "dod-pr-1544-binding"
    description: >-
      OmniNode-ai/omnimarket PR #1544 carries the OMN-13774 fix at feature commit 510c94d2085c24b385ed587a5c3791748dc3d0b0.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13774/dod-pr-1544-binding/command.yaml"
  - id: "dod-occ-self"
    description: >-
      onex_change_control PR #3401 carries this central OMN-13774 contract and PASS receipts for the downstream
      omnimarket PR #1544 merge sweep.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13774/dod-occ-self/command.yaml"
