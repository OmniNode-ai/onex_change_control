---
schema_version: "1.0.0"
ticket_id: "OMN-14028"
title: "Raise .201 hot runtime service memory caps"
summary: >-
  Bind OCC evidence for omnibase_infra PR #2221, which raises the memory limits for the observed hot .201
  runtime services: agent-actions-consumer, skill-lifecycle-consumer, and intelligence-api. No live deploy
  or restart is performed by the source PR; rollout remains gated by normal deploy approval and post-merge
  container inspection.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #2221 carries the memory-cap source change."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14028/dod-infra-pr-2221/command.yaml"
  - kind: "tests"
    description: "Compose invariant tests and lint checks passed locally."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14028/dod-local-validation/command.yaml"
  - kind: "manual"
    description: "Deploy gate is recorded: no live deploy/restart before merge."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14028/dod-deploy-gate/command.yaml"
  - kind: "manual"
    description: "OCC PR #3649 binds this evidence bundle to the current change-control PR."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14028/dod-occ-self/command.yaml"
dod_evidence:
  - id: "dod-infra-pr-2221"
    description: "Bind omnibase_infra PR #2221 at the observed head commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14028/dod-infra-pr-2221/command.yaml && grep
          -q '^ticket_id: OMN-14028$' drift/dod_receipts/OMN-14028/dod-infra-pr-2221/command.yaml && grep
          -q '^pr_number: 2221$' drift/dod_receipts/OMN-14028/dod-infra-pr-2221/command.yaml && grep -q
          '708bdfbd67be51eec36e317efae17351a4ccd7c5' drift/dod_receipts/OMN-14028/dod-infra-pr-2221/command.yaml
  - id: "dod-local-validation"
    description: "Targeted compose invariant test and ruff checks passed for PR #2221."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14028/dod-local-validation/command.yaml && grep
          -q '2 passed' drift/dod_receipts/OMN-14028/dod-local-validation/command.yaml && grep -q 'All
          checks passed' drift/dod_receipts/OMN-14028/dod-local-validation/command.yaml
  - id: "dod-deploy-gate"
    description: "No live .201 deploy or restart was performed before merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14028/dod-deploy-gate/command.yaml && grep -qi
          'no live deploy' drift/dod_receipts/OMN-14028/dod-deploy-gate/command.yaml
  - id: "dod-occ-self"
    description: "Bind OCC PR #3649 at the observed head commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14028/dod-occ-self/command.yaml && grep -q '^ticket_id:
          OMN-14028$' drift/dod_receipts/OMN-14028/dod-occ-self/command.yaml && grep -q '^pr_number: 3649$'
          drift/dod_receipts/OMN-14028/dod-occ-self/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
