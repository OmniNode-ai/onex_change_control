---
schema_version: "1.0.0"
ticket_id: "OMN-16237"
title: "OCC evidence for OMN-16237"
summary: >
  Manual OCC companion for OmniNode-ai/omnibase_infra PR #2802 (teach the static SQL-text Application
  Database Domain Enforcement gate, application_database_domain_enforcement.py / OMN-15361, to consult
  the same physical-schema-mapping allowlist the runtime grants system already trusts -- TENANT_TABLES_PHYSICALLY_IN_PUBLIC_UNTIL_OMN15359
  and INTERNAL_TABLES_PHYSICALLY_IN_PUBLIC_UNTIL_OMN15359 -- instead of unconditionally rejecting every
  unqualified or public-qualified application relation. Records a real before/after differential of the
  gate's own output against the exact SQL statement OMN-14751's migration ships, proving the fix changes
  gate behavior rather than only adding passing tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2802"
    description: "PR #2802 on OmniNode-ai/omnibase_infra -- Evidence-Source (hand-authored: build lane
      opened this PR directly, per the established hand-authored-companion pattern)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2802 --repo OmniNode-ai/omnibase_infra --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2802-ci"
    description: "PR #2802 on OmniNode-ai/omnibase_infra -- falsifiable pytest proof of the fix, plus
      a separately-captured real before/after differential of application_database_domain_enforcement.lint_application_database_sql()
      against the exact statement OMN-14751's 0001_intent_classification_agent_source.sql ships (an unqualified
      ALTER TABLE on intent_classification_events), recorded in this receipt's probe_stdout. The differential
      loaded both commits' real module source directly (git show <sha>:<path>, exec'd as a module, called
      against the same real topology profile) -- not a stubbed or hypothetical comparison. Before the
      fix (commit fa3c706fa, the PR's merge-base) the gate rejects the statement with 'must be schema-qualified';
      after the fix (commit a9c9415d1, the PR head) the gate returns zero violations, because intent_classification_events
      is enumerated in INTERNAL_TABLES_PHYSICALLY_IN_PUBLIC_UNTIL_OMN15359. The check_value below is the
      test this repo's CI actually runs, which is RED against the pre-fix module and GREEN against the
      PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/validation/test_application_database_sql_enforcement_regressions.py::test_allowlisted_physically_public_table_passes_unqualified_or_public
          -q"
  - id: "occ-self-bind-pr-6718"
    description: "OCC companion PR #6718 -- self-bind for OMN-16237: every other entry in this contract
      binds to omnibase_infra#2802, not this OCC PR, so occ-preflight's merge-eligibility validator (validator_occ_merge_eligibility._receipt_bound_to_pr)
      needs this entry to find a PASS receipt bound to the OCC PR carrying it (established pattern: occ-self-bind-pr-6694)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6718 --repo OmniNode-ai/onex_change_control --json number,state"
