---
# OMN-9629 contract file for omnibase_infra deploy-gate (OMN-8912).
# The canonical ticket contract lives in onex_change_control.
# This file is the per-repo deploy-evidence carrier only.
schema_version: '1.0.0'
ticket_id: OMN-9629
title: "Task P9: Register new Kafka topics in TopicBase"
summary: 'Task P9: Register new Kafka topics in TopicBase'
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - topics
  - events
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: PR opened for A2A topic registration runtime path
    source: generated
    checks:
      - check_type: command
        check_value: 'gh pr view 1420 --repo OmniNode-ai/omnibase_infra --json state -q .state'
  - id: dod-002
    description: Contract artifact file present in repo
    source: generated
    checks:
      - check_type: command
        check_value: 'test -f contracts/OMN-9629.yaml'
  - id: dod-003
    description: Runtime deploy validates invocation and lifecycle topics after merge
    source: manual
    checks:
      - check_type: command
        check_value: 'rpk topic produce onex.cmd.omnibase-infra.invocation.v1 after deploy validation'
