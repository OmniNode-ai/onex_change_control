---
schema_version: "1.0.0"
ticket_id: "OMN-10638"
title: "Shadow mode for delegation A/B testing"
summary: >
  Adds dormant Bifrost delegation shadow-mode configuration and validation so comparison decisions can
  be logged without changing live routing output.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-shadow-mode-unit-suite"
    description: "Focused shadow-mode unit and contract-loading tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10638/dod-shadow-mode-unit-suite/command.yaml\""
  - id: "dod-pr-1534-head"
    description: "Bind OMN-10638 evidence to omnibase_infra PR #1534 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10638/dod-pr-1534-head/command.yaml\""
