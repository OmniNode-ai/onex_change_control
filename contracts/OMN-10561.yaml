---
schema_version: "1.0.0"
ticket_id: "OMN-10561"
title: "Generic contract-driven reducer node"
summary: >
  Adds omnimarket node_contract_reducer as a pure COMPUTE node for contract-declared transition tables.
  The node interprets current state, user response, and transition configuration without external I/O
  or authoritative persistence.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-mypy-strict"
    description: "Reducer node type-checks under strict mypy."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10561/dod-mypy-strict/command.yaml"
  - id: "dod-unit-tests"
    description: "22 reducer unit tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10561/dod-unit-tests/command.yaml"
  - id: "dod-ruff"
    description: "Reducer node sources pass ruff."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10561/dod-ruff/command.yaml"
  - id: "dod-deploy-gate"
    description: "Repo-local deploy gate recognizes OMN-10561 deploy evidence for runtime-touching reducer
      node paths."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10561/dod-deploy-gate/command.yaml"
  - id: "dod-post-merge-ticket-skip"
    description: "Post-merge workflows skip cleanly when a merged PR branch has no OMN ticket ID."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10561/dod-post-merge-ticket-skip/command.yaml"
  - id: "dod-pr-553-head"
    description: "Bind OMN-10561 post-merge workflow evidence to omnimarket PR #553 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10561/dod-pr-553-head/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10561 post-merge workflow contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10561/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 798$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10561/dod-occ-pr/command.yaml"
