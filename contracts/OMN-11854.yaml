---
schema_version: "1.0.0"
ticket_id: "OMN-11854"
title: "[Phase 0.4] omninode_infra deploy-gate queue (#449, #451, #452)"
summary: >-
  Merge three omninode_infra PRs: #449 (ci-gate-wiring), #451 (fix-prod-deploy-tag),
  #452 (prod-tf-tag-based-discovery). Provides OCC evidence for all three.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "FULL"
evidence_requirements:
  - kind: "ci"
    description: "omninode_infra PR #451 CI green after pr-title fix and rebase."
    command: "gh pr checks 451 --repo OmniNode-ai/omninode_infra"
  - kind: "manual"
    description: "Self-bound OCC PR carrying this contract."
    command: "See drift/dod_receipts/OMN-11854/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-458"
    description: "omninode_infra PR #458 corrects SYSTEM_INSTANCE_TAG_NAME for prod deploy targeting."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11854/dod-omninode-infra-pr-458/command.yaml"
          && grep -q '^pr_number: 458$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11854/dod-omninode-infra-pr-458/command.yaml"
  - id: "dod-omninode-infra-pr-457"
    description: "omninode_infra PR #457 — port tag-based network discovery to prod cluster Terraform (migrate from broken terraform_remote_state)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11854/dod-omninode-infra-pr-457/command.yaml"
          && grep -q '^pr_number: 457$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11854/dod-omninode-infra-pr-457/command.yaml"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for this OCC PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11854/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: OMN-11854$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11854/dod-occ-pr/command.yaml"
