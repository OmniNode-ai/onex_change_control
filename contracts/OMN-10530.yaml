---
schema_version: "1.0.0"
ticket_id: OMN-10530
title: "Rollback checkpoint PyPI patch release chain"
summary: "Cut patch releases for the infrastructure package chain so runtime deployments can pin a known rollback checkpoint while development continues."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Release PR CI gates pass before merge."
    command: "gh pr checks"
  - kind: "manual"
    description: "Package builds a wheel and sdist for the target patch version."
    command: "uv build"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-build"
    description: "omnibase-core 0.40.1 wheel and sdist build locally."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv build"
  - id: "dod-core-version-smoke"
    description: "omnibase-core package metadata and __version__ report 0.40.1."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run python -c \"from importlib.metadata import version; import omnibase_core; assert version('omnibase-core') == '0.40.1'; assert omnibase_core.__version__ == '0.40.1'\""
  - id: "dod-core-diff-check"
    description: "omnibase-core release diff has no whitespace errors."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "git diff --check"
