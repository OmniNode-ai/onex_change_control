---
schema_version: "1.0.0"
ticket_id: OMN-10530
title: "Rollback checkpoint PyPI patch release chain"
summary: "Cut patch releases for the infrastructure package chain so runtime deployments can pin a known
  rollback checkpoint while development continues."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Release PR CI gates pass before merge."
    command: "gh pr checks"
  - kind: "manual"
    description: "Package builds a wheel and sdist for the target patch version."
    command: "uv build"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-build"
    description: "omnibase-core 0.40.1 wheel and sdist build locally."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-core-build/command.yaml
          && grep -q 'pr_number: 1030' drift/dod_receipts/OMN-10530/dod-core-build/command.yaml"
  - id: "dod-core-version-smoke"
    description: "omnibase-core package metadata and __version__ report 0.40.1."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-core-version-smoke/command.yaml
          && grep -q 'omnibase-core 0.40.1' drift/dod_receipts/OMN-10530/dod-core-version-smoke/command.yaml"
  - id: "dod-core-diff-check"
    description: "omnibase-core release diff has no whitespace errors."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-core-diff-check/command.yaml
          && grep -q 'git diff --check exited 0' drift/dod_receipts/OMN-10530/dod-core-diff-check/command.yaml"
  - id: "dod-occ-evidence-validate"
    description: "OCC release checkpoint contract validates and receipt gate passes locally."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run validate-yaml contracts/OMN-10530.yaml && grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-occ-evidence-validate/command.yaml
          && grep -q 'pr_number: 693' drift/dod_receipts/OMN-10530/dod-occ-evidence-validate/command.yaml"
  - id: "dod-spi-build"
    description: "omnibase-spi 0.20.6 wheel and sdist build locally against omnibase-core 0.40.1."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-spi-build/command.yaml &&
          grep -q 'pr_number: 202' drift/dod_receipts/OMN-10530/dod-spi-build/command.yaml"
  - id: "dod-spi-version-smoke"
    description: "omnibase-spi package metadata reports 0.20.6 and resolves omnibase-core 0.40.1."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-spi-version-smoke/command.yaml
          && grep -q 'omnibase-spi 0.20.6' drift/dod_receipts/OMN-10530/dod-spi-version-smoke/command.yaml
          && grep -q 'omnibase-core 0.40.1' drift/dod_receipts/OMN-10530/dod-spi-version-smoke/command.yaml"
  - id: "dod-spi-tests"
    description: "omnibase-spi test suite passes for the 0.20.6 release branch."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-spi-tests/command.yaml &&
          grep -q '2825 passed, 4 skipped' drift/dod_receipts/OMN-10530/dod-spi-tests/command.yaml"
  - id: "dod-spi-diff-check"
    description: "omnibase-spi release diff has no whitespace errors."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-spi-diff-check/command.yaml
          && grep -q 'git diff --check exited 0' drift/dod_receipts/OMN-10530/dod-spi-diff-check/command.yaml"
  - id: "dod-occ-spi-evidence-validate"
    description: "OCC SPI release evidence update validates and receipt gate passes locally."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run validate-yaml contracts/OMN-10530.yaml && grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-occ-spi-evidence-validate/command.yaml
          && grep -q 'pr_number: 694' drift/dod_receipts/OMN-10530/dod-occ-spi-evidence-validate/command.yaml"
  - id: "dod-infra-build"
    description: "omnibase-infra 0.34.2 wheel and sdist build locally against published core and SPI checkpoint
      versions."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-infra-build/command.yaml
          && grep -q 'pr_number: 1498' drift/dod_receipts/OMN-10530/dod-infra-build/command.yaml"
  - id: "dod-infra-version-smoke"
    description: "omnibase-infra package metadata reports 0.34.2 and resolves omnibase-core 0.40.1 plus
      omnibase-spi 0.20.6."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-infra-version-smoke/command.yaml
          && grep -q 'omnibase-infra 0.34.2' drift/dod_receipts/OMN-10530/dod-infra-version-smoke/command.yaml
          && grep -q 'omnibase-core 0.40.1' drift/dod_receipts/OMN-10530/dod-infra-version-smoke/command.yaml
          && grep -q 'omnibase-spi 0.20.6' drift/dod_receipts/OMN-10530/dod-infra-version-smoke/command.yaml"
  - id: "dod-infra-version-pin-check"
    description: "omnibase-infra dependency pins comply with the shared version matrix after moving to
      published checkpoint packages."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-infra-version-pin-check/command.yaml
          && grep -q 'RESULT: All repos are compliant' drift/dod_receipts/OMN-10530/dod-infra-version-pin-check/command.yaml"
  - id: "dod-infra-diff-check"
    description: "omnibase-infra release diff has no whitespace errors."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-infra-diff-check/command.yaml
          && grep -q 'git diff --check exited 0' drift/dod_receipts/OMN-10530/dod-infra-diff-check/command.yaml"
  - id: "dod-infra-runtime-fallback-floor-tests"
    description: "omnibase-infra runtime fallback dependency floors match the 0.34.2 release dependencies."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-infra-runtime-fallback-floor-tests/command.yaml
          && grep -q '28 passed' drift/dod_receipts/OMN-10530/dod-infra-runtime-fallback-floor-tests/command.yaml
          && grep -q '3bac7511' drift/dod_receipts/OMN-10530/dod-infra-runtime-fallback-floor-tests/command.yaml"
  - id: "dod-occ-infra-evidence-validate"
    description: "OCC infra release evidence update validates and receipt gate passes locally."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run validate-yaml contracts/OMN-10530.yaml && grep -q 'status: PASS' drift/dod_receipts/OMN-10530/dod-occ-infra-evidence-validate/command.yaml
          && grep -q 'pr_number: 695' drift/dod_receipts/OMN-10530/dod-occ-infra-evidence-validate/command.yaml"
