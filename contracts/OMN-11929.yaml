---
schema_version: "1.0.0"
ticket_id: "OMN-11929"
title: "Unified knowledge query federation orchestrator"
summary: >
  Bind central evidence for omnimarket PR #835, which adds node_knowledge_query_federation_orchestrator.
  The implementation routes knowledge queries deterministically across Memgraph, Repowise, and Qdrant,
  merges backend results with provenance, deduplicates by content hash, and declares canonical handler
  routing.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-835"
    description: "Bind OMN-11929 evidence to omnimarket PR #835."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11929/dod-omnimarket-pr-835/command.yaml
          && grep -q '^pr_number: 835$' drift/dod_receipts/OMN-11929/dod-omnimarket-pr-835/command.yaml"
  - id: "dod-knowledge-query-federation-local-validation"
    description: "Focused federation tests, cross-node reach-in guard, ruff, leaked-literals, dep-health,
      and imperative contract guard pass for the current omnimarket PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11929/dod-knowledge-query-federation-local-validation/command.yaml
          && grep -q '24 passed' drift/dod_receipts/OMN-11929/dod-knowledge-query-federation-local-validation/command.yaml
          && grep -q 'baseline_delta: 0' drift/dod_receipts/OMN-11929/dod-knowledge-query-federation-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: the new federation orchestrator is locally validated and requires
      no live runtime deployment before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11929/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11929/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11929 contract, deploy evidence, and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11929/dod-occ-pr/command.yaml"
