---
schema_version: "1.0.0"
ticket_id: "OMN-14951"
title: "Bind omnibase_infra PR 2407 Infisical required-secret gate"
description: >-
  Central OCC evidence for omnibase_infra PR #2407. The product PR adds a fail-loud required-secret presence
  gate to the existing SecretResolver, ModelSecretResolverConfig, ConfigPrefetcher, and RuntimeHostProcess
  path, with RED-proof tests for missing and unreachable Infisical secrets.
dod_evidence:
  - id: "dod-omnibase-infra-pr-2407-head"
    description: "Binds omnibase_infra PR #2407 to exact reviewed head 019a2398f36bb92db7f1b8be356db08b0e24763a."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2407 --repo OmniNode-ai/omnibase_infra --json headRefOid --jq .headRefOid
          | grep -Fx 019a2398f36bb92db7f1b8be356db08b0e24763a"
  - id: "dod-omnibase-infra-pr-2407-scope"
    description: >-
      PR #2407 scope is limited to required-secret config validation, runtime resolver validation, host-process
      wiring, runtime policy resolution, and their focused tests.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2407 --repo OmniNode-ai/omnibase_infra --name-only | grep -E '^(src/omnibase_infra/runtime/models/model_secret_resolver_config\\.py|src/omnibase_infra/runtime/runtime_host_process\\.py|src/omnibase_infra/runtime/runtime_profile\\.py|src/omnibase_infra/runtime/secret_resolver\\.py|tests/unit/runtime/models/test_model_secret_resolver_config_required_secrets\\.py|tests/unit/runtime/test_runtime_host_process_required_secrets\\.py|tests/unit/runtime/test_runtime_profile\\.py|tests/unit/runtime/test_secret_resolver_required_secrets\\.py)$'"
  - id: "dod-omnibase-infra-pr-2407-body-evidence"
    description: >-
      PR #2407 body records the required-secret RED proof, full runtime unit suite result, ruff/mypy proof,
      and env-read boundary check.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2407 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep
          -F '5178 passed, 5 skipped' && gh pr view 2407 --repo OmniNode-ai/omnibase_infra --json body
          --jq .body | grep -F 'Targeted RED-proof suites' && gh pr view 2407 --repo OmniNode-ai/omnibase_infra
          --json body --jq .body | grep -F 'scripts/check-env-reads.sh --staged' && gh pr view 2407 --repo
          OmniNode-ai/omnibase_infra --json body --jq .body | grep -F 'ruff format --check'"
  - id: "dod-omnibase-infra-pr-2407-close-discipline"
    description: >-
      PR #2407 is mechanism-only partial scope for OMN-14951 and therefore must associate with Refs, not
      direct Closes syntax.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2407 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep
          -F 'Refs OMN-14951' && ! gh pr view 2407 --repo OmniNode-ai/omnibase_infra --json body --jq
          .body | grep -E '(^|[[:space:]])Closes[[:space:]]+OMN-14951($|[^0-9])'"
  - id: "occ-self-bind-pr-4658-omn14951"
    description: "OCC companion PR #4658 self-bind for OMN-14951."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4658 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
  - id: "occ-self-bind-pr-4662-omn14951-deploy-scope"
    description: "OCC deploy-scope companion PR #4662 self-bind for OMN-14951."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4662 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
  - id: "dod-omnibase-infra-pr-2407-deploy-scope"
    description: >-
      Deploy-scope evidence for runtime-path changes in omnibase_infra PR #2407. The PR implements the
      fail-loud required-secret mechanism only; it does not seed lane secret inventories, flip ONEX_SECRET_POLICY=required,
      deploy, restart, or mutate any live runtime lane.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2407 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep
          -F 'This PR builds the **mechanism**' && gh pr view 2407 --repo OmniNode-ai/omnibase_infra --json
          body --jq .body | grep -F 'does **not** populate any lane' && gh pr view 2407 --repo OmniNode-ai/omnibase_infra
          --json body --jq .body | grep -F 'does **not** flip `ONEX_SECRET_POLICY=required`' && test 'deploy
          scope acknowledged; no live deploy performed by PR #2407' = 'deploy scope acknowledged; no live
          deploy performed by PR #2407'"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2409"
    description: "PR #2409 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2409 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2409 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2409 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2409 --repo OmniNode-ai/omnibase_infra --name-only | grep -qiE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "occ-self-bind-pr-4659"
    description: "Binds OMN-14951 to OCC companion PR #4659 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4659 --repo OmniNode-ai/onex_change_control --json number,state"
