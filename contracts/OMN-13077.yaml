---
schema_version: "1.0.0"
ticket_id: "OMN-13077"
title: "Expose cost.by_repo projection API contract"
summary: >-
  Companion central evidence for the OMN-13077 NC-04 slice absorbed into omnimarket PR #1393. The existing
  node_projection_cost_by_repo now exposes the missing projection_api block for the cost_by_repo_snapshots
  table carrying repo_name, total_cost_usd, and window. The change is additive to the existing event topic
  and is covered by the same product PR and deploy assessment as OMN-13083.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: OMN-13077 is included in omnimarket PR #1393, which adds the contract-declared
      cost.by_repo projection API and node-owned idempotent migration. No live deploy or restart is performed
      in the PR; live readback is deferred to the normal redeploy step.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13077/dod-deploy-assessment/command.yaml"
