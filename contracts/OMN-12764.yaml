---
schema_version: "1.0.0"
ticket_id: "OMN-12764"
title: "Delegate-skill test task stalls after inference response on dev runtime"
summary: >-
  Tracks the golden-chain regression proving delegate-skill test tasks terminalize after an inference
  response instead of stalling in the inference to quality-gate to terminal chain.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused delegate-skill test-task terminalization regression passes."
    command: "uv run pytest tests/test_golden_chain_delegate_skill_test_task_terminalize_omn12764.py -q"
  - kind: "tests"
    description: "Unit suite, strict mypy, ruff, and pre-commit proof is recorded in the product PR body."
    command: "uv run pytest tests/ -q -m unit && uv run mypy src/ --strict && uv run ruff check src/ tests/
      && pre-commit run --files tests/test_golden_chain_delegate_skill_test_task_terminalize_omn12764.py"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-test-task-terminalization-regression"
    description: >-
      Focused golden-chain regression proves delegate-skill test tasks emit completed or failed terminal
      events before timeout after inference response processing.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12764/dod-test-task-terminalization-regression/command.yaml"
  - id: "dod-omnimarket-pr-1366"
    description: >-
      omnimarket PR #1366 carries the OMN-12764 delegate-skill test-task terminalization regression.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12764/dod-omnimarket-pr-1366/command.yaml"
  - id: "dod-deploy-no-live-mutation"
    description: >-
      Regression tests only; no live deploy, runtime restart, broker mutation, migration, or runtime state
      mutation is required before merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12764/dod-deploy-no-live-mutation/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-12764 contract and receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12764/dod-occ-pr-self/command.yaml"
