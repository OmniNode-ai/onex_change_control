---
schema_version: "1.0.0"
ticket_id: "OMN-13549"
title: "fix(OMN-13549): de-flake sub-ms cache-timing assertion blocking the infra merge queue"
summary: >-
  OCC evidence for the omnibase_infra fix PR that de-flakes a recurring merge-queue-blocking test. tests/integration/mixins/test_mixin_node_introspection_contract_integration.py::TestContractIntegrationPerformance::test_performance_metrics_available_after_introspection
  asserted a cache-hit-vs-miss wall-clock comparison at sub-millisecond resolution (observed miss 0.243ms
  vs hit 1.335ms), where the result is pure scheduler/JIT/GC noise. It passed locally and on retry but
  flaked intermittently in CI, taking down a required Tests (Split N/15) -> CI Tests Gate, which blocked
  merge-queue admission for every infra PR (confirmed flaking on #2088 and #2089 in one night). The source
  change removes the sub-ms relative wall-clock comparison and instead asserts that the timing metric
  is present and recorded (a non-negative float); the cache-hit behavior itself stays fully proven by
  the deterministic cache_hit is True flag. The fix is test-only — no source, runtime, or deploy change
  — so no deploy-gate evidence applies.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-deflake-deterministic"
    description: >-
      The required test path no longer asserts a sub-millisecond relative wall-clock comparison: the cache_hit_faster/both_very_fast
      logic is removed and replaced by a present/recorded non-negative-float metric assertion. The target
      test runs deterministically 20x locally (20/20 passed) and the full test file passes (20/20).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13549/dod-deflake-deterministic/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13549.yaml consumed
      by the omnibase_infra fix PR Receipt Gate via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13549\"$' contracts/OMN-13549.yaml"
