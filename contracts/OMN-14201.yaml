---
schema_version: "1.0.0"
ticket_id: "OMN-14201"
title: "Autobind OCC evidence for OMN-14201"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1310."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1310 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #1310 on OmniNode-ai/omninode_infra changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (k8s/onex-prod/memgraph-lab.yaml, k8s/onex-prod/omnidash/deployment.yaml, k8s/onex-prod/omniintelligence/deployment.yaml, k8s/onex-prod/omniweb/deployment.yaml) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1310"
    description: "PR #1310 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1310 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1310-ci"
    description: "PR #1310 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1310 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1310"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-8968"
    description: "OCC companion PR #8968 — self-bind for OMN-14201 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8968 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omn14201-behavior-proof-manual"
    description: >-
      The behavior proof this contract's tests evidence_requirement records as OWED. Hand-authored as
      a source: manual item because omninode_infra#1310 changes only Kubernetes manifests and touches
      no pytest target, so no derived suite could prove it. What the change does is verified by rendering
      it: kubectl kustomize k8s/onex-prod builds clean at 54 objects, and the replica counts are read
      back from the RENDERED output rather than the source files — 13 Deployments at 0 (memgraph-lab,
      omnibase-intelligence-api, omnidash, omniintelligence, omninode-agent-actions-consumer, omninode-contract-resolver,
      omninode-runtime, omninode-runtime-effects, omninode-runtime-worker, omninode-skill-lifecycle-consumer,
      pgweb, redpanda-console, umami), omniweb at 1, onex-api at 1, cronjob-baselines-batch-compute and
      cronjob-cross-repo-validation suspended, and ecr-token-refresh deliberately left enabled so its
      three consecutive failures stay visible. The repo's own gates agree: scripts/validate-cronjob-manifests.sh
      reports 0 violations and scripts/validate_deployment_entrypoints.py reports OK.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr checks 1310 --repo OmniNode-ai/omninode_infra"
  - id: "dod-omn14201-promotion-refused-today"
    description: >-
      The promotion this work prepares is currently REFUSED, recorded here rather than discovered at dispatch
      time. The OMN-17357 staging-green premise is evaluated before the grant by the same consumer script.
      Measured twice, offline, against the rendered grant entry with prod health HEALTHY: with no report
      it exits 1 on BLOCKED (absent); with the real current report (bar revision r10, collected 2026-09-10T16:44:13Z)
      it exits 1 on BLOCKED (not_green), the bar being BLOCKED at 2 PASS / 3 FAIL / 3 UNPROVEN. Prod is
      HEALTHY so the confirmed-unhealthy waiver does not apply. Falsifiable against the request bundle,
      which carries the verbatim refusal output.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -c 'BLOCKED (not_green)' .github/prod-promotion-requests/OMN-14201/receipts/dod-omn14201-consumer-gate-blocks-on-staging-green/command.yaml
  - id: "dod-omn14201-grant-bundle-well-formed"
    description: >-
      The prod-promotion grant request bundle at .github/prod-promotion-requests/OMN-14201/ carries the
      five artifacts stage-prod-promotion-grant.yml requires, and its single entry validates against this
      repo's own grant schema validator ("1 grant(s) validated successfully", exit 0). Proven by a negative
      control in the same run: a byte-identical copy with runtime_lane blanked returns "FAIL: 1 violation(s)",
      exit 1, so the pass is a real verdict rather than a validator that accepts anything. grants/prod_promotion_grants.yaml
      is untouched and stays at rest.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -c 'grant-5e1078c5-f906-4a6c-8825-ae500c9a1dfc' .github/prod-promotion-requests/OMN-14201/entries_block.yaml
