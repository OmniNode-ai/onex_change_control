---
schema_version: 1.0.0
ticket_id: OMN-9339
title: Extend normalization validator to Pydantic schema field-set symmetry
summary: The normalization symmetry validator compares producer and consumer Pydantic model field sets
  for each topic/event pair.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Product PR  #1114 verifies red/green Pydantic field-set symmetry fixtures, alias resolution, strict typing, and the blocking pre-commit hook.
    command: env PYTHONPATH=src uv run pytest tests/unit/validation/test_checker_normalization_symmetry.py
      -q --timeout=30 -n 0
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-field-set-symmetry
    description: Product verification evidence is captured as a canonical PASS receipt.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9339/dod-field-set-symmetry/command.yaml"
          && grep -q '^pr_number: 1114$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9339/dod-field-set-symmetry/command.yaml"
          && grep -q '^verifier: agent$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9339/dod-field-set-symmetry/command.yaml"
