---
schema_version: "1.0.0"
ticket_id: "OMN-9068"
title: "Enable gap auth probes by default"
summary: >
  Updates the omniclaude gap skill contract and prompt so auth_config probes are enabled by default, preserves
  an explicit opt-out flag, and adds regression coverage for the default behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused gap auth-probe default tests pass on omniclaude PR #1610."
    command: "uv run pytest tests/unit/skills/test_gap_auth_probe_defaults.py -q"
  - kind: "ci"
    description: "omniclaude PR #1610 is bound to OMN-9068 at the recorded head SHA."
    command: "gh pr view 1610 --repo OmniNode-ai/omniclaude --json headRefOid,title"
  - kind: "ci"
    description: "omniclaude PR #1610 non-receipt checks are green; Receipt Gate is the only blocker."
    command: "gh pr checks 1610 --repo OmniNode-ai/omniclaude"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1610"
    description: "omniclaude PR #1610 is open against main at the recorded OMN-9068 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9068/dod-omniclaude-pr-1610/command.yaml\"
          && grep -q '^pr_number: 1610$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9068/dod-omniclaude-pr-1610/command.yaml\"
          && grep -q '^commit_sha: \"e6a50dd9ef3fed8d05953c9185f4483666e43990\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9068/dod-omniclaude-pr-1610/command.yaml\""
  - id: "dod-product-ci-gates"
    description: "omniclaude PR #1610 product CI is green except Receipt Gate pending this central evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9068/dod-product-ci-gates/command.yaml\"
          && grep -q 'Receipt Gate is the only failing check' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9068/dod-product-ci-gates/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-9068 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9068/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number: 1201$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9068/dod-occ-pr/command.yaml\"
          && grep -q '^commit_sha: \"8a69df986d5ca6beba2cfbf82cdb6c55ae3e1d2e\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9068/dod-occ-pr/command.yaml\""
