---
schema_version: "1.0.0"
ticket_id: "OMN-13130"
title: "OMN-13130 — UI contract primitives + EnumEmptyStateReason in omnibase_core (Phase 0)"
summary: >-
  Phase 0 of the Contract-Driven UI Platform (epic OMN-13129). omnibase_core PR #1255 adds six frozen
  Pydantic UI contract primitives to src/omnibase_core/models/dashboard (ModelComponentContract, ModelActionContract,
  ModelDataBindingContract, ModelPermissionContract, ModelEvidenceRequirementContract, ModelRendererCapabilityContract)
  plus EnumEmptyStateReason (four values mirroring omnidash/shared/types/chart-config.ts exactly) and
  supporting enums (EnumBindingOrderDirection, EnumEvidenceGateMoment, EnumRendererInteractionModel, EnumAccessibilityTier).
  ModelActionContract COMPOSES the existing ModelGate approval primitive (no duplication, per ADR docs/adr/2026-06-17-ui-deterministic-projection-effect-layer.md
  D2); ModelEvidenceRequirementContract composes the OCC ModelEvidenceRequirement. ModelGate and ModelGateSpec
  source are untouched. All six models are registered in scripts/emit_ts_types.py for Stage 2 TS codegen.
  The change is additive over ModelWidgetConfig — every existing omnidash manifest still validates and
  the dashboard export-surface test was extended (not broken). This OCC contract is the central Evidence-Source
  for PR #1255's Receipt Gate; the paired receipts carry the real probe evidence captured by re-running
  the code PR's verification commands at PR head 7d35dc1f86a21d9ef970bc1310ba81d7e329dccf.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1255"
    description: >-
      Evidence for omnibase_core PR #1255 (head 7d35dc1f86a21d9ef970bc1310ba81d7e329dccf): the six UI
      contract primitives + EnumEmptyStateReason were re-verified independently by the OCC-authoring agent
      against the PR head. 36 new unit tests pass (tests/unit/models/dashboard/test_ui_contract_primitives.py),
      mypy --strict reports zero errors in any new file (the only 2 surfaced errors are in src/omnibase_core/contracts/contract_loader.py
      lines 570/704, confirmed byte-identical to origin/dev via an empty git diff — pre-existing, not
      introduced here), ruff check on the new files is clean, and scripts/emit_ts_types.py emits all six
      new $defs (ModelComponentContract, ModelActionContract, ModelDataBindingContract, ModelPermissionContract,
      ModelEvidenceRequirementContract, ModelRendererCapabilityContract) so the Stage 2 TS mirrors can
      be generated.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13130/dod-core-pr-1255/command.yaml && grep
          -q 'commit_sha: "7d35dc1f86a21d9ef970bc1310ba81d7e329dccf"' drift/dod_receipts/OMN-13130/dod-core-pr-1255/command.yaml
  - id: "dod-occ-self"
    description: >-
      onex_change_control PR (dev) carries the OMN-13130 central contract and the paired core-PR receipt;
      this self-binding receipt makes the OCC PR satisfy its own OCC eligibility check (binds on the OCC
      pr_number, resolving pr_ticket_mismatch).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13130/dod-occ-self/command.yaml && grep -q '^ticket_id:
          "OMN-13130"$' drift/dod_receipts/OMN-13130/dod-occ-self/command.yaml
