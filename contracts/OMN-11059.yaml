---
schema_version: "1.0.0"
ticket_id: "OMN-11059"
title: "Evidence-backed allowlist for 466 MAJOR MISSING_TOPIC_EDGE findings"
summary: >
  Central OCC evidence for omnimarket PR #656. Samples 50 representative topics from 466 MAJOR MISSING_TOPIC_EDGE
  findings across all 5 service prefixes, verifies each via contract.yaml inspection and source grep,
  and extends dep_health_allowlist.yaml with 73 new EVT entries (41 intentionally_unconsumed + 32 runtime_owned_outside_scan).
  MAJOR findings reduced from 466 to 432 (34 suppressed).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Dep-health sweep confirms MAJOR count dropped from 466 to 432."
    command: "See drift/dod_receipts/OMN-11059/dod-sweep-clean/command.yaml"
  - kind: "ci"
    description: "Deploy assessment — allowlist + pre-commit exclude change, no live deploy required."
    command: "See drift/dod_receipts/OMN-11059/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sweep-clean"
    description: "Test suite passes; dep-health sweep shows 432 MAJOR findings after 73 new allowlist
      entries."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-11059/dod-sweep-clean/command.yaml && echo
          'OMN-11059 sweep-clean receipt present'"
  - id: "dod-deploy"
    description: "Deploy assessment recorded — allowlist + pre-commit hook exclude change; no live deploy
      required."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-11059/dod-deploy/command.yaml && grep
          -q 'evidence_item_id: \"dod-deploy\"' drift/dod_receipts/OMN-11059/dod-deploy/command.yaml &&
          echo 'OMN-11059 deploy receipt present and PASS'"
