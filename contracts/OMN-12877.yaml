---
schema_version: "1.0.0"
ticket_id: "OMN-12877"
title: "[build] Guardrail: block raw provider/model/endpoint env authority"
summary: >-
  Tracks CI/pre-commit enforcement rejecting new provider, model, or endpoint routing authority sourced from
  raw environment variables or hardcoded literals.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-raw-env-authority-guard"
    description: >-
      Failing fixture trips the raw provider/model/endpoint env-authority guard; green fixture passes; CI
      is wired as enforcement.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12877/dod-raw-env-authority-guard/command.yaml"
