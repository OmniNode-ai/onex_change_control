---
schema_version: "1.0.0"
ticket_id: "OMN-12877"
title: "[build] Guardrail: block raw provider/model/endpoint env authority"
summary: >-
  Tracks CI/pre-commit enforcement rejecting new provider, model, or endpoint routing authority sourced
  from raw environment variables or hardcoded literals. Extended in M7.2 to add a residue audit over four
  confirmed env-var residue files (bridge_config_loader, cli_ab_compare_suite, model_policy.yaml, and
  cross-repo omnibase_infra service_llm_endpoint_health), baselined with ticket refs to prevent regression
  without requiring immediate remediation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-raw-env-authority-guard"
    description: >-
      Failing fixture trips the residue audit regression gate; green fixture passes; CI (routing-authority-demo-gate)
      is wired as enforcement. Pre-commit hook extended. Synthetic violation output recorded in docs/evidence/2026-06-12-weekend-pass/config-authority/ratchet/.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12877/dod-raw-env-authority-guard/command.yaml"
