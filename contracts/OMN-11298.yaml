---
schema_version: "1.0.0"
ticket_id: "OMN-11298"
title: "Bind omniintelligence PR #656 idna 3.15 receipt"
summary: "Bind omniintelligence PR #656 idna Dependabot lockfile update evidence."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omniintelligence uv lockfile is already resolved after the idna 3.15 update."
    command: "uv lock --check"
  - kind: "ci"
    description: "onex_change_control PR binds the OMN-11298 central contract and PR #656 receipt."
    command: "gh pr checks <occ-pr> --repo OmniNode-ai/onex_change_control"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Bind omniintelligence PR #656 idna lockfile evidence to its Dependabot head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11298/dod-001/command.yaml\"; else uv lock --check;
          fi"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-11298 central contract and PR #656 receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11298/dod-occ-pr/command.yaml\""
