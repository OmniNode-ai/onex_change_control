---
schema_version: '1.0.0'
ticket_id: OMN-10363
title: "Task 11: [Wave 3 Co-Change] Static import graph builder"
summary: "Add build_import_graph(repo_root) to omnibase_core analysis package, returning ImportGraph with
  edges_out mapping repo-relative paths to import targets for Python and JS/TS files"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "10 unit tests for import_graph pass (run in omnibase_core repo)"
    command: "gh pr checks 1001 --repo OmniNode-ai/omnibase_core"
  - kind: ci
    description: "mypy --strict and ruff check pass (verified via receipts)"
    command: "grep -q 'status: PASS' drift/dod_receipts/OMN-10363/dod-002/command.yaml && grep -q 'status:
      PASS' drift/dod_receipts/OMN-10363/dod-003/command.yaml"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "10 unit tests pass — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10363/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-10363$' drift/dod_receipts/OMN-10363/dod-001/command.yaml && grep -q '^evidence_item_id:
          dod-001$' drift/dod_receipts/OMN-10363/dod-001/command.yaml"
  - id: dod-002
    description: "mypy --strict clean — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10363/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-10363$' drift/dod_receipts/OMN-10363/dod-002/command.yaml && grep -q '^evidence_item_id:
          dod-002$' drift/dod_receipts/OMN-10363/dod-002/command.yaml"
  - id: dod-003
    description: "ruff check passes — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10363/dod-003/command.yaml && grep
          -q '^ticket_id: OMN-10363$' drift/dod_receipts/OMN-10363/dod-003/command.yaml && grep -q '^evidence_item_id:
          dod-003$' drift/dod_receipts/OMN-10363/dod-003/command.yaml"
