---
schema_version: "1.0.0"
ticket_id: "OMN-11138"
title: "Task 2: Receipt Model"
summary: >
  Central OCC evidence for omnibase_core PR #1090, which adds the typed OmniGate receipt and check-result
  models plus canonical receipt serialization and schema fingerprint helpers.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_core PR #1090 carries the OMN-11138 receipt model change."
    command: "gh pr view 1090 --repo OmniNode-ai/omnibase_core --json state,headRefOid"
  - kind: "tests"
    description: "Focused receipt model tests, lint, and mypy pass locally."
    command: "PYTHONPATH=src uv run pytest tests/unit/models/gate/ -q && PYTHONPATH=src uv run ruff check
      src/omnibase_core/gate src/omnibase_core/models/gate tests/unit/models/gate && PYTHONPATH=src uv
      run mypy src/omnibase_core/gate/receipt_canonical.py"
  - kind: "manual"
    description: "No runtime deploy is required for pure receipt model changes."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11138/dod-deploy-assessment/command.yaml"
  - kind: "manual"
    description: "OCC PR #1083 carries the central OMN-11138 contract and receipts."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11138/dod-occ-pr-1083/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1090"
    description: "Bind OMN-11138 evidence to omnibase_core PR #1090 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11138/dod-omnibase-core-pr-1090/command.yaml\"
          && grep -q '^pr_number: 1090$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11138/dod-omnibase-core-pr-1090/command.yaml\"
          && grep -q '^commit_sha: e1606f120b39dafabad8af1efe5e3c945cf84f91$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11138/dod-omnibase-core-pr-1090/command.yaml\""
  - id: "dod-focused-checks"
    description: "Focused receipt model tests, lint, and mypy pass locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11138/dod-focused-checks/command.yaml\"
          && grep -q '18 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11138/dod-focused-checks/command.yaml\"
          && grep -q 'Success: no issues found' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11138/dod-focused-checks/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "No live deploy, runtime restart, .201 mutation, or event-bus write was performed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11138/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11138/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-1083"
    description: "Bind OMN-11138 central evidence to OCC PR #1083."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11138/dod-occ-pr-1083/command.yaml\"
          && grep -q '^pr_number: 1083$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11138/dod-occ-pr-1083/command.yaml\""
