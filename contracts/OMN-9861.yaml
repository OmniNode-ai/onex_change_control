---
schema_version: 1.0.0
ticket_id: OMN-9861
title: "Proof-of-life evidence document and deferred live-validation runbook for change-aware CI"
summary: Proof-of-life evidence document and deferred live-validation runbook for change-aware CI
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'omnibase_core PR #941 CI checks pass'
    command: gh pr checks 941 --repo OmniNode-ai/omnibase_core --watch
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Proof-of-life evidence document exists at docs/evidence/change-aware-ci-proof-of-life.md
    source: generated
    checks:
      - check_type: command
        check_value: test -f docs/evidence/change-aware-ci-proof-of-life.md
  - id: dod-002
    description: Evidence document states DEFERRED and includes the open stack table for Path B
    source: generated
    checks:
      - check_type: command
        check_value: grep -q DEFERRED docs/evidence/change-aware-ci-proof-of-life.md && grep -q "Path
          B" docs/evidence/change-aware-ci-proof-of-life.md
  - id: dod-003
    description: Evidence document includes the follow-up runbook steps 1 through 7
    source: generated
    checks:
      - check_type: command
        check_value: "python3 - <<'PY'\nfrom pathlib import Path\ntext = Path(\"docs/evidence/change-aware-ci-proof-of-life.md\").read_text()\nfor
          n in range(1, 8):\n    assert f\"Step {n}\" in text or f\"{n}.\" in text, n\nprint(\"runbook
          steps present\")\nPY"
  - id: dod-004
    description: Pending evidence section has placeholders for all required proof assertions
    source: generated
    checks:
      - check_type: command
        check_value: grep -qi "pending evidence" docs/evidence/change-aware-ci-proof-of-life.md && grep
          -qi "placeholder" docs/evidence/change-aware-ci-proof-of-life.md
  - id: dod-005
    description: 'pre-commit run --all-files passes on PR #941'
    source: generated
    checks:
      - check_type: command
        check_value: pre-commit run --all-files
