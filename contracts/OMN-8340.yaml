---
schema_version: "1.0.0"
ticket_id: "OMN-8340"
title: "Remove deprecated onex handoff skill"
summary: >-
  OMN-8340 removes the deprecated /onex:handoff skill, its dedicated node_skill_handoff_orchestrator registration,
  and stale validator references. The skill already declared /onex:session as its replacement. The separate
  session-handoff document-writing and injection machinery remains intact because it is runtime state
  support, not the deprecated skill command.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1775"
    description: >-
      Per-code-PR binding receipt for omniclaude PR #1775 (jonahgabriel/omn-8340-remove-deprecated-handoff-skill,
      base=dev). The PR removes the deprecated handoff skill files, removes the orphaned shell-node registration,
      updates skill validator lists, and corrects the pr_review/pr_review_bot backing-node metadata required
      by the current omniclaude validator. This receipt binds OMN-8340 to the omniclaude PR by pr_number
      and commit_sha so downstream Receipt Gate can resolve central OCC evidence for the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-8340/dod-omniclaude-pr-1775/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate evidence for the plugin skill and omniclaude node registration paths touched by OMN-8340.
      No live deploy or runtime restart was performed pre-merge. The required operational step is a normal
      post-merge dev deploy/redeploy or plugin refresh followed by validation that /onex:handoff is absent
      and /onex:session remains available.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-8340/dod-deploy-assessment/command.yaml && grep
          -q 'post-merge dev deploy' drift/dod_receipts/OMN-8340/dod-deploy-assessment/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-8340 contract and omniclaude
      PR #1775 binding. This binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility
      check can resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-8340/dod-occ-pr-self/command.yaml
