---
schema_version: "1.0.0"
ticket_id: "OMN-10204"
summary: "Implement Pattern B broker process and terminal-event flow"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "topics"
  - "envelopes"
evidence_requirements:
  - kind: "tests"
    description: "Focused Pattern B broker process tests pass in omnibase_infra"
    command: "uv run pytest tests/unit/runtime/test_service_pattern_b_broker.py -q"
  - kind: "manual"
    description: "Broker lifecycle and terminal-result flow are documented in receipts"
    command: "See canonical receipts under drift/dod_receipts/OMN-10204/"
  - kind: "ci"
    description: "Downstream omnibase_infra PR checks are green"
    command: "gh pr checks --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Pattern B broker service starts, subscribes, and stops cleanly"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10204/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "Command to terminal-result round-trip is proven on the in-memory bus"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10204/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "Timeout and failure paths are explicit and produce structured broker results"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10204/dod-003 -type f -name '*.yaml' | grep -q ."
