---
schema_version: "1.0.0"
ticket_id: "OMN-14273"
title: "refactor(OMN-14273): canonicalize HandlerAntipatternValidatorOrchestrator to thin contract+handler"
summary: >-
  omnimarket product PR (branch jonah/omn-14273-canonicalize-antipattern-validator, head 0282479be9bcecd739eaad1bafc4ffff797282d4)
  canonicalizes HandlerAntipatternValidatorOrchestrator (node_semantic_antipattern_validator_orchestrator)
  to the thin contract+handler shape: handle() takes a single typed payload; the ORCHESTRATOR emits its
  events/intents. Public-safe generic architecture-hygiene work under epic OMN-14242.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      Independent landing verification at product head 0282479be9bcecd739eaad1bafc4ffff797282d4: node
      tests, mypy --strict on the handler, and the strict state-coverage gate all pass. Runner (implementer)
      is distinct from the verifier (landing verification), satisfying the receipt-honesty gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14273/dod-001/command.yaml && grep -q '0282479be9bcecd739eaad1bafc4ffff797282d4'
          drift/dod_receipts/OMN-14273/dod-001/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Honest deploy-surface assessment: HandlerAntipatternValidatorOrchestrator has no production callers
      today outside its node contract.yaml and tests. Pure in-module signature change; no live deploy
      required or performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14273/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-14273/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14273 contract and
      PASS receipts consumed by the product PR through its Evidence-Source line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14273/dod-occ-self/command.yaml
