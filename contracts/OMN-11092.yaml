---
schema_version: "1.0.0"
ticket_id: "OMN-11092"
title: "Wire generation consumer as real ONEX node in omnimarket"
summary: >
  Central OCC evidence for omnimarket PR #672. Adds node_generation_consumer as a proper ONEX node with
  contract.yaml, HandlerGenerationConsumer (LLM-driven node generation with retry), HandlerGeneratedExecutor
  (hot-deploy via Kafka event), metadata.yaml, and pyproject.toml entry-point. Ports logic from the hackathon
  pipeline in onex-self-extending-agent. 38 unit tests; no network/Kafka/Docker dependencies.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "38 unit tests pass for node_generation_consumer handlers and executor."
    command: "See drift/dod_receipts/OMN-11092/dod-unit-tests/command.yaml"
  - kind: "ci"
    description: "Omnimarket PR #672 is bound to OMN-11092."
    command: "See drift/dod_receipts/OMN-11092/dod-omnimarket-pr-672/command.yaml"
  - kind: "ci"
    description: "Deploy assessment — new node added; hot-deploy event wired in contract.yaml."
    command: "See drift/dod_receipts/OMN-11092/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "38 unit tests pass for node_generation_consumer handlers and executor."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-11092/dod-unit-tests/command.yaml && echo
          'OMN-11092 unit-test receipt present'"
  - id: "dod-omnimarket-pr-672"
    description: "Omnimarket PR #672 is bound to OMN-11092."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-11092/dod-omnimarket-pr-672/command.yaml &&
          echo 'OMN-11092 omnimarket-pr-672 receipt present'"
  - id: "dod-deploy-assessment"
    description: "Deploy assessment recorded — new node added with hot-deploy Kafka event topic."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-11092/dod-deploy-assessment/command.yaml &&
          echo 'OMN-11092 deploy-assessment receipt present'"
