---
schema_version: "1.0.0"
ticket_id: OMN-10440
title: "Implement Pattern B broker terminal event consumer"
summary: "Add broker-owned terminal event wait/correlation for Pattern B requests using enum-backed terminal
  events and contract-loaded topics."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Pattern B broker terminal consumer tests pass"
    command: "uv run pytest tests/unit/nodes/node_pattern_b_broker -q"
  - kind: "manual"
    description: "Touched Pattern B broker files pass lint"
    command: "uv run ruff check src/omnimarket/nodes/node_pattern_b_broker tests/unit/nodes/node_pattern_b_broker"
  - kind: "manual"
    description: "Ticket contract validates"
    command: "uv run validate-yaml contracts/OMN-10440.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Pattern B broker terminal consumer tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_pattern_b_broker -q"
  - id: "dod-002"
    description: "Touched Pattern B broker files pass lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_pattern_b_broker tests/unit/nodes/node_pattern_b_broker"
  - id: "dod-003"
    description: "Ticket contract validates"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run validate-yaml contracts/OMN-10440.yaml"
  - id: "dod-deploy"
    description: |
      Deploy posture for this slice. OMN-10440 adds a broker-owned terminal
      event wait/correlation adapter and EventBusInmemory tests for completed,
      failed, and timed-out flows. It does not add live Kafka wiring, process
      registration, shell execution, or runtime deployment hooks.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for OMN-10440 - terminal consumer adapter tested with EventBusInmemory
          only; follow-up tickets carry runtime deploy evidence'"
