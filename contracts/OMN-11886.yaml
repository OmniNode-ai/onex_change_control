---
schema_version: "1.0.0"
ticket_id: "OMN-11886"
title: "Add redpanda-partition-cap init service to dev compose"
summary: >
  Bind central evidence for omnibase_infra PR #1725, which adds the dev-lane Redpanda partition-cap one-shot
  init service and corresponding catalog manifest.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1725"
    description: "Bind OMN-11886 evidence to omnibase_infra PR #1725."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11886/dod-omnibase-infra-pr-1725/command.yaml
          && grep -q '^pr_number: 1725$' drift/dod_receipts/OMN-11886/dod-omnibase-infra-pr-1725/command.yaml"
  - id: "dod-catalog-completeness"
    description: "Catalog completeness and manifest schema tests pass after adding redpanda-partition-cap."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11886/dod-catalog-completeness/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence for dev Redpanda partition-cap init service."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11886/dod-deploy-readiness/command.yaml
          && printf '%s\\n' 'deploy evidence: redpanda partition-cap init service validated before merge'"
