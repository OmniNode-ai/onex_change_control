---
schema_version: "1.0.0"
ticket_id: "OMN-9159"
title: "Extend ModelBifrostRequest with session_id/run_id; make correlation_id required"
summary: >
  Binds omnibase_infra PR #1541, which makes correlation_id a required UUID field on ModelBifrostRequest
  and adds optional session_id (str | None, max_length=255) and run_id (str | None, max_length=255) fields
  for session-level identity propagation through the Bifrost gateway.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1541-head"
    description: "omnibase_infra PR #1541 carries the OMN-9159 implementation at the recorded head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9159/dod-pr-1541-head/command.yaml\""
  - id: "dod-unit-integration-tests"
    description: "Bifrost request unit and integration tests pass with session/run identity fields."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9159/dod-unit-integration-tests/command.yaml\""
  - id: "dod-ruff"
    description: "Changed Bifrost request model and focused tests pass ruff."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9159/dod-ruff/command.yaml\""
  - id: "dod-deploy-gate"
    description: "Repo-local deploy-gate validation passes with the OMN-9159 deploy evidence contract."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9159/dod-deploy-gate/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9159/dod-occ-pr/command.yaml\""
