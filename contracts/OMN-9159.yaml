---
schema_version: "1.0.0"
ticket_id: "OMN-9159"
title: "Extend ModelBifrostRequest with session_id/run_id; make correlation_id required"
summary: >
  Binds omnibase_infra PR #1541, which makes correlation_id a required UUID field on ModelBifrostRequest
  and adds optional session_id (str | None, max_length=255) and run_id (str | None, max_length=255) fields
  for session-level identity propagation through the Bifrost gateway.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1541"
    description: "omnibase_infra PR #1541 carries the OMN-9159 ModelBifrostRequest schema changes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1541 --repo OmniNode-ai/omnibase_infra --json state -q .state"
  - id: "dod-deploy-evidence"
    description: "Bifrost model change validated against the running gateway via docker exec health probe."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec omnibase-infra-redpanda rpk topic list | grep -q onex"
  - id: "dod-integration-tests"
    description: "Integration tests for session_id/run_id propagation and correlation_id enforcement pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1541 --repo OmniNode-ai/omnibase_infra --json statusCheckRollup -q '.statusCheckRollup[]
          | select(.name==\"Integration Test Coverage\") | .conclusion'"
  - id: "dod-occ-pr"
    description: "Bind this OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view --repo OmniNode-ai/onex_change_control --head jonah/omn-9159-9161-9162-occ-contracts
          --json state -q .state"
