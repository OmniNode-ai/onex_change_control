---
schema_version: "1.0.0"
ticket_id: OMN-8061
title: 'Task 3: Agent scope presets in omnibase_compat'
summary: "Task 3: Agent scope presets in omnibase_compat"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state -q .state | grep -qE 'OPEN|MERGED'"
  - id: "dod-002"
    description: "PR title contains OMN-8061"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json title -q .title | grep -q 'OMN-8061'"
  - id: "dod-003"
    description: "Tests pass in CI — validate job green"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --repo ${REPO} | grep -q 'validate.*pass'"
  - id: "dod-004"
    description: "Source file present in PR diff"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} --repo ${REPO} | grep -q 'model_agent_scope_presets'"
  - id: "dod-occ-validation"
    description: "OCC evidence PR validates against receipt gate"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --required"
