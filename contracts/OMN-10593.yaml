---
schema_version: "1.0.0"
ticket_id: "OMN-10593"
title: "register-repo omnimarket idempotency proof"
summary: >
  Add integration tests that prove scripts/register-repo.py onboard-repo --repo omnimarket is idempotent:
  dry-run succeeds without live infrastructure, and two consecutive --execute runs produce zero errors
  and zero duplicate secrets (second run reports only 'skipped'). Also fixes two pre-existing pre-commit
  failures: SPDX header in empty __init__.py and topic parity allowlist gap for onex.evt.platform.registration-completed.v1.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-integration-tests"
    description: "5 integration tests pass (3 runnable dry-run + 2 skipped pending admin token provisioning)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          if [ "$REPO" = "OmniNode-ai/onex_change_control" ]; then grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10593/dod-integration-tests/command.yaml";
          else uv run pytest tests/integration/scripts/test_register_repo_omnimarket.py -q; fi
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10593 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10593/dod-occ-pr/command.yaml"
