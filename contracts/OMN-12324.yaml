---
schema_version: "1.0.0"
ticket_id: "OMN-12324"
title: "Implement compute skill handlers for remaining node-backed market skills"
summary: >
  OCC evidence for omnimarket PR #953. The change implements the next safe deterministic compute subset
  from the remaining OMN-12324 nodes: node_insights_to_plan_compute and node_rrh_compute. Both nodes are
  pure deterministic transforms over declared request inputs and remove node_not_implemented only after
  focused contract/model tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-953-focused-tests"
    description: "omnimarket PR #953 focused tests and strict typing pass for the implemented deterministic
      compute subset."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12324/dod-omnimarket-pr-953-focused-tests/command.yaml
          && grep -q '47 passed' drift/dod_receipts/OMN-12324/dod-omnimarket-pr-953-focused-tests/command.yaml
          && grep -q 'Success: no issues found in 2190 source files' drift/dod_receipts/OMN-12324/dod-omnimarket-pr-953-focused-tests/command.yaml
  - id: "dod-omnimarket-pr-953-deploy-assessment"
    description: "No live deploy or runtime restart is required before merge for this pure compute handler
      slice."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12324/dod-omnimarket-pr-953-deploy-assessment/command.yaml
          && grep -q 'no live deploy or runtime restart required before merge' drift/dod_receipts/OMN-12324/dod-omnimarket-pr-953-deploy-assessment/command.yaml
  - id: "dod-occ-pr-1803-evidence"
    description: "onex_change_control PR carries the central OMN-12324 contract and PASS receipts for
      omnimarket PR #953."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12324/dod-occ-pr-1803-evidence/command.yaml
          && grep -q 'onex_change_control PR #1803' drift/dod_receipts/OMN-12324/dod-occ-pr-1803-evidence/command.yaml
