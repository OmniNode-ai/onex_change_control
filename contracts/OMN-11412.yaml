---
schema_version: "1.0.0"
ticket_id: "OMN-11412"
title: "chore(OMN-11412): propagate skip-token rejection hook"
summary: >
  Propagate local skip-token rejection enforcement across the active platform repositories so bracketed
  gate-bypass tokens are rejected before push and by the shared GitHub Actions gate. The batch also adds
  missing local pre-commit coverage where needed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "onex_change_control PR #1267 carries the central contract and receipt bundle."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-occ-pr-1267/command.yaml"
  - kind: ci
    description: "omnimarket PR #732 is bound to the OMN-11412 propagation batch."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-omnimarket-pr-732/command.yaml"
  - kind: ci
    description: "omniclaude PR #1626 is bound to the OMN-11412 propagation batch."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-omniclaude-pr-1626/command.yaml"
  - kind: ci
    description: "omnibase_core PR #1121 is bound to the OMN-11412 propagation batch."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-omnibase-core-pr-1121/command.yaml"
  - kind: ci
    description: "omnibase_compat PR #96 is bound to the OMN-11412 propagation batch."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-omnibase-compat-pr-96/command.yaml"
  - kind: ci
    description: "onex-self-extending-agent PR #77 is bound to the OMN-11412 propagation batch."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-onex-self-extending-agent-pr-77/command.yaml"
  - kind: ci
    description: "omnibase_infra PR #1688 is bound to the OMN-11412 propagation batch."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-omnibase-infra-pr-1688/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr-1267"
    description: "onex_change_control PR #1267 carries the central OMN-11412 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-occ-pr-1267/command.yaml"
  - id: "dod-omnimarket-pr-732"
    description: "omnimarket PR #732 propagates the local skip-token rejection hook and mypy pre-commit
      coverage."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-omnimarket-pr-732/command.yaml"
  - id: "dod-omniclaude-pr-1626"
    description: "omniclaude PR #1626 adds the additional statusline PR list coverage for the propagation
      batch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-omniclaude-pr-1626/command.yaml"
  - id: "dod-omnibase-core-pr-1121"
    description: "omnibase_core PR #1121 propagates the local skip-token rejection hook."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-omnibase-core-pr-1121/command.yaml"
  - id: "dod-omnibase-compat-pr-96"
    description: "omnibase_compat PR #96 propagates the local skip-token rejection hook."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-omnibase-compat-pr-96/command.yaml"
  - id: "dod-onex-self-extending-agent-pr-77"
    description: "onex-self-extending-agent PR #77 adds baseline pre-commit configuration and skip-token
      rejection."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-onex-self-extending-agent-pr-77/command.yaml"
  - id: "dod-omnibase-infra-pr-1688"
    description: "omnibase_infra PR #1688 propagates the local skip-token rejection hook."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11412/dod-omnibase-infra-pr-1688/command.yaml"
