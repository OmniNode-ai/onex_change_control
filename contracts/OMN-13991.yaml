---
schema_version: "1.0.0"
ticket_id: "OMN-13991"
title: "Autobind OCC evidence for OMN-13991"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1838."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1838 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1838"
    description: "PR #1838 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1838-ci"
    description: "PR #1838 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-OmniNode-ai-omnimarket-pr-1838-deploy"
    description: "PR #1838 on OmniNode-ai/omnimarket - deploy-scope evidence for runtime linear-triage
      handler change."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy gate scope: PR #1838 changes src/omnimarket/nodes/node_linear_triage/handlers/handler_linear_triage.py;
          staged rollout is flag-gated with OMNI_LINEAR_TRIAGE_STRICT_RECEIPT_MODEL default OFF and requires
          no runtime deployment before merge.'"
  - id: "occ-self-bind-pr-4496"
    description: "OCC companion PR #4496 — self-bind for OMN-13991 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4497"
    description: "OCC companion PR #4497 - self-bind for OMN-13991 deploy-scope amendment."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
