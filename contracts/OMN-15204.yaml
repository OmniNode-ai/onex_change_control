---
schema_version: "1.0.0"
ticket_id: "OMN-15204"
title: "Slim omnibase_core CLAUDE.md — correctness lane of org-wide repo CLAUDE.md refresh"
summary: >
  OCC companion evidence for omnibase_core PR #1514: slims CLAUDE.md from 17298 to 14178 bytes per context-engineering
  doctrine, correctness-first. Corrects false claims (nonexistent ContractConfigExtractor replaced with
  the real topic/config enforcers; "tests sequential by default" corrected to the -n4 addopts parallel
  default; drifted file-naming table replaced with a pointer to the enforced DIRECTORY_PREFIX_RULES map;
  incomplete test-marker list replaced with its two registration sources). Docstring Guidelines moved
  to docs/conventions/FILE_HEADERS.md with its inbound link retargeted.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Product PR binding and content probes for omnibase_core#1514."
    command: "gh pr view 1514 --repo OmniNode-ai/omnibase_core --json number,state,headRefName,headRefOid,files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1514-head-ac648504"
    description: >
      Consuming-PR binding: omnibase_core PR #1514 head ac6485040a9a94bb666cb9d049b555574013a7ac carries
      the slimmed, corrected CLAUDE.md plus the FILE_HEADERS.md docstring-guidelines relocation for OMN-15204.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1514 --repo OmniNode-ai/omnibase_core --json number,state"
  - id: "dod-core1514-claude-slim-ac648504"
    description: >
      Falsifiable product-head probe for omnibase_core PR #1514: CLAUDE.md at the pinned head points at
      the enforced DIRECTORY_PREFIX_RULES naming map and the real topic enforcer validator_hardcoded_topics,
      no longer mentions the nonexistent ContractConfigExtractor, and docs/conventions/FILE_HEADERS.md
      at the same head carries the relocated Docstring Guidelines section.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnibase_core/contents/CLAUDE.md?ref=ac6485040a9a94bb666cb9d049b555574013a7ac'
          --jq .content | base64 -d | grep -q 'DIRECTORY_PREFIX_RULES' && gh api 'repos/OmniNode-ai/omnibase_core/contents/CLAUDE.md?ref=ac6485040a9a94bb666cb9d049b555574013a7ac'
          --jq .content | base64 -d | grep -q 'validator_hardcoded_topics' && gh api 'repos/OmniNode-ai/omnibase_core/contents/docs/conventions/FILE_HEADERS.md?ref=ac6485040a9a94bb666cb9d049b555574013a7ac'
          --jq .content | base64 -d | grep -q 'Docstring Guidelines'
  - id: "dod-occ-self"
    description: >
      OCC companion self-bind for OMN-15204 (deterministic commit-sha bind, no PR-number prediction).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15204/dod-occ-self/command.yaml"
  - id: "occ-self-bind-pr-5054"
    description: "Binds OMN-15204 to OCC durability-rebind PR #5054 (self-bind for the merge-commit receipt
      supersession)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5054 --repo OmniNode-ai/onex_change_control --json number,state"
