---
schema_version: "1.0.0"
ticket_id: OMN-11616
title: "test(OMN-11616): guard duplicate CI scripts"
summary: "Add a regression test that scans non-empty CI script files and fails on any byte-identical group
  of three or more files."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Duplicate-CI-script regression test passes."
    command: "uv run pytest tests/test_no_duplicate_ci_scripts.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Duplicate-CI-script guard regression test passes."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_no_duplicate_ci_scripts.py -q"
