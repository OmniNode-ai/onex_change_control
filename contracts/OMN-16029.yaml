---
schema_version: "1.0.0"
ticket_id: "OMN-16029"
title: "Autobind OCC evidence for OMN-16029"
summary: >
  OCC contract for OmniNode-ai/omninode_infra PR #900 — deferred per-tenant broker quota state, migration-chain
  wiring for the tenants.acl_state and tenants.quota_state columns, and the onex-dev Keycloak-admin /
  quota-provider env wiring.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #900 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-900"
    description: "PR #900 on OmniNode-ai/omninode_infra — Evidence-Source bind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 900 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 900 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-900"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6477"
    description: "Binds OMN-16029 to OCC companion PR #6477 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6477 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-901"
    description: >-
      PR #901 on OmniNode-ai/omninode_infra — migrate-image digest repin so the tenants.acl_state and
      tenants.quota_state migrations actually execute on deploy. Follow-up to #900; separate PR because
      the ECR OIDC trust policy admits no feature branch.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 901 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 901 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-6483"
    description: "Binds OMN-16029 to OCC companion PR #6483 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6483 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-906"
    description: "PR #906 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 906 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-906-ci"
    description: "PR #906 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 906 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-6515"
    description: "OCC companion PR #6515 — self-bind for OMN-16029 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6515 --repo OmniNode-ai/onex_change_control --json number,state"
