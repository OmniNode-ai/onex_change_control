---
schema_version: "1.0.0"
ticket_id: OMN-10453
title: "Runtime boot validation for main-profile wire_from_manifest"
summary: "Add an integration gate proving the real installed contract manifest, filtered to the main runtime
  profile, completes wire_from_manifest without failures while including omnimarket contracts and excluding
  known memory/intelligence crashers."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
evidence_requirements:
  - kind: "manual"
    description: "Main-profile runtime boot auto-wiring validation passes"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10453/dod-runtime-boot-tests/command.yaml"
  - kind: "manual"
    description: "Runtime boot validation test passes ruff"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10453/dod-lint/command.yaml"
  - kind: "manual"
    description: "Repo-local ticket contract validates"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10453/dod-contract-validate/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-runtime-boot-tests"
    description: "Main-profile runtime boot auto-wiring validation passes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10453/dod-runtime-boot-tests/command.yaml
          && grep -q '^ticket_id: OMN-10453$' drift/dod_receipts/OMN-10453/dod-runtime-boot-tests/command.yaml
          && grep -q '^evidence_item_id: dod-runtime-boot-tests$' drift/dod_receipts/OMN-10453/dod-runtime-boot-tests/command.yaml"
  - id: "dod-lint"
    description: "Runtime boot validation test passes ruff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10453/dod-lint/command.yaml && grep
          -q '^ticket_id: OMN-10453$' drift/dod_receipts/OMN-10453/dod-lint/command.yaml && grep -q '^evidence_item_id:
          dod-lint$' drift/dod_receipts/OMN-10453/dod-lint/command.yaml"
  - id: "dod-contract-validate"
    description: "Repo-local ticket contract validates"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10453/dod-contract-validate/command.yaml
          && grep -q '^ticket_id: OMN-10453$' drift/dod_receipts/OMN-10453/dod-contract-validate/command.yaml
          && grep -q '^evidence_item_id: dod-contract-validate$' drift/dod_receipts/OMN-10453/dod-contract-validate/command.yaml"
