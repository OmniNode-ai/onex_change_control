---
schema_version: "1.0.0"
ticket_id: "OMN-14979"
title: "Autobind OCC evidence for OMN-14979"
summary: >
  OCC contract authored by node_occ_companion_compute/autobind for OMN-14979 product PRs: OmniNode-ai/omninode_infra#746
  and OmniNode-ai/omnimarket#1944.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #746 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "ci"
    description: "PR #1944 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-746"
    description: "PR #746 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/746/files --paginate --jq '.[] | select(.filename=="aws/cluster-dev/push_validation_bundles_s3.tf") | .patch' | grep -c 'push_validation_bundle_reader'
  - id: "dod-occ-evidence-admissibility-validator"
    description: >
      Supersedes the generated cross-repo PR-files probe, which is not executable in hosted onex_change_control
      CI because github.token cannot read OmniNode-ai/omninode_infra. Proves the unchanged OCC admissibility
      validator suite that enforces Evidence-Source checks for this companion.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-746"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5406"
    description: "Binds OMN-14979 to OCC companion PR #5406 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/onex_change_control/pulls/5406 --jq .number"
  - id: "dod-OmniNode-ai-omnimarket-pr-1944"
    description: "PR #1944 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_push_validation_effect/models/model_push_validation_request.py?ref=1e6a69233400c5028b35406be09f09a9814c8071 --jq '.content' | base64 -d | grep -c 'class ModelBundleRef'
  - id: "dod-OmniNode-ai-omnimarket-pr-1944-ci"
    description: "PR #1944 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5408"
    description: "OCC companion PR #5408 — self-bind for OMN-14979 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: dod-14979-infra746-bucket-rb15391
    description: 'Append-only replacement for dod-occ-evidence-admissibility-validator (OMN-15391). That
      entry''s prose says it supersedes the omninode_infra#746 cross-repo PR-files probe and ''proves
      the unchanged OCC admissibility validator suite'' -- true of the suite, and silent about OMN-14979.
      The superseded entry''s only check was ''uv run pytest tests/test_evidence_admissibility.py -q''.
      node_dod_verify''s EvidenceCollector._infer_occ_cwd resolves that command into the OCC checkout,
      where it runs OCC''s own OMN-15309 admissibility-predicate suite (76 tests, ~0.1s) and passes identically
      whether or not the product fact it is filed under is true. This entry binds the OMN-14979 product
      fact itself: the push-validation bundle transfer bucket and its reader IAM policy exist in aws/cluster-dev/push_validation_bundles_s3.tf
      at omninode_infra#746''s merge commit ba51bcd33cd59affd02d8458efcce741d8abc839, and did NOT exist
      at its first parent 564e5dba61f396c1be2bbc26949be8e7aba85fb7 -- the pair is fix-discriminating:
      revert the PR and check 1 goes RED. Scope limit stated plainly: OmniNode-ai/omninode_infra is a
      PRIVATE repository. This check was EXECUTED here on 2026-07-29 with a cross-repo-capable token and
      returned rc=0; under the OCC hosted Contract Compliance runner, whose GH_TOKEN is the repo-scoped
      github.token, the same endpoint returns HTTP 404 and the check goes RED. That RED is fail-closed
      ''cannot evaluate on this runner'', not a false claim about the product, and it is preferred over
      the surrogate''s vacuous GREEN (OMN-15391). node_dod_verify, which runs with an operator token,
      evaluates it for real.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-occ-evidence-admissibility-validator
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omninode_infra/contents/aws/cluster-dev/push_validation_bundles_s3.tf?ref=ba51bcd33cd59affd02d8458efcce741d8abc839''
          | grep -q ''push_validation_bundle_reader'''
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omninode_infra/contents/aws/cluster-dev/push_validation_bundles_s3.tf?ref=564e5dba61f396c1be2bbc26949be8e7aba85fb7''
          | grep -c ''push_validation_bundle_reader'' | grep -qx 0'
  - id: occ-self-bind-5408-lit-rb15391
    description: 'Append-only replacement for occ-self-bind-pr-5408 (OMN-15391 collateral repair). The
      superseded stamp''s id pins OCC companion PR #5408, but its check_value is the bare ${PR_NUMBER}
      runner placeholder, which the compliance runner pre-substitutes with whatever PR it is currently
      evaluating -- the OMN-15382 class-3 mis-binding, frozen in .onex_ratchets/omn_15382_rule_b_baseline.yaml.
      This entry pins the literal number so the stamp can only ever be true of the PR it names. Stated
      plainly: this is a binding/stamp item, not product proof -- it does not count toward the substance
      floor, and the product fact for this ticket is carried by the -rb15391 rebind item above.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:occ-self-bind-pr-5408
    checks:
      - check_type: command
        check_value: gh pr view 5408 --repo OmniNode-ai/onex_change_control --json number --jq '.number
          == 5408' | grep -qx true
  - id: dod-14979-market1944-scope-rb15391
    description: 'Append-only replacement for dod-OmniNode-ai-omnimarket-pr-1944-ci (OMN-15391 collateral
      repair). The superseded item''s id pins omnimarket#1944 while its check_value is ''gh pr view ${PR_NUMBER}
      --repo ${REPO} --json files'' -- a bare placeholder that resolves to the runner''s own PR in its
      own repo, so it never touched #1944 (OMN-15382 class-3, frozen in the Rule B baseline). This entry
      pins the literal PR on a PUBLIC repo and asserts the file the ticket''s bundle-transfer seam actually
      adds is in that PR''s diff, so it goes RED if the scope changes.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-1944-ci
    checks:
      - check_type: command
        check_value: gh api repos/OmniNode-ai/omnimarket/pulls/1944/files --paginate --jq '.[].filename'
          | grep -qx 'src/omnimarket/nodes/node_push_validation_effect/models/model_push_validation_request.py'
