---
schema_version: 1.0.0
ticket_id: OMN-7980
title: Wire OpenRouter as delegation backend for build loop code generation
summary: Typed OpenRouter backend selection added with local fallback behavior and tests.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - events
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Wave worker local verification evidence is captured in central OCC receipt.
    command: focused delegation/config pytest, ruff, mypy, env registry validation, validate_no_env_fallbacks.py
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-wave-local-evidence
    description: Wave worker local verification or readiness evidence is captured as a canonical PASS
      receipt.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: focused delegation/config pytest, ruff, mypy, env registry validation, validate_no_env_fallbacks.py
