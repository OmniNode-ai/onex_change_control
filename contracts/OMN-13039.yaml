---
schema_version: "1.0.0"
ticket_id: "OMN-13039"
title: "OMN-13039 — B-10: Epic auto-start ratchet + handoff epic-state gate"
summary: >-
  Retro B-10 (PROCESS_FAILURE_RETRO.md §3.B): epic OMN-12952 sat unstarted all day under 22 merged PRs;
  detected twice, fixed zero times; manually marked In Progress 2026-06-12. #magic___^_^___line Two changes
  ship together. (1) omnimarket PR #1242: adds Phase 5c (_phase_epic_autostart) to HandlerLinearTriage
  — unstarted epics (Backlog/Todo) with >=1 started/completed child are transitioned to In Progress on
  every triage sweep tick (monotone; never auto-Done). Adds MARK_IN_PROGRESS + WOULD_MARK_IN_PROGRESS
  to EnumTriageAction, epics_started field to ModelLinearTriageResult and ModelLinearTriageCompletedEvent.
  10 new TDD unit tests. (2) omniclaude PR #1767: adds required epic_states_reconciled gate to /onex:handoff
  skill — the gate verifies every cited epic is In Progress or later before writing the manifest; unstarted
  epics without an exception line block the write.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1242"
    description: >-
      Phase 5c epic auto-start ratchet: omnimarket PR #1242 (jonah/omn-13039-epic-autostart-ratchet).
      HandlerLinearTriage._phase_epic_autostart transitions Backlog/Todo epics with active/done children
      to In Progress. 17 unit tests green (10 new TDD tests for Phase 5c). Ruff + mypy --strict pass.
      Pre-commit all hooks pass on changed files.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13039/dod-omnimarket-pr-1242/command.yaml"
          && grep -q 'commit_sha:' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13039/dod-omnimarket-pr-1242/command.yaml"
  - id: "dod-omniclaude-pr-1767"
    description: >-
      Handoff epic-state gate: omniclaude PR #1767 (jonah/omn-13039-epic-autostart-ratchet). /onex:handoff
      SKILL.md and prompt.md gain required epic_states_reconciled gate. Pre-commit passes on changed files
      (lint-skill-mcp-refs, skill-hygiene, all other hooks). validate-skill-dispatch-receipt-mode fails
      identically on unmodified canonical files due to omnibase_core v0.43.0 pin; pre-existing, not caused
      by this change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13039/dod-omniclaude-pr-1767/command.yaml"
          && grep -q 'commit_sha:' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13039/dod-omniclaude-pr-1767/command.yaml"
