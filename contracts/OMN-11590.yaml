---
schema_version: "1.0.0"
ticket_id: "OMN-11590"
title: "Extract models/protocols from manifest_injector"
summary: >
  Central OCC evidence for omniclaude PR #1669 (Wave 4B Step 3). Extracts pure data structures (EnumTargetAgent,
  DisabledPattern, CacheMetrics, CacheEntry) from the 5573-line manifest_injector.py into a new models_manifest_injector.py.
  All four are StrEnum/dataclass definitions with no business logic dependencies. manifest_injector.py
  re-exports all extracted symbols via __all__ for full backward compatibility. 127 golden tests pass
  post-extraction (61 manifest_injector + 66 agent_router).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-tests"
    description: "127 golden tests pass after extraction. 61 from test_manifest_injector_golden.py covering
      ManifestInjector, ManifestInjectionStorage, ManifestCache. 66 from test_agent_router_golden.py."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11590/dod-golden-tests/command.yaml
          && grep -q '127 passed' drift/dod_receipts/OMN-11590/dod-golden-tests/command.yaml"
  - id: "dod-local-validation"
    description: "0 new ruff violations. 47 pre-existing BLE001 violations unchanged. Pre-commit passes
      (2 pre-existing hook failures confirmed on main)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11590/dod-local-validation/command.yaml"
  - id: "dod-omniclaude-pr"
    description: "omniclaude PR #1669 is MERGED. Branch jonah/omn-11590-extract-models-protocols-manifest-injector."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11590/dod-omniclaude-pr/command.yaml
          && grep -q 'MERGED' drift/dod_receipts/OMN-11590/dod-omniclaude-pr/command.yaml"
