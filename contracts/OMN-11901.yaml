---
schema_version: "1.0.0"
ticket_id: "OMN-11901"
title: "Wave 1 PR pipeline declarative contract migration"
summary: >
  Bind central evidence for omnimarket PR #806, which migrates PR pipeline nodes to declarative handler_routing
  contracts and updates the golden-chain guardrail for the PR watch orchestrator runtime routing surface.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-806"
    description: "Bind OMN-11901 evidence to omnimarket PR #806."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11901/dod-omnimarket-pr-806/command.yaml
          && grep -q '^pr_number: 806$' drift/dod_receipts/OMN-11901/dod-omnimarket-pr-806/command.yaml"
  - id: "dod-golden-chain-stub-routing"
    description: "Golden-chain stub-node routing guard passes after the declarative handler_routing migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11901/dod-golden-chain-stub-routing/command.yaml
          && grep -q '9 passed' drift/dod_receipts/OMN-11901/dod-golden-chain-stub-routing/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence for PR pipeline runtime contract migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11901/dod-deploy-readiness/command.yaml
          && grep -q 'runtime contract migration has focused golden-chain proof' drift/dod_receipts/OMN-11901/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr-1507"
    description: "OCC PR #1507 carries the central OMN-11901 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11901/dod-occ-pr-1507/command.yaml
          && grep -q '^pr_number: 1507$' drift/dod_receipts/OMN-11901/dod-occ-pr-1507/command.yaml"
