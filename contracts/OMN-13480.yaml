---
schema_version: "1.0.0"
ticket_id: "OMN-13480"
title: "Land SEA-generated todo-fixme-marker COMPUTE validator (corpus-accepted, shadow-clean)"
summary: >-
  Lands the generated, corpus-accepted agent-left TODO/FIXME/HACK unfinished-work-marker COMPUTE validator
  in omnibase_core (PR #1304) — the "agent lands what SEA generated" step. The scanning logic was generated
  by node_generation_consumer (omnimarket) against the live model (Qwen3.6-35B-A3B on the local-coder
  backend, routing_source=contract) and accepted only because it passed a deterministic acceptance corpus
  (corpus_todo_marker.TODO_MARKER_CORPUS) in the hardened sandbox on the first attempt — the corpus verdict,
  not the LLM, is the acceptance authority (OMN-13289). Mirrors the G1 local-paths + G2 private-IP COMPUTE
  validators: pure COMPUTE handler over a two-transport bus, wired blocking in .pre-commit-config.yaml
  + exported in .pre-commit-hooks.yaml (check-todo-fixme-marker-compute). This OCC evidence binds OMN-13480
  to omnibase_core PR #1304 and records that the PR adds no live deploy mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1304"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1304 at the current head. The receipt binds by
      pr_number and commit_sha for downstream Receipt Gate resolution. The PR lands the generated, corpus-accepted
      todo-fixme-marker COMPUTE validator (handler + models + two-transport runner), wired blocking, with
      real corpus re-run (5/5 violations flagged, 4/4 clean passed), PR-changed-source shadow-run (exit
      0), and fail-closed proof (planted TODO exit 1, clean revert exit 0).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-omnibase-core-pr-1304/command.yaml
  - id: "dod-deploy-no-live-mutation"
    description: >-
      Deploy-path evidence for OMN-13480. The PR adds a build-time repository validation gate (a COMPUTE
      node executed over the in-memory event bus at pre-commit time) plus its unit tests and provenance
      doc only. It adds no runtime node deploy, no Kafka topic in a live broker, no migration, no image-build
      input, no .201 mutation, and no live deploy. The two new validation topic constants (todo-marker-scan-requested/completed)
      are pre-commit/in-memory transport names, never published to a live broker.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-deploy-no-live-mutation/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13480 contract and receipts.
      Binds the OCC PR by pr_number so the OCC receipt-gate eligibility check can resolve a PASS receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-occ-pr-self/command.yaml
