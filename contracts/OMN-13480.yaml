---
schema_version: "1.0.0"
ticket_id: "OMN-13480"
title: "Validator residuals: generated TODO-marker gate and localhost URL authority coverage"
summary: >
  Binds the two OMN-13480 omnibase_core closeout PRs. PR #1304 landed the generated, corpus-accepted TODO/FIXME
  marker COMPUTE validator. PR #1312 closes the residual URL authority coverage gap by adding a blocking
  localhost-literal rule, proving planted loopback literals fail, and burning down rather than growing
  the URL authority baseline.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "URL authority validator tests pass with localhost-literal coverage."
    command: "uv run pytest tests/unit/validation/test_validator_url_authority.py -q"
  - kind: "ci"
    description: "PR branch CI and receipt/deploy gates pass before merge."
    command: "gh pr checks 1312 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1304"
    description: "omnibase_core PR #1304 carries the generated TODO/FIXME marker COMPUTE validator."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-omnibase-core-pr-1304/command.yaml"
  - id: "dod-omnibase-core-pr-1315"
    description: >-
      omnibase_core PR #1315 lands the dedicated SEA-generated, corpus-accepted hardcoded-localhost-URL
      COMPUTE validator (omnibase_core.validation.localhost_url) — the generate-first canonical path the
      OMN-13294 G2 DoD prescribes, matching the private_ip / pin_hygiene family. The loopback-URL scanning
      logic was generated by node_generation_consumer against the live local-coder model and accepted
      only because it passed corpus_hardcoded_localhost_url in the hardened sandbox. ALTERNATIVE to the
      url-authority localhost-literal rule bound from PR #1312: both close the same gap; #1315 is the
      dedicated COMPUTE validator with no grandfathering (all pre-existing sites triaged), #1312 bolts
      a rule onto url-authority. Operator decides which supersedes. Receipt binds by pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-omnibase-core-pr-1315/command.yaml"
  - id: "dod-omnibase-core-pr-1312"
    description: "omnibase_core PR #1312 carries the localhost-literal URL authority rule, model move,
      contract update, tests, and baseline burn-down."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-omnibase-core-pr-1312/command.yaml"
  - id: "dod-url-authority-localhost-rule"
    description: "Focused URL authority tests and full-tree gate pass with no new localhost URL violations."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-url-authority-localhost-rule/command.yaml"
  - id: "dod-g2ci-ci-gate"
    description: >-
      G2-CI residual for the generated TODO/FIXME marker COMPUTE validator: omnibase_core PR #1313 adds
      the standalone required CI workflow that runs the same in-memory-bus COMPUTE handler over changed
      files and fails closed on agent-left markers. The gate ran green on the PR branch, including corpus-pinned
      unit tests and changed-file scanning.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-g2ci-ci-gate/command.yaml"
  - id: "dod-g2ci-generation"
    description: >-
      Generation provenance for the TODO/FIXME marker validator: the scanning logic was generated through
      node_generation_consumer against the live local-coder model, then accepted by deterministic corpus
      evidence rather than LLM assertion.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-g2ci-generation/command.yaml"
  - id: "dod-g2ci-deploy-no-live-mutation"
    description: >-
      G2-CI deploy-path evidence: omnibase_core PR #1313 added CI workflow/provenance files only, with
      no live deploy, broker mutation, migration, image-build input, .201 mutation, or runtime restart.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-g2ci-deploy-no-live-mutation/command.yaml"
  - id: "dod-g2ci-occ-pr-self"
    description: >-
      OCC self-binding receipt for the G2-CI evidence refresh carrying the OMN-13480 PR #1313 CI-gate
      contract update and receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-g2ci-occ-pr-self/command.yaml"
  - id: "dod-deploy-no-live-mutation"
    description: "Validation code, tests, and baseline metadata only; no live deploy, broker mutation,
      migration, or runtime restart required before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-deploy-no-live-mutation/command.yaml"
  - id: "dod-occ-pr-self"
    description: "OCC self-binding receipt for the central evidence PR carrying the OMN-13480 residual
      contract update and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13480/dod-occ-pr-self/command.yaml"
