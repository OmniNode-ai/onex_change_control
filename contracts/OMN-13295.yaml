---
schema_version: "1.0.0"
ticket_id: "OMN-13295"
title: "feat(W6a): wire check_delegation_env_reads.py as pre-commit hook in omnimarket"
summary: >-
  W6(a) quick win from the validator-standardization remediation plan. Wires the existing omnimarket/scripts/ci/check_delegation_env_reads.py
  as a local pre-commit hook (delegation-env-read-gate) in omnimarket/.pre-commit-config.yaml. The script
  was already present but had zero hook entries. Provenance-bypass violations (raw reads of BIFROST_CONTRACT_PATH
  and sibling path-config keys outside delegation_config_provenance.py) always exit 1; broad delegation-module
  env reads run in report mode (Wave 0).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1271"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1271 (jonah/omn-13295-wire-delegation-env-gate, base=dev).
      Wires check_delegation_env_reads.py as delegation-env-read-gate hook in omnimarket/.pre-commit-config.yaml.
      Fail-closed proof: provenance-bypass violation exits 1; clean tree exits 0. This receipt binds OMN-13295
      to omnimarket PR #1271 by pr_number and commit_sha so downstream Receipt Gate can resolve central
      OCC evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13295/dod-omnimarket-pr-1271/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13295 contract and omnimarket
      PR #1271 binding. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check
      can resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13295/dod-occ-pr-self/command.yaml
