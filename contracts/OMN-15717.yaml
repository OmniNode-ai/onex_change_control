---
schema_version: "1.0.0"
ticket_id: "OMN-15717"
title: "OCC evidence for OMN-15717"
summary: >
  Central OCC evidence for OmniNode-ai/omnibase_infra PR #2678.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2678 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2678"
    description: "PR #2678 on OmniNode-ai/omnibase_infra - exact-head migration stream/domain declaration
      gate."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2678 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,files
          --jq 'if (.number == 2678 and .state == "OPEN" and .headRefName == "jonah/omn-15717-migration-stream-domain-declaration"
          and .headRefOid == "bb7762c9721423049a086c78b976021a94f9fc38" and ([.files[].path] | sort) ==
          [".github/workflows/ci.yml",".pre-commit-config.yaml","docker/migrations/forward/_ledger/application-migrations.tsv","docker/migrations/forward/nodes/node_pr_review_bot/001_create_review_bot_bypass_log.sql","scripts/ci/ci_summary_gate.py","scripts/sync-node-migrations.sh","tests/fixtures/omn15717/001_create_review_bot_bypass_log.sql.captured","tests/incident_replays/registry.yaml","tests/integration/migrations/test_application_migration_ledger_omn15413.py","tests/unit/migrations/test_node_migration_discovery.py","tests/unit/scripts/validation/test_application_migration_manifest.py"])
          then "true" else error("unexpected #2678 state, head, or file set") end'
  - id: "dod-occ-evidence-validator-pr-2678"
    description: "Hosted OCC evidence admissibility validator for OMN-15717 / PR #2678."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2678"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6153"
    description: "OCC companion PR self-binding stamp for OMN-15717."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-evidence-validator-pr-2678"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 6153 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid
          --jq 'if (.number == 6153 and .state == "OPEN" and .headRefName == "codex/omn-15717-occ-pr2678")
          then "true" else error("unexpected OCC self-bind PR state") end'
  - id: "dod-occ-evidence-admissibility-active-pr-6153"
    description: "Active OCC evidence admissibility check for OMN-15717 companion PR #6153."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-active-bind-pr-2678-5d4061c9"
    description: "PR #2678 on OmniNode-ai/omnibase_infra advanced beyond its original head with a new
      commit addressing CodeRabbit findings (IFS bug in scripts/run-forward-migrations.sh, two docstring
      examples). All prior dod_evidence entries for this ticket bind only to the merged OCC companion
      PR #6153, not to PR #2678 itself, which left OCC merge-eligibility unbound (pr_ticket_mismatch)
      for #2678's live head. This entry re-binds active evidence to PR #2678 at its current head."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-evidence-admissibility-active-pr-6153"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/2678 --jq 'if (.number == 2678 and .state == "open"
          and .head.ref == "jonah/omn-15717-migration-stream-domain-declaration" and .head.sha == "5d4061c9ce2b4ef85bcbddb58edbcdde2d4d189b")
          then "true" else error("unexpected #2678 state, branch, or head") end'
  - id: "occ-self-bind-pr-6188"
    description: "Active OCC companion PR self-binding stamp for OMN-15717 companion PR #6188."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-active-bind-pr-2678-5d4061c9"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/6188 --jq 'if (.number == 6188 and .state
          == "open" and .head.ref == "codex/omn-15717-active-bind-pr2678-v2-dev" and .head.sha == "736da45b534cc9a72fb197b80bd03cd4062b82d6")
          then "true" else error("unexpected OCC self-bind PR #6188 state, branch, or head") end'
  - id: "occ-self-bind-pr-6188-loose"
    description: "Loose existence/state re-bind for OCC companion PR #6188, replacing the prior head-SHA-pinned
      self-bind. The predecessor entry pins .head.sha to a specific commit and breaks (unexpected-state
      error) on every subsequent commit pushed to the companion branch -- exactly the structural trap
      this entry avoids: existence + open-state only, never head-equality on the companion's own PR."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-6188"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/6188 --jq 'if (.number == 6188 and .state
          == "open") then "true" else error("unexpected OCC self-bind PR #6188 state") end'
  - id: "dod-active-bind-pr-2678-3eaf3c03"
    description: "PR #2678 on OmniNode-ai/omnibase_infra advanced again: the OMN-15717 reland lane rebased
      the branch onto a then-current origin/dev (afbba99c) and force-pushed, moving the head from 5d4061c9c
      to 3eaf3c030. occ-preflight/eligibility reason=pr_ticket_mismatch confirmed no PASS receipt bound
      to the new head (or PR #2678 generally): missing_or_nonpass_receipts=[] but receipt_ids only cite
      the merged-companion-bound occ-self-bind-pr-6153 / dod-occ-evidence-admissibility-active-pr-6153,
      and the live dod-active-bind-pr-2678-5d4061c9 entry is now stale (pins the pre-rebase head). This
      entry re-binds active evidence to PR #2678's new head."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-active-bind-pr-2678-5d4061c9"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/2678 --jq 'if (.number == 2678 and .state == "open"
          and .head.ref == "jonah/omn-15717-migration-stream-domain-declaration" and .head.sha == "3eaf3c03012cf4ebfbdc0b7d92e4dd8ddbb158db")
          then "true" else error("unexpected #2678 state, branch, or head") end'
  - id: "dod-deploy-scope-omn15717-app-migrations"
    description: "Deploy-scope DoD evidence (OMN-14681 / omnibase_infra#2319 gap) for OMN-15717. PR #2678's
      full diff (grown via the legacy-node-ledger follow-up commit) touches deploy-scoped runtime paths
      under src/omnibase_infra/nodes/**/handlers and src/omnibase_infra/services/**, so the hosted deploy-gate
      and its OMN-14681 local pre-push mirror both require a falsifiable deploy-scope probe in this contract.
      This probe reads the actual declaration row PR #2678 introduced in docker/migrations/forward/_ledger/application-migrations.tsv,
      live from GitHub's contents API at the PR's then-current head (5d4061c9c, still resolvable after
      later rebases since the object stays reachable via the PR's commit history) -- falsifiable: it fails
      RED against any tree lacking the node_pr_review_bot declaration line, and it observes the product
      repo's committed content, not this OCC PR's own diff."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/_ledger/application-migrations.tsv?ref=5d4061c9ce2b4ef85bcbddb58edbcdde2d4d189b
          --jq '.content' | base64 -d | grep -c 'node:node_pr_review_bot'
  - id: "dod-active-bind-pr-2678-4eed3d91"
    description: "PR #2678 on OmniNode-ai/omnibase_infra advanced again: the reland lane pushed a follow-up
      commit fixing two real CI-gate findings surfaced on the reopened/rebased PR (Application Database
      Domain Enforcement OMN-15361 schema-qualification exemption for the vendored node_pr_review_bot
      SQL, plus a Contract Sync Gate Wave C touch for node_setup_validate_effect), moving the head from
      3eaf3c030 to 4eed3d912. Re-binds active evidence to PR #2678's new head."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-active-bind-pr-2678-3eaf3c03"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/2678 --jq 'if (.number == 2678 and .state == "open"
          and .head.ref == "jonah/omn-15717-migration-stream-domain-declaration" and .head.sha == "4eed3d912ec5b5fcb295902d9bdabd38a30e246a")
          then "true" else error("unexpected #2678 state, branch, or head") end'
  - id: "occ-self-bind-pr-6305-loose"
    description: "Loose existence/state self-bind for successor OCC companion PR #6305 (the append-only
      successor to merged PR #6188), matching the occ-self-bind-pr-6188-loose pattern: existence + open-state
      only, never head-equality on the companion's own PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/6305 --jq 'if (.number == 6305 and .state
          == "open") then "true" else error("unexpected OCC self-bind PR #6305 state") end'
  - id: "occ-self-bind-pr-6307-loose"
    description: "Loose existence/state self-bind for successor OCC companion PR #6307 (the append-only
      successor to merged PR #6305), matching the occ-self-bind-pr-6188-loose pattern: existence + open-state
      only, never head-equality on the companion's own PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/6307 --jq 'if (.number == 6307 and .state
          == "open") then "true" else error("unexpected OCC self-bind PR #6307 state") end'
