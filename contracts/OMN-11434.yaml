---
schema_version: "1.0.0"
ticket_id: "OMN-11434"
title: "Propagate context bundle to event chain display"
summary: >
  Central OCC evidence for the onex-self-extending-agent context bundle display propagation. The PR adds
  context_display to run_agent_demo output by extracting ModelContextBundle rows from generation-completed
  events and verifies bundle serialization through the event chain.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-145"
    description: "onex-self-extending-agent PR #145 carries the OMN-11434 context bundle propagation changes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11434/dod-sea-pr-145/command.yaml"
  - id: "dod-sea-unit-tests"
    description: "Focused context bundle tests, lint, mypy, and full unit suite passed on the cleaned
      dev-based branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11434/dod-sea-unit-tests/command.yaml"
  - id: "dod-occ-pr-1765"
    description: "OCC PR #1765 binds the central OMN-11434 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11434/dod-occ-pr-1765/command.yaml"
