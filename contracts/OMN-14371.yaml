---
schema_version: "1.0.0"
ticket_id: "OMN-14371"
title: "RSD mechanical-wave canary: node_projection_replay_check_compute check()->handle() (golden-equivalence
  proven)"
summary: >-
  omnimarket product PR #1714 (branch jonah/omn-14371-canary-projection-replay, head c4af019a4c42e1f07be03410b16bd6c97e553206)
  is the FIRST real flip of the canonical handler-shape ratchet (OMN-14355 / core #1428) outside omnibase_core
  — the template proof for the ~151-node omnimarket mechanical wave, built on the enablement gaps OMN-14368
  landed (parameterized classifier, core #1429; vendored compute_golden.py + omnimarket canon-shape baseline,
  omnimarket #1713). The PR renames HandlerProjectionReplayCheck.check -> handle (method only, identical
  body/signature; no contract.yaml change — LocalRuntimeBusAdapter.on_message hardcodes self.handler.handle),
  removes the now-dead _ReplayCheckBusHandler test-only shim, records 7 legacy-behavior goldens on disk
  BEFORE the codemod via the vendored compute_golden.record_golden, proves byte-equivalence of all 7 through
  the new handle() (zero diffs), and produces a real coverage-guided ModelAdequacyReceipt (80.0% branch
  coverage, meets the 80.0% target, no waiver) plus a green equivalence-replay artifact via a newly-vendored
  scripts/ci/adequacy_receipt.py, satisfying canonical_handler_shape.py's 3-part flip gate. The omnimarket
  baseline is regenerated SCOPED to exactly this one flip (single-line diff), not a blind --update, because
  a full regen would have silently folded in an unrelated pre-existing gap (node_hybrid_codegen_orchestrator
  missing from the #1713 baseline — filed separately as OMN-14372, not fixed in this PR).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      SELF-AUTHORED by the implementing agent (build-canary) — NOT an independent verification. Flagged
      for independent verify-canary sign-off before Codex lands this PR; the self-attestation (runner
      == verifier) mechanically auto-downgrades this receipt's status to ADVISORY per ModelDodReceipt.enforce_adversarial_invariants
      (Rule 1) regardless of the PASS value recorded below, which is the correct, honest signal that this
      proof has not yet been independently re-derived. Underlying technical claim: at product head c4af019a4c42e1f07be03410b16bd6c97e553206,
      the check()->handle() rename is behavior-preserving. 7 goldens recorded from the LEGACY check()
      method (serialized on disk under tests/fixtures/golden/node_projection_replay_check_compute/*.json)
      all replay byte-equivalent through the NEW handle() (compare_output == [] for all 7, test_projection_replay_check_golden_equivalence.py).
      The coverage-guided adequacy receipt (scripts/ci/adequacy_receipts/omnimarket.nodes.node_projection_replay_check_compute.json)
      measures 80.0% branch coverage against a 80.0% target (meets_target=true, no waiver) over 2 of 7
      candidates selected by the greedy covering algorithm; the sibling .equivalence.json artifact proves
      status=pass for exactly those 2 selected_input_hashes. The parameterized canonical_handler_shape.py
      classifier (run with --package omnimarket --src-root <worktree>/src --baseline <worktree>/scripts/ci/canonical_handler_shape_baseline.py
      --receipts-dir <worktree>/scripts/ci/adequacy_receipts --full --json) classifies the node category=canonical,
      is_canonical=true, detail=magic:request, and the node no longer appears in the WARN or FAIL sections
      of a non-JSON run. All 17 legacy unit tests, all 7 bus-coverage integration tests (driving the real
      handler directly, shim removed), 5 vendored adequacy_receipt.py smoke tests, and 5 vendored compute_golden.py
      smoke tests pass (42 total). ruff format/check and mypy --strict are clean on all changed files.
      pre-commit run --all-files is clean on the changeset (OMNIBASE_INFRA_PATH env var required for the
      topic-naming-lint hook under a worktree checkout — pre-existing repo quirk).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14371/dod-001/command.yaml && grep -q '^pr_number:
          1714$' drift/dod_receipts/OMN-14371/dod-001/command.yaml && grep -q 'c4af019a4c42e1f07be03410b16bd6c97e553206'
          drift/dod_receipts/OMN-14371/dod-001/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-surface assessment: product PR #1714 is a pure code-only rename inside one existing COMPUTE
      node (method rename + test/fixture/baseline/vendored-script changes). No new runtime wiring, no
      topic changes, no contract.yaml change, no live deploy, restart, .201 mutation, or migration was
      performed or is required by this OCC companion.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14371/dod-deploy-assessment/command.yaml &&
          grep -qi 'no.*deploy' drift/dod_receipts/OMN-14371/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the net-new OMN-14371 central contract
      and receipts consumed by omnimarket#1714 through its Evidence-Source line. Self-binds deterministically
      via the first commit's SHA for occ-preflight eligibility (no PR-number prediction).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14371/dod-occ-self/command.yaml
