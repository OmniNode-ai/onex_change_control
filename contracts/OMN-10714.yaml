---
schema_version: "1.0.0"
ticket_id: "OMN-10714"
title: "Declare miscellaneous handler config in contract.yaml"
summary: >
  Central OCC evidence for omnimarket PR #655 and omnibase_infra PR #1606. The downstream PRs declare
  miscellaneous handler environment configuration as typed contract dependencies and verify that contract-config
  extraction covers the affected keys.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #655 carries typed contract dependencies for OMN-10714 handler config."
    command: "See drift/dod_receipts/OMN-10714/dod-omnimarket-pr-655/command.yaml"
  - kind: "ci"
    description: "omnibase_infra PR #1606 carries typed contract dependencies for OMN-10714 handler config."
    command: "See drift/dod_receipts/OMN-10714/dod-omnibase-infra-pr-1606/command.yaml"
  - kind: "tests"
    description: "Focused local validators and extractor checks pass for both downstream repos."
    command: "See drift/dod_receipts/OMN-10714/dod-focused-validation/command.yaml"
  - kind: "manual"
    description: "Deploy assessment confirms this is contract metadata only and needs no live runtime
      restart."
    command: "See drift/dod_receipts/OMN-10714/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-655"
    description: "omnimarket PR #655 is open and carries the OMN-10714 contract dependency declarations."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10714/dod-omnimarket-pr-655/command.yaml
          && grep -q '^pr_number: 655$' drift/dod_receipts/OMN-10714/dod-omnimarket-pr-655/command.yaml"
  - id: "dod-omnibase-infra-pr-1606"
    description: "omnibase_infra PR #1606 is open and carries the OMN-10714 contract dependency declarations."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10714/dod-omnibase-infra-pr-1606/command.yaml
          && grep -q '^pr_number: 1606$' drift/dod_receipts/OMN-10714/dod-omnibase-infra-pr-1606/command.yaml"
  - id: "dod-focused-validation"
    description: "Focused YAML parse, missing_contract_config, extractor, tests, and whitespace checks
      passed locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10714/dod-focused-validation/command.yaml
          && grep -q '25 passed' drift/dod_receipts/OMN-10714/dod-focused-validation/command.yaml && grep
          -q '84 passed' drift/dod_receipts/OMN-10714/dod-focused-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Deploy DoD evidence for PRs #655 and #1606: contract metadata declarations only, with no live deploy,
      runtime restart, .201 mutation, topic change, or database migration required before merge.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10714/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-10714/dod-deploy-assessment/command.yaml"
