---
schema_version: "1.0.0"
ticket_id: "OMN-10714"
title: "Declare miscellaneous handler config in contract.yaml"
summary: >
  Declares the OMN-10714 miscellaneous handler environment variables as typed contract dependencies so
  contract-config discovery can see Linear, Slack, Gmail, worktree, OCC, conflict/rebase, filesystem,
  pricing, and delegation task-class configuration.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-misc-contract-deps"
    description: "omnimarket PR #655 declares the OMN-10714 miscellaneous handler environment dependencies."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10714/dod-omnimarket-misc-contract-deps/command.yaml"
  - id: "dod-infra-misc-contract-deps"
    description: "omnibase_infra PR #1606 declares the OMN-10714 miscellaneous handler environment dependencies."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10714/dod-infra-misc-contract-deps/command.yaml"
  - id: "dod-local-verification"
    description: "Focused local verification passed for the OMN-10714 contract dependency declarations."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10714/dod-local-verification/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10714 central contract and downstream receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10714/dod-occ-pr/command.yaml"
