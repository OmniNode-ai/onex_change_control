---
schema_version: "1.0.0"
ticket_id: "OMN-15060"
title: "Autobind OCC evidence for OMN-15060"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3193.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3193 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3193 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/unit/scripts/test_prepush_host_table.py tests/unit/scripts/test_prepush_smart_tests_seam.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3193"
    description: "PR #3193 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/unit/scripts/test_prepush_host_table.py?ref=98e59b01cfd5f04e752d0c01d4f2bad6cfc52be4 --jq '.content' | base64 -d | grep -c 'def test_runtime_sized_impacted_selection_reuses_the_heavy_slot_and_cleanup'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3193/files --paginate --jq '.[].filename' | grep -cFx 'scripts/hooks/prepush_smart_tests.sh'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3193 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/scripts/test_prepush_host_table.py tests/unit/scripts/test_prepush_smart_tests_seam.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-8227"
    description: "Binds OMN-15060 to OCC companion PR #8227 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8227 --repo OmniNode-ai/onex_change_control --json number,state"
