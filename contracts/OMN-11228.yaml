---
schema_version: "1.0.0"
ticket_id: "OMN-11228"
title: "Register session phase topics in TopicBase"
summary: >-
  Add SESSION_PHASE_EVALUATED (evt), SESSION_PHASE_TRANSITION (cmd), SESSION_PHASE_STATE (evt), SESSION_PHASE_BUDGET_WARNING
  (evt), and SESSION_HALT_REQUIRED (cmd) to the TopicBase enum in omnibase_core following the onex.{cmd|evt}.omnimarket.session-phase-*.v1
  naming convention.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: FULL
evidence_requirements:
  - kind: tests
    description: "Session phase topic tests pass with all 5 new TopicBase members"
    command: >-
      PYTHONPATH=/Users/jonah/Code/omni_worktrees/OMN-11228/omnibase_core/src uv run pytest tests/unit/test_session_phase_topics.py
      -v
  - kind: ci
    description: "CI pipeline green on omnibase_core PR #1105"
    command: "gh pr checks 1105 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-core-pr-1105
    description: "omnibase_core PR #1105 open with 5 session phase topics registered in TopicBase"
    source: generated
    checks:
      - check_type: command
        check_value: >-
          test -f "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11228/dod-core-pr-1105/command.yaml" && grep
          -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11228/dod-core-pr-1105/command.yaml"
          && grep -q '^pr_number: 1105$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11228/dod-core-pr-1105/command.yaml"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for this OCC PR — anchors evidence chain."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11228/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-11228"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11228/dod-occ-pr/command.yaml"
