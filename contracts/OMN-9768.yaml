---
schema_version: "1.0.0"
ticket_id: OMN-9768
title: "[OMN-9757 child] Task 11: Implement validate_contract_file() with mode selection"
summary: "Phase 3 central seam — implement validate_contract_file() with STRICT and MIGRATION_AUDIT modes.
  Entry point for both strict CI validation and migration_audit corpus sweeps."
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #1035 CI checks pass"
    command: "gh pr checks 1035 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "contract_validator.py exists in src/omnibase_core/normalization/"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9768/dod-001/command.yaml && grep
          -q '^evidence_item_id: dod-001$' drift/dod_receipts/OMN-9768/dod-001/command.yaml && grep -q
          '^ticket_id: OMN-9768$' drift/dod_receipts/OMN-9768/dod-001/command.yaml"
  - id: dod-002
    description: "13 unit tests pass for validate_contract_file (STRICT, MIGRATION_AUDIT, bucket skipping,
      aliases, pre-checks)"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9768/dod-002/command.yaml && grep
          -q '^evidence_item_id: dod-002$' drift/dod_receipts/OMN-9768/dod-002/command.yaml && grep -q
          '^ticket_id: OMN-9768$' drift/dod_receipts/OMN-9768/dod-002/command.yaml"
  - id: dod-003
    description: "mypy --strict passes on normalization/contract_validator.py with no errors"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9768/dod-003/command.yaml && grep
          -q '^evidence_item_id: dod-003$' drift/dod_receipts/OMN-9768/dod-003/command.yaml && grep -q
          '^ticket_id: OMN-9768$' drift/dod_receipts/OMN-9768/dod-003/command.yaml"
  - id: dod-occ-validation
    description: "OCC evidence contract validates and binds this evidence PR"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9768/dod-occ-validation/command.yaml
          && grep -q '^pr_number: 713$' drift/dod_receipts/OMN-9768/dod-occ-validation/command.yaml"
