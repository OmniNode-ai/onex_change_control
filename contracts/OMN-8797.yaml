---
schema_version: '1.0.0'
ticket_id: OMN-8797
title: 'test(omnibase_core): hermetic integration tests for onex health CLI'
summary: 'Add 20 hermetic integration tests for onex health CLI command. All external services mocked
  — no network access required.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: 20 hermetic integration tests pass
    command: uv run pytest tests/integration/test_onex_health_hermetic.py -v -n 0
  - kind: ci
    description: CI pipeline green on PR
    command: gh pr checks 1020 --repo OmniNode-ai/omnibase_core
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Hermetic integration test file present in omnibase_core PR 1020 diff
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 1020 --repo OmniNode-ai/omnibase_core | grep -q 'test_onex_health_hermetic'
  - id: dod-002
    description: All 20 tests pass locally with zero network access (Kafka mocked)
    source: manual
    checks:
      - check_type: command
        check_value: uv run pytest tests/integration/test_onex_health_hermetic.py -v -n 0 --timeout=30
          2>&1 | grep -q '20 passed'
