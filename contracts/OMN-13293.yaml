---
schema_version: "1.0.0"
ticket_id: "OMN-13293"
title: "feat(generation): canary — generate hardcoded-absolute-path COMPUTE validator, accept vs validator_local_paths.py
  on ARCH-005 corpus, two-transport (G1)"
summary: >-
  G1 canary of the validator-standardization plan. The hardcoded-absolute-path validator is GENERATED
  by node_generation_consumer (omnimarket), local-model-first via the real delegation chain (Qwen3.6-35B-A3B
  on the local-coder .201 backend, usage_source=measured, first-attempt success), and accepted ONLY because
  it reproduces the hand-authored ground truth validator_local_paths.py across a deterministic fixture
  corpus (14 violation + 5 mutation + 11 clean fixtures whose verdicts are derived from the ground truth;
  equivalence proven on all 25, 0 mismatches; shadow-bake 0 false-positives on 6999 real fleet files).
  The generated source lands in omnibase_core (#1278) as a COMPUTE handler on ProtocolMessageHandler,
  wired two-transport (in-memory bus at pre-commit, Kafka in CI). The omnimarket PR (#1280) makes the
  generator's handler-security pre-filter corpus-aware so a path-detector validator (which must embed
  path literals) can be generated at all — the integration problem this dogfood run surfaced.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr-2800"
    description: >-
      OCC self-binding receipt for onex_change_control PR #2800. The PR replaces #2799 with a branch name
      that satisfies identity binding for OMN-13291 and carries the central evidence update for OMN-13293
      (generated hardcoded-absolute-path COMPUTE validator and bus wiring), including active omnibase_core
      PR #1280 binding receipts. This receipt binds the OCC evidence PR itself by pr_number so its own
      OCC preflight and Receipt Gate can pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13293/dod-occ-pr-2800/command.yaml
  - id: "dod-occ-pr-2799"
    description: >-
      OCC self-binding receipt for onex_change_control PR #2799. The PR carries the central evidence update
      for OMN-13293 (generated hardcoded-absolute-path COMPUTE validator and bus wiring), including active
      omnibase_core PR #1280 binding receipts. This receipt binds the OCC evidence PR itself by pr_number
      so its own OCC preflight and Receipt Gate can pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13293/dod-occ-pr-2799/command.yaml
  - id: "dod-omnibase_core-pr-1280"
    description: >-
      Consolidation binding receipt for omnibase_core PR #1280. The PR carries the active validator-fleet
      consolidation for OMN-13293 (generated hardcoded-absolute-path COMPUTE validator and bus wiring)
      after superseding closed PR #1279. This receipt binds the central OCC contract to the active consolidated
      citing PR by pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13293/dod-omnibase_core-pr-1280/command.yaml
  - id: "dod-omnibase_core-pr-1279"
    description: >-
      Consolidation binding receipt for omnibase_core PR #1279. The PR unions the conflict-coupled validator-fleet
      additions for OMN-13293 (generated hardcoded-absolute-path COMPUTE validator and bus wiring) into
      a single queue-safe branch. This receipt binds the central OCC contract to the consolidated citing
      PR by pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13293/dod-omnibase_core-pr-1279/command.yaml
  - id: "dod-omnibase-core-pr-1278"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1278 (jonah/omn-13293-canary-localpaths-validator,
      base=dev). Lands the generated hardcoded-path COMPUTE validator (validation/local_paths: HandlerLocalPathsCompute
      on ProtocolMessageHandler, two-transport runner over the in-memory bus) plus the BLOCKING check-local-paths-compute
      pre-commit hook + .pre-commit-hooks.yaml export. Binds OMN-13293 to the core PR by pr_number and
      commit_sha so the Receipt Gate can resolve central OCC evidence for the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13293/dod-omnibase-core-pr-1278/command.yaml
  - id: "dod-omnibase-core-pr-1278-deploy"
    description: >-
      Deploy-path DoD evidence for the runtime-path validator landed in omnibase_core PR #1278. The validator
      is a canonical COMPUTE node executed over the event-bus runtime (in-memory backend at pre-commit,
      Kafka backend in CI). The deploy-relevant behavior is that the gate FAILS CLOSED: a planted hardcoded
      path makes the bus-executed validator exit non-zero naming the violation, and revert restores green
      — proven deterministically by the runner (verifier != runner). No .201 deploy is required for a
      build-time gate executed over the in-memory bus backend; the cold-start was benchmarked at/below
      the direct-script time (no warmed profile needed).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13293/dod-omnibase-core-pr-1278-deploy/command.yaml
          # deploy-path: bus-executed validator fails closed on a planted violation, green on revert
  - id: "dod-omnimarket-pr-1280"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1280 (jonah/omn-13293-canary-driver, base=dev). Makes
      node_generation_consumer's handler-security pre-filter corpus-aware (_validate_generation / _check_handler_security
      take is_validator_generation) so a validator-generation run can produce a hardcoded-path detector
      that legitimately embeds path literals; the topic-literal check stays unconditional. Binds OMN-13293
      to the omnimarket PR by pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13293/dod-omnimarket-pr-1280/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13293 contract and the omnibase_core
      #1278 + omnimarket #1280 binding receipts. Binds the OCC PR itself by pr_number so the OCC receipt-gate
      eligibility check can resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13293/dod-occ-pr-self/command.yaml
