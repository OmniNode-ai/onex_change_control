---
schema_version: "1.0.0"
ticket_id: "OMN-16799"
title: "Autobind OCC evidence for OMN-16799"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #1034.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1034 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1034"
    description: "PR #1034 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1034 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 1034 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1034"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7342"
    description: "Binds OMN-16799 to OCC companion PR #7342 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7342 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1044"
    description: >-
      PR #1044 on OmniNode-ai/omninode_infra — Evidence-Source bind for the gated api.omninode.ai hostname
      reclaim. Hand-authored rather than autobound: node_occ_companion_compute's F-17 guard deliberately
      suppresses companion authoring while the product PR is a draft or is title-marked do-not-merge/WIP,
      and #1044 is both (DRAFT, title carries "[DO NOT MERGE — awaiting grant]") by design, because it
      must not become mergeable before the OMN-16799 prod-promotion grant lands on onex_change_control@main.
      The suppression is correct; this entry supplies the Evidence-Source the receipt gate needs without
      un-drafting #1044 to get it.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1044 --repo OmniNode-ai/omninode_infra --json number,state,isDraft,headRefName,files"
  - id: "dod-omn-16799-reclaim-behavior-ratchet"
    description: >-
      Behavior proof for the surface #1044 changes, hand-authored rather than owed. #1044 adds tests/k8s/test_omn_16799_api_hostname_reclaim.py,
      8 tests pinning the fail-closed shape of the reclaim_api_hostname input: opt-in default false, runtime-only
      refusal cross-checked against the OMN-14209 guard it depends on, both steps gated on the input,
      the delete chained to the precondition step's outcome, the three preconditions probed, and a ratchet
      asserting no ungated `delete ingress` exists anywhere in the workflow. This is a real behavior-class
      check on the changed surface, not a ticket-independent suite standing in for one (OMN-16434).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/k8s/test_omn_16799_api_hostname_reclaim.py -q"
