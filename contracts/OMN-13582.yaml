---
schema_version: "1.0.0"
ticket_id: "OMN-13582"
title: "Correct merge_sweep skill routing topic documentation"
summary: >-
  Central evidence for the omniclaude documentation-only repo-health fix that corrects the ONEX merge_sweep
  skill routing contract documentation to the live node_pr_lifecycle_orchestrator start command topic.
  The product PR adds the matching repo-local contract and changes only plugins/onex/skills/merge_sweep/SKILL.md;
  it does not change runtime routing, schemas, topics, code, or deployed behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-routing-topic-doc"
    description: >-
      omniclaude PR #1801 documents the actual node_pr_lifecycle_orchestrator start command topic with
      the required -start suffix in plugins/onex/skills/merge_sweep/SKILL.md, and the PR is documentation-only
      apart from its repo-local contract evidence.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13582/dod-routing-topic-doc/command.yaml &&
          grep -q 'plugins/onex/skills/merge_sweep/SKILL.md' drift/dod_receipts/OMN-13582/dod-routing-topic-doc/command.yaml
  - id: "dod-occ-pr-3077"
    description: >-
      OCC PR #3077 is the central evidence PR that carries the OMN-13582 contract and receipt set cited
      by omniclaude PR #1801 as Evidence-Source. This item binds the OCC preflight eligibility check to
      the current OCC PR itself.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid"
