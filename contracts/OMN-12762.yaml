---
schema_version: "1.0.0"
ticket_id: "OMN-12762"
title: "Allow quality-gate DoD checks in delegation overlays"
summary: >-
  Central OCC evidence for omnibase_core PR #1207 and omnimarket PR #1075. The
  change lets delegation request overlays use the same named quality-gate DoD
  checks that the reducer already supports, so SEA-specific contracts can
  replace the generic code-generation class without DTO validation failures.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "public_api"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1207"
    description: "omnibase_core PR #1207 extends the canonical delegation wire DTO allow-list."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12762/dod-omnibase-core-pr-1207/command.yaml
          && grep -q '^pr_number: 1207$' drift/dod_receipts/OMN-12762/dod-omnibase-core-pr-1207/command.yaml
          && grep -q 'a5aaa245' drift/dod_receipts/OMN-12762/dod-omnibase-core-pr-1207/command.yaml
  - id: "dod-omnimarket-pr-1075"
    description: "omnimarket PR #1075 extends the mirrored delegation DTO and preserves reducer semantics."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12762/dod-omnimarket-pr-1075/command.yaml
          && grep -q '^pr_number: 1075$' drift/dod_receipts/OMN-12762/dod-omnimarket-pr-1075/command.yaml
          && grep -q '776589fa' drift/dod_receipts/OMN-12762/dod-omnimarket-pr-1075/command.yaml
  - id: "dod-overlay-contract-local-verification"
    description: "Focused local tests prove overlay DoD checks validate in both DTOs."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12762/dod-overlay-contract-local-verification/command.yaml
          && grep -q '59 passed' drift/dod_receipts/OMN-12762/dod-overlay-contract-local-verification/command.yaml
          && grep -q '16 passed' drift/dod_receipts/OMN-12762/dod-overlay-contract-local-verification/command.yaml
  - id: "dod-occ-evidence-pr-2247"
    description: "onex_change_control PR #2247 carries the central OCC evidence for OMN-12762."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12762/dod-occ-evidence-pr-2247/command.yaml
          && grep -q '^pr_number: 2247$' drift/dod_receipts/OMN-12762/dod-occ-evidence-pr-2247/command.yaml
