---
schema_version: '1.0.0'
ticket_id: OMN-10064
summary: 'feat(omnibase_core): merged ModelTicketContract absorbs OCC-local fields (Task 2 of OMN-9582)'
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: tests
    description: 6 merged-model tests pass green (xfail removed)
    command: uv run pytest tests/unit/models/ticket/test_model_ticket_contract_merged.py -v
  - kind: tests
    description: mypy strict zero new errors
    command: uv run mypy src/ --strict
  - kind: tests
    description: pre-commit all hooks pass
    command: pre-commit run --all-files
  - kind: ci
    description: CI pipeline green on PR
    command: gh pr checks 953 --repo OmniNode-ai/omnibase_core
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: 6 merged-model tests pass green (xfail markers removed)
    source: manual
    checks:
      - check_type: command
        check_value: uv run pytest tests/unit/models/ticket/test_model_ticket_contract_merged.py -v
  - id: dod-002
    description: mypy --strict zero new errors introduced by PR
    source: manual
    checks:
      - check_type: command
        check_value: uv run mypy src/ --strict 2>&1 | tail -1
  - id: dod-003
    description: pre-commit all hooks pass on PR branch
    source: manual
    checks:
      - check_type: command
        check_value: git -C /Users/jonah/Code/omni_worktrees/OMN-10064/omnibase_core log --oneline -1
  - id: dod-004
    description: PR 953 open against main with all new model files present
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 953 --repo OmniNode-ai/omnibase_core --json state,baseRefName --jq 'select(.state=="OPEN"
          and .baseRefName=="main") | "ok"'
