---
schema_version: "1.0.0"
ticket_id: "OMN-10348"
title: "Define ONEX (OmniNode eXecution) on first use across all canonical repo docs"
summary: "Add canonical ONEX expansion on first use in primary doc entrypoints; correct stale/wrong expansions
  in secondary docs across omniclaude, omnibase_core, omnibase_infra, omnibase_spi, omnidash, omniintelligence,
  omnimemory, onex_change_control, and omnimarket."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "All 9 per-repo PRs pass CI"
    command: "gh pr checks 1470 --repo OmniNode-ai/omniclaude && gh pr checks 985 --repo OmniNode-ai/omnibase_core
      && gh pr checks 1475 --repo OmniNode-ai/omnibase_infra && gh pr checks 200 --repo OmniNode-ai/omnibase_spi
      && gh pr checks 58 --repo OmniNode-ai/omnidash && gh pr checks 624 --repo OmniNode-ai/omniintelligence
      && gh pr checks 298 --repo OmniNode-ai/omnimemory && gh pr checks 559 --repo OmniNode-ai/onex_change_control
      && gh pr checks 463 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "omniclaude README.md updated with ONEX (OmniNode eXecution) on first use"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 1470 --repo OmniNode-ai/omniclaude | grep -q 'OmniNode eXecution'"
  - id: "dod-002"
    description: "omnibase_core README.md, CONTRIBUTING.md, and onex_terminology.md updated to canonical
      form"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 985 --repo OmniNode-ai/omnibase_core | grep -q 'OmniNode eXecution'"
  - id: "dod-003"
    description: "omnibase_infra README.md and architecture/overview.md updated to canonical form"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 1475 --repo OmniNode-ai/omnibase_infra | grep -q 'OmniNode eXecution'"
  - id: "dod-004"
    description: "omnibase_spi README.md, GLOSSARY.md, and CONTRIBUTING.md updated to canonical form"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 200 --repo OmniNode-ai/omnibase_spi | grep -q 'OmniNode eXecution'"
  - id: "dod-005"
    description: "omnidash README.md updated with ONEX (OmniNode eXecution) on first use"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 58 --repo OmniNode-ai/omnidash | grep -q 'OmniNode eXecution'"
  - id: "dod-006"
    description: "omniintelligence README.md updated with ONEX (OmniNode eXecution) on first use"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 624 --repo OmniNode-ai/omniintelligence | grep -q 'OmniNode eXecution'"
  - id: "dod-007"
    description: "omnimemory README.md updated with ONEX (OmniNode eXecution) on first use"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 298 --repo OmniNode-ai/omnimemory | grep -q 'OmniNode eXecution'"
  - id: "dod-008"
    description: "onex_change_control README.md updated with ONEX (OmniNode eXecution) on first use"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 559 --repo OmniNode-ai/onex_change_control | grep -q 'OmniNode eXecution'"
  - id: "dod-009"
    description: "omnimarket README.md updated with ONEX (OmniNode eXecution) on first use"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 463 --repo OmniNode-ai/omnimarket | grep -q 'OmniNode eXecution'"
  - id: "dod-010"
    description: "Deploy-gate evidence: docs-only change across 9 repos, no Docker image, runtime process,
      broker topic, or service deploy required"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy-gate: OMN-10348 changes only markdown documentation across 9 repos;
          no Docker image, runtime process, broker topic, or service deploy is required'"
