---
schema_version: "1.0.0"
ticket_id: "OMN-11282"
title: "feat(OMN-11282): wire session phase enforcement hook in omniclaude"
summary: >
  Typed ModelPhaseState Pydantic model and build_directive function added to the session_phase_enforcement
  module in omniclaude. Reads .onex_state/session/phase_state.yaml (written by node_session_phase_reducer,
  OMN-11279) and injects enforcement directives for transition_required, halt_required, budget_warning.
  Graceful no-op when state file absent. No network calls, no env vars, pure YAML read + string format.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: 19 unit tests pass covering all evaluation states, no-op paths, and file read edge cases.
    command: uv run pytest tests/unit/hooks/test_session_phase_enforcement.py -v
  - kind: manual
    description: omniclaude PR  #1625 carries the hook implementation.
    command: gh pr checks 1625 --repo OmniNode-ai/omniclaude
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "19 unit tests pass — all evaluation states covered (transition_required, halt_required,
      budget_warning, no_action, unknown)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -q '"status": "PASS"' "$CONTRACT_REPO_DIR/evidence/OMN-11282/dod_report.json" && grep -q
          '"verified_count": 8' "$CONTRACT_REPO_DIR/evidence/OMN-11282/dod_report.json"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11282 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -q '"overall_status": "PASS"' "$CONTRACT_REPO_DIR/evidence/OMN-11282/dod_report.json"
