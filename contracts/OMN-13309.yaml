---
schema_version: "1.0.0"
ticket_id: "OMN-13309"
title: "Add no-plugin-daemon-classes validator to omnibase_core"
summary: >-
  Bind omnibase_core PR #1268 to central OCC evidence for porting the no-plugin-daemon-classes validator
  into omnibase_core as a canonical static validator with Pydantic input/output models, an exported pre-commit
  hook, and focused unit coverage. The PR changes validator and model code only; no live service deploy,
  runtime restart, Kafka topic mutation, or database migration is required before merge.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1268"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1268 (jonah/omn-13309-no-plugin-daemon-core, base=dev).
      The PR carries the OMN-13309 no-plugin-daemon-classes validator, Pydantic validation models, exported
      hook metadata, and focused unit tests at the cited head commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13309/dod-omnibase-core-pr-1268/command.yaml
  - id: "dod-no-plugin-daemon-tests"
    description: >-
      Focused local proof for the no-plugin-daemon-classes validator port. The omnibase_core PR branch
      passed tests/unit/validators/test_no_plugin_daemon_classes.py with 31 tests covering violation,
      clean, protocol compliance, and CLI entry-point behavior.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13309/dod-no-plugin-daemon-tests/command.yaml
  - id: "dod-static-validator-deploy-assessment"
    description: >-
      Deploy-gate evidence for omnibase_core PR #1268. The touched files add a pure static validator,
      models, tests, and pre-commit hook export only; no live service deploy, runtime restart, Kafka topic
      mutation, or database migration is required before merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13309/dod-static-validator-deploy-assessment/command.yaml
          # deploy: static validator only; no live deploy or runtime restart required
