---
schema_version: "1.0.0"
ticket_id: "OMN-13309"
title: "Wire no-plugin-daemon-classes validator as required CI gate in omnibase_core"
summary: >-
  Bind omnibase_core PR #1336 to central OCC evidence for the final W9 deliverable: wiring the canonical
  no-plugin-daemon-classes validator as a required server-side CI gate on omnibase_core. The validator
  implementation, Pydantic finding model, unit tests, exported pre-commit hook, and remote consumers (omnibase_infra,
  omnimarket, omniclaude) already landed via core#1280 (OMN-13284); the earlier attempt core#1268 bundled
  an unrelated routing-authority refactor and was closed unmerged. This PR closes the remaining enforcement
  gap (Operating Rule #5) by adding the standalone validator-no-plugin-daemon-classes.yml workflow plus
  a self-scan regression test. The PR changes a CI workflow and a test only; no live service deploy, runtime
  restart, Kafka topic mutation, or database migration is required before merge.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1336"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1336 (jonah/omn-13309-no-plugin-daemon-ci-gate,
      base=dev). The PR adds .github/workflows/validator-no-plugin-daemon-classes.yml (unique requirable
      status-check context no-plugin-daemon-classes) and a self-scan regression test at the cited head
      commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13309/dod-omnibase-core-pr-1336/command.yaml
  - id: "dod-no-plugin-daemon-tests"
    description: >-
      Focused local proof for the no-plugin-daemon-classes validator CI gate. The omnibase_core PR branch
      passed tests/unit/validators/test_no_plugin_daemon_classes.py with 9 tests covering violation, clean,
      compute-base allow-listing, CLI entry-point, and the new src/-tree self-scan regression. The validator
      self-scan of core src/ exits 0.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13309/dod-no-plugin-daemon-tests/command.yaml
  - id: "dod-static-validator-deploy-assessment"
    description: >-
      Deploy-gate evidence for omnibase_core PR #1336. The touched files add a CI workflow and a unit
      test only; no live service deploy, runtime restart, Kafka topic mutation, or database migration
      is required before merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13309/dod-static-validator-deploy-assessment/command.yaml
          # deploy: CI workflow + test only; no live deploy or runtime restart required
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13309 contract and omnibase_core
      PR #1336 binding. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check
      can resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13309/dod-occ-pr-self/command.yaml
