---
schema_version: "1.0.0"
ticket_id: "OMN-11189"
title: "Task 1: Create EnumDataProvenance, EnumDegradedBehavior, and EnumDoctrineCoverage"
summary: >
  Adds three new enums to omnibase_core for doctrine coverage tracking and dashboard data-quality provenance
  classification. EnumDataProvenance (5 members), EnumDegradedBehavior (3 members), EnumDoctrineCoverage
  (3 members). All follow the StrValueHelper + @unique pattern with _missing_ returning None. PR #1097
  in omnibase_core.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "23 unit tests across all three enums pass on the PR branch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11189/dod-tests/command.yaml\""
  - id: "dod-lint-typecheck"
    description: "Ruff format, ruff check, and pre-commit hooks all pass on new files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11189/dod-lint-typecheck/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries OMN-11189 contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11189/dod-occ-pr/command.yaml\""
