---
schema_version: "1.0.0"
ticket_id: "OMN-11935"
title: "Knowledge context assembler orchestrator and reducer"
summary: >
  Bind central evidence for omnimarket PR #834, which adds the node_knowledge_context_assembler_orchestrator
  and node_knowledge_context_assembler_reducer pair. The implementation fans out to knowledge backends,
  reduces fragments into a shared event bundle, and handles partial backend failures through contract-driven
  routing.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-834"
    description: "Bind OMN-11935 evidence to omnimarket PR #834."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11935/dod-omnimarket-pr-834/command.yaml
          && grep -q '^pr_number: 834$' drift/dod_receipts/OMN-11935/dod-omnimarket-pr-834/command.yaml"
  - id: "dod-knowledge-context-local-validation"
    description: "Focused assembler tests, cross-node reach-in guard, ruff, leaked-literals, dep-health,
      and imperative contract guard pass for the current omnimarket PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11935/dod-knowledge-context-local-validation/command.yaml
          && grep -q '24 passed' drift/dod_receipts/OMN-11935/dod-knowledge-context-local-validation/command.yaml
          && grep -q 'baseline_delta: 0' drift/dod_receipts/OMN-11935/dod-knowledge-context-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: the new assembler nodes are locally validated and require no live
      runtime deployment before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11935/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11935/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11935 contract, deploy evidence, and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11935/dod-occ-pr/command.yaml"
