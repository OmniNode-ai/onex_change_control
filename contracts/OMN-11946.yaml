---
schema_version: "1.0.0"
ticket_id: "OMN-11946"
title: "Golden-chain closeout for contract-driven LLM ecosystem update"
summary: >
  Central OCC evidence for omnimarket PR #839, which enforces the LLM contract overlay boundary by removing
  remaining contract-owned served model IDs, model route literals, and endpoint URL defaults from node
  contracts. Runtime served model identity remains overlay-owned, and final closeout proof covers contract
  validation plus routing/delegation golden-chain evidence.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-839"
    description: "omnimarket PR #839 carries the contract overlay boundary hardening."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11946/dod-omnimarket-pr-839/command.yaml
          && grep -q '^pr_number: 839$' drift/dod_receipts/OMN-11946/dod-omnimarket-pr-839/command.yaml"
  - id: "dod-contract-overlay-boundary"
    description: "Contract overlay validator passes with no hardcoded model or endpoint defaults in node
      contracts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11946/dod-contract-overlay-boundary/command.yaml
          && grep -q 'contract-overlay-boundary: PASS' drift/dod_receipts/OMN-11946/dod-contract-overlay-boundary/command.yaml"
  - id: "dod-runtime-proof"
    description: "Focused runtime routing and golden-chain proof passes for routing/delegation closeout."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11946/dod-runtime-proof/command.yaml
          && grep -q 'golden-chain routing/delegation pass' drift/dod_receipts/OMN-11946/dod-runtime-proof/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Deploy gate assessment for overlay-only contract/runtime config hardening."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11946/dod-deploy-assessment/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11946/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-1592"
    description: "OCC PR #1592 carries the central OMN-11946 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11946/dod-occ-pr-1592/command.yaml
          && grep -q '^pr_number: 1592$' drift/dod_receipts/OMN-11946/dod-occ-pr-1592/command.yaml"
