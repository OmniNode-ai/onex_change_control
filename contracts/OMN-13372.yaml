---
schema_version: "1.0.0"
ticket_id: "OMN-13372"
title: "Fix delegation projection quality-bar migration ordering so dev materializes terminal rows"
summary: >-
  Central evidence for omnimarket PR #1301 and omnibase_infra PR #2047. Live dev delegation e2e after
  OMN-13371 reached request consumption, routing, inference, quality-gate result, delegation-completed,
  and task-delegated terminal events with no DLQ hit, but projection failed because the deployed infra
  vendored node migration tree stopped at node_projection_delegation/0015 and did not include the omnimarket
  0016 quality-bar and judge-verdict migrations. The fix tightens the omnimarket source migrations and
  vendors those node migrations into omnibase_infra so forward migration can create the required projection
  columns and judge table on dev and future deploys.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1301"
    description: >-
      omnimarket PR #1301 carries the OMN-13372 source migration hardening at commit 112ed0d5c3a09d35c68923a68954d0f159648176.
      It tightens judge verdict failure-metadata semantics, aligns score/bar precision, and fixes quality-gate
      aggregate denominators before the infra vendor tree is refreshed from omnimarket dev.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-omnimarket-pr-1301/command.yaml"
          && grep -q '^pr_number: 1301$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-omnimarket-pr-1301/command.yaml"
          && grep -q '112ed0d5c3a09d35c68923a68954d0f159648176' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-omnimarket-pr-1301/command.yaml"
  - id: "dod-infra-pr-2047"
    description: >-
      omnibase_infra PR #2047 carries the OMN-13372 implementation at commit 8182916f8aa8972caf4e8e2e5edc3ed111b2bfe7.
      It vendors the missing node_projection_delegation 0016 migrations from current omnimarket dev into
      the deploy-time forward migration tree.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-infra-pr-2047/command.yaml"
          && grep -q '^pr_number: 2047$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-infra-pr-2047/command.yaml"
          && grep -q '8182916f8aa8972caf4e8e2e5edc3ed111b2bfe7' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-infra-pr-2047/command.yaml"
  - id: "dod-focused-checks"
    description: >-
      Focused verification for the vendored migration change passed. The sync script reports infra's node
      migration vendor tree is in sync with current omnimarket dev, migration sequence validation passes,
      and file-scoped pre-commit passes for the two SQL files.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-focused-checks/command.yaml"
          && grep -q 'sync: in sync' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-focused-checks/command.yaml"
          && grep -q 'file-scoped pre-commit: passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-focused-checks/command.yaml"
  - id: "dod-dev-e2e-blocker"
    description: >-
      Live dev e2e blocker evidence. Correlation ID cb2feaf2-d4c0-4345-96f5-a047712a43d7 proved the delegation
      event-bus chain reaches terminal events after OMN-13371. Projection failed only at database materialization
      with UndefinedColumn for required_bar because the live delegation_events table lacked the quality-bar
      columns.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-dev-e2e-blocker/command.yaml"
          && grep -q 'cb2feaf2-d4c0-4345-96f5-a047712a43d7' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-dev-e2e-blocker/command.yaml"
          && grep -q 'column "required_bar" of relation "delegation_events" does not exist' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-dev-e2e-blocker/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13372 contract and receipts.
      Binds a PASS receipt directly to onex_change_control PR #2834 so OCC eligibility can validate the
      evidence PR itself before downstream infra PR #2047 cites it.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-occ-pr-self/command.yaml"
          && grep -q '^pr_number: 2834$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-occ-pr-self/command.yaml"
  - id: "dod-occ-pr-2835"
    description: >-
      OCC self-binding receipt for the follow-up central evidence PR that adds omnimarket PR #1301 as
      source migration evidence after the infra CodeRabbit review identified valid source-level SQL hardening.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-occ-pr-2835/command.yaml"
          && grep -q '^pr_number: 2835$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-occ-pr-2835/command.yaml"
  - id: "dod-occ-pr-2836"
    description: >-
      OCC self-binding receipt for the final central evidence refresh that binds omnibase_infra PR #2047
      to amended head 8182916f8aa8972caf4e8e2e5edc3ed111b2bfe7 after source migration hardening landed
      in omnimarket PR #1301.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-occ-pr-2836/command.yaml"
          && grep -q '^pr_number: 2836$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13372/dod-occ-pr-2836/command.yaml"
