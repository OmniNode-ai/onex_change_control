---
schema_version: "1.0.0"
ticket_id: "OMN-11753"
title: "Fix omnibase_core semantic diff CLI xdist worker crash"
summary: >
  Bind central evidence for omnibase_core PR #1141, which skips the expensive semantic-diff consumer graph
  scan when the diff has no changed Python files.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1141"
    description: "Bind OMN-11753 evidence to omnibase_core PR #1141."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11753/dod-omnibase-core-pr-1141/command.yaml
          && grep -q '^pr_number: 1141$' drift/dod_receipts/OMN-11753/dod-omnibase-core-pr-1141/command.yaml"
  - id: "dod-focused-semantic-diff-tests"
    description: "Focused semantic diff CLI tests pass locally with and without xdist."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11753/dod-focused-semantic-diff-tests/command.yaml
          && grep -q '7 passed' drift/dod_receipts/OMN-11753/dod-focused-semantic-diff-tests/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Bind OMN-11753 central evidence to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11753/dod-occ-pr-self/command.yaml"
  - id: "dod-occ-rerun-state-pr-1392"
    description: "Bind the generated OCC rerun state PR #1392 to OMN-11753."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11753/dod-occ-rerun-state-pr-1392/command.yaml
          && grep -q '^pr_number: 1392$' drift/dod_receipts/OMN-11753/dod-occ-rerun-state-pr-1392/command.yaml"
