---
schema_version: "1.0.0"
ticket_id: "OMN-15003"
title: "omniweb onex-dev digest placeholder source repair evidence"
summary: "OCC evidence for OmniNode-ai/omninode_infra#632: source-level omniweb onex-dev image pin repair
  and build workflow trigger fix; no live deploy performed."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #632 product diff scope present"
    command: "gh pr view 632 --repo OmniNode-ai/omninode_infra --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-632-head"
    description: "PR #632 is bound to the reviewed product head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -F 'PASS: omninode_infra PR #632 is bound to reviewed head c9a0c13f94ef54e26063fdec78cfb3e9a07a0775.'
          drift/dod_receipts/OMN-15003/dod-omninode-infra-pr-632-head/command.yaml && grep -F 'commit_sha:
          \"c9a0c13f94ef54e26063fdec78cfb3e9a07a0775\"' drift/dod_receipts/OMN-15003/dod-omninode-infra-pr-632-head/command.yaml"
  - id: "dod-omninode-infra-pr-632-scope"
    description: "PR #632 scope is limited to the onex-dev omniweb image overlay and matching build workflow."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -F 'PASS: PR #632 scope is limited to omniweb dev overlay and build workflow
          files.' drift/dod_receipts/OMN-15003/dod-omninode-infra-pr-632-scope/command.yaml && grep -F
          '.github/workflows/build-and-push-omniweb.yml' drift/dod_receipts/OMN-15003/dod-omninode-infra-pr-632-scope/command.yaml
          && grep -F 'k8s/onex-dev/omniweb/kustomization.yaml' drift/dod_receipts/OMN-15003/dod-omninode-infra-pr-632-scope/command.yaml"
  - id: "dod-omninode-infra-pr-632-tests"
    description: "PR #632 body records manifest render, actionlint, pre-commit, and ECR ImageNotFound
      proof."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -F 'PASS: PR #632 body records manifest render, actionlint, pre-commit, and
          ECR ImageNotFound evidence.' drift/dod_receipts/OMN-15003/dod-omninode-infra-pr-632-tests/command.yaml
          && grep -F 'kubectl kustomize k8s/onex-dev/omniweb' drift/dod_receipts/OMN-15003/dod-omninode-infra-pr-632-tests/command.yaml
          && grep -F 'ImageNotFound' drift/dod_receipts/OMN-15003/dod-omninode-infra-pr-632-tests/command.yaml"
  - id: "dod-omninode-infra-pr-632-live-boundary"
    description: "PR #632 explicitly avoids cluster mutation and defers live deploy/readback to operator-approved
      dispatch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -F 'PASS: PR #632 records no cluster mutation and defers live deploy/readback.'
          drift/dod_receipts/OMN-15003/dod-omninode-infra-pr-632-live-boundary/command.yaml && grep -F
          'No cluster mutation' drift/dod_receipts/OMN-15003/dod-omninode-infra-pr-632-live-boundary/command.yaml
          && grep -F 'deploy-onex-dev.yml' drift/dod_receipts/OMN-15003/dod-omninode-infra-pr-632-live-boundary/command.yaml"
  - id: "occ-self-bind-pr-4678"
    description: "OCC companion PR #4678 — self-bind for OMN-15003."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4678 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
