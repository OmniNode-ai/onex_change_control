---
schema_version: "1.0.0"
ticket_id: "OMN-12471"
title: "SEA: enum-typed ModelRegistryEntry + entry_for_tier coercion"
summary: >-
  Bind central evidence for onex-self-extending-agent PR #186, which lifts ModelRegistryEntry.provider/model_tier/cost_basis
  from str to the EnumModelProvider/EnumModelTier/EnumModelCostBasis StrEnums, adds EnumModelTier coercion
  to entry_for_tier, and replaces the cost-basis-keyed secret-overlay validator with provider-keyed overlay/usage
  validators while preserving entries_for_tier / entry_by_name for the eval path.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-189"
    description: "Bind onex-self-extending-agent PR #189 head and branch for the OMN-12471 change."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-sea-pr-189/command.yaml"
          && grep -q '^pr_number: 189$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-sea-pr-189/command.yaml"
          && grep -q '^commit_sha: "e7f10f1092d9706065cb2e51b5bc96fde5d30bdc"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-sea-pr-189/command.yaml"
  - id: "dod-enum-typing"
    description: "Prove the registry entry fields are enum-typed, entry_for_tier coerces, and no httpx
      surface was added."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-enum-typing/command.yaml"
          && grep -q 'provider: EnumModelProvider' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-enum-typing/command.yaml"
          && grep -q 'model_tier: EnumModelTier' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-enum-typing/command.yaml"
          && grep -q 'cost_basis: EnumModelCostBasis' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-enum-typing/command.yaml"
          && grep -q 'httpx added: 0' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-enum-typing/command.yaml"
  - id: "dod-local-validation"
    description: "Full unit suite, mypy strict, ruff format, ruff check, and pre-commit passed locally."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-local-validation/command.yaml"
          && grep -q '1200 passed, 15 skipped' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-local-validation/command.yaml"
          && grep -q 'Success: no issues found in 156 source files' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-local-validation/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12471 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-occ-pr-self/command.yaml"
          && grep -q '^pr_number: 1993$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12471/dod-occ-pr-self/command.yaml"
