---
schema_version: "1.0.0"
ticket_id: "OMN-13305"
title: "Add backend secret discipline compute hook"
summary: >-
  Adds the omnibase_core backend secret discipline compute hook and validation surface. This central OCC
  evidence binds OMN-13305 to omnibase_core PR #1284 and records that the PR has no live deploy mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1284"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1284 at the current head. The receipt binds by
      pr_number and commit_sha for downstream Receipt Gate resolution.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13305/dod-omnibase-core-pr-1284/command.yaml
  - id: "dod-deploy-no-live-mutation"
    description: >-
      Deploy-path evidence for OMN-13305. The PR changes repository validation code and CI evidence only.
      It adds no runtime node, Kafka topic, migration, image-build input, .201 mutation, or live deploy.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13305/dod-deploy-no-live-mutation/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying OMN-13305 contract and receipts. Binds
      the OCC PR by pr_number so the OCC receipt-gate eligibility check can resolve a PASS receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13305/dod-occ-pr-self/command.yaml
