---
schema_version: "1.0.0"
ticket_id: "OMN-15185"
title: "Autobind OCC evidence for OMN-15185"
summary: "OCC Evidence-Source autobind companion for omnibase_infra PR #2468."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2468 product diff scope present"
    command: "gh pr view 2468 --repo OmniNode-ai/omnibase_infra --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2468"
    description: "PR #2468 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2468 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2468-ci"
    description: "PR #2468 on OmniNode-ai/omnibase_infra — product diff scope check."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2468 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-omn15185-hermetic-symbol-check"
    description: >-
      Compute-only ticket: OMN-15185 adds a --source flag to the onex delegate CLI (OmniNode-ai/omnibase_infra#2468),
      threading a closed choice into the delegation payload's source field. No new bus topic, container,
      or live runtime deploy surface is introduced -- the change is CLI-flag- scoped inside a single module.
      Per the deploy-gate falsifiability guidance (OMN-14505) for compute-only/no-live-deploy tickets,
      this is a hermetic GitHub-content check (OMN-14051 exemption): it reads the actual file content
      at PR #2468's real head commit and asserts the class of change the PR introduces is genuinely present.
      It is NOT self-referential (does not read onex_change_control's own dod_receipts/contracts tree)
      and goes RED if the line is ever removed. Pinned to PR #2468's own head commit (ccb50e06464f6fab56b4cb471da0ed55db63f257)
      rather than @dev because this repo's own Contract Compliance Check EXECUTES the command at authoring
      time -- pinning to @dev here would 404 until after #2468 merges. Follow-up: once #2468 merges, re-pin
      to the merged squash- commit SHA on omnibase_infra@dev per reference_never_pin_a_feature_branch_head.
      Live-tested before authoring: matches at the pinned head commit (exit 0), does NOT match at the
      pre-change dev HEAD a899750d3cdf6e01ded8053a6d1223c62b20801e (exit 1) -- proven falsifiable.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/cli/cli_delegate.py?ref=ccb50e06464f6fab56b4cb471da0ed55db63f257 --jq .content | base64 -d | grep -q 'DELEGATE_SOURCE_CHOICES: tuple\[str, ...\] = ("claude-code", "codex", "external-client")'
  - id: "occ-self-bind-pr-4964"
    description: "OCC companion PR #4964 — self-bind for OMN-15185 (deploy-gate evidence companion)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4964 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
