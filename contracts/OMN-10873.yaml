---
schema_version: "1.0.0"
ticket_id: "OMN-10873"
title: "DI refactor: NodeOverseerBenchmarker accepts ProtocolLlmEvalHarness"
summary: >
  Adds ProtocolLlmEvalHarness structural protocol to handler_llm_eval_harness.py. NodeOverseerBenchmarker.__init__
  now accepts ProtocolLlmEvalHarness | None instead of the concrete NodeLlmEvalHarness. Default construction
  unchanged. No runtime deploy required for this pure code seam refactor. Part of Wave 2 DI injection
  seam refactor.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests-pass"
    description: "DI injection unit tests pass: protocol satisfaction, injection routing, default-construction
      fallback, dry-run bypass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10873/dod-unit-tests-pass/command.yaml\"
          && grep -q '^commit_sha: \"eed1dc539ee76f887e42366e87efd2e59c4a4dc0\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10873/dod-unit-tests-pass/command.yaml\""
  - id: "dod-deploy"
    description: "No runtime deploy required; pure code seam refactor with no behaviour change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10873/dod-deploy/command.yaml\"
          && grep -q '^commit_sha: \"eed1dc539ee76f887e42366e87efd2e59c4a4dc0\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10873/dod-deploy/command.yaml\""
  - id: "dod-omnimarket-pr-693"
    description: "omnimarket PR #693 (DI Wave 2) open with CI green at commit eed1dc539ee76f887e42366e87efd2e59c4a4dc0."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10873/dod-omnimarket-pr-693/command.yaml\"
          && grep -q '^pr_number: 693$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10873/dod-omnimarket-pr-693/command.yaml\""
  - id: "dod-occ-pr-1113"
    description: "onex_change_control PR #1113 carries the OMN-10873 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10873/dod-occ-pr-1113/command.yaml\"
          && grep -q '^pr_number: 1113$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10873/dod-occ-pr-1113/command.yaml\""
