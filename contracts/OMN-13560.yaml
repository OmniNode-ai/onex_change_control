---
schema_version: "1.0.0"
ticket_id: "OMN-13560"
title: "env-to-contract migration (omniclaude): endpoint, config, and secret-read reductions"
summary: >-
  OMN-13560 is the omniclaude child of epic OMN-13556 for env-to-contract migration. The central OCC record
  now covers both shipped slices. Wave 1 moved diagnostics service-endpoint reads out of scattered direct
  os.environ.get(...) calls and onto the canonical contract-overlay descriptor path. Wave 2/Wave 3, carried
  by omniclaude PR #1820, deletes the dead ANTHROPIC_API_KEY read in manifest_injector and migrates ConfigSessionAggregator
  off pydantic-settings .env/env_prefix binding to the contract_omniclaude_runtime.yaml aggregator overlay
  section. The Wave 2/Wave 3 slice preserves direct construction defaults, adds from_contract() resolution
  tests, fails closed on invalid overlay values at the pydantic boundary, and keeps live runtime mutation
  outside this PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      Wave 1 diagnostics endpoint resolution equivalence: the overlay seam resolves each diagnostics service
      endpoint identically to the pre-migration direct env reads across lane values, preserves the MCP
      legacy alias chain, fails closed for strict access, and keeps diagnostics import-safe.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13560/dod-001/command.yaml"
  - id: "dod-gates"
    description: >-
      Wave 1 ratchet gates stayed green after the diagnostics endpoint migration: URL Authority, Canonical
      Inference, no localhost/default env fallbacks, ruff, and changed-file pre-commit checks passed with
      fewer raw endpoint reads.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13560/dod-gates/command.yaml"
  - id: "dod-deploy"
    description: >-
      Wave 1 runtime-impact assessment: diagnostics endpoint resolution moved to a contract-overlay descriptor
      without changing runtime topology, container build inputs, Kafka topics, or resolved endpoint values.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13560/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Wave 1 OCC self-binding receipt for the merged OCC evidence PR that supplied the central contract
      and receipts consumed by omniclaude PR #1805.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13560/dod-occ-pr/command.yaml"
  - id: "dod-wave23-tests"
    description: >-
      Wave 2/Wave 3 product verification for omniclaude PR #1820: focused aggregator tests cover unset-env
      default equivalence, overlay override resolution, and invalid overlay fail-closed behavior; the
      touched config module type-checks cleanly under mypy strict; product CI test shards and type checks
      are green at head a2e41f19846f57395df0509085f61b7edd2b20bf.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13560/dod-wave23-tests/command.yaml"
  - id: "dod-wave23-gates"
    description: >-
      Wave 2/Wave 3 migration gates for omniclaude PR #1820 are green at head a2e41f19846f57395df0509085f61b7edd2b20bf
      for the relevant non-evidence checks: URL Authority, Canonical Inference, no localhost/default env
      fallbacks, Contract Validation, Secret Detection, Secret Scanning, and PR title.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13560/dod-wave23-gates/command.yaml"
  - id: "dod-wave23-runtime-impact"
    description: >-
      Wave 2/Wave 3 runtime-impact assessment for omniclaude PR #1820: the changed files are limited to
      aggregator config, runtime contract YAML, manifest injector, and focused unit tests; no Dockerfile,
      compose file, infrastructure manifest, Kafka topic registry, or live runtime control surface is
      touched.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13560/dod-wave23-runtime-impact/command.yaml"
  - id: "dod-occ-wave23-pr"
    description: >-
      Wave 2/Wave 3 OCC self-binding receipt for the OCC PR that supplies central evidence for omniclaude
      PR #1820. The receipt binds to a commit in that OCC PR and proves the expanded OMN-13560 contract
      is present.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13560/dod-occ-wave23-pr/command.yaml"
