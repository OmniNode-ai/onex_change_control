---
schema_version: "1.0.0"
ticket_id: OMN-11280
title: "feat(OMN-11280): create node_session_phase_dispatcher (EFFECT) in omnimarket"
summary: >
  node_session_phase_dispatcher is the ONLY side-effecting node in the session-phase subsystem. Receives
  phase transition commands (onex.cmd.omnimarket.session-phase-transition.v1) and publishes phase-state
  events (onex.evt.omnimarket.session-phase-state.v1) and budget-warning events (onex.evt.omnimarket.session-phase-budget-warning.v1).
  Worker dispatch_items from phase spec counted on enter transitions. Topics loaded from contract.yaml.
  4 unit tests pass. Core implementation delivered under OMN-11231 (PR #704, merged). This OCC contract
  provides OMN-11280 DoD evidence for the same node.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Implementation PR #729 open against omnimarket main."
    command: "gh pr view 729 --repo OmniNode-ai/omnimarket --json url,state,baseRefName,headRefName,headRefOid"
  - kind: "manual"
    description: >
      EFFECT node — subscribes to transition topic and publishes events. No runtime service restart, no
      DB migration, no .201 mutation required.
    command: "grep -q '^node_type: effect$' src/omnimarket/nodes/node_session_phase_dispatcher/contract.yaml"
  - kind: "manual"
    description: "OCC PR self-binds OMN-11280 contract and receipts."
    command: "gh pr view <OCC-PR-NUM> --repo OmniNode-ai/onex_change_control --json number,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ci-pass"
    description: "Implementation PR #729 is open for OMN-11280 against omnimarket main."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^status: \"?PASS\"?$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11280/dod-ci-pass/command.yaml\"
          && grep -Eq '^ticket_id: \"?OMN-11280\"?$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11280/dod-ci-pass/command.yaml\"
          && grep -q '^pr_number: 729$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11280/dod-ci-pass/command.yaml\""
  - id: "dod-deploy-assessment"
    description: >
      EFFECT node ships with passive subscription — no live deploy, runtime restart, .201 mutation, or
      docker exec required for this PR. Node activates when orchestrator wires it post-merge.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^status: \"?PASS\"?$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11280/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11280/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR self-binds OMN-11280 central contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf 'self-attested: dod-occ-pr for OMN-11280\n'"
