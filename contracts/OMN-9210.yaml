---
schema_version: "1.0.0"
ticket_id: "OMN-9210"
title: "Fix 30+ nodes using uppercase node_type (COMPUTE/EFFECT/ORCHESTRATOR/REDUCER)"
summary: >-
  Mechanically lowercases node_type in contract.yaml files across omnimarket (24 files), omnimemory (16
  files), and omniintelligence (2 files). Fixes 54+ invalid_node_type violations detected by contract
  sweep. Also adds missing handler blocks to node_intent_storage_effect and node_semantic_analyzer_compute,
  and fixes ARCH-TOPIC-002 description string false positive. Part of OMN-9532 epic.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: manual
    description: omnimarket PR  #681 bound at observed head.
    command: grep -q '^status. PASS$' drift/dod_receipts/OMN-9210/dod-omnimarket-pr-681/command.yaml
  - kind: manual
    description: omnimemory PR  #316 bound at observed head.
    command: grep -q '^status. PASS$' drift/dod_receipts/OMN-9210/dod-omnimemory-pr-316/command.yaml
  - kind: manual
    description: omniintelligence PR  #653 bound at observed head.
    command: grep -q '^status. PASS$' drift/dod_receipts/OMN-9210/dod-omniintelligence-pr-653/command.yaml
  - kind: manual
    description: No deploy required.
    command: grep -q '^status. PASS$' drift/dod_receipts/OMN-9210/dod-deploy/command.yaml
  - kind: manual
    description: OCC PR carries OMN-9210 evidence.
    command: grep -q '^status. PASS$' drift/dod_receipts/OMN-9210/dod-occ-self/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-omnimarket-pr-681
    description: Bind omnimarket PR  #681 to OMN-9210 at the observed PR head.
    source: manual
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9210/dod-omnimarket-pr-681/command.yaml"
  - id: dod-omnimemory-pr-316
    description: Bind omnimemory PR  #316 to OMN-9210 at the observed PR head.
    source: manual
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9210/dod-omnimemory-pr-316/command.yaml"
  - id: dod-omniintelligence-pr-653
    description: Bind omniintelligence PR  #653 to OMN-9210 at the observed PR head.
    source: manual
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9210/dod-omniintelligence-pr-653/command.yaml"
  - id: dod-deploy
    description: No live deploy required; pure contract metadata change with no behavior change.
    source: manual
    checks:
      - check_type: command
        check_value: printf 'deploy gate. OMN-9210 is a pure contract metadata fix'
  - id: dod-occ-self
    description: Self-binding receipt for OCC PR carrying OMN-9210 evidence.
    source: manual
    checks:
      - check_type: command
        check_value: printf 'OCC self-bind. this OCC PR carries OMN-9210 evidence'
