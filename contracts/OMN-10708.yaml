---
schema_version: "1.0.0"
ticket_id: "OMN-10708"
title: "Replace omnimarket direct handler imports with node package public APIs"
summary: >
  Updates omnimarket proof-of-life scripts to import node handlers and models through node package public
  APIs instead of private handler/model submodules. This is an import-boundary cleanup only; it does not
  change runtime wiring, topics, migrations, or deploy behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-import-boundary"
    description: "omnimarket PR #609 replaces direct private handler imports with node package public
      APIs"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10708/dod-import-boundary/command.yaml && grep -q
          '^status:.*PASS' drift/dod_receipts/OMN-10708/dod-import-boundary/command.yaml"
  - id: "dod-deploy"
    description: >
      Deploy verification: import-boundary-only proof-of-life script and package-export change. No runtime
      topology, migration, docker exec action, Redpanda topic change, or service restart is required.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10708/dod-deploy/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10708/dod-deploy/command.yaml && grep -q 'deploy' drift/dod_receipts/OMN-10708/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for the OCC PR carrying this contract."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10708/dod-occ-pr/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10708/dod-occ-pr/command.yaml"
