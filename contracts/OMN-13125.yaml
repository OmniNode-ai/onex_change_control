---
schema_version: "1.0.0"
ticket_id: "OMN-13125"
title: "fix(omnimarket): fail fast when sweep OCC root is missing"
summary: >-
  Removes silent Path.cwd() fallback behavior from the integration sweep and DoD sweep orchestrator OCC
  root resolvers. ONEX_CC_REPO_PATH is now required and must point at a real onex_change_control checkout
  with a contracts directory. The PR also adds focused fail-fast tests, updates orchestrator contracts,
  fixes related tests, and adds golden-chain coverage for node_pipeline_fill.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Focused fail-fast, golden-chain, and affected-node tests pass"
    command: "uv run pytest tests/test_resolve_root_fail_fast.py tests/test_golden_chain_pipeline_fill.py
      -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: >-
      Focused local tests cover unset, nonexistent, missing-contracts, and valid OCC root behavior for
      both orchestrators, plus the pipeline-fill golden chain added to satisfy the live-path coverage
      gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-13125/dod-focused-tests/command.yaml" && grep
          -q '^ticket_id: "OMN-13125"$' "drift/dod_receipts/OMN-13125/dod-focused-tests/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: runtime-gated omnimarket node handler and contract paths changed, but no live
      deploy, restart, topic mutation, envelope change, or .201 mutation was performed. The change is
      fail-fast validation plus tests; normal post-merge build/promotion is sufficient.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'No live deploy' "drift/dod_receipts/OMN-13125/dod-deploy-assessment/command.yaml" &&
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-13125/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      This onex_change_control OCC PR carries the central OMN-13125 contract and receipts that omnimarket
      PR #1207 resolves via Evidence-Source: OCC#<this-pr> / Evidence-Ticket: OMN-13125.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-13125/dod-occ-pr-self/command.yaml" && grep
          -q '^ticket_id: "OMN-13125"$' "drift/dod_receipts/OMN-13125/dod-occ-pr-self/command.yaml"
