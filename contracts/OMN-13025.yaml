---
schema_version: "1.0.0"
ticket_id: OMN-13025
title: "A-3: Evidence-language guard — pre-commit on docs/evidence/** rejecting UI/e2e claims with no
  Playwright artifact referenced"
summary: >-
  Adds a pre-commit hook (.pre-commit-hooks/reject-evidence-ui-claims.sh) to omniclaude that enforces
  evidence-language honesty on docs/evidence/** markdown files. Any doc containing "UI POST", "click",
  "full chain live", or "end-to-end" must also reference a Playwright artifact; otherwise the commit is
  blocked. Root cause: PROCESS_FAILURE_RETRO.md §3.A (top-10 failure #1) — the 2026-06-11 19:27Z evidence
  doc overstated UI coverage when only curl was run.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "8 unit tests covering reject/accept/ignore cases pass in omniclaude worktree"
    command: "uv run pytest tests/hooks/test_reject_evidence_ui_claims.py -v --tb=short 2>&1 | grep '8
      passed'"
  - kind: manual
    description: "Hook script present and executable in omniclaude"
    command: "test -x .pre-commit-hooks/reject-evidence-ui-claims.sh"
  - kind: manual
    description: "Hook wired in omniclaude .pre-commit-config.yaml"
    command: "grep -q 'reject-evidence-ui-claims' .pre-commit-config.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-hook-exists-executable
    description: "Hook script reject-evidence-ui-claims.sh is present and executable in omniclaude"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13025/dod-hook-exists-executable/command.yaml"
  - id: dod-hook-wired-in-precommit
    description: "Hook is registered in omniclaude .pre-commit-config.yaml as reject-evidence-ui-claims"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13025/dod-hook-wired-in-precommit/command.yaml"
  - id: dod-unit-tests-pass
    description: "All 8 unit tests for reject-evidence-ui-claims.sh pass"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13025/dod-unit-tests-pass/command.yaml"
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt — this onex_change_control PR carries the OMN-13025 contract + receipts
      consumed by omniclaude PR via Evidence-Source.
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13025/dod-occ-self/command.yaml"
