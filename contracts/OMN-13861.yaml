---
schema_version: "1.0.0"
ticket_id: "OMN-13861"
title: "fix(OMN-13861): cloud escalation ladder functional on bus-less delegation path"
summary: >-
  omnimarket PR #1584 fixes the bus-less LocalDelegationDispatchPort cloud escalation path by threading
  secret references into LLM delegation calls, making initial backend selection honor escalation_policy.tier_order,
  and adding convention-based logical secret-ref resolution at the effect boundary.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1584"
    description: >-
      Consuming-PR binding: omnimarket PR #1584 carries the OMN-13861 cloud escalation ladder fix at head
      43d4cc9308bd42aba522209860122b07298a0711, with unit coverage for secret_ref authorization, routing-authority
      tier selection, and local-dispatch escalation behavior.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13861/dod-omnimarket-pr-1584/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13861 contract and receipts consumed
      by omnimarket PR #1584.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13861/dod-occ-self/command.yaml"
