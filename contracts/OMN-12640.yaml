---
schema_version: "1.0.0"
ticket_id: "OMN-12640"
title: "SEA scaffold fail-fast: typed SeaGenerationError + contract_passed authority (D1+D2)"
summary: >-
  D1: scaffold_onex_node raises a typed SeaGenerationError instead of returning node_name="unknown" stub.
  D2: run_agent derives contract_passed solely from scaffold validation result, never from registration
  flag. Both defects caused SEA to mask generation failures as benign nodes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pytest-green"
    description: >-
      Full test suite passes with no -k filter: uv run pytest tests/ -v → 1353 passed, 15 skipped. No
      generation-stub or registration-authority paths survive.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12640/dod-pytest-green/command.yaml"
  - id: "dod-mypy-clean"
    description: >-
      uv run mypy src/ --strict: Success, no issues in 200 files. Typed SeaGenerationError + ModelGenerationFailurePayload
      carry full strict annotations.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12640/dod-mypy-clean/command.yaml"
  - id: "dod-precommit-green"
    description: >-
      pre-commit run --all-files: all hooks pass including SEA inference architecture guard (OMN-12608).
      Ruff format + lint: clean.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12640/dod-precommit-green/command.yaml"
  - id: "dod-no-unknown-stub"
    description: >-
      No code path in scaffold_onex_node returns node_name="unknown". The typed error is always raised
      when contract_passed is False. Regression test in test_tools.py + test_tools_pipeline.py asserts
      this.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12640/dod-no-unknown-stub/command.yaml"
  - id: "dod-registration-not-authoritative"
    description: >-
      registration flag never sets contract_passed=True. Authority centralized in contract_authority.py.
      Regression test in test_main.py asserts registration alone never flips contract_passed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12640/dod-registration-not-authoritative/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-12640 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12640/dod-occ-pr-self/command.yaml"
