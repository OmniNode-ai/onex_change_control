---
schema_version: "1.0.0"
ticket_id: "OMN-13648"
title: "Expose delegation savings-over-time series projection"
summary: >-
  Adds the bus-fed delegation savings over time projection for the omnidash delegation dashboard depth
  view. omnimarket PR #1457 adds node-owned migration 078 and the projection_api exposure onex.snapshot.projection.delegation.savings-series.v1
  for projection_delegation_savings_series, using the same savings_estimates UNION delegation_events source
  as the existing savings projection and aggregating into one UTC-day row per bucket. omnibase_infra PR
  #2114 vendors the same migration into docker/migrations/forward/nodes/node_projection_savings so a clean
  redeploy materializes the view. No new REST route and no client-side computation; the projection API
  remains the serving surface.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1457"
    description: >-
      omnimarket PR #1457 adds migration 078 for projection_delegation_savings_series, wires the projection_api
      exposure on onex.snapshot.projection.delegation.savings-series.v1, and proves the bucketed daily
      aggregate with TestDelegationSavingsSeriesProjection plus projection materialization ratchet coverage.
      CI on the product PR is green except the expected OCC/deploy evidence gates that resolve when this
      contract lands.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13648/dod-omnimarket-pr-1457/command.yaml &&
          grep -q '^pr_number: 1457$' drift/dod_receipts/OMN-13648/dod-omnimarket-pr-1457/command.yaml
  - id: "dod-infra-pr-2114"
    description: >-
      omnibase_infra PR #2114 vendors the byte-identical omnimarket node migration 078 under docker/migrations/forward/nodes/node_projection_savings.
      The infra PR's node-migration-sync gate is expected to go green after omnimarket #1457 merges to
      dev, because the gate compares the vendored migration tree against the omnimarket dev tip.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13648/dod-infra-pr-2114/command.yaml && grep
          -q '^pr_number: 2114$' drift/dod_receipts/OMN-13648/dod-infra-pr-2114/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for OMN-13648. These PRs add projection view DDL and the vendored migration but
      do not perform a live deploy, runtime restart, topic mutation, or .201 mutation. Static diff inspection
      confirms this OCC evidence covers merge eligibility only; runtime proof is not claimed by this receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13648/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-13648/dod-deploy-assessment/command.yaml &&
          grep -qi 'runtime proof is not claimed' drift/dod_receipts/OMN-13648/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC eligibility receipt for this onex_change_control PR. Carries the OMN-13648 contract
      and PASS receipts onto dev so omnimarket #1457 and omnibase_infra #2114 can reference Evidence-Source:
      OCC#3191.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13648/dod-occ-self/command.yaml && grep -q '^pr_number:
          3191$' drift/dod_receipts/OMN-13648/dod-occ-self/command.yaml
