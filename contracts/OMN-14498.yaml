---
schema_version: "1.0.0"
ticket_id: "OMN-14498"
title: "Autobind OCC evidence for OMN-14498"
summary: "Central deploy-gate evidence for omnibase_infra PR #2436."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2436 product diff scope present"
    command: "gh pr view 2436 --repo OmniNode-ai/omnibase_infra --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2436-deploy-scope"
    description: "Deploy-gate scope for OMN-14498 delivered by omnibase_infra PR #2436. The runtime handler
      path adds _route_sync_publisher_failure so failed fire-and-forget sync publisher payloads are routed
      to the existing duck-typed _publish_raw_to_dlq surface instead of being only logged. No live deploy,
      docker exec, or .201 mutation is authorized by this evidence repair."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=d2be06024541bd5fa3dfd1e90ce655570f10a0bd'
          --jq .content | base64 -d | grep -Fq '_route_sync_publisher_failure'"
  - id: "occ-self-bind-pr-4799"
    description: "OCC companion PR #4799 -- self-bind for OMN-14498 evidence used by omnibase_infra#2436."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4799 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
  - id: "dod-omnibase-infra-pr-2604-ack-dlq-lineage"
    description: "PR #2604 preserves ingress correlation lineage across ACK/DLQ roundtrip and refuses
      to ACK a non-durable DLQ write at exact head ca599dc74ea3c7fbbe8c47135f54293c5f10e971."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 2604 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid,baseRefName --jq '.number == 2604 and .headRefOid == "ca599dc74ea3c7fbbe8c47135f54293c5f10e971" and .baseRefName == "dev"' | grep -qx true &&
          files="$(gh api repos/OmniNode-ai/omnibase_infra/pulls/2604/files --jq '.[].filename')" &&
          printf '%s\n' "$files" | grep -qx 'src/omnibase_infra/runtime/auto_wiring/handler_wiring.py' &&
          printf '%s\n' "$files" | grep -qx 'tests/fixtures/seams/ack_dlq/poisoned_command.json' &&
          printf '%s\n' "$files" | grep -qx 'tests/unit/runtime/auto_wiring/test_boundary_dlq_omn14507.py' &&
          printf '%s\n' "$files" | grep -qx 'tests/unit/runtime/auto_wiring/test_seam_correlation_survives_ack_and_dlq_roundtrip_omn14498.py' &&
          test "$(printf '%s\n' "$files" | grep -c .)" -eq 4 &&
          handler="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=ca599dc74ea3c7fbbe8c47135f54293c5f10e971' --jq .content | base64 -d)" &&
          printf '%s' "$handler" | grep -qF 'def _ingress_correlation_id' &&
          printf '%s' "$handler" | grep -qF 'class BoundaryDlqNotPersistedError' &&
          test_body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/tests/unit/runtime/auto_wiring/test_seam_correlation_survives_ack_and_dlq_roundtrip_omn14498.py?ref=ca599dc74ea3c7fbbe8c47135f54293c5f10e971' --jq .content | base64 -d)" &&
          printf '%s' "$test_body" | grep -qF 'test_seam_correlation_survives_ack_and_dlq_roundtrip' &&
          printf '%s' "$test_body" | grep -qF 'poisoned_command.json'
  - id: "occ-self-bind-pr-5835"
    description: "OCC companion PR #5835 -- self-bind for OMN-14498 evidence used by omnibase_infra#2604."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5835 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2672"
    description: "PR #2672 on OmniNode-ai/omnibase_infra - exact-head result-applier publish seam diff."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2672 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,files
          --jq 'if (.number == 2672 and .state == "OPEN" and .headRefName == "jonah/omn-14498-result-applier-dlq-seam"
          and .headRefOid == "cd4966c0d197d2c1c0ee2528ebbacfbf32e5a0d8" and ([.files[].path] | sort) ==
          ["src/omnibase_infra/runtime/auto_wiring/handler_wiring.py","tests/unit/runtime/auto_wiring/test_result_applier_publish_failure_seam_omn14498.py"])
          then "true" else error("unexpected #2672 state, head, or file set") end'
  - id: "dod-occ-evidence-validator-pr-2672"
    description: "Hosted OCC evidence admissibility validator for OMN-14498 / PR #2672."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2672"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6139"
    description: "Binds OMN-14498 to OCC companion PR #6139."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6139 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2672-rebind"
    description: >
      Rebind of dod-OmniNode-ai-omnibase_infra-pr-2672 to PR #2672's current head after new commits landed
      post-#6139 merge (same file set, new head commit). Append-only: does not edit the original entry's
      bytes.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2672"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2672 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,files
          --jq 'if (.number == 2672 and .state == "OPEN" and .headRefName == "jonah/omn-14498-result-applier-dlq-seam"
          and .headRefOid == "be65d9c4ddf2b391f4b730922331011940cf8a1c" and ([.files[].path] | sort) ==
          ["src/omnibase_infra/runtime/auto_wiring/handler_wiring.py","tests/unit/runtime/auto_wiring/test_result_applier_publish_failure_seam_omn14498.py"])
          then "true" else error("unexpected #2672 state, head, or file set") end'
  - id: "occ-self-bind-pr-6266"
    description: "Binds OMN-14498 to replacement OCC companion PR #6266, rebinding infra PR #2672 to its
      current head after #6139 merged against a stale head."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-6139"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 6266 --repo OmniNode-ai/onex_change_control --json number,state,headRefName --jq
          'if (.number == 6266 and (.state == "OPEN" or .state == "MERGED") and .headRefName == "mergesweep-0809-occ2672-rebind")
          then "true" else error("unexpected OCC self-bind PR state") end'
  - id: "dod-infra-pr-2672-merged-bind"
    description: >-
      Merged-state rebind of the result-applier publish seam carrier: PR #2672 landed on dev, so the exact-head
      OPEN bind frozen in dod-OmniNode-ai-omnibase_infra-pr-2672 no longer describes live state and fails
      closed forever. Asserts the carrier is MERGED from its expected branch at final head 8d8cf119461c5c054f2731c5cc0ff8b96dcf3068
      with merge commit 6fdbba6d8b6bcd7cc98a3da7dc8eb510f67a0e6a and the merged three-file set (an integration
      RED seam test joined the file set after the original bind was frozen). Append-only: does not edit
      the original entry's bytes.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2672"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2672 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,mergeCommit,files
          --jq 'if (.number == 2672 and .state == "MERGED" and .headRefName == "jonah/omn-14498-result-applier-dlq-seam"
          and .headRefOid == "8d8cf119461c5c054f2731c5cc0ff8b96dcf3068" and .mergeCommit.oid == "6fdbba6d8b6bcd7cc98a3da7dc8eb510f67a0e6a"
          and ([.files[].path] | sort) == ["src/omnibase_infra/runtime/auto_wiring/handler_wiring.py","tests/integration/test_state_io_outbox_seam_red.py","tests/unit/runtime/auto_wiring/test_result_applier_publish_failure_seam_omn14498.py"])
          then "dod-infra-pr-2672-merged-bind" else error("unexpected merged 2672 state") end'
  - id: "dod-infra-pr-2672-rebind-retire"
    description: >-
      Retires the interim OPEN-head rebind (dod-OmniNode-ai-omnibase_infra-pr-2672-rebind), whose pinned
      pre-merge head be65d9c4 and two-file set no longer match the merged carrier, so it fails closed
      forever. Same merged-state assertion as dod-infra-pr-2672-merged-bind but discriminated to this
      item. Append-only: does not edit the original entry's bytes.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2672-rebind"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2672 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid,mergeCommit
          --jq 'if (.number == 2672 and .state == "MERGED" and .headRefOid == "8d8cf119461c5c054f2731c5cc0ff8b96dcf3068"
          and .mergeCommit.oid == "6fdbba6d8b6bcd7cc98a3da7dc8eb510f67a0e6a") then "dod-infra-pr-2672-rebind-retire"
          else error("unexpected merged 2672 state for rebind retire") end'
  - id: "dod-infra-pr-2436-deploy-scope-pinned"
    description: >-
      Pipe-safe re-execution of the deploy-scope content probe. The original check's 'grep -Fq' closes
      the pipe on first match while base64 is still writing, so the probe dies of SIGPIPE (observed exit
      141) even though the asserted content is present; the replacement counts matches inside a command
      substitution (reads full input, no early pipe close) and pairs the probe with a literal live view
      of the merged carrier PR so the live-state binding derives from this entry's own check_value. Append-only:
      does not edit the original entry's bytes.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-omnibase-infra-pr-2436-deploy-scope"
    checks:
      - check_type: "command"
        check_value: |-
          test "$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=d2be06024541bd5fa3dfd1e90ce655570f10a0bd' --jq .content | base64 -d | grep -Fc '_route_sync_publisher_failure')" -ge 1 &&
          gh pr view 2436 --repo OmniNode-ai/omnibase_infra --json number,state --jq '.number == 2436 and .state == "MERGED"' | grep -qx true &&
          echo dod-infra-pr-2436-deploy-scope-pinned
  - id: "dod-infra-pr-2604-ack-dlq-lineage-pinned"
    description: >-
      Pipe-safe re-execution of the ACK/DLQ lineage probe at the same frozen head ca599dc74ea3c7fbbe8c47135f54293c5f10e971
      (now the MERGED carrier's final head). The original check greps large decoded file bodies with early-exit
      '-q' matchers, which kills the writer with SIGPIPE (observed exit 141); the replacement performs
      the same content assertions via counting matchers inside command substitutions and adds the carrier's
      merged live state. Append-only: does not edit the original entry's bytes.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-omnibase-infra-pr-2604-ack-dlq-lineage"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 2604 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid,baseRefName --jq '.number == 2604 and .state == "MERGED" and .headRefOid == "ca599dc74ea3c7fbbe8c47135f54293c5f10e971" and .baseRefName == "dev"' | grep -qx true &&
          files="$(gh api repos/OmniNode-ai/omnibase_infra/pulls/2604/files --jq '.[].filename')" &&
          test "$(printf '%s\n' "$files" | grep -Fxc 'src/omnibase_infra/runtime/auto_wiring/handler_wiring.py')" -eq 1 &&
          test "$(printf '%s\n' "$files" | grep -Fxc 'tests/fixtures/seams/ack_dlq/poisoned_command.json')" -eq 1 &&
          test "$(printf '%s\n' "$files" | grep -Fxc 'tests/unit/runtime/auto_wiring/test_boundary_dlq_omn14507.py')" -eq 1 &&
          test "$(printf '%s\n' "$files" | grep -Fxc 'tests/unit/runtime/auto_wiring/test_seam_correlation_survives_ack_and_dlq_roundtrip_omn14498.py')" -eq 1 &&
          test "$(printf '%s\n' "$files" | grep -c .)" -eq 4 &&
          handler="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=ca599dc74ea3c7fbbe8c47135f54293c5f10e971' --jq .content | base64 -d)" &&
          test "$(printf '%s' "$handler" | grep -Fc 'def _ingress_correlation_id')" -ge 1 &&
          test "$(printf '%s' "$handler" | grep -Fc 'class BoundaryDlqNotPersistedError')" -ge 1 &&
          test_body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/tests/unit/runtime/auto_wiring/test_seam_correlation_survives_ack_and_dlq_roundtrip_omn14498.py?ref=ca599dc74ea3c7fbbe8c47135f54293c5f10e971' --jq .content | base64 -d)" &&
          test "$(printf '%s' "$test_body" | grep -Fc 'test_seam_correlation_survives_ack_and_dlq_roundtrip')" -ge 1 &&
          test "$(printf '%s' "$test_body" | grep -Fc 'poisoned_command.json')" -ge 1 &&
          echo dod-infra-pr-2604-ack-dlq-lineage-pinned
  - id: "dod-occ-evidence-validator-pr-2672-pinned"
    description: >-
      Re-runs the hosted evidence admissibility validator verbatim and pairs it with a literal live view
      of the subject product PR, so the live-state derivation stops skipping: the base entry's receipt
      records a carrier pr_number that no receipt field consistently pairs with a repo, and its check_value
      carries no PR pin of its own. Append-only: does not edit the original entry's bytes.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-evidence-validator-pr-2672"
    checks:
      - check_type: "command"
        check_value: |-
          uv run pytest tests/test_evidence_admissibility.py -q &&
          gh pr view 2672 --repo OmniNode-ai/omnibase_infra --json number,state --jq '.number == 2672 and .state == "MERGED"' | grep -qx true &&
          echo dod-occ-evidence-validator-pr-2672-pinned
  - id: "occ-self-bind-pr-6541"
    description: >-
      Binds OMN-14498 to OCC evidence PR #6541, the merged-state rebind + pipe-safe probe supersede set
      that this contract's five newest entries declare. Every OCC evidence PR carries a self-bind entry
      so at least one PASS receipt for the ticket resolves to the PR under review; without it the eligibility
      gate reports pr_ticket_mismatch even with every other receipt green. Append-only: supersedes the
      prior self-bind rather than editing it.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-6266"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 6541 --repo OmniNode-ai/onex_change_control --json number,state,headRefName --jq
          'if (.number == 6541 and (.state == "OPEN" or .state == "MERGED") and .headRefName == "jonah/omn-14498-dod-merged-rebind")
          then "true" else error("unexpected OCC self-bind PR state") end'
