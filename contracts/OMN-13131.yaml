---
schema_version: "1.0.0"
ticket_id: "OMN-13131"
title: "OMN-13131 — Phase 1 bus-native action loop (renderer-as-effect emitter + capability projection)
  DoD contract"
summary: >-
  Phase 1 of the Contract-Driven UI Platform (epic OMN-13129), per plan docs/plans/2026-06-13-contract-driven-ui-platform-unified-plan.md
  §8 Phase 1 and §7. Generalizes the runtime action loop so that the renderer effect node emits every
  UI action as a canonical command envelope directly onto the event bus over bus transport (the WebSocket↔Kafka
  write-direction bridge), removing both HTTP hops from the action path — the omnidash per-action route
  /api/delegation/trigger and the /v1/effects REST surface. Generalizes ProjectionContainer + the OMN-10282
  resolveChartAdapter into a ModelRendererCapabilityContract-driven dispatcher (capabilities become dispatch-driving,
  not metadata). Adds the capability registry projection per §7: each renderer publishes its capability
  contract on startup/heartbeat over command topic onex.cmd.ui.renderer-capability-declared.v1, consumed
  by node_renderer_capability_projection (omnimarket reducer) which materializes the projection; the projection
  service refuses to send a client a UI contract requiring a component kind that client's capability row
  does not freshly advertise, surfacing typed upstream-blocked rather than rendering blind. Hard rules
  become enforcement gates: every action is a declared command emitter; every disabled state carries an
  EnumEmptyStateReason; the no-projection-fallback generalizes into a cross-renderer enforcement gate;
  every screen traces to a projection. This OCC contract is the central, citable Evidence-Source making
  OMN-13131 a gate-valid ticket so the Phase-1 code PRs pass the verify/verify Receipt Gate. The per-code-PR
  binding receipts (renderer effect-node emitter, capability-projection reducer, typed empty-state, cross-renderer
  gate) accrue under drift/dod_receipts/OMN-13131/ as the Phase-1 PRs land — and are consolidated via
  the W8 contract-derived golden-chain OCC pass; this initial landing carries the dod-occ-self self-binding
  evidence item so the OCC PR satisfies its own OCC eligibility check now.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "protocols"
  - "envelopes"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-self"
    description: >-
      onex_change_control PR (dev) carries the OMN-13131 central Phase-1 contract; this self-binding receipt
      makes the OCC PR satisfy its own OCC eligibility check (binds on the OCC pr_number, resolving pr_ticket_mismatch).
      The Phase-1 interface surfaces declared here — the new command topic onex.cmd.ui.renderer-capability-declared.v1,
      ModelRendererCapabilityContract (omnibase_core Phase 0, OMN-13130), and the spi ProtocolRenderer
      (OMN-13221) — are the interfaces the per-code-PR binding receipts will bind against as the Phase-1
      PRs land. The receipt's probe re-runs validate-yaml against the authored contract so the evidence
      is the real validator output, not a self-claim.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13131/dod-occ-self/command.yaml && grep -q '^ticket_id:
          "OMN-13131"$' drift/dod_receipts/OMN-13131/dod-occ-self/command.yaml
  - id: "dod-core-pr-1260"
    description: >-
      Per-code-PR binding receipt for omnibase_core promotion PR #1260 (promotion/omn-13131-core-dev-to-main,
      base=main). The dod-occ-self item above binds the OCC PR on its own pr_number; the OCC merge-eligibility
      gate additionally requires at least one PASS receipt to bind to the downstream citing PR via pr_number
      or one of its commit SHAs. This receipt binds OMN-13131 to core PR #1260 so the dev->main promotion
      that carries the Phase-1 core models (v0.45.0) passes verify/verify OCC eligibility. Mirrors the
      proven OMN-12934 dod-core-pr-1240 per-PR binding shape.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13131/dod-core-pr-1260/command.yaml
  - id: "dod-core-pr-1261"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1261 (jonah/omn-13131-action-gate-policy, base=dev)
      — item W7 / ADR D2. This is the code PR that gives the UI action-gate risk/confidence fields their
      canonical home: it adds ModelActionGatePolicy (confidence_threshold, requires_user_confirmation,
      risk_level, reversible, commit_level) plus EnumRiskLevel + EnumCommitLevel under enums/ui, and composes
      the policy into ModelActionContract as an optional gate_policy member. ModelGateSpec (objective
      scoring, OMN-2537) is left untouched and distinct per ADR D2. This receipt binds OMN-13131 to core
      PR #1261 (pr_number + commit_sha) so the verify/verify OCC merge-eligibility gate finds a PASS receipt
      bound to the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13131/dod-core-pr-1261/command.yaml
  - id: "dod-omnibase-infra-pr-2028"
    description: >-
      Per-code-PR binding receipt for omnibase_infra PR #2028 (jonah/omn-13131-w5-ui-producer-allowlist,
      base=dev) — item W5 prerequisite. Adds the canonical cross-renderer producer segment 'ui' to the
      topic-naming-lint allowlist (_KNOWN_PRODUCERS in scripts/validation/lint_topic_names.py) so the
      new W5 command topic onex.cmd.ui.renderer-capability-declared.v1 is accepted (producer 'ui' is a
      cross-renderer domain — the UI renderer effect nodes ui.effect.web/cli/ios thin-publish onto the
      bus, owned by no single backend repo). omnimarket CI clones omnibase_infra dev for the lint script,
      so this must land first. This receipt binds OMN-13131 to infra PR #2028 (pr_number + commit_sha)
      so the verify/verify OCC merge-eligibility gate finds a PASS receipt bound to the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13131/dod-omnibase-infra-pr-2028/command.yaml
  - id: "dod-omnimarket-pr-1266"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1266 (jonah/omn-13131-w5-capability-projection, base=dev)
      — item W5. Adds node_renderer_capability_projection, the sole-writer NodeReducer that materializes
      the Renderer Capability Registry as a heartbeat-backed projection (contract.yaml + ProtocolMessageHandler.handle(envelope)
      -> ModelHandlerOutput.for_reducer, pure/deterministic; no CapabilityRegistry class, no in-memory
      authority, no UI-owned state). Registers the command topic onex.cmd.ui.renderer-capability-declared.v1
      via the RENDERER_CAPABILITY_DECLARED_TOPIC_V1 constant (validate_topic_suffix; no onex.* literal
      in the node Python path, G-E). Projection schema carries renderer_id/platform/supported_component_kinds[]/interaction_model/accessibility_tier/contract_version/
      declared_at/last_heartbeat with heartbeat-TTL freshness -> is_degraded and typed EnumEmptyStateReason.UPSTREAM_BLOCKED
      for stale/absent rows. 16 tests (reducer fold, TTL freshness false AND true, real-dispatch-path
      via canonical contract_loader, G-E no-literal); mypy --strict + ruff + runtime-sweep + node-drift
      clean. This receipt binds OMN-13131 to omnimarket PR #1266 (pr_number + commit_sha) so the verify/verify
      OCC merge-eligibility gate finds a PASS receipt bound to the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13131/dod-omnimarket-pr-1266/command.yaml
  - id: "dod-w5-deploy-readiness"
    description: >-
      Deploy-readiness attestation for W5 (dev lane only). omnimarket PR #1266 adds the runtime-discovered
      node_renderer_capability_projection reducer contract, handler, model, migration, and projection
      exposure. The PR does NOT itself deploy, rebuild runtime images, restart runtime, mutate stability-test/prod/judge/.201,
      or publish live traffic. It is deploy-safe because the new node is contract-declared, pure/deterministic,
      covered by the 16-test reducer + real-dispatch-path suite, and uses only topic names validated by
      the current omnibase_infra topic-naming lint. The live projection read proof is exercised on the
      next dev-lane deploy agent run.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13131/dod-w5-deploy-readiness/command.yaml &&
          grep -q 'deploy-safe' drift/dod_receipts/OMN-13131/dod-w5-deploy-readiness/command.yaml
  - id: "dod-omnimarket-w8-pr-1269"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1269 (jonah/omn-13131-w8-golden-chains, base=dev)
      — item W8 / gate G-F. Turns the W5 sole-writer renderer-capability projection reducer (node_renderer_capability_projection,
      merged @ 7abdc1d6) into deterministic replay material: three contract-derived golden chains authored
      as JSON fixtures and replayed through the real W5 fold AND the real-dispatch-path handler resolved
      via the canonical omnibase_core contract_loader.load_contract; each chain's projection end-state
      is asserted byte-for-byte against its fixture. POSITIVE: capability declared on onex.cmd.ui.renderer-capability-declared.v1
      -> projection materialized -> heartbeat fresh -> is_degraded=false. NEGATIVE (a): declared -> heartbeat
      TTL expires -> is_degraded=true carrying typed EnumEmptyStateReason.UPSTREAM_BLOCKED. NEGATIVE (b):
      NO capability -> dispatcher requests a renderer -> resolution against the empty projection finds
      no row -> the typed EnumEmptyStateReason.UPSTREAM_BLOCKED gate is surfaced rather than a blind render.
      This receipt binds OMN-13131 to omnimarket PR #1269 (pr_number + commit_sha) so the verify/verify
      OCC merge-eligibility gate finds a PASS receipt bound to the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13131/dod-omnimarket-w8-pr-1269/command.yaml
  - id: "dod-omnimarket-pr-1281"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1281 (jonah/omn-13131-w5-projection-runner-materialize,
      base=dev) — W5 projection-runner materialization fix. The PR conforms node_renderer_capability_projection
      to the canonical projection-runner protocol used by db_io materializing projections: handle(input_data:
      dict) receives the flattened domain payload plus injected _db adapter, folds the renderer capability
      declaration, and upserts rows directly into renderer_capability_projection. It adds a golden-chain
      test through the real MessageDispatchEngine materialization path with the exact omnidash on-wire
      renderer-capability heartbeat bytes, proving row_count > 0 where the previous reducer-shaped handler
      crashed on envelope.payload and returned projections that the projection runner discards. This receipt
      binds OMN-13131 to omnimarket PR #1281 (pr_number + commit_sha) so the verify/verify OCC merge-eligibility
      gate finds a PASS receipt bound to the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13131/dod-omnimarket-pr-1281/command.yaml
  - id: "dod-occ-omnimarket-1281-self"
    description: >-
      Self-binding receipt for onex_change_control PR #2794 (jonah/omn-13131-omnimarket-1281-receipt,
      base=dev), which adds the central OCC receipt binding OMN-13131 to omnimarket PR #1281. The OCC
      PR refreshes every OMN-13131 receipt contract_sha256 after appending dod-omnimarket-pr-1281, so
      the receipt hardening invariant remains true across the ticket receipt set. This self-binding receipt
      carries the OCC PR number so the OCC PR satisfies its own merge-eligibility check while proving
      the contract validates locally.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13131/dod-occ-omnimarket-1281-self/command.yaml
  - id: "dod-occ-pinfix-self"
    description: >-
      Self-binding receipt for the OCC core-pin alignment PR (onex_change_control PR #2771, jonah/omn-13131-core-pin-045-occ,
      base=dev). The PR moves [tool.uv.sources].omnibase-core git rev from f325e759 (omnibase_core 0.44.0)
      to dc69b726 (the released v0.45.0 tag) so OCC resolves the same omnibase_core 0.45.0 as omnibase_infra
      (PR #2034) and omnimarket in the dev runtime image — restoring a single 0.45.0 resolution and the
      renderer-capability contract (model_renderer_capability_contract) that W5 reducer auto-wiring requires.
      The OCC's PyPI constraint (>=0.36.0) already permitted 0.45.0; the source git rev was the binding
      cap. This self-binding receipt carries the OCC pr_number (2771) so the OCC PR satisfies its own
      OCC merge-eligibility check (binds on pr_number, resolving pr_ticket_mismatch). The probe re-runs
      validate-yaml against the authored contract so the evidence is the real validator output, not a
      self-claim.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13131/dod-occ-pinfix-self/command.yaml && grep
          -q '^ticket_id: "OMN-13131"$' drift/dod_receipts/OMN-13131/dod-occ-pinfix-self/command.yaml
  - id: "dod-omnibase-infra-pinfix"
    description: >-
      Per-code-PR binding receipt for omnibase_infra PR #2034 (jonah/omn-13131-core-pin-045-infra, base=dev)
      — the renderer-capability runtime core-pin bump. The dev runtime image vendored omnibase_core 0.44.2
      because omnibase_infra capped omnibase-core>=0.44.0,<0.45.0 (PyPI constraint + [tool.uv] override
      + [tool.uv.sources] git rev ae8793bd=0.44.2) while omnimarket pins >=0.45.0,<0.46.0; the incompatible
      upper bound forced the resolver to 0.44.2, missing model_renderer_capability_contract and breaking
      W5 reducer auto-wiring (ModuleNotFoundError). PR #2034 bumps all three pin surfaces to the released
      v0.45.0 (additive over 0.44.2), re-resolves uv.lock (omnibase-core 0.44.2 -> 0.45.0), regenerates
      the version_compatibility _FALLBACK_MATRIX, and refreshes the runner-image identity lock for the
      new shared-env digest. This receipt binds OMN-13131 to infra PR #2034 (pr_number + commit_sha) so
      the verify/verify OCC merge-eligibility gate finds a PASS receipt bound to the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13131/dod-omnibase-infra-pinfix/command.yaml
