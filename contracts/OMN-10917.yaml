---
schema_version: "1.0.0"
ticket_id: "OMN-10917"
title: "Delegation env read scanner in report mode"
summary: "Adds AST-based delegation env-var read scanners in omnimarket and omnibase_infra report mode
  so Wave 2 can inventory direct os.environ/os.getenv usage before enforcement."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Scanner unit tests pass for omnimarket and omnibase_infra report-mode implementations."
    command: "gh pr checks --repo OmniNode-ai/omnimarket 615 && gh pr checks --repo OmniNode-ai/omnibase_infra
      1577"
  - kind: "manual"
    description: "Central OCC receipts bind OMN-10917 to both downstream scanner PRs."
    command: "find drift/dod_receipts/OMN-10917 -type f -name command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-615"
    description: "omnimarket PR #615 adds the delegation env read scanner in report mode."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10917/dod-omnimarket-pr-615/command.yaml
          && grep -q '^pr_number: 615$' drift/dod_receipts/OMN-10917/dod-omnimarket-pr-615/command.yaml"
  - id: "dod-infra-pr-1577"
    description: "omnibase_infra PR #1577 adds the delegation env read scanner in report mode."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10917/dod-infra-pr-1577/command.yaml
          && grep -q '^pr_number: 1577$' drift/dod_receipts/OMN-10917/dod-infra-pr-1577/command.yaml"
  - id: "dod-occ-pr-982"
    description: "OCC PR #982 carries the OMN-10917 central contract and downstream receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10917/dod-occ-pr-982/command.yaml
          && grep -q '^pr_number: 982$' drift/dod_receipts/OMN-10917/dod-occ-pr-982/command.yaml"
