---
schema_version: "1.0.0"
ticket_id: "OMN-11922"
title: "Semantic antipattern validator orchestrator and classifier compute"
summary: >
  Bind central evidence for omnimarket PR #833, which splits semantic antipattern validation into node_semantic_antipattern_validator_orchestrator
  and node_semantic_antipattern_classifier_compute. The orchestrator emits antipattern-match commands
  and the classifier computes deterministic blocking or advisory violations from explicit match input.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-833"
    description: "Bind OMN-11922 evidence to omnimarket PR #833."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11922/dod-omnimarket-pr-833/command.yaml
          && grep -q '^pr_number: 833$' drift/dod_receipts/OMN-11922/dod-omnimarket-pr-833/command.yaml"
  - id: "dod-semantic-antipattern-local-validation"
    description: "Focused semantic antipattern classifier and validator orchestrator tests pass for the
      current omnimarket PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11922/dod-semantic-antipattern-local-validation/command.yaml
          && grep -q '17 passed' drift/dod_receipts/OMN-11922/dod-semantic-antipattern-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: validator orchestration and classifier compute contracts declare
      runtime topics, handler routing, purity, and deploy readiness without requiring live runtime deploy
      before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11922/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11922/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR #1559 carries the central OMN-11922 contract, deploy evidence, and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11922/dod-occ-pr/command.yaml &&
          grep -q '^pr_number: 1559$' drift/dod_receipts/OMN-11922/dod-occ-pr/command.yaml"
