---
schema_version: "1.0.0"
ticket_id: "OMN-14844"
title: "Autobind OCC evidence for OMN-14844"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1839."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1839 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1839"
    description: "PR #1839 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1839-ci"
    description: "PR #1839 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-OmniNode-ai-omnimarket-pr-1839-deploy-scope"
    description: "PR #1839 on OmniNode-ai/omnimarket — deploy-scope evidence for runtime-path change;
      no live deploy, restart, or runtime mutation performed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "SOURCE_PR_NUMBER=1839; gh pr view \"$SOURCE_PR_NUMBER\" --repo OmniNode-ai/omnimarket
          --json files # deploy-scope: runtime-path change; no live deploy, restart, or runtime mutation
          performed in this evidence lane."
  - id: "occ-self-bind-pr-4500"
    description: "OCC companion PR #4500 — self-bind for OMN-14844 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4503"
    description: "OCC deploy-evidence patch PR #4503 — self-bind for OMN-14844."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
