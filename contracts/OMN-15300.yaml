---
schema_version: "1.0.0"
ticket_id: "OMN-15300"
title: "Autobind OCC evidence for OMN-15300"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1928."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1928 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1928"
    description: "PR #1928 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1928-ci"
    description: "PR #1928 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5262"
    description: "OCC companion PR #5262 — self-bind for OMN-15300 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omn15300-observation-store-deploy-probe"
    description: >-
      Deploy-scope probe for the OCC observation store. Reads this repo's dev branch through the GitHub
      API and requires the drift/occ_observations subtree to be present and non-empty. Its exit status
      depends on live repo state at a ref rather than on text the author wrote, and it goes RED if the
      store surface this change protects disappears. Added because the autobind-generated entries above
      are all `gh pr view` self-binds, which the OMN-14443 falsifiability ratchet correctly classifies
      as vacuous, leaving omnimarket#1928 blocked on deploy-gate with "no cited ticket has deploy DoD
      evidence". WHAT IT DOES NOT PROVE, stated plainly: it is not evidence that the write path is alive
      again. The stronger readback for that is a record committed under drift/occ_observations after the
      freeze point (newest landed record before the fix: 2026-07-27T03:58:48Z), which reads 0 as of 2026-07-28T06:0xZ
      and by construction cannot go GREEN until the fixed producer has merged AND run. A pre-merge gate
      cannot assert a post-merge outcome, so that readback is carried as the ticket's live-readback acceptance
      criterion instead of as a check that would block its own contract PR forever.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/onex_change_control/contents/drift/occ_observations?ref=dev" --jq 'length' | grep -qv '^0$'
