---
schema_version: "1.0.0"
ticket_id: "OMN-15300"
title: "Autobind OCC evidence for OMN-15300"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1928."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1928 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1928"
    description: "PR #1928 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1928-ci"
    description: "PR #1928 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5262"
    description: "OCC companion PR #5262 — self-bind for OMN-15300 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1928-deploy"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-1928"
    description: >-
      Deploy-scope probe: reads this repo's dev branch through the GitHub API and requires the drift/occ_observations
      store subtree to be present and non-empty. It is falsifiable in the sense the ratchet asks for —
      its exit status depends on live repo state at a ref, not on text the author wrote — and it goes
      RED if the store surface this change protects is missing. WHAT IT DOES NOT PROVE, stated plainly:
      it does not prove the write path is alive again. The stronger probe for that is a record committed
      under drift/occ_observations after the freeze point (newest landed record before the fix: 2026-07-27T03:58:48Z),
      which reads 0 as of 2026-07-28T05:20Z and by construction cannot go GREEN until the fixed producer
      has merged AND actually run. A pre-merge gate can never assert a post-merge outcome, so that readback
      is carried as the ticket's live-readback acceptance criterion instead of being embedded here as
      a check that would block its own contract PR forever. Supersedes dod-OmniNode-ai-omnimarket-pr-1928,
      whose only check was the PR-existence probe `gh pr view --json number,state` — true of any PR that
      exists, in any state, with any diff. The superseded entry is preserved for audit and reported WARN,
      not re-executed (OMN-15247).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/onex_change_control/contents/drift/occ_observations?ref=dev" --jq 'length' | grep -qv '^0$'
  - id: "occ-self-bind-pr-5268"
    description: "OCC companion PR #5268 — self-bind for OMN-15300 supersede deploy-evidence repair."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5268 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
