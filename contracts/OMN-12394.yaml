---
schema_version: "1.0.0"
ticket_id: "OMN-12394"
title: "Preserve real dirty clone work into worktrees or follow-up tickets before cleanup"
summary: >
  Dirty-clone audit found real uncommitted work across omnistream, onex-self-extending-agent,
  omniclaude, onex_change_control, omnicrush, and omnimusic canonical clones. This ticket
  owns preservation: classify every dirty file as real-work-to-preserve vs generated/disposable,
  move real work into worktrees and PRs, and produce a classification report.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-classification-report"
    description: >
      Classification report at docs/evidence/OMN-12368-verification/dirty-clone-classification.md
      in omni_home. Covers all 13 repos with dirty state; every file classified with verdict
      and preservation action.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f \"$OMNI_HOME/docs/evidence/OMN-12368-verification/dirty-clone-classification.md\""
  - id: "dod-preservation-prs"
    description: >
      Preservation PRs opened for all real-work repos with no blockers:
      omnidash PR #165 (gitignore), omnistream PR #4 (overlays), omnibase_infra PR #1787 (pins),
      onex-self-extending-agent PR #165 (eval harness), onex_change_control PR #1865 (batch OCC),
      omniclaude PR #1705 (nodes/hooks/validation), omnimusic PR #1 (uv.lock+README).
      omnicrush: initial commit pushed to new private repo OmniNode-ai/omnicrush.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 165 --repo OmniNode-ai/omnidash --json state -q .state | grep -q OPEN"
