---
schema_version: "1.0.0"
ticket_id: OMN-9530
title: "Compile ticket pipeline IMPLEMENT dispatch without runtime side effects"
summary: "Extend node_ticket_pipeline from PRE_FLIGHT into a bounded compile-only IMPLEMENT slice that
  prepares a dispatch-worker command, records the compiled result in phase details, and still stops LOCAL_REVIEW
  as not_implemented."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #447 checks pass including golden-chain tests, deploy-gate, and CodeRabbit
      thread gate"
    command: "gh pr checks 447 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "Ticket pipeline golden-chain and market skill smoke tests pass"
    command: "env -u PYTHONPATH uv run pytest tests/test_golden_chain_ticket_pipeline.py tests/test_market_skill_smokes.py
      -q"
  - kind: "manual"
    description: "Touched ticket pipeline code and baseline runner pass ruff"
    command: "uv run ruff check src/omnimarket/market_skill_baseline.py src/omnimarket/nodes/node_ticket_pipeline/handlers/handler_ticket_pipeline.py
      tests/test_golden_chain_ticket_pipeline.py tests/test_market_skill_smokes.py"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "IMPLEMENT phase compiles a dispatch-worker command with no agent spawn, runtime mutation,
      or dispatch-record persistence"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run pytest tests/test_golden_chain_ticket_pipeline.py -q"
  - id: "dod-002"
    description: "Market skill smoke baseline observes ticket_pipeline compiled_dispatch=true and LOCAL_REVIEW
      blocked as not_implemented"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run pytest tests/test_market_skill_smokes.py -q"
  - id: "dod-003"
    description: "Post-merge deploy/readiness smoke plan for ticket_pipeline IMPLEMENT compile-only path"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -m omnimarket.nodes.node_ticket_pipeline
          OMN-9530 --dry-run"
