---
# ModelTicketContract — OMN-10137
# Register claude and opencode as CLI subprocess backends in omnibase_infra
schema_version: "1.0.0"
ticket_id: OMN-10137
summary: "feat(OMN-10137): register claude-cli and opencode-cli as CLI subprocess backends in node_llm_inference_effect/contract.yaml
  and routing_tiers.yaml"
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - protocols
evidence_requirements:
  - kind: tests
    description: Unit tests pass (9 new tests, 442 total in node)
    command: uv run pytest tests/unit/nodes/node_llm_inference_effect/ -v
  - kind: ci
    description: CI pipeline green
    command: gh pr checks 1429 --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "inference.claude_cli and inference.opencode_cli present in contract.yaml handler_routing
      — 9 unit tests pass"
    source: generated
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/nodes/node_llm_inference_effect/handlers/test_handler_llm_cli_subprocess_claude_opencode.py
          -v — 9 passed"
  - id: dod-002
    description: "cli_agents tier with claude-cli and opencode-cli present in routing_tiers.yaml"
    source: generated
    checks:
      - check_type: command
        check_value: "deploy: contract.yaml is declarative config — no runtime deploy needed; HandlerLlmCliSubprocess
          uses shutil.which at dispatch time"
