---
# ModelTicketContract — OMN-10137
# Register claude and opencode as CLI subprocess backends in omnibase_infra
schema_version: "1.0.0"
ticket_id: OMN-10137
summary: "feat(OMN-10137): register claude-cli and opencode-cli as CLI subprocess backends in node_llm_inference_effect/contract.yaml
  and routing_tiers.yaml"
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - protocols
evidence_requirements:
  - kind: tests
    description: Unit tests pass (9 new tests, 442 total in node)
    command: uv run pytest tests/unit/nodes/node_llm_inference_effect/ -v
  - kind: ci
    description: CI pipeline green
    command: gh pr checks 1429 --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "9 unit tests pass proving inference.claude_cli and inference.opencode_cli are registered
      — receipt at drift/dod_receipts/OMN-10137/dod-001/command.yaml"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10137/dod-001/command.yaml && grep
          -q '^evidence_item_id: dod-001$' drift/dod_receipts/OMN-10137/dod-001/command.yaml && grep -q
          '^ticket_id: OMN-10137$' drift/dod_receipts/OMN-10137/dod-001/command.yaml && grep -q 'test_claude_handler_is_registered_in_contract'
          drift/dod_receipts/OMN-10137/dod-001/command.yaml"
  - id: dod-002
    description: "deploy gate: cli_agents tier registered; declarative-config change, no runtime deploy
      required — receipt at drift/dod_receipts/OMN-10137/dod-002/command.yaml"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10137/dod-002/command.yaml && grep
          -q '^evidence_item_id: dod-002$' drift/dod_receipts/OMN-10137/dod-002/command.yaml && grep -q
          '^ticket_id: OMN-10137$' drift/dod_receipts/OMN-10137/dod-002/command.yaml && grep -q 'cli_agents'
          drift/dod_receipts/OMN-10137/dod-002/command.yaml"
