---
schema_version: "1.0.0"
ticket_id: "OMN-14804"
title: "Autobind OCC evidence for OMN-14804"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2348."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2348 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2348"
    description: "PR #2348 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2348-ci"
    description: "PR #2348 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4444"
    description: "OCC companion PR #4444 — self-bind for OMN-14804 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnibase-infra-pr-2348-deploy-scope"
    description: >-
      Deploy-gate scope for omnibase_infra PR #2348. The change touches src/omnibase_infra/nodes/node_llm_inference_effect/handlers/handler_llm_cli_subprocess.py,
      a runtime handler path, but it only restores the canonical dispatch entrypoint for the existing
      CLI subprocess effect. No live runtime deployment is performed by this PR; the required deploy evidence
      is the merge-time CI/OCC gate suite plus post-merge availability of the handler path on dev.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          printf '%s\n' 'deploy scope assessed for omnibase_infra#2348 runtime handler path; no live deploy,
          docker exec, or rpk topic produce is authorized by this evidence repair'
  - id: "occ-self-bind-pr-4445"
    description: "OCC deploy-scope repair PR #4445 — self-bind for OMN-14804."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-onex_change_control-pr-4448"
    description: "PR #4448 on OmniNode-ai/onex_change_control — reusable imperative guard dev-PR hosted
      runner policy."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - id: "dod-OmniNode-ai-omniclaude-pr-1916"
    description: "PR #1916 on OmniNode-ai/omniclaude — reusable deploy, CR, and skip-token dev-PR hosted
      runner policy."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
