---
schema_version: "1.0.0"
ticket_id: "OMN-13623"
title: "WS-C SEA Phase 6.1: wire generation + entropy events to omnidash projection reducers"
summary: "Surface semantic + corpus verdict fields from the generation projection onto the omnidash SEA
  Control Plane"

is_seam_ticket: false
interface_change: false
interfaces_touched: []

evidence_requirements:
  - kind: ci
    description: "omnidash PR typecheck + tests + ESLint + all gate checks pass"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: docs
    description: "Entropy projection reducer BLOCKED status documented in PR body (no reducer, no DB migration,
      no live events)"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json body --jq '.body' | grep -q 'BLOCKED'"

emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""

dod_evidence:
  - id: "dod-001"
    description: "NodeGenerationRow in omnidash event-dash-api.ts includes semantic_checked, semantic_passed,
      corpus_checked, corpus_passed, corpus_errors, timestamp fields matching node_projection_delegation
      contract.yaml projection_api.exposures"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13623/dod-001/command.yaml && grep -q '^pr_number:
          244$' drift/dod_receipts/OMN-13623/dod-001/command.yaml
  - id: "dod-002"
    description: "SeaControlPage renders Semantic and Corpus verdict columns in the Recent Generations
      table and verdict badges on the latest-artifact panel"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13623/dod-002/command.yaml && grep -q '^pr_number:
          244$' drift/dod_receipts/OMN-13623/dod-002/command.yaml
  - id: "dod-003"
    description: "omnidash typecheck, ESLint, and all 1481 tests pass in CI (Typecheck and Tests job green)"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13623/dod-003/command.yaml && grep -q '^pr_number:
          244$' drift/dod_receipts/OMN-13623/dod-003/command.yaml
  - id: "dod-004"
    description: "Entropy projection reducer dependency documented as BLOCKED: no reducer node, no DB
      migration, no deployed service, no live events in dev lane"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13623/dod-004/command.yaml && grep -q '^pr_number:
          244$' drift/dod_receipts/OMN-13623/dod-004/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding OCC eligibility receipt for onex_change_control PR #3199. Carries the OMN-13623 contract
      and DoD receipts onto dev and binds the OCC evidence PR with a PASS receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13623/dod-occ-pr-self/command.yaml
