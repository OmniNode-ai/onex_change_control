---
schema_version: "1.0.0"
ticket_id: "OMN-11923"
title: "Pre-commit hook for KB antipattern checking"
summary: >
  Central OCC evidence for omnibase_core PR #1153, which adds antipattern_checker.py and a
  .pre-commit-hooks.yaml check-antipatterns entry. The offline CLI uses ValidatorBase.main() pattern,
  filters non-semantic rules only (no network), outputs registry version + hash, and is validated by
  13 unit tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1153"
    description: "omnibase_core PR #1153 adds the KB antipattern pre-commit hook."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11923/dod-omnibase_core-pr-1153/command.yaml
          && grep -q '^pr_number: 1153$' drift/dod_receipts/OMN-11923/dod-omnibase_core-pr-1153/command.yaml"
  - id: "dod-kb-antipattern-hook-local-validation"
    description: "13 unit tests for KB antipattern pre-commit hook pass; mypy --strict clean; violations detected and suppressed correctly."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11923/dod-kb-antipattern-hook-local-validation/command.yaml
          && grep -q '13 passed' drift/dod_receipts/OMN-11923/dod-kb-antipattern-hook-local-validation/command.yaml"
