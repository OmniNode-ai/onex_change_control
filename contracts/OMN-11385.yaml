---
schema_version: "1.0.0"
ticket_id: "OMN-11385"
title: "Golden task fixture and deterministic replay mode"
summary: >-
  onex-self-extending-agent PR #70 adds a golden fixture and `--replay` CLI mode that validates, registers,
  and invokes a deterministic generated node without making LLM calls. The PR was rebased onto current
  main during the merge sweep, preserving the replay-only delta on top of already-landed agent-demo work.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: tests
    description: "Replay-mode unit tests pass locally for onex-self-extending-agent PR #70."
    command: "uv run pytest tests/unit/test_replay.py -q"
  - kind: manual
    description: "Replay-mode touched files pass ruff."
    command: "uv run ruff check src/__main__.py tests/unit/test_replay.py"
  - kind: manual
    description: "No live deploy required; change is a local demo CLI replay path and fixture."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11385/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-70"
    description: "onex-self-extending-agent PR #70 carries the replay fixture and CLI implementation at
      the rebased head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11385/dod-product-pr-70/command.yaml"
          && grep -q '^pr_number: 70$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11385/dod-product-pr-70/command.yaml"
          && grep -q '^commit_sha: "d7ecde4f9cb1e47cadefc497829c6b6611dfa3f0"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11385/dod-product-pr-70/command.yaml"
  - id: "dod-local-proof"
    description: "Focused local replay tests and ruff checks pass after the rebase conflict repair."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11385/dod-local-proof/command.yaml"
          && grep -q '9 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11385/dod-local-proof/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11385/dod-local-proof/command.yaml"
  - id: "dod-deploy"
    description: "No live deploy required; deterministic replay mode is a local CLI/demo path with no
      runtime service mutation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11385/dod-deploy/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11385 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11385/dod-occ-self/command.yaml"
