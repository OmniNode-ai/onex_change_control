---
schema_version: "1.0.0"
ticket_id: "OMN-12191"
title: "Migrate routing models from compat to core"
summary: >
  Central OCC evidence for omnibase_core PR #1159. The change adds canonical routing and telemetry models
  to omnibase_core, keeps routing enum wire values in the enum layer, and proves the focused model tests
  plus naming, typing, and lint gates.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Routing and telemetry model unit tests pass on the PR head."
    command: "See drift/dod_receipts/OMN-12191/dod-focused-model-tests/command.yaml"
  - kind: "tests"
    description: "Class naming validation passes after moving routing enums out of models."
    command: "See drift/dod_receipts/OMN-12191/dod-class-naming/command.yaml"
  - kind: "tests"
    description: "Strict mypy validation passes for the three new model source files."
    command: "See drift/dod_receipts/OMN-12191/dod-strict-mypy/command.yaml"
  - kind: "tests"
    description: "Targeted ruff format and lint checks pass for touched routing files and tests."
    command: "See drift/dod_receipts/OMN-12191/dod-targeted-ruff/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carries the central OMN-12191 contract and receipts."
    command: "See drift/dod_receipts/OMN-12191/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-model-tests"
    description: "Focused routing and telemetry model tests passed on the omnibase_core PR #1159 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12191/dod-focused-model-tests/command.yaml
          && grep -q '23 passed' drift/dod_receipts/OMN-12191/dod-focused-model-tests/command.yaml"
  - id: "dod-class-naming"
    description: "Class naming validation passed after moving routing enums out of the models package."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12191/dod-class-naming/command.yaml
          && grep -q 'conform to naming conventions' drift/dod_receipts/OMN-12191/dod-class-naming/command.yaml"
  - id: "dod-strict-mypy"
    description: "Strict mypy validation passed for the three new model source files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12191/dod-strict-mypy/command.yaml
          && grep -q 'Success: no issues found in 3 source files' drift/dod_receipts/OMN-12191/dod-strict-mypy/command.yaml"
  - id: "dod-targeted-ruff"
    description: "Targeted ruff checks passed for touched routing files and tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12191/dod-targeted-ruff/command.yaml
          && grep -q 'All checks passed' drift/dod_receipts/OMN-12191/dod-targeted-ruff/command.yaml"
  - id: "dod-occ-pr"
    description: "This onex_change_control PR carries the central OMN-12191 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12191/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: \"OMN-12191\"$' drift/dod_receipts/OMN-12191/dod-occ-pr/command.yaml"
