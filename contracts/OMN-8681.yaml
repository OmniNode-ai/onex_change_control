---
schema_version: 1.0.0
ticket_id: OMN-8681
title: DeepSeek-R1 endpoint health for hostile reviewer pipeline
summary: Hostile reviewer inference bridge health/config degradation slice added with local tests.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - events
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Wave worker local verification evidence is captured in central OCC receipt.
    command: uv run pytest tests/test_adapter_inference_bridge.py tests/unit/test_bridge_config_loader.py
      tests/test_node_hostile_reviewer_contract.py tests/test_proof_of_life_e2e.py -q
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-wave-local-evidence
    description: Wave worker local verification or readiness evidence is captured as a canonical PASS
      receipt.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: uv run pytest tests/test_adapter_inference_bridge.py tests/unit/test_bridge_config_loader.py
          tests/test_node_hostile_reviewer_contract.py tests/test_proof_of_life_e2e.py -q
