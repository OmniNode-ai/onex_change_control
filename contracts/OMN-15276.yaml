---
schema_version: "1.0.0"
ticket_id: "OMN-15276"
title: "Autobind OCC evidence for OMN-15276"
summary: >
  OCC Evidence-Source autobind companion (OMN-13317 F1) for OMN-15276, covering OmniNode-ai/omniintelligence
  PR #819 and OmniNode-ai/omnibase_infra PR #2506.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #819 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "ci"
    description: "PR #2506 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniintelligence-pr-819"
    description: "PR #819 on OmniNode-ai/omniintelligence — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omniintelligence-pr-819-ci"
    description: "PR #819 on OmniNode-ai/omniintelligence — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5203"
    description: "OCC companion PR #5203 — self-bind for OMN-15276 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2506"
    description: "PR #2506 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5204"
    description: "Binds OMN-15276 to OCC companion PR #5204 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: dod-infra-omn-15276-canonical-ddl-in-applied-set
    description: "Binds OMN-15276 to OmniNode-ai/omnibase_infra PR #2506 at 0f7e3278ac76. The PRODUCT-OBSERVING
      probe lives in the receipt: it reads docker/migrations/intelligence/026_create_code_entities.sql
      -- the directory the .201 lanes actually apply, per MIGRATIONS_DIR: /migrations/intelligence in
      docker-compose.infra.yml and the ${MIGRATIONS_DIR}/*.sql glob in scripts/run-intelligence-migrations.sh
      -- at that commit and counts the canonical CREATE TABLE, with the base commit 45d4709ea recorded
      as the RED control and a ref-resolution control (the sibling 025_fix_llm_delegation_call_log_date_index.sql
      resolves at the same base ref) proving that RED is absence rather than a 404. The check_value here
      is the receipt-status read because the onex_change_control workflow GITHUB_TOKEN is repo-scoped
      and cannot reach the private OmniNode-ai/omnibase_infra contents API."
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15276/dod-infra-omn-15276-canonical-ddl-in-applied-set/command.yaml'
  - id: dod-oi-omn-15276-conflicting-migrations-retired
    description: "Binds OMN-15276 to OmniNode-ai/omniintelligence PR #819 at f3243ad17254. The PRODUCT-OBSERVING
      probe lives in the receipt: it asserts all four superseded migrations are ABSENT at the PR head
      and PRESENT at the parent c229901bbdc8 (the RED control), and that the 025 prefix -- triple-claimed
      before this change -- resolves to exactly one file afterwards. Absence-at-head plus presence-at-parent
      is the pair that makes the RED meaningful: a 404 alone could be a bad ref. The check_value here
      is the receipt-status read because the onex_change_control workflow GITHUB_TOKEN is repo-scoped
      and cannot reach the private OmniNode-ai/omniintelligence contents API."
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15276/dod-oi-omn-15276-conflicting-migrations-retired/command.yaml'
