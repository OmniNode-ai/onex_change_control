---
schema_version: "1.0.0"
ticket_id: "OMN-11816"
title: "SEA swarm quarantine — remove src/swarm/ after omnimarket nodes pass tests"
summary: >
  Removes src/swarm/ (6 files, 1014 lines) and the 3 test files that imported from it from onex-self-extending-agent.
  The swarm logic was prototyped in SEA (OMN-11781) and has been fully rehomed to omnimarket nodes OMN-11809
  through OMN-11815. Quarantine prevents SEA/omnimarket divergence on swarm models and dispatch logic.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-129"
    description: "SEA PR #129 removes src/swarm/ and 3 swarm test files from onex-self-extending-agent"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-11816/dod-sea-pr-129/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR binds this contract to onex_change_control as the evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-11816/dod-occ-pr/command.yaml\""
  - id: "dod-deploy"
    description: "Deploy assessment — pure deletion, no runtime deploy required"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-11816/dod-deploy/command.yaml\""
