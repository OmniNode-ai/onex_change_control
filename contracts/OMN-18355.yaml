---
schema_version: "1.0.0"
ticket_id: "OMN-18355"
title: "Autobind OCC evidence for OMN-18355"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3515.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3515 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3515 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/ci/test_ci_summary_gate.py tests/ci/test_incident_replay_omn18355.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3515"
    description: "PR #3515 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/ci/ci_summary_gate.py?ref=7db4d1cc9a1da1bcf79e751f3947eb3d9285b2ac --jq '.content' | base64 -d | grep -c 'def _is_non_verdict_row'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3515/files --paginate --jq '.[].filename' | grep -cFx 'scripts/ci/ci_summary_gate.py'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3515 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/ci/test_ci_summary_gate.py tests/ci/test_incident_replay_omn18355.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-9458"
    description: "Binds OMN-18355 to OCC companion PR #9458 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9458 --repo OmniNode-ai/onex_change_control --json number,state"
