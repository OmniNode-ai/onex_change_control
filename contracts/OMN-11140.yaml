---
schema_version: "1.0.0"
ticket_id: "OMN-11140"
title: "Task 4: Diff Hash Computation"
summary: >
  Central OCC evidence for omnibase_core PR #1089, which adds deterministic OmniGate diff and config hashing
  helpers using binary, full-index, no-renames Git diff context.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_core PR #1089 carries the OMN-11140 diff hashing change."
    command: "gh pr view 1089 --repo OmniNode-ai/omnibase_core --json state,headRefOid"
  - kind: "tests"
    description: "Focused diff hash tests and lint pass locally."
    command: "PYTHONPATH=src uv run pytest tests/unit/gate/test_diff_hash.py -q && PYTHONPATH=src uv run
      ruff check src/omnibase_core/gate tests/unit/gate"
  - kind: "manual"
    description: "No runtime deploy is required for pure hashing helper changes."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11140/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1089"
    description: "Bind OMN-11140 evidence to omnibase_core PR #1089 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11140/dod-omnibase-core-pr-1089/command.yaml\"
          && grep -q '^pr_number: 1089$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11140/dod-omnibase-core-pr-1089/command.yaml\"
          && grep -q '^commit_sha: b7d919b4ede5d087b0cad633ad99f129108e9496$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11140/dod-omnibase-core-pr-1089/command.yaml\""
  - id: "dod-focused-checks"
    description: "Focused diff hash tests and lint pass locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11140/dod-focused-checks/command.yaml\"
          && grep -q '12 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11140/dod-focused-checks/command.yaml\"
          && grep -q 'All checks passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11140/dod-focused-checks/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "No live deploy, runtime restart, .201 mutation, or event-bus write was performed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11140/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11140/dod-deploy-assessment/command.yaml\""
