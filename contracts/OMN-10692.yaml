---
schema_version: "1.0.0"
ticket_id: "OMN-10692"
title: "Extend inference bridge with OpenRouter free-tier support"
summary: >
  Adds an OpenRouter free-tier model catalog and registers OpenRouter-backed model keys in omnimarket
  inference bridge configuration when OPENROUTER_API_KEY is present. Hardens OpenRouter base URL handling
  so blank env overrides fall back to the contract default, and prevents config-provided extra_headers
  from overriding reserved Authorization or Content-Type request headers.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-openrouter-tests"
    description: "Focused OpenRouter and inference bridge tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10692/dod-openrouter-tests/command.yaml\""
  - id: "dod-config-hardening"
    description: "Blank OpenRouter base URL fallback and reserved header protection are covered by tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10692/dod-config-hardening/command.yaml\""
  - id: "dod-precommit"
    description: "Pre-commit hooks pass for omnimarket PR #560 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10692/dod-precommit/command.yaml\""
  - id: "dod-pr-560"
    description: "Bind OMN-10692 evidence to omnimarket PR #560 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10692/dod-pr-560/command.yaml\""
