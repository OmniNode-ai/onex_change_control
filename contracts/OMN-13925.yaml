---
schema_version: "1.0.0"
ticket_id: "OMN-13925"
title: "feat(OMN-13925): live env-parity collection effect + shared parity engine"
summary: >-
  omnimarket PR #1603 adds node_env_parity_collect_effect (read-only EFFECT): it snapshots each contract-declared
  runtime lane's container env over ssh (docker ps + docker inspect .Config.Env, never a mutation) and
  evaluates the shared omnimarket.parity engine over the freshly collected env, so the env_parity skill
  reports live lane parity instead of a static sample. The parity models + pure engine are promoted out
  of node_env_parity_compute into omnimarket.parity; the compute node is re-wired to the shared engine
  and is now the TEST-ONLY caller-supplied-snapshot primitive.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-verification"
    description: >-
      PR #1603 records the new-node integration + golden-chain coverage (fail-fast paths, live-collection
      provenance verdict, required-lane-down gap, secret-never-leaks), the compute refactor onto the shared
      engine, full non-kafka pytest (11889 passed), runtime sweep --import-check clean, node metadata
      dependency check PASS, ruff, and mypy evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13925/dod-product-verification/command.yaml"
  - id: "dod-pr-1603-binding"
    description: >-
      OmniNode-ai/omnimarket PR #1603 carries the OMN-13925 change at feature commit 8bba348f5ffb11ea2ba49bb1e73cff0f253a1aa2.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13925/dod-pr-1603-binding/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for the runtime paths touched by PR #1603 (the new node_env_parity_collect_effect
      contract/handler/models and the node_env_parity_compute refactor). No live deployment is performed
      in this PR; the node is a read-only lane collector picked up on the next runtime rebuild. The contract
      records the required deploy-gate evidence and post-merge runtime disposition.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13925/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      This onex_change_control companion PR carries the central OMN-13925 contract and PASS receipts for
      the downstream omnimarket PR #1603 merge sweep.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13925/dod-occ-self/command.yaml"
