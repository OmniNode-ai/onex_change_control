---
schema_version: 1.0.0
ticket_id: OMN-13638
title: exclude worktree clones from node_gap_compute contract walk
summary: >-
  node_gap_compute's repo-root walk re-scanned frozen per-ticket worktree snapshots (.claude/worktrees/
  inside canonical clones + omni_worktrees/ siblings), inflating findings ~2x and manufacturing a phantom
  CRITICAL (topic_name_mismatch routing.feedback) that existed only in a stale clone. Prune worktree path
  segments so only canonical clones are scanned. Sibling-class fix to OMN-13521 (doc_freshness_sweep).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: All tests pass
    command: uv run pytest tests/ -v
  - kind: ci
    description: CI passes
    command: pre-commit run --all-files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnimarket-pr
    description: >-
      node_gap_compute walk excludes omni_worktrees/ and .claude/worktrees/; unit test asserts worktree
      paths are pruned; full non-kafka suite green.
    source: linear
    linear_dod_text: >-
      node_gap_compute walk excludes worktree dirs; unit test asserts worktree paths pruned; uv run pytest
      tests/ (no -k) green; mypy --strict clean.
    checks:
      - check_type: command
        check_value: uv run pytest tests/test_gap_compute_worktree_exclusion.py -q
  - id: dod-occ-pr-self
    description: OCC PR carries the OMN-13638 contract + DoD receipts; self-binding receipt binds to this
      OCC PR's own number.
    source: generated
    checks:
      - check_type: command
        check_value: uv run validate-yaml contracts/OMN-13638.yaml
  - id: dod-deploy-assessment
    description: Deploy assessment — no live-lane mutation; the gap sweep is deterministic compute, no
      new node/envelope/contract-API change.
    source: generated
    checks:
      - check_type: command
        check_value: uv run python -m omnimarket.nodes.node_runtime_sweep --import-check
