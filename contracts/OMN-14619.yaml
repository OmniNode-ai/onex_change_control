---
schema_version: "1.0.0"
ticket_id: "OMN-14619"
title: "RSD-2 read-EFFECT for deterministic OCC companion production"
summary: >-
  Central OCC evidence for omnimarket PR #1761. The product PR adds the read-only node_occ_state_effect
  surface, extends node_occ_companion_compute with an optional downstream content-read check, and records
  live RED-control proof against real GitHub/OCC PRs while deliberately excluding write-side clone/push/PR-open
  effects.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1761-product-head"
    description: >-
      Product-observing verification for omnimarket PR #1761 at head cc8aa7034d92f050b67d3d0ba0ca7856de39780b.
      The proof reads PR metadata and changed files from GitHub at that exact head, verifies the PR targets
      dev, and checks the load-bearing RSD-2 surfaces: node_occ_state_effect contract/handler/request
      model, the optional downstream_check_value seam in node_occ_companion_compute, and focused regression
      tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1761; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=cc8aa7034d92f050b67d3d0ba0ca7856de39780b;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,baseRefName,files,title
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
          > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fxq dev "$d/pr.txt" && grep -Fq
          'feat(OMN-14619): RSD-2 read-EFFECT for node_occ_companion_compute' "$d/pr.txt" && for p in
          src/omnimarket/nodes/node_occ_state_effect/contract.yaml src/omnimarket/nodes/node_occ_state_effect/handlers/handler_occ_state_effect.py
          src/omnimarket/nodes/node_occ_state_effect/models/model_occ_state_request.py src/omnimarket/nodes/node_occ_companion_compute/models/model_occ_companion_request.py
          src/omnimarket/nodes/node_occ_companion_compute/handlers/handler_occ_companion_compute.py tests/unit/nodes/node_occ_state_effect/test_symbol_derivation.py
          tests/nodes/node_occ_state_effect/test_golden_chain_occ_state_effect.py tests/unit/nodes/node_occ_companion_compute/test_occ_companion_compute_seam.py;
          do grep -Fq "$p" "$d/pr.txt" && mkdir -p "$d/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "$d/$p"; done && grep -Fq 'node_occ_state_effect' "$d/src/omnimarket/nodes/node_occ_state_effect/contract.yaml"
          && grep -Fq 'class ModelOccStateRequest' "$d/src/omnimarket/nodes/node_occ_state_effect/models/model_occ_state_request.py"
          && grep -Fq 'downstream_check_value' "$d/src/omnimarket/nodes/node_occ_companion_compute/models/model_occ_companion_request.py"
          && grep -Fq 'select_asserted_check' "$d/src/omnimarket/nodes/node_occ_state_effect/handlers/handler_occ_state_effect.py"
          && grep -Fq 'build_content_read_check' "$d/src/omnimarket/nodes/node_occ_state_effect/handlers/handler_occ_state_effect.py"
          && grep -Fq 'TestDownstreamCheckOverride' "$d/tests/unit/nodes/node_occ_companion_compute/test_occ_companion_compute_seam.py"
          && grep -Fq 'build_content_read_check(' "$d/tests/unit/nodes/node_occ_state_effect/test_symbol_derivation.py"
  - id: "dod-omnimarket-pr-1761-local-verification"
    description: >-
      Verification evidence recorded in the omnimarket PR #1761 body at head cc8aa7034d92f050b67d3d0ba0ca7856de39780b.
      The proof binds the live PR head and checks that the body records the live canary RED-control proof,
      no-op/idempotency proof against already-bound OCC references, focused tests, mypy/ruff, and pre-commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1761; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=cc8aa7034d92f050b67d3d0ba0ca7856de39780b;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,body --jq '.headRefOid
          == "'$PRODUCT_HEAD'" and (.body | contains("exit 0 (1 match) at head SHA")) and (.body | contains("exit
          1 (0 matches, RED) at base SHA")) and (.body | contains("OCC#4135")) and (.body | contains("OCC#4136"))
          and (.body | contains("proof_class: receipt-bound")) and (.body | contains("tests/unit/nodes/node_occ_state_effect/test_symbol_derivation.py"))
          and (.body | contains("tests/nodes/node_occ_state_effect/test_golden_chain_occ_state_effect.py"))
          and (.body | contains("TestDownstreamCheckOverride")) and (.body | contains("uv run mypy --strict"))
          and (.body | contains("uv run pre-commit run"))' | grep -qx true
  - id: "dod-omnimarket-pr-1761-runtime-scope"
    description: >-
      Runtime-surface assessment for omnimarket PR #1761. The product diff adds a read-only OCC state-effect
      node, contract metadata, GitHub API helpers, and unit/golden-chain tests. It deliberately excludes
      write-side clone/push/PR-open effects, Kafka wiring, live deploys, restarts, docker exec, rpk topic
      produce, database mutation, and .201 mutation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1761; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=cc8aa7034d92f050b67d3d0ba0ca7856de39780b;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,files,body
          --jq '.headRefOid + "\n---FILES---\n" + ((.files | map(.path)) | join("\n")) + "\n---BODY---\n"
          + .body' > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fq 'src/omnimarket/github_api.py'
          "$d/pr.txt" && grep -Fq 'src/omnimarket/nodes/node_occ_state_effect/contract.yaml' "$d/pr.txt"
          && grep -Fq 'src/omnimarket/nodes/node_occ_state_effect/handlers/handler_occ_state_effect.py'
          "$d/pr.txt" && grep -Fq 'RSD-3 (write-EFFECT: clone/push/PR-open + trigger/orchestrator)' "$d/pr.txt"
          && grep -Fq 'Not wired to Kafka' "$d/pr.txt" && ! grep -Eiq '(docker exec|rpk topic produce|database
          mutation|live deploy|restart .201|restart.*omninode-pc)' "$d/pr.txt"
  - id: "dod-occ-self-bind-pr-4194"
    description: >-
      OCC companion self-binding for onex_change_control PR #4194. Verifies the PR is on the expected
      OMN-14619 evidence branch and contains the initial evidence commit 1113fca2a9e2d7b2761b3a3315e1c6c9bfe17a13.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          OCC_PR_NUMBER=4194; OCC_REPO=OmniNode-ai/onex_change_control; OCC_BRANCH=jonah/omn-14619-rsd2-occ-evidence;
          INITIAL_EVIDENCE_COMMIT=1113fca2a9e2d7b2761b3a3315e1c6c9bfe17a13; gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}"
          --jq '.head.ref == "'$OCC_BRANCH'" and ((.state == "open") or (.state == "closed" and .merged_at
          != null))' | grep -qx true && gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}/commits" --paginate
          --jq '.[].sha' | grep -Fx "$INITIAL_EVIDENCE_COMMIT"
  - id: "dod-omnimarket-pr-1761-product-head-0d97"
    evidence_artifact: "supersedes_dod_evidence:dod-omnimarket-pr-1761-product-head"
    description: >-
      Current-head product verification for omnimarket PR #1761 after merging current dev moved the head
      to 0d97cbf1831a7961746a9621df534c30247190de. The proof verifies the live PR head, dev base, and
      the same RSD-2 implementation/test surfaces as the original evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1761; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=0d97cbf1831a7961746a9621df534c30247190de;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,baseRefName,files
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + ((.files | map(.path)) | join("\n"))' > "$d/pr.txt"
          && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fxq dev "$d/pr.txt" && for p in src/omnimarket/nodes/node_occ_state_effect/contract.yaml
          src/omnimarket/nodes/node_occ_state_effect/handlers/handler_occ_state_effect.py src/omnimarket/nodes/node_occ_companion_compute/models/model_occ_companion_request.py
          tests/unit/nodes/node_occ_state_effect/test_symbol_derivation.py tests/unit/nodes/node_occ_companion_compute/test_occ_companion_compute_seam.py;
          do grep -Fq "$p" "$d/pr.txt" && mkdir -p "$d/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "$d/$p"; done && grep -Fq 'downstream_check_value' "$d/src/omnimarket/nodes/node_occ_companion_compute/models/model_occ_companion_request.py"
          && grep -Fq 'select_asserted_check' "$d/src/omnimarket/nodes/node_occ_state_effect/handlers/handler_occ_state_effect.py"
          && grep -Fq 'build_content_read_check(' "$d/tests/unit/nodes/node_occ_state_effect/test_symbol_derivation.py"
  - id: "dod-omnimarket-pr-1761-local-verification-0d97"
    evidence_artifact: "supersedes_dod_evidence:dod-omnimarket-pr-1761-local-verification"
    description: >-
      Current-head PR-body verification for omnimarket PR #1761 after the dev merge moved the head to
      0d97cbf1831a7961746a9621df534c30247190de. The proof verifies the body still records the live RED-control
      canary proof, focused tests, strict mypy, ruff, and pre-commit evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1761; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=0d97cbf1831a7961746a9621df534c30247190de;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,body --jq '.headRefOid
          == "'$PRODUCT_HEAD'" and (.body | contains("exit 0 (1 match) at head SHA")) and (.body | contains("exit
          1 (0 matches, RED) at base SHA")) and (.body | contains("proof_class: receipt-bound")) and (.body
          | contains("tests/unit/nodes/node_occ_state_effect/test_symbol_derivation.py")) and (.body |
          contains("TestDownstreamCheckOverride")) and (.body | contains("uv run mypy --strict")) and
          (.body | contains("uv run pre-commit run"))' | grep -qx true
  - id: "dod-omnimarket-pr-1761-runtime-scope-0d97"
    evidence_artifact: "supersedes_dod_evidence:dod-omnimarket-pr-1761-runtime-scope"
    description: >-
      Current-head runtime-scope audit for omnimarket PR #1761 at 0d97cbf1831a7961746a9621df534c30247190de.
      The proof verifies the same read-only RSD-2 product surface and confirms the PR body still excludes
      live deploy/restart/database/.201 mutation work.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1761; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=0d97cbf1831a7961746a9621df534c30247190de;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,files,body
          --jq '.headRefOid + "\n---FILES---\n" + ((.files | map(.path)) | join("\n")) + "\n---BODY---\n"
          + .body' > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fq 'src/omnimarket/nodes/node_occ_state_effect/contract.yaml'
          "$d/pr.txt" && grep -Fq 'Not wired to Kafka' "$d/pr.txt" && ! grep -Eiq '(docker exec|rpk topic
          produce|database mutation|live deploy|restart .201|restart.*omninode-pc)' "$d/pr.txt"
  - id: "dod-occ-self-bind-pr-4195"
    description: >-
      OCC companion self-binding for refreshed onex_change_control PR #4195. Verifies the PR is on the
      expected OMN-14619 refreshed evidence branch and contains the initial evidence commit e0d35c4da0dad82fec3c8a4cad1c701af903c93f.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          OCC_PR_NUMBER=4195; OCC_REPO=OmniNode-ai/onex_change_control; OCC_BRANCH=jonah/omn-14619-rsd2-head-0d97;
          INITIAL_EVIDENCE_COMMIT=e0d35c4da0dad82fec3c8a4cad1c701af903c93f; gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}"
          --jq '.head.ref == "'$OCC_BRANCH'" and ((.state == "open") or (.state == "closed" and .merged_at
          != null))' | grep -qx true && gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}/commits" --paginate
          --jq '.[].sha' | grep -Fx "$INITIAL_EVIDENCE_COMMIT"
  - id: "dod-omnimarket-pr-1761-product-head-d119"
    evidence_artifact: "supersedes_dod_evidence:dod-omnimarket-pr-1761-product-head-0d97"
    description: >-
      Current-head product verification for omnimarket PR #1761 after the canonical handler-shape repair
      moved the head to d119c05c6259d5f4748e1517d7360b44d99eac24. The proof verifies the live PR head,
      dev base, shared OCC companion event model, compute compatibility shim, canonical read-EFFECT handle(request)
      signature, and state-effect runtime topics.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1761; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=d119c05c6259d5f4748e1517d7360b44d99eac24;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,baseRefName,files
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + ((.files | map(.path)) | join("\n"))' > "$d/pr.txt"
          && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fxq dev "$d/pr.txt" && for p in src/omnimarket/events/occ_companion.py
          src/omnimarket/nodes/node_occ_companion_compute/models/model_occ_companion_request.py src/omnimarket/nodes/node_occ_state_effect/contract.yaml
          src/omnimarket/nodes/node_occ_state_effect/handlers/handler_occ_state_effect.py tests/nodes/node_occ_state_effect/test_golden_chain_occ_state_effect.py
          tests/test_market_node_runtime_dogfood.py; do grep -Fq "$p" "$d/pr.txt" && mkdir -p "$d/$(dirname
          "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}" --jq .content | base64
          --decode > "$d/$p"; done && grep -Fq 'class ModelOccCompanionRequest' "$d/src/omnimarket/events/occ_companion.py"
          && grep -Fq 'from omnimarket.events.occ_companion import' "$d/src/omnimarket/nodes/node_occ_companion_compute/models/model_occ_companion_request.py"
          && grep -Fq 'async def handle(self, request: ModelOccStateRequest)' "$d/src/omnimarket/nodes/node_occ_state_effect/handlers/handler_occ_state_effect.py"
          && grep -Fq 'onex.cmd.omnimarket.occ-state-effect-requested.v1' "$d/src/omnimarket/nodes/node_occ_state_effect/contract.yaml"
          && grep -Fq 'onex.evt.omnimarket.occ-state-effect-completed.v1' "$d/tests/nodes/node_occ_state_effect/test_golden_chain_occ_state_effect.py"
  - id: "dod-omnimarket-pr-1761-local-verification-d119"
    evidence_artifact: "supersedes_dod_evidence:dod-omnimarket-pr-1761-local-verification-0d97"
    description: >-
      Current-head local verification for omnimarket PR #1761 at d119c05c6259d5f4748e1517d7360b44d99eac24.
      The proof binds the live PR head and verifies that the product branch carries tests for the canonical
      state-effect topics plus the dogfood inventory ratchet for the new addressable node.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1761; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=d119c05c6259d5f4748e1517d7360b44d99eac24;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,files
          --jq '.headRefOid + "\n" + ((.files | map(.path)) | join("\n"))' > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD"
          "$d/pr.txt" && for p in tests/nodes/node_occ_state_effect/test_golden_chain_occ_state_effect.py
          tests/test_market_node_runtime_dogfood.py; do grep -Fq "$p" "$d/pr.txt" && mkdir -p "$d/$(dirname
          "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}" --jq .content | base64
          --decode > "$d/$p"; done && grep -Fq 'test_contract_declares_runtime_dispatch_topics' "$d/tests/nodes/node_occ_state_effect/test_golden_chain_occ_state_effect.py"
          && grep -Fq 'assert summary["node_dirs"] == 367' "$d/tests/test_market_node_runtime_dogfood.py"
          && grep -Fq 'assert summary["entry_points"] == 364' "$d/tests/test_market_node_runtime_dogfood.py"
  - id: "dod-omnimarket-pr-1761-runtime-scope-d119"
    evidence_artifact: "supersedes_dod_evidence:dod-omnimarket-pr-1761-runtime-scope-0d97"
    description: >-
      Current-head runtime-scope audit for omnimarket PR #1761 at d119c05c6259d5f4748e1517d7360b44d99eac24.
      The proof verifies the read-only state-effect surface and confirms the PR body still excludes live
      deploy/restart/database/.201 mutation work.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1761; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=d119c05c6259d5f4748e1517d7360b44d99eac24;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,files,body
          --jq '.headRefOid + "\n---FILES---\n" + ((.files | map(.path)) | join("\n")) + "\n---BODY---\n"
          + .body' > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fq 'src/omnimarket/nodes/node_occ_state_effect/contract.yaml'
          "$d/pr.txt" && grep -Fq 'src/omnimarket/events/occ_companion.py' "$d/pr.txt" && grep -Fq 'read-only'
          "$d/pr.txt" && ! grep -Eiq '(docker exec|rpk topic produce|database mutation|live deploy|restart
          .201|restart.*omninode-pc)' "$d/pr.txt"
