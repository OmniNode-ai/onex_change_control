---
schema_version: "1.0.0"
ticket_id: "OMN-17863"
title: "Autobind OCC evidence for OMN-17863"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omniweb PR #353.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #353 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #353 on OmniNode-ai/omniweb changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (.github/workflows/ci.yml, .github/workflows/security-audit.yml, package.json, scripts/ci/pnpm-audit-verdict.ts) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniweb-pr-353"
    description: "PR #353 on OmniNode-ai/omniweb — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 353 --repo OmniNode-ai/omniweb --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniweb/pulls/353/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/ci.yml'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omniweb-pr-353"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-omn17863-audit-verdict-behavior-proof"
    description: |-
      Hand-authored behavior proof (the producer mints none for a TypeScript surface).
      Executes the transport-vs-finding classifier this ticket claims: scripts/ci/pnpm-audit-verdict.ts
      must report REGISTRY_UNREACHABLE as its own verdict and still fail closed. The test pins that
      against real captured output from four live runs and asserts the invariant directly - for every
      exit code and every output in the corpus, a non-zero child exit produces a non-zero gate exit.
      Same target ci.yml Gate 8 runs. The check installs its own deps because the sweep's
      hermetic environment carries pnpm but not omniweb's node_modules; measured cold against
      a fresh store, install + test is 34s (33s + 1s) against the 180s per-check ceiling.
    source: "manual"
    checks:
      - check_type: "test_passes"
        check_value: "pnpm install --frozen-lockfile && pnpm test:audit-verdict"
        cwd: "${OMNI_HOME}/omniweb"
  - id: "occ-self-bind-pr-8194"
    description: "Binds OMN-17863 to OCC companion PR #8194 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8194 --repo OmniNode-ai/onex_change_control --json number,state"
