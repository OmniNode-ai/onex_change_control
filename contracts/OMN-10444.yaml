---
schema_version: "1.0.0"
ticket_id: OMN-10444
title: "Align runtime-backed skill shims with run-node input envelopes"
summary: "Updates omniclaude runtime-backed shim docs to use the actual `onex run-node --input` CLI surface,
  fixes a nonexistent `onex run` reference, and adds an audit test that prevents the invalid `run-node
  <node> -- <flags>` form from returning."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Focused shim audit and monorepo-reference tests pass"
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-10444/omniclaude &&
      uv run pytest tests/audit/test_r_class_shim_enforcement.py tests/skills/test_no_monorepo_refs_in_plugin_skills.py
      -q"
  - kind: manual
    description: "Touched audit test passes ruff format/check"
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-10444/omniclaude &&
      uv run ruff format tests/audit/test_r_class_shim_enforcement.py && uv run ruff check tests/audit/test_r_class_shim_enforcement.py"
  - kind: manual
    description: "Skill aspiration validation passes with OMNI_HOME set"
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-10444/omniclaude &&
      OMNI_HOME=${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home} python scripts/validation/validate_skill_aspiration.py"
  - kind: manual
    description: "Dogfood confirms actual ONEX CLI requires run-node --input and has no onex run command"
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-10433/omnimarket &&
      uv run onex run-node --help && ! uv run onex run --help"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused shim audit and monorepo-reference tests pass"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10444/dod-001/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10444/dod-001/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10444/dod-001/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-002
    description: "Touched audit test passes ruff format/check"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10444/dod-002/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10444/dod-002/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10444/dod-002/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-003
    description: "Skill aspiration validation passes with OMNI_HOME set"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10444/dod-003/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10444/dod-003/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10444/dod-003/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-004
    description: "Dogfood confirms actual ONEX CLI requires run-node --input and has no onex run command"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10444/dod-004/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10444/dod-004/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10444/dod-004/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
