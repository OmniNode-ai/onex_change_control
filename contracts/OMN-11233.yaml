---
schema_version: "1.0.0"
ticket_id: OMN-11233
title: "Wire session phase enforcement UserPromptSubmit hook"
summary: "Adds the omniclaude UserPromptSubmit hook that reads session phase state and emits deterministic
  enforcement directives."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "events"
evidence_requirements:
  - kind: "manual"
    description: "omniclaude PR 1606 carries the session phase enforcement hook implementation and local
      test evidence"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid"
  - kind: "manual"
    description: "OCC PR 1205 carries the OMN-11233 central contract and receipt evidence"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1606"
    description: "omniclaude PR 1606 is open at the recorded implementation head and carries local unit/lint/typecheck
      evidence in the PR body."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid"
  - id: "dod-occ-pr-1205"
    description: "OCC PR 1205 carries the OMN-11233 central contract and receipt evidence for omniclaude
      PR 1606."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid"
