---
schema_version: "1.0.0"
ticket_id: "OMN-10733"
title: "Fix silent localhost env fallbacks in omnibase_core (OMN-10658 sweep)"
summary: >
  Binds omnibase_core PR #1054, which replaces all 5 silent os.environ.get(KEY, "localhost...") calls
  with fail-fast os.environ[KEY] in doctor checks and CLI modules, and wires validate_no_env_fallbacks.py
  as pre-commit hook and CI gate, to central OCC evidence.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1054-head"
    description: "omnibase_core PR #1054 carries the fallback sweep fix at the recorded implementation
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10733/dod-pr-1054-head/command.yaml\""
  - id: "dod-001"
    description: "validate_no_env_fallbacks.py CI gate passes on omnibase_core PR #1054."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10733/dod-001/command.yaml\""
  - id: "dod-deploy"
    description: "deploy-gate passes on omnibase_core PR #1054 — pure CLI/doctor hardening, no service
      deploy."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10733/dod-deploy/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10733/dod-occ-pr/command.yaml\""
