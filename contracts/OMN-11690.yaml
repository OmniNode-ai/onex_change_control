---
schema_version: "1.0.0"
ticket_id: "OMN-11690"
title: "fix: replace asciinema placeholder URL and update model registry to gemini-2.5-flash"
summary: >
  Fixes two demo-blocking issues in onex-self-extending-agent: (1) README asciinema embed used placeholder
  ticket ID omn-11523 (not a real cast ID); replaced with durable local .cast file playback instructions.
  (2) model_registry.yaml, cost_pricing.yaml, and agent.py still referenced gemini-2.0-flash; updated
  to gemini-2.5-flash across all contract surfaces. Covers OMN-11690 (asciinema) and OMN-11691 (model
  references).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests-pass"
    description: "883 unit tests pass with no -k filter after model ID update"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11690/dod-unit-tests-pass/command.yaml\""
  - id: "dod-sea-pr-127"
    description: "onex-self-extending-agent PR #127 implements all fixes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11690/dod-sea-pr-127/command.yaml\"
          && grep -q '^pr_number: 127$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11690/dod-sea-pr-127/command.yaml\""
