---
schema_version: "1.0.0"
ticket_id: "OMN-18364"
title: "Autobind OCC evidence for OMN-18364"
summary: >
  OCC contract evidence for OMN-18364 companions on OmniNode-ai/omnibase_infra PR #3528 and OmniNode-ai/omninode_infra
  PR #1461.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3528 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3528 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/ci/test_incident_replay_omn18364.py tests/ci/test_workflow_uv_available_omn18364.py -q
  - kind: "ci"
    description: "PR #1461 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "ci"
    description: |-
      Behavior proof for PR #1461 on OmniNode-ai/omninode_infra, derived from the PR's own diff and replayed through GitHub's external CI job readback. Bound as dod_evidence item `dod-node1461-behavior-proof`.
    command: |-
      gh api repos/OmniNode-ai/omninode_infra/actions/runs/34854981921/attempts/1/jobs --paginate --jq '.jobs[] | select(.name=="Tests Gate") | .conclusion' | grep -Fx success
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3528"
    description: "PR #3528 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/ci/check_workflow_uv_available.py?ref=1c741c67ef1d993d5eb146897330ec93178dea5a --jq '.content' | base64 -d | grep -c 'class Violation'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3528/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/ci.yml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3528 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/ci/test_incident_replay_omn18364.py tests/ci/test_workflow_uv_available_omn18364.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-9505"
    description: "Binds OMN-18364 to OCC companion PR #9505 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9505 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-3528-ci-replay"
    description: "PR #3528 on OmniNode-ai/omnibase_infra — merged CI replay proof for the prior behavior
      item."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-diff-derived-behavior-proof"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 3528 --repo OmniNode-ai/omnibase_infra --json state,headRefOid --jq '.state == "MERGED" and .headRefOid == "a90c341fce9480e5db85190f6c3e993e923a3058"' | grep -Fx true
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/actions/runs/34857750120/attempts/1/jobs --paginate --jq '.jobs[] | select(.name=="CI Summary") | .conclusion' | grep -Fx success
  - id: "dod-OmniNode-ai-omninode_infra-pr-1461"
    description: "PR #1461 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1461 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1461-ci"
    description: "PR #1461 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1461 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-node1461-behavior-proof"
    description: "PR #1461 on OmniNode-ai/omninode_infra — CI replay behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1461"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 1461 --repo OmniNode-ai/omninode_infra --json state,headRefOid --jq '.state == "OPEN" and .headRefOid == "a7b6e39d5a7354c29f40ca09dfaa0b0b0694c353"' | grep -Fx true
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/actions/runs/34854981921/attempts/1/jobs --paginate --jq '.jobs[] | select(.name=="Tests Gate") | .conclusion' | grep -Fx success
  - id: "occ-self-bind-pr-9528"
    description: "OCC companion PR #9528 — self-bind for OMN-18364 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9528 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-node1461-behavior-proof-5d08e045"
    description: >-
      PR #1461 on OmniNode-ai/omninode_infra — CI replay behavior proof re-bound after the product head
      moved from a7b6e39d to 5d08e045. The move was a deliberate update-branch: #1461 had to take omninode_infra
      dev at 25e56723 (the OMN-18205 routing merge) because every one of its own GitHub-hosted jobs was
      dying in about three seconds on the org-wide hosted-runner refusal, and that merge is what re-homed
      them onto the self-hosted fleet. The autobind publisher that would normally re-mint this item for
      the new head did run (job 104134065259, runner omninode-runner-77) and failed closed: the OCC command
      bus it publishes to is reachable only on the tailnet, and the cloud fleet is not on it. This entry
      re-binds the same two observations to the moved head without rewriting the append-stable receipts
      already merged for a7b6e39d.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-node1461-behavior-proof"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 1461 --repo OmniNode-ai/omninode_infra --json state,headRefOid --jq '.state == "OPEN" and .headRefOid == "5d08e045c2e22a10e54c7aeb55e0fcdbe790e620"' | grep -Fx true
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/actions/runs/34891204191/attempts/1/jobs --paginate --jq '.jobs[] | select(.name=="Tests Gate") | .conclusion' | grep -Fx success
