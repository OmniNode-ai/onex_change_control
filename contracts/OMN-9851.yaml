---
schema_version: "1.0.0"
ticket_id: OMN-9851
summary: "Pull integration tests into a separate always-run CI job (4-split matrix); tests-gate now depends
  on both test-parallel and tests-integration; unit job ignores tests/integration"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #937 CI checks pass"
    command: "gh pr checks 937 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "ci.yml is valid YAML (yaml.safe_load succeeds)"
    source: generated
    checks:
      - check_type: command
        check_value: "uv run python -c \"import yaml; yaml.safe_load(open('.github/workflows/ci.yml').read())\""
  - id: dod-002
    description: "tests-integration job exists in ci.yml with 4-split matrix"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -n 'tests-integration' .github/workflows/ci.yml"
  - id: dod-003
    description: "Tests Gate job present and depends on both test-parallel and tests-integration"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -n 'Tests Gate' .github/workflows/ci.yml"
  - id: dod-004
    description: "Unit test-parallel job excludes tests/integration via --ignore flag"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -n -- '--ignore=tests/integration' .github/workflows/ci.yml"
  - id: dod-005
    description: "pre-commit run --all-files passes on omnibase_core worktree"
    source: generated
    checks:
      - check_type: test_passes
        check_value: "pre-commit run --all-files"
