---
schema_version: "1.0.0"
ticket_id: OMN-8762
title: "Create missing node - node_pr_watch_orchestrator"
summary: "Record central OCC evidence that OmniMarket PR #525 declares the PR watch orchestrator stub routing and discovery surface."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Shared stub-node golden-chain tests pass for OmniMarket PR #525."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8762/dod-stub-tests/command.yaml"
  - kind: "ci"
    description: "Touched PR watch stub contract and tests pass ruff for OmniMarket PR #525."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8762/dod-lint/command.yaml"
  - kind: "manual"
    description: "Current ONEX discovery output includes node_pr_watch_orchestrator for OmniMarket PR #525."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8762/dod-node-discovery/command.yaml"
  - kind: "ci"
    description: "OCC evidence contract validates and local receipt gate passes for OCC PR #715."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8762/dod-occ-validation/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-stub-tests"
    description: "Shared stub-node golden-chain tests pass for OmniMarket PR #525."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8762/dod-stub-tests/command.yaml && grep -q '^pr_number: 525$' drift/dod_receipts/OMN-8762/dod-stub-tests/command.yaml"
  - id: "dod-lint"
    description: "Touched PR watch stub contract and tests pass ruff for OmniMarket PR #525."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8762/dod-lint/command.yaml && grep -q '^pr_number: 525$' drift/dod_receipts/OMN-8762/dod-lint/command.yaml"
  - id: "dod-node-discovery"
    description: "Current ONEX discovery output includes node_pr_watch_orchestrator for OmniMarket PR #525."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8762/dod-node-discovery/command.yaml && grep -q '^pr_number: 525$' drift/dod_receipts/OMN-8762/dod-node-discovery/command.yaml"
  - id: "dod-occ-validation"
    description: "OCC evidence contract validates and local receipt gate passes for OCC PR #715."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8762/dod-occ-validation/command.yaml && grep -q '^pr_number: 715$' drift/dod_receipts/OMN-8762/dod-occ-validation/command.yaml"
