---
schema_version: "1.0.0"
ticket_id: "OMN-14221"
title: "refactor(OMN-14221): retire constants→models import-layering back-edge in omnibase_core"
summary: >-
  omnibase_core PR #1410 retires the frozen `constants.** -> models.**` import-layering back-edge by changing
  the two boundary-validation raises in constants/constants_handler_capabilities.py's get_capabilities_by_node_kind
  from ModelOnexError to the built-in ValueError (matching the function's own docstring Raises: ValueError
  and CLAUDE.md error-handling doctrine), folding error_code/context into the message text with the sanctioned
  # error-ok: boundary marker, dropping the now-unused ModelOnexError import (the sole constants->models
  edge), and retiring the .importlinter ignore line. Verified by the import-linter oracle: the core-foundation-no-upward
  contract stays KEPT with the allowlist entry removed, proving the edge is genuinely gone.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1410"
    description: >-
      Independent verification (a separate verifier, NOT the implementing agent — the refactor was authored
      by delegation and the implementer stood down) reproduced the import-linter oracle end-to-end against
      omnibase_core PR #1410 head beb1a342 in an isolated worktree with PYTHONPATH cleared (grimp analyzed
      the worktree src, not the canonical clone). lint-imports kept all 4 contracts (Analyzed 4078 files,
      10326 dependencies; 4 kept, 0 broken) with the constants->models ignore line removed. Negative control:
      restoring the old ModelOnexError import (ignore line still removed) turned core-foundation-no-upward
      BROKEN (3 kept, 1 broken: constants.constants_handler_capabilities -> models.errors.model_onex_error
      l.43), proving the edge is real and genuinely removed, not wildcard-masked. grep proof: no `from
      omnibase_core.models` import remains under src/omnibase_core/constants/. ValueError correctness:
      ModelOnexError derives directly from Exception (MRO Exception, BaseException, object), so issubclass(ModelOnexError,
      ValueError) is False — the flipped test assertion pytest.raises(ValueError) is a genuine flip, not
      passing by inheritance. mypy Success (1 source file); 68 tests passed (tests/unit/constants/test_handler_capabilities.py);
      ruff format + check clean; pre-commit on changed files 0 failed (import-linter Passed; ONEX Error
      Raising Validation Passed, and a marker-stripped negative control returned exit 1 with 2 violations,
      proving the # error-ok: markers are genuinely required). No runtime or deploy scope.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14221/dod-omnibase_core-pr-1410/command.yaml
          && grep -q '^pr_number: 1410$' drift/dod_receipts/OMN-14221/dod-omnibase_core-pr-1410/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the OMN-14221 contract and PASS
      receipt for omnibase_core PR #1410, binding itself to occ-preflight by commit_sha (contract commit
      C1), with no runtime or deploy scope.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14221/dod-occ-self/command.yaml
