---
schema_version: "1.0.0"
ticket_id: "OMN-14898"
title: "Writer-boundary tenant isolation guard"
summary: >-
  OCC Evidence-Source companion for omnimarket PR #1864. The product PR adds an opt-in fail-closed tenant_id
  guard for delegation projection writers so configured lanes refuse unstamped writes before PostgreSQL
  defaults can silently materialize them under the interim single-tenant default.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #1864 is open at the expected product head."
    command: "gh pr view 1864 --repo OmniNode-ai/omnimarket --json number,state,headRefOid,headRefName"
  - kind: "ci"
    description: "omnimarket PR #1864 diff is scoped to settings, projection tenant guard wiring, env
      example, and regression tests."
    command: "gh pr view 1864 --repo OmniNode-ai/omnimarket --json files"
  - kind: "tests"
    description: "The writer tenant-isolation regression test is present in the product PR diff."
    command: "gh pr diff 1864 --repo OmniNode-ai/omnimarket --name-only"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1864-head"
    description: "Binds omnimarket PR #1864 at head 4cd855600f3430d81fe9653fb31d0c457f2d349e."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1864 --repo OmniNode-ai/omnimarket --json number,state,headRefOid,headRefName"
  - id: "dod-omnimarket-pr-1864-diff-scope"
    description: >-
      PR #1864 product diff touches Settings, .env.example, the new projection tenant_isolation helper,
      delegation projection writer call sites, and tests/test_writer_tenant_isolation_omn14898.py. This
      is a code-side opt-in guard only; it does not claim live-readback ticket closeout, flip runtime
      configuration, apply DB migrations, change secrets, or deploy/restart .201.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1864 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-omnimarket-pr-1864-env-sync-repair"
    description: >-
      The product branch now includes generated .env.example documentation for ENFORCE_TENANT_ISOLATION,
      repairing the hosted env-example sync test failure observed before this OCC companion.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 1864 --repo OmniNode-ai/omnimarket -- .env.example"
  - id: "dod-omnimarket-pr-1864-local-selector"
    description: >-
      Codex repair commit 4cd85560 passed the governed impacted pre-push selector locally with 1704 passed
      and 5 skipped after regenerating .env.example; no --no-verify bypass was used.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1864 --repo OmniNode-ai/omnimarket --json headRefOid"
  - id: "occ-self-bind-pr-4643"
    description: "OCC companion PR #4643 self-bind for OMN-14898."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4643 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
