---
schema_version: "1.0.0"
ticket_id: "OMN-13834"
title: "Dispatch engine router and skill mapping registration"
summary: >
  omnimarket PR #1563 rebuilds node_skill_dispatch_engine_orchestrator as a thin
  router over RSD scoring and self-healing dispatch, replacing the placeholder
  facade with deterministic worker-spec receipts. omnibase_infra PR #2176 registers
  the dispatch_engine skill mapping so onex skill dispatch_engine resolves to the
  backing node. This central OCC contract binds both downstream PRs to durable
  receipt evidence.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "omnimarket node_skill_dispatch_engine_orchestrator contract and handler behavior"
  - "omnibase_infra skill_mapping.yaml dispatch_engine registration"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr-omninode-13834-binding"
    description: >
      This onex_change_control PR carries the OMN-13834 central contract and receipt
      evidence required by downstream omnimarket PR #1563 and omnibase_infra PR #2176.
    source: "manual"
    status: verified
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13834/dod-occ-pr-omninode-13834-binding/command.yaml"
  - id: "dod-omnimarket-dispatch-engine-router-pr-1563"
    description: >
      omnimarket PR #1563 rebuilds dispatch_engine as a real router over
      node_rsd_fill_compute and node_self_healing_dispatch_orchestrator. CI is green
      at head 829d845c4b179c3686c4d8daefb6e929fed4454b, including lint, typecheck,
      golden chain, dispatcher route coverage, contract validation, deploy gate,
      imperative contract guard, and split tests.
    source: "manual"
    status: verified
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13834/dod-omnimarket-dispatch-engine-router-pr-1563/command.yaml
          && grep -q '^pr_number: 1563$' drift/dod_receipts/OMN-13834/dod-omnimarket-dispatch-engine-router-pr-1563/command.yaml
          && grep -q '829d845c4b179c3686c4d8daefb6e929fed4454b' drift/dod_receipts/OMN-13834/dod-omnimarket-dispatch-engine-router-pr-1563/command.yaml"
  - id: "dod-omnibase-infra-dispatch-engine-skill-mapping-pr-2176"
    description: >
      omnibase_infra PR #2176 registers dispatch_engine in skill_mapping.yaml so the
      CLI skill resolves to node_skill_dispatch_engine_orchestrator. Local unit proof
      reported in the PR body passed at head e096fbb02a0f4abd1adbabf9a0480155332b9f9d.
    source: "manual"
    status: verified
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13834/dod-omnibase-infra-dispatch-engine-skill-mapping-pr-2176/command.yaml
          && grep -q '^pr_number: 2176$' drift/dod_receipts/OMN-13834/dod-omnibase-infra-dispatch-engine-skill-mapping-pr-2176/command.yaml
          && grep -q 'e096fbb02a0f4abd1adbabf9a0480155332b9f9d' drift/dod_receipts/OMN-13834/dod-omnibase-infra-dispatch-engine-skill-mapping-pr-2176/command.yaml"
