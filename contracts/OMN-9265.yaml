---
schema_version: "1.0.0"
ticket_id: "OMN-9265"
title: "Add FakePostgres + FakeValkeyClient fixtures for pure-mock runtime tests"
summary: >-
  Pure-in-memory FakePostgresAdapter and FakeValkeyClient fixture evidence is carried by omnibase_infra
  PR #1659. This OCC contract preserves the existing readiness receipt and binds the downstream fixture
  evidence plus the onex_change_control PR #1155 self-receipt.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "tests"
    description: "Wave worker local verification evidence is captured in central OCC receipt."
    command: "uv run pytest tests/ci/test_reusable_runtime_boot_schema.py -q"
  - kind: "tests"
    description: >-
      42 unit and integration tests cover FakePostgresAdapter, FakeValkeyClient, protocol compliance,
      happy paths, error injection, concurrency, and RuntimeScheduler sequence round-trip.
    command: "uv run pytest tests/unit/test_utils/test_fake_postgres_adapter.py tests/unit/test_utils/test_fake_valkey_client.py
      tests/integration/test_fake_fixtures_integration.py -v"
  - kind: "ci"
    description: "omnibase_infra PR #1659 carries downstream CI and implementation evidence."
    command: "gh pr checks 1659 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-wave-local-evidence"
    description: "Wave worker local verification or readiness evidence is captured as a canonical PASS
      receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-wave-local-evidence/command.yaml\""
  - id: "dod-001"
    description: "FakePostgresAdapter is present in omnibase_infra PR #1659 diff."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-001/command.yaml\"
          && grep -q '^pr_number: 1659$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-001/command.yaml\""
  - id: "dod-002"
    description: "FakeValkeyClient is present in omnibase_infra PR #1659 diff."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-002/command.yaml\"
          && grep -q '^pr_number: 1659$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-002/command.yaml\""
  - id: "dod-003"
    description: "FakePostgresAdapter satisfies ProtocolPostgresAdapter."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-003/command.yaml\"
          && grep -q '^pr_number: 1659$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-003/command.yaml\""
  - id: "dod-004"
    description: "42 unit and integration tests pass in the downstream evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-004/command.yaml\"
          && grep -q '^pr_number: 1659$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-004/command.yaml\""
  - id: "dod-005"
    description: "omnibase_infra PR #1659 merged into main."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-005/command.yaml\"
          && grep -q '^pr_number: 1659$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-005/command.yaml\""
  - id: "dod-occ-pr-1155"
    description: "onex_change_control PR #1155 carries the OMN-9265 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-occ-pr-1155/command.yaml\"
          && grep -q '^pr_number: 1155$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9265/dod-occ-pr-1155/command.yaml\""
