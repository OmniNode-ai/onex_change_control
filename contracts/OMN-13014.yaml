---
schema_version: "1.0.0"
ticket_id: "OMN-13014"
title: "Hot-patch ledger rebuild preflight gate"
summary: >-
  Ships the rebuild-path gate for container-layer hot patches so runtime rebuilds fail before silently
  dropping live .prepatch changes. omnibase_infra PR #1959 adds scripts/preflight_hotpatch_ledger.py,
  wires guard_hotpatch_ledger into deploy-runtime.sh, and covers ancestor checks, ledger validation, tripwire
  behavior, post-rebuild checks, and bypass validation with focused unit tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-hotpatch-pr-1959"
    description: "omnibase_infra PR #1959 carries the hot-patch ledger preflight implementation and focused
      tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13014/dod-hotpatch-pr-1959/command.yaml
          && grep -q '^pr_number: 1959$' drift/dod_receipts/OMN-13014/dod-hotpatch-pr-1959/command.yaml"
  - id: "dod-ledger-operational-state"
    description: "The PR documents the live .prepatch census, installed ledger checksum, merged source
      PRs, and deploy posture."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13014/dod-ledger-operational-state/command.yaml
          && grep -q '50b28669c6539c242c5bace9fd955d5b88d685787098654f52e485fcf142bec5' drift/dod_receipts/OMN-13014/dod-ledger-operational-state/command.yaml
          && grep -q 'deploy_pending = true' drift/dod_receipts/OMN-13014/dod-ledger-operational-state/command.yaml"
  - id: "dod-occ-pr"
    description: "The OCC evidence PR carries the central OMN-13014 contract and receipts for downstream
      Receipt Gate linkage."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13014/dod-occ-pr/command.yaml &&
          test -f contracts/OMN-13014.yaml && test -f drift/dod_receipts/OMN-13014/dod-hotpatch-pr-1959/command.yaml"
