---
schema_version: "1.0.0"
ticket_id: "OMN-15282"
title: "Autobind OCC evidence for OMN-15282"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #713.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #713 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-713"
    description: "PR #713 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15282/dod-OmniNode-ai-omninode_infra-pr-713/command.yaml"
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15282/dod-OmniNode-ai-omninode_infra-pr-713/command.yaml"
  - id: "occ-self-bind-pr-5223"
    description: "Binds OMN-15282 to OCC companion PR #5223 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omn15282-verify-node-discovery-content"
    description: >
      Independent adversarial verification (verifier != implementer): content-bound assertions against
      PR #713's ACTUAL merged diff (gh pr diff, not gh pr view existence), plus a re-executed RED-before/GREEN-after
      proof on .200 holding tests/scripts/test_node_migration_discovery.py byte-identical across bb43769
      (dev tip pre-#713) and origin/dev (post-#713). Supersedes the prior dod-OmniNode-ai-omninode_infra-pr-713
      autobind entry's bare existence check with falsifiable content assertions.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -qx 'NODE_DISCOVERY_MARKER_COUNT=7' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
          && grep -qx 'VALIDATE_NODE_MIGRATION_ID_COUNT=9' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
          && grep -qx 'RED_AT_PRE_713=4 failed, 2 passed' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
          && grep -qx 'GREEN_AT_DEV_TIP=6 passed' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
  - id: "occ-self-bind-pr-5235"
    description: "Binds OMN-15282 to the adversarial-verification OCC companion PR #5235 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5235 --repo OmniNode-ai/onex_change_control --json number,state"
