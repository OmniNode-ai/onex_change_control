---
schema_version: "1.0.0"
ticket_id: "OMN-13512"
title: "fix(OMN-13512): rewrite skill_functional_audit heuristics to the SKILL.md methodology"
summary: >-
  OCC evidence for the omnimarket handler rewrite (node_skill_functional_audit_compute) and the paired
  omniclaude SKILL.md doc fix. The omnimarket change replaces crude heuristics with the documented skill_functional_audit
  methodology: resilient declared-backing-node extraction (mirroring omnibase_core.validation.validator_skill_backing_node),
  Phase 3c pure-instruction exemption, orchestrator handler_routing support, multi-root backing-node resolution,
  and shell-node wiring recognition. Dogfooding against the live omniclaude skills dir collapses the false-positive
  gap count from 50 to 6 with zero stubs; the residual 6 are genuine FACADE candidates. The omniclaude
  change clears the stale "Known Tracked Findings" table (pipeline_fill/OMN-8700, session/OMN-8699, delegate/OMN-8701
  are all Done).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-node-tests"
    description: >-
      The node_skill_functional_audit_compute unit suite (13 tests including the new pure-instruction-exemption,
      prose-token-rejection, handler_routing, and shell-node-wiring cases) passes on the omnimarket feature
      branch.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13512/dod-node-tests/command.yaml"
  - id: "dod-dogfood-gap-collapse"
    description: >-
      Running the rewritten handler against the live omniclaude skills dir collapses the reported gaps
      from 50 to 6 (zero stubs); the residual 6 are genuine stateful-orchestration FACADE candidates.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13512/dod-dogfood-gap-collapse/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13512.yaml consumed
      by the omnimarket and omniclaude feature PR Receipt Gates via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13512\"$' contracts/OMN-13512.yaml"
