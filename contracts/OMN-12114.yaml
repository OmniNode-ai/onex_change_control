---
schema_version: "1.0.0"
ticket_id: "OMN-12114"
title: "Wire auto_fix dispatch loop and max_fix_cycles in node_ci_watch"
summary: >
  Central OCC evidence for omnimarket PR #854, which upgrades node_ci_watch from a pure CI classifier
  to an active fix loop. Adds auto_fix field (was accepted but never read), implements the classify->dispatch->repoll
  loop using HandlerDispatchWorker + HandlerDispatchWorkerExecution (same pattern as node_pr_polish),
  adds FIXED/UNFIXABLE terminal statuses, and ships 10 new unit tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-854"
    description: >
      omnimarket PR #854 wires the auto_fix field and max_fix_cycles loop in node_ci_watch. Adds ModelCiFixCycle,
      FIXED/UNFIXABLE statuses, and auto_fix_status/cycles fields to ModelCiWatchResult. 16 tests passing.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12114/dod-omnimarket-pr-854/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      No live deploy required for this change. node_ci_watch is a WorkflowPackage invoked by callers;
      the runtime will pick up the new auto_fix loop on the next invocation after the omnimarket package
      is updated.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy: no live deploy required; node_ci_watch is a workflow package consumed
          at call time'"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carrying the central OCC evidence for OMN-12114."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12114/dod-occ-pr/command.yaml"
