---
schema_version: "1.0.0"
ticket_id: "OMN-16726"
title: "Autobind OCC evidence for OMN-16726"
summary: |-
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omniclaude PR #2366.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2366 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2366 on OmniNode-ai/omniclaude, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omniclaude`.
    command: |-
      uv run pytest tests/workflows/test_private_repo_runner_placement_workflow.py tests/workflows/test_reusable_workflow_inner_checkouts.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniclaude-pr-2366"
    description: "PR #2366 on OmniNode-ai/omniclaude — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniclaude/contents/tests/workflows/test_reusable_workflow_inner_checkouts.py?ref=db5fe325379aa525ebb4e563bdfae32ee6a02b8f --jq '.content' | base64 -d | grep -c 'def test_self_checkout_moving_or_missing_refs_are_refused'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniclaude/pulls/2366/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/advisory-job-gate-reusable.yml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2366 on OmniNode-ai/omniclaude — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/workflows/test_private_repo_runner_placement_workflow.py tests/workflows/test_reusable_workflow_inner_checkouts.py -q
        cwd: "${OMNI_HOME}/omniclaude"
  - id: "occ-self-bind-pr-11445"
    description: "Binds OMN-16726 to OCC companion PR #11445 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 11445 --repo OmniNode-ai/onex_change_control --json number,state"
