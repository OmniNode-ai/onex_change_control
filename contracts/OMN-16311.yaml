---
schema_version: "1.0.0"
ticket_id: "OMN-16311"
title: "Wave 2: onex_change_control canonical-doc migration to KB + repo thin-out"
summary: >
  Migrated onex_change_control's Bucket-A candidates (docs/standards/**, docs/policy/**, docs/governance/**)
  into the public knowledge-base repo and thinned the 5 migrated source docs to pointer stubs in this
  repo. docs/standards/doctrine_clauses.yaml and docs/standards/doctrine_coverage.md were reclassified
  bucket A -> B and retained in-repo unthinned: the latter is auto-generated by scripts/generate_doctrine_coverage.py
  from the former, and this repo's own required "Doctrine Coverage Matrix" CI job dry-run-validates the
  registry against live CI-gate wiring. Two documents required correct-before-publication fixes verified
  live against this repo's main branch rather than migrated unchanged: receipt_hashing_and_supersession.md's
  append-only gate claim (was "advisory", is actually enforced via STRICT_GATE_JOBS) and typed-metadata-policy.md's
  guard-script centralization claim (was "future path", already shipped). db-boundary-policy.md's Service
  Ownership table was corrected to include omnimarket, which reads two other services' database URLs directly
  with no corresponding registry exception -- flagged, not resolved, in the migrated copy. KB-add PR (knowledge-base#38)
  merged before this thin-out PR per the two-PR migration pattern.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "docs"
    description: "knowledge-base PR #38 (KB-add) merged before this thin-out PR."
    command: "gh pr view 38 --repo OmniNode-ai/knowledge-base --json state,mergedAt"
  - kind: "docs"
    description: "All 5 migrated source docs carry the exact pointer stub string."
    command: >-
      grep -rl 'Full documentation → https://github.com/OmniNode-ai/knowledge-base' docs/standards/receipt_hashing_and_supersession.md
      docs/standards/DUAL_BINDING_CASES.md docs/policy/db-boundary-policy.md docs/policy/typed-metadata-policy.md
      docs/governance/2026-04-27-required-gates-rollout.md | wc -l | grep -qx 5
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-knowledge-base-pr-38"
    description: "knowledge-base PR #38 (onex_change_control docs migration, KB-add half of the two-PR
      pattern) merged to main before this thin-out PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 38 --repo OmniNode-ai/knowledge-base --json state,mergedAt,mergeCommit"
  - id: "dod-occ-thin-out-pointer-stubs"
    description: "All 5 migrated source docs (docs/standards/receipt_hashing_and_supersession.md, docs/standards/DUAL_BINDING_CASES.md,
      docs/policy/db-boundary-policy.md, docs/policy/typed-metadata-policy.md, docs/governance/2026-04-27-required-gates-rollout.md)
      carry the exact taxonomy pointer string in place of their original content."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -l 'Full documentation → https://github.com/OmniNode-ai/knowledge-base' docs/standards/receipt_hashing_and_supersession.md
          docs/standards/DUAL_BINDING_CASES.md docs/policy/db-boundary-policy.md docs/policy/typed-metadata-policy.md
          docs/governance/2026-04-27-required-gates-rollout.md | wc -l | tr -d ' ' | grep -qx 5
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7099"
    description: "Binds OMN-16311 to this OCC thin-out PR #7099 (self-bind)."
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7099 --repo OmniNode-ai/onex_change_control --json number,state"
