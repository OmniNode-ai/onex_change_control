---
schema_version: "1.0.0"
ticket_id: "OMN-15414"
title: "Autobind OCC evidence for OMN-15414"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #772.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #772 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-772"
    description: "PR #772 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 772 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 772 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-772"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5661"
    description: "Binds OMN-15414 to OCC companion PR #5661 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5661 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-5701"
    description: "Binds OMN-15414 to OCC companion PR #5701 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5701 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-deploy-assessment-ghapi-head-29bb6a88"
    description: >-
      Deploy-scope DoD for omnibase_infra#2549. This probe reads immutable product PR head 29bb6a88 through
      GitHub's content API and asserts the authoritative application database topology source is present.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/topology/application_database.py?ref=29bb6a8879d7dcb8f1371d8e3b5abe2817f877a7' --jq '.content' | base64 -d)" && printf '%s\n' "$body" | grep -q 'ModelApplicationDatabaseTopologyProfile'
  - id: "occ-self-bind-pr-5705"
    description: "Binds OMN-15414 to OCC deploy-scope repair PR #5705 (self-bind)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5705 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "dod-deploy-assessment-ghapi-head-867b54d0"
    description: >-
      Deploy-scope DoD for omnibase_infra#2549 rebased head 867b54d0. This probe reads the immutable runtime
      contract source through GitHub's content API and asserts typed event wiring support is present.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/kafka_contract_source.py?ref=867b54d0e0f9ec3e605e9ad63febda2da7bcd718' --jq '.content' | base64 -d)" && printf '%s\n' "$body" | grep -q 'yaml_consumed_events' && printf '%s\n' "$body" | grep -q 'yaml_published_events'
  - id: "occ-self-bind-pr-5710"
    description: "Binds OMN-15414 to OCC rebased deploy-scope repair PR #5710 (self-bind)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5710 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "dod-deploy-assessment-ghapi-head-9d437449"
    description: >-
      Deploy-scope DoD for omnibase_infra#2549 repaired head 9d437449. This probe reads the immutable
      runtime contract source through GitHub's content API and asserts typed event wiring support remains
      present.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/kafka_contract_source.py?ref=9d437449c5761e0922cb1736de824fa476e58a5e' --jq '.content' | base64 -d)" && printf '%s\n' "$body" | grep -q 'yaml_consumed_events' && printf '%s\n' "$body" | grep -q 'yaml_published_events'
  - id: "occ-self-bind-pr-5717"
    description: "Binds OMN-15414 to OCC deploy-scope repair PR #5717 (self-bind)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5717 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
