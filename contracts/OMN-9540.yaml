---
schema_version: 1.0.0
ticket_id: OMN-9540
title: Quarantine registration verification when DB schema is unavailable
summary: Registration verifier reports QUARANTINE instead of placeholder empty projection evidence when
  schema or probe introspection is unavailable.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - events
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Targeted verifier tests prove schema-introspection failures quarantine the registration
      proof path.
    command: .venv/bin/python -m pytest tests/unit/verification/test_cli.py tests/unit/verification/test_projection_state_schema_grounding.py
      tests/unit/verification/test_registration_contract_verify_unit.py tests/unit/verification/test_verify_registration_schema_grounding.py
      tests/integration/verification/test_registration_schema_grounding_integration.py -q
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-infra-pr-1663
    description: omnibase_infra PR  #1663 carries the fail-closed registration verifier regression proof.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9540/dod-infra-pr-1663/command.yaml
          && grep -q '^exit_code: 0$' drift/dod_receipts/OMN-9540/dod-infra-pr-1663/command.yaml && grep
          -q '^pr_number: 1663$' drift/dod_receipts/OMN-9540/dod-infra-pr-1663/command.yaml"
