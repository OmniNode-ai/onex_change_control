---
schema_version: "1.0.0"
ticket_id: "OMN-9529"
title: "Runtime Config Topic Var Cleanup (ONEX_OUTPUT/INPUT_TOPIC)"
summary: >
  Removes all stale comment references to banned env vars ONEX_OUTPUT_TOPIC and ONEX_INPUT_TOPIC from
  compose files, catalog service YAMLs, Dockerfile.runtime, and .secretresolver_allowlist. The vars were
  removed in OMN-8784; only cleanup of residual comment noise remains.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-stale-refs-removed"
    description: "All stale ONEX_OUTPUT/INPUT_TOPIC comment blocks removed from compose/catalog/Dockerfile
      files"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "! grep -rn 'OMN-8840.*ONEX_OUTPUT_TOPIC\\|OMN-8840.*ONEX_INPUT_TOPIC' docker/ .secretresolver_allowlist"
  - id: "dod-tests-pass"
    description: "Stale topic env var enforcement tests continue to pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/runtime/test_stale_topic_env_var_docs_removal.py tests/unit/runtime/test_kernel_topic_env_var_removal.py
          -v"
  - id: "dod-deploy"
    description: >
      Comment-only change — no env vars added, removed, or modified. No runtime restart required. Compose
      files and Dockerfile are functionally identical; only dead comment lines removed. Verified: deploy
      omnibase_infra PR#1575 comment-only; no docker exec or rpk topic change needed.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "deploy: comment-only cleanup, no runtime behavior change; docker compose files validated
          by Compose Required-Env Coverage CI gate"
