---
schema_version: "1.0.0"
ticket_id: "OMN-9529"
title: "Runtime Config Topic Var Cleanup (ONEX_OUTPUT/INPUT_TOPIC)"
summary: >
  Removes all stale comment references to banned env vars ONEX_OUTPUT_TOPIC and ONEX_INPUT_TOPIC from
  compose files, catalog service YAMLs, Dockerfile.runtime, and .secretresolver_allowlist. The vars were
  removed in OMN-8784; only cleanup of residual comment noise remains.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-stale-refs-removed"
    description: "All stale ONEX_OUTPUT/INPUT_TOPIC comment blocks removed from compose/catalog/Dockerfile
      files"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9529/dod-stale-refs-removed/command.yaml\""
  - id: "dod-tests-pass"
    description: "Stale topic env var enforcement tests continue to pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9529/dod-tests-pass/command.yaml\""
  - id: "dod-deploy"
    description: >
      Comment-only change — no env vars added, removed, or modified. No runtime restart required. Compose
      files and Dockerfile are functionally identical; only dead comment lines removed.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9529/dod-deploy/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to onex_change_control PR #967."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9529/dod-occ-pr/command.yaml\""
