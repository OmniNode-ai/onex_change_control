---
schema_version: "1.0.0"
ticket_id: "OMN-12714"
title: "Surface ADK runner background-thread failures in SEA evidence"
summary: >-
  Central evidence for onex-self-extending-agent PR #212. SEA ADK runs now capture uncaught ADK runner
  worker-thread exceptions during the bounded runner call and fail the run before terminal PASS evidence
  can be emitted. The guard covers both the CLI `--agent` path and the bus-wired `node_agent_orchestrator`
  handler.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-thread-tests"
    description: "Focused SEA CLI and bus-node ADK background-thread failure tests passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12714/dod-focused-thread-tests/command.yaml"
          && grep -q '2 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12714/dod-focused-thread-tests/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12714/dod-focused-thread-tests/command.yaml"
  - id: "dod-sea-pr-212"
    description: "onex-self-extending-agent PR #212 carries the ADK runner thread-failure guard against
      dev."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12714/dod-sea-pr-212/command.yaml"
          && grep -q '^pr_number: 212$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12714/dod-sea-pr-212/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR #2198 carries the OMN-12714 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12714/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2198$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12714/dod-occ-pr/command.yaml"
