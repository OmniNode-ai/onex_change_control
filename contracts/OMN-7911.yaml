---
schema_version: "1.0.0"
ticket_id: OMN-7911
title: 'test(coverage): add tests for adapter_debug_store (omniintelligence)'
summary: "test(coverage): add tests for adapter_debug_store (omniintelligence)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}"
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json mergedAt -q .mergedAt --repo ${REPO}"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --watch --repo ${REPO}"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-005"
    description: "Test coverage meets minimum threshold"
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest --cov --cov-fail-under=80"
  - id: "product-pr-787"
    description: "Product PR #787 on OmniNode-ai/omniintelligence adds focused unit coverage for adapter_debug_store."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=787; REPO=OmniNode-ai/omniintelligence; gh pr diff ${PR_NUMBER} --repo
          ${REPO} --name-only | grep -q 'tests/unit/debug_intel/test_adapter_debug_store.py'"
  - id: "occ-self-bind-pr-4317"
    description: "OCC batch PR #4317 binds OMN-7911 evidence for OmniNode-ai/omniintelligence#787."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4317; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
