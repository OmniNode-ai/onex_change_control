---
schema_version: 1.0.0
ticket_id: OMN-14794
title: Route ModelRoutingDecision to routing-decision.v1 through any applier
summary: Central OCC evidence for omnibase_infra#2344. The product PR maps ModelRoutingDecision to the
  canonical delegation routing-decision topic through the generic dispatch-result applier, with regression
  coverage proving the decision no longer falls back to the delegation terminal topic at exact product
  head 2583ef7bae142f7c54d37b05e83d4030b983674e.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #2344 product head is exact for OMN-14794 evidence.'
    command: 'PRODUCT_PR_NUMBER=2344; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=2583ef7bae142f7c54d37b05e83d4030b983674e;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnibase_infra-pr-2344
    description: 'PR #2344 on OmniNode-ai/omnibase_infra exists and is bound to OMN-14794 at exact product
      head 2583ef7bae142f7c54d37b05e83d4030b983674e.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2344; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=2583ef7bae142f7c54d37b05e83d4030b983674e;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnibase_infra-pr-2344-diff
    description: 'PR #2344 on OmniNode-ai/omnibase_infra touches the dispatch-result applier routing implementation
      and the routing regression test suite.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2344; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "src/omnibase_infra/runtime/service_dispatch_result_applier.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "tests/unit/runtime/test_dispatch_result_applier_topic_routing.py"'
  - id: dod-omnibase_infra-pr-2344-local-verification
    description: 'Product PR body records local verification for OMN-14794: targeted routing tests, applier-consuming
      modules, ruff format/check, mypy over the changed source, pre-commit, and pre-push smart-tests selector.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2344; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "test_dispatch_result_applier_topic_routing.py"
          && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "21671
          passed / 40 skipped"'
  - id: dod-omnibase_infra-pr-2344-deploy
    description: Deploy-readiness assessment for OMN-14794. The PR changes dispatch-result applier routing
      behavior and tests; it does not perform live deploy, restart, .201, Kafka, database, or production
      mutation in this OCC evidence lane, and the PR body marks live re-canary as a required follow-up
      operator-run step.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14794/dod-omnibase_infra-pr-2344-deploy/command.yaml
          && grep -qi ''no live deploy, restart, .201, Kafka, database, or production mutation'' drift/dod_receipts/OMN-14794/dod-omnibase_infra-pr-2344-deploy/command.yaml'
  - id: occ-self-bind-pr-4422
    description: 'OCC PR #4422 on OmniNode-ai/onex_change_control self-binding proof for OMN-14794 evidence.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
