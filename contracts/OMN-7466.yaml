---
schema_version: "1.0.0"
ticket_id: OMN-7466
title: "Build loop: auto-ship dirty canonical repos to worktrees + PRs"
summary: >
  Add omnimarket PR #951 node_dirty_canonical_sweep, which detects dirty canonical omni_home repo clones
  and ships those changes into dev-based worktrees and PRs without committing in canonical repos.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnimarket PR #951 carries the OMN-7466 dirty canonical sweep node at the recorded head."
    command: "gh pr view 951 --repo OmniNode-ai/omnimarket --json number,url,headRefOid,headRefName,title,state,baseRefName"
  - kind: "tests"
    description: "Focused unit, lint, type, and pre-commit checks pass for node_dirty_canonical_sweep."
    command: "uv run pytest tests/unit/nodes/node_dirty_canonical_sweep -q && uv run ruff check src/omnimarket/nodes/node_dirty_canonical_sweep
      tests/unit/nodes/node_dirty_canonical_sweep && uv run mypy src/omnimarket/nodes/node_dirty_canonical_sweep
      --strict && OMNIBASE_INFRA_PATH=/Users/jonah/Code/omni_home/omnibase_infra uv run pre-commit run
      --all-files"
  - kind: "manual"
    description: "Deploy impact is reviewed for the new omnimarket runtime node and dev-target PR behavior."
    command: "deploy impact review for omnimarket PR #951"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-951-local-verification"
    description: "omnimarket PR #951 dirty canonical sweep node passes focused local verification at the
      recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7466/dod-omnimarket-pr-951-local-verification/command.yaml"
          && grep -q '^commit_sha: "5dfd47d742071460d11b0146b3ebb5a10d5e2188"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7466/dod-omnimarket-pr-951-local-verification/command.yaml"
          && grep -q '4 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7466/dod-omnimarket-pr-951-local-verification/command.yaml"
          && grep -q 'All checks passed!' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7466/dod-omnimarket-pr-951-local-verification/command.yaml"
          && grep -q 'Success: no issues found in 7 source files' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7466/dod-omnimarket-pr-951-local-verification/command.yaml"
  - id: "dod-omnimarket-pr-951-deploy-impact"
    description: "omnimarket PR #951 deploy impact is reviewed for adding a new runtime node that opens
      dev-targeted rescue PRs."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7466/dod-omnimarket-pr-951-deploy-impact/command.yaml"
          && grep -q '^commit_sha: "5dfd47d742071460d11b0146b3ebb5a10d5e2188"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7466/dod-omnimarket-pr-951-deploy-impact/command.yaml"
          && grep -q 'deploy impact review passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7466/dod-omnimarket-pr-951-deploy-impact/command.yaml"
          && echo 'deploy impact review passed'
  - id: "dod-occ-pr-1807-evidence-publication"
    description: "onex_change_control PR #1807 publishes the central OMN-7466 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7466/dod-occ-pr-1807-evidence-publication/command.yaml"
          && grep -q '^pr_number: 1807$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7466/dod-occ-pr-1807-evidence-publication/command.yaml"
