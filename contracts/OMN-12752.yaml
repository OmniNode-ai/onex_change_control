---
schema_version: "1.0.0"
ticket_id: "OMN-12752"
title: "Route local runtime terminal events for single handlers"
summary: >-
  Central OCC evidence for omnibase_infra PR #1890. The PR publishes terminal output when a local runtime
  contract has one handler and no downstream output events, preserving projection materialization for
  single-handler delegation workflows while keeping chained handler routing unchanged.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1890"
    description: "omnibase_infra PR #1890 routes single-handler local runtime results to the terminal
      topic."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12752/dod-omnibase-infra-pr-1890/command.yaml
          && grep -q '^pr_number: 1890$' drift/dod_receipts/OMN-12752/dod-omnibase-infra-pr-1890/command.yaml
          && grep -q '3d88a74' drift/dod_receipts/OMN-12752/dod-omnibase-infra-pr-1890/command.yaml
  - id: "dod-runtime-local-verification"
    description: "Focused local verification proves single-handler terminal publish and existing chained
      routing."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12752/dod-runtime-local-verification/command.yaml
          && grep -q '81 passed' drift/dod_receipts/OMN-12752/dod-runtime-local-verification/command.yaml
          && grep -q 'All checks passed' drift/dod_receipts/OMN-12752/dod-runtime-local-verification/command.yaml
  - id: "dod-deploy-gate-prerequisite"
    description: "Deploy-gate prerequisite evidence for runtime path changes before post-merge dev deployment."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12752/dod-deploy-gate-prerequisite/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-12752/dod-deploy-gate-prerequisite/command.yaml
          && grep -q '3d88a74' drift/dod_receipts/OMN-12752/dod-deploy-gate-prerequisite/command.yaml
  - id: "dod-occ-evidence-pr-2246"
    description: "onex_change_control PR #2246 carries the central OMN-12752 contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12752/dod-occ-evidence-pr-2246/command.yaml
          && grep -q '^pr_number: 2246$' drift/dod_receipts/OMN-12752/dod-occ-evidence-pr-2246/command.yaml
