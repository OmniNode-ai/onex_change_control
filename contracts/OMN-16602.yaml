---
schema_version: "1.0.0"
ticket_id: "OMN-16602"
title: "Autobind OCC evidence for OMN-16602"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_compat PR #201.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #201 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #201 on OmniNode-ai/omnibase_compat changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (.github/CONTRIBUTING.md, .github/workflows/kb-doc-gate.yml, .kb-doc-gate.yaml, .pre-commit-config.yaml) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_compat-pr-201"
    description: "PR #201 on OmniNode-ai/omnibase_compat — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 201 --repo OmniNode-ai/omnibase_compat --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_compat/pulls/201/files --paginate --jq '.[].filename' | grep -cFx '.github/CONTRIBUTING.md'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_compat-pr-201"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
