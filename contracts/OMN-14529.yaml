---
schema_version: "1.0.0"
ticket_id: "OMN-14529"
title: "feat(OMN-14529): wire decision_store dispatch route and real default adapters"
summary: >-
  Central OCC evidence for the paired decision_store landing lane: omnibase_infra PR #2292 wires the `onex
  skill decision_store` dispatch route, JSON CLI argument support, and decision-store query row conversion;
  omnimarket PR #1749 wires the zero-argument orchestrator defaults, Postgres persistence bridge, conflict
  check, and event-loop-safe sync/async handoff. Together they turn the previously unreachable documented
  decision_store skill into a product-observed CLI path.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2292-product-head"
    description: >-
      Product-observing verification for omnibase_infra PR #2292 at head 3697151836f963bb25e8e52193f641a78d7f17cb.
      The proof reads PR metadata and changed files from GitHub, then checks that the PR adds JSON CLI
      argument support, registers the decision_store skill route to node_decision_store_orchestrator,
      fixes decision_type row conversion, and includes the decision_store regression tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=2292; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=3697151836f963bb25e8e52193f641a78d7f17cb;
          d="$(mktemp -d)" && gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,baseRefName,files,title
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
          > "${d}/pr.txt" && grep -Fq "${PRODUCT_HEAD}" "${d}/pr.txt" && grep -Fxq dev "${d}/pr.txt" &&
          grep -Fq 'feat(OMN-14529): wire decision_store skill dispatch route' "${d}/pr.txt" && for p
          in src/omnibase_infra/cli/enum_skill_arg_type.py src/omnibase_infra/cli/cli_skill.py src/omnibase_infra/cli/skill_mapping.yaml
          src/omnibase_infra/nodes/node_decision_store_query_compute/handlers/handler_query_decisions.py
          tests/unit/cli/test_cli_skill.py tests/unit/nodes/test_decision_store_query_compute.py; do grep
          -Fq "$p" "${d}/pr.txt" && mkdir -p "${d}/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "${d}/$p"; done && grep -Fq 'JSON = "json"' "${d}/src/omnibase_infra/cli/enum_skill_arg_type.py"
          && grep -Fq 'json.loads(raw)' "${d}/src/omnibase_infra/cli/cli_skill.py" && grep -Fq 'skill_name:
          decision_store' "${d}/src/omnibase_infra/cli/skill_mapping.yaml" && grep -Fq 'node_decision_store_orchestrator'
          "${d}/src/omnibase_infra/cli/skill_mapping.yaml" && grep -Fq 'arg_type: json' "${d}/src/omnibase_infra/cli/skill_mapping.yaml"
          && grep -Fq 'decision_type": str(row["decision_type"]).lower()' "${d}/src/omnibase_infra/nodes/node_decision_store_query_compute/handlers/handler_query_decisions.py"
          && grep -Fq 'test_decision_store_registered_and_wired' "${d}/tests/unit/cli/test_cli_skill.py"
          && grep -Fq 'test_row_to_entry_lowercases_decision_type_for_omnibase_core_enum' "${d}/tests/unit/nodes/test_decision_store_query_compute.py"
  - id: "dod-infra-pr-2292-local-verification"
    description: >-
      Local verification for omnibase_infra PR #2292 at head 3697151836f963bb25e8e52193f641a78d7f17cb.
      The focused unit suite was rerun in Codex from the pinned product proof worktree and the PR body
      records the broader CLI seam and ephemeral Postgres row-count proof.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=2292; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=3697151836f963bb25e8e52193f641a78d7f17cb;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,body --jq '.headRefOid
          == "'${PRODUCT_HEAD}'" and (.body | contains("SELECT count(*) FROM decision_store")) and (.body
          | contains("0 → 1 → 2 → 3")) and (.body | contains("3 → 4 → 5")) and (.body | contains("full
          CLI seam proven end-to-end")) and (.body | contains("uv run pytest tests/unit/cli/test_cli_skill.py
          tests/unit/nodes/test_decision_store_query_compute.py -m unit"))' | grep -qx true
  - id: "dod-omnimarket-pr-1749-product-head"
    description: >-
      Product-observing verification for omnimarket PR #1749 at head bbf248a5ab55887d78558f8f94d5a5f2000139cf.
      The proof reads PR metadata and changed files from GitHub, then checks that the PR adds real default
      decision_store orchestrator collaborators, the Postgres bridge, structural conflict detection, honest
      deferred semantic/notify defaults, event-loop-safe _run_async behavior, and golden-chain coverage.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1749; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=bbf248a5ab55887d78558f8f94d5a5f2000139cf;
          d="$(mktemp -d)" && gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,baseRefName,files,title
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
          > "${d}/pr.txt" && grep -Fq "${PRODUCT_HEAD}" "${d}/pr.txt" && grep -Fxq dev "${d}/pr.txt" &&
          grep -Fq 'feat(OMN-14529): wire real default adapters for decision_store orchestrator' "${d}/pr.txt"
          && for p in src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_decision_store.py
          src/omnimarket/nodes/node_decision_store_orchestrator/adapters/structural_conflict_check.py
          src/omnimarket/nodes/node_decision_store_orchestrator/adapters/deferred_semantic_review.py src/omnimarket/nodes/node_decision_store_orchestrator/adapters/log_only_notify.py
          src/omnimarket/nodes/node_decision_store_orchestrator/handlers/handler_decision_store_orchestrator.py
          tests/test_golden_chain_decision_store_orchestrator.py; do grep -Fq "$p" "${d}/pr.txt" && mkdir
          -p "${d}/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "${d}/$p"; done && grep -Fq 'def _run_async' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_decision_store.py"
          && grep -Fq 'PostgresDecisionStore' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/handlers/handler_decision_store_orchestrator.py"
          && grep -Fq 'StructuralConflictCheck' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/handlers/handler_decision_store_orchestrator.py"
          && grep -Fq 'DeferredSemanticReview' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/handlers/handler_decision_store_orchestrator.py"
          && grep -Fq 'LogOnlyNotify' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/handlers/handler_decision_store_orchestrator.py"
          && grep -Fq 'test_run_async_bridge_works_from_inside_a_running_loop' "${d}/tests/test_golden_chain_decision_store_orchestrator.py"
          && grep -Fq 'decision-query-result.v1' "${d}/tests/test_golden_chain_decision_store_orchestrator.py"
  - id: "dod-omnimarket-pr-1749-local-verification"
    description: >-
      Local verification for omnimarket PR #1749 at head bbf248a5ab55887d78558f8f94d5a5f2000139cf. The
      focused golden-chain unit suite was rerun in Codex from the pinned product proof worktree and the
      PR body records the full CLI seam and ephemeral Postgres row-count proof.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1749; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=bbf248a5ab55887d78558f8f94d5a5f2000139cf;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,body --jq '.headRefOid
          == "'${PRODUCT_HEAD}'" and (.body | contains("SELECT count(*) FROM decision_store")) and (.body
          | contains("0 → 1 → 2 → 3")) and (.body | contains("3 → 4 → 5")) and (.body | contains("full
          CLI seam proven end-to-end")) and (.body | contains("uv run pytest tests/test_golden_chain_decision_store_orchestrator.py
          -m unit"))' | grep -qx true
  - id: "dod-omn14529-deploy-scope"
    description: >-
      Deploy-scope audit for OMN-14529. The product PRs change CLI dispatch, Python handler/adapters,
      tests, and one forward migration file, and the PR bodies explicitly state that the proof used local
      ephemeral Postgres with no Docker and no .201 runtime mutation. No live deploy or restart was performed
      by this OCC merge lane.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          INFRA_PR_NUMBER=2292; MARKET_PR_NUMBER=1749; INFRA_REPO=OmniNode-ai/omnibase_infra; MARKET_REPO=OmniNode-ai/omnimarket;
          INFRA_HEAD=3697151836f963bb25e8e52193f641a78d7f17cb; MARKET_HEAD=bbf248a5ab55887d78558f8f94d5a5f2000139cf;
          gh pr view "${INFRA_PR_NUMBER}" --repo "${INFRA_REPO}" --json headRefOid,body --jq '.headRefOid
          == "'${INFRA_HEAD}'" and (.body | contains("no `.201` mutation")) and (.body | contains(".201`
          stability-test lane readback — OWED"))' | grep -qx true && gh pr view "${MARKET_PR_NUMBER}"
          --repo "${MARKET_REPO}" --json headRefOid,body --jq '.headRefOid == "'${MARKET_HEAD}'" and (.body
          | contains("no `.201` mutation")) and (.body | contains(".201` stability-test lane readback
          — OWED"))' | grep -qx true && gh pr diff "${INFRA_PR_NUMBER}" --repo "${INFRA_REPO}" --name-only
          | grep -Fxq src/omnibase_infra/cli/skill_mapping.yaml && gh pr diff "${INFRA_PR_NUMBER}" --repo
          "${INFRA_REPO}" --name-only | grep -Fxq docker/migrations/forward/nodes/node_projection_context_roi/002_add_factor_subset_hash_and_routing_source.sql
          && gh pr diff "${MARKET_PR_NUMBER}" --repo "${MARKET_REPO}" --name-only | grep -Fxq src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_decision_store.py
          && ! gh pr diff "${MARKET_PR_NUMBER}" --repo "${MARKET_REPO}" --name-only | grep -qE '(^|/)(systemd|deploy/scripts)(/|$)'
  - id: "dod-occ-self-bind-pr-4208"
    description: >-
      OCC companion self-binding for onex_change_control PR #4208. Verifies the PR is on the expected
      OMN-14529 evidence branch and contains the initial evidence commit ea1867caeb617fe999c587fe481b65ceffc65642.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          OCC_PR_NUMBER=4208; OCC_REPO=OmniNode-ai/onex_change_control; OCC_BRANCH=jonah/omn-14529-occ-decision-store-pair;
          INITIAL_EVIDENCE_COMMIT=ea1867caeb617fe999c587fe481b65ceffc65642; gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}"
          --jq '.head.ref == "'$OCC_BRANCH'" and ((.state == "open") or (.state == "closed" and .merged_at
          != null))' | grep -qx true && gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}/commits" --paginate
          --jq '.[].sha' | grep -Fx "$INITIAL_EVIDENCE_COMMIT"
  - id: "dod-infra-pr-2292-product-head-42f566"
    evidence_artifact: "supersedes_dod_evidence:dod-infra-pr-2292-product-head"
    description: >-
      Superseding product-observing verification for omnibase_infra PR #2292 at head 42f56600048e3154882300be741f9704dbe6443d.
      This refresh covers the contract-sync repair that moved node_decision_store_query_compute contract_version/node_version
      to patch 1 and documents the omnibase_core-compatible row-shaping behavior required by the changed
      handler.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=2292; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=42f56600048e3154882300be741f9704dbe6443d;
          d="$(mktemp -d)" && gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,baseRefName,files,title
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
          > "${d}/pr.txt" && grep -Fq "${PRODUCT_HEAD}" "${d}/pr.txt" && grep -Fxq dev "${d}/pr.txt" &&
          grep -Fq 'feat(OMN-14529): wire decision_store skill dispatch route' "${d}/pr.txt" && for p
          in src/omnibase_infra/cli/enum_skill_arg_type.py src/omnibase_infra/cli/cli_skill.py src/omnibase_infra/cli/skill_mapping.yaml
          src/omnibase_infra/nodes/node_decision_store_query_compute/handlers/handler_query_decisions.py
          src/omnibase_infra/nodes/node_decision_store_query_compute/contract.yaml tests/unit/cli/test_cli_skill.py
          tests/unit/nodes/test_decision_store_query_compute.py; do grep -Fq "$p" "${d}/pr.txt" && mkdir
          -p "${d}/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "${d}/$p"; done && grep -Fq 'JSON = "json"' "${d}/src/omnibase_infra/cli/enum_skill_arg_type.py"
          && grep -Fq 'json.loads(raw)' "${d}/src/omnibase_infra/cli/cli_skill.py" && grep -Fq 'skill_name:
          decision_store' "${d}/src/omnibase_infra/cli/skill_mapping.yaml" && grep -Fq 'node_decision_store_orchestrator'
          "${d}/src/omnibase_infra/cli/skill_mapping.yaml" && grep -Fq 'arg_type: json' "${d}/src/omnibase_infra/cli/skill_mapping.yaml"
          && grep -Fq 'decision_type": str(row["decision_type"]).lower()' "${d}/src/omnibase_infra/nodes/node_decision_store_query_compute/handlers/handler_query_decisions.py"
          && grep -Fq 'patch: 1' "${d}/src/omnibase_infra/nodes/node_decision_store_query_compute/contract.yaml"
          && grep -Fq 'omnibase_core-compatible decision entries' "${d}/src/omnibase_infra/nodes/node_decision_store_query_compute/contract.yaml"
          && grep -Fq 'test_decision_store_registered_and_wired' "${d}/tests/unit/cli/test_cli_skill.py"
          && grep -Fq 'test_row_to_entry_lowercases_decision_type_for_omnibase_core_enum' "${d}/tests/unit/nodes/test_decision_store_query_compute.py"
  - id: "dod-infra-pr-2292-local-verification-42f566"
    evidence_artifact: "supersedes_dod_evidence:dod-infra-pr-2292-local-verification"
    description: >-
      Superseding local verification for omnibase_infra PR #2292 at head 42f56600048e3154882300be741f9704dbe6443d.
      Codex reran the focused decision_store/CLI unit suite against the current product head and verified
      the PR body plus head commit record the contract-sync repair without performing any live deploy,
      restart, or .201 mutation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=2292; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=42f56600048e3154882300be741f9704dbe6443d;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,body --jq '.headRefOid
          == "'${PRODUCT_HEAD}'" and (.body | contains("SELECT count(*) FROM decision_store")) and (.body
          | contains("0 → 1 → 2 → 3")) and (.body | contains("3 → 4 → 5")) and (.body | contains("full
          CLI seam proven end-to-end"))' | grep -qx true && gh api "repos/${PRODUCT_REPO}/commits/${PRODUCT_HEAD}"
          --jq '.commit.message' | grep -Fq 'fix(OMN-14529): sync decision store query contract'
  - id: "dod-omn14529-deploy-scope-42f566"
    evidence_artifact: "supersedes_dod_evidence:dod-omn14529-deploy-scope"
    description: >-
      Superseding deploy-scope audit for OMN-14529 after omnibase_infra PR #2292 moved to head 42f56600048e3154882300be741f9704dbe6443d.
      The additional product change is a node contract YAML synchronization for an already-changed handler;
      this merge lane still performed no live deploy, restart, or .201 mutation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          INFRA_PR_NUMBER=2292; MARKET_PR_NUMBER=1749; INFRA_REPO=OmniNode-ai/omnibase_infra; MARKET_REPO=OmniNode-ai/omnimarket;
          INFRA_HEAD=42f56600048e3154882300be741f9704dbe6443d; MARKET_HEAD=bbf248a5ab55887d78558f8f94d5a5f2000139cf;
          gh pr view "${INFRA_PR_NUMBER}" --repo "${INFRA_REPO}" --json headRefOid,body --jq '.headRefOid
          == "'${INFRA_HEAD}'" and (.body | contains("no `.201` mutation")) and (.body | contains(".201`
          stability-test lane readback — OWED"))' | grep -qx true && gh pr view "${MARKET_PR_NUMBER}"
          --repo "${MARKET_REPO}" --json headRefOid,body --jq '.headRefOid == "'${MARKET_HEAD}'" and (.body
          | contains("no `.201` mutation")) and (.body | contains(".201` stability-test lane readback
          — OWED"))' | grep -qx true && gh pr diff "${INFRA_PR_NUMBER}" --repo "${INFRA_REPO}" --name-only
          | grep -Fxq src/omnibase_infra/nodes/node_decision_store_query_compute/contract.yaml && gh pr
          diff "${INFRA_PR_NUMBER}" --repo "${INFRA_REPO}" --name-only | grep -Fxq src/omnibase_infra/cli/skill_mapping.yaml
          && gh pr diff "${MARKET_PR_NUMBER}" --repo "${MARKET_REPO}" --name-only | grep -Fxq src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_decision_store.py
          && ! gh pr diff "${MARKET_PR_NUMBER}" --repo "${MARKET_REPO}" --name-only | grep -qE '(^|/)(systemd|deploy/scripts)(/|$)'
  - id: "dod-occ-self-bind-pr-4209"
    description: >-
      OCC companion self-binding for onex_change_control PR #4209. Verifies the PR is on the expected
      OMN-14529 infra-head-refresh evidence branch and contains the initial refresh evidence commit 38307daa36274f12a905cc75ff6771817c8a70e0.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          OCC_PR_NUMBER=4209; OCC_REPO=OmniNode-ai/onex_change_control; OCC_BRANCH=jonah/omn-14529-occ-infra-2292-head-42f566;
          INITIAL_EVIDENCE_COMMIT=38307daa36274f12a905cc75ff6771817c8a70e0; gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}"
          --jq '.head.ref == "'$OCC_BRANCH'" and ((.state == "open") or (.state == "closed" and .merged_at
          != null))' | grep -qx true && gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}/commits" --paginate
          --jq '.[].sha' | grep -Fx "$INITIAL_EVIDENCE_COMMIT"
  - id: "dod-omnimarket-pr-1749-product-head-c31d950b"
    evidence_artifact: "supersedes_dod_evidence:dod-omnimarket-pr-1749-product-head"
    description: >-
      Superseding product-observing verification for omnimarket PR #1749 at head c31d950b35ab42a522a315cd48b10863245db137.
      This refresh covers the metadata dependency declaration repair for node_decision_store_orchestrator
      while preserving the real default adapters, Postgres bridge, structural conflict detection, event-loop-safe
      _run_async behavior, and golden-chain coverage.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1749; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=c31d950b35ab42a522a315cd48b10863245db137;
          d="$(mktemp -d)" && gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,baseRefName,files,title
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
          > "${d}/pr.txt" && grep -Fq "${PRODUCT_HEAD}" "${d}/pr.txt" && grep -Fxq dev "${d}/pr.txt" &&
          grep -Fq 'feat(OMN-14529): wire real default adapters for decision_store orchestrator' "${d}/pr.txt"
          && for p in src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_decision_store.py
          src/omnimarket/nodes/node_decision_store_orchestrator/adapters/structural_conflict_check.py
          src/omnimarket/nodes/node_decision_store_orchestrator/adapters/deferred_semantic_review.py src/omnimarket/nodes/node_decision_store_orchestrator/adapters/log_only_notify.py
          src/omnimarket/nodes/node_decision_store_orchestrator/handlers/handler_decision_store_orchestrator.py
          src/omnimarket/nodes/node_decision_store_orchestrator/metadata.yaml tests/test_golden_chain_decision_store_orchestrator.py;
          do grep -Fq "$p" "${d}/pr.txt" && mkdir -p "${d}/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "${d}/$p"; done && grep -Fq 'def _run_async' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_decision_store.py"
          && grep -Fq 'PostgresDecisionStore' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/handlers/handler_decision_store_orchestrator.py"
          && grep -Fq 'StructuralConflictCheck' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/handlers/handler_decision_store_orchestrator.py"
          && grep -Fq 'DeferredSemanticReview' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/handlers/handler_decision_store_orchestrator.py"
          && grep -Fq 'LogOnlyNotify' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/handlers/handler_decision_store_orchestrator.py"
          && grep -Fq 'asyncpg>=0.31.0' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/metadata.yaml"
          && grep -Fq 'omnibase-infra>=0.1.0' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/metadata.yaml"
          && grep -Fq 'test_run_async_bridge_works_from_inside_a_running_loop' "${d}/tests/test_golden_chain_decision_store_orchestrator.py"
          && grep -Fq 'decision-query-result.v1' "${d}/tests/test_golden_chain_decision_store_orchestrator.py"
  - id: "dod-omnimarket-pr-1749-local-verification-c31d950b"
    evidence_artifact: "supersedes_dod_evidence:dod-omnimarket-pr-1749-local-verification"
    description: >-
      Superseding local verification for omnimarket PR #1749 at head c31d950b35ab42a522a315cd48b10863245db137.
      The PR body preserves the live row-proof and full CLI seam proof, and the head commit records the
      metadata dependency declaration repair for the decision_store orchestrator.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1749; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=c31d950b35ab42a522a315cd48b10863245db137;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,body --jq '.headRefOid
          == "'${PRODUCT_HEAD}'" and (.body | contains("SELECT count(*) FROM decision_store")) and (.body
          | contains("0 → 1 → 2 → 3")) and (.body | contains("3 → 4 → 5")) and (.body | contains("full
          CLI seam proven end-to-end"))' | grep -qx true && gh api "repos/${PRODUCT_REPO}/commits/${PRODUCT_HEAD}"
          --jq '.commit.message' | grep -Fq 'fix(OMN-14529): declare decision store orchestrator dependencies'
  - id: "dod-omn14529-deploy-scope-c31d950b"
    evidence_artifact: "supersedes_dod_evidence:dod-omn14529-deploy-scope-42f566"
    description: >-
      Superseding deploy-scope audit for OMN-14529 after omnimarket PR #1749 moved to head c31d950b35ab42a522a315cd48b10863245db137.
      The additional market change is a metadata.yaml dependency declaration for already-imported packages;
      this merge lane still performed no live deploy, restart, or .201 mutation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          INFRA_PR_NUMBER=2292; MARKET_PR_NUMBER=1749; INFRA_REPO=OmniNode-ai/omnibase_infra; MARKET_REPO=OmniNode-ai/omnimarket;
          INFRA_HEAD=42f56600048e3154882300be741f9704dbe6443d; MARKET_HEAD=c31d950b35ab42a522a315cd48b10863245db137;
          gh pr view "${INFRA_PR_NUMBER}" --repo "${INFRA_REPO}" --json headRefOid,body --jq '.headRefOid
          == "'${INFRA_HEAD}'" and (.body | contains("no `.201` mutation")) and (.body | contains(".201`
          stability-test lane readback — OWED"))' | grep -qx true && gh pr view "${MARKET_PR_NUMBER}"
          --repo "${MARKET_REPO}" --json headRefOid,body --jq '.headRefOid == "'${MARKET_HEAD}'" and (.body
          | contains("no `.201` mutation")) and (.body | contains(".201` stability-test lane readback
          — OWED"))' | grep -qx true && gh pr diff "${MARKET_PR_NUMBER}" --repo "${MARKET_REPO}" --name-only
          | grep -Fxq src/omnimarket/nodes/node_decision_store_orchestrator/metadata.yaml && gh pr diff
          "${MARKET_PR_NUMBER}" --repo "${MARKET_REPO}" --name-only | grep -Fxq src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_decision_store.py
          && ! gh pr diff "${MARKET_PR_NUMBER}" --repo "${MARKET_REPO}" --name-only | grep -qE '(^|/)(systemd|deploy/scripts)(/|$)'
  - id: "dod-occ-self-bind-pr-4210"
    description: >-
      OCC companion self-binding for onex_change_control PR #4210. Verifies the PR is on the expected
      OMN-14529 market-head-refresh evidence branch and contains the initial refresh evidence commit 9786ba836ab12e766219701a8f3f4cd90b12e28a.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          OCC_PR_NUMBER=4210; OCC_REPO=OmniNode-ai/onex_change_control; OCC_BRANCH=jonah/omn-14529-occ-market-1749-head-c31d950b;
          INITIAL_EVIDENCE_COMMIT=9786ba836ab12e766219701a8f3f4cd90b12e28a; gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}"
          --jq '.head.ref == "'$OCC_BRANCH'" and ((.state == "open") or (.state == "closed" and .merged_at
          != null))' | grep -qx true && gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}/commits" --paginate
          --jq '.[].sha' | grep -Fx "$INITIAL_EVIDENCE_COMMIT"
  - id: "dod-omnimarket-pr-1749-product-head-7adf122"
    evidence_artifact: "supersedes_dod_evidence:dod-omnimarket-pr-1749-product-head-c31d950b"
    description: >-
      Superseding product-observing verification for omnimarket PR #1749 at head 7adf122ebf5fd721e878293f2186aaa1560868e8.
      This refresh covers the imperative-contract repair that moves asyncpg pool construction behind an
      injected pool-resource boundary and adds the AsyncpgAdapter.pool accessor used by canonical omnibase_infra
      handlers.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1749; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=7adf122ebf5fd721e878293f2186aaa1560868e8;
          d="$(mktemp -d)" && gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,files
          --jq '.headRefOid + "\n" + ((.files | map(.path)) | join("\n"))' > "${d}/pr.txt" && grep -Fq
          "${PRODUCT_HEAD}" "${d}/pr.txt" && for p in src/omnimarket/adapters/asyncpg_adapter.py src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_decision_store.py
          src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_pool_resource.py; do
          grep -Fq "$p" "${d}/pr.txt" && mkdir -p "${d}/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "${d}/$p"; done && grep -Fq 'pool_resource_factory' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_decision_store.py"
          && ! grep -Fq 'AsyncpgAdapter(' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_decision_store.py"
          && grep -Fq 'class AsyncpgPoolResource' "${d}/src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_pool_resource.py"
          && grep -Fq 'def pool(self)' "${d}/src/omnimarket/adapters/asyncpg_adapter.py"
  - id: "dod-omnimarket-pr-1749-local-verification-7adf122"
    evidence_artifact: "supersedes_dod_evidence:dod-omnimarket-pr-1749-local-verification-c31d950b"
    description: >-
      Superseding local verification for omnimarket PR #1749 at head 7adf122ebf5fd721e878293f2186aaa1560868e8.
      Codex reran ruff, format, and the focused decision_store golden-chain suite, then committed with
      hooks enabled and OMNIBASE_INFRA_PATH set for sibling validators.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1749; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=7adf122ebf5fd721e878293f2186aaa1560868e8;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid --jq '.headRefOid
          == "'${PRODUCT_HEAD}'"' | grep -qx true && gh api "repos/${PRODUCT_REPO}/commits/${PRODUCT_HEAD}"
          --jq '.commit.message' | grep -Fq 'fix(OMN-14529): inject decision store postgres pool resource'
  - id: "dod-omn14529-deploy-scope-7adf122"
    evidence_artifact: "supersedes_dod_evidence:dod-omn14529-deploy-scope-c31d950b"
    description: >-
      Superseding deploy-scope audit for OMN-14529 after omnimarket PR #1749 moved to head 7adf122ebf5fd721e878293f2186aaa1560868e8.
      The additional market change is Python adapter-boundary code and tests only; this merge lane still
      performed no live deploy, restart, or .201 mutation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          INFRA_PR_NUMBER=2292; MARKET_PR_NUMBER=1749; INFRA_REPO=OmniNode-ai/omnibase_infra; MARKET_REPO=OmniNode-ai/omnimarket;
          INFRA_HEAD=42f56600048e3154882300be741f9704dbe6443d; MARKET_HEAD=7adf122ebf5fd721e878293f2186aaa1560868e8;
          gh pr view "${INFRA_PR_NUMBER}" --repo "${INFRA_REPO}" --json headRefOid,body --jq '.headRefOid
          == "'${INFRA_HEAD}'" and (.body | contains("no `.201` mutation"))' | grep -qx true && gh pr
          view "${MARKET_PR_NUMBER}" --repo "${MARKET_REPO}" --json headRefOid,body --jq '.headRefOid
          == "'${MARKET_HEAD}'" and (.body | contains("no `.201` mutation"))' | grep -qx true && gh pr
          diff "${MARKET_PR_NUMBER}" --repo "${MARKET_REPO}" --name-only | grep -Fxq src/omnimarket/nodes/node_decision_store_orchestrator/adapters/postgres_pool_resource.py
          && ! gh pr diff "${MARKET_PR_NUMBER}" --repo "${MARKET_REPO}" --name-only | grep -qE '(^|/)(systemd|deploy/scripts)(/|$)'
  - id: "dod-occ-self-bind-pr-4211"
    description: >-
      OCC companion self-binding for onex_change_control PR #4211. Verifies the PR is on the expected
      OMN-14529 market-head-refresh evidence branch and contains the initial refresh evidence commit c4cea3c9a838dee8cf318f15a9ab72f21fc2d19a.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          OCC_PR_NUMBER=4211; OCC_REPO=OmniNode-ai/onex_change_control; OCC_BRANCH=jonah/omn-14529-occ-market-1749-head-7adf122;
          INITIAL_EVIDENCE_COMMIT=c4cea3c9a838dee8cf318f15a9ab72f21fc2d19a; gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}"
          --jq '.head.ref == "'$OCC_BRANCH'" and ((.state == "open") or (.state == "closed" and .merged_at
          != null))' | grep -qx true && gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}/commits" --paginate
          --jq '.[].sha' | grep -Fx "$INITIAL_EVIDENCE_COMMIT"
