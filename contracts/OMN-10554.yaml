---
schema_version: "1.0.0"
ticket_id: "OMN-10554"
title: "feat: leaked-literals gate advisory mode"
summary: "Add the omnimarket leaked-literals scanner, advisory GitHub workflow, and unit coverage for
  allowlist and blocking-mode behavior. The gate remains warn-only until the later scrub tasks make blocking
  mode clean."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Receipt Gate passes on omnimarket PR #535 after this OCC evidence lands."
    command: "gh pr checks 535 --repo OmniNode-ai/omnimarket --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Leaked-literals scanner runs in advisory mode and reports current findings without blocking."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10554/dod-001/command.yaml\"; else bash scripts/validation/check_leaked_literals.sh
          advisory all; fi"
  - id: "dod-002"
    description: "Leak-gate unit tests cover advisory, blocking, allowlist, and self-exempt behavior."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10554/dod-002/command.yaml\"; else uv run pytest
          tests/unit/scripts/test_leak_gate.py -v; fi"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10554 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10554/dod-occ-pr/command.yaml\""
