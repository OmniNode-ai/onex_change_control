---
schema_version: "1.0.0"
ticket_id: "OMN-12072"
title: "Swarm Control Plane widget"
summary: >
  OCC evidence for omnidash PR #125. The change adds the Swarm Control Plane dashboard widget, projection
  topic wiring, API route support, and subtask detail panel for decomposition tree, wave view, live counters,
  savings, and per-subtask runtime metrics.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain"
    description: "25 focused Vitest component tests pass for the Swarm Control Plane UI."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12072/dod-golden-chain/command.yaml
          && grep -q '25 passed' drift/dod_receipts/OMN-12072/dod-golden-chain/command.yaml"
  - id: "dod-lint"
    description: "omnidash ESLint passes with zero warnings."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12072/dod-lint/command.yaml && grep
          -q 'eslint' drift/dod_receipts/OMN-12072/dod-lint/command.yaml"
