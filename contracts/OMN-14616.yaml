---
schema_version: "1.0.0"
ticket_id: "OMN-14616"
title: "fix(OMN-14616): add def-B handle entrypoint to node_pattern_b_broker"
summary: >-
  Central OCC evidence for omnimarket PR #1769. The product PR adds canonical def-B handle(request) entrypoints
  to the node_pattern_b_broker publish and terminal-consumer adapters, binds handler_routing entries to
  the typed ModelPatternBBrokerDispatchRequest event model, and adds non-vacuous RED/GREEN auto-wiring
  coverage for the missing-handle dispatch failure.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1769-product-head"
    description: >-
      Product-observing verification for omnimarket PR #1769 at head 0797a2bba662edf082812902b863a430861ce8f8.
      The proof reads PR metadata and changed files from GitHub at that exact head, verifies the PR targets
      dev, checks the two handler entrypoint additions, checks the typed event_model declarations in contract.yaml,
      and checks the focused missing-handle regression tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1769; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=0797a2bba662edf082812902b863a430861ce8f8;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,baseRefName,files,title
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
          > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fxq dev "$d/pr.txt" && grep -Fq
          'fix(OMN-14616): add def-B handle() entrypoint to node_pattern_b_broker handlers' "$d/pr.txt"
          && for p in src/omnimarket/nodes/node_pattern_b_broker/contract.yaml src/omnimarket/nodes/node_pattern_b_broker/handlers/adapter_broker_publish.py
          src/omnimarket/nodes/node_pattern_b_broker/handlers/adapter_broker_terminal_consumer.py tests/unit/nodes/node_pattern_b_broker/test_broker_contract_config.py
          tests/unit/nodes/node_pattern_b_broker/test_broker_handle_entrypoint.py; do grep -Fq "$p" "$d/pr.txt"
          && mkdir -p "$d/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "$d/$p"; done && grep -Fq 'async def handle(' "$d/src/omnimarket/nodes/node_pattern_b_broker/handlers/adapter_broker_publish.py"
          && grep -Fq 'return await self.publish(request)' "$d/src/omnimarket/nodes/node_pattern_b_broker/handlers/adapter_broker_publish.py"
          && grep -Fq 'async def handle(' "$d/src/omnimarket/nodes/node_pattern_b_broker/handlers/adapter_broker_terminal_consumer.py"
          && grep -Fq 'return await self.wait_for_terminal_event(request)' "$d/src/omnimarket/nodes/node_pattern_b_broker/handlers/adapter_broker_terminal_consumer.py"
          && grep -Fq 'ModelPatternBBrokerDispatchRequest' "$d/src/omnimarket/nodes/node_pattern_b_broker/contract.yaml"
          && grep -Fq 'test_real_engine_dispatch_binds_missing_handle_pre_fix' "$d/tests/unit/nodes/node_pattern_b_broker/test_broker_handle_entrypoint.py"
          && grep -Fq 'test_real_engine_dispatch_reaches_publish_via_handle_post_fix' "$d/tests/unit/nodes/node_pattern_b_broker/test_broker_handle_entrypoint.py"
          && grep -Fq 'test_real_engine_dispatch_reaches_wait_for_terminal_event_via_handle_post_fix'
          "$d/tests/unit/nodes/node_pattern_b_broker/test_broker_handle_entrypoint.py"
  - id: "dod-omnimarket-pr-1769-local-verification"
    description: >-
      Verification evidence recorded in the omnimarket PR #1769 body. The PR body records that the focused
      node test suite passed, RED proof was confirmed against reverted pre-fix source, ruff format/check
      passed, mypy --strict passed for the node package, node runtime import sweep passed, and pre-commit
      passed on changed files.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1769; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=0797a2bba662edf082812902b863a430861ce8f8;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,body --jq '.headRefOid
          == "'$PRODUCT_HEAD'" and (.body | contains("37/37 pass")) and (.body | contains("RED confirmed
          against reverted pre-fix source")) and (.body | contains("ruff format --check")) and (.body
          | contains("mypy src/omnimarket/nodes/node_pattern_b_broker/ --strict")) and (.body | contains("node_runtime_sweep
          --import-check")) and (.body | contains("pre-commit run"))' | grep -qx true
  - id: "dod-omnimarket-pr-1769-deploy-scope"
    description: >-
      Deploy-surface audit for omnimarket PR #1769. The PR changes handler, contract, and test files for
      node_pattern_b_broker only; it does not change Dockerfiles, compose manifests, deploy scripts, runtime
      service definitions, database migrations, or live infrastructure. No live deploy or restart was
      performed by this merge lane.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1769; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=0797a2bba662edf082812902b863a430861ce8f8;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,files --jq '.headRefOid
          == "'$PRODUCT_HEAD'"' | grep -qx true && gh pr diff "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO"
          --name-only | grep -Fxq src/omnimarket/nodes/node_pattern_b_broker/contract.yaml && gh pr diff
          "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --name-only | grep -Fxq src/omnimarket/nodes/node_pattern_b_broker/handlers/adapter_broker_publish.py
          && gh pr diff "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --name-only | grep -Fxq src/omnimarket/nodes/node_pattern_b_broker/handlers/adapter_broker_terminal_consumer.py
          && ! gh pr diff "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --name-only | grep -qE '(^|/)(Dockerfile|docker-compose[^/]*|compose[^/]*|deploy|migrations|systemd)(/|$)'
