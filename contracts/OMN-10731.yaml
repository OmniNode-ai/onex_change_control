---
schema_version: "1.0.0"
ticket_id: "OMN-10731"
title: "Fix handler-owned sqlite3 connection in SQLiteProjectionAdapter (DI)"
summary: >
  Binds omniclaude PR #1547, which refactors SQLiteProjectionAdapter to accept an injected sqlite3.Connection
  instead of calling sqlite3.connect() internally. Introduces make_adapter() factory as the single authorised
  connection construction site. Updates server.py and all affected tests to use the new constructor and
  factory.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1547-head"
    description: "omniclaude PR #1547 carries the DI refactor at the recorded implementation head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10731/dod-pr-1547-head/command.yaml\""
  - id: "dod-ci-1547-green"
    description: "All non-receipt CI checks on omniclaude PR #1547 are passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10731/dod-ci-1547-green/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10731/dod-occ-pr/command.yaml\""
