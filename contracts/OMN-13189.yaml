---
schema_version: "1.0.0"
ticket_id: "OMN-13189"
title: "B1: delete dead ModelOnexEnvelopeV1 + its __init__ exports + V1 predecessor mentions — completes
  OMN-4152"
summary: >-
  Deletes ModelOnexEnvelopeV1 from omnibase_core. Verification confirmed the class has zero production
  callers and zero test imports across omnibase_core / omnibase_infra / omnimarket (src + tests), so this
  phase deletes it outright rather than retaining it for backward-compat deserialization. Removes the
  class file model_onex_envelope_v1.py (152 lines), both export lines in models/core/__init__.py (the
  import statement and the __all__ entry), and the 3 V1 predecessor mentions in model_onex_envelope.py
  (module docstring, class docstring, and versionadded note) that referenced the now-deleted class. A
  fourth incidental reference — a docstring mention in tests/unit/models/test_model_onex_envelope.py —
  is also removed so grep -rn "ModelOnexEnvelopeV1" src/ tests/ returns zero. Completes the long-standing
  OMN-4152 (Task 6: Retire ModelOnexEnvelopeV1 from active code paths). No source behavior change, no
  Kafka topic schema change, no DDL/migration, no runtime/deploy/.201 mutation — pure dead-code deletion.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "pr-omnibase_core"
    description: >-
      omnibase_core implementation PR (branch jonah/omn-13189-retire-modelonexenvelopev1) deletes ModelOnexEnvelopeV1.
      The paired OCC receipt drift/dod_receipts/OMN-13189/pr-omnibase_core/command.yaml binds the code
      PR head sha (commit_sha) and OCC PR number (pr_number) with status PASS, and records the zero-reference
      grep proof plus the green gate results (ruff, mypy CI-equivalent, full pytest 41770 passed).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13189/pr-omnibase_core/command.yaml"
