---
schema_version: "1.0.0"
ticket_id: "OMN-10332"
title: "Task 1: Add idempotency unique constraint to llm_call_metrics"
summary: "Add migration 071, rollback, schema fingerprint stamp, writer conflict handling, null-session
  uniqueness, and repeatable behavior tests for LLM call metric idempotency."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
evidence_requirements:
  - kind: "tests"
    description: "Migration, integration, and writer idempotency behavior tests pass, including NULL session_id
      replay coverage"
    command: "cd omnibase_infra && uv run pytest tests/ci/test_llm_call_metrics_idempotency_migration.py
      tests/integration/observability/test_llm_call_metrics_idempotency.py tests/unit/services/observability/llm_cost_aggregation/test_writer_postgres.py
      -q"
  - kind: "tests"
    description: "Schema fingerprint verifies after migration 071"
    command: "cd omnibase_infra && uv run python scripts/check_schema_fingerprint.py verify"
  - kind: "ci"
    description: "Implementation PR created for OMN-10332"
    command: "gh pr view 1454 --repo OmniNode-ai/omnibase_infra --json number,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Migration, integration, and writer idempotency behavior tests passed locally"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10332/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-10332$' drift/dod_receipts/OMN-10332/dod-001/command.yaml"
  - id: "dod-002"
    description: "Schema fingerprint artifact was stamped and verified after adding migration 071"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10332/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-10332$' drift/dod_receipts/OMN-10332/dod-002/command.yaml"
  - id: "dod-003"
    description: "omnibase_infra implementation PR was created and linked"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10332/dod-003/command.yaml && grep
          -q '^pr_number: 1454$' drift/dod_receipts/OMN-10332/dod-003/command.yaml"
