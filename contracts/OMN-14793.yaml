---
schema_version: 1.0.0
ticket_id: OMN-14793
title: Single-producer enforcement lease for OccCompanionEmitter
summary: Central OCC evidence for omnimarket#1822. The product PR implements a shared OCC git-ref lease
  for OccCompanionEmitter so concurrent producers cannot mint competing companions for the same product
  PR head, with lease helper coverage, emitter guard coverage, and no live deploy or runtime mutation
  at exact product head 664f6372398e83709e933166b47fc40277d076cf.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #1822 product head is exact for OMN-14793 evidence.'
    command: 'PRODUCT_PR_NUMBER=1822; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=664f6372398e83709e933166b47fc40277d076cf;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnimarket-pr-1822
    description: 'PR #1822 on OmniNode-ai/omnimarket exists and is bound to OMN-14793 at exact product
      head 664f6372398e83709e933166b47fc40277d076cf.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1822; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=664f6372398e83709e933166b47fc40277d076cf;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnimarket-pr-1822-diff
    description: 'PR #1822 on OmniNode-ai/omnimarket touches the OCC git transport lease helper, the companion
      emitter guard, the future node_occ_companion_effect pointer, and lease/emitter regression tests.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1822; PRODUCT_REPO=OmniNode-ai/omnimarket; : ${PR_NUMBER}; gh
          pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "src/omnimarket/occ_git_transport.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "src/omnimarket/nodes/node_pr_lifecycle_fix_effect/handlers/occ_companion_emitter.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "src/omnimarket/nodes/node_occ_companion_effect/handlers/handler_occ_companion_effect.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "tests/unit/test_occ_git_transport_lease.py"'
  - id: dod-omnimarket-pr-1822-local-verification
    description: 'Product PR body records local verification for OMN-14793: ruff format/check clean, mypy
      strict over src/omnimarket, changed test/golden/parity/effect/golden-chain suites green, changed-file
      pre-commit green including occ-emitter-golden and One OCC producer per contract, and runtime-sweep
      import-check clean.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1822; PRODUCT_REPO=OmniNode-ai/omnimarket; : ${PR_NUMBER}; gh
          pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "mypy --strict
          src/omnimarket" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body
          | grep -q "occ-emitter-golden" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json
          body --jq .body | grep -q "runtime-sweep import-check clean"'
  - id: dod-omnimarket-pr-1822-deploy
    description: Deploy-readiness assessment for OMN-14793. The PR adds lease enforcement code, tests,
      and a future-wiring pointer comment; it does not perform live deploy, restart, .201, Kafka, database,
      or production mutation in this OCC evidence lane, and the PR body explicitly leaves runtime/deploy
      work out of scope.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14793/dod-omnimarket-pr-1822-deploy/command.yaml
          && grep -qi ''no live deploy, restart, .201, Kafka, database, or production mutation'' drift/dod_receipts/OMN-14793/dod-omnimarket-pr-1822-deploy/command.yaml'
