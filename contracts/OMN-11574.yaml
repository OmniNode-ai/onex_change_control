---
schema_version: "1.0.0"
ticket_id: "OMN-11574"
title: "Bind build loop orchestrator topics from contract"
summary: >-
  Bind central OCC evidence for omnimarket PR #755, which replaces build-loop orchestrator inline topic
  literals with contract-derived bindings while preserving exported topic constants for existing consumers.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omnimarket PR #755 build-loop topic binding tests pass in CI."
    command: "gh pr checks 755 --repo OmniNode-ai/omnimarket"
  - kind: "manual"
    description: "Deploy-gate assessment confirms the runtime handler topic-binding change needs post-merge
      validation, not live pre-merge deployment."
    command: "deploy assessment: verify contract-derived topic bindings in CI and rerun downstream deploy
      gate after OCC merge"
dod_evidence:
  - id: "dod-omnimarket-pr-755"
    description: "Bind OMN-11574 evidence to omnimarket PR #755 at the observed PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11574/dod-omnimarket-pr-755/command.yaml\"
          && grep -q '^pr_number: 755$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11574/dod-omnimarket-pr-755/command.yaml\"
          && grep -q '^commit_sha: \"706fcecef1d58a29550aad052a2d4d75ce557a75\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11574/dod-omnimarket-pr-755/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Record deploy-gate assessment for the runtime handler topic-binding path."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11574/dod-deploy-assessment/command.yaml\"
          && grep -qi 'deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11574/dod-deploy-assessment/command.yaml\"
          && grep -q '^pr_number: 755$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11574/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Bind OMN-11574 central evidence to onex_change_control PR #1361."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11574/dod-occ-pr-self/command.yaml\"
          && grep -q '^ticket_id: \"OMN-11574\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11574/dod-occ-pr-self/command.yaml\"
          && grep -q '^pr_number: 1361$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11574/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
