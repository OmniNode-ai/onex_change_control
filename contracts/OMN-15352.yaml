---
schema_version: 1.0.0
ticket_id: OMN-15352
title: Autobind OCC evidence for OMN-15352
summary: 'OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra
  PR #2541. #magic___^_^___line '
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #2541 CI checks green'
    command: gh pr checks ${PR_NUMBER} --repo ${REPO}
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-OmniNode-ai-omnibase_infra-pr-2541
    description: "PR #2541 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
  - id: dod-occ-evidence-admissibility-validator
    description: Hosted OCC evidence admissibility validator (OMN-15247).
    source: generated
    evidence_artifact: supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2541
    checks:
      - check_type: command
        check_value: uv run pytest tests/test_evidence_admissibility.py -q
  - id: occ-self-bind-pr-5479
    description: 'Binds OMN-15352 to OCC companion PR #5479 (self-bind).'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view 5479 --repo OmniNode-ai/onex_change_control --json number,state
  # ---------------------------------------------------------------------------
  # APPEND-ONLY AC4 PROOF-SURFACE REPAIR (2026-07-30).
  #
  # Nothing above this line is edited or removed. Every entry above is preserved
  # byte-intact and its per-entry hash (OMN-13888) is unchanged, so every prior
  # merged receipt stays valid.
  #
  # THE GAP THIS CLOSES. Until now this contract's entire evidence surface was
  # PR/CI-state assertions plus OCC's own 76-test admissibility suite (which is
  # byte-identical in the OMN-15378 and OMN-15430 contracts, so it says nothing
  # about THIS ticket). Nothing in it probed `.201`. AC4 declares
  # `proof_class: live-readback`, so a `dod_verify` green produced no red on AC4
  # only because it produced no probe — the absence of a red is the absence of a
  # probe, not a pass. Three independent parties said so on the ticket
  # (comments `2dcf691b` item 4, `4d85c99e`, `ad4b23cb` "What this lane did NOT
  # prove" item 2) and so did the AC4 lane's adversarial verifier. This block is
  # the mechanism, not another narrative.
  # ---------------------------------------------------------------------------
  - id: dod-omn-15352-ac4-dev-lane-live-readback
    description: >-
      AC4 (.201 dev-lane `registry.json` reconciled to the actually-running revision), proof_class live-readback.
      The dev lane (compose project `omnibase-infra`) was redeployed 2026-07-30T02:16:34Z via `deploy-runtime.sh
      --execute --cold --force` and `registry.json` moved from `git_sha 7964da738bc2` / `deployed_at 2026-07-29T15:22:05Z`
      (sha256 fcc344836dcbf0cbd65caf74a4f76ceb260dac86d2feddd6efca87f179e45d0e) to `git_sha f420d5fa26f7`
      / `deployed_at 2026-07-30T02:27:37Z` (sha256 3f5bfc1c574898bd7af4e3da45174ec52ea10d542b39e0dfcdbd5dd84475fd1f),
      in three-way agreement with the deploy-source HEAD and the in-container `/app/build-provenance.json
      infra_vcs_ref` on omninode-runtime, omninode-runtime-effects and omnimarket-projection-api. The
      full readback is durable at drift/dod_receipts/OMN-15352/dod-omn-15352-ac4-dev-lane-live-readback/command.yaml.
      SCOPE, STATED HONESTLY - this check_value is a LIVE probe against `.201` over Tailscale MagicDNS
      and CANNOT run in a hosted GitHub runner, which has no route to that host and no `docker` daemon
      holding those containers. It is retained here for audit provenance and is SUPERSEDED by the next
      entry, which carries the hosted-runnable half. Only a party with `.201` access can re-execute this
      one.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: "ssh omni-201-ts 'sha256sum /tmp/ac4/registry.json.before /home/jonah/.omnibase/infra/registry.json
          && grep -E \"git_sha|deployed_at|parent_command\" /home/jonah/.omnibase/infra/registry.json
          && for c in omninode-runtime omninode-runtime-effects omnimarket-projection-api; do printf \"%s
          \" \"$c\"; docker exec \"$c\" grep infra_vcs_ref /app/build-provenance.json; done && git -C
          /data/omninode/omni_home/omnibase_infra rev-parse origin/dev'"
  - id: dod-omn-15352-ac4-live-readback-hosted-seal
    description: >-
      Append-only supersession of `dod-omn-15352-ac4-dev-lane-live-readback` with the HOSTED-RUNNABLE
      half of the same AC4 fact, plus a tamper-evident seal over the committed readback receipt. Four
      checks, and the honest scoping of each matters more than the count: (1) and (2) are ADMISSIBLE under
      the OMN-14505/OMN-15309 evidence predicate - `gh api` reads of a surface this PR does not author,
      pinned to immutable SHAs. (1) asserts GitHub's own compare status for `b6df2d16169a71394a087f268f2be6d72d99551e...f420d5fa26f704c75c3854ba2efc788d36175d13`
      is exactly `ahead`, i.e. the merge commit of omnibase_infra PR #2541 IS an ancestor of the revision
      the dev lane reports deployed. That is the merged-AND-deployed half of AC4 and it goes RED if either
      SHA is wrong: reversing the pair returns `behind` (verified). (2) reads `scripts/deploy-runtime.sh`
      at the DEPLOYED ref and requires `snapshot_latest_image_tags` to be present (live count 5); the
      pre-fix marker string `registry.json may contain stale metadata` returns 0 and exits 1 at the same
      ref, so the pair is falsifiable in both directions. (3) and (4) are DELIBERATELY INADMISSIBLE under
      that same predicate and are labelled as such rather than dressed up: they read `drift/dod_receipts/...`,
      which the predicate DENIES unconditionally as self-referential (`evidence_admissibility.SELF_REFERENTIAL_PATTERNS`),
      and the file is authored by this very PR, so rule (4) INSIDE_OWN_DIFF would refuse it too. They
      are a SEAL, not proof: (3) pins the receipt's exact sha256 so any later byte edit of the readback
      hard-fails, and (4) pins the six load-bearing readback values so a hash-preserving rewrite is impossible.
      Both EXECUTE and both can go RED - they just cannot establish the underlying runtime fact, and no
      green here should be read as doing so. The irreducible residual is that the live `.201` half is
      not hosted-reproducible at all; that is a property of the surface, not a gap in this contract, and
      it is why (1) and (2) exist alongside the seal. This entry does NOT prove AC1/AC2 - those remain
      carried by tests/scripts/test_deploy_runtime_registry_commit_on_success.py in https://github.com/OmniNode-ai/omnibase_infra/pull/2541
      and an independent RED re-execution is still owed.
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-omn-15352-ac4-dev-lane-live-readback
    checks:
      - check_type: command
        check_value: test "$(gh api repos/OmniNode-ai/omnibase_infra/compare/b6df2d16169a71394a087f268f2be6d72d99551e...f420d5fa26f704c75c3854ba2efc788d36175d13
          --jq '.status')" = "ahead"
      - check_type: command
        check_value: gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/deploy-runtime.sh?ref=f420d5fa26f704c75c3854ba2efc788d36175d13'
          --jq '.content' | base64 -d | grep -c 'snapshot_latest_image_tags'
      - check_type: command
        check_value: test "$(sha256sum drift/dod_receipts/OMN-15352/dod-omn-15352-ac4-dev-lane-live-readback/command.yaml
          | cut -d' ' -f1)" = "d8dcc7a34f2e931989fbe430ada79b433858c103df34cb86affdf8fc06bc4c96"
      - check_type: command
        check_value: grep -q 'f420d5fa26f704c75c3854ba2efc788d36175d13' drift/dod_receipts/OMN-15352/dod-omn-15352-ac4-dev-lane-live-readback/command.yaml
          && grep -q '3f5bfc1c574898bd7af4e3da45174ec52ea10d542b39e0dfcdbd5dd84475fd1f' drift/dod_receipts/OMN-15352/dod-omn-15352-ac4-dev-lane-live-readback/command.yaml
          && grep -q 'fcc344836dcbf0cbd65caf74a4f76ceb260dac86d2feddd6efca87f179e45d0e' drift/dod_receipts/OMN-15352/dod-omn-15352-ac4-dev-lane-live-readback/command.yaml
          && grep -q '2026-07-30T02:27:37Z' drift/dod_receipts/OMN-15352/dod-omn-15352-ac4-dev-lane-live-readback/command.yaml
          && grep -q '2026-07-29T15:22:05Z' drift/dod_receipts/OMN-15352/dod-omn-15352-ac4-dev-lane-live-readback/command.yaml
          && grep -q 'bash scripts/deploy-runtime.sh --execute --cold --force' drift/dod_receipts/OMN-15352/dod-omn-15352-ac4-dev-lane-live-readback/command.yaml
  - id: occ-self-bind-pr-5568
    description: >-
      Self-bind for OCC PR #5568, the companion that ships the AC4 durable receipt and the two entries
      above. Required by `occ-preflight / eligibility`, which fails `pr_ticket_mismatch` unless some PASS
      receipt for every cited ticket binds to this PR or one of its commit SHAs. The check asserts the
      PR's own file list literally contains `contracts/OMN-15352.yaml`, which is falsifiable (a companion
      that did not touch this contract fails it) rather than a bare existence probe. The producer is BUFFERED
      into a variable before `grep -q` rather than piped straight in - OMN-15411 Rule E: an unbounded
      producer piped into an early-exiting `grep -q` is killed by SIGPIPE and, under the dod_verify runner's
      `bash -o pipefail`, 141 becomes the pipeline's exit status, i.e. a false RED on evidence that is
      present. Also satisfies OMN-15382 Rule B by pinning 5568 literally instead of using the bare ${PR_NUMBER}
      placeholder, which would silently re-check whatever PR the runner happened to be evaluating.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: files="$(gh api repos/OmniNode-ai/onex_change_control/pulls/5568/files --paginate
          --jq '.[].filename')" && printf '%s\n' "$files" | grep -qx 'contracts/OMN-15352.yaml'
