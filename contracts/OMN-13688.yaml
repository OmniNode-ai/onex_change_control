---
schema_version: "1.0.0"
ticket_id: "OMN-13688"
title: "Register pipeline_fill ONEX skill mapping and sync infra node migrations"
summary: >-
  omnibase_infra PR #2155 adds the missing pipeline_fill -> node_pipeline_fill CLI skill mapping, extends
  CLI skill mapping unit coverage, and vendors the drifted node_projection_baselines_quality and node_projection_baselines_roi
  forward migrations under the infra node migration tree. The PR is bound to head commit 6ca9d22f276b614ecc62f393c96dc6a415878df0
  and is mergeable with code/CI gates green; this OCC contract supplies the central Evidence-Source/Evidence-Ticket
  proof required by the receipt and OCC eligibility gates.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #2155 code and CI checks are green except OCC evidence gates."
    command: >-
      gh pr view 2155 --repo OmniNode-ai/omnibase_infra --json headRefOid,mergeable,statusCheckRollup
  - kind: "tests"
    description: "Focused CLI skill mapping unit test suite passed locally for PR #2155."
    command: "uv run pytest tests/unit/cli/test_cli_skill.py -q"
  - kind: "manual"
    description: "Deploy assessment documents that this evidence PR performs no live deploy, restart,
      or .201 mutation."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13688/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2155"
    description: >-
      omnibase_infra PR #2155 is bound to OMN-13688 at head commit 6ca9d22f276b614ecc62f393c96dc6a415878df0.
      The PR modifies the CLI skill mapping, CLI unit expectations, and two infra-vendored node migration
      SQL files, and current GitHub state shows code/CI checks green with only OCC/receipt evidence gates
      failing before this contract lands.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13688/dod-infra-pr-2155/command.yaml && grep
          -q '^pr_number: 2155$' drift/dod_receipts/OMN-13688/dod-infra-pr-2155/command.yaml && grep -q
          '^commit_sha: "6ca9d22f276b614ecc62f393c96dc6a415878df0"$' drift/dod_receipts/OMN-13688/dod-infra-pr-2155/command.yaml
  - id: "dod-local-validation"
    description: >-
      Focused local validation for the changed CLI skill mapping test surface passed: uv run pytest tests/unit/cli/test_cli_skill.py
      -q reported 24 passed and 5 skipped.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13688/dod-local-validation/command.yaml && grep
          -q '24 passed, 5 skipped' drift/dod_receipts/OMN-13688/dod-local-validation/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Static deploy/contract assessment for PR #2155. The change is merge eligibility evidence for a CLI
      skill mapping addition plus infra-vendored forward migration files. This OCC evidence PR does not
      perform or claim a live deploy, runtime restart, topic mutation, or .201 mutation; normal downstream
      deploy validation remains a post-merge lane.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13688/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-13688/dod-deploy-assessment/command.yaml &&
          grep -qi 'runtime proof is not claimed' drift/dod_receipts/OMN-13688/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC eligibility receipt for this onex_change_control PR. This PR carries the OMN-13688
      central contract and PASS receipts so omnibase_infra PR #2155 can reference the OCC PR through Evidence-Source
      and satisfy validator_occ_merge_eligibility.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13688/dod-occ-self/command.yaml
