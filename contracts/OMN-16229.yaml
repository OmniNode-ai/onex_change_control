---
schema_version: "1.0.0"
ticket_id: "OMN-16229"
title: "OCC companion for OMN-16229"
summary: >
  Hand-authored OCC Evidence-Source companion for OmniNode-ai/omnibase_infra#2794 (expiring-ignore policy
  for fix-unavailable CVEs in the Trivy image gate, companion to OMN-16228, both born from the 2026-08-18
  sqlparse/Trivy incident, OMN-16170).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2794 CI checks green"
    command: "gh pr checks 2794 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2794"
    description: "PR #2794 on OmniNode-ai/omnibase_infra — Evidence-Source binding."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2794 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName"
  - id: "dod-omnibase_infra-pr-2794-strict-gate"
    description: "PR #2794 on OmniNode-ai/omnibase_infra — content-bound probe confirming the new trivyignore-expiry-check
      job is registered in scripts/ci/ci_summary_gate.py's STRICT_GATE_JOBS at the PR's head ref (OMN-15309
      admissibility: reads product content at a pinned ref, not PR/issue metadata; OMN-15411 Rule E SIGPIPE-safe:
      producer output is buffered into a shell variable before the grep -q consumer reads it)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "body=\"$(gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/ci/ci_summary_gate.py?ref=7c3d5bbb7281f8b248b07671c4c4594501d27ff8
          --jq .content | base64 -d)\" && printf '%s' \"$body\" | grep -qF 'Trivyignore Expiry Check (OMN-16229)'"
  - id: "occ-self-bind-pr-6691"
    description: "Binds OMN-16229 to OCC companion PR #6691 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6691 --repo OmniNode-ai/onex_change_control --json number,state"
