---
schema_version: "1.0.0"
ticket_id: "OMN-13566"
title: "Wave 4 — env-read enforcement ratchet: canonical no-new-os.environ validator + REQUIRED CI on
  omnibase_core"
summary: >-
  Lands the canonical AST-based no-new-os-environ validator in omnibase_core (omnibase_core.validators.no_new_os_environ)
  that blocks ALL os.environ[KEY], os.environ.get(KEY), and os.getenv(KEY) reads outside an explicit KEEP_ALLOWLIST
  of structurally required bootstrap/path/CI variables. Generalises omnibase_infra/scripts/check-env-reads.sh
  (OMN-11069/11186) and the regex validate_no_new_env_vars.py into one canonical packageable implementation
  with 27 unit tests including a deliberate red-test corpus. Exports a no-new-os-environ pre-commit hook
  from .pre-commit-hooks.yaml for fleet consumption. Wires a no-new-os-environ CI job into the quality-gate
  (feeds the CI Summary rollup) so any new os.environ read in src/ turns the required check red. Registers
  the validator in architecture-handshakes/validator-requirements.yaml as required + Model-B validator_jobs.
  The deliberate red-test corpus (test_deliberate_violation_corpus) is the DoD proof that CI catches real
  violations. Product PR is omnibase_core #1339.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >-
      27 unit tests in tests/unit/validators/test_no_new_os_environ.py pass, including test_deliberate_violation_corpus
      (plants two known-bad snippets, asserts both flagged).
    command: "uv run pytest tests/unit/validators/test_no_new_os_environ.py -q"
  - kind: "ci"
    description: >-
      src/ scan exits 0 (no violations in the existing codebase).
    command: "uv run python -m omnibase_core.validators.no_new_os_environ --all --src src/"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-evidence-source"
    description: >-
      This OCC contract is the Receipt-Gate Evidence-Source for OMN-13566; the omnibase_core product PR
      #1339 cites Evidence-Source: OCC#<this-pr>.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-13566.yaml"
  - id: "dod-core-pr-1339"
    description: >-
      omnibase_core PR #1339 carries the no-new-os-environ validator implementation, 27 unit tests, pre-commit
      hook, CI job wired into quality-gate, and validator-requirements.yaml update. Deliberate red-test
      corpus proves CI catches real violations. PR opened in state OPEN against dev.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13566/dod-core-pr-1339/command.yaml
          && grep -q '^pr_number: 1339$' drift/dod_receipts/OMN-13566/dod-core-pr-1339/command.yaml"
