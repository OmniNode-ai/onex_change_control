---
schema_version: "1.0.0"
ticket_id: "OMN-13904"
title: "gh pr edit Projects-classic trap — mechanical patch_pr_body/patch_pr_title helpers"
summary: >-
  OCC evidence for omniclaude PR #1851, which fixes the `gh pr edit` Projects-classic trap at the skill/tooling
  layer (2026-07-03 parallel-workstream audit item 22, same defect class as the OMN-8653/OMN-5635 merge-queue
  silent no-op). The `patch_pr_body()` helper that dispatch_worker/prompt.md referenced was a phantom
  callable; PR #1851 makes it real in _lib/pr-safety/helpers.md as patch_pr_body()/patch_pr_title() —
  REST PATCH /repos/{owner}/{repo}/pulls/{n} via gh api plus MANDATORY read-back verification that hard-fails
  on mismatch, so a silent no-op cannot survive. The dead [[reference_gh_pr_edit_projects_classic]] tribal-memory
  pointer is removed from dispatch_worker/prompt.md. Raw `gh pr edit` in non-allowlisted skills stays
  mechanically flagged by the existing "Enforce PR mutation command bans" step in omni-standards-compliance.yml.
  Failure mode pinned live 2026-07-03: all probed OmniNode-ai repos have has_projects true, the standard
  token lacks read:project; on gh 2.89.0 a plain body edit applied (exposure is version-dependent), while
  omnimarket adapter_occ_autobind.py friction #7 documents live silent no-ops — REST PATCH + read-back
  is the canonical path either way. Markdown-only tooling change; no runtime change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-self"
    description: "This onex_change_control PR carries the net-new OMN-13904 contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13904/dod-occ-self/command.yaml && grep -q '^ticket_id:
          "OMN-13904"$' drift/dod_receipts/OMN-13904/dod-occ-self/command.yaml
  - id: "dod-omniclaude-pr-1851"
    description: "omniclaude PR #1851 ships the real patch_pr_body/patch_pr_title REST-PATCH helpers and
      removes the tribal-memory pointer."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13904/dod-omniclaude-pr-1851/command.yaml &&
          grep -q '^pr_number: 1851$' drift/dod_receipts/OMN-13904/dod-omniclaude-pr-1851/command.yaml
