---
schema_version: "1.0.0"
ticket_id: "OMN-10730"
title: "Refactor handler_projection_overnight to inject sibling handler (DI)"
summary: >
  Remove handler-constructs-handler pattern: HandlerProjectionOvernightPhaseEnd and HandlerProjectionOvernightSessionComplete
  both constructed HandlerProjectionOvernightSessionStart() internally. Both now accept session_start_handler
  via constructor. contract.yaml updated with sub_handler_dependencies. Tests updated to pass the handler
  explicitly.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain"
    description: "Golden-chain projection_overnight tests pass (13+ tests)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10730/dod-golden-chain/command.yaml\""
  - id: "dod-ownership-validator"
    description: "Contract ownership validator passes for HandlerProjectionOvernightSessionComplete."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10730/dod-ownership-validator/command.yaml\""
  - id: "dod-mypy"
    description: "mypy strict passes on src/omnimarket/ (1293 source files)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10730/dod-mypy/command.yaml\""
  - id: "dod-ruff"
    description: "ruff check passes on changed files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10730/dod-ruff/command.yaml\""
  - id: "dod-deploy"
    description: "No live deploy or runtime restart performed; post-merge deploy plan documented."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10730/dod-deploy/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10730/dod-occ-pr/command.yaml\""
