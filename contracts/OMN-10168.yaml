---
schema_version: "1.0.0"
ticket_id: OMN-10168
summary: "Add strict orchestrator dispatcher coverage gate for start-command aliases"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - protocols
evidence_requirements:
  - kind: tests
    description: "Focused unit and integration tests prove strict dispatcher coverage behavior"
    command: "cd ../omnibase_infra && uv run pytest tests/unit/runtime/auto_wiring/test_orchestrator_dispatcher_coverage.py
      tests/integration/test_orchestrator_dispatcher_coverage_integration.py tests/unit/runtime/auto_wiring/test_wiring.py
      tests/unit/runtime/auto_wiring/test_handler_wiring_event_type_alias.py -q"
  - kind: ci
    description: "omnibase_infra PR checks pass for OMN-10168"
    command: "gh pr checks 1440 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Strict dispatcher coverage tests pass for the auto-wiring boundary"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10168/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-10168$' drift/dod_receipts/OMN-10168/dod-001/command.yaml && grep -q '53
          passed' drift/dod_receipts/OMN-10168/dod-001/command.yaml"
  - id: dod-002
    description: "SEAM-4 parser audit found no missing orchestrator start dispatchers"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10168/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-10168$' drift/dod_receipts/OMN-10168/dod-002/command.yaml && grep -q 'missing_dispatchers:
          \\[\\]' drift/dod_receipts/OMN-10168/dod-002/command.yaml"
