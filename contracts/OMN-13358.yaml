---
schema_version: "1.0.0"
ticket_id: "OMN-13358"
title: "Remove dead delegation-bridge block from user-prompt-submit.sh"
summary: >-
  omniclaude PR #1785 removes the stale UserPromptSubmit delegation bridge block that attempted to call
  plugins/onex/skills/delegate/_lib/handler_delegate_skill.py after that adapter had already been removed.
  The remaining block could only emit a warning and skip Kafka publish; deleting it aligns hook behavior
  and documentation with the current architecture where delegation runs by explicit /onex:delegate invocation
  through the market-owned node_delegate_skill_orchestrator. Documentation updates mark the historical
  bridge flow as no longer wired.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1785"
    description: >-
      Per-code-PR binding receipt for omniclaude PR #1785 at head 0ac35a7997ee4303a966fe76859db6a6f3e10053.
      The PR deletes the dead delegation-rule/bridge block from plugins/onex/hooks/scripts/user-prompt-submit.sh
      and updates delegation architecture docs to identify the bridge flow as historical.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13358/dod-omniclaude-pr-1785/command.yaml
  - id: "dod-deploy-no-runtime-impact"
    description: >-
      Deploy-path assessment: this is a hook-script dead-code removal plus documentation alignment. It
      adds no runtime node, changes no bus contract, publishes no topic, adds no migration, alters no
      image build input beyond the omniclaude hook script, and performs no live lane deploy/restart or
      .201 mutation. Runtime adoption occurs through the normal omniclaude artifact update path.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13358/dod-deploy-no-runtime-impact/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13358 contract and receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13358/dod-occ-pr-self/command.yaml
