---
schema_version: "1.0.0"
ticket_id: "OMN-14984"
title: "Read lane identity from contract-rendered runtime policy"
description: >-
  Central OCC evidence for omnibase_infra PR #2406. The product PR migrates the unblocked compose project
  and runtime main-port lane identity values from duplicated shell literals to the already-sourced contract-rendered
  docker/runtime-policy.env file, while leaving the resolver leg explicitly open.
dod_evidence:
  - id: "dod-omnibase-infra-pr-2406-head"
    description: "Binds omnibase_infra PR #2406 to exact reviewed head 841160c999f7bd6f6a896c8127b22e0f0c9e1cf7."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2406 --repo OmniNode-ai/omnibase_infra --json headRefOid --jq .headRefOid
          | grep -Fx 841160c999f7bd6f6a896c8127b22e0f0c9e1cf7"
  - id: "dod-omnibase-infra-pr-2406-scope"
    description: "PR #2406 scope is limited to the lane-refresh scripts and focused unit tests."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2406 --repo OmniNode-ai/omnibase_infra --name-only | grep -E '^(scripts/runtime_build/refresh_dev_lane\\.sh|scripts/runtime_build/refresh_stability_lane\\.sh|tests/unit/scripts/test_lane_refresh_contract_var_dedup\\.py|tests/unit/scripts/test_refresh_lane_probe_context\\.py)$'"
  - id: "dod-omnibase-infra-pr-2406-local-verification"
    description: "PR #2406 body records shell checks, focused pytest coverage, and local formatting/type
      gates."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2406 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep
          -F '820 passed' && gh pr view 2406 --repo OmniNode-ai/omnibase_infra --json body --jq .body
          | grep -F 'shellcheck' && gh pr view 2406 --repo OmniNode-ai/omnibase_infra --json body --jq
          .body | grep -F 'uv run pre-commit run --files <changed files>'"
  - id: "dod-omnibase-infra-pr-2406-close-discipline"
    description: >-
      PR #2406 references OMN-14984 without closing it because the PR body explicitly states the terminal
      resolver leg remains open.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2406 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep
          -F 'Refs OMN-14984' && ! gh pr view 2406 --repo OmniNode-ai/omnibase_infra --json body --jq
          .body | grep -F 'Closes OMN-14984'"
  - id: "occ-self-bind-pr-4652-omn14984"
    description: "OCC companion PR #4652 self-bind for OMN-14984."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4652 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
