---
schema_version: "1.0.0"
ticket_id: "OMN-13243"
title: "fix(OMN-13243): split omniintelligence subscription startup"
summary: >
  Extracts PluginIntelligence.start_consumers topic filtering, concurrent subscription, cancellation,
  and partial-start rollback into focused helpers in omniintelligence. Adds unit coverage for missing-handler
  topic skips and rollback after partial subscription failure. Dispatch contracts and subscription behavior
  are preserved.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Runtime dispatch unit tests pass for the changed plugin behavior."
    command: "uv run pytest tests/unit/runtime/test_plugin_dispatch.py -q"
  - kind: "ci"
    description: "Changed runtime plugin file passes strict type checking and pre-commit."
    command: "uv run mypy src/omniintelligence/runtime/plugin.py --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-pr-716"
    description: "omniintelligence PR #716 contains the scoped runtime plugin extraction and tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13243/dod-omniintelligence-pr-716/command.yaml"
  - id: "dod-focused-verification"
    description: "Focused ruff, pytest, mypy, diff, and pre-commit checks passed before push."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13243/dod-focused-verification/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Deploy assessment: no live deploy is required. The change is a behavior-preserving runtime plugin
      refactor plus unit tests; it changes no broker topic, Docker image, database migration, or service
      deployment.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13243/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy required' drift/dod_receipts/OMN-13243/dod-deploy-assessment/command.yaml"
  - id: "pr-occ-2768"
    description: "OCC PR #2768 carries the central OMN-13243 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13243/pr-occ-2768/command.yaml &&
          grep -q '^pr_number: 2768$' drift/dod_receipts/OMN-13243/pr-occ-2768/command.yaml"
