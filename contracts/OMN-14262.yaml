---
schema_version: "1.0.0"
ticket_id: "OMN-14262"
title: "fix(OMN-14262): force local-path reinstall for compat/occ/market in workspace build"
summary: >-
  omnibase_infra product PR #2248 (branch jonah/omn-14262-dockerfile-compat-forced-reinstall, head e6c65c50663a3c0e4c63938acf0d7925438ac7c6)
  updates docker/Dockerfile.runtime so omnibase_compat, onex_change_control, and omnimarket workspace
  installs mirror the existing omnibase-core forced uninstall plus no-cache reinstall-package pattern.
  This makes uv record file:// direct_url provenance even when the staged dev-tree package version equals
  the lockfile-installed version, closing the same-version local-path provenance false-fail seen during
  OMN-14208 readback.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      Product fix verification at head e6c65c50663a3c0e4c63938acf0d7925438ac7c6: docker/Dockerfile.runtime
      now uses forced local-path reinstall for omnibase_compat, onex_change_control, and omnimarket, matching
      the existing omnibase-core provenance pattern. The live OMN-14208 stability-test lane proved the
      same forced-reinstall shape records file:// direct_url provenance and lets the runtime image build
      pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14262/dod-001/command.yaml && grep -q 'e6c65c50663a3c0e4c63938acf0d7925438ac7c6'
          drift/dod_receipts/OMN-14262/dod-001/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-surface assessment: this is runtime-image build logic only. No live deploy, restart, .201
      mutation, topic mutation, or migration was performed by this OCC companion; product PR merge requires
      normal CI/build gates and downstream deploy validation instead of a bypass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14262/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-14262/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14262 contract and
      PASS receipts consumed by omnibase_infra#2248 through its Evidence-Source line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14262/dod-occ-self/command.yaml
