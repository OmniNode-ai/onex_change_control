---
schema_version: "1.0.0"
ticket_id: "OMN-17527"
title: "Autobind OCC evidence for OMN-17527"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3159.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3159 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3159 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: "uv run pytest scripts/tests/test_keycloak_desired_clients_contract.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3159"
    description: "PR #3159 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/tests/test_keycloak_desired_clients_contract.py?ref=ce626274d478498751bdcb5727a2c1c6ea04bccf --jq '.content' | base64 -d | grep -c 'def test_onex_customer_is_a_dedicated_public_customer_client'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3159/files --paginate --jq '.[].filename' | grep -cFx 'docker/keycloak/desired-clients.json'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3159 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest scripts/tests/test_keycloak_desired_clients_contract.py -q"
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-8127"
    description: "Binds OMN-17527 to OCC companion PR #8127 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8127 --repo OmniNode-ai/onex_change_control --json number,state"
