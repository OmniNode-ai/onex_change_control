---
schema_version: "1.0.0"
ticket_id: "OMN-12086"
title: "Add cost/savings/speedup fields to swarm dispatch completed payload for B5-T4"
summary: >
  Central OCC evidence for omnimarket PR #886 (B5-T4). Adds cost accounting fields to _build_completed_payload
  in HandlerSwarmDispatchOrchestrator: total_cost_usd=0.0 (local inference), cloud_equivalent_cost_usd
  (token-based estimate), savings_usd, parallelism_speedup_ratio (sum subtask latency / wall latency),
  and dispatch_wall_latency_ms. Enables the B5 demo aggregate display.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-886"
    description: >
      omnimarket PR #886 adds cost/savings/speedup fields to the swarm orchestrator completed payload.
      4 new tests in TestCostFieldsInCompletedPayload verify total_cost=0.0, cloud_equivalent>0, speedup>1
      when parallel, wall latency present.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy: swarm orchestrator completed payload now includes cost fields; active
          on next omnimarket runtime image rebuild'"
  - id: "dod-deploy-assessment"
    description: >
      No immediate live deploy required. The cost fields are additive to the completed payload — existing
      consumers ignore unknown fields (Pydantic extra=allow or dict consumers). Will be visible in swarm_runs
      projection after next omnimarket runtime image rebuild on stability-test lane.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy: use deploy-runtime.sh on stability-test lane (.201:18085) after PR
          #886 merges to activate B5-T4 cost display'"
