---
schema_version: "1.0.0"
ticket_id: "OMN-13584"
title: "RH-2: node_repo_health_repair_effect — emit durable repo-baseline repair task (EFFECT)"
summary: >-
  Central evidence for omnimarket PR #1415, which adds the node_repo_health_repair_effect EFFECT node
  for the merge-sweep repo-health repair lane. The node receives a REPO_BASELINE classification and emits
  a durable Linear repair task (idempotent on content key) under parent epic OMN-13316. This contract
  supplies the Receipt-Gate and deploy-gate evidence cited by the product PR.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1415"
    description: >-
      omnimarket PR #1415 carries node_repo_health_repair_effect: contract.yaml, metadata.yaml, handler
      (LinearRepairClientProtocol + HandlerRepoHealthRepairEffect), input/output models, and 8 unit tests
      covering idempotency, payload assertion, dry_run, content-key determinism, no-literal-api-key, and
      missing-api-key-raises.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13584/dod-omnimarket-pr-1415/command.yaml"
  - id: "dod-occ-evidence-source"
    description: >-
      This OCC contract is the central Evidence-Source for OMN-13584 and is cited by omnimarket PR #1415.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-13584.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: omnimarket PR #1415 adds a runtime EFFECT node. The PR scope is code, tests,
      and contract evidence only; post-merge runtime rollout follows the normal deployment lane.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13584/dod-deploy-assessment/command.yaml"
