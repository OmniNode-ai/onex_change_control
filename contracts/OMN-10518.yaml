---
schema_version: "1.0.0"
ticket_id: "OMN-10518"
title: "Overnight proof push and runtime build hardening"
summary: "Bind the conference/YC overnight proof work to durable evidence: stability runtime healthy proof
  and a runtime image build fix that avoids recursive ownership walks."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #1495 removes the recursive Dockerfile.runtime app chown and preserves
      virtualenv ownership at COPY time."
    command: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10518/dod-001/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Runtime build ownership fix and live stability runtime proof pass for omnibase_infra
      PR #1495."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10518/dod-001/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-001\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10518/dod-001/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10518\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10518/dod-001/command.yaml\"
          && grep -q '^commit_sha: \"a263fbf7982ae37b9ff65c6e3e4a77cb38e46197\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10518/dod-001/command.yaml\"
          && grep -q '^pr_number: 1495$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10518/dod-001/command.yaml\""
