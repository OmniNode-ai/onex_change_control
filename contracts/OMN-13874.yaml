---
schema_version: '1.0.0'
ticket_id: OMN-13874
title: 'omnimemory dev merges with ZERO code validation — CI Summary is a real gate but not in required_status_checks'
summary: 'Live gh api probe confirms CI Summary is now a required status check on omnimemory dev and main'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: Live branch-protection probe against omnimemory dev/main required contexts
    command: "gh api repos/OmniNode-ai/omnimemory/branches/dev/protection/required_status_checks --jq
      '.contexts' | grep -q 'CI Summary'"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: CI Summary is a required status check on omnimemory dev (live gh api probe)
    source: generated
    checks:
      - check_type: command
        check_value: "gh api repos/OmniNode-ai/omnimemory/branches/dev/protection/required_status_checks
          --jq '.contexts' | grep -q 'CI Summary'"
  - id: dod-002
    description: CI Summary is a required status check on omnimemory main (live gh api probe)
    source: generated
    checks:
      - check_type: command
        check_value: "gh api repos/OmniNode-ai/omnimemory/branches/main/protection/required_status_checks
          --jq '.contexts' | grep -q 'CI Summary'"
  - id: dod-003
    description: Overnight O5a evidence doc captures the live probe + reconciliation of the handoff's
      "evidence on ticket" claim
    source: generated
    checks:
      - check_type: command
        cwd: "${OMNI_HOME}"
        check_value: "grep -F 'Both dev and main carry' docs/evidence/M5-capstone-dogfood/2026-07-03-overnight-o5-board-hygiene.md"
  - id: dod-occ-self
    description: This onex_change_control PR carries the central OMN-13874 contract and PASS receipts
      backfilling the sole git-durable OCC evidence for the Done ops-ticket.
    source: generated
    checks:
      - check_type: command
        check_value: "ls drift/dod_receipts/OMN-13874/ | wc -l"
