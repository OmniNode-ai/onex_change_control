---
schema_version: '1.0.0'
ticket_id: OMN-13874
title: 'omnimemory dev merges with ZERO code validation — CI Summary is a real gate but not in required_status_checks'
summary: 'Live gh api probe confirms CI Summary is now a required status check on omnimemory dev and main'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: Live branch-protection probe against omnimemory dev/main required contexts
    command: "gh api repos/OmniNode-ai/omnimemory/branches/dev/protection/required_status_checks --jq
      '.contexts' | grep -q 'CI Summary'"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: CI Summary is a required status check on omnimemory dev (live gh api probe, captured
      in the dod-001 receipt)
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13874/dod-001/command.yaml\""
  - id: dod-002
    description: CI Summary is a required status check on omnimemory main (live gh api probe, captured
      in the dod-002 receipt)
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13874/dod-002/command.yaml\""
  - id: dod-003
    description: Overnight O5a evidence doc captures the live probe + reconciliation of the handoff's
      "evidence on ticket" claim (captured in the dod-003 receipt)
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13874/dod-003/command.yaml\""
  - id: dod-occ-self
    description: This onex_change_control PR carries the central OMN-13874 contract and PASS receipts
      backfilling the sole git-durable OCC evidence for the Done ops-ticket.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13874/dod-occ-self/command.yaml\""
