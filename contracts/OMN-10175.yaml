---
schema_version: '1.0.0'
ticket_id: OMN-10175
title: "Create substrate integration master map and advisory freshness check"
summary: "Substrate stabilization Wave 8 — integration master map + advisory CI freshness check"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: docs
    description: Integration map covers all 10+ seams with required columns
    command: "test -f docs/integration/2026-04-27-substrate-integration-map.md"
  - kind: ci
    description: Advisory GHA workflow wires the freshness check
    command: "test -f .github/workflows/integration_map_freshness.yml"
  - kind: tests
    description: Unit tests pass for the advisory freshness checker
    command: "uv run pytest onex_change_control/tests/unit/scripts/test_check_integration_map_freshness.py
      -v -m unit"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Integration map file exists at required path
    source: generated
    checks:
      - check_type: file_exists
        check_value: docs/integration/2026-04-27-substrate-integration-map.md
  - id: dod-002
    description: Integration map contains all 10 required seams (SEAM-1 through SEAM-10)
    source: generated
    checks:
      - check_type: command
        check_value: "grep -c 'SEAM-[0-9]' /Users/jonah/Code/omni_home/docs/integration/2026-04-27-substrate-integration-map.md
          | awk '{exit ($1 >= 10) ? 0 : 1}'"
  - id: dod-003
    description: Integration map contains How To Add A New Interface section
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q 'How To Add A New Interface' /Users/jonah/Code/omni_home/docs/integration/2026-04-27-substrate-integration-map.md"
  - id: dod-004
    description: Advisory GHA workflow present at required path
    source: generated
    checks:
      - check_type: file_exists
        check_value: .github/workflows/integration_map_freshness.yml
  - id: dod-005
    description: Advisory workflow uses --advisory flag (warn-only mode)
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q -- '--advisory' /Users/jonah/Code/omni_home/.github/workflows/integration_map_freshness.yml"
  - id: dod-006
    description: Freshness checker source exists in onex_change_control
    source: generated
    checks:
      - check_type: file_exists
        check_value: onex_change_control/src/onex_change_control/scripts/check_integration_map_freshness.py
  - id: dod-007
    description: Unit tests for freshness checker exist
    source: generated
    checks:
      - check_type: file_exists
        check_value: onex_change_control/tests/unit/scripts/test_check_integration_map_freshness.py
  - id: dod-008
    description: Wave 3 SEAM-2 ADR present (skill liveness validator home)
    source: generated
    checks:
      - check_type: file_exists
        check_value: docs/decisions/adr-2026-04-28-skill-liveness-validator-home.md
  - id: dod-009
    description: Wave 3 SEAM-9 ADR present (dispatch lifecycle canonical)
    source: generated
    checks:
      - check_type: file_exists
        check_value: docs/decisions/adr-2026-04-28-dispatch-lifecycle-canonical.md
  - id: dod-010
    description: Wave 3 SEAM-10 ADR present (skills canonical plan)
    source: generated
    checks:
      - check_type: file_exists
        check_value: docs/decisions/adr-2026-04-28-skills-canonical-plan.md
