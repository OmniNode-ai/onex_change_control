---
schema_version: "1.0.0"
ticket_id: "OMN-13825"
title: "fix(OMN-13825): add dep_health projection handle shim"
summary: >-
  omnimarket PR #1559 fixes the dep_health projection consume-then-drop defect by adding the runtime-required
  handle(input_data) entrypoint to HandlerProjectionDepHealth. The shim consumes the injected DatabaseAdapter
  from input_data['_db'], builds ModelDepHealthSweepCompletedEvent, delegates to project(), and returns
  the serialized projection result. The PR adds focused golden-chain projection tests and records local
  ruff, mypy, pre-commit, and targeted test evidence. Runtime rebuild/deploy is explicitly outside this
  OCC evidence PR and remains operator-gated.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1559"
    description: >-
      Consuming-PR binding: omnimarket PR #1559 carries the OMN-13825 dep_health projection handle() shim.
      The PR body records the live root cause, the local focused projection tests, and clean ruff/mypy/pre-commit
      evidence. Real probe: gh pr view 1559 --repo OmniNode-ai/omnimarket --json number,state,headRefOid,headRefName,title,files.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13825/dod-omnimarket-pr-1559/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying this OMN-13825 contract and the dod-omnimarket-pr-1559
      receipt consumed by omnimarket PR #1559 through Receipt Gate and OCC eligibility.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13825/dod-occ-self/command.yaml"
