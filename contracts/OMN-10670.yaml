---
schema_version: "1.0.0"
ticket_id: "OMN-10670"
title: "Bind onex-prod manifest stack evidence"
summary: >
  Bind omninode_infra PR #390 to central OCC evidence for the onex-prod Kubernetes manifest stack and
  the review remediation that keeps runtime configuration deterministic.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-390-head"
    description: "omninode_infra PR #390 carries the OMN-10670 onex-prod manifest stack."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10670/dod-pr-390-head/command.yaml\""
  - id: "dod-manifest-validation"
    description: "onex-prod manifest YAML parses and the kustomize overlay renders."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10670/dod-manifest-validation/command.yaml\""
