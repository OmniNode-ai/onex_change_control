---
schema_version: "1.0.0"
ticket_id: "OMN-11381"
title: "Progressive agent demo with registry growth snapshots"
summary: >-
  onex-self-extending-agent PR #73 adds progressive agent demo support for running multiple tasks through
  the self-extending agent loop, recording registry growth snapshots, and printing trend output that highlights
  retrieval-augmented retry context and example reuse.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: tests
    description: "Focused progressive agent demo unit tests pass locally after rebase conflict repair."
    command: "uv run pytest tests/unit/test_agent_demo.py -q"
  - kind: manual
    description: "Touched progressive demo entrypoint and unit tests pass ruff."
    command: "uv run ruff check src/__main__.py tests/unit/test_agent_demo.py"
  - kind: manual
    description: "No live deploy required; local CLI/demo behavior only."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11381/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-73"
    description: "onex-self-extending-agent PR #73 carries the rebased progressive agent demo implementation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11381/dod-product-pr-73/command.yaml"
          && grep -q '^pr_number: 73$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11381/dod-product-pr-73/command.yaml"
          && grep -q '^commit_sha: "4e7ab842119d22637dbcc2e5664f7f83aab19f90"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11381/dod-product-pr-73/command.yaml"
  - id: "dod-local-proof"
    description: "Focused local progressive-demo tests, ruff checks, and format checks pass after the
      rebase."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11381/dod-local-proof/command.yaml"
          && grep -q '7 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11381/dod-local-proof/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11381/dod-local-proof/command.yaml"
          && grep -q '2 files already formatted' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11381/dod-local-proof/command.yaml"
  - id: "dod-deploy"
    description: "No live deploy required; progressive agent demo is a local CLI/demo behavior."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11381/dod-deploy/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11381 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11381/dod-occ-self/command.yaml"
