---
schema_version: "1.0.0"
ticket_id: "OMN-12712"
title: "Declare .201 reasoner served-model routing context"
summary: >-
  Central evidence for omnimarket PR #1054. The live .201 local-reasoner backend serves `Qwen3.6-27B-MTP-IQ4_XS.gguf`;
  routing tiers now declare that served model id directly with contract-owned max_context_tokens so SEA
  and delegation resolve token bounds from the shared routing contract instead of client fallbacks.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-routing-tests"
    description: "Focused omnimarket routing-tier and delegation tests passed locally."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12712/dod-focused-routing-tests/command.yaml"
          && grep -q '16 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12712/dod-focused-routing-tests/command.yaml"
          && grep -q '32 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12712/dod-focused-routing-tests/command.yaml"
  - id: "dod-sea-runtime-proof"
    description: "SEA local agent run completed over the .201 bus after the routing-tier hotpatch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12712/dod-sea-runtime-proof/command.yaml"
          && grep -q '69148f7f-2ff8-4358-8e2a-1409ebd404a3' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12712/dod-sea-runtime-proof/command.yaml"
          && grep -q 'is_healthy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12712/dod-sea-runtime-proof/command.yaml"
  - id: "dod-omnimarket-pr-1054"
    description: "omnimarket PR #1054 carries the routing-tier served-model alias."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12712/dod-omnimarket-pr-1054/command.yaml"
          && grep -q '^pr_number: 1054$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12712/dod-omnimarket-pr-1054/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR #2197 carries the OMN-12712 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12712/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2197$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12712/dod-occ-pr/command.yaml"
