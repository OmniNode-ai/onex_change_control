---
schema_version: "1.0.0"
ticket_id: "OMN-12480"
title: "Make SEA stability Kafka transport overlay-owned"
summary: >-
  Remove process-env Kafka transport authority from the SEA stability live path by passing the validated
  delegation overlay into overlay-aware bus factories and constructing Kafka transports from typed overlay-derived
  config instead of `KAFKA_BOOTSTRAP_SERVERS` / `KAFKA_API_VERSION`.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-227"
    description: >-
      onex-self-extending-agent PR #227 carries the OMN-12480 implementation that makes SEA stability
      inference and generation-event Kafka transports resolve from the validated overlay instead of process env.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12480/dod-sea-pr-227/command.yaml"
  - id: "dod-focused-tests"
    description: >-
      Focused SEA stability bus, generation-event publisher, sanctioned-live readiness, lint, format, and strict
      mypy checks pass for the OMN-12480 implementation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12480/dod-focused-tests/command.yaml"
  - id: "dod-no-env-overlay-proof"
    description: >-
      With `KAFKA_BOOTSTRAP_SERVERS` and `KAFKA_API_VERSION` unset, SEA loads the stability overlay and resolves
      the broker/API version from `src/contracts/delegation_bootstrap_overlay.stability-test.yaml`.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12480/dod-no-env-overlay-proof/command.yaml"
          && grep -q 'broker=100.109.203.94:39092' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12480/dod-no-env-overlay-proof/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      OMN-12480 changes runtime transport construction. No live `.201` deployment or restart was performed during
      PR validation; hosted runtime validation remains a post-merge deploy step.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12480/dod-deploy-assessment/command.yaml"
          && grep -q 'No live deployment or restart was performed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12480/dod-deploy-assessment/command.yaml"
