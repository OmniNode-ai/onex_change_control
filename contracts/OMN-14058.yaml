---
schema_version: "1.0.0"
ticket_id: "OMN-14058"
title: "feat(OMN-14058): thread tenant identity through delegation projections"
summary: >-
  Central OCC evidence for the beta-blocking interim tenant-id fix. omnibase_core PR #1404 adds optional
  `tenant_id` to the canonical delegation wire request/result DTOs. omnimarket PR #1648 reads `ONEX_TENANT_ID`
  once at request acceptance, carries the tenant identity through the delegation FSM and local dispatch
  path, and stamps delegation/savings projection rows when a non-blank tenant is present. Durable per-tenant
  identity design remains tracked separately under OMN-14107. This evidence PR performs no live deploy,
  restart, branch-protection mutation, or bypass.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
  - "envelopes"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1404"
    description: >-
      omnibase_core PR #1404 is open against dev at head 8ff4b5072fb0221805009d9b7bdf94a43c232904 and
      adds optional tenant_id fields to the canonical delegation wire DTOs used by omnimarket.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14058/dod-core-pr-1404/command.yaml && grep
          -q '^pr_number: 1404$' drift/dod_receipts/OMN-14058/dod-core-pr-1404/command.yaml && grep -q
          '^commit_sha: 8ff4b5072fb0221805009d9b7bdf94a43c232904$' drift/dod_receipts/OMN-14058/dod-core-pr-1404/command.yaml
  - id: "dod-omnimarket-pr-1648"
    description: >-
      omnimarket PR #1648 is open against dev at head 989aaf9be7e4f0e6e4defa37479b54c6b75c35a4 and carries
      the delegation FSM, local dispatch, and projection-row tenant_id threading half.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14058/dod-omnimarket-pr-1648/command.yaml &&
          grep -q '^pr_number: 1648$' drift/dod_receipts/OMN-14058/dod-omnimarket-pr-1648/command.yaml
          && grep -q '^commit_sha: 989aaf9be7e4f0e6e4defa37479b54c6b75c35a4$' drift/dod_receipts/OMN-14058/dod-omnimarket-pr-1648/command.yaml
  - id: "dod-no-live-deploy"
    description: >-
      Deploy-scope evidence: the downstream PRs are code/model changes only. This OCC PR performs no live
      deploy, no runtime restart, no branch-protection change, and no bypass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14058/dod-no-live-deploy/command.yaml && grep
          -qi 'no live deploy' drift/dod_receipts/OMN-14058/dod-no-live-deploy/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding receipt: this onex_change_control PR carries the central OMN-14058 contract and PASS
      receipts consumed by omnibase_core PR #1404 and omnimarket PR #1648 via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14058/dod-occ-pr-self/command.yaml && grep -q
          '^ticket_id: "OMN-14058"$' drift/dod_receipts/OMN-14058/dod-occ-pr-self/command.yaml
