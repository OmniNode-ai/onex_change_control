---
schema_version: "1.0.0"
ticket_id: "OMN-18067"
title: "onex-dev runtime plane: OMNIINTELLIGENCE_DB_URL arrives by reference, and the Valkey mirror's
  store gap is named"
summary: >
  OCC companion for OmniNode-ai/omninode_infra PR #1265. Bound by hand (union-resolve): the OMN-18067
  autobind command published on omninode_infra run 34304700122 (event_id fa8cbad7-7d09-4b69-a02e-6caa4db7dd18)
  and no companion PR was minted, so #1265 had no receipt to cite and both the Receipt Gate and occ-preflight
  failed on the missing citation. Bar run 34302708857 leg 5 (store_sourced_secrets) scored FAIL on the
  onex-dev runtime plane (DEV-SYSTEM i-06169517a92b45f86) with two shapes: OMNIINTELLIGENCE_DB_URL bound
  by neither a literal nor a secretKeyRef -- an OMN-13769 empty-string blank, which the API server drops
  because EnvVar.Value is omitempty -- and VALKEY_PASSWORD bound into onex-valkey-credentials, a Secret
  no InfisicalSecret owns. #1265 binds the first by reference into the store-synced Secret on all three
  runtime profiles, retires the OMN-14950 C2 waiver it made redundant (ratchet 22 -> 21), and records
  the second as a single-entry, ticket-bound gap that cannot close without an operator store write. No
  credential was rotated, re-issued or minted, and no secret value was read (rule 22).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1265 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1265 on OmniNode-ai/omninode_infra, derived from the PR's own diff: the new render gate, which feeds the onex-dev overlay's runtime-family pod templates through the bar collector's OWN classifier, plus the ownership/parity suites the manifest change moves. Executed at cwd `${OMNI_HOME}/omninode_infra`.
    command: |-
      uv run pytest tests/k8s/test_omn18067_runtime_plane_store_sourced_bindings.py tests/k8s/test_onex_lab_overlay_omn17533.py tests/k8s/test_onex_lab_control_plane_omn17537.py tests/k8s/test_omn17769_store_resolved_credential_registration.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1265"
    description: >
      PR #1265 on OmniNode-ai/omninode_infra — the runtime-plane binding fix and its render gate. Bound
      by hand (union-resolve) because the autobind published no companion for this ticket.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1265 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1265/files --paginate --jq '.[].filename' | grep -cFx 'tests/k8s/test_omn18067_runtime_plane_store_sourced_bindings.py'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #1265 on OmniNode-ai/omninode_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1265"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/k8s/test_omn18067_runtime_plane_store_sourced_bindings.py tests/k8s/test_onex_lab_overlay_omn17533.py tests/k8s/test_onex_lab_control_plane_omn17537.py tests/k8s/test_omn17769_store_resolved_credential_registration.py -q
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "occ-self-bind-pr-8755"
    description: "Binds OMN-18067 to OCC companion PR #8755 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8755 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1294"
    description: "PR #1294 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1294 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1294/files --paginate --jq '.[].filename' | grep -cFx '.secrets.baseline'
  - id: "occ-self-bind-pr-8896"
    description: "Binds OMN-18067 to OCC companion PR #8896 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8896 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "ac1-bar-leg5-pass-on-the-landed-sha"
    description: >-
      AC1. Bar leg 5 (store_sourced_secrets) reads PASS on the landed sha, naming the instance id, and
      the facts artifact shows every runtime binding arriving by valueFrom.secretKeyRef into a Secret
      an InfisicalSecret owns. omninode_infra #1294 merged 2026-09-10T07:26:48Z squash 78b59915efdeda6c911f1cd8a07aaf4457400325;
      deploy-onex-staging run 34449991366 applied the overlay on that sha and concluded success. BEFORE/AFTER,
      not an assertion: bar run 34449190281 collected 2026-09-10T07:18:08Z -- before that deploy -- scored
      leg 5 FAIL with the detail "pod omninode-runtime-74887dd8c5-8cbcp in namespace onex-dev on i-06169517a92b45f86
      binds env key VALKEY_PASSWORD to Secret onex-valkey-credentials, which no InfisicalSecret owns".
      Runs 34465176555 (10:16:48Z) and 34465906041 (10:25:05Z) both score leg 5 PASS with "all 8 secret-bearing
      env keys across 1 runtime pod(s) in namespace onex-dev on i-06169517a92b45f86 resolve through valueFrom.secretKeyRef
      into store-owned Secrets, with no literal value in any manifest". Two independent runs rather than
      one, so a single reading cannot carry the criterion. The facts artifact records VALKEY_PASSWORD
      -> onex-runtime-credentials with store_sourced true; the only false store_sourced entries left are
      the two INFISICAL_* keys, whose role is store_bootstrap -- the credential the runtime authenticates
      TO the store WITH, which the leg exempts by construction because requiring it to come from the store
      is circular. Legs 1, 3 and 4 read FAIL in those runs for causes outside this ticket: leg 4 was ALREADY
      FAIL at 07:18Z, before this change deployed, and legs 1 and 3 turn on staging deploy run 34463307676
      (head d8b21a3326fa, not this ticket's sha) concluding failure with a mixed-digest plane. This change
      touches no image, digest or replica count. execution_scope is local_done_gate (OMN-15392): every
      check here needs something the hosted gate does not have. The bar report and facts live as Actions
      artifacts on omninode_infra, and the OCC CI identity has no actions:read there -- it gets HTTP 403
      -- while the test checks run in the omninode_infra clone, which the hosted gate does not check out.
      The local Done gate owns them and the hosted gate records NOT-EVALUATED rather than guessing. Every
      check below was executed against landed state before this entry was committed.
    source: "manual"
    execution_scope: "local_done_gate"
    status: "verified"
    binds_ac: ["AC1"]
    checks:
      - check_type: "command"
        check_value: |-
          d="$(mktemp -d)" && gh run download 34465906041 --repo OmniNode-ai/omninode_infra --name staging-green-report --dir "$d" && python3 -c "import json,sys; r=json.load(open(sys.argv[1]+'/staging-green-report.json')); leg=[x for x in r['legs'] if x['checklist_item']==5][0]; assert leg['outcome']=='PASS', leg; assert 'i-06169517a92b45f86' in leg['detail'], leg; print('leg5', leg['outcome'])" "$d"
      - check_type: "command"
        check_value: |-
          d="$(mktemp -d)" && gh run download 34465906041 --repo OmniNode-ai/omninode_infra --name staging-green-report --dir "$d" && python3 -c "import json,sys; f=json.load(open(sys.argv[1]+'/staging-green-facts.json')); pods=f['legs']['store_sourced_secrets']['secret_env_bindings']['pods']; b=[x for p in pods for x in p['secret_bearing_bindings']]; assert b, 'no bindings classified - a zero here would satisfy every assertion below it'; v=[x for x in b if x['env_key']=='VALKEY_PASSWORD']; assert v, 'VALKEY_PASSWORD absent from the classified bindings'; assert all(x['binding_kind']=='secret_key_ref' and x['secret_name']=='onex-runtime-credentials' and x['store_sourced'] for x in v), v; assert all(x['store_sourced'] or x['role']=='store_bootstrap' for x in b), [x for x in b if not x['store_sourced'] and x['role']!='store_bootstrap']; print('bindings', len(b), 'valkey store_sourced', [x['store_sourced'] for x in v])" "$d"
      - check_type: "command"
        check_value: |-
          d="$(mktemp -d)" && gh run download 34449190281 --repo OmniNode-ai/omninode_infra --name staging-green-report --dir "$d" && python3 -c "import json,sys; r=json.load(open(sys.argv[1]+'/staging-green-report.json')); leg=[x for x in r['legs'] if x['checklist_item']==5][0]; assert leg['outcome']=='FAIL', leg; assert 'onex-valkey-credentials' in leg['detail'], leg; print('pre-change control: leg5', leg['outcome'])" "$d"
  - id: "ac2-lab-red-green-gate-and-companion-order"
    description: >-
      AC2. The lab readback, the RED/GREEN gate, the companion-first order and the ledger row. LAB: a
      k8s/onex-lab render with per-run generated values resolves all six lab consumers AND the lane's
      own Valkey server to ONE Secret and ONE key, so the lane's server/client no-drift invariant survives
      the move; the lane's Valkey server was rebound with them, and the now-unreferenced generator and
      its properties file are deleted. RED/GREEN: both gates were run against a deliberately reverted
      binding before the change was accepted -- the render gate failed with "runtime-plane secretKeyRefs
      into a Secret no InfisicalSecret in k8s/onex-dev-overlay-dev declares: ['omninode-runtime[omninode-runtime]
      VALKEY_PASSWORD -> Secret onex-valkey-credentials']", and the Valkey contract gate failed with "expected
      mandatory onex-runtime-credentials:VALKEY_PASSWORD, got ('onex-valkey-credentials', 'password',
      False)"; both pass on the change. The single-entry UNOWNED_SECRET_ALLOWANCE ratchet is DELETED rather
      than amended, exactly as its own note required, and a successor test asserts the symbol cannot be
      reintroduced. COMPANION FIRST: OCC #8896 merged 2026-09-10T07:12:15Z squash ccef8709bb489de7e8fc0197c4c92fe365000b33,
      ahead of omninode_infra #1294 at 07:26:48Z. execution_scope is local_done_gate (OMN-15392): every
      check here needs something the hosted gate does not have. The bar report and facts live as Actions
      artifacts on omninode_infra, and the OCC CI identity has no actions:read there -- it gets HTTP 403
      -- while the test checks run in the omninode_infra clone, which the hosted gate does not check out.
      The local Done gate owns them and the hosted gate records NOT-EVALUATED rather than guessing. Every
      check below was executed against landed state before this entry was committed.
    source: "manual"
    execution_scope: "local_done_gate"
    status: "verified"
    binds_ac: ["AC2"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/k8s/test_omn18067_runtime_plane_store_sourced_bindings.py tests/k8s/test_omn_16594_valkey_auth_wiring.py tests/k8s/test_onex_lab_overlay_omn17533.py -q
        cwd: "${OMNI_HOME}/omninode_infra"
      - check_type: "command"
        check_value: |-
          python3 scripts/validation/test_valkey_credential_contract.py
        cwd: "${OMNI_HOME}/omninode_infra"
      - check_type: "command"
        check_value: |-
          python3 -c "import re,sys; t=open('tests/k8s/test_omn18067_runtime_plane_store_sourced_bindings.py').read(); assert not re.search(r'^UNOWNED_SECRET_ALLOWANCE', t, re.M), 'the allowance was reintroduced as a module symbol'; assert 'test_no_unowned_secret_allowance_may_be_reintroduced' in t, 'the successor ratchet test is gone'; print('allowance deleted, successor ratchet present')"
        cwd: "${OMNI_HOME}/omninode_infra"
      - check_type: "command"
        check_value: |-
          python3 -c "import json,subprocess; occ=json.loads(subprocess.run(['gh','pr','view','8896','--repo','OmniNode-ai/onex_change_control','--json','mergedAt'],capture_output=True,text=True,check=True).stdout)['mergedAt']; pr=json.loads(subprocess.run(['gh','pr','view','1294','--repo','OmniNode-ai/omninode_infra','--json','mergedAt'],capture_output=True,text=True,check=True).stdout)['mergedAt']; assert occ and pr, (occ,pr); assert occ < pr, ('companion did not merge first', occ, pr); print('companion', occ, 'before product', pr)"
