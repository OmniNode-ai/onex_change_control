---
schema_version: "1.0.0"
ticket_id: "OMN-16883"
title: "Autobind OCC evidence for OMN-16883"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_core PR #1610.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1610 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1610 on OmniNode-ai/omnibase_core, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omni_worktrees/OMN-16883/omnibase_core-1610`.
    command: |-
      uv run pytest tests/unit/models/dashboard/test_dashboard_imports.py tests/unit/models/dashboard/test_widget_envelope.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_core-pr-1610"
    description: "PR #1610 on OmniNode-ai/omnibase_core — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/models/dashboard/model_widget_envelope.py?ref=7f594f0ff06edcd307d169f0be40f71a698a45ab --jq '.content' | base64 -d | grep -c 'class ModelWidgetEnvelope'
      - check_type: "command"
        check_value: "gh pr view 1610 --repo OmniNode-ai/omnibase_core --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #1610 on OmniNode-ai/omnibase_core — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/models/dashboard/test_dashboard_imports.py tests/unit/models/dashboard/test_widget_envelope.py -q
        cwd: "${OMNI_HOME}/omni_worktrees/OMN-16883/omnibase_core-1610"
  - id: "occ-self-bind-pr-7484"
    description: "Binds OMN-16883 to OCC companion PR #7484 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7484 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-occ-diff-derived-behavior-proof-cwd-fix"
    description: "PR #1610 on OmniNode-ai/omnibase_core — diff-derived behavior proof, re-asserted against
      the canonical registry clone. The original dod-occ-diff-derived-behavior-proof item declared cwd
      ${OMNI_HOME}/omni_worktrees/OMN-16883/omnibase_core-1610, a transient PR worktree; the OMN-16902
      cwd-materialisation sweep independently flagged that shape of dest as unclassifiable. This item
      asserts the identical check_value against the canonical clone cwd instead. Corpus-hygiene repair,
      append-only: the original item and its receipts are untouched."
    source: "generated"
    execution_scope: "local_done_gate"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-diff-derived-behavior-proof"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/models/dashboard/test_dashboard_imports.py tests/unit/models/dashboard/test_widget_envelope.py -q
        cwd: "${OMNI_HOME}/omnibase_core"
