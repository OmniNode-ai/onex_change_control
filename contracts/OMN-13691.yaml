---
schema_version: "1.0.0"
ticket_id: "OMN-13691"
title: "Migrate handler declarations to canonical handler_routing for four omnimarket nodes"
summary: >-
  omnimarket PR #1489 migrates node_canary_score_reducer, node_persona_retrieval_effect, node_persona_storage_effect,
  and node_intent_event_consumer_effect contracts from the deprecated handler-only schema toward canonical
  handler_routing blocks. The product PR keeps the existing handler declarations while adding operation_match
  handler_routing entries and regression coverage for the validator scan.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnimarket PR #1489 checks pass after OCC evidence lands"
    command: "gh pr checks 1489 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr"
    description: >-
      omnimarket PR #1489 updates the four node contracts to include canonical handler_routing and adds
      regression coverage for the validator scan.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13691/dod-omnimarket-pr/command.yaml && grep
          -q '^pr_number: 1489$' drift/dod_receipts/OMN-13691/dod-omnimarket-pr/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-readiness assessment for runtime contract-path edits. No live deploy, restart, docker exec,
      rpk topic produce, or .201 mutation was performed by this OCC PR; the normal post-merge dev deploy
      picks up the contract schema-only changes.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13691/dod-deploy-assessment/command.yaml &&
          grep -qi 'deploy' drift/dod_receipts/OMN-13691/dod-deploy-assessment/command.yaml && grep -qi
          'docker exec' drift/dod_receipts/OMN-13691/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: "Self-binding OCC eligibility receipt for this OCC PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13691/dod-occ-self/command.yaml"
