---
schema_version: "1.0.0"
ticket_id: "OMN-14126"
title: "Disable automatic PR merge enrollment"
summary: >-
  Disable automatic PR auto-merge enrollment so merge sweep ordering remains manual and runner pressure
  is not increased by background queue arming. Keep the explicit manual helper and fix its non-interactive
  squash strategy.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-pr-771"
    description: >-
      omniintelligence PR #771 carries the workflow change disabling automatic merge enrollment and validates
      the manual helper path.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName,headRefOid,title
  - id: "dod-occ-pr-3755"
    description: >-
      onex_change_control PR #3755 carries the central OMN-14126 contract and PASS receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName,headRefOid,title
