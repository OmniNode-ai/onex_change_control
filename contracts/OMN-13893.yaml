---
schema_version: "1.0.0"
ticket_id: "OMN-13893"
title: "fix(OMN-13893): Infra Consistency Check diffs against PR base ref, not hardcoded origin/main"
summary: >-
  omninode_infra PR #560 fixes check_infra_consistency.py to diff against the PR's actual base ref (GITHUB_BASE_REF,
  e.g. dev) instead of a hardcoded origin/main, which produced false failures on every dev-targeted PR
  once dev diverged from main. _get_diff() resolves the base ref via a new _resolve_base_ref() helper
  while preserving the zero-arg call contract the existing test suite's monkeypatches depend on. 18/18
  tests passed (13 pre-existing + 5 new), ruff/pre-commit clean, and the Infra Consistency Check itself
  now reports PASS on this PR (previously it would have false-failed on main-divergence noise).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-560"
    description: >-
      Consuming-PR binding: omninode_infra PR #560 carries the OMN-13893 base-ref fix, with a local 18/18
      pytest run, clean ruff lint/format, and clean pre-commit run as evidence.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13893/dod-infra-pr-560/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13893 contract and receipts consumed
      by omninode_infra PR #560.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13893/dod-occ-self/command.yaml"
