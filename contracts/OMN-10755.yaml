---
schema_version: "1.0.0"
ticket_id: "OMN-10755"
title: "Remove stale event_bus_events references and add correlation_id forward gate"
summary: >
  event_bus_events was dropped in the 2026-05-09 DB rebuild. This ticket removes the stale retention-cleanup
  target from omnimarket node_retention_cleanup (contract.yaml + handler) and adds a WARNING-level forward
  gate to omnibase_infra HandlerLedgerAppend that fires when correlation_id is None, making envelope-contract
  violations observable without dropping events.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-unit-tests"
    description: "omnimarket unit test suite (921 tests) passes with event_bus_events removed from node_retention_cleanup"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: "PASS"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10755/dod-omnimarket-unit-tests/command.yaml"
  - id: "dod-omnimarket-lint"
    description: "Ruff lint and mypy --strict clean on node_retention_cleanup"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: "PASS"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10755/dod-omnimarket-lint/command.yaml"
  - id: "dod-infra-integration-tests"
    description: "2 integration tests pass: null correlation_id emits WARNING and stores event"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: "PASS"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10755/dod-infra-integration-tests/command.yaml"
  - id: "dod-infra-lint"
    description: "Ruff lint and mypy --strict clean on handler_ledger_append"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: "PASS"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10755/dod-infra-lint/command.yaml"
  - id: "dod-deploy"
    description: "No runtime deploy required — changes are handler-layer and contract cleanup only; no
      new topology or Kafka topics. Deploy-agent inactive per OMN-8841."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: "PASS"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10755/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: "PASS"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10755/dod-occ-pr/command.yaml"
  - id: "dod-occ-contract-cleanup-pr"
    description: "Bind the follow-up OCC contract-compliance cleanup to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: "PASS"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10755/dod-occ-contract-cleanup-pr/command.yaml"
