---
schema_version: "1.0.0"
ticket_id: "OMN-10586"
title: "Finish ConfigPrefetcher contract-directory prefetch"
summary: >
  Add ConfigPrefetcher.prefetch_for_contracts in omnibase_infra so runtime bootstrap can extract config
  requirements from contract YAML and prefetch matching Infisical keys. The PR includes focused unit coverage,
  integration coverage, and deploy-gate evidence for post-merge runtime import/readiness validation without
  performing a live deploy or .201 restart.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-integration-tests"
    description: "Unit and integration coverage for ConfigPrefetcher.prefetch_for_contracts passes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          if [ "$REPO" = "OmniNode-ai/onex_change_control" ]; then grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10586/dod-unit-integration-tests/command.yaml";
          else uv run pytest tests/unit/runtime/config_discovery/test_config_prefetcher_for_contracts.py
          tests/integration/test_config_prefetcher_for_contracts_integration.py -q; fi
  - id: "dod-deploy"
    description: >
      Deploy evidence: no live deploy or .201 restart was performed by this PR; post-merge smoke validates
      ConfigPrefetcher.prefetch_for_contracts is importable in the target runtime image.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10586/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10586 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10586/dod-occ-pr/command.yaml"
