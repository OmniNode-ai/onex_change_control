---
schema_version: "1.0.0"
ticket_id: "OMN-12864"
title: "feat(OMN-12864, OMN-12814, OMN-12945): Bifrost endpoints → committed overlay authority + fail-loud
  loader"
summary: >
  Moves the four BIFROST_LOCAL_*_ENDPOINT_URL values (coder :8000, reasoner :8001, embedding :8100, ds4-flash
  :8101) from ephemeral shell exports on .201 into a committed typed YAML lane overlay (docker/lane-overlays/dev.bifrost.yaml),
  validated by ModelBifrostLaneOverlay (OMN-12815: complete URL enforcement). The compose stack reads
  a generated dotenv sidecar via env_file — no shell state required. BIFROST_CONTRACT_PATH footgun removed
  (OMN-12814: hardcoded path, no :-/empty fallback). render_bifrost_delegation_contract now fails-loud
  on any error (no silent empty result, no lru_cache). Entrypoint sets BIFROST_FORCE_RESEED=1 on every
  restart to eliminate named-volume drift (OMN-12945).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "bifrost lane overlay model + CI sync-check + render tests pass"
    command: "cd omni_worktrees/OMN-12864/omnibase_infra && uv run pytest tests/unit/runtime/models/test_model_bifrost_lane_overlay.py
      tests/ci/test_bifrost_lane_overlay.py tests/unit/runtime/test_render_bifrost_delegation_contract.py
      -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >
      omnibase_infra PR carries committed lane overlay (dev.bifrost.yaml + dev.bifrost.env), fail-loud
      loader (ProtocolConfigurationError on all error paths), BIFROST_CONTRACT_PATH footgun removed, BIFROST_FORCE_RESEED=1
      in entrypoint, and 39 passing unit/CI tests. Pre-commit all-pass. Ruff clean.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12864/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-12864$' drift/dod_receipts/OMN-12864/dod-001/command.yaml && grep -q '^pr_number:
          2567$' drift/dod_receipts/OMN-12864/dod-001/command.yaml"
