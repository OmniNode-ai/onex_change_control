---
schema_version: "1.0.0"
ticket_id: "OMN-14836"
title: "Autobind OCC evidence for OMN-14836"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1834."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1834 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1834"
    description: "PR #1834 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1834-ci"
    description: "PR #1834 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-omnimarket-pr-1834-deploy-scope"
    description: "PR #1834 on OmniNode-ai/omnimarket touches deploy-scoped runtime handler node_persona_builder_compute/handler_persona_classify.py;
      deploy gate evidence records the runtime path and requires post-merge deploy fitness validation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy-scope assertion: runtime path src/omnimarket/nodes/node_persona_builder_compute/handlers/handler_persona_classify.py
          changed in PR #1834; post-merge deploy validation must include omnimarket runtime profile deploy
          fitness before promotion.'"
  - id: "occ-self-bind-pr-4465"
    description: "OCC companion PR #4465 — self-bind for OMN-14836 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
