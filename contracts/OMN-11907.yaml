---
schema_version: "1.0.0"
ticket_id: "OMN-11907"
title: "Wave 7 intelligence and quality declarative contract migration"
summary: >
  Bind central evidence for omnimarket PR #805, which migrates intelligence and quality nodes to declarative
  handler_routing contracts and proves the conflict-resolved rebase with local lint, imperative-contract,
  topic-lint, and focused overseer verifier test evidence.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-805"
    description: "Bind OMN-11907 evidence to omnimarket PR #805."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11907/dod-omnimarket-pr-805/command.yaml
          && grep -q '^pr_number: 805$' drift/dod_receipts/OMN-11907/dod-omnimarket-pr-805/command.yaml"
  - id: "dod-conflict-resolution-local-gates"
    description: "Conflict-resolved rebase has no conflict markers and pre-commit passes on changed files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11907/dod-conflict-resolution-local-gates/command.yaml
          && grep -q 'slice passed' drift/dod_receipts/OMN-11907/dod-conflict-resolution-local-gates/command.yaml"
  - id: "dod-overseer-verifier-focused-tests"
    description: "Focused overseer verifier tests pass after the Wave 7 rebase."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11907/dod-overseer-verifier-focused-tests/command.yaml
          && grep -q '55 passed' drift/dod_receipts/OMN-11907/dod-overseer-verifier-focused-tests/command.yaml"
  - id: "dod-imperative-contract-guard"
    description: "Imperative contract guard remains clean for the intelligence and quality migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11907/dod-imperative-contract-guard/command.yaml
          && grep -q 'omnimarket                     203      289     39      250     0' drift/dod_receipts/OMN-11907/dod-imperative-contract-guard/command.yaml"
  - id: "dod-node-drift-gate"
    description: "Strict changed-node drift gate passes after adding the missing overseer observer handler
      block."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11907/dod-node-drift-gate/command.yaml
          && grep -q 'node-drift-gate: PASS' drift/dod_receipts/OMN-11907/dod-node-drift-gate/command.yaml"
  - id: "dod-topic-lint"
    description: "Topic lint accepts the intelligence and quality contract migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11907/dod-topic-lint/command.yaml
          && grep -q 'OK: no topic naming violations found under src/omnimarket' drift/dod_receipts/OMN-11907/dod-topic-lint/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence for the intelligence and quality declarative contract migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11907/dod-deploy-readiness/command.yaml
          && grep -q 'intelligence and quality contract migration has conflict-resolution proof' drift/dod_receipts/OMN-11907/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr-1521"
    description: "OCC PR #1521 carries the central OMN-11907 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11907/dod-occ-pr-1521/command.yaml
          && grep -q '^pr_number: 1521$' drift/dod_receipts/OMN-11907/dod-occ-pr-1521/command.yaml"
