---
schema_version: "1.0.0"
ticket_id: "OMN-13294"
title: "Mass-produce mechanical scanner validators (corpus-accepted, bake-to-blocking) (G2)"
summary: >-
  G2 mass-production of mechanical scanner validators. Lands the generated, corpus-accepted hardcoded-private-IP
  COMPUTE validator in omnibase_core (PR #1286) and the generation producer (acceptance corpus + driver
  + live-run evidence) in omnimarket (PR #1312). The scanning logic was generated by node_generation_consumer
  local-model-first and accepted only because it passed a deterministic acceptance corpus in the hardened
  sandbox (corpus is the authority, not the LLM). This central OCC evidence binds OMN-13294 to omnibase_core
  PR #1286 and omnimarket PR #1312 and records that the PRs add no live deploy mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1286"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1286 at the current head. The receipt binds by
      pr_number and commit_sha for downstream Receipt Gate resolution.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13294/dod-omnibase-core-pr-1286/command.yaml
  - id: "dod-omnimarket-pr-1312"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1312 at the current head. The receipt binds by pr_number
      and commit_sha for downstream Receipt Gate resolution.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13294/dod-omnimarket-pr-1312/command.yaml
  - id: "dod-omnimarket-pr-1318"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1318 — the G2 mass-produce continuation that adds
      the mechanical-scanner long tail (hardcoded-localhost-url, hardcoded-topic-string, todo-fixme-marker)
      to the same generation producer, each corpus-accepted against the live local model. The receipt
      binds by pr_number and commit_sha for downstream Receipt Gate resolution.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13294/dod-omnimarket-pr-1318/command.yaml
  - id: "dod-omnibase-core-pr-1314"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1314 — the G2 residual that lands the generated,
      corpus-accepted hardcoded-topic-string COMPUTE validator (the "topic-naming convention" mechanical
      scanner named in the OMN-13294 taxonomy). Generated by node_generation_consumer against the live
      local model (Qwen3.6-35B-A3B on .201:8000) and corpus-accepted on the first attempt (corpus_checked=corpus_passed=true).
      Wired BLOCKING as a pre-commit hook on staged files plus a required CI gate; the full-src CI self-scan
      lands report-only for the pre-existing legitimate-SOT burn-down (no_faked_boundary precedent). The
      receipt binds by pr_number and commit_sha for downstream Receipt Gate resolution.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13294/dod-omnibase-core-pr-1314/command.yaml
  - id: "dod-deploy-no-live-mutation"
    description: >-
      Deploy-path evidence for OMN-13294. The PRs add a repository validation gate (a COMPUTE node executed
      over the in-memory bus at pre-commit time) and its generation producer + CI evidence only. They
      add no runtime node deploy, no Kafka topic in a live broker, no migration, no image-build input,
      no .201 mutation, and no live deploy. The new validation topic constants are pre-commit/in-memory
      transport names, not live-broker topics.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13294/dod-deploy-no-live-mutation/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13294 contract and receipts.
      Binds the OCC PR by pr_number so the OCC receipt-gate eligibility check can resolve a PASS receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13294/dod-occ-pr-self/command.yaml
