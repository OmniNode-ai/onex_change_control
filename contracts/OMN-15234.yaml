---
schema_version: "1.0.0"
ticket_id: "OMN-15234"
title: "Autobind OCC evidence for OMN-15234"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2502.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2502 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2502"
    description: "PR #2502 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2502 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} --repo ${REPO} --name-only | grep -qiE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-deploy-scope-omn15234-matcher-evaluator"
    description: >-
      Deploy-scope probe for OMN-15234 at product commit 2d6170a4: the narrowed per-name grandfathering
      matcher is present in scripts/check-env-reads.sh and the node evaluator requires composite corroboration
      before recommending RESTART_RUNNER. Falsifiable against the shipped artifact — it reads the product
      repo at a pinned commit, not a receipt this PR authors.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/check-env-reads.sh?ref=2d6170a45c7aeab1e39617968dc888c549b70d92'
          --jq .content | base64 -d | grep -q 'ENV_NAME_READ_PATTERN'
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_runner_fleet_health_compute/handlers/handler_runner_fleet_health_evaluate.py?ref=2d6170a45c7aeab1e39617968dc888c549b70d92'
          --jq .content | base64 -d | grep -q '_zombie_restart_corroboration'
  - id: "occ-self-bind-pr-5150"
    description: "Binds OMN-15234 to OCC companion PR #5150 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
