---
schema_version: "1.0.0"
ticket_id: "OMN-12986"
title: "MSK managed Kafka cutover: Python IAM auth path"
summary: >-
  Adds repo-side support for the managed MSK IAM auth path needed by the OMN-12986 hosted Kafka cutover.
  omnibase_infra PR #2199 wires KAFKA_SASL_MECHANISM=AWS_MSK_IAM through the shared Kafka config and direct
  aiokafka admin/client construction, validates blank MSK regions before token generation, and keeps the
  new OAuth token fetch on the contract-managed HTTP provider rather than raw HTTP transport. This is
  pre-deploy code evidence only; no runtime deploy, restart, docker exec, rpk topic produce, AWS mutation,
  or live cluster mutation was performed by this merge-sweep fix.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2199"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2199 implements the Python MSK IAM auth path and local
      validation needed before a managed Kafka runtime cutover.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12986/dod-omnibase-infra-pr-2199/command.yaml"
  - id: "dod-no-runtime-deploy"
    description: >-
      Deploy-gate evidence for this runtime-path PR: this change is pre-deploy code wiring only. Post-merge
      runtime validation should perform the real deploy and MSK produce/consume proof under the managed
      data-plane cutover plan.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'No runtime deploy, restart, docker exec, rpk topic produce, AWS mutation'
          drift/dod_receipts/OMN-12986/dod-no-runtime-deploy/command.yaml"
  - id: "dod-occ-pr-3576"
    description: >-
      OCC self-binding: onex_change_control PR #3576 carries the OMN-12986 contract and receipts consumed
      by omnibase_infra PR #2199.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 3576$' drift/dod_receipts/OMN-12986/dod-occ-pr-3576/command.yaml"
  - id: "dod-omnibase-infra-pr-2200"
    description: >-
      Replacement consuming-PR binding: omnibase_infra PR #2200 carries the same OMN-12986 MSK IAM auth
      changes after the original PR #2199 was closed during the branch rename required by identity gates.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 2200$' drift/dod_receipts/OMN-12986/dod-omnibase-infra-pr-2200/command.yaml"
  - id: "dod-occ-pr-3577"
    description: >-
      OCC self-binding: onex_change_control PR #3577 carries the OMN-12986 receipt binding the replacement
      omnibase_infra PR #2200.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 3577$' drift/dod_receipts/OMN-12986/dod-occ-pr-3577/command.yaml"
  - id: "dod-omnibase-infra-pr-2204"
    description: >-
      Follow-up consuming-PR binding: omnibase_infra PR #2204 carries the OMN-12986 MSK IAM auth follow-up
      that switches direct aiokafka clients to ssl_context and wires auth kwargs into direct client
      construction sites.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 2204$' drift/dod_receipts/OMN-12986/dod-omnibase-infra-pr-2204/command.yaml"
  - id: "dod-occ-pr-3602"
    description: >-
      OCC self-binding: onex_change_control PR #3602 carries the OMN-12986 receipt binding the follow-up
      omnibase_infra PR #2204.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 3602$' drift/dod_receipts/OMN-12986/dod-occ-pr-3602/command.yaml"
