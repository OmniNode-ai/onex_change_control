---
schema_version: "1.0.0"
ticket_id: "OMN-16824"
title: "Autobind OCC evidence for OMN-16824"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/onex_change_control
  PR #7393.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #7393 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: "Shared cross-runner semantics case table executed against the hosted runner"
    command: "uv run pytest tests/test_omn16824_test_passes_semantics.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-onex_change_control-pr-7393"
    description: "PR #7393 on OmniNode-ai/onex_change_control — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/contents/src/onex_change_control/scripts/contract_compliance_check.py?ref=04e7445d4396a794b09ebba40289f2a897e9d65d --jq '.content' | base64 -d | grep -c 'def _resolve_check_cwd'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/contents/src/onex_change_control/scripts/contract_compliance_check.py?ref=04e7445d4396a794b09ebba40289f2a897e9d65d --jq '.content' | base64 -d | grep -c 'def _resolve_check_cwd'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7395"
    description: "Binds OMN-16824 to OCC companion PR #7395 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7395 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-16824-one-semantic-executed"
    description: >-
      AC1/AC3/AC4. Runs the OMN-16824 suite in this repo. It executes the shared cross-runner case table
      (tests/fixtures/check_type_runner_semantics.yaml, checked into omnimarket byte-for-byte and run
      there against node_dod_verify) through the hosted runner, and pins the AC4 regression by execution
      through run_compliance_check itself: with every gh call mocked to report a fully green PR, a contract
      carrying a test_passes entry whose check_value fails returns exit 1, and the discriminating control
      with a passing check_value returns exit 0. It also asserts the runner makes no gh pr checks call
      at all, so the PR-status reading cannot come back by another route.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/test_omn16824_test_passes_semantics.py -q"
  - id: "dod-16824-runner-regression-suite"
    description: >-
      AC2. Runs the compliance runner's own suite, which now pins the executed reading of test_passes
      (replacing four tests that pinned the opposite), the bash -o pipefail invocation, and that command
      and test_passes are one runner. Together with the suite above this is the cwd proof: a declared
      cwd is honoured, an unresolvable one is NOT_EVALUATED, and the command demonstrably does not run
      in the workspace instead (the case asserts no file the command would have created exists anywhere).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/test_contract_compliance_check.py -q"
  - id: "occ-self-bind-pr-7393"
    description: >-
      OCC self-binding for PR #7393, the in-repo PR carrying the fix, the shared case table, and the two
      behaviour-proving legs above. Binds OMN-16824 to that OCC PR by pr_number so eligibility can resolve
      a PASS receipt for the PR itself (OMN-14650).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 7393 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "dod-OmniNode-ai-omnimarket-pr-2182"
    description: >-
      Behaviour proof for the omnimarket half of the ambiguity test (omnimarket PR #2182). Reads the local
      runner's semantics suite back from omnimarket at the PR head SHA and asserts the cross-runner parity
      test is present in it. Falsifiable and outside this OCC PR's own diff: the file lives in another
      repo at a pinned ref, so the check goes RED if the parity test is renamed, removed, or never landed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api repos/OmniNode-ai/omnimarket/contents/tests/unit/nodes/node_dod_verify/test_omn16824_test_passes_semantics.py?ref=a90df84094882b7c7898996a9729f9b62fd31ef6
          --jq '.content' | base64 -d)" && printf '%s' "$body" | grep -qF 'test_local_runner_matches_the_shared_semantic'
  - id: "occ-self-bind-pr-7399"
    description: >-
      OCC self-binding for PR #7399, the evidence companion carrying the omnimarket#2182 behaviour proof
      above. Binds OMN-16824 to that OCC PR by pr_number so eligibility can resolve a PASS receipt for
      the companion itself (OMN-14650).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 7399 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
