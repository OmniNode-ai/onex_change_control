---
schema_version: "1.0.0"
ticket_id: "OMN-15206"
title: "Slim omnicursor CLAUDE.md — org-wide repo CLAUDE.md refresh"
summary: >
  OCC companion evidence for omnicursor PR #16 (MERGED): slims CLAUDE.md from 10,441 to 9,435 bytes as
  a correctness-first pass — every retained claim live-verified against source; false claims corrected
  (omniclaude-main/ was documented as gitignored but is not; the pre-commit gate is 5 steps not 3; lint
  scope was missing scripts/ci/). Also pins [tool.ruff.lint] select to the pre-0.16 defaults because CI
  installs unpinned ruff and ruff 0.16.0 fails the whole tree with 685 findings on a zero-code-change
  diff (bisected: 0.15.1 green, 0.16.0 red, same commit). This companion binds directly to the merged
  product commit bb0c0637590171688af9cd73f97a9325284040cb on omnicursor main.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Merged product PR binding and content probes for omnicursor#16."
    command: "gh pr view 16 --repo OmniNode-ai/omnicursor --json number,state,mergeCommit"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-cursor-pr-16-merged-bb0c0637"
    description: >
      Consuming-PR binding: omnicursor PR #16 is MERGED; its merge commit bb0c0637590171688af9cd73f97a9325284040cb
      on main carries the slimmed CLAUDE.md for OMN-15206 (deterministic merged-commit bind, not a branch
      head).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 16 --repo OmniNode-ai/omnicursor --json state --jq .state | grep -qx
          MERGED"
  - id: "dod-cursor16-claude-slim-bb0c0637"
    description: >
      Falsifiable product probe at the merged commit: CLAUDE.md at bb0c0637590171688af9cd73f97a9325284040cb
      is exactly 9,435 bytes (slimmed from 10,441), still opens with the Cursor-native plugin overview,
      and carries the corrected gitignore trap (omniclaude-main/ is NOT ignored) that replaces the prior
      false claim.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          [ "$(gh api 'repos/OmniNode-ai/omnicursor/contents/CLAUDE.md?ref=bb0c0637590171688af9cd73f97a9325284040cb'
          --jq .size)" = "9435" ] && gh api 'repos/OmniNode-ai/omnicursor/contents/CLAUDE.md?ref=bb0c0637590171688af9cd73f97a9325284040cb'
          --jq .content | base64 -d | grep -q 'plugin porting OmniClaude'
  - id: "dod-occ-self"
    description: >
      OCC companion self-bind for OMN-15206 (deterministic commit-sha bind, no PR-number prediction).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15206/dod-occ-self/command.yaml"
