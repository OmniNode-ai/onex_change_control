---
schema_version: "1.0.0"
ticket_id: "OMN-16846"
title: "Autobind OCC evidence for OMN-16846"
summary: >
  OCC evidence companion for OmniNode-ai/omnibase_infra PR #2971 — separating the evidence-autoclose sweep's
  dispatch venv from the gate venv the behaviour checks run pytest in (D1 on the CI path).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2971 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  # ---------------------------------------------------------------------------
  # Behaviour-proving entries (OMN-15911 proof-class axis: BEHAVIOR).
  #
  # Both execute the product change. A `gh pr view` or a
  # `gh api .../contents/<file>?ref=<sha> | grep -c` probe is true for every PR
  # that exists and stays green with the change entirely reverted; these two go
  # RED the moment the sweep job is put back on a single venv, or the handler is
  # put back on `uv run onex skill dod_verify`.
  #
  # Pinned via `cwd` to the repo that owns the code, per the OMN-16759 shape.
  # ---------------------------------------------------------------------------
  - id: "dod-16846-dispatch-venv-separation"
    description: >-
      AC2 + AC6 behaviour proof. Runs omnibase_infra's tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_16846_dispatch_venv_separation.py,
      which asserts (a) the sweep dispatches the verifier from its own interpreter's `onex` rather than
      through `uv run`, proven through the real runner's exec path and not only the helper, (b) the workflow
      composes a dispatch venv separate from the project's `.venv`, (c) the omnimarket co-install targets
      that dispatch venv and never `.venv`, (d) every dod_verify/sweep dispatch runs through it, and (e)
      a fail-fast gate-venv purity assertion runs BEFORE any dispatch. Confirmed RED against the pre-fix
      tree: the four workflow assertions failed and the handler assertions could not import `_dod_verify_argv`.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_16846_dispatch_venv_separation.py
          -q"
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-16846-sweep-node-suite-intact"
    description: >-
      AC6 regression proof for the co-changed workflow-lint matchers. Runs the whole node_evidence_autoclose_sweep_effect
      unit suite, which carries the OMN-16792 kill-switch wiring assertions and the OMN-16845 OMNI_HOME
      wiring assertions alongside the new OMN-16846 ones. Those two modules located their subject by the
      substring `onex skill <name>`; this PR moved the dispatch onto `"${DISPATCH_VENV}/bin/onex"`, so
      an unwidened matcher would have matched NOTHING and asserted an ordering over an empty set instead
      of failing on its subject. This entry is what proves the widening kept them anchored rather than
      silently disarming them.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/ -q"
        cwd: "${OMNI_HOME}/omnibase_infra"
  # ---------------------------------------------------------------------------
  # Provenance / binding entries. Admitted as provenance; NOT completion proof —
  # each is green with the product change entirely reverted.
  # ---------------------------------------------------------------------------
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2971-ci"
    description: "PR #2971 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2971 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
