---
schema_version: '1.0.0'
ticket_id: OMN-10361
title: "Task 9: [Wave 3 Co-Change] Git log parser + co-change matrix"
summary: "Create analysis/ package in omnibase_core with CoChangeMatrix dataclass and build_cochange_matrix()"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Unit tests for co_change_matrix pass"
    command: "uv run pytest tests/unit/analysis/test_co_change_matrix.py -v"
  - kind: ci
    description: "mypy --strict passes on co_change_matrix.py"
    command: "uv run mypy src/omnibase_core/analysis/co_change_matrix.py --strict"
  - kind: ci
    description: "ruff check passes on analysis/ package"
    command: "uv run ruff check src/omnibase_core/analysis/ tests/unit/analysis/"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "6 unit tests pass covering pair counts, empty commits, dedup, sorted tuples, frozen
      enforcement"
    source: manual
    checks:
      - check_type: test_passes
        check_value: "uv run pytest tests/unit/analysis/test_co_change_matrix.py -v"
  - id: dod-002
    description: "mypy --strict clean on co_change_matrix.py"
    source: manual
    checks:
      - check_type: command
        check_value: "uv run mypy src/omnibase_core/analysis/co_change_matrix.py --strict"
  - id: dod-003
    description: "ruff check passes on new analysis/ package"
    source: manual
    checks:
      - check_type: command
        check_value: "uv run ruff check src/omnibase_core/analysis/ tests/unit/analysis/"
