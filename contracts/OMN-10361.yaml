---
schema_version: '1.0.0'
ticket_id: OMN-10361
title: "Task 9: [Wave 3 Co-Change] Git log parser + co-change matrix"
summary: "Create analysis/ package in omnibase_core with CoChangeMatrix dataclass and build_cochange_matrix()"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Unit tests for co_change_matrix pass (run in omnibase_core repo)"
    command: "gh pr checks 989 --repo OmniNode-ai/omnibase_core"
  - kind: ci
    description: "mypy --strict and ruff check pass (verified via receipts)"
    command: "grep -q 'status: PASS' drift/dod_receipts/OMN-10361/dod-002/command.yaml && grep -q 'status:
      PASS' drift/dod_receipts/OMN-10361/dod-003/command.yaml"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "6 unit tests pass — verified via receipt file"
    source: manual
    checks:
      - check_type: test_passes
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10361/dod-001/test_passes.yaml &&
          grep -q '^ticket_id: OMN-10361$' drift/dod_receipts/OMN-10361/dod-001/test_passes.yaml && grep
          -q '^evidence_item_id: dod-001$' drift/dod_receipts/OMN-10361/dod-001/test_passes.yaml"
  - id: dod-002
    description: "mypy --strict clean — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10361/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-10361$' drift/dod_receipts/OMN-10361/dod-002/command.yaml && grep -q '^evidence_item_id:
          dod-002$' drift/dod_receipts/OMN-10361/dod-002/command.yaml"
  - id: dod-003
    description: "ruff check passes — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10361/dod-003/command.yaml && grep
          -q '^ticket_id: OMN-10361$' drift/dod_receipts/OMN-10361/dod-003/command.yaml && grep -q '^evidence_item_id:
          dod-003$' drift/dod_receipts/OMN-10361/dod-003/command.yaml"
