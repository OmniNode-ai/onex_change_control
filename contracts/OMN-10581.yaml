---
schema_version: "1.0.0"
ticket_id: "OMN-10581"
title: ".env.example + omnimarket-validate-config CLI"
summary: >
  Add scripts/generate_env_example.py (generates .env.example from Settings model), src/omnimarket/cli/validate_config.py
  (omnimarket-validate-config CLI entry point), and unit tests enforcing .env.example stays in sync. OMN-10581.
  Wave 1 / Task 9 of the Public-Shippable plan.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "omnimarket PR #544 validate_config and env_example_sync unit tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          if [ "$REPO" = "OmniNode-ai/onex_change_control" ]; then grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10581/dod-unit-tests/command.yaml";
          else uv run pytest tests/unit/cli/test_validate_config.py tests/unit/config/test_env_example_sync.py
          -v; fi
  - id: "dod-generate"
    description: ".env.example generated from Settings model and matches committed file."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          if [ "$REPO" = "OmniNode-ai/onex_change_control" ]; then grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10581/dod-generate/command.yaml";
          else uv run python scripts/generate_env_example.py && uv run pytest tests/unit/config/test_env_example_sync.py
          -v; fi
  - id: "dod-deploy"
    description: >
      Post-merge deploy smoke plan: omnimarket-validate-config CLI is a local dev tool; no live deploy
      or runtime restart required.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10581/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10581 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10581/dod-occ-pr/command.yaml"
