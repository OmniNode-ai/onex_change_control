---
schema_version: "1.0.0"
ticket_id: "OMN-10581"
title: ".env.example + omnimarket-validate-config CLI"
summary: >
  Add scripts/generate_env_example.py (generates .env.example from Settings model), src/omnimarket/cli/validate_config.py
  (omnimarket-validate-config CLI entry point), and unit tests enforcing .env.example stays in sync. OMN-10581.
  Wave 1 / Task 9 of the Public-Shippable plan.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr"
    description: "OCC PR carries contract and dod receipts for OMN-10581."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-10581/dod-unit-tests/command.yaml && grep -q
          '^ticket_id: "OMN-10581"$' drift/dod_receipts/OMN-10581/dod-unit-tests/command.yaml
  - id: "dod-unit-tests"
    description: "omnimarket PR #544 validate_config and env_example_sync unit tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-10581/dod-unit-tests/command.yaml && grep -q
          '^ticket_id: "OMN-10581"$' drift/dod_receipts/OMN-10581/dod-unit-tests/command.yaml && grep
          -q '^pr_number: 544$' drift/dod_receipts/OMN-10581/dod-unit-tests/command.yaml
  - id: "dod-generate"
    description: ".env.example generated from Settings model and matches committed file."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-10581/dod-generate/command.yaml && grep -q '^ticket_id:
          "OMN-10581"$' drift/dod_receipts/OMN-10581/dod-generate/command.yaml
  - id: "dod-deploy"
    description: >
      Post-merge deploy smoke plan: omnimarket-validate-config CLI is a local dev tool; no live deploy
      or runtime restart required.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-10581/dod-deploy/command.yaml && grep -q '^ticket_id:
          "OMN-10581"$' drift/dod_receipts/OMN-10581/dod-deploy/command.yaml
