---
schema_version: "1.0.0"
ticket_id: "OMN-13034"
title: "B-6: Lane-census ratchet — census timer emits machine-readable lane map; CLAUDE.md lane table
  becomes a generated block"
summary: >-
  Ships and maintains the lane-census ratchet (retro B-6): a CI staleness gate (lane-census-staleness.yml)
  that fails if the census snapshot is missing or older than 7 days; a pre-commit attestation gate (check_lane_attestation.py)
  that rejects lane-boundary attestation language without a same-session verified annotation; a CLAUDE.md
  lane table generator (generate_claude_lane_block.py) that derives from lane-manifest.yaml + census-snapshot.json;
  and an E-5 env-vs-manifest drift checker (check_lane_env_drift.py, OMN-13018). The current refresh PR
  updates the committed census snapshot from .201 and preserves live drift findings rather than masking
  them.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2009"
    description: "omnibase_infra PR #2106 refreshes the committed census-snapshot.json from .201, proves
      the staleness and env-vs-manifest gates locally, and preserves the live drift findings instead of
      masking runtime drift."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13034/dod-infra-pr-2009/command.yaml
          && grep -q '^pr_number: 2106$' drift/dod_receipts/OMN-13034/dod-infra-pr-2009/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-13034 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13034/dod-occ-pr/command.yaml"
