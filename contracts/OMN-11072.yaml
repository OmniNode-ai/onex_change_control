---
schema_version: "1.0.0"
ticket_id: "OMN-11072"
title: "Guard test for omnibase_compat pin (delegation wire contracts)"
summary: >
  Adds a guard test asserting the runtime image's omnibase_compat pin includes delegation wire contracts
  so stability runtime boots without ModuleNotFoundError.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Unit/integration tests covering the change pass locally."
    command: "See drift/dod_receipts/OMN-11072/dod-tests/command.yaml"
  - kind: "ci"
    description: "Pre-commit hooks pass for changed files (ruff format + ruff check + mypy)."
    command: "See drift/dod_receipts/OMN-11072/dod-lint/command.yaml"
  - kind: "ci"
    description: "omnibase_infra PR #1628 carries the implementation."
    command: "See drift/dod_receipts/OMN-11072/dod-infra-pr-1628/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Unit/integration tests for the change pass."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11072/dod-tests/command.yaml"
  - id: "dod-lint"
    description: "ruff format + ruff check pass for changed files."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11072/dod-lint/command.yaml"
  - id: "dod-infra-pr-1628"
    description: "omnibase_infra PR #1628 carries the implementation."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'pr_number: 1628' drift/dod_receipts/OMN-11072/dod-infra-pr-1628/command.yaml"
  - id: "dod-occ-pr-OCC_PR_NUMBER"
    description: "onex_change_control PR #OCC_PR_NUMBER carries the contract and receipts for OMN-11072."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11072/dod-occ-pr-OCC_PR_NUMBER/command.yaml
          && grep -q '^pr_number: OCC_PR_NUMBER$' drift/dod_receipts/OMN-11072/dod-occ-pr-OCC_PR_NUMBER/command.yaml"
