---
schema_version: "1.0.0"
ticket_id: "OMN-13947"
title: "Repair runner-monitor missing-container convergence"
summary: >-
  Fixes runner-fleet non-convergence in the existing runner-monitor remediation path. The prior omnibase_infra
  PR #2208 hardened auto_bounce against overlap, timeout, and created-but-not-running stragglers. The
  follow-up omnibase_infra PR #2213 adds the missing expected runner containers to the monitor's repair
  target set and makes the receipt-honesty checkout idempotent so the PR gate can clone omnibase_core
  in a reused workspace. This is pre-deploy code evidence only; no runner restart, docker compose execution
  against .201, image build/push, or live cluster mutation was performed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2208"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2208 implements the runner-monitor auto_bounce hardening
      in docker/runners/runner-monitor.sh and adds end-to-end behavioral tests for stuck-created targeting,
      compose dispatch, verify/retry startup, and lock contention.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13947/dod-omnibase-infra-pr-2208/command.yaml"
  - id: "dod-omnibase-infra-pr-2213"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2213 carries the missing-container runner-monitor repair
      in docker/runners/runner-monitor.sh and the receipt-honesty checkout idempotency fix.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13947/dod-omnibase-infra-pr-2213/command.yaml
          && grep -q '^pr_number: 2213$' drift/dod_receipts/OMN-13947/dod-omnibase-infra-pr-2213/command.yaml
          && grep -q '8d087a168cab10e587721bfa1d60307d95411451' drift/dod_receipts/OMN-13947/dod-omnibase-infra-pr-2213/command.yaml"
  - id: "dod-no-runtime-deploy"
    description: >-
      Deploy-gate evidence for this PR: the change modifies runner monitor code and tests only. It did
      not restart runners, run docker compose against .201, rebuild or push a runner image, or mutate
      any live runtime/cluster state.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'No runner restart, docker compose execution against .201, image build/push,
          or live cluster mutation' drift/dod_receipts/OMN-13947/dod-no-runtime-deploy/command.yaml"
  - id: "dod-no-runtime-deploy-pr-2213"
    description: >-
      Deploy-gate evidence for omnibase_infra PR #2213: the change modifies runner-monitor and workflow
      code only. It did not restart runners, run docker compose against .201, rebuild or push a runner
      image, or mutate any live runtime/cluster state.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'No runner restart, docker compose execution against .201, image build/push,
          or live cluster mutation' drift/dod_receipts/OMN-13947/dod-no-runtime-deploy-pr-2213/command.yaml
          && grep -q '^pr_number: 2213$' drift/dod_receipts/OMN-13947/dod-no-runtime-deploy-pr-2213/command.yaml"
  - id: "dod-occ-pr-3610"
    description: >-
      OCC self-binding: onex_change_control PR #3610 carries the OMN-13947 contract and receipts that
      omnibase_infra PR #2208 cites via Evidence-Source: OCC#3610.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 3610$' drift/dod_receipts/OMN-13947/dod-occ-pr-3610/command.yaml"
  - id: "dod-occ-pr-3625"
    description: >-
      OCC self-binding: onex_change_control PR #3625 carries the refreshed OMN-13947 contract and receipts
      that omnibase_infra PR #2213 cites via Evidence-Source: OCC#3625.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 3625$' drift/dod_receipts/OMN-13947/dod-occ-pr-3625/command.yaml
          && grep -q 'ea1fade7b8be11e5b8cd747615756676a13ec28c' drift/dod_receipts/OMN-13947/dod-occ-pr-3625/command.yaml"
  - id: "dod-omnibase-infra-pr-2243"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2243 suppresses runner-monitor alert noise by separating
      Slack alert counts from remediation target counts, treating Docker Up/unhealthy runners with live
      listener evidence as degraded instead of actionable failures, and preserving auto-bounce behavior
      for missing, stuck, crashloop, wedge, and persistent offline-idle targets.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13947/dod-omnibase-infra-pr-2243/command.yaml
          && grep -q '^pr_number: 2243$' drift/dod_receipts/OMN-13947/dod-omnibase-infra-pr-2243/command.yaml
          && grep -q '25bada6a704f3d5ac02edca34308783d80912b1e' drift/dod_receipts/OMN-13947/dod-omnibase-infra-pr-2243/command.yaml"
  - id: "dod-live-impact-pr-2243"
    description: >-
      Live-impact evidence for omnibase_infra PR #2243: the runner-monitor script on .201 was patched
      with a syntax-checked temp-file replacement and backup, but no runner containers were restarted
      or reset and no image build/push or docker compose execution was performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13947/dod-live-impact-pr-2243/command.yaml
          && grep -q '^pr_number: 2243$' drift/dod_receipts/OMN-13947/dod-live-impact-pr-2243/command.yaml
          && grep -q 'No runner containers were restarted or reset' drift/dod_receipts/OMN-13947/dod-live-impact-pr-2243/command.yaml"
