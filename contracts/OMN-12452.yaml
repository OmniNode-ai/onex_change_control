---
schema_version: "1.0.0"
ticket_id: OMN-12452
title: "feat(OMN-12452): build gitignore_baseline validator + unit tests"
summary: >
  Adds src/omnibase_core/validators/gitignore_baseline.py (T2 validator) and tests/validation/test_gitignore_baseline.py
  (8 unit tests) to omnibase_core. The validator reads architecture-handshakes/gitignore-baseline.yaml,
  detects language (pyproject.toml present -> universal+python; else universal only), and asserts each
  managed block appears VERBATIM in the target repo's .gitignore. Depends on OMN-12451 (T1, already merged
  to dev).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "8/8 unit tests pass for gitignore_baseline validator in omnibase_core PR #1183."
    command: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12452/dod-001/command.yaml\""
  - kind: "manual"
    description: "279/279 full validation suite passes with new validator in omnibase_core PR #1183."
    command: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12452/dod-002/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "8 unit tests pass for the new gitignore_baseline validator (test_gitignore_baseline.py)
      in omnibase_core PR #1183."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12452/dod-001/command.yaml\" &&
          grep -q '^pr_number: 1183$' \"drift/dod_receipts/OMN-12452/dod-001/command.yaml\""
  - id: "dod-002"
    description: "Full validation test suite (279 tests) passes with the new validator included in omnibase_core
      PR #1183."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12452/dod-002/command.yaml\" &&
          grep -q '^pr_number: 1183$' \"drift/dod_receipts/OMN-12452/dod-002/command.yaml\""
  - id: "dod-occ-pr-1930"
    description: "OCC PR #1930 carries the OMN-12452 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12452/dod-occ-pr-1930/command.yaml\"
          && grep -q '^pr_number: 1930$' \"drift/dod_receipts/OMN-12452/dod-occ-pr-1930/command.yaml\""
