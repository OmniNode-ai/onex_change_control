---
schema_version: "1.0.0"
ticket_id: "OMN-11087"
title: "Benchmark models with provider provenance"
summary: >
  Adds frozen Pydantic models for tracking model generation benchmarks with provider provenance. ModelGenerationAttempt
  captures per-attempt metrics (provider, model_id, token usage, latency, contract_passed). ModelGenerationBenchmark
  aggregates attempts with computed fields (attempt_count, cost_to_contract_pass_usd). ModelGenerationReceipt
  binds artifacts (contract_hash, handler_hash, validation_result_id, contract_version). All models use
  ConfigDict(frozen=True, extra="forbid", from_attributes=True). Exported from omnibase_core.models.benchmark.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "5 unit tests in tests/unit/models/benchmark/test_model_generation_benchmark.py pass."
    command: "See drift/dod_receipts/OMN-11087/dod-tests/command.yaml"
  - kind: "ci"
    description: "Central OCC PR carries the OMN-11087 contract and receipt bundle."
    command: "See drift/dod_receipts/OMN-11087/dod-occ-pr-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "5 unit tests covering attempt metrics, computed fields, frozen enforcement, and receipt
      artifact binding pass."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11087/dod-tests/command.yaml &&
          grep -q '^pr_number: 1086$' drift/dod_receipts/OMN-11087/dod-tests/command.yaml"
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carrying the central OMN-11087 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11087/dod-occ-pr-self/command.yaml"
