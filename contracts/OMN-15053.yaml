---
schema_version: "1.0.0"
ticket_id: "OMN-15053"
title: "Autobind OCC evidence for OMN-15053"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1884."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1884 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1884"
    description: "PR #1884 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1884-ci"
    description: "PR #1884 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4751"
    description: "OCC companion PR #4751 — self-bind for OMN-15053 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4786"
    description: "OCC companion PR #4786 — self-bind repair for OMN-15053 after the original companion
      was superseded."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4786 --repo OmniNode-ai/onex_change_control --json number,state --jq
          '.number == 4786 and .state == \"OPEN\"' | grep -qx true"
  - id: "dod-omnimarket-pr-1884-deploy-gate"
    description: >-
      deploy-gate (OMN-8912) evidence for PR #1884. This is a compute-only handler change — node_auto_merge_effect
      and node_merge_sweep_auto_merge_arm_effect now raise LegacyMergeArmDisabledError in-process instead
      of a silent no-op; no new runtime surface, container, or topic is introduced, so no live redeploy
      is required. The check below is a live, falsifiable probe (gh api against GitHub content, not a
      self-referential read of this repo's own dod_receipts/contract tree): it fetches both changed handler
      files at the PR's pinned head SHA and greps for the guard symbol the fix introduces. It goes RED
      if the code does not exist at that SHA.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_auto_merge_effect/handlers/handler_auto_merge_effect.py?ref=2c97630848152a4447841b1635409884ffcbfdbc
          --jq .content | base64 -d | grep -q 'LegacyMergeArmDisabledError' && gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_merge_sweep_auto_merge_arm_effect/handlers/handler_auto_merge_arm.py?ref=2c97630848152a4447841b1635409884ffcbfdbc
          --jq .content | base64 -d | grep -q 'LegacyMergeArmDisabledError'  # deploy-gate: no-live-redeploy-required,
          compute-only change proven at pinned PR head SHA"
