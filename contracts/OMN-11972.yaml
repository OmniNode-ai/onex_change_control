---
schema_version: "1.0.0"
ticket_id: "OMN-11972"
title: "Unwrap ADK response payloads for self-extending agent registration"
summary: >
  Central OCC evidence for onex-self-extending-agent PR #128, which fixes the ADK response-wrapper mismatch
  for generated tool registration and guards non-object result payloads from replacing the parsed response
  map.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-onex-self-extending-agent-pr-128"
    description: "onex-self-extending-agent PR #128 carries the OMN-11972 ADK response unwrap fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11972/dod-onex-self-extending-agent-pr-128/command.yaml
          && grep -q '^pr_number: 128$' drift/dod_receipts/OMN-11972/dod-onex-self-extending-agent-pr-128/command.yaml"
  - id: "dod-local-validation"
    description: "Focused unit tests and lint passed for the ADK unwrap behavior."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11972/dod-local-validation/command.yaml
          && grep -q 'pytest tests/unit/test_main.py' drift/dod_receipts/OMN-11972/dod-local-validation/command.yaml
          && grep -q 'ruff check' drift/dod_receipts/OMN-11972/dod-local-validation/command.yaml"
  - id: "dod-coderabbit-thread"
    description: "CodeRabbit ADK unwrap review thread was addressed by guarding decoded result payload
      type."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11972/dod-coderabbit-thread/command.yaml
          && grep -q 'PRRT_kwDOSfXbW86EcyHq' drift/dod_receipts/OMN-11972/dod-coderabbit-thread/command.yaml"
  - id: "dod-occ-pr-1597"
    description: "onex_change_control PR #1597 carries the central OMN-11972 contract and receipt bundle."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11972/dod-occ-pr-1597/command.yaml
          && grep -q '^pr_number: 1597$' drift/dod_receipts/OMN-11972/dod-occ-pr-1597/command.yaml"
