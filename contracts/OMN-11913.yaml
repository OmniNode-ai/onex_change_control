---
schema_version: "1.0.0"
ticket_id: "OMN-11913"
title: "Antipattern vector index effect node"
summary: >
  Bind central evidence for omnimarket PR #824, which adds node_antipattern_index_effect: an effect node
  that loads the antipattern registry, embeds vector-enabled entries through the configured embedding
  endpoint, and upserts them into the Qdrant-backed antipattern index.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-824"
    description: "Bind OMN-11913 evidence to omnimarket PR #824."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11913/dod-omnimarket-pr-824/command.yaml
          && grep -q '^pr_number: 824$' drift/dod_receipts/OMN-11913/dod-omnimarket-pr-824/command.yaml"
  - id: "dod-antipattern-index-local-validation"
    description: "Focused antipattern index tests, strict mypy, imperative contract guard, and local contract
      checks pass for the current omnimarket PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11913/dod-antipattern-index-local-validation/command.yaml
          && grep -q '17 passed, 1 skipped' drift/dod_receipts/OMN-11913/dod-antipattern-index-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: antipattern index effect has deploy readiness coverage without
      requiring a live runtime deploy before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11913/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11913/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11913 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11913/dod-occ-pr/command.yaml"
