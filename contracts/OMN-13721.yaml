---
schema_version: "1.0.0"
ticket_id: "OMN-13721"
title: "test(coverage): fill error-path gaps in crypto_file_key_provider"
summary: "Add tests for three uncovered error branches in FileKeyProvider: base64 TypeError (lines 103-104),
  UnicodeDecodeError as ValueError (lines 117-123), and OSError atomic-write cleanup (lines 235-247).
  No source changes."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "PASS receipts bind the omnibase_core test evidence and this OCC PR self-binding."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13721/dod-001/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-13721/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "omnibase_core PR adds 6 new tests covering lines 103-104 (TypeError from non-string
      base64 value), 117-123 (UnicodeDecodeError triggering outer ValueError handler), and 235-247 (OSError
      cleanup in _save_keys). Coverage rises from 85.62% to 98.63%."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13721/dod-001/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding OCC receipt: this onex_change_control PR carries the OMN-13721 contract
      and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13721/dod-occ-self/command.yaml"
