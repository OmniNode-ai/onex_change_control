---
schema_version: "1.0.0"
ticket_id: "OMN-19188"
title: "Autobind OCC evidence for OMN-19188"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1677."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
requirements:
  - id: "req-transcribed-acceptance-criteria"
    statement: |-
      The acceptance criteria this ticket's author declared, transcribed from the ticket body at autobind time by occ-autobind. The ticket body is the source and this block is the copy; each criterion's id is the label the sibling binds_ac entry claims.
    acceptance:
      - id: "AC1"
        statement: |-
          AC1: The count is established as a defect rather than a deliberate choice -- falsifier: a written statement naming whether secrets.infisical.com/auto-reload was deliberately omitted from these Deployments or never applied, citing the manifest history that decides it.
      - id: "AC2"
        statement: |-
          AC2: At least one runtime Deployment in onex-dev is annotated so the operator counts it -- falsifier: the AutoRedeployReady condition on onex-runtime-credentials reports a non-zero deployment count, read from the live CR.
      - id: "AC3"
        statement: |-
          AC3: Propagation is proven end to end on a non-credential key -- falsifier: a value changed in Infisical is observed in the Kubernetes Secret AND in the environment of a pod that was already running before the change, both readbacks recorded, no value printed.
      - id: "AC4"
        statement: |-
          AC4: Rotations that cannot auto-redeploy carry the restart in their runbook -- falsifier: the rotation procedure names an explicit rollout step with its readback, rather than ending at the Secret. Gate: live-gate defect: onex-dev Probe: kubectl get infisicalsecret -n onex-dev -o jsonpath over .status.conditions, reading AutoRedeployReady
evidence_requirements:
  - kind: "ci"
    description: "PR #1677 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1677 on OmniNode-ai/omninode_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omninode_infra`.
    command: "uv run pytest tests/k8s/test_onex_dev_infisical_auto_reload_omn19188.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1677"
    description: "PR #1677 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1677 --repo OmniNode-ai/omninode_infra --json number,state"
    binds_ac:
      - "AC1"
      - "AC2"
      - "AC3"
      - "AC4"
    ac_bindings:
      - label: "AC1"
        criterion_hash: "bbed6ee160672f1a92758d04fcce0a39c7baf07d97a8521da88b9521bdb1e530"
        proposed_by: "occ-autobind"
        accepted_by: "fc04d95f-0fcf-4a47-8ac2-26ee573a7cc3"
        accepted_at: "2026-09-22T17:56:58Z"
      - label: "AC2"
        criterion_hash: "2248242aa232c2c3c2b7052ad3ae4aeb5156ffbbdd982956cca94d8483221442"
        proposed_by: "occ-autobind"
        accepted_by: "fc04d95f-0fcf-4a47-8ac2-26ee573a7cc3"
        accepted_at: "2026-09-22T17:56:58Z"
      - label: "AC3"
        criterion_hash: "0771fbd1f90637d7fff7b87b3bf95f1264dc10c9a760413f8b7a3b893587b458"
        proposed_by: "occ-autobind"
        accepted_by: "fc04d95f-0fcf-4a47-8ac2-26ee573a7cc3"
        accepted_at: "2026-09-22T17:56:58Z"
      - label: "AC4"
        criterion_hash: "89d01cbd21882b781734e25461c484bf97493c424ad03f4becdcbbbe818258c6"
        proposed_by: "occ-autobind"
        accepted_by: "fc04d95f-0fcf-4a47-8ac2-26ee573a7cc3"
        accepted_at: "2026-09-22T17:56:58Z"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1677-ci"
    description: "PR #1677 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1677 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-diff-derived-behavior-proof-pr-1677"
    description: "PR #1677 on OmniNode-ai/omninode_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1677"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/k8s/test_onex_dev_infisical_auto_reload_omn19188.py -q"
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "occ-self-bind-pr-11311"
    description: "OCC companion PR #11311 — self-bind for OMN-19188 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 11311 --repo OmniNode-ai/onex_change_control --json number,state"
  # OMN-19188 / 2026-09-25 -- ADD-ONLY CORRECTION OF THE AUTOBIND'S AC BINDINGS.
  #
  # OCC#11311 bound all four criteria to `dod-OmniNode-ai-omninode_infra-pr-1677`, whose whole check is
  # `gh pr view 1677 --json number,state`. That command succeeds for as long as the
  # PR exists, so as bound this contract discharged every criterion without
  # measuring any of them (OMN-18849). The merged item is immutable, so the four
  # bindings are RETIRED below with their reason and re-made against evidence that
  # measures the thing each criterion names.
  - id: "ac1-finding-written-and-enforced-pr-1677"
    description: >-
      AC1 -- the count is a defect rather than a deliberate choice, with the manifest history that decides
      it. The test module added by omninode_infra#1677 is that written statement: its docstring names
      the finding (the key was applied once, as a `metadata.labels` entry, by OMN-16685 / omninode_infra#1025,
      and every InfisicalSecret CR on both clusters reported 0 as a result) and cites the commit that
      decides it, and `test_the_key_is_never_carried_as_a_label` enforces it so the statement cannot rot
      into prose nothing checks.
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/k8s/test_onex_dev_infisical_auto_reload_omn19188.py -q"
        cwd: "${OMNI_HOME}/omninode_infra"
    binds_ac:
      - "AC1"
    ac_bindings:
      - label: "AC1"
        criterion_hash: "bbed6ee160672f1a92758d04fcce0a39c7baf07d97a8521da88b9521bdb1e530"
        proposed_by: "occ-autobind"
        accepted_by: "fc04d95f-0fcf-4a47-8ac2-26ee573a7cc3"
        accepted_at: "2026-09-22T17:56:58Z"
    supersedes_ac_binding:
      - item: "dod-OmniNode-ai-omninode_infra-pr-1677"
        label: "AC1"
        reason: >-
          The retired item reads the PR's number and state. That answers whether a pull request exists,
          never whether anyone wrote the statement AC1 asks for or whether it is still true.
      - item: "dod-OmniNode-ai-omninode_infra-pr-1677"
        label: "AC3"
        reason: >-
          AC3 is settled by changing a value in Infisical and observing it reach a serving pod. The retired
          item contacts no cluster and no secret store. It is retired with no replacement binding, which
          reports AC3 as a visible gap rather than leaving it discharged by a PR read.
  - id: "ac2-operator-enrolment-live-readback"
    description: >-
      AC2 -- at least one runtime Deployment is annotated so the operator ENROLS it. The check reads the
      live Deployment on the staging node over SSM and asserts the annotation the OPERATOR writes, `secrets.infisical.com/managed-secret.onex-runtime-credentials`,
      which it stamps only onto a workload it has taken responsibility for. It is a receipt-grep, not
      a live call: the compliance runner's role occ-compliance-ssm-readonly is denied ssm:SendCommand,
      so the probe is made by an operator and recorded here. Read live 2026-09-25T17:59Z, value `W/"secrets-1536-8811A248"`,
      corroborated by the operator naming that Deployment on every 60s reconcile.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-19188/ac2-operator-enrolment-live-readback/command.yaml"
    binds_ac:
      - "AC2"
    ac_bindings:
      - label: "AC2"
        criterion_hash: "518a0f4b878f83029832505582ba5b26c2ca593795716a086ce8c917828783d1"
        proposed_by: "occ-autobind"
        accepted_by: "fc04d95f-0fcf-4a47-8ac2-26ee573a7cc3"
        accepted_at: "2026-09-25T18:05:00Z"
    supersedes_ac_binding:
      - item: "dod-OmniNode-ai-omninode_infra-pr-1677"
        label: "AC2"
        reason: >-
          AC2 is settled by measuring a running operator. The retired item reads a pull request. It also
          pinned the criterion text as it stood before 2026-09-25, whose falsifier named the AutoRedeployReady
          condition -- a surface whose message is only rewritten on a status transition, last taken 2026-09-09T18:47:57Z,
          so it still reads "0 deployments" while enrolment is live.
  - id: "ac4-runbook-names-the-restart"
    description: >-
      AC4 -- the rotation procedure names an explicit rollout step with its readback, rather than ending
      at the Secret. A receipt-grep rather than a live grep of the runbook: the runbook lives in knowledge-base-internal,
      other lanes edit it, and the Contract Compliance Check re-runs every declared item at the PR head,
      so a live grep would re-assert at every future head a fact measured once. The probe and its output
      are recorded in the receipt.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-19188/ac4-runbook-names-the-restart/command.yaml"
    binds_ac:
      - "AC4"
    ac_bindings:
      - label: "AC4"
        criterion_hash: "89d01cbd21882b781734e25461c484bf97493c424ad03f4becdcbbbe818258c6"
        proposed_by: "occ-autobind"
        accepted_by: "fc04d95f-0fcf-4a47-8ac2-26ee573a7cc3"
        accepted_at: "2026-09-22T17:56:58Z"
    supersedes_ac_binding:
      - item: "dod-OmniNode-ai-omninode_infra-pr-1677"
        label: "AC4"
        reason: >-
          The retired item reads the PR's number and state, which says nothing about what the rotation
          runbook contains.
  - id: "dod-OmniNode-ai-omninode_infra-pr-1690"
    description: "PR #1690 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1690 --repo OmniNode-ai/omninode_infra --json number,state"
    binds_ac:
      - "AC1"
      - "AC2"
      - "AC3"
      - "AC4"
    ac_bindings:
      - label: "AC1"
        criterion_hash: "bbed6ee160672f1a92758d04fcce0a39c7baf07d97a8521da88b9521bdb1e530"
        proposed_by: "occ-autobind"
        accepted_by: "fc04d95f-0fcf-4a47-8ac2-26ee573a7cc3"
        accepted_at: "2026-09-22T17:56:58Z"
      - label: "AC2"
        criterion_hash: "518a0f4b878f83029832505582ba5b26c2ca593795716a086ce8c917828783d1"
        proposed_by: "occ-autobind"
      - label: "AC3"
        criterion_hash: "7850884ffb3c90eec58674d57b2bbec26ee9663b784092b99a7f0b8f72961459"
        proposed_by: "occ-autobind"
      - label: "AC4"
        criterion_hash: "89d01cbd21882b781734e25461c484bf97493c424ad03f4becdcbbbe818258c6"
        proposed_by: "occ-autobind"
        accepted_by: "fc04d95f-0fcf-4a47-8ac2-26ee573a7cc3"
        accepted_at: "2026-09-22T17:56:58Z"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1690-ci"
    description: "PR #1690 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1690 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-diff-derived-behavior-proof-pr-1690"
    description: "PR #1690 on OmniNode-ai/omninode_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1690"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/k8s/test_omn19188_propagation_probe_key.py -q"
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "occ-self-bind-pr-11374"
    description: "OCC companion PR #11374 — self-bind for OMN-19188 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 11374 --repo OmniNode-ai/onex_change_control --json number,state"
  # OMN-19188 / 2026-09-26 -- THE SAME CORRECTION, A SECOND TIME.
  #
  # OCC#11314 retired the four bindings the autobind had put on
  # `dod-OmniNode-ai-omninode_infra-pr-1677` and re-made three of them against
  # evidence that measures something. OCC#11374, the companion for the NEXT
  # product PR on this ticket, bound all four criteria again -- to
  # `gh pr view 1690 --json number,state`, a command that succeeds for as long
  # as the PR exists -- and again left the `test_passes` item it generated in
  # the same run unbound. So the correction does not stick: it is re-applied
  # per product PR until OMN-18849 is fixed in the producer.
  #
  # Nothing here is re-made. AC1, AC2 and AC4 are already bound to real
  # evidence by #11314 and those bindings are untouched; this entry only
  # withdraws the second, weaker binding each of them picked up. AC3 returns to
  # retired-and-unbound, which is honest: the probe key it needs ships in
  # omninode_infra#1690 and the propagation readback has not been run.
  - id: "ac-binding-retirement-pr-1690"
    description: >-
      Withdraws the four merge-state bindings OCC#11374 added, and records the readback that found them.
      Its own check is a receipt-grep rather than a live parse of the contract: the Contract Compliance
      Check re-runs every declared item at the PR head, and an item that parsed this contract would re-assert
      at every future head a fact measured once -- and would invert the moment the duplicate it reports
      is gone, which is the outcome this entry exists to produce.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-19188/ac-binding-retirement-pr-1690/command.yaml"
    supersedes_ac_binding:
      - item: "dod-OmniNode-ai-omninode_infra-pr-1690"
        label: "AC1"
        reason: >-
          Reads the PR's number and state. AC1 asks for a written statement naming whether the annotation
          was deliberately omitted, with the manifest history that decides it; a pull request existing
          says nothing about either. AC1 keeps its binding to ac1-finding-written-and-enforced-pr-1677.
      - item: "dod-OmniNode-ai-omninode_infra-pr-1690"
        label: "AC2"
        reason: >-
          Reads the PR's number and state. AC2 is settled by the operator's own enrolment annotation on
          a live Deployment. AC2 keeps its binding to ac2-operator-enrolment-live-readback.
      - item: "dod-OmniNode-ai-omninode_infra-pr-1690"
        label: "AC3"
        reason: >-
          Reads the PR's number and state, and AC3 is the one criterion here with no evidence at all yet:
          it needs the probe key changed in Infisical and observed in the pod the operator rolls, which
          cannot happen until omninode_infra#1690 deploys. Retired with no replacement, so it reports
          as a visible gap rather than as discharged by a PR read.
      - item: "dod-OmniNode-ai-omninode_infra-pr-1690"
        label: "AC4"
        reason: >-
          Reads the PR's number and state, which says nothing about what the rotation runbook contains.
          AC4 keeps its binding to ac4-runbook-names-the-restart.
