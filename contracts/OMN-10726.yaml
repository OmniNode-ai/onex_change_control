---
schema_version: "1.0.0"
ticket_id: OMN-10726
title: "Architectural gate — handlers cannot own infrastructure lifecycle, DI-only resource acquisition"
summary: "AST-based scanner in omnibase_core enforces that handler and node __init__ signatures accept
  exactly (self, container) and that resource assignments inside __init__ use container resolution rather
  than direct infrastructure construction."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: FULL
evidence_requirements:
  - kind: tests
    description: "16 unit tests green in omnibase_core handler_di_gate test suite"
    command: "uv run pytest tests/unit/validators/test_handler_di_gate.py -v"
  - kind: ci
    description: "CI pipeline green on omnibase_core PR #1063"
    command: "gh pr checks 1063 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-core-pr-1063
    description: "omnibase_core PR #1063 open with handler_di_gate validator and 16 passing tests"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10726/dod-core-pr-1063/command.yaml\"
          && grep -q '^pr_number: 1063$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10726/dod-core-pr-1063/command.yaml\""
  - id: dod-occ-contract
    description: "OCC contract for OMN-10726 merged to main"
    source: generated
    checks:
      - check_type: command
        check_value: "test -f \"$CONTRACT_REPO_DIR/contracts/OMN-10726.yaml\""
