---
schema_version: "1.0.0"
ticket_id: "OMN-12124"
title: "[Hackathon Integration] --agent with invalid API key crashes with unhandled 100-line ADK stack trace"
summary: >
  Catch ADK authentication errors during client init and runner iteration, printing a clean one-line
  error instead of a 100-line traceback. Covers GoogleAuthError, Unauthenticated, and PermissionDenied
  without broad Exception catching. Non-auth exceptions are re-raised unchanged.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "12 new unit tests covering both auth failure points (init + iteration), all exception types, and re-raise for non-auth errors. All 1113 tests pass."
    command: "cd onex-self-extending-agent && uv run pytest tests/unit -v"
  - kind: "ci"
    description: "onex-self-extending-agent PR #137 CI green across all required gates"
    command: "gh pr checks 137 --repo OmniNode-ai/onex-self-extending-agent"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "12 new unit tests covering auth error detection helper, init failure path, iteration failure path, and non-auth re-raise. 1113 total tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 137 --repo OmniNode-ai/onex-self-extending-agent --json number,state,headRefOid,title"
  - id: "dod-rendered-output"
    description: "Invalid API key now shows 'Error: Invalid or missing API key. Set GOOGLE_API_KEY or check your credentials.' and exits 1, instead of a 100-line ADK stack trace."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 137 --repo OmniNode-ai/onex-self-extending-agent --json number,state,headRefOid,title"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC evidence for OMN-12124."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json number,state,headRefOid,headRefName,title"
