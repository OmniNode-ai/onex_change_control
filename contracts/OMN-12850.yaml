---
schema_version: "1.0.0"
ticket_id: "OMN-12850"
title: "Stop classifying generic SEA inference 404s as model registry drift"
summary: >-
  Fixes SEA diagnostic classification so a generic HTTP 404 from the inference
  effect remains endpoint or URL-construction evidence instead of being reported
  as stale model registry drift. Explicit model-not-found text still maps to
  MODEL_REGISTRY_DRIFT.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-source-pr-221"
    description: >-
      onex-self-extending-agent PR #221 carries the SEA diagnostic-classification
      fix for OMN-12850.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12850/dod-source-pr-221/command.yaml"
  - id: "dod-focused-verification"
    description: >-
      Focused local lint, typecheck, and consumer tests passed for the changed
      SEA files.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12850/dod-focused-verification/command.yaml"
  - id: "dod-diagnostic-classification"
    description: >-
      Regression evidence proves generic endpoint 404s now raise an inference
      failure rather than MODEL_REGISTRY_DRIFT while explicit model-not-found
      remains classified as registry drift.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12850/dod-diagnostic-classification/command.yaml"
