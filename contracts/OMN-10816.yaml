---
schema_version: "1.0.0"
ticket_id: "OMN-10816"
title: "fix(OMN-10816): remove ModelONEXContainer instantiation from agent_coordinator_orchestrator docstring"
summary: >
  node_agent_coordinator_orchestrator/__init__.py contained ModelONEXContainer() in its Handler Integration
  docstring Example:: block. Fix updates the docstring to show container as a DI-injected parameter and
  adds a test verifying that importing the package produces zero ModelONEXContainer instantiations.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Golden chain agent coordinator tests pass with clean import purity"
    command: "cd omni_worktrees/OMN-10816/omnimarket && uv run pytest tests/test_golden_chain_agent_coordinator_orchestrator.py
      -v --tb=short"
  - kind: "manual"
    description: "omnimarket PR #746 is open and deploys the fix at the recorded head SHA"
    command: "gh pr view 746 --repo OmniNode-ai/omnimarket --json state -q '.state'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "All 10 agent coordinator golden chain tests pass including the new import-purity test."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10816/dod-tests/command.yaml"
          && grep -q '^ticket_id: "OMN-10816"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10816/dod-tests/command.yaml"
  - id: "dod-product-pr-746"
    description: "omnimarket PR #746 carries the fix at commit 71205a5b."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10816/dod-product-pr-746/command.yaml"
          && grep -q '^pr_number: 746$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10816/dod-product-pr-746/command.yaml"
  - id: "dod-deploy"
    description: "Docstring fix only — no Docker restart or Kafka topic change required. deploy gate:
      import-purity fix, not a runtime topology change."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy gate: OMN-10816 removes ModelONEXContainer() from agent_coordinator_orchestrator
          docstring — docstring fix only, no docker exec or rpk topic produce required'"
  - id: "dod-occ-self"
    description: "Self-binding OCC receipt for the contract carrying OMN-10816 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10816/dod-occ-self/command.yaml"
