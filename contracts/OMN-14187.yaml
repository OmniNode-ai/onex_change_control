---
schema_version: "1.0.0"
ticket_id: "OMN-14187"
title: "feat(OMN-14187): add 4 canonical OCC PR stamp-model core models"
summary: >-
  omnibase_core code-only change (Piece 1/5 of epic OMN-14180). Adds the canonical OCC PR stamp-model
  vocabulary — EnumPrEvidenceSourceKind plus 4 frozen/extra-forbid Pydantic models (ModelPrEvidenceSource,
  ModelPrReceiptGateSkipToken, ModelPrBodySection, ModelPrOccMetadataStamp) under src/omnibase_core/models/validation/
  and src/omnibase_core/enums/, each with deterministic as_dict()/to_json(sort_keys) serializers and per-model
  unit tests (66 passed locally). Pure domain models: zero I/O, zero renderer/parser logic (the deterministic
  renderer/parser is deferred to Piece 2 / OMN-14188, which this unblocks). No runtime services, no deploy,
  no restart, no branch-protection mutation. This companion is authored by an independent adversarial
  verifier, NOT by the implementing agent (no-self-authored-evidence rule).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1406"
    description: >-
      omnibase_core PR #1406 is open against dev at head e797f713bd7bab5307ccda9282a52be10773266a and
      adds the EnumPrEvidenceSourceKind discriminator and 4 canonical OCC PR stamp-model Pydantic models
      (all frozen=True, extra="forbid", tuple-based, deterministic as_dict()/to_json()) plus 5 unit test
      files under omnibase_core. Independent verifier re-ran the 5 new test files: 66 passed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14187/dod-omnibase_core-pr-1406/command.yaml
          && grep -q '^pr_number: 1406$' drift/dod_receipts/OMN-14187/dod-omnibase_core-pr-1406/command.yaml
          && grep -q '^commit_sha: e797f713bd7bab5307ccda9282a52be10773266a$' drift/dod_receipts/OMN-14187/dod-omnibase_core-pr-1406/command.yaml
  - id: "dod-no-live-deploy"
    description: >-
      Scope evidence: OMN-14187 adds pure Pydantic domain models to omnibase_core. It performs no live
      deploy, no runtime restart, no branch-protection mutation, and no bypass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14187/dod-no-live-deploy/command.yaml && grep
          -qi 'no live deploy' drift/dod_receipts/OMN-14187/dod-no-live-deploy/command.yaml
