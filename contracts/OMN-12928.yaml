---
schema_version: "1.0.0"
ticket_id: "OMN-12928"
title: "OCC evidence for structlog dependency range update"
summary: >
  Central OCC evidence for omnibase_infra PR #1932. The product change updates the structlog dependency
  range to allow 26.x, carries the matching uv.lock package metadata, and regenerates the CI runner image
  identity lock because pyproject.toml and uv.lock participate in the runner binding.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-1932"
    description: "omnibase_infra PR #1932 carries the OMN-12928 structlog dependency update and targets
      dev."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12928/dod-product-pr-1932/command.yaml
          && grep -q '^pr_number: 1932$' drift/dod_receipts/OMN-12928/dod-product-pr-1932/command.yaml"
  - id: "dod-runner-identity"
    description: "The runner image lock was regenerated and verified after the dependency manifest change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12928/dod-runner-identity/command.yaml
          && grep -q '7f4a28050457a278a6712b05de154cb8' drift/dod_receipts/OMN-12928/dod-runner-identity/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused runner identity tests passed on the product PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12928/dod-focused-tests/command.yaml
          && grep -q '12 passed' drift/dod_receipts/OMN-12928/dod-focused-tests/command.yaml"
  - id: "dod-dependency-resolution"
    description: "Frozen dependency resolution succeeds with the updated structlog range and lock metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12928/dod-dependency-resolution/command.yaml
          && grep -q 'uv sync --frozen' drift/dod_receipts/OMN-12928/dod-dependency-resolution/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-12928 evidence-ticket contract and self-binding central receipt."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12928/dod-occ-pr/command.yaml &&
          grep -q '^pr_number: 2440$' drift/dod_receipts/OMN-12928/dod-occ-pr/command.yaml"
