---
schema_version: "1.0.0"
ticket_id: "OMN-11884"
title: "Support default_handler shorthand in handler_routing parser"
summary: >
  Bind central evidence for omnibase_infra PR #1724, which teaches the runtime Kafka contract source to
  parse handler_routing.default_handler shorthand when no explicit handlers list is present.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1724"
    description: "Bind OMN-11884 evidence to omnibase_infra PR #1724."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11884/dod-omnibase-infra-pr-1724/command.yaml
          && grep -q '^pr_number: 1724$' drift/dod_receipts/OMN-11884/dod-omnibase-infra-pr-1724/command.yaml"
  - id: "dod-default-handler-unit-tests"
    description: "Focused default_handler shorthand unit tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11884/dod-default-handler-unit-tests/command.yaml
          && grep -q '8 passed' drift/dod_receipts/OMN-11884/dod-default-handler-unit-tests/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence for runtime contract parser change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11884/dod-deploy-readiness/command.yaml
          && printf '%s\\n' 'deploy evidence: default_handler contract parser change validated before
          merge'"
  - id: "dod-occ-pr-1505"
    description: "OCC PR #1505 carries the central OMN-11884 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11884/dod-occ-pr-1505/command.yaml"
