---
schema_version: 1.0.0
ticket_id: OMN-14757
title: Evidence for omnibase_core#1467 multi-partition nack conformance
summary: >
  Central OCC evidence for omnibase_core#1467. The product PR adds a multi-partition nack conformance
  assertion to the shared transport substitutability suite at exact product head 3157330605ef13bbfa5d0bc21535a730fcd7edfa.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
evidence_requirements:
  - kind: ci
    description: PR  #1467 product head is exact for OMN-14757 evidence.
    command: 'PRODUCT_PR_NUMBER=1467; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=3157330605ef13bbfa5d0bc21535a730fcd7edfa;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
  - kind: ci
    description: CI completed successfully for exact PR  #1467 head.
    command: 'PRODUCT_RUN_ID=29631879086; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=3157330605ef13bbfa5d0bc21535a730fcd7edfa;
      : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
      = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
      = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnibase_core-pr-1467
    description: >
      PR #1467 on OmniNode-ai/omnibase_core exists and is bound to OMN-14757 at exact product head 3157330605ef13bbfa5d0bc21535a730fcd7edfa.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1467; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=3157330605ef13bbfa5d0bc21535a730fcd7edfa;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnibase_core-pr-1467-diff
    description: >
      PR #1467 on OmniNode-ai/omnibase_core has a non-empty reviewed product diff for OMN-14757.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1467; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q .'
  - id: dod-omnibase_core-pr-1467-ci
    description: >
      CI run 29631879086 completed successfully for omnibase_core PR #1467 at exact head 3157330605ef13bbfa5d0bc21535a730fcd7edfa
      before OCC evidence was minted.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29631879086; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=3157330605ef13bbfa5d0bc21535a730fcd7edfa;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-omnibase_core-pr-1467-local-verification
    description: >
      Product PR body records local verification for OMN-14757, including the full transport suite, focused
      multi-partition nack assertion, ruff, mypy, and pre-commit proof.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1467; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "Full transport
          suite green" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body |
          grep -q "test_nack_on_one_partition_does_not_strand_sibling_partitions PASSED"'
  - id: dod-omnibase_core-pr-1467-deploy-scope
    description: >
      Deploy-readiness assessment for OMN-14757. The PR is test-only and changes the shared transport
      conformance suite plus in-memory fixture provisioning; no live deploy, restart, docker exec, rpk
      topic produce, or production mutation was performed by this OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14757/dod-omnibase_core-pr-1467-deploy-scope/command.yaml
          && grep -qi ''test-only'' drift/dod_receipts/OMN-14757/dod-omnibase_core-pr-1467-deploy-scope/command.yaml
          && grep -qi ''no live deploy, restart, docker exec, rpk topic produce, or production mutation''
          drift/dod_receipts/OMN-14757/dod-omnibase_core-pr-1467-deploy-scope/command.yaml'
