---
schema_version: "1.0.0"
ticket_id: "OMN-14564"
title: "Runner listener liveness recycle for alive-but-silent self-hosted runners"
summary: >
  OCC evidence for omnibase_infra PR #2396, which remediates the runner fleet mode where Runner.Listener
  remains alive but stops emitting diagnostics and never exits for the existing wrapper supervisor to
  restart it.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #2396 is open at head 79dca1e69af44f7183c504961bfdff38c3b5bc8b."
    command: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-head/command.yaml"
  - kind: "manual"
    description: "omnibase_infra PR #2396 diff scope is limited to runner entrypoint recycle logic, its
      runbook, and runner listener liveness tests."
    command: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-product-diff-scope/command.yaml"
  - kind: "manual"
    description: "omnibase_infra PR #2396 body records focused pytest, ruff, and pre-commit verification
      provenance for the runner liveness change."
    command: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-verification-provenance/command.yaml"
  - kind: "manual"
    description: "omnibase_infra PR #2396 keeps host sync/container restart rollout as an explicit follow-up
      and does not claim a live runtime deployment."
    command: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14564/dod-infra2396-runtime-rollout-gated/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_infra-pr-2396-head"
    description: "Binds omnibase_infra PR #2396 at head 79dca1e69af44f7183c504961bfdff38c3b5bc8b."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-head/command.yaml
          && grep -q '79dca1e69af44f7183c504961bfdff38c3b5bc8b' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-head/command.yaml
  - id: "dod-omnibase_infra-pr-2396-product-diff-scope"
    description: "PR #2396 product diff scope covers only the runner entrypoint, runner liveness runbook,
      and listener liveness tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-product-diff-scope/command.yaml
          && grep -q 'docker/runners/entrypoint.sh' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-product-diff-scope/command.yaml
          && grep -q 'docs/runbooks/runner-fleet-listener-liveness.md' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-product-diff-scope/command.yaml
          && grep -q 'tests/ci/test_runner_listener_liveness.py' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-product-diff-scope/command.yaml
  - id: "dod-omnibase_infra-pr-2396-verification-provenance"
    description: "PR #2396 body records focused listener liveness pytest, ruff, and changed-file pre-commit
      proof."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-verification-provenance/command.yaml
          && grep -q '25 passed' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-verification-provenance/command.yaml
          && grep -q 'ruff format' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-verification-provenance/command.yaml
          && grep -q 'pre-commit run --files' drift/dod_receipts/OMN-14564/dod-omnibase_infra-pr-2396-verification-provenance/command.yaml
  - id: "dod-infra2396-runtime-rollout-gated"
    description: "PR #2396 body states the bind-mounted entrypoint fix requires host file sync and container
      restart; no live restart/deploy is claimed by the PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14564/dod-infra2396-runtime-rollout-gated/command.yaml
          && grep -q 'bind-mounted' drift/dod_receipts/OMN-14564/dod-infra2396-runtime-rollout-gated/command.yaml
          && grep -q 'container restart' drift/dod_receipts/OMN-14564/dod-infra2396-runtime-rollout-gated/command.yaml
