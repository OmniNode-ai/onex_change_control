---
schema_version: "1.0.0"
ticket_id: "OMN-12536"
title: "Preserve runtime tmpfs in catalog-generated compose"
summary: >-
  Bind central evidence for omnibase_infra PR #1816, which fixes the catalog generator so generated runtime
  compose preserves the runtime-owned local ingress tmpfs required for /run/onex-runtime.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1816"
    description: "Bind omnibase_infra PR #1816 head and catalog tmpfs changes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12536/dod-infra-pr-1816/command.yaml"
          && grep -q '^pr_number: 1816$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12536/dod-infra-pr-1816/command.yaml"
          && grep -q '^commit_sha: "733361d2e69ba031d03068f6d7e8b129636e0d8f"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12536/dod-infra-pr-1816/command.yaml"
  - id: "dod-local-validation"
    description: "Local focused and expanded non-Docker catalog validation passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12536/dod-local-validation/command.yaml"
          && grep -q '3 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12536/dod-local-validation/command.yaml"
          && grep -q '10 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12536/dod-local-validation/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12536/dod-local-validation/command.yaml"
  - id: "dod-runtime-hotpatch-proof"
    description: ".201 dev runtime proof shows runtime main healthy and Pattern-B broker stable after
      tmpfs hotpatch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12536/dod-runtime-hotpatch-proof/command.yaml"
          && grep -q 'local_ingress.running=true' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12536/dod-runtime-hotpatch-proof/command.yaml"
          && grep -q 'TOTAL-LAG=0' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12536/dod-runtime-hotpatch-proof/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12536 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12536/dod-occ-pr-self/command.yaml"
