---
schema_version: '1.0.0'
ticket_id: OMN-10382
title: "Usage source enum stack"
summary: "Move LLM usage_source and compute_usage_source values onto the shared EnumUsageSource vocabulary
  across core, intelligence, market projections, and infra writers/migrations."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - public_api
  - protocols
evidence_requirements:
  - kind: tests
    description: "Core EnumUsageSource legacy aliases are covered by focused enum tests."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10382/dod-core-alias/command.yaml"
  - kind: tests
    description: "omniintelligence emits shared usage source values from LLM call completed events."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10382/dod-intelligence-events/command.yaml"
  - kind: tests
    description: "omnimarket projections and build-loop writers use shared usage source values."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10382/dod-market-projection/command.yaml"
  - kind: tests
    description: "omnibase_infra writer, GPU evidence, recompute pricing, and migration paths use shared
      usage source values."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10382/dod-infra-migration/command.yaml"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-core-alias
    description: "omnibase_core PR #993 merged EnumUsageSource legacy aliases for API/api, ESTIMATED,
      and MISSING/missing."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10382/dod-core-alias/command.yaml
          && grep -q '^ticket_id: OMN-10382$' drift/dod_receipts/OMN-10382/dod-core-alias/command.yaml
          && grep -q '^evidence_item_id: dod-core-alias$' drift/dod_receipts/OMN-10382/dod-core-alias/command.yaml"
  - id: dod-intelligence-events
    description: "omniintelligence PR #629 emits and serializes ModelLLMCallCompletedEvent usage source
      fields with EnumUsageSource."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10382/dod-intelligence-events/command.yaml
          && grep -q '^ticket_id: OMN-10382$' drift/dod_receipts/OMN-10382/dod-intelligence-events/command.yaml
          && grep -q '^evidence_item_id: dod-intelligence-events$' drift/dod_receipts/OMN-10382/dod-intelligence-events/command.yaml"
  - id: dod-market-projection
    description: "omnimarket PR #464 normalizes LLM cost projections and build-loop event payloads to
      measured, estimated, and unknown."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10382/dod-market-projection/command.yaml
          && grep -q '^ticket_id: OMN-10382$' drift/dod_receipts/OMN-10382/dod-market-projection/command.yaml
          && grep -q '^evidence_item_id: dod-market-projection$' drift/dod_receipts/OMN-10382/dod-market-projection/command.yaml"
  - id: dod-infra-migration
    description: "omnibase_infra PR #1478 migrates writer inputs, GPU evidence payloads, recompute pricing,
      schema fingerprint, and migration 077 to shared usage source values."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10382/dod-infra-migration/command.yaml
          && grep -q '^ticket_id: OMN-10382$' drift/dod_receipts/OMN-10382/dod-infra-migration/command.yaml
          && grep -q '^evidence_item_id: dod-infra-migration$' drift/dod_receipts/OMN-10382/dod-infra-migration/command.yaml"
