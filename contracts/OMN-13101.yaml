---
schema_version: "1.0.0"
ticket_id: "OMN-13101"
title: "fix(OMN-13101): spec transport mocks batch 1 — drain baseline to zero"
summary: >-
  Drains the transport_mock_baseline.yaml completely to zero. All 63 formerly-baselined test files across
  tests/unit/ and tests/integration/ now carry spec= or spec_set= on every AsyncMock/MagicMock bound to
  an EventBus*/transport surface. The OMN-13026 lint gate now blocks ALL new violations with no remaining
  exemptions.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr"
    description: >-
      omnibase_infra PR #2002 adds spec= to all 63 formerly-baselined mock sites, empties transport_mock_baseline.yaml
      to {}, and fixes the suppression comment placement in test_node_runtime_orchestrator.py. Pre-commit
      Transport Mock Lint (OMN-13026) gate PASSED. mypy --strict clean on 2442 source files. 20491 unit
      tests passed (5 pre-existing failures on dev, unrelated to this change).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13101/dod-infra-pr/command.yaml"
          && grep -q '^ticket_id: OMN-13101$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13101/dod-infra-pr/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      This OCC PR carries the central OMN-13101 ticket contract and the dod-infra-pr receipt that the
      omnibase_infra code PR #2002 Receipt-Gate resolves via its Evidence-Source line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13101/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: OMN-13101$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13101/dod-occ-pr/command.yaml"
