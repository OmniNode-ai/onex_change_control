---
schema_version: "1.0.0"
ticket_id: "OMN-13505"
title: "Re-pin omnimarket dev omnibase-core git source to core dev HEAD 1b22fd18 (OMN-12835 typed workflow_definition
  field)"
summary: >-
  omnimarket dev's omnibase-core git pin was at 81c865f69a15781d526125ee13f39834a3ce69fd (the OMN-13504
  relock SHA), which predates OMN-12835. OMN-12835 (omnibase_core commit 3048cecab / PR #1303) wired the
  typed contract-side execution-graph DAG into orchestrator contracts: ModelWorkflowConfig.workflow_definition
  and the new model_contract_workflow_definition.py. This re-pins the omnimarket omnibase-core dev git
  source to current omnibase_core origin/dev HEAD 1b22fd18bc0c3aebe9501da63dc57e33a47a3d70 (v0.46.0),
  which carries OMN-12835 (3048cecab is in the 81c865f6..1b22fd18 range and present in HEAD's tree). The
  re-pin updates all three places: pyproject.toml [tool.uv.sources] rev=, the git+@ dependency line, and
  uv.lock (regenerated via uv lock). Version constraint omnibase_core>=0.45.0,<0.47.0 is unchanged (core
  dev is v0.46.0 -- satisfied). core-only re-pin; omnibase_infra/compat/spi pins untouched. No source,
  version-specifier, migration, schema, endpoint, or env change in omnimarket -- only pyproject.toml pins
  + uv.lock (plus this in-repo contract mirror). This OCC PR carries the central contract consumed by
  the omnimarket PR #1353 Receipt Gate via Evidence-Source.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ancestry"
    description: >-
      The new core pin 1b22fd18bc0c3aebe9501da63dc57e33a47a3d70 is an ancestor of omnibase_core origin/dev
      (it is origin/dev HEAD itself; merge-base --is-ancestor exits 0; is_ancestor=true) -- the OMN-12977
      guard precondition. The probe greps the recorded receipt and proves the PASS line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13505/dod-ancestry/command.yaml"
  - id: "dod-field-present"
    description: >-
      The newly-pinned omnibase_core (1b22fd18, v0.46.0) carries OMN-12835: the installed package's ModelWorkflowConfig.model_fields
      contains 'workflow_definition', and model_contract_workflow_definition.py is present in the dev
      tree (commit 3048cecab, PR #1303). This is the field that unblocks the OMN-12835-dependent omnimarket
      waves (W2/W5/B3.1/B3.2). The probe greps the recorded receipt and proves the PASS line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13505/dod-field-present/command.yaml"
  - id: "dod-guard-ok"
    description: >-
      The OMN-12977 sibling-pin guard (check_sibling_lock_pins.py) reports omnibase-core [OK] (lock=0.46.0
      clone=0.46.0) and exits 0 against the origin/dev-synced canonical clones using the re-pinned omnimarket
      uv.lock. The canonical omnibase_core clone HEAD is 1b22fd18, exactly the new pin; all 6 sibling
      pins match uv.lock. The probe greps the recorded guard receipt and proves the PASS line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13505/dod-guard-ok/command.yaml"
  - id: "dod-suite"
    description: >-
      The full omnimarket pytest suite passes against the newer core: 7992 passed, 66 skipped, 9 deselected
      (the @pytest.mark.kafka live-broker tests, excluded by 'not kafka'), 0 failures; mypy src/ --strict
      Success (2463 files); ruff format + check clean; pre-commit run --all-files all gates passed. The
      probe greps the suite receipt and proves the PASS line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13505/dod-suite/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contract used by the omnimarket
      PR #1353 Receipt Gate via Evidence-Source. The probe greps the OCC tree and proves contracts/OMN-13505.yaml
      is present (carried by this OCC PR).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13505\"$' contracts/OMN-13505.yaml"
