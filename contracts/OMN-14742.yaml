---
schema_version: 1.0.0
ticket_id: OMN-14742
title: Autobind OCC evidence for OMN-14742
summary: 'OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1804.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #1804 product diff scope present'
    command: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-OmniNode-ai-omnimarket-pr-1804
    description: "PR #1804 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-OmniNode-ai-omnimarket-pr-1804-ci
    description: "PR #1804 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
  - id: occ-self-bind-pr-4364
    description: "OCC companion PR #4364 — self-bind for OMN-14742 (OMN-14650)."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-omnimarket-1804-deploy
    description: Deploy-readiness assessment for OMN-14742. The omnimarket PR touches runtime OCC companion
      producer paths and requires deploy-gate evidence; this evidence lane performed no live deploy, restart,
      .201, or production mutation and records the deploy hold/readiness assessment for the runtime-sensitive
      change.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14742/dod-omnimarket-1804-deploy/command.yaml
          && grep -qi ''deploy'' drift/dod_receipts/OMN-14742/dod-omnimarket-1804-deploy/command.yaml'
  - id: occ-self-bind-pr-4365
    description: 'OCC companion PR #4365 self-binding proof for OMN-14742 deploy evidence follow-up.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
