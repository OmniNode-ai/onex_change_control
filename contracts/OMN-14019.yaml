---
schema_version: "1.0.0"
ticket_id: "OMN-14019"
title: "fix(OMN-14019): bounded-recency window on ROI overlay aggregation"
summary: >-
  omnimarket PR #1635 adds opt-in recency bounding for ROI overlay aggregation so recently regressed tiers
  can be suppressed without lifetime success history making them effectively unsuppressible. The default
  configuration remains byte-identical to the prior whole-table aggregation behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Targeted ROI overlay routing tests and local quality checks passed"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14019/dod-local-verification/command.yaml"
  - kind: "manual"
    description: "omnimarket PR #1635 carries the OMN-14019 implementation"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14019/dod-omnimarket-pr-1635/command.yaml"
  - kind: "manual"
    description: "Runtime deploy scope is bounded to normal post-merge rollout"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14019/dod-deploy-scope/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-local-verification"
    description: >-
      Local verification documented on omnimarket PR #1635 covers the ROI overlay regression window, default-off
      byte identity, deterministic recency ordering, time-window filtering, environment override behavior,
      end-to-end resolver threading, broader delegation tests, ruff, strict mypy, and pre-commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14019/dod-local-verification/command.yaml"
  - id: "dod-omnimarket-pr-1635"
    description: >-
      Consuming-PR binding: omnimarket PR #1635 at head 94bd9ea209a89bac119a2e5d2dd413a53de1ce68 carries
      the OMN-14019 implementation and verification summary.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14019/dod-omnimarket-pr-1635/command.yaml"
  - id: "dod-deploy-scope"
    description: >-
      Deploy-scope binding: this routing aggregation change is default-off and rides the normal post-merge
      dev and stability redeploy cadence. No out-of-band live runtime mutation is in scope.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14019/dod-deploy-scope/command.yaml
          && echo no-out-of-band-deploy"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-14019 central contract and
      PASS receipts consumed by omnimarket PR #1635 via its Evidence-Source line.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14019/dod-occ-self/command.yaml"
