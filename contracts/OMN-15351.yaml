---
schema_version: "1.0.0"
ticket_id: "OMN-15351"
title: "Autobind OCC evidence for OMN-15351"
summary: >
  OCC contract authored for OmniNode-ai/omnimarket PR #1943 and OmniNode-ai/omnibase_infra PR #2535.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1943 and PR #2535 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1943"
    description: "PR #1943 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/tests/test_generation_events_tenant_access_omn14974.py?ref=36d632b7bf83fd6749b4264defbb4104978a3d44 --jq '.content' | base64 -d | grep -c 'def test_absent_role_omnidash_warns_instead_of_aborting_the_migration'
  - id: "dod-OmniNode-ai-omnimarket-pr-1943-ci"
    description: "PR #1943 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5400"
    description: "OCC companion PR #5400 — self-bind for OMN-15351 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2535"
    description: "PR #2535 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5397"
    description: "Binds OMN-15351 to OCC companion PR #5397 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
