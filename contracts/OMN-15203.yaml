---
schema_version: "1.0.0"
ticket_id: "OMN-15203"
title: "Slim omninode_infra CLAUDE.md — org-wide repo CLAUDE.md refresh"
summary: >
  OCC companion evidence for omninode_infra PR #679: slims CLAUDE.md from 18348 to 5296 bytes per context-engineering
  doctrine (stale Agent Execution Rules section deleted; post-deploy manual checklist, data-source table,
  and OMN-4598 tools table moved to docs/POST_DEPLOY_VERIFICATION.md; stale ALLOWED_NON_LIVE health-gate
  claim corrected to the real k8s-readiness MIN_LIVE gate; all live-verified repo-specific traps retained
  inline).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Product PR binding for omninode_infra#679 (private repo; bound via committed receipts)."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ninfra-pr-679-head-f8fb9325"
    description: >
      Consuming-PR binding: omninode_infra PR #679 head f8fb9325339a591bcdd8f92781b71b7bb713b41d carries
      the slimmed CLAUDE.md (5296 bytes) plus the new docs/POST_DEPLOY_VERIFICATION.md for OMN-15203.
      omninode_infra is a private repo, so the live probe was run out of band and recorded in the committed
      receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-15203/dod-ninfra-pr-679-head-f8fb9325/command.yaml
          && grep -q '^pr_number: 679$' drift/dod_receipts/OMN-15203/dod-ninfra-pr-679-head-f8fb9325/command.yaml
          && grep -q 'f8fb9325339a591bcdd8f92781b71b7bb713b41d' drift/dod_receipts/OMN-15203/dod-ninfra-pr-679-head-f8fb9325/command.yaml
  - id: "dod-ninfra679-claude-slim-f8fb9325"
    description: >
      Falsifiable product-head probe for omninode_infra PR #679, run out of band against the pinned head
      f8fb9325339a591bcdd8f92781b71b7bb713b41d: docs/POST_DEPLOY_VERIFICATION.md exists at the head with
      the moved data-source table; CLAUDE.md at the same head points to it, retains the OMN-3556 validate_version
      trap and the omniweb Keycloak do-not-revert warning, and no longer claims an ALLOWED_NON_LIVE allowlist
      exists.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-15203/dod-ninfra679-claude-slim-f8fb9325/command.yaml
          && grep -q 'f8fb9325339a591bcdd8f92781b71b7bb713b41d' drift/dod_receipts/OMN-15203/dod-ninfra679-claude-slim-f8fb9325/command.yaml
  - id: "dod-occ-self"
    description: >
      OCC companion self-bind for OMN-15203 (deterministic commit-sha bind, no PR-number prediction) plus
      a live CI-side observation of this OCC PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15203/dod-occ-self/command.yaml"
