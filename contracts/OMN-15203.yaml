---
schema_version: "1.0.0"
ticket_id: "OMN-15203"
title: "Slim omninode_infra CLAUDE.md — org-wide repo CLAUDE.md refresh"
summary: >
  OCC companion evidence for omninode_infra PR #679 (MERGED): slims CLAUDE.md from 18,348 to 5,296 bytes
  per context-engineering doctrine. Re-cut evidence companion: the original companions (#5013 autobind
  and #5016 hand-authored, both closed unmerged) were closed by an OCC queue-hygiene batch pass after
  the product PR merged, leaving the merged product change with no durable receipt. omninode_infra is
  a PRIVATE repo unreachable from OCC CI tokens, so per the OMN-14051 hermetic pattern the live product
  probes were run out of band against the already-merged product commit d6a3c80374029fbc66b1956704a13001adfade27
  and recorded in the committed receipts; contract checks verify those receipts plus a live self-observation
  of this OCC PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Merged product PR binding (out-of-band, private repo) for omninode_infra#679."
    command: "gh pr view 679 --repo OmniNode-ai/omninode_infra --json number,state,mergeCommit"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ninfra-pr-679-merged-d6a3c803"
    description: >
      Consuming-PR binding: omninode_infra PR #679 is MERGED; its merge commit d6a3c80374029fbc66b1956704a13001adfade27
      on dev carries the slimmed CLAUDE.md for OMN-15203 (deterministic merged-commit bind, not a branch
      head). Live probe run out of band 2026-07-26 (private repo, OMN-14051 hermetic pattern); check verifies
      the committed receipt attests PASS, pr_number 679, and the merge commit sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-15203/dod-ninfra-pr-679-merged-d6a3c803/command.yaml
          && grep -q '^pr_number: 679$' drift/dod_receipts/OMN-15203/dod-ninfra-pr-679-merged-d6a3c803/command.yaml
          && grep -q 'd6a3c80374029fbc66b1956704a13001adfade27' drift/dod_receipts/OMN-15203/dod-ninfra-pr-679-merged-d6a3c803/command.yaml
  - id: "dod-ninfra679-claude-slim-d6a3c803"
    description: >
      Falsifiable product probe for omninode_infra PR #679, run out of band against the merged commit
      d6a3c80374029fbc66b1956704a13001adfade27: CLAUDE.md at that ref is exactly 5,296 bytes (slimmed
      from 18,348) and carries the slim-pattern monorepo overview line. Check verifies the committed receipt
      attests PASS bound to the merge commit sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-15203/dod-ninfra679-claude-slim-d6a3c803/command.yaml
          && grep -q 'd6a3c80374029fbc66b1956704a13001adfade27' drift/dod_receipts/OMN-15203/dod-ninfra679-claude-slim-d6a3c803/command.yaml
  - id: "dod-occ-self"
    description: >
      OCC companion self-bind for OMN-15203 (deterministic commit-sha bind, no PR-number prediction) plus
      a live CI-side observation of this OCC PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15203/dod-occ-self/command.yaml"
