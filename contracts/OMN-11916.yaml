---
schema_version: "1.0.0"
ticket_id: "OMN-11916"
title: "Memgraph architecture graph query node"
summary: >
  Bind central evidence for omnimarket PR #811, which adds the architecture graph query effect node and
  proves the conflict-resolved rebase plus metadata dependency repair for the neo4j Bolt driver.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - topics
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-811"
    description: "Bind OMN-11916 evidence to omnimarket PR #811."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11916/dod-omnimarket-pr-811/command.yaml
          && grep -q '^pr_number: 811$' drift/dod_receipts/OMN-11916/dod-omnimarket-pr-811/command.yaml"
  - id: "dod-architecture-graph-local-validation"
    description: "Focused architecture graph node tests, entry-point validation, mypy, and metadata dependency
      scope checks pass locally."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11916/dod-architecture-graph-local-validation/command.yaml
          && grep -q '11 passed' drift/dod_receipts/OMN-11916/dod-architecture-graph-local-validation/command.yaml"
  - id: "dod-architecture-graph-precommit"
    description: "Changed-file pre-commit slice passes after adding the neo4j metadata dependency."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11916/dod-architecture-graph-precommit/command.yaml
          && grep -q 'Topic Naming Lint.*Passed' drift/dod_receipts/OMN-11916/dod-architecture-graph-precommit/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness assessment for the read-only architecture graph query effect."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11916/dod-deploy-readiness/command.yaml
          && grep -q 'read-only architecture graph query effect' drift/dod_receipts/OMN-11916/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr-1525"
    description: "OCC PR #1525 carries the central OMN-11916 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11916/dod-occ-pr-1525/command.yaml
          && grep -q '^pr_number: 1525$' drift/dod_receipts/OMN-11916/dod-occ-pr-1525/command.yaml"
