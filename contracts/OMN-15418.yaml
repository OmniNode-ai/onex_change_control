---
schema_version: "1.0.0"
ticket_id: "OMN-15418"
title: "Autobind OCC evidence for OMN-15418"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2548.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2548 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2548"
    description: "PR #2548 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2548 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2548 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2548 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/pulls/2548/files --paginate --jq '.[].filename'
          | grep -Fx docker/docker-compose.e2e.yml"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2548"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5635"
    description: "Binds OMN-15418 to OCC companion PR #5635 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "python3 -c \"import json, subprocess; data=json.loads(subprocess.check_output(['gh','api','repos/OmniNode-ai/onex_change_control/pulls/5635']));
          assert data['number'] == 5635 and data['state'] == 'open'\""
  - id: "occ-self-bind-pr-5635-merged"
    description: >-
      Falsifiable replacement for occ-self-bind-pr-5635, APPENDED under OCC append-only doctrine rather
      than edited in place: a merged dod_evidence entry is immutable and its receipt pins the entry hash,
      so an in-place rewrite is rejected by validator_occ_append_only and would invalidate the receipt
      binding. The superseded item asserts data['state'] == 'open' against the live OCC API for companion
      PR #5635. That PR MERGED at 2026-07-30T18:29:51Z (merge commit 9690de99fb47a76f52eb3e06e1c0a6b32dab70db),
      so its state is permanently "closed" and the assertion can never be satisfied again by any action
      on this ticket, this repo, or the product source. It is unsatisfiable-by-construction — the mirror
      image of the OMN-14443 unfalsifiable class: where that class can never go RED, this one can never
      go GREEN, and both are checks wired to nothing. It was emitted on the node_occ_companion_compute
      (OMN-14285) autobind path, which stamped the companion's state at authoring time rather than a terminal
      fact, and it is one of the two probes that made dod_verify correlation 3ccea7e8 (run 150caa0a, 2026-08-01T05:16Z)
      refuse OMN-15418 while every product PR was already merged. Replacement semantics — assert the terminal,
      immutable fact instead of the transient one: companion #5635 is closed AND merged AND carries merge
      commit 9690de99fb47a76f52eb3e06e1c0a6b32dab70db. Falsifiability was demonstrated rather than asserted:
      the identical shape pointed at OCC #5758 exits 1 (different merge SHA), and pointed at a nonexistent
      repository exits 1 rather than passing on empty stdout. Executor shape — the first shlex token is
      gh, a resolvable executable, and the value contains zero command substitution, so node_dod_verify's
      pre-execution shlex.split shape guard cannot mis-branch on it (the OMN-15597 caution and the OMN-15535
      INVALID_CHECK_VALUE_NOT_A_COMMAND false-RED class). The consumer is grep -Fx redirected to /dev/null
      on fully drained input, avoiding both the grep -q SIGPIPE exit-141 hazard and the grep -c counting
      idiom that fails in both polarities under pipefail. The jq program ANDs three falsifiable conjuncts
      and never compares an identity field against its own selector, so it is not an OMN-15391 Rule C
      tautology.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-5635"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/pulls/5635 --jq '.state == "closed" and .merged == true and .merge_commit_sha == "9690de99fb47a76f52eb3e06e1c0a6b32dab70db"' | grep -Fx true > /dev/null
  - id: "dod-deploy-assessment-falsifiable"
    description: >-
      Falsifiable replacement for dod-deploy-assessment, APPENDED for the same append-only reason as the
      item above. The superseded item pipes PR #2548's own paginated file list into grep -Fx docker/docker-compose.e2e.yml.
      PR #2548 changed 130 files and NOT ONE of them is a compose file — the path does not appear in that
      list at all — so the check exits 1 permanently and no change to the product source, the deploy surface,
      or the runtime can ever drive it GREEN. It was emitted by the node_occ_companion_compute autobind
      path as a generic deploy-scope stamp (F-05) without consulting the PR's actual diff, and it is the
      second of the two probes behind the dod_verify refusal cited above. Note the replacement deliberately
      does NOT re-point the same file-list grep at a path #2548 really changed: #2548 is merged, so its
      file list is immutable, and any grep over it exits 0 unconditionally — that would retire an unsatisfiable
      check by installing an unfalsifiable one, the exact OMN-14443 class that the direct precedent for
      this PR (OCC #5768, merge 7d33955282175aee27c3122a838ffb0a4d4aff77, which retired OMN-15472's dod-deploy-assessment)
      exists to retire. Replacement semantics follow that precedent's mechanism instead — an immutable-ref
      CONTENT probe of the deploy-relevant source at PR #2548's squash merge commit 3863623e4844dc9b0db69ceb4e77942737fbca32,
      asserting the OMN-15418 typed database-ownership wiring is present BOTH as the validator definition
      and at the exactly-one-owner enforcement call site the ticket exists to establish. The probed file,
      src/omnibase_infra/validation/application_relation_ownership.py, was ADDED by #2548 (858 additions)
      and 404s at that merge commit's first parent beebd1d636b9182b5ba80c7b68daf5ecfc7e76d3, so falsifiability
      was demonstrated rather than asserted: the identical probe repointed at that parent exits 1. Executor
      shape — first shlex token is gh, zero command substitution, and each leg terminates in grep -F redirected
      to /dev/null on fully drained input, so neither leg is exposed to the base64 -d SIGPIPE tier-1 producer
      hazard that grep -q would introduce. The ?ref= pin is a 40-hex commit SHA, not a deletable branch
      ref.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: |-
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/validation/application_relation_ownership.py?ref=3863623e4844dc9b0db69ceb4e77942737fbca32' --jq .content | base64 -d | grep -F 'def validate_application_relation_ownership(' > /dev/null && gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/validation/application_relation_ownership.py?ref=3863623e4844dc9b0db69ceb4e77942737fbca32' --jq .content | base64 -d | grep -F 'code=EnumApplicationRelationViolation.DUPLICATE_OWNER,' > /dev/null
  - id: "occ-self-bind-pr-5937"
    description: >-
      Binding stamp, not substantive proof: binds OMN-15418 to OCC evidence PR #5937 — the append-only
      supersession PR that retires this contract's two unsatisfiable generated probes — so occ-preflight
      eligibility can resolve a PASS receipt for this ticket against THIS PR, per the OCC in-repo invariant
      that an OCC PR validates contracts and receipts from its own checkout. The pre-existing occ-self-bind-pr-5635
      stamp binds the earlier autobind companion and is left untouched and verbatim; it is retired only
      as a CHECK (superseded by occ-self-bind-pr-5635-merged above), not as a record, since a ticket may
      be bound by more than one OCC PR over its life. This stamp adjudicates nothing about the OMN-15418
      work and flips no Linear status; the substantive evidence is dod-deploy-assessment-falsifiable and
      occ-self-bind-pr-5635-merged. Mirrors the occ-self-bind-pr-5768 precedent on contracts/OMN-15472.yaml.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5937 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
  - id: "occ-self-bind-pr-5946"
    description: "Binds OMN-15418 to OCC companion PR #5946 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5946 --repo OmniNode-ai/onex_change_control --json number,state"
