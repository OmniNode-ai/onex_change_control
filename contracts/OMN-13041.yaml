---
schema_version: '1.0.0'
ticket_id: OMN-13041
title: 'feat(skills): handoff skill overhaul — claim-certification lint, supersession tombstones, terminal
  commit+push, typed stale-doc findings, generated scorecard, deep-dive reconcile'
summary: >-
  Overhauled /onex:handoff from the deprecated "context save" skill to the final-session enforcement skill
  per PROCESS_FAILURE_RETRO.md §3.C item C-1. Six mechanical behaviors: (a) claim-certification lint,
  (b) supersession tombstones + LATEST.md pointer, (c) terminal commit+push, (d) typed stale-doc findings
  with ModelStaleDocFinding Pydantic model (FIXED:<sha>|DEFERRED:<ticket> only — free text unrepresentable),
  (e) live-gh scorecard with hard-fail on missing/red session-window PRs, and (f) deep-dive reconcile
  with auto-appended SUPERSEDED banner. Target repo: omniclaude. No runtime deployment — skill-only change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >-
      21 unit tests for the skill overhaul pass — SKILL.md structure, prompt.md structure, and ModelStaleDocFinding
      schema validation (FIXED/DEFERRED acceptance, free-text rejection, immutability).
    command: >-
      cd $OMNI_HOME/omni_worktrees/OMN-13041/omniclaude && KAFKA_BOOTSTRAP_SERVERS="" uv run pytest tests/skills/test_handoff_skill.py
      -m unit -q
  - kind: ci
    description: CI pipeline green on omniclaude PR
    command: >-
      env -u GITHUB_TOKEN gh pr checks --repo OmniNode-ai/omniclaude OMN-13041
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >-
      21 unit tests for the handoff skill overhaul pass. Tests cover SKILL.md not-deprecated assertion,
      six behavior sections present, prompt.md gh-command and commit+push steps present, ModelStaleDocFinding
      accepts FIXED/DEFERRED and rejects free text and empty values.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13041/dod-001/command.yaml"
  - id: dod-deploy-gate
    description: >-
      Deploy-gate policy evidence for omniclaude PR #1815: runtime-path node changes are bound to OCC
      evidence and validated at the product PR head; no live deploy or restart was performed by this receipt.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13041/dod-deploy-gate/command.yaml"
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt — this onex_change_control PR carries the OMN-13041 contract and DoD receipts
      consumed by the omniclaude PR via Evidence-Source.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13041/dod-occ-self/command.yaml"
