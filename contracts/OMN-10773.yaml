---
schema_version: "1.0.0"
ticket_id: "OMN-10773"
title: "feat(OMN-10773): atomic env file writer for interactive onboarding config output"
summary: >
  Preserves the explicit write_env_file API on top of ConfigWriter, keeps file writes as caller-controlled
  effects, and verifies merge-preserve atomic env output through unit and integration coverage.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1550 carries the OMN-10773 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10773/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Focused unit, integration, and ruff checks passed for the config writer wrapper."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10773/dod-local-tests/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC contract and receipt update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10773/dod-occ-pr/command.yaml\""
