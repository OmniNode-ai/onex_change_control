---
schema_version: "1.0.0"
ticket_id: "OMN-10778"
title: "feat(OMN-10778): interactive policy + step Pydantic models"
summary: >
  Adds four Pydantic models for interactive branching onboarding policies (ModelInteractiveStep, ModelTransitionBranch,
  ModelTransition, ModelInteractivePolicy) plus a re-export shim and graph integrity validation at construction
  time.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1551 carries the OMN-10778 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10778/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Focused unit and integration checks passed for interactive policy models."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10778/dod-local-tests/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC contract and receipt update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10778/dod-occ-pr/command.yaml\""
