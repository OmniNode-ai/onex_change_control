---
schema_version: "1.0.0"
ticket_id: "OMN-10778"
title: "feat(OMN-10778): interactive policy and step Pydantic models"
summary: >
  Adds typed interactive onboarding policy model validation, keeps graph integrity in Pydantic models,
  and proves the re-exported model surface through focused unit and integration coverage.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1551 carries the OMN-10778 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10778/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Focused unit, integration, and ruff checks passed for the interactive model graph."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10778/dod-local-tests/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC contract and receipt update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10778/dod-occ-pr/command.yaml\""
