---
schema_version: "1.0.0"
ticket_id: OMN-9615
title: "Wire hook-bits drift into pre-commit"
summary: "Wire gen_hook_bits.py --check into omnibase_core pre-commit so hook_bits.sh drift is blocked
  when EnumHookBit or the generator changes."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Focused gen_hook_bits and hook-bits pre-commit wiring tests pass on omnibase_core PR
      #1012 head"
    command: "env -u PYTHONPATH uv run pytest tests/unit/scripts/test_gen_hook_bits.py tests/unit/scripts/test_gen_hook_bits_precommit.py
      -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused gen_hook_bits and hook-bits pre-commit wiring tests pass on omnibase_core PR
      #1012 head"
    source: generated
    checks:
      - check_type: command
        check_value: "env -u PYTHONPATH uv run pytest tests/unit/scripts/test_gen_hook_bits.py tests/unit/scripts/test_gen_hook_bits_precommit.py
          -v"
