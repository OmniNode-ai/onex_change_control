---
schema_version: "1.0.0"
ticket_id: "OMN-12573"
title: "Re-point runtime rebuild CI to node_redeploy redeploy-start"
summary: >-
  Bind central evidence for omnibase_infra PR #1831, which re-points runtime rebuild CI away from directly
  publishing deploy-agent rebuild-requested commands and instead publishes the node_redeploy start command
  with the triggering base branch, lane, and merge SHA. node_redeploy remains the only emitter of deploy-agent
  rebuild-requested commands.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
  - "envelopes"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1831"
    description: "omnibase_infra PR #1831 carries the runtime rebuild trigger re-point to redeploy-start.v1."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12573/dod-infra-pr-1831/command.yaml"
          && grep -q '^pr_number: 1831$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12573/dod-infra-pr-1831/command.yaml"
          && grep -q '^commit_sha: "f8c08c438b1a7d62e1859ea1cf2af70ab2d6b3ce"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12573/dod-infra-pr-1831/command.yaml"
  - id: "dod-local-validation"
    description: "Script, redeploy, CI topic parity, ruff, mypy, and pre-commit validation passed for
      the implementation branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12573/dod-local-validation/command.yaml"
          && grep -q '563 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12573/dod-local-validation/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12573/dod-local-validation/command.yaml"
  - id: "dod-topic-boundary"
    description: "The trigger publishes onex.cmd.omnimarket.redeploy-start.v1 with lane/ref and no longer
      publishes deploy-agent rebuild-requested.v1 directly."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12573/dod-topic-boundary/command.yaml"
          && grep -q 'onex.cmd.omnimarket.redeploy-start.v1' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12573/dod-topic-boundary/command.yaml"
          && grep -q 'sole emitter' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12573/dod-topic-boundary/command.yaml"
  - id: "dod-occ-repair-pr"
    description: "Green OCC repair PR creates the missing OMN-12573 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12573/dod-occ-repair-pr/command.yaml"
