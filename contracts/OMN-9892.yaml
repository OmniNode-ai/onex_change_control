---
# ModelTicketContract — Deploy Gate caller fix (parent epic OMN-9876)
# Repoints omnibase_core/.github/workflows/deploy-gate.yml from a deleted-branch SHA to @main.
# Receipts migrated from check_type=file_exists to check_type=command in OMN-9893
# (post-OMN-9788 receipt_gate hardening; file_exists is in WEAK_PROOF_CHECK_TYPES).
schema_version: "1.0.0"
ticket_id: OMN-9892
title: "fix(deploy-gate): repoint omnibase_core caller from orphan SHA 16d9355f to @main — unblocks merge
  queue (~26 PRs) where every Deploy Gate run since 2026-04-25 17:12Z fails with workflow-load error because
  the pinned SHA is on a deleted branch unreachable to Actions cross-repo `uses:`"
summary: "fix(deploy-gate): repoint omnibase_core caller from orphan SHA 16d9355f to @main — unblocks
  merge queue (~26 PRs) where every Deploy Gate run since 2026-04-25 17:12Z fails with workflow-load error
  because the pinned SHA is on a deleted branch unreachable to Actions cross-repo `uses:`"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: omnibase_core PR CI checks pass
    command: gh pr checks <pr-number> --repo OmniNode-ai/omnibase_core --watch
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "deploy-gate.yml line 24 references @main, not the orphan SHA — receipt at drift/dod_receipts/OMN-9892/dod-001/command.yaml"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9892/dod-001/command.yaml && grep
          -q '^evidence_item_id: dod-001$' drift/dod_receipts/OMN-9892/dod-001/command.yaml && grep -q
          '^ticket_id: OMN-9892$' drift/dod_receipts/OMN-9892/dod-001/command.yaml && grep -q 'deploy-gate-reusable.yml@main'
          drift/dod_receipts/OMN-9892/dod-001/command.yaml"
  - id: dod-002
    description: "actionlint passes on the updated workflow file — receipt at drift/dod_receipts/OMN-9892/dod-002/command.yaml"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9892/dod-002/command.yaml && grep
          -q '^evidence_item_id: dod-002$' drift/dod_receipts/OMN-9892/dod-002/command.yaml && grep -q
          '^ticket_id: OMN-9892$' drift/dod_receipts/OMN-9892/dod-002/command.yaml && grep -q 'actionlint'
          drift/dod_receipts/OMN-9892/dod-002/command.yaml"
