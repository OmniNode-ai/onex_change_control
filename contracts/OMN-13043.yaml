---
schema_version: "1.0.0"
ticket_id: "OMN-13043"
title: "feat(OMN-13043): typed Durable-findings / Runtime-state-as-of sections in deep-dive generator"
summary: >-
  C-3 retro item: generate_deep_dive.py emits typed Durable Findings and Runtime State sections, and adds
  runtime-state timestamp parsing plus an ingest freshness validator so stale runtime observations cannot
  poison the deep-dive corpus.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Focused deep-dive typed-section unit tests pass."
    command: "uv run pytest tests/unit/scripts/test_deep_dive_typed_sections.py -q"
  - kind: ci
    description: "Ruff and pre-commit pass on the changed files."
    command: "uv run ruff check scripts/generate_deep_dive.py tests/unit/scripts/test_deep_dive_typed_sections.py"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: >-
      17 unit tests pass for runtime-state timestamp parsing, ingest freshness validation, generated Durable
      Findings section, and generated Runtime State section.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13043/dod-001/command.yaml"
  - id: dod-002
    description: >-
      parse_runtime_state_timestamp and validate_deep_dive_ingest are present in scripts/generate_deep_dive.py
      on omnibase_infra PR #2135.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13043/dod-002/command.yaml"
  - id: dod-003
    description: >-
      Generated output emits Durable Findings and Runtime State section headers and the generated output
      passes its own validate-ingest gate.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13043/dod-003/command.yaml"
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt: this onex_change_control branch carries the OMN-13043 contract and receipts
      consumed by omnibase_infra PR #2135.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13043/dod-occ-self/command.yaml"
