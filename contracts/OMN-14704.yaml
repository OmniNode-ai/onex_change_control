---
schema_version: "1.0.0"
ticket_id: "OMN-14704"
title: "Autobind OCC evidence for OMN-14704"
summary: >
  OCC contract bound by C3 manual merge lane for onex_change_control#4309, proving the Phase 3 shadow
  Product Readiness diff and OCC self-bind before the receipt gate is rerun.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #4309 product diff scope present"
    command: "PRODUCT_PR_NUMBER=4309; PRODUCT_REPO=OmniNode-ai/onex_change_control; gh pr diff ${PRODUCT_PR_NUMBER}
      --repo ${PRODUCT_REPO} --name-only | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-onex_change_control-pr-4309"
    description: "Product PR exists for OMN-14704"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=4309; PRODUCT_REPO=OmniNode-ai/onex_change_control; gh pr view
          ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --json number,state,headRefName"
  - id: "dod-OmniNode-ai-onex_change_control-pr-4309-ci"
    description: "Product PR diff is non-empty for OMN-14704"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=4309; PRODUCT_REPO=OmniNode-ai/onex_change_control; gh pr diff
          ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --name-only | grep -q ."
  - id: "occ-self-bind-pr-4309"
    description: "OCC PR #4309 exists and binds OMN-14704 evidence"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4309; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
