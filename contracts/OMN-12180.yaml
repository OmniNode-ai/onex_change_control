---
schema_version: "1.0.0"
ticket_id: "OMN-12180"
title: "Test critical services in omnibase_core"
summary: >
  Central OCC evidence for omnibase_core PR #1167. The change adds focused
  unit coverage for high-complexity workflow, compute, and effect code paths
  in omnibase_core.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused critical service tests pass on the omnibase_core PR head."
    command: "See drift/dod_receipts/OMN-12180/dod-critical-service-tests/command.yaml"
  - kind: "tests"
    description: "Targeted ruff format and lint checks pass for the new critical test files."
    command: "See drift/dod_receipts/OMN-12180/dod-targeted-ruff/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carries the central OMN-12180 contract and receipts."
    command: "See drift/dod_receipts/OMN-12180/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-critical-service-tests"
    description: "Focused critical service tests passed on the omnibase_core PR #1167 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12180/dod-critical-service-tests/command.yaml
          && grep -q '46 passed' drift/dod_receipts/OMN-12180/dod-critical-service-tests/command.yaml"
  - id: "dod-targeted-ruff"
    description: "Targeted ruff format and lint checks passed for the critical test files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12180/dod-targeted-ruff/command.yaml
          && grep -q 'All checks passed' drift/dod_receipts/OMN-12180/dod-targeted-ruff/command.yaml"
  - id: "dod-occ-pr"
    description: "This onex_change_control PR carries the central OMN-12180 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12180/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: \"OMN-12180\"$' drift/dod_receipts/OMN-12180/dod-occ-pr/command.yaml"
