---
schema_version: "1.0.0"
ticket_id: "OMN-16899"
title: "Autobind OCC evidence for OMN-16899"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omniintelligence PR #866.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #866 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #866 on OmniNode-ai/omniintelligence, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omniintelligence`.
    command: |-
      uv run pytest src/omniintelligence/nodes/node_pattern_demotion_effect/node_tests/test_demotion.py tests/unit/nodes/node_pattern_demotion_effect/test_handler_demotion.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniintelligence-pr-866"
    description: "PR #866 on OmniNode-ai/omniintelligence — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniintelligence/contents/tests/unit/nodes/node_pattern_demotion_effect/test_handler_demotion.py?ref=208408ed725016803981d4e7a429e00d7ec47585 --jq '.content' | base64 -d | grep -c 'def test_naive_utc_input_matches_aware_utc_input'
      - check_type: "command"
        check_value: "gh pr view 866 --repo OmniNode-ai/omniintelligence --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #866 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniintelligence/pulls/866/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #866 on OmniNode-ai/omniintelligence — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest src/omniintelligence/nodes/node_pattern_demotion_effect/node_tests/test_demotion.py tests/unit/nodes/node_pattern_demotion_effect/test_handler_demotion.py -q
        cwd: "${OMNI_HOME}/omniintelligence"
