---
schema_version: 1.0.0
ticket_id: OMN-15021
title: Autobind OCC evidence for OMN-15021
summary: 'OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2425.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #2425 product diff scope present'
    command: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-OmniNode-ai-omnibase_infra-pr-2425
    description: "PR #2425 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-OmniNode-ai-omnibase_infra-pr-2425-ci
    description: "PR #2425 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
  - id: occ-self-bind-pr-4722
    description: "OCC companion PR #4722 — self-bind for OMN-15021 (OMN-14650)."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-omnibase_infra-pr-2425-deploy-gate-disposition
    description: 'PR #2425 deploy-gate disposition -- runtime handler path touched and post-merge dev
      redeploy validation bar is explicit in the PR body.'
    source: manual
    checks:
      - check_type: command
        check_value: 'gh pr view 2425 --repo OmniNode-ai/omnibase_infra --json body,files --jq ''if ((.body
          | test("Post-merge dev redeploy")) and any(.files[].path; . == "src/omnibase_infra/nodes/node_dlq_replay_effect/handlers/handler_dlq_replay.py"))
          then "deploy gate disposition verified: runtime handler path touched; PR body carries post-merge
          dev redeploy validation bar" else error("deploy gate disposition missing runtime path or PR
          body deploy bar") end'''
