---
schema_version: "1.0.0"
ticket_id: "OMN-10358"
title: "Task 6: [Wave 2 Completion-Verify] Implement identifier extraction"
summary: "Add deterministic identifier extraction for completion-verify in omnibase_core, covering backticks,
  camelCase, PascalCase, dotted config keys, path exclusion, and deduplication."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Completion-verify identifier extraction focused tests are present and covered by the
      implementation PR"
    command: "gh pr diff 990 --repo OmniNode-ai/omnibase_core | grep -E 'completion_verify.py|test_completion_verify_extract.py'"
  - kind: "ci"
    description: "Implementation PR #990 exists in omnibase_core; CodeQL is tracked separately from this
      OCC evidence repair"
    command: "gh pr view 990 --repo OmniNode-ai/omnibase_core --json number,state,url,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Completion-verify source and focused tests are present in omnibase_core PR #990"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10358/dod-001/command.yaml && grep
          -q 'completion_verify.py' drift/dod_receipts/OMN-10358/dod-001/command.yaml && grep -q 'test_completion_verify_extract.py'
          drift/dod_receipts/OMN-10358/dod-001/command.yaml"
  - id: "dod-002"
    description: "omnibase_core PR #990 records CodeQL as a separate downstream blocker, not an OCC receipt
      blocker"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10358/dod-002/command.yaml && grep
          -q 'CodeQL: FAILURE' drift/dod_receipts/OMN-10358/dod-002/command.yaml && grep -q 'separate
          worker' drift/dod_receipts/OMN-10358/dod-002/command.yaml"
  - id: "dod-003"
    description: "omnibase_core implementation PR #990 exists for OMN-10358"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10358/dod-003/command.yaml && grep
          -q '^pr_number: 990$' drift/dod_receipts/OMN-10358/dod-003/command.yaml && grep -q 'e4d9209feeb9b639225bb6194b22a43fbfa7358a'
          drift/dod_receipts/OMN-10358/dod-003/command.yaml"
