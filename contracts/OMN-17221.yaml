---
schema_version: "1.0.0"
ticket_id: "OMN-17221"
title: "Autobind OCC evidence for OMN-17221"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3092.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3092 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3092 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/unit/scripts/test_prepush_host_table.py tests/unit/scripts/test_push_lane_scripts_versioned.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3092"
    description: "PR #3092 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/push_lanes/detect_foreign_prepush.py?ref=50d45a7f824acdbe7daeb8455cd974019178e7eb --jq '.content' | base64 -d | grep -c 'class ProcessInfo'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3092/files --paginate --jq '.[].filename' | grep -cFx 'docs/runbooks/lab-prepush-host-table.md'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3092 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/scripts/test_prepush_host_table.py tests/unit/scripts/test_push_lane_scripts_versioned.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-7914"
    description: "Binds OMN-17221 to OCC companion PR #7914 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7914 --repo OmniNode-ai/onex_change_control --json number,state"
