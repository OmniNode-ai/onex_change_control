---
schema_version: "1.0.0"
ticket_id: "OMN-12881"
title: "[build] Guardrail: require event publisher injection for side-emitting auto-wired handlers"
summary: >-
  Tracks enforcement proving auto-wired handlers that side-emit events receive a publisher/event bus injection
  and fail fast when the publisher dependency is missing.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-side-emit-publisher-injection"
    description: >-
      Failing side-emitting handler fixture without publisher injection trips the guard; green fixture proves
      injected publisher emits the expected side event.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12881/dod-side-emit-publisher-injection/command.yaml"
