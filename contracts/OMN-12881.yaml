---
schema_version: "1.0.0"
ticket_id: "OMN-12881"
title: "[build] Guardrail: require event publisher injection for side-emitting auto-wired handlers"
summary: >-
  Tracks enforcement proving auto-wired handlers that side-emit events receive a publisher/event bus injection
  and fail fast when the publisher dependency is missing.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused publisher-injection guardrail tests pass for failing, green, and crash-proof
      fixtures."
    command: "uv run pytest tests/unit/validators/test_publisher_injection_gate.py -q"
  - kind: "tests"
    description: "Validator suite, strict mypy, ruff format, and ruff check proof is recorded in the product
      PR body."
    command: "uv run pytest tests/unit/validators/ -q && uv run mypy src/omnibase_core/validators/publisher_injection_gate.py
      src/omnibase_core/models/validation/model_publisher_injection_finding.py --strict && uv run ruff
      format --check src/ tests/ && uv run ruff check src/ tests/"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-side-emit-publisher-injection"
    description: >-
      Failing side-emitting handler fixture without publisher injection trips the guard; green fixture
      proves injected publisher emits the expected side event.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12881/dod-side-emit-publisher-injection/command.yaml"
  - id: "dod-omnibase-core-pr-1316"
    description: >-
      omnibase_core PR #1316 carries the static publisher-injection validator, finding model, and regression
      tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12881/dod-omnibase-core-pr-1316/command.yaml"
  - id: "dod-deploy-no-live-mutation"
    description: >-
      Core validator and tests only; no live deploy, runtime restart, broker mutation, migration, or runtime
      state mutation is required before merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12881/dod-deploy-no-live-mutation/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-12881 contract and receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12881/dod-occ-pr-self/command.yaml"
