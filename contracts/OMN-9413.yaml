---
schema_version: "1.0.0"
ticket_id: OMN-9413
title: "fix(OMN-9413): node_registration_orchestrator subscribes all 7 declared topics"
summary: "Regression test proving all 7 contract.yaml subscribe_topics receive consumer subscriptions
  via subscribe_wired_contract_topics"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Integration regression test verifies all 7 declared subscribe_topics get subscribed
      via auto-wiring path"
    command: "uv run pytest tests/integration/registration/test_registration_orchestrator_all_topics_subscribed.py
      -v -q"
  - kind: ci
    description: "omnibase_infra PR #1634 carries regression test and CI evidence for OMN-9413"
    command: "gh pr checks 1634 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Regression tests pass: both test_registration_orchestrator_contract_has_7_subscribe_topics
      and test_registration_orchestrator_all_declared_topics_get_subscribed"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9413/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-9413$' drift/dod_receipts/OMN-9413/dod-001/command.yaml && grep -q '^evidence_item_id:
          dod-001$' drift/dod_receipts/OMN-9413/dod-001/command.yaml"
  - id: dod-002
    description: "PR #1634 is open and CI passes in omnibase_infra"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9413/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-9413$' drift/dod_receipts/OMN-9413/dod-002/command.yaml && grep -q '^evidence_item_id:
          dod-002$' drift/dod_receipts/OMN-9413/dod-002/command.yaml"
  - id: dod-occ-pr-1094
    description: "onex_change_control PR #1094 carries the OMN-9413 regression test contract and receipts."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9413/dod-occ-pr-1094/command.yaml
          && grep -q '^pr_number: 1094$' drift/dod_receipts/OMN-9413/dod-occ-pr-1094/command.yaml"
