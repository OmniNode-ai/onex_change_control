---
schema_version: "1.0.0"
ticket_id: "OMN-16170"
title: "OCC evidence for OMN-16170 — raise sqlparse floor to 0.6.0 (3 HIGH CVEs)"
summary: >
  OCC contract for OmniNode-ai/omnibase_infra PR #2783. Raises the sqlparse dependency floor to 0.6.0
  in pyproject.toml (+ uv.lock relock) to close 3 HIGH-severity CVEs. Unblocks every omninode-runtime
  image rebuild org-wide, which was pinned on the vulnerable sqlparse floor.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2783 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2783"
    description: "PR #2783 on OmniNode-ai/omnibase_infra — sqlparse floor raised to 0.6.0 in pyproject.toml,
      uv.lock relocked."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2783 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2783 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2783 clears the deploy-gate — dependency-floor bump only, no
      nodes/handlers/runtime/services/docker path touched."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2783 --repo OmniNode-ai/omnibase_infra --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-deploy-assessment-falsifiable"
    description: >-
      Falsifiable replacement for dod-deploy-assessment, APPENDED under OCC append-only doctrine rather
      than edited in place: a merged dod_evidence entry is immutable and its receipt pins the entry hash,
      so an in-place rewrite is rejected by validator_occ_append_only and would invalidate the receipt
      binding. The superseded item ran gh pr diff 2783 --repo OmniNode-ai/omnibase_infra --name-only piped
      into grep -ciE over PR #2783's own changed-path list, matched against a fixed keyword set that never
      matches (this PR touches only pyproject.toml and uv.lock) — grep exits 1 on zero matches, so the
      check BLOCKs precisely because the true, correct answer ("no deploy-relevant path touched") is 0.
      The command-position tokens are just gh + grep, neither of which is a live-surface probe (OMN-14443
      classify_check_value verdict, reproduced live: commands found ['gh', 'grep'], none in LIVE_PROBE_COMMANDS).
      It is unfalsifiable by construction — the OMN-14443 class, same defect shape already fixed on contracts/OMN-16071.yaml
      via dod-deploy-assessment-falsifiable. Replacement semantics — an immutable-ref CONTENT probe of
      PR #2783's actual dependency-floor bump at its head SHA 1bdfdcf051e26e89af1bec8fddec4b5334236a4d
      (permanently resolvable via GitHub's retained refs/pull/2783/head even after the source branch is
      deleted post-squash-merge), asserting the raised sqlparse floor is present in both pyproject.toml
      and the relocked uv.lock. Live-verified RED at PR #2783's base b99e1a9a90dcf889ef4e77d02b52845b85bb3a8c
      (neither string present, grep exit 1) and GREEN at its head (both present, exit 0) before this entry
      was authored — not simulated.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/pyproject.toml?ref=1bdfdcf051e26e89af1bec8fddec4b5334236a4d'
          --jq .content | base64 -d | grep -F 'sqlparse>=0.6.0,<0.7.0' > /dev/null && gh api 'repos/OmniNode-ai/omnibase_infra/contents/uv.lock?ref=1bdfdcf051e26e89af1bec8fddec4b5334236a4d'
          --jq .content | base64 -d | grep -F 'b861c0288ce2fa56209a9a6412d2e066ac664b3873b89c26c9d8415e8e32996f'
          > /dev/null"
  - id: "occ-self-bind-pr-6677"
    description: "Binds OMN-16170 to OCC companion PR #6677 (self-bind)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6677 --repo OmniNode-ai/onex_change_control --json number,state"
