---
schema_version: "1.0.0"
ticket_id: "OMN-8162"
title: "Wire omniclaude pattern-discovered producer"
summary: >
  Binds central OCC evidence for omniclaude PR #1638, which wires emit_pattern_discovered() to the cross-domain
  pattern.discovered.v1 topic and validates the intentional multi-producer topic naming exception.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Pattern-discovered hook emitter unit tests pass on omniclaude PR #1638."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8162/dod-pattern-discovered-tests/command.yaml"
  - kind: "tests"
    description: "Topic naming validation passes with the explicit multi-producer topic exception."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8162/dod-topic-naming/command.yaml"
  - kind: "manual"
    description: "OCC evidence PR receipt is present and PASS."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8162/dod-occ-pr-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pattern-discovered-tests"
    description: "Hook emitter tests prove pattern-discovered success, payload, partition key, and Kafka
      failure behavior."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8162/dod-pattern-discovered-tests/command.yaml
          && grep -q '^pr_number: 1638$' drift/dod_receipts/OMN-8162/dod-pattern-discovered-tests/command.yaml
          && echo 'PASS: pattern-discovered tests are bound to omniclaude PR #1638'"
  - id: "dod-topic-naming"
    description: "Architecture topic naming check passes after documenting the cross-domain multi-producer
      exception."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8162/dod-topic-naming/command.yaml
          && grep -q '^pr_number: 1638$' drift/dod_receipts/OMN-8162/dod-topic-naming/command.yaml &&
          echo 'PASS: topic naming validation is bound to omniclaude PR #1638'"
  - id: "dod-occ-pr-self"
    description: "OCC PR carries the OMN-8162 contract and central receipt bundle."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8162/dod-occ-pr-self/command.yaml
          && grep -q '^pr_number:' drift/dod_receipts/OMN-8162/dod-occ-pr-self/command.yaml && echo 'PASS:
          OCC PR receipt is present'"
