---
schema_version: "1.0.0"
ticket_id: "OMN-14141"
title: "Preserve handler_routing default_handler discovery for Dependabot FastAPI update"
summary: >-
  Bind omnibase_infra PR #2228 to central OCC evidence for the runtime loader compatibility fix needed
  by the FastAPI Dependabot update after dev moved forward. The PR keeps the dependency bump current with
  the already-merged Uvicorn Dependabot update, refreshes the runner-image identity lock, makes the build-loop
  append handler constructible for offline auto-wiring discovery, and teaches package discovery to parse
  package-relative handler_routing default_handler shorthand without weakening the OMN-14141 flat-schema
  fail-closed guard. No live deploy, restart, docker exec, rpk produce, or .201 mutation was performed;
  runtime validation is local tests plus normal CI.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #2228 is open against dev at the bound head SHA"
    command: "gh pr view 2228 --repo OmniNode-ai/omnibase_infra --json state,headRefOid,baseRefName"
  - kind: "tests"
    description: "Focused auto-wiring, default_handler, runner identity, and real-manifest tests pass
      locally"
    command: >-
      uv run pytest tests/integration/infra/test_omn_12765_release_backmerge_identity.py::test_release_backmerge_preserves_runner_identity_lock
      tests/unit/runtime/auto_wiring/test_discovery.py tests/unit/runtime/auto_wiring/test_handler_routing_flat_schema_guard.py
      tests/unit/runtime/test_build_handler_routing_default_handler.py tests/integration/runtime/test_build_loop_auto_wiring_boot.py
      tests/integration/test_auto_wiring_real_manifest.py -q
dod_evidence:
  - id: "dod-omnibase-infra-pr-2228"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2228 at head 2aa4208d8e2b4b2f51ef0dada529e55a0c26ea3f carries
      the FastAPI range update, dev Uvicorn range merge, runner identity refresh, package-relative default_handler
      parser compatibility, and build-loop append offline wiring compatibility.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14141/dod-omnibase-infra-pr-2228/command.yaml"
  - id: "dod-pr-2228-local-validation"
    description: >-
      Local validation passed in the PR worktree: runner image identity verifies, Ruff passed on touched
      Python files, uv lock is consistent, and the focused auto-wiring/default_handler/build-loop/real-manifest/runner-identity
      test slice passed.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14141/dod-pr-2228-local-validation/command.yaml"
  - id: "dod-pr-2228-deploy-plan"
    description: >-
      Runtime deploy-gate binding: PR #2228 touches runtime auto-wiring loader code only to preserve contract
      discovery semantics for the installed omnibase-core compliance_orchestrator contract. No docker
      exec, service deploy, restart, rpk topic produce, or .201 mutation was run; post-merge runtime rollout
      remains the normal dev deploy/redeploy cadence.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14141/dod-pr-2228-deploy-plan/command.yaml &&
          grep -qi 'deploy' drift/dod_receipts/OMN-14141/dod-pr-2228-deploy-plan/command.yaml
  - id: "dod-occ-pr-2228"
    description: >-
      OCC self-binding: this onex_change_control companion PR carries the OMN-14141 contract entry and
      receipts that bind omnibase_infra#2228 to central evidence.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14141/dod-occ-pr-2228/command.yaml"
