---
schema_version: "1.0.0"
ticket_id: "OMN-14141"
title: "Fail-closed guard + CI gate for handler_routing flat-schema silent-zero-parse"
summary: >-
  Bind omnibase_infra#2235 to central OCC evidence for the systemic fix of the handler_routing flat-schema
  silent-zero-parse defect class (root cause behind the WI-14 saga, OMN-14139 / OMN-14135). The implementation
  PR makes runtime/auto_wiring/discovery.py::_parse_handler_routing raise on a flat handler_class/handler_module
  entry (Guard A), makes handler_wiring.py::_commit_contract_wiring return FAILED instead of phantom-WIRED
  when a subscribe contract registers zero dispatchers (Guard B), and ships a new omnibase_infra.validators.handler_routing_schema
  gate wired as a ci.yml step and a pre-commit hook (CLAUDE.md Rule #5 — gate ships WITH the fixes). It
  also converts 7 infra node contracts from flat to nested handler routing. This is a runtime-loader hardening
  plus CI/pre-commit gate and node-contract normalization; it performs no deploy, container restart, docker
  compose against .201, image build/push, rpk produce, or live cluster/runtime mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
evidence_requirements:
  - kind: "ci"
    description: "Implementation PR #2235 is open against omnibase_infra dev at the bound head SHA"
    command: "gh pr view 2235 --repo OmniNode-ai/omnibase_infra --json state,headRefOid,baseRefName"
  - kind: "tests"
    description: "Flat-schema guard and gate tests pass on the implementation PR"
    command: "pytest tests/unit/runtime/auto_wiring/test_handler_routing_flat_schema_guard.py"
dod_evidence:
  - id: "dod-infra-pr-2235"
    description: >-
      omnibase_infra#2235 carries the handler_routing flat-schema fail-closed guards (parse-time raise
      + wire-time FAILED), the new handler_routing_schema CI gate + pre-commit hook, and the 7 flat->nested
      node contract conversions.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14141/dod-infra-pr-2235/command.yaml
          && grep -q '^pr_number: 2235$' drift/dod_receipts/OMN-14141/dod-infra-pr-2235/command.yaml"
  - id: "dod-local-validation"
    description: >-
      Local validation recorded on the implementation PR proves the guards and gate: full-tree loader
      run discovers 111/111 contracts with zero errors, Guard B fires on zero real contracts, 354 auto_wiring
      + kernel-contract tests green, and 11 new flat-schema guard tests green.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14141/dod-local-validation/command.yaml
          && grep -q '111/111' drift/dod_receipts/OMN-14141/dod-local-validation/command.yaml"
  - id: "dod-deploy-scope"
    description: >-
      Deploy scope is explicit: the change touches runtime auto-wiring loader code, a new validator, ci.yml,
      .pre-commit-config.yaml, node contract.yaml files, and tests only. It rides the normal post-merge
      dev deploy/redeploy cadence; no out-of-band .201 mutation or production promotion is requested.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14141/dod-deploy-scope/command.yaml
          && grep -q 'deploy' drift/dod_receipts/OMN-14141/dod-deploy-scope/command.yaml"
  - id: "dod-occ-self"
    description: >-
      OCC self-binding: this onex_change_control companion PR carries the OMN-14141 contract entry and
      receipts that bind omnibase_infra#2235 to the central evidence set. Satisfies this PR's own occ-preflight/receipt-gate
      binding.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14141/dod-occ-self/command.yaml"
