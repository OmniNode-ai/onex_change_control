---
schema_version: "1.0.0"
ticket_id: "OMN-16950"
title: "Autobind OCC evidence for OMN-16950"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #1068
  and OmniNode-ai/omnimemory PR #460.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1068 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #1068 on OmniNode-ai/omninode_infra changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (pyproject.toml, uv.lock) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
  - kind: "ci"
    description: "PR #460 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #460 on OmniNode-ai/omnimemory, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimemory`.
    command: |-
      uv run pytest tests/unit/nodes/test_memory_lifecycle_orchestrator/test_handler_memory_tick.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1068"
    description: "PR #1068 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1068 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1068/files --paginate --jq '.[].filename' | grep -cFx 'pyproject.toml'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1068"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7577"
    description: "Binds OMN-16950 to OCC companion PR #7577 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7577 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimemory-pr-460"
    description: "PR #460 on OmniNode-ai/omnimemory — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimemory/contents/uv.lock?ref=a171bdd865050de48829068d76ca5841e6c6f76f --jq '.content' | base64 -d | grep -cF '>=0.47.0,<0.48.0'
  - id: "dod-OmniNode-ai-omnimemory-pr-460-ci"
    description: "PR #460 on OmniNode-ai/omnimemory — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 460 --repo OmniNode-ai/omnimemory --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #460 on OmniNode-ai/omnimemory — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/test_memory_lifecycle_orchestrator/test_handler_memory_tick.py -q
        cwd: "${OMNI_HOME}/omnimemory"
  - id: "occ-self-bind-pr-7574"
    description: "OCC companion PR #7574 — self-bind for OMN-16950 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7574 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-7580"
    description: "Binds OMN-16950 to OCC companion PR #7580 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7580 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-7598"
    description: "Binds OMN-16950 to OCC companion PR #7598 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7598 --repo OmniNode-ai/onex_change_control --json number,state"
