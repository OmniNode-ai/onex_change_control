---
schema_version: "1.0.0"
ticket_id: "OMN-11772"
title: "Canonical LLM delegation event models and failure taxonomy enums"
summary: >
  Add EnumDelegationFailureClass (9 values) and EnumCostBasis (3 values) StrEnums, plus 7 frozen Pydantic
  event models covering the full LLM delegation lifecycle: requested command, started, completed, escalation-triggered,
  all-tiers-failed, model-degraded, and single-attempt-failed (epic OMN-11771 delegation pipeline).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-event-model-tests"
    description: "18 unit tests pass: enum value validation, frozen enforcement, extra-field rejection,
      cost field precision"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11772/dod-event-model-tests/command.yaml\""
  - id: "dod-omnimarket-pr-770"
    description: "omnimarket PR #770 implements 7 delegation event models, 2 failure taxonomy enums, and
      18 unit tests"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11772/dod-omnimarket-pr-770/command.yaml\"
          && grep -q '^pr_number: 770$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11772/dod-omnimarket-pr-770/command.yaml\"
          && grep -q '^commit_sha: \"78615b71c9eb2f33411780f926e8f1b0d7e65e46\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11772/dod-omnimarket-pr-770/command.yaml\""
