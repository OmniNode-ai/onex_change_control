---
schema_version: "1.0.0"
ticket_id: "OMN-15380"
title: "OCC companion for OmniNode-ai/omnimarket PR #1953 (dod_verify fail-closed on missing contract)"
summary: >
  Hand-authored OCC evidence for OmniNode-ai/omnimarket PR #1953 (OMN-15380: dod_verify previously reported
  success on zero verified checks -- an absent OCC contract read as green because the CLI exit code and
  the RuntimeLocal-mediated envelope both stayed success even though the inner state was status=skipped,
  verified_count=0). #1953 fixes handler_dod_verify.py to stamp a distinct machine-checkable error_message
  (CONTRACT_MISSING / NO_CHECKS_VERIFIED) that RuntimeLocal._classify_result already treats as FAILED,
  and hardens node_dod_verify/__main__.py's CLI exit code + receipt status mapping to require an actual
  VERIFIED status. Authored by a different actor than #1953's implementer (implementer!=author rule) after
  the machine autobind companion OCC#5487 was closed as superseded -- lint-clean only via an improper
  supersedes_dod_evidence pointer to an unrelated admissibility-test check, with zero genuine PR-1953
  binding underneath, and CI-red (Pre-commit/yamlfmt) besides.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1953 CI checks green"
    command: "gh pr checks 1953 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1953"
    description: >
      PR #1953 on OmniNode-ai/omnimarket is OPEN and its diff touches both the CLI exit-code/receipt-status
      fix (node_dod_verify/__main__.py) and the handler error_message fix (handlers/handler_dod_verify.py)
      -- literal PR #1953 pinned in every gh pr call, no ${PR_NUMBER} placeholder.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1953 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: >-
          gh pr diff 1953 --repo OmniNode-ai/omnimarket --name-only | grep -q "src/omnimarket/nodes/node_dod_verify/__main__.py"
          && gh pr diff 1953 --repo OmniNode-ai/omnimarket --name-only | grep -q "src/omnimarket/nodes/node_dod_verify/handlers/handler_dod_verify.py"
  - id: "dod-omnimarket-pr-1953-test-module-content"
    description: >
      Falsifiable content probe: the RED-first regression test module tests/unit/nodes/node_dod_verify/test_omn_15380_fail_closed_missing_contract.py,
      pinned to PR #1953's head commit beb76a4502033e585044a84864417dcf09c23538, contains the test_missing_contract_sets_contract_missing_reason
      assertion (the OMN-15172-class fail-closed regression case). Path and file name verified live via
      gh api contents before authoring this check.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          gh api "repos/OmniNode-ai/omnimarket/contents/tests/unit/nodes/node_dod_verify/test_omn_15380_fail_closed_missing_contract.py?ref=beb76a4502033e585044a84864417dcf09c23538"
          --jq ".content" | base64 -d | grep -c "def test_missing_contract_sets_contract_missing_reason"
  - id: "occ-self-bind-pr-5497"
    description: >
      Binds OMN-15380 to OCC companion PR #5497 (self-bind, hardcoded literal PR number, no ${PR_NUMBER}
      placeholder).
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5497 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-5506"
    description: >
      Binds OMN-15380 to the follow-up OCC companion PR #5506 (self-bind, hardcoded literal PR number,
      no ${PR_NUMBER} placeholder). #5506 re-binds the dod-OmniNode-ai-omnimarket-pr-1953 and dod-omnimarket-pr-1953-test-module-content
      receipts to omnimarket PR #1953's live pr_number/commit_sha via append-only supersession, fixing
      the pr_ticket_mismatch occ-preflight failure on #1953. OCC's own in-repo occ-preflight invariant
      (I3) requires THIS PR's contract/receipts to be self-bound, hence this net-new evidence item rather
      than reusing #5497's.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5506 --repo OmniNode-ai/onex_change_control --json number,state"
