---
schema_version: "1.0.0"
ticket_id: "OMN-16434"
title: "Autobind OCC evidence for OMN-16434"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2180."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2180 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2180"
    description: "PR #2180 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_pr_lifecycle_fix_effect/handlers/occ_evidence_stamp.py?ref=a9fafd1894b6741287d701615fd296c7c2c053f8 --jq '.content' | base64 -d | grep -c 'def derive_behavior_test_paths'
  - id: "dod-OmniNode-ai-omnimarket-pr-2180-ci"
    description: "PR #2180 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2180 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-omn-16434-diff-derived-behavior-proof"
    description: >-
      OMN-15911 behavior proof for the producer fix (omnimarket#2180). Every check the OMN-14285 autobind
      minted onto this contract is exit-status-invariant over the product change or ticket-independent:
      the `?ref=` content read is static inspection (SURROGATE), `gh pr view 2180 --json files` and the
      #7379 self-bind are PR_STATE_SURROGATE/MERGE_STATE, and `uv run pytest tests/test_evidence_admissibility.py
      -q` is on FOREIGN_SUITE_DENYLIST — so node_dod_verify reads behavior_proving_count=0 on the very
      contract for the ticket that exists to fix that. This item runs the producer instead. It executes
      omnimarket's tests/unit/nodes/node_occ_companion_compute/test_autobind_diff_derived_behavior_proof_omn_16434.py,
      which drives the REAL compute_companion_plan seam and asserts: a PR whose diff carries a pytest
      target yields behavior_proving_count >= 1 computed by the real classify_item_checks; the minted
      check names that exact target with cwd ${OMNI_HOME}/<product repo>; the FOREIGN_SUITE_DENYLIST value
      is absent; the item has its backing receipt; and — the RED control — the SAME producer run on the
      SAME PR with the test file removed from changed_files mints no behavior item at all. BEHAVIOR proof
      class (`pytest` is an allowlisted head in omnimarket.nodes.node_dod_verify.services.check_proof_class,
      `uv` a stripped wrapper). Falsifiable, MEASURED both ways below.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/nodes/node_occ_companion_compute/test_autobind_diff_derived_behavior_proof_omn_16434.py
          -q"
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-7379"
    description: "OCC companion PR #7379 — self-bind for OMN-16434 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7379 --repo OmniNode-ai/onex_change_control --json number,state"
