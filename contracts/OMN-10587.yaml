---
schema_version: "1.0.0"
ticket_id: "OMN-10587"
title: "Wire ConfigPrefetcher into kernel via runtime profile"
summary: >
  Add ModelRuntimeProfile schema with prefetch_policy field in omnibase_infra so the kernel bootstrap
  can gate ConfigPrefetcher behaviour per deployment environment. disabled=skip entirely, best_effort=run
  and warn on missing keys, required=run and raise ProtocolConfigurationError on missing keys. Includes
  integration tests for all three policies.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-integration-tests"
    description: "Integration tests for all three prefetch_policy modes and existing isolation tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          if [ "$REPO" = "OmniNode-ai/onex_change_control" ]; then grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10587/dod-unit-integration-tests/command.yaml";
          else uv run pytest tests/integration/test_kernel_prefetcher_wiring.py tests/unit/runtime/test_prefetch_contract_isolation.py
          -q; fi
  - id: "dod-deploy"
    description: >
      Deploy evidence: no live deploy or .201 restart was performed by this PR; post-merge smoke validates
      runtime_profile module is importable in the target runtime image.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10587/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10587 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10587/dod-occ-pr/command.yaml"
