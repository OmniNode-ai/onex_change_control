---
schema_version: "1.0.0"
ticket_id: "OMN-15471"
title: "Bind OCC evidence for delegation provider provenance"
summary: >
  OCC contract for OmniNode-ai/omnibase_infra PR #2561. The product PR derives delegation provenance from
  terminal payload facts instead of defaulting delegated_to to the hardcoded literal local.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2561 CI checks green"
    command: "gh pr checks 2561 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2561"
    description: "PR #2561 on OmniNode-ai/omnibase_infra derives delegated_to from terminal provenance
      facts."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "python -c 'import base64, subprocess, sys; body = base64.b64decode(subprocess.check_output([\"gh\",
          \"api\", \"repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/service_delegation_dispatch_port.py?ref=4d33ed425c8d9134e171560933d4a5680406c525\",
          \"--jq\", \".content\"])).decode(); old = \"normalized.setdefault(\\\"delegated_to\\\", normalized.get(\\\"provider\\\",
          \\\"local\\\"))\"; new = \"normalized.setdefault(\\\"delegated_to\\\", _resolve_delegation_provenance(normalized))\";
          sys.exit(0 if \"def _resolve_delegation_provenance\" in body and old not in body and new in
          body else 1)'"
  - id: "occ-self-bind-pr-5580"
    description: "Binds OMN-15471 to OCC companion PR #5580 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5580 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "dod-deploy-assessment-pr-2561-gh-api"
    description: "Deploy-gate repair for PR #2561: direct gh api exact-head probe plus Python assertion
      over decoded runtime source."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment-pr-2561"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/service_delegation_dispatch_port.py?ref=4d33ed425c8d9134e171560933d4a5680406c525'
          --jq .content > /tmp/omn-15471-source.b64 && base64 -d /tmp/omn-15471-source.b64 > /tmp/omn-15471-source.py
          && python -c 'from pathlib import Path; body = Path(\"/tmp/omn-15471-source.py\").read_text();
          old = \"normalized.setdefault(\\\"delegated_to\\\", normalized.get(\\\"provider\\\", \\\"local\\\"))\";
          new = \"normalized.setdefault(\\\"delegated_to\\\", _resolve_delegation_provenance(normalized))\";
          raise SystemExit(0 if \"def _resolve_delegation_provenance\" in body and old not in body and
          new in body else 1)'"
  - id: "dod-deploy-assessment-pr-2561"
    description: "Deploy-gate assessment for PR #2561: exact-head runtime source contains the provenance
      resolver and omits the removed hardcoded local default line."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2561"
    checks:
      - check_type: "command"
        check_value: "python -c 'import base64, subprocess, sys; body = base64.b64decode(subprocess.check_output([\"gh\",
          \"api\", \"repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/service_delegation_dispatch_port.py?ref=4d33ed425c8d9134e171560933d4a5680406c525\",
          \"--jq\", \".content\"])).decode(); old = \"normalized.setdefault(\\\"delegated_to\\\", normalized.get(\\\"provider\\\",
          \\\"local\\\"))\"; new = \"normalized.setdefault(\\\"delegated_to\\\", _resolve_delegation_provenance(normalized))\";
          sys.exit(0 if \"def _resolve_delegation_provenance\" in body and old not in body and new in
          body else 1)'"
  - id: "occ-self-bind-pr-5581"
    description: "Binds OMN-15471 to OCC companion PR #5581 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5581 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
