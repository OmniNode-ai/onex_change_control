---
schema_version: "1.0.0"
ticket_id: "OMN-13047"
title: "D-1: Concurrency-primitive clause — blocking/consuming/polling primitives require starvation +
  ordering answers pre-mutation"
summary: >
  Retro D-1 (PROCESS_FAILURE_RETRO.md §3.D, failure #8). Adds a CI grep gate (concurrency-primitive-guard)
  to omniclaude that fails any PR diff introducing seek_to_end / poll / KafkaConsumer in non-test Python
  files without a concurrent change under tests/. Enforces that ladder layers 3-4 (starvation analysis
  + publish/subscribe ordering proof) are answered before live mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >
      Unit tests in tests/validation/test_validate_no_blocking_primitive_without_tests.py cover all RED/GREEN
      paths including suppression, CI mode, staged mode.
    command: >
      uv run pytest tests/validation/test_validate_no_blocking_primitive_without_tests.py -v
  - kind: ci
    description: "omniclaude CI green on the implementation PR"
    command: "gh pr checks --repo OmniNode-ai/omniclaude <PR_NUMBER>"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-impl-pr"
    description: >
      omniclaude PR implementing validate_no_blocking_primitive_without_tests.py, concurrency-primitive-guard
      GHA workflow, pre-commit hook registration, and 28 unit tests covering all RED/GREEN paths.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13047/dod-omniclaude-impl-pr/command.yaml
  - id: "dod-occ-pr"
    description: >
      This OCC PR carries the OMN-13047 central contract and the Evidence-Source OCC SHA for the omniclaude
      implementation PR.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13047/dod-occ-pr/command.yaml
