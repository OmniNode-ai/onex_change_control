---
schema_version: "1.0.0"
ticket_id: "OMN-16277"
title: "Raw Bash tool output has no secret-redaction hook: 2 credential leaks 2026-08-19"
summary: >
  OCC contract for OmniNode-ai/omniclaude PR #2004 — new PostToolUse secret-redaction guard for raw Bash
  tool output, fixing OMN-15462 (unbounded URL-userinfo regex) as a prerequisite and closing the clientSecret
  camelCase-key pattern gap. Companion to the autobind companion PR #6751, which self-bound to OMN-15462
  (the prerequisite sub-fix) instead of OMN-16277 (the primary ticket cited in the PR title) — this contract
  closes that gap for the ticket the Receipt Gate resolves from the PR title.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2004 CI checks green"
    command: "gh pr checks 2004 --repo OmniNode-ai/omniclaude"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniclaude-pr-2004-red-before"
    description: "RED-first: both 2026-08-19 leak shapes (synthetic values only) unredacted before the
      fix — proven via test_post_tool_use_secret_redact_guard.py and test_secret_redactor.py OMN-16277
      test classes, which fail against the pre-fix pattern set/module."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "git -C omniclaude show ed25e8f22^:plugins/onex/hooks/lib/secret_redactor.py 2>/dev/null
          | grep -q 'clientSecret' && echo MISSING_BEFORE_FIX || echo CONFIRMED_MISSING_BEFORE_FIX"
  - id: "dod-OmniNode-ai-omniclaude-pr-2004-green-after"
    description: "GREEN-after: 246 tests passed, 1 skipped (tests/hooks/test_secret_redactor.py, test_post_tool_use_secret_redact_guard.py,
      test_subagent_secret_leak_guard.py, test_schemas.py, tests/unit/hooks/test_skill_output_suppressor.py,
      test_subagent_report_contract_guard.py), run on the .200 clean host."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks 2004 --repo OmniNode-ai/omniclaude"
  - id: "dod-OmniNode-ai-omniclaude-pr-2004-mypy-full"
    description: "uv run mypy src/ --config-file mypy.ini (CI-matching invocation) — Success: no issues
      found in 714 source files, run on .200."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2004 --repo OmniNode-ai/omniclaude --json number,state"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omniclaude-pr-2004-green-after"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
