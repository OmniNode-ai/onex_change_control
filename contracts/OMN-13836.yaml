---
schema_version: "1.0.0"
ticket_id: "OMN-13836"
title: "fix(OMN-13836): drop omnimarket self-referential uv overrides"
summary: >-
  omnimarket PR #1562 removes git-URL override-dependencies for ONEX core packages and raises the direct
  omnibase-core / omnibase-spi dependency ranges to the current resolvable line. This keeps omnimarket
  resolvable as a declared dependency without imposing foreign git URL requirements on consumers.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1562"
    description: >-
      Consuming-PR binding: omnimarket PR #1562 carries the pyproject dependency override change and regression
      test at the recorded head commit.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13836/dod-omnimarket-pr-1562/command.yaml"
  - id: "dod-local-validation"
    description: >-
      Local validation recorded during merge sweep: uv lock remains clean on dev and the OMN-13836 regression
      test passes.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13836/dod-local-validation/command.yaml"
  - id: "dod-occ-self"
    description: >-
      This onex_change_control PR carries the central OMN-13836 contract and PASS receipts consumed by
      omnimarket PR #1562 through OCC preflight and Receipt Gate.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13836/dod-occ-self/command.yaml"
