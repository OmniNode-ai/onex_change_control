---
schema_version: "1.0.0"
ticket_id: "OMN-10725"
title: "Add CI gates blocking direct bus instantiation and raw sqlite3 in handlers"
summary: >
  Binds omniclaude PR #1546, which adds AST-based pre-commit and CI validators to enforce EventBus DI
  injection and sqlite3 adapter gating, to central OCC evidence so the Receipt Gate can verify the architectural
  enforcement gates shipped correctly.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1546-head"
    description: "omniclaude PR #1546 carries the DI enforcement validators at the recorded implementation
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10725/dod-pr-1546-head/command.yaml\""
  - id: "dod-ci-1546-green"
    description: "All non-receipt CI checks on omniclaude PR #1546 are passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10725/dod-ci-1546-green/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10725/dod-occ-pr/command.yaml\""
