---
schema_version: "1.0.0"
ticket_id: "OMN-16385"
title: "omnibase-infra-migrate: drop dead CREATE EXTENSION pgcrypto from migration 045"
summary: >
  Root-caused and fixed the silently-failing omnibase-infra-migrate job on deploy-onex-staging. Live-reproduced
  via an instrumented whole-run rerun (run 32584529033): migration 045 died with an explicit permission-denied
  error on CREATE EXTENSION pgcrypto against RDS-managed omnibase_infra (the migration role does not own
  the database). gen_random_uuid() is PostgreSQL 13+ core, so the extension statement was unneeded. Fix
  lands as OmniNode-ai/omnibase_infra PR #2844.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2844 CI checks"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2844"
    description: "PR #2844 on OmniNode-ai/omnibase_infra — Evidence-Source binding."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2844 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid"
      - check_type: "command"
        check_value: "gh pr view 2844 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2844"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
