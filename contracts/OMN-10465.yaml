---
schema_version: "1.0.0"
ticket_id: OMN-10465
title: "Wire Build Loop + Merge Sweep as Event-Bus-Driven Nodes"
summary: "Converts the build-loop and merge-sweep cron scripts into proper omnimarket nodes triggered
  by the ONEX event bus, replacing polling-based launchd invocations with contract-driven Kafka consumers."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
  - "public_api"
evidence_requirements:
  - kind: tests
    description: "35 unit tests pass covering bus trigger handling, node entry-points, and dispatch paths"
    command: "cd ${OMNI_HOME:-/Users/jonah/Code/omni_home}/omni_worktrees/OMN-10465/omnimarket && env
      -u PYTHONPATH uv run pytest tests/unit/nodes/node_build_loop tests/unit/nodes/node_merge_sweep -q"
  - kind: ci
    description: "CodeRabbit review passed with no unresolved threads"
    command: "gh pr view 493 --repo OmniNode-ai/omnimarket --json reviews,reviewDecision"
  - kind: ci
    description: "Hostile reviewer passed — no blocking findings"
    command: "gh pr checks 493 --repo OmniNode-ai/omnimarket"
  - kind: manual
    description: "Node entry-points registered in omnimarket pyproject.toml console_scripts"
    command: "cd ${OMNI_HOME:-/Users/jonah/Code/omni_home}/omni_worktrees/OMN-10465/omnimarket && grep
      -E 'node_build_loop|node_merge_sweep' pyproject.toml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "35 unit tests pass covering bus trigger handling, node entry-points, and dispatch paths"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10465/dod-001/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10465/dod-001/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10465/dod-001/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-002
    description: "CodeRabbit review passed with no unresolved threads"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10465/dod-002/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10465/dod-002/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10465/dod-002/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-003
    description: "Hostile reviewer passed — no blocking findings"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10465/dod-003/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10465/dod-003/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10465/dod-003/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-004
    description: "Node entry-points registered in omnimarket pyproject.toml console_scripts"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10465/dod-004/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10465/dod-004/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10465/dod-004/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
