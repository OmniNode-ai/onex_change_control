---
schema_version: "1.0.0"
ticket_id: "OMN-14211"
title: "chore(OMN-14211): drop stale omnimarket imperative-contract allowlist entries"
summary: >-
  Mechanical imperative-contract ratchet for omnimarket. Removes five stale missing_handler_routing allowlist
  entries from allowlists/omnimarket.yaml after verifying that the referenced files are either gone from
  the dev tree or now compliant. The reduced allowlist preserves a zero-new-violation imperative-contract
  result and changes only the central OCC allowlist.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-allowlist-ratchet"
    description: >-
      The omnimarket imperative-contract scan remains green after removing five stale missing_handler_routing
      allowlist entries; no removed path appears in the live violation lists.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14211/dod-omnimarket-allowlist-ratchet/command.yaml
          && grep -q '^pr_number: 3764$' drift/dod_receipts/OMN-14211/dod-omnimarket-allowlist-ratchet/command.yaml
  - id: "dod-occ-companion-pr-3764"
    description: >-
      OCC self-binding: onex_change_control PR #3764 carries the OMN-14211 contract and receipt set for
      the omnimarket imperative-contract allowlist ratchet, with no runtime or deploy scope.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14211/dod-occ-companion-pr-3764/command.yaml
          && grep -q '^pr_number: 3764$' drift/dod_receipts/OMN-14211/dod-occ-companion-pr-3764/command.yaml
