---
schema_version: "1.0.0"
ticket_id: "OMN-10780"
title: "feat(OMN-10780): transition reducer — pure state machine for interactive onboarding"
summary: >
  Pure state machine that advances through interactive onboarding steps, evaluating transitions using
  condition expressions, applying state updates, and interpolating env output templates. Graph integrity
  validated at construction time.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1552 carries the OMN-10780 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10780/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Unit and integration tests for transition reducer pass (45 tests)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10780/dod-local-tests/command.yaml\""
  - id: "dod-occ-contract"
    description: "OCC contract exists for OMN-10780."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10780/dod-occ-contract/command.yaml\""
