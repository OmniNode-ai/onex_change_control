---
schema_version: "1.0.0"
ticket_id: "OMN-12390"
title: "Consolidate omniclaude hook subscriber loops with coverage"
summary: >
  Adds 35 new unit tests covering the run_subscriber loop contract across all three hook subscribers (skill_execution_log,
  compliance_result, decision_record) and the _emit_event/_emit_injection_record paths in HandlerContextInjection.
  Tests cover stop_event behavior, error handling, and event emission contract. No source changes. Shipped
  via omniclaude PR #1704.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "35 new behavior tests for hook subscriber loops and event emission all pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12390/dod-focused-tests/command.yaml\"
          && grep -q 'passed' \"drift/dod_receipts/OMN-12390/dod-focused-tests/command.yaml\""
  - id: "dod-omniclaude-pr-1704"
    description: "omniclaude PR #1704 carries the subscriber loop and emit behavior tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12390/dod-omniclaude-pr-1704/command.yaml\"
          && grep -q '^pr_number: 1704$' \"drift/dod_receipts/OMN-12390/dod-omniclaude-pr-1704/command.yaml\""
