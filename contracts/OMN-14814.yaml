---
schema_version: "1.0.0"
ticket_id: "OMN-14814"
title: "Autobind OCC evidence for OMN-14814"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1829."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1829 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1829"
    description: "PR #1829 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1829-ci"
    description: "PR #1829 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4451"
    description: "OCC companion PR #4451 — self-bind for OMN-14814 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1829-deploy-scope"
    description: "omnimarket#1829 touches runtime deploy-sensitive prod-promotion/redeploy paths; deploy-scope
      evidence is declared for OMN-14814."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files # deploy"
  - id: "occ-self-bind-pr-4452"
    description: "OCC deploy-scope repair PR #4452 — self-bind for OMN-14814."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
