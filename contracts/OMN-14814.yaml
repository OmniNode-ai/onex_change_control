---
schema_version: "1.0.0"
ticket_id: "OMN-14814"
title: "Remove prod-promotion dual-control solo CODEOWNER"
summary: "Central OCC evidence for omnimarket#1829. The product PR removes prod-promotion dual-control
  assumptions for the solo CODEOWNER path and touches runtime deploy-sensitive code, so the contract declares
  deploy-scope DoD evidence without performing a live deploy or .201 mutation."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket#1829 product diff scope is inspectable"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1829-deploy-scope"
    description: "omnimarket#1829 touches runtime deploy-sensitive prod-promotion/redeploy paths; deploy-scope
      evidence is declared for OMN-14814."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files # deploy"
