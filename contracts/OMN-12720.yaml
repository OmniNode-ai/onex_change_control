---
schema_version: "1.0.0"
ticket_id: "OMN-12720"
title: "Publish correlated failure events for delegation inference-intent validation failures"
summary: >-
  Central evidence for omnibase_infra PR #1873. Runtime auto-wiring now keeps malformed canonical ModelInferenceIntent
  payloads that declare intent=llm_inference on the inference handler path and converts pre-handler event-model
  import/schema validation failures into correlated ModelInferenceResponseData error responses on onex.evt.omnibase-infra.inference-response.v1.
  Existing provider/runtime failures inside HandlerInferenceIntent.handle() remain unchanged.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "protocols"
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-inference-validation-regression"
    description: >-
      Focused auto-wiring regression proves a malformed ModelInferenceIntent with a valid correlation
      id produces a ModelInferenceResponseData error and publishes to the inference-response topic.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-inference-validation-regression/command.yaml"
          && grep -q '3 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-inference-validation-regression/command.yaml"
          && grep -q 'onex.evt.omnibase-infra.inference-response.v1' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-inference-validation-regression/command.yaml"
  - id: "dod-auto-wiring-suite"
    description: >-
      The full auto-wiring unit suite passes with the inference-intent matcher/callback change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-auto-wiring-suite/command.yaml"
          && grep -q '248 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-auto-wiring-suite/command.yaml"
  - id: "dod-infra-pr-1873"
    description: "omnibase_infra PR #1873 carries the runtime auto-wiring fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-infra-pr-1873/command.yaml"
          && grep -q '^pr_number: 1873$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-infra-pr-1873/command.yaml"
          && grep -q '689567f277d13dcfd17094d20f9499bfc6c0e0d4' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-infra-pr-1873/command.yaml"
  - id: "dod-omnimarket-pr-1056"
    description: >-
      omnimarket PR #1056 proves the consumed inference-effect timeout path publishes a correlated inference-response
      and terminalizes through the delegation workflow.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-omnimarket-pr-1056/command.yaml"
          && grep -q '^pr_number: 1056$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-omnimarket-pr-1056/command.yaml"
          && grep -q '0e2ad3fd2e997c76e491c93067fab369e695698b' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-omnimarket-pr-1056/command.yaml"
  - id: "dod-occ-pr-2207"
    description: >-
      onex_change_control PR #2207 carries the central OMN-12720 contract and PASS receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-occ-pr-2207/command.yaml"
          && grep -q '^pr_number: 2207$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-occ-pr-2207/command.yaml"
          && grep -q 'validate_yaml.py contracts/OMN-12720.yaml' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-occ-pr-2207/command.yaml"
  - id: "dod-deploy-gate-post-merge"
    description: >-
      Runtime dispatch behavior changed, but no live deploy or .201 restart was performed in this PR.
      Post-merge deploy validation must publish a malformed inference-intent probe and observe the correlated
      inference-response error and downstream delegation-failed terminal event.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-deploy-gate-post-merge/command.yaml"
          && grep -q 'No live deploy was performed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-deploy-gate-post-merge/command.yaml"
          && grep -q 'delegation-failed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12720/dod-deploy-gate-post-merge/command.yaml"
