---
schema_version: "1.0.0"
ticket_id: "OMN-11505"
title: "Add usage normalization adapter"
summary: >
  Add provider-aware usage normalization for the onex-self-extending-agent pipeline so measured, estimated,
  and unknown token usage are recorded with explicit provenance instead of hardcoded zero values.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "tests"
    description: "Unit tests for usage normalization pass at the recorded PR head."
    command: "uv run pytest tests/unit/test_usage_normalizer.py -q"
  - kind: "manual"
    description: "onex-self-extending-agent PR #92 is open against main at the recorded head SHA."
    command: "gh pr view 92 --repo OmniNode-ai/onex-self-extending-agent --json number,url,headRefOid,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-92"
    description: "onex-self-extending-agent PR #92 carries OMN-11505 usage normalization at the recorded
      head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11505/dod-product-pr-92/command.yaml"
          && grep -q '^pr_number: 92$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11505/dod-product-pr-92/command.yaml"
          && grep -q '^commit_sha: "01abc253872ed48f177c9b141d5f781a7542b82b"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11505/dod-product-pr-92/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11505 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11505/dod-occ-self/command.yaml"
          && grep -q '^ticket_id: "OMN-11505"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11505/dod-occ-self/command.yaml"
