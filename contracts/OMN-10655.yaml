---
schema_version: "1.0.0"
ticket_id: "OMN-10655"
title: "Wire contract reducer to onboarding cloud and hybrid paths"
summary: >
  Adds central evidence for the paired OMN-10655 onboarding work: omnimarket proves the generic contract
  reducer can execute onboarding-shaped transition tables, while omnibase_infra adds canonical cloud and
  hybrid onboarding policy paths.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-interactive-policy-tests"
    description: "33 focused interactive_onboarding policy tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-interactive-policy-tests/command.yaml\""
  - id: "dod-onboarding-suite"
    description: "Merged onboarding policy suite passes with main's omnimarket_quickstart policy included."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-onboarding-suite/command.yaml\""
  - id: "dod-policy-file-exists"
    description: "interactive_onboarding.yaml exists at the canonical omnibase_infra policy path."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-policy-file-exists/command.yaml\""
  - id: "dod-pr-1522-head"
    description: "Bind earlier OMN-10655 evidence to omnibase_infra PR #1522 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-pr-1522-head/command.yaml\""
  - id: "dod-omnimarket-ci-summary"
    description: "omnimarket PR #558 CI Summary passed for reducer onboarding integration coverage."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-omnimarket-ci-summary/command.yaml\""
  - id: "dod-infra-ci-summary"
    description: "omnibase_infra PR #1526 CI Summary passed for cloud and hybrid onboarding policy coverage."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-infra-ci-summary/command.yaml\""
  - id: "dod-pr-558-head"
    description: "Bind OMN-10655 evidence to omnimarket PR #558 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-pr-558-head/command.yaml\""
  - id: "dod-pr-1526-head"
    description: "Bind OMN-10655 evidence to omnibase_infra PR #1526 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-pr-1526-head/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10655 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number: 801$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-occ-pr/command.yaml\""
