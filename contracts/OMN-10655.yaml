---
schema_version: "1.0.0"
ticket_id: "OMN-10655"
title: "Branching interactive onboarding policy"
summary: >
  Adds the omnibase_infra interactive_onboarding policy as a contract-declared transition table for local,
  cloud, and hybrid onboarding paths. The reducer interprets the policy at runtime without Python branching
  logic.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-interactive-policy-tests"
    description: "33 focused interactive_onboarding policy tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-interactive-policy-tests/command.yaml"
  - id: "dod-onboarding-suite"
    description: "Merged onboarding policy suite passes with main's omnimarket_quickstart policy included."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-onboarding-suite/command.yaml"
  - id: "dod-policy-file-exists"
    description: "interactive_onboarding.yaml exists at the canonical omnibase_infra policy path."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-policy-file-exists/command.yaml"
  - id: "dod-pr-1522-head"
    description: "Bind the current omnibase_infra PR #1522 head after the onboarding test cleanup commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1522 --repo OmniNode-ai/omnibase_infra --json headRefOid,title"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10655 central contract and receipt evidence for omnibase_infra
      PR #1522."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number: 794$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10655/dod-occ-pr/command.yaml\""
