---
schema_version: '1.0.0'
ticket_id: OMN-9344
title: "feat(ci): cross-repo config propagator bot — release-tag triggered"
summary: 'feat(ci): cross-repo config propagator bot — release-tag triggered'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: CI passes on merge PR
    command: gh pr checks
  - kind: tests
    description: Dry-run tests for propagator script
    command: uv run pytest tests/scripts/test_propagate_config.py -v
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: PR opened against main branch
    source: generated
    checks:
      - check_type: command
        check_value: 'state=$(gh pr view {pr} --repo {repo} --json state,baseRefName -q ''[.state, .baseRefName]
          | @tsv''); [ "$(echo "$state" | cut -f1)" = "OPEN" ] && [ "$(echo "$state" | cut -f2)" = "main"
          ]'
  - id: dod-002
    description: Contract artifact file present in repo
    source: generated
    checks:
      - check_type: command
        check_value: test -f contracts/OMN-9344.yaml
  - id: dod-003
    description: CI passes on PR
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view {pr} --repo {repo} --json state -q .state
  - id: dod-occ-pr-685
    description: OCC PR  #685 self-binding receipt exists for the OMN-9344 evidence PR
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9344/dod-occ-pr-685/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-occ-pr-685\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9344/dod-occ-pr-685/command.yaml\"
          && grep -q '^ticket_id: \"OMN-9344\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9344/dod-occ-pr-685/command.yaml\"
          && grep -q '^commit_sha: \"47aa04742ec77d7748dd7aa5c09e2938af89d7b0\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9344/dod-occ-pr-685/command.yaml\"
          && grep -q '^pr_number: 685$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9344/dod-occ-pr-685/command.yaml\""
