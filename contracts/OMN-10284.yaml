---
schema_version: "1.0.0"
ticket_id: OMN-10284
title: "Define chart manifest config schema extension"
summary: "Add omnidash/shared/types/chart-config.ts with five new exported types: BarChartFieldMapping,
  TrendChartFieldMapping, KPITileMetricConfig, DataTableColumnConfig, EmptyStateConfig"
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: tests
    description: "Unit tests for all five chart-config types pass (chart-config.test.ts)"
    command: "cd omnidash && npm run test:run 2>&1 | grep 'passed'"
  - kind: tests
    description: "Manifest envelope acceptance tests pass (component-manifest.test.ts chart-config section)"
    command: "cd omnidash && npm run test:run 2>&1 | grep 'passed'"
  - kind: ci
    description: "TypeScript typecheck passes with no errors"
    command: "cd omnidash && npm run check 2>&1; echo exitcode=$?"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "All five chart-config types exported from chart-config.ts"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -cE 'export (interface|type) (BarChartFieldMapping|TrendChartFieldMapping|KPITileMetricConfig|DataTableColumnConfig|EmptyStateConfig|EmptyStateReason)'
          omnidash/shared/types/chart-config.ts"
  - id: dod-002
    description: "Test suite passes 564 tests across 69 files"
    source: generated
    checks:
      - check_type: command
        check_value: "cd omnidash && npm run test:run 2>&1 | grep -E '[0-9]+ passed'"
  - id: dod-003
    description: "TypeScript typecheck exits 0"
    source: generated
    checks:
      - check_type: command
        check_value: "cd omnidash && npm run check 2>&1 && echo typecheck-ok"
  - id: dod-004
    description: "component-manifest.ts is NOT modified in this ticket (envelope acceptance only via test
      additions)"
    source: generated
    checks:
      - check_type: command
        check_value: "git -C omnidash diff HEAD -- shared/types/component-manifest.ts | grep -cE '^\\+[^+]|^-[^-]'
          && echo 'FAIL: component-manifest.ts was modified' || echo 'PASS: component-manifest.ts untouched'"
