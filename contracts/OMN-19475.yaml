---
schema_version: "1.0.0"
ticket_id: "OMN-19475"
title: "omnimarket README lists a service archetype and stale node counts"
summary: "Hand-authored OCC evidence (OMN-15247) for OmniNode-ai/omnimarket#2876. occ-autobind declined
  the PR with skip:NO_RED_DERIVABLE_CHECK (correlation_id 3aeff513-8b9e-416e-b812-ff14cbd460c4) because
  the diff is README-only. Each acceptance criterion is bound to a content-bound, fail-closed probe of
  the PR head that exits 0 at head 827b6ba3e4d6 and exits 1 at merge base 8e5b1dbeffb8."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
requirements:
  - id: "req-transcribed-acceptance-criteria"
    statement: |-
      The acceptance criteria declared on OMN-19475, transcribed verbatim from the ticket body. The ticket body is the source and this block is the copy.
    acceptance:
      - id: "AC1"
        statement: |-
          AC1: The README archetype table lists only compute, effect, reducer and orchestrator -- falsifier: grep -n -i "service" in the README archetype table returns a row
      - id: "AC2"
        statement: |-
          AC2: The README does not state a node count that the dogfood test contradicts -- falsifier: a count in README.md that differs from the entry_points assertion in tests/test_market_node_runtime_dogfood.py
evidence_requirements:
  - kind: "docs"
    description: "The archetype table and the entry-point count in omnimarket README.md at the PR head
      match AC1 and AC2."
    command: |-
      [ "$(gh api "repos/OmniNode-ai/omnimarket/contents/README.md?ref=827b6ba3e4d63fded8bb5fdac850b4e1657def69" --jq '.content' | base64 -d | awk '/^[|] Archetype/{f=1;next} f&&/^[|] `/{n++; if ($0 !~ /^[|] `(compute|effect|reducer|orchestrator)`/) bad++} f&&!/^[|]/{exit} END{print n+0 ":" bad+0}')" = "4:0" ]
  - kind: "ci"
    description: "OmniNode-ai/omnimarket#2876 product diff scope present"
    command: "gh pr view 2876 --repo OmniNode-ai/omnimarket --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2876-ac1"
    description: "PR #2876 on OmniNode-ai/omnimarket — hand-authored evidence (OMN-15247) for AC1. The
      archetype table at the PR head has exactly four data rows, all of them compute, effect, reducer
      or orchestrator; at the merge base it has six, two of them non-canonical."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          [ "$(gh api "repos/OmniNode-ai/omnimarket/contents/README.md?ref=827b6ba3e4d63fded8bb5fdac850b4e1657def69" --jq '.content' | base64 -d | awk '/^[|] Archetype/{f=1;next} f&&/^[|] `/{n++; if ($0 !~ /^[|] `(compute|effect|reducer|orchestrator)`/) bad++} f&&!/^[|]/{exit} END{print n+0 ":" bad+0}')" = "4:0" ]
    binds_ac:
      - "AC1"
    ac_bindings:
      - label: "AC1"
        criterion_hash: "927a2542acc95bccb9b1cd21b4c867e4601031d6b199577091e8182c1c97eeae"
        proposed_by: "dm-docs"
  - id: "dod-OmniNode-ai-omnimarket-pr-2876-ac2"
    description: "PR #2876 on OmniNode-ai/omnimarket — hand-authored evidence (OMN-15247) for AC2. The
      count the README states at the PR head equals the entry_points assertion in tests/test_market_node_runtime_dogfood.py
      at the same ref; at the merge base the README says 384 and the test asserts 404."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          [ "$(gh api "repos/OmniNode-ai/omnimarket/contents/README.md?ref=827b6ba3e4d63fded8bb5fdac850b4e1657def69" --jq '.content' | base64 -d | grep -o -E '[0-9]+ contract-backed' | cut -d " " -f 1)" = "$(gh api "repos/OmniNode-ai/omnimarket/contents/tests/test_market_node_runtime_dogfood.py?ref=827b6ba3e4d63fded8bb5fdac850b4e1657def69" --jq '.content' | base64 -d | grep -o -E 'summary\["entry_points"\] == [0-9]+' | grep -o -E '[0-9]+$')" ]
    binds_ac:
      - "AC2"
    ac_bindings:
      - label: "AC2"
        criterion_hash: "a175c5f6baf80d5adbc130ad82a989a6930d61264308ea3f6a4e8b6c0bf56dbb"
        proposed_by: "dm-docs"
  - id: "dod-OmniNode-ai-omnimarket-pr-2876-ci"
    description: "PR #2876 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2876 --repo OmniNode-ai/omnimarket --json files"
