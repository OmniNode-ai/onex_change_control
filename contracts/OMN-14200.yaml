---
schema_version: "1.0.0"
ticket_id: "OMN-14200"
title: "test(OMN-14200): sync model_registry loader test with qwen3-review/qwen3-review-b keys"
summary: >-
  omniintelligence test-only fix. omniintelligence PR #767 added the qwen3-review and qwen3-review-b keys
  to review_pairing/model_registry.yaml (local_model_keys) but did not update the loader test's hard-coded
  expected-key sets, so test_model_registry_loader.py::test_load_registry_returns_contract_with_expected_keys
  asserted a stale 4-key set and failed on dev. PR #772 adds the two keys to the loader test's exact-set
  assertions so the test mirrors the committed registry. Test-only: no source change, no runtime services,
  no deploy, no restart, no branch-protection mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-pr-772"
    description: >-
      omniintelligence PR #772 is open against dev at head bfa4eba8098147ff876c071cb0adbb0817880fa1 and
      adds qwen3-review and qwen3-review-b to the local_model_keys and models.keys() exact-set assertions
      in tests/unit/review_pairing/test_model_registry_loader.py so the loader test mirrors model_registry.yaml.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14200/dod-omniintelligence-pr-772/command.yaml
          && grep -q '^pr_number: 772$' drift/dod_receipts/OMN-14200/dod-omniintelligence-pr-772/command.yaml
          && grep -q '^commit_sha: bfa4eba8098147ff876c071cb0adbb0817880fa1$' drift/dod_receipts/OMN-14200/dod-omniintelligence-pr-772/command.yaml
  - id: "dod-no-live-deploy"
    description: >-
      Scope evidence: OMN-14200 is a test-only assertion sync. It performs no live deploy, no runtime
      restart, no branch-protection mutation, and no bypass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14200/dod-no-live-deploy/command.yaml && grep
          -qi 'no live deploy' drift/dod_receipts/OMN-14200/dod-no-live-deploy/command.yaml
