---
schema_version: "1.0.0"
ticket_id: OMN-14475
title: "scripts/** canonical-form guard — DEFAULT-DENY new scripts (Rule 7a)"
summary: >-
  Extend the imperative-contract guard to scripts/** under a deterministic deny-new inventory policy.
  New governed scripts (.py/.sh/.bash) pass only if in the frozen baseline (allowlisted_scripts) or the
  CODEOWNERS-approved exceptions registry (scripts_exceptions.yaml, resolved from @main so approved_by
  != author). Adds new interface-surface models (ModelScriptCanonicalResult, ModelScriptException) and
  enums.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against dev branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}"
  - id: "dod-002"
    description: "Scripts canonical-form guard runs green on the baselined repo"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run check-imperative-contracts --repo-root . --allowlists-dir allowlists --scan-scripts"
  - id: "dod-003"
    description: "Scripts canonical-form scanner module landed in the product"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f src/onex_change_control/scanners/script_canonical_form.py"
  - id: "dod-004"
    description: "Ticket contract artifact validates against ModelTicketContract"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run validate-yaml contracts/OMN-14475.yaml"
