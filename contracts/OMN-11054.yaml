---
schema_version: "1.0.0"
ticket_id: "OMN-11054"
title: "Update onboarding SKILL.md with setup-first policy grouping"
summary: >
  Docs + test update for omniclaude PR #1589. Groups onboarding policies into Environment Setup (setup,
  standalone_quickstart) and Full Platform (new_employee, contributor_local, full_platform) sections.
  Changes default policy from new_employee to setup. Updates standalone_quickstart time estimate and adds
  setup to EXPECTED_POLICIES in the skill test.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Onboarding skill tests pass with setup added to EXPECTED_POLICIES."
    command: "See drift/dod_receipts/OMN-11054/dod-tests/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Onboarding skill test suite passes with setup policy included."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-11054/dod-tests/command.yaml && grep
          -q 'pr_number: 1036' drift/dod_receipts/OMN-11054/dod-tests/command.yaml && echo 'OMN-11054
          test receipt present and PASS'"
