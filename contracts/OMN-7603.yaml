---
schema_version: '1.0.0'
ticket_id: OMN-7603
title: Implement automated consumer health monitoring and alerting
summary: Enable ENABLE_CONSUMER_HEALTH_EMITTER flag in consumer-health-projection catalog
  manifest and add validate-runtime CLI command for catalog structural completeness checks
is_seam_ticket: false
interface_change: false
interfaces_touched:
  - docker-catalog
evidence_requirements:
  - kind: tests
    description: 5 unit tests for validate-runtime command pass
    command: uv run pytest tests/unit/infra/test_catalog_validate_runtime.py -v
  - kind: ci
    description: CI pipeline green on omnibase_infra PR 1766
    command: gh pr checks 1766 --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-tests
    description: 5 unit tests for validate-runtime pass
    source: automated
    checks:
      - check_type: command
        check_value: uv run pytest tests/unit/infra/test_catalog_validate_runtime.py -v
  - id: dod-emitter-flag
    description: ENABLE_CONSUMER_HEALTH_EMITTER=true set in consumer-health-projection operational_defaults
    source: manual
    checks:
      - check_type: command
        check_value: grep -q "ENABLE_CONSUMER_HEALTH_EMITTER.*true" docker/catalog/services/consumer-health-projection.yaml
