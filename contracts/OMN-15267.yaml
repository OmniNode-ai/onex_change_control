---
schema_version: "1.0.0"
ticket_id: "OMN-15267"
title: "Autobind OCC evidence for OMN-15267"
summary: >
  OCC contract authored for OmniNode-ai/omninode_infra PR #744. The product PR re-arms the terminal business-proof
  gate on push and workflow_run deploys, but does not claim the downstream deploy-proof DoD while OMN-15353
  and migration blockers remain in front of the terminal proof.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #744 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-744"
    description: "PR #744 on OmniNode-ai/omninode_infra - business-proof gate re-arm is present at the
      product PR head."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/contents/.github/workflows/deploy-onex-dev.yml?ref=a42c15f739f10c829b50ccf59f6e007e29d354f9 --jq '.content' | base64 -d | grep -c 'if: ${{ inputs.skip-business-proof != true }}'
  - id: "dod-OmniNode-ai-omninode_infra-pr-744-ratchet"
    description: "PR #744 on OmniNode-ai/omninode_infra - unconditional ratchet rejects reintroducing
      trigger narrowing."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/contents/tests/k8s/test_deploy_onex_dev_business_proof_gate.py?ref=a42c15f739f10c829b50ccf59f6e007e29d354f9 --jq '.content' | base64 -d | grep -c 'class TestGateIsArmedForEveryTrigger'
