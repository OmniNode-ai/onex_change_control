---
schema_version: '1.0.0'
ticket_id: OMN-10362
title: "Task 10: [Wave 3 Co-Change] NPMI + lift computation"
summary: "Add compute_npmi() and compute_lift() to co_change_matrix.py in omnibase_core"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "9 unit tests for compute_npmi and compute_lift pass"
    command: "gh pr checks 997 --repo OmniNode-ai/omnibase_core"
  - kind: ci
    description: "mypy --strict and ruff check pass (verified via receipts)"
    command: >-
      grep -q 'status: PASS' drift/dod_receipts/OMN-10362/dod-002/command.yaml && grep -q 'status: PASS'
      drift/dod_receipts/OMN-10362/dod-003/command.yaml
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "9 unit tests pass — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-10362/dod-001/command.yaml && grep -q '^ticket_id:
          OMN-10362$' drift/dod_receipts/OMN-10362/dod-001/command.yaml && grep -q '^evidence_item_id:
          dod-001$' drift/dod_receipts/OMN-10362/dod-001/command.yaml
  - id: dod-002
    description: "mypy --strict clean — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-10362/dod-002/command.yaml && grep -q '^ticket_id:
          OMN-10362$' drift/dod_receipts/OMN-10362/dod-002/command.yaml && grep -q '^evidence_item_id:
          dod-002$' drift/dod_receipts/OMN-10362/dod-002/command.yaml
  - id: dod-003
    description: "ruff check passes — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-10362/dod-003/command.yaml && grep -q '^ticket_id:
          OMN-10362$' drift/dod_receipts/OMN-10362/dod-003/command.yaml && grep -q '^evidence_item_id:
          dod-003$' drift/dod_receipts/OMN-10362/dod-003/command.yaml
