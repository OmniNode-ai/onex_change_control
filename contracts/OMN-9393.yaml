---
schema_version: "1.0.0"
ticket_id: OMN-9393
summary: "fix(deploy-agent): unsigned trigger envelopes silently dropped by auth.py — fix and tests shipped
  in OMN-9411"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Unit tests confirm auth.py::verify_command() rejects unsigned envelopes
    command: uv run pytest tests/unit/test_auth_hmac.py -v
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: Auth unit tests pass — verify_command() rejects unsigned envelopes
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/test_auth_hmac.py -v"
