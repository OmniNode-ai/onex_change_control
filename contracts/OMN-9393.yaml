---
schema_version: "1.0.0"
ticket_id: OMN-9393
summary: "fix(deploy-agent): silent drop of unsigned trigger envelope — root cause of 2026-04-20 overnight
  trigger failure"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Unit tests verify auth.py::verify_command() rejects unsigned envelopes
    command: uv run pytest tests/unit/test_auth_hmac.py -v
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: PR merged with signed trigger helper and auth tests
    checks:
      - check_type: command
        check_value: "gh pr view --repo OmniNode-ai/omnibase_infra --json state -q .state"
