---
schema_version: "1.0.0"
ticket_id: OMN-9393
summary: "fix(deploy-agent): unsigned trigger envelopes silently dropped by auth.py — implementation in
  OMN-9411"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
evidence_requirements:
  - kind: tests
    description: >
      OMN-9411 unit tests verify auth.py::verify_command() rejects unsigned envelopes and that the signed
      trigger helper produces envelopes that pass verification. Note: this is an additive fix — unsigned
      rejection was already the intended behavior per the protocol spec; the helper adds the correct tooling
      so operators no longer send unsigned envelopes accidentally. No migration period needed; all existing
      callers already use the helper.
    command: uv run pytest tests/unit/test_auth_hmac.py tests/unit/test_trigger_helper_signature.py -v
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: Auth + trigger signature tests pass (implemented in OMN-9411)
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/test_auth_hmac.py tests/unit/test_trigger_helper_signature.py
          -v"
