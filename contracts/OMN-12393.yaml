---
schema_version: "1.0.0"
ticket_id: OMN-12393
title: "Resolve duplicate dashboard lineage across omnidash, omnidash-v2, and omnidash-v2-new"
summary: >
  Documented the canonical dashboard repo and classified omnidash-v2 (SUPERSEDED)
  and omnidash-v2-new (TEMP_STAGING) vs omnidash (CANONICAL_ACTIVE). Lineage table
  merged into omnidash CLAUDE.md via PR #162; classification doc tracked in omni_home.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "docs"
    description: "Dashboard lineage classification merged into omnidash CLAUDE.md (PR #162)"
    command: "gh pr view 162 --repo OmniNode-ai/omnidash --json state"
  - kind: "docs"
    description: "Dashboard lineage classification recorded in OMN-12368 verification doc"
    command: "test -f docs/evidence/OMN-12368-verification/dashboard.md"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-lineage-merged"
    description: "omnidash PR #162 (carrying the OMN-12393 lineage table) is merged"
    source: "docs"
    checks:
      - check_type: "command"
        check_value: "test \"$(gh pr view 162 --repo OmniNode-ai/omnidash --json state --jq .state)\" = MERGED"
  - id: "dod-lineage-doc"
    description: "Dashboard lineage classification doc is tracked in omni_home"
    source: "docs"
    checks:
      - check_type: "command"
        check_value: "git -C \"$OMNI_HOME\" ls-files --error-unmatch docs/evidence/OMN-12368-verification/dashboard.md && grep -qiE 'lineage|canonical|superseded|TEMP_STAGING' \"$OMNI_HOME/docs/evidence/OMN-12368-verification/dashboard.md\""
