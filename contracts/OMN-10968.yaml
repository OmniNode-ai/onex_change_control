---
schema_version: "1.0.0"
ticket_id: "OMN-10968"
title: "Harden pre-commit skip-token hook from advisory to BLOCKING"
summary: "Updates reject-deploy-gate-skip-token.sh in omniclaude to declare itself as the LOCAL BLOCKING
  enforcement layer rather than advisory. Removes all 'ADVISORY ONLY' / 'developer-convenience warning'
  language. The GHA workflow is the remote enforcement layer; this hook is the local enforcement layer.
  Both are required and neither is advisory."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omniclaude PR #1583 ships with all 9 self-tests passing and all CI gates green."
    command: "gh pr view 1583 --repo OmniNode-ai/omniclaude --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1583"
    description: "omniclaude PR #1583 hardens the skip-token hook to BLOCKING."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10968/dod-omniclaude-pr-1583/command.yaml
          && grep -q '^pr_number: 1583$' drift/dod_receipts/OMN-10968/dod-omniclaude-pr-1583/command.yaml"
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carries the OMN-10968 contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10968/dod-occ-pr-self/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10968/dod-occ-pr-self/command.yaml"
