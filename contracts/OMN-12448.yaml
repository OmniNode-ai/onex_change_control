---
schema_version: "1.0.0"
ticket_id: "OMN-12448"
title: "Bound EventBusKafka consumer startup concurrency"
summary: >
  Bound runtime Kafka consumer startup concurrency and retry transient consumer start failures so cold
  runtime boot does not stampede the group coordinator and crash-loop when a single consumer times out.
  The implementation is carried by omnibase_infra PR #1795 with contract-backed configuration fields on
  ModelKafkaEventBusConfig and kafka_event_bus_config.yaml; no new environment fallback path is introduced.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "src/omnibase_infra/event_bus/models/config/model_kafka_event_bus_config.py"
  - "src/omnibase_infra/event_bus/configs/kafka_event_bus_config.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1795"
    description: "omnibase_infra PR #1795 carries the bounded consumer-startup concurrency implementation at the cleaned dev-based head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12448/dod-omnibase-infra-pr-1795/command.yaml
          && grep -q '^pr_number: 1795$' drift/dod_receipts/OMN-12448/dod-omnibase-infra-pr-1795/command.yaml
          && grep -q '^commit_sha: \"72fbe42d0d5138a83b58f6d2364f4d1685f5281c\"$' drift/dod_receipts/OMN-12448/dod-omnibase-infra-pr-1795/command.yaml"
  - id: "dod-focused-event-bus-tests"
    description: "Focused EventBusKafka concurrency/retry tests pass locally against the cleaned OMN-12448 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12448/dod-focused-event-bus-tests/command.yaml
          && grep -q '6 passed' drift/dod_receipts/OMN-12448/dod-focused-event-bus-tests/command.yaml"
