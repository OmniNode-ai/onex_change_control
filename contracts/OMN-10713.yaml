---
schema_version: "1.0.0"
ticket_id: "OMN-10713"
title: "Declare memory/graph config in contract.yaml"
summary: >
  Declares the OMN-10713 memory and graph environment variables as typed contract dependencies so the
  runtime config discovery path can see them: OMNIMEMORY_ARCHIVE_COMPRESSION_LEVEL, OMNIMEMORY_ARCHIVE_PATH,
  OMNIMEMORY_PG_DSN, GRAPH_BOLT_URI, MEMGRAPH_BOLT_URI, OMNIMEMORY_MEMGRAPH_HOST, and OMNIMEMORY_MEMGRAPH_PORT.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-contract-deps"
    description: "omnimarket PR #653 declares the memory-side OMN-10713 environment dependencies."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10713/dod-omnimarket-contract-deps/command.yaml"
  - id: "dod-infra-graph-contract-deps"
    description: "omnibase_infra PR #1604 declares the graph-side OMN-10713 environment dependencies."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10713/dod-infra-graph-contract-deps/command.yaml"
  - id: "dod-local-verification"
    description: "Focused local verification passed for the OMN-10713 contract dependency declarations."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10713/dod-local-verification/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR #1035 carries the OMN-10713 central contract and downstream receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10713/dod-occ-pr/command.yaml"
