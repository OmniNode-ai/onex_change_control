---
schema_version: "1.0.0"
ticket_id: "OMN-11809"
title: "node_swarm_decomposer_compute pure compute node"
summary: >
  COMPUTE node node_swarm_decomposer_compute that rehomes TaskDecomposer validation logic from SEA into
  omnimarket. Parses pre-fetched planner LLM output into typed ModelSubtask objects with validation (cycle
  detection, unknown affinity fallback, empty description rejection, context window warning, dangling
  dependency check). Never calls an LLM. Subscribes to onex.cmd.omnimarket.swarm-decompose.v1 and publishes
  onex.evt.omnimarket.swarm-decomposition-completed.v1.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-784"
    description: "omnimarket PR #784 implements node_swarm_decomposer_compute with 37 unit tests"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11809/dod-omnimarket-pr-784/command.yaml\"
          && grep -q '^pr_number: 784$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11809/dod-omnimarket-pr-784/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR binds this contract to onex_change_control as the evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11809/dod-occ-pr/command.yaml\""
  - id: "dod-deploy"
    description: "Deploy assessment — new COMPUTE node, pure and side-effect-free, no live deploy required
      for initial merge; node is invoked on-demand via contract topic"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11809/dod-deploy/command.yaml\""
