---
schema_version: "1.0.0"
ticket_id: "OMN-12695"
title: "Workstream J / Gate 4: live-path reachability on imperative/aislop smell guards"
summary: >-
  Adds reachability classification to the imperative-contract freestanding smell guard so only LIVE env/provider/transport
  findings block, while DEAD and TEST_HARNESS findings remain reported evidence.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-reachability-model"
    description: "Imperative smell result models carry typed live/dead/test-harness reachability."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12695/dod-reachability-model/command.yaml"
          && grep -q "^pr_number: ${PR_NUMBER}$" "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12695/dod-reachability-model/command.yaml"
  - id: "dod-live-freestanding-guard"
    description: "Freestanding imperative guard uses repo entrypoint/import reachability and blocks only
      LIVE findings."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12695/dod-live-freestanding-guard/command.yaml"
          && grep -q 'live_freestanding 0' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12695/dod-live-freestanding-guard/command.yaml"
  - id: "dod-ci-precommit-wiring"
    description: "CI and pre-commit invoke the imperative guard with freestanding reachability scanning."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12695/dod-ci-precommit-wiring/command.yaml"
          && grep -q -- '--scan-freestanding' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12695/dod-ci-precommit-wiring/command.yaml"
  - id: "dod-focused-tests-and-lint"
    description: "Focused tests, ruff, mypy, workflow syntax, and whitespace checks pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12695/dod-focused-tests-and-lint/command.yaml"
          && grep -q '46 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12695/dod-focused-tests-and-lint/command.yaml"
  - id: "dod-no-live-runtime-mutation"
    description: "Gate 4 implementation changes governance tooling and CI hooks only; no live deploy,
      restart, or .201 mutation was performed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12695/dod-no-live-runtime-mutation/command.yaml"
          && grep -q 'no live deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12695/dod-no-live-runtime-mutation/command.yaml"
