---
schema_version: "1.0.0"
ticket_id: "OMN-14198"
title: "onex-dev 502 root-cause source repair evidence"
summary: "OCC evidence for OmniNode-ai/omninode_infra#631: source-level NetworkPolicy, dev deploy trigger,
  and stale smoke probe fixes with read-only live diagnostics."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #631 product diff scope present"
    command: "gh pr view 631 --repo OmniNode-ai/omninode_infra --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-631-head"
    description: "PR #631 is bound to the reviewed product head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -F 'PASS: omninode_infra PR #631 is bound to reviewed head 8d0aaaaed1916ff633e2f6b5c688c0206fa46fa7.'
          drift/dod_receipts/OMN-14198/dod-omninode-infra-pr-631-head/command.yaml && grep -F 'commit_sha:
          \"8d0aaaaed1916ff633e2f6b5c688c0206fa46fa7\"' drift/dod_receipts/OMN-14198/dod-omninode-infra-pr-631-head/command.yaml"
  - id: "dod-omninode-infra-pr-631-scope"
    description: "PR #631 scope is limited to onex-dev NetworkPolicy/workflow/smoke-test source and the
      matching static guard."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -F 'PASS: PR #631 scope is limited to onex-dev NetworkPolicy/workflow/smoke-test
          source and' drift/dod_receipts/OMN-14198/dod-omninode-infra-pr-631-scope/command.yaml && grep
          -F 'k8s/onex-dev/networkpolicies.yaml' drift/dod_receipts/OMN-14198/dod-omninode-infra-pr-631-scope/command.yaml
          && grep -F 'scripts/smoke-test-post-deploy.sh' drift/dod_receipts/OMN-14198/dod-omninode-infra-pr-631-scope/command.yaml"
  - id: "dod-omninode-infra-pr-631-tests"
    description: "PR #631 body records full repo, k8s, static guard, and pre-commit validation evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -F 'PASS: PR #631 body records k8s/full-suite/static-guard/pre-commit validation
          evidence.' drift/dod_receipts/OMN-14198/dod-omninode-infra-pr-631-tests/command.yaml && grep
          -F '40/40 passing' drift/dod_receipts/OMN-14198/dod-omninode-infra-pr-631-tests/command.yaml
          && grep -F '125/125 passing' drift/dod_receipts/OMN-14198/dod-omninode-infra-pr-631-tests/command.yaml"
  - id: "dod-omninode-infra-pr-631-live-boundary"
    description: "PR #631 body records read-only live diagnostics and explicitly defers live deploy/readback
      to operator-approved dispatch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -F 'PASS: PR #631 records read-only live diagnostics and explicitly defers
          live deploy/readback' drift/dod_receipts/OMN-14198/dod-omninode-infra-pr-631-live-boundary/command.yaml
          && grep -F 'no `kubectl apply`/`patch`/`delete` was run' drift/dod_receipts/OMN-14198/dod-omninode-infra-pr-631-live-boundary/command.yaml
          && grep -F 'needs a `deploy-onex-dev.yml` dispatch on `dev` after merge' drift/dod_receipts/OMN-14198/dod-omninode-infra-pr-631-live-boundary/command.yaml"
  - id: "occ-self-bind-pr-4677"
    description: "OCC companion PR #4677 — self-bind for OMN-14198."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4677 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
