---
schema_version: "1.0.0"
ticket_id: "OMN-14897"
title: "Reject duplicate smart-test selector adjacency keys"
summary: "OCC evidence for the OMN-14897 portion of omnibase_core#1477, which removes duplicate selector
  adjacency keys and adds a duplicate-key rejecting YAML loader for the oracle and canonical node."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_core#1477 product PR is open on the expected smart-test selector branch and
      includes the OMN-14897 duplicate-key fix."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - kind: "ci"
    description: "omnibase_core#1477 changes the selector adjacency YAML, duplicate-key rejecting loader,
      canonical selector runtime, and focused selector tests."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1477-dup-key-fix"
    description: "PR #1477 on OmniNode-ai/omnibase_core includes the OMN-14897 duplicate adjacency-key
      fix at the expected product head."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - id: "dod-core-pr-1477-files"
    description: "PR #1477 on OmniNode-ai/omnibase_core includes the selector adjacency YAML, loader,
      runtime, and tests that prove the duplicate-key fix."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
