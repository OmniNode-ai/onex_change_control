---
schema_version: "1.0.0"
ticket_id: "OMN-13712"
title: "feat(OMN-13712): wire skill_mapping.yaml entries for 17 node-proven WIRE-MAP skills"
summary: >
  17 WIRE-MAP skills had a working omnimarket backing node but no skill_mapping.yaml entry, so onex skill
  <name> returned "Unknown skill". This appends one declarative mapping entry per skill (agent_healthcheck,
  env_parity, bus_audit, plan_audit, recall, dispatch_watchdog, env_sync_alert, resume_session, verification_receipt_generator,
  rrh, rewind, checkpoint, insights_to_plan, local_review, autopilot, two_strike_arbiter, feature_dashboard),
  converging each onto the single onex skill <name> path. Pure data + test change; no Python source, no
  new endpoint, no Plugin*/engine/router.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "CLI skill tests pass for the 17 OMN-13712 WIRE-MAP skills."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13712/dod-001/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-13712/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >
      CLI skill unit tests pass (26 passed, 5 skipped due to omnimarket not installed). The two new OMN-13712
      tests assert all 17 skills are registered + wired to the correct node and that all 17 backing nodes
      resolve in the canonical omnimarket onex.nodes catalog.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13712/dod-001/command.yaml"
  - id: "dod-occ-self"
    description: >
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13712 contract and receipts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13712/dod-occ-self/command.yaml"
