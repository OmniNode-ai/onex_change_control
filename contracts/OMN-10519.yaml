---
schema_version: "1.0.0"
ticket_id: "OMN-10519"
title: "Lock stability runtime report-poster auto-wiring"
summary: "Add focused omnimarket regression coverage for the stability runtime startup blocker where HandlerReportPoster
  required an unsatisfied github_bridge constructor dependency during ONEX auto-wiring."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused omnimarket report-poster auto-wiring and adjacent PR review bot wiring tests
      pass on PR #519."
    command: "uv run pytest tests/nodes/node_pr_review_bot/test_handler_report_poster_autowire.py tests/nodes/node_pr_review_bot/test_workflow_runner_wiring.py::test_report_poster_zero_arg_dry_run_is_runtime_wirable
      tests/nodes/node_pr_review_bot/test_handler_thread_poster_autowire.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused omnimarket report-poster auto-wiring and adjacent PR review bot wiring tests
      pass on PR #519."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10519/dod-001/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-001\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10519/dod-001/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10519\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10519/dod-001/command.yaml\"
          && grep -q '^commit_sha: \"54c5b5a0a099fecda0047827cf26f455daf908c3\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10519/dod-001/command.yaml\"
          && grep -q '^pr_number: 519$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10519/dod-001/command.yaml\""
