---
schema_version: "1.0.0"
ticket_id: "OMN-15742"
title: "Autobind OCC evidence for OMN-15742"
summary: "OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra
  PR #2688. dod-deploy-assessment amended (2026-08-08, jonahgabriel) to replace the non-falsifiable PR-diff
  grep with a live gh-api content probe against the actual merged/head commit, per the deploy-gate falsifiability
  ratchet (OMN-14443) rejection message."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2688 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2688"
    description: "PR #2688 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2688 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2688 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2688 clears the deploy-gate (F-05). Falsifiable live-surface
      probe: fetches the actual gateway_forwarder.py content from GitHub at head commit 09b17dbdc8a6719801948680a6bda4c2fb55241d
      and asserts the new reconnect-supervision symbol this PR introduces is present -- exit status depends
      on the real state of the pushed commit, not a self-authored receipt."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/gateway_forwarder.py?ref=09b17dbdc8a6719801948680a6bda4c2fb55241d
          --jq .content | base64 -d | grep -q '_supervise_gateway_delivery'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2688"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6206"
    description: "Binds OMN-15742 to OCC companion PR #6206 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6206 --repo OmniNode-ai/onex_change_control --json number,state"
