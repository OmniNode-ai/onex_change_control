---
schema_version: "1.0.0"
ticket_id: "OMN-11088"
title: "Register node generation Kafka topics in TopicBase"
summary: >-
  Add NODE_GENERATION_REQUESTED (cmd), NODE_GENERATION_COMPLETED (evt), and NODE_GENERATION_FAILED (evt)
  to the TopicBase enum in omnibase_core following the onex.{cmd|evt}.omnimarket.{event-name}.v1 naming
  convention.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: FULL
evidence_requirements:
  - kind: tests
    description: "5 unit tests pass covering node generation topic existence, values, and naming convention"
    command: >-
      env -u PYTHONPATH uv run pytest tests/unit/test_node_generation_topics.py -v
  - kind: ci
    description: "CI pipeline green on omnibase_core PR #1084"
    command: "gh pr checks 1084 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-core-pr-1084
    description: "omnibase_core PR #1084 open with node generation topics registered in TopicBase"
    source: generated
    checks:
      - check_type: command
        check_value: >-
          test -f "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11088/dod-core-pr-1084/command.yaml" && grep
          -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11088/dod-core-pr-1084/command.yaml"
          && grep -q '^pr_number: 1084$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11088/dod-core-pr-1084/command.yaml"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for this OCC PR — anchors evidence chain."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-11088/dod-occ-pr/command.yaml && grep -q '^ticket_id:
          "OMN-11088"$' drift/dod_receipts/OMN-11088/dod-occ-pr/command.yaml && grep -q '^pr_number: 1059$'
          drift/dod_receipts/OMN-11088/dod-occ-pr/command.yaml
