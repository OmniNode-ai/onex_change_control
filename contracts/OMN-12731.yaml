---
schema_version: "1.0.0"
ticket_id: "OMN-12731"
title: "Delegation terminal event blocked by plugin result-applier allowlist"
summary: >-
  Central evidence for omnimarket PR #1062. The PR passes the delegation contract publish-topic allowlist
  into the plugin-managed DispatchResultApplier so topic-bearing delegation terminal events can publish
  to their embedded delegation-completed and task-delegated topics instead of being rejected or routed
  to a fallback topic.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1062"
    description: "omnimarket PR #1062 carries the delegation terminal event allowlist fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12731/dod-omnimarket-pr-1062/command.yaml"
          && grep -q '^pr_number: 1062$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12731/dod-omnimarket-pr-1062/command.yaml"
  - id: "dod-local-validation"
    description: "Focused plugin/applier, orchestrator, dispatcher, and useful-artifact delegation tests
      passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12731/dod-local-validation/command.yaml"
          && grep -q '68 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12731/dod-local-validation/command.yaml"
          && grep -q 'test_plugin_result_applier_allows_contract_terminal_topics' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12731/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Runtime impact is bounded to post-merge dev-lane deploy and repeated useful delegation
      validation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12731/dod-deploy-assessment/command.yaml"
          && grep -q 'post-merge dev-lane deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12731/dod-deploy-assessment/command.yaml"
          && grep -q 'docker exec' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12731/dod-deploy-assessment/command.yaml"
          && grep -q 'rpk topic consume' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12731/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2217"
    description: "onex_change_control PR #2217 carries the central OMN-12731 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12731/dod-occ-pr-2217/command.yaml"
          && grep -q '^pr_number: 2217$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12731/dod-occ-pr-2217/command.yaml"
