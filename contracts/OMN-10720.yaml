---
schema_version: "1.0.0"
ticket_id: "OMN-10720"
title: "Wire 5 substrate gates + stubbed-functionality as blocking pre-commit hooks"
summary: >
  Wires all previously unwired substrate validators as blocking pre-commit hooks using --max-violations
  ratchet mode at baseline violation counts. Adds env -u PYTHONPATH isolation, negative threshold rejection,
  pytest markers, and fixes audit_optional advisory exit code.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1060-head"
    description: "omnibase_core PR #1060 carries the substrate gate wiring."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10720/dod-pr-1060-head/command.yaml\""
  - id: "dod-ci-1060-green"
    description: "All CI checks on omnibase_core PR #1060 are passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10720/dod-ci-1060-green/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10720/dod-occ-pr/command.yaml\""
