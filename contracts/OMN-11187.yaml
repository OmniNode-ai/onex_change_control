---
schema_version: "1.0.0"
ticket_id: "OMN-11187"
title: "Fix: projection reducer nodes must emit terminal event on projection-delegation-applied topic"
summary: >
  DelegationProjectionRunner was writing rows to delegation_events but never publishing to onex.evt.omnimarket.projection-delegation-applied.v1.
  This fix adds _emit_terminal_event() to DelegationProjectionRunner after each successful project_event()
  DB write, publishing a ModelEventEnvelope JSON envelope to the terminal topic declared in contract.yaml.
  Pattern B broker consumers can now observe projection completions on the event bus.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Golden chain tests pass including new TestTerminalEventEmission suite."
    command: "See drift/dod_receipts/OMN-11187/dod-tests/command.yaml"
  - kind: "ci"
    description: "ruff, mypy strict pass for changed files."
    command: "See drift/dod_receipts/OMN-11187/dod-lint/command.yaml"
  - kind: "manual"
    description: "Terminal topic receives envelope after delegation event written to DB."
    command: "See drift/dod_receipts/OMN-11187/dod-deploy-terminal-topic/command.yaml"
  - kind: "ci"
    description: "omnimarket PR carries the fix with green CI."
    command: "See drift/dod_receipts/OMN-11187/dod-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Golden chain projection delegation tests pass including 4 new TestTerminalEventEmission
      tests."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-11187/dod-tests/command.yaml"
  - id: "dod-lint"
    description: "ruff and mypy strict pass for node_projection_delegation."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-11187/dod-lint/command.yaml"
  - id: "dod-deploy-terminal-topic"
    description: "After a delegation event is projected, terminal topic receives a valid envelope."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-11187/dod-deploy-terminal-topic/command.yaml"
  - id: "dod-pr"
    description: "omnimarket PR #685 carries the fix with green CI."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11187/dod-pr/command.yaml && grep
          -q '^pr_number: 1101$' drift/dod_receipts/OMN-11187/dod-pr/command.yaml"
