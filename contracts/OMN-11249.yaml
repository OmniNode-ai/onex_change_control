---
schema_version: "1.0.0"
ticket_id: "OMN-11249"
title: "Golden event path fixtures and tests"
summary: >
  Central OCC evidence for omnibase_infra PR #1657. The PR adds golden-chain dynamic registration fixtures
  plus unit and integration test coverage for success and error event paths.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1657 visible non-receipt checks pass at the recorded head."
    command: "gh pr checks 1657 --repo OmniNode-ai/omnibase_infra"
  - kind: "manual"
    description: "PR #1657 carries the expected golden-chain fixture and test files at the recorded head."
    command: "gh pr diff 1657 --repo OmniNode-ai/omnibase_infra --name-only"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1657"
    description: "omnibase_infra PR #1657 is open against main at the recorded golden event path head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11249/dod-omnibase-infra-pr-1657/command.yaml\"
          && grep -q '^commit_sha: \"75ab0faa965700c0751dc3666b76220ab6644833\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11249/dod-omnibase-infra-pr-1657/command.yaml\""
  - id: "dod-golden-fixture-artifacts"
    description: "The PR carries the golden-chain fixture files and dynamic registration test files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11249/dod-golden-fixture-artifacts/command.yaml\"
          && grep -q 'dynamic_registration_success.json' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11249/dod-golden-fixture-artifacts/command.yaml\"
          && grep -q 'test_golden_chain_dynamic_registration.py' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11249/dod-golden-fixture-artifacts/command.yaml\""
  - id: "dod-product-gates"
    description: "Visible non-receipt product gates are passing; the missing receipt context is supplied
      by this OCC evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11249/dod-product-gates/command.yaml\"
          && grep -q 'deploy-gate / deploy-gate pass' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11249/dod-product-gates/command.yaml\"
          && grep -q 'CI Summary pass' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11249/dod-product-gates/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-11249 central contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11249/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: \"OMN-11249\"' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11249/dod-occ-pr/command.yaml\""
