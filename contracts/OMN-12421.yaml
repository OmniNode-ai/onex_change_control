---
schema_version: "1.0.0"
ticket_id: "OMN-12421"
title: "Advance stale OMNIBASE_COMPAT_REF pin in omnibase_infra runtime Dockerfile"
summary: >
  Bind central evidence for omnibase_infra PR #1782: advance the runtime Dockerfile's OMNIBASE_COMPAT_REF
  from the stale c1a878f1 (missing MAX_WORDS_PER_SENTENCE_RE) to compat main HEAD 4d887307, so a clean
  dev/prod runtime redeploy boots without the PluginDelegation ImportError that crashed a fresh build
  during the OMN-12416 proof.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1782"
    description: "omnibase_infra PR #1782 advances the OMNIBASE_COMPAT_REF pin to 4d887307."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12421/dod-omnibase-infra-pr-1782/command.yaml\"
          && grep -q '^pr_number: 1782$' \"drift/dod_receipts/OMN-12421/dod-omnibase-infra-pr-1782/command.yaml\""
  - id: "dod-pin-deploy-build"
    description: "A clean runtime image build from the advanced pin imports omnimarket@dev delegation
      wiring without the MAX_WORDS_PER_SENTENCE_RE ImportError; pin validator passes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12421/dod-pin-deploy-build/command.yaml\"
          && grep -q 'docker' \"drift/dod_receipts/OMN-12421/dod-pin-deploy-build/command.yaml\""
