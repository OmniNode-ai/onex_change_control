---
schema_version: "1.0.0"
ticket_id: OMN-9398
summary: "fix(ci): explicitly enqueue PR after arming auto-merge (cross-repo)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json state -q .state"
  - id: "dod-002"
    description: "Contract artifact file present in repo"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-9398.yaml"
  - id: "dod-003"
    description: "CI passes on PR"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks {pr} --repo {repo}"
