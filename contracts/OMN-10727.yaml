---
schema_version: "1.0.0"
ticket_id: "OMN-10727"
title: "Align ADR grading mock signature with production interface"
summary: >
  Binds omnimarket PR #582, which adds a golden-chain inline proof-of-life test for HandlerCanaryOrchestrator
  with protocol-compliant mocks whose grade() signature includes the required source_summary keyword argument,
  fixing the silent TypeError that blocked draft-gen in the ADR canary PoL pipeline.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-582-head"
    description: "omnimarket PR #582 carries the grading mock signature fix at the recorded implementation
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10727/dod-pr-582-head/command.yaml\""
  - id: "dod-ci-582-green"
    description: "All non-receipt CI checks on omnimarket PR #582 are passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10727/dod-ci-582-green/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10727/dod-occ-pr/command.yaml\""
