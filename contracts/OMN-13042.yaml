---
schema_version: "1.0.0"
ticket_id: OMN-13042
title: "C-2: resume_session reads LATEST.md and re-runs verify: blocks before acting"
summary: >-
  resume_session SKILL.md now specifies that on session resume the skill reads LATEST.md (fallback: newest-mtime
  handoff in docs/handoffs/), identifies verify: blocks in live-state sections and re-runs each probe,
  treats any section without a verify: block as historical (not authoritative for current state), and
  treats un-reverifiable live-state alarms (e.g. BUS-IS-DOWN) as stale that do not gate action. Documentation
  + test change in omniclaude; no runtime contract, topic, or interface is touched.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >-
      TDD test asserts the LATEST.md-reading step, verify-block re-execution rule, historical-section
      rule, and stale-alarm policy are present in the resume_session SKILL.md.
    command: "uv run pytest tests/skills/test_resume_session_skill.py -q"
  - kind: ci
    description: omniclaude PR CI green across required gates.
    command: "gh pr checks --repo OmniNode-ai/omniclaude {pr}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: >-
      resume_session SKILL.md contains the LATEST.md reading step (with the newest-mtime docs/handoffs/
      fallback), the verify-block re-execution rule, the historical (not-authoritative) classification
      for sections lacking a verify: block, and the stale-alarm-does-not-gate policy.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13042/dod-001/command.yaml"
  - id: dod-002
    description: >-
      The TDD test tests/skills/test_resume_session_skill.py passes, mechanically asserting all four policy
      points and the no-instructional-routing-violation guard.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13042/dod-002/command.yaml"
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt — this onex_change_control PR carries the OMN-13042 contract + receipts
      consumed by the omniclaude PR via Evidence-Source.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13042/dod-occ-self/command.yaml"
