---
schema_version: "1.0.0"
ticket_id: OMN-10395
title: "Implement YAML handler"
summary: "Add the shared YAML output handler for typed market CLI reports."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "YAML output handler tests pass"
    command: "uv run pytest tests/unit/cli/output/handlers/test_handler_yaml.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "YAML output handler tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/cli/output/handlers/test_handler_yaml.py -q"
  - id: "dod-002"
    description: "No deploy required; YAML rendering is local CLI presentation logic"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for OMN-10395 - local CLI renderer only'"
