---
schema_version: "1.0.0"
ticket_id: "OMN-9069"
title: "Default env parity critical ticket creation"
summary: >
  Makes env-parity CRITICAL Linear ticket creation the documented default in omniclaude, preserves explicit
  caller controls, adds an opt-out flag, and covers the default behavior with static regression tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused env-parity ticket-default tests pass on omniclaude PR #1609."
    command: "uv run pytest tests/unit/skills/test_env_parity_ticket_defaults.py -q"
  - kind: "ci"
    description: "omniclaude PR #1609 is bound to OMN-9069 at the recorded head SHA."
    command: "gh pr view 1609 --repo OmniNode-ai/omniclaude --json headRefOid,title"
  - kind: "ci"
    description: "omniclaude PR #1609 non-receipt checks are green; Receipt Gate is the only blocker."
    command: "gh pr checks 1609 --repo OmniNode-ai/omniclaude"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1609"
    description: "omniclaude PR #1609 is open against main at the recorded OMN-9069 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9069/dod-omniclaude-pr-1609/command.yaml\"
          && grep -q '^pr_number: 1609$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9069/dod-omniclaude-pr-1609/command.yaml\"
          && grep -q '^commit_sha: \"fffd64a04a34a6a8793529c5f30d9d19896aa371\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9069/dod-omniclaude-pr-1609/command.yaml\""
  - id: "dod-product-ci-gates"
    description: "omniclaude PR #1609 product CI is green except Receipt Gate pending this central evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9069/dod-product-ci-gates/command.yaml\"
          && grep -q 'Receipt Gate is the only failing check' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9069/dod-product-ci-gates/command.yaml\""
