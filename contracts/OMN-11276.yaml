---
schema_version: "1.0.0"
ticket_id: "OMN-11276"
title: "Register Kafka topic and add CLI entry point for runner health in omnibase_infra"
summary: >
  Ensures SUFFIX_RUNNER_HEALTH_SNAPSHOT is registered in ALL_OMNIBASE_INFRA_TOPIC_SPECS with proper 7-day
  retention config, and that the runner health CLI (cli_runner_health.py) exposes --json, --emit, and
  --alert flags. Adds unit test coverage for --emit flag behavior and topic registration assertion. Bumps
  INFRA_MAX_UNIONS threshold to 154 to match actual count post-OMN-11200 (projection freshness SLA monitor).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: 8 unit tests pass for runner health CLI covering all flags.
    command: uv run pytest tests/unit/observability/runner_health/test_cli_runner_health.py -v -q
  - kind: manual
    description: omnibase_infra PR  #1685 carries the implementation.
    command: gh pr checks 1685 --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "8 unit tests pass — all CLI flags covered (default, --json, --alert, --emit no-bootstrap,
      --emit publish, topic registration)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11276/dod-unit-tests/command.yaml"
          && grep -q '^pr_number: 1685$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11276/dod-unit-tests/command.yaml"
          && grep -q '^verifier: agent$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11276/dod-unit-tests/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11276 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11276/dod-occ-pr-self/command.yaml"
