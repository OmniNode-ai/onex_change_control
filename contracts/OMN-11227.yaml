---
schema_version: "1.0.0"
ticket_id: "OMN-11227"
title: "Add phase budget and parallel dispatch fields"
summary: >
  Central OCC evidence for omnibase_core PR #1109. The session contract model gains phase budget controls,
  phase parallel-dispatch metadata, and session-level worker/cost budget fields, with focused overseer
  model coverage at the recorded PR head.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
  - "protocols"
evidence_requirements:
  - kind: "tests"
    description: "Focused overseer model tests pass against the omnibase_core PR #1109 source tree."
    command: "PYTHONPATH=src uv run pytest tests/unit/models/overseer/test_phase_budget.py tests/unit/models/overseer/test_session_contract_unification.py
      tests/unit/models/overseer/test_phase_exit_condition.py tests/unit/models/overseer/test_overseer_model_imports.py
      -q"
  - kind: "manual"
    description: "omnibase_core PR #1109 carries the phase budget fields, parallel dispatch fields, and
      focused tests at the recorded head SHA."
    command: "gh pr view 1109 --repo OmniNode-ai/omnibase_core --json files,headRefOid,state,url"
  - kind: "ci"
    description: "Visible product PR checks for CodeRabbit, Sonar, title, and skip-token gates pass; central
      receipt contexts are supplied by this OCC evidence PR."
    command: "gh pr checks 1109 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-phase-budget-artifacts"
    description: "omnibase_core PR #1109 carries the session phase budget, parallel dispatch, and worker
      budget model changes plus focused tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11227/dod-phase-budget-artifacts/command.yaml\"
          && grep -q '^commit_sha: \"3873201d59aeeac4f1f9a1ceffd8de22dac1bd1a\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11227/dod-phase-budget-artifacts/command.yaml\""
  - id: "dod-focused-overseer-tests"
    description: "Focused overseer model tests pass against the PR source tree."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11227/dod-focused-overseer-tests/command.yaml\"
          && grep -q '64 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11227/dod-focused-overseer-tests/command.yaml\""
  - id: "dod-omnibase-core-pr-1109"
    description: "omnibase_core PR #1109 is open against main at the recorded phase-budget head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11227/dod-omnibase-core-pr-1109/command.yaml\"
          && grep -q '^pr_number: 1109$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11227/dod-omnibase-core-pr-1109/command.yaml\"
          && grep -q '^commit_sha: \"3873201d59aeeac4f1f9a1ceffd8de22dac1bd1a\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11227/dod-omnibase-core-pr-1109/command.yaml\""
  - id: "dod-product-gates"
    description: "Visible non-receipt product gates are passing; missing receipt contexts are the blocker
      addressed by this central OCC evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11227/dod-product-gates/command.yaml\"
          && grep -q 'CodeRabbit' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11227/dod-product-gates/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-11227 central contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11227/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: \"OMN-11227\"' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11227/dod-occ-pr/command.yaml\""
