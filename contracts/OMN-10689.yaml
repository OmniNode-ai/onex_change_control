---
schema_version: "1.0.0"
ticket_id: "OMN-10689"
title: "Bind rollback workflow environment tag evidence"
summary: >
  Bind omninode_infra PR #388 to central OCC evidence for the rollback-onex environment tag targeting
  fix, including the deterministic single-instance SSM target guard added during review.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-388-head"
    description: "omninode_infra PR #388 carries the OMN-10689 rollback workflow fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10689/dod-pr-388-head/command.yaml\""
  - id: "dod-workflow-validation"
    description: "rollback-onex workflow YAML and updated bash snippet validate locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10689/dod-workflow-validation/command.yaml\""
