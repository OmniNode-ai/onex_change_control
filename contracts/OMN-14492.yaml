---
schema_version: "1.0.0"
ticket_id: "OMN-14492"
title: "feat(OMN-14492): classify DLQ/no-dispatcher drops with validation detail"
summary: >-
  Central OCC evidence for omnibase_infra PR #2314. The product PR distinguishes a true no-dispatcher
  wiring gap from a type-scoped dispatcher whose event_model rejects malformed publisher payloads, records
  the real validation detail in dispatch results and DLQ envelopes, and adds a quarantine fallback for
  projection-handler errors with no declared dlq_topics.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2314-product-head"
    description: >-
      Product-observing verification for omnibase_infra PR #2314 at head 7e305a3ba83b78b82a94248545f3980c38118e8f.
      The proof reads PR metadata and changed files from GitHub and checks the DLQ failure-class enum,
      dispatch matcher/runtime changes, DLQ wiring, and focused regression tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=2314; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=7e305a3ba83b78b82a94248545f3980c38118e8f;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,baseRefName,files,title
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
          > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fxq dev "$d/pr.txt" && grep -Fq
          'feat(OMN-14492): classify DLQ/no-dispatcher drops as publisher_malformed vs no_dispatcher with
          real ValidationError detail' "$d/pr.txt" && for p in src/omnibase_infra/enums/enum_dlq_failure_class.py
          src/omnibase_infra/runtime/message_dispatch_engine.py src/omnibase_infra/runtime/auto_wiring/handler_wiring.py
          src/omnibase_infra/runtime/event_bus_subcontract_wiring.py src/omnibase_infra/event_bus/mixin_kafka_dlq.py
          tests/unit/runtime/test_message_dispatch_engine_type_scoping.py tests/unit/runtime/auto_wiring/test_handler_wiring_db_injection.py
          tests/unit/runtime/auto_wiring/test_dispatcher_route_coverage_omn14488.py; do grep -Fq "$p"
          "$d/pr.txt" && mkdir -p "$d/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "$d/$p"; done && grep -Fq 'publisher_malformed' "$d/src/omnibase_infra/enums/enum_dlq_failure_class.py"
          && grep -Fq 'validation_detail' "$d/src/omnibase_infra/runtime/message_dispatch_engine.py" &&
          grep -Fq 'PayloadTypeMatcher' "$d/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py"
          && grep -Fq 'failure_class' "$d/src/omnibase_infra/event_bus/mixin_kafka_dlq.py" && grep -Fq
          'validation_detail' "$d/src/omnibase_infra/event_bus/mixin_kafka_dlq.py" && grep -Fq 'serialization_fallback'
          "$d/src/omnibase_infra/event_bus/mixin_kafka_dlq.py" && grep -Fq 'TestDlqFailureClassification'
          "$d/tests/unit/runtime/test_message_dispatch_engine_type_scoping.py" && grep -Fq 'build_dlq_topic("quarantine")'
          "$d/tests/unit/runtime/auto_wiring/test_handler_wiring_db_injection.py"
  - id: "dod-infra-pr-2314-local-verification"
    description: >-
      Verification evidence recorded in the omnibase_infra PR #2314 body. The PR body records 5628 passing
      focused tests, ruff format/check, mypy on changed source files, and pre-commit over changed files.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=2314; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=7e305a3ba83b78b82a94248545f3980c38118e8f;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,body --jq '.headRefOid
          == "'$PRODUCT_HEAD'" and (.body | contains("5628 passed")) and (.body | contains("ruff format"))
          and (.body | contains("uv run mypy")) and (.body | contains("pre-commit run")) and (.body |
          contains("failure_class")) and (.body | contains("quarantine-fallback"))' | grep -qx true
  - id: "dod-infra-pr-2314-deploy-scope"
    description: >-
      Runtime-surface audit for omnibase_infra PR #2314. The PR changes runtime dispatch/DLQ classification
      source and unit tests. It does not change Dockerfiles, compose manifests, deploy scripts, migrations,
      or systemd units. No live deploy or restart was performed by this merge lane.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=2314; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=7e305a3ba83b78b82a94248545f3980c38118e8f;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid --jq '.headRefOid ==
          "'$PRODUCT_HEAD'"' | grep -qx true && gh pr diff "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO"
          --name-only | grep -Fxq src/omnibase_infra/runtime/message_dispatch_engine.py && gh pr diff
          "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --name-only | grep -Fxq src/omnibase_infra/event_bus/mixin_kafka_dlq.py
          && ! gh pr diff "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --name-only | grep -qE '(^|/)(Dockerfile|docker-compose[^/]*|compose[^/]*|deploy|migrations|systemd)(/|$)'
