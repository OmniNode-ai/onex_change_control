---
schema_version: "1.0.0"
ticket_id: "OMN-11484"
title: "Strip fenced YAML output"
summary: >-
  Bind central OCC evidence for onex-self-extending-agent PR #106, which strips markdown fences from LLM-generated
  YAML before validation and updates retry prompts to request raw YAML.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Product PR #106 test, lint, typecheck, security, title, skip-token, and review-thread
      checks pass at the observed head."
    command: "gh pr checks 106 --repo OmniNode-ai/onex-self-extending-agent"
  - kind: "manual"
    description: "Product PR #106 is bound to OMN-11484 and head b2bf47546e40835aad4b37296a7323d3c2eac247."
    command: "gh pr view 106 --repo OmniNode-ai/onex-self-extending-agent --json number,title,headRefOid,headRefName,state,url"
dod_evidence:
  - id: "dod-product-pr-head"
    description: "Bind OMN-11484 evidence to onex-self-extending-agent PR #106 at the observed product
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11484/dod-product-pr-head/command.yaml\"
          && grep -q '^pr_number: 106$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11484/dod-product-pr-head/command.yaml\"
          && grep -q '^commit_sha: \"b2bf47546e40835aad4b37296a7323d3c2eac247\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11484/dod-product-pr-head/command.yaml\""
  - id: "dod-product-checks"
    description: "Product PR #106 has passing non-receipt checks; Receipt Gate is the expected missing-OCC-evidence
      blocker."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11484/dod-product-checks/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
