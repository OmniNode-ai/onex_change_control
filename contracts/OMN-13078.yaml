---
schema_version: "1.0.0"
ticket_id: "OMN-13078"
title: "feat(omnimarket): projection contract for intent-classification (NC-05)"
summary: >-
  Evidence-Source for OMN-13078 intent-classification projection node. omnidash declares topic onex.snapshot.projection.intent-classification.v1
  (widgets: intent-distribution, session-timeline) but no contract or projection node existed. This cycle
  adds node_projection_intent_classification (reducer) to omnimarket: contract.yaml subscribing to onex.evt.omniintelligence.intent-classified.v1,
  HandlerProjectionIntentClassification with UPSERT into intent_classification_events, migration SQL,
  and 18 golden-chain tests (all passing). The projection API exposes both snapshot topics: intent-classification.v1
  (session-timeline) and intent-classification.distribution.v1 (intent-distribution). No live deploy in
  scope.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
evidence_requirements:
  - kind: "tests"
    description: >-
      18 golden-chain tests in tests/test_golden_chain_projection_intent_classification.py covering UPSERT,
      dedup, batch, missing fields, contract wiring, projection API exposures, and migration presence.
    command: "uv run pytest tests/test_golden_chain_projection_intent_classification.py -v -q"
  - kind: "ci"
    description: >-
      mypy --strict green on the new node; all pre-commit hooks pass (topic-naming-lint, golden-chain-coverage,
      operation_match operation-field, projection-DLQ path, migration-base-before-view, watchdog-topic
      authority, stub-implementation detector).
    command: "uv run mypy src/omnimarket/nodes/node_projection_intent_classification/ --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr"
    description: >-
      The omnimarket product PR carries node_projection_intent_classification: contract.yaml (reducer,
      subscribes intent-classified.v1, exposes both snapshot topics), HandlerProjectionIntentClassification
      (stateless UPSERT handler), migration SQL (intent_classification_events table), and 18 golden-chain
      tests all green. The PR cites Evidence-Source: OCC#<this-pr>.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-13078.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: this cycle adds the contract-owned reducer + projection table DDL. No live deploy
      or runtime restart is performed in this PR scope; live materialization is deferred to the standard
      dev redeploy workstream.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13078/dod-deploy-assessment/command.yaml"
