---
schema_version: "1.0.0"
ticket_id: "OMN-10834"
title: "Replace delegation Unix socket transport with contract-driven Kafka bus"
summary: "Delete transport.py and daemon_fallback.py. Rewrite delegate skill fallback to publish directly
  to onex.cmd.omniclaude.delegate-task.v1 via confluent_kafka.Producer. Add transport_type: kafka to node_delegation_orchestrator
  contract.yaml."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - topics
  - events
evidence_requirements:
  - kind: "tests"
    description: "Unit tests for Kafka transport path pass"
    command: "uv run pytest tests/delegation/test_kafka_transport.py -v"
  - kind: "ci"
    description: "All CI gates green on omniclaude PR #1564"
    command: "gh pr checks 1564 --repo OmniNode-ai/omniclaude"
  - kind: "manual"
    description: "Dead code deleted: transport.py and daemon_fallback.py absent"
    command: "test ! -f src/omniclaude/delegation/transport.py && test ! -f src/omniclaude/delegation/daemon_fallback.py"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-kafka-transport-tests"
    description: "Kafka transport test file exists in omniclaude PR #1565 diff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} -R OmniNode-ai/omniclaude | grep -q test_kafka --repo ${REPO}"
  - id: "dod-transport-py-deleted"
    description: "transport.py deleted from omniclaude delegation module in PR #1565"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} -R OmniNode-ai/omniclaude | grep -q transport.py --repo
          ${REPO}"
  - id: "dod-daemon-fallback-deleted"
    description: "daemon_fallback.py deleted from omniclaude delegation module in PR #1565"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} -R OmniNode-ai/omniclaude | grep -q daemon_fallback.py --repo
          ${REPO}"
  - id: "dod-contract-transport-type"
    description: "node_delegation_orchestrator contract.yaml declares transport_type: kafka"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} -R OmniNode-ai/omniclaude | grep -q 'transport_type: kafka'
          --repo ${REPO}"
  - id: "dod-deploy-kafka-topic"
    description: "Delegate skill PR diff contains delegate-task.v1 Kafka topic reference"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} -R OmniNode-ai/omniclaude | grep -q delegate-task.v1 --repo
          ${REPO}"
  - id: "dod-pr-open"
    description: "omniclaude PR #1564 (contract alignment) exists and is open for merge"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1564 -R OmniNode-ai/omniclaude --json state -q .state | grep -q OPEN"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for this OCC PR #943."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 943 --repo OmniNode-ai/onex_change_control --json state,baseRefName --jq
          '.state + \":\" + .baseRefName' | grep -q 'OPEN:main'"
  - id: "dod-unit-tests"
    description: "6 new unit tests in test_delegate_contract_resolution.py all pass (OMN-10834 contract
      alignment, PR #1564)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10834/dod-unit-tests/command.yaml
          && grep -q '^evidence_item_id: dod-unit-tests$' drift/dod_receipts/OMN-10834/dod-unit-tests/command.yaml
          && grep -q '^ticket_id: OMN-10834$' drift/dod_receipts/OMN-10834/dod-unit-tests/command.yaml"
  - id: "dod-pr-1564"
    description: "omniclaude PR #1564 (contract alignment) CI gates pass and is open for merge"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10834/dod-pr-1564/command.yaml &&
          grep -q '^evidence_item_id: dod-pr-1564$' drift/dod_receipts/OMN-10834/dod-pr-1564/command.yaml
          && grep -q '^ticket_id: OMN-10834$' drift/dod_receipts/OMN-10834/dod-pr-1564/command.yaml"
