---
schema_version: "1.0.0"
ticket_id: "OMN-13085"
title: "feat(omnimarket): projection contract for sandbox.decisions (NC-12)"
summary: >-
  omnidash declared topic onex.snapshot.projection.sandbox.decisions.v1 (NC-12 debt item) but no contract
  or matching projection node existed. Creates node_projection_sandbox_decisions: a reducer that projects
  onex.evt.omnimarket.generated-node-invoked.v1 sandbox invocation terminal events (emitted by HandlerGeneratedExecutor)
  into the sandbox_decisions table in omnidash_analytics. The projection is append-only, deduped by correlation_id,
  and exposed via the projection API at onex.snapshot.projection.sandbox.decisions.v1.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - topics
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain-tests"
    description: >-
      13 golden chain tests in tests/test_golden_chain_projection_sandbox_decisions.py all PASS: row-delta
      proof, dedup by correlation_id, batch projection, extra-field tolerance, alias parsing, contract
      event_bus wiring, snapshot topic declaration, db_io declaration, frozen model, handle shim type
      check.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13085/dod-golden-chain-tests/command.yaml"
  - id: "dod-pre-commit-all-files"
    description: >-
      pre-commit run --all-files exits 0 in the omnimarket worktree: ruff format, ruff check, mypy --strict
      (4 source files, no issues), all custom gates (topic naming, orphan graph, golden-chain coverage,
      migration base-table gate, etc.) PASS.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13085/dod-pre-commit-all-files/command.yaml"
  - id: "dod-omnimarket-pr"
    description: >-
      omnimarket PR feat(OMN-13085): projection contract for sandbox.decisions (NC-12). Source commit:
      7f5f03861820015395daf3d6147847ecb7f5afba
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13085/dod-omnimarket-pr/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding OCC receipt for PR #3343 carrying the OMN-13085 deploy-assessment evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13085/dod-occ-self/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for the rebuilt omnimarket PR: this runtime-path change adds a new node-owned
      projection contract, handler, and migration. No live deploy or restart was performed during PR repair;
      post-merge lane deployment must apply the node-owned migration and start the reducer through the
      standard omnimarket deploy path.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13085/dod-deploy-assessment/command.yaml
          && grep -q 'deploy assessment recorded' drift/dod_receipts/OMN-13085/dod-deploy-assessment/command.yaml"
