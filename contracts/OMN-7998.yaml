---
schema_version: "1.0.0"
ticket_id: OMN-7998
summary: "fix(projection): intelligence-operations page shows mock data — no upstream emit"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 588 --repo OmniNode-ai/omnidash --json state,baseRefName -q '[.state,
          .baseRefName] | @tsv' | awk '$1 ~ /^(OPEN|MERGED)$/ && $2 == \"main\" {found=1} END {exit !found}'"
  - id: "dod-002"
    description: "PR targets main branch (verified at gate time)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 588 --repo OmniNode-ai/omnidash --json state,baseRefName -q '[.state,
          .baseRefName] | @tsv' | awk '$1 ~ /^(OPEN|MERGED)$/ && $2 == \"main\" {found=1} END {exit !found}'"
  - id: "dod-003"
    description: "Omnimemory projections consumer exists"
    source: "generated"
    checks:
      - check_type: "command"
        command: "test -f server/consumers/read-model/omnimemory-projections.ts"
        check_value: "server/consumers/read-model/omnimemory-projections.ts"
  - id: "dod-004"
    description: "Pre-commit config present"
    source: "generated"
    checks:
      - check_type: "command"
        command: "test -f .pre-commit-config.yaml"
        check_value: ".pre-commit-config.yaml"
