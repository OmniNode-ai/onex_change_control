---
schema_version: "1.0.0"
ticket_id: "OMN-12873"
title: "Dev/stability closure: delegation final proof packet"
summary: >-
  Tracks the final delegation closure pass from real entrypoints through the command bus, live routing
  authority, model/effect attempt, quality gate, terminal and compatibility events, projection, dashboard
  render, and replay classification.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-delegation-final-proof"
    description: >-
      Fresh same-day delegation proof packet includes provider, model, endpoint ref, resolved endpoint,
      route source, attempt count, quality result, terminal event, projection row, dashboard capture,
      runtime identity, and replay status.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12873/dod-delegation-final-proof/command.yaml"
