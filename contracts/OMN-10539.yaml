---
schema_version: "1.0.0"
ticket_id: "OMN-10539"
title: "Correct dod_verify SKILL.md output path and CLI invocation"
summary: "Doc-only fix: update omniclaude/plugins/onex/skills/dod_verify/SKILL.md to match actual HandlerDodVerify
  behavior. Output is workflow_result.json (not .evidence/<ticket>/dod_report.json), and the CLI form
  is `onex node node_dod_verify --input <file>` (not `onex run node_dod_verify -- --ticket-id`)."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "docs"
    description: "SKILL.md updated to match real handler output path and CLI invocation."
    command: "git diff origin/main -- plugins/onex/skills/dod_verify/SKILL.md"
  - kind: "manual"
    description: "Documented example invocation runs end-to-end against a real ticket contract."
    command: "uv run onex node node_dod_verify --input <payload.json>"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "SKILL.md no longer references the unwritten .evidence/<ticket>/dod_report.json path."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "! grep -q 'dod_report.json' plugins/onex/skills/dod_verify/SKILL.md || grep -q 'does
          \\*\\*not\\*\\* write' plugins/onex/skills/dod_verify/SKILL.md"
  - id: "dod-002"
    description: "SKILL.md documents the real CLI form `onex node node_dod_verify --input <file>`."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'onex node node_dod_verify --input' plugins/onex/skills/dod_verify/SKILL.md"
  - id: "dod-003"
    description: "Verification transcript captured proving the documented invocation succeeds."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f .evidence/OMN-10539/example_invocation_transcript.txt"
