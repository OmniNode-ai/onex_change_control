---
schema_version: "1.0.0"
ticket_id: "OMN-11967"
title: "Wire delegation routing handlers to model registry"
summary: >
  Central OCC evidence for omnimarket PR #829, which removes hardcoded delegation model pricing paths
  and routes pricing through contract-backed routing tiers.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-829"
    description: "Bind OMN-11967 evidence to omnimarket PR #829."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11967/dod-omnimarket-pr-829/command.yaml
          && grep -q '^pr_number: 829$' drift/dod_receipts/OMN-11967/dod-omnimarket-pr-829/command.yaml"
  - id: "dod-delegation-routing-local-validation"
    description: "Focused delegation routing and LLM delegation call tests pass for the current omnimarket
      PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11967/dod-delegation-routing-local-validation/command.yaml
          && grep -q '36 passed' drift/dod_receipts/OMN-11967/dod-delegation-routing-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Deploy gate assessment for delegation routing model-registry wiring."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11967/dod-deploy-assessment/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11967/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR #1548 carries the central OMN-11967 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11967/dod-occ-pr/command.yaml &&
          grep -q '^pr_number: 1548$' drift/dod_receipts/OMN-11967/dod-occ-pr/command.yaml"
