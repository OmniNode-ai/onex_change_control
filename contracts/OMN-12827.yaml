---
schema_version: '1.0.0'
ticket_id: OMN-12827
title: "MCP exposure of generated COMPUTE node (close-the-loop Plan B2)"
summary: >
  Relax ServiceMCPToolSync exposure rule (omnibase_infra) so generated COMPUTE nodes (mcp-enabled + node-type:compute
  + generated) surface as MCP tools alongside orchestrators, without relabeling them as orchestrators.
  Non-generated compute nodes and non-mcp nodes remain excluded. Predicate renamed _is_mcp_orchestrator
  -> _is_mcp_exposable. Implements Plan B2 of omni_home/docs/plans/2026-06-08-close-the-loop-plan.md.
  Infra PR #1914.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
evidence_requirements:
  - kind: ci
    description: >
      omnibase_infra: 6 new MCP exposure unit tests (orchestrator + generated compute exposed; non-generated
      compute, non-mcp, empty excluded); MCP unit suite 85 pass; mypy strict + ruff clean. Verified locally
      before push.
    command: >
      grep -q '^status: PASS$' drift/dod_receipts/OMN-12827/dod-omn12827-infra-tests/command.yaml
  - kind: docs
    description: OCC contract file OMN-12827.yaml present and schema-valid
    command: >
      cd onex_change_control && uv run validate-yaml contracts/OMN-12827.yaml
dod_evidence:
  - id: dod-omn12827-infra-tests
    description: >
      omnibase_infra MCP exposure: 6 new unit tests prove generated compute nodes and orchestrators are
      exposed and non-generated compute / non-mcp nodes are excluded; TDD red->green observed. omnibase_infra
      PR #1914.
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12827/dod-omn12827-infra-tests/command.yaml"
  - id: dod-omn12827-mcp-sweep
    description: >
      omnibase_infra MCP unit suite (tests/unit/services/mcp/) remains green after the exposure relaxation;
      mypy --strict clean on the changed service module.
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12827/dod-omn12827-mcp-sweep/command.yaml"
  - id: dod-deploy-gate-1914
    description: >
      Deploy-gate evidence for omnibase_infra PR #1914: runtime-path MCP exposure change is code-only
      pre-merge, with live runtime appearance left to the approved Phase D proof track.
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12827/dod-deploy-gate-1914/command.yaml
          && grep -q deploy drift/dod_receipts/OMN-12827/dod-deploy-gate-1914/command.yaml"
  - id: dod-occ-pr-2352
    description: "Self-binding receipt for the OCC PR carrying the OMN-12827 contract and B2 receipts."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12827/dod-occ-pr-2352/command.yaml"
