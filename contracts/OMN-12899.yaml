---
schema_version: "1.0.0"
ticket_id: "OMN-12899"
title: "Preserve provider error bodies for SEA stability Gemini failures"
summary: >-
  Preserve sanitized provider HTTP error bodies in omnimarket delegation inference responses so the SEA
  stability Gemini HTTP 400 blocker can be diagnosed without leaking credentials.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1145"
    description: >-
      omnimarket PR #1145 carries the OMN-12899 implementation for bounded provider error body evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12899/dod-omnimarket-pr-1145/command.yaml"
  - id: "dod-focused-tests"
    description: >-
      Focused handler tests, lint, and format checks prove provider HTTP status bodies are preserved without
      leaking auth material.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12899/dod-focused-tests/command.yaml"
  - id: "dod-gemini-classification"
    description: >-
      OMN-12890 evidence classifies the original Gemini key depletion blocker as resolved and isolates the
      residual runtime HTTP 400 failure.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12899/dod-gemini-classification/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding receipt for the OCC PR carrying the OMN-12899 central contract and DoD receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12899/dod-occ-pr-self/command.yaml"
