---
schema_version: "1.0.0"
ticket_id: "OMN-13048"
title: "D-2: two_strike_arbiter skill — first-strike dispatch-surface trace protocol"
summary: >-
  Retro D-2 (PROCESS_FAILURE_RETRO.md §3.D): at first strike on a dispatch-surface defect, the two_strike_arbiter
  skill now mandates a full end-to-end static trace (contract → dispatch callback → handler deps → injected
  consumers → terminal correlation) enumerating ALL defects before fixing any. The fix-then-rediscover
  ladder is structurally disallowed. Changes: plugins/onex/skills/two_strike_arbiter/SKILL.md updated
  to version 1.1.0 with First-Strike Protocol section and Anti-Patterns table; tripwire tests added in
  tests/unit/skills/test_two_strike_arbiter_guards.py.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      nine guardrail tests in tests/unit/skills/test_two_strike_arbiter_guards.py pass against the updated
      two_strike_arbiter SKILL.md (v1.1.0). Tests assert: First-Strike Protocol section present, static
      trace chain documented, enumerate-all-before-fix mandate, fix-then-rediscover disallowed, Retro
      D-2 + OMN-13048 cited, one-PR-set-per-repo rule, Anti-Patterns section, dispatch-surface concept
      defined, first_strike output requires trace.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13048/dod-001/command.yaml\""
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt — this onex_change_control PR carries the OMN-13048 contract and receipts
      consumed by omniclaude PR via Evidence-Source.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13048/dod-occ-self/command.yaml\""
