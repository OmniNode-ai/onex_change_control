---
schema_version: "1.0.0"
ticket_id: "OMN-12384"
title: "Focused unit coverage for omnibase_infra runtime kernel hotspots"
summary: >
  Bind central evidence for omnibase_infra PR #1793, the dev-targeted replacement for PR #1785. The PR
  adds focused unit coverage for runtime kernel hotspot surfaces, including service kernel allowlist and
  topic matching helpers, contract path resolution, marketplace skill root resolution, handler plugin
  loader error paths, exception message sanitization, and auto-wiring handler guards. The cleaned PR branch
  changes only the focused unit test file.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1785"
    description: "omnibase_infra PR #1785 carries 29 focused runtime kernel hotspot unit tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12384/dod-omnibase-infra-pr-1785/command.yaml\"
          && grep -q '^pr_number: 1785$' \"drift/dod_receipts/OMN-12384/dod-omnibase-infra-pr-1785/command.yaml\""
  - id: "dod-omnibase-infra-pr-1793"
    description: "omnibase_infra PR #1793 carries the dev-targeted replacement branch for 29 focused runtime
      kernel hotspot unit tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12384/dod-omnibase-infra-pr-1793/command.yaml\"
          && grep -q '^pr_number: 1793$' \"drift/dod_receipts/OMN-12384/dod-omnibase-infra-pr-1793/command.yaml\""
  - id: "dod-focused-tests"
    description: "The focused OMN-12384 runtime kernel hotspot unit tests pass locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12384/dod-focused-tests/command.yaml\"
          && grep -q '29 passed' \"drift/dod_receipts/OMN-12384/dod-focused-tests/command.yaml\""
