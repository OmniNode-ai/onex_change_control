---
schema_version: "1.0.0"
ticket_id: "OMN-592"
title: "Extract NodeReducer FSM context construction"
summary: >
  Binds omnibase_core PR #1311, which extracts NodeReducer FSM context construction into _build_fsm_context,
  preserves reserved _fsm_ system keys, and emits a warning intent when caller metadata attempts to overwrite
  reserved keys.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused NodeReducer FSM context tests pass."
    command: "uv run pytest tests/unit/nodes/test_node_reducer_build_fsm_context.py -q"
  - kind: "ci"
    description: "PR branch CI and receipt/deploy gates pass before merge."
    command: "gh pr checks 1311 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1311"
    description: "omnibase_core PR #1311 carries the _build_fsm_context extraction and reserved-key tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid -q .headRefOid"
  - id: "dod-deploy-no-runtime-impact"
    description: "Refactor of NodeReducer context construction plus tests only; no live deploy, broker
      mutation, or runtime restart required before merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy-gate: OMN-592 refactors omnibase_core NodeReducer source and tests
          only; no live deploy required before merge'"
  - id: "dod-occ-pr-self"
    description: "OCC self-binding receipt for the central evidence PR carrying the OMN-592 contract and
      receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid -q .headRefOid"
