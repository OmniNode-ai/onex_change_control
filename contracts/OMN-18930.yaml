---
schema_version: "1.0.0"
ticket_id: "OMN-18930"
title: "K3 complete delegation-cohort provenance acceptance"
summary: >-
  Parent K3 contract for OMN-18925. Comparisons require the complete delegation-cohort key and final source,
  image, build, route, policy, deadline, and retry provenance; no selected-field comparison can stand
  in for complete-key equality.
is_seam_ticket: true
interface_change: true
interfaces_touched: [events, envelopes]
contract_completeness: ENRICHED
proof_class: live-readback
evidence_requirements:
  - kind: tests
    description: >-
      The reconciled cohort validator rejects every missing key dimension and a changed source, image,
      build, route, or policy identity.
    command: >-
      uv run --frozen pytest -q tests/ci/test_delegation_stable_cohort_key_omn18925.py
  - kind: manual
    description: >-
      Final evidence retains both complete keys and source/image/build provenance for each compared run.
      An incomplete historical offset record is a negative control, not a completion substitute. Its PASS
      receipt binds reconciled Core, Market, and Infra commits, release and lock identities, and both
      compared runs.
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: k3-complete-cohort-key
    description: >-
      Planned K3 validator rejects missing dimensions and changed provenance. Its final receipt must bind
      the reconciled source head and both complete run keys rather than a selected-field comparison.
    source: manual
    status: pending
    execution_scope: local_done_gate
    checks:
      - check_type: test_passes
        check_value: >-
          uv run --frozen pytest -q tests/ci/test_delegation_stable_cohort_key_omn18925.py
        cwd: "${OMNI_HOME}/omnibase_infra"
