---
schema_version: '1.0.0'
ticket_id: OMN-12634
title: 'Move DLQ feature gating from env vars to a contract overlay'
summary: >
  PluginDlq activation is moved from OMNIBASE_INFRA_DLQ_ENABLED / OMNIBASE_INFRA_DB_URL env vars to DLQ_ENABLED
  / DLQ_DB_URL keys in the contract overlay (config.overlay_config on ModelDomainPluginConfig). The kernel
  loads ~/.omnibase/overlay.yaml at step 1d and passes resolved key-value pairs to all domain plugins.
  Fail-fast on DLQ_ENABLED=true without DLQ_DB_URL.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: DLQ overlay activation unit tests pass
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12634/dod-001/command.yaml"
  - kind: ci
    description: CI pipeline green on PR
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12634/dod-deploy/command.yaml"
dod_evidence:
  - id: dod-001
    description: Unit tests for PluginDlq overlay activation pass (14 tests)
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12634/dod-001/command.yaml"
  - id: dod-deploy
    description: >
      Runtime deploy preflight — this change rewires plugin config passing in the kernel (no new service,
      no schema migration, no Kafka topic). The deploy gate is satisfied by CI green on the infra PR and
      the runtime boot test which exercises service_kernel initialization.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12634/dod-deploy/command.yaml &&
          grep -q '^pr_number: 2695$' drift/dod_receipts/OMN-12634/dod-deploy/command.yaml"
  - id: dod-occ-validation
    description: OCC contract lint and DoD evidence validation
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12634/dod-occ-validation/command.yaml"
