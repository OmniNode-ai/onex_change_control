---
schema_version: "1.0.0"
ticket_id: "OMN-10768"
title: "feat(OMN-10768): interactive onboarding policy Pydantic models"
summary: >
  Adds typed interactive onboarding policy models and validates start_step references so empty step lists
  and unknown explicit start steps fail during policy loading instead of producing invalid graph entry
  state.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1546 carries the OMN-10768 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10768/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Focused unit and integration tests passed for the interactive policy validation change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10768/dod-local-tests/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR #886 carries the central OCC contract and receipt update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10768/dod-occ-pr/command.yaml\""
