---
schema_version: "1.0.0"
ticket_id: OMN-8401
title: "Wrapper base and environment diagnostics helper"
summary: "Central receipt contract for the shared OmniMarket adapter wrapper base and environment diagnostics
  helper delivered in omnimarket PR #501."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "protocols"
evidence_requirements:
  - kind: "manual"
    description: "Shared wrapper base helpers and Codex runtime client tests pass"
    command: "test -f drift/dod_receipts/OMN-8401/dod-wrapper-tests/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-8401/dod-wrapper-tests/command.yaml && grep -q '38 passed' drift/dod_receipts/OMN-8401/dod-wrapper-tests/command.yaml"
  - kind: "manual"
    description: "Codex plugin shim contract remains dispatch-only and compatible"
    command: "test -f drift/dod_receipts/OMN-8401/dod-codex-shim-tests/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-8401/dod-codex-shim-tests/command.yaml && grep -q '5 passed' drift/dod_receipts/OMN-8401/dod-codex-shim-tests/command.yaml"
  - kind: "manual"
    description: "Adapter generator tests confirm generated Codex/Cursor wrapper instructions"
    command: "test -f drift/dod_receipts/OMN-8401/dod-generator-tests/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-8401/dod-generator-tests/command.yaml && grep -q '68 passed' drift/dod_receipts/OMN-8401/dod-generator-tests/command.yaml"
  - kind: "manual"
    description: "Touched adapter wrapper code passes lint"
    command: "test -f drift/dod_receipts/OMN-8401/dod-lint/command.yaml && grep -q '^status: PASS$' drift/dod_receipts/OMN-8401/dod-lint/command.yaml
      && grep -q 'All checks passed!' drift/dod_receipts/OMN-8401/dod-lint/command.yaml"
  - kind: "manual"
    description: "Touched adapter wrapper code is formatted"
    command: "test -f drift/dod_receipts/OMN-8401/dod-format/command.yaml && grep -q '^status: PASS$'
      drift/dod_receipts/OMN-8401/dod-format/command.yaml && grep -q 'already formatted' drift/dod_receipts/OMN-8401/dod-format/command.yaml"
  - kind: "manual"
    description: "Repo-local contract YAML parses"
    command: "test -f drift/dod_receipts/OMN-8401/dod-contract-parse/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-8401/dod-contract-parse/command.yaml && grep -q 'contracts/OMN-8401.yaml'
      drift/dod_receipts/OMN-8401/dod-contract-parse/command.yaml"
  - kind: "manual"
    description: "Codex runtime request compile-only path remains event-bus-free"
    command: "test -f drift/dod_receipts/OMN-8401/dod-compile-only/command.yaml && grep -q '^status: PASS$'
      drift/dod_receipts/OMN-8401/dod-compile-only/command.yaml && grep -q '\"ok\": true' drift/dod_receipts/OMN-8401/dod-compile-only/command.yaml
      && grep -q '\"status\": \"compiled\"' drift/dod_receipts/OMN-8401/dod-compile-only/command.yaml"
  - kind: "manual"
    description: "Touched adapter wrapper code passes mypy"
    command: "test -f drift/dod_receipts/OMN-8401/dod-mypy/command.yaml && grep -q '^status: PASS$' drift/dod_receipts/OMN-8401/dod-mypy/command.yaml"
  - kind: "manual"
    description: "Full pre-commit hooks pass with OMNIBASE_INFRA_PATH set"
    command: "test -f drift/dod_receipts/OMN-8401/dod-precommit/command.yaml && grep -q '^status: PASS$'
      drift/dod_receipts/OMN-8401/dod-precommit/command.yaml && grep -q 'normalization-symmetry.*Passed'
      drift/dod_receipts/OMN-8401/dod-precommit/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-wrapper-tests"
    description: "Shared wrapper base helpers and Codex runtime client tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-8401/dod-wrapper-tests/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-8401/dod-wrapper-tests/command.yaml && grep -q '38 passed' drift/dod_receipts/OMN-8401/dod-wrapper-tests/command.yaml"
  - id: "dod-codex-shim-tests"
    description: "Codex plugin shim contract remains dispatch-only and compatible"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-8401/dod-codex-shim-tests/command.yaml && grep -q
          '^status: PASS$' drift/dod_receipts/OMN-8401/dod-codex-shim-tests/command.yaml && grep -q '5
          passed' drift/dod_receipts/OMN-8401/dod-codex-shim-tests/command.yaml"
  - id: "dod-generator-tests"
    description: "Adapter generator tests confirm generated Codex/Cursor wrapper instructions"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-8401/dod-generator-tests/command.yaml && grep -q
          '^status: PASS$' drift/dod_receipts/OMN-8401/dod-generator-tests/command.yaml && grep -q '68
          passed' drift/dod_receipts/OMN-8401/dod-generator-tests/command.yaml"
  - id: "dod-lint"
    description: "Touched adapter wrapper code passes lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-8401/dod-lint/command.yaml && grep -q '^status: PASS$'
          drift/dod_receipts/OMN-8401/dod-lint/command.yaml && grep -q 'All checks passed!' drift/dod_receipts/OMN-8401/dod-lint/command.yaml"
  - id: "dod-format"
    description: "Touched adapter wrapper code is formatted"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-8401/dod-format/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-8401/dod-format/command.yaml && grep -q 'already formatted' drift/dod_receipts/OMN-8401/dod-format/command.yaml"
  - id: "dod-contract-parse"
    description: "Repo-local contract YAML parses"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-8401/dod-contract-parse/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-8401/dod-contract-parse/command.yaml && grep -q 'contracts/OMN-8401.yaml'
          drift/dod_receipts/OMN-8401/dod-contract-parse/command.yaml"
  - id: "dod-compile-only"
    description: "Codex runtime request compile-only path remains event-bus-free"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-8401/dod-compile-only/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-8401/dod-compile-only/command.yaml && grep -q '\"ok\": true' drift/dod_receipts/OMN-8401/dod-compile-only/command.yaml
          && grep -q '\"status\": \"compiled\"' drift/dod_receipts/OMN-8401/dod-compile-only/command.yaml"
  - id: "dod-mypy"
    description: "Touched adapter wrapper code passes mypy"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-8401/dod-mypy/command.yaml && grep -q '^status: PASS$'
          drift/dod_receipts/OMN-8401/dod-mypy/command.yaml"
  - id: "dod-precommit"
    description: "Full pre-commit hooks pass with OMNIBASE_INFRA_PATH set"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-8401/dod-precommit/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-8401/dod-precommit/command.yaml && grep -q 'normalization-symmetry.*Passed'
          drift/dod_receipts/OMN-8401/dod-precommit/command.yaml"
