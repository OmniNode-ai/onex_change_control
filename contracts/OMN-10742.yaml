---
schema_version: "1.0.0"
ticket_id: "OMN-10742"
title: "Extract EventBusKafka construction to DI bootstrap"
summary: >
  Binds omniclaude PR #1548, which moves direct EventBusKafka construction out of routing and intelligence
  clients into src/omniclaude/lib/core/bus_bootstrap.py. The change removes direct bus construction from
  business client code while keeping the construction site in an explicit DI bootstrap helper with focused
  unit coverage.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1548-head"
    description: "omniclaude PR #1548 carries the DI bootstrap refactor at the recorded implementation
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10742/dod-pr-1548-head/command.yaml\""
  - id: "dod-no-direct-bus"
    description: "Direct EventBus instantiation validator passes with the new bootstrap allowlist."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10742/dod-no-direct-bus/command.yaml\""
  - id: "dod-bus-bootstrap-unit"
    description: "Focused bus bootstrap unit test passes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10742/dod-bus-bootstrap-unit/command.yaml\""
  - id: "dod-ci-1548-green"
    description: "All non-receipt CI checks on omniclaude PR #1548 are passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10742/dod-ci-1548-green/command.yaml\""
