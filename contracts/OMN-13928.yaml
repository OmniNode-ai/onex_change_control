---
schema_version: "1.0.0"
ticket_id: "OMN-13928"
title: "omnibase_core 0.46.5 dev->main promotion (OMN-13888 per-entry OCC hashing + dual-accept)"
summary: >-
  Release-chain enabler. Binds OCC evidence for omnibase_core promotion PR #1387 (promotion/omn-13928-core-0465-dev-to-main,
  base=main, v0.46.5). The PR promotes origin/dev (@a68e9e0d, CI-green) to main via git merge -s ours
  origin/main (main is strict:false; tree byte-identical to origin/dev except two pre-existing .onex_state/evidence/*
  files restored from main so the squash does not delete them). 0.46.5 ships the OCC per-entry receipt
  hashing + append-only + supersession + dual-accept validator redesign (OMN-13888, #1382), the project.version
  bump 0.46.4 -> 0.46.5 (OMN-13910, #1384), Handshake Policy Gate live merge_group wiring (OMN-13911,
  #1383), and pr-title-check synchronize+reopened triggers (OMN-13914, #1385). Net-new OCC evidence for
  OMN-13928 because the merged OMN-13888/OMN-13910 receipts bind to dev PRs #1382/#1384, not this dev->main
  promotion PR, and the net-new-only OCC constraint under the core@main 0.46.4 whole-file-hash validator
  forbids re-editing the merged contracts.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-promotion-pr"
    description: >-
      omnibase_core PR #1387 (promotion/omn-13928-core-0465-dev-to-main, base=main) promotes origin/dev
      to main as omnibase_core v0.46.5, carrying the OMN-13888 per-entry OCC hashing + dual-accept validator
      redesign (#1382) and the OMN-13910 version bump (#1384). This PASS receipt binds OMN-13928 to core
      PR #1387 (pr_number + commit_sha) so the verify/verify OCC merge-eligibility gate finds a PASS receipt
      bound to the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13928/dod-core-promotion-pr/command.yaml &&
          grep -q '^pr_number: 1387$' drift/dod_receipts/OMN-13928/dod-core-promotion-pr/command.yaml
  - id: "dod-occ-self-binding"
    description: >-
      onex_change_control PR #3558 carries the OMN-13928 contract and receipt evidence that unblocks the
      omnibase_core 0.46.5 dev->main promotion, with a PASS receipt bound to this OCC evidence PR itself
      (pr_number) so the OCC PR's own occ-preflight eligibility binds.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13928/dod-occ-self-binding/command.yaml && grep
          -q '^pr_number: 3558$' drift/dod_receipts/OMN-13928/dod-occ-self-binding/command.yaml
