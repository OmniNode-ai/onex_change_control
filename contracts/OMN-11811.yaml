---
schema_version: "1.0.0"
ticket_id: "OMN-11811"
title: "node_swarm_registry_compute — pure compute endpoint selection"
summary: >
  COMPUTE node node_swarm_registry_compute. Rehomes endpoint selection logic from SEA. Subscribes to onex.cmd.omnimarket.swarm-select-endpoints.v1
  and publishes onex.evt.omnimarket.swarm-endpoints-selected.v1. Pure deterministic: affinity-first routing
  with capability + context-window fallback, pre-filtered to healthy endpoints only. #magic___^_^___line
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-783"
    description: "omnimarket PR #783 implements node_swarm_registry_compute with 30 unit tests"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11811/dod-omnimarket-pr-783/command.yaml\""
  - id: "dod-occ-pr-1462"
    description: "OCC PR #1462 binds this contract to onex_change_control as the evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11811/dod-occ-pr-1462/command.yaml\""
  - id: "dod-deploy"
    description: "Deploy assessment — new COMPUTE node, no live deploy required for initial merge; node
      is pure and invoked via contract topic by the swarm dispatch orchestrator (OMN-11814)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11811/dod-deploy/command.yaml\""
