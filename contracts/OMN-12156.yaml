---
schema_version: "1.0.0"
ticket_id: "OMN-12156"
title: "Narrow Postgres NetworkPolicy to specific podSelector"
summary: >
  Central OCC evidence for omninode_infra PR #470, which narrows k8s/data-plane/postgres/netpol-allow-onex-dev.yaml
  from allowing all pods in the onex-dev namespace to reach Postgres to a specific podSelector targeting
  app.kubernetes.io/name: onex-api. Resolves OMN-2338.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-470"
    description: >
      omninode_infra PR #470 narrows the Postgres NetworkPolicy ingress from onex-dev to podSelector app.kubernetes.io/name:
      onex-api. YAML valid, pre-commit green, Validate NetworkPolicies CI check passed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12156/dod-omninode-infra-pr-470/command.yaml"
  - id: "dod-yaml-validity"
    description: >
      yamllint and Validate NetworkPolicies CI checks passed on PR #470. NetworkPolicy uses combined namespaceSelector
      + podSelector pattern consistent with netpol-per-service-db.yaml:33.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12156/dod-yaml-validity/command.yaml"
