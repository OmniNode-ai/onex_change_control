---
schema_version: "1.0.0"
ticket_id: "OMN-14112"
title: "Stripe customer creation at tenant email-verification seam"
summary: "OCC evidence for omninode_infra#625, the code-side tenant provisioning seam that creates or
  reuses a Stripe customer after email verification and mirrors stripe_customer_id onto the tenant row.
  Live Stripe credentials, Keycloak claim configuration, and database migration application remain operator-gated
  follow-up steps."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "protocols"
evidence_requirements:
  - kind: "manual"
    description: "omninode_infra PR #625 is open at head d64cdb91397b2d39781d706fd6c38f3654a8e564."
    command: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-head/command.yaml"
  - kind: "manual"
    description: "omninode_infra PR #625 diff scope covers tenant stripe_customer_id migrations, provisioning/billing
      seam code, rate-limit registration, and regression tests."
    command: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-product-diff-scope/command.yaml"
  - kind: "manual"
    description: "omninode_infra PR #625 body records local Docker/onex-api, seam, migration, mypy, ruff,
      and pre-commit verification provenance."
    command: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-verification-provenance/command.yaml"
  - kind: "manual"
    description: "omninode_infra PR #625 explicitly scopes live Stripe secret seeding, Keycloak email_verified
      claim wiring, and dev/prod migration application as operator-gated follow-up steps, not completed
      by this code PR."
    command: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14112/dod-infra625-gated-live-steps/command.yaml"
  - kind: "manual"
    description: "OCC companion PR #4621 carries central OMN-14112 evidence for omninode_infra#625."
    command: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14112/occ-self-bind-pr-4621-omn14112/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode_infra-pr-625-head"
    description: "Binds omninode_infra PR #625 at head d64cdb91397b2d39781d706fd6c38f3654a8e564."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-head/command.yaml
          && grep -q 'd64cdb91397b2d39781d706fd6c38f3654a8e564' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-head/command.yaml"
  - id: "dod-omninode_infra-pr-625-product-diff-scope"
    description: "PR #625 product diff scope covers the tenant stripe_customer_id migration, billing/provision
      seam, read endpoint/rate bucket, and focused regression suites."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-product-diff-scope/command.yaml
          && grep -q 'db/migrations/20260722_tenants_stripe_customer_id.sql' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-product-diff-scope/command.yaml
          && grep -q 'docker/onex-api/billing_stripe.py' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-product-diff-scope/command.yaml
          && grep -q 'docker/onex-api/routers/provision.py' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-product-diff-scope/command.yaml
          && grep -q 'docker/onex-api/tests/test_provision_stripe_seam.py' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-product-diff-scope/command.yaml"
  - id: "dod-omninode_infra-pr-625-verification-provenance"
    description: "PR #625 body records local Docker/onex-api suite, focused seam and migration tests,
      mypy, ruff, and pre-commit verification."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-verification-provenance/command.yaml
          && grep -q '1492 passed, 85 skipped' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-verification-provenance/command.yaml
          && grep -q 'tests/test_provision_stripe_seam.py' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-verification-provenance/command.yaml
          && grep -q 'mypy billing_stripe.py routers/provision.py auth_rate_limit.py' drift/dod_receipts/OMN-14112/dod-omninode_infra-pr-625-verification-provenance/command.yaml"
  - id: "dod-infra625-gated-live-steps"
    description: "PR #625 body keeps live Stripe secret seeding, Keycloak email_verified access-token
      claim wiring, and migration application as explicit gated follow-up work."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^(\"status\": \"PASS\"|status: PASS)$' drift/dod_receipts/OMN-14112/dod-infra625-gated-live-steps/command.yaml
          && grep -q 'STRIPE_API_KEY' drift/dod_receipts/OMN-14112/dod-infra625-gated-live-steps/command.yaml
          && grep -q 'email_verified' drift/dod_receipts/OMN-14112/dod-infra625-gated-live-steps/command.yaml
          && grep -q 'Apply migration' drift/dod_receipts/OMN-14112/dod-infra625-gated-live-steps/command.yaml"
  - id: "occ-self-bind-pr-4621-omn14112"
    description: "OCC companion PR #4621 carries central OMN-14112 evidence for omninode_infra#625."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4621 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid,headRefName"
