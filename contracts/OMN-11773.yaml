---
schema_version: "1.0.0"
ticket_id: "OMN-11773"
title: "feat(OMN-11773): SQL migration for llm_delegation_call_log and llm_delegation_daily_projection"
summary: >
  Creates raw SQL migration 024 in omnibase_infra under docker/migrations/intelligence/ with two idempotent
  projection tables: llm_delegation_call_log (per-call log, UUID PK, UNIQUE call_id idempotency key, CHECK
  constraints on model_tier/usage_source/cost_basis) and llm_delegation_daily_projection (daily aggregate,
  BIGINT token totals, FLOAT8 avg fields, UNIQUE (date, task_type, model_id, model_tier), freshness_state
  CHECK). Part of epic OMN-11771 (LLM delegation cost tracking pipeline).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Migration file 024_create_llm_delegation_projection_tables.sql present and pre-commit
      hooks pass"
    command: "pre-commit run --all-files in omnibase_infra worktree for OMN-11773"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-migration-precommit"
    description: "omnibase_infra PR #1720 carries migration 024 with all pre-commit hooks passing including
      ONEX Migration Sequence Duplicate Check and Writer-Migration Coupling Check."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11773/dod-migration-precommit/command.yaml
          && grep -q '^ticket_id: \"OMN-11773\"$' drift/dod_receipts/OMN-11773/dod-migration-precommit/command.yaml
          && grep -q '^pr_number: 1720$' drift/dod_receipts/OMN-11773/dod-migration-precommit/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-11773 central contract and receipt needed by omnibase_infra PR
      #1720."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11773/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: \"OMN-11773\"$' drift/dod_receipts/OMN-11773/dod-occ-pr/command.yaml"
