---
schema_version: "1.0.0"
ticket_id: "OMN-12919"
title: "Move Gemini provider key/model/endpoint selection behind overlay secret refs"
summary: >-
  onex-self-extending-agent PR #228 removes the Gemini GOOGLE_API_KEY fallback from the judge/demo CLI
  path and requires API-key resolution through the selected model registry entry's declared backend.api_key_env
  secret ref. The current Gemini model, provider, and endpoint remain contract-selected from model_registry.yaml
  and node_agent_orchestrator overlays; alternate local key names must be explicitly bound to the declared
  ref.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-228"
    description: >-
      onex-self-extending-agent PR #228 carries the OMN-12919 implementation that removes provider-specific
      Gemini alias traversal from the judge reproduction path.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12919/dod-sea-pr-228/command.yaml"
  - id: "dod-focused-tests"
    description: >-
      Focused CLI, startup preflight, registry, ADK construction, and orchestrator tests prove the registry
      secret-ref path remains functional and provider aliases no longer satisfy Gemini preflight.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12919/dod-focused-tests/command.yaml"
  - id: "dod-full-unit-suite"
    description: >-
      Full SEA unit suite remains green after the CLI/preflight authority change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12919/dod-full-unit-suite/command.yaml"
  - id: "dod-lint-and-guards"
    description: >-
      Ruff, removed-fallback scan, and commit hooks including the SEA architecture guard passed for the
      product worktree.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12919/dod-lint-and-guards/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      No live deployment or restart was performed for this source-only judge reproducibility fix; hosted
      stability-lane proof remains gated on the approved P2 deploy window.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12919/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for the OCC PR carrying the OMN-12919 central contract and DOD receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12919/dod-occ-pr/command.yaml"
