---
schema_version: "1.0.0"
ticket_id: "OMN-11931"
title: "Knowledge health probe effect and compute nodes"
summary: >
  Bind central evidence for omnimarket PR #828, which adds node_knowledge_health_probe_effect for backend
  probe collection and node_knowledge_health_compute for deterministic health classification.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-828"
    description: "Bind OMN-11931 evidence to omnimarket PR #828."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11931/dod-omnimarket-pr-828/command.yaml
          && grep -q '^pr_number: 828$' drift/dod_receipts/OMN-11931/dod-omnimarket-pr-828/command.yaml"
  - id: "dod-knowledge-health-local-validation"
    description: "Focused knowledge health probe and compute tests plus strict mypy pass for the current
      omnimarket PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11931/dod-knowledge-health-local-validation/command.yaml
          && grep -q '15 passed' drift/dod_receipts/OMN-11931/dod-knowledge-health-local-validation/command.yaml
          && grep -q 'Success: no issues found' drift/dod_receipts/OMN-11931/dod-knowledge-health-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: knowledge health probe and compute contracts declare runtime topics,
      purity, environment dependencies, and deploy readiness without requiring live runtime deploy before
      merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11931/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11931/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR #1561 carries the central OMN-11931 contract, deploy evidence, and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11931/dod-occ-pr/command.yaml &&
          grep -q '^pr_number: 1561$' drift/dod_receipts/OMN-11931/dod-occ-pr/command.yaml"
