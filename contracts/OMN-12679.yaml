---
schema_version: "1.0.0"
ticket_id: "OMN-12679"
title: "SEA DelegationExecutor resolves cloud secrets at the effect boundary"
summary: >-
  Central evidence for replacing DelegationExecutor's direct os.environ secret read with an injected tier
  API-key resolver. The default resolver delegates to the typed model registry entry resolver, and the
  GenerationConsumer effect receives the resolved value only while the provider call effect is being built.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-executor-secret-boundary-tests"
    description: "Focused SEA executor tests prove the GenerationConsumer receives a key from the injected
      effect resolver."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12679/dod-sea-executor-secret-boundary-tests/command.yaml"
