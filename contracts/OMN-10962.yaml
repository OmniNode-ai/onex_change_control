---
schema_version: "1.0.0"
ticket_id: "OMN-10962"
title: "Fix float equality bugs in handler_quality_gate.py (SonarCloud S1244)"
summary: "Replaces float == comparisons with math.isclose() in source and pytest.approx() in tests. Resolves
  SonarCloud S1244 float equality instances. Pure logic fix with no behavior change."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Full omnimarket test suite passes: 4068 passed"
    command: "gh pr checks 675 --repo OmniNode-ai/omnimarket"
  - kind: "ci"
    description: "SonarCloud passes on omnimarket PR #675"
    command: "gh pr checks 675 --repo OmniNode-ai/omnimarket"
  - kind: "manual"
    description: "Float comparison fix — no runtime restart or Kafka topic change required"
    command: "printf '%s\n' 'deploy gate: float equality fix — math.isclose substitution; no docker exec
      or rpk topic produce required'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Full omnimarket test suite passes: 4068 passed, 0 failed"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json headRefOid,title"
  - id: "dod-deploy"
    description: "Float comparison fix — no runtime restart or Kafka topic change required"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\n' 'deploy gate: float equality fix — math.isclose substitution; no docker
          exec or rpk topic produce required'"
