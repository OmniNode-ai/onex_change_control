---
schema_version: "1.0.0"
ticket_id: "OMN-12416"
title: "Type-scope multi-handler runtime dispatch + per-handler result application"
summary: >
  Bind central evidence for the OMN-12416 runtime dispatch fix in omnibase_infra PR #1781: the auto-wired
  MessageDispatchEngine now type-scopes a multi-handler contract so a consumed message routes only to
  the handler whose declared event_model matches the payload, and the dispatch result applier publishes
  a succeeding handler's output even when a sibling handler fails (no poisoning). Proven live on the stability-test
  delegation golden chain — inference-response.v1 publishes a real 284B LLM response (the hop blocked
  in the OMN-12294 diagnosis), with all six omnibase-infra hops + terminal + projection present.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
  - protocols
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1781"
    description: "omnibase_infra PR #1781 carries the type-scoped dispatch + per-handler result application
      fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12416/dod-omnibase-infra-pr-1781/command.yaml\"
          && grep -q '^pr_number: 1781$' \"drift/dod_receipts/OMN-12416/dod-omnibase-infra-pr-1781/command.yaml\""
  - id: "dod-unit-suite"
    description: "Full omnibase_infra unit suite passes with the dispatch fix (20341 passed), plus new
      type-scoping, partial-success applier, and matcher-construction tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12416/dod-unit-suite/command.yaml\"
          && grep -q '20341 passed' \"drift/dod_receipts/OMN-12416/dod-unit-suite/command.yaml\""
  - id: "dod-deploy-stability-test"
    description: "Runtime image rebuilt from the branch and deployed to the stability-test lane (runtime
      + effects only) via docker compose; both services healthy on the new image."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12416/dod-deploy-stability-test/command.yaml\"
          && grep -q 'docker exec' \"drift/dod_receipts/OMN-12416/dod-deploy-stability-test/command.yaml\""
  - id: "dod-live-golden-chain"
    description: "Live delegation golden chain on stability-test: rpk topic produce drove a delegation
      command and all six omnibase-infra hops + terminal published for the correlation id, with the previously-blocked
      inference-response.v1 carrying a real 284B LLM response."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12416/dod-live-golden-chain/command.yaml\"
          && grep -q 'rpk topic produce' \"drift/dod_receipts/OMN-12416/dod-live-golden-chain/command.yaml\"
          && grep -q 'inference-response' \"drift/dod_receipts/OMN-12416/dod-live-golden-chain/command.yaml\""
  - id: "dod-occ-pr-1854"
    description: "onex_change_control PR #1854 carries this central OMN-12416 contract and receipt bundle,
      making the OCC evidence PR itself eligible under the PR-bound receipt gate."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12416/dod-occ-pr-1854/command.yaml\"
          && grep -q '^pr_number: 1854$' \"drift/dod_receipts/OMN-12416/dod-occ-pr-1854/command.yaml\""
  - id: "dod-omnimarket-pr-989"
    description: "omnimarket PR #989 lands the clean OMN-12416 F-a handler endpoint: event_model scoping
      for HandlerLlmDelegationCall plus a runtime handle() path that emits exactly one terminal event."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12416/dod-omnimarket-pr-989/command.yaml\"
          && grep -q '^pr_number: 989$' \"drift/dod_receipts/OMN-12416/dod-omnimarket-pr-989/command.yaml\""
