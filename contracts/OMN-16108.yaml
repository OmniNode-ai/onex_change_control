---
schema_version: "1.0.0"
ticket_id: "OMN-16108"
title: "auto-merge.yml calls bare `gh pr merge --auto` for a disabled merge queue"
summary: >
  OCC evidence companion for OmniNode-ai/omnibase_infra PR #2771 — passes --squash explicitly in the Enable
  Auto-Merge arming step (the "Merge Queue" ruleset id 13269702 is disabled repo-wide, so bare `--auto`
  deterministically fails gh CLI's non-interactive method check). Cosmetic-only fix: Enable Auto-Merge
  is not a required check (dev's required set is exactly `CI Summary`), so this clears red noise, not
  a merge blocker.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2771 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2771"
    description: >
      PR #2771 on OmniNode-ai/omnibase_infra — auto-merge.yml Enable Auto-Merge step now passes `--squash`
      explicitly instead of bare `--auto`, matching the repo's documented squash-only/ no-queue method-flag
      policy. Single-file workflow-only change (.github/workflows/auto-merge.yml), no runtime/product
      code touched.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2771 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,files"
  - id: "occ-self-bind-pr-6643"
    description: "Binds OMN-16108 to OCC companion PR #6643 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6643 --repo OmniNode-ai/onex_change_control --json number,state"
