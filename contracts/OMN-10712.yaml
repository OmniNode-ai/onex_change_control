---
schema_version: "1.0.0"
ticket_id: "OMN-10712"
title: "Declare bare env var reads in omnibase_infra node contracts"
summary: >
  Adds env_dependencies blocks to three omnibase_infra node contracts (node_intent_storage_effect, node_rrh_emit_effect,
  node_runtime_orchestrator) to formally declare bare os.environ/os.getenv reads that existed in handler
  source but had no contract declaration. All declarations use required=false — all env vars have documented
  defaults or graceful fallback paths.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-contract-schema"
    description: "env_dependencies blocks added to three node contracts; yamlfmt and ONEX Contract Validation
      pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-deploy"
    description: >
      Deploy verification: changes are contract schema declarations only — no handler code changes, no
      migration, no new runtime service. env_dependencies declarations are metadata; they do not alter
      runtime behaviour at deploy time. No restart required. The existing running runtime on .201 continues
      to operate identically. No Redpanda topic changes required.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "deploy: no-op — env_dependencies are schema metadata, not runtime wiring; existing
          runtime unaffected"
