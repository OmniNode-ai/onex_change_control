---
schema_version: "1.0.0"
ticket_id: "OMN-10953"
title: "HandlerDelegateSkill dispatch_port optional with fail-closed default"
summary: "Binds omnimarket PR #623, which makes HandlerDelegateSkill.__init__ dispatch_port optional,
  defaulting to a private _UnwiredDelegationDispatchPort that fails closed on first dispatch attempt rather
  than crashing the runtime. Restores wire_from_manifest boot for RUNTIME_PROFILE=main (0 failures over
  159 contracts, previously TypeError abort)."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Downstream PR #623 reports unit test coverage including 2 new tests for the unwired-port
      path, plus wire_from_manifest boot probe with 0 failures."
    command: "gh pr view 623 --repo OmniNode-ai/omnimarket --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-623"
    description: "omnimarket PR #623 carries the OMN-10953 HandlerDelegateSkill optional dispatch_port
      fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10953/dod-omnimarket-pr-623/command.yaml
          && grep -q '^pr_number: 623$' drift/dod_receipts/OMN-10953/dod-omnimarket-pr-623/command.yaml"
  - id: "dod-occ-pr-1000"
    description: "onex_change_control PR #1000 carries the OMN-10953 contract and downstream receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10953/dod-occ-pr-1000/command.yaml
          && grep -q '^pr_number: 1000$' drift/dod_receipts/OMN-10953/dod-occ-pr-1000/command.yaml"
