---
schema_version: "1.0.0"
ticket_id: "OMN-15496"
title: "OCC companion for OmniNode-ai/omnibase_infra#2569 (CI Summary external-context gate)"
summary: >
  Evidence companion binding OMN-15496 to OmniNode-ai/omnibase_infra PR #2569 at head 434e99ca6424c51487856812ae5d2c0e6806ec19.
  The PR teaches the single required `CI Summary` context to assert a measured set of cross-workflow check-run
  contexts against commits/{sha}/check-runs, fail-closed. Authored by the independent adversarial verifier
  for the authorized-wave-0730 lane, not by the implementer.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2569 CI checks on OmniNode-ai/omnibase_infra"
    command: "gh pr checks 2569 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2569"
    description: >
      EXPECTED_EXTERNAL_CONTEXTS exists in scripts/ci/ci_summary_gate.py at PR #2569's exact head 434e99ca6424c51487856812ae5d2c0e6806ec19.
      Falsifiable: the same read at the merge-base 5dc68190 returns zero matches (grep exit 1).
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/ci/ci_summary_gate.py?ref=434e99ca6424c51487856812ae5d2c0e6806ec19
          --jq '.content' | base64 -d | grep -c 'EXPECTED_EXTERNAL_CONTEXTS'
  - id: "dod-ci-yml-verdict-path-wiring"
    description: >
      The ci.yml ci-summary poll step passes the cross-workflow check-runs payload into the VERDICT path
      (not the report-only OMN-14909 cascade step) at PR #2569's exact head. Falsifiable: the merge-base
      5dc68190 returns zero matches for the verdict-path invocation, while still containing the report-only
      step's own --check-runs-file use.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/ci.yml?ref=434e99ca6424c51487856812ae5d2c0e6806ec19
          --jq '.content' | base64 -d | grep -c -- '--check-runs-file check_runs.json --event-name'
  - id: "occ-self-bind-pr-5637"
    description: >
      Self-bind: this OCC companion PR #5637 is the evidence source cited by omnibase_infra#2569. Satisfies
      occ-preflight invariant I3 (OCC's own PRs validate in-tree) by giving OMN-15496 a receipt bound
      to this OCC PR number.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 5637 --repo OmniNode-ai/onex_change_control --json number,state
