---
schema_version: "1.0.0"
ticket_id: "OMN-11168"
title: "Make omniweb Playwright E2E check blocking"
summary: >-
  Central OCC evidence for omniweb PR #129, which promotes the Playwright billing E2E workflow from an
  advisory check to a hard CI gate. The current product PR is intentionally blocked until the repo has
  the required E2E test account secret configured.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "omniweb PR #129 carries the Playwright hard-gate workflow change."
    command: "See drift/dod_receipts/OMN-11168/dod-omniweb-pr-129-head/command.yaml"
  - kind: "manual"
    description: "The red Playwright check is classified as a missing CI secret/configuration blocker."
    command: "See drift/dod_receipts/OMN-11168/dod-playwright-secret-blocker/command.yaml"
  - kind: "manual"
    description: "The OCC PR carries this central contract and blocker evidence bundle."
    command: "See drift/dod_receipts/OMN-11168/dod-occ-pr-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniweb-pr-129-head"
    description: "omniweb PR #129 carries the Playwright E2E hard-gate workflow change at the observed
      head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11168/dod-omniweb-pr-129-head/command.yaml"
          && grep -q '^pr_number: 129$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11168/dod-omniweb-pr-129-head/command.yaml"
          && grep -q '^commit_sha: "9e781e6b74c0c48d29fbbe8ae51a6616104c23fd"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11168/dod-omniweb-pr-129-head/command.yaml"
  - id: "dod-playwright-secret-blocker"
    description: "The hard Playwright gate currently fails closed because E2E_TEST_PASSWORD is not configured
      in the omniweb repo."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11168/dod-playwright-secret-blocker/command.yaml"
          && grep -q 'E2E_TEST_PASSWORD must be set in CI' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11168/dod-playwright-secret-blocker/command.yaml"
          && grep -q 'no E2E secrets or variables configured' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11168/dod-playwright-secret-blocker/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11168 blocker evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11168/dod-occ-pr-self/command.yaml"
