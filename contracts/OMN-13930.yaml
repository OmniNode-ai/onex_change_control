---
schema_version: "1.0.0"
ticket_id: "OMN-13930"
title: "Autobind OCC evidence for OMN-13930"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2503."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2503 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2503"
    description: "PR #2503 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2503-ci"
    description: "PR #2503 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5155"
    description: "OCC companion PR #5155 — self-bind for OMN-13930 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnibase-infra-pr-2503-deploy-scope-304891d1"
    description: >-
      Deploy-scope falsifiable probe (OMN-8912 gate) for omnibase_infra PR #2503 at pinned head 304891d1.
      HONEST SCOPE STATEMENT: this diff ships NO deployed artifact -- it touches only src/omnibase_infra/cli/*.py,
      tests, and a runbook; no Dockerfile, no compose file, no k8s manifest, no container image, no service.
      `onex delegate` is a locally-installed CLI entry point, so there is no running surface on any .201
      lane, cluster, or container whose state a docker/kubectl/rpk/psql probe could interrogate for this
      change; a probe against one would be vacuous theatre, not proof. The deploy-gate's own error text
      names the hermetic `gh api .../contents/<changed_path>?ref=<sha>` form as the sanctioned probe for
      exactly this compute-only / no-live-deploy class. Four clauses over the two runtime paths the gate
      flagged: the DRIFT_OVERRIDE_ENV constant is defined with its literal value in omnimarket_drift_guard.py;
      check_omnimarket_drift carries the keyword-only fail-closed `allow_drift: bool = False` parameter;
      cli_delegate.py actually CALLS check_omnimarket_drift (the wiring that was missing -- an import
      alone would not fire the guard); and cli_delegate.py exposes the --allow-omnimarket-drift override
      option. Falsifiability PROVEN by execution: exit 0 at 304891d1, exit 1 at omnibase_infra@dev. Reads
      product source over the live GitHub API, never this receipt or this contract. Independent verifier,
      not the implementer. Read-only; no deploy, restart, lane mutation, Kafka mutation, DB mutation,
      or prod mutation was performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/cli/omnimarket_drift_guard.py?ref=304891d121cf2551a95fae2c834b23fa716065e5'
          --jq .content | base64 -d | grep -q 'DRIFT_OVERRIDE_ENV = "ONEX_ALLOW_OMNIMARKET_DRIFT"' &&
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/cli/omnimarket_drift_guard.py?ref=304891d121cf2551a95fae2c834b23fa716065e5'
          --jq .content | base64 -d | grep -q 'allow_drift: bool = False' && gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/cli/cli_delegate.py?ref=304891d121cf2551a95fae2c834b23fa716065e5'
          --jq .content | base64 -d | grep -q 'check_omnimarket_drift(' && gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/cli/cli_delegate.py?ref=304891d121cf2551a95fae2c834b23fa716065e5'
          --jq .content | base64 -d | grep -q 'allow-omnimarket-drift'
  - id: "occ-self-bind-pr-5171"
    description: >-
      OCC PR #5171 -- self-bind for the OMN-13930 deploy-scope probe (OMN-14650). Binds the falsifiable
      content probe over omnibase_infra #2503's two gate-flagged runtime paths to the OCC PR that carries
      it.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 5171 --repo OmniNode-ai/onex_change_control --json number,state
