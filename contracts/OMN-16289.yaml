---
schema_version: "1.0.0"
ticket_id: "OMN-16289"
title: "Release-synced main: on every release, main fast-forwards to the tag SHA"
summary: >
  OCC evidence for the release-synced-main policy (operator confirmed 2026-08-20): on every release tag
  vX.Y.Z, the repo's main is automatically fast-forwarded to the tag SHA via a new "Sync main to release
  tag" step appended to release.yml. Promotion PRs to main are retired. This contract accumulates one
  dod_evidence entry per wave-1 repo's release.yml PR (omnimarket first as live proof, then omnibase_core,
  omnibase_infra, omnibase_spi, omnibase_compat, omnimemory), each a content-pinned differential probe
  proving the sync step is absent at the PR's base SHA and present at its head SHA. It also records the
  onex_change_control Lane B cleanup that removes the obsolete nightly-promote cron while retaining workflow_dispatch
  for manual/internal reconciliation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2112"
    description: >
      PR #2112 on OmniNode-ai/omnimarket -- adds a "Sync main to release tag" step to .github/workflows/release.yml,
      run after "Create GitHub Release" succeeds (skipped on rc prereleases): resolves the published tag
      to its commit SHA via `git rev-list -n1 <tag>` and non-force-pushes that SHA to refs/heads/main,
      using the same GITHUB_TOKEN identity actions/checkout already persisted for the job. Content-pinned
      check: fetches release.yml at the PR's base and head SHAs via the GitHub contents API and confirms
      the step name is absent at base and present at head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnimarket/contents/.github/workflows/release.yml?ref=cbc7ffeeb333e171ad4ff0e2f78ad66c4b4fc4e4"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnimarket/contents/.github/workflows/release.yml?ref=a611ff25874164a08d8632d310533620f0d4a0de"
          --jq .content | base64 -d)"; ! printf '%s' "$base_body" | grep -qF 'Sync main to release tag'
          && printf '%s' "$head_body" | grep -qF 'Sync main to release tag' && printf '%s' "$head_body"
          | grep -qF 'git push origin "${TAG_SHA}:refs/heads/main"' && echo "OMN-16289-OMNIMARKET-2112-DIFFERENTIAL-PROBE-PASS:
          release.yml gains the Sync-main-to-release-tag step between base cbc7ffee and head a611ff25"
  - id: "occ-self-bind-pr-6775"
    description: "Binds OMN-16289 to OCC companion PR #6775 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6775 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2813"
    description: >
      PR #2813 on OmniNode-ai/omnibase_infra -- adds the same "Sync main to release tag" step to .github/workflows/release.yml
      as omnimarket#2112, run after "Create GitHub Release" succeeds (skipped on rc prereleases). Content-pinned
      check: fetches release.yml at the PR's base and head SHAs via the GitHub contents API and confirms
      the step name is absent at base and present at head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/release.yml?ref=381333da507754fa35dc6c292fbe76208f34f314"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/release.yml?ref=f75dfdeb4f6e08f5f96111e35e419605a9830a0e"
          --jq .content | base64 -d)"; ! printf '%s' "$base_body" | grep -qF 'Sync main to release tag'
          && printf '%s' "$head_body" | grep -qF 'Sync main to release tag' && printf '%s' "$head_body"
          | grep -qF 'git push origin "${TAG_SHA}:refs/heads/main"' && echo "OMN-16289-OMNIBASE_INFRA-2813-DIFFERENTIAL-PROBE-PASS:
          release.yml gains the Sync-main-to-release-tag step between base 381333da5 and head f75dfdeb4"
  - id: "dod-OmniNode-ai-omnibase_compat-pr-189"
    description: >
      PR #189 on OmniNode-ai/omnibase_compat -- adds the same "Sync main to release tag" step to .github/workflows/release.yml
      as omnimarket#2112, run after "Create GitHub Release" succeeds (skipped on rc prereleases). Content-pinned
      check: fetches release.yml at the PR's base and head SHAs via the GitHub contents API and confirms
      the step name is absent at base and present at head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnibase_compat/contents/.github/workflows/release.yml?ref=4c744485638008207b3c4f2abaa349b4868d3fe4"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnibase_compat/contents/.github/workflows/release.yml?ref=d76f78aaf019d7d527f3e544e4e6654469d1a228"
          --jq .content | base64 -d)"; ! printf '%s' "$base_body" | grep -qF 'Sync main to release tag'
          && printf '%s' "$head_body" | grep -qF 'Sync main to release tag' && printf '%s' "$head_body"
          | grep -qF 'git push origin "${TAG_SHA}:refs/heads/main"' && echo "OMN-16289-OMNIBASE_COMPAT-189-DIFFERENTIAL-PROBE-PASS:
          release.yml gains the Sync-main-to-release-tag step between base 4c744485 and head d76f78aa"
  - id: "dod-OmniNode-ai-omnimemory-pr-431"
    description: >
      PR #431 on OmniNode-ai/omnimemory -- adds the same "Sync main to release tag" step to .github/workflows/release.yml
      as omnimarket#2112, run after "Create GitHub Release" succeeds (skipped on rc prereleases), using
      raw_tag rather than the rc/namespace-normalized tag because that is the actual git ref (this repo's
      tags may carry an omnimemory/ namespace prefix). Content-pinned check: fetches release.yml at the
      PR's base and head SHAs via the GitHub contents API and confirms the step name is absent at base
      and present at head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnimemory/contents/.github/workflows/release.yml?ref=1b1092e6b1abc1806aa602c8eaabe4261c1d8336"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnimemory/contents/.github/workflows/release.yml?ref=c267b89c716d4f4d81d10f15820781b872fe93b1"
          --jq .content | base64 -d)"; ! printf '%s' "$base_body" | grep -qF 'Sync main to release tag'
          && printf '%s' "$head_body" | grep -qF 'Sync main to release tag' && printf '%s' "$head_body"
          | grep -qF 'git push origin "${TAG_SHA}:refs/heads/main"' && echo "OMN-16289-OMNIMEMORY-431-DIFFERENTIAL-PROBE-PASS:
          release.yml gains the Sync-main-to-release-tag step between base 1b1092e6 and head c267b89c"
  - id: "dod-OmniNode-ai-omnibase_spi-pr-271"
    description: >
      PR #271 on OmniNode-ai/omnibase_spi -- adds the same "Sync main to release tag" step to .github/workflows/release.yml
      as omnimarket#2112, run after "Create GitHub Release" succeeds (skipped on rc prereleases). Content-pinned
      check: fetches release.yml at the PR's base and head SHAs via the GitHub contents API and confirms
      the step name is absent at base and present at head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnibase_spi/contents/.github/workflows/release.yml?ref=342991764712b439df4fd62317eed4289bd69ab9"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnibase_spi/contents/.github/workflows/release.yml?ref=9c1537aa348aceb7608c790c7987ca05958cba2e"
          --jq .content | base64 -d)"; ! printf '%s' "$base_body" | grep -qF 'Sync main to release tag'
          && printf '%s' "$head_body" | grep -qF 'Sync main to release tag' && printf '%s' "$head_body"
          | grep -qF 'git push origin "${TAG_SHA}:refs/heads/main"' && echo "OMN-16289-OMNIBASE_SPI-271-DIFFERENTIAL-PROBE-PASS:
          release.yml gains the Sync-main-to-release-tag step between base 34299176 and head 9c1537aa"
  - id: "occ-self-bind-pr-6776"
    description: "Binds OMN-16289 to OCC companion PR #6776 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6776 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "dod-OmniNode-ai-omnibase_core-pr-1569"
    description: >
      PR #1569 on OmniNode-ai/omnibase_core -- adds the same "Sync main to release tag" step to .github/workflows/release.yml
      as omnimarket#2112, run after "Create GitHub Release" succeeds (skipped on rc prereleases). Content-pinned
      check: fetches release.yml at the PR's base and head SHAs via the GitHub contents API and confirms
      the step name is absent at base and present at head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnibase_core/contents/.github/workflows/release.yml?ref=cd7289790ea7f69be2101f71aaf6bd1a1b4bb53d"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnibase_core/contents/.github/workflows/release.yml?ref=874de43c28e0c2203a74b8c57869a15ea4209828"
          --jq .content | base64 -d)"; ! printf '%s' "$base_body" | grep -qF 'Sync main to release tag'
          && printf '%s' "$head_body" | grep -qF 'Sync main to release tag' && printf '%s' "$head_body"
          | grep -qF 'git push origin "${TAG_SHA}:refs/heads/main"' && echo "OMN-16289-OMNIBASE_CORE-1569-DIFFERENTIAL-PROBE-PASS:
          release.yml gains the Sync-main-to-release-tag step between base cd728979 and head 874de43c"
  - id: "occ-self-bind-pr-6790"
    description: "Binds OMN-16289 to OCC companion PR #6790 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6790 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "dod-nightly-promote-cron-removed"
    description: "nightly-promote.yml's on: block no longer declares a schedule cron trigger; workflow_dispatch
      is preserved unchanged."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python3 -c \"import yaml; d=yaml.safe_load(open('.github/workflows/nightly-promote.yml'));
          assert 'schedule' not in d[True] and 'workflow_dispatch' in d[True]; print('PASS')\""
  - id: "occ-self-bind-pr-6777"
    description: "Binds OMN-16289 (Lane B) to OCC PR #6777."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6777 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "dod-OmniNode-ai-omnibase_core-pr-1659"
    description: >
      PR #1659 on OmniNode-ai/omnibase_core -- adds the SYNC-ONLY recovery path for the release-synced-main
      policy. The main fast-forward is the last step of release.yml's `release` job, sequenced behind
      `Publish to PyPI` and `Create GitHub Release`, so when only that step fails (runs 34030901325 for
      v0.47.4 and 34052816626 for v0.47.5: publish SUCCESS, GitHub Release SUCCESS, `Mint onexbot-occ-writer
      app token` FAILURE with HTTP 422 because the App installation lacked `workflows: write`) neither
      a `--failed` re-run nor a `-f tag=` dispatch can complete the pointer move without re-publishing.
      This PR adds a `sync_main_to_tag` workflow_dispatch input and a `sync-main` job that runs the mint
      + fast-forward alone, guarded ancestor-of-dev and fast-forward-only. Content-pinned check: fetches
      release.yml at the PR's base and head SHAs via the GitHub contents API and confirms the sync-only
      input and job are absent at base and present at head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnibase_core/contents/.github/workflows/release.yml?ref=65079d6541b347c379afd86007f0fa10590f9939"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnibase_core/contents/.github/workflows/release.yml?ref=31f593133362c808b288337a47d7f599c5ff3f35"
          --jq .content | base64 -d)"; ! printf '%s' "$base_body" | grep -qF 'sync_main_to_tag' && printf
          '%s' "$head_body" | grep -qF 'sync_main_to_tag' && printf '%s' "$head_body" | grep -qF 'sync-main:'
          && printf '%s' "$head_body" | grep -qF 'Validate sync tag' && echo "OMN-16289-OMNIBASE_CORE-1659-DIFFERENTIAL-PROBE-PASS:
          release.yml gains the sync-only sync_main_to_tag dispatch path between base 65079d65 and head
          31f59313"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3256"
    description: >
      PR #3256 on OmniNode-ai/omnibase_infra -- the same sync-only recovery path as omnibase_core#1659,
      for the release whose sync did not run (run 34037128862 for v0.38.19: publish SUCCESS, GitHub Release
      SUCCESS, `Mint onexbot-occ-writer app token` FAILURE with HTTP 422). Here a `-f tag=` re-dispatch
      would additionally re-fire the dependency cascade and re-dispatch the runtime image rebuild, so
      both of those jobs also gain an explicit `needs.release.result == 'success'` guard. Content-pinned
      check: fetches release.yml at the PR's base and head SHAs via the GitHub contents API and confirms
      the sync-only input and job are absent at base and present at head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/release.yml?ref=bc672163b8d04ada98ae59c9dc185a14a41b2432"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/release.yml?ref=9ccf4568a982fa62a4cb5246bb5abab98ce48bf3"
          --jq .content | base64 -d)"; ! printf '%s' "$base_body" | grep -qF 'sync_main_to_tag' && printf
          '%s' "$head_body" | grep -qF 'sync_main_to_tag' && printf '%s' "$head_body" | grep -qF 'sync-main:'
          && printf '%s' "$head_body" | grep -qF 'Validate sync tag' && echo "OMN-16289-OMNIBASE_INFRA-3256-DIFFERENTIAL-PROBE-PASS:
          release.yml gains the sync-only sync_main_to_tag dispatch path between base bc672163 and head
          9ccf4568"
  - id: "occ-self-bind-pr-6878"
    description: "Binds OMN-16289 to OCC companion PR #6878 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6878 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-8464"
    description: "Binds OMN-16289 to OCC companion PR #8464 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8464 --repo OmniNode-ai/onex_change_control --json number,state"
