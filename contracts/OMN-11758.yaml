---
schema_version: 1.0.0
ticket_id: OMN-11758
title: Fix post-merge main-green blockers before OMN-11714 branch split
summary: Fix post-merge main-green blockers before OMN-11714 branch split
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: All tests pass
    command: uv run pytest tests/ -v
  - kind: ci
    description: CI passes
    command: pre-commit run --all-files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Main-green blockers are documented with exact run/job evidence
    source: linear
    linear_dod_text: Main-green blockers are documented with exact run/job evidence
    checks:
      - check_type: command
        check_value: grep -q 'omnibase_compat main `84e41b699352898b202c14a4b8f2c3f24b21ae58`' drift/dod_receipts/OMN-11758/dod-001/command.yaml
  - id: dod-002
    description: At least one code-level workflow blocker is fixed with local validation
    source: linear
    linear_dod_text: At least one code-level workflow blocker is fixed with local validation
    checks:
      - check_type: command
        check_value: git -C "$OMNI_HOME/omni_worktrees/OMN-11758/omnibase_compat" diff --check && python
          -c "import yaml; yaml.safe_load(open('$OMNI_HOME/omni_worktrees/OMN-11758/omnibase_compat/.github/workflows/ci.yml'))"
  - id: dod-003
    description: Any remaining external/permission blockers are ticketed or classified with owner evidence
    source: linear
    linear_dod_text: Any remaining external/permission blockers are ticketed or classified with owner
      evidence
    checks:
      - check_type: command
        check_value: grep -q 'AccessDeniedException' drift/dod_receipts/OMN-11758/dod-003/command.yaml
  - id: dod-004
    description: Worktree cleanup status is recorded before closeout
    source: linear
    linear_dod_text: Worktree cleanup status is recorded before closeout
    checks:
      - check_type: command
        check_value: test -d "$OMNI_HOME/omni_worktrees/OMN-11758/omnibase_compat" && test -d "$OMNI_HOME/omni_worktrees/OMN-11758/onex_change_control"
