---
schema_version: "1.0.0"
ticket_id: "OMN-13858"
title: "docs(OMN-13858): fix pipeline_fill's broken run-node dispatch docs"
summary: >-
  omniclaude PR #1837 fixes pipeline_fill/SKILL.md, whose Routing Contract only documented the Kafka-backed
  onex run-node dispatch (unusable when the broker is unreachable) and whose State Files section contradicted
  its own Verification section on where state actually lives. Adds the verified onex node/onex run local
  fallback and corrects the state path to $ONEX_STATE_DIR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1837"
    description: >-
      Consuming-PR binding: omniclaude PR #1837 carries the OMN-13858 pipeline_fill documentation fix,
      verified live against the real onex node/onex run-node CLI and the real node_pipeline_fill handler.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13858/dod-omniclaude-pr-1837/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13858 contract and receipts consumed
      by omniclaude PR #1837.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13858/dod-occ-self/command.yaml"
