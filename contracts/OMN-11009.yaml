---
schema_version: "1.0.0"
ticket_id: "OMN-11009"
title: "Runtime paths fail closed when Kafka bootstrap config is absent"
summary: >
  Bind the scoped runtime fallback hardening for omnimarket PR #668 and omniclaude PR #1594. The omnimarket
  live build-loop event bus now resolves Kafka bootstrap config through typed Settings and refuses to
  downgrade to EventBusInmemory. The omniclaude constraint-injection representative path was triaged as
  policy injection, not bus bootstrap, and its BUS_POLICY rule now requires typed Kafka settings or fail-closed
  runtime behavior while isolating in-memory buses to explicit unit/local test profiles.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused omnimarket live event-bus bootstrap tests pass."
    command: "See drift/dod_receipts/OMN-11009/dod-omnimarket-tests/command.yaml"
  - kind: "tests"
    description: "Focused omniclaude constraint-injection tests pass."
    command: "See drift/dod_receipts/OMN-11009/dod-omniclaude-tests/command.yaml"
  - kind: "ci"
    description: "Focused ruff checks pass for the touched files in both repos."
    command: "See drift/dod_receipts/OMN-11009/dod-focused-lint/command.yaml"
  - kind: "manual"
    description: "Deploy assessment recorded: no live deploy, runtime restart, or .201 mutation performed."
    command: "See drift/dod_receipts/OMN-11009/dod-deploy-assessment/command.yaml"
  - kind: "ci"
    description: "Downstream implementation PRs are open and linked to OMN-11009."
    command: "See drift/dod_receipts/OMN-11009/dod-pr-links/command.yaml"
  - kind: "ci"
    description: "Central OCC PR carries the OMN-11009 contract and receipt bundle."
    command: "See drift/dod_receipts/OMN-11009/dod-occ-pr-1074/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-tests"
    description: "Focused omnimarket tests prove live runtime event-bus bootstrap refuses in-memory downgrade."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '3 passed' drift/dod_receipts/OMN-11009/dod-omnimarket-tests/command.yaml"
  - id: "dod-omniclaude-tests"
    description: "Focused omniclaude tests prove the injected bus policy requires typed Kafka config or
      fail-closed runtime behavior."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '23 passed' drift/dod_receipts/OMN-11009/dod-omniclaude-tests/command.yaml"
  - id: "dod-focused-lint"
    description: "Focused ruff format/check commands passed for the touched files in both downstream repos."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'All checks passed' drift/dod_receipts/OMN-11009/dod-focused-lint/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Deploy assessment: no live deploy required for this scoped source/test policy change;
      no runtime restart or .201 mutation performed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'No live deploy required' drift/dod_receipts/OMN-11009/dod-deploy-assessment/command.yaml"
  - id: "dod-pr-links"
    description: "Downstream PRs are open: omnimarket #668 and omniclaude #1594."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'omnimarket/pull/668' drift/dod_receipts/OMN-11009/dod-pr-links/command.yaml
          && grep -q 'omniclaude/pull/1594' drift/dod_receipts/OMN-11009/dod-pr-links/command.yaml"
  - id: "dod-occ-pr-1074"
    description: "onex_change_control PR #1074 carries the central OMN-11009 contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11009/dod-occ-pr-1074/command.yaml
          && grep -q '^pr_number: 1074$' drift/dod_receipts/OMN-11009/dod-occ-pr-1074/command.yaml"
