---
schema_version: "1.0.0"
ticket_id: OMN-10053
title: "Fix platform_readiness runner and route skills to the local module entry point"
summary: "Adds a Kafka-free `python -m omnimarket.nodes.node_platform_readiness` runner and updates the
  omniclaude readiness skill surfaces away from the non-existent `onex run` command and the Kafka-only
  `onex run-node` path."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "omnimarket platform readiness golden-chain tests, including module CLI coverage, pass"
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-10053/omnimarket &&
      uv run pytest tests/test_golden_chain_platform_readiness.py -q"
  - kind: manual
    description: "platform readiness module runner emits valid JSON locally without Kafka"
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-10053/omnimarket &&
      uv run python -m omnimarket.nodes.node_platform_readiness --dry-run --output-format json"
  - kind: manual
    description: "omniclaude readiness skills no longer reference the missing `onex run node_platform_readiness`
      or Kafka-only `run-node node_platform_readiness` paths"
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-10053/omniclaude &&
      ! rg -n 'onex run node_platform_readiness|run-node node_platform_readiness' plugins/onex/skills"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused omnimarket readiness tests pass, including module CLI JSON and dimension-filter
      coverage"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10053/dod-001/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10053/dod-001/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10053/dod-001/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-002
    description: "Dry-run module runner exits 0 and emits valid PASS JSON without Kafka"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10053/dod-002/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10053/dod-002/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10053/dod-002/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-003
    description: "Live module runner exits 0 and emits valid platform readiness JSON; remaining FAIL dimensions
      are real readiness findings"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10053/dod-003/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10053/dod-003/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10053/dod-003/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-004
    description: "Targeted omniclaude readiness skill surfaces no longer reference missing or Kafka-only
      platform_readiness dispatch paths"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10053/dod-004/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10053/dod-004/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10053/dod-004/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-005
    description: "omniclaude skill aspiration validation passes after aligning the stale verification_receipt_generator
      claim"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10053/dod-005/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10053/dod-005/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10053/dod-005/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
