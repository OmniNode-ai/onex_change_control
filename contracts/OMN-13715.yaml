---
schema_version: "1.0.0"
ticket_id: "OMN-13715"
title: "feat(OMN-13715): accept runtime_sweep scope in RuntimeSweepRequest"
summary: >
  omnimarket PR #1512 adds `scope: str | None = None` to RuntimeSweepRequest so the documented `onex skill
  runtime_sweep --scope <value>` payload is accepted by the pydantic request model, whose `extra="forbid"`
  setting previously rejected `scope` as an extra input. The change is limited to src/omnimarket/nodes/node_runtime_sweep/handlers/handler_runtime_sweep.py
  at PR head a8b3aa1a7a0d94b0e12842c7f407e86f5f1585dc and does not perform or authorize a live deploy,
  runtime restart, topic mutation, schema migration, database migration, or .201/.200 mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Runtime sweep request-model and handler tests pass for the omnimarket PR #1512 change."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13715/dod-local-runtime-sweep-tests/command.yaml"
  - kind: "manual"
    description: "omnimarket PR #1512 is a single request-model code change and no live deploy or runtime
      mutation was performed."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13715/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1512"
    description: >
      omnimarket PR #1512 is bound to branch jonah/omn-13715-bug-runtime_sweep-reports-clean-while-checking-0-entities
      at head a8b3aa1a7a0d94b0e12842c7f407e86f5f1585dc and changes only the runtime sweep handler request
      model.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13715/dod-omnimarket-pr-1512/command.yaml
          && grep -q '^pr_number: 1512$' drift/dod_receipts/OMN-13715/dod-omnimarket-pr-1512/command.yaml"
  - id: "dod-local-runtime-sweep-tests"
    description: >
      Focused local validation in the OMN-13715 omnimarket worktree proved RuntimeSweepRequest accepts
      `scope`, preserves `extra="forbid"` for unknown fields, and passes the targeted runtime-sweep test
      files.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13715/dod-local-runtime-sweep-tests/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Deploy assessment for omnimarket PR #1512: no live deploy, runtime restart, topic mutation, schema
      migration, database migration, or .201/.200 mutation was performed or authorized. Live GitHub PR
      metadata shows the PR changes only the runtime sweep handler file.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13715/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-13715/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-self"
    description: >
      Self-binding OCC eligibility receipt for the onex_change_control PR carrying this refreshed OMN-13715
      central contract and receipts for omnimarket PR #1512.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13715/dod-occ-pr-self/command.yaml"
