---
schema_version: "1.0.0"
ticket_id: "OMN-11384"
title: "Event chain ASCII visualization"
summary: >-
  onex-self-extending-agent PR #74 adds an event-chain ASCII visualizer and prints observed event lineage
  after agent demo runs without changing authoritative runtime topology.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: tests
    description: "Focused event-chain visualizer and agent demo unit tests pass locally after rebase."
    command: "uv run pytest tests/unit/test_chain_visualizer.py tests/unit/test_agent_demo.py -q"
  - kind: manual
    description: "Touched visualization and demo entrypoints pass ruff."
    command: "uv run ruff check src/pipeline/chain_visualizer.py src/agent/agent_demo.py src/__main__.py
      tests/unit/test_chain_visualizer.py tests/unit/test_agent_demo.py"
  - kind: manual
    description: "No live deploy required; local CLI/demo visualization only."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11384/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-74"
    description: "onex-self-extending-agent PR #74 carries the rebased ASCII event-chain visualization
      implementation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11384/dod-product-pr-74/command.yaml"
          && grep -q '^pr_number: 74$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11384/dod-product-pr-74/command.yaml"
          && grep -q '^commit_sha: "1d3711169addf75b3cf28945c39f1122a9398dd7"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11384/dod-product-pr-74/command.yaml"
  - id: "dod-local-proof"
    description: "Focused local visualization tests and ruff checks pass after the rebase."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11384/dod-local-proof/command.yaml"
          && grep -q '11 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11384/dod-local-proof/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11384/dod-local-proof/command.yaml"
  - id: "dod-deploy"
    description: "No live deploy required; event-chain visualization is local CLI/demo output."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11384/dod-deploy/command.yaml"
