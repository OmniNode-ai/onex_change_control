---
schema_version: '1.0.0'
ticket_id: OMN-10558
title: 'Task 12: AdapterEnvSecretStore (nullable env-var lookup)'
summary: 'env-backed ProtocolSecretStore for local-dev / no-Infisical runtime profiles'
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Unit tests cover nullable lookup, read-only writes, prefix list, lifecycle
    command: uv run pytest tests/unit/secret_stores/test_adapter_env_secret_store.py -v
  - kind: ci
    description: omnibase_infra PR 1507 CI green
    command: gh pr checks 1507 --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: New AdapterEnvSecretStore module present in PR 1507 diff
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 1507 --repo OmniNode-ai/omnibase_infra | grep -q 'src/omnibase_infra/secret_stores/adapter_env_secret_store.py'
  - id: dod-002
    description: get_secret returns os.environ.get(key) (nullable)
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 1507 --repo OmniNode-ai/omnibase_infra | grep -q 'os.environ.get(key)'
  - id: dod-003
    description: set_secret and delete_secret raise RuntimeError("read-only")
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 1507 --repo OmniNode-ai/omnibase_infra | grep -q 'AdapterEnvSecretStore is read-only'
  - id: dod-004
    description: omnibase_infra PR 1507 has CI passing
    source: manual
    checks:
      - check_type: command
        check_value: gh pr checks 1507 --repo OmniNode-ai/omnibase_infra | grep -c 'pass' | grep -qv '^0$'
  - id: dod-005
    description: PR 1507 open against main branch
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 1507 --repo OmniNode-ai/omnibase_infra --json state,baseRefName --jq 'select(.state=="OPEN" and .baseRefName=="main") | "ok"' | grep -q ok
