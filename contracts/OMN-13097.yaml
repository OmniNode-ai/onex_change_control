---
schema_version: "1.0.0"
ticket_id: OMN-13097
title: "Phase 4a: onex skill subcommand + declarative skill->node mapping + 24 omniclaude shim migrations"
summary: >-
  Add the `onex skill <name> [args]` dispatch surface (omnibase_infra) that the 24 omniclaude dispatch-shim
  migrations build on. The command resolves the skill via the declarative skill_mapping.yaml registry,
  builds the backing node's input payload from the skill's CLI args (written under the state root, never
  /tmp), resolves the node's packaged contract exactly like `onex node`, and dispatches through the proven
  receipt-mode path (run_receipt_mode, OMN-13094) — stdout is exactly one typed ModelSkillResult JSON
  with the FULL handler result. The skill->node mapping is declarative DATA: all 24 shims mapped to their
  backing onex.nodes node + typed result-model FQN (verified against each live handler handle() return
  type) + per-arg payload specs + classifiers; adding a skill is a YAML edit, never CLI code. Typed models
  split one-per-file.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-skill-tests"
    description: >-
      onex skill unit tests pass (21 passed, 1 skipped): registry validity, all-24-dispatch-shim coverage,
      fully qualified result models, arg parsing/coercion/positional/required, keyword classifiers, payload
      construction, receipt-mode dispatch wiring, and payload-written-under-state-root-not-/tmp.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13097/dod-skill-tests/command.yaml && grep -q
          '21 passed' drift/dod_receipts/OMN-13097/dod-skill-tests/command.yaml
  - id: "dod-mypy-strict"
    description: "mypy --strict clean across src/ (2438 source files)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13097/dod-mypy-strict/command.yaml && grep -q
          'no issues found' drift/dod_receipts/OMN-13097/dod-mypy-strict/command.yaml
  - id: "dod-omniclaude-shim-migrations"
    description: >-
      All 24 omniclaude dispatch-only shims migrated to the single-command pattern: SKILL.md declares
      skill_kind: dispatch; prompt.md procedure bodies DELETED (frontmatter-free, one onex skill / onex
      delegate command + result presentation, no bare onex run/node/run-node, no cat workflow_result.json,
      no 'surface verbatim'); skill-local executable logic removed (merge_sweep _lib/run.py + run.sh,
      hostile_reviewer _lib/aggregate_reviews.py, pr_review sub-skill scripts, delegate _lib). New parametrized
      contract suite test_dispatch_skill_receipt_contract.py validates all 24 against the new single-command
      receipt invariants (120 passed); skills + hooks suites green (2650 passed, 1 skipped). Stale per-skill
      shim-contract tests encoding the deleted onex run-node procedure-body pattern removed.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13097/dod-omniclaude-shim-migrations/command.yaml
          && grep -q '120 passed' drift/dod_receipts/OMN-13097/dod-omniclaude-shim-migrations/command.yaml
  - id: "dod-omniclaude-precommit"
    description: >-
      omniclaude pre-commit run --all-files passes (51 hooks) on the migrated skills, including the skill-mcp-ref-lint,
      aislop, skill-hygiene, instructional-skill-enforcement, and imperative-pattern guards; mypy src/omniclaude/
      clean (711 source files); ruff format + check clean.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13097/dod-omniclaude-precommit/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      onex_change_control OCC PR carries the OMN-13097 contract + dod receipts (self-binding OCC eligibility
      receipt).
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13097/dod-occ-pr-self/command.yaml
  - id: "dod-deploy-user-gated"
    description: >-
      Deploy gate for the gap-skill follow-up PRs. omnimarket #1292 changes the node_gap_compute contract.yaml
      handler_routing (declares operation: gap on the operation_match handler) — a runtime behavior change.
      No docker exec, rpk topic produce, or .201 mutation is performed by these PRs; the live apply is
      the user-gated runtime redeploy on the next deploy.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13097/dod-deploy-user-gated/command.yaml &&
          printf '%s\n' 'deploy gate: node_gap_compute contract-op change applies on the next user-gated
          runtime redeploy'
