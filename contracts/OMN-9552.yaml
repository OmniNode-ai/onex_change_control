---
schema_version: "1.0.0"
ticket_id: OMN-9552
title: "Runtime health monitor boot grace"
summary: "Suppress runtime health monitor publishes during startup until Kafka topic provisioning has
  had a chance to complete."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Focused health-monitor tests prove boot-grace validation and delayed first-emission
      semantics."
    command: "uv run pytest tests/unit/services/test_service_runtime_health_monitor.py tests/integration/test_runtime_crash_loop_fix.py
      -q"
  - kind: ci
    description: "omnibase_infra PR #1393 checks pass after OCC evidence is merged."
    command: "gh pr checks 1393 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "PR #1393 diff keeps ServiceRuntimeHealthMonitor boot grace anchored at start/first emit,
      not object construction."
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr diff ${PR_NUMBER} --repo ${REPO} | grep -q 'test_construction_does_not_start_boot_grace_timer'"
  - id: dod-002
    description: "Focused runtime health monitor and crash-loop tests pass locally on PR #1393 head."
    source: manual
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/services/test_service_runtime_health_monitor.py tests/integration/test_runtime_crash_loop_fix.py
          -q"
  - id: dod-deploy
    description: "deploy gate evidence: PR #1393 records the post-merge runtime validation command; pre-merge
      live RestartCount proof is intentionally not claimed."
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json body --jq '.body | if contains(\"Post-merge
          runtime validation\") and contains(\"deploy: docker exec omninode-runtime\") then \"deploy validation
          recorded\" else error(\"missing deploy validation\") end'"
