---
schema_version: "1.0.0"
ticket_id: OMN-9607
title: "Task 13: Cross-repo docs validation gate and freshness sweep wiring"
summary: "Wire cross-repo docs validation, freshness/drift checks, event-surface coverage, canonical-owner
  link checks, and onboarding smoke validation across refreshed repos."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Unit tests for exclusion config patterns pass (TestMarkdownLinkConfigExclusions)"
    command: "cd omnibase_core && uv run pytest tests/unit/scripts/test_validate_markdown_links.py -v"
  - kind: ci
    description: "omnibase_core PR #979 CI Documentation Validation check passes"
    command: "gh pr checks 979 --repo OmniNode-ai/omnibase_core"
  - kind: manual
    description: "Cross-repo validate-links scan passes with 0 broken links after config fix"
    command: "cd omnibase_core && uv run onex-validate-links --verbose --cross-repo-root ${OMNI_HOME:-$(git
      rev-parse --show-toplevel)/..}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Unit tests for omni_worktrees/** and plugins/.venv/** exclusion patterns pass (74/74)"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9607/dod-001/file_exists.yaml"
  - id: dod-002
    description: "omnibase_core PR #979 Documentation Validation CI job passes"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9607/dod-002/file_exists.yaml"
  - id: dod-003
    description: "Per-repo freshness audit completed for all 10 in-scope repos with findings documented"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9607/dod-003/file_exists.yaml"
