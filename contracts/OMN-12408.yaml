---
schema_version: "1.0.0"
ticket_id: "OMN-12408"
title: "Runtime auto-wiring silently skips entire contract on one broken handler entry — should fail loud"
summary: "Hard-fail on missing handler class in handler_routing; add CI validation gate for class existence"

is_seam_ticket: false
interface_change: false
interfaces_touched: []

evidence_requirements:
  - kind: "tests"
    description: "Unit + integration tests prove broken handler class causes hard failure regardless of
      ONEX_WIRING_STRICT_MODE"
    command: "uv run pytest tests/unit/runtime/contract_loaders/test_handler_class_existence_validation.py
      tests/integration/test_auto_wiring_strict_invariant.py -q"

emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""

dod_evidence:
  - id: "dod-001"
    description: "validate_handler_class_exists raises ProtocolConfigurationError on missing module or
      class"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12408/dod-001/command.yaml"
  - id: "dod-002"
    description: "load_handler_class_info_from_contract raises on missing handler name, module, or non-existent
      class"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12408/dod-002/command.yaml"
  - id: "dod-003"
    description: "wire_from_manifest hard-fails on missing handler class even without ONEX_WIRING_STRICT_MODE=1"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12408/dod-003/command.yaml"
  - id: "dod-004"
    description: "CI gate: all deployed handler_routing entries in repo contracts resolve to real classes"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12408/dod-004/command.yaml"
  - id: "dod-occ-pr-self"
    description: "OCC self-binding receipt for the central evidence PR carrying the OMN-12408 contract
      and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12408/dod-occ-pr-self/command.yaml"
