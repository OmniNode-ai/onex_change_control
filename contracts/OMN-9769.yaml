---
schema_version: "1.0.0"
ticket_id: "OMN-9769"
title: "Add batch validator summary and validator mode CLI wiring"
summary: >
  Adds the omnibase_core batch contract validator and ModelBatchValidationSummary so contract corpora
  can be swept with strict or migration_audit modes. The paired omnibase_infra CLI PR wires omni-infra
  validate contracts --mode to the shared batch validator after the core API lands.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-batch-validator-tests"
    description: "Focused omnibase_core batch validator tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9769/dod-core-batch-validator-tests/command.yaml\""
  - id: "dod-core-batch-validator-lint"
    description: "Focused ruff check and format check pass for the core batch validator files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9769/dod-core-batch-validator-lint/command.yaml\""
  - id: "dod-pr-1049-head"
    description: "Bind OMN-9769 evidence to omnibase_core PR #1049 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9769/dod-pr-1049-head/command.yaml\""
  - id: "dod-occ-pr-819"
    description: "Bind this central OCC evidence update to onex_change_control PR #819."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9769/dod-occ-pr-819/command.yaml\""
