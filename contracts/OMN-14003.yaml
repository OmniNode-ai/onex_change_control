---
schema_version: "1.0.0"
ticket_id: "OMN-14003"
title: "fix(omnimarket): re-resolve omnibase-spi lock pin for ProtocolDispatchEngine"
summary: >-
  omnimarket PR #1627 re-locks omnibase-spi within the existing pyproject range from
  0.23.0 to 0.23.1 so omnimarket's own virtualenv can import ProtocolDispatchEngine
  from omnibase_spi.protocols.runtime. This removes the infra-venv workaround found
  by the ratchet-canary and restores local-first onex delegate execution from the
  omnimarket environment.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1627"
    description: >-
      Consuming PR binding: omnimarket PR #1627 at head
      4e62f9a0c5e74b0a493e9c456df021c4754e81f2 changes only uv.lock to
      re-resolve omnibase-spi to 0.23.1 within the existing dependency range.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14003/dod-omnimarket-pr-1627/command.yaml"
  - id: "dod-local-verification"
    description: >-
      Local verification binding: ProtocolDispatchEngine imports from omnimarket's
      own virtualenv, onex delegate completes on the local tier, and full local
      gates were run with only pre-existing infra-dependent Postgres/Kafka tests
      failing as documented in PR #1627.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14003/dod-local-verification/command.yaml"
  - id: "dod-occ-pr-3639"
    description: >-
      OCC companion binding: onex_change_control PR #3639 carries the central
      OMN-14003 contract and PASS receipts consumed by omnimarket PR #1627.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14003/dod-occ-pr-3639/command.yaml"
