---
schema_version: "1.0.0"
ticket_id: OMN-9856
title: "Task 5: Adjacency expansion in scripts/ci/detect_test_paths.py — expand changed src/ modules through
  reverse_deps from test_selection_adjacency.yaml; add exact-set expansion tests for models/ (8 dirs)
  and protocols/ (4 dirs)"
summary: "Task 5: Adjacency expansion in scripts/ci/detect_test_paths.py — expand changed src/ modules
  through reverse_deps from test_selection_adjacency.yaml; add exact-set expansion tests for models/ (8
  dirs) and protocols/ (4 dirs)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core bundle PR #940 merged the change-aware CI stack that superseded PR #930"
    command: "gh pr view 940 --repo OmniNode-ai/omnibase_core --json state,mergeCommit --jq 'select(.state==\"MERGED\"
      and .mergeCommit.oid==\"a80fe01dc32f2abc21e5ed4bcee2871f64db9cca\")'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "scripts/ci/detect_test_paths.py _resolve expands direct src/ modules through adjacency.reverse_deps
      and emits sorted unit-test dirs"
    source: generated
    checks:
      - check_type: command
        check_value: "cd ${OMNI_HOME:-/Users/jonah/Code/omni_home}/omnibase_core && uv run python -c \"from
          pathlib import Path; import sys; sys.path.insert(0, 'scripts/ci'); from detect_test_paths import
          resolve_test_paths; paths = resolve_test_paths(['src/omnibase_core/models/foo.py'], adjacency_path=Path('scripts/ci/test_selection_adjacency.yaml'));
          assert paths == sorted(f'tests/unit/{m}/' for m in ['models','nodes','contracts','runtime','validation','services','factories','container']),
          paths; print('models expansion OK:', paths)\""
  - id: dod-002
    description: "models/ change expands to exactly 8 unit-test dirs and protocols/ change expands to
      exactly 4 unit-test dirs (cli/ absent); all 4 prior Task 4 tests still pass — total 6 tests pass"
    source: generated
    checks:
      - check_type: test_passes
        check_value: "cd ${OMNI_HOME:-/Users/jonah/Code/omni_home}/omnibase_core && uv run pytest tests/unit/scripts/ci/test_detect_test_paths.py
          -v"
  - id: dod-003
    description: "Ticket contract file exists and parses as valid ModelTicketContract YAML in onex_change_control"
    source: generated
    checks:
      - check_type: command
        check_value: "cd ${ONEX_CC_REPO_PATH:-${OMNI_HOME:-/Users/jonah/Code/omni_home}/onex_change_control}
          && uv run validate-yaml contracts/OMN-9856.yaml"
