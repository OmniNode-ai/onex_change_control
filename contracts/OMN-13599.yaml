---
schema_version: "1.0.0"
ticket_id: "OMN-13599"
title: "fix(OMN-13599): local-first code_generation routing + local-coder code-only scope"
summary: >-
  omnimarket PR #1604 makes the AI-PC local-coder the preferred first hop for code_generation (tier_order
  [local, cheap_cloud, claude]; Gemini removed from the code_generation routing path; local-coder gains
  a fast_path_threshold) and scopes local-coder use_for to code-only (code_generation, code_review, refactor)
  so it no longer bleeds into test/research — restoring test/research -> local-reasoner (Qwen3.6-27B-MTP)
  per test_contract_routing_policy. This is a routing-config + test change (no runtime contract.yaml touched;
  deploy-gate passes).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-verification"
    description: >-
      PR #1604 reconciles the 8 order-encoding tests + test_contract_routing_policy research/test -> reasoner
      + a code-only recurrence guard + the judge-verdict deterministic bifrost, and re-records the delegation
      golden-chain fixture via the sanctioned record_fixture API for the test->reasoner route. All delegation/routing/golden-chain
      suites green (938 passed, 4 skipped).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13599/dod-product-verification/command.yaml"
  - id: "dod-pr-1604-binding"
    description: >-
      OmniNode-ai/omnimarket PR #1604 carries the OMN-13599 change at feature commit ae58cab73232e095f47113b8743a4f3873e070c3.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13599/dod-pr-1604-binding/command.yaml"
  - id: "dod-occ-self"
    description: >-
      This onex_change_control companion PR carries the central OMN-13599 contract and PASS receipts for
      the downstream omnimarket PR #1604 merge sweep.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13599/dod-occ-self/command.yaml"
