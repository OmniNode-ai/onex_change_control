---
schema_version: "1.0.0"
ticket_id: "OMN-15196"
title: "Autobind OCC evidence for OMN-15196"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1917."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1917 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1917"
    description: "PR #1917 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1917-ci"
    description: "PR #1917 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5122"
    description: "OCC companion PR #5122 — self-bind for OMN-15196 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-agent-delegation-response-contract"
    description: >
      Deploy-scope DoD for PR #1917's runtime-path changes. Hermetic content probe reads omnimarket product
      commit 7828829818ad137151803af3bc05c5e37a108309 and asserts the agent_delegation task class declares
      the DispatchReport response_contract_ref that the runtime delegation and quality-gate paths consume.
      This proves the source artifact that must be deployed, without reading back OCC receipts or mutating
      any live lane.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/configs/task_class_contracts.v1.yaml?ref=7828829818ad137151803af3bc05c5e37a108309'
          --jq .content | base64 -d | grep -q 'response_contract_ref: "omnibase_core.models.dispatch.report.DispatchReport"'
  - id: "occ-self-bind-pr-5126"
    description: >
      OCC repair PR #5126 self-bind for the OMN-15196 deploy-evidence append; proves this PR carries the
      deploy-scope contract and receipt repair commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5126 --repo OmniNode-ai/onex_change_control --json number,state"
