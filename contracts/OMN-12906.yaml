---
schema_version: "1.0.0"
ticket_id: "OMN-12906"
title: "Move projection runner Kafka/DB bindings and dashboard feed diagnostics to contract overlays"
summary: >-
  Add a typed projection runtime binding so Kafka-backed projection consumers resolve Kafka bootstrap,
  consumer group, client id, and projection database DSN from explicit overlay/runtime config instead
  of ambient Kafka/Postgres env authority.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1147"
    description: >-
      omnimarket PR #1147 carries the OMN-12906 implementation for projection runtime overlay binding.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12906/dod-omnimarket-pr-1147/command.yaml"
  - id: "dod-focused-tests"
    description: >-
      Focused projection runner and handler tests, lint, format, and strict mypy checks pass for the OMN-12906
      implementation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12906/dod-focused-tests/command.yaml"
  - id: "dod-no-env-overlay-proof"
    description: >-
      With Kafka and projection database env variables unset, the runner resolves broker, group, client,
      and database binding from `OMNIMARKET_PROJECTION_RUNTIME_BINDING_OVERLAY`.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12906/dod-no-env-overlay-proof/command.yaml"
          && grep -q 'broker=redpanda.noenv:9092' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12906/dod-no-env-overlay-proof/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      OMN-12906 changes projection runner runtime configuration. No live `.201` deployment or restart
      was performed during PR validation; hosted runtime validation remains a post-merge deploy step.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12906/dod-deploy-assessment/command.yaml"
          && grep -q 'No live deployment or restart was performed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12906/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2426"
    description: >-
      OCC PR #2426 carries the central OMN-12906 contract and DoD receipts used by downstream receipt
      gates.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12906/dod-occ-pr-2426/command.yaml"
