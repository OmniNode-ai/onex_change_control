---
schema_version: "1.0.0"
ticket_id: "OMN-10564"
title: "Task 1: Audit raw env-var usage in omnimarket production source"
summary: "Extend and run the raw env-var audit script to enumerate all os.environ.get/os.environ[]/os.getenv
  calls plus hardcoded /Users/, /Volumes/, 192.168.x.x, and model-ID strings across src/omnimarket. Emit
  a dated CSV with severity classification as the canonical scope input for downstream public-shippable
  cleanup tasks."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Unit tests pass after audit script changes."
    command: "uv run pytest tests/ -m unit -x -q"
  - kind: "ci"
    description: "CI pipeline green on omnimarket PR #540."
    command: "gh pr checks 540 --repo OmniNode-ai/omnimarket --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Audit script runs and emits the 2026-05-07 dated CSV with env-var, hardcoded-path, lan-ip,
      and model-id pattern coverage."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10564/dod-001/command.yaml\"; else uv run python
          scripts/audit/raw_env_usage_audit.py && test -f docs/audits/2026-05-07-raw-env-usage.csv; fi"
  - id: "dod-002"
    description: "Unit test suite passes on the audit branch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10564/dod-002/command.yaml\"; else uv run pytest
          tests/ -m unit -x -q; fi"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10564 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10564/dod-occ-pr/command.yaml\""
