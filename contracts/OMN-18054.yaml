---
schema_version: "1.0.0"
ticket_id: "OMN-18054"
title: "Autobind OCC evidence for OMN-18054"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2567.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2567 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #2567 on OmniNode-ai/omnimarket changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (src/omnimarket/projection/credential_publisher.py) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2567"
    description: "PR #2567 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2567 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2567/files --paginate --jq '.[].filename' | grep -cFx 'src/omnimarket/projection/credential_publisher.py'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-2567"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-9668"
    description: "Binds OMN-18054 to OCC companion PR #9668 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9668 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "ac1-mint-probe-2026-09-09"
    description: >
      AC1: Probe on dev cluster (2026-09-09T17:19Z). Pod omninode-runtime-6f476644f-bnsch, SSM command
      16acd5be-3148-4c48-adfe-6173a679d7ed. Credential omn18054-probe-byok minted via POST /v1/tenants/me/inference-credentials
      (HTTP 201). Ref shape cred_{tenant_id}_openrouter_{uuid4hex} confirmed. In-pod resolution: sha256-12=b0736768bf94,
      len=86, match=true. Value never printed. ROTATION-CONSENT: docs/tracking/ROLLING_WORK_LEDGER.md:5269.
      Live cluster run, scoped to local_done_gate.
    source: "manual"
    binds_ac:
      - "AC1"
    execution_scope: "local_done_gate"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2567 --jq '.state' | grep -Fx 'MERGED'
  - id: "ac2-revoke-probe-2026-09-15"
    description: >
      AC2: Probe on dev cluster (2026-09-15T18:49Z). Pod omninode-runtime-worker-7c7b76b554-nkxg9, SSM
      command 9e0f6ce2. Revoke DELETE HTTP 200 (SSM c9f724e7). Projection confirmed revoked_at=2026-09-15T18:49:41.103629Z
      (SSM 0c2b2945). In-pod resolver raised SecretResolutionError on attempt=1, elapsed=0.0s. Fail-closed
      as required. ROTATION-CONSENT: docs/tracking/ROLLING_WORK_LEDGER.md:5269. Requires both PR #2504
      (delete-first sequencing) and PR #2567 (allow_delete=True). Live cluster run, scoped to local_done_gate.
    source: "manual"
    binds_ac:
      - "AC2"
    execution_scope: "local_done_gate"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2504 --jq '.state' | grep -Fx 'MERGED'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2567 --jq '.state' | grep -Fx 'MERGED'
