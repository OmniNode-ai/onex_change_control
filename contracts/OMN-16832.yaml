---
schema_version: "1.0.0"
ticket_id: "OMN-16832"
title: "Autobind OCC evidence for OMN-16832"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3004.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3004 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3004 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: "uv run pytest tests/ci/test_evidence_autoclose_sweep_credential.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3004"
    description: "PR #3004 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/ci/test_evidence_autoclose_sweep_credential.py?ref=9df2fd3b2bd12488f16aa531b176fe2be87f8508 --jq '.content' | base64 -d | grep -c 'def _load_workflow'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3004/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/evidence-autoclose-sweep.yml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3004 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/ci/test_evidence_autoclose_sweep_credential.py -q"
        cwd: "${OMNI_HOME}/omnibase_infra"
