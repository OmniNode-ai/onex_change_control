---
schema_version: "1.0.0"
ticket_id: "OMN-12383"
title: "Focused unit coverage for omnimarket build loop and PR lifecycle hotspots"
summary: >
  Bind central evidence for omnimarket PR #991. The PR adds coverage-only tests for build loop orchestrator
  helpers and PR lifecycle orchestrator hotspots. It does not change production code.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-991"
    description: "omnimarket PR #991 carries 45 focused build-loop and PR-lifecycle unit tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12383/dod-omnimarket-pr-991/command.yaml\"
          && grep -q '^pr_number: 991$' \"drift/dod_receipts/OMN-12383/dod-omnimarket-pr-991/command.yaml\""
  - id: "dod-focused-tests"
    description: "The focused OMN-12383 build-loop and PR-lifecycle unit tests pass locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12383/dod-focused-tests/command.yaml\"
          && grep -q '45 new unit tests' \"drift/dod_receipts/OMN-12383/dod-focused-tests/command.yaml\""
