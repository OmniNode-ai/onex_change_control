---
schema_version: "1.0.0"
ticket_id: "OMN-15102"
title: "OCC evidence for omnimarket#1892 — mutate write+readback test for node_occ_observation_effect"
summary: "Central OCC evidence for omnimarket#1892, which adds a hermetic unit test proving HandlerOccObservationEffect.handle()
  actually writes a NEW record and reads it back in mode=\"mutate\" (real local git repo, no network).
  Test-only change; no edits to handler_occ_observation_effect.py or any workflow YAML; no runtime kill-switch
  (OMNI_OCC_OBSERVATION_STORE_MODE) change or live mutate run is claimed by this contract."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnimarket PR #1892 is open at the recorded OMN-15102 head."
    command: "test -f drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-head/command.yaml && grep -Fq
      'e9047f8b83ddfb92dfc6741b6637793fdfe8a08a' drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-head/command.yaml"
  - kind: "manual"
    description: "omnimarket PR #1892 scope is limited to the one new test file."
    command: "test -f drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-scope/command.yaml && grep -Fq
      'tests/unit/nodes/node_occ_observation_effect/test_new_record_mutate_write_readback.py' drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-scope/command.yaml"
  - kind: "manual"
    description: "omnimarket PR #1892 body records the RED-then-GREEN mutation-test evidence and the local+CI
      focused/full test pass counts."
    command: "test -f drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-verification/command.yaml &&
      grep -Fq '8 passed' drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-verification/command.yaml
      && grep -Fq '9262 passed' drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-verification/command.yaml
      && grep -Fq 'HEAD did not move' drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-verification/command.yaml"
  - kind: "manual"
    description: "OCC companion PR #4791 carries central OMN-15102 evidence for omnimarket#1892."
    command: "test \"$(gh api repos/OmniNode-ai/onex_change_control/pulls/4791 --jq .head.ref)\" = jonah/occ-omn-15102-market1892"
  - kind: "ci"
    description: "PR #1892 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1892-head"
    description: "Binds omnimarket PR #1892 at head e9047f8b83ddfb92dfc6741b6637793fdfe8a08a."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-head/command.yaml &&
          grep -Fq 'e9047f8b83ddfb92dfc6741b6637793fdfe8a08a' drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-head/command.yaml"
  - id: "dod-omnimarket-pr-1892-scope"
    description: "PR #1892 scope is exactly one new test file, no product/workflow changes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-scope/command.yaml &&
          grep -Fq 'tests/unit/nodes/node_occ_observation_effect/test_new_record_mutate_write_readback.py'
          drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-scope/command.yaml"
  - id: "dod-omnimarket-pr-1892-verification"
    description: "PR #1892 body records RED-then-GREEN mutation-test evidence, the focused local pass,
      and the CI full fast-slice pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-verification/command.yaml
          && grep -Fq '8 passed' drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-verification/command.yaml
          && grep -Fq '9262 passed' drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-verification/command.yaml
          && grep -Fq 'HEAD did not move' drift/dod_receipts/OMN-15102/dod-omnimarket-pr-1892-verification/command.yaml"
  - id: "occ-self-bind-omn15102"
    description: "OCC companion PR #4791 (this PR) is open and carries the OMN-15102 contract and receipts
      binding omnimarket#1892."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "test \"$(gh api repos/OmniNode-ai/onex_change_control/pulls/4791 --jq .head.ref)\"
          = jonah/occ-omn-15102-market1892"
  - id: "dod-OmniNode-ai-omnimarket-pr-1892"
    description: "PR #1892 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1892-ci"
    description: "PR #1892 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4789"
    description: "OCC companion PR #4789 — self-bind for OMN-15102 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
