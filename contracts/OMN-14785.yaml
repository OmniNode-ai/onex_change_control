---
schema_version: "1.0.0"
ticket_id: "OMN-14785"
title: "Autobind OCC evidence for OMN-14785"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1818."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1818 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1818"
    description: "PR #1818 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1818-ci"
    description: "PR #1818 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4408"
    description: "OCC companion PR #4408 — self-bind for OMN-14785 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-assessment"
    description: "Deploy gate assessment for omnimarket#1818 runtime-path changes; no live deploy or restart
      performed in merge sweep."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "DEPLOY_ASSESSMENT=deploy-required gh pr view 1818 --repo OmniNode-ai/omnimarket
          --json number,state,headRefOid --jq '.number == 1818 and .headRefOid == \"7115da2d7ac689e3b5053bb7784ec4fe2090b87a\"'"
  - id: "occ-self-bind-pr-4409"
    description: "OCC companion PR #4409 — self-bind for OMN-14785 deploy evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
