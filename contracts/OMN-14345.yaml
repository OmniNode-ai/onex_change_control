---
schema_version: 1.0.0
ticket_id: OMN-14345
title: "fix(OMN-14345): gateway stamps verified tenant_id into inbound payload"
summary: >-
  omnibase_infra PR #2252 fixes the inbound gateway hop so the config-validated tenant identity from ModelGatewayEnvelope
  is stamped into the republished payload consumed by local runtime subscribers. The verified gateway
  identity overwrites any payload-supplied tenant value.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - envelopes
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: >-
      Independent verification of omnibase_infra PR #2252 at head 6ef4b46b98ca1a023794553d4a7b6207197702aa.
      Local tests drive the real ServiceGatewayForwarder path and confirm that a forged payload tenant_id
      is overwritten with the verified gateway tenant_id, tenant_slug is stamped when absent, cross-tenant
      outer-envelope mismatch is rejected, and existing outbound/inbound gateway behavior still passes.
      Focused unit test file passed 8/8; ruff format/check and mypy on the changed handler were clean.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14345/dod-001/command.yaml && grep -q '^pr_number:
          2252$' drift/dod_receipts/OMN-14345/dod-001/command.yaml && grep -q '6ef4b46b98ca1a023794553d4a7b6207197702aa'
          drift/dod_receipts/OMN-14345/dod-001/command.yaml
  - id: dod-deploy-assessment
    description: >-
      Deploy-surface assessment for PR #2252. The change is scoped to an existing node_bus_forwarder_effect
      handler plus unit tests. It adds no new node, no topic, no migration, no dependency, no service-catalog
      deployment footprint, and no live .201 mutation. The PR body explicitly notes this is necessary
      substrate and does not itself add an authenticated deployed ingress boundary.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14345/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-14345/dod-deploy-assessment/command.yaml
  - id: dod-occ-self
    description: >-
      OCC companion self-binding for omnibase_infra #2252. This onex_change_control PR carries the central
      OMN-14345 contract and PASS receipts consumed by #2252 through its Evidence-Source line.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14345/dod-occ-self/command.yaml
