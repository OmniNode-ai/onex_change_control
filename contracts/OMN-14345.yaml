---
schema_version: 1.0.0
ticket_id: OMN-14345
title: "fix(OMN-14345): gateway stamps verified tenant_id into inbound payload"
summary: >-
  omnibase_infra PR #2252 fixes the inbound gateway hop so the config-validated tenant identity from ModelGatewayEnvelope
  is stamped into the republished payload consumed by local runtime subscribers. The verified gateway
  identity overwrites any payload-supplied tenant value.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - envelopes
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: >-
      Independent verification of omnibase_infra PR #2252 at head 6ef4b46b98ca1a023794553d4a7b6207197702aa.
      Local tests drive the real ServiceGatewayForwarder path and confirm that a forged payload tenant_id
      is overwritten with the verified gateway tenant_id, tenant_slug is stamped when absent, cross-tenant
      outer-envelope mismatch is rejected, and existing outbound/inbound gateway behavior still passes.
      Focused unit test file passed 8/8; ruff format/check and mypy on the changed handler were clean.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14345/dod-001/command.yaml && grep -q '^pr_number:
          2252$' drift/dod_receipts/OMN-14345/dod-001/command.yaml && grep -q '6ef4b46b98ca1a023794553d4a7b6207197702aa'
          drift/dod_receipts/OMN-14345/dod-001/command.yaml
  - id: dod-deploy-assessment
    description: >-
      Deploy-surface assessment for PR #2252. The change is scoped to an existing node_bus_forwarder_effect
      handler plus unit tests. It adds no new node, no topic, no migration, no dependency, no service-catalog
      deployment footprint, and no live .201 mutation. The PR body explicitly notes this is necessary
      substrate and does not itself add an authenticated deployed ingress boundary.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14345/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-14345/dod-deploy-assessment/command.yaml
  - id: dod-occ-self
    description: >-
      OCC companion self-binding for omnibase_infra #2252. This onex_change_control PR carries the central
      OMN-14345 contract and PASS receipts consumed by #2252 through its Evidence-Source line.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14345/dod-occ-self/command.yaml
  - id: dod-2252-head-e703169b
    description: >-
      Follow-up verification of omnibase_infra PR #2252 at head e703169b9d85743c1da662b5a23df8fe79a1c1d4
      after adding the required node_bus_forwarder_effect contract update for the Wave C contract-sync
      gate. Focused gateway forwarder unit tests passed 8/8, Ruff format/check and mypy passed on the
      changed handler/test surfaces, and validate-pr-contract-sync.sh 2252 passed against the remote PR
      diff.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14345/dod-2252-head-e703169b/command.yaml &&
          grep -q '^pr_number: 2252$' drift/dod_receipts/OMN-14345/dod-2252-head-e703169b/command.yaml
          && grep -q 'e703169b9d85743c1da662b5a23df8fe79a1c1d4' drift/dod_receipts/OMN-14345/dod-2252-head-e703169b/command.yaml
  - id: dod-occ-pr-2252-head-e703169b
    description: >-
      OCC follow-up self-binding: this onex_change_control PR carries the append-only OMN-14345 evidence
      refresh for omnibase_infra#2252 head e703169b9d85743c1da662b5a23df8fe79a1c1d4 after the contract-sync
      repair. The receipt binds the allocated OCC PR number and repair evidence commit.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14345/dod-occ-pr-2252-head-e703169b/command.yaml
  - id: dod-occ-pr-3900
    description: >-
      OCC supersession self-binding: onex_change_control PR #3900 carries append-only supersession receipts
      that re-bind the original OMN-14345 base receipts to per-entry contract hashes after follow-up evidence
      changed the whole-file contract hash. The receipt binds PR #3900 and its OCC branch head.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14345/dod-occ-pr-3900/command.yaml
