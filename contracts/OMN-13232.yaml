---
schema_version: "1.0.0"
ticket_id: OMN-13232
title: "feat(delegation): resolve endpoint URLs from the store overlay, retire local-file bifrost_overrides.yaml"
summary: >
  Replace ~/.omninode/delegation/bifrost_overrides.yaml file resolution with a store-backed overlay in
  delegation_backend_resolution.load_bifrost_backends. The store key delegation.bifrost.overlay holds
  the overlay YAML blob; the file overlay is demoted to a DEV-ONLY fallback with a deprecation warning.
  Complete-URL fail-closed invariant (OMN-12815) is preserved. Two lanes with different store overlays
  resolve different complete endpoints from the same committed contract. (ADR D2, plan A3.)
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Store-overlay unit tests pass (10 new tests in test_delegation_store_overlay_omn13232.py)
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13232/dod-store-overlay-tests/command.yaml"
  - kind: ci
    description: omnimarket PR is bound to the central OCC evidence
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13232/dod-omnimarket-pr/command.yaml"
dod_evidence:
  - id: dod-store-overlay-tests
    description: >
      10 new unit tests in tests/unit/routing/test_delegation_store_overlay_omn13232.py pass, proving:
      (1) store overlay merges over base config, (2) bare-base URL carried verbatim (fail-closed at effect
      boundary per OMN-12815), (3) two lanes with different store overlays resolve different complete
      endpoints from the same committed contract, (4) file fallback with deprecation log when store key
      is absent.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13232/dod-store-overlay-tests/command.yaml"
  - id: dod-mypy-lint
    description: >
      src/omnimarket/routing/delegation_backend_resolution.py passes ruff format, ruff check, and mypy
      --strict with no new violations.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13232/dod-mypy-lint/command.yaml"
  - id: dod-bifrost-overlay-store-key-exported
    description: >
      BIFROST_OVERLAY_STORE_KEY is exported from omnimarket.routing.delegation_backend_resolution and
      equals "delegation.bifrost.overlay".
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13232/dod-bifrost-overlay-store-key-exported/command.yaml"
  - id: dod-file-overlay-deprecated
    description: >
      When store is configured but key is absent, a WARNING-level deprecation log is emitted (proven by
      test_store_key_absent_falls_back_to_file_overlay in the new test file).
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13232/dod-file-overlay-deprecated/command.yaml"
  - id: dod-omnimarket-pr
    description: omnimarket PR implementing OMN-13232 is open with the expected Evidence-Source OCC SHA
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13232/dod-omnimarket-pr/command.yaml"
  - id: dod-occ-self
    description: >
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13232 contract and receipts
      consumed by omnimarket PR #1484.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13232/dod-occ-self/command.yaml"
