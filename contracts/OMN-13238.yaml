---
schema_version: "1.0.0"
ticket_id: "OMN-13238"
title: "Contract-declared per-topic provisioning config"
summary: >-
  OMN-13238 moves infra-owned topic provisioning settings out of the transitional ALL_PROVISIONED_TOPIC_SPECS
  registry and into owning node contracts. The PR extends the existing contract topic extractor to carry
  partitions, replication_factor, and kafka_config from structured topic declarations, maps those values
  through service_topic_manager, and verifies parity for the migrated infra-owned topics. The legacy registry
  remains transitional for cross-repo and runtime-kernel topics that need follow-up migrations.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2032"
    description: >-
      Per-code-PR binding receipt for omnibase_infra PR #2032 (jonahgabriel/omn-13238-contract-declared-per-topic-config,
      base=dev). The PR adds contract-declared topic_config extraction, maps it onto topic specs, migrates
      the infra-owned configured topics, and records local verification in the PR body. This receipt binds
      OMN-13238 to the infra PR by pr_number and commit_sha so downstream Receipt Gate can resolve central
      OCC evidence for the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13238/dod-infra-pr-2032/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate evidence for contract/topic provisioning paths and vendored node migration paths touched
      by OMN-13238. No live deploy or runtime restart was performed pre-merge. The required operational
      step is a normal post-merge dev deploy/redeploy followed by topic-provisioning parity validation
      against the deployed contract set.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13238/dod-deploy-assessment/command.yaml &&
          grep -q 'post-merge dev deploy' drift/dod_receipts/OMN-13238/dod-deploy-assessment/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13238 contract and omnibase_infra
      PR #2032 binding.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13238/dod-occ-pr-self/command.yaml
