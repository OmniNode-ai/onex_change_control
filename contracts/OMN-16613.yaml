---
schema_version: "1.0.0"
ticket_id: "OMN-16613"
title: "Autobind OCC evidence for OMN-16613"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2275.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2275 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2275 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: |-
      uv run pytest tests/test_harness_parity_doc.py tests/test_node_service_registry_migration_ownership.py tests/unit/classifiers/test_pipeline_touch.py tests/unit/nodes/node_generation_consumer/test_sea_generation_routing_parity.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2275"
    description: "PR #2275 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2275 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2275/files --paginate --jq '.[].filename' | grep -cFx '.github/CONTRIBUTING.md'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2275 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-2275"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/test_harness_parity_doc.py tests/test_node_service_registry_migration_ownership.py tests/unit/classifiers/test_pipeline_touch.py tests/unit/nodes/node_generation_consumer/test_sea_generation_routing_parity.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-8086"
    description: "Binds OMN-16613 to OCC companion PR #8086 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8086 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-kb-doc-gate-strict-green"
    description: |-
      Hand-authored addition (OCC#8091). The kb-doc-gate check runs on PR #2275 in strict mode — a
      whole-tree scan, not a diff scan — and passes: zero tracked markdown outside the allowed set
      remains on the branch. The same scan against origin/dev reports 67 violations, so the check
      discriminates in both directions.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr checks 2275 --repo OmniNode-ai/omnimarket --json name,state --jq '.[] | select(.name == "kb-doc-gate / kb-doc-gate") | .state'
  - id: "dod-OmniNode-ai-omnimarket-pr-2275-behavior"
    description: |-
      Hand-authored addition (OCC#8091). Diff-derived behavior proof on a test target PR #2275 KEEPS.
      The autobind item `dod-occ-diff-derived-behavior-proof` above names four targets, and PR #2275
      DELETES the first of them (tests/test_harness_parity_doc.py — it asserted the presence of a doc
      that moved to the knowledge base), so that command is unrunnable at and after the merge commit
      it attests. This item pins a target the PR modifies and keeps instead.
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_occ_observation_effect/test_self_bind_eligibility_omn_15323.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-8091"
    description: "Binds OMN-16613 to OCC companion PR #8091 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 8091 --repo OmniNode-ai/onex_change_control --json number,state
