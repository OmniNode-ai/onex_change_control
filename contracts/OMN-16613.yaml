---
schema_version: "1.0.0"
ticket_id: "OMN-16613"
title: "OCC evidence companion for the omnimarket documentation scrub"
summary: >
  Hand-authored OCC companion for OmniNode-ai/omnimarket PR #2275 (OMN-16613, child of epic OMN-16602).
  OCC autobind declined to mint a companion for that PR ("no changed-file candidate could be proven RED
  against the merge base", OMN-15247), so the evidence below is authored from the PR's own diff and from
  the kb-doc-gate strict scan, both independently falsifiable.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2275 CI checks green on OmniNode-ai/omnimarket"
    command: "gh pr checks 2275 --repo OmniNode-ai/omnimarket"
  - kind: "tests"
    description: |-
      Behavior proof derived from PR #2275's own diff: the PR modifies
      tests/unit/nodes/node_occ_observation_effect/test_self_bind_eligibility_omn_15323.py and its
      conftest, repointing their doc citations at the knowledge base. That target must still pass.
    command: "uv run pytest tests/unit/nodes/node_occ_observation_effect/test_self_bind_eligibility_omn_15323.py
      -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2275"
    description: "PR #2275 on OmniNode-ai/omnimarket — omnimarket documentation scrub, Evidence-Source
      bind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2275 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2275/files --paginate --jq '.[].filename' | grep -cFx 'docs/README.md'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2275/files --paginate --jq '.[].filename' | grep -cFx '.kb-doc-gate.yaml'
  - id: "dod-kb-doc-gate-strict-green"
    description: |-
      The kb-doc-gate check runs in strict mode on this PR (whole-tree scan, not a diff scan) and
      passes: zero tracked markdown outside the allowed set remains on the branch. The same scan
      against origin/dev reports 67 violations, so the check is falsifiable in both directions.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr checks 2275 --repo OmniNode-ai/omnimarket --json name,state --jq '.[] | select(.name == "kb-doc-gate / kb-doc-gate") | .state'
  - id: "dod-OmniNode-ai-omnimarket-pr-2275-behavior"
    description: "PR #2275 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-2275"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/nodes/node_occ_observation_effect/test_self_bind_eligibility_omn_15323.py
          -q"
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-8091"
    description: "Binds OMN-16613 to OCC companion PR #8091 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 8091 --repo OmniNode-ai/onex_change_control --json number,state
