---
schema_version: "1.0.0"
ticket_id: "OMN-13937"
title: "feat(OMN-13937): add paid-cloud ceiling rungs (GLM + OpenRouter-free) to graded ladder benchmark"
summary: >-
  omnimarket PR #1610 applies the operator correction that paid-cloud is NOT deferred: the delegation
  graded ladder benchmark (OMN-13935) now spans the FULL ladder up to the paid-cloud ceiling. Adds rung_cloud_glm
  (glm-5.2 via z.ai direct, backend cloud-glm, paid LLM_GLM_API_KEY) and rung_openrouter_free (OpenRouter
  free tier, OPEN_ROUTER_API_KEY, free-only), sourced from routing_tiers.yaml + bifrost_delegation.yaml,
  and strips the three "Paid-cloud ceiling is deferred" strings. ModelLadderRung gains endpoint_url /
  api_key_env / extra_headers for public cloud rungs (public URLs committed; key is an env var NAME only).
  The recorder gains Bearer auth + per-rung headers, 429 backoff, --retries, --task filter, and None-content
  coercion. recorded_rung_outputs.json carries GENUINE live outputs for both cloud rungs (glm-5.2 z.ai
  + OpenRouter free). Recorded 5-rung result: 4090-27B 0.719 < openrouter-free 0.844 < 5090-35B 1.0 =
  284B 1.0 = cloud-GLM 1.0; separation floor->cloud-ceiling 0.281 >= 0.15, monotonic, PASS. Deploy-neutral:
  no node contract, no topic, no handler wiring, no live deploy; the recorder is a non-CI live-capture
  script.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1610"
    description: >-
      Consuming-PR binding: omnimarket PR #1610 adds the paid-cloud ceiling rungs (GLM + OpenRouter-free),
      the ModelLadderRung cloud fields, the recorder auth/backoff support, and the genuine recorded cloud
      outputs, with the floor->cloud-ceiling separation asserted. Real probe: gh pr view 1610 --repo OmniNode-ai/omnimarket
      --json number,state,headRefOid,headRefName,title.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13937/dod-omnimarket-pr-1610/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment: adds cloud rungs to a benchmark config + two Pydantic fields to a non-node
      support module + recorder-script auth/backoff support + recorded evidence fixtures + unit tests.
      No node contract.yaml, no topic registration, no handler dispatch wiring; no live deploy, no docker
      exec, no rpk topic produce. The live cloud calls are made only by the opt-in recorder script (never
      in CI). Deploy-neutral.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13937/dod-deploy-assessment/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-13937/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying this OMN-13937 contract and the consuming-PR
      evidence for omnimarket PR #1610.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13937/dod-occ-self/command.yaml"
