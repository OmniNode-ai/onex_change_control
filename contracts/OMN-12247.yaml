---
schema_version: "1.0.0"
ticket_id: "OMN-12247"
title: "Wire swarm dispatch FSM e2e auto-wiring gaps"
summary: >
  OCC evidence for omnimarket PR #925. The change fixes swarm dispatch, fanout, aggregator, and registry
  wiring so endpoint-backed swarm commands route through the FSM and queue-driven delegation path.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain"
    description: "122 swarm dispatch/fanout/aggregator tests pass on omnimarket PR #925 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12247/dod-golden-chain/command.yaml
          && grep -q '122 passed' drift/dod_receipts/OMN-12247/dod-golden-chain/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy or runtime restart is required before merge; runtime deployment can consume
      the merged dev head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12247/dod-deploy-assessment/command.yaml
          && grep -q 'no live deploy or runtime restart required before merge' drift/dod_receipts/OMN-12247/dod-deploy-assessment/command.yaml"
