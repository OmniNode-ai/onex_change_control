---
schema_version: "1.0.0"
ticket_id: "OMN-13878"
title: "chore(OMN-13878): track onex_change_control omnibase-core git-source override"
summary: >-
  Tracks the pre-existing omnibase-core [tool.uv.sources] git-rev override in onex_change_control that
  the OMN-13877 dep-provenance gate flags. The override line carries the OMN-13878 escape token (# raw-override-ok:
  OMN-13878) so the gate passes while migration of that pin to a PyPI-resolved source is tracked separately.
  The pin value is unchanged by OMN-13877.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-escape-token"
    description: >-
      onex_change_control pyproject.toml carries the OMN-13878 escape token on the omnibase-core override
      line, so the OMN-13877 gate reports OK while the override migration is tracked under this ticket.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13878/dod-occ-escape-token/command.yaml"
