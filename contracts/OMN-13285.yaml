---
schema_version: "1.0.0"
ticket_id: "OMN-13285"
title: "W6/GAP-3: port routing-authority + delegation-profile into a core COMPUTE validator; wire core+mkt"
summary: >-
  Ports the omnimarket-local scripts/ci/check_routing_authority.py (OMN-12821/12877/12883) and folds in
  the previously-unwired core validator_delegation_profile (O3) into ONE canonical core COMPUTE validator
  on ProtocolMessageHandler (validator_routing_authority). Exposed in core .pre-commit-hooks.yaml as check-routing-authority,
  wired in core .pre-commit-config.yaml (skips cleanly where no demo path lives), and wired in omnimarket
  (pre-commit + ci.yml) with the omnimarket-local script deleted (no shim). Build-time validator — lives
  in omnibase_core by dependency layering (core->market would be circular). Plan §3 W6.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1277"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1277 (jonah/omn-13285-routing-authority-core,
      base=dev). Adds the core COMPUTE validator validator_routing_authority + supporting models, exposes
      check-routing-authority in .pre-commit-hooks.yaml, wires it in core .pre-commit-config.yaml, deletes
      the unwired validator_delegation_profile, and adds positive+negative unit tests. Binds OMN-13285
      to the citing PR by pr_number and commit_sha so the Receipt Gate can resolve central OCC evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13285/dod-omnibase_core-pr-1277/command.yaml
  - id: "dod-omnimarket-pr-1279"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1279 (jonah/omn-13285-routing-authority-wire, base=dev).
      Wires the routing-authority demo gate to the canonical omnibase_core validator, deletes the omnimarket-local
      script, updates the core dependency pin to the validator commit, and keeps the focused integration
      test over the canonical gate. Binds OMN-13285 to omnimarket PR #1279 by pr_number and commit_sha
      so the Receipt Gate can resolve central OCC evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13285/dod-omnimarket-pr-1279/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13285 contract and the omnibase_core
      PR binding. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check can resolve
      a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13285/dod-occ-pr-self/command.yaml
  - id: "dod-occ-pr-omnimarket-receipt-self"
    description: >-
      OCC self-binding receipt for follow-up central evidence PR #2791 carrying the omnimarket PR #1279
      binding receipt. Binds the follow-up OCC PR by pr_number so the OCC receipt-gate eligibility check
      can resolve a PASS receipt for this evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13285/dod-occ-pr-omnimarket-receipt-self/command.yaml
