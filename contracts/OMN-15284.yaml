---
schema_version: "1.0.0"
ticket_id: "OMN-15284"
title: "Autobind OCC evidence for OMN-15284"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #712. Extended by an independent
  adversarial-verifier re-derivation (net-new dod_evidence entries below): the admin-client seam match
  against workflow_publisher.py/PR #703, a RED-before/GREEN-after re-run of the new test file on .200
  against origin/dev vs the PR head, the default-env regression proof, and an open caveat that the PR's
  full onex-api pytest CI check was still in_progress (not green) ~25 minutes into its run at verification
  time -- well outside every other completed run of the same workflow in the repo's last 10 runs."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #712 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-712"
    description: "PR #712 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15284/dod-OmniNode-ai-omninode_infra-pr-712/command.yaml"
  - id: "dod-OmniNode-ai-omninode_infra-pr-712-ci"
    description: "PR #712 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15284/dod-OmniNode-ai-omninode_infra-pr-712-ci/command.yaml"
  - id: "occ-self-bind-pr-5222"
    description: "OCC companion PR #5222 — self-bind for OMN-15284 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omn15284-seam-matches-workflow-publisher"
    description: "Independent verifier check: the admin client is constructed from the SAME ModelKafkaClientConfig-derived
      kwargs seam workflow_publisher.py/workflow_terminal_consumer.py already use (PR #703) -- a kwargs
      spread, not a hand-copied dict or a second config path. Asserted against the actual PR #712 diff
      content, not the PR's own description of itself."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -qx 'SEAM_KWARGS_SPREAD_COUNT=1' docs/evidence/OMN-15284/kafka-topic-provisioner-iam-probes.md
          && grep -qx 'SEAM_BARE_BOOTSTRAP_SERVERS_KWARG_REMOVED=1' docs/evidence/OMN-15284/kafka-topic-provisioner-iam-probes.md
          && grep -qx 'FILES_CHANGED=2' docs/evidence/OMN-15284/kafka-topic-provisioner-iam-probes.md
  - id: "dod-omn15284-red-before-green-after"
    description: "Independent verifier check, the load-bearing proof: the SAME unmodified new test file
      was executed on .200 twice against two drivers -- origin/dev (pre-fix) and PR #712's head (post-fix)
      -- changing nothing else. Pre-fix it fails both new tests with KeyError('security_protocol'), proving
      the pre-fix admin client never carries a security_protocol kwarg at all (RED-against-exists-but-wrong:
      the pre-fix function exists and completes; its kwargs are what's wrong). Post-fix all 15 unit tests
      in the file pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -qx 'RED_BEFORE=2 failed, 8 passed, 1 skipped' docs/evidence/OMN-15284/kafka-topic-provisioner-iam-probes.md
          && grep -qx 'GREEN_AFTER=15 passed, 1 skipped' docs/evidence/OMN-15284/kafka-topic-provisioner-iam-probes.md
          && grep -q "RED_BEFORE_FAILURE_MODE=KeyError: 'security_protocol'" docs/evidence/OMN-15284/kafka-topic-provisioner-iam-probes.md
  - id: "dod-omn15284-default-env-unchanged"
    description: "Independent verifier check: default (no SASL env) behavior is proven byte-identical
      to the pre-fix construction -- PLAINTEXT security_protocol, no sasl_mechanism kwarg, same bootstrap_servers/client_id
      -- via a dedicated regression test, not inferred from the IAM-path test alone."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -qx 'TEST_DEFAULT_ENV_FN_PRESENT=1' docs/evidence/OMN-15284/kafka-topic-provisioner-iam-probes.md
          && grep -qx 'TEST_SASL_MECHANISM_ABSENT_ASSERTION_PRESENT=1' docs/evidence/OMN-15284/kafka-topic-provisioner-iam-probes.md
  - id: "dod-omn15284-pytest-gate-caveat"
    description: "OPEN CAVEAT, not a pass: the full onex-api pytest CI check on PR #712 was still in_progress
      after ~25 minutes at verification time, well outside the 2-3 minute duration of every other completed
      run of the same workflow in the repo's last 10 runs. This entry exists to force re-verification
      of that gate before the PR is merged -- it is falsifiable against a fresh `gh pr checks` poll, not
      against a restated claim."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -qx 'CHECKS_PENDING=pytest' docs/evidence/OMN-15284/kafka-topic-provisioner-iam-probes.md
