---
schema_version: "1.0.0"
ticket_id: "OMN-15484"
title: "OCC evidence for OMN-15484 — merge-hold-gate fan-out to the incident repos"
summary: >
  OCC companion authored by the INDEPENDENT ADVERSARIAL VERIFIER for lane next-wave-0730/hold-gate-fanout,
  not by the implementer (no-self-authored-evidence). Binds OMN-15484 to the three PRs that fan the OMN-15483
  merge-hold gate out of omnimarket into the two repos carrying every OMN-15483 incident: omnimarket#1976
  (the one authored reusable surface + first caller), onex_change_control#5629 (incident sections A/B/D/E)
  and omnibase_infra#2570 (incident section C). Every check below was re-derived from live GitHub surfaces
  by the verifier, never taken from the build report.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "merge-hold-gate / evaluate reports a SUCCESS conclusion on each adopting PR"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json statusCheckRollup"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1976"
    description: >
      omnimarket#1976 — the one authored surface (merge-hold-gate-reusable.yml) plus omnimarket as first
      caller. The behavioural check asserts the gate context actually concluded SUCCESS, and the unit
      suite asserts the fan-out contract.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1976 --repo OmniNode-ai/omnimarket --json statusCheckRollup --jq '.statusCheckRollup[]|select(.name==\"merge-hold-gate
          / evaluate\")|.conclusion' | grep -qx SUCCESS"
      - check_type: "command"
        check_value: "uv run pytest tests/unit/scripts/ci/test_merge_hold_gate_fanout_omn15484.py -q"
  - id: "dod-OmniNode-ai-omnibase-infra-pr-2570"
    description: >
      omnibase_infra#2570 — OMN-15483 incident repo (section C, #2560). The AC4 falsifier: assert the
      hold gate still concluded SUCCESS in the SAME CI run in which occ-preflight/eligibility failed,
      so no unrelated upstream failure can cascade-skip it. Fails if the gate is ever made needs-gated.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/actions/runs/30565261108/jobs --jq '.jobs[]|select(.name==\"merge-hold-gate
          / evaluate\")|.conclusion' | grep -qx success"
      - check_type: "command"
        check_value: "uv run pytest tests/ci/test_merge_hold_gate_omn15484.py -q"
  - id: "occ-self-bind-pr-5629"
    description: >
      onex_change_control#5629 — OMN-15483 incident repo (sections A/B/D/E), self-bind. Asserts the anti-removal
      wiring validator passes against the shipped ci.yml, so deleting either half of the registration
      (the job, or ci-summary's success-only block) fails a required check rather than silently ending
      enforcement.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/validation/check_merge_hold_gate_wiring.py"
      - check_type: "command"
        check_value: "uv run pytest tests/unit/scripts/test_merge_hold_gate_wiring_omn15484.py -q"
