---
schema_version: "1.0.0"
ticket_id: OMN-4627
title: Investigate `omninode-cloud-migrate` job crash
summary: "Investigate `omninode-cloud-migrate` job crash"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}"
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json mergedAt -q .mergedAt --repo ${REPO}"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --watch --repo ${REPO}"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-OmniNode-ai-omninode_infra-pr-642"
    description: "PR #642 on OmniNode-ai/omninode_infra -- migration tracking collision fix present."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-4627/dod-OmniNode-ai-omninode_infra-pr-642/command.yaml"
  - id: "dod-OmniNode-ai-omninode_infra-pr-642-ci"
    description: "PR #642 on OmniNode-ai/omninode_infra -- CI and receipt gates pass for migration fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-4627/dod-OmniNode-ai-omninode_infra-pr-642-ci/command.yaml"
  - id: "occ-self-bind-pr-4730"
    description: "OCC companion PR #4730 -- self-bind for OMN-4627 PR #642 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4730 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-supersede-001-pr638-open-dev"
    description: "Append-only supersession for the generated PR-open check; product PR #638 is open against
      dev for OMN-4627."
    evidence_artifact: "supersedes_dod_evidence:dod-001"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-4627/dod-supersede-001-pr638-open-dev/command.yaml"
  - id: "dod-supersede-002-pr638-head"
    description: "Append-only supersession for the generated merged-to-main check; product PR #638 is
      open at the exact reviewed head and will merge after OCC evidence lands."
    evidence_artifact: "supersedes_dod_evidence:dod-002"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-4627/dod-supersede-002-pr638-head/command.yaml"
  - id: "dod-supersede-003-pr638-tests"
    description: "Append-only supersession for the generated CI watch check; product PR #638 body records
      focused and suite-level verification for the log-capture repair."
    evidence_artifact: "supersedes_dod_evidence:dod-003"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-4627/dod-supersede-003-pr638-tests/command.yaml"
  - id: "dod-supersede-004-pr638-diff-scope"
    description: "Append-only supersession for the generated all-files pre-commit check; product PR #638
      changes only the migration runner and focused tests."
    evidence_artifact: "supersedes_dod_evidence:dod-004"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-4627/dod-supersede-004-pr638-diff-scope/command.yaml"
  - id: "dod-OmniNode-ai-omninode_infra-pr-638"
    description: "PR #638 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-4627/dod-OmniNode-ai-omninode_infra-pr-638/command.yaml"
  - id: "dod-OmniNode-ai-omninode_infra-pr-638-ci"
    description: "PR #638 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-4627/dod-OmniNode-ai-omninode_infra-pr-638-ci/command.yaml"
  - id: "occ-self-bind-pr-4718"
    description: "OCC companion PR #4718 — self-bind for OMN-4627 (OMN-14650)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4739"
    description: "Binds OMN-4627 to OCC companion PR #4739 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
