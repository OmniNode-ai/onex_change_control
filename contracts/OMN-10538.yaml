---
schema_version: "1.0.0"
ticket_id: "OMN-10538"
title: "omninode_infra second-pass review remediation"
summary: >
  Closes residual findings from the OMN-10538 second-pass review cohort. OMN-10599: removes rollback_attempted
  from migrations_log.direction CHECK constraint across 35 SQL migration files; adds 20260512_migrations_log_direction_cleanup.sql
  to alter the live constraint. OMN-10328: refactors test_migration_plan_entitlements.py to call the production
  function directly and replaces fake checksum with NULL.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "CI tests pass on omninode_infra PR #420"
    command: "gh pr checks 420 --repo OmniNode-ai/omninode_infra"
  - kind: "ci"
    description: "All CI gates green on omninode_infra PR #420 (receipt gate satisfied by this OCC PR)"
    command: "gh pr view 420 --repo OmniNode-ai/omninode_infra --json statusCheckRollup"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omn-10599-sql-files"
    description: "35 SQL migration files updated to remove rollback_attempted from direction CHECK constraint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 420 -R OmniNode-ai/omninode_infra | grep -q 'rollback_attempted'"
  - id: "dod-omn-10599-cleanup-migration"
    description: "20260512_migrations_log_direction_cleanup.sql added to alter live constraint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 420 -R OmniNode-ai/omninode_infra | grep -q 'migrations_log_direction_cleanup'"
  - id: "dod-omn-10328-test-refactor"
    description: "test_migration_plan_entitlements.py calls production function _m4_get_api_rpm_limit_for_tenant
      directly"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 420 -R OmniNode-ai/omninode_infra | grep -q '_m4_get_api_rpm_limit_for_tenant'"
  - id: "dod-omn-10328-fake-checksum"
    description: "Fake manual-application checksum replaced with NULL in 20260429_plan_entitlements.sql"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 420 -R OmniNode-ai/omninode_infra | grep -q 'plan_entitlements'"
  - id: "dod-ci-green"
    description: "All CI gates pass on omninode_infra PR #420 except Receipt Gate (unblocked by this OCC
      PR)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 420 --repo OmniNode-ai/omninode_infra --json mergeStateStatus --jq '.mergeStateStatus'
          | grep -qv BLOCKED"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for this OCC PR."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr list --repo OmniNode-ai/onex_change_control --head jonah/omn-10538-occ --json
          state --jq '.[0].state' | grep -q OPEN"
