---
schema_version: "1.0.0"
ticket_id: "OMN-11527"
title: "docs(OMN-11523): DEMO_GUIDE agent primary, replay provenance, dashboard boundaries"
summary: >-
  Restructure the judge-facing DEMO_GUIDE so the live ADK agent loop is the primary proof path, the unified
  demo and deterministic replay modes have explicit runbooks, replay provenance records fixture and prompt
  identity, and dashboard guidance states the projection-only truth boundary.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "onex-self-extending-agent PR #110 carries the OMN-11527 DEMO_GUIDE update."
    command: "gh pr view 110 --repo OmniNode-ai/onex-self-extending-agent --json number,state,headRefName,headRefOid,url,title"
  - kind: "tests"
    description: "Docs-adjacent replay and declared demo tests pass locally."
    command: "uv run pytest tests/unit/test_replay.py tests/unit/test_demo_pipeline.py tests/unit/test_main.py::test_main_demo_without_api_key_uses_replay_mode
      -q"
  - kind: "manual"
    description: "Golden fixture replay executes and proves deterministic no-LLM registration/invocation
      behavior."
    command: "uv run python -m src --replay docs/evidence/golden/golden_fixture.json"
  - kind: "manual"
    description: "DEMO_GUIDE documents the primary agent path, --demo, --replay, replay provenance, dashboard
      boundary, and canonical customer review sentiment task."
    command: "grep -q 'Replay mode performs deterministic verification. No live model inference.' docs/DEMO_GUIDE.md"
  - kind: "ci"
    description: "onex_change_control PR carrying this central evidence is bound to OMN-11527."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11527/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-110-open"
    description: "onex-self-extending-agent PR #110 carries OMN-11527 at the recorded head SHA."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11527/dod-product-pr-110-open/command.yaml\"
          && grep -q '^pr_number: 110$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11527/dod-product-pr-110-open/command.yaml\"
          && grep -q '^commit_sha: \"1556af1e67e5ccd338077218ba14511ea03de851\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11527/dod-product-pr-110-open/command.yaml\""
  - id: "dod-local-checks"
    description: "Docs-adjacent replay and declared demo tests passed locally."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11527/dod-local-checks/command.yaml\"
          && grep -q '13 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11527/dod-local-checks/command.yaml\""
  - id: "dod-golden-replay"
    description: "Golden replay validates, registers, invokes, and confirms no LLM call."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11527/dod-golden-replay/command.yaml\"
          && grep -q 'Done — no LLM calls made' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11527/dod-golden-replay/command.yaml\""
  - id: "dod-guide-content"
    description: "DEMO_GUIDE contains the required agent-primary, replay provenance, dashboard boundary,
      and canonical task guidance."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11527/dod-guide-content/command.yaml\"
          && grep -q 'Dashboard projection truth boundary' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11527/dod-guide-content/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OMN-11527 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11527/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number:' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11527/dod-occ-pr/command.yaml\""
