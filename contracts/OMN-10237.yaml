---
schema_version: "1.0.0"
ticket_id: "OMN-10237"
title: "[Canonical Skill Path] Collapse local ingress into broker passthrough only"
summary: "Collapse local ingress into broker passthrough only for runtime-backed skill requests"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused omnibase_infra runtime ingress and broker passthrough proof passes locally"
    command: "cd ../omnibase_infra && uv run pytest tests/unit/runtime tests/integration -k 'local_ingress
      or runtime_host_process or pattern_b_broker or node_alias' -q"
  - kind: "ci"
    description: "omnibase_infra PR checks pass for the local-ingress broker cutover"
    command: "gh pr checks --repo OmniNode-ai/omnibase_infra --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Canonical proof exists that local ingress cannot dispatch handlers directly for skill-originated
      work"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10237/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "Canonical proof exists that broker-native command_name requests are forwarded through
      RuntimePatternBBroker"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10237/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "Canonical proof exists that node_alias compatibility is only a deprecated translation
      path with telemetry"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10237/dod-003 -type f -name '*.yaml' | grep -q ."
  - id: "dod-004"
    description: "Focused host-local transport or broker passthrough proof is captured canonically"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10237/dod-004 -type f -name '*.yaml' | grep -q ."
