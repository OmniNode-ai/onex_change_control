---
schema_version: "1.0.0"
ticket_id: "OMN-11184"
title: "Wire generation terminal events into delegation_events projection"
summary: >
  Subscribes node_projection_delegation to onex.evt.omnimarket.node-generation-completed.v1 and projects
  benchmark payloads into a new generation_events table so node generation activity is visible on the
  delegation dashboard alongside FSM delegation events.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-688"
    description: "Bind omnimarket PR #688 to OMN-11184 at observed head SHA."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11184/dod-omnimarket-pr-688/command.yaml\"
          && grep -q '^pr_number: 688$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11184/dod-omnimarket-pr-688/command.yaml\"
          && grep -q '^commit_sha: \"3413a37df107552e8f43d9ecf04c047c6db6d2e7\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11184/dod-omnimarket-pr-688/command.yaml\""
  - id: "dod-focused-tests"
    description: "Projection handler and golden-chain tests pass for generation event routing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11184/dod-focused-tests/command.yaml\"
          && grep -q '^commit_sha: \"3413a37df107552e8f43d9ecf04c047c6db6d2e7\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11184/dod-focused-tests/command.yaml\""
  - id: "dod-deploy"
    description: "Migration 0008_generation_events.sql applied; projection runner restarted to pick up
      new topic subscription."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11184/dod-deploy/command.yaml\"
          && grep -q '^commit_sha: \"3413a37df107552e8f43d9ecf04c047c6db6d2e7\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11184/dod-deploy/command.yaml\""
  - id: "dod-occ-self"
    description: "Self-binding receipt for OCC PR carrying OMN-11184 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11184/dod-occ-self/command.yaml\""
