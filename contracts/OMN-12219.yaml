---
schema_version: "1.0.0"
ticket_id: "OMN-12219"
title: "Create node_decision_store_orchestrator contract + handler stub"
summary: >
  OCC evidence for omnimarket PR #911. The change adds node_decision_store_orchestrator (ORCHESTRATOR)
  to omnimarket as a contract-only stub with node_not_implemented: true and 10 golden-chain unit tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain"
    description: "10 golden-chain tests pass for node_decision_store_orchestrator on omnimarket PR #911
      head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12219/dod-golden-chain/command.yaml
          && grep -q '10 passed' drift/dod_receipts/OMN-12219/dod-golden-chain/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy or runtime restart is required before merge for this contract + handler-stub
      slice."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12219/dod-deploy-assessment/command.yaml
          && grep -q 'no live deploy or runtime restart required before merge' drift/dod_receipts/OMN-12219/dod-deploy-assessment/command.yaml"
