---
schema_version: "1.0.0"
ticket_id: "OMN-9877"
title: "feat(OMN-9877): clean up runtime projection wiring"
summary: >
  OCC evidence for omnibase_infra PR #1596. Cleans up runtime projection wiring: refactors auto-wiring
  handler dispatch result application to use _select_dispatch_result_output_topic helper, adds util_runtime_packages
  for runtime package activation checks, updates service_kernel to use is_runtime_package_active for skill-manifest
  root discovery, fixes embedding effect example URL (8002→8100), and improves service_runtime_health_monitor
  contract discovery. Internal refactors only — no new Kafka topics, no schema changes, no API surface
  changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Auto-wiring and health monitor unit tests pass"
    command: "See drift/dod_receipts/OMN-9877/dod-unit-tests/command.yaml"
  - kind: "ci"
    description: "Deploy smoke — internal refactor, no live deploy required"
    command: "See drift/dod_receipts/OMN-9877/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "Auto-wiring output topic selection and health monitor unit tests pass"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'PASS' drift/dod_receipts/OMN-9877/dod-unit-tests/command.yaml && echo 'OMN-9877 unit-tests
          receipt present'
  - id: "dod-deploy"
    description: >-
      Deploy assessment recorded — internal refactor only, no live deploy or runtime restart performed.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'status: PASS' drift/dod_receipts/OMN-9877/dod-deploy/command.yaml && grep -q 'evidence_item_id:
          "dod-deploy"' drift/dod_receipts/OMN-9877/dod-deploy/command.yaml && echo 'OMN-9877 deploy receipt
          present and PASS'
  - id: "dod-infra-pr-1596"
    description: "omnibase_infra PR #1596 carries all runtime wiring cleanup changes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'PASS' drift/dod_receipts/OMN-9877/dod-infra-pr-1596/command.yaml && grep -q 'pr_number:
          1596' drift/dod_receipts/OMN-9877/dod-infra-pr-1596/command.yaml
  - id: "dod-omnimarket-pr-645"
    description: "omnimarket PR #645 carries session bootstrap runtime projection hardening changes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'PASS' drift/dod_receipts/OMN-9877/dod-omnimarket-pr-645/command.yaml && grep -q 'pr_number:
          645' drift/dod_receipts/OMN-9877/dod-omnimarket-pr-645/command.yaml
  - id: "dod-omnibase-core-pr-1081"
    description: "omnibase_core PR #1081 routes run-node through contract dispatch"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'PASS' drift/dod_receipts/OMN-9877/dod-omnibase-core-pr-1081/command.yaml && grep -q
          'pr_number: 1081' drift/dod_receipts/OMN-9877/dod-omnibase-core-pr-1081/command.yaml
  - id: "dod-occ-pr-1039"
    description: "Self-binding receipt for OCC PR #1039 — anchors evidence chain to this OCC PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'status: PASS' drift/dod_receipts/OMN-9877/dod-occ-pr-1039/command.yaml && grep -q 'pr_number:
          1039' drift/dod_receipts/OMN-9877/dod-occ-pr-1039/command.yaml
