---
schema_version: "1.0.0"
ticket_id: OMN-7390
title: Reconcile cron-closeout.sh with /close_out skill
summary: "Bind closeout cron/phase-contract parity and omnimarket node_close_out FSM alignment evidence."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omniclaude closeout phase checker proves cron contract to market node phase parity."
    command: "uv run python scripts/check_closeout_phase_compliance.py --market-node-state <omnimarket>/src/omnimarket/nodes/node_close_out/models/model_close_out_state.py"
  - kind: "tests"
    description: "omnimarket golden-chain closeout tests prove contract.yaml and CLOSE_OUT_PHASE_ORDER
      parity."
    command: "uv run pytest tests/test_golden_chain_close_out.py tests/test_close_out_concurrent_run_guard.py
      -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr"
    description: "omniclaude PR #1595 carries the closeout phase compliance checker and docs update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-7390/dod-omniclaude-pr/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-7390/dod-omniclaude-pr/command.yaml"
  - id: "dod-omniclaude-tests"
    description: "omniclaude closeout phase checker and unit tests passed locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-7390/dod-omniclaude-tests/command.yaml && grep -q
          '^status: PASS$' drift/dod_receipts/OMN-7390/dod-omniclaude-tests/command.yaml"
  - id: "dod-omnimarket-pr"
    description: "omnimarket PR #670 carries the node_close_out FSM and contract parity update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-7390/dod-omnimarket-pr/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-7390/dod-omnimarket-pr/command.yaml"
  - id: "dod-omnimarket-tests"
    description: "omnimarket closeout golden-chain and concurrent guard tests passed locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-7390/dod-omnimarket-tests/command.yaml && grep -q
          '^status: PASS$' drift/dod_receipts/OMN-7390/dod-omnimarket-tests/command.yaml"
  - id: "dod-omnimarket-lint"
    description: "omnimarket node_close_out ruff check passed locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-7390/dod-omnimarket-lint/command.yaml && grep -q
          '^status: PASS$' drift/dod_receipts/OMN-7390/dod-omnimarket-lint/command.yaml"
  - id: "dod-occ-pr-self"
    description: "OCC PR carries the OMN-7390 central contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-7390/dod-occ-pr-self/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-7390/dod-occ-pr-self/command.yaml"
