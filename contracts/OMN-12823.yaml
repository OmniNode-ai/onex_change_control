---
schema_version: "1.0.0"
ticket_id: "OMN-12823"
title: "Emit platform.node-registration.v1 with conformant payload; preserve correlation_id"
summary: >
  Close-the-loop plan track A4. omnimarket node_generation_consumer retargets its registration event from
  the dead onex.evt.omnimarket.node-registered.v1 to the canonical platform node-registration topic that
  ServiceMCPToolSync consumes, and makes the payload conformant (event_type=registered, service_name,
  tags with mcp-enabled / node-type:orchestrator / mcp-tool:<name>) while preserving correlation_id. Bound
  to omnimarket PR #1111 at head SHA 28025afd05817ae1393ca1843437b7bb6dc8cafa (rebased onto dev).
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr"
    description: >
      omnimarket PR #1111 retargets the generation-consumer registration emit to the platform node-registration
      topic and makes the payload MCP-sync conformant (event_type, service_name, tags, correlation_id),
      covered by five new TDD payload-conformance unit tests. The local full suite is green under the
      repo standard command and mypy --strict passes.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12823/dod-omnimarket-pr/command.yaml
          && grep -q '^pr_number: 1111$' drift/dod_receipts/OMN-12823/dod-omnimarket-pr/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Deploy-gate assessment for the runtime-path touch (node_generation_consumer contract + handlers).
      This is a source-level change to the registration event topic + payload; no live deploy, runtime
      restart, .201 mutation, or hot-load was performed. The registration topic flows over the bus and
      is exercised end-to-end by the Phase D stability-test loop proof (D1/D2), not by this A4 PR. CI
      deploy-gate + verify gates validate the evidence binding before merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12823/dod-deploy-assessment/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-12823/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2334"
    description: >
      onex_change_control PR #2334 publishes the OMN-12823 ticket contract and DoD receipts that bind
      the omnimarket registration-topic + payload-conformance work (track A4) to durable evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12823/dod-occ-pr-2334/command.yaml
          && grep -q '^pr_number: 2334$' drift/dod_receipts/OMN-12823/dod-occ-pr-2334/command.yaml"
