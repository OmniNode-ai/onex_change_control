---
schema_version: "1.0.0"
ticket_id: "OMN-12477"
title: "Enforce dev-only promotion to main by removing hotfix bypass"
summary: >
  Remove the hotfix/* bypass from the main-target-guard workflow so main accepts only dev/promotion promotion
  PRs with promotion receipts. This prevents unverified direct hotfix merges from drifting main ahead
  of dev.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-main-target-guard-dev-only"
    description: "OCC PR #1915 removes the hotfix/* bypass from main-target-guard while preserving the
      dev/promotion receipt path."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12477/dod-main-target-guard-dev-only/command.yaml
          && grep -q '^pr_number: 1915$' drift/dod_receipts/OMN-12477/dod-main-target-guard-dev-only/command.yaml"
  - id: "dod-occ-pr-1915-self-binding"
    description: "OCC PR #1915 publishes the central OMN-12477 contract and PASS receipt required by Receipt
      Gate."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12477/dod-occ-pr-1915-self-binding/command.yaml
          && grep -q '^pr_number: 1915$' drift/dod_receipts/OMN-12477/dod-occ-pr-1915-self-binding/command.yaml"
