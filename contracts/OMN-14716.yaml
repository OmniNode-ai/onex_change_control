---
schema_version: 1.0.0
ticket_id: OMN-14716
title: "Evidence for OmniNode-ai/omnibase_infra#2330: coerce def-B wire dicts at Kafka boundary"
summary: >
  OCC evidence for omnibase_infra#2330 validating the Kafka auto-wiring boundary fix that coerces raw
  materialized wire dictionaries into declared def-B pydantic input models and surfaces silent dispatch
  failures through the existing DLQ path.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "PR #2330 product diff scope present"
    command: "PRODUCT_PR_NUMBER=2330; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER}; gh pr diff
      $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-omnibase_infra-pr-2330
    description: "PR #2330 on OmniNode-ai/omnibase_infra exists and is bound to OMN-14716."
    source: generated
    checks:
      - check_type: command
        check_value: "PRODUCT_PR_NUMBER=2330; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json number,state,headRefOid"
  - id: dod-omnibase_infra-pr-2330-diff
    description: "PR #2330 on OmniNode-ai/omnibase_infra has a non-empty reviewed product diff for OMN-14716."
    source: generated
    checks:
      - check_type: command
        check_value: "PRODUCT_PR_NUMBER=2330; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q ."
  - id: dod-omnibase_infra-pr-2330-deploy
    description: "Deploy-readiness assessment for omnibase_infra PR #2330 runtime auto-wiring edits. No
      live deploy, restart, docker exec, rpk topic produce, or .201 mutation was performed by this OCC
      PR; post-merge validation remains in the normal dev deploy pipeline."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14716/dod-omnibase_infra-pr-2330-deploy/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14716/dod-omnibase_infra-pr-2330-deploy/command.yaml"
  - id: occ-self-bind-pr-4339
    description: "OCC companion PR #4339 on OmniNode-ai/onex_change_control self-binding proof for OMN-14716."
    source: generated
    checks:
      - check_type: command
        check_value: "SELF_PR_NUMBER=4339; SELF_REPO=OmniNode-ai/onex_change_control; : ${PR_NUMBER};
          gh pr view $SELF_PR_NUMBER --repo $SELF_REPO --json number,state"
