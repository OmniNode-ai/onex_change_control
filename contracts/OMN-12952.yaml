---
schema_version: "1.0.0"
ticket_id: "OMN-12952"
title: "Per-leg delegation e2e probe harness — DEL-01 red-with-cause, GEN-01 green proof leg"
summary: >-
  Adds test_leg_probe.py to the existing e2e_probe directory (OMN-12789 harness). Two per-leg probe classes:
  TestE2EDEL01DelegateSkillLeg (delegate-skill path, known dispatcher-broken on dev; xfail documents DEL-01,
  reports red-with-cause without failing dry-run) and TestE2EGEN01GenerationLeg (generation consumer path;
  publishes to onex.cmd.omnimarket.node-generation-requested.v1, races completed/failed terminals, polls
  generation_events DB row, reads projection API; green proof leg). LegResult dataclass emits structured
  PROBE_LEG_STATUS log lines per leg. All 9 tests opt-in guarded by OMN_ALLOW_LIVE_E2E_PROBE=true — CI
  never hits bus. Dry-run: 9 skipped, 0 errors, 0 warnings. Assertion thresholds are TODO OMN-12952 placeholders
  pending GEN-01 packet calibration from Milestone 4.4(b).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-e2e-probe-dry-run"
    description: >-
      omnimarket PR (branch jonah/omn-12952-lane-parameterized-e2e-probe, HEAD eee2087c030a86ec2e46268e7cf41a7f0c8afd3d)
      adds test_leg_probe.py. Dry-run from clean checkout: 9 skipped, 0 errors. All hooks pass (leaked-literals-gate,
      no-hardcoded-topics, topic-naming-lint, no-untracked-todos). Pre-existing test failure on test_golden_chain_generation_self_extension.py
      confirmed on canonical main before this PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12952/dod-e2e-probe-dry-run/command.yaml
