---
schema_version: 1.0.0
ticket_id: OMN-14051
title: Reject non-hermetic check_value in contract-compliance validator
summary: Central OCC evidence for onex_change_control#4426. The product PR folds a non-hermetic command
  guard into the existing contract-compliance validator so new/touched contracts fail at authoring time
  when check_value invokes remote shell, live container/orchestration daemons, non-loopback network egress,
  or private/Tailscale IP literals. Exact product head 5120e3fa4f056d6ee51fe16589ebf0fc641d7c45.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #4426 product head is exact for OMN-14051 evidence.'
    command: 'PRODUCT_PR_NUMBER=4426; PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=5120e3fa4f056d6ee51fe16589ebf0fc641d7c45;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-onex_change_control-pr-4426
    description: 'PR #4426 on OmniNode-ai/onex_change_control exists and is bound to OMN-14051 at exact
      product head 5120e3fa4f056d6ee51fe16589ebf0fc641d7c45.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=4426; PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=5120e3fa4f056d6ee51fe16589ebf0fc641d7c45;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-onex_change_control-pr-4426-diff
    description: 'PR #4426 changes the contract-compliance validator, its CI wrapper, and focused regression
      tests for non-hermetic check_value rejection.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=4426; PRODUCT_REPO=OmniNode-ai/onex_change_control; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "src/onex_change_control/scripts/contract_compliance_check.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "scripts/ci/run_contract_compliance_check.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "tests/test_contract_compliance_check.py"'
  - id: dod-onex_change_control-pr-4426-local-verification
    description: Product PR body records focused pytest, ruff, mypy, changed-file pre-commit, and pre-push
      verification for OMN-14051.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=4426; PRODUCT_REPO=OmniNode-ai/onex_change_control; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "66 passed"
          && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "mypy
          --strict" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep
          -q "pre-commit"'
  - id: dod-onex_change_control-pr-4426-deploy
    description: Deploy-readiness assessment for OMN-14051. The PR changes OCC validator logic, a CI wrapper,
      and tests only; no live deploy, restart, .201, Kafka, database, or production mutation was performed
      by this OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14051/dod-onex_change_control-pr-4426-deploy/command.yaml
          && grep -qi ''no live deploy, restart, .201, Kafka, database, or production mutation'' drift/dod_receipts/OMN-14051/dod-onex_change_control-pr-4426-deploy/command.yaml'
  - id: occ-self-bind-pr-4427
    description: 'OCC PR #4427 on OmniNode-ai/onex_change_control self-binding proof for OMN-14051 evidence.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
