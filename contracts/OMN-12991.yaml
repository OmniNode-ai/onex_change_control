---
schema_version: "1.0.0"
ticket_id: "OMN-12991"
title: "Document June 11 hackathon SEA proof and judge reproducibility"
summary: >-
  Central evidence for omniintelligence PR #704, which adds a June 11 hackathon SEA proof and judge reproduction
  guide. The documentation captures accepted proof packet fields, routing authority, dashboard evidence
  boundaries, topic/projection authority distinctions, and fallback classifications without changing runtime
  code.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
evidence_requirements:
  - kind: "docs"
    description: "SEA proof guide is added and linked from docs/INDEX.md."
    command: "git diff --name-only origin/dev...HEAD"
  - kind: "manual"
    description: "Docs-only pre-commit slice passes for the guide and index."
    command: "uv run pre-commit run --files docs/INDEX.md docs/guides/JUNE_11_HACKATHON_SEA_PROOF.md"
dod_evidence:
  - id: "dod-omniintelligence-pr-704"
    description: >-
      omniintelligence PR #704 adds the June 11 SEA proof and judge reproduction guide and links it from
      the documentation index.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12991/dod-omniintelligence-pr-704/command.yaml"
          && grep -q '^pr_number: 704$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12991/dod-omniintelligence-pr-704/command.yaml"
          && grep -q '3917daab47a633021ab836a1c006edfe366b1895' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12991/dod-omniintelligence-pr-704/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      The onex_change_control PR carries the central OMN-12991 contract and receipts required by downstream
      Receipt Gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12991/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-12991"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12991/dod-occ-pr/command.yaml"
