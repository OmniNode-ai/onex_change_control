---
schema_version: "1.0.0"
ticket_id: "OMN-12991"
title: "Document June 11 OmniNode SEA proof, architecture, and deterministic workflow"
summary: >
  Add the June 11 hackathon SEA proof documentation to the canonical onex-self-extending-agent repo, not
  OmniIntelligence. The docs define the accepted proof packet, OmniNode system diagram, authority diagram,
  technical design, deterministic workflow invariants, judge reproduction boundary, and the distinction
  between SEA local proof and full OmniNode runtime/dashboard proof.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
dod_evidence:
  - id: "dod-sea-pr-230"
    description: >
      onex-self-extending-agent PR #230 adds the June 11 proof packet, OmniNode-centered architecture
      diagrams, and SEA technical design. It links those docs from README, DEMO_GUIDE, and JUDGE_REPRODUCE.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12991/dod-sea-pr-230/command.yaml"
          && grep -q '^ticket_id: "OMN-12991"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12991/dod-sea-pr-230/command.yaml"
          && grep -q '^pr_number: 230$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12991/dod-sea-pr-230/command.yaml"
  - id: "dod-occ-pr"
    description: >
      This OCC PR carries the OMN-12991 central contract and receipts used by the onex-self-extending-agent
      PR Receipt Gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12991/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-12991"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12991/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2507$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12991/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
