---
schema_version: "1.0.0"
ticket_id: "OMN-9870"
title: "Stabilize merge-sweep CI-fix runtime effect path"
summary: "Central receipt contract for omnimarket PR #492: contract-owned LLM routing, CI-log GitHub API
  fetches, patch preflight hardening, phase logging, triage routing policy validation, and compile-only
  runtime proof."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused omnimarket tests and lint pass for the CI-fix effect path and merge-sweep triage
      routing policy."
    command: "See drift/dod_receipts/OMN-9870/dod-001/command.yaml"
  - kind: "ci"
    description: "Compile-only runtime request proof validates the merge-sweep command path without live
      deploy, restart, event publish, or PR worktree mutation."
    command: "See drift/dod_receipts/OMN-9870/dod-002/command.yaml"
  - kind: "ci"
    description: "Read-only GitHub API proof fetches a CI job log through the new helper path."
    command: "See drift/dod_receipts/OMN-9870/dod-003/command.yaml"
  - kind: "ci"
    description: "Repo-local deploy contract and whitespace checks pass for the omnimarket branch."
    command: "See drift/dod_receipts/OMN-9870/dod-004/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused omnimarket unit/golden tests plus ruff evidence is present for PR #492."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '98 passed, 19 warnings' drift/dod_receipts/OMN-9870/dod-001/command.yaml
          && grep -q 'ruff check' drift/dod_receipts/OMN-9870/dod-001/command.yaml && echo 'OMN-9870 dod-001
          receipt markers present'"
  - id: "dod-002"
    description: "Compile-only merge-sweep runtime request proof is present and records no live deploy/restart/runtime
      mutation."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'compile-only' drift/dod_receipts/OMN-9870/dod-002/command.yaml && grep
          -q 'ok: true' drift/dod_receipts/OMN-9870/dod-002/command.yaml && grep -q 'No live deploy, restart,
          event publish, or PR worktree mutation' drift/dod_receipts/OMN-9870/dod-002/command.yaml &&
          echo 'OMN-9870 dod-002 receipt markers present'"
  - id: "dod-003"
    description: "Read-only GitHub API CI-log fetch proof is present for the new effect-path helper."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'fetched 2,848 chars' drift/dod_receipts/OMN-9870/dod-003/command.yaml &&
          grep -q 'contains_ci_tests_gate: True' drift/dod_receipts/OMN-9870/dod-003/command.yaml && echo
          'OMN-9870 dod-003 receipt markers present'"
  - id: "dod-004"
    description: "Repo-local deploy contract validation and diff whitespace evidence is present."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'validate-yaml contracts/OMN-9870.yaml' drift/dod_receipts/OMN-9870/dod-004/command.yaml
          && grep -q 'git diff --check' drift/dod_receipts/OMN-9870/dod-004/command.yaml && echo 'OMN-9870
          dod-004 receipt markers present'"
