---
schema_version: "1.0.0"
ticket_id: "OMN-12691"
title: "Workstream I-C: golden-chain coverage enforcement gate"
summary: >-
  Central evidence that omnimarket PR #1042 adds a baseline-friendly CI and pre-commit gate which blocks
  changed active market nodes from shipping without matching golden-chain coverage. The gate derives enforcement
  targets from changed node directories, skips deprecated or non-runtime metadata, and accepts repo-level
  or node-local golden-chain tests matched by node name or contract handler references.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-golden-chain-coverage-gate"
    description: "Focused omnimarket tests and static checks prove the golden-chain coverage gate behavior
      and CI/pre-commit wiring."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12691/dod-omnimarket-golden-chain-coverage-gate/command.yaml"
          && grep -q '^pr_number: 1042$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12691/dod-omnimarket-golden-chain-coverage-gate/command.yaml"
  - id: "dod-deploy-gate-no-runtime-mutation"
    description: "Deploy-gate evidence for omnimarket CI/pre-commit-only enforcement changes in PR #1042."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12691/dod-deploy-gate-no-runtime-mutation/command.yaml"
          && grep -q '^pr_number: 1042$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12691/dod-deploy-gate-no-runtime-mutation/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-12691 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12691/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2177$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12691/dod-occ-pr/command.yaml"
