---
schema_version: "1.0.0"
ticket_id: OMN-9410
summary: "fix(auto-wiring): async pre-resolution bypasses asyncio.run() crash in EFFECTS profile"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on omnibase_infra PR #1366"
    command: "gh pr checks 1366 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1366 --repo OmniNode-ai/omnibase_infra --json state -q .state"
  - id: "dod-002"
    description: "Contract artifact file present in repo"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-9410.yaml"
  - id: "dod-003"
    description: "Unit tests pass (7 new tests for _async_resolve_from_container and wire_from_manifest)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks 1366 --repo OmniNode-ai/omnibase_infra"
  - id: "dod-004"
    description: "Deploy: omninode-runtime-effects rebuilt on .201 after merge"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "ssh jonah@192.168.86.201 docker ps --filter name=omninode-runtime-effects --format
          '{{.Status}}'"
