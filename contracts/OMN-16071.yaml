---
schema_version: "1.0.0"
ticket_id: "OMN-16071"
title: "Autobind OCC evidence for OMN-16071"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2081.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2081 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2081"
    description: "PR #2081 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2081 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2081 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2081 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2081 --repo OmniNode-ai/omnimarket --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-deploy-assessment-falsifiable"
    description: >-
      Falsifiable replacement for dod-deploy-assessment, APPENDED under OCC append-only doctrine rather
      than edited in place: a merged dod_evidence entry is immutable and its receipt pins the entry hash,
      so an in-place rewrite is rejected by validator_occ_append_only and would invalidate the receipt
      binding. The superseded item ran gh pr diff 2081 --repo OmniNode-ai/omnimarket --name-only piped
      into grep -ciE over PR #2081's own changed-path list, matched against a fixed keyword set including
      the literal substring "deploy" — the PR's diff is immutable once pushed to that exact head, so the
      check's command-position tokens are just gh + grep, neither of which is a live-surface probe (OMN-14443
      classify_check_value verdict, reproduced live: commands found ['gh', 'grep'], none in LIVE_PROBE_COMMANDS).
      It is unfalsifiable by construction — the OMN-14443 class — and a check whose exit status cannot
      depend on the state of the deployed system proves nothing about deploy scope. Replacement semantics
      — an immutable-ref CONTENT probe of the deploy-relevant source at PR #2081's current head a5bf9076a9e277e14824b0fe7cf7157b0b7bcaf9,
      permanently resolvable via GitHub's retained refs/pull/2081/head even after the source branch is
      deleted post-squash-merge, asserting two symbols the OMN-16071 fix introduces are present: the new
      OCC_AUTHOR_TIME_LABELS tuple constant in src/omnimarket/events/occ_autoauthor.py (pairing the pre-existing
      occ:machine-minted marker with the new ci:ready label so a machine-minted OCC companion's CI wave
      can actually fire), and the runtime handler's import of that constant in handler_occ_companion_effect.py
      (the exact node_occ_companion_effect handler that IS the runtime surface find_runtime_paths() flagged
      on this PR). Falsifiability was demonstrated rather than asserted: both probes repointed at PR #2081's
      base commit ca90f81d9fe6734f00b4fe6a1961ffcb93494e96 exit 1 — neither symbol exists there, since
      OCC_AUTHOR_TIME_LABELS and the handler's import of it are net-new in this PR. Executor shape — the
      first shlex token is gh, a resolvable executable on PATH, and the command contains no command substitution
      at all, so node_dod_verify's pre-execution shlex.split shape guard cannot mis-branch on it (the
      OMN-15597 caution and the OMN-15535 INVALID_CHECK_VALUE_NOT_A_COMMAND false-RED class). Each leg
      terminates in a grep -F redirected to /dev/null whose input is fully drained, which avoids both
      the SIGPIPE exit-141 hazard of grep -q and the grep -c counting idiom that fails in both polarities
      under pipefail. Mirrors the OCC #5768 precedent on contracts/OMN-15472.yaml (same defect class,
      same supersession idiom).
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: |-
          gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/events/occ_autoauthor.py?ref=a5bf9076a9e277e14824b0fe7cf7157b0b7bcaf9' --jq .content | base64 -d | grep -F 'OCC_AUTHOR_TIME_LABELS = (OCC_MACHINE_MINTED_LABEL, OCC_CI_READY_LABEL)' > /dev/null && gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_occ_companion_effect/handlers/handler_occ_companion_effect.py?ref=a5bf9076a9e277e14824b0fe7cf7157b0b7bcaf9' --jq .content | base64 -d | grep -F 'from omnimarket.events.occ_autoauthor import OCC_AUTHOR_TIME_LABELS' > /dev/null
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-2081"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6550"
    description: "Binds OMN-16071 to OCC companion PR #6550 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6550 --repo OmniNode-ai/onex_change_control --json number,state"
