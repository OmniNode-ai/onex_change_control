---
schema_version: "1.0.0"
ticket_id: "OMN-10846"
title: "Bridge capability_scores to routing policy engine via score lookup"
summary: >
  Implement build_available_models_from_scores() caller-side bridge that converts capability_scores rows
  into ModelAvailableModel instances for HandlerRoutingPolicy. Reuses existing ModelAvailableModel from
  routing policy engine without modifying the handler. Part of the canary-to-routing feedback loop (OMN-10843).
  Code delivered via PR #606 (cherry-picked from PR #604).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #606 merged to main on OmniNode-ai/omnimarket"
    command: "gh pr view 606 --repo OmniNode-ai/omnimarket --json state --jq '.state'"
  - kind: "manual"
    description: "build_available_models_from_scores() function exists in omnimarket PR #606 diff"
    command: "gh pr diff 606 -R OmniNode-ai/omnimarket | grep 'build_available_models_from_scores' | head
      -1"
  - kind: "tests"
    description: "Non-Kafka test suite passes with no regressions"
    command: "uv run pytest tests/ -m 'not kafka' --tb=short -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-merged"
    description: "Code delivered via PR #606 merged to main on OmniNode-ai/omnimarket"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 606 --repo OmniNode-ai/omnimarket --json state --jq '.state'"
  - id: "dod-bridge-function-exists"
    description: "build_available_models_from_scores() bridge function exists in PR #606 diff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 606 -R OmniNode-ai/omnimarket | grep 'build_available_models_from_scores'
          | head -1"
  - id: "dod-no-handler-modification"
    description: "HandlerRoutingPolicy is NOT modified — bridge is caller-side only"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 606 -R OmniNode-ai/omnimarket | grep 'def build_available_models_from_scores'
          | head -1"
