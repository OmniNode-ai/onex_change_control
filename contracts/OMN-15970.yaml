---
schema_version: "1.0.0"
ticket_id: "OMN-15970"
title: "Autobind OCC evidence for OMN-15970"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2733.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2733 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2733"
    description: "PR #2733 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2733 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2733 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2733 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2733 --repo OmniNode-ai/omnibase_infra --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2733"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6431"
    description: "Binds OMN-15970 to OCC companion PR #6431 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6431 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-deploy-falsifiable-probe"
    description: >-
      Falsifiable deploy probe (OMN-14443 ratchet / OMN-14505). Reads the live emit_daemon package listing
      in OmniNode-ai/omnibase_infra at the pinned, immutable fix ref, proves the read landed via a positive
      anchor that exists at both the parent and the fix ref (event_registry.py), and only then asserts
      that notification_consumer.py — the dead surface this ticket deletes — is absent. Red-derivable:
      the same value exits 1 against ?ref=dev (the parent), where the file is still present.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "body=$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/emit_daemon?ref=bfbc5d07fb9e88caab83c6e0d5e76c475169cefc'
          --jq '.[].name') && printf '%s' \"$body\" | grep -qFx 'event_registry.py' && ! printf '%s' \"$body\"
          | grep -qFx 'notification_consumer.py'"
