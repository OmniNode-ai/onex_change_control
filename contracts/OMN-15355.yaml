---
schema_version: "1.0.0"
ticket_id: "OMN-15355"
title: "Autobind OCC evidence for OMN-15355"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2571.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2571 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2571"
    description: "PR #2571 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2571 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2571 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2571"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5632"
    description: "Binds OMN-15355 to OCC companion PR #5632 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5632 --repo OmniNode-ai/onex_change_control --json number,state"
  # -------------------------------------------------------------------------
  # APPEND-ONLY SUPERSESSION AMENDMENT (2026-08-02) — evidence shape only.
  #
  # Nothing above this line is edited, deleted, or reordered; nothing below
  # weakens a substantive check. The two entries appended here re-bind the two
  # PR-bound items to the DEV-MERGED PARENT of the same work, because the
  # dod_verify live-state probe cannot read required-check greenness through a
  # feature-branch base.
  #
  # DEFECT (twice-reproduced, gate correlation ids 8870151e-03d7-442e-a9b1-
  # 3d7d66cde84d and 98ea11fa-bb5f-4970-a326-c3991f0e7110; both runs 6 total /
  # 4 verified / 2 failed): the auto-appended
  # `dod-OmniNode-ai-omnibase_infra-pr-2571::pr-live-state` and
  # `dod-occ-evidence-admissibility-validator::pr-live-state` checks both bind
  # to OmniNode-ai/omnibase_infra#2571 (the first by the OMN-15382 autobind-id
  # convention, the second by its durable receipt's `pr_number: 2571`).
  # #2571 IS merged (7228ce0c, 2026-07-30T17:33:32Z) — but into the STACKED
  # FEATURE BRANCH `jonah/omn-15418-p0-replace-raw-db_io-loading-with-typed-
  # declarations-and`, not `dev`. A feature branch carries an EMPTY
  # required-status-check set, and `_fetch_pr_checks_green` fails CLOSED on an
  # empty set, so the probe reports "no required checks reported on the
  # jonah/omn-15355-acl-proof-dsn branch" and can never clear. Same shape for
  # sibling #2568 (merged 3b03156e into the identical stacked base).
  #
  # REPAIR: bind to OmniNode-ai/omnibase_infra#2548 — the dev-based parent PR
  # whose squash merge carried the whole stack onto `dev`
  # (base `dev`, MERGED 2026-07-31T06:09:46Z, merge commit
  # 3863623e4844dc9b0db69ceb4e77942737fbca32, which `git merge-base
  # --is-ancestor` confirms is on origin/dev). #2548 has a real required set
  # (`CI Summary`, pass) so the live-state probe is non-vacuous and readable.
  #
  # CARRIAGE IS PROVEN, NOT ASSUMED — squash merges discard lineage, so
  # ancestry is the wrong oracle and content identity is used instead. The
  # exact symbol the landed #2571 receipt greps (`def _admin_dsn_for_database`
  # in scripts/ci/prove_application_database_acl.py, the "preserve ACL proof
  # credentials" fix) is present at 3863623e and at origin/dev head; #2548's
  # file set is a strict superset of #2571's and #2568's. The
  # `?ref=3863623e...` check below is the hosted, re-runnable form of that
  # readback, pinned to an immutable mainline SHA (never a deletable branch
  # head, per OMN-15540 Rule F / reference_never_pin_a_feature_branch_head).
  #
  # NOT WEAKENED: the admissibility suite
  # (`uv run pytest tests/test_evidence_admissibility.py -q`) is re-declared
  # VERBATIM by its replacement, so the family binding is preserved and no
  # substantive assertion is dropped; the hosted PG16 proof run 30566136064
  # is not referenced by this contract and is left exactly as it stands
  # elsewhere. Supersession here is non-vacuous by construction: under
  # OMN-15390 an appended marker retires its target ONLY when the appended
  # item is itself VERIFIED, so if these checks go red the original entries
  # execute and carry their own verdicts.
  #
  # AUTHORSHIP: authored by the evidence-repair lane
  # [fable-resume-0802/evrepair-15355], which is NOT the implementer of
  # OMN-15355's product work (that landed in omnibase_infra#2568/#2571/#2548).
  # Non-implementer evidence authorship per feedback_no_self_authored_evidence.
  # -------------------------------------------------------------------------
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2548"
    description: >-
      Re-binds the OMN-15355 product proof from the stacked feature-branch PR OmniNode-ai/omnibase_infra#2571
      to its dev-merged parent OmniNode-ai/omnibase_infra#2548 (base dev, merge commit 3863623e), whose
      required-status-check set is real and readable. Supersedes dod-OmniNode-ai-omnibase_infra-pr-2571.
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2571"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2548 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2548 --repo OmniNode-ai/omnibase_infra --json files"
      - check_type: "command"
        check_value: "gh pr checks 2548 --repo OmniNode-ai/omnibase_infra --required"
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/ci/prove_application_database_acl.py?ref=3863623e4844dc9b0db69ceb4e77942737fbca32
          --jq '.content' | base64 -d | grep -c 'def _admin_dsn_for_database'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2548-admiss"
    description: >-
      Re-declares the hosted OCC evidence admissibility validator (OMN-15247) verbatim, with its PR binding
      moved off the stacked-base OmniNode-ai/omnibase_infra#2571 and onto the dev-merged parent OmniNode-ai/omnibase_infra#2548.
      Supersedes dod-occ-evidence-admissibility-validator.
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-evidence-admissibility-validator"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
      - check_type: "command"
        check_value: "gh pr view 2548 --repo OmniNode-ai/omnibase_infra --json state,baseRefName,mergeCommit"
  # Self-bind for the carrier PR of this append-only amendment. The OCC merge
  # eligibility validator requires a PASS receipt binding the ticket to the OCC
  # PR that carries the evidence change (reason `pr_ticket_mismatch` otherwise);
  # occ-self-bind-pr-5632 remains true and is NOT superseded — it binds the
  # ORIGINAL autobind companion, this one binds the repair companion.
  - id: "occ-self-bind-pr-5989"
    description: "Binds OMN-15355 to OCC evidence-rebind PR #5989 (self-bind for this amendment)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5989 --repo OmniNode-ai/onex_change_control --json number,state"
