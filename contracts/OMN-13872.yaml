---
schema_version: "1.0.0"
ticket_id: "OMN-13872"
title: "chore(OMN-13872): remove premature dispatch-chain OCC receipts"
summary: >-
  Removes premature dispatch-chain OCC evidence from onex_change_control dev to close a pr_number-bound
  false-eligible surface for the held core / spi / infra release chain. The foreground regenerates fresh,
  correct OCC evidence bound to final merged heads at release-sequencing time.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR removes the premature dispatch-chain OCC contract
      and receipts, closing the false-eligible surface for the held release chain.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13872/dod-occ-self/command.yaml"
