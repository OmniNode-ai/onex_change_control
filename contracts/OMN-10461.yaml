---
schema_version: "1.0.0"
ticket_id: OMN-10461
title: "Projection Query API Server (Port 3002)"
summary: "Adds a FastAPI server on port 3002 that exposes a /query endpoint for projection-backed dashboard
  reads, replacing direct DB calls from omnidash with a typed contract surface."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: tests
    description: "18 unit tests pass covering query handler, projection read, and error paths"
    command: "cd ${OMNI_HOME:-/Users/jonah/Code/omni_home}/omni_worktrees/OMN-10461/omnimarket && env
      -u PYTHONPATH uv run pytest tests/unit/nodes/node_projection_query -q"
  - kind: ci
    description: "CodeRabbit review passed with no unresolved threads"
    command: "gh pr view 490 --repo OmniNode-ai/omnimarket --json reviews,reviewDecision"
  - kind: ci
    description: "Hostile reviewer passed — no blocking findings"
    command: "gh pr checks 490 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "18 unit tests pass covering query handler, projection read, and error paths"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10461/dod-001/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10461/dod-001/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10461/dod-001/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-002
    description: "CodeRabbit review passed with no unresolved threads"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10461/dod-002/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10461/dod-002/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10461/dod-002/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-003
    description: "Hostile reviewer passed — no blocking findings"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10461/dod-003/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10461/dod-003/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10461/dod-003/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
