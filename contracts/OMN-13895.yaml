---
schema_version: "1.0.0"
ticket_id: "OMN-13895"
title: "flip omnibase_core no-faked-boundary gate BLOCKING (pre-commit + required CI)"
summary: >-
  WS-F F-BURNDOWN step 5 (core portion). omnibase_core is clean (0 findings) under the fixed no-faked-boundary
  detector (OMN-13502, PR #1377), so its gate flips blocking: omnibase_core PR #1378 drops --report-only
  from the check-no-faked-boundary pre-commit hook and adds the required CI workflow no-faked-boundary.yml
  (job "No Faked Boundary Gate"; wedge-safe: merge_group trigger, no paths filter, fail-propagating).
  Net-new OCC evidence for OMN-13895 because the merged OMN-13502 receipts bind to the detector-fix PR
  #1377, not this CI-wiring PR, and the net-new-only OCC constraint forbids re-editing the merged OMN-13502
  contract.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-gate-blocking"
    description: >-
      omnibase_core PR #1378 flips the no-faked-boundary gate blocking: the check-no-faked-boundary pre-commit
      hook no longer carries --report-only and the required CI workflow no-faked-boundary.yml is present,
      with the detector reporting 0 findings across src + tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13895/dod-core-gate-blocking/command.yaml &&
          grep -q '^pr_number: 1378$' drift/dod_receipts/OMN-13895/dod-core-gate-blocking/command.yaml
  - id: "dod-occ-self-binding"
    description: >-
      onex_change_control PR #3534 carries the OMN-13895 contract and receipt evidence that unblocks the
      omnibase_core gate-flip PR, with a PASS receipt bound to the OCC evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13895/dod-occ-self-binding/command.yaml && grep
          -q '^pr_number: 3534$' drift/dod_receipts/OMN-13895/dod-occ-self-binding/command.yaml
