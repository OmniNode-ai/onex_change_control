---
schema_version: "1.0.0"
ticket_id: OMN-10399
title: "Retrofit ticket_pipeline CLI as the first integration"
summary: "Retrofit the ticket_pipeline market skill CLI to render typed ModelMarketCliReport output across
  text, JSON, YAML, and markdown."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Ticket pipeline CLI reporting tests pass"
    command: "uv run pytest tests/unit/cli/test_reporting.py tests/unit/nodes/node_ticket_pipeline/test_main_cli_outputs.py
      -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Ticket pipeline CLI reporting and output tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/cli/test_reporting.py tests/unit/nodes/node_ticket_pipeline/test_main_cli_outputs.py
          -q"
  - id: "dod-002"
    description: "No deploy required; ticket_pipeline output rendering is a direct local CLI integration"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for OMN-10399 - direct local CLI output integration only'"
