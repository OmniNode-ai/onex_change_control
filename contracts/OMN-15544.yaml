---
schema_version: "1.0.0"
ticket_id: "OMN-15544"
title: "OCC evidence for OMN-15544 durable omnibase_core repin"
summary: >
  OCC contract for OmniNode-ai/omnimarket PR #1985. The product PR repins omnibase_core from a deleted
  feature-branch head to the durable squash-merged dev commit with an identical tree, unwedging omnimarket's
  uv.lock pin reachability gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1985 CI checks green"
    command: "gh pr checks 1985 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1985-head-3ff393d3"
    description: >-
      PR #1985 product head 3ff393d3 repins omnibase_core to durable dev squash commit 4d7c5fba and preserves
      the lockfile reachability proof.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api 'repos/OmniNode-ai/omnimarket/contents/uv.lock?ref=3ff393d3190f37f205e60c6331b321fc49bc3f42'
          --jq '.content' | base64 -d | grep -c '4d7c5fba3')" -ge 1
      - check_type: "command"
        check_value: >-
          test "$(gh api 'repos/OmniNode-ai/omnimarket/contents/uv.lock?ref=3ff393d3190f37f205e60c6331b321fc49bc3f42'
          --jq '.content' | base64 -d | grep -c '5a907b71')" -eq 0
  - id: "dod-occ-evidence-present-check"
    description: "OCC evidence-present validator passes for the OMN-15544 contract."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/check_dod_evidence_present.py contracts/OMN-15544.yaml"
  - id: "occ-self-bind-pr-5698"
    description: "Binds OMN-15544 to OCC companion PR #5698."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/onex_change_control/pulls/5698' --jq 'select(.number == 5698 and .state
          == "open" and .head.ref == "codex-b/omn-15544-occ-companion") | .number' | grep -Fqx '5698'
