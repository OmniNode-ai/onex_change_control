---
schema_version: "1.0.0"
ticket_id: "OMN-15544"
title: "Bind the durable omnibase_core repin for omnimarket"
summary: >
  Binds omnimarket PR #1985 at its exact reviewed head and two-file dependency-pin scope so the runtime
  candidate can stage a core revision reachable from protected dev.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1985 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-market1985-core-durable-repin-head"
    description: >
      omnimarket PR #1985 replaces the deleted OMN-15392 feature-head pin with the byte-identical squash-merged
      omnibase_core dev commit at exact reviewed head 3ff393d3190f37f205e60c6331b321fc49bc3f42 and its
      complete two-file scope.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 1985 --repo OmniNode-ai/omnimarket --json headRefOid,files --jq '(.headRefOid == "3ff393d3190f37f205e60c6331b321fc49bc3f42")
          and ([.files[].path] | sort == ["pyproject.toml", "uv.lock"])' | grep -qx true

  - id: "dod-market1985-hosted-repin-supersession"
    description: >
      Hosted replacement for the private omnimarket PR #1985 exact-head and file-scope probe. The immutable
      live readback remains bound by its receipt while OCC CI exercises the hosted-accessible evidence-admissibility
      validator.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-market1985-core-durable-repin-head"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"

  - id: "occ-self-bind-pr-5697"
    description: "Binds OMN-15544 to the minimal omnimarket core-repin OCC companion PR #5697."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5697 --repo OmniNode-ai/onex_change_control --json number,state"
