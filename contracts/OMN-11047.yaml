---
schema_version: "1.0.0"
ticket_id: "OMN-11047"
title: "Fix dependency health repo labels for src roots"
summary: >
  Central OCC evidence for omnimarket PR #648. Ensures dependency health sweep labels a repo root ending
  in /src by its parent repository name instead of the literal src directory name.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused dependency health scanner tests cover /src repo label handling."
    command: "See drift/dod_receipts/OMN-11047/dod-focused-tests/command.yaml"
  - kind: "ci"
    description: "Deploy/import smoke for the sweep handler passes without live deploy or runtime restart."
    command: "See drift/dod_receipts/OMN-11047/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused dependency health scanner tests cover /src repo label handling."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '13 passed' drift/dod_receipts/OMN-11047/dod-focused-tests/command.yaml
          && echo 'OMN-11047 focused-test receipt present'"
  - id: "dod-deploy"
    description: "Deploy/import smoke for HandlerDepHealthSweep is recorded; no live deploy or runtime
      restart was performed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'HandlerDepHealthSweep' drift/dod_receipts/OMN-11047/dod-deploy/command.yaml
          && grep -q 'deploy' drift/dod_receipts/OMN-11047/dod-deploy/command.yaml && echo 'OMN-11047
          deploy receipt present'"
