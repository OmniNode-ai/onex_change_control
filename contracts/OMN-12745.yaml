---
schema_version: "1.0.0"
ticket_id: "OMN-12745"
title: "feat(demo): dashboard renders model OUTPUT (generated text) via DelegationModelOutputWidget"
summary: >-
  Central evidence that omnidash ships DelegationModelOutputWidget rendering ModelLlmInferenceResponse.generated_text
  sourced from bus topic onex.evt.omnibase-infra.inference-response.v1. Full chain: projection contract
  for response text → Query API endpoint → widget → widget added to the delegation dashboard template
  + fixtures. Widget must show real generated text in fixture mode (offline, no .201 dependency). Live
  surfacing requires the demo redeploy OMN-12746. Replaces the demo output-widget work briefly created
  at OMN-12739.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-widget-fixture-mode"
    description: "DelegationModelOutputWidget renders generated_text in fixture mode (offline, no .201
      dependency). Screenshot and/or Playwright snapshot proving the widget populates from fixture inference-response
      data."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-widget-fixture-mode/command.yaml"
  - id: "dod-query-api-endpoint"
    description: "Query API endpoint for inference response text is wired and reachable (local or .201-tunnelled).
      curl or integration test output proving the endpoint returns generated_text from the projection."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-query-api-endpoint/command.yaml"
  - id: "dod-omnidash-pr"
    description: "omnidash PR carrying DelegationModelOutputWidget implementation merges to main."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-omnidash-pr/command.yaml"
  - id: "dod-occ-pr-evidence"
    description: "OCC PR carries the OMN-12745 central contract and PASS receipts."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-occ-pr-evidence/command.yaml"
