---
schema_version: "1.0.0"
ticket_id: "OMN-12745"
title: "feat(demo): dashboard renders model OUTPUT via DelegationModelOutputWidget"
summary: >-
  Adds the DelegationModelOutputWidget to omnidash so the SEA demo dashboard can render ModelLlmInferenceResponse.generated_text
  from the delegation inference-response projection. The merged implementation includes fixture-mode rendering,
  topic registration, component registry integration, and SEA demo template wiring.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "topics"
evidence_requirements:
  - kind: "ci"
    description: "omnidash PR #177 is merged to dev with required checks passing."
    command: "gh pr view 177 --repo OmniNode-ai/omnidash --json state,mergeCommit,statusCheckRollup"
  - kind: "tests"
    description: "omnidash PR #177 reports local widget, registry, lint, typecheck, and full Vitest coverage."
    command: "gh pr view 177 --repo OmniNode-ai/omnidash --json body"
  - kind: "manual"
    description: "Central OCC evidence carries this contract and PASS receipts."
    command: "test -f contracts/OMN-12745.yaml && find drift/dod_receipts/OMN-12745 -name command.yaml
      | wc -l"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnidash-pr-177"
    description: "omnidash PR #177 is merged to dev and carries the OMN-12745 model output widget."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-omnidash-pr-177/command.yaml"
          && grep -q '^pr_number: 177$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-omnidash-pr-177/command.yaml"
          && grep -q '"state":"MERGED"' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-omnidash-pr-177/command.yaml"
          && grep -q 'DelegationModelOutputWidget' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-omnidash-pr-177/command.yaml"
  - id: "dod-omnidash-pr-177-verification"
    description: "omnidash PR #177 verification evidence reports full Vitest, ESLint, TypeScript, and
      pre-commit coverage for the model output widget change."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-omnidash-pr-177-verification/command.yaml"
          && grep -q '1198 tests pass' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-omnidash-pr-177-verification/command.yaml"
          && grep -q 'TypeScript clean' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-omnidash-pr-177-verification/command.yaml"
          && grep -q 'CodeRabbit.*pass' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-omnidash-pr-177-verification/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OMN-12745 contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12745/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-12745"$' "$CONTRACT_REPO_DIR/contracts/OMN-12745.yaml"
