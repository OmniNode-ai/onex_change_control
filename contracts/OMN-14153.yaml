---
schema_version: "1.0.0"
ticket_id: "OMN-14153"
title: "Autobind OCC evidence for OMN-14153"
summary: >
  OCC contract bound by node_pr_lifecycle_fix_effect Evidence-Source autobind (OMN-13317 F1) when OmniNode-ai/omnidash
  PR #258 carried a product-SHA Evidence-Source or was missing an OCC contract, and failed the gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #258 product diff scope present"
    command: "PRODUCT_PR_NUMBER=258; PRODUCT_REPO=OmniNode-ai/omnidash; gh pr diff ${PRODUCT_PR_NUMBER}
      --repo ${PRODUCT_REPO} --name-only | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnidash-pr-258"
    description: "PR #258 on OmniNode-ai/omnidash — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=258; PRODUCT_REPO=OmniNode-ai/omnidash; gh pr view ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnidash-pr-258-ci"
    description: "PR #258 on OmniNode-ai/omnidash — product diff scope (OMN-14425 substance check; OMN-14650:
      no longer gated on source-PR CI being green)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=258; PRODUCT_REPO=OmniNode-ai/omnidash; gh pr diff ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --name-only | grep -q ."
  - id: "occ-self-bind-pr-4294"
    description: "OCC companion PR #4294 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14153
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4294; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4295"
    description: "OCC companion PR #4295 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14153
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4295; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnidash-pr-259"
    description: "PR #259 on OmniNode-ai/omnidash — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=259; PRODUCT_REPO=OmniNode-ai/omnidash; gh pr view ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omnidash-pr-259-ci"
    description: "PR #259 on OmniNode-ai/omnidash — product diff scope (OMN-14425 substance check; OMN-14650:
      no longer gated on source-PR CI being green)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=259; PRODUCT_REPO=OmniNode-ai/omnidash; gh pr diff ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --name-only | grep -q ."
  - id: "dod-OmniNode-ai-omninode_infra-pr-603"
    description: "PR #603 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "RECEIPT=drift/dod_receipts/OMN-14153/dod-OmniNode-ai-omninode_infra-pr-603/command.yaml;
          grep -q 'pr_number: 603' ${RECEIPT} && grep -q 'commit_sha: \"4529bad25f20773b828bfbd4551e9199e495fb88\"'
          ${RECEIPT}"
  - id: "dod-OmniNode-ai-omninode_infra-pr-603-ci"
    description: "PR #603 on OmniNode-ai/omninode_infra — product diff scope (OMN-14425 substance check;
      OMN-14650: no longer gated on source-PR CI being green)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "RECEIPT=drift/dod_receipts/OMN-14153/dod-OmniNode-ai-omninode_infra-pr-603-ci/command.yaml;
          grep -q '.github/workflows/build-and-push-omnidash.yml' ${RECEIPT} && grep -q 'docker/omnidash/Dockerfile'
          ${RECEIPT} && grep -q 'k8s/onex-dev/omnidash/deployment.yaml' ${RECEIPT}"
  - id: "occ-self-bind-pr-4307"
    description: "OCC companion PR #4307 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14153
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4307; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
