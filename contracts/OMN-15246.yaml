---
schema_version: "1.0.0"
ticket_id: "OMN-15246"
title: "Autobind OCC evidence for OMN-15246"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1918."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1918 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1918"
    description: "PR #1918 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1918-ci"
    description: "PR #1918 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5130"
    description: "OCC companion PR #5130 — self-bind for OMN-15246 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1918-deploy-head-b793635c"
    description: "Falsifiable deploy probe for omnimarket PR #1918 at head b793635ce0ca96498994dd878a2931b41b4411fe.
      OMN-15246 changes runtime contract routing for node_code_embedding_effect and node_code_enrichment_effect;
      this evidence reads the product repo content at the pinned PR head and asserts both nested handler
      mappings are present and the old flat handler_class shape is absent. The probe exits from comparisons
      against GitHub content, not from reading OCC receipts or this contract."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_code_embedding_effect/contract.yaml?ref=b793635ce0ca96498994dd878a2931b41b4411fe'
          --jq .content | base64 -d | grep -c 'name: "HandlerCodeEmbeddingEffect"')" = "1" && test "$(gh
          api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_code_enrichment_effect/contract.yaml?ref=b793635ce0ca96498994dd878a2931b41b4411fe'
          --jq .content | base64 -d | grep -c 'name: "HandlerCodeEnrichmentEffect"')" = "1" && ! gh api
          'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_code_embedding_effect/contract.yaml?ref=b793635ce0ca96498994dd878a2931b41b4411fe'
          --jq .content | base64 -d | grep -q 'handler_class:' && ! gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_code_enrichment_effect/contract.yaml?ref=b793635ce0ca96498994dd878a2931b41b4411fe'
          --jq .content | base64 -d | grep -q 'handler_class:'
  - id: "occ-self-bind-pr-5138"
    description: "OCC repair PR #5138 — self-bind for OMN-15246 deploy evidence repair (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5138 --repo OmniNode-ai/onex_change_control --json number,state"
