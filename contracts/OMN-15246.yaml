---
schema_version: "1.0.0"
ticket_id: "OMN-15246"
title: "Autobind OCC evidence for OMN-15246"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1918."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1918 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1918"
    description: "PR #1918 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1918-ci"
    description: "PR #1918 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5130"
    description: "OCC companion PR #5130 — self-bind for OMN-15246 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-code-embedding-nested-handler-routing"
    description: >
      Deploy-scope DoD for PR #1918's runtime-path change to src/omnimarket/nodes/node_code_embedding_effect/contract.yaml.
      Hermetic content probe reads omnimarket product commit b793635ce0ca96498994dd878a2931b41b4411fe
      and asserts the handler_routing entry uses the nested handler mapping (name/module) that the OMN-14141
      fail-closed loader requires. Falsifiable: the same probe against omnimarket dev before this PR exits
      non-zero, because the pre-change tree declares handler_class: "HandlerCodeEmbeddingEffect" with
      no nested handler name key. Proves the source artifact that must be deployed; reads no OCC receipt
      or contract this PR authors, and mutates no live lane.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_code_embedding_effect/contract.yaml?ref=b793635ce0ca96498994dd878a2931b41b4411fe'
          --jq .content | base64 -d | grep 'name: "HandlerCodeEmbeddingEffect"'
  - id: "dod-deploy-code-enrichment-nested-handler-routing"
    description: >
      Deploy-scope DoD for PR #1918's runtime-path change to src/omnimarket/nodes/node_code_enrichment_effect/contract.yaml.
      Hermetic content probe reads omnimarket product commit b793635ce0ca96498994dd878a2931b41b4411fe
      and asserts the handler_routing entry uses the nested handler mapping (name/module) that the OMN-14141
      fail-closed loader requires. Falsifiable: the same probe against omnimarket dev before this PR exits
      non-zero, because the pre-change tree declares handler_class: "HandlerCodeEnrichmentEffect" with
      no nested handler name key. Proves the source artifact that must be deployed; reads no OCC receipt
      or contract this PR authors, and mutates no live lane.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_code_enrichment_effect/contract.yaml?ref=b793635ce0ca96498994dd878a2931b41b4411fe'
          --jq .content | base64 -d | grep 'name: "HandlerCodeEnrichmentEffect"'
  - id: "occ-self-bind-pr-5139"
    description: "OCC repair PR #5139 — self-bind for the OMN-15246 deploy-evidence append."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5139 --repo OmniNode-ai/onex_change_control --json number,state"
