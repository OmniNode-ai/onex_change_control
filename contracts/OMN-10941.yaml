---
schema_version: "1.0.0"
ticket_id: "OMN-10941"
title: "test(OMN-10941): add regression tests proving max_tokens propagates unchanged through delegation
  chain"
summary: >-
  Adds 22 parametrized regression tests proving ModelDelegationRequest.max_tokens passes through all delegation
  pipeline layers unchanged — ModelInferenceIntent, ModelLlmAdapterRequest, ModelLlmInferenceRequest —
  with no hardcoded cap at 512 or any other value.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omnibase_infra PR #1587 regression tests pass."
    command: "gh pr view 1587 --repo OmniNode-ai/omnibase_infra --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1587"
    description: "omnibase_infra PR #1587 carries the OMN-10941 max_tokens regression tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10941/dod-omnibase-infra-pr-1587/command.yaml
          && grep -q '^pr_number: 1587$' drift/dod_receipts/OMN-10941/dod-omnibase-infra-pr-1587/command.yaml"
  - id: "dod-occ-pr-999"
    description: "onex_change_control PR #999 carries the OMN-10941 contract and downstream receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10941/dod-occ-pr-999/command.yaml
          && grep -q '^pr_number: 999$' drift/dod_receipts/OMN-10941/dod-occ-pr-999/command.yaml"
