---
schema_version: "1.0.0"
ticket_id: "OMN-12213"
title: "Create dep cascade dedup orchestrator contract and handler stub"
summary: >
  Central OCC evidence for omnimarket PR #907. The change adds the node_dep_cascade_dedup_orchestrator
  contract, typed request/result models, handler stub, thin skill metadata, and golden-chain coverage
  for the post-release dependency cascade dedup workflow. The node is explicitly marked node_not_implemented,
  so this slice establishes contract surfaces only and does not require a live runtime deploy before merge.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Golden-chain test for node_dep_cascade_dedup_orchestrator passes on the omnimarket PR
      head."
    command: "See drift/dod_receipts/OMN-12213/dod-golden-chain/command.yaml"
  - kind: "tests"
    description: "Swarm dispatch regression tests pass after removing None-return assignments."
    command: "See drift/dod_receipts/OMN-12213/dod-swarm-regression/command.yaml"
  - kind: "ci"
    description: "Handler wiring scanner passes after declaring HandlerCreateFollowupTicketsEffect in
      contract metadata."
    command: "See drift/dod_receipts/OMN-12213/dod-handler-wiring/command.yaml"
  - kind: "ci"
    description: "Targeted mypy covers the CI-named typecheck failures."
    command: "See drift/dod_receipts/OMN-12213/dod-targeted-mypy/command.yaml"
  - kind: "ci"
    description: "Targeted ruff and YAML parsing checks pass for changed files."
    command: "See drift/dod_receipts/OMN-12213/dod-targeted-lint/command.yaml"
  - kind: "tests"
    description: "Split-gate regressions pass for cross-node reach-ins, dashboard runtime profile, and
      PR lifecycle live-adapter wiring."
    command: "See drift/dod_receipts/OMN-12213/dod-split-gates/command.yaml"
  - kind: "ci"
    description: "Deploy assessment confirms this contract + handler-stub slice needs no live runtime
      deploy before merge."
    command: "See drift/dod_receipts/OMN-12213/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain"
    description: "Golden-chain test for node_dep_cascade_dedup_orchestrator passed on omnimarket PR #907
      head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12213/dod-golden-chain/command.yaml
          && grep -q '16 passed' drift/dod_receipts/OMN-12213/dod-golden-chain/command.yaml"
  - id: "dod-swarm-regression"
    description: "Swarm dispatch regression tests passed after removing assignments from a None-returning
      coroutine."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12213/dod-swarm-regression/command.yaml
          && grep -q '29 passed' drift/dod_receipts/OMN-12213/dod-swarm-regression/command.yaml"
  - id: "dod-handler-wiring"
    description: "Unimported-handler scanner passed after contract-declaring HandlerCreateFollowupTicketsEffect."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12213/dod-handler-wiring/command.yaml
          && grep -q 'all Handler' drift/dod_receipts/OMN-12213/dod-handler-wiring/command.yaml"
  - id: "dod-targeted-mypy"
    description: "Targeted mypy passed for the source and tests named by the failing CI typecheck."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12213/dod-targeted-mypy/command.yaml
          && grep -q 'Success: no issues found' drift/dod_receipts/OMN-12213/dod-targeted-mypy/command.yaml"
  - id: "dod-targeted-lint"
    description: "Targeted ruff and YAML parsing checks passed for changed files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12213/dod-targeted-lint/command.yaml
          && grep -q 'All checks passed' drift/dod_receipts/OMN-12213/dod-targeted-lint/command.yaml &&
          grep -q 'yaml ok' drift/dod_receipts/OMN-12213/dod-targeted-lint/command.yaml"
  - id: "dod-split-gates"
    description: "Focused reproductions for the failed split-test gates passed on the latest omnimarket
      PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12213/dod-split-gates/command.yaml
          && grep -q '7 passed' drift/dod_receipts/OMN-12213/dod-split-gates/command.yaml && grep -q '36
          passed' drift/dod_receipts/OMN-12213/dod-split-gates/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy or runtime restart is required before merge for this contract + handler-stub
      slice."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12213/dod-deploy-assessment/command.yaml
          && grep -q 'no live deploy or runtime restart required before merge' drift/dod_receipts/OMN-12213/dod-deploy-assessment/command.yaml"
