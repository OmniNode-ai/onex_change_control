---
schema_version: "1.0.0"
ticket_id: "OMN-10696"
title: "Build ADR draft generation compute node"
summary: >
  Pure deterministic COMPUTE node that renders ADR markdown from ModelDecisionExtraction with no LLM calls.
  Same input always produces identical output — required for extraction quality measurement in the ADR
  canary pipeline.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "22 unit tests pass covering all ADR sections and determinism invariants."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10696/dod-unit-tests/command.yaml\""
  - id: "dod-deploy"
    description: "node_adr_draft_generation_compute is purity=pure COMPUTE type — no runtime deploy required."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10696/dod-deploy/command.yaml\""
  - id: "dod-pr-559-head"
    description: "omnimarket PR #559 passes all CI checks."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10696/dod-pr-559-head/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10696 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10696/dod-occ-pr/command.yaml\""
