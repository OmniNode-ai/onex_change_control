---
schema_version: "1.0.0"
ticket_id: "OMN-14828"
title: "Autobind OCC evidence for OMN-14828"
summary: "OCC Evidence-Source companion for omnibase_infra PR #2362."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2362 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2362"
    description: "PR #2362 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2362-ci"
    description: "PR #2362 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-omnibase-infra-pr-2362-deploy-scope"
    description: >-
      Deploy-gate scope for omnibase_infra PR #2362. The change touches src/omnibase_infra/nodes/node_rsd_score_compute/handlers/handler_rsd_score_calculate.py,
      a runtime node handler path, but only flips the existing RSD score compute handler to the canonical
      typed def-B dispatch shape with equivalence receipts. No live runtime deployment is performed by
      this PR; the required deploy evidence is the merge-time CI/OCC gate suite plus post-merge availability
      of the handler path on dev.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          printf '%s\n' 'deploy scope assessed for omnibase_infra#2362 runtime RSD score compute handler
          path; no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'
