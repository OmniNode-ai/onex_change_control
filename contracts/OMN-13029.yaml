---
schema_version: "1.0.0"
ticket_id: "OMN-13029"
title: "A-11 defect-anchor gate for evidence docs"
summary: >-
  Retro A-11 hardening: omniclaude PR #1812 adds a pre-commit hook and CI gate that reject docs/evidence
  markdown files containing BLOCKER, UNFIXED, DEFECT, REGRESSION, or BUG-CONFIRMED markers unless the
  file includes an OMN ticket reference or an explicit file-level defect-anchor escape hatch. This prevents
  diagnosis documents from stranding unresolved defects without a queue identity, owner, or close signal.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-defect-anchor-hook"
    description: "The defect-anchor pre-commit hook exists and carries the OMN-13029 enforcement marker."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13029/dod-defect-anchor-hook/command.yaml"
  - id: "dod-defect-anchor-registration"
    description: "The defect-anchor hook is registered in pre-commit configuration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13029/dod-defect-anchor-registration/command.yaml"
  - id: "dod-defect-anchor-ci"
    description: "The Defect Anchor Gate workflow exists and runs the same evidence-doc anchor policy."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13029/dod-defect-anchor-ci/command.yaml"
  - id: "dod-defect-anchor-tests"
    description: "Focused tests cover rejecting unanchored defect markers and allowed cases."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13029/dod-defect-anchor-tests/command.yaml"
  - id: "dod-pr-contract"
    description: "Repo-local omniclaude PR contract exists for OMN-13029."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13029/dod-pr-contract/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding OCC receipt for the OMN-13029 central evidence PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13029/dod-occ-self/command.yaml"
