---
schema_version: "1.0.0"
ticket_id: OMN-10711
title: "Declare storage/vector config in contract.yaml"
summary: >
  Adds env_dependencies sections to four omnimarket nodes whose handlers read storage/vector connection
  env vars without contract.yaml declaration: node_agent_coordinator_orchestrator (VALKEY_HOST), node_baseline_capture
  (OMNIBASE_INFRA_DB_URL, POSTGRES_HOST/PORT, VALKEY_HOST/PORT, QDRANT_HOST/PORT), node_navigation_history_reducer
  (QDRANT_HOST/PORT), node_projection_llm_cost (POSTGRES_HOST/PORT). Also adds missing handler block to
  node_navigation_history_reducer to repair node-drift-gate strict violation (pre-existing, now fixed
  on touch). Contract declarations only — no Python code changed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-unit-tests
    description: "Full omnimarket unit test suite passes (2271 tests)."
    source: generated
    checks:
      - check_type: command
        check_value: "uv run pytest tests/ -m unit -q"
  - id: dod-occ-pr
    description: "Self-binding receipt for this OCC PR."
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view --repo OmniNode-ai/onex_change_control --json state -q .state"
