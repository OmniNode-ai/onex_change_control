---
schema_version: "1.0.0"
ticket_id: "OMN-13999"
title: "ci(omnibase_core): run check-check-names validator on bare python3 (drop needless uv sync)"
summary: >-
  WS-F Track 1 runner-load reduction (parent OMN-13932). omnibase_core PR #1391 removes the Install-uv
  + uv-sync-all-extras setup from the check_name Validator gate (check-check-names.yml) and runs scripts/validation/validate-check-names.py
  on the bare interpreter. The validator is stdlib-only (argparse/re/sys/pathlib/typing; line-based required-checks.yaml
  parse, no yaml module), so the dep-sync was pure overhead that, under runner-fleet saturation, blew
  the job timeout and surfaced as a false red. Invocation change only; gate coverage is unchanged.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1391"
    description: >-
      Consuming-PR binding: omnibase_core PR #1391 drops the uv sync / Install uv steps from check-check-names.yml
      and runs the stdlib-only validator on bare python3, at head 4b314adf34ca8ec46de004a251f620d3ff8864a4.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13999/dod-omnibase_core-pr-1391/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13999 contract and receipt consumed
      by omnibase_core PR #1391.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13999/dod-occ-self/command.yaml"
