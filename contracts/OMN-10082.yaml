---
schema_version: "1.0.0"
ticket_id: "OMN-10082"
title: Add typed request-response models and health for local runtime ingress
summary: "Add typed request-response models and health for local runtime ingress"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "envelopes"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused omnibase_infra model, validation, and health tests for runtime ingress pass
      locally"
    command: "cd ../omnibase_infra && uv run pytest tests/unit/runtime tests/unit/services tests/integration
      -k 'runtime_ingress or local_ingress or service_health' -q"
  - kind: "ci"
    description: "omnibase_infra PR checks pass for the typed ingress protocol change"
    command: "gh pr checks --repo OmniNode-ai/omnibase_infra --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Typed request-response models validate node_name, payload, timeout_ms, and correlation_id
      deterministically"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10082/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "Ingress-specific health and attachment state is visible in runtime diagnostics"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10082/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "Validation failure, timeout, and startup-order regression proof is captured canonically"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10082/dod-003 -type f -name '*.yaml' | grep -q ."
