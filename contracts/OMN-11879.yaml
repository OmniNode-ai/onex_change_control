---
schema_version: "1.0.0"
ticket_id: "OMN-11879"
title: "Add contract_version field to all 100 omnibase_infra contracts"
summary: >
  Adds the required contract_version field (major/minor/patch semver) to all 100 contract.yaml files in
  omnibase_infra that were missing it. Mechanical schema-only change; no behavior change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "ONEX Validators CI job passes — all contracts validate against ModelYamlContract"
    command: "uv run python scripts/validate.py all --verbose"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1746"
    description: >
      omnibase_infra PR #1746 adds contract_version to all 100 contracts. No deploy action required: this
      is a YAML metadata-only change with no runtime behavior change. The deploy gate is satisfied because
      contract validators confirm correct field naming post-change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "echo 'no live deploy required: schema-only field addition, contract validators pass'"
  - id: "dod-contract-version-coverage"
    description: "All contracts have contract_version field; no version field present."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11879/dod-contract-version-coverage/command.yaml\""
