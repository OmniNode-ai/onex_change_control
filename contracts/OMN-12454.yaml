---
schema_version: "1.0.0"
ticket_id: OMN-12454
title: "feat(OMN-12454): add gitignore-baseline-hook to propagation-targets.yaml (all repos)"
summary: >
  Adds a new propagation entry to .github/propagation-targets.yaml in omnibase_core so the
  validate-gitignore-baseline pre-commit hook (defined in OMN-12453) fans out to all 12 repos
  via scripts/propagate-config.sh on release. Targets: omnibase_compat, omnibase_spi,
  omnibase_infra, omniclaude, omnidash, omniintelligence, omnimarket, omnimemory,
  omninode_infra, onex_change_control, omnigemini, omnibase. Final ticket in OMN-12450 epic.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "13/13 propagation-related tests pass in omnibase_core PR."
    command: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12454/dod-001/command.yaml\""
  - kind: "manual"
    description: "YAML parses correctly and new entry matches schema of existing normalization-symmetry-hook."
    command: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12454/dod-002/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "13 propagation-related tests pass (test_propagate_config.py + test_completion_verify_targets.py)
      in omnibase_core PR for OMN-12454."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12454/dod-001/command.yaml\" &&
          grep -q '^pr_number: 1185$' \"drift/dod_receipts/OMN-12454/dod-001/command.yaml\""
  - id: "dod-002"
    description: "YAML parse + schema validation confirms new gitignore-baseline-hook entry
      matches existing normalization-symmetry-hook shape exactly."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12454/dod-002/command.yaml\" &&
          grep -q '^pr_number: 1185$' \"drift/dod_receipts/OMN-12454/dod-002/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-12454 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12454/dod-occ-pr/command.yaml\"
          && grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12454/dod-occ-pr/command.yaml\""
