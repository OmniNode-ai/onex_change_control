---
schema_version: "1.0.0"
ticket_id: "OMN-10734"
title: "Fix 5 silent fallback violations in omniclaude and wire no-env-fallbacks gate"
summary: >
  Removes localhost defaults from context_config.py (memory_fabric_url and session_projector_url), replacing
  Field(default="http://localhost:8085/...") with Field(default="") plus fail-fast model_validator guards.
  Wires validate_no_env_fallbacks.py as a pre-commit hook and CI gate (arch-no-env-fallbacks job) so the
  pattern cannot regress. Part of OMN-10658 fallback enforcement sweep across 11 repos.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1552-ci-green"
    description: "omniclaude PR #1552 all CI checks green (minus Receipt Gate which requires this OCC
      PR)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10734/dod-pr-1552-ci-green/command.yaml\""
  - id: "dod-validator-pass"
    description: "validate_no_env_fallbacks.py passes clean on omniclaude src/ after the two context_config.py
      fixes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10734/dod-validator-pass/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR (jonah/omn-10734-occ-contract) open against main with contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10734/dod-occ-pr/command.yaml\""
