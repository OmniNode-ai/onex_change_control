---
schema_version: "1.0.0"
ticket_id: "OMN-11937"
title: "Omniclaude LLM routing migration"
summary: >
  Central OCC evidence for removing hardcoded served-model defaults from Omniclaude routing, local verification,
  Bifrost delegation, transition selection, and hook hint surfaces. Missing model routing configuration
  must fail explicitly rather than falling back to hidden local defaults.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused Omniclaude routing/config test suite passed for the model-routing migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11937/dod-focused-tests/command.yaml
          && grep -q '211 passed' drift/dod_receipts/OMN-11937/dod-focused-tests/command.yaml"
  - id: "dod-validate-pr-contracts"
    description: "Omniclaude PR contract presence/completeness validation passes after adding local OMN-11917
      and OMN-11937 contracts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11937/dod-validate-pr-contracts/command.yaml
          && grep -q 'PASS: All contract checks passed' drift/dod_receipts/OMN-11937/dod-validate-pr-contracts/command.yaml"
  - id: "dod-omniclaude-pr-1646"
    description: "Omniclaude PR #1646 is open with the LLM routing migration branch head bound to OCC
      evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11937/dod-omniclaude-pr-1646/command.yaml
          && grep -q '^pr_number: 1646$' drift/dod_receipts/OMN-11937/dod-omniclaude-pr-1646/command.yaml"
  - id: "dod-env-registry"
    description: "Transition selector served-model environment config is classified as overlay-supplied
      instead of defaulted in .env.example."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11937/dod-env-registry/command.yaml
          && grep -q 'Unregistered: 0' drift/dod_receipts/OMN-11937/dod-env-registry/command.yaml"
  - id: "dod-deploy"
    description: "Deploy-gate proof surface is recorded for runtime-touching Omniclaude routing/config
      changes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11937/dod-deploy/command.yaml &&
          grep -q 'deploy verification' drift/dod_receipts/OMN-11937/dod-deploy/command.yaml"
