---
schema_version: "1.0.0"
ticket_id: "OMN-14189"
title: "refactor(OMN-14189): OCC autobind authors stamps through canonical renderer"
summary: >-
  omnimarket PR #1665 routes OCC Evidence-Source and Evidence-Ticket authoring in node_pr_lifecycle_fix_effect
  through the shared omnibase_compat pr_occ_stamp schema/parser/renderer. The change adds a pure occ_stamp_authoring
  helper seam, removes inline Evidence-Source regex/rendering paths from the OCC autobind and contract
  adapters, switches companion/product PR body writes and read-back through the shared seam, declares
  the omnibase_compat dependency, and covers render/parse/idempotency plus adapter round-trip behavior
  with focused tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1665"
    description: >-
      Live GitHub verification shows omnimarket PR #1665 targets dev from jonah/omn-14189-autobind-renderer
      at head 0ea164c0814bfe9b96013a6fcc5b086d97acc070. The diff is scoped to the PR lifecycle fix-effect
      OCC stamp authoring seam, OCC autobind/contract/verifier adapters, metadata dependency declaration,
      pyproject/lock updates for omnibase-compat 0.5.6, and focused unit tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14189/dod-omnimarket-pr-1665/command.yaml &&
          grep -q '^pr_number: 1665$' drift/dod_receipts/OMN-14189/dod-omnimarket-pr-1665/command.yaml
          && grep -q '^commit_sha: "0ea164c0814bfe9b96013a6fcc5b086d97acc070"$' drift/dod_receipts/OMN-14189/dod-omnimarket-pr-1665/command.yaml
  - id: "dod-local-verification"
    description: >-
      Independent local verification in the clean OMN-14189 omnimarket worktree passed the focused OCC
      stamp authoring/autobind tests, a broader fix-effect node plus golden-chain suite, Ruff formatting
      and lint checks over touched files, metadata.yaml parse/dependency assertion, and mypy over the
      touched source files.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14189/dod-local-verification/command.yaml &&
          grep -q '258 passed' drift/dod_receipts/OMN-14189/dod-local-verification/command.yaml
  - id: "dod-compat-056-resolution"
    description: >-
      The upstream omnibase_compat 0.5.6 release required by this PR is visible in uv.lock from PyPI,
      and the OMN-14189 worktree environment was synced with distribution metadata reporting omnibase-compat
      0.5.6 before running verification. The package module's legacy __version__ still reports 0.5.2,
      so durable proof uses importlib.metadata.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14189/dod-compat-056-resolution/command.yaml
          && grep -q 'metadata-version 0.5.6' drift/dod_receipts/OMN-14189/dod-compat-056-resolution/command.yaml
  - id: "dod-occ-companion-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14189 contract and
      PASS receipts consumed by omnimarket PR #1665 through its Evidence-Source line. The receipt binds
      the OCC companion by PR number after the PR is opened for occ-preflight validation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14189/dod-occ-companion-self/command.yaml
  - id: "dod-current-head-rebase-1665"
    description: >-
      After OMN-14191 merged, omnimarket PR #1665 was rebased onto current dev to resolve a pyproject.toml
      conflict. The new product head is cc6e1e6341ea267713140cdc51b8ac74c0300e52. The rebase retained
      OMN-14189's OCC stamp authoring changes, dropped the uv.lock regen commit because the compat 0.5.6
      lock content was already upstream, and focused tests plus Ruff passed on the rebased head.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14189/dod-current-head-rebase-1665/command.yaml
          && grep -q '^pr_number: 1665$' drift/dod_receipts/OMN-14189/dod-current-head-rebase-1665/command.yaml
          && grep -q '^commit_sha: "cc6e1e6341ea267713140cdc51b8ac74c0300e52"$' drift/dod_receipts/OMN-14189/dod-current-head-rebase-1665/command.yaml
  - id: "dod-occ-current-head-refresh"
    description: >-
      OCC companion refresh self-binding: this follow-up onex_change_control PR appends current-head evidence
      for OMN-14189 after the #1665 rebase. It leaves the merged #3781 receipts immutable and binds this
      refresh companion by PR number for occ-preflight validation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14189/dod-occ-current-head-refresh/command.yaml
