---
schema_version: "1.0.0"
ticket_id: OMN-9556
title: Resolve subscribe-capable runtime event bus from the service container
summary: "Resolve subscribe-capable runtime event bus from the service container"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - public_api
evidence_requirements:
  - kind: tests
    description: "Focused unit and integration tests prove subscriber bus registration and container resolution."
    command: "uv run pytest tests/integration/runtime/test_event_bus_subscriber_container_resolution.py
      tests/unit/runtime/test_event_bus_subscriber_container_resolution.py -q"
  - kind: ci
    description: "omnibase_infra PR #1391 carries deploy-gate contract and CI evidence for OMN-9556."
    command: "gh pr checks 1391 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused subscriber-container unit and integration test slice passed locally."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9556/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-9556$' drift/dod_receipts/OMN-9556/dod-001/command.yaml && grep -q '^evidence_item_id:
          dod-001$' drift/dod_receipts/OMN-9556/dod-001/command.yaml && grep -q '11 passed in 0.34s' drift/dod_receipts/OMN-9556/dod-001/command.yaml"
  - id: dod-002
    description: "PR #1391 includes per-repo deploy evidence and remains gated by normal CI/receipt checks."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9556/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-9556$' drift/dod_receipts/OMN-9556/dod-002/command.yaml && grep -q '^evidence_item_id:
          dod-002$' drift/dod_receipts/OMN-9556/dod-002/command.yaml && grep -q '8919138b73583baed4c014ca90067e3af11d7e35'
          drift/dod_receipts/OMN-9556/dod-002/command.yaml"
