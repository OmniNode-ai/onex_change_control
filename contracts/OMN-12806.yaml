---
schema_version: "1.0.0"
ticket_id: "OMN-12806"
title: "feat(OMN-12806): external-service URLs to contract authority"
summary: >-
  Moves GitHub and Linear API base URL literals in omnimarket source modules to src/omnimarket/configs/service_endpoints.yaml,
  loaded by omnimarket.config.service_endpoints. The rebuilt product branch is based on current dev and
  carries only the service-endpoint authority change set. #magic___^_^___line
is_seam_ticket: false

interface_change: false
interfaces_touched: []

evidence_requirements:
  - kind: tests
    description: >-
      Focused service endpoint contract tests pass, covering canonical URL values, fail-closed loader
      behavior, and zero unapproved source literals.
    command: "uv run pytest tests/test_service_endpoints_contract.py -q"
  - kind: ci
    description: "Ruff and strict mypy pass on the touched Python files."
    command: "uv run ruff check <touched-files> && uv run mypy <touched-files> --strict"
  - kind: ci
    description: "Pre-commit passes on the rebuilt product change set."
    command: "OMNIBASE_INFRA_PATH=$OMNI_HOME/omnibase_infra uv run pre-commit run --files <touched-files>"

emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""

dod_evidence:
  - id: dod-001
    description: >-
      configs/service_endpoints.yaml exists and defines github.rest_url, github.graphql_url, and linear.graphql_url.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12806/dod-001/command.yaml"
  - id: dod-002
    description: >-
      service_endpoints.py loader exposes GITHUB_REST_URL, GITHUB_GRAPHQL_URL, and LINEAR_GRAPHQL_URL.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12806/dod-002/command.yaml"
  - id: dod-003
    description: >-
      test_no_bare_hardcoded_external_service_urls passes on the rebuilt product branch.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12806/dod-003/command.yaml"
  - id: dod-004
    description: "Focused unit tests, ruff, and strict mypy pass on touched files."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12806/dod-004/command.yaml"
  - id: dod-005
    description: "Pre-commit passes on the rebuilt product change set."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12806/dod-005/command.yaml"
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-12806 contract and receipts
      consumed by omnimarket PR #1483.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12806/dod-occ-self/command.yaml"
