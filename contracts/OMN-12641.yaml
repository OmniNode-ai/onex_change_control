---
schema_version: "1.0.0"
ticket_id: "OMN-12641"
title: "SEA D2: run_agent must read scaffold contract_passed, not the register_generated_tool registered
  flag"
summary: >-
  run_agent derives contract_passed solely from scaffold validation result. Registration is downstream
  and gated; it is never the source of truth for contract validity. Authority rule centralized in contract_authority.py;
  regression test asserts registration alone never flips contract_passed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-registration-not-authoritative"
    description: >-
      registration flag never sets contract_passed=True. Authority centralized in contract_authority.py.
      Regression test in test_main.py asserts registration alone never flips contract_passed (fails closed).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12640/dod-registration-not-authoritative/command.yaml"
  - id: "dod-pytest-green"
    description: >-
      Full test suite passes with no -k filter: uv run pytest tests/ -v → 1353 passed, 15 skipped. D2
      authority rule enforced; no registration→contract_passed path survives.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12640/dod-pytest-green/command.yaml"
  - id: "dod-mypy-clean"
    description: >-
      uv run mypy src/ --strict: Success, no issues in 200 files. contract_authority.py helpers are fully
      typed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12640/dod-mypy-clean/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-12641 contract and shared receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12640/dod-occ-pr-self/command.yaml"
