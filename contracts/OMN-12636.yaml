---
schema_version: "1.0.0"
ticket_id: "OMN-12636"
title: "Base dirty-canonical rescue worktree on canonical HEAD and expand untracked dirs"
summary: >
  OCC evidence for omnimarket PR #1023 (merge commit 03f08fd1). The change fixes the dirty-canonical sweep
  so the rescue worktree is based on the canonical clone HEAD rather than a detached ref, and expands
  untracked directories so nested dirty files are shipped. PR #1023 merged to omnimarket dev with green
  CI.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1023"
    description: "omnimarket#1023 carries the OMN-12636 dirty-canonical rescue-worktree fix and merged
      to dev at merge commit 03f08fd1 with green CI."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12636/dod-omnimarket-pr-1023/command.yaml"
  - id: "dod-occ-pr-2132"
    description: "onex_change_control PR #2132 binds the OMN-12636 dod evidence onto the OCC governance
      ref."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12636/dod-occ-pr-2132/command.yaml
          && grep -q '^pr_number: 2132$' drift/dod_receipts/OMN-12636/dod-occ-pr-2132/command.yaml"
