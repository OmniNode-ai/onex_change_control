---
schema_version: "1.0.0"
ticket_id: "OMN-12489"
title: "Read delegation events through the projection"
summary: >
  Bind central OCC evidence for omnimarket PR #1005, which exposes the live
  delegation projection read path through contract-discovered projection API
  topics backed by bus-fed delegation_events and savings_estimates projections.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
  - protocols
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1005"
    description: "Bind OMN-12489 evidence to omnimarket PR #1005."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12489/dod-omnimarket-pr-1005/command.yaml
          && grep -q '^pr_number: 1005$' drift/dod_receipts/OMN-12489/dod-omnimarket-pr-1005/command.yaml"
  - id: "dod-focused-validation"
    description: "Focused ruff, pytest, projection API, and delegation golden-chain validation passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12489/dod-focused-validation/command.yaml
          && grep -q '158 passed' drift/dod_receipts/OMN-12489/dod-focused-validation/command.yaml"
  - id: "dod-bus-audit-compile"
    description: "Bus audit compile-only preflight validated the changed projection contracts and topics."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12489/dod-bus-audit-compile/command.yaml
          && grep -q 'bus_audit_compute' drift/dod_receipts/OMN-12489/dod-bus-audit-compile/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence: projection API/read-model changes require normal post-merge rollout and DB migrations, not an ad hoc live deploy before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12489/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-12489/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr-1955"
    description: "OCC PR #1955 publishes the central OMN-12489 contract and receipt bundle."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12489/dod-occ-pr-1955/command.yaml
          && grep -q '^pr_number: 1955$' drift/dod_receipts/OMN-12489/dod-occ-pr-1955/command.yaml"
