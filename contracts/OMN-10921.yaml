---
schema_version: "1.0.0"
ticket_id: "OMN-10921"
title: "Canonical delegation runtime profile contract v1"
summary: "Adds the canonical delegation runtime profile YAML contract for the main delegation runtime
  profile, including event bus, LLM backend, security, pricing, dashboard, and datastore references."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Delegation runtime profile contract shape tests pass."
    command: "uv run pytest tests/test_delegation_runtime_profile.py -q"
  - kind: "manual"
    description: "OCC PR #983 carries the OMN-10921 contract file and self-binding evidence."
    command: "gh pr view 983 --repo OmniNode-ai/onex_change_control --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-runtime-profile-contract-tests"
    description: "The delegation runtime profile contract YAML shape tests pass for OCC PR #983."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10921/dod-runtime-profile-contract-tests/command.yaml
          && grep -q '^pr_number: 983$' drift/dod_receipts/OMN-10921/dod-runtime-profile-contract-tests/command.yaml"
  - id: "dod-occ-pr-983"
    description: "OCC PR #983 carries the canonical delegation runtime profile contract and OMN-10921
      receipt bindings."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10921/dod-occ-pr-983/command.yaml
          && grep -q '^pr_number: 983$' drift/dod_receipts/OMN-10921/dod-occ-pr-983/command.yaml"
