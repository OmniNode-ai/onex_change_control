---
schema_version: "1.0.0"
ticket_id: "OMN-13159"
title: "Bind June 15 omninode_infra dependency PR batch to OCC evidence"
summary: >
  Bind the June 15 omninode_infra Dependabot dependency maintenance PR batch to central OCC evidence so
  Receipt Gate can evaluate each PR from a pinned Evidence-Source without bypass tokens.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-499-head"
    description: "omninode_infra PR #499 carries the python-production dependency group update for docker/onex-api."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-499-head/command.yaml\""
  - id: "dod-omninode-infra-499-nonreceipt-checks"
    description: "omninode_infra PR #499 has no failing or pending non-Receipt-Gate checks at the captured
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-499-nonreceipt-checks/command.yaml\""
  - id: "dod-omninode-infra-500-head"
    description: "omninode_infra PR #500 carries the pytest dependency update for docker/onex-api."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-500-head/command.yaml\""
  - id: "dod-omninode-infra-500-nonreceipt-checks"
    description: "omninode_infra PR #500 has no failing or pending non-Receipt-Gate checks at the captured
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-500-nonreceipt-checks/command.yaml\""
  - id: "dod-omninode-infra-501-head"
    description: "omninode_infra PR #501 carries the aws/cluster Terraform provider lock update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-501-head/command.yaml\""
  - id: "dod-omninode-infra-501-nonreceipt-checks"
    description: "omninode_infra PR #501 has no failing or pending non-Receipt-Gate checks at the captured
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-501-nonreceipt-checks/command.yaml\""
  - id: "dod-omninode-infra-502-head"
    description: "omninode_infra PR #502 carries the aws/network Terraform provider lock update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-502-head/command.yaml\""
  - id: "dod-omninode-infra-502-nonreceipt-checks"
    description: "omninode_infra PR #502 has no failing or pending non-Receipt-Gate checks at the captured
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-502-nonreceipt-checks/command.yaml\""
  - id: "dod-omninode-infra-503-head"
    description: "omninode_infra PR #503 carries the claude-code-action GitHub Actions dependency update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-503-head/command.yaml\""
  - id: "dod-omninode-infra-503-nonreceipt-checks"
    description: "omninode_infra PR #503 has no failing or pending non-Receipt-Gate checks at the captured
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-503-nonreceipt-checks/command.yaml\""
  - id: "dod-omninode-infra-504-head"
    description: "omninode_infra PR #504 batches the June 15 dependency updates from superseded Dependabot
      PRs #499, #500, #501, #502, and #503 on an OMN-named branch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-504-head/command.yaml\""
  - id: "dod-omninode-infra-504-nonreceipt-checks"
    description: "omninode_infra PR #504 has no failing or pending non-Receipt-Gate checks at the captured
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-504-nonreceipt-checks/command.yaml\""
  - id: "dod-omninode-infra-506-head"
    description: "omninode_infra PR #506 carries the FastAPI 0.137.1 dependency update from superseded
      Dependabot PR #505 on an OMN-named branch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-506-head/command.yaml\""
  - id: "dod-omninode-infra-506-nonreceipt-checks"
    description: "omninode_infra PR #506 has no failing or pending non-Receipt-Gate checks at the captured
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-omninode-infra-506-nonreceipt-checks/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carries the OMN-13159 contract and receipts for the June 15 dependency
      batch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-occ-pr-self/command.yaml\""
  # ---------------------------------------------------------------------------
  # OMN-13159 (delegation complete-URL fix) — distinct effort that reuses this
  # Linear ticket id. Additive evidence for the DirectCurl verbatim-post fix +
  # base_url_env -> endpoint_url_env rename. Does NOT supersede the omninode_infra
  # Dependabot-batch bindings above; both share ticket_id OMN-13159.
  # ---------------------------------------------------------------------------
  - id: "dod-delegation-omnimarket-pr1228"
    description: "omnimarket PR #1228 deletes the rstrip+append URL construction in port_direct_curl_dispatch
      (POSTs endpoint_url verbatim, fail-closed on non-http), renames base_url_env -> endpoint_url_env
      across the bifrost delegation config + consumers, and adds test_directcurl_verbatim_url asserting
      the curl argv URL == resolved endpoint_url byte-for-byte. ruff + ruff format clean; mypy src/omnimarket
      --strict clean (2349 files); full suite 7419 passed / 74 skipped (NO -k; only kafka-marked broker
      tests fail env-side)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-delegation-omnimarket-pr1228/command.yaml"
          && grep -q '^pr_number: 1228$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-delegation-omnimarket-pr1228/command.yaml"
  - id: "dod-delegation-infra-pr1989"
    description: "omnibase_infra PR #1989 renames the bifrost render-hint field base_url_env -> endpoint_url_env
      in render_bifrost_delegation_contract (env value materialized into endpoint_url verbatim; no base+path
      concat) + updates its tests. ruff + ruff format clean; mypy --strict clean on the changed module;
      changed-area tests 25 passed; broad unit suite 20477 passed / 1 pre-existing-on-dev failure (verification
      CLI, outside this change's blast radius)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-delegation-infra-pr1989/command.yaml"
          && grep -q '^pr_number: 1989$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-delegation-infra-pr1989/command.yaml"
  - id: "dod-delegation-deploy-assessment"
    description: "Deploy assessment for the delegation complete-URL fix: no live deploy, restart, topic
      mutation, or .201 mutation by these PRs. The DirectCurl port fix takes effect on the next dev-lane
      redeploy that bakes omnimarket origin/dev HEAD; the bus runtime path was already verbatim-correct."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-delegation-deploy-assessment/command.yaml"
          && grep -q 'No live deploy' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-delegation-deploy-assessment/command.yaml"
  - id: "dod-delegation-occ-pr"
    description: "onex_change_control PR #2647 carries the dod-delegation-* contract entries + receipts
      that omnimarket PR #1228 and omnibase_infra PR #1989 resolve via Evidence-Source: OCC#2647."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-delegation-occ-pr/command.yaml"
          && grep -q '^pr_number: 2647$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13159/dod-delegation-occ-pr/command.yaml"
