---
schema_version: "1.0.0"
ticket_id: "OMN-14269"
title: "refactor(OMN-14269): canonicalize HandlerCloseoutVerifier to thin contract+handler"
summary: >-
  omnimarket product PR (branch jonah/omn-14269-lock-closeout-verifier-thin-shape, head 1c1d841a569f101dc7b9b645f66ef604a09cc486)
  adds a shape-contract test locking HandlerCloseoutVerifier (node_closeout_verifier_compute) to the thin
  contract+handler shape plus a top-level state-coverage test for its declared output states. Test-only
  change; handler already canonical. Public-safe generic architecture-hygiene work under epic OMN-14242.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      Independent landing verification at product head 1c1d841a569f101dc7b9b645f66ef604a09cc486: node
      tests, mypy --strict on the handler, and the strict state-coverage gate all pass. Runner (implementer)
      is distinct from the verifier (landing verification), satisfying the receipt-honesty gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14269/dod-001/command.yaml && grep -q '1c1d841a569f101dc7b9b645f66ef604a09cc486'
          drift/dod_receipts/OMN-14269/dod-001/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Honest deploy-surface assessment: HandlerCloseoutVerifier has no production callers today outside
      its node contract.yaml and tests. Pure in-module signature change; no live deploy required or performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14269/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-14269/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14269 contract and
      PASS receipts consumed by the product PR through its Evidence-Source line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14269/dod-occ-self/command.yaml
