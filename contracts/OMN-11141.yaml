---
schema_version: "1.0.0"
ticket_id: "OMN-11141"
title: "Task 5: onex gate CLI Commands"
summary: >
  Central OCC evidence for omnibase_core PR #1092, which adds the core-only OmniGate `onex gate` CLI surface,
  pure local commands, and dynamic delegation for effectful infra commands.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_core PR #1092 carries the OMN-11141 CLI change."
    command: "gh pr view 1092 --repo OmniNode-ai/omnibase_core --json state,headRefOid"
  - kind: "tests"
    description: "Focused CLI tests and lint pass locally."
    command: "PYTHONPATH=$PWD/src uv run pytest tests/unit/cli/test_cli_gate.py -q && PYTHONPATH=$PWD/src
      uv run ruff check src/omnibase_core/cli/cli_gate.py tests/unit/cli/test_cli_gate.py"
  - kind: "manual"
    description: "No runtime deploy is required for CLI registration changes."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11141/dod-deploy-assessment/command.yaml"
  - kind: "manual"
    description: "OCC PR #1083 carries the central OMN-11141 contract and receipts."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11141/dod-occ-pr-1083/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1092"
    description: "Bind OMN-11141 evidence to omnibase_core PR #1092 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11141/dod-omnibase-core-pr-1092/command.yaml\"
          && grep -q '^pr_number: 1092$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11141/dod-omnibase-core-pr-1092/command.yaml\"
          && grep -q '^commit_sha: f9d7af4d6ec7b708349d607a767b3499adad6d13$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11141/dod-omnibase-core-pr-1092/command.yaml\""
  - id: "dod-focused-checks"
    description: "Focused CLI tests and lint pass locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11141/dod-focused-checks/command.yaml\"
          && grep -q '9 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11141/dod-focused-checks/command.yaml\"
          && grep -q 'All checks passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11141/dod-focused-checks/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "No live deploy, runtime restart, .201 mutation, or event-bus write was performed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11141/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11141/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-1083"
    description: "Bind OMN-11141 central evidence to OCC PR #1083."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11141/dod-occ-pr-1083/command.yaml\"
          && grep -q '^pr_number: 1083$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11141/dod-occ-pr-1083/command.yaml\""
