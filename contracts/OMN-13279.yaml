---
schema_version: "1.0.0"
ticket_id: "OMN-13279"
title: "Remove superseded Generic Validator Node mechanism (NodeValidator ABC + ModelValidatorDescriptor
  + registry_validator)"
summary: >-
  Removes the stalled OMN-2362 partial build (NodeValidator ABC, ValidatorRegistry, ModelValidatorDescriptor,
  ModelValidationRequest) from omnibase_core. This mechanism predates the canonical 3-primitives architecture
  (OMN-12525), was last touched 2026-03-01, and is wired into nothing live. A bespoke validator base class
  + descriptor + private registry is exactly what the canonical architecture forbids; validators are COMPUTE
  handlers on ProtocolMessageHandler. The canonical ValidationReport semantics model is retained for adoption
  by the follow-up validation workflow (OMN-13280). No backward-compat shim is left behind.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1266"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1266 (jonah/omn-13279-remove-generic-validator-node,
      base=dev). The PR deletes node_validator.py, registry_validator.py, model_validator_descriptor.py,
      model_validation_request.py, and the OMN-2550 test, keeping the canonical ValidationReport semantics.
      This receipt binds OMN-13279 to omnibase_core PR #1266 by pr_number and commit_sha so downstream
      Receipt Gate can resolve central OCC evidence for the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13279/dod-omnibase_core-pr-1266/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13279 contract and omnibase_core
      PR #1266 binding. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check
      can resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13279/dod-occ-pr-self/command.yaml
  - id: "dod-deploy-no-runtime-impact"
    description: >-
      Deploy evidence for OMN-13279. The deleted files (node_validator.py, registry_validator.py, model_validator_descriptor.py,
      model_validation_request.py) are dead, unwired code: zero live importers and no runtime/entry-point
      registration. Deleting them is a no-op for the deployed runtime, so no image rebuild or redeploy
      is required. The deploy-gate path matcher fires on src/*/nodes/*.py for the deletion of node_validator.py;
      this receipt records that no deploy action is needed and proves zero live wiring.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13279/dod-deploy-no-runtime-impact/command.yaml
          # deploy: dead unwired code; no runtime deploy required
