---
schema_version: "1.0.0"
ticket_id: "OMN-10951"
title: "Deploy Kafka-to-Postgres delegation consumer on .201"
summary: "Add omnimarket-projection-delegation to the runtime-observability-projections bundle and deploy
  the Kafka-to-Postgres consumer for delegation events."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR passes CI with updated bundles.yaml"
    command: "gh pr checks 1593 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1593"
    description: "omnibase_infra PR #1593 adds omnimarket-projection-delegation to runtime-observability-projections
      bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10951/dod-infra-pr-1593/command.yaml
          && grep -q '^pr_number: 1593$' drift/dod_receipts/OMN-10951/dod-infra-pr-1593/command.yaml"
  - id: "dod-catalog-validation"
    description: "Catalog validation passes for the bundle including the new service."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10951/dod-catalog-validation/command.yaml"
