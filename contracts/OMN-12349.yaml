---
schema_version: "1.0.0"
ticket_id: "OMN-12349"
title: "Implement runtime handler for node_pr_watch_orchestrator"
summary: >
  Central OCC evidence for omnimarket PR #961. The change replaces the node_pr_watch_orchestrator runtime
  stub with a native Onex handler that consumes typed repo/PR inputs, polls gh pr checks through a typed
  effect boundary, and emits contract-declared pr-watch-completed or pr-watch-failed terminal events for
  green, red, and timeout outcomes.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "protocols"
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-runtime-handler"
    description: "PR watch handler implements typed CI polling loop and removes node_not_implemented."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12349/dod-runtime-handler/command.yaml\""
  - id: "dod-focused-tests"
    description: "Focused golden-chain tests cover green, red, and timeout gh transition cases through
      runtime dispatch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12349/dod-focused-tests/command.yaml\""
  - id: "dod-broader-tests"
    description: "Broader relevant market skill/runtime truth tests pass for the OMN-12349 surface."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12349/dod-broader-tests/command.yaml\""
  - id: "dod-full-suite-attempt"
    description: "Full-suite command was attempted; unrelated baseline failures and Kafka integration
      hang were documented."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12349/dod-full-suite-attempt/command.yaml\""
  - id: "dod-deploy-plan"
    description: "Deploy gate evidence for runtime-path changes; no live deploy was performed for this
      PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12349/dod-deploy-plan/command.yaml\"
          && grep -q 'deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12349/dod-deploy-plan/command.yaml\"
          && grep -q 'rpk topic produce' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12349/dod-deploy-plan/command.yaml\""
  - id: "dod-omnimarket-pr-961"
    description: "omnimarket PR #961 carries the OMN-12349 implementation at the recorded head SHA."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12349/dod-omnimarket-pr-961/command.yaml\"
          && grep -q '^pr_number: 961$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12349/dod-omnimarket-pr-961/command.yaml\""
  - id: "dod-occ-pr-1814"
    description: "onex_change_control PR #1814 carries the OMN-12349 central contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12349/dod-occ-pr-1814/command.yaml\"
          && grep -q '^pr_number: 1814$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12349/dod-occ-pr-1814/command.yaml\""
