---
schema_version: "1.0.0"
ticket_id: "OMN-7601"
title: "Implement Kafka static group membership to prevent rebalance storms"
summary: >
  Adds static consumer group membership to Kafka consumers in omnibase_infra to prevent rebalance storms
  during pod restarts. Improves runtime stability by keeping partition assignments stable across consumer
  restarts. Replaces prior placeholder contract with a single concrete receipt bound to omnibase_infra
  PR #1632.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1632 carries the static group membership implementation."
    command: "See drift/dod_receipts/OMN-7601/dod-infra-pr-1632/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1632"
    description: "omnibase_infra PR #1632 carries the OMN-7601 static group membership implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-7601/dod-infra-pr-1632/command.yaml
          && grep -q '^pr_number: 1632$' drift/dod_receipts/OMN-7601/dod-infra-pr-1632/command.yaml"
