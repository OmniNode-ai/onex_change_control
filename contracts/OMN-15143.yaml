---
schema_version: "1.0.0"
ticket_id: "OMN-15143"
title: "Close two vector-5 needs-cascade required-check skip vectors in omnibase_core"
summary: "Adds if: always() to the two omnibase_core workflow jobs surfaced by OMN-15120's required-check
  manifest fan-out: required-check-skip-guard and no-unguarded-git-subprocess. These jobs need occ-preflight
  but must still run and report honestly when occ-preflight fails, rather than being implicitly skipped
  by GitHub's default success() job condition."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_core PR #1509 product diff scope is limited to the two workflow files."
    command: "gh pr view 1509 --repo OmniNode-ai/omnibase_core --json files"
  - kind: "ci"
    description: "omnibase_core PR #1509 remains bound to the reviewed head commit."
    command: "test \"$(gh pr view 1509 --repo OmniNode-ai/omnibase_core --json headRefOid --jq .headRefOid)\"
      = \"5eab56b098a63c0a8e04d79cb929d67a7bdbade1\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core1509-product-head"
    description: "omnibase_core PR #1509 remains bound to reviewed commit 5eab56b098a63c0a8e04d79cb929d67a7bdbade1."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test \"$(gh pr view 1509 --repo OmniNode-ai/omnibase_core --json headRefOid --jq
          .headRefOid)\" = \"5eab56b098a63c0a8e04d79cb929d67a7bdbade1\""
  - id: "dod-core1509-product-diff-scope"
    description: "omnibase_core PR #1509 changes only the two workflow files that own the vector-5 findings."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1509 --repo OmniNode-ai/omnibase_core --json files --jq '[.files[].path]
          | sort | join(\"|\")' | grep -qx '.github/workflows/required-check-skip-guard-caller.yml|.github/workflows/validator-no-unguarded-git-subprocess.yml'"
  - id: "dod-core1509-required-check-skip-guard-always"
    description: "required-check-skip-guard-caller.yml at PR #1509 head sets job required-check-skip-guard
      to if: always() while retaining needs: occ-preflight."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_core/contents/.github/workflows/required-check-skip-guard-caller.yml?ref=5eab56b098a63c0a8e04d79cb929d67a7bdbade1'
          --jq .content | base64 -d | grep -A6 '^  required-check-skip-guard:' | grep -q 'if: always()'"
  - id: "dod-core1509-no-unguarded-git-subprocess-always"
    description: "validator-no-unguarded-git-subprocess.yml at PR #1509 head sets job no-unguarded-git-subprocess
      to if: always() while retaining needs: occ-preflight."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_core/contents/.github/workflows/validator-no-unguarded-git-subprocess.yml?ref=5eab56b098a63c0a8e04d79cb929d67a7bdbade1'
          --jq .content | base64 -d | grep -A6 '^  no-unguarded-git-subprocess:' | grep -q 'if: always()'"
  - id: "occ-self-bind-pr-4901"
    description: "OCC companion PR #4901 — self-bind for OMN-15143."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4901 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid"
