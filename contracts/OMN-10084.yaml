---
schema_version: "1.0.0"
ticket_id: "OMN-10084"
summary: "Finish emit-daemon and launcher cutover under one-Python policy"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused launcher and emit-daemon cutover checks pass locally"
    command: "cd ../omniclaude && uv run pytest tests/scripts -q"
  - kind: "docs"
    description: "Host-side launcher scripts pin /opt/homebrew/bin/python3.13 and strip PYTHONPATH"
    command: "cd ../omniclaude && rg -n '/opt/homebrew/bin/python3.13|env -u PYTHONPATH' plugins/onex/hooks/scripts
      scripts/repair-plugin-venv.sh"
  - kind: "ci"
    description: "omniclaude or related PR checks pass for the launcher and emit-daemon cutover"
    command: "gh pr checks --repo OmniNode-ai/omniclaude --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Launcher and repair paths are pinned to /opt/homebrew/bin/python3.13 and strip PYTHONPATH
      explicitly"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10084/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "Legacy publisher launch path is retired in favor of the omnimarket emit-daemon path"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10084/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "Watchdog and logging behavior remain healthy after the launcher cutover"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10084/dod-003 -type f -name '*.yaml' | grep -q ."
