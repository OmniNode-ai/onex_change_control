---
schema_version: "1.0.0"
ticket_id: "OMN-10841"
title: "DI Refactoring — Handler Injection Cleanup (wave 1: OMN-10751, OMN-10752, OMN-10746)"
summary: >
  Binds omnimarket PR #689 which adds optional DI injection seams to three handlers: HandlerOverseerVerifierConsumer
  (OMN-10751), HandlerLlmReviewer (OMN-10752), and HandlerBaselineCompare (OMN-10746). Each handler now
  accepts an optional injectable param with lifecycle-ok: optional-di-fallback annotation. 6 new injection-path
  unit tests added. All 4081 existing tests pass.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #689 merged to main with CI green."
    command: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json state,mergeCommit"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-689"
    description: "omnimarket PR #689 (DI injection seams for 3 handlers) merged to main."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json state --jq .state |
          grep -q MERGED"
  - id: "dod-omnimarket-commit"
    description: "omnimarket commit 36e4256b (PR 689) is on branch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "git -C omnimarket log --oneline | grep -q 36e4256b"
  - id: "dod-occ-pr-1110"
    description: "onex_change_control PR #1110 carries the OMN-10841 contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10841/dod-occ-pr-1110/command.yaml && grep -q '^status:
          PASS' drift/dod_receipts/OMN-10841/dod-occ-pr-1110/command.yaml && grep -q '^pr_number: 1110$'
          drift/dod_receipts/OMN-10841/dod-occ-pr-1110/command.yaml"
  - id: "dod-deploy"
    description: >
      Pure DI refactor — no runtime behavior change. Handler constructors gain optional injectable params;
      existing callers unaffected. Deploy gate: verify runtime container can import the three changed
      handler modules without error after next deploy.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo deploy: docker exec omninode-runtime python -c 'from omnimarket.nodes.node_overseer_verifier.handlers.handler_overseer_verifier_consumer
          import HandlerOverseerVerifierConsumer; from omnimarket.nodes.node_pr_review_bot.handlers.handler_llm_reviewer
          import HandlerLlmReviewer; from omnimarket.nodes.node_baseline_compare.handlers.handler_baseline_compare
          import HandlerBaselineCompare; print(HandlerOverseerVerifierConsumer, HandlerLlmReviewer, HandlerBaselineCompare)'"
