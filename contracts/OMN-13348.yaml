---
schema_version: "1.0.0"
ticket_id: "OMN-13348"
title: "refactor(omnibase_core): decompose receipt gate receipt loading"
summary: >-
  omnibase_core PR #1281 extracts receipt file loading, YAML parsing, schema validation, adversarial pre-schema
  guard handling, and canonical path/content binding into a focused private _load_bound_receipt helper.
  The existing _check_one_receipt path remains responsible for post-load receipt checks including adversarial
  status enforcement, runtime SHA classification, and contract hash binding. Public receipt-gate CLI/function
  behavior, skip-token handling, identity binding, Evidence-Ticket, Evidence-Source, and receipt status
  enforcement are preserved.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1281"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1281 (jonah/omn-13348-receipt-gate-decompose,
      base=dev). Binds OMN-13348 to the receipt-gate refactor by pr_number and commit_sha. Targeted receipt-gate
      unit suites pass with 254 tests, changed-file ruff and mypy strict pass, and changed-file pre-commit
      passes per PR verification.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13348/dod-omnibase-core-pr-1281/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13348 contract and code-PR
      binding. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check can resolve
      a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13348/dod-occ-pr-self/command.yaml
