---
schema_version: '1.0.0'
ticket_id: OMN-10415
title: "feat(OMN-10415): [onex_change_control] Wire verify / verify caller workflow"
summary: 'Add .github/workflows/call-receipt-gate.yml to onex_change_control so every OCC PR is gated
  by the receipt-gate, closing the sham-contract merge surface.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: call-receipt-gate.yml file present in the repo at the expected path
    command: "uv run pytest tests/unit/test_call_receipt_gate_workflow_omn10415.py -q"
  - kind: ci
    description: Workflow file calls the omnibase_core-hosted receipt-gate reusable
    command: "uv run pytest tests/unit/test_call_receipt_gate_workflow_omn10415.py -q"
  - kind: ci
    description: In-repo caller validates PR-head OCC contracts and receipts rather than main checkout
    command: "uv run pytest tests/unit/test_call_receipt_gate_workflow_omn10415.py -q"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: call-receipt-gate.yml present in .github/workflows/
    source: generated
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/test_call_receipt_gate_workflow_omn10415.py -q"
  - id: dod-002
    description: Workflow triggers on pull_request and merge_group
    source: generated
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/test_call_receipt_gate_workflow_omn10415.py -q"
  - id: dod-003
    description: Workflow job calls omnibase_core receipt-gate reusable
    source: generated
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/test_call_receipt_gate_workflow_omn10415.py -q"
  - id: dod-004
    description: Workflow passes PR-head OCC evidence paths for onex_change_control self-validation
    source: generated
    checks:
      - check_type: command
        check_value: "uv run pytest tests/unit/test_call_receipt_gate_workflow_omn10415.py -q"
