---
schema_version: "1.0.0"
ticket_id: "OMN-11730"
title: "Update merge_group CI triggers for dev"
summary: >-
  Central OCC evidence for the omninode_infra OMN-11730 follow-up that scopes merge_group CI triggers
  to both main and dev during the OMN-11714 dev/main transition.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "omninode_infra PR #447 carries explicit merge_group branch filters for main and dev."
    command: "See drift/dod_receipts/OMN-11730/dod-omninode-infra-pr-447/command.yaml"
  - kind: "manual"
    description: "Workflow trigger audit proves every merge_group trigger declares main and dev."
    command: "See drift/dod_receipts/OMN-11730/dod-trigger-audit/command.yaml"
  - kind: "manual"
    description: "Focused workflow YAML validation passed."
    command: "See drift/dod_receipts/OMN-11730/dod-yamllint/command.yaml"
  - kind: "manual"
    description: "No runtime deployment is required for CI trigger wiring."
    command: "See drift/dod_receipts/OMN-11730/dod-deploy/command.yaml"
  - kind: "manual"
    description: "OCC PR #1397 carries the central contract and receipt evidence for this ticket."
    command: "See drift/dod_receipts/OMN-11730/dod-occ-pr-1397/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-447"
    description: "omninode_infra PR #447 carries the merge_group dev/main branch filters."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11730/dod-omninode-infra-pr-447/command.yaml"
          && grep -q '^pr_number: 447$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11730/dod-omninode-infra-pr-447/command.yaml"
          && grep -q '^commit_sha: "d723f91505c0fbc3f97c40106919ca473ccde0cf"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11730/dod-omninode-infra-pr-447/command.yaml"
  - id: "dod-trigger-audit"
    description: "All merge_group triggers in omninode_infra declare branches [main, dev]."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11730/dod-trigger-audit/command.yaml"
          && grep -q 'OK: merge_group branches are explicit for main/dev' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11730/dod-trigger-audit/command.yaml"
  - id: "dod-yamllint"
    description: "Focused yamllint validation passed on modified workflow files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11730/dod-yamllint/command.yaml"
          && grep -q 'yamllint.*Passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11730/dod-yamllint/command.yaml"
  - id: "dod-deploy"
    description: "No live deploy required; this is GitHub Actions trigger wiring only."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11730/dod-deploy/command.yaml"
  - id: "dod-occ-pr-1397"
    description: "OCC evidence PR for OMN-11730 is bound to pull request 1397."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^pr_number: 1397$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11730/dod-occ-pr-1397/command.yaml"
  - id: "dod-occ-pr-1397"
    description: "onex_change_control PR #1397 carries the OMN-11730 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11730/dod-occ-pr-1397/command.yaml"
          && grep -q '^pr_number: 1397$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11730/dod-occ-pr-1397/command.yaml"
