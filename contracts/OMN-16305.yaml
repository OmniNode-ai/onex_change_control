---
schema_version: "1.0.0"
ticket_id: "OMN-16305"
title: "Autobind OCC evidence for OMN-16305"
summary: >
  OCC contract for OmniNode-ai/omnibase_core PR #1568 (Wave-2 docs consolidation: thins 4 of 17 docs/decisions/
  files to a verbatim pointer at the taxonomy's new knowledge-base home, per the org-wide docs consolidation
  plan). Local-CLI mint fallback: no autobind/occ-companion-effect companion existed for this PR (docs-only
  diff, no code path for autobind to derive a RED/GREEN probe from), so a content-pinned differential
  probe was hand-authored instead.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_core-pr-1568"
    description: >
      PR #1568 on OmniNode-ai/omnibase_core -- thins docs/decisions/ADR-006-receipt-consolidation.md (and
      3 sibling ADR files) from full in-repo content to a verbatim pointer at its new home, OmniNode-ai/knowledge-base
      ADR-0028 (already merged there via knowledge-base#30). Content-pinned RED-before/GREEN-after differential:
      fetches ADR-006-receipt-consolidation.md at both the PR's base and head SHAs via the GitHub contents
      API and confirms the file is full ADR content (>30 lines) at base but thinned to a short knowledge-base
      pointer at head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnibase_core/contents/docs/decisions/ADR-006-receipt-consolidation.md?ref=cd7289790ea7f69be2101f71aaf6bd1a1b4bb53d"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnibase_core/contents/docs/decisions/ADR-006-receipt-consolidation.md?ref=b55949cac79372661d7a49f85de59b3377dcc81e"
          --jq .content | base64 -d)"; base_lines=$(printf '%s' "$base_body" | wc -l | tr -d ' '); [ "$base_lines"
          -gt 30 ] && printf '%s' "$head_body" | grep -qF 'knowledge-base' && printf '%s' "$head_body"
          | grep -qF 'Migrated to the OmniNode knowledge base' && echo "OMN-16305-CORE-1568-DIFFERENTIAL-PROBE-PASS:
          ADR-006-receipt-consolidation.md is full ADR content ($base_lines lines) at base, thinned to
          a knowledge-base pointer at head"
  - id: "occ-self-bind-pr-6789"
    description: "Binds OMN-16305 to OCC companion PR #6789 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6789 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_core-pr-1589"
    description: >
      PR #1589 on OmniNode-ai/omnibase_core -- completes the Wave-2 docs consolidation started by #1568:
      thins the remaining 11 docs/decisions/**, all 7 docs/standards/**, and the 1 docs/troubleshooting/**
      file (19 files total) from full in-repo content to verbatim pointers at their new knowledge-base
      home (OmniNode-ai/knowledge-base PR #33). Content-pinned RED-before/GREEN-after differential: fetches
      each of the 19 files at both the PR's base and head SHAs via the GitHub contents API and confirms
      every file is substantial content (>20 lines) at base but thinned to a knowledge-base pointer at
      head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          for f in docs/decisions/ADR-001-protocol-based-di-architecture.md docs/decisions/ADR-002-field-limit-constants.md
          docs/decisions/ADR-003-reducer-output-exception-consistency.md docs/decisions/ADR-004-registration-trigger-architecture.md
          docs/decisions/ADR-005-core-infra-dependency-boundary.md docs/decisions/ADR-006-status-taxonomy.md
          docs/decisions/ADR-007-context-mutability-design-decision.md docs/decisions/ADR-012-VALIDATOR-ERROR-HANDLING.md
          docs/decisions/RISK-009-ci-workflow-modification-risk.md docs/decisions/ADR_BEST_PRACTICES.md
          docs/decisions/README.md docs/standards/AGENT_MD_TEMPLATE.md docs/standards/CI_CD_STANDARDS.md
          docs/standards/CI_VALIDATION_SETUP.md docs/standards/MERGE_DEPENDENCY_GRAPH.md docs/standards/onex_terminology.md
          docs/standards/onex_topic_taxonomy.md docs/standards/STANDARD_DOC_LAYOUT.md docs/troubleshooting/ASYNC_HANG_DEBUGGING.md;
          do base_body="$(gh api "repos/OmniNode-ai/omnibase_core/contents/$f?ref=c5d6bfea2e150ee830335edf65aeed3f96844c6e"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnibase_core/contents/$f?ref=827a4df11dfcc708eb1577f59c876ff88be7252a"
          --jq .content | base64 -d)"; base_lines=$(printf '%s' "$base_body" | wc -l | tr -d ' '); [ "$base_lines"
          -gt 20 ] && printf '%s' "$head_body" | grep -qF 'Migrated to the OmniNode knowledge base' ||
          { echo "OMN-16305-CORE-1589-DIFFERENTIAL-PROBE-FAIL: $f"; exit 1; }; done; echo "OMN-16305-CORE-1589-DIFFERENTIAL-PROBE-PASS:
          all 19 docs/decisions+standards+troubleshooting files full content (>20 lines) at base, thinned
          to a knowledge-base pointer at head"
  - id: "occ-self-bind-pr-7088"
    description: "Binds OMN-16305 to OCC companion PR #7088 (self-bind, second companion for the second
      omnibase_core PR #1589)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7088 --repo OmniNode-ai/onex_change_control --json number,state"
