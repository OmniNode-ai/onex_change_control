---
schema_version: "1.0.0"
ticket_id: "OMN-15247"
title: "Autobind OCC evidence for OMN-15247"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1922."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1922 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1922"
    description: "PR #1922 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1922-ci"
    description: "PR #1922 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5161"
    description: "OCC companion PR #5161 — self-bind for OMN-15247 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1922-deploy"
    description: "PR #1922 compute-only deploy proof: changed contention module is readable at product
      head and carries the new decision function."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/occ_contention.py?ref=cac88675d0419cb9ac4e102270dc9c43954ab613
          --jq .content | base64 -d | grep 'def decide_contention'"
  - id: "occ-self-bind-pr-5269"
    description: "OCC companion PR #5269 — self-bind for OMN-15247 deploy-evidence repair."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5269 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omnimarket-pr-1933"
    description: "PR #1933 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1933-ci"
    description: "PR #1933 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5301"
    description: "OCC companion PR #5301 — self-bind for OMN-15247 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: dod-OmniNode-ai-omnimarket-pr-1947
    description: "omnimarket#1947 carries the OMN-15247 generated-OCC-evidence admissibility fix at its
      exact reviewed head."
    source: manual
    checks:
      - check_type: command
        check_value: >
          gh pr view 1947 --repo OmniNode-ai/omnimarket --json headRefOid,files --jq '.headRefOid == "3178aeb1b45c9fe22ab33ef34a8a6e63d81eb235"
          and (.files | length == 16) and any(.files[]; .path == "src/omnimarket/nodes/node_pr_lifecycle_fix_effect/handlers/occ_companion_emitter.py")
          and any(.files[]; .path == "src/omnimarket/nodes/node_occ_companion_compute/handlers/handler_occ_companion_compute.py")
          and any(.files[]; .path == "tests/unit/scripts/test_check_generated_checks_red_derivable.py")'
          | grep -qx true
  - id: occ-self-bind-pr-5448
    description: "OCC PR #5448 carries the OMN-15247 evidence binding for omnimarket#1947."
    source: manual
    checks:
      - check_type: command
        check_value: >
          gh pr view 5448 --repo OmniNode-ai/onex_change_control --json number,state,headRefName --jq
          '.number == 5448 and .state == "OPEN" and .headRefName == "codex-a/omn-15247-omnimarket-1947-occ"'
          | grep -qx true
