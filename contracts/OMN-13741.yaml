---
schema_version: "1.0.0"
ticket_id: "OMN-13741"
title: "test(OMN-13741): raise coverage of src/omnimarket/events/emit_client.py"
summary: >
  Adds 27 unit tests covering all uncovered branches in src/omnimarket/events/emit_client.py (previously
  ~83%). Covers _connect() failure + reraise, _send_and_recv() OSError retry, _read_response() empty recv
  / buffer overflow / iteration limit, is_daemon_running_sync() any-exception → False, close() OSError
  suppression + idempotency + buf reset, context manager __enter__/__exit__ (normal + exception), and
  happy-path emit_sync/health_sync/is_daemon_running_sync round-trips via socket.socketpair(). Zero external
  dependencies — stdlib-only (socket + json + contextlib + unittest.mock). No node, contract, runtime,
  event-bus topic, REST endpoint, or live deploy change is introduced.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >
      27 new unit tests in tests/unit/events/test_emit_client_protocol.py covering all previously uncovered
      branches in src/omnimarket/events/emit_client.py. All 27 pass.
    command: "uv run pytest tests/unit/events/test_emit_client_protocol.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-emit-client-coverage"
    description: >
      omnimarket PR #1528 adds 27 unit tests to tests/unit/events/test_emit_client_protocol.py. 27/27
      tests pass locally; ruff format+check clean; pre-commit run --all-files passes. Full unit suite:
      2817 passed, 2 skipped, no regressions.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13741/dod-emit-client-coverage/command.yaml
          && grep -q '27/27 unit tests' drift/dod_receipts/OMN-13741/dod-emit-client-coverage/command.yaml
  - id: "dod-deploy-assessment"
    description: >
      Test-only PR; no new node, contract, runtime, event-bus topic, REST endpoint, or live deploy performed.
      No deploy action required; no .201 mutation, no container restart, no runtime topology change. Normal
      CI merge pipeline applies.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13741/dod-deploy-assessment/command.yaml
          && grep -q 'No deploy action required' drift/dod_receipts/OMN-13741/dod-deploy-assessment/command.yaml
  - id: "dod-occ-pr-self"
    description: >
      Self-binding OCC eligibility receipt for this onex_change_control PR carrying the OMN-13741 contract
      onto dev.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13741/dod-occ-pr-self/command.yaml &&
          grep -Eq '^pr_number: [0-9]+$' drift/dod_receipts/OMN-13741/dod-occ-pr-self/command.yaml
