---
schema_version: '1.0.0'
ticket_id: OMN-13386
title: "spike(OMN-13386): discriminator-union -> JSON-Schema for @rjsf inspector"
summary: >
  Spike proving that Pydantic-generated JSON Schema for the six OMN-13130 Phase 0 UI contract primitives
  is directly consumable by @rjsf/core 6.5.1 without a custom Field or Widget. Four gaps identified and
  resolved via uiSchema entries. Decision committed in docs/adr/005-rjsf-discriminator-union.md.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >
      15 vitest tests pass across three suites: schema structural assertions (required fields, anyOf nullable,
      oneOf + discriminator presence, const-value matching per branch), AJV direct-validation of Pydantic-
      generated schemas, and React rendering assertions (render, selector, submit round-trip).
    command: >
      cd omni_worktrees/OMN-13386/omnidash && OMNIBASE_CORE_PATH=$OMNI_HOME/omnibase_core npx vitest run
      src/components/dashboard/contract-inspector/ContractInspector.test.tsx
  - kind: docs
    description: >
      ADR committed at docs/adr/005-rjsf-discriminator-union.md documenting four gaps (nullable anyOf,
      discriminator label, AJV empty anyOf warning, frozen model runtime note) and resolutions for each.
    command: >
      test -f omni_worktrees/OMN-13386/omnidash/docs/adr/005-rjsf-discriminator-union.md
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >
      ContractInspector.test.tsx: 15/15 tests pass — schema structural, AJV validation, and React rendering
      suites all green.
    source: manual
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13386/dod-001/command.yaml
  - id: dod-002
    description: >
      ModelComponentContract JSON Schema rendered in @rjsf without crash; round-trip submit returns formData
      intact (verified by submit test).
    source: manual
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13386/dod-002/command.yaml
  - id: dod-003
    description: >
      ModelWidgetDefinition oneOf + discriminator schema verified: 5-branch oneOf with discriminator.propertyName
      = config_kind, each branch has config_kind.const matching the mapping.
    source: manual
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13386/dod-003/command.yaml
  - id: dod-004
    description: >
      ADR 005 committed with four documented gaps and Phase 1 decision: @rjsf 6.5.1 is the inspector library;
      no custom widget needed.
    source: manual
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13386/dod-004/command.yaml
  - id: dod-occ-pr-self
    description: >
      The onex_change_control evidence PR carries the central OMN-13386 contract and PASS receipts for
      the merged omnidash implementation PR.
    source: manual
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13386/dod-occ-pr-self/command.yaml
