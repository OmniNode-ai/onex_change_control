---
schema_version: "1.0.0"
ticket_id: "OMN-13577"
title: "Orphan-validator governance (SAFE SUBSET): triage + inventory, zero deletions"
summary: >-
  Governs the orphan validators flagged by the 2026-06-24 enforcement audit (epic OMN-13573). Scope is
  the SAFE SUBSET: delete genuinely-dead modules, inventory the rest, touch no spec/CI wiring (deferred
  to OMN-13574 Model B + OMN-13576; avoids conflict with in-flight PR #1327). A rigorous per-module analysis
  across all 11 local repos (351 pre-commit + CI config files), importer/entry-point/package-export checks,
  and an independent Repowise get_dead_code pass (which lists none of these modules as dead) found that
  NO module met the strict DEAD bar (no hook AND no CI AND no importer AND no behavior test) without a
  confounding live signal: the 14 validators/ modules are consumed pre-commit hooks and/or dedicated CI
  workflows (adopt-candidates), an exported-but-unconsumed remote hook (handler_di_gate), an intentional
  COMPUTE-node duplicate (handler_github_token_env_reads), or recently-landed guardrail scaffolding (publisher_injection_gate
  OMN-12881, validator_proof_tier_gate OMN-13338); the validation/ orphans are package-__init__ re-exports,
  console entry-points, or library imports. Per the task's explicit fallback, zero modules deleted; only
  the inventory doc ships. Doc-only change in omnibase_core; no code, no topic schema, no contract API
  change, no deploy.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1328"
    description: >-
      omnibase_core PR #1328 adds docs/audits/2026-06-24-orphan-validator-triage.md — the per-module governance
      inventory (status: deleted | adopt-candidate | library, with wiring/importer evidence and OMN-13576
      follow-up recommendations). Zero modules deleted (none safely deletable); no code touched, so import
      omnibase_core stays clean. pre-commit passes on the staged doc. Doc-only; no deploy.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13577/dod-core-pr-1328/command.yaml"
          && grep -q '^ticket_id: "OMN-13577"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13577/dod-core-pr-1328/command.yaml"
          && grep -q '^pr_number: 1328$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13577/dod-core-pr-1328/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      This OCC PR carries the central OMN-13577 contract and the dod-core-pr-1328 receipt that the omnibase_core
      docs PR's Receipt-Gate resolves via Evidence-Source: OCC#<this-pr>.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13577/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-13577"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13577/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 3052$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13577/dod-occ-pr/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: this is a doc-only change in omnibase_core (a single markdown audit file under
      docs/audits/). No source code, no Kafka topic, no Postgres migration, no contract API change, and
      no runtime-lane deploy. Dev/stability/prod lanes untouched.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13577/dod-deploy-assessment/command.yaml"
          && grep -q '^ticket_id: "OMN-13577"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13577/dod-deploy-assessment/command.yaml"
