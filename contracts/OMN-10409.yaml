---
schema_version: "1.0.0"
ticket_id: OMN-10409
title: "Write integration sweep artifacts"
summary: "Replace the explicit integration sweep stub with deterministic drift artifact persistence."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Integration sweep golden-chain and explicit-stub tests pass"
    command: "uv run pytest tests/test_golden_chain_integration_sweep_orchestrator.py tests/test_golden_chain_new_stub_nodes.py
      -q"
  - kind: "manual"
    description: "Touched integration sweep paths pass lint"
    command: "uv run ruff check src/omnimarket/nodes/node_integration_sweep_orchestrator tests/test_golden_chain_integration_sweep_orchestrator.py
      tests/test_golden_chain_new_stub_nodes.py"
  - kind: "manual"
    description: "Post-merge deploy smoke imports the integration sweep handler in the runtime container"
    command: "docker exec omninode-runtime python -c \"from omnimarket.nodes.node_integration_sweep_orchestrator.handlers.handler_integration_sweep_orchestrator
      import HandlerIntegrationSweepOrchestrator; print(HandlerIntegrationSweepOrchestrator.__name__)\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Integration sweep golden-chain and explicit-stub tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-10409/dod-tests/command.yaml\""
  - id: "dod-lint"
    description: "Touched integration sweep paths pass lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-10409/dod-lint/command.yaml\""
  - id: "dod-deploy"
    description: "Post-merge deploy smoke imports the integration sweep handler in the runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-10409/dod-deploy/command.yaml\""
  - id: "dod-occ-pr-1844"
    description: "OCC PR #1844 carries the OMN-10409 PR-binding receipt follow-up."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-10409/dod-occ-pr-1844/command.yaml\""
  - id: "dod-occ-pr-1845"
    description: "OCC PR #1845 carries the OMN-10409 receipt hash-binding follow-up."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-10409/dod-occ-pr-1845/command.yaml\""
  - id: "dod-occ-pr-1846"
    description: "OCC PR #1846 carries the final manual merge sweep integration artifact."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-10409/dod-occ-pr-1846/command.yaml\""
  - id: "dod-omniclaude-pr-1701"
    description: "OmniClaude PR #1701 restores cross-repo contract description coverage for release sweep
      validation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-10409/dod-omniclaude-pr-1701/command.yaml\""
  - id: "dod-occ-pr-1847"
    description: "OCC PR #1847 carries the OMN-10409 receipt for the omniclaude contract description fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-10409/dod-occ-pr-1847/command.yaml\""
  - id: "dod-omniclaude-pr-1702"
    description: "OmniClaude PR #1702 restores cross-repo contract description coverage from a ticket-scoped
      branch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-10409/dod-omniclaude-pr-1702/command.yaml\""
  - id: "dod-occ-pr-1848"
    description: "OCC PR #1848 carries the OMN-10409 receipt for the ticket-scoped omniclaude PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-10409/dod-occ-pr-1848/command.yaml\""
  - id: "dod-infra-pr-1780"
    description: "Omnibase Infra PR #1780 makes projection API Bifrost contract rendering explicitly disabled
      so the stability runtime can restart from committed compose configuration."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-10409/dod-infra-pr-1780/command.yaml\""
  - id: "dod-occ-pr-1849"
    description: "OCC PR #1849 carries the OMN-10409 receipt for the omnibase_infra projection API Bifrost
      compose fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-10409/dod-occ-pr-1849/command.yaml\""
