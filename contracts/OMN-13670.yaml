---
schema_version: '1.0.0'
ticket_id: OMN-13670
title: 'EMERGENCY PROD RECOVERY: surgical main hotfix — clear all Trivy HIGH CVEs in runtime builder'
summary: >-
  Emergency prod recovery authorized by operator. Two phases: (1) PR #2128 re-applied OMN-13654 dep floors
  (pyjwt>=2.13.0, python-multipart>=0.0.30, starlette>=1.3.1 in pyproject.toml) and OMN-13666 entrypoint
  stamp tolerance to clear 4 HIGH CVEs and fix crash-looping prod containers. (2) PR #2130 adds cryptography>=48.0.1
  Dockerfile floor to clear GHSA-537c-gmf6-5ccf (last remaining HIGH CVE). Together these unblock build-and-push-runtime.yml
  so a fresh prod ECR image can be built and pushed. The prod re-pin (ECR retag + container restart) follows
  separately under OMN-13418 gating. Neither PR deploys, restarts, or touches any cluster.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >-
      7 behavioral + static tests for entrypoint stamp tolerance pass. Version-compatibility tests pass.
    command: 'uv run pytest tests/unit/docker/test_runtime_entrypoint_stamp_tolerance.py tests/unit/runtime/test_version_compatibility.py
      -q'
  - kind: ci
    description: CI pipeline green on PRs
    command: 'gh pr checks 2130 --repo OmniNode-ai/omnibase_infra'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >-
      pyproject.toml on main declares explicit security-floor constraints (pyjwt>=2.13.0, python-multipart>=0.0.30,
      starlette>=1.3.1) and uv.lock resolves all three at or above the CVE-fixed minimums (PR #2128).
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13670/dod-001/command.yaml"
  - id: dod-002
    description: >-
      Entrypoint marks primary DB (omnibase_infra) stamp REQUIRED and secondary DB (omniintelligence)
      stamp BEST-EFFORT (PR #2128 via OMN-13666).
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13670/dod-002/command.yaml"
  - id: dod-003
    description: >-
      7 behavioral + static tests for the entrypoint stamp tolerance policy pass (PR #2128).
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13670/dod-003/command.yaml"
  - id: dod-004
    description: >-
      docker/Dockerfile.runtime BUILDER stage adds cryptography>=48.0.1 pip floor after all plugin installs,
      matching the existing protobuf/setuptools floor pattern, clearing GHSA-537c-gmf6-5ccf (PR #2130).
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13670/dod-004/command.yaml"
  - id: dod-deploy-assessment
    description: >-
      Static deploy assessment: no live deploy, runtime restart, ECR push, or cluster mutation performed
      by OMN-13670 PRs #2128 or #2130. Prod re-pin follows separately under OMN-13418 gating.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13670/dod-deploy-assessment/command.yaml"
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt — this onex_change_control PR carries the OMN-13670 contract + receipts
      consumed by omnibase_infra #2130 via Evidence-Source.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13670/dod-occ-self/command.yaml"
