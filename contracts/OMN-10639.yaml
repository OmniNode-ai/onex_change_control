---
schema_version: "1.0.0"
ticket_id: OMN-10639
title: "feat(OMN-10639): wire evidence bundle for delegation demo"
summary: "Add EvidenceBundleWriter that produces five immutable artifacts per delegation run (run_manifest,
  bifrost_response, quality_gate_result, cost_event, receipt) written atomically to $ONEX_STATE_DIR/delegation/bundles/<correlation_id>/.
  Schema-tolerant composition over runner/savings models."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omniclaude PR #1532 CI green and merged"
    command: "gh pr view 1532 --repo OmniNode-ai/omniclaude --json state,mergedAt"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "OCC contract for OMN-10639 exists — evidence bundle work registered"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-10639.yaml"
  - id: "dod-002"
    description: "PR merged to main with verified merge commit"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json state,mergedAt"
  - id: "dod-occ-pr-761"
    description: "Self-binding receipt for OCC PR #761 — required for OCC's own receipt-gate"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 761$' drift/dod_receipts/OMN-10639/dod-occ-pr-761/command.yaml"
