---
schema_version: "1.0.0"
ticket_id: "OMN-11502"
title: "Rich terminal display module"
summary: >-
  onex-self-extending-agent PR #80 adds the rich terminal display module for the self-extending agent.
  Provides formatted console output with rich tables and panels.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: tests
    description: "Unit tests for the display module pass."
    command: "uv run pytest tests/unit/ -q"
  - kind: manual
    description: "Rich display module renders correctly in the terminal."
    command: "uv run pytest tests/unit/test_demo_ui.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-80"
    description: "onex-self-extending-agent PR #80 carries the rich terminal display module."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11502/dod-product-pr-80/command.yaml"
          && grep -q '^pr_number: 80$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11502/dod-product-pr-80/command.yaml"
          && grep -q '^commit_sha: "8f4827cb4cfbbca39cbc0c685edeb7c7241a3be0"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11502/dod-product-pr-80/command.yaml"
  - id: "dod-unit-tests"
    description: "Full unit test suite passes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11502/dod-unit-tests/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11502 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11502/dod-occ-self/command.yaml"
