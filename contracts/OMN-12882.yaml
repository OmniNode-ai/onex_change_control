---
schema_version: "1.0.0"
ticket_id: "OMN-12882"
title: "[build] Guardrail: dashboard rejects non-authoritative demo read sources"
summary: >-
  Tracks dashboard lint/test enforcement rejecting demo-visible reads from non-authoritative sources such
  as fixture snapshots, rejected local ports, bespoke readers, and the evidence-pipeline second backend.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused guardrail unit tests pass for rejected and allowed dashboard read sources."
    command: "npx vitest run src/no-non-authoritative-read-source.test.ts"
  - kind: "tests"
    description: "Dashboard lint, typecheck, build, and unit suite proof is recorded in the product PR
      body."
    command: "npm run lint && npx tsc --noEmit && npm run build && npx vitest run"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-dashboard-read-authority-guard"
    description: >-
      Failing fixture/source with rejected read origin trips the guard; standard projection backend source
      passes and CI is wired.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12882/dod-dashboard-read-authority-guard/command.yaml"
  - id: "dod-omnidash-pr-235"
    description: >-
      omnidash PR #235 carries the dashboard ESLint guardrail, rule registration, and focused regression
      tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12882/dod-omnidash-pr-235/command.yaml"
  - id: "dod-deploy-no-live-mutation"
    description: >-
      Dashboard lint-rule and test wiring only; no live deploy, runtime restart, broker mutation, migration,
      or server-side state mutation is required before merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12882/dod-deploy-no-live-mutation/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-12882 contract and receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12882/dod-occ-pr-self/command.yaml"
