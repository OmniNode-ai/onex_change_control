---
schema_version: "1.0.0"
ticket_id: "OMN-12882"
title: "[build] Guardrail: dashboard rejects non-authoritative demo read sources"
summary: >-
  Tracks dashboard lint/test enforcement rejecting demo-visible reads from non-authoritative sources such as
  fixture snapshots, rejected local ports, bespoke readers, and the evidence-pipeline second backend.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-dashboard-read-authority-guard"
    description: >-
      Failing fixture/source with rejected read origin trips the guard; standard projection backend source
      passes and CI is wired.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12882/dod-dashboard-read-authority-guard/command.yaml"
