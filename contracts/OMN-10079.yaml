---
schema_version: "1.0.0"
ticket_id: "OMN-10079"
title: "Codex runtime local runtime ingress"
summary: "Restore brokered market-package command execution through the Codex runtime path by accepting
  runtime target addresses on dispatch bus commands, honoring explicit Pattern B broker package scope,
  and publishing handler terminal results for multi-publish contracts."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "protocols"
  - "envelopes"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "omnibase_core dispatch bus command model accepts target_runtime_address for runtime-directed
      commands."
    command: "cd omnibase_core && uv run pytest tests/unit/dispatch/test_dispatch_bus_client.py -q"
  - kind: "tests"
    description: "omnibase_infra auto-wiring publishes handler results to the declared terminal_event
      and Pattern B broker discovery honors pattern_b_broker.package_names independently of active runtime
      package env."
    command: "cd omnibase_infra && uv run pytest tests/integration/test_pattern_b_terminal_result_applier.py
      tests/unit/runtime/auto_wiring/test_wiring.py tests/unit/runtime/test_service_pattern_b_broker.py
      -q"
  - kind: "manual"
    description: "Live .201 Codex runtime executed omnimarket session_bootstrap through the runtime adapter
      and returned ready terminal output."
    command: "ssh jonah@192.168.86.201 docker exec omninode-runtime /app/.venv/bin/python -m omnimarket.adapters.codex.runtime_client
      --command-name session_bootstrap --payload <payload> --timeout-ms 90000"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "omnibase_core PR #1045 carries the dispatch bus target_runtime_address model fix with
      focused tests passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10079/dod-001/command.yaml && grep
          -q '^ticket_id: \"OMN-10079\"$' drift/dod_receipts/OMN-10079/dod-001/command.yaml && grep -q
          '^pr_number: 1045$' drift/dod_receipts/OMN-10079/dod-001/command.yaml"
  - id: "dod-002"
    description: "omnibase_infra PR #1515 carries terminal result publication, broker package scoping,
      repo deploy evidence, and top-level integration coverage."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10079/dod-002/command.yaml && grep
          -q '^ticket_id: \"OMN-10079\"$' drift/dod_receipts/OMN-10079/dod-002/command.yaml && grep -q
          '^pr_number: 1515$' drift/dod_receipts/OMN-10079/dod-002/command.yaml"
  - id: "dod-003"
    description: "Live .201 runtime session_bootstrap proof returned ready terminal output through the
      brokered omnimarket runtime adapter path."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10079/dod-003/command.yaml && grep
          -q '^ticket_id: \"OMN-10079\"$' drift/dod_receipts/OMN-10079/dod-003/command.yaml && grep -q
          'dispatch_result.status: completed' drift/dod_receipts/OMN-10079/dod-003/command.yaml && grep
          -q 'terminal_payload.status: ready' drift/dod_receipts/OMN-10079/dod-003/command.yaml"
  - id: "dod-occ-pr-759"
    description: "OCC PR #759 carries the OMN-10079 central contract and receipts needed by downstream
      runtime-ingress PRs."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10079/dod-occ-pr-759/command.yaml
          && grep -q '^ticket_id: \"OMN-10079\"$' drift/dod_receipts/OMN-10079/dod-occ-pr-759/command.yaml
          && grep -q '^pr_number: 759$' drift/dod_receipts/OMN-10079/dod-occ-pr-759/command.yaml"
  - id: "dod-occ-pr-762"
    description: "OCC PR #762 carries the OMN-10079 runtime dogfood receipts for omnibase_infra #1518
      and omnimarket #539."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10079/dod-occ-pr-762/command.yaml
          && grep -q '^ticket_id: \"OMN-10079\"$' drift/dod_receipts/OMN-10079/dod-occ-pr-762/command.yaml
          && grep -q '^pr_number: 762$' drift/dod_receipts/OMN-10079/dod-occ-pr-762/command.yaml"
  - id: "dod-004"
    description: "omnibase_infra PR #1518 carries the runtime projection wiring fix found by live .201
      dogfood, including materialized dispatch payload and JSONB row adaptation coverage."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10079/dod-004/command.yaml && grep
          -q '^ticket_id: \"OMN-10079\"$' drift/dod_receipts/OMN-10079/dod-004/command.yaml && grep -q
          '^pr_number: 1518$' drift/dod_receipts/OMN-10079/dod-004/command.yaml"
  - id: "dod-005"
    description: "omnimarket PR #539 carries runtime node payload alias support, registry migration, Pattern
      B null-field serialization, and .201 brokered dry-run proof."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10079/dod-005/command.yaml && grep
          -q '^ticket_id: \"OMN-10079\"$' drift/dod_receipts/OMN-10079/dod-005/command.yaml && grep -q
          '^pr_number: 539$' drift/dod_receipts/OMN-10079/dod-005/command.yaml && grep -q 'docker exec
          omninode-runtime-effects' drift/dod_receipts/OMN-10079/dod-005/command.yaml"
