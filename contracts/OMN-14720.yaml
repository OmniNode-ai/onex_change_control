---
schema_version: 1.0.0
ticket_id: OMN-14720
title: 'Evidence for OmniNode-ai/omnibase_core#1464: in-memory transport and conformance suite'
summary: OCC evidence for omnibase_core#1464 validating exact product head, product CI success, reviewed
  diff scope, and deploy-readiness assessment for net-new in-memory runtime transport foundation.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - protocols
  - events
  - public_api
evidence_requirements:
  - kind: ci
    description: 'PR #1464 product CI is green for exact head c0fc2290c49374b62c42b89cdefd662c23b40569'
    command: 'PRODUCT_RUN_ID=29610505833; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=c0fc2290c49374b62c42b89cdefd662c23b40569;
      : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
      = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
      = "$PRODUCT_HEAD"'
  - kind: ci
    description: 'PR #1464 product diff scope present'
    command: 'PRODUCT_PR_NUMBER=1464; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER}; gh pr diff
      $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q .'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnibase_core-pr-1464
    description: 'PR #1464 on OmniNode-ai/omnibase_core exists and is bound to OMN-14720 at the product
      head validated by CI.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1464; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=c0fc2290c49374b62c42b89cdefd662c23b40569;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnibase_core-pr-1464-diff
    description: 'PR #1464 on OmniNode-ai/omnibase_core has a non-empty reviewed product diff for OMN-14720.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1464; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q .'
  - id: dod-omnibase_core-pr-1464-ci
    description: 'Product CI completed successfully for omnibase_core PR #1464 at exact head c0fc2290c49374b62c42b89cdefd662c23b40569
      before OCC evidence was minted.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29610505833; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=c0fc2290c49374b62c42b89cdefd662c23b40569;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-omnibase_core-pr-1464-deploy
    description: 'Deploy-readiness assessment for omnibase_core PR #1464 in-memory runtime transport foundation
      edits. No live deploy, restart, docker exec, rpk topic produce, or .201 mutation was performed by
      this OCC PR; the change is net-new foundation and post-merge validation remains in the normal dev
      deploy pipeline.'
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14720/dod-omnibase_core-pr-1464-deploy/command.yaml
          && grep -qi ''no live deploy'' drift/dod_receipts/OMN-14720/dod-omnibase_core-pr-1464-deploy/command.yaml'
