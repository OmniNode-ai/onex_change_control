---
schema_version: "1.0.0"
ticket_id: "OMN-9869"
title: "Propagate LLM adapter timeout into infra inference requests"
summary: "Add timeout_seconds to ModelLlmAdapterRequest (default 30.0, bounds 1.0-600.0) and pass it through
  AdapterLlmProviderOpenai._translate_request into ModelLlmInferenceRequest so contract-owned endpoint
  timeouts reach the HTTP transport instead of being silently overridden by the inference layer's 30s
  default."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused unit suites for the adapter request model and provider translation path pass
      under the new timeout_seconds field"
    command: "cd ../omnibase_infra && uv run pytest tests/unit/adapters/llm/test_model_llm_adapter_request.py
      tests/unit/adapters/llm/test_adapter_llm_provider_openai.py -q"
  - kind: "tests"
    description: "Integration test proves the contract-owned timeout reaches the MixinLlmHttpTransport._execute_llm_http_call
      boundary end-to-end"
    command: "cd ../omnibase_infra && uv run pytest tests/integration/adapters/llm/test_adapter_llm_provider_openai_timeout_propagation.py
      -q"
  - kind: "ci"
    description: "Modified source files type-check under mypy --strict"
    command: "cd ../omnibase_infra && uv run mypy --strict src/omnibase_infra/adapters/llm/model_llm_adapter_request.py
      src/omnibase_infra/adapters/llm/adapter_llm_provider_openai.py"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused adapter unit tests (model + translation) pass with the new timeout_seconds field"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-9869/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "Integration test proves contract timeout reaches the HTTP transport boundary end-to-end"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-9869/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "mypy --strict passes on both modified source files"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-9869/dod-003 -type f -name '*.yaml' | grep -q ."
