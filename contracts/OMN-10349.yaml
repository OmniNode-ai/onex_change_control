---
schema_version: "1.0.0"
ticket_id: OMN-10349
title: "Fix normalization-symmetry pre-commit hook in omninode_infra"
summary: "Pin omnibase-core>=0.40.0 (first PyPI release containing
  omnibase_core.validation.checker_normalization_symmetry) and switch the
  normalization-symmetry hook from `language: system` + `uv run` to
  `language: python` with `additional_dependencies`. The previous setup only
  worked when PYTHONPATH leaked the local omnibase_core source checkout into
  resolution; on clean checkouts and CI it failed with `No module named
  omnibase_core.validation.checker_normalization_symmetry` on every commit."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: FULL
evidence_requirements:
  - kind: tests
    description: "OMN-10349 ticket contract validates locally"
    command: "uv run validate-yaml contracts/OMN-10349.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "OMN-10349 ticket contract validates locally"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10349/dod-001/command.yaml"
