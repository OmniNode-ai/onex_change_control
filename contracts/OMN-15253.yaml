---
schema_version: "1.0.0"
ticket_id: "OMN-15253"
title: "Autobind OCC evidence for OMN-15253"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1920."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1920 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1920"
    description: "PR #1920 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1920-ci"
    description: "PR #1920 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5152"
    description: "OCC companion PR #5152 — self-bind for OMN-15253 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-staging-readiness-node-3096f242"
    description: >
      Deploy-scope falsifiable probe for the OMN-15253 slice-1 omnimarket diff at product commit 3096f242.
      Slice 1 is source-only and ships no live deployment (proof_class: code-only; the verdict only becomes
      live-proven in slice 2 when the collect EFFECT captures a real snapshot), so this is the hermetic
      content probe the deploy gate accepts for compute-only tickets. It reads the deployed-source surface
      at the product commit, never a receipt or contract this ticket's own PRs author, and goes RED if
      the def-B signature regresses, if the pre-def-B envelope shape reappears, or if the node stops declaring
      itself a pure COMPUTE.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_staging_readiness_compute/handlers/handler_staging_readiness_compute.py?ref=3096f242c349c95cdaacb664be9116079ad48138'
          --jq .content | base64 -d | grep -c 'request: ModelStagingReadinessRequest' && ! gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_staging_readiness_compute/handlers/handler_staging_readiness_compute.py?ref=3096f242c349c95cdaacb664be9116079ad48138'
          --jq .content | base64 -d | grep -qE '(import|from).*Model(EventEnvelope|HandlerOutput)' &&
          gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_staging_readiness_compute/contract.yaml?ref=3096f242c349c95cdaacb664be9116079ad48138'
          --jq .content | base64 -d | grep -c 'purity: pure'
  - id: "dod-deploy-staging-composition-document-9b919595"
    description: >
      Deploy-scope falsifiable probe for the paired omninode_infra diff at product commit 9b919595: the
      canonical-dev composition document carries the LIVE-verified broker instance class and the pinned
      migration digest, and the retired hand-maintained required-config-key list is actually gone — the
      net-negative-surface claim asserted as an absence probe, so a re-added file goes RED.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omninode_infra/contents/k8s/onex-dev/readiness/staging-composition.canonical-dev.v1.yaml?ref=9b9195956bc47a0a841bf2b53f4ea2c5a29e4b56'
          --jq .content | base64 -d | grep -c 'instance_class: kafka.m5.large' && gh api 'repos/OmniNode-ai/omninode_infra/contents/k8s/onex-dev/readiness/staging-composition.canonical-dev.v1.yaml?ref=9b9195956bc47a0a841bf2b53f4ea2c5a29e4b56'
          --jq .content | base64 -d | grep -c 'sha256:6a433490894523fcde3bccbd1a6489754f7ffe83ab44ad03a674de63b7adc7d1'
          && ! gh api 'repos/OmniNode-ai/omninode_infra/contents/scripts/required-configmap-vars.txt?ref=9b9195956bc47a0a841bf2b53f4ea2c5a29e4b56'
          --jq .content
