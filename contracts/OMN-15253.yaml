---
schema_version: "1.0.0"
ticket_id: "OMN-15253"
title: "Autobind OCC evidence for OMN-15253"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1920."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1920 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1920"
    description: "PR #1920 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1920-ci"
    description: "PR #1920 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5152"
    description: "OCC companion PR #5152 — self-bind for OMN-15253 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-staging-readiness-node-292d81f3"
    description: >
      Deploy-scope falsifiable probe for the OMN-15253 slice-1 omnimarket diff at product commit 292d81f3.
      Slice 1 is source-only and ships no live deployment (proof_class: code-only; the verdict only becomes
      live-proven in slice 2 when the collect EFFECT captures a real snapshot), so this is the hermetic
      content probe the deploy gate accepts for compute-only tickets. It reads the deployed-source surface
      at the product commit, never a receipt or contract this ticket's own PRs author, and goes RED if
      the def-B signature regresses, if the pre-def-B envelope shape reappears, or if the node stops declaring
      itself a pure COMPUTE.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_staging_readiness_compute/handlers/handler_staging_readiness_compute.py?ref=292d81f35f29bed39cfa2b2e91482a6d818ce546'
          --jq .content | base64 -d | grep -c 'request: ModelStagingReadinessRequest' && ! gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_staging_readiness_compute/handlers/handler_staging_readiness_compute.py?ref=292d81f35f29bed39cfa2b2e91482a6d818ce546'
          --jq .content | base64 -d | grep -qE '(import|from).*Model(EventEnvelope|HandlerOutput)' &&
          gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_staging_readiness_compute/contract.yaml?ref=292d81f35f29bed39cfa2b2e91482a6d818ce546'
          --jq .content | base64 -d | grep -c 'purity: pure'
  - id: "occ-self-bind-pr-5164"
    description: "OCC repair PR #5164 — self-bind for OMN-15253 slice-1 deploy evidence (OMN-14650)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5164 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-product-bind-pr-702-4ab3cb4"
    description: >
      Product PR #702 — product-bind for the omninode_infra half of OMN-15253 after the branch was rebased
      onto current dev. OCC#5164 proved the slice-1 deploy evidence and self-bound the OCC companion,
      but omninode_infra#702 failed receipt-gate with reason=pr_ticket_mismatch because no PASS receipt
      in that OCC commit was bound to product PR #702 or rebased head 4ab3cb4f5a21285a97773cebcd11a92ea1d2d562.
      This entry adds that binding without rewriting the append-stable receipts already merged in OCC#5164.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -qx 'pr_number: 702' drift/dod_receipts/OMN-15253/occ-product-bind-pr-702-4ab3cb4/command.yaml
          && grep -qx 'commit_sha: 4ab3cb4f5a21285a97773cebcd11a92ea1d2d562' drift/dod_receipts/OMN-15253/occ-product-bind-pr-702-4ab3cb4/command.yaml
  - id: "occ-self-bind-pr-5172"
    description: >
      OCC repair companion PR #5172 — self-bind for the append-only OMN-15253 omninode_infra#702 product-bind
      receipt repair. This is separate from the #702 product binding because occ-preflight requires a
      PASS receipt bound to the evidence-source PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5172 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
