---
schema_version: "1.0.0"
ticket_id: "OMN-10669"
title: "Make post-deploy verification env-aware"
summary: >
  Update the deploy verification script so dev and prod smoke checks use explicit environment configuration,
  public hostname probes, dashboard body validation, and Infisical-managed secret checks. Live dev smoke
  execution remains gated on live kube access and explicit operational timing.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-script-env-aware"
    description: "omninode_infra PR #415 updates scripts/post-deploy-verify.sh for ENV=dev|prod."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10669/dod-script-env-aware/command.yaml"
  - id: "dod-local-validation"
    description: "Local shell validation and commit hooks passed for the env-aware verifier."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10669/dod-local-validation/command.yaml"
  - id: "dod-pr-open"
    description: "omninode_infra PR #415 was opened and merged for OMN-10669."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10669/dod-pr-open/command.yaml"
  - id: "dod-health-endpoint-contract"
    description: "omninode_infra PR #416 aligns the deploy verification health probe with the onex-api
      /health contract."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10669/dod-health-endpoint-contract/command.yaml"
  - id: "dod-occ-pr"
    description: "Bind the central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10669/dod-occ-pr/command.yaml"
  - id: "dod-occ-pr416-follow-up"
    description: "Bind the central OCC evidence follow-up for omninode_infra PR #416 to onex_change_control
      PR #940."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10669/dod-occ-pr416-follow-up/command.yaml"
