---
schema_version: "1.0.0"
ticket_id: "OMN-14705"
title: "Autobind OCC evidence for OMN-14705"
summary: >
  OCC contract bound by C3 manual merge lane for omnibase_core#1453, proving the Phase 3 shadow Product
  Readiness diff before downstream receipt gates are rerun.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1453 product diff scope present"
    command: "PRODUCT_PR_NUMBER=1453; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr diff ${PRODUCT_PR_NUMBER}
      --repo ${PRODUCT_REPO} --name-only | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_core-pr-1453"
    description: "Product PR exists for OMN-14705"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1453; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr view ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omnibase_core-pr-1453-ci"
    description: "Product PR diff is non-empty for OMN-14705"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1453; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr diff ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --name-only | grep -q ."
  - id: "occ-self-bind-pr-4311"
    description: "OCC PR #4311 exists and binds OMN-14705 evidence"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4311; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
