---
schema_version: "1.0.0"
ticket_id: "OMN-14735"
title: "Autobind OCC evidence for OMN-14735"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1840."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1840 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1840"
    description: "PR #1840 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1840-ci"
    description: "PR #1840 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4502"
    description: "OCC companion PR #4502 — self-bind for OMN-14735 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1840-deploy-assessment"
    description: "Deploy-gate scope for omnimarket PR #1840 at head 7a886da8. The change adds the canary
      monitoring gate compute node, contract, models, runtime metadata, and tests under src/omnimarket/nodes/node_canary_monitoring_gate_compute.
      No live deploy, restart, .201 mutation, docker exec, or production mutation is authorized by this
      evidence repair; hosted merge gates and post-merge dev availability remain the deployment boundary."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy scope assessed for omnimarket#1840 canary monitoring gate
          compute runtime node; no live deploy, restart, .201 mutation, docker exec, or production mutation
          is authorized by this evidence repair'"
  - id: "occ-self-bind-pr-4504"
    description: "OCC amendment PR #4504 -- self-bind for OMN-14735 deploy-scope evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
