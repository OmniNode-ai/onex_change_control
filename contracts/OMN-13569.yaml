---
schema_version: "1.0.0"
ticket_id: "OMN-13569"
title: "generated doc_content_scan COMPUTE validator package"
summary: >
  Generated doc_content_scan COMPUTE validator package for omnibase_core. The feature PR adds the typed
  validator package under src/omnibase_core/validation/doc_content_scan, topic constants, a console entry
  point, generation provenance, and focused unit/corpus acceptance coverage. The validator scans documentation
  content for local-environment traces, personal paths, ssh/email leaks, and Linear ticket references,
  while preserving explicit suppression markers and documented exemptions. Gate wiring is intentionally
  deferred to OMN-13572; this evidence proves the package and deterministic handler behavior only.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >
      Focused unit tests for the generated COMPUTE handler, model shapes, corpus-derived fixtures, suppression
      markers, path exemptions, and in-memory two-transport runner seam.
    command: "uv run pytest tests/unit/validation/doc_content_scan/test_handler_doc_content_scan_compute.py
      -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1326"
    description: >
      Binds omnibase_core PR #1326 at head commit 165445c485576d2438986ccf58d276aca20453ad. The focused
      doc_content_scan test module passes 45 tests, covering corpus fixtures, suppression markers, path
      exemptions, typed finding shape, and the in-memory bus runner seam.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13569/dod-omnibase-core-pr-1326/command.yaml
          && grep -q '^pr_number: 1326$' drift/dod_receipts/OMN-13569/dod-omnibase-core-pr-1326/command.yaml
          && grep -q '45 passed' drift/dod_receipts/OMN-13569/dod-omnibase-core-pr-1326/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Deploy assessment: omnibase_core PR #1326 adds a validator package and CLI entry point but explicitly
      does not wire .pre-commit-hooks.yaml, validator-requirements.yaml, CI workflows, live topics, or
      deployed runtime lanes. Gate wiring is deferred to OMN-13572, and no live deploy is performed by
      this PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13569/dod-deploy-assessment/command.yaml
          && grep -q '^pr_number: 1326$' drift/dod_receipts/OMN-13569/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-13569/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-3048"
    description: >
      Self-binding OCC eligibility receipt for the dev-targeted evidence PR. onex_change_control PR #3048
      carries the OMN-13569 contract and receipts onto dev, satisfying the receipt-to-PR binding required
      by validator_occ_merge_eligibility.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13569/dod-occ-pr-3048/command.yaml
          && grep -q '^pr_number: 3048$' drift/dod_receipts/OMN-13569/dod-occ-pr-3048/command.yaml"
