---
schema_version: "1.0.0"
ticket_id: "OMN-13052"
title: "D-6: UI verification line in dispatch template"
summary: >-
  Add an eighth auto-injected Operating Rule to the omniclaude dispatch_worker skill (prompt.md + SKILL.md
  mirror) so every compiled worker prompt requires Playwright proof for any UI-touching DoD item — live
  URL, screenshot, and network log of the actual emitted request — and explicitly rejects a curl of the
  canonical endpoint as evidence for a UI claim. Bridges the gap until the A-2 Receipt-Gate evidence-class
  check (OMN-13024) is live. Skill-markdown + tests + contract only; no runtime dependency.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >-
      The dispatch_worker Operating Rules injection tests pass, including the new UI-verification rule
      in REQUIRED_RULES and the parametrized Playwright-presence and curl-rejection assertions across
      prompt.md and SKILL.md.
    command: "env -u PYTHONPATH uv run pytest tests/skills/test_dispatch_worker_operating_rules_injection.py
      -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ui-verification-rule"
    description: >-
      omniclaude code PR adds the UI-verification operating rule (rule 8) to prompt.md and SKILL.md —
      Playwright proof on the operator's running surface (URL + screenshot + network log) with an explicit
      curl-rejection for UI claims — plus TDD assertions. The full dispatch_worker injection suite (14
      tests) passes.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^status: PASS$' drift/dod_receipts/OMN-13052/dod-ui-verification-rule/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      This onex_change_control PR carries the OMN-13052 contract + DoD receipts onto dev. The self-binding
      receipt binds the OCC PR by its own pr_number, satisfying the validator_occ_merge_eligibility self-preflight.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^status: PASS$' drift/dod_receipts/OMN-13052/dod-occ-pr-self/command.yaml
