---
schema_version: "1.0.0"
ticket_id: "OMN-12917"
title: "Auto-provision all contract-declared topics without marketplace allowlists"
summary: >-
  Remove the static installed-package topic discovery allowlist so contract-declared topics from installed
  ONEX marketplace packages are discovered through runtime package selection and entry points.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1930"
    description: "omnibase_infra PR #1930 carries the OMN-12917 implementation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12917/dod-infra-pr-1930/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused extractor, topic provisioner, startup validator, lint, format, and mypy checks
      pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12917/dod-focused-tests/command.yaml"
  - id: "dod-bus-audit-compile"
    description: "Bus audit adapter compile-only proof validates command wiring without writing runtime
      events."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12917/dod-bus-audit-compile/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-12917 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12917/dod-occ-pr/command.yaml"
