---
schema_version: "1.0.0"
ticket_id: "OMN-10844"
title: "Scaffold node_canary_score_reducer contract and models"
summary: >
  Create the full node scaffold for node_canary_score_reducer: contract.yaml, metadata.yaml, ModelCapabilityScoreRow,
  ModelScoreReducerState, and empty __init__ stubs. No handler implementation in this PR — scaffold only.
  Closes the first gap in the canary-to-routing feedback loop (OMN-10843).
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - topics
  - events
evidence_requirements:
  - kind: "tests"
    description: "Full test suite passes with no regressions"
    command: "uv run pytest tests/ -m 'not kafka' --tb=short -q"
  - kind: "manual"
    description: "Models import cleanly"
    command: "uv run python -c \"from omnimarket.nodes.node_canary_score_reducer.models.model_score_reducer_state
      import ModelCapabilityScoreRow, ModelScoreReducerState; print('OK')\""
  - kind: "ci"
    description: "All CI gates green on omnimarket PR #602"
    command: "gh pr checks 602 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-models-import"
    description: "ModelCapabilityScoreRow and ModelScoreReducerState import cleanly"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python -c \"from omnimarket.nodes.node_canary_score_reducer.models.model_score_reducer_state
          import ModelCapabilityScoreRow, ModelScoreReducerState; print('OK')\""
  - id: "dod-contract-yaml"
    description: "contract.yaml subscribes to onex.evt.omnimarket.adr-canary-completed.v1 and projects
      to capability_scores"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 602 -R OmniNode-ai/omnimarket | grep -q 'adr-canary-completed.v1'"
  - id: "dod-omnimarket-pr"
    description: "omnimarket PR #602 exists for this scaffold"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 602 -R OmniNode-ai/omnimarket --json state -q .state | grep -q OPEN"
  - id: "dod-deploy"
    description: >
      Scaffold-only PR: no handler implementation, no Kafka consumer, no runtime registration. Deploy
      evidence deferred to OMN-10845 (handler + bus wiring). No live deploy performed.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy-deferred: scaffold-only, handler in OMN-10845'"
