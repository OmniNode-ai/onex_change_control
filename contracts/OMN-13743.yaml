---
schema_version: "1.0.0"
ticket_id: "OMN-13743"
title: "test(OMN-13743): raise coverage of handler_llm_cli_subprocess.py execute_cli_inference"
summary: >-
  Add focused unit coverage for HandlerLlmCliSubprocess.execute_cli_inference covering unavailable binary
  handling, invalid request validation, subprocess errors, empty responses, success metadata, timeout
  handling, correlation propagation, and CLI argument forwarding. This is a pure test-only omnibase_infra
  change; no runtime lane, deployed service, data store, event-bus topic, or production configuration
  is modified.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused handler subprocess tests and node_llm_inference_effect unit suite pass on omnibase_infra
      PR #2153."
    command: >-
      grep -q '^status: PASS$' drift/dod_receipts/OMN-13743/dod-focused-tests/command.yaml && grep -q
      '^status: PASS$' drift/dod_receipts/OMN-13743/dod-no-runtime-mutation/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-13743/dod-occ-self/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: >-
      Focused unit tests prove HandlerLlmCliSubprocess.execute_cli_inference behavior across error, timeout,
      empty-response, and success paths.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13743/dod-focused-tests/command.yaml"
  - id: "dod-no-runtime-mutation"
    description: >-
      OMN-13743 is a test-only coverage PR with no live deploy, restart, .201 mutation, production data
      mutation, new REST endpoint, event-bus topic change, or runtime lane mutation.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13743/dod-no-runtime-mutation/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13743 contract and receipts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13743/dod-occ-self/command.yaml"
