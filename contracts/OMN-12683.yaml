---
schema_version: "1.0.0"
ticket_id: "OMN-12683"
title: "Workstream D: supersede #1031 with contract/overlay endpoint descriptors"
summary: >-
  Central evidence that omnimarket's generation consumer no longer decides endpoint request shape by URL
  path inspection. PR #1038 adds contract-declared model_routing.endpoint_mode, deletes the _endpoint_url_is_complete/urlsplit
  path predicate, and proves complete Gemini endpoint and local OpenAI-compatible base URL behavior through
  focused tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-endpoint-mode"
    description: "Focused omnimarket tests and grep prove endpoint behavior is contract-declared."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12683/dod-omnimarket-endpoint-mode/command.yaml"
  - id: "dod-deploy-gate-runtime-path"
    description: "Deploy-gate evidence for the generation-consumer runtime path changes in omnimarket
      PR #1038."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12683/dod-deploy-gate-runtime-path/command.yaml"
          && grep -q '^pr_number: 1038$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12683/dod-deploy-gate-runtime-path/command.yaml"
          && echo deploy-gate-evidence-present >/dev/null
  - id: "dod-occ-pr-2169"
    description: "onex_change_control PR #2169 carries the OMN-12683 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12683/dod-occ-pr-2169/command.yaml"
          && grep -q '^pr_number: 2169$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12683/dod-occ-pr-2169/command.yaml"
  - id: "dod-occ-pr-2170"
    description: "onex_change_control PR #2170 carries the OMN-12683 deploy-gate evidence update."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12683/dod-occ-pr-2170/command.yaml"
          && grep -q '^pr_number: 2170$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12683/dod-occ-pr-2170/command.yaml"
