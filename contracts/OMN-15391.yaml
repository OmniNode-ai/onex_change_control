---
schema_version: 1.0.0
ticket_id: OMN-15391
title: Retire evidence items whose description narrates a proof their command cannot produce
summary: '14 dod_evidence items across 11 OCC contracts carried a description narrating a product proof
  while their only check_value was ''uv run pytest tests/test_evidence_admissibility.py -q'' -- OCC''s
  own OMN-15309 predicate suite, which node_dod_verify resolves into the OCC checkout and which passes
  whether or not the fact it is filed under is true. This PR retires all 14 by the append-only supersession
  idiom (#4528), binding each claim to a real product fact at a pinned merge commit with an absence control
  at that commit''s first parent where one applies. Contract repair only: no ticket is flipped, because
  node_dod_verify has no contract-entry supersedes_dod_evidence handling until OMN-15390 lands. Also repairs,
  by the same idiom, the 9 pre-existing OMN-15382 Rule B violations inside the touched contracts and shrinks
  the frozen Rule B baseline by exactly those 9 entries.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: manual
    description: Every superseding check executes with a real exit code and can go RED
    command: uv run pytest tests/unit/scripts/test_lint_contract_check_values_corpus_baseline.py -q
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-15391-infra2537-baselines-bound
    description: 'The OMN-15376 supersession this PR adds binds a real, fix-discriminating product fact
      rather than OCC''s own predicate suite: the baselines_snapshots reconciliation is present in the
      guarded CREATE migration at omnibase_infra#2537''s merge commit 78b8731103a019ea90eee3164b53e882a064fe34
      and absent at its first parent 11b460db23d40dd2ca7704d0c3e9fd89851af60c. Public repo, so every runner
      can execute it.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_baselines/0001_create_baselines_tables.sql?ref=78b8731103a019ea90eee3164b53e882a064fe34''
          | grep -q ''ALTER TABLE baselines_snapshots ADD COLUMN IF NOT EXISTS snapshot_id'''
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_baselines/0001_create_baselines_tables.sql?ref=11b460db23d40dd2ca7704d0c3e9fd89851af60c''
          | grep -c ''ALTER TABLE baselines_snapshots ADD COLUMN IF NOT EXISTS snapshot_id'' | grep -qx
          0'
  - id: dod-15391-omniclaude1961-bound
    description: 'The OMN-15362 supersession this PR adds binds the shipped artifact, not the validator
      suite: the reusable verifier-stamp workflow exists at omniclaude#1961''s merge commit 7009244192111177d172825bb8374308797a0413
      and did not exist at its first parent 5703f123fe272a1214cca50d0fdd66e34180ed14.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omniclaude/contents/.github/workflows/verifier-stamp-reusable.yml?ref=7009244192111177d172825bb8374308797a0413''
          | grep -q ''name: Verifier Stamp (reusable)'''
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omniclaude/contents/.github/workflows/verifier-stamp-reusable.yml?ref=5703f123fe272a1214cca50d0fdd66e34180ed14''
          | grep -c ''name: Verifier Stamp (reusable)'' | grep -qx 0'
  - id: dod-15391-market1944-scope-bound
    description: 'The OMN-14979 Rule B collateral repair this PR adds pins a literal cross-repo PR instead
      of the bare ${PR_NUMBER} placeholder: omnimarket#1944''s diff contains the push-validation request
      model the seam adds, so the check goes RED if that scope changes. Honest limit on this contract
      as a whole: 9 of the 11 contracts repaired here bind facts in the PRIVATE OmniNode-ai/omninode_infra
      repo, whose endpoints 404 under this runner''s repo-scoped github.token; those checks are carried
      in their own contracts with that limit stated, and are deliberately NOT restated here, because a
      check that cannot evaluate on the runner executing this contract would report RED for an authorization
      reason rather than a product reason.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh api repos/OmniNode-ai/omnimarket/pulls/1944/files --paginate --jq '.[].filename'
          | grep -qx 'src/omnimarket/nodes/node_push_validation_effect/models/model_push_validation_request.py'
  - id: "occ-self-bind-pr-5502"
    description: "Binds OMN-15391 to OCC companion PR #5502 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5502 --repo OmniNode-ai/onex_change_control --json number,state"
