---
schema_version: "1.0.0"
ticket_id: "OMN-14917"
title: "Autobind OCC evidence for OMN-14917"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1856."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1856 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1856"
    description: "PR #1856 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1856-ci"
    description: "PR #1856 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4571"
    description: "OCC companion PR #4571 — self-bind for OMN-14917 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1857"
    description: "PR #1857 on OmniNode-ai/omnimarket — Evidence-Source autobind replacement after branch
      rename closed #1856."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omnimarket-pr-1857-ci"
    description: "PR #1857 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4577"
    description: "OCC companion PR #4577 — self-bind for OMN-14917 replacement companion."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1857-head-refresh"
    description: "PR #1857 on OmniNode-ai/omnimarket moved to head 5477f30cab45d0a77b8c74ce86e197cc99ead4f8
      after rebasing onto dev; this receipt refreshes the OMN-14917 binding."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1857 --repo OmniNode-ai/omnimarket --json number,state,headRefOid"
