---
schema_version: "1.0.0"
ticket_id: "OMN-11891"
title: "May 24 Parallel Execution"
summary: >
  Bind central OCC evidence for omnimarket PR #820, which repairs the delegation terminal path by carrying
  backend inference timeouts through routing, converting LLM failures into typed inference responses,
  and reducing errored inference responses to delegate-skill failed terminal events.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - envelopes
  - protocols
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-820"
    description: "omnimarket PR #820 carries the OMN-11891 delegation terminal-gap repair."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11891/dod-omnimarket-pr-820/command.yaml\"
          && grep -q '^pr_number: 820$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11891/dod-omnimarket-pr-820/command.yaml\""
  - id: "dod-local-validation"
    description: "Focused delegation tests, full delegation unit suite, ruff, and strict mypy passed locally
      for the repair branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11891/dod-local-validation/command.yaml\"
          && grep -q '81 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11891/dod-local-validation/command.yaml\""
  - id: "dod-stability-hotpatch-deploy"
    description: "Manual stability-test deploy/hotpatch proof used docker exec/docker cp and produced
      a deployed terminal success event."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11891/dod-stability-hotpatch-deploy/command.yaml\"
          && grep -q 'docker exec' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11891/dod-stability-hotpatch-deploy/command.yaml\"
          && grep -q 'delegate-skill-completed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11891/dod-stability-hotpatch-deploy/command.yaml\""
  - id: "dod-occ-pr-1532"
    description: "OCC PR #1532 carries the central OMN-11891 contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11891/dod-occ-pr-1532/command.yaml\"
          && grep -q '^pr_number: 1532$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11891/dod-occ-pr-1532/command.yaml\""
