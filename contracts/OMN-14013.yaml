---
schema_version: "1.0.0"
ticket_id: "OMN-14013"
title: "Autobind OCC evidence for OMN-14013"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2496.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2496 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2496"
    description: "PR #2496 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-deploy-assessment"
    description: >
      Deploy-scope DoD so PR #2496 clears the deploy-gate falsifiability ratchet (OMN-14443). Hermetic
      live-content probe (OMN-14051-compliant, no live lane/broker touched): reads the actual file content
      of docker/docker-compose.stability-test.yml from omnibase_infra PR #2496 commit c4f87c8f2f0727c15b5c1c81d8119d1757b2a7ff
      via the GitHub contents API and asserts the raised, pinned partition cap (topic_partitions_per_shard=15000)
      is present. This is RED before the product change and GREEN on the reviewed product commit; it does
      not read back this contract, a receipt, or the PR's own diff/body.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.stability-test.yml?ref=c4f87c8f2f0727c15b5c1c81d8119d1757b2a7ff
          --jq .content | base64 -d | grep -q 'topic_partitions_per_shard=15000'"
  - id: "occ-self-bind-pr-5111"
    description: "Binds OMN-14013 to OCC companion PR #5111 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
