---
schema_version: "1.0.0"
ticket_id: "OMN-10929"
title: "Create reviewdog reusable workflow skeleton"
summary: "Central reviewdog-review.yml reusable workflow in omniclaude with tiered reporter, fork safety,
  and flexible dep install"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "reviewdog-review.yml workflow file exists and YAML syntax validates"
    command: "python3 -c \"import yaml; yaml.safe_load(open('omniclaude/.github/workflows/reviewdog-review.yml'));
      print('YAML valid')\""
  - kind: "ci"
    description: "PR checks pass for omniclaude PR containing the workflow file"
    command: "gh pr checks --repo OmniNode-ai/omniclaude 1575 --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "reviewdog-review.yml workflow file exists with valid YAML syntax"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10929/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "PR CI passes for omniclaude#1575 with workflow_call trigger and 8 inputs"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10929/dod-002 -type f -name '*.yaml' | grep -q ."
