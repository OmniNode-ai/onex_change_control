---
schema_version: 1.0.0
ticket_id: OMN-14832
title: Autobind OCC evidence for OMN-14832
summary: OCC Evidence-Source autobind companion for OmniNode-ai/omnibase_infra#2365.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #2365 product diff scope present'
    command: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-OmniNode-ai-omnibase_infra-pr-2365
    description: 'PR #2365 on OmniNode-ai/omnibase_infra -- Evidence-Source autobind.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-OmniNode-ai-omnibase_infra-pr-2365-ci
    description: 'PR #2365 on OmniNode-ai/omnibase_infra -- product diff scope check.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
  - id: dod-omnibase-infra-pr-2365-deploy-scope
    description: 'Deploy-gate scope for omnibase_infra PR #2365. The change touches src/omnibase_infra/nodes/node_scope_extract_compute/handlers/handler_scope_extract.py
      runtime handler path, but only flips the existing scope extract compute node to canonical def-B
      dispatch shape with equivalence proof while preserving deterministic scope extraction semantics
      and golden outputs. No live runtime deployment, .201 mutation, docker exec, or rpk topic produce
      is authorized by this evidence repair; hosted merge gates and post-merge dev availability remain
      the deployment boundary.'
    source: generated
    checks:
      - check_type: command
        check_value: printf '%s\n' 'deploy scope assessed for omnibase_infra#2365 runtime handler path;
          no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'
  - id: occ-self-bind-pr-4487
    description: OCC companion PR  #4487 -- self-bind for OMN-14832 deploy evidence repair.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-infra-2365-head-86f781ec
    description: 'PR #2365 on OmniNode-ai/omnibase_infra -- refreshed product head 86f781ec.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName
    evidence_artifact: supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2365
  - id: dod-infra-2365-diff-86f781ec
    description: 'PR #2365 on OmniNode-ai/omnibase_infra -- refreshed product diff scope at head 86f781ec.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
    evidence_artifact: supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2365-ci
  - id: dod-infra-2365-deploy-86f781ec
    description: 'Deploy-gate scope for omnibase_infra PR #2365 at head 86f781ec. The change touches src/omnibase_infra/nodes/node_scope_extract_compute/contract.yaml
      and src/omnibase_infra/nodes/node_scope_extract_compute/handlers/handler_scope_extract.py runtime
      contract/handler paths, but only records the existing canonical def-B scope extraction dispatch
      shape and preserves deterministic scope extraction semantics and golden outputs. No live runtime
      deployment, .201 mutation, docker exec, or rpk topic produce is authorized by this evidence repair;
      hosted merge gates and post-merge dev availability remain the deployment boundary.'
    source: generated
    checks:
      - check_type: command
        check_value: printf '%s\n' 'deploy scope assessed for omnibase_infra#2365 refreshed runtime contract/handler
          paths; no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'
    evidence_artifact: supersedes_dod_evidence:dod-omnibase-infra-pr-2365-deploy-scope
  - id: occ-self-bind-pr-4489
    description: OCC follow-up PR  #4489 -- self-bind for OMN-14832 refreshed head evidence repair.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
    evidence_artifact: supersedes_dod_evidence:occ-self-bind-pr-4487
