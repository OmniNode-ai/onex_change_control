---
schema_version: "1.0.0"
ticket_id: "OMN-15136"
title: "Codify omniweb CI SSM deploy policy"
summary: >
  Manual OCC companion for OmniNode-ai/omninode_infra PR #769. The product PR codifies the omniweb-ci
  SSM deploy policy in Terraform at exact head 89369119e6598f78e7bfa29314f505fac583a3ee. This evidence
  binds source shape and the explicit no-live-mutation hold only; it does not claim Terraform import,
  apply, deployment, restart, or runtime readback.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #769 CI checks and explicit remaining live gate"
    command: "gh pr checks 769 --repo OmniNode-ai/omninode_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra769-terraform-policy-source"
    description: >-
      Binds the locally executed receipt artifact for omninode_infra PR #769 at exact head 89369119e6598f78e7bfa29314f505fac583a3ee.
      The underlying receipt was executed with an operator token able to read the private omninode_infra
      repository; hosted OCC compliance cannot read that private repo with its repo-scoped GITHUB_TOKEN,
      so this check binds the immutable PASS receipt and its recorded product file/blob facts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          receipt="drift/dod_receipts/OMN-15136/dod-infra769-terraform-policy-source/command.yaml" &&
          grep -q '^status: PASS$' "$receipt" && grep -q '89369119e6598f78e7bfa29314f505fac583a3ee' "$receipt"
          && grep -q 'aws/cluster-dev/omniweb_ci_iam.tf' "$receipt" && grep -q 'ac317c88e7656d76aac6997452fcb26664106578'
          "$receipt" && grep -q 'infra769 Terraform source binding PASS' "$receipt"
  - id: "dod-infra769-no-live-mutation-claim"
    description: >-
      Binds the locally executed receipt artifact for the PR's explicit remaining gate: no Terraform import
      or apply has run, and the PR must not merge as a claim that the live policy is already Terraform-managed.
      The underlying receipt was executed with an operator token able to read the private omninode_infra
      PR body; hosted OCC compliance binds the immutable PASS receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          receipt="drift/dod_receipts/OMN-15136/dod-infra769-no-live-mutation-claim/command.yaml" && grep
          -q '^status: PASS$' "$receipt" && grep -q 'No import or apply has been run' "$receipt" && grep
          -q 'Do not merge as a claim that the live policy is already Terraform-managed' "$receipt" &&
          grep -q 'infra769 live-boundary hold binding PASS' "$receipt"
  - id: "occ-self-bind-pr-5745"
    description: >-
      Binds OMN-15136 central evidence to OCC companion PR #5745 so OCC preflight can prove this evidence
      change is itself associated with the ticket it satisfies.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          number="$(gh api repos/OmniNode-ai/onex_change_control/pulls/5745 --jq .number)" && state="$(gh
          api repos/OmniNode-ai/onex_change_control/pulls/5745 --jq .state)" && branch="$(gh api repos/OmniNode-ai/onex_change_control/pulls/5745
          --jq .head.ref)" && test "$number" = "5745" && test "$state" = "open" && test "$branch" = "jonah/omn-15136-infra769-occ"
          && printf 'OCC 5745 self-bind PASS\n'
  - id: "dod-occ-hosted-private-token-boundary"
    description: >-
      Binds the hosted OCC compliance credential boundary that makes direct private omninode_infra probes
      fail closed in this PR. The checked workflow on dev runs contract compliance with repo-scoped github.token
      as GH_TOKEN and does not pass CROSS_REPO_PAT to the command execution step, so private product facts
      must be captured by operator-token receipts rather than re-fetched by hosted OCC CI.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          workflow="$(gh api -H 'Accept: application/vnd.github.raw' 'repos/OmniNode-ai/onex_change_control/contents/.github/workflows/ci.yml?ref=dev')"
          && printf '%s' "$workflow" | grep 'uv run python /tmp/onex_change_control/scripts/ci/run_contract_compliance_check.py'
          >/dev/null && printf '%s' "$workflow" | grep 'GH_TOKEN: \${{ github.token }}' >/dev/null &&
          printf '%s' "$workflow" | grep 'OMNI_REPO_CLONE_TOKEN: \${{ secrets.CROSS_REPO_PAT || github.token
          }}' >/dev/null && printf 'OCC hosted compliance private-token boundary PASS\n'
  # --- Appended by this companion (OMN-15374/OMN-15136 stale-evidence refresh) ---
  - id: "dod-infra769-no-live-mutation-claim-superseded"
    description: >-
      Append-only supersession of dod-infra769-no-live-mutation-claim. That entry binds PR #769's static
      body text ("No import or apply has been run" / "Do not merge as a claim that the live policy is
      already Terraform-managed") -- text which still mechanically matches today, since PR bodies do not
      change post-merge, so the original check_value keeps returning PASS on re-execution. It is superseded
      here not for a check failure but because the underlying CLAIM it was minted to prove -- that no
      live Terraform mutation has occurred -- is no longer true, and re-asserting it as current fact would
      misrepresent the ticket. Per operator Ruling R3 (recorded 2026-07-30T16:24Z in docs/tracking/ROLLING_WORK_LEDGER.md,
      propagated to Linear OMN-15136 comment 476d1e6f: "OMN-15136 omniweb-ci Terraform APPLY APPROVED
      (narrow sequence only)"), lane authorized-wave-0730/terraform-omn15136 (claimed 2026-07-30T16:18:38Z)
      executed the approved narrow sequence: state-import of the live inline omniweb-ci-ssm-deploy policy
      into aws_iam_user_policy.omniweb_ci_ssm_deploy, then a clean targeted apply once the post-import
      plan matched the approved scope exactly (sole diff: pinned ephemeral EC2 instance ID -> Name=omninode-k3s-dev-system
      tag condition on ssm:SendCommand; no IAM user replacement, no access-key change, no ECR-policy detachment,
      no unrelated resource). PR #769 itself has since MERGED (2026-07-31T23:37:07Z, merge commit d309339e5efc350301c1196db1301d6cfd2070b2,
      confirmed live via `gh pr view 769 --repo OmniNode-ai/omninode_infra --json state,mergedAt,mergeCommit`),
      and a follow-up IAM simulation readback (2026-08-03, Linear OMN-15136 comment acbcf2a3) confirmed
      zero drift between the live policy and the Terraform render. This entry retires the no-live-mutation
      claim and records the honest, approved, executed outcome; the superseded entry stays verbatim above
      for audit.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-infra769-no-live-mutation-claim"
    checks:
      - check_type: "command"
        check_value: >-
          receipt="drift/dod_receipts/OMN-15136/dod-infra769-no-live-mutation-claim-superseded/command.yaml"
          && grep -q '^status: PASS$' "$receipt" && grep -q 'd309339e5efc350301c1196db1301d6cfd2070b2'
          "$receipt" && grep -q 'authorized-wave-0730/terraform-omn15136' "$receipt" && grep -q 'infra769
          approved-and-executed mutation binding PASS' "$receipt"
  - id: "occ-self-bind-pr-5745-superseded"
    description: >-
      Append-only supersession of occ-self-bind-pr-5745. That entry's check_value literally asserts PR
      #5745 state == "open", but #5745 has since MERGED (2026-07-31T13:11:21Z, merge commit e0449955bf2427a01efa6df86ad6e6629091434b,
      confirmed live via `gh pr view 5745 --repo OmniNode-ai/onex_change_control --json state,mergedAt,mergeCommit`),
      so re-executing the original check_value now fails closed on a merged, evidence-bearing PR -- the
      same pr_ticket_mismatch-class failure OCC#6049 hit on OMN-15422's own #2596 entry, and the same
      class as OMN-15374's occ-self-bind-pr-5855 fixed alongside this entry in the same companion. This
      entry retires only the occ-self-bind-pr-5745 open-state assertion with a merged-terminal-state binding;
      the superseded entry stays verbatim above for audit.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-5745"
    checks:
      - check_type: "command"
        check_value: >-
          state="$(gh pr view 5745 --repo OmniNode-ai/onex_change_control --json state --jq .state)" &&
          merge_sha="$(gh pr view 5745 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq
          .mergeCommit.oid)" && test "$state" = "MERGED" && test "$merge_sha" = "e0449955bf2427a01efa6df86ad6e6629091434b"
          && printf 'OCC 5745 merged-terminal-state PASS\n'
