---
schema_version: "1.0.0"
ticket_id: "OMN-13713"
title: "Uninvokable/facade nodes — verify_effect, agent_learning_retrieval, decision_store_query, setup_preflight"
summary: >
  OCC receipt for the four uninvokable-node fixes in OMN-13713, split across omnimarket (PR #1516) and
  omnibase_infra (PR #2148) on branch jonah/omn-13713-bug-uninvokablefacade-nodes-verify_effect. node_verify_effect
  gains a top-level input_model; node_agent_learning_retrieval_effect gains a canonical HandlerAgentLearningRetrieval.handle()
  (the old entry-point marker had no handle()); node_decision_store_query_compute and node_setup_preflight_effect
  gain handler_routing.default_handler so RuntimeLocal resolves a concrete handler instead of failing
  closed with "missing 'terminal_event' topic and no handler spec found". All four are proven over the
  in-memory bus via RuntimeLocal; the changes are node contract.yaml and handler edits with no live runtime
  mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >
      PASS receipts bind the omnimarket RuntimeLocal proof, the omnibase_infra handler-resolution proof,
      the no-runtime-mutation deploy assessment, and this OCC PR self-binding.
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13713/dod-omnimarket-pr-1516/command.yaml
      && grep -q '^status: PASS$' drift/dod_receipts/OMN-13713/dod-omnibase-infra-pr-2148/command.yaml
      && grep -q '^status: PASS$' drift/dod_receipts/OMN-13713/dod-deploy-assessment/command.yaml && grep
      -q '^status: PASS$' drift/dod_receipts/OMN-13713/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1516"
    description: >
      Binds omnimarket PR #1516. node_verify_effect declares a top-level input_model and node_agent_learning_retrieval_effect
      gets a canonical handle(); the reproducing RuntimeLocal proof runs both nodes end-to-end over the
      in-memory bus (3 passed).
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13713/dod-omnimarket-pr-1516/command.yaml"
  - id: "dod-omnibase-infra-pr-2148"
    description: >
      Binds omnibase_infra PR #2148. handler_routing.default_handler added to node_decision_store_query_compute
      and node_setup_preflight_effect; RuntimeLocal._resolve_handler_spec now returns the concrete handler
      (2 passed).
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13713/dod-omnibase-infra-pr-2148/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Deploy assessment: this ticket only edits node contract.yaml + handler modules and is proven over
      the local in-memory bus (RuntimeLocal). No live deploy, container recreate, or runtime-lane mutation
      is performed; the effect/DB nodes' live execution dependencies (Postgres pool, deployment topology)
      are a separate systemic DI concern and are not deployed here.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13713/dod-deploy-assessment/command.yaml
          && grep -q 'deploy gate' drift/dod_receipts/OMN-13713/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13713 contract and durable
      receipt files that the central gate can verify inside the OCC checkout.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13713/dod-occ-self/command.yaml"
