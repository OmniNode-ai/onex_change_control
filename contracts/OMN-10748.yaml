---
schema_version: "1.0.0"
ticket_id: "OMN-10748"
title: "Add injectable bridge param to HandlerLlmReviewer (config-driven construction)"
summary: >
  HandlerLlmReviewer now accepts ModelInferenceAdapter via bridge= kwarg instead of always constructing
  AdapterInferenceBridge from config.inference_bridge_config. Config-driven construction remains the fallback
  when bridge=None. Removes config-driven-construction lifecycle annotation; replaces with optional-di-fallback.
  Part of OMN-10841 DI Wave 2.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests-pass"
    description: "4 new DI unit tests pass: injected bridge used, default bridge constructed from config,
      injected bridge skips config construction, multiple models fan-out."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10748/dod-unit-tests-pass/command.yaml\""
  - id: "dod-omnimarket-pr-692"
    description: "omnimarket PR #692 open with CI green at commit e4ec50b8ceed48406955d7f187a77664befb20c7."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10748/dod-omnimarket-pr-692/command.yaml\"
          && grep -q '^pr_number: 692$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10748/dod-omnimarket-pr-692/command.yaml\""
  - id: "dod-occ-pr-1120"
    description: "onex_change_control PR #1120 carries the OMN-10748 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10748/dod-occ-pr-1120/command.yaml\"
          && grep -q '^pr_number: 1120$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10748/dod-occ-pr-1120/command.yaml\""
  - id: "dod-deploy"
    description: "No runtime deploy required; pure code seam refactor with no behaviour change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10748/dod-deploy/command.yaml\""
