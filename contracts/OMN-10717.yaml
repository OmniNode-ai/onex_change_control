---
schema_version: 1.0.0
ticket_id: OMN-10717
title: Bifrost contract overlay pattern default override merge semantics
summary: Omnimarket loads a repo-default Bifrost delegation contract with empty endpoints and deep-merges
  a user endpoint overlay at runtime.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Product PR  #718 verifies Bifrost default and overlay merge semantics plus downstream delegation routing fixtures.
    command: uv run pytest tests/nodes/node_delegation_routing_reducer/test_bifrost_overlay_loader.py
      tests/nodes/node_delegation_routing_reducer/test_contract_routing_policy.py tests/unit/delegation/test_delegation_contracts.py
      tests/unit/delegation/test_delegation_chain_e2e.py tests/unit/delegation/test_delegation_quality_contract.py
      -q
  - kind: ci
    description: omniclaude PR  #1615 carries the Bifrost endpoint overlay installer and runtime merge repair.
    command: gh pr checks 1615 --repo OmniNode-ai/omniclaude
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-bifrost-overlay-merge
    description: Product verification evidence is captured as a canonical PASS receipt.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10717/dod-bifrost-overlay-merge/command.yaml"
          && grep -q '^pr_number: 718$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10717/dod-bifrost-overlay-merge/command.yaml"
          && grep -q '^verifier: agent$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10717/dod-bifrost-overlay-merge/command.yaml"
  - id: dod-omniclaude-pr-1615
    description: omniclaude PR  #1615 is bound to OMN-10717 for the Bifrost endpoint overlay installer and runtime merge repair.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10717/dod-omniclaude-pr-1615/command.yaml"
          && grep -q '^pr_number: 1615$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10717/dod-omniclaude-pr-1615/command.yaml"
          && grep -q '^verifier: receipt-gate-local$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10717/dod-omniclaude-pr-1615/command.yaml"
  - id: dod-occ-pr-1211
    description: onex_change_control PR  #1211 self-binds the OMN-10717 omniclaude#1615 receipt repair.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10717/dod-occ-pr-1211/command.yaml"
          && grep -q '^pr_number: 1211$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10717/dod-occ-pr-1211/command.yaml"
          && grep -q '^verifier: receipt-gate-local$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10717/dod-occ-pr-1211/command.yaml"
