---
schema_version: "1.0.0"
ticket_id: "OMN-12821"
title: "feat(OMN-12821): routing-authority demo gate — positive route-source proof + negative env/hardcode audit"
summary: >
  Close-the-loop plan track A1.5 (DEMO GATE). omnimarket adds an enforcing
  routing-authority gate that proves the exact demo path (node generation +
  delegation inference) resolves provider, model (served_model_id), endpoint_ref,
  the resolved endpoint, and route_source from the contract / overlay / routing
  authority — recording the source file:line for each — PLUS a negative static
  audit proving no demo-path source reads env vars for endpoint/provider/model,
  hardcodes a provider literal, or reads a fallback endpoint string after route
  resolution. Wired as a required CI step and a pre-commit hook (Rule 5).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "routing-authority gate passes and its 18-test suite is green"
    command: "cd omnimarket && uv run python scripts/ci/check_routing_authority.py && uv run pytest tests/test_routing_authority_gate.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >
      omnimarket PR #1115 carries the routing-authority demo gate
      (scripts/ci/check_routing_authority.py + tests/test_routing_authority_gate.py)
      wired as CI step + pre-commit hook, with the evidence packet at
      docs/evidence/OMN-12821/routing-authority-proof.json, bound to OMN-12821 at
      the observed PR head SHA.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12821/dod-001/command.yaml && grep -q '^ticket_id: OMN-12821$' drift/dod_receipts/OMN-12821/dod-001/command.yaml && grep -q '^pr_number: 1115$' drift/dod_receipts/OMN-12821/dod-001/command.yaml"
