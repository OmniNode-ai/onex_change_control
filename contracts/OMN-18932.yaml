---
schema_version: "1.0.0"
ticket_id: "OMN-18932"
title: "K5 caller-visible delegation evidence acceptance"
summary: >-
  Parent K5 contract for OMN-18925. The caller-visible document or scorecard must retain raw provider
  bytes and the requested output shape; final evidence also retains the internal prompt, complete response,
  and terminal payload so extraction cannot manufacture a pass or replace a missing carrier.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
proof_class: live-readback
evidence_requirements:
  - kind: tests
    description: >-
      Output-only validators accept the requested artifact and reject preambles, trailing self-review,
      and malformed structure.
    command: >-
      uv run --frozen pytest -q tests/unit/delegation/test_omn18278_truncated_response_vetoes_gate.py
      tests/unit/delegation/test_reasoning_preamble_veto_omn18379.py
  - kind: manual
    description: >-
      A final correlated capture retains hashes of caller-visible document or scorecard bytes, the internal
      prompt, full response, terminal payload or carrier, and projection join. The actual projection readback
      must match correlation, tenant, source, build, and route identities and reject any missing or mismatched
      identity. Its PASS receipt binds reconciled Core, Market, and Infra commits plus release and lock
      identities. Historical captures missing any required bytes or readback remain not met.
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: k5-caller-visible-raw-evidence
    description: >-
      Planned K5 output validators cover artifact-only acceptance and polluted-output refusal. Final evidence
      must additionally preserve raw caller-visible bytes and the full prompt, response, and terminal
      carrier.
    source: manual
    status: pending
    execution_scope: local_done_gate
    checks:
      - check_type: test_passes
        check_value: >-
          uv run --frozen pytest -q tests/unit/delegation/test_omn18278_truncated_response_vetoes_gate.py
          tests/unit/delegation/test_reasoning_preamble_veto_omn18379.py
        cwd: "${OMNI_HOME}/omnimarket"
