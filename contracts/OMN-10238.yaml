---
schema_version: "1.0.0"
ticket_id: "OMN-10238"
title: "[Canonical Skill Path] Split shared runtime-skill client across core and infra"
summary: "Split the shared runtime-skill client across core and infra ownership boundaries"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused cross-repo proof covers transport-agnostic models in core, concrete transport
      in infra, and wrapper-only payload mapping downstream"
    command: "cd ../omnibase_core && uv run pytest tests -k 'runtime_skill or dispatch_bus or skill_client'
      -q"
  - kind: "ci"
    description: "PR checks pass for the shared runtime-skill client split across touched repos"
    command: "gh pr checks --repo OmniNode-ai/omnibase_core --watch && gh pr checks --repo OmniNode-ai/omnibase_infra
      --watch && gh pr checks --repo OmniNode-ai/omnimarket --watch && gh pr checks --repo OmniNode-ai/omniclaude
      --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Canonical proof exists that transport-agnostic runtime-skill models and protocols live
      in omnibase_core"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10238/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "Canonical proof exists that concrete host-local transport ownership lives in omnibase_infra"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10238/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "Canonical proof exists that downstream wrappers are limited to payload mapping rather
      than long-term transport ownership"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10238/dod-003 -type f -name '*.yaml' | grep -q ."
