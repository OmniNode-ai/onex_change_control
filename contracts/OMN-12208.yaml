---
schema_version: "1.0.0"
ticket_id: "OMN-12208"
title: "Create node_self_healing_dispatch_orchestrator contract + handler stub"
summary: >
  OCC evidence for omnimarket PR #903. The change adds the node_self_healing_dispatch_orchestrator contract,
  typed request/result models, handler stub with node_not_implemented: true, and 20 golden-chain unit
  tests. No live runtime deploy is required before merge for this contract-only stub slice.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain"
    description: "20 golden-chain tests pass for node_self_healing_dispatch_orchestrator on omnimarket
      PR #903 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12208/dod-golden-chain/command.yaml
          && grep -q '20 passed' drift/dod_receipts/OMN-12208/dod-golden-chain/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy or runtime restart is required before merge for this contract + handler-stub
      slice."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12208/dod-deploy-assessment/command.yaml
          && grep -q 'no live deploy or runtime restart required before merge' drift/dod_receipts/OMN-12208/dod-deploy-assessment/command.yaml"
