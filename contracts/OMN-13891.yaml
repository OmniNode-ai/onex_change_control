---
schema_version: "1.0.0"
ticket_id: "OMN-13891"
title: "chore(OMN-13891): declare pytest dev-dependencies so uv sync preserves the test runner"
summary: >-
  omninode_infra PR #559 adds pytest==9.1.1, pytest-mock==3.15.1, pytest-asyncio==1.4.0, and pytest-cov==7.1.0
  to the pyproject.toml dev dependency group (pinned to match docker/onex-api/requirements-test.txt) and
  regenerates uv.lock, so uv sync no longer uninstalls the test runner. Local full pytest suite verified
  green (43/43 passed).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-559"
    description: >-
      Consuming-PR binding: omninode_infra PR #559 carries the OMN-13891 pytest dev-dependency fix at
      head fe87a88e705e3b8a67d137fd4fe6a001584a61ae, with a local full pytest run (43/43 passed) and clean
      pre-commit run as evidence.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13891/dod-infra-pr-559/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13891 contract and receipts consumed
      by omninode_infra PR #559.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13891/dod-occ-self/command.yaml"
