---
schema_version: "1.0.0"
ticket_id: "OMN-12359"
title: "Implement node_design_to_plan Phase 3 native launch routing"
summary: >
  Replace the OmniMarket node_design_to_plan Phase 3 NotImplementedError with
  typed Onex-native routing to node_plan_to_tickets, including contract-declared
  dry_run and plan_only modes that build the real dispatch plan without live
  mutation.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused design_to_plan and plan_to_tickets golden-chain tests pass."
    command: "uv run pytest tests/test_golden_chain_design_to_plan.py tests/test_golden_chain_plan_to_tickets.py -q"
  - kind: "tests"
    description: "Relevant market skill smoke, entry-point, and topic naming checks pass."
    command: "uv run pytest tests/test_market_skill_smokes.py tests/test_all_node_entry_points_are_package_form.py tests/validation/test_topic_naming_lint_producer_allowlist.py -q"
  - kind: "ci"
    description: "OmniMarket implementation PR is open and bound to this OCC evidence."
    command: "gh pr view 956 --repo OmniNode-ai/omnimarket"
  - kind: "ci"
    description: "Deploy gate evidence exists for runtime-path changes."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12359/dod-deploy-gate-plan/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-phase3-routing-tests"
    description: "Focused golden-chain tests prove native Phase 3 routing and downstream command shape."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12359/dod-phase3-routing-tests/command.yaml"
  - id: "dod-smoke-topic-validation"
    description: "Market skill smoke, node entry-point, and topic naming validation passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12359/dod-smoke-topic-validation/command.yaml"
  - id: "dod-omnimarket-pr-956"
    description: "OmniMarket PR #956 carries the OMN-12359 implementation branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12359/dod-omnimarket-pr-956/command.yaml"
  - id: "dod-deploy-gate-plan"
    description: "Runtime-path deploy gate evidence for OMN-12359."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12359/dod-deploy-gate-plan/command.yaml"
