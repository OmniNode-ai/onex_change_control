---
schema_version: "1.0.0"
ticket_id: "OMN-13447"
title: "omnibase_core main->dev backmerge (prereq for 0.46.0 dev->main promotion)"
summary: >-
  OCC central evidence for the omnibase_core main->dev backmerge that precedes the 0.46.0 dev->main promotion
  (precedent: the OMN-12934 wave paired the v0.45.0 promotion #1260 with the main->dev backmerge #1226).
  omnibase_core dev (591ba5d58) was 91 commits ahead of and 3 commits behind main (30c31c177); the 3 main-only
  commits are #1344 (occ-preflight pin to Evidence-Source OCC SHA), #1240 (continue dev->main promotion:
  ci.yml + url-authority-gate.yml + main-target-guard.yml), and #1260 (the v0.45.0 squash that dev already
  supersedes). The backmerge PR (omnibase_core #1345, merge commit on branch jonah/omn-13447-core-main-to-dev-backmerge)
  makes dev contain main's tip so the 0.46.0 dev->main promotion is a clean merge. main's two unique CI
  changes are preserved (occ-preflight Evidence-Source OCC SHA pin and main-target-guard promotion/* allowance);
  every other conflict resolved in dev's favor because dev is 91 commits ahead and supersedes the #1260/#1240
  snapshot; version kept at 0.46.0 (dev). This is a release-engineering / evidence change with no runtime
  topology, topic schema, or product source change in this repo; no prod/judge/stability runtime lane
  is touched.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-backmerge-clean"
    description: >-
      omnibase_core main->dev backmerge PR #1345 is a clean merge whose only content delta vs dev is the
      two main-unique CI files (occ-preflight.yml Evidence-Source OCC SHA pin from #1344, main-target-guard.yml
      promotion/* allowance from #1240) plus a new CHANGELOG v0.46.0 entry; every other conflict resolved
      in dev's favor (dev is 91 ahead / 3 behind main; the 3 main-only commits are #1344, #1240, and the
      #1260 v0.45.0 squash dev supersedes). main's two unique CI changes are preserved (HARD VERIFY: the
      Resolve Evidence-Source step + ref pin survive, and the promotion/* head-ref allowance survives);
      dev's 6 added required validator jobs in ci.yml are retained; pyproject stays 0.46.0; uv.lock regen
      is a no-op. This PASS receipt binds to PR #1345 so the Receipt Gate OCC eligibility check passes
      for the backmerge PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13447/dod-core-backmerge-clean/command.yaml
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contract consumed by the omnibase_core
      backmerge PR #1345 Receipt Gate via Evidence-Source. The probe greps the OCC tree and proves contracts/OMN-13447.yaml
      is present at this OCC PR head.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13447/dod-occ-pr/command.yaml
