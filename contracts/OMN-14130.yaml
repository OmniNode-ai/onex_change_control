---
schema_version: "1.0.0"
ticket_id: OMN-14130
title: "Bump omnibase_infra omnibase-core pin to 0.46.5 for OCC grandfather validator"
summary: "Bind omnibase_infra#2231 to central OCC evidence for the omnibase-core 0.46.5 pin bump that
  lets infra OCC preflight use the OMN-13888 per-entry receipt grandfather validator."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "Implementation PR is open against omnibase_infra dev"
    command: "gh pr view 2231 --repo OmniNode-ai/omnibase_infra --json state,headRefOid,baseRefName"
  - kind: "tests"
    description: "Version compatibility and local validation evidence from the implementation PR"
    command: "pytest tests/unit/runtime/test_version_compatibility.py"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2231"
    description: "omnibase_infra#2231 carries the omnibase-core 0.46.5 pin bump"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14130/dod-infra-pr-2231/command.yaml
          && grep -q '^pr_number: 2231$' drift/dod_receipts/OMN-14130/dod-infra-pr-2231/command.yaml"
  - id: "dod-local-validation"
    description: "Local validation collected on the implementation PR proves compatibility with omnibase-core
      0.46.5"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14130/dod-local-validation/command.yaml
          && grep -q '25 passed' drift/dod_receipts/OMN-14130/dod-local-validation/command.yaml"
  - id: "dod-deploy-scope"
    description: "Runtime-path deploy scope is explicit for the version compatibility pin bump"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14130/dod-deploy-scope/command.yaml
          && grep -q 'deploy' drift/dod_receipts/OMN-14130/dod-deploy-scope/command.yaml"
  - id: "dod-occ-self"
    description: "OCC evidence PR validates and binds this ticket"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14130/dod-occ-self/command.yaml
          && grep -q '^pr_number: 3699$' drift/dod_receipts/OMN-14130/dod-occ-self/command.yaml"
