---
schema_version: "1.0.0"
ticket_id: "OMN-9871"
title: "Provide source context and strict diff validation for CI-fix LLM patches"
summary: "Central receipt contract for omnimarket PR #494: bounded source context, strict git-style unified
  diff validation, and validation-error retry prompting before patch preflight or mutation."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused CI-fix tests cover source context, strict diff validation, and retry prompting."
    command: "See drift/dod_receipts/OMN-9871/dod-001/command.yaml"
  - kind: "tests"
    description: "CI-fix and merge-sweep adjacent suites pass."
    command: "See drift/dod_receipts/OMN-9871/dod-002/command.yaml"
  - kind: "ci"
    description: "Touched CI-fix files pass ruff."
    command: "See drift/dod_receipts/OMN-9871/dod-003/command.yaml"
  - kind: "ci"
    description: "Repo-local contract validation, compile-only runtime request proof, and whitespace checks
      pass."
    command: "See drift/dod_receipts/OMN-9871/dod-004/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused CI-fix tests cover source context, strict diff validation, and retry prompting."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '36 passed, 2 warnings' drift/dod_receipts/OMN-9871/dod-001/command.yaml
          && grep -q 'strict diff validation' drift/dod_receipts/OMN-9871/dod-001/command.yaml && echo
          'OMN-9871 dod-001 receipt markers present'"
  - id: "dod-002"
    description: "CI-fix and merge-sweep adjacent suites pass."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '103 passed, 19 warnings' drift/dod_receipts/OMN-9871/dod-002/command.yaml
          && echo 'OMN-9871 dod-002 receipt markers present'"
  - id: "dod-003"
    description: "Touched CI-fix files pass ruff."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'ruff check' drift/dod_receipts/OMN-9871/dod-003/command.yaml && grep -q
          'All checks passed' drift/dod_receipts/OMN-9871/dod-003/command.yaml && echo 'OMN-9871 dod-003
          receipt markers present'"
  - id: "dod-004"
    description: "Repo-local contract validation, compile-only runtime request proof, and whitespace checks
      pass."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'validate-yaml contracts/OMN-9871.yaml' drift/dod_receipts/OMN-9871/dod-004/command.yaml
          && grep -q 'ok: true' drift/dod_receipts/OMN-9871/dod-004/command.yaml && grep -q 'git diff
          --check' drift/dod_receipts/OMN-9871/dod-004/command.yaml && echo 'OMN-9871 dod-004 receipt
          markers present'"
