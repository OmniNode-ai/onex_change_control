---
schema_version: "1.0.0"
ticket_id: OMN-9871
title: "Provide source context and strict diff validation for CI-fix LLM patches"
summary: "Add bounded source context, strict git-style unified diff validation, and validation-error retry
  prompting to node_ci_fix_effect before any patch preflight or worktree mutation."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "CI-fix focused tests cover source context, strict diff validation, and retry prompting."
    command: "uv run pytest tests/nodes/node_ci_fix_effect/test_handler_ci_fix.py -q"
  - kind: "tests"
    description: "CI-fix and merge-sweep adjacent suites pass."
    command: "uv run pytest tests/nodes/node_ci_fix_effect/test_handler_ci_fix.py tests/test_triage_phase2_emit_rules.py
      tests/test_triage_orchestrator.py tests/test_golden_chain_fixer_dispatcher.py tests/test_merge_sweep_executor_safety.py
      tests/test_golden_chain_merge_sweep_executor.py tests/test_merge_sweep_state_reducer_phase2.py -q"
  - kind: "ci"
    description: "Touched CI-fix files pass ruff."
    command: "uv run ruff check src/omnimarket/nodes/node_ci_fix_effect/handlers/handler_ci_fix.py tests/nodes/node_ci_fix_effect/test_handler_ci_fix.py"
  - kind: "ci"
    description: "Compile-only runtime request proof validates the merge-sweep command path without live
      deploy, restart, event publish, or PR worktree mutation."
    command: "env -u PYTHONPATH uv run python scripts/run_codex_runtime_request.py --command-name pr_lifecycle_orchestrator
      --payload '{\"repos\":[\"OmniNode-ai/omnimarket\"],\"dry_run\":true,\"fix_only\":true,\"verify\":false}'
      --timeout-ms 300000 --compile-only"
  - kind: "manual"
    description: "Post-merge runtime proof plan only; live mutation requires explicit operator approval."
    command: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -m omnimarket.nodes.node_ci_fix_effect
      --help"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "CI-fix focused tests cover source context, strict diff validation, and retry prompting."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/nodes/node_ci_fix_effect/test_handler_ci_fix.py -q"
  - id: "dod-merge-sweep-tests"
    description: "CI-fix and merge-sweep adjacent suites pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/nodes/node_ci_fix_effect/test_handler_ci_fix.py tests/test_triage_phase2_emit_rules.py
          tests/test_triage_orchestrator.py tests/test_golden_chain_fixer_dispatcher.py tests/test_merge_sweep_executor_safety.py
          tests/test_golden_chain_merge_sweep_executor.py tests/test_merge_sweep_state_reducer_phase2.py
          -q"
  - id: "dod-ruff"
    description: "Touched CI-fix files pass ruff."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_ci_fix_effect/handlers/handler_ci_fix.py
          tests/nodes/node_ci_fix_effect/test_handler_ci_fix.py"
  - id: "dod-runtime-compile-only"
    description: "Compile-only runtime request proof; no live deploy, restart, event publish, or PR worktree
      mutation performed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run python scripts/run_codex_runtime_request.py --command-name
          pr_lifecycle_orchestrator --payload '{\"repos\":[\"OmniNode-ai/omnimarket\"],\"dry_run\":true,\"fix_only\":true,\"verify\":false}'
          --timeout-ms 300000 --compile-only"
  - id: "dod-runtime-proof-plan"
    description: "Post-merge runtime proof plan only; live mutation requires explicit operator approval."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -m omnimarket.nodes.node_ci_fix_effect
          --help"
