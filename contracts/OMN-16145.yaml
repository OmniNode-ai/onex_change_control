---
schema_version: "1.0.0"
ticket_id: "OMN-16145"
title: "Autobind OCC evidence for OMN-16145"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #936.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #936 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-936"
    description: "PR #936 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 936 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 936 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-936"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-pr-936-head-ab681076"
    description: "PR #936 current-head rebinding after dev update advanced the product branch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 936 --repo OmniNode-ai/omninode_infra --json number,state,headRefOid"
  - id: "occ-self-bind-pr-6636"
    description: "Self-bind item for OCC#6636, the companion PR that carries the #936 Evidence-Source
      autobind (OMN-16403: retroactively wired in — the receipt landed with #6636 but this contract entry
      was never appended)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6636 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid"
  - id: "occ-self-bind-pr-6893"
    description: "Self-bind item for the OCC follow-up PR carrying the #936 rebinding receipt."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6893 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
