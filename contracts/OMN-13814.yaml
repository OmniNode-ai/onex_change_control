---
schema_version: '1.0.0'
ticket_id: OMN-13814
title: "fix(OMN-13814): bump omnimarket sibling pins to match released core 0.46.2 / compat 0.5.5 (unblocks
  .201 dev lane)"
summary: >
  The .201 dev lane cold bring-up is blocked by the OMN-12977 sibling-pin preflight: omnimarket's uv.lock/pyproject.toml
  pinned omnibase-core==0.46.1 (rev 0d56b4c9) and omnibase-compat==0.5.4 (rev 331a9de4), while the .201
  host clones of those repos are already at core 0.46.2 (rev 29e9057b, released main HEAD) and compat
  0.5.5 (rev c0fc7168, released main HEAD). This ticket bumps omnimarket's [tool.uv.sources] git revs,
  override-dependencies pins, and the omnibase-compat== version constraint to match, and regenerates uv.lock.
  Repo: OmniNode-ai/omnimarket, PR #1553.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
dod_evidence:
  - id: dod-001
    description: >
      uv lock resolves cleanly against the bumped [tool.uv.sources] pins: omnibase-core 0.46.1 (0d56b4c9)
      -> 0.46.2 (29e9057b), omnibase-compat 0.5.4 (331a9de4) -> 0.5.5 (c0fc7168). No dependency conflicts.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13814/dod-001/command.yaml"
  - id: dod-002
    description: >
      Full omnimarket test suite (uv run pytest tests/ -v -m "not kafka") passes on the feature branch
      after the pin bump. 11188 passed, 52 skipped, 9 deselected, 4 pre-existing failures unrelated to
      this change (tests/test_datasource_postgres.py, requires a live local Postgres on :5436 — ConnectionRefusedError,
      no dependency-pin-touched code path).
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13814/dod-002/command.yaml"
  - id: dod-003
    description: >
      ruff format and ruff check --fix are clean on src/ and tests/ after the pin bump (no diff needed
      by the dependency change; pre-existing invalid-noqa warnings in config/settings.py are unrelated).
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13814/dod-003/command.yaml"
  - id: dod-004
    description: >
      pre-commit run --files pyproject.toml uv.lock passes, including the Dependency Health Gate and Reject
      skip-gate bypass tokens hooks.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13814/dod-004/command.yaml"
  - id: dod-occ-self
    description: >
      This onex_change_control PR carries the central OMN-13814 contract and PASS receipts required by
      the downstream omnimarket PR #1553 Receipt Gate and OCC preflight eligibility check.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13814/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
