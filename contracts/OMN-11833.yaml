---
schema_version: "1.0.0"
ticket_id: "OMN-11833"
title: "ADR corpus scan stack"
summary: >
  Parent evidence contract for the ADR/corpus scan omnimarket PR stack covering ADR inventory,
  discovery manifests, and incremental scan publishing.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-stack-prs"
    description: "The bounded ADR/corpus scan stack is represented by omnimarket PRs #788, #789, and #790."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11833/dod-stack-prs/command.yaml\"
          && grep -q 'pr_numbers: \"788,789,790\"' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11833/dod-stack-prs/command.yaml\""
  - id: "dod-deploy-no-prod"
    description: "Deploy-gate evidence: the stack repair performed no production deploy and no runtime mutation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'no production deploy or runtime mutation performed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11833/dod-deploy-no-prod/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR binds OMN-11833 stack evidence as the central evidence source."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11833/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: OMN-11833$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11833/dod-occ-pr/command.yaml\""
