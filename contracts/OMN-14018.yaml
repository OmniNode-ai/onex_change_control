---
schema_version: "1.0.0"
ticket_id: "OMN-14018"
title: "feat(OMN-14018): per-run backend pin on context-ROI runner + generation-consumer honor path"
summary: >-
  omnimarket PR #1636 adds a per-run backend pin so the context-ROI battery can capture genuine per-tier
  graded outcomes for context_roi_scores (the OMN-14011 Path-1 escalation-skip flip). The runner threads
  an optional target_endpoint onto every generation command as forced_endpoint_ref; the generation consumer
  honors it by pinning the STARTING route to that backend's tier through the same routing authority the
  escalation path uses, with a fail-safe fallback to the contract-declared route. Empty target_endpoint
  preserves prior routing byte-for-byte. This PR lands the enrichment MECHANISM; the live sub-floor capture
  + observed flip is gated on OMN-14001/#1629 merge and a stability-lane redeploy (the ticket's own Blocked-by)
  and authors no synthetic rows.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >-
      Focused omnimarket coverage proves the runner threads/omits forced_endpoint_ref by target_endpoint
      presence and the generation consumer pins attempt #1 to the forced backend's tier through the real
      routing authority (empty preserves local; a bad ref falls back to local).
    command: "uv run pytest tests/nodes/node_context_roi_runner/test_handler_context_roi_runner.py tests/unit/nodes/node_generation_consumer/test_generation_rides_ladder_omn13359.py
      -v"
  - kind: "docs"
    description: "Design authority records the Path-1 enrichment mechanism and honesty guards."
    command: "test -f docs/plans/2026-07-06-OMN-14011-roi-enrichment-design.md"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1636"
    description: >-
      Consuming-PR binding: omnimarket PR #1636 carries the runner target_endpoint field, the command
      forced_endpoint_ref threading, the generation-consumer _forced_starting_route honor path, the battery
      --target-endpoint flag, and focused tests for OMN-14018.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14018/dod-omnimarket-pr-1636/command.yaml\""
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-14018 central contract and
      PASS receipts consumed by omnimarket PR #1636 via its Evidence-Source line.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14018/dod-occ-self/command.yaml\""
