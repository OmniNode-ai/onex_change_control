---
schema_version: "1.0.0"
ticket_id: "OMN-14018"
title: "feat(OMN-14018): per-run backend pin on context-ROI runner"
summary: >-
  omnimarket PR #1636 adds a default-empty per-run backend pin for the context-ROI runner and generation
  consumer. The mechanism threads target_endpoint through the context-ROI battery into generation requests
  as forced_endpoint_ref, then resolves the forced starting route through the existing routing authority
  while preserving byte-identical behavior when unset or invalid.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Targeted product verification and state coverage passed"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14018/dod-local-verification/command.yaml"
  - kind: "tests"
    description: "State coverage gate covers node_context_roi_runner terminal output topics"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14018/dod-state-coverage/command.yaml"
  - kind: "manual"
    description: "omnimarket PR #1636 carries the OMN-14018 implementation"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14018/dod-omnimarket-pr-1636/command.yaml"
  - kind: "manual"
    description: "Runtime deploy scope is bounded to normal post-merge rollout"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14018/dod-deploy-scope/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-local-verification"
    description: >-
      Local verification documented on omnimarket PR #1636 covers runner threading of forced_endpoint_ref,
      generation-consumer forced starting route resolution through the real routing authority, empty-default
      preservation, invalid-ref fallback, lint, type checks, and pre-commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14018/dod-local-verification/command.yaml"
  - id: "dod-state-coverage"
    description: >-
      State coverage repair: node_context_roi_runner tests assert exact literal completed and failed terminal
      output topics, satisfying the strict changed-node state coverage gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14018/dod-state-coverage/command.yaml"
  - id: "dod-omnimarket-pr-1636"
    description: >-
      Consuming-PR binding: omnimarket PR #1636 at head e80f33bdbf138d7e681cf863893a37703335af6b carries
      the OMN-14018 implementation and state-coverage repair.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14018/dod-omnimarket-pr-1636/command.yaml"
  - id: "dod-deploy-scope"
    description: >-
      Deploy-scope binding: this default-off routing mechanism rides the normal post-merge dev and stability
      redeploy cadence. Live sub-floor capture and observed escalation-skip A/B remain post-merge/post-redeploy
      proof.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14018/dod-deploy-scope/command.yaml
          && echo no-out-of-band-deploy"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-14018 central contract and
      PASS receipts consumed by omnimarket PR #1636 via its Evidence-Source line.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14018/dod-occ-self/command.yaml"
