---
schema_version: "1.0.0"
ticket_id: "OMN-10926"
title: "feat(OMN-10926): enforce delegation environment scanner"
summary: >-
  Switch the omnimarket delegation environment-read scanner from report mode to enforce mode, keep formatter-wrapped
  ONEX_EXCLUDE handling correct, and wire the enforce gate into CI.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Delegation environment scanner tests pass"
    command: "uv run pytest tests/test_delegation_env_scanner.py -q"
  - kind: "manual"
    description: "Delegation environment scanner enforce mode passes on current omnimarket code"
    command: "uv run python scripts/ci/check_delegation_env_reads.py --mode enforce --verbose"
  - kind: "manual"
    description: "omnimarket PR #617 carries the scanner enforce-mode CI changes"
    command: "gh pr diff 617 -R OmniNode-ai/omnimarket --name-only"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-scanner-tests"
    description: "Delegation environment scanner tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          test -f drift/dod_receipts/OMN-10926/dod-scanner-tests/command.yaml && grep -q '^status: PASS$'
          drift/dod_receipts/OMN-10926/dod-scanner-tests/command.yaml
  - id: "dod-scanner-enforce-mode"
    description: "Delegation environment scanner enforce mode passes on current omnimarket code"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          test -f drift/dod_receipts/OMN-10926/dod-scanner-enforce-mode/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10926/dod-scanner-enforce-mode/command.yaml
  - id: "dod-ruff"
    description: "Touched scanner code and tests pass ruff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          test -f drift/dod_receipts/OMN-10926/dod-ruff/command.yaml && grep -q '^status: PASS$' drift/dod_receipts/OMN-10926/dod-ruff/command.yaml
  - id: "dod-pr-617"
    description: "omnimarket PR #617 carries scanner enforce-mode CI changes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          test -f drift/dod_receipts/OMN-10926/dod-pr-617/command.yaml && grep -q '^status: PASS$' drift/dod_receipts/OMN-10926/dod-pr-617/command.yaml
