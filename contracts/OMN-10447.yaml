---
schema_version: "1.0.0"
ticket_id: OMN-10447
title: "Capture handler_wiring.py improvements from .201"
summary: "Add operation-derived handler entry keys for auto-wiring route and dispatcher IDs, key async
  pre-resolution by entry instead of handler class name, and extract constructor dependency checks for
  sync container skip decisions."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "topics"
evidence_requirements:
  - kind: "manual"
    description: "Focused handler entry-key and sync container skip tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10447/dod-entry-key-tests/command.yaml"
  - kind: "manual"
    description: "Adjacent auto-wiring regression tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10447/dod-auto-wiring-regressions/command.yaml"
  - kind: "manual"
    description: "Touched handler wiring source and tests pass ruff"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10447/dod-lint/command.yaml"
  - kind: "manual"
    description: "Repo-local ticket contract validates"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10447/dod-contract-validate/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-entry-key-tests"
    description: "Focused handler entry-key and sync container skip tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10447/dod-entry-key-tests/command.yaml
          && grep -q '^ticket_id: OMN-10447$' drift/dod_receipts/OMN-10447/dod-entry-key-tests/command.yaml
          && grep -q '^evidence_item_id: dod-entry-key-tests$' drift/dod_receipts/OMN-10447/dod-entry-key-tests/command.yaml"
  - id: "dod-auto-wiring-regressions"
    description: "Adjacent auto-wiring regression tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10447/dod-auto-wiring-regressions/command.yaml
          && grep -q '^ticket_id: OMN-10447$' drift/dod_receipts/OMN-10447/dod-auto-wiring-regressions/command.yaml
          && grep -q '^evidence_item_id: dod-auto-wiring-regressions$' drift/dod_receipts/OMN-10447/dod-auto-wiring-regressions/command.yaml"
  - id: "dod-lint"
    description: "Touched handler wiring source and tests pass ruff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10447/dod-lint/command.yaml && grep
          -q '^ticket_id: OMN-10447$' drift/dod_receipts/OMN-10447/dod-lint/command.yaml && grep -q '^evidence_item_id:
          dod-lint$' drift/dod_receipts/OMN-10447/dod-lint/command.yaml"
  - id: "dod-contract-validate"
    description: "Repo-local ticket contract validates"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10447/dod-contract-validate/command.yaml
          && grep -q '^ticket_id: OMN-10447$' drift/dod_receipts/OMN-10447/dod-contract-validate/command.yaml
          && grep -q '^evidence_item_id: dod-contract-validate$' drift/dod_receipts/OMN-10447/dod-contract-validate/command.yaml"
