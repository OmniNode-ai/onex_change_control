---
schema_version: "1.0.0"
ticket_id: "OMN-12597"
title: "Land infra#1820 omnimarket node migration auto-discovery + re-sync drift fix"
summary: >-
  Bind durable process evidence for landing omnibase_infra#1820 (auto-discovery of omnimarket node-owned
  migrations) and the OMN-12597 follow-up that re-syncs the vendored node_projection_savings 076 savings
  view to its canonical omnimarket source. infra#1820 merged on dev; the vendored copy had a swapped COALESCE
  argument order on latency_ms that diverged from the omnimarket-owned source, caught by the auto-discovery
  drift guard. infra#1837 restores the verbatim copy so the drift guard passes and the savings view prefers
  the delegation-specific latency column the node intends.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
dod_evidence:
  - id: "dod-infra-1820-merged"
    description: "omnibase_infra#1820 (auto-discover omnimarket node migrations) merged on dev with proof."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12597/dod-infra-1820-merged/command.yaml"
          && grep -q 'd64b7f8198862b172f4a7e1d2f147876a700d4b6' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12597/dod-infra-1820-merged/command.yaml"
  - id: "dod-autodiscovery-test"
    description: "omnimarket node migrations are auto-discovered, covered by a passing test, including
      the vendored-tree drift guard that fails when the source diverges."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12597/dod-autodiscovery-test/command.yaml"
          && grep -q 'test_node_migration_discovery' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12597/dod-autodiscovery-test/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-12597 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12597/dod-occ-pr-self/command.yaml"
          && grep -q '^pr_number: 2093$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12597/dod-occ-pr-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
