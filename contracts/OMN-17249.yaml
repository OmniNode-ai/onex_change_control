---
schema_version: "1.0.0"
ticket_id: "OMN-17249"
title: "Autobind OCC evidence for OMN-17249"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1309."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1309 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1309 on OmniNode-ai/omninode_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omninode_infra`.
    command: |-
      uv run pytest tests/k8s/test_managed_kafka_rollout_roster.py tests/k8s/test_onex_dev_managed_postgres_networkpolicy.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1309"
    description: "PR #1309 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1309 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1309-ci"
    description: "PR #1309 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1309 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #1309 on OmniNode-ai/omninode_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1309"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/k8s/test_managed_kafka_rollout_roster.py tests/k8s/test_onex_dev_managed_postgres_networkpolicy.py -q
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "occ-self-bind-pr-8964"
    description: >-
      OCC self-binding receipt for PR #8964, the in-repo evidence PR carrying this contract. Binds OMN-17249
      to the OCC PR by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8964 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "ac3-runtime-image-carries-the-receipt-gate-runner"
    description: >-
      AC3 binding. AC3 asks that the pin be cited by digest AND by the omnimarket commit it was built
      from. No repin is carried by OmniNode-ai/omninode_infra#1309, so the citation is of the pin the
      plane already runs: digest sha256:3f6e48fb108f97d08a15686bae2606423f457a68cb0df152cc28d037bc9ddf56,
      ECR tag candidate-249eabb-20260910144709, omnibase_infra delivery run 34491258629 job 102918275308,
      which staged omnimarket f2c418346f610e54d81ebeb070ed731a4f9e1d97. The check below is the half of
      that chain that is deterministic and reproducible without cloud credentials: that the staged omnimarket
      ref is a descendant of 50dd2e902b57ecb2bd00ecb022d0c2c269a455e5, the merge commit of OmniNode-ai/omnimarket#2225
      which authored HandlerReceiptGateProjectionRunner. Without that property the Deployment would CrashLoopBackOff
      on ModuleNotFoundError, which is the exact failure this ticket was blocked on.
    source: "manual"
    status: "verified"
    binds_ac: ["AC3"]
    checks:
      - check_type: "command"
        check_value: >-
          git -C omnimarket merge-base --is-ancestor 50dd2e902b57ecb2bd00ecb022d0c2c269a455e5 f2c418346f610e54d81ebeb070ed731a4f9e1d97
          && echo ANCESTOR_YES
        cwd: "${OMNI_HOME}"
