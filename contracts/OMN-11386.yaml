---
schema_version: "1.0.0"
ticket_id: "OMN-11386"
title: "One-command judge setup and README environment docs"
summary: >-
  onex-self-extending-agent PR #72 updates README and demo-guide documentation to surface the one-command
  self-extending agent demo, OpenAI-compatible model environment variables, progressive and task-specific
  agent modes, and the relationship between the agent loop and the native entropy workflow.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: manual
    description: "Documentation PR #72 is open at the rebased head and contains only README/demo-guide
      changes."
    command: "gh pr view 72 --repo OmniNode-ai/onex-self-extending-agent --json headRefOid,files"
  - kind: manual
    description: "Conflict marker scan and ruff documentation-path invocation pass locally."
    command: "rg -n '<<<<<<<|=======|>>>>>>>' README.md docs/DEMO_GUIDE.md || true; uv run ruff check
      README.md docs/DEMO_GUIDE.md"
  - kind: manual
    description: "No live deploy required; documentation-only update."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11386/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-72"
    description: "onex-self-extending-agent PR #72 carries the rebased README and demo guide documentation
      update."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11386/dod-product-pr-72/command.yaml"
          && grep -q '^pr_number: 72$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11386/dod-product-pr-72/command.yaml"
          && grep -q '^commit_sha: "a00d2800acb3f64f622753b12ca146a5e4dde91b"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11386/dod-product-pr-72/command.yaml"
  - id: "dod-local-proof"
    description: "Local docs-only proof passed: no conflict markers remain and ruff accepted the documentation
      paths."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11386/dod-local-proof/command.yaml"
          && grep -q 'No Python files found' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11386/dod-local-proof/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11386/dod-local-proof/command.yaml"
  - id: "dod-deploy"
    description: "No live deploy required; README and demo guide changes do not mutate runtime services."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11386/dod-deploy/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11386 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11386/dod-occ-self/command.yaml"
