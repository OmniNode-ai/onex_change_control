---
schema_version: 1.0.0
ticket_id: OMN-9888
title: "Event-bus contract parser handles classic + nested subscription shapes — canonical impl in omnibase_core
  PR #944 adds parser_event_bus.py + EventBusContractShapeError + ModelEventBusSubscription/ParseResult"
summary: "Event-bus contract parser handles classic + nested subscription shapes — canonical impl in omnibase_core
  PR #944 adds parser_event_bus.py + EventBusContractShapeError + ModelEventBusSubscription/ParseResult"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'omnibase_core PR #944 CI checks pass'
    command: gh pr checks 944 --repo OmniNode-ai/omnibase_core --watch
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "parser_event_bus.py exposes parse_event_bus(contract) walking both event_bus.subscribe_topics
      (classic list[str]) and event_bus.subscribe[*].topic (nested list[dict] with optional consumer_group)
      — receipt at drift/dod_receipts/OMN-9888/dod-001/"
    source: generated
    checks:
      - check_type: test_passes
        check_value: uv run pytest tests/unit/contracts/test_parser_event_bus_shapes.py::test_classic_subscribe_topics_shape_parses
          tests/unit/contracts/test_parser_event_bus_shapes.py::test_nested_subscribe_topic_shape_parses
          -q
  - id: dod-002
    description: "EventBusContractShapeError is a typed exception (subclass of ValueError) raised on unknown
      shapes and malformed entries; not a silent skip — receipt at drift/dod_receipts/OMN-9888/dod-002/"
    source: generated
    checks:
      - check_type: command
        check_value: uv run python -c 'from omnibase_core.errors.error_event_bus_contract_shape import
          EventBusContractShapeError; assert issubclass(EventBusContractShapeError, ValueError); print(EventBusContractShapeError.__name__)'
  - id: dod-003
    description: "Unit test suite tests/unit/contracts/test_parser_event_bus_shapes.py passes 7/7 covering
      classic shape, nested shape, both shapes merge, missing event_bus, missing topic field, non-mapping
      event_bus, unknown-shape failure — receipt at drift/dod_receipts/OMN-9888/dod-003/"
    source: generated
    checks:
      - check_type: test_passes
        check_value: uv run pytest tests/unit/contracts/test_parser_event_bus_shapes.py -v
