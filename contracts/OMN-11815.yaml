---
schema_version: "1.0.0"
ticket_id: "OMN-11815"
title: "node_projection_swarm reducer"
summary: >
  Reducer node node_projection_swarm in omnimarket. Subscribes to onex.evt.omnimarket.swarm-dispatch-completed.v1
  and onex.evt.omnimarket.swarm-dispatch-failed.v1. Idempotent UPSERT into swarm_runs table (keyed by
  run_id). Publishes onex.evt.omnimarket.projection-swarm-applied.v1 with projection row and freshness
  metadata. Freshness SLA: max_lag=30s, degraded_after=60s.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-808"
    description: "omnimarket PR #808 implements node_projection_swarm reducer with 24 golden-chain tests"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11815/dod-omnimarket-pr-808/command.yaml\"
          && grep -q '^pr_number: 808$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11815/dod-omnimarket-pr-808/command.yaml\""
  - id: "dod-occ-pr-1512"
    description: "OCC PR #1512 binds this contract and adds allowlist entry for imperative contract guard"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11815/dod-occ-pr-1512/command.yaml\"
          && grep -q '^pr_number: 1512$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11815/dod-occ-pr-1512/command.yaml\""
  - id: "dod-deploy"
    description: "Deploy assessment — new reducer node, invoked on-demand via contract topic; no always-on
      deploy required for initial merge"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11815/dod-deploy/command.yaml\""
