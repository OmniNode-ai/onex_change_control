---
schema_version: "1.0.0"
ticket_id: "OMN-11815"
title: "Projection swarm reducer node"
summary: >
  Bind central evidence for omnimarket PR #808, which adds node_projection_swarm as an idempotent reducer
  for swarm dispatch terminal events and repairs merge-gate findings by declaring contract-owned handler
  routing.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-808"
    description: "Bind OMN-11815 evidence to omnimarket PR #808."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11815/dod-omnimarket-pr-808/command.yaml
          && grep -q '^pr_number: 808$' drift/dod_receipts/OMN-11815/dod-omnimarket-pr-808/command.yaml"
  - id: "dod-projection-swarm-focused-tests"
    description: "Projection swarm focused golden-chain tests pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11815/dod-projection-swarm-focused-tests/command.yaml
          && grep -q '24 passed' drift/dod_receipts/OMN-11815/dod-projection-swarm-focused-tests/command.yaml"
  - id: "dod-imperative-contract-guard"
    description: "Imperative contract guard passes without adding an allowlist entry."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11815/dod-imperative-contract-guard/command.yaml
          && grep -q 'omnimarket.* 0 /Users/jonah/Code/omni_home/onex_change_control/allowlists/omnimarket.yaml'
          drift/dod_receipts/OMN-11815/dod-imperative-contract-guard/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence for projection swarm reducer merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11815/dod-deploy-readiness/command.yaml
          && grep -q 'projection swarm reducer has focused test and imperative guard proof' drift/dod_receipts/OMN-11815/dod-deploy-readiness/command.yaml"
