---
schema_version: "1.0.0"
ticket_id: "OMN-10392"
title: "Add kafka replay compute primitive"
summary: "Add node_kafka_replay_compute with typed models, handler, contract registration, and focused
  replay verification."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused contract and isolated kafka replay tests pass in omnibase_infra PR #1476."
    command: "uv run pytest tests/unit/nodes/node_kafka_replay_compute/test_contract_loads.py tests/integration/nodes/node_kafka_replay_compute/test_replay_against_isolated_cluster.py
      -q"
  - kind: "tests"
    description: "Strict typing passes for kafka replay compute source in omnibase_infra PR #1476."
    command: "uv run mypy --strict src/omnibase_infra/nodes/node_kafka_replay_compute/"
  - kind: "ci"
    description: "Ruff check passes for kafka replay compute source, tests, and pyproject entry point
      registration in omnibase_infra PR #1476."
    command: "uv run ruff check pyproject.toml src/omnibase_infra/nodes/node_kafka_replay_compute tests/unit/nodes/node_kafka_replay_compute
      tests/integration/nodes/node_kafka_replay_compute"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused kafka replay contract and isolated replay tests passed locally on omnibase_infra
      PR #1476."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_kafka_replay_compute/test_contract_loads.py
          tests/integration/nodes/node_kafka_replay_compute/test_replay_against_isolated_cluster.py -q"
  - id: "dod-002"
    description: "Strict typing passed for kafka replay compute source on omnibase_infra PR #1476."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run mypy --strict src/omnibase_infra/nodes/node_kafka_replay_compute/"
  - id: "dod-003"
    description: "Ruff check passed for kafka replay compute source, tests, and pyproject entry point
      registration on omnibase_infra PR #1476."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check pyproject.toml src/omnibase_infra/nodes/node_kafka_replay_compute
          tests/unit/nodes/node_kafka_replay_compute tests/integration/nodes/node_kafka_replay_compute"
