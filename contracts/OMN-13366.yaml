---
schema_version: "1.0.0"
ticket_id: "OMN-13366"
title: "P1: Delegation verifiable classes use deterministic acceptance evidence"
summary: >-
  Central evidence for omnimarket PR #1299. The PR implements the verified delegation quality-gate redesign
  P1 for verifiable task classes: code_generation, test, and validator_generation declare deterministic_acceptance
  authority; quality-gate result events carry deterministic acceptance evidence; node_generation_consumer
  corpus acceptance carries the same artifact/corpus evidence bundle; and focused tests prove pass, fail,
  false-positive/false-negative, mutation, and non-verifiable heuristic behavior.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1299"
    description: >-
      omnimarket PR #1299 carries the OMN-13366 implementation at commit 9524acb2d71f255b3e008dd0fb54bcb054d83ec3.
      It adds deterministic acceptance authority declarations, result evidence fields, and focused tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13366/dod-omnimarket-pr-1299/command.yaml"
          && grep -q '^pr_number: 1299$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13366/dod-omnimarket-pr-1299/command.yaml"
          && grep -q '9524acb2d71f255b3e008dd0fb54bcb054d83ec3' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13366/dod-omnimarket-pr-1299/command.yaml"
  - id: "dod-focused-tests"
    description: >-
      Focused and broad local verification passed for the deterministic acceptance implementation, including
      delegation quality-gate tests, generation corpus acceptance tests, routing reducer contract tests,
      generation corpus projection tests, delegation projection tests, ruff, pre-commit, and the broad
      non-Kafka/non-integration pytest set.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13366/dod-focused-tests/command.yaml"
          && grep -q '373 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13366/dod-focused-tests/command.yaml"
          && grep -q '7523 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13366/dod-focused-tests/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for omnimarket PR #1299. The PR touches runtime reducer paths, so deploy-gate
      evidence is required. No live deploy is claimed here: the change is event/result semantics and deterministic
      acceptance evidence, with no Docker image, Kafka topic, migration, or service config change. Post-merge
      live verification should run after the dev lane updates. This check_value carries the deploy keyword
      for deploy-gate and binds the no-live-deploy assessment receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          echo 'deploy-evidence: OMN-13366 deterministic acceptance evidence; no live deploy performed
          by OCC evidence PR; post-merge dev lane verification required' && grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13366/dod-deploy-assessment/command.yaml"
          && grep -q 'No live deploy performed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13366/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2831"
    description: >-
      onex_change_control PR #2831 carries the central OMN-13366 contract and PASS receipts required by
      omnimarket PR #1299 Receipt Gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13366/dod-occ-pr-2831/command.yaml"
          && grep -q '^pr_number: 2831$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13366/dod-occ-pr-2831/command.yaml"
