---
schema_version: "1.0.0"
ticket_id: "OMN-10813"
title: "Complete DEC-003 constructor injection migration for 4 effect nodes in omnibase_infra"
summary: >
  Remove ONEX_EXCLUDE: declarative_node bypass comments from 4 effect nodes (node_contract_persistence_effect,
  node_delta_bundle_effect, node_decision_store_effect, node_delta_metrics_effect). Strip dead _resolved_dependencies
  instance variable and the dependencies constructor parameter from all 4 nodes and their registry create()
  methods. Nodes now follow canonical declarative pattern: only super().__init__(container) in __init__.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #1698 carries the DEC-003 migration at the recorded head SHA."
    command: "gh pr view 1698 --repo OmniNode-ai/omnibase_infra --json number,url,headRefOid,headRefName,title,state,baseRefName"
  - kind: "ci"
    description: "Pre-commit and mypy pass at the omnibase_infra PR head."
    command: "gh pr checks 1698 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1698-open"
    description: "omnibase_infra PR #1698 carries the DEC-003 migration branch at the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10813/dod-pr-1698-open/command.yaml"
          && grep -q '^pr_number: 1698$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10813/dod-pr-1698-open/command.yaml"
          && grep -q '^commit_sha: "907f014e1c9b26fcba8420566005d0bf7542893c"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10813/dod-pr-1698-open/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-10813 contract evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10813/dod-occ-self/command.yaml"
