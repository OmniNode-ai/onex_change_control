---
schema_version: "1.0.0"
ticket_id: "OMN-11171"
title: "Fix PR polish node to launch real repair workflow"
summary: >
  Central OCC evidence for omnimarket PR #679. Proves pr_polish skill/runtime requests route through node_pr_polish
  live workflow, the node emits dispatch-worker artifacts, and focused runtime/code checks pass.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-skill-adapter-smoke"
    description: "pr_polish skill adapter compile-only request validated command routing shape."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11171/dod-skill-adapter-smoke/command.yaml\""
  - id: "dod-node-smoke-artifacts"
    description: "node_pr_polish dry-run created dispatch worker artifacts and result.json."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11171/dod-node-smoke-artifacts/command.yaml\""
  - id: "dod-focused-tests"
    description: "Focused PR polish runtime and dispatch-worker tests passed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11171/dod-focused-tests/command.yaml\""
  - id: "dod-lint-typecheck"
    description: "Ruff, metadata dependency scope, and strict mypy passed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11171/dod-lint-typecheck/command.yaml\""
  - id: "dod-dispatch-worker-worktree-root"
    description: "Dispatch-worker prompts default to $OMNI_HOME/omni_worktrees instead of the legacy external
      worktree root."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11171/dod-dispatch-worker-worktree-root/command.yaml\""
  - id: "dod-merge-sweep-fix-only-canary"
    description: "merge-sweep fix_only dry-run completes without merge, fix, or verification side effects."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11171/dod-merge-sweep-fix-only-canary/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR #1091 carries the OMN-11171 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11171/dod-occ-pr/command.yaml\""
