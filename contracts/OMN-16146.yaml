---
schema_version: "1.0.0"
ticket_id: "OMN-16146"
title: "OCC evidence for OMN-16146"
summary: >
  Manual OCC companion for OmniNode-ai/omnimarket PR #2092 (create the missing projection_watermarks table
  via a proper node-owned migration, so the .201 dev projection-registration writer stops logging "Failed
  to update watermark: relation projection_watermarks does not exist" on every batch). Records an Evidence-Source
  autobind existence check plus a product-diff-scope check confirming the PR's exact five-file change
  (the migration SQL file, the db_io contract declaration, the regenerated relation-inventory evidence
  file, and the two touched test files).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2092"
    description: "PR #2092 on OmniNode-ai/omnimarket — Evidence-Source (hand-authored: build lane opened
      this PR directly rather than waiting on the live occ_companion_emitter consumer, per operator-authorized
      hand-authored-companion pattern established same-session on occ#6633)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2092 --repo OmniNode-ai/omnimarket --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2092-ci"
    description: "PR #2092 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425): confirms
      the PR's five changed files are exactly the node-owned migration (node_projection_registration/migrations/0005_create_projection_watermarks.sql),
      its db_io.db_tables contract declaration, the regenerated OMN-15423 relation-inventory evidence
      file, and the two touched test files (test_projection_migration_runner.py, test_application_relation_inventory.py)
      — no unrelated surface."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2092 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-6640"
    description: "OCC companion PR #6640 — self-bind for OMN-16146: every other entry in this contract
      binds to omnimarket#2092, not this OCC PR, so occ-preflight's merge-eligibility validator (validator_occ_merge_eligibility._receipt_bound_to_pr)
      needs this entry to find a PASS receipt bound to the OCC PR carrying it (established pattern: occ-self-bind-pr-6633)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6640 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2092-deploy"
    description: "PR #2092 on OmniNode-ai/omnimarket — falsifiable deploy probe (OMN-8912/OMN-14505):
      the deploy-gate flagged the PR's two changed runtime paths (node_projection_registration/contract.yaml,
      node_projection_registration/handlers/handler_registration.py) as requiring a deploy DoD probe.
      No live deploy has occurred yet (this ticket's live .201 dev apply/verify/restart happens post-merge,
      in this same lane). Per the gate's own accepted compute-only/hermetic pattern (OMN-14051), this
      is a source-presence probe pinned to the PR's exact head SHA: reads the changed handler file's raw
      content at that immutable git blob and counts occurrences of the introduced symbol (projection_watermarks)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api \"repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_registration/handlers/handler_registration.py?ref=9d0e4001df812741189ff2ae447dc0351432f175\"
          --jq '.content' | base64 -d | grep -c 'projection_watermarks'"
  - id: "occ-self-bind-pr-6694"
    description: "Binds OMN-16146 to OCC companion PR #6694 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6694 --repo OmniNode-ai/onex_change_control --json number,state"
