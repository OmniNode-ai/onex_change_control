---
schema_version: '1.0.0'
ticket_id: OMN-10556
title: 'Task 10: Audit ProtocolSecretStore semantics; lock signatures'
summary: 'Lock ProtocolSecretStore behavioural contract with module docstring + behavioural test'
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: New behavioural semantics test exercises nullable lookup, RuntimeError on read-only writes, prefix listing, and absence of get_secrets_batch
    command: uv run pytest tests/protocols/services/test_protocol_secret_store_semantics.py -v
  - kind: ci
    description: omnibase_spi PR 204 CI green
    command: gh pr checks 204 --repo OmniNode-ai/omnibase_spi
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Module docstring contains "Protocol invariants (locked by OMN-10556)" section
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 204 --repo OmniNode-ai/omnibase_spi | grep -q 'Protocol invariants (locked by OMN-10556)'
  - id: dod-002
    description: Semantics test module added at tests/protocols/services/test_protocol_secret_store_semantics.py
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 204 --repo OmniNode-ai/omnibase_spi | grep -q 'test_protocol_secret_store_semantics.py'
  - id: dod-003
    description: omnibase_spi PR 204 has CI passing
    source: manual
    checks:
      - check_type: command
        check_value: gh pr checks 204 --repo OmniNode-ai/omnibase_spi | grep -c 'pass' | grep -qv '^0$'
  - id: dod-004
    description: PR 204 open against main branch
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 204 --repo OmniNode-ai/omnibase_spi --json state,baseRefName --jq 'select(.state=="OPEN" and .baseRefName=="main") | "ok"' | grep -q ok
