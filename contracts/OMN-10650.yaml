---
schema_version: "1.0.0"
ticket_id: "OMN-10650"
title: "Remove lab paths from core checks"
summary: >
  Removes Jonah-lab absolute paths from omnibase_core validation checks and cross-repo agent YAML tests
  by resolving optional omniclaude paths from OMNICLAUDE_AGENTS_PATH or OMNI_HOME.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-agent-yaml-tests"
    description: "Focused agent YAML validation test file runs without lab-specific paths."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10650/dod-agent-yaml-tests/command.yaml\""
  - id: "dod-precommit"
    description: "Pre-commit hooks pass for touched omnibase_core files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10650/dod-precommit/command.yaml\""
  - id: "dod-pr-1047-head"
    description: "Bind OMN-10650 evidence to omnibase_core PR #1047 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1047 --repo OmniNode-ai/omnibase_core --json headRefOid,title"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10650 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10650/dod-occ-pr/command.yaml\""
