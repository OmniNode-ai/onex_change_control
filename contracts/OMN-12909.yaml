---
schema_version: "1.0.0"
ticket_id: "OMN-12909"
title: "Gateway P0A outbound attach proof: forwarder node and fail-closed ACL onboarding"
summary: >-
  Central evidence for the first P0A gateway implementation slice. omnibase_infra PR #1946 adds the provider-neutral
  node_bus_forwarder_effect scaffold, gateway envelope/config models, canonical topic validation, and
  ARCH-002-safe transform handlers/service. omninode_infra PR #496 hardens tenant onboarding so Kafka
  ACL provisioning fails closed, rejects reserved identities, and adds gateway NetworkPolicy conformance
  checks for avoiding the unauthenticated Redpanda 9092 listener.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
  - "protocols"
  - "envelopes"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1946"
    description: >-
      omnibase_infra PR #1946 carries the gateway forwarder P0A node scaffold with provider-neutral cloud
      broker refs, canonical topic validation, and focused local validators.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-omnibase-infra-pr-1946/command.yaml"
          && grep -q '^pr_number: 1946$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-omnibase-infra-pr-1946/command.yaml"
          && grep -q 'c77173ee33a0c48d856230d38787be28b5cc3444' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-omnibase-infra-pr-1946/command.yaml"
  - id: "dod-omninode-infra-pr-496"
    description: >-
      omninode_infra PR #496 carries fail-closed tenant Kafka ACL onboarding, reserved identity rejection,
      and gateway NetworkPolicy conformance tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-omninode-infra-pr-496/command.yaml"
          && grep -q '^pr_number: 496$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-omninode-infra-pr-496/command.yaml"
          && grep -q 'c29017796d0b3600bd3e6fd976f4c836cce0753a' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-omninode-infra-pr-496/command.yaml"
  - id: "dod-local-validation"
    description: >-
      Focused local validation passed for both downstream implementation slices before PR open.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-local-validation/command.yaml"
          && grep -q '17 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-local-validation/command.yaml"
          && grep -q '220 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-local-validation/command.yaml"
          && grep -q 'Architecture: PASS' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-local-validation/command.yaml"
          && grep -q 'Patterns: PASS' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-local-validation/command.yaml"
          && grep -q 'Any Types: PASS' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-local-validation/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-local-validation/command.yaml"
  - id: "dod-no-runtime-deploy"
    description: >-
      No runtime deploy, restart, docker exec, rpk topic produce, or live cluster mutation was performed
      for the P0A gateway implementation PRs; downstream PRs carry post-merge deploy validation steps
      only.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-no-runtime-deploy/command.yaml"
          && grep -qi 'no runtime deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-no-runtime-deploy/command.yaml"
          && grep -qi 'docker exec' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-no-runtime-deploy/command.yaml"
          && grep -qi 'rpk topic produce' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-no-runtime-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      The onex_change_control PR carries the central OMN-12909 contract and receipts required by downstream
      Receipt Gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-12909"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr/command.yaml"
  - id: "dod-occ-pr-2511"
    description: >-
      onex_change_control PR #2511 refreshes the central OMN-12909 receipts after omnibase_infra PR #1946
      gained the generated gateway forwarder entry point.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr-2511/command.yaml"
          && grep -q '^pr_number: 2511$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr-2511/command.yaml"
          && grep -q 'f9188b7492dd0ea0c8d93096c15017d924949c68' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr-2511/command.yaml"
  - id: "dod-occ-pr-2513"
    description: >-
      onex_change_control PR #2513 refreshes the central OMN-12909 receipts after omnibase_infra PR #1946
      gained the final runner image identity lock update.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr-2513/command.yaml"
          && grep -q '^pr_number: 2513$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr-2513/command.yaml"
          && grep -q 'ab3d66ad7014cbf6334c424f6651e1e82e1b4f1d' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr-2513/command.yaml"
  - id: "dod-occ-pr-2518"
    description: >-
      onex_change_control PR #2518 is the final self-bound OCC refresh carrying the OMN-12909 receipt
      chain for omnibase_infra PR #1946 head c77173ee33a0c48d856230d38787be28b5cc3444.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr-2518/command.yaml"
          && grep -q '^pr_number: 2518$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr-2518/command.yaml"
          && grep -q '017b2543493c812fc617d92d7bc376a8144c5535' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr-2518/command.yaml"
