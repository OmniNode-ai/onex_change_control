---
schema_version: "1.0.0"
ticket_id: "OMN-12909"
title: "Gateway P0A outbound attach proof: forwarder node and fail-closed ACL onboarding"
summary: >-
  Central evidence for the first P0A gateway implementation slice. omnibase_infra PR #1946 adds
  the provider-neutral node_bus_forwarder_effect scaffold, gateway envelope/config models, canonical
  topic validation, and ARCH-002-safe transform handlers/service. omninode_infra PR #496 hardens
  tenant onboarding so Kafka ACL provisioning fails closed, rejects reserved identities, and adds
  gateway NetworkPolicy conformance checks for avoiding the unauthenticated Redpanda 9092 listener.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
  - "protocols"
  - "envelopes"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1946"
    description: >-
      omnibase_infra PR #1946 carries the gateway forwarder P0A node scaffold with provider-neutral
      cloud broker refs, canonical topic validation, and focused local validators.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-omnibase-infra-pr-1946/command.yaml"
          && grep -q '^pr_number: 1946$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-omnibase-infra-pr-1946/command.yaml"
          && grep -q 'af9162ec7c79b6e34124ac7038f235d2bc5ee3fb' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-omnibase-infra-pr-1946/command.yaml"
  - id: "dod-omninode-infra-pr-496"
    description: >-
      omninode_infra PR #496 carries fail-closed tenant Kafka ACL onboarding, reserved identity
      rejection, and gateway NetworkPolicy conformance tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-omninode-infra-pr-496/command.yaml"
          && grep -q '^pr_number: 496$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-omninode-infra-pr-496/command.yaml"
          && grep -q 'c29017796d0b3600bd3e6fd976f4c836cce0753a' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-omninode-infra-pr-496/command.yaml"
  - id: "dod-local-validation"
    description: >-
      Focused local validation passed for both downstream implementation slices before PR open.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-local-validation/command.yaml"
          && grep -q '16 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-local-validation/command.yaml"
          && grep -q '220 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-local-validation/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-local-validation/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      The onex_change_control PR carries the central OMN-12909 contract and receipts required by
      downstream Receipt Gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-12909"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12909/dod-occ-pr/command.yaml"
