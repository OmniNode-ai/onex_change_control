---
schema_version: "1.0.0"
ticket_id: "OMN-11067"
title: "Stabilize stability-test runtime ingress startup"
summary: >-
  Central OCC evidence for omnibase_infra PR #1607. Replaces the local runtime socket host bind mount
  with a runtime-owned tmpfs at the existing contract path and adds startup cleanup for partially initialized
  runtime resources.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused Pattern B, local ingress, compose, and startup cleanup tests pass."
    command: "See drift/dod_receipts/OMN-11067/dod-focused-tests/command.yaml"
  - kind: "manual"
    description: "Stability compose renders /run/onex-runtime as owned tmpfs, not a host bind mount."
    command: "See drift/dod_receipts/OMN-11067/dod-compose-render/command.yaml"
  - kind: "manual"
    description: "omnibase_infra PR #1607 carries the runtime ingress startup fix."
    command: "See drift/dod_receipts/OMN-11067/dod-infra-pr-1607/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR #1038 carries the central OMN-11067 contract and receipts."
    command: "See drift/dod_receipts/OMN-11067/dod-occ-pr-1038/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused local ingress, Pattern B, startup cleanup, and model roundtrip tests pass."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11067/dod-focused-tests/command.yaml
          && grep -q '73 passed' drift/dod_receipts/OMN-11067/dod-focused-tests/command.yaml"
  - id: "dod-compose-render"
    description: "Compose render proves runtime socket tmpfs replaces the host bind mount."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11067/dod-compose-render/command.yaml
          && grep -q '/run/onex-runtime:uid=1000,gid=1000,mode=0770' drift/dod_receipts/OMN-11067/dod-compose-render/command.yaml"
  - id: "dod-infra-pr-1607"
    description: "omnibase_infra PR #1607 carries the OMN-11067 runtime startup fix."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11067/dod-infra-pr-1607/command.yaml
          && grep -q '^pr_number: 1607$' drift/dod_receipts/OMN-11067/dod-infra-pr-1607/command.yaml"
  - id: "dod-occ-pr-1038"
    description: "onex_change_control PR #1038 carries the central OMN-11067 contract and receipts."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11067/dod-occ-pr-1038/command.yaml
          && grep -q '^pr_number: 1038$' drift/dod_receipts/OMN-11067/dod-occ-pr-1038/command.yaml"
