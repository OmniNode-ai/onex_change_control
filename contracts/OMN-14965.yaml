---
schema_version: "1.0.0"
ticket_id: "OMN-14965"
title: "Autobind OCC evidence for OMN-14965"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_core PR #1521.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1521 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_core-pr-1521"
    description: "PR #1521 on OmniNode-ai/omnibase_core — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #1521 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} --repo ${REPO} --name-only | grep -qiE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "occ-self-bind-pr-5405"
    description: "Binds OMN-14965 to OCC companion PR #5405 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-assessment-gh-api-5412"
    description: "Append-only replacement for dod-deploy-assessment using a pinned product-tree probe
      for omnibase_core#1521."
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/runtime/runtime_local.py?ref=f9b1bc3770cde995b5b4ecc0181a7e44f20af8eb
          --jq .content | base64 -d | grep -q 'from omnibase_core.errors.model_onex_error import ModelOnexError'
  - id: "occ-self-bind-pr-5412"
    description: "Binds OMN-14965 to OCC repair PR #5412."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/5412 --jq '.number == 5412 and .state ==
          "open" and .head.ref == "codex-a/omn-14965-deploy-evidence-repair"' | grep -qx true
