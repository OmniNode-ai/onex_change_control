---
schema_version: "1.0.0"
ticket_id: OMN-7261
title: "feat: task-boundary test hooks — constrained (Workstream E Phase 1)"
summary: "feat: task-boundary test hooks — constrained (Workstream E Phase 1)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "EnumHookBit appends TASK_BOUNDARY_TESTS at bit 58"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'TASK_BOUNDARY_TESTS = 1 << 58' src/omnibase_core/enums/enum_hook_bit.py"
  - id: "dod-002"
    description: "Focused EnumHookBit unit tests pass at PR head"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/test_enum_hook_bit.py -q"
  - id: "dod-003"
    description: "Touched enum and unit test files pass ruff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnibase_core/enums/enum_hook_bit.py tests/unit/test_enum_hook_bit.py"
  - id: "dod-004"
    description: "Central OCC contract and receipts satisfy receipt gate locally"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python -m omnibase_core.validation.receipt_gate_cli --pr-body '' --pr-title 'feat(enums):
          add TASK_BOUNDARY_TESTS bit to EnumHookBit [OMN-7261]' --contracts-dir contracts --receipts-dir
          drift/dod_receipts"
