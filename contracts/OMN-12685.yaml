---
schema_version: "1.0.0"
ticket_id: "OMN-12685"
title: "Workstream F: fresh-verify delegation compat wire closeout"
summary: >-
  Central evidence that omnimarket's OMN-12659 compat-wire closeout is complete on current dev:
  the stale omnibase_compat.contracts.delegation.wire path is absent under src, the Bifrost
  adapter-local config shim is deleted, and a regression guard now fails if the deleted compat
  wire path returns.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-compat-wire-guard"
    description: "Focused omnimarket tests and grep prove delegation compat-wire refs remain deleted."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12685/dod-omnimarket-compat-wire-guard/command.yaml"
