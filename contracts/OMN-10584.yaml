---
schema_version: "1.0.0"
ticket_id: "OMN-10584"
title: "AdapterEnvSecretStore nullable env-var-backed ProtocolSecretStore"
summary: >
  Add AdapterEnvSecretStore in omnibase_infra.adapters as a local-dev/no-Infisical ProtocolSecretStore
  implementation backed by os.environ. Missing keys return None; callers own required-value validation.
  OMN-10584.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-integration-tests"
    description: "omnibase_infra PR #1519 env secret store unit and integration tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          if [ "$REPO" = "OmniNode-ai/onex_change_control" ]; then grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10584/dod-unit-integration-tests/command.yaml";
          else uv run --group dev python -m pytest tests/unit/adapters/test_env_secret_store.py tests/integration/adapters/test_env_secret_store_integration.py
          -q; fi
  - id: "dod-mypy"
    description: "AdapterEnvSecretStore passes strict mypy protocol validation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          if [ "$REPO" = "OmniNode-ai/onex_change_control" ]; then grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10584/dod-mypy/command.yaml";
          else uv run --group dev python -m mypy src/omnibase_infra/adapters/adapter_env_secret_store.py
          --strict; fi
  - id: "dod-deploy"
    description: >
      Deploy evidence: AdapterEnvSecretStore is a library adapter used by callers at import/runtime. No
      live deploy or .201 restart was performed by this PR; post-merge smoke is import and protocol assignment
      in the target runtime environment.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10584/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10584 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10584/dod-occ-pr/command.yaml"
