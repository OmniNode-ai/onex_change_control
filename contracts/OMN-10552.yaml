---
schema_version: "1.0.0"
ticket_id: "OMN-10552"
title: "chore: move omnimarket work-tracking contracts"
summary: "Move omnimarket work-tracking OMN yaml files out of the runtime contracts directory, add a cross-repo
  reference audit, and prove runtime code no longer depends on the legacy path."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Receipt Gate passes on omnimarket PR #538 after this OCC evidence lands."
    command: "gh pr checks 538 --repo OmniNode-ai/omnimarket --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Contracts directory reference audit runs and emits the cross-repo CSV."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10552/dod-001/command.yaml\"; else OMNI_HOME=${OMNI_HOME:-/Users/jonah/Code/omni_home}
          bash scripts/audit/contracts_dir_references_audit.sh && test -f docs/audits/2026-05-05-contracts-dir-references.csv;
          fi"
  - id: "dod-002"
    description: "Structural and dod_verify tests pass after the move."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10552/dod-002/command.yaml\"; else uv run pytest
          tests/unit/structure/ tests/unit/nodes/node_dod_verify/ -q; fi"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10552 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10552/dod-occ-pr/command.yaml\""
