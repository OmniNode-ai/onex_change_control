---
schema_version: "1.0.0"
ticket_id: OMN-11271
title: "Handoff CI enforcement: pre-commit + CI check that handoff docs include infra health section"
summary: >
  Wire a CI check and pre-commit hook in omniclaude that rejects handoff documents missing the mandatory
  ## Infra Health section (OMN-8867 spec). Both layers block unconditionally — no warn-only mode.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Pre-commit hook and GHA workflow added in omniclaude PR"
    command: "gh pr view 1617 --repo OmniNode-ai/omniclaude --json state,statusCheckRollup"
  - kind: "manual"
    description: "6 unit tests cover accept/reject/ignore paths for the hook"
    command: "gh pr view 1617 --repo OmniNode-ai/omniclaude --json additions,deletions"
  - kind: "ci"
    description: "onex_change_control PR #1174 binds OMN-11271 central evidence to an OCC PR receipt"
    command: "gh pr checks 1174 --repo OmniNode-ai/onex_change_control"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Pre-commit hook check-handoff-infra-health.sh added to omniclaude"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11271/dod-001/command.yaml\"
          && grep -q '^pr_number: 1617$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11271/dod-001/command.yaml\""
  - id: "dod-002"
    description: "GHA workflow handoff-infra-health-gate.yml added to omniclaude"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11271/dod-002/command.yaml\"
          && grep -q '^pr_number: 1617$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11271/dod-002/command.yaml\""
  - id: "dod-003"
    description: "6 unit tests pass for the hook (accept/reject/ignore paths)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11271/dod-003/command.yaml\"
          && grep -q '^pr_number: 1617$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11271/dod-003/command.yaml\""
  - id: "dod-onex-change-control-pr-1174"
    description: "onex_change_control#1174 OCC evidence PR is bound to OMN-11271."
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11271/dod-onex-change-control-pr-1174/command.yaml\"
          && grep -q '^ticket_id: OMN-11271$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11271/dod-onex-change-control-pr-1174/command.yaml\"
          && grep -q '^pr_number: 1174$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11271/dod-onex-change-control-pr-1174/command.yaml\""
