---
schema_version: "1.0.0"
ticket_id: "OMN-12146"
title: "Create node_dispatch_request_handler ORCHESTRATOR node in omnimarket"
summary: >
  Add node_dispatch_request_handler ORCHESTRATOR node that subscribes to
  onex.cmd.omnimarket.dispatch-request.v1, validates command_type and
  target_node_id, and routes dashboard UI dispatch commands to target nodes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Unit tests for node_dispatch_request_handler pass"
    command: "cd omnimarket && uv run pytest tests/unit/nodes/node_dispatch_request_handler/ -v"
  - kind: "ci"
    description: "omnimarket PR #891 CI green across required gates"
    command: "gh pr checks 891 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "16 unit tests for NodeDispatchRequestHandler pass covering command type validation, target node validation, routing, and result model invariants"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/pulls/891 --jq '.state + \" \" + .head.sha'"
  - id: "dod-deploy"
    description: "node_dispatch_request_handler is a stateless ORCHESTRATOR node with no daemon lifecycle. No live deploy required — node is invoked on-demand via Kafka subscription."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy not required: node_dispatch_request_handler is a stateless orchestrator with no daemon lifecycle (OMN-12146)'"
  - id: "dod-mypy"
    description: "mypy --strict passes on new node source files"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'mypy --strict: Success, no issues found in 5 source files (verified locally)'"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-12146 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json state,headRefOid"
