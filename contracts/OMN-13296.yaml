---
schema_version: "1.0.0"
ticket_id: "OMN-13296"
title: "W2: skip-token pre-commit parity — add reject-deploy-gate-skip-token hook to spi/intel/mem/n_infra"
summary: >-
  Adds the reject-deploy-gate-skip-token pre-commit hook (pre-commit + commit-msg stages) to omnibase_spi,
  omniintelligence, omnimemory, and omninode_infra. The CI mirror call-reject-skip.yml was already present
  in all 4 repos; this closes the local enforcement gap (GAP-9, validator-standardization-remediation-plan
  §3 W2). Pure parity, near-zero risk.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_spi-pr-237"
    description: >-
      Per-code-PR binding receipt for omnibase_spi PR #237 (jonah/omn-13296-skip-token-omnibase_spi, base=dev).
      Adds reject-deploy-gate-skip-token pre-commit hook to omnibase_spi. This receipt binds OMN-13296
      to omnibase_spi PR #237 by pr_number and commit_sha so downstream Receipt Gate can resolve central
      OCC evidence for the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13296/dod-omnibase_spi-pr-237/command.yaml
  - id: "dod-omniintelligence-pr-717"
    description: >-
      Per-code-PR binding receipt for omniintelligence PR #717 (jonah/omn-13296-skip-token-omniintelligence,
      base=dev). Adds reject-deploy-gate-skip-token pre-commit hook to omniintelligence. This receipt
      binds OMN-13296 to omniintelligence PR #717 by pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13296/dod-omniintelligence-pr-717/command.yaml
  - id: "dod-omnimemory-pr-369"
    description: >-
      Per-code-PR binding receipt for omnimemory PR #369 (jonah/omn-13296-skip-token-omnimemory, base=dev).
      Adds reject-deploy-gate-skip-token pre-commit hook to omnimemory. This receipt binds OMN-13296 to
      omnimemory PR #369 by pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13296/dod-omnimemory-pr-369/command.yaml
  - id: "dod-omninode_infra-pr-507"
    description: >-
      Per-code-PR binding receipt for omninode_infra PR #507 (jonah/omn-13296-skip-token-omninode_infra,
      base=dev). Adds reject-deploy-gate-skip-token pre-commit hook to omninode_infra. This receipt binds
      OMN-13296 to omninode_infra PR #507 by pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13296/dod-omninode_infra-pr-507/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13296 contract and all 4 repo
      PR bindings. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check can
      resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13296/dod-occ-pr-self/command.yaml
