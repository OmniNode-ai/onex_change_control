---
schema_version: "1.0.0"
ticket_id: "OMN-12682"
title: "Fix-set #5: derive ServiceTopicRegistry defaults from the topic catalog"
summary: >-
  Central evidence that omnibase_infra PR #1870 removes embedded onex.* topic literals from ServiceTopicRegistry.from_defaults(),
  resolves defaults through the provisioned topic suffix catalog, and promotes previously registry-only
  topics into named catalog constants and ModelTopicSpec entries. Regression coverage proves from_defaults()
  has no embedded topic strings and every resolved default is backed by the topic spec catalog.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-topic-registry-catalog"
    description: "Focused omnibase_infra tests and static checks prove ServiceTopicRegistry defaults derive
      from catalog symbols."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12682/dod-omnibase-infra-topic-registry-catalog/command.yaml"
          && grep -q '^pr_number: 1870$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12682/dod-omnibase-infra-topic-registry-catalog/command.yaml"
  - id: "dod-deploy-gate-no-runtime-mutation"
    description: "Deploy-gate evidence for omnibase_infra topic catalog code changes in PR #1870."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12682/dod-deploy-gate-no-runtime-mutation/command.yaml"
          && grep -q '^pr_number: 1870$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12682/dod-deploy-gate-no-runtime-mutation/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-12682 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12682/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2175$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12682/dod-occ-pr/command.yaml"
