---
schema_version: "1.0.0"
ticket_id: "OMN-13234"
title: "Typed tier cost model on delegation wire DTOs (free_local / metered / budgeted)"
summary: >-
  Add the canonical typed per-tier cost model to the delegation routing wire DTOs: EnumTierCostType (free_local
  | metered | budgeted), ModelTierCost (rate/cap/overage with regime-aware validation), a typed cost field
  on ModelRoutingTier, and cost-measurement carrier fields on ModelTaskDelegatedEvent (cost_tier_type,
  cost_tier_name, cost_measurement_source, budget_headroom_consumed_usd). These are the shared cross-repo
  models the omnimarket routing parser, pricing computation, and delegation_events projection consume
  (OMN-13234). Proven by the omnibase_core wire-model unit suite (79 passing) and the omnimarket tier-cost
  suite (11 passing).
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1290"
    description: >-
      omnibase_core PR #1290 wire-model unit tests green including ModelTierCost regime validation (free_local
      zero-rate, metered positive-rate, budgeted cap+overage), ModelRoutingTier typed-cost acceptance,
      and ModelTaskDelegatedEvent carrier fields.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '79 passed' drift/dod_receipts/OMN-13234/dod-core-pr-1290/command.yaml"
  - id: "dod-market-pr-1320-tier-cost"
    description: >-
      omnimarket PR #1320 tier-cost suite green: routing_tiers.yaml typed cost blocks parse into ModelTierCost
      and pricing.compute_tier_cost_usd computes per-regime cost.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '11 passed' drift/dod_receipts/OMN-13234/dod-market-pr-1320-tier-cost/command.yaml"
  - id: "dod-deploy"
    description: >-
      Deploy-gate evidence: OMN-13234 in omnibase_core is a pure typed-model addition (Pydantic DTOs +
      enum) with no runtime process, broker topic, or service deploy; omnimarket consumes the symbols
      on the next core pin bump and runtime redeploy. The probe greps the changed core module and proves
      the typed-cost symbols are present and that no migration / Dockerfile / compose file was added by
      the core PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'EnumTierCostType class definition' drift/dod_receipts/OMN-13234/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the savings-cluster central contracts and receipts
      used by the product PR Receipt Gates via Evidence-Source. The probe greps the OCC tree and proves
      contracts/OMN-13234.yaml is present (carried by this OCC PR).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13234\"$' contracts/OMN-13234.yaml"
