---
schema_version: "1.0.0"
ticket_id: "OMN-14015"
title: "fix(OMN-14015): local delegation evidence DB is a config choice"
summary: >-
  omnimarket PR #1634 removes the hardcoded SQLite evidence database default from local delegation dispatch
  composition. The evidence database is now resolved from the projection runtime binding overlay at the
  composition root, preserving byte-identical SQLite fallback behavior when no overlay is configured while
  allowing the local path to target the platform Postgres substrate when the lane declares that binding.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Targeted delegation, projection, lint, type, and import checks passed locally"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14015/dod-local-verification/command.yaml"
  - kind: "manual"
    description: "omnimarket PR #1634 carries the OMN-14015 implementation"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14015/dod-omnimarket-pr-1634/command.yaml"
  - kind: "manual"
    description: "Runtime-path change has deploy-scope evidence and no live deploy mutation"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14015/dod-deploy-scope/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-local-verification"
    description: >-
      Local verification covered targeted delegation plus projection tests, the new PostgresSyncProjectionAdapter
      SQL construction tests, evidence DB resolution tests, ruff format/check, strict mypy for omnimarket,
      runtime entrypoint import sweep, and pre-commit for the changed files.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14015/dod-local-verification/command.yaml"
  - id: "dod-omnimarket-pr-1634"
    description: >-
      Consuming-PR binding: omnimarket PR #1634 at head 4517b5a566f14f43da8426ad81469335869ba825 carries
      the OMN-14015 implementation and verification summary.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14015/dod-omnimarket-pr-1634/command.yaml"
  - id: "dod-deploy-scope"
    description: >-
      Deploy-scope binding: this runtime-path delegation storage change rides the normal post-merge dev
      and stability redeploy cadence; no out-of-band production deploy is in scope. Live Postgres round-trip
      proof requires a deployed lane and is explicitly deferred until after merge and redeploy.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14015/dod-deploy-scope/command.yaml
          && echo no-out-of-band-deploy"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-14015 central contract and
      PASS receipts consumed by omnimarket PR #1634 via its Evidence-Source line.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14015/dod-occ-self/command.yaml"
