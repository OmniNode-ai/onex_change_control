---
schema_version: "1.0.0"
ticket_id: "OMN-15041"
title: "Autobind OCC evidence for OMN-15041"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1880."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1880 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1880"
    description: "PR #1880 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1880-ci"
    description: "PR #1880 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4738"
    description: "OCC companion PR #4738 — self-bind for OMN-15041 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-assessment-pr-1880"
    description: >-
      Deploy-surface assessment for omnimarket PR #1880 (OMN-15041), head 61bd431a91c44b6b74928817b665f45ce55c323c:
      a hermetic, source-only COMPUTE-node change (node_runtime_sweep's CONSUMER_LIVENESS phase gains
      a new pure check + allowlist). No docker-compose, Dockerfile, deploy/, or migration path is touched;
      no new topic, service, or container is registered; nothing is deployed or restarted by this PR.
      Falsifiable per OMN-14505/OMN-14443 (live GitHub content read at the pinned commit SHA, not a self-authored
      receipt read-back): asserts the real merged source and test files actually contain the new UNDECLARED_CONSUMER_GROUP
      symbol at that exact commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_runtime_sweep/handlers/handler_runtime_sweep.py?ref=61bd431a91c44b6b74928817b665f45ce55c323c"
          --jq .content | base64 -d | grep -q 'UNDECLARED_CONSUMER_GROUP' && gh api "repos/OmniNode-ai/omnimarket/contents/tests/nodes/test_omn_15041_undeclared_consumer_group.py?ref=61bd431a91c44b6b74928817b665f45ce55c323c"
          --jq .content | base64 -d | grep -q 'UNDECLARED_CONSUMER_GROUP'
