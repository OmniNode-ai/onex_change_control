---
schema_version: "1.0.0"
ticket_id: "OMN-18334"
title: "Autobind OCC evidence for OMN-18334"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnidash PR #318.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #318 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #318 on OmniNode-ai/omnidash, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnidash`.
    command: "uv run pytest tests/ci/test_occ_companion_effect_edited_trigger_omn_18334.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnidash-pr-318"
    description: "PR #318 on OmniNode-ai/omnidash — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnidash/contents/tests/ci/test_occ_companion_effect_edited_trigger_omn_18334.py?ref=c66bde356b3087fb3d7f3ba01d980e238520c074 --jq '.content' | base64 -d | grep -c 'def _pull_request_types'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnidash/pulls/318/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/call-occ-companion-effect.yml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #318 on OmniNode-ai/omnidash — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/ci/test_occ_companion_effect_edited_trigger_omn_18334.py -q"
        cwd: "${OMNI_HOME}/omnidash"
