---
schema_version: "1.0.0"
ticket_id: "OMN-10870"
title: "DI refactor: HandlerAbCompareOrchestrator replaces Any effect_handler with protocol injection"
summary: >
  HandlerAbCompareOrchestrator.__init__ accepted effect_handler: Any | None erasing the protocol contract.
  Introduces ProtocolLlmEffectHandler runtime_checkable Protocol with async handle(). effect_handler param
  now typed as ProtocolLlmEffectHandler | None. DI fallback construction of HandlerLlmOpenaiCompatible
  annotated with lifecycle-ok: optional-di-fallback. Part of OMN-10841 DI Wave 2 refactor.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests-pass"
    description: "4 new injection-path unit tests pass: protocol exported, fake satisfies protocol, typed
      injection, end-to-end injection used in handle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10870/dod-unit-tests-pass/command.yaml\""
  - id: "dod-omnimarket-pr-691"
    description: "omnimarket PR #691 open with CI green at commit 382674e2b5b62ba6dd826e6a8e45b78026adeba8."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10870/dod-omnimarket-pr-691/command.yaml\"
          && grep -q '^pr_number: 691$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10870/dod-omnimarket-pr-691/command.yaml\""
  - id: "dod-occ-pr-1120"
    description: "onex_change_control PR #1120 carries the OMN-10870 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10870/dod-occ-pr-1120/command.yaml\"
          && grep -q '^pr_number: 1120$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10870/dod-occ-pr-1120/command.yaml\""
