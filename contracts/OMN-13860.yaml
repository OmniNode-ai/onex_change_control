---
schema_version: "1.0.0"
ticket_id: "OMN-13860"
title: "feat(OMN-13860): aislop_sweep hardcoded-paths check"
summary: >-
  omnimarket PR #1582 adds the hardcoded-paths check to node_aislop_sweep, closing a live enforcement
  hole where aislop_sweep's own SKILL.md already documented a `hardcoded-paths` check category that the
  handler never implemented (CLAUDE.md rule #6: no /Users/... or /Volumes/... literals in source). Pure-compute
  detector addition mirroring the existing ARCH-005 invariant in node_architectural_invariant_loop; no
  runtime redeploy required.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1582"
    description: >-
      Consuming-PR binding: omnimarket PR #1582 carries the OMN-13860 hardcoded-paths check with focused
      test coverage (tests/test_aislop_hardcoded_paths.py, 10 tests) and a verified planted-canary dogfood
      run.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13860/dod-omnimarket-pr-1582/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for the runtime path touched by PR #1582. node_aislop_sweep is a pure COMPUTE
      node (no I/O beyond reading target directories); this change adds a detection pattern only and requires
      no live deploy to take effect on the next sweep dispatch.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13860/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13860 contract and receipts consumed
      by omnimarket PR #1582.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13860/dod-occ-self/command.yaml"
