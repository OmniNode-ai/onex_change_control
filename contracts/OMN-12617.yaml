---
schema_version: "1.0.0"
ticket_id: "OMN-12617"
title: "Support Vertex ADC for the SEA ADK runtime"
summary: >-
  Central evidence for onex-self-extending-agent PR #200. The PR updates the SEA ADK runtime and model
  registry contract path to support Vertex ADC-backed model access, including strict typing repairs, registry
  example coverage, CLI/demo pipeline wiring, agent orchestrator runtime wiring, and focused unit tests
  for the ADK runtime and model registry contract.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-200-binding"
    description: "Bind OMN-12617 central OCC evidence to onex-self-extending-agent PR #200 and its current
      head commit."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12617/dod-sea-pr-200-binding/command.yaml"
          && grep -q '^pr_number: 200$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12617/dod-sea-pr-200-binding/command.yaml"
          && grep -q '^commit_sha: "eb34714fb74cd68eb97e362f419cec8fad173ce6"$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12617/dod-sea-pr-200-binding/command.yaml"
  - id: "dod-sea-pr-200-ci-validation"
    description: "The source PR reports successful lint, typecheck, test, security, PR title, CodeRabbit
      thread, and skip-token checks; only Receipt Gate remains blocked awaiting central OCC evidence and
      downstream Evidence-Source wiring."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12617/dod-sea-pr-200-ci-validation/command.yaml"
          && grep -q 'lint.*SUCCESS' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12617/dod-sea-pr-200-ci-validation/command.yaml"
          && grep -q 'typecheck.*SUCCESS' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12617/dod-sea-pr-200-ci-validation/command.yaml"
          && grep -q 'test.*SUCCESS' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12617/dod-sea-pr-200-ci-validation/command.yaml"
          && grep -q 'security.*SUCCESS' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12617/dod-sea-pr-200-ci-validation/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12617 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12617/dod-occ-pr-self/command.yaml"
