---
schema_version: "1.0.0"
ticket_id: OMN-10765
title: "feat(OMN-10765): port change-aware test selection to omniintelligence"
summary: >
  Ports the detect_test_paths pipeline from omnibase_core (OMN-9852) to omniintelligence. Adds scripts/ci/
  with detect_test_paths.py, test_selection_loader.py, test_selection_models.py, and test_selection_adjacency.yaml.
  Adds pytest-split to dev deps. Updates ci.yml with detect-changes job and matrix-driven test-unit job.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: All CI checks pass on omniintelligence PR  #645
    command: >
      gh pr checks 645 --repo OmniNode-ai/omniintelligence | grep -v "fail\|pending" | wc -l
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: scripts/ci/detect_test_paths.py present in omniintelligence
    source: generated
    checks:
      - check_type: command
        check_value: >
          gh api "repos/OmniNode-ai/omniintelligence/contents/scripts/ci/detect_test_paths.py?ref=jonah/omn-10765-port-change-aware-test-selection-to-omniintelligence"
          --jq '.name'
  - id: dod-002
    description: test_selection_adjacency.yaml present in omniintelligence
    source: generated
    checks:
      - check_type: command
        check_value: >
          gh api "repos/OmniNode-ai/omniintelligence/contents/scripts/ci/test_selection_adjacency.yaml?ref=jonah/omn-10765-port-change-aware-test-selection-to-omniintelligence"
          --jq '.name'
  - id: dod-003
    description: 17 unit tests pass for the detect_test_paths module
    source: generated
    checks:
      - check_type: command
        check_value: >
          gh pr view 645 --repo OmniNode-ai/omniintelligence --json statusCheckRollup --jq '[.statusCheckRollup[]
          | select(.name != null) | select(.name | startswith("Unit Tests"))] | length'
  - id: dod-004
    description: PR cites this ticket
    source: generated
    checks:
      - check_type: command
        check_value: >
          gh pr view 645 --repo OmniNode-ai/omniintelligence --json title,body --jq '.title + " " + (.body
          // "")' | grep -q "OMN-10765"
