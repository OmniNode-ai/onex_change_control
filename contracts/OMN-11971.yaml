---
schema_version: "1.0.0"
ticket_id: "OMN-11971"
title: "Clean hardcoded model IDs from omniintelligence tests/fixtures"
summary: >
  Central OCC evidence for omniintelligence PR #671, which replaces hardcoded
  model ID strings in omniintelligence tests and fixtures with symbolic constants
  from tests/fixtures/model_constants.py.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-pr-671"
    description: "omniintelligence PR #671 replaces hardcoded model ID strings in tests with symbolic constants."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11971/dod-omniintelligence-pr-671/command.yaml
          && grep -q '^pr_number: 671$' drift/dod_receipts/OMN-11971/dod-omniintelligence-pr-671/command.yaml"
