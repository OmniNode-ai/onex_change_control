---
schema_version: "1.0.0"
ticket_id: "OMN-12878"
title: "[build] Guardrail: prefer logical api_key_ref over api_key_env"
summary: >-
  Tracks validator enforcement requiring logical secret_ref/api_key_ref over new api_key_env use, with
  compatibility-only treatment for legacy api_key_env during migration.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-api-key-ref-guard"
    description: >-
      Failing api_key_env fixture trips the guard, logical api_key_ref fixture passes, and routing-authority
      checks no longer treat api_key_env as preferred authority.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12878/dod-api-key-ref-guard/command.yaml"
  - id: "dod-occ-pr-self"
    description: "OCC self-binding receipt for the central evidence PR carrying the OMN-12878 receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12878/dod-occ-pr-self/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for omnibase_core PR #1317: the runtime-path change is a pure validator/authority
      guardrail update and requires no live deploy during PR validation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12878/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2992-self"
    description: "OCC self-binding receipt for the central evidence PR carrying the OMN-12878 deploy assessment."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12878/dod-occ-pr-2992-self/command.yaml"
