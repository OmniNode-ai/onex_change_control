---
schema_version: "1.0.0"
ticket_id: "OMN-12457"
title: "chore(OMN-12457): omninode_infra: add managed gitignore blocks"
summary: >
  Appends two onex-managed blocks (universal and python) to omninode_infra/.gitignore. Ensures __pycache__,
  *.py[cod], .venv, .pytest_cache, .mypy_cache, .ruff_cache and related Python build artifacts are consistently
  ignored. No previously-tracked files were affected.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >
      .gitignore in omninode_infra contains both onex-managed blocks (universal and python) with correct
      start/end markers. The cross-repo proof is captured in the paired OCC receipt; the compliance check
      asserts that receipt is PASS.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12457/dod-001/command.yaml && grep
          -q '^pr_number: 479$' drift/dod_receipts/OMN-12457/dod-001/command.yaml"
  - id: "dod-occ-pr-1920"
    description: >
      onex_change_control PR #1920 carries the OMN-12457 central contract and receipt evidence for omninode_infra
      PR #479.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12457/dod-occ-pr-1920/command.yaml
          && grep -q '^pr_number: 1920$' drift/dod_receipts/OMN-12457/dod-occ-pr-1920/command.yaml"
