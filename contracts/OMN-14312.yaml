---
schema_version: "1.0.0"
ticket_id: "OMN-14312"
title: "refactor(OMN-14312): canonicalize PR review handlers to thin contract+handler"
summary: >-
  omnimarket PR #1698 canonicalizes node_judge_verdict_parse_compute and node_pr_review_orchestrator handlers
  to return typed outputs directly instead of ModelHandlerOutput wrappers. The branch was refreshed onto
  current dev before evidence capture.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1698-head-45d0e127"
    description: >-
      Foreground Codex verification of omnimarket PR #1698 at head 45d0e12758efd2c369de89274e489061229915bd
      after rebasing onto current dev. Local focused checks passed: PR review golden-chain and integration
      pytest suite (33 passed), ruff check and ruff format --check on changed handler/test paths, CI-shaped
      mypy src/omnimarket --strict, strict changed-node state coverage, changed-node golden-chain coverage,
      unimported-handler wiring, strict node-drift gate, and market_node_runtime_dogfood over 364 node
      dirs.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14312/dod-omnimarket-pr-1698-head-45d0e127/command.yaml
          && grep -q '^pr_number: 1698$' drift/dod_receipts/OMN-14312/dod-omnimarket-pr-1698-head-45d0e127/command.yaml
          && grep -q '45d0e12758efd2c369de89274e489061229915bd' drift/dod_receipts/OMN-14312/dod-omnimarket-pr-1698-head-45d0e127/command.yaml
  - id: "dod-deploy-assessment-head-45d0e127"
    description: >-
      Deploy-surface assessment for omnimarket PR #1698 head 45d0e12758efd2c369de89274e489061229915bd:
      the diff is limited to two PR review handler direct-return refactors and focused tests. It does
      not mutate live deployment configuration, perform a deploy/restart, change topics, run migrations,
      or touch .201.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14312/dod-deploy-assessment-head-45d0e127/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14312/dod-deploy-assessment-head-45d0e127/command.yaml
  - id: "dod-occ-self-commit-initial"
    description: >-
      OCC companion self-binding: the initial onex_change_control commit for OMN-14312 carries this central
      contract plus the product and deploy PASS receipts for omnimarket PR #1698 head 45d0e12758efd2c369de89274e489061229915bd.
      The follow-up receipt binds that initial OCC evidence commit for occ-preflight eligibility before
      the OCC PR is merged.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14312/dod-occ-self-commit-initial/command.yaml
          && grep -Eq '^[[:space:]]*commit_sha: [0-9a-f]{40}$' drift/dod_receipts/OMN-14312/dod-occ-self-commit-initial/command.yaml
