---
schema_version: "1.0.0"
ticket_id: "OMN-17210"
title: "Autobind OCC evidence for OMN-17210"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1094."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1094 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1094 on OmniNode-ai/omninode_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omninode_infra`.
    command: |-
      uv run pytest tests/k8s/test_managed_kafka_rollout_roster.py tests/k8s/test_onex_dev_managed_postgres_networkpolicy.py tests/k8s/test_post_deploy_verify_env_roster.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1094"
    description: "PR #1094 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1094 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1094-ci"
    description: "PR #1094 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1094 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #1094 on OmniNode-ai/omninode_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1094"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/k8s/test_managed_kafka_rollout_roster.py tests/k8s/test_onex_dev_managed_postgres_networkpolicy.py tests/k8s/test_post_deploy_verify_env_roster.py -q
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-scope DoD for OMN-17210 so both product PRs clear the deploy-gate. The autobind wrote only
      a `uv run pytest` behavior proof, which the gate correctly rejects as non-falsifiable: pytest's
      exit status cannot depend on the state of any deployed or remote surface. All three checks below
      put a `gh api` read of the LIVE dev branch in command position, and each greps for a symbol that
      exists nowhere on dev today -- so each is RED right now and only goes GREEN once the corresponding
      PR actually lands. None reads back a receipt or contract this ticket's own PRs author.
    source: "generated"
    checks:
      # omnimarket#2225 -- the receipt-gate writer class must be on dev.
      # RED until #2225 merges: node_projection_receipt_gate ships only the
      # pure reducer and its handle_dict shim today, no BaseProjectionRunner.
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_receipt_gate/handlers/handler_receipt_gate.py?ref=dev --jq .content | base64 -d | grep -q 'class HandlerReceiptGateProjectionRunner(BaseProjectionRunner)'
      # omninode_infra#1094 -- the savings writer Deployment must be on dev AND
      # wired into the overlay. RED until #1094 merges: the manifest does not
      # exist on dev and the kustomization lists twelve runtime Deployments.
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/contents/k8s/onex-dev/runtime/deployment-omnimarket-projection-savings-writer.yaml?ref=dev --jq .content | base64 -d | grep -q 'omnimarket.nodes.node_projection_savings.handlers.handler_savings'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/contents/k8s/onex-dev/runtime/kustomization.yaml?ref=dev --jq .content | base64 -d | grep -q 'deployment-omnimarket-projection-savings-writer.yaml'
  - id: "occ-self-bind-pr-7720"
    description: "OCC companion PR #7720 — self-bind for OMN-17210 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7720 --repo OmniNode-ai/onex_change_control --json number,state"
