---
schema_version: '1.0.0'
ticket_id: OMN-10359
title: "Task 7: [Wave 2 Completion-Verify] File-target resolution + path-escape guard"
summary: "Add resolve_file_targets() to completion_verify.py — extracts quoted file paths from task text,
  merges with files_touched, deduplicates, raises ValueError on path traversal"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "6 unit tests for resolve_file_targets pass (run in omnibase_core repo)"
    command: "gh pr checks 999 --repo OmniNode-ai/omnibase_core"
  - kind: ci
    description: "mypy --strict and ruff check pass (verified via receipts)"
    command: "grep -q 'status: PASS' drift/dod_receipts/OMN-10359/dod-002/command.yaml && grep -q 'status:
      PASS' drift/dod_receipts/OMN-10359/dod-003/command.yaml"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "6 unit tests pass — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10359/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-10359$' drift/dod_receipts/OMN-10359/dod-001/command.yaml && grep -q '^evidence_item_id:
          dod-001$' drift/dod_receipts/OMN-10359/dod-001/command.yaml"
  - id: dod-002
    description: "mypy --strict clean — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10359/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-10359$' drift/dod_receipts/OMN-10359/dod-002/command.yaml && grep -q '^evidence_item_id:
          dod-002$' drift/dod_receipts/OMN-10359/dod-002/command.yaml"
  - id: dod-003
    description: "ruff check passes — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10359/dod-003/command.yaml && grep
          -q '^ticket_id: OMN-10359$' drift/dod_receipts/OMN-10359/dod-003/command.yaml && grep -q '^evidence_item_id:
          dod-003$' drift/dod_receipts/OMN-10359/dod-003/command.yaml"
