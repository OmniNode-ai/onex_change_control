---
schema_version: "1.0.0"
ticket_id: "OMN-14600"
title: "OCC companion for omnibase_core#1439 — ModelDelegationCompleted/ModelDelegationFailed terminal
  subclasses"
summary: >
  OCC evidence companion for OmniNode-ai/omnibase_core#1439 (OMN-14600). #1439 adds two thin terminal
  subclasses of ModelDelegationResult — ModelDelegationCompleted and ModelDelegationFailed — in src/omnibase_core/models/delegation/wire/model_delegation_result.py,
  and exports them from the wire package __init__. The two classes add NO new fields and model_dump()
  is byte-identical to a plain ModelDelegationResult; their SOLE purpose is distinct class identity so
  the delegation routing layer (one topic per class) can map COMPLETED and FAILED terminals to separate
  topics, replacing the earlier single bespoke ModelDelegationEventEnvelope{topic,payload} carrier the
  routing layer could never resolve (one wrapped class covered both outcomes on one topic-per-class map).
  Every substantive check below is a CONTENT READ pinned to #1439's head commit SHA (b2d2b43dce147da823b009bffef9d8ff31e44c87).
  It observes the source #1439 actually wrote, so it does not depend on #1439's CI — which matters because
  #1439's occ-preflight is red precisely BECAUSE this contract was missing (missing Evidence-Source).
  A check keyed on #1439's CI would be circular. No check greps this repo's own receipt corpus for "status:
  PASS". The GREEN direction was verified independently: an implementer-blind reviewer (not the author
  of #1439) checked out the PR head in a worktree and ran `uv run pytest tests/ -k delegation -m "not
  slow"` -> 215 passed, 4 skipped (matching the PR's cited proof), then imported both subclasses and asserted
  each subclasses ModelDelegationResult with a distinct class identity. Each product-tree read below carries
  its own RED control: the same command at omnibase_core dev HEAD (79c620f904526f773f6b92eb98743b011fe2eb12
  — the fix commit's exact parent), where #1439 has not landed, finds zero occurrences and would fail.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_core#1439 head SHA defines and exports the two terminal delegation subclasses"
    command: "see dod_evidence check_values (content reads pinned to b2d2b43d)"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-verify-1439-completed-subclass"
    description: >-
      Product-observing: reads the actual source of src/omnibase_core/models/delegation/wire/model_delegation_result.py
      at #1439's exact head commit SHA (b2d2b43dce147da823b009bffef9d8ff31e44c87) via the GitHub contents
      API and asserts it defines "class ModelDelegationCompleted(ModelDelegationResult)" — the COMPLETED
      terminal subclass whose distinct class identity is the entire substance of the fix (it lets the
      topic-per-class routing map the success terminal to its own topic). RED DIRECTION: the class does
      not exist at omnibase_core dev HEAD (79c620f904526f773f6b92eb98743b011fe2eb12 — the fix commit's
      parent; grep -c == 0, exit 1); it exits 0 only at #1439's head (grep -c == 1).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api "repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/models/delegation/wire/model_delegation_result.py?ref=b2d2b43dce147da823b009bffef9d8ff31e44c87"
          --jq .content | base64 -d | grep -c "class ModelDelegationCompleted(ModelDelegationResult)")"
          -ge 1
  - id: "dod-verify-1439-failed-subclass"
    description: >-
      Product-observing: reads the same source at #1439's head commit SHA and asserts it defines "class
      ModelDelegationFailed(ModelDelegationResult)" — the FAILED terminal subclass, the second half of
      the two-class split (the failure terminal routes to its own topic). RED DIRECTION: the class does
      not exist at dev HEAD (79c620f904526f773f6b92eb98743b011fe2eb12 — grep -c == 0, exit 1); it exits
      0 only at #1439's head (grep -c == 1).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api "repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/models/delegation/wire/model_delegation_result.py?ref=b2d2b43dce147da823b009bffef9d8ff31e44c87"
          --jq .content | base64 -d | grep -c "class ModelDelegationFailed(ModelDelegationResult)")" -ge
          1
  - id: "dod-verify-1439-wire-exports"
    description: >-
      Product-observing: reads src/omnibase_core/models/delegation/wire/__init__.py at #1439's head commit
      SHA and asserts it re-exports ModelDelegationCompleted (both the import and the __all__ entry —
      grep count == 2), so downstream consumers and the routing layer can import the new terminal classes
      from the canonical wire package. RED DIRECTION: the symbol is absent from the same file at dev HEAD
      (79c620f904526f773f6b92eb98743b011fe2eb12 — grep -c == 0, exit 1); it exits 0 only at #1439's head
      (grep -c == 2).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api "repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/models/delegation/wire/__init__.py?ref=b2d2b43dce147da823b009bffef9d8ff31e44c87"
          --jq .content | base64 -d | grep -c "ModelDelegationCompleted")" -ge 1
  - id: "dod-deploy-assessment-1439"
    description: >-
      Deploy-gate assessment for omnibase_core#1439. The PR changes exactly two files — the delegation
      wire result model and its package __init__ — neither a Dockerfile, compose file, nor deploy script;
      it is a pure model/wire-schema change (byte-identical model_dump), not a live runtime rollout, and
      deploy-gate resolves it deploy-neutral. Falsifiable: the command fails if the PR diff ever grows
      a Dockerfile/compose/deploy change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          ! (gh pr diff 1439 --repo OmniNode-ai/omnibase_core --name-only | grep -qE "docker-compose|Dockerfile|deploy/")
  - id: "dod-occ-self-bind-pr-4134"
    description: >-
      Self-bind receipt for this OCC evidence companion. Binds an OMN-14600 PASS receipt to this companion's
      own PR (OCC#4134, branch jonah/omn-14600-occ-companion) so onex_change_control's own occ-merge-eligibility
      gate resolves the companion — the product-observing items above all bind to omnibase_core PR #1439,
      so without this item nothing binds to #4134 itself (reason pr_ticket_mismatch). Binding/stamp item
      only (L0); the product content-reads above carry the substance floor.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'jonah/omn-14600-occ-companion' drift/dod_receipts/OMN-14600/dod-occ-self-bind-pr-4134/command.yaml
          && grep -q 'pr_number: 4134' drift/dod_receipts/OMN-14600/dod-occ-self-bind-pr-4134/command.yaml
  - id: "dod-infra-pr-2309"
    description: >-
      Product-observing (2nd-consumer / merged-path bridge, OMN-14623): reads pyproject.toml at omnibase_infra#2309's
      exact head commit SHA (36c17914660d83644e9435f680353e104ca6c0be) via the GitHub contents API and
      asserts the [tool.uv.sources] git-rev override pins omnibase-core to rev "a9f25887a51851ddf24258e6a7a507e7e56b28d8"
      — the MERGED squash commit of #1439 on dev that carries the ModelDelegationCompleted/ModelDelegationFailed
      terminal classes this OMN-14600 contract already covers. #2309 is the infra half of the downstream
      re-pin (the core fix already merged via OCC#4134; this item binds the SECOND consumer PR to the
      same ticket). RED DIRECTION: the override is absent at omnibase_infra dev HEAD (84f7a940ec537062db04b4183a3014cd98bb39a8
      — the commit #2309 is based on; grep -c == 0, exit 1); it exits 0 only at #2309's head (grep -c
      == 1).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/pyproject.toml?ref=36c17914660d83644e9435f680353e104ca6c0be"
          --jq '.content' | base64 -d | grep -c 'rev = "a9f25887a51851ddf24258e6a7a507e7e56b28d8"')" -ge
          1
  - id: "dod-infra-pr-2309-recovery-sweep"
    description: >-
      Product-observing: reads src/omnibase_infra/runtime/auto_wiring/handler_wiring.py at #2309's head
      commit SHA and asserts it defines the module-level constant "_STATE_IO_RECOVERY_SWEEP_INTERVAL_SECONDS
      = 30.0" — the periodic re-run interval that makes the state_io outbox recovery sweep opportunistic
      (re-runs every 30s instead of once per adapter lifetime), the substance of #2309's in-flight-lock
      defer-strand recovery fix. RED DIRECTION: the constant is absent at omnibase_infra dev HEAD (84f7a940ec537062db04b4183a3014cd98bb39a8;
      grep -c == 0, exit 1); it exits 0 only at #2309's head (grep -c == 1).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=36c17914660d83644e9435f680353e104ca6c0be"
          --jq '.content' | base64 -d | grep -c '_STATE_IO_RECOVERY_SWEEP_INTERVAL_SECONDS = 30.0')" -ge
          1
  - id: "dod-market-pr-1762"
    description: >-
      Product-observing (2nd-consumer / merged-path bridge, OMN-14623): reads pyproject.toml at omnimarket#1762's
      exact head commit SHA (4fbf642ad83a4a36b9909decafc33a989518bfdf) via the GitHub contents API and
      asserts the [tool.uv.sources] git-rev override pins omnibase-core to rev "a9f25887a51851ddf24258e6a7a507e7e56b28d8"
      — the same merged #1439 commit dod-infra-pr-2309 above binds to, superseding the prior OMN-14402
      pin (d08bb231, a strict ancestor). #1762 is the market half of the downstream re-pin. RED DIRECTION:
      the override still points at d08bb231 (not a9f25887) at omnimarket dev HEAD (b4839eac4a615cd0df787e71e8ca8570704b64a8;
      grep -c == 0, exit 1); it exits 0 only at #1762's head (grep -c == 1).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api "repos/OmniNode-ai/omnimarket/contents/pyproject.toml?ref=4fbf642ad83a4a36b9909decafc33a989518bfdf"
          --jq '.content' | base64 -d | grep -c 'rev = "a9f25887a51851ddf24258e6a7a507e7e56b28d8"')" -ge
          1
  - id: "dod-market-pr-1762-terminal-emit"
    description: >-
      Product-observing: reads src/omnimarket/models/delegation/wire/__init__.py at #1762's head commit
      SHA and asserts it re-exports ModelDelegationCompleted (import + __all__ entry, grep count == 2)
      — the market-side consumption of core#1439's two-class terminal split (replaces the retired bespoke
      ModelDelegationEventEnvelope carrier). RED DIRECTION: the symbol is absent from the same file at
      omnimarket dev HEAD (b4839eac4a615cd0df787e71e8ca8570704b64a8; grep -c == 0, exit 1); it exits 0
      only at #1762's head (grep -c == 2).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/models/delegation/wire/__init__.py?ref=4fbf642ad83a4a36b9909decafc33a989518bfdf"
          --jq '.content' | base64 -d | grep -c 'ModelDelegationCompleted')" -ge 2
  - id: "dod-occ-self-bind-pr-4147"
    description: >-
      Self-bind receipt for this OCC evidence companion (merged-path 2nd-consumer bridge). Binds an OMN-14600
      PASS receipt to this companion's own PR (OCC#4147, branch jonah/omn-14600-merged-path-occ-companion)
      so onex_change_control's own occ-merge-eligibility gate resolves the companion — the product-observing
      items above bind to omnibase_infra#2309 and omnimarket#1762, so without this item nothing binds
      to #4147 itself (reason pr_ticket_mismatch). Binding/stamp item only (L0); the product content-reads
      above carry the substance floor.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'jonah/omn-14600-merged-path-occ-companion' drift/dod_receipts/OMN-14600/dod-occ-self-bind-pr-4147/command.yaml
          && grep -q 'pr_number: 4147' drift/dod_receipts/OMN-14600/dod-occ-self-bind-pr-4147/command.yaml
  - id: "dod-infra-pr-2309-head-c464fe89-core-pin"
    description: >-
      Product-observing follow-up after #2309 CI repair moved the product head to c464fe891afc766a3f856c0e9c625b5a4e8bb06f.
      Reads pyproject.toml at that exact head commit via the GitHub contents API and asserts the [tool.uv.sources]
      git-rev override still pins omnibase-core to rev "a9f25887a51851ddf24258e6a7a507e7e56b28d8" (the
      merged #1439 squash commit already covered by this OMN-14600 contract). This supersedes the earlier
      #2309 product-head binding at 36c17914660d83644e9435f680353e104ca6c0be after the CI repair commit.
      RED DIRECTION: the override is absent at omnibase_infra dev HEAD 84f7a940ec537062db04b4183a3014cd98bb39a8
      (grep count 0); it exits 0 only at the repaired #2309 head (grep count 1).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/pyproject.toml?ref=c464fe891afc766a3f856c0e9c625b5a4e8bb06f"
          --jq '.content' | base64 -d | grep -c 'rev = "a9f25887a51851ddf24258e6a7a507e7e56b28d8"')" -ge
          1
  - id: "dod-infra-pr-2309-head-c464fe89-ci-refresh"
    description: >-
      Product-observing follow-up for the #2309 CI repair commit. Reads the repaired head c464fe891afc766a3f856c0e9c625b5a4e8bb06f
      and asserts both CI-unblocking surfaces are present: the regenerated runner image lock binds identity_digest
      "8a9355b9644c810c16a778947e31e4cc", and the HTTP default handler contract contains handler_routing.version
      required by the upgraded omnibase_core ModelHandlerContract. RED DIRECTION: the old #2309 head 36c17914660d83644e9435f680353e104ca6c0be
      has neither the regenerated identity nor handler_routing.version in that HTTP handler contract,
      so this command fails there and exits 0 only at the repaired head.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/docker/runners/runner-image.lock.json?ref=c464fe891afc766a3f856c0e9c625b5a4e8bb06f"
          --jq '.content' | base64 -d | grep -c '8a9355b9644c810c16a778947e31e4cc')" -ge 1 && test "$(gh
          api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/contracts/handlers/http/handler_contract.yaml?ref=c464fe891afc766a3f856c0e9c625b5a4e8bb06f"
          --jq '.content' | base64 -d | grep -A4 -c '^handler_routing:$')" -ge 1 && gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/contracts/handlers/http/handler_contract.yaml?ref=c464fe891afc766a3f856c0e9c625b5a4e8bb06f"
          --jq '.content' | base64 -d | awk '/^handler_routing:/{flag=1; next} flag && /^  version:/{found=1}
          END{exit found ? 0 : 1}'
  - id: "dod-infra-pr-2309-head-05198996-lock-json"
    description: >-
      Product-observing follow-up after #2309 moved to 05198996007de6601ab01358c6100d3f2eed2811 to repair
      the generated runner image lock JSON syntax. Reads docker/runners/runner-image.lock.json at that
      exact head via the GitHub contents API, asserts it parses as JSON with python -m json.tool, and
      asserts the identity_digest remains "8a9355b9644c810c16a778947e31e4cc". RED DIRECTION: the previous
      #2309 head c464fe891afc766a3f856c0e9c625b5a4e8bb06f carries a trailing comma in the same JSON file,
      so python -m json.tool fails there; this exits 0 only at the repaired head.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api "repos/OmniNode-ai/omnibase_infra/contents/docker/runners/runner-image.lock.json?ref=05198996007de6601ab01358c6100d3f2eed2811"
          --jq '.content' | base64 -d | tee /tmp/omn14600-runner-image.lock.json | python -m json.tool
          >/tmp/omn14600-runner-image.lock.pretty.json && grep -q '"identity_digest": "8a9355b9644c810c16a778947e31e4cc"'
          /tmp/omn14600-runner-image.lock.pretty.json
  - id: "dod-infra-2309-1ebc8a16-honesty-install"
    description: >-
      Product-observing follow-up after #2309 moved to 1ebc8a1634157419932b8016c04d214c88c202e9 to repair
      the receipt-honesty workflow's omnibase_core validator install. Reads .github/workflows/receipt-honesty.yml
      at that exact head via the GitHub contents API and asserts the workflow now creates a seeded Python
      venv and installs the cloned omnibase_core validator package with `pip install --no-deps`, avoiding
      the failed attempt to resolve unpublished PyPI package omnibase-core==0.46.8. RED DIRECTION: the
      previous #2309 head 05198996007de6601ab01358c6100d3f2eed2811 still contains the failing `uv pip
      install --python .honesty-venv/bin/python .ci/omnibase_core` line, so the positive grep count for
      the seeded/no-deps repair is 0 there; it exits 0 only at the repaired head.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/receipt-honesty.yml?ref=1ebc8a1634157419932b8016c04d214c88c202e9"
          --jq '.content' | base64 -d | grep -q 'uv venv .honesty-venv --python 3.12 --seed' && gh api
          "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/receipt-honesty.yml?ref=1ebc8a1634157419932b8016c04d214c88c202e9"
          --jq '.content' | base64 -d | grep -q '.honesty-venv/bin/python -m pip install --no-deps .ci/omnibase_core'
  - id: "dod-market-pr-1762-head-6a21564a-core-pin"
    description: >-
      Product-observing follow-up after #1762 CI repair moved the product head to 6a21564ab7bd39d7fc1307b74f7cd80b4dbc1826.
      Reads pyproject.toml at that exact head commit via the GitHub contents API and asserts the [tool.uv.sources]
      git-rev override still pins omnibase-core to rev "a9f25887a51851ddf24258e6a7a507e7e56b28d8" (the
      merged #1439 squash commit already covered by this OMN-14600 contract). This supersedes the earlier
      #1762 product-head binding at 4fbf642ad83a4a36b9909decafc33a989518bfdf after the cross-node reach-in
      repair. RED DIRECTION: omnimarket dev HEAD b4839eac4a615cd0df787e71e8ca8570704b64a8 still points
      at the prior pin, so grep count is 0 there; it exits 0 only at the repaired #1762 head.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api "repos/OmniNode-ai/omnimarket/contents/pyproject.toml?ref=6a21564ab7bd39d7fc1307b74f7cd80b4dbc1826"
          --jq '.content' | base64 -d | grep -c 'rev = "a9f25887a51851ddf24258e6a7a507e7e56b28d8"')" -ge
          1
  - id: "dod-market-1762-head-6a21564a-rehome"
    description: >-
      Product-observing follow-up for the #1762 cross-node reach-in CI repair. Reads the repaired head
      6a21564ab7bd39d7fc1307b74f7cd80b4dbc1826 and asserts the swarm subtask state reducer imports ModelLlmDelegationCallRequest
      and ModelSwarmFanoutResult from omnimarket.events.* rather than sibling node-local models. RED DIRECTION:
      the old #1762 head 4fbf642ad83a4a36b9909decafc33a989518bfdf imports both models from omnimarket.nodes.*.models,
      so this command fails there and exits 0 only at the repaired head.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_swarm_subtask_state_reducer/handlers/handler_swarm_subtask_state.py?ref=6a21564ab7bd39d7fc1307b74f7cd80b4dbc1826"
          --jq '.content' | base64 -d | grep -q 'from omnimarket.events.llm_delegation_call import ModelLlmDelegationCallRequest'
          && gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_swarm_subtask_state_reducer/handlers/handler_swarm_subtask_state.py?ref=6a21564ab7bd39d7fc1307b74f7cd80b4dbc1826"
          --jq '.content' | base64 -d | grep -q 'from omnimarket.events.swarm_fanout import ModelSwarmFanoutResult'
  - id: "dod-occ-self-bind-pr-4149"
    description: >-
      Self-bind receipt for this OCC follow-up companion. Binds an OMN-14600 PASS receipt to OCC#4149
      (branch jonah/omn-14600-occ-followup-head-refresh) so onex_change_control's own occ-merge-eligibility
      gate resolves the companion after the four product-observing follow-up items above bind only to
      omnibase_infra#2309 and omnimarket#1762. Binding/stamp item only (L0); the product content reads
      above carry the substance floor.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'jonah/omn-14600-occ-followup-head-refresh' drift/dod_receipts/OMN-14600/dod-occ-self-bind-pr-4149/command.yaml
          && grep -q 'pr_number: 4149' drift/dod_receipts/OMN-14600/dod-occ-self-bind-pr-4149/command.yaml
  - id: "dod-occ-self-bind-pr-4151"
    description: >-
      Self-bind receipt for this OCC lock-json follow-up companion. Binds an OMN-14600 PASS receipt to
      OCC#4151 (branch jonah/omn-14600-occ-infra-lock-json-05198996) so onex_change_control's own occ-merge-eligibility
      gate resolves the companion after the product-observing item above binds only to omnibase_infra#2309.
      Binding/stamp item only (L0); the product content read above carries the substance floor.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'jonah/omn-14600-occ-infra-lock-json-05198996' drift/dod_receipts/OMN-14600/dod-occ-self-bind-pr-4151/command.yaml
          && grep -q 'pr_number: 4151' drift/dod_receipts/OMN-14600/dod-occ-self-bind-pr-4151/command.yaml
  - id: "dod-occ-self-bind-pr-4153"
    description: >-
      Self-bind receipt for this OCC receipt-honesty workflow follow-up companion. Binds an OMN-14600
      PASS receipt to OCC#4153 (branch jonah/omn-14600-occ-infra-receipt-honesty-1ebc8a16) so onex_change_control's
      own occ-merge-eligibility gate resolves the companion after the product-observing item above binds
      only to omnibase_infra#2309. Binding/stamp item only (L0); the product content read above carries
      the substance floor.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'jonah/omn-14600-occ-infra-receipt-honesty-1ebc8a16' drift/dod_receipts/OMN-14600/dod-occ-self-bind-pr-4153/command.yaml
          && grep -q 'pr_number: 4153' drift/dod_receipts/OMN-14600/dod-occ-self-bind-pr-4153/command.yaml
