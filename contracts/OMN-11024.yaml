---
schema_version: "1.0.0"
ticket_id: OMN-11024
title: "Add canonical delegation wire DTO package in omnibase_compat"
summary: "Bind omnibase_compat PR #90 to central OCC evidence for the canonical delegation wire DTO package."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "omnibase_compat PR #90 carries the canonical delegation wire DTO package and local verification."
    command: "gh pr view 90 --repo OmniNode-ai/omnibase_compat --json state,headRefOid"
  - kind: "tests"
    description: "Focused delegation wire DTO round-trip and validation tests pass."
    command: "uv run pytest src/omnibase_compat/tests/contracts/delegation/wire/test_delegation_wire_dtos.py
      -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-implementation-pr"
    description: "omnibase_compat PR #90 is open and points at the OMN-11024 implementation head."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11024/dod-implementation-pr/command.yaml
          && grep -q '^pr_number: 90$' drift/dod_receipts/OMN-11024/dod-implementation-pr/command.yaml
          && grep -q '381789b' drift/dod_receipts/OMN-11024/dod-implementation-pr/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused delegation wire DTO tests passed locally before opening PR #90."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11024/dod-focused-tests/command.yaml
          && grep -q '7 passed' drift/dod_receipts/OMN-11024/dod-focused-tests/command.yaml && grep -q
          'test_delegation_wire_dtos.py' drift/dod_receipts/OMN-11024/dod-focused-tests/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR #1016 carries the central OMN-11024 contract and receipts."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11024/dod-occ-pr/command.yaml &&
          grep -q '^pr_number: 1016$' drift/dod_receipts/OMN-11024/dod-occ-pr/command.yaml && grep -q
          '0c096480' drift/dod_receipts/OMN-11024/dod-occ-pr/command.yaml"
