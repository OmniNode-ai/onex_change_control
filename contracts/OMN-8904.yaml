---
schema_version: "1.0.0"
ticket_id: "OMN-8904"
title: "Machine-class overlay profiles and seed-infisical extension"
summary: >-
  Adds machine-class overlay profiles for mac-dev, linux-server, and cloud-k8s, plus Infisical project
  registry scaffolding and seed-infisical support for machine-class dry runs.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-machine-class-overlay-tests-pr-1767"
    description: "omnibase_infra PR #1767 carries machine-class overlay and seed-infisical unit tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title"
  - id: "dod-machine-class-precommit-pr-1767"
    description: "omnibase_infra PR #1767 reports all pre-commit hooks passing."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title"
  - id: "dod-seed-infisical-dry-run-pr-1767"
    description: "omnibase_infra PR #1767 verifies seed-infisical --machine-class dry-run behavior without
      requiring live Infisical connectivity."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title"
  - id: "dod-occ-pr-1721"
    description: "OCC PR #1721 carries the OMN-8904 contract and receipt binding for omnibase_infra PR
      #1767."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title"
  - id: "machine-class-overlay-tests-pr-1767-r15481"
    description: "Append-only replacement for dod-machine-class-overlay-tests-pr-1767. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1767 on OmniNode-ai/omnibase_infra, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at e06b60fc48427da778880df86291f8f57e2e88b6,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-machine-class-overlay-tests-pr-1767"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1767 --repo OmniNode-ai/omnibase_infra --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'e06b60fc48427da778880df86291f8f57e2e88b6' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1767 --repo OmniNode-ai/omnibase_infra --json files --jq '.files[].path'
          | grep -cxF 'tests/unit/config/test_machine_class_overlays.py' | grep -qx 1"
  - id: "machine-class-precommit-pr-1767-r15481"
    description: "Append-only replacement for dod-machine-class-precommit-pr-1767. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1767 on OmniNode-ai/omnibase_infra, and `gh pr view --json` with no assertion exits 0
      for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at e06b60fc48427da778880df86291f8f57e2e88b6,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-machine-class-precommit-pr-1767"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1767 --repo OmniNode-ai/omnibase_infra --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'e06b60fc48427da778880df86291f8f57e2e88b6' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1767 --repo OmniNode-ai/omnibase_infra --json statusCheckRollup --jq
          '.statusCheckRollup[] | select((.name // .context) == \"Lint\") | (.conclusion // .state)' |
          grep -cxF 'SUCCESS' | grep -qx 1"
  - id: "seed-infisical-dry-run-pr-1767-r15481"
    description: "Append-only replacement for dod-seed-infisical-dry-run-pr-1767. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1767 on OmniNode-ai/omnibase_infra, and `gh pr view --json` with no assertion exits 0
      for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at e06b60fc48427da778880df86291f8f57e2e88b6,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-seed-infisical-dry-run-pr-1767"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1767 --repo OmniNode-ai/omnibase_infra --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'e06b60fc48427da778880df86291f8f57e2e88b6' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1767 --repo OmniNode-ai/omnibase_infra --json files --jq '.files[].path'
          | grep -cxF 'scripts/seed-infisical.py' | grep -qx 1"
  - id: "occ-pr-1721-r15481"
    description: "Append-only replacement for dod-occ-pr-1721. The historical entry is immutable and stays
      byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner placeholders, which the
      compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never pinned PR #1721 on OmniNode-ai/onex_change_control,
      and `gh pr view --json` with no assertion exits 0 for any PR that exists (mis-bound AND unfalsifiable).
      This entry pins the literal PR number and repo and asserts two facts the selector does not determine:
      the PR is MERGED at 8c827c92c4b5ef4343a8e07cd07db8c1b24a32a1, plus a per-item distinguishing binding.
      Both checks executed GREEN under `bash -o pipefail -c` at 2026-07-30T09:32:56Z and are RED-proven
      by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-pr-1721"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1721 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '8c827c92c4b5ef4343a8e07cd07db8c1b24a32a1' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1721 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-8904.yaml' | grep -qx 1"
