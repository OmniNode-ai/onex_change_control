---
schema_version: "1.0.0"
ticket_id: "OMN-10365"
title: "[Wave 3 Co-Change] CLI script + JSON output"
summary: "Add scripts/analysis/co_change_dark_matter.py — a CLI entry point with three modes: --json prints
  {\"pairs\": [{\"a\": \"tokenA\", \"b\": \"tokenB\", \"npmi\": 1.23, \"co_changes\": 42}]} to stdout;
  --write-state writes the same JSON to .onex_state/co-change-map.json (mkdir -p, overwrite); no args
  prints human-readable top-20 table. Exits 0 even on empty result. Part of the selective swarm pattern
  adoption epic OMN-10350."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "10 CLI tests pass covering all 4 acceptance criteria"
    command: "cd omnibase_core && uv run pytest tests/scripts/test_co_change_dark_matter_cli.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "All 10 CLI tests pass (--json, --write-state, no-args, exit-0 modes)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10365/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-10365$' drift/dod_receipts/OMN-10365/dod-001/command.yaml && grep -q '10
          passed' drift/dod_receipts/OMN-10365/dod-001/command.yaml"
