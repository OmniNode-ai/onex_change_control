---
schema_version: "1.0.0"
ticket_id: OMN-12380
title: "Integrate or retire orphaned omnibase_spi protocol packages surfaced by Repowise"
summary: >
  Cross-repo verification of orphaned omnibase_spi protocols. Retired
  ProtocolLLMAgentProvider after cross-repo proof; surfaced file_handling
  LACKS_INTEGRATION protocols. Shipped via merged omnibase_spi PR #230.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "protocols"
evidence_requirements:
  - kind: "ci"
    description: "omnibase_spi PR #230 merged to main"
    command: "gh pr view 230 --repo OmniNode-ai/omnibase_spi --json state"
  - kind: "docs"
    description: "Cross-repo SPI protocol classification recorded in OMN-12368 verification doc"
    command: "test -f docs/evidence/OMN-12368-verification/spi-protocols.md"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-merged"
    description: "omnibase_spi PR #230 is merged (durable code proof)"
    source: "ci"
    checks:
      - check_type: "command"
        check_value: "test \"$(gh pr view 230 --repo OmniNode-ai/omnibase_spi --json state --jq .state)\" = MERGED"
  - id: "dod-classification-doc"
    description: "SPI protocol cross-repo classification evidence doc is tracked in omni_home"
    source: "docs"
    checks:
      - check_type: "command"
        check_value: "git -C \"$OMNI_HOME\" ls-files --error-unmatch docs/evidence/OMN-12368-verification/spi-protocols.md"
