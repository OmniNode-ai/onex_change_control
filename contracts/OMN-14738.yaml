---
schema_version: "1.0.0"
ticket_id: "OMN-14738"
title: "Evidence for OMN-14738 managed-staging canary teardown and rollback runbook"
summary: >-
  Central OCC evidence for omnibase_infra#2336. The product PR adds the managed-staging canary teardown
  and rollback runbook at exact product head 311bf53bfaec23e9e7d5b6d4492be92b3c041827.
is_seam_ticket: false
interface_change: false
evidence_requirements:
  - kind: ci
    description: "PR #2336 product head is exact for OMN-14738 evidence"
    command: 'PRODUCT_PR_NUMBER=2336; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=311bf53bfaec23e9e7d5b6d4492be92b3c041827;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
  - kind: ci
    description: "PR #2336 product-readiness-shadow completed successfully at exact product head"
    command: 'PRODUCT_RUN_ID=29627689742; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=311bf53bfaec23e9e7d5b6d4492be92b3c041827;
      : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
      = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
      = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-omnibase_infra-pr-2336
    description: "PR #2336 on OmniNode-ai/omnibase_infra exists and is bound to OMN-14738 at exact product
      head 311bf53bfaec23e9e7d5b6d4492be92b3c041827."
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2336; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=311bf53bfaec23e9e7d5b6d4492be92b3c041827;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnibase_infra-pr-2336-diff
    description: "PR #2336 on OmniNode-ai/omnibase_infra has the expected docs-only runbook diff for OMN-14738."
    source: generated
    checks:
      - check_type: command
        check_value: "PRODUCT_PR_NUMBER=2336; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          test \"$(gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only)\" = \"docs/runbooks/managed-staging-canary-teardown-rollback.md\""
  - id: dod-omnibase_infra-pr-2336-product-readiness
    description: "The product-readiness-shadow workflow completed successfully for omnibase_infra PR #2336
      at exact product head 311bf53bfaec23e9e7d5b6d4492be92b3c041827 before OCC evidence was minted."
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29627689742; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=311bf53bfaec23e9e7d5b6d4492be92b3c041827;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-omnibase_infra-pr-2336-local-verification
    description: "Product PR body records local verification for OMN-14738: pre-commit green and full
      pre-push unit suite 21544 passed / 40 skipped."
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2336; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "21544 passed,
          40 skipped"'
  - id: dod-omnibase_infra-pr-2336-deploy
    description: "Deploy-readiness assessment for OMN-14738. The PR is docs-only; no live MSK connect/apply,
      AWS, ECR, deploy, restart, .201, or production mutation was performed by this OCC evidence lane."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14738/dod-omnibase_infra-pr-2336-deploy/command.yaml
          && grep -qi 'docs-only' drift/dod_receipts/OMN-14738/dod-omnibase_infra-pr-2336-deploy/command.yaml"
  - id: occ-self-bind-pr-4361
    description: "OCC PR #4361 on OmniNode-ai/onex_change_control self-binding proof for OMN-14738 evidence."
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
