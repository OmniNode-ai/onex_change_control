---
schema_version: "1.0.0"
ticket_id: OMN-8009
title: "fix: doc_freshness_sweep PosixPath→str bug silently zeroes all results"
summary: "fix: doc_freshness_sweep PosixPath→str bug silently zeroes all results"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,baseRefName --jq 'select(.state
          == \"OPEN\" and .baseRefName == \"main\")'"
  - id: "dod-002"
    description: "PR carries the OMN-8009 implementation"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json title,headRefName --jq 'select((.title
          + \" \" + .headRefName) | contains(\"OMN-8009\"))'"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --repo ${REPO} --json name,state --jq 'map(select(.name
          == \"Tests\" and .state == \"SUCCESS\")) | if length > 0 then empty else error(\"Tests check
          is not green\") end'"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --repo ${REPO} --json name,state --jq 'map(select(.name
          == \"Pre-commit\" and .state == \"SUCCESS\")) | if length > 0 then empty else error(\"Pre-commit
          check is not green\") end'"
