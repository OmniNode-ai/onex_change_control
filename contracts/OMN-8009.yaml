---
schema_version: "1.0.0"
ticket_id: OMN-8009
title: "fix: doc_freshness_sweep PosixPath→str bug silently zeroes all results"
summary: "fix: doc_freshness_sweep PosixPath→str bug silently zeroes all results"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Regression tests pass confirming PosixPath input returns non-zero results"
    command: "uv run pytest tests/unit/test_doc_freshness.py -k posixpath -v"
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks 556 --repo OmniNode-ai/onex_change_control"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Regression tests for PosixPath input pass locally"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8009/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-8009$' drift/dod_receipts/OMN-8009/dod-001/command.yaml"
  - id: "dod-002"
    description: "Full test suite passes locally (1268 tests)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8009/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-8009$' drift/dod_receipts/OMN-8009/dod-002/command.yaml"
  - id: "dod-003"
    description: "mypy --strict passes on src/"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8009/dod-003/command.yaml && grep
          -q '^ticket_id: OMN-8009$' drift/dod_receipts/OMN-8009/dod-003/command.yaml"
  - id: "dod-004"
    description: "PR #556 created and auto-merge armed"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8009/dod-004/command.yaml && grep
          -q '^pr_number: 556$' drift/dod_receipts/OMN-8009/dod-004/command.yaml"
