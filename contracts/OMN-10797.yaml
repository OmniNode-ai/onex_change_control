---
schema_version: "1.0.0"
ticket_id: OMN-10797
title: "fix(OMN-10797): condition evaluator supports quoted-literal LHS"
summary: >
  OMN-10779 followup. The interactive onboarding policy uses LHS literals such as `"llm_inference" in
  selected_local_services`; the evaluator was treating every LHS as a state key and raising ConditionEvaluationError.
  Extend the LHS grammar to accept quoted literals; quote the affected policy conditions. Unblocks failing
  Tests (Split 2/15) job on omnibase_infra.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-unit-tests
    description: "All 49 condition_evaluator unit + integration tests pass on omnibase_infra branch."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10797/dod-unit-tests/command.yaml
          && grep -q '^ticket_id: OMN-10797$' drift/dod_receipts/OMN-10797/dod-unit-tests/command.yaml"
  - id: dod-occ-pr
    description: "Self-binding receipt for this OCC PR."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10797/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: OMN-10797$' drift/dod_receipts/OMN-10797/dod-occ-pr/command.yaml"
