---
schema_version: "1.0.0"
ticket_id: "OMN-10746"
title: "DI injection seam — child ticket of OMN-10841 handler injection cleanup wave 1"
summary: >
  Binds omnimarket PR #689 which adds optional DI injection seam to the handler addressed by this child
  ticket of epic OMN-10841. See parent contract OMN-10841.yaml for full context.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #689 merged to main with CI green."
    command: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json state,mergeCommit"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-689"
    description: "omnimarket PR #689 (DI injection seams for 3 handlers) merged to main."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json state --jq .state |
          grep -q MERGED"
  - id: "dod-deploy"
    description: >
      Pure DI refactor — no runtime behavior change. Constructor signature extension only.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo deploy: docker exec omninode-runtime python -c 'import omnimarket.nodes.node_overseer_verifier;
          import omnimarket.nodes.node_pr_review_bot; import omnimarket.nodes.node_baseline_compare'"
