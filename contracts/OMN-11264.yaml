---
schema_version: "1.0.0"
ticket_id: "OMN-11264"
title: "feat(OMN-11241): Wave 5 Task 17 — Proof of Life with Eval Composition"
summary: >
  Full pipeline proof of life including eval node composition. Generate node -> deploy -> generate eval
  node -> deploy eval -> run multi-model eval -> verify learning loop closes. Replaces original Task 14
  scope.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Generate + deploy sentiment classifier. Generate + deploy eval node. Multi-model eval
      results with per-model scores. Second generation run informed by eval results. Evidence bundle written."
    command: "gh pr view 27 --repo OmniNode-ai/onex-self-extending-agent --json number,state"
  - kind: "ci"
    description: "PR #27 open with CI passing"
    command: "gh pr view 27 --repo OmniNode-ai/onex-self-extending-agent --json number,state,statusCheckRollup"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-27"
    description: "onex-self-extending-agent PR #27 — full proof of life with eval composition. Sentiment
      classifier + eval node both generated and deployed via dynamic registration. Multi-model eval with
      per-model scores. Learning loop closed (2nd run shows fewer retries/better model selection). Dashboard
      shows composition + quality trend."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11264/dod-pr-27/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carrying this contract and binding receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11264/dod-occ-pr/command.yaml"
