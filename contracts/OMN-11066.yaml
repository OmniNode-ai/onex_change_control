---
schema_version: "1.0.0"
ticket_id: "OMN-11066"
title: "Mark pattern-b-dispatch topic as plugin_managed to prevent DLQ routing"
summary: >
  Fixes pattern-b-dispatch topic registration so the generic runtime treats it as plugin_managed and does
  not route messages to the DLQ. Resolves dispatch consumption regression on the .201 host.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1629 carries the implementation."
    command: "See drift/dod_receipts/OMN-11066/dod-infra-pr-1629/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1629"
    description: "omnibase_infra PR #1629 carries the OMN-11066 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11066/dod-infra-pr-1629/command.yaml
          && grep -q '^pr_number: 1629$' drift/dod_receipts/OMN-11066/dod-infra-pr-1629/command.yaml"
