---
schema_version: "1.0.0"
ticket_id: "OMN-11066"
title: "Mark pattern-b-dispatch topic as plugin_managed"
summary: >
  Marks pattern-b-dispatch topic as plugin_managed so the generic runtime dispatcher does not consume
  Pattern B commands and route them to DLQ.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Unit/integration tests covering the change pass locally."
    command: "See drift/dod_receipts/OMN-11066/dod-tests/command.yaml"
  - kind: "ci"
    description: "Pre-commit hooks pass for changed files (ruff format + ruff check + mypy)."
    command: "See drift/dod_receipts/OMN-11066/dod-lint/command.yaml"
  - kind: "ci"
    description: "omnibase_infra PR #1629 carries the implementation."
    command: "See drift/dod_receipts/OMN-11066/dod-infra-pr-1629/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Unit/integration tests for the change pass."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11066/dod-tests/command.yaml"
  - id: "dod-lint"
    description: "ruff format + ruff check pass for changed files."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11066/dod-lint/command.yaml"
  - id: "dod-infra-pr-1629"
    description: "omnibase_infra PR #1629 carries the implementation."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'pr_number: 1629' drift/dod_receipts/OMN-11066/dod-infra-pr-1629/command.yaml"
  - id: "dod-occ-pr-OCC_PR_NUMBER"
    description: "onex_change_control PR #OCC_PR_NUMBER carries the contract and receipts for OMN-11066."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11066/dod-occ-pr-OCC_PR_NUMBER/command.yaml
          && grep -q '^pr_number: OCC_PR_NUMBER$' drift/dod_receipts/OMN-11066/dod-occ-pr-OCC_PR_NUMBER/command.yaml"
