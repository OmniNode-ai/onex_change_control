---
schema_version: "1.0.0"
ticket_id: "OMN-11921"
title: "aislop validator KB integration"
summary: >
  Central OCC evidence for omnibase_core PR #1152, which adds KB integration to the aislop validator.
  Adds resolve_rules_from_registry() and resolve_rules_unified() to the aislop rule loader public API,
  enabling antipattern registry entries to feed into aislop rule resolution alongside legacy YAML rules.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1152"
    description: "omnibase_core PR #1152 adds aislop validator KB integration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11921/dod-omnibase_core-pr-1152/command.yaml
          && grep -q '^pr_number: 1152$' drift/dod_receipts/OMN-11921/dod-omnibase_core-pr-1152/command.yaml"
  - id: "dod-aislop-kb-local-validation"
    description: "16 new unit tests for aislop KB integration pass; mypy --strict clean; no regressions
      in 3960 existing tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11921/dod-aislop-kb-local-validation/command.yaml
          && grep -q '16 passed' drift/dod_receipts/OMN-11921/dod-aislop-kb-local-validation/command.yaml"
  - id: "dod-occ-pr-1585"
    description: "onex_change_control PR #1585 carries the central OMN-11921 evidence bundle."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11921/dod-occ-pr-1585/command.yaml
          && grep -q '^pr_number: 1585$' drift/dod_receipts/OMN-11921/dod-occ-pr-1585/command.yaml"
