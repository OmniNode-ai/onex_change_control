---
schema_version: "1.0.0"
ticket_id: "OMN-12684"
title: "Workstream E: select ADK runtime from contract tier"
summary: >-
  Central evidence for removing the mandatory Gemini/cloud-key ADK runtime path from SEA's --agent
  entrypoint. The ADK backend is selected from the node_agent_orchestrator contract and optional local
  overlay, with local OpenAI-compatible vLLM support for LOCAL_VLLM/NONE tiers and explicit credential
  injection preserved for selected Gemini API-key or Vertex ADC tiers.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-agent-runtime-tier-tests"
    description: "Focused SEA runtime and CLI tests prove no-cloud local ADK mode and selected Gemini credential injection."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12684/dod-sea-agent-runtime-tier-tests/command.yaml"
