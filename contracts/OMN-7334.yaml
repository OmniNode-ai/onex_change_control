---
schema_version: "1.0.0"
ticket_id: OMN-7334
title: "refactor(OMN-7334): Move 4 hardcoded topic constants to central registry"
summary: >
  Three omnibase_infra handler modules declared redundant module-level topic aliases (_TOPIC_BASELINES_COMPUTED,
  _TOPIC_REWARD_ASSIGNED, _TOPIC_POLICY_STATE_UPDATED, _GMAIL_INTENT_TOPIC) that merely re-bound an already-imported
  omnibase_infra.topics SUFFIX_* registry constant. The aliases are deleted and the central registry constants
  are referenced directly at the usage sites; reward_binder unit + integration tests now import the SUFFIX_*
  constants from omnibase_infra.topics instead of the removed handler aliases.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >
      Scoped node test suites for the three touched nodes pass after the alias removal (98 passed, 2 Kafka-skipped).
    command: >
      uv run pytest -q tests/unit/nodes/node_reward_binder_effect tests/integration/nodes/test_node_reward_binder_effect.py
      tests/unit/nodes/node_baselines_batch_compute tests/unit/nodes/node_gmail_intent_poller_effect
  - kind: ci
    description: "mypy --strict passes for the three changed node packages."
    command: >
      uv run mypy src/omnibase_infra/nodes/node_baselines_batch_compute src/omnibase_infra/nodes/node_reward_binder_effect
      src/omnibase_infra/nodes/node_gmail_intent_poller_effect --strict
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: >
      All 4 local topic aliases are removed from the three handlers and the handlers reference the omnibase_infra.topics
      SUFFIX_* registry constants directly (zero residual _TOPIC_* / _GMAIL_INTENT_TOPIC references in
      the three node packages).
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-7334/dod-001/command.yaml"
  - id: dod-002
    description: >
      Scoped node test suites (reward_binder unit + integration, baselines_batch_compute unit, gmail_intent_poller
      unit) pass: 98 passed, 2 skipped (Kafka unavailable locally).
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-7334/dod-002/command.yaml"
  - id: dod-003
    description: "mypy --strict is clean across the three changed node packages."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-7334/dod-003/command.yaml"
  - id: dod-004
    description: >
      Full unit suite green (21546 passed; the only 2 reported failures are timing-flaky runtime perf
      tests unrelated to topics, which pass serially) and pre-commit hooks pass on the changed files.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-7334/dod-004/command.yaml"
  - id: dod-005
    description: >
      onex_change_control PR #3376 carries the central OMN-7334 contract and receipt bundle used by omnibase_infra
      PR #2161.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^pr_number: 3376$' drift/dod_receipts/OMN-7334/dod-005/command.yaml"
