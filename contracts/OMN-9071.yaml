---
schema_version: "1.0.0"
ticket_id: OMN-9071
title: "Enable full runtime cron verification"
summary: "Adds omniclaude cron contract verification coverage and tests for full runtime verification
  prompts."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
evidence_requirements:
  - kind: "manual"
    description: "omniclaude PR 1608 carries the cron runtime verification implementation and local test
      evidence"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid"
  - kind: "manual"
    description: "OCC PR 1206 carries the OMN-9071 central contract and receipt evidence"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1608"
    description: "omniclaude PR 1608 is open at the recorded implementation head and the cron contract
      verification test passes locally."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid"
  - id: "dod-occ-pr-1206"
    description: "OCC PR 1206 carries the OMN-9071 central contract and receipt evidence for omniclaude
      PR 1608."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid"
