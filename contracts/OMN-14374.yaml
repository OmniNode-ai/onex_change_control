---
schema_version: "1.0.0"
ticket_id: "OMN-14374"
title: "feat(OMN-14374): wire GitHub PR state gateway into onex skill"
summary: >-
  omnibase_infra PR #2260 adds the onex skill mapping for pr_state so the CLI dispatches the existing
  node_github_repo_gateway_effect. The mapping exposes typed GitHub PR-state rows through the gateway
  request/result models instead of relying on raw gh JSON dumps, and the focused CLI test proves the generated
  payload validates against ModelGithubGatewayRequest for PR-scoped and repo-scoped operations.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2260-head-3984c390"
    description: >-
      Foreground Codex verification of omnibase_infra PR #2260 at head 3984c39071d1b2eb362c0cd2c2ad3e321e42a605.
      The exact pr_state CLI payload regression test passed, the full tests/unit/cli/test_cli_skill.py
      file passed with OMNI_HOME and OMNIMARKET_SRC pointed at the canonical sibling repos, ruff check
      and ruff format --check passed for the touched Python test, and file-scoped pre-commit passed for
      the YAML mapping plus test file.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14374/dod-omnibase-infra-pr-2260-head-3984c390/command.yaml
          && grep -q '^pr_number: 2260$' drift/dod_receipts/OMN-14374/dod-omnibase-infra-pr-2260-head-3984c390/command.yaml
          && grep -q '3984c39071d1b2eb362c0cd2c2ad3e321e42a605' drift/dod_receipts/OMN-14374/dod-omnibase-infra-pr-2260-head-3984c390/command.yaml
  - id: "dod-deploy-assessment-head-3984c390"
    description: >-
      Deploy-surface assessment for omnibase_infra PR #2260 head 3984c39071d1b2eb362c0cd2c2ad3e321e42a605:
      the diff is limited to a CLI skill mapping YAML entry and a focused unit test. It does not register
      a new service deployment, mutate runtime configuration, perform a deploy or restart, change topics,
      add a migration, or touch .201.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14374/dod-deploy-assessment-head-3984c390/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14374/dod-deploy-assessment-head-3984c390/command.yaml
  - id: "dod-occ-self-commit"
    description: >-
      OCC self-binding: this onex_change_control branch carries the OMN-14374 central contract and PASS
      receipts for omnibase_infra PR #2260 head 3984c39071d1b2eb362c0cd2c2ad3e321e42a605. The receipt
      binds the first evidence commit for occ-preflight eligibility before the OCC PR number exists.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14374/dod-occ-self-commit/command.yaml && grep
          -q 'jonah/omn-14374-occ-infra-2260' drift/dod_receipts/OMN-14374/dod-occ-self-commit/command.yaml
  - id: "dod-omniclaude-pr-1894-head-91048fdf"
    description: >-
      Second half of OMN-14374 (the infra#2260 evidence above wires the pr_state skill itself; this is
      the consuming docs half): independent adversarial verification of omniclaude PR #1894 at head 91048fdf1ebf1378fa88fbed396930ff51a644c3,
      a docs-only diff (2 markdown files: rolling_plan_governor SKILL.md + prompt.md) that routes the
      governor's PR-probing guidance from raw `gh pr view/checks` to `onex skill pr_state --operation
      pr_status|ci_checks --repo <repo> --pr <n>`. Independently confirmed the documented flags (operation/repo/pr)
      and operation values (pr_status, ci_checks) match the real, merged skill_mapping.yaml entry and
      EnumGithubGatewayOperation exactly (read both source files directly, not inferred). Live-ran both
      documented commands end-to-end against a real PR (omnibase_infra#2262) after rebuilding the omnimarket
      venv to pick up the OMN-14307 node (an environment sync issue in the verifier's own sandbox, unrelated
      to this PR's content) -- both returned structured JSON whose overall=red, blocked=true, and failing_contexts
      (35x occ-preflight/eligibility + CI Summary) cross-validated exactly against this same verifier's
      independent `gh pr checks` findings for that PR earlier in the same session. No test suite exists
      for a docs-only change; ruff/mypy/etc are not applicable. Live CI reds on #1894 itself classify
      as occ-preflight dependency cascade only (missing Evidence-Source; confirmed via actual failed-job
      logs, not just the rollup), not real content failures.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14374/dod-omniclaude-pr-1894-head-91048fdf/command.yaml
          && grep -q '^pr_number: 1894$' drift/dod_receipts/OMN-14374/dod-omniclaude-pr-1894-head-91048fdf/command.yaml
          && grep -q '91048fdf1ebf1378fa88fbed396930ff51a644c3' drift/dod_receipts/OMN-14374/dod-omniclaude-pr-1894-head-91048fdf/command.yaml
  - id: "dod-deploy-assessment-head-91048fdf"
    description: >-
      Deploy-surface assessment for omniclaude PR #1894 head 91048fdf1ebf1378fa88fbed396930ff51a644c3:
      the diff is limited to two skill markdown files (SKILL.md, prompt.md). It does not register a new
      service deployment, mutate runtime configuration, perform a deploy or restart, change topics, add
      a migration, or touch .201.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14374/dod-deploy-assessment-head-91048fdf/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14374/dod-deploy-assessment-head-91048fdf/command.yaml
  - id: "dod-occ-self-commit-omniclaude-1894"
    description: >-
      OCC self-binding: this onex_change_control branch carries the OMN-14374 contract append (omniclaude
      half) and PASS receipts for omniclaude PR #1894 head 91048fdf1ebf1378fa88fbed396930ff51a644c3. The
      receipt binds the first evidence commit for occ-preflight eligibility before the OCC PR number exists.
      A distinct evidence_item_id from the existing "dod-occ-self-commit" entry above (different OCC PR,
      additive, does not touch it).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14374/dod-occ-self-commit-omniclaude-1894/command.yaml
          && grep -q 'jonah/omn-14374-occ-omniclaude-1894' drift/dod_receipts/OMN-14374/dod-occ-self-commit-omniclaude-1894/command.yaml
