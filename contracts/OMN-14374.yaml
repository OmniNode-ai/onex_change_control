---
schema_version: "1.0.0"
ticket_id: "OMN-14374"
title: "feat(OMN-14374): wire GitHub PR state gateway into onex skill"
summary: >-
  omnibase_infra PR #2260 adds the onex skill mapping for pr_state so the CLI dispatches the existing
  node_github_repo_gateway_effect. The mapping exposes typed GitHub PR-state rows through the gateway
  request/result models instead of relying on raw gh JSON dumps, and the focused CLI test proves the generated
  payload validates against ModelGithubGatewayRequest for PR-scoped and repo-scoped operations.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2260-head-3984c390"
    description: >-
      Foreground Codex verification of omnibase_infra PR #2260 at head 3984c39071d1b2eb362c0cd2c2ad3e321e42a605.
      The exact pr_state CLI payload regression test passed, the full tests/unit/cli/test_cli_skill.py
      file passed with OMNI_HOME and OMNIMARKET_SRC pointed at the canonical sibling repos, ruff check
      and ruff format --check passed for the touched Python test, and file-scoped pre-commit passed for
      the YAML mapping plus test file.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14374/dod-omnibase-infra-pr-2260-head-3984c390/command.yaml
          && grep -q '^pr_number: 2260$' drift/dod_receipts/OMN-14374/dod-omnibase-infra-pr-2260-head-3984c390/command.yaml
          && grep -q '3984c39071d1b2eb362c0cd2c2ad3e321e42a605' drift/dod_receipts/OMN-14374/dod-omnibase-infra-pr-2260-head-3984c390/command.yaml
  - id: "dod-deploy-assessment-head-3984c390"
    description: >-
      Deploy-surface assessment for omnibase_infra PR #2260 head 3984c39071d1b2eb362c0cd2c2ad3e321e42a605:
      the diff is limited to a CLI skill mapping YAML entry and a focused unit test. It does not register
      a new service deployment, mutate runtime configuration, perform a deploy or restart, change topics,
      add a migration, or touch .201.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14374/dod-deploy-assessment-head-3984c390/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14374/dod-deploy-assessment-head-3984c390/command.yaml
  - id: "dod-occ-self-commit"
    description: >-
      OCC self-binding: this onex_change_control branch carries the OMN-14374 central contract and PASS
      receipts for omnibase_infra PR #2260 head 3984c39071d1b2eb362c0cd2c2ad3e321e42a605. The receipt
      binds the first evidence commit for occ-preflight eligibility before the OCC PR number exists.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14374/dod-occ-self-commit/command.yaml && grep
          -q 'jonah/omn-14374-occ-infra-2260' drift/dod_receipts/OMN-14374/dod-occ-self-commit/command.yaml
