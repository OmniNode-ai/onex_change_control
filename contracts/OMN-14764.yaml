---
schema_version: 1.0.0
ticket_id: OMN-14764
title: omnidash exhaustive-deps and fetch-header preservation lint gate evidence
summary: OCC evidence for omnidash#262 F-19 C/D local lint gate enforcement.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: OmniNode-ai/omnidash#262 CI succeeded on exact product head.
    command: gh run view ${RUN_ID} --repo ${REPO} --json headSha,conclusion
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnidash-262-head-ae65cbc4
    description: OmniNode-ai/omnidash#262 is open at exact product head ae65cbc41159d665c150d30725caabadb0065e0b.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid,body
  - id: dod-omnidash-262-diff-ae65cbc4
    description: OmniNode-ai/omnidash#262 has a non-empty reviewed product diff for OMN-14764.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr diff ${PR_NUMBER} --repo ${REPO} --name-only
  - id: dod-omnidash-262-ci-ae65cbc4
    description: CI run 29637402314 completed successfully for OmniNode-ai/omnidash#262 at exact head
      ae65cbc41159d665c150d30725caabadb0065e0b.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29637402314; PRODUCT_REPO=OmniNode-ai/omnidash; PRODUCT_HEAD=ae65cbc41159d665c150d30725caabadb0065e0b;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-omn-14764-deploy-scope
    description: 'Deploy-scope proof for OMN-14764: product diff is limited to eslint local rule and config
      changes; this OCC lane performed no live deploy, restart, docker exec, rpk topic produce, or production
      mutation.'
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14764/dod-omn-14764-deploy-scope/command.yaml
          && grep -qi ''no live deploy, restart, docker exec, rpk topic produce, or production mutation''
          drift/dod_receipts/OMN-14764/dod-omn-14764-deploy-scope/command.yaml'
  - id: occ-self-bind-pr-4383-omn-14764
    description: 'OCC PR #4383 self-binding proof for OMN-14764 evidence.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
