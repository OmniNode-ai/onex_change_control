---
schema_version: "1.0.0"
ticket_id: "OMN-14679"
title: "Autobind OCC evidence for OMN-14679"
summary: >
  OCC contract bound by node_pr_lifecycle_fix_effect Evidence-Source autobind (OMN-13317 F1) when OmniNode-ai/omnimarket
  PR #1789 carried a product-SHA Evidence-Source or was missing an OCC contract, and failed the gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1789 product diff scope present"
    command: "PRODUCT_PR_NUMBER=1789; PRODUCT_REPO=OmniNode-ai/omnimarket; gh pr view ${PRODUCT_PR_NUMBER}
      --repo ${PRODUCT_REPO} --json files --jq '.files | length > 0' | grep -qx true"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1789"
    description: "PR #1789 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1789; PRODUCT_REPO=OmniNode-ai/omnimarket; gh pr view ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1789-ci"
    description: "PR #1789 on OmniNode-ai/omnimarket — product diff scope (OMN-14425 substance check;
      OMN-14650: no longer gated on source-PR CI being green)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1789; PRODUCT_REPO=OmniNode-ai/omnimarket; gh pr view ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --json files --jq '.files | length > 0' | grep -qx true"
  - id: "occ-self-bind-pr-4297"
    description: "OCC companion PR #4297 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14679
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4297; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
  - id: "dod-deploy-assessment"
    description: "Deploy-gate assessment for omnimarket#1789: runtime-path change is limited to OCC evidence
      rendering logic; no live deploy, restart, Kafka mutation, database mutation, or .201 mutation is
      performed pre-merge. Standard post-merge omnimarket deploy validation remains the runtime proof
      surface."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14679/dod-deploy-assessment/command.yaml
          && grep -q 'deploy validation remains' drift/dod_receipts/OMN-14679/dod-deploy-assessment/command.yaml"
