---
schema_version: "1.0.0"
ticket_id: "OMN-13499"
title: "feat(OMN-13499): canonical recorded-from-real golden-chain replay harness"
summary: >-
  OCC evidence for the omnibase_core Phase 0 golden-chain harness. The product PR adds the shared omnibase_core.runtime.golden_chain
  recorded-from-real replay surface used by downstream repos to replace hand-written fake inference adapters
  without violating repo layering. Replay keeps routing-contract resolution, model/provider selection,
  endpoint resolution, request construction, and dispatch live; only the recorded model response bytes
  come from the fixture. This contract exists so the omnibase_core + omnimarket product PR Receipt Gates
  bind via Evidence-Source.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain-unit"
    description: >-
      The new golden-chain runtime unit suite passes, including routing-failure proof and record-mode
      gate coverage for the recorded replay transport.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: \"PASS\"$' drift/dod_receipts/OMN-13499/dod-golden-chain-unit/command.yaml"
  - id: "dod-golden-chain-typecheck"
    description: >-
      The new runtime and model packages type-check cleanly under mypy strict for the golden-chain replay
      harness surface.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: \"PASS\"$' drift/dod_receipts/OMN-13499/dod-golden-chain-typecheck/command.yaml"
  - id: "dod-reference-chain-real"
    description: >-
      The reference delegation golden chain (omnimarket product PR) is migrated end-to-end to the canonical
      harness: real routing + real OpenAI-compatible request construction + recorded replay + quality
      gate. 4 tests pass FOR REAL including the planted wrong-route proof (REQUEST_HASH_MISMATCH) and
      the tier-name proof (ROUTE_NOT_RESOLVED). This receipt binds to the omnimarket product PR commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: \"PASS\"$' drift/dod_receipts/OMN-13499/dod-reference-chain-real/command.yaml"
  - id: "dod-validator-clean"
    description: >-
      The OMN-13497 no-faked-boundary validator, run in BLOCKING mode (no --report-only) over the migrated
      reference chain, reports zero findings (exit 0, "No faked-boundary violations found"). Verifier
      (the validator authority) is distinct from the runner. Binds to the omnimarket product PR commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: \"PASS\"$' drift/dod_receipts/OMN-13499/dod-validator-clean/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13499.yaml consumed
      by the omnibase_core + omnimarket product PR Receipt Gates via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: \"PASS\"$' drift/dod_receipts/OMN-13499/dod-occ-pr/command.yaml"
  - id: "dod-pin-relock-import"
    description: >-
      The omnimarket Phase-0 pin relock imports the canonical golden-chain harness from the merged omnibase_core
      dev SHA 8ecb7efc1, proving the relock is not pinned to the orphaned harness commit. This receipt
      binds OMN-13499 PASS evidence to omnimarket PR #1354.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: \"PASS\"$' drift/dod_receipts/OMN-13499/dod-pin-relock-import/command.yaml"
  - id: "dod-occ-relock-pr"
    description: >-
      Self-binding receipt for the OCC relock evidence PR, which carries the OMN-13499 relock receipt
      consumed by omnimarket PR #1354 via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: \"PASS\"$' drift/dod_receipts/OMN-13499/dod-occ-relock-pr/command.yaml"
