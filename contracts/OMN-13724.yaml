---
schema_version: "1.0.0"
ticket_id: "OMN-13724"
title: "feat(OMN-13724): node_report_format_compute — md+metrics to Slack Block Kit"
summary: >-
  omnimarket PR adds node_report_format_compute, a pure COMPUTE node that converts pre-fetched report
  markdown and structured metrics into a Slack Block Kit blocks array with a mrkdwn fallback.  Enforces
  Block Kit hard limits (50 blocks, 3000 chars/section), emits a link-out block on truncation, always
  produces a payload on quiet days. No I/O — zero git/gh/Linear/Slack calls. 40 golden-chain tests covering
  limits, oversized input, truncation, quiet-day, allowlist filtering.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnimarket PR checks pass"
    command: "env -u GITHUB_TOKEN gh pr checks --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr"
    description: >-
      omnimarket PR adds node_report_format_compute with contract, handler, models, metadata, pyproject
      entry-point, and 40 golden-chain tests. All pass locally: ruff format, ruff check, mypy --strict,
      pre-commit --all-files, pytest (40/40).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13724/dod-omnimarket-pr/command.yaml
          && grep -q '40/40 golden-chain tests green' drift/dod_receipts/OMN-13724/dod-omnimarket-pr/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC eligibility receipt for the dev-targeted evidence PR. This onex_change_control
      PR carries the OMN-13724 contract onto dev, satisfying the receipt-to-PR binding required by validator_occ_merge_eligibility.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13724/dod-occ-self/command.yaml
          && grep -Eq '^pr_number: [0-9]+$' drift/dod_receipts/OMN-13724/dod-occ-self/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for node_report_format_compute. The PR adds an omnimarket pure COMPUTE node with
      contract, handler, models, metadata, and pyproject entry-point; no live deploy or runtime restart
      was performed in this evidence PR. Rollout is gated by the normal omnimarket merge/deploy pipeline
      after CI and deploy-gate pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13724/dod-deploy-assessment/command.yaml
          && grep -q 'normal omnimarket merge/deploy pipeline' drift/dod_receipts/OMN-13724/dod-deploy-assessment/command.yaml"
