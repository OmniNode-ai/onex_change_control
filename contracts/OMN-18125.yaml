---
schema_version: "1.0.0"
ticket_id: "OMN-18125"
title: "Autobind OCC evidence for OMN-18125"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3390.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3390 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3390 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_handler_evidence_autoclose_sweep.py tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_16106_unconfirmed_readback_is_a_refusal.py tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_16821_non_probative_denominator.py tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_17658_closer_safety_fences.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3390"
    description: "PR #3390 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py?ref=b370ab699d384ced084018a4f55c327706b38ed8 --jq '.content' | base64 -d | grep -c 'def _coverage_corpus_counts'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3390/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/contract.yaml'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #3390 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3390/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3390 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_handler_evidence_autoclose_sweep.py tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_16106_unconfirmed_readback_is_a_refusal.py tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_16821_non_probative_denominator.py tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_17658_closer_safety_fences.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-8890"
    description: "Binds OMN-18125 to OCC companion PR #8890 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8890 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omn18125-ac1-red-then-green"
    description: >
      AC1 — RED first, then green. The module whose five failures at the parent commit bcb3e6460 are quoted
      verbatim in PR #3390 passes in full after the fix, and the five RED test names are still the ones
      it carries, so the captured RED cannot drift away from the suite that replaced it.
    source: "manual"
    binds_ac: ["AC1"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18125_coverage_corpus.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
      - check_type: "command"
        check_value: |-
          test "$(grep -cE 'def (test_the_omn_17478_shape_is_not_held_by_the_coverage_rule|test_the_omn_17478_shape_reaches_the_flip|test_minting_more_provenance_does_not_release_a_real_hold|test_a_check_bound_to_a_criterion_this_ticket_does_not_list_is_excluded|test_the_call_site_feeds_the_coverage_corpus_not_the_verdict_totals)' tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18125_coverage_corpus.py)" = "5"
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-omn18125-ac2-shape-releases"
    description: >
      AC2 — the fixture is OMN-17478's measured shape and it releases. The fixture pins 34 checks, 12
      verified and 22 non-probative before it trusts the verdict, and asserts that none of the 22 declares
      a binding, so a fixture drifting away from the measurement fails rather than quietly proving something
      easier.
    source: "manual"
    binds_ac: ["AC2"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest 'tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18125_coverage_corpus.py::test_the_omn_17478_shape_is_not_held_by_the_coverage_rule' 'tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18125_coverage_corpus.py::test_the_omn_17478_shape_reaches_the_flip' -q
        cwd: "${OMNI_HOME}/omnibase_infra"
      - check_type: "command"
        check_value: |-
          test "$(grep -cE 'assert len\(checks\) == 34|"verified"\) == 12|"non_probative"\) == 22' tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18125_coverage_corpus.py)" = "3"
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-omn18125-ac3-still-holds"
    description: >
      AC3 — the rule keeps its teeth. Three controls: a coverage corpus that is itself majority non-probative
      holds; a criterion count exceeding the verified probative checks holds; an unchecked checkbox holds.
      Each asserts the stated arithmetic, not only the decision, so a hold arriving for a different reason
      fails.
    source: "manual"
    binds_ac: ["AC3"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest 'tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18125_coverage_corpus.py::test_a_majority_non_probative_coverage_corpus_still_holds' 'tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18125_coverage_corpus.py::test_more_criteria_than_covering_checks_still_holds' 'tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18125_coverage_corpus.py::test_an_unchecked_checkbox_still_holds' -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-omn18125-ac4-padding-closed"
    description: >
      AC4 — minting cannot buy a release. Twenty added unbound self-binds leave the stated arithmetic
      byte-identical; a self-bind that DOES declare a criterion turns a flip into a hold; and checks bound
      to a criterion this ticket never listed are excluded from the numerator.
    source: "manual"
    binds_ac: ["AC4"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest 'tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18125_coverage_corpus.py::test_minting_more_provenance_does_not_release_a_real_hold' 'tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18125_coverage_corpus.py::test_a_self_bind_that_declares_a_criterion_hurts_its_own_contract' 'tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18125_coverage_corpus.py::test_a_check_bound_to_a_criterion_this_ticket_does_not_list_is_excluded' -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-omn18125-ac5-from-source"
    description: >
      AC5 — the consumption path, read from the workflow rather than assumed. evidence-autoclose-sweep.yml
      resolves omnimarket's dev HEAD and installs that exact sha from git, and checks out this repo for
      the closer itself, so no published release gates this change. The second check proves the claim's
      other half: the diff touches no omnimarket path, because binds_ac already reaches the closer on
      every per-check record.
    source: "manual"
    binds_ac: ["AC5"]
    checks:
      - check_type: "command"
        check_value: |-
          grep -q 'omnimarket @ git+https://github.com/OmniNode-ai/omnimarket@' .github/workflows/evidence-autoclose-sweep.yml && grep -q 'Resolve omnimarket dev HEAD' .github/workflows/evidence-autoclose-sweep.yml
        cwd: "${OMNI_HOME}/omnibase_infra"
      - check_type: "command"
        check_value: |-
          test "$(gh api repos/OmniNode-ai/omnibase_infra/pulls/3390/files --paginate --jq '.[].filename' | grep -vcE '^(src/omnibase_infra|tests)/')" = "0"
  - id: "dod-omn18125-ac6-suite-ci-green"
    description: >
      AC6 — the whole closer suite is green on the changed rule, and the gap-comment fingerprint version
      moved with the rule it describes. The second check reads both halves of that pinning, because a
      constant and a contract that disagree is exactly the drift the pinning test exists to catch. PR
      #3390's own CI is deliberately NOT a check here: it cannot be green until this companion merges,
      so binding it would be a circular bar the receipt could never honestly record. against the gap-comment
      fingerprint constant, and PR #3390's own CI is green.
    source: "manual"
    binds_ac: ["AC6"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/ -q
        cwd: "${OMNI_HOME}/omnibase_infra"
      - check_type: "command"
        check_value: |-
          grep -q 'node_version: "1.11.0"' src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/contract.yaml && grep -q '_GAP_FINGERPRINT_CONTRACT_VERSION = "1.11.0"' src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3397"
    description: "PR #3397 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_18125_coverage_corpus.py?ref=e43b680227c42a2bce0cd6cccd7278728d47e9ef --jq '.content' | base64 -d | grep -c 'def _omn_16493_checks'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3397-ci"
    description: "PR #3397 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3397 --repo OmniNode-ai/omnibase_infra --json files"
