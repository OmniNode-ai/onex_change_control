---
schema_version: "1.0.0"
ticket_id: "OMN-9690"
title: "Auto-inject worker Operating Rules boilerplate"
summary: >
  Adds worker_template_version metadata and deterministic Operating Rules injection to dispatch_worker
  so every compiled worker prompt receives the v1 rules block before spawning an agent.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Dispatch worker Operating Rules injection test receipt is present and PASS."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9690/dod-operating-rules-tests/command.yaml"
  - kind: "tests"
    description: "S-class skill guard receipt is present and PASS."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9690/dod-s-class-skill-guard/command.yaml"
  - kind: "manual"
    description: "OCC evidence PR receipt is present and PASS."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9690/dod-occ-pr-1345/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-operating-rules-tests"
    description: "Unit tests prove dispatch_worker metadata, rules text, and final_prompt injection behavior."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9690/dod-operating-rules-tests/command.yaml
          && grep -q '^pr_number: 1635$' drift/dod_receipts/OMN-9690/dod-operating-rules-tests/command.yaml
          && echo 'PASS: dispatch_worker Operating Rules receipt is bound to omniclaude PR #1635'"
  - id: "dod-s-class-skill-guard"
    description: "Existing S-class skill guard still passes after dispatch_worker prompt changes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9690/dod-s-class-skill-guard/command.yaml
          && grep -q '^pr_number: 1635$' drift/dod_receipts/OMN-9690/dod-s-class-skill-guard/command.yaml
          && echo 'PASS: S-class skill guard receipt is bound to omniclaude PR #1635'"
  - id: "dod-occ-pr-1345"
    description: "OCC PR #1345 carries the OMN-9690 contract and central receipt bundle."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9690/dod-occ-pr-1345/command.yaml
          && grep -q '^pr_number: 1345$' drift/dod_receipts/OMN-9690/dod-occ-pr-1345/command.yaml && echo
          'PASS: OCC PR #1345 receipt is present'"
