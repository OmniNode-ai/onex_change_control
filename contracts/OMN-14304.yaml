---
schema_version: "1.0.0"
ticket_id: "OMN-14304"
title: "refactor(OMN-14304): canonicalize process watchdog handler"
summary: >-
  omnimarket PR #1692 canonicalizes node_process_watchdog to the thin contract+handler shape. The handler
  now accepts a single typed payload and resolves the production check target set internally, matching
  runtime single-parameter dispatch while preserving the watchdog behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1692-head-54249bab"
    description: >-
      Foreground Codex verification of omnimarket PR #1692 at rebased head 54249babfa26f3d6b6a710ae849aeb5fcb4bd928.
      Local focused checks passed: watchdog unit plus golden-chain tests (69 passed), ruff check and ruff
      format --check on changed handler/test paths, mypy on touched handlers, strict changed-node state
      coverage, changed-node golden-chain coverage, unimported-handler wiring, strict node-drift gate,
      and market_node_runtime_dogfood over 364 node dirs.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14304/dod-omnimarket-pr-1692-head-54249bab/command.yaml
          && grep -q '^pr_number: 1692$' drift/dod_receipts/OMN-14304/dod-omnimarket-pr-1692-head-54249bab/command.yaml
          && grep -q '54249babfa26f3d6b6a710ae849aeb5fcb4bd928' drift/dod_receipts/OMN-14304/dod-omnimarket-pr-1692-head-54249bab/command.yaml
  - id: "dod-deploy-assessment-head-54249bab"
    description: >-
      Deploy-surface assessment for omnimarket PR #1692 head 54249babfa26f3d6b6a710ae849aeb5fcb4bd928:
      the diff is limited to a process watchdog handler canonicalization, production target typing, and
      focused tests. It does not mutate live deployment configuration, perform a deploy/restart, change
      topics, run migrations, or touch .201.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14304/dod-deploy-assessment-head-54249bab/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14304/dod-deploy-assessment-head-54249bab/command.yaml
  - id: "dod-occ-self-commit-initial"
    description: >-
      OCC companion self-binding: the initial onex_change_control commit for OMN-14304 carries this central
      contract plus the product and deploy PASS receipts for omnimarket PR #1692 head 54249babfa26f3d6b6a710ae849aeb5fcb4bd928.
      The follow-up receipt binds that initial OCC evidence commit for occ-preflight eligibility before
      the OCC PR is merged.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14304/dod-occ-self-commit-initial/command.yaml
          && grep -Eq '^[[:space:]]*commit_sha: [0-9a-f]{40}$' drift/dod_receipts/OMN-14304/dod-occ-self-commit-initial/command.yaml
