---
schema_version: 1.0.0
ticket_id: OMN-11274
title: Session phase topics registered in TopicBase
summary: omnibase_core PR  #1115 adds unit coverage proving the five canonical session phase topic enum members resolve to exact wire values and pass build_topic validation.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - topics
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Product PR  #1115 verifies canonical wire values and build_topic validation for all five session phase topics.
    command: env PYTHONPATH=src uv run pytest tests/unit/test_topics.py -q
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-session-phase-topic-tests
    description: Product verification evidence is captured as a canonical PASS receipt.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11274/dod-session-phase-topic-tests/command.yaml"
          && grep -q '^pr_number: 1115$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11274/dod-session-phase-topic-tests/command.yaml"
          && grep -q '^verifier: agent$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11274/dod-session-phase-topic-tests/command.yaml"
