---
schema_version: 1.0.0
ticket_id: OMN-16159
title: sibling-lock-refresh.yml bot PR title carries no OMN-13902 ref
summary: 'OCC evidence companion for the 4-repo fan-out fix: bake the proven ticket-bearing title (`chore(deps,
  OMN-13902): refresh omninode sibling locks [bot]`) into every repo''s copy of `.github/workflows/sibling-lock-refresh.yml`,
  so the daily bot PR satisfies the receipt-gate''s ticket-extraction title fallback without a manual
  per-PR retitle. Single-line workflow-template change in each of omnibase_infra, omniclaude, omniintelligence,
  omnimemory; no runtime behavior change.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: All 4 product PRs CI green
    command: gh pr checks ${PR_NUMBER} --repo ${REPO}
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-OmniNode-ai-omnibase_infra-pr-2781
    description: "PR #2781 on OmniNode-ai/omnibase_infra — retitles sibling-lock-refresh.yml's bot PR
      creation to bake in the OMN-13902 ticket ref. Single-file workflow-only change.\n"
    source: generated
    checks:
      - check_type: command
        check_value: 'body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/sibling-lock-refresh.yml?ref=9ecf6e1eb33ff3fbc98830bc705ab4635c47d6a5"
          --jq .content | base64 -d)" && printf ''%s'' "$body" | grep -qF "chore(deps, OMN-13902): refresh
          omninode sibling locks [bot]" && printf ''{"title_present":true}'''
  - id: dod-OmniNode-ai-omniclaude-pr-1998
    description: "PR #1998 on OmniNode-ai/omniclaude — same class as infra#2781, this repo's copy of sibling-lock-refresh.yml.\n"
    source: generated
    checks:
      - check_type: command
        check_value: 'body="$(gh api "repos/OmniNode-ai/omniclaude/contents/.github/workflows/sibling-lock-refresh.yml?ref=0d9c18d24b1317f6ee33a5af87e81b72c0b92de9"
          --jq .content | base64 -d)" && printf ''%s'' "$body" | grep -qF "chore(deps, OMN-13902): refresh
          omninode sibling locks [bot]" && printf ''{"title_present":true}'''
  - id: dod-OmniNode-ai-omniintelligence-pr-826
    description: "PR #826 on OmniNode-ai/omniintelligence — same class as infra#2781, this repo's copy
      of sibling-lock-refresh.yml.\n"
    source: generated
    checks:
      - check_type: command
        check_value: 'body="$(gh api "repos/OmniNode-ai/omniintelligence/contents/.github/workflows/sibling-lock-refresh.yml?ref=044fad91c208290e9960779d353b2916a2ca1ef1"
          --jq .content | base64 -d)" && printf ''%s'' "$body" | grep -qF "chore(deps, OMN-13902): refresh
          omninode sibling locks [bot]" && printf ''{"title_present":true}'''
  - id: dod-OmniNode-ai-omnimemory-pr-429
    description: "PR #429 on OmniNode-ai/omnimemory — same class as infra#2781, this repo's copy of sibling-lock-refresh.yml.\n"
    source: generated
    checks:
      - check_type: command
        check_value: 'body="$(gh api "repos/OmniNode-ai/omnimemory/contents/.github/workflows/sibling-lock-refresh.yml?ref=2a1d4e2f21fa480d780787d6cedfa87cd68a5580"
          --jq .content | base64 -d)" && printf ''%s'' "$body" | grep -qF "chore(deps, OMN-13902): refresh
          omninode sibling locks [bot]" && printf ''{"title_present":true}'''
  - id: occ-self-bind-pr-6649
    description: 'Binds OMN-16159 to OCC companion PR #6649 (self-bind).'
    source: generated
    checks:
      - check_type: command
        check_value: gh api repos/OmniNode-ai/onex_change_control/pulls/6649 --jq '{number,state}'
