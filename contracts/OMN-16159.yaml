---
schema_version: "1.0.0"
ticket_id: "OMN-16159"
title: "sibling-lock-refresh.yml bot PR title carries no OMN-13902 ref"
summary: >
  OCC evidence companion for the 4-repo fan-out fix: bake the proven ticket-bearing title (`chore(deps,
  OMN-13902): refresh omninode sibling locks [bot]`) into every repo's copy of `.github/workflows/sibling-lock-refresh.yml`,
  so the daily bot PR satisfies the receipt-gate's ticket-extraction title fallback without a manual per-PR
  retitle. Single-line workflow-template change in each of omnibase_infra, omniclaude, omniintelligence,
  omnimemory; no runtime behavior change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "All 4 product PRs CI green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2781"
    description: >
      PR #2781 on OmniNode-ai/omnibase_infra — retitles sibling-lock-refresh.yml's bot PR creation to
      bake in the OMN-13902 ticket ref. Single-file workflow-only change.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2781 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,files"
  - id: "dod-OmniNode-ai-omniclaude-pr-1998"
    description: >
      PR #1998 on OmniNode-ai/omniclaude — same class as infra#2781, this repo's copy of sibling-lock-refresh.yml.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1998 --repo OmniNode-ai/omniclaude --json number,state,headRefName,files"
  - id: "dod-OmniNode-ai-omniintelligence-pr-826"
    description: >
      PR #826 on OmniNode-ai/omniintelligence — same class as infra#2781, this repo's copy of sibling-lock-refresh.yml.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 826 --repo OmniNode-ai/omniintelligence --json number,state,headRefName,files"
  - id: "dod-OmniNode-ai-omnimemory-pr-429"
    description: >
      PR #429 on OmniNode-ai/omnimemory — same class as infra#2781, this repo's copy of sibling-lock-refresh.yml.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 429 --repo OmniNode-ai/omnimemory --json number,state,headRefName,files"
