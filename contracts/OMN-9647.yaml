---
schema_version: '1.0.0'
ticket_id: OMN-9647
summary: 'fix(runtime): guard LLM endpoint env drift — move LLM_EMBEDDING_URL ownership to .201:8100 and
  add contract enforcement script'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_infra PR #1398 moves LLM_EMBEDDING_URL in contracts/llm_endpoints.yaml from
      disabled .200:8100 to running .201:8100, adds check_llm_endpoint_env_contract.py to reject env URLs
      that drift from the canonical contract, and tightens check_llm_endpoints.sh so a configured embedding
      endpoint being down fails the runtime health probe."
    command: gh pr view 1398 --repo OmniNode-ai/omnibase_infra --json state -q .state
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: contracts/llm_endpoints.yaml updated — LLM_EMBEDDING_URL slot moved from disabled .200:8100
      to running .201:8100
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9647/dod-001/file_exists.yaml
  - id: dod-002
    description: scripts/check_llm_endpoint_env_contract.py added — rejects env URLs that drift from the
      canonical running endpoint contract; unit tests pass
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9647/dod-002/file_exists.yaml
  - id: dod-003
    description: scripts/check_llm_endpoints.sh tightened — configured embedding endpoint being down now
      fails the runtime health probe; lint, format, mypy pass
    source: generated
    checks:
      - check_type: file_exists
        check_value: drift/dod_receipts/OMN-9647/dod-003/file_exists.yaml
