---
schema_version: "1.0.0"
ticket_id: "OMN-12659"
title: "feat(OMN-12659): repoint all omnimarket delegation.wire importers from compat to core"
summary: >-
  WS-B1 of the SEA e2e OMN-12588 epic. All 29 omnimarket importers of omnibase_compat.contracts.delegation.wire
  are repointed to omnibase_core.models.delegation.wire (graduation completed in OMN-12126). omnibase_core
  pin bumped to git SHA 307e51f9 (dev tip, 0.44.0) which includes TopicBase infra delegation topics, widened
  tier Literal, and full 12-value task_type Literal (OMN-12663). Aggregator src/omnimarket/models/delegation/wire/__init__.py
  updated with explicit per-module imports from core. 22 individual node/adapter shim modules repointed.
  5 test files updated for core module path identity assertions. Result: 0 omnibase_compat.contracts.delegation.wire
  importers in src/ or tests/; 6025 unit tests pass, 0 failures (2 Kafka connectivity failures are pre-existing
  infrastructure tests that require live .201 Kafka, unrelated to this change).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-repoint-tests"
    description: >-
      Full omnimarket test suite runs with 6025 passed, 12 skipped after the compat→core repoint. The
      2 Kafka connectivity failures (test_idempotency_key_is_stable_on_reprocess, test_kafka_integration_bus_publishes_and_consumes)
      are pre-existing infrastructure failures requiring live 192.168.86.201:19092, not introduced by
      this change. All 29 delegation.wire importer files confirmed repointed (grep returns 0).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12659/dod-repoint-tests/command.yaml"
          && grep -q '6025 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12659/dod-repoint-tests/command.yaml"
  - id: "dod-compat-zero-importers"
    description: >-
      grep -r omnibase_compat.contracts.delegation.wire src/ returns 0 results in the omnimarket worktree
      after the repoint, proving complete compat elimination.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12659/dod-compat-zero-importers/command.yaml"
          && grep -q 'importer_count=0' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12659/dod-compat-zero-importers/command.yaml"
  - id: "dod-precommit"
    description: >-
      All pre-commit hooks pass on the omnimarket worktree after the repoint: ruff format, ruff check,
      end-of-file-fixer, leaked-literals, topic-naming, event-registry-drift, dependency-health, reject-skip-gate-bypass
      all PASS.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12659/dod-precommit/command.yaml"
  - id: "dod-omnimarket-pr-1032"
    description: >-
      omnimarket PR #1032 carries the OMN-12659 compat→core repoint change (branch jonahgabriel/omn-12659-ws-b1-repoint-delegationwire-compat-importers-to-core-delete).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12659/dod-omnimarket-pr-1032/command.yaml"
          && grep -q '^pr_number: 1032$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12659/dod-omnimarket-pr-1032/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-12659 central contract and receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12659/dod-occ-pr-self/command.yaml"
