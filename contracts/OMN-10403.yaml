---
schema_version: "1.0.0"
ticket_id: OMN-10403
title: "Add harness parity follow-up notes"
summary: "Record the follow-up boundary that market skills return ModelMarketCliReport and external harnesses
  consume or render that typed report without owning a parallel contract."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Harness parity follow-up doc test passes"
    command: "uv run pytest tests/test_harness_parity_doc.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Harness parity follow-up doc exists and has required sections"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_harness_parity_doc.py -q"
  - id: "dod-002"
    description: "No deploy required; this is documentation and a local doc-coverage test only"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for OMN-10403 - documentation-only follow-up boundary'"
