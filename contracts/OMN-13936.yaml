---
schema_version: "1.0.0"
ticket_id: "OMN-13936"
title: "fix(OMN-13936): CodeRabbit follow-up for graded ladder benchmark — subprocess env hardening +
  recorder robustness"
summary: >-
  omnimarket PR #1609 lands the CodeRabbit review findings for the OMN-13935 graded ladder benchmark (merged
  #1607, which auto-merged via the queue before review completed). Changes: (1) the candidate-code grading
  subprocess now runs with a MINIMAL environment (PATH only) so LLM-derived recorded output cannot exfiltrate
  an ambient secret into the failure tail serialized into the committed evidence packet; (2) the live
  recorder catches IndexError on an empty choices[] and exits non-zero when a resolved rung captured zero
  successful (200) cells; (3) the persisted fixture ticket is corrected to OMN-13935; (4) module docstring
  ticket tags retargeted OMN-13369 -> OMN-13935; (5) the regenerable evidence packet is dropped from docs/
  (docs must not carry ticket identifiers; regenerate via run_delegation_graded_benchmark.py --emit).
  Deploy-neutral: no node contract, no topic, no handler wiring, no live deploy.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1609"
    description: >-
      Consuming-PR binding: omnimarket PR #1609 carries the OMN-13936 CodeRabbit follow-up (grading subprocess
      minimal-env hardening, recorder IndexError + exit-code robustness, docstring ticket retarget, docs
      evidence-packet removal). Real probe: gh pr view 1609 --repo OmniNode-ai/omnimarket --json number,state,headRefOid,headRefName,title.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13936/dod-omnimarket-pr-1609/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment: docstring edits, a subprocess env-hardening change in the graded_ladder
      grader, robustness fixes in the non-CI live-capture recorder script, and removal of a docs evidence
      JSON. No node contract.yaml, no topic registration, no handler dispatch wiring; no live deploy,
      no docker exec, no rpk topic produce. Deploy-neutral.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13936/dod-deploy-assessment/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-13936/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying this OMN-13936 contract and the consuming-PR
      evidence for omnimarket PR #1609.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13936/dod-occ-self/command.yaml"
