---
schema_version: "1.0.0"
ticket_id: "OMN-13783"
title: "OMN-13674 child: Wave 5 Linear/DoD state-coverage suite"
summary: >-
  omnimarket PR #1549 delivers full declared-state coverage for the Linear/DoD cluster
  (node_linear_triage, node_dod_verify, node_dod_sweep_orchestrator). It closes the
  MARK_DONE_SUPERSEDED / WOULD_MARK_DONE_SUPERSEDED / MARK_DONE_EPIC / WOULD_MARK_DONE_EPIC
  gap in node_linear_triage (previously zero test references), adds negative controls for
  the OMN-13759 suppression guards, and adds constructor-injectable gh collaborator seams to
  node_dod_sweep_orchestrator so pr_merged/ci_green/gh_search_query states are proven without
  monkeypatching subprocess. Passes the OMN-13781 state-coverage-gate in strict mode with zero
  uncovered declared states across all three nodes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-state-coverage-full-suite"
    description: >-
      uv run pytest tests/ -m "not kafka" on omnimarket PR #1549's head commit passes with
      zero failures attributable to the change (11093 passed; the only local gaps are
      pre-existing tests requiring a live Postgres port-forward/Kafka broker not active in CI).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13783/dod-state-coverage-full-suite/command.yaml"
  - id: "dod-state-coverage-gate-pass"
    description: >-
      scripts/validate_state_coverage.py --check-changed origin/dev --strict reports zero FAIL
      for node_linear_triage, node_dod_verify, and node_dod_sweep_orchestrator on PR #1549's head.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13783/dod-state-coverage-gate-pass/command.yaml"
  - id: "dod-pr-1549-binding"
    description: >-
      OmniNode-ai/omnimarket PR #1549 carries the OMN-13783 implementation at the recorded feature commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13783/dod-pr-1549-binding/command.yaml"
  - id: "dod-occ-self"
    description: >-
      This onex_change_control PR carries the central OMN-13783 contract and PASS receipts for the
      downstream omnimarket PR #1549.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13783/dod-occ-self/command.yaml"
