---
schema_version: "1.0.0"
ticket_id: "OMN-13409"
title: "fix(OMN-13409): summarization quality gate rejects heuristic refusal"
summary: >-
  OCC evidence for the omnimarket fix PR (PR #1334) that closes the delegation quality-gate defect where
  LLM refusals were recorded as delegation-completed with quality_passed=True. Two-layer fix: (1) extend
  _check_no_refusal in handler_quality_gate.py with 18 additional soft-refusal phrases plus an ultra-short
  content-free pre-pass; (2) add result.passed to quality_accepted in handler_delegation_workflow.handle_gate_result
  so the orchestrator honours the gate's authoritative verdict. Dogfood repro cid 758871f8 reproduced
  and fixed. 10 new real-dispatch-path tests added; 2379 unit tests pass.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-failing-test-added"
    description: >-
      A failing test (red before fix) was added on the real dispatch path: test_refusal_not_pass_omn13409.py::TestRefusalRealDispatchPath::test_dogfood_repro_no_refusal_is_not_completed
      drives the full routing->inference->quality-gate->orchestrator chain with dogfood repro input ('NO',
      2 tokens) and asserts delegation-completed is NOT emitted.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c 'test_dogfood_repro_no_refusal_is_not_completed' contracts/OMN-13409.yaml"
  - id: "dod-all-tests-pass"
    description: >-
      All 10 new tests pass after the fix. The full delegation unit test suite (298 tests) and full unit
      suite (2379 tests) pass with no regressions. Calibration ratchet (5 tests) passes. mypy --strict
      clean on 2459 source files. pre-commit all hooks passed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c 'test_refusal_not_pass_omn13409' contracts/OMN-13409.yaml"
  - id: "dod-refusal-rejects-correctly"
    description: >-
      Verified that 'NO', 'No.', 'This request cannot be fulfilled.', and 'Unable to complete this task.'
      all produce passed=False, fallback_recommended=True from quality_gate_delta with the summarization
      DoD, and that delegation-completed is NOT emitted by the orchestrator for any of these inputs.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c 'Dogfood repro cid' contracts/OMN-13409.yaml"
  - id: "dod-deploy-evidence"
    description: >-
      Deploy-gate evidence is required because omnimarket PR #1334 touches runtime delegation and quality-gate
      handler paths. The PR is dev-targeted and this deploy DoD item records deploy-gate coverage; it
      does not authorize any live prod deploy.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c 'deploy' contracts/OMN-13409.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13409.yaml consumed
      by the omnimarket fix PR Receipt Gate via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13409\"$' contracts/OMN-13409.yaml"
