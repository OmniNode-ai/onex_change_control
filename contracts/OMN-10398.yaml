---
schema_version: "1.0.0"
ticket_id: OMN-10398
title: "Add shared CLI argparse helpers"
summary: "Add shared argparse helpers for market CLI output format, verbosity, and log-level handling."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "CLI argparse helper tests pass"
    command: "uv run pytest tests/unit/cli/test_args.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Shared argparse helper tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/cli/test_args.py -q"
  - id: "dod-002"
    description: "No deploy required; argparse helpers are local CLI parsing logic"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for OMN-10398 - local CLI parser helper only'"
