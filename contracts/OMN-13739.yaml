---
schema_version: "1.0.0"
ticket_id: "OMN-13739"
title: "test(OMN-13739): raise coverage of src/omnimarket/review/response_parser.py"
summary: >-
  Adds tests/unit/review/test_response_parser.py (19 unit tests) to exercise all previously uncovered
  branches in src/omnimarket/review/response_parser.py, raising statement coverage from ~70%. This is
  a pure test-only change that exercises parser edge-case behavior without changing runtime logic, deployed
  services, data stores, event-bus topics, or production configuration.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused response_parser unit tests pass on omnimarket PR #1529."
    command: >-
      grep -q '^status: PASS$' drift/dod_receipts/OMN-13739/dod-focused-tests/command.yaml && grep -q
      '^status: PASS$' drift/dod_receipts/OMN-13739/dod-no-runtime-mutation/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-13739/dod-occ-self/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused unit tests prove response_parser.py edge-case branch behavior."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13739/dod-focused-tests/command.yaml"
  - id: "dod-no-runtime-mutation"
    description: >-
      OMN-13739 is a test-only coverage PR with no live deploy, restart, .201 mutation, production data
      mutation, new REST endpoint, event-bus topic change, or runtime lane mutation.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13739/dod-no-runtime-mutation/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13739 contract and receipts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13739/dod-occ-self/command.yaml"
