---
schema_version: "1.0.0"
ticket_id: "OMN-12423"
title: "F-a: HandlerLlmDelegationCall handle() + event_model for clean call_effect type-scoping"
summary: >
  Bind central evidence for omnimarket PR #989 (F-a): node_llm_delegation_call_effect's execute_delegation_call
  entry declares event_model ModelLlmDelegationCallRequest and HandlerLlmDelegationCall exposes handle()
  returning the terminal event for runtime auto-publish. With OMN-12416's type-scoped routing, HandlerLlmDelegationCall
  is scoped OUT of inference-request messages entirely, so the chain reaches terminal with no sibling
  handler_error (clean, not A2-masked).
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-989"
    description: "omnimarket PR #989 carries the F-a handler handle() + event_model + published_events
      change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12423/dod-omnimarket-pr-989/command.yaml\"
          && grep -q '^pr_number: 989$' \"drift/dod_receipts/OMN-12423/dod-omnimarket-pr-989/command.yaml\""
  - id: "dod-focused-tests"
    description: "Focused call-effect + inference tests pass (23), runtime sweep 290/290, mypy --strict
      + ruff + pre-commit clean."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12423/dod-focused-tests/command.yaml\"
          && grep -q 'runtime sweep' \"drift/dod_receipts/OMN-12423/dod-focused-tests/command.yaml\""
  - id: "dod-clean-chain-deploy"
    description: "Live delegation chain on stability-test reaches terminal with NO HandlerLlmDelegationCall
      handler_error after F-a deploy (clean type-scoping, not A2-masked)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12423/dod-clean-chain-deploy/command.yaml\"
          && grep -q 'docker exec' \"drift/dod_receipts/OMN-12423/dod-clean-chain-deploy/command.yaml\""
