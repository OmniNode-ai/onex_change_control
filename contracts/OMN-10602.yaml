---
schema_version: "1.0.0"
ticket_id: "OMN-10602"
title: "Fix omnibase_core Auto-Merge workflow fanout queue flood"
summary: "Replace omnibase_core's wildcard workflow_run Auto-Merge retrigger with PR-context-aware check_suite
  handling, and cancel superseded merge_group CI runs so queue rotations do not accumulate stale Actions work."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_core PR #1039 workflow YAML parses and diff has no whitespace errors."
    command: "cd omni_worktrees/OMN-10602/omnibase_core && python - <<'PY'\nfrom pathlib import Path\nimport yaml\nfor p in ['.github/workflows/auto-merge.yml', '.github/workflows/ci.yml']:\n    yaml.safe_load(Path(p).read_text())\n    print(f'{p}: yaml ok')\nPY\n\ngit diff --check"
  - kind: "manual"
    description: "No runtime deploy is required; this change only modifies GitHub Actions workflow behavior."
    command: "echo 'no runtime deploy required: GitHub Actions workflow-only change'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "omnibase_core PR #1039 workflow YAML parses and diff has no whitespace errors."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10602/dod-001/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-001\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10602/dod-001/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10602\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10602/dod-001/command.yaml\"
          && grep -q '^commit_sha: \"aa8efcefc5fefd6038c114a1464cd80a6b1b6151\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10602/dod-001/command.yaml\"
          && grep -q '^pr_number: 1039$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10602/dod-001/command.yaml\""
  - id: "dod-deploy"
    description: "No runtime deploy is required for the omnibase_core workflow-only change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10602/dod-deploy/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-deploy\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10602/dod-deploy/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10602\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10602/dod-deploy/command.yaml\"
          && grep -q '^commit_sha: \"aa8efcefc5fefd6038c114a1464cd80a6b1b6151\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10602/dod-deploy/command.yaml\"
          && grep -q '^pr_number: 1039$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10602/dod-deploy/command.yaml\""
