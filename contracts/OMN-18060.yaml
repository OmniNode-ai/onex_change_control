---
schema_version: "1.0.0"
ticket_id: "OMN-18060"
title: "Autobind OCC evidence for OMN-18060"
summary: >
  OCC Evidence-Source autobind companion evidence for OMN-18060, including OmniNode-ai/omnimarket PR #2420
  and OmniNode-ai/omnibase_infra PRs #3345, #3350 and #3352.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2420 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2420 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: "uv run pytest tests/unit/scripts/test_ci_bus_lane_projection_readback.py -q"
  - kind: "ci"
    description: "PR #3345 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3345 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-omnibase-infra-pr-3345-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/ci/test_chain_canary_workflow.py tests/unit/infra/test_compose_no_silent_fallbacks.py tests/unit/nodes/node_chain_canary_effect/test_handler_chain_canary.py tests/unit/nodes/node_chain_canary_effect/test_handler_chain_canary_ledger.py -q
  - kind: "ci"
    description: "PR #3352 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3352 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the live RED/GREEN proof of the migration runner's least-privilege grant seam and its static pins. Bound as dod_evidence item `dod-omnibase-infra-pr-3352-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/scripts/test_login_only_role_grants_live_omn18060.py tests/unit/infra/test_login_only_role_grants_omn18060.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2420"
    description: "PR #2420 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/tests/unit/scripts/test_ci_bus_lane_projection_readback.py?ref=3f152e9a9280719830c72537feb0102f76249c85 --jq '.content' | base64 -d | grep -c 'def lanes'
  - id: "dod-OmniNode-ai-omnimarket-pr-2420-ci"
    description: "PR #2420 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2420 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2420 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/scripts/test_ci_bus_lane_projection_readback.py -q"
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-8739"
    description: "OCC companion PR #8739 — self-bind for OMN-18060 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8739 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3345"
    description: "PR #3345 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_chain_canary_effect/lane_transport.py?ref=ca88bdbcb3a105d4a3d11b6524d122dcaccf5655 --jq '.content' | base64 -d | grep -c 'def looks_like_a_dsn'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3345/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/chain-canary.yml'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #3345 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3345/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "dod-omnibase-infra-pr-3345-behavior-proof"
    description: "PR #3345 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/ci/test_chain_canary_workflow.py tests/unit/infra/test_compose_no_silent_fallbacks.py tests/unit/nodes/node_chain_canary_effect/test_handler_chain_canary.py tests/unit/nodes/node_chain_canary_effect/test_handler_chain_canary_ledger.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-8743"
    description: "Binds OMN-18060 to OCC companion PR #8743 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8743 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3350"
    description: "PR #3350 on OmniNode-ai/omnibase_infra — the narrowed DSN predicate is present."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_chain_canary_effect/lane_transport.py?ref=cb3c70724d9325af98e22d53af8fd9be111e0a3f --jq '.content' | base64 -d | grep -c '_URI_USERINFO_PASSWORD_PATTERN'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3350/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_infra/nodes/node_chain_canary_effect/lane_transport.py'
  - id: "dod-omnibase-infra-pr-3350-behavior-proof"
    description: "PR #3350 — diff-derived behavior proof: the negative control that fails on the shipped
      predicate and passes on the narrowed one."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_chain_canary_effect/test_projection_dsn_declaration_omn18060.py tests/ci/test_chain_canary_workflow.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-deploy-assessment-pr-3350"
    description: "Deploy-scope DoD so PR #3350 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3350/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "occ-self-bind-pr-3350-companion"
    description: "Binds OMN-18060 to the OCC companion for PR #3350 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3350 --repo OmniNode-ai/omnibase_infra --json number,state"
  - id: "occ-self-bind-pr-8757"
    description: >-
      OCC self-binding receipt for PR #8757, the in-repo evidence PR carrying this contract. Binds OMN-18060
      to the OCC PR by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8757 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3352"
    description: "PR #3352 on OmniNode-ai/omnibase_infra — the least-privilege grant seam is present in
      the migration runner, and the frozen flat stream is untouched."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/run-forward-migrations.sh?ref=01f4fbfa346a3205f5ab57201613362c6012b5a2 --jq '.content' | base64 -d | grep -c 'reassert_login_only_role_grants'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3352/files --paginate --jq '.[].filename' | grep -cFx 'scripts/run-forward-migrations.sh'
      - check_type: "command"
        check_value: |-
          test "$(gh api repos/OmniNode-ai/omnibase_infra/pulls/3352/files --paginate --jq '.[].filename' | grep -c '^docker/migrations/forward/')" = "0"
  - id: "dod-omnibase-infra-pr-3352-behavior-proof"
    description: "PR #3352 — diff-derived behavior proof: the live RED/GREEN grant proof against a scratch
      Postgres, plus the static pins on the seam."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/scripts/test_login_only_role_grants_live_omn18060.py tests/unit/infra/test_login_only_role_grants_omn18060.py tests/unit/db/test_migration_104_retired_omn17923.py tests/unit/infra/test_warm_volume_login_credential_omn16993.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-deploy-assessment-pr-3352"
    description: "Deploy-scope DoD so PR #3352 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3352/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "occ-self-bind-pr-3352-companion"
    description: "Binds OMN-18060 to the product PR #3352 this contract carries evidence for."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3352 --repo OmniNode-ai/omnibase_infra --json number,state"
