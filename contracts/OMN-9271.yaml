---
schema_version: '1.0.0'
ticket_id: OMN-9271
title: Receipt-gate CLI import broken in PyPI wheel transitive install
summary: >-
  The receipt-gate verify job fails with ImportError: cannot import name 'receipt_gate_cli' because the
  pip resolver picks the stale published PyPI wheel instead of the in-repo editable install. Fix: use
  --no-deps --no-index wheel build in omnibase_core receipt-gate.yml; add guard tests in omnibase_infra
  asserting the install safeguards are in place.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: >-
      omnibase_infra PR #1691 adds guard tests asserting receipt-gate installs omnibase_core from source
      wheel with --no-index/--no-deps safeguards.
    command: >-
      gh pr view 1691 --repo OmniNode-ai/omnibase_infra --json state -q '.state' | grep -q OPEN
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >-
      omnibase_infra PR #1691 adds guard tests for receipt-gate PyPI bypass safeguards; all 8 tests pass
      and pre-commit hooks are clean.
    source: manual
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-9271/dod-001/command.yaml && grep -q '^pr_number:
          1691$' drift/dod_receipts/OMN-9271/dod-001/command.yaml
  - id: dod-occ-pr
    description: Self-binding receipt for this OCC PR.
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json state -q '.state'
          | grep -q OPEN"
