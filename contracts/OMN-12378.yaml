---
schema_version: "1.0.0"
ticket_id: "OMN-12378"
title: "Add Repowise freshness receipt after workspace sync"
summary: >-
  Publish durable evidence for the Repowise freshness receipt work. The omnibase_infra PR adds a tracked
  stdlib-only receipt emitter, a sync wrapper that writes receipts under .onex_state/repowise-sync, and
  a focused unit test plus runbook so the OMN-12368 tech-debt closeout can cite exact repo SHAs and Repowise
  index freshness state.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1799"
    description: >-
      omnibase_infra PR #1799 carries the tracked Repowise freshness receipt emitter, sync wrapper, and
      runbook for OMN-12378.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12378/dod-omnibase-infra-pr-1799/command.yaml
          && grep -q '^pr_number: 1799$' drift/dod_receipts/OMN-12378/dod-omnibase-infra-pr-1799/command.yaml"
  - id: "dod-local-verification"
    description: >-
      Focused local tests and lint passed for the Repowise freshness receipt emitter, shell wrapper, and
      unit test before opening omnibase_infra PR #1799.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12378/dod-local-verification/command.yaml"
  - id: "dod-runbook"
    description: >-
      The PR documents the manual run path and receipt schema in docs/runbooks/repowise-freshness-receipt.md
      until the OMN-12368 closeout flow wires the script automatically.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12378/dod-runbook/command.yaml"
