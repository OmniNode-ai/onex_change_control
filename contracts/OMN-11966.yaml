---
schema_version: "1.0.0"
ticket_id: "OMN-11966"
title: "fix(OMN-11966): migration 024 functional index uses STABLE date() instead of IMMUTABLE ::date
  cast"
summary: >
  Fix idx_llm_delegation_call_log_date in migration 024 to use (created_at::date) cast form, which is
  IMMUTABLE in PostgreSQL. date(timestamptz) is STABLE and causes CREATE INDEX to fail. Add corrective
  migration 025 for existing deployments.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Migration and delegation unit tests pass (315 tests)"
    command: "uv run pytest tests/ -v -k 'migration or delegation' --timeout=60"
  - kind: "ci"
    description: "All CI checks green on omnibase_infra PR #1727"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1727"
    description: "omnibase_infra PR #1727 fixes migration 024 index and adds corrective migration 025"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11966/dod-omnibase-infra-pr-1727/command.yaml
          && grep -q '^pr_number: 1727$' drift/dod_receipts/OMN-11966/dod-omnibase-infra-pr-1727/command.yaml"
