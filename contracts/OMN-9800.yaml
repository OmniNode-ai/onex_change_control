---
schema_version: "1.0.0"
ticket_id: OMN-9800
title: "Add memory lifecycle contract models"
summary: "Add typed input/output envelope models for the memory lifecycle orchestrator, preserve canonical
  omnimemory DTO compatibility, and bind omnimarket PR #713 to central receipt evidence."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
  - envelopes
evidence_requirements:
  - kind: tests
    description: "Memory lifecycle DTO imports and contract model references pass."
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-9800/omnimarket-713-polish
      && uv run pytest tests/test_memory_lifecycle_dto_omnimemory_imports.py -q"
  - kind: manual
    description: "Touched memory lifecycle model and test files pass ruff."
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-9800/omnimarket-713-polish
      && uv run ruff check src/omnimarket/nodes/node_memory_lifecycle_orchestrator/models/__init__.py
      tests/test_memory_lifecycle_dto_omnimemory_imports.py"
  - kind: ci
    description: "omnimarket#713 carries the OMN-9800 memory lifecycle contract model repair."
    command: "gh pr checks 713 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-local-validation
    description: "Focused memory lifecycle ruff, pytest, and diff checks pass locally."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9800/dod-local-validation/command.yaml
          && grep -q '^ticket_id: OMN-9800$' drift/dod_receipts/OMN-9800/dod-local-validation/command.yaml"
  - id: dod-omnimarket-pr-713
    description: "omnimarket PR #713 is bound to OMN-9800 for memory lifecycle contract model verification."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9800/dod-omnimarket-pr-713/command.yaml
          && grep -q '^ticket_id: OMN-9800$' drift/dod_receipts/OMN-9800/dod-omnimarket-pr-713/command.yaml
          && grep -q '^pr_number: 713$' drift/dod_receipts/OMN-9800/dod-omnimarket-pr-713/command.yaml"
  - id: dod-occ-pr-1212
    description: "onex_change_control PR #1212 self-binds the OMN-9800 omnimarket#713 receipt repair."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9800/dod-occ-pr-1212/command.yaml
          && grep -q '^ticket_id: OMN-9800$' drift/dod_receipts/OMN-9800/dod-occ-pr-1212/command.yaml
          && grep -q '^pr_number: 1212$' drift/dod_receipts/OMN-9800/dod-occ-pr-1212/command.yaml"
