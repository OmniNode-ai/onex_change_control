---
schema_version: "1.0.0"
ticket_id: "OMN-12681"
title: "SEA guards against deleted compat delegation wire imports"
summary: >-
  Central evidence for migrating the SEA Track-B routing compute handler from
  the deleted omnibase_compat.contracts.delegation.wire package to the canonical
  omnibase_core.models.delegation.wire package. A unit guard now fails if the
  deleted compat package is reintroduced under src/.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-compat-import-guard"
    description: "Focused SEA Track-B and import-guard tests prove src/ contains no imports of the deleted omnibase_compat.contracts.delegation.wire package."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12681/dod-sea-compat-import-guard/command.yaml"
