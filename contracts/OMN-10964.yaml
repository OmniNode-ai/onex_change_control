---
schema_version: "1.0.0"
ticket_id: "OMN-10964"
title: "Fix logging.exception usage in delegation plugin (SonarCloud S8572)"
summary: >-
  Fixes SonarCloud S8572 code-smell in omnimarket's delegation plugin: replace 3 logger.error() calls
  inside except blocks with logger.exception() for idiomatic exception logging in wire_handlers, wire_dispatchers,
  start_consumers. Pure code-smell cleanup - no behavior change, no new runtime paths, no deploy required.
  Covered by omnimarket PR #676 together with OMN-10963.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnimarket PR #676 is bound to OMN-10964 at observed head."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10963/dod-omnimarket-pr-676/command.yaml"
  - kind: "manual"
    description: "No deploy required; pure code-smell cleanup."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10963/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-676"
    description: "Bind omnimarket PR #676 to OMN-10964 at the observed PR head (shared with OMN-10963)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10963/dod-omnimarket-pr-676/command.yaml\""
  - id: "dod-deploy"
    description: "No live deploy or runtime restart required; pure code-smell cleanup with no behavior
      change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10963/dod-deploy/command.yaml\""
