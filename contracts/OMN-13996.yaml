---
schema_version: "1.0.0"
ticket_id: "OMN-13996"
title: "Overseer tick raw ledger projection"
summary: >-
  Central OCC evidence for omnibase_infra PR #2216, the first migration slice of the event-sourced ledger
  substrate. The consuming PR adds the receiving projection surface for the existing overseer tick flat-file
  ledger: migration 088, rollback 088, schema fingerprint refresh, a ModelProjectorContract for onex.evt.omnimarket.overseer_tick.v1,
  and focused projector tests. This is pre-deploy code evidence only; no runtime deploy, runner restart,
  docker compose execution against .201, image build/push, Redpanda/MSK mutation, AWS mutation, or live
  cluster mutation was performed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2216"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2216 carries OMN-13996 at head 1ca9d929aa2aeddd48bbf23f5e2414a67e3bb9e9
      and targets dev.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13996/dod-omnibase-infra-pr-2216/command.yaml
          && grep -q '^pr_number: 2216$' drift/dod_receipts/OMN-13996/dod-omnibase-infra-pr-2216/command.yaml
          && grep -q '1ca9d929aa2aeddd48bbf23f5e2414a67e3bb9e9' drift/dod_receipts/OMN-13996/dod-omnibase-infra-pr-2216/command.yaml"
  - id: "dod-no-runtime-deploy"
    description: >-
      Deploy-gate evidence: omnibase_infra PR #2216 is a database migration, projector contract, schema
      fingerprint, and unit-test change only. No runtime deploy, runner restart, docker compose execution
      against .201, image build/push, Redpanda/MSK mutation, AWS mutation, or live cluster mutation was
      performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'No runtime deploy, runner restart, docker compose execution against .201'
          drift/dod_receipts/OMN-13996/dod-no-runtime-deploy/command.yaml && grep -q '^pr_number: 2216$'
          drift/dod_receipts/OMN-13996/dod-no-runtime-deploy/command.yaml"
