---
schema_version: "1.0.0"
ticket_id: "OMN-12769"
title: "Dashboard-delegation working dispatch not durably in dev HEAD (OMN-12525 reconciliation)"
summary: >-
  A clean rebuild from dev HEAD (infra 8a14d61b, omnimarket 360e9d45) does NOT reproduce the green delegation
  chain. The demo depends on a specific pre-backmerge infra SHA (8a684c16). The dispatch wiring for 'omnibase-infra.delegation-request'
  is not durably committed in current dev HEAD (OMN-12525 routing divergence: omnibase_core.route_to_handlers
  is dead; real routing lives in omnibase_infra.MessageDispatchEngine; backmerge #1892/#1894 broke the
  infra-side dispatch path). Closeout: identify regression cause, reconcile in dev HEAD, run clean dev
  HEAD build, confirm full delegation chain is green without SHA pinning. Phase-2 NO-GO blocker per OMN-12525:
  dev HEAD must produce a working demo build before any main promotion.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-dispatch-durable-in-dev-head"
    description: >-
      Clean rebuild from dev HEAD (no SHA pinning) runs the full delegation chain green. Proof: fresh
      correlation ID, delegation_events row with quality_gate_passed, prompt+response present, no DLQ
      events for omnibase-infra.delegation-request.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12769/dod-dispatch-durable-in-dev-head/command.yaml
          && grep -q 'quality_gate_passed' drift/dod_receipts/OMN-12769/dod-dispatch-durable-in-dev-head/command.yaml
  - id: "dod-omn-12525-routing-reconciled"
    description: >-
      OMN-12525 routing divergence addressed: dispatch path for omnibase-infra.delegation-request durably
      wired in infra MessageDispatchEngine at dev HEAD. Architecture doc or PR evidence references the
      durable fix.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12769/dod-omn-12525-routing-reconciled/command.yaml
  - id: "dod-occ-evidence"
    description: "onex_change_control OCC contract + receipts for OMN-12769."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12769/dod-occ-evidence/command.yaml && grep
          -q '^ticket_id: "OMN-12769"$' contracts/OMN-12769.yaml
