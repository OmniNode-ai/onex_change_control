---
schema_version: "1.0.0"
ticket_id: "OMN-9739"
title: "Replace env contract dataclasses with Pydantic models"
summary: >
  Replace check_env_var_contract.py dataclass contract containers with frozen Pydantic BaseModel classes,
  keep EnvContract as a compatibility alias for existing callers, and preserve the env contract scanner
  behavior with unit coverage.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-model-contract"
    description: "ModelEnvContract is a frozen Pydantic BaseModel and EnvContract remains an alias."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9739/dod-model-contract/command.yaml\""
  - id: "dod-unit-tests"
    description: "Env contract scanner tests pass, including the new ModelEnvContract coverage."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9739/dod-unit-tests/command.yaml\""
  - id: "dod-pr-809-head"
    description: "Bind OMN-9739 evidence to onex_change_control PR #809 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 809 --repo OmniNode-ai/onex_change_control --json headRefOid,title"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-9739 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9739/dod-occ-pr/command.yaml\""
