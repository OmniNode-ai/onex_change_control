---
schema_version: "1.0.0"
ticket_id: "OMN-9739"
title: "Add omniclaude install extras"
summary: >
  Adds omniclaude skills and full optional dependency extras for standalone skill dispatch and complete
  local development installs.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-extras-contract"
    description: "pyproject optional-dependency extras include skills and full bundles."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9739/dod-extras-contract/command.yaml\""
  - id: "dod-precommit"
    description: "Pre-commit hooks passed for the package metadata update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9739/dod-precommit/command.yaml\""
  - id: "dod-pr-1543-head"
    description: "omniclaude PR #1543 carries the OMN-9739 implementation head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9739/dod-pr-1543-head/command.yaml\""
  - id: "dod-occ-pr-827"
    description: "Bind this central OCC evidence update to onex_change_control PR #827."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9739/dod-occ-pr-827/command.yaml\""
