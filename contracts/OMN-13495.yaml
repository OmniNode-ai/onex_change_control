---
schema_version: "1.0.0"
ticket_id: "OMN-13495"
title: "chore(OMN-13495): bind omniintelligence dependency maintenance PRs"
summary: >-
  OCC evidence for two omniintelligence dependency maintenance PRs opened during the manual merge sweep.
  PR #728 is the ticketed replacement for Dependabot PR #726 and bumps actions/checkout from v6 to v7.
  PR #729 is the ticketed replacement for Dependabot PR #727 and widens the omnibase-core dependency range
  to allow <0.46.0. The replacements use OMN-13495-bearing branch names so the receipt gate identity binding
  can evaluate normally. PR #728 also carries a focused test expectation fix for the no-LLM-env dispatch-count
  surface exposed by CI plus the CodeRabbit-requested checkout SHA pinning/persist-credentials hardening
  for reviewed workflows. No runtime deploy, lane restart, database migration, contract/topic change,
  or live-lane mutation is part of either dependency PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-actions-checkout-pr-728"
    description: >-
      omniintelligence PR #728 replaces Dependabot PR #726 on a ticket-bearing branch, updates the GitHub
      Actions checkout action from v6 to v7, aligns four dispatch-count assertions with the no-LLM-env
      handler surface proven by CI, and addresses CodeRabbit checkout hardening review threads with SHA-pinned
      checkout references plus explicit credential non-persistence in the reviewed workflows. Product
      CI remains authoritative for the dependency compatibility result before merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PR #728' contracts/OMN-13495.yaml"
  - id: "dod-omnibase-core-pr-729"
    description: >-
      omniintelligence PR #729 carries the same head commit as Dependabot PR #727 and widens the omnibase-core
      requirement to permit the current <0.46.0 release line on a ticket-bearing branch. Product CI remains
      authoritative for the dependency compatibility result before merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PR #729' contracts/OMN-13495.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for the OCC PR that carries contracts/OMN-13495.yaml and the two product PR
      binding receipts consumed by omniintelligence receipt gates.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13495\"$' contracts/OMN-13495.yaml"
