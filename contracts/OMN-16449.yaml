---
schema_version: "1.0.0"
ticket_id: "OMN-16449"
title: "gateway-forwarder cloud leg unroutable from .201 — scoped DNS-sidecar for the MSK bastion route"
summary: >
  Central OCC evidence for OmniNode-ai/omnibase_infra PR #2862, which adds a purpose-built dnsmasq sidecar
  (docker/gateway/dns-bastion/) scoped to the .201 gateway-forwarder container only, resolving the 6 MSK
  broker hostnames to the live SNI-passthrough bastion. This lands the operator- ruled (2026-08-24) interim
  bridge: the forwarder's direct-MSK leg stays live to accelerate cloud migration, with an explicit sunset
  once an HTTPS-ingest path replaces it. No extra_hosts key added and no bastion-IP literal in any tracked
  repo file (OMN-15743's own regression test still passes) -- the IP is injected at container start from
  the operator-supplied GATEWAY_BASTION_IP env var.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2862 CI and receipt gates green"
    command: "gh pr checks 2862 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2862"
    description: "PR #2862 on OmniNode-ai/omnibase_infra is open on dev, adding the scoped DNS-bastion
      sidecar."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2862 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2862-ci"
    description: "PR #2862 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425): touches
      only the gateway-dns-bastion sidecar + compose wiring."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2862 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-omnibase_infra-2862-no-omn15743-regression"
    description: "OMN-15743's own regression test (test_gateway_resolves_cloud_broker_from_contract_not_extra_hosts)
      still passes against PR #2862's head -- no extra_hosts key added, no bastion-IP literal in a tracked
      repo file."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/unit/infra/test_gateway_compose_credentials.py?ref=efda11c35ce20656683122904a44d1ff45c02949 --jq '.content' | base64 -d | grep -c 'def test_gateway_resolves_cloud_broker_from_contract_not_extra_hosts'
