---
schema_version: "1.0.0"
ticket_id: "OMN-13349"
title: "refactor(omniintelligence): decompose Claude hook check extraction"
summary: >-
  omniintelligence PR #723 decomposes STOP payload check-result extraction into focused private helpers
  for gate, test, static-analysis, and numeric telemetry parsing. It preserves _extract_check_results_from_payload
  as the existing private import surface and keeps handler, model, and topic behavior unchanged while
  improving unit coverage around the extraction branches.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-pr-723"
    description: >-
      Per-code-PR binding receipt for omniintelligence PR #723 (jonah/omn-13349-claude-hook-decompose,
      base=dev). Binds OMN-13349 to the Claude hook check-result extraction refactor by pr_number and
      commit_sha. Targeted STOP/payload extractor tests pass with 65 tests, changed-file ruff and mypy
      strict pass, and pre-commit was run per PR verification.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13349/dod-omniintelligence-pr-723/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13349 contract and code-PR
      binding. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check can resolve
      a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13349/dod-occ-pr-self/command.yaml
