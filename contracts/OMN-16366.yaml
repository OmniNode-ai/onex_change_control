---
schema_version: "1.0.0"
ticket_id: "OMN-16366"
title: "Autobind OCC evidence for OMN-16366"
summary: >
  OCC Evidence-Source autobind companion for OmniNode-ai/omnibase_infra PR #3036 -- check-omnimarket-venv-drift.sh
  --repair fast-forwards the canonical omnimarket clone to origin/dev before installing, closing the reversed-drift
  regression where a repair could leave the in-process guard immediately re-failing on the next dispatch.
  The guard's refusal message also now names the repair/install commands by full path.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3036 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3036 on OmniNode-ai/omnibase_infra: the two hermetic --repair
      regression tests (RED before the fix, GREEN after) plus the full-path message test.
      Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd
      `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/scripts/test_check_omnimarket_venv_drift.py tests/unit/cli/test_omnimarket_drift_guard.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3036"
    description: "PR #3036 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/check-omnimarket-venv-drift.sh?ref=25756f4be05f002212152cffb08985084cf41da6 --jq '.content' | base64 -d | grep -c 'merge --ff-only origin/dev'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/cli/omnimarket_drift_guard.py?ref=25756f4be05f002212152cffb08985084cf41da6 --jq '.content' | base64 -d | grep -c 'infra_scripts = Path(omni_home)'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3036 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/scripts/test_check_omnimarket_venv_drift.py tests/unit/cli/test_omnimarket_drift_guard.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
