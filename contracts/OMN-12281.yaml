---
schema_version: "1.0.0"
ticket_id: "OMN-12281"
title: "feat(OMN-12281): add CLAUDE.md instruction effectiveness eval harness"
summary: >
  Add a sterile, host-run harness in onex-self-extending-agent that measures whether injecting a specific
  CLAUDE.md instruction changes Claude Code's behavior versus no instruction. Ships a contract-first surface:
  contract.yaml (node_instruction_evaluator, compute), typed frozen Pydantic models, and a golden-chain
  validation test. This OCC contract is the Evidence-Source for SEA PR #140.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Contract<->model field parity, fixture parse, and golden-chain topic naming validated
      by the unit suite."
    command: "cd onex-self-extending-agent && uv run pytest eval/instruction-eval/test_contract.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-140"
    description: >
      onex-self-extending-agent PR #140 carries the instruction-eval harness: contract.yaml, models.py,
      fixtures/sample_result.json, test_contract.py, and the run_eval.py typed-model wiring.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12281/dod-sea-pr-140/command.yaml
          && grep -q '^ticket_id: \"OMN-12281\"$' drift/dod_receipts/OMN-12281/dod-sea-pr-140/command.yaml
          && grep -q '^pr_number: 140$' drift/dod_receipts/OMN-12281/dod-sea-pr-140/command.yaml"
  - id: "dod-tests-pass"
    description: >
      The contract validation suite (eval/instruction-eval/test_contract.py) passes: 6 unit tests covering
      input/output field parity, model-name match, fixture parse, and golden-chain topic naming.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12281/dod-tests-pass/command.yaml
          && grep -q '^ticket_id: \"OMN-12281\"$' drift/dod_receipts/OMN-12281/dod-tests-pass/command.yaml"
  - id: "dod-occ-pr-1737"
    description: >
      OCC PR #1737 binds the central OMN-12281 instruction-eval harness contract and receipts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12281/dod-occ-pr-1737/command.yaml"
