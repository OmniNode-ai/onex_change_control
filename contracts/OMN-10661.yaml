---
schema_version: "1.0.0"
ticket_id: "OMN-10661"
title: "Bootstrap k3s addons on the dev cluster"
summary: >
  Add the env-aware addon bootstrap script required before live dev cluster addon installation. The product
  PR supplies the script and local validation; live dev cluster bootstrap remains gated on AWS credentials
  and explicit deploy approval.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-script-added"
    description: "omninode_infra PR #411 adds scripts/bootstrap-cluster-addons.sh."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10661/dod-script-added/command.yaml"
  - id: "dod-local-validation"
    description: "Local shell validation and pre-commit hooks pass for the addon bootstrap script."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10661/dod-local-validation/command.yaml"
  - id: "dod-pr-open"
    description: "omninode_infra PR #411 is open for OMN-10661 code review."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10661/dod-pr-open/command.yaml"
  - id: "dod-occ-pr"
    description: "Bind the central OCC evidence update to onex_change_control PR #930."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10661/dod-occ-pr/command.yaml"
