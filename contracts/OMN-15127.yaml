---
schema_version: "1.0.0"
ticket_id: "OMN-15127"
title: "nightly-promote scheduled runs default to real path and fail loud"
summary: "Repair nightly-promote.yml so scheduled runs are not permanent dry runs, use the Tier-2 fleet
  default, and fail loudly when real promotion gates block."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #4846 remains open and scoped to nightly-promote.yml."
    command: "gh pr view 4846 --repo OmniNode-ai/onex_change_control --json files"
  - kind: "manual"
    description: "nightly-promote.yml parses as valid workflow YAML."
    command: "python3 -c \"import pathlib, yaml; yaml.safe_load(pathlib.Path('.github/workflows/nightly-promote.yml').read_text());
      print('workflow-yaml-parse-pass')\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-onex-change-control-pr-4846-workflow-diff"
    description: "PR #4846 is scoped to .github/workflows/nightly-promote.yml."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4846 --repo OmniNode-ai/onex_change_control --json files --jq '[.files[].path]'"
  - id: "dod-onex-change-control-pr-4846-actionlint"
    description: "nightly-promote.yml parses as valid workflow YAML after the OMN-15127 workflow repair."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python3 -c \"import pathlib, yaml; yaml.safe_load(pathlib.Path('.github/workflows/nightly-promote.yml').read_text());
          print('workflow-yaml-parse-pass')\""
  - id: "occ-self-bind-pr-4846"
    description: "Binds OMN-15127 to OCC PR #4846."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4846 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
  - id: "dod-onex-change-control-pr-4895-workflow-diff"
    description: "PR #4895 remains scoped to .github/workflows/nightly-promote.yml."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4895 --repo OmniNode-ai/onex_change_control --json files --jq '[.files[].path]'"
  - id: "dod-onex-change-control-pr-4895-actionlint"
    description: "nightly-promote.yml parses as valid workflow YAML after the parity no-op repair."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python3 -c \"import pathlib, yaml; yaml.safe_load(pathlib.Path('.github/workflows/nightly-promote.yml').read_text());
          print('workflow-yaml-parse-pass')\""
  - id: "occ-self-bind-pr-4895"
    description: "Binds OMN-15127 follow-up evidence to OCC PR #4895."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4895 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid"
