---
schema_version: "1.0.0"
ticket_id: "OMN-10171"
title: "feat(OMN-10171): add skill-backing-node liveness validator to omnibase_core (SEAM-2)"
summary: >
  Move the skill-backing-node liveness validator from omniclaude to omnibase_core per ADR adr-2026-04-28-skill-liveness-validator-home.md.
  Adds src/omnibase_core/validation/validator_skill_backing_node.py with load_allowlist, extract_backing_node,
  check_node_liveness, scan, and main entry points, plus 20 unit tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "validator unit tests pass and the new module is mypy-strict clean"
    command: "cd omnibase_core && uv run pytest tests/unit/validation/test_validator_skill_backing_node.py
      -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "omnibase_core PR #1069 carries the relocated skill-backing-node liveness validator with
      its unit suite and mypy-strict check passing locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10171/dod-001/command.yaml && grep
          -q '^ticket_id: \"OMN-10171\"$' drift/dod_receipts/OMN-10171/dod-001/command.yaml && grep -q
          '^pr_number: 1069$' drift/dod_receipts/OMN-10171/dod-001/command.yaml"
  - id: "dod-occ-pr-972"
    description: "OCC PR #972 carries the OMN-10171 central contract and downstream receipt needed by
      omnibase_core PR #1069."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10171/dod-occ-pr-972/command.yaml
          && grep -q '^ticket_id: \"OMN-10171\"$' drift/dod_receipts/OMN-10171/dod-occ-pr-972/command.yaml
          && grep -q '^pr_number: 972$' drift/dod_receipts/OMN-10171/dod-occ-pr-972/command.yaml"
