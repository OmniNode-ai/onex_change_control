---
schema_version: "1.0.0"
ticket_id: "OMN-13009"
title: "Broker disk watermark compute node"
summary: >
  omnibase_infra PR #2085 adds node_broker_disk_watermark_compute, an infra-exclusive COMPUTE node that
  probes docker data-root and named broker volume disk usage, classifies each path against WARN and P0
  thresholds, and independently escalates when free bytes are at or below Redpanda's storage_min_free_bytes
  floor. The change encodes the demo-day system:28 failure mode as a deterministic node with injectable
  docker_info and disk_usage seams plus focused unit coverage.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Broker disk watermark unit suite passes."
    command: "uv run pytest tests/unit/nodes/test_handler_broker_disk_watermark.py -q"
  - kind: "tests"
    description: "New node source passes strict mypy."
    command: "uv run mypy src/omnibase_infra/nodes/node_broker_disk_watermark_compute/ --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2085"
    description: "omnibase_infra PR #2085 carries the broker disk watermark compute node and allowlist
      entry."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13009/dod-infra-pr-2085/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused unit suite for broker disk watermark classification, seams, and aggregation
      passes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13009/dod-focused-tests/command.yaml"
  - id: "dod-deploy-no-live-mutation"
    description: "Source, contract, allowlist, and tests only; no live deploy, docker mutation, broker
      mutation, migration, or runtime restart required before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13009/dod-deploy-no-live-mutation/command.yaml"
  - id: "dod-occ-pr-self"
    description: "OCC self-binding receipt for the central evidence PR carrying the OMN-13009 contract
      and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13009/dod-occ-pr-self/command.yaml"
