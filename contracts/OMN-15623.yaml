---
schema_version: "1.0.0"
ticket_id: "OMN-15623"
title: "OCC evidence for omninode_infra PR 799"
summary: >
  Manual OCC companion for OmniNode-ai/omninode_infra PR #799, which cites OMN-15623 in its title and
  therefore requires central OCC evidence for that ticket at the pinned Evidence-Source.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #799 CI checks green"
    command: "gh pr checks 799 --repo OmniNode-ai/omninode_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-799-1176de19"
    description: "PR #799 on OmniNode-ai/omninode_infra at head 1176de195af3ac53740f9ce0e1ef87a2a3deae90."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/799 --jq 'if (.base.ref == "dev" and .head.sha == "1176de195af3ac53740f9ce0e1ef87a2a3deae90" and .state == "open") then "true" else error("unexpected #799 state") end'
  - id: "dod-omninode-infra-pr-799-repo-scripts-tests"
    description: "Repo Scripts Tests passed for omninode_infra PR #799 at head 1176de195af3ac53740f9ce0e1ef87a2a3deae90."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          python - <<'PY'
          import json
          import subprocess

          raw = subprocess.check_output([
              "gh",
              "api",
              "repos/OmniNode-ai/omninode_infra/commits/1176de195af3ac53740f9ce0e1ef87a2a3deae90/check-runs?per_page=100",
          ], text=True)
          runs = json.loads(raw)["check_runs"]
          assert any(
              run["name"] == "Repo Scripts Tests"
              and run["status"] == "completed"
              and run["conclusion"] == "success"
              for run in runs
          ), "Repo Scripts Tests not green for #799 head"
          print("true")
          PY
  - id: "occ-self-bind-pr-5944"
    description: "Binds OMN-15623 to OCC companion PR #5944."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/pulls/5944 --jq 'if (.number == 5944 and .state == "open" and .head.ref == "codex/omn-15623-omninode-infra-799-occ") then "true" else error("unexpected #5944 state") end'
