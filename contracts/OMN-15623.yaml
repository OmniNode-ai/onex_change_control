---
schema_version: "1.0.0"
ticket_id: "OMN-15623"
title: "OCC evidence for omninode_infra PR 799"
summary: >
  Manual OCC companion repair for OmniNode-ai/omninode_infra PR #799 current head, because the PR title
  cites OMN-15623 and receipt gate requires central OCC evidence for that ticket at the pinned Evidence-Source.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #799 CI checks green"
    command: "gh pr checks 799 --repo OmniNode-ai/omninode_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-799-26f9010d"
    description: "PR #799 on OmniNode-ai/omninode_infra at head 26f9010dafdddff92c99dfa36064c339eac30df9."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/799 --jq 'if (.base.ref == "dev" and .head.sha == "26f9010dafdddff92c99dfa36064c339eac30df9" and .state == "open") then "true" else error("unexpected #799 state") end'
  - id: "dod-pr799-tests-26f9010d"
    description: "Repo Scripts Tests passed for omninode_infra PR #799 at head 26f9010dafdddff92c99dfa36064c339eac30df9."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          python - <<'PY'
          import json
          import subprocess

          raw = subprocess.check_output([
              "gh",
              "api",
              "repos/OmniNode-ai/omninode_infra/commits/26f9010dafdddff92c99dfa36064c339eac30df9/check-runs?per_page=100",
          ], text=True)
          runs = json.loads(raw)["check_runs"]
          assert any(
              run["name"] == "Repo Scripts Tests"
              and run["status"] == "completed"
              and run["conclusion"] == "success"
              for run in runs
          ), "Repo Scripts Tests not green for #799 head"
          print("true")
          PY
