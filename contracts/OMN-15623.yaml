---
schema_version: "1.0.0"
ticket_id: "OMN-15623"
title: "OCC evidence for omninode_infra PR 799"
summary: >
  Manual OCC companion repair for OmniNode-ai/omninode_infra PR #799, because the PR title cites OMN-15623
  and receipt gate requires central OCC evidence for that ticket at the pinned Evidence-Source.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #799 CI checks green"
    command: "gh pr checks 799 --repo OmniNode-ai/omninode_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-799"
    description: "PR #799 on OmniNode-ai/omninode_infra at head 3dac56d2310ee5be21a54dccd9b066e4ce0ebecf."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/799 --jq 'if (.base.ref == "dev" and .head.sha == "3dac56d2310ee5be21a54dccd9b066e4ce0ebecf" and .state == "open") then "true" else error("unexpected #799 state") end'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5942"
    description: "Binds OMN-15623 to OCC companion PR #5942."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/pulls/5942 --jq 'if (.number == 5942 and .state == "open" and .head.ref == "auto/omninode-ai-omninode_infra-pr-799-occ-autobind") then "true" else error("unexpected #5942 state") end'
  - id: "occ-self-bind-pr-5947"
    description: "Binds OMN-15623 to OCC companion PR #5947 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5947 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-5949"
    description: "Binds OMN-15623 to OCC companion PR #5949 (self-bind)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/pulls/5949 --jq 'if (.number == 5949 and .state == "open" and .head.sha == "d80b57ffcbe4909226401b901f3ade3b0082e2f4") then "true" else error("unexpected #5949 state") end'
  - id: "dod-OmniNode-ai-omninode_infra-pr-802"
    description: >-
      PR #802 on OmniNode-ai/omninode_infra at head c405ddb17077fd63dda3ada83910e2348c098ef2 — the onex-dev
      runtime+migrate digest bump to omnibase_infra build SHA bf070a94e plus the DELEGATION_ROUTING_TIERS_PATH
      convergence. Predicate accepts the merged terminal state as well as open, so it does not go stale
      the moment the PR lands (OMN-15567 lesson: `.state == "open"` predicates fail dod_verify post-merge).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/802 --jq 'if (.base.ref == "dev" and .head.sha == "c405ddb17077fd63dda3ada83910e2348c098ef2" and (.state == "open" or .merged == true)) then "true" else error("unexpected #802 state") end'
  - id: "dod-omn15623-pr802-image-precondition-ancestry"
    description: >-
      The substantive claim of #802: the newly pinned images were built from omnibase_infra bf070a94e,
      which carries BOTH boot-fix preconditions as ancestors — 406d0d698 (OMN-15656, the deployment-evidence-reducer
      schema fix) and 381fccc8d (OMN-15645, the COPY that bakes /app/config/delegation/routing_tiers.yaml
      and is the precondition for the tiers-path convergence). Pinned on immutable commit SHAs, not branch
      refs.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/compare/406d0d698...bf070a94e26f95068c7de6fc72a10f1551dbcc07 --jq 'if (.status == "ahead" or .status == "identical") then "true" else error("406d0d698 is not an ancestor of bf070a94e") end' && gh api repos/OmniNode-ai/omnibase_infra/compare/381fccc8d...bf070a94e26f95068c7de6fc72a10f1551dbcc07 --jq 'if (.status == "ahead" or .status == "identical") then "true" else error("381fccc8d is not an ancestor of bf070a94e") end'
  - id: "occ-self-bind-pr-5991"
    description: >-
      Binds OMN-15623 to OCC companion PR #5991 (self-bind), the companion carrying the #802 evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5991 --repo OmniNode-ai/onex_change_control --json number,state"
