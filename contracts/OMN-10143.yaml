---
schema_version: "1.0.0"
ticket_id: OMN-10143
title: "Add cross-CLI originator effect node"
summary: "Shared CLI entry point for opencode/codex delegation. Thin EFFECT node: accepts delegation prompt,
  builds envelope, publishes to onex.cmd.omnimarket.cross-cli-delegation-requested.v1 via EmitClient Unix
  socket."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Unit tests for cross-CLI originator handler pass"
    command: "uv run pytest src/omnimarket/nodes/node_cross_cli_originator/tests/ -v"
  - kind: "manual"
    description: "Ruff lint and format clean"
    command: "uv run ruff check src/omnimarket/nodes/node_cross_cli_originator/ && uv run ruff format
      --check src/omnimarket/nodes/node_cross_cli_originator/"
  - kind: "manual"
    description: "mypy strict clean on handler"
    command: "uv run mypy src/omnimarket/nodes/node_cross_cli_originator/ --strict"
  - kind: "manual"
    description: "Post-merge deploy smoke plan for cross-CLI originator node"
    command: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -m omnimarket.nodes.node_cross_cli_originator
      --prompt 'smoke test'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "7 unit tests covering publish path, correlation_id passthrough, event_type assertion,
      client close, contract topic validation, handler type/category"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest src/omnimarket/nodes/node_cross_cli_originator/tests/ -v"
  - id: "dod-lint"
    description: "Ruff lint and format clean on node_cross_cli_originator"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_cross_cli_originator/ && uv run ruff
          format --check src/omnimarket/nodes/node_cross_cli_originator/"
  - id: "dod-typecheck"
    description: "mypy --strict clean on node_cross_cli_originator"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run mypy src/omnimarket/nodes/node_cross_cli_originator/ --strict"
  - id: "dod-deploy"
    description: "Post-merge deploy smoke plan for cross-CLI originator node; no live deploy performed
      in this PR — deploy-agent inactive per OMN-8841"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -m omnimarket.nodes.node_cross_cli_originator
          --prompt 'smoke test'"
