---
schema_version: "1.0.0"
ticket_id: OMN-10143
title: "Add cross-CLI originator effect node"
summary: "Shared CLI entry point for opencode/codex delegation. Thin EFFECT node: accepts delegation prompt,
  builds envelope, publishes to onex.cmd.omnimarket.cross-cli-delegation-requested.v1 via EmitClient Unix
  socket."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Unit tests for cross-CLI originator handler pass"
    command: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10143/dod-unit-tests/command.yaml\""
  - kind: "manual"
    description: "Ruff lint and format clean"
    command: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10143/dod-lint/command.yaml\""
  - kind: "manual"
    description: "mypy strict clean on handler"
    command: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10143/dod-typecheck/command.yaml\""
  - kind: "manual"
    description: "Post-merge deploy smoke plan for cross-CLI originator node"
    command: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10143/dod-deploy/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "7 unit tests covering publish path, correlation_id passthrough, event_type assertion,
      client close, contract topic validation, handler type/category"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10143/dod-unit-tests/command.yaml\""
  - id: "dod-lint"
    description: "Ruff lint and format clean on node_cross_cli_originator"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10143/dod-lint/command.yaml\""
  - id: "dod-typecheck"
    description: "mypy --strict clean on node_cross_cli_originator"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10143/dod-typecheck/command.yaml\""
  - id: "dod-deploy"
    description: "Post-merge deploy smoke plan for cross-CLI originator node; no live deploy performed
      in this PR — deploy-agent inactive per OMN-8841"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10143/dod-deploy/command.yaml\""
