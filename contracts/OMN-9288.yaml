---
schema_version: "1.0.0"
ticket_id: OMN-9288
title: "merge-sweep stalled merge queue check-suite remediation"
summary: "Detect PRs stuck in merge queue AWAITING_CHECKS with no merge_group workflow run for the queue
  head SHA, then cap dequeue/requeue remediation at two attempts per PR per hour."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Focused PR lifecycle inventory, merge queue adapter, and orchestrator tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9288/dod-focused-tests/command.yaml"
  - kind: "manual"
    description: "Touched PR lifecycle paths pass lint"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9288/dod-lint/command.yaml"
  - kind: "manual"
    description: "PR lifecycle upgrade suite remains compatible with legacy stuck queue detection"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9288/dod-legacy-upgrade/command.yaml"
  - kind: "manual"
    description: "Repo-local contract YAML parses"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9288/dod-contract-parse/command.yaml"
  - kind: "manual"
    description: "Codex merge-sweep runtime request compiles without broker publish"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9288/dod-compile-only/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused PR lifecycle inventory, merge queue adapter, and orchestrator tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9288/dod-focused-tests/command.yaml
          && grep -q '^ticket_id: OMN-9288$' drift/dod_receipts/OMN-9288/dod-focused-tests/command.yaml
          && grep -q '^evidence_item_id: dod-focused-tests$' drift/dod_receipts/OMN-9288/dod-focused-tests/command.yaml"
  - id: "dod-lint"
    description: "Touched PR lifecycle paths pass lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9288/dod-lint/command.yaml && grep
          -q '^ticket_id: OMN-9288$' drift/dod_receipts/OMN-9288/dod-lint/command.yaml && grep -q '^evidence_item_id:
          dod-lint$' drift/dod_receipts/OMN-9288/dod-lint/command.yaml"
  - id: "dod-legacy-upgrade"
    description: "PR lifecycle upgrade suite remains compatible with legacy stuck queue detection"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9288/dod-legacy-upgrade/command.yaml
          && grep -q '^ticket_id: OMN-9288$' drift/dod_receipts/OMN-9288/dod-legacy-upgrade/command.yaml
          && grep -q '^evidence_item_id: dod-legacy-upgrade$' drift/dod_receipts/OMN-9288/dod-legacy-upgrade/command.yaml"
  - id: "dod-contract-parse"
    description: "Repo-local contract YAML parses"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9288/dod-contract-parse/command.yaml
          && grep -q '^ticket_id: OMN-9288$' drift/dod_receipts/OMN-9288/dod-contract-parse/command.yaml
          && grep -q '^evidence_item_id: dod-contract-parse$' drift/dod_receipts/OMN-9288/dod-contract-parse/command.yaml"
  - id: "dod-compile-only"
    description: "Codex merge-sweep runtime request compiles without broker publish"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9288/dod-compile-only/command.yaml
          && grep -q '^ticket_id: OMN-9288$' drift/dod_receipts/OMN-9288/dod-compile-only/command.yaml
          && grep -q '^evidence_item_id: dod-compile-only$' drift/dod_receipts/OMN-9288/dod-compile-only/command.yaml"
