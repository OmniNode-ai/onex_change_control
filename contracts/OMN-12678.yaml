---
schema_version: "1.0.0"
ticket_id: "OMN-12678"
title: "Delegation call effect consumes route-supplied endpoint URLs"
summary: >-
  Central evidence for the delegation call effect change that removes endpoint
  environment resolution from the handler. The handler validates the
  route-supplied endpoint URL and does not double-append OpenAI chat-completion
  paths when a full endpoint is provided.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-call-effect-endpoint-tests"
    description: "Focused omnimarket call-effect tests prove invalid endpoint refs fail and full chat-completions URLs are not double-appended."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12678/dod-call-effect-endpoint-tests/command.yaml"
