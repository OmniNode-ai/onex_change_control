---
schema_version: "1.0.0"
ticket_id: "OMN-12690"
title: "Workstream I-B / Gate 1: require live golden-chain CI gate"
summary: >-
  Central evidence that omniclaude PR #1731 wires the existing
  node_golden_chain_sweep_orchestrator.run_sweep harness into required CI via
  a live Redpanda/Postgres golden-chain job and a module CLI caller. The gate
  publishes the five registered golden-chain events to Kafka, materializes
  synthetic projection rows into Postgres through a bounded CI bridge, then
  relies on the authoritative sweep to poll, assert, persist, and fail on any
  non-PASS chain summary.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-live-golden-chain-ci"
    description: "Focused omniclaude tests and static checks prove the live golden-chain CI gate, module
      entrypoint, workflow wiring, and CI projection bridge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12690/dod-omniclaude-live-golden-chain-ci/command.yaml"
          && grep -q '^pr_number: 1731$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12690/dod-omniclaude-live-golden-chain-ci/command.yaml"
  - id: "dod-deploy-gate-runtime-path"
    description: "Deploy-gate evidence for omniclaude CI-only live golden-chain gate changes in PR #1731."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12690/dod-deploy-gate-runtime-path/command.yaml"
          && grep -q '^pr_number: 1731$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12690/dod-deploy-gate-runtime-path/command.yaml"
  - id: "dod-occ-pr-2174"
    description: "onex_change_control PR #2174 carries the OMN-12690 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12690/dod-occ-pr-2174/command.yaml"
          && grep -q '^pr_number: 2174$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12690/dod-occ-pr-2174/command.yaml"
