---
schema_version: "1.0.0"
ticket_id: "OMN-8065"
title: "Scoped wrapper adapters for TicketService, EventBus, LLMProvider"
summary: "Add scoped wrapper adapters that enforce allowed actions before protocol I/O."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - public_api
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #1504 scoped adapter and invariant violation tests pass locally."
    command: "cd omni_worktrees/OMN-8065/omnibase_infra-fix && PYTHONPATH=\"$PWD/src\" uv run pytest tests/unit/adapters/test_scoped_adapters.py tests/unit/test_invariant_violation.py tests/integration/test_invariant_violation.py -q"
  - kind: "manual"
    description: "Runtime-adjacent env parity, topic provisioning, and raw topic literal checks pass or are locally skipped for missing omninode_infra checkout."
    command: "cd omni_worktrees/OMN-8065/omnibase_infra-fix && PYTHONPATH=\"$PWD/src\" uv run pytest tests/ci/test_env_parity.py tests/unit/event_bus/test_service_topic_manager.py tests/integration/event_bus/test_topic_provisioner_integration.py -q && uv run python scripts/validation/check_topic_literals.py --baseline scripts/validation/topic_literal_baseline.txt"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Scoped adapter unit tests prove denied actions raise before delegated I/O."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8065/dod-001/command.yaml\" && grep -q '^evidence_item_id: \"dod-001\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8065/dod-001/command.yaml\" && grep -q '^commit_sha: \"091e4a5b' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8065/dod-001/command.yaml\" && grep -q '^pr_number: 1504$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8065/dod-001/command.yaml\""
  - id: "dod-002"
    description: "InvariantViolation tests prove structured CONTRACT_VIOLATION context and exported error behavior."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8065/dod-002/command.yaml\" && grep -q '^evidence_item_id: \"dod-002\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8065/dod-002/command.yaml\" && grep -q '^commit_sha: \"091e4a5b' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8065/dod-002/command.yaml\" && grep -q '^pr_number: 1504$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8065/dod-002/command.yaml\""
  - id: "dod-deploy"
    description: "Runtime-adjacent deploy preflight checks pass without live runtime restart."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8065/dod-deploy/command.yaml\" && grep -q '^evidence_item_id: \"dod-deploy\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8065/dod-deploy/command.yaml\" && grep -q '^commit_sha: \"091e4a5b' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8065/dod-deploy/command.yaml\" && grep -q '^pr_number: 1504$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8065/dod-deploy/command.yaml\""
