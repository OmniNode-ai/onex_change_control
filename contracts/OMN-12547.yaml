---
schema_version: "1.0.0"
ticket_id: "OMN-12547"
title: "refactor(OMN-12547): S-1c unify ModelHandlerRoutingEntry — remove dead validators, rewrite build_routing_table
  for 5-field shape"
summary: >-
  Remove broken validators from ModelHandlerRoutingSubcontract that referenced undefined names (model_validator,
  ModelOnexError, EnumCoreErrorCode, Self) and non-existent fields (routing_key, handler_key, priority)
  on the new 5-field ModelHandlerRoutingEntry shape. Rewrite build_routing_table to use the live 5-field
  shape (handler.name as handler key, event_model.name / operation / event_type as discriminators per
  routing strategy). Delete test_mixin_handler_routing.py (1515 lines exercising the dead routing_key/handler_key
  shape). Fix test_model_contract_base_extensions.py to use new 5-field shape.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr"
    description: >-
      omnibase_core PR merges model_handler_routing_entry.py (5-field shape) and model_handler_routing_subcontract.py
      (dead validators removed, build_routing_table rewritten). test_mixin_handler_routing.py deleted;
      test_model_contract_base_extensions.py updated. 765 tests pass in models + mixins scope; ruff clean;
      mypy pre-existing errors only (not in changed files).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12547/dod-core-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-12547"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12547/dod-core-pr/command.yaml"
