---
schema_version: "1.0.0"
ticket_id: "OMN-10959"
title: "Add ulimit support to CatalogManifest schema to prevent broker FD exhaustion"
summary: "Add typed catalog ulimit support, render ulimits into generated compose, and bind Redpanda nofile
  limits in the catalog manifest."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Catalog manifest schema, renderer, resolver, and validator tests pass."
    command: "uv run pytest tests/unit/infra tests/integration/docker/test_catalog_ulimits_render.py -q"
  - kind: "ci"
    description: "omnibase_infra PR #1594 carries the catalog ulimit schema/render change."
    command: "gh pr checks 1594 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-local-tests"
    description: "omnibase_infra catalog unit tests pass for OMN-10959."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10959/dod-local-tests/command.yaml"
  - id: "dod-lint"
    description: "Touched catalog schema/render Python files pass ruff check."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10959/dod-lint/command.yaml"
  - id: "dod-infra-pr-1594"
    description: "omnibase_infra PR #1594 is open with the committed OMN-10959 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10959/dod-infra-pr-1594/command.yaml
          && grep -q '^pr_number: 1594$' drift/dod_receipts/OMN-10959/dod-infra-pr-1594/command.yaml"
  - id: "dod-occ-pr-1026"
    description: "onex_change_control PR #1026 carries the OMN-10959 contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10959/dod-occ-pr-1026/command.yaml
          && grep -q '^pr_number: 1026$' drift/dod_receipts/OMN-10959/dod-occ-pr-1026/command.yaml"
