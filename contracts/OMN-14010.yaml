---
schema_version: "1.0.0"
ticket_id: "OMN-14010"
title: "fix(OMN-14010): repoint and inline dead omnimemory imports"
summary: >-
  omnimarket PR #1633 repairs five runtime discovery failures caused by stale omnimemory import paths.
  Three nodes are repointed from deleted utility paths to the current omnimemory adapter modules, and
  two persona DTO model sets are made self-contained in omnimarket after their never-shipped omnimemory
  node scaffolding was removed. The PR also refreshes the omnimarket omninode-memory pin so local and
  CI resolution match the deployed import surface.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnimarket PR #1633 carries the OMN-14010 import repair"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14010/dod-omnimarket-pr-1633/command.yaml"
  - kind: "manual"
    description: "runtime-path import repair deploy scope is explicitly documented"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14010/dod-deploy-scope/command.yaml"
  - kind: "manual"
    description: "this onex_change_control PR carries central OCC evidence for OMN-14010"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14010/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1633"
    description: >-
      Consuming-PR binding: omnimarket PR #1633 at head dd9409ccb5e4b20d2e0a5d96f3c2d7df9de3493a carries
      the OMN-14010 dead omnimemory import repair, dependency pin refresh, and focused regression coverage
      described in the PR test plan.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14010/dod-omnimarket-pr-1633/command.yaml
          && grep -q '^pr_number: 1633$' drift/dod_receipts/OMN-14010/dod-omnimarket-pr-1633/command.yaml"
  - id: "dod-deploy-scope"
    description: >-
      Deploy-scope binding: this runtime-path import repair rides the normal post-merge dev and stability
      redeploy cadence; no out-of-band production deploy is in scope. Post-merge validation must confirm
      the repaired runtime import paths load on the deployed lane before promotion.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14010/dod-deploy-scope/command.yaml
          && grep -q 'deploy' drift/dod_receipts/OMN-14010/dod-deploy-scope/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-14010 central contract and
      PASS receipts consumed by omnimarket PR #1633 via its Evidence-Source line.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14010/dod-occ-self/command.yaml"
