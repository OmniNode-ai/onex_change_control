---
schema_version: "1.0.0"
ticket_id: "OMN-12676"
title: "Delegation orchestrator carries API key references, not secret values"
summary: >-
  Central evidence for the core and omnimarket proof-slice changes that remove
  resolved API-key values from ModelInferenceIntent. The wire model now carries
  api_key_ref, and omnimarket resolves that reference only inside the inference
  effect handler immediately before the outbound provider call.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-intent-reference-tests"
    description: "omnibase_core focused wire-model tests pass for api_key_ref and reject the old api_key field."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12676/dod-core-intent-reference-tests/command.yaml"
  - id: "dod-omnimarket-secret-boundary-tests"
    description: "omnimarket focused proof-slice tests pass with the core api_key_ref model overlaid."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12676/dod-omnimarket-secret-boundary-tests/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Deploy gate evidence: no live deploy was performed; runtime-path changes require post-merge validation before promotion."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12676/dod-deploy-assessment/command.yaml"
          && grep -qi 'deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12676/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2164"
    description: "onex_change_control PR #2164 carries the proof-slice contracts and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12676/dod-occ-pr-2164/command.yaml"
          && grep -q '^pr_number: 2164$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12676/dod-occ-pr-2164/command.yaml"
