---
schema_version: "1.0.0"
ticket_id: OMN-12392
title: "Classify omnidash projection-reader and fixture generator reachability findings"
summary: >
  Classified omnidash projection-reader and fixture-generator findings.
  Repointed run-generate-fixtures.sh to the local script and corrected
  CLAUDE.md fixture/lineage docs. Shipped via merged omnidash PR #162 (base dev).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnidash PR #162 merged to dev with green CI"
    command: "gh pr view 162 --repo OmniNode-ai/omnidash --json state"
  - kind: "docs"
    description: "omnidash projection/dashboard classification recorded in OMN-12368 verification doc"
    command: "test -f docs/evidence/OMN-12368-verification/dashboard.md"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-merged"
    description: "omnidash PR #162 is merged (durable code proof)"
    source: "ci"
    checks:
      - check_type: "command"
        check_value: "test \"$(gh pr view 162 --repo OmniNode-ai/omnidash --json state --jq .state)\" = MERGED"
  - id: "dod-classification-doc"
    description: "omnidash dashboard/projection classification evidence doc is tracked in omni_home"
    source: "docs"
    checks:
      - check_type: "command"
        check_value: "git -C \"$OMNI_HOME\" ls-files --error-unmatch docs/evidence/OMN-12368-verification/dashboard.md"
