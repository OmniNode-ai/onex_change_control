---
schema_version: "1.0.0"
ticket_id: "OMN-16426"
title: "Autobind OCC evidence for OMN-16426"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2852.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2852 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2852"
    description: "PR #2852 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2852 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2852 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2852 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2852 --repo OmniNode-ai/omnibase_infra --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2852"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6930"
    description: "Binds OMN-16426 to OCC companion PR #6930 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6930 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-deploy-assessment-falsifiable"
    description: >
      Falsifiable deploy probe (OMN-14443/OMN-14505) superseding dod-deploy-assessment's vacuous gh/grep-over-own-diff
      check. PR #2852 fixes a nested variable expansion in docker-compose.infra.yml's GATEWAY_ATTACH_KEYCLOAK_INTROSPECTION_URL
      default so it resolves to the flat literal http://keycloak:8080/realms/omninode/protocol/openid-connect/token/introspect
      instead of the broken pre-fix nested fallback. Hermetic live-content probe (OMN-14051 carve-out
      -- the dev-lane runtime has not been redeployed with this compose change yet, so a live SSH/docker-exec
      probe cannot pass in CI and is inadmissible there per OMN-15309): reads the actual file content
      at PR #2852's head commit straight from GitHub and asserts the fixed flat literal is genuinely present
      -- falsifiable against the real merged source, not this PR's own diff or receipt text.
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.infra.yml?ref=6e5958e626412086136a5d4a8c8caa35bcb4792f
          --jq .content | base64 -d | grep -q 'GATEWAY_ATTACH_KEYCLOAK_INTROSPECTION_URL: ${GATEWAY_ATTACH_KEYCLOAK_INTROSPECTION_URL:-http://keycloak:8080/realms/omninode/protocol/openid-connect/token/introspect}'"
