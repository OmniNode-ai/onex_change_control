---
schema_version: "1.0.0"
ticket_id: "OMN-16426"
title: "Autobind OCC evidence for OMN-16426"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2852.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2852 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2852"
    description: "PR #2852 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2852 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2852 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: >
      Deploy-scope DoD for PR #2852 (OMN-16426): fixes a nested variable expansion in docker-compose.infra.yml's
      GATEWAY_ATTACH_KEYCLOAK_INTROSPECTION_URL default so it resolves to the flat literal http://keycloak:8080/realms/omninode/protocol/openid-connect/token/introspect
      instead of the broken pre-fix nested fallback. Falsifiable live-surface probe (OMN-14443/OMN-14505):
      queries the dev-lane omninode-runtime container's actually-resolved env var on .201 over SSH — its
      exit status depends on the deployed compose file's real state, not on a grep of this PR's own diff
      or receipt.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "ssh jonah@192.168.86.201 \"docker exec omninode-runtime printenv GATEWAY_ATTACH_KEYCLOAK_INTROSPECTION_URL\"
          | grep -q '^http://keycloak:8080/realms/omninode/protocol/openid-connect/token/introspect$'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2852"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6930"
    description: "Binds OMN-16426 to OCC companion PR #6930 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6930 --repo OmniNode-ai/onex_change_control --json number,state"
