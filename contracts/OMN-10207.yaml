---
schema_version: "1.0.0"
ticket_id: "OMN-10207"
title: "Host-live Pattern B broker proof"
summary: >
  Enables and proves the runtime-owned Pattern B ingress path for host-live OmniMarket skill dispatch
  by binding the main runtime profile to local ingress, the OmniMarket Pattern B command topic, and a
  host-visible runtime socket mount.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1531-head"
    description: "Bind OMN-10207 evidence to omnibase_infra PR #1531 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1531 --repo OmniNode-ai/omnibase_infra --json headRefOid,title"
  - id: "dod-focused-tests"
    description: "Focused runtime local ingress, Pattern B broker, runtime config, and serialization tests
      passed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10207/dod-focused-tests/command.yaml\""
  - id: "dod-runtime-config-loader"
    description: "Runtime config loader accepts the shipped local ingress and Pattern B broker settings."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10207/dod-runtime-config-loader/command.yaml\""
  - id: "dod-compose-config"
    description: "Stability compose render includes the host-visible local runtime socket bind mount."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10207/dod-compose-config/command.yaml\""
