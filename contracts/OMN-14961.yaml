---
schema_version: "1.0.0"
ticket_id: "OMN-14961"
title: "Add core execution-tier no-nodes import-linter contract"
description: >-
  Central OCC evidence for omnibase_core PR #1485. The product PR adds the [importlinter:contract:core-execution-tier-no-nodes]
  block, binding runtime, pipeline, cli, and validators against direct omnibase_core.nodes imports while
  documenting why infrastructure is intentionally excluded from this mechanical lock.
dod_evidence:
  - id: "dod-omnibase-core-pr-1485-head"
    description: "Binds omnibase_core PR #1485 to exact reviewed head d9e0124cc2f96876a866df441578b532eb12fcea."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1485 --repo OmniNode-ai/omnibase_core --json headRefOid --jq .headRefOid
          | grep -Fx d9e0124cc2f96876a866df441578b532eb12fcea"
  - id: "dod-omnibase-core-pr-1485-scope"
    description: "PR #1485 diff is limited to the import-linter contract file."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test \"$(gh pr diff 1485 --repo OmniNode-ai/omnibase_core --name-only)\" = \".importlinter\""
  - id: "dod-omnibase-core-pr-1485-contract-content"
    description: "PR #1485 diff adds the core execution-tier no-nodes contract and forbids omnibase_core.nodes."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 1485 --repo OmniNode-ai/omnibase_core | grep -F '[importlinter:contract:core-execution-tier-no-nodes]'
          && gh pr diff 1485 --repo OmniNode-ai/omnibase_core | grep -F '    omnibase_core.nodes'"
  - id: "dod-omnibase-core-pr-1485-body-evidence"
    description: "PR #1485 body records lint-imports green, seeded violation RED->GREEN, precondition
      readback, and pre-commit proof."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1485 --repo OmniNode-ai/omnibase_core --json body --jq .body | grep -F
          'uv run lint-imports' && gh pr view 1485 --repo OmniNode-ai/omnibase_core --json body --jq .body
          | grep -F 'Seeded-violation RED->GREEN' && gh pr view 1485 --repo OmniNode-ai/omnibase_core
          --json body --jq .body | grep -F 'uv run pre-commit run --files .importlinter'"
  - id: "dod-omnibase-core-pr-1485-close-discipline"
    description: >-
      PR #1485 directly closes OMN-14961 because its diff implements that ticket's import-linter contract
      acceptance surface.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1485 --repo OmniNode-ai/omnibase_core --json body --jq .body | grep -F
          'Closes OMN-14961'"
  - id: "occ-self-bind-pr-4680-omn14961"
    description: "OCC companion PR #4680 self-bind for OMN-14961."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test \"$(gh api repos/OmniNode-ai/onex_change_control/pulls/4680 --jq .head.ref)\"
          = codex-letter/occ-omn-14961-core1485"
