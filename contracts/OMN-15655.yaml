---
schema_version: "1.0.0"
ticket_id: "OMN-15655"
title: "Classify deployment-evidence relations as omninode_internal"
summary: >
  Central OCC evidence for OmniNode-ai/omnimarket PR #2010, which repairs the strict auto-wiring schema
  classification for deployment evidence projections.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2010 CI and receipt gates green"
    command: "gh pr checks 2010 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2010"
    description: "PR #2010 on OmniNode-ai/omnimarket is open on dev at the exact evidence-bound head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2010 --jq 'if (.base.ref == "dev" and .head.sha == "c0484781c4932a3afe9136f2041b441e7179ead4" and .state == "open") then "true" else error("unexpected #2010 state") end'
  - id: "dod-market-2010-schema-source-probe"
    description: "Exact-head source probe proves deployment evidence reducer relations now target omninode_internal,
      not public."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_deployment_evidence_reducer/contract.yaml?ref=c0484781c4932a3afe9136f2041b441e7179ead4' --jq .content | base64 -d | python3 -c 'import sys,yaml; doc=yaml.safe_load(sys.stdin.read()); tables={t["name"]:t for t in doc["db_io"]["db_tables"]}; assert tables["deployment_evidence_projection"]["schema"] == "omninode_internal"; assert tables["deployment_readiness_projection"]["schema"] == "omninode_internal"; assert all(tables[name]["schema"] != "public" for name in ("deployment_evidence_projection","deployment_readiness_projection"))'
  - id: "occ-self-bind-pr-5962"
    description: "Binds OMN-15655 to OCC companion PR #5962."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/pulls/5962 --jq 'if (.number == 5962 and .state == "open" and .base.ref == "dev" and .head.ref == "codex/omn-15655-market2010-occ") then "true" else error("unexpected #5962 state") end'
  - id: "dod-omn15655-carried-in-omninode-infra-pr-802"
    description: >-
      Second-consumer binding: OmniNode-ai/omninode_infra#802 pins onex-dev images whose staged omnimarket
      sibling HEAD is 488e4e05d3d9f5cb577ba3a9fef862076e07a91a — the squash merge commit of omnimarket#2010,
      this ticket's boot fix. #802 is the vehicle that delivers OMN-15655 to the cluster, so its eligibility
      check binds this ticket.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2010 --jq 'if (.merged == true and .merge_commit_sha == "488e4e05d3d9f5cb577ba3a9fef862076e07a91a") then "true" else error("omnimarket#2010 is not merged at 488e4e05") end'
  - id: "occ-self-bind-pr-5984"
    description: "Binds OMN-15655 to OCC companion PR #5984 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5984 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-5978"
    description: "Binds OMN-15655 to OCC companion PR #5978 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5978 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2632"
    description: >-
      Consumer binding for OmniNode-ai/omnibase_infra#2632, which declares the omniintelligence service
      database so strict auto-wiring resolves node_dispatch_outcome_bridge_effect on the effects pod.
      The merged receipt drift/dod_receipts/OMN-15655/dod-OmniNode-ai-omnibase_infra-pr-2632/command.yaml
      has existed since 2026-08-02T17:14Z but had no contract entry, and validator_occ_merge_eligibility
      enumerates receipts BY CONTRACT ENTRY (_iter_dod_evidence), so it was invisible to the gate and
      #2632 failed occ-preflight with reason=pr_ticket_mismatch on every run. This entry is a pure append
      that makes the existing receipt visible; it introduces no new bar.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/topology/application_database.py?ref=f26acc6fd242eba40b450c8fba9f4111aba00b67 --jq '.content' | base64 -d | grep -c 'def validate_omniintelligence_database_invariants'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2633"
    description: >-
      Consumer binding for OmniNode-ai/omnibase_infra#2633, which vendors the nine house-tenant node migrations
      and clears the OMN-15423 application migration manifest blockers. The existing receipt drift/dod_receipts/OMN-15655/dod-OmniNode-ai-omnibase_infra-pr-2633/command.yaml
      was minted before this contract append and therefore carries a stale legacy whole-file contract
      hash; this entry makes the receipt key visible to OCC eligibility, and the sibling supersession
      binds the same actual source probe with contract_entry_sha256.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/unit/scripts/validation/test_application_migration_manifest.py?ref=acd4af2e15299695a372cce259402313676f32f0 --jq '.content' | base64 -d | grep -c 'def test_completion_gate_is_green_after_domain_classification_is_complete'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2634"
    description: >-
      Consumer binding for OmniNode-ai/omnibase_infra#2634, which derives the tenant_projection_writer
      TABLE grants for the nine house-tenant relations and fixes the catalog writer's YAML dumper. Same
      orphan class as the #2632 entry above: the merged receipt drift/dod_receipts/OMN-15655/dod-OmniNode-ai-omnibase_infra-pr-2634/command.yaml
      has existed since 2026-08-02T18:43Z with no contract entry, so the eligibility gate never enumerated
      it. Pure append; no new bar.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/topology/application_database.py?ref=6ea5342d31c87cb82cded4a37eaffeee0c278857 --jq '.content' | base64 -d | grep -c 'class ProjectionDumper'
  - id: "dod-OmniNode-ai-omnimarket-pr-2011"
    description: >-
      Consumer binding for OmniNode-ai/omnimarket#2011, which completes the house-tenant classification
      for the nine residual OMN-15655 relations after omnibase_infra#2633 landed the required vendored
      node migrations. The exact-head source probe proves the PR carries the house-tenant identity ratchet
      at f7dd6c1180cd1727aeb6b19b6bde0c9696561cfc.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/tests/unit/projection/test_house_tenant_identity.py?ref=f7dd6c1180cd1727aeb6b19b6bde0c9696561cfc --jq '.content' | base64 -d | grep -c 'def test_the_house_tenant_uuid_is_derived_not_typed'
  - id: "dod-occ-evidence-admissibility-validator"
    description: >-
      OCC-side admissibility bar for this ticket's evidence: the onex_change_control evidence-admissibility
      suite passes at the state that carries these receipts. The merged receipt drift/dod_receipts/OMN-15655/dod-occ-evidence-admissibility-validator/command.yaml
      (2026-08-02T18:43Z) and its command.supersede.2632.yaml rebind both predate any contract entry naming
      this item, so the gate could not enumerate either. Pure append; no new bar.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          uv run pytest tests/test_evidence_admissibility.py -q
  - id: "occ-self-bind-pr-5996"
    description: "Binds OMN-15655 to OCC companion PR #5996 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5996 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-5979"
    description: "Binds OMN-15655 to OCC companion PR #5979 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5979 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-6008"
    description: "Binds OMN-15655 to OCC companion PR #6008 (self-bind)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 6008 --repo OmniNode-ai/onex_change_control --json number,state,headRefName --jq 'if (.number == 6008 and .state == "OPEN" and .headRefName == "codex/omn-15655-2011-occ") then "true" else error("unexpected #6008 state") end'
  - id: "dod-OmniNode-ai-omninode_infra-pr-811"
    description: >-
      Consumer binding for OmniNode-ai/omninode_infra#811, which repins the onex-dev runtime and migration
      image manifests to the first image pair built from omnibase_infra#2648 after its CVE-floor repair
      merged to dev. The evidence-bound PR only touches the runtime Kustomize image digest, the migration
      init-container image pin, and the staging composition readiness document that records the paired
      source revision and digest bundle.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 811 --repo OmniNode-ai/omninode_infra --json number,state,baseRefName,headRefOid,files --jq 'if (.number == 811 and .state == "OPEN" and .baseRefName == "dev" and .headRefOid == "1bb34b12fca5bda8186bf0d48e3a218a43e8bde6" and ([.files[].path] | sort) == ["k8s/migrations/omnibase-infra-migrate.yaml","k8s/onex-dev/readiness/staging-composition.canonical-dev.v1.yaml","k8s/onex-dev/runtime/kustomization.yaml"]) then "true" else error("unexpected #811 state, head, base, or file set") end'
  - id: "occ-self-bind-pr-6048"
    description: "Binds OMN-15655 to OCC companion PR #6048 (self-bind for omninode_infra#811)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 6048 --repo OmniNode-ai/onex_change_control --json number,state,headRefName --jq 'if (.number == 6048 and .state == "OPEN" and .headRefName == "codex/omn-15655-occ-companion-811") then "true" else error("unexpected #6048 state") end'
  - id: "dod-OmniNode-ai-omninode_infra-pr-811-repin"
    description: >-
      Append-only repair of dod-OmniNode-ai-omninode_infra-pr-811: that entry's own check_value pins state
      == "OPEN" and headRefOid == "1bb34b12fca5bda8186bf0d48e3a218a43e8bde6", both stale now that #811
      MERGED (mergeCommit eb2861a677184ae70125ad37a3a40ca265621477, confirmed live via `gh pr view 811
      --repo OmniNode-ai/omninode_infra`) with a different head (a "Merge branch 'dev' into ..." commit
      landed after OCC#6048 merged advanced headRefOid to cf9f737c03ffb3e9940c17edc8865c9f0f061655). Re-running
      the original entry's committed jq verbatim against live state now exits 1 ("unexpected #811 state,
      head, base, or file set"), so it fails permanently and cannot be satisfied by any future run. This
      repair entry also binds the load-bearing build facts the original entry omitted (source SHA, runtime
      + migrate image digests) directly against the merged readiness composition document, so a later
      digest edit is asserted, not merely narrated in PR prose.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-811"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 811 --repo OmniNode-ai/omninode_infra --json number,state,baseRefName,mergeCommit,files --jq 'if (.number == 811 and .state == "MERGED" and .baseRefName == "dev" and .mergeCommit.oid == "eb2861a677184ae70125ad37a3a40ca265621477" and ([.files[].path] | sort) == ["k8s/migrations/omnibase-infra-migrate.yaml","k8s/onex-dev/readiness/staging-composition.canonical-dev.v1.yaml","k8s/onex-dev/runtime/kustomization.yaml"]) then "true" else error("unexpected #811 state, head, base, or file set") end'
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omninode_infra/contents/k8s/onex-dev/readiness/staging-composition.canonical-dev.v1.yaml?ref=cf9f737c03ffb3e9940c17edc8865c9f0f061655" --jq .content | base64 -d | python3 -c 'import sys,yaml; doc=yaml.safe_load(sys.stdin.read()); assert doc["source_rev"]=="2e5ef7da5d08df9f1bcbe7eff58eed696c14d1e4"; assert doc["runtime"]["image"]["digest"]=="sha256:5507667152e2dc420fc4dd6bd337f8dbe2ecf592939572dc8f353efb10a05dd6"; assert doc["migrations"]["image"]["digest"]=="sha256:b1e2a561cc48aac49b36b6746f6d2df0afedb4bfa43aacb20f340e1eb4d2f0a2"; print("true")'
  - id: "occ-self-bind-pr-6048-repin"
    description: >-
      Append-only repair of occ-self-bind-pr-6048: that entry's own check_value pins state == "OPEN",
      stale now that OCC#6048 MERGED (mergeCommit b25ab6e8dc8c03c0b7ef3b62ea250ecd2dda4607, confirmed
      live via `gh pr view 6048 --repo OmniNode-ai/onex_change_control`). Re-running the original entry's
      committed jq verbatim against live state now exits 1.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-6048"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 6048 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,mergeCommit --jq 'if (.number == 6048 and .state == "MERGED" and .headRefName == "codex/omn-15655-occ-companion-811" and .mergeCommit.oid == "b25ab6e8dc8c03c0b7ef3b62ea250ecd2dda4607") then "true" else error("unexpected #6048 state") end'
