---
schema_version: "1.0.0"
ticket_id: "OMN-18391"
title: "Autobind OCC evidence for OMN-18391"
summary: "OCC Evidence-Source autobind companions (OMN-13317 F1) for PR #3574 and PR #1479."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3574 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3574 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/unit/scripts/test_seed_keycloak_clients_consumer_secret_fingerprint_guard.py -q
  - kind: "ci"
    description: "PR #1479 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #1479 on OmniNode-ai/omninode_infra changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (k8s/onex-dev/jobs/seed-keycloak-clients-job.yaml, k8s/onex-dev/secret-ownership-manifest.yaml) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3574"
    description: "PR #3574 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/seed-keycloak-clients.py?ref=076f5c3000339d9db6065cc350f78f2c4993305a --jq '.content' | base64 -d | grep -c 'def _secret_fingerprint'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3574-ci"
    description: "PR #3574 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3574 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3574 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/scripts/test_seed_keycloak_clients_consumer_secret_fingerprint_guard.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-9639"
    description: "OCC companion PR #9639 — self-bind for OMN-18391 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9639 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1479"
    description: "PR #1479 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1479 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1479-ci"
    description: "PR #1479 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1479 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1479"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-9641"
    description: "OCC companion PR #9641 — self-bind for OMN-18391 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9641 --repo OmniNode-ai/onex_change_control --json number,state"
