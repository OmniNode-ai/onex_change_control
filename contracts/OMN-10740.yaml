---
schema_version: "1.0.0"
ticket_id: OMN-10740
title: "Fix 2 silent fallback violations in omninode_infra (OMN-10658 follow-up)"
summary: "Annotate 2 kubectl port-forward dev convenience fallbacks with # fallback-ok in tools/bootstrap-cloud-dev.sh
  and scripts/seed-infisical-dev.sh. Add no-silent-fallbacks validator script + CI gate + pre-commit hook
  to prevent regressions."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Both localhost fallbacks annotated with # fallback-ok or fixed"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10740/dod-fallbacks-annotated/command.yaml"
  - kind: "manual"
    description: "Validator exits 0 on repo after annotations"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10740/dod-validator-clean/command.yaml"
  - kind: "manual"
    description: "OCC PR open with contract and receipts"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10740/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-fallbacks-annotated"
    description: "Both INFISICAL_URL localhost fallbacks annotated with # fallback-ok in omninode_infra"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10740/dod-fallbacks-annotated/command.yaml
          && grep -q '^ticket_id: OMN-10740$' drift/dod_receipts/OMN-10740/dod-fallbacks-annotated/command.yaml
          && grep -q '^evidence_item_id: dod-fallbacks-annotated$' drift/dod_receipts/OMN-10740/dod-fallbacks-annotated/command.yaml"
  - id: "dod-validator-clean"
    description: "check-no-silent-fallbacks.sh exits 0 on the annotated repo"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10740/dod-validator-clean/command.yaml
          && grep -q '^ticket_id: OMN-10740$' drift/dod_receipts/OMN-10740/dod-validator-clean/command.yaml
          && grep -q '^evidence_item_id: dod-validator-clean$' drift/dod_receipts/OMN-10740/dod-validator-clean/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carrying the OMN-10740 contract and receipts"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10740/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: OMN-10740$' drift/dod_receipts/OMN-10740/dod-occ-pr/command.yaml && grep
          -q '^evidence_item_id: dod-occ-pr$' drift/dod_receipts/OMN-10740/dod-occ-pr/command.yaml"
