---
schema_version: "1.0.0"
ticket_id: "OMN-12893"
title: "SEA judge provider overlays and cloud fallback controls"
summary: >-
  Central evidence for onex-self-extending-agent PR #226. The PR adds typed openai_compat provider support
  for judge BYO OpenAI-compatible overlays, documents the OpenRouter secret ref as OPEN_ROUTER_API_KEY,
  and adds an explicit native entropy --include-cloud-fallback flag while preserving local-only behavior
  by default.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-226-source-checks"
    description: >-
      onex-self-extending-agent PR #226 carries W1B provider overlay and cloud fallback controls and passed
      focused tests, full unit tests, lint/type checks, and commit-time SEA unit/integration gates.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-sea-pr-226-source-checks/command.yaml"
          && grep -q '^pr_number: 226$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-sea-pr-226-source-checks/command.yaml"
          && grep -q '1270 passed, 1 skipped' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-sea-pr-226-source-checks/command.yaml"
          && grep -q 'd081eff3c808c5e42c2e2722f0d7cdcc5ddb48e8' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-sea-pr-226-source-checks/command.yaml"
  - id: "dod-openai-compat-overlay-smoke"
    description: >-
      A local registry overlay can add an OpenRouter-style OpenAI-compatible endpoint with provider openai_compat,
      OPEN_ROUTER_API_KEY, and a complete /v1/chat/completions endpoint without editing source-controlled
      contracts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-openai-compat-overlay-smoke/command.yaml"
          && grep -q 'provider: openai_compat' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-openai-compat-overlay-smoke/command.yaml"
          && grep -q 'api_key_env: OPEN_ROUTER_API_KEY' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-openai-compat-overlay-smoke/command.yaml"
          && grep -q 'https://openrouter.ai/api/v1/chat/completions' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-openai-compat-overlay-smoke/command.yaml"
  - id: "dod-no-deploy-assessment"
    description: >-
      PR #226 changes source, tests, and documentation only. It does not deploy, restart, mutate .201,
      rebuild images, or change production runtime state.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-no-deploy-assessment/command.yaml"
          && grep -q 'No live .201 deployment' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-no-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2417-self"
    description: >-
      onex_change_control PR #2417 carries the OMN-12893 central contract and receipt evidence for product
      PR #226.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-occ-pr-2417-self/command.yaml"
          && grep -q '^pr_number: 2417$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-occ-pr-2417-self/command.yaml"
          && grep -q 'fee3d193ac9dc59e306e0a7557d87995e3a2ac41' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12893/dod-occ-pr-2417-self/command.yaml"
