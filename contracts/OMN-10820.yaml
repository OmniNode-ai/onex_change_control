---
schema_version: "1.0.0"
ticket_id: "OMN-10820"
title: "Add handler Any signature gate"
summary: >
  Add an omnibase_infra AST validation gate that rejects Any annotations in handler handle() and __init__()
  signatures, with pre-commit and CI wiring to prevent future handler signature regressions.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "tests"
    description: "Validator unit tests and full source scan pass at the recorded PR head."
    command: "uv run pytest tests/unit/validators/test_handler_any_signature.py -q"
  - kind: "ci"
    description: "omnibase_infra PR #1701 CI gates run against the recorded head SHA."
    command: "gh pr checks 1701 --repo OmniNode-ai/omnibase_infra"
  - kind: "manual"
    description: "omnibase_infra PR #1701 is open against main at the recorded head SHA."
    command: "gh pr view 1701 --repo OmniNode-ai/omnibase_infra --json number,url,headRefOid,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-1701"
    description: "omnibase_infra PR #1701 carries the handler Any signature gate at the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10820/dod-product-pr-1701/command.yaml"
          && grep -q '^pr_number: 1701$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10820/dod-product-pr-1701/command.yaml"
          && grep -q '^commit_sha: "4703b449113983823f435e1145ab6f9903e3d8ef"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10820/dod-product-pr-1701/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-10820 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10820/dod-occ-self/command.yaml"
          && grep -q '^ticket_id: "OMN-10820"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10820/dod-occ-self/command.yaml"
