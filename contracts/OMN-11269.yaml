---
schema_version: 1.0.0
ticket_id: OMN-11269
title: Unify ModelSessionContract and ModelOvernightContract in omnibase_core
summary: >
  omnibase_core PR #1116 adds TestOMN11269AcceptanceCriteria proving that ModelSessionPhaseSpec carries
  required_outcomes (EnumCompletionOutcome) and per-phase halt_conditions, and that overnight model modules
  do not exist in omnibase_core.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: PR  #1116 adds acceptance criteria tests verifying the unified session contract.
    command: env PYTHONPATH=src uv run pytest tests/unit/models/overseer/test_session_contract_unification.py
      -q
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-session-contract-unification-tests
    description: Acceptance criteria tests pass — ModelSessionPhaseSpec has typed required_outcomes and
      halt_conditions; overnight modules not importable from omnibase_core.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11269/dod-session-contract-unification-tests/command.yaml"
          && grep -q '^pr_number: 1116$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11269/dod-session-contract-unification-tests/command.yaml"
          && grep -q '^verifier: agent$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11269/dod-session-contract-unification-tests/command.yaml"
