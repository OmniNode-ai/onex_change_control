---
schema_version: "1.0.0"
ticket_id: "OMN-10837"
title: "Route plugin hooks through node_emit_daemon"
summary: >
  Replace the legacy embedded_publisher EventBusKafka path in _emit_tool_content() with synchronous emission
  via node_emit_daemon Unix socket client (emit_client_wrapper). Adds "tool.content" to SUPPORTED_EVENT_TYPES,
  EVENT_REGISTRY, and the YAML fan-out registry. Daemon provides circuit-breaker + exponential backoff
  + disk-spool durability.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
  - "topics"
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "tests"
    description: "Hooks system tests pass with daemon path replacing legacy Kafka path."
    command: "uv run pytest tests/hooks/test_cli_emit.py tests/hooks/test_emit_client_wrapper.py -q"
  - kind: "ci"
    description: "omniclaude PR #1630 CI green at recorded head SHA."
    command: "gh pr checks 1630 --repo OmniNode-ai/omniclaude"
  - kind: "manual"
    description: "omniclaude PR #1630 is open against main at the recorded head SHA."
    command: "gh pr view 1630 --repo OmniNode-ai/omniclaude --json number,url,headRefOid,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1630-open"
    description: "omniclaude PR #1630 carries the daemon routing branch at the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10837/dod-pr-1630-open/command.yaml"
          && grep -q '^pr_number: 1630$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10837/dod-pr-1630-open/command.yaml"
          && grep -q '^commit_sha: "fbe07a00246e74e57c9d85b54e0fc58a90e31fab"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10837/dod-pr-1630-open/command.yaml"
  - id: "dod-focused-tests"
    description: "Hooks system tests (113 passed, 1 skipped) at the recorded head SHA."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10837/dod-focused-tests/command.yaml"
          && grep -q '^commit_sha: "fbe07a00246e74e57c9d85b54e0fc58a90e31fab"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10837/dod-focused-tests/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-10837 contract evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10837/dod-occ-self/command.yaml"
