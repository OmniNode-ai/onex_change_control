---
schema_version: 1.0.0
ticket_id: OMN-10417
title: "Harden receipt-gate skip-token override to require allowlist-authenticated approval IDs"
summary: >
  Replace free-text [skip-receipt-gate: <reason>] acceptance with allowlist-authenticated approval IDs
  in receipt_gate.py. Any skip token must match an entry in onex_change_control/allowlists/skip_token_approvals.yaml
  with scope_repos, scope_pr_numbers, expires_at, and granted_by != PR author checks.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >
      6 required test cases for skip-token allowlist validation (unknown id, expired, scope-mismatch repo,
      scope-mismatch PR, self-approved, valid) plus edge cases — all 11 tests in tests/unit/validation/test_receipt_gate_skip_token_allowlist.py
      pass. Receipt at drift/dod_receipts/OMN-10417/dod-001/
    source: generated
    checks:
      - check_type: command
        check_value: >
          uv run python -c "from pathlib import Path; import yaml; p = Path('drift/dod_receipts/OMN-10417/dod-001/command.yaml');
          data = yaml.safe_load(p.read_text()); assert data['status'] == 'PASS'; assert data['commit_sha']
          == 'be9691c44efda93307f2f84e59a81068b153af41'; assert '11 passed' in data.get('probe_stdout',
          '')"
