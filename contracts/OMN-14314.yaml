---
schema_version: "1.0.0"
ticket_id: "OMN-14314"
title: "refactor(OMN-14314): canonicalize merge-sweep handlers to thin contract+handler"
summary: >-
  omnimarket PR #1700 canonicalizes the node_ci_rerun_effect, node_rebase_effect, and node_sweep_outcome_classify
  handlers to return typed outputs directly. The repair refreshes the branch onto current dev and resolves
  the merge-sweep workflow GitHub client construction through the contract-declared GITHUB_TOKEN secret
  ref.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1700-head-f53b765e"
    description: >-
      Foreground Codex verification of omnimarket PR #1700 at head f53b765eb3369e5fe80e4b6bb1971ba8971dd716
      after rebasing onto current dev and fixing the merge-sweep workflow GitHubHttpClient token construction.
      Local focused checks passed: six touched pytest files (72 passed), ruff check and ruff format --check
      on the changed script/handler/test paths, mypy on the changed script and three handlers, strict
      changed-node state coverage, changed-node golden-chain coverage, and node_runtime_sweep import-check
      over 364 entry points.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14314/dod-omnimarket-pr-1700-head-f53b765e/command.yaml
          && grep -q '^pr_number: 1700$' drift/dod_receipts/OMN-14314/dod-omnimarket-pr-1700-head-f53b765e/command.yaml
          && grep -q 'f53b765eb3369e5fe80e4b6bb1971ba8971dd716' drift/dod_receipts/OMN-14314/dod-omnimarket-pr-1700-head-f53b765e/command.yaml
  - id: "dod-deploy-assessment-head-f53b765e"
    description: >-
      Deploy-surface assessment for omnimarket PR #1700 head f53b765eb3369e5fe80e4b6bb1971ba8971dd716:
      the diff is limited to merge-sweep workflow script call-site wiring, three handler direct-return
      refactors, and focused tests. It does not mutate live deployment configuration, perform a deploy/restart,
      change topics, run migrations, or touch .201.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14314/dod-deploy-assessment-head-f53b765e/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14314/dod-deploy-assessment-head-f53b765e/command.yaml
  - id: "dod-occ-self-commit-initial"
    description: >-
      OCC companion self-binding: the initial onex_change_control commit for OMN-14314 carries this central
      contract plus the product and deploy PASS receipts for omnimarket PR #1700 head f53b765eb3369e5fe80e4b6bb1971ba8971dd716.
      The follow-up receipt binds that initial OCC evidence commit for occ-preflight eligibility before
      the OCC PR is merged.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14314/dod-occ-self-commit-initial/command.yaml
          && grep -Eq '^[[:space:]]*commit_sha: [0-9a-f]{40}$' drift/dod_receipts/OMN-14314/dod-occ-self-commit-initial/command.yaml
