---
schema_version: "1.0.0"
ticket_id: "OMN-12120"
title: "Add swarm subtask state reducer"
summary: >
  Central OCC evidence for omnimarket PR #860 and follow-up PR #865, which add node_swarm_subtask_state_reducer
  and align the post-merge test and handler routing gates for the reducer surface.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-860"
    description: "omnimarket PR #860 merged the OMN-12120 swarm subtask state reducer."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=860; REPO=OmniNode-ai/omnimarket; gh pr view \"$PR_NUMBER\" --repo \"$REPO\"
          --json number,state,mergedAt,mergeCommit,headRefName,title"
  - id: "dod-omnimarket-pr-865"
    description: "omnimarket PR #865 carries the OMN-12120 post-merge gate alignment fixes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=865; REPO=OmniNode-ai/omnimarket; gh pr view \"$PR_NUMBER\" --repo \"$REPO\"
          --json number,state,headRefOid,headRefName,title"
  - id: "dod-local-validation"
    description: "Focused bridge config test, ruff check, and imperative contract guard pass for the follow-up
      fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12120/dod-local-validation/command.yaml
          && grep -q '8 passed' drift/dod_receipts/OMN-12120/dod-local-validation/command.yaml && grep
          -q 'new 0' drift/dod_receipts/OMN-12120/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      No live deploy was performed during the merge sweep. The reducer and handler-routing contract updates
      apply on the next normal runtime deploy.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12120/dod-deploy-assessment/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-12120/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC evidence for OMN-12120."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12120/dod-occ-pr/command.yaml"
