---
schema_version: "1.0.0"
ticket_id: "OMN-12120"
title: "feat(OMN-12120): add node_swarm_subtask_state_reducer"
summary: >
  Central OCC evidence for omnimarket PR #860, which adds node_swarm_subtask_state_reducer.
  Pure reducer that materializes per-subtask lifecycle state from delegation events.
  Idempotent via run_id+subtask_id+terminal_event_id key. Aggregate counters are computed_field
  properties derived from subtasks dict, never stored directly. Subscribes to delegation-execute,
  delegation-call-completed, delegation-escalation-triggered, delegation-all-tiers-failed,
  swarm-fanout-completed. Publishes swarm-subtask-state-changed.v1 and
  swarm-subtask-projection-applied.v1 with full freshness fields. 10 unit tests passing.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-860"
    description: >
      omnimarket PR #860 adds node_swarm_subtask_state_reducer with contract.yaml, handler,
      models, and 10 unit tests. All CI checks pass. Merged to dev.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12120/dod-omnimarket-pr-860/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      No live deploy required for this change. node_swarm_subtask_state_reducer is a new reducer
      node that will be picked up by the runtime on next deploy cycle. No existing endpoints or
      topics are modified; only new publish topics are added.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy: no live deploy required; node_swarm_subtask_state_reducer is a new reducer node picked up on next runtime deploy cycle'"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carrying the central OCC evidence for OMN-12120."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12120/dod-occ-pr/command.yaml"
