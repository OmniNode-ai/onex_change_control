---
schema_version: "1.0.0"
ticket_id: "OMN-16048"
title: "Emitter envelope-enrichment parity: replicate the daemon enrichment byte-for-byte"
summary: >-
  OCC companion for OmniNode-ai/omnimarket#2077. The shadow-mode parity harness measured 65/65 clean publishes
  but only 1/62 byte-identical outputs from node_event_emit_effect versus the legacy node_emit_daemon.
  The decision is REPLICATE (the parity spec is not narrowed), so the node now reproduces the daemon's
  metadata injection, per-fan-out-rule payload transforms, and registry-driven partition-key derivation.
  Measured after: 62/62 byte-identical.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2077 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2077"
    description: >-
      PR #2077 on OmniNode-ai/omnimarket — the enrichment module that reproduces the daemon's _inject_metadata
      exists at the fix ref. Content-bound probe against the GitHub contents API, a surface outside this
      OCC PR's own diff.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_event_emit_effect/enrichment.py?ref=66fc30e5ffdec2817281141add69e58f2fbcf27f --jq '.content' | base64 -d | grep -c 'def inject_metadata'
  - id: "dod-OmniNode-ai-omnimarket-pr-2077-partition-key"
    description: >-
      PR #2077 on OmniNode-ai/omnimarket — registry-driven partition-key derivation is wired into the
      handler at the fix ref.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_event_emit_effect/handlers/handler_event_emit_effect.py?ref=66fc30e5ffdec2817281141add69e58f2fbcf27f --jq '.content' | base64 -d | grep -c 'derive_partition_key'
  - id: "dod-OmniNode-ai-omnimarket-pr-2077-ci"
    description: "PR #2077 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2077 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6499"
    description: "OCC companion PR #6499 — self-bind for OMN-16048 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6499 --repo OmniNode-ai/onex_change_control --json number,state"
