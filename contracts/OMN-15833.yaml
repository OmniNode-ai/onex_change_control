# SPDX-FileCopyrightText: 2025 OmniNode.ai Inc.
# SPDX-License-Identifier: MIT
---
schema_version: "1.0.0"
ticket_id: "OMN-15833"
title: "Autobind OCC evidence for OMN-15833"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2058.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2058 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2058"
    description: "PR #2058 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2058 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2058 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2058 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2058 --repo OmniNode-ai/omnimarket --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-2058"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6394"
    description: "Binds OMN-15833 to OCC companion PR #6394 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6394 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-6405"
    description: "Binds OMN-15833 to OCC companion PR #6405 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6405 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-deploy-assessment-falsifiable"
    description: >-
      Falsifiable live-surface deploy probe (OMN-14443 ratchet), replacing dod-deploy-assessment's non-live
      gh-diff+grep check (F-05). A first draft of this check used a live `ssh`+`docker exec` probe against
      .201's dev lane; onex_change_control's own Contract Compliance Check (OMN-14051 hermetic-command
      guard) correctly rejected it as NON-HERMETIC -- a GitHub-hosted CI runner has no route to the .201/LAN
      host, so that check could never pass in CI even though the work is real. Per the guard's own prescribed
      hermetic pattern, this fetches the merged file content from the live GitHub contents API at OmniNode-ai/omnimarket
      PR #2058's merge commit (43f83a367965e3f03c9a9de68f57510dc933278b) and asserts the symbol the fix
      introduces -- `**build_aiokafka_auth_kwargs_from_env()` wired into the AIOKafkaConsumer/AIOKafkaProducer
      construction call in node_autopilot_orchestrator/consumer.py, one of the 24 previously-bare construction
      sites this ticket closes -- is present. Falsifiability verified both directions before authoring:
      at the merge SHA the probe exits 0 (PASS); at the PR's pre-merge base SHA (b55d5403577a39ea45785adb743ac398656262bb)
      the same probe exits 1 (the symbol is absent), confirming the check is a real assertion and not
      a tautology.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          body="$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_autopilot_orchestrator/consumer.py?ref=43f83a367965e3f03c9a9de68f57510dc933278b" --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF '**build_aiokafka_auth_kwargs_from_env()'
  - id: "occ-self-bind-pr-6403"
    description: "Binds OMN-15833 to OCC deploy-gate-fix PR #6403 (self-bind, F-01 in-repo trap)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6403 --repo OmniNode-ai/onex_change_control --json number,state"
