# SPDX-FileCopyrightText: 2025 OmniNode.ai Inc.
# SPDX-License-Identifier: MIT
---
schema_version: "1.0.0"
ticket_id: "OMN-15833"
title: "Autobind OCC evidence for OMN-15833"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2058.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2058 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2058"
    description: "PR #2058 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2058 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2058 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2058 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2058 --repo OmniNode-ai/omnimarket --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-2058"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6394"
    description: "Binds OMN-15833 to OCC companion PR #6394 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6394 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-6405"
    description: "Binds OMN-15833 to OCC companion PR #6405 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6405 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-deploy-assessment-falsifiable"
    description: >-
      Falsifiable live-surface deploy probe (OMN-14443 ratchet), replacing dod-deploy-assessment's non-live
      gh-diff+grep check (F-05). Probes .201's dev lane (compose project omnibase-infra, container omnimarket-projection-api,
      port 3002): (1) asserts the container's own Docker healthcheck status is 'healthy', a live runtime
      state that fails if the dev lane is down; (2) execs into the live container and imports+calls omnibase_infra.event_bus.kafka_auth.build_aiokafka_auth_kwargs_from_env
      — the exact shared helper OMN-15833's fix routes omnimarket's 24 previously-bare AIOKafkaConsumer/Producer
      construction sites through — confirming it is present and callable in a live running dev-lane process.
      omnimarket's own consumer/projection workloads deploy to onex-dev (cloud k3s), not .201; no cluster-scoped
      kubectl/SSM access was available at authoring time to probe that surface directly, so this establishes
      the shared auth mechanism is live in the dev lane rather than re-verifying omnimarket's own deployed
      process.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          ssh jonah@192.168.86.201 "test \"\$(docker inspect --format='{{.State.Health.Status}}' omnimarket-projection-api)\" = healthy && docker exec omnimarket-projection-api python3 -c \"from omnibase_infra.event_bus.kafka_auth import build_aiokafka_auth_kwargs_from_env; build_aiokafka_auth_kwargs_from_env()\""
  - id: "occ-self-bind-pr-6403"
    description: "Binds OMN-15833 to OCC deploy-gate-fix PR #6403 (self-bind, F-01 in-repo trap)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6403 --repo OmniNode-ai/onex_change_control --json number,state"
