---
schema_version: "1.0.0"
ticket_id: "OMN-16368"
title: "wire the OMN-15792 unified topic resolver into onex-api publish/subscribe call sites"
summary: >
  Manual OCC companion for OmniNode-ai/omninode_infra PR #958 (wires the OMN-15792 unified runtime topic
  resolver into onex-api's KafkaWorkflowPublisher.publish and workflow_terminal_consumer call sites via
  a new docker/onex-api/topic_resolver.py, porting the minimal resolver contract since onex-api ships
  no omnibase_* packages). The automated occ-autobind for PR #958 minted evidence keyed to the related
  ticket OMN-15757 (mentioned in the PR body) rather than this PR's own title ticket OMN-16368 -- the
  receipt gate's ticket extraction is title-only, so that companion (OCC PR #6840, already merged) alone
  does not satisfy the gate for this ticket. This is a second, separate companion supplying the OMN-16368-keyed
  contract + receipts.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #958 CI green; zero-behavior-change proven by unit + seam tests; local ruff/mypy/pytest/pre-commit
      clean."
    command: "gh pr checks 958 --repo OmniNode-ai/omninode_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-958"
    description: >
      PR #958 on OmniNode-ai/omninode_infra, pinned head 82e1a833f4b6bebc68ec8e2d4417dfacc07308c0 -- docker/onex-api/topic_resolver.py
      at that exact commit defines resolve_physical_topic, the OMN-15792-mirroring resolver this ticket
      ports (OMN-15309 admissibility: reads CONTENT at a pinned ref in a different repo than this PR's
      own diff, not PR/issue metadata -- falsifiable, would go RED if the function were absent or renamed).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh api "repos/OmniNode-ai/omninode_infra/contents/docker/onex-api/topic_resolver.py?ref=82e1a833f4b6bebc68ec8e2d4417dfacc07308c0"
          --jq .content | base64 -d | grep 'def resolve_physical_topic'
  - id: "dod-omninode_infra-pr-958-subscribe"
    description: >
      Same pinned head -- docker/onex-api/workflow_terminal_consumer.py actually CALLS resolve_physical_topic(topic,
      tenant_slug=None) to build ALL_TERMINAL_TOPICS, proving the subscribe/dispatch call site (not just
      the publish call site) is wired through the shared resolver per the ticket's scope item 2. Reads
      content outside this PR's own diff, falsifiable.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh api "repos/OmniNode-ai/omninode_infra/contents/docker/onex-api/workflow_terminal_consumer.py?ref=82e1a833f4b6bebc68ec8e2d4417dfacc07308c0"
          --jq .content | base64 -d | grep 'resolve_physical_topic(topic, tenant_slug=None)'
  - id: "occ-self-bind-pr-6848"
    description: "Binds OMN-16368 to this OCC companion PR #6848 (self-bind)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6848 --repo OmniNode-ai/onex_change_control --json number,state"
