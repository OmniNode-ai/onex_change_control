---
schema_version: "1.0.0"
ticket_id: "OMN-11599"
title: "Migrate agent_router to node structure"
summary: >
  OCC evidence for omniclaude PR #1677. The change adds node_agent_router as a typed COMPUTE_GENERIC node
  wrapper around the legacy AgentRouter while preserving the existing hook-compatible router path.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-agent-router-unit-tests"
    description: "Agent router node unit tests pass on omniclaude PR #1677 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11599/dod-agent-router-unit-tests/command.yaml
          && grep -q '17 passed' drift/dod_receipts/OMN-11599/dod-agent-router-unit-tests/command.yaml"
  - id: "dod-occ-promotion-pr-1712"
    description: "OCC main-promotion PR #1712 carries OMN-11599 router evidence onto main."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title"
