---
schema_version: "1.0.0"
ticket_id: "OMN-11599"
title: "Migrate agent_router to node structure"
summary: >
  OCC evidence for omniclaude PR #1677 (merge commit 29fdb73d6). The change adds node_agent_router as
  a typed COMPUTE_GENERIC node wrapper around the legacy AgentRouter while preserving the existing hook-compatible
  router path. PR #1677 merged to omniclaude dev with green CI.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-agent-router-unit-tests"
    description: "Agent router node unit tests pass on omniclaude PR #1677 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11599/dod-agent-router-unit-tests/command.yaml
          && grep -q '17 passed' drift/dod_receipts/OMN-11599/dod-agent-router-unit-tests/command.yaml"
  - id: "dod-omniclaude-pr-1677"
    description: "omniclaude#1677 carries the OMN-11599 agent_router node migration and merged to dev
      at merge commit 29fdb73d6 with green CI."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11599/dod-omniclaude-pr-1677/command.yaml"
  - id: "dod-occ-pr-2132"
    description: "onex_change_control PR #2132 binds the OMN-11599 dod evidence onto the OCC governance
      ref."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11599/dod-occ-pr-2132/command.yaml
          && grep -q '^pr_number: 2132$' drift/dod_receipts/OMN-11599/dod-occ-pr-2132/command.yaml"
