---
schema_version: "1.0.0"
ticket_id: "OMN-16127"
title: "omnibase_spi public docs rewrite: 15 fabricated protocol names + 2 fictional READMEs + 16-file
  stale version badge"
summary: >
  OCC evidence companion for OmniNode-ai/omnibase_spi PR #270 -- rewrites the public docs against the
  real protocol surface (docs-only + one net-new doc-drift assertion test file, tests/unit/test_doc_protocol_imports.py).
  No src/ behavior change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_spi-pr-270"
    description: >
      PR #270 on OmniNode-ai/omnibase_spi -- docs-only diff (29 markdown files rewritten against the live
      protocol surface) plus one net-new test file, tests/unit/test_doc_protocol_imports.py, which parses
      every fenced python code block in the repo's docs for omnibase_spi imports and asserts each one
      resolves against the live package, plus asserts the SPI-vX.Y.Z version badges and protocol_*.py
      file counts match the live package. Independently re-run against the PR's exact head: all 4 tests
      in that file pass locally (test_every_documented_import_resolves, test_no_documented_import_statement_is_empty,
      test_version_badges_match_live_package_version, test_protocol_file_count_matches_api_reference_readme).
      Content-pinned check (OMN-15309 admissibility corrective, 2026-08-18): reads the actual net-new
      test file at the PR's head SHA via the GitHub contents API and asserts a real test function defined
      by this PR is present -- a PR-existence probe alone proves the PR exists, not that the test file
      it claims to add actually contains the assertion suite.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnibase_spi/contents/tests/unit/test_doc_protocol_imports.py?ref=703582add273ebfada0473a9830c192c71260d9c"
          --jq .content | base64 -d)"; printf '%s' "$body" | grep -qF 'def test_every_documented_import_resolves'
          && echo "OMN-16127-PR-270-PROBE-PASS: test_every_documented_import_resolves is present in tests/unit/test_doc_protocol_imports.py
          at the pinned head"
  - id: "occ-self-bind-pr-6653"
    description: "OCC companion PR #6653 -- self-bind for OMN-16127: every other entry in this contract
      binds to the omnibase_spi PR, not this OCC PR, so occ-preflight's merge-eligibility validator (validator_occ_merge_eligibility._receipt_bound_to_pr)
      needs this entry to find a PASS receipt bound to the OCC PR carrying it (established pattern: occ-self-bind-pr-6633)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6653 --repo OmniNode-ai/onex_change_control --json number,state"
