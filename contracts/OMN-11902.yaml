---
schema_version: "1.0.0"
ticket_id: "OMN-11902"
title: "Wave 2 build and dispatch declarative contract migration"
summary: >
  Bind central evidence for omnimarket PR #803, which migrates 25 build/dispatch nodes to declarative
  handler_routing contracts and proves the conflict-resolved rebase with local lint, conflict-resolution,
  and focused handler_model_router test evidence.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-803"
    description: "Bind OMN-11902 evidence to omnimarket PR #803."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11902/dod-omnimarket-pr/command.yaml
          && grep -q '^pr_number: 803$' drift/dod_receipts/OMN-11902/dod-omnimarket-pr/command.yaml"
  - id: "dod-conflict-resolution-local-gates"
    description: "Conflict-resolved rebase has no conflict markers and focused tests pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11902/dod-conflict-resolution-local-gates/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence for the Wave 2 build and dispatch declarative contract migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11902/dod-deploy-readiness/command.yaml
          && grep -q 'deploy readiness' drift/dod_receipts/OMN-11902/dod-deploy-readiness/command.yaml"
