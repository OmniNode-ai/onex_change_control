---
schema_version: "1.0.0"
ticket_id: "OMN-11902"
title: "Wave 2 build and dispatch declarative contract migration"
summary: >
  Central OCC evidence for omnimarket PR #803, which migrates build, dispatch, ticket-work, delegation,
  and routing nodes to canonical declarative handler routing.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-803"
    description: "Bind OMN-11902 evidence to omnimarket PR #803."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11902/dod-omnimarket-pr-803/command.yaml
          && grep -q '^pr_number: 803$' drift/dod_receipts/OMN-11902/dod-omnimarket-pr-803/command.yaml"
  - id: "dod-local-validation"
    description: "PR-local validation passed for the Wave 2 build and dispatch declarative migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11902/dod-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: declarative contract routing changes require no live runtime deploy
      before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11902/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11902/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11902 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11902/dod-occ-pr/command.yaml"
