---
schema_version: "1.0.0"
ticket_id: "OMN-11439"
title: "Client-side trend accumulator and SVG sparkline"
summary: >
  Central OCC evidence for the onex-self-extending-agent dashboard trend panel. The PR adds a tracked
  static dashboard source, copies it into the runtime dashboard directory at startup, and renders accumulated
  attempt, latency, and cost trend data after multiple runs.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-141"
    description: "onex-self-extending-agent PR #141 carries the OMN-11439 trend accumulator dashboard
      changes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11439/dod-sea-pr-141/command.yaml"
  - id: "dod-sea-unit-tests"
    description: "Focused dashboard tests, lint, mypy, and full unit suite passed on the cleaned dev-based
      branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11439/dod-sea-unit-tests/command.yaml"
  - id: "dod-occ-pr-1764"
    description: "OCC PR #1764 binds the central OMN-11439 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11439/dod-occ-pr-1764/command.yaml"
