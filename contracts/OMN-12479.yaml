---
schema_version: "1.0.0"
ticket_id: "OMN-12479"
title: "node_redeploy validates input model and emits terminal failure"
summary: >
  Harden omnimarket node_redeploy so raw dispatched envelopes are validated at
  the handler boundary and malformed input returns the contracted terminal
  ModelRedeployWorkflowResult instead of raising out of handle() and leaving
  callers blocked until timeout.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-998-local-proof"
    description: >-
      omnimarket PR #998 changes only the node_redeploy workflow runner and its
      input-boundary tests. Focused redeploy input-boundary tests and ruff
      validation pass against PR head 0fd154781daec580bf6e18600e6ca1258d47066c.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12479/dod-omnimarket-pr-998-local-proof/command.yaml"
  - id: "dod-occ-pr-1953"
    description: "onex_change_control PR #1953 publishes the OMN-12479 central contract and local proof receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12479/dod-occ-pr-1953/command.yaml && grep -q '^pr_number: 1953$' drift/dod_receipts/OMN-12479/dod-occ-pr-1953/command.yaml"
