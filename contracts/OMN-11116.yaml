---
schema_version: "1.0.0"
ticket_id: "OMN-11116"
title: "Replace file:lineno allowlist with module-path matching in cross-node reach-in test"
summary: >-
  Refactors _KNOWN_VIOLATIONS in tests/test_no_cross_node_reach_in.py to use "importer_module:from_module_path:ImportedSymbol"
  keys instead of "file.py:lineno" keys. This eliminates repeated fixup PRs (#649, #657, #661) caused
  by import line shifts. No production code changes; test-only refactor.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #673 carries the test refactor at the observed head."
    command: "gh pr view 673 --repo OmniNode-ai/omnimarket --json headRefOid,state,url"
dod_evidence:
  - id: "dod-omnimarket-pr-673"
    description: "Bind omnimarket PR #673 to OMN-11116 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11116/dod-omnimarket-pr-673/command.yaml\"
          && grep -q '^pr_number: 673$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11116/dod-omnimarket-pr-673/command.yaml\"
          && grep -q '^commit_sha: \"a5250f944f83f41b618a2d561e672f85af2dd506\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11116/dod-omnimarket-pr-673/command.yaml\""
  - id: "dod-test-stability"
    description: "All 3 cross-node reach-in tests pass; stability verified by running pytest on the refactored
      test file."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11116/dod-test-stability/command.yaml\"
          && grep -q '^commit_sha: \"a5250f944f83f41b618a2d561e672f85af2dd506\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11116/dod-test-stability/command.yaml\""
  - id: "dod-deploy"
    description: "No live deploy or runtime restart required; test-only refactor."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11116/dod-deploy/command.yaml\"
          && grep -q '^commit_sha: \"a5250f944f83f41b618a2d561e672f85af2dd506\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11116/dod-deploy/command.yaml\""
  - id: "dod-occ-self"
    description: "Self-binding receipt for OCC PR #1077 carrying OMN-11116 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11116/dod-occ-self/command.yaml\"
          && grep -q '^pr_number: 1077$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11116/dod-occ-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
