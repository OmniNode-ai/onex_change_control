---
schema_version: "1.0.0"
ticket_id: "OMN-11137"
title: "Task 1: OmniGate Contract Model"
summary: >
  Central OCC evidence for omnibase_core PR #1088, which adds the typed OmniGate configuration contract
  model, policy models, and OmniGate-specific enums needed by later receipt and loader work.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_core PR #1088 carries the OMN-11137 config model change."
    command: "gh pr view 1088 --repo OmniNode-ai/omnibase_core --json state,headRefOid"
  - kind: "tests"
    description: "Focused config model tests and lint pass locally."
    command: "PYTHONPATH=src uv run pytest tests/unit/models/gate/test_model_omnigate_config.py -q &&
      PYTHONPATH=src uv run ruff check src/omnibase_core/enums/enum_omnigate.py src/omnibase_core/models/gate
      tests/unit/models/gate/test_model_omnigate_config.py"
  - kind: "manual"
    description: "No runtime deploy is required for pure contract model changes."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11137/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1088"
    description: "Bind OMN-11137 evidence to omnibase_core PR #1088 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11137/dod-omnibase-core-pr-1088/command.yaml\"
          && grep -q '^pr_number: 1088$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11137/dod-omnibase-core-pr-1088/command.yaml\"
          && grep -q '^commit_sha: 80fc91509c85cd404323fea213fe001e79a1eae7$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11137/dod-omnibase-core-pr-1088/command.yaml\""
  - id: "dod-focused-checks"
    description: "Focused config model tests and lint pass locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11137/dod-focused-checks/command.yaml\"
          && grep -q '5 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11137/dod-focused-checks/command.yaml\"
          && grep -q 'All checks passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11137/dod-focused-checks/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "No live deploy, runtime restart, .201 mutation, or event-bus write was performed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11137/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11137/dod-deploy-assessment/command.yaml\""
