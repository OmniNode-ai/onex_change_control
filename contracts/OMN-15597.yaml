---
schema_version: "1.0.0"
ticket_id: "OMN-15597"
title: "Autobind OCC evidence for OMN-15597"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #1992.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1992 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1992"
    description: "PR #1992 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1992 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: "gh pr view 1992 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #1992 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 1992 --repo OmniNode-ai/omnimarket --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-1992"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5781"
    description: "Binds OMN-15597 to OCC companion PR #5781 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5781 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-deploy-assessment-falsifiable-head-2d7abf4d"
    description: >-
      Supersedes the generated deploy assessment with a falsifiable compute-only probe accepted by the
      OMN-14443 deploy-gate ratchet. The original dod-deploy-assessment only counted runtime-path filenames
      in the PR diff; this replacement reads the product PR head through GitHub's contents API and asserts
      that the runtime evidence collector contains the command substitution scanner introduced by OMN-15597.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: 'content="$(gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_dod_verify/services/evidence_collector.py?ref=2d7abf4dda1747eece92b8b73094e1f511119858
          --jq .content)" && CONTENT="$content" python -c ''import base64, os; body = base64.b64decode(os.environ["CONTENT"]).decode();
          raise SystemExit(0 if "def _scan_command_substitution" in body else 1)'''
  - id: "occ-self-bind-pr-5784"
    description: "Binds OMN-15597 deploy-assessment repair to OCC PR #5784 (self-bind)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5784 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid"
