---
schema_version: "1.0.0"
ticket_id: "OMN-10970"
title: "Wire skip-token rejection CI gate across all remaining repos"
summary: "Rolls out the reject-deploy-gate-skip reusable workflow as a required caller workflow across
  all OmniNode repos that did not already have it. Ensures unapproved bypass tokens are rejected org-wide
  before merge."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Per-repo caller workflow added and CI passes on each repo's PR."
    command: "gh pr list --repo OmniNode-ai/omnibase_compat --search 'OMN-10970' --state merged --json
      number --jq '.[0].number'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-merged-repos"
    description: "Skip-token rejection CI gate merged on omnibase_compat, omnibase_spi, omnidash, omnimemory,
      omninode_infra."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10970/dod-merged-repos/command.yaml"
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carries the OMN-10970 contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10970/dod-occ-pr-self/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10970/dod-occ-pr-self/command.yaml"
