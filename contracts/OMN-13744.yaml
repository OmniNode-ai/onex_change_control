---
schema_version: "1.0.0"
ticket_id: "OMN-13744"
title: "test(OMN-13744): raise branch coverage of evaluator_invariant.py"
summary: >-
  Add focused unit coverage for node_invariant_evaluate_compute evaluator branches, including import path
  allowlist handling, safe regex evaluation, regex-safety rejection paths, latency and threshold invariants,
  field-value regex checks, schema validation, and schema-cache reuse. This is a pure test-only omnibase_infra
  change; no runtime lane, deployed service, data store, event-bus topic, or production configuration
  is modified.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused evaluator invariant branch tests pass on omnibase_infra PR #2154."
    command: >-
      grep -q '^status: PASS$' drift/dod_receipts/OMN-13744/dod-focused-tests/command.yaml && grep -q
      '^status: PASS$' drift/dod_receipts/OMN-13744/dod-no-runtime-mutation/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-13744/dod-occ-self/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: >-
      Focused unit tests prove evaluator_invariant branch behavior for import allowlists, regex safety,
      latency and threshold invariants, field-value regex checks, schema validation, and cache reuse.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13744/dod-focused-tests/command.yaml"
  - id: "dod-no-runtime-mutation"
    description: >-
      OMN-13744 is a test-only coverage PR with no live deploy, restart, .201 mutation, production data
      mutation, new REST endpoint, event-bus topic change, or runtime lane mutation.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13744/dod-no-runtime-mutation/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13744 contract and receipts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13744/dod-occ-self/command.yaml"
