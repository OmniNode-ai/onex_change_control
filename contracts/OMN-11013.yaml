---
schema_version: "1.0.0"
ticket_id: "OMN-11013"
title: "Wire migration suppressions into duplication sweep D3"
summary: >
  Binds omnimarket PR #630, which threads the existing onex_change_control/migration_conflict_suppressions.yaml
  file into the duplication sweep D3 check (`_check_d3_migration_prefixes`) when present. Restores parity
  between the official sweep output and the suppressions-aware substitute command used during OMN-11001
  classification (4 actionable conflicts vs. 63 raw).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #630 carries the OMN-11013 D3 suppressions wiring."
    command: "gh pr view 630 --repo OmniNode-ai/omnimarket --json state,headRefOid"
  - kind: "tests"
    description: "Focused duplication-sweep regression tests cover suppressions-present and suppressions-absent
      argv shapes."
    command: "uv run pytest tests/test_golden_chain_duplication_sweep.py -q"
  - kind: "manual"
    description: "Central OCC receipts bind OMN-11013 to omnimarket PR #630 and the deploy/test evidence."
    command: "find drift/dod_receipts/OMN-11013 -type f -name command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-630"
    description: "omnimarket PR #630 carries the OMN-11013 duplication sweep D3 suppressions wiring."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11013/dod-omnimarket-pr-630/command.yaml
          && grep -q '^pr_number: 630$' drift/dod_receipts/OMN-11013/dod-omnimarket-pr-630/command.yaml
          && grep -q '^commit_sha: e922553a03f246509059b4ce882638e1425936aa$' drift/dod_receipts/OMN-11013/dod-omnimarket-pr-630/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused duplication-sweep D3 suppressions regression tests pass (2 new + 14 baseline)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11013/dod-focused-tests/command.yaml
          && grep -q '16 passed' drift/dod_receipts/OMN-11013/dod-focused-tests/command.yaml"
  - id: "dod-deploy"
    description: >
      Deploy DoD evidence for PR #630: handler logic change inside an offline duplication-sweep node,
      no live deploy, restart, or .201 mutation required before merge.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11013/dod-deploy/command.yaml &&
          grep -q '^pr_number: 630$' drift/dod_receipts/OMN-11013/dod-deploy/command.yaml && grep -q 'No
          live deploy' drift/dod_receipts/OMN-11013/dod-deploy/command.yaml"
  - id: "dod-occ-pr-1020"
    description: "onex_change_control PR #1020 carries the central OMN-11013 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11013/dod-occ-pr-1020/command.yaml
          && grep -q '^pr_number: 1020$' drift/dod_receipts/OMN-11013/dod-occ-pr-1020/command.yaml"
