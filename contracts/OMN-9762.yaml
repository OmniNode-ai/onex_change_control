---
schema_version: "1.0.0"
ticket_id: OMN-9762
summary: "Phase 2 Task 5 — add normalize_event_bus normalization function for legacy event_bus block stripping"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnibase_core PR #916 CI checks pass"
    command: "gh pr checks 916 --repo OmniNode-ai/omnibase_core --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "contract_normalizer.py exists in src/omnibase_core/normalization/"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9762/dod-001/file_exists.yaml"
  - id: dod-002
    description: "6 unit tests pass for normalize_event_bus (strip behavior, idempotency, non-mutation,
      preservation)"
    source: generated
    checks:
      - check_type: file_exists
        check_value: "drift/dod_receipts/OMN-9762/dod-002/file_exists.yaml"
