---
schema_version: "1.0.0"
ticket_id: "OMN-15255"
title: "Autobind OCC evidence for OMN-15255"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2500."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2500 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2500"
    description: "PR #2500 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2500-ci"
    description: "PR #2500 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5147"
    description: "OCC companion PR #5147 — self-bind for OMN-15255 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-runner-readiness-composite"
    description: >-
      Deploy-scope probe (OMN-8912 gate): the composite readiness classifier is present in the product
      tree at the pinned commit. Falsifiable against the live GitHub contents API — it reads the product
      source, not this receipt or this contract, and returns non-zero at any commit where the conjunction
      classifier is absent.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_runner_fleet_health_compute/handlers/handler_runner_fleet_health_evaluate.py?ref=4bc8caf03'
          --jq .content | base64 -d | grep -q 'def _evaluate_readiness_signals'
  - id: "occ-self-bind-pr-5159"
    description: "OCC PR #5159 — self-bind for the OMN-15255 deploy-scope probe (OMN-14650)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5159 --repo OmniNode-ai/onex_change_control --json number,state"
