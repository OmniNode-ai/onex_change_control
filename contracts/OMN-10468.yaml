---
schema_version: "1.0.0"
ticket_id: OMN-10468
title: "Codex Cost Emission Hook for hostile_reviewer"
summary: "Adds a codex_cost_wrapper.py hook that writes a JSON cost event to .onex_state/llm-cost-events/
  after each codex CLI invocation in aggregate_reviews.py, with usage_source=UNKNOWN because the codex
  review subcommand produces no token usage output."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "5 unit tests for codex cost wrapper pass (file creation, schema correctness, missing
      OMNI_HOME, auto-generated correlation_id, correct output directory)"
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-10468/omniclaude &&
      uv run pytest tests/unit/hooks/scripts/test_codex_cost_wrapper.py -v"
  - kind: tests
    description: "Full omniclaude unit suite passes (4743 tests)"
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-10468/omniclaude &&
      uv run pytest tests/unit/ -v"
  - kind: manual
    description: "CodeRabbit review passed with no unresolved threads"
    command: "gh pr view 1494 --repo OmniNode-ai/omniclaude --json reviews,comments"
  - kind: manual
    description: "Hostile reviewer passed"
    command: "gh pr checks 1494 --repo OmniNode-ai/omniclaude"
  - kind: docs
    description: "Discovery finding documented: codex review CLI has no token usage output; usage_source=UNKNOWN
      with estimation_method=codex_cli_no_usage_output. codex exec --json does expose token counts via
      turn.completed events but hostile_reviewer uses the review subcommand."
    command: null
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "5 unit tests for codex cost wrapper pass"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10468/dod-001/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10468/dod-001/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10468/dod-001/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-002
    description: "Full omniclaude unit suite passes (4743 tests)"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10468/dod-002/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10468/dod-002/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10468/dod-002/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-003
    description: "CodeRabbit review passed with no unresolved threads"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10468/dod-003/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10468/dod-003/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10468/dod-003/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-004
    description: "Discovery finding documented: codex CLI no-usage-output constraint recorded in PR body"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10468/dod-004/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10468/dod-004/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10468/dod-004/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
