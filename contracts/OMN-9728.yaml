---
schema_version: "1.0.0"
ticket_id: "OMN-9728"
summary: "Fix deploy-agent runtime health verification to probe canonical runtime endpoints"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused regression test proves deploy-agent verify probes 8085/8086 and rejects degraded
      config prefetch"
    command: "cd ../omnibase_infra/scripts/deploy-agent && uv run --extra dev pytest tests/unit/test_executor_runtime_health_verify.py
      -q"
  - kind: "tests"
    description: "Deploy-agent unit suite remains green"
    command: "cd ../omnibase_infra/scripts/deploy-agent && uv run --extra dev pytest tests/unit -q"
  - kind: "ci"
    description: "Runtime health verifier files pass ruff"
    command: "cd ../omnibase_infra/scripts/deploy-agent && uv run ruff check deploy_agent/executor.py
      tests/unit/test_executor_runtime_health_verify.py"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused deploy-agent runtime health verification regression test passed"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-9728/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "Deploy-agent unit suite passed after runtime health verification change"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-9728/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "Ruff check passed for touched deploy-agent verifier files"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-9728/dod-003 -type f -name '*.yaml' | grep -q ."
