---
schema_version: "1.0.0"
ticket_id: OMN-10642
title: "Track omnibase_infra python-multipart dependabot bump"
summary: "Track and receipt-gate omnibase_infra PR #1516 for python-multipart 0.0.26 -> 0.0.27."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "The dependabot PR only updates uv.lock and all non-receipt CI checks pass."
    command: "gh pr checks 1516 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "PR #1516 updates python-multipart from 0.0.26 to 0.0.27 in uv.lock only."
    source: generated
    checks:
      - check_type: command
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/pulls/1516/files --jq '.[] | select(.filename==\"uv.lock\")
          | .patch' | grep -F 'version = \"0.0.27\"'"
  - id: dod-002
    description: "All non-receipt-gate checks on omnibase_infra PR #1516 are green."
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr checks 1516 --repo OmniNode-ai/omnibase_infra --json name,state --jq 'all(.[];
          .name == \"verify / verify\" or .state == \"SUCCESS\" or .state == \"SKIPPED\")'"
