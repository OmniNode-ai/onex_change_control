---
schema_version: 1.0.0
ticket_id: OMN-11270
title: Add typed exit conditions (ModelPhaseExitCondition) to ModelSessionPhaseSpec
summary: >
  omnibase_core PR #1118 expands test coverage for ModelPhaseExitCondition and ModelSessionPhaseSpec.exit_conditions,
  adding 11 validation-rejection tests covering all 5 condition types and multi-condition phase spec assembly.
  The model implementation was delivered in PR #1108 (OMN-11226); this ticket closes the acceptance criteria
  with complete test coverage.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: PR  #1118 adds validation rejection tests for all 5 ModelPhaseExitCondition types.
    command: env PYTHONPATH=src uv run pytest tests/unit/models/overseer/test_phase_exit_condition.py
      -q
  - kind: manual
    description: onex_change_control#1181 binds the typed exit condition test receipt to central OCC evidence.
    command: gh pr checks 1181 --repo OmniNode-ai/onex_change_control
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-exit-condition-tests
    description: 20 unit tests pass — all 5 typed condition types validated, validator rejection paths
      covered, ModelSessionPhaseSpec.exit_conditions is a frozen tuple of typed conditions.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11270/dod-exit-condition-tests/command.yaml"
          && grep -q '^pr_number: 1118$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11270/dod-exit-condition-tests/command.yaml"
          && grep -q '^verifier: agent$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11270/dod-exit-condition-tests/command.yaml"
  - id: dod-onex-change-control-pr-1181
    description: onex_change_control#1181 evidence correction is bound to OMN-11270.
    source: generated
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11270/dod-onex-change-control-pr-1181/command.yaml"
          && grep -q '^ticket_id: OMN-11270$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11270/dod-onex-change-control-pr-1181/command.yaml"
          && grep -q '^pr_number: 1181$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11270/dod-onex-change-control-pr-1181/command.yaml"
