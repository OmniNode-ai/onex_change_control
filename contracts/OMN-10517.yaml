---
schema_version: "1.0.0"
ticket_id: OMN-10517
title: "test(OMN-10517): Task 6 Envelope Compatibility Proof (unit test)"
summary: "Assert CLI outbound envelope shape matches runtime auto-wiring deserialization path"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Unit test file added and all tests pass"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,additions,files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "8 unit tests pass asserting CLI envelope shape and HandlerBusAdapter compatibility"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,additions,files"
