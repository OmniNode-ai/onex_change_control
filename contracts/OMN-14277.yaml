---
schema_version: "1.0.0"
ticket_id: "OMN-14277"
title: "ci(OMN-14277): host canonical deploy-gate reusable in omnibase_core (additive Phase 1, no caller
  repoint)"
summary: >-
  omnibase_core product PR #1414 (branch jonah/omn-14277-relocate-deploy-gate-reusable-to-core, head 37a7554a7ae236910da9c087886c6fa4ca34f49c)
  relocates the canonical org-wide deploy-gate reusable workflow from omniclaude (top plugin layer) into
  omnibase_core (Phase 1: additive -- adds the four deploy-gate surfaces, repoints no existing caller,
  deletes nothing). This fixes the inverted compat -> core -> spi -> infra layering (CLAUDE.md rule #7):
  omnibase_core / omnibase_infra / omnimarket must not depend up-layer on the omniclaude plugin. The two
  data-plane files (validate_pr_deploy_required.py, checkout-occ-contracts.sh) are byte-identical to the
  omniclaude originals; the reusable/action changes are exactly the documented repository:/uses: repoint
  (omniclaude -> omnibase_core) plus the .omniclaude-deploy-gate -> .deploy-gate-support side-path rename,
  plus one CodeRabbit Major credential-leak fix (git auth extraheader redaction in run_git_bounded) landed
  at head 37a7554a and backported byte-identically to the omniclaude copy (omniclaude#1889).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1414"
    description: >-
      Independent adversarial verification of omnibase_core#1414 at head 37a7554a7ae236910da9c087886c6fa4ca34f49c
      by a verifier that did NOT implement the PR: (1) the relocation is mechanical -- validate_pr_deploy_required.py
      (stdlib + lazy pyyaml, zero omniclaude-internal deps) is byte-identical to the omniclaude original;
      the reusable/action changes are exactly the documented repository:/uses: repoint plus the .omniclaude-deploy-gate
      -> .deploy-gate-support side-path rename; the reusable job name stays `deploy-gate` so the required
      status-check context stays `deploy-gate / deploy-gate` (verified present on omnibase_core and omnibase_infra
      dev branch protection). (2) The CodeRabbit Major credential-leak (git auth extraheader captured
      in LAST_GIT_CMD and printed by emit_diagnostics) is fixed by per-argument redaction in run_git_bounded,
      byte-identical to the omniclaude#1889 backport, and the CodeRabbit thread is resolved (gate / CodeRabbit
      Thread Check green). Config-only PR: deploy-gate is deploy-neutral because no runtime path pattern
      is matched.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14277/dod-core-pr-1414/command.yaml && grep
          -q '^pr_number: 1414$' drift/dod_receipts/OMN-14277/dod-core-pr-1414/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14277 contract and
      PASS receipts consumed by omnibase_core#1414 through its Evidence-Source line. Bound to this OCC
      PR by commit_sha (two-commit deterministic self-bind; no pr_number prediction under concurrent OCC
      lanes).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14277/dod-occ-self/command.yaml
