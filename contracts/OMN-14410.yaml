---
schema_version: 1.0.0
ticket_id: OMN-14410
title: Autobind OCC evidence for OMN-14410
summary: 'omnibase_core#1433 (OMN-14410) fixes two evidence gates that regex-matched prose without structural
  context: the Receipt Gate read an OCC companion''s plain-English Evidence-Source disclaimer as a genuine
  stamp, and the Receipt Honesty Gate''s deferral-language regex fired on verbatim third-party tool output.
  This contract''s honest evidence items (appended by an independent verifier, not the implementer) assert
  against the GitHub API — the source PR''s diff, its code at a pinned head SHA, and its check-run outcomes
  — so they fail when the source work is absent or red. The nine pre-existing autobind items above are
  L0 binding probes and are retained for identity binding only; they certify nothing about correctness.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #1433 CI checks green'
    command: gh pr checks 1433 --repo OmniNode-ai/omnibase_core
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-OmniNode-ai-omnibase_core-pr-1433
    description: 'PR #1433 on OmniNode-ai/omnibase_core — Evidence-Source autobind.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: occ-self-bind-pr-3991
    description: 'OCC companion PR #3991 is itself bound as evidence for OMN-14410.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-omnibase-core-pr-1433-head-2970944a
    description: 'PR #1433 on OmniNode-ai/omnibase_core is rebound to remediated head 2970944a.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
  - id: occ-self-bind-pr-3993
    description: 'OCC follow-up PR #3993 is itself bound as evidence for OMN-14410.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: occ-self-bind-pr-3997
    description: 'OCC supersession repair PR #3997 is itself bound as evidence for OMN-14410.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-omnibase-core-pr-1433-head-76403d74
    description: 'PR #1433 on OmniNode-ai/omnibase_core is rebound to remediated head 76403d74.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
  - id: occ-self-bind-pr-4001
    description: 'OCC follow-up PR #4001 is itself bound as evidence for OMN-14410.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-omnibase-core-pr-1433-head-402298db
    description: 'PR #1433 on OmniNode-ai/omnibase_core is rebound to remediated head 402298db.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
  - id: occ-self-bind-pr-4004
    description: 'OCC follow-up PR #4004 is itself bound as evidence for OMN-14410.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-omn14410-core-1433-diff-assertion
    description: omnibase_core#1433's diff actually touches the Receipt Gate validator (src/omnibase_core/validation/validator_receipt_gate.py)
      — a diff assertion read from the GitHub API, not from this repo's receipt store. Fails (grep -c
      prints 0, exit 1) if the PR does not touch the file.
    source: manual
    checks:
      - check_type: command
        check_value: gh api repos/OmniNode-ai/omnibase_core/pulls/1433/files --jq '.[].filename' | grep
          -cx 'src/omnibase_core/validation/validator_receipt_gate.py'
  - id: dod-omn14410-core-1433-fix-substance
    description: 'The OMN-14410 fix is present in the source PR''s code at its exact head SHA 402298db:
      classify_evidence_source_stamp (the structural discriminator that replaces the prose-matching regex)
      exists in the validator. FALSIFIABLE, verified by execution: the identical probe against pre-fix
      origin/dev prints 0 and exits 1.'
    source: manual
    checks:
      - check_type: command
        check_value: gh api "repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/validation/validator_receipt_gate.py?ref=402298db7375ec2e1017354e35c402d97c8a6972"
          --jq .content | base64 -d | grep -c 'def classify_evidence_source_stamp'
  - id: dod-omn14410-core-1433-ci-green
    description: 'Zero failing check-runs on omnibase_core#1433 at head 402298db, read from the GitHub
      check-runs API with full pagination. FALSIFIABLE, verified by execution: the identical probe against
      a known-red PR (omnimarket#1724, 47 failures) prints false and exits 1. This is the check that goes
      RED if the source PR is red.'
    source: manual
    checks:
      - check_type: command
        check_value: gh api --paginate --slurp "repos/OmniNode-ai/omnibase_core/commits/402298db7375ec2e1017354e35c402d97c8a6972/check-runs?per_page=100"
          | jq -e '[.[].check_runs[] | select(.conclusion=="failure")] | length == 0'
  - id: occ-self-bind-pr-4006
    description: 'OCC follow-up PR #4006 is itself bound as evidence for OMN-14410 at head 69cd8e189c3ccbebabb19c29a8479e84dd254310.'
    source: manual
    checks:
      - check_type: command
        check_value: test "$(gh pr view 4006 --repo OmniNode-ai/onex_change_control --json headRefName,baseRefName,headRefOid
          --jq '.headRefName + " " + .baseRefName + " " + .headRefOid')" = "jonah/omn-14410-honest-occ-evidence
          dev 69cd8e189c3ccbebabb19c29a8479e84dd254310"
