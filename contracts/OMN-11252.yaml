---
schema_version: "1.0.0"
ticket_id: "OMN-11252"
title: "feat(OMN-11241): Wave 2 Task 7 — Integration test — ADK agent uses pipeline E2E"
summary: >
  E2E integration test mocking at HTTP boundary (httpx.post). Proves full path: tool call -> GenerationConsumer
  -> validation -> result. Tests retry behavior, event chain capture, and google.generativeai poison test.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "4 integration tests pass: full pipeline, retry behavior, event chain, poison test"
    command: "gh pr view 20 --repo OmniNode-ai/onex-self-extending-agent --json number,state"
  - kind: "ci"
    description: "PR #20 open with CI workflows wired"
    command: "gh pr view 20 --repo OmniNode-ai/onex-self-extending-agent --json number,state,statusCheckRollup"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-20"
    description: "onex-self-extending-agent PR #20 — integration test proving ADK agent uses pipeline
      E2E. 4 tests: full pipeline, retry, event chain, google.generativeai poison. HTTP boundary mocked."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11252/dod-pr-20/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carrying this contract and binding receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11252/dod-occ-pr/command.yaml"
