---
schema_version: "1.0.0"
ticket_id: "OMN-13297"
title: "W3: Wire runtime-profiles gate into omniintelligence and omnimemory (GAP-7)"
summary: >-
  Wires the existing ValidatorRuntimeProfiles pre-commit + CI gate into omniintelligence and omnimemory.
  Both repos author local node contracts that subscribe to onex.cmd.* topics (command-consuming) but lacked
  the runtime_profiles enforcement gate present in omnibase_core, omnibase_infra, and omnimarket. This
  adds the wrapper script, allowlist baseline-freeze for pre-existing violations, pre-commit hook, and
  CI workflow to each repo. Part of W3 (GAP-7) from the validator standardization remediation plan. Parent
  epic: OMN-9048.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-pr-718"
    description: >-
      Per-code-PR binding receipt for omniintelligence PR #718 (jonah/omn-13297-runtime-profiles-gate,
      base=dev). Adds ValidatorRuntimeProfiles pre-commit hook + CI gate + repo-local allowlist baseline
      to omniintelligence. Fail-closed proof: planting a cmd-consuming contract without runtime_profiles
      exits non-zero. This receipt binds OMN-13297 to omniintelligence PR #718 by pr_number and commit_sha
      so downstream Receipt Gate can resolve central OCC evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13297/dod-omniintelligence-pr-718/command.yaml
  - id: "dod-omnimemory-pr-370"
    description: >-
      Per-code-PR binding receipt for omnimemory PR #370 (jonah/omn-13297-runtime-profiles-gate-mem, base=dev).
      Adds ValidatorRuntimeProfiles pre-commit hook + CI gate + repo-local allowlist baseline to omnimemory.
      Same fail-closed proof as omniintelligence. This receipt binds OMN-13297 to omnimemory PR #370 by
      pr_number and commit_sha so downstream Receipt Gate can resolve central OCC evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13297/dod-omnimemory-pr-370/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13297 contract and both implementation
      PR bindings. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check can
      resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13297/dod-occ-pr-self/command.yaml
