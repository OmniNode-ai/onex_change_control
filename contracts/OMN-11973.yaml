---
schema_version: "1.0.0"
ticket_id: OMN-11973
title: "Rebuild stale omnibase_infra migrate image"
summary: "Bind omnibase_infra PR #1744 to central OCC evidence for removing stale migrate-image Dockerfile
  COPY paths."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1744 carries the Dockerfile.migrate COPY path fix and CI verification."
    command: "gh pr view 1744 --repo OmniNode-ai/omnibase_infra --json state,headRefOid,title"
  - kind: "manual"
    description: "Deploy assessment documents that the runtime-path change rebuilds the migrate image
      after merge and does not perform a live deploy from the PR."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11973/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1744"
    description: "Bind OCC evidence to omnibase_infra PR #1744 head."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11973/dod-omnibase-infra-pr-1744/command.yaml\"
          && grep -q '^pr_number: 1744$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11973/dod-omnibase-infra-pr-1744/command.yaml\"
          && grep -q '360947ba28929985fc089e3cc0579069a05edd20' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11973/dod-omnibase-infra-pr-1744/command.yaml\""
  - id: "dod-dockerfile-migrate-copy-fix"
    description: "Dockerfile.migrate no longer references nonexistent src/omnibase_infra/migrations paths."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11973/dod-dockerfile-migrate-copy-fix/command.yaml\"
          && grep -q 'Dockerfile.migrate' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11973/dod-dockerfile-migrate-copy-fix/command.yaml\"
          && grep -q 'src/omnibase_infra/migrations' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11973/dod-dockerfile-migrate-copy-fix/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Deploy readiness assessment for the migrate-image Dockerfile runtime path change."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11973/dod-deploy-assessment/command.yaml\"
          && grep -qi 'deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11973/dod-deploy-assessment/command.yaml\"
          && grep -q 'build-and-push-migrate-image' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11973/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC contract and receipts for OMN-11973."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11973/dod-occ-pr/command.yaml\""
