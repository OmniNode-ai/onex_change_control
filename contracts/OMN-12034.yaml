---
schema_version: "1.0.0"
ticket_id: "OMN-12034"
title: "ModelContextPackExtended with factor_ordering and validity scope"
summary: >
  OCC evidence for omnimarket PR #872 (A3-T2): adds ModelContextPackExtended
  frozen Pydantic model with tuple[EnumContextFactor,...] factor_ordering,
  ModelContextPackValidityScope, and provenance hash fields. Schema-only node;
  no live deployment required.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-872"
    description: "omnimarket PR #872 carries the OMN-12034 ModelContextPackExtended schema."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12034/dod-omnimarket-pr-872/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Schema-only compute node addition. No live deploy performed; node activates on
      next normal runtime image rebuild when omnimarket package is updated.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12034/dod-deploy-assessment/command.yaml && grep -qi 'deploy' drift/dod_receipts/OMN-12034/dod-deploy-assessment/command.yaml"
