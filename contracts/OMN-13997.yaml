---
schema_version: "1.0.0"
ticket_id: "OMN-13997"
title: "feat(OMN-13997): verification-receipts raw ledger projection (WS-L slice 2, the §1 fix)"
summary: >-
  Second migration slice of the event-sourced ledger substrate (epic OMN-13989). omnibase_infra PR #2217
  builds the receiving end of the verification-receipts ledger migration — the ledger the initiative exists
  to fix (the completion-evidence ledger on local disk, a doctrine §1 contradiction). Pure DB projection
  over the existing ProjectorShell + ModelProjectorContract engine — zero new node, zero bespoke class.
  Adds the append-only verification_receipt_ledger table (forward migration 089 + rollback + restamped
  schema fingerprint), the verification_receipt_projector.yaml ModelProjectorContract (consumes onex.evt.omniclaude.verification_receipt.v1,
  append mode, envelope_id idempotency, single_partition ordering), and 4 unit tests including heterogeneous-shape
  nullable survival and lossless receipt_body capture. Emit-side dual-write, golden-chain replay-equivalence,
  and cutover are explicitly deferred follow-ups. Code plus PR evidence only; the migration is applied
  by the standard forward-migration runner on the next deploy — no live-lane or prod mutation was performed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_infra-pr-2217"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2217 at head ff4622603879ac9d21b1af87e02acb467a1e9b37 carries
      forward migration 089_create_verification_receipt_ledger.sql + rollback + restamped schema fingerprint,
      the verification_receipt_projector.yaml projector contract, and tests/unit/runtime/test_verification_receipt_projector.py
      (4 passing). Projector loader/shell suites (457) unaffected; ruff, migration-sequence, writer-coupling,
      and schema-fingerprint checks green locally.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13997/dod-omnibase_infra-pr-2217/command.yaml"
  - id: "dod-deploy-plan"
    description: >-
      Runtime deploy-gate binding: PR #2217 adds a forward migration (new append-only projection table)
      and a projector contract. No live-lane deploy, restart, or .201/prod mutation was performed; the
      table is provisioned by the canonical forward-migration runner on the next deploy, gated to an explicit
      operator rollout after merge.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13997/dod-deploy-plan/command.yaml && grep -qi
          'deploy' drift/dod_receipts/OMN-13997/dod-deploy-plan/command.yaml
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt for the OMN-13997 central contract, bound to this OCC PR by the C1 commit
      sha.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13997/dod-occ-self/command.yaml"
