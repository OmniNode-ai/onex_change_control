---
schema_version: "1.0.0"
ticket_id: OMN-12710
title: "SEA sanctioned live --agent waits past freshness window for delegation inference response"
summary: "Bound SEA stability-lane delegation inference waits by the overlay freshness timeout and emit
  typed timeout evidence for missing correlated responses."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
  - "protocols"
evidence_requirements:
  - kind: "tests"
    description: "SEA stability delegation bus unit tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12710/dod-focused-tests/command.yaml"
  - kind: "ci"
    description: "Touched SEA stability bus files pass ruff"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12710/dod-ruff-check/command.yaml"
  - kind: "manual"
    description: "Live .201 bounded probe emits typed timeout evidence"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12710/dod-live-bounded-probe/command.yaml"
  - kind: "manual"
    description: "OCC PR carries central OMN-12710 evidence"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12710/dod-occ-pr-2194/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "SEA stability delegation bus unit tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12710/dod-focused-tests/command.yaml
          && grep -q '^ticket_id: OMN-12710$' drift/dod_receipts/OMN-12710/dod-focused-tests/command.yaml
          && grep -q '^evidence_item_id: dod-focused-tests$' drift/dod_receipts/OMN-12710/dod-focused-tests/command.yaml"
  - id: "dod-ruff-check"
    description: "Touched SEA stability bus files pass ruff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12710/dod-ruff-check/command.yaml
          && grep -q '^ticket_id: OMN-12710$' drift/dod_receipts/OMN-12710/dod-ruff-check/command.yaml
          && grep -q '^evidence_item_id: dod-ruff-check$' drift/dod_receipts/OMN-12710/dod-ruff-check/command.yaml"
  - id: "dod-live-bounded-probe"
    description: "Live .201 bounded probe emits typed timeout evidence"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12710/dod-live-bounded-probe/command.yaml
          && grep -q '^ticket_id: OMN-12710$' drift/dod_receipts/OMN-12710/dod-live-bounded-probe/command.yaml
          && grep -q '^evidence_item_id: dod-live-bounded-probe$' drift/dod_receipts/OMN-12710/dod-live-bounded-probe/command.yaml"
  - id: "dod-occ-pr-2194"
    description: "onex_change_control PR #2194 carries the central OMN-12710 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12710/dod-occ-pr-2194/command.yaml
          && grep -q '^pr_number: 2194$' drift/dod_receipts/OMN-12710/dod-occ-pr-2194/command.yaml"
