---
schema_version: "1.0.0"
ticket_id: "OMN-10319"
title: "Autobind OCC evidence for OMN-10319"
summary: >
  OCC contract bound by node_pr_lifecycle_fix_effect Evidence-Source autobind (OMN-13317 F1) when OmniNode-ai/omninode_infra
  PR #605 carried a product-SHA Evidence-Source or was missing an OCC contract, and failed the gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #605 product diff scope present"
    command: "gh pr diff 605 --repo OmniNode-ai/omninode_infra --name-only | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-605"
    description: "PR #605 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "RECEIPT=drift/dod_receipts/OMN-10319/dod-OmniNode-ai-omninode_infra-pr-605/command.yaml;
          grep -q 'pr_number: 605' ${RECEIPT} && grep -q 'commit_sha: \"eb99ac0fccb47d11b1bbed6bd9ced327234773db\"'
          ${RECEIPT}"
  - id: "dod-OmniNode-ai-omninode_infra-pr-605-ci"
    description: "PR #605 on OmniNode-ai/omninode_infra — product diff scope (OMN-14425 substance check;
      OMN-14650: no longer gated on source-PR CI being green)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "RECEIPT=drift/dod_receipts/OMN-10319/dod-OmniNode-ai-omninode_infra-pr-605-ci/command.yaml;
          grep -q 'docker/onex-api/tests/test_pg.py' ${RECEIPT} && grep -q 'docker/onex-api/tests/test_provision.py'
          ${RECEIPT}"
  - id: "occ-self-bind-pr-4312"
    description: "OCC companion PR #4312 on OmniNode-ai/onex_change_control — self-binding proof for OMN-10319
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4312; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
