---
schema_version: "1.0.0"
ticket_id: OMN-10464
title: "Start projection consumers from Kafka to Postgres"
summary: "Add the LLM cost projection consumer that subscribes to Kafka LLM call-completed events and
  materializes durable Postgres rows with idempotent deduplication."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Focused LLM cost projection consumer tests pass"
    command: "uv run pytest src/omnimarket/nodes/node_projection_llm_cost/tests/test_consumer_projection_llm_cost.py
      -q"
  - kind: "manual"
    description: "Touched LLM cost projection consumer files pass lint"
    command: "uv run ruff check src/omnimarket/nodes/node_projection_llm_cost/consumer.py src/omnimarket/nodes/node_projection_llm_cost/handlers/handler_projection_llm_cost.py
      src/omnimarket/nodes/node_projection_llm_cost/tests/test_consumer_projection_llm_cost.py"
  - kind: "manual"
    description: "Ticket contract validates"
    command: "uv run validate-yaml contracts/OMN-10464.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused LLM cost projection consumer tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest src/omnimarket/nodes/node_projection_llm_cost/tests/test_consumer_projection_llm_cost.py
          -q"
  - id: "dod-002"
    description: "Touched LLM cost projection consumer files pass lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_projection_llm_cost/consumer.py src/omnimarket/nodes/node_projection_llm_cost/handlers/handler_projection_llm_cost.py
          src/omnimarket/nodes/node_projection_llm_cost/tests/test_consumer_projection_llm_cost.py"
  - id: "dod-003"
    description: "Ticket contract validates"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run validate-yaml contracts/OMN-10464.yaml"
  - id: "dod-deploy"
    description: |
      Deploy posture for this runtime consumer slice. OMN-10464 adds a
      long-running Kafka-to-Postgres consumer entry point but this PR does not
      deploy, restart, or mutate the live runtime. Post-merge deploy validation
      must prove the installed runtime can import the consumer and then run an
      operator-managed smoke against the configured Kafka/Postgres environment.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\n' 'post-merge deploy smoke: docker exec ${RUNTIME_CONTAINER:-omninode-runtime}
          python -c \"from omnimarket.nodes.node_projection_llm_cost.consumer import main; print(main.__name__)\"'"
