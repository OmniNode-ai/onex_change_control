---
schema_version: "1.0.0"
ticket_id: "OMN-15410"
title: "Autobind OCC evidence for OMN-15410"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2555.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2555 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2555"
    description: "PR #2555 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2555 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} --repo ${REPO} --name-only | grep -qiE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-deploy-assessment-pr-2555"
    description: "Falsifiable deploy-gate probe for omnibase_infra PR #2555 test-root collection remediation."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: |-
          tmp="$(mktemp)"
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/services/observability/agent_actions/tests/test_hardening.py?ref=0dbedbca0ee0c3fa2c0258632021c86209028893' --jq .content > "${tmp}.b64"
          python3 -c 'import base64, pathlib, sys; sys.stdout.buffer.write(base64.b64decode(pathlib.Path(sys.argv[1]).read_bytes()))' "${tmp}.b64" > "$tmp"
          grep -qF 'kafka_bootstrap_servers="localhost:19092"' "$tmp"
          grep -qF 'OMN-15410: ``kafka_bootstrap_servers`` is a REQUIRED field' "$tmp"
          grep -qF 'assert config.health_check_host == "127.0.0.1"' "$tmp"
          rm -f "$tmp" "${tmp}.b64"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2555"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5556"
    description: "Binds OMN-15410 to OCC companion PR #5556 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5556 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-5566"
    description: "Binds OMN-15410 to OCC repair PR #5566 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5566 --repo OmniNode-ai/onex_change_control --json number,state"
