---
schema_version: "1.0.0"
ticket_id: "OMN-12727"
title: "Update omnibase_infra core pin for delegation routing models"
summary: >-
  Central evidence for omnibase_infra PR #1877. The PR updates the
  omnibase-core dependency/source pin to the dev HEAD carrying delegation
  routing model compatibility needed by the .201 delegation path, then refreshes
  the fallback compatibility matrix and runner image identity lock generated
  from pyproject.toml. This converts the runtime hotpatch dependency into a
  releasable dev-lane dependency pin.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1877"
    description: "omnibase_infra PR #1877 carries the core pin update and generated compatibility artifacts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12727/dod-infra-pr-1877/command.yaml"
          && grep -q '^pr_number: 1877$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12727/dod-infra-pr-1877/command.yaml"
  - id: "dod-generated-artifacts"
    description: "Fallback version matrix and runner image identity checks pass after the core pin change."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12727/dod-generated-artifacts/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-12727 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12727/dod-occ-pr/command.yaml"
