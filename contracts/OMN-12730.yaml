---
schema_version: "1.0.0"
ticket_id: "OMN-12730"
title: "Delegation passed quality gate but terminal completion event is not published"
summary: >-
  Central evidence for omnimarket PR #1061. The PR scopes the OCC evidence draft orchestrator
  contract to its explicit start command and removes broad subscriptions to delegation lifecycle
  topics so delegation completion remains owned by the delegation orchestrator.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "delegation workflow lifecycle"
  - "OCC evidence draft subscriptions"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1061"
    description: "omnimarket PR #1061 carries the OCC draft subscription scoping fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12730/dod-omnimarket-pr-1061/command.yaml"
          && grep -q '^pr_number: 1061$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12730/dod-omnimarket-pr-1061/command.yaml"
  - id: "dod-local-validation"
    description: "OCC subscription guard tests and useful-artifact golden-chain tests passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12730/dod-local-validation/command.yaml"
          && grep -q '10 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12730/dod-local-validation/command.yaml"
  - id: "dod-live-useful-delegation-proof"
    description: "Live .201 hotpatch produced a useful pytest artifact after OCC draft subscriptions were scoped."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12730/dod-live-useful-delegation-proof/command.yaml"
          && grep -q '05a59d83-924a-4e95-a3d2-3dd5659d15db' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12730/dod-live-useful-delegation-proof/command.yaml"
          && grep -q 'OCC draft subscriptions limited' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12730/dod-live-useful-delegation-proof/command.yaml"
          && grep -q '@pytest.mark.unit' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12730/dod-live-useful-delegation-proof/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Runtime impact is bounded to post-merge dev-lane deploy, OCC subscription verification, and repeated delegation validation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12730/dod-deploy-assessment/command.yaml"
          && grep -q 'post-merge dev-lane deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12730/dod-deploy-assessment/command.yaml"
