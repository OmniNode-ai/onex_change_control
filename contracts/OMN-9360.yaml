---
schema_version: "1.0.0"
ticket_id: OMN-9360
title: "Make ticket pipeline execution honest with a market-owned preflight slice"
summary: "Wire node_ticket_pipeline PRE_FLIGHT execution, validate skip_to, emit phase results, and block
  unwired phases as not_implemented without Linear/GitHub/runtime side effects."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #446 checks pass"
    command: "gh pr checks 446 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "Ticket pipeline golden-chain and market skill smoke tests pass"
    command: "env -u PYTHONPATH uv run pytest tests/test_golden_chain_ticket_pipeline.py tests/test_market_skill_smokes.py
      -q"
  - kind: "manual"
    description: "Touched ticket pipeline code passes lint and type checks"
    command: "uv run ruff check src/omnimarket/market_skill_baseline.py src/omnimarket/nodes/node_ticket_pipeline
      tests/test_golden_chain_ticket_pipeline.py tests/test_market_skill_smokes.py && uv run mypy src/omnimarket/nodes/node_ticket_pipeline"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PRE_FLIGHT execution is deterministic and side-effect-free in dry-run mode"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run pytest tests/test_golden_chain_ticket_pipeline.py -q"
  - id: "dod-002"
    description: "ticket_pipeline is included in the market-only skill baseline smoke"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run pytest tests/test_market_skill_smokes.py -q"
  - id: "dod-003"
    description: "CLI dry-run emits parseable JSON for PRE_FLIGHT without Linear/GitHub/runtime mutation"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run python -m omnimarket.nodes.node_ticket_pipeline OMN-9360
          --skip-to pre_flight --dry-run"
  - id: "dod-004"
    description: "Post-merge deploy/readiness smoke plan for the market-owned ticket pipeline CLI"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -m omnimarket.nodes.node_ticket_pipeline
          OMN-9360 --skip-to pre_flight --dry-run"
