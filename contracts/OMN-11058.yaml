---
schema_version: "1.0.0"
ticket_id: "OMN-11058"
title: "Triage CRITICAL command topics — runtime ownership annotations"
summary: >
  Central OCC evidence for omnimarket PR #652. Resolves 9 CRITICAL MISSING_TOPIC_EDGE findings from the
  dependency health sweep: 1 topic annotated as intentionally_external (cross-repo consumer in omnibase_infra);
  7 topics allowlisted with expiry 2026-08-15; 1 topic handled in companion omnibase_infra PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Sweep re-run returns status=clean with 0 CRITICAL findings."
    command: "See drift/dod_receipts/OMN-11058/dod-sweep-clean/command.yaml"
  - kind: "ci"
    description: "Deploy smoke — annotation-only change, no live deploy required."
    command: "See drift/dod_receipts/OMN-11058/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sweep-clean"
    description: "Test suite passes with 0 CRITICAL sweep findings after applying contract annotations
      and allowlist."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-11058/dod-sweep-clean/command.yaml && echo
          'OMN-11058 sweep-clean receipt present'"
  - id: "dod-deploy"
    description: "Deploy assessment recorded — annotation-only change, no live deploy or runtime restart
      performed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'deploy' drift/dod_receipts/OMN-11058/dod-deploy/command.yaml && echo 'OMN-11058
          deploy receipt present'"
  - id: "dod-infra-pr-1603"
    description: "omnibase_infra PR #1603 carries the consumer-restart.v1 allowlist entry (1 of 9 CRITICAL
      findings)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-11058/dod-infra-pr-1603/command.yaml && grep
          -q 'pr_number: 1603' drift/dod_receipts/OMN-11058/dod-infra-pr-1603/command.yaml"
