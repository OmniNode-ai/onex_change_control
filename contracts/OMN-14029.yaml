---
schema_version: "1.0.0"
ticket_id: "OMN-14029"
title: "Phase-A self-hosted runner scale from 48 to 56"
summary: >-
  Bind OCC evidence for omnibase_infra PR #2220, which updates the authoritative runner fleet config and
  Docker Compose runner fleet from 48 to 56 expected runners. The source PR does not deploy or restart
  runners; rollout remains gated by operator approval, a fresh RUNNER_TOKEN, and post-rollout GitHub runner-count
  validation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #2220 carries the runner scale source change."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14029/dod-infra-pr-2220/command.yaml"
  - kind: "tests"
    description: "Runner fleet config tests and lint checks passed locally."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14029/dod-local-validation/command.yaml"
  - kind: "manual"
    description: "Deploy gate is recorded: no live runner deploy/restart before merge."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14029/dod-deploy-gate/command.yaml"
dod_evidence:
  - id: "dod-infra-pr-2220"
    description: "Bind omnibase_infra PR #2220 at the observed head commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14029/dod-infra-pr-2220/command.yaml && grep
          -q '^ticket_id: OMN-14029$' drift/dod_receipts/OMN-14029/dod-infra-pr-2220/command.yaml && grep
          -q '^pr_number: 2220$' drift/dod_receipts/OMN-14029/dod-infra-pr-2220/command.yaml && grep -q
          '86bf63b5968c8580b46e29b4ed92723915991538' drift/dod_receipts/OMN-14029/dod-infra-pr-2220/command.yaml
  - id: "dod-local-validation"
    description: "Targeted runner fleet config test and ruff checks passed for PR #2220."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14029/dod-local-validation/command.yaml && grep
          -q '11 passed' drift/dod_receipts/OMN-14029/dod-local-validation/command.yaml && grep -q 'All
          checks passed' drift/dod_receipts/OMN-14029/dod-local-validation/command.yaml
  - id: "dod-deploy-gate"
    description: "No live .201 runner deploy or restart was performed before merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14029/dod-deploy-gate/command.yaml && grep -qi
          'no live deploy' drift/dod_receipts/OMN-14029/dod-deploy-gate/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
