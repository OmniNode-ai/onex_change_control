---
schema_version: "1.0.0"
ticket_id: "OMN-14151"
title: "feat(OMN-14151): fail-closed merge-queue arm gate"
summary: >-
  omnimarket PR #1668 adds a pure COMPUTE PR arm-gate node and routes the merge lifecycle orchestrator
  through that gate before any merge fanout can arm a PR. It also flips the default merge-queue posture
  to report-only with an engaged kill switch, separates stall remediation from new arming, threads tri-state
  draft and CodeRabbit facts through inventory records, and deregisters/hard-gates legacy auto-arm surfaces
  so unattended queue mutation cannot bypass the new decision point.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1668"
    description: >-
      omnimarket PR #1668 is open against dev at head 2b6a2318ecc3edbed68949020c40b0d06167680c and implements
      the fail-closed PR arm-gate, report-only/kill-switch default, legacy arm-surface deregistration,
      and the single-arm-path policy guard. Independent Codex verification reran the focused arm-gate
      unit, state-coverage, and single-arm-path policy tests: 33 passed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14151/dod-omnimarket-pr-1668/command.yaml &&
          grep -q '^pr_number: 1668$' drift/dod_receipts/OMN-14151/dod-omnimarket-pr-1668/command.yaml
          && grep -q '^commit_sha: 2b6a2318ecc3edbed68949020c40b0d06167680c$' drift/dod_receipts/OMN-14151/dod-omnimarket-pr-1668/command.yaml
  - id: "dod-occ-pr-3820"
    description: >-
      OCC self-binding: onex_change_control PR #3820 carries the net-new OMN-14151 central contract and
      PASS receipt consumed by omnimarket PR #1668 through its Evidence-Source line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14151/dod-occ-pr-3820/command.yaml && grep -q
          '^pr_number: 3820$' drift/dod_receipts/OMN-14151/dod-occ-pr-3820/command.yaml
  - id: "dod-omnimarket-pr-1668-refresh"
    description: >-
      omnimarket PR #1668 was refreshed to head e0d9bd9db2c09ef29495f8e661a13b11dfbbd54d after merging
      current dev and adding the missing pr_lifecycle_orchestrator contract inputs for the arm-gate policy
      controls. Independent Codex verification reran state coverage, market skill baseline, focused arm-gate
      tests, and file-scoped pre-commit successfully.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14151/dod-omnimarket-pr-1668-refresh/command.yaml
          && grep -q '^commit_sha: e0d9bd9db2c09ef29495f8e661a13b11dfbbd54d$' drift/dod_receipts/OMN-14151/dod-omnimarket-pr-1668-refresh/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for omnimarket PR #1668 runtime-path changes: no live deploy, restart, docker
      exec, or rpk topic produce was authorized or performed by this merge sweep. The change is a dev-branch
      source update that will reach runtime on the normal post-merge deployment cadence; the arm-gate
      defaults fail closed until an operator explicitly enables enforce mode and disengages the kill switch.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14151/dod-deploy-assessment/command.yaml &&
          grep -qi 'deploy' drift/dod_receipts/OMN-14151/dod-deploy-assessment/command.yaml
  - id: "dod-occ-pr-3821"
    description: >-
      OCC self-binding: onex_change_control PR #3821 refreshes OMN-14151 central evidence for omnimarket
      PR #1668 head e0d9bd9d and adds deploy assessment evidence required by downstream Deploy Gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14151/dod-occ-pr-3821/command.yaml && grep -q
          '^pr_number: 3821$' drift/dod_receipts/OMN-14151/dod-occ-pr-3821/command.yaml
  - id: "dod-occ-pr-3822"
    description: >-
      OCC self-binding: onex_change_control PR #3822 adds append-only supersession receipts for OMN-14151
      so prior receipts remain immutable while downstream gates resolve the active per-entry-hash-bound
      receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14151/dod-occ-pr-3822/command.yaml && grep -q
          '^pr_number: 3822$' drift/dod_receipts/OMN-14151/dod-occ-pr-3822/command.yaml
