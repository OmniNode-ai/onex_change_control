---
schema_version: "1.0.0"
ticket_id: "OMN-16557"
title: "Autobind OCC evidence for OMN-16557"
summary: >
  OCC contract for OmniNode-ai/omnibase_infra PR #2901 -- gateway-forwarder canary-probe healthcheck fix
  (swallowed empty exception on cloud-leg connect timeout, and the 25s Docker healthcheck timeout that
  was tighter than the probe's own achievable worst-case single-leg budget).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2901 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2901"
    description: "PR #2901 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/gateway_canary_probe.py?ref=3487654d376168999dcc19c5fa64cc55cb0989bf --jq '.content' | base64 -d | grep -c 'def _describe_exception'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2901 clears the deploy-gate (F-05): the raised produce_deadline_seconds/timeout
      values are live in the built image."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.gateway.yml?ref=3487654d376168999dcc19c5fa64cc55cb0989bf --jq '.content' | base64 -d | grep -c 'timeout: 45s'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7134"
    description: >-
      OCC self-binding receipt for PR #7134, the in-repo evidence PR carrying this contract. Binds OMN-16557
      to the OCC PR by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 7134 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2901-ci"
    description: "PR #2901 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2901 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "occ-self-bind-pr-7136"
    description: "OCC companion PR #7136 — self-bind for OMN-16557 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7136 --repo OmniNode-ai/onex_change_control --json number,state"
