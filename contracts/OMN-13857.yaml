---
schema_version: "1.0.0"
ticket_id: "OMN-13857"
title: "fix(OMN-13857): make run-node and dod_verify failures actionable and deterministic"
summary: >-
  OMN-13857 binds two dispatch-blocker fixes: omnibase_core PR #1374 makes `onex run-node` fail fast with
  actionable Kafka bootstrap guidance when the broker is missing or unreachable, and omnimarket PR #1585
  resolves CONTRACT_REPO_DIR deterministically for dod_verify command evidence checks.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1374"
    description: >-
      Consuming-PR binding: omnibase_core PR #1374 carries the run-node Kafka bootstrap error handling
      fix at head 24b0e21ad11c25758c86413aab43bd7255f64aaf, with unit coverage for unset, blank, and unreachable
      KAFKA_BOOTSTRAP_SERVERS paths.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13857/dod-omnibase-core-pr-1374/command.yaml"
  - id: "dod-omnimarket-pr-1585"
    description: >-
      Consuming-PR binding: omnimarket PR #1585 carries the dod_verify CONTRACT_REPO_DIR resolution fix
      at head 59186b05949b766f8aab881a9f6ac6de8b45248d, with unit coverage for explicit env, contract-path
      derivation, configured fallbacks, and fail-closed unresolvable cases.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13857/dod-omnimarket-pr-1585/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13857 contract and receipts consumed
      by omnibase_core PR #1374 and omnimarket PR #1585.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13857/dod-occ-self/command.yaml"
