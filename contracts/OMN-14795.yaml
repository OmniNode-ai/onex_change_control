---
schema_version: 1.0.0
ticket_id: OMN-14795
title: Harden 4b single-owner plugin-managed fan-out-owner trap
summary: Central OCC evidence for omnibase_infra#2346. The product PR hardens single-owner boot gate messaging
  and regression coverage for the plugin-managed fan-out-owner trap without changing runtime ownership
  behavior. Exact product head 89ab2a9467f285b51d4b1066b4caec2e035ab77e.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #2346 product head is exact for OMN-14795 evidence.'
    command: 'PRODUCT_PR_NUMBER=2346; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=89ab2a9467f285b51d4b1066b4caec2e035ab77e;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnibase_infra-pr-2346
    description: 'PR #2346 on OmniNode-ai/omnibase_infra exists and is bound to OMN-14795 at exact product
      head 89ab2a9467f285b51d4b1066b4caec2e035ab77e.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2346; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=89ab2a9467f285b51d4b1066b4caec2e035ab77e;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnibase_infra-pr-2346-diff
    description: 'PR #2346 on OmniNode-ai/omnibase_infra touches the expected implementation and regression
      test files for OMN-14795.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2346; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "src/omnibase_infra/runtime/core_runtime/single_owner.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "tests/unit/runtime/core_runtime/test_single_owner.py"'
  - id: dod-omnibase_infra-pr-2346-local-verification
    description: Product PR body records local verification evidence for OMN-14795.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2346; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "test_plugin_managed_owner_of_fanout_topic_fails_closed_omn14795"
          && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "21670
          passed, 40 skipped"'
  - id: dod-omnibase_infra-pr-2346-deploy
    description: Deploy-readiness assessment for OMN-14795. The PR changes validation logic messaging
      and unit tests only, does not deploy or restart anything, and documents live re-canary as a later
      operator-run step. No live deploy, restart, .201, Kafka, database, or production mutation was performed
      by this OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14795/dod-omnibase_infra-pr-2346-deploy/command.yaml
          && grep -qi ''no live deploy, restart, .201, Kafka, database, or production mutation'' drift/dod_receipts/OMN-14795/dod-omnibase_infra-pr-2346-deploy/command.yaml'
  - id: occ-self-bind-pr-4425
    description: 'OCC PR #4425 on OmniNode-ai/onex_change_control self-binding proof for OMN-14795 evidence.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
