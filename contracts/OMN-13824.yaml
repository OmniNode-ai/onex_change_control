---
schema_version: "1.0.0"
ticket_id: "OMN-13824"
title: "feat(omnidash): multi-tenant dashboard foundation — Keycloak tenant claims as code, OIDC tenant
  context, Postgres RLS (with OMN-1636)"
summary: >-
  Repo-side multi-tenant foundation for the dashboard (beta gate), covering the non-gated scope of OMN-13824
  plus OMN-1636 (Postgres RLS): (1) Keycloak realm/client config as source-controlled code (deploy/keycloak/
  — tenant client scope minting tenant_id/org_id claims, confidential omnidash client, operator APPLY
  PLAN; live realm NOT touched); (2) app-side OIDC tenant extraction in the Express services layer (server/auth/
  — jose JWKS verification, AsyncLocalStorage tenant context, contract-driven auth.tenant_mode with disabled
  default) threaded into PostgresProjectionReader via the app.tenant_id GUC with provable RESET-or-destroy
  on connection release; (3) db/migrations/0001_tenant_rls.sql — tenant_id columns, fail-closed tenant_isolation
  RLS policies (USING + WITH CHECK on current_setting('app.tenant_id', true)), omnidash_app read role;
  (4) the tenant-isolation invariant (tenant A cannot read tenant B rows; no context => zero rows; cross-tenant
  INSERT rejected) proven against a real Postgres by server/__tests__/tenant-rls.integration.test.ts and
  enforced as the new tenant-rls CI job (postgres:16 service) wired into CI Summary. Live pieces (realm
  update, migration run, tenant_mode flip, rest-express cutover) remain operator-gated APPLY PLANs and
  are NOT executed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnidash-pr-252"
    description: >-
      Consuming-PR binding: omnidash PR #252 (base dev) carries the Keycloak config-as-code, server/auth
      tenant extraction, tenant-scoped Postgres reader, RLS migration, isolation test suite, and the tenant-rls
      CI gate, at head cfb7a9dcf934378a1c6985a7c5216cc7bd435c2f.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13824/dod-omnidash-pr-252/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13824 contract and receipts consumed
      by omnidash PR #252.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13824/dod-occ-self/command.yaml"
