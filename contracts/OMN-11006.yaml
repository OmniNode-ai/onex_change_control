---
schema_version: "1.0.0"
ticket_id: "OMN-11006"
title: "Remove residual delegation contract path env reads"
summary: >
  Removes residual Bifrost and task-class contract path environment overrides from delegation routing
  surfaces. Runtime code now resolves canonical contract paths directly, while tests inject explicit module-level
  path constants.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-routing-cleanup"
    description: "omnimarket routing reducer no longer reads legacy delegation contract path env overrides."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11006/dod-omnimarket-routing-cleanup/command.yaml"
  - id: "dod-omniclaude-runner-cleanup"
    description: "omniclaude delegation runner no longer reads the legacy Bifrost contract path env override."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11006/dod-omniclaude-runner-cleanup/command.yaml"
  - id: "dod-infra-runtime-cleanup"
    description: "omnibase_infra runtime render path no longer reads the legacy Bifrost target path env
      override."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11006/dod-infra-runtime-cleanup/command.yaml"
  - id: "dod-local-verification"
    description: "Focused local tests and residual-string scans passed across touched repos."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11006/dod-local-verification/command.yaml"
