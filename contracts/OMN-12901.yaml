---
schema_version: "1.0.0"
ticket_id: "OMN-12901"
title: "Unblock OCC evidence PRs from baseline omnibase_infra migration conflicts"
summary: >-
  Make the OCC migration-conflict checker inventory-aware so duplicate table names are compared inside
  logical database boundaries instead of across unrelated databases, while preserving conservative conflict
  behavior for uninventoried migration paths.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-code-pr"
    description: "OCC migration-conflict checker implementation commit exists on the OMN-12901 branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12901/dod-code-pr/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused migration-conflict and column-reference tests pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12901/dod-focused-tests/command.yaml"
  - id: "dod-lint-type"
    description: "Ruff, format check, and strict mypy pass for the changed checker."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12901/dod-lint-type/command.yaml"
  - id: "dod-migration-gate"
    description: "The OCC migration-conflict gate command reports no unsuppressed migration conflicts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12901/dod-migration-gate/command.yaml"
