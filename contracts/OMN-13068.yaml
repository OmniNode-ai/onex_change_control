---
schema_version: "1.0.0"
ticket_id: "OMN-13068"
title: "fix(OMN-13068): correct volume_config_drift_sweep runtime profile"
summary: >
  Corrects a pre-existing invalid runtime profile in omnimarket PR #1203: node_volume_config_drift_sweep/contract.yaml
  used runtime_profiles: [compute], but compute is not a registered profile. The fix changes the profile
  to workers so Runtime Profiles validation can wire the node to an actual runtime process. This is a
  single-line contract metadata correction with no node implementation, Kafka topic, or container changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
dod_evidence:
  - id: "dod-omnimarket-pr"
    description: >
      omnimarket PR #1203 carries the single-line runtime profile correction for node_volume_config_drift_sweep/contract.yaml:
      compute → workers.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13068/dod-omnimarket-pr/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR #2568 carries the central OMN-13068 contract and receipts for omnimarket PR #1203."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13068/dod-occ-pr/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Deploy assessment: no live deploy is performed by this ticket. The fix is a single-line YAML correction
      (runtime_profiles: compute → workers) in node_volume_config_drift_sweep/contract.yaml. This corrects
      a pre-existing invalid profile name introduced in PR #1172 — no node implementation changes, no
      Kafka topic changes, no container changes. No deploy action is needed; the profile change only affects
      the runtime_profiles validator CI gate. The check_value carries the deploy keyword for the deploy-gate.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13068/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-13068/dod-deploy-assessment/command.yaml"
