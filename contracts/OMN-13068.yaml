---
schema_version: "1.0.0"
ticket_id: "OMN-13068"
title: "fix(OMN-13068): green 33 pre-existing test failures — projection/runner.py ValidationError gap
  + env isolation"
summary: >
  Five clusters of pre-existing omnimarket test failures fixed without touching node_generation_consumer
  src/: (A/C) widen contextlib.suppress in BaseProjectionRunner.__init__ to also catch pydantic.ValidationError
  in addition to RuntimeError — both can be raised by from_legacy_settings() when KAFKA_BOOTSTRAP_SERVERS
  is empty or invalid; (B/E) add autouse conftest fixture _isolate_unit_env that clears live broker env
  vars and redirects ONEX_STATE_DIR to tmp_path for all non-integration-dir tests, preventing replay-state
  pollution from hardcoded correlation IDs; (D) fix subprocess env in test_codex_runtime_client.py to
  exclude Kafka vars and increase timeout 10→30s.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
dod_evidence:
  - id: "dod-omnimarket-pr"
    description: "omnimarket PR carries fix with full test suite green (7263 passed, 47 skipped, 3 pre-existing
      integration-only failures)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13068/dod-omnimarket-pr/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC contract and receipt PR merged to OCC dev."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13068/dod-occ-pr/command.yaml"
