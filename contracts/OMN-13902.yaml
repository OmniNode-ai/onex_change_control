---
schema_version: "1.0.0"
ticket_id: "OMN-13902"
title: "chore(omniintelligence): refresh omninode sibling lock pins"
summary: >-
  omniintelligence PR #800 is an automated sibling version-alignment refresh. It updates only uv.lock
  so the omninode sibling pins resolve to the latest in-range PyPI releases while leaving git/tag-pinned
  sibling sources untouched.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-pr-800-lock-refresh"
    description: >-
      Consuming PR binding: omniintelligence PR #800 is open at head b39e1f554bca117428a09bf3c34f4057db4481df
      and changes only uv.lock.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13902/dod-omniintelligence-pr-800-lock-refresh/command.yaml"
  - id: "occ-self-bind-pr-4598"
    description: >-
      OCC companion binding: this onex_change_control PR carries the central OMN-13902 contract and PASS
      receipts consumed by omniintelligence PR #800.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13902/occ-self-bind-pr-4598/command.yaml"
