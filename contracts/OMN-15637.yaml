---
schema_version: "1.0.0"
ticket_id: "OMN-15637"
title: "Autobind OCC evidence for OMN-15637"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #794.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #794 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-794"
    description: "PR #794 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 794 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 794 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-794"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5880"
    description: "Binds OMN-15637 to OCC companion PR #5880 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5880 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-795"
    description: "PR #795 on OmniNode-ai/omninode_infra - Evidence-Source autobind."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 795 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 795 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-5889"
    description: "Binds OMN-15637 to OCC companion PR #5889 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5889 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omn15637-infra-pr-795-head-binding"
    description: >-
      Discriminating head binding for OMN-15637 criterion (c) — omninode_infra PR #795 (branch jonah/omn-15637-pin-corrected-tree)
      at head e5a1929b7dc11dd7da8575d224286f04b3894dbb with base dev. #795 pins the onex-dev runtime +
      migrate images from omnibase_infra d42cbe70 to 72d27efa (29 merged-but-undeployed commits, including
      the OMN-15621 strict boot-time wiring bijection restore). This item STRENGTHENS, and does not supersede,
      the dod-OmniNode-ai-omninode_infra-pr-795 item merged by OCC#5889: that item's probes are `gh pr
      view 795 --json number,state` and `--json files`, which are the OMN-14443 vacuous class — they pass
      whenever the PR merely exists, regardless of head, base, or the outcome being claimed, so nothing
      in the merged evidence actually binds OMN-15637 to the tree #795 pins. Here headRefOid and baseRefName
      are asserted together in ONE equality, so a re-pushed head, a retargeted base, or a different PR
      each yield a mismatch. This establishes BINDING, not correctness; #795 carries its own verification
      matrix (tests/k8s 288 passed / 2 skipped, pre-commit green, kustomize rendering exactly one distinct
      digest across 10 image references, and both digests dual-read from GHA push receipts 30718901467
      / 30718906707 AND an independent `aws ecr describe-images`) in its PR body. AUTHORSHIP: net-new
      and append-only, authored by session fable-drain-0801's OPUS repair lane, which did NOT implement
      #795 — satisfying the Self-companion guard (OMN-15334) and the no-self-authored-evidence rule. Zero
      deletions, no merged receipt edited, no supersession record minted.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          bind="$(gh pr view 795 --repo OmniNode-ai/omninode_infra --json headRefOid,baseRefName --jq '.headRefOid + " " + .baseRefName')" && test "$bind" = "e5a1929b7dc11dd7da8575d224286f04b3894dbb dev"
  - id: "occ-self-bind-pr-5894"
    description: >-
      Binds OMN-15637 to OCC companion PR #5894 (self-bind) — the append-only companion carrying the discriminating
      #795 head binding above. PR number and head branch are asserted together in ONE equality, so a different
      OCC PR carrying the same number-only probe does not satisfy it. This is a DISTINCT probe from dod-omn15637-infra-pr-795-head-binding
      (different repo, different fields), so the OMN-15459 distinctness property holds by construction
      rather than by convention, and it is deliberately not the OMN-14443 vacuous shape carried by the
      merged occ-self-bind-pr-5880 and occ-self-bind-pr-5889 items. #5894 was authored as a clean-branch
      replacement while OCC#5889 was hard-red on the second-companion producer regression — it mutated
      the merged receipt drift/dod_receipts/OMN-15637/dod-occ-evidence-admissibility-validator/command.yaml
      (OCC Append-Only Gate, job 91420368934, producer bug OMN-15485) and minted three command.supersede.795.yaml
      records over three DISTINCT evidence items sharing one generic replacement.check_value (Supersession
      Binding Ratchet OMN-15459, jobs 91420369014 / 91420414597, producer bug OMN-15477). #5889 was subsequently
      hand-repaired to an append-only diff and merged as dd8002664, so #5894 is no longer a replacement
      for it — it is a strictly additive upgrade of that merged evidence from existence-only probes to
      a real head binding. #5894 deletes nothing, edits no merged receipt, and mints no supersession record.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          bind="$(gh pr view 5894 --repo OmniNode-ai/onex_change_control --json number,headRefName --jq '(.number|tostring) + " " + .headRefName')" && test "$bind" = "5894 jonah/omn-15637-occ-companion-pr795"
