---
schema_version: "1.0.0"
ticket_id: "OMN-16077"
title: "Bind the omnimarket omnibase-infra 0.38.6 re-bump for the onex-dev runtime deploy"
summary: >-
  OCC companion for OmniNode-ai/omnimarket#2078. Re-bumps the omnibase-infra git rev in omnimarket [tool.uv.sources]
  + uv.lock from b6bd79c3 (0.38.4 lineage) to infra dev 94247acffdab730105408d93c761986f4591cf55 (v0.38.6)
  — the lineage the onex-dev runtime image rebuild and deploy consume — together with the test adaptations
  the pin move was designed to force (S6 seam re-score at the new pin, packaged gateway contract version
  pin 0.1.0 -> 0.1.1, contract-declared canary block in the goldens harness, topology residual ratchet
  shrink + grants-tripwire promotion). The pinned rev deliberately pre-dates omnibase_infra#2736 (revoked
  -> termination_reason rename), keeping the deployed runtime clear of that seam until the tolerant edge
  and its migration are live. The head also carries the contract-topic-graph adaptations the pin forced:
  external_producers declarations for the three gateway attach/detach/heartbeat command topics plus a
  new symmetric external_consumers baseline surface for the gateway-session event (both edges terminate
  in private omninode_infra, outside the graph corpus), and the S6 delegation pin upgraded from a source-text
  substring check to a behavioral invocation spy (CodeRabbit review finding).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2078 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2078"
    description: >-
      PR #2078 on OmniNode-ai/omnimarket carries the 0.38.6 re-bump and its tripwire adaptations.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2078 --repo OmniNode-ai/omnimarket --json number,state"
  - id: "dod-market2078-rebump-exact-head-and-scope"
    description: >-
      omnimarket PR #2078 is bound at exact reviewed head ea77ba914d8641740d960e828f22a4d45491557e with
      its complete eleven-file scope: the two dependency-pin files, the six seam/topology adaptation files,
      and the three contract-topic-graph gate files (validator external_consumers surface, baseline declarations,
      validator test).
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 2078 --repo OmniNode-ai/omnimarket --json headRefOid,files --jq '(.headRefOid == "ea77ba914d8641740d960e828f22a4d45491557e") and ([.files[].path] | sort == ["pyproject.toml", "src/omnimarket/configs/seams.v1.yaml", "src/omnimarket/validators/contract_topic_graph.py", "src/omnimarket/validators/data/contract_topic_graph_baseline.yaml", "tests/seam_goldens/harness.py", "tests/seam_goldens/slice_manifest.yaml", "tests/seam_goldens/test_manifest_registry_binding.py", "tests/seam_goldens/test_s6_contract_handler_dispatch.py", "tests/unit/runtime/test_topology_strict_wiring_omn15655.py", "tests/validators/test_contract_topic_graph.py", "uv.lock"])' | grep -qx true
  - id: "dod-pin-is-0386-head-predating-2736"
    description: >-
      The pyproject [tool.uv.sources] rev at the PR head is exactly 94247acffdab730105408d93c761986f4591cf55,
      and omnibase_infra#2736 was OPEN (unmerged) at bind time, so the pinned lineage cannot contain the
      revoked -> termination_reason rename. Point-in-time seam-guard clearance readback; the guard is
      re-verified again at image build and deploy time.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/pyproject.toml?ref=ea77ba914d8641740d960e828f22a4d45491557e --jq '.content' | base64 -d | grep -c 'rev = "94247acffdab730105408d93c761986f4591cf55"' | grep -qx 1 && gh pr view 2736 --repo OmniNode-ai/omnibase_infra --json state --jq '.state' | grep -qx OPEN
  - id: "occ-self-bind-pr-6527"
    description: "OCC companion PR #6527 — self-bind for OMN-16077 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6527 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-910"
    description: "PR #910 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 910 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-910-ci"
    description: "PR #910 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 910 --repo OmniNode-ai/omninode_infra --json files"
