---
schema_version: "1.0.0"
ticket_id: "OMN-13470"
title: "feat(OMN-13470): combine LLM judge adequacy score into the delegation quality gate"
summary: >-
  OCC evidence for omnimarket PR #1338. The change adds HandlerJudgeAdequacy as a drop-in judge handler
  using the canonical AdapterInferenceBridge, threads judge_adequacy_score into the quality-gate delta
  combine path, and emits replayable delegation-judge-verdict.v1 events. Deterministic checks remain a
  hard floor: refusals and empty answers fail before the judge score can combine. The PR also adds delegation_judge_rubrics.v1.yaml
  coverage plus focused tests for the combined pass path, deterministic refusal block, judge failure modes,
  replayable verdict hashing, and OMN-13367 projection/judge regressions.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-quality-gate-judge-combine"
    description: >-
      Product PR #1338 wires judge adequacy into the quality gate without making the judge a silent state
      authority. Real-dispatch-path tests in tests/unit/delegation/test_quality_gate_judge_combine_omn13470.py
      cover the good-but-mechanically-incomplete code answer completing through the combined score, refusal
      hard-blocking via fail_deterministic even with a maximal judge score, low judge score remaining
      below the required bar, JUDGE_LLM_CALL_FAILED / JUDGE_PARSE_FAILED fail-closed verdicts, and replayable
      verdict hashing through compute_event_hash().
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c 'test_quality_gate_judge_combine_omn13470' contracts/OMN-13470.yaml"
  - id: "dod-local-verification"
    description: >-
      Local verification reported in omnimarket #1338: uv run pytest tests/unit/delegation returned 318
      passed; OMN-13367 non-regression returned 10 passed; projection/judge slice returned 84 passed;
      uv run mypy src --strict was clean across 2461 files; ruff format/check and pre-commit on changed
      files were clean; runtime_sweep reported 321/321 OK and node metadata dependency scope passed.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '318 passed' contracts/OMN-13470.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13470.yaml consumed
      by omnimarket PR #1338 Receipt Gate via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13470\"$' contracts/OMN-13470.yaml"
