---
schema_version: "1.0.0"
ticket_id: "OMN-14272"
title: "refactor(OMN-14272): canonicalize HandlerAntipatternMatchEffect to thin contract+handler"
summary: >-
  omnimarket product PR (branch jonah/omn-14272-canonicalize-antipattern-match, head 62dbb60a13fb990c2052bbda217d3ea7d3d9c0b0)
  canonicalizes HandlerAntipatternMatchEffect (node_antipattern_match_effect) to the thin contract+handler
  shape: handle() takes a single typed ModelAntipatternMatchRequest payload; the EFFECT handler emits
  its events. Public-safe generic architecture-hygiene work under epic OMN-14242.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      Independent landing verification at product head 62dbb60a13fb990c2052bbda217d3ea7d3d9c0b0: node
      tests, mypy --strict on the handler, and the strict state-coverage gate all pass. Runner (implementer)
      is distinct from the verifier (landing verification), satisfying the receipt-honesty gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14272/dod-001/command.yaml && grep -q '62dbb60a13fb990c2052bbda217d3ea7d3d9c0b0'
          drift/dod_receipts/OMN-14272/dod-001/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Honest deploy-surface assessment: HandlerAntipatternMatchEffect has no production callers today
      outside its node contract.yaml and tests. Pure in-module signature change; no live deploy required
      or performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14272/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-14272/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14272 contract and
      PASS receipts consumed by the product PR through its Evidence-Source line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14272/dod-occ-self/command.yaml
