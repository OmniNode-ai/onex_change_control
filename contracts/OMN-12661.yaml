---
schema_version: "1.0.0"
ticket_id: "OMN-12661"
title: "WS-F: Bounded ON-vs-OFF experiment evidence bundle (demo-blocking minimum)"
summary: >-
  Central evidence for Workstream F: bounded ON-vs-OFF cost experiment producing reproducible evidence.
  Fixed task set run through both ON path (OmniNode delegation) and OFF path (direct LLM call), capturing
  token counts, cost totals, and a summary report. Evidence bundle with receipts; final proof explicitly
  classified as replay-proven or runtime-observed only. Built on Context Pack Pipeline OMN-11674 (Done)
  and Context Matrix v3 harness. Post-demo items (curriculum, online learning loop, adaptive exemplar
  promotion) explicitly deferred and do not block this ticket.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-on-off-run-evidence"
    description: "Fixed task set executed through both ON path and OFF path. Token counts and cost totals
      captured. Summary report produced. Evidence explicitly classified as replay-proven or runtime-observed
      only."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12661/dod-on-off-run-evidence/command.yaml"
  - id: "dod-cost-delta-bundle"
    description: "Reproducible ON/OFF cost-delta evidence bundle with receipts. Cost delta table showing
      token savings percentage and absolute cost difference across the fixed task set."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12661/dod-cost-delta-bundle/command.yaml"
  - id: "dod-proof-classification"
    description: "Final proof explicitly classified replay-proven or runtime-observed only. No ambiguous
      or unclassified evidence items in the bundle."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12661/dod-proof-classification/command.yaml"
  - id: "dod-occ-pr-evidence"
    description: "OCC PR carries the OMN-12661 central contract and PASS receipts."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12661/dod-occ-pr-evidence/command.yaml"
