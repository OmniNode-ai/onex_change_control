---
schema_version: "1.0.0"
ticket_id: "OMN-14104"
title: "Gate non-OCC CI fanout behind OCC preflight"
summary: >-
  Central evidence for the cross-repo CI process change that makes non-OCC repository CI run OCC eligibility
  before runner-heavy validation fans out. The change wires OCC preflight into every PR/merge-group workflow
  dependency graph for omnimarket, omnibase_core, omnibase_infra, and omniclaude, and updates the reusable
  OCC preflight workflow to no-op successfully on non-PR events so push CI remains intact.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "tests"
    description: "Workflow YAML parses, actionlint syntax validation passes, and every targeted PR/merge-group
      workflow has OCC as the prerequisite for all non-OCC jobs."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14104/dod-workflow-syntax/command.yaml"
  - kind: "manual"
    description: "Downstream branch heads are recorded for the four CI fanout-gating PR branches."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14104/dod-downstream-branch-heads/command.yaml"

  - kind: "manual"
    description: "Each downstream process-change PR has a PASS receipt bound to its PR number and branch
      head."
    command: >-
      grep -q '^status: PASS$' drift/dod_receipts/OMN-14104/dod-omnimarket-pr-1643/command.yaml && grep
      -q '^status: PASS$' drift/dod_receipts/OMN-14104/dod-omnibase-core-pr-1396/command.yaml && grep
      -q '^status: PASS$' drift/dod_receipts/OMN-14104/dod-omnibase-infra-pr-2229/command.yaml && grep
      -q '^status: PASS$' drift/dod_receipts/OMN-14104/dod-omniclaude-pr-1868/command.yaml
  - kind: "manual"
    description: "The omnibase_core PR #1402 OCC preflight wheel installer regression fix has a PASS receipt
      bound to its PR number and branch head."
    command: >-
      grep -q '^status: PASS$' drift/dod_receipts/OMN-14104/dod-omnibase-core-pr-1402/command.yaml &&
      grep -q '^pr_number: 1402$' drift/dod_receipts/OMN-14104/dod-omnibase-core-pr-1402/command.yaml
  - kind: "manual"
    description: "The OCC follow-up PR has a PASS receipt bound to PR #3674 and its branch head."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14104/dod-occ-pr-3674/command.yaml"
  - kind: "manual"
    description: "No live deploy, restart, or runtime mutation is required for this CI process change."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14104/dod-deploy-scope/command.yaml"
  - kind: "manual"
    description: "The OCC companion PR carries the OMN-14104 contract and PASS receipts."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14104/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-workflow-syntax"
    description: >-
      Local workflow proof: all modified PR/merge-group workflows parse as YAML; actionlint syntax validation
      passes with ShellCheck disabled to ignore pre-existing inline script style findings; whitespace
      checks pass; every non-OCC job depends on OCC preflight.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-workflow-syntax/command.yaml"
          && grep -q 'actionlint -shellcheck=' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-workflow-syntax/command.yaml"
  - id: "dod-downstream-branch-heads"
    description: >-
      The process-change branches are pushed in omnimarket, omnibase_core, omnibase_infra, and omniclaude
      at the recorded commit SHAs.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-downstream-branch-heads/command.yaml"
          && grep -q 'omnimarket 27d976e24c13e2060cf7b233b2ae4feda6506b04' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-downstream-branch-heads/command.yaml"
          && grep -q 'omnibase_core b072b37eacf657a3dde90a31ada25aeacfa92315' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-downstream-branch-heads/command.yaml"
          && grep -q 'omnibase_infra 68d22b2088ef7ec435391100b66c460433dce798' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-downstream-branch-heads/command.yaml"
          && grep -q 'omniclaude 767eb3c5c8cfee99801ff4132ee198119b6674b6' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-downstream-branch-heads/command.yaml"

  - id: "dod-omnimarket-pr-1643"
    description: "omnimarket PR #1643 is the OMN-14104 process-change branch pinned by this receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnimarket-pr-1643/command.yaml"
          && grep -q '^pr_number: 1643$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnimarket-pr-1643/command.yaml"
  - id: "dod-omnimarket-pr-1645"
    description: >-
      omnimarket PR #1645 is the OMN-14104 follow-up that makes the OCC preflight dependency probes duplicate-safe
      by selecting a single check-run conclusion inside jq, avoiding pipefail/head SIGPIPE exit 141 when
      stale and fresh occ-preflight check-runs both exist for a PR head SHA.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnimarket-pr-1645/command.yaml"
          && grep -q '^pr_number: 1645$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnimarket-pr-1645/command.yaml"
  - id: "dod-omnibase-core-pr-1396"
    description: "omnibase_core PR #1396 is the OMN-14104 process-change branch pinned by this receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnibase-core-pr-1396/command.yaml"
          && grep -q '^pr_number: 1396$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnibase-core-pr-1396/command.yaml"
  - id: "dod-omnibase-infra-pr-2229"
    description: "omnibase_infra PR #2229 is the OMN-14104 process-change branch pinned by this receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnibase-infra-pr-2229/command.yaml"
          && grep -q '^pr_number: 2229$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnibase-infra-pr-2229/command.yaml"
  - id: "dod-omniclaude-pr-1868"
    description: "omniclaude PR #1868 is the OMN-14104 process-change branch pinned by this receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omniclaude-pr-1868/command.yaml"
          && grep -q '^pr_number: 1868$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omniclaude-pr-1868/command.yaml"
  - id: "dod-occ-pr-3674"
    description: "onex_change_control PR #3674 carries the OMN-14104 downstream product PR binding receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-occ-pr-3674/command.yaml"
          && grep -q '^pr_number: 3674$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-occ-pr-3674/command.yaml"
  - id: "dod-occ-pr-3693-self"
    description: >-
      Follow-up OCC self-binding receipt for PR #3693, which carries the OMN-14104 omnimarket PR #1645
      duplicate-safe OCC dependency probe evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-occ-pr-3693-self/command.yaml"
          && grep -q '^pr_number: 3693$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-occ-pr-3693-self/command.yaml"
  - id: "dod-deploy-scope"
    description: >-
      CI workflow dependency ordering changes do not deploy code, restart services, mutate .201, or change
      runtime state. Post-merge validation is GitHub Actions check ordering and OCC gate behavior.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-deploy-scope/command.yaml"
          && grep -q 'No live deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-deploy-scope/command.yaml"
  - id: "dod-occ-self"
    description: "The OCC companion branch carries the OMN-14104 central contract and receipt evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-occ-self/command.yaml"
          && grep -q '^ticket_id: OMN-14104$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-occ-self/command.yaml"
  - id: "dod-omnibase-core-pr-1399"
    description: >-
      omnibase_core PR #1399 is the main-targeted OMN-14104 follow-up that forces the reusable OCC preflight
      workflow to install the freshly built local omnibase_core wheel with --no-index/--no-config, preventing
      downstream callers from validating with stale published core wheels that lack receipt supersession
      support.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnibase-core-pr-1399/command.yaml"
          && grep -q '^pr_number: 1399$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnibase-core-pr-1399/command.yaml"
  - id: "dod-occ-pr-3705"
    description: >-
      onex_change_control PR #3705 carries the OMN-14104 central evidence binding for omnibase_core PR
      #1399, the main-targeted OCC-preflight local-wheel install fix.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-occ-pr-3705/command.yaml"
          && grep -q '^pr_number: 3705$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-occ-pr-3705/command.yaml"
  - id: "dod-omnibase-core-pr-1400"
    description: >-
      omnibase_core PR #1400 replaces PR #1399 on a main-release-policy-compliant hotfix branch while
      carrying the same OMN-14104 OCC-preflight local-wheel install fix and version bump.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnibase-core-pr-1400/command.yaml"
          && grep -q '^pr_number: 1400$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnibase-core-pr-1400/command.yaml"
  - id: "dod-omnibase-core-pr-1402"
    description: >-
      omnibase_core PR #1402 is the follow-up OMN-14104 regression fix that removes uv's package-name
      reinstall path from OCC preflight, installs only the exact locally built omnibase_core wheel, bypasses
      runner uv cache for that final install, and prints the installed distribution version/import path
      before eligibility runs.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnibase-core-pr-1402/command.yaml"
          && grep -q '^pr_number: 1402$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-omnibase-core-pr-1402/command.yaml"
  - id: "dod-occ-pr-3706"
    description: >-
      onex_change_control PR #3706 carries the OMN-14104 central evidence binding for omnibase_core PR
      #1400, the hotfix-branch replacement for the main-targeted OCC-preflight local-wheel install fix.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-occ-pr-3706/command.yaml"
          && grep -q '^pr_number: 3706$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14104/dod-occ-pr-3706/command.yaml"
