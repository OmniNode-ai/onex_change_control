---
schema_version: "1.0.0"
ticket_id: "OMN-10715"
title: "Port change-aware CI test selection to omnibase_infra"
summary: >
  Adds contract-backed change-aware CI test selection for omnibase_infra, with validated source-to-test
  adjacency, conservative full-suite escalation, and focused unit coverage for the dynamic test matrix
  computation.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-test-selection-unit-suite"
    description: "Focused change-aware CI test selection unit suite passes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10715/dod-test-selection-unit-suite/command.yaml\""
  - id: "dod-pr-1537-head"
    description: "Bind OMN-10715 evidence to omnibase_infra PR #1537 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10715/dod-pr-1537-head/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to the onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10715/dod-occ-pr/command.yaml\""
