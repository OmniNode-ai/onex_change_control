---
schema_version: 1.0.0
ticket_id: OMN-14712
title: 'Evidence for OmniNode-ai/omnibase_core#1462: no-hardcoded-ip multi-root scan coverage'
summary: OCC evidence for omnibase_core#1462 validating multi-root no-hardcoded-ip scan coverage.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #1462 product diff scope present'
    command: 'PRODUCT_PR_NUMBER=1462; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER}; gh pr diff
      $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q .'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnibase_core-pr-1462
    description: 'PR #1462 on OmniNode-ai/omnibase_core exists and is bound to OMN-14712.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1462; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json number,state,headRefOid'
  - id: dod-omnibase_core-pr-1462-diff
    description: 'PR #1462 on OmniNode-ai/omnibase_core has a non-empty reviewed product diff for OMN-14712.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1462; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q .'
  - id: occ-self-bind-pr-4336
    description: 'OCC batch PR #4336 on OmniNode-ai/onex_change_control self-binding proof for OMN-14712.'
    source: generated
    checks:
      - check_type: command
        check_value: 'SELF_PR_NUMBER=4336; SELF_REPO=OmniNode-ai/onex_change_control; : ${PR_NUMBER};
          gh pr view $SELF_PR_NUMBER --repo $SELF_REPO --json number,state'
