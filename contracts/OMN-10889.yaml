---
schema_version: "1.0.0"
ticket_id: "OMN-10889"
title: "Delegation thin-shim node, adapter, and CLI entry point (Wave 1)"
summary: >
  Adds the omnimarket node_delegate_skill_orchestrator (typed request/response models, contract.yaml with
  named runtime_dispatch topic fields and cross_repo_dependencies, handler with an injected dispatch port),
  the DelegationDispatchAdapter that resolves routing from named contract fields, and the onex-delegate
  CLI entry point. New-node and adapter addition only; no existing runtime topology, migration, docker
  exec action, Redpanda topic produce, or service restart is required for this PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-impl"
    description: "omnimarket PR #612 adds node_delegate_skill_orchestrator, adapter, and CLI entry point
      with passing unit tests"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10889/dod-impl/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10889/dod-impl/command.yaml"
  - id: "dod-deploy"
    description: >
      Deploy verification: new-node and adapter addition only. No runtime topology change, migration,
      docker exec action, Redpanda topic produce, or service restart is required for this PR.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10889/dod-deploy/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10889/dod-deploy/command.yaml && grep -q 'deploy' drift/dod_receipts/OMN-10889/dod-deploy/command.yaml"
  - id: "dod-pr-1576-head"
    description: "Bind OMN-10889 Wave 2 topic parity evidence to omnibase_infra PR #1576 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10889/dod-pr-1576-head/command.yaml\"
          && grep -q '^pr_number: 1576$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10889/dod-pr-1576-head/command.yaml\"
          && grep -q '^commit_sha: \"7d9d35e25b1e2e5a92872ee7537ae1066009089e\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10889/dod-pr-1576-head/command.yaml\""
  - id: "dod-pr-613-head"
    description: "Bind OMN-10889 Wave 2 contract dependency and topic literal evidence to omnimarket PR
      #613 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10889/dod-pr-613-head/command.yaml\"
          && grep -q '^pr_number: 613$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10889/dod-pr-613-head/command.yaml\"
          && grep -q '^commit_sha: \"c5f5d791c05e1f6d1344580e920eeeaf1e891901\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10889/dod-pr-613-head/command.yaml\""
  - id: "dod-occ-pr"
    description: "Self-binding receipt for the OCC PR carrying this contract."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10889/dod-occ-pr/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10889/dod-occ-pr/command.yaml"
