---
schema_version: "1.0.0"
ticket_id: "OMN-15346"
title: "Autobind OCC evidence for OMN-15346"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #742."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #742 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "ci"
    description: "OMN-15853 template-expand repair: PR #742 product diff scope present. The entry above
      never had its ${PR_NUMBER}/${REPO} template expanded (a latent same-class defect alongside the occ-self-bind-pr-5381
      template below); this entry carries the same check with the real values substituted. Not receipt-gated
      (evidence_requirements is not evaluated by dod_verify), appended so no unexpanded template remains
      in this contract."
    command: "gh pr view 742 --repo OmniNode-ai/omninode_infra --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-742"
    description: "PR #742 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15346/dod-OmniNode-ai-omninode_infra-pr-742/command.yaml"
  - id: "dod-OmniNode-ai-omninode_infra-pr-742-ci"
    description: "PR #742 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15346/dod-OmniNode-ai-omninode_infra-pr-742-ci/command.yaml"
  - id: "occ-self-bind-pr-5381"
    description: "OCC companion PR #5381 — self-bind for OMN-15346 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-5381-template-expand"
    description: >-
      OMN-15853 template-expand repair: the occ-self-bind-pr-5381 entry above never had its ${PR_NUMBER}/${REPO}
      template expanded when node_pr_lifecycle_fix_effect authored it -- the merged receipt at drift/dod_receipts/OMN-15346/occ-self-bind-pr-5381/command.yaml
      recorded the correctly-expanded command and passed, but the CONTRACT entry's own check_value kept
      the literal, unexpanded placeholder text. Executed as written by dod_verify, `gh pr view ${PR_NUMBER}
      --repo ${REPO} --json number,state` resolves PR_NUMBER/REPO as empty (unset shell vars), which is
      PR_LOOKUP_FAILED -- the sole remaining blocker on OMN-15346. This entry supersedes it (evidence_artifact
      pointer below) with the real, live-executed values (PR #5381, OmniNode-ai/onex_change_control) without
      editing the merged base entry (OMN-14623 merged-path supersede; append-only).
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-5381"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5381 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-6347"
    description: "Binds OMN-15346 to OCC companion PR #6347 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6347 --repo OmniNode-ai/onex_change_control --json number,state"
