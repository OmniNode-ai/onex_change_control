---
schema_version: "1.0.0"
ticket_id: "OMN-12979"
title: "Expire-bound topic completeness suppressions"
summary: >-
  Central evidence for omnibase_infra PR #1940, which replaces permanent topic-completeness allowlist
  behavior with metadata-rich temporary suppressions. Topic authority remains contract YAML, topics.yaml
  manifests, generated topic enums, and canonical topic constants; suppression files are validation-only
  exceptions and must include owner, reason, created_at, expires_at, and replacement_authority.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
evidence_requirements:
  - kind: "tests"
    description: "Focused topic-completeness validator tests pass."
    command: "uv run pytest tests/unit/scripts/validation/test_check_topic_completeness.py -q"
  - kind: "manual"
    description: "Topic-completeness validator runs against the repo without suppressing missing authority."
    command: "uv run python scripts/validation/check_topic_completeness.py"
  - kind: "manual"
    description: "Touched validator and tests pass ruff."
    command: "uv run ruff check scripts/validation/check_topic_completeness.py tests/unit/scripts/validation/test_check_topic_completeness.py"
dod_evidence:
  - id: "dod-omnibase-infra-pr-1940"
    description: >-
      omnibase_infra PR #1940 adds expiring topic-completeness suppressions, rejects legacy bare allowlist
      entries, and documents that suppression files are not topic authority.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12979/dod-omnibase-infra-pr-1940/command.yaml"
          && grep -q '^pr_number: 1940$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12979/dod-omnibase-infra-pr-1940/command.yaml"
          && grep -q 'c068eb283e6788a22303d514f830180a20e9b424' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12979/dod-omnibase-infra-pr-1940/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      The onex_change_control PR carries the central OMN-12979 contract and receipts required by downstream
      Receipt Gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12979/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-12979"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12979/dod-occ-pr/command.yaml"
