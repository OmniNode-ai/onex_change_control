---
schema_version: "1.0.0"
ticket_id: "OMN-10645"
title: "Strip lab LAN endpoint literals from ab_compare models registry"
summary: >
  Removes local lab endpoint literals from the AB compare orchestrator registry and requires local models
  to resolve endpoints through endpoint_env while preserving public cloud endpoint literals.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ci-summary"
    description: "omnimarket PR #557 CI Summary passed on the evidence head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10645/dod-ci-summary/command.yaml\""
  - id: "dod-pr-557-head"
    description: "Bind OMN-10645 evidence to omnimarket PR #557 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10645/dod-pr-557-head/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10645 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10645/dod-occ-pr/command.yaml\""
