---
schema_version: "1.0.0"
ticket_id: "OMN-11674"
title: "Migrate SEA gates to omnimarket nodes"
summary: >
  Central OCC evidence for omnimarket PR #937. Replaces importable-only smoke tests for node_chain_diff_compute
  and node_closeout_verifier_compute with comprehensive unit test coverage (32 tests total). Both nodes
  were structurally complete prior to this PR; the missing piece was meaningful test assertions covering
  all diff scenarios, failure classes, identity gate, and determinism.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-937"
    description: "omnimarket PR #937 adds full unit test coverage for SEA gate compute nodes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json mergedAt -q .mergedAt --repo ${REPO}"
  - id: "dod-deploy-assessment"
    description: >
      No live deploy was performed; change is test-only (replaces importable-only smoke tests with scenario
      coverage). Runtime handler code for node_chain_diff_compute and node_closeout_verifier_compute is
      unchanged. CI and deploy gates validate before merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-11674/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-11674/dod-deploy-assessment/command.yaml
