---
schema_version: "1.0.0"
ticket_id: "OMN-10088"
summary: "Add the seed-keycloak-clients reconciler for desired client state"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused reconciler tests pass locally"
    command: "cd ../omnibase_infra && uv run pytest scripts/tests/test_seed_keycloak_clients.py -v"
  - kind: "ci"
    description: "omnibase_infra PR checks pass for the Keycloak reconciler"
    command: "gh pr checks 1432 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "The seed-keycloak-clients reconciler unit suite passes"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10088/dod-001 -type f -name '*.yaml' | grep -q ."
