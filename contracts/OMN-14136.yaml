---
schema_version: "1.0.0"
ticket_id: "OMN-14136"
title: "fix(OMN-14136): add writable run-onex mount for onex-prod runtime"
summary: >-
  Central OCC evidence for omninode_infra PR #579. The PR adds the missing writable `/run/onex-runtime`
  emptyDir mount to the onex-prod runtime main/API manifest while preserving readOnlyRootFilesystem and
  the existing hardened security context. This OCC evidence update performs no live deploy, no runtime
  restart, no prod promotion, no branch-protection mutation, and no bypass.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-579"
    description: >-
      omninode_infra PR #579 is open against dev at head 158bf6b244df7538de11351f10059ad466b2bf4b and
      carries the onex-prod runtime writable /run/onex-runtime manifest fix for OMN-14136.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14136/dod-omninode-infra-pr-579/command.yaml
          && grep -q '^pr_number: 579$' drift/dod_receipts/OMN-14136/dod-omninode-infra-pr-579/command.yaml
          && grep -q '^commit_sha: 158bf6b244df7538de11351f10059ad466b2bf4b$' drift/dod_receipts/OMN-14136/dod-omninode-infra-pr-579/command.yaml
  - id: "dod-no-live-deploy"
    description: >-
      Deploy-scope evidence: this OCC PR only records central evidence for a source manifest PR. It performs
      no live deploy, no runtime restart, no prod promotion, no branch-protection change, and no bypass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14136/dod-no-live-deploy/command.yaml && grep
          -qi 'no live deploy' drift/dod_receipts/OMN-14136/dod-no-live-deploy/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding receipt: this onex_change_control PR carries the central OMN-14136 contract and PASS
      receipts consumed by omninode_infra PR #579 via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14136/dod-occ-pr-self/command.yaml && grep -q
          '^pr_number: 3729$' drift/dod_receipts/OMN-14136/dod-occ-pr-self/command.yaml
  - id: "dod-occ-pr-self-3730"
    description: >-
      Self-binding receipt: onex_change_control PR #3730 refreshes the central OMN-14136 evidence after
      omninode_infra PR #579 was updated with the latest dev base.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14136/dod-occ-pr-self-3730/command.yaml && grep
          -q '^pr_number: 3730$' drift/dod_receipts/OMN-14136/dod-occ-pr-self-3730/command.yaml
