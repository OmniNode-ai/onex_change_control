---
schema_version: "1.0.0"
ticket_id: "OMN-12743"
title: "fix(demo): remove hardcoded model/provider/path literals on demo-visible paths (Track A agent.py:89
  + classify all literals)"
summary: >-
  Central evidence that demo-visible code paths no longer hardcode model/provider/endpoint literals. Track
  A agent.py:89 either no longer hardcodes gemini-flash-latest (resolved from contract/overlay config)
  OR Track A is explicitly marked out-of-demo-scope in the plan doc. All remaining hardcoded literals
  classified as demo-path / experiment-only / dead-code / deferred. Runtime model identity confirmed via
  inference-response event, not source inspection.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-literal-classification-table"
    description: "Literal classification table produced: every hardcoded model/provider/endpoint literal
      classified as exactly one of demo-path / experiment-only / dead-code / deferred. Track A scope decision
      (in-demo vs out-of-scope) made explicit in the plan doc."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12743/dod-literal-classification-table/command.yaml"
  - id: "dod-track-a-literal-resolved"
    description: "Track A agent.py:89 hardcode removed (model sourced from contract/registry) OR explicit
      out-of-demo-scope note added to plan doc. Work PR or plan doc diff cited as evidence."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12743/dod-track-a-literal-resolved/command.yaml"
  - id: "dod-runtime-model-identity"
    description: "Runtime-observed model identity via correlation id + inference-response event proves
      the resolved model, not source-read. Event log excerpt or runtime log output."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12743/dod-runtime-model-identity/command.yaml"
  - id: "dod-occ-pr-evidence"
    description: "OCC PR carries the OMN-12743 central contract and PASS receipts."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12743/dod-occ-pr-evidence/command.yaml"
