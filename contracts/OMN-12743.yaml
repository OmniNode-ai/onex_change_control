---
schema_version: "1.0.0"
ticket_id: "OMN-12743"
title: "fix(OMN-12743): remove hardcoded model/provider/path literals on demo-visible paths"
summary: >-
  Central OCC evidence for omnimarket PR #1071. Demo-visible code paths no longer hardcode model/provider/endpoint
  literals: Track A agent.py model is resolved from TRACK_A_GEMINI_MODEL (env-overridable) instead of
  a bare gemini-flash-latest literal, every remaining literal is classified (demo-path / experiment-only
  / dead-code / deferred) by a permanent classification test, and the demo fanout golden-chain literals
  are annotated as preflight-error fixtures. The PR touches the runtime path node_demo_fanout_orchestrator/tests/test_golden_chain.py,
  so deploy-gate evidence is carried here: runtime-observed model identity is proven via a dev-lane inference
  request/response on the bus, not by source inspection. Runtime model-identity proof remains gated on
  the OMN-12742 dev/stability redeploy.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-literal-classification-table"
    description: "Permanent classification test classifies every hardcoded model/provider/endpoint literal
      as exactly one of demo-path / experiment-only / dead-code / deferred, and a production scanner asserts
      no bare gemini-flash-latest literal outside experiment/test/data subtrees."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12743/dod-literal-classification-table/command.yaml"
          && grep -q 'test_omn12743_literal_classification' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12743/dod-literal-classification-table/command.yaml"
  - id: "dod-track-a-literal-resolved"
    description: "Track A agent.py model literal resolved from TRACK_A_GEMINI_MODEL (env-overridable constant)
      instead of a bare gemini-flash-latest source literal."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12743/dod-track-a-literal-resolved/command.yaml"
          && grep -q 'TRACK_A_GEMINI_MODEL' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12743/dod-track-a-literal-resolved/command.yaml"
  - id: "dod-deploy-runtime-model-identity"
    description: "Deploy-gate evidence for the runtime-path change in node_demo_fanout_orchestrator: after
      merge and the OMN-12742 dev-lane redeploy, produce a demo fanout request on the dev broker and consume
      the matching inference-response event to confirm the resolved model identity at runtime (not source-read).
      Gated on the OMN-12742 redeploy."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12743/dod-deploy-runtime-model-identity/command.yaml"
          && grep -q 'deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12743/dod-deploy-runtime-model-identity/command.yaml"
          && grep -q 'rpk topic produce' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12743/dod-deploy-runtime-model-identity/command.yaml"
  - id: "dod-occ-pr-evidence"
    description: "onex_change_control PR carries the OMN-12743 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12743/dod-occ-pr-evidence/command.yaml"
