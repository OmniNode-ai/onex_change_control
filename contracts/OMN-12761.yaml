---
schema_version: "1.0.0"
ticket_id: "OMN-12761"
title: "Route savings projection to omnidash_analytics + fix registration projection schema"
summary: >-
  Two contract.yaml corrections in omnimarket: (1) node_projection_savings: db_io.db_tables[].database
  corrected from omnibase_infra to omnidash_analytics. Migration 075 (savings_estimates + updated_at)
  was applied to omnidash_analytics; the wrong value caused HandlerProjectionSavings to emit UndefinedColumn:
  updated_at on every delegate-skill-completed event. (2) node_projection_registration: projection_api.schema
  corrected from "omnidash_analytics" (a database name) to "public" (the Postgres schema). The wrong value
  caused omnimarket-projection-api to latch a degraded flag at startup. Verified live on .201 dev lane:
  savings_estimates populated (15 rows after backfill), registration.v1 returns row_count=36 fresh.
is_seam_ticket: false
interface_change: false
interfaces_touched:
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1076"
    description: "omnimarket PR #1076 corrects savings db_io database and registration projection_api
      schema."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12761/dod-omnimarket-pr-1076/command.yaml &&
          grep -q '^pr_number: 1076$' drift/dod_receipts/OMN-12761/dod-omnimarket-pr-1076/command.yaml
          && grep -q 'a0c98437' drift/dod_receipts/OMN-12761/dod-omnimarket-pr-1076/command.yaml
  - id: "dod-live-verification"
    description: >-
      Live .201 dev-lane deploy verification: docker exec omnibase-infra-postgres confirms savings_estimates
      15 rows, registration.v1 row_count=36 fresh, e2e delegation correlation 13bf4549 round-trips savings_estimates
      +1. Contract patched in running containers.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12761/dod-live-verification/command.yaml &&
          grep -q 'docker exec omnibase-infra-postgres' drift/dod_receipts/OMN-12761/dod-live-verification/command.yaml
  - id: "dod-occ-evidence"
    description: "onex_change_control OCC contract + receipts for OMN-12761."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12761/dod-occ-evidence/command.yaml
