---
schema_version: "1.0.0"
ticket_id: "OMN-12116"
title: "Set event_type on dispatch result applier output envelopes"
summary: >
  Central OCC evidence for omnibase_infra PR #1742. The dispatch result applier derives ModelEventEnvelope.event_type
  from resolved ONEX output topics so multi-step FSM orchestrators can match response events to dispatcher
  registration aliases instead of routing them to DLQ.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1742"
    description: "omnibase_infra PR #1742 carries the dispatch result applier event_type fix and focused
      tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12116/dod-infra-pr-1742/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Runtime path change requires normal post-merge deploy validation. No live production deploy is executed
      by this OCC PR; downstream deploy gate is satisfied by the explicit deploy assessment and post-merge
      validation requirement.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy: post-merge runtime deploy validation required for omnibase_infra dispatch
          result applier'"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carrying the central OCC evidence for OMN-12116."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12116/dod-occ-pr/command.yaml"
