---
schema_version: "1.0.0"
ticket_id: "OMN-12116"
title: "Set event_type on dispatch result applier output envelopes"
summary: >
  Central OCC evidence for omnibase_infra PR #1742. The dispatch result applier derives ModelEventEnvelope.event_type
  from resolved ONEX output topics so multi-step FSM orchestrators can match response events to dispatcher
  registration aliases instead of routing them to DLQ.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1742"
    description: "omnibase_infra PR #1742 carries the dispatch result applier event_type fix and focused
      tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12116/dod-infra-pr-1742/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Runtime path change validated via hot-patch on stability-test containers. docker exec smoke test
      confirmed _derive_event_type_from_topic returns 'omnimarket.swarm-dispatch-completed' on the live
      runtime (run_id=8d68c8e7-5b5f-4e92-a401-356a4abd9cd9, offset 15).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12116/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR #1618 carries the central OCC evidence for OMN-12116."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12116/dod-occ-pr/command.yaml"
  - id: "dod-occ-pr-1627"
    description: "onex_change_control PR #1627 binds the verified hot-patch deploy receipt for OMN-12116."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12116/dod-occ-pr-1627/command.yaml
          && grep -q '^pr_number: 1627$' drift/dod_receipts/OMN-12116/dod-occ-pr-1627/command.yaml"
