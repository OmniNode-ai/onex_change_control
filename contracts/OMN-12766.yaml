---
schema_version: 1.0.0
ticket_id: OMN-12766
title: 'fix(OMN-12766): correct dishonest PASS receipts to PENDING on OCC main'
summary: 'Integrity remediation: four DoD receipts on OMN-12742 and OMN-12743 were promoted to OCC main
  with status: PASS while their probe_stdout explicitly stated PENDING. One receipt (dod-user-approval-receipt)
  fabricated verifier: jonah without a real user-issued approval handle. All four are corrected to status:
  PENDING with verifier: automation-pending and honest actual_output/exit_code. Evidence: this OCC PR
  itself carries the corrected receipt files and a truthful PASS receipt for the correction work only.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-receipts-corrected
    description: 'All four dishonest receipts corrected to status: PENDING with honest verifier and probe_stdout.
      Verified by grep on the corrected files in this OCC PR branch.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12766/dod-receipts-corrected/command.yaml"'
  - id: dod-occ-pr-evidence
    description: onex_change_control dev PR carries the OMN-12766 contract and PASS receipt for the correction.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12766/dod-occ-pr-evidence/command.yaml"'
  - id: dod-occ-main-pr
    description: onex_change_control main promotion PR promotes the OMN-12766 correction from dev to main.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12766/dod-occ-main-pr/command.yaml"'
