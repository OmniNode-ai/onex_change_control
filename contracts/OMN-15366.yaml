---
schema_version: 1.0.0
ticket_id: OMN-15366
title: Autobind OCC evidence for OMN-15366
summary: 'OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #749.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #749 product diff scope present'
    command: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-OmniNode-ai-omninode_infra-pr-749
    description: 'PR #749 on OmniNode-ai/omninode_infra — Evidence-Source autobind.'
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15366/dod-OmniNode-ai-omninode_infra-pr-749/command.yaml'
  - id: dod-OmniNode-ai-omninode_infra-pr-749-ci
    description: 'PR #749 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425).'
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15366/dod-OmniNode-ai-omninode_infra-pr-749-ci/command.yaml'
  - id: occ-self-bind-pr-5413
    description: 'OCC companion PR #5413 — self-bind for OMN-15366 (OMN-14650).'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: omn-15366-admissibility-engine-still-green
    description: 'Executed, falsifiable, outside its own diff: runs this repo''s admissibility suite,
      which is the machinery deciding whether any OMN-15366 evidence counts. SCOPE, stated plainly: this
      does NOT prove the omninode_infra#749 product facts. The four cross-repo contents probes that did
      prove them were removed because the Contract Compliance job''s token is scoped to this repository
      and returned HTTP 404 for every one of them -- they passed when run with a multi-repo token, and
      BLOCKED here. Do not re-add that form. The product facts are carried by omninode_infra#749''s own
      CI (tests/k8s/test_staging_composition_contract.py, which binds the composition digest to the kustomization
      and the migrate Job) and by the deploy-onex-dev run on merged dev.'
    source: manual
    checks:
      - check_type: command
        check_value: uv run pytest tests/test_evidence_admissibility.py -q
