---
schema_version: "1.0.0"
ticket_id: OMN-8153
summary: "Task 8: Remove Dead omnidash Topic Constants (Gap 1)"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json state -q .state"
  - id: "dod-002"
    description: "SUFFIX_INTELLIGENCE_EVAL_COMPLETED uses eval-completed.v1 suffix (not bloom variant)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'SUFFIX_INTELLIGENCE_EVAL_COMPLETED.*eval-completed\\.v1' shared/topics.ts"
  - id: "dod-003"
    description: "shared/topics.ts is touched in this PR (topic constants updated)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json files --jq '[.files[].path] | map(select(.
          == \"shared/topics.ts\")) | length' | grep -q '^[1-9]'"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
