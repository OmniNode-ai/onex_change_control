---
schema_version: "1.0.0"
ticket_id: OMN-8153
summary: "Task 8: Remove Dead omnidash Topic Constants (Gap 1)"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json state -q .state"
  - id: "dod-002"
    description: "SUFFIX_INTELLIGENCE_EVAL_COMPLETED uses eval-completed.v1 in omnidash topics.ts"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnidash/contents/shared/topics.ts --jq '.content' | base64
          -d | grep -q 'SUFFIX_INTELLIGENCE_EVAL_COMPLETED.*eval-completed\\.v1'"
  - id: "dod-003"
    description: "PR is open or merged (not closed/abandoned)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json state -q .state | grep -qE 'OPEN|MERGED'"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
