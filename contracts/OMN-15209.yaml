---
schema_version: "1.0.0"
ticket_id: "OMN-15209"
title: "Slim onex_change_control CLAUDE.md end to end with live-verified KEEP contract"
summary: >
  Slims onex_change_control/CLAUDE.md from 8327 to 4360 bytes on this repo's own dev branch (in-repo product
  change; contract + receipts ride the same PR per the I3 in-tree-checkout invariant). Corrects live-false
  claims inherited from the 2026-06-21 refresh: ModelTicketContract is defined in omnibase_core.models.ticket.model_ticket_contract
  and only re-exported here (OMN-10066), not OCC-owned; models/ count drifted 30->33 and enums/ 25->28;
  [project.scripts] drifted 22->26; the architecture tree omitted integrations/ and overlays/; the SPDX
  section listed a nonexistent examples/ directory. Adds the canonical receipt-surface facts (drift/dod_receipts/<TICKET>/<ITEM_ID>/<run_timestamp>.yaml,
  contract_sha256 / contract_entry_sha256 binding, yamlfmt reflow trap) and the no-deploy-gate.yml-by-design
  rationale so nobody wires a never-reporting required context and wedges merges.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "In-tree probes against product commit b31bbae8fdd0e7411944566c2874c21ddc5bd564 on this
      branch."
    command: "git cat-file -e b31bbae8fdd0e7411944566c2874c21ddc5bd564:CLAUDE.md"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-claude-md-slim-b31bbae8"
    description: >
      Falsifiable product probe: CLAUDE.md at product commit b31bbae8fdd0e7411944566c2874c21ddc5bd564
      carries the corrected ModelTicketContract-is-a-core-re-export statement (naming omnibase_core.models.ticket.model_ticket_contract,
      a string absent from the dev-base file, so this check is RED against any ref without the slim) and
      is at most 5000 bytes (dev base is 8327 bytes).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          git cat-file -p b31bbae8fdd0e7411944566c2874c21ddc5bd564:CLAUDE.md | grep -q 'omnibase_core.models.ticket.model_ticket_contract'
          && test "$(git cat-file -s b31bbae8fdd0e7411944566c2874c21ddc5bd564:CLAUDE.md)" -le 5000
  - id: "dod-no-deploy-gate-rationale-b31bbae8"
    description: >
      Falsifiable product probe: CLAUDE.md at product commit b31bbae8fdd0e7411944566c2874c21ddc5bd564
      carries the no-deploy-gate.yml-by-design rationale (grep target 'no runtime contracts, so it has
      no `deploy-gate.yml`', absent from the dev-base file) plus the canonical receipt-location line,
      so the load-bearing receipt-surface facts survive the slim.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          git cat-file -p b31bbae8fdd0e7411944566c2874c21ddc5bd564:CLAUDE.md | grep -q 'no runtime contracts,
          so it has no' && git cat-file -p b31bbae8fdd0e7411944566c2874c21ddc5bd564:CLAUDE.md | grep -q
          'drift/dod_receipts/<TICKET>/<ITEM_ID>/<run_timestamp>.yaml'
  - id: "dod-occ-self"
    description: >
      OCC self-bind for OMN-15209: the PASS receipt corpus for this ticket exists in-tree on the same
      branch as the product change (deterministic commit-sha bind, no PR-number prediction).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15209/dod-occ-self/command.yaml"
