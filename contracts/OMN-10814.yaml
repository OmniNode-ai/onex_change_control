---
schema_version: "1.0.0"
ticket_id: "OMN-10814"
title: "fix(OMN-10814): add config injection path for LINEAR_API_KEY in node_merge_gate_effect"
summary: >
  omnibase_infra PR #1647 removes direct os.environ reads for LINEAR_API_KEY and LINEAR_TEAM_ID from HandlerUpsertMergeGate,
  declares both values in the node_merge_gate_effect contract config, and verifies constructor injection
  with focused unit and integration tests. Live runtime deploy/restart is not performed pre-merge; post-merge
  runtime validation is carried by the target repo deploy contract.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1647 carries the OMN-10814 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10814/dod-product-pr/command.yaml\""
  - id: "dod-focused-tests"
    description: "Focused merge-gate config injection unit and integration tests pass locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10814/dod-focused-tests/command.yaml\""
  - id: "dod-ruff-up006-up007"
    description: "PEP 604 and collection type syntax gate passes locally on src/."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10814/dod-ruff-up006-up007/command.yaml\""
  - id: "dod-deploy-contract"
    description: "omnibase_infra repo-local deploy contract exists for OMN-10814."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10814/dod-deploy-contract/command.yaml\""
