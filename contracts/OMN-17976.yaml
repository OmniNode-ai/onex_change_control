---
schema_version: "1.0.0"
ticket_id: "OMN-17976"
title: "Autobind OCC evidence for OMN-17976"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3226.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3226 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3226 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: "uv run pytest tests/ci/test_autoclose_collector_fetch_credential_omn17976.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3226"
    description: "PR #3226 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/ci/test_autoclose_collector_fetch_credential_omn17976.py?ref=c9493c919441e6810f926d3490ccaa716fc57a35 --jq '.content' | base64 -d | grep -c 'def workflow'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3226/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/evidence-autoclose-sweep.yml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3226 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/ci/test_autoclose_collector_fetch_credential_omn17976.py -q"
        cwd: "${OMNI_HOME}/omnibase_infra"
