---
schema_version: "1.0.0"
ticket_id: "OMN-8694"
title: "fix(OMN-8694): populate cost fields for non-dispatch savings estimates"
summary: >
  infra_routing_decisions projection was leaving cost_usd, token_count, and latency_ms as None for non-dispatch
  savings estimates (OMN-8690 follow-up). Fix populates those fields from the savings estimate payload
  so the evidence dashboard can render accurate cost comparisons.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Projection pipeline tests pass at the omnibase_infra PR head."
    command: "uv run pytest tests/ -k 'routing_decisions' -v"
  - kind: "manual"
    description: "omnibase_infra PR #1699 is open against main at the recorded head SHA."
    command: "gh pr view 1699 --repo OmniNode-ai/omnibase_infra --json state -q '.state'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-1699"
    description: "omnibase_infra PR #1699 carries the cost field population fix at commit 6203b959."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8694/dod-product-pr-1699/command.yaml"
          && grep -q '^pr_number: 1699$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8694/dod-product-pr-1699/command.yaml"
  - id: "dod-deploy"
    description: "deploy gate: cost field population fix in projection handler — no new Kafka topics or
      container restart required."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy gate: OMN-8694 populates cost_usd/token_count/latency_ms
          in infra_routing_decisions projection — projection logic fix only, no docker exec or topic topology
          change required'"
  - id: "dod-occ-self"
    description: "Self-binding OCC receipt for the contract carrying OMN-8694 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8694/dod-occ-self/command.yaml"
          && grep -q '^ticket_id: "OMN-8694"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8694/dod-occ-self/command.yaml"
