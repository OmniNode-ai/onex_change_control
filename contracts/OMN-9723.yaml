---
schema_version: "1.0.0"
ticket_id: OMN-9723
title: "C2a: session_orchestrator silent-zero adversarial tests (OMN-9561)"
summary: "C2a: session_orchestrator silent-zero adversarial tests (OMN-9561)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR checks pass including typecheck and test"
    command: "gh pr checks 408 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "Primary adversarial tests pass (no live network required)"
    command: "uv run pytest src/omnimarket/nodes/node_session_orchestrator/tests/test_session_orchestrator_silent_zero.py
      -v -m 'not slow'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Adversarial silent-zero tests pass (11 primary, mock-based)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest src/omnimarket/nodes/node_session_orchestrator/tests/test_session_orchestrator_silent_zero.py
          -v -m 'not slow'"
  - id: "dod-002"
    description: "mypy strict passes on new test file"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run mypy src/omnimarket/nodes/node_session_orchestrator/tests/test_session_orchestrator_silent_zero.py
          --strict"
  - id: "dod-003"
    description: "Post-merge smoke: session_orchestrator handler imports cleanly in deployed runtime"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_session_orchestrator.handlers.handler_session_orchestrator
          import HandlerSessionOrchestrator; print('deploy smoke ok')\""
