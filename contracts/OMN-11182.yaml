---
schema_version: "1.0.0"
ticket_id: "OMN-11182"
title: "Resolve generation consumer endpoint via contract, not env vars"
summary: >
  Remove bespoke GENERATION_CONSUMER_ENDPOINT/MODEL_ID/API_KEY/PROVIDER/ENDPOINT_CLASS env vars from node_generation_consumer.
  Add model_routing section to contract.yaml declaring endpoint_env, model_id, and auth. Add dependencies
  entries so the overlay config resolver knows LLM_CODER_URL, LOCAL_LLM_SHARED_SECRET, LLM_ENDPOINT_CIDR_ALLOWLIST
  are required at boot. Handler reads endpoint_env from the contract at construction and resolves os.environ[endpoint_env]
  (LLM_CODER_URL) at call time.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "42 unit tests pass for node_generation_consumer including 4 new contract-routing tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11182/dod-unit-tests/command.yaml"
  - id: "dod-no-bespoke-env-vars"
    description: "GENERATION_CONSUMER_ENDPOINT no longer appears in handler source."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11182/dod-no-bespoke-env-vars/command.yaml"
  - id: "dod-deploy"
    description: >
      Contract+handler-only change: removes env var reads, adds contract model_routing declaration. No
      new node registration, no new Kafka topics, no schema changes. Node imports cleanly in the deployed
      runtime. Post-merge deploy validation imports the handler in the runtime container.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11182/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-11182 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11182/dod-occ-pr/command.yaml"
