---
schema_version: "1.0.0"
ticket_id: "OMN-11427"
title: "Repowise critical: add proof for omnimarket build-loop and PR lifecycle hotspots"
summary: >
  Add targeted proof coverage for omnimarket Repowise critical untested hotspot surfaces in delegation
  wire exports, baseline capture, build-loop state, delegation routing, build-loop orchestration, and
  PR lifecycle orchestration.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused OMN-11427 omnimarket proof tests pass."
    command: "uv run pytest <focused OMN-11427 test selection> -q"
  - kind: "manual"
    description: "omnimarket PR #737 is open against main at the recorded head SHA."
    command: "gh pr view 737 --repo OmniNode-ai/omnimarket --json headRefOid,state,url,headRefName,baseRefName"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused OMN-11427 proof tests passed locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11427/dod-focused-tests/command.yaml\"
          && grep -q '^commit_sha: \"d6f4239ee3c27de17f179c2b7f86318c21a20e7e\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11427/dod-focused-tests/command.yaml\""
  - id: "dod-pr-737-open"
    description: "omnimarket PR #737 is open against main at the recorded head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11427/dod-pr-737-open/command.yaml\"
          && grep -q '^pr_number: 737$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11427/dod-pr-737-open/command.yaml\"
          && grep -q '^commit_sha: \"d6f4239ee3c27de17f179c2b7f86318c21a20e7e\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11427/dod-pr-737-open/command.yaml\""
