---
schema_version: "1.0.0"
ticket_id: OMN-10449
title: "Align compose with .201 production health windows, memory, and env vars"
summary: "Align docker-compose.infra.yml runtime environment with the production runtime contract by replacing
  legacy OmniClaude roots with ONEX package and marketplace skill roots while preserving the already-aligned
  memory, health, and autoheal windows."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
evidence_requirements:
  - kind: "manual"
    description: "Runtime env anchor and compose env parity tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10449/dod-compose-env-tests/command.yaml"
  - kind: "manual"
    description: "Base compose config renders successfully"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10449/dod-base-compose-config/command.yaml"
  - kind: "manual"
    description: "Stability overlay compose config renders successfully"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10449/dod-overlay-compose-config/command.yaml"
  - kind: "manual"
    description: "Compose runtime env and health values match OMN-10449"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10449/dod-compose-value-probe/command.yaml"
  - kind: "manual"
    description: "Repo-local ticket contract validates"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10449/dod-contract-validate/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-compose-env-tests"
    description: "Runtime env anchor and compose env parity tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10449/dod-compose-env-tests/command.yaml
          && grep -q '^ticket_id: OMN-10449$' drift/dod_receipts/OMN-10449/dod-compose-env-tests/command.yaml
          && grep -q '^evidence_item_id: dod-compose-env-tests$' drift/dod_receipts/OMN-10449/dod-compose-env-tests/command.yaml"
  - id: "dod-base-compose-config"
    description: "Base compose config renders successfully"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10449/dod-base-compose-config/command.yaml
          && grep -q '^ticket_id: OMN-10449$' drift/dod_receipts/OMN-10449/dod-base-compose-config/command.yaml
          && grep -q '^evidence_item_id: dod-base-compose-config$' drift/dod_receipts/OMN-10449/dod-base-compose-config/command.yaml"
  - id: "dod-overlay-compose-config"
    description: "Stability overlay compose config renders successfully"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10449/dod-overlay-compose-config/command.yaml
          && grep -q '^ticket_id: OMN-10449$' drift/dod_receipts/OMN-10449/dod-overlay-compose-config/command.yaml
          && grep -q '^evidence_item_id: dod-overlay-compose-config$' drift/dod_receipts/OMN-10449/dod-overlay-compose-config/command.yaml"
  - id: "dod-compose-value-probe"
    description: "Compose runtime env and health values match OMN-10449"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10449/dod-compose-value-probe/command.yaml
          && grep -q '^ticket_id: OMN-10449$' drift/dod_receipts/OMN-10449/dod-compose-value-probe/command.yaml
          && grep -q '^evidence_item_id: dod-compose-value-probe$' drift/dod_receipts/OMN-10449/dod-compose-value-probe/command.yaml"
  - id: "dod-contract-validate"
    description: "Repo-local ticket contract validates"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10449/dod-contract-validate/command.yaml
          && grep -q '^ticket_id: OMN-10449$' drift/dod_receipts/OMN-10449/dod-contract-validate/command.yaml
          && grep -q '^evidence_item_id: dod-contract-validate$' drift/dod_receipts/OMN-10449/dod-contract-validate/command.yaml"
