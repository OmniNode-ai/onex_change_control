---
schema_version: "1.0.0"
ticket_id: "OMN-14837"
title: "Autobind OCC evidence for OMN-14837"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1832."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1832 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1832"
    description: "PR #1832 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1832-ci"
    description: "PR #1832 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4462"
    description: "OCC companion PR #4462 — self-bind for OMN-14837 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1832-deploy-scope"
    description: >-
      Deploy-gate scope for omnimarket PR #1832. The change touches runtime merge-sweep and PR lifecycle
      handler/protocol paths, but only regenerates the existing PR lifecycle triage compute lane to the
      canonical typed def-B dispatch shape with equivalence receipts. No live runtime deployment is performed
      by this PR; the required deploy evidence is the merge-time CI/OCC gate suite plus post-merge availability
      of the handler paths on dev.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          printf '%s\n' 'deploy scope assessed for omnimarket#1832 runtime PR lifecycle triage and merge-sweep
          handler paths; no live deploy, docker exec, or rpk topic produce is authorized by this evidence
          repair'
  - id: "occ-self-bind-pr-4467"
    description: "OCC deploy-scope repair PR #4467 — self-bind for OMN-14837."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4473"
    description: "Binds OMN-14837 to OCC companion PR #4473 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
