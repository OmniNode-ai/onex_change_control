---
schema_version: "1.0.0"
ticket_id: "OMN-12892"
title: "SEA ADK model selection from contract overlay"
summary: >-
  Central evidence for onex-self-extending-agent PR #224. The current SEA ADK runtime already resolves
  the active ADK model reference from node_agent_orchestrator/contract.yaml or contract.local.yaml and
  joins it to the typed model_registry.yaml entry. PR #224 adds focused proof for the contract-local overlay
  path and updates the README so judge-facing documentation no longer shows a stale source-level model
  literal. A follow-up OCC PR keeps an open evidence source available while the product PR is gated.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-224-source-checks"
    description: >-
      onex-self-extending-agent PR #224 carries the W1A proof update against dev and passed focused tests
      plus commit-time SEA unit/integration gates.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12892/dod-sea-pr-224-source-checks/command.yaml"
          && grep -q '^pr_number: 224$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12892/dod-sea-pr-224-source-checks/command.yaml"
          && grep -q '66 passed, 1 skipped' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12892/dod-sea-pr-224-source-checks/command.yaml"
          && grep -q '947fb4855777e8a2b401e802efe83435f8504d92' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12892/dod-sea-pr-224-source-checks/command.yaml"
  - id: "dod-contract-overlay-readback"
    description: >-
      A contract.local.yaml overlay selects local_qwen_coder by adk_model_ref and the ADK runtime resolves
      the local registry backend descriptor instead of using a source-level model literal.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12892/dod-contract-overlay-readback/command.yaml"
          && grep -q 'local_qwen_coder' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12892/dod-contract-overlay-readback/command.yaml"
          && grep -q 'local-coder' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12892/dod-contract-overlay-readback/command.yaml"
  - id: "dod-no-deploy-assessment"
    description: >-
      PR #224 changes only README documentation and unit proof coverage. It does not deploy, restart,
      mutate .201, or change runtime contracts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12892/dod-no-deploy-assessment/command.yaml"
          && grep -q 'No live .201 deployment' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12892/dod-no-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2414-self"
    description: >-
      onex_change_control PR #2414 carries the OMN-12892 central contract and receipt evidence for product
      PR #224.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12892/dod-occ-pr-2414-self/command.yaml"
          && grep -q '^pr_number: 2414$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12892/dod-occ-pr-2414-self/command.yaml"
          && grep -q '8989b6ab8bcdaa4a2721974016be8fce0a4d16ec' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12892/dod-occ-pr-2414-self/command.yaml"
