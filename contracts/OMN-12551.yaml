---
schema_version: "1.0.0"
ticket_id: "OMN-12551"
title: "Codex direct terminal listener honors Kafka API version"
summary: >-
  Bind central evidence for omnimarket PR #1009, which fixes the Codex runtime adapter direct terminal
  listener so it passes the configured Kafka API version to aiokafka and falls back to the transport subscription
  path when direct listener startup fails before publish.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1009"
    description: "Bind omnimarket PR #1009 head and changed Codex runtime adapter files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12551/dod-omnimarket-pr-1009/command.yaml"
          && grep -q '^pr_number: 1009$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12551/dod-omnimarket-pr-1009/command.yaml"
          && grep -q '^commit_sha: "a96b394b76260497205ee61afdb69017dbaefc20"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12551/dod-omnimarket-pr-1009/command.yaml"
  - id: "dod-local-validation"
    description: "Focused local validation for the Codex runtime adapter fix passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12551/dod-local-validation/command.yaml"
          && grep -q '5 passed, 51 deselected' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12551/dod-local-validation/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12551/dod-local-validation/command.yaml"
  - id: "dod-runtime-dispatch-proof"
    description: ".201 runtime proof shows deployed session_orchestrator dispatch returns a terminal complete
      event with the OMN-12551 and OMN-12523 source stack."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12551/dod-runtime-dispatch-proof/command.yaml"
          && grep -q 'ok: true' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12551/dod-runtime-dispatch-proof/command.yaml"
          && grep -q 'session_id: sess-20260531-1148' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12551/dod-runtime-dispatch-proof/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12551 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12551/dod-occ-pr-self/command.yaml"
