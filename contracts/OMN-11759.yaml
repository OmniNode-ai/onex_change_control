---
schema_version: "1.0.0"
ticket_id: "OMN-11759"
title: "Wire node_omnigate_projection with handler class"
summary: >-
  Bind central OCC evidence for omnimarket PR #791, which wires node_omnigate_projection
  through a runtime handler class. The PR adds HandlerOmniGateProjection, points
  contract.yaml at the class, adds handler_routing metadata, and covers reducer delegation
  with focused tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused omnimarket handler/reducer/autowiring tests pass locally for PR #791."
    command: "uv run pytest tests/nodes/node_omnigate_projection/test_handler_omnigate_projection.py tests/nodes/node_omnigate_projection/test_reducer_omnigate_projection.py tests/test_pr_lifecycle_contract_autowiring.py -q"
  - kind: "manual"
    description: "Deploy-gate assessment records that this runtime handler routing change is deploy-gated and needs normal post-merge runtime rollout."
    command: "deploy assessment: no live pre-merge deploy; validate via downstream deploy gate after OCC evidence is merged"
dod_evidence:
  - id: "dod-local-tests-pass"
    description: "Focused local tests passed for node_omnigate_projection handler routing."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11759/dod-local-tests-pass/command.yaml\"
          && grep -q '^commit_sha: \"0527fc4ba30c66e784822b2d17ddb3e516a323a8\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11759/dod-local-tests-pass/command.yaml\""
  - id: "dod-omnimarket-pr-791"
    description: "Bind OMN-11759 evidence to omnimarket PR #791 at the observed PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11759/dod-omnimarket-pr-791/command.yaml\"
          && grep -q '^pr_number: 791$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11759/dod-omnimarket-pr-791/command.yaml\"
          && grep -q '^commit_sha: \"0527fc4ba30c66e784822b2d17ddb3e516a323a8\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11759/dod-omnimarket-pr-791/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Record deploy-gate assessment for the runtime handler routing path."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11759/dod-deploy-assessment/command.yaml\"
          && grep -qi 'deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11759/dod-deploy-assessment/command.yaml\"
          && grep -q '^pr_number: 791$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11759/dod-deploy-assessment/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
