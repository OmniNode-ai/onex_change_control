---
schema_version: "1.0.0"
ticket_id: OMN-8161
title: Wire omniintelligence producer for pattern-scored.v1 topic (omnidash constant ha
summary: "Wire omniintelligence producer for pattern-scored.v1 topic (omnidash constant ha"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-open"
    description: "PR #659 opened against main on OmniNode-ai/omniintelligence"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state -q .state"
  - id: "dod-producer-wired"
    description: "pattern-scored.v1 producer live in node_pattern_feedback_effect Step 6c"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} --repo ${REPO} | grep -q 'pattern_scored'"
  - id: "dod-unit-tests"
    description: "71/71 unit tests pass, 0 new failures vs main"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_pattern_feedback_effect/ -v"
  - id: "dod-precommit"
    description: "All pre-commit hooks pass on PR head commit"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
