---
schema_version: "1.0.0"
ticket_id: OMN-8161
title: Wire omniintelligence producer for pattern-scored.v1 topic
summary: "Bind central OCC evidence for omniintelligence PR #659, which wires the pattern-scored.v1 producer
  in node_pattern_feedback_effect."
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-open"
    description: "PR #659 opened against main on OmniNode-ai/omniintelligence"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8161/dod-pr-open/command.yaml\"
          && grep -q '^pr_number: 659$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8161/dod-pr-open/command.yaml\""
  - id: "dod-producer-wired"
    description: "pattern-scored.v1 producer live in node_pattern_feedback_effect Step 6c"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8161/dod-producer-wired/command.yaml\"
          && grep -q 'pattern_scored' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8161/dod-producer-wired/command.yaml\""
  - id: "dod-unit-tests"
    description: "71/71 unit tests pass, 0 new failures vs main"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8161/dod-unit-tests/command.yaml\"
          && grep -q '71 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8161/dod-unit-tests/command.yaml\""
  - id: "dod-precommit"
    description: "All pre-commit hooks pass on PR head commit"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8161/dod-precommit/command.yaml\"
          && grep -q 'All checks passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8161/dod-precommit/command.yaml\""
  - id: "dod-occ-pr-1347"
    description: "Bind OMN-8161 central evidence to onex_change_control PR #1347."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8161/dod-occ-pr-1347/command.yaml\"
          && grep -q '^pr_number: 1347$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8161/dod-occ-pr-1347/command.yaml\""
