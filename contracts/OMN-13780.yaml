---
schema_version: "1.0.0"
ticket_id: "OMN-13780"
title: "docs(OMN-13780): disposition 5 no-node skill facades (M2c)"
summary: >-
  checkpoint/dispatch_watchdog/epic_team/executing_plans/wave_scheduler all declared no real backing node
  per skill_functional_audit. Each was individually root-caused in omniclaude PR #1830: checkpoint has
  real backing already (omnibase_infra node_checkpoint_effect / node_checkpoint_validate_compute / checkpoint_manager.py
  CLI) — the omniclaude orchestrator shell was a dead Polly-passthrough that never was the real dispatch
  path, so SKILL.md now declares the real backing. dispatch_watchdog is a genuine facade duplicating onex:agent_healthcheck
  (node-backed via node_worker_stall_recovery) and is de-listed pointing at agent_healthcheck. epic_team/executing_plans/wave_scheduler
  are genuine facades, de-listed from event-bus/node-routed dispatch by design (inherently Claude-session-native
  orchestration with no I/O boundary a node can call to spawn a live agent session). skill_functional_audit's
  Known Tracked Findings table is updated with all 5 dispositions and their per-skill follow-up tickets
  (OMN-13799/13800/13801/13802/13803) so the audit reports them as tracked, not silent facades. Docs-only
  diff (6 SKILL.md files); no runtime/contract/topic changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tracked-findings-updated"
    description: >-
      omniclaude PR #1830 (branch jonah/omn-13780-m2c-disposition-5-no-node-facades, commit 3050db5ce3dc3056770fecbaef47c498ad0952ab)
      updates plugins/onex/skills/skill_functional_audit/SKILL.md's "Known Tracked Findings" table with
      a verdict + tracking-ticket entry for all 5 skills (checkpoint, dispatch_watchdog, epic_team, executing_plans,
      wave_scheduler), and adds a "## Backing Status" section to each of the 5 skills' own SKILL.md self-documenting
      real backing status. Verified via `git show 3050db5ce3:plugins/onex/skills/skill_functional_audit/SKILL.md
      | grep -cE "OMN-137(99)|OMN-138(0[0-3])"` (5 matches — one per follow-up ticket cited).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13780/dod-tracked-findings-updated/command.yaml
  - id: "dod-local-gates-green"
    description: >-
      All local pre-commit + pre-push hooks pass clean on the omniclaude branch head (3050db5ce3dc3056770fecbaef47c498ad0952ab):
      mypy, pyright, bandit, ruff format/check, onex-validate (0 errors against latest dev), duplicate-model
      check, secret detection, and the reject-imperative- skill-patterns gate (fixed a wave_scheduler
      wording collision with the TeamCreate-pattern rule during development). `uv run pytest tests/ -m
      "not integration"` was run locally; the failure set that surfaced (test_handler_skill_requested.py,
      test_wiring_dispatchers.py, test_backend_vllm.py, test_backend_subprocess.py) reproduces identically
      on a clean dev checkout with no diff applied (confirmed by direct comparison), i.e. it is a pre-existing
      local-machine environment issue, not a regression from this docs-only change. CI's own merge_group
      full-suite run on the latest dev merge (run 28546888825) is green across all 5 Tests splits, which
      is the authoritative signal per VERIFICATION_DOCTRINE.md. Focused tests directly touching the changed
      skills all pass locally: test_mode_metadata_integrity.py, test_skill_orchestrator_shape.py, test_r_class_shim_enforcement.py,
      test_redeploy_shim.py, test_skill_mode_filtering.py, test_validate_instructional_skill_routing.py,
      test_agent_healthcheck.py, test_epic_team_stacked_branches.py, test_post_skill_delegation_enforcer.py.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13780/dod-local-gates-green/command.yaml
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13780 contract plus the dod-tracked-findings-updated
      and dod-local-gates-green receipts required by downstream Receipt Gate resolution on omniclaude
      PR #1830.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13780/dod-occ-self/command.yaml
