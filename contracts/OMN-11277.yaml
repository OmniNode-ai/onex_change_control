---
schema_version: "1.0.0"
ticket_id: "OMN-11277"
title: "Add runner health check cron to deploy script in omnibase_infra"
summary: >
  Adds idempotent local cron installation to deploy-runners.sh that runs cli_runner_health --emit --alert
  every 3 minutes, sources ~/.omnibase/.env for credentials, and logs to /tmp/runner-health.log. Implementation
  was already present in deploy-runners.sh on main (landed OMN-10445); this ticket ships unit test coverage
  verifying all acceptance criteria.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: 8 unit tests pass verifying cron schedule, flags, env sourcing, log path, idempotence,
      and wiring.
    command: uv run pytest tests/unit/observability/runner_health/test_deploy_runner_health_cron.py -v
  - kind: manual
    description: omnibase_infra PR  #1687 carries the test file.
    command: gh pr checks 1687 --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "8 unit tests pass verifying all OMN-11277 acceptance criteria."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11277/dod-unit-tests/command.yaml"
          && grep -q '^pr_number: 1687$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11277/dod-unit-tests/command.yaml"
          && grep -q '^verifier: "agent"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11277/dod-unit-tests/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11277 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11277/dod-occ-pr-self/command.yaml"
