---
schema_version: "1.0.0"
ticket_id: "OMN-14400"
title: "Autobind OCC evidence for OMN-14400"
summary: >
  OCC contract bound by node_pr_lifecycle_fix_effect Evidence-Source autobind (OMN-13317 F1) when OmniNode-ai/omnimarket
  PR #1721 carried a product-SHA Evidence-Source and failed the Receipt Gate. APPENDED (OMN-14409/OMN-14411):
  the autobind items above prove only that PR #1721 and its OCC companions exist, not that #1721's code
  is correct. dod-omnimarket-pr-1721-behavioral-proof-3ce48de9 below adds the missing behavioral proof:
  an independent adversarial verification (verify-14400, distinct from implementer fix-14398) of omnimarket
  PR #1721, RSD-1 of the OMN-14398 EvidenceCollector canonicalization epic. It is a behavior-identical
  carve-out of node_dod_verify's 4 gh-CLI evidence methods into a canonical EFFECT handler and does NOT
  complete the canonicalization: node_dod_verify's contract.yaml still declares node_archetype: compute
  / purity: pure while now hosting an EFFECT handler doing subprocess I/O — pre-existing on origin/dev,
  confirmed unmodified by #1721, out of scope for this RSD-1 carve and tracked as RSD-3 work under the
  OMN-14398 epic. This append is the second whole-file contract mutation for OMN-14400 (after PR #3981's
  deploy-assessment item); per OMN-14411, appending invalidates every prior receipt's contract_sha256
  binding, so this PR also carries fresh command.supersede.NNNN.yaml rebinds for all 4 pre-existing dod_evidence
  items' receipts (PR #3981 independently produced its own supersessions for the first 2, live evidence
  of the same rot recurring within minutes).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1721 CI checks green"
    command: "PR_NUMBER=1721 REPO=OmniNode-ai/omnimarket gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1721"
    description: "PR #1721 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=1721 REPO=OmniNode-ai/omnimarket gh pr view ${PR_NUMBER} --repo ${REPO}
          --json number,state"
  - id: "occ-self-bind-pr-3971"
    description: "OCC self-binding receipt for onex_change_control PR #3971."
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=3971 REPO=OmniNode-ai/onex_change_control gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
  - id: "dod-deploy-assessment-head-3ce48de9"
    description: >
      Deploy assessment for omnimarket PR #1721 at head 3ce48de9: the PR refactors dod_verify GitHub evidence
      I/O into an EFFECT handler and touches runtime handler/model paths, but this evidence repair performs
      no live deploy, runtime restart, docker exec, rpk topic produce, or production lane mutation. Rollout
      remains under the normal omnimarket merge/deploy pipeline.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14400/dod-deploy-assessment-head-3ce48de9/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14400/dod-deploy-assessment-head-3ce48de9/command.yaml
  - id: "occ-self-bind-pr-3981"
    description: "OCC self-binding receipt for onex_change_control PR #3981."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=3981 REPO=OmniNode-ai/onex_change_control gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1721-behavioral-proof-3ce48de9"
    description: >-
      Independent adversarial verification (verify-14400, not the implementer fix-14398) of omnimarket
      PR #1721 at head 3ce48de975156510735de66a87224d78551c752d. Line-by-line diff of the 4 carved gh-CLI
      methods against origin/dev proved behavior parity (same argv incl. --required, same timeouts, same
      fail-closed exception handling). Confirmed HandlerDodVerify's _make_collector seam and #1719/OMN-14392's
      hermetic test are untouched. Ran no-plugin-daemon-classes and no-noncanonical-lifecycle-classes
      directly against the PR's changed files in an isolated worktree (both Passed, zero allowlist additions
      in the diff), plus mypy --strict (0 errors) and ruff check (clean). Built two clean isolated worktrees
      (PR head vs. unmodified origin/dev) and independently re-ran the test suite: the one failing test
      (test_static_pass_receipt_no_longer_masks_unmerged_pr) fails identically on both (182 vs 168 passed;
      delta of exactly 14 matches the new handler test file), proving it is genuinely pre-existing (the
      known OMN-14392 live-PR-drift bug #1719 fixes) and not caused or worsened by this PR. This item
      supplements (does not replace) dod-OmniNode-ai-omnimarket-pr-1721: that item proves #1721 exists;
      this item proves what #1721's code actually does.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14400/dod-omnimarket-pr-1721-behavioral-proof-3ce48de9/command.yaml
          && grep -q '^pr_number: 1721$' drift/dod_receipts/OMN-14400/dod-omnimarket-pr-1721-behavioral-proof-3ce48de9/command.yaml
          && grep -q '3ce48de975156510735de66a87224d78551c752d' drift/dod_receipts/OMN-14400/dod-omnimarket-pr-1721-behavioral-proof-3ce48de9/command.yaml
  - id: "occ-self-bind-pr-3985"
    description: "OCC self-binding receipt for onex_change_control PR #3985."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PR_NUMBER=3985 REPO=OmniNode-ai/onex_change_control gh pr view ${PR_NUMBER} --repo ${REPO} --json
          number,state,headRefOid | grep -q '"number":3985' && PR_NUMBER=3985 REPO=OmniNode-ai/onex_change_control
          gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid | grep -q 'fdf6077efb372289fee886c898b6197acb1f96d8'
