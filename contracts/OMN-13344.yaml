---
schema_version: "1.0.0"
ticket_id: "OMN-13344"
title: "Stabilize omnimemory tests gate"
summary: >-
  Stabilizes the omnimemory tests gate by replacing brittle shell changed-file diff logic with a tested
  CI helper that uses GitHub PR merge parents when available, distinguishing selector failure from upstream
  prerequisite skips, and including central contract-compliance in CI Summary output. This central OCC
  evidence binds OMN-13344 to omnimemory PR #374 and records that the PR has no live deploy mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimemory-pr-374"
    description: >-
      Per-code-PR binding receipt for omnimemory PR #374 at the current head. The PR updates CI helper
      logic and tests for changed-file collection and test-path detection. The receipt binds by pr_number
      and commit_sha for downstream Receipt Gate resolution.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13344/dod-omnimemory-pr-374/command.yaml
  - id: "dod-deploy-no-live-mutation"
    description: >-
      Deploy-path evidence for OMN-13344. The PR changes CI scripts, workflow behavior, and tests only.
      It adds no runtime node, no Kafka topic, no contract, no migration, no image-build input, and performs
      no .201 mutation or live deploy.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13344/dod-deploy-no-live-mutation/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying OMN-13344 contract and receipts. Binds
      the OCC PR by pr_number so the OCC receipt-gate eligibility check can resolve a PASS receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13344/dod-occ-pr-self/command.yaml
