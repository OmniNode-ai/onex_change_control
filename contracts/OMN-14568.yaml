---
schema_version: "1.0.0"
ticket_id: "OMN-14568"
title: "Autobind OCC evidence for OMN-14568"
summary: >
  OCC contract bound by node_pr_lifecycle_fix_effect Evidence-Source autobind (OMN-13317 F1) when OmniNode-ai/omnimarket
  PR #1752 carried a product-SHA Evidence-Source or was missing an OCC contract, and failed the gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1752 required CI Summary check green"
    command: "PRODUCT_PR_NUMBER=1752; PRODUCT_REPO=OmniNode-ai/omnimarket; gh pr checks \"$PRODUCT_PR_NUMBER\"
      --repo \"$PRODUCT_REPO\" --json name,state --jq 'map(select(.name == \"CI Summary\" and .state ==
      \"SUCCESS\")) | length == 1' | grep -Fx true"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1752"
    description: "PR #1752 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1752; PRODUCT_REPO=OmniNode-ai/omnimarket; gh pr view \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1752-ci"
    description: "PR #1752 on OmniNode-ai/omnimarket — required CI Summary outcome (OMN-14425 substance
      check)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1752; PRODUCT_REPO=OmniNode-ai/omnimarket; gh pr checks \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json name,state --jq 'map(select(.name == \"CI Summary\" and .state
          == \"SUCCESS\")) | length == 1' | grep -Fx true"
  - id: "occ-self-bind-pr-4119"
    description: "OCC self-binding receipt for onex_change_control#4119."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
