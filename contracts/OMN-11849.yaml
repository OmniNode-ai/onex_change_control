---
schema_version: "1.0.0"
ticket_id: "OMN-11849"
title: "Negative Test Cases (5 SOW-required) for onex-api"
summary: >
  Adds 5 SOW-required negative test cases to docker/onex-api/tests/test_negative_cases.py covering AUTH_FAILURE
  (revoked key 401), AUTHZ_FAILURE (disabled tenant 403), CONTRACT_VALIDATION (invalid schema 422), CROSS_TENANT_ISOLATION
  (403), IDEMPOTENCY_COLLISION (one row), and REPLAY_DETERMINISM (BLOCKED/skip). Registers idempotency
  and replay pytest markers. Test-only change; no runtime code.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-negative-tests-pass"
    description: "All negative test cases pass (11 pass, 1 skip) in CI pytest run"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11849/dod-negative-tests-pass/command.yaml\""
  - id: "dod-no-deployable-artifact"
    description: "Test-only change: no runtime code, no migration, no k8s manifest modified"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11849/dod-no-deployable-artifact/command.yaml\""
