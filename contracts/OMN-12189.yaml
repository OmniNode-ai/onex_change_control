---
schema_version: "1.0.0"
ticket_id: "OMN-12189"
title: "Migrate three enums from compat to core"
summary: >
  Central OCC evidence for omnibase_core PR #1158. The change adds enum migration parity tests proving
  compat enum values match the canonical core enum definitions for NodeKind, ExecutionStatus, and MessageCategory.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Compat-to-core enum parity tests pass on the PR head."
    command: "See drift/dod_receipts/OMN-12189/dod-parity-tests/command.yaml"
  - kind: "tests"
    description: "Core enum regression tests pass on the PR head."
    command: "See drift/dod_receipts/OMN-12189/dod-core-enum-tests/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carries the central OMN-12189 contract and receipts."
    command: "See drift/dod_receipts/OMN-12189/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-parity-tests"
    description: "Compat enum parity tests passed on the omnibase_core PR #1158 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12189/dod-parity-tests/command.yaml
          && grep -q '7 passed' drift/dod_receipts/OMN-12189/dod-parity-tests/command.yaml"
  - id: "dod-core-enum-tests"
    description: "Core enum regression tests passed on the PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12189/dod-core-enum-tests/command.yaml
          && grep -q '139 passed' drift/dod_receipts/OMN-12189/dod-core-enum-tests/command.yaml"
  - id: "dod-occ-pr"
    description: "This onex_change_control PR carries the central OMN-12189 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12189/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: \"OMN-12189\"$' drift/dod_receipts/OMN-12189/dod-occ-pr/command.yaml"
