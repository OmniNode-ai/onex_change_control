---
schema_version: "1.0.0"
ticket_id: "OMN-12742"
title: "chore(demo): rebuild/redeploy dev/stability-test runtime off current dev HEAD (picks up node_agent_orchestrator
  PR#193 + omnimarket#1065)"
summary: >-
  Central evidence that dev/stability-test runtime is rebuilt/redeployed off current dev HEAD, picking
  up node_agent_orchestrator (PR #193, already on dev) and omnimarket#1065 (OMN-12738) reasoning-suppression
  once that merges. SEA orchestrator reachable on demo lane. ONEX_TRACK_A_API_KEY populated so Gemini
  orchestrates (cloud_fallback tier → gemini-2.5-flash). OMN-12700/12664/12721 cloud-tier 404 chain cleared
  confirmed via live .201 runtime state inspection. Hard gates: OMN-12738 must merge first; live runtime
  change requires explicit user approval; API keys require user-authorized credential action.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-deployed-image-digest"
    description: "Deployed image digest/SHA for the stability runtime proving it is built off current
      dev HEAD (post-PR#193 + post-omnimarket#1065). docker inspect or registry digest output."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-deployed-image-digest/command.yaml"
  - id: "dod-gemini-orchestration-proof"
    description: "Runtime-observed inference event showing Gemini orchestrating with non-$0.0000 cost.
      Correlation id + inference-response event log excerpt."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-gemini-orchestration-proof/command.yaml"
  - id: "dod-404-chain-cleared"
    description: "OMN-12700/12664/12721 cloud-tier 404 chain cleared post-deploy. A cloud-tier --agent
      run that no longer returns the two-URL 404 signature. Live .201 state inspection output."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-404-chain-cleared/command.yaml"
  - id: "dod-user-approval-receipt"
    description: "Explicit user approval receipt for live dev/stability-test runtime change (Operating
      Rule #2 hard gate). API key population (ONEX_TRACK_A_API_KEY) is a user-authorized credential action
      (plan Hard Gate #3)."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-user-approval-receipt/command.yaml"
  - id: "dod-occ-pr-evidence"
    description: "OCC PR carries the OMN-12742 central contract and PASS receipts."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-occ-pr-evidence/command.yaml"
