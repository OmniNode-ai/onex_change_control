---
schema_version: "1.0.0"
ticket_id: "OMN-12742"
title: "chore(OMN-12742): rebuild/redeploy dev/stability-test runtime off current dev HEAD"
summary: >-
  Central OCC evidence that the dev/stability-test runtime is rebuilt/redeployed off current dev HEAD
  so demo-visible model/provider fixes (OMN-12743) and the SEA/delegation orchestration path are exercised
  against the deployed image rather than source. Deploy evidence: deployed image digest off dev HEAD,
  a dev-lane bus inference request/response confirming Gemini orchestration with non-zero cost, and the
  OMN-12700/12664/12721 cloud-tier 404 chain cleared via live .201 state inspection. Hard gates: live
  dev/stability runtime change and credential population require explicit user approval (Operating Rule
  #2); runtime-observed proof is marked PENDING the operator-approved .201 redeploy and is not claimed
  as run.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-deploy-image-digest"
    description: "Deploy evidence: deployed image digest/SHA for the dev/stability runtime proving it
      is built off current dev HEAD. docker inspect / registry digest output. PENDING the operator-approved
      .201 redeploy."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-deploy-image-digest/command.yaml"
          && grep -q 'deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-deploy-image-digest/command.yaml"
          && grep -q 'docker exec' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-deploy-image-digest/command.yaml"
  - id: "dod-gemini-orchestration-proof"
    description: "Deploy-gate runtime evidence: a dev-lane bus inference request produced via rpk topic
      produce and the matching inference-response event consumed, showing Gemini orchestrating with a
      non-zero cost. PENDING the operator-approved .201 redeploy."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-gemini-orchestration-proof/command.yaml"
          && grep -q 'rpk topic produce' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-gemini-orchestration-proof/command.yaml"
  - id: "dod-user-approval-receipt"
    description: "Explicit user approval receipt for the live dev/stability-test runtime change and any
      credential population (Operating Rule #2 hard gate). PENDING operator approval."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-user-approval-receipt/command.yaml"
  - id: "dod-occ-pr-evidence"
    description: "onex_change_control PR carries the OMN-12742 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-occ-pr-evidence/command.yaml"
  - id: "dod-occ-main-pr-2254"
    description: "onex_change_control PR #2254 promotes the OMN-12742 contract and PASS receipts to main."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-occ-main-pr-2254/command.yaml"
          && grep -q '^pr_number: 2254$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12742/dod-occ-main-pr-2254/command.yaml"
