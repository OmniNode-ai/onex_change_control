---
schema_version: "1.0.0"
ticket_id: OMN-6375
title: 'Task 9: Add end-to-end injection regression suite [OMN-6366]'
summary: "Task 9: Add end-to-end injection regression suite [OMN-6366]"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}"
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json mergedAt -q .mergedAt --repo ${REPO}"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --watch --repo ${REPO}"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-005"
    description: "Golden chain E2E test passes"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/ -k golden -v"
