---
schema_version: "1.0.0"
ticket_id: "OMN-12760"
title: "Live projection API does not serve delegation correlation-trace topic"
summary: >-
  The OMN-12748 dashboard branch reads correlation-trace projection topic onex.snapshot.projection.delegation.correlation-trace.v1
  via projection-API. On .201 dev during OMN-12756 validation the projection-API at :3002 was healthy
  but did not advertise or serve that topic (404). Required: deploy/configure the projection API so it
  exposes the correlation-trace topic as a contract-declared read surface over public.delegation_events.
  Dashboard must continue reading the projection-API topic surface — no fallback to bespoke endpoint or
  raw SQL.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-projection-api-topic-exposed"
    description: >-
      GET :3002/projections includes onex.snapshot.projection.delegation.correlation-trace.v1 with status
      ok; GET :3002/projection/onex.snapshot.projection.delegation.correlation-trace.v1?correlation_id=<cid>
      returns rows from delegation_events for that correlation.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12760/dod-projection-api-topic-exposed/command.yaml
          && grep -q 'correlation-trace.v1' drift/dod_receipts/OMN-12760/dod-projection-api-topic-exposed/command.yaml
  - id: "dod-dashboard-renders-trace"
    description: >-
      Omnidash Correlation Trace panel renders prompt/response from the correlation-trace projection-API
      endpoint (not a bespoke route or raw SQL).
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12760/dod-dashboard-renders-trace/command.yaml
  - id: "dod-occ-evidence"
    description: "onex_change_control OCC contract + receipts for OMN-12760."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12760/dod-occ-evidence/command.yaml && grep
          -q '^ticket_id: "OMN-12760"$' contracts/OMN-12760.yaml
