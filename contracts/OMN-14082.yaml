---
schema_version: "1.0.0"
ticket_id: "OMN-14082"
title: "omnibase_core: fail closed when validator gates scan zero files"
summary: >-
  Fixes three omnibase_core validator gates that silently passed after scanning
  zero files when invoked with --report by separating flags from positional
  paths, restoring the intended src/ plus scripts/ self-scan, and making empty
  scan scope fatal.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omnibase_core validator regression suite passes for OMN-14082."
    command: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14082/dod-validator-tests/command.yaml\""
  - kind: "ci"
    description: "Ruff passes for the changed validator scripts and regression tests."
    command: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14082/dod-ruff-check/command.yaml\""
  - kind: "manual"
    description: "omnibase_core PR #1394 carries the OMN-14082 validator gate fix."
    command: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14082/dod-omnibase-core-pr-1394/command.yaml\""
  - kind: "manual"
    description: "onex_change_control PR carries the central OMN-14082 contract and PASS receipts."
    command: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14082/dod-occ-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-validator-tests"
    description: "omnibase_core validator regression suite passes for OMN-14082."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14082/dod-validator-tests/command.yaml"
          && grep -q '^commit_sha: "9adf54ca2376e596fdcef7f99bd56574e04b614b"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14082/dod-validator-tests/command.yaml"
  - id: "dod-ruff-check"
    description: "Ruff passes for the changed validator scripts and regression tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14082/dod-ruff-check/command.yaml"
          && grep -q '^commit_sha: "9adf54ca2376e596fdcef7f99bd56574e04b614b"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14082/dod-ruff-check/command.yaml"
  - id: "dod-omnibase-core-pr-1394"
    description: "omnibase_core PR #1394 carries the OMN-14082 validator gate fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14082/dod-omnibase-core-pr-1394/command.yaml"
          && grep -q '^pr_number: 1394$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14082/dod-omnibase-core-pr-1394/command.yaml"
          && grep -q '9adf54ca2376e596fdcef7f99bd56574e04b614b' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14082/dod-omnibase-core-pr-1394/command.yaml"
  - id: "dod-occ-self"
    description: "onex_change_control PR carries the central OMN-14082 contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14082/dod-occ-self/command.yaml"
