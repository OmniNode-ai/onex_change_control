---
schema_version: "1.0.0"
ticket_id: "OMN-11413"
title: "Add merge_group trigger to self-extending agent CI"
summary: >-
  onex-self-extending-agent PR #75 adds the merge_group trigger to the main CI workflow so lint, typecheck,
  tests, and security checks run during merge queue promotion.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: tests
    description: "Focused unit tests pass after rebasing the workflow trigger change."
    command: "uv run pytest tests/unit/test_chain_visualizer.py tests/unit/test_agent_demo.py -q"
  - kind: manual
    description: "GitHub Actions workflow parses as YAML after adding merge_group."
    command: "uv run python -c \"import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('workflow
      yaml ok')\""
  - kind: manual
    description: "No live deploy required; CI trigger wiring only."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11413/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-75"
    description: "onex-self-extending-agent PR #75 carries the rebased merge_group CI trigger change."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11413/dod-product-pr-75/command.yaml"
          && grep -q '^pr_number: 75$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11413/dod-product-pr-75/command.yaml"
          && grep -q '^commit_sha: "c8143e5f21b927c9d898353acc902ab4ab17ede4"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11413/dod-product-pr-75/command.yaml"
  - id: "dod-local-proof"
    description: "Focused local tests and YAML parse proof pass after the rebase."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11413/dod-local-proof/command.yaml"
          && grep -q '11 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11413/dod-local-proof/command.yaml"
          && grep -q 'workflow yaml ok' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11413/dod-local-proof/command.yaml"
  - id: "dod-deploy"
    description: "No live deploy required; this is CI workflow trigger wiring."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11413/dod-deploy/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11413 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11413/dod-occ-self/command.yaml"
          && grep -q '^pr_number: 1291$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11413/dod-occ-self/command.yaml"
