---
schema_version: "1.0.0"
ticket_id: "OMN-11781"
title: "feat: add swarm decomposition models, enums, and decomposer (SEA Phase 1.1)"
summary: >
  Adds src/swarm/models.py with all enums (EnumSwarmCapability, EnumSubtaskCategory, EnumDecompositionStatus,
  EnumSubtaskStatus, EnumSwarmRunStatus, EnumEndpointStatus, EnumModelStatus) and frozen Pydantic models
  (ModelSubtask, ModelDecomposition) with deterministic subtask_id generation via sha256. Adds src/swarm/decomposer.py
  with TaskDecomposer wrapping GenerationConsumer. Includes ModelContextBundle for LLM call observability
  (prompt hashes, ledger hits, error counts). 23+ unit tests. Also resolves merge conflicts from daisy-chained
  PRs #119-#125 built on this branch.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-117"
    description: "onex-self-extending-agent PR #117 implements swarm decomposition models with 23+ unit
      tests, passes mypy --strict and full test suite"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-11781/dod-sea-pr-117/command.yaml\"
          && grep -q '^pr_number: 117$' \"drift/dod_receipts/OMN-11781/dod-sea-pr-117/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR binds this contract to onex_change_control as the evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-11781/dod-occ-pr/command.yaml\""
