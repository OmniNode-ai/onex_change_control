---
schema_version: "1.0.0"
ticket_id: "OMN-12696"
title: "Workstream J / Gate 5: block agent-surfaced bypass tokens"
summary: >-
  Adds Stop/SubagentStop blocking for unauthorized bracketed bypass-token surfaces and extends committed
  session-evidence scanning so agents cannot propose gate-bypass tokens without an approved receipt.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-stop-subagent-hook-block"
    description: "Stop and SubagentStop hooks block unauthorized agent-surfaced bypass-token output."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12696/dod-stop-subagent-hook-block/command.yaml"
          && grep -q '^pr_number: 1733$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12696/dod-stop-subagent-hook-block/command.yaml"
  - id: "dod-subagent-verifier-final-message"
    description: "SubagentStop claim verifier rejects a final assistant message that surfaces an unauthorized
      bypass token even when a json-report exists."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12696/dod-subagent-verifier-final-message/command.yaml"
          && grep -q 'unauthorized_skip_token_surface' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12696/dod-subagent-verifier-final-message/command.yaml"
  - id: "dod-session-evidence-surface"
    description: "Local and reusable workflow scanners include committed .onex_state/evidence text surfaces."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12696/dod-session-evidence-surface/command.yaml"
          && grep -q '.onex_state/evidence' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12696/dod-session-evidence-surface/command.yaml"
  - id: "dod-focused-tests-and-lint"
    description: "Focused tests, ruff, mypy, workflow syntax, shell syntax, and changed-file pre-commit
      pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12696/dod-focused-tests-and-lint/command.yaml"
          && grep -q '181 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12696/dod-focused-tests-and-lint/command.yaml"
  - id: "dod-no-live-runtime-mutation"
    description: "Gate 5 implementation changes hooks, tests, and CI only; no live deploy, restart, or
      .201 mutation was performed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12696/dod-no-live-runtime-mutation/command.yaml"
          && grep -q 'no live deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12696/dod-no-live-runtime-mutation/command.yaml"
  - id: "dod-occ-pr"
    description: "Central OCC contract and receipts are carried by onex_change_control PR #2184."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12696/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2184$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12696/dod-occ-pr/command.yaml"
