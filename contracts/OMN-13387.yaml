---
schema_version: "1.0.0"
ticket_id: "OMN-13387"
title: "ModelReviewPacket + ModelOmniStudioEvidenceBundle dashboard models"
summary: >-
  Add two frozen Pydantic models under omnibase_core models/dashboard: ModelReviewPacket (source_contract_hash,
  ir_hash, patch_hash, validation_pipeline_version, typed receipt_gate_result, deterministic compute_packet_hash)
  and ModelOmniStudioEvidenceBundle (order-independent compute_bundle_fingerprint). Both registered in
  scripts/emit_ts_types.py so the omnidash-v2 TS drift gate mirrors them.
is_seam_ticket: false
interface_change: true
interfaces_touched: ["public_api"]
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-review-packet-pr-1293"
    description: >-
      Feature PR #1293 adds the two frozen models, exports them from models/dashboard/__init__.py, registers
      them in scripts/emit_ts_types.py, and proves determinism via 25 passing unit tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13387/dod-review-packet-pr-1293/command.yaml"
          && grep -q '^ticket_id: "OMN-13387"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13387/dod-review-packet-pr-1293/command.yaml"
          && grep -q '^pr_number: 1293$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13387/dod-review-packet-pr-1293/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR carrying the OMN-13387 contract + receipts used by the feature
      PR Receipt Gate via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13387/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-13387"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13387/dod-occ-pr/command.yaml"
