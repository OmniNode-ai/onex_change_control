---
schema_version: "1.0.0"
ticket_id: "OMN-15916"
title: "OCC companion for OMN-15916 tenant-creation schema-drift fix"
summary: >
  OCC contract for OmniNode-ai/omninode_infra PR #876 — insert_tenant_row's INSERT...RETURNING referenced
  nonexistent tenants.status/tenants.created_at columns, causing UndefinedColumn 500s on POST /v1/tenants
  and POST /v1/tenants/bootstrap.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #876 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-876"
    description: "PR #876 on OmniNode-ai/omninode_infra — product diff scope (files endpoint, OMN-14425/OMN-15309
      admissible form)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omninode_infra/pulls/876/files --jq '.[].filename'"
  - id: "dod-OmniNode-ai-omninode_infra-pr-876-ci"
    description: "PR #876 on OmniNode-ai/omninode_infra — content-bound proof that the created_at migration
      exists at the fix commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omninode_infra/contents/db/migrations/20260812_tenants_created_at_column.sql?ref=ad046691559fb2ee986ff94f6f45d6148c59bc48'
          --jq '.content' | base64 -d | grep -c 'ADD COLUMN IF NOT EXISTS created_at'"
  - id: "occ-self-bind-pr-PENDING"
    description: "Binds OMN-15916 to OCC companion PR (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
