---
schema_version: "1.0.0"
ticket_id: "OMN-14291"
title: "refactor(OMN-14291): retire models→nodes import-layering back-edge in omnibase_core"
summary: >-
  omnibase_core PR #1419 retires the frozen `models.** -> nodes.**` import-layering back-edge by physically
  relocating four misplaced classes that were never domain models — they subclass Node* base classes plus
  mixins — from models/services/ to nodes/: model_service_compute.py -> node_service_compute.py (ModelServiceCompute,
  subclasses NodeCompute), model_service_reducer.py -> node_service_reducer.py (subclasses NodeReducer),
  model_service_effect.py -> node_service_effect.py (subclasses NodeEffect), and model_service_orchestrator.py
  -> node_service_orchestrator.py (subclasses NodeOrchestrator). It updates the sole src consumer (infrastructure/infra_bases.py),
  ~14 test files, two docs files, and drops the now-unmatched .importlinter ignore line. Verified by the
  import-linter oracle: the core-models-no-upward contract stays KEPT with the allowlist entry removed,
  proving the edge is genuinely gone.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1419"
    description: >-
      Independent verification (a separate verifier, NOT the implementing agent) reproduced the import-linter
      oracle end-to-end against omnibase_core PR #1419 head fb507105042cd0491ee4e256c792bd6ed7943fb1 in
      an isolated worktree with PYTHONPATH cleared (grimp analyzed the worktree src, not the canonical
      clone). lint-imports kept all 4 contracts (Analyzed 4073 files, 10334 dependencies; 4 kept, 0 broken)
      with the models->nodes ignore line removed. Negative control: adding a throwaway models/_negative_control_zzz.py
      importing omnibase_core.nodes.node_compute.NodeCompute (ignore line still removed) turned core-models-no-upward
      BROKEN (3 kept, 1 broken: models._negative_control_zzz -> nodes.node_compute l.1; Analyzed 4074
      files, 10335 dependencies), proving the edge is real and genuinely removed by the relocation, not
      wildcard-masked; deleting the scratch file restored 4 kept, 0 broken (4073 files, 10334 dependencies).
      grep proof: no omnibase_core.nodes import remains under src/omnibase_core/models/. mypy clean (4074
      source files); 368 collected / 367 passed, 1 skipped in the affected node_services + infra_bases
      test scope; ruff format --check and ruff check clean on all 5 touched source files; no runtime or
      deploy scope.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14291/dod-omnibase_core-pr-1419/command.yaml
          && grep -q '^pr_number: 1419$' drift/dod_receipts/OMN-14291/dod-omnibase_core-pr-1419/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the OMN-14291 contract and PASS
      receipt for omnibase_core PR #1419, binding itself to occ-preflight by commit_sha (contract commit
      C1), with no runtime or deploy scope.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14291/dod-occ-self/command.yaml
