---
schema_version: "1.0.0"
ticket_id: "OMN-18770"
title: "C3 data: error-fingerprint exposure — a ranked, bus-backed runtime-error surface"
summary: >
  OCC contract for OmniNode-ai/omnimarket PR #2664 (C3 of epic OMN-18767, the lab observability tab).
  Adds node_projection_runtime_error_fingerprints, a contract-first reducer over onex.evt.omnibase-infra.runtime-error.v1
  that DERIVES the error category from the event's own evidence instead of copying the producer's stamp,
  re-derives the fingerprint over the derived category, accumulates occurrence_count atomically in SQL,
  and publishes fingerprint-keyed snapshot deltas to onex.snapshot.projection.runtime-error-fingerprints.v1
  with correlation_id as a first-class column. The surface it replaces held 69 rows on the .201 dev lane
  with 100% error_category unknown, because the only classifier in the pipeline was an eight-entry logger-NAME
  prefix map evaluated by the producer and no emitting logger name matched a prefix in it.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2664 CI checks green"
    command: "gh pr checks 2664 --repo OmniNode-ai/omnimarket"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2664 on OmniNode-ai/omnimarket: the AC-pinned unit
      suite and the golden chain the PR adds. Executed at cwd
      `${OMNI_HOME}/omnimarket`.
    command: "uv run pytest tests/test_omn18770_runtime_error_fingerprints.py tests/test_golden_chain_projection_runtime_error_fingerprints.py
      -q"
  - kind: "manual"
    description: |-
      Lab readback quoted in the PR body with the host named (doctrine rule 24).
      On the .201 dev lane (compose project omnibase-infra): max(last_seen_at)
      on omnidash_analytics.omninode_internal.runtime_error_fingerprints
      advanced from 2026-09-10 05:15:03 to 2026-09-19 00:03:16 across one
      further runtime-error occurrence; the unknown share of the category
      census fell from 100% to 75.7%; and a fingerprint-keyed snapshot delta
      was published to the lane broker and consumed back at high-watermark 1.
    command: "gh pr view 2664 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -c '192.168.86.201'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-deploy"
    description: >-
      Deploy probe for PR #2664 on OmniNode-ai/omnimarket. Hermetic content-addressed form (OMN-14505
      / OCC OMN-14051 guard): it reads the pinned head's own tree from the forge, so its exit status depends
      on whether the landed code carries the mechanism, and it cannot be satisfied by any receipt or contract
      this PR authors. It greps for the tier-3 classifier table, which is the symbol that distinguishes
      this change from a copy of the producer's logger-prefix map — the flat ordering without it categorised
      an aiokafka.producer failure as kafka_consumer.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_runtime_error_fingerprints/handlers/handler_projection_runtime_error_fingerprints.py?ref=4a229c1c0fcf86b894d15ef92c8ce3660704acdc --jq '.content' | base64 -d | grep -c '_UNSIDED_EXCEPTION_RULES'
  - id: "dod-deploy-bus-backed"
    description: >-
      Second half of the deploy probe, split into its own item because a dod_evidence item carries exactly
      one receipt file per check_type and two "command" checks under one id collide on that path — which
      is why the first attempt's preflight reported OMN-18770:dod-deploy:command twice and could never
      be satisfied. It pins bus_backed: true in the landed node contract: the exposure is refused as not_yet_bus_backed
      unless its producing writer ships with it, which is the OMN-15864 requirement this change has to
      meet on the first commit rather than in a follow-up.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_runtime_error_fingerprints/contract.yaml?ref=4a229c1c0fcf86b894d15ef92c8ce3660704acdc --jq '.content' | base64 -d | grep -c 'bus_backed: true'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: >-
      PR #2664 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434). The AC-pinned suite
      and the golden chain, both added by this PR, both RED before the mechanism.
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/test_omn18770_runtime_error_fingerprints.py tests/test_golden_chain_projection_runtime_error_fingerprints.py
          -q"
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "dod-vendored-migration-parity"
    description: >-
      The node-owned migration is vendored into omnibase_infra, which omnimarket's vendor-parity gate
      requires BEFORE the product PR can pass — the ordering that stops the node-migration-sync drift
      class (OMN-14975 and six prior tickets). omnibase_infra PR #3795.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3795 --repo OmniNode-ai/omnibase_infra --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2664"
    description: "PR #2664 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_runtime_error_fingerprints/__init__.py?ref=71a7792390a71a883d8b6cc45bd4617b9a2a6fcd --jq '.content' | base64 -d | grep -c 'class NodeProjectionRuntimeErrorFingerprints'
    binds_ac:
      - "AC1"
      - "AC2"
      - "AC3"
      - "AC4"
      - "AC5"
      - "AC6"
    ac_bindings:
      - label: "AC1"
        criterion_hash: "681ee5869ff3f7d261ab73ead5e38e36fb8bad0d56a64f5d3030026155b02c41"
        proposed_by: "occ-autobind"
        accepted_by: "7a850ce1-f95e-431f-b4e3-62f7449f04c0"
        accepted_at: "2026-09-18T23:15:59Z"
      - label: "AC2"
        criterion_hash: "4b1a657f1f6e41e4887c42448d1672cdb4812f7107566d680634fe22eb674efc"
        proposed_by: "occ-autobind"
        accepted_by: "7a850ce1-f95e-431f-b4e3-62f7449f04c0"
        accepted_at: "2026-09-18T23:15:59Z"
      - label: "AC3"
        criterion_hash: "d091645c584ec5b66836942a6f754c5eb7cc68f807dbba7d4baadc5ee23b646b"
        proposed_by: "occ-autobind"
        accepted_by: "7a850ce1-f95e-431f-b4e3-62f7449f04c0"
        accepted_at: "2026-09-18T23:15:59Z"
      - label: "AC4"
        criterion_hash: "8ea717c7e37f6317ca3b1baf023f616a1c5b0a47f892bd0059ea8f6d12798c59"
        proposed_by: "occ-autobind"
        accepted_by: "7a850ce1-f95e-431f-b4e3-62f7449f04c0"
        accepted_at: "2026-09-18T23:15:59Z"
      - label: "AC5"
        criterion_hash: "6f8c1301460f99814fd691fb197db48bfdf23ec1140d51db21d4bc18631466f5"
        proposed_by: "occ-autobind"
        accepted_by: "7a850ce1-f95e-431f-b4e3-62f7449f04c0"
        accepted_at: "2026-09-18T23:15:59Z"
      - label: "AC6"
        criterion_hash: "58a55234df18be66068114fd43381dc01418c278ed48aedbae3b2fb417343706"
        proposed_by: "occ-autobind"
        accepted_by: "7a850ce1-f95e-431f-b4e3-62f7449f04c0"
        accepted_at: "2026-09-18T23:15:59Z"
  - id: "dod-OmniNode-ai-omnimarket-pr-2664-ci"
    description: "PR #2664 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2664 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-10400"
    description: "OCC companion PR #10400 — self-bind for OMN-18770 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 10400 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3795"
    description: >-
      PR #3795 on OmniNode-ai/omnibase_infra — the vendored half, bound by hand. occ-autobind DECLINED
      this PR with skip:NO_RED_DERIVABLE_CHECK (OMN-15247): its whole diff is .sql plus a ledger .tsv
      row, and the autobind's candidate patterns are source-symbol greps, so it correctly refused to emit
      a PR-existence probe that could never go RED. The probe below is content-addressed and IS RED-derivable,
      which is the property the autobind was protecting: it greps the vendored grant migration for the
      sequence GRANT — the half whose absence caused OMN-17379 — and the file does not exist at the merge
      base at all.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_runtime_error_fingerprints/0001_grant_omninode_runtime_runtime_error_fingerprints.sql?ref=5f7b7648f3a523c2696b4a9ec9ef1ec1f80117ac --jq '.content' | base64 -d | grep -c 'GRANT USAGE ON SEQUENCE omninode_internal.runtime_error_fingerprints_projection_cursor_seq'
