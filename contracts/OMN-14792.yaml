---
schema_version: 1.0.0
ticket_id: OMN-14792
title: Scope done-flip guard PR scan and accept deploy-readback proof
summary: Central OCC evidence for omniclaude#1913. The product PR adds a deploy-readback durable-evidence
  path for runtime-deploy tickets, scopes implementing PR scans to relevant non-scratch references, and
  preserves fail-closed behavior for open implementing PRs or empty markers. Exact product head 9740b70547abe13bc1fce4cabbac4029492d3dce.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #1913 product head is exact for OMN-14792 evidence.'
    command: 'PRODUCT_PR_NUMBER=1913; PRODUCT_REPO=OmniNode-ai/omniclaude; PRODUCT_HEAD=9740b70547abe13bc1fce4cabbac4029492d3dce;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omniclaude-pr-1913
    description: 'PR #1913 on OmniNode-ai/omniclaude exists and is bound to OMN-14792 at exact product
      head 9740b70547abe13bc1fce4cabbac4029492d3dce.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1913; PRODUCT_REPO=OmniNode-ai/omniclaude; PRODUCT_HEAD=9740b70547abe13bc1fce4cabbac4029492d3dce;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omniclaude-pr-1913-diff
    description: 'PR #1913 changes the done-flip guard, Linear verification helper, and focused hook regression
      tests for deploy-readback proof handling.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1913; PRODUCT_REPO=OmniNode-ai/omniclaude; : ${PR_NUMBER}; gh
          pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "plugins/onex/hooks/lib/done_flip_guard.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "plugins/onex/hooks/lib/linear_done_verify.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "tests/hooks/test_done_flip_guard.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "tests/hooks/test_linear_done_verify.py"'
  - id: dod-omniclaude-pr-1913-local-verification
    description: Product PR body records focused hook tests, full hook-suite caveat, ruff, mypy, changed-file
      pre-commit, pre-push, and live done-flip guard proof for OMN-14792.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1913; PRODUCT_REPO=OmniNode-ai/omniclaude; : ${PR_NUMBER}; gh
          pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "93 passed"
          && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "3385
          passed, 25 skipped, 1 failed" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body
          --jq .body | grep -q "pre_tool_use_done_flip_guard.sh"'
  - id: dod-omniclaude-pr-1913-deploy
    description: Deploy-readiness assessment for OMN-14792. The PR changes hook library code and tests
      only; no live deploy, restart, .201, Kafka, database, or production mutation was performed by this
      OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14792/dod-omniclaude-pr-1913-deploy/command.yaml
          && grep -qi ''no live deploy, restart, .201, Kafka, database, or production mutation'' drift/dod_receipts/OMN-14792/dod-omniclaude-pr-1913-deploy/command.yaml'
  - id: occ-self-bind-pr-4427
    description: 'OCC PR #4427 on OmniNode-ai/onex_change_control self-binding proof for OMN-14792 evidence.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
