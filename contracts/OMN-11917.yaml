---
schema_version: "1.0.0"
ticket_id: "OMN-11917"
title: "Contract-Driven LLM Ecosystem Update"
summary: >
  Central OCC evidence for the LLM ecosystem migration. The epic moves model behavior away from hardcoded
  served-model defaults and into declared topology, overlays, registry-backed routing policy, runtime
  events, and durable proof.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-inventory-evidence"
    description: "Inventory evidence bundle exists for the ecosystem LLM hardcoding scan."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11917/dod-inventory-evidence/command.yaml
          && grep -q 'inventory-2026-05-24' drift/dod_receipts/OMN-11917/dod-inventory-evidence/command.yaml"
  - id: "dod-omniclaude-pr-1646"
    description: "Omniclaude PR #1646 carries the OMN-11937 runtime/config migration under this parent
      epic."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11917/dod-omniclaude-pr-1646/command.yaml
          && grep -q '^pr_number: 1646$' drift/dod_receipts/OMN-11917/dod-omniclaude-pr-1646/command.yaml"
  - id: "dod-deploy"
    description: "Deploy-gate proof surface is recorded for runtime-touching Omniclaude routing/config
      changes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11917/dod-deploy/command.yaml &&
          grep -q 'deploy verification' drift/dod_receipts/OMN-11917/dod-deploy/command.yaml"
