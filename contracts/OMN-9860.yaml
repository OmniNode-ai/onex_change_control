---
schema_version: 1.0.0
ticket_id: OMN-9860
summary: Add module adjacency YAML config (scripts/ci/test_selection_adjacency.yaml)
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'omnibase_core PR #925 CI checks pass (canonical implementation lives in omnibase_core)'
    command: gh pr checks 925 --repo OmniNode-ai/omnibase_core --watch
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "Adjacency YAML parses with schema_version=1, all 38 src/omnibase_core subdirs have adjacency
      entries, shared_modules is non-empty subset of adjacency keys — receipt at drift/dod_receipts/OMN-9860/dod-001/"
    source: generated
    checks:
      - check_type: command
        check_value: uv run python -c "import yaml; data = yaml.safe_load(open('scripts/ci/test_selection_adjacency.yaml'));
          assert data.get('schema_version') == 1; assert len(data.get('adjacency', {})) == 38; assert
          set(data.get('shared_modules', [])).issubset(data.get('adjacency', {}).keys()); print('adjacency
          ok')"
