---
schema_version: "1.0.0"
ticket_id: "OMN-604"
title: "FSM state mutation protection"
summary: >
  Binds omnibase_core PR #1309, which ensures MixinFSMExecution mutates _fsm_state only after successful
  FSM transitions and deep-copies committed context snapshots so caller-side dictionary mutation cannot
  corrupt committed state.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused FSM state mutation tests pass."
    command: "uv run pytest tests/unit/mixins/test_mixin_fsm_execution.py -q"
  - kind: "ci"
    description: "PR branch CI and receipt/deploy gates pass before merge."
    command: "gh pr checks 1309 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1309"
    description: "omnibase_core PR #1309 carries the FSM mutation-protection implementation and tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid -q .headRefOid"
  - id: "dod-deploy-no-runtime-impact"
    description: "Library mixin and unit-test change only; no live deploy, broker mutation, or runtime
      restart required before merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy-gate: OMN-604 changes omnibase_core mixin source and tests only; no
          live deploy required before merge'"
