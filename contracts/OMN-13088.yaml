---
schema_version: "1.0.0"
ticket_id: "OMN-13088"
title: "feat(omnimarket): projection contract for delegation.inference-response-text (NC-15)"
summary: >-
  omnimarket PR creates node_projection_delegation_inference_response, a new reducer that closes the NC-15
  coverage gap where omnidash declared onex.snapshot.projection.delegation.inference-response-text.v1
  but no contract served this snapshot topic. Verified that node_projection_delegation and node_llm_delegation_projection
  publish_topics do not include the target topic before creating the new reducer.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnimarket PR CI checks pass"
    command: "env -u GITHUB_TOKEN gh pr checks --repo OmniNode-ai/omnimarket --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-gap-verified"
    description: >-
      Verified that node_projection_delegation publish_topics do not include onex.snapshot.projection.delegation.inference-response-text.v1
      and that node_llm_delegation_projection only publishes to onex.evt.omnimarket.delegation-projection-snapshot.v1.
      Gap is real; new reducer required.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13088/dod-gap-verified/command.yaml
  - id: "dod-omnimarket-pr"
    description: >-
      omnimarket PR creates node_projection_delegation_inference_response with contract.yaml (reducer),
      handler, migration, models, and 5 unit tests (RED->GREEN). All pre-commit hooks pass, mypy --strict
      clean, 940 unit tests pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13088/dod-omnimarket-pr/command.yaml && grep
          -q '^evidence_source_sha:' drift/dod_receipts/OMN-13088/dod-omnimarket-pr/command.yaml
  - id: "dod-occ-self"
    description: "Self-binding OCC eligibility receipt for this OCC PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13088/dod-occ-self/command.yaml"
