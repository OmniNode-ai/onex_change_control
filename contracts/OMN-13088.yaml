---
schema_version: "1.0.0"
ticket_id: "OMN-13088"
title: "feat(omnimarket): projection contract for delegation.inference-response-text (NC-15)"
summary: >-
  omnimarket PR creates node_projection_delegation_inference_response, a new reducer that closes the NC-15
  coverage gap where omnidash declared onex.snapshot.projection.delegation.inference-response-text.v1
  but no contract served this snapshot topic. Verified that node_projection_delegation and node_llm_delegation_projection
  publish_topics do not include the target topic before creating the new reducer.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnimarket PR CI checks pass"
    command: "env -u GITHUB_TOKEN gh pr checks --repo OmniNode-ai/omnimarket --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-gap-verified"
    description: >-
      Verified that node_projection_delegation publish_topics do not include onex.snapshot.projection.delegation.inference-response-text.v1
      and that node_llm_delegation_projection only publishes to onex.evt.omnimarket.delegation-projection-snapshot.v1.
      Gap is real; new reducer required.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13088/dod-gap-verified/command.yaml
  - id: "dod-omnimarket-pr"
    description: >-
      omnimarket PR creates node_projection_delegation_inference_response with contract.yaml (reducer),
      handler, migration, models, and 5 golden-chain unit tests. The PR was rebuilt on current omnimarket
      dev at f9f4dc3b58d230b1da5eefb18db41e02d4f9f5db; focused projection tests, dogfood inventory, mypy
      --strict, and pre-commit all-files pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13088/dod-omnimarket-pr/command.yaml && grep
          -q '^commit_sha: "f9f4dc3b58d230b1da5eefb18db41e02d4f9f5db"$' drift/dod_receipts/OMN-13088/dod-omnimarket-pr/command.yaml
  - id: "dod-occ-self"
    description: "Self-binding OCC eligibility receipt for this OCC PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13088/dod-occ-self/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Static deploy assessment for the rebuilt omnimarket PR: this runtime-path change adds a new node-owned
      projection reducer contract, handler, and migration. No live deploy, runtime restart, Kafka mutation,
      database mutation, .201 mutation, or cluster mutation was performed during PR repair. Runtime rollout
      follows the standard post-merge omnimarket deploy path, which must apply the node-owned migration
      and start the reducer through normal runtime ownership.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13088/dod-deploy-assessment/command.yaml &&
          grep -q 'No live deploy' drift/dod_receipts/OMN-13088/dod-deploy-assessment/command.yaml
