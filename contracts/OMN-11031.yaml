---
schema_version: "1.0.0"
ticket_id: "OMN-11031"
title: "Dependency health sweep foundation (Tasks 1-3)"
summary: >
  Binds omnimarket PR #631, which introduces the node_dependency_health_sweep EFFECT node foundation:
  contract.yaml declaring subscribe topic onex.cmd.omnimarket.dep-health-sweep-start.v1 and publish topics
  onex.evt.omnimarket.dep-health-finding.v1 and onex.evt.omnimarket.dep-health-sweep-completed.v1; six
  Pydantic models covering request, finding, result, graph types, and sweep-completed event (all frozen=True,
  extra=forbid, PEP 604 unions); and HandlerDepHealthSweep skeleton with __main__.py CLI. Runtime sweep
  passes 174/174 entry points. 15 TDD tests pass; mypy --strict clean on 10 source files.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #631 carries the OMN-11031 dep-health-sweep foundation."
    command: "gh pr view 631 --repo OmniNode-ai/omnimarket --json state,headRefOid"
  - kind: "tests"
    description: "15 TDD tests cover all new models and handler skeleton."
    command: "uv run pytest tests/nodes/test_dep_health_models.py tests/nodes/test_dep_health_handler_stub.py
      -q"
  - kind: "manual"
    description: "OCC receipts bind OMN-11031 to omnimarket PR #631 and evidence."
    command: "find drift/dod_receipts/OMN-11031 -type f -name command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-631"
    description: "omnimarket PR #631 carries the OMN-11031 dep-health-sweep foundation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11031/dod-omnimarket-pr-631/command.yaml
          && grep -q '^pr_number: 631$' drift/dod_receipts/OMN-11031/dod-omnimarket-pr-631/command.yaml
          && grep -q '^commit_sha: 189d029babdf46b88e032e0b5b74e9545d6606b6$' drift/dod_receipts/OMN-11031/dod-omnimarket-pr-631/command.yaml"
  - id: "dod-focused-tests"
    description: "15 TDD model and handler tests pass (11 model + 4 handler stub)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11031/dod-focused-tests/command.yaml
          && grep -q '15 passed' drift/dod_receipts/OMN-11031/dod-focused-tests/command.yaml"
  - id: "dod-deploy"
    description: >
      Deploy DoD evidence for PR #631: new node skeleton with no Kafka wiring, no runtime restart, no
      migration, no service topology change. Handler returns a static stub result. Runtime pickup occurs
      on next normal omnimarket deployment.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11031/dod-deploy/command.yaml &&
          grep -q '^pr_number: 631$' drift/dod_receipts/OMN-11031/dod-deploy/command.yaml && grep -q 'No
          live deploy' drift/dod_receipts/OMN-11031/dod-deploy/command.yaml"
  - id: "dod-occ-pr-self"
    description: "This onex_change_control PR carries the central OMN-11031 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11031/dod-occ-pr-self/command.yaml"
