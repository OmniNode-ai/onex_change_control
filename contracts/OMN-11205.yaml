---
schema_version: "1.0.0"
ticket_id: "OMN-11205"
title: "Add doctrine coverage matrix and CI job"
summary: >
  Adds scripts/generate_doctrine_coverage.py to classify each DT-* doctrine clause as ENFORCED/ADVISORY/UNCOVERED
  by matching its ci_gate field against actual workflow job names. Generates docs/standards/doctrine_coverage.md
  (4 ENFORCED, 11 UNCOVERED). Adds .github/workflows/doctrine-coverage.yml which fails when a previously-ENFORCED
  clause loses its required CI gate (regression detection). Builds on OMN-11204 doctrine clauses registry.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "28 unit tests in tests/unit/doctrine/ pass (loader + coverage classification + regression
      detection)."
    command: "See drift/dod_receipts/OMN-11205/dod-tests/command.yaml"
  - kind: "ci"
    description: "ruff format + ruff check + mypy pass for new files."
    command: "See drift/dod_receipts/OMN-11205/dod-lint/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "28 doctrine loader and coverage tests passed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '28 passed' drift/dod_receipts/OMN-11205/dod-tests/command.yaml"
  - id: "dod-lint"
    description: "ruff format, ruff check, and mypy pass for new files."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11205/dod-lint/command.yaml"
  - id: "dod-occ-pr-1116"
    description: "onex_change_control PR #1116 carries the OMN-11205 doctrine coverage matrix and CI job."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11205/dod-occ-pr-1116/command.yaml
          && grep -q '^pr_number: 1116$' drift/dod_receipts/OMN-11205/dod-occ-pr-1116/command.yaml"
