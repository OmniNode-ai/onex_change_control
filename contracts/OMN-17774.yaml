---
schema_version: "1.0.0"
ticket_id: "OMN-17774"
title: "Autobind OCC evidence for OMN-17774"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnidash PR #309.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #309 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #309 on OmniNode-ai/omnidash changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (scripts/generate-registry.ts, shared/types/palette-visibility.ts, src/components/dashboard/index.ts, src/components/dashboard/session-replay/SessionReplayWidget.test.tsx) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnidash-pr-309"
    description: "PR #309 on OmniNode-ai/omnidash — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 309 --repo OmniNode-ai/omnidash --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnidash/pulls/309/files --paginate --jq '.[].filename' | grep -cFx 'scripts/generate-registry.ts'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnidash-pr-309"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-8133"
    description: "Binds OMN-17774 to OCC companion PR #8133 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8133 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2290"
    description: "PR #2290 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_session_replay/handlers/handler_projection_session_replay.py?ref=fadda5b16f3808d74bb150f91ee866fc7bcf55a2 --jq '.content' | base64 -d | grep -c 'def __init__'
  - id: "dod-OmniNode-ai-omnimarket-pr-2290-ci"
    description: "PR #2290 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2290 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-8166"
    description: "OCC companion PR #8166 — self-bind for OMN-17774 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8166 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omnimarket-pr-2290-session-replay-bus-backed"
    description: >
      OMN-17774 product PR OmniNode-ai/omnimarket#2290 — bus-backs onex.snapshot.projection.session.replay.v1.
      The contract flag and its publish call site land in the same commit (OMN-15864 ordering rule), so
      this item proves BOTH halves are present, not just the flag. A flag without its writer renders a
      confident empty, which is the failure mode the OMN-16776 epic exists to close.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2290 --repo OmniNode-ai/omnimarket --json number,state,mergedAt"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2290/files --paginate --jq '.[].filename' | grep -cFx 'src/omnimarket/projection/snapshot_publisher.py'
      - check_type: "command"
        check_value: |-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-17774/dod-omnimarket-pr-2290-session-replay-bus-backed/command.yaml
