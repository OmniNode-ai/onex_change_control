---
schema_version: "1.0.0"
ticket_id: "OMN-12725"
title: "Preflight runtime projection schema"
summary: >
  Central OCC evidence for omnibase_infra PR #1875, which ensures runtime deploys apply projection migrations
  into omnidash_analytics and assert the delegation_events and node_service_registry tables before runtime
  startup.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1875"
    description: "omnibase_infra PR #1875 carries the projection schema migration preflight fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12725/dod-omnibase-infra-pr-1875/command.yaml
          && grep -q '^pr_number: 1875$' drift/dod_receipts/OMN-12725/dod-omnibase-infra-pr-1875/command.yaml"
  - id: "dod-local-validation"
    description: "Focused migration and runtime deploy preflight checks passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12725/dod-local-validation/command.yaml
          && grep -q '65 passed' drift/dod_receipts/OMN-12725/dod-local-validation/command.yaml"
