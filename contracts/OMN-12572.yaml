---
schema_version: "1.0.0"
ticket_id: "OMN-12572"
title: "Extend deploy-agent for runtime_lane and image_digest"
summary: >-
  Bind central evidence for omnibase_infra PR #1829, which extends the deploy-agent command and completion
  boundary with runtime_lane plus image_ref/image_digest, makes image_digest authoritative for production
  deploys, parameterizes executor compose overlays, project names, and runtime health targets by lane,
  pulls the pinned digest for prod instead of rebuilding, verifies the running digest before health checks,
  and rejects prod requests without a matching stability READY digest before any deploy effect.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
  - "envelopes"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1829"
    description: "omnibase_infra PR #1829 carries the deploy-agent runtime_lane/image_digest boundary
      and executor lane/digest implementation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12572/dod-infra-pr-1829/command.yaml"
          && grep -q '^pr_number: 1829$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12572/dod-infra-pr-1829/command.yaml"
  - id: "dod-local-tests"
    description: "The deploy-agent lane/digest test suite and full deploy-agent suite pass locally on
      the implementation branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12572/dod-local-tests/command.yaml"
          && grep -q '134 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12572/dod-local-tests/command.yaml"
  - id: "dod-lint-format"
    description: "Deploy-agent formatting and lint checks passed for the implementation branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12572/dod-lint-format/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12572/dod-lint-format/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12572 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12572/dod-occ-pr-self/command.yaml"
