---
schema_version: 1.0.0
ticket_id: OMN-14196
title: 'Evidence for OmniNode-ai/omnibase_infra#2331: runtime build push retry parity'
summary: Central OCC evidence for omnibase_infra#2331 proving the exact product PR head, workflow-only
  diff scope, bounded docker push retry restoration, and deploy-readiness hold for the runtime build workflow
  parity fix.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #2331 on OmniNode-ai/omnibase_infra is open at exact head a3b3ec6fddd63ee908a2ce17da9cd3e18395dd1f.'
    command: 'PRODUCT_PR_NUMBER=2331; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=a3b3ec6fddd63ee908a2ce17da9cd3e18395dd1f;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
  - kind: ci
    description: 'PR #2331 changes only .github/workflows/build-and-push-runtime.yml.'
    command: 'PRODUCT_PR_NUMBER=2331; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER}; test "$(gh
      pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only)" = ".github/workflows/build-and-push-runtime.yml"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnibase_infra-pr-2331-head
    description: 'PR #2331 on OmniNode-ai/omnibase_infra exists and is bound to OMN-14196 at exact head
      a3b3ec6fddd63ee908a2ce17da9cd3e18395dd1f.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2331; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=a3b3ec6fddd63ee908a2ce17da9cd3e18395dd1f;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnibase_infra-pr-2331-workflow-only-diff
    description: 'PR #2331 changes only the runtime image build-and-push workflow file.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2331; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          test "$(gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only)" = ".github/workflows/build-and-push-runtime.yml"'
  - id: dod-omnibase_infra-pr-2331-push-retry
    description: 'PR #2331 restores bounded docker push retry configuration to the runtime image workflow.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2331; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO | grep -Fq "PUSH_MAX_ATTEMPTS" && gh pr diff
          $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO | grep -Fq "docker push" && gh pr diff $PRODUCT_PR_NUMBER
          --repo $PRODUCT_REPO | grep -Fq "BACKOFF_FACTOR"'
  - id: dod-omnibase_infra-pr-2331-deploy-hold
    description: 'Deploy-readiness assessment for PR #2331. No live build, ECR image push, deploy, restart,
      docker exec, rpk topic produce, or .201 mutation was performed by this OCC evidence lane.'
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14196/dod-omnibase_infra-pr-2331-deploy-hold/command.yaml
          && grep -qi ''no live build'' drift/dod_receipts/OMN-14196/dod-omnibase_infra-pr-2331-deploy-hold/command.yaml'
  - id: occ-self-bind-pr-4344
    description: 'OCC PR #4344 on OmniNode-ai/onex_change_control self-binding proof for OMN-14196 evidence.'
    source: generated
    checks:
      - check_type: command
        check_value: 'SELF_PR_NUMBER=4344; SELF_REPO=OmniNode-ai/onex_change_control; : ${PR_NUMBER};
          gh pr view $SELF_PR_NUMBER --repo $SELF_REPO --json number,state,headRefOid'
