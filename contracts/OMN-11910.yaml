---
schema_version: "1.0.0"
ticket_id: "OMN-11910"
title: "Antipattern registry schema and models"
summary: >
  Bind central evidence for omnibase_core PR #1144, which adds the antipattern registry YAML contract,
  Pydantic registry models, and loader validation used by downstream quality scanning workflows.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1144"
    description: "Bind OMN-11910 evidence to omnibase_core PR #1144."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11910/dod-omnibase-core-pr-1144/command.yaml
          && grep -q '^pr_number: 1144$' drift/dod_receipts/OMN-11910/dod-omnibase-core-pr-1144/command.yaml"
  - id: "dod-antipattern-registry-local-validation"
    description: "Focused antipattern registry model and loader tests pass for the current omnibase_core
      PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11910/dod-antipattern-registry-local-validation/command.yaml
          && grep -q '38 passed' drift/dod_receipts/OMN-11910/dod-antipattern-registry-local-validation/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11910 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11910/dod-occ-pr/command.yaml"
