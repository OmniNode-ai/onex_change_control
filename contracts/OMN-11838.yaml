---
schema_version: "1.0.0"
ticket_id: "OMN-11838"
title: "Manifest schema validator and spot-check report"
summary: >
  Add ADR canary manifest validation tooling, validation report evidence, and non-fatal source drift
  handling for point-in-time manifest snapshots.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-798"
    description: "omnimarket PR #798 adds the manifest validator, validation report, path normalization,
      and non-fatal source drift handling"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11838/dod-omnimarket-pr-798/command.yaml\"
          && grep -q '^pr_number: 798$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11838/dod-omnimarket-pr-798/command.yaml\"
          && grep -q '^commit_sha: \"8157aba1f356d305a9de1138828c954866fa3142\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11838/dod-omnimarket-pr-798/command.yaml\""
  - id: "dod-manifest-validation"
    description: "Manifest validator passes with 60 entries checked, 59 non-fatal drift warnings,
      and 0 failures"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11838/dod-manifest-validation/command.yaml\"
          && grep -q 'Failures: 0' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11838/dod-manifest-validation/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR binds OMN-11838 contract and receipts as the central evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11838/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: \"OMN-11838\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11838/dod-occ-pr/command.yaml\""
