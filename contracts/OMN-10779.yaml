---
schema_version: "1.0.0"
ticket_id: OMN-10779
title: "feat(OMN-10779): condition evaluator — pure guard expression evaluator"
summary: >
  Adds evaluate_condition(expr, state) to omnibase_infra/onboarding with regex-based parser (no eval()).
  Supports ==, in [...], not in [...], in state_key, and conjunctions. None condition returns True. 29
  unit tests. Part of OMN-10777 epic (Task 2).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 29/29 unit tests pass
    command: "uv run pytest tests/unit/onboarding/test_condition_evaluator.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: condition_evaluator source file exists in omnibase_infra PR branch
    source: generated
    checks:
      - check_type: command
        check_value: >
          gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/onboarding/condition_evaluator.py?ref=jonah/omn-10779-condition-evaluator"
          --jq '.name'
  - id: dod-002
    description: Unit test file exists in omnibase_infra PR branch
    source: generated
    checks:
      - check_type: command
        check_value: >
          gh api "repos/OmniNode-ai/omnibase_infra/contents/tests/unit/onboarding/test_condition_evaluator.py?ref=jonah/omn-10779-condition-evaluator"
          --jq '.name'
  - id: dod-003
    description: PR cites this ticket
    source: generated
    checks:
      - check_type: command
        check_value: >
          gh pr view 1548 --repo OmniNode-ai/omnibase_infra --json title,body --jq '.title + " " + (.body
          // "")' | grep -q 'OMN-10779'
