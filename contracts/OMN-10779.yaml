---
schema_version: "1.0.0"
ticket_id: "OMN-10779"
title: "feat(OMN-10779): condition evaluator pure guard expression evaluator"
summary: >
  Hardens the onboarding condition evaluator with quote-aware and-clause splitting and explicit unit marker
  coverage while preserving existing integration behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1548 carries the OMN-10779 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10779/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Focused unit, integration, and ruff checks passed for the quote-aware condition evaluator
      fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10779/dod-local-tests/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR #886 carries the central OCC contract and receipt update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10779/dod-occ-pr/command.yaml\""
