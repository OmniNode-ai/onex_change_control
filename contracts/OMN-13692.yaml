---
schema_version: "1.0.0"
ticket_id: "OMN-13692"
title: "Migrate node_e2e_orchestrator contract topics to canonical event_bus schema"
summary: >-
  omnimarket PR #1488 fixes schema drift in node_e2e_orchestrator/contract.yaml by migrating legacy topics.subscribes/topics.publishes
  declarations to event_bus.subscribe_topics/event_bus.publish_topics. The product PR retains the existing
  runtime_dispatch exemption and adds unit coverage for the canonical contract shape and topic constants.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "omnimarket PR #1488 checks pass after OCC evidence lands"
    command: "gh pr checks 1488 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr"
    description: >-
      omnimarket PR #1488 migrates node_e2e_orchestrator/contract.yaml to canonical event_bus topic schema
      and adds regression tests for the contract shape.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13692/dod-omnimarket-pr/command.yaml && grep
          -q '^pr_number: 1488$' drift/dod_receipts/OMN-13692/dod-omnimarket-pr/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-readiness assessment for runtime contract-path edits. No live deploy, restart, docker exec,
      rpk topic produce, or .201 mutation was performed by this OCC PR; the normal post-merge dev deploy
      picks up the contract schema-only change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13692/dod-deploy-assessment/command.yaml &&
          grep -qi 'deploy' drift/dod_receipts/OMN-13692/dod-deploy-assessment/command.yaml && grep -qi
          'docker exec' drift/dod_receipts/OMN-13692/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: "Self-binding OCC eligibility receipt for this OCC PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13692/dod-occ-self/command.yaml"
