---
schema_version: "1.0.0"
ticket_id: "OMN-16307"
title: "Autobind OCC evidence for OMN-16307"
summary: >
  OCC contract for OmniNode-ai/omnibase_infra PR #2891 (Wave-2 docs consolidation: thins 38 of 50 docs/architecture/**,
  docs/guides/**, docs/runbooks/** candidates to a verbatim pointer at the taxonomy's knowledge-base home,
  per the org-wide docs consolidation plan). Local-CLI mint fallback: autobind declined to mint a companion
  for this PR (docs-only diff, no changed-file candidate could be proven RED against the merge base —
  onexbot-occ-writer comment on the PR), so a content-pinned differential probe was hand-authored instead.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2891"
    description: >
      PR #2891 on OmniNode-ai/omnibase_infra -- thins docs/architecture/CURRENT_NODE_ARCHITECTURE.md (and
      37 sibling architecture/guide/runbook files) from full in-repo content to a verbatim pointer at
      its new home in OmniNode-ai/knowledge-base (already merged there via knowledge-base#35). Content-pinned
      RED-before/GREEN-after differential: fetches CURRENT_NODE_ARCHITECTURE.md at both the PR's base
      and head SHAs via the GitHub contents API and confirms the file is full architecture content (>100
      lines) at base but thinned to a short knowledge-base pointer at head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/docs/architecture/CURRENT_NODE_ARCHITECTURE.md?ref=c7060786375771a36327805c061704b38f2a253c"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/docs/architecture/CURRENT_NODE_ARCHITECTURE.md?ref=ce1d46473f77f8a85342ef321484144fae1f36e0"
          --jq .content | base64 -d)"; base_lines=$(printf '%s' "$base_body" | wc -l | tr -d ' '); [ "$base_lines"
          -gt 100 ] && printf '%s' "$head_body" | grep -qF 'knowledge-base' && printf '%s' "$head_body"
          | grep -qF 'Migrated to the OmniNode knowledge base' && echo "OMN-16307-INFRA-2891-DIFFERENTIAL-PROBE-PASS:
          CURRENT_NODE_ARCHITECTURE.md is full architecture content ($base_lines lines) at base, thinned
          to a knowledge-base pointer at head"
  - id: "occ-self-bind-pr-7089"
    description: "Binds OMN-16307 to OCC companion PR #7089 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7089 --repo OmniNode-ai/onex_change_control --json number,state"
