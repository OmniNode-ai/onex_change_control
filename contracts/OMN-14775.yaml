---
schema_version: 1.0.0
ticket_id: OMN-14775
title: Signal-based timeout harness for product-readiness shadow slice
summary: Central OCC evidence for omnimarket#1823. The product PR replaces the product-readiness shadow-slice
  hard-exit timeout path with a stdlib signal-based harness that runs slices in their own process group,
  emits the canonical runner-infra hang signatures, reaps child processes, and preserves normal product
  exit codes. Exact product head 5b8f9db176f02f8b214c056f52a1be199aeb620c.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #1823 product head is exact for OMN-14775 evidence.'
    command: 'PRODUCT_PR_NUMBER=1823; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=5b8f9db176f02f8b214c056f52a1be199aeb620c;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnimarket-pr-1823
    description: 'PR #1823 on OmniNode-ai/omnimarket exists and is bound to OMN-14775 at exact product
      head 5b8f9db176f02f8b214c056f52a1be199aeb620c.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1823; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=5b8f9db176f02f8b214c056f52a1be199aeb620c;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnimarket-pr-1823-diff
    description: 'PR #1823 on OmniNode-ai/omnimarket touches only the product-readiness shadow workflow,
      the signal-based shadow-slice harness, and its focused regression tests.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1823; PRODUCT_REPO=OmniNode-ai/omnimarket; : ${PR_NUMBER}; gh
          pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx ".github/workflows/product-readiness-shadow.yml"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "scripts/ci/run_shadow_slice.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "tests/ci/test_run_shadow_slice.py"
          && test "$(gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | awk ''END{print
          NR}'')" = "3"'
  - id: dod-omnimarket-pr-1823-local-verification
    description: Product PR body records local verification evidence for OMN-14775.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1823; PRODUCT_REPO=OmniNode-ai/omnimarket; : ${PR_NUMBER}; gh
          pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "uv run pytest
          tests/ci/test_run_shadow_slice.py" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json
          body --jq .body | grep -q "10 passed" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO
          --json body --jq .body | grep -q "334 passed" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO
          --json body --jq .body | grep -q "mypy --strict scripts/ci/run_shadow_slice.py"'
  - id: dod-omnimarket-pr-1823-deploy
    description: Deploy-readiness assessment for OMN-14775. The PR changes CI harness code, a GitHub workflow
      invocation, and focused tests only. No live deploy, restart, .201, Kafka, database, or production
      mutation was performed by this OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14775/dod-omnimarket-pr-1823-deploy/command.yaml
          && grep -qi ''no live deploy, restart, .201, Kafka, database, or production mutation'' drift/dod_receipts/OMN-14775/dod-omnimarket-pr-1823-deploy/command.yaml'
  - id: occ-self-bind-pr-4428
    description: 'OCC PR #4428 on OmniNode-ai/onex_change_control self-binding proof for OMN-14775 evidence.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
