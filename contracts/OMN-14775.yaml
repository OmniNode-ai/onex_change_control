---
schema_version: 1.0.0
ticket_id: OMN-14775
title: Signal-based timeout harness for product-readiness shadow slice
summary: Central OCC evidence for omnimarket#1823. The product PR replaces the product-readiness shadow-slice
  hard-exit timeout path with a signal-based process-group harness that emits runner-infra hang signatures,
  reaps child processes, preserves successful and failed slice exit codes, and wires the tests+coverage
  shadow job through that harness. Exact product head 5b8f9db176f02f8b214c056f52a1be199aeb620c.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #1823 product head is exact for OMN-14775 evidence.'
    command: 'PRODUCT_PR_NUMBER=1823; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=5b8f9db176f02f8b214c056f52a1be199aeb620c;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnimarket-pr-1823
    description: 'PR #1823 on OmniNode-ai/omnimarket exists and is bound to OMN-14775 at exact product
      head 5b8f9db176f02f8b214c056f52a1be199aeb620c.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1823; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=5b8f9db176f02f8b214c056f52a1be199aeb620c;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnimarket-pr-1823-diff
    description: 'PR #1823 adds the signal-based shadow-slice harness, wires product-readiness shadow
      tests through it, and adds focused timeout/reap/classification regression tests.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1823; PRODUCT_REPO=OmniNode-ai/omnimarket; : ${PR_NUMBER}; gh
          pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "scripts/ci/run_shadow_slice.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx ".github/workflows/product-readiness-shadow.yml"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "tests/ci/test_run_shadow_slice.py"'
  - id: dod-omnimarket-pr-1823-local-verification
    description: Product PR body records focused shadow-slice tests, governed pre-push selector, ruff,
      mypy, and changed-file pre-commit verification for OMN-14775.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1823; PRODUCT_REPO=OmniNode-ai/omnimarket; : ${PR_NUMBER}; gh
          pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "10 passed"
          && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "334
          passed" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep
          -q "mypy --strict scripts/ci/run_shadow_slice.py" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO
          --json body --jq .body | grep -q "pre-commit"'
  - id: dod-omnimarket-pr-1823-deploy
    description: Deploy-readiness assessment for OMN-14775. The PR changes CI harness/workflow code and
      tests only; no live deploy, restart, .201, Kafka, database, or production mutation was performed
      by this OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14775/dod-omnimarket-pr-1823-deploy/command.yaml
          && grep -qi ''no live deploy, restart, .201, Kafka, database, or production mutation'' drift/dod_receipts/OMN-14775/dod-omnimarket-pr-1823-deploy/command.yaml'
  - id: occ-self-bind-pr-4428
    description: 'OCC PR #4428 on OmniNode-ai/onex_change_control self-binding proof for OMN-14775 evidence.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
