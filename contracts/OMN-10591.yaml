---
schema_version: "1.0.0"
ticket_id: "OMN-10591"
title: "refactor: flatten model registry to config-driven paths"
summary: >
  Remove hardcoded model IDs and LAN endpoint literals from models_registry.yaml, bridge_config_loader.py,
  node_ci_fix_effect/contract.yaml, and assemble_live.py. All model resolution uses env vars / Settings.
  Unit tests added for bridge_config_loader.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-no-hardcoded-model-ids"
    description: "No hardcoded model IDs remain in the modified files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10591/dod-no-hardcoded-model-ids/command.yaml"
  - id: "dod-unit-tests"
    description: "bridge_config_loader unit tests pass (13 tests)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10591/dod-unit-tests/command.yaml"
  - id: "dod-deploy"
    description: >
      Config-only refactor: runtime paths touched (assemble_live.py, handler_ci_fix.py, contract.yaml)
      now resolve LLM_* env vars at runtime. No docker exec or service restart required.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10591/dod-deploy/command.yaml"
