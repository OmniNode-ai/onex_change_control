---
schema_version: "1.0.0"
ticket_id: OMN-10637
title: "feat(OMN-10637): Bifrost delegation routing rules config for task classes"
summary: "Add bifrost_delegation.yaml config, Pydantic schema, loader with cross-reference validation,
  and unit tests"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1514 CI green and merged"
    command: "gh pr view 1514 --repo OmniNode-ai/omnibase_infra --json state,mergedAt"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "30 unit tests pass in omnibase_infra PR — schema validation, provenance fields, task
      class coverage, cross-reference invariants"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr checks 1514 --repo OmniNode-ai/omnibase_infra"
  - id: "dod-002"
    description: "PR merged to main with verified merge commit"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json state,mergedAt"
