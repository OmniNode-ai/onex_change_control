---
schema_version: "1.0.0"
ticket_id: OMN-10637
title: "feat(OMN-10637): Bifrost delegation routing rules config for task classes"
summary: "Add bifrost_delegation.yaml config, Pydantic schema, loader with cross-reference validation,
  and unit tests"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1514 CI green and merged"
    command: "gh pr view 1514 --repo OmniNode-ai/omnibase_infra --json state,mergedAt"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "OCC contract for OMN-10637 exists — bifrost delegation config work registered"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-10637.yaml"
  - id: "dod-002"
    description: "PR merged to main with verified merge commit"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json state,mergedAt"
  - id: "dod-003"
    description: "Bind bifrost delegation routing evidence to omnibase_infra PR #1514"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1514 --repo OmniNode-ai/omnibase_infra --json headRefOid,title"
  - id: "dod-occ-pr-760"
    description: "Self-binding receipt for OCC PR #760 — required for OCC's own receipt-gate"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^pr_number: 760$' drift/dod_receipts/OMN-10637/dod-occ-pr-760/command.yaml"
