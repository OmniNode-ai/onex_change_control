---
schema_version: "1.0.0"
ticket_id: OMN-10637
title: "feat(OMN-10637): Bifrost delegation routing rules config for task classes"
summary: "Add bifrost_delegation.yaml config, Pydantic schema, loader with cross-reference validation,
  and unit tests"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Merged PR confirms work shipped"
    command: "gh pr view {pr} --repo {repo} --json state,mergedAt"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "All 30 unit tests pass: schema validation, provenance fields, task class coverage, cross-reference
      invariants"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_llm_inference_effect/handlers/bifrost/test_bifrost_delegation_config.py
          -q"
  - id: "dod-002"
    description: "Config loads cleanly after deploy — loader validates cross-references and rule_id uniqueness"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "deploy omninode-runtime after merge; verify load_bifrost_delegation_config() loads
          bifrost_delegation.yaml without ValueError"
  - id: "dod-003"
    description: "PR merged to main with verified merge commit"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json state,mergedAt"
