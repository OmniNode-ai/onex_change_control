---
schema_version: "1.0.0"
ticket_id: OMN-16626
title: Central OCC evidence for OMN-16626 prod-promotion grant PREP landing on main
summary: >-
  Central OCC evidence for the hotfix/* PR (#7213) that lands the onex-prod runtime same-digest grant
  (grant-3c885a33) and the omniintelligence digest-pin grant (grant-9c23ed5d), both under promotion_batch_id
  batch-92a794cd-680a-4af6-8879-32969bf7eeb1, on onex_change_control@main. PREP ONLY: this contract proves
  the PR itself is well-formed, locally validated, and CI-clean; it does NOT authorize prod mutation and
  it is NOT the approval act. The PR still requires a human CODEOWNERS operator's explicit ready+merge
  action per OMN-13418. This contract exists on the PR branch itself because occ-preflight pins OCC evidence
  at the PR head SHA for onex_change_control's own PRs (invariant I3, in-tree checkout) — the same shape
  OCC#7084 used for OMN-16205.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: manual
    description: >-
      Prod-promotion grant registry values and local grant-schema test validation for the OMN-16626 grant
      PREP landing on main.
    command: >-
      grep -Fq 'grant-3c885a33-552b-4ff1-a5c6-a3c31011fa9e' grants/prod_promotion_grants.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-prod-promotion-grant-values-omn16626"
    description: >-
      Prod-promotion grant registry (grants/prod_promotion_grants.yaml) carries both OMN-16626 grant entries
      under promotion_batch_id batch-92a794cd-680a-4af6-8879-32969bf7eeb1: grant-3c885a33-552b-4ff1-a5c6-a3c31011fa9e
      (omninode-runtime sha256:13c96028…, the digest the deploy-onex-prod grant gate actually resolves,
      a same-digest authorization) and grant-9c23ed5d-2f44-4757-90d4-1fb84b4b3b43 (omniintelligence sha256:a66ff063…,
      the digest the promotion actually changes), landed on hotfix/omn-16626-prod-promotion-grant (PR
      #7213). Falsifiable against the repo checkout: the greps fail on any base that lacks these entries,
      including onex_change_control@main.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Fq 'grant-3c885a33-552b-4ff1-a5c6-a3c31011fa9e' grants/prod_promotion_grants.yaml && grep
          -Fq 'grant-9c23ed5d-2f44-4757-90d4-1fb84b4b3b43' grants/prod_promotion_grants.yaml && grep -Fq
          'promotion_batch_id: batch-92a794cd-680a-4af6-8879-32969bf7eeb1' grants/prod_promotion_grants.yaml
  - id: "dod-prod-promotion-grant-validation-omn16626"
    description: >-
      The repo's own grant-schema suite passes against the grant file carrying the two new OMN-16626 entries
      and with the two expired OMN-16205 entries pruned — including the OMN-13424 at-rest invariant test
      that refuses any expired entry. Executed hermetically from the repo checkout.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_prod_promotion_grants.py -q"
  - id: "occ-self-bind-pr-7213"
    description: >-
      OCC PR #7213 — self-bind for OMN-16626 prod-promotion grant PREP (onex-prod runtime same-digest
      grant + omniintelligence digest-pin grant, batch-92a794cd), DRAFT, NOT to be merged by any agent
      per the PR's own operator-approval-act scope statement.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid
