---
schema_version: "1.0.0"
ticket_id: "OMN-15048"
title: "Autobind OCC evidence for OMN-15048"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1887."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1887 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1887"
    description: "PR #1887 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1887-ci"
    description: "PR #1887 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4765"
    description: "OCC companion PR #4765 — self-bind for OMN-15048 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omn15048-pr1887-deploy"
    description: "PR #1887 deploy disposition — runtime-path change (node_swarm_fleet_discovery_effect
      handler + contract.yaml, node_model_router contract.yaml) carries an explicit post-merge dev deploy
      validation bar."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1887 --repo OmniNode-ai/omnimarket --json body,files --jq 'if ((.body
          | test(\"Post-merge dev deploy validation\")) and (.body | test(\"node_swarm_fleet_discovery_effect\"))
          and any(.files[].path; . == \"src/omnimarket/nodes/node_swarm_fleet_discovery_effect/handlers/handler_swarm_fleet_discovery.py\"))
          then \"deploy disposition verified for OMN-15048 swarm fleet discovery OPEN_ROUTER_API_KEY fix\"
          else error(\"OMN-15048 deploy disposition missing PR body bar or runtime path\") end'"
