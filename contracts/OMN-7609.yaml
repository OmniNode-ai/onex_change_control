---
schema_version: '1.0.0'
ticket_id: OMN-7609
title: "fix(omniintelligence): wire Kafka producer into crawl scheduler registry"
summary: >
  The dispatch engine created crawl-requested and document-indexed handlers but never registered the Kafka
  producer with RegistryCrawlSchedulerEffect, so crawl-tick.v1 events were never forwarded to downstream
  crawler nodes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: All dispatch handler tests pass including new producer wiring tests
    command: "cd omniintelligence && uv run pytest tests/unit/runtime/test_dispatch_handlers.py -v"
  - kind: ci
    description: Crawl scheduler node governance tests pass
    command: "cd omniintelligence && uv run pytest src/omniintelligence/nodes/node_crawl_scheduler_effect/node_tests/
      -v"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >
      Producer is registered with RegistryCrawlSchedulerEffect when kafka_producer is provided to create_intelligence_dispatch_engine()
    source: generated
    checks:
      - check_type: command
        check_value: >
          cd omniintelligence && uv run pytest tests/unit/runtime/test_dispatch_handlers.py::TestCrawlSchedulerProducerWiring::test_publisher_registered_when_provided
          -v --no-header | grep -q 'PASSED'
  - id: dod-002
    description: >
      End-to-end crawl-requested handler emits crawl-tick.v1 event via the registered producer after dispatch
      engine creation
    source: generated
    checks:
      - check_type: command
        check_value: >
          cd omniintelligence && uv run pytest tests/unit/runtime/test_dispatch_handlers.py::TestCrawlSchedulerProducerWiring::test_crawl_requested_handler_emits_tick_event
          -v --no-header | grep -q 'PASSED'
