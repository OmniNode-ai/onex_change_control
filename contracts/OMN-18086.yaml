---
schema_version: "1.0.0"
ticket_id: "OMN-18086"
title: "Autobind OCC evidence for OMN-18086"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3468.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3468 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3468 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/unit/adapters/test_adapter_infisical.py tests/unit/secret_stores/test_infisical_secret_store.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3468"
    description: "PR #3468 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/adapters/_internal/adapter_infisical.py?ref=fa1f4345194b4529704588eceab87801d98dac46 --jq '.content' | base64 -d | grep -c 'def delete_secret'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3468/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_infra/adapters/_internal/adapter_infisical.py'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3468 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/adapters/test_adapter_infisical.py tests/unit/secret_stores/test_infisical_secret_store.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-9285"
    description: "Binds OMN-18086 to OCC companion PR #9285 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9285 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3472"
    description: "PR #3472 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3472 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3472/files --paginate --jq '.[].filename' | grep -cFx 'CHANGELOG.md'
  - id: "occ-self-bind-pr-9298"
    description: "Binds OMN-18086 to OCC companion PR #9298 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9298 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3517-release-gap"
    description: "PR #3517 on OmniNode-ai/omnibase_infra — release retry evidence for v0.38.24."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          test -z "$(git -C "$OMNI_HOME/omnibase_infra" ls-remote --tags origin 'v0.38.24')"
      - check_type: "command"
        check_value: |-
          tmpdir="$(mktemp -d)"; cd "$tmpdir"; uv venv --seed --python 3.12 .venv >/dev/null; . .venv/bin/activate; python -m pip index versions omnibase-infra | head -1 | grep -Fx 'omnibase-infra (0.38.23)'
      - check_type: "command"
        check_value: |-
          gh pr view 3517 --repo OmniNode-ai/omnibase_infra --json headRefOid --jq '.headRefOid' | grep -Fx '48ef3024473d42b2e9d2e4e12025f5f2a8e5943a'
      - check_type: "command"
        check_value: |-
          tmpdir="$(mktemp -d)"; cd "$tmpdir"; uv venv --python 3.12 .venv >/dev/null; . .venv/bin/activate; printf '%s\n' 'omnimarket @ git+https://github.com/OmniNode-ai/omnimarket.git@dev' > req.in; if uv pip compile req.in >compile.out 2>&1; then cat compile.out; exit 1; fi; grep -F 'omnimarket==0.4.81 depends on omnibase-core>=0.47.13' compile.out; grep -F 'omnibase-infra>=0.38.23 depends on omnibase-core==0.47.12' compile.out
  - id: "occ-self-bind-pr-9462"
    description: "Binds OMN-18086 to OCC companion PR #9462 (self-bind)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9462 --repo OmniNode-ai/onex_change_control --json number,state"
