---
schema_version: "1.0.0"
ticket_id: "OMN-10698"
title: "Add ADR canary orchestrator node"
summary: >
  Binds omnimarket PR #565, which adds the ADR canary orchestrator node, shared ADR protocol models, adapter
  boundaries, local manifest fixtures, and the command publisher, plus follow-up PR #566, which removes
  adapter shims and requires explicit sub-handler dependency injection, to central OCC receipt evidence.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-565-head"
    description: "omnimarket PR #565 is open at the rebased OMN-10698 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10698/dod-pr-565-head/command.yaml\""
  - id: "dod-pr-566-head"
    description: "omnimarket PR #566 is open at the OMN-10698 dependency-injection follow-up head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10698/dod-pr-566-head/command.yaml\""
  - id: "dod-local-verification"
    description: "Focused local verification for orchestrator tests, typecheck, reach-in guard, and literal
      gate passed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10698/dod-local-verification/command.yaml\""
  - id: "dod-di-local-verification"
    description: "Focused local verification for the dependency-injection follow-up passed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10698/dod-di-local-verification/command.yaml\""
  - id: "dod-deploy-contract"
    description: "Repo-local deploy contract was added for OMN-10698 runtime-impacting node work."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10698/dod-deploy-contract/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10698/dod-occ-pr/command.yaml\""
  - id: "dod-di-occ-pr"
    description: "Bind the dependency-injection follow-up evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10698/dod-di-occ-pr/command.yaml\""
