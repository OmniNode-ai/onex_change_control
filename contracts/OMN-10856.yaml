---
schema_version: "1.0.0"
ticket_id: "OMN-10856"
title: "Autobind OCC evidence for OMN-10856"
summary: >
  OCC contract authored by Codex merge controller for OmniNode-ai/omninode_infra PR #786.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #786 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-786"
    description: "PR #786 on OmniNode-ai/omninode_infra - cloud runtime worker deployment evidence bind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 786 --repo OmniNode-ai/omninode_infra --json number,state,headRefOid"
      - check_type: "command"
        check_value: "gh pr view 786 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-786"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5797"
    description: "Binds OMN-10856 to OCC companion PR #5797 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5797 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omn10856-infra-pr-788-head-binding"
    description: >-
      Binds OMN-10856 to omninode_infra PR #788 (branch jonah/omn-10856-worker-consumer-group-seam), the
      second product PR on this ticket after #786. Head OID and base branch are asserted together in one
      equality, so a re-pushed head, a retargeted base, or a different PR each yield a mismatch — this
      deliberately avoids the OMN-14443 vacuous-probe class, where `gh pr view <n> --json number,state`
      passes regardless of the outcome being claimed. This establishes binding, not correctness; #788
      carries its own RED-before/GREEN-after guard matrix (5 mutation-proven REDs, 7 passed) in its PR
      body. AUTHORSHIP: net-new append-only companion authored by session fable-drain-0801, which did
      not implement #788. It replaces the autobind companion OCC#5848, which wedged itself by minting
      three byte-identical `command.supersede.788.yaml` records over three DISTINCT evidence items (OMN-15459
      wrong-item rebind, S1+S2) and editing the merged receipt drift/dod_receipts/OMN-10856/dod-occ-evidence-admissibility-validator/command.yaml
      in place (OMN-14623 append-only violation). Nothing merged is edited here and no supersession record
      is minted; this contract is appended to only.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          bind="$(gh pr view 788 --repo OmniNode-ai/omninode_infra --json headRefOid,baseRefName --jq '.headRefOid + " " + .baseRefName')" && test "$bind" = "a099a92d52db3f1bb43b432d94c39090ee29de58 dev"
  - id: "occ-self-bind-pr-5851"
    description: >-
      Binds OMN-10856 to its OCC companion PR #5851 (self-bind). Number and head branch are asserted together
      in one equality, so a different OCC PR carrying the same number-only probe does not satisfy it —
      this deliberately avoids the OMN-14443 vacuous-probe class, where `gh pr view <n> --json number,state`
      passes regardless of the outcome being claimed. #5851 is the append-only replacement for the wedged
      autobind companion OCC#5848.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          bind="$(gh pr view 5851 --repo OmniNode-ai/onex_change_control --json number,headRefName --jq '(.number|tostring) + " " + .headRefName')" && test "$bind" = "5851 jonah/omn-10856-occ-companion-pr788"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2673"
    description: "PR #2673 on OmniNode-ai/omnibase_infra - exact-head runtime topology manifest identity
      diff."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2673 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,files
          --jq 'if (.number == 2673 and .state == "OPEN" and .headRefName == "jonah/omn-10856-runtime-topology-manifest"
          and .headRefOid == "0ee053282a95d5be79449a5c5444708296c94088" and ([.files[].path] | sort) ==
          ["src/omnibase_infra/runtime/auto_wiring/introspection_manifest_identity.py","src/omnibase_infra/runtime/auto_wiring/models/__init__.py","src/omnibase_infra/runtime/auto_wiring/models/model_auto_wiring_manifest.py","src/omnibase_infra/runtime/auto_wiring/models/model_runtime_build_sha.py","src/omnibase_infra/runtime/service_kernel.py","tests/unit/runtime/auto_wiring/test_runtime_topology_manifest_identity_omn10856.py","tests/unit/runtime/test_service_kernel_introspection_identity_wiring_omn10856.py"])
          then "true" else error("unexpected #2673 state, head, or file set") end'
  - id: "dod-occ-evidence-validator-pr-2673"
    description: "Hosted OCC evidence admissibility validator for OMN-10856 / PR #2673."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2673"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6143"
    description: "Binds OMN-10856 to OCC companion PR #6143 for omnibase_infra PR #2673."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6143 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2673-85942c4"
    description: >-
      Binds OMN-10856 to omnibase_infra PR #2673 at exact head 85942c441 (jonah/omn-10856-runtime-topology-manifest,
      base dev) after (1) an OMN-15789 dev merge that landed since the prior ffc8527 bind (occ#6183, closed
      stale) and (2) a further dev merge picking up #2670/#2676/#2680/#2697/#2703/#2705/#2706. Head OID,
      base branch, and the exact 7-file diff set are asserted together in one equality, so a re-pushed
      head, a retargeted base, or a different file set each yield a mismatch (OMN-14443 vacuous-probe
      class avoided).
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2673"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2673 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,baseRefName,files
          --jq 'if (.number == 2673 and .state == "OPEN" and .headRefName == "jonah/omn-10856-runtime-topology-manifest"
          and .baseRefName == "dev" and .headRefOid == "85942c44131e6699a6d0a57d287867f46c8e6168" and
          ([.files[].path] | sort) == ["src/omnibase_infra/runtime/auto_wiring/introspection_manifest_identity.py","src/omnibase_infra/runtime/auto_wiring/models/__init__.py","src/omnibase_infra/runtime/auto_wiring/models/model_auto_wiring_manifest.py","src/omnibase_infra/runtime/auto_wiring/models/model_runtime_build_sha.py","src/omnibase_infra/runtime/service_kernel.py","tests/unit/runtime/auto_wiring/test_runtime_topology_manifest_identity_omn10856.py","tests/unit/runtime/test_service_kernel_introspection_identity_wiring_omn10856.py"])
          then "true" else error("unexpected #2673 state, head, base, or file set") end'
  - id: "dod-deploy-scope-omnibase-infra-pr-2673-85942c4"
    description: >-
      Deploy-scope proof for omnibase_infra PR #2673 runtime introspection-manifest-identity surface at
      exact product head 85942c441 — the item the prepush-deploy-scope-dod hook requires for the deploy-scoped
      file set it names (src/omnibase_infra/runtime/auto_wiring/introspection_manifest_identity.py, src/omnibase_infra/runtime/auto_wiring/models/__init__.py,
      src/omnibase_infra/runtime/auto_wiring/models/model_auto_wiring_manifest.py, src/omnibase_infra/runtime/auto_wiring/models/model_runtime_build_sha.py,
      src/omnibase_infra/runtime/service_kernel.py). Falsifiable: fetches live file bytes from GitHub
      at the pinned ref and greps for symbols the fix introduces (ModelRuntimeBuildSha, bind_introspection_manifest_identity,
      ENV_VAR_DEPLOYMENT_SHA) — it does not read back this PR's own receipt or contract, so it goes RED
      if the ref lacks the surface (OMN-14051 hermetic falsifiable-probe shape).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          body1="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/models/model_runtime_build_sha.py?ref=85942c44131e6699a6d0a57d287867f46c8e6168' --jq .content | base64 -d)" && printf '%s' "$body1" | grep -qF 'class ModelRuntimeBuildSha' &&
          body2="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/introspection_manifest_identity.py?ref=85942c44131e6699a6d0a57d287867f46c8e6168' --jq .content | base64 -d)" && printf '%s' "$body2" | grep -qF 'bind_introspection_manifest_identity' &&
          body3="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/service_kernel.py?ref=85942c44131e6699a6d0a57d287867f46c8e6168' --jq .content | base64 -d)" && printf '%s' "$body3" | grep -qF 'ENV_VAR_DEPLOYMENT_SHA'
  - id: "occ-self-bind-pr-6298"
    description: "Binds OMN-10856 to current OCC companion PR #6298 for omnibase_infra PR #2673 deploy-scope
      evidence (loose existence/state probe, never head-equality on the companion itself, per the standing
      self-bind rule)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6298 --repo OmniNode-ai/onex_change_control --json number,state"
