---
schema_version: "1.0.0"
ticket_id: "OMN-12175"
title: "Eliminate hardcoded paths in remaining repos"
summary: >
  Central OCC evidence for onex_change_control PR #1669. The change removes hardcoded local workstation
  paths from repository configuration and an existing ticket contract, replacing them with workspace-relative
  `$OMNI_HOME` references.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "onex_change_control YAML remains parseable after replacing the hardcoded planning-doc
      path."
    command: "See drift/dod_receipts/OMN-12175/dod-yaml-valid/command.yaml"
  - kind: "ci"
    description: "Pre-commit passes across the repository after the path cleanup."
    command: "See drift/dod_receipts/OMN-12175/dod-pre-commit/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR #1669 carries the central OMN-12175 contract and receipts."
    command: "See drift/dod_receipts/OMN-12175/dod-occ-pr-1669/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-yaml-valid"
    description: "YAML parser accepted the updated pre-commit configuration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12175/dod-yaml-valid/command.yaml
          && grep -q 'YAML valid' drift/dod_receipts/OMN-12175/dod-yaml-valid/command.yaml"
  - id: "dod-pre-commit"
    description: "Pre-commit passed for the path cleanup branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12175/dod-pre-commit/command.yaml
          && grep -q 'all checks passed' drift/dod_receipts/OMN-12175/dod-pre-commit/command.yaml"
  - id: "dod-occ-pr-1669"
    description: "onex_change_control PR #1669 carries the central OMN-12175 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12175/dod-occ-pr-1669/command.yaml
          && grep -q '^pr_number: 1669$' drift/dod_receipts/OMN-12175/dod-occ-pr-1669/command.yaml"
