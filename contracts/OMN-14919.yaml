---
schema_version: "1.0.0"
ticket_id: "OMN-14919"
title: "Fail-closed bot-authored authorization guard"
summary: "OCC evidence for onex_change_control#4574, the OMN-14919 change that adds a fail-closed guard
  rejecting bot-authored PRs touching grants or allowlists authorization surfaces."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "onex_change_control#4574 product PR is open on the expected OMN-14919 bot-authored authorization
      guard branch."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - kind: "ci"
    description: "onex_change_control#4574 changes only CI wiring, package entrypoint registration, the
      bot-authored authorization guard script, and focused unit tests."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr-4574"
    description: "PR #4574 on OmniNode-ai/onex_change_control is the OMN-14919 bot-authored authorization
      guard change."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - id: "dod-occ-pr-4574-files"
    description: "PR #4574 on OmniNode-ai/onex_change_control is scoped to CI wiring, package entrypoint
      registration, the guard script, and focused unit tests."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
