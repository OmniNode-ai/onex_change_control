---
schema_version: "1.0.0"
ticket_id: "OMN-13583"
title: "Repo-health failure-origin classifier compute node"
summary: >-
  Central evidence for omnimarket PR #1395, which adds the repo-health failure-origin classifier compute
  node and shared repo-health event models for the merge-sweep repo-health lane. The change is runtime-path
  touching and build/PR scoped; this contract supplies the central Receipt-Gate and deploy-gate evidence
  cited by the product PR.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1395"
    description: >-
      omnimarket PR #1395 carries node_repo_health_classify_compute, shared repo-health event models,
      node metadata, handler logic, and golden-chain/runtime dogfood tests for failure-origin classification.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid,baseRefName"
  - id: "dod-occ-evidence-source"
    description: >-
      This OCC contract is the central Evidence-Source for OMN-13583 and is cited by omnimarket PR #1395.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-13583.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: omnimarket PR #1395 adds a runtime compute node and event models. The PR scope
      is code, tests, and contract evidence only; post-merge runtime rollout follows the normal deployment
      lane.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13583/dod-deploy-assessment/command.yaml"
