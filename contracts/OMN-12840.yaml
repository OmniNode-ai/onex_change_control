---
schema_version: "1.0.0"
ticket_id: "OMN-12840"
title: "Reconcile stale endpoint-authority artifacts to shipped reality (doc/policy-only)"
summary: >-
  Three artifacts (docs/handoffs/2026-06-07-sea-comprehensive-fix-handoff.md lines 22/47/63/64, docs/handoffs/2026-06-04-delegation-provider-hardcoding-handoff.md
  line 73, and the Claude memory file reference_bifrost_bare_base_vs_complete_url.md) still assert the
  superseded bare-base+append / per-backend chat_path / shared-helper framing after OMN-12815 shipped
  the opposite (COMPLETE endpoint_url VERBATIM, all URL construction helpers deleted, fail-closed if empty).
  This ticket adds SUPERSEDED(OMN-12815) markers with forward pointers to all stale sections, fixes the
  memory file frontmatter (status: active → superseded, removes "being deleted" language), and adds tests/test_endpoint_authority_docs_consistency.py
  to enforce the corrected state in CI going forward. Doc-only: no source behaviour change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >-
      tests/test_endpoint_authority_docs_consistency.py (4 tests) must be GREEN: asserts SUPERSEDED(OMN-12815)
      markers present in both handoff docs; asserts memory file has status: superseded and lacks stale
      "being deleted" language; asserts memory file contains VERBATIM and lacks stale shared-helper-NOW
      / bare-base+append framing. Tests were RED before OMN-12840 annotations (3 failing) and GREEN after
      (4 passing).
    command: "python3 -m pytest tests/test_endpoint_authority_docs_consistency.py -v"
  - kind: "docs"
    description: >-
      SUPERSEDED(OMN-12815) HTML-comment markers with forward pointers added to: lines 22, 47, 63, 64
      of docs/handoffs/2026-06-07-sea-comprehensive-fix-handoff.md and line 73 of docs/handoffs/2026-06-04-delegation-provider-hardcoding-handoff.md.
      Memory file reference_bifrost_bare_base_vs_complete_url.md updated: status active → superseded;
      "being deleted" → past-tense "deleted by OMN-12815"; description updated.
    command: ""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests-green"
    description: >-
      4/4 tests in tests/test_endpoint_authority_docs_consistency.py pass GREEN after OMN-12840 annotations
      are applied. Prior to the annotations, 3/4 were RED: test_sea_handoff_omn12815_stale_sections_annotated,
      test_delegation_handoff_omn12815_stale_section_annotated, and test_bifrost_memory_file_is_superseded
      all failed. GREEN proof: all 4 tests pass in the omni_home worktree after the doc edits and memory
      file update are applied.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12840/dod-tests-green/command.yaml
          && grep -q '4 passed' drift/dod_receipts/OMN-12840/dod-tests-green/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC eligibility: this onex_change_control PR carries the OMN-12840 contract and receipts,
      satisfying the receipt-to-PR binding required by validator_occ_merge_eligibility. The omni_home
      PR cites Evidence-Source OCC SHA from this PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-12840.yaml && echo present"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: OMN-12840 is doc-only. It edits Markdown handoff files and the Claude memory
      file (not a tracked code artifact). It adds a Python test file under tests/ (excluded from the omni_home
      no-functional-code pre-commit hook). No runtime contract, topic, handler, or deployment artifact
      is changed. No live deploy or runtime restart is required or performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12840/dod-deploy-assessment/command.yaml
          && grep -q 'doc-only' drift/dod_receipts/OMN-12840/dod-deploy-assessment/command.yaml"
