---
schema_version: 1.0.0
ticket_id: OMN-10294
title: 'Task 12: [Phase 2] Audit quality-score-panel for primitive migration eligibility'
summary: >
  STAY-BESPOKE for both QualityScoreHistogram (2D) and QualityScoreTilted3D. 2D has threshold-line/mean-marker/pass-rate
  overlays not expressible via IBarChartAdapter.BarChartFieldMapping. 3D is a custom three.js tilted camera
  + translucent threshold plane + raycasting hover. Re-evaluate 2D migration after IBarChartAdapter gains
  overlay extension points.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: FULL
evidence_requirements:
  - kind: tests
    description: Audit tracking test asserting QualityScore router + sub-component shape
    command: cd omnidash && npm run test -- --run src/components/dashboard/quality/QualityScore.audit.test.tsx
  - kind: docs
    description: Decision comment in QualityScore.tsx router citing OMN-10294
    command: grep -F 'OMN-10294' omnidash/src/components/dashboard/quality/QualityScore.tsx
  - kind: ci
    description: Typecheck + pre-commit pass
    command: cd omnidash && npx tsc --noEmit && pre-commit run --all-files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: QualityScore.tsx contains OMN-10294 decision comment
    source: generated
    checks:
      - check_type: command
        check_value: grep -F 'OMN-10294' omnidash/src/components/dashboard/quality/QualityScore.tsx
  - id: dod-002
    description: Audit tracking test file exists
    source: generated
    checks:
      - check_type: file_exists
        check_value: omnidash/src/components/dashboard/quality/QualityScore.audit.test.tsx
  - id: dod-003
    description: Typecheck passes
    source: generated
    checks:
      - check_type: command
        check_value: cd omnidash && npx tsc --noEmit
  - id: dod-004
    description: Quality component tests pass
    source: generated
    checks:
      - check_type: command
        check_value: cd omnidash && npm run test -- --run src/components/dashboard/quality/
