---
schema_version: "1.0.0"
ticket_id: "OMN-15173"
title: "Autobind OCC evidence for OMN-15173"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2495."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2495 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2495"
    description: "PR #2495 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2495-ci"
    description: "PR #2495 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5110"
    description: "OCC companion PR #5110 — self-bind for OMN-15173 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-dev-redpanda-advertise-host-failfast"
    description: >
      Deploy-scope DoD for PR #2495's docker/docker-compose.infra.yml runtime-lane change. Hermetic content
      probe reads omnibase_infra product commit 0d92c1f8203cd97469128abb4ff4789e050033c3 and asserts the
      dev-lane Redpanda advertise host uses the fail-fast ${DEV_REDPANDA_ADVERTISE_HOST:?...} interpolation
      rather than the previous silent localhost default. This proves the source artifact that must be
      deployed, without mutating any live lane.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.infra.yml?ref=0d92c1f8203cd97469128abb4ff4789e050033c3'
          --jq .content | base64 -d | grep -q 'DEV_REDPANDA_ADVERTISE_HOST:?DEV_REDPANDA_ADVERTISE_HOST
          is required'
  - id: "occ-self-bind-pr-5128"
    description: "OCC repair PR #5128 — self-bind for OMN-15173 deploy-evidence append."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5128 --repo OmniNode-ai/onex_change_control --json number,state"
