---
schema_version: "1.0.0"
ticket_id: OMN-10439
title: "Implement Pattern B broker publish adapter"
summary: "Add the publish-side Pattern B broker adapter that accepts typed dispatch requests and publishes
  them through the event bus using contract-loaded topics."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Pattern B broker publish adapter tests pass"
    command: "uv run pytest tests/unit/nodes/node_pattern_b_broker -q"
  - kind: "manual"
    description: "Touched Pattern B broker files pass lint"
    command: "uv run ruff check src/omnimarket/nodes/node_pattern_b_broker tests/unit/nodes/node_pattern_b_broker"
  - kind: "manual"
    description: "Ticket contract validates"
    command: "uv run validate-yaml contracts/OMN-10439.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Pattern B broker publish adapter tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_pattern_b_broker -q"
  - id: "dod-002"
    description: "Touched Pattern B broker files pass lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_pattern_b_broker tests/unit/nodes/node_pattern_b_broker"
  - id: "dod-003"
    description: "Ticket contract validates"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run validate-yaml contracts/OMN-10439.yaml"
  - id: "dod-deploy"
    description: |
      Deploy posture for this slice. OMN-10439 adds an async publish adapter
      and typed publish receipt on top of the OMN-10438 broker contract/model
      boundary. Unit tests use EventBusInmemory and no live Kafka, no CLI shell,
      no subprocess, and no long-running consumer process is added in this PR.
      Follow-up tickets carry runtime consumer and quality-gate deploy evidence.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for OMN-10439 - publish adapter tested with EventBusInmemory
          only; follow-up tickets carry runtime consumer deploy evidence'"
