---
schema_version: "1.0.0"
ticket_id: "OMN-12732"
title: "Delegation projection drops useful prompt/response/token fields after terminal success"
summary: >-
  Central evidence for omnimarket PR #1063. The PR fixes the delegation projection
  reducer so canonical delegation terminal events enrich existing delegation_events
  rows with prompt text, response text, and token metrics instead of being ignored
  after the compatibility task-delegated row has already materialized.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "projections"
  - "database"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1063"
    description: "omnimarket PR #1063 carries the delegation projection fidelity fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12732/dod-omnimarket-pr-1063/command.yaml"
          && grep -q '^pr_number: 1063$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12732/dod-omnimarket-pr-1063/command.yaml"
  - id: "dod-local-validation"
    description: "Focused projection reducer tests and adjacent dispatch terminal parsing tests passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12732/dod-local-validation/command.yaml"
          && grep -q '49 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12732/dod-local-validation/command.yaml"
          && grep -q '5 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12732/dod-local-validation/command.yaml"
          && grep -q 'test_delegation_terminal_enriches_nested_result_payload' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12732/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Runtime impact is bounded to post-merge dev-lane deploy and repeated useful delegation projection validation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12732/dod-deploy-assessment/command.yaml"
          && grep -q 'post-merge dev-lane deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12732/dod-deploy-assessment/command.yaml"
          && grep -q 'docker exec' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12732/dod-deploy-assessment/command.yaml"
          && grep -q 'rpk topic consume' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12732/dod-deploy-assessment/command.yaml"
