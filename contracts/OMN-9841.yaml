---
schema_version: 1.0.0
ticket_id: OMN-9841
title: 'Task 19: [Epic B] Move omnibase_infra/contracts/OMN-*.yaml to onex_change_control/contracts/'
summary: 'Task 19: [Epic B] Move omnibase_infra/contracts/OMN-*.yaml to onex_change_control/contracts/'
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: tests
    description: Unit tests pass (where applicable)
    command: uv run pytest tests/ -m unit -x
  - kind: ci
    description: CI pipeline green on the PR
    command: gh pr checks
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Contract YAML for this ticket parses as valid YAML
    source: generated
    checks:
      - check_type: command
        check_value: uv run python -c "import yaml; yaml.safe_load(open('contracts/${TICKET_ID}.yaml').read());
          print('ok')"
  - id: dod-002
    description: PR title or body cites this ticket
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json title,body --jq '.title + " " + (.body
          // "")' | grep -q '${TICKET_ID}'
