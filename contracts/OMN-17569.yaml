---
schema_version: "1.0.0"
ticket_id: "OMN-17569"
title: "Autobind OCC evidence for OMN-17569"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #352."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #352 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #352 on OmniNode-ai/omniweb changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (package.json, packages/@omninode/ui/src/components/ApiKeyRow.tsx, packages/@omninode/ui/src/index.ts, tests/api-key-copy-affordance.test.ts) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniweb-pr-352"
    description: "PR #352 on OmniNode-ai/omniweb — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 352 --repo OmniNode-ai/omniweb --json number,state"
  - id: "dod-OmniNode-ai-omniweb-pr-352-ci"
    description: "PR #352 on OmniNode-ai/omniweb — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 352 --repo OmniNode-ai/omniweb --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omniweb-pr-352"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
