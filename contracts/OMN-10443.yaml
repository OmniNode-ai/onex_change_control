---
schema_version: "1.0.0"
ticket_id: OMN-10443
title: "Overseer Verifier Live PR Checks"
summary: "Update node_overseer_verifier to use the current GitHub CLI check schema and make the --pr CLI
  path run live PR check evidence."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: "manual"
    description: "Focused overseer verifier CLI and PR check tests pass"
    command: "uv run pytest tests/test_handler_overseer_verifier_pr_checks.py tests/test_node_overseer_verifier_cli.py
      -q"
  - kind: "manual"
    description: "Golden-chain overseer verifier tests pass"
    command: "uv run pytest tests/test_golden_chain_overseer_verifier.py tests/test_golden_chain_overseer_verifier_consumer.py
      -q"
  - kind: "ci"
    description: "omnimarket PR #484 checks pass across required gates"
    command: "gh pr checks 484 --repo OmniNode-ai/omnimarket --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused overseer verifier CLI and PR check tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_handler_overseer_verifier_pr_checks.py tests/test_node_overseer_verifier_cli.py
          -q"
  - id: "dod-002"
    description: "Golden-chain overseer verifier tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_golden_chain_overseer_verifier.py tests/test_golden_chain_overseer_verifier_consumer.py
          -q"
  - id: "dod-003"
    description: "Live PR dogfood verifies the --pr path runs pr_checks_live"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python -m omnimarket.nodes.node_overseer_verifier --pr omnimarket#483 --json"
  - id: "dod-deploy"
    description: "Post-merge deploy smoke imports the overseer verifier CLI in the runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_overseer_verifier.__main__
          import _parse_pr; print(_parse_pr('omnimarket#484'))\""
