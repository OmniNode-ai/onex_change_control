---
schema_version: "1.0.0"
ticket_id: "OMN-13291"
title: "W0: make validate-validator-requirements a real fleet gate + formalize baseline schema"
summary: >-
  Exports validate-validator-requirements as a remote pre-commit hook in omnibase_core and formalizes
  architecture-handshakes/validator-requirements-baseline.yaml with a versioned schema (schema_version,
  per-repo classification, explicit required/optional/N_A semantics, report_format: all_gaps). Wires the
  meta-gate as a remote PC hook + CI workflow into the repos that lacked it: omnimarket, omnibase_infra,
  omnibase_spi, omnibase_compat, omniclaude, omniintelligence, omnimemory (onex_change_control was wired
  by OMN-13299/#2775). The meta-gate reports ALL missing gates per run (never fail-on-first) and blocks.
  W0 of the validator-standardization remediation plan (§3 W0); parent epic OMN-9048.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr-2800"
    description: >-
      OCC self-binding receipt for onex_change_control PR #2800. The PR replaces #2799 with a branch name
      that satisfies identity binding for OMN-13291 and carries the central evidence update for OMN-13291
      (validate-validator-requirements remote-hook export and baseline schema formalization), including
      active omnibase_core PR #1280 binding receipts. This receipt binds the OCC evidence PR itself by
      pr_number so its own OCC preflight and Receipt Gate can pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-occ-pr-2800/command.yaml
  - id: "dod-occ-pr-2799"
    description: >-
      OCC self-binding receipt for onex_change_control PR #2799. The PR carries the central evidence update
      for OMN-13291 (validate-validator-requirements remote-hook export and baseline schema formalization),
      including active omnibase_core PR #1280 binding receipts. This receipt binds the OCC evidence PR
      itself by pr_number so its own OCC preflight and Receipt Gate can pass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-occ-pr-2799/command.yaml
  - id: "dod-omnibase_core-pr-1280"
    description: >-
      Consolidation binding receipt for omnibase_core PR #1280. The PR carries the active validator-fleet
      consolidation for OMN-13291 (validate-validator-requirements remote-hook export and baseline schema
      formalization) after superseding closed PR #1279. This receipt binds the central OCC contract to
      the active consolidated citing PR by pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-omnibase_core-pr-1280/command.yaml
  - id: "dod-omnibase_core-pr-1279"
    description: >-
      Consolidation binding receipt for omnibase_core PR #1279. The PR unions the conflict-coupled validator-fleet
      additions for OMN-13291 (validate-validator-requirements remote-hook export and baseline schema
      formalization) into a single queue-safe branch. This receipt binds the central OCC contract to the
      consolidated citing PR by pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-omnibase_core-pr-1279/command.yaml
  - id: "dod-omnibase_core-pr-1275"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1275 (base=dev). The PR exports the validate-validator-requirements
      remote hook + formalizes the baseline schema. This receipt binds OMN-13291 to omnibase_core PR #1275
      by pr_number and commit_sha so downstream Receipt Gate can resolve central OCC evidence for the
      citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-omnibase_core-pr-1275/command.yaml
  - id: "dod-omnimarket-pr-1276"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1276 (base=dev). The PR wires the validate-validator-requirements
      meta-gate (remote PC hook + CI + baseline). This receipt binds OMN-13291 to omnimarket PR #1276
      by pr_number and commit_sha so downstream Receipt Gate can resolve central OCC evidence for the
      citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-omnimarket-pr-1276/command.yaml
  - id: "dod-omnibase_infra-pr-2039"
    description: >-
      Per-code-PR binding receipt for omnibase_infra PR #2039 (base=dev). The PR wires the validate-validator-requirements
      meta-gate (remote PC hook + CI + baseline). This receipt binds OMN-13291 to omnibase_infra PR #2039
      by pr_number and commit_sha so downstream Receipt Gate can resolve central OCC evidence for the
      citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-omnibase_infra-pr-2039/command.yaml
  - id: "dod-omnibase_spi-pr-238"
    description: >-
      Per-code-PR binding receipt for omnibase_spi PR #238 (base=dev). The PR wires the validate-validator-requirements
      meta-gate (remote PC hook + CI + baseline). This receipt binds OMN-13291 to omnibase_spi PR #238
      by pr_number and commit_sha so downstream Receipt Gate can resolve central OCC evidence for the
      citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-omnibase_spi-pr-238/command.yaml
  - id: "dod-omnibase_compat-pr-156"
    description: >-
      Per-code-PR binding receipt for omnibase_compat PR #156 (base=dev). The PR wires the validate-validator-requirements
      meta-gate (remote PC hook + CI + baseline). This receipt binds OMN-13291 to omnibase_compat PR #156
      by pr_number and commit_sha so downstream Receipt Gate can resolve central OCC evidence for the
      citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-omnibase_compat-pr-156/command.yaml
  - id: "dod-omniclaude-pr-1781"
    description: >-
      Per-code-PR binding receipt for omniclaude PR #1781 (base=dev). The PR wires the validate-validator-requirements
      meta-gate (remote PC hook + CI + baseline). This receipt binds OMN-13291 to omniclaude PR #1781
      by pr_number and commit_sha so downstream Receipt Gate can resolve central OCC evidence for the
      citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-omniclaude-pr-1781/command.yaml
  - id: "dod-omniintelligence-pr-721"
    description: >-
      Per-code-PR binding receipt for omniintelligence PR #721 (base=dev). The PR wires the validate-validator-requirements
      meta-gate (remote PC hook + CI + baseline). This receipt binds OMN-13291 to omniintelligence PR
      #721 by pr_number and commit_sha so downstream Receipt Gate can resolve central OCC evidence for
      the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-omniintelligence-pr-721/command.yaml
  - id: "dod-omnimemory-pr-372"
    description: >-
      Per-code-PR binding receipt for omnimemory PR #372 (base=dev). The PR wires the validate-validator-requirements
      meta-gate (remote PC hook + CI + baseline). This receipt binds OMN-13291 to omnimemory PR #372 by
      pr_number and commit_sha so downstream Receipt Gate can resolve central OCC evidence for the citing
      PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-omnimemory-pr-372/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13291 contract and all 8 code-PR
      bindings. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check can resolve
      a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13291/dod-occ-pr-self/command.yaml
