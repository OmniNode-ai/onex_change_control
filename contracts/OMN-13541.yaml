---
schema_version: "1.0.0"
ticket_id: "OMN-13541"
title: "Add code_review to delegation wire task_type taxonomy"
summary: >-
  Central OCC evidence for omnibase_core PR #1318. The PR adds code_review to the closed ModelDelegationRequest.task_type
  Literal so consumer-facing delegation requests accepted by omnimarket can cross the omnibase_core wire
  DTO without pydantic rejection.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused delegation wire model tests accept code_review and preserve the existing task_type
      set."
    command: "uv run pytest tests/unit/models/delegation/wire/test_delegation_wire_models.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1318"
    description: "omnibase_core PR #1318 carries the additive code_review task_type Literal change."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13541/dod-core-pr-1318/command.yaml"
  - id: "dod-focused-wire-tests"
    description: "Focused delegation wire model tests pass on the PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13541/dod-focused-wire-tests/command.yaml"
  - id: "dod-omnimarket-pr-1370"
    description: "omnimarket PR #1370 carries the code_review task-class contract, the cheap_cloud routing
      fallback, the local wire DTO Literal sync, the CLI system prompt, and the focused unit tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13541/dod-omnimarket-pr-1370/command.yaml"
  - id: "dod-live-code-review"
    description: "Live dev-lane proof: a fresh code_review delegation terminalizes with real inference
      (non-empty model_name + tokens>0), where before the fix it Pattern-B timed out with a blank row."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13541/dod-live-code-review/command.yaml"
  - id: "dod-occ-pr-self"
    description: "OCC self-binding receipt for the central evidence PR carrying the OMN-13541 contract
      and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13541/dod-occ-pr-self/command.yaml"
