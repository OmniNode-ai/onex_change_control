---
schema_version: "1.0.0"
ticket_id: "OMN-17856"
title: "fix(OMN-17856): the sanctioned raw-producer construction moved file, so its allowlist entry follows
  it"
summary: >-
  Relocation of one already-sanctioned imperative-contract entry in allowlists/omnimarket.yaml. OMN-17772
  extracts SnapshotDeltaPublisher out of BaseProjectionRunner so a def-B handler on the runtime's DB-injection
  arm can publish a snapshot delta without becoming a runner subclass, and the AIOKafkaProducer(...) call
  moved with it from src/omnimarket/projection/runner.py to src/omnimarket/projection/snapshot_publisher.py.
  The new entry carries the SAME freestanding_kafka_client violation and the SAME sanctioning ticket OMN-12750
  the runner.py entry already carries; runner.py keeps its entry because it still constructs the AIOKafkaConsumer.
  The violation count across the two files is unchanged, which is what distinguishes this from the prohibited
  move of allowlisting a new violation to unblock a gate. Central OCC allowlist only; no runtime, deploy
  or scanner change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-allowlist-follows-producer"
    description: >-
      The producer construction this entry sanctions really is at the path the entry names, and it really
      did leave the path the neighbouring entry names. Both facts are read out of OmniNode-ai/omnimarket
      at the exact commit the guard ran against -- a repository this PR does not author -- so the verdict
      cannot be decided by text written here.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/projection/snapshot_publisher.py?ref=224ed2baf4dd856c6dec36f5eb1c3fd2c892f497 --jq '.content' | base64 -d | grep -c 'producer = AIOKafkaProducer('
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/projection/runner.py?ref=224ed2baf4dd856c6dec36f5eb1c3fd2c892f497 --jq '.content' | base64 -d | grep -c 'self._consumer = AIOKafkaConsumer('
  - id: "dod-occ-companion-pr-8171"
    description: >-
      OCC self-binding: onex_change_control PR #8171 carries this contract and its receipts for the omnimarket
      imperative-contract allowlist relocation, and it does touch allowlists/omnimarket.yaml. Read from
      GitHub's record of the PR rather than from the tree this PR writes.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/pulls/8171/files --paginate --jq '.[].filename' | grep -cFx 'allowlists/omnimarket.yaml'
      - check_type: "command"
        check_value: "gh pr view 8171 --repo OmniNode-ai/onex_change_control --json number,state"
