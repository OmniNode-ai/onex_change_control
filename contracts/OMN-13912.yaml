---
schema_version: "1.0.0"
ticket_id: "OMN-13912"
title: "runner monitor alert cron durability"
summary: >-
  The self-hosted runner-fleet alert monitor was installed via a cron line that did not source .monitor-env,
  so under cron the monitor could not resolve its host/token config and silently stopped alerting on offline
  listeners. omnibase_infra PR #2192 installs the monitor cron through bash so .monitor-env loads, captures
  monitor setup failures in /tmp/runner-monitor.log, uses the Tailscale MagicDNS hostname for the fleet
  deploy/monitor host, keeps offline listeners alert-only (no auto-remediation), survives listener self-update
  exits, and adds unit coverage so rebuild deploys do not regress to the broken cron line. Config + scripts
  + tests only; no runtime contract or Dockerfile.runtime change (deploy-gate not applicable). Also rebases
  the branch onto dev (originally cut from main) and COPYs omni-curl instead of the BuildKit-only heredoc
  so runner-image-build-smoke passes under the legacy builder (shared root cause with OMN-13915).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_infra-pr-2192"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2192 makes the runner monitor alert cron durable (bash-sourced
      .monitor-env, MagicDNS host, alert-only offline handling, self-update survival) with unit coverage,
      and COPYs omni-curl to unblock runner-image-build-smoke under the legacy builder. Config + scripts
      + tests only.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13912/dod-omnibase_infra-pr-2192/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13912 contract and receipt consumed
      by omnibase_infra PR #2192.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13912/dod-occ-self/command.yaml"
  - id: "dod-omnibase_infra-pr-2958"
    description: >-
      The ticket's ORIGINAL DoD, discharged. omnibase_infra PR #2958 adds the post-publish version-bump
      step the ticket was opened for: scripts/ci/post_release_dev_bump.py (pure decide() + table-scoped
      write) plus the release.yml job post-release-dev-bump that applies it, re-locks uv.lock and opens
      an auto-merging PR against dev. Behaviour proof is a real pytest run over 35 tests: 21 unit tests
      on the decision (armed dev==published => bump, behind dev<published => bump past published, already-ahead
      => idempotent noop, rc refused, write is table-scoped), 10 CI tests pinning the workflow wiring
      (job exists, needs release, gated on needs.release.outputs.version NOT on job success, never fires
      for rc, lands via a PR against dev never a direct push, PR title carries an OMN ref), and 4 incident-replay
      tests driving the guard over the verbatim pyproject.toml of 4529c3486 -- the commit v0.38.11 tags,
      declaring version = "0.38.11" -- which the guard must call ARMED.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13912/dod-omnibase_infra-pr-2958/command.yaml"
  - id: "dod-omnibase_infra-pr-2958-ci"
    description: >-
      Product diff scope for PR #2958 on OmniNode-ai/omnibase_infra (OMN-14425).
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2958 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "occ-self-bind-pr-7382"
    description: >-
      OCC companion PR #7382 -- self-bind for the OMN-13912 post-release-dev-bump evidence (OMN-14650).
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7382 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omnibase_infra-pr-2958-wiring"
    description: >-
      Hosted, out-of-diff, falsifiable: the release.yml job that performs the post-release disarm is actually
      present on omnibase_infra at the reviewed sha, and the bump script it calls exists there too. Fails
      if either is absent or renamed.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/release.yml?ref=50ab8952d9bd5fe151a09a542142311ee1e168ae --jq '.content' | base64 -d | grep -c 'post-release-dev-bump'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/ci/post_release_dev_bump.py?ref=50ab8952d9bd5fe151a09a542142311ee1e168ae --jq '.content' | base64 -d | grep -c 'def decide'
