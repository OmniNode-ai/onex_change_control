---
schema_version: 1.0.0
ticket_id: OMN-11173
title: Publish proof of life overseer through bus
summary: Wire overseer proof-of-life event publication through the configured event bus so overseer liveness
  events are observable at the Kafka/bus layer.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - protocols
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Overseer bus publisher tests pass verifying event publication wiring.
    command: uv run pytest tests/ -q -k "overseer or proof_of_life or bus"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-overseer-bus-publisher
    description: Overseer proof-of-life events are published through the configured bus — verified by
      omnimarket PR  #742.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' drift/dod_receipts/OMN-11173/dod-overseer-bus-publisher/command.yaml
          && grep -q '^pr_number: 742$' drift/dod_receipts/OMN-11173/dod-overseer-bus-publisher/command.yaml
