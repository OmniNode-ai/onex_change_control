---
schema_version: "1.0.0"
ticket_id: "OMN-13854"
title: "feat(dod-sweep): flag Done tickets with startedAt=null + zero attachments as gate-escape indicators"
summary: >-
  omnimarket PR #1583 adds the L3 close-path audit layer to node_dod_sweep_orchestrator: a gate_escape_audit
  mode that flags Done tickets carrying the wf_1628d9a5 signature (startedAt=null, zero attachments/documents,
  no merged PR discoverable) after excluding the design doc's carve-outs, with regression fixtures proving
  the known bad batches are flagged and a control window of legitimate closes produces zero false positives.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1583"
    description: >-
      Consuming-PR binding: omnimarket PR #1583 carries the OMN-13854 gate-escape audit implementation,
      the wf_1628d9a5/OMN-13500 batch regression, and the control-window false-positive check.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13854/dod-omnimarket-pr-1583/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13854 contract and receipts consumed
      by omnimarket PR #1583.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13854/dod-occ-self/command.yaml"
