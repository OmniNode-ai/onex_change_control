---
schema_version: "1.0.0"
ticket_id: OMN-8002
title: Port skills to thin shells over deterministic omnimarket nodes
summary: "Port skills to thin shells over deterministic omnimarket nodes"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR is open against main with OMN-8002 cited"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8002/dod-001/command.yaml && grep
          -q '^evidence_item_id: \"dod-001\"$' drift/dod_receipts/OMN-8002/dod-001/command.yaml"
  - id: "dod-002"
    description: "Changed skill files route through onex run-node primary paths"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8002/dod-002/command.yaml && grep
          -q '^evidence_item_id: \"dod-002\"$' drift/dod_receipts/OMN-8002/dod-002/command.yaml"
  - id: "dod-003"
    description: "Changed files are limited to skill wiring and the contract validator helper"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8002/dod-003/command.yaml && grep
          -q '^evidence_item_id: \"dod-003\"$' drift/dod_receipts/OMN-8002/dod-003/command.yaml"
  - id: "dod-004"
    description: "PR verification records local pre-commit and CI follow-up"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8002/dod-004/command.yaml && grep
          -q '^evidence_item_id: \"dod-004\"$' drift/dod_receipts/OMN-8002/dod-004/command.yaml"
