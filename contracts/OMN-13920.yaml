---
schema_version: "1.0.0"
ticket_id: "OMN-13920"
title: "fix(OMN-13920): bus_audit resolves topics.yaml package-relative; vacuous zero-topic audits fail
  loudly"
summary: >-
  omnimarket PR #1601 fixes the bus_audit skill executable-path defect. node_bus_audit_compute resolved
  its default topic registry and contract root via parents[5]/src/omnimarket/... — a repo-root reconstruction
  that only exists in a source checkout, so an installed wheel found no registry and onex skill bus_audit
  audited zero topics with a success receipt (vacuous pass). The fix resolves defaults relative to the
  omnimarket package dir (parents[3]), identical in source checkout and installed wheel, and fails loudly
  on a missing packaged registry or a zero-contract/zero-topic audit; the CLI exits 1 on ERROR status.
  Runtime rebuild/deploy is outside this OCC evidence PR and remains operator-gated.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1601"
    description: >-
      Consuming-PR binding: omnimarket PR #1601 carries the OMN-13920 package-relative default resolution
      and fail-loud guards. Real probe: gh pr view 1601 --repo OmniNode-ai/omnimarket --json number,state,headRefOid,headRefName,title,files.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13920/dod-omnimarket-pr-1601/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment: omnimarket PR #1601 touches node_bus_audit_compute runtime paths (handler
      + __main__), so deploy-gate requires deploy evidence. This is a pure package-relative path-resolution
      + fail-loud fix with no schema/topic change; it performs no live deploy, docker exec, or rpk topic
      produce. Runtime rebuild/redeploy of the omninode-runtime image remains operator-gated and is outside
      this evidence PR.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13920/dod-deploy-assessment/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-13920/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying this OMN-13920 contract and the consuming-PR
      evidence for omnimarket PR #1601.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13920/dod-occ-self/command.yaml"
