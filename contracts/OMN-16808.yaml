---
schema_version: "1.0.0"
ticket_id: "OMN-16808"
title: "OCC evidence for OMN-16808 — autoclose sweep comment idempotency"
summary: >
  OCC evidence companion for OmniNode-ai/omnibase_infra PR #2954: the evidence-autoclose sweep reads a
  ticket's existing comments before any gap write and refuses to repeat a statement it has already made,
  failing closed when that history cannot be read.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2954 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2954"
    description: >
      PR #2954 on OmniNode-ai/omnibase_infra — the read-before-write gate exists on the merged handler:
      every gap comment funnels through _emit_gap_comment, which consults _dedup_gate before any create_comment
      call.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py?ref=547c7e5f92c2fdddee9cabf248293e07069ada0b --jq '.content' | base64 -d | grep -c 'async def _emit_gap_comment'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py?ref=547c7e5f92c2fdddee9cabf248293e07069ada0b --jq '.content' | base64 -d | grep -c 'async def fetch_comment_bodies'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2954-decision"
    description: >
      The suppressed-duplicate outcome is a first-class decision, not a silent no-op: SKIPPED_DUPLICATE_COMMENT
      exists on the decision enum at the merged ref.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/models/enum_evidence_autoclose_decision.py?ref=547c7e5f92c2fdddee9cabf248293e07069ada0b --jq '.content' | base64 -d | grep -c 'SKIPPED_DUPLICATE_COMMENT'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2954-tests"
    description: >
      The idempotency suite exists and pins the headline rule — one fixture window scanned twice writes
      exactly one comment.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/unit/nodes/node_evidence_autoclose_sweep_effect/test_omn_16808_comment_idempotency.py?ref=547c7e5f92c2fdddee9cabf248293e07069ada0b --jq '.content' | base64 -d | grep -c 'test_same_window_twice_posts_exactly_one_gap_comment'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2954-lookback"
    description: >
      OMN-16792 in-scope half: the sweep workflow's lookback now exceeds the observed p100 inter-run gap
      (236 min) rather than sitting under it at 2h.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/evidence-autoclose-sweep.yml?ref=547c7e5f92c2fdddee9cabf248293e07069ada0b --jq '.content' | base64 -d | grep -c "lookback_hours || '6'"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2954 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py?ref=547c7e5f92c2fdddee9cabf248293e07069ada0b --jq '.content' | base64 -d | grep -c 'async def _emit_gap_comment'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
