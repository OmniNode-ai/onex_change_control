---
schema_version: "1.0.0"
ticket_id: OMN-10535
title: "Wire core orchestration shims as runtime-effects consumers"
summary: "Bind omnimarket runtime ingress wiring PR to OCC evidence"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - topics
  - events
evidence_requirements:
  - kind: "tests"
    description: "Consumer topic guard and focused consumer tests pass"
    command: "python scripts/lint_no_hardcoded_topics.py && uv run pytest tests/test_overnight_consumer.py
      tests/test_build_loop_orchestrator_consumer.py -q"
  - kind: "ci"
    description: "Receipt gate and required CI checks pass on omnimarket PR #526"
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Production consumer topic literals removed from new runtime ingress consumers"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10535/dod-001/command.yaml\"; else python scripts/lint_no_hardcoded_topics.py;
          fi"
  - id: "dod-002"
    description: "Focused consumer regression tests pass"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10535/dod-002/command.yaml\"; else uv run pytest
          tests/test_overnight_consumer.py tests/test_build_loop_orchestrator_consumer.py -q; fi"
  - id: "dod-003"
    description: "Runtime ingress PR branch is updated with main and pushed"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json mergeStateStatus,headRefOid"
  - id: "dod-occ-validation"
    description: "OCC evidence PR validates against receipt gate"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10535/dod-occ-validation/command.yaml\""
