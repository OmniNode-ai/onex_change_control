---
schema_version: "1.0.0"
ticket_id: "OMN-17523"
title: "Typed route-event identity references"
summary: >-
  Exact-head evidence for OmniNode-ai/omnibase_core PR #1668. The Core-only change models the router-owned
  decision id as UUID and the served model as an explicit provider-qualified immutable reference; rejected
  events use null when no model was attempted.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
evidence_requirements:
  - kind: "tests"
    description: "The focused route-event DTO suite passes on the product head."
    command: "uv run pytest tests/unit/models/routing/test_model_llm_route_events.py -q"
  - kind: "ci"
    description: "The product PR remains associated with its exact head."
    command: "gh pr view 1668 --repo OmniNode-ai/omnibase_core --json headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1668-route-event-contract"
    description: >-
      Core PR #1668 at c33ca209180e97ef665896c7c7375530b5087f56 exposes UUID decision ids, provider-qualified
      served-model references, and the rejected-event null representation in the exact changed DTO/test
      paths.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          test "$(gh api repos/OmniNode-ai/omnibase_core/pulls/1668/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_core/models/routing/model_served_model_ref.py')" = "1" && test "$(gh api repos/OmniNode-ai/omnibase_core/pulls/1668/files --paginate --jq '.[].filename' | grep -cFx 'tests/unit/models/routing/test_model_llm_route_events.py')" = "1"
  - id: "dod-omnibase-core-pr-1668-route-event-tests"
    description: >-
      Focused route-event tests validate UUID JSON serialization, provider/model reference shape, nonblank
      names, provider agreement, and rejected-event nullability on the exact product head.
    source: "manual"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/models/routing/test_model_llm_route_events.py -q
        cwd: "${OMNI_HOME}/omnibase_core"
  - id: "occ-self-bind-pr-8759"
    description: >-
      Self-binding receipt for OCC PR #8759, the evidence companion carrying this OMN-17523 contract and
      its exact-head product receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8759 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
