---
schema_version: "1.0.0"
ticket_id: "OMN-17549"
title: "Autobind OCC evidence for OMN-17549"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2274.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2274 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2274 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: |-
      uv run pytest tests/chains/test_event_chain_gate_projection_write.py tests/validators/test_event_registry_drift.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2274"
    description: "PR #2274 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/tests/chains/test_event_chain_gate_projection_write.py?ref=69f6e0738e5ff412cfacd51b89e3a8786d520aed --jq '.content' | base64 -d | grep -c 'def test_a_write_that_produces_no_row_fails_closed'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2274/files --paginate --jq '.[].filename' | grep -cFx 'pyproject.toml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2274 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/chains/test_event_chain_gate_projection_write.py tests/validators/test_event_registry_drift.py -q
        cwd: "${OMNI_HOME}/omnimarket"
