---
schema_version: "1.0.0"
ticket_id: "OMN-12218"
title: "Create node_runner_orchestrator contract + handler stub"
summary: >
  OCC evidence for omnimarket PR #912. The change adds node_runner_orchestrator (ORCHESTRATOR) to omnimarket
  wrapping the runner skill. Contract-only stub with node_not_implemented: true and 13 golden-chain unit
  tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain"
    description: "13 golden-chain tests pass for node_runner_orchestrator on omnimarket PR #912 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12218/dod-golden-chain/command.yaml
          && grep -q '13 passed' drift/dod_receipts/OMN-12218/dod-golden-chain/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy or runtime restart is required before merge for this contract + handler-stub
      slice."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12218/dod-deploy-assessment/command.yaml
          && grep -q 'no live deploy or runtime restart required before merge' drift/dod_receipts/OMN-12218/dod-deploy-assessment/command.yaml"
