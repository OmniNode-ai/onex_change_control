---
schema_version: "1.0.0"
ticket_id: "OMN-18035"
title: "Autobind OCC evidence for OMN-18035"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2405."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2405 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2405 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: "uv run pytest tests/unit/projection/test_omn_18035_evidence_cursor_truncation.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2405"
    description: "PR #2405 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/tests/unit/projection/test_omn_18035_evidence_cursor_truncation.py?ref=f695e525e3a30ca927b52c25db9a2276cda627db --jq '.content' | base64 -d | grep -c 'def _cfg'
  - id: "dod-OmniNode-ai-omnimarket-pr-2405-ci"
    description: "PR #2405 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2405 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2405 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/projection/test_omn_18035_evidence_cursor_truncation.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-8661"
    description: "OCC companion PR #8661 — self-bind for OMN-18035 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8661 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "ac1-complete-page-advertises-no-cursor"
    description: >-
      AC1 binding. Runs the single test that executes the criterion's own sentence — a complete page from
      /v1/evidence-pipeline/* returns next_cursor: null. Named as its own entry rather than added to a
      merged item, because every existing entry on this contract is merged and the Append-Only Gate holds
      them immutable (ci.yml:1446).
    source: "manual"
    status: "verified"
    binds_ac: ["AC1"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/projection/test_omn_18035_evidence_cursor_truncation.py::test_complete_page_advertises_no_cursor -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "ac2-truncated-page-advertises-a-cursor"
    description: >-
      AC2 binding. Runs the single test that executes the criterion's own sentence — a truncated page
      returns a non-null next_cursor. Paired with the AC1 entry: the same rows answer differently by truncation,
      so the two together are what make either meaningful.
    source: "manual"
    status: "verified"
    binds_ac: ["AC2"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/projection/test_omn_18035_evidence_cursor_truncation.py::test_truncated_page_advertises_a_cursor -q
        cwd: "${OMNI_HOME}/omnimarket"
