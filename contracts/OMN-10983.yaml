---
schema_version: "1.0.0"
ticket_id: "OMN-10983"
title: "Graduate omnimarket PR review bot to a required blocking gate"
summary: >
  Updates omnimarket's PR Review Bot workflow copy so a blocking verdict is presented as a merge-blocking
  check instead of rollout or informational language.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "The omnimarket workflow YAML parses after the rebase."
    command: "uv run python -c \"import yaml; yaml.safe_load(open('.github/workflows/pr-review-bot.yml'));
      print('PASS: parsed .github/workflows/pr-review-bot.yml')\""
  - kind: "ci"
    description: "omnimarket PR #699 is bound to this ticket at the observed head."
    command: "gh pr view 699 --repo OmniNode-ai/omnimarket --json headRefOid,title"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Workflow YAML parses after the rebase."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10983/dod-001/command.yaml\"; else uv run python
          -c \"import yaml; yaml.safe_load(open('.github/workflows/pr-review-bot.yml')); print('PASS:
          parsed .github/workflows/pr-review-bot.yml')\"; fi"
  - id: "dod-002"
    description: "Bind OMN-10983 evidence to omnimarket PR #699 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "REPO=OmniNode-ai/omnimarket PR_NUMBER=699 gh pr view ${PR_NUMBER} --repo ${REPO}
          --json headRefOid,title"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10983 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10983/dod-occ-pr/command.yaml\""
