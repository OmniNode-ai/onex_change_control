---
schema_version: 1.0.0
ticket_id: OMN-11272
title: Add phase budget and parallel dispatch fields to ModelSessionPhaseSpec
summary: omnibase_core PR  #1117 adds DoD evidence confirming the phase budget fields (max_duration_minutes, budget_warning_pct, parallel_with) and contract-level fields (max_parallel_workers, max_daily_cost_usd) landed in #1109 with 9 unit tests passing.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
  - protocols
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Phase budget unit tests pass against omnibase_core PR source tree.
    command: env PYTHONPATH=src uv run pytest tests/unit/models/overseer/test_phase_budget.py -q
  - kind: manual
    description: onex_change_control#1180 binds the phase budget test receipt to central OCC evidence.
    command: gh pr checks 1180 --repo OmniNode-ai/onex_change_control
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-phase-budget-unit-tests
    description: 9 unit tests covering all phase budget and parallel dispatch fields pass at product PR
      head.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11272/dod-phase-budget-unit-tests/command.yaml"
          && grep -q '^pr_number: 1117$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11272/dod-phase-budget-unit-tests/command.yaml"
          && grep -q '^verifier: agent$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11272/dod-phase-budget-unit-tests/command.yaml"
  - id: dod-onex-change-control-pr-1180
    description: onex_change_control#1180 evidence correction is bound to OMN-11272.
    source: generated
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11272/dod-onex-change-control-pr-1180/command.yaml"
          && grep -q '^ticket_id: OMN-11272$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11272/dod-onex-change-control-pr-1180/command.yaml"
          && grep -q '^pr_number: 1180$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11272/dod-onex-change-control-pr-1180/command.yaml"
