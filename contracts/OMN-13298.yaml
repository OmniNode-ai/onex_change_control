---
schema_version: "1.0.0"
ticket_id: "OMN-13298"
title: "feat(OMN-12803): url-authority fleet wiring — omnibase_infra, omnimarket, omniclaude, omniintelligence,
  omnimemory"
summary: >-
  Wires the existing check-url-authority remote pre-commit hook (already exposed in omnibase_core/.pre-commit-hooks.yaml)
  and companion url-authority-gate.yml CI workflow into five repos still missing it: omnibase_infra, omnimarket,
  omniclaude, omniintelligence, omnimemory. Pure wiring — no new validator code. Existing violations are
  grandfathered in the shared baseline seeded in omnibase_core PR #1264. Only NEW hardcoded URL fingerprints
  fail the gate going forward. Child of OMN-12803 (url-authority ratchet epic).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_infra-pr-2035"
    description: >-
      Per-code-PR binding receipt for omnibase_infra PR #2035 (jonah/omn-13298-url-authority-omnibase_infra,
      base=dev). Wires check-url-authority pre-commit hook and url-authority-gate.yml CI workflow. This
      receipt binds OMN-13298 to omnibase_infra PR #2035 by pr_number and commit_sha so downstream Receipt
      Gate can resolve central OCC evidence for the citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13298/dod-omnibase_infra-pr-2035/command.yaml
  - id: "dod-omnimarket-pr-1272"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1272 (jonah/omn-13298-url-authority-omnimarket, base=dev).
      Wires check-url-authority pre-commit hook and url-authority-gate.yml CI workflow.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13298/dod-omnimarket-pr-1272/command.yaml
  - id: "dod-omniclaude-pr-1777"
    description: >-
      Per-code-PR binding receipt for omniclaude PR #1777 (jonah/omn-13298-url-authority-omniclaude, base=dev).
      Wires check-url-authority pre-commit hook and url-authority-gate.yml CI workflow.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13298/dod-omniclaude-pr-1777/command.yaml
  - id: "dod-omniintelligence-pr-719"
    description: >-
      Per-code-PR binding receipt for omniintelligence PR #719 (jonah/omn-13298-url-authority-omniintelligence,
      base=dev). Wires check-url-authority pre-commit hook and url-authority-gate.yml CI workflow.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13298/dod-omniintelligence-pr-719/command.yaml
  - id: "dod-omnimemory-pr-371"
    description: >-
      Per-code-PR binding receipt for omnimemory PR #371 (jonah/omn-13298-url-authority-omnimemory, base=dev).
      Wires check-url-authority pre-commit hook and url-authority-gate.yml CI workflow.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13298/dod-omnimemory-pr-371/command.yaml
  - id: "dod-omnibase_core-pr-1264"
    description: >-
      Per-code-PR binding receipt for omnibase_core PR #1264 (jonah/omn-13298-url-authority-baseline,
      base=dev). Seeds the shared url_authority_baseline.json with grandfathered violations for the 5
      fleet repos.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13298/dod-omnibase_core-pr-1264/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13298 contract and all repo
      PR bindings. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check can
      resolve a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13298/dod-occ-pr-self/command.yaml
