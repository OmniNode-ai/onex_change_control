---
schema_version: "1.0.0"
ticket_id: "OMN-11283"
title: "Proof of Life — end-to-end session phase enforcement integration test"
summary: >-
  Bind OCC evidence for omnimarket PR #733 (OMN-11283), which adds a proof-of-life integration test for
  the session phase enforcement pipeline. The test chains all 4 nodes (evaluator → orchestrator → dispatcher
  → reducer) with real models and real state files, verifies phase_state.yaml is written correctly on
  transition, and verifies the hook returns the correct enforcement directive.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Integration test suite (4 tests) passes: full enforcement chain, budget exhaustion,
      reducer state at every step, hook directive for all evaluation states."
    command: "gh pr checks 733 --repo OmniNode-ai/omnimarket"
dod_evidence:
  - id: "dod-tests"
    description: "Bind OMN-11283 evidence to omnimarket PR #733 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11283/dod-tests/command.yaml\"
          && grep -q '^pr_number: 733$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11283/dod-tests/command.yaml\"
          && grep -q '^commit_sha: \"66e162a23f1321f0a98aff951d7c6b53b3bc49b2\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11283/dod-tests/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-11283 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11283/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
