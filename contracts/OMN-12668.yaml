---
schema_version: "1.0.0"
ticket_id: "OMN-12668"
title: "Bind OCC evidence for omnibase_infra upload-artifact action bump"
summary: >
  Central OCC evidence for omnibase_infra PR #1861, a ticketed replacement for Dependabot PR #1852. The
  product PR bumps actions/upload-artifact from v4 to v7 in omnibase_infra Omnigate artifact upload.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #1861 is open against dev at the recorded ticketed head."
    command: "gh pr view 1861 --repo OmniNode-ai/omnibase_infra --json state,headRefOid,baseRefName"
  - kind: "ci"
    description: "All visible non-Receipt-Gate checks on omnibase_infra PR #1861 are SUCCESS or SKIPPED."
    command: "gh pr checks 1861 --repo OmniNode-ai/omnibase_infra --json name,state"
  - kind: "manual"
    description: "The OCC PR carrying this central evidence is open and bound to OMN-12668."
    command: "gh pr view 0 --repo OmniNode-ai/onex_change_control --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1861-head"
    description: "omnibase_infra PR #1861 is open against dev at the recorded ticketed upload-artifact
      head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12668/dod-infra-pr-1861-head/command.yaml"
          && grep -q '^pr_number: 1861$' "drift/dod_receipts/OMN-12668/dod-infra-pr-1861-head/command.yaml"
          && grep -q '^commit_sha: "ccb95338fd4e982834fb91b007ce642387b7c2c1"$' "drift/dod_receipts/OMN-12668/dod-infra-pr-1861-head/command.yaml"
  - id: "dod-infra-pr-1861-non-receipt-checks"
    description: "All visible non-Receipt-Gate checks on omnibase_infra PR #1861 are green or skipped."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12668/dod-infra-pr-1861-non-receipt-checks/command.yaml"
          && grep -q 'all visible non-Receipt-Gate checks are SUCCESS or SKIPPED' "drift/dod_receipts/OMN-12668/dod-infra-pr-1861-non-receipt-checks/command.yaml"
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR #0 carries the OMN-12668 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12668/dod-occ-pr-self/command.yaml" && grep
          -q '^pr_number: 0$' "drift/dod_receipts/OMN-12668/dod-occ-pr-self/command.yaml"
