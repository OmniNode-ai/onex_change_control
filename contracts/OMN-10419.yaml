---
schema_version: '1.0.0'
ticket_id: OMN-10419
title: "feat(OMN-10419): pin OCC checkout to Evidence-Source SHA in receipt-gate"
summary: "Modify receipt-gate.yml to require an Evidence-Source commit-ish in every PR body (post-cutoff),
  resolve it to an exact OCC SHA, and checkout onex_change_control at that pinned SHA. OCC fetch failure
  is a hard FAIL. GHA cache disabled to enforce merge_group re-evaluation. Adds parse_evidence_source
  to receipt_gate.py with 37 unit tests covering all plan-specified PASS/FAIL fixtures."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "37 new unit tests pass in omnibase_core PR #1017"
    command: "gh pr checks 1017 --repo OmniNode-ai/omnibase_core"
  - kind: ci
    description: "ruff + mypy + pre-commit all pass — verified via receipts"
    command: "grep -q 'status: PASS' drift/dod_receipts/OMN-10419/dod-001/command.yaml"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "37 unit tests pass in test_receipt_gate_evidence_source.py"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10419/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-10419$' drift/dod_receipts/OMN-10419/dod-001/command.yaml && grep -q '^evidence_item_id:
          dod-001$' drift/dod_receipts/OMN-10419/dod-001/command.yaml"
  - id: dod-002
    description: "All pre-commit hooks pass (ruff, mypy, purity checks)"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10419/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-10419$' drift/dod_receipts/OMN-10419/dod-002/command.yaml && grep -q '^evidence_item_id:
          dod-002$' drift/dod_receipts/OMN-10419/dod-002/command.yaml"
  - id: dod-003
    description: "Workflow shape tests confirm new steps present in receipt-gate.yml"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10419/dod-003/command.yaml && grep
          -q '^ticket_id: OMN-10419$' drift/dod_receipts/OMN-10419/dod-003/command.yaml && grep -q '^evidence_item_id:
          dod-003$' drift/dod_receipts/OMN-10419/dod-003/command.yaml"
