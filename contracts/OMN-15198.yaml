---
schema_version: "1.0.0"
ticket_id: "OMN-15198"
title: "Slim omnibase_infra CLAUDE.md — canary for org-wide repo CLAUDE.md refresh"
summary: >
  OCC companion evidence for omnibase_infra PR #2482: slims CLAUDE.md from 43049 to 18136 bytes per context-engineering
  doctrine (drift-prone factual state replaced with regenerating probes; Install Model + Service Catalog
  walkthrough moved to docs/patterns/service_catalog.md; stale Agent-Driven Development section deleted;
  all 8 KEEP-contract gotchas retained inline).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Product PR binding and content probes for omnibase_infra#2482."
    command: "gh pr view 2482 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2482-head-27197366"
    description: >
      Consuming-PR binding: omnibase_infra PR #2482 head 27197366cc341d03f8a5392202e76d6a047bcb88 carries
      the slimmed CLAUDE.md plus the new docs/patterns/service_catalog.md for OMN-15198.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2482 --repo OmniNode-ai/omnibase_infra --json number,state"
  - id: "dod-infra2482-claude-slim-27197366"
    description: >
      Falsifiable product-head probe for omnibase_infra PR #2482: the new docs/patterns/service_catalog.md
      exists at the pinned head and CLAUDE.md at the same head both points to it and still carries the
      AMBIGUOUS_CONTRACT_CONFIGURATION keep-contract gotcha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/docs/patterns/service_catalog.md?ref=27197366cc341d03f8a5392202e76d6a047bcb88'
          --jq .content | base64 -d | grep -q 'Service Catalog Architecture & Install Model' && gh api
          'repos/OmniNode-ai/omnibase_infra/contents/CLAUDE.md?ref=27197366cc341d03f8a5392202e76d6a047bcb88'
          --jq .content | base64 -d | grep -q 'AMBIGUOUS_CONTRACT_CONFIGURATION'
  - id: "dod-occ-self"
    description: >
      OCC companion self-bind for OMN-15198 (deterministic commit-sha bind, no PR-number prediction).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-15198/dod-occ-self/command.yaml"
  - id: "occ-self-bind-pr-5054"
    description: "Binds OMN-15198 to OCC durability-rebind PR #5054 (self-bind for the merge-commit receipt
      supersession)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5054 --repo OmniNode-ai/onex_change_control --json number,state"
