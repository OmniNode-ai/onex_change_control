---
schema_version: '1.0.0'
ticket_id: OMN-13671
title: "feat(OMN-13671): make deploy-onex-prod.yml runtime-only-aware"
summary: >
  Add deploy_target input (runtime-only | onex-api | all) to deploy-onex-prod.yml so a runtime-only prod
  deploy can proceed without being blocked by the stale onex-api Trivy scan (b4c32554, 2026-03-30 image
  with accumulated CVEs). Onex-api is still fully Trivy-scanned whenever it is the deploy target.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >
      TDD: 8-test suite was RED before implementation (6 failures) and GREEN after (34/34 tests pass).
      Test file tests/k8s/test_deploy_onex_prod_workflow.py proves Trivy still runs when onex-api IS the
      deploy target.
    command: >
      uv run pytest tests/k8s/test_deploy_onex_prod_workflow.py -v --no-header 2>&1 | grep -E "passed|failed"
  - kind: ci
    description: >
      deploy-onex-prod.yml has deploy_target input with runtime-only option and Trivy step carries if
      condition referencing runtime-only.
    command: >
      grep -q "deploy_target" .github/workflows/deploy-onex-prod.yml && grep -A2 "Security scan image
      with Trivy" .github/workflows/deploy-onex-prod.yml | grep -q "runtime-only"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >
      All 34 tests pass including 8 new workflow-routing tests (was 26 before OMN-13671, pre-existing
      collection error also fixed).
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13671/dod-001/command.yaml"
  - id: dod-002
    description: >
      deploy-onex-prod.yml deploy_target input exists with runtime-only option.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13671/dod-002/command.yaml"
  - id: dod-003
    description: >
      Trivy step has if condition skipping it for runtime-only.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13671/dod-003/command.yaml"
  - id: dod-004
    description: >
      Runtime apply step is NOT skipped for runtime-only (always runs).
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13671/dod-004/command.yaml"
  - id: dod-005
    description: >
      Feature commit SHA: c7a0d7d53d4e12de2b48963808d490ea7a5c2009 Branch: jonah/omn-13671-deploy-onex-prod-runtime-only-aware
      Repo: OmniNode-ai/omninode_infra
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13671/dod-005/command.yaml"
  - id: dod-occ-self
    description: >
      onex_change_control PR #3362 carries the central OMN-13671 contract and PASS receipts required by
      the downstream omninode_infra PR #532 Receipt Gate.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13671/dod-occ-self/command.yaml"
