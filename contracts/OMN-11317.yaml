---
schema_version: "1.0.0"
ticket_id: OMN-11317
title: "Migration conflict gate blocks non-migration infra PRs on existing cross-repo drift"
summary: "Scope the validate-boundaries migration-conflicts action to SQL migration diffs so existing
  cross-repo schema drift does not block unrelated runtime config PRs."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "validate-boundaries action regression tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11317/dod-action-tests/command.yaml"
  - kind: "ci"
    description: "Touched validate-boundaries test passes ruff checks"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11317/dod-ruff-check/command.yaml"
  - kind: "manual"
    description: "OCC PR carrying the migration-conflict scope fix is open"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11317/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-action-tests"
    description: "validate-boundaries action regression tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11317/dod-action-tests/command.yaml
          && grep -q '^ticket_id: OMN-11317$' drift/dod_receipts/OMN-11317/dod-action-tests/command.yaml
          && grep -q '^evidence_item_id: dod-action-tests$' drift/dod_receipts/OMN-11317/dod-action-tests/command.yaml"
  - id: "dod-ruff-check"
    description: "Touched validate-boundaries test passes ruff checks"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11317/dod-ruff-check/command.yaml
          && grep -q '^ticket_id: OMN-11317$' drift/dod_receipts/OMN-11317/dod-ruff-check/command.yaml
          && grep -q '^evidence_item_id: dod-ruff-check$' drift/dod_receipts/OMN-11317/dod-ruff-check/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carrying the migration-conflict scope fix is open"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11317/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: OMN-11317$' drift/dod_receipts/OMN-11317/dod-occ-pr/command.yaml && grep
          -q '^evidence_item_id: dod-occ-pr$' drift/dod_receipts/OMN-11317/dod-occ-pr/command.yaml"
