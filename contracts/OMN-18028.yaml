---
schema_version: "1.0.0"
ticket_id: "OMN-18028"
title: "Hand-authored OCC evidence for OmniNode-ai/omnidash#321 (public-repo-hygiene pin bump)"
summary: >
  OCC contract for OmniNode-ai/omnidash PR #321, which bumps that repo's caller pin of omniclaude/.github/workflows/public-repo-hygiene-reusable.yml
  from the pre-fix commit c5f4e8fda9e62ce8c74360e7cd7d0b60f3cd8f82 to 0e19ed86012eb051fefe5ff0d84968b01f749474,
  a descendant of the omniclaude#2124 fix (5003030a79b410a0b08337c9f273b53e0db807e3) that moved the reusable's
  token-mint step to the ALL-visibility ONEXBOT_APP_ID/ONEXBOT_APP_PRIVATE_KEY secret pair. omnidash is
  not on the SELECTED-visibility ONEXBOT_OCC_APP_ID/ONEXBOT_OCC_PRIVATE_KEY secret's repository list,
  so every hygiene run there failed closed at the mint step until this bump. Hand-authored because occ-autobind
  declined to mint a companion for PR #321 (occ-autobind-no-red-derivable:321): a single-line workflow
  pin bump has no changed-file RED/GREEN test to derive automatically.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #321 CI checks green, including public-repo-hygiene reaching and passing its evaluation
      step (not just skipping at mint)"
    command: "gh pr checks 321 --repo OmniNode-ai/omnidash"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnidash-pr-321-pin-bump"
    description: "PR #321 on OmniNode-ai/omnidash changes exactly one line: the hygiene reusable pin,
      to the post-fix sha."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnidash/contents/.github/workflows/public-repo-hygiene.yml?ref=884dd96989fdff0beadd7981b4c5ecdff81365e8 --jq '.content' | base64 -d | grep -c '0e19ed86012eb051fefe5ff0d84968b01f749474'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnidash/pulls/321/files --jq '[.[].filename]' | grep -cFx '[".github/workflows/public-repo-hygiene.yml"]'
  - id: "dod-OmniNode-ai-omnidash-pr-321-hygiene-evaluated"
    description: "PR #321's public-repo-hygiene job reaches and passes the mint-token step and the evaluation
      step, proving the pin bump fixed the fail-closed mint failure this ticket diagnoses."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnidash/actions/jobs/105093174871 --jq '[.steps[] | select(.name=="Mint token for the private vocabulary repo" or .name=="Run the public-repo hygiene gate") | .conclusion] | join(",")' | grep -cFx 'success,success'
