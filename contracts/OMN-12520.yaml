---
schema_version: "1.0.0"
ticket_id: "OMN-12520"
title: "Register the 10 unrouted SEA node handlers in contract handler_routing"
summary: >
  Registers each of the 10 self-extending-agent demo node handlers in its node contract.yaml handler_routing.handlers[]
  block (module + operation, routing_strategy operation_match) so each handler module is registered for
  DI-routed dispatch. Resolves all 10 missing_handler_routing violations the official onex_change_control
  scanner flags for the SEA repo. Contract-native only; no handler .py changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "onex-self-extending-agent PR #179 carries the handler_routing registration and scanner-clean
      proof."
    command: "See drift/dod_receipts/OMN-12520/dod-sea-pr-179/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carrying this contract binds the central evidence publication."
    command: "See drift/dod_receipts/OMN-12520/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-179"
    description: "onex-self-extending-agent PR #179 adds handler_routing to all 10 node contracts; official
      scanner reports 0 missing_handler_routing."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12520/dod-sea-pr-179/command.yaml
          && grep -q '^pr_number: 179$' drift/dod_receipts/OMN-12520/dod-sea-pr-179/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control publishes the OMN-12520 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12520/dod-occ-pr/command.yaml"
