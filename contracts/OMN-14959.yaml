---
schema_version: "1.0.0"
ticket_id: "OMN-14959"
title: "Relocate StrEnumBase foundation utility out of utils"
description: >-
  Central OCC evidence for omnibase_core PR #1488. The product PR relocates the StrEnumBase helper into
  the enums layer, preserves compatibility through shims, extends the foundation import-linter boundary,
  refreshes the import ratchet baseline, and records local and hosted gate evidence.
dod_evidence:
  - id: "dod-omnibase-core-pr-1488-head"
    description: "Binds omnibase_core PR #1488 to exact reviewed head 1bdea55a5a6db6f2cb64fb74a7ff036d765d3ad0."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1488 --repo OmniNode-ai/omnibase_core --json headRefOid --jq .headRefOid
          | grep -Fx 1bdea55a5a6db6f2cb64fb74a7ff036d765d3ad0"
  - id: "dod-omnibase-core-pr-1488-scope"
    description: >-
      PR #1488 scope is the import-layer relocation surface: 448 files total via GitHub's paginated PR
      files API, including .importlinter, scripts/ci/import_ratchet_baseline.py, tests/unit/scripts/ci/test_check_import_ratchet.py,
      438 enum files, 4 type files, 2 utils shims, and scripts/validate_class_naming.py.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "tmp=$(mktemp) && gh api --paginate repos/OmniNode-ai/omnibase_core/pulls/1488/files
          --jq '.[].filename' > \"$tmp\" && test \"$(wc -l < \"$tmp\" | tr -d ' ')\" = 448 && grep -Fx
          .importlinter \"$tmp\" && grep -Fx scripts/ci/import_ratchet_baseline.py \"$tmp\" && grep -Fx
          tests/unit/scripts/ci/test_check_import_ratchet.py \"$tmp\" && test \"$(grep -c '^src/omnibase_core/enums/'
          \"$tmp\")\" = 438 && test \"$(grep -c '^src/omnibase_core/types/' \"$tmp\")\" = 4 && test \"$(grep
          -c '^src/omnibase_core/utils/' \"$tmp\")\" = 2 && rm \"$tmp\""
  - id: "dod-omnibase-core-pr-1488-body-evidence"
    description: >-
      PR #1488 body records RED->GREEN import-linter proof, ratchet proof, shim identity, focused tests,
      full pre-push suite, and Refs-only ticket association.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "body=$(gh pr view 1488 --repo OmniNode-ai/omnibase_core --json body --jq .body)
          && printf '%s\\n' \"$body\" | grep -F 'Seeded-violation RED->GREEN' && printf '%s\\n' \"$body\"
          | grep -F 'uv run lint-imports' && printf '%s\\n' \"$body\" | grep -F 'check_import_ratchet.py'
          && printf '%s\\n' \"$body\" | grep -F 'Shim is Canonical' && printf '%s\\n' \"$body\" | grep
          -F '43268 passed, 53 skipped, 3 xpassed' && printf '%s\\n' \"$body\" | grep -F 'Refs OMN-14959'
          && ! printf '%s\\n' \"$body\" | grep -F 'Closes OMN-14959'"
  - id: "dod-omnibase-core-pr-1488-prepush-suite"
    description: >-
      Local pre-push hook ran the governed full suite for PR #1488 and pushed the exact reviewed head
      after passing.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1488 --repo OmniNode-ai/omnibase_core --json body,headRefOid --jq '.headRefOid
          + \"\\n\" + .body' | grep -F '1bdea55a5a6db6f2cb64fb74a7ff036d765d3ad0' && gh pr view 1488 --repo
          OmniNode-ai/omnibase_core --json body --jq .body | grep -F 'pre-push hook duration 5624.11s'"
  - id: "occ-self-bind-pr-4688-omn14959"
    description: "OCC companion PR #4688 self-bind for OMN-14959."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4688 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
