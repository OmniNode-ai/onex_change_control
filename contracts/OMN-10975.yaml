---
schema_version: "1.0.0"
ticket_id: "OMN-10975"
title: "Implement postgres projection reader for omnidash Express bridge"
summary: "Binds omnidash PR #87, which implements the postgres-backed projection reader for the omnidash
  Express bridge, replacing the sqlite reader as the primary data source for projection queries."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "omnidash PR #87 passes CI with postgres projection reader implementation."
    command: "gh pr view 87 --repo OmniNode-ai/omnidash --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnidash-pr-87"
    description: "omnidash PR #87 carries the OMN-10975 postgres projection reader implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10975/dod-omnidash-pr-87/command.yaml
          && grep -q '^pr_number: 87$' drift/dod_receipts/OMN-10975/dod-omnidash-pr-87/command.yaml"
  - id: "dod-occ-pr-1009"
    description: "onex_change_control PR #1009 carries the OMN-10975 contract and downstream receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10975/dod-occ-pr-1009/command.yaml
          && grep -q '^pr_number: 1009$' drift/dod_receipts/OMN-10975/dod-occ-pr-1009/command.yaml"
