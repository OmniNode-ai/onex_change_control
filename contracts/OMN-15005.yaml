---
schema_version: "1.0.0"
ticket_id: "OMN-15005"
title: "Autobind OCC evidence for OMN-15005"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1872."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1872 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1872"
    description: "PR #1872 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1872-ci"
    description: "PR #1872 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4691"
    description: "OCC companion PR #4691 — self-bind for OMN-15005 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1872-deploy-gate-disposition"
    description: "PR #1872 deploy-gate disposition — runtime contract paths touched and post-merge dev
      redeploy validation bar is explicit in the PR body."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1872 --repo OmniNode-ai/omnimarket --json body,files --jq 'if ((.body
          | test(\"Post-merge dev redeploy validation\")) and any(.files[].path; . == \"src/omnimarket/nodes/node_agent_coordinator_orchestrator/contract.yaml\")
          and any(.files[].path; . == \"src/omnimarket/nodes/node_memory_lifecycle_orchestrator/contract.yaml\")
          and any(.files[].path; . == \"src/omnimarket/nodes/node_persona_lifecycle_orchestrator/contract.yaml\"))
          then \"deploy gate disposition verified: runtime contract paths touched; PR body carries post-merge
          dev redeploy validation bar\" else error(\"deploy gate disposition missing runtime paths or
          PR body deploy bar\") end'"
  - id: "occ-self-bind-pr-4694-omn15005"
    description: "OCC companion PR #4694 self-bind for the OMN-15005 deploy-gate disposition evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4694 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
