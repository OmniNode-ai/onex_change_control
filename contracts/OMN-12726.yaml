---
schema_version: "1.0.0"
ticket_id: "OMN-12726"
title: "Sync runtime Docker build context paths during deploy"
summary: >-
  Central evidence for omnibase_infra PR #1879. The PR fixes the stable runtime deployment context so
  clean dev-lane rebuilds include the Dockerfile runtime COPY sources under scripts/runtime_build/ and
  workspace/sibling-repos/. This removes the deploy-time missing-context failure found during the delegation/SEA
  integration rollout.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1879"
    description: "omnibase_infra PR #1879 carries the deploy-runtime build-context sync fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12726/dod-infra-pr-1879/command.yaml"
          && grep -q '^pr_number: 1879$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12726/dod-infra-pr-1879/command.yaml"
  - id: "dod-local-validation"
    description: "Focused syntax, lint, and regression tests passed for the deploy-context fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12726/dod-local-validation/command.yaml"
          && grep -q '11 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12726/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Runtime rebuild impact is identified and bounded to post-merge dev-lane validation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12726/dod-deploy-assessment/command.yaml"
          && grep -q 'post-merge dev deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12726/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-12726 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12726/dod-occ-pr/command.yaml"
