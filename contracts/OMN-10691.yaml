---
schema_version: "1.0.0"
ticket_id: "OMN-10691"
title: "Define ADR extraction domain models in omnibase_core"
summary: >
  Adds src/omnibase_core/models/adr/ with 6 frozen Pydantic models (ModelDocumentSegment, ModelDecisionExtraction,
  ModelADRDraft, ModelADRExtractionMetadata, ModelExtractionScore, ModelCanaryResult, ModelLLMCallEvidence)
  and src/omnibase_core/enums/adr/ with 3 enums (EnumSegmentType, EnumDecisionType, EnumUsageSource).
  23 unit tests cover frozen invariants, deterministic ID computation, confidence bounds, and nullable
  field behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests-pass"
    description: "23/23 unit tests pass for ADR domain models and enums."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10691/dod-tests-pass/command.yaml\""
  - id: "dod-pr-1052-head"
    description: "omnibase_core PR #1052 carries the ADR domain models implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10691/dod-pr-1052-head/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10691 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10691/dod-occ-pr/command.yaml\""
