---
schema_version: "1.0.0"
ticket_id: "OMN-14981"
title: "Repair OCC lease tree creation for companion minting"
description: >-
  Central OCC evidence companion for omnimarket PR #1865. The product PR fixes the shared OCC git transport
  lease commit creation path by reusing the default branch tree instead of posting an empty tree to GitHub's
  git/trees API, which returns 422 and prevents companion minting.
dod_evidence:
  - id: "dod-omnimarket-pr-1865-head"
    description: "Binds omnimarket PR #1865 to exact reviewed head 65b03f0dca8a6dbd6732a9400fcc288ce6f7c86d."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1865 --repo OmniNode-ai/omnimarket --json headRefOid --jq .headRefOid
          | grep -Fx 65b03f0dca8a6dbd6732a9400fcc288ce6f7c86d"
  - id: "dod-omnimarket-pr-1865-diff-scope"
    description: "PR #1865 scope is limited to the OCC git transport lease fix and its unit tests."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 1865 --repo OmniNode-ai/omnimarket --name-only | grep -E '^(src/omnimarket/occ_git_transport\\.py|tests/unit/test_occ_git_transport_lease\\.py)$'"
  - id: "dod-omnimarket-pr-1865-local-verification"
    description: >-
      PR #1865 body records local proof: 290 targeted tests passed, governed pre-push selector passed
      4075 tests with documented unrelated deselections, and ruff/mypy/pre-commit were clean.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1865 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -F '290
          passed' && gh pr view 1865 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -F '4075
          passed'"
  - id: "dod-omnimarket-pr-1865-close-discipline"
    description: >-
      PR #1865 references OMN-14981 without directly closing it because the canary proof remains in progress
      and should not trigger ticket closeout before acceptance evidence exists.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "body=$(gh pr view 1865 --repo OmniNode-ai/omnimarket --json body --jq .body); printf
          '%s' \"$body\" | grep -F 'Refs OMN-14981' && ! printf '%s' \"$body\" | grep -F 'Closes OMN-14981'"
  - id: "dod-omnimarket-pr-1865-code-only-deploy-hold"
    description: >-
      Deploy-gate hold evidence for omnimarket PR #1865: the PR repairs code and tests for OCC lease creation
      but does not deploy, restart, mutate .201, flip org-wide OCC producers, or claim the follow-on canary
      proof. The PR body explicitly leaves canary proof in progress and org-wide flip out of scope.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1865 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -F 'Canary
          proof (in progress' && gh pr view 1865 --repo OmniNode-ai/omnimarket --json body --jq .body
          | grep -F 'Remaining work before any org-wide flip'"
  - id: "occ-self-bind-pr-4648-omn14981"
    description: "OCC companion PR #4648 self-bind for OMN-14981."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4648 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2410"
    description: "PR #2410 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2410-ci"
    description: "PR #2410 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4661"
    description: "OCC companion PR #4661 — self-bind for OMN-14981 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
