---
schema_version: "1.0.0"
ticket_id: OMN-10208
title: "node_dispatch_worker persists ModelDispatchRecord before returning"
summary: "node_dispatch_worker persists ModelDispatchRecord before returning"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR checks pass (lint, typecheck, test, contract-validation, deploy-gate)"
    command: "gh pr checks 436 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "Unit tests for handler dispatch-record persistence pass"
    command: "uv run pytest tests/unit/test_dispatch_worker_persists_dispatch_record.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Handler persistence + fail-loud unit tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/test_dispatch_worker_persists_dispatch_record.py -v"
  - id: "dod-002"
    description: "Full -k dispatch_worker suite stays green with ONEX_STATE_DIR unset (CI env)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u ONEX_STATE_DIR uv run pytest tests/ -k dispatch_worker -v"
  - id: "dod-003"
    description: "Post-merge deploy smoke: handler imports cleanly in deployed runtime container after
      redeploy"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_dispatch_worker.handlers.handler_dispatch_worker
          import HandlerDispatchWorker, _persist_dispatch_record; print('deploy smoke ok')\""
