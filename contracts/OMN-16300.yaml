---
schema_version: "1.0.0"
ticket_id: "OMN-16300"
title: "Autobind OCC evidence for OMN-16300"
summary: >
  OCC contract for OmniNode-ai/omnibase_infra PR #2817 (fix Enable Auto-Merge workflow's bare `gh pr merge
  --auto` to pass `--squash` explicitly, matching the repo's live squash-only/no-queue merge policy).
  Local-CLI mint fallback: occ-autobind/occ-companion-effect had not produced a companion within the observed
  autobind window.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2817"
    description: >
      PR #2817 on OmniNode-ai/omnibase_infra -- fixes .github/workflows/auto-merge.yml's Enable Auto-Merge
      job. Content-pinned RED-before/GREEN-after differential: fetches the workflow source via the GitHub
      contents API at the PR's base and head SHAs and confirms the bare `gh pr merge "$PR" --repo "$GH_REPO"
      --auto` invocation present at base is replaced by `gh pr merge "$PR" --repo "$GH_REPO" --squash
      --auto` at head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/auto-merge.yml?ref=381333da507754fa35dc6c292fbe76208f34f314"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/auto-merge.yml?ref=1c089b211163669765471498a98b31ea6bc58050"
          --jq .content | base64 -d)"; printf '%s' "$base_body" | grep -qF 'gh pr merge "$PR" --repo "$GH_REPO"
          --auto 2>&1' && printf '%s' "$head_body" | grep -qF 'gh pr merge "$PR" --repo "$GH_REPO" --squash
          --auto 2>&1' && echo "OMN-16300-INFRA-2817-DIFFERENTIAL-PROBE-PASS: bare --auto present at base,
          --squash --auto present at head"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2817-diffscope"
    description: >
      PR #2817 on OmniNode-ai/omnibase_infra -- product diff scope check (OMN-14425): exactly the one
      workflow file carrying the fix.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2817 --repo OmniNode-ai/omnibase_infra --json files --jq '[.files[].path] | sort
          | join(",")' | grep -x '.github/workflows/auto-merge.yml'
