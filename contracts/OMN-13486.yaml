---
schema_version: "1.0.0"
ticket_id: "OMN-13486"
title: "feat(OMN-13486): ARCH-004 Signal B — orchestration-monolith complexity rule + baseline dimension"
summary: >-
  OCC evidence for omnibase_infra PR #2070 (Track B · B2). Implements ARCH-004 Signal B as a DISTINCT
  rule, NOT a widened Signal A. Signal A (OMN-13472, merged) catches the decorative-FSM + handler-driven
  _transition triad; Signal B catches the Class-B monolith — an oversized + complex orchestrator handler
  that is not executor-bound — INDEPENDENT of FSM decorativeness. build_loop / session / memory_lifecycle
  / swarm_dispatch all carry 0 handler-owned _transition and so are invisible to Signal A. New validator_orchestration_monolith.RuleOrchestrationMonolith
  (rule id ARCH-004B, distinct from ARCH-004) owns a B* finding-code namespace; B1 hard-fail = oversized
  (>750L) AND complex (branch/event density or routing fan-in/out) AND not executor-bound. Executor-bound
  and thin orchestrators pass; reducers and non-orchestrator nodes are exempt. New scanner_orchestration_monolith_ratchet
  ships --check-all/--report, --check-changed/--ratchet, --strict, --write-baseline against its OWN baseline
  dimension architecture-handshakes/orchestration-monolith-baseline.yaml (separate file; does NOT commingle
  Signal A finding codes / accepted_rationale; adds not_applicable_rationale). The baseline records the
  confirmed Class-B catalog (omnimarket): build_loop, session, memory_lifecycle, swarm_dispatch + pr_lifecycle,
  task_execution, merge_sweep_triage, node_generation_consumer, overnight, delegation; redeploy (422L,
  prod-promotion authority) is assessed-only and recorded NOT Class B (below threshold; authority/runtime
  surface untouched). Wired through the OMN-12550 path (validate.py orchestration_monoliths subcommand
  + pre-commit changed-node ratchet blocking + CI full report non-blocking initially); NO parallel hook.
  New validator + scanner + baseline YAML + tests + CI/pre-commit wiring; no runtime/contract/topic/envelope
  change, no live-lane mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-signal-b-rule"
    description: >-
      Product PR #2070 adds validator_orchestration_monolith.py (RuleOrchestrationMonolith, rule id ARCH-004B
      distinct from ARCH-004) and scanner_orchestration_monolith_ratchet.py with its own baseline dimension
      architecture-handshakes/orchestration-monolith-baseline.yaml. TDD tests (test_orchestration_monolith_signal_b.py,
      16 passed) prove: synthetic monolith with NO decorative FSM -> Signal A PASSES / Signal B FAILS;
      thin executor-bound orchestrator -> both pass; full-audit detects the real Class-B catalog; distinct
      B* finding-code namespace; baseline round-trip + ratchet + strict behaviour. Full architecture-validator
      suite 240 passed.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c 'ARCH-004B' contracts/OMN-13486.yaml"
  - id: "dod-deploy-gate"
    description: >-
      Deploy-gate evidence for omnibase_infra PR #2070. No live deploy, restart, docker exec, rpk topic
      produce, or runtime lane mutation was performed. The change is a new static validator + scanner
      + baseline YAML + tests + CI/pre-commit wiring; no runtime behavior change. redeploy's prod-promotion
      authority surface is assessed only and NOT modified.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c 'deploy-gate evidence' contracts/OMN-13486.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13486.yaml consumed
      by omnibase_infra PR #2070 Receipt Gate and Deploy Gate via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13486\"$' contracts/OMN-13486.yaml"
