---
schema_version: "1.0.0"
ticket_id: "OMN-12163"
title: "Wire ServiceTimeInjector in omnibase_core mixins"
summary: >
  Central OCC evidence for omnibase_core PR #1162. The change wires ServiceTimeInjector into core mixins/utilities
  for deterministic replay and fixes the PR's ruff Code Quality failure in the new replay tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused ServiceTimeInjector replay tests pass on the PR head."
    command: "See drift/dod_receipts/OMN-12163/dod-focused-replay-tests/command.yaml"
  - kind: "tests"
    description: "Full ruff format and lint checks pass for src/ and tests/ after the lint fix."
    command: "See drift/dod_receipts/OMN-12163/dod-code-quality-ruff/command.yaml"
  - kind: "tests"
    description: "Targeted ruff checks pass for the edited replay test file."
    command: "See drift/dod_receipts/OMN-12163/dod-targeted-ruff/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carries the central OMN-12163 contract and receipts."
    command: "See drift/dod_receipts/OMN-12163/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-replay-tests"
    description: "Focused ServiceTimeInjector replay tests passed on the omnibase_core PR #1162 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12163/dod-focused-replay-tests/command.yaml
          && grep -q '14 passed' drift/dod_receipts/OMN-12163/dod-focused-replay-tests/command.yaml"
  - id: "dod-code-quality-ruff"
    description: "Full ruff format and lint checks passed for src/ and tests/ after the replay test lint
      fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12163/dod-code-quality-ruff/command.yaml
          && grep -q 'All checks passed' drift/dod_receipts/OMN-12163/dod-code-quality-ruff/command.yaml"
  - id: "dod-targeted-ruff"
    description: "Targeted ruff checks passed for the edited replay test file."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12163/dod-targeted-ruff/command.yaml
          && grep -q 'All checks passed' drift/dod_receipts/OMN-12163/dod-targeted-ruff/command.yaml"
  - id: "dod-occ-pr"
    description: "This onex_change_control PR carries the central OMN-12163 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12163/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: \"OMN-12163\"$' drift/dod_receipts/OMN-12163/dod-occ-pr/command.yaml"
