---
schema_version: "1.0.0"
ticket_id: "OMN-12693"
title: "Workstream J / Gate 2: required dead-node gate"
summary: >-
  Central evidence that omnimarket PR #1044 adds required dead-node enforcement by extending node-drift-gate
  with contract-level producer/consumer shape checks, promoting Runtime Sweep to import validation, and
  removing the contract-declared shortcut from the unimported-handler gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-node-drift-liveness-gate"
    description: "Node drift gate checks all nodes with orphan/dead-node liveness enabled."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-node-drift-liveness-gate/command.yaml"
          && grep -q '^pr_number: 1044$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-node-drift-liveness-gate/command.yaml"
  - id: "dod-runtime-sweep-import-check"
    description: "Runtime Sweep validates entry-point modules, contract handlers, and importable model
      refs."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-runtime-sweep-import-check/command.yaml"
          && grep -q '^pr_number: 1044$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-runtime-sweep-import-check/command.yaml"
  - id: "dod-unimported-handler-boundary"
    description: "Unimported-handler gate no longer treats contract metadata as Python import wiring."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-unimported-handler-boundary/command.yaml"
          && grep -q '^pr_number: 1044$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-unimported-handler-boundary/command.yaml"
  - id: "dod-focused-tests-and-lint"
    description: "Focused tests, lint, workflow syntax, and whitespace checks pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-focused-tests-and-lint/command.yaml"
          && grep -q '^pr_number: 1044$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-focused-tests-and-lint/command.yaml"
  - id: "dod-required-contexts"
    description: "omnimarket dev branch protection requires node-drift-gate and Runtime Sweep."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-required-contexts/command.yaml"
          && grep -q '^pr_number: 1044$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-required-contexts/command.yaml"
  - id: "dod-deploy-gate-no-runtime-mutation"
    description: "Deploy-gate evidence for omnimarket CI/tooling/contract metadata enforcement in PR #1044."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-deploy-gate-no-runtime-mutation/command.yaml"
          && grep -q '^pr_number: 1044$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-deploy-gate-no-runtime-mutation/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-12693 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2180$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12693/dod-occ-pr/command.yaml"
