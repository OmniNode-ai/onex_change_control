---
schema_version: "1.0.0"
ticket_id: "OMN-12994"
title: "Extract usage_source from usage_normalized.source for infra wire format"
summary: >
  Central OCC evidence for omnimarket PR #1368. The PR fixes projection of llm_call_metrics rows when
  the infra wire format carries usage provenance under usage_normalized.source instead of a top-level
  usage_source field. The row builder now falls back to usage_normalized.source while preserving top-level
  usage_source priority.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused projection metrics tests cover API, ESTIMATED, MISSING, top-level priority,
      and absent-source fallback."
    command: "uv run pytest src/omnimarket/nodes/node_projection_llm_cost/tests/test_row_and_backfill_llm_call_metrics.py
      -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1368"
    description: "omnimarket PR #1368 carries the usage_normalized.source fallback in row_llm_call_metrics.py."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12994/dod-omnimarket-pr-1368/command.yaml"
  - id: "dod-focused-tests-pass"
    description: "All 56 focused llm_call_metrics row/backfill tests pass on the PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12994/dod-focused-tests-pass/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Deploy assessment for the runtime-path omnimarket PR #1368 projection handler change."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12994/dod-deploy-assessment/command.yaml
          # deploy assessment"
  - id: "dod-occ-pr-self"
    description: "OCC self-binding receipt for the central evidence PR carrying the OMN-12994 contract
      and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12994/dod-occ-pr-self/command.yaml"
  - id: "dod-occ-pr-2993-self"
    description: "OCC self-binding receipt for the deploy-assessment evidence PR carrying the OMN-12994
      deploy receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12994/dod-occ-pr-2993-self/command.yaml"
