---
schema_version: "1.0.0"
ticket_id: "OMN-12694"
title: "Workstream J / Gate 3: plan-vs-live verification gate"
summary: >-
  Adds the staged Gate 3 plan-vs-live verifier for changed docs/plans files: v1 validates PR citations,
  target-branch path references, and explicit Linear ticket-state claims; v2 live-path-authority classification
  remains represented as a typed reference class for follow-on enforcement.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-reference-types"
    description: "Documentation reference extraction includes PR, ticket-state, and live-path-authority
      reference classes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12694/dod-reference-types/command.yaml"
          && grep -q 'PR_NUMBER' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12694/dod-reference-types/command.yaml"
  - id: "dod-plan-vs-live-cli"
    description: "Strict plan-vs-live CLI verifies PR citations, target-branch paths, and Linear ticket-state
      claims."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12694/dod-plan-vs-live-cli/command.yaml"
          && grep -q 'check_plan_vs_live.py' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12694/dod-plan-vs-live-cli/command.yaml"
  - id: "dod-root-workflow-wiring"
    description: "omni_home plan freshness workflow calls the strict checker for changed docs/plans files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12694/dod-root-workflow-wiring/command.yaml"
          && grep -q 'integration_map_freshness.yml' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12694/dod-root-workflow-wiring/command.yaml"
  - id: "dod-focused-tests-and-lint"
    description: "Focused OCC tests, ruff checks, whitespace checks, and root workflow syntax validation
      pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12694/dod-focused-tests-and-lint/command.yaml"
          && grep -q '49 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12694/dod-focused-tests-and-lint/command.yaml"
  - id: "dod-no-live-runtime-mutation"
    description: "Gate 3 implementation changes governance tooling/workflow only; no live deploy, restart,
      or .201 mutation was performed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12694/dod-no-live-runtime-mutation/command.yaml"
          && grep -q 'no live deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12694/dod-no-live-runtime-mutation/command.yaml"
