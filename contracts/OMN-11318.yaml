---
schema_version: "1.0.0"
ticket_id: OMN-11318
title: "Pattern B local ingress waiter hangs after delegate-skill terminal event"
summary: "Carry central OCC evidence for the omnibase_infra fix that changes Pattern B one-shot terminal
  waits to an ungrouped, manually assigned Kafka consumer so `/skill` returns after a request-correlated
  terminal event."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Pattern B broker regression tests pass in omnibase_infra"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11318/dod-pattern-b-tests/command.yaml"
  - kind: "ci"
    description: "Touched Pattern B files pass ruff checks in omnibase_infra"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11318/dod-ruff-check/command.yaml"
  - kind: "manual"
    description: "Downstream omnibase_infra PR carrying the fix is open"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11318/dod-infra-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pattern-b-tests"
    description: "Pattern B broker regression tests pass in omnibase_infra"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11318/dod-pattern-b-tests/command.yaml
          && grep -q '^ticket_id: OMN-11318$' drift/dod_receipts/OMN-11318/dod-pattern-b-tests/command.yaml
          && grep -q '^evidence_item_id: dod-pattern-b-tests$' drift/dod_receipts/OMN-11318/dod-pattern-b-tests/command.yaml"
  - id: "dod-ruff-check"
    description: "Touched Pattern B files pass ruff checks in omnibase_infra"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11318/dod-ruff-check/command.yaml
          && grep -q '^ticket_id: OMN-11318$' drift/dod_receipts/OMN-11318/dod-ruff-check/command.yaml
          && grep -q '^evidence_item_id: dod-ruff-check$' drift/dod_receipts/OMN-11318/dod-ruff-check/command.yaml"
  - id: "dod-infra-pr"
    description: "Downstream omnibase_infra PR carrying the fix is open"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11318/dod-infra-pr/command.yaml
          && grep -q '^ticket_id: OMN-11318$' drift/dod_receipts/OMN-11318/dod-infra-pr/command.yaml &&
          grep -q '^evidence_item_id: dod-infra-pr$' drift/dod_receipts/OMN-11318/dod-infra-pr/command.yaml"
