---
schema_version: "1.0.0"
ticket_id: "OMN-13762"
title: "feat(OMN-13762): R3 infra repin — core→0.46.1/spi→0.23.0 PyPI, delete git-rev overrides, pin occ
  immutable SHA"
summary: >-
  Step 5 of the OMN-13762 R3 real-release remediation. omnibase_infra pyproject.toml is updated to consume
  omnibase-core 0.46.1 and omnibase-spi 0.23.0 from PyPI (replacing git-rev git-source overrides). The
  [tool.uv.sources] entries for omnibase-core and omnibase-spi are deleted entirely. The onex-change-control
  source is repinned from branch=main to the immutable main HEAD SHA (2dd26ade7caaa7131e532473ec9d8a207d0e77ab)
  so the lockfile is reproducible from pure PyPI + immutable-rev sources. The PR is intentionally red
  until omnibase-core 0.46.1 and omnibase-spi 0.23.0 are published to PyPI (upstream steps 3 and 4 of
  the OMN-13762 plan). This contract also accumulates the compat R0 (dod-compat-dev-promotable-to-main),
  spi R2 (dod-spi-release-0.23.0, dod-spi-dev-promotable-to-main), and core R1 (dod-repin-core-occ-sha,
  dod-core-dev-promotable-to-main) receipts merged from OCC #3386 and #3385, so the cumulative dod_evidence
  set spans the compat, spi, core, and infra release tiers of OMN-13762.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-compat-dev-promotable-to-main"
    description: >-
      omnibase_compat dev head c039587a3395235ce0bb56af3232449a75ac6159 is 8 commits ahead of main and
      1 behind (behind_by=1 is the v0.5.4 release back-merge on main, expected and non-blocking). PRs
      #161 (ci: PYTHONPATH fix, OMN-13762) and #160 (chore: v0.5.5 bump, OMN-13764) are both merged to
      compat dev as of 2026-06-30. The compat dev->main promotion PR carries this OCC contract and receipt
      at its head SHA; no live prod mutation, deploy, or restart is performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13762/dod-compat-dev-promotable-to-main/command.yaml
  - id: "dod-spi-release-0.23.0"
    description: >-
      omnibase-spi release PR (jonahgabriel/omn-13762-spi-release-0.23.0) is open against dev with version
      bumped to 0.23.0, CHANGELOG updated, and omnibase-core repinned to PyPI >=0.46.1,<0.47.0. CI is
      staged-red pending omnibase-core 0.46.1 PyPI publication; once core publishes, uv lock resolves
      and all CI gates turn green.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13762/dod-spi-release-0.23.0/command.yaml
  - id: "dod-spi-dev-promotable-to-main"
    description: >-
      omnibase_spi dev head fd239e559c6ed72ac88e5d4e50908b0c11b88f8c is 15 commits ahead of main and 0
      behind (status: ahead) — a clean linear dev-to-main promotion candidate. All 15 commits are merged
      feature PRs. The release PR (version bump + repin) is authored on top and will be the +1 commit
      when merged to dev, making dev promotable to main at that point.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13762/dod-spi-dev-promotable-to-main/command.yaml
  - id: "dod-repin-core-occ-sha"
    description: >-
      omnibase_core PR #1365 (chore(OMN-13762): repin onex-change-control from branch=main to immutable
      SHA) updates [tool.uv.sources] to rev = "41054810a25db826a4c649f491a2d980bffb0c17" (OCC v0.1.0,
      last version without omnibase_core circular dep). Commit SHA: 97b454efd96f31d7d8e3df796480e4865b2240b7.
      OCC is a [dependency-groups].dev-only dep -- no runtime or PyPI artifact impact. uv.lock specifier
      updated; existing lock (OCC v0.1.0 @ 41054810) remains valid.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13762/dod-repin-core-occ-sha/command.yaml
  - id: "dod-core-dev-promotable-to-main"
    description: >-
      omnibase_core dev head 30a6dfe5d5203a65927ac4c3f42a06faa3a216d9 is 110 commits ahead of main and
      3 behind (the 3 behind are release/version-bump back-merges from v0.44.x and v0.45.x, expected and
      non-blocking for the 0.46.1 promotion). Status: diverged (ahead>0, behind>0 from back-merges only).
      The dev-to-main promotion PR for 0.46.1 must carry promotion-receipt: OCC-3385 referencing this
      contract. Verification: gh api repos/OmniNode-ai/omnibase_core/compare/main...dev --jq result =
      {ahead:110,behind:3,status:diverged}.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13762/dod-core-dev-promotable-to-main/command.yaml
  - id: "dod-infra-r3-pypi-repin"
    description: >-
      pyproject.toml in omnibase_infra branch jonahgabriel/omn-13762-r3-repin-pypi (commit 0769d7e32c25fb7973fc57e21483b484265df3b7)
      carries the correct R3 pin changes: omnibase-core >=0.46.1,<0.47.0 (PyPI), omnibase-spi >=0.23.0,<0.24.0
      (PyPI), onex-change-control rev=2dd26ade7caaa7131e532473ec9d8a207d0e77ab (immutable). The [tool.uv.sources]
      git-rev entries for core + spi are deleted. uv lock is deferred pending PyPI publication of 0.46.1
      / 0.23.0 (upstream steps 3+4 of OMN-13762 plan).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13762/dod-infra-r3-pypi-repin/command.yaml
  - id: "dod-infra-dev-promotable-to-main"
    description: >-
      omnibase_infra dev is 190 commits ahead of main and 4 behind (the 4 commits main is ahead are release/version-bump
      back-merges, expected and non-blocking). Once the R3 repin PR merges to infra dev, the dev-to-main
      promotion PR (with promotion-receipt: OCC-<n>) promotes a clean, real-PyPI-sourced image. Probe:
      gh api repos/OmniNode-ai/omnibase_infra/compare/main...dev --jq '{ahead:.ahead_by,behind:.behind_by,status:.status}'.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13762/dod-infra-dev-promotable-to-main/command.yaml
  - id: "dod-receipt-gate-caller-policy-mode"
    description: >-
      omnibase_core PR #1367 (commit e4752450ac659a818906aff7af977621ca7569f9) adds a `with: { branch-policy-mode:
      ... }` input to the receipt-gate caller (.github/workflows/call-receipt-gate.yml) so main-targeted
      PRs (and merge_group SHAs against refs/heads/main) run the reusable receipt-gate in main-release
      mode — which applies the dev->main promotion exemption that skips the Axis-2 branch-name identity
      check — while dev-targeted PRs stay in legacy mode. Previously the caller passed no `with:` inputs,
      so branch-policy-mode defaulted to legacy and the Axis-2 branch-identity check failed on OMN-13762
      dev->main promotion PRs whose head branch is literally 'dev' and therefore cannot contain the ticket
      id. CI-config only: no runtime, contract, or topic-schema change; deploy-gate not applicable.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13762/dod-receipt-gate-caller-policy-mode/command.yaml
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13762 contract item dod-receipt-gate-caller-policy-mode,
      plus the accumulated compat/spi/core/infra items and this dod-occ-self receipt required by OCC eligibility
      and downstream Receipt Gate resolution.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13762/dod-occ-self/command.yaml
