---
schema_version: "1.0.0"
ticket_id: "OMN-15918"
title: "OCC evidence for OMN-15918"
summary: >
  OCC companion authored directly (claude-code) for OmniNode-ai/omnibase_infra PR #2727 -- node_gateway_attach_effect
  hardening (JWKS signature verification, heartbeat/detach identity binding, atomic session-store transitions
  via put_if_present, outage-vs-revocation circuit breaker split). Companion authored directly because
  the event-driven occ-autobind / occ-companion-effect consumer (node_occ_companion_compute) had not produced
  a companion PR within the CI window; the source PR's occ-autobind and occ-companion-effect commands
  both published successfully (see command.yaml evidence below), matching the current precedent shape
  (e.g. OCC#6392 for OMN-15937).
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "PR #2727 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2727"
    description: "PR #2727 on OmniNode-ai/omnibase_infra -- Evidence-Source companion."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2727 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2727 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2727"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-deploy-falsifiable-probe"
    description: >
      Falsifiable deploy probe (OMN-14505/OMN-8912) for node_gateway_attach_effect -- this node has no
      live deployed instance yet (OMN-15750 has not shipped a running runtime), so per the hermetic-compute-only
      carve-out (OMN-14051) the probe reads the actual merged source content live from GitHub (never this
      ticket's own receipts) and asserts the introduced symbol is genuinely present at the PR head.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_gateway_attach_effect/services/service_keycloak_token_validator.py?ref=96c8bcaf2be5f4d5cd155098241b3c22c6713553"
          --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF "def verify_and_decode_claims"
  - id: "occ-self-bind-pr-6396"
    description: "OCC companion PR #6396 — self-bind for OMN-15918 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6396 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2731"
    description: "PR #2731 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/integration/runtime/test_manifest_pool_injection_integration.py?ref=992c17073975e98605e01d88fa12cf3d2d1fc6d8 --jq '.content' | base64 -d | grep -c 'def test_gateway_runtime_dependencies_fail_closed_on_missing_mapping'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2731-ci"
    description: "PR #2731 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2731 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "occ-self-bind-pr-6418"
    description: "OCC companion PR #6418 — self-bind for OMN-15918 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6418 --repo OmniNode-ai/onex_change_control --json number,state"
