---
schema_version: "1.0.0"
ticket_id: "OMN-16288"
title: "Autobind OCC evidence for OMN-16288"
summary: >
  OCC contract for OmniNode-ai/omnibase_core PR #1570 (removes the redundant heads/main OCC eligibility
  re-check from auto-merge.yml -- that step re-fetched onex_change_control@heads/main and re-ran validator_occ_merge_eligibility
  against a stale checkout, permanently failing since OCC main is thousands of commits behind dev for
  ticket contracts; the auto-merge job already gates on the required occ-preflight job, so the duplicate
  never actually gated arming). Local-CLI mint fallback: occ-companion-effect published the event but
  no companion PR materialized after 40+ minutes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_core-pr-1570"
    description: >
      PR #1570 on OmniNode-ai/omnibase_core -- removes the redundant occ-auto-merge-preflight step (and
      its supporting Python/uv setup steps) from .github/workflows/auto-merge.yml, trusting the existing
      required occ-preflight job's conclusion instead. Content-pinned RED-before/GREEN-after differential:
      fetches auto-merge.yml at both the PR's base and head SHAs via the GitHub contents API and confirms
      a live (non-comment) invocation of `gh api repos/OmniNode-ai/onex_change_control/git/ref/heads/main`
      exists at base but not at head (only explanatory comments remain there).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnibase_core/contents/.github/workflows/auto-merge.yml?ref=cd7289790ea7f69be2101f71aaf6bd1a1b4bb53d"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnibase_core/contents/.github/workflows/auto-merge.yml?ref=37721619f4b2637321efa0c153e5d90abedaa35c"
          --jq .content | base64 -d)"; base_has=$(printf '%s' "$base_body" | grep -vE '^\s*#' | grep -c
          'git/ref/heads/main'); head_has=$(printf '%s' "$head_body" | grep -vE '^\s*#' | grep -c 'git/ref/heads/main');
          [ "$base_has" -ge 1 ] && [ "$head_has" -eq 0 ] && echo "OMN-16288-CORE-1570-DIFFERENTIAL-PROBE-PASS:
          auto-merge.yml executes a live onex_change_control@heads/main re-check at base ($base_has occurrence(s))
          and has zero live occurrences at head (comments only)"
  - id: "occ-self-bind-pr-6793"
    description: "Binds OMN-16288 to OCC companion PR #6793 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6793 --repo OmniNode-ai/onex_change_control --json number,state"
