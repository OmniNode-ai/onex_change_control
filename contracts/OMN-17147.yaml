---
schema_version: "1.0.0"
ticket_id: "OMN-17147"
title: "CODEOWNERS overstates prod-anchor self-approval defense"
summary: >-
  Rewrite .github/CODEOWNERS lines 13-19 (prod-promotion grant anchor comment) to stop naming the approved_by
  != PR-author check that OMN-14814 removed. State the truth: CODEOWNERS review is the only defense against
  self-approval of grants/prod_promotion_grants.yaml in this repo, it is advisory (not mechanically enforced),
  the surviving mechanical approved_by != requested_by control lives in omninode_infra's dispatch gate,
  and reinstating a mechanical control here is tracked in OMN-17157.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "OCC in-repo evidence PR carrying this contract exists and is open/merged"
    command: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json number,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "occ-self-bind-omn-17147"
    description: >-
      OCC self-binding receipt for the in-repo evidence PR carrying this contract. Binds OMN-17147 to
      the OCC PR by pr_number so eligibility can resolve a PASS receipt for the PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "dod-codeowners-prod-anchor-text"
    description: >-
      .github/CODEOWNERS lines 13-19 no longer name approved_by != PR-author and correctly attribute the
      surviving control to omninode_infra.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -c 'approved_by != PR-author\|is a complete defense only while' .github/CODEOWNERS
