---
schema_version: "1.0.0"
ticket_id: OMN-10407
title: "Durable-evidence gate before Linear Done transition"
summary: "Refuse Linear Done when the OCC receipt is local-only or the contract cites a non-merged/superseded
  PR"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR checks pass"
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Durable-evidence gate unit tests pass (pass + 4 hard-fail surfaces + helpers)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_dod_verify/test_durable_evidence_gate.py -v"
  - id: "dod-002"
    description: "Gate refuses Linear Done when OCC receipt is local-only (untracked) — covers OMN-9855
      incident shape"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_dod_verify/test_durable_evidence_gate.py::TestDurableEvidenceGate::test_untracked_receipt_hard_fails
          -v"
  - id: "dod-003"
    description: "Gate refuses Linear Done when contract cites a superseded (CLOSED-not-merged) PR"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_dod_verify/test_durable_evidence_gate.py::TestDurableEvidenceGate::test_contract_cites_superseded_pr_hard_fails
          -v"
  - id: "dod-004"
    description: "Gate refuses Linear Done when OCC main still has the stale contract version"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_dod_verify/test_durable_evidence_gate.py::TestDurableEvidenceGate::test_stale_occ_main_hard_fails
          -v"
  - id: "dod-005"
    description: |
      Deploy posture for this slice. The DurableEvidenceGate service this PR adds
      is pure-logic + Protocol-typed probes — there is no runtime container to
      deploy, no Kafka topic to produce, and no subprocess-backed wiring. Concrete
      probe implementations (subprocess git ls-tree, gh pr view, yaml-load-on-ref)
      and the FSM-phase wiring that invokes the gate before any Linear Done
      transition ship in a follow-up ticket and will carry their own deploy
      evidence at that time.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for this slice — pure-logic gate, no runtime container
          or topic touched; follow-up ticket carries the deploy evidence when concrete probes and FSM
          wiring land'"
