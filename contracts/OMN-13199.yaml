---
schema_version: "1.0.0"
ticket_id: "OMN-13199"
title: "OMN-13199 — A5 un-allowlist topic_constants.py from the topic-literal gate (fail-closed)"
summary: >-
  Phase A5 (LAST) of epic OMN-13188 (Workstream A — all topics from contracts). omnibase_infra PR #2018
  completes the un-allowlisting of event_bus/topic_constants.py from the local topic-literal gate (scripts/validation/check_topic_literals.py).
  After OMN-13195/A4 (#2006) narrowed the whole-file exemption and OMN-13202 (#2017) deleted the last
  TOPIC_* literals (migrating the enum codegen to read the contract-declarative runtime/topics.yaml manifest)
  and removed the per-line baseline entries, the file holds only DLQ builder logic. This PR drops the
  now-dead 'topic_constants\\.py' trigger from the topic-enum-drift-check files: regex in .pre-commit-config.yaml
  (the codegen no longer reads that file's AST, so re-running drift on its changes was a dead trigger),
  retargets it to 'topics\\.yaml', refreshes the stale historical comments in check_topic_literals.py
  / topic_literal_baseline.txt / the no-hardcoded-topics block, and adds a permanent fail-closed regression
  test proving (a) topic_constants.py is not in _EXCLUDED_FILENAMES, (b) it is scanned, (c) it is literal-free
  today, and (d) the gate returns exit 1 on a reintroduced literal. Both topic gates remain green; the
  gate now fails closed on any future reintroduced literal in topic_constants.py.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2018"
    description: >-
      A5 (OMN-13188 Workstream A) evidence for omnibase_infra PR #2018: topic_constants.py is fully un-allowlisted
      from the topic-literal gate. POSITIVE proof — check_topic_literals.py exits 0 on the current tree,
      collect_files includes topic_constants.py (scanned), find_topic_literals returns [] for it. NEGATIVE
      / fail-closed proof — reintroducing a bare topic literal in topic_constants.py makes the gate print
      'FAIL: Found 1 new raw topic literal(s)' and return exit 1. The dead 'topic_constants\\.py' trigger
      is removed from the topic-enum-drift-check files: regex and retargeted to 'topics\\.yaml'; topic-enum-drift-check
      (generate_topic_enums.py --check) still exits 0. 5 new fail-closed tests pass; 182 validation +
      topic-tooling tests pass; ruff format/check + mypy clean on changed files; pre-commit run --files
      (all changed) all hooks Passed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13199/dod-infra-pr-2018/command.yaml"
          && grep -q 'commit_sha: "500e9ccecd0a244f14fbbeecce36e985c16a7cf5"' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13199/dod-infra-pr-2018/command.yaml"
  - id: "dod-occ-pr-2713"
    description: >-
      onex_change_control PR #2713 (dev) carries the OMN-13199 central Evidence-Source contract and receipt
      evidence; this self-binding receipt makes the OCC PR satisfy its own OCC eligibility check.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13199/dod-occ-pr-2713/command.yaml"
          && grep -q '^pr_number: 2713$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13199/dod-occ-pr-2713/command.yaml"
