---
schema_version: "1.0.0"
ticket_id: "OMN-16773"
title: "Autobind OCC evidence for OMN-16773"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2940.
  PR #2940 closed unmerged; the actual delivered work landed via PR #2955 (merged 8e1be3c32). Per the
  2026-08-28 sprint-board adjudication (docs/tracking/2026-08-28-sprint-board-adjudication.md), the two
  dod_evidence items whose live-PR-state binder is permanently bound to #2940 (dod-OmniNode-ai-omnibase_infra-pr-2940,
  dod-deploy-assessment) are superseded below by append-only -rebind-16773 entries bound to #2955; the
  historical entries remain immutable per the OCC Append-Only Gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2955 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2940"
    description: "PR #2940 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_chain_canary_effect/handlers/handler_chain_canary.py?ref=8c150e622cff1aba7fe13bfee65d074f70c6d9bd --jq '.content' | base64 -d | grep -c 'def _post_skill_via_httpx'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_chain_canary_effect/handlers/handler_chain_canary.py?ref=8c150e622cff1aba7fe13bfee65d074f70c6d9bd --jq '.content' | base64 -d | grep -c 'def _post_skill_via_httpx'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2940 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_chain_canary_effect/handlers/handler_chain_canary.py?ref=8c150e622cff1aba7fe13bfee65d074f70c6d9bd --jq '.content' | base64 -d | grep -c 'def _post_skill_via_httpx'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7328"
    description: "Binds OMN-16773 to OCC companion PR #7328 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7328 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2955"
    description: "PR #2955 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_chain_canary_effect/handlers/handler_chain_canary.py?ref=73b53a717737c30d9f618e514943505e8841823a --jq '.content' | base64 -d | grep -c 'def _post_skill_via_httpx'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2955-ci"
    description: "PR #2955 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2955 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "occ-self-bind-pr-7372"
    description: "OCC companion PR #7372 — self-bind for OMN-16773 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7372 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-pr-2940-rebind-16773"
    description: >-
      Append-only replacement for dod-OmniNode-ai-omnibase_infra-pr-2940. The historical entry pinned
      PR #2940, which closed unmerged (never landed) -- its auto-appended ::pr-live-state check can never
      be satisfied, since that check defaults to requiring MERGED. This entry asserts PR #2940's real,
      intentional CLOSED state in the same clause as its own PR reference (OMN-16087 override), so the
      historical entry's stale MERGED expectation is not inverted into a permanent false failure. The
      actual delivered work is proven by dod-OmniNode-ai-omnibase_infra-pr-2955 above (PR #2955, merged
      8e1be3c32a67a700c16b351bfc4236fe6a984e90).
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2940"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2940 --repo OmniNode-ai/omnibase_infra --json state --jq .state | grep
          -qx CLOSED"
  - id: "dod-deploy-assessment-rebind-16773"
    description: >-
      Append-only replacement for dod-deploy-assessment. The historical entry's own receipt records pr_number:
      2940 (PR #2940, closed unmerged), so its live-PR-state fallback can never resolve to MERGED. This
      entry re-proves the F-05 deploy-scope requirement against the PR that actually delivered the work,
      #2955 (merged 8e1be3c32a67a700c16b351bfc4236fe6a984e90) -- same content-bound probe, rebased onto
      the merged head's commit SHA.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_chain_canary_effect/handlers/handler_chain_canary.py?ref=73b53a717737c30d9f618e514943505e8841823a --jq '.content' | base64 -d | grep -c 'def _post_skill_via_httpx'
  - id: "occ-self-bind-pr-7440"
    description: "OCC companion PR #7440 — self-bind for OMN-16773 rebind onto #2955 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7440 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-admissibility-rebind-16773"
    description: >-
      Append-only replacement for dod-occ-evidence-admissibility-validator. Missed in the first #7440
      rebind pass: that entry's id names no PR, but its own historical receipt records pr_number: 2940
      / probe_command "gh pr view 2940 ..." (receipt-derived fallback, tier c), so its auto-appended ::pr-live-state
      check inherited the same permanent-CLOSED-PR failure as the other two #2940-bound entries -- confirmed
      live by re-running dod_verify after the #7440 merge (status=failed, "PR not merged (state=CLOSED)").
      This entry re-runs the same admissibility suite with a receipt bound to #2955 instead.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-evidence-admissibility-validator"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
