---
schema_version: "1.0.0"
ticket_id: "OMN-10588"
title: "Task 16: Add services.omnimarket to shared_key_registry.yaml; bump version if new"
summary: >
  Extend the services.omnimarket block in omnibase_infra/config/shared_key_registry.yaml (schema 1.2,
  added in OMN-10563) with db connection keys (POSTGRES_HOST, POSTGRES_PORT, POSTGRES_USER, POSTGRES_PASSWORD)
  and a new infisical transport section (INFISICAL_ADDR, INFISICAL_PROJECT_ID). Version stays at 1.2 —
  services section already existed. Part of Stage 4 of the omnimarket public-shippable plan (OMN-10561).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "19 unit tests pass covering new db connection keys and infisical section."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          if [ "$REPO" = "OmniNode-ai/onex_change_control" ]; then grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10588/dod-unit-tests/command.yaml";
          else uv run pytest tests/unit/config/test_shared_key_registry_omnimarket.py -q; fi
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10588 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10588/dod-occ-pr/command.yaml"
