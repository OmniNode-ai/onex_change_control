---
schema_version: "1.0.0"
ticket_id: "OMN-10724"
title: "Build bus-backed protocol adapters for ADR canary orchestrator sub-handler dependencies"
summary: >
  Convert the ADR canary orchestrator to DI-container-only construction and add bus-backed protocol adapters
  for document ingestion, decision extraction, extraction grading, and draft generation.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
  - topics
  - protocols
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-adr-canary-tests"
    description: "ADR canary orchestrator focused tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10724/dod-adr-canary-tests/command.yaml\""
  - id: "dod-ruff-check"
    description: "Touched ADR canary files pass ruff."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10724/dod-ruff-check/command.yaml\""
  - id: "dod-ruff-format"
    description: "Touched ADR canary files are formatted."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10724/dod-ruff-format/command.yaml\""
  - id: "dod-constructor-di"
    description: "Touched runtime constructors are DI-container-only."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10724/dod-constructor-di/command.yaml\""
  - id: "dod-real-bus-chain"
    description: "ADR Canary real bus-backed golden chain reaches ingestion, extraction, grading, and
      draft sub-nodes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10724/dod-real-bus-chain/command.yaml\""
  - id: "dod-deploy-plan"
    description: "No live deploy or runtime restart was performed; post-merge validation plan is documented."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10724/dod-deploy-plan/command.yaml\""
