---
schema_version: "1.0.0"
ticket_id: "OMN-9773"
title: "Add E2E verification tests for contract validation pipeline"
summary: >
  Adds end-to-end verification coverage for contract classification, normalization, and Pydantic validation
  across canonical, legacy, and known-bad contract shapes in omnibase_core.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-e2e-validation-tests"
    description: "Focused E2E contract validation test suite passes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9773/dod-e2e-validation-tests/command.yaml\""
  - id: "dod-pr-1050-head"
    description: "Bind OMN-9773 evidence to omnibase_core PR #1050 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9773/dod-pr-1050-head/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to the onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9773/dod-occ-pr/command.yaml\""
