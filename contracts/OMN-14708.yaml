---
schema_version: "1.0.0"
ticket_id: "OMN-14708"
title: "Reap leaked terminal-correlator daemon threads in runtime tests"
summary: >
  Bind OCC evidence for OmniNode-ai/omnibase_infra PR #2326, which repairs test-isolation flakes by reaping
  terminal-correlator daemon threads and sanitizing leaked GIT_* hook environment in runtime test helpers.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2326 product diff scope present"
    command: "PR_NUMBER=2326; REPO=OmniNode-ai/omnibase_infra; gh pr diff ${PR_NUMBER} --repo ${REPO}
      --name-only | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "product-pr-2326"
    description: "Product PR #2326 on OmniNode-ai/omnibase_infra adds test-scope runtime isolation fixes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=2326; REPO=OmniNode-ai/omnibase_infra; gh pr diff ${PR_NUMBER} --repo
          ${REPO} --name-only | grep -q 'tests/unit/runtime/test_terminal_correlator_thread_teardown.py'"
  - id: "occ-self-bind-pr-4317"
    description: "OCC batch PR #4317 binds OMN-14708 evidence for OmniNode-ai/omnibase_infra#2326."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4317; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
