---
schema_version: "1.0.0"
ticket_id: "OMN-13501"
title: "Repin omnibase-core 0.46.4 + wire no-faked-boundary gate report-only (omnimarket)"
summary: >-
  OCC evidence for omnimarket PR #1590, which repins omnibase-core 0.46.1 -> 0.46.4 (PyPI; carries the
  OMN-13502/#1377 no-faked-boundary detector FP-fix + #1379 gate) and wires the no-faked-boundary gate
  into omnimarket in REPORT-ONLY mode. The gate stays advisory until omnimarket's ~50 pre-existing boundary
  fakes (35 patch_httpx + 10 mock_assigned + 5 class) are burned down with the OMN-13499 recorded-replay
  harness, then flips blocking (OMN-13502 Phase 3). Config + lock + comment + one test-assertion update;
  no runtime contract change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-self"
    description: "This onex_change_control PR carries the net-new OMN-13501 contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13501/dod-occ-self/command.yaml && grep -q '^ticket_id:
          "OMN-13501"$' drift/dod_receipts/OMN-13501/dod-occ-self/command.yaml
  - id: "dod-omnimarket-pr-1590"
    description: "omnimarket PR #1590 repins omnibase-core to 0.46.4 and wires the report-only no-faked-boundary
      gate."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13501/dod-omnimarket-pr-1590/command.yaml &&
          grep -q '^pr_number: 1590$' drift/dod_receipts/OMN-13501/dod-omnimarket-pr-1590/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for omnimarket PR #1590. The PR touches a runtime validator corpus but only wires
      a report-only CI/pre-commit gate and dependency repin; no live runtime deploy was performed by this
      PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13501/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-13501/dod-deploy-assessment/command.yaml &&
          grep -q '^pr_number: 1590$' drift/dod_receipts/OMN-13501/dod-deploy-assessment/command.yaml
  - id: "dod-occ-3537-self-binding"
    description: >-
      onex_change_control PR #3537 carries follow-up OMN-13501 deploy-assessment evidence and binds this
      OCC evidence PR itself so occ-preflight eligibility can verify the PR/ticket linkage.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13501/dod-occ-3537-self-binding/command.yaml
          && grep -q '^pr_number: 3537$' drift/dod_receipts/OMN-13501/dod-occ-3537-self-binding/command.yaml
