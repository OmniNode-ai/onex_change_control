---
schema_version: 1.0.0
ticket_id: OMN-14556
title: OCC companion for omnibase_infra#2290 - expose node-migration-sync required-context parity gap
summary: >-
  OCC evidence companion for OMN-14556. omnibase_infra#2290 adds node-migration-sync to scripts/enforcement_parity_manifest.yaml
  as a direct gate so the report-only parity ratchet surfaces that node-migration-sync is missing from
  dev branch protection. The product PR documents that it does not execute the live required_status_checks
  branch-protection mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: manual
    description: >-
      omnibase_infra#2290 must make the node-migration-sync parity gap visible in the required-context
      parity manifest without mutating live branch protection.
    command: >-
      gh pr view 2290 --repo OmniNode-ai/omnibase_infra --json headRefOid,files,body and inspect scripts/enforcement_parity_manifest.yaml
      at the product head.
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-2290-head-d5e0d1e-parity-manifest
    description: >-
      Product-observing verification that omnibase_infra#2290 head d5e0d1e1910d6bdae9bdc3ce9681e4f7d641c253
      declares node-migration-sync in scripts/enforcement_parity_manifest.yaml as the direct OMN-14556
      parity gate.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          m="$(mktemp)" && gh api "repos/OmniNode-ai/omnibase_infra/contents/scripts/enforcement_parity_manifest.yaml?ref=d5e0d1e1910d6bdae9bdc3ce9681e4f7d641c253"
          --jq .content | base64 -d > "$m" && grep -q 'node-migration-sync' "$m" && grep -q 'coverage:
          direct' "$m" && grep -q 'OMN-14556' "$m"
  - id: dod-2290-head-d5e0d1e-no-live-protection-mutation
    description: >-
      Product PR #2290 documents that OMN-14556 is a required-context parity gap: node-migration-sync
      is structurally outside CI Summary, the PR adds only manifest/report visibility, and the live branch-protection
      mutation remains operator/main gated.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          f="$(mktemp)" && gh pr view 2290 --repo OmniNode-ai/omnibase_infra --json headRefOid,files,body
          > "$f" && grep -q 'd5e0d1e1910d6bdae9bdc3ce9681e4f7d641c253' "$f" && grep -q 'scripts/enforcement_parity_manifest.yaml'
          "$f" && grep -q 'OMN-14556' "$f" && grep -qi 'structurally invisible to `CI Summary`' "$f" &&
          grep -qi 'What this PR deliberately does NOT do' "$f" && grep -qi 'required_status_checks' "$f"
