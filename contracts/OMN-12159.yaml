---
schema_version: "1.0.0"
ticket_id: "OMN-12159"
title: "Fix core→compat layer inversion in delegation models"
summary: >
  Graduates all 9 delegation runtime profile models from omnibase_compat into omnibase_core/models/contracts/
  as canonical core models. Removes the lazy __getattr__ re-export machinery from contracts/__init__.py
  and fixes validator_delegation_profile.py to import from the canonical core path. Enforces the compat→core→spi→infra
  layering rule.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1157"
    description: "omnibase_core PR #1157 — graduate 9 delegation models from compat to core, remove lazy
      __getattr__ re-exports."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12159/dod-core-pr-1157/command.yaml"
  - id: "dod-no-compat-imports"
    description: "grep -r omnibase_compat src/ in omnibase_core returns zero delegation model imports."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12159/dod-no-compat-imports/command.yaml"
  - id: "dod-tests-pass"
    description: "112 delegation tests pass: uv run pytest tests/ -k delegation -v."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12159/dod-tests-pass/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC evidence for OMN-12159."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12159/dod-occ-pr/command.yaml"
