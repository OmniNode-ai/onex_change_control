---
schema_version: "1.0.0"
ticket_id: "OMN-17888"
title: "Autobind OCC evidence for OMN-17888"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3234.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3234 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3234 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/scripts/test_trigger_producer_effect_assertion.py tests/scripts/test_trigger_redeploy_start.py tests/unit/scripts/test_pr_merged_rebuild_trigger.py -q
  - kind: "tests"
    description: |-
      AC4 behavior proof for PR #3251 on OmniNode-ai/omnibase_infra: the dev-lane staleness guard rejects the verbatim captured bytes of the 2026-09-06 incident (lane 9 commits / 8h35m behind dev with every mechanism signal green) and accepts a lane sitting on dev head. Bound as dod_evidence item `dod-ac4-dev-lane-staleness-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/ci/test_dev_lane_staleness.py tests/ci/test_incident_replay_omn17888.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3234"
    description: "PR #3234 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/scripts/test_trigger_redeploy_start.py?ref=b83cc4ac9315898e8a207643b196b1748fda996e --jq '.content' | base64 -d | grep -c 'class _DeliveredMessage'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3234/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/runtime-rebuild-trigger.yml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3234 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/scripts/test_trigger_producer_effect_assertion.py tests/scripts/test_trigger_redeploy_start.py tests/unit/scripts/test_pr_merged_rebuild_trigger.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-8358"
    description: "Binds OMN-17888 to OCC companion PR #8358 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8358 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3251"
    description: "PR #3251 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/ci/check_dev_lane_staleness.py?ref=475e1612aed8112698313bef92a110ba8e1c8400 --jq '.content' | base64 -d | grep -c 'class Finding'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3251-ci"
    description: "PR #3251 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3251 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-ac4-dev-lane-staleness-behavior-proof"
    description: >-
      AC4 behavior proof (PR #3251): the dev-lane staleness guard fires on the real, measured incident
      state and stays clean on a lane that tracks dev. Added by hand because the autobind minted only
      a pr_state_surrogate for #3251, and a surrogate is exactly the shape this ticket exists to reject.
    source: "manual"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/ci/test_dev_lane_staleness.py tests/ci/test_incident_replay_omn17888.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-8433"
    description: "OCC companion PR #8433 — self-bind for OMN-17888 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8433 --repo OmniNode-ai/onex_change_control --json number,state"
