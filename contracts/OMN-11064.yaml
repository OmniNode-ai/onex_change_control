---
schema_version: "1.0.0"
ticket_id: "OMN-11064"
title: "Onboarding standalone_quickstart targets core_installed and uses env-var verification"
summary: >
  Central OCC evidence spanning two implementation PRs: omnimarket PR #654 updates the omnimarket local
  fallback standalone_quickstart policy to target core_installed (was first_node_running) and excludes
  run_standalone_node; omnibase_infra PR #1605 introduces env-var interpolation in the verification executor
  and updates the canonical onboarding graph to reference ${POSTGRES_HOST}/${REDPANDA_HOST}/${OMNIDASH_HOST}
  placeholders instead of hardcoded localhost addresses.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omnimarket golden chain + standalone_quickstart policy tests pass."
    command: "See drift/dod_receipts/OMN-11064/dod-tests-omnimarket/command.yaml"
  - kind: "tests"
    description: "omnibase_infra verification_executor unit + canonical graph interpolation integration
      tests pass."
    command: "See drift/dod_receipts/OMN-11064/dod-tests-omnibase-infra/command.yaml"
  - kind: "ci"
    description: "Central OCC PR carries the OMN-11064 contract and receipt bundle."
    command: "See drift/dod_receipts/OMN-11064/dod-occ-pr-1052/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests-omnimarket"
    description: "omnimarket #654 local fallback standalone_quickstart targets core_installed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-11064/dod-tests-omnimarket/command.yaml
          && grep -q 'pr_number: 654' drift/dod_receipts/OMN-11064/dod-tests-omnimarket/command.yaml"
  - id: "dod-tests-omnibase-infra"
    description: "omnibase_infra #1605 env-var interpolation in verification executor + canonical graph
      integration."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-11064/dod-tests-omnibase-infra/command.yaml
          && grep -q 'pr_number: 1605' drift/dod_receipts/OMN-11064/dod-tests-omnibase-infra/command.yaml"
  - id: "dod-occ-pr-1052"
    description: "onex_change_control PR #1052 carries the central OMN-11064 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11064/dod-occ-pr-1052/command.yaml
          && grep -q '^pr_number: 1052$' drift/dod_receipts/OMN-11064/dod-occ-pr-1052/command.yaml"
