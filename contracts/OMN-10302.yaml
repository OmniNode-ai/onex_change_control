---
schema_version: 1.0.0
ticket_id: OMN-10302
title: 'feat(OMN-10302): add cost-by-repo manifest entry on IBarChartAdapter'
summary: >
  Adds cost-by-repo entry to MVP_COMPONENTS, binding to IBarChartAdapter (implementationKey: IBarChartAdapter/threejs)
  with fieldMappings { x: 'repo_name', y: 'total_cost_usd' }. Upstream blocked: repo_name column absent
  from llm_cost_aggregates (migration 031:142 has only aggregation_key). Widget renders upstream-blocked
  empty state until upstream resolves. Also extends ComponentManifest.emptyState with optional reasons
  field (additive, backward-compatible).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: FULL
evidence_requirements:
  - kind: tests
    description: 48 tests pass covering manifest validation and upstream-blocked scenario
    command: npx vitest run shared/types/component-manifest.test.ts src/components/charts/threejs/BarChart.test.tsx
  - kind: ci
    description: Registry generator exits clean with 9 components
    command: npx tsx scripts/generate-registry.ts
  - kind: ci
    description: TypeScript typecheck clean
    command: npx tsc --noEmit
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >
      Manifest entry validates; uses TOPICS.costByRepo; implementationKey routes to IBarChartAdapter/threejs;
      emptyState.reasons has both no-data and upstream-blocked citing migration 031:142
    source: generated
    checks:
      - check_type: command
        check_value: "npx vitest run shared/types/component-manifest.test.ts 2>&1 | grep 'Tests.*31 passed'"
  - id: dod-002
    description: BarChart renders missing-field when fixture rows lack repo_name (upstream-blocked scenario)
    source: generated
    checks:
      - check_type: command
        check_value: "npx vitest run src/components/charts/threejs/BarChart.test.tsx 2>&1 | grep 'Tests.*17
          passed'"
  - id: dod-003
    description: Registry generator exits clean with 9 components (no validation errors)
    source: generated
    checks:
      - check_type: command
        check_value: "npx tsx scripts/generate-registry.ts 2>&1 | grep '9 components'"
  - id: dod-004
    description: PR created and auto-merge armed — PR  #55 omnidash
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view 55 --repo OmniNode-ai/omnidash --json autoMergeRequest 2>&1 | grep enabledAt"
