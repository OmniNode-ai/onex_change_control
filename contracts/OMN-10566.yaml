---
schema_version: "1.0.0"
ticket_id: "OMN-10566"
title: "Task 2: Create omnimarket.config.Settings with empty defaults"
summary: "Create omnimarket.config.Settings — a pydantic-settings BaseSettings class with all fields defaulting
  to empty/zero/false, per-service enable_* flags, and validate_required_services() fail-fast validation.
  The single canonical config layer for omnimarket production code."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "13 unit tests pass: empty construction, per-service validation, forbidden-literal regex,
      DSN precedence, singleton caching."
    command: "uv run pytest tests/unit/config/test_settings.py -v"
  - kind: "ci"
    description: "CI pipeline green on omnimarket PR #543."
    command: "gh pr checks 543 --repo OmniNode-ai/omnimarket --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "13 unit tests pass covering empty construction, per-service validation, forbidden-literal
      regex, DSN precedence, and singleton caching."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10566/dod-001/command.yaml\"; else uv run pytest
          tests/unit/config/test_settings.py -v; fi"
  - id: "dod-002"
    description: "Full unit+non-kafka test suite passes on the settings branch (3415 passed)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10566/dod-002/command.yaml\"; else uv run pytest
          tests/ -m \"not kafka and not integration\" -q; fi"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10566 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10566/dod-occ-pr/command.yaml\""
