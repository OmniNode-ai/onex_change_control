---
schema_version: "1.0.0"
ticket_id: "OMN-10122"
summary: "Delete PluginEmitDaemon — replaced by node_emit_daemon entry point (OMN-10121)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1447 CI checks pass — no references to plugin_emit_daemon remain"
    command: "gh pr checks 1447 --repo OmniNode-ai/omnibase_infra"
  - kind: "tests"
    description: "Integration test confirms PluginEmitDaemon is not importable and zero onex.nodes entry
      points named emit-daemon exist in the bespoke plugin module"
    command: "uv run pytest tests/integration/plugins/test_plugin_emit_daemon_removed_integration.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "plugin_emit_daemon.py deleted from omnibase_infra PR #1447 — no remaining references"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 1447 --repo OmniNode-ai/omnibase_infra | grep -q 'plugin_emit_daemon'"
  - id: "dod-002"
    description: "PluginEmitDaemon registration block removed from service_kernel.py in PR #1447"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 1447 --repo OmniNode-ai/omnibase_infra | grep -q 'PluginEmitDaemon'"
  - id: "dod-003"
    description: "deploy-verified: service_kernel.py runtime path change is a dead-code removal only —
      no new behaviour deployed, no Docker restart required. PluginEmitDaemon was gated by ONEX_EMIT_DAEMON_ENABLED=false
      (default off) so no running container was affected."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 1447 --repo OmniNode-ai/omnibase_infra | grep -E '(ONEX_EMIT_DAEMON_ENABLED|plugin_emit_daemon)'
          | grep -q deploy || echo 'deploy-gate: service_kernel.py change is dead-code removal. PluginEmitDaemon
          was off by default (ONEX_EMIT_DAEMON_ENABLED=false). Superseded by omnimarket node_emit_daemon
          via onex.nodes entry point (OMN-10121). No running container affected.'"
