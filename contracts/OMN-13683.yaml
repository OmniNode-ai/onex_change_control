---
schema_version: '1.0.0'
ticket_id: OMN-13683
title: 'WS-5 Wave 9: deploy/dashboard/repo-hygiene EFFECT multi-param integration tests (read-only)'
summary: >-
  Adds real multi-parameter integration tests (NOT smoke, NOT shape-grep, NOT assert-no-raise) for five
  omnimarket nodes — node_prod_promotion_gate_compute, node_dashboard_sweep, node_dirty_canonical_sweep,
  node_agent_learning_retrieval_effect, node_golden_chain_sweep. Each test parametrizes >=3 distinct param
  sets and carries >=1 negative control that forces a finding, asserting typed result fields / gate decisions
  / tri-state aggregation. The prod-promotion coverage is the gate sub-node ONLY (ALLOW / BLOCK / UNKNOWN-fail-closed);
  no live deploy EFFECT, no live prod, no .201 — all read-only / in-memory on injected collaborators.
  The live Qdrant vector-query path for node_agent_learning_retrieval_effect is env-gated like e2e_probe
  (skipped unless OMN_ALLOW_LIVE_QDRANT_RETRIEVAL), not faked. Delivered by omnimarket PR #1479.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >-
      The five new multi-param integration test dirs run green (59 passed, 1 env-gated skip) with no external
      services.
    command: 'uv run pytest tests/integration/node_prod_promotion_gate_compute tests/integration/node_dashboard_sweep
      tests/integration/node_dirty_canonical_sweep tests/integration/node_agent_learning_retrieval_effect
      tests/integration/node_golden_chain_sweep -q'
  - kind: ci
    description: CI pipeline green on the omnimarket PR
    command: 'gh pr checks 1479 --repo OmniNode-ai/omnimarket'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >-
      Five new multi-param integration test files added (one per node), each @pytest.mark.integration
      @pytest.mark.parametrize with >=3 distinct param sets and >=1 negative control. The five dirs run
      59 passed, 1 skipped (env-gated live Qdrant E2E).
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13683/dod-001/command.yaml"
  - id: dod-002
    description: >-
      Full omnimarket suite (no -k filter) green: 9948 passed, 47 skipped, 9 deselected under `uv run
      pytest tests/ -m "not kafka" -q`.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13683/dod-002/command.yaml"
  - id: dod-003
    description: >-
      Static gates clean: `uv run mypy src/omnimarket/ --strict` Success (2572 files) and mypy --strict
      clean on the five new test files; ruff format/check clean; pre-commit hooks Passed on the new files.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13683/dod-003/command.yaml"
  - id: dod-deploy-assessment
    description: >-
      Static deploy assessment: OMN-13683 is a test-only change. No live deploy, runtime restart, prod
      promotion, ECR push, cluster mutation, or .201 interaction is performed. The prod-promotion coverage
      exercises the gate COMPUTE sub-node as a pure in-memory decision only.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13683/dod-deploy-assessment/command.yaml"
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt — this onex_change_control PR carries the OMN-13683 contract + receipts
      consumed by omnimarket PR #1479 via Evidence-Source.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13683/dod-occ-self/command.yaml"
