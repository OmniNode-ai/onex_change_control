---
schema_version: 1.0.0
ticket_id: OMN-14977
title: Autobind OCC evidence for OMN-14977
summary: 'OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1881.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #1881 product diff scope present'
    command: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-OmniNode-ai-omnimarket-pr-1881
    description: "PR #1881 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-OmniNode-ai-omnimarket-pr-1881-ci
    description: "PR #1881 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
  - id: occ-self-bind-pr-4740
    description: "OCC companion PR #4740 — self-bind for OMN-14977 (OMN-14650)."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-omn14977-pr1881-deploy
    description: "PR #1881 deploy disposition — new node_worker_memory_admission_compute pure-COMPUTE
      node (zero I/O, zero bus topics) carries an explicit post-merge dev deploy validation bar."
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 1881 --repo OmniNode-ai/omnimarket --json body,files --jq 'if ((.body
          | test("Post-merge dev deploy validation")) and (.body | test("node_worker_memory_admission_compute"))
          and any(.files[].path; . == "src/omnimarket/nodes/node_worker_memory_admission_compute/handlers/handler_worker_memory_admission_compute.py"))
          then "deploy disposition verified for OMN-14977 worker memory admission compute node" else error("OMN-14977
          deploy disposition missing PR body bar or runtime path") end'
  - id: occ-self-bind-pr-4785
    description: "OCC companion PR #4785 — self-bind for OMN-14977 deploy disposition."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view 4785 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: dod-omn14977-pr1881-head-6553f2c2
    description: 'PR #1881 current product head after required-check skip-vector repair.'
    source: manual
    checks:
      - check_type: command
        check_value: 'gh pr view 1881 --repo OmniNode-ai/omnimarket --json headRefOid,state --jq ''if
          .state == "OPEN" and .headRefOid == "6553f2c2ff72ac7feae4f48c723ba95f078529df" then "OMN-14977
          PR #1881 current head verified" else error("OMN-14977 PR #1881 head/state mismatch") end'''
  - id: occ-self-bind-pr-4806
    description: "OCC companion PR #4806 — self-bind for OMN-14977 current-head evidence."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view 4806 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: dod-omn14977-pr1881-head-aee671d4
    description: 'PR #1881 final product head after rebase onto dev following OMN-15057/OMN-15059 base
      merges.'
    source: manual
    checks:
      - check_type: command
        check_value: 'gh pr view 1881 --repo OmniNode-ai/omnimarket --json headRefOid,state --jq ''if
          .state == "OPEN" and .headRefOid == "aee671d48f1817b20f3293e2a4468d20b2c79570" then "OMN-14977
          PR #1881 final rebased head verified" else error("OMN-14977 PR #1881 head/state mismatch") end'''
    evidence_artifact: supersedes_dod_evidence:dod-omn14977-pr1881-head-6553f2c2
  - id: occ-self-bind-pr-4816
    description: "OCC companion PR #4816 — self-bind for OMN-14977 final-head evidence."
    source: generated
    checks:
      - check_type: command
        check_value: 'gh pr view 4816 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq ''if .number == 4816 and .state == "OPEN" and .headRefName == "codex-letter/omn-14977-pr1881-final-head-aee671d4"
          then "OMN-14977 OCC PR #4816 self-bind verified" else error("OMN-14977 OCC PR #4816 self-bind
          mismatch") end'''
