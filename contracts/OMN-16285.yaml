---
schema_version: "1.0.0"
ticket_id: "OMN-16285"
title: "Autobind OCC evidence for OMN-16285"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/onex_change_control
  PR #6771.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #6771 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-onex_change_control-pr-6771"
    description: "PR #6771 on OmniNode-ai/onex_change_control — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6771 --repo OmniNode-ai/onex_change_control --json number,state"
      - check_type: "command"
        check_value: "gh pr view 6771 --repo OmniNode-ai/onex_change_control --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-onex_change_control-pr-6771"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6773"
    description: >-
      Stable self-bind proving this OCC companion PR exists. The probe intentionally checks only the immutable
      PR number through the GitHub API; it does not assert transient open/closed state.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/6773 --jq .number | grep -x 6773
  - id: "dod-omn16285-companion-audit"
    description: "Live audit of 5 recently merged OCC companion PRs (#6685, #6735, #6676, #6757, #6755)
      — every changed file matched contracts/*.yaml or drift/dod_receipts/**/*.yaml (including *.supersede.<N>.yaml),
      including one with 15 files. Derives the exact-allowlist predicate's two regex patterns."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/onex_change_control/pulls/6755/files --jq '[.[].filename]'"
  - id: "dod-omn16285-predicate-tests"
    description: "27 unit tests for scripts/ci/evidence_only_predicate.py pass: exact-allowlist matches
      (contracts/, contracts/v1/, drift/dod_receipts/, .yml variant), negative controls (scripts/, .github/,
      nested contracts/v1/subdir, grants/, allowlists/, substring traps), whole-diff predicate (pure/mixed/pure-code/empty),
      the single-non-evidence-file falsification control, and GITHUB_OUTPUT wiring."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/ci/test_evidence_only_predicate.py -q"
  - id: "dod-omn16285-gate-tests"
    description: "78 tests pass across tests/ci/ (46 pre-existing ci_summary_gate.py tests, unmodified
      in substance; 6 new OMN-16285 tests: predicate tier is a subset of SKIPPABLE not STRICT, predicate
      job is CLASSIFICATION_ONLY not a gate, all-skipped-with-predicate-success evaluates SUCCESS, failed-predicate-cascade
      fails closed via the sweep, a genuine job failure in the tier still fails closed; plus the 27 predicate
      tests)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/ci/test_ci_summary_gate.py tests/ci/test_evidence_only_predicate.py
          -q"
  - id: "dod-omn16285-regression-scope-tests"
    description: "Regression-scope tests pass: tests/ci/ (both new modules + the pre-existing gate suite)
      and tests/unit/scripts/test_check_scripts_canonical.py (the scripts-canonical-form guard whose allowlist
      this PR extends) -- 185 tests, the two surfaces this diff actually touches. Re-executable within
      the receipt gate's hard 60s command-execution ceiling (confirmed live: the original 6216-test full-suite
      check_value was BLOCKed here with 'Command timed out after 60s'). The full local suite (6216 passed,
      47 skipped, 0 failed) was verified manually twice outside this gate -- see dod-omn16285-companion-audit's
      sibling PR-body section and the OMN-16285 PR body for that non-re-executable evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/ci/ tests/unit/scripts/test_check_scripts_canonical.py -q"
  - id: "dod-omn16285-mypy"
    description: "mypy --strict clean on both changed CI-layer modules."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run mypy scripts/ci/evidence_only_predicate.py scripts/ci/ci_summary_gate.py
          --strict"
  - id: "dod-omn16285-precommit-clean"
    description: "pre-commit run on the changed-file set is clean, including the scripts-canonical-form
      guard (OMN-14475) after registering the new script as a permanent CI-layer-glue exception in allowlists/scripts_exceptions.yaml,
      and the yamlfmt hook after it reflowed that new entry's folded block scalar (verified no contamination-sentinel
      corruption, benign line-wrap only)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          SKIP=check-receipt-honesty,check-receipt-hardening,reject-required-check-skip-vector uv run
          pre-commit run --files .github/workflows/ci.yml scripts/ci/ci_summary_gate.py tests/ci/test_ci_summary_gate.py
          scripts/ci/evidence_only_predicate.py tests/ci/test_evidence_only_predicate.py allowlists/scripts_exceptions.yaml
  - id: "occ-self-bind-pr-6771"
    description: "Binds OMN-16285 to OCC PR #6771 via its actual file list (not a bare PR-existence lookup,
      per the OMN-15309 admissibility predicate's PR-metadata-only rejection -- /pulls/N/files returns
      real diff content, /pulls/N alone would not)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/onex_change_control/pulls/6771/files --jq '[.[].filename]'"
