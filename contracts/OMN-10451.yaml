---
schema_version: "1.0.0"
ticket_id: OMN-10451
title: "Fix runtime_profiles on omnimarket contracts that crash under main profile"
summary: "Audit omnimarket contract handlers with required constructor dependencies, declare runtime profile
  ownership or dependency ownership, and add a validation test that keeps main-profile crashers excluded."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Runtime profile ownership validation tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10451/dod-runtime-profile-tests/command.yaml"
  - kind: "manual"
    description: "Touched runtime profile validation test passes lint"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10451/dod-lint/command.yaml"
  - kind: "manual"
    description: "All omnimarket node contracts parse as YAML mappings"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10451/dod-contract-parse/command.yaml"
  - kind: "manual"
    description: "Repo-local ticket contract validates"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10451/dod-contract-validate/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-runtime-profile-tests"
    description: "Runtime profile ownership validation tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10451/dod-runtime-profile-tests/command.yaml
          && grep -q '^ticket_id: OMN-10451$' drift/dod_receipts/OMN-10451/dod-runtime-profile-tests/command.yaml
          && grep -q '^evidence_item_id: dod-runtime-profile-tests$' drift/dod_receipts/OMN-10451/dod-runtime-profile-tests/command.yaml"
  - id: "dod-lint"
    description: "Touched runtime profile validation test passes lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10451/dod-lint/command.yaml && grep
          -q '^ticket_id: OMN-10451$' drift/dod_receipts/OMN-10451/dod-lint/command.yaml && grep -q '^evidence_item_id:
          dod-lint$' drift/dod_receipts/OMN-10451/dod-lint/command.yaml"
  - id: "dod-contract-parse"
    description: "All omnimarket node contracts parse as YAML mappings"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10451/dod-contract-parse/command.yaml
          && grep -q '^ticket_id: OMN-10451$' drift/dod_receipts/OMN-10451/dod-contract-parse/command.yaml
          && grep -q '^evidence_item_id: dod-contract-parse$' drift/dod_receipts/OMN-10451/dod-contract-parse/command.yaml"
  - id: "dod-contract-validate"
    description: "Repo-local ticket contract validates"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10451/dod-contract-validate/command.yaml
          && grep -q '^ticket_id: OMN-10451$' drift/dod_receipts/OMN-10451/dod-contract-validate/command.yaml
          && grep -q '^evidence_item_id: dod-contract-validate$' drift/dod_receipts/OMN-10451/dod-contract-validate/command.yaml"
