---
schema_version: "1.0.0"
ticket_id: "OMN-16271"
title: "Autobind OCC evidence for OMN-16271"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #953
  and OmniNode-ai/omniweb PR #300 (two Evidence-Source PRs on this ticket; reconciled from two independently
  bot-minted companion PRs, OCC#6740 (merged first) and OCC#6739, per docs/standards/receipt_hashing_and_supersession.md
  union-append semantics).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #953 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "ci"
    description: "PR #300 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-953"
    description: "PR #953 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 953 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 953 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-953"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6740"
    description: "Binds OMN-16271 to OCC companion PR #6740 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6740 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omniweb-pr-300"
    description: "PR #300 on OmniNode-ai/omniweb — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 300 --repo OmniNode-ai/omniweb --json number,state"
      - check_type: "command"
        check_value: "gh pr view 300 --repo OmniNode-ai/omniweb --json files"
  - id: "occ-self-bind-pr-6739"
    description: "Binds OMN-16271 to OCC companion PR #6739 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6739 --repo OmniNode-ai/onex_change_control --json number,state"
