---
schema_version: 1.0.0
ticket_id: OMN-14760
title: Evidence for omnibase_infra#2339 git-gc-auto helper
summary: >
  Central OCC evidence for omnibase_infra#2339. The product PR adds a non-destructive worktree-aware git-gc-auto
  helper and unit coverage at exact product head 2969b00d8e7bbb29f50e0ff70b5172e0b92d6dc3.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: PR  #2339 product head is exact for OMN-14760 evidence.
    command: 'PRODUCT_PR_NUMBER=2339; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=2969b00d8e7bbb29f50e0ff70b5172e0b92d6dc3;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
  - kind: ci
    description: Product-readiness-shadow completed successfully for exact PR  #2339 head.
    command: 'PRODUCT_RUN_ID=29637137758; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=2969b00d8e7bbb29f50e0ff70b5172e0b92d6dc3;
      : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
      = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
      = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-infra2339-head-2969b00
    description: >
      Current-head proof: omnibase_infra PR #2339 is bound to OMN-14760 at exact product head 2969b00d8e7bbb29f50e0ff70b5172e0b92d6dc3.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2339; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=2969b00d8e7bbb29f50e0ff70b5172e0b92d6dc3;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-infra2339-diff-2969b00
    description: >
      PR #2339 on OmniNode-ai/omnibase_infra has a non-empty reviewed product diff for OMN-14760.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2339; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q .'
  - id: dod-infra2339-shadow-2969b00
    description: >
      Product-readiness-shadow completed successfully for omnibase_infra PR #2339 at exact head 2969b00d8e7bbb29f50e0ff70b5172e0b92d6dc3
      before OCC evidence was minted.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29637137758; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=2969b00d8e7bbb29f50e0ff70b5172e0b92d6dc3;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-infra2339-deploy-2969b00
    description: >
      Deploy-scope proof for OMN-14760 evidence: the product diff is limited to a local git-gc helper
      script and unit tests; no live deploy, restart, docker exec, rpk topic produce, or production mutation
      was performed by this OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14760/dod-infra2339-deploy-2969b00/command.yaml
          && grep -qi ''no live deploy, restart, docker exec, rpk topic produce, or production mutation''
          drift/dod_receipts/OMN-14760/dod-infra2339-deploy-2969b00/command.yaml'
  - id: occ-self-bind-pr-4376
    description: >
      OCC PR #4376 on OmniNode-ai/onex_change_control self-binding proof for OMN-14760 evidence.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
