---
schema_version: "1.0.0"
ticket_id: "OMN-11048"
title: "Propagate dependency health topology file paths"
summary: >
  Central OCC evidence for omnimarket PR #648. Carries topology source file paths into dependency health
  findings and recognizes handler_routing handler modules during untested-handler analysis.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused topology and cross-reference tests cover file_path propagation and handler_routing
      paths."
    command: "See drift/dod_receipts/OMN-11048/dod-focused-tests/command.yaml"
  - kind: "ci"
    description: "Deploy/import smoke for topology parser and sweep handler passes without live deploy
      or runtime restart."
    command: "See drift/dod_receipts/OMN-11048/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused topology and cross-reference tests cover file_path propagation and handler_routing
      paths."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '30 passed' drift/dod_receipts/OMN-11048/dod-focused-tests/command.yaml
          && echo 'OMN-11048 focused-test receipt present'"
  - id: "dod-deploy"
    description: "Deploy/import smoke for ContractTopologyParser and HandlerDepHealthSweep is recorded;
      no live deploy or runtime restart was performed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'ContractTopologyParser HandlerDepHealthSweep' drift/dod_receipts/OMN-11048/dod-deploy/command.yaml
          && grep -q 'deploy' drift/dod_receipts/OMN-11048/dod-deploy/command.yaml && echo 'OMN-11048
          deploy receipt present'"
