---
schema_version: "1.0.0"
ticket_id: "OMN-14280"
title: "feat(OMN-14280): wire tenant_id onto the delegation inference leg (OMN-14208 slice-2 A-now)"
summary: >-
  Central OCC evidence for OMN-14280 (OMN-14208 slice-2 A-now). omnibase_core PR #1415 adds an optional
  top-level tenant_id (str|None, default None) to ModelInferenceIntent and ModelInferenceResponseData;
  the Breaking-Schema-Change (OMN-12621) gate reports the change non-breaking. omnimarket PR #1686 stamps
  the workflow tenant onto every ModelInferenceIntent (producer, both _build_model_inference_intent call
  sites via the slice-1 _resolve_tenant_id precedence), reads intent.tenant_id in node_llm_delegation_call_effect
  to tenant-tag its success and failure logs and round-trip the tenant onto ModelInferenceResponseData
  (consumer), and adds the mutation-verified TestInferenceTenantRoundTripOMN14280 cross-boundary test.
  An independent verifier (not the implementing agent) re-ran the mutation proof — neutering the consumer
  round-trip makes the cross-boundary test FAIL (assert None == 'tenant-alpha'/'tenant-beta') and reverting
  makes it pass — and the 80 golden-chain + inference-response projection tests pass with no re-record
  (request_hash is body-only). Isolation-READY wire, NOT isolation-enforced: the fail-closed wire==topic-tenant
  guard and the optional->required flip are a later slice (A-enforce), gated behind the gateway topic-tenant
  stamp and out of scope here. No live deploy, no runtime restart, no branch-protection mutation.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1415"
    description: >-
      Live GitHub verification shows omnibase_core PR #1415 targets dev from branch jonah/omn-14280-core-inference-tenant
      at head ed04a37fec40543173be84fd999be7042a8f47d4. The diff adds an optional top-level tenant_id
      (str|None = None) to ModelInferenceIntent and ModelInferenceResponseData in model_orchestrator_intents.py
      plus optional/default/settable unit coverage. Breaking-Schema-Change (OMN-12621) and every Tests/Integration
      split are green; the only red is the OCC Evidence-Source chain satisfied by this companion.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14280/dod-omnibase_core-pr-1415/command.yaml
          && grep -q '^pr_number: 1415$' drift/dod_receipts/OMN-14280/dod-omnibase_core-pr-1415/command.yaml
  - id: "dod-omnimarket-pr-1686"
    description: >-
      Live GitHub verification shows omnimarket PR #1686 targets dev from branch jonah/omn-14280-market-inference-tenant
      at head 2a39c4b825670cd037a2f170be0deb5612087637. The diff stamps the workflow tenant onto every
      ModelInferenceIntent (producer), reads intent.tenant_id in the inference effect to tenant-tag success
      and failure logs and round-trip the tenant onto ModelInferenceResponseData (consumer), and adds
      the mutation-verified TestInferenceTenantRoundTripOMN14280 cross-boundary test. The independent
      verifier reproduced the mutation proof (neuter the round-trip -> the test fails; revert -> it passes)
      and confirmed 80 golden-chain + projection tests green with no re-record.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14280/dod-omnimarket-pr-1686/command.yaml &&
          grep -q '^pr_number: 1686$' drift/dod_receipts/OMN-14280/dod-omnimarket-pr-1686/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment for OMN-14280 runtime-path changes on omnimarket PR #1686 (node_delegation_orchestrator
      and node_llm_delegation_call_effect handlers). No live deploy, restart, docker exec, rpk topic produce,
      or .201/runtime mutation was performed as part of this pre-merge evidence. The change is a backward-compatible
      wire field plus producer-stamp/consumer-read code; it ships in the next routine runtime image with
      no new service, topic, or migration. Merge remains gated by product CI plus post-merge deploy validation
      through the standard deploy-gate path.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14280/dod-deploy-assessment/command.yaml &&
          grep -q 'deploy-gate assessment' drift/dod_receipts/OMN-14280/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the OMN-14280 contract and PASS
      receipts consumed by omnibase_core PR #1415 and omnimarket PR #1686 through their Evidence-Source
      lines. It binds itself to occ-preflight by commit_sha from this PR's commit set instead of predicting
      the OCC PR number.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14280/dod-occ-self/command.yaml
