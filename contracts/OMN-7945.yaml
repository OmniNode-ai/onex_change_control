---
schema_version: "1.0.0"
ticket_id: OMN-7945
title: "Add budget policy COMPUTE node in omnimarket"
summary: "Add node_budget_policy_compute in omnimarket as a pure COMPUTE node for deterministic token,
  cost, and time budget policy evaluation."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
evidence_requirements:
  - kind: "ci"
    description: "omnimarket#572 checks pass for node_budget_policy_compute"
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001-omnimarket-572"
    description: "omnimarket#572 carries node_budget_policy_compute implementation and deploy-gate contract"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json headRefOid -q .headRefOid"
  - id: "dod-occ-pr-842"
    description: "onex_change_control#842 carries OMN-7945 central contract and receipt evidence"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json state,baseRefName
          --jq '.state + \":\" + .baseRefName'"
