---
schema_version: '1.0.0'
ticket_id: OMN-10412
title: "feat(OMN-10412): wire verify/verify receipt-gate caller workflow in omnimarket"
summary: >-
  Adds .github/workflows/call-receipt-gate.yml to omnimarket, invoking OmniNode-ai/omnibase_core/.github/workflows/receipt-gate.yml@main
  on pull_request and merge_group. Surfaces as the required status check `verify / verify`. Wave 1 of
  the gate-collapse fix (OMN-10410).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: Workflow file committed to omnimarket PR branch
    command: >-
      gh pr view 473 --repo OmniNode-ai/omnimarket --json files --jq '[.files[].path] | map(select(. ==
      ".github/workflows/call-receipt-gate.yml")) | length > 0'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: call-receipt-gate.yml committed to omnimarket on PR  #473
    source: generated
    checks:
      - check_type: command
        check_value: >-
          gh pr view 473 --repo OmniNode-ai/omnimarket --json files --jq '[.files[].path] | map(select(.
          == ".github/workflows/call-receipt-gate.yml")) | length > 0'
