---
schema_version: "1.0.0"
ticket_id: "OMN-12780"
title: "Wave 1C: persist + expose generated output via projection-API"
summary: >-
  Adds contract_yaml and handler_source columns to generation_events (migration 0012) so the full generated
  output from node-generation-completed terminal events is persisted without truncation. Maps both columns
  in _project_generation_completed and exposes the generation topic via projection_api.expose in contract.yaml.
  Projection-API is the only valid read path — no raw DB reads, no express reader. Migration is authored
  only; apply to live lanes is Wave 3 (user-gated).
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-migration-0012"
    description: >-
      Migration 0012 adds contract_yaml + handler_source columns to generation_events with ADD COLUMN
      IF NOT EXISTS (idempotent). Migration authored only; not applied to live lanes (Wave 3 deploy is
      user-gated).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12780/dod-migration-0012/command.yaml"
          && grep -q 'contract_yaml' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12780/dod-migration-0012/command.yaml"
          && grep -q 'IF NOT EXISTS' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12780/dod-migration-0012/command.yaml"
  - id: "dod-tdd-tests-16-pass"
    description: >-
      16 TDD tests written before implementation covering: migration structure, handler mapping of contract_yaml
      + handler_source, projection-API exposure, 100 KB no-truncation proof, SHA256 payload provenance
      verifiability. Full suite: 6303 passed, 0 failed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12780/dod-tdd-tests-16-pass/command.yaml"
          && grep -q '6303 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12780/dod-tdd-tests-16-pass/command.yaml"
  - id: "dod-projection-api-exposure"
    description: >-
      contract.yaml exposes onex.evt.omnimarket.node-generation-completed.v1 via projection_api.expose
      pointing at generation_events with all 14 output columns including contract_yaml and handler_source.
      No raw DB read path exists.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12780/dod-projection-api-exposure/command.yaml"
          && grep -q 'node-generation-completed.v1' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12780/dod-projection-api-exposure/command.yaml"
  - id: "dod-omnimarket-pr-1095"
    description: >-
      omnimarket PR #1095 carries the implementation: migration 0012, handler update, contract.yaml update,
      16 TDD tests. Target branch: dev.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12780/dod-omnimarket-pr-1095/command.yaml"
          && grep -q '^pr_number: 1095$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12780/dod-omnimarket-pr-1095/command.yaml"
          && grep -q 'eb743e9858ce13a1085c196cf3aa901c7df10da1' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12780/dod-omnimarket-pr-1095/command.yaml"
  - id: "dod-deploy-gate-migration-only"
    description: >-
      Deploy gate: migration authored and tested only. Apply to live lanes is Wave 3 (user-gated per plan
      docs/plans/2026-06-07-bus-native-sea-demo-full-plan.md §2). No docker exec or rpk topic produce
      required for this PR — schema authoring only.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12780/dod-deploy-gate-migration-only/command.yaml"
          && grep -q 'migration-authored-only' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12780/dod-deploy-gate-migration-only/command.yaml"
