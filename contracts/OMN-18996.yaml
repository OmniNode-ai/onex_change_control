---
schema_version: "1.0.0"
ticket_id: "OMN-18996"
title: "Delegation K1 Core source delivery: terminal outcome/content wire models"
summary: >-
  Child of OMN-18928 for the omnibase_core source slice. Deliver the canonical terminal outcome/content
  wire-model change independently so Core publication is not gated on the parent K1 Market emitter and
  live-lab acceptance. OMN-18928 remains the full K1 contract and is not satisfied by this child.
is_seam_ticket: true
interface_change: true
interfaces_touched: [envelopes]
contract_completeness: ENRICHED
proof_class: live-readback
evidence_requirements:
  - kind: tests
    description: >-
      The pinned omnibase_core source commit passes the focused terminal outcome/content wire-model test;
      repository-required checks for the Core PR are also recorded against that exact commit.
    command: >-
      uv run --frozen pytest -q tests/unit/models/delegation/wire/test_omn18928_terminal_outcome_content.py
  - kind: manual
    description: >-
      Evidence-Ticket names OMN-18996 and Evidence-Source names the OCC PR carrying this contract and
      receipt; no Market or runtime acceptance is asserted by this child.
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: core-wire-source-slice
    description: >-
      Core-only source slice for OMN-18928. Proves the focused canonical wire model tests on the exact
      pinned Core commit. It does not prove Market terminal emission, paired consumer compatibility, or
      complete K1.
    source: manual
    status: pending
    checks:
      - check_type: test_passes
        check_value: >-
          uv run --frozen pytest -q tests/unit/models/delegation/wire/test_omn18928_terminal_outcome_content.py
        cwd: "${OMNI_HOME}/omnibase_core"
  - id: "dod-OmniNode-ai-omnibase_core-pr-1730"
    description: "PR #1730 on OmniNode-ai/omnibase_core — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_core/contents/.github/actions/deploy-gate/models/deploy_evidence_check.py?ref=f6ee1d544d0ad06eaad339e39238650e1c496330 --jq '.content' | base64 -d | grep -c 'class ModelDeployEvidenceCheck'
  - id: "dod-OmniNode-ai-omnibase_core-pr-1730-ci"
    description: "PR #1730 on OmniNode-ai/omnibase_core — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1730 --repo OmniNode-ai/omnibase_core --json files"
  - id: "dod-occ-evidence-admissibility-validator-pr-1730"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-10680"
    description: "OCC companion PR #10680 — self-bind for OMN-18996 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 10680 --repo OmniNode-ai/onex_change_control --json number,state"
