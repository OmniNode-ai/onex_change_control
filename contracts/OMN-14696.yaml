---
schema_version: "1.0.0"
ticket_id: "OMN-14696"
title: "Autobind OCC evidence for OMN-14696"
summary: >
  OCC contract bound by node_pr_lifecycle_fix_effect Evidence-Source autobind (OMN-13317 F1) when OmniNode-ai/omnibase_infra
  PR #2321 carried a product-SHA Evidence-Source or was missing an OCC contract, and failed the gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2321 product diff scope present"
    command: "PR_NUMBER=2321; REPO=OmniNode-ai/omnibase_infra; gh pr diff ${PR_NUMBER} --repo ${REPO}
      --name-only | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2321"
    description: "PR #2321 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=2321; REPO=OmniNode-ai/omnibase_infra; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2321-ci"
    description: "PR #2321 on OmniNode-ai/omnibase_infra — product diff scope (OMN-14425 substance check;
      OMN-14650: no longer gated on source-PR CI being green)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=2321; REPO=OmniNode-ai/omnibase_infra; gh pr diff ${PR_NUMBER} --repo
          ${REPO} --name-only | grep -q ."
  - id: "occ-self-bind-pr-4301"
    description: "OCC companion PR #4301 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14696
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4301; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4302"
    description: "OCC companion PR #4302 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14696 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4302 --repo OmniNode-ai/onex_change_control --json number,state"
