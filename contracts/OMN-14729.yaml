---
schema_version: "1.0.0"
ticket_id: "OMN-14729"
title: "Align MSK managed-Kafka client group patterns to dot-form (omninode_infra #678)"
summary: >
  OCC companion evidence for omninode_infra PR #678 (MERGED): aligns managed_kafka_client_group_patterns
  in aws/cluster-dev/managed-data-plane.auto.tfvars from glob-form ("onex-*"/"omninode-*") to dot-form
  ("onex.*"/"omninode.*") so MSK client group authorization matches the actual consumer-group naming.
  Re-cut evidence companion: the original autobind companion (OCC #5011, closed unmerged) was closed by
  an OCC queue-hygiene batch pass after the product PR merged, leaving the merged product change with
  no durable receipt. This companion binds directly to the already-merged dev merge commit c3cadc25723fe57ba7c8d1e64781eae6727ba2ae
  on omninode_infra dev — never the PR head.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Merged product PR binding and content probes for omninode_infra#678."
    command: "gh pr view 678 --repo OmniNode-ai/omninode_infra --json number,state,mergeCommit"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-678-merged-c3cadc25"
    description: >
      Consuming-PR binding: omninode_infra PR #678 is MERGED; its dev merge commit c3cadc25723fe57ba7c8d1e64781eae6727ba2ae
      carries the MSK group-pattern alignment for OMN-14729. Deterministic merged-commit bind that still
      replays after merge — the receipt records the live probes (state MERGED; compare dev...c3cadc25
      = behind, never diverged; no headRefOid equality anywhere). Contract checks pin the receipt in-tree
      because omninode_infra is private and OCC CI's token cannot read it (404) — same pattern as every
      other omninode_infra entry on this evidence surface; the second check pins the exact merge commit,
      so a head-bound or retargeted receipt fails closed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'c3cadc25723fe57ba7c8d1e64781eae6727ba2ae' contracts/'OMN-14729'.yaml"
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14729/dod-infra-pr-678-merged-c3cadc25/command.yaml"
      - check_type: "command"
        check_value: "grep -q '^commit_sha: c3cadc25723fe57ba7c8d1e64781eae6727ba2ae$' drift/dod_receipts/OMN-14729/dod-infra-pr-678-merged-c3cadc25/command.yaml"
  - id: "dod-infra678-msk-group-patterns-c3cadc25"
    description: >
      Falsifiable product probe at the merged commit: aws/cluster-dev/managed-data-plane.auto.tfvars at
      c3cadc25723fe57ba7c8d1e64781eae6727ba2ae carries dot-form group patterns "onex.*" and "omninode.*"
      and zero occurrences of the removed glob forms "onex-*" / "omninode-*". The receipt records the
      replayed gh probe and its stdout (the tfvars group-pattern block at the merge commit); contract
      checks pin that recorded probe output in-tree because omninode_infra is private and OCC CI's token
      cannot read it (404).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14729/dod-infra678-msk-group-patterns-c3cadc25/command.yaml"
      - check_type: "command"
        check_value: 'grep -q ''"onex\.\*",'' drift/dod_receipts/OMN-14729/dod-infra678-msk-group-patterns-c3cadc25/command.yaml'
      - check_type: "command"
        check_value: 'grep -q ''"omninode\.\*",'' drift/dod_receipts/OMN-14729/dod-infra678-msk-group-patterns-c3cadc25/command.yaml'
  - id: "dod-occ-self"
    description: >
      OCC companion self-bind for OMN-14729 (deterministic commit-sha bind, no PR-number prediction).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14729/dod-occ-self/command.yaml"
