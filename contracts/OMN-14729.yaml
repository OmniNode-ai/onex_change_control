---
schema_version: "1.0.0"
ticket_id: "OMN-14729"
title: "Align MSK managed-Kafka client group patterns to dot-form (omninode_infra #678)"
summary: >
  OCC companion evidence for omninode_infra PR #678 (MERGED): aligns managed_kafka_client_group_patterns
  in aws/cluster-dev/managed-data-plane.auto.tfvars from glob-form ("onex-*"/"omninode-*") to dot-form
  ("onex.*"/"omninode.*") so MSK client group authorization matches the actual consumer-group naming.
  Re-cut evidence companion: the original autobind companion (OCC #5011, closed unmerged) was closed by
  an OCC queue-hygiene batch pass after the product PR merged, leaving the merged product change with
  no durable receipt. This companion binds directly to the already-merged dev merge commit c3cadc25723fe57ba7c8d1e64781eae6727ba2ae
  on omninode_infra dev — never the PR head.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Merged product PR binding and content probes for omninode_infra#678."
    command: "gh pr view 678 --repo OmniNode-ai/omninode_infra --json number,state,mergeCommit"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-678-merged-c3cadc25"
    description: >
      Consuming-PR binding: omninode_infra PR #678 is MERGED; its dev merge commit c3cadc25723fe57ba7c8d1e64781eae6727ba2ae
      carries the MSK group-pattern alignment for OMN-14729. Deterministic merged-commit bind that still
      replays after merge: the compare status of dev...c3cadc25 must be behind or identical (reachable
      from dev), never diverged — no headRefOid equality anywhere.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 678 --repo OmniNode-ai/omninode_infra --json state --jq .state | grep
          -qx MERGED"
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omninode_infra/compare/dev...c3cadc25723fe57ba7c8d1e64781eae6727ba2ae
          --jq .status | grep -Eqx 'behind|identical'"
  - id: "dod-infra678-msk-group-patterns-c3cadc25"
    description: >
      Falsifiable product probe at the merged commit: aws/cluster-dev/managed-data-plane.auto.tfvars at
      c3cadc25723fe57ba7c8d1e64781eae6727ba2ae carries dot-form group patterns "onex.*" and "omninode.*"
      and zero occurrences of the removed glob forms "onex-*" / "omninode-*".
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omninode_infra/contents/aws/cluster-dev/managed-data-plane.auto.tfvars?ref=c3cadc25723fe57ba7c8d1e64781eae6727ba2ae'
          --jq .content | base64 -d | grep -q '"onex\.\*"'
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omninode_infra/contents/aws/cluster-dev/managed-data-plane.auto.tfvars?ref=c3cadc25723fe57ba7c8d1e64781eae6727ba2ae'
          --jq .content | base64 -d | grep -q '"omninode\.\*"'
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omninode_infra/contents/aws/cluster-dev/managed-data-plane.auto.tfvars?ref=c3cadc25723fe57ba7c8d1e64781eae6727ba2ae'
          --jq .content | base64 -d | grep -c '"onex-\*"' | grep -qx 0
  - id: "dod-occ-self"
    description: >
      OCC companion self-bind for OMN-14729 (deterministic commit-sha bind, no PR-number prediction).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14729/dod-occ-self/command.yaml"
