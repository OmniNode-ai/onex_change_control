---
schema_version: "1.0.0"
ticket_id: "OMN-12729"
title: "Delegation inference-response re-enters routing instead of quality gate/completion"
summary: >-
  Central evidence for omnimarket PR #1060. The PR treats nonretryable delegation inference
  response-shape errors, including finish_reason=length and empty message content, as terminal
  delegation failures instead of routing them back through model selection. Valid responses still enter
  the quality gate and terminal completion path.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "delegation workflow lifecycle"
  - "inference response handling"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1060"
    description: "omnimarket PR #1060 carries the nonretryable inference response handling fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12729/dod-omnimarket-pr-1060/command.yaml"
          && grep -q '^pr_number: 1060$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12729/dod-omnimarket-pr-1060/command.yaml"
  - id: "dod-local-validation"
    description: "Focused nonretryable response error tests and useful-artifact golden-chain tests passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12729/dod-local-validation/command.yaml"
          && grep -q '11 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12729/dod-local-validation/command.yaml"
  - id: "dod-live-useful-delegation-proof"
    description: "Live .201 hotpatch produced a useful pytest artifact through delegation, not merely a model response."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12729/dod-live-useful-delegation-proof/command.yaml"
          && grep -q '05a59d83-924a-4e95-a3d2-3dd5659d15db' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12729/dod-live-useful-delegation-proof/command.yaml"
          && grep -q 'quality_gate_passed=true' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12729/dod-live-useful-delegation-proof/command.yaml"
          && grep -q '@pytest.mark.unit' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12729/dod-live-useful-delegation-proof/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Runtime impact is bounded to post-merge dev-lane deploy and repeated delegation validation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12729/dod-deploy-assessment/command.yaml"
          && grep -q 'post-merge dev-lane deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12729/dod-deploy-assessment/command.yaml"
