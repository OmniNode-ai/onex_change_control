---
schema_version: "1.0.0"
ticket_id: OMN-13735
title: "feat(OMN-13735): FSM YAML-vs-handler drift guard — validator + pre-commit hook + CI gate"
summary: >
  Add a strongly-typed validator (omnibase_core.validators.fsm_handler_drift) that reconciles every reducer
  contract.yaml's state_machine.transitions — filtered by a declared, typed fsm_handler_binding.state_filter
  — against the handler module's exported VALID_TRANSITIONS and GUARD_CONDITIONS Python symbols via AST
  literal evaluation. Fails on any (from_state, trigger)->to_state or guard field/value divergence; zero
  allowlist entries. Wired as a pre-commit hook and a required CI status check on omnibase_core (local)
  and omnimarket (CI gate + binding section in node_intelligence_reducer/contract.yaml). Pre-condition
  verified: zero existing YAML-vs-handler drift at time of landing.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PASS receipts bind the core validator proof, omnimarket integration proof, full source
      scan, no-runtime-mutation deploy assessment, and this OCC PR self-binding."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13735/dod-core-validator-proof/command.yaml
      && grep -q '^status: PASS$' drift/dod_receipts/OMN-13735/dod-market-integration-proof/command.yaml
      && grep -q '^status: PASS$' drift/dod_receipts/OMN-13735/dod-market-source-scan/command.yaml &&
      grep -q '^status: PASS$' drift/dod_receipts/OMN-13735/dod-deploy-assessment/command.yaml && grep
      -q '^status: PASS$' drift/dod_receipts/OMN-13735/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-validator-proof"
    description: "omnibase_core PR carries the fsm_handler_drift validator + unit tests + local pre-commit
      hook + required CI gate; injected-divergence test fires (exit 1), aligned fixture passes (exit 0),
      self-scan of src/ exits 0."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13735/dod-core-validator-proof/command.yaml"
  - id: "dod-market-integration-proof"
    description: "omnimarket PR carries the fsm_handler_binding section on node_intelligence_reducer,
      the fsm-handler-drift CI gate, and the integration test; validator scan of src/ finds zero drift."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13735/dod-market-integration-proof/command.yaml"
  - id: "dod-market-source-scan"
    description: "Direct validator scan of the omnimarket src/ tree exits 0 with the OMN-13735 binding
      in place, proving no current FSM YAML-vs-handler drift in node_intelligence_reducer."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13735/dod-market-source-scan/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Deploy assessment: this is a validator, workflow, test, and contract-binding change
      only. No live deploy, restart, .201 mutation, or runtime lane mutation was performed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13735/dod-deploy-assessment/command.yaml
          && grep -q 'deploy gate' drift/dod_receipts/OMN-13735/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding OCC receipt: this onex_change_control PR carries the OMN-13735 contract
      and receipt files that the central gate can verify inside the OCC checkout."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13735/dod-occ-self/command.yaml"
