---
schema_version: "1.0.0"
ticket_id: "OMN-13796"
title: "fix(OMN-13796): disposition node_release facade registry honesty and I/O guard"
summary: >-
  omnimarket PR #1543 corrects node_release contract and metadata disposition:
  HandlerRelease is an orchestrator-style FSM facade, not an impure compute
  node. The PR adds registry-honesty tests that mechanically assert the
  contract/metadata disposition and guard handler_release.py against introducing
  forbidden direct I/O without an explicit registry change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-registry-honesty"
    description: >-
      node_release contract.yaml and metadata.yaml declare orchestrator/
      orchestration disposition and full_runtime false for the facade.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13796/dod-registry-honesty/command.yaml"
  - id: "dod-io-guard"
    description: >-
      Registry-honesty tests statically guard handler_release.py against direct
      git, shell, HTTP, and filesystem process I/O being added silently.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13796/dod-io-guard/command.yaml"
  - id: "dod-pr-1543-binding"
    description: >-
      OmniNode-ai/omnimarket PR #1543 carries the OMN-13796 fix at feature
      commit 0f0ad9c75f0a3e23daec124c16234bc1f1ba8749.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13796/dod-pr-1543-binding/command.yaml"
