---
schema_version: "1.0.0"
ticket_id: "OMN-14289"
title: "refactor(OMN-14289): retire validation->nodes import-layering back-edge in omnibase_core"
summary: >-
  omnibase_core product PR #1417 (branch jonah/omn-14289-validation-nodes-backedge, head 94fd98e7e31ad835e7b36bc96124fdb343851adc)
  retires the validation->nodes import-layering back-edge under epic OMN-3210. The routing-authority check
  logic is extracted from nodes/node_routing_authority_check_compute/handler.py into a new validation/checker_routing_authority.py;
  validator_routing_authority.py and the node handler now depend on that checker instead of the validation
  module importing up into nodes. The OMN-3210 burn-down ignore_imports exception for "omnibase_core.validation.**
  -> omnibase_core.nodes.**" is removed from .importlinter (a ratchet, not a weakening). Independently
  verified by a distinct agent (verifier != implementer); public-safe library-only import-layering change.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      Independent import-linter ratchet verification at product head 94fd98e7e31ad835e7b36bc96124fdb343851adc,
      run by an agent distinct from the implementer (verifier != runner, satisfying the receipt-honesty
      gate). In a clean checkout at the PR head, lint-imports reports the "Services and validation must
      not import nodes" contract KEPT with the ignore line removed (4 contracts kept, 0 broken, exit 0);
      the ignore removal is a pure deletion with no compensating ignore added; the extracted checker and
      the validator no longer import from omnibase_core.nodes; and the 22 routing-authority tests pass
      (behavior-preserving).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14289/dod-001/command.yaml && grep -q '94fd98e7e31ad835e7b36bc96124fdb343851adc'
          drift/dod_receipts/OMN-14289/dod-001/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Honest deploy-surface assessment: the product PR moves routing-authority check logic between library
      modules and removes an import-linter ignore line. No runtime behavior change, no live deploy required
      or performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14289/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-14289/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14289 contract and
      PASS receipts consumed by the product PR through its Evidence-Source line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14289/dod-occ-self/command.yaml
