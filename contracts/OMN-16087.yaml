---
schema_version: "1.0.0"
ticket_id: "OMN-16087"
title: "OCC evidence for OMN-16087"
summary: >
  Manual OCC companion for OmniNode-ai/omnimarket PR #2091 (node_dod_verify pr-live-state binder honors
  intentional non-merged PR-state assertions -- OPEN and CLOSED/REPLACED shapes). The live occ_companion_emitter
  consumer's "OCC Companion Author" job runs in dry_run mode by default (OMNI_OCC_AUTOAUTHOR_MODE), so
  no live companion PR was authored within a reasonable window after the product PR opened -- hand-authored
  evidence is required per the same OMN-15247 fallback policy used for prior hand-authored companions
  (e.g. occ#6633).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2091"
    description: "PR #2091 on OmniNode-ai/omnimarket — Evidence-Source (hand-authored: the occ_companion_emitter
      live consumer's OCC Companion Author job runs in dry_run mode by default and did not produce a companion
      within a reasonable window after the product PR opened, so hand-authored evidence is required per
      OMN-15247)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2091 --repo OmniNode-ai/omnimarket --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2091-ci"
    description: "PR #2091 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425): confirms
      the PR's exact head ca7ce1ac08059f3e92322c0bc875f633f633dffd carries the three-file scope fixing
      the pr-live-state binder (evidence_collector.py + a new regression test) plus the separately-committed,
      unrelated drive-by fix for a stale pre-existing test assertion in node_delegation_routing_reducer."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2091 --repo OmniNode-ai/omnimarket --json headRefOid,files --jq '(.headRefOid ==
          "ca7ce1ac08059f3e92322c0bc875f633f633dffd") and ([.files[].path] | sort == ["src/omnimarket/nodes/node_dod_verify/services/evidence_collector.py",
          "tests/nodes/node_delegation_routing_reducer/test_contract_routing_policy.py", "tests/unit/nodes/node_dod_verify/test_omn_16087_pr_live_state_intentional_state_assertion.py"])'
          | grep -qx true
  - id: "occ-self-bind-pr-6639"
    description: "OCC companion PR #6639 — self-bind for OMN-16087: every other entry in this contract
      binds to omnimarket#2091, not this OCC PR, so occ-preflight's merge-eligibility validator (validator_occ_merge_eligibility._receipt_bound_to_pr)
      needs this entry to find a PASS receipt bound to the OCC PR carrying it (established pattern: occ-self-bind-pr-6633)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6639 --repo OmniNode-ai/onex_change_control --json number,state"
  # ---------------------------------------------------------------------------
  # APPEND-ONLY FOLLOW-UP (R2): product PR head moved after a post-review
  # correction. Does NOT edit either entry above -- dod-OmniNode-ai-omnimarket-pr-2091-ci
  # keeps its bytes and hash unchanged.
  #
  # After occ#6639 merged, PR #2091's own assertion at head ca7ce1ac was found
  # to be WRONG (it papered over a local BIFROST_OVERLAY_PATH test-isolation
  # leak by asserting a completed endpoint_url the production code never
  # actually constructs -- every real resolution path carries endpoint_url
  # verbatim). Corrected in a follow-up commit: reverted the assertion to the
  # original bare-URL expectation and closed the isolation gap by explicitly
  # clearing BIFROST_OVERLAY_PATH for the test, the same way the test already
  # does for the other three delegation env vars. This entry re-pins the SAME
  # falsifiable product-diff-scope probe at the new head.
  # ---------------------------------------------------------------------------
  - id: "dod-omnimarket-pr-2091-ci-rebind-27ce3561"
    description: "PR #2091 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425), rebound to
      the post-correction head 27ce356171b307684befb9469498c251e8a5a710. Same three-file scope as the
      original entry (evidence_collector.py + the new regression test + the delegation-routing test file,
      now corrected to fix the ACTUAL bug -- a BIFROST_OVERLAY_PATH test-isolation leak -- instead of
      the wrong prior fix that masked it)."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-2091-ci"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2091 --repo OmniNode-ai/omnimarket --json headRefOid,files --jq '(.headRefOid ==
          "27ce356171b307684befb9469498c251e8a5a710") and ([.files[].path] | sort == ["src/omnimarket/nodes/node_dod_verify/services/evidence_collector.py",
          "tests/nodes/node_delegation_routing_reducer/test_contract_routing_policy.py", "tests/unit/nodes/node_dod_verify/test_omn_16087_pr_live_state_intentional_state_assertion.py"])'
          | grep -qx true
  - id: "occ-self-bind-pr-6692"
    description: "OCC companion PR #6692 — self-bind for OMN-16087: every other entry in this contract
      binds to omnimarket#2091, not this OCC PR, so occ-preflight's merge-eligibility validator (validator_occ_merge_eligibility._receipt_bound_to_pr)
      needs this entry to find a PASS receipt bound to the OCC PR carrying it (established pattern: occ-self-bind-pr-6639)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6692 --repo OmniNode-ai/onex_change_control --json number,state"
  # ---------------------------------------------------------------------------
  # APPEND-ONLY FOLLOW-UP (R3): PR #2091 was DIRTY/CONFLICTING against dev
  # (an independent lane, OMN-16149, landed the identical BIFROST_OVERLAY_PATH
  # fix on dev first -- same root cause, same fix shape, different ticket).
  # Resolved by merging dev into the PR branch; the conflicting hunk's CODE
  # was byte-identical on both sides (only the explanatory comment differed),
  # so the merge resolution keeps dev's OMN-16149 comment and the PR's diff
  # against dev for that file collapses to empty. Consequently PR #2091 now
  # shows only TWO changed files (test_contract_routing_policy.py dropped out
  # of the diff entirely, since it now matches dev byte-for-byte) -- not a
  # narrower ticket scope, just git correctly reporting zero delta against
  # the new merge base. Does NOT touch either 2091-ci entry above.
  # ---------------------------------------------------------------------------
  - id: "dod-mkt-pr-2091-ci-rebind-91582a18"
    description: "PR #2091 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425), rebound to
      the post-merge-conflict-resolution head 91582a18ee560618db2eca21fe09a49dbb80ba96. Two-file scope
      now (evidence_collector.py + the new regression test) -- the delegation-routing test fix landed
      on dev independently via OMN-16149 and the PR's diff against it is now empty."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-omnimarket-pr-2091-ci-rebind-27ce3561"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2091 --repo OmniNode-ai/omnimarket --json headRefOid,files --jq '(.headRefOid ==
          "91582a18ee560618db2eca21fe09a49dbb80ba96") and ([.files[].path] | sort == ["src/omnimarket/nodes/node_dod_verify/services/evidence_collector.py",
          "tests/unit/nodes/node_dod_verify/test_omn_16087_pr_live_state_intentional_state_assertion.py"])'
          | grep -qx true
  # ---------------------------------------------------------------------------
  # APPEND-ONLY FOLLOW-UP (R4): two independent, unrelated content-level CI
  # gate failures on PR #2091 needed fixing after R3 -- neither changes the
  # ticket's scope, both rebind/extend evidence for the same two-file diff.
  #
  # (a) `gate / CodeRabbit Thread Check` (comment 3807974259): the new
  #     _pr_binding_asserted_state (this ticket's own binder fix) recognized
  #     only the `gh pr view <N> --repo <repo>` binding shape, not the
  #     `repos/<owner>/<repo>/pulls/<N>` URL form that its sibling
  #     _hardcoded_pr_bindings_in_value already understands (OMN-15465) --
  #     a URL-bound intentional OPEN/CLOSED assertion would resolve its
  #     binding but fall through to the default merged/green derivation,
  #     the exact inversion this ticket exists to fix, for a different
  #     binding-expression shape. Fixed + regression-tested (commit
  #     57ad3c72522cbd22194b797f5142e8c0be2a8573), thread resolved.
  #
  # (b) `deploy-gate / deploy-gate`: this contract declared dod_evidence
  #     items that satisfy occ-preflight's PR-binding/self-bind machinery
  #     but NONE of them are a FALSIFIABLE deploy probe under OMN-14505's
  #     classify_check_value() -- every check_value here is `gh pr view ...`
  #     (metadata read, not a live-surface probe in command position) or a
  #     receipt/contract self-reference. OMN-16087 is not in the OMN-14443
  #     grandfather snapshot (authored well after it was frozen), so the
  #     ratchet holds it to the real bar unconditionally. This ticket's
  #     change is compute-only (a pure-function binder inside node_dod_verify,
  #     no deployed runtime service) -- per DEPLOY_EVIDENCE_GUIDANCE's
  #     hermetic form for compute-only/no-live-deploy tickets, the new entry
  #     below reads the actual repo CONTENT at the merge SHA via `gh api`
  #     (LIVE_PROBE_COMMANDS: command position "gh api") and asserts a
  #     symbol this fix introduces (`_pr_binding_asserted_state` does not
  #     exist anywhere on omnimarket dev pre-fix) -- falsifiable: run against
  #     un-deployed/unfixed code and the grep goes RED. Uses the established
  #     sigpipe-safe buffered shape (OMN-15170/OMN-15430, per this repo's own
  #     Contract Corpus Ratchets Rule E): the producer is captured into a
  #     shell variable before the early-exit `grep -qF` consumer runs, so a
  #     first-match exit can never SIGPIPE the `gh api | base64 -d` producer.
  # ---------------------------------------------------------------------------
  - id: "dod-deploy-omn-16087-content-probe"
    description: "Falsifiable deploy/compute-only evidence (OMN-14505/OMN-14443) for PR #2091's runtime-
      flagged path src/omnimarket/nodes/node_dod_verify/services/evidence_collector.py -- reads the file's
      actual content on OmniNode-ai/omnimarket at the final head 57ad3c72522cbd22194b797f5142e8c0be2a8573
      via the GitHub content API and asserts the presence of _pr_binding_asserted_state, the function
      this ticket introduces. This symbol does not exist anywhere in the function pre-fix (unpatched omnimarket
      dev), so the check is falsifiable: it fails against un-deployed/unfixed code and can only pass against
      the actual fixed content at the pinned ref."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_dod_verify/services/evidence_collector.py?ref=57ad3c72522cbd22194b797f5142e8c0be2a8573"
          --jq .content | base64 -d)"; printf '%s' "$body" | grep -qF '_pr_binding_asserted_state' &&
          echo "OMN-16087-DEPLOY-PROBE-PASS: _pr_binding_asserted_state is present in evidence_collector.py
          at 57ad3c72522cbd22194b797f5142e8c0be2a8573"
  # ---------------------------------------------------------------------------
  # APPEND-ONLY FOLLOW-UP (R5): product PR head moved again (CodeRabbit fix
  # commit, same R4 note (a) above) after R3's rebind. Does NOT touch R3 or
  # R4's bytes -- this entry only re-pins the same falsifiable product-diff-
  # scope probe (OMN-14425) at the new head.
  # ---------------------------------------------------------------------------
  - id: "dod-mkt-pr-2091-ci-rebind-57ad3c72"
    description: "PR #2091 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425), rebound to
      the post-CodeRabbit-fix head 57ad3c72522cbd22194b797f5142e8c0be2a8573. Same two-file scope as dod-mkt-pr-2091-ci-rebind-91582a18
      (evidence_collector.py + the regression test, now extended with TestUrlFormBindingAssertionIsNotAutoBoundToMergedGreen
      for the URL-form binding fix)."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-mkt-pr-2091-ci-rebind-91582a18"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2091 --repo OmniNode-ai/omnimarket --json headRefOid,files --jq '(.headRefOid ==
          "57ad3c72522cbd22194b797f5142e8c0be2a8573") and ([.files[].path] | sort == ["src/omnimarket/nodes/node_dod_verify/services/evidence_collector.py",
          "tests/unit/nodes/node_dod_verify/test_omn_16087_pr_live_state_intentional_state_assertion.py"])'
          | grep -qx true
