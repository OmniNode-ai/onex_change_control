---
schema_version: "1.0.0"
ticket_id: "OMN-10320"
title: "Autobind OCC evidence for OMN-10320"
summary: >
  OCC contract bound by Codex merge-controller Evidence-Source autobind for OmniNode-ai/omninode_infra
  PR #604 after the product PR lacked central OCC evidence and failed Receipt Gate/OCC preflight at Resolve
  Evidence-Source.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #604 product diff scope present"
    command: "gh pr diff 604 --repo OmniNode-ai/omninode_infra --name-only | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-604"
    description: "PR #604 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "RECEIPT=drift/dod_receipts/OMN-10320/dod-OmniNode-ai-omninode_infra-pr-604/command.yaml;
          grep -q 'pr_number: 604' ${RECEIPT} && grep -q 'commit_sha: \"1ca9eb1a7fec437ce23e6b92ad0ac15e486b3986\"'
          ${RECEIPT}"
  - id: "dod-OmniNode-ai-omninode_infra-pr-604-ci"
    description: "PR #604 on OmniNode-ai/omninode_infra — product diff scope (OMN-14425 substance check;
      OMN-14650: not gated on source-PR CI being green)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "RECEIPT=drift/dod_receipts/OMN-10320/dod-OmniNode-ai-omninode_infra-pr-604-ci/command.yaml;
          grep -q 'docker/onex-api/routers/provision.py' ${RECEIPT} && grep -q 'docker/onex-api/tests/test_provision.py'
          ${RECEIPT}"
  - id: "occ-self-bind-pr-4318"
    description: "OCC companion PR #4318 on OmniNode-ai/onex_change_control — self-binding proof for OMN-10320
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4318; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state,headRefOid"
