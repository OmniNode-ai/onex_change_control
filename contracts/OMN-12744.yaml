---
schema_version: "1.0.0"
ticket_id: "OMN-12744"
title: "chore(demo): judge access to .201 demo — Tailscale machine-share scoped to demo ports + app auth
  + revocation runbook"
summary: >-
  Central evidence that judge access to the .201 demo is configured per the judge-access plan (docs/plans/2026-06-05-sea-hackathon-judge-access-plan.md).
  Tailscale machine-share scoped to demo ports only. App-layer auth in front of demo surfaces. Non-admin
  validation: external identity reaches demo through share and is correctly gated. Revocation runbook
  documented. Funnel fallback path documented. Hard gate: exposing .201 to external judges requires explicit
  user approval (Operating Rule #2).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tailscale-share-config"
    description: "Tailscale machine-share configured scoped to demo ports only (not full-host exposure).
      App-layer auth in front of demo surfaces. Configuration evidence (tailscale share output / app auth
      config)."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12744/dod-tailscale-share-config/command.yaml"
  - id: "dod-non-admin-reachability-test"
    description: "Non-admin / external identity reaches demo through share and is correctly gated by app
      auth. Test result output showing successful reach + correct gate behavior."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12744/dod-non-admin-reachability-test/command.yaml"
  - id: "dod-revocation-runbook"
    description: "Written revocation runbook (how to pull access immediately after demo) + Tailscale Funnel
      fallback steps documented. Doc path or inline content as evidence."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12744/dod-revocation-runbook/command.yaml"
  - id: "dod-user-approval-receipt"
    description: "Explicit user approval receipt for exposing .201 to external judges (Operating Rule
      #2 hard gate)."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12744/dod-user-approval-receipt/command.yaml"
  - id: "dod-occ-pr-evidence"
    description: "OCC PR carries the OMN-12744 central contract and PASS receipts."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12744/dod-occ-pr-evidence/command.yaml"
