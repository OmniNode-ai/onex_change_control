---
schema_version: "1.0.0"
ticket_id: "OMN-13570"
title: "Sanitize omnimarket documentation — local-env traces + OMN-XXXX refs + renames"
summary: >-
  Documentation-only sanitization of the public omnimarket repository. Removes private LAN IP addresses,
  host shorthands, personal filesystem paths, and personal email references from all in-scope documentation
  files. Rewrites all OMN-XXXX ticket references so text reads naturally without ticket identifiers. Renames
  7 documentation files and 7 evidence directories whose names contained OMN-XXXX identifiers. No source
  code, contracts, or CI yaml files are modified. Part of epic OMN-13567 (omnimarket public readiness
  documentation cleanup).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "docs"
    description: >-
      All in-scope documentation files (*.md, *.txt, *.adoc, *.rst, *.mdx, README*) are free of private
      LAN IP addresses, host shorthands (.201/.200), personal filesystem paths, and OMN-XXXX ticket references.
      Verified via grep-clean proof in the PR.
    command: >-
      git grep -nIE '192\.168\.|[^0-9]\.20[01]\b|/Users/[a-z]|jonah@' -- '*.md' '*.txt' '*.adoc' '*.rst'
      | grep -v '^contracts/' | wc -l
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      grep-clean proof: no private LAN IPs, no .201/.200 host shorthands, no personal filesystem paths
      in documentation files (outside contracts/). Verified locally before PR push.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13570\"$' contracts/OMN-13570.yaml"
