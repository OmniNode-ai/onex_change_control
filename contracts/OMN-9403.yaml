---
schema_version: "1.0.0"
ticket_id: "OMN-9403"
title: "Fixer dispatch envelope compatibility for PR lifecycle sweep"
summary: >
  Bind omnimarket PR #776 evidence for wrapping pr_lifecycle_orchestrator fixer-dispatch-start messages
  in canonical ModelEventEnvelope so runtime auto-wiring consumers can validate and process fixer dispatch
  commands.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-local-verification"
    description: "Focused local verification for fixer dispatch envelope publishing and PR lifecycle golden
      chain"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-local-verification/command.yaml\""
  - id: "dod-omnimarket-pr-776"
    description: "omnimarket PR #776 publishes fixer dispatch commands as ModelEventEnvelope payloads"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-omnimarket-pr-776/command.yaml\"
          && grep -q '^pr_number: 776$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-omnimarket-pr-776/command.yaml\"
          && grep -q '^commit_sha: \"520e8cce7d9428fdfc5648e4f220a20d8a419b2f\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-omnimarket-pr-776/command.yaml\""
  - id: "dod-omnimarket-pr-781"
    description: "omnimarket PR #781 aligns fixer dispatch envelope event_type with the runtime dispatcher
      alias"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-omnimarket-pr-781/command.yaml\"
          && grep -q '^pr_number: 781$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-omnimarket-pr-781/command.yaml\"
          && grep -q '^commit_sha: \"2b78e0bd42a5bbe048317904ca33a907a4c25df3\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-omnimarket-pr-781/command.yaml\""
  - id: "dod-occ-pr-1426"
    description: "OCC PR #1426 binds this contract to onex_change_control as the evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-occ-pr-1426/command.yaml\"
          && grep -q '^pr_number: 1426$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-occ-pr-1426/command.yaml\""
  - id: "dod-deploy-smoke-plan"
    description: "Post-merge deploy smoke verifies the runtime imports the envelope-publishing PR lifecycle
      handler"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-deploy-smoke-plan/command.yaml\"
          && grep -q 'deploy smoke' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-deploy-smoke-plan/command.yaml\""
  - id: "dod-occ-pr-1428"
    description: "OCC PR #1428 binds the deploy evidence update to onex_change_control"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-occ-pr-1428/command.yaml\"
          && grep -q '^pr_number: 1428$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-occ-pr-1428/command.yaml\""
  - id: "dod-occ-pr-1433"
    description: "OCC PR #1433 binds the PR #781 evidence receipt to onex_change_control"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-occ-pr-1433/command.yaml\"
          && grep -q '^pr_number: 1433$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9403/dod-occ-pr-1433/command.yaml\""
