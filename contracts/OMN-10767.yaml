---
schema_version: "1.0.0"
ticket_id: "OMN-10767"
title: "Interactive Onboarding Executor"
summary: >
  Binds omnibase_infra PRs #1546-#1559 which implement the full interactive onboarding executor: policy
  Pydantic models, condition evaluator, transition reducer, env writer, interactive executor, handler
  integration, and proof-of-life E2E tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "All 8 child PRs merged to omnibase_infra main with CI green."
    command: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnibase_infra --json state,mergeCommit"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1559"
    description: "omnibase_infra PR #1559 (proof-of-life E2E) merged to main — final delivery."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnibase_infra --json state --jq .state
          | grep -q MERGED"
  - id: "dod-infra-pr-1556"
    description: "omnibase_infra PR #1556 (handler integration) merged to main."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnibase_infra --json state --jq .state
          | grep -q MERGED"
  - id: "dod-infra-pr-1555"
    description: "omnibase_infra PR #1555 (interactive executor) merged to main."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnibase_infra --json state --jq .state
          | grep -q MERGED"
  - id: "dod-tests-pass"
    description: "277 unit + integration tests pass on omnibase_infra main (237 unit + 40 onboarding integration)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "cd /Users/jonah/Code/omni_home/omnibase_infra && uv run pytest tests/unit/onboarding/
          tests/integration/onboarding/ -q --tb=no 2>&1 | grep -q 'passed'"
