---
schema_version: "1.0.0"
ticket_id: "OMN-17862"
title: "Autobind OCC evidence for OMN-17862"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3302
  and OmniNode-ai/omnimarket PR #2391. Both are OMN-17862 (repair item R3), landed as a pair: #3302 makes
  the projection quarantine arm durable before the offset advances, and #2391 adds the parse-time refusal
  that depends on it. Two autobind companions were minted for the one ticket (#8621, #8622); this is their
  UNION -- neither set of evidence is dropped.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3302 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3302 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: |-
      uv run pytest tests/unit/runtime/auto_wiring/test_handler_wiring_db_injection.py tests/unit/runtime/auto_wiring/test_omn17379_projection_silent_ack.py tests/unit/runtime/auto_wiring/test_omn17862_quarantine_publish_confirmed.py tests/unit/runtime/health/test_omn16994_projection_liveness.py -q
  - kind: "ci"
    description: "PR #2391 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2391 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof-pr-2391`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: |-
      uv run pytest src/omnimarket/nodes/node_projection_session_replay/tests/test_handler_projection_session_replay.py src/omnimarket/nodes/node_projection_session_replay/tests/test_omn17888_indexed_reads.py tests/test_golden_chain_projection_session_replay.py tests/test_omn17183_real_postgres_session_replay_write_path.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3302"
    description: "PR #3302 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/errors/error_projection.py?ref=260ceb5b8b53b9e7804ae1a1384006f41a13902d --jq '.content' | base64 -d | grep -c 'class QuarantinePublishUnconfirmedError'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3302/files --paginate --jq '.[].filename' | grep -cFx 'pyproject.toml'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #3302 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3302/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3302 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/runtime/auto_wiring/test_handler_wiring_db_injection.py tests/unit/runtime/auto_wiring/test_omn17379_projection_silent_ack.py tests/unit/runtime/auto_wiring/test_omn17862_quarantine_publish_confirmed.py tests/unit/runtime/health/test_omn16994_projection_liveness.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-8621"
    description: "Binds OMN-17862 to OCC companion PR #8621 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8621 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2391"
    description: "PR #2391 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_session_replay/handlers/handler_projection_session_replay.py?ref=0888cf53777b5c2ae42d849ce8967914516d1bd7 --jq '.content' | base64 -d | grep -c 'def _source_event_material'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2391/files --paginate --jq '.[].filename' | grep -cFx 'src/omnimarket/nodes/node_projection_session_replay/handlers/handler_projection_session_replay.py'
  - id: "dod-deploy-assessment-pr-2391"
    description: "Deploy-scope DoD so PR #2391 clears the deploy-gate (F-05). Suffixed because the sibling
      companion for PR #3302 already owns `dod-deploy-assessment` on this ticket; a shared id would collapse
      two products' evidence onto one receipt."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2391/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "dod-occ-diff-derived-behavior-proof-pr-2391"
    description: "PR #2391 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434). Suffixed
      for the same reason as `dod-deploy-assessment-pr-2391`."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest src/omnimarket/nodes/node_projection_session_replay/tests/test_handler_projection_session_replay.py src/omnimarket/nodes/node_projection_session_replay/tests/test_omn17888_indexed_reads.py tests/test_golden_chain_projection_session_replay.py tests/test_omn17183_real_postgres_session_replay_write_path.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-8622"
    description: "Binds OMN-17862 to OCC companion PR #8622 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8622 --repo OmniNode-ai/onex_change_control --json number,state"
