---
schema_version: "1.0.0"
ticket_id: OMN-12779
title: "Wave 1B: generation model/provider/endpoint_ref from contract, not env"
summary: >
  All four routing authorities (provider, served_model_id, endpoint_ref, routing_source) for node_generation_consumer
  are now declared in contract.yaml model_routing, not resolved via env-var indirection or handler literals.
  Handler reads all four at construction time and fails fast if any is absent. Transitional LLM_CODER_MODEL_NAME
  env var removed. Part of the bus-native SEA demo Wave 1 (OMN-12779).
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "42 unit tests for node_generation_consumer all pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12779/dod-unit-tests/command.yaml"
  - kind: "ci"
    description: "Ruff + mypy --strict pass on node_generation_consumer"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12779/dod-lint-typecheck/command.yaml"
  - kind: "ci"
    description: "pre-commit run --all-files passes in omnimarket worktree"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12779/dod-precommit/command.yaml"
  - kind: "manual"
    description: "omnimarket PR #1096 carry OMN-12779 contract changes"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12779/dod-omnimarket-pr/command.yaml"
  - kind: "manual"
    description: "Wave 3 redeploy will exercise contract-driven routing (gated milestone)"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12779/dod-deploy-wave3/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "42 unit tests for node_generation_consumer pass (all four routing authorities asserted)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12779/dod-unit-tests/command.yaml
          && grep -q '^ticket_id: OMN-12779$' drift/dod_receipts/OMN-12779/dod-unit-tests/command.yaml
          && grep -q '^evidence_item_id: dod-unit-tests$' drift/dod_receipts/OMN-12779/dod-unit-tests/command.yaml"
  - id: "dod-lint-typecheck"
    description: "Ruff + mypy --strict pass on node_generation_consumer"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12779/dod-lint-typecheck/command.yaml
          && grep -q '^ticket_id: OMN-12779$' drift/dod_receipts/OMN-12779/dod-lint-typecheck/command.yaml
          && grep -q '^evidence_item_id: dod-lint-typecheck$' drift/dod_receipts/OMN-12779/dod-lint-typecheck/command.yaml"
  - id: "dod-precommit"
    description: "pre-commit run --all-files passes in omnimarket worktree"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12779/dod-precommit/command.yaml
          && grep -q '^ticket_id: OMN-12779$' drift/dod_receipts/OMN-12779/dod-precommit/command.yaml
          && grep -q '^evidence_item_id: dod-precommit$' drift/dod_receipts/OMN-12779/dod-precommit/command.yaml"
  - id: "dod-omnimarket-pr"
    description: "omnimarket PR #1096 carries the Wave 1B contract+handler changes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12779/dod-omnimarket-pr/command.yaml
          && grep -q '^pr_number: 1096$' drift/dod_receipts/OMN-12779/dod-omnimarket-pr/command.yaml"
  - id: "dod-deploy-wave3"
    description: "Wave 3 redeploy exercises contract-driven routing (gated milestone, deferred by plan
      §2)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy: Wave 3 gated milestone; node_generation_consumer will be redeployed
          with contract-driven routing after Wave 1C and 2A land. deploy receipt at dod-deploy-wave3 promoted
          to PASS at that time.'"
