---
schema_version: "1.0.0"
ticket_id: "OMN-19531"
title: "Raise omnimarket's omnibase_core floor to 0.47.23 so its CI imports the omnibase_infra dev runtime
  scope model"
summary: >
  Hand-authored OCC companion for OmniNode-ai/omnimarket PR #2895, authored on the OMN-15247 path after
  the occ-autobind producer declined terminally for head 792f1876462348bc068eaa3722d6ad7c55d38036 (skip
  NO_RED_DERIVABLE_CHECK, occ-preflight eligibility job 107957563281). The producer's pin-only classifier
  does not recognise tool.uv.override-dependencies as a dependency-pin surface (OMN-19027), so a diff
  of pyproject.toml plus uv.lock alone is read as owing evidence. Both checks below are content-bound
  and derive RED against the merge base 3bc087be77c384e258876001f18e5183452c39d3.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2895 CI checks green"
    command: "gh pr checks 2895 --repo OmniNode-ai/omnimarket"
  - kind: "manual"
    description: |-
      Content-bound proof that the declared floor and the resolved lock both
      name omnibase-core 0.47.23 at the PR head and neither does at the merge
      base. Read from the GitHub contents API at an explicit ref, so the probe
      observes the pull request rather than the machine running it.
    command: "gh api repos/OmniNode-ai/omnimarket/contents/pyproject.toml?ref=792f1876462348bc068eaa3722d6ad7c55d38036"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2895"
    description: >-
      PR #2895 on OmniNode-ai/omnimarket — hand-authored evidence (OMN-15247) that the omnibase-core floor
      names 0.47.23 in both dependency tables at the head and in neither at the merge base.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnimarket/contents/pyproject.toml?ref=792f1876462348bc068eaa3722d6ad7c55d38036" --jq '.content' | base64 -d | grep -cF 'omnibase-core>=0.47.23,<0.48.0'
  - id: "dod-omnimarket-2895-lock-resolves-core-0-47-23"
    description: >-
      PR #2895 on OmniNode-ai/omnimarket — hand-authored evidence that uv.lock resolves omnibase-core
      to 0.47.23 at the head, the half CI actually installs and the half the declared range alone never
      proves.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          gh api -H 'Accept: application/vnd.github.raw' "repos/OmniNode-ai/omnimarket/contents/uv.lock?ref=792f1876462348bc068eaa3722d6ad7c55d38036" | grep -A1 '^name = "omnibase-core"$' | grep -cF 'version = "0.47.23"'
  - id: "dod-OmniNode-ai-omnimarket-pr-2896"
    description: >-
      PR #2896 on OmniNode-ai/omnimarket, which replaces #2895 on a branch naming OMN-19531 — hand-authored
      evidence (OMN-15247) that the omnibase-core floor names 0.47.23 in both dependency tables at the
      head and in neither at the merge base.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnimarket/contents/pyproject.toml?ref=72aa062bae13237a0e2cad8aaeb77fea8a3fc7e5" --jq '.content' | base64 -d | grep -cF 'omnibase-core>=0.47.23,<0.48.0'
  - id: "dod-omnimarket-2896-lock-resolves-core-0-47-23"
    description: >-
      PR #2896 on OmniNode-ai/omnimarket — hand-authored evidence that uv.lock resolves omnibase-core
      to 0.47.23 at the head, the half CI actually installs.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          gh api -H 'Accept: application/vnd.github.raw' "repos/OmniNode-ai/omnimarket/contents/uv.lock?ref=72aa062bae13237a0e2cad8aaeb77fea8a3fc7e5" | grep -A1 '^name = "omnibase-core"$' | grep -cF 'version = "0.47.23"'
