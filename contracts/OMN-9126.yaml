---
schema_version: "1.0.0"
ticket_id: "OMN-9126"
title: "Runtime Startup is a First-Class CI Gate"
summary: "Binds omnibase_infra PR #1589, which pins omnibase-core to main branch (0.40.2) in [tool.uv.sources]
  so the runtime resolves the multi-param handler injection extension. Core 0.40.1 (PyPI) only supported
  single-param handler injection (event_bus); core main extends Step 4 to multi-param injection covering
  event_bus, container, and ownership_query. This fixes the TypeError on 10+ handlers declaring container:
  ModelONEXContainer in __init__ that caused three consecutive runtime boot failures on .201."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Downstream PR #1589 pins omnibase-core to the main branch in pyproject.toml so the multi-param
      resolver injection lands in the runtime image. Verified by CI passing on the PR plus runtime boot
      probe with RestartCount == 0 after merge."
    command: "gh pr view 1589 --repo OmniNode-ai/omnibase_infra --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1589"
    description: "omnibase_infra PR #1589 carries the OMN-9126 core source pin enabling multi-param handler
      injection at runtime boot."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9126/dod-omnibase-infra-pr-1589/command.yaml
          && grep -q '^pr_number: 1589$' drift/dod_receipts/OMN-9126/dod-omnibase-infra-pr-1589/command.yaml"
  - id: "dod-occ-pr-1001"
    description: "onex_change_control PR #1001 carries the OMN-9126 contract and downstream receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9126/dod-occ-pr-1001/command.yaml
          && grep -q '^pr_number: 1001$' drift/dod_receipts/OMN-9126/dod-occ-pr-1001/command.yaml"
