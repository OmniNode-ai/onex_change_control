---
schema_version: "1.0.0"
ticket_id: "OMN-15349"
title: "Autobind OCC evidence for OMN-15349"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2662.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2662 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2662"
    description: "PR #2662 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2662 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2662 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2662"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6120"
    description: "Binds OMN-15349 to OCC companion PR #6120 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6120 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-817"
    description: "PR #817 on OmniNode-ai/omninode_infra - Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 817 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 817 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator-pr-817"
    description: "Hosted OCC evidence admissibility validator for PR #817 (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-817"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-OmniNode-ai-omninode_infra-pr-818"
    description: >-
      Consumer binding for OmniNode-ai/omninode_infra#818, the follow-up to #817 that rebuilds and re-pins
      k8s/migrations/omnibase-infra-migrate.yaml to omnibase_infra dev@a853500a (sha256:64f519fe...),
      the first migrate image built from a commit containing docker/migrations/forward/fenced-node-migrations.yaml
      (added by omnibase_infra#2662, this same ticket's other consumer). This binds the exact evidence-bound
      head so a later push cannot silently satisfy this entry.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 818 --repo OmniNode-ai/omninode_infra --json number,state,baseRefName,headRefOid,files --jq 'if (.number == 818 and .state == "OPEN" and .baseRefName == "dev" and .headRefOid == "cb0208badf7a81f4ca97a2995d88b90f06440900" and ([.files[].path] | sort) == ["k8s/migrations/omnibase-infra-migrate.yaml","k8s/onex-dev/readiness/staging-composition.canonical-dev.v1.yaml","k8s/onex-dev/runtime/kustomization.yaml"]) then "true" else error("unexpected #818 state, head, base, or file set") end'
  - id: "dod-node-pr818-migration-digest"
    description: "PR #818 migration manifest pins the migrate image tag and digest built from omnibase_infra
      a853500a."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api 'repos/OmniNode-ai/omninode_infra/contents/k8s/migrations/omnibase-infra-migrate.yaml?ref=cb0208badf7a81f4ca97a2995d88b90f06440900' --jq .content | base64 -d | grep -c 'a853500ab3c74620acdba34b418f6bd087081153@sha256:64f519fe7907329e01bce38693f77f34a033ee9c33b8c91e22044248a3a7ec18'
  - id: "dod-occ-evidence-admissibility-validator-pr-818"
    description: "Hosted OCC evidence admissibility validator for PR #818 (OMN-15247)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-818"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
