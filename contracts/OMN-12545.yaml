---
schema_version: "1.0.0"
ticket_id: "OMN-12545"
title: "Consolidate duplicated dispatch enums to single CORE EnumDispatchStatus copy"
summary: >-
  Binds central OCC evidence for omnibase_core PR #1187, which merges the two infra-only members (NO_DISPATCHER,
  INTERNAL_ERROR) from the duplicate omnibase_infra.enums.enum_dispatch_status into the canonical core
  EnumDispatchStatus and wires them into is_terminal/is_error/get_description. The change is purely additive:
  no existing member, string value, or helper-method result changes, so the core enum becomes a lossless
  superset that lets the infra dup be deleted and importers repointed. ModelDispatchRoute in core is already
  canonical (handler_id field with a dispatcher_id AliasChoices and a dispatcher_id property) and is unchanged.
  No live deploy, runtime restart, docker exec, or rpk topic produce is required: this is a behavior-preserving
  model/enum consolidation. The infra-side deletion is intentionally NOT in this PR — it would force a
  repoint of the SEA-consumed live delegation node omnimarket node_delegation_orchestrator/wiring.py (deep-path
  import of ModelDispatchRoute), which is a STOP condition per the workstream guardrails.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "EnumDispatchStatus unit test reflects the 10-member superset and exhaustive classification;
      core dispatch consumers stay green."
    command: "uv run pytest tests/unit/enums/test_enum_dispatch_status.py -v"
  - kind: "ci"
    description: "omnibase_core PR #1187 CI green across required gates."
    command: "gh pr checks 1187 --repo OmniNode-ai/omnibase_core --watch"
dod_evidence:
  - id: "dod-core-pr-1187"
    description: "Bind OMN-12545 evidence to omnibase_core PR #1187 at the observed PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12545/dod-core-pr-1187/command.yaml\"
          && grep -q '^pr_number: 1187$' \"drift/dod_receipts/OMN-12545/dod-core-pr-1187/command.yaml\"
          && grep -q '^commit_sha: \"12ed4fecff03245e05253c630adc6742c4743f73\"$' \"drift/dod_receipts/OMN-12545/dod-core-pr-1187/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Behavior-preserving additive enum consolidation requires CI and receipt-gate validation
      but no live deploy before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12545/dod-deploy-assessment/command.yaml\"
          && grep -q 'deploy gate' \"drift/dod_receipts/OMN-12545/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind OMN-12545 central evidence to this onex_change_control PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12545/dod-occ-pr/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
