---
schema_version: "1.0.0"
ticket_id: "OMN-18223"
title: "Autobind OCC evidence for OMN-18223"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #1392
  and OmniNode-ai/omnimarket PR #2498.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1392 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1392 on OmniNode-ai/omninode_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omninode_infra`.
    command: "uv run pytest tests/ci/test_provider_key_chain_observations_omn18086.py -q"
  - kind: "ci"
    description: "PR #2498 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2498 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof-pr-2498`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: |-
      uv run pytest tests/unit/delegation/test_omn18223_credential_source_survives_every_terminal.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1392"
    description: "PR #1392 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1392 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1392/files --paginate --jq '.[].filename' | grep -cFx 'tests/ci/test_provider_key_chain_observations_omn18086.py'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #1392 on OmniNode-ai/omninode_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1392"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/ci/test_provider_key_chain_observations_omn18086.py -q"
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "occ-self-bind-pr-9257"
    description: "Binds OMN-18223 to OCC companion PR #9257 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9257 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2498"
    description: "PR #2498 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/tests/unit/delegation/test_omn18223_credential_source_survives_every_terminal.py?ref=394da6ca42bef2cbaf8d84ead760170176cde484 --jq '.content' | base64 -d | grep -c 'def _make_request'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2498/files --paginate --jq '.[].filename' | grep -cFx 'src/omnimarket/nodes/node_delegation_orchestrator/handlers/handler_delegation_workflow.py'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2498 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2498/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "dod-occ-diff-derived-behavior-proof-pr-2498"
    description: "PR #2498 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-diff-derived-behavior-proof"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/delegation/test_omn18223_credential_source_survives_every_terminal.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-9256"
    description: "Binds OMN-18223 to OCC companion PR #9256 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9256 --repo OmniNode-ai/onex_change_control --json number,state"
