---
schema_version: "1.0.0"
ticket_id: "OMN-12168"
title: "Delete high-confidence dead code in omnibase_core"
summary: >
  Central OCC evidence for omnibase_core PR #1163. The core change removes the unused ModelNodeConfigurationValue.from_value
  helper, keeps the dynamically loaded handler contract validator, and keeps the remaining referenced
  utilities covered by focused tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused tests for the touched contract diff, node configuration, and bootstrap modules
      pass."
    command: "See drift/dod_receipts/OMN-12168/dod-focused-tests/command.yaml"
  - kind: "ci"
    description: "Ruff check passes for the touched omnibase_core files."
    command: "See drift/dod_receipts/OMN-12168/dod-ruff/command.yaml"
  - kind: "manual"
    description: "Dead-code reference scan finds no non-doc canonical references to the removed helper."
    command: "See drift/dod_receipts/OMN-12168/dod-reference-scan/command.yaml"
  - kind: "ci"
    description: "Deploy-gate posture is recorded for omnibase_core PR #1163."
    command: "See drift/dod_receipts/OMN-12168/dod-deploy-gate/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR #1670 carries the central OMN-12168 contract and receipts."
    command: "See drift/dod_receipts/OMN-12168/dod-occ-pr-1670/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused tests passed for the modules touched by OMN-12168."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12168/dod-focused-tests/command.yaml
          && grep -q '259 passed' drift/dod_receipts/OMN-12168/dod-focused-tests/command.yaml"
  - id: "dod-ruff"
    description: "Ruff check passed for the touched contract, node metadata, and bootstrap files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12168/dod-ruff/command.yaml && grep
          -q 'All checks passed' drift/dod_receipts/OMN-12168/dod-ruff/command.yaml"
  - id: "dod-reference-scan"
    description: "Reference scan confirms no non-doc canonical references to the removed helper."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12168/dod-reference-scan/command.yaml
          && grep -q 'no non-doc canonical references' drift/dod_receipts/OMN-12168/dod-reference-scan/command.yaml"
  - id: "dod-deploy-gate"
    description: "Deploy Gate for PR #1163 passed; no live pre-merge deploy was required for this dead-code
      deletion."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12168/dod-deploy-gate/command.yaml
          && grep -q 'deploy-gate / deploy-gate' drift/dod_receipts/OMN-12168/dod-deploy-gate/command.yaml"
  - id: "dod-occ-pr-1670"
    description: "onex_change_control PR #1670 carries the central OMN-12168 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12168/dod-occ-pr-1670/command.yaml
          && grep -q '^pr_number: 1670$' drift/dod_receipts/OMN-12168/dod-occ-pr-1670/command.yaml"
