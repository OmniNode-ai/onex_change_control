---
schema_version: "1.0.0"
ticket_id: "OMN-10649"
title: "Replace credential literal in delegation cost projection test fixture"
summary: >
  Replaces the super-secret test fixture password with an explicit redacted placeholder while preserving
  the projection-process secret redaction test coverage.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ci-summary"
    description: "omnimarket PR #555 CI Summary passed on the evidence head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10649/dod-ci-summary/command.yaml\""
  - id: "dod-pr-555-head"
    description: "Bind OMN-10649 evidence to omnimarket PR #555 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10649/dod-pr-555-head/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10649 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10649/dod-occ-pr/command.yaml\""
