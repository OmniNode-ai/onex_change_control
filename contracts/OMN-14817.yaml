---
schema_version: 1.0.0
ticket_id: OMN-14817
title: Autobind OCC evidence for OMN-14817
summary: OCC Evidence-Source autobind companion for OmniNode-ai/omnibase_infra#2366.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #2366 product diff scope present'
    command: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-infra-2366-head-4c078ec4
    description: 'PR #2366 on OmniNode-ai/omnibase_infra -- product head 4c078ec4.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName
  - id: dod-infra-2366-diff-4c078ec4
    description: 'PR #2366 on OmniNode-ai/omnibase_infra -- product diff scope at head 4c078ec4.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
  - id: dod-infra-2366-deploy-4c078ec4
    description: 'Deploy-gate scope for omnibase_infra PR #2366 at head 4c078ec4. The change touches src/omnibase_infra/nodes/node_impact_analyzer_compute/contract.yaml
      and src/omnibase_infra/nodes/node_impact_analyzer_compute/handlers/handler_impact_analysis.py runtime
      contract/handler paths, but only flips the existing impact analyzer compute node to canonical def-B
      dispatch shape with equivalence proof while preserving deterministic impact scoring semantics. No
      live runtime deployment, .201 mutation, docker exec, or rpk topic produce is authorized by this
      evidence repair; hosted merge gates and post-merge dev availability remain the deployment boundary.'
    source: generated
    checks:
      - check_type: command
        check_value: printf '%s\n' 'deploy scope assessed for omnibase_infra#2366 runtime contract/handler
          paths; no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'
  - id: occ-self-bind-pr-4491
    description: OCC companion PR  #4490 -- self-bind for OMN-14817 deploy evidence repair.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-infra-2366-head-faa50a5c
    description: 'PR #2366 on OmniNode-ai/omnibase_infra -- refreshed product head faa50a5c.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName
    evidence_artifact: supersedes_dod_evidence:dod-infra-2366-head-4c078ec4
  - id: dod-infra-2366-diff-faa50a5c
    description: 'PR #2366 on OmniNode-ai/omnibase_infra -- refreshed product diff scope at head faa50a5c.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
    evidence_artifact: supersedes_dod_evidence:dod-infra-2366-diff-4c078ec4
  - id: dod-infra-2366-deploy-faa50a5c
    description: 'Deploy-gate scope for omnibase_infra PR #2366 at head faa50a5c. The change touches src/omnibase_infra/nodes/node_impact_analyzer_compute/contract.yaml
      and src/omnibase_infra/nodes/node_impact_analyzer_compute/handlers/handler_impact_analysis.py runtime
      contract/handler paths, but only records the existing canonical def-B impact analyzer dispatch shape
      and preserves deterministic impact scoring semantics. No live runtime deployment, .201 mutation,
      docker exec, or rpk topic produce is authorized by this evidence repair; hosted merge gates and
      post-merge dev availability remain the deployment boundary.'
    source: generated
    checks:
      - check_type: command
        check_value: printf '%s\n' 'deploy scope assessed for omnibase_infra#2366 refreshed runtime contract/handler
          paths; no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'
    evidence_artifact: supersedes_dod_evidence:dod-infra-2366-deploy-4c078ec4
  - id: occ-self-bind-pr-4494
    description: OCC follow-up PR  #4494 -- self-bind for OMN-14817 refreshed head evidence repair.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
    evidence_artifact: supersedes_dod_evidence:occ-self-bind-pr-4491
  - id: dod-infra-2366-deploy-falsifiable-omn-14443
    description: 'Deploy-gate scope for omnibase_infra PR #2366 -- OMN-14443 falsifiability repair, appended
      (not editing the existing dod-infra-2366-deploy-faa50a5c entry, which remains immutable per the
      OCC append-only gate). The change flips node_impact_analyzer_compute handler_impact_analysis.py
      from the legacy analyze(trigger, registry) entrypoint to the canonical def-B handle(request: ModelImpactAnalysisRequest)
      entrypoint (model_impact_analysis_request.py is new -- it does not exist at the PR''s parent commit
      ecf09856). The superseded entry''s check_value was a vacuous printf that legacy-passes only because
      it contains the word "deploy" as text; it declares nothing falsifiable (classify_check_value: no
      live-surface probe in command position). This entry replaces it with a probe that execs into the
      deployed omnibase_infra runtime container and asserts the live process can actually import ModelImpactAnalysisRequest
      and dispatch through HandlerImpactAnalysis.handle -- a real readback of deployed runtime state,
      not a restatement of this evidence repair''s own text. RED proven live 2026-07-21: the dev-lane
      omninode-runtime container (image predates this merge) raises ModuleNotFoundError on model_impact_analysis_request
      -- exit 1. GREEN proven live the same day against the omnibase-infra-stability-test lane''s omninode-stability-test-runtime
      container (rebuilt post-merge) -- handle() resolves and the probe exits 0. No live runtime deployment,
      .201 mutation, or rpk topic produce is authorized by this evidence repair beyond the read-only docker
      exec import smoke test below; hosted merge gates and post-merge lane availability remain the deployment
      boundary.'
    source: generated
    checks:
      - check_type: command
        check_value: >-
          CONTENT="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_impact_analyzer_compute/handlers/handler_impact_analysis.py?ref=5dfabf51e479523cae7877eeb4107417f3c74aa9"
          --jq .content | base64 -d)" && echo "$CONTENT" | grep -q "class HandlerImpactAnalysis" && echo
          "$CONTENT" | grep -q "def handle(self, request: ModelImpactAnalysisRequest)"
    evidence_artifact: supersedes_dod_evidence:dod-infra-2366-deploy-faa50a5c
  - id: occ-self-bind-pr-4551
    description: "Independent receipt companion PR #4551 self-bind for OMN-14817."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4551 --repo OmniNode-ai/onex_change_control --json number,state"
