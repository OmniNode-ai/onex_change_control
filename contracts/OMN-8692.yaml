---
schema_version: "1.0.0"
ticket_id: "OMN-8692"
title: "feat(OMN-8692): wire infra_routing_decisions projection pipeline"
summary: >
  Wires the infra_routing_decisions projection pipeline end-to-end: contract.yaml topic registration,
  handler auto-wiring, and projection reducer for routing decision events so the evidence dashboard can
  render infrastructure routing metrics.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
evidence_requirements:
  - kind: "tests"
    description: "Projection pipeline wiring tests pass at the omnibase_infra PR head."
    command: "uv run pytest tests/ -k 'routing_decisions' -v"
  - kind: "manual"
    description: "omnibase_infra PR #1700 is open against main at the recorded head SHA."
    command: "gh pr view 1700 --repo OmniNode-ai/omnibase_infra --json state -q '.state'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-1700"
    description: "omnibase_infra PR #1700 carries the infra_routing_decisions pipeline wiring at commit
      1470a3c6."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8692/dod-product-pr-1700/command.yaml"
          && grep -q '^pr_number: 1700$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8692/dod-product-pr-1700/command.yaml"
  - id: "dod-deploy"
    description: "deploy gate: new projection pipeline wired from contract.yaml — auto-wiring handles
      registration on next runtime deploy."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy gate: OMN-8692 wires infra_routing_decisions projection pipeline
          via contract.yaml — auto-wiring registers handler on next omninode-runtime deploy, no manual
          docker exec required'"
