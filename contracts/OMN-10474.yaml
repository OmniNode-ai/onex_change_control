---
schema_version: "1.0.0"
ticket_id: OMN-10474
title: "Add golden-chain coverage for omnimarket node_quality_scoring_compute"
summary: "Add focused local golden-chain/unit coverage for omnimarket node_quality_scoring_compute so
  existing handler behavior is protected by direct tests."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Focused quality scoring golden-chain tests pass locally"
    command: "cd ${OMNI_HOME:-/Users/jonah/Code/omni_home}/omni_worktrees/quality-scoring/omnimarket &&
      env -u PYTHONPATH uv run pytest tests/test_golden_chain_quality_scoring_compute.py -q"
  - kind: tests
    description: "Touched quality scoring test file passes ruff"
    command: "cd ${OMNI_HOME:-/Users/jonah/Code/omni_home}/omni_worktrees/quality-scoring/omnimarket &&
      env -u PYTHONPATH uv run ruff check tests/test_golden_chain_quality_scoring_compute.py"
  - kind: tests
    description: "Pre-commit hooks pass for the touched quality scoring test file"
    command: "cd ${OMNI_HOME:-/Users/jonah/Code/omni_home}/omni_worktrees/quality-scoring/omnimarket &&
      OMNIBASE_INFRA_PATH=${OMNI_HOME:-/Users/jonah/Code/omni_home}/omnibase_infra env -u PYTHONPATH uv
      run pre-commit run --files tests/test_golden_chain_quality_scoring_compute.py"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused quality scoring golden-chain tests pass locally"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10474/dod-001/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10474/dod-001/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10474/dod-001/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-002
    description: "Touched quality scoring test file passes ruff"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10474/dod-002/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10474/dod-002/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10474/dod-002/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-003
    description: "Pre-commit hooks pass for the touched quality scoring test file"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10474/dod-003/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10474/dod-003/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10474/dod-003/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
