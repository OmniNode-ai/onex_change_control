---
schema_version: "1.0.0"
ticket_id: "OMN-14855"
title: "Autobind OCC evidence for OMN-14855"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1842."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1842 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1842"
    description: "PR #1842 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1842-ci"
    description: "PR #1842 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4509"
    description: "OCC companion PR #4509 — self-bind for OMN-14855 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1842-deploy-assessment"
    description: "Deploy-gate scope for omnimarket PR #1842 at head 267c87a8. The change updates the runtime
      handler src/omnimarket/nodes/node_projection_delegation/handlers/handler_projection_delegation.py
      to drop envelope-only _topic before judge-verdict model construction. No live deploy, restart, .201
      mutation, docker exec, or production mutation is authorized by this evidence repair; hosted merge
      gates and post-merge dev availability remain the deployment boundary."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy scope assessed for omnimarket#1842 projection delegation
          runtime handler; no live deploy, restart, .201 mutation, docker exec, or production mutation
          is authorized by this evidence repair'"
  - id: "occ-self-bind-pr-4510"
    description: "OCC amendment PR #4510 -- self-bind for OMN-14855 deploy-scope evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
