---
schema_version: "1.0.0"
ticket_id: "OMN-10706"
title: "Replace direct baselines handler import with node package public API"
summary: >
  Updates omnibase_infra proof-of-life scripting to import the baselines batch compute handler and command
  model through the node package public API instead of reaching into the private handlers module. This
  is an import-boundary cleanup only; it does not change runtime wiring, topics, migrations, or deploy
  behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-import-boundary"
    description: "omnibase_infra PR #1572 replaces the direct private handler import with the node package
      public API"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10706/dod-import-boundary/command.yaml && grep -q
          '^status:.*PASS' drift/dod_receipts/OMN-10706/dod-import-boundary/command.yaml"
  - id: "dod-deploy"
    description: >
      Deploy verification: import-boundary-only script change. No handler code, runtime topology, migration,
      docker exec action, Redpanda topic change, or service restart is required.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10706/dod-deploy/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10706/dod-deploy/command.yaml && grep -q 'deploy' drift/dod_receipts/OMN-10706/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for the OCC PR carrying this contract."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10706/dod-occ-pr/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10706/dod-occ-pr/command.yaml"
