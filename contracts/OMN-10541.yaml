---
schema_version: "1.0.0"
ticket_id: "OMN-10541"
title: "dod-guard fails closed on status=skipped, missing receipt, parse error"
summary: "Tighten the DoD completion guard so only an explicit status=PASS with a fresh, parseable
  ModelDodReceipt and all required keys present permits the Done transition. Block ADVISORY,
  PENDING, FAIL, unknown statuses (skipped), missing receipts, parse failures, missing
  run_timestamp/status, and naive timestamps."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "39 dod-guard tests pass on omniclaude PR #1515 (hooks, unit hard-mode, integration chain)"
    command: "cd omni_worktrees/OMN-10540/omniclaude && KAFKA_BOOTSTRAP_SERVERS=localhost:19092
      KAFKA_INTEGRATION_TESTS=1 uv run pytest tests/integration/test_dod_enforcement_chain.py
      tests/hooks/test_dod_completion_guard.py tests/unit/test_dod_enforcement_hard_mode.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "39 dod-guard tests pass on omniclaude PR #1515 at commit 29712204"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10541/dod-001/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-001\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10541/dod-001/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10541\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10541/dod-001/command.yaml\"
          && grep -q '^commit_sha: \"29712204e09650b1df000a6f70f4d54611e13d5f\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10541/dod-001/command.yaml\"
          && grep -q '^pr_number: 1515$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10541/dod-001/command.yaml\""
  - id: "dod-deploy"
    description: "No runtime deploy required — change is a hook script + test fixtures.
      Hook script is loaded fresh from disk on every Claude Code session; no daemon restart
      or container rebuild is needed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10541/dod-deploy/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-deploy\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10541/dod-deploy/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10541\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10541/dod-deploy/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carrying the OMN-10541 contract and receipt evidence"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10541/dod-occ-pr/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-occ-pr\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10541/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10541\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10541/dod-occ-pr/command.yaml\""
