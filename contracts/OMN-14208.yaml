---
schema_version: "1.0.0"
ticket_id: "OMN-14208"
title: "feat(OMN-14208): durable state_io runtime seam for delegation workflow state"
summary: >-
  OMN-14208 slice 1 introduces a cross-repo `state_io` runtime seam for the delegation orchestrator. omnibase_infra
  PR #2247 adds the durable load-before/CAS-persist-after wiring arm, state store adapter, forward migration,
  and runtime tests. omnimarket PR #1667 adds the delegation workflow codec/proxy/contract declaration
  and seam-match regression tests that prove the infra ContextVar, codec flush bridge, and top-level payload
  keys interoperate. Both PRs remain draft until this central OCC evidence lands and downstream receipt
  gates pass.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2247"
    description: >-
      Live GitHub verification shows omnibase_infra PR #2247 targets dev from branch jonah/omn-14208-state-io-runtime-seam
      at head ddef3c20d48b32ffbdcd11f761270510cc7d0d49. The diff adds the `delegation_workflow_state`
      forward migration, the runtime state_io adapter, handler wiring support, env-read guard updates,
      and focused unit/integration coverage for the state_io dispatch seam.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14208/dod-infra-pr-2247/command.yaml && grep
          -q '^pr_number: 2247$' drift/dod_receipts/OMN-14208/dod-infra-pr-2247/command.yaml
  - id: "dod-market-pr-1667"
    description: >-
      Live GitHub verification shows omnimarket PR #1667 targets dev from branch jonah/omn-14208-delegation-durable-state
      at head 5a5c013759e72fb45e1fa303170c12d6a21da1ad. The diff adds the delegation workflow state codec/proxy
      bridge, declares the contract-level state_io block, updates wiring to share the proxy used by the
      codec, and adds focused state round-trip/proxy coverage.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14208/dod-market-pr-1667/command.yaml && grep
          -q '^pr_number: 1667$' drift/dod_receipts/OMN-14208/dod-market-pr-1667/command.yaml
  - id: "dod-joint-seam-verification"
    description: >-
      Joint seam verification is recorded from the two clean product worktrees and the PR-local verification
      transcripts: omnimarket #1667 proves the codec declaration shape, infra-owned ContextVar import,
      codec flush bridge, and well-known payload keys; omnibase_infra #2247 proves the load-before/CAS-persist-after
      wiring arm, opaque JSONB state store, stale-row recovery call, and column metadata extraction. The
      two PR bodies document the pair-verify remediations M0/M1/M2/G1/R1 and the local test suites that
      passed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14208/dod-joint-seam-verification/command.yaml
          && grep -q 'TestStateIoCodecFlushBridge' drift/dod_receipts/OMN-14208/dod-joint-seam-verification/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment for OMN-14208 runtime-path changes across omnimarket PR #1667 and omnibase_infra
      PR #2247. No live deploy, restart, docker exec, rpk topic produce, or .201/runtime mutation was
      performed as part of this pre-merge evidence. The change adds a durable state_io runtime seam, a
      forward migration, and handler wiring; merge remains gated by product CI plus post-merge deploy
      validation through the standard deploy gate path.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14208/dod-deploy-assessment/command.yaml &&
          grep -q 'deploy-gate assessment' drift/dod_receipts/OMN-14208/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the OMN-14208 contract and PASS
      receipts consumed by omnibase_infra PR #2247 and omnimarket PR #1667 through their Evidence-Source
      lines. It binds itself to occ-preflight by commit_sha from this PR's commit set instead of predicting
      the OCC PR number.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14208/dod-occ-self/command.yaml
  - id: "dod-occ-deploy-refresh-self"
    description: >-
      OCC follow-up self-binding: this onex_change_control PR appends the OMN-14208 deploy-gate assessment
      evidence required by downstream product deploy gates and supersedes prior OMN-14208 receipts to
      the new contract hash.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14208/dod-occ-deploy-refresh-self/command.yaml
  - id: "dod-deploy-assessment-runtime-seam-live"
    description: >-
      OMN-14443 falsifiability backfill. dod-deploy-assessment's original check_value only greps this
      ticket's own receipt file for the literal word "deploy" -- self-referential, satisfies the legacy
      deploy-gate substring rule, and is REJECTED by classify_check_value() under the OMN-14505/OMN-14443
      falsifiability enforcement (fails the SELF_REFERENTIAL_PATTERNS deny on drift/dod_receipts/). This
      is the ticket the 2026-07-21 org-wide shadow-measure identified as the ONLY currently-open-PR regression
      under enforce: omnibase_infra#2375 (OMN-14222) cites both OMN-14222 (no contract) and OMN-14208
      (self-referential-only) in its body, so it PASSes today only via the legacy substring match and
      would FAIL under enforce with no other falsifiable evidence on either cited ticket. This item supersedes
      dod-deploy-assessment with a genuine live-runtime probe: the delegation_workflow_state table created
      by omnibase_infra#2247's forward migration (docker/migrations/forward/090_create_delegation_workflow_state.sql)
      is asserted to exist on the deployed dev-lane Postgres (container omnibase-infra-postgres, reached
      via the Tailscale MagicDNS host omninode-pc.tail75df5e.ts.net -- never the raw LAN IP, which is
      unreachable from off-LAN sessions). RED proven: the identical probe run against a nonexistent table
      name ("nonexistent_undeployed_table_omn14208") exits 1 with "Did not find any relation named ...".
      GREEN proven: the same probe against the real table name exits 0. The probe's exit status therefore
      depends on the live deployed database state, not on any text this PR's author wrote -- it is NOT
      self-referential (no drift/dod_receipts/ or contracts/OMN-\d+ reference in the check_value) and
      classify_check_value() classifies it falsifiable via the `ssh` live-probe command in command position.
      Prior item's "No live deploy...was performed" language is now superseded -- the OMN-14208 slice-1
      runtime seam is demonstrably deployed on the dev lane.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: |-
          ssh -o ConnectTimeout=8 -o BatchMode=yes jonah@omninode-pc.tail75df5e.ts.net "docker exec omnibase-infra-postgres psql -U postgres -d omnibase_infra -c '\dt delegation_workflow_state'" | grep -q "delegation_workflow_state"
