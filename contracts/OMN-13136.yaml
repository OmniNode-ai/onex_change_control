---
schema_version: "1.0.0"
ticket_id: "OMN-13136"
title: "Add util_omni_home_paths — canonical OMNI_HOME-derived path resolver"
summary: >
  Adds omnibase_core.utils.util_omni_home_paths with three public resolver functions (resolve_worktrees_root,
  resolve_evidence_root, resolve_omnibase_infra_path) that derive paths from OMNI_HOME at call-time, fail-fast
  when OMNI_HOME is unset, and replace the brittle shell-expansion approach in ~/.omnibase/.env that baked
  broken bare-root paths when OMNI_HOME was not exported at source time.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1352"
    description: "omnibase_core PR #1352 adds util_omni_home_paths with 3 resolvers and 9 passing unit
      tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13136/dod-core-pr-1352/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "onex_change_control OCC PR carries the OMN-13136 contract and receipt."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13136/dod-occ-pr-self/command.yaml\""
