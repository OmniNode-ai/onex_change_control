---
schema_version: "1.0.0"
ticket_id: OMN-14099
title: "infra: install pre-commit git hooks via pull-all"
summary: >
  Installs pre-commit git hooks idempotently from the canonical pull-all sync
  entrypoint so local enforcement runs before CI for repositories that already
  define pre-commit hooks.
is_seam_ticket: false
interface_change: false
evidence_requirements:
  - kind: "tests"
    description: "omnibase_infra pull-all hook installation tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14099/dod-tests/command.yaml"
  - kind: "ci"
    description: "omnibase_infra lint and shell validation pass for pull-all hook install change"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14099/dod-lint-shell-precommit/command.yaml"
  - kind: "manual"
    description: "omnibase_infra PR #2225 carries the OMN-14099 pull-all pre-commit install change"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14099/dod-omnibase-infra-pr-2225/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carries the central OMN-14099 contract and PASS receipts"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14099/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "omnibase_infra pull-all hook installation tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14099/dod-tests/command.yaml"
  - id: "dod-lint-shell-precommit"
    description: "omnibase_infra lint and shell validation pass for pull-all hook install change"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14099/dod-lint-shell-precommit/command.yaml"
  - id: "dod-omnibase-infra-pr-2225"
    description: "omnibase_infra PR #2225 carries the OMN-14099 pull-all pre-commit install change"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14099/dod-omnibase-infra-pr-2225/command.yaml
          && grep -q '^pr_number: 2225$' drift/dod_receipts/OMN-14099/dod-omnibase-infra-pr-2225/command.yaml"
  - id: "dod-occ-self"
    description: "onex_change_control PR carries the central OMN-14099 contract and PASS receipts"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14099/dod-occ-self/command.yaml"
