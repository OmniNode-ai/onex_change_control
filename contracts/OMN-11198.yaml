---
schema_version: "1.0.0"
ticket_id: "OMN-11198"
title: "Add /v1/introspection/manifest endpoint (internal only)"
summary: >
  Adds an internal-only HTTP endpoint /v1/introspection/manifest that serves the runtime ModelRuntimeManifest
  as JSON. Enables introspection of live runtime topology for operators and ops dashboards (part of OMN-11188).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1640 carries the implementation."
    command: "See drift/dod_receipts/OMN-11198/dod-infra-pr-1640/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1640"
    description: "omnibase_infra PR #1640 carries the OMN-11198 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11198/dod-infra-pr-1640/command.yaml
          && grep -q '^pr_number: 1640$' drift/dod_receipts/OMN-11198/dod-infra-pr-1640/command.yaml"
  - id: "dod-occ-pr-1121"
    description: "onex_change_control PR #1121 carries the OMN-11198 contract and receipt."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11198/dod-occ-pr-1121/command.yaml
          && grep -q '^pr_number: 1121$' drift/dod_receipts/OMN-11198/dod-occ-pr-1121/command.yaml"
  - id: "dod-deploy-introspection-endpoint"
    description: "Post-deploy: /v1/introspection/manifest reachable on the deployed runtime."
    source: "manual"
    checks:
      - check_type: "command"
        # deploy keyword present so the runtime deploy-gate (OMN-8912) recognizes this contract
        # as deploy-validated for runtime-path PRs (OMN-11198 modifies runtime/service_kernel.py + service_health.py).
        check_value: "docker exec omninode-runtime curl -fsS http://localhost:8085/v1/introspection/manifest"
  - id: "dod-occ-pr-1124"
    description: "onex_change_control PR #1124 adds the dod-deploy-introspection-endpoint item to this
      contract."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11198/dod-occ-pr-1124/command.yaml
          && grep -q '^pr_number: 1124$' drift/dod_receipts/OMN-11198/dod-occ-pr-1124/command.yaml"
