---
schema_version: "1.0.0"
ticket_id: "OMN-11553"
title: "refactor(OMN-11553, OMN-11558, OMN-11563): rename validators + split debug_utils [omniclaude]"
summary: >
  Pure mechanical renames in omniclaude lib/utils/: naming_validator.py → validator_naming_conventions.py
  (OMN-11553), quality_enforcer.py → validator_quality.py (OMN-11558), debug_utils.py → diagnostics.py
  (OMN-11563). All internal imports, lazy-load map, hook scripts, and 5 affected test files updated. 46
  unit tests pass. No deployable artifact — rename only.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: 46 unit tests pass covering all renamed modules and their consumers.
    command: >
      cd omniclaude && uv run pytest tests/unit/test_session_id_migration_src.py tests/unit/hooks/lib/test_quality_enforcer_emission.py
      tests/unit/hooks/lib/test_quality_enforcer_enforcement_mode.py tests/unit/lib/utils/test_circular_import_regression.py
      tests/unit/lib/utils/test_sanitize.py -v
  - kind: manual
    description: omniclaude PR  #1658 carries the rename implementation.
    command: gh pr checks 1658 --repo OmniNode-ai/omniclaude
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "46 unit tests pass — all renamed module consumers verified clean."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11553/dod-unit-tests/command.yaml\""
  - id: "dod-no-deploy"
    description: >
      No deployable artifact. This ticket is a pure file rename with no runtime behavior change. Pre-commit
      hooks pass; no new nodes, topics, or contracts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11553/dod-no-deploy/command.yaml\""
