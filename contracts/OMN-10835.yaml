---
schema_version: "1.0.0"
ticket_id: OMN-10835
title: "Fix env_silent_fallback validator to exempt os.environ.get(KEY, None)"
summary: "Adds _default_arg_is_none() helper to EnvSilentFallbackGate so that os.environ.get(KEY, None)
  and os.getenv(KEY, None) are no longer flagged as silent fallbacks. Explicit None is semantically identical
  to no default — the caller still receives None and must handle the missing-key case explicitly."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: FULL
evidence_requirements:
  - kind: tests
    description: "20 unit tests green in omnibase_core env_silent_fallback test suite"
    command: "uv run pytest tests/unit/cli/substrate_gates/test_env_silent_fallback.py -v"
  - kind: ci
    description: "CI pipeline green on omnibase_core PR #1062"
    command: "gh pr checks 1062 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-core-pr-1062
    description: "omnibase_core PR #1062 open with None-default exemption and 20 passing tests"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10835/dod-core-pr-1062/command.yaml\"
          && grep -q '^pr_number: 1062$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10835/dod-core-pr-1062/command.yaml\""
  - id: dod-occ-contract
    description: "OCC contract for OMN-10835 merged to main"
    source: generated
    checks:
      - check_type: command
        check_value: "test -f \"$CONTRACT_REPO_DIR/contracts/OMN-10835.yaml\""
