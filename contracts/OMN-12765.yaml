---
schema_version: "1.0.0"
ticket_id: "OMN-12765"
title: "Backmerge omnimarket main release coverage into dev"
summary: >-
  Central OCC evidence for omnimarket PR #1078. The backmerge merges main release/demo coverage
  into dev while preserving the current dev pure-Kafka delegation path, provider request option
  handling, direct-curl delegation evidence, and delegation projection behavior.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1078"
    description: "omnimarket PR #1078 carries the OMN-12765 main-to-dev backmerge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12765/dod-omnimarket-pr-1078/command.yaml
          && grep -q '^pr_number: 1078$' drift/dod_receipts/OMN-12765/dod-omnimarket-pr-1078/command.yaml
          && grep -q '49f7019e' drift/dod_receipts/OMN-12765/dod-omnimarket-pr-1078/command.yaml
  - id: "dod-focused-delegation-tests"
    description: "Focused local delegation/delegate-skill tests passed on the backmerge worktree."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12765/dod-focused-delegation-tests/command.yaml
          && grep -q '102 passed' drift/dod_receipts/OMN-12765/dod-focused-delegation-tests/command.yaml
  - id: "dod-deploy-gate-evidence"
    description: "Deploy gate evidence assessment for the backmerge without live restart."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12765/dod-deploy-gate-evidence/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-12765/dod-deploy-gate-evidence/command.yaml
          && grep -q 'no live deploy or restart performed' drift/dod_receipts/OMN-12765/dod-deploy-gate-evidence/command.yaml
  - id: "dod-occ-evidence-pr-2259"
    description: "onex_change_control PR #2259 carries the central OCC evidence for OMN-12765."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12765/dod-occ-evidence-pr-2259/command.yaml
          && grep -q '^pr_number: 2259$' drift/dod_receipts/OMN-12765/dod-occ-evidence-pr-2259/command.yaml
          && grep -q '2460a47a' drift/dod_receipts/OMN-12765/dod-occ-evidence-pr-2259/command.yaml
