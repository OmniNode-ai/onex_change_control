---
schema_version: "1.0.0"
ticket_id: "OMN-12765"
title: "Promote omnimarket dev to main"
summary: >-
  Central OCC evidence for omnimarket PR #1077. The release promotion merges the current dev
  integration state into main while preserving the pure-Kafka delegation path, provider request
  option handling, direct-curl delegation evidence, and main-target promotion guard behavior.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1077"
    description: "omnimarket PR #1077 carries the OMN-12765 dev-to-main promotion merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12765/dod-omnimarket-pr-1077/command.yaml
          && grep -q '^pr_number: 1077$' drift/dod_receipts/OMN-12765/dod-omnimarket-pr-1077/command.yaml
          && grep -q '576c7b40' drift/dod_receipts/OMN-12765/dod-omnimarket-pr-1077/command.yaml
  - id: "dod-focused-delegation-tests"
    description: "Focused local delegation/delegate-skill tests passed on the promotion worktree."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12765/dod-focused-delegation-tests/command.yaml
          && grep -q '51 passed' drift/dod_receipts/OMN-12765/dod-focused-delegation-tests/command.yaml
          && grep -q 'projection_rows.json: valid' drift/dod_receipts/OMN-12765/dod-focused-delegation-tests/command.yaml
  - id: "dod-deploy-gate-evidence"
    description: "Deploy gate evidence assessment for runtime-path promotion changes without live restart."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12765/dod-deploy-gate-evidence/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-12765/dod-deploy-gate-evidence/command.yaml
          && grep -q 'no live deploy or restart performed' drift/dod_receipts/OMN-12765/dod-deploy-gate-evidence/command.yaml
