---
schema_version: "1.0.0"
ticket_id: "OMN-11279"
title: "feat(OMN-11279): create node_session_phase_reducer (REDUCER) in omnimarket"
summary: >
  node_session_phase_reducer is the deterministic REDUCER node for session phase state. Core reducer implementation,
  contract, metadata, and unit tests were delivered under OMN-11230 / omnimarket#703; omnimarket#727 completes
  the OMN-11279 file-list requirement by adding the missing __main__.py CLI entry point for the existing
  node.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: 16 reducer unit tests pass and full unit suite reported green in the product PR body.
    command: uv run pytest tests/unit/nodes/node_session_phase_reducer/ -v -q
  - kind: manual
    description: omnimarket PR  #727 carries the missing node_session_phase_reducer __main__.py entry point.
    command: gh pr view 727 --repo OmniNode-ai/omnimarket --json headRefOid,state,url
  - kind: manual
    description: OCC PR self-binds the OMN-11279 contract and receipts.
    command: gh pr view <OCC-PR-NUM> --repo OmniNode-ai/onex_change_control --json number,state
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-cli-entrypoint"
    description: "omnimarket#727 adds the required node_session_phase_reducer __main__.py CLI entry point."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -Eq '^status: "?PASS"?$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11279/dod-cli-entrypoint/command.yaml"
          && grep -q '^pr_number: 727$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11279/dod-cli-entrypoint/command.yaml"
          && grep -q '^commit_sha: "32fa118979a60187a0941c23beb887636b0e1932"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11279/dod-cli-entrypoint/command.yaml"
  - id: "dod-product-checks"
    description: "All current omnimarket#727 checks except Receipt Gate are green; Receipt Gate is the
      evidence repaired here."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -Eq '^status: "?PASS"?$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11279/dod-product-checks/command.yaml"
          && grep -q 'Receipt Gate is the only failing check' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11279/dod-product-checks/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11279 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >
          grep -Eq '^status: "?PASS"?$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11279/dod-occ-pr-self/command.yaml"
