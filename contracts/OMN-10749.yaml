---
schema_version: "1.0.0"
ticket_id: "OMN-10749"
title: "DI refactor: HandlerBaselineCompare injects HandlerBaselineCapture via optional param"
summary: >
  HandlerBaselineCompare now accepts optional capture_handler: HandlerBaselineCapture | None param. Falls
  back to constructing default with probe_registry when not injected. lifecycle-ok: optional-di-fallback
  pattern. Part of OMN-10841 DI Wave 2 (Bucket A #3).
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests-pass"
    description: "4 injection-path unit tests in test_di_injection_baseline_compare_capture_handler.py
      verify mock.assert_called_once() and is-identity."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10749/dod-unit-tests-pass/command.yaml\""
  - id: "dod-omnimarket-pr-690"
    description: "omnimarket PR #690 open with CI green at commit c821a0d5a1b20b18260e00be1d5a4d2da2a64cf1."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10749/dod-omnimarket-pr-690/command.yaml\"
          && grep -q '^pr_number: 690$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10749/dod-omnimarket-pr-690/command.yaml\""
  - id: "dod-occ-pr-1120"
    description: "onex_change_control PR #1120 carries the OMN-10749 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10749/dod-occ-pr-1120/command.yaml\"
          && grep -q '^pr_number: 1120$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10749/dod-occ-pr-1120/command.yaml\""
  - id: "dod-deploy"
    description: "No runtime deploy required; pure code seam refactor with no behaviour change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10749/dod-deploy/command.yaml\""
