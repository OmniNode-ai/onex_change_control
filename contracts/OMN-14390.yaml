---
schema_version: "1.0.0"
ticket_id: "OMN-14390"
title: "fix(OMN-14390): scope dod_verify PR-check gate to required checks only"
summary: >-
  omnimarket PR #1718 fixes node_dod_verify's live PR-state check so it asks GitHub for required checks
  only via gh pr checks --required before deciding whether a PR has merge-blocking red contexts. The change
  prevents stale or informational non-required checks from falsely blocking Done verification while keeping
  required check failures fail-closed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1718-head-7aadbaf5"
    description: >-
      Foreground Codex verification of omnimarket PR #1718 at head 7aadbaf5d242ecfb5ff348a77f01c7aa3a643a09.
      The exact OMN-14390 regression test passed and asserts _fetch_pr_checks_green invokes gh pr checks
      with --required, then treats the required-only all-green payload as green. A live seam probe against
      omnibase_infra#2232 returned only required contexts, all SUCCESS. Ruff check/format, mypy for the
      touched service, and file-scoped pre-commit passed with OMNIBASE_INFRA_PATH set. The broader focused
      test file still has one known unrelated OMN-14392 live-PR-state fixture failure, so it is not used
      as PASS evidence for this receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14390/dod-omnimarket-pr-1718-head-7aadbaf5/command.yaml
          && grep -q '^pr_number: 1718$' drift/dod_receipts/OMN-14390/dod-omnimarket-pr-1718-head-7aadbaf5/command.yaml
          && grep -q '7aadbaf5d242ecfb5ff348a77f01c7aa3a643a09' drift/dod_receipts/OMN-14390/dod-omnimarket-pr-1718-head-7aadbaf5/command.yaml
  - id: "dod-deploy-assessment-head-7aadbaf5"
    description: >-
      Deploy-surface assessment for omnimarket PR #1718 head 7aadbaf5d242ecfb5ff348a77f01c7aa3a643a09:
      the diff is limited to node_dod_verify evidence collection logic and a focused unit test. It does
      not register a new runtime workflow, mutate live deployment configuration, perform a deploy/restart,
      change topics, or touch .201.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14390/dod-deploy-assessment-head-7aadbaf5/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14390/dod-deploy-assessment-head-7aadbaf5/command.yaml
  - id: "dod-occ-self-commit"
    description: >-
      OCC self-binding: this onex_change_control branch carries the OMN-14390 central contract and PASS
      receipts for omnimarket PR #1718 head 7aadbaf5d242ecfb5ff348a77f01c7aa3a643a09. The receipt binds
      the first evidence commit for occ-preflight eligibility before the OCC PR number exists.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14390/dod-occ-self-commit/command.yaml && grep
          -q 'jonah/omn-14390-occ-market-1718' drift/dod_receipts/OMN-14390/dod-occ-self-commit/command.yaml
