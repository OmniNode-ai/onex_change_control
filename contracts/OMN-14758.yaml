---
schema_version: 1.0.0
ticket_id: OMN-14758
title: RuntimeDispatch delegation command topic wiring
summary: >-
  Central OCC evidence for omnibase_infra PR #2341, which wires delegation command topics onto RuntimeDispatch
  behind an empty-by-default allowlist without flipping the live canary.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - topics
  - protocols
  - public_api
evidence_requirements:
  - kind: ci
    description: Product-readiness and shell-hygiene gates pass at the exact product head.
    command: gh api repos/OmniNode-ai/omnibase_infra/actions/runs/${RUN_ID}
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-infra2341-head-6e0682bc
    description: omnibase_infra#2341 is open at exact product head 6e0682bc63802ef67d7c693001402f3f584200fb.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2341; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=6e0682bc63802ef67d7c693001402f3f584200fb;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-infra2341-diff-6e0682bc
    description: omnibase_infra#2341 has a non-empty reviewed RuntimeDispatch delegation wiring product
      diff.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2341; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q .'
  - id: dod-infra2341-product-readiness-6e0682bc
    description: product-readiness-shadow completed successfully for omnibase_infra#2341 at exact product
      head 6e0682bc63802ef67d7c693001402f3f584200fb.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29641231639; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=6e0682bc63802ef67d7c693001402f3f584200fb;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-infra2341-shell-hygiene-6e0682bc
    description: shell-hygiene gate completed successfully for omnibase_infra#2341 at exact product head
      6e0682bc63802ef67d7c693001402f3f584200fb.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29641231645; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=6e0682bc63802ef67d7c693001402f3f584200fb;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-infra2341-deploy-scope
    description: 'Deploy-scope proof for OMN-14758: the PR wires dormant RuntimeDispatch delegation routing
      behind ONEX_CORE_RUNTIME_TOPICS, whose default empty value preserves zero live behavior change;
      the canary flip is held for operator approval and this OCC lane performed no live deploy, restart,
      docker exec, rpk topic produce, or production mutation.'
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14758/dod-infra2341-deploy-scope/command.yaml
          && grep -qi ''default empty value preserves zero live behavior change'' drift/dod_receipts/OMN-14758/dod-infra2341-deploy-scope/command.yaml'
  - id: occ-self-bind-pr-4391
    description: 'OCC PR #4391 self-binding proof for OMN-14758 / omnibase_infra#2341.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
  - id: occ-self-bind-pr-4395
    description: 'OCC PR #4395 self-binding proof for OMN-14758: allowlists the S6 KafkaTransport freestanding
      Kafka client so the imperative-contract guard stays green once omnibase_infra#2341 wires the transport
      live.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
