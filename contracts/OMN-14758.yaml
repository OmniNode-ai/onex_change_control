---
schema_version: "1.0.0"
ticket_id: "OMN-14758"
title: "Allowlist S6 KafkaTransport guard on OCC main"
summary: >-
  Main-branch OCC hotfix for omnibase_infra#2341: allowlist the new src/omnibase_infra/event_bus/kafka_transport.py
  transport face for the existing freestanding Kafka-client guard class so the central Imperative Contract
  Guard remains precise.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Imperative contract guard passes against omnibase_infra#2341 with this allowlist."
    command: >-
      uv run check-imperative-contracts --repo-root /Users/jonah/Code/omni_home/omni_worktrees/OMN-14758/omnibase_infra
      --allowlists-dir allowlists --scan-freestanding
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-main-kafka-transport-allowlist"
    description: >-
      The main-branch allowlist contains exactly the S6 KafkaTransport file under the existing OMN-12751
      freestanding Kafka-client exception, and the local Imperative Contract Guard passes against omnibase_infra#2341
      with zero live violations.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'src/omnibase_infra/event_bus/kafka_transport.py' allowlists/omnibase_infra.yaml &&
          grep -q 'freestanding_kafka_client' allowlists/omnibase_infra.yaml
  - id: "occ-self-bind-pr-4394"
    description: "OCC main PR #4394 self-binding proof for OMN-14758."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - id: "dod-migration-inventory-093"
    description: >-
      The central migration inventory lists omnibase_infra forward migration 093_add_delegation_workflow_state_outbox_columns.sql,
      matching the live hosted inventory drift failure.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '093_add_delegation_workflow_state_outbox_columns.sql' src/onex_change_control/boundaries/migration_inventory.yaml
