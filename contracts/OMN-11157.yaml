---
schema_version: "1.0.0"
ticket_id: "OMN-11157"
title: "Flip file naming convention hook to blocking in omnibase_core"
summary: >
  Central OCC evidence for the OMN-11157 slice of omnibase_core PR #1106, which removes advisory bypass
  behavior from file naming convention checks.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Naming convention validation passes."
    command: "uv run python -m omnibase_core.validation.checker_naming_convention"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-file-naming-blocking"
    description: "File naming validation passes after the hook is made blocking."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11157/dod-file-naming-blocking/command.yaml\"
          && grep -q 'All files conform to naming conventions' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11157/dod-file-naming-blocking/command.yaml\""
