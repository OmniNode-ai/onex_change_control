---
schema_version: "1.0.0"
ticket_id: "OMN-12006"
title: "fix(OMN-12006): null model_name in public bifrost_delegation.yaml, resolved via contract overlay"
summary: >
  Remove hardcoded site-specific model IDs from bifrost_delegation.yaml in the public omnimarket repo.
  Local backend model_name values are set to null in the source; each deployment site provides them via
  ~/.omninode/delegation/bifrost_overrides.yaml, deep-merged by config_loader_bifrost_delegation.py at
  runtime.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "bifrost overlay loader tests pass with null model_name assertion"
    command: "cd omnimarket && uv run pytest tests/nodes/node_delegation_routing_reducer/test_bifrost_overlay_loader.py
      -v"
  - kind: "manual"
    description: "Overlay file written to .201 stability-test container; config loader resolves Qwen3.6-35B-A3B
      for local-qwen-coder-30b"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-853"
    description: >
      omnimarket PR #853 nulls model_name for all three local backends in bifrost_delegation.yaml, removes
      onex-allow-model-id annotations, bumps config_version to 1.4.0, and updates the test assertion to
      expect null. 5/5 bifrost overlay loader tests pass. All CI gates green except verify/verify (receipt
      gate — blocked pending this OCC PR).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'model_name: null' omnimarket/src/omnimarket/configs/bifrost_delegation.yaml"
