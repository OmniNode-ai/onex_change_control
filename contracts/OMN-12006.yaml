---
schema_version: "1.0.0"
ticket_id: "OMN-12006"
title: "Rewrite bifrost_delegation backends for current fleet with contract overlay"
summary: >
  Replaces site-specific model IDs and AIPC backend names in omnimarket's public bifrost_delegation.yaml
  with capability-named backends (local-coder, local-reasoner, local-heavy-reasoning, local-embedding)
  and null model_name fields resolved at deploy time via the bifrost_overrides.yaml overlay. Fixes a merge-conflict
  regression from PR #851 that reverted PR #856's capability-named backend renames.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "protocols"
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-853"
    description: >
      omnimarket PR #853 sets model_name to null for all local backends in bifrost_delegation.yaml so
      no site-specific model IDs appear in the public repo.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12006/dod-omnimarket-pr-853/command.yaml"
  - id: "dod-omnimarket-pr-856"
    description: >
      omnimarket PR #856 rewrites bifrost_delegation backends with capability-named IDs (local-coder,
      local-reasoner, local-heavy-reasoning, local-embedding) and updates routing_tiers.yaml to match.
      Tests updated for new backend IDs.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12006/dod-omnimarket-pr-856/command.yaml"
  - id: "dod-omnimarket-pr-871"
    description: >
      omnimarket PR #871 restores capability-named backends after PR #851 merged out-of-order and reverted
      PR #856. Capability names and null model_name values confirmed in main.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12006/dod-omnimarket-pr-871/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC evidence for OMN-12006."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12006/dod-occ-pr/command.yaml"
  - id: "dod-quality-gate-deploy"
    description: >
      omnimarket PR #888 strips thinking traces (<think>...</think>) from Qwen3.6/DeepSeek-R1 model responses
      before running quality gate checks. Verified locally on stability-test runtime after rebuild with
      PR #888 SHA. Receipt captures probe output.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12006/dod-quality-gate-deploy/command.yaml"
  - id: "dod-infra-pr-1749"
    description: >
      omnibase_infra PR #1749 — inject ContainerBackedDelegationDispatchPort at auto-wiring time; lazy-resolves
      DirectBridgeDelegationDispatchPort from DI container at first dispatch() call, falls back to RuntimeDelegationDispatchPort
      if bridge port is not registered.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnibase_infra --json state --jq '.state'"
