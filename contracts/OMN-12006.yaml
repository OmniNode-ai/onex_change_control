---
schema_version: "1.0.0"
ticket_id: "OMN-12006"
title: "Resolve local Bifrost model names from deployment overlay"
summary: >
  Central OCC evidence for omnimarket PR #853. Public omnimarket configuration now keeps deployment-specific
  local backend model_name values null, with host-specific model IDs supplied by the deployment overlay
  at ~/.omninode/delegation/bifrost_overrides.yaml. This prevents site-local model IDs from becoming authoritative
  source configuration.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-853"
    description: "omnimarket PR #853 carries the OMN-12006 public config null-model-name overlay fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12006/dod-omnimarket-pr-853/command.yaml"
  - id: "dod-overlay-deploy-assessment"
    description: >
      No repository-tracked deployment artifact is required. The runtime model IDs are deployment configuration
      supplied through ~/.omninode/delegation/bifrost_overrides.yaml on the target host.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12006/dod-overlay-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-1615"
    description: "onex_change_control PR #1615 carries the central OCC evidence for OMN-12006."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12006/dod-occ-pr-1615/command.yaml"
  - id: "dod-omnimarket-pr-856"
    description: "omnimarket PR #856 carries the OMN-12006 fleet backend rewrite with public null model_name
      overlay policy."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12006/dod-omnimarket-pr-856/command.yaml"
  - id: "dod-occ-pr-1622"
    description: "onex_change_control PR #1622 carries the OMN-12006 omnimarket PR #856 receipt repair."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12006/dod-occ-pr-1622/command.yaml"
