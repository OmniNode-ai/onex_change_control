---
schema_version: "1.0.0"
ticket_id: "OMN-12164"
title: "Fix datetime.now in omnibase_infra RSD score handler"
summary: >
  Central OCC evidence for omnibase_infra PR #1754. The infra change replaces naive datetime.now() time-decay
  arithmetic with timezone-aware UTC arithmetic in node_rsd_score_compute and updates the node contract
  to document UTC normalization for naive created_at timestamps.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused RSD priority engine unit tests pass for the UTC time-decay change."
    command: "See drift/dod_receipts/OMN-12164/dod-rsd-tests/command.yaml"
  - kind: "ci"
    description: "Ruff check passes for touched RSD score compute code and tests."
    command: "See drift/dod_receipts/OMN-12164/dod-ruff/command.yaml"
  - kind: "ci"
    description: "Contract Sync Gate replay passes with the handler and node contract changes."
    command: "See drift/dod_receipts/OMN-12164/dod-contract-sync/command.yaml"
  - kind: "ci"
    description: "The updated node_rsd_score_compute contract validates locally."
    command: "See drift/dod_receipts/OMN-12164/dod-node-contract-validation/command.yaml"
  - kind: "ci"
    description: "Deploy-gate posture is recorded for the runtime handler path touched by PR #1754."
    command: "See drift/dod_receipts/OMN-12164/dod-deploy-gate/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-rsd-tests"
    description: "Focused RSD priority engine tests passed in the omnibase_infra worktree."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12164/dod-rsd-tests/command.yaml
          && grep -q '40 passed' drift/dod_receipts/OMN-12164/dod-rsd-tests/command.yaml"
  - id: "dod-ruff"
    description: "Ruff check passed for the touched node package and focused RSD tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12164/dod-ruff/command.yaml && grep
          -q 'All checks passed' drift/dod_receipts/OMN-12164/dod-ruff/command.yaml"
  - id: "dod-contract-sync"
    description: "Contract Sync Gate replay passed after the handler and contract changed together."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12164/dod-contract-sync/command.yaml
          && grep -q 'OK: contract-sync gate passed' drift/dod_receipts/OMN-12164/dod-contract-sync/command.yaml"
  - id: "dod-node-contract-validation"
    description: "The updated node_rsd_score_compute contract validates with the repo validator."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12164/dod-node-contract-validation/command.yaml
          && grep -q 'Batch contract validation: 1/1 passed, 0 failed' drift/dod_receipts/OMN-12164/dod-node-contract-validation/command.yaml"
  - id: "dod-deploy-gate"
    description: "Runtime handler path deploy-gate evidence is recorded; no live pre-merge deploy was
      required."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12164/dod-deploy-gate/command.yaml
          && grep -q 'deploy-gate:' drift/dod_receipts/OMN-12164/dod-deploy-gate/command.yaml"
