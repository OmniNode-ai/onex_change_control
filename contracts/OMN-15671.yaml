---
schema_version: "1.0.0"
ticket_id: "OMN-15671"
title: "Autobind OCC evidence for OMN-15671"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_core PR #1540.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1540 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_core-pr-1540"
    description: "PR #1540 on OmniNode-ai/omnibase_core — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1540 --repo OmniNode-ai/omnibase_core --json number,state"
      - check_type: "command"
        check_value: "gh pr view 1540 --repo OmniNode-ai/omnibase_core --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #1540 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 1540 --repo OmniNode-ai/omnibase_core --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-deploy-falsifiable-omn15671-core1540"
    description: "Append-only replacement deploy-scope DoD for omnibase_core PR #1540 using exact-head
      gh-api content probes."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: >-
          SHA=c69cc5a50a0cf7045093d2f6f720da5eb1fdeeac; gh api "repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/cli/cli_run_node.py?ref=${SHA}"
          --jq .content > /tmp/omn-15671-cli.b64 && gh api "repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/runtime/runtime_local.py?ref=${SHA}"
          --jq .content > /tmp/omn-15671-runtime.b64 && base64 -d < /tmp/omn-15671-cli.b64 > /tmp/omn-15671-cli.py
          && base64 -d < /tmp/omn-15671-runtime.b64 > /tmp/omn-15671-runtime.py && python3 -c 'from pathlib
          import Path; cli = Path("/tmp/omn-15671-cli.py").read_text(); runtime = Path("/tmp/omn-15671-runtime.py").read_text();
          required_cli = ["def derive_run_node_group_id", "derive_service_group_id(", "\"group.id\": derive_run_node_group_id(correlation_uuid)"];
          required_runtime = ["def derive_runtime_local_group_id", "RUNTIME_LOCAL_SERVICE: Final[str]
          = \"omnibase_core\"", "TERMINAL_CONSUMER_NODE_NAME: Final[str] = \"runtime_local_terminal\"",
          "group_id=derive_runtime_local_group_id(TERMINAL_CONSUMER_NODE_NAME)", "group_id=derive_runtime_local_group_id(entry.handler_name)"];
          forbidden_runtime = ["group_id=\"runtime-local-terminal\"", "group_id=f\"runtime-local-{entry.handler_name}\""];
          missing = [marker for marker in required_cli if marker not in cli] + [marker for marker in required_runtime
          if marker not in runtime] + [f"forbidden:{marker}" for marker in forbidden_runtime if marker
          in runtime]; raise SystemExit(1 if missing else 0)' && echo "OMN-15671-DEPLOY-PROBE-PASS: exact-head
          gh-api content confirms derived consumer group ids for cli_run_node and RuntimeLocal"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_core-pr-1540"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5980"
    description: "Binds OMN-15671 to OCC companion PR #5980 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5980 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-6018"
    description: "Binds OMN-15671 to OCC repair PR #6018 (self-bind for OCC eligibility)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6018 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid"
