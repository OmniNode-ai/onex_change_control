---
schema_version: "1.0.0"
ticket_id: "OMN-17182"
title: "Autobind OCC evidence for OMN-17182"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2222
  and OmniNode-ai/omnibase_infra PR #3035. Both product PRs carry the same ticket, so their separately-minted
  companions collided add/add on this file; resolved as the union of both evidence sets, no entry from
  either side dropped.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2222 and PR #3035 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      OWED, not claimed. Neither PR #2222 on OmniNode-ai/omnimarket nor PR #3035 on OmniNode-ai/omnibase_infra changes a pytest target, so this producer cannot derive a behavior-class check from either diff and mints none - the changed surfaces (.github/workflows/linear-demotion-guard.yml, .github/workflows/linear-done-guard.yml, src/omnimarket/nodes/node_linear_triage/demotion_guard_cli.py, src/omnimarket/nodes/node_linear_triage/guard_cli.py, .github/workflows/linear-done-guard-armed.yml) are unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2222"
    description: "PR #2222 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2222 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2222/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/linear-demotion-guard.yml'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2222 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2222/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-2222"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7703"
    description: "Binds OMN-17182 to OCC companion PR #7703 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7703 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3035"
    description: "PR #3035 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3035 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3035/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/linear-done-guard-armed.yml'
  - id: "occ-self-bind-pr-7702"
    description: "Binds OMN-17182 to OCC companion PR #7702 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7702 --repo OmniNode-ai/onex_change_control --json number,state"
