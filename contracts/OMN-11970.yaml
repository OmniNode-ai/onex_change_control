---
schema_version: "1.0.0"
ticket_id: "OMN-11970"
title: "Clean hardcoded model IDs from omniclaude tests and fix transition_selector runtime violation"
summary: >
  Central OCC evidence for omniclaude PR #1653, which replaces hardcoded model ID strings in omniclaude
  test assertions with symbolic constants and fixes the node_transition_selector_effect runtime violation
  where "qwen3-14b" was hardcoded in a chat completion payload.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1653"
    description: "omniclaude PR #1653 carries hardcoded model ID cleanup and transition_selector runtime
      fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11970/dod-omniclaude-pr-1653/command.yaml
          && grep -q '^pr_number: 1653$' drift/dod_receipts/OMN-11970/dod-omniclaude-pr-1653/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Deploy gate assessment for node_transition_selector_effect runtime fix — model name
      now resolved via LocalLlmEndpointRegistry."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11970/dod-deploy-assessment/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11970/dod-deploy-assessment/command.yaml"
