---
schema_version: "1.0.0"
ticket_id: "OMN-13056"
title: "SEA judge docs finalization: pin 010cc35, Track B definition, ConnectError + cost caveats, untrack
  .pyc"
summary: >-
  Night-plan P6 (2026-06-11) SEA finalization docs PR on onex-self-extending-agent dev. Closes the four
  documentation-friction items that persisted at dev tip 010cc35 (TOTAL_ARCHITECTURE_FINDINGS.md section
  2.7): (1) JUDGE_REPRODUCE.md pins the reproduction commit to 010cc356e82008ec81a16601f305486e51a21ed8
  (#233), noting the proven fresh-clone SHA 8431277 is code-identical (docs-only delta); (2) Track B gets
  a real definition at its only README mention (node_track_b_routing_compute, bus-wired COMPUTE node consuming
  the omnimarket routing decision, fail-closed without a local route); (3) expected ConnectError lines
  during cheapest-first escalation are documented with the code-backed mechanism (_build_failed_result_from_exception,
  src/delegation/executor.py) and the local tier is framed as "cheapest-first escalation observed live",
  not a served endpoint; (4) the --agent Run Summary "Cost: $0.0000" is documented as a hardcoded "cost_inference_usd":
  0.0 literal in src/__main__.py — never measured, excluded from cost narratives. Wording bars enforced:
  event chain = post-hoc replayable event ledger (not a real-time trace); Tier 3 hosted watch path = DEGRADED/HIDDEN
  (live probe 2026-06-12T06:54:15Z: tailscale serve/funnel both "No serve config" on the host; OMN-12744
  reopened). Also untracks the 44 committed .pyc files (.gitignore already covers them). Docs + index
  only; zero src/tests content changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-234"
    description: >-
      onex-self-extending-agent PR #234 (base=dev, head 92d4b40e0223c7ea7f89204404ecc7754fb9070e) lands
      the six P6 items: SHA pin, Track B definition, expected-ConnectError note, $0.0000 caveat, wording
      bars (post-hoc ledger; Tier 3 DEGRADED), and the .pyc untrack. Local verification on the branch:
      ruff format/check clean; PYTHONDONTWRITEBYTECODE=1 uv run pytest tests/ -v (no filter) -> 1289 passed,
      16 skipped; uv run mypy src/ --strict --ignore-missing-imports -> no issues in 162 files; pre-commit
      run --all-files -> all hooks passed (incl. SEA inference architecture guard, unit + integration
      tests). Probe re-run on the changed docs files recorded in the receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13056/dod-sea-pr-234/command.yaml"
          && grep -q '^ticket_id: "OMN-13056"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13056/dod-sea-pr-234/command.yaml"
          && grep -q '^pr_number: 234$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13056/dod-sea-pr-234/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      This OCC PR carries the central OMN-13056 contract and the dod-sea-pr-234 receipt that the onex-self-extending-agent
      docs PR #234 Receipt-Gate resolves via Evidence-Source: OCC#<this-pr>.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13056/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-13056"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13056/dod-occ-pr/command.yaml"
