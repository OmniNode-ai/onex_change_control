---
schema_version: "1.0.0"
ticket_id: "OMN-11301"
title: "Pricing manifest abstraction unwired"
summary: >-
  Bind OCC evidence for omnimarket PR #724, which wires delegation orchestrator pricing through the canonical
  pricing manifest instead of hardcoded Claude token-price constants and stamps pricing_manifest_version
  on emitted events.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #724 passes CI, deploy gate, and receipt gate without bypass tokens."
    command: "gh pr checks 724 --repo OmniNode-ai/omnimarket"
dod_evidence:
  - id: "dod-omnimarket-pr-724-head"
    description: "omnimarket PR #724 head carries the manifest-backed pricing implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11301/dod-omnimarket-pr-724-head/command.yaml\"
          && grep -q '^pr_number: 724$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11301/dod-omnimarket-pr-724-head/command.yaml\"
          && grep -q '^commit_sha: \"e6e2c2eb27bd83aa2f538b3ae48a7cc6b9071fcd\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11301/dod-omnimarket-pr-724-head/command.yaml\""
  - id: "dod-focused-tests"
    description: "Focused delegation pricing tests pass and no hardcoded token-price constants remain
      in the touched handlers."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11301/dod-focused-tests/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Runtime deploy assessment is explicitly captured for the handler-path change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11301/dod-deploy-assessment/command.yaml\"
          && grep -q 'deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11301/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11301 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11301/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
