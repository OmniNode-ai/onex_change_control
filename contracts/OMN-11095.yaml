---
schema_version: "1.0.0"
ticket_id: "OMN-11095"
title: "Prevent delegation intent terminal topic fallback"
summary: >
  Central OCC evidence for omnibase_infra PR #1615. Intermediate delegation intent payloads (routing,
  inference, quality-gate, baseline, remote-agent invocation) now route to their per-class command topics
  instead of falling back to the terminal delegation-completed topic. Terminal ModelDelegationResult payloads
  still publish to the configured fallback topic. Unit + integration coverage proves both behaviors.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused dispatch-result topic routing unit + integration tests pass."
    command: "See drift/dod_receipts/OMN-11095/dod-focused-tests/command.yaml"
  - kind: "ci"
    description: "Touched dispatch-result applier and topic-routing test files pass ruff."
    command: "See drift/dod_receipts/OMN-11095/dod-lint/command.yaml"
  - kind: "ci"
    description: "Touched dispatch-result applier passes mypy --strict."
    command: "See drift/dod_receipts/OMN-11095/dod-mypy/command.yaml"
  - kind: "ci"
    description: "omnibase_infra PR #1615 carries the implementation, focused tests, integration coverage,
      and OMN-11095 ticket contract."
    command: "See drift/dod_receipts/OMN-11095/dod-infra-pr-1615/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused dispatch-result applier unit + integration tests passed (21 tests)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '21 passed' drift/dod_receipts/OMN-11095/dod-focused-tests/command.yaml"
  - id: "dod-lint"
    description: "Ruff check passed for touched dispatch-result applier + tests."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'All checks passed' drift/dod_receipts/OMN-11095/dod-lint/command.yaml"
  - id: "dod-mypy"
    description: "mypy --strict passed for the dispatch-result applier."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'no issues found' drift/dod_receipts/OMN-11095/dod-mypy/command.yaml"
  - id: "dod-infra-pr-1615"
    description: "omnibase_infra PR #1615 carries the OMN-11095 implementation and ticket contract."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11095/dod-infra-pr-1615/command.yaml
          && grep -q '^pr_number: 1615$' drift/dod_receipts/OMN-11095/dod-infra-pr-1615/command.yaml"
  - id: "dod-occ-pr-1053"
    description: "Self-binding receipt for OCC PR #1053 — anchors evidence chain to this OCC PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'status: PASS' drift/dod_receipts/OMN-11095/dod-occ-pr-1053/command.yaml && grep -q
          'pr_number: 1053' drift/dod_receipts/OMN-11095/dod-occ-pr-1053/command.yaml
