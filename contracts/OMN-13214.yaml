---
schema_version: "1.0.0"
ticket_id: "OMN-13214"
title: "Close merge-queue armed-but-not-enqueued gap"
summary: >-
  OMN-13214 hardens omnimarket auto-merge behavior after the 2026-06-17 dev merge wave exposed that GitHub
  auto-merge arming is not equivalent to entering a merge queue. The omnimarket PR adds a stdlib-only
  helper with unit-tested queue classification/verification behavior and updates .github/workflows/auto-merge.yml
  to arm auto-merge, update behind branches when strict-update requires it, explicitly enqueue via GraphQL,
  and verify the PR actually enters the merge queue. A follow-up fix in the same PR fetches queue-only
  PullRequest fields through GraphQL because this runner's gh pr view JSON surface does not include isInMergeQueue.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "pr-omnimarket"
    description: >-
      omnimarket PR #1253 adds merge queue enqueue/verification enforcement to the auto-merge workflow
      and 32 focused unit tests for the queue decision helper. The PR head also fixes the workflow to
      read isInMergeQueue via GraphQL rather than unsupported gh pr view fields.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13214/pr-omnimarket/command.yaml && grep -q
          '^pr_number: 1253$' drift/dod_receipts/OMN-13214/pr-omnimarket/command.yaml && grep -q '^commit_sha:
          "4ef5a762625ae0af837394ec01f80b24887d63b5"$' drift/dod_receipts/OMN-13214/pr-omnimarket/command.yaml
  - id: "pr-occ-2717"
    description: >-
      OCC self-binding receipt for the central evidence PR. This gives the receipt gate a PASS receipt
      tied directly to onex_change_control PR #2717.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13214/pr-occ-2717/command.yaml && grep -q '^pr_number:
          2717$' drift/dod_receipts/OMN-13214/pr-occ-2717/command.yaml && grep -q '^commit_sha: "f7a7ab24854753f617aaa0fd6cce7205ed5817ab"$'
          drift/dod_receipts/OMN-13214/pr-occ-2717/command.yaml
  - id: "pr-occ-2720"
    description: >-
      OCC self-binding receipt for the dev->main promotion PR #2720. Ties the OMN-13214 evidence directly
      to onex_change_control PR #2720 so this promotion PR satisfies its own OCC eligibility check.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13214/pr-occ-2720/command.yaml && grep -q '^pr_number:
          2720$' drift/dod_receipts/OMN-13214/pr-occ-2720/command.yaml
