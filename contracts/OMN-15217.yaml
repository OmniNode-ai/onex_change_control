---
schema_version: "1.0.0"
ticket_id: "OMN-15217"
title: "Autobind OCC evidence for OMN-15217"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2494."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2494 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2494"
    description: "PR #2494 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2494-ci"
    description: "PR #2494 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5109"
    description: "OCC companion PR #5109 — self-bind for OMN-15217 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnibase-infra-pr-2494-deploy-head-08ad2bc4"
    description: "Falsifiable deploy probe for omnibase_infra PR #2494 at reachable head 08ad2bc4c44bc11bb1a9d45dd471dee15e9df588.
      OMN-15217 changes what the runtime image ships and what Docker executes as its health verdict. This
      deploy evidence asserts that the remotely reachable product head carries the semantic check end
      to end: the stability lane invokes the strict check on the two runtime containers changed at this
      head; both changed runtime containers disarm autoheal via `labels: !override`; Dockerfile.runtime
      installs the check at the path compose invokes; publisher and consumer agree on the details.runtime_health
      seam key. Count assertions use `test \"$(...)\" = \"N\"` so the exit status comes from the comparison,
      not from a successful GitHub API read. No live deploy, restart, lane mutation, Kafka mutation, database
      mutation or production mutation is performed by this evidence lane."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.stability-test.yml?ref=08ad2bc4c44bc11bb1a9d45dd471dee15e9df588'
          --jq .content | base64 -d | grep -c '^ *- /usr/local/bin/onex-container-healthcheck$')" = "2"
          && test "$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.stability-test.yml?ref=08ad2bc4c44bc11bb1a9d45dd471dee15e9df588'
          --jq .content | base64 -d | grep -c '^ *labels: !override$')" = "2" && gh api 'repos/OmniNode-ai/omnibase_infra/contents/docker/Dockerfile.runtime?ref=08ad2bc4c44bc11bb1a9d45dd471dee15e9df588'
          --jq .content | base64 -d | grep -q '/usr/local/bin/onex-container-healthcheck' && gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/health/runtime_health_block.py?ref=08ad2bc4c44bc11bb1a9d45dd471dee15e9df588'
          --jq .content | base64 -d | grep -q 'RUNTIME_HEALTH_DETAIL_KEY = "runtime_health"' && gh api
          'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/health/container_healthcheck.py?ref=08ad2bc4c44bc11bb1a9d45dd471dee15e9df588'
          --jq .content | base64 -d | grep -q 'RUNTIME_HEALTH_DETAIL_KEY = "runtime_health"'
  - id: "occ-self-bind-pr-5134"
    description: "OCC repair PR #5134 — self-bind for OMN-15217 deploy evidence repair (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5134 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omnibase-infra-pr-2494-deploy-head-4f8e6d5e"
    description: "Corrective deploy probe for omnibase_infra PR #2494 at its MERGED head 4f8e6d5ebce4d48aa8eec9287b11fef5f283d507.
      This entry supersedes the attestation recorded by dod-omnibase-infra-pr-2494-deploy-head-08ad2bc4,
      which pinned a superseded head: that probe is true at 08ad2bc4 but attests the PRE-fix configuration
      of the very artifacts OMN-15217 changes. At 08ad2bc4 only two of the stability lane's three runtime
      containers carried the strict semantic check — runtime-worker was still on the shallow `curl -sf`
      probe with `autoheal=true` — so the counts were 2, and the deploy evidence for OMN-15217 recorded
      the two-of-three configuration as proven. Commit 4f8e6d5e closed that gap; both counts are 3 at
      the merged head. This entry re-pins every ref to 4f8e6d5e, corrects both count assertions to 3,
      and adds an awk-scoped clause asserting the strict check inside the runtime-worker service block
      specifically (OMN-15142 finding: an unscoped `grep -c` matches anywhere in the file, so a bare count
      of 3 can be satisfied by the wrong three services). Count assertions use `test \"$(...)\" = \"N\"`
      so the exit status comes from the comparison, not from a successful GitHub API read. RED proven,
      not assumed: the six-clause form exits 0 at 4f8e6d5e and exits 1 at 08ad2bc4. Read-only GitHub contents
      API only — no live deploy, restart, lane mutation, Kafka mutation, database mutation or production
      mutation is performed by this evidence lane."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.stability-test.yml?ref=4f8e6d5ebce4d48aa8eec9287b11fef5f283d507'
          --jq .content | base64 -d | grep -c '^ *- /usr/local/bin/onex-container-healthcheck$')" = "3"
          && test "$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.stability-test.yml?ref=4f8e6d5ebce4d48aa8eec9287b11fef5f283d507'
          --jq .content | base64 -d | grep -c '^ *labels: !override$')" = "3" && gh api 'repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.stability-test.yml?ref=4f8e6d5ebce4d48aa8eec9287b11fef5f283d507'
          --jq .content | base64 -d | awk '/^  runtime-worker:/{f=1;next} /^  [^ ]/{f=0} f' | grep -q
          '^ *- /usr/local/bin/onex-container-healthcheck$' && gh api 'repos/OmniNode-ai/omnibase_infra/contents/docker/Dockerfile.runtime?ref=4f8e6d5ebce4d48aa8eec9287b11fef5f283d507'
          --jq .content | base64 -d | grep -q '/usr/local/bin/onex-container-healthcheck' && gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/health/runtime_health_block.py?ref=4f8e6d5ebce4d48aa8eec9287b11fef5f283d507'
          --jq .content | base64 -d | grep -q 'RUNTIME_HEALTH_DETAIL_KEY = "runtime_health"' && gh api
          'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/health/container_healthcheck.py?ref=4f8e6d5ebce4d48aa8eec9287b11fef5f283d507'
          --jq .content | base64 -d | grep -q 'RUNTIME_HEALTH_DETAIL_KEY = "runtime_health"'
  - id: "occ-self-bind-pr-5141"
    description: "OCC corrective PR #5141 — self-bind for the OMN-15217 deploy-evidence re-pin to merged
      head 4f8e6d5e (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5141 --repo OmniNode-ai/onex_change_control --json number,state"
