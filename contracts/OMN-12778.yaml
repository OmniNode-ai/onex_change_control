---
schema_version: "1.0.0"
ticket_id: "OMN-12778"
title: "Wave 1A — retire legacy in-process sea-demo.* node archetype"
summary: >-
  Retires the legacy in-process node archetype (src/nodes/*) in onex-self-extending-agent that used sea-demo.*
  topics (DEMO_* constants). The canonical Kafka path (src/agent/contract.yaml + GenerationConsumer emitting
  onex.evt.omnimarket.node-generation-completed.v1) is untouched. Adds a guard test asserting canonical
  emit topic == projection subscribe topic and no legacy archetype imports in demo entrypoints.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-215-merged"
    description: >-
      onex-self-extending-agent PR #215 retires src/nodes/* legacy archetype, removes DEMO_* topic constants,
      adds guard test test_canonical_topic_coherence.py; 1252 unit tests pass on the rebuilt dev-based
      branch.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12778/dod-sea-pr-215-merged/command.yaml &&
          grep -q '^pr_number: 215$' drift/dod_receipts/OMN-12778/dod-sea-pr-215-merged/command.yaml &&
          grep -q 'c9625cd2' drift/dod_receipts/OMN-12778/dod-sea-pr-215-merged/command.yaml
  - id: "dod-guard-test-passes"
    description: >-
      Guard test test_canonical_topic_coherence.py passes: no DEMO_* constants in pipeline/topics.py,
      no sea-demo.* topic strings, canonical GENERATION_COMPLETED_TOPIC matches agent/contract.yaml publish_topics,
      no legacy node imports in demo entrypoints.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12778/dod-guard-test-passes/command.yaml &&
          grep -q '1252 passed' drift/dod_receipts/OMN-12778/dod-guard-test-passes/command.yaml
  - id: "dod-occ-pr"
    description: "onex_change_control PR carrying this contract and receipt."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12778/dod-occ-pr/command.yaml
