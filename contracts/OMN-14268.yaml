---
schema_version: "1.0.0"
ticket_id: "OMN-14268"
title: "refactor(OMN-14268): canonicalize HandlerCandidatePool to thin contract+handler"
summary: >-
  omnimarket product PR (branch jonah/omn-14268-lock-candidate-pool-thin-shape, head b3e035839fa562c15f9d51c36e608f6d01162ffb)
  adds a shape-contract test locking HandlerCandidatePool (node_candidate_pool_compute) to the thin contract+handler
  shape: handle(self, request) takes a single typed ModelCandidatePoolRequest and returns ModelCandidatePoolResult
  directly. Test-only change; handler already canonical. Public-safe generic architecture-hygiene work
  under epic OMN-14242.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      Independent landing verification at product head b3e035839fa562c15f9d51c36e608f6d01162ffb: node
      tests, mypy --strict on the handler, and the strict state-coverage gate all pass. Runner (implementer)
      is distinct from the verifier (landing verification), satisfying the receipt-honesty gate.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14268/dod-001/command.yaml && grep -q 'b3e035839fa562c15f9d51c36e608f6d01162ffb'
          drift/dod_receipts/OMN-14268/dod-001/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Honest deploy-surface assessment: HandlerCandidatePool has no production callers today outside its
      node contract.yaml and tests. Pure in-module signature change; no live deploy required or performed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14268/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-14268/dod-deploy-assessment/command.yaml
  - id: "dod-occ-self"
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14268 contract and
      PASS receipts consumed by the product PR through its Evidence-Source line.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14268/dod-occ-self/command.yaml
