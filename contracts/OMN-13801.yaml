---
schema_version: "1.0.0"
ticket_id: "OMN-13801"
title: "OmniCursor core hook contracts and agent source enum"
summary: >
  omnibase_core PR #1372 replaces the orphan OmniCursor draft PR #1332 with an OMN-bound branch and adds
  the shared Cursor hook contract surface: EnumAgentSource, EnumCursorHookEventType, ModelCursorHookEvent,
  and ModelCursorHookEventPayload. The change is additive and supplies the W1 backend contract prerequisite
  for the OmniCursor dispatcher receive path.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
  - "envelopes"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr-3443-binding"
    description: >
      onex_change_control PR #3443 carries the OMN-13801 central contract and downstream receipt evidence
      required by omnibase_core PR #1372 Receipt Gate.
    source: "manual"
    status: verified
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13801/dod-occ-pr-3443-binding/command.yaml
          && grep -q '^pr_number: 3443$' drift/dod_receipts/OMN-13801/dod-occ-pr-3443-binding/command.yaml"
  - id: "dod-omnibase-core-cursor-hook-contracts-pr-1372"
    description: >
      omnibase_core PR #1372 is the OMN-13801-bound replacement for draft PR #1332. Local validation proved
      the new enum/model imports, package re-exports, ruff compliance, and the focused EnumAgentSource
      test suite with PYTHONPATH unset so the worktree package was tested.
    source: "manual"
    status: verified
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13801/dod-omnibase-core-cursor-hook-contracts-pr-1372/command.yaml
          && grep -q '^pr_number: 1372$' drift/dod_receipts/OMN-13801/dod-omnibase-core-cursor-hook-contracts-pr-1372/command.yaml
          && grep -q '3b1e10a524c5e23ff3f7f9e43199c01bf768db8e' drift/dod_receipts/OMN-13801/dod-omnibase-core-cursor-hook-contracts-pr-1372/command.yaml"
