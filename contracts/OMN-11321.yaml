---
schema_version: "1.0.0"
ticket_id: OMN-11321
title: "Bug: omnimemory CI shards omit pytest-asyncio after dependency cascade"
summary: "Carry central OCC evidence for the omnimemory CI shard repair that adds pytest-asyncio to the
  isolated uv run test environment so async tests and pytest asyncio configuration are loaded during dependency
  cascade validation."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Representative omnimemory async test reproduces without pytest-asyncio and passes with
      it"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11321/dod-local-async-repro/command.yaml"
  - kind: "manual"
    description: "Downstream omnimemory PR carrying the CI shard fix is open"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11321/dod-omnimemory-pr/command.yaml"
  - kind: "manual"
    description: "OCC PR carrying the central OMN-11321 evidence is open"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11321/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-local-async-repro"
    description: "Representative omnimemory async test reproduces without pytest-asyncio and passes with
      it"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11321/dod-local-async-repro/command.yaml
          && grep -q '^ticket_id: OMN-11321$' drift/dod_receipts/OMN-11321/dod-local-async-repro/command.yaml
          && grep -q '^evidence_item_id: dod-local-async-repro$' drift/dod_receipts/OMN-11321/dod-local-async-repro/command.yaml"
  - id: "dod-omnimemory-pr"
    description: "Downstream omnimemory PR carrying the CI shard fix is open"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11321/dod-omnimemory-pr/command.yaml
          && grep -q '^ticket_id: OMN-11321$' drift/dod_receipts/OMN-11321/dod-omnimemory-pr/command.yaml
          && grep -q '^evidence_item_id: dod-omnimemory-pr$' drift/dod_receipts/OMN-11321/dod-omnimemory-pr/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carrying the central OMN-11321 evidence is open"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11321/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: OMN-11321$' drift/dod_receipts/OMN-11321/dod-occ-pr/command.yaml && grep
          -q '^evidence_item_id: dod-occ-pr$' drift/dod_receipts/OMN-11321/dod-occ-pr/command.yaml"
