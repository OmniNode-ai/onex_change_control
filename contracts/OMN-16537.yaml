---
schema_version: "1.0.0"
ticket_id: "OMN-16537"
title: "OCC companion for OmniNode-ai/omnibase_infra#2894 (OMN-16537)"
summary: >
  Fixes a dev-tip regression discovered while landing OMN-16532: OMN-16307's docs/runbooks KB-thinning
  rewrote docs/runbooks/gateway-lane-deploy.md to a taxonomy pointer, removing the prose test_help_rollback_matches_runbook_guidance
  (tests/scripts/test_deploy_gateway.py) diffed against, which broke Tests (Split 1/2) / CI Tests Gate
  / Test-Failure Ratchet Gate / CI Summary on every PR reaching that job. Updates the test to assert the
  KB pointer is intact instead -- the same pattern omniclaude applied for its own KB migration (OMN-16306,
  "update charter invariant test for KB pointer content"). No coverage lost: --help's own self-consistency
  stays independently covered by test_help_rollback_reads_registry_command_not_hand_reconstructed in the
  same file.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2894 CI checks green"
    command: "gh pr checks 2894 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_infra-pr-2894-kb-pointer"
    description: "tests/scripts/test_deploy_gateway.py asserts the KB-pointer text instead of the removed
      runbook prose."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/tests/scripts/test_deploy_gateway.py?ref=ff42e60c30286e12b2a5ca8a9e76c624bf462e2b' --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF 'Full documentation'
