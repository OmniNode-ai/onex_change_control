---
schema_version: "1.0.0"
ticket_id: "OMN-12718"
title: "Persist useful SEA artifact proof for bus-backed --agent"
summary: >-
  Central evidence for onex-self-extending-agent PR #213. Clean SEA dev now resolves omnimarket from the
  dev lane so served Qwen3.6 27B context metadata is available without venv hotpatching, and the SEA CLI
  completion event persists contract-valid scaffold artifact identity instead of treating an empty ADK
  final message as useful proof.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-sea-tests"
    description: "Focused SEA CLI regression tests, lint, lock check, and clean dependency probe passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-focused-sea-tests/command.yaml"
          && grep -q '44 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-focused-sea-tests/command.yaml"
          && grep -q 'ctx_27b 24576' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-focused-sea-tests/command.yaml"
  - id: "dod-live-useful-artifact-agent-proof"
    description: "Live .201 stability-test bus-backed SEA --agent run produced useful artifact evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-live-useful-artifact-agent-proof/command.yaml"
          && grep -q '56434d06-3494-426f-b1e6-432a5406656d' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-live-useful-artifact-agent-proof/command.yaml"
          && grep -q 'StatusHealthChecker' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-live-useful-artifact-agent-proof/command.yaml"
          && grep -q 'contract_passed=true' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-live-useful-artifact-agent-proof/command.yaml"
  - id: "dod-sea-pr-213"
    description: "onex-self-extending-agent PR #213 carries the dependency alignment and useful-artifact
      completion payload fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-sea-pr-213/command.yaml"
          && grep -q '^pr_number: 213$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-sea-pr-213/command.yaml"
          && grep -q 'b7765367b9516c8b8125a44c41dc395813bb606b' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-sea-pr-213/command.yaml"
  - id: "dod-occ-pr-2202"
    description: "onex_change_control PR #2202 carries the OMN-12718 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-occ-pr-2202/command.yaml"
          && grep -q '^pr_number: 2202$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-occ-pr-2202/command.yaml"
          && grep -q '4f35d956a43a6aca4453076f884174e8066daed4' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12718/dod-occ-pr-2202/command.yaml"
