---
schema_version: "1.0.0"
ticket_id: "OMN-13727"
title: "test(omnimarket): golden-chain secret fail-closed coverage for node_slack_publish_effect"
summary: >
  T3 coverage audit for node_slack_publish_effect (OMN-13723). Case (b) — secret fail-closed when SLACK_BOT_TOKEN
  is absent from the secret store → RuntimeError raised, no HTTP POST attempted — was missing from the
  21 tests merged in PR #1515. This PR (omnimarket #1526) adds TestSlackPublishSecretFailClosed.test_missing_token_raises_runtime_error_no_post
  to the existing golden-chain test module. 22/22 unit tests pass; no node, contract, runtime, event-bus
  topic, REST endpoint, or live deploy change is introduced.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >
      22 golden-chain/unit tests covering all four T3-required cases including the new secret fail-closed
      path.
    command: "uv run pytest tests/test_golden_chain_slack_publish_effect.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-slack-publish-coverage"
    description: >
      omnimarket PR #1526 adding test_missing_token_raises_runtime_error_no_post. 22/22 unit tests pass
      in tests/test_golden_chain_slack_publish_effect.py; ruff format+check clean; all four T3-required
      cases now covered.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13727/dod-omnimarket-slack-publish-coverage/command.yaml
          && grep -q '22/22 unit tests' drift/dod_receipts/OMN-13727/dod-omnimarket-slack-publish-coverage/command.yaml
  - id: "dod-occ-pr-self"
    description: >
      Self-binding OCC eligibility receipt for this onex_change_control PR carrying the OMN-13727 contract
      onto dev.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13727/dod-occ-pr-self/command.yaml &&
          grep -Eq '^pr_number: [0-9]+$' drift/dod_receipts/OMN-13727/dod-occ-pr-self/command.yaml
  - id: "dod-deploy-assessment"
    description: >
      Test-only PR; no new node, contract, runtime, event-bus topic, REST endpoint, or live deploy performed.
      No deploy action required; no .201 mutation, no container restart, no runtime topology change. Normal
      CI merge pipeline applies.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13727/dod-deploy-assessment/command.yaml
          && grep -q 'No deploy action required' drift/dod_receipts/OMN-13727/dod-deploy-assessment/command.yaml
