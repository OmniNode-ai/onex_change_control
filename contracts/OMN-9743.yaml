---
schema_version: "1.0.0"
ticket_id: "OMN-9743"
title: "Resolve package hook activation declarations into hook bitmasks"
summary: >
  Adds HookActivationResolver in omnibase_infra to compute an ONEX hook bitmask from ModelHookActivation
  declarations, setting bits only for enabled_by_default activations and skipping malformed activations
  under the lenient rollout-safety policy. Adds focused unit coverage and integration coverage proving
  validated hook activation contracts resolve into a runtime mask consumable by hook_enabled().
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-hook-resolver-tests"
    description: "Focused HookActivationResolver unit and integration tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9743/dod-hook-resolver-tests/command.yaml\""
  - id: "dod-hook-resolver-lint"
    description: "Focused ruff check and format check pass for HookActivationResolver and tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9743/dod-hook-resolver-lint/command.yaml\""
  - id: "dod-precommit"
    description: "Pre-commit hooks pass for the OMN-9743 integration coverage commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9743/dod-precommit/command.yaml\""
  - id: "dod-pr-1536-head"
    description: "Bind OMN-9743 evidence to omnibase_infra PR #1536 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9743/dod-pr-1536-head/command.yaml\""
  - id: "dod-occ-pr-818"
    description: "Bind this central OCC evidence update to onex_change_control PR #818."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9743/dod-occ-pr-818/command.yaml\""
