---
schema_version: "1.0.0"
ticket_id: "OMN-13347"
title: "refactor(omnimarket): split hook prober parsing branches"
summary: >-
  omnimarket PR #1286 decomposes the environment-health scanner hook prober parsing path into focused
  private helper branches for missing log directories, daily-summary parsing, malformed summaries, JSONL
  fallback, error-rate classification, violation log counting, and finding construction. The change keeps
  the public prober behavior intact while making the runtime handler path easier to verify and extending
  focused golden-chain coverage around FAIL thresholds and malformed/missing input cases.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1286"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1286 (jonah/omn-13347-hook-prober-refactor, base=dev).
      Splits hook health probing into focused helper branches and adds targeted branch coverage for FAIL
      thresholds, daily summary format, malformed summaries, JSONL fallback, and missing log files. Binds
      OMN-13347 to omnimarket PR #1286 by pr_number and commit_sha.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13347/dod-omnimarket-pr-1286/command.yaml
  - id: "dod-deploy-no-runtime-impact"
    description: >-
      Deploy-path DoD evidence for OMN-13347. The change touches the runtime handler path src/omnimarket/nodes/node_environment_health_scanner/handlers/prober_hooks.py,
      but it is a focused refactor plus tests: it changes no runtime node registration, no event-bus topic,
      no deployment manifest, no Dockerfile, and no migration. The corrected code reaches runtime on the
      next normal dev promotion/image rebuild; no separate live deploy, docker exec, rpk topic produce,
      or .201 mutation is performed by this PR. deploy
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13347/dod-deploy-no-runtime-impact/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the central evidence PR carrying the OMN-13347 contract and code-PR
      binding. Binds the OCC PR itself by pr_number so the OCC receipt-gate eligibility check can resolve
      a PASS receipt for the evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13347/dod-occ-pr-self/command.yaml
