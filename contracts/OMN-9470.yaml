---
schema_version: "1.0.0"
ticket_id: "OMN-9470"
title: "Workspace mode packages sibling repos with per-repo provenance"
summary: >
  Implements workspace-mode runtime build provenance for omnibase_infra by staging sibling repos, computing
  per-repo SHA-256 digests, verifying local-path installs, and recording build provenance.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-workspace-provenance-tests-pr-1764"
    description: "omnibase_infra PR #1764 carries workspace provenance unit and Dockerfile contract tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title"
  - id: "dod-workspace-provenance-precommit-pr-1764"
    description: "omnibase_infra PR #1764 reports all pre-commit hooks passing."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title"
  - id: "dod-runtime-image-deploy-gate-pr-1764"
    description: "omnibase_infra PR #1764 carries the runtime-image deploy gate evidence for the Dockerfile
      workspace placeholder fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "deploy validation: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title"
  - id: "dod-occ-pr-1715"
    description: "OCC PR #1715 carries the OMN-9470 contract and receipt binding for omnibase_infra PR
      #1764."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title"
  - id: "dod-occ-pr-1719"
    description: "OCC PR #1719 carries the OMN-9470 runtime-image deploy gate receipt for omnibase_infra
      PR #1764."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title"
  - id: "workspace-provenance-tests-pr-1764-r15481"
    description: "Append-only replacement for dod-workspace-provenance-tests-pr-1764. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1764 on OmniNode-ai/omnibase_infra, and `gh pr view --json` with no assertion exits 0
      for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at be5f7e514d5886d0ae06b10a7b432f594caf02ea,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-workspace-provenance-tests-pr-1764"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1764 --repo OmniNode-ai/omnibase_infra --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'be5f7e514d5886d0ae06b10a7b432f594caf02ea' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1764 --repo OmniNode-ai/omnibase_infra --json files --jq '.files[].path'
          | grep -cxF 'scripts/deploy-agent/tests/unit/test_executor_workspace_provenance.py' | grep -qx
          1"
  - id: "workspace-provenance-precommit-pr-1764-r15481"
    description: "Append-only replacement for dod-workspace-provenance-precommit-pr-1764. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1764 on OmniNode-ai/omnibase_infra, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at be5f7e514d5886d0ae06b10a7b432f594caf02ea,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-workspace-provenance-precommit-pr-1764"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1764 --repo OmniNode-ai/omnibase_infra --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'be5f7e514d5886d0ae06b10a7b432f594caf02ea' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1764 --repo OmniNode-ai/omnibase_infra --json statusCheckRollup --jq
          '.statusCheckRollup[] | select((.name // .context) == \"Lint\") | (.conclusion // .state)' |
          grep -cxF 'SUCCESS' | grep -qx 1"
  - id: "runtime-image-deploy-gate-pr-1764-r15481"
    description: "Append-only replacement for dod-runtime-image-deploy-gate-pr-1764. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1764 on OmniNode-ai/omnibase_infra, and `gh pr view --json` with no assertion exits 0
      for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at be5f7e514d5886d0ae06b10a7b432f594caf02ea,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-runtime-image-deploy-gate-pr-1764"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1764 --repo OmniNode-ai/omnibase_infra --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'be5f7e514d5886d0ae06b10a7b432f594caf02ea' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1764 --repo OmniNode-ai/omnibase_infra --json files --jq '.files[].path'
          | grep -cxF 'docker/Dockerfile.runtime' | grep -qx 1"
  - id: "occ-pr-1715-r15481"
    description: "Append-only replacement for dod-occ-pr-1715. The historical entry is immutable and stays
      byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner placeholders, which the
      compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never pinned PR #1715 on OmniNode-ai/onex_change_control,
      and `gh pr view --json` with no assertion exits 0 for any PR that exists (mis-bound AND unfalsifiable).
      This entry pins the literal PR number and repo and asserts two facts the selector does not determine:
      the PR is MERGED at d0f60f3e57de0ccff9aa086c7e68e3b424ea0e75, plus a per-item distinguishing binding.
      Both checks executed GREEN under `bash -o pipefail -c` at 2026-07-30T09:32:56Z and are RED-proven
      by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-pr-1715"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1715 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'd0f60f3e57de0ccff9aa086c7e68e3b424ea0e75' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1715 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-9470.yaml' | grep -qx 1"
  - id: "occ-pr-1719-r15481"
    description: "Append-only replacement for dod-occ-pr-1719. The historical entry is immutable and stays
      byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner placeholders, which the
      compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never pinned PR #1719 on OmniNode-ai/onex_change_control,
      and `gh pr view --json` with no assertion exits 0 for any PR that exists (mis-bound AND unfalsifiable).
      This entry pins the literal PR number and repo and asserts two facts the selector does not determine:
      the PR is MERGED at d9aa830b5b2c120c43683b93b6f711857d00c419, plus a per-item distinguishing binding.
      Both checks executed GREEN under `bash -o pipefail -c` at 2026-07-30T09:32:56Z and are RED-proven
      by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-pr-1719"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1719 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'd9aa830b5b2c120c43683b93b6f711857d00c419' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1719 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-9470.yaml' | grep -qx 1"
