---
schema_version: "1.0.0"
ticket_id: OMN-8761
title: "Implement node_gap_compute"
summary: "Promote node_gap_compute from explicit stub to deterministic contract/report gap detection."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "events"
  - "topics"
evidence_requirements:
  - kind: "manual"
    description: "Gap compute golden-chain and explicit-stub tests pass"
    command: "uv run pytest tests/test_golden_chain_gap_compute.py tests/test_golden_chain_new_stub_nodes.py
      -q"
  - kind: "manual"
    description: "Touched gap compute paths pass lint"
    command: "uv run ruff check src/omnimarket/nodes/node_gap_compute tests/test_golden_chain_gap_compute.py
      tests/test_golden_chain_new_stub_nodes.py"
  - kind: "manual"
    description: "Gap compute CLI emits JSON and exits zero"
    command: "uv run python -m omnimarket.nodes.node_gap_compute detect --dry-run --skip-infra-probes"
  - kind: "manual"
    description: "Post-merge deploy smoke imports the gap compute CLI in the runtime container"
    command: "docker exec omninode-runtime python -m omnimarket.nodes.node_gap_compute --help"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Gap compute golden-chain and explicit-stub tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_golden_chain_gap_compute.py tests/test_golden_chain_new_stub_nodes.py
          -q"
  - id: "dod-lint"
    description: "Touched gap compute paths pass lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_gap_compute tests/test_golden_chain_gap_compute.py
          tests/test_golden_chain_new_stub_nodes.py"
  - id: "dod-cli"
    description: "Gap compute CLI emits JSON and exits zero"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python -m omnimarket.nodes.node_gap_compute detect --dry-run --skip-infra-probes"
  - id: "dod-deploy"
    description: "Post-merge deploy smoke imports the gap compute CLI in the runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec omninode-runtime python -m omnimarket.nodes.node_gap_compute --help"
