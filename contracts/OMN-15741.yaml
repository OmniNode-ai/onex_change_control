---
schema_version: "1.0.0"
ticket_id: "OMN-15741"
title: "Deploy-scope OCC evidence for OMN-15741"
summary: "OCC contract for OmniNode-ai/omnibase_infra PR #2690 (G1 of the gateway node-architecture-lift
  Phase 0): replaces the gateway container healthcheck's test -f /tmp/gateway-forwarder-ready sentinel
  check with a real produce+readback canary probe across the local and cloud broker legs."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2690 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2690"
    description: "PR #2690 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2690 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2690 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2690 clears the deploy-gate. Falsifiable live-surface probe:
      fetches the actual gateway_canary_probe.py content from GitHub at head commit 11548f36fadad02d6a963f0b94deca24938a3413
      and asserts the new probe function this PR introduces is present -- exit status depends on the real
      state of the pushed commit, not a self-authored receipt."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/gateway_canary_probe.py?ref=11548f36fadad02d6a963f0b94deca24938a3413
          --jq .content | base64 -d | grep -q 'async def check_canary_leg'"
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.gateway.yml?ref=11548f36fadad02d6a963f0b94deca24938a3413
          --jq .content | base64 -d | grep -q 'onex-gateway-canary-probe'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2690"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6212"
    description: "Binds OMN-15741 to OCC companion PR #6212 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6212 --repo OmniNode-ai/onex_change_control --json number,state"
