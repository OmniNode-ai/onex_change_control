---
schema_version: "1.0.0"
ticket_id: "OMN-10791"
title: "Register delegation output schema in compliance registry"
summary: >
  Register ModelDelegationResult in the output schema registry under key 'delegation_result', following
  the existing direct-import pattern used by review_output and plan_document registrations.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnimarket PR #590 carries the OMN-10791 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10791/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Unit tests for schema registry delegation_result key pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10791/dod-local-tests/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OCC contract and receipt."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10791/dod-occ-pr/command.yaml\""
