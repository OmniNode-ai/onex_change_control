---
schema_version: "1.0.0"
ticket_id: "OMN-11584"
title: "Restructure omniintelligence tools and clients into ONEX taxonomy"
summary: >
  Tracks the merged omniintelligence PR #689 taxonomy refactor plus follow-up PRs #690 and #691, which
  align dependency lock state and CI configuration after #689 auto-merged before those repair commits
  landed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-pr-689"
    description: "omniintelligence PR #689 merged the scoped OMN-11584 taxonomy refactor into dev."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-omniintelligence-pr-689/command.yaml\"
          && grep -q '^pr_number: 689$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-omniintelligence-pr-689/command.yaml\""
  - id: "dod-omniintelligence-pr-690"
    description: "omniintelligence PR #690 carries the dependency lock and import-order follow-up after
      #689."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-omniintelligence-pr-690/command.yaml\"
          && grep -q '^pr_number: 690$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-omniintelligence-pr-690/command.yaml\""
  - id: "dod-focused-validation-pr-690"
    description: "Focused dependency-resolution, lint, and pytest checks pass for the #690 follow-up."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-focused-validation-pr-690/command.yaml\"
          && grep -q '33 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-focused-validation-pr-690/command.yaml\""
  - id: "dod-omniintelligence-pr-691"
    description: "omniintelligence PR #691 carries the CI taxonomy cleanup after the validators rename."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-omniintelligence-pr-691/command.yaml\"
          && grep -q '^pr_number: 691$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-omniintelligence-pr-691/command.yaml\""
  - id: "dod-ci-validation-pr-691"
    description: "Focused CI-config validation passes for the #691 follow-up."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-ci-validation-pr-691/command.yaml\"
          && grep -q 'no issues found' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-ci-validation-pr-691/command.yaml\""
  - id: "dod-occ-pr-1830"
    description: "onex_change_control PR #1830 carries the OMN-11584 central contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-occ-pr-1830/command.yaml\"
          && grep -q '^pr_number: 1830$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-occ-pr-1830/command.yaml\""
  - id: "dod-occ-pr-1831"
    description: "onex_change_control PR #1831 corrects OMN-11584 receipts for the actual #689 merge and
      #690 follow-up."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-occ-pr-1831/command.yaml\"
          && grep -q '^pr_number: 1831$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-occ-pr-1831/command.yaml\""
  - id: "dod-occ-pr-1832"
    description: "onex_change_control PR #1832 adds the #691 central receipts and rebinds OMN-11584 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-occ-pr-1832/command.yaml\"
          && grep -q '^pr_number: 1832$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-occ-pr-1832/command.yaml\""
