---
schema_version: "1.0.0"
ticket_id: "OMN-11584"
title: "Restructure omniintelligence tools and clients into ONEX taxonomy"
summary: >
  Rebuilds omniintelligence PR #689 on current dev, narrows it to the OMN-11584 taxonomy refactor, moves
  clients into adapters, moves tools into validators, and aligns dependency source pins so CI can resolve
  the release-tagged dependency graph.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-pr-689"
    description: "omniintelligence PR #689 carries the scoped OMN-11584 taxonomy refactor on current dev."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-omniintelligence-pr-689/command.yaml\"
          && grep -q '^pr_number: 689$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-omniintelligence-pr-689/command.yaml\""
  - id: "dod-focused-validation-pr-689"
    description: "Focused syntax, lint, stale-reference, and pytest checks pass for the taxonomy refactor."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-focused-validation-pr-689/command.yaml\"
          && grep -q '336 passed, 1 skipped' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-focused-validation-pr-689/command.yaml\""
  - id: "dod-occ-pr-1830"
    description: "onex_change_control PR #1830 carries the OMN-11584 central contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-occ-pr-1830/command.yaml\"
          && grep -q '^pr_number: 1830$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11584/dod-occ-pr-1830/command.yaml\""
