---
schema_version: "1.0.0"
ticket_id: "OMN-11766"
title: "Registration golden chain e2e test: routing-decision.v1 -> agent_routing_decisions"
summary: >
  Add integration test that publishes onex.evt.omniclaude.routing-decision.v1 to Kafka and asserts the
  row materializes in agent_routing_decisions (omnibase_infra db). Covers the full reducer chain including
  idempotency and required field validation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Three golden-chain integration tests in omnimarket covering publish, idempotency, and
      field validation"
    command: "uv run pytest tests/integration/golden_chain/test_registration_e2e.py -v -m integration"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-768"
    description: "Bind OMN-11766 evidence to omnimarket PR #768 (registration golden chain test)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11766/dod-omnimarket-pr-768/command.yaml
          && grep -q '^pr_number: 768$' drift/dod_receipts/OMN-11766/dod-omnimarket-pr-768/command.yaml"
