---
schema_version: "1.0.0"
ticket_id: "OMN-12832"
title: "Make stability broker advertised identity contract-owned"
summary: >-
  Moves the stability-test Redpanda advertised Kafka/Pandaproxy identity out of host environment variables
  and into the omnibase_infra stability-test compose contract overlay. The rendered stability broker must
  advertise 100.109.203.94:39092 and must never render the LAN-only 192.168.86.201:39092 metadata address.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-no-stability-redpanda-env"
    description: >-
      Static lane checks prove the stability Redpanda advertised identity is owned by the compose contract
      overlay, not by STABILITY_TEST_REDPANDA_* host environment variables.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12832/dod-no-stability-redpanda-env/command.yaml"
  - id: "dod-rendered-metadata-address"
    description: >-
      Non-mutating compose render proves the stability broker advertises the Tailscale-routable metadata
      address and rejects the LAN-only metadata address.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12832/dod-rendered-metadata-address/command.yaml"
  - id: "dod-env-gate-location"
    description: >-
      Existing CI/pre-commit environment-read gates were identified and their focused tests pass; the
      new outage class is covered by the stability compose render/static tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12832/dod-env-gate-location/command.yaml"
  - id: "dod-live-bret-unblock"
    description: >-
      Live stability broker metadata was restored to the Tailscale-routable address without recreating
      runtime, effects, or projection containers.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12832/dod-live-bret-unblock/command.yaml"
  - id: "dod-deploy-gate-evidence"
    description: >-
      Deploy-gate evidence for the docker compose runtime path: the fix was validated by a scoped
      stability broker container recreate and did not deploy or restart runtime/effects/projection containers.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12832/dod-deploy-gate-evidence/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-12832 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12832/dod-occ-pr-self/command.yaml"
