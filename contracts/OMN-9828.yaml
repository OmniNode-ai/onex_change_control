---
schema_version: "1.0.0"
ticket_id: "OMN-9828"
title: "feat(OMN-9828): centralize ticket contracts into onex_change_control"
summary: "Copy 49 OMN-XXXX ticket contracts that existed only in downstream repos (omnimarket, omniclaude,
  omnibase_infra) into onex_change_control/contracts/, the declared canonical home per the README. Adds
  missing title fields to 14 older-schema contracts so all pass validate-yaml."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "All centralized contracts pass validate-yaml against ModelTicketContract"
    command: "uv run validate-yaml contracts/OMN-*.yaml"
  - kind: "ci"
    description: "All CI gates green on onex_change_control PR"
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/onex_change_control"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-contracts-present"
    description: "Centralized contract files are present in this repo's contracts/ directory"
    source: "generated"
    checks:
      - check_type: command
        check_value: "test -f contracts/OMN-10864.yaml && test -f contracts/OMN-10429.yaml && test -f
          contracts/OMN-9620.yaml"
  - id: "dod-contracts-valid"
    description: "All contract files validate against ModelTicketContract"
    source: "manual"
    checks:
      - check_type: command
        check_value: "uv run validate-yaml contracts/OMN-9828.yaml"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for this OCC PR."
    source: "generated"
    checks:
      - check_type: command
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json state -q '.state'
          | grep -q OPEN"
