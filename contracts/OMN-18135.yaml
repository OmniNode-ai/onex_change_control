---
schema_version: "1.0.0"
ticket_id: "OMN-18135"
title: "Autobind OCC evidence for OMN-18135"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2447."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2447 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2447 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: "uv run pytest tests/unit/nodes/node_dod_verify/test_omn_18135_transparent_prefixes.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2447"
    description: "PR #2447 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_dod_verify/services/check_proof_class.py?ref=7fed3a18c8335b75bdf0a2601c2aab7aff77c081 --jq '.content' | base64 -d | grep -c 'def _is_transparent_segment'
  - id: "dod-OmniNode-ai-omnimarket-pr-2447-ci"
    description: "PR #2447 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2447 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2447 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_dod_verify/test_omn_18135_transparent_prefixes.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-8944"
    description: "OCC companion PR #8944 — self-bind for OMN-18135 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8944 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omn18135-ac1-env-options"
    description: >
      AC1 — the six `env` option forms that classified indeterminate now classify behavior. Red at the
      parent f36822c5, where all six failed; green here.
    source: "manual"
    binds_ac: ["AC1"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest 'tests/unit/nodes/node_dod_verify/test_omn_18135_transparent_prefixes.py::test_env_options_are_transparent' -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "dod-omn18135-ac6-cd-transparent"
    description: >
      AC6 — a `cd` segment no longer ends the walk before the runner is reached, and `cd x` alone still
      asserts nothing rather than becoming a surrogate.
    source: "manual"
    binds_ac: ["AC6"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest 'tests/unit/nodes/node_dod_verify/test_omn_18135_transparent_prefixes.py::test_cd_is_transparent_and_does_not_short_circuit' 'tests/unit/nodes/node_dod_verify/test_omn_18135_transparent_prefixes.py::test_a_cd_only_command_asserts_nothing_and_says_so' -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "dod-omn18135-ac2-controls-unmoved"
    description: >
      AC2 — every shape that is not an `env` option or a `cd` classifies exactly as before, the exit-code-laundering
      guard still fires ahead of transparency, and a denylisted foreign suite stays a surrogate behind
      a transparent prefix. That last one is the single direction this change must never move.
    source: "manual"
    binds_ac: ["AC2"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest 'tests/unit/nodes/node_dod_verify/test_omn_18135_transparent_prefixes.py::test_controls_are_unmoved' 'tests/unit/nodes/node_dod_verify/test_omn_18135_transparent_prefixes.py::test_exit_code_laundering_still_wins_over_transparency' 'tests/unit/nodes/node_dod_verify/test_omn_18135_transparent_prefixes.py::test_a_foreign_suite_stays_a_surrogate_behind_a_transparent_prefix' -q
        cwd: "${OMNI_HOME}/omnimarket"
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_dod_verify/test_omn_15911_check_proof_class.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "dod-omn18135-ac7-prefix-set-named"
    description: >
      AC7 — the transparent-prefix set is named in the module docstring where a contract author will meet
      it, and `bash -c` is named as deliberately excluded, so the set cannot quietly grow.
    source: "manual"
    binds_ac: ["AC7"]
    checks:
      - check_type: "command"
        check_value: |-
          grep -q 'Transparent prefixes resolve through, and the set is closed' src/omnimarket/nodes/node_dod_verify/services/check_proof_class.py && grep -q 'is NOT' src/omnimarket/nodes/node_dod_verify/services/check_proof_class.py && grep -q 'cd <dir>`` as a whole pipeline segment' src/omnimarket/nodes/node_dod_verify/services/check_proof_class.py
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "dod-omn18135-ac8-gates-not-exclusive"
    description: >
      AC8 — the form that satisfies receipt hardening and keeps its behaviour class is pinned as a test
      and written into the module docstring, so the "mutually exclusive gates" premise cannot be rediscovered.
    source: "manual"
    binds_ac: ["AC8"]
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest 'tests/unit/nodes/node_dod_verify/test_omn_18135_transparent_prefixes.py::test_the_repo_qualified_anchor_never_downgraded_anything' -q
        cwd: "${OMNI_HOME}/omnimarket"
      - check_type: "command"
        check_value: |-
          grep -q 'two gates look like they conflict' src/omnimarket/nodes/node_dod_verify/services/check_proof_class.py
        cwd: "${OMNI_HOME}/omnimarket"
