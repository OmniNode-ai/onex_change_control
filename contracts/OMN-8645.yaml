---
schema_version: "1.0.0"
ticket_id: OMN-8645
title: "test(integration): golden chain — bloom eval orchestrator → intelligenceBloomEvalResults"
summary: "test(integration): golden chain — bloom eval orchestrator → intelligenceBloomEvalResults"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 574 --repo OmniNode-ai/omniintelligence --json state -q .state"
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 574 --repo OmniNode-ai/omniintelligence --json mergedAt -q .mergedAt"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks 574 --repo OmniNode-ai/omniintelligence"
