---
schema_version: "1.0.0"
ticket_id: "OMN-11768"
title: "Golden chain test: routing (llm-routing-decision -> llm_routing_decisions)"
summary: >
  Adds node_projection_llm_routing handler + contract to omnimarket and golden chain e2e test. Closes
  OMN-8540 tail gap: the llm_routing_decisions table existed in DB migrations but had no projection handler
  consuming onex.evt.omniclaude.llm-routing-decision.v1. Ships 10 passing tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr"
    description: "omnimarket PR #772 ships node_projection_llm_routing handler and 10 golden chain tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11768/dod-omnimarket-pr/command.yaml
          && grep -q '^pr_number: 772$' drift/dod_receipts/OMN-11768/dod-omnimarket-pr/command.yaml"
  - id: "dod-tests-pass"
    description: "10/10 golden chain routing tests pass locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11768/dod-tests-pass/command.yaml
          && grep -q '10 passed' drift/dod_receipts/OMN-11768/dod-tests-pass/command.yaml"
  - id: "dod-contract-wired"
    description: "Contract subscribe_topics includes onex.evt.omniclaude.llm-routing-decision.v1."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11768/dod-contract-wired/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC fix PR binds evidence to onex_change_control."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11768/dod-occ-pr/command.yaml"
  - id: "dod-deploy-smoke"
    description: "node_projection_llm_routing handler importable in runtime container."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'docker exec' drift/dod_receipts/OMN-11768/dod-deploy-smoke/command.yaml
          && grep -q '^status: PASS$' drift/dod_receipts/OMN-11768/dod-deploy-smoke/command.yaml"
