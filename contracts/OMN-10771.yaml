---
schema_version: "1.0.0"
ticket_id: "OMN-10771"
title: "feat(OMN-10771): input adapter protocol and CLI/fake adapters"
summary: >
  Adds the interactive onboarding input adapter protocol plus CLI and fake implementations, with direct
  integration coverage for protocol compatibility across a representative step sequence.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1549 carries the OMN-10771 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10771/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Focused unit, integration, and ruff checks passed for input adapters."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10771/dod-local-tests/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR #888 carries the central OCC contract and receipt update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10771/dod-occ-pr/command.yaml\""
