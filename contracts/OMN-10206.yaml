---
schema_version: "1.0.0"
ticket_id: "OMN-10206"
title: "[SEAM-6] Cut cross-CLI originators to Pattern B broker surface"
summary: "Route delegate originator dispatch through runtime ingress and Pattern B broker semantics instead
  of the legacy Claude hook emit daemon."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "envelopes"
  - "topics"
evidence_requirements:
  - kind: "tests"
    description: "Delegate runtime dispatch and runtime-backed skill path validator tests pass."
    command: "cd ../omniclaude && uv run pytest tests/validation/test_validate_runtime_backed_skill_paths.py
      tests/unit/hooks/lib/test_delegate_kafka_publish.py -q"
  - kind: "tests"
    description: "Delegate manifest strict validation proves the direct emit-daemon path is no longer
      canonical."
    command: "cd ../omniclaude && uv run python scripts/validation/validate_runtime_backed_skill_paths.py
      --skill delegate --strict"
  - kind: "ci"
    description: "omniclaude PR and local push hooks provide pre-merge validation evidence for the runtime
      ingress cutover."
    command: "gh pr checks --repo OmniNode-ai/omniclaude 1517"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Delegate shim builds a ModelRuntimeSkillRequest for node_delegation_orchestrator and
      uses LocalRuntimeSkillClient."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10206/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "Delegate migration manifest marks runtime_skill_client as canonical and rejects direct
      emit-daemon publish as noncanonical."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10206/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "PR and pre-push evidence records the runtime ingress cutover without requiring legacy
      Claude hook emit socket recovery."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10206/dod-003 -type f -name '*.yaml' | grep -q ."
