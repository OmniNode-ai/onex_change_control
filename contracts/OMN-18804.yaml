---
schema_version: "1.0.0"
ticket_id: "OMN-18804"
title: "Privileged-path changes are told to use the writer-App path before a human PR is opened"
summary: |-
  OCC evidence for OmniNode-ai/onex_change_control PR #10360. Hand-authored
  rather than autobound: this PR is opened AS the onexbot-occ-writer App
  through the OMN-18327 dispatch path, which is exactly the shape the
  companion autobind declines to mint for, so no companion exists to bind it.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #10360 product diff scope present"
    command: "gh pr view 10360 --repo OmniNode-ai/onex_change_control --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #10360, derived from the PR's own diff: the test
      module it adds. Bound as dod_evidence item
      `dod-occ-diff-derived-behavior-proof`, executed at cwd
      `${OMNI_HOME}/onex_change_control`.
    command: "uv run pytest tests/unit/scripts/test_check_privileged_path_push.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-onex_change_control-pr-10360"
    description: |-
      PR #10360 — the pre-push hint module and the gate's shared recovery
      command literal are both present in the merged tree.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 10360 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path' | grep -Fx src/onex_change_control/scripts/check_privileged_path_push.py
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #10360 — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/scripts/test_check_privileged_path_push.py -q"
        cwd: "${OMNI_HOME}/onex_change_control"
  - id: "dod-OmniNode-ai-onex_change_control-pr-10360-ci"
    description: "PR #10360 — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 10360 --repo OmniNode-ai/onex_change_control --json files"
