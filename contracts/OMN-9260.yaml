---
schema_version: "1.0.0"
ticket_id: "OMN-9260"
title: "Add 'uv run onex run <node>' CLI entry point for RuntimeLocal"
summary: >-
  Registers `onex run` as a canonical alias for `onex node` in cli_commands.py via cli.add_command(run_node_by_name,
  name="run"). Same flags and RuntimeLocal path. omnimarket docs reference `uv run onex run <node>` as
  the canonical invocation but the command returned "No such command 'run'" before this fix. omnibase_core
  PR #1122 carries the implementation and tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: tests
    description: "6 unit tests in tests/unit/cli/test_onex_run.py pass: command exists, flags, error parity,
      override."
    command: "uv run pytest tests/unit/cli/test_onex_run.py -v"
  - kind: ci
    description: "omnibase_core PR #1122 CI checks green."
    command: "gh pr checks 1122 --repo OmniNode-ai/omnibase_core"
  - kind: manual
    description: "No live deploy required; CLI-only alias addition with no runtime behavior change."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9260/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-core-pr-1122
    description: "omnibase_core PR #1122 carries implementation and tests for onex run alias."
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9260/dod-core-pr-1122/command.yaml"
          && grep -q '^pr_number: 1122$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9260/dod-core-pr-1122/command.yaml"
  - id: dod-deploy
    description: "No live deploy required; this is a pure CLI alias addition with no runtime or service
      behavior change."
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9260/dod-deploy/command.yaml"
  - id: dod-occ-self
    description: "Self-binding receipt: this OCC PR carries OMN-9260 evidence."
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9260/dod-occ-self/command.yaml"
