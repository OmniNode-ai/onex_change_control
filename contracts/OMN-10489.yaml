---
schema_version: "1.0.0"
ticket_id: "OMN-10489"
title: "LLM endpoint_url support for exact provider endpoints"
summary: "Add typed endpoint_url support to LLM inference requests so handlers can post to exact contract-declared
  provider endpoints without appending legacy OpenAI paths."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Endpoint URL unit and integration coverage passes on omnibase_infra PR #1491"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-001/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-001\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-001/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10489\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-001/command.yaml\"
          && grep -q '^check_type: \"command\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-001/command.yaml\""
  - id: "dod-002"
    description: "Repo-local deploy evidence contract validates on omnibase_infra PR #1491"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-002/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-002\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-002/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10489\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-002/command.yaml\"
          && grep -q '^check_type: \"command\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-002/command.yaml\""
  - id: "dod-deploy"
    description: "Post-merge deploy smoke plan covers exact endpoint URL inference without live deploy
      in this PR"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-deploy/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-deploy\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-deploy/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10489\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-deploy/command.yaml\"
          && grep -q '^check_type: \"command\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-deploy/command.yaml\""
  - id: "dod-occ-eligibility"
    description: "OCC PR #672 receipt evidence is locally eligible for downstream omnibase_infra PR #1491"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-occ-eligibility/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-occ-eligibility\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-occ-eligibility/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10489\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-occ-eligibility/command.yaml\"
          && grep -q '^pr_number: 672$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-occ-eligibility/command.yaml\"
          && grep -q 'eligible=true' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10489/dod-occ-eligibility/command.yaml\""
