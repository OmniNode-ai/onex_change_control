---
schema_version: 1.0.0
ticket_id: OMN-10285
title: 'Extend ComponentManifest with projectionSchema and displayContract'
summary: 'Adds projectionSchema, displayContract, and ProjectionOrderingAuthority to ComponentManifest;
  updates validateComponentManifest() for envelope acceptance.'
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
contract_completeness: FULL
evidence_requirements:
  - kind: tests
    description: All unit tests pass (583 tests, 70 files)
    command: npx vitest run
  - kind: ci
    description: Lint passes zero warnings
    command: npm run lint
  - kind: ci
    description: TypeScript type check clean
    command: npm run check
  - kind: ci
    description: Pre-commit hooks pass
    command: pre-commit run --all-files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: All tests pass including 9 new OMN-10285 tests
    source: generated
    checks:
      - check_type: command
        check_value: npx vitest run
  - id: dod-002
    description: Backward-compat — manifests without new fields still validate
    source: generated
    checks:
      - check_type: command
        check_value: npx vitest run shared/types/component-manifest.test.ts
  - id: dod-003
    description: Pre-commit hooks pass
    source: generated
    checks:
      - check_type: command
        check_value: pre-commit run --all-files
