---
schema_version: "1.0.0"
ticket_id: "OMN-12876"
title: "Escalation ladder closure proof after logical secret authority"
summary: >-
  Tracks E1-E6 escalation ladder proof after logical secret and endpoint authority are sufficient for
  the configured frontier route, without blocking base delegation or SEA closure when frontier credentials
  are not in the accepted path.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-escalation-ladder-proof"
    description: >-
      Escalation proof records task and correlation IDs, context arm, provider/model/endpoint ref, attempt
      count, escalation reason, quality score, terminal event, projection row, and dashboard render where
      visible.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12876/dod-escalation-ladder-proof/command.yaml"
