---
schema_version: "1.0.0"
ticket_id: "OMN-12876"
title: "Escalation ladder closure proof after logical secret authority"
summary: >-
  Tracks E1-E6 escalation ladder proof after logical secret and endpoint authority are sufficient for
  the configured frontier route, without blocking base delegation or SEA closure when frontier credentials
  are not in the accepted path.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-escalation-ladder-proof"
    description: >-
      Escalation proof: bifrost model_name repoint (OMN-12890/omnimarket#1190) is the prerequisite contract
      change. The staged escalation procedure is documented in docs/evidence/2026-06-12-weekend-pass/p2-pass/escalation/RESTART_STAGED.md
      (omni_home) which specifies restart steps, the ROUTED→ESCALATING→ROUTED cycle, and the ungameable
      success probe (terminal event + projection row with provider=gemini model=gemini-2.5-flash-lite
      traced to contract+overlay, finish_reason != length, output artifact attached). The live runtime
      proof will be captured at the announced dev-lane mutation window.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12876/dod-escalation-ladder-proof/command.yaml"
