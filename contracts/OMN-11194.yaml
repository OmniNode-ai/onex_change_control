---
schema_version: "1.0.0"
ticket_id: "OMN-11194"
title: "Register runtime manifest and projection health topics in TopicBase"
summary: >-
  Add PLATFORM_RUNTIME_MANIFEST_PUBLISHED (evt), PLATFORM_PROJECTION_DEGRADED (evt), and PLATFORM_PROJECTION_RECOVERED
  (evt) to the TopicBase enum in omnibase_core following the onex.evt.platform.{event-name}.v1 naming
  convention.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: FULL
evidence_requirements:
  - kind: tests
    description: "Topic naming convention tests pass with new platform health members"
    command: >-
      env -u PYTHONPATH uv run pytest tests/unit/constants/ -k "topic" -v
  - kind: ci
    description: "CI pipeline green on omnibase_core PR #1102"
    command: "gh pr checks 1102 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-core-pr-1102
    description: "omnibase_core PR #1102 open with platform health topics registered in TopicBase"
    source: generated
    checks:
      - check_type: command
        check_value: >-
          test -f "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11194/dod-core-pr-1102/command.yaml" && grep
          -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11194/dod-core-pr-1102/command.yaml"
          && grep -q '^pr_number: 1102$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11194/dod-core-pr-1102/command.yaml"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for this OCC PR — anchors evidence chain."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-11194/dod-occ-pr/command.yaml && grep -q '^ticket_id:
          "OMN-11194"$' drift/dod_receipts/OMN-11194/dod-occ-pr/command.yaml
