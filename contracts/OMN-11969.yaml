---
schema_version: "1.0.0"
ticket_id: "OMN-11969"
title: "Deploy ONEX runtime triad to cloud k8s onex-dev"
summary: >
  Central OCC evidence for omninode_infra PR #461, which updates the onex-dev runtime manifests for the
  ONEX runtime triad and records the cloud-dev runtime validation state.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-461"
    description: "omninode_infra PR #461 merged the OMN-11969 runtime triad manifest updates."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "cd /tmp/onex_change_control && grep -q '^status: PASS$' drift/dod_receipts/OMN-11969/dod-omninode-infra-pr-461/command.yaml
          && grep -q '^pr_number: 461$' drift/dod_receipts/OMN-11969/dod-omninode-infra-pr-461/command.yaml"
  - id: "dod-local-validation"
    description: "Focused local validation passed for shellcheck and Kubernetes kustomize rendering."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "cd /tmp/onex_change_control && grep -q '^status: PASS$' drift/dod_receipts/OMN-11969/dod-local-validation/command.yaml
          && grep -q 'shellcheck' drift/dod_receipts/OMN-11969/dod-local-validation/command.yaml && grep
          -q 'kubectl kustomize' drift/dod_receipts/OMN-11969/dod-local-validation/command.yaml"
  - id: "dod-coderabbit-thread"
    description: "CodeRabbit unresolved thread was addressed by excluding the unpinned omnimarket migration
      manifest."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "cd /tmp/onex_change_control && grep -q '^status: PASS$' drift/dod_receipts/OMN-11969/dod-coderabbit-thread/command.yaml
          && grep -q 'isResolved=true' drift/dod_receipts/OMN-11969/dod-coderabbit-thread/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live production deploy was performed in this sweep; PR records cloud-dev runtime
      state and post-merge validation expectations."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "cd /tmp/onex_change_control && grep -q '^status: PASS$' drift/dod_receipts/OMN-11969/dod-deploy-assessment/command.yaml
          && grep -qi 'no live production deploy' drift/dod_receipts/OMN-11969/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-1595"
    description: "onex_change_control PR #1595 carries the central OMN-11969 contract and receipt bundle."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "cd /tmp/onex_change_control && grep -q '^status: PASS$' drift/dod_receipts/OMN-11969/dod-occ-pr-1595/command.yaml
          && grep -q '^pr_number: 1595$' drift/dod_receipts/OMN-11969/dod-occ-pr-1595/command.yaml"
  - id: "dod-compat-pr-108"
    description: "omnibase_compat PR #108 carries the OMN-11969 delegation wire model compatibility updates."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "cd /tmp/onex_change_control && grep -q '^status: PASS$' drift/dod_receipts/OMN-11969/dod-compat-pr-108/command.yaml
          && grep -q '^pr_number: 108$' drift/dod_receipts/OMN-11969/dod-compat-pr-108/command.yaml"
  - id: "dod-occ-pr-1620"
    description: "onex_change_control PR #1620 carries the OMN-11969 compat PR #108 receipt repair."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "cd /tmp/onex_change_control && grep -q '^status: PASS$' drift/dod_receipts/OMN-11969/dod-occ-pr-1620/command.yaml
          && grep -q '^pr_number: 1620$' drift/dod_receipts/OMN-11969/dod-occ-pr-1620/command.yaml"
