---
schema_version: "1.0.0"
ticket_id: OMN-10130
title: "Add DaemonHealthCheckTarget"
summary: "Central receipt contract for the omnimarket process watchdog daemon health check target delivered
  in omnimarket PR #503."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Daemon health check target unit tests pass"
    command: "test -f drift/dod_receipts/OMN-10130/dod-daemon-health-target-tests/command.yaml && grep
      -q '^status: PASS$' drift/dod_receipts/OMN-10130/dod-daemon-health-target-tests/command.yaml &&
      grep -q '4 passed' drift/dod_receipts/OMN-10130/dod-daemon-health-target-tests/command.yaml"
  - kind: "manual"
    description: "Full node_process_watchdog unit suite passes"
    command: "test -f drift/dod_receipts/OMN-10130/dod-watchdog-tests/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-10130/dod-watchdog-tests/command.yaml && grep -q '51 passed' drift/dod_receipts/OMN-10130/dod-watchdog-tests/command.yaml"
  - kind: "manual"
    description: "Touched daemon health watchdog paths pass lint and format checks"
    command: "test -f drift/dod_receipts/OMN-10130/dod-lint-format/command.yaml && grep -q '^status: PASS$'
      drift/dod_receipts/OMN-10130/dod-lint-format/command.yaml && grep -q 'All checks passed!' drift/dod_receipts/OMN-10130/dod-lint-format/command.yaml
      && grep -q 'already formatted' drift/dod_receipts/OMN-10130/dod-lint-format/command.yaml"
  - kind: "manual"
    description: "Repo-local contract YAML parses"
    command: "test -f drift/dod_receipts/OMN-10130/dod-contract-parse/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-10130/dod-contract-parse/command.yaml && grep -q 'contracts/OMN-10130.yaml'
      drift/dod_receipts/OMN-10130/dod-contract-parse/command.yaml"
  - kind: "manual"
    description: "Daemon health check target passes mypy"
    command: "test -f drift/dod_receipts/OMN-10130/dod-mypy/command.yaml && grep -q '^status: PASS$' drift/dod_receipts/OMN-10130/dod-mypy/command.yaml"
  - kind: "manual"
    description: "Full pre-commit hooks pass with OMNIBASE_INFRA_PATH set"
    command: "test -f drift/dod_receipts/OMN-10130/dod-precommit/command.yaml && grep -q '^status: PASS$'
      drift/dod_receipts/OMN-10130/dod-precommit/command.yaml && grep -q 'normalization-symmetry.*Passed'
      drift/dod_receipts/OMN-10130/dod-precommit/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-daemon-health-target-tests"
    description: "Daemon health check target unit tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10130/dod-daemon-health-target-tests/command.yaml
          && grep -q '^status: PASS$' drift/dod_receipts/OMN-10130/dod-daemon-health-target-tests/command.yaml
          && grep -q '4 passed' drift/dod_receipts/OMN-10130/dod-daemon-health-target-tests/command.yaml"
  - id: "dod-watchdog-tests"
    description: "Full node_process_watchdog unit suite passes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10130/dod-watchdog-tests/command.yaml && grep -q
          '^status: PASS$' drift/dod_receipts/OMN-10130/dod-watchdog-tests/command.yaml && grep -q '51
          passed' drift/dod_receipts/OMN-10130/dod-watchdog-tests/command.yaml"
  - id: "dod-lint-format"
    description: "Touched daemon health watchdog paths pass lint and format checks"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10130/dod-lint-format/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10130/dod-lint-format/command.yaml && grep -q 'All checks passed!'
          drift/dod_receipts/OMN-10130/dod-lint-format/command.yaml && grep -q 'already formatted' drift/dod_receipts/OMN-10130/dod-lint-format/command.yaml"
  - id: "dod-contract-parse"
    description: "Repo-local contract YAML parses"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10130/dod-contract-parse/command.yaml && grep -q
          '^status: PASS$' drift/dod_receipts/OMN-10130/dod-contract-parse/command.yaml && grep -q 'contracts/OMN-10130.yaml'
          drift/dod_receipts/OMN-10130/dod-contract-parse/command.yaml"
  - id: "dod-mypy"
    description: "Daemon health check target passes mypy"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10130/dod-mypy/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10130/dod-mypy/command.yaml"
  - id: "dod-precommit"
    description: "Full pre-commit hooks pass with OMNIBASE_INFRA_PATH set"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10130/dod-precommit/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10130/dod-precommit/command.yaml && grep -q 'normalization-symmetry.*Passed'
          drift/dod_receipts/OMN-10130/dod-precommit/command.yaml"
