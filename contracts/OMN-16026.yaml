---
schema_version: "1.0.0"
ticket_id: "OMN-16026"
title: "Autobind OCC evidence for OMN-16026"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2739.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2739 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2739"
    description: "PR #2739 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2739 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2739 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2739 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2739 --repo OmniNode-ai/omnibase_infra --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2739"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-runtime-image-carries-seed-script"
    description: >-
      Falsifiable deploy probe (OMN-14443): the runtime image's build recipe must install scripts/seed-keycloak-clients.py
      at the exact path the onex-dev seed Job execs (/app/scripts/seed-keycloak-clients.py). Reads docker/Dockerfile.runtime
      back from GitHub at the pinned omnibase_infra ref via gh-api and greps for the COPY destination.
      This is the hermetic form the deploy-gate blesses: it reads a surface this PR does not author, so
      it goes RED the moment the COPY is absent or renamed. The live-cluster half of the proof (pod imageID
      readback + seed Job RC=0) lands with the omninode_infra digest-bump ticket, after the image exists.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/contents/docker/Dockerfile.runtime?ref=5ecbb3733c46551010021c8f50160a659aad83cf
          --jq .content | base64 -d | grep -q '/app/scripts/seed-keycloak-clients.py'"
  - id: "occ-self-bind-pr-6468"
    description: "Binds OMN-16026 to OCC companion PR #6468 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6468 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-6478"
    description: "Binds OMN-16026 to OCC companion PR #6478, which carries the falsifiable deploy probe
      (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6478 --repo OmniNode-ai/onex_change_control --json number,state"
