---
schema_version: "1.0.0"
ticket_id: "OMN-13822"
title: "fix(OMN-13822): bump omnimarket omnibase-spi pin to >=0.23.0,<0.24.0"
summary: >-
  omnimarket PR #1557 updates the omnibase-spi dependency constraint from >=0.22.0,<0.23.0 to >=0.23.0,<0.24.0
  and refreshes uv.lock to resolve omnibase-spi 0.23.0. This removes the dependency conflict that blocks
  the .201 stability-test workspace from installing the OMN-13768-ready omnibase_infra runtime alongside
  omnimarket. The product PR is limited to pyproject.toml and uv.lock; no runtime deploy, restart, or
  .201 mutation is performed by this evidence PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1557"
    description: >-
      Consuming-PR binding: omnimarket PR #1557 carries the OMN-13822 dependency bump for omnibase-spi
      >=0.23.0,<0.24.0. The PR body records the local full-suite result (11199 passed, 4 environment-only
      Postgres failures, 16 skipped), zero collection errors across 11263 collected tests, no import use
      of the spi 0.23 relocated runtime protocol paths, and passing pre-commit. Real probe: gh pr view
      1557 --repo OmniNode-ai/omnimarket --json number,state,headRefOid,headRefName,title,files.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13822/dod-omnimarket-pr-1557/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR #3452 carries the OMN-13822 contract and the dod-omnimarket-pr-1557
      receipt consumed by omnimarket PR #1557 through Receipt Gate and OCC eligibility.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13822/dod-occ-self/command.yaml"
