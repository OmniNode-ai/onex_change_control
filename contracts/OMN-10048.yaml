---
schema_version: "1.0.0"
ticket_id: OMN-10048
summary: "Replace broken MCP-callable Linear adapter with Linear GraphQL httpx adapter"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "New AdapterLinearGraphQLProjectTracker module exists in omnibase_infra"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f /Users/jonah/Code/omni_home/omnibase_infra/src/omnibase_infra/adapters/project_tracker/linear_graphql_project_tracker_adapter.py"
  - id: "dod-002"
    description: "Old MCP-callable adapter file deleted (no backwards compat shim)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test ! -f /Users/jonah/Code/omni_home/omnibase_infra/src/omnibase_infra/adapters/project_tracker/linear_project_tracker_adapter.py"
  - id: "dod-003"
    description: "Unit tests for AdapterLinearGraphQLProjectTracker pass with mocked httpx (covers all
      Protocol methods + error paths)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "cd /Users/jonah/Code/omni_home/omnibase_infra && uv run pytest tests/unit/adapters/test_linear_graphql_project_tracker_adapter.py
          -v"
  - id: "dod-004"
    description: "Integration test hits real Linear GraphQL API (gated on LINEAR_API_KEY env var)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "cd /Users/jonah/Code/omni_home/omnibase_infra && uv run pytest tests/integration/adapters/test_linear_graphql_adapter_integration.py
          -v -m linear"
  - id: "dod-005"
    description: "Resolver test confirms tracker.get_issue() does NOT raise NotImplementedError outside
      node_session_compose"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "cd /Users/jonah/Code/omni_home/omnibase_infra && uv run pytest tests/services/test_resolver.py::TestResolveProjectTracker
          -v"
  - id: "dod-006"
    description: "Adapter module imports cleanly under mypy --strict (proxy for thorough typed-coverage)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "cd /Users/jonah/Code/omni_home/omnibase_infra && uv run mypy src/omnibase_infra/adapters/project_tracker/
          src/omnibase_infra/services/project_tracker/ --strict"
  - id: "dod-007"
    description: "PR title or body cites this ticket"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: 'gh pr view ${PR_NUMBER} --repo ${REPO} --json title,body --jq ''.title + " " + (.body
          // "")'' | grep -q ''${TICKET_ID}'''
