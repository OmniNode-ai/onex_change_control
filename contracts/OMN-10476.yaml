---
schema_version: "1.0.0"
ticket_id: "OMN-10476"
title: "Fix node_dod_verify template placeholder substitution in probe execution"
summary: "Add full placeholder substitution ({pr}, {repo}, {ticket_id}, ${PR_NUMBER}, ${REPO}, ${TICKET_ID})
  to EvidenceCollector._run_command_check before subprocess execution, and auto-inject onex_change_control
  cwd for OCC contracts."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Evidence collector unit tests pass"
    command: "uv run pytest tests/unit/nodes/node_dod_verify/test_evidence_collector.py -v -m unit"
  - kind: "tests"
    description: "Full test suite passes"
    command: "uv run pytest tests/ -v --tb=short -m 'not kafka'"
  - kind: "ci"
    description: "CI pipeline green on PR"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "Evidence collector unit tests pass (including 6 new placeholder/cwd tests)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10476/dod-unit-tests/command.yaml
          && grep -q '^ticket_id: OMN-10476$' drift/dod_receipts/OMN-10476/dod-unit-tests/command.yaml
          && grep -q '^evidence_item_id: dod-unit-tests$' drift/dod_receipts/OMN-10476/dod-unit-tests/command.yaml"
  - id: "dod-full-suite"
    description: "Full test suite passes with no regressions"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10476/dod-full-suite/command.yaml
          && grep -q '^ticket_id: OMN-10476$' drift/dod_receipts/OMN-10476/dod-full-suite/command.yaml
          && grep -q '^evidence_item_id: dod-full-suite$' drift/dod_receipts/OMN-10476/dod-full-suite/command.yaml"
  - id: "dod-lint"
    description: "Ruff lint and format checks pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10476/dod-lint/command.yaml && grep
          -q '^ticket_id: OMN-10476$' drift/dod_receipts/OMN-10476/dod-lint/command.yaml && grep -q '^evidence_item_id:
          dod-lint$' drift/dod_receipts/OMN-10476/dod-lint/command.yaml"
  - id: "dod-deploy"
    description: "Post-merge deploy smoke: import evidence_collector from runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10476/dod-deploy/command.yaml &&
          grep -q '^ticket_id: OMN-10476$' drift/dod_receipts/OMN-10476/dod-deploy/command.yaml && grep
          -q '^evidence_item_id: dod-deploy$' drift/dod_receipts/OMN-10476/dod-deploy/command.yaml &&
          grep -q 'docker exec' drift/dod_receipts/OMN-10476/dod-deploy/command.yaml"
