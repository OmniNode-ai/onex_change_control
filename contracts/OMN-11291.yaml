---
schema_version: "1.0.0"
ticket_id: OMN-11291
title: "Unblock omniclaude dependabot Actions bump with OCC evidence"
summary: "Tracks omniclaude PR #1616, a Dependabot GitHub Actions group update touching workflow pins
  only, and binds central OCC evidence for receipt-gate closeout."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omniclaude PR 1616 is open at the recorded Dependabot head and all non-receipt checks
      are green or skipped."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid"
  - kind: "manual"
    description: "OCC PR 1207 carries the OMN-11291 central contract and receipt evidence for omniclaude
      PR 1616."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1616"
    description: "omniclaude PR 1616 updates only GitHub Actions workflow files and all non-receipt checks
      are green or skipped."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --repo ${REPO} --json name,state --jq 'all(.[]; .name
          == \"verify / verify\" or .state == \"SUCCESS\" or .state == \"SKIPPED\")'"
  - id: "dod-occ-pr-1207"
    description: "OCC PR 1207 carries the OMN-11291 central contract and receipt evidence for omniclaude
      PR 1616."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid"
