---
schema_version: "1.0.0"
ticket_id: "OMN-17538"
title: "Lane desired-state identity models and the lane desired-state compute"
summary: |-
  Hand-authored OCC companion for OmniNode-ai/omnibase_core#1765, the core half of OMN-17538. One dod_evidence item per core-half definition-of-done requirement written on the ticket before the models were delegated (lane dm-nodes, delegation matrix 2026-09-25). The compute-node half is not covered here; it moves to omnimarket and needs a core release first.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
dod_evidence:
  - id: "dod-omn17538-c1-roundtrip-strict"
    description: |-
      DoD-C1 (AC1): the six models round-trip through JSON unchanged, are frozen, forbid extra fields, and validate the digest and sha shapes. Behaviour: uv run pytest tests/unit/models/lane_desired_state/test_lane_desired_state_models_omn17538.py -q -k "roundtrip or strict" at the head of OmniNode-ai/omnibase_core#1765.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/models/lane_desired_state/model_migration_bundle_id.py?ref=26738a842198b1f639b72ca37f14fac34232c36f" --jq '.content' | base64 -d | grep -cF 'pattern=r"^sha256:[0-9a-f]{64}$"'
  - id: "dod-omn17538-c2-unresolved"
    description: |-
      DoD-C2 (AC5): an unresolved field serializes as ModelUnresolvedField and identity_hash still computes. Behaviour: -k unresolved on the same test file.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/models/lane_desired_state/model_lane_desired_state_identity.py?ref=26738a842198b1f639b72ca37f14fac34232c36f" --jq '.content' | base64 -d | grep -cF 'resolved_images: dict[str, ModelResolvedImage] | ModelUnresolvedField'
  - id: "dod-omn17538-c3-secrets"
    description: |-
      DoD-C3 (AC8): ModelSecretsSyncIdentity declares exactly four reference fields and refuses an inline secret. Behaviour: -k secrets on the same test file.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/models/lane_desired_state/model_secrets_sync_identity.py?ref=26738a842198b1f639b72ca37f14fac34232c36f" --jq '.content' | base64 -d | grep -cF 'machine_identity_ref'
  - id: "dod-omn17538-c4-hash"
    description: |-
      DoD-C4 (AC3, hash half): identity_hash is deterministic, independent of dict order, and moves on a single jobs digest. Behaviour: -k hash on the same test file.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/models/lane_desired_state/model_lane_desired_state_identity.py?ref=26738a842198b1f639b72ca37f14fac34232c36f" --jq '.content' | base64 -d | grep -cF 'def identity_hash(self) -> str:'
  - id: "dod-omn17538-c5-types-lint"
    description: |-
      DoD-C5: mypy --strict, ruff check and ruff format check are clean on the package.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/models/lane_desired_state/__init__.py?ref=26738a842198b1f639b72ca37f14fac34232c36f" --jq '.content' | base64 -d | grep -cF '"ModelLaneDesiredStateIdentity",'
  - id: "dod-omn17538-c6-string-versions"
    description: |-
      DoD-C6: the repo's validate-string-versions gate passes on the package; the two schema versions are ModelSemVer and lane_id carries the string-id-ok exemption.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/models/lane_desired_state/model_lane_desired_state_identity.py?ref=26738a842198b1f639b72ca37f14fac34232c36f" --jq '.content' | base64 -d | grep -cF 'overlay_schema_version: ModelSemVer | ModelUnresolvedField'
