---
schema_version: "1.0.0"
ticket_id: "OMN-10745"
title: "Extract cost accounting sqlite3.connect to adapter"
summary: >
  Binds omniclaude PR #1554, which removes the direct sqlite3.connect() call (stopgap di-ok annotation)
  from plugins/onex/hooks/lib/cost_accounting.py. A new cost_accounting_adapter.py file is introduced,
  allowlisted by the _adapter.py suffix pattern in the AST validator. The dead try/except ImportError
  shim is removed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1554-head"
    description: "omniclaude PR #1554 carries the cost accounting sqlite3 adapter refactor at the recorded
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10745/dod-pr-1554-head/command.yaml\""
  - id: "dod-no-raw-sqlite3"
    description: "Raw sqlite3.connect validator passes after refactor."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10745/dod-no-raw-sqlite3/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10745/dod-occ-pr/command.yaml\""
