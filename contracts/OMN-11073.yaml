---
schema_version: "1.0.0"
ticket_id: "OMN-11073"
title: "fix(OMN-11073): forward delegation quality contract fields through runtime dispatch port"
summary: >-
  RuntimeDelegationDispatchPort.dispatch() accepts quality_contract_mode and acceptance_criteria and forwards
  them into the delegation command payload. Fixes unexpected keyword argument error when delegation smoke
  sends these fields through the runtime-owned port.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: "tests"
    description: "Focused unit and integration tests prove dispatch field forwarding and default payload
      values."
    command: >-
      uv run pytest tests/unit/runtime/test_service_delegation_dispatch_port.py tests/integration/test_service_delegation_dispatch_port_quality_contract.py
      -q
  - kind: "manual"
    description: "Deploy-gate proof: port signature-compatible change; next runtime image build carries
      the aligned dispatch port."
    command: "deploy-gate: next runtime image build carries quality_contract_mode in dispatch port"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Unit and integration coverage for quality contract dispatch fields passes (9 tests)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-11073/dod-focused-tests/command.yaml && grep
          -q 'pr_number: 1624' drift/dod_receipts/OMN-11073/dod-focused-tests/command.yaml
  - id: "dod-deploy-gate"
    description: "Deploy scope is a signature-compatible runtime dispatch payload change; next runtime
      image build carries the aligned port."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-11073/dod-deploy-gate/command.yaml && grep -q
          'pr_number: 1624' drift/dod_receipts/OMN-11073/dod-deploy-gate/command.yaml
  - id: "dod-occ-pr"
    description: "OCC PR carrying this contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-11073/dod-occ-pr/command.yaml
