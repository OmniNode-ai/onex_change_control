---
schema_version: "1.0.0"
ticket_id: "OMN-15002"
title: "Autobind OCC evidence for OMN-15002"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1871."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1871 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1871"
    description: "PR #1871 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1871-ci"
    description: "PR #1871 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4675"
    description: "OCC companion PR #4675 — self-bind for OMN-15002 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1871-deploy-scope"
    description: "PR #1871 touches runtime handler/contract paths; deploy-scope boundary is acknowledged
      without performing a live deploy in this PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1871 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -F 'Live
          redeploy + `.201` readback (out of scope for this PR' && test 'deploy scope acknowledged; no
          live deploy performed by PR #1871' = 'deploy scope acknowledged; no live deploy performed by
          PR #1871'"
  - id: "occ-self-bind-pr-4676"
    description: "OCC repair PR #4676 — self-bind for OMN-15002 deploy-scope evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4676 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
