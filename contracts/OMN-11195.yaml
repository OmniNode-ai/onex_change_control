---
schema_version: "1.0.0"
ticket_id: "OMN-11195"
title: "Add data_provenance field to EventEnvelopeV1Minimal"
summary: >
  Adds optional data_provenance: str | None field to EventEnvelopeV1Minimal in omnibase_compat. Field
  defaults to None for backwards compatibility. Uses str (not enum) to preserve omnibase_compat's zero
  upstream runtime deps constraint. Allowed label values are demo_seeded, demo_projected_shortcut, measured,
  estimated, unknown.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Provenance field unit tests (4 tests) pass."
    command: "See drift/dod_receipts/OMN-11195/dod-focused-tests/command.yaml"
  - kind: "ci"
    description: "ruff format + ruff check pass for modified files."
    command: "See drift/dod_receipts/OMN-11195/dod-lint/command.yaml"
  - kind: "ci"
    description: "omnibase_compat PR #92 carries the implementation and tests."
    command: "See drift/dod_receipts/OMN-11195/dod-compat-pr-92/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "4 provenance field unit tests passed in omnibase_compat."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '4 passed' drift/dod_receipts/OMN-11195/dod-focused-tests/command.yaml"
  - id: "dod-lint"
    description: "ruff format and ruff check passed for omnibase_compat src/ and tests/."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'All checks passed' drift/dod_receipts/OMN-11195/dod-lint/command.yaml"
  - id: "dod-compat-pr-92"
    description: "omnibase_compat PR #92 carries the OMN-11195 implementation and tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11195/dod-compat-pr-92/command.yaml
          && grep -q '^pr_number: 92$' drift/dod_receipts/OMN-11195/dod-compat-pr-92/command.yaml"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for OCC PR — anchors evidence chain."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'status: PASS' drift/dod_receipts/OMN-11195/dod-occ-pr/command.yaml && grep -q 'contracts/OMN-11195.yaml'
          drift/dod_receipts/OMN-11195/dod-occ-pr/command.yaml
