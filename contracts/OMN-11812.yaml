---
schema_version: "1.0.0"
ticket_id: "OMN-11812"
title: "node_swarm_fanout_effect"
summary: >
  EFFECT node for swarm parallel fan-out dispatch. Sends subtask prompts to model endpoints via HTTP,
  managing wave scheduling, bounded concurrency, retry, and fallback. Subscribes to onex.cmd.omnimarket.swarm-fanout.v1
  and publishes onex.evt.omnimarket.swarm-fanout-completed.v1.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-792"
    description: "omnimarket PR #792 implements node_swarm_fanout_effect with 17 unit tests"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11812/dod-omnimarket-pr-792/command.yaml\"
          && grep -q '^pr_number: 792$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11812/dod-omnimarket-pr-792/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR binds this contract to onex_change_control as the evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11812/dod-occ-pr/command.yaml\""
  - id: "dod-deploy"
    description: "Deploy assessment — new EFFECT node, no live deploy required for initial merge; node
      is invoked on-demand via contract topic"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11812/dod-deploy/command.yaml\""
