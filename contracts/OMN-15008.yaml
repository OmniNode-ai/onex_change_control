---
schema_version: "1.0.0"
ticket_id: "OMN-15008"
title: "OCC evidence for onex_change_control PR 4692"
summary: "Evidence companion for the bot-authored authorization pre-commit hook mirror in onex_change_control#4692."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #4692 product diff scope present"
    command: "gh pr view 4692 --repo OmniNode-ai/onex_change_control --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-onex-change-control-pr-4692"
    description: "PR #4692 on OmniNode-ai/onex_change_control — product scope and exact head evidence
      for OMN-15008."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4692 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid,files
          --jq 'if (.number == 4692 and .state == \"OPEN\" and .headRefOid == \"d0c4ff10e566a67093f5ee499da738811279491b\"
          and any(.files[].path; . == \".pre-commit-config.yaml\") and any(.files[].path; . == \".pre-commit-hooks.yaml\")
          and any(.files[].path; . == \"src/onex_change_control/scripts/check_bot_authored_authz_guard.py\")
          and any(.files[].path; . == \"tests/unit/scripts/test_check_bot_authored_authz_guard.py\"))
          then \"OMN-15008 PR scope verified: bot-authored authz pre-commit hook mirror files present
          at exact head\" else error(\"OMN-15008 PR scope or head mismatch\") end'"
  - id: "occ-self-bind-pr-4695-omn15008"
    description: "OCC companion PR #4695 self-bind for the OMN-15008 evidence companion."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4695 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
          --jq '.number, .state, .headRefName'"
  - id: "dod-onex-change-control-pr-4692-r2"
    description: "PR #4692 on OmniNode-ai/onex_change_control — rebased product scope and product commit
      evidence for OMN-15008."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-onex-change-control-pr-4692"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4692 --repo OmniNode-ai/onex_change_control --json number,state,commits,files
          --jq 'if (.number == 4692 and .state == \"OPEN\" and any(.commits[].oid; . == \"23a209f2b11737e70a19596b0527fa96ddcff560\")
          and any(.files[].path; . == \".pre-commit-config.yaml\") and any(.files[].path; . == \".pre-commit-hooks.yaml\")
          and any(.files[].path; . == \"src/onex_change_control/scripts/check_bot_authored_authz_guard.py\")
          and any(.files[].path; . == \"tests/unit/scripts/test_check_bot_authored_authz_guard.py\"))
          then \"OMN-15008 PR scope verified: bot-authored authz pre-commit hook mirror files present
          at rebased product commit\" else error(\"OMN-15008 rebased PR scope or head mismatch\") end'"
