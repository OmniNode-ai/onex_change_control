---
schema_version: "1.0.0"
ticket_id: "OMN-15375"
title: "Autobind OCC evidence for OMN-15375"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #752.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #752 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-752"
    description: "PR #752 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15375/dod-OmniNode-ai-omninode_infra-pr-752/command.yaml"
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15375/dod-OmniNode-ai-omninode_infra-pr-752/command.yaml"
  - id: "occ-self-bind-pr-5418"
    description: "Binds OMN-15375 to OCC companion PR #5418 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-occ-evidence-admissibility-validator"
    description: >
      Hosted-accessible OCC evidence admissibility validator suite for this companion. The generated Evidence-Source
      checks are preserved for audit, but hosted contract compliance must also execute a non-self-referential
      validator probe.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: dod-15375-infra752-noderole-rb15391
    description: 'Append-only replacement for dod-occ-evidence-admissibility-validator (OMN-15391). The
      superseded entry''s only check was ''uv run pytest tests/test_evidence_admissibility.py -q''. node_dod_verify''s
      EvidenceCollector._infer_occ_cwd resolves that command into the OCC checkout, where it runs OCC''s
      own OMN-15309 admissibility-predicate suite (76 tests, ~0.1s) and passes identically whether or
      not the product fact it is filed under is true. This entry binds the OMN-15375 product fact: the
      bundle-reader policy attachment is repointed at the server node role, recorded in aws/cluster-dev/push_validation_bundles_s3.tf
      at omninode_infra#752''s merge commit 75632308916fb4b759d2a5bb0f1307379986995f and absent at its
      first parent ae400c5cd21b21fa9f3e962a2229dd7abf2f10d6. Scope limit stated plainly: OmniNode-ai/omninode_infra
      is a PRIVATE repository. This check was EXECUTED here on 2026-07-29 with a cross-repo-capable token
      and returned rc=0; under the OCC hosted Contract Compliance runner, whose GH_TOKEN is the repo-scoped
      github.token, the same endpoint returns HTTP 404 and the check goes RED. That RED is fail-closed
      ''cannot evaluate on this runner'', not a false claim about the product, and it is preferred over
      the surrogate''s vacuous GREEN (OMN-15391). node_dod_verify, which runs with an operator token,
      evaluates it for real.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-occ-evidence-admissibility-validator
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omninode_infra/contents/aws/cluster-dev/push_validation_bundles_s3.tf?ref=75632308916fb4b759d2a5bb0f1307379986995f''
          | grep -q ''CORRECTED UNDER OMN-15375'''
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omninode_infra/contents/aws/cluster-dev/push_validation_bundles_s3.tf?ref=ae400c5cd21b21fa9f3e962a2229dd7abf2f10d6''
          | grep -c ''CORRECTED UNDER OMN-15375'' | grep -qx 0'
  - id: occ-self-bind-5418-lit-rb15391
    description: 'Append-only replacement for occ-self-bind-pr-5418 (OMN-15391 collateral repair). The
      superseded stamp''s id pins OCC companion PR #5418, but its check_value is the bare ${PR_NUMBER}
      runner placeholder, which the compliance runner pre-substitutes with whatever PR it is currently
      evaluating -- the OMN-15382 class-3 mis-binding, frozen in .onex_ratchets/omn_15382_rule_b_baseline.yaml.
      This entry pins the literal number so the stamp can only ever be true of the PR it names. Stated
      plainly: this is a binding/stamp item, not product proof -- it does not count toward the substance
      floor, and the product fact for this ticket is carried by the -rb15391 rebind item above.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:occ-self-bind-pr-5418
    checks:
      - check_type: command
        check_value: gh pr view 5418 --repo OmniNode-ai/onex_change_control --json number --jq '.number
          == 5418' | grep -qx true
  - id: occ-self-bind-5418-unprovable-rb2
    description: 'Append-only replacement for occ-self-bind-5418-lit-rb15391 (OMN-15391 round 2, Rule
      C). The superseded stamp''s only check was "gh pr view 5418 --repo OmniNode-ai/onex_change_control
      --json number --jq ''.number == 5418'' | grep -qx true" -- it hands PR #5418 to the API and then
      asserts the returned number equals 5418. That is `N == N`: true by construction for every PR that
      exists, and incapable of going RED for any product reason. It was EXECUTED at review time and returned
      rc=0, as it always will. THIS ENTRY DOES NOT REPLACE IT WITH A BETTER BINDING CHECK, BECAUSE NO
      ADMISSIBLE ONE EXISTS. The two exhaustive candidate shapes are both refused by the live OMN-14505/OMN-15309
      predicate, for two different and individually correct reasons: reading PR #5418''s file list to
      assert it carries contracts/OMN-15375.yaml is INSIDE_OWN_DIFF (it reads back the contract tree the
      evidence author writes), and reading /pulls/5418 metadata to assert it merged is also INSIDE_OWN_DIFF
      (PR metadata proves the change exists, not that anything it claims is true). An OCC self-bind stamp
      is by construction a statement about OCC''s own tree and a PR''s own metadata, so both halves are
      exactly what the predicate refuses. THE BINDING OF OMN-15375 TO OCC COMPANION PR #5418 IS THEREFORE
      RECORDED AS UNPROVABLE-ON-RECORD, mirroring the operator''s OMN-15376 amend-on-record ruling: state
      the limit rather than manufacture a vacuous command that passes. The check below does not assert
      the binding. It asserts the UNPROVABILITY -- it executes the predicate against both candidate shapes
      and goes RED if either is ever admitted, which would mean this recorded limit no longer holds and
      the stamp must be revisited. The product fact for this ticket is carried by the sibling -rb2 item,
      not here; this item counts toward no substance floor.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:occ-self-bind-5418-lit-rb15391
    checks:
      - check_type: command
        check_value: uv run pytest 'tests/unit/test_occ_self_bind_unprovable_on_record_omn15391.py::test_pr_5418_omn_15375_binding_unprovable'
          -q
  - id: dod-15375-infra752-noderole-rb2
    description: 'Append-only replacement for dod-15375-infra752-noderole-rb15391 (OMN-15391 round 2,
      Rule D). The superseded item''s GREEN leg is correct and is carried forward here unchanged; its
      ABSENCE leg was ''... | grep -c ''CORRECTED UNDER OMN-15375'' | grep -qx 0'', which FAILS OPEN.
      check_values are handed to `sh -c` without pipefail, so a producer that 404s, is denied, or hits
      a network error emits nothing, `grep -c` prints 0, and `grep -qx 0` exits 0 -- GREEN, having read
      nothing. Executed proof of the defect at authoring time: the superseded shape returns rc=0 against
      OmniNode-ai/no_such_repo_xyz, a repository that does not exist. The replacement absence leg reads
      the parent revision ONCE into a variable, PROVES the read landed by requiring the structural anchor
      ''PUSH-VALIDATION BUNDLE TRANSFER S3'' (present in this file at both ae400c5cd21b and 75632308916f,
      so it tracks the read rather than the fix), and only then asserts the marker is absent. A failed
      fetch now yields an empty body, the anchor leg fails, and the check goes RED. Claim bound, unchanged:
      the node-role correction exists at omninode_infra#752''s merge commit 75632308916fb4b759d2a5bb0f1307379986995f,
      and was absent at its first parent ae400c5cd21b21fa9f3e962a2229dd7abf2f10d6. Executed at authoring
      time: green leg rc=0, absence leg rc=0; and the absence leg goes RED under all four mutations --
      invalid token, nonexistent repo, pointed at the merge commit instead of the parent, and anchor mutated.
      Scope limit restated: OmniNode-ai/omninode_infra is a PRIVATE repository, so under the OCC hosted
      runner''s repo-scoped github.token both legs return HTTP 404 and go RED. That RED now means ''cannot
      evaluate here'', and -- unlike the superseded leg -- it is reached by a real failure rather than
      by a vacuous pass. node_dod_verify, which runs with an operator token, evaluates both for real.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-15375-infra752-noderole-rb15391
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omninode_infra/contents/aws/cluster-dev/push_validation_bundles_s3.tf?ref=75632308916fb4b759d2a5bb0f1307379986995f''
          | grep -q ''CORRECTED UNDER OMN-15375'''
      - check_type: command
        check_value: 'body=$(gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omninode_infra/contents/aws/cluster-dev/push_validation_bundles_s3.tf?ref=ae400c5cd21b21fa9f3e962a2229dd7abf2f10d6'')
          && printf ''%s'' "$body" | grep -qF ''PUSH-VALIDATION BUNDLE TRANSFER S3'' && ! printf ''%s''
          "$body" | grep -qF ''CORRECTED UNDER OMN-15375'''
