---
schema_version: "1.0.0"
ticket_id: "OMN-15375"
title: "Autobind OCC evidence for OMN-15375"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #752.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #752 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-752"
    description: "PR #752 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15375/dod-OmniNode-ai-omninode_infra-pr-752/command.yaml"
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15375/dod-OmniNode-ai-omninode_infra-pr-752/command.yaml"
  - id: "occ-self-bind-pr-5418"
    description: "Binds OMN-15375 to OCC companion PR #5418 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-occ-evidence-admissibility-validator"
    description: >
      Hosted-accessible OCC evidence admissibility validator suite for this companion. The generated Evidence-Source
      checks are preserved for audit, but hosted contract compliance must also execute a non-self-referential
      validator probe.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: dod-15375-infra752-noderole-rb15391
    description: 'Append-only replacement for dod-occ-evidence-admissibility-validator (OMN-15391). The
      superseded entry''s only check was ''uv run pytest tests/test_evidence_admissibility.py -q''. node_dod_verify''s
      EvidenceCollector._infer_occ_cwd resolves that command into the OCC checkout, where it runs OCC''s
      own OMN-15309 admissibility-predicate suite (76 tests, ~0.1s) and passes identically whether or
      not the product fact it is filed under is true. This entry binds the OMN-15375 product fact: the
      bundle-reader policy attachment is repointed at the server node role, recorded in aws/cluster-dev/push_validation_bundles_s3.tf
      at omninode_infra#752''s merge commit 75632308916fb4b759d2a5bb0f1307379986995f and absent at its
      first parent ae400c5cd21b21fa9f3e962a2229dd7abf2f10d6. Scope limit stated plainly: OmniNode-ai/omninode_infra
      is a PRIVATE repository. This check was EXECUTED here on 2026-07-29 with a cross-repo-capable token
      and returned rc=0; under the OCC hosted Contract Compliance runner, whose GH_TOKEN is the repo-scoped
      github.token, the same endpoint returns HTTP 404 and the check goes RED. That RED is fail-closed
      ''cannot evaluate on this runner'', not a false claim about the product, and it is preferred over
      the surrogate''s vacuous GREEN (OMN-15391). node_dod_verify, which runs with an operator token,
      evaluates it for real.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-occ-evidence-admissibility-validator
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omninode_infra/contents/aws/cluster-dev/push_validation_bundles_s3.tf?ref=75632308916fb4b759d2a5bb0f1307379986995f''
          | grep -q ''CORRECTED UNDER OMN-15375'''
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omninode_infra/contents/aws/cluster-dev/push_validation_bundles_s3.tf?ref=ae400c5cd21b21fa9f3e962a2229dd7abf2f10d6''
          | grep -c ''CORRECTED UNDER OMN-15375'' | grep -qx 0'
  - id: occ-self-bind-5418-lit-rb15391
    description: 'Append-only replacement for occ-self-bind-pr-5418 (OMN-15391 collateral repair). The
      superseded stamp''s id pins OCC companion PR #5418, but its check_value is the bare ${PR_NUMBER}
      runner placeholder, which the compliance runner pre-substitutes with whatever PR it is currently
      evaluating -- the OMN-15382 class-3 mis-binding, frozen in .onex_ratchets/omn_15382_rule_b_baseline.yaml.
      This entry pins the literal number so the stamp can only ever be true of the PR it names. Stated
      plainly: this is a binding/stamp item, not product proof -- it does not count toward the substance
      floor, and the product fact for this ticket is carried by the -rb15391 rebind item above.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:occ-self-bind-pr-5418
    checks:
      - check_type: command
        check_value: gh pr view 5418 --repo OmniNode-ai/onex_change_control --json number --jq '.number
          == 5418' | grep -qx true
