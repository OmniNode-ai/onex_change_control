---
schema_version: "1.0.0"
ticket_id: "OMN-10693"
title: "Build document ingestion node (read-only filesystem effect)"
summary: >
  EFFECT node node_adr_document_ingestion_effect added to omnimarket. Crawls markdown files recursively,
  computes sha256 content hashes, and extracts git metadata via async subprocess. Subscribes to adr-document-ingestion-requested.v1,
  publishes adr-document-ingestion-completed.v1. Read-only — no writes, no LLM, no network. 29 unit tests,
  all pre-commit hooks pass.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "29 unit tests pass covering all handler logic, exclusion patterns, sha256, and git metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10693/dod-unit-tests/command.yaml\""
  - id: "dod-precommit"
    description: "Pre-commit hooks pass for all new node files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10693/dod-precommit/command.yaml\""
  - id: "dod-deploy-verify"
    description: "Node is registered in omnimarket pyproject.toml entry-points and ready to deploy via
      omnimarket runtime."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10693/dod-deploy-verify/command.yaml\""
  - id: "dod-pr-562"
    description: "Bind OMN-10693 evidence to omnimarket PR #562 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10693/dod-pr-562/command.yaml\""
