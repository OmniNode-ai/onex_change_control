---
schema_version: "1.0.0"
ticket_id: "OMN-11549"
title: "test(OMN-11547, OMN-11549): capture golden fixtures for manifest_injector + agent_router [omniclaude]"
summary: >
  Adds tests/golden/test_agent_router_golden.py (61 unit tests, OMN-11549) and
  tests/golden/test_manifest_injector_golden.py (66 unit tests, OMN-11547) to omniclaude. These
  golden fixture tests document current AgentRouter and ManifestInjector behavioral contracts as
  regression guards before Wave 4B restructuring. All 127 tests pass on current code. All external
  dependencies (Kafka, Qdrant, PostgreSQL, Valkey, aiohttp) are fully mocked — no network calls.
  No deployable artifact — test-only PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: 127 unit tests pass covering AgentRouter and ManifestInjector golden behavioral contracts.
    command: >
      cd omniclaude && uv run pytest tests/golden/ -v
  - kind: manual
    description: omniclaude PR #1662 carries the golden fixture implementation.
    command: gh pr checks 1662 --repo OmniNode-ai/omniclaude
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "127 unit tests pass — AgentRouter (61) and ManifestInjector (66) golden behavioral contracts verified."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11549/dod-unit-tests/command.yaml\""
  - id: "dod-no-deploy"
    description: >
      No deployable artifact. This ticket adds golden fixture tests only — no runtime behavior
      change, no new nodes, topics, or contracts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11549/dod-no-deploy/command.yaml\""
