---
schema_version: "1.0.0"
ticket_id: "OMN-11421"
title: "Fix entropy demo to use OmniNode-native node workflow"
summary: >-
  Bind OCC evidence for onex-self-extending-agent PR #78, which replaces the legacy demo.py live path
  with a contract-driven OmniNode-native entropy workflow composed from typed node handlers, removes the
  GEMINI_ENDPOINT direct cloud path, and proves the native module and console script produce complete
  evidence bundles.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Full unit suite, lint, and strict mypy pass for the product repo."
    command: "uv run pytest tests/unit -q && uv run ruff check src tests scripts && uv run mypy src/ --strict
      --ignore-missing-imports"
  - kind: "manual"
    description: "Native entropy module, console script, and legacy wrapper all run through the OmniNode-native
      path."
    command: "See drift/dod_receipts/OMN-11421/*/command.yaml"
dod_evidence:
  - id: "dod-product-pr-78-head"
    description: "onex-self-extending-agent PR #78 carries the native entropy demo implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-product-pr-78-head/command.yaml\"
          && grep -q '^pr_number: 78$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-product-pr-78-head/command.yaml\"
          && grep -q '^commit_sha: \"44e487d86ec745144adfe7f4c3e7048aa55b7585\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-product-pr-78-head/command.yaml\""
  - id: "dod-product-local-verification"
    description: "Full local product verification passed: unit tests, ruff, strict mypy, and demo endpoint
      guard."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-product-local-verification/command.yaml\"
          && grep -q '590 passed, 1 skipped' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-product-local-verification/command.yaml\"
          && grep -q 'GEMINI_ENDPOINT grep: no matches' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-product-local-verification/command.yaml\""
  - id: "dod-native-demo-smoke"
    description: "Native module command produced a COMPLETE aggregate and committed deterministic evidence
      bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-native-demo-smoke/command.yaml\"
          && grep -q 'Aggregation status: COMPLETE' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-native-demo-smoke/command.yaml\"
          && grep -q 'omn-11421-native-demo-smoke' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-native-demo-smoke/command.yaml\""
  - id: "dod-console-script-smoke"
    description: "Installed console script produced a COMPLETE aggregate after package reinstall."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-console-script-smoke/command.yaml\"
          && grep -q 'Aggregation status: COMPLETE' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-console-script-smoke/command.yaml\"
          && grep -q 'omn-11421-console-script-smoke' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-console-script-smoke/command.yaml\""
  - id: "dod-legacy-wrapper-smoke"
    description: "scripts/demo.py --live delegates to the native entropy workflow and no longer uses GEMINI_ENDPOINT."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-legacy-wrapper-smoke/command.yaml\"
          && grep -q 'Delegating to OmniNode-native entropy workflow' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-legacy-wrapper-smoke/command.yaml\"
          && grep -q 'Aggregation status: COMPLETE' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-legacy-wrapper-smoke/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11421 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11421/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
