---
schema_version: "1.0.0"
ticket_id: "OMN-13694"
title: "Extract hardcoded omniclaude event topic prefix to named constant"
summary: >-
  omnimarket PR #1492 replaces the hardcoded "onex.evt.omniclaude." prefix in node_bus_audit_compute with
  OMNICLAUDE_EVT_TOPIC_PREFIX from omnimarket.events.topics. The change keeps runtime behavior unchanged
  while making the canonical omniclaude event-topic prefix explicit, importable, and test-pinned.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1492"
    description: >-
      omnimarket PR #1492 carries the OMN-13694 implementation at head commit d0dd9bbe539cecb1e1ad243b8ab4803b25a4f978.
      The diff adds OMNICLAUDE_EVT_TOPIC_PREFIX, imports it in handler_bus_audit_compute, and adds unit
      coverage pinning the constant value plus the unregistered omniclaude topic audit path.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13694/dod-omnimarket-pr-1492/command.yaml &&
          grep -q '^pr_number: 1492$' drift/dod_receipts/OMN-13694/dod-omnimarket-pr-1492/command.yaml
          && grep -q '^commit_sha: "d0dd9bbe539cecb1e1ad243b8ab4803b25a4f978"$' drift/dod_receipts/OMN-13694/dod-omnimarket-pr-1492/command.yaml
  - id: "dod-local-validation"
    description: >-
      PR #1492 records local validation evidence: grep confirms the hardcoded prefix is absent from handler_bus_audit_compute.py,
      the named constant is importable, the non-Kafka pytest suite passed, strict mypy passed, and pre-commit
      passed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13694/dod-local-validation/command.yaml && grep
          -q '7416 passed, 0 failed' drift/dod_receipts/OMN-13694/dod-local-validation/command.yaml &&
          grep -q 'mypy src/omnimarket/ --strict' drift/dod_receipts/OMN-13694/dod-local-validation/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for omnimarket PR #1492. The PR touches runtime handler code but only replaces
      a literal prefix with a same-value named constant; no live deploy, runtime restart, topic mutation,
      schema migration, database migration, or .201/.200 mutation was performed by this evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13694/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-13694/dod-deploy-assessment/command.yaml &&
          grep -q '^pr_number: 1492$' drift/dod_receipts/OMN-13694/dod-deploy-assessment/command.yaml
  - id: "dod-occ-pr-3336"
    description: >-
      Self-binding OCC eligibility receipt for onex_change_control PR #3336, which carries the OMN-13694
      contract and receipts onto dev so downstream omnimarket PR #1492 can cite Evidence-Source: OCC#3336.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13694/dod-occ-pr-3336/command.yaml && grep -q
          '^pr_number: 3336$' drift/dod_receipts/OMN-13694/dod-occ-pr-3336/command.yaml
