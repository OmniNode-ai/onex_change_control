---
schema_version: "1.0.0"
ticket_id: "OMN-12546"
title: "S-1b infra re-export of promoted dispatch models from core"
summary: >-
  Bind omnibase_infra PR #2119, the S-1b companion to omnibase_core PR #1351, to central OCC evidence
  after repinning the infra dependency to the merged core commit. The infra PR re-exports ModelDispatchResult,
  ModelDispatchOutputs, ModelDispatchMetadata, and ModelHandlerRef from omnibase_core so existing omnibase_infra
  import paths resolve to the canonical core model classes.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2119-head"
    description: >-
      omnibase_infra PR #2119 is bound to the refreshed S-1b head after the dependency pin moved from
      the pre-merge core feature commit to the merged omnibase_core #1351 commit.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12546/dod-infra-pr-2119-head/command.yaml"
  - id: "dod-infra-pr-2119-local-gates"
    description: >-
      Focused local proof passed for the refreshed infra pin: pre-commit accepted pyproject.toml and uv.lock,
      and the four infra import paths resolve to the same class objects exported by omnibase_core.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12546/dod-infra-pr-2119-local-gates/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding OCC receipt for the onex_change_control PR that carries this OMN-12546 contract and
      the product receipts consumed by omnibase_infra PR #2119 through Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12546/dod-occ-pr-self/command.yaml"
