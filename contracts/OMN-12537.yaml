---
schema_version: "1.0.0"
ticket_id: "OMN-12537"
title: "Stabilize Codex runtime adapter terminal replies"
summary: >-
  Bind central evidence for omnimarket PR #1008, which fixes the Codex runtime adapter so deployed
  runtime dispatch waits for terminal readiness before publishing, uses a no-group direct terminal
  listener for one-shot terminal replies, and preserves node terminal payloads from complete events.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1008"
    description: "Bind omnimarket PR #1008 head and runtime adapter stabilization changes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-omnimarket-pr-1008/command.yaml"
          && grep -q '^pr_number: 1008$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-omnimarket-pr-1008/command.yaml"
          && grep -q '^commit_sha: "a5454b0e8fb8bfaaec07aa14ee5b8bdd28bb2cba"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-omnimarket-pr-1008/command.yaml"
  - id: "dod-local-validation"
    description: "Local static and focused runtime adapter validation passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-local-validation/command.yaml"
          && grep -q 'py_compile PASS' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-local-validation/command.yaml"
          && grep -q 'ruff check PASS' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-local-validation/command.yaml"
          && grep -q '6 passed, 48 deselected' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-local-validation/command.yaml"
  - id: "dod-runtime-dispatch-proof"
    description: ".201 dev runtime proof shows deployed session_orchestrator dispatch returns the terminal
      complete event through the Codex runtime adapter."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-runtime-dispatch-proof/command.yaml"
          && grep -q 'ok: true' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-runtime-dispatch-proof/command.yaml"
          && grep -q 'status: complete' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-runtime-dispatch-proof/command.yaml"
          && grep -q 'terminal_topic: onex.evt.omnimarket.session-orchestrator-completed.v1' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-runtime-dispatch-proof/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12537 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-occ-pr-self/command.yaml"
          && grep -q '^pr_number: 1989$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12537/dod-occ-pr-self/command.yaml"
