---
schema_version: "1.0.0"
ticket_id: "OMN-12904"
title: "Move projection API database binding from env to contract overlay"
summary: >-
  Add typed projection API database binding and overlay-file support so dashboard projection reads can
  resolve their database DSN from explicit runtime config instead of ambient `OMNIDASH_ANALYTICS_DB_URL`
  / `OMNIBASE_INFRA_DB_URL` env authority.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1146"
    description: >-
      omnimarket PR #1146 carries the OMN-12904 implementation that adds typed projection database binding,
      overlay-file loading, and secret-ref support.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12904/dod-omnimarket-pr-1146/command.yaml"
  - id: "dod-focused-tests"
    description: >-
      Focused projection API tests, lint, format, and strict mypy checks pass for the OMN-12904 implementation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12904/dod-focused-tests/command.yaml"
  - id: "dod-no-env-overlay-proof"
    description: >-
      With `OMNIDASH_ANALYTICS_DB_URL` and `OMNIBASE_INFRA_DB_URL` unset, `_dsn()` resolves from `OMNIMARKET_PROJECTION_DATABASE_BINDING_OVERLAY`.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12904/dod-no-env-overlay-proof/command.yaml"
          && grep -q 'resolved from overlay selector' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12904/dod-no-env-overlay-proof/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      OMN-12904 changes runtime projection API configuration. No live `.201` deployment or restart was
      performed during PR validation; hosted runtime validation remains a post-merge deploy step.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12904/dod-deploy-assessment/command.yaml"
          && grep -q 'No live deployment or restart was performed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12904/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2425"
    description: >-
      OCC PR #2425 carries the central OMN-12904 contract and DoD receipts used by downstream receipt
      gates.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12904/dod-occ-pr-2425/command.yaml"
