---
schema_version: "1.0.0"
ticket_id: "OMN-12126"
title: "Graduate delegation wire DTOs to omnibase_core"
summary: >
  Central OCC evidence for graduating delegation wire DTOs from omnibase_compat into omnibase_core, retaining
  compat shims with explicit migration/removal comments while the core package becomes the canonical model
  owner.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1173"
    description: "omnibase_core PR #1173 carries the canonical delegation wire DTO implementation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12126/dod-core-pr-1173/command.yaml"
  - id: "dod-core-wire-tests"
    description: "Focused omnibase_core delegation wire DTO unit tests pass after validation hardening."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12126/dod-core-wire-tests/command.yaml"
  - id: "dod-core-precommit"
    description: "Touched-file omnibase_core pre-commit suite passes for the delegation wire changes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12126/dod-core-precommit/command.yaml"
  - id: "dod-compat-pr-120"
    description: "omnibase_compat PR #120 retains compatibility shims with migration target and removal-date
      comments."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12126/dod-compat-pr-120/command.yaml"
  - id: "dod-occ-pr-1762"
    description: "OCC PR #1762 binds the central OMN-12126 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12126/dod-occ-pr-1762/command.yaml"
