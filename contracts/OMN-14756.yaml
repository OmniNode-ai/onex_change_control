---
schema_version: 1.0.0
ticket_id: OMN-14756
title: Evidence for omnibase_infra#2337 Kafka transport face over EventBusKafka
summary: >
  Central OCC evidence for omnibase_infra#2337. The product PR adds KafkaTransport, the concrete Kafka
  implementation of the S1/S2 transport protocols for the single RuntimeDispatch lane, at exact product
  head 06ceb7342c16b346fd8262ac9027c6ef0a510024.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - protocols
evidence_requirements:
  - kind: ci
    description: PR  #2337 product head is exact for OMN-14756 evidence.
    command: 'PRODUCT_PR_NUMBER=2337; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=06ceb7342c16b346fd8262ac9027c6ef0a510024;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
  - kind: ci
    description: Product-readiness-shadow completed successfully for exact PR  #2337 head.
    command: 'PRODUCT_RUN_ID=29631015970; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=06ceb7342c16b346fd8262ac9027c6ef0a510024;
      : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
      = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
      = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnibase_infra-pr-2337
    description: >
      PR #2337 on OmniNode-ai/omnibase_infra exists and is bound to OMN-14756 at exact product head 06ceb7342c16b346fd8262ac9027c6ef0a510024.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2337; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=06ceb7342c16b346fd8262ac9027c6ef0a510024;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnibase_infra-pr-2337-diff
    description: >
      PR #2337 on OmniNode-ai/omnibase_infra has a non-empty reviewed product diff for OMN-14756.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2337; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q .'
  - id: dod-omnibase_infra-pr-2337-product-shadow
    description: >
      Product-readiness-shadow completed successfully for omnibase_infra PR #2337 at exact head 06ceb7342c16b346fd8262ac9027c6ef0a510024
      before OCC evidence was minted.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29631015970; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=06ceb7342c16b346fd8262ac9027c6ef0a510024;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-omnibase_infra-pr-2337-local-verification
    description: >
      Product PR body records local/live verification for OMN-14756, including Kafka conformance, committed-offset
      readback, broker-free structural tests, ruff, mypy, pre-commit, and full unit suite.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2337; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "Kafka-side
          conformance parity" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body
          | grep -q "Full unit suite: 21550 passed, 40 skipped, 0 failed"'
  - id: dod-omnibase_infra-pr-2337-deploy
    description: >
      Deploy-readiness assessment for OMN-14756. The PR adds a Kafka transport implementation and tests,
      plus an omnibase_core pin bump and contract_resolver adaptation; no live deploy, restart, docker
      exec, rpk topic produce, or production mutation was performed by this OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14756/dod-omnibase_infra-pr-2337-deploy/command.yaml
          && grep -qi ''no live deploy, restart, docker exec, rpk topic produce, or production mutation''
          drift/dod_receipts/OMN-14756/dod-omnibase_infra-pr-2337-deploy/command.yaml'
  - id: occ-self-bind-pr-4368
    description: >
      OCC PR #4368 on OmniNode-ai/onex_change_control self-binding proof for OMN-14756 evidence.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
  - id: dod-infra2337-head-9ea74603
    description: >
      Current-head proof: omnibase_infra PR #2337 is bound to OMN-14756 at exact refreshed product head
      9ea74603b2e90c0cec9ab9f5017dfef637d7048e.
    evidence_artifact: supersedes_dod_evidence:dod-omnibase_infra-pr-2337
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2337; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=9ea74603b2e90c0cec9ab9f5017dfef637d7048e;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-infra2337-shadow-9ea74603
    description: >
      Product-readiness-shadow completed successfully for omnibase_infra PR #2337 at refreshed exact head
      9ea74603b2e90c0cec9ab9f5017dfef637d7048e.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29633971591; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=9ea74603b2e90c0cec9ab9f5017dfef637d7048e;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-infra2337-ci-9ea74603
    description: >
      Full CI completed successfully for omnibase_infra PR #2337 at refreshed exact head 9ea74603b2e90c0cec9ab9f5017dfef637d7048e.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29633971564; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=9ea74603b2e90c0cec9ab9f5017dfef637d7048e;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-infra2337-local-9ea74603
    description: >
      Current local verification proof: product PR body records Kafka conformance, broker-free structural
      tests, ruff, mypy, pre-commit, and full unit suite at refreshed head.
    evidence_artifact: supersedes_dod_evidence:dod-omnibase_infra-pr-2337-local-verification
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=2337; PRODUCT_REPO=OmniNode-ai/omnibase_infra; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "Kafka-side
          conformance parity" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body
          | grep -q "Full unit suite: 21553 passed, 40 skipped, 0 failed"'
  - id: dod-infra2337-deploy-9ea74603
    description: >
      Deploy-scope proof for refreshed OMN-14756 evidence: no live deploy, restart, docker exec, rpk topic
      produce, or production mutation was performed by this OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14756/dod-infra2337-deploy-9ea74603/command.yaml
          && grep -qi ''no live deploy, restart, docker exec, rpk topic produce, or production mutation''
          drift/dod_receipts/OMN-14756/dod-infra2337-deploy-9ea74603/command.yaml'
  - id: occ-self-bind-pr-4370
    description: >
      OCC PR #4370 on OmniNode-ai/onex_change_control self-binding proof for the append-only OMN-14756
      refreshed evidence supplement.
    evidence_artifact: supersedes_dod_evidence:occ-self-bind-pr-4368
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid
