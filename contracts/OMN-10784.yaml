---
schema_version: "1.0.0"
ticket_id: "OMN-10784"
title: "feat(OMN-10784): wire interactive executor into onboarding handler"
summary: >
  Extend handle_onboarding to detect policy_type: interactive and dispatch to InteractiveExecutor. Adds
  policy_name, dry_run, env_output_path to ModelOnboardingInput and provenance fields to ModelOnboardingOutput.
  DI outside models: input_adapter is a function parameter, not in Pydantic model.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1556 carries the OMN-10784 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10784/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Unit tests for interactive handler integration and DAG regression pass (12 tests)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10784/dod-local-tests/command.yaml\""
  - id: "dod-occ-contract"
    description: "OCC contract exists for OMN-10784."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10784/dod-occ-contract/command.yaml\""
