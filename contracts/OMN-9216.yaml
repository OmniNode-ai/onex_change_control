---
schema_version: "1.0.0"
ticket_id: "OMN-9216"
title: "Drain poison record on onex.cmd.omnimarket.pr-lifecycle-orchestrator-start.v1"
summary: >
  Investigate and drain the poison record on the pr-lifecycle-orchestrator-start topic that causes the
  PostToolUse crash-escalation guard (OMN-9085) to fire twice per session from log replay. Adds runbook
  documentation for future drain operations.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: "onex_change_control PR carrying the central OMN-9216 contract and receipt."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9216/dod-occ-pr-self/command.yaml"
  - kind: ci
    description: "omnimarket PR #734 adds the Kafka poison drain runbook."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9216/dod-omnimarket-pr-734/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carries the central OMN-9216 contract and receipt bundle."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9216/dod-occ-pr-self/command.yaml
          && grep -q '^ticket_id: OMN-9216$' drift/dod_receipts/OMN-9216/dod-occ-pr-self/command.yaml"
  - id: "dod-omnimarket-pr-734"
    description: "omnimarket PR #734 is open with the Kafka poison drain runbook at the recorded head
      SHA."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9216/dod-omnimarket-pr-734/command.yaml
          && grep -q '^pr_number: 734$' drift/dod_receipts/OMN-9216/dod-omnimarket-pr-734/command.yaml"
