---
schema_version: "1.0.0"
ticket_id: "OMN-13839"
title: "Skill executions snapshot projection topic"
summary: >
  omnimarket PR #1564 adds node_projection_skill_executions, a reducer that materializes skill lifecycle
  events into the skill_execution_snapshots table and exposes the projection API read model consumed by
  the omnidash skill-adoption widget. This central OCC contract binds the downstream PR to durable receipt
  evidence.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - topics
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr-omninode-13839-binding"
    description: >
      This onex_change_control PR carries the OMN-13839 central contract and receipt evidence required
      by downstream omnimarket PR #1564.
    source: "manual"
    status: verified
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13839/dod-occ-pr-omninode-13839-binding/command.yaml"
  - id: "dod-omnimarket-skill-exec-projection-pr-1564"
    description: >
      omnimarket PR #1564 materializes the skill executions snapshot projection, including contract, reducer
      runner, row model, migration, entry-point registration, projection API tests, and golden-chain coverage.
      Required CI checks pass at head 8782c776877110706c19f9a81c37a89fa966f219 after the projection-topic
      arch-lint description fix.
    source: "manual"
    status: verified
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13839/dod-omnimarket-skill-exec-projection-pr-1564/command.yaml
          && grep -q '^pr_number: 1564$' drift/dod_receipts/OMN-13839/dod-omnimarket-skill-exec-projection-pr-1564/command.yaml
          && grep -q '8782c776877110706c19f9a81c37a89fa966f219' drift/dod_receipts/OMN-13839/dod-omnimarket-skill-exec-projection-pr-1564/command.yaml"
