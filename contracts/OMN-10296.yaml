---
schema_version: 1.0.0
ticket_id: OMN-10296
title: 'Task 14: [Phase 2] Audit baselines-roi-card — confirm STAY-BESPOKE'
summary: 'Document the STAY-BESPOKE decision for BaselinesROICard; add ADR + inline code comment'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: docs
    description: ADR file exists at docs/adr/003-baselines-roi-card-stay-bespoke.md
    command: test -f docs/adr/003-baselines-roi-card-stay-bespoke.md
  - kind: ci
    description: Typecheck + lint + tests pass
    command: npm run check && npm run lint && npm run test:run
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: ADR file present in omnidash repo
    source: generated
    checks:
      - check_type: command
        check_value: test -f docs/adr/003-baselines-roi-card-stay-bespoke.md && echo 'ADR present'
  - id: dod-002
    description: STAY-BESPOKE comment present in BaselinesROICard.tsx citing OMN-10296
    source: generated
    checks:
      - check_type: command
        check_value: grep -q 'STAY-BESPOKE (OMN-10296)' src/components/dashboard/baselines/BaselinesROICard.tsx
          && echo 'comment present'
  - id: dod-003
    description: CI green (typecheck + lint + tests)
    source: generated
    checks:
      - check_type: command
        check_value: gh pr checks --repo OmniNode-ai/omnidash --watch 2>&1 | grep -E '(pass|success)'
