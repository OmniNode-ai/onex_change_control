---
schema_version: "1.0.0"
ticket_id: "OMN-11130"
title: "Phase 1: Merge Sweep and PR Triage"
summary: >
  Central OCC evidence for omnimarket PR #674 and omnibase_infra PR #1627. The changes fix merge-sweep
  PR inventory CI detection for the current GitHub CLI check schema and repair the runtime Pattern-B execution
  path by installing gh in runtime containers, passing GitHub auth, and isolating non-local Kafka consumer
  groups.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Focused inventory golden-chain tests pass."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11130/dod-inventory-tests/command.yaml"
  - kind: "manual"
    description: "Inventory and orchestrator golden-chain tests pass."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11130/dod-orchestrator-tests/command.yaml"
  - kind: "manual"
    description: "Focused ruff checks pass for touched files."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11130/dod-lint/command.yaml"
  - kind: "manual"
    description: "Live diagnostic proves CI no longer degrades to unknown."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11130/dod-live-diagnostic/command.yaml"
  - kind: "manual"
    description: "Deploy assessment is recorded for the runtime-path change."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11130/dod-deploy-assessment/command.yaml"
  - kind: "manual"
    description: "Evidence is bound to omnimarket PR #674 head."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11130/dod-omnimarket-pr-674/command.yaml"
  - kind: "manual"
    description: "OCC PR carries the OMN-11130 central contract and receipts."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11130/dod-occ-pr-1081/command.yaml"
  - kind: "manual"
    description: "Evidence is bound to omnibase_infra PR #1627 head."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11130/dod-omnibase-infra-pr-1627/command.yaml"
  - kind: "manual"
    description: "Infra runtime env and lane-isolation tests pass."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11130/dod-infra-runtime-tests/command.yaml"
  - kind: "manual"
    description: "Live Pattern-B runtime inventory returns open PRs after the runtime repair."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11130/dod-pattern-b-live-inventory/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-inventory-tests"
    description: "Focused inventory golden-chain tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11130/dod-inventory-tests/command.yaml\""
  - id: "dod-orchestrator-tests"
    description: "Inventory and orchestrator golden-chain tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11130/dod-orchestrator-tests/command.yaml\""
  - id: "dod-lint"
    description: "Focused ruff format and lint checks pass for touched files."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11130/dod-lint/command.yaml\""
  - id: "dod-live-diagnostic"
    description: "Live local diagnostic classifies PR CI as failure/success instead of unknown."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11130/dod-live-diagnostic/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "No live deploy, runtime restart, .201 mutation, or event-bus write was performed pre-merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11130/dod-deploy-assessment/command.yaml\""
  - id: "dod-omnimarket-pr-674"
    description: "Bind OCC evidence to omnimarket PR #674 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11130/dod-omnimarket-pr-674/command.yaml\""
  - id: "dod-occ-pr-1081"
    description: "Bind OMN-11130 central evidence to OCC PR #1081 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11130/dod-occ-pr-1081/command.yaml\""
  - id: "dod-omnibase-infra-pr-1627"
    description: "Bind OCC evidence to omnibase_infra PR #1627 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11130/dod-omnibase-infra-pr-1627/command.yaml\""
  - id: "dod-infra-runtime-tests"
    description: "Infra runtime env and lane-isolation tests pass for PR #1627."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11130/dod-infra-runtime-tests/command.yaml\""
  - id: "dod-pattern-b-live-inventory"
    description: "Live Pattern-B runtime inventory returns open PRs after the runtime repair."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11130/dod-pattern-b-live-inventory/command.yaml\""
