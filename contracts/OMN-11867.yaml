---
schema_version: "1.0.0"
ticket_id: "OMN-11867"
title: "Wire imperative contract guard into repo CI"
summary: >
  Wires the central imperative contract guard into handler-surface repositories so pull requests fail
  on unbaselined imperative node/handler drift. Also repairs the newly detected omnimarket LLM routing
  projection by moving dispatch truth into contract handler_routing.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-ci-guard"
    description: "omnibase_infra PR wires the central imperative contract guard and passes the repo scan
      locally"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-11867/dod-omnibase-infra-ci-guard/command.yaml\""
  - id: "dod-omniclaude-ci-guard"
    description: "omniclaude PR wires the central imperative contract guard and passes the repo scan locally"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-11867/dod-omniclaude-ci-guard/command.yaml\""
  - id: "dod-omniintelligence-ci-guard"
    description: "omniintelligence PR wires the central imperative contract guard and passes the repo
      scan locally"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-11867/dod-omniintelligence-ci-guard/command.yaml\""
  - id: "dod-omnimarket-ci-guard"
    description: "omnimarket PR wires the central guard, repairs routing projection dispatch, and passes
      the repo scan locally"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-11867/dod-omnimarket-ci-guard/command.yaml\""
  - id: "dod-omnimarket-deploy-gate"
    description: "omnimarket runtime-path change carries deploy-gate evidence for the routing projection
      handler contract repair"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-11867/dod-omnimarket-deploy-gate/command.yaml\"
          && printf '%s\\n' 'deploy evidence: omnimarket routing projection handler contract repair validated
          before merge'"
  - id: "dod-omnimemory-ci-guard"
    description: "omnimemory PR wires the central imperative contract guard and passes the repo scan locally"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-11867/dod-omnimemory-ci-guard/command.yaml\""
  - id: "dod-occ-evidence-pr"
    description: "onex_change_control PR carries the OMN-11867 OCC contract and downstream rollout receipts"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-11867/dod-occ-evidence-pr/command.yaml\""
