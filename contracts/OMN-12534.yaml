---
schema_version: "1.0.0"
ticket_id: "OMN-12534"
title: "Register dispatcher command topic event_type aliases"
summary: >
  Bind central evidence for omnibase_infra PR #1814, which fixes auto-wired dispatcher registration so
  literal subscribed command topics are registered as event_type aliases alongside semantic event aliases.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1814"
    description: "Bind OMN-12534 evidence to omnibase_infra PR #1814."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12534/dod-omnibase-infra-pr-1814/command.yaml
          && grep -q '^pr_number: 1814$' drift/dod_receipts/OMN-12534/dod-omnibase-infra-pr-1814/command.yaml
          && grep -q '^commit_sha: \"c2ef73322\"$' drift/dod_receipts/OMN-12534/dod-omnibase-infra-pr-1814/command.yaml"
  - id: "dod-focused-auto-wiring-tests"
    description: "Focused auto-wiring tests cover literal command-topic event_type alias registration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12534/dod-focused-auto-wiring-tests/command.yaml
          && grep -q '16 passed' drift/dod_receipts/OMN-12534/dod-focused-auto-wiring-tests/command.yaml"
  - id: "dod-auto-wiring-suite"
    description: "The full runtime auto-wiring unit suite passes with the dispatcher alias fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12534/dod-auto-wiring-suite/command.yaml
          && grep -q '239 passed' drift/dod_receipts/OMN-12534/dod-auto-wiring-suite/command.yaml"
  - id: "dod-ruff"
    description: "Ruff accepts the omnibase_infra dispatcher registration change."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12534/dod-ruff/command.yaml && grep
          -q 'All checks passed' drift/dod_receipts/OMN-12534/dod-ruff/command.yaml"
  - id: "dod-diff-check"
    description: "Git whitespace validation is clean for the omnibase_infra PR diff."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12534/dod-diff-check/command.yaml
          && grep -q 'git diff --check passed' drift/dod_receipts/OMN-12534/dod-diff-check/command.yaml"
  - id: "dod-live-dlq-root-cause"
    description: >
      Read-only .201 diagnostics established that direct command topics were consumed before the fix but
      dispatched to DLQ because the envelope event_type used the full topic string.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12534/dod-live-dlq-root-cause/command.yaml
          && grep -q 'No dispatcher found for onex.cmd.omnimarket.pr-lifecycle-orchestrator-start.v1'
          drift/dod_receipts/OMN-12534/dod-live-dlq-root-cause/command.yaml"
  - id: "dod-post-merge-runtime-deploy"
    description: >
      Runtime deploy DoD for OMN-12534: after PR #1814 lands, roll the .201 runtime-effects service and
      prove the dispatcher alias fix is loaded before running live Pattern B dispatch.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12534/dod-post-merge-runtime-deploy/command.yaml
          && grep -q 'deploy: ssh jonah@192.168.86.201' drift/dod_receipts/OMN-12534/dod-post-merge-runtime-deploy/command.yaml
          && grep -q 'docker exec omninode-runtime-effects' drift/dod_receipts/OMN-12534/dod-post-merge-runtime-deploy/command.yaml"
  - id: "dod-occ-pr-1979"
    description: "onex_change_control PR #1979 carries the OMN-12534 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12534/dod-occ-pr-1979/command.yaml
          && grep -q '^pr_number: 1979$' drift/dod_receipts/OMN-12534/dod-occ-pr-1979/command.yaml"
  - id: "dod-occ-pr-1980"
    description: "onex_change_control PR #1980 carries the OMN-12534 receipt formatting cleanup."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12534/dod-occ-pr-1980/command.yaml
          && grep -q '^pr_number: 1980$' drift/dod_receipts/OMN-12534/dod-occ-pr-1980/command.yaml"
  - id: "dod-occ-pr-1981"
    description: "onex_change_control PR #1981 carries the OMN-12534 runtime deploy DoD evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12534/dod-occ-pr-1981/command.yaml
          && grep -q '^pr_number: 1981$' drift/dod_receipts/OMN-12534/dod-occ-pr-1981/command.yaml"
