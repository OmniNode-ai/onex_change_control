---
schema_version: "1.0.0"
ticket_id: "OMN-18931"
title: "K4 construction-quality metric denominator acceptance"
summary: >-
  Parent K4 contract for OMN-18925. The final 0043z-to-0044-to-0045 chain preserves stored construction
  quality true and false facts while excluding the exact construction pair from every eligible quality
  numerator and denominator; ordinary and legacy-null rows remain eligible.
is_seam_ticket: true
interface_change: true
interfaces_touched: [events, envelopes]
contract_completeness: ENRICHED
proof_class: live-readback
evidence_requirements:
  - kind: tests
    description: >-
      A disposable PostgreSQL 16 run applies the final migration and vendor chain and asserts all named
      reader values for ordinary, legacy-null, construction-true, and construction-false rows.
    command: >-
      uv run --frozen pytest -q tests/test_omn19013_terminal_construction_metric_readers.py
  - kind: manual
    description: >-
      The final receipt retains 0043z, immutable 0044, and 0045 hashes; manifest bytes; PostgreSQL version;
      reader output; a predicate-removal negative control; and scratch database teardown/readback. Historical
      K4 or source-only named-view runs are not completion. It also retains same-route normal, 429, and
      503 terminals with attempt and escalation counts, keeping provider availability separate from content
      quality. Its PASS receipt binds reconciled Core, Market, and Infra commits, release and lock identities,
      and final 0043z, immutable 0044, and 0045 bytes.
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: k4-final-metric-denominator
    description: >-
      Planned K4 PG16 validator proves true and false quality preservation with denominator exclusion
      across the named readers. Final evidence adds immutable migration, vendor, manifest, scratch controls,
      and normal/429/503 attempt-and-escalation records so availability does not become quality evidence.
    source: manual
    status: pending
    execution_scope: local_done_gate
    checks:
      - check_type: test_passes
        check_value: >-
          uv run --frozen pytest -q tests/test_omn19013_terminal_construction_metric_readers.py
        cwd: "${OMNI_HOME}/omnimarket"
