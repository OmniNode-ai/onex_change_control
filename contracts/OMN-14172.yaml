---
schema_version: "1.0.0"
ticket_id: "OMN-14172"
title: "ci(OMN-14172): fail-closed gate against integration silent-skip false-greens"
summary: >-
  Central OCC evidence for omnimarket PR #1652. The PR adds a fail-closed integration silent-skip guard
  to omnimarket CI, including data-driven service skip policy, local self-test coverage, and CI summary
  wiring. This evidence PR performs no live deploy, runtime restart, branch-protection mutation, or bypass.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1652"
    description: >-
      omnimarket PR #1652 is open against dev at head cc7303e4976772620506420f12802fa3deffee5b and carries
      the integration silent-skip guard implementation plus CI/pre-commit wiring.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14172/dod-omnimarket-pr-1652/command.yaml &&
          grep -q '^pr_number: 1652$' drift/dod_receipts/OMN-14172/dod-omnimarket-pr-1652/command.yaml
          && grep -q '^commit_sha: cc7303e4976772620506420f12802fa3deffee5b$' drift/dod_receipts/OMN-14172/dod-omnimarket-pr-1652/command.yaml
  - id: "dod-silent-skip-proof"
    description: >-
      PR #1652 records proof that the guard fails on provisioned-service false-greens and passes for provisioned
      execution with only optional skips.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14172/dod-silent-skip-proof/command.yaml &&
          grep -q '11/11' drift/dod_receipts/OMN-14172/dod-silent-skip-proof/command.yaml
  - id: "dod-no-live-deploy"
    description: >-
      Scope evidence: this is CI/test-process work only. The OCC PR performs no live deploy, no runtime
      restart, no branch-protection change, and no bypass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14172/dod-no-live-deploy/command.yaml && grep
          -qi 'no live deploy' drift/dod_receipts/OMN-14172/dod-no-live-deploy/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding receipt: onex_change_control PR #3741 carries the central OMN-14172 contract and PASS
      receipts consumed by omnimarket PR #1652 via Evidence-Source after merge.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14172/dod-occ-pr-self/command.yaml && grep -q
          '^pr_number: 3741$' drift/dod_receipts/OMN-14172/dod-occ-pr-self/command.yaml
