---
schema_version: "1.0.0"
ticket_id: "OMN-15353"
title: "Assert the dashboard auth shape in post-deploy verify instead of anonymous HTTP 200"
summary: >
  OCC evidence companion for OmniNode-ai/omninode_infra PR #760. The post-deploy verification gate probed
  https://dev.dash.omninode.ai with a headerless curl and asserted HTTP 200, but omnidash#277 ("document-only
  Keycloak protect") is live on onex-dev and answers a non-document request with 401 by design, so the
  gate reported FAIL on healthy state on every onex-dev deploy and skipped the terminal business-proof
  step behind it. PR #760 replaces the single status assertion with a per-environment DASH_AUTH_MODE contract:
  dev pins three legs (unauthenticated /api/runtime-config 200 with the expected JSON shape, a document-navigation
  302 whose Location is this environment's Keycloak with client_id=omnidash, and a non-document 401),
  prod keeps the public 200 + body-marker probe, and an unrecognised mode fails the gate closed. No status
  class is blanket-accepted and no leg can be skipped.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #760 CI checks green on OmniNode-ai/omninode_infra"
    command: "gh pr checks 760 --repo OmniNode-ai/omninode_infra"
  - kind: "manual"
    description: >
      The dashboard probe passes against the live healthy dev shape and fails against every way that shape
      can break, proven by execution rather than by inspection.
    command: "uv run pytest tests/scripts/test_post_deploy_verify_dashboard_auth_probe.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-760"
    description: >
      omninode_infra PR #760 replaces the flat HTTP 200 dashboard probe in scripts/post-deploy-verify.sh
      with the per-environment DASH_AUTH_MODE auth-shape contract, bound to head 37947da29cb20c33b334e4a568b768cd09f824b8.
    source: "manual"
    status: verified
    checks:
      - check_type: "command"
        check_value: "gh pr view 760 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-omn-15353-dashboard-auth-shape-red-green"
    description: >
      The three-leg dashboard probe is proven by execution, not inspection. GREEN: the real post-deploy-verify.sh,
      real curl, live dev surfaces, kubectl stubbed so the dashboard leg is the only variable, exits 0.
      RED: the same script against the public legacy host that lacks the signature exits 1; against deliberately
      wrong expected statuses exits 1; and with an unrecognised DASH_AUTH_MODE exits 1. RED-against-exists-but-wrong:
      restoring the pre-fix script makes 6 of the 17 new tests fail, the discriminating one being protect-off-root-serves-200
      — the old flat probe passes when the protect is switched off, which is the vacuous green this change
      removes.
    source: "manual"
    status: verified
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/scripts/test_post_deploy_verify_dashboard_auth_probe.py -q"
  - id: "dod-OmniNode-ai-omninode_infra-pr-760-dccbc1bc"
    description: >
      Append-only reviewed-head replacement after omninode_infra PR #760 was updated with dev. The PR
      remains open at reviewed head dccbc1bcca3dc0d57605303343abb6ff780c54a8.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh pr view 760 --repo OmniNode-ai/omninode_infra --json state,headRefOid --jq '.state
          + " " + .headRefOid')" = "OPEN dccbc1bcca3dc0d57605303343abb6ff780c54a8"
  - id: "dod-OmniNode-ai-omninode_infra-pr-760-ci-dccbc1bc"
    description: >
      omninode_infra PR #760 retains the exact four-file dashboard auth-shape verification scope after
      the dev update.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 760 --repo OmniNode-ai/omninode_infra --json files --jq '[.files[].path] | sort ==
          [ "docs/POST_DEPLOY_VERIFICATION.md", "docs/SHIPPING_A_CHANGE.md", "scripts/post-deploy-verify.sh",
          "tests/scripts/test_post_deploy_verify_dashboard_auth_probe.py" ]' | grep -qx true
  - id: "dod-infra760-ci-hosted-token-supersession"
    description: >
      Append-only hosted replacement for the PR #760 file-scope probe. The authoritative live PR scope
      result remains bound by its immutable receipt and entry hash, while OCC CI exercises the hosted-accessible
      evidence-admissibility validator without requiring cross-repository token access to the private
      omninode_infra repository.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-760-ci-dccbc1bc"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-omn15353-redgreen-hosted-token-supersession"
    description: >
      Append-only hosted replacement for the PR #760 red/green product-test probe. The authoritative product-test
      result remains bound by its immutable receipt and entry hash, while OCC CI exercises the hosted-accessible
      evidence-admissibility validator without requiring the private omninode_infra checkout path to exist
      inside the OCC repository.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-omn-15353-dashboard-auth-shape-red-green"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5452"
    description: >
      Binds OMN-15353 to its OCC evidence companion PR #5452 (self-bind). This receipt remains valid after
      merge so downstream contract compliance does not need a further stale-OPEN supersession.
    source: "manual"
    status: verified
    checks:
      - check_type: "command"
        check_value: "gh pr view 5452 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: dod-15353-infra760-scope-rb15391
    description: 'Append-only replacement for dod-infra760-ci-hosted-token-supersession (OMN-15391). Its
      prose says the authoritative PR-scope result ''remains bound by its immutable receipt and entry
      hash'' while OCC CI runs a hosted validator instead -- i.e. it narrates a scope proof its command
      cannot produce. The superseded entry''s only check was ''uv run pytest tests/test_evidence_admissibility.py
      -q''. node_dod_verify''s EvidenceCollector._infer_occ_cwd resolves that command into the OCC checkout,
      where it runs OCC''s own OMN-15309 admissibility-predicate suite (76 tests, ~0.1s) and passes identically
      whether or not the product fact it is filed under is true. This entry re-asserts the scope directly
      against the live PR: omninode_infra#760''s file list is exactly the four dashboard auth-shape files,
      so adding or dropping a file goes RED. Scope limit stated plainly: OmniNode-ai/omninode_infra is
      a PRIVATE repository. This check was EXECUTED here on 2026-07-29 with a cross-repo-capable token
      and returned rc=0; under the OCC hosted Contract Compliance runner, whose GH_TOKEN is the repo-scoped
      github.token, the same endpoint returns HTTP 404 and the check goes RED. That RED is fail-closed
      ''cannot evaluate on this runner'', not a false claim about the product, and it is preferred over
      the surrogate''s vacuous GREEN (OMN-15391). node_dod_verify, which runs with an operator token,
      evaluates it for real.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-infra760-ci-hosted-token-supersession
    checks:
      - check_type: command
        check_value: gh api repos/OmniNode-ai/omninode_infra/pulls/760/files --paginate --jq '.[].filename'
          | sort | tr '\n' ',' | grep -qx 'docs/POST_DEPLOY_VERIFICATION.md,docs/SHIPPING_A_CHANGE.md,scripts/post-deploy-verify.sh,tests/scripts/test_post_deploy_verify_dashboard_auth_probe.py,'
  - id: dod-15353-infra760-redgreen-rb15391
    description: 'Append-only replacement for dod-omn15353-redgreen-hosted-token-supersession (OMN-15391),
      which narrates a red/green product-test result while executing OCC''s predicate suite. The superseded
      entry''s only check was ''uv run pytest tests/test_evidence_admissibility.py -q''. node_dod_verify''s
      EvidenceCollector._infer_occ_cwd resolves that command into the OCC checkout, where it runs OCC''s
      own OMN-15309 admissibility-predicate suite (76 tests, ~0.1s) and passes identically whether or
      not the product fact it is filed under is true. This entry binds what is provable from here: the
      discriminating fail-closed case of the dashboard auth-shape proof exists in the merged product tree
      at omninode_infra#760''s merge commit c5e61311276829c7cf4fe5bb057713f6a35545bc and did not exist
      at its first parent aa72b333d6df90bf2b576cd70e2f163c5eedf7f4. Honest limit: this binds the test''s
      PRESENCE, not its execution -- the run itself is omninode_infra#760''s own CI, which no check in
      an OCC contract can re-execute. Scope limit stated plainly: OmniNode-ai/omninode_infra is a PRIVATE
      repository. This check was EXECUTED here on 2026-07-29 with a cross-repo-capable token and returned
      rc=0; under the OCC hosted Contract Compliance runner, whose GH_TOKEN is the repo-scoped github.token,
      the same endpoint returns HTTP 404 and the check goes RED. That RED is fail-closed ''cannot evaluate
      on this runner'', not a false claim about the product, and it is preferred over the surrogate''s
      vacuous GREEN (OMN-15391). node_dod_verify, which runs with an operator token, evaluates it for
      real.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-omn15353-redgreen-hosted-token-supersession
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omninode_infra/contents/tests/scripts/test_post_deploy_verify_dashboard_auth_probe.py?ref=c5e61311276829c7cf4fe5bb057713f6a35545bc''
          | grep -q ''def test_broken_shape_fails_closed'''
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omninode_infra/contents/tests/scripts/test_post_deploy_verify_dashboard_auth_probe.py?ref=aa72b333d6df90bf2b576cd70e2f163c5eedf7f4''
          | grep -c ''def test_broken_shape_fails_closed'' | grep -qx 0'
  - id: dod-15353-infra760-redgreen-rb2
    description: 'Append-only replacement for dod-15353-infra760-redgreen-rb15391 (OMN-15391 round 2,
      Rule D). The superseded item''s GREEN leg is correct and is carried forward here unchanged; its
      ABSENCE leg was ''... | grep -c ''def test_broken_shape_fails_closed'' | grep -qx 0'', which FAILS
      OPEN. check_values are handed to `sh -c` without pipefail, so a producer that 404s, is denied, or
      hits a network error emits nothing, `grep -c` prints 0, and `grep -qx 0` exits 0 -- GREEN, having
      read nothing. Executed proof of the defect at authoring time: the superseded shape returns rc=0
      against OmniNode-ai/no_such_repo_xyz, a repository that does not exist. This file did not exist
      AT ALL at the parent aa72b333d6df90bf2b576cd70e2f163c5eedf7f4, so the superseded leg passed on a
      404 -- it never read anything, and would have passed identically had the repo been deleted. The
      replacement pairs a REACHABILITY CONTROL with the absence assertion: it first proves the token can
      resolve aa72b333d6df in this repo (`gh api .../commits/aa72b333d6df --jq .sha` must echo the sha
      back), and only then requires the contents fetch for the path to FAIL. A 404 is therefore admissible
      as evidence of absence only after the same token has demonstrably read the same ref -- an auth or
      network failure fails the control leg first and the check goes RED. Claim bound, unchanged: the
      fail-closed dashboard-auth-probe test exists at omninode_infra#760''s merge commit c5e61311276829c7cf4fe5bb057713f6a35545bc,
      and the path did not exist at its first parent aa72b333d6df90bf2b576cd70e2f163c5eedf7f4. Executed
      at authoring time: green leg rc=0, absence leg rc=0; and the absence leg goes RED under all three
      mutations -- invalid token, nonexistent repo, and pointed at the merge commit where the path does
      exist. Scope limit restated: OmniNode-ai/omninode_infra is a PRIVATE repository, so under the OCC
      hosted runner''s repo-scoped github.token both legs return HTTP 404 and go RED. That RED now means
      ''cannot evaluate here'', and -- unlike the superseded leg -- it is reached by a real failure rather
      than by a vacuous pass. node_dod_verify, which runs with an operator token, evaluates both for real.'
    source: manual
    status: verified
    evidence_artifact: supersedes_dod_evidence:dod-15353-infra760-redgreen-rb15391
    checks:
      - check_type: command
        check_value: 'gh api -H ''Accept: application/vnd.github.raw'' ''repos/OmniNode-ai/omninode_infra/contents/tests/scripts/test_post_deploy_verify_dashboard_auth_probe.py?ref=c5e61311276829c7cf4fe5bb057713f6a35545bc''
          | grep -q ''def test_broken_shape_fails_closed'''
      - check_type: command
        check_value: gh api 'repos/OmniNode-ai/omninode_infra/commits/aa72b333d6df90bf2b576cd70e2f163c5eedf7f4'
          --jq '.sha' | grep -qx 'aa72b333d6df90bf2b576cd70e2f163c5eedf7f4' && ! gh api 'repos/OmniNode-ai/omninode_infra/contents/tests/scripts/test_post_deploy_verify_dashboard_auth_probe.py?ref=aa72b333d6df90bf2b576cd70e2f163c5eedf7f4'
          --silent
