---
schema_version: "1.0.0"
ticket_id: OMN-9411
summary: "feat(deploy-agent): signed trigger helper + auth tests (root fix for OMN-9393)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on omnibase_infra PR 1367"
    command: "gh pr checks 1367 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1367 --repo OmniNode-ai/omnibase_infra --json state -q .state"
  - id: "dod-002"
    description: "Contract artifact file present in repo"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-9411.yaml"
  - id: "dod-003"
    description: "trigger helper script present in omnibase_infra PR branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1367 --repo OmniNode-ai/omnibase_infra --json files -q '[.files[].path]
          | any(. == \"scripts/deploy-agent/deploy-agent-trigger.sh\")"
