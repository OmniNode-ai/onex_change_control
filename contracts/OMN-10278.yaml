---
schema_version: "1.0.0"
ticket_id: OMN-10278
title: "Mandatory DI params — resolver multi-param injection + handler migration"
summary: "Extend ServiceHandlerResolver Step 4 to inject any combination of known params (event_bus, container,
  ownership_query); remove = None defaults from 19 injectable handler init params across omnibase_core,
  omnibase_infra, and omnimarket."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: FULL
evidence_requirements:
  - kind: tests
    description: "Unit tests green in omnibase_core (resolver + enum)"
    command: "uv run pytest tests/unit/enums/test_enum_handler_resolution_outcome.py tests/unit/services/test_service_handler_resolver.py
      tests/unit/services/test_service_handler_resolver_multi_param.py -v"
  - kind: ci
    description: "CI pipeline green on omnibase_core PR 1057"
    command: "gh pr checks 1057 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "RESOLVED_VIA_KNOWN_PARAMS added to EnumHandlerResolutionOutcome"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10278/dod-001/command.yaml && grep
          -q '^pr_number: 681$' drift/dod_receipts/OMN-10278/dod-001/command.yaml"
  - id: dod-session-id-bridge
    description: "Session-ID bridge: unit tests and E2E proof-of-life pass on omniclaude PR #1558"
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10278/dod-session-id-bridge/command.yaml
          && grep -q '^pr_number: 1558$' drift/dod_receipts/OMN-10278/dod-session-id-bridge/command.yaml"
  - id: dod-occ-pr-912
    description: "Self-binding receipt for OCC PR #912."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10278/dod-occ-pr-912/command.yaml
          && grep -q '^ticket_id: OMN-10278$' drift/dod_receipts/OMN-10278/dod-occ-pr-912/command.yaml
          && grep -q '^pr_number: 912$' drift/dod_receipts/OMN-10278/dod-occ-pr-912/command.yaml"
  - id: dod-core-known-params-enum
    description: "RESOLVED_VIA_KNOWN_PARAMS added to EnumHandlerResolutionOutcome on omnibase_core PR #1057"
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr diff 1057 --repo OmniNode-ai/omnibase_core | grep -q 'RESOLVED_VIA_KNOWN_PARAMS'"
  - id: dod-core-known-providers
    description: "Resolver Step 4 extended to multi-param known injection"
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr diff 1057 --repo OmniNode-ai/omnibase_core | grep -q '_known_providers'"
  - id: dod-core-tests
    description: "Unit tests pass — 69 green in resolver+enum tests"
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr checks 1057 --repo OmniNode-ai/omnibase_core | grep -c 'pass'"
  - id: dod-core-pr-open
    description: "omnibase_core PR 1057 open against main"
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr view 1057 --repo OmniNode-ai/omnibase_core --json state,baseRefName --jq 'select(.state==\"OPEN\"
          and .baseRefName==\"main\") | .state'"
  - id: dod-005
    description: "Post-merge deploy verification — runtime boots without handler TypeError"
    source: manual
    checks:
      - check_type: command
        check_value: "ssh jonah@192.168.86.201 'docker logs omninode-runtime 2>&1 | grep -c \"TypeError\"'
          | grep -q '^0$' && echo 'deploy verified: no TypeError in runtime logs'"
