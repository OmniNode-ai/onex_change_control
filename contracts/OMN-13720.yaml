---
schema_version: "1.0.0"
ticket_id: "OMN-13720"
title: "Consolidate EnumBudgetAction to omnibase_core canonical — remove inline definitions in omnimarket"
summary: >-
  Pure code-consolidation refactor in omnimarket. EnumBudgetAction (CONTINUE/WARN/THROTTLE/ABORT) was
  defined inline in three places; this PR removes the two omnimarket duplicates and redirects all consumers
  to the canonical omnibase_core.enums.enum_budget_action import path. No new runtime surface, event-bus
  topic, REST endpoint, node contract, or live infrastructure mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Full pytest suite passes (pre-existing Kafka integration failures exempted)"
    command: "uv run pytest tests/ -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr"
    description: >-
      omnimarket PR for OMN-13720 carries the consolidated import refactor with all 29 budget-policy tests
      passing, cross-node reach-in allowlist shrunk to 83 entries (from 84), and mypy --strict clean.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'OMN-13720' drift/dod_receipts/OMN-13720/dod-omnimarket-pr/command.yaml
