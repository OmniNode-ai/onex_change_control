---
schema_version: "1.0.0"
ticket_id: "OMN-13720"
title: "Consolidate EnumBudgetAction to omnibase_core canonical — remove inline definitions in omnimarket"
summary: >-
  Pure code-consolidation refactor in omnimarket. EnumBudgetAction (CONTINUE/WARN/THROTTLE/ABORT) was
  defined inline in three places; this PR removes the two omnimarket duplicates and redirects all consumers
  to the canonical omnibase_core.enums.enum_budget_action import path. No new runtime surface, event-bus
  topic, REST endpoint, node contract, or live infrastructure mutation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "PASS receipts bind targeted omnimarket evidence for the EnumBudgetAction consolidation."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13720/dod-omnimarket-pr/command.yaml &&
      grep -q '^status: PASS$' drift/dod_receipts/OMN-13720/dod-mypy-grep/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-13720/dod-deploy-assessment/command.yaml && grep -q '^status: PASS$'
      drift/dod_receipts/OMN-13720/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr"
    description: >-
      omnimarket PR for OMN-13720 carries the consolidated import refactor with the targeted budget-policy
      tests and cross-node reach-in guard passing.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13720/dod-omnimarket-pr/command.yaml"
  - id: "dod-mypy-grep"
    description: >-
      Targeted strict mypy passes for the budget-policy surface, and no inline class EnumBudgetAction
      definitions remain under src/.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13720/dod-mypy-grep/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment for the runtime-path touch: this is a pure canonical enum import consolidation
      with no live deploy, restart, broker topic, or runtime mutation required before merge.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13720/dod-deploy-assessment/command.yaml
          && grep -q 'deploy-gate' drift/dod_receipts/OMN-13720/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13720 contract and receipts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13720/dod-occ-self/command.yaml"
