---
schema_version: "1.0.0"
ticket_id: "OMN-11170"
title: "Flip migration inventory check from advisory to blocking"
summary: >
  Remove the --warn-only flag from the check-migration-inventory CI step so cross-repo migration inventory
  drift fails the build instead of merely warning, and regenerate the inventory so the blocking gate passes
  against current on-disk migrations. Part of OMN-10972 (22-gate hardening sweep). Delivery via onex_change_control
  PR #1132.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Migration inventory CI step no longer passes --warn-only"
    command: "grep -n 'check-migration-inventory' .github/workflows/ci.yml"
  - kind: "manual"
    description: "Inventory validates clean against all cloned repos"
    command: "uv run check-migration-inventory --repos-root /tmp/repos"
  - kind: "ci"
    description: "onex_change_control PR #1132 checks pass before merge"
    command: "gh pr checks 1132 --repo OmniNode-ai/onex_change_control --watch=false"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-inventory-gate-blocking"
    description: "The check-migration-inventory CI step no longer carries --warn-only."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11170/dod-inventory-gate-blocking/command.yaml\""
  - id: "dod-inventory-validates-clean"
    description: "Regenerated migration inventory validates with zero errors against all repos."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11170/dod-inventory-validates-clean/command.yaml\""
  - id: "dod-occ-pr"
    description: "Self-binding receipt for OCC PR #1132 carrying the OMN-11170 contract and receipt bundle."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11170/dod-occ-pr/command.yaml\""
