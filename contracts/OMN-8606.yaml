---
schema_version: "1.0.0"
ticket_id: OMN-8606
summary: "omnidash migration number collisions (5 groups, 11 files)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json state -q .state"
  - id: "dod-002"
    description: "Migration idempotency: 0056 constraint guarded with DO block"
    source: "generated"
    checks:
      - check_type: "file_exists"
        check_value: "migrations/0056_session_post_mortems.sql"
  - id: "dod-003"
    description: "Fresh-install parity: 0063 includes token columns"
    source: "generated"
    checks:
      - check_type: "grep"
        check_value:
          pattern: "prompt_tokens"
          path: "migrations/0063_llm_routing_decisions.sql"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
