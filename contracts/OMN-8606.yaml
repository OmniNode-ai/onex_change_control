---
schema_version: "1.0.0"
ticket_id: OMN-8606
summary: "omnidash migration number collisions (5 groups, 11 files)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view {pr} --repo {repo} --json state -q .state"
  - id: "dod-002"
    description: "omnidash PR #598 migration idempotency fix pushed"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 598 --repo OmniNode-ai/omnidash --json headRefName -q .headRefName"
  - id: "dod-003"
    description: "dod-001 command structure present in contract YAML"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep 'json state -q .state' contracts/OMN-8606.yaml"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
