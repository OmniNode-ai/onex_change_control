---
schema_version: "1.0.0"
ticket_id: "OMN-11507"
title: "Add typed architecture surface model"
summary: >
  Add typed architecture surface coverage models and reuse/bespoke metrics for the onex-self-extending-agent
  entropy comparison experiment.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "tests"
    description: "Architecture surface unit tests pass at the recorded PR head."
    command: "uv run pytest tests/unit/test_architecture_surfaces.py -q"
  - kind: "manual"
    description: "onex-self-extending-agent PR #91 is open against main at the recorded head SHA."
    command: "gh pr view 91 --repo OmniNode-ai/onex-self-extending-agent --json number,url,headRefOid,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-91"
    description: "onex-self-extending-agent PR #91 carries OMN-11507 architecture surfaces at the recorded
      head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11507/dod-product-pr-91/command.yaml"
          && grep -q '^pr_number: 91$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11507/dod-product-pr-91/command.yaml"
          && grep -q '^commit_sha: "09f228cde1c8eeca33c6bfc18a8b69b67698ebbc"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11507/dod-product-pr-91/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11507 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11507/dod-occ-self/command.yaml"
          && grep -q '^ticket_id: "OMN-11507"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11507/dod-occ-self/command.yaml"
