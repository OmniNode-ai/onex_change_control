---
schema_version: "1.0.0"
ticket_id: "OMN-13846"
title: "fix(OMN-13846): restore OMN-13244 empty hooks measurement baseline"
summary: >-
  omniclaude PR #1832 reverts the hook re-enable landed by OMN-13835 (PR #1831) and restores the deliberate
  OMN-13244 empty hooks measurement baseline (plugins/onex/hooks/hooks.json == {"hooks": {}}, byte-identical
  to commit 92ff78b38). The skill-substitution guard machinery added by #1831 is retained on disk but
  left unregistered so re-activation is an explicit operator decision. A regression test locks in both
  invariants (empty hooks object with metadata keys retained, and guard files present on disk).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1832"
    description: >-
      Consuming-PR binding: omniclaude PR #1832 resets plugins/onex/hooks/hooks.json to the OMN-13244
      empty baseline (byte-identical to 92ff78b38), retains the four guard files on disk unregistered,
      and adds the regression test tests/hooks/test_hooks_baseline_omn_13244.py at the recorded head commit.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13846/dod-omniclaude-pr-1832/command.yaml"
  - id: "dod-local-validation"
    description: >-
      Local validation recorded by the PR: the new baseline regression test passes, existing hooks tests
      pass, ruff/pre-commit gates pass on the changed files, and full-suite residual failures are documented
      as pre-existing on dev (ModelSkillResult correlation_id schema drift from an omnibase_core bump).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13846/dod-local-validation/command.yaml"
  - id: "dod-occ-self"
    description: >-
      This onex_change_control PR carries the central OMN-13846 contract and PASS receipts consumed by
      omniclaude PR #1832 through OCC preflight and Receipt Gate.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13846/dod-occ-self/command.yaml"
