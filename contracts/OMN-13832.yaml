---
schema_version: "1.0.0"
ticket_id: "OMN-13832"
title: "OmniDash skill-adoption widget over the skill_executions projection"
summary: >
  OmniDash PR #250 adds a Skill Adoption widget that renders per-skill lifecycle adoption from the skill_executions
  projection (omnibase_infra migration 048; populated on the .201 stability bus as of OMN-13830). The
  read path uses omnidash's canonical projection pattern (useProjectionQuery(TOPICS.skillExecutions))
  with no bespoke REST endpoint and no new HTTP route. This contract binds the downstream omnidash PR
  to central OCC evidence.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr-250-binding"
    description: >
      This onex_change_control PR carries the OMN-13832 central contract and receipt evidence required
      by the downstream omnidash PR #250 Receipt Gate.
    source: "manual"
    status: verified
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13832/dod-occ-pr-250-binding/command.yaml"
  - id: "dod-omnidash-skill-adoption-pr-250"
    description: >
      omnidash PR #250 adds the skill-adoption widget + data service + tests over the skill_executions
      projection using the canonical projection read path. Local gate proof: npm run check (tsc), npm
      run lint (eslint --max-warnings=0), npx vitest run (full suite), npm run build, and pre-commit run
      --all-files all PASS at head commit 5d2fdca5386c1a15c56487c5828ec5653f740fe3.
    source: "manual"
    status: verified
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13832/dod-omnidash-skill-adoption-pr-250/command.yaml
          && grep -q '^pr_number: 250$' drift/dod_receipts/OMN-13832/dod-omnidash-skill-adoption-pr-250/command.yaml
          && grep -q '5d2fdca5386c1a15c56487c5828ec5653f740fe3' drift/dod_receipts/OMN-13832/dod-omnidash-skill-adoption-pr-250/command.yaml"
