---
schema_version: "1.0.0"
ticket_id: "OMN-11846"
title: "Revive org-wide imperative-to-declarative CI guard"
summary: >
  Adds check-imperative-contracts, a workspace/repo guard that scans ONEX node handlers for imperative
  contract bypasses, applies explicit baselines, and fails on any new unbaselined violation. The sweep
  covers every local repo clone and adds the missing omnimarket baseline.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "Focused unit tests prove workspace discovery, baseline precedence, and fail-on-new behavior"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11846/dod-unit-tests/command.yaml\""
  - id: "dod-workspace-sweep"
    description: "Workspace sweep covers every local repository and exits clean with explicit baselines"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11846/dod-workspace-sweep/command.yaml\""
  - id: "dod-ci-wiring"
    description: "CI wiring exists: reusable workflow plus onex_change_control self-test job"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11846/dod-ci-wiring/command.yaml\""
