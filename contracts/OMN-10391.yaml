---
schema_version: "1.0.0"
ticket_id: OMN-10391
title: "Add output handler protocol and registry"
summary: "Add the shared output handler protocol and registry for resolving market CLI renderers by output
  format."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Output handler registry tests pass"
    command: "uv run pytest tests/unit/cli/output/test_registry.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Output handler protocol and registry tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/cli/output/test_registry.py -q"
  - id: "dod-002"
    description: "No deploy required; registry resolution is an in-process CLI helper"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for OMN-10391 - in-process CLI output registry only'"
