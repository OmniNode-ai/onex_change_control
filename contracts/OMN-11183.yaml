---
schema_version: "1.0.0"
ticket_id: "OMN-11183"
title: "Graduate delegation wire models from omnibase_compat to omnimarket"
summary: >
  Central OCC evidence for omnimarket PR #686. All 23 delegation wire DTOs were staged in the temporary
  omnibase_compat/contracts/delegation/wire/ area. Since every consumer was omnimarket-only (single repo),
  they graduate to omnimarket/src/omnimarket/models/delegation/wire/ per the repo-layering rule. Pure
  import-path refactor — no runtime topology change, no Kafka topics added or removed, no container restart
  required.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "2616 unit tests pass including 29 parametrized delegation DTO graduation tests confirming
      models resolve from omnimarket.models.delegation.wire.*"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11183/dod-unit-tests/command.yaml\""
  - id: "dod-deploy"
    description: "No live deploy required — pure import-path refactor, no runtime topology change, no
      Kafka topic addition or removal, no container restart needed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11183/dod-deploy/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-11183 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11183/dod-occ-pr/command.yaml\""
