---
schema_version: "1.0.0"
ticket_id: "OMN-12717"
title: "Require useful delegation artifacts for test and code-generation routes"
summary: >-
  Central evidence for omnimarket PR #1055. Delegation test/code-generation routes now reject OpenAI-compatible
  responses truncated by `finish_reason=length`, require a `final_artifact_only` deterministic DoD check
  for artifact-producing task classes, and prove useful artifacts through a golden-chain DI path for both
  OmniNode infra-style unit-test generation and generic non-OmniNode code generation.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-useful-artifact-golden-chain"
    description: "Golden-chain DI tests prove useful test and code-generation artifacts, not just non-empty
      responses."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-useful-artifact-golden-chain/command.yaml"
          && grep -q '2 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-useful-artifact-golden-chain/command.yaml"
          && grep -q '61 passed, 3 skipped' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-useful-artifact-golden-chain/command.yaml"
  - id: "dod-focused-quality-unit-tests"
    description: "Focused inference and quality-gate unit tests plus lint passed for the useful-artifact
      hardening."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-focused-quality-unit-tests/command.yaml"
          && grep -q '37 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-focused-quality-unit-tests/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-focused-quality-unit-tests/command.yaml"
  - id: "dod-omnimarket-pr-1055"
    description: "omnimarket PR #1055 carries the useful-artifact delegation hardening against dev."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-omnimarket-pr-1055/command.yaml"
          && grep -q '^pr_number: 1055$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-omnimarket-pr-1055/command.yaml"
          && grep -q 'a21cc3e18431154ffe72e4b75503d1df28ff3d66' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-omnimarket-pr-1055/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR #2200 carries the OMN-12717 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2200$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-occ-pr/command.yaml"
  - id: "dod-deploy"
    description: "Post-merge dev runtime deploy must rerun live SEA plus direct delegation useful-artifact
      probes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-deploy/command.yaml"
          && grep -q '^pr_number: 1055$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-deploy/command.yaml"
          && grep -q 'dev-runtime SEA and direct delegation useful-artifact probes' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-deploy/command.yaml"
  - id: "dod-occ-deploy-pr"
    description: "Follow-up onex_change_control branch carries deploy-gate evidence for OMN-12717."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-occ-deploy-pr/command.yaml"
          && grep -q 'a07937b88' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12717/dod-occ-deploy-pr/command.yaml"
