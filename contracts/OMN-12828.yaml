---
schema_version: "1.0.0"
ticket_id: OMN-12828
title: "B3 — cloud URLs + api-key references through bifrost; remove host-env routability coupling on
  the demo cloud path"
summary: "Cloud demo backends (cloud-gemini-flash, openrouter-glm-flash, openrouter-qwen3-coder-480b)
  carry the COMPLETE verbatim endpoint_url (A1/OMN-12815) and a declared API-key reference NAME; the secret
  VALUE resolves fail-closed at the inference effect boundary via the secret store (HG2/OMN-12824). B3
  removes the remaining host-.env runtime dependency on the demo cloud path: the routing resolvers no
  longer read os.environ to gate cloud-backend routability. Routability is now a contract property (complete
  endpoint_url + declared key reference); the POST URL equals the contract endpoint_url VERBATIM and resolution
  fails closed when endpoint_url is empty (no silent fallback). The api_key_env->api_key_ref field rename
  + base_url_env deletion are owned by omnibase_core and did not land there (cross-repo blocked; no shim
  added). The OPENROUTER_BASE_URL env path in bridge_config_loader is not on the demo cloud path (hostile_reviewer/pr_review_bot
  A1 append-debt; follow-up)."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "B3 cloud-endpoint verbatim + contract-routability + fail-closed tests pass on omnimarket"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12828/dod-cloud-endpoints-contract-routability/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-cloud-endpoints-contract-routability"
    description: "B3 verbatim cloud endpoints + contract-driven routability + fail-closed tests pass on
      omnimarket (36 tests); no os.environ secret-presence routability gate remains in the two routing
      resolvers."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12828/dod-cloud-endpoints-contract-routability/command.yaml
          && grep -q '^ticket_id: \"OMN-12828\"$' drift/dod_receipts/OMN-12828/dod-cloud-endpoints-contract-routability/command.yaml
          && grep -q '^evidence_item_id: \"dod-cloud-endpoints-contract-routability\"$' drift/dod_receipts/OMN-12828/dod-cloud-endpoints-contract-routability/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-12828 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12828/dod-occ-pr/command.yaml"
