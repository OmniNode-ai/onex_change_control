---
schema_version: "1.0.0"
ticket_id: "OMN-12660"
title: "WS-G: Golden + error event chains for SEA e2e session defects"
summary: >
  Extends the existing golden-chain surface with a happy-path SEA acceptance chain and five permanent
  error chains (D3/D9/D1+D2/D4/F1) for the SEA e2e session defects. Wires the new sea-e2e-chains gate
  into CI so promotion blocks when any chain is red.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1029"
    description: >
      omnimarket PR #1029 adds the sea_acceptance happy-path golden chain, five error chains (d3_local_routing,
      d1_d2_scaffold, d4_blank_content, d9_wheel_module, f1_publish_loop), registers all 6 in golden_chains.yaml,
      and wires the sea-e2e-chains CI job into ci-summary as a required gate. 38 new unit tests green.
      Full suite: 6061 passed, 0 failed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12660/dod-omnimarket-pr-1029/command.yaml
          && grep -q '^pr_number: 1029$' drift/dod_receipts/OMN-12660/dod-omnimarket-pr-1029/command.yaml"
  - id: "dod-occ-pr"
    description: >
      OCC PR publishes the OMN-12660 contract and receipt bundle.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12660/dod-occ-pr/command.yaml"
