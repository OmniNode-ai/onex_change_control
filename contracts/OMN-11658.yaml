---
schema_version: "1.0.0"
ticket_id: "OMN-11658"
title: "Wire SEA demo dashboard projections"
summary: >
  Central OCC evidence for the SEA demo dashboard integration. The backend PR exposes projection endpoints
  from event-chain evidence and the omnidash PR renders the instruction-eval dashboard surface from those
  projections.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-backend-pr-147"
    description: "onex-self-extending-agent PR #147 carries the SEA projection backend and demo routes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11658/dod-sea-backend-pr-147/command.yaml"
  - id: "dod-sea-backend-unit-tests"
    description: "SEA backend focused and full unit verification passed on the cleaned dev-based branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11658/dod-sea-backend-unit-tests/command.yaml"
  - id: "dod-omnidash-pr-140"
    description: "omnidash PR #140 carries the SEA instruction-eval dashboard projection surface."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11658/dod-omnidash-pr-140/command.yaml"
  - id: "dod-omnidash-verification"
    description: "omnidash lint, typecheck, and Vitest suite passed on the cleaned dev-based branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11658/dod-omnidash-verification/command.yaml"
  - id: "dod-occ-pr-1763"
    description: "OCC PR #1763 binds the central OMN-11658 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11658/dod-occ-pr-1763/command.yaml"
