---
schema_version: "1.0.0"
ticket_id: "OMN-13148"
title: "docs(OMN-13148): release SKILL.md doc-honesty — align to node_release structural-placeholder reality"
summary: >-
  Docs-only reconciliation of plugins/onex/skills/release/SKILL.md in omniclaude. The skill body documented
  a fully-executing release pipeline (a 14-state phase FSM plus per-repo gh pr create / git tag / PyPI
  publish side effects, an idempotency dedup table, and crash-safe state-file resume), but the routed
  node (node_release in omnimarket) is a pure in-memory FSM bookkeeper that performs no git, gh, uv, or
  PyPI I/O. The fix adds a STRUCTURAL PLACEHOLDER banner governing the whole body, documents the real
  9-state EnumReleasePhase FSM under a new "What actually executes today" section, and moves the aspirational
  pipeline under a clearly marked "Target design (NOT YET IMPLEMENTED)" section. The historical placeholder-origin
  ticket (OMN-8004, Done) is cited as the migration tracker and is NOT reopened. No behavioral code change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "docs"
    description: "All changes are docs-only (release/SKILL.md + contract evidence); no Python source changed"
    command: "git diff --name-only origin/dev...HEAD"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-docs-honesty-verified"
    description: >-
      release/SKILL.md was reconciled against live source before editing: handler_release.py docstring
      "Pure logic — no external I/O" and grep confirming no subprocess/gh/git/twine calls in node_release;
      the real 9-state EnumReleasePhase enum in model_release_state.py; node_release/__main__.py self-noting
      "structural placeholder". The SKILL.md now carries a STRUCTURAL PLACEHOLDER banner governing the
      whole body, a "What actually executes today" section documenting the real FSM, and a "Target design
      (NOT YET IMPLEMENTED)" section for the aspirational pipeline.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-13148/dod-docs-honesty-verified/command.yaml"
          && grep -q '^ticket_id: "OMN-13148"$' "drift/dod_receipts/OMN-13148/dod-docs-honesty-verified/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      No live deploy, restart, topic mutation, envelope change, container image change, or .201 mutation
      is required. The omniclaude PR changes one skill SKILL.md plus contract evidence only.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-13148/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      This OCC PR carries the OMN-13148 contract + PASS receipts that the Receipt-Gate on omniclaude PR
      #1758 resolves against.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-13148/dod-occ-pr-self/command.yaml" && grep
          -q '^ticket_id: "OMN-13148"$' "drift/dod_receipts/OMN-13148/dod-occ-pr-self/command.yaml"
