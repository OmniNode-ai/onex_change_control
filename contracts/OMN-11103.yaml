---
schema_version: "1.0.0"
ticket_id: "OMN-11103"
title: "fix(omniclaude): release skill SKILL.md has inverted tier order"
summary: >
  Central OCC evidence for omniclaude PR #1596. Corrects the dependency tier table in the release skill
  SKILL.md: compat(1) → core(2) → spi(3) → infra(4) → app repos(5) → frontend(6). Documentation-only change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Pre-commit and pre-push hook suites pass on the omniclaude branch."
    command: "See drift/dod_receipts/OMN-11103/dod-local-git-hooks/command.yaml"
  - kind: "ci"
    description: "Central OCC PR carries the OMN-11103 contract and receipt bundle."
    command: "See drift/dod_receipts/OMN-11103/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-local-git-hooks"
    description: "Pre-commit and pre-push hook suites passed while committing and pushing omniclaude PR
      #1596."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11103/dod-local-git-hooks/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OMN-11103 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11103/dod-occ-pr/command.yaml"
