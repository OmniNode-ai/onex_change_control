---
schema_version: "1.0.0"
ticket_id: OMN-10297
title: "Refresh market-only skill baseline with focused tests"
summary: "Regenerate the omnimarket-owned skill baseline from direct node entrypoints, include focused
  pytest evidence, and normalize workstation-local values from persisted reference artifacts."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #447 checks pass including market-skill-baseline, CI, deploy-gate, and
      CodeRabbit thread gate"
    command: "gh pr checks 447 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "Market-only skill baseline regenerates with all covered direct node entrypoints working"
    command: "PYTHONDONTWRITEBYTECODE=1 env -u PYTHONPATH uv run python scripts/run_market_skill_baseline.py
      --json-out docs/reference/2026-04-28-market-skill-baseline.json --markdown-out docs/reference/2026-04-28-market-skill-baseline.md"
  - kind: "manual"
    description: "Focused market skill smoke tests pass"
    command: "env -u PYTHONPATH uv run pytest tests/test_market_skill_smokes.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Regenerated baseline reports eight working market-owned skills and zero degraded/failing
      skills"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PYTHONDONTWRITEBYTECODE=1 env -u PYTHONPATH uv run python scripts/run_market_skill_baseline.py
          --json-out docs/reference/2026-04-28-market-skill-baseline.json --markdown-out docs/reference/2026-04-28-market-skill-baseline.md"
  - id: "dod-002"
    description: "Persisted baseline JSON is valid and redacts workstation-local paths and run identifiers"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python3 -m json.tool docs/reference/2026-04-28-market-skill-baseline.json >/dev/null
          && ! rg -n '/Users/|/home/|C:\\\\Users\\\\|sess-2026|Task directory not found' docs/reference/2026-04-28-market-skill-baseline.{json,md}"
  - id: "dod-003"
    description: "Post-merge deploy/readiness smoke plan: market skill baseline runner imports in deployed
      runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.market_skill_baseline
          import iter_market_skill_specs; assert len(iter_market_skill_specs()) >= 8; print('deploy smoke
          ok')\""
