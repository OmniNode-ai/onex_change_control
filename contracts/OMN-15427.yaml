---
schema_version: "1.0.0"
ticket_id: "OMN-15427"
title: "Autobind OCC evidence for OMN-15427"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #1962.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1962 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1962"
    description: "PR #1962 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1962 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: "gh pr view 1962 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-1962"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5548"
    description: "Binds OMN-15427 to OCC companion PR #5548 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5548 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-occ-companion-merged-gate-behaviour"
    description: >-
      Behavioural check on the DELIVERABLE (hand-authored under OMN-15475; source: manual). Every other
      item on this contract asserts that PR 1962 merged and that its OCC companion merged; none of them
      run the gate, so a clean table said nothing about whether the ported mechanism works. This item
      downloads the gate script as it stands on OmniNode-ai/omnimarket `dev` and EXECUTES it against two
      constructed PR-body fixtures, resolving companion state from LIVE onex_change_control (OCC#5487
      is CLOSED-unmerged; OCC#5548 is MERGED), so the verdict is decided by deployed state plus real behaviour,
      never by text in this file. Falsifiable, and proven so by executing both controls before landing:
      RED against omnimarket dev at aea0c33d (the pre-OMN-15475 gate answered PASS on the dead-companion
      fixture, exit 1), GREEN against the OMN-15475 fix (FAIL naming OCC#5487 then PASS, exit 0). No pipes
      are used, so there is no SIGPIPE truncation path; both values are shlex-parseable and run on a hosted
      runner with only `python3` and an authenticated `gh`. The fixture uses TILDE fences (`~~~`, accepted
      by the gate's fence regex exactly like backticks) because the compliance runner executes check_value
      through a shell, where a backtick fence is command substitution: the first hosted run silently ate
      the dead citation and produced a RED for the wrong reason. Tilde fences make the value shell-inert.
      A THIRD fixture covers the locale residual found by adversarial verification of the first fix: `[[:space:]]`
      is not a fixed class, and under the hosted runner's default C.UTF-8 (GNU grep 3.11, ubuntu-24.04)
      it also matches U+2003, so a stamp separated by that character binds the DEAD OCC#5487 at the arbiter
      while a binder pinned to LC_ALL=C's five characters saw only the live OCC#5548 and answered PASS.
      The separator is built with `chr(0x2003)` rather than pasted, so it survives the shell and stays
      visible in review; the fixture-integrity guard asserts it is still present before any verdict is
      reported. Controls executed for this fixture too: RED against omnimarket dev and against the first-cut
      branch head 3debcfe4 (both answered PASS on the fixture, so the check exits 1), GREEN against the
      remediated head 6aacf598 (FAIL naming OCC#5487).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          python3 -c "import base64,subprocess,sys,types; src=base64.b64decode(subprocess.run(['gh','api','repos/OmniNode-ai/omnimarket/contents/scripts/ci/check_occ_companion_merged.py?ref=dev','--jq','.content'],capture_output=True,text=True,check=True).stdout).decode(); m=types.ModuleType('occgate'); sys.modules['occgate']=m; exec(compile(src,'check_occ_companion_merged.py','exec'),m.__dict__); body='Fenced dead example above the real stamp:\n\n~~~\nEvidence-Source: OCC#5487\n~~~\n\nEvidence-Source: OCC#5548\n'; assert body.count('Evidence-Source:')==2 and 'OCC#5487' in body and '~~~' in body, 'fixture mangled in transit - refusing to report a vacuous verdict'; f=m.GhFetcher(); o=f.pr_view; f.pr_view=lambda repo,number,fields,_o=o: {'body':body,'author':{'login':'human'}} if repo=='OmniNode-ai/omnimarket' else _o(repo,number,fields); v=m.evaluate_once(f,event_name='pull_request',repo='OmniNode-ai/omnimarket',pr_number='1962',occ_repo='OmniNode-ai/onex_change_control'); print('dead-companion fixture ->',v.name,'|',v.reason); sys.exit(0 if (v.code==m.EXIT_FAIL and 'OCC#5487' in v.reason) else 1)"
      - check_type: "command"
        check_value: |-
          python3 -c "import base64,subprocess,sys,types; src=base64.b64decode(subprocess.run(['gh','api','repos/OmniNode-ai/omnimarket/contents/scripts/ci/check_occ_companion_merged.py?ref=dev','--jq','.content'],capture_output=True,text=True,check=True).stdout).decode(); m=types.ModuleType('occgate'); sys.modules['occgate']=m; exec(compile(src,'check_occ_companion_merged.py','exec'),m.__dict__); sep=chr(0x2003); body='Evidence-Source:'+sep+'OCC#5487\n\nEvidence-Source: OCC#5548\n'; assert body.count('Evidence-Source:')==2 and body.startswith('Evidence-Source:'+sep+'OCC#5487') and 'Evidence-Source: OCC#5487' not in body and 'OCC#5548' in body, 'fixture mangled in transit (separator normalised or citation lost) - refusing to report a vacuous verdict'; f=m.GhFetcher(); o=f.pr_view; f.pr_view=lambda repo,number,fields,_o=o: {'body':body,'author':{'login':'human'}} if repo=='OmniNode-ai/omnimarket' else _o(repo,number,fields); v=m.evaluate_once(f,event_name='pull_request',repo='OmniNode-ai/omnimarket',pr_number='1962',occ_repo='OmniNode-ai/onex_change_control'); print('unicode-space dead-companion fixture ->',v.name,'|',v.reason); sys.exit(0 if (v.code==m.EXIT_FAIL and 'OCC#5487' in v.reason) else 1)"
      - check_type: "command"
        check_value: |-
          python3 -c "import base64,subprocess,sys,types; src=base64.b64decode(subprocess.run(['gh','api','repos/OmniNode-ai/omnimarket/contents/scripts/ci/check_occ_companion_merged.py?ref=dev','--jq','.content'],capture_output=True,text=True,check=True).stdout).decode(); m=types.ModuleType('occgate'); sys.modules['occgate']=m; exec(compile(src,'check_occ_companion_merged.py','exec'),m.__dict__); body='Evidence-Source: OCC#5548\n'; assert body.count('Evidence-Source:')==1 and 'OCC#5487' not in body, 'fixture mangled in transit - refusing to report a vacuous verdict'; f=m.GhFetcher(); o=f.pr_view; f.pr_view=lambda repo,number,fields,_o=o: {'body':body,'author':{'login':'human'}} if repo=='OmniNode-ai/omnimarket' else _o(repo,number,fields); v=m.evaluate_once(f,event_name='pull_request',repo='OmniNode-ai/omnimarket',pr_number='1962',occ_repo='OmniNode-ai/onex_change_control'); print('merged-companion fixture ->',v.name,'|',v.reason); sys.exit(0 if v.code==m.EXIT_PASS else 1)"
