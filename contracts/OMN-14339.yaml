---
schema_version: "1.0.0"
ticket_id: "OMN-14339"
title: "OCC evidence for core residual types-to-models edge retirement"
summary: >
  PR-bound evidence for OmniNode-ai/omnibase_core#1492, which retires residual types-to-models back-edges
  and related import-linter/type alias cleanup.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Product PR #1492 hosted checks are evaluated on the product PR."
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1492-identity"
    description: "PR #1492 exists on OmniNode-ai/omnibase_core with the OMN-14339 branch identity."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName,headRefOid"
  - id: "dod-omnibase-core-pr-1492-scope"
    description: "PR #1492 file scope is limited to import-linter, docs, type/model cleanup, validators,
      and focused tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} --repo ${REPO} --name-only"
  - id: "dod-omnibase-core-pr-1492-close-discipline"
    description: "PR #1492 uses Refs OMN-14339 rather than Closes OMN-14339, avoiding premature ticket
      closeout."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json body"
  - id: "dod-omnibase-core-pr-1492-diff-content"
    description: "PR #1492 diff retires the .importlinter types-to-models residual edge allowance instead
      of only changing metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff ${PR_NUMBER} --repo ${REPO} | grep -q 'types -> models .*RETIRED'"
  - id: "occ-self-bind-pr-4685"
    description: "Binds OMN-14339 to OCC companion PR #4685."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
