---
schema_version: "1.0.0"
ticket_id: OMN-10422
title: "Make omniclaude reject-skip reusable merge_group safe"
summary: >-
  Harden the omniclaude-hosted reject-deploy-gate-skip reusable workflow so merge_group events resolve
  the queue PR identity from the merge-group head_ref instead of searching for pull requests by the synthetic
  merge_group head_sha.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: PR 1505 updates the omniclaude reject-skip reusable workflow to resolve merge_group PR
      identity safely
    command: >-
      gh pr diff 1505 --repo OmniNode-ai/omniclaude | grep -q 'MERGE_GROUP_HEAD_REF'
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: >-
      omniclaude PR #1505 updates reject-deploy-gate-skip.yml to resolve merge_group PR identity from
      head_ref and avoid matching pull requests by synthetic merge_group head SHA.
    source: generated
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-10422/dod-001/command.yaml && grep -q '^ticket_id:
          OMN-10422$' drift/dod_receipts/OMN-10422/dod-001/command.yaml && grep -q '^pr_number: 1505$'
          drift/dod_receipts/OMN-10422/dod-001/command.yaml && grep -q '^commit_sha: 7172c2440a16d14ed1bc28e12a8bc44a910fd7b3$'
          drift/dod_receipts/OMN-10422/dod-001/command.yaml && grep -q 'MERGE_GROUP_HEAD_REF' drift/dod_receipts/OMN-10422/dod-001/command.yaml
          && grep -q 'synthetic queue commit' drift/dod_receipts/OMN-10422/dod-001/command.yaml
