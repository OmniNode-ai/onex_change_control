---
schema_version: "1.0.0"
ticket_id: OMN-10422
title: "Add omnibase_core reject-skip caller for merge_group re-scan"
summary: >-
  Add .github/workflows/call-reject-skip.yml to omnibase_core so the omniclaude-hosted reject-deploy-gate-skip
  reusable workflow runs on both pull_request and merge_group. The merge_group invocation re-scans bypass
  tokens without cached PR state before entry to the merge queue.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: PR 1016 adds the reject-skip caller wired to the omniclaude reusable workflow
    command: >-
      gh pr diff 1016 --repo OmniNode-ai/omnibase_core | grep -q 'OmniNode-ai/omniclaude/.github/workflows/reject-deploy-gate-skip.yml@main'
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: >-
      omnibase_core PR #1016 adds call-reject-skip.yml and invokes the omniclaude reject-deploy-gate-skip
      reusable workflow on the PR branch.
    source: generated
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-10422/dod-001/command.yaml && grep -q '^ticket_id:
          OMN-10422$' drift/dod_receipts/OMN-10422/dod-001/command.yaml && grep -q '^pr_number: 1016$'
          drift/dod_receipts/OMN-10422/dod-001/command.yaml && grep -q '^commit_sha: ca8d5619c97fc3adb87500c9eb78c0b96ecddd31$'
          drift/dod_receipts/OMN-10422/dod-001/command.yaml && grep -q 'reject-deploy-gate-skip.yml@main'
          drift/dod_receipts/OMN-10422/dod-001/command.yaml
