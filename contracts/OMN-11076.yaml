---
schema_version: "1.0.0"
ticket_id: "OMN-11076"
title: "Share delegation workflow state across live routing decision"
summary: >
  Central OCC evidence for omnimarket PR #661. Keeps the delegation orchestrator workflow FSM shared across
  live dispatcher/handler wiring so a delegation request that reaches routing-decision emits the same-correlation
  inference request instead of losing state between handler instances. Also records in-flight inference
  de-duplication for duplicate intent delivery.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused delegation dispatcher/wiring/orchestrator tests pass."
    command: "See drift/dod_receipts/OMN-11076/dod-focused-tests/command.yaml"
  - kind: "tests"
    description: "Delegation chain E2E test passes through request, routing, inference, and gate."
    command: "See drift/dod_receipts/OMN-11076/dod-chain-e2e/command.yaml"
  - kind: "ci"
    description: "Touched delegation handler/wiring test files pass ruff."
    command: "See drift/dod_receipts/OMN-11076/dod-lint/command.yaml"
  - kind: "ci"
    description: "Deploy/import smoke for the touched delegation workflow modules passes without live
      deploy."
    command: "See drift/dod_receipts/OMN-11076/dod-deploy/command.yaml"
  - kind: "ci"
    description: "Central OCC PR carries the OMN-11076 contract and receipt bundle."
    command: "See drift/dod_receipts/OMN-11076/dod-occ-pr-1051/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused delegation wiring, dispatcher event-bus, and orchestrator tests passed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '47 passed' drift/dod_receipts/OMN-11076/dod-focused-tests/command.yaml"
  - id: "dod-chain-e2e"
    description: "Delegation chain E2E test passed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '6 passed' drift/dod_receipts/OMN-11076/dod-chain-e2e/command.yaml"
  - id: "dod-lint"
    description: "Ruff check passed for touched delegation files."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'All checks passed' drift/dod_receipts/OMN-11076/dod-lint/command.yaml"
  - id: "dod-deploy"
    description: "Import smoke passed for touched delegation modules; no live deploy or runtime restart
      performed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'HandlerDelegationWorkflow get_shared_delegation_workflow_handler DelegationIntentBridge'
          drift/dod_receipts/OMN-11076/dod-deploy/command.yaml"
  - id: "dod-occ-pr-1051"
    description: "onex_change_control PR #1051 carries the central OMN-11076 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11076/dod-occ-pr-1051/command.yaml
          && grep -q '^pr_number: 1051$' drift/dod_receipts/OMN-11076/dod-occ-pr-1051/command.yaml"
