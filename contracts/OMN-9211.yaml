---
schema_version: "1.0.0"
ticket_id: "OMN-9211"
title: "Fix 6 nodes declaring topics outside onex.{cmd|evt|intent}.producer.event.vN grammar"
summary: >-
  Renames invalid topics across omniintelligence (*.v1.dlq to *-dlq.v1 pattern, removed bare routing.feedback
  topic), omnibase_infra (onex.snapshot.* to onex.evt.* with enum regeneration and integration test coverage),
  and omnimemory (dlq topic renamed). Part of OMN-9532 epic.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: manual
    description: omniintelligence PR  #653 bound at observed head.
    command: grep -q '^status. PASS$' drift/dod_receipts/OMN-9211/dod-omniintelligence-pr-653/command.yaml
  - kind: manual
    description: omnibase_infra PR  #1633 bound at observed head.
    command: grep -q '^status. PASS$' drift/dod_receipts/OMN-9211/dod-omnibase-infra-pr-1633/command.yaml
  - kind: manual
    description: omnimemory PR  #316 bound at observed head.
    command: grep -q '^status. PASS$' drift/dod_receipts/OMN-9211/dod-omnimemory-pr-316/command.yaml
  - kind: manual
    description: omnimarket PR  #681 bound at observed head.
    command: grep -q '^status. PASS$' drift/dod_receipts/OMN-9211/dod-omnimarket-pr-681/command.yaml
  - kind: manual
    description: No deploy required.
    command: grep -q '^status. PASS$' drift/dod_receipts/OMN-9211/dod-deploy/command.yaml
  - kind: manual
    description: OCC PR carries OMN-9211 evidence.
    command: grep -q '^status. PASS$' drift/dod_receipts/OMN-9211/dod-occ-self/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-omniintelligence-pr-653
    description: Bind omniintelligence PR  #653 to OMN-9211 at the observed PR head.
    source: manual
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9211/dod-omniintelligence-pr-653/command.yaml"
  - id: dod-omnibase-infra-pr-1633
    description: Bind omnibase_infra PR  #1633 to OMN-9211 at the observed PR head.
    source: manual
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9211/dod-omnibase-infra-pr-1633/command.yaml"
  - id: dod-omnimemory-pr-316
    description: Bind omnimemory PR  #316 to OMN-9211 at the observed PR head.
    source: manual
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9211/dod-omnimemory-pr-316/command.yaml"
  - id: dod-omnimarket-pr-681
    description: Bind omnimarket PR  #681 to OMN-9211 at the observed PR head.
    source: manual
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9211/dod-omnimarket-pr-681/command.yaml"
  - id: dod-deploy
    description: No live deploy required; topic rename is a pure contract fix.
    source: manual
    checks:
      - check_type: command
        check_value: printf 'deploy gate. OMN-9211 is a topic grammar fix; no live deploy required'
  - id: dod-occ-self
    description: Self-binding receipt for OCC PR carrying OMN-9211 evidence.
    source: manual
    checks:
      - check_type: command
        check_value: printf 'OCC self-bind. this OCC PR carries OMN-9211 evidence'
