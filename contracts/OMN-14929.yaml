---
schema_version: "1.0.0"
ticket_id: "OMN-14929"
title: "Repair onex-dev runtime overlay kustomize patch"
summary: >
  omninode_infra PR #615 splits the multi-document runtime recovery strategic-merge patch into single-document
  patch files for onex-dev and onex-prod so kustomize can build the runtime overlays again.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omninode_infra PR #615 is open at the recorded head with the runtime overlay patch split."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14929/dod-omninode_infra-pr-615-head/command.yaml"
  - kind: "ci"
    description: "K8s Manifest Validation succeeded for omninode_infra PR #615 at the recorded head."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14929/dod-omninode_infra-pr-615-kustomize-ci/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode_infra-pr-615-head"
    description: >
      Binds omninode_infra PR #615 at head 8b868062a4ba2cb9f1521c81f2e9b6611b33de82.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 615 --repo OmniNode-ai/omninode_infra --json number,state,headRefOid,headRefName
  - id: "dod-omninode_infra-pr-615-product-diff-scope"
    description: "PR #615 product diff scope is limited to the runtime overlay kustomization and split
      recovery patch files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr diff 615 --repo OmniNode-ai/omninode_infra --name-only
  - id: "dod-omninode_infra-pr-615-kustomize-ci"
    description: "K8s Manifest Validation run 29933290328 completed successfully at PR #615 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh run view 29933290328 --repo OmniNode-ai/omninode_infra --json conclusion --jq .conclusion)"
          = success && test "$(gh run view 29933290328 --repo OmniNode-ai/omninode_infra --json headSha
          --jq .headSha)" = 8b868062a4ba2cb9f1521c81f2e9b6611b33de82
