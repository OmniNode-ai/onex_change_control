---
schema_version: "1.0.0"
ticket_id: "OMN-12709"
title: "SEA live delegation resolves max_context_tokens for Qwen3.6-27B-MTP-IQ4_XS.gguf"
summary: >-
  Central evidence for omnimarket PR #1052. SEA live --agent selected the stability-lane served model
  id Qwen3.6-27B-MTP-IQ4_XS.gguf, but the consumed omnimarket routing_tiers.yaml contract only declared
  the logical deepseek-r1-14b key, so SEA failed closed in resolve_max_context_tokens before emitting
  a valid ModelInferenceIntent. The product fix declares the served id in the authoritative omnimarket
  routing-tier contract under the existing local-reasoner backend with max_context_tokens 24576. No SEA-local
  fallback, literal max-token default, deploy, restart, or .201 mutation is introduced.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-routing-tier-served-id"
    description: "omnimarket PR #1052 declares Qwen3.6-27B-MTP-IQ4_XS.gguf in routing_tiers.yaml under
      local-reasoner with max_context_tokens 24576."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12709/dod-routing-tier-served-id/command.yaml"
          && grep -q '^pr_number: 1052$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12709/dod-routing-tier-served-id/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused omnimarket routing-tier contract and routing reducer policy tests pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12709/dod-focused-tests/command.yaml"
          && grep -q '20 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12709/dod-focused-tests/command.yaml"
  - id: "dod-sea-resolver-proof"
    description: "SEA resolve_max_context_tokens resolves the fixed served id to 24576 when pointed at
      the product routing contract."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12709/dod-sea-resolver-proof/command.yaml"
          && grep -q '24576' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12709/dod-sea-resolver-proof/command.yaml"
  - id: "dod-live-overlay-proof"
    description: "Live stability overlay run passes the previous max-context failure point with the fixed
      local omnimarket source."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12709/dod-live-overlay-proof/command.yaml"
          && grep -q 'bea5c150-16cf-4ca9-86fc-671e42e8b59f' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12709/dod-live-overlay-proof/command.yaml"
  - id: "dod-deploy-gate-no-runtime-mutation"
    description: "The routing contract fix needs no live deploy, restart, or .201 mutation; downstream
      consumption requires merging the product contract and refreshing consumers."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12709/dod-deploy-gate-no-runtime-mutation/command.yaml"
          && grep -qi 'no live deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12709/dod-deploy-gate-no-runtime-mutation/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-12709 central contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12709/dod-occ-pr/command.yaml"
