---
schema_version: "1.0.0"
ticket_id: "OMN-14665"
title: "Autobind OCC evidence for OMN-14665"
summary: >
  OCC contract bound by node_pr_lifecycle_fix_effect Evidence-Source autobind (OMN-13317 F1) when OmniNode-ai/omnibase_core
  PR #1454 carried a product-SHA Evidence-Source or was missing an OCC contract, and failed the gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1454 product diff scope present"
    command: "PR_NUMBER=1454; REPO=OmniNode-ai/omnibase_core; gh pr diff ${PR_NUMBER} --repo ${REPO} --name-only
      | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_core-pr-1454"
    description: "PR #1454 on OmniNode-ai/omnibase_core — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=1454; REPO=OmniNode-ai/omnibase_core; gh pr view ${PR_NUMBER} --repo ${REPO}
          --json number,state"
  - id: "dod-OmniNode-ai-omnibase_core-pr-1454-ci"
    description: "PR #1454 on OmniNode-ai/omnibase_core — product diff scope (OMN-14425 substance check;
      OMN-14650: no longer gated on source-PR CI being green)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=1454; REPO=OmniNode-ai/omnibase_core; gh pr diff ${PR_NUMBER} --repo ${REPO}
          --name-only | grep -q ."
  - id: "occ-self-bind-pr-4313"
    description: "OCC companion PR #4313 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14665
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4313; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
  - id: "dod-deploy-no-prod"
    description: "Deploy-gate evidence: PR #1454 changes runtime-adjacent validators and harness code,
      but this merge sweep performed no production deploy, docker exec, or rpk topic produce runtime mutation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'deploy evidence: no production deploy or runtime mutation performed' \"drift/dod_receipts/OMN-14665/dod-deploy-no-prod/command.yaml\""
