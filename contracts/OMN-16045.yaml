---
schema_version: "1.0.0"
ticket_id: "OMN-16045"
title: "Autobind OCC evidence for OMN-16045"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_compat PR #188.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #188 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "ci"
    description: "PR #1991 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "ci"
    description: "PR #2073 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "ci"
    description: "PR #1994 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_compat-pr-188"
    description: "PR #188 on OmniNode-ai/omnibase_compat — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 188 --repo OmniNode-ai/omnibase_compat --json number,state"
      - check_type: "command"
        check_value: "gh pr view 188 --repo OmniNode-ai/omnibase_compat --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_compat-pr-188"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6475"
    description: "Binds OMN-16045 to OCC companion PR #6475 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6475 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omniclaude-pr-1991"
    description: "PR #1991 on OmniNode-ai/omniclaude — Evidence-Source recut (sibling OCC#6474 closed,
      collided with merged OCC#6475's contracts/OMN-16045.yaml mint; OMN-15214 companion-merged gate,
      OMN-15845 concurrent-mint-race class)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1991 --repo OmniNode-ai/omniclaude --json number,state"
      - check_type: "command"
        check_value: "gh pr view 1991 --repo OmniNode-ai/omniclaude --json files"
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omniclaude/contents/.github/workflows/security-scan.yml?ref=${PR_HEAD_SHA}
          --jq .content | base64 -d | grep -c exclude-detectors=lob"
  - id: "dod-occ-evidence-admissibility-validator-pr-1991"
    description: "Hosted OCC evidence admissibility validator (OMN-15247), PR #1991."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omniclaude-pr-1991"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-OmniNode-ai-omnimarket-pr-2073"
    description: "PR #2073 on OmniNode-ai/omnimarket — Evidence-Source recut (sibling OCC#6476 closed,
      collided with merged OCC#6475's contracts/OMN-16045.yaml mint; OMN-15214 companion-merged gate,
      OMN-15845 concurrent-mint-race class)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2073 --repo OmniNode-ai/omnimarket --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2073 --repo OmniNode-ai/omnimarket --json files"
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/contents/.github/workflows/security-scan.yml?ref=${PR_HEAD_SHA}
          --jq .content | base64 -d | grep -c exclude-detectors=lob"
  - id: "dod-occ-evidence-admissibility-validator-pr-2073"
    description: "Hosted OCC evidence admissibility validator (OMN-15247), PR #2073."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-2073"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6482"
    description: "OCC companion PR #6482 — self-bind for OMN-16045, sibling recut of the collided OCC#6474/#6476
      citations for omniclaude#1991 and omnimarket#2073."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6482 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-6535"
    description: "OCC batch receipt-repair PR #6535 — self-bind (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6535 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omniclaude-pr-1994"
    description: "PR #1994 on OmniNode-ai/omniclaude — Evidence-Source binding for the secret-scan execution
      repair (the scheduled Secret Scanning job scanned nothing for 14+ consecutive nights). Hand-authored
      companion: the autobind writer minted no companion for this product PR, so this entry is authored
      by the OCC prep lane rather than by node_occ_companion_compute."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1994 --repo OmniNode-ai/omniclaude --json number,state"
      - check_type: "command"
        check_value: "gh pr view 1994 --repo OmniNode-ai/omniclaude --json files"
      - check_type: "command"
        check_value: |-
          H=ec6277ffd472cf20d7dbac11556a660076db1f5f; B=b8b3e2eea00141f94ce5fe20f0b479e48baa6892; R=OmniNode-ai/omniclaude; W=.github/workflows/security-scan.yml; D=$(mktemp -d); gh api "repos/${R}/contents/${W}?ref=${H}" --jq .content | base64 -d > "${D}/head.yml" || exit 1; gh api "repos/${R}/contents/${W}?ref=${B}" --jq .content | base64 -d > "${D}/base.yml" || exit 1; python3 -c 'import sys,re,pathlib; d=pathlib.Path(sys.argv[1]); rd=lambda p:(d/p).read_text(); jb=lambda t,n:(lambda m: m.group(1) if m else "")(re.search(r"(?ms)^  "+n+r":\n(.*?)(?=^  [A-Za-z0-9_-]+:\n|\Z)",t)); nc=lambda s:"\n".join(l for l in s.splitlines() if not l.lstrip().startswith("#")); h=nc(jb(rd("head.yml"),"secret-scan")); b=nc(jb(rd("base.yml"),"secret-scan")); hu="runs-on: ubuntu-latest" in h; bu="runs-on: ubuntu-latest" in b; hf="fetch-depth: 0" in h; bf="fetch-depth: 0" in b; hc=h.count("uses: trufflesecurity/trufflehog@"); bc=b.count("uses: trufflesecurity/trufflehog@"); hi=h.count("if: github.event_name"); hp="pull_request" in h; bp="pull_request" in b; print("HEAD_UBUNTU_RUNNER:",hu); print("BASE_UBUNTU_RUNNER:",bu); print("HEAD_FULL_HISTORY:",hf); print("BASE_FULL_HISTORY:",bf); print("HEAD_SCAN_STEPS:",hc); print("BASE_SCAN_STEPS:",bc); print("HEAD_STEP_CONDITIONS:",hi); print("HEAD_EXECUTABLE_PULL_REQUEST_REF:",hp); print("BASE_EXECUTABLE_PULL_REQUEST_REF:",bp); sys.exit(0 if (hu and not bu and hf and not bf and hc==2 and bc==3 and hi==2 and not hp and bp) else 1)' "$D" && echo "OMN-16045-SECRET-SCAN-EXECUTES-PROBE-PASS: at head ec6277ffd the secret-scan job runs on ubuntu-latest with unconditional fetch-depth 0 and exactly two mutually exclusive trufflehog steps and no executable pull_request reference; the pre-fix base b8b3e2eea is the RED control (self-hosted selector, conditional fetch-depth, three steps, dead pull_request step)" && gh pr view 1994 --repo OmniNode-ai/omniclaude --json number,state
  - id: "dod-occ-evidence-admissibility-validator-pr-1994"
    description: "Hosted OCC evidence admissibility validator (OMN-15247), PR #1994. The probe pairs the
      suite run with a repo-qualified live read of the carrier PR in the same command, so the receipt
      carries a consistent PR binding rather than a bare pr_number (the NO_CONSISTENT_PR_BINDING defect
      this ticket's earlier receipts had to be repaired for)."
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omniclaude-pr-1994"
    checks:
      - check_type: "command"
        check_value: |-
          uv run pytest tests/test_evidence_admissibility.py -q && gh pr view 1994 --repo OmniNode-ai/omniclaude --json number,state && echo dod-occ-evidence-admissibility-validator-pr-1994
