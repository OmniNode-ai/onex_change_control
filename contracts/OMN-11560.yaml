---
schema_version: "1.0.0"
ticket_id: "OMN-11560"
title: "Delete confirmed dead symbols from omnibase_core"
summary: >-
  Bind central OCC evidence for omnibase_core PR #1134, which removes confirmed zero-importer symbols
  from omnibase_core while preserving canonical replacements and refreshing the secrets baseline after
  line-number drift.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omnibase_core PR #1134 CI and receipt gates pass after the dead-symbol cleanup."
    command: "gh pr checks 1134 --repo OmniNode-ai/omnibase_core"
  - kind: "manual"
    description: "Central OCC evidence PR carries this contract and PASS receipts."
    command: "gh pr view <occ-pr> --repo OmniNode-ai/onex_change_control"
dod_evidence:
  - id: "dod-omnibase-core-pr-1134"
    description: "Bind OMN-11560 evidence to omnibase_core PR #1134 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11560/dod-omnibase-core-pr-1134/command.yaml\"
          && grep -q '^pr_number: 1134$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11560/dod-omnibase-core-pr-1134/command.yaml\"
          && grep -q '^commit_sha: \"10f6a37bbd1b83be7cda90d511da224cdf4fa122\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11560/dod-omnibase-core-pr-1134/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Dead-code cleanup only — no live deploy, runtime restart, or topic produce required
      pre-merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11560/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11560/dod-deploy-assessment/command.yaml\"
          && grep -q 'dead-code cleanup' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11560/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Bind OMN-11560 central evidence to the OCC PR that carries this bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11560/dod-occ-pr-self/command.yaml\"
          && grep -q '^ticket_id: \"OMN-11560\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11560/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
