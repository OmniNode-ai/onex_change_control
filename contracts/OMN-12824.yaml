---
schema_version: "1.0.0"
ticket_id: OMN-12824
title: "HG2 — secret-store resolver at inference effect boundary; remove direct os.environ secret reads;
  delete hardcoded OpenRouter base"
summary: "Resolve the API-key VALUE at the inference effect boundary through the canonical ProtocolSecretStore
  (env default, Infisical-swappable) instead of direct os.environ reads; the routing authority carries
  only the api_key_ref NAME. Delete the hardcoded OpenRouter base URL default (fail-closed). Rename the
  canonical omnibase_core bifrost backend field api_key_env->api_key_ref and delete base_url_env (paired
  core PR)."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "events"
evidence_requirements:
  - kind: "tests"
    description: "Secret-store resolver + effect-boundary tests pass on omnimarket"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12824/dod-secret-store-resolver-effect-boundary/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-secret-store-resolver-effect-boundary"
    description: "Secret-store resolver + effect-boundary tests pass on omnimarket (37 tests)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12824/dod-secret-store-resolver-effect-boundary/command.yaml
          && grep -q '^ticket_id: \"OMN-12824\"$' drift/dod_receipts/OMN-12824/dod-secret-store-resolver-effect-boundary/command.yaml
          && grep -q '^evidence_item_id: \"dod-secret-store-resolver-effect-boundary\"$' drift/dod_receipts/OMN-12824/dod-secret-store-resolver-effect-boundary/command.yaml"
  - id: "dod-occ-pr-2337"
    description: "onex_change_control PR #2337 carries the OMN-12824 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12824/dod-occ-pr-2337/command.yaml
          && grep -q '^pr_number: 2337$' drift/dod_receipts/OMN-12824/dod-occ-pr-2337/command.yaml"
