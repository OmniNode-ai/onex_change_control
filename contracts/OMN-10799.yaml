---
schema_version: "1.0.0"
ticket_id: "OMN-10799"
title: "fix: HandlerIntentQuery DI crash-loop — make container param optional"
summary: >
  HandlerIntentQuery required container: ModelONEXContainer in __init__, which the declarative service_handler_resolver
  could not satisfy, causing a crash-loop on every runtime boot. Fix makes container optional (default
  None) so the resolver can construct the handler zero-arg. initialize() raises RuntimeError if called
  without a container, preserving the invariant for real Memgraph callers.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnimarket PR #593 carries the OMN-10799 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10799/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Intent query handler tests and unit suite pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10799/dod-local-tests/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OCC contract and receipt."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10799/dod-occ-pr/command.yaml\""
