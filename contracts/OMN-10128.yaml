---
schema_version: "1.0.0"
ticket_id: OMN-10128
title: "Build daemon-health probe CLI"
summary: "Central receipt contract for the omnimarket daemon-health probe delivered in omnimarket PR #502."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
  - "envelopes"
evidence_requirements:
  - kind: "manual"
    description: "Daemon health probe unit and hermetic integration tests pass"
    command: "test -f drift/dod_receipts/OMN-10128/dod-health-probe-tests/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-10128/dod-health-probe-tests/command.yaml && grep -q '10 passed' drift/dod_receipts/OMN-10128/dod-health-probe-tests/command.yaml"
  - kind: "manual"
    description: "Existing emit daemon golden-chain regression tests pass"
    command: "test -f drift/dod_receipts/OMN-10128/dod-golden-chain-tests/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-10128/dod-golden-chain-tests/command.yaml && grep -q '54 passed' drift/dod_receipts/OMN-10128/dod-golden-chain-tests/command.yaml"
  - kind: "manual"
    description: "Touched daemon health probe paths pass lint"
    command: "test -f drift/dod_receipts/OMN-10128/dod-lint/command.yaml && grep -q '^status: PASS$' drift/dod_receipts/OMN-10128/dod-lint/command.yaml
      && grep -q 'All checks passed!' drift/dod_receipts/OMN-10128/dod-lint/command.yaml"
  - kind: "manual"
    description: "Touched daemon health probe paths are formatted"
    command: "test -f drift/dod_receipts/OMN-10128/dod-format/command.yaml && grep -q '^status: PASS$'
      drift/dod_receipts/OMN-10128/dod-format/command.yaml && grep -q 'already formatted' drift/dod_receipts/OMN-10128/dod-format/command.yaml"
  - kind: "manual"
    description: "Repo-local contract YAML parses"
    command: "test -f drift/dod_receipts/OMN-10128/dod-contract-parse/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-10128/dod-contract-parse/command.yaml && grep -q 'contracts/OMN-10128.yaml'
      drift/dod_receipts/OMN-10128/dod-contract-parse/command.yaml"
  - kind: "manual"
    description: "Touched daemon health probe paths pass mypy"
    command: "test -f drift/dod_receipts/OMN-10128/dod-mypy/command.yaml && grep -q '^status: PASS$' drift/dod_receipts/OMN-10128/dod-mypy/command.yaml"
  - kind: "manual"
    description: "Full pre-commit hooks pass with OMNIBASE_INFRA_PATH set"
    command: "test -f drift/dod_receipts/OMN-10128/dod-precommit/command.yaml && grep -q '^status: PASS$'
      drift/dod_receipts/OMN-10128/dod-precommit/command.yaml && grep -q 'normalization-symmetry.*Passed'
      drift/dod_receipts/OMN-10128/dod-precommit/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-health-probe-tests"
    description: "Daemon health probe unit and hermetic integration tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10128/dod-health-probe-tests/command.yaml && grep
          -q '^status: PASS$' drift/dod_receipts/OMN-10128/dod-health-probe-tests/command.yaml && grep
          -q '10 passed' drift/dod_receipts/OMN-10128/dod-health-probe-tests/command.yaml"
  - id: "dod-golden-chain-tests"
    description: "Existing emit daemon golden-chain regression tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10128/dod-golden-chain-tests/command.yaml && grep
          -q '^status: PASS$' drift/dod_receipts/OMN-10128/dod-golden-chain-tests/command.yaml && grep
          -q '54 passed' drift/dod_receipts/OMN-10128/dod-golden-chain-tests/command.yaml"
  - id: "dod-lint"
    description: "Touched daemon health probe paths pass lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10128/dod-lint/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10128/dod-lint/command.yaml && grep -q 'All checks passed!' drift/dod_receipts/OMN-10128/dod-lint/command.yaml"
  - id: "dod-format"
    description: "Touched daemon health probe paths are formatted"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10128/dod-format/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10128/dod-format/command.yaml && grep -q 'already formatted' drift/dod_receipts/OMN-10128/dod-format/command.yaml"
  - id: "dod-contract-parse"
    description: "Repo-local contract YAML parses"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10128/dod-contract-parse/command.yaml && grep -q
          '^status: PASS$' drift/dod_receipts/OMN-10128/dod-contract-parse/command.yaml && grep -q 'contracts/OMN-10128.yaml'
          drift/dod_receipts/OMN-10128/dod-contract-parse/command.yaml"
  - id: "dod-mypy"
    description: "Touched daemon health probe paths pass mypy"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10128/dod-mypy/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10128/dod-mypy/command.yaml"
  - id: "dod-precommit"
    description: "Full pre-commit hooks pass with OMNIBASE_INFRA_PATH set"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10128/dod-precommit/command.yaml && grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10128/dod-precommit/command.yaml && grep -q 'normalization-symmetry.*Passed'
          drift/dod_receipts/OMN-10128/dod-precommit/command.yaml"
