---
schema_version: '1.0.0'
ticket_id: OMN-13784
title: "test(OMN-13784): Wave 7 session/dispatch state-coverage suite"
summary: >
  OMN-13674 (epic) child, WS-M Wave 7. Closes declared-but-untested state/transition gaps in the WS-5
  Wave 7 (OMN-13681) multiparam suites for node_session_orchestrator, node_session_phase_orchestrator,
  node_session_phase_dispatcher, node_session_phase_reducer, node_dispatch_worker, and node_skill_dispatch_engine_orchestrator,
  per the strengthened DoD requiring full declared-state coverage against each contract.yaml rather than
  a sample. Test-only change (no production code touched) in the omnimarket repo, PR #1546.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
dod_evidence:
  - id: dod-001
    description: >
      node_session_orchestrator: added the third EnumSessionStatus member (error, via Phase 2 Linear-fetch
      failure on an empty fixture) and the Phase 3 dispatch fallthrough (phase=0 default, previously only
      phase=1/phase=2 early-returns were bus-tested) to tests/integration/node_session_orchestrator/test_session_orchestrator_multiparam.py.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13784/dod-001/command.yaml"
  - id: dod-002
    description: >
      node_session_phase_orchestrator: added the budget_warning action branch (warning_delegated tick_outcome,
      the fourth and last declared action) plus the halt-priority interaction to tests/unit/nodes/node_session_phase_orchestrator/test_handler_session_phase_orchestrator.py.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13784/dod-002/command.yaml"
  - id: dod-003
    description: >
      node_session_phase_dispatcher: added the exit/skip/fail transition values (contract declares enter/exit/skip/fail;
      only enter was tested) to tests/unit/nodes/node_session_phase_dispatcher/test_handler_session_phase_dispatcher.py
      — phase-state still published, workers never dispatched on non-enter transitions.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13784/dod-003/command.yaml"
  - id: dod-004
    description: >
      node_session_phase_reducer: added duplicate-event and out-of-order-event idempotency coverage (REDUCER
      DoD requirement) to tests/unit/nodes/node_session_phase_reducer/test_session_phase_reducer.py.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13784/dod-004/command.yaml"
  - id: dod-005
    description: >
      node_dispatch_worker: added the L0/L1/L3 knowledge_context_level branches (only none/L2 were covered)
      and the replace=True dedup-override branch to tests/integration/node_dispatch_worker/test_dispatch_worker_multiparam.py.
      Also audited node_skill_dispatch_engine_orchestrator and documented (rather than faked) the declared-but-unreachable
      SkillResultStatus.FAILED member and unwired failure_topic as a real follow-up wiring gap, not a
      test-coverage gap, in tests/integration/node_skill_dispatch_engine_orchestrator/test_dispatch_engine_multiparam.py.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13784/dod-005/command.yaml"
  - id: dod-006
    description: >
      Full omnimarket test suite (uv run pytest tests/ -v -m "not kafka") passes on the feature branch.
      Feature commit SHA f4b995f283a5ad2b53a0f4e62129fbf002bc8851. Branch: jonah/omn-13784-wave7-session-dispatch-state-coverage.
      Repo: OmniNode-ai/omnimarket. PR: 1546. 11089 passed, 52 skipped, 4 pre-existing failures unrelated
      to this change (test_datasource_postgres.py, requires a live local Postgres on :5436, no session/dispatch
      files touched).
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13784/dod-006/command.yaml"
  - id: dod-007
    description: >
      Follow-up quality fix: omnimarket PR #1546 merged via the queue before a fix commit
      (replacing vacuous truthy-string-literal state-coverage-gate assertions with real
      contract-pinning / lifecycle-mapping assertions) reached its branch tip. PR #1551
      lands that fix directly on dev — same ticket, no new scope. Feature commit SHA
      4899325b (cherry-pick of the original fix commit d8f6550d). Branch:
      jonah/omn-13784-fix-vacuous-coverage-assertions. Repo: OmniNode-ai/omnimarket. PR: 1551.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13784/dod-007/command.yaml"
  - id: dod-occ-self
    description: >
      This onex_change_control PR carries the central OMN-13784 contract and PASS receipts required by
      the downstream omnimarket PR #1551 Receipt Gate and OCC preflight eligibility check.
    source: generated
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13784/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
