---
schema_version: "1.0.0"
ticket_id: "OMN-13821"
title: "fix(OMN-13821): reconcile Redpanda capacity manifest to live prod state"
summary: >-
  omninode_infra PR #566 reconciles the committed Redpanda data-plane manifest
  with values already running in prod after the incident response that unblocked
  omninode-runtime-worker startup. The change records the live partition and
  memory capacity settings so a future pod recreate or kustomize apply does not
  silently revert prod to the pre-incident capacity ceiling. This is an interim
  bridge until the managed MSK runtime cutover removes the single-node Redpanda
  partition ceiling.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode_infra-pr-566"
    description: >-
      Consuming-PR binding: omninode_infra PR #566 targets dev from
      codex/omn-13821-redpanda-capacity-reconcile and changes only the
      Redpanda configmap/statefulset capacity fields for the live-state
      reconciliation.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13821/dod-omninode_infra-pr-566/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-gate assessment: PR #566 is a manifest reconciliation of values
      already running in prod. The PR does not perform a live deploy, restart,
      docker exec, kubectl apply, or rpk mutation; it records the current live
      settings for future GitOps/pod-recreate safety.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13821/dod-deploy-assessment/command.yaml && grep -qi 'deploy' drift/dod_receipts/OMN-13821/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying this OMN-13821
      contract and the consuming-PR evidence for omninode_infra PR #566.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13821/dod-occ-self/command.yaml"
