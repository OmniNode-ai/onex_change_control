---
schema_version: '1.0.0'
ticket_id: OMN-13281
title: 'feat(validation): receipt-honesty pre-commit wiring to omnibase_infra'
summary: >-
  Wire the check-receipt-honesty pre-commit hook (OMN-12791) into omnibase_infra. The hook scans staged
  ModelDodReceipt YAML files under drift/dod_receipts/ for gaming patterns (echo-probe PASS, deferred
  evidence, self-attestation, fake human verifiers, deploy-keyword echo probes). omnibase_infra has an
  active drift/dod_receipts/ corpus on dev; omniclaude and omniintelligence have none and are marked N/A.
  Pure pre-commit config wiring — no source changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: >-
      PR omnibase_infra checks green (receipt-honesty hook wired, pre-commit run --all-files passes, fail-closed
      proof captured).
    command: gh pr checks --repo OmniNode-ai/omnibase_infra
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-scope-check
    description: >-
      Pre-scope grep: drift/dod_receipts/ exists in omnibase_infra dev branch (OMN-12184, OMN-13532 present).
      omniclaude and omniintelligence have no drift/dod_receipts/ directory — marked N/A per DoD.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13281/dod-scope-check/command.yaml"
  - id: dod-hook-wired
    description: >-
      check-receipt-honesty hook entry present in omnibase_infra .pre-commit-config.yaml under the omnibase_core
      repo block at rev 309d89fa7254701347f95bf99205cccfb3f6c1df (confirmed to contain the hook).
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13281/dod-hook-wired/command.yaml"
  - id: dod-fail-closed
    description: >-
      Fail-closed proof (verifier != runner): staged a gamed PASS receipt (self-attestation: verifier
      == runner == 'jonah/omn-13281-impl-agent') -> validator_receipt_honesty exits non-zero with SELF_ATTESTATION
      + FAKE_HUMAN_VERIFIER violations; reverted -> green on 5 existing drift/dod_receipts/**/*.yaml files.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13281/dod-fail-closed/command.yaml"
  - id: dod-green-baseline
    description: >-
      pre-commit run check-receipt-honesty on the 5 existing clean receipts in omnibase_infra/drift/dod_receipts/
      returns exit 0 with 'RECEIPT HONESTY GATE PASSED: 0 violations in 5 explicit receipt file(s)'.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13281/dod-green-baseline/command.yaml"
  - id: dod-occ-self
    description: >-
      Self-binding OCC receipt — this onex_change_control PR carries the OMN-13281 contract + receipts
      consumed by omnibase_infra PR via Evidence-Source.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13281/dod-occ-self/command.yaml"
