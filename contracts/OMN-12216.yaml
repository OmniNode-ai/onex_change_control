---
schema_version: "1.0.0"
ticket_id: "OMN-12216"
title: "Create node_recall_compute — fix bus bypass"
summary: >
  OCC evidence for omnimarket PR #909. The change adds node_recall_compute (COMPUTE) to omnimarket to
  fix the architectural violation where the recall skill made direct HTTP calls to localhost:8085 bypassing
  the event bus. Contract-only stub with node_not_implemented: true and 18 unit tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain"
    description: "18 unit tests pass for node_recall_compute on omnimarket PR #909 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12216/dod-golden-chain/command.yaml
          && grep -q '18 passed' drift/dod_receipts/OMN-12216/dod-golden-chain/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy or runtime restart is required before merge for this contract + handler-stub
      slice."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12216/dod-deploy-assessment/command.yaml
          && grep -q 'no live deploy or runtime restart required before merge' drift/dod_receipts/OMN-12216/dod-deploy-assessment/command.yaml"
