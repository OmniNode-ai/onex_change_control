---
schema_version: "1.0.0"
ticket_id: "OMN-13522"
title: "Bind June 23 omniclaude Dependabot dependency PR batch to OCC evidence"
summary: >
  Bind the June 23 omniclaude Dependabot dependency maintenance PR batch to central OCC evidence so Receipt
  Gate can evaluate each PR from a pinned Evidence-Source without bypass tokens.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-1790-head"
    description: "omniclaude PR #1790 carries the kafka-python dependency update for consumers."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13522/dod-omniclaude-1790-head/command.yaml\""
  - id: "dod-omniclaude-1790-nonreceipt-checks"
    description: "omniclaude PR #1790 has no failing non-Receipt-Gate checks at the captured head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13522/dod-omniclaude-1790-nonreceipt-checks/command.yaml\""
  - id: "dod-omniclaude-1791-head"
    description: "omniclaude PR #1791 carries the pytest dependency update for consumers."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13522/dod-omniclaude-1791-head/command.yaml\""
  - id: "dod-omniclaude-1791-nonreceipt-checks"
    description: "omniclaude PR #1791 has no failing non-Receipt-Gate checks at the captured head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13522/dod-omniclaude-1791-nonreceipt-checks/command.yaml\""
  - id: "dod-omniclaude-1792-head"
    description: "omniclaude PR #1792 carries the omnibase-core dependency range update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13522/dod-omniclaude-1792-head/command.yaml\""
  - id: "dod-omniclaude-1792-nonreceipt-checks"
    description: "omniclaude PR #1792 has no failing non-Receipt-Gate checks at the captured head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13522/dod-omniclaude-1792-nonreceipt-checks/command.yaml\""
  - id: "dod-omniclaude-1793-head"
    description: "omniclaude PR #1793 carries the GitHub Actions dependency group update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13522/dod-omniclaude-1793-head/command.yaml\""
  - id: "dod-omniclaude-1793-nonreceipt-checks"
    description: "omniclaude PR #1793 has no failing non-Receipt-Gate checks at the captured head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13522/dod-omniclaude-1793-nonreceipt-checks/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR #2957 carries the OMN-13522 contract and receipts for the June
      23 omniclaude dependency batch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13522/dod-occ-pr-self/command.yaml\""
