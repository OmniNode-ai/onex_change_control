---
schema_version: "1.0.0"
ticket_id: "OMN-11498"
title: "ADK tools — register_generated_tool + invoke_generated_tool"
summary: >-
  onex-self-extending-agent PR #82 adds two ADK tool functions to src/agent/tools.py: register_generated_tool()
  validates and registers a dynamically generated node into the in-process ToolRegistry, and invoke_generated_tool()
  invokes it by name with JSON input, returning JSON output.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: tests
    description: "All 4 new ADK tool unit tests pass plus full suite."
    command: "uv run pytest tests/unit/test_tools_register_invoke.py -v && uv run pytest tests/unit -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-82"
    description: "onex-self-extending-agent PR #82 carries the ADK tool functions."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11498/dod-product-pr-82/command.yaml"
          && grep -q '^pr_number: 82$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11498/dod-product-pr-82/command.yaml"
  - id: "dod-unit-tests"
    description: "4 ADK tool tests pass; 620 total pass."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11498/dod-unit-tests/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11498 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11498/dod-occ-self/command.yaml"
