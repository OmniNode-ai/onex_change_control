---
schema_version: "1.0.0"
ticket_id: "OMN-14753"
title: "Docs-only selector should not over-escalate to full unit suite"
summary: >
  omnibase_infra PR #2372 updates the governed test-path selector so docs-only markdown diffs do not over-escalate
  to the full unit test suite, with model and selector regression coverage.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #2372 is open at the recorded head with expected selector files and
      verification claims."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14753/dod-omnibase_infra-pr-2372/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_infra-pr-2372"
    description: >
      Binds omnibase_infra PR #2372 at head 1122cc6a4d0e50e28273d1abe0c82b1320bc4f73. The PR body records
      targeted selector/model tests, pre-commit, and governed pre-push selector evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14753/dod-omnibase_infra-pr-2372/command.yaml
          && grep -q '1122cc6a4d0e50e28273d1abe0c82b1320bc4f73' drift/dod_receipts/OMN-14753/dod-omnibase_infra-pr-2372/command.yaml
          && grep -q 'scripts/ci/detect_test_paths.py' drift/dod_receipts/OMN-14753/dod-omnibase_infra-pr-2372/command.yaml
          && grep -q '32 passed' drift/dod_receipts/OMN-14753/dod-omnibase_infra-pr-2372/command.yaml
          && grep -q '1620 passed, 1 skipped' drift/dod_receipts/OMN-14753/dod-omnibase_infra-pr-2372/command.yaml
  - id: "occ-self-bind-pr-4549"
    description: >
      Self-binding OCC eligibility receipt for onex_change_control PR #4549, which carries OMN-14753 central
      evidence for omnibase_infra PR #2372.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 4549 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
