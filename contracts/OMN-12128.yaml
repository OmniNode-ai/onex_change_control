---
schema_version: "1.0.0"
ticket_id: "OMN-12128"
title: "Add swarm handler routing for missing auto-wired contracts"
summary: >
  Central OCC evidence for omnimarket PR #866, which adds handler_routing to the swarm endpoint health,
  decomposer, and fanout contracts so Kafka dispatch can wire their handlers from manifests.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "event_bus"
  - "runtime_node_contract"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-866"
    description: "omnimarket PR #866 carries the OMN-12128 handler_routing contract repair."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12128/dod-omnimarket-pr-866/command.yaml"
  - id: "dod-local-validation"
    description: >
      Foreground merge-sweep validation passed for YAML hygiene and the imperative contract guard after
      rebasing #866 on current dev.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12128/dod-local-validation/command.yaml
          && grep -q 'new 0' drift/dod_receipts/OMN-12128/dod-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      No live deploy was performed during the merge sweep. The handler-routing contract updates apply
      on the next normal runtime deploy.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12128/dod-deploy-assessment/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-12128/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC evidence for OMN-12128."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12128/dod-occ-pr/command.yaml"
