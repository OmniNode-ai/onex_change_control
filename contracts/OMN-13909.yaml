---
schema_version: "1.0.0"
ticket_id: "OMN-13909"
title: "ci(OMN-13909): add synchronize+reopened triggers to pr-title-check to fix org-wide merge wedge"
summary: >-
  onex_change_control PR #3542 adds synchronize and reopened to the pull_request trigger types in .github/workflows/pr-title-check.yml,
  fixing a required-check wedge where a PR pushed without a title edit never re-reports pr-title / check-title
  on its new head SHA. Full local test suite passed (3324/3325 relevant, 1 pre-existing unrelated failure,
  46 legitimate skips), pre-commit clean. This contract is self-referencing: the consuming PR and the
  evidence PR are the same PR, since the fix lands in onex_change_control itself.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-self"
    description: >-
      Self-binding: onex_change_control PR #3542 carries both the OMN-13909 fix and this evidence contract
      in the same commit set.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13909/dod-self/command.yaml"
