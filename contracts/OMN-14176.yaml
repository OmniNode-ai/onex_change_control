---
schema_version: "1.0.0"
ticket_id: "OMN-14176"
title: "ci(OMN-14176): short-circuit hostile reviewer when model endpoint is unreachable"
summary: >-
  Central OCC evidence for omnimarket PR #1654. The PR adds an endpoint preflight to the Hostile Reviewer
  workflow so the known-unreachable deepseek-r1 endpoint returns the existing degraded non-blocking verdict
  before performing the large dependency install. This is CI-process latency work only; it does not change
  reviewer verdict semantics, deploy runtime services, restart live systems, or mutate branch protection.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1654"
    description: >-
      omnimarket PR #1654 is open against dev at head e940771847d724a6ef430f591b391baf1cd5c7d4 and changes
      only .github/workflows/hostile-reviewer.yml to add a fail-open endpoint preflight that preserves
      the current degraded exit-0 behavior when the review endpoint is down.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14176/dod-omnimarket-pr-1654/command.yaml &&
          grep -q '^pr_number: 1654$' drift/dod_receipts/OMN-14176/dod-omnimarket-pr-1654/command.yaml
          && grep -q '^commit_sha: e940771847d724a6ef430f591b391baf1cd5c7d4$' drift/dod_receipts/OMN-14176/dod-omnimarket-pr-1654/command.yaml
  - id: "dod-no-live-deploy"
    description: >-
      Scope evidence: OMN-14176 is CI workflow optimization only. It performs no live deploy, no runtime
      restart, no branch-protection mutation, and no bypass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14176/dod-no-live-deploy/command.yaml && grep
          -qi 'no live deploy' drift/dod_receipts/OMN-14176/dod-no-live-deploy/command.yaml
  - id: "dod-occ-self"
    description: >-
      Self-binding receipt: this onex_change_control PR carries the OMN-14176 central contract and PASS
      receipts consumed by omnimarket PR #1654 through its Evidence-Source line.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14176/dod-occ-self/command.yaml
