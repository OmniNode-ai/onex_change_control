---
schema_version: "1.0.0"
ticket_id: "OMN-16228"
title: "OCC companion for OMN-16228"
summary: >
  Hand-authored OCC Evidence-Source companion for OmniNode-ai/omnibase_infra#2793 (scan uv.lock for known
  CVEs in PR CI -- shift lockfile vulnerability detection left of image-build time, born from the 2026-08-18
  sqlparse/Trivy incident, OMN-16170).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2793 CI checks green"
    command: "gh pr checks 2793 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2793"
    description: "PR #2793 on OmniNode-ai/omnibase_infra — Evidence-Source binding."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2793 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName"
  - id: "dod-omnibase_infra-pr-2793-strict-gate"
    description: "PR #2793 on OmniNode-ai/omnibase_infra — content-bound probe confirming the new lockfile-cve-scan
      job is registered in scripts/ci/ci_summary_gate.py's STRICT_GATE_JOBS at the PR's head ref (OMN-15309
      admissibility: reads product content at a pinned ref, not PR/issue metadata; OMN-15411 Rule E SIGPIPE-safe:
      producer output is buffered into a shell variable before the grep -q consumer reads it)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "body=\"$(gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/ci/ci_summary_gate.py?ref=8623ed52944909ca53ac336479c09714c24d4226
          --jq .content | base64 -d)\" && printf '%s' \"$body\" | grep -qF 'Lockfile CVE Scan (OMN-16228)'"
