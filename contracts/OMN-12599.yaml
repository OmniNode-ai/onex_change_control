---
schema_version: "1.0.0"
ticket_id: "OMN-12599"
title: "Bind compat no-infra-edge gate to OCC evidence"
summary: >-
  Central evidence for omnibase_compat PR #131, which moves the durable compat validation source of truth
  into omnibase_compat and proves the compat package does not import, install, or resolve omnibase_infra/core/spi
  as part of its validation closure.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-compat-pr-131-binding"
    description: "Bind OMN-12599 central OCC evidence to downstream omnibase_compat PR #131 and its current
      head commit."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12599/dod-compat-pr-131-binding/command.yaml"
          && grep -q '^pr_number: 131$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12599/dod-compat-pr-131-binding/command.yaml"
          && grep -q '^commit_sha: "eaa2e32457f83f426dfb932679df96f1c0d5e02c"$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12599/dod-compat-pr-131-binding/command.yaml"
  - id: "dod-no-infra-edge-local-validation"
    description: "The compat no-infra-edge guard, upstream-import validator, and unit tests pass locally
      with no infra dependency edge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12599/dod-no-infra-edge-local-validation/command.yaml"
          && grep -q 'no omnibase_infra/core/spi edge' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12599/dod-no-infra-edge-local-validation/command.yaml"
          && grep -q '7 passed' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12599/dod-no-infra-edge-local-validation/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12599 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12599/dod-occ-pr-self/command.yaml"
