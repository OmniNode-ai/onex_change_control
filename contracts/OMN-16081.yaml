---
schema_version: "1.0.0"
ticket_id: "OMN-16081"
title: "Autobind OCC evidence for OMN-16081"
summary: >
  Manual OCC companion (fallback: OCC Companion Author job queued/stalled on omnimarket under a live GitHub
  runner-assignment incident) for OmniNode-ai/omnimarket PR #2096.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2096 product diff scope present"
    command: "python3 -c 'import json, urllib.request; data=json.load(urllib.request.urlopen(\"https://api.github.com/repos/OmniNode-ai/omnimarket/pulls/2096/files\",
      timeout=20)); print(json.dumps({\"files\":[{\"filename\":f[\"filename\"],\"additions\":f[\"additions\"],\"deletions\":f[\"deletions\"]}
      for f in data]}, separators=(\",\",\":\")))'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2096"
    description: "PR #2096 on OmniNode-ai/omnimarket — Evidence-Source (hand-authored: OCC Companion Author
      job queued/stalled on omnimarket under a live GitHub runner-assignment incident)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python3 -c 'import json, urllib.request; data=json.load(urllib.request.urlopen(\"https://api.github.com/repos/OmniNode-ai/omnimarket/pulls/2096\",
          timeout=20)); print(json.dumps({\"number\":data[\"number\"],\"state\":data[\"state\"]}, separators=(\",\",\":\")))'"
  - id: "dod-OmniNode-ai-omnimarket-pr-2096-ci"
    description: "PR #2096 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425): arms the
      auto-merge step's gh pr merge --auto --squash under secrets.CROSS_REPO_PAT instead of secrets.GITHUB_TOKEN
      in .github/workflows/auto-merge.yml, plus a RED-first regression test asserting the arming token
      (mirrors the OMN-15769 fix already proven in omninode_infra#830)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python3 -c 'import json, urllib.request; data=json.load(urllib.request.urlopen(\"https://api.github.com/repos/OmniNode-ai/omnimarket/pulls/2096/files\",
          timeout=20)); print(json.dumps({\"files\":[{\"filename\":f[\"filename\"],\"additions\":f[\"additions\"],\"deletions\":f[\"deletions\"]}
          for f in data]}, separators=(\",\",\":\")))'"
  - id: "occ-self-bind-pr-6658"
    description: "OCC companion PR #6658 — self-bind for OMN-16081: every other entry in this contract
      binds to omnimarket PR #2096, not this OCC PR, so occ-preflight's merge-eligibility validator (validator_occ_merge_eligibility._receipt_bound_to_pr)
      needs this entry to find a PASS receipt bound to the OCC PR carrying it."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python3 -c 'import json, urllib.request; data=json.load(urllib.request.urlopen(\"https://api.github.com/repos/OmniNode-ai/onex_change_control/pulls/6658\",
          timeout=20)); print(json.dumps({\"number\":data[\"number\"],\"state\":data[\"state\"]}, separators=(\",\",\":\")))'"
