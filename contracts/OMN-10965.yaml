---
schema_version: "1.0.0"
ticket_id: "OMN-10965"
title: "Reduce cognitive complexity in handler_quality_gate.py"
summary: "Extracts _evaluate_deterministic_checks and _evaluate_heuristic_checks sub-functions from _run_contract_checks
  to resolve SonarCloud S3776 cognitive complexity violation. Pure refactor with identical behavior."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Full omnimarket test suite passes: 4068 passed"
    command: "gh pr checks 675 --repo OmniNode-ai/omnimarket"
  - kind: "ci"
    description: "SonarCloud passes on omnimarket PR #675"
    command: "gh pr checks 675 --repo OmniNode-ai/omnimarket"
  - kind: "manual"
    description: "Pure refactor — no runtime restart or Kafka topic change required"
    command: "printf '%s\n' 'deploy gate: cognitive complexity refactor — pure Python restructuring; no
      docker exec or rpk topic produce required'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Full omnimarket test suite passes: 4068 passed, 0 failed"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json headRefOid,title"
  - id: "dod-deploy"
    description: "Pure refactor — no runtime restart or Kafka topic change required"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\n' 'deploy gate: cognitive complexity refactor — pure Python restructuring;
          no docker exec or rpk topic produce required'"
