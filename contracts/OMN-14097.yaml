---
schema_version: "1.0.0"
ticket_id: "OMN-14097"
title: "Autobind OCC evidence for OMN-14097"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1845."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1845 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1845"
    description: "PR #1845 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1845-ci"
    description: "PR #1845 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4525"
    description: "OCC companion PR #4525 — self-bind for OMN-14097 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-assessment-pr-1845"
    description: "Deploy-scope assessment for omnimarket#1845 runtime handler change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14097/dod-deploy-assessment-pr-1845/command.yaml
          && grep -q 'deploy assessment' drift/dod_receipts/OMN-14097/dod-deploy-assessment-pr-1845/command.yaml"
  - id: "occ-self-bind-pr-4527"
    description: "OCC companion PR #4527 -- self-bind for OMN-14097 deploy assessment."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName"
