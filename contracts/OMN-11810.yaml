---
schema_version: "1.0.0"
ticket_id: "OMN-11810"
title: "node_swarm_endpoint_health_effect"
summary: >
  EFFECT node that probes swarm model endpoints via HTTP to determine
  reachability and model availability. Subscribes to
  onex.cmd.omnimarket.swarm-check-endpoint-health.v1 and publishes
  onex.evt.omnimarket.swarm-endpoint-health-completed.v1. Returns typed
  EndpointHealth per endpoint with endpoint_status and model_status fields.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-782"
    description: "omnimarket PR #782 implements node_swarm_endpoint_health_effect with 14 unit tests"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11810/dod-omnimarket-pr-782/command.yaml\"
          && grep -q '^pr_number: 782$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11810/dod-omnimarket-pr-782/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR binds this contract to onex_change_control as the evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11810/dod-occ-pr/command.yaml\""
  - id: "dod-deploy"
    description: "Deploy assessment — new EFFECT node, no live deploy required for initial merge; node
      is invoked on-demand via contract topic"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11810/dod-deploy/command.yaml\""
