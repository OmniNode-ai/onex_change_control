---
schema_version: "1.0.0"
ticket_id: "OMN-10915"
title: "Delegation contract-config remediation: typed runtime profile"
summary: "Replace env var reads in delegation runtime with contract-driven typed config. Wave 1 delivers
  typed models, validators, canonical YAML, and CI scanner; Wave 2 wires config loader and replaces direct
  env reads."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
  - protocols
evidence_requirements:
  - kind: "tests"
    description: "Typed delegation runtime profile models and validators pass CI"
    command: "gh pr checks --repo OmniNode-ai/omnibase_core 1072 && gh pr checks --repo OmniNode-ai/omnibase_core
      1073"
  - kind: "ci"
    description: "Config loader and CI env scanner PRs pass CI"
    command: "gh pr checks --repo OmniNode-ai/omnibase_infra 1577 && gh pr checks --repo OmniNode-ai/omnibase_infra
      1578"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1072"
    description: "omnibase_core PR #1072 adds typed ModelDelegationRuntimeProfileContract models (OMN-10919)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10915/dod-core-pr-1072/command.yaml
          && grep -q '^pr_number: 1072$' drift/dod_receipts/OMN-10915/dod-core-pr-1072/command.yaml"
  - id: "dod-core-pr-1073"
    description: "omnibase_core PR #1073 adds delegation runtime profile contract validators (OMN-10920)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10915/dod-core-pr-1073/command.yaml
          && grep -q '^pr_number: 1073$' drift/dod_receipts/OMN-10915/dod-core-pr-1073/command.yaml"
  - id: "dod-infra-pr-1577"
    description: "omnibase_infra PR #1577 adds CI env read scanner for delegation modules (OMN-10917)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10915/dod-infra-pr-1577/command.yaml
          && grep -q '^pr_number: 1577$' drift/dod_receipts/OMN-10915/dod-infra-pr-1577/command.yaml"
  - id: "dod-infra-pr-1578"
    description: "omnibase_infra PR #1578 adds DelegationProfileConfigLoader (OMN-10923)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10915/dod-infra-pr-1578/command.yaml
          && grep -q '^pr_number: 1578$' drift/dod_receipts/OMN-10915/dod-infra-pr-1578/command.yaml"
  - id: "dod-occ-pr-983"
    description: "OCC PR #983 carries canonical delegation runtime profile YAML contract (OMN-10921)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10915/dod-occ-pr-983/command.yaml
          && grep -q '^pr_number: 983$' drift/dod_receipts/OMN-10915/dod-occ-pr-983/command.yaml"
  - id: "dod-occ-self"
    description: "This OCC PR carries the OMN-10915 epic contract and all Wave 1 downstream receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10915 -type f -name 'command.yaml' | grep -q ."
