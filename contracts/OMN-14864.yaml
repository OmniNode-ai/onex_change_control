---
schema_version: "1.0.0"
ticket_id: "OMN-14864"
title: "Autobind OCC evidence for OMN-14864"
summary: "OCC Evidence-Source autobind companion for onex_change_control PR #4512."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #4512 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-onex-change-control-pr-4512"
    description: "PR #4512 on OmniNode-ai/onex_change_control -- Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4512 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-onex-change-control-pr-4512-ci"
    description: "PR #4512 on OmniNode-ai/onex_change_control -- product diff scope check."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4512 --repo OmniNode-ai/onex_change_control --json files"
  - id: "occ-self-bind-pr-4512"
    description: "OCC/product PR #4512 -- self-bind for OMN-14864."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4512 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omniclaude-pr-1977-product-head-b6081778"
    description: >-
      Product-head verification for omniclaude PR #1977 at b6081778787953038cd5ea63535cefaf3d3ad747. The
      proof reads live PR metadata and changed files from GitHub, then checks the branch is based on dev,
      carries the OMN-14864 required-OCC-context title, and includes the reusable OCC companion-effect
      workflow, secrets baseline update, and workflow/CI regression tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1977; PRODUCT_REPO=OmniNode-ai/omniclaude; PRODUCT_HEAD=b6081778787953038cd5ea63535cefaf3d3ad747;
          d="$(mktemp -d)" && gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,baseRefName,title,files
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
          > "${d}/pr.txt" && grep -Fq "${PRODUCT_HEAD}" "${d}/pr.txt" && grep -Fxq dev "${d}/pr.txt" &&
          grep -Fq 'fix(ci): materialize required OCC context for bots (OMN-14864)' "${d}/pr.txt" && for
          p in .github/workflows/call-occ-companion-effect-reusable.yml .secrets.baseline tests/ci/test_omn_15373_no_ungated_linear_done_policy.py
          tests/workflows/test_call_occ_companion_effect_reusable.py; do grep -Fq "$p" "${d}/pr.txt";
          done
  - id: "dod-omniclaude-pr-1977-diff-scope-b6081778"
    description: >-
      Diff-scope verification for omniclaude PR #1977 at b6081778787953038cd5ea63535cefaf3d3ad747. The
      diff is limited to GitHub workflow configuration, the secrets baseline, and regression tests; it
      does not touch runtime source, live deploy scripts, systemd units, Docker compose, or .201 deployment
      surfaces.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1977; PRODUCT_REPO=OmniNode-ai/omniclaude; PRODUCT_HEAD=b6081778787953038cd5ea63535cefaf3d3ad747;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,files --jq '.headRefOid
          == "'${PRODUCT_HEAD}'" and ((.files | map(.path)) | all((startswith(".github/") or startswith("tests/")
          or (. == ".secrets.baseline")) and (. | test("(^|/)(src|systemd|deploy/scripts|docker/compose)(/|$)")
          | not)))' | grep -qx true
  - id: "dod-omniclaude-pr-1977-body-proof-b6081778"
    description: >-
      Hosted-readable proof-summary verification for omniclaude PR #1977 at b6081778787953038cd5ea63535cefaf3d3ad747.
      Reads the live PR body and asserts it carries the concrete verification markers for the focused
      workflow/CI regression tests, changed-file pre-commit run, and pre-push hooks reported by the author.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1977; PRODUCT_REPO=OmniNode-ai/omniclaude; PRODUCT_HEAD=b6081778787953038cd5ea63535cefaf3d3ad747;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid --jq '.headRefOid
          == "'${PRODUCT_HEAD}'"' | grep -qx true && BODY="$(gh pr view "${PRODUCT_PR_NUMBER}" --repo
          "${PRODUCT_REPO}" --json body --jq '.body')" && echo "$BODY" | grep -Fq 'uv run pytest tests/workflows/test_call_occ_companion_effect_reusable.py
          tests/ci/test_omn_15373_no_ungated_linear_done_policy.py -q' && echo "$BODY" | grep -Fq '19
          passed' && echo "$BODY" | grep -Fq 'uv run pre-commit run --files .github/workflows/call-occ-companion-effect-reusable.yml'
          && echo "$BODY" | grep -Fq 'pre-push hooks: mypy, pyright, ONEX validation, root cleanliness,
          secret detection, model and DoD checks'
