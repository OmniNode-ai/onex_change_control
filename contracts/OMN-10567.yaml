---
schema_version: "1.0.0"
ticket_id: "OMN-10567"
title: "chore: apply audited contract config declarations"
summary: "For each config_required row in the OMN-10565 audit, set metadata.transport_type on the contract.
  needs_review rows are not auto-edited per ChatGPT review §8."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Receipt Gate passes on omnimarket PR #536 after this OCC evidence lands."
    command: "gh pr checks 536 --repo OmniNode-ai/omnimarket --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Apply script idempotently writes metadata.transport_type to 122 config_required contracts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10567/dod-001/command.yaml\"; else uv run python
          scripts/audit/apply_contract_config.py --summary; fi"
  - id: "dod-002"
    description: "5 unit tests assert every config_required node has metadata.transport_type set to a
      known transport, and the apply script is idempotent."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10567/dod-002/command.yaml\"; else uv run pytest
          tests/unit/contracts/test_contract_classification.py -v; fi"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10567 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10567/dod-occ-pr/command.yaml\""
