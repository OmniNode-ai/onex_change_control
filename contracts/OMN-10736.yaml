---
schema_version: "1.0.0"
ticket_id: "OMN-10736"
title: "Fix 4 silent fallback violations in omniintelligence (OMN-10658 follow-up)"
summary: >
  Binds omniintelligence PR #644, which replaces 4 hardcoded private-IP env fallbacks with fail-fast os.environ[KEY]
  reads, extends the fallback validator to catch 192.168.x.x patterns, and wires it as a pre-commit hook
  + CI gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-644-head"
    description: "omniintelligence PR #644 carries the fallback fixes and validator wiring at the recorded
      implementation head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10736/dod-pr-644-head/command.yaml\""
  - id: "dod-validator-clean"
    description: "scripts/validate_no_env_fallbacks.py exits 0 with no violations on the PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10736/dod-validator-clean/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10736/dod-occ-pr/command.yaml\""
