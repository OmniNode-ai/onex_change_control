---
schema_version: "1.0.0"
ticket_id: "OMN-15335"
title: "Autobind OCC evidence for OMN-15335"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #738."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #738 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-738"
    description: "PR #738 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15335/dod-OmniNode-ai-omninode_infra-pr-738/command.yaml"
  - id: "dod-OmniNode-ai-omninode_infra-pr-738-ci"
    description: "PR #738 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15335/dod-OmniNode-ai-omninode_infra-pr-738-ci/command.yaml"
  - id: "occ-self-bind-pr-5364"
    description: "OCC companion PR #5364 — self-bind for OMN-15335 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-5376"
    description: "Binds OMN-15335 to OCC companion PR #5376 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-5391"
    description: "Binds OMN-15335 tombstone evidence repair to OCC PR #5391 (self-bind)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5391 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "occ-self-bind-pr-5364-superseded"
    description: >-
      Append-only supersession of occ-self-bind-pr-5364. The original check_value (`gh pr view ${PR_NUMBER}
      --repo ${REPO} --json number,state`) is a template-placeholder defect: PR_NUMBER/REPO are substituted
      only for whichever onex_change_control PR is currently running the compliance gate, never for OCC#5364
      itself, so the check can never verify what it claims (same class as the OMN-14888 D2 template-placeholder
      repair precedent). Repaired with a hardcoded merged-terminal-state predicate against the real target:
      OCC PR #5364, MERGED 2026-07-28T20:53:15Z, merge commit 9c9c932bd1389228c206ad91571bbd478722d40c
      (confirmed live via `gh pr view 5364 --repo OmniNode-ai/onex_change_control --json state,mergedAt,mergeCommit`).
      The superseded entry stays verbatim above for audit.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-5364"
    checks:
      - check_type: "command"
        check_value: >-
          state="$(gh pr view 5364 --repo OmniNode-ai/onex_change_control --json state --jq .state)" &&
          merge_sha="$(gh pr view 5364 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq
          .mergeCommit.oid)" && test "$state" = "MERGED" && test "$merge_sha" = "9c9c932bd1389228c206ad91571bbd478722d40c"
          && printf 'OCC 5364 merged-terminal-state PASS\n'
  - id: "occ-self-bind-pr-5376-superseded"
    description: >-
      Append-only supersession of occ-self-bind-pr-5376. Same template-class defect as occ-self-bind-pr-5364-superseded
      above (check_value `gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state`). Repaired with
      a hardcoded merged-terminal-state predicate against the real target: OCC PR #5376, MERGED 2026-07-28T22:48:06Z,
      merge commit 33bffbac24441c6d5193f1a669ec151205be0ed7 (confirmed live via `gh pr view 5376 --repo
      OmniNode-ai/onex_change_control --json state,mergedAt,mergeCommit`). The superseded entry stays
      verbatim above for audit.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-5376"
    checks:
      - check_type: "command"
        check_value: >-
          state="$(gh pr view 5376 --repo OmniNode-ai/onex_change_control --json state --jq .state)" &&
          merge_sha="$(gh pr view 5376 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq
          .mergeCommit.oid)" && test "$state" = "MERGED" && test "$merge_sha" = "33bffbac24441c6d5193f1a669ec151205be0ed7"
          && printf 'OCC 5376 merged-terminal-state PASS\n'
