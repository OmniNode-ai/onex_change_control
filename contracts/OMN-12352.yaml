---
schema_version: "1.0.0"
ticket_id: "OMN-12352"
title: "Implement env parity compute runtime handler"
summary: >
  Canonical OCC evidence for omnimarket PR #962. The PR replaces node_env_parity_compute's runtime stub
  with a native Onex compute handler, contract-owned env parity configuration, typed request/result models,
  and runtime adapter coverage for command plus terminal event-bus topics.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-962"
    description: "omnimarket PR #962 carries the native env parity compute implementation at the recorded
      head SHA."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12352/dod-omnimarket-pr-962/command.yaml
          && grep -q '^pr_number: 962$' drift/dod_receipts/OMN-12352/dod-omnimarket-pr-962/command.yaml
          && grep -q '^commit_sha: \"26eaac85c251dbaadd7e960bc61ac50cb8a6ee91\"$' drift/dod_receipts/OMN-12352/dod-omnimarket-pr-962/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused env parity golden-chain tests pass through the native runtime adapter."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12352/dod-focused-tests/command.yaml"
  - id: "dod-runtime-adapter-proof"
    description: "Runtime adapter dispatch proves contract-owned command and terminal topics for env_parity_compute."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12352/dod-runtime-adapter-proof/command.yaml"
  - id: "dod-broader-relevant-tests"
    description: "Broader relevant omnimarket tests pass outside unrelated repo-wide CI debt."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12352/dod-broader-relevant-tests/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Repo-local deploy-gate contract is present and validates; no live deploy or runtime
      restart was performed pre-merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12352/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-1816"
    description: "OCC PR #1816 carries the central OMN-12352 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12352/dod-occ-pr-1816/command.yaml
          && grep -q '^pr_number: 1816$' drift/dod_receipts/OMN-12352/dod-occ-pr-1816/command.yaml"
  - id: "dod-occ-pr-1820"
    description: "OCC PR #1820 rebinds OMN-12352 receipts to the final omnimarket PR #962 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12352/dod-occ-pr-1820/command.yaml
          && grep -q '^pr_number: 1820$' drift/dod_receipts/OMN-12352/dod-occ-pr-1820/command.yaml"
