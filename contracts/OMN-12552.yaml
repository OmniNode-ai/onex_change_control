---
schema_version: "1.0.0"
ticket_id: "OMN-12552"
title: "Bind SEA stack reconciliation evidence"
summary: >-
  Bind central evidence for onex-self-extending-agent PR #191, which imports the full SEA stack-vs-dev
  reconciliation report so OMN-12468 children can be re-scoped or marked superseded without force-merging
  stale stack branches.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-191"
    description: "Bind onex-self-extending-agent PR #191 head and evidence import."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12552/dod-sea-pr-191/command.yaml"
          && grep -q '^pr_number: 191$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12552/dod-sea-pr-191/command.yaml"
          && grep -q '^commit_sha: "b77c63f26f77e4e84b2a8627bba2e99b72f97aec"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12552/dod-sea-pr-191/command.yaml"
  - id: "dod-local-validation"
    description: "Local docs-only validation and commit hooks passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12552/dod-local-validation/command.yaml"
          && grep -q 'diff --check passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12552/dod-local-validation/command.yaml"
          && grep -q 'Reject skip-gate bypass tokens.*Passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12552/dod-local-validation/command.yaml"
  - id: "dod-stale-pr-comments"
    description: "Stale SEA stack PRs #167 and #172 were marked with reconciliation guidance."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12552/dod-stale-pr-comments/command.yaml"
          && grep -q 'pull/167#issuecomment-4586721819' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12552/dod-stale-pr-comments/command.yaml"
          && grep -q 'pull/172#issuecomment-4586721817' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12552/dod-stale-pr-comments/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12552 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12552/dod-occ-pr-self/command.yaml"
          && grep -q '^pr_number: 1997$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12552/dod-occ-pr-self/command.yaml"
