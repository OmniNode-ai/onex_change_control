---
schema_version: "1.0.0"
ticket_id: "OMN-14774"
title: "Autobind OCC evidence for OMN-14774"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1821."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1821 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1821"
    description: "PR #1821 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1821-ci"
    description: "PR #1821 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4417"
    description: "OCC companion PR #4417 — self-bind for OMN-14774 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-1821-deploy"
    description: >-
      Deploy gate assessment for omnimarket#1821 runtime PR lifecycle orchestrator change. No live deploy,
      restart, Kafka mutation, database mutation, or .201 mutation is performed by this merge sweep.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          DEPLOY_ASSESSMENT=deploy-required PRODUCT_PR_NUMBER=1821 PRODUCT_REPO=OmniNode-ai/omnimarket
          PRODUCT_HEAD=cb48748953c1540f8aca4c59e015addfd8639a56 gh pr view 1821 --repo OmniNode-ai/omnimarket
          --json headRefOid,files --jq '.headRefOid == "cb48748953c1540f8aca4c59e015addfd8639a56" and
          ([.files[].path] | index("src/omnimarket/nodes/node_pr_lifecycle_orchestrator/handlers/handler_pr_lifecycle_orchestrator.py")
          != null)' | grep -qx true
  - id: "occ-self-bind-pr-4418"
    description: "OCC deploy-evidence repair PR #4418 — self-bind for OMN-14774."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
