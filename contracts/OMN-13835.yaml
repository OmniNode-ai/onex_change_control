---
schema_version: "1.0.0"
ticket_id: "OMN-13835"
title: "feat(OMN-13835): re-enable onex hooks and skill-substitution guard"
summary: >-
  omniclaude PR #1831 restores the onex hook registrations that were deliberately disabled for the OMN-13244
  measurement baseline and adds a PreToolUse skill-substitution guard. The guard blocks raw gh pr merge
  usage in favor of the merge automation skill, warns on raw sweep/dispatch substitutions, and records
  friction when an operator uses the proceed-anyway override window.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1831"
    description: >-
      Consuming-PR binding: omniclaude PR #1831 carries the restored hooks.json, the guard module/wrapper,
      the raw-command-to-skill map, and focused unit tests at the recorded head commit.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13835/dod-omniclaude-pr-1831/command.yaml"
  - id: "dod-local-validation"
    description: >-
      Local validation recorded by the PR: focused guard tests pass, ruff/mypy/pre-commit/pre-push gates
      pass on the changed files, and full-suite residual failures are documented as pre-existing on dev.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13835/dod-local-validation/command.yaml"
  - id: "dod-occ-self"
    description: >-
      This onex_change_control PR carries the central OMN-13835 contract and PASS receipts consumed by
      omniclaude PR #1831 through OCC preflight and Receipt Gate.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13835/dod-occ-self/command.yaml"
