---
schema_version: "1.0.0"
ticket_id: "OMN-13090"
title: "Phase 0a: updatedToolOutput shape probe + evidence doc"
summary: >-
  Empirical resolution of the contested PostToolUse updatedToolOutput shape (F4) for the OMN-13089 skill-output-suppression
  epic. Probes ran against the installed Claude Code CLI 2.1.175 via throwaway hooks in a scratch project,
  with session-transcript JSONL as ground truth. Verdicts: string form is schema-rejected (fail-open,
  invisible to the model); object form {stdout, stderr, interrupted, isImage} REPLACES the Bash tool result
  and is the Layer C mechanism (PreToolUse updatedInput fallback NOT needed). Hook ordering: with two
  updatedToolOutput emitters on one matcher, the LAST registered hook wins (positional, confirmed with
  reversed-order control). Hook environment: ONEX_STATE_DIR is inherited by hook processes (settings env
  block); ONEX_ARTIFACT_STORE_ROOT is absent everywhere — hook wrapper injection required before Phase
  1 ArtifactStore calls. Bonus (plan Open Q5): the Bash tool merges stderr into stdout before PostToolUse
  hooks fire. Evidence doc committed at omniclaude docs/research/2026-06-12-updated-tool-output-shape-probe.md.
  The PR also clears pre-existing pre-commit gate violations surfaced by --all-files (SPDX headers, legacy
  session-id read, hardcoded demo topic strings) so the branch ships with a fully green gate run.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-probe-doc"
    description: "Probe evidence doc exists and full omniclaude suite is green."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13090/dod-probe-doc/command.yaml"
  - id: "dod-deploy-not-required"
    description: >-
      Deploy disposition for the runtime path touched by this PR (src/omniclaude/nodes/node_golden_chain_sweep_orchestrator/__main__.py):
      the change is a comment-only SPDX header normalization (2026 -> 2025) plus a blank line — zero behavior
      change, no contract/handler/topic change, so no redeploy is dispatched for this ticket.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13090/dod-deploy-not-required/command.yaml
          \ # deploy disposition: comment-only SPDX change, no redeploy required"
