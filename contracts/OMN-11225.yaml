---
schema_version: "1.0.0"
ticket_id: "OMN-11225"
title: "feat(OMN-11225): merge ModelOvernightContract into ModelSessionContract"
summary: >
  Task 1 of epic OMN-11224. Merges the three overnight-specific model files (model_overnight_contract.py,
  model_overnight_phase_spec.py, model_overnight_halt_condition.py) into their session equivalents. Expands
  ModelSessionHaltCondition with on_halt, skill, pr, threshold_minutes, and outcome fields. Expands ModelSessionPhaseSpec
  with required_outcomes and halt_conditions fields. Adds ModelPhaseExitCondition with per-condition-type
  validation. Deletes overnight model files and removes their exports. Ships 17 new unit tests (TDD-first).
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "55 overseer model unit tests pass (17 new for unification, 8 new for exit conditions)"
    command: "cd omnibase_core && PYTHONPATH=src uv run pytest tests/unit/models/overseer/ -v"
  - kind: "ci"
    description: "omnibase_core PR #1107 carries the unification implementation with CI green"
    command: "gh api repos/OmniNode-ai/omnibase_core/pulls/1107 --jq '.state + \" \" + .base.ref'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1107"
    description: "omnibase_core PR #1107 carries ModelSessionContract unification with 55 passing overseer
      model unit tests, mypy strict clean, ruff clean, and all pre-commit hooks passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_core/pulls/1107 --jq '.state + \" \" + .base.ref'"
  - id: "dod-deploy-assessment"
    description: "No live deploy, runtime restart, .201 mutation, or event-bus write performed. Pure model-layer
      change — deletes overnight models, expands session models. No DB migration, no runtime service restart,
      no bus subscription activated in this PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'Model-only change; no deploy required'"
  - id: "dod-occ-pr"
    description: "OCC PR carrying this contract and binding receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr list --repo OmniNode-ai/onex_change_control --head jonah/omn-11225-occ-contract
          --json number,state"
