---
schema_version: "1.0.0"
ticket_id: OMN-12982
title: "Batch 1 — fix ~15 node contracts that declare nonexistent runtime profiles"
summary: >
  OMN-12982 Batch 1: correct 14 omnimarket node contracts that declared unregistered runtime_profiles
  values (compute, intelligence, memory, local_macos_claude_hooks, full_runtime, standalone, manual_pr_review,
  omninode_runtime, local_cli, contributor_workstation, ci_action). Each corrected to a registered lane
  (main, effects) based on node archetype and subscription topology. Removes the corresponding 15 allowlist
  entries (including volume_config_drift_sweep which was previously fixed but not de-listed). Updates
  2 tests that were asserting the old unregistered values.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #1235 (jonah/omn-12982-b1-nonexistent-profiles) passes all CI checks"
    command: "gh pr checks 1235 --repo OmniNode-ai/omnimarket"
  - kind: "ci"
    description: "contract-validation and Runtime Sweep pass on omnimarket PR 1235"
    command: "gh pr checks 1235 --repo OmniNode-ai/omnimarket"
  - kind: "tests"
    description: "Unit and validation tests pass (omnimarket PR 1235 all CI green)"
    command: "gh pr checks 1235 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "14 node contracts corrected: unregistered profiles replaced with valid lanes (main/effects)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-12982/dod-001/command.yaml"
  - id: "dod-002"
    description: "15 allowlist entries removed from validation/runtime_profiles_allowlist.yaml (omnimarket
      PR 1235)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-12982/dod-002/command.yaml"
  - id: "dod-003"
    description: "contract-validation CI gate passes on omnimarket PR 1235 (runtime_profile_unregistered=0)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-12982/dod-003/command.yaml"
  - id: "dod-occ-validation"
    description: "OCC receipt PR passes CI checks in onex_change_control"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
