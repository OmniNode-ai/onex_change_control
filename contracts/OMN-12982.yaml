---
schema_version: "1.0.0"
ticket_id: OMN-12982
title: "Batch 1 — fix ~15 node contracts that declare nonexistent runtime profiles"
summary: >
  OMN-12982 Batch 1: correct 14 omnimarket node contracts that declared unregistered runtime_profiles
  values (compute, intelligence, memory, local_macos_claude_hooks, full_runtime, standalone, manual_pr_review,
  omninode_runtime, local_cli, contributor_workstation, ci_action). Each corrected to a registered lane
  (main, effects) based on node archetype and subscription topology. Removes the corresponding 15 allowlist
  entries (including volume_config_drift_sweep which was previously fixed but not de-listed). Updates
  2 tests that were asserting the old unregistered values.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR jonah/omn-12982-b1-nonexistent-profiles passes all CI checks on omnimarket"
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket"
  - kind: "ci"
    description: "ValidatorRuntimeProfiles reports zero runtime_profile_unregistered issues"
    command: "uv run python scripts/ci/run_runtime_profiles_validator.py"
  - kind: "tests"
    description: "Unit and validation tests pass (4635 unit + 391 validation)"
    command: "uv run pytest tests/ -m unit -q && uv run pytest tests/validation/ -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "14 node contracts corrected: unregistered profiles replaced with valid lanes (main/effects)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/ci/run_runtime_profiles_validator.py"
  - id: "dod-002"
    description: "15 allowlist entries removed from validation/runtime_profiles_allowlist.yaml"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/ci/run_runtime_profiles_validator.py"
  - id: "dod-003"
    description: "All pre-commit hooks pass including Validate runtime_profiles on node contracts (OMN-12957)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-occ-validation"
    description: "OCC receipt PR bound to this evidence"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/onex_change_control"
