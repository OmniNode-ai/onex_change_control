---
schema_version: "1.0.0"
ticket_id: "OMN-12911"
title: "Gateway P0B per-tenant credentials: Keycloak client + broker quota + explicit topics"
summary: >-
  Finishes the P0B per-tenant credentials lane in omninode_infra: adds the missing forward/rollback DB
  migration for tenants.principal_id + tenant_credentials (referenced by docker/onex-api/main.py's provisioning/rotation
  code paths but never created by the prior automated lane before it died mid-edit), and adds full unit
  test coverage for tenant_identity, the principal_id ACL binding path, the per-tenant Keycloak client
  manager, the broker quota manager, and the explicit topic provisioner. GATEWAY_P0B_PER_TENANT_CREDENTIALS
  remains default "disabled" -- zero behavior change until an operator opts in. See omninode_infra PR
  #562.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode_infra-pr-562"
    description: >-
      Consuming-PR binding: omninode_infra PR #562 adds the tenant_credentials migration pair + full unit
      test coverage for the Gateway P0B per-tenant credentials modules (tenant_identity, keycloak_client_manager,
      kafka_quota_manager, kafka_topic_provisioner, principal_id ACL binding). Local full-suite verification:
      `python -m pytest -q` from docker/onex-api -- 1247 passed, 60 skipped (Postgres-integration tests,
      no local PG), 0 failed. ruff format --check and ruff check clean; mypy . clean (31 source files,
      strict mode); pre-commit run --files <changed> all hooks passed.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12911/dod-omninode_infra-pr-562/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-12911 contract and receipt consumed
      by omninode_infra PR #562.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12911/dod-occ-self/command.yaml"
