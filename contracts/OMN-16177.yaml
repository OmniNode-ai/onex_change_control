---
schema_version: "1.0.0"
ticket_id: "OMN-16177"
title: "OCC evidence for OMN-16177 — work-event ledger schema foundation (C1)"
summary: >
  OCC companion for OmniNode-ai/omnibase_core PR #1563 — the shared schema foundation (ModelWorkEventBase,
  the five work-event payload models, the ModelActor discriminated union, and ModelQuantClaim) for the
  work-event ledger design (omni_home/docs/plans/2026-08-18-kafka-ledger-migration-design.md, parent OMN-16176).
  Schema-only: no emit path, no claims arbitration, no projection, no topic registration.
is_seam_ticket: true
interface_change: false
evidence_requirements:
  - kind: "ci"
    description: "PR #1563 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_core-pr-1563"
    description: >
      PR #1563 on OmniNode-ai/omnibase_core -- no autobind companion was published for this PR (occ-companion-effect
      job on the PR's head 5b8a4492 logged "OCC companion publication is not applicable; required context
      materialized as a no-op"), so this is a hand-authored companion. Verifies the ModelWorkEventBase.emitted_at
      field's naive-datetime rejection validator, described in the PR as making `emitted_at` emitter-assigned
      with no `datetime.now()` default and timezone-aware-only, exists at the PR's head via the GitHub
      contents API.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/models/events/work/model_work_event_base.py?ref=5b8a4492d0530c53fe2e6fe86af0c88e0931bfc2
          --jq .content | base64 -d | grep -c 'def _reject_naive_emitted_at'"
  - id: "dod-OmniNode-ai-omnibase_core-pr-1563-quant-claim"
    description: >
      Verifies ModelQuantClaim carries a required `probe_command: str` field alongside its numeric value
      -- the PR's described change turning OMN-15897's write-time regex lint into a typed field so an
      unverified number cannot be constructed -- via the GitHub contents API at the PR's head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_core/contents/src/omnibase_core/models/events/work/model_quant_claim.py?ref=5b8a4492d0530c53fe2e6fe86af0c88e0931bfc2
          --jq .content | base64 -d | grep -c 'probe_command: str = Field'"
