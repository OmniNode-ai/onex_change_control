---
schema_version: "1.0.0"
ticket_id: "OMN-14839"
title: "Autobind OCC evidence for OMN-14839"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1837."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1837 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1837"
    description: "PR #1837 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1837-ci"
    description: "PR #1837 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-omnimarket-pr-1837-deploy-scope"
    description: "PR #1837 on OmniNode-ai/omnimarket touches deploy-scoped runtime handlers for node_rsd_fill_compute
      and related live-build orchestration paths; deploy gate evidence records the runtime paths and requires
      post-merge deploy fitness validation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy-scope assertion: runtime paths src/omnimarket/nodes/node_build_loop_orchestrator/assemble_live.py,
          src/omnimarket/nodes/node_build_loop_orchestrator/handlers/adapter_linear_fill.py, src/omnimarket/nodes/node_build_loop_orchestrator/handlers/handler_build_loop_orchestrator.py,
          src/omnimarket/nodes/node_build_loop_orchestrator/protocols/protocol_sub_handlers.py, src/omnimarket/nodes/node_pipeline_fill/handlers/handler_pipeline_fill.py,
          src/omnimarket/nodes/node_rsd_fill_compute/handlers/handler_rsd_fill.py, and src/omnimarket/nodes/node_skill_dispatch_engine_orchestrator/handlers/handler_dispatch_router.py
          changed in PR #1837; post-merge deploy validation must include omnimarket runtime profile deploy
          fitness before promotion.'"
  - id: "occ-self-bind-pr-4471"
    description: "OCC companion PR #4471 — self-bind for OMN-14839 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
