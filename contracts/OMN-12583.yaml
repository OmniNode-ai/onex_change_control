---
schema_version: "1.0.0"
ticket_id: "OMN-12583"
title: "Bind June 2 dependency and runner PR batch to OCC evidence"
summary: >
  Bind the open June 2 omninode_infra dependency maintenance PR batch and the omnibase_infra runner-image
  remediation PR to central OCC evidence so Receipt Gate can evaluate each PR from a pinned Evidence-Source
  without bypass tokens.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omninode_infra dependency PR heads and current CI classifications are recorded."
    command: "find drift/dod_receipts/OMN-12583 -name command.yaml -print"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-488-head"
    description: "omninode_infra PR #488 carries the python-production dependency update plus the Redis
      typing fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-omninode-infra-488-head/command.yaml\""
  - id: "dod-omninode-infra-488-local-validation"
    description: "omninode_infra PR #488 local onex-api quality checks pass after the Redis typing fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-omninode-infra-488-local-validation/command.yaml\""
  - id: "dod-omninode-infra-482-blocker"
    description: "omninode_infra PR #482 is classified as blocked by pytest/pytest-asyncio dependency
      incompatibility."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-omninode-infra-482-blocker/command.yaml\""
  - id: "dod-omninode-infra-483-blocker"
    description: "omninode_infra PR #483 is classified as blocked by the same Redis untyped factory mypy
      failure."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-omninode-infra-483-blocker/command.yaml\""
  - id: "dod-omninode-infra-484-head"
    description: "omninode_infra PR #484 carries the redis dependency update and is currently blocked
      only by Receipt Gate metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-omninode-infra-484-head/command.yaml\""
  - id: "dod-omninode-infra-485-head"
    description: "omninode_infra PR #485 carries the aws/cluster Terraform provider lock update and is
      currently blocked only by Receipt Gate metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-omninode-infra-485-head/command.yaml\""
  - id: "dod-omninode-infra-486-head"
    description: "omninode_infra PR #486 carries the aws/network Terraform provider lock update and is
      currently blocked only by Receipt Gate metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-omninode-infra-486-head/command.yaml\""
  - id: "dod-omninode-infra-487-head"
    description: "omninode_infra PR #487 carries the GitHub Actions dependency group update and is currently
      blocked only by Receipt Gate metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-omninode-infra-487-head/command.yaml\""
  - id: "dod-omninode-infra-489-head"
    description: "omninode_infra PR #489 batches the outstanding #482-#487 dependency changes onto an
      OMN-12583-named branch for Receipt Gate identity binding."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-omninode-infra-489-head/command.yaml\""
  - id: "dod-omnibase-infra-1835-runner-node24"
    description: "omnibase_infra PR #1835 bumps the self-hosted runner image to Actions runner 2.334.0
      and runner image v5, skipping setup-python/setup-uv for baked shared-env jobs so Node 24 actions
      and required lightweight Python setup jobs can run on the CI fleet without PyPI bootstrap downloads."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-omnibase-infra-1835-runner-node24/command.yaml\""
  - id: "dod-occ-pr-2052-eligibility"
    description: "onex_change_control PR #2052 is bound to this OCC evidence package for merge eligibility."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-occ-pr-2052-eligibility/command.yaml\""
  - id: "dod-occ-pr-2063-eligibility"
    description: "onex_change_control PR #2063 binds the replacement omninode_infra PR #489 receipt and
      central-contract path fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-occ-pr-2063-eligibility/command.yaml\""
  - id: "dod-occ-pr-2065-eligibility"
    description: "onex_change_control PR #2065 binds the omnibase_infra runner-image PR #1835 receipt."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-occ-pr-2065-eligibility/command.yaml\""
  - id: "dod-occ-pr-2066-eligibility"
    description: "onex_change_control PR #2066 refreshes the omnibase_infra runner-image PR #1835 receipt
      to the v4 runner image."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-occ-pr-2066-eligibility/command.yaml\""
  - id: "dod-occ-pr-2067-eligibility"
    description: "onex_change_control PR #2067 refreshes the omnibase_infra runner-image PR #1835 receipt
      after the v5 head-refresh commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-occ-pr-2067-eligibility/command.yaml\""
  - id: "dod-occ-pr-2069-eligibility"
    description: "onex_change_control PR #2069 refreshes the omnibase_infra runner-image PR #1835 receipt
      after the v5 shared-env setup fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-occ-pr-2069-eligibility/command.yaml\""
  - id: "dod-occ-pr-2070-eligibility"
    description: "onex_change_control PR #2070 refreshes the omnibase_infra runner-image PR #1835 receipt
      after routing required lightweight Python setup call sites through the shared setup action."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-occ-pr-2070-eligibility/command.yaml\""
  - id: "dod-occ-pr-2071-eligibility"
    description: "onex_change_control PR #2071 refreshes the omnibase_infra runner-image PR #1835 receipt
      after forcing the shared setup action for lightweight Python setup jobs."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-occ-pr-2071-eligibility/command.yaml\""
  - id: "dod-occ-pr-2072-eligibility"
    description: "onex_change_control PR #2072 refreshes the omnibase_infra runner-image PR #1835 receipt
      after adding the node24-capable runner floor gate."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-occ-pr-2072-eligibility/command.yaml\""
  - id: "dod-occ-pr-2073-eligibility"
    description: "onex_change_control PR #2073 refreshes the omnibase_infra runner-image PR #1835 receipt
      after forcing Docker integration tests onto the shared runner env."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-occ-pr-2073-eligibility/command.yaml\""
  - id: "dod-occ-pr-2074-eligibility"
    description: "onex_change_control PR #2074 refreshes the omnibase_infra runner-image PR #1835 receipt
      after preserving uv run option-bearing invocations in the shared env wrapper and rebinding the runner
      image identity."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-occ-pr-2074-eligibility/command.yaml\""
  - id: "dod-occ-pr-2075-eligibility"
    description: "onex_change_control PR #2075 formats the OMN-12583 receipts from PR #2074 and refreshes
      the omnibase_infra runner-image PR #1835 receipt after wiring deploy-runners through the versioned
      runner image build path."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-12583/dod-occ-pr-2075-eligibility/command.yaml\""
