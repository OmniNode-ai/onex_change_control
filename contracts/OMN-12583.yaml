---
schema_version: "1.0.0"
ticket_id: "OMN-12583"
title: "Bind omninode_infra June 2 dependency PR batch to OCC evidence"
summary: >
  Bind the open June 2 omninode_infra dependency maintenance PR batch to central OCC evidence so
  Receipt Gate can evaluate each PR from a pinned Evidence-Source without bypass tokens.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omninode_infra dependency PR heads and current CI classifications are recorded."
    command: "find drift/dod_receipts/OMN-12583 -name command.yaml -print"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-488-head"
    description: "omninode_infra PR #488 carries the python-production dependency update plus the Redis typing fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12583/dod-omninode-infra-488-head/command.yaml\""
  - id: "dod-omninode-infra-488-local-validation"
    description: "omninode_infra PR #488 local onex-api quality checks pass after the Redis typing fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12583/dod-omninode-infra-488-local-validation/command.yaml\""
  - id: "dod-omninode-infra-482-blocker"
    description: "omninode_infra PR #482 is classified as blocked by pytest/pytest-asyncio dependency incompatibility."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12583/dod-omninode-infra-482-blocker/command.yaml\""
  - id: "dod-omninode-infra-483-blocker"
    description: "omninode_infra PR #483 is classified as blocked by the same Redis untyped factory mypy failure."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12583/dod-omninode-infra-483-blocker/command.yaml\""
  - id: "dod-omninode-infra-484-head"
    description: "omninode_infra PR #484 carries the redis dependency update and is currently blocked only by Receipt Gate metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12583/dod-omninode-infra-484-head/command.yaml\""
  - id: "dod-omninode-infra-485-head"
    description: "omninode_infra PR #485 carries the aws/cluster Terraform provider lock update and is currently blocked only by Receipt Gate metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12583/dod-omninode-infra-485-head/command.yaml\""
  - id: "dod-omninode-infra-486-head"
    description: "omninode_infra PR #486 carries the aws/network Terraform provider lock update and is currently blocked only by Receipt Gate metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12583/dod-omninode-infra-486-head/command.yaml\""
  - id: "dod-omninode-infra-487-head"
    description: "omninode_infra PR #487 carries the GitHub Actions dependency group update and is currently blocked only by Receipt Gate metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12583/dod-omninode-infra-487-head/command.yaml\""
