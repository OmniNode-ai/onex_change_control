---
schema_version: "1.0.0"
ticket_id: "OMN-10085"
summary: "Prove market skill round-trips through local runtime ingress and retire direct CLI fallback"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "envelopes"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused runtime-ingress proof and market-skill verification passes locally or against
      the host runtime as appropriate"
    command: "See canonical receipts under drift/dod_receipts/OMN-10085/"
  - kind: "manual"
    description: "Current market skills are proven through the host runtime without direct Kafka publish
      access from the client"
    command: "See canonical receipts under drift/dod_receipts/OMN-10085/"
  - kind: "ci"
    description: "Downstream PR checks are green for the runtime-ingress proof gate"
    command: "gh pr checks --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "session-bootstrap round-trips through the local runtime ingress"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10085/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "session-orchestrator round-trips through the local runtime ingress"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10085/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "merge-sweep round-trips through the local runtime ingress"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10085/dod-003 -type f -name '*.yaml' | grep -q ."
  - id: "dod-004"
    description: "aislop-sweep round-trips through the local runtime ingress"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10085/dod-004 -type f -name '*.yaml' | grep -q ."
  - id: "dod-005"
    description: "Direct CLI fallback is retired or explicitly downgraded to debug-only use after proof
      passes"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10085/dod-005 -type f -name '*.yaml' | grep -q ."
