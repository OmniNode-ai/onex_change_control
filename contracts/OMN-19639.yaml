---
schema_version: "1.0.0"
ticket_id: "OMN-19639"
title: "Autobind OCC evidence for OMN-19639"
summary: |-
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_core PR #1780.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
requirements:
  - id: "req-transcribed-acceptance-criteria"
    statement: |-
      The acceptance criteria this ticket's author declared, transcribed from the ticket body at autobind time by occ-autobind. The ticket body is the source and this block is the copy; each criterion's id is the label the sibling binds_ac entry claims.
    acceptance:
      - id: "AC1"
        statement: |-
          AC1: the aggregating job's cancelled-supersession grace is confirmed (or extended) so a cancelled leaf job never turns it red on its own -- falsifier: read the job's own pass/fail logic against a synthetic cancelled leaf-job conclusion and confirm the aggregator does not fail on it alone
      - id: "AC2"
        statement: |-
          AC2: every pull_request-triggered workflow in omnibase_core has a top-level concurrency: block with a per-PR group and cancel-in-progress: true, and push/merge_group/schedule/dispatch runs are unaffected -- falsifier: python3 cancel_sweep.py (or an equivalent parse) reports 0 remaining PR-triggered workflows without a per-PR cancel-in-progress group in omnibase_core, and a PR with two pushes to the same branch shows the first run's jobs cancelled on GitHub's own run list
evidence_requirements:
  - kind: "ci"
    description: "PR #1780 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #1780 on OmniNode-ai/omnibase_core changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (.github/workflows/auto-tag-on-merge.yml, .github/workflows/call-occ-preflight.yml, .github/workflows/call-receipt-gate.yml, .github/workflows/call-reject-skip.yml) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_core-pr-1780"
    description: "PR #1780 on OmniNode-ai/omnibase_core — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1780 --repo OmniNode-ai/omnibase_core --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_core/pulls/1780/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/auto-tag-on-merge.yml'
    binds_ac:
      - "AC1"
      - "AC2"
    ac_bindings:
      - label: "AC1"
        criterion_hash: "3dc636d0baa22e4f90c3d18b7c7832c7956d8e6bdecee9ae4fc3d7b4f6dc0186"
        proposed_by: "occ-autobind"
        accepted_by: "7a850ce1-f95e-431f-b4e3-62f7449f04c0"
        accepted_at: "2026-09-25T17:38:14Z"
      - label: "AC2"
        criterion_hash: "02a3d496007135e3a9fd2a59593672db193e68bb3f53edc3f859ff5dd6ff5d38"
        proposed_by: "occ-autobind"
        accepted_by: "7a850ce1-f95e-431f-b4e3-62f7449f04c0"
        accepted_at: "2026-09-25T17:38:14Z"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_core-pr-1780"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-11372"
    description: "Binds OMN-19639 to OCC companion PR #11372 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 11372 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-admissibility-omn19639-rb-skip"
    description: >-
      Disclosed-skip supersession of dod-occ-evidence-admissibility-validator, appended by hand. That
      item's only check is 'uv run pytest tests/test_evidence_admissibility.py -q' with no declared cwd.
      The test file exists only in onex_change_control, so the check exits 5 (no tests ran) in every product
      workspace, which is what omnibase_core#1780's Contract Compliance Check read (run 36193303370, job
      108265042505, head ec819ccb875014987e308c32bde146d4b136d99f, evidence tree 70c943c2cd). The pinned
      runner (OCC 91f5b691) has no cwd or execution_scope field, so no declaration can route that check
      to an OCC checkout. Same shape as occ-admissibility-omn19512-rb-skip in contracts/OMN-19512.yaml
      and the OMN-19384 supersessions in contracts/OMN-18595.yaml. The admissibility suite itself stays
      covered by OCC's own required CI on every OCC PR. Nothing here produces a PASS.
    source: "manual"
    status: "skipped"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-evidence-admissibility-validator"
  - id: "dod-OmniNode-ai-omnibase_core-pr-1780-restored"
    description: >-
      Restores the product fact for omnibase_core#1780. The producer minted dod-occ-evidence-admissibility-validator
      with evidence_artifact supersedes_dod_evidence:dod-OmniNode-ai-omnibase_core-pr-1780, so the pinned
      runner stopped executing the product item. An append-only contract cannot remove that marker, and
      the item above only retires the admissibility check, so this item re-states the product item's two
      checks verbatim under a new id. The AC bindings stay on the original item, which the binding gate
      still reads.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1780 --repo OmniNode-ai/omnibase_core --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_core/pulls/1780/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/auto-tag-on-merge.yml'
