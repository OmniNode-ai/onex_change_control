---
schema_version: "1.0.0"
ticket_id: "OMN-8757"
title: "Thin dashboard_sweep shim with HTTP recon"
summary: "Replace dashboard_sweep Playwright MCP orchestration with HTTP metadata recon while keeping
  classification and triage in node_dashboard_sweep."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "dashboard_sweep skill no longer references Playwright"
    command: "cd omni_worktrees/OMN-8757/omniclaude && if rg -qi 'playwright' plugins/onex/skills/dashboard_sweep/;
      then exit 1; else test \"$?\" -eq 1; fi"
  - kind: "tests"
    description: "dashboard_sweep skill no longer references Playwright MCP tools"
    command: "cd omni_worktrees/OMN-8757/omniclaude && if rg -q 'mcp__playwright' plugins/onex/skills/dashboard_sweep/;
      then exit 1; else test \"$?\" -eq 1; fi"
  - kind: "tests"
    description: "dashboard_sweep skill files pass pre-commit skill and markdown gates"
    command: "cd omni_worktrees/OMN-8757/omniclaude && uv run pre-commit run --files plugins/onex/skills/dashboard_sweep/SKILL.md
      plugins/onex/skills/dashboard_sweep/prompt.md"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "dashboard_sweep skill has zero case-insensitive Playwright references"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8757/dod-001/command.yaml && grep
          -q 'pr_number: 1503' drift/dod_receipts/OMN-8757/dod-001/command.yaml"
  - id: "dod-002"
    description: "dashboard_sweep skill has zero mcp__playwright tool references"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8757/dod-002/command.yaml && grep
          -q 'pr_number: 1503' drift/dod_receipts/OMN-8757/dod-002/command.yaml"
  - id: "dod-003"
    description: "dashboard_sweep skill files pass pre-commit gates"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8757/dod-003/command.yaml && grep
          -q 'Skill Hygiene.*Passed' drift/dod_receipts/OMN-8757/dod-003/command.yaml"
  - id: "dod-004"
    description: "OCC PR #666 receipt evidence is locally eligible for downstream omniclaude PR #1503"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-8757/dod-004/command.yaml && grep
          -q 'pr_number: 666' drift/dod_receipts/OMN-8757/dod-004/command.yaml && grep -q 'eligible=true'
          drift/dod_receipts/OMN-8757/dod-004/command.yaml"
