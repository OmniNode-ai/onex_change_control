---
schema_version: "1.0.0"
ticket_id: "OMN-10354"
title: "Add ModelZonePolicy for per-zone QA gate configuration"
summary: "Frozen Pydantic model with zone:EnumFileZone, qa_depth:Literal, and four bool gate fields (requires_lint,
  requires_test, requires_review, requires_security_scan). Stacked on OMN-10351 (EnumFileZone). Part of
  epic OMN-10350 selective swarm patterns."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #995 on omnibase_core passes CI"
    command: "gh pr checks 995 --repo OmniNode-ai/omnibase_core"
  - kind: "manual"
    description: "6 unit tests for ModelZonePolicy pass"
    command: "cd omnibase_core && env -u PYTHONPATH uv run pytest tests/unit/models/validation/test_model_zone_policy.py
      -v"
  - kind: "manual"
    description: "mypy --strict passes on model_zone_policy.py"
    command: "cd omnibase_core && env -u PYTHONPATH uv run mypy src/omnibase_core/models/validation/model_zone_policy.py
      --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "ModelZonePolicy frozen Pydantic model created with correct 6 fields"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 995 --repo OmniNode-ai/omnibase_core | grep -q 'ModelZonePolicy'"
  - id: "dod-002"
    description: "6 unit tests pass: frozen immutability, qa_depth validation, all zones accepted, field
      access, zone type check"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr checks 995 --repo OmniNode-ai/omnibase_core"
  - id: "dod-003"
    description: "deploy-gate: model-only addition in omnibase_core, no Docker image, runtime process,
      broker topic, or service deploy required"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'deploy-gate: OMN-10354 adds a pure Pydantic model with no runtime services,
          Kafka topics, DB migrations, or HTTP endpoints'"
