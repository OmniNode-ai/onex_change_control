---
schema_version: "1.0.0"
ticket_id: "OMN-18329"
title: "Autobind OCC evidence for OMN-18329"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3499.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3499 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3499 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: "uv run pytest tests/unit/diagnostics/test_closer_receipt_residual.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3499"
    description: "PR #3499 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/diagnostics/closer_receipt_residual.py?ref=7ee98f044df40d18bda7e4d30accb5292d2db0c1 --jq '.content' | base64 -d | grep -c 'class TicketDecisionEvent'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3499/files --paginate --jq '.[].filename' | grep -cFx 'pyproject.toml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3499 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/diagnostics/test_closer_receipt_residual.py -q"
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-9403"
    description: "Binds OMN-18329 to OCC companion PR #9403 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9403 --repo OmniNode-ai/onex_change_control --json number,state"
