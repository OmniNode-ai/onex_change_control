---
schema_version: '1.0.0'
ticket_id: OMN-13855
title: 'fix(OMN): stability redpanda at partition-allocation cap (1514 topics) — blocks new topic creation'
summary: 'Live-probe closing evidence — .201 stability redpanda partition-allocation cap is clear (49/7000
  headroom), topic create succeeds live'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: docs
    description: Overnight O5a board-hygiene evidence doc captures the fresh live re-probe
    command: "grep -F 'baseline (post-cleanup) = 1474 topics, 6951/7000 partitions -> 49 headroom' ${OMNI_HOME}/docs/evidence/M5-capstone-dogfood/2026-07-03-overnight-o5-board-hygiene.md"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Overnight handoff records OMN-13855 as resolved (topic prune freed partition headroom)
    source: generated
    checks:
      - check_type: command
        cwd: "${OMNI_HOME}"
        check_value: "grep -F '**OMN-13855** RESOLVED' docs/handoff/2026-07-03-overnight-execution-handoff.md"
  - id: dod-002
    description: Fresh live re-probe (this session) confirms partition headroom and a successful topic
      create, captured in the O5a evidence doc
    source: generated
    checks:
      - check_type: command
        cwd: "${OMNI_HOME}"
        check_value: "grep -F 'baseline (post-cleanup) = 1474 topics, 6951/7000 partitions -> 49 headroom'
          docs/evidence/M5-capstone-dogfood/2026-07-03-overnight-o5-board-hygiene.md"
  - id: dod-003
    description: O5a evidence doc records the live topic-create success proving the cap is clear
    source: generated
    checks:
      - check_type: command
        cwd: "${OMNI_HOME}"
        check_value: "grep -F 'omn13855.closeout.probe.1783056959  OK' docs/evidence/M5-capstone-dogfood/2026-07-03-overnight-o5-board-hygiene.md"
