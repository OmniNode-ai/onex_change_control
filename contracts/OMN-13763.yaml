---
schema_version: "1.0.0"
ticket_id: "OMN-13763"
title: "fix(OMN-13763): evacuate run_coro_sync compat->core, drop compat from spine"
summary: >-
  Evacuates run_coro_sync from omnibase_compat into omnibase_core, closing the only live compat runtime
  import in the core->spi->infra spine. Adds src/omnibase_core/utils/util_run_coro_sync.py (stdlib-only,
  graduated verbatim from omnibase_compat.concurrency.util_run_coro_sync), updates model_onex_container.py
  to use the direct core import, removes omnibase-compat from pyproject.toml optional extras and uv.sources,
  and refreshes uv.lock confirming removal of omnibase-compat v0.3.1. Four unit tests added covering both
  execution paths and exception propagation. Epic OMN-13762.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "4 unit tests in tests/unit/utils/test_run_coro_sync.py pass, covering both execution
      paths (no-loop via asyncio.run, running-loop via ThreadPoolExecutor) and exception propagation on
      each path."
    command: >-
      grep -q '^status: PASS$' drift/dod_receipts/OMN-13763/dod-unit-tests/command.yaml
  - kind: "ci"
    description: "grep -rnE zero matches for omnibase_compat imports in src/; uv.lock confirms removal
      of omnibase-compat v0.3.1."
    command: >-
      grep -q '^status: PASS$' drift/dod_receipts/OMN-13763/dod-implementation/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: >-
      4 new tests in tests/unit/utils/test_run_coro_sync.py cover the two execution paths (no-loop via
      asyncio.run, running-loop via ThreadPoolExecutor) and exception propagation on each. All 4 pass.
      Probe: cd omni_worktrees/OMN-13763/omnibase_core && uv run pytest tests/unit/utils/test_run_coro_sync.py
      -q. Result: 4 passed in 0.38s. Implementing PR: OmniNode-ai/omnibase_core#1364 commit 3aac57a54a019a55c70f78cc092532807a117ad1.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13763/dod-unit-tests/command.yaml"
  - id: "dod-implementation"
    description: >-
      grep -rnE zero omnibase_compat imports in src/ of omnibase_core branch jonahgabriel/omn-13763-evacuate-run-coro-sync
      commit 3aac57a54a019a55c70f78cc092532807a117ad1. uv.lock confirms Removed omnibase-compat v0.3.1
      (d86ee00a). The compat optional extra and uv.sources stanza are removed from pyproject.toml.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13763/dod-implementation/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-readiness assessment. OMN-13763 is a code-only change to omnibase_core: adds a new stdlib-only
      utility module (util_run_coro_sync.py), updates an import in model_onex_container.py, and removes
      the omnibase-compat optional dependency. No Dockerfile, docker-compose, topic contract, published_events,
      or infra/runtime-kernel change; no prod/stability mutation. Package version bump follows the standard
      release pipeline. No repository-tracked deploy artifact is required.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13763/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13763 contract and DoD receipts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "printf 'PASS\n'"
