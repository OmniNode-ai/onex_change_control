---
schema_version: 1.0.0
ticket_id: OMN-16039
title: OCC companion for the interactive onboarding wiring fix
summary: "Evidence companion for OmniNode-ai/omnimarket#2072 — the interactive onboarding path was unreachable
  because the node_onboarding handler dropped policy_name and injected no input_adapter."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #2072 product diff scope present'
    command: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-OmniNode-ai-omnimarket-pr-2072
    description: "PR #2072 on OmniNode-ai/omnimarket — content-bound probe: the regression test that pins
      this fix is present at the PR head and absent at the merge base."
    source: generated
    checks:
      - check_type: command
        check_value: gh api repos/OmniNode-ai/omnimarket/contents/tests/unit/test_handler_onboarding_interactive_wiring.py?ref=90af8e9ae6e8fdb05f1e0386d3a78d5d64a8a086
          --jq '.content' | base64 -d | grep -c 'def test_interactive_policy_runs_interactive_path'
  - id: dod-OmniNode-ai-omnimarket-pr-2072-ci
    description: "PR #2072 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view 2072 --repo OmniNode-ai/omnimarket --json files
  - id: occ-self-bind-pr-6472
    description: "OCC companion PR #6472 — self-bind for OMN-16039 (OMN-14650)."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view 6472 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
