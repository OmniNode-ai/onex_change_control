---
schema_version: "1.0.0"
ticket_id: "OMN-11767"
title: "Golden chain test: pattern_learning (pattern-stored.v1 → pattern_learning_artifacts)"
summary: >
  Adds golden chain e2e test covering the pattern_learning chain: onex.evt.omniintelligence.pattern-stored.v1
  → pattern_learning_artifacts. Tests registry wiring (head topic, tail table, expected fields read from
  golden_chains.yaml), PASS/FAIL/TIMEOUT/GATED sweep outcomes, and registry-driven full-suite validation.
  Ships 10 passing unit tests via NodeGoldenChainSweep. No hardcoded topic strings.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr"
    description: "omnimarket PR #766 ships pattern_learning golden chain e2e tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11767/dod-omnimarket-pr/command.yaml
          && grep -q '^pr_number: 766$' drift/dod_receipts/OMN-11767/dod-omnimarket-pr/command.yaml"
  - id: "dod-tests-pass"
    description: "10/10 pattern_learning golden chain tests pass locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11767/dod-tests-pass/command.yaml
          && grep -q '10 passed' drift/dod_receipts/OMN-11767/dod-tests-pass/command.yaml"
