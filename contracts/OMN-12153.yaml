---
schema_version: "1.0.0"
ticket_id: OMN-12153
title: "Remove --unsafe-bypass-fsync from prod Redpanda overlay"
summary: "Remove the dev-only --unsafe-bypass-fsync flag from the shared data-plane Redpanda StatefulSet
  overlay. The flag was inadvertently present in the production-shared overlay, creating a data loss risk
  under node failure. The flag remains correctly scoped in the dev-only overlay."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omninode_infra PR removing --unsafe-bypass-fsync from data-plane overlay is open"
    command: "gh pr view 469 --repo OmniNode-ai/omninode_infra"
  - kind: "manual"
    description: "Flag is absent from k8s/data-plane/redpanda/statefulset.yaml and present in k8s/dev/redpanda/statefulset.yaml"
    command: "gh pr view 469 --repo OmniNode-ai/omninode_infra --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR #469 in omninode_infra removes --unsafe-bypass-fsync from the shared data-plane overlay"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12153/dod-001/command.yaml && grep
          -q '^pr_number: 469$' drift/dod_receipts/OMN-12153/dod-001/command.yaml"
  - id: "dod-occ-validation"
    description: "OCC evidence contract validates and binds this evidence PR"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12153/dod-occ-validation/command.yaml"
