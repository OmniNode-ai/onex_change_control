---
schema_version: "1.0.0"
ticket_id: "OMN-13413"
title: "Runtime closeout orchestrator and /onex:runtime_closeout"
summary: >-
  Bind omnimarket PR #1408, which adds the bus-native node_runtime_closeout_orchestrator and shared runtime
  closeout domain types. The PR is code and contract only; live deploy/proof execution remains owned by
  the redeploy runtime workstream.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1408"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1408 (jonah/omn-13413-node-runtime-closeout-orchestrator,
      base=dev). The PR adds the closeout orchestrator node, typed closeout phase models, contract registration,
      and tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13413/dod-omnimarket-pr-1408/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for PR #1408. The touched runtime paths add a bus-native orchestrator and type
      surfaces only. No live lane deploy, runtime restart, Kafka topic mutation, docker exec, or database
      migration is performed by this PR; live proof execution is explicitly deferred to the redeploy workstream.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13413/dod-deploy-assessment/command.yaml # deploy:
          static runtime-node code only; no live deploy/docker exec/rpk topic produce before merge
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the evidence PR carrying the OMN-13413 contract and receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13413/dod-occ-pr-self/command.yaml
