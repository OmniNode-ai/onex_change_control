---
schema_version: "1.0.0"
ticket_id: "OMN-15142"
title: "deploy-runners.sh SYNC_PATHS missing omni-curl artifacts + undocumented required compose vars"
summary: "Fixes deploy-runners.sh's SYNC_PATHS/rsync_artifacts() gap (omni-curl artifacts never synced,
  causing a fresh-deployment-dir build to fail at the Dockerfile COPY layer) and consolidates the three
  DEPLOY_RUNNER_* env vars required by docker-compose.runners.yml's omninode-deploy-runner service into
  one documented location. Supersedes the vacuous auto-generated OCC#4896 self-bind contract for this
  ticket with a real, falsifiable deploy probe -- the autobind entries are retained (append-only)."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2453 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "ci"
    description: "omnibase_infra PR #2453 head is bound to the reviewed commit."
    command: "gh pr view 2453 --repo OmniNode-ai/omnibase_infra --json headRefOid --jq '.headRefOid ==
      \"81407bcb0ae4f1c1d4215637d01ab9000edc3238\"'"
  - kind: "ci"
    description: "SYNC_PATHS in deploy-runners.sh at the reviewed commit includes the omni-curl artifact."
    command: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/deploy-runners.sh?ref=81407bcb0ae4f1c1d4215637d01ab9000edc3238'
      --jq .content | base64 -d | grep -q '\"docker/runners/omni-curl\"'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2453"
    description: "PR #2453 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2453-ci"
    description: "PR #2453 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4896"
    description: "OCC companion PR #4896 — self-bind for OMN-15142 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-infra2453-product-head"
    description: "omnibase_infra PR #2453 remains bound to the reviewed commit."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2453 --repo OmniNode-ai/omnibase_infra --json headRefOid --jq '.headRefOid
          == \"81407bcb0ae4f1c1d4215637d01ab9000edc3238\"'"
  - id: "dod-infra2453-sync-paths-omni-curl"
    description: "deploy-runners.sh's SYNC_PATHS array includes docker/runners/omni-curl at the reviewed
      commit -- the real, falsifiable fix for the fresh-deployment-dir build failure this ticket diagnosed
      (a build against an un-synced deployment dir fails at the Dockerfile COPY layer)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/deploy-runners.sh?ref=81407bcb0ae4f1c1d4215637d01ab9000edc3238'
          --jq .content | base64 -d | grep -q '\"docker/runners/omni-curl\"'"
  - id: "dod-infra2453-rsync-artifacts-omni-curl"
    description: "rsync_artifacts() at the reviewed commit actually passes docker/runners/omni-curl.sh
      to an rsync invocation -- proving the second, independently-maintained filename list (not just the
      SYNC_PATHS declaration) was also fixed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/deploy-runners.sh?ref=81407bcb0ae4f1c1d4215637d01ab9000edc3238'
          --jq .content | base64 -d | grep -q 'docker/runners/omni-curl.sh'"
  - id: "dod-infra2453-consolidated-env-var-docs"
    description: "deploy-runners.sh's usage docstring at the reviewed commit documents all three required
      DEPLOY_RUNNER_* vars together (previously only DEPLOY_RUNNER_OMNI_HOME had an in-file comment)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/deploy-runners.sh?ref=81407bcb0ae4f1c1d4215637d01ab9000edc3238'
          --jq .content | base64 -d | grep -q 'DEPLOY_RUNNER_OPERATOR_ENV_FILE  Host path of the operator
          env file'"
  - id: "occ-self-bind-pr-4897"
    description: "OCC deploy-gate contract PR #4897 — self-bind for OMN-15142."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4897 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid"
  - id: "dod-infra2453-product-head-v2"
    description: "Supersedes dod-infra2453-product-head (append-only; the original entry is immutable
      and stays PASS). CodeRabbit finding on PR #4897: `gh ... --jq '.a == \"x\"'` prints a boolean but
      `gh` always exits 0 on a successful API call, so the original check could PASS on a mismatched head.
      Hardened with a fail-closed shell string-equality test."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test \"$(gh pr view 2453 --repo OmniNode-ai/omnibase_infra --json headRefOid --jq
          .headRefOid)\" = \"81407bcb0ae4f1c1d4215637d01ab9000edc3238\""
  - id: "dod-infra2453-sync-paths-omni-curl-v2"
    description: "Supersedes dod-infra2453-sync-paths-omni-curl (append-only; the original entry is immutable
      and stays PASS). CodeRabbit finding on PR #4897: the grep matched the omni-curl literal anywhere
      in the file rather than specifically inside the SYNC_PATHS array. Hardened with an awk scope to
      the SYNC_PATHS=(...) array block."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/deploy-runners.sh?ref=81407bcb0ae4f1c1d4215637d01ab9000edc3238'
          --jq .content | base64 -d | awk '/^SYNC_PATHS=\\(/,/^\\)/' | grep -q '\"docker/runners/omni-curl\"'"
  - id: "dod-infra2453-rsync-artifacts-omni-curl-v2"
    description: "Supersedes dod-infra2453-rsync-artifacts-omni-curl (append-only; the original entry
      is immutable and stays PASS). CodeRabbit finding on PR #4897: the SYNC_PATHS array match alone could
      satisfy this check without rsync_artifacts() itself being correct. Hardened with an awk scope to
      the rsync_artifacts() function body."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/deploy-runners.sh?ref=81407bcb0ae4f1c1d4215637d01ab9000edc3238'
          --jq .content | base64 -d | awk '/^rsync_artifacts\\(\\) \\{/,/^\\}/' | grep -q 'docker/runners/omni-curl.sh'"
  - id: "dod-infra2453-consolidated-env-var-docs-v2"
    description: "Supersedes dod-infra2453-consolidated-env-var-docs (append-only; the original entry
      is immutable and stays PASS). CodeRabbit finding on PR #4897: only DEPLOY_RUNNER_OPERATOR_ENV_FILE
      was asserted. Hardened to assert all three documented vars: DEPLOY_RUNNER_OMNI_HOME, DEPLOY_RUNNER_TOKEN,
      DEPLOY_RUNNER_OPERATOR_ENV_FILE."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "(gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/deploy-runners.sh?ref=81407bcb0ae4f1c1d4215637d01ab9000edc3238'
          --jq .content | base64 -d | grep -q 'DEPLOY_RUNNER_OMNI_HOME') && (gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/deploy-runners.sh?ref=81407bcb0ae4f1c1d4215637d01ab9000edc3238'
          --jq .content | base64 -d | grep -q 'DEPLOY_RUNNER_TOKEN') && (gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/deploy-runners.sh?ref=81407bcb0ae4f1c1d4215637d01ab9000edc3238'
          --jq .content | base64 -d | grep -q 'DEPLOY_RUNNER_OPERATOR_ENV_FILE  Host path of the operator
          env file')"
  - id: "occ-self-bind-pr-4899"
    description: "OCC deploy-gate probe hardening PR #4899 — self-bind for OMN-15142."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4899 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid"
