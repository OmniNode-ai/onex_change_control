---
schema_version: "1.0.0"
ticket_id: "OMN-10512"
title: "Task 1: Extract shared contract-to-topics resolver"
summary: "Create cli_resolve_contract_topics.py in omnibase_core CLI layer with two reusable functions:
  resolve_node_contract (entry-point → contract.yaml Path, raises ModelOnexError not ClickException)
  and resolve_contract_topics (contract.yaml → (command_topic, terminal_topic), reading subscribe_topics
  since the CLI is the sender). Foundational for OMN-10511 epic."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "11 unit tests in test_cli_resolve_contract_topics.py pass on omnibase_core PR #1032"
    command: "cd omni_worktrees/OMN-10512/omnibase_core && env -u PYTHONPATH uv run pytest tests/unit/cli/test_cli_resolve_contract_topics.py
      -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "11 unit tests pass for cli_resolve_contract_topics on omnibase_core PR #1032 at commit
      fba18a39d1cf2a1368975560b360b4981328f92f"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10512/dod-001/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-001\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10512/dod-001/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10512\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10512/dod-001/command.yaml\"
          && grep -q '^commit_sha: \"fba18a39d1cf2a1368975560b360b4981328f92f\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10512/dod-001/command.yaml\"
          && grep -q '^pr_number: 1032$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10512/dod-001/command.yaml\""
  - id: "dod-deploy"
    description: "No runtime deploy required — cli_resolve_contract_topics.py is a pure CLI utility
      with no runtime service, daemon, or Docker image dependency. deploy gate satisfied by confirming
      no docker exec or service restart is needed for this change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10512/dod-deploy/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-deploy\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10512/dod-deploy/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10512\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10512/dod-deploy/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carrying the OMN-10512 contract and receipt evidence"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10512/dod-occ-pr/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-occ-pr\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10512/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10512\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10512/dod-occ-pr/command.yaml\""
