---
schema_version: "1.0.0"
ticket_id: "OMN-17293"
title: "Autobind OCC evidence for OMN-17293"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omniintelligence PR
  #874.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #874 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #874 on OmniNode-ai/omniintelligence, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omniintelligence`.
    command: "uv run pytest tests/unit/review_pairing/test_cli_review.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniintelligence-pr-874"
    description: "PR #874 on OmniNode-ai/omniintelligence — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniintelligence/contents/tests/unit/review_pairing/test_cli_review.py?ref=441ee5ed9c042dd33e4500c75d1becdf6c095370 --jq '.content' | base64 -d | grep -c 'def test_oversized_served_diff_is_bounded_before_dispatch'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniintelligence/pulls/874/files --paginate --jq '.[].filename' | grep -cFx 'src/omniintelligence/review_pairing/adapters/adapter_ai_reviewer.py'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #874 on OmniNode-ai/omniintelligence — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/review_pairing/test_cli_review.py -q"
        cwd: "${OMNI_HOME}/omniintelligence"
  - id: "occ-self-bind-pr-7818"
    description: "Binds OMN-17293 to OCC companion PR #7818 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7818 --repo OmniNode-ai/onex_change_control --json number,state"
