---
schema_version: 1.0.0
ticket_id: OMN-14360
title: "feat(OMN-14360): fail-closed tenant_scoped_ingress opt-in gate"
summary: >-
  omnibase_infra PR #2255 adds a fail-closed static validator for the OMN-14349 tenant_scoped_ingress
  runtime seam. Contracts that opt into event_bus.tenant_scoped_ingress must subscribe only to tenant-prefixed
  wire topics and must carry an explicit config-as-data seam-test attestation before the opt-in can merge.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: >-
      Independent verification of omnibase_infra PR #2255 at head abac80023c89690268744d83997f74c70a8fe0a5.
      The PR adds src/omnibase_infra/validators/tenant_scoped_ingress_schema.py, an empty config/validation/tenant_scoped_ingress_allowlist.yaml,
      unit coverage, a pre-commit hook, CI lint-job enforcement, and a validator-requirements entry. Local
      verification confirmed 13 unit tests pass, the validator exits 0 over the current source tree because
      no contract sets the flag, ruff format/check are clean, mypy is clean, and the changed-file pre-commit
      suite passes when OMNI_HOME is set for sibling source discovery.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14360/dod-001/command.yaml && grep -q '^pr_number:
          2255$' drift/dod_receipts/OMN-14360/dod-001/command.yaml && grep -q 'abac80023c89690268744d83997f74c70a8fe0a5'
          drift/dod_receipts/OMN-14360/dod-001/command.yaml
  - id: dod-deploy-assessment
    description: >-
      Deploy-surface assessment for PR #2255. The change is a static validation gate wired into pre-commit
      and existing CI lint enforcement. It adds no runtime service, no daemon, no topic, no migration,
      no deployable runtime container change, and no live .201 mutation. Existing contracts do not set
      tenant_scoped_ingress, so the new gate is behavior-preserving until a future contract deliberately
      opts in with a prefixed-topic shape and seam-test allowlist entry.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14360/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-14360/dod-deploy-assessment/command.yaml
  - id: dod-occ-self
    description: >-
      OCC companion self-binding: this onex_change_control PR carries the central OMN-14360 contract and
      receipts consumed by omnibase_infra #2255 through its Evidence-Source line. Self-binds via the first
      commit SHA so occ-preflight eligibility does not require predicting the OCC PR number.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14360/dod-occ-self/command.yaml
