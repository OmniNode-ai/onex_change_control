---
schema_version: "1.0.0"
ticket_id: "OMN-15578"
title: "OCC evidence for OMN-15578"
summary: >
  OCC companion authored directly (claude-code) for OmniNode-ai/omnibase_infra PR #2784 -- NOT-READY /
  reconciliation-exhausted warnings in handler_wiring.py now carry per-topic reason+detail (structured
  extra= fields, matching the OMN-14492 discipline) instead of discarding the classified EnumTopicReadinessFailureReason
  and detail already attached to each ModelTopicReadinessFailure. Companion authored directly because
  the event-driven occ-autobind / occ-companion-effect consumer (node_occ_companion_compute) had not produced
  a companion PR within the CI window -- the source PR's occ-autobind and occ-companion-effect jobs remained
  queued/pending (live GitHub runner-assignment incident), matching the current precedent shape (e.g.
  OCC#6396 for OMN-15918).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2784 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2784"
    description: "PR #2784 on OmniNode-ai/omnibase_infra -- Evidence-Source companion."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2784 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2784 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2784"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-deploy-falsifiable-probe"
    description: >
      Falsifiable deploy probe (OMN-14505/OMN-8912) for the OMN-15578 log-field fix -- reads the actual
      merged source content live from GitHub (never this ticket's own receipts) and asserts both new structured
      "readiness_failures" extra= log fields (the NOT-READY and reconciliation-exhausted sites) are genuinely
      present at the PR head, not just claimed.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=7993b11553e16441b04bb09a24fa86a6158d4f28"
          --jq .content | base64 -d)" && count="$(printf '%s' "$body" | grep -c '"readiness_failures":
          failure_details')" && [ "$count" -eq 2 ]
