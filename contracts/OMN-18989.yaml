---
schema_version: "1.0.0"
ticket_id: OMN-18989
title: A declared closed label set reaches the provider as a constraint, not only the prompt
summary: >-
  A classification answer whose label was NOT in the requested set passed the delegation quality gate
  at 1.0. Asked in prose for one of HEALTH_SIGNAL or INSTRUMENTATION_DEFECT, the served Qwen3.8-27B returned
  the corrupted token INSTRUMENTEMENT_DEFECT and the run terminalised completed against a 0.8 bar with
  no failed gates, three times out of three. Correlations a75a211c-0828-4714-ad7e-610f226529fe and 582e957c-d2bf-49c8-83e7-5e545d0e7765,
  plus a direct endpoint probe. HALF THE REMEDY WAS ALREADY SHIPPED and is pinned rather than rebuilt:
  the gate validates a declared response_contract with a real JSON-Schema validator, so an enum is already
  enforced inbound and the violation already names the offending token. The failing run simply declared
  no contract. What was missing is the OUTBOUND half, which the codebase itself had already named as blocked
  on "a capability field on the binding contract". This adds that field, defaulting OFF, carries it onto
  the RESOLVED backend so an escalation to another rung cannot reuse the previous rung's capability, forwards
  the caller's schema verbatim so the shape the provider enforces cannot drift from the shape the gate
  grades, and declares support on exactly one backend on the strength of a live probe. A backend that
  does not declare support, or a caller that declared no contract, sends nothing and behaves byte-identically
  to before. THE LIMIT IS STATED RATHER THAN IMPLIED - this closes the vocabulary defect and not the wrong-in-vocabulary
  defect. A label outside the declared set becomes mechanically impossible; a wrong label that IS in the
  set stays invisible to every check here and always will. WHY THE EVIDENCE BELOW IS HERMETIC RATHER THAN
  A LIVE DOCKER PROBE - this change is compute-only. It adds a pure function, a config flag and its threading;
  it deploys no new surface and starts no process, so there is nothing about a running container a probe
  could ask that the merged tree does not already answer. A docker probe here would also be refused outright
  by the OMN-14051 guard, which is correct rather than an obstacle to route around: a CI runner has no
  docker daemon, so such a check could never pass and would block real work with a runtime error.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: CI passes on the product pull request.
    command: gh pr checks 2746 --repo OmniNode-ai/omnimarket
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-module-entrypoint-present-omn18989"
    description: >-
      The new module and the entry point the dispatch seam calls exist in the product tree, read through
      the API at a pinned sha rather than from any local working copy. Falsifiable in the useful direction,
      which is what makes it a measurement: the identical command FAILS at any earlier omnimarket commit,
      because neither the file nor the symbol exists there. It names the symbol the fix introduces, not
      a phrase this contract chose, so it cannot pass on a tree that dropped the change.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/delegation/structured_output.py?ref=3253cd2914dbb2f8397115a1d41e7710b7d90a64
          --jq .content | base64 -d | grep -c 'def provider_response_format_for_contract'"
  - id: "dod-capability-declared-on-binding-omn18989"
    description: >-
      The binding contract that ships in the wheel declares structured-output support on local-heavy-reasoning.
      This is the half that decides behaviour: the directive is only sent where a backend declares support,
      so a tree carrying the code but not the declaration would change nothing, and evidence that only
      checked for the module would report success over an inert change. Fails at any earlier commit, where
      the key is absent from the binding.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/configs/bifrost_delegation.yaml?ref=3253cd2914dbb2f8397115a1d41e7710b7d90a64
          --jq .content | base64 -d | grep -c 'supports_response_format_json_schema: true'"
  - id: "dod-default-off-omn18989"
    description: >-
      The capability defaults to OFF on the model, so a backend nobody probed is never sent the directive.
      Asserted as its own item because it is the fail-safe the whole design rests on, and a default flipped
      to true would be invisible to both checks above while changing the blast radius of every backend
      at once.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/models/delegation/wire/model_bifrost_delegation_config.py?ref=3253cd2914dbb2f8397115a1d41e7710b7d90a64
          --jq .content | base64 -d | grep -c 'supports_response_format_json_schema: bool = Field('"
