---
schema_version: "1.0.0"
ticket_id: "OMN-13452"
title: "chore(OMN-13452): unblock omnibase_infra pydantic-settings/msgpack security bump"
summary: >-
  OCC evidence for the omnibase_infra replacement PR that supersedes dependabot #2057. The source change
  preserves the dependency bump from pydantic-settings 2.14.1 to 2.14.2 and msgpack 1.1.2 to 1.2.1, rebased
  onto current dev, and refreshes docker/runners/runner-image.lock.json so runner image identity verification
  stays green after the uv lock update. This contract exists because dependabot branches cannot satisfy
  receipt-gate identity binding; the replacement PR is authored on an OMN-13452 branch and cites this
  OCC evidence source.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-dependency-versions"
    description: >-
      The rebased omnibase_infra replacement branch contains the intended security dependency versions:
      pydantic-settings 2.14.2 and msgpack 1.2.1 in uv.lock.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13452/dod-dependency-versions/command.yaml"
  - id: "dod-runner-identity-verify"
    description: >-
      The runner image identity lock was refreshed and the runner_image_identity verifier accepts the
      resulting docker/runners/runner-image.lock.json.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13452/dod-runner-identity-verify/command.yaml"
  - id: "dod-runner-identity-tests"
    description: >-
      The focused runner image identity test suite passes on the rebased omnibase_infra replacement branch.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13452/dod-runner-identity-tests/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13452.yaml consumed
      by the omnibase_infra replacement PR Receipt Gate via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13452\"$' contracts/OMN-13452.yaml"
