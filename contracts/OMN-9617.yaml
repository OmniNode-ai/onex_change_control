---
schema_version: "1.0.0"
ticket_id: OMN-9617
title: "Behavioral cutover to ONEX_HOOKS_MASK bitmask gates"
summary: "Add HANDOFF_NUDGE bit to enum, create onex_hook_gate() helper, cutover 58 bash wrappers from
  legacy kill-switches"
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "CI passes on both PRs"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "HANDOFF_NUDGE bit added to EnumHookBit (1 << 57)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -c 'HANDOFF_NUDGE' src/omnibase_core/enums/enum_hook_bit.py"
  - id: "dod-002"
    description: "onex_hook_gate() helper added to common.sh"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -c 'onex_hook_gate' plugins/onex/hooks/scripts/common.sh"
  - id: "dod-003"
    description: "All 58 bash wrappers use onex_hook_gate, zero legacy kill-switches remain"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -rc 'onex_hook_gate' plugins/onex/hooks/scripts/ | grep -v common.sh | wc -l"
  - id: "dod-004"
    description: "No OMNICLAUDE_HOOK_*=0/1 patterns remain in bash wrappers"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -r '=0' plugins/onex/hooks/scripts/*.sh | grep -v common.sh | wc -l"
