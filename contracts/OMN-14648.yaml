---
schema_version: "1.0.0"
ticket_id: "OMN-14648"
title: "Autobind OCC evidence for OMN-14648"
summary: >
  OCC contract bound by node_pr_lifecycle_fix_effect Evidence-Source autobind (OMN-13317 F1) when OmniNode-ai/omnimarket
  PR #1780 carried a product-SHA Evidence-Source or was missing an OCC contract, and failed the gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1780 product diff scope present"
    command: "PRODUCT_PR_NUMBER=1780; : ${PR_NUMBER}; gh pr diff $PRODUCT_PR_NUMBER --repo OmniNode-ai/omnimarket
      --name-only | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1780"
    description: "PR #1780 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1780; : ${PR_NUMBER}; gh pr view $PRODUCT_PR_NUMBER --repo OmniNode-ai/omnimarket
          --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1780-ci"
    description: "PR #1780 on OmniNode-ai/omnimarket — product diff scope (OMN-14425 substance check;
      OMN-14650: no longer gated on source-PR CI being green)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1780; : ${PR_NUMBER}; gh pr diff $PRODUCT_PR_NUMBER --repo OmniNode-ai/omnimarket
          --name-only | grep -q ."
  - id: "occ-self-bind-pr-4246"
    description: "OCC companion PR #4246 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14648
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omnimarket-pr-1780-deploy"
    description: "Deploy-readiness assessment for omnimarket PR #1780 runtime-path edits. No live deploy,
      restart, docker exec, rpk topic produce, or .201 mutation was performed by this OCC PR; post-merge
      validation remains in the normal dev deploy pipeline."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14648/dod-omnimarket-pr-1780-deploy/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14648/dod-omnimarket-pr-1780-deploy/command.yaml"
  - id: "occ-self-bind-pr-4247"
    description: "OCC follow-up PR #4247 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14648
      deploy assessment."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4249"
    description: "OCC companion PR #4249 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14648
      rebased head evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1780-head-eb57631a"
    description: "Rebased product PR #1780 evidence for OmniNode-ai/omnimarket after OMN-14648 dev rebase."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=\"${PRODUCT_PR_NUMBER:-1780}\"; PRODUCT_REPO=\"${PRODUCT_REPO:-OmniNode-ai/omnimarket}\";
          gh pr view ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --json number,state,headRefName"
  - id: "dod-omnimarket-pr-1780-ci-head-eb57631a"
    description: "Rebased product diff scope for OmniNode-ai/omnimarket PR #1780 after OMN-14648 dev rebase."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=\"${PRODUCT_PR_NUMBER:-1780}\"; PRODUCT_REPO=\"${PRODUCT_REPO:-OmniNode-ai/omnimarket}\";
          gh pr diff ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --name-only | grep -q ."
  - id: "occ-self-bind-pr-4251"
    description: "OCC companion PR #4251 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14648 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4251 --repo OmniNode-ai/onex_change_control --json number,state"
