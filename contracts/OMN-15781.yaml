---
schema_version: "1.0.0"
ticket_id: "OMN-15781"
title: "Autobind OCC evidence for OMN-15781"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2697.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2697 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2697"
    description: "PR #2697 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2697 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2697 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2697 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2697 --repo OmniNode-ai/omnibase_infra --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2697"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6254"
    description: "Binds OMN-15781 to OCC companion PR #6254 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6254 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-deploy-live-probe-pr-2697"
    description: "Falsifiable deploy probe (OMN-14443/OMN-14505): the original dod-deploy-assessment check
      only greps a PR's own file-path list (gh + grep), so its exit status never depends on the deployed
      system's state. This entry probes the actual content of the fix PR #2697 introduces -- the auto_offset_reset=earliest
      validator guard in model_gateway_forwarder_runtime_config.py -- against the live GitHub content
      API at the PR's own head, which is falsifiable (fails at any pre-fix ref)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_bus_forwarder_effect/models/model_gateway_forwarder_runtime_config.py?ref=df74b4cb379f0cbc54c91fbceb58256a679e0651
          --jq '.content' | base64 -d | grep -c 'require auto_offset_reset=earliest'"
  - id: "occ-self-bind-pr-6281"
    description: "Binds OMN-15781 to OCC companion PR #6281 (self-bind, OMN-14650) -- the falsifiable-deploy-probe
      PR itself. Required by occ-preflight's invariant I3: an OCC PR carries its own contract + a receipt
      binding to its own PR/commit."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6281 --repo OmniNode-ai/onex_change_control --json number,state"
