---
schema_version: "1.0.0"
ticket_id: "OMN-11548"
title: "Add invariant evaluation compute node"
summary: >-
  Bind central OCC evidence for omnibase_infra PR #1712 and omnibase_core PR #1135, which remove the core
  invariant evaluator service surface and add node_invariant_evaluate_compute as the runtime boundary
  for invariant evaluation.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "omnibase_infra PR #1712 CI and receipt gates pass for invariant evaluation compute."
    command: "gh pr checks 1712 --repo OmniNode-ai/omnibase_infra"
  - kind: "tests"
    description: "omnibase_core PR #1135 CI and receipt gates pass for invariant evaluator service removal."
    command: "gh pr checks 1135 --repo OmniNode-ai/omnibase_core"
  - kind: "manual"
    description: "Central OCC evidence PR carries this contract and PASS receipts."
    command: "gh pr view <occ-pr> --repo OmniNode-ai/onex_change_control"
dod_evidence:
  - id: "dod-omnibase-core-pr-1135"
    description: "Bind OMN-11548 evidence to omnibase_core PR #1135 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11548/dod-omnibase-core-pr-1135/command.yaml\"
          && grep -q '^pr_number: 1135$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11548/dod-omnibase-core-pr-1135/command.yaml\""
  - id: "dod-omnibase-infra-pr-1712"
    description: "Bind OMN-11548 evidence to omnibase_infra PR #1712 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11548/dod-omnibase-infra-pr-1712/command.yaml\"
          && grep -q '^pr_number: 1712$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11548/dod-omnibase-infra-pr-1712/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Runtime node addition requires CI validation but no live deploy before merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11548/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11548/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Bind OMN-11548 central evidence to the OCC PR that carries this bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11548/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
