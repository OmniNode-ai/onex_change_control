---
schema_version: "1.0.0"
ticket_id: "OMN-12746"
title: "chore(demo): redeploy omnidash canonical on .201 + run locally (post #171/#172)"
summary: >-
  Central evidence that omnidash canonical is redeployed on .201 after omnidash#171 (OMN-12739) and #172
  (OMN-12740) merge. .201:3001 was serving a stale worktree at fd3f4df; this ticket closes the gap. Local
  Mac run also confirmed clean. Live display of generated text depends on P0-1 output widget (OMN-12745)
  landing first. Hard gate: .201 deploy requires explicit user approval (Operating Rule #2).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-201-redeploy-proof"
    description: ".201 redeployed dashboard reflecting post-#171/#172 state. Deployed canonical SHA confirming
      it is no longer fd3f4df. Screenshot of .201:3001 dashboard showing updated content."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12746/dod-201-redeploy-proof/command.yaml"
  - id: "dod-local-run-proof"
    description: "Local Mac run of omnidash confirmed clean. npm run dev boots without errors. Fixture
      mode works offline without .201 dependency. Console output or screenshot as evidence."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12746/dod-local-run-proof/command.yaml"
  - id: "dod-user-approval-receipt"
    description: "Explicit user approval receipt for .201 live runtime change (Operating Rule #2 hard
      gate)."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12746/dod-user-approval-receipt/command.yaml"
  - id: "dod-occ-pr-evidence"
    description: "OCC PR carries the OMN-12746 central contract and PASS receipts."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12746/dod-occ-pr-evidence/command.yaml"
