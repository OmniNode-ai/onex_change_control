---
schema_version: "1.0.0"
ticket_id: "OMN-9120"
title: "CI Gate — Runtime Boot Smoke Test"
summary: "Wires the existing reusable-runtime-boot.yml workflow into ci.yml as a caller job (runtime-boot-smoke).
  The workflow boots the runtime and runtime-effects services in compose mode after the test suite passes,
  asserts /health, 60s stability, startup log cleanliness, registration_projections liveness, and onex-runtime
  consumer group membership. ci-summary is updated to include runtime-boot-smoke as a hard merge gate."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omnibase_infra PR #1649 adds the runtime-boot-smoke caller job and fixes to reusable-runtime-boot.yml
      for compose-mode runner compatibility. CI passes on the PR and the runtime-boot-smoke job appears
      in checks."
    command: "gh pr view 1649 --repo OmniNode-ai/omnibase_infra --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1649"
    description: "omnibase_infra PR #1649 wires the runtime boot smoke test into CI as a required check,
      making it a hard merge gate via ci-summary."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9120/dod-omnibase-infra-pr-1649/command.yaml
          && grep -q '^pr_number: 1649$' drift/dod_receipts/OMN-9120/dod-omnibase-infra-pr-1649/command.yaml"
  - id: "dod-omnibase-infra-deploy-smoke"
    description: "Runtime boot smoke CI job verifies docker compose e2e stack (postgres, redpanda, runtime)
      reaches healthy state. The CI job uses docker exec to confirm the runtime container is healthy."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'docker exec' drift/dod_receipts/OMN-9120/dod-omnibase-infra-deploy-smoke/command.yaml
          && grep -q 'status: PASS' drift/dod_receipts/OMN-9120/dod-omnibase-infra-deploy-smoke/command.yaml"
