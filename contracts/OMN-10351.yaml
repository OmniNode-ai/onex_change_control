---
schema_version: "1.0.0"
ticket_id: "OMN-10351"
title: "Task 1: [Wave 1 Zone] Add EnumFileZone enum"
summary: "Add EnumFileZone to omnibase_core so tiered QA gates can classify files by directory role instead
  of extension or format."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "EnumFileZone focused tests are present and covered by the implementation PR"
    command: "gh pr diff 987 --repo OmniNode-ai/omnibase_core | grep -E 'enum_file_zone|test_enum_file_zone'"
  - kind: "ci"
    description: "Implementation PR #987 exists in omnibase_core and carries the OMN-10351 change"
    command: "gh pr view 987 --repo OmniNode-ai/omnibase_core --json number,state,url,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "EnumFileZone source and focused tests are present in omnibase_core PR #987"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10351/dod-001/command.yaml && grep
          -q 'enum_file_zone.py' drift/dod_receipts/OMN-10351/dod-001/command.yaml && grep -q 'test_enum_file_zone.py'
          drift/dod_receipts/OMN-10351/dod-001/command.yaml"
  - id: "dod-002"
    description: "omnibase_core PR #987 has completed non-receipt CI checks passing; receipt gate is the
      known blocker"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10351/dod-002/command.yaml && grep
          -q 'verify / verify: FAILURE' drift/dod_receipts/OMN-10351/dod-002/command.yaml && grep -q 'non-receipt
          checks succeeded' drift/dod_receipts/OMN-10351/dod-002/command.yaml"
  - id: "dod-003"
    description: "omnibase_core implementation PR #987 exists for OMN-10351"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10351/dod-003/command.yaml && grep
          -q '^pr_number: 987$' drift/dod_receipts/OMN-10351/dod-003/command.yaml && grep -q 'ad4dd7593cef92efc7f9a0577ca00daec51abf53'
          drift/dod_receipts/OMN-10351/dod-003/command.yaml"
