---
schema_version: "1.0.0"
ticket_id: "OMN-10754"
title: "Create session_outcomes table migration in omnidash_analytics"
summary: >
  The session_outcomes table is referenced by 6 projection handlers but does not exist in the omnidash_analytics
  PostgreSQL database. Fix adds migration 0021_session_outcomes.sql with the full schema (session_id PK,
  outcome, emitted_at, ingested_at, created_at, updated_at) including updated_at trigger matching existing
  migration patterns.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-migration-file"
    description: "Migration file 0021_session_outcomes.sql exists with full schema matching all 6 projection
      handler references."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10754/dod-migration-file/command.yaml\""
  - id: "dod-golden-chain-tests"
    description: "7 golden-chain tests pass in test_golden_chain_projection_session_outcome.py."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10754/dod-golden-chain-tests/command.yaml\""
  - id: "dod-omnimarket-pr-678"
    description: "omnimarket PR #678 open with CI green at commit 30348c38f3755fb18cb0f6bf513d45a232cc2dc5."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10754/dod-omnimarket-pr-678/command.yaml\"
          && grep -q '^pr_number: 678$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10754/dod-omnimarket-pr-678/command.yaml\""
