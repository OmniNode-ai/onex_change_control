---
schema_version: "1.0.0"
ticket_id: "OMN-12936"
title: "Unwrap transport envelope in evidence/readiness coerce + ship overnight base-table migration"
summary: >
  Fixes two BROKEN defects from the 2026-06-11 full-feature pass: the shared coerce(**payload)
  envelope-unwrap defect in the evidence/readiness node family (readiness-gate handler +
  evidence_pipeline_native coerce_* functions), and the missing projection_overnight_readiness
  materialization (HTTP 503 at projection-api startup). Code change is an envelope-aware
  _unwrap_envelope helper at the coercion boundary plus a node-owned idempotent base-table
  migration (0000_create_overnight_sessions_tables.sql) ordered before the existing 0001 view.
  No runtime topology, topic schema, or contract API changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1152"
    description: >
      omnimarket PR #1152 routes every coerce_* function and the readiness-gate dispatch decision
      through an envelope-aware _unwrap_envelope helper, fixing the shared coerce(**payload) defect
      class. New regression suite tests/test_evidence_readiness_envelope_unwrap.py (11 cases): the 5
      envelope-unwrap cases fail on the pre-fix tree (TDD) and pass after the fix. mypy --strict clean,
      ruff clean, pre-commit hooks pass, runtime sweep 296/296 OK.
    source: "manual"
    status: "verified"
  - id: "dod-overnight-migration"
    description: >
      Ships node_projection_overnight/migrations/0000_create_overnight_sessions_tables.sql (idempotent,
      IF NOT EXISTS throughout) ordered before the 0001 view, so the omnimarket projection migration
      runner is self-sufficient and projection_overnight_readiness materializes instead of returning 503.
      Migration discovery + apply order verified (0000 before 0001).
    source: "manual"
    status: "verified"
  - id: "dod-deploy-assessment"
    description: >
      Deploy assessment: this change touches omnimarket runtime handler paths
      (evidence_pipeline_native.py, handler_readiness_gate_orchestrator.py) and adds one idempotent
      projection migration. Live materialization requires a standard image rebuild + projection
      migration run on the stability-test lane: the projection migration runner applies the new
      0000 base-table migration (`uv run python scripts/run-projection-migrations.py`), then a
      `docker exec` projection-api restart picks up projection_overnight_readiness. No new Kafka
      topic, no contract API change, no prod mutation. The coerce fix is exercised by the existing
      deployed evidence/readiness nodes once the rebuilt image is rolled. Deploy is gated on the
      post-merge stability-test redeploy cycle, not performed by this PR.
    source: "manual"
    status: "verified"
