---
# OMN-9620 contract file for omnibase_infra deploy-gate (OMN-8912).
# The canonical ticket contract lives in onex_change_control.
# This file is the per-repo deploy-evidence carrier only.
schema_version: '1.0.0'
ticket_id: OMN-9620
title: "A2A Delegation Bridge (Part 1)"
summary: 'A2A Delegation Bridge (Part 1)'
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - events
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: PR opened for A2A delegation bridge runtime path
    source: generated
    checks:
      - check_type: command
        check_value: 'gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnibase_infra --json state -q .state'
  - id: dod-002
    description: Contract artifact file present in repo
    source: generated
    checks:
      - check_type: command
        check_value: 'test -f contracts/OMN-9620.yaml'
  - id: dod-003
    description: Runtime deploy validates A2A delegation bridge wiring after merge
    source: manual
    checks:
      - check_type: command
        check_value: 'deploy omninode-runtime on .201 after merge and verify delegation bridge lifecycle
          consumers start'
