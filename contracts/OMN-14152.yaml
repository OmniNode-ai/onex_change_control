---
schema_version: "1.0.0"
ticket_id: "OMN-14152"
title: "Serve omnidash SPA from the Express bridge"
summary: "Co-locate omnidash static SPA serving with the Express bridge while preserving API and projection
  route behavior."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnidash PR #254 carries the static-serve implementation at the observed head and has
      receipt-honesty green; local build, lint, route tests, and branch-preview evidence are recorded
      in the PR body."
    command: "gh -R OmniNode-ai/omnidash pr view 254 --json number,title,headRefName,headRefOid,files,statusCheckRollup"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnidash-pr-254"
    description: "omnidash PR #254 is bound to OMN-14152 at head 76e2d89130519d6bf849f21908a55d0c906aaec0
      and carries the expected server/projection changes."
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh -R OmniNode-ai/omnidash pr view 254 --json number,title,headRefName,headRefOid,files,statusCheckRollup
          --jq '{number,title,headRefName,headRefOid,files:[.files[].path],receipt_honesty:[.statusCheckRollup[]?
          | select(.name==\"receipt-honesty\") | {name,conclusion,status}]}'"
    evidence_artifact: "drift/dod_receipts/OMN-14152/dod-omnidash-pr-254/command.yaml"
