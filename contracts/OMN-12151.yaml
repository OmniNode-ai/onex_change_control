---
schema_version: "1.0.0"
ticket_id: "OMN-12151"
title: "Suppress DispatchResultApplier terminal event for multi-step FSM orchestrators"
summary: >
  Two-repo fix: omnibase_infra PR #1745 widens DispatchResultApplier.apply() to accept ModelDispatchResult
  | None and exits immediately on None; omnimarket PR #887 makes handle_async return None for non-terminal
  FSM states so the swarm orchestrator health-check sub-command fires before any terminal event is published.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1745"
    description: "omnibase_infra PR #1745 — widen DispatchResultApplier.apply to accept None."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12151/dod-infra-pr-1745/command.yaml"
  - id: "dod-omnimarket-pr-887"
    description: "omnimarket PR #887 — handle_async returns None for non-terminal FSM states."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12151/dod-omnimarket-pr-887/command.yaml"
  - id: "dod-tests-pass"
    description: >
      Unit tests pass in both repos: test_none_result_suppresses_terminal_event (infra) and TestHandleAsyncReturnsNone
      (omnimarket).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12151/dod-tests-pass/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OCC evidence for OMN-12151."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12151/dod-occ-pr/command.yaml"
  - id: "dod-runtime-deploy"
    description: >
      Runtime image rebuild evidence: omnibase_infra PR #1745 includes a successful 'Build Runtime Image'
      CI job, confirming the deploy path is exercised. Verified via gh run view on the PR's build job.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12151/dod-runtime-deploy/command.yaml
          && grep -q 'deploy' drift/dod_receipts/OMN-12151/dod-runtime-deploy/command.yaml"
