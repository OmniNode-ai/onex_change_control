---
schema_version: "1.0.0"
ticket_id: "OMN-13530"
title: "Validator requiring operation on operation_match handler entries"
summary: >
  Adds an omnibase_core validator, pre-commit hook, and CI workflow that fail when handler_routing entries
  using operation_match, or the runtime's absent-strategy default branch, omit a non-empty operation field.
  This is the enforcement gate for the routing regression that broke multiple headless automation contracts.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Operation-match validator unit tests and local hook wiring pass."
    command: "uv run pytest tests/unit/validators/test_operation_match_requires_operation.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1310"
    description: "omnibase_core PR #1310 adds the validator, model, hook exports, CI workflow, and unit
      tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13530/dod-omnibase-core-pr-1310/command.yaml"
  - id: "dod-omnibase-infra-pr-2081"
    description: "omnibase_infra PR #2081 adds operation fields to infra operation_match contracts and
      keeps the enforcement gate aligned with the core validator."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13530/dod-omnibase-infra-pr-2081/command.yaml"
  - id: "dod-omnimarket-pr-1361"
    description: "omnimarket PR #1361 sweeps operation_match contracts for the headless closeout fleet
      and adds golden-chain coverage for the touched live-path nodes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13530/dod-omnimarket-pr-1361/command.yaml"
  - id: "dod-deploy-no-runtime-impact"
    description: "Static validation gate only; no live deploy, runtime process, broker topic, or schema
      migration required before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13530/dod-deploy-no-runtime-impact/command.yaml"
  - id: "dod-occ-pr-self"
    description: "OCC self-binding receipt for the central evidence PR carrying the OMN-13530 contract
      and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13530/dod-occ-pr-self/command.yaml"
