---
schema_version: "1.0.0"
ticket_id: "OMN-13722"
title: "Daily deep-dive report epic"
summary: >-
  Epic-level contract for the daily deep-dive report workstream. omnimarket PR #1523 references this epic
  in its body while the concrete implementation and receipt identity are owned by OMN-13725.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: manual
    description: >-
      Deploy-gate reference for PRs that cite the OMN-13722 epic while implementing concrete slices under
      child tickets such as OMN-13725.
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13722/dod-epic-deploy-reference/command.yaml
      && grep -q 'deploy' drift/dod_receipts/OMN-13722/dod-epic-deploy-reference/command.yaml && grep
      -q 'docker exec' drift/dod_receipts/OMN-13722/dod-epic-deploy-reference/command.yaml && grep -q
      'rpk topic produce' drift/dod_receipts/OMN-13722/dod-epic-deploy-reference/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-epic-deploy-reference
    description: >-
      Epic deploy-gate reference for omnimarket PR #1523. Concrete implementation evidence remains bound
      to OMN-13725; this exists because deploy-gate scans all OMN ticket mentions in runtime PRs.
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13722/dod-epic-deploy-reference/command.yaml
          && grep -q '^pr_number: 1523$' drift/dod_receipts/OMN-13722/dod-epic-deploy-reference/command.yaml
          && grep -q 'deploy' drift/dod_receipts/OMN-13722/dod-epic-deploy-reference/command.yaml && grep
          -q 'docker exec' drift/dod_receipts/OMN-13722/dod-epic-deploy-reference/command.yaml && grep
          -q 'rpk topic produce' drift/dod_receipts/OMN-13722/dod-epic-deploy-reference/command.yaml"
