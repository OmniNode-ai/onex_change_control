---
schema_version: "1.0.0"
ticket_id: "OMN-10343"
title: "Task 12: Runner usage savings and emitter"
summary: "Add runner usage event emission in omninode_infra and savings estimation in omnibase_infra for
  Task 12 cost observability."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
  - "protocols"
evidence_requirements:
  - kind: "tests"
    description: "omninode_infra runner usage emitter tests pass"
    command: "cd omninode_infra && uv run pytest tests/scripts/test_emit_runner_usage.py -q"
  - kind: "tests"
    description: "omninode_infra runner usage emitter shell script passes shellcheck"
    command: "cd omninode_infra && shellcheck scripts/emit-runner-usage.sh"
  - kind: "tests"
    description: "omnibase_infra runner usage savings node, pricing manifest, and golden fixture tests
      pass"
    command: "cd omnibase_infra && uv run pytest tests/integration/nodes/test_node_runner_usage_effect.py
      tests/unit/models/pricing/test_model_pricing_table.py tests/ci/test_pricing_manifest_schema.py -q"
  - kind: "ci"
    description: "Task 12 implementation PRs are created and linked"
    command: "gh pr view 373 --repo OmniNode-ai/omninode_infra --json number,state,headRefOid && gh pr
      view 1464 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "omninode_infra runner usage emitter payload tests passed locally"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' ${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-001/command.yaml
          && grep -q '^ticket_id: OMN-10343$' ${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-001/command.yaml
          && grep -q '^pr_number: 373$' ${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-001/command.yaml"
  - id: "dod-002"
    description: "omninode_infra runner usage emitter shellcheck passed locally"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' ${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-002/command.yaml
          && grep -q '^ticket_id: OMN-10343$' ${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-002/command.yaml
          && grep -q '^pr_number: 373$' ${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-002/command.yaml"
  - id: "dod-003"
    description: "omnibase_infra runner usage savings, pricing, and golden fixture tests passed locally"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' ${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-003/command.yaml
          && grep -q '^ticket_id: OMN-10343$' ${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-003/command.yaml
          && grep -q '^pr_number: 1464$' ${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-003/command.yaml"
  - id: "dod-004"
    description: "Task 12 implementation PRs exist with expected head commits"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-004/command.yaml" \
          && grep -q 'omninode_infra PR #373 is open at head a27a72009f3f642baf3522fa635af71cb83bd1b4' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-004/command.yaml" \
          && grep -q 'omnibase_infra PR #1464 is merged at head ea6cc4d83efeacf19aa0429d157ffde6ff2c0c91' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-004/command.yaml"
  - id: "dod-occ-pr-686"
    description: "OCC PR #686 self-binding receipt exists for the OMN-10343 contract path repair"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          grep -q '^status: PASS$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-occ-pr-686/command.yaml" \
          && grep -q '^evidence_item_id: "dod-occ-pr-686"$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-occ-pr-686/command.yaml" \
          && grep -q '^ticket_id: "OMN-10343"$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-occ-pr-686/command.yaml" \
          && grep -q '^pr_number: 686$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-occ-pr-686/command.yaml" \
          && grep -q '^commit_sha: "7333c48f14e1537482d03b2a843aabf0266beee5"$' "${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-10343/dod-occ-pr-686/command.yaml"
