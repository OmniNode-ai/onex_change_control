---
schema_version: "1.0.0"
ticket_id: "OMN-12880"
title: "[build] Guardrail: include compatibility publish topics in route discovery"
summary: >-
  Tracks extension of route discovery and validation so compatibility publish topics used by delegation
  and runtime compatibility surfaces are included in coverage analysis.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-compat-topic-route-discovery"
    description: >-
      Failing compatibility-topic omission fixture trips the guard; expected compatibility event topics
      are discoverable and green.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12880/dod-compat-topic-route-discovery/command.yaml"
  - id: "dod-omnimarket-pr-1198-deploy-assessment"
    description: >-
      omnimarket PR #1198 includes fixture coverage for compatibility topic route discovery and a metadata-only
      runtime profile correction (node_volume_config_drift_sweep runtime_profiles: compute -> workers)
      carried forward from dev validation drift. No live deploy is required for this PR; the deploy-gate
      evidence is limited to the CI/runtime-profile contract metadata check.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12880/dod-omnimarket-pr-1198-deploy-assessment/command.yaml"
          && grep -q 'No live deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12880/dod-omnimarket-pr-1198-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      This OCC PR carries the central OMN-12880 receipt repair that omnimarket PR #1198 resolves via Evidence-Source
      while preserving the existing infra PR #1965 receipt binding.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12880/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-12880"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12880/dod-occ-pr/command.yaml"
