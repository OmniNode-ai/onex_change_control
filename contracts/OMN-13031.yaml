---
schema_version: "1.0.0"
ticket_id: "OMN-13031"
title: "Armed-not-enqueued detector — read-only sweep tooling"
summary: >-
  Ships the armed-not-enqueued detector in omniclaude PR #1748: a read-only sweep tool that flags PRs
  where autoMergeRequest != null AND mergeStateStatus CLEAN AND no ADDED_TO_MERGE_QUEUE_EVENT appears
  in the PR timeline newer than the arming timestamp, sustained >30 minutes. Covers all 7 queue repos.
  Ships with 29 unit tests, a CLI entry point, and a thin shell entrypoint. No runtime deploy, restart,
  topic mutation, or service contract change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1748"
    description: "omniclaude PR #1748 carries the armed-not-enqueued detector, CLI entry point, shell
      entrypoint, and 29 unit tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13031/dod-omniclaude-pr-1748/command.yaml
          && grep -q '^pr_number: 1748$' drift/dod_receipts/OMN-13031/dod-omniclaude-pr-1748/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-13031 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13031/dod-occ-pr/command.yaml"
