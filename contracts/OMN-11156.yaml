---
schema_version: "1.0.0"
ticket_id: "OMN-11156"
title: "Flip node purity check to blocking in omnibase_core"
summary: >
  Central OCC evidence for the OMN-11156 slice of omnibase_core PR #1106, which removes advisory behavior
  from the node-purity gate and confirms no current pure-node violations.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Node purity validation passes with zero impure pure-node files."
    command: "uv run python scripts/check_node_purity.py --verbose"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-node-purity-blocking"
    description: "Node purity check is blocking and local validation reports all pure nodes maintain guarantees."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11156/dod-node-purity-blocking/command.yaml\"
          && grep -q 'PASSED: All pure nodes maintain purity guarantees' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11156/dod-node-purity-blocking/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-11156 central contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11156/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number: 1151$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11156/dod-occ-pr/command.yaml\""
