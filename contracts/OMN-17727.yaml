---
schema_version: "1.0.0"
ticket_id: "OMN-17727"
title: "feat(infra): harden first-effect ledger PostgreSQL ownership and least-privilege boundary"
summary: "Governs only candidate migration 107 ACL/topology hardening atop the OMN-17463 ledger base;
  no product PR, receipt, migration application, role provisioning, deployment, restart, secret action,
  or live database action is claimed."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused static and disposable PostgreSQL tests must prove the named owner, verifier,
      and stage-publisher boundaries before a product PR is proposed."
    command: "uv run pytest tests/unit/migrations/test_107_first_effect_ledger_privileges_sql.py tests/unit/runtime/test_rsd_verified_first_effect_grant_ingress.py
      tests/unit/topology/test_application_database_table_grants.py tests/integration/migrations/test_105_verified_first_effect_grant_ledger.py
      -q"
  - kind: "ci"
    description: "Migration 107 must remain append-only and represented by the committed schema fingerprint
      after every candidate rebase."
    command: "uv run python scripts/check_schema_fingerprint.py verify"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-107-privilege-static"
    description: "Focused SQL, ingress, and topology tests prove the verifier recorder boundary and the
      narrow stage-publisher grants without a live database."
    source: "manual"
    execution_scope: "local_done_gate"
    status: "pending"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/migrations/test_107_first_effect_ledger_privileges_sql.py
          tests/unit/runtime/test_rsd_verified_first_effect_grant_ingress.py tests/unit/topology/test_application_database_table_grants.py
          -q"
        cwd: "${OMNI_HOME}/omni_worktrees/OMN-17463-migration-107-governance-pending/omnibase_infra"
  - id: "dod-107-disposable-postgres-acl"
    description: "The disposable PostgreSQL migration fixture proves restricted ACL behavior and rollback
      restoration without using a shared or live database."
    source: "manual"
    execution_scope: "local_done_gate"
    status: "pending"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/integration/migrations/test_105_verified_first_effect_grant_ledger.py
          -q"
        cwd: "${OMNI_HOME}/omni_worktrees/OMN-17463-migration-107-governance-pending/omnibase_infra"
  - id: "dod-107-fingerprint"
    description: "The candidate's appended 107 forward and rollback migrations are represented by the
      committed schema fingerprint."
    source: "manual"
    execution_scope: "local_done_gate"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/check_schema_fingerprint.py verify"
        cwd: "${OMNI_HOME}/omni_worktrees/OMN-17463-migration-107-governance-pending/omnibase_infra"
  - id: "dod-occ-contract-schema"
    description: "The central contract remains parseable by the canonical OCC validator; this is governance-artifact
      integrity only, not product or deployment evidence."
    source: "manual"
    execution_scope: "hosted_and_local"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: "uv run validate-yaml contracts/OMN-17727.yaml"
