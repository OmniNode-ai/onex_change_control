---
schema_version: "1.0.0"
ticket_id: "OMN-13587"
title: "Capture pre-commit failure signal in pr-polish-completed event"
summary: >-
  Central evidence for omnimarket PR #1394, which adds structured pre-commit failure capture to the node_pr_polish
  workflow and exposes the signal on the pr-polish-completed terminal event. The change is runtime-path
  touching and build/PR scoped; this contract supplies the central Receipt-Gate and deploy-gate evidence
  cited by the product PR.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1394"
    description: >-
      omnimarket PR #1394 carries the typed ModelPrPolishPrecommitFailure model, event payload wiring,
      workflow-runner capture path, and focused tests for pre-commit failure signal persistence.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid,baseRefName"
  - id: "dod-occ-evidence-source"
    description: >-
      This OCC contract is the central Evidence-Source for OMN-13587 and is cited by omnimarket PR #1394.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-13587.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: omnimarket PR #1394 changes runtime node_pr_polish paths and the completed-event
      contract. The PR scope is code, tests, and contract evidence only; post-merge runtime rollout follows
      the normal deployment lane.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13587/dod-deploy-assessment/command.yaml"
