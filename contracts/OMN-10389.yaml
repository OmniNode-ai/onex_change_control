---
schema_version: "1.0.0"
ticket_id: OMN-10389
title: "Add typed CLI report models"
summary: "Add the typed market CLI report models used by shared output renderers and market skill integrations."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "CLI report model unit tests pass"
    command: "uv run pytest tests/unit/models/test_cli_report.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Typed CLI report model tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/models/test_cli_report.py -q"
  - id: "dod-002"
    description: "No deploy required; this is a typed model surface consumed in-process by market CLIs"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for OMN-10389 - typed model surface only'"
