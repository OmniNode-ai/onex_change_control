---
schema_version: "1.0.0"
ticket_id: "OMN-15804"
title: "disk-gc.sh untag-then-remove for multi-tag images + fresh in-use recheck"
summary: >
  Manual OCC companion for omnibase_infra PR #2709, the recurrence-prevention fix for the onex-disk-gc
  timer's multi-tag removal failure ("must be forced - referenced in multiple repositories") and the short-id
  vs full-id protect_running matching gap.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2709 exact head and disk-gc multi-tag/in-use-recheck file set are live."
    command: "gh pr view 2709 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2709-1dfdb402"
    description: "PR #2709 on OmniNode-ai/omnibase_infra - exact-head disk-gc.sh untag-then-remove + fresh
      in-use recheck diff (OMN-15804)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2709 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,headRefOid,files
          --jq 'if (.number == 2709 and .state == "OPEN" and .headRefName == "jonah/omn-15804-disk-gc-multitag-fix"
          and .headRefOid == "1dfdb4024129e85ab5551098472e0246acd5b7b0" and ([.files[].path] | sort) ==
          ["scripts/disk-gc.sh","scripts/disk_gc_plan.py","tests/unit/scripts/test_disk_gc_execute_untag.py","tests/unit/scripts/test_disk_gc_plan.py"])
          then "true" else error("unexpected #2709 state, head, or file set") end'
