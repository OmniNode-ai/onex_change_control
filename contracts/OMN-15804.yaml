---
schema_version: "1.0.0"
ticket_id: "OMN-15804"
title: "disk-gc.sh untag-then-remove for multi-tag images + fresh in-use recheck"
summary: >
  Manual OCC companion for omnibase_infra PR #2709, the recurrence-prevention fix for the onex-disk-gc
  timer's multi-tag removal failure ("must be forced - referenced in multiple repositories") and the short-id
  vs full-id protect_running matching gap.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2709 source-level content proof: disk_gc_plan.py carries remove_image_refs and disk-gc.sh
      carries the ancestor-filter fresh recheck."
    command: "gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/disk_gc_plan.py?ref=1dfdb4024129e85ab5551098472e0246acd5b7b0"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2709-1dfdb402"
    description: "PR #2709 on OmniNode-ai/omnibase_infra - source-level proof that the disk-gc.sh untag-then-remove
      fix and remove_image_refs plan field are live at the PR head (OMN-15804)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          plan_py="$(gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/disk_gc_plan.py?ref=1dfdb4024129e85ab5551098472e0246acd5b7b0 --jq .content | base64 -d)" &&
          printf '%s' "$plan_py" | grep -qF 'remove_image_refs' &&
          gc_sh="$(gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/disk-gc.sh?ref=1dfdb4024129e85ab5551098472e0246acd5b7b0 --jq .content | base64 -d)" &&
          printf '%s' "$gc_sh" | grep -qF 'ancestor='
