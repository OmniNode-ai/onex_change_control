---
schema_version: "1.0.0"
ticket_id: "OMN-10123"
title: "Document plugin_compute_base scope and forbid new Plugin* daemon classes"
summary: "Add a pre-merge AST guard for Plugin* lifecycle classes and document that PluginComputeBase
  is compute-only."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "AST validator unit tests pass"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10123/dod-validator-tests/command.yaml"
  - kind: "manual"
    description: "Current source tree has no Plugin* lifecycle violations"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10123/dod-source-scan/command.yaml"
  - kind: "manual"
    description: "Pre-commit hook enforces the Plugin* lifecycle guard"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10123/dod-precommit-hook/command.yaml"
  - kind: "manual"
    description: "CI and repo-local contract evidence are wired"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10123/dod-ci-contract/command.yaml"
  - kind: "manual"
    description: "Root CLAUDE.md documents PluginComputeBase compute-only scope"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10123/dod-root-doc/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-validator-tests"
    description: "AST validator unit tests pass, including fake PluginXyzDaemon detection"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10123/dod-validator-tests/command.yaml
          && grep -q '^ticket_id: OMN-10123$' drift/dod_receipts/OMN-10123/dod-validator-tests/command.yaml"
  - id: "dod-source-scan"
    description: "Validator scans current omnibase_infra source tree without baseline violations"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10123/dod-source-scan/command.yaml
          && grep -q '^ticket_id: OMN-10123$' drift/dod_receipts/OMN-10123/dod-source-scan/command.yaml"
  - id: "dod-precommit-hook"
    description: "Pre-commit hook blocks Plugin* daemon/service/worker classes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10123/dod-precommit-hook/command.yaml
          && grep -q '^ticket_id: OMN-10123$' drift/dod_receipts/OMN-10123/dod-precommit-hook/command.yaml"
  - id: "dod-ci-contract"
    description: "CI lint job and repo-local contract evidence are wired"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10123/dod-ci-contract/command.yaml
          && grep -q '^ticket_id: OMN-10123$' drift/dod_receipts/OMN-10123/dod-ci-contract/command.yaml"
  - id: "dod-root-doc"
    description: "Root CLAUDE.md documents PluginComputeBase scope"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10123/dod-root-doc/command.yaml
          && grep -q '^ticket_id: OMN-10123$' drift/dod_receipts/OMN-10123/dod-root-doc/command.yaml"
