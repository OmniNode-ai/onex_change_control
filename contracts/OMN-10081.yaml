---
schema_version: "1.0.0"
ticket_id: "OMN-10081"
summary: "Add local Unix-socket command ingress to RuntimeHostProcess"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "envelopes"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused omnibase_infra runtime-ingress tests pass locally"
    command: "cd ../omnibase_infra && uv run pytest tests/unit/runtime tests/integration -k 'runtime_ingress
      or local_ingress' -q"
  - kind: "ci"
    description: "omnibase_infra PR checks pass for the runtime ingress change"
    command: "gh pr checks --repo OmniNode-ai/omnibase_infra --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Runtime-owned Unix-socket ingress starts and stops with the runtime lifecycle"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10081/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "Ingress requests reach the existing runtime dispatch path under the active runtime package
      boundary"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10081/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "Socket cleanup and runtime-ordering regression proof is captured canonically"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10081/dod-003 -type f -name '*.yaml' | grep -q ."
