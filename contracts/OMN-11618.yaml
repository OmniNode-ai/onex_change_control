---
schema_version: "1.0.0"
ticket_id: "OMN-11618"
title: "Extract shared projection protocol test fixtures"
summary: >-
  Bind omnibase_compat PR #103 evidence for extracting shared projection protocol test fixtures into tests/unit/protocols/conftest.py.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Projection protocol tests pass after fixture extraction in omnibase_compat."
    command: "uv run pytest tests/unit/protocols/test_protocol_projection_database.py tests/unit/protocols/test_protocol_projection_database_sync.py
      -q"
dod_evidence:
  - id: "dod-compat-pr-103"
    description: "omnibase_compat PR #103 carries the projection protocol fixture extraction at the observed
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11618/dod-compat-pr-103/command.yaml"
          && grep -q '^pr_number: 103$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11618/dod-compat-pr-103/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11618 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11618/dod-occ-pr-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
