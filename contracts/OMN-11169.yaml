---
schema_version: "1.0.0"
ticket_id: "OMN-11169"
title: "Flip seam contract coverage hook from advisory to blocking"
summary: >
  Remove the --warn-only flag from the check-seam-contract-coverage pre-commit hook so a missing seam
  contract fails the hook instead of merely warning. Part of OMN-10972 (22-gate hardening sweep). Delivery
  via onex_change_control PR #1132.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "Seam contract coverage hook no longer passes --warn-only"
    command: "grep -n 'check_seam_contract_coverage.py' .pre-commit-config.yaml"
  - kind: "ci"
    description: "onex_change_control PR #1132 checks pass before merge"
    command: "gh pr checks 1132 --repo OmniNode-ai/onex_change_control --watch=false"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-seam-hook-blocking"
    description: "The check-seam-contract-coverage pre-commit hook entry no longer carries --warn-only."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11169/dod-seam-hook-blocking/command.yaml\""
  - id: "dod-occ-pr"
    description: "Self-binding receipt for OCC PR #1132 carrying the OMN-11169 contract and receipt bundle."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11169/dod-occ-pr/command.yaml\""
