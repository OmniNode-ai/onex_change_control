---
schema_version: "1.0.0"
ticket_id: "OMN-12438"
title: "Restore consumer.py max_tokens to 8192 and add regression guard"
summary: >
  PR #166 (commit 13127a7) reverted src/pipeline/consumer.py max_tokens from 8192 back to 2048. reg-005
  (7-task delegation) truncates deterministically at 2048 (finish_reason:length; needs ~3485 tokens).
  This ticket restores 8192 and adds a consumer-path regression guard so the revert cannot go uncaught
  in the future.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "onex-self-extending-agent PR #168 carries the max_tokens fix and regression guard."
    command: "See drift/dod_receipts/OMN-12438/dod-sea-pr-168/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carrying this contract binds the central evidence publication."
    command: "See drift/dod_receipts/OMN-12438/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-168"
    description: "onex-self-extending-agent PR #168 restores max_tokens=8192 in consumer.py and adds test_call_llm_sends_max_tokens_8192
      regression guard."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12438/dod-sea-pr-168/command.yaml
          && grep -q '^pr_number: 168$' drift/dod_receipts/OMN-12438/dod-sea-pr-168/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control publishes the OMN-12438 central contract and receipts."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12438/dod-occ-pr/command.yaml"
