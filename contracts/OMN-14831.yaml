---
schema_version: 1.0.0
ticket_id: OMN-14831
title: Autobind OCC evidence for OMN-14831
summary: OCC Evidence-Source autobind companion for OmniNode-ai/omnibase_infra#2364.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #2364 product diff scope present'
    command: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-OmniNode-ai-omnibase_infra-pr-2364
    description: 'PR #2364 on OmniNode-ai/omnibase_infra -- Evidence-Source autobind.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-OmniNode-ai-omnibase_infra-pr-2364-ci
    description: 'PR #2364 on OmniNode-ai/omnibase_infra -- product diff scope check.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
  - id: dod-omnibase-infra-pr-2364-deploy-scope
    description: 'Deploy-gate scope for omnibase_infra PR #2364. The change touches src/omnibase_infra/nodes/node_validation_ledger_projection_compute/handlers/handler_validation_ledger_projection.py
      runtime handler path, but only flips the existing validation ledger projection compute node to canonical
      def-B dispatch shape with equivalence proof while preserving deterministic validation ledger projection
      semantics. No live runtime deployment, .201 mutation, docker exec, or rpk topic produce is authorized
      by this evidence repair; hosted merge gates and post-merge dev availability remain the deployment
      boundary.'
    source: generated
    checks:
      - check_type: command
        check_value: printf '%s\n' 'deploy scope assessed for omnibase_infra#2364 runtime handler path;
          no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'
  - id: occ-self-bind-pr-4486
    description: OCC companion PR  #4486 -- self-bind for OMN-14831 deploy evidence repair.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: dod-infra-2364-head-fde23bdf
    description: 'PR #2364 on OmniNode-ai/omnibase_infra -- refreshed product head fde23bdf.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName
    evidence_artifact: supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2364
  - id: dod-infra-2364-diff-fde23bdf
    description: 'PR #2364 on OmniNode-ai/omnibase_infra -- refreshed product diff scope at head fde23bdf.'
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json files
    evidence_artifact: supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2364-ci
  - id: dod-omnibase-infra-pr-2364-deploy-scope-fde23bdf
    description: 'Deploy-gate scope for omnibase_infra PR #2364 at head fde23bdf. The change touches src/omnibase_infra/nodes/node_validation_ledger_projection_compute/contract.yaml
      and src/omnibase_infra/nodes/node_validation_ledger_projection_compute/handlers/handler_validation_ledger_projection.py
      runtime contract/handler paths, but only records the existing canonical def-B validation ledger
      projection dispatch shape and keeps validation_ledger.append intent routing explicit. No live runtime
      deployment, .201 mutation, docker exec, or rpk topic produce is authorized by this evidence repair;
      hosted merge gates and post-merge dev availability remain the deployment boundary.'
    source: generated
    checks:
      - check_type: command
        check_value: printf '%s\n' 'deploy scope assessed for omnibase_infra#2364 refreshed runtime contract/handler
          paths; no live deploy, docker exec, or rpk topic produce is authorized by this evidence repair'
    evidence_artifact: supersedes_dod_evidence:dod-omnibase-infra-pr-2364-deploy-scope
  - id: occ-self-bind-pr-4488
    description: OCC follow-up PR  #4488 -- self-bind for OMN-14831 refreshed head evidence repair.
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
    evidence_artifact: supersedes_dod_evidence:occ-self-bind-pr-4486
