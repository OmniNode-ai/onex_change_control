---
schema_version: "1.0.0"
ticket_id: "OMN-12749"
title: "Runtime local adapter invokes typed delegation handlers with kwargs"
summary: >
  Fix LocalRuntimeBusAdapter so typed handlers such as HandlerDelegateSkill.handle(request: ModelDelegateSkillRequest)
  receive the validated Pydantic request object instead of splatted JSON fields, while preserving legacy
  explicit kwargs handlers.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1888"
    description: "omnibase_infra PR #1888 carries the typed local runtime handler invocation fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-omnibase-infra-pr-1888/command.yaml"
          && grep -q '^pr_number: 1888$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-omnibase-infra-pr-1888/command.yaml"
          && grep -q '7f4b3593dc7f23dadeed2242d8ba58ebe7d592df' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-omnibase-infra-pr-1888/command.yaml"
  - id: "dod-local-validation"
    description: "Focused runtime adapter unit tests, handler wiring tests, lint, and diff checks passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-local-validation/command.yaml"
          && grep -q '80 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-local-validation/command.yaml"
          && grep -q 'All checks passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-local-validation/command.yaml"
  - id: "dod-cross-repo-smoke"
    description: "Cross-repo smoke proves the real omnimarket HandlerDelegateSkill runs through LocalRuntimeBusAdapter
      without the prompt kwargs TypeError."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-cross-repo-smoke/command.yaml"
          && grep -q 'HandlerDelegateSkill' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-cross-repo-smoke/command.yaml"
          && grep -q 'status completed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-cross-repo-smoke/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Live /onex:delegate remains gated on approved runtime refresh after PR #1888 merges."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-deploy-assessment/command.yaml"
          && grep -q 'approved runtime refresh' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2235"
    description: "onex_change_control PR #2235 carries the central OMN-12749 contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-occ-pr-2235/command.yaml"
          && grep -q '^pr_number: 2235$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-occ-pr-2235/command.yaml"
  - id: "dod-occ-main-pr-2250"
    description: "onex_change_control PR #2250 promotes the OMN-12749 contract and PASS receipts to main."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-occ-main-pr-2250/command.yaml"
          && grep -q '^pr_number: 2250$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12749/dod-occ-main-pr-2250/command.yaml"
