---
schema_version: 1.0.0
ticket_id: OMN-14682
title: Receipt Gate canonical evidence block extractor hardening
summary: Central OCC evidence for omnibase_core#1470. The product PR anchors Receipt Gate evidence extraction
  to a single top-level canonical evidence block, ignores fenced code and blockquotes, adds a pre-push
  evidence shape hook, and regression-tests the false-positive and false-negative evidence parsing paths
  at exact product head 1fae075ca2bb57a508a7b9002fd3f7827bdc2688.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #1470 product head is exact for OMN-14682 evidence.'
    command: 'PRODUCT_PR_NUMBER=1470; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=1fae075ca2bb57a508a7b9002fd3f7827bdc2688;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
  - kind: ci
    description: 'PR #1470 carries the canonical Receipt Gate extractor hardening diff.'
    command: 'PRODUCT_PR_NUMBER=1470; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER}; gh pr diff
      $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "src/omnibase_core/validation/validator_receipt_gate.py"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnibase_core-pr-1470
    description: 'PR #1470 on OmniNode-ai/omnibase_core exists and is bound to OMN-14682 at exact product
      head 1fae075ca2bb57a508a7b9002fd3f7827bdc2688.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1470; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=1fae075ca2bb57a508a7b9002fd3f7827bdc2688;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnibase_core-pr-1470-diff
    description: 'PR #1470 on OmniNode-ai/omnibase_core touches the Receipt Gate parser, the new evidence
      shape CLI and hook, and the canonical evidence block regression suite.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1470; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "src/omnibase_core/validation/validator_receipt_gate.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "src/omnibase_core/validation/validator_evidence_shape_cli.py"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx ".pre-commit-hooks/check-evidence-shape.sh"
          && gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -qx "tests/unit/validation/test_receipt_gate_canonical_evidence_block.py"'
  - id: dod-omnibase_core-pr-1470-local-verification
    description: 'Product PR body records local verification for OMN-14682: ruff format/check clean, mypy
      strict clean on changed source, 4843 validation tests passed with 5 skipped, and pre-push hook self-test
      plus negative and positive body-shape probes.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1470; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "4843 passed,
          5 skipped" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep
          -q "Pre-push hook" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body
          | grep -q "mypy --strict"'
  - id: dod-omnibase_core-pr-1470-deploy
    description: Deploy-readiness assessment for OMN-14682. The PR changes validation code, tests, and
      local pre-push hook wiring only; no deploy, restart, .201, Kafka, database, or production mutation
      was performed by this OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14682/dod-omnibase_core-pr-1470-deploy/command.yaml
          && grep -qi ''no deploy, restart, .201, Kafka, database, or production mutation'' drift/dod_receipts/OMN-14682/dod-omnibase_core-pr-1470-deploy/command.yaml'
