---
schema_version: 1.0.0
ticket_id: OMN-9058
title: Eliminate direct runtime env reads for LLM runtime vars
summary: LLM runtime endpoints and keys moved to typed runtime config slice.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - events
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Wave worker local verification evidence is captured in central OCC receipt.
    command: uv run pytest tests/unit/runtime/test_model_llm_runtime_config.py tests/unit/adapters/llm/test_plugin_llm_config_activation.py
      -q
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-wave-local-evidence
    description: Wave worker local verification or readiness evidence is captured as a canonical PASS
      receipt.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9058/dod-wave-local-evidence/command.yaml"
          && grep -q '^pr_number: 1661$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9058/dod-wave-local-evidence/command.yaml"
          && grep -q '^verifier: agent$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9058/dod-wave-local-evidence/command.yaml"
