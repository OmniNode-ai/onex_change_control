---
schema_version: 1.0.0
ticket_id: OMN-9058
title: Eliminate direct runtime env reads for LLM runtime vars
summary: LLM runtime endpoints and keys moved to typed runtime config slice.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - events
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Wave worker local verification evidence is captured in central OCC receipt.
    command: uv run pytest tests/unit/runtime/test_model_llm_runtime_config.py tests/unit/adapters/llm/test_plugin_llm_config_activation.py
      -q
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-wave-local-evidence
    description: Wave worker local verification or readiness evidence is captured as a canonical PASS
      receipt.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: uv run python -c "import os, yaml; from pathlib import Path; from omnibase_core.models.contracts.ticket.model_dod_receipt
          import ModelDodReceipt; p = Path(os.environ['CONTRACT_REPO_DIR']) / 'drift/dod_receipts/OMN-9058/dod-wave-local-evidence/command.yaml';
          r = ModelDodReceipt.model_validate(yaml.safe_load(p.read_text())); assert r.status == 'PASS';
          assert r.pr_number == int(os.environ['PR_NUMBER']); print('OMN-9058 OCC receipt PASS and PR-bound')"
