---
schema_version: "1.0.0"
ticket_id: OMN-7906
title: 'test(coverage): add tests for model_schema_dict (omnibase_core)'
summary: "test(coverage): add tests for model_schema_dict (omnibase_core)"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}"
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json mergedAt -q .mergedAt --repo ${REPO}"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --watch --repo ${REPO}"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-005"
    description: "Test coverage meets minimum threshold"
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest --cov --cov-fail-under=80"
  - id: "dod-006"
    description: "Contract/schema artifact file exists"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "contracts/OMN-7906.yaml"
  - id: "product-pr-1455"
    description: "Product PR #1455 on OmniNode-ai/omnibase_core adds focused unit coverage for model_schema_dict."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1455; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr diff \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --name-only | grep -q 'tests/unit/models/core/test_model_schema_dict.py'"
  - id: "dod-supersede-003-product-ci-1455"
    description: "Append-only supersession for the generated CI watch check; product PR #1455 has a green
      CI Summary."
    evidence_artifact: "supersedes_dod_evidence:dod-003"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1455; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr checks \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json name,bucket --jq 'map(select(.name == \"CI Summary\" and .bucket
          == \"pass\")) | length == 1' | grep -q true"
  - id: "dod-supersede-004-product-precommit-1455"
    description: "Append-only supersession for the generated all-files pre-commit check; product PR #1455
      has a green Precommit Parity Gate."
    evidence_artifact: "supersedes_dod_evidence:dod-004"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1455; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr checks \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json name,bucket --jq 'map(select(.name == \"Precommit Parity Gate\"
          and .bucket == \"pass\")) | length == 1' | grep -q true"
  - id: "dod-supersede-005-product-coverage-1455"
    description: "Append-only supersession for the generated coverage command; product PR #1455 has a
      green tests+coverage shadow check."
    evidence_artifact: "supersedes_dod_evidence:dod-005"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=1455; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr checks \"$PRODUCT_PR_NUMBER\"
          --repo \"$PRODUCT_REPO\" --json name,bucket --jq 'map(select(.name == \"tests+coverage (shadow)\"
          and .bucket == \"pass\")) | length == 1' | grep -q true"
  - id: "occ-self-bind-pr-4317"
    description: "OCC batch PR #4317 binds OMN-7906 evidence for OmniNode-ai/omnibase_core#1455."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4317; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state"
