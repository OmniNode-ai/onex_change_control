---
schema_version: "1.0.0"
ticket_id: "OMN-12753"
title: "Carry provider request options for useful delegation inference"
summary: >-
  Central OCC evidence for the paired omnibase_core PR #1206 and omnimarket PR #1074. The change lets
  protocol overlays provide provider-native request options, including Qwen chat_template_kwargs.enable_thinking=false,
  while preserving prompt evidence and keeping handler selection generic rather than endpoint-specific.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1206"
    description: "omnibase_core PR #1206 adds typed provider_request_options to ModelInferenceIntent."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12753/dod-omnibase-core-pr-1206/command.yaml
          && grep -q '^pr_number: 1206$' drift/dod_receipts/OMN-12753/dod-omnibase-core-pr-1206/command.yaml
          && grep -q 'c81347a6' drift/dod_receipts/OMN-12753/dod-omnibase-core-pr-1206/command.yaml
  - id: "dod-omnimarket-pr-1074"
    description: "omnimarket PR #1074 carries protocol overlay request options through direct-curl and
      event-bus inference."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12753/dod-omnimarket-pr-1074/command.yaml &&
          grep -q '^pr_number: 1074$' drift/dod_receipts/OMN-12753/dod-omnimarket-pr-1074/command.yaml
          && grep -q 'a80f7b92' drift/dod_receipts/OMN-12753/dod-omnimarket-pr-1074/command.yaml
  - id: "dod-useful-response-local-verification"
    description: "Focused local verification proves current-core compatibility and paired new-core useful-response
      behavior."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12753/dod-useful-response-local-verification/command.yaml
          && grep -q '21 passed' drift/dod_receipts/OMN-12753/dod-useful-response-local-verification/command.yaml
          && grep -q '20 passed' drift/dod_receipts/OMN-12753/dod-useful-response-local-verification/command.yaml
  - id: "dod-occ-evidence-pr-2245"
    description: "onex_change_control PR #2245 carries the central OCC evidence for OMN-12753."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12753/dod-occ-evidence-pr-2245/command.yaml
          && grep -q '^pr_number: 2245$' drift/dod_receipts/OMN-12753/dod-occ-evidence-pr-2245/command.yaml
