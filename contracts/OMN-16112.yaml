---
schema_version: "1.0.0"
ticket_id: "OMN-16112"
title: "OCC companion for OMN-16112"
summary: "Hand-authored OCC Evidence-Source companion for OmniNode-ai/omnimarket#2085 (mcp CVE-2026-59950
  bump). Autobind never minted a companion for this PR — its diff is a single-package uv.lock bump with
  no RED-derivable source file, which is outside the autobind writer's current derivation surface (tracked
  separately as OMN-16118). Authored by hand on the occ#6573/OMN-10221 manual-companion precedent per
  controller authorization."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2085 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2085"
    description: "PR #2085 on OmniNode-ai/omnimarket — content-bound probe confirming uv.lock pins mcp==1.29.0
      (CVE-2026-59950 fix) at the PR's head ref (OMN-15309 admissibility: reads product content at a pinned
      ref, not PR/issue metadata)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/contents/uv.lock?ref=2b4fac8076f5667c1ac44a3e87cdcac0c2efd51f
          --jq '.content' | base64 -d | grep -A2 '^name = \"mcp\"' | grep -q '^version = \"1.29.0\"'"
  - id: "dod-OmniNode-ai-omnimarket-pr-2085-ci"
    description: "PR #2085 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425), admissible
      form: reads the actual diff file list via gh api .../pulls/<n>/files, not PR metadata."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/pulls/2085/files --jq '[.[].filename]' | grep
          -qxF '[\"uv.lock\"]'"
  - id: "occ-self-bind-pr-6589"
    description: "OCC companion PR #6589 — self-bind for OMN-16112 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6589 --repo OmniNode-ai/onex_change_control --json number,state"
