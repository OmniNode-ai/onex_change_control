---
schema_version: "1.0.0"
ticket_id: "OMN-15029"
title: "Autobind OCC evidence for OMN-15029"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2436."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2436 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2436"
    description: "PR #2436 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2436-ci"
    description: "PR #2436 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-omnibase-infra-pr-2436-deploy-scope"
    description: "Deploy-gate scope for omnibase_infra PR #2436. The runtime handler path adds _route_sync_publisher_failure
      so failed fire-and-forget sync publisher payloads are routed to the existing duck-typed _publish_raw_to_dlq
      surface instead of being only logged. No live deploy, docker exec, or .201 mutation is authorized
      by this evidence repair."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=d2be06024541bd5fa3dfd1e90ce655570f10a0bd'
          --jq .content | base64 -d | grep -Fq '_route_sync_publisher_failure'"
  - id: "occ-self-bind-pr-4797"
    description: "OCC companion PR #4797 — self-bind for OMN-15029 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
