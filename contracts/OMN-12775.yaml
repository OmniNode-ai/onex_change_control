---
schema_version: "1.0.0"
ticket_id: "OMN-12775"
title: "Wave 2A: Dashboard thin publisher — node-generation-requested.v1"
summary: >-
  Adds SEA thin publisher to omnidash: COMMAND_TOPICS.nodeGenerationRequested → onex.cmd.omnimarket.node-generation-requested.v1
  (byte-matches node_generation_consumer subscribe_topics). Adds /api/sea/generate Express route that
  wraps user input (task_description) into the canonical command envelope — no business-payload truth.
  Wires ControlPlanePage "Run SEA" to the new route; removes the phantom /api/hackathon/generate client
  call (no server route existed). Disables file-mode client-side fabrication (simulated:true events, :68-104)
  — default is bus/projection.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
evidence_requirements:
  - kind: "tests"
    description: >-
      server/__tests__/dispatch.test.ts suite for /api/sea/generate: envelope → node-generation-requested.v1,
      payload = user input only (no business logic), /api/hackathon/generate returns 404. COMMAND_TOPICS.nodeGenerationRequested
      key registry test.
    command: "cd omnidash && ./node_modules/.bin/vitest run --reporter=verbose"
  - kind: "ci"
    description: >-
      All CI checks green on omnidash PR #192: Typecheck & Tests, contract-validation, golden-chain, gate/CodeRabbit
      Thread Check, pr-title/check-title.
    command: "gh pr checks 192 --repo OmniNode-ai/omnidash"
dod_evidence:
  - id: "dod-omnidash-pr-192"
    description: >-
      omnidash PR #192 adds COMMAND_TOPICS.nodeGenerationRequested, /api/sea/generate thin publisher route,
      removes file-mode fabrication from ControlPlanePage. All 1229 tests pass. All 20 CI checks green.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12775/dod-omnidash-pr-192/command.yaml && grep
          -q '^pr_number: 192$' drift/dod_receipts/OMN-12775/dod-omnidash-pr-192/command.yaml && grep
          -q '697c1ef' drift/dod_receipts/OMN-12775/dod-omnidash-pr-192/command.yaml
  - id: "dod-occ-evidence"
    description: "onex_change_control OCC contract + receipts for OMN-12775 (PR #2302)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12775/dod-occ-evidence/command.yaml && grep
          -q '^pr_number: 2302$' drift/dod_receipts/OMN-12775/dod-occ-evidence/command.yaml
  - id: "dod-omnimarket-pr-1113"
    description: >-
      Close-the-loop track A3 (omnimarket PR #1113): populate the six generation_events proof fields (output_payload_sha256,
      contract_sha256, handler_sha256, routing_source, resolved_endpoint, projection_owner) in the write
      path via the shared compute_generation_proof_fields helper used by both write paths; SHA256 fields
      are full-payload digests (no truncation); the benchmark carries routing_source + resolved_endpoint
      verbatim from the routing authority; the projection_api exposure lists the six columns (canonical
      read path). Migration 0013 adds the columns idempotently.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12775/dod-omnimarket-pr-1113/command.yaml &&
          grep -q '^pr_number: 1113$' drift/dod_receipts/OMN-12775/dod-omnimarket-pr-1113/command.yaml
          && grep -q 'aeb40e43' drift/dod_receipts/OMN-12775/dod-omnimarket-pr-1113/command.yaml
  - id: "dod-omnimarket-pr-1113-tests"
    description: >-
      omnimarket PR #1113 TDD proof-fields suite (14 tests) green; full suite green (6477 passed, kafka-broker
      integration tests deselected).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12775/dod-omnimarket-pr-1113-tests/command.yaml
          && grep -q '14 passed' drift/dod_receipts/OMN-12775/dod-omnimarket-pr-1113-tests/command.yaml
