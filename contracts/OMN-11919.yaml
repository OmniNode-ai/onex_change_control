---
schema_version: "1.0.0"
ticket_id: "OMN-11919"
title: "Antipattern match effect node"
summary: >
  Bind central evidence for omnimarket PR #826, which adds node_antipattern_match_effect for embedding-backed
  Qdrant antipattern similarity search and match response emission.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-826"
    description: "Bind OMN-11919 evidence to omnimarket PR #826."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11919/dod-omnimarket-pr-826/command.yaml
          && grep -q '^pr_number: 826$' drift/dod_receipts/OMN-11919/dod-omnimarket-pr-826/command.yaml"
  - id: "dod-antipattern-match-local-validation"
    description: "Focused antipattern match effect tests plus strict mypy pass for the current omnimarket
      PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11919/dod-antipattern-match-local-validation/command.yaml
          && grep -q '25 passed' drift/dod_receipts/OMN-11919/dod-antipattern-match-local-validation/command.yaml
          && grep -q 'Success: no issues found' drift/dod_receipts/OMN-11919/dod-antipattern-match-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: antipattern match effect declares effects runtime profile, vector
      and embedding dependencies, Kafka topics, and deploy readiness without requiring live runtime deploy
      before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11919/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11919/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11919 contract, deploy evidence, and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11919/dod-occ-pr/command.yaml &&
          grep -q '^pr_number: 0$' drift/dod_receipts/OMN-11919/dod-occ-pr/command.yaml"
