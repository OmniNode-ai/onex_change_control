---
schema_version: "1.0.0"
ticket_id: OMN-9437
title: "Dispatch queue drainer compile-only slice"
summary: "Add a compile-only node_dispatch_queue_drainer that validates one legacy dispatch_queue YAML
  item, compiles it through node_dispatch_worker, and writes a terminal result artifact without spawning
  agents or moving queue files."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #445 checks pass"
    command: "gh pr checks 445 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "Focused dispatch queue drainer tests pass"
    command: "env -u PYTHONPATH uv run pytest tests/unit/nodes/node_dispatch_queue_drainer -v"
  - kind: "manual"
    description: "Post-merge runtime readiness smoke imports the compile-only drainer path"
    command: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_dispatch_queue_drainer.handlers.handler_dispatch_queue_drainer
      import HandlerDispatchQueueDrainer; from omnimarket.nodes.node_dispatch_queue_drainer.__main__ import
      main; print('dispatch queue drainer deploy readiness smoke ok')\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused compile-only drainer unit and CLI tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run pytest tests/unit/nodes/node_dispatch_queue_drainer -v"
  - id: "dod-002"
    description: "Drainer node drift, lint, and strict package typecheck pass locally"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/validate_node_drift.py --check-changed origin/main --strict
          && env -u PYTHONPATH uv run ruff check src/omnimarket/nodes/node_dispatch_queue_drainer tests/unit/nodes/node_dispatch_queue_drainer
          pyproject.toml && env -u PYTHONPATH uv run mypy src/omnimarket/nodes/node_dispatch_queue_drainer
          --strict"
  - id: "dod-003"
    description: "Post-merge deploy/readiness smoke plan only; this compile-only PR has not deployed,
      restarted runtime, mutated .201, spawned agents, or drained live queue files"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_dispatch_queue_drainer.handlers.handler_dispatch_queue_drainer
          import HandlerDispatchQueueDrainer; from omnimarket.nodes.node_dispatch_queue_drainer.__main__
          import main; print('dispatch queue drainer deploy readiness smoke ok')\""
