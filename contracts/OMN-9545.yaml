---
schema_version: "1.0.0"
ticket_id: "OMN-9545"
title: "Remove dead instance-discovery fields from registry API"
summary: >
  Rebase and validate omnibase_infra PR #1777, which removes permanently-empty Consul-era registry instance-discovery
  fields, deletes the deprecated list-instances response model, and removes the registry instances route
  and capability wiring.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #1777 carries the OMN-9545 registry API cleanup branch at the recorded
      head."
    command: "gh pr view 1777 --repo OmniNode-ai/omnibase_infra --json number,url,headRefOid,headRefName,title,state,baseRefName"
  - kind: "tests"
    description: "Focused registry API unit tests, formatting, lint, and type checks pass against the
      PR worktree."
    command: "uv run ruff format --check src/ tests/ && uv run ruff check src/ tests/ && uv run mypy src/omnibase_infra
      && uv run python -m pytest tests/unit/nodes/test_handler_registry_api_operational.py tests/unit/nodes/test_registry_api_effect_handler_stubs.py
      tests/unit/nodes/node_registry_api_effect/test_node_registry_api_effect.py tests/unit/services/registry_api/test_registry_discovery_semantics.py
      -q"
  - kind: "manual"
    description: "Deploy impact is reviewed for the registry API route and contract surface removal."
    command: "deploy impact review for omnibase_infra PR #1777"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1777-local-verification"
    description: "omnibase_infra PR #1777 registry API cleanup passes focused local verification at the
      recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9545/dod-infra-pr-1777-local-verification/command.yaml"
          && grep -q '^commit_sha: "f554c58d950cc30f09f2668e07dad0c7f84984e2"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9545/dod-infra-pr-1777-local-verification/command.yaml"
          && grep -q '3954 files already formatted' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9545/dod-infra-pr-1777-local-verification/command.yaml"
          && grep -q 'All checks passed!' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9545/dod-infra-pr-1777-local-verification/command.yaml"
          && grep -q 'Success: no issues found in 2371 source files' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9545/dod-infra-pr-1777-local-verification/command.yaml"
          && grep -q '40 passed, 1 skipped' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9545/dod-infra-pr-1777-local-verification/command.yaml"
  - id: "dod-infra-pr-1777-deploy-impact"
    description: "omnibase_infra PR #1777 registry API surface removal is deploy-reviewed with focused
      tests covering the removed route, contract, and discovery response semantics."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9545/dod-infra-pr-1777-deploy-impact/command.yaml"
          && grep -q '^commit_sha: "f554c58d950cc30f09f2668e07dad0c7f84984e2"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9545/dod-infra-pr-1777-deploy-impact/command.yaml"
          && grep -q 'deploy impact review passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9545/dod-infra-pr-1777-deploy-impact/command.yaml"
          && echo 'deploy impact review passed'
