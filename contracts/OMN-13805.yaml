---
schema_version: "1.0.0"
ticket_id: "OMN-13805"
title: "fix(OMN-13805): re-sync vendored omnimarket node migrations"
summary: >-
  omnibase_infra PR #2179 re-syncs the vendored omnimarket node-owned projection migrations under docker/migrations/forward/nodes/
  to match omnimarket origin/dev. The only drift was node_projection_skill_executions/0001_create_skill_execution_snapshots.sql
  (shipped by omnimarket OMN-13839), which had not yet been vendored into infra. The missing vendor left
  the always_run onex-check-node-migration-sync pre-commit hook and the repo-wide node-migration-sync
  CI gate RED for every infra PR, wedging the merge queue. The change is a pure 1:1 SQL vendor produced
  by scripts/sync-node-migrations.sh; the vendored file is byte-identical to its omnimarket origin/dev
  source. No Python or runtime code changes; no live deploy.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2179"
    description: >-
      Consuming-PR binding: omnibase_infra PR #2179 vendors the omnimarket origin/dev skill_executions
      node migration into docker/migrations/forward/nodes/ at the recorded head commit, changing only
      that vendored SQL file.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13805/dod-infra-pr-2179/command.yaml"
  - id: "dod-vendor-sync"
    description: >-
      Local validation: scripts/sync-node-migrations.sh --check exits 0 (vendor tree in sync with omnimarket
      origin/dev), the vendored file byte-matches its source, and the onex-check-node-migration-sync pre-commit
      hook plus all file-scoped hooks pass.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13805/dod-vendor-sync/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy disposition for the vendored SQL migration: no live deploy is performed by this PR or its
      CI. The vendored migration is applied only at a future deploy-time forward-migration run under a
      namespaced migration_id; that is an explicit operator deploy action, not part of this PR.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13805/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      This onex_change_control PR carries the central OMN-13805 contract and PASS receipts consumed by
      omnibase_infra PR #2179 through OCC preflight and Receipt Gate.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13805/dod-occ-self/command.yaml"
