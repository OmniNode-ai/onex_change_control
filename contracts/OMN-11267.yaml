---
schema_version: "1.0.0"
ticket_id: "OMN-11267"
title: "Add service catalog probe for runtime health"
summary: >
  Adds a reusable read-only service catalog probe module in omnimarket for .201 service health snapshots.
  The probe runs each service independently, records timestamped results, and avoids mutating the runtime
  pre-merge.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused service catalog tests pass in omnimarket PR #719."
    command: "uv run pytest tests/test_service_catalog.py -q"
  - kind: "ci"
    description: "Leaked literal gate passes with ticketed .201 test fixture annotations."
    command: "bash scripts/validation/check_leaked_literals.sh blocking all"
  - kind: "ci"
    description: "omnimarket PR #719 is bound to this ticket at the observed head."
    command: "gh pr view 719 --repo OmniNode-ai/omnimarket --json headRefOid,title"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused service catalog tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11267/dod-001/command.yaml\"; else uv run pytest
          tests/test_service_catalog.py -q; fi"
  - id: "dod-002"
    description: "Leaked literal gate passes with ticketed .201 test fixture annotations."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11267/dod-002/command.yaml\"; else bash scripts/validation/check_leaked_literals.sh
          blocking all; fi"
  - id: "dod-003"
    description: "Bind OMN-11267 evidence to omnimarket PR #719 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,title"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-11267 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11267/dod-occ-pr/command.yaml\""
