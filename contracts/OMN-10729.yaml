---
schema_version: "1.0.0"
ticket_id: "OMN-10729"
title: "Add canary catalog service for ADR runtime profile"
summary: >
  Adds docker/catalog/services/runtime-canary.yaml and canary bundle to omnibase_infra so that RUNTIME_PROFILE=canary
  is an actual running container (port 8088, group onex-runtime-canary) rather than paper-only. Binds
  to omnibase_infra PR #1539.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1539"
    description: "omnibase_infra PR #1539 carries runtime-canary.yaml + canary bundle + 10 unit tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10729/dod-infra-pr-1539/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10729/dod-occ-pr/command.yaml\""
