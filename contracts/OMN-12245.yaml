---
schema_version: 1.0.0
ticket_id: OMN-12245
title: May 25 integration release promotion
summary: 'OCC evidence for the May 25 release promotion wave. The downstream PRs promote current dev integration
  heads to main across the OmniNode release set and are bound individually by PR number and release branch
  head SHA. #magic___^_^___line '
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-release-omnibase-compat-pr-112
    description: 'omnibase_compat release PR #112 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-compat-pr-112/command.yaml'
  - id: dod-release-omnibase-core-pr-1168
    description: 'omnibase_core release PR #1168 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-core-pr-1168/command.yaml'
  - id: dod-release-omnibase-infra-pr-1758
    description: 'omnibase_infra release PR #1758 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-infra-pr-1758/command.yaml'
  - id: dod-release-omnibase-spi-pr-227
    description: 'omnibase_spi release PR #227 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-spi-pr-227/command.yaml'
  - id: dod-release-omniclaude-pr-1674
    description: 'omniclaude release PR #1674 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omniclaude-pr-1674/command.yaml'
  - id: dod-release-omnidash-pr-134
    description: 'omnidash release PR #134 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnidash-pr-134/command.yaml'
  - id: dod-release-omniintelligence-pr-680
    description: 'omniintelligence release PR #680 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omniintelligence-pr-680/command.yaml'
  - id: dod-release-omnimarket-pr-922
    description: 'omnimarket release PR #922 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnimarket-pr-922/command.yaml'
  - id: dod-release-omnimemory-pr-336
    description: 'omnimemory release PR #336 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnimemory-pr-336/command.yaml'
  - id: dod-release-omninode-infra-pr-474
    description: 'omninode_infra release PR #474 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omninode-infra-pr-474/command.yaml'
  - id: dod-release-omnibase-compat-pr-118
    description: 'omnibase_compat replacement release PR #118 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-compat-pr-118/command.yaml'
  - id: dod-release-omnibase-compat-pr-121
    description: 'omnibase_compat hotfix PR #121 releases delegation escalation DTOs required by the OMN-12245
      runtime proof.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-compat-pr-121/command.yaml'
  - id: dod-release-omnibase-core-pr-1170
    description: 'omnibase_core replacement release PR #1170 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-core-pr-1170/command.yaml'
  - id: dod-release-omnibase-core-pr-1174
    description: 'omnibase_core PR #1174 refreshes the onex_change_control boundary action pin needed
      by the OMN-12245 release gates.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-core-pr-1174/command.yaml'
  - id: dod-release-omnibase-infra-pr-1760
    description: 'omnibase_infra replacement release PR #1760 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-infra-pr-1760/command.yaml'
  - id: dod-release-omnibase-infra-pr-1772
    description: 'omnibase_infra hotfix release PR #1772 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-infra-pr-1772/command.yaml'
  - id: dod-release-omnibase-infra-pr-1773
    description: 'omnibase_infra hotfix PR #1773 routes ModelEventEnvelope projection topics required
      by the OMN-12245 post-release runtime proof.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-infra-pr-1773/command.yaml'
  - id: dod-release-omnibase-infra-pr-1774
    description: 'omnibase_infra hotfix PR #1774 registers full ONEX dispatch topic keys required by the
      OMN-12245 post-release runtime proof.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-infra-pr-1774/command.yaml'
  - id: dod-release-omnibase-infra-pr-1775
    description: 'omnibase_infra hotfix PR #1775 dispatches projection handlers without event-loop contention
      for the OMN-12245 post-release runtime proof.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-infra-pr-1775/command.yaml'
  - id: dod-release-omnibase-spi-pr-228
    description: 'omnibase_spi replacement release PR #228 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-spi-pr-228/command.yaml'
  - id: dod-release-omniclaude-pr-1681
    description: 'omniclaude replacement release PR #1681 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omniclaude-pr-1681/command.yaml'
  - id: dod-release-omnidash-pr-135
    description: 'omnidash replacement release PR #135 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnidash-pr-135/command.yaml'
  - id: dod-release-omniintelligence-pr-681
    description: 'omniintelligence replacement release PR #681 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omniintelligence-pr-681/command.yaml'
  - id: dod-release-omnimarket-pr-926
    description: 'omnimarket replacement release PR #926 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnimarket-pr-926/command.yaml'
  - id: dod-release-omnimarket-pr-932
    description: 'omnimarket hotfix PR #932 releases the runtime proof fixes required by the OMN-12245
      post-release proof.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnimarket-pr-932/command.yaml'
  - id: dod-release-omnimarket-pr-933
    description: 'omnimarket hotfix PR #933 projects delegation quality-gate counts required by the OMN-12245
      post-release runtime proof.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnimarket-pr-933/command.yaml'
  - id: dod-release-omnimemory-pr-337
    description: 'omnimemory replacement release PR #337 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnimemory-pr-337/command.yaml'
  - id: dod-release-omninode-infra-pr-475
    description: 'omninode_infra replacement release PR #475 is mergeable release evidence for OMN-12245.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omninode-infra-pr-475/command.yaml'
  - id: dod-occ-promotion-pr-1712
    description: 'OCC main-promotion PR #1712 carries OMN-12245 release evidence onto main.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title
  - id: dod-occ-rerun-state-pr-1713
    description: 'OCC rerun-state PR #1713 is bound to OMN-12245 release evidence cleanup.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title
  - id: dod-occ-entrypoint-hotfix-pr-1733
    description: 'OCC hotfix PR #1733 removes stale onex.nodes entry points so OMN-12245 release validation
      can install onex-change-control cleanly.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-infra-1772-evidence-pr-1735
    description: 'OCC hotfix PR #1735 carries refreshed OMN-12245 evidence for omnibase_infra PR #1772.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-omnimarket-926-evidence-pr-1738
    description: 'OCC hotfix PR #1738 carries refreshed OMN-12245 evidence for omnimarket PR #926.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-omnimarket-926-final-head-pr-1742
    description: 'OCC hotfix PR #1742 carries refreshed OMN-12245 evidence for omnimarket PR #926 head
      bf4a5269.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-omniintelligence-681-gates-pr-1746
    description: 'OCC hotfix PR #1746 carries refreshed OMN-12245 evidence and guard allowlist for omniintelligence
      PR #681.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-omniintelligence-681-final-head-pr-1748
    description: 'OCC hotfix PR #1748 carries refreshed OMN-12245 evidence for omniintelligence PR #681
      head f70613a.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-omniclaude-1681-final-head-pr-1750
    description: 'OCC hotfix PR #1750 carries refreshed OMN-12245 evidence for omniclaude PR #1681 head
      8393eff.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-omniclaude-1681-workflow-head-pr-1752
    description: 'OCC hotfix PR #1752 carries refreshed OMN-12245 evidence for omniclaude PR #1681 head
      244ce65.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-omniclaude-1681-ci-head-pr-1754
    description: 'OCC hotfix PR #1754 carries refreshed OMN-12245 evidence for omniclaude PR #1681 head
      69cb4ca.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-omniclaude-1681-dod-source-pr-1756
    description: 'OCC hotfix PR #1756 carries refreshed OMN-12245 evidence for omniclaude PR #1681 head
      1aeb5df.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-core-1174-evidence-pr-1770
    description: 'OCC hotfix PR #1770 carries OMN-12245 receipt evidence for omnibase_core PR #1174.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-compat-runtime-dtos-pr-1773
    description: 'OCC hotfix PR #1773 carries OMN-12245 receipt evidence for omnibase_compat PR #121.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-omnimarket-932-evidence-pr-1775
    description: 'OCC hotfix PR #1775 carries OMN-12245 receipt evidence for omnimarket PR #932.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-infra-1773-evidence-pr-1777
    description: 'OCC hotfix PR #1777 carries OMN-12245 receipt evidence for omnibase_infra PR #1773.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-infra-1773-head-refresh-pr-1779
    description: 'OCC hotfix PR #1779 refreshes OMN-12245 receipt evidence for omnibase_infra PR #1773
      head 48f260a.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-infra-1774-evidence-pr-1781
    description: 'OCC hotfix PR #1781 carries refreshed OMN-12245 evidence for omnibase_infra PR #1774
      head ab25812.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-infra-1774-head-refresh-pr-1783
    description: 'OCC hotfix PR #1783 refreshes OMN-12245 evidence for omnibase_infra PR #1774 retry head
      e9f9192.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-infra-1774-handler-scope-pr-1785
    description: 'OCC hotfix PR #1785 refreshes OMN-12245 evidence for omnibase_infra PR #1774 handler-scope
      head 9f97f3b.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-runtime-projection-evidence-pr-1787
    description: 'OCC hotfix PR #1787 binds OMN-12245 projection runtime evidence for omnibase_infra PR
      #1775 and omnimarket PR #933.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-omnimarket-guard-main-pr-1791
    description: 'OCC hotfix PR #1791 carries the omnimarket imperative-contract guard allowlist needed
      by omnimarket PR #934.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-release-wave-deploy-gate
    description: Release wave runtime deploy evidence is present for OMN-12245 runtime-touching PRs.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'printf ''%s #magic___^_^___line '' deploy-gate-evidence-for-OMN-12245'
  - id: dod-release-omnibase-infra-dev-backmerge-pr-1776
    description: 'omnibase_infra dev backmerge PR #1776 carries the projection runtime dispatch hotfix
      back to dev.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnibase-infra-dev-backmerge-pr-1776/command.yaml'
  - id: dod-release-omnimarket-dev-backmerge-pr-934
    description: 'omnimarket dev backmerge PR #934 carries the delegation projection count hotfix back
      to dev.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-release-omnimarket-dev-backmerge-pr-934/command.yaml'
  - id: dod-occ-runtime-projection-backmerge-pr-1788
    description: 'OCC dev backmerge PR #1788 binds the OMN-12245 projection runtime evidence hotfix back
      to dev.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-dev-backmerge-bindings-pr-1790
    description: 'OCC dev evidence PR #1790 binds the OMN-12245 infra and market dev backmerge PRs.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,state,title,url
  - id: dod-occ-main-release-20260606-pr-2225
    description: 'OCC main release PR #2225 binds the June 6 OMN-12245 promotion evidence cleanup to main.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,baseRefName,state,title,url
  - id: dod-occ-main-release-market-pr-1068
    description: 'omnimarket main release PR #1068 binds the June 6 OMN-12245 promotion branch to main.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,baseRefName,state,title,url
  - id: dod-occ-main-release-core-pr-1205
    description: 'omnibase_core main release PR #1205 binds the June 6 OMN-12245 promotion branch to main.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,baseRefName,state,title,url
  - id: dod-occ-main-release-infra-pr-1885
    description: 'omnibase_infra main release PR #1885 binds the June 6 OMN-12245 promotion branch to
      main.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,baseRefName,state,title,url
  - id: dod-occ-main-release-occ-pr-2226
    description: 'onex_change_control main release PR #2226 binds the June 6 OMN-12245 evidence cleanup
      branch to main.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json headRefOid,baseRefName,state,title,url
  - id: dod-occ-release-market-v043-pr-1069
    description: 'omnimarket release-version PR #1069 bumps current main release metadata to v0.4.3.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-occ-release-market-v043-pr-1069/command.yaml'
  - id: dod-occ-release-infra-v0381-pr-1886
    description: 'omnibase_infra release-version PR #1886 bumps current main release metadata to v0.38.1.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-occ-release-infra-v0381-pr-1886/command.yaml'
  - id: dod-occ-release-occ-v051-pr-2228
    description: 'onex_change_control release-version PR #2228 bumps current main release metadata to
      v0.5.1.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-occ-release-occ-v051-pr-2228/command.yaml'
  - id: dod-occ-release-dash-v111-pr-175
    description: 'omnidash release-version PR #175 bumps current main release metadata to v1.1.1.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-occ-release-dash-v111-pr-175/command.yaml'
  - id: dod-occ-release-compat-v052-pr-135
    description: 'omnibase_compat release-version PR #135 bumps current main release metadata to v0.5.2.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-occ-release-compat-v052-pr-135/command.yaml'
  - id: dod-occ-main-version-evidence-pr-2229
    description: 'OCC PR #2229 carries the OMN-12245 main release-version receipt evidence.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-occ-main-version-evidence-pr-2229/command.yaml'
  - id: dod-omninode-infra-phase5-pr-541
    description: 'omninode_infra PR #541 carries the OMN-12245 phase-5 infrastructure mini-task branch
      after dev merge conflict resolution.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-omninode-infra-phase5-pr-541/command.yaml'
  - id: dod-occ-omninode-infra-541-pr-3441
    description: 'OCC PR #3441 carries the OMN-12245 receipt binding for omninode_infra PR #541.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-12245/dod-occ-omninode-infra-541-pr-3441/command.yaml'
  - id: "occ-promotion-pr-1712-r15481"
    description: "Append-only replacement for dod-occ-promotion-pr-1712. The historical entry is immutable
      and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner placeholders,
      which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never pinned PR
      #1712 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion exits 0 for
      any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and repo
      and asserts two facts the selector does not determine: the PR is MERGED at c3d38e71fb17dc71380be2aeeb204994c885424a,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-promotion-pr-1712"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1712 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'c3d38e71fb17dc71380be2aeeb204994c885424a' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1712 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-rerun-state-pr-1713-r15481"
    description: "Append-only replacement for dod-occ-rerun-state-pr-1713. The historical entry is immutable
      and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner placeholders,
      which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never pinned PR
      #1713 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion exits 0 for
      any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and repo
      and asserts two facts the selector does not determine: the PR is MERGED at 96b4c08136fc7cc0f0d8d61da1f08f1b378ddd9e,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-rerun-state-pr-1713"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1713 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '96b4c08136fc7cc0f0d8d61da1f08f1b378ddd9e' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1713 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-entrypoint-hotfix-pr-1733-r15481"
    description: "Append-only replacement for dod-occ-entrypoint-hotfix-pr-1733. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1733 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion exits
      0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at ad054ca0bdeb7c9c7031755e5e4ec5333d7c863f,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-entrypoint-hotfix-pr-1733"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1733 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'ad054ca0bdeb7c9c7031755e5e4ec5333d7c863f' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1733 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-infra-1772-evidence-pr-1735-r15481"
    description: "Append-only replacement for dod-occ-infra-1772-evidence-pr-1735. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1735 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion exits
      0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at 848b5b783cabca4794b83cf57521750a2280a444,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-infra-1772-evidence-pr-1735"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1735 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '848b5b783cabca4794b83cf57521750a2280a444' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1735 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-omnimarket-926-evidence-pr-1738-r15481"
    description: "Append-only replacement for dod-occ-omnimarket-926-evidence-pr-1738. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1738 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at b78e93668f3cff39cd1dce4e8b54eb82ef9b394e,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-omnimarket-926-evidence-pr-1738"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1738 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'b78e93668f3cff39cd1dce4e8b54eb82ef9b394e' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1738 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-omnimarket-926-final-head-pr-1742-r15481"
    description: "Append-only replacement for dod-occ-omnimarket-926-final-head-pr-1742. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1742 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at fa97e51064e2df6bef66e98949e1e7f14b47f4a0,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-omnimarket-926-final-head-pr-1742"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1742 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'fa97e51064e2df6bef66e98949e1e7f14b47f4a0' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1742 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-omniintelligence-681-gates-pr-1746-r15481"
    description: "Append-only replacement for dod-occ-omniintelligence-681-gates-pr-1746. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1746 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at fb44f9f590a435671a8f31c36072022f89239664,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-omniintelligence-681-gates-pr-1746"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1746 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'fb44f9f590a435671a8f31c36072022f89239664' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1746 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-omniintelligence-681-final-head-pr-1748-r15481"
    description: "Append-only replacement for dod-occ-omniintelligence-681-final-head-pr-1748. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1748 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at 5ecfa8b7da28e9b9755b11172ece79a18062ec04,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-omniintelligence-681-final-head-pr-1748"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1748 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '5ecfa8b7da28e9b9755b11172ece79a18062ec04' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1748 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-omniclaude-1681-final-head-pr-1750-r15481"
    description: "Append-only replacement for dod-occ-omniclaude-1681-final-head-pr-1750. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1750 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at 89ba53e57d72d68f32cc7989100c593837f276f9,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-omniclaude-1681-final-head-pr-1750"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1750 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '89ba53e57d72d68f32cc7989100c593837f276f9' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1750 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-omniclaude-1681-workflow-head-pr-1752-r15481"
    description: "Append-only replacement for dod-occ-omniclaude-1681-workflow-head-pr-1752. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1752 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at 0479254ed74dfcc12a0753ec45e2e5d81a6f22ab,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-omniclaude-1681-workflow-head-pr-1752"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1752 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '0479254ed74dfcc12a0753ec45e2e5d81a6f22ab' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1752 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-omniclaude-1681-ci-head-pr-1754-r15481"
    description: "Append-only replacement for dod-occ-omniclaude-1681-ci-head-pr-1754. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1754 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at ef683a8eed0ba37f0f600edc82db176c60a6924b,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-omniclaude-1681-ci-head-pr-1754"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1754 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'ef683a8eed0ba37f0f600edc82db176c60a6924b' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1754 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-omniclaude-1681-dod-source-pr-1756-r15481"
    description: "Append-only replacement for dod-occ-omniclaude-1681-dod-source-pr-1756. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1756 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at 3adbc0e77d49839b24c1a92780b661c1f00ee401,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-omniclaude-1681-dod-source-pr-1756"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1756 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '3adbc0e77d49839b24c1a92780b661c1f00ee401' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1756 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-core-1174-evidence-pr-1770-r15481"
    description: "Append-only replacement for dod-occ-core-1174-evidence-pr-1770. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1770 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion exits
      0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at 6f38fcc3c4cf11c61e06f946ea964f824fd08fc2,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-core-1174-evidence-pr-1770"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1770 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '6f38fcc3c4cf11c61e06f946ea964f824fd08fc2' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1770 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-compat-runtime-dtos-pr-1773-r15481"
    description: "Append-only replacement for dod-occ-compat-runtime-dtos-pr-1773. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1773 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion exits
      0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at d5752cc9a4907a56faf93a8ca525d993bc598aae,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-compat-runtime-dtos-pr-1773"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1773 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'd5752cc9a4907a56faf93a8ca525d993bc598aae' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1773 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-omnimarket-932-evidence-pr-1775-r15481"
    description: "Append-only replacement for dod-occ-omnimarket-932-evidence-pr-1775. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1775 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at e69b5045b67a4ffb794b9c4a654f95ddb291cded,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-omnimarket-932-evidence-pr-1775"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1775 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'e69b5045b67a4ffb794b9c4a654f95ddb291cded' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1775 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-infra-1773-evidence-pr-1777-r15481"
    description: "Append-only replacement for dod-occ-infra-1773-evidence-pr-1777. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1777 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion exits
      0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at aed980bbc020836d2bba3985f137d166b484b4e9,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-infra-1773-evidence-pr-1777"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1777 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'aed980bbc020836d2bba3985f137d166b484b4e9' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1777 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-infra-1773-head-refresh-pr-1779-r15481"
    description: "Append-only replacement for dod-occ-infra-1773-head-refresh-pr-1779. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1779 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at 308a4d09c88b9a328ae5ec24042c336604fe8d0c,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-infra-1773-head-refresh-pr-1779"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1779 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '308a4d09c88b9a328ae5ec24042c336604fe8d0c' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1779 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-infra-1774-evidence-pr-1781-r15481"
    description: "Append-only replacement for dod-occ-infra-1774-evidence-pr-1781. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1781 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion exits
      0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at 4a8e541a8423c22a7b9817083d474a625aeefcce,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-infra-1774-evidence-pr-1781"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1781 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '4a8e541a8423c22a7b9817083d474a625aeefcce' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1781 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-infra-1774-head-refresh-pr-1783-r15481"
    description: "Append-only replacement for dod-occ-infra-1774-head-refresh-pr-1783. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1783 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at 7cb88b70b93b2e690683fa2da5d645291e3864e6,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-infra-1774-head-refresh-pr-1783"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1783 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '7cb88b70b93b2e690683fa2da5d645291e3864e6' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1783 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-infra-1774-handler-scope-pr-1785-r15481"
    description: "Append-only replacement for dod-occ-infra-1774-handler-scope-pr-1785. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1785 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at ab913d0252be7d30a73dea9d7e2f19683b64474d,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-infra-1774-handler-scope-pr-1785"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1785 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'ab913d0252be7d30a73dea9d7e2f19683b64474d' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1785 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-runtime-projection-evidence-pr-1787-r15481"
    description: "Append-only replacement for dod-occ-runtime-projection-evidence-pr-1787. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1787 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at 055f8e6d281dafc7f4675995e34d9aa2f6a54473,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-runtime-projection-evidence-pr-1787"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1787 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '055f8e6d281dafc7f4675995e34d9aa2f6a54473' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1787 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-omnimarket-guard-main-pr-1791-r15481"
    description: "Append-only replacement for dod-occ-omnimarket-guard-main-pr-1791. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1791 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion exits
      0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at 2741a4096e2eaa48ce516a374b9765beda908073,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-omnimarket-guard-main-pr-1791"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1791 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '2741a4096e2eaa48ce516a374b9765beda908073' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1791 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-runtime-projection-backmerge-pr-1788-r15481"
    description: "Append-only replacement for dod-occ-runtime-projection-backmerge-pr-1788. The historical
      entry is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO}
      runner placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the
      item never pinned PR #1788 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion
      exits 0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number
      and repo and asserts two facts the selector does not determine: the PR is MERGED at 89b09dab7b121b3b3bcc91302233a802a83a49bc,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-runtime-projection-backmerge-pr-1788"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1788 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '89b09dab7b121b3b3bcc91302233a802a83a49bc' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1788 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-dev-backmerge-bindings-pr-1790-r15481"
    description: "Append-only replacement for dod-occ-dev-backmerge-bindings-pr-1790. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1790 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion exits
      0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at 3eb3dd13eef7eb132b438a67e6f5cd6ce8305d80,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-dev-backmerge-bindings-pr-1790"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1790 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '3eb3dd13eef7eb132b438a67e6f5cd6ce8305d80' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1790 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
          | grep -cxF 'contracts/OMN-12245.yaml' | grep -qx 1"
  - id: "occ-main-release-20260606-pr-2225-r15481"
    description: "Append-only replacement for dod-occ-main-release-20260606-pr-2225. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #2225 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion exits
      0 for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is CLOSED (never merged), plus
      a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at 2026-07-30T09:32:56Z
      and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-main-release-20260606-pr-2225"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2225 --repo OmniNode-ai/onex_change_control --json state,mergedAt --jq
          '.state + \"/\" + (.mergedAt // \"never\")' | grep -cxF 'CLOSED/never' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 2225 --repo OmniNode-ai/onex_change_control --json baseRefName --jq '.baseRefName'
          | grep -cxF 'main' | grep -qx 1"
  - id: "occ-main-release-market-pr-1068-r15481"
    description: "Append-only replacement for dod-occ-main-release-market-pr-1068. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1068 on OmniNode-ai/omnimarket, and `gh pr view --json` with no assertion exits 0 for
      any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and repo
      and asserts two facts the selector does not determine: the PR is MERGED at 3dc5139bccfdaa433e0da9c6229671133f282551,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-main-release-market-pr-1068"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1068 --repo OmniNode-ai/omnimarket --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '3dc5139bccfdaa433e0da9c6229671133f282551' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1068 --repo OmniNode-ai/omnimarket --json baseRefName --jq '.baseRefName'
          | grep -cxF 'main' | grep -qx 1"
  - id: "occ-main-release-core-pr-1205-r15481"
    description: "Append-only replacement for dod-occ-main-release-core-pr-1205. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1205 on OmniNode-ai/omnibase_core, and `gh pr view --json` with no assertion exits 0
      for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at bdfc88701eb3c3391b85c230da6c6812c54c729b,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-main-release-core-pr-1205"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1205 --repo OmniNode-ai/omnibase_core --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF 'bdfc88701eb3c3391b85c230da6c6812c54c729b' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1205 --repo OmniNode-ai/omnibase_core --json baseRefName --jq '.baseRefName'
          | grep -cxF 'main' | grep -qx 1"
  - id: "occ-main-release-infra-pr-1885-r15481"
    description: "Append-only replacement for dod-occ-main-release-infra-pr-1885. The historical entry
      is immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner
      placeholders, which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never
      pinned PR #1885 on OmniNode-ai/omnibase_infra, and `gh pr view --json` with no assertion exits 0
      for any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and
      repo and asserts two facts the selector does not determine: the PR is MERGED at 6e5718e34d37caf566a378047ec371c6ad7be1ee,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-main-release-infra-pr-1885"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1885 --repo OmniNode-ai/omnibase_infra --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '6e5718e34d37caf566a378047ec371c6ad7be1ee' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 1885 --repo OmniNode-ai/omnibase_infra --json baseRefName --jq '.baseRefName'
          | grep -cxF 'main' | grep -qx 1"
  - id: "occ-main-release-occ-pr-2226-r15481"
    description: "Append-only replacement for dod-occ-main-release-occ-pr-2226. The historical entry is
      immutable and stays byte-intact, but its check_value used the bare ${PR_NUMBER}/${REPO} runner placeholders,
      which the compliance runner pre-substitutes with ITS OWN pr/repo -- so the item never pinned PR
      #2226 on OmniNode-ai/onex_change_control, and `gh pr view --json` with no assertion exits 0 for
      any PR that exists (mis-bound AND unfalsifiable). This entry pins the literal PR number and repo
      and asserts two facts the selector does not determine: the PR is MERGED at 764d76b521d7c44e52fca87b95c13ebce633efbb,
      plus a per-item distinguishing binding. Both checks executed GREEN under `bash -o pipefail -c` at
      2026-07-30T09:32:56Z and are RED-proven by mutation (OMN-15481 batch 1)."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-main-release-occ-pr-2226"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2226 --repo OmniNode-ai/onex_change_control --json mergeCommit --jq '.mergeCommit.oid'
          | grep -cxF '764d76b521d7c44e52fca87b95c13ebce633efbb' | grep -qx 1"
      - check_type: "command"
        check_value: "gh pr view 2226 --repo OmniNode-ai/onex_change_control --json baseRefName --jq '.baseRefName'
          | grep -cxF 'main' | grep -qx 1"
