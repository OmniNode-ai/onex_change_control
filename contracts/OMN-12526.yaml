---
schema_version: "1.0.0"
ticket_id: "OMN-12526"
title: "Delete dead EnvelopeRouter scaffold in omnibase_core"
summary: >-
  Binds central OCC evidence for omnibase_core PR #1186, which deletes the dead EnvelopeRouter dispatch
  scaffold and its exclusively-dead supporting cluster (ProtocolNodeRuntime, ModelRuntimeNodeInstance,
  the divergent ProtocolHandler, TypedDictRoutingInfo, and the dev/test HandlerLocal echo handler). The
  Phase 1 dispatch parity proof (docs/audits/2026-05-30-phase1-dispatch-parity-proof.md) confirmed EnvelopeRouter
  is dead: the live dispatch path is the infra MessageDispatchEngine + RuntimeLocal. The PR touches src/omnibase_core/runtime
  paths but is pure dead-code removal, so no live deploy, runtime restart, docker exec, or rpk topic produce
  is required before merge. DLQ vocabulary (EnumDispatchLifecycleState.DLQ, topics.py DLQ entries) and
  EnumHandlerType are preserved.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Full pytest collection is clean (no import/collection errors after deletions); deletion-adjacent
      suites pass."
    command: "uv run pytest tests/ --co -q"
  - kind: "ci"
    description: "omnibase_core PR #1186 CI green across required gates."
    command: "gh pr checks 1186 --repo OmniNode-ai/omnibase_core --watch"
dod_evidence:
  - id: "dod-core-pr-1186"
    description: "Bind OMN-12526 evidence to omnibase_core PR #1186 at the observed PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12526/dod-core-pr-1186/command.yaml\"
          && grep -q '^pr_number: 1186$' \"drift/dod_receipts/OMN-12526/dod-core-pr-1186/command.yaml\"
          && grep -q '^commit_sha: \"77146f3841f285b371e0d59d3035f6d938e0a2e2\"$' \"drift/dod_receipts/OMN-12526/dod-core-pr-1186/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Dead-code deletion touching runtime paths requires CI and receipt-gate validation but
      no live deploy before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12526/dod-deploy-assessment/command.yaml\"
          && grep -q 'deploy gate' \"drift/dod_receipts/OMN-12526/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind OMN-12526 central evidence to this onex_change_control PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12526/dod-occ-pr/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
