---
schema_version: '1.0.0'
ticket_id: OMN-7835
title: "Remove all API key requirements from frontier model auth — use SSO/OAuth"
summary: >
  Remove hard API key requirements for frontier model providers (Anthropic, OpenAI, Google). All frontier
  providers now use SSO/OAuth per OMN-7467. Only GLM (Zhipu AI) retains an explicit API key requirement
  (LLM_GLM_API_KEY).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: EnumLlmProvider.requires_api_key() returns False for all members
    command: "PYTHONPATH='' uv run pytest tests/unit/enums/test_enum_llm_provider.py -v"
  - kind: tests
    description: ModelAgentConfig.api_key is optional (str | None, default=None)
    command: "PYTHONPATH='' uv run mypy src/omnibase_core/models/configuration/model_agent_config.py --strict"
  - kind: tests
    description: >
      Claude API fallback does not raise on missing key.  # anthropic-key-ok: describing removal of requirement,
      not adding one
    command: "PYTHONPATH='' uv run ruff check src/omniintelligence/review_pairing/adapters/adapter_ai_reviewer.py"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >
      omnibase_core PR removes requires_api_key=True for CLAUDE/OPENAI/GEMINI/ANTHROPIC and makes ModelAgentConfig.api_key
      optional. All 15 enum tests pass.  # anthropic-key-ok: describing removal
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view 1021 --repo OmniNode-ai/omnibase_core --json state --jq '.state'"
  - id: dod-002
    description: >
      omniintelligence PR removes RuntimeError on missing key.  # anthropic-key-ok: describing removal
      of requirement Fallback returns empty string with warning instead.
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view 632 --repo OmniNode-ai/omniintelligence --json state --jq '.state'"
