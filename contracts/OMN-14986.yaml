---
schema_version: "1.0.0"
ticket_id: "OMN-14986"
title: "Autobind OCC evidence for OMN-14986"
summary: "OCC Evidence-Source autobind companions (OMN-13317 F1) for PR #2412 and PR #1870."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2412 and PR #1870 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "manual"
    description: >-
      Post-deploy runtime proof plan: confirm the renamed onex.evt.omniclaude.context-injected.v1 topic
      is actually consumed by the redeployed savings-estimation-consumer. Live mutation requires the standard
      prod-promotion / redeploy gate; this ticket only declares the verification plan, it does not perform
      a live deploy.
    command: "test 'deploy verification plan recorded for OMN-14986; live rpk/docker proof is post-merge
      redeploy-gated' = 'deploy verification plan recorded for OMN-14986; live rpk/docker proof is post-merge
      redeploy-gated'"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2412"
    description: "PR #2412 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1870"
    description: "PR #1870 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2412-ci"
    description: "PR #2412 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-OmniNode-ai-omnimarket-pr-1870-ci"
    description: "PR #1870 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4666"
    description: "OCC companion PR #4666 — self-bind for OMN-14986 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-4667"
    description: "OCC companion PR #4667 — self-bind for OMN-14986 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-verify-plan"
    description: >-
      Post-deploy runtime proof plan for the onex.evt.omniclaude.hook-context-injected.v1 -> context-injected.v1
      topic rename: after the next redeploy of the savings-estimation-consumer, produce a synthetic context-injected.v1
      event and confirm ConfigSavingsEstimation.consumed_topics now includes the corrected topic string.
      No live deploy or mutation is performed by this ticket; the live proof routes through the standard
      prod-promotion / redeploy gate.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test 'deploy verification plan recorded for OMN-14986; live rpk/docker proof is
          post-merge redeploy-gated' = 'deploy verification plan recorded for OMN-14986; live rpk/docker
          proof is post-merge redeploy-gated'"
