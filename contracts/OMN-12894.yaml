---
schema_version: "1.0.0"
ticket_id: "OMN-12894"
title: "SEA judge reproduction guide with overlay-based BYO models"
summary: >-
  Central evidence for onex-self-extending-agent PR #225. The PR adds the
  judge-facing reproduction guide for the June 11 SEA demo, separates the
  portable local clone proof from the hosted home-LAN watch path, documents
  Gemini key binding and contract overlays, and prevents judge setup from
  depending on source edits or hidden local state.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-225-source-checks"
    description: >-
      onex-self-extending-agent PR #225 carries the W2 judge reproduction guide
      and passed focused tests plus commit-time SEA unit/integration gates.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-sea-pr-225-source-checks/command.yaml"
          && grep -q '^pr_number: 225$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-sea-pr-225-source-checks/command.yaml"
          && grep -q '53 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-sea-pr-225-source-checks/command.yaml"
          && grep -q '076cf2aeb1ad00e2cc70eeb97136718651d870d0' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-sea-pr-225-source-checks/command.yaml"
  - id: "dod-judge-replay-proof"
    description: >-
      The judge no-key proof path replays the golden fixture through both
      documented entrypoints without live model calls.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-judge-replay-proof/command.yaml"
          && grep -q 'python -m src --replay docs/evidence/golden/golden_fixture.json' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-judge-replay-proof/command.yaml"
          && grep -q 'onex sea run --replay docs/evidence/golden/golden_fixture.json' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-judge-replay-proof/command.yaml"
          && grep -q 'no LLM calls made' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-judge-replay-proof/command.yaml"
  - id: "dod-no-deploy-assessment"
    description: >-
      PR #225 is documentation-only and does not deploy, restart, mutate .201,
      rebuild images, or change runtime contracts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-no-deploy-assessment/command.yaml"
          && grep -q 'No live .201 deployment' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-no-deploy-assessment/command.yaml"
