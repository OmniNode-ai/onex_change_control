---
schema_version: "1.0.0"
ticket_id: "OMN-12894"
title: "SEA judge reproduction guide with overlay-based BYO models"
summary: >-
  Central evidence for onex-self-extending-agent PR #225. The PR adds the judge-facing reproduction guide
  for the June 11 SEA demo, separates the portable local clone proof from the hosted home-LAN watch path,
  documents Gemini key binding and contract overlays, and prevents judge setup from depending on source
  edits or hidden local state.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "protocols"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-225-source-checks"
    description: >-
      onex-self-extending-agent PR #225 carries the W2 judge reproduction guide and passed focused tests
      plus commit-time SEA unit/integration gates.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-sea-pr-225-source-checks/command.yaml"
          && grep -q '^pr_number: 225$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-sea-pr-225-source-checks/command.yaml"
          && grep -q '54 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-sea-pr-225-source-checks/command.yaml"
          && grep -q '1a11359433b3097bf6e9e9903dda5fa77f61b931' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-sea-pr-225-source-checks/command.yaml"
  - id: "dod-judge-replay-proof"
    description: >-
      The judge no-key proof path replays the golden fixture through both documented entrypoints without
      live model calls.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-judge-replay-proof/command.yaml"
          && grep -q 'python -m src --replay docs/evidence/golden/golden_fixture.json' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-judge-replay-proof/command.yaml"
          && grep -q 'onex sea run --replay docs/evidence/golden/golden_fixture.json' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-judge-replay-proof/command.yaml"
          && grep -q 'no LLM calls made' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-judge-replay-proof/command.yaml"
  - id: "dod-no-deploy-assessment"
    description: >-
      PR #225 is documentation-only and does not deploy, restart, mutate .201, rebuild images, or change
      runtime contracts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-no-deploy-assessment/command.yaml"
          && grep -q 'No live .201 deployment' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-no-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2415-self"
    description: >-
      onex_change_control PR #2415 carries the OMN-12894 central contract and receipt evidence for product
      PR #225.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-occ-pr-2415-self/command.yaml"
          && grep -q '^pr_number: 2415$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-occ-pr-2415-self/command.yaml"
          && grep -q '5377f4bf443290f98a58d5f15257d2a305e0baa7' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-occ-pr-2415-self/command.yaml"
  - id: "dod-occ-pr-2419-self"
    description: >-
      onex_change_control PR #2419 carries the refreshed OMN-12894 evidence source for product PR #225
      after resolving the downstream dev conflict.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-occ-pr-2419-self/command.yaml"
          && grep -q '^pr_number: 2419$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-occ-pr-2419-self/command.yaml"
          && grep -q '99bc27c529812ee072eddd9bbd1f707f5f2a3f48' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-occ-pr-2419-self/command.yaml"
  - id: "dod-occ-pr-2497-self"
    description: >-
      onex_change_control PR #2497 carries the P4 clean-clone judge verification evidence bundle for OMN-12894.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-occ-pr-2497-self/command.yaml"
          && grep -q '^pr_number: 2497$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-occ-pr-2497-self/command.yaml"
          && grep -q '2026-06-11-p4-clean-clone-judge-verification' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12894/dod-occ-pr-2497-self/command.yaml"
