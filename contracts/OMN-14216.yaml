---
schema_version: "1.0.0"
ticket_id: "OMN-14216"
title: "chore(OMN-14216): enforce omnibase_core intra-package import layering"
summary: >-
  omnibase_core PR #1407 installs import-linter and adds a deterministic intra-core dependency-layering
  oracle, wiring it into CI and pre-commit so new direct back-edges between core sub-packages fail mechanically.
  The current known back-edges are frozen as an explicit allowlist for OMN-3210 burn-down.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1407"
    description: >-
      omnibase_core PR #1407 adds the import-linter contract, pre-commit hook, and CI quality-gate wiring.
      Local verification proved the oracle keeps all three contracts and fails closed when a frozen ignore
      is removed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14216/dod-omnibase_core-pr-1407/command.yaml
          && grep -q '^pr_number: 1407$' drift/dod_receipts/OMN-14216/dod-omnibase_core-pr-1407/command.yaml
  - id: "dod-occ-companion-pr-3767"
    description: >-
      OCC self-binding: onex_change_control PR #3767 carries the OMN-14216 central contract and PASS receipt
      for omnibase_core PR #1407, with no runtime or deploy scope.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14216/dod-occ-companion-pr-3767/command.yaml
          && grep -q '^pr_number: 3767$' drift/dod_receipts/OMN-14216/dod-occ-companion-pr-3767/command.yaml
  - id: "dod-omnibase_core-pr-1407-indep-verify"
    description: >-
      Independent verification (a separate verifier, NOT the implementing agent) reproduced the import-linter
      oracle end-to-end against omnibase_core PR #1407 head bb19d723 in an isolated worktree with PYTHONPATH
      cleared. lint-imports kept all 3 contracts (Analyzed 4078 files, 10327 dependencies; 3 kept, 0 broken).
      Negative tests: injecting a forbidden enums->nodes, models->services, and services->nodes edge each
      turned the corresponding contract BROKEN (exit 1); removing any one of the 7 frozen ignore families
      turned exactly one contract BROKEN (each ignore is load-bearing, no padding); an unmatched ignore
      errors (self-cleaning ratchet active). mypy clean (4079 files); test_no_circular_imports 20 passed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14216/dod-omnibase_core-pr-1407-indep-verify/command.yaml
          && grep -q '^pr_number: 1407$' drift/dod_receipts/OMN-14216/dod-omnibase_core-pr-1407-indep-verify/command.yaml
          && grep -q '^commit_sha: bb19d723b329fd96f5a0c049346280df6d68adba$' drift/dod_receipts/OMN-14216/dod-omnibase_core-pr-1407-indep-verify/command.yaml
  - id: "dod-occ-indep-self"
    description: >-
      OCC companion self-binding (independent-verification lane): this onex_change_control PR carries
      the independent-verify receipt for omnibase_core PR #1407 and binds itself by commit_sha, with no
      runtime or deploy scope.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14216/dod-occ-indep-self/command.yaml
