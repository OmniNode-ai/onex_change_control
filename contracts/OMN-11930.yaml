---
schema_version: "1.0.0"
ticket_id: "OMN-11930"
title: "Post-merge knowledge sync orchestrator"
summary: >
  Bind central evidence for omnimarket PR #827, which adds the node_post_merge_knowledge_sync_orchestrator
  runtime node. The node consumes post-merge sync requests, writes durable knowledge-sync evidence, and
  conditionally fans out to Repowise, Memgraph, Qdrant antipattern indexing, and ADR canary workflows
  based on changed files and PR metadata.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-827"
    description: "Bind OMN-11930 evidence to omnimarket PR #827."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11930/dod-omnimarket-pr-827/command.yaml
          && grep -q '^pr_number: 827$' drift/dod_receipts/OMN-11930/dod-omnimarket-pr-827/command.yaml"
  - id: "dod-post-merge-sync-local-validation"
    description: "Focused post-merge knowledge sync tests, ruff, leaked-literals, dep-health, and imperative
      contract guard pass for the current omnimarket PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11930/dod-post-merge-sync-local-validation/command.yaml
          && grep -q '16 passed' drift/dod_receipts/OMN-11930/dod-post-merge-sync-local-validation/command.yaml
          && grep -q 'baseline_delta: 0' drift/dod_receipts/OMN-11930/dod-post-merge-sync-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: the new orchestrator node is locally validated and requires no
      live runtime deployment before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11930/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11930/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11930 contract, deploy evidence, and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11930/dod-occ-pr/command.yaml"
