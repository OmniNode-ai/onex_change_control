---
schema_version: "1.0.0"
ticket_id: "OMN-9162"
title: "Canonicalize delegate session ID envelope to ONEX_SESSION_ID"
summary: >
  Binds omniclaude PR #1550, which updates the delegate skill runtime request and evidence/cost envelope
  construction to read ONEX_SESSION_ID instead of CLAUDE_SESSION_ID. The matching delegate Kafka publish
  test fixture now sets the canonical environment variable.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1550-head"
    description: "omniclaude PR #1550 carries the OMN-9162 implementation at the recorded head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9162/dod-pr-1550-head/command.yaml\""
  - id: "dod-delegate-kafka-tests"
    description: "Focused delegate Kafka publish tests pass with ONEX_SESSION_ID."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9162/dod-delegate-kafka-tests/command.yaml\""
  - id: "dod-ci-1550-green"
    description: "All non-receipt CI checks on omniclaude PR #1550 are passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9162/dod-ci-1550-green/command.yaml\""
