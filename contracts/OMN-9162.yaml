---
schema_version: "1.0.0"
ticket_id: "OMN-9162"
title: "Canonicalize session ID env var to ONEX_SESSION_ID in delegate skill"
summary: >
  Binds omniclaude PR #1550, which replaces three occurrences of os.environ.get('CLAUDE_SESSION_ID') with
  os.environ.get('ONEX_SESSION_ID') in the delegate skill's run.py. Aligns the delegate layer with the
  ONEX-standard session identity variable propagated by OMN-9159 and OMN-9161.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1550"
    description: "omniclaude PR #1550 carries the OMN-9162 ONEX_SESSION_ID canonicalization."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1550 --repo OmniNode-ai/omniclaude --json state -q .state"
  - id: "dod-delegate-tests"
    description: "Delegate skill Kafka publish tests pass with ONEX_SESSION_ID fixture."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1550 --repo OmniNode-ai/omniclaude --json statusCheckRollup -q '.statusCheckRollup[]
          | select(.name==\"Tests Gate\") | .conclusion'"
  - id: "dod-occ-pr"
    description: "Bind this OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view --repo OmniNode-ai/onex_change_control --head jonah/omn-9159-9161-9162-occ-contracts
          --json state -q .state"
