---
schema_version: 1.0.0
ticket_id: OMN-7384
title: Add partial infrastructure migration detector to CI
summary: Add partial infrastructure migration detector to CI
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: CI passes on merge PR
    command: gh pr checks ${PR_NUMBER} --repo ${REPO}
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Contract compliance runner loaded this ticket contract
    source: manual
    checks:
      - check_type: command
        check_value: python3 -c "print('contract loaded for ${TICKET_ID}')"
  - id: dod-002
    description: PR title or body cites this ticket
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json title,body --jq '.title + " " + (.body
          // "")' | grep -q '${TICKET_ID}'
  - id: dod-003
    description: Infra consistency detector synthetic fixture tests passed in the implementation PR
    source: generated
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-7384/dod-003/command.yaml && grep -q '^ticket_id:
          OMN-7384$' drift/dod_receipts/OMN-7384/dod-003/command.yaml && grep -q '^pr_number: 371$' drift/dod_receipts/OMN-7384/dod-003/command.yaml
