---
schema_version: "1.0.0"
ticket_id: "OMN-14310"
title: "refactor(OMN-14310): canonicalize post-merge knowledge sync orchestrator"
summary: >-
  omnimarket PR #1696 canonicalizes node_post_merge_knowledge_sync_orchestrator to return its typed fan-out
  command payload directly instead of a ModelHandlerOutput wrapper. The branch was refreshed onto current
  dev before evidence capture.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1696-head-6f23269a"
    description: >-
      Foreground Codex verification of omnimarket PR #1696 at head 6f23269a972b03fd11265df803ede7172083bbd0
      after rebasing onto current dev. Local focused checks passed: post-merge knowledge sync pytest suite
      (16 passed), ruff check and ruff format --check on changed handler/test paths, CI-shaped mypy src/omnimarket
      --strict, strict changed-node state coverage, changed-node golden-chain coverage, unimported-handler
      wiring, strict node-drift gate, and market_node_runtime_dogfood over 364 node dirs.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14310/dod-omnimarket-pr-1696-head-6f23269a/command.yaml
          && grep -q '^pr_number: 1696$' drift/dod_receipts/OMN-14310/dod-omnimarket-pr-1696-head-6f23269a/command.yaml
          && grep -q '6f23269a972b03fd11265df803ede7172083bbd0' drift/dod_receipts/OMN-14310/dod-omnimarket-pr-1696-head-6f23269a/command.yaml
  - id: "dod-deploy-assessment-head-6f23269a"
    description: >-
      Deploy-surface assessment for omnimarket PR #1696 head 6f23269a972b03fd11265df803ede7172083bbd0:
      the diff is limited to a post-merge knowledge sync orchestrator direct-return refactor and focused
      tests. It does not mutate live deployment configuration, perform a deploy/restart, change topics,
      run migrations, or touch .201.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14310/dod-deploy-assessment-head-6f23269a/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14310/dod-deploy-assessment-head-6f23269a/command.yaml
  - id: "dod-occ-self-commit-initial"
    description: >-
      OCC companion self-binding: the initial onex_change_control commit for OMN-14310 carries this central
      contract plus the product and deploy PASS receipts for omnimarket PR #1696 head 6f23269a972b03fd11265df803ede7172083bbd0.
      The follow-up receipt binds that initial OCC evidence commit for occ-preflight eligibility before
      the OCC PR is merged.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14310/dod-occ-self-commit-initial/command.yaml
          && grep -Eq '^[[:space:]]*commit_sha: [0-9a-f]{40}$' drift/dod_receipts/OMN-14310/dod-occ-self-commit-initial/command.yaml
