---
schema_version: "1.0.0"
ticket_id: "OMN-16840"
title: "Autobind OCC evidence for OMN-16840"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnidash PR #296.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #296 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      OWED, not claimed. PR #296 on OmniNode-ai/omnidash changes no pytest target, so this producer cannot derive a behavior-class check from its diff and mints none - the changed surface (server/__tests__/dispatch.test.ts, server/routes.ts, shared/contracts/delegation-task-types.json, shared/types/delegation-task-availability.ts) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnidash-pr-296"
    description: "PR #296 on OmniNode-ai/omnidash — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 296 --repo OmniNode-ai/omnidash --json number,state"
      - check_type: "command"
        check_value: "gh pr view 296 --repo OmniNode-ai/omnidash --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnidash-pr-296"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7402"
    description: "Binds OMN-16840 to OCC companion PR #7402 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7402 --repo OmniNode-ai/onex_change_control --json number,state"
