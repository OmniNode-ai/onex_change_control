---
schema_version: "1.0.0"
ticket_id: "OMN-12419"
title: "Fix all 19 non-addressable market nodes so each has valid Onex-native invocation"
summary: >-
  Bind evidence for omnimarket PR #987, which corrects the former non-addressable market node set so
  compute/effect nodes are command-addressable and only event-driven reducers/orchestrators remain
  native non-addressable.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
  - "public_api"
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "tests"
    description: "Market node runtime dogfood strict audit passes with 287 routable entry points, 3 valid native event nodes, and 0 failures."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12419/dod-dogfood/command.yaml"
  - kind: "tests"
    description: "Focused market node and runtime client tests pass for the corrected 19-node set."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12419/dod-tests/command.yaml"
  - kind: "ci"
    description: "Deploy-gate evidence declares no live deploy is required before merge and supplies post-merge runtime validation guidance."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12419/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-dogfood"
    description: "Strict market-node runtime dogfood classifies all 290 entry points with no route failures."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12419/dod-dogfood/command.yaml"
  - id: "dod-all-19"
    description: "All 19 formerly non-addressable market nodes are proven as 16 command-addressable routes and 3 valid native event consumers."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12419/dod-all-19/command.yaml"
  - id: "dod-tests"
    description: "Focused pytest, ruff, mypy, node drift, and whitespace checks pass for the omnimarket PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12419/dod-tests/command.yaml"
  - id: "dod-deploy"
    description: "No live deploy is required before merge; runtime-impacting contract changes carry post-merge validation guidance for the stability runtime."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          echo deploy: no live deploy required before merge; after merge validate the stability runtime with
          `docker exec omninode-runtime env -u PYTHONPATH uv run python scripts/audit/market_node_runtime_dogfood.py --strict`
  - id: "dod-pr"
    description: "omnimarket PR #987 carries the OMN-12419 market-node runtime changes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12419/dod-pr/command.yaml"
