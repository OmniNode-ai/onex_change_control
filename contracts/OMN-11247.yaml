---
schema_version: "1.0.0"
ticket_id: "OMN-11247"
title: "Runtime manifest pool injection and post-freeze contract listener"
summary: >
  Central OCC evidence for OMN-11247 runtime work. The original evidence bound omnibase_infra PR #1655
  for the post-freeze RuntimeHostProcess listener. The current evidence also binds omnibase_infra PR #1677,
  which threads the registration service postgres pool into manifest auto-wiring, starts the dynamic Kafka
  contract source in normal hybrid runtime mode, and records the .201 stability-runtime event-bus hot-load
  proof.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1677 is open against main at the recorded runtime manifest pool injection
      and dynamic listener head."
    command: "gh pr view 1677 --repo OmniNode-ai/omnibase_infra --json headRefOid,state,url,headRefName,baseRefName"
  - kind: "tests"
    description: "Runtime unit tests and focused runtime/auto-wiring integration tests pass locally in
      the OMN-11247 worktree."
    command: "uv run pytest tests/unit -n auto --tb=short && uv run pytest tests/integration/runtime/test_auto_wiring_resolver_end_to_end.py
      tests/unit/runtime/test_kernel.py tests/unit/runtime/test_policy_registry_performance.py::TestPolicyRegistryPerformanceRegression::test_secondary_index_speedup
      tests/unit/services/session_registry/test_session_graph_projector.py::TestModelConfigGraphProjector::test_defaults
      tests/unit/scripts/test_pull_all.py::TestPluginCacheRefresh tests/unit/runtime/test_runtime_host_dynamic_registration.py
      tests/integration/test_runtime_host_dynamic_registration.py tests/integration/runtime/test_manifest_pool_injection_integration.py
      tests/unit/runtime/test_handler_wiring_resolver_integration.py -q --tb=short"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1677"
    description: "omnibase_infra PR #1677 is open against main at the recorded runtime manifest pool injection
      and dynamic listener test-cleanup head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-omnibase-infra-pr-1677/command.yaml\"
          && grep -q '^commit_sha: \"600f5e3b9659da4d42f4e129479ae0e1cc7dc84e\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-omnibase-infra-pr-1677/command.yaml\""
  - id: "dod-pr-1677-local-verification"
    description: "Local unit, focused runtime/auto-wiring, ruff, and full-suite verification were run
      for PR #1677 after the test-cleanup commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-pr-1677-local-verification/command.yaml\"
          && grep -q '19662 passed, 14 skipped' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-pr-1677-local-verification/command.yaml\"
          && grep -q '33 failed, 22932 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-pr-1677-local-verification/command.yaml\"
          && grep -q '52 errors' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-pr-1677-local-verification/command.yaml\""
  - id: "dod-pr-1677-stability-runtime-proof"
    description: ".201 stability runtime was rebuilt and proved with health checks plus event-bus dynamic
      contract hot-load for the PR #1677 runtime branch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-pr-1677-stability-runtime-proof/command.yaml\"
          && grep -q 'node_stability_eventbus_hotload_1779310657' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-pr-1677-stability-runtime-proof/command.yaml\""
  - id: "dod-omnibase-infra-pr-1655"
    description: "omnibase_infra PR #1655 is open against main at the recorded dynamic registration listener
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-omnibase-infra-pr-1655/command.yaml\"
          && grep -q '^commit_sha: \"b6dd99928ea6826a6827e36dc26129abdfacea5c\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-omnibase-infra-pr-1655/command.yaml\""
  - id: "dod-dynamic-registration-artifacts"
    description: "The PR carries the product deploy contract and the root-level integration test required
      by the integration coverage gate."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-dynamic-registration-artifacts/command.yaml\"
          && grep -q 'contracts/OMN-11247.yaml' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-dynamic-registration-artifacts/command.yaml\"
          && grep -q 'tests/integration/test_runtime_host_dynamic_registration.py' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-dynamic-registration-artifacts/command.yaml\""
  - id: "dod-local-targeted-tests"
    description: "The focused unit and integration test slice passes in the product worktree."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-local-targeted-tests/command.yaml\"
          && grep -q '15 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-local-targeted-tests/command.yaml\""
  - id: "dod-product-deploy-contract"
    description: "The product repo deploy-gate contract for OMN-11247 validates locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-product-deploy-contract/command.yaml\"
          && grep -q 'contracts/OMN-11247.yaml (ticket_contract)' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-product-deploy-contract/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the OMN-11247 central contract and receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-occ-pr/command.yaml\"
          && grep -q '^ticket_id: \"OMN-11247\"' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-occ-pr/command.yaml\""
  - id: "dod-occ-boundary-action-cleanup-pr-1238"
    description: "onex_change_control PR #1238 cleans the boundary validation clone workspace before reruns
      so self-hosted runners do not inherit stale /tmp/omni_repos directories."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-occ-boundary-action-cleanup-pr-1238/command.yaml\"
          && grep -q '^pr_number: 1238$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-occ-boundary-action-cleanup-pr-1238/command.yaml\"
          && grep -q '^commit_sha: \"22f632ef7f6594b7e35b150d2a6927fd92b681d0\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-occ-boundary-action-cleanup-pr-1238/command.yaml\""
  - id: "dod-omnimarket-pr-722"
    description: "omnimarket PR #722 publishes runtime-ready contract registration events and adds deploy-gate
      contract."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-omnimarket-pr-722/command.yaml\"
          && grep -q '^commit_sha: \"81bccf2833f653e39bbb4387ffe5f01f7efcf2dc\"' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11247/dod-omnimarket-pr-722/command.yaml\""
