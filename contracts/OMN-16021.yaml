---
schema_version: "1.0.0"
ticket_id: "OMN-16021"
title: "Autobind OCC evidence for OMN-16021"
summary: "OCC Evidence-Source companion (OMN-13317 F1, hand-authored fallback per OMN-15247 -- the occ-autobind
  live consumer on .201 is unavailable behind the active GitHub self-hosted runner-assignment incident,
  so the mechanical bus-driven path could not author this companion) for PR #2099."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2099 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2099"
    description: "PR #2099 on OmniNode-ai/omnimarket — Evidence-Source (hand-authored: the occ_companion_emitter
      live consumer on .201 could not process the OCC Autobind Publisher event because no self-hosted
      omnibase-ci runner is currently online, so hand-authored evidence is required per OMN-15247)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2099 --repo OmniNode-ai/omnimarket --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-2099-ci"
    description: "PR #2099 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425): confirms
      the PR's changes are scoped to node_event_emit_effect's topic_resolver.py fix plus its two regression
      test files (OMN-16021 -- resolve_event_type no longer raises UnknownEventTypeError for a registered
      event_type with an explicitly empty fan_out list)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2099 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-6672"
    description: "OCC companion PR #6672 — self-bind for OMN-16021 (OMN-14650)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6672 --repo OmniNode-ai/onex_change_control --json number,state"
