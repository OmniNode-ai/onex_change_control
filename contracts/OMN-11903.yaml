---
schema_version: "1.0.0"
ticket_id: "OMN-11903"
title: "Wave 3 projections declarative contract migration"
summary: >
  Bind central evidence for omnimarket PR #799, which migrates projection nodes to declarative handler_routing
  contracts, adds dependency-health-compatible handler_module mirrors, and proves the local routing, topic-lint,
  and imperative-contract gates before merge.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-799"
    description: "Bind OMN-11903 evidence to omnimarket PR #799."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11903/dod-omnimarket-pr-799/command.yaml
          && grep -q '^pr_number: 799$' drift/dod_receipts/OMN-11903/dod-omnimarket-pr-799/command.yaml"
  - id: "dod-projection-routing-local-gates"
    description: "Projection handler_routing entries expose top-level handler_module mirrors for dependency
      health."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11903/dod-projection-routing-local-gates/command.yaml
          && grep -q 'handler_module mirrors ok' drift/dod_receipts/OMN-11903/dod-projection-routing-local-gates/command.yaml"
  - id: "dod-imperative-contract-guard"
    description: "Imperative contract guard remains clean for the projection contract migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11903/dod-imperative-contract-guard/command.yaml
          && grep -q 'omnimarket                     203      289     39      250     0' drift/dod_receipts/OMN-11903/dod-imperative-contract-guard/command.yaml"
  - id: "dod-topic-lint"
    description: "Topic lint accepts the projection snapshot allow annotations and renamed registry assertion."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11903/dod-topic-lint/command.yaml
          && grep -q 'OK: no topic naming violations found under src/omnimarket' drift/dod_receipts/OMN-11903/dod-topic-lint/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence for the projections declarative contract migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11903/dod-deploy-readiness/command.yaml
          && grep -q 'projection contract migration has local routing, topic-lint, and imperative guard
          proof' drift/dod_receipts/OMN-11903/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr-1518"
    description: "OCC PR #1518 carries the central OMN-11903 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11903/dod-occ-pr-1518/command.yaml
          && grep -q '^pr_number: 1518$' drift/dod_receipts/OMN-11903/dod-occ-pr-1518/command.yaml"
