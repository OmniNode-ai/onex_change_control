---
schema_version: "1.0.0"
ticket_id: OMN-13203
title: "Quarantine per-handler resolution failures instead of crashing runtime-effects boot"
summary: >
  ServiceHandlerResolver resolution failures for individual handler_routing entries are quarantined as
  UNRESOLVABLE_HANDLER instead of aborting runtime-effects boot before the health server binds. Strict
  mode still re-raises the resolution failures, and fatal infra errors remain outside the quarantine boundary.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Focused auto-wiring quarantine tests pass
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13203/dod-focused-tests/command.yaml"
  - kind: ci
    description: omnibase_infra PR 2011 is bound to the central OCC evidence
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13203/dod-infra-pr-2011/command.yaml"
dod_evidence:
  - id: dod-focused-tests
    description: Focused auto-wiring quarantine and strict-mode tests pass
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13203/dod-focused-tests/command.yaml"
  - id: dod-lint-mypy
    description: Changed auto-wiring files pass ruff and strict mypy
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13203/dod-lint-mypy/command.yaml"
  - id: dod-infra-pr-2011
    description: omnibase_infra PR 2011 is open with the expected head SHA
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13203/dod-infra-pr-2011/command.yaml
          && grep -q '^pr_number: 2011$' drift/dod_receipts/OMN-13203/dod-infra-pr-2011/command.yaml"
  - id: dod-deploy
    description: >
      Runtime deploy preflight: this change keeps runtime-effects boot alive when one handler cannot be
      resolved, while strict mode and fatal infra errors still fail closed. No new service, Kafka topic,
      migration, or external API surface is introduced.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13203/dod-deploy/command.yaml"
  - id: dod-occ-pr-2696
    description: onex_change_control PR 2696 carries the central OCC evidence for OMN-13203
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13203/dod-occ-pr-2696/command.yaml
          && grep -q '^pr_number: 2696$' drift/dod_receipts/OMN-13203/dod-occ-pr-2696/command.yaml"
  - id: dod-infra-pr-2014
    description: >
      omnibase_infra PR 2014 (the reopened quarantine-guard PR that supersedes the closed PR 2011) is
      open with the expected head SHA. Binds the OCC evidence to the live PR so the merge-eligibility
      validator can confirm a PASS receipt for OMN-13203 binds to PR 2014 or one of its commit SHAs.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13203/dod-infra-pr-2014/command.yaml
          && grep -q '^pr_number: 2014$' drift/dod_receipts/OMN-13203/dod-infra-pr-2014/command.yaml"
  - id: dod-occ-pr-2715
    description: onex_change_control PR 2715 carries the PR-2014-bound OCC evidence for OMN-13203
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13203/dod-occ-pr-2715/command.yaml
          && grep -q '^pr_number: 2715$' drift/dod_receipts/OMN-13203/dod-occ-pr-2715/command.yaml"
