---
schema_version: "1.0.0"
ticket_id: "OMN-13738"
title: "test(OMN-13738): raise coverage of src/omnimarket/inference/adapter_inference_bridge.py"
summary: >
  Adds 18 unit tests for AdapterInferenceBridge covering all previously uncovered branches: __init__ config
  storage, infer() unknown model_key/transport routing, _call_http_model() missing base_url/endpoint_url_env/model_id,
  api_key bearer header, env-var resolution, reserved header warning, temperature override/fallback, successful
  HTTP POST parse, _call_cli_model() missing cli_command, subprocess result, prompt assembly. Also adds
  tests for check_backend_secret_discipline CI gate, including vertex backend secret_ref/endpoint_url_env
  discipline assertions. No live infrastructure required; all I/O mocked. Also fixes test_vertex_backend_uses_secret_ref_bearer_path
  to assert endpoint_url_env (not the removed base_url_env field name).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "PASS receipts confirm unit tests pass and backend secret discipline holds."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13738/dod-unit-tests/command.yaml && grep
      -q '^status: PASS$' drift/dod_receipts/OMN-13738/dod-deploy-assessment/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-13738/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: >
      All 18 new unit tests in tests/unit/inference/test_adapter_inference_bridge.py pass. All 7 tests
      in tests/ci/test_check_backend_secret_discipline.py pass including the fixed test_vertex_backend_uses_secret_ref_bearer_path
      (now asserts endpoint_url_env). Full unit suite runs clean with no regressions.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13738/dod-unit-tests/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Deploy assessment: this change is tests-only (new unit tests + one test assertion fix). No live
      deploy, restart, Kafka mutation, database mutation, .201 mutation, or runtime lane mutation was
      performed or required. No contract.yaml, no new topics, no handler changes — purely test coverage
      additions.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13738/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13738 contract and receipts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13738/dod-occ-self/command.yaml"
