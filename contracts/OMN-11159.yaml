---
schema_version: "1.0.0"
ticket_id: "OMN-11159"
title: "Flip cross-repo validation workflow to blocking in omnibase_core"
summary: >
  Central OCC evidence for the OMN-11159 slice of omnibase_core PR #1106, which removes advisory behavior
  from cross-repo validation.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "Cross-repo boundary validation passes on omnibase_core PR #1106."
    command: "gh pr checks 1106 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-cross-repo-validation-blocking"
    description: "Cross-repo boundary validation is green after the workflow is made blocking."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11159/dod-cross-repo-validation-blocking/command.yaml\"
          && grep -q 'Cross-repo boundary validation' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11159/dod-cross-repo-validation-blocking/command.yaml\""
