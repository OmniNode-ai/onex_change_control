---
schema_version: "1.0.0"
ticket_id: "OMN-10648"
title: "Remove personal GitHub username default from pr_review_bot contract"
summary: >
  Removes the static personal emergency bypass actor from node_pr_review_bot/contract.yaml and adds repo-local
  deploy-gate evidence requiring explicit GITHUB_REVIEWER_LOGIN runtime configuration.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-contract-validate"
    description: "Repo-local OMN-10648 deploy-gate contract validates."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10648/dod-contract-validate/command.yaml\""
  - id: "dod-precommit"
    description: "Repo-local OMN-10648 deploy-gate contract passes pre-commit hooks."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10648/dod-precommit/command.yaml\""
  - id: "dod-pr-554-head"
    description: "Bind OMN-10648 evidence to omnimarket PR #554 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10648/dod-pr-554-head/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10648 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10648/dod-occ-pr/command.yaml\""
