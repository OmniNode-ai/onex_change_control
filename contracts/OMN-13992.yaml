---
schema_version: "1.0.0"
ticket_id: "OMN-13992"
title: "fix(projections): inject live-events topic and declare malformed-event DLQ"
summary: >-
  omnibase_infra PR #2218 fixes HandlerProjectionLiveEvents dispatch by injecting
  input_data._topic into projection handler calls, eliminating the dev-lane
  ValueError raised every node-heartbeat interval. omnimarket PR #1628 declares
  the corresponding projection-live-events malformed-event DLQ topic and adds
  visible state-coverage tests so future malformed projection events are routed
  durably instead of only logged and dropped.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_infra-pr-2218"
    description: >-
      Consuming infra PR binding: omnibase_infra PR #2218 at head
      1bf80b3adf9761135010df2c9cb34f8a2d8f01a3 injects _topic into projection
      dispatch input_data and verifies the strict HandlerProjectionLiveEvents path.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13992/dod-omnibase_infra-pr-2218/command.yaml"
  - id: "dod-omnimarket-pr-1628"
    description: >-
      Consuming market PR binding: omnimarket PR #1628 at head
      fd3b94495b14efdf03a0d76bc0d176fd797f4c20 declares the
      node_projection_live_events DLQ topic and adds top-level state coverage
      mirror tests for the node contract.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13992/dod-omnimarket-pr-1628/command.yaml"
  - id: "dod-local-verification"
    description: >-
      Local verification binding: focused handler-wiring tests, node-local
      projection tests, state coverage strict check, projection DLQ path check,
      ruff, mypy, and pre-commit verification were run as documented in the
      consuming PRs; broad infra-dependent failures were explicitly identified
      as pre-existing local Docker/Postgres/Kafka availability issues.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13992/dod-local-verification/command.yaml"
