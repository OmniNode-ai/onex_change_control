---
schema_version: "1.0.0"
ticket_id: "OMN-13664"
title: "Bind omnimarket PR 1468 and omnibase_infra PR 2122 to OCC evidence"
summary: >
  Bind omnimarket PR #1468, which preserves authoritative savings tier data in the source node migration,
  and replacement omnibase_infra PR #2122, which bumps python-engineio/python-socketio, refreshes runner
  identity, and vendors the corrected node migrations required by current gates, to central OCC evidence.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1468-head"
    description: "omnimarket PR #1468 carries the source node migration fix for savings tier preservation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13664/dod-omnimarket-pr-1468-head/command.yaml\""
  - id: "dod-omnimarket-pr-1468-local-gates"
    description: "Focused omnimarket local gates passed for the source migration fix."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13664/dod-omnimarket-pr-1468-local-gates/command.yaml\""
  - id: "dod-infra-pr-2122-head"
    description: "omnibase_infra PR #2122 carries the dependency bump, generated gate-refresh commit,
      and corrected vendored node migration."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13664/dod-infra-pr-2122-head/command.yaml\""
  - id: "dod-infra-pr-2122-local-gates"
    description: "Focused infra local gates passed for the runner identity lock and corrected vendored
      node migration sync."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13664/dod-infra-pr-2122-local-gates/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR #3213 carries the OMN-13664 contract and receipts for omnimarket
      PR #1468 and omnibase_infra PR #2122."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13664/dod-occ-pr-self/command.yaml\""
