---
schema_version: "1.0.0"
ticket_id: "OMN-13664"
title: "Bind omnibase_infra PR 2122 to OCC evidence"
summary: >
  Bind replacement omnibase_infra PR #2122, which bumps python-engineio and python-socketio and refreshes
  the runner image identity plus vendored node migrations required by current gates, to central OCC evidence.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2122-head"
    description: "omnibase_infra PR #2122 carries the dependency bump and generated gate-refresh commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13664/dod-infra-pr-2122-head/command.yaml\""
  - id: "dod-infra-pr-2122-local-gates"
    description: "Focused local gates passed for the runner identity lock and vendored node migration
      sync."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13664/dod-infra-pr-2122-local-gates/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR #3212 carries the OMN-13664 contract and receipts for omnibase_infra
      PR #2122."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"${CONTRACT_REPO_DIR:-.}/drift/dod_receipts/OMN-13664/dod-occ-pr-self/command.yaml\""
