---
schema_version: "1.0.0"
ticket_id: "OMN-14852"
title: "Autobind OCC evidence for OMN-14852"
summary: "OCC Evidence-Source companion for OmniNode-ai/omnidash#264."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #264 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnidash-264-head-08124ea4"
    description: "PR #264 on OmniNode-ai/omnidash -- product head 08124ea4."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefName,headRefOid"
  - id: "dod-omnidash-264-diff-08124ea4"
    description: "PR #264 on OmniNode-ai/omnidash -- product diff scope at head 08124ea4."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files,headRefOid"
  - id: "dod-omnidash-264-deploy-08124ea4"
    description: "Deploy-gate scope for omnidash PR #264 at head 08124ea4. The change repairs Express
      bridge projection-reader schema compatibility for delegation_events/node_service_registry/event_bus_events,
      adds OIDC logout rejection handling in the dashboard view, and updates targeted projection-reader
      tests. No live deploy, restart, .201 mutation, docker exec, or production mutation is authorized
      by this evidence repair; hosted merge gates and post-merge dev availability remain the deployment
      boundary."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "printf '%s\\n' 'deploy scope assessed for omnidash#264 projection-reader schema
          compatibility and OIDC logout handling; no live deploy, restart, .201 mutation, docker exec,
          or production mutation is authorized by this evidence repair'"
  - id: "occ-self-bind-pr-4501"
    description: "OCC companion PR #4501 -- self-bind for OMN-14852 omnidash#264 Evidence-Source companion."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
