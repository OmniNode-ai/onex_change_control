---
schema_version: "1.0.0"
ticket_id: "OMN-14852"
title: "Align omnidash live projection reader schema and OIDC logout"
summary: >
  Central OCC evidence for OmniNode-ai/omnidash#264, which aligns the Postgres projection reader with
  the live delegation/event-bus schemas and adds an OIDC-gated logout control to the dashboard.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: "ci"
    description: "omnidash#264 remains bound to exact product head 08124ea4e0b04f4f571c07fd76d35b6e60193f2a."
    command: "PRODUCT_PR_NUMBER=264; PRODUCT_REPO=OmniNode-ai/omnidash; PRODUCT_HEAD=08124ea4e0b04f4f571c07fd76d35b6e60193f2a;
      test \"$(gh pr view ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --json headRefOid --jq .headRefOid)\"
      = \"${PRODUCT_HEAD}\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnidash-pr-264-head"
    description: "omnidash#264 is bound to OMN-14852 at exact product head 08124ea4e0b04f4f571c07fd76d35b6e60193f2a."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=264; PRODUCT_REPO=OmniNode-ai/omnidash; PRODUCT_HEAD=08124ea4e0b04f4f571c07fd76d35b6e60193f2a;
          test \"$(gh pr view ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --json headRefOid --jq .headRefOid)\"
          = \"${PRODUCT_HEAD}\""
  - id: "dod-omnidash-pr-264-diff-scope"
    description: "omnidash#264 carries the reviewed dashboard projection-reader and logout-control diff
      scope for OMN-14852."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=264; PRODUCT_REPO=OmniNode-ai/omnidash; gh pr diff ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --name-only | grep -q '^server/postgres-projection-reader.ts$' && gh
          pr diff ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --name-only | grep -q '^src/pages/DashboardView.tsx$'"
  - id: "dod-omnidash-pr-264-local-verification"
    description: "omnidash#264 PR body records OMN-14852 local verification for the schema-drift and tenant/auth
      test families."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=264; PRODUCT_REPO=OmniNode-ai/omnidash; gh pr view ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --json body --jq .body | grep -q 'OMN-14852 local verification' && gh
          pr view ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --json body --jq .body | grep -q '25 passed'
          && gh pr view ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --json body --jq .body | grep -q 'npm
          run check PASS'"
  - id: "dod-omnidash-pr-264-deploy-scope"
    description: "Deploy-readiness assessment for OMN-14852. The dashboard server/UI diff requires normal
      omnidash redeploy after merge; this OCC lane performed no live deploy, restart, .201, or production
      mutation."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14852/dod-omnidash-pr-264-deploy-scope/command.yaml
          && grep -qi 'no live deploy, restart, .201, or production mutation' drift/dod_receipts/OMN-14852/dod-omnidash-pr-264-deploy-scope/command.yaml"
  - id: "occ-self-bind-pr-4499"
    description: "OCC companion PR #4499 self-binding proof for OMN-14852 central evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4499; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state,headRefOid"
