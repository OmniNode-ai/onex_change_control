---
schema_version: "1.0.0"
ticket_id: "OMN-11012"
title: "Settle node_service_registry migration ownership"
summary: >-
  Binds evidence for omnimarket PR #667, which declares node_service_registry DDL ownership in omnimarket
  node metadata and the create migration while confirming the former omnibase_infra duplicate migration
  is absent from current main.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR #667 carries the ownership classification at the observed head."
    command: "gh pr view 667 --repo OmniNode-ai/omnimarket --json headRefOid,state,url"
dod_evidence:
  - id: "dod-omnimarket-pr-667"
    description: "Bind omnimarket PR #667 to OMN-11012 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-omnimarket-pr-667/command.yaml\"
          && grep -q '^pr_number: 667$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-omnimarket-pr-667/command.yaml\"
          && grep -q '^commit_sha: \"9ce537674aae7668439eda394e3f699ddc5623c7\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-omnimarket-pr-667/command.yaml\""
  - id: "dod-ownership-tests"
    description: "Focused omnimarket tests prove metadata, migration, and docs declare node_service_registry
      ownership."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-ownership-tests/command.yaml\"
          && grep -q '^commit_sha: \"9ce537674aae7668439eda394e3f699ddc5623c7\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-ownership-tests/command.yaml\""
  - id: "dod-projection-regression"
    description: "Existing registration projection golden-chain tests still pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-projection-regression/command.yaml\"
          && grep -q '^commit_sha: \"9ce537674aae7668439eda394e3f699ddc5623c7\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-projection-regression/command.yaml\""
  - id: "dod-owner-scan"
    description: "Targeted scan proves the infra duplicate is absent and the only remaining CREATE TABLE
      statement is the omnimarket projection migration."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-owner-scan/command.yaml\"
          && grep -q '^commit_sha: \"9ce537674aae7668439eda394e3f699ddc5623c7\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-owner-scan/command.yaml\""
  - id: "dod-deploy"
    description: "No live deploy or runtime restart is required; the downstream change is metadata, docs,
      and tests only."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-deploy/command.yaml\"
          && grep -q '^commit_sha: \"9ce537674aae7668439eda394e3f699ddc5623c7\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-deploy/command.yaml\""
  - id: "dod-occ-pr-1073-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11012 central evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-occ-pr-1073-self/command.yaml\"
          && grep -q '^pr_number: 1073$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11012/dod-occ-pr-1073-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
