---
schema_version: "1.0.0"
ticket_id: "OMN-11500"
title: "Wire --agent flag with ADK InMemoryRunner"
summary: >-
  onex-self-extending-agent PR #84 wires the --agent CLI flag to an ADK InMemoryRunner that runs the self-extending
  agent loop, invoking scaffold_onex_node, validate_contract, register_generated_tool, and invoke_generated_tool
  in sequence.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: tests
    description: "Unit tests pass with --agent flag wiring."
    command: "uv run pytest tests/unit -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-84"
    description: "onex-self-extending-agent PR #84 carries the --agent flag wiring."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11500/dod-product-pr-84/command.yaml"
          && grep -q '^pr_number: 84$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11500/dod-product-pr-84/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11500 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11500/dod-occ-self/command.yaml"
