---
schema_version: "1.0.0"
ticket_id: "OMN-11577"
title: "Implement NodeCiErrorClassifierCompute handler"
summary: >
  Central OCC evidence for omniintelligence PR #673, which implements handle_classify_ci_error for NodeCiErrorClassifierCompute
  — parses raw LLM failure output (JSON or plain-text) and normalizes via _parse_llm_response. Adds handler_routing
  to contract.yaml and 8 new unit tests.
is_seam_ticket: false
interface_change: false
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniintelligence-pr-673"
    description: "omniintelligence PR #673 carries the CiErrorClassifier + DebugRetrieval implementation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11577/dod-omniintelligence-pr-673/command.yaml
          && grep -q '^pr_number: 673$' drift/dod_receipts/OMN-11577/dod-omniintelligence-pr-673/command.yaml"
