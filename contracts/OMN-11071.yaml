---
schema_version: "1.0.0"
ticket_id: "OMN-11071"
title: "Retry uv sync in CI setup action"
summary: >-
  Central OCC evidence for omnibase_infra PR #1609. Adds bounded retry controls around uv sync in the
  shared setup-python-uv composite action to absorb transient git/HTTP failures from upstream omnibase_core
  branch resolution, and adds CI workflow resilience regression coverage for the shared action behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "CI workflow resilience tests pass against the updated setup-python-uv action."
    command: "See drift/dod_receipts/OMN-11071/dod-tests/command.yaml"
  - kind: "manual"
    description: "omnibase_infra PR #1609 carries the uv sync retry change."
    command: "See drift/dod_receipts/OMN-11071/dod-infra-pr-1609/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "CI workflow resilience tests cover the retry semantics of setup-python-uv."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11071/dod-tests/command.yaml"
  - id: "dod-infra-pr-1609"
    description: "omnibase_infra PR #1609 carries the OMN-11071 uv sync retry change."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11071/dod-infra-pr-1609/command.yaml
          && grep -q '^pr_number: 1609$' drift/dod_receipts/OMN-11071/dod-infra-pr-1609/command.yaml"
