---
schema_version: "1.0.0"
ticket_id: OMN-7913
title: 'test(coverage): add tests for claude_md_cross_ref scanner (onex_change_control)'
summary: "test(coverage): add tests for claude_md_cross_ref scanner (onex_change_control)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}"
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json mergedAt -q .mergedAt --repo ${REPO}"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --watch --repo ${REPO}"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-005"
    description: "Test coverage meets minimum threshold"
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest --cov --cov-fail-under=80"
  - id: "dod-occ-4325-head"
    description: "OCC PR #4325 exists on dev at exact head 3d99c4fe."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=4325; PRODUCT_REPO=OmniNode-ai/onex_change_control; gh pr view
          \"$PRODUCT_PR_NUMBER\" --repo \"$PRODUCT_REPO\" --json number,headRefOid,headRefName,baseRefName,title"
  - id: "dod-occ-4325-test-only"
    description: "PR #4325 changes only the dedicated claude_md_cross_ref scanner unit test file."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=4325; PRODUCT_REPO=OmniNode-ai/onex_change_control; gh pr view
          \"$PRODUCT_PR_NUMBER\" --repo \"$PRODUCT_REPO\" --json files"
  - id: "dod-occ-4325-local-proof"
    description: "PR #4325 body records 19 passing tests, 94.12 percent target-module coverage, and local
      quality gates."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=4325; PRODUCT_REPO=OmniNode-ai/onex_change_control; gh pr view
          \"$PRODUCT_PR_NUMBER\" --repo \"$PRODUCT_REPO\" --json body --jq '.body' | grep -E '19 passed|94.12%|ruff|pre-commit'"
  - id: "dod-occ-4325-no-deploy"
    description: "PR #4325 is test-only and does not deploy, push images, restart runtime, mutate live
      infrastructure, mutate branch protection, or cut over required contexts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=4325; PRODUCT_REPO=OmniNode-ai/onex_change_control; diff_paths=\"$(gh
          pr diff \"$PRODUCT_PR_NUMBER\" --repo \"$PRODUCT_REPO\" --name-only)\"; printf '%s\\n' \"$diff_paths\"
          | grep -qx 'tests/unit/test_claude_md_cross_ref.py'; test \"$(printf '%s\\n' \"$diff_paths\"
          | wc -l)\" -eq 1"
  - id: "dod-supersede-002-product-open-4325"
    description: "Append-only supersession for the generated merged-to-main check; product PR #4325 is
      open on dev at the exact tested head."
    evidence_artifact: "supersedes_dod_evidence:dod-002"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=4325; PRODUCT_REPO=OmniNode-ai/onex_change_control; gh pr view
          \"$PRODUCT_PR_NUMBER\" --repo \"$PRODUCT_REPO\" --json state,baseRefName,headRefOid --jq '.state
          == \"OPEN\" and .baseRefName == \"dev\" and .headRefOid == \"3d99c4fef431ca4b61aaee909f7afb4fca9f066f\"'
          | grep -q true"
  - id: "dod-supersede-003-occ-cascade-4325"
    description: "Append-only supersession for the generated CI watch check; PR #4325 failures before
      this evidence are the expected missing-receipt/placeholder DoD cascade."
    evidence_artifact: "supersedes_dod_evidence:dod-003"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=4325; PRODUCT_REPO=OmniNode-ai/onex_change_control; gh pr view
          \"$PRODUCT_PR_NUMBER\" --repo \"$PRODUCT_REPO\" --json statusCheckRollup --jq '[.statusCheckRollup[]
          | select((.__typename == \"CheckRun\" and .status == \"COMPLETED\" and (.conclusion | IN(\"SUCCESS\",\"SKIPPED\",\"NEUTRAL\")
          | not)) or (.__typename == \"StatusContext\" and (.state | IN(\"SUCCESS\",\"PENDING\") | not)))
          | select((((.name // .context) + \" \" + (.workflowName // \"\")) | test(\"occ-preflight|Receipt
          Gate|Contract Compliance\")) | not)] | length == 0' | grep -q true"
  - id: "dod-supersede-004-product-precommit-4325"
    description: "Append-only supersession for the generated all-files pre-commit check; PR #4325 body
      records pre-commit and ruff proof for the test-only change."
    evidence_artifact: "supersedes_dod_evidence:dod-004"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=4325; PRODUCT_REPO=OmniNode-ai/onex_change_control; gh pr view
          \"$PRODUCT_PR_NUMBER\" --repo \"$PRODUCT_REPO\" --json body --jq '.body' | grep -E 'pre-commit|ruff'"
  - id: "dod-supersede-005-product-coverage-4325"
    description: "Append-only supersession for the generated coverage command; PR #4325 body records the
      targeted claude_md_cross_ref coverage proof."
    evidence_artifact: "supersedes_dod_evidence:dod-005"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=4325; PRODUCT_REPO=OmniNode-ai/onex_change_control; gh pr view
          \"$PRODUCT_PR_NUMBER\" --repo \"$PRODUCT_REPO\" --json body --jq '.body' | grep -E 'claude_md_cross_ref|19
          passed|94.12%'"
  - id: "occ-self-bind-pr-4325"
    description: "OCC PR #4325 exists and carries OMN-7913 evidence for onex_change_control PR #4325 head
      3d99c4fe."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "PR_NUMBER=4325; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state,headRefName"
