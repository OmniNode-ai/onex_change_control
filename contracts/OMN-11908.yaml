---
schema_version: "1.0.0"
ticket_id: "OMN-11908"
title: "Wave 8 remaining nodes declarative contract migration"
summary: >
  Bind central evidence for omnimarket PR #801, which migrates 33 remaining nodes to declarative handler_routing
  contracts and proves the conflict-resolved rebase. The one-off migration script fix_handler_routing.py
  was removed before final push.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-801"
    description: "Bind OMN-11908 evidence to omnimarket PR #801."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11908/dod-omnimarket-pr/command.yaml
          && grep -q '^pr_number: 801$' drift/dod_receipts/OMN-11908/dod-omnimarket-pr/command.yaml"
  - id: "dod-conflict-resolution-local-gates"
    description: "Conflict-resolved rebase has no conflict markers, evidence JSON conflicts resolved to
      main version."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11908/dod-conflict-resolution-local-gates/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence for the Wave 8 remaining nodes declarative contract migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11908/dod-deploy-readiness/command.yaml
          && grep -q 'deploy readiness' drift/dod_receipts/OMN-11908/dod-deploy-readiness/command.yaml"
