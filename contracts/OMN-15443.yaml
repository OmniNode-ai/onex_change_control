---
schema_version: "1.0.0"
ticket_id: "OMN-15443"
title: "Autobind OCC evidence for OMN-15443"
summary: >
  OCC contract for OmniNode-ai/omnimarket PR #1960, which fail-closes invalid DoD evidence execution scope
  before supersession resolution, command checks, or live GitHub reads.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1960 CI checks green"
    command: "gh pr checks 1960 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1960"
    description: "PR #1960 on OmniNode-ai/omnimarket - Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1960 --repo OmniNode-ai/omnimarket --json number,state,headRefOid"
      - check_type: "command"
        check_value: "gh pr view 1960 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnimarket-pr-1960"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "dod-omnimarket-pr-1960-deploy-falsifiability"
    description: "Deploy-gate falsifiability proof for the OMN-15443 runtime-adjacent evidence collector
      change."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          body="$(gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_dod_verify/services/evidence_collector.py?ref=d26ef0953b29c02c7fb7d9bc18e05d74cfb54903' --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF '_validate_evidence_audiences'
  - id: "occ-self-bind-pr-5624"
    description: "Binds OMN-15443 to OCC companion PR #5624 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5624 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-5639"
    description: "Binds OMN-15443 deploy-evidence amendment PR #5639 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5639 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "occ-self-bind-pr-5671"
    description: "Binds OMN-15443 to OCC companion PR #5671 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5671 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: omn16916-mkt1960-deploy-fals-hs
    description: >-
      Append-only re-assertion of dod-omnimarket-pr-1960-deploy-falsifiability under OMN-16916 AC3 (SIGPIPE
      corpus remediation). The asserted CONTENT is unchanged and still holds -- same symbol needle (_validate_evidence_audiences)
      read from the same pinned evidence_collector.py at ref d26ef0953b29c02c7fb7d9bc18e05d74cfb54903
      through the same GitHub contents API. What was wrong was the SHELL FORM: the compliance runner executes
      check_values under bash -o pipefail -c, and the printf '%s' "$body" | grep -qF idiom is a latent
      exit 141 -- grep -q exits on its first match and closes the pipe, killing printf with SIGPIPE, and
      pipefail surfaces that as 141 even though every assertion actually passed. Measured directly this
      session: 10/10 bash -o pipefail -c runs on the original item's exact bytes exit 141, deterministic
      for this instance. The merged item and its merged receipt are left byte-untouched per this repo's
      append-only evidence discipline; this item re-states the identical assertion with here-string redirection
      (grep -qF '...' <<< "$body"), which has no pipe stage and so cannot be SIGPIPE'd regardless of size
      or match position. Confirmed deterministically safe: 5/5 runs exit 0 under bash -o pipefail -c.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: |-
          body="$(gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_dod_verify/services/evidence_collector.py?ref=d26ef0953b29c02c7fb7d9bc18e05d74cfb54903' --jq .content | base64 -d)" && grep -qF '_validate_evidence_audiences' <<< "$body"
    evidence_artifact: supersedes_dod_evidence:dod-omnimarket-pr-1960-deploy-falsifiability
