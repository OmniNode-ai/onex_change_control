---
schema_version: "1.0.0"
ticket_id: "OMN-11294"
title: "Capture delegate-skill stability runtime hot-patch proof"
summary: >
  Bind omnibase_infra PR #1676 to central OCC evidence for the May 20 delegate-skill stability-runtime
  hot patch. The product PR documents that OMN-11073 was already fixed in source, but the .201 stability
  runtime image had a stale runtime-owned dispatch port until patched and restarted. This contract records
  the product PR head, focused local dispatch-port validation, and live stability-runtime proof.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1676-head"
    description: "omnibase_infra PR #1676 captures the delegate-skill stability hot-patch evidence at
      the recorded head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11294/dod-omnibase-infra-pr-1676-head/command.yaml\""
  - id: "dod-occ-pr-1231-head"
    description: "onex_change_control PR #1231 carries the central OCC contract and receipts for OMN-11294."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11294/dod-occ-pr-1231-head/command.yaml\""
  - id: "dod-focused-dispatch-tests"
    description: "Focused runtime dispatch-port tests passed for the source contract that the hot patch
      restored in stability."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11294/dod-focused-dispatch-tests/command.yaml\""
  - id: "dod-stability-runtime-proof"
    description: "Live .201 stability runtime delegate-skill probes completed through the local model
      and quality gate after the hot patch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11294/dod-stability-runtime-proof/command.yaml\""
