---
schema_version: "1.0.0"
ticket_id: "OMN-13138"
title: "fix(omnimarket): let merge queue choose auto-merge strategy"
summary: >-
  Omnimarket's Auto-Merge workflow armed auto-merge with gh pr merge --auto --squash, but dev is merge-queue
  controlled and rejects explicit merge strategies. The fix removes --squash and uses bare --auto so the
  queue chooses the configured strategy.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Workflow diff is one-line auto-merge command policy change"
    command: "git diff --name-only origin/dev...HEAD"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-auto-merge-proof"
    description: >-
      Reproduction proved gh pr merge --auto --squash is rejected for the dev merge queue, while bare
      gh pr merge --auto enters the queue and lets the queue choose the merge method.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-13138/dod-auto-merge-proof/command.yaml" &&
          grep -q '^ticket_id: "OMN-13138"$' "drift/dod_receipts/OMN-13138/dod-auto-merge-proof/command.yaml"
  - id: "dod-occ-pr-self"
    description: >-
      This onex_change_control OCC PR carries the central OMN-13138 contract and receipts that omnimarket
      PR #1208 resolves via Evidence-Source: OCC#<this-pr> / Evidence-Ticket: OMN-13138.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-13138/dod-occ-pr-self/command.yaml" && grep
          -q '^ticket_id: "OMN-13138"$' "drift/dod_receipts/OMN-13138/dod-occ-pr-self/command.yaml"
