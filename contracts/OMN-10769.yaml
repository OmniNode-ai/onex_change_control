---
schema_version: "1.0.0"
ticket_id: "OMN-10769"
title: "feat(OMN-10769): pure condition evaluator for interactive onboarding transitions"
summary: >
  Adds a pure condition evaluator for onboarding transition guards and covers the not-in branch plus policy
  integration behavior with focused tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1547 carries the OMN-10769 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10769/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Focused unit and integration tests passed for the condition evaluator not-in coverage."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10769/dod-local-tests/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR #886 carries the central OCC contract and receipt update."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10769/dod-occ-pr/command.yaml\""
