---
schema_version: "1.0.0"
ticket_id: OMN-10792
title: "fix(OMN-10792): HandlerComplianceLoop — single-attempt schema-compliance evaluator"
summary: >
  Wave 2 / Task 4 of the tokens-to-compliance epic. Adds HandlerComplianceLoop in node_delegation_orchestrator
  that composes the omnimarket schema-registry, schema-repair, and budget-policy compute nodes into a
  per-attempt evaluator. The delegation orchestrator (Task 5) owns the retry loop. Pure compute, no I/O.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-unit-tests
    description: "All 7 unit tests for HandlerComplianceLoop pass on omnibase_infra branch."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10792/dod-unit-tests/command.yaml
          && grep -q '^ticket_id: OMN-10792$' drift/dod_receipts/OMN-10792/dod-unit-tests/command.yaml"
  - id: dod-occ-pr
    description: "Self-binding receipt for this OCC PR."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10792/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: OMN-10792$' drift/dod_receipts/OMN-10792/dod-occ-pr/command.yaml"
