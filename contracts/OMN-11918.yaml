---
schema_version: "1.0.0"
ticket_id: "OMN-11918"
title: "Architecture graph populate effect node"
summary: >
  Bind central evidence for omnimarket PR #822, which adds the node_architecture_graph_populate_effect
  Memgraph populator and its contract-routed handler, dependency metadata, and golden-chain tests.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-822"
    description: "Bind OMN-11918 evidence to omnimarket PR #822."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11918/dod-omnimarket-pr-822/command.yaml
          && grep -q '^pr_number: 822$' drift/dod_receipts/OMN-11918/dod-omnimarket-pr-822/command.yaml"
  - id: "dod-graph-populate-local-validation"
    description: "Focused architecture graph populate tests and imperative contract guard pass for the
      current omnimarket PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11918/dod-graph-populate-local-validation/command.yaml
          && grep -q '15 passed' drift/dod_receipts/OMN-11918/dod-graph-populate-local-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Deploy gate assessment for the new architecture graph populate effect node."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11918/dod-deploy-assessment/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11918/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11918 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11918/dod-occ-pr/command.yaml"
