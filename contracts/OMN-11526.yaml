---
schema_version: "1.0.0"
ticket_id: "OMN-11526"
title: "docs(OMN-11526): README real vs simplified, setup, and demo modes"
summary: >-
  Restructure README around judge readiness: real-vs-simplified boundaries, one-secret setup, demo mode
  labels, dashboard truth boundary, and replay provenance.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "onex-self-extending-agent PR #108 carries the OMN-11526 README update at the recorded
      head."
    command: "gh pr view 108 --repo OmniNode-ai/onex-self-extending-agent --json number,state,headRefName,headRefOid,url,title"
  - kind: "tests"
    description: "README docs checks and CLI help smoke passed locally."
    command: "git diff --check && PYTHONDONTWRITEBYTECODE=1 uv run python -m src --help"
  - kind: "manual"
    description: "README no longer documents legacy endpoint/model env overrides for judge setup."
    command: "rg -n 'ONEX_TRACK_A_ENDPOINT|ONEX_TRACK_A_MODEL|LLM_CODER_URL|LLM_CODER_MODEL' README.md
      || true"
  - kind: "ci"
    description: "onex_change_control PR carrying this central evidence is bound to OMN-11526."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11526/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-108-open"
    description: "onex-self-extending-agent PR #108 carries OMN-11526 at the recorded head SHA."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11526/dod-product-pr-108-open/command.yaml\"
          && grep -q '^pr_number: 108$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11526/dod-product-pr-108-open/command.yaml\"
          && grep -q '^commit_sha: \"89a03138a346b489e4b146ab72346ccb4f4719cf\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11526/dod-product-pr-108-open/command.yaml\""
  - id: "dod-docs-verification"
    description: "README diff whitespace check and CLI help smoke passed locally."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11526/dod-docs-verification/command.yaml\"
          && grep -q 'onex-demo' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11526/dod-docs-verification/command.yaml\""
  - id: "dod-readme-env-grep"
    description: "README does not document legacy endpoint/model env overrides."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11526/dod-readme-env-grep/command.yaml\"
          && grep -q 'no legacy endpoint/model README env docs remain' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11526/dod-readme-env-grep/command.yaml\""
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OMN-11526 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11526/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number:' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11526/dod-occ-pr/command.yaml\""
