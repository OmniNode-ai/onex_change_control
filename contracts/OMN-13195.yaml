---
schema_version: "1.0.0"
ticket_id: "OMN-13195"
title: "OMN-13195 — A4 delete delegation TOPIC_* constants (+ a reused-number action-pin evidence entry)"
summary: >-
  This contract carries two independent evidence entries that share the OMN-13195 ticket number. (1) The
  canonical A4 work of epic OMN-13188 (Workstream A — all topics from contracts): omnibase_infra PR #2006
  deletes the 13 orphaned TOPIC_DELEGATION_* constants from event_bus/topic_constants.py (zero production
  importers after OMN-13191/OMN-13193), keeps the 3 verified-live constants + 16 DLQ helpers, regenerates
  the topic enums (Topic Enum Drift Check green), fixes the false-green guard with a planted-violation
  regression test, and narrows the topic gate exemptions to a per-line allowlist. (2) A pre-existing,
  already-merged entry for an unrelated action-pin bump (omnibase_core PR #1249, OCC PR #2684) that reused
  this ticket number; preserved verbatim, not modified.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1249"
    description: >-
      Bound receipt for omnibase_core PR #1249, the ticketed replacement for Dependabot PR #1247. (Pre-existing,
      already merged via OCC PR #2684 — preserved, not part of the A4 work.)
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13195/dod-omnibase-core-pr-1249/command.yaml"
          && grep -q '^commit_sha: "98287cb0400454443cc6af5499d9e358901c2b92"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13195/dod-omnibase-core-pr-1249/command.yaml"
  - id: "dod-infra-pr-2006"
    description: >-
      A4 (OMN-13188 Workstream A) evidence for omnibase_infra PR #2006: the 13 orphaned TOPIC_DELEGATION_*
      constants are deleted from event_bus/topic_constants.py (zero production importers proven by per-constant
      grep across all repos under $OMNI_HOME), the 3 verified-live constants (TOPIC_SESSION_COORDINATION_SIGNAL
      + TOPIC_DELEGATE_SKILL_COMPLETED/FAILED codegen AST source) and 16 DLQ helpers kept, the topic enums
      regenerated (Topic Enum Drift Check PASSED, 10 now-unused members dropped), the false-green guard
      fixed + a planted multiline-violation regression test added, and the topic gate exemptions narrowed
      to a per-line allowlist (3 kept literals). Both topic gates green; 717 relevant unit/integration
      tests green. Follow-up OMN-13202 → A5-full OMN-13199.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13195/dod-infra-pr-2006/command.yaml"
          && grep -q 'commit_sha: "52164f36b831b93e2105613f9292007591b52181"' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13195/dod-infra-pr-2006/command.yaml"
