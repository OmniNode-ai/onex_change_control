---
schema_version: "1.0.0"
ticket_id: "OMN-10875"
title: "feat(omnidash): self-service onboarding backend slice — Keycloak provisioning + tenant registry,
  disabled by default"
summary: >-
  Self-service tenant onboarding backend slice for the dashboard: POST /api/onboarding/provision and GET
  /api/onboarding/me, backed by idempotent tenant provisioning keyed on the verified OIDC subject (db/migrations/0002_tenant_onboarding.sql
  — tenants registry table, RLS-enabled mirroring the OMN-13824/ OMN-1636 posture) and a plan/execute
  Keycloak admin client (server/onboarding/keycloak-admin.ts) that never mutates the live realm unless
  an operator explicitly flips onboarding.keycloak_apply_mode to "execute". Onboarding routes mount before
  the OMN-13824 tenant-auth gate because a brand-new user's token carries no tenant claim yet; the routes
  verify bearer tokens themselves against the realm JWKS. Ships fully disabled (onboarding.enabled: "false"
  in contract.yaml) — every endpoint returns 503 and no verifier/pool/admin client is constructed until
  an operator opts in. auth.tenant_mode stays "disabled" — zero behavior change.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnidash-pr-253"
    description: >-
      Consuming-PR binding: omnidash PR #253 (base dev) carries the onboarding backend slice — provisioning
      routes, tenant registry migration, plan/execute Keycloak admin client, contract-driven onboarding
      config (disabled by default), and the CI wiring extending the tenant-RLS integration job to also
      run the onboarding integration/unit suites.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10875/dod-omnidash-pr-253/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-10875 contract and receipts consumed
      by omnidash PR #253.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10875/dod-occ-self/command.yaml"
