---
schema_version: "1.0.0"
ticket_id: "OMN-13037"
title: "feat(OMN-13037): compose-config lint — fail when overlay injects credential for disabled service"
summary: >
  Retro B-9 / PROCESS_FAILURE_RETRO §3.B (OMN-12966 INFISICAL_ADDR pattern). Implements scripts/lint_compose_dangling_deps.py
  in omnibase_infra to statically detect dangling credential/address env vars for services disabled in
  the same compose overlay. Wired as required CI step (compose-config-lint.yml).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "lint script exists; bad fixture exits 1 naming container+env; clean fixture exits 0"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/scripts/test_lint_compose_dangling_deps.py -v"
  - id: "dod-002"
    description: "Real overlay files (stability-test, prod, judge) pass with exit 0"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python scripts/lint_compose_dangling_deps.py docker/docker-compose.stability-test.yml
          docker/docker-compose.prod.yml docker/docker-compose.judge.yml"
  - id: "dod-deploy"
    description: >-
      deploy-gate: CI tooling only — scripts/lint_compose_dangling_deps.py, tests, .github/workflows/compose-config-lint.yml,
      contracts/OMN-13037.yaml. No runtime containers, no database schema changes, no service restarts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "deploy omninode-runtime not required: CI tooling only"
