---
schema_version: "1.0.0"
ticket_id: "OMN-10704"
title: "Contract-config compliance cleanup"
summary: >
  Central OCC evidence for omnimarket PR #663. The first cleanup batch removes the remaining omnimarket
  bus-bypass validator finding by routing the dependency health CI gate through the node package public
  API instead of importing directly from the private handlers package.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Dep-health CI script tests pass."
    command: "See drift/dod_receipts/OMN-10704/dod-focused-tests/command.yaml"
  - kind: "ci"
    description: "Contract-config validator passes for the touched CI script."
    command: "See drift/dod_receipts/OMN-10704/dod-validator-script/command.yaml"
  - kind: "ci"
    description: "Touched files pass ruff and formatting checks."
    command: "See drift/dod_receipts/OMN-10704/dod-lint/command.yaml"
  - kind: "ci"
    description: "Deploy/import smoke for the public node API passes without live deploy or runtime restart."
    command: "See drift/dod_receipts/OMN-10704/dod-deploy/command.yaml"
  - kind: "ci"
    description: "Central OCC PR carries the OMN-10704 contract and receipt bundle."
    command: "See drift/dod_receipts/OMN-10704/dod-occ-pr-1064/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Dep-health CI script tests passed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '7 passed' drift/dod_receipts/OMN-10704/dod-focused-tests/command.yaml"
  - id: "dod-validator-script"
    description: "Contract-config validator passed for the touched CI script."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-10704/dod-validator-script/command.yaml"
  - id: "dod-lint"
    description: "Ruff check and format check passed for touched files."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'All checks passed' drift/dod_receipts/OMN-10704/dod-lint/command.yaml &&
          grep -q '3 files already formatted' drift/dod_receipts/OMN-10704/dod-lint/command.yaml"
  - id: "dod-deploy"
    description: "Import smoke passed for the dep-health node public API; no live deploy, runtime restart,
      or .201 mutation was performed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'HandlerDepHealthSweep' drift/dod_receipts/OMN-10704/dod-deploy/command.yaml"
  - id: "dod-occ-pr-1064"
    description: "onex_change_control PR #1064 carries the central OMN-10704 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10704/dod-occ-pr-1064/command.yaml
          && grep -q '^pr_number: 1064$' drift/dod_receipts/OMN-10704/dod-occ-pr-1064/command.yaml"
