---
schema_version: "1.0.0"
ticket_id: "OMN-14103"
title: "Wire ADR extraction pipeline end-to-end"
summary: >-
  Central OCC evidence for omnimarket PR #1642, which repairs the ADR-extraction canary path by aligning
  orchestrator output with publisher input, allowing discovery manifests to load through production models,
  and writing proposed ADRs in the knowledge-base flat ADR layout.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "omnimarket PR #1642 is scoped to the ADR canary/publisher pipeline repair."
    command: "gh pr view 1642 --repo OmniNode-ai/omnimarket --json files,headRefOid"
  - kind: "manual"
    description: "PR #1642 body records focused ADR pipeline tests and local gates."
    command: "gh pr view 1642 --repo OmniNode-ai/omnimarket --json body"
  - kind: "manual"
    description: "No live deploy or runtime restart is required before merge; paid canary execution is
      explicitly gated."
    command: "gh pr diff 1642 --repo OmniNode-ai/omnimarket --name-only"
  - kind: "manual"
    description: "This OCC PR carries the OMN-14103 contract and PASS receipts."
    command: "gh pr view 3672 --repo OmniNode-ai/onex_change_control --json files,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1642-scope"
    description: >-
      omnimarket PR #1642 is open at the recorded head and changes only ADR canary manifests, ADR pipeline/publisher
      implementation, renderer behavior, and focused ADR pipeline tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14103/dod-omnimarket-pr-1642-scope/command.yaml"
          && grep -q '^pr_number: 1642$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14103/dod-omnimarket-pr-1642-scope/command.yaml"
          && grep -q '^commit_sha: "53f42f3e1d0ac829f455ff8f48544c35187bb774"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14103/dod-omnimarket-pr-1642-scope/command.yaml"
  - id: "dod-omnimarket-pr-1642-proof"
    description: >-
      PR #1642 records focused proof for orchestrator-to-publisher round trip, discovery manifest loading,
      flat ADR publisher layout, and local lint/type/test gates.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14103/dod-omnimarket-pr-1642-proof/command.yaml"
          && grep -q 'orchestrator emits extracted_decisions.json' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14103/dod-omnimarket-pr-1642-proof/command.yaml"
          && grep -q 'discovery_manifest.yaml' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14103/dod-omnimarket-pr-1642-proof/command.yaml"
  - id: "dod-deploy-plan"
    description: >-
      No live deploy is required before merge; running the paid ADR canary remains an operator-cost-gated
      follow-up after the code path lands.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14103/dod-deploy-plan/command.yaml"
          && grep -q 'No live deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14103/dod-deploy-plan/command.yaml"
  - id: "dod-occ-self"
    description: "OCC PR #3672 carries the OMN-14103 central contract and receipt evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14103/dod-occ-self/command.yaml"
          && grep -q '^pr_number: 3672$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14103/dod-occ-self/command.yaml"
