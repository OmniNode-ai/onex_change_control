---
schema_version: "1.0.0"
ticket_id: "OMN-11828"
title: "Harden self-extending agent sandbox response handling"
summary: >
  Central OCC evidence for onex-self-extending-agent PR #128, which tightens generated-handler sandbox
  guidance and guards ADK-wrapped registration responses so only object payloads replace the parsed response
  map.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-onex-self-extending-agent-pr-128"
    description: "onex-self-extending-agent PR #128 carries the OMN-11828 sandbox and ADK response handling
      fix."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11828/dod-onex-self-extending-agent-pr-128/command.yaml
          && grep -q '^pr_number: 128$' drift/dod_receipts/OMN-11828/dod-onex-self-extending-agent-pr-128/command.yaml"
  - id: "dod-local-validation"
    description: "Focused unit tests and lint passed for the ADK unwrap guard."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11828/dod-local-validation/command.yaml
          && grep -q 'pytest tests/unit/test_main.py' drift/dod_receipts/OMN-11828/dod-local-validation/command.yaml
          && grep -q 'ruff check' drift/dod_receipts/OMN-11828/dod-local-validation/command.yaml"
  - id: "dod-coderabbit-thread"
    description: "CodeRabbit ADK unwrap review thread was addressed by guarding decoded result payload
      type."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11828/dod-coderabbit-thread/command.yaml
          && grep -q 'PRRT_kwDOSfXbW86EcyHq' drift/dod_receipts/OMN-11828/dod-coderabbit-thread/command.yaml"
  - id: "dod-occ-pr-1597"
    description: "onex_change_control PR #1597 carries the central OMN-11828 contract and receipt bundle."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11828/dod-occ-pr-1597/command.yaml
          && grep -q '^pr_number: 1597$' drift/dod_receipts/OMN-11828/dod-occ-pr-1597/command.yaml"
