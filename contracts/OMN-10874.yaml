---
schema_version: "1.0.0"
ticket_id: "OMN-10874"
title: "DI refactor: AdapterLlmClassify accepts ProtocolLlmClassifyProvider via injection"
summary: >
  AdapterLlmClassify now accepts ProtocolLlmClassifyProvider via provider= kwarg instead of always constructing
  AdapterLlmProviderOpenai from env. Env-driven construction remains the fallback when provider=None.
  Part of OMN-10841 DI Wave 2 refactor.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests-pass"
    description: "5 new unit tests cover injected provider used, env not required when injected, missing
      env + no provider raises, keyword fallback with failing provider, close() delegation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10874/dod-unit-tests-pass/command.yaml\""
  - id: "dod-omnimarket-pr-692"
    description: "omnimarket PR #692 open with CI green at commit e4ec50b8ceed48406955d7f187a77664befb20c7."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10874/dod-omnimarket-pr-692/command.yaml\"
          && grep -q '^pr_number: 692$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10874/dod-omnimarket-pr-692/command.yaml\""
  - id: "dod-occ-pr-1120"
    description: "onex_change_control PR #1120 carries the OMN-10874 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10874/dod-occ-pr-1120/command.yaml\"
          && grep -q '^pr_number: 1120$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10874/dod-occ-pr-1120/command.yaml\""
  - id: "dod-deploy"
    description: "No runtime deploy required; pure code seam refactor with no behaviour change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10874/dod-deploy/command.yaml\""
