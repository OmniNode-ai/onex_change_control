---
schema_version: "1.0.0"
ticket_id: "OMN-11752"
title: "Fix OCC rerun downstream protected-main state update"
summary: >
  Updates occ-rerun-downstream so post-merge rerun state changes are committed to an automation branch
  and opened as a pull request instead of direct-pushed to protected main.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-workflow-pr-path"
    description: "occ-rerun-downstream workflow parses and routes state updates through a PR path"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "python3 -c \"import yaml; yaml.safe_load(open('.github/workflows/occ-rerun-downstream.yml'));
          print('OK')\""
  - id: "dod-occ-rerun-state-pr-1390"
    description: "Generated OCC rerun state update PR #1390 is normalized and PR-bound."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^edges: \\[\\]$' drift/occ_dependency_edges.yaml && grep -q '^schema_version:
          1.0.0$' drift/occ_dependency_edges.yaml"
