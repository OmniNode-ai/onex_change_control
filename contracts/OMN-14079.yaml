---
schema_version: "1.0.0"
ticket_id: "OMN-14079"
title: "ci(OMN-14079): remove merge_group triggers from non-required omnimarket workflows"
summary: >-
  omnimarket PR #1640 removes merge_group triggers from 11 workflows that do not carry required dev branch
  protection contexts. Pull request, push, and workflow_dispatch coverage remains intact, while merge
  queue fan-out is reduced for non-required checks.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Workflow fan-out reduction proof is documented on omnimarket PR #1640"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14079/dod-local-verification/command.yaml"
  - kind: "manual"
    description: "omnimarket PR #1640 carries the OMN-14079 workflow trigger-only change"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14079/dod-omnimarket-pr-1640/command.yaml"
  - kind: "manual"
    description: "No live runtime deploy is in scope for this CI trigger reduction"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14079/dod-deploy-scope/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-local-verification"
    description: >-
      Local and PR-body verification confirms the edited workflows are non-required merge_group carriers,
      required context carrier workflows are kept, and the change is limited to trigger fan-out reduction.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14079/dod-local-verification/command.yaml"
  - id: "dod-omnimarket-pr-1640"
    description: >-
      Consuming-PR binding: omnimarket PR #1640 at head bb7cdef5e49035ba1054b71013bd8422023e2e44 carries
      the OMN-14079 workflow trigger reduction.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14079/dod-omnimarket-pr-1640/command.yaml"
  - id: "dod-deploy-scope"
    description: >-
      Deploy-scope binding: this is a GitHub Actions trigger-only change and performs no live runtime,
      host, runner, Docker, Redpanda, AWS, SSH, or production deployment mutation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14079/dod-deploy-scope/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-14079 central contract and
      PASS receipts consumed by omnimarket PR #1640 via its Evidence-Source line.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14079/dod-occ-self/command.yaml"
