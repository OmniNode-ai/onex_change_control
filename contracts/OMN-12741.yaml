---
schema_version: "1.0.0"
ticket_id: "OMN-12741"
title: "chore(demo): capture clean Claude-Code-delegate-FROM demo run + evidence"
summary: >-
  Central evidence for a clean /onex:delegate run captured end-to-end for the Monday hackathon demo. Run
  goes through node_delegate_skill_orchestrator. Correlation id + output captured. Prior proven local-tier
  delegation correlation 6ae9f65c (Qwen, useful pytest output, quality gate passed) is the known-good
  baseline. Optionally surfaced in dashboard via P0-1 DelegationModelOutputWidget if dash surfacing is
  wanted. Low risk; just needs a clean run captured.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-clean-delegate-run"
    description: "One clean /onex:delegate run captured end-to-end. Correlation id + generated output
      captured. Run confirmed to go through node_delegate_skill_orchestrator."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12741/dod-clean-delegate-run/command.yaml"
  - id: "dod-orchestrator-routing-proof"
    description: "Evidence that the run routes through node_delegate_skill_orchestrator. Runtime log excerpt
      or correlation id trace showing the orchestrator handling the request."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12741/dod-orchestrator-routing-proof/command.yaml"
  - id: "dod-occ-pr-evidence"
    description: "OCC PR carries the OMN-12741 central contract and PASS receipts."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12741/dod-occ-pr-evidence/command.yaml"
