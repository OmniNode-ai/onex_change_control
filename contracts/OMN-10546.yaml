---
schema_version: "1.0.0"
ticket_id: "OMN-10546"
title: "epic: public-shippable SecretStore evidence"
summary: "Umbrella OCC evidence contract for the Epic 2 SecretStore Protocol and Adapters batch. The implementation
  contracts live under OMN-10556 through OMN-10559 and this contract binds the evidence PR that carries
  them."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Receipt Gate passes on this OCC evidence PR and the downstream SecretStore PRs cite
      it."
    command: "gh pr checks 741 --repo OmniNode-ai/onex_change_control --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "OCC PR carries contracts and PASS receipts for OMN-10556, OMN-10557, OMN-10558, and
      OMN-10559."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f \"$CONTRACT_REPO_DIR/contracts/OMN-10556.yaml\" && test -f \"$CONTRACT_REPO_DIR/contracts/OMN-10557.yaml\"
          && test -f \"$CONTRACT_REPO_DIR/contracts/OMN-10558.yaml\" && test -f \"$CONTRACT_REPO_DIR/contracts/OMN-10559.yaml\"
          && grep -R -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10556\" && grep -R
          -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10557\" && grep -R -q '^status:
          PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10558\" && grep -R -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10559\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10546 umbrella contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10546/dod-occ-pr/command.yaml\""
