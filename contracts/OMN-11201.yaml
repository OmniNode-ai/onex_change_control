---
schema_version: "1.0.0"
ticket_id: "OMN-11201"
title: "Add data_provenance to projection tables"
summary: >
  Migration adding the data_provenance column (EnumDataProvenance) to existing projection tables in omnibase_infra.
  Establishes the schema foundation for provenance tracking across projections (part of OMN-11188).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1639 carries the implementation."
    command: "See drift/dod_receipts/OMN-11201/dod-infra-pr-1639/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1639"
    description: "omnibase_infra PR #1639 carries the OMN-11201 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11201/dod-infra-pr-1639/command.yaml
          && grep -q '^pr_number: 1639$' drift/dod_receipts/OMN-11201/dod-infra-pr-1639/command.yaml"
  - id: "dod-deploy-provenance-migration"
    description: "Post-deploy: data_provenance column present on contracts/topics/registration_projections
      tables."
    source: "manual"
    checks:
      - check_type: "command"
        # deploy keyword present so the runtime deploy-gate (OMN-8912) recognizes this contract
        # as deploy-validated for runtime-path PRs (OMN-11201 modifies reducer + router paths under nodes/).
        check_value: "docker exec omnibase-infra-postgres psql -U postgres -d omnibase_infra -c '\\d contracts'
          | grep -q data_provenance"
