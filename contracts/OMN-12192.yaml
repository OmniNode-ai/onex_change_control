---
schema_version: "1.0.0"
ticket_id: "OMN-12192"
title: "Migrate three evidence models from compat to core"
summary: >
  Central OCC evidence for omnibase_core PR #1160. The change confirms the canonical evidence contract
  models live in omnibase_core and adds Pydantic standards coverage for ModelEvidenceProvenance, ModelContractEvidenceProof,
  and ModelContractEvidenceSpec.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Evidence contract model unit tests pass on the PR head."
    command: "See drift/dod_receipts/OMN-12192/dod-evidence-model-tests/command.yaml"
  - kind: "tests"
    description: "Targeted ruff format and lint checks pass for touched evidence model files."
    command: "See drift/dod_receipts/OMN-12192/dod-targeted-ruff/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carries the central OMN-12192 contract and receipts."
    command: "See drift/dod_receipts/OMN-12192/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-evidence-model-tests"
    description: "Evidence contract model unit tests passed on the omnibase_core PR #1160 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12192/dod-evidence-model-tests/command.yaml
          && grep -q '12 passed' drift/dod_receipts/OMN-12192/dod-evidence-model-tests/command.yaml"
  - id: "dod-targeted-ruff"
    description: "Targeted ruff checks passed for the touched evidence model files."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12192/dod-targeted-ruff/command.yaml
          && grep -q 'All checks passed' drift/dod_receipts/OMN-12192/dod-targeted-ruff/command.yaml"
  - id: "dod-occ-pr"
    description: "This onex_change_control PR carries the central OMN-12192 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12192/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: \"OMN-12192\"$' drift/dod_receipts/OMN-12192/dod-occ-pr/command.yaml"
