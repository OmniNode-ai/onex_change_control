---
schema_version: "1.0.0"
ticket_id: "OMN-15014"
title: "Autobind OCC evidence for OMN-15014"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #413."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #413 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimemory-pr-413"
    description: "PR #413 on OmniNode-ai/omnimemory — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimemory-pr-413-ci"
    description: "PR #413 on OmniNode-ai/omnimemory — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4704"
    description: "OCC companion PR #4704 — self-bind for OMN-15014 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimemory-413-r2-head"
    description: "PR #413 on OmniNode-ai/omnimemory — exact repaired head after PLR0917 policy alignment."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 413 --repo OmniNode-ai/omnimemory --json number,state,headRefOid,headRefName"
  - id: "dod-omnimemory-413-r2-files"
    description: "PR #413 on OmniNode-ai/omnimemory — product diff scope after repair includes workflow
      wiring plus pyproject lint policy."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 413 --repo OmniNode-ai/omnimemory --json files"
  - id: "dod-omnimemory-413-r2-local"
    description: "PR #413 on OmniNode-ai/omnimemory — hosted status readback after the PLR0917 policy
      repair."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 413 --repo OmniNode-ai/omnimemory --json headRefOid,mergeStateStatus,statusCheckRollup
          --jq 'if .headRefOid == \"88bf80a11cf95c7e38930372fc9ca1a29c50bbcb\" and ([.statusCheckRollup[]?
          | select(.conclusion == \"FAILURE\")] | length) == 0 then \"omnimemory#413 repaired head has
          no failed hosted checks\" else error(\"omnimemory#413 repaired head is stale or has failed hosted
          checks\") end'"
  - id: "occ-self-bind-pr-4713"
    description: "OCC companion PR #4713 — self-bind for OMN-15014 r2 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4713 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
