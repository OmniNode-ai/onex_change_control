---
schema_version: "1.0.0"
ticket_id: "OMN-14456"
title: "test(OMN-14456): pin OCC receipt PR/commit dual binding regression contract"
summary: >-
  omnibase_core PR #1438 adds regression coverage for the already-shipped _receipt_bound_to_pr dual-accept
  behavior. The production predicate already accepts a receipt bound by either the PR number or by one
  of the PR commit SHAs. This ticket's landing requirement is tests-only: pin commit-sha-only and pr-number-only
  binding as independently eligible so a future accidental tightening back to AND fails locally and in
  CI.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1438-dual-bind-regression"
    description: >-
      omnibase_core PR #1438 head 3bd25b06ba30136172b9678380961548109b0e48 changes only tests/unit/validation/test_occ_merge_eligibility.py.
      Focused validation passed with 21/21 tests, including the new commit_sha-only and pr_number-only
      eligible cases plus the existing neither-bound reject case.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1438 PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr checks "$PRODUCT_PR_NUMBER"
          --repo "$PRODUCT_REPO" --json name,state --jq 'if ([.[] | select((.name == "Tests Gate" or .name
          == "CI Summary") and .state == "SUCCESS")] | length) == 2 then "ok" else error("required CI
          checks are not green") end'
  - id: "dod-deploy-assessment"
    description: >-
      Deploy-surface assessment for omnibase_core PR #1438. The product diff is limited to a unit test
      file and does not add or alter runtime code, contracts, migrations, topics, dependencies, service
      deployment surfaces, or live .201 state.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1438 PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr view "$PRODUCT_PR_NUMBER"
          --repo "$PRODUCT_REPO" --json files --jq 'if ([.files[].path] == ["tests/unit/validation/test_occ_merge_eligibility.py"])
          then "ok" else error("unexpected deploy surface") end'
  - id: "dod-occ-self-4122"
    description: >-
      OCC companion self-binding for onex_change_control PR #4122. This binding item asserts that the
      OCC PR carries the central OMN-14456 contract and receipts consumed by omnibase_core PR #1438 through
      its Evidence-Source line; substantive product evidence is carried by the two preceding DoD items.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          OCC_PR_NUMBER=4122 OCC_REPO=OmniNode-ai/onex_change_control; test "$(gh pr view "$OCC_PR_NUMBER"
          --repo "$OCC_REPO" --json headRefName --jq .headRefName)" = "jonah/omn-14456-occ-core-1438-dual-bind"
