---
schema_version: 1.0.0
ticket_id: OMN-9857
title: "Rollout activation criteria for change-aware test selection (shadow phase, gate, rollback triggers)
  — canonical impl in omnibase_core PR #924 adds scripts/ci/test_selection_rollout.md"
summary: "Rollout activation criteria for change-aware test selection (shadow phase, gate, rollback triggers)
  — canonical impl in omnibase_core PR #924 adds scripts/ci/test_selection_rollout.md"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'omnibase_core PR #924 CI checks pass'
    command: gh pr checks 924 --repo OmniNode-ai/omnibase_core --watch
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "Rollout doc exists at scripts/ci/test_selection_rollout.md with the three required sections:
      Shadow phase, Activation gate, Rollback trigger — receipt at drift/dod_receipts/OMN-9857/dod-001/"
    source: generated
    checks:
      - check_type: command
        check_value: grep -E '^## (Shadow phase|Activation gate|Rollback trigger)' scripts/ci/test_selection_rollout.md
  - id: dod-002
    description: "Activation gate enumerates exactly 4 numbered conditions (1., 2., 3., 4.) — measurable,
      no subjective qualifiers — receipt at drift/dod_receipts/OMN-9857/dod-002/"
    source: generated
    checks:
      - check_type: command
        check_value: grep -cE '^[1-4]\.' scripts/ci/test_selection_rollout.md
  - id: dod-003
    description: "Miss-rate threshold is a literal numeric fraction (<= 0.01), not a phrase — receipt
      at drift/dod_receipts/OMN-9857/dod-003/"
    source: generated
    checks:
      - check_type: command
        check_value: grep -F '<= 0.01' scripts/ci/test_selection_rollout.md
