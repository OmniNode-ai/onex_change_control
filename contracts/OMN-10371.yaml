---
schema_version: '1.0.0'
ticket_id: OMN-10371
title: "Task 19: [Wave 5 AST Diff] Add EnumDiffSeverity and result models"
summary: "Add EnumDiffSeverity, EnumChangeKind, ModelSymbolChange, and ModelSemanticDiffReport to omnibase_core
  as foundation types for Wave 5 AST semantic diff"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Unit tests for semantic diff models pass (run in omnibase_core repo)"
    command: "gh pr checks 1003 --repo OmniNode-ai/omnibase_core"
  - kind: ci
    description: "mypy --strict and ruff check pass (verified via receipts)"
    command: "grep -q 'status: PASS' drift/dod_receipts/OMN-10371/dod-002/command.yaml && grep -q 'status:
      PASS' drift/dod_receipts/OMN-10371/dod-003/command.yaml"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "9 unit tests pass — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10371/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-10371$' drift/dod_receipts/OMN-10371/dod-001/command.yaml && grep -q '^evidence_item_id:
          dod-001$' drift/dod_receipts/OMN-10371/dod-001/command.yaml"
  - id: dod-002
    description: "mypy --strict clean — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10371/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-10371$' drift/dod_receipts/OMN-10371/dod-002/command.yaml && grep -q '^evidence_item_id:
          dod-002$' drift/dod_receipts/OMN-10371/dod-002/command.yaml"
  - id: dod-003
    description: "ruff check passes — verified via receipt file"
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10371/dod-003/command.yaml && grep
          -q '^ticket_id: OMN-10371$' drift/dod_receipts/OMN-10371/dod-003/command.yaml && grep -q '^evidence_item_id:
          dod-003$' drift/dod_receipts/OMN-10371/dod-003/command.yaml"
