---
schema_version: 1.0.0
ticket_id: OMN-14656
title: "feat(OMN-14656): RSD canary — sync file-gather EFFECT + no-utcnow ValidatorNode COMPUTE (revive
  OMN-2362)"
summary: 'Central OCC evidence for omnibase_core PR #1446, an RSD canary reviving the OMN-2362 Generic
  Validator Node Architecture as an EFFECT/COMPUTE split: node_source_file_gather_effect (EFFECT, sole
  filesystem I/O boundary) and node_no_utcnow_check_compute (COMPUTE, pure AST scan porting UtcNowVisitor
  byte-for-byte), plus the OMN-13305-pattern remote pre-commit hook (check-no-utcnow) wiring for consumer
  repos. Bound at head a70a8769f22a622c40092bde35abe0e76dece58a.'
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnibase-core-pr-1446-product-head
    description: 'Product-head verification for omnibase_core PR #1446 at a70a8769f22a622c40092bde35abe0e76dece58a.
      The proof reads live PR metadata and changed files from GitHub, then checks the branch is based
      on dev, carries the expected OMN-14656 title, and includes the new EFFECT/COMPUTE node handler,
      runtime, visitor, and focused regression test surfaces.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: "PRODUCT_PR_NUMBER=1446; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=a70a8769f22a622c40092bde35abe0e76dece58a;
          d=\"$(mktemp -d)\" && gh pr view \"${PRODUCT_PR_NUMBER}\" --repo \"${PRODUCT_REPO}\" --json
          headRefOid,baseRefName,title,files --jq '.headRefOid + \"\\n\" + .baseRefName + \"\\n\" + .title
          + \"\\n\" + ((.files | map(.path)) | join(\"\\n\"))' > \"${d}/pr.txt\" && grep -Fq \"${PRODUCT_HEAD}\"
          \"${d}/pr.txt\" && grep -Fxq dev \"${d}/pr.txt\" && grep -Fq 'feat(OMN-14656): RSD canary —
          sync file-gather EFFECT + no-utcnow ValidatorNode COMPUTE (revive OMN-2362)' \"${d}/pr.txt\"
          && for p in src/omnibase_core/nodes/node_no_utcnow_check_compute/handler.py src/omnibase_core/nodes/node_no_utcnow_check_compute/runtime_no_utcnow_check.py
          src/omnibase_core/nodes/node_no_utcnow_check_compute/visitor_utcnow.py src/omnibase_core/nodes/node_source_file_gather_effect/handler.py
          tests/unit/nodes/node_no_utcnow_check_compute/test_node_no_utcnow_check_compute.py tests/unit/nodes/node_no_utcnow_check_compute/test_runtime_no_utcnow_check.py
          tests/unit/nodes/node_source_file_gather_effect/test_node_source_file_gather_effect.py; do grep
          -Fq \"$p\" \"${d}/pr.txt\"; done"
  - id: dod-omnibase-core-pr-1446-local-proof
    description: "Hosted-portable local-proof verification for omnibase_core PR #1446. Reads the live
      PR body (not a local Mac-path artifact — avoiding the OMN-14634/OCC#4218 defect that this contract
      intentionally does not repeat) and asserts it carries the specific evidence markers for the 39-passed
      unit/characterization run, the mypy --strict 15-source-file clean result, the 0-Failed-lines pre-commit
      run, the clean --all-files check-no-utcnow hook run, and the shadow-compare-identical DirectoryTraverser
      oracle match."
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1446; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=a70a8769f22a622c40092bde35abe0e76dece58a;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid --jq ''.headRefOid
          == "''${PRODUCT_HEAD}''"'' | grep -qx true && BODY="$(gh pr view "${PRODUCT_PR_NUMBER}" --repo
          "${PRODUCT_REPO}" --json body --jq ''.body'')" && echo "$BODY" | grep -Fq ''39 passed'' && echo
          "$BODY" | grep -Fq ''Success: no issues found in 15 source files'' && echo "$BODY" | grep -Fq
          ''0 Failed lines'' && echo "$BODY" | grep -Fq ''clean (0 violations in `src/`)'' && echo "$BODY"
          | grep -Fq ''every comparison IDENTICAL'''
  - id: dod-omnibase-core-pr-1446-deploy-scope
    description: 'Deploy-scope audit for omnibase_core PR #1446. The diff is limited to omnibase_core
      node/model/test source plus root-level dependency and pre-commit-hook config (pyproject.toml, uv.lock,
      .pre-commit-config.yaml, .pre-commit-hooks.yaml); it does not perform a live deploy, restart, .201
      mutation, systemd change, or docker-compose change.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=1446; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=a70a8769f22a622c40092bde35abe0e76dece58a;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,files --jq '.headRefOid
          == "'${PRODUCT_HEAD}'" and ((.files | map(.path)) | all((startswith("src/omnibase_core/") or
          startswith("tests/") or (. == ".pre-commit-config.yaml") or (. == ".pre-commit-hooks.yaml")
          or (. == "pyproject.toml") or (. == "uv.lock")) and (. | test("(^|/)(systemd|deploy/scripts|docker/compose)(/|$)")
          | not)))' | grep -qx true
  - id: dod-occ-self-bind-pr-4250
    description: 'OCC companion self-binding for onex_change_control PR #4250. Verifies the PR is on the
      expected OMN-14656 evidence branch and contains initial evidence commit 3bba9819eb136272616ce6aaed7797947eec0228.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: OCC_PR_NUMBER=4250; OCC_REPO=OmniNode-ai/onex_change_control; OCC_BRANCH=jonah/omn-14656-occ-core-1446;
          INITIAL_EVIDENCE_COMMIT=3bba9819eb136272616ce6aaed7797947eec0228; gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}"
          --jq '.head.ref == "'$OCC_BRANCH'" and ((.state == "open") or (.state == "closed" and .merged_at
          != null))' | grep -qx true && gh api "repos/${OCC_REPO}/pulls/${OCC_PR_NUMBER}/commits" --jq
          '.[].sha' | grep -Fx "$INITIAL_EVIDENCE_COMMIT"
  - id: dod-OmniNode-ai-omnibase_core-pr-1446
    description: "PR #1446 on OmniNode-ai/omnibase_core — Evidence-Source autobind for current head."
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1446; : ${PR_NUMBER}; gh pr view $PRODUCT_PR_NUMBER --repo OmniNode-ai/omnibase_core
          --json number,state'
  - id: dod-OmniNode-ai-omnibase_core-pr-1446-ci
    description: "PR #1446 on OmniNode-ai/omnibase_core — product diff scope for current head."
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1446; : ${PR_NUMBER}; gh pr diff $PRODUCT_PR_NUMBER --repo OmniNode-ai/omnibase_core
          --name-only | grep -q .'
  - id: dod-core1446-product-872c430b
    description: 'Product-head verification for omnibase_core PR #1446 at 872c430bee3d2553fdad538c0524593c9955a9f7.
      The proof reads live PR metadata and changed files from GitHub, then checks the branch is based
      on dev, carries the expected OMN-14656 title, and includes the new EFFECT/COMPUTE node handler,
      runtime, visitor, and focused regression test surfaces.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: "PRODUCT_PR_NUMBER=1446; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=872c430bee3d2553fdad538c0524593c9955a9f7;
          d=\"$(mktemp -d)\" && gh pr view \"${PRODUCT_PR_NUMBER}\" --repo \"${PRODUCT_REPO}\" --json
          headRefOid,baseRefName,title,files --jq '.headRefOid + \"\\n\" + .baseRefName + \"\\n\" + .title
          + \"\\n\" + ((.files | map(.path)) | join(\"\\n\"))' > \"${d}/pr.txt\" && grep -Fq \"${PRODUCT_HEAD}\"
          \"${d}/pr.txt\" && grep -Fxq dev \"${d}/pr.txt\" && grep -Fq 'feat(OMN-14656): RSD canary —
          sync file-gather EFFECT + no-utcnow ValidatorNode COMPUTE (revive OMN-2362)' \"${d}/pr.txt\"
          && for p in src/omnibase_core/nodes/node_no_utcnow_check_compute/handler.py src/omnibase_core/nodes/node_no_utcnow_check_compute/runtime_no_utcnow_check.py
          src/omnibase_core/nodes/node_no_utcnow_check_compute/visitor_utcnow.py src/omnibase_core/nodes/node_source_file_gather_effect/handler.py
          tests/unit/nodes/node_no_utcnow_check_compute/test_node_no_utcnow_check_compute.py tests/unit/nodes/node_no_utcnow_check_compute/test_runtime_no_utcnow_check.py
          tests/unit/nodes/node_source_file_gather_effect/test_node_source_file_gather_effect.py; do grep
          -Fq \"$p\" \"${d}/pr.txt\"; done"
    evidence_artifact: supersedes_dod_evidence:dod-omnibase-core-pr-1446-product-head
  - id: dod-core1446-local-872c430b
    description: "Hosted-portable local-proof verification for omnibase_core PR #1446. Reads the live
      PR body (not a local Mac-path artifact — avoiding the OMN-14634/OCC#4218 defect that this contract
      intentionally does not repeat) and asserts it carries the specific evidence markers for the 39-passed
      unit/characterization run, the mypy --strict 15-source-file clean result, the 0-Failed-lines pre-commit
      run, the clean --all-files check-no-utcnow hook run, and the shadow-compare-identical DirectoryTraverser
      oracle match."
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1446; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=872c430bee3d2553fdad538c0524593c9955a9f7;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid --jq ''.headRefOid
          == "''${PRODUCT_HEAD}''"'' | grep -qx true && BODY="$(gh pr view "${PRODUCT_PR_NUMBER}" --repo
          "${PRODUCT_REPO}" --json body --jq ''.body'')" && echo "$BODY" | grep -Fq ''39 passed'' && echo
          "$BODY" | grep -Fq ''Success: no issues found in 15 source files'' && echo "$BODY" | grep -Fq
          ''0 Failed lines'' && echo "$BODY" | grep -Fq ''clean (0 violations in `src/`)'' && echo "$BODY"
          | grep -Fq ''every comparison IDENTICAL'''
    evidence_artifact: supersedes_dod_evidence:dod-omnibase-core-pr-1446-local-proof
  - id: dod-core1446-deploy-872c430b
    description: 'Deploy-scope audit for omnibase_core PR #1446. The diff is limited to omnibase_core
      node/model/test source plus root-level dependency and pre-commit-hook config (pyproject.toml, uv.lock,
      .pre-commit-config.yaml, .pre-commit-hooks.yaml); it does not perform a live deploy, restart, .201
      mutation, systemd change, or docker-compose change.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=1446; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=872c430bee3d2553fdad538c0524593c9955a9f7;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,files --jq '.headRefOid
          == "'${PRODUCT_HEAD}'" and ((.files | map(.path)) | all((startswith("src/omnibase_core/") or
          startswith("tests/") or (. == ".pre-commit-config.yaml") or (. == ".pre-commit-hooks.yaml")
          or (. == "pyproject.toml") or (. == "uv.lock")) and (. | test("(^|/)(systemd|deploy/scripts|docker/compose)(/|$)")
          | not)))' | grep -qx true
    evidence_artifact: supersedes_dod_evidence:dod-omnibase-core-pr-1446-deploy-scope
  - id: dod-core1446-product-bb9d0790
    description: 'Product-head verification for omnibase_core PR #1446 at bb9d0790ad2e426cca4b670f45543d65da3dcea0.
      The proof reads live PR metadata and changed files from GitHub, then checks the branch is based
      on dev, carries the expected OMN-14656 title, and includes the CodeRabbit follow-up runtime, source-gather
      reason model, and regression test surfaces.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: "PRODUCT_PR_NUMBER=1446; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=bb9d0790ad2e426cca4b670f45543d65da3dcea0;
          d=\"$(mktemp -d)\" && gh pr view \"${PRODUCT_PR_NUMBER}\" --repo \"${PRODUCT_REPO}\" --json
          headRefOid,baseRefName,title,files --jq '.headRefOid + \"\\n\" + .baseRefName + \"\\n\" + .title
          + \"\\n\" + ((.files | map(.path)) | join(\"\\n\"))' > \"${d}/pr.txt\" && grep -Fq \"${PRODUCT_HEAD}\"
          \"${d}/pr.txt\" && grep -Fxq dev \"${d}/pr.txt\" && grep -Fq 'feat(OMN-14656): RSD canary —
          sync file-gather EFFECT + no-utcnow ValidatorNode COMPUTE (revive OMN-2362)' \"${d}/pr.txt\"
          && for p in src/omnibase_core/models/nodes/source_file_gather/model_skipped_source_file.py src/omnibase_core/nodes/node_no_utcnow_check_compute/runtime_no_utcnow_check.py
          tests/unit/nodes/node_no_utcnow_check_compute/test_runtime_no_utcnow_check.py; do grep -Fq \"$p\"
          \"${d}/pr.txt\"; done"
    evidence_artifact: supersedes_dod_evidence:dod-core1446-product-872c430b
  - id: dod-core1446-local-bb9d0790
    description: "Hosted-portable local-proof verification for omnibase_core PR #1446 after the CodeRabbit
      follow-up. Reads the live PR body and asserts it records the focused 25-test pass, ruff pass, and
      unreadable-file ERROR behavior."
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1446; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=bb9d0790ad2e426cca4b670f45543d65da3dcea0;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid --jq ''.headRefOid
          == "''${PRODUCT_HEAD}''"'' | grep -qx true && BODY="$(gh pr view "${PRODUCT_PR_NUMBER}" --repo
          "${PRODUCT_REPO}" --json body --jq ''.body'')" && echo "$BODY" | grep -Fq ''25 passed'' && echo
          "$BODY" | grep -Fq ''ruff format/check passed'' && echo "$BODY" | grep -Fq ''unreadable Python
          files now return ERROR'''
    evidence_artifact: supersedes_dod_evidence:dod-core1446-local-872c430b
  - id: dod-core1446-deploy-bb9d0790
    description: 'Deploy-scope audit for omnibase_core PR #1446 after the CodeRabbit follow-up. The diff
      remains limited to omnibase_core node/model/test source plus root-level dependency and pre-commit-hook
      config (pyproject.toml, uv.lock, .pre-commit-config.yaml, .pre-commit-hooks.yaml); it does not perform
      a live deploy, restart, .201 mutation, systemd change, or docker-compose change.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=1446; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=bb9d0790ad2e426cca4b670f45543d65da3dcea0;
          gh pr view "${PRODUCT_PR_NUMBER}" --repo "${PRODUCT_REPO}" --json headRefOid,files --jq '.headRefOid
          == "'${PRODUCT_HEAD}'" and ((.files | map(.path)) | all((startswith("src/omnibase_core/") or
          startswith("tests/") or (. == ".pre-commit-config.yaml") or (. == ".pre-commit-hooks.yaml")
          or (. == "pyproject.toml") or (. == "uv.lock")) and (. | test("(^|/)(systemd|deploy/scripts|docker/compose)(/|$)")
          | not)))' | grep -qx true
    evidence_artifact: supersedes_dod_evidence:dod-core1446-deploy-872c430b
  - id: occ-self-bind-pr-4253
    description: "OCC companion PR #4253 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14656
      (OMN-14650)."
    source: generated
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: occ-self-bind-pr-4255
    description: "OCC follow-up PR #4255 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14656
      legacy receipt supersession rebind."
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state
  - id: "occ-self-bind-pr-4256"
    description: "OCC companion PR #4256 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14656
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
