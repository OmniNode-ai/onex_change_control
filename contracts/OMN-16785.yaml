---
schema_version: "1.0.0"
ticket_id: "OMN-16785"
title: "Autobind OCC evidence for OMN-16785"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1032."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1032 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1032"
    description: "PR #1032 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1032 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1032-ci"
    description: "PR #1032 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1032 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1032"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7319"
    description: "OCC companion PR #7319 — self-bind for OMN-16785 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7319 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1038"
    description: "PR #1038 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1038 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1038-ci"
    description: "PR #1038 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1038 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-7365"
    description: "OCC companion PR #7365 — self-bind for OMN-16785 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7365 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OMN-16785-ac1-ac3-tenant-delete-grant-ratchet"
    description: "AC1 + AC2 + AC3, made falsifiable. Every other item on this contract is exit-status-invariant
      over the product change — two bare `gh pr view 1032` probes, OCC's own tests/test_evidence_admissibility.py,
      and bare self-binds — so the whole corpus stays green with omninode_infra#1032 reverted. This item
      executes the suite that landed with omninode_infra#1038 against the real aws/cluster-dev HCL: the
      OMN16255TenantTopicDeleteNarrow statement is still declared with the Sid live IAM carries (so `terraform
      plan` converges on the existing statement rather than proposing to replace it — the property AC1's
      readback checks), its Action is exactly ['kafka-cluster:DeleteTopic'], its Resource iterates var.managed_kafka_client_topic_delete_patterns
      and never the read/write list, the variable default is the live ['tenant-*.onex.*'], delete patterns
      are a PROPER subset of the read/write patterns, the wildcard waiver precondition survives, aws/iam/README.md
      still records the grant and every granted pattern (AC2), and no kafka-cluster:AlterTopic / AlterTopicDynamicConfiguration
      / DescribeTopicDynamicConfiguration grant exists anywhere under aws/ (AC3), with a negative control
      proving the AC3 scanner can still see granted actions at all. Falsifiability executed, not asserted:
      against the pre-#1032 tree (f98003d^) 5 failed / 3 errors; with kafka-cluster:AlterTopic injected
      into the topic read/write statement the AC3 leg goes red. Scoped local_done_gate because the suite
      lives in omninode_infra and hosted OCC compliance executes inside the OCC checkout (OMN-15392)."
    source: "manual"
    execution_scope: "local_done_gate"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_msk_tenant_topic_delete_grant.py -q"
        cwd: "${OMNI_HOME}/omninode_infra"
