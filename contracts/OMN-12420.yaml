---
schema_version: "1.0.0"
ticket_id: OMN-12420
title: "node_dod_verify has never emitted a receipt — input not unwrapped"
summary: >
  Bind omnimarket PR to central OCC evidence for the node_dod_verify dispatch envelope-unwrap fix. The
  operation_match handler routing now declares an inline event_model so the runtime unwraps envelope.payload
  and validates it against ModelDodVerifyStartCommand before dispatch, restoring the terminal event and
  the dod_report.json receipt.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Regression test proves the wrapped envelope dispatches without ValidationError"
    command: "uv run pytest tests/unit/nodes/node_dod_verify/test_dispatch_envelope_unwrap.py -v"
  - kind: "ci"
    description: "node_dod_verify suite and runtime sweep pass on the omnimarket PR"
    command: "gh pr checks <omnimarket-pr> --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Wrapped-envelope dispatch returns a terminal result with no ValidationError (regression
      test)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12420/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-12420$' drift/dod_receipts/OMN-12420/dod-001/command.yaml"
  - id: "dod-002"
    description: "node_dod_verify CLI writes a guard-valid ModelDodReceipt dod_report.json"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12420/dod-002/command.yaml && grep
          -q '^ticket_id: OMN-12420$' drift/dod_receipts/OMN-12420/dod-002/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Deploy assessment: this fix DOES touch a runtime path (node_dod_verify contract.yaml + command model)
      and requires a stability-test runtime redeploy to take effect on the deployed container (currently
      omnimarket 0.4.2, the broken source). Until redeploy, the deployed Kafka path still times out; the
      fix is proven against the fixed source via the runtime dispatch callback and the CLI receipt path.
      Redeploy is the documented follow-up (per OMN-12420 acceptance: "After merge + redeploy, generate
      receipts ...").
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12420/dod-deploy-assessment/command.yaml
          && grep -q '^ticket_id: OMN-12420$' drift/dod_receipts/OMN-12420/dod-deploy-assessment/command.yaml"
