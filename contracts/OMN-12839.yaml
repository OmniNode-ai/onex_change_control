---
schema_version: "1.0.0"
ticket_id: "OMN-12839"
title: "Complete dashboard projection integration"
summary: >
  Validates and closes the remaining dashboard integration gaps by aligning omnidash with the projection
  backend websocket URL and by exposing the missing omnimarket projection topics for routing decisions,
  cost savings overview, and overnight readiness. The work is intentionally split across dashboard and
  projection-backend PRs so each interface change has focused local tests, golden-chain coverage, and
  explicit deploy evidence.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "topics"
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnidash-pr-199"
    description: "omnidash PR #199 aligns the dashboard websocket client with the configured projection
      backend URL and preserves existing projection REST behavior."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12839/dod-omnidash-pr-199/command.yaml
          && grep -q '^pr_number: 199$' drift/dod_receipts/OMN-12839/dod-omnidash-pr-199/command.yaml
          && grep -q '724c411fd64622f4ef39c04f797099bb38a24caf' drift/dod_receipts/OMN-12839/dod-omnidash-pr-199/command.yaml"
  - id: "dod-routing-pr-1142"
    description: "omnimarket PR #1142 exposes the routing decision dashboard projection topic from projection_routing_decision
      and covers it with discovery plus golden-chain tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12839/dod-routing-pr-1142/command.yaml
          && grep -q '^pr_number: 1142$' drift/dod_receipts/OMN-12839/dod-routing-pr-1142/command.yaml
          && grep -q '8ee3b264a60d41d1a12764b45bebf1a72cd8b449' drift/dod_receipts/OMN-12839/dod-routing-pr-1142/command.yaml"
  - id: "dod-cost-savings-pr-1143"
    description: "omnimarket PR #1143 exposes the cost savings overview projection topic from projection_cost_savings_overview
      and covers it with discovery plus golden-chain tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12839/dod-cost-savings-pr-1143/command.yaml
          && grep -q '^pr_number: 1143$' drift/dod_receipts/OMN-12839/dod-cost-savings-pr-1143/command.yaml
          && grep -q '4242e000b67abf09d374ff67871260db52778cff' drift/dod_receipts/OMN-12839/dod-cost-savings-pr-1143/command.yaml"
  - id: "dod-overnight-pr-1144"
    description: "omnimarket PR #1144 exposes the overnight readiness projection topic from projection_overnight_readiness
      and covers it with discovery plus golden-chain tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12839/dod-overnight-pr-1144/command.yaml
          && grep -q '^pr_number: 1144$' drift/dod_receipts/OMN-12839/dod-overnight-pr-1144/command.yaml
          && grep -q 'd6b815d405635367325fe83a6780832c623939e8' drift/dod_receipts/OMN-12839/dod-overnight-pr-1144/command.yaml"
  - id: "dod-browser-validation"
    description: "Local browser validation renders SEA Demo and Delegation Evidence against the projection
      backend and records the dashboard projection evidence artifact paths."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12839/dod-browser-validation/command.yaml
          && grep -q 'dashboard-browser-validation-20260610.json' drift/dod_receipts/OMN-12839/dod-browser-validation/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      No live .201 deployment or restart was performed from this integration slice. The backend PRs add
      projection database views and projection contract topics; post-merge deploy must apply migrations,
      publish the updated projection contracts, and validate that the projection API advertises the new
      topics. The omnidash PR requires only the standard dashboard image/static deploy with VITE_PROJECTION_API_URL
      set to the projection backend.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12839/dod-deploy-assessment/command.yaml
          && grep -q 'No live .201 deployment' drift/dod_receipts/OMN-12839/dod-deploy-assessment/command.yaml
          && grep -q 'projection API advertises the new topics' drift/dod_receipts/OMN-12839/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2412"
    description: "onex_change_control PR #2412 carries the OMN-12839 central contract and receipt evidence
      for the dashboard projection integration PR set."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12839/dod-occ-pr-2412/command.yaml
          && grep -q '^pr_number: 2412$' drift/dod_receipts/OMN-12839/dod-occ-pr-2412/command.yaml"
