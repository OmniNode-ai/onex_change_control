---
schema_version: "1.0.0"
ticket_id: OMN-12398
title: "Fix pre-existing check-local-paths and hook-bits-drift failures in omniclaude"
summary: >
  Resolves pre-existing pre-commit failures in omniclaude: hook-bits-drift fails with "No such file or
  directory" in git worktrees (hardcoded ../omnibase_core path); SPDX header violations in test fixture
  files added without required headers; .secrets.baseline false-positive from demo skill.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omniclaude PR #1698 CI passes relevant quality gates."
    command: "gh pr checks 1698 --repo OmniNode-ai/omniclaude"
  - kind: "tests"
    description: "Unit tests pass after SPDX header and baseline fixes."
    command: "uv run pytest tests/unit/ tests/validation/ -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-precommit-hooks-pass"
    description: "check-local-paths, hook-bits-drift, and SPDX hooks all pass in omniclaude worktree."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12398/dod-precommit-hooks-pass/command.yaml\""
  - id: "dod-unit-tests-pass"
    description: "5013+ unit tests pass; 63 fixture tests pass after SPDX header fixes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12398/dod-unit-tests-pass/command.yaml\""
  - id: "dod-omniclaude-pr-1698"
    description: "omniclaude PR #1698 carried the fix commits on the jonah/omn-12398-fix-preexisting-ci-v2
      branch."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12398/dod-omniclaude-pr-1698/command.yaml\"
          && grep -q '^pr_number: 1698$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12398/dod-omniclaude-pr-1698/command.yaml\""
  - id: "dod-occ-pr-1822"
    description: "onex_change_control PR #1822 carries the OMN-12398 central contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12398/dod-occ-pr-1822/command.yaml\"
          && grep -q '^pr_number: 1822$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12398/dod-occ-pr-1822/command.yaml\""
