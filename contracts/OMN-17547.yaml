---
schema_version: "1.0.0"
ticket_id: "OMN-17547"
title: "A length-constrained prose prompt resolves to its own declared response shape"
summary: |-
  Hand-authored OCC companion for OmniNode-ai/omnimarket#2880. One dod_evidence item per definition-of-done requirement written on the ticket before the fix was delegated (lane dm-nodes, delegation matrix 2026-09-25). Each check is content-bound at the product PR head; each receipt carries the behaviour proof run locally at that head.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
dod_evidence:
  - id: "dod-omn17547-dod1-resolves"
    description: |-
      DoD1 (AC1): prose length directives resolve to EnumRequestedResponseShape.LENGTH_CONSTRAINED (wire value length_constrained). Behaviour: uv run pytest tests/unit/delegation/test_length_constrained_shape_omn17547.py -q -k resolves at the head of OmniNode-ai/omnimarket#2880.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/enums/enum_requested_response_shape.py?ref=51b8df64d100e32b792013e281e796d047bd41ca" --jq '.content' | base64 -d | grep -cF 'LENGTH_CONSTRAINED = "length_constrained"'
  - id: "dod-omn17547-dod2-override"
    description: |-
      DoD2 (AC2): the research heuristic band for the new shape is exactly (no_refusal, short_form_adequacy) and EXACT_LITERAL keeps precedence. Behaviour: -k override on the same test file.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnimarket/contents/tests/unit/delegation/test_length_constrained_shape_omn17547.py?ref=51b8df64d100e32b792013e281e796d047bd41ca" --jq '.content' | base64 -d | grep -cF 'def test_override_band_is_short_form'
  - id: "dod-omn17547-dod3-bifrost"
    description: |-
      DoD3 (AC3): the one-sentence answer to the live Bifrost prompt passes the research gate. Behaviour: -k bifrost on the same test file.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnimarket/contents/tests/unit/delegation/test_length_constrained_shape_omn17547.py?ref=51b8df64d100e32b792013e281e796d047bd41ca" --jq '.content' | base64 -d | grep -cF 'def test_bifrost_live_case_passes_research_gate'
  - id: "dod-omn17547-dod4-mentions"
    description: |-
      DoD4 (AC4): prose that only mentions a length resolves unconstrained. Behaviour: -k mentions on the same test file.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnimarket/contents/tests/unit/delegation/test_length_constrained_shape_omn17547.py?ref=51b8df64d100e32b792013e281e796d047bd41ca" --jq '.content' | base64 -d | grep -cF 'def test_mentions_do_not_constrain'
  - id: "dod-omn17547-dod5-no-regression"
    description: |-
      DoD5: the OMN-16932 shape suite still passes, including the guard that broadened directives do not capture ordinary prose. Behaviour: uv run pytest tests/unit/delegation/test_local_accept_ends_chain_omn16932.py -q.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnimarket/contents/tests/unit/delegation/test_local_accept_ends_chain_omn16932.py?ref=51b8df64d100e32b792013e281e796d047bd41ca" --jq '.content' | base64 -d | grep -cF 'def test_broadened_directives_do_not_capture_ordinary_prose'
  - id: "dod-omn17547-dod6-types-lint"
    description: |-
      DoD6: mypy --strict clean on the enum and resolver modules, ruff check and format check clean on the changed files; the resolver resolves the new shape last in precedence.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/inference/requested_response_shape.py?ref=51b8df64d100e32b792013e281e796d047bd41ca" --jq '.content' | base64 -d | grep -cF 'EnumRequestedResponseShape.LENGTH_CONSTRAINED'
  - id: "dod-omn17547-dod7-research-fallback"
    description: |-
      DoD7 (AC5): the PR body states the decision on whether research stays the unclassified fallback, with the reason.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 2880 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -c 'DoD7'
  - id: "dod-OmniNode-ai-omnimarket-pr-2880"
    description: "PR #2880 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/tests/unit/delegation/test_length_constrained_shape_omn17547.py?ref=51b8df64d100e32b792013e281e796d047bd41ca --jq '.content' | base64 -d | grep -c 'def test_resolves_length_constrained'
  - id: "dod-OmniNode-ai-omnimarket-pr-2880-ci"
    description: "PR #2880 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2880 --repo OmniNode-ai/omnimarket --json files"
  - id: "dod-occ-diff-derived-behavior-proof-pr-2880"
    description: "PR #2880 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/delegation/test_length_constrained_shape_omn17547.py -q"
        cwd: "${OMNI_HOME}/omnimarket"
