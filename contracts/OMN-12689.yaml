---
schema_version: "1.0.0"
ticket_id: "OMN-12689"
title: "Workstream C: move service_delegation_dispatch_port constants to contract"
summary: >-
  Central evidence that omnimarket PR #1041 moves RuntimeDelegationDispatchPort downstream delegation
  transport settings out of source-code constants and into a typed, contract-derived runtime dispatch
  configuration.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-contract-derived-dispatch-port"
    description: "Focused omnimarket tests prove the delegate-skill runtime dispatch port loads transport
      config from contract.yaml."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12689/dod-occ-pr-2173/command.yaml"
          && grep -q '^pr_number: 2173$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12689/dod-occ-pr-2173/command.yaml"
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12689/dod-omnimarket-contract-derived-dispatch-port/command.yaml"
          && grep -q '^pr_number: 1041$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12689/dod-omnimarket-contract-derived-dispatch-port/command.yaml"
  - id: "dod-deploy-gate-runtime-path"
    description: "Deploy-gate evidence for omnimarket delegate-skill runtime dispatch port changes in
      PR #1041."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12689/dod-deploy-gate-runtime-path/command.yaml"
          && grep -q '^pr_number: 1041$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12689/dod-deploy-gate-runtime-path/command.yaml"
  - id: "dod-occ-pr-2173"
    description: "onex_change_control PR #2173 carries the OMN-12689 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: ""
