---
schema_version: "1.0.0"
ticket_id: "OMN-14907"
title: "Count-locked pull_request workflow ratchet for omnibase_core"
summary: "OCC evidence for omnibase_core#1475, the OMN-14907 canary that adds a count-locked ratchet over
  pull_request-triggered workflow files to constrain CI fan-out growth."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_core#1475 product PR is open on the expected C7 ratchet branch."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - kind: "ci"
    description: "omnibase_core#1475 changes only the workflow ratchet implementation, registry, tests,
      CI wiring, and pre-commit hook."
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1475"
    description: "PR #1475 on OmniNode-ai/omnibase_core is the OMN-14907 pull_request workflow ratchet
      canary."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
  - id: "dod-core-pr-1475-files"
    description: "PR #1475 on OmniNode-ai/omnibase_core touches only the C7 workflow-ratchet files and
      CI/pre-commit wiring."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "dod-occ-self-bind-pr-4570"
    description: "OCC evidence PR #4570 carries central OMN-14907 evidence for omnibase_core#1475."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state,headRefOid"
