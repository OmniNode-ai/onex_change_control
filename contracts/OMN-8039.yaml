---
schema_version: "1.0.0"
ticket_id: "OMN-8039"
title: "Task 3: Responder chain YAML config and ModelResponderChain"
summary: "Add typed responder-chain configuration for routing policy capability tiers C1-C4."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused pytest validates responder_chains.yaml parses and declares C1-C4 with expected
      models"
    command: "cd ../omnimarket && uv run pytest tests/test_responder_chain_loading.py -v"
  - kind: "tests"
    description: "Ruff validates new responder-chain model and loading test"
    command: "cd ../omnimarket && uv run ruff check src/omnimarket/nodes/node_routing_policy_engine/models/model_responder_chain.py
      tests/test_responder_chain_loading.py"
  - kind: "ci"
    description: "omnimarket PR checks pass for responder-chain config"
    command: "gh pr checks 523 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Responder-chain YAML parses and covers C1-C4"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-8039/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "New responder-chain model and test pass Ruff checks"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-8039/dod-002 -type f -name '*.yaml' | grep -q ."
