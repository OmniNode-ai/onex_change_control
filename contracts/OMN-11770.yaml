---
schema_version: "1.0.0"
ticket_id: "OMN-11770"
title: "Wire schema handshake, staleness, and consumer health CI gates"
summary: >-
  Adds kafka-schema-handshake workflow (blocking gate on PR + merge_group),
  post-deploy consumer group health check (blocking), and post-deploy staleness
  check (advisory) to omninode_infra CI.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "FULL"
evidence_requirements:
  - kind: "ci"
    description: "omninode_infra PR #449 CI green (except verify/receipt-gate which this OCC PR resolves)"
    command: "gh pr checks 449 --repo OmniNode-ai/omninode_infra"
  - kind: "manual"
    description: "Self-bound OCC PR carrying this contract."
    command: "See drift/dod_receipts/OMN-11770/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-449"
    description: "omninode_infra PR #449 wires kafka-schema-handshake, consumer health, and staleness gates."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11770/dod-omninode-infra-pr-449/command.yaml"
          && grep -q '^pr_number: 449$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11770/dod-omninode-infra-pr-449/command.yaml"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for this OCC PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11770/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: OMN-11770$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11770/dod-occ-pr/command.yaml"
