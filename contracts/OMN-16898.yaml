---
schema_version: "1.0.0"
ticket_id: "OMN-16898"
title: "Autobind OCC evidence for OMN-16898"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #867."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #867 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      Behavior proof for PR #867 on OmniNode-ai/omniintelligence, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omniintelligence`.
    command: "uv run pytest tests/unit/code_projection/test_cli.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniintelligence-pr-867"
    description: "PR #867 on OmniNode-ai/omniintelligence — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniintelligence/contents/src/omniintelligence/code_projection/__main__.py?ref=5b26e533f9b5022dbcee79af7aaa0b8df5716928 --jq '.content' | base64 -d | grep -c 'def _require_projection_repository_id'
  - id: "dod-OmniNode-ai-omniintelligence-pr-867-ci"
    description: "PR #867 on OmniNode-ai/omniintelligence — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 867 --repo OmniNode-ai/omniintelligence --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #867 on OmniNode-ai/omniintelligence — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/unit/code_projection/test_cli.py -q"
        cwd: "${OMNI_HOME}/omniintelligence"
