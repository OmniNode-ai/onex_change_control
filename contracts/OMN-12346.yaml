---
schema_version: "1.0.0"
ticket_id: "OMN-12346"
title: "Implement omnimarket contract drift compute runtime handler"
summary: >
  Bind central evidence for omnimarket PR #955, which removes the
  node_contract_drift_compute runtime stub and implements deterministic
  contract-vs-handler topic drift detection using the node contract and typed
  Pydantic request/result models as the source of truth.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-955"
    description: "Bind OMN-12346 evidence to omnimarket PR #955."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12346/dod-omnimarket-pr-955/command.yaml && grep -q '^pr_number: 955$' drift/dod_receipts/OMN-12346/dod-omnimarket-pr-955/command.yaml"
  - id: "dod-contract-drift-focused-tests"
    description: "Focused OMN-12346 golden-chain tests, lint, and formatting pass for the contract drift compute node."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12346/dod-contract-drift-focused-tests/command.yaml && grep -q '23 passed' drift/dod_receipts/OMN-12346/dod-contract-drift-focused-tests/command.yaml && grep -q 'All checks passed' drift/dod_receipts/OMN-12346/dod-contract-drift-focused-tests/command.yaml"
  - id: "dod-contract-drift-broader-validation"
    description: "Adjacent contract sweep, node drift, topic lint, market-skill smoke, and projection-query validation pass for omnimarket PR #955."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12346/dod-contract-drift-broader-validation/command.yaml && grep -q '290 contracts checked' drift/dod_receipts/OMN-12346/dod-contract-drift-broader-validation/command.yaml && grep -q '0 drift findings' drift/dod_receipts/OMN-12346/dod-contract-drift-broader-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: the compute node remains pure/read-only, declares native event-bus topics, and requires no live runtime deploy before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12346/dod-deploy-readiness/command.yaml && grep -qi 'deploy readiness' drift/dod_receipts/OMN-12346/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR #1810 carries the central OMN-12346 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12346/dod-occ-pr/command.yaml && grep -q '^pr_number: 1810$' drift/dod_receipts/OMN-12346/dod-occ-pr/command.yaml"
