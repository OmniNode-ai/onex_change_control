---
schema_version: "1.0.0"
ticket_id: "OMN-12771"
title: "Make SEA local ADK runtime overlay durable for local-first e2e proofs"
summary: >-
  During 2026-06-07 release validation SEA failed because node_agent_orchestrator contract selects cloud_gemini
  by default; ADK called generativelanguage.googleapis.com and received HTTP 503. A local worktree overlay
  selecting local_qwen_coder plus SEA_LOCAL_INFERENCE_BASE_URL=http://192.168.86.201 produced a clean
  SEA e2e pass (correlation 5d35fc2f). Closeout: make the local-first ADK runtime selection durable and
  contract-driven via an explicit overlay pointer or tracked lane overlay consistent with existing contract
  overlay patterns. Do not add provider-specific handler fields; backend handlers remain generic and selected
  by contract/model registry. No untracked worktree mutation.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-local-overlay-tracked"
    description: >-
      A tracked lane overlay file or explicit overlay pointer makes local_qwen_coder selection durable
      without an untracked worktree mutation to src/nodes/node_agent_orchestrator/contract.local.yaml.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12771/dod-local-overlay-tracked/command.yaml
  - id: "dod-sea-e2e-local-first-clean"
    description: >-
      SEA e2e proof using local-first overlay: contract_passed=true, Result=PASS, model/provider/endpoint
      all sourced from contract/overlay/routing authority (no env literal or hardcoded provider string),
      no HTTP 503 from cloud endpoint.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12771/dod-sea-e2e-local-first-clean/command.yaml
          && grep -q 'contract_passed' drift/dod_receipts/OMN-12771/dod-sea-e2e-local-first-clean/command.yaml
  - id: "dod-occ-evidence"
    description: "onex_change_control OCC contract + receipts for OMN-12771."
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12771/dod-occ-evidence/command.yaml && grep
          -q '^ticket_id: "OMN-12771"$' contracts/OMN-12771.yaml
