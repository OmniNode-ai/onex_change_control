---
schema_version: "1.0.0"
ticket_id: "OMN-12902"
title: "Audit and eliminate residual runtime env authority in demo and judge paths"
summary: >-
  onex-self-extending-agent PR #229 closes the remaining judge/contractor mismatch where `uv run onex
  sea run --agent` bypassed the sanctioned-live delegation overlay preflight used by `python -m src --agent`.
  The CLI now fails closed before ADK starts when SEA_DELEGATION_BUS_BOOTSTRAP is absent, and the judge
  guide documents deterministic replay as the portable Tier 1 proof while live Gemini requires an approved
  overlay-backed lane.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-229"
    description: >-
      onex-self-extending-agent PR #229 carries the OMN-12902 implementation aligning `onex sea run --agent`,
      `--demo`, and `--progressive` with the sanctioned-live overlay preflight.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12902/dod-sea-pr-229/command.yaml"
  - id: "dod-cli-live-overlay-guard"
    description: >-
      The no-overlay live CLI path exits before ADK with a clear SEA_DELEGATION_BUS_BOOTSTRAP requirement,
      replacing the previous DelegationBusRuntimeNotWiredError background-thread failure.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12902/dod-cli-live-overlay-guard/command.yaml"
  - id: "dod-focused-tests"
    description: >-
      Focused CLI tests prove agent/demo/progressive live modes load the sanctioned overlay, assert bus
      readiness where required, and do not start ADK when the overlay pointer is missing.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12902/dod-focused-tests/command.yaml"
  - id: "dod-replay-proof"
    description: >-
      The deterministic Tier 1 replay and orchestrator regression tests remain green, and the golden replay
      still completes with no live model call.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12902/dod-replay-proof/command.yaml"
  - id: "dod-lint-and-hooks"
    description: >-
      Ruff formatting/linting and commit hooks, including the SEA architecture guard and integration hooks,
      passed for the product branch.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12902/dod-lint-and-hooks/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      No live deployment, restart, or .201 runtime mutation was performed for this source and judge reproducibility
      path fix.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12902/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      onex_change_control PR #2435 carries the central OMN-12902 contract and receipt evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12902/dod-occ-pr/command.yaml"
          && grep -q '^pr_number: 2435$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12902/dod-occ-pr/command.yaml"
