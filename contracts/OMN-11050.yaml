---
schema_version: "1.0.0"
ticket_id: "OMN-11050"
title: "Rename imperative steps to verification semantics in canonical onboarding DAG"
summary: >-
  Renames 5 imperative step keys in canonical.yaml to verification-mode names (check_docker_infra, check_secrets,
  check_event_bus, check_omnidash, check_node_bus_connection) to reflect that the DAG verifies state rather
  than mutating it. Updates all depends_on references and test assertions.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "All 219 onboarding unit tests pass after the rename."
    command: "uv run pytest tests/unit/onboarding/ -v --tb=short"
  - kind: "manual"
    description: "omnibase_infra PR #1599 carries the step key rename."
    command: "gh pr view 1599 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid,title"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1599"
    description: "omnibase_infra PR #1599 carries the OMN-11050 rename at the recorded head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11050/dod-infra-pr-1599/command.yaml
          && grep -q '^pr_number: 1599$' drift/dod_receipts/OMN-11050/dod-infra-pr-1599/command.yaml"
  - id: "dod-local-tests"
    description: "219 onboarding unit tests pass locally after rename."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11050/dod-local-tests/command.yaml
          && grep -q 'passed: 219' drift/dod_receipts/OMN-11050/dod-local-tests/command.yaml"
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carrying the OMN-11050 contract is open."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11050/dod-occ-pr-self/command.yaml"
