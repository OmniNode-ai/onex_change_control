---
schema_version: "1.0.0"
ticket_id: "OMN-11523"
title: "Materialize SEA entropy dashboard projection"
summary: >-
  Bind central OCC evidence for onex-self-extending-agent PR #115, which materializes a typed entropy
  dashboard projection from finalized evidence bundles and aligns demo preflight with that projection
  surface.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "onex-self-extending-agent PR #115 local and CI checks pass for dashboard projection
      integration."
    command: "gh pr checks 115 --repo OmniNode-ai/onex-self-extending-agent"
  - kind: "manual"
    description: "Native all-track entropy delegation run completes and emits dashboard projection evidence."
    command: "uv run python -m src.experiments.entropy_comparison.demo --all-tracks --evidence-root /tmp/omn-11523-sea-evidence"
  - kind: "manual"
    description: "Central OCC evidence PR carries this contract and PASS receipts."
    command: "gh pr view <occ-pr> --repo OmniNode-ai/onex_change_control"
dod_evidence:
  - id: "dod-onex-self-extending-agent-pr-115"
    description: "Bind OMN-11523 implementation evidence to onex-self-extending-agent PR #115 at the observed
      PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11523/dod-onex-self-extending-agent-pr-115/command.yaml"
          && grep -q '^pr_number: 115$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11523/dod-onex-self-extending-agent-pr-115/command.yaml"
          && grep -q '^commit_sha: "72ccf6c21cf2b8527f3cc1effdaf4a4af8d6e1e1"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11523/dod-onex-self-extending-agent-pr-115/command.yaml"
  - id: "dod-local-verification"
    description: "Record local verification for replay, demo, all-track native delegation, lint, and tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11523/dod-local-verification/command.yaml"
          && grep -q '858 passed, 14 skipped' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11523/dod-local-verification/command.yaml"
          && grep -q '3,990 total tokens' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11523/dod-local-verification/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Bind OMN-11523 central evidence to the OCC PR that carries this bundle."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11523/dod-occ-pr-self/command.yaml"
          && grep -q '^ticket_id: "OMN-11523"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11523/dod-occ-pr-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
