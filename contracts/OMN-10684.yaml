---
schema_version: "1.0.0"
ticket_id: OMN-10684
title: "Environment configuration to node contract migration"
summary: "Bind omnimarket PR #701 to central evidence for migrating raw environment-variable reads into
  node contract config sections."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - protocols
  - public_api
evidence_requirements:
  - kind: ci
    description: "omnimarket#701 carries the environment configuration migration."
    command: "gh pr checks 701 --repo OmniNode-ai/omnimarket"
  - kind: manual
    description: "Deploy assessment records that no live runtime mutation was performed before merge."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10684/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-omnimarket-pr-701
    description: "omnimarket PR #701 is bound to OMN-10684 for contract-backed environment configuration
      migration."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10684/dod-omnimarket-pr-701/command.yaml
          && grep -q '^ticket_id: OMN-10684$' drift/dod_receipts/OMN-10684/dod-omnimarket-pr-701/command.yaml
          && grep -q '^pr_number: 701$' drift/dod_receipts/OMN-10684/dod-omnimarket-pr-701/command.yaml"
  - id: dod-deploy-assessment
    description: "No live deploy, restart, Kafka mutation, database mutation, or .201 mutation was performed
      for PR #701."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10684/dod-deploy-assessment/command.yaml
          && grep -q '^ticket_id: OMN-10684$' drift/dod_receipts/OMN-10684/dod-deploy-assessment/command.yaml
          && grep -q '^pr_number: 701$' drift/dod_receipts/OMN-10684/dod-deploy-assessment/command.yaml"
