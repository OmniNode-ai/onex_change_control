---
schema_version: "1.0.0"
ticket_id: "OMN-18121"
title: "Autobind OCC evidence for OMN-18121"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2440.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2440 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2440 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: |-
      uv run pytest tests/test_redeploy_contextless_gate_refusal_omn18121.py tests/test_redeploy_orchestrator_dispatch_resolution.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2440"
    description: >-
      PR #2440 on OmniNode-ai/omnimarket — Evidence-Source autobind. Reads the merged handler at the PR's
      own commit and asserts the refusal type is present, which is the AC2 requirement that a context-less
      gate-evaluated payload be refused rather than reconstructed from field defaults.
    source: "generated"
    binds_ac:
      - "AC2"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_redeploy_orchestrator/handlers/handler_redeploy_orchestrator.py?ref=2577ec892e91c8341926eb669d0e8b0b9f594909 --jq '.content' | base64 -d | grep -c 'class RedeployContextMissingError'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2440/files --paginate --jq '.[].filename' | grep -cFx 'src/omnimarket/events/runtime_deployment.py'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2440 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2440/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: >-
      PR #2440 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434). Runs the ticket's
      own tests. AC1 both models assert git_ref is None by default; AC2 the refusal is asserted against
      the exact bare-decision payload read off the gate-evaluated topic; AC3 the shared-command path refuses
      naming both git_ref and image_digest; AC4 the digest-only path asserts runtime_lane comes from the
      echoed gate command rather than the dev field default; AC5 a handler-level test asserts no deploy-publish
      event is emitted on a refusal; AC6 a test asserts the branch literal is never assigned in the node's
      sources.
    source: "generated"
    binds_ac:
      - "AC1"
      - "AC2"
      - "AC3"
      - "AC4"
      - "AC5"
      - "AC6"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/test_redeploy_contextless_gate_refusal_omn18121.py tests/test_redeploy_orchestrator_dispatch_resolution.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-8882"
    description: "Binds OMN-18121 to OCC companion PR #8882 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8882 --repo OmniNode-ai/onex_change_control --json number,state"
