---
schema_version: "1.0.0"
ticket_id: "OMN-13781"
title: "feat(OMN-13781): contract-state-coverage CI gate"
summary: >-
  omnimarket PR #1545 implements the contract-state-coverage gate, wiring the
  validator as required CI and as a pre-commit hook. It extracts declared node
  states from contract.yaml, compares them with states asserted by tests, and
  uses a baseline so pre-existing gaps are tracked without weakening strict
  enforcement for directly touched nodes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-state-coverage-gate"
    description: >-
      The state-coverage validator, required GitHub Actions workflow, pre-commit
      hook, and baseline are present in omnimarket PR #1545.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13781/dod-state-coverage-gate/command.yaml"
  - id: "dod-product-verification"
    description: >-
      PR #1545 records full non-kafka pytest, focused validator unit tests,
      ruff, strict mypy, pre-commit, and strict-baseline promotion checks.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13781/dod-product-verification/command.yaml"
  - id: "dod-pr-1545-binding"
    description: >-
      OmniNode-ai/omnimarket PR #1545 carries the OMN-13781 implementation at
      feature commit 4c6ea7ff4b0c1ed51de20053848b08397201bd41.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13781/dod-pr-1545-binding/command.yaml"
