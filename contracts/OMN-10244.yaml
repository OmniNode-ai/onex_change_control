---
schema_version: "1.0.0"
ticket_id: "OMN-10244"
summary: "Task 4: Create core/enums/overseer/ Namespace - 14 Enums [omnibase_core, Wave 2]"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_core PR #962 CI checks pass"
    command: "gh pr checks 962 --repo OmniNode-ai/omnibase_core --watch"
  - kind: "tests"
    description: "All 15 overseer enum import tests pass"
    command: "cd ../omnibase_core && uv run pytest tests/unit/enums/overseer/ -v"
  - kind: "tests"
    description: "mypy --strict clean on overseer namespace"
    command: "cd ../omnibase_core && uv run mypy src/omnibase_core/enums/overseer/ --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "14 enum files created at src/omnibase_core/enums/overseer/enum_*.py with SPDX headers"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10244/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "All 15 TDD tests pass (RED confirmed before implementation, GREEN after)"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10244/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "mypy --strict passes on all 15 files in overseer namespace"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10244/dod-003 -type f -name '*.yaml' | grep -q ."
  - id: "dod-004"
    description: "All pre-commit hooks pass including SPDX, ruff, mypy strict"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10244/dod-004 -type f -name '*.yaml' | grep -q ."
