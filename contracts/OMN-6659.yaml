---
schema_version: "1.0.0"
ticket_id: "OMN-6659"
title: "Justify omnibase_infra type suppressions"
summary: >-
  Bind central OCC evidence for omnibase_infra PR #1714, which documents and justifies existing infra
  type suppressions across adapters, handlers, runtime wiring, observability, and validators.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1714 CI and receipt gates pass."
    command: "gh pr checks 1714 --repo OmniNode-ai/omnibase_infra --watch"
  - kind: "manual"
    description: "Central OCC evidence PR carries this contract and PASS receipts."
    command: "gh pr view <occ-pr> --repo OmniNode-ai/onex_change_control"
dod_evidence:
  - id: "dod-omnibase-infra-pr-1714"
    description: "Bind OMN-6659 evidence to omnibase_infra PR #1714 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6659/dod-omnibase-infra-pr-1714/command.yaml\"
          && grep -q '^pr_number: 1714$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6659/dod-omnibase-infra-pr-1714/command.yaml\"
          && grep -q '^commit_sha: \"fb6f2ca06faa26cd1e0eb0563dbf06d97c27ee4b\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6659/dod-omnibase-infra-pr-1714/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Runtime path touch requires CI and receipt-gate validation but no live deploy before
      merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6659/dod-deploy-assessment/command.yaml\"
          && grep -q 'deploy gate' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6659/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind OMN-6659 central evidence to this onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6659/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number: 1379$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6659/dod-occ-pr/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
