---
schema_version: "1.0.0"
ticket_id: "OMN-11776"
title: "LLM delegation call effect node"
summary: >
  Effect node implementing LLM delegation call dispatch for the ONEX delegation pipeline (epic OMN-11771
  delegation pipeline Phase 1).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-779"
    description: "omnimarket PR #779 implements LLM delegation call effect node"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11776/dod-omnimarket-pr-779/command.yaml\"
          && grep -q '^pr_number: 779$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11776/dod-omnimarket-pr-779/command.yaml\"
          && grep -q '^commit_sha: \"6c6729308a4fe4dab24d2c46c96f8b9ad9b87c22\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11776/dod-omnimarket-pr-779/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Record deploy-gate assessment for the runtime LLM delegation call effect node addition"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11776/dod-deploy-assessment/command.yaml\"
          && grep -qi 'deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11776/dod-deploy-assessment/command.yaml\"
          && grep -q '^pr_number: 779$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11776/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-batch-pr"
    description: "OCC batch PR binds OMN-11776 contract to onex_change_control as the evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11776/dod-occ-batch-pr/command.yaml\"
          && grep -q '^ticket_id: \"OMN-11776\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11776/dod-occ-batch-pr/command.yaml\""
