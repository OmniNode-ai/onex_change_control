---
schema_version: "1.0.0"
ticket_id: "OMN-11904"
title: "Wave 4 session and overnight declarative contract migration"
summary: >
  Bind central evidence for omnimarket PR #800, which migrates 14 session and overnight nodes to declarative
  handler_routing contracts and proves the conflict-resolved rebase.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-800"
    description: "Bind OMN-11904 evidence to omnimarket PR #800."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11904/dod-omnimarket-pr/command.yaml
          && grep -q '^pr_number: 800$' drift/dod_receipts/OMN-11904/dod-omnimarket-pr/command.yaml"
  - id: "dod-conflict-resolution-local-gates"
    description: "Conflict-resolved rebase has no conflict markers and passes local gates."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11904/dod-conflict-resolution-local-gates/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence for the Wave 4 session and overnight declarative contract
      migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11904/dod-deploy-readiness/command.yaml
          && grep -q 'deploy readiness' drift/dod_receipts/OMN-11904/dod-deploy-readiness/command.yaml"
