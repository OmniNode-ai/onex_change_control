---
schema_version: "1.0.0"
ticket_id: "OMN-11904"
title: "Wave 4 session and overnight declarative contract migration"
summary: >
  Central OCC evidence for omnimarket PR #800, which migrates session and overnight nodes to canonical
  declarative handler routing and records the local gate proof required for receipt and deploy readiness.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-800"
    description: "Bind OMN-11904 evidence to omnimarket PR #800."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11904/dod-omnimarket-pr-800/command.yaml
          && grep -q '^pr_number: 800$' drift/dod_receipts/OMN-11904/dod-omnimarket-pr-800/command.yaml"
  - id: "dod-local-validation"
    description: "PR-local validation passed for the Wave 4 session and overnight declarative migration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11904/dod-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: declarative contract routing changes require no live runtime deploy
      before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11904/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11904/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11904 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11904/dod-occ-pr/command.yaml"
