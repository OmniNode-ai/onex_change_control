---
schema_version: "1.0.0"
ticket_id: "OMN-11255"
title: "feat(OMN-11241): Wave 3 Task 10 — MCP tool invocation from local system"
summary: >
  MCPClient for .201:8090. Integration tests invoke deployed pattern extraction tool with session snapshots,
  verify structured output, prove correlation_id round-trip through MCP boundary.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "4 unit tests (mocked HTTP) + 4 integration tests on live .201. correlation_id propagated
      through MCP boundary."
    command: "gh pr view 22 --repo OmniNode-ai/onex-self-extending-agent --json number,state"
  - kind: "ci"
    description: "PR #22 open with CI passing"
    command: "gh pr view 22 --repo OmniNode-ai/onex-self-extending-agent --json number,state,statusCheckRollup"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-22"
    description: "onex-self-extending-agent PR #22 — MCPClient implementation with 4 unit + 4 integration
      tests. correlation_id round-trip through MCP boundary verified. Multi-session invocation works."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11255/dod-pr-22/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carrying this contract and binding receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11255/dod-occ-pr/command.yaml"
