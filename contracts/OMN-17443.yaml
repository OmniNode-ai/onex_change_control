---
schema_version: "1.0.0"
ticket_id: "OMN-17443"
title: "Autobind OCC evidence for OMN-17443"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3111.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3111 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3111 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: "uv run pytest tests/scripts/test_maintenance_sync_clone_privilege_omn17443.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3111"
    description: "PR #3111 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/check_reconciler_privilege.py?ref=a3d14db8396b71571686fd588d016059b51a1d4b --jq '.content' | base64 -d | grep -c 'def host_artifact_map'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3111/files --paginate --jq '.[].filename' | grep -cFx 'deploy/maintenance/omninode-host-maintenance-sync.sh'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3111 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/scripts/test_maintenance_sync_clone_privilege_omn17443.py -q"
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-7949"
    description: "Binds OMN-17443 to OCC companion PR #7949 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7949 --repo OmniNode-ai/onex_change_control --json number,state"
