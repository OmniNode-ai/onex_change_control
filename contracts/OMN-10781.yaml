---
schema_version: "1.0.0"
ticket_id: "OMN-10781"
title: "feat(OMN-10781): add onboarding input adapter protocol"
summary: >
  OCC evidence for omnibase_infra PR #1598. Adds interactive onboarding protocol surface: ProtocolInputAdapter,
  AdapterCliInput (stdin), AdapterFakeInput (test), EnumInteractiveStepType, EnumInteractivePolicyType,
  and supporting Pydantic models (ModelInteractiveStep, ModelInteractivePolicy, ModelInteractiveResult,
  ModelTransition, ModelTransitionBranch). Pure library addition — no Kafka topics, no schema changes,
  no API surface changes, no runtime restart required.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Onboarding model and adapter unit tests pass"
    command: "See drift/dod_receipts/OMN-10781/dod-unit-tests/command.yaml"
  - kind: "ci"
    description: "Deploy smoke — library-only addition, no live deploy required"
    command: "See drift/dod_receipts/OMN-10781/dod-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "Onboarding model and adapter unit tests pass"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'PASS' drift/dod_receipts/OMN-10781/dod-unit-tests/command.yaml && echo 'OMN-10781 unit-tests
          receipt present'
  - id: "dod-deploy"
    description: >-
      Deploy assessment recorded — library-only onboarding protocol additions, no live deploy or runtime
      restart performed.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'status: PASS' drift/dod_receipts/OMN-10781/dod-deploy/command.yaml && grep -q 'evidence_item_id:
          "dod-deploy"' drift/dod_receipts/OMN-10781/dod-deploy/command.yaml && echo 'OMN-10781 deploy
          receipt present and PASS'
  - id: "dod-infra-pr-1598"
    description: "omnibase_infra PR #1598 carries all onboarding input adapter protocol changes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'PASS' drift/dod_receipts/OMN-10781/dod-infra-pr-1598/command.yaml && grep -q 'pr_number:
          1598' drift/dod_receipts/OMN-10781/dod-infra-pr-1598/command.yaml
  - id: "dod-occ-pr-1040"
    description: "Self-binding receipt for OCC PR #1040 — anchors evidence chain to this OCC PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q 'status: PASS' drift/dod_receipts/OMN-10781/dod-occ-pr-1040/command.yaml && grep -q
          'pr_number: 1040' drift/dod_receipts/OMN-10781/dod-occ-pr-1040/command.yaml
