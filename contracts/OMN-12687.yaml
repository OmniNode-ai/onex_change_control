---
schema_version: "1.0.0"
ticket_id: "OMN-12687"
title: "Workstream I-A: minimum proof-path golden chains for the live delegation/SEA lane"
summary: >-
  Central evidence that omnimarket PR #1039 extends the golden-chain sweep registry with the minimum I-A
  proof-path chains for delegation inference request/response and terminal projection materialization,
  and upgrades the SEA acceptance regression to execute the real routing, inference, and projection handlers
  on one correlation ID.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-proof-path-chains"
    description: "Focused omnimarket tests prove the I-A proof-path chains and behavioral SEA handler
      lane."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12687/dod-omnimarket-proof-path-chains/command.yaml"
  - id: "dod-deploy-gate-runtime-path"
    description: "Deploy-gate evidence for omnimarket golden-chain runtime path changes in PR #1039."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12687/dod-deploy-gate-runtime-path/command.yaml"
          && grep -q '^pr_number: 1039$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12687/dod-deploy-gate-runtime-path/command.yaml"
          && echo deploy-gate-evidence-present >/dev/null
  - id: "dod-occ-pr-2171"
    description: "onex_change_control PR #2171 carries the OMN-12687 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12687/dod-occ-pr-2171/command.yaml"
          && grep -q '^pr_number: 2171$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12687/dod-occ-pr-2171/command.yaml"
