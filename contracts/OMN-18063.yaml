---
schema_version: "1.0.0"
ticket_id: "OMN-18063"
title: "Autobind OCC evidence for OMN-18063"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #1259.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1259 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1259 on OmniNode-ai/omninode_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omninode_infra`.
    command: |-
      uv run pytest scripts/validation/test_valkey_credential_contract.py tests/scripts/test_collect_staging_green_facts.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1259"
    description: "PR #1259 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1259 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1259/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/staging-green-bar.yml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #1259 on OmniNode-ai/omninode_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1259"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest scripts/validation/test_valkey_credential_contract.py tests/scripts/test_collect_staging_green_facts.py -q
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "occ-self-bind-pr-8737"
    description: "Binds OMN-18063 to OCC companion PR #8737 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8737 --repo OmniNode-ai/onex_change_control --json number,state"
