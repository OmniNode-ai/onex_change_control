---
schema_version: "1.0.0"
ticket_id: "OMN-11423"
title: "Deploy gate reads ticket contracts from OCC"
summary: >-
  Bind evidence for omniclaude PR #1627, which changes the reusable deploy-gate workflow to resolve ticket
  contracts from the central onex_change_control contract repository instead of caller-local contract
  directories.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "omniclaude PR #1627 carries the deploy-gate OCC contract lookup change."
    command: "See drift/dod_receipts/OMN-11423/dod-omniclaude-pr-1627/command.yaml"
  - kind: "manual"
    description: "GitHub checks show deploy-gate and core CI surfaces passing; only missing evidence gates
      remain."
    command: "See drift/dod_receipts/OMN-11423/dod-ci-surface/command.yaml"
  - kind: "manual"
    description: "No live deploy is required; this changes GitHub Actions deploy-gate validation behavior."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11423/dod-deploy-gate-proof/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1627"
    description: "omniclaude PR #1627 carries the deploy-gate OCC contract lookup change."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11423/dod-omniclaude-pr-1627/command.yaml"
          && grep -q '^pr_number: 1627$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11423/dod-omniclaude-pr-1627/command.yaml"
          && grep -q '^commit_sha: "c86190e9a166fd18ceb94942965675dfef97bf1c"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11423/dod-omniclaude-pr-1627/command.yaml"
  - id: "dod-ci-surface"
    description: "Live GitHub check surface shows deploy-gate and the main CI matrix passed; only missing
      evidence gates remain red."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11423/dod-ci-surface/command.yaml"
          && grep -q 'successes=83' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11423/dod-ci-surface/command.yaml"
          && grep -q 'deploy-gate: SUCCESS' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11423/dod-ci-surface/command.yaml"
  - id: "dod-deploy-gate-proof"
    description: "No live deploy required; change is deploy-gate workflow validation logic."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11423/dod-deploy-gate-proof/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11423 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11423/dod-occ-self/command.yaml"
          && grep -q '^pr_number: 1298$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11423/dod-occ-self/command.yaml"
