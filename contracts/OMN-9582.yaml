---
schema_version: "1.0.0"
ticket_id: OMN-9582
title: "Auto-scaffold OCC contract + receipt stubs"
summary: "Auto-scaffold OCC contract + receipt stubs"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: "Focused scaffold tests pass, including existing-contract idempotency."
    command: "uv run pytest tests/test_auto_scaffold_contract.py -q"
  - kind: ci
    description: "Changed scaffold scripts and tests pass Ruff, mypy, and commit hooks."
    command: "uv run ruff check scripts/auto_scaffold_and_pr.py scripts/auto_scaffold_contract.py tests/test_auto_scaffold_contract.py
      && uv run mypy src/ tests/"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Focused scaffold tests cover generated contracts, receipts, idempotency, and existing-contract
      behavior."
    source: generated
    checks:
      - check_type: test_passes
        check_value: "uv run pytest tests/test_auto_scaffold_contract.py -q"
  - id: dod-002
    description: "Changed Python surfaces pass Ruff, repository mypy, and commit hook validation."
    source: generated
    checks:
      - check_type: command
        check_value: "uv run ruff check scripts/auto_scaffold_and_pr.py scripts/auto_scaffold_contract.py
          tests/test_auto_scaffold_contract.py && uv run mypy src/ tests/"
