---
schema_version: "1.0.0"
ticket_id: "OMN-12989"
title: "OCC evidence for OMN-12989 workspace-mode sibling lock-pin ratchet"
summary: >-
  Bind OMN-12989 omnibase_infra PR #1947 to durable OCC evidence. The PR adds a fail-fast ratchet so the
  workspace-mode image build honors the consuming repo's uv.lock sibling pins (parse uv.lock pins, compare
  staged/installed siblings, abort on a regression below the pin, and emit a pin_comparison block into
  build-provenance.json), closing the deploy-composition hole that caused the 2026-06-11 stability crash.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1947"
    description: >-
      omnibase_infra PR #1947 carries the workspace-build sibling-pin ratchet (resolve_workspace_pins.py
      + provenance enforcement + Dockerfile COPY) with TDD tests proving the 0.37.0-vs-0.38.1 regression
      aborts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12989/dod-omnibase-infra-pr-1947/command.yaml"
  - id: "dod-occ-pr-2498"
    description: "onex_change_control PR #2498 carries the OMN-12989 contract and PASS receipt evidence"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12989/dod-occ-pr-2498/command.yaml"
          && grep -q '^pr_number: 2498$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12989/dod-occ-pr-2498/command.yaml"
