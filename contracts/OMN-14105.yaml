---
schema_version: "1.0.0"
ticket_id: OMN-14105
title: "docs(OMN-14105): genericize onex skills — strip internal operating detail for public exposure"
summary: >
  OCC companion for omniclaude PR #1869, which genericizes plugins/onex/skills/** (85 skill files) ahead
  of the "Fable Going Behind API Pricing" public article driving readers to the repo. Strips OmniNode-internal
  operating context — operator identity, LAN IPs, host nicknames, machine-specific paths, specific OMN-<number>
  ticket references, feedback_*/project_*/reference_* memory-doc citations — while preserving functional
  identifiers ($OMNI_HOME env var, OmniNode-ai org, node_* names, topic strings). Ships with a new enforcement
  gate in the same change (Rule #5): scripts/check_public_skill_hygiene.py wired as both a pre-commit
  hook and a CI workflow (Public Skill Hygiene), not left as advisory-only detection. Independently re-verified
  (not self-authored by the implementing PR) per no-self-authored-evidence policy: hygiene gate re-run
  clean, diff spot-checked for soundness, all changed Python files re-compiled.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omniclaude PR #1869 genericizes plugins/onex/skills/** and wires the new public-skill-hygiene
      gate as both a pre-commit hook and a required CI workflow"
    command: "gh pr diff 1869 --repo OmniNode-ai/omniclaude --name-only"
  - kind: "manual"
    description: "Independent re-run of the hygiene gate against the PR head confirms 0 blocked hits,
      and every changed .py file re-compiles cleanly"
    command: "uv run --with pyyaml python3 scripts/check_public_skill_hygiene.py"
  - kind: "manual"
    description: "OCC PR is explicitly bound as the evidence source for OMN-14105"
    command: "gh pr view 3676 --repo OmniNode-ai/onex_change_control --json number,headRefOid,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1869"
    description: "omniclaude PR #1869 is open at head 0ed4ab5d2 and carries the OMN-14105 genericization
      (85 skill files) plus the new public-skill-hygiene gate wired as a pre-commit hook and a required
      CI workflow."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14105/dod-omniclaude-pr-1869/command.yaml &&
          grep -q '^ticket_id: OMN-14105$' drift/dod_receipts/OMN-14105/dod-omniclaude-pr-1869/command.yaml
          && grep -q '^pr_number: 1869$' drift/dod_receipts/OMN-14105/dod-omniclaude-pr-1869/command.yaml
  - id: "dod-hygiene-gate"
    description: "Independent verifier re-ran the hygiene gate against the PR head (0 blocked hits, 54
      allowlisted) and re-compiled every changed .py file cleanly — not self-attested by the implementing
      PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14105/dod-hygiene-gate/command.yaml && grep
          -q '^evidence_item_id: dod-hygiene-gate$' drift/dod_receipts/OMN-14105/dod-hygiene-gate/command.yaml
  - id: "dod-occ-self"
    description: "Self-binding OCC receipt: this onex_change_control PR carries the OMN-14105 central
      contract and PASS receipts consumed by omniclaude PR #1869."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14105/dod-occ-self/command.yaml && grep -q '^pr_number:
          3676$' drift/dod_receipts/OMN-14105/dod-occ-self/command.yaml
