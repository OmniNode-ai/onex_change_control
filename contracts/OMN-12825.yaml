---
schema_version: "1.0.0"
ticket_id: "OMN-12825"
title: "Gate-coverage prep: queue-safe evidence gates + receipt-honesty gate (omnimarket + omnibase_core)"
summary: >-
  Prep-only work to make evidence gates safe to require later across the gate-coverage epic. For omnimarket
  and omnibase_core: confirms the existing Receipt Gate (call-receipt-gate.yml) and Deploy Gate (deploy-gate.yml)
  already trigger on both pull_request and merge_group, and wires the OMN-12791 receipt-honesty validator:
  a new receipt-honesty.yml CI workflow (pull_request + merge_group) plus the check-receipt-honesty pre-commit
  hook consumed from omnibase_core. omnibase_core coverage lands via PR #1222 (base dev). No branch-protection
  / required_status_checks changes are made here; the required-check flip is a separate later step.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-honesty-precommit-loads"
    description: >-
      The check-receipt-honesty pre-commit hook (OMN-12791, sourced from omnibase_core) is wired into
      omnimarket's .pre-commit-config.yaml and loads cleanly. pre-commit resolves the hook from the bumped
      omnibase_core rev and runs it (no-op skip for omnimarket since the repo carries no drift/dod_receipts/*.yaml
      files).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12825/dod-honesty-precommit-loads/command.yaml
  - id: "dod-honesty-ci-triggers"
    description: >-
      The new receipt-honesty.yml CI workflow parses and triggers on BOTH pull_request and merge_group,
      making it queue-safe for a later additive flip to required.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12825/dod-honesty-ci-triggers/command.yaml
  - id: "dod-existing-gates-queue-safe"
    description: >-
      The pre-existing evidence gates Receipt Gate (call-receipt-gate.yml -> verify / verify) and Deploy
      Gate (deploy-gate.yml -> deploy-gate / deploy-gate) already trigger on both pull_request and merge_group,
      so requiring them later cannot wedge the merge queue.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12825/dod-existing-gates-queue-safe/command.yaml
  - id: "dod-core-pr-1222-honesty-ci"
    description: >-
      omnibase_core PR #1222 adds .github/workflows/receipt-honesty.yml, which parses and triggers on
      BOTH pull_request and merge_group (check-run context 'Receipt Honesty / validate'), making it queue-safe
      for a later additive flip to required.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12825/dod-core-pr-1222-honesty-ci/command.yaml
          && grep -q '^pr_number: 1222$' drift/dod_receipts/OMN-12825/dod-core-pr-1222-honesty-ci/command.yaml
  - id: "dod-core-pr-1222-precommit"
    description: >-
      omnibase_core PR #1222 self-consumes the check-receipt-honesty pre-commit hook (OMN-12791, exported
      by the repo's own .pre-commit-hooks.yaml) in .pre-commit-config.yaml; pre-commit validate-config
      confirms the hook loads.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12825/dod-core-pr-1222-precommit/command.yaml
          && grep -q '^pr_number: 1222$' drift/dod_receipts/OMN-12825/dod-core-pr-1222-precommit/command.yaml
  - id: "dod-core-pr-1222-existing-gates"
    description: >-
      omnibase_core's pre-existing Receipt Gate (verify / verify) and Deploy Gate (deploy-gate / deploy-gate)
      already trigger on both pull_request and merge_group; occ-preflight ('eligibility') is workflow_call-only
      with no caller in omnibase_core and is intentionally excluded from ready-to-require.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12825/dod-core-pr-1222-existing-gates/command.yaml
          && grep -q '^pr_number: 1222$' drift/dod_receipts/OMN-12825/dod-core-pr-1222-existing-gates/command.yaml
  - id: "dod-occ-pr-2340"
    description: "Self-binding receipt for the OCC PR carrying the OMN-12825 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12825/dod-occ-pr-2340/command.yaml && grep -q
          '^pr_number: 2340$' drift/dod_receipts/OMN-12825/dod-occ-pr-2340/command.yaml
