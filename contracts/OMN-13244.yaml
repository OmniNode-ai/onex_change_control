---
schema_version: "1.0.0"
ticket_id: "OMN-13244"
title: "Disable all onex plugin hooks for a clean measurement baseline"
summary: >-
  Remove every hook registration from plugins/onex/hooks/hooks.json by setting hooks to an empty object,
  so Claude Code invokes no onex hooks. This establishes a clean, no-instrumentation baseline for measuring
  whether the hook surface helps or hurts before re-introducing hooks deliberately. Hook scripts remain
  on disk; rollback is a config-only revert. The test suite is adjusted so hook registration checks accept
  the empty-registration baseline while retaining contract and script-path validation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-hooks-pr-1771"
    description: >-
      Per-code-PR binding receipt for omniclaude PR #1771 (jonahgabriel/omn-13244-disable-all-onex-plugin-hooks-gut-hooksjson-for-a-clean,
      base=dev). The PR sets plugins/onex/hooks/hooks.json to hooks == {} and adjusts hook registration
      tests for the empty-registration measurement baseline. This receipt binds OMN-13244 to PR #1771
      by pr_number and commit_sha so downstream Receipt Gate can resolve central OCC evidence for the
      citing PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13244/dod-hooks-pr-1771/command.yaml
  - id: "dod-occ-pr-2752"
    description: >-
      OCC self-binding receipt for onex_change_control PR #2752, which carries the central OMN-13244 contract
      and receipt binding for omniclaude PR #1771. This receipt binds the OCC PR itself by pr_number so
      OCC eligibility can validate the central evidence PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13244/dod-occ-pr-2752/command.yaml
