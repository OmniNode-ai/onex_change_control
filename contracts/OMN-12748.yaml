---
schema_version: "1.0.0"
ticket_id: "OMN-12748"
title: "Expose delegated prompt and response text through correlation projection"
summary: >
  Adds the contract-declared onex.snapshot.projection.delegation.correlation-trace.v1 projection exposure
  so dashboard and integration tests can inspect prompt_text and response_text for a delegated run without
  adding a bespoke REST route or bloating the high-frequency decisions projection.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "topics"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1073"
    description: "omnimarket PR #1073 carries the OMN-12748 projection contract change."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12748/dod-omnimarket-pr-1073/command.yaml"
          && grep -q '^pr_number: 1073$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12748/dod-omnimarket-pr-1073/command.yaml"
          && grep -q '67f83fb1' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12748/dod-omnimarket-pr-1073/command.yaml"
  - id: "dod-projection-contract-test"
    description: "Focused projection contract test proves prompt_text and response_text are exposed on
      the correlation trace and omitted from decisions."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12748/dod-projection-contract-test/command.yaml"
          && grep -q '7 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12748/dod-projection-contract-test/command.yaml"
  - id: "dod-contract-topic-declaration"
    description: "Correlation trace projection topic is declared in the delegation projection contract."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12748/dod-contract-topic-declaration/command.yaml"
          && grep -q 'onex.snapshot.projection.delegation.correlation-trace.v1' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12748/dod-contract-topic-declaration/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Post-merge dev-lane deploy is required before live projection-api serves the new topic."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12748/dod-deploy-assessment/command.yaml"
          && grep -q 'dev-lane deploy required' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12748/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OMN-12748 contract and PASS receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12748/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-12748"$' "$CONTRACT_REPO_DIR/contracts/OMN-12748.yaml"
