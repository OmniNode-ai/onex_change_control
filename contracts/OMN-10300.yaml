---
schema_version: 1.0.0
ticket_id: OMN-10300
title: 'Add cost.token_usage.v1 topic to TOPICS constant'
summary: 'Adds costTokenUsage key to the TOPICS constant in omnidash shared/types/topics.ts'
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - topics
contract_completeness: FULL
evidence_requirements:
  - kind: ci
    description: TypeScript typecheck passes with new topic key
    command: cd omnidash && npm run check
  - kind: ci
    description: Topics type test passes
    command: cd omnidash && npm run test:run -- shared/types/topics.test.ts
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: TOPICS.costTokenUsage key present with correct value
    source: generated
    checks:
      - check_type: command
        check_value: "grep -F \"costTokenUsage: 'onex.snapshot.projection.cost.token_usage.v1'\" omnidash/shared/types/topics.ts"
  - id: dod-002
    description: Topics type test file present
    source: generated
    checks:
      - check_type: command
        check_value: test -f omnidash/shared/types/topics.test.ts
  - id: dod-003
    description: Topics type tests pass (4 tests)
    source: generated
    checks:
      - check_type: command
        check_value: "cd omnidash && npm run test:run -- shared/types/topics.test.ts 2>&1 | grep -E '4
          passed'"
  - id: dod-004
    description: Pre-commit hooks pass clean
    source: generated
    checks:
      - check_type: command
        check_value: cd omnidash && pre-commit run --all-files
