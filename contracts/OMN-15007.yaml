---
schema_version: "1.0.0"
ticket_id: "OMN-15007"
title: "Autobind OCC evidence for OMN-15007"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1875."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1875 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1875"
    description: "PR #1875 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1875-ci"
    description: "PR #1875 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4725"
    description: "OCC companion PR #4725 — self-bind for OMN-15007 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1875-deploy-gate-disposition"
    description: "PR #1875 deploy-gate disposition -- runtime contract paths touched and post-merge dev
      redeploy validation bar is explicit in the PR body."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1875 --repo OmniNode-ai/omnimarket --json body,files --jq 'if ((.body
          | test(\"Post-merge dev redeploy\")) and any(.files[].path; . == \"src/omnimarket/nodes/node_adr_canary_orchestrator/contract.yaml\"
          or . == \"src/omnimarket/nodes/node_adr_document_ingestion_effect/contract.yaml\" or . == \"src/omnimarket/nodes/node_intent_query_effect/contract.yaml\"))
          then \"deploy gate disposition verified: runtime contract paths touched; PR body carries post-merge
          dev redeploy validation bar\" else error(\"deploy gate disposition missing runtime contract
          path or PR body deploy bar\") end'"
