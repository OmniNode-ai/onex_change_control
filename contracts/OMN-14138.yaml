---
schema_version: "1.0.0"
ticket_id: "OMN-14138"
title: "Refresh omnimarket sibling lock pins"
summary: >-
  Central evidence for the omnimarket lockfile refresh that repins omnibase-core from 0.46.4 to the latest
  published 0.46.5 release while leaving omnibase-spi at its already-current 0.23.1 pin. The downstream
  PR is intentionally limited to uv.lock; the remaining omnibase-core 0.46.6 dev-head drift requires a
  separate publish or operator drift decision.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "omnimarket PR #1644 is open at the recorded lock-refresh branch head."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14138/dod-omnimarket-pr-1644/command.yaml"
  - kind: "manual"
    description: "The OCC companion PR carries the OMN-14138 central contract and receipts."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14138/dod-occ-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1644"
    description: >-
      omnimarket PR #1644 carries the OMN-14138 uv.lock-only sibling pin refresh at the recorded branch
      head. Its PR body records local verification: uv lock upgrade, Ruff format/check, pre-commit on
      uv.lock, and the sibling-pin preflight outcome showing omnibase-spi resolved and omnibase-core reduced
      to the unreleased 0.46.6 gap.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14138/dod-omnimarket-pr-1644/command.yaml"
          && grep -q '^pr_number: 1644$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14138/dod-omnimarket-pr-1644/command.yaml"
  - id: "dod-occ-self"
    description: "The OCC companion branch carries the OMN-14138 central contract and receipt evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14138/dod-occ-self/command.yaml"
          && grep -q '^ticket_id: OMN-14138$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14138/dod-occ-self/command.yaml"
  - id: "dod-omnibase-infra-pr-2233"
    description: >-
      omnibase_infra PR #2233 is the build-infra companion fix for the same OMN-14138 sibling-pin recurrence:
      it updates the workspace sibling-pin guard so registry-sourced forward version drift is allowed
      in workspace builds while backward/stale clone drift remains fatal. This binds the OMN-14138 citation
      in #2233 to central OCC evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14138/dod-omnibase-infra-pr-2233/command.yaml"
          && grep -q '^pr_number: 2233$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14138/dod-omnibase-infra-pr-2233/command.yaml"
