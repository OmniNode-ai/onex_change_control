---
schema_version: "1.0.0"
ticket_id: "OMN-15028"
title: "Autobind OCC evidence for OMN-15028"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1879."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1879 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1879"
    description: "PR #1879 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1879-ci"
    description: "PR #1879 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4736"
    description: "OCC companion PR #4736 — self-bind for OMN-15028 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omn15028-pr1879-deploy"
    description: "PR #1879 deploy disposition — runtime-path change carries explicit post-merge dev deploy
      validation bar."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1879 --repo OmniNode-ai/omnimarket --json body,files --jq 'if ((.body
          | test(\"Post-merge dev deploy validation\")) and (.body | test(\"node_occ_companion_compute\"))
          and any(.files[].path; . == \"src/omnimarket/nodes/node_occ_companion_compute/handlers/handler_occ_companion_compute.py\"))
          then \"deploy disposition verified for OMN-15028 omnimarket OCC companion renderer\" else error(\"OMN-15028
          deploy disposition missing PR body bar or runtime path\") end'"
  - id: "occ-self-bind-pr-4737"
    description: "OCC companion PR #4737 — self-bind for OMN-15028 deploy disposition."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4737 --repo OmniNode-ai/onex_change_control --json number,state"
