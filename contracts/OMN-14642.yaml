---
schema_version: 1.0.0
ticket_id: OMN-14642
title: Route dashboard projection reads through projection-api HTTP proxy
summary: Central OCC evidence for omnidash#261, which changes the dashboard bridge default from direct
  Postgres/RDS projection reads to the projection-api HTTP proxy path and removes the default bridge RDS
  credential reference.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: ci
    description: omnidash#261 product CI succeeds at exact head 6d0dfe1439106db441ea44bd96ea0a6018cc3aa7.
    command: gh api repos/OmniNode-ai/omnidash/actions/runs/29625846846
  - kind: ci
    description: omnidash#261 PR body records local dashboard verification and seam regression evidence.
    command: gh pr view 261 --repo OmniNode-ai/omnidash --json body
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnidash-pr-261
    description: omnidash#261 is bound to OMN-14642 at exact product head 6d0dfe1439106db441ea44bd96ea0a6018cc3aa7.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=261; PRODUCT_REPO=OmniNode-ai/omnidash; PRODUCT_HEAD=6d0dfe1439106db441ea44bd96ea0a6018cc3aa7;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnidash-pr-261-diff
    description: omnidash#261 has a non-empty reviewed product diff for OMN-14642.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=261; PRODUCT_REPO=OmniNode-ai/omnidash; : ${PR_NUMBER}; gh pr
          diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q .'
  - id: dod-omnidash-pr-261-ci
    description: omnidash#261 hosted product CI completed successfully at exact head 6d0dfe1439106db441ea44bd96ea0a6018cc3aa7.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29625846846; PRODUCT_REPO=OmniNode-ai/omnidash; PRODUCT_HEAD=6d0dfe1439106db441ea44bd96ea0a6018cc3aa7;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-omnidash-pr-261-local-verification
    description: omnidash#261 PR body records local dashboard verification, including the full test suite
      and bridge HTTP seam regression.
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=261; PRODUCT_REPO=OmniNode-ai/omnidash; : ${PR_NUMBER}; gh pr
          view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "1608 passed /
          15 skipped" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep
          -q "New http-seam regression tests pass"'
  - id: dod-omnidash-pr-261-deploy
    description: Deploy-readiness assessment for OMN-14642. The omnidash PR changes dashboard bridge runtime
      defaults and requires deploy-gate evidence; this evidence lane performed no live deploy, restart,
      .201, or production mutation and records the deployment overlay dependency for projection-api base
      URL.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14642/dod-omnidash-pr-261-deploy/command.yaml
          && grep -qi ''deploy'' drift/dod_receipts/OMN-14642/dod-omnidash-pr-261-deploy/command.yaml'
