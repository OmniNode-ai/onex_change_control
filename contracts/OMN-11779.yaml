---
schema_version: "1.0.0"
ticket_id: "OMN-11779"
title: "Model registry snapshot + pricing manifest tooling"
summary: >
  Versioned YAML model registry with Pydantic loader and deterministic SHA-256 hash generation for LLM
  model capabilities and pricing (epic OMN-11771 delegation pipeline).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-model-registry-tests"
    description: "17 unit tests pass: profile validation, hash determinism, loader round-trip"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/models/delegation/llm_cost_routing/test_model_registry.py
          -v --tb=short"
  - id: "dod-omnimarket-pr-767"
    description: "omnimarket PR #767 implements registry YAML, Pydantic models, loader, and hash generation"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omnimarket --json state -q .state"
