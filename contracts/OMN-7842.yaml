---
schema_version: '1.0.0'
ticket_id: OMN-7842
title: 'feat(omniclaude): launchd plists for zero-touch overnight automation'
summary: 'Adds cron-closeout and nightly-merge-sweep launchd plists with session ID correlation for overnight
  automation.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: New plist files pass XML validation
    command: plutil -lint scripts/launchd/ai.omninode.cron-closeout.plist scripts/launchd/ai.omninode.nightly-merge-sweep.plist
  - kind: ci
    description: CI pipeline green on PR
    command: gh pr checks 1499 --repo OmniNode-ai/omniclaude
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: cron-closeout plist template exists and passes XML validation
    source: manual
    checks:
      - check_type: command
        check_value: plutil -lint scripts/launchd/ai.omninode.cron-closeout.plist
  - id: dod-002
    description: nightly-merge-sweep plist template exists with StartCalendarInterval for 2:00 AM
    source: manual
    checks:
      - check_type: command
        check_value: plutil -lint scripts/launchd/ai.omninode.nightly-merge-sweep.plist
      - check_type: command
        check_value: grep -q 'StartCalendarInterval' scripts/launchd/ai.omninode.nightly-merge-sweep.plist
  - id: dod-003
    description: tick-bundle-install.sh includes both new plists in TICKS array
    source: manual
    checks:
      - check_type: command
        check_value: grep -q 'ai.omninode.cron-closeout' scripts/tick-bundle-install.sh
      - check_type: command
        check_value: grep -q 'ai.omninode.nightly-merge-sweep' scripts/tick-bundle-install.sh
  - id: dod-004
    description: nightly-merge-sweep.sh generates ONEX_RUN_ID for session correlation
    source: manual
    checks:
      - check_type: command
        check_value: grep -q 'ONEX_RUN_ID' scripts/nightly-merge-sweep.sh
