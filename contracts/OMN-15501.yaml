---
schema_version: "1.0.0"
ticket_id: "OMN-15501"
title: "Require exact current-head binding for receipt-gated product PRs"
summary: >-
  Makes product receipt eligibility content-addressed to the pull request's current headRefOid, while
  retaining explicit PR-identity self-binding for the central OCC evidence companion so squash merges
  do not invalidate its own evidence.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >-
      Focused validator and reusable-workflow tests prove stale additive and rebase receipt bindings fail,
      exact product-head bindings pass, malformed or absent head identity fails closed, and OCC self-binding
      remains PR-identity based.
    command: >-
      uv run pytest tests/unit/validation/test_occ_merge_eligibility.py tests/unit/validation/test_occ_per_entry_hashing.py
      tests/unit/validation/test_occ_awaiting_runner_receipt_omn_16859.py tests/unit/validation/test_receipt_gate_workflow_shape.py
      -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-exact-head-receipt-gate"
    description: >-
      The core product proof at immutable commit 37ff0ee4 exercises the required current-head receipt
      binding, stale/rebase RED paths, exact-head GREEN path, fail-closed head validation, OCC PR-identity
      self-bind, and reusable-workflow propagation.
    source: "manual"
    execution_scope: "local_done_gate"
    checks:
      - check_type: "test_passes"
        check_value: >-
          gh api repos/OmniNode-ai/omnibase_core/commits/37ff0ee4bcd497f248975dd89e94e4abe9737f09 --jq
          .sha >/dev/null && uv run pytest tests/unit/validation/test_occ_merge_eligibility.py tests/unit/validation/test_occ_per_entry_hashing.py
          tests/unit/validation/test_occ_awaiting_runner_receipt_omn_16859.py tests/unit/validation/test_receipt_gate_workflow_shape.py
          -q
        cwd: "${OMNI_HOME}/omni_worktrees/OMN-15501/omnibase_core"
