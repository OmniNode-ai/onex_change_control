---
schema_version: "1.0.0"
ticket_id: "OMN-10653"
title: "Add concrete dev/prod cloud deployment plan"
summary: >
  Adds the docs-only Phase 0 cloud deployment plan for isolated dev and prod environments, including topology,
  CI/CD promotion model, DNS split, database isolation, and execution phases.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-cloud-plan-doc"
    description: "omninode_infra PR #386 adds the cloud deployment plan document."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10653/dod-cloud-plan-doc/command.yaml\""
  - id: "dod-pr-386-head"
    description: "Bind OMN-10653 evidence to omninode_infra PR #386 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10653/dod-pr-386-head/command.yaml\""
