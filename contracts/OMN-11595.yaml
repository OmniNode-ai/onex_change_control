---
schema_version: "1.0.0"
ticket_id: "OMN-11595"
title: "Register GitHub API poller in effects runtime"
summary: >-
  Binds central OCC evidence for omnibase_infra PR #1709, which scopes node_github_pr_poller_effect to
  the effects runtime profile, validates runtime-effects GITHUB_TOKEN catalog coverage, and proves runtime-tick
  invocation resolves contract-backed poller configuration.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused runtime profile, poller, catalog, boot, and integration tests pass on omnibase_infra
      PR #1709."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11595/dod-omnibase-infra-pr-1709/command.yaml"
  - kind: "manual"
    description: "Deploy-gate assessment is recorded for the runtime-path changes."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11595/dod-deploy-assessment/command.yaml"
  - kind: "manual"
    description: "OCC evidence PR receipt is present and PASS."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11595/dod-occ-pr-self/command.yaml"
dod_evidence:
  - id: "dod-omnibase-infra-pr-1709"
    description: "Bind omnibase_infra PR #1709 implementation and focused validation at observed PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11595/dod-omnibase-infra-pr-1709/command.yaml"
          && grep -q '^pr_number: 1709$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11595/dod-omnibase-infra-pr-1709/command.yaml"
          && grep -q '^commit_sha: "0fcf30530def288dc434a0838a051aa86e047e89"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11595/dod-omnibase-infra-pr-1709/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Deploy DoD evidence for PR #1709 runtime effects poller registration."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11595/dod-deploy-assessment/command.yaml"
          && grep -qi 'deploy' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11595/dod-deploy-assessment/command.yaml"
          && grep -q '^pr_number: 1709$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11595/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11595 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11595/dod-occ-pr-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
