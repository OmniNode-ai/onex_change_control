---
schema_version: "1.0.0"
ticket_id: OMN-10859
title: "Semantic ticket verification gate — Phase 1 advisory"
summary: "Add semantic_grading check_type to ModelDodCheck and implement node_pr_semantic_grader_llm_effect
  in omnimarket for advisory-mode PR acceptance criteria grading."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: tests
    description: OCC unit tests pass with new check_type
    command: uv run pytest tests/ -v -m unit
  - kind: tests
    description: omnimarket semantic grader node tests pass
    command: uv run pytest src/omnimarket/nodes/node_pr_semantic_grader_llm_effect/tests/ -v
  - kind: ci
    description: CI passes on OCC PR  #954
    command: gh pr checks 954 --repo OmniNode-ai/onex_change_control
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-enum-core
    description: SEMANTIC_GRADING added to EnumDodCheckType in omnibase_core PR  #1066
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10859/dod-enum-core/command.yaml\""
  - id: dod-occ-check-type
    description: semantic_grading added to ModelDodCheck.check_type Literal in OCC
    source: generated
    checks:
      - check_type: grep
        check_value:
          pattern: "semantic_grading"
          path: src/onex_change_control/models/model_dod_check.py
  - id: dod-occ-tests
    description: OCC unit tests pass with all 7 check_types including semantic_grading
    source: generated
    checks:
      - check_type: command
        check_value: "uv run pytest tests/ -v -m unit -q 2>&1 | grep -q 'passed'"
  - id: dod-grader-node
    description: node_pr_semantic_grader_llm_effect exists in omnimarket with contract, handler, models,
      tests
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10859/dod-grader-node/command.yaml\""
  - id: dod-grader-tests
    description: 16 unit tests for HandlerPrSemanticGrader pass including OMN-10834 anti-pattern fixture
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10859/dod-grader-tests/command.yaml\""
  - id: dod-deploy
    description: "Deploy-gate evidence: Phase 1 advisory node, no runtime deploy or docker exec required."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10859/dod-deploy/command.yaml\""
  - id: dod-pr-occ
    description: OCC PR  #954 opened and CI green
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr view 954 --repo OmniNode-ai/onex_change_control --json state -q .state | grep
          -q OPEN"
