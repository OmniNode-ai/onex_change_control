---
schema_version: "1.0.0"
ticket_id: "OMN-9906"
title: "feat(OMN-9906): add WORKTREE_GUARD bit to EnumHookBit"
summary: >
  Add WORKTREE_GUARD = 1 << 59 to EnumHookBit in enum_hook_bit.py. This bit is the canonical bitmask entry
  for the git-worktree path enforcement hook, replacing the legacy ONEX_WORKTREE_GUARD env-var toggle.
  Append-only per bit governance policy (bit 59 follows TASK_BOUNDARY_TESTS at bit 58).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "hook-bits drift check passes after the enum addition and the unit suite stays green"
    command: "cd omnibase_core && uv run pytest tests/ -m unit -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "omnibase_core PR #1068 carries the WORKTREE_GUARD EnumHookBit addition with hook-bits
      drift check and unit suite passing locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9906/dod-001/command.yaml && grep
          -q '^ticket_id: \"OMN-9906\"$' drift/dod_receipts/OMN-9906/dod-001/command.yaml && grep -q '^pr_number:
          1068$' drift/dod_receipts/OMN-9906/dod-001/command.yaml"
  - id: "dod-occ-pr-972"
    description: "OCC PR #972 carries the OMN-9906 central contract and downstream receipt needed by omnibase_core
      PR #1068."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9906/dod-occ-pr-972/command.yaml
          && grep -q '^ticket_id: \"OMN-9906\"$' drift/dod_receipts/OMN-9906/dod-occ-pr-972/command.yaml
          && grep -q '^pr_number: 972$' drift/dod_receipts/OMN-9906/dod-occ-pr-972/command.yaml"
  - id: "dod-omniclaude-pr-1572"
    description: "omniclaude PR #1572 migrates the worktree and team-lead guards to the ONEX_HOOKS_MASK
      WORKTREE_GUARD bit gate."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9906/dod-omniclaude-pr-1572/command.yaml
          && grep -q '^ticket_id: OMN-9906$' drift/dod_receipts/OMN-9906/dod-omniclaude-pr-1572/command.yaml
          && grep -q 'TEAM_LEAD_GUARD' drift/dod_receipts/OMN-9906/dod-omniclaude-pr-1572/command.yaml
          && grep -q '^pr_number: 1572$' drift/dod_receipts/OMN-9906/dod-omniclaude-pr-1572/command.yaml"
  - id: "dod-occ-pr-974"
    description: "OCC PR #974 carries the OMN-9906 omniclaude receipt needed by omniclaude PR #1572."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9906/dod-occ-pr-974/command.yaml
          && grep -q '^ticket_id: OMN-9906$' drift/dod_receipts/OMN-9906/dod-occ-pr-974/command.yaml &&
          grep -q '^pr_number: 974$' drift/dod_receipts/OMN-9906/dod-occ-pr-974/command.yaml"
