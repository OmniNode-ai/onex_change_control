---
schema_version: "1.0.0"
ticket_id: "OMN-12935"
title: "Unwrap runtime envelope in evidence-pipeline coerce path"
summary: >
  Central OCC evidence for omnimarket PR #1151, which fixes the BROKEN evidence-pipeline envelope-unwrap
  defect: coerce_command and every sibling coerce_* constructed their wire models directly from the runtime-delivered
  envelope dict, raising ValidationError and stranding the FSM at hop 1 with all four projections at 0
  rows. Adds a shared unwrap_envelope helper, routes every coerce through it, and fixes the dashboard-effect
  normalize path that read ids off the outer envelope and propagated a malformed UUID downstream.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >
      Failing-first tests reproduce runtime-shaped (bare + doubly-wrapped) envelope delivery for coerce_command,
      the collector effect, the orchestrator, and the dashboard effect; all pass after the unwrap fix.
    command: "uv run pytest tests/test_evidence_pipeline_native_nodes.py tests/test_evidence_dashboard_nodes.py
      tests/test_golden_chain_evidence_dashboard_effect.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-1151"
    description: "omnimarket PR #1151 carries the OMN-12935 evidence-pipeline envelope-unwrap fix with
      failing-first tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12935/dod-product-pr-1151/command.yaml"
  - id: "dod-deploy-assessment"
    description: >
      Source-only fix in omnimarket nodes. No live deploy performed; the unwrap applies on the next stability-test
      effects-image rebuild (pre-approved stability hot-patch path) and on the next normal deploy.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12935/dod-deploy-assessment/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-12935/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR #2448 carries the central OCC evidence (contract + PASS receipts)
      for OMN-12935."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12935/dod-occ-pr/command.yaml"
