---
schema_version: "1.0.0"
ticket_id: OMN-10129
title: "Replace mock SocketCheckTarget with real os.stat()+S_ISSOCK() implementation in node_process_watchdog"
summary: "Replace mock SocketCheckTarget with real os.stat()+S_ISSOCK() implementation in node_process_watchdog"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR checks pass including typecheck and test"
    command: "gh pr checks 429 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "Unit tests for SocketCheckTarget pass (3 cases: missing, stale, fresh)"
    command: "uv run pytest tests/unit/nodes/node_process_watchdog/test_check_socket.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "All 3 unit tests for SocketCheckTarget pass (FAIL/missing, WARN/stale, PASS/fresh)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_process_watchdog/test_check_socket.py -v"
  - id: "dod-002"
    description: "mypy strict passes on node_process_watchdog handlers"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run mypy src/omnimarket/nodes/node_process_watchdog/ --strict"
  - id: "dod-003"
    description: "Post-merge deploy smoke: SocketCheckTarget imports cleanly in deployed runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_process_watchdog.handlers.check_socket
          import SocketCheckTarget; print('deploy smoke ok')\""
