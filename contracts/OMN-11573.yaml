---
schema_version: "1.0.0"
ticket_id: "OMN-11573"
title: "Consolidate GitHub PR poller HTTP adapter"
summary: >-
  Bind central OCC evidence for omnibase_infra PR #1707, which consolidates GitHub PR poller API calls
  onto the canonical GitHubHttpClient adapter and updates focused unit, integration, protocol, and stability
  compose evidence.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "omnibase_infra PR #1707 CI and receipt gates pass for the adapter consolidation."
    command: "gh pr checks 1707 --repo OmniNode-ai/omnibase_infra"
  - kind: "manual"
    description: "Central OCC evidence PR carries this contract and PASS receipts."
    command: "gh pr view <occ-pr> --repo OmniNode-ai/onex_change_control"
dod_evidence:
  - id: "dod-omnibase-infra-pr-1707"
    description: "Bind OMN-11573 evidence to omnibase_infra PR #1707 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11573/dod-omnibase-infra-pr-1707/command.yaml\"
          && grep -q '^pr_number: 1707$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11573/dod-omnibase-infra-pr-1707/command.yaml\"
          && grep -q '^commit_sha: \"ded097977324aed02364b7a7b96364e91b758e36\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11573/dod-omnibase-infra-pr-1707/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Adapter consolidation changes runtime node paths but requires no live deploy before
      merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11573/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11573/dod-deploy-assessment/command.yaml\"
          && grep -q 'adapter consolidation' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11573/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Bind OMN-11573 central evidence to the OCC PR that carries this bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11573/dod-occ-pr-self/command.yaml\"
          && grep -q '^ticket_id: \"OMN-11573\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11573/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
