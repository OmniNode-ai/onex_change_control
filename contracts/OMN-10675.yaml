---
schema_version: "1.0.0"
ticket_id: "OMN-10675"
title: "Standardize build workflows on sha tags and remove latest images"
summary: >
  Bind omninode_infra PR #417 evidence for removing deploy-side :latest image references, standardizing
  OmniNode-built image workflows on deterministic sha tags, and correcting Kubernetes validation gates
  for the updated placeholder/image-override pattern.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-no-latest"
    description: "No deploy-side k8s manifest or relevant OmniNode image build workflow contains an image:
      ... :latest reference."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10675/dod-no-latest/command.yaml"
  - id: "dod-kustomize-hygiene"
    description: "onex-dev kustomize rendering and placeholder hygiene checks pass after adding explicit
      image overrides."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10675/dod-kustomize-hygiene/command.yaml"
  - id: "dod-pr-open"
    description: "omninode_infra PR #417 carries the OMN-10675 follow-up implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10675/dod-pr-open/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR #941 carries the central OCC evidence for OMN-10675."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10675/dod-occ-pr/command.yaml"
