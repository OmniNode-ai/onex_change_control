---
schema_version: "1.0.0"
ticket_id: "OMN-10239"
summary: "Add advisory validators and a migration manifest for runtime-backed skill path consolidation"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused validator and manifest checks prove advisory drift reporting without blocking
      the migration prematurely"
    command: "cd ../omnibase_infra && uv run pytest tests -k 'validator or migration_manifest or canonical_skill_path'
      -q"
  - kind: "ci"
    description: "PR checks pass for advisory validators and migration manifest changes across touched
      repos"
    command: "gh pr checks --repo OmniNode-ai/omnibase_infra --watch && gh pr checks --repo OmniNode-ai/omnimarket
      --watch && gh pr checks --repo OmniNode-ai/omniclaude --watch"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Canonical proof exists that advisory validators report forbidden non-canonical skill
      paths"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10239/dod-001 -type f -name '*.yaml' | grep -q ."
  - id: "dod-002"
    description: "Canonical proof exists that a migration manifest tracks runtime-backed skills and exemptions"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10239/dod-002 -type f -name '*.yaml' | grep -q ."
  - id: "dod-003"
    description: "Canonical proof exists that validator flip criteria from advisory to blocking are explicit"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10239/dod-003 -type f -name '*.yaml' | grep -q ."
