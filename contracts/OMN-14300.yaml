---
schema_version: 1.0.0
ticket_id: OMN-14300
title: 'refactor(OMN-14300): canonicalize prod health fact resolver handler'
summary: 'omnimarket PR #1689 canonicalizes node_prod_health_fact_resolver_effect to the thin contract+handler
  shape. HandlerProdHealthFactResolver.handle now accepts a single typed ModelProdHealthResolveCommand
  payload and returns the typed ModelProdHealthResolvedEvent directly while preserving the existing health
  fact resolution behavior and golden-chain coverage.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnimarket-pr-1689-head-3ea27a9
    description: 'Foreground Codex verification of omnimarket PR #1689 at refreshed head 3ea27a9c84d59eda47296f68b76daf0fe0256476.
      Local focused checks passed: ruff check and ruff format --check on node_prod_health_fact_resolver_effect
      source/test paths, mypy on node_prod_health_fact_resolver_effect, pytest golden-chain prod health
      fact resolver suite (16 passed), changed-node golden-chain coverage, strict changed-node state coverage,
      unimported-handler wiring, changed-node node-drift gate, and market_node_runtime_dogfood.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14300/dod-omnimarket-pr-1689-head-3ea27a9/command.yaml
          && grep -q ''^pr_number: 1689$'' drift/dod_receipts/OMN-14300/dod-omnimarket-pr-1689-head-3ea27a9/command.yaml
          && grep -q ''3ea27a9c84d59eda47296f68b76daf0fe0256476'' drift/dod_receipts/OMN-14300/dod-omnimarket-pr-1689-head-3ea27a9/command.yaml'
  - id: dod-deploy-assessment-head-3ea27a9
    description: 'Deploy-surface assessment for omnimarket PR #1689 head 3ea27a9c84d59eda47296f68b76daf0fe0256476:
      the diff is limited to prod health fact resolver handler signature canonicalization, direct typed
      event return, dead import cleanup, and focused tests. It does not mutate live deployment configuration,
      perform a deploy/restart, change topics, run migrations, or touch .201.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14300/dod-deploy-assessment-head-3ea27a9/command.yaml
          && grep -qi ''no live deploy'' drift/dod_receipts/OMN-14300/dod-deploy-assessment-head-3ea27a9/command.yaml'
  - id: dod-occ-self-commit-initial
    description: 'OCC companion self-binding: the initial onex_change_control commit for OMN-14300 carries
      this central contract plus the product and deploy PASS receipts for omnimarket PR #1689 head 3ea27a9c84d59eda47296f68b76daf0fe0256476.
      The follow-up receipt binds that initial OCC evidence commit for occ-preflight eligibility before
      the OCC PR is merged.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14300/dod-occ-self-commit-initial/command.yaml
          && grep -Eq ''^[[:space:]]*commit_sha: [0-9a-f]{40}$'' drift/dod_receipts/OMN-14300/dod-occ-self-commit-initial/command.yaml'
