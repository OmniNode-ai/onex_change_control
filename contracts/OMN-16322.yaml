---
schema_version: "1.0.0"
ticket_id: "OMN-16322"
title: "occ-preflight cold-runner timeout lands passing jobs as CANCELLED"
summary: >-
  Raises the occ-preflight / eligibility job timeout from 10m to 15m. On a cold trusted-CI runner the
  job spends ~2m38s in Set up job (image pull) and ~4m05s checking out omnibase_compat before the eligibility
  check starts, so it reaches ~10m wall and the timeout fires during results upload, after every step
  including Complete job has already succeeded. GitHub stamps a timed-out job cancelled, and both CI Summary
  and GitHub's required-check semantics fail closed on cancelled, so a job that logged "OCC eligibility:
  PASS" still blocks the PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-onex-change-control-pr-6805-timeout"
    description: >-
      onex_change_control PR #6805 changes only .github/workflows/call-occ-preflight.yml, raising the
      occ-preflight / eligibility job timeout from 10 to 15 minutes. Observed against the live PR file
      list rather than a pinned head commit so a branch refresh from dev cannot stale the evidence, while
      still failing if the workflow file is absent from the PR.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 6805 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path' | grep
          -Fx '.github/workflows/call-occ-preflight.yml'
  - id: "occ-self-bind-pr-6805"
    description: >-
      OCC self-binding receipt for PR #6805, the in-repo evidence PR carrying this contract and the workflow
      repair. Binds OMN-16322 to the OCC PR by pr_number so the OCC receipt-gate eligibility check can
      resolve a PASS receipt for the evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 6805 --repo OmniNode-ai/onex_change_control --json number,state
  - id: "dod-OmniNode-ai-omnibase_core-pr-1573"
    description: >-
      omnibase_core PR #1573 raises the same occ-preflight / eligibility timeout from 10 to 15 minutes
      in .github/workflows/occ-preflight.yml, the REUSABLE workflow consumed via uses:...occ-preflight.yml@main
      by omnibase_core, omniclaude, omnidash and omnimarket. This is the sibling of the inlined-copy fix
      already carried by onex_change_control#6805. Content-bound probe: it reads the omnibase_core blob
      at the PR head through the GitHub API, so it executes against a surface this OCC PR does not author,
      and it is falsifiable — verified GREEN at head 632b5350bd717b9d649ffe9dfd154ada05c93885 and RED
      at merge-base bfc05d136123efa680844ce4a12c3c040cba848f, where the value is still 10.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api 'repos/OmniNode-ai/omnibase_core/contents/.github/workflows/occ-preflight.yml?ref=632b5350bd717b9d649ffe9dfd154ada05c93885'
          --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF 'timeout-minutes: 15'
  - id: "occ-self-bind-pr-6838"
    description: "Binds OMN-16322 to OCC companion PR #6838 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6838 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-onex-change-control-pr-6850-corebump"
    description: >-
      onex_change_control PR #6850 is OCC's own self-referential dependency-bump PR, bumping the omnibase-core
      pin in this repo's own uv.lock from 0.46.9 to 0.46.11. The OCC Autobind Publisher declined to mint
      a companion for it (2026-08-23T07:39:49Z, onexbot-occ-writer[bot]): a pure lockfile-only bump has
      no changed-file candidate the emitter can prove RED-at-merge-base-then-GREEN- at-head against, and
      it correctly refused to emit a bare PR-existence probe as non-falsifiable evidence (OMN-15247).
      This entry is the manually-authored, content-bound replacement the emitter itself named as the required
      path (tracked structurally as OMN-16410): it reads the PR's own diff rather than asserting mere
      existence, and it is falsifiable — verified GREEN against PR #6850's diff, which adds the exact
      line `+version = "0.46.11"` under the `omnibase-core` package block, and RED against origin/dev's
      uv.lock, which still pins `version = "0.46.9"` for the same package as of this probe. Mirrors the
      dod-onex-change-control-pr-6805-timeout / occ-self-bind-pr-6805 precedent (source: "manual", genuinely
      falsifiable file-content check) rather than a bare existence probe.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr diff 6850 --repo OmniNode-ai/onex_change_control | grep -F '+version = "0.46.11"'
  - id: "dod-OmniNode-ai-omniclaude-pr-2010-current-head"
    description: >-
      omniclaude PR #2010 is the downstream dependency-bump PR for the omnibase_core 0.46.11 release.
      It carries no source changes beyond the lockfile/pin refresh, so this evidence binds OMN-16322 to
      the exact current product PR head that updates the dependency version. The probe reads the live
      PR head and diff and fails if the PR no longer exists, moves off the recorded head, or stops adding
      version 0.46.11.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2010 --repo OmniNode-ai/omniclaude --json number,state,headRefOid | grep -F '"headRefOid":"7ddd4f26f9628a29f332d4ffff752239f53e47e3"'
          && gh pr diff 2010 --repo OmniNode-ai/omniclaude | grep -F '+version = "0.46.11"'
  - id: "dod-omniclaude-pr-2010-merged"
    description: >-
      Append-only replacement for dod-OmniNode-ai-omniclaude-pr-2010-current-head after omniclaude PR
      #2010 advanced and merged. The historical current-head entry remains immutable; this item binds
      the durable merged state, merge commit, and lockfile diff scope so later OMN-16322 companions do
      not fail on the intentionally changed PR lifecycle state.
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omniclaude-pr-2010-current-head"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2010 --repo OmniNode-ai/omniclaude --json state,mergeCommit --jq '.state + " " +
          .mergeCommit.oid' | grep -Fx 'MERGED 8de3c0f728bf21822964c7f38d276cba56759460' && gh pr view
          2010 --repo OmniNode-ai/omniclaude --json files --jq '.files[].path' | grep -Fx 'uv.lock'
  - id: "occ-self-bind-pr-6946"
    description: >-
      OCC self-binding receipt for PR #6946, the in-repo evidence PR carrying the omniclaude#2010 current-head
      receipt repair. Binds OMN-16322 to the OCC PR by pr_number so the OCC receipt-gate eligibility check
      can resolve a PASS receipt for this evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 6946 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "dod-oi-pr-830-current-head"
    description: >-
      omniintelligence PR #830 is the downstream dependency-bump PR for the omnibase_core 0.46.11 release.
      It changes only uv.lock, so this evidence binds OMN-16322 to the exact current product PR head that
      updates the dependency version. The probe reads the live PR head and diff and fails if the PR no
      longer exists, moves off the recorded head, or stops adding version 0.46.11.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 830 --repo OmniNode-ai/omniintelligence --json number,state,headRefOid | grep -F
          '"headRefOid":"ca7b630e85eee41e7d23ea03d0a4755cc45540b1"' && gh pr diff 830 --repo OmniNode-ai/omniintelligence
          | grep -F '+version = "0.46.11"'
  - id: "dod-oi-pr-830-merged"
    description: >-
      Append-only replacement for dod-oi-pr-830-current-head after omniintelligence PR #830 advanced and
      merged. The historical current-head evidence remains immutable; this item binds the durable merged
      state, merge commit, and lockfile-only diff scope so later OMN-16322 companions do not fail on the
      intentionally changed PR lifecycle state.
    evidence_artifact: "supersedes_dod_evidence:dod-oi-pr-830-current-head"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 830 --repo OmniNode-ai/omniintelligence --json state,mergeCommit --jq '.state + "
          " + .mergeCommit.oid' | grep -Fx 'MERGED 9244ea9bea3be44b07b7c2462ca5d4209cb4274b' && gh pr
          view 830 --repo OmniNode-ai/omniintelligence --json files --jq '.files[].path' | grep -Fx 'uv.lock'
  - id: "dod-omnibase-infra-pr-2836-current-head"
    description: >-
      omnibase_infra PR #2836 is the downstream dependency-bump PR for the omnibase_core 0.46.11 release.
      The PR updates the dependency pin, lockfile, runtime compatibility floor, and released-core fixture,
      so this evidence binds OMN-16322 to the exact current product PR head and content diff. The probe
      fails if the PR moves off the recorded head or stops adding the 0.46.11 dependency markers.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2836 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid | grep -F '"headRefOid":"c3a88f5d2fef61188a74236c0d99b3b6b3b6d786"'
          && gh pr diff 2836 --repo OmniNode-ai/omnibase_infra | grep -F '+version = "0.46.11"' && gh
          pr diff 2836 --repo OmniNode-ai/omnibase_infra | grep -F '+    "omnibase-core==0.46.11",'
  - id: "dod-omnibase-infra-pr-2836-merged"
    description: >-
      Append-only replacement for dod-omnibase-infra-pr-2836-current-head after omnibase_infra PR #2836
      advanced and merged. The historical current-head evidence remains immutable; this item binds the
      durable merged state, merge commit, and lockfile/runtime diff scope so later OMN-16322 companions
      do not fail on the intentionally changed PR lifecycle state.
    evidence_artifact: "supersedes_dod_evidence:dod-omnibase-infra-pr-2836-current-head"
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 2836 --repo OmniNode-ai/omnibase_infra --json state,mergeCommit --jq '.state + "
          " + .mergeCommit.oid' | grep -Fx 'MERGED cde0d9a6815152ef89f03509e7e4ef2d2ff55f13' && gh pr
          view 2836 --repo OmniNode-ai/omnibase_infra --json files --jq '.files[].path' | grep -Fx 'uv.lock'
          && gh pr view 2836 --repo OmniNode-ai/omnibase_infra --json files --jq '.files[].path' | grep
          -Fx 'src/omnibase_infra/runtime/version_compatibility.py'
  - id: "occ-self-bind-pr-6967"
    description: >-
      OCC self-binding receipt for PR #6967, the in-repo evidence PR carrying the omniintelligence#830
      current-head receipt repair. Binds OMN-16322 to the OCC PR by pr_number so the OCC receipt-gate
      eligibility check can resolve a PASS receipt for this evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 6967 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "occ-self-bind-pr-6970"
    description: >-
      OCC self-binding receipt for PR #6970, the in-repo evidence PR carrying the omnibase_infra#2836
      current-head evidence repair. Binds OMN-16322 to the OCC PR by pr_number so the OCC receipt-gate
      eligibility check can resolve a PASS receipt for this evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 6970 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2836"
    description: "PR #2836 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/uv.lock?ref=82c1e510d6649612ea73e41c126ed6b19806f553 --jq '.content' | base64 -d | grep -cF 'https://files.pythonhosted.org/packages/39/d1/982a0b4535606e8aa9bb3fb5e5c6d1cd321004585a84fb10eacd718dad60/omnibase_core-0.46.11.tar.gz'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2836-ci"
    description: "PR #2836 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2836 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "occ-self-bind-pr-6971"
    description: "OCC companion PR #6971 — self-bind for OMN-16322 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6971 --repo OmniNode-ai/onex_change_control --json number,state"
