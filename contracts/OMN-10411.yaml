---
schema_version: '1.0.0'
ticket_id: OMN-10411
title: "[omniclaude] Wire `verify / verify` caller workflow"
summary: >-
  Add .github/workflows/call-receipt-gate.yml to omniclaude. Caller invokes OmniNode-ai/omnibase_core/.github/workflows/receipt-gate.yml@main
  on pull_request and merge_group triggers. Surfaces as required status check `verify / verify`.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: call-receipt-gate.yml present in omniclaude PR diff
    command: "gh pr diff --repo OmniNode-ai/omniclaude | grep -q 'call-receipt-gate.yml'"
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: >-
      .github/workflows/call-receipt-gate.yml added to omniclaude with correct cross-repo uses reference
      to omnibase_core receipt-gate.yml@main
    source: generated
    checks:
      - check_type: command
        check_value: >-
          gh pr diff --repo OmniNode-ai/omniclaude | grep -q 'OmniNode-ai/omnibase_core/.github/workflows/receipt-gate.yml@main'
