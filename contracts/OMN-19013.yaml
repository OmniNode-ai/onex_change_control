---
schema_version: "1.0.0"
ticket_id: "OMN-19013"
title: "Preserve terminal construction failure truth across Core, Market, and Infra"
summary: >-
  Introduce the typed TERMINAL_CONSTRUCTION_FAILED / UNDETERMINED terminal pair with its exact stable
  reason, preserve the actual quality boolean, reject constructed quality evidence, persist the pair through
  Market and Infra, and exclude that exact pair from quality metric denominators. This companion records
  the currently retained Core source proof only; Market publication, the released Core floor and lock,
  composed consumer proof, and final release/cleanup remain required and are not asserted as complete
  here.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "envelopes"
  - "public_api"
contract_completeness: ENRICHED
proof_class: live-readback
evidence_requirements:
  - kind: tests
    description: >-
      Core PR #1737 head b1e8bc8a0f496c7ff88c86ab488d07ed0e5acef9 passes the exact 47-case wire-contract
      suite for the construction outcome/content pair, invalid pairs, completed rejection, actual-quality
      preservation, and JSON/schema round-trip.
    command: >-
      uv run --frozen pytest -q tests/unit/models/delegation/wire/test_omn18928_terminal_outcome_content.py
  - kind: manual
    description: >-
      Infra PR #3945 head 9ce129cef0d9dd27532139de03c620c67d8b59c4 is an open source delivery for the
      typed terminal mirror, persisted stable reason, and byte-identical 0045 migration vendor binding.
      Its product gate remains dependent on the final released Core package and Market consumer composition.
  - kind: manual
    description: >-
      Market local commit d9958a096794179a74bce7aaa3916e55aa013028 is deliberately not represented as
      remote source evidence: its normal push stopped at strict mypy because the locked omnibase-core
      0.47.20 lacks this ticket's new enum modules and fields. After Core is released, Market must raise
      its canonical floor and lock, pass normal gates, push/open its source PR, and prove the released
      Core/Market/Infra composition before this ticket can be complete.
  - kind: manual
    description: >-
      The final release proof must verify the released Core artifact containing b1e8 semantics, the updated
      Market floor and lock, the merged Infra and Market source, combined 0043z/0044/0045 migration parity,
      and clean ticket worktrees. No runtime deployment or restart is part of this ticket.
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "core-terminal-construction-wire-source"
    description: >-
      The immutable Core source head for PR #1737 defines TERMINAL_CONSTRUCTION_FAILED and UNDETERMINED,
      accepts only their exact pair with terminal_construction_failed as the stable reason, preserves
      the caller's actual quality boolean, rejects invented quality/provider evidence, and rejects the
      outcome on a completed carrier. The proof is source-only and makes no Core package, Market, Infra,
      migration, release, or runtime assertion.
    source: manual
    status: verified
    checks:
      - check_type: test_passes
        check_value: >-
          uv run --frozen pytest -q tests/unit/models/delegation/wire/test_omn18928_terminal_outcome_content.py
        cwd: "${OMNI_HOME}/omnibase_core"
