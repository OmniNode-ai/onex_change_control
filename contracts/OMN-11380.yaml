---
schema_version: "1.0.0"
ticket_id: "OMN-11380"
title: "Agent-driven demo mode with --agent flag"
summary: >
  Adds src/agent/agent_demo.py and --agent CLI flag to onex-self-extending-agent. Orchestrates the full
  self-extending loop: build request envelope → generate with retry → validate_generation_structured →
  ToolRegistry.register → ToolRegistry.invoke → EventChainCapture.save. PR: OmniNode-ai/onex-self-extending-agent#67.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Unit tests for agent_demo full loop, retry on validation failure, event chain capture,
      and registration-skipped path all pass."
    command: "See drift/dod_receipts/OMN-11380/dod-unit-tests/command.yaml"
  - kind: "ci"
    description: "mypy --strict passes with 0 new errors."
    command: "See drift/dod_receipts/OMN-11380/dod-typecheck/command.yaml"
  - kind: "ci"
    description: "ruff check and ruff format pass."
    command: "See drift/dod_receipts/OMN-11380/dod-lint/command.yaml"
  - kind: "ci"
    description: "OCC PR carries the OMN-11380 contract and receipt bundle."
    command: "See drift/dod_receipts/OMN-11380/dod-occ-pr/command.yaml"
  - kind: "ci"
    description: "OCC PR #1498 carries the central OMN-11380 receipt extension for omnimarket PR #796."
    command: "See drift/dod_receipts/OMN-11380/dod-occ-pr-1498/command.yaml"
  - kind: "ci"
    description: "OCC PR #1500 carries the central OMN-11380 deploy assessment extension for omnimarket
      PR #796."
    command: "See drift/dod_receipts/OMN-11380/dod-occ-pr-1500/command.yaml"
  - kind: "ci"
    description: "omnimarket PR #796 carries the P1 handler_routing contract updates for eight runtime-manifest
      nodes."
    command: "See drift/dod_receipts/OMN-11380/dod-omnimarket-pr-796/command.yaml"
  - kind: "tests"
    description: "The eight P1 handler_routing entries parse, declare operation_match routing, and import
      their handlers."
    command: "See drift/dod_receipts/OMN-11380/dod-handler-routing-pr-796/command.yaml"
  - kind: "ci"
    description: "Deploy assessment for omnimarket PR #796 confirms no live runtime mutation is performed
      by this PR."
    command: "See drift/dod_receipts/OMN-11380/dod-deploy-assessment-pr-796/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "Unit tests for agent_demo full loop, retry, event chain capture, and no-registration
      path passed (429 passed, 1 skipped)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '429 passed' drift/dod_receipts/OMN-11380/dod-unit-tests/command.yaml"
  - id: "dod-typecheck"
    description: "mypy --strict reported 0 new errors for the agent_demo changes."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-11380/dod-typecheck/command.yaml"
  - id: "dod-lint"
    description: "ruff check and ruff format passed for the new agent_demo module and tests."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-11380/dod-lint/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR carries the central OMN-11380 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11380/dod-occ-pr/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11380 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11380/dod-occ-pr-self/command.yaml"
  - id: "dod-occ-pr-1498"
    description: "OCC PR #1498 carries the central OMN-11380 receipt extension for omnimarket PR #796."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11380/dod-occ-pr-1498/command.yaml"
  - id: "dod-occ-pr-1500"
    description: "OCC PR #1500 carries the central OMN-11380 deploy assessment extension for omnimarket
      PR #796."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11380/dod-occ-pr-1500/command.yaml"
  - id: "dod-omnimarket-pr-796"
    description: "omnimarket PR #796 is open against main and carries the P1 handler_routing contract
      updates."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-11380/dod-omnimarket-pr-796/command.yaml"
  - id: "dod-handler-routing-pr-796"
    description: "The eight P1 handler_routing contract entries validate locally and import their declared
      handlers."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q 'handler_routing import validation passed for 8 P1 nodes' drift/dod_receipts/OMN-11380/dod-handler-routing-pr-796/command.yaml"
  - id: "dod-deploy-assessment-pr-796"
    description: "Deploy assessment for omnimarket PR #796; no live runtime mutation is performed by this
      PR."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11380/dod-deploy-assessment-pr-796/command.yaml
          && grep -qi 'no live runtime mutation' drift/dod_receipts/OMN-11380/dod-deploy-assessment-pr-796/command.yaml"
