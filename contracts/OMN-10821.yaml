---
schema_version: "1.0.0"
ticket_id: OMN-10821
title: "Add unimported-handler CI check"
summary: "Adds an omnimarket CI gate for Handler* classes in handler_*.py files. The gate accepts Python
  imports, contract-declared handler routing, and a small documented legacy baseline so new unwired handlers
  cannot merge silently."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Focused unimported-handler check and tests pass in the omnimarket PR #697 worktree."
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-10821/omnimarket-697
      && uv run python scripts/ci/check_unimported_handlers.py && uv run pytest tests/ci/test_check_unimported_handlers.py
      -q && uv run ruff check scripts/ci/check_unimported_handlers.py tests/ci/test_check_unimported_handlers.py
      && uv run ruff format --check scripts/ci/check_unimported_handlers.py tests/ci/test_check_unimported_handlers.py"
  - kind: ci
    description: "omnimarket PR #697 carries the OMN-10821 CI gate repair."
    command: "gh pr view 697 --repo OmniNode-ai/omnimarket --json number,state,headRefName,headRefOid,files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-local-validation
    description: "Focused handler gate, tests, ruff, and format checks pass locally."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10821/dod-local-validation/command.yaml
          && grep -q '^ticket_id: OMN-10821$' drift/dod_receipts/OMN-10821/dod-local-validation/command.yaml"
  - id: dod-omnimarket-pr-697
    description: "omnimarket PR #697 is bound to OMN-10821 at the repaired head."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10821/dod-omnimarket-pr-697/command.yaml
          && grep -q '^ticket_id: OMN-10821$' drift/dod_receipts/OMN-10821/dod-omnimarket-pr-697/command.yaml
          && grep -q '^pr_number: 697$' drift/dod_receipts/OMN-10821/dod-omnimarket-pr-697/command.yaml"
  - id: dod-occ-pr-1224
    description: "onex_change_control PR #1224 self-binds OMN-10821 central evidence for omnimarket#697."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10821/dod-occ-pr-1224/command.yaml
          && grep -q '^ticket_id: OMN-10821$' drift/dod_receipts/OMN-10821/dod-occ-pr-1224/command.yaml
          && grep -q '^pr_number: 1224$' drift/dod_receipts/OMN-10821/dod-occ-pr-1224/command.yaml"
