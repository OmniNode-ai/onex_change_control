---
schema_version: 1.0.0
ticket_id: OMN-14531
title: OCC companion for omnibase_infra#2291 - omnimarket drift guard for onex skill dispatch
summary: >-
  OCC evidence companion for OMN-14531. omnibase_infra#2291 closes the detector-shelf blind spot where
  `onex skill aislop_sweep` failed with a bare Unknown node error when omnimarket was absent from the
  omnibase_infra venv even though a canonical OMNI_HOME/omnimarket clone existed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: >-
      omnibase_infra#2291 must wire the omnimarket drift guard into the `onex skill` dispatch path and
      preserve focused CLI/drift guard tests.
    command: >-
      gh pr view 2291 --repo OmniNode-ai/omnibase_infra --json headRefOid,files,body and inspect changed
      product files at head d8b5e4c11ce118cdc053a3d6724fd624ded53bfe.
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-2291-head-d8b5e4c-skill-drift-guard-surfaces
    description: >-
      Product-observing verification that omnibase_infra#2291 head d8b5e4c11ce118cdc053a3d6724fd624ded53bfe
      modifies the omnimarket drift guard and `onex skill` CLI dispatch surfaces, with the unrelated vendored
      migration file explicitly present as the local pre-commit unblocker.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          f="$(mktemp)" && gh pr view 2291 --repo OmniNode-ai/omnibase_infra --json headRefOid,files --jq
          '.headRefOid + "\n" + (.files | map(.path) | join("\n"))' > "$f" && grep -Fq 'd8b5e4c11ce118cdc053a3d6724fd624ded53bfe'
          "$f" && grep -Fq 'src/omnibase_infra/cli/cli_skill.py' "$f" && grep -Fq 'src/omnibase_infra/cli/omnimarket_drift_guard.py'
          "$f" && grep -Fq 'tests/unit/cli/test_cli_skill.py' "$f" && grep -Fq 'tests/unit/cli/test_omnimarket_drift_guard.py'
          "$f" && grep -Fq 'docker/migrations/forward/nodes/node_projection_context_roi/002_add_factor_subset_hash_and_routing_source.sql'
          "$f"
  - id: dod-2291-head-d8b5e4c-red-green-proof
    description: >-
      Product PR body verification for OMN-14531: the PR documents the real failing `onex skill aislop_sweep`
      symptom, the repaired success path, and focused RED-to-GREEN automated regression coverage plus
      local gates.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          f="$(mktemp)" && gh pr view 2291 --repo OmniNode-ai/omnibase_infra --json headRefOid,body --jq
          '.headRefOid + "\n" + .body' > "$f" && grep -Fq 'd8b5e4c11ce118cdc053a3d6724fd624ded53bfe' "$f"
          && grep -Fq 'Unknown node' "$f" && grep -Fq 'repos_scanned=1' "$f" && grep -Fq 'test_drift_check_raises_when_not_installed_but_canonical_clone_present'
          "$f" && grep -Fq 'test_cli_skill.py` full suite' "$f" && grep -Fq 'full `pre-commit run` clean'
          "$f"
  - id: dod-2291-head-d8b5e4c-no-live-deploy-required
    description: >-
      Deploy assessment for omnibase_infra#2291. The product change is limited to CLI drift-guard Python/tests
      plus one vendored migration sync file used to satisfy local pre-commit; it does not change deploy
      manifests, runtime images, compose files, or live .201 runtime configuration. No live deploy or
      restart was performed for this PR.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          f="$(mktemp)" && gh pr view 2291 --repo OmniNode-ai/omnibase_infra --json headRefOid,files --jq
          '.headRefOid + "\n" + (.files | map(.path) | join("\n"))' > "$f" && grep -Fq 'd8b5e4c11ce118cdc053a3d6724fd624ded53bfe'
          "$f" && grep -Fq 'src/omnibase_infra/cli/cli_skill.py' "$f" && grep -Fq 'src/omnibase_infra/cli/omnimarket_drift_guard.py'
          "$f" && ! grep -Eq '(^|/)deploy/|docker/compose|compose[.]ya?ml|Dockerfile|runtime|k3s|helm'
          "$f"
  - id: dod-occ-self-4103
    description: >-
      Self-binding verification that onex_change_control#4103 carries the OMN-14531 contract and product-observing
      receipts, and that its initial evidence commit remains in the PR commit history.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          f="$(mktemp)" && gh pr view 4103 --repo OmniNode-ai/onex_change_control --json number,title,files,commits
          --jq '(.number|tostring) + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n")) + "\n"
          + ((.commits | map(.oid)) | join("\n"))' > "$f" && grep -Fxq '4103' "$f" && grep -Fq 'evidence(OMN-14531):
          cover aislop drift guard' "$f" && grep -Fq 'contracts/OMN-14531.yaml' "$f" && grep -Fq 'drift/dod_receipts/OMN-14531/dod-2291-head-d8b5e4c-skill-drift-guard-surfaces/command.yaml'
          "$f" && grep -Fq 'drift/dod_receipts/OMN-14531/dod-2291-head-d8b5e4c-red-green-proof/command.yaml'
          "$f" && grep -Fq 'drift/dod_receipts/OMN-14531/dod-2291-head-d8b5e4c-no-live-deploy-required/command.yaml'
          "$f" && grep -Fq '6b67d45abc7736e74aa612a393804415f2d03e88' "$f"
  - id: dod-1746-head-117eaf3-dataflow-failclosed-surfaces
    description: >-
      Product-observing verification that omnimarket#1746 head 117eaf3fce1ee0efe7f4837a77791dc819d8fdd0
      changes the data_flow_sweep lag probe, metadata collector, handler, lane-target host resolution,
      and focused node regression tests.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          f="$(mktemp)" && gh pr view 1746 --repo OmniNode-ai/omnimarket --json headRefOid,files,commits
          --jq '.headRefOid + "\n" + ((.files | map(.path)) | join("\n")) + "\n" + ((.commits | map(.oid))
          | join("\n"))' > "$f" && grep -Fq '117eaf3fce1ee0efe7f4837a77791dc819d8fdd0' "$f" && grep -Fq
          'src/omnimarket/nodes/node_data_flow_sweep/collector.py' "$f" && grep -Fq 'src/omnimarket/nodes/node_data_flow_sweep/handlers/handler_data_flow_sweep.py'
          "$f" && grep -Fq 'src/omnimarket/nodes/node_data_flow_sweep/lane_target.py' "$f" && grep -Fq
          'tests/test_golden_chain_data_flow_sweep_collect.py' "$f" && grep -Fq 'tests/test_data_flow_sweep_dispatch_resolution.py'
          "$f"
  - id: dod-1746-head-117eaf3-red-green-proof
    description: >-
      Product PR body verification for OMN-14531 data_flow_sweep: the PR documents the rpk failure fail-open
      bug, the LAG_UNKNOWN fail-closed behavior, the Tailscale host replacement for raw 192.168.86.201,
      and focused RED-to-GREEN tests plus local verification.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          f="$(mktemp)" && gh pr view 1746 --repo OmniNode-ai/omnimarket --json headRefOid,body --jq '.headRefOid
          + "\n" + .body' > "$f" && grep -Fq '117eaf3fce1ee0efe7f4837a77791dc819d8fdd0' "$f" && grep -Fq
          'Fail-open on rpk failure' "$f" && grep -Fq 'EnumFlowStatus.LAG_UNKNOWN' "$f" && grep -Fq 'omninode-pc.tail75df5e.ts.net'
          "$f" && grep -Fq 'test_probe_consumer_lag_fails_closed_on_rpk_failure' "$f" && grep -Fq '52/52
          passed' "$f" && grep -Fq '13259 passed' "$f" && grep -Fq 'mypy --strict src/omnimarket/nodes/node_data_flow_sweep/'
          "$f"
  - id: dod-1746-head-117eaf3-no-live-deploy-required
    description: >-
      Deploy assessment for omnimarket#1746. The product diff is limited to data_flow_sweep Python module
      and test files; it does not change deploy manifests, runtime images, compose files, migrations,
      k3s/helm surfaces, or live .201 runtime configuration. No live deploy or restart was performed for
      this PR.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          f="$(mktemp)" && gh pr view 1746 --repo OmniNode-ai/omnimarket --json headRefOid,files --jq
          '.headRefOid + "\n" + (.files | map(.path) | join("\n"))' > "$f" && grep -Fq '117eaf3fce1ee0efe7f4837a77791dc819d8fdd0'
          "$f" && grep -Fq 'src/omnimarket/nodes/node_data_flow_sweep/lane_target.py' "$f" && ! grep -Eq
          '(^|/)deploy/|docker/|compose[.]ya?ml|Dockerfile|k3s|helm|migrations?/' "$f"
  - id: dod-occ-self-4108
    description: >-
      Self-binding verification that onex_change_control#4108 carries the OMN-14531 data_flow_sweep supplement
      receipts for omnimarket#1746, and that its initial evidence commit remains in the PR commit history.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          f="$(mktemp)" && gh pr view 4108 --repo OmniNode-ai/onex_change_control --json number,title,files,commits
          --jq '(.number|tostring) + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n")) + "\n"
          + ((.commits | map(.oid)) | join("\n"))' > "$f" && grep -Fxq '4108' "$f" && grep -Fq 'evidence(OMN-14531):
          cover data_flow_sweep fail-closed fix' "$f" && grep -Fq 'contracts/OMN-14531.yaml' "$f" && grep
          -Fq 'drift/dod_receipts/OMN-14531/dod-1746-head-117eaf3-dataflow-failclosed-surfaces/command.yaml'
          "$f" && grep -Fq 'drift/dod_receipts/OMN-14531/dod-1746-head-117eaf3-red-green-proof/command.yaml'
          "$f" && grep -Fq 'drift/dod_receipts/OMN-14531/dod-1746-head-117eaf3-no-live-deploy-required/command.yaml'
          "$f" && grep -Fq '8856e663ff68b3dbf2d63f2683a5bc9de11ccbed' "$f"
