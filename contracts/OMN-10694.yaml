---
schema_version: "1.0.0"
ticket_id: "OMN-10694"
title: "Build semantic segmentation LLM effect node"
summary: >
  Adds node_adr_segmentation_llm_effect to omnimarket for ADR canary semantic segmentation. The node uses
  an env-configured inference bridge model to split markdown documents into typed semantic units with
  deterministic segment IDs, content hashes, parse-failure evidence, and low-confidence UNKNOWN handling.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "28 unit tests pass for node_adr_segmentation_llm_effect."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10694/dod-unit-tests/command.yaml\""
  - id: "dod-leak-gate"
    description: "Blocking leaked-literals gate passes after removing endpoint/model literals from contract
      defaults."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10694/dod-leak-gate/command.yaml\""
  - id: "dod-deploy"
    description: "Deploy readiness evidence confirms the node entry point exists and uses env-backed inference
      bridge configuration."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10694/dod-deploy/command.yaml\""
  - id: "dod-pr-563-head"
    description: "omnimarket PR #563 carries the OMN-10694 implementation at the refreshed head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10694/dod-pr-563-head/command.yaml\""
