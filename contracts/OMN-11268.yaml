---
schema_version: 1.0.0
ticket_id: OMN-11268
title: Runner health models and state enum
summary: omnibase_infra PR  #1671 verifies ModelRunnerStatus, ModelRunnerHealthSnapshot, ModelRunnerHealthAlert, and EnumRunnerHealthState meet acceptance criteria with 7 unit tests and 4 integration tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Product PR  #1671 verifies all runner health models are frozen, correctly typed, and that to_slack_message covers all degraded states.
    command: uv run pytest tests/unit/observability/runner_health/test_runner_health_models.py tests/integration/observability/test_runner_health_models_integration.py
      -q
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-runner-health-models
    description: Product verification evidence is captured as a canonical PASS receipt.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11268/dod-runner-health-models/command.yaml"
          && grep -q '^pr_number: 1671$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11268/dod-runner-health-models/command.yaml"
          && grep -q '^verifier: agent$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11268/dod-runner-health-models/command.yaml"
