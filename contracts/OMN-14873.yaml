---
schema_version: "1.0.0"
ticket_id: "OMN-14873"
title: "Health-gated stability lane refresh mechanism"
summary: >
  omnibase_infra PR #2370 implements a health-gated, rollback-on-failure refresh mechanism for the stability-test
  lane, including the refresh script, health verifier, consumer-group contract, runbook, and regression
  tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_infra PR #2370 is open at the recorded head with the expected stability refresh
      files and verification claims."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14873/dod-omnibase_infra-pr-2370/command.yaml"
  - kind: "manual"
    description: "Deploy-scope evidence is present in the PR body for the live stability-test lane canary
      and rollback proof."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14873/dod-omnibase_infra-pr-2370-deploy/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_infra-pr-2370"
    description: >
      Binds omnibase_infra PR #2370 at head b30de03c101a1f84e1d6dd73df0d4098df0a1e67 with the expected
      stability-lane refresh implementation files and local verification claims.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14873/dod-omnibase_infra-pr-2370/command.yaml
          && grep -q 'b30de03c101a1f84e1d6dd73df0d4098df0a1e67' drift/dod_receipts/OMN-14873/dod-omnibase_infra-pr-2370/command.yaml
          && grep -q 'scripts/runtime_build/refresh_stability_lane.sh' drift/dod_receipts/OMN-14873/dod-omnibase_infra-pr-2370/command.yaml
          && grep -q '21833 passed, 36 skipped' drift/dod_receipts/OMN-14873/dod-omnibase_infra-pr-2370/command.yaml
  - id: "dod-omnibase_infra-pr-2370-deploy"
    description: >
      Binds deploy-scope evidence from omnibase_infra PR #2370: the PR body records a manual canary against
      the live .201 stability-test lane with health-gate PASS, lane healthy confirmation, and rollback-path
      proof via unit tests.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14873/dod-omnibase_infra-pr-2370-deploy/command.yaml
          && grep -q 'Manual canary run against the live' drift/dod_receipts/OMN-14873/dod-omnibase_infra-pr-2370-deploy/command.yaml
          && grep -q 'health-gate PASS' drift/dod_receipts/OMN-14873/dod-omnibase_infra-pr-2370-deploy/command.yaml
          && grep -q 'Rollback path' drift/dod_receipts/OMN-14873/dod-omnibase_infra-pr-2370-deploy/command.yaml
  - id: "occ-self-bind-pr-4548"
    description: >
      Self-binding OCC eligibility receipt for onex_change_control PR #4548, which carries OMN-14873 central
      evidence for omnibase_infra PR #2370.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 4548 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
