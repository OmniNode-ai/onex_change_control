---
schema_version: "1.0.0"
ticket_id: "OMN-10644"
title: "Strip hardcoded LLM endpoints from ci_fix_effect contract"
summary: >
  Removes lab-specific LLM endpoint literals from node_ci_fix_effect and keeps patch allowlist loading
  contract-driven without requiring endpoint env vars in provider-mocked unit paths.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-handler-tests"
    description: "CI fix effect handler tests pass with LLM_CODER_FAST_URL unset."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10644/dod-handler-tests/command.yaml\""
  - id: "dod-precommit"
    description: "Touched handler passes pre-commit hooks."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10644/dod-precommit/command.yaml\""
  - id: "dod-pr-556-head"
    description: "Bind OMN-10644 evidence to omnimarket PR #556 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10644/dod-pr-556-head/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10644 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10644/dod-occ-pr/command.yaml\""
