---
schema_version: "1.0.0"
ticket_id: "OMN-11204"
title: "Add machine-readable doctrine clauses registry"
summary: >
  Creates docs/standards/doctrine_clauses.yaml with all 15 DT-001..DT-015 clauses extracted from OMNINODE_DETERMINISTIC_TRUTH_DOCTRINE.md.
  Adds src/onex_change_control/doctrine/loader.py with Pydantic validation (no duplicate clause_ids, DT-
  prefix required, coverage one of enforced/advisory/uncovered). Coverage field is evidence-based: enforced
  only where a merge-blocking CI gate exists today, otherwise uncovered.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "12 unit tests in tests/unit/doctrine/test_doctrine_loader.py pass."
    command: "See drift/dod_receipts/OMN-11204/dod-tests/command.yaml"
  - kind: "ci"
    description: "ruff format + ruff check + mypy pass for new files."
    command: "See drift/dod_receipts/OMN-11204/dod-lint/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "12 doctrine loader unit tests passed."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '12 passed' drift/dod_receipts/OMN-11204/dod-tests/command.yaml"
  - id: "dod-lint"
    description: "ruff format, ruff check, and mypy pass for new files."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11204/dod-lint/command.yaml"
  - id: "dod-occ-pr-1115"
    description: "onex_change_control PR #1115 carries the OMN-11204 doctrine clauses registry."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11204/dod-occ-pr-1115/command.yaml
          && grep -q '^pr_number: 1115$' drift/dod_receipts/OMN-11204/dod-occ-pr-1115/command.yaml"
