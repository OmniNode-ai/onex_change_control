---
schema_version: "1.0.0"
ticket_id: "OMN-16287"
title: "OCC evidence for OMN-16287"
summary: >
  Hand-authored OCC companion (autobind declined — no RED-derivable candidate, OMN-15247) for OmniNode-ai/omniintelligence
  PR #828: removes the stale omnibase-core [tool.uv.sources] git-rev override and regenerates uv.lock
  to resolve omnibase-core from PyPI.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #828 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniintelligence-pr-828"
    description: "PR #828 on OmniNode-ai/omniintelligence — Evidence-Source hand mint."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 828 --repo OmniNode-ai/omniintelligence --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omniintelligence-pr-828-ci"
    description: "PR #828 on OmniNode-ai/omniintelligence — product diff scope check (OMN-14425): exactly
      pyproject.toml + uv.lock."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 828 --repo OmniNode-ai/omniintelligence --json files --jq '[.files[].path]
          | sort | join(\",\")' | grep -x 'pyproject.toml,uv.lock'"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-6770"
    description: "OCC companion PR #6770 — self-bind for OMN-16287 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6770 --repo OmniNode-ai/onex_change_control --json number,state"
