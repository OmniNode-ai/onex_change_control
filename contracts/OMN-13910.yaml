---
schema_version: "1.0.0"
ticket_id: "OMN-13910"
title: "fix(omnibase_core): dev wedged by OMN-13411 release-identity gate — bump 0.46.4 to 0.46.5"
summary: >-
  omnibase_core dev is wedged: project.version (0.46.4) already equals the latest published PyPI version
  while 104+ commits have touched src/ since the last release tag, so the OMN-13411 release-identity gate
  fails on every PR to dev regardless of content (confirmed live on OMN-13875's #1381/#1383, a workflow-YAML-only
  PR). omnibase_core PR #1384 bumps project.version to 0.46.5 and regenerates uv.lock (embeds the self-version),
  the gate's own prescribed remedy. No PyPI publish action; 0.46.5 publishes on the next release train.
  Covered by the standing releases-autonomous grant.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1384"
    description: >-
      Consuming-PR binding: omnibase_core PR #1384 bumps project.version 0.46.4 -> 0.46.5 and regenerates
      uv.lock, at head 8de5398d61a6fa2cf6c64ed1f1f3a900907eeb38.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13910/dod-omnibase_core-pr-1384/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: onex_change_control PR carrying the OMN-13910 contract and receipt consumed
      by omnibase_core PR #1384.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13910/dod-occ-self/command.yaml"
