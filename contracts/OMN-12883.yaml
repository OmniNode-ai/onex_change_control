---
schema_version: "1.0.0"
ticket_id: "OMN-12883"
title: "[build] Guardrail: enforce provider-class endpoint URL shape"
summary: >-
  Tracks endpoint URL shape enforcement that respects provider class: cloud/frontier routes carry complete
  endpoint_url values (verbatim, full chat path), while overlay-supplied backends declare base_url_env
  and set endpoint_url to null. CLI-agent-tier backends are exempt. The gate is extended into check_routing_authority.py
  (routing-authority-demo-gate) in M7.2.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-provider-endpoint-shape-guard"
    description: >-
      Cloud incomplete URL fixture fails; local bare-base route fixture avoids a false positive; provider-class
      fixtures pass or fail as expected. All 14 committed bifrost backends pass. Synthetic violation output
      recorded in docs/evidence/2026-06-12-weekend-pass/config-authority/ratchet/.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12883/dod-provider-endpoint-shape-guard/command.yaml"
