---
schema_version: "1.0.0"
ticket_id: "OMN-12883"
title: "[build] Guardrail: enforce provider-class endpoint URL shape"
summary: >-
  Tracks endpoint URL shape enforcement that respects provider class: cloud/frontier routes carry complete
  endpoint_url values, while local routes may use bare-base overlay plus append behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-provider-endpoint-shape-guard"
    description: >-
      Cloud incomplete URL fixture fails, local bare-base route fixture avoids a false positive, and provider-class
      fixtures pass or fail as expected.
    source: "manual"
    status: "pending"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12883/dod-provider-endpoint-shape-guard/command.yaml"
