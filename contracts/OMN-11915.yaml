---
schema_version: "1.0.0"
ticket_id: "OMN-11915"
title: "Codebase Intelligence Bridge Node (Task 2.1)"
summary: >-
  Bind central OCC evidence for omnimarket PR #823, which adds node_codebase_intelligence_bridge_effect:
  a new ONEX effect node proxying codebase intelligence queries to a configured backend provider
  (default: Repowise via CLI subprocess). Five operations: get_answer, get_context, get_symbol,
  search_codebase, get_why. Handler delegates to ProtocolCodebaseIntelligence adapter — never imports
  Repowise internals directly.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omnimarket PR #823 golden-chain tests pass (16 unit tests)."
    command: "gh pr checks 823 --repo OmniNode-ai/omnimarket"
  - kind: "manual"
    description: "Central OCC evidence PR carries this contract and PASS receipts."
    command: "gh pr view <occ-pr> --repo OmniNode-ai/onex_change_control"
dod_evidence:
  - id: "dod-unit-tests"
    description: "16 unit tests pass covering all five operations, error path, timeout path, and missing _meta."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11915/dod-unit-tests/command.yaml\"
          && grep -q '16 passed' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11915/dod-unit-tests/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "New effect node proxying CLI subprocess — no live deploy, docker exec, or rpk topic produce required pre-merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11915/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11915/dod-deploy-assessment/command.yaml\"
          && grep -q 'deploy-gated' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11915/dod-deploy-assessment/command.yaml\""
  - id: "dod-omnimarket-pr-823"
    description: "Bind OMN-11915 evidence to omnimarket PR #823."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11915/dod-omnimarket-pr-823/command.yaml\"
          && grep -q '^pr_number: 823$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11915/dod-omnimarket-pr-823/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Bind OMN-11915 central evidence to the OCC PR that carries this bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11915/dod-occ-pr-self/command.yaml\"
          && grep -q '^ticket_id: \"OMN-11915\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11915/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
