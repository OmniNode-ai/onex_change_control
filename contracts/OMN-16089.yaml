---
schema_version: "1.0.0"
ticket_id: "OMN-16089"
title: "Bind the omnimarket omnibase-infra 0.38.7 (post-#2672) re-bump for the onex-dev runtime deploy"
summary: >-
  OCC companion for OmniNode-ai/omnimarket#2105. Re-bumps the omnibase-infra git rev in omnimarket [tool.uv.sources]
  + uv.lock from 254c3672471261c596c565245fb51ac9d951c494 (v0.38.7, the OMN-16086 deploy lineage, pre-#2672)
  to 6fdbba6d8b6bcd7cc98a3da7dc8eb510f67a0e6a — the OmniNode-ai/omnibase_infra#2672 merge commit on dev
  (fix(OMN-14498): closes the non-outbox result-applier publish seam so a node no longer commits its Kafka
  input offset when a downstream publish fails). Resolved version stays 0.38.7. This is the lineage the
  post-#2672 onex-dev runtime image rebuild and deploy consume; the currently-deployed onex-dev image
  is pinned one commit behind this fix, so beta-user events can silently vanish on a downstream publish
  failure until this lands. This ticket was originally claimed 2026-08-16 by an earlier chain (deploy-2672/-b/-d)
  which produced the omnimarket commit locally but never pushed it or opened a PR; this OCC companion
  finishes that unfinished work.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2105 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-2105"
    description: >-
      PR #2105 on OmniNode-ai/omnimarket carries the 0.38.7 (post-#2672) re-bump.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2105 --repo OmniNode-ai/omnimarket --json number,state"
  - id: "dod-market2105-rebump-exact-head-and-scope"
    description: >-
      omnimarket PR #2105 is bound at exact reviewed head 8163a50c73a0a50c6814a837da87e1e8cd2635a1 with
      its complete two-file scope: the dependency-pin files pyproject.toml and uv.lock, nothing else —
      the pin move surfaced no gate or test adaptations (full-suite pre-push escalation clean at the new
      rev).
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 2105 --repo OmniNode-ai/omnimarket --json headRefOid,files --jq '(.headRefOid == "8163a50c73a0a50c6814a837da87e1e8cd2635a1") and ([.files[].path] | sort == ["pyproject.toml", "uv.lock"])' | grep -x true
  - id: "dod-pin-is-2672-merge-commit"
    description: >-
      The pyproject [tool.uv.sources] rev at the PR head is exactly 6fdbba6d8b6bcd7cc98a3da7dc8eb510f67a0e6a,
      and that SHA is the merge commit of OmniNode-ai/omnibase_infra#2672 (MERGED) — so the pinned lineage
      contains the OMN-14498 result-applier publish-seam fix by construction.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/pyproject.toml?ref=8163a50c73a0a50c6814a837da87e1e8cd2635a1 --jq '.content' | base64 -d | grep -c 'rev = "6fdbba6d8b6bcd7cc98a3da7dc8eb510f67a0e6a"' | grep -qx 1 && gh pr view 2672 --repo OmniNode-ai/omnibase_infra --json state,mergeCommit --jq '(.state == "MERGED") and (.mergeCommit.oid == "6fdbba6d8b6bcd7cc98a3da7dc8eb510f67a0e6a")' | grep -x true
  - id: "occ-self-bind-pr-6684"
    description: "OCC companion PR #6684 — self-bind for OMN-16089."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6684 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omninode-infra-pr-944"
    description: >-
      PR #944 on OmniNode-ai/omninode_infra carries the batched onex-dev repin to post-#2783 source rev
      32c48f22c (runtime digest sha256:f1698264, migrate digest sha256:ebff08b8, one source rev), bound
      at exact head 13c8cb774a8616d4c2b2a97cf2adbc9674a12445 with its complete three-file scope: the runtime
      kustomization, the migrate Job manifest, and the readiness composition contract.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 944 --repo OmniNode-ai/omninode_infra --json headRefOid,files --jq '(.headRefOid == "13c8cb774a8616d4c2b2a97cf2adbc9674a12445") and ([.files[].path] | sort == ["k8s/migrations/omnibase-infra-migrate.yaml", "k8s/onex-dev/readiness/staging-composition.canonical-dev.v1.yaml", "k8s/onex-dev/runtime/kustomization.yaml"])' | grep -x true
  - id: "occ-self-bind-pr-6702"
    description: "OCC companion PR #6702 — self-bind for the omninode_infra#944 binding round."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6702 --repo OmniNode-ai/onex_change_control --json number,state"
