---
schema_version: "1.0.0"
ticket_id: "OMN-11564"
title: "Delete unwired omnibase_infra subsystems"
summary: >-
  Bind central OCC evidence for omnibase_infra PR #1708, which deletes confirmed zero-importer subsystems
  and adds integration coverage proving the removed code paths remain absent.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "omnibase_infra PR #1708 CI and receipt gates pass for dead subsystem removal."
    command: "gh pr checks 1708 --repo OmniNode-ai/omnibase_infra"
  - kind: "manual"
    description: "Central OCC evidence PR carries this contract and PASS receipts."
    command: "gh pr view <occ-pr> --repo OmniNode-ai/onex_change_control"
dod_evidence:
  - id: "dod-omnibase-infra-pr-1708"
    description: "Bind OMN-11564 evidence to omnibase_infra PR #1708 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11564/dod-omnibase-infra-pr-1708/command.yaml\"
          && grep -q '^pr_number: 1708$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11564/dod-omnibase-infra-pr-1708/command.yaml\"
          && grep -q '^commit_sha: \"8c2aaae278a7d2b88e920cf260071b5c89f1658c\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11564/dod-omnibase-infra-pr-1708/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Dead subsystem removal changes runtime paths but requires no live deploy before merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11564/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11564/dod-deploy-assessment/command.yaml\"
          && grep -q 'dead subsystem removal' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11564/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Bind OMN-11564 central evidence to the OCC PR that carries this bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11564/dod-occ-pr-self/command.yaml\"
          && grep -q '^ticket_id: \"OMN-11564\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11564/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
