---
schema_version: "1.0.0"
ticket_id: "OMN-14751"
title: "B3 (omniintelligence + omnidash): agent_source read-mapping in intent projection + dashboard"
summary: >
  OCC evidence companion for OmniNode-ai/omnibase_infra PR #2772 — vendored mirror of the node_projection_intent_classification
  forward migration, adding the nullable agent_source column so a clean redeploy recreates the projection
  table with the same shape omnimarket writes. Paired with OmniNode-ai/omnimarket#2088 (B3 projection
  side of the OMN-14749 agent_source parity seam); this PR goes first per the migration-vendor-parity
  gate's forced merge order.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2772 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2772"
    description: >
      PR #2772 on OmniNode-ai/omnibase_infra — adds docker/migrations/forward/nodes/node_projection_intent_classification/0001_intent_classification_agent_source.sql
      (idempotent `ALTER TABLE intent_classification_events ADD COLUMN IF NOT EXISTS agent_source TEXT`,
      nullable by design) plus the matching ledger entry. Two-file, additive-only migration change; no
      runtime code touched. Content-pinned check (OMN-15309 admissibility corrective, 2026-08-18): reads
      the actual migration file at the PR's head SHA via the GitHub contents API and asserts the real
      ADD COLUMN statement text is present -- a PR-existence probe alone proves the PR exists, not that
      the migration says what this contract claims.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_intent_classification/0001_intent_classification_agent_source.sql?ref=a690cd85fa46286d5afba9c5ca4b936ff9e2917d"
          --jq .content | base64 -d)"; printf '%s' "$body" | grep -qF 'ADD COLUMN IF NOT EXISTS agent_source
          TEXT' && echo "OMN-14751-PR-2772-PROBE-PASS: ADD COLUMN IF NOT EXISTS agent_source TEXT is present
          in the migration file at the pinned head"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2873"
    description: >
      PR #2873 on OmniNode-ai/omnibase_infra supersedes the closed fork-backed #2772 with the same vendored
      mirror of the node_projection_intent_classification forward migration. The replacement PR carries
      docker/migrations/forward/nodes/node_projection_intent_classification/0001_intent_classification_agent_source.sql
      at pinned head 4408776dec86b560c175695a02899cb79cd6dcf0 and keeps the required nullable `ALTER TABLE
      intent_classification_events ADD COLUMN IF NOT EXISTS agent_source TEXT` statement. Content-pinned
      check reads the actual replacement PR file through the GitHub contents API and asserts the claimed
      ADD COLUMN statement is present.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_intent_classification/0001_intent_classification_agent_source.sql?ref=4408776dec86b560c175695a02899cb79cd6dcf0"
          --jq .content | base64 -d)"; printf '%s' "$body" | grep -qF 'ADD COLUMN IF NOT EXISTS agent_source
          TEXT' && echo "OMN-14751-PR-2873-PROBE-PASS: ADD COLUMN IF NOT EXISTS agent_source TEXT is present
          in the migration file at the pinned head"
  - id: "occ-self-bind-pr-6645"
    description: "Binds OMN-14751 to OCC companion PR #6645 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6645 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnidash-pr-289"
    description: >
      PR #289 on OmniNode-ai/omnidash -- 10-file diff (1 net-new fixture, 9 modified: server/postgres-projection-reader.ts
      and server/sqlite-projection-reader.ts add a case for onex.snapshot.projection.intent-classification.v1
      mapping correlation_id AS intent_id, session_id AS session_ref, intent_class AS intent_category,
      carrying agent_source; IntentDistributionWidget.tsx rewritten to group raw event rows client-side
      instead of expecting pre-aggregation; SessionTimelineWidget.tsx renders an agent_source chip; fixtures
      + registry regenerated to match). Independently verified against the PR's exact head in a scratch
      worktree, not just the PR's own prose: the postgres/sqlite reader SQL column mapping matches exactly
      as described, the sqlite reader uses the existing hasTable/hasColumn guard pattern, and the chip
      CSS classes (chip.accent / chip.good) already exist at src/styles/design-tokens.css:214,217 (no
      new styles added, as claimed). CI's Typecheck & Tests job independently confirms tsc/eslint/1651
      tests green. Human-authored PR (andywu42) -- this entry attests to the diff content verified above,
      not to authorship.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnidash/contents/server/postgres-projection-reader.ts?ref=13762969126cad6c5f37fba5d77d8804110e1eef"
          --jq .content | base64 -d)"; printf '%s' "$body" | grep -qF 'correlation_id AS intent_id' &&
          echo "OMN-14751-PR-289-PROBE-PASS: correlation_id AS intent_id mapping is present in server/postgres-projection-reader.ts
          at the pinned head"
  - id: "dod-OmniNode-ai-omnimarket-pr-2088"
    description: >
      PR #2088 on OmniNode-ai/omnimarket -- 6-file diff (contract.yaml, handler_intent_classification.py,
      handler_projection_intent_classification.py, migrations/0001_intent_classification_agent_source.sql,
      tests/test_golden_chain_projection_intent_classification.py, tests/test_omn14751_intent_agent_source_real_postgres.py).
      Fixes the wire mismatch (producer emits intent_category, model accepted only intent_class) via validation_alias=AliasChoices,
      and projects agent_source: str | None through the INSERT / ON CONFLICT path. Independently re-ran
      tests/test_golden_chain_projection_intent_classification.py against the PR's exact head (824b10f2)
      in a scratch worktree: 25 passed, matching the PR's claim exactly. Independently diffed the migration
      file against the OmniNode-ai/omnibase_infra#2772 vendored mirror: byte-for-byte identical (1221
      bytes both sides). Other CI reds independently triaged by reading job logs, not asserted: Architectural
      Compliance Lint's own boundary-violation script reported "Errors: 0, Warnings: 0" -- the job failed
      on a downstream actions/github-script step hitting "HttpError: Resource not accessible by integration"
      (GITHUB_TOKEN scope issue posting a PR comment), not a content violation; PR Arch Review Gate fails
      only because it reads that job's overall (non-)success, a cascade; Hostile Reviewer / Hostile Review
      Gate returned verdict=degraded with 0 findings because "all review endpoints [192.168.86.201:8000
      192.168.86.201:8001] unreachable" -- an infra-availability flake, not a review finding; node-migration-vendor-parity-gate
      correctly reports no vendored counterpart yet in omnibase_infra@dev because the paired PR (OmniNode-ai/omnibase_infra#2772)
      has not merged there yet -- a real, currently-open sequencing dependency, not a defect in this PR's
      own content, which was independently confirmed byte-identical to the vendored file above.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          body="$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_intent_classification/handlers/handler_projection_intent_classification.py?ref=824b10f2c4a0eafc5eac009a6f2a20f0c4f5633d"
          --jq .content | base64 -d)"; printf '%s' "$body" | grep -qF 'AliasChoices("intent_class", "intent_category")'
          && echo "OMN-14751-PR-2088-PROBE-PASS: AliasChoices(intent_class, intent_category) wire-mismatch
          fix is present in handler_projection_intent_classification.py at the pinned head"
  - id: "occ-self-bind-pr-6689"
    description: "OCC companion PR #6689 -- self-bind for the omnidash#289/omnimarket#2088 follow-up entries.
      Established pattern: occ-self-bind-pr-6645."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6689 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-7026"
    description: >
      OCC companion PR #7026 -- self-bind for the omnibase_infra#2873 replacement evidence entry. This
      binds the OMN-14751 OCC companion PR itself to the ticket so OCC preflight and Receipt Gate can
      validate the companion's own PR number and exact head.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7026 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "dod-OmniNode-ai-omnimarket-pr-2137"
    description: "PR #2137 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/tests/test_golden_chain_projection_intent_classification.py?ref=82af8222c98f5bd885e6ab14afec0f6f14f707a8 --jq '.content' | base64 -d | grep -c 'class TestAgentSourceSeam'
  - id: "dod-OmniNode-ai-omnimarket-pr-2137-ci"
    description: "PR #2137 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2137 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-7030"
    description: "OCC companion PR #7030 — self-bind for OMN-14751 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7030 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omniclaude-pr-2034"
    description: >
      PR #2034 on OmniNode-ai/omniclaude — adds explicit durability tiers to the omniclaude event registry.
      The PR changes plugins/onex/lib/event_registry/omniclaude.yaml at pinned head 40c974adc1a80255f78d710aaabab0ceaf13d315,
      increasing real `tier:` entries from 2 at the base commit to 62 at the head commit. Content-pinned
      probe reads the exact registry file through the GitHub contents API and asserts the expected tier
      count at the pinned head, with the base count recorded in the receipt as the red comparison.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          count="$(gh api 'repos/OmniNode-ai/omniclaude/contents/plugins/onex/lib/event_registry/omniclaude.yaml?ref=40c974adc1a80255f78d710aaabab0ceaf13d315'
          --jq .content | base64 -d | grep -c 'tier:')"; test "$count" = "62" && echo "OMN-14751-OMNICLAUDE-PR-2034-PASS:
          omniclaude registry has 62 tier entries at pinned head"
  - id: "dod-OmniNode-ai-omniclaude-pr-2034-ci"
    description: >
      PR #2034 on OmniNode-ai/omniclaude — product diff scope check. The PR is a two-file change: .secrets.baseline
      timestamp refresh (+1/-1) and plugins/onex/lib/event_registry/omniclaude.yaml durability-tier additions
      (+60/-0), with exact head 40c974adc1a80255f78d710aaabab0ceaf13d315 and base bfa618e2208c89537569931a8d416720ccb22d9d.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2034 --repo OmniNode-ai/omniclaude --json files,headRefOid,baseRefOid"
  - id: "occ-self-bind-pr-7070"
    description: >
      OCC companion PR #7070 — self-bind for the OMN-14751 omniclaude#2034 evidence companion. Binds the
      companion's own PR number and first pushed head so OCC preflight and Receipt Gate can validate the
      companion before downstream product PR reruns.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7070 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "occ-self-bind-pr-7141"
    description: "Binds OMN-14751 to OCC companion PR #7141 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7141 --repo OmniNode-ai/onex_change_control --json number,state"
