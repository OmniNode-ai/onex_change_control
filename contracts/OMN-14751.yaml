---
schema_version: "1.0.0"
ticket_id: "OMN-14751"
title: "B3 (omniintelligence + omnidash): agent_source read-mapping in intent projection + dashboard"
summary: >
  OCC evidence companion for OmniNode-ai/omnibase_infra PR #2772 — vendored mirror of the node_projection_intent_classification
  forward migration, adding the nullable agent_source column so a clean redeploy recreates the projection
  table with the same shape omnimarket writes. Paired with OmniNode-ai/omnimarket#2088 (B3 projection
  side of the OMN-14749 agent_source parity seam); this PR goes first per the migration-vendor-parity
  gate's forced merge order.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2772 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2772"
    description: >
      PR #2772 on OmniNode-ai/omnibase_infra — adds docker/migrations/forward/nodes/node_projection_intent_classification/0001_intent_classification_agent_source.sql
      (idempotent `ALTER TABLE intent_classification_events ADD COLUMN IF NOT EXISTS agent_source TEXT`,
      nullable by design) plus the matching ledger entry. Two-file, additive-only migration change; no
      runtime code touched.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2772 --repo OmniNode-ai/omnibase_infra --json number,state,headRefName,files"
  - id: "occ-self-bind-pr-6645"
    description: "Binds OMN-14751 to OCC companion PR #6645 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6645 --repo OmniNode-ai/onex_change_control --json number,state"
