---
schema_version: "1.0.0"
ticket_id: "OMN-15215"
title: "Autobind OCC evidence for OMN-15215"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #2488."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2488 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2488"
    description: "PR #2488 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2488-ci"
    description: "PR #2488 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-5065"
    description: "OCC companion PR #5065 — self-bind for OMN-15215 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-deploy-falsifiable-omn15215"
    description: >-
      Falsifiable deploy probe (OMN-8912/OMN-14505) for the OMN-15215 fix in OmniNode-ai/omnibase_infra#2488:
      verifies the pinned PR head SHA's raw file content on GitHub (not this repo's own receipt/contract)
      actually contains the new NOT_READY reconciliation functions and the widened routing-strategy allowlist.
      Hermetic, no-live-deploy probe per the OMN-14051 guard — this is a code-only ticket; the live re-proof
      (a real node_ledger_projection_compute Kafka consumer group on stability-test) happens post-merge
      via OMN-15169's own warm refresh + golden-chain assertion, which the sanctioned refresh_stability_lane.sh
      script can only run against origin/dev, never an open PR branch.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          SHA=ae59056839b0ae2cdfb09a4d5fb21a3319255355; gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=${SHA}"
          --jq .content | base64 -d | grep -q "async def reattach_not_ready_contracts" && gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=${SHA}"
          --jq .content | base64 -d | grep -q "async def run_not_ready_reconciliation_loop" && gh api
          "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/service_kernel.py?ref=${SHA}"
          --jq .content | base64 -d | grep -q "not_ready_reconciliation_task" && gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/contract_loaders/handler_routing_loader.py?ref=${SHA}"
          --jq .content | base64 -d | grep -q '"topic_match"' && echo "OMN-15215-DEPLOY-PROBE-PASS: all
          4 symbols present at ${SHA}"
