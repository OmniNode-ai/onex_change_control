---
schema_version: "1.0.0"
ticket_id: "OMN-12791"
title: "receipt-honesty CI gate — fail gamed DoD receipts"
summary: >-
  Adds validator_receipt_honesty.py to omnibase_core with five honesty rules (A-E) over ModelDodReceipt:
  no-op echo probes, deferral language in PASS receipts, self-attestation, fake human verifiers, and deploy-keyword
  evidence backed only by echo. Triggered by OMN-12780/dod-deploy-gate-migration-only and OMN-12779/dod-deploy-wave3
  echo-PASS receipts. Wired as blocking pre-commit hook and required CI job in onex_change_control. TDD:
  82 tests pass.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-validator-impl"
    description: >-
      validator_receipt_honesty.py implements all five honesty rules (A-E) as discrete functions. Module
      is pure (no I/O in rule functions), mypy --strict clean, ruff clean.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12791/dod-validator-impl/command.yaml
          && grep -q 'Success: no issues found' drift/dod_receipts/OMN-12791/dod-validator-impl/command.yaml"

  - id: "dod-tdd-tests-82-pass"
    description: >-
      82 unit tests pass, seeding actual gamed receipts (OMN-12780 dod-deploy-gate-migration-only, OMN-12779
      dod-deploy-wave3) as fixtures that MUST fail.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12791/dod-tdd-tests-82-pass/command.yaml
          && grep -q '82 passed' drift/dod_receipts/OMN-12791/dod-tdd-tests-82-pass/command.yaml"

  - id: "dod-precommit-hook"
    description: >-
      check-receipt-honesty hook added to omnibase_core/.pre-commit-hooks.yaml and wired in onex_change_control/.pre-commit-config.yaml.
      Fires on any staged drift/dod_receipts/*.yaml.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12791/dod-precommit-hook/command.yaml
          && grep -q 'FOUND' drift/dod_receipts/OMN-12791/dod-precommit-hook/command.yaml"

  - id: "dod-ci-gate"
    description: >-
      honesty-gate job added to onex_change_control/.github/workflows/ci.yml and added to ci-summary needs
      list. Blocking on every PR. No warn-only mode.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q honesty-gate .github/workflows/ci.yml"

  - id: "dod-scan-confirms-gamed-receipts"
    description: >-
      Live scan over all drift/dod_receipts/ confirms OMN-12780/dod-deploy-gate-migration-only and OMN-12779/dod-deploy-wave3
      are both flagged by NO_OP_PROBE + DEPLOY_REALNESS rules.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12791/dod-scan-confirms-gamed-receipts/command.yaml
          && grep -q 'OMN-12780' drift/dod_receipts/OMN-12791/dod-scan-confirms-gamed-receipts/command.yaml
          && grep -q 'NO_OP_PROBE' drift/dod_receipts/OMN-12791/dod-scan-confirms-gamed-receipts/command.yaml"

  - id: "dod-omnibase-core-pr"
    description: >-
      omnibase_core PR containing validator_receipt_honesty.py + tests + pre-commit hook entry.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12791/dod-omnibase-core-pr/command.yaml
          && grep -q '^pr_number: 1219$' drift/dod_receipts/OMN-12791/dod-omnibase-core-pr/command.yaml
          && gh pr view 1219 --repo OmniNode-ai/omnibase_core --json state,url"

  - id: "dod-occ-pr"
    description: >-
      onex_change_control PR containing ci.yml honesty-gate job + pre-commit-config.yaml hook wire.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12791/dod-occ-pr/command.yaml &&
          grep -q '^pr_number: 2306$' drift/dod_receipts/OMN-12791/dod-occ-pr/command.yaml && gh pr view
          2306 --repo OmniNode-ai/onex_change_control --json state,url"
