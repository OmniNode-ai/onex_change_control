---
schema_version: "1.0.0"
ticket_id: "OMN-12688"
title: "Workstream B: converge node_delegation_orchestrator dispatcher authority to canonical handler
  routing"
summary: >-
  Central evidence that omnimarket PR #1040 moves live node_delegation_orchestrator MessageDispatchEngine
  wiring from six bespoke dispatcher registrations to one canonical workflow dispatcher that routes supported
  payloads through HandlerDelegationWorkflow.handle().
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-canonical-workflow-dispatcher"
    description: "Focused omnimarket tests prove canonical handler routing owns live delegation workflow
      dispatch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12688/dod-omnimarket-canonical-workflow-dispatcher/command.yaml"
          && grep -q '^pr_number: 1040$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12688/dod-omnimarket-canonical-workflow-dispatcher/command.yaml"
  - id: "dod-deploy-gate-runtime-path"
    description: "Deploy-gate evidence for omnimarket runtime dispatcher wiring changes in PR #1040."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12688/dod-deploy-gate-runtime-path/command.yaml"
          && grep -q '^pr_number: 1040$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12688/dod-deploy-gate-runtime-path/command.yaml"
  - id: "dod-occ-pr-2172"
    description: "onex_change_control PR #2172 carries the OMN-12688 contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12688/dod-occ-pr-2172/command.yaml"
          && grep -q '^pr_number: 2172$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12688/dod-occ-pr-2172/command.yaml"
