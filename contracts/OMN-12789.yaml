---
schema_version: "1.0.0"
ticket_id: OMN-12789
title: "feat(omnimarket): delegation e2e probe harness — Wave-4 4A-4F slice"
summary: >
  Permanent, parameterized e2e probe harness under omnimarket/tests/integration/e2e_probe/
  covering the full delegation pipeline: thin-publish → bus → terminal event →
  projection write → projection API read → dashboard assertions (18 tests, 4A-4F).
  Replaces per-session hand-driven probes. All tests opt-in guarded by
  OMN_ALLOW_LIVE_E2E_PROBE=true. Assertion thresholds left as clearly-marked
  TODO placeholders for calibration from GEN-01 packet (Milestone 4.4(b) / OMN-12952).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Dry-run: 18 tests collected and skip cleanly without OMN_ALLOW_LIVE_E2E_PROBE"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12789/dod-001/command.yaml"
