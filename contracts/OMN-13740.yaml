---
schema_version: "1.0.0"
ticket_id: "OMN-13740"
title: "test(OMN-13740): add unit tests for ModelFileSystemRequest validators"
summary: >-
  Add focused unit coverage for ModelFileSystemRequest validators in omnibase_infra. This is a pure test-only
  change that exercises request validation behavior without changing runtime logic, deployed services,
  data stores, event-bus topics, or production configuration.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused ModelFileSystemRequest validator tests pass on omnibase_infra PR #2151."
    command: >-
      grep -q '^status: PASS$' drift/dod_receipts/OMN-13740/dod-focused-tests/command.yaml && grep -q
      '^status: PASS$' drift/dod_receipts/OMN-13740/dod-no-runtime-mutation/command.yaml && grep -q '^status:
      PASS$' drift/dod_receipts/OMN-13740/dod-occ-self/command.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused unit tests prove ModelFileSystemRequest validator behavior."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13740/dod-focused-tests/command.yaml"
  - id: "dod-no-runtime-mutation"
    description: >-
      OMN-13740 is a test-only coverage PR with no live deploy, restart, .201 mutation, production data
      mutation, new REST endpoint, event-bus topic change, or runtime lane mutation.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13740/dod-no-runtime-mutation/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-13740 contract and receipts.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13740/dod-occ-self/command.yaml"
