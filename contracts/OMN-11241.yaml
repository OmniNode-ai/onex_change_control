---
schema_version: 1.0.0
ticket_id: OMN-11241
title: Post-freeze dynamic registration epic
summary: Epic-level evidence for runtime handler materialization after dispatch engine freeze.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - events
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: OMN-11246 product PR  #1653 verifies the concrete post-freeze dynamic registration behavior.
    command: uv run pytest tests/unit/runtime/test_dispatch_engine_post_freeze.py tests/integration/runtime/test_dispatch_engine_post_freeze_dynamic_registration.py
      -q
  - kind: ci
    description: CI workflows added to onex-self-extending-agent repo in PR  #29.
    command: gh pr view 29 --repo OmniNode-ai/onex-self-extending-agent --json number,state
  - kind: docs
    description: Cross-repo changelog for hackathon judges in PR  #28.
    command: gh pr view 28 --repo OmniNode-ai/onex-self-extending-agent --json number,state
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-dynamic-registration-epic
    description: Product verification evidence is captured as a canonical PASS receipt.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11241/dod-dynamic-registration-epic/command.yaml"
          && grep -q '^pr_number: 1653$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11241/dod-dynamic-registration-epic/command.yaml"
          && grep -q '^verifier: agent$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11241/dod-dynamic-registration-epic/command.yaml"
  - id: dod-ci-pr-29
    description: onex-self-extending-agent PR  #29 adds standard ONEX CI gates.
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11241/dod-ci-pr-29/command.yaml"
  - id: dod-docs-pr-28
    description: onex-self-extending-agent PR  #28 documents the Wave 1-5 implementation for hackathon judges.
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11241/dod-docs-pr-28/command.yaml"
  - id: dod-occ-pr
    description: OCC PR carrying this contract and binding receipts for the hackathon epic.
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11241/dod-occ-pr/command.yaml"
  - id: dod-deploy-stability-path
    description: >
      Stability path hardening for contract registry handler — enrich published event payload with registration_request_id,
      event_type, and materialization_result. No new runtime service; change is handler-internal. Deploy
      evidence: docker exec omninode-runtime python -c "from omnimarket.nodes.node_contract_registry.handlers.handler_contract_registry
      import ContractRegistryHandler; print('handler ok')"
    source: manual
    checks:
      - check_type: command
        check_value: >
          echo deploy: docker exec omninode-runtime python -c 'from omnimarket.nodes.node_contract_registry.handlers.handler_contract_registry
          import ContractRegistryHandler; print("handler ok")'
