---
schema_version: "1.0.0"
ticket_id: "OMN-10589"
title: "chore: generate contract config audit CSV (2026-05-07 refresh)"
summary: "Update the omnimarket contract config audit script to use 2026-05-07 output paths and remove
  private ticket identifiers from generated markdown. Run against all 153 contracts (2 new nodes since
  last audit). Input for Task 18 (OMN-10567)."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Receipt Gate passes on omnimarket PR #549 after this OCC evidence lands."
    command: "gh pr checks 549 --repo OmniNode-ai/omnimarket --watch=false --required"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Audit script runs and writes 153 rows (one per contract.yaml); counts match CSV."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10589/dod-001/command.yaml\"; else uv run python
          scripts/audit/contract_config_audit.py --print-summary; fi"
  - id: "dod-002"
    description: "8 unit tests cover classification logic, CSV/MD emission, and repo-wide audit run."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10589/dod-002/command.yaml\"; else uv run pytest
          tests/unit/scripts/test_contract_config_audit.py -v; fi"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10589 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10589/dod-occ-pr/command.yaml\"; else gh pr checks
          \"$(gh pr list --repo OmniNode-ai/onex_change_control --head jonah/omn-10589-occ-evidence --json
          number --jq '.[0].number')\" --repo OmniNode-ai/onex_change_control --watch=false --required;
          fi"
