---
schema_version: "1.0.0"
ticket_id: "OMN-18220"
title: "Autobind OCC evidence for OMN-18220"
summary: >
  OCC contract authored by node_occ_companion_compute and the evidence-source autobind path
  for OmniNode-ai/omniweb PRs #397 and #398.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #397 and PR #398 product diff scopes present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "tests"
    description: |-
      OWED, not claimed. PRs #397 and #398 on OmniNode-ai/omniweb change no pytest target, so this producer cannot derive a behavior-class check from their diffs and mints none - the changed surface (.github/workflows/m3-customer-pass-chain.yml, tests/customer-pass-workflow.test.ts) is unproven by this contract. A behavior proof must be hand-authored as a `source: manual` dod_evidence item before OMN-15911's flip conjunct can be satisfied. Recorded here rather than papered over with a ticket-independent suite that would read as proof (OMN-16434).
    command: "uv run pytest <hand-authored target> -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniweb-pr-397"
    description: "PR #397 on OmniNode-ai/omniweb — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 397 --repo OmniNode-ai/omniweb --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniweb/pulls/397/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/m3-customer-pass-chain.yml'
  - id: "dod-OmniNode-ai-omniweb-pr-398"
    description: "PR #398 on OmniNode-ai/omniweb — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 398 --repo OmniNode-ai/omniweb --json number,state"
  - id: "dod-OmniNode-ai-omniweb-pr-398-ci"
    description: "PR #398 on OmniNode-ai/omniweb — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 398 --repo OmniNode-ai/omniweb --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omniweb-pr-397"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-9253"
    description: "Binds OMN-18220 to OCC companion PR #9253 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9253 --repo OmniNode-ai/onex_change_control --json number,state"
