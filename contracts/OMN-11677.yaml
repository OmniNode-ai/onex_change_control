---
schema_version: "1.0.0"
ticket_id: "OMN-11677"
title: "node_test_generator compute node (deterministic, contract-declared)"
summary: >-
  New ONEX compute node in omnimarket that generates deterministic Python test source from a ticket contract.
  Same contract + same generator_version + same template_hash + same generation_profile_hash produces
  the same test_hash. Part of the context pack pipeline epic.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "All 9 unit tests pass (determinism, AST parse, model names, topic compliance)."
    command: "uv run pytest tests/nodes/node_test_generator/ -v --tb=short -m 'not kafka'"
  - kind: "manual"
    description: "Central OCC evidence PR carries this contract and receipts."
    command: "gh pr view <occ-pr> --repo OmniNode-ai/onex_change_control"
dod_evidence:
  - id: "dod-omnimarket-pr-753"
    description: "Bind omnimarket PR #753 node_test_generator implementation at observed PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11677/dod-omnimarket-pr-753/command.yaml"
          && grep -q '^pr_number: 753$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11677/dod-omnimarket-pr-753/command.yaml"
          && grep -q '^commit_sha: "eeed5d9e49e3120bebfdf351e0cd6e1b06c0e3e0"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11677/dod-omnimarket-pr-753/command.yaml"
  - id: "dod-deploy-assessment"
    description: "Deploy-gate assessment for omnimarket PR #753 runtime-path node addition."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11677/dod-deploy-assessment/command.yaml"
          && grep -q 'deploy-gate' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11677/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11677 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11677/dod-occ-pr-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
