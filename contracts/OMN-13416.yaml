---
schema_version: "1.0.0"
ticket_id: "OMN-13416"
title: "Detect CI event-delivery gaps and safely retrigger required workflows"
summary: >-
  Bind omnimarket PR #1407, which teaches node_ci_watch and node_merge_sweep_triage_orchestrator to detect
  missing required workflow contexts and request a bounded empty-commit retrigger through node_ci_rerun_effect.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1407"
    description: >-
      Per-code-PR binding receipt for omnimarket PR #1407 (jonah/omn-13416-ci-event-delivery-gap, base=dev).
      The PR adds required-check gap detection and bounded empty-commit CI retrigger behavior.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13416/dod-omnimarket-pr-1407/command.yaml
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment for PR #1407. The touched runtime paths change CI-watch, merge-sweep triage, and
      CI-rerun node code. No live lane deploy, runtime restart, Kafka topic mutation, docker exec, or
      database migration is performed by this PR; runtime activation is owned by the redeploy workstream.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13416/dod-deploy-assessment/command.yaml # deploy:
          static runtime-node code only; no live deploy/docker exec/rpk topic produce before merge
  - id: "dod-occ-pr-self"
    description: >-
      OCC self-binding receipt for the evidence PR carrying the OMN-13416 contract and receipts.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13416/dod-occ-pr-self/command.yaml
