---
schema_version: "1.0.0"
ticket_id: "OMN-11379"
title: "Structured ModelValidationResult + failure taxonomy"
summary: >-
  Bind OCC evidence for onex-self-extending-agent PR #66 (OMN-11379), which adds ModelValidationResult
  (frozen Pydantic model) to src/agent/models.py, FailureClass StrEnum with 6 values to src/pipeline/failures.py,
  and validate_generation_structured() to src/pipeline/validator.py with sha256 hash computation. Includes
  7 unit tests covering construction, frozen enforcement, serialization roundtrip, enum values, and hash
  computation.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Downstream PR test suite passes: ModelValidationResult, FailureClass, and validate_generation_structured
      are exercised by 7 unit tests."
    command: "gh pr checks 66 --repo OmniNode-ai/onex-self-extending-agent"
dod_evidence:
  - id: "dod-tests"
    description: "Bind OMN-11379 evidence to onex-self-extending-agent PR #66 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11379/dod-tests/command.yaml\"
          && grep -q '^pr_number: 66$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11379/dod-tests/command.yaml\"
          && grep -q '^commit_sha: \"bc660fb96572b32a3e2b247fac81a820107b1e81\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11379/dod-tests/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-11379 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11379/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
