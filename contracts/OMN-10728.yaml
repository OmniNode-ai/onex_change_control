---
schema_version: "1.0.0"
ticket_id: "OMN-10728"
title: "Fix BuildKit uv-cache silently serving stale omnimarket on runtime deploys"
summary: >
  Binds omnibase_infra PR fixing the BuildKit uv cache mount stale-install bug to central OCC evidence.
  Dockerfile.runtime now passes OMNIMARKET_REF and ONEX_CHANGE_CONTROL_REF as full commit SHAs so the
  install URL changes when main advances, forcing a uv cache miss and a fresh fetch on every deploy.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-dockerfile-cache-bust"
    description: "Dockerfile.runtime uses ARG OMNIMARKET_REF and ARG ONEX_CHANGE_CONTROL_REF in the install
      URLs so the uv cache mount misses on SHA change."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10728/dod-dockerfile-cache-bust/command.yaml\""
  - id: "dod-executor-passes-refs"
    description: "deploy-agent executor._compose_build passes OMNIMARKET_REF and ONEX_CHANGE_CONTROL_REF
      as --build-arg to docker compose build."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10728/dod-executor-passes-refs/command.yaml\""
  - id: "dod-tests-pass"
    description: "5 new TDD tests in test_executor_cache_bust.py covering SHA resolution, fallback, and
      build-arg propagation all pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10728/dod-tests-pass/command.yaml\""
  - id: "dod-deploy-verify"
    description: "Post-deploy verification: docker exec into omninode-runtime and confirm import omnimarket.adapters.adr
      succeeds, proving the stale-cache bug is fixed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10728/dod-deploy-verify/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10728/dod-occ-pr/command.yaml\""
