---
schema_version: "1.0.0"
ticket_id: "OMN-10865"
title: "Migrate delegation pipeline nodes from omnibase_infra to omnimarket"
summary: >
  Moves node_delegation_orchestrator, node_delegation_quality_gate_reducer, and node_delegation_routing_reducer
  from omnibase_infra to omnimarket. Updates all import paths, migrates 56 delegation unit tests, adds
  metadata.yaml and entry points for all 3 nodes. Fixes arch lint regex to allow hyphens in producer names
  (omnibase-infra, omniclaude) in contract.yaml topic convention check.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-nodes-migrated"
    description: "All 3 delegation nodes exist in omnimarket PR #607 diff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10865/dod-nodes-migrated/command.yaml && grep -q
          '^status:.*PASS' drift/dod_receipts/OMN-10865/dod-nodes-migrated/command.yaml && grep -q 'all
          three delegation node names' drift/dod_receipts/OMN-10865/dod-nodes-migrated/command.yaml"
  - id: "dod-unit-tests"
    description: "56 delegation unit tests migrated/present in omnimarket PR #607 diff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10865/dod-unit-tests/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10865/dod-unit-tests/command.yaml && grep -q 'delegation unit test references
          found' drift/dod_receipts/OMN-10865/dod-unit-tests/command.yaml"
  - id: "dod-arch-lint-fix"
    description: "Arch lint regex updated to allow hyphens in producer segment"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10865/dod-arch-lint-fix/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10865/dod-arch-lint-fix/command.yaml && grep -q 'arch lint regex fix'
          drift/dod_receipts/OMN-10865/dod-arch-lint-fix/command.yaml"
  - id: "dod-omnimarket-pr"
    description: "omnimarket PR #607 is open for OMN-10865 node migration"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10865/dod-omnimarket-pr/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10865/dod-omnimarket-pr/command.yaml && grep -q 'omnimarket PR #607 is
          OPEN' drift/dod_receipts/OMN-10865/dod-omnimarket-pr/command.yaml"
  - id: "dod-omnimarket-pr-614"
    description: "omnimarket PR #614 is open and bound to the delegation schema-owner fix."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10865/dod-omnimarket-pr-614/command.yaml && grep
          -q '^status:.*PASS' drift/dod_receipts/OMN-10865/dod-omnimarket-pr-614/command.yaml && grep
          -q 'omnimarket PR #614 is OPEN' drift/dod_receipts/OMN-10865/dod-omnimarket-pr-614/command.yaml"
  - id: "dod-omnibase-infra-pr-1569"
    description: "omnibase_infra PR #1569 is open and bound to the delegation-node removal."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10865/dod-omnibase-infra-pr-1569/command.yaml &&
          grep -q '^status:.*PASS' drift/dod_receipts/OMN-10865/dod-omnibase-infra-pr-1569/command.yaml
          && grep -q 'omnibase_infra PR #1569 is OPEN' drift/dod_receipts/OMN-10865/dod-omnibase-infra-pr-1569/command.yaml"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for this OCC PR."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/onex_change_control --json state -q '.state'
          | grep -q OPEN"
  - id: "dod-occ-pr-979"
    description: "Self-binding receipt for the downstream receipt OCC PR."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10865/dod-occ-pr-979/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10865/dod-occ-pr-979/command.yaml && grep -q 'OCC PR #979 is OPEN' drift/dod_receipts/OMN-10865/dod-occ-pr-979/command.yaml"
