---
schema_version: "1.0.0"
ticket_id: "OMN-10865"
title: "Migrate delegation pipeline nodes from omnibase_infra to omnimarket"
summary: >
  Moves node_delegation_orchestrator, node_delegation_quality_gate_reducer, and node_delegation_routing_reducer
  from omnibase_infra to omnimarket. Updates all import paths, migrates 56 delegation unit tests, adds
  metadata.yaml and entry points for all 3 nodes. Fixes arch lint regex to allow hyphens in producer names
  (omnibase-infra, omniclaude) in contract.yaml topic convention check.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-nodes-migrated"
    description: "All 3 delegation nodes exist in omnimarket PR #607 diff"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr diff 607 -R OmniNode-ai/omnimarket | grep 'node_delegation_orchestrator' | head -1
  - id: "dod-unit-tests"
    description: "56 delegation unit tests pass in omnimarket"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr diff 607 -R OmniNode-ai/omnimarket | grep 'test.*delegation' | head -1
  - id: "dod-arch-lint-fix"
    description: "Arch lint regex updated to allow hyphens in producer segment"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr diff 607 -R OmniNode-ai/omnimarket | grep 'a-z_-' | head -1
  - id: "dod-omnimarket-pr"
    description: "omnimarket PR #607 is open for OMN-10865 node migration"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 607 -R OmniNode-ai/omnimarket --json state -q .state | grep -q OPEN"
