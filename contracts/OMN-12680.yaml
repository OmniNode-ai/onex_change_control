---
schema_version: "1.0.0"
ticket_id: "OMN-12680"
title: "Delete omnimarket bifrost delegation config re-export shim"
summary: >-
  Central evidence for deleting the omnimarket adapter-level bifrost delegation config re-export shim.
  The live config loader now imports the canonical omnibase_core delegation wire model directly.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-bifrost-shim-delete-tests"
    description: "Focused omnimarket DTO mapping test proves the adapter shim module is deleted and remaining
      delegation DTO shims resolve to core."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12680/dod-bifrost-shim-delete-tests/command.yaml"
