---
schema_version: "1.0.0"
ticket_id: OMN-6790
title: Add GLM (z.ai) provider handler
summary: "Add GLM (z.ai) provider handler"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}"
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json mergedAt -q .mergedAt --repo ${REPO}"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks ${PR_NUMBER} --watch --repo ${REPO}"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3040"
    description: "PR #3040 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/test_glm_endpoint_surface_omn6790.py?ref=6706fc109ac171cdf3509f206382261868674e63 --jq '.content' | base64 -d | grep -c 'def _iter_committed_files'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3040-ci"
    description: "PR #3040 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3040 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "occ-self-bind-pr-7723"
    description: "OCC companion PR #7723 — self-bind for OMN-6790 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7723 --repo OmniNode-ai/onex_change_control --json number,state"
