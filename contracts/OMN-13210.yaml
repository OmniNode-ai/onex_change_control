---
schema_version: "1.0.0"
ticket_id: "OMN-13210"
title: "[B1] Rebuild node_hostile_reviewer canonically"
summary: >-
  Phase B1 of the canonical node migration (epic OMN-13205) rebuilds the non-canonical node_hostile_reviewer
  workflow into canonical CONTRACT + NODE + HANDLER primitives. omnimarket PR #1260 deletes the bespoke
  workflow shell and introduces canonical orchestrator, compute, and effect nodes: node_hostile_reviewer_orchestrator,
  node_review_prompt_builder_compute, node_review_response_parser_compute, node_github_diff_effect, and
  node_review_convergence_compute. The reviewer start and completed topics are preserved, model inference
  reuses the A1 canonical inference effect bridge, and GitHub diff retrieval moves from shelled gh pr
  diff to a GitHub REST effect.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "pr-omnimarket"
    description: >-
      omnimarket PR #1260 carries the canonical node_hostile_reviewer rebuild. The receipt binds the PR
      head SHA and records local verification: full pytest tests/ -m "not kafka" passed, ruff and mypy
      were clean, runtime_sweep and metadata dependency gates were green, and pre-commit --all-files passed.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13210/pr-omnimarket/command.yaml"
  - id: "dod-deploy"
    description: >-
      Runtime/deploy preflight for the hostile reviewer canonical rebuild. The new canonical nodes are
      runtime registered, the preserved hostile-reviewer start and completed topics remain the external
      wire, and this change introduces no live deploy, DDL, or external service mutation before merge.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13210/dod-deploy/command.yaml"
