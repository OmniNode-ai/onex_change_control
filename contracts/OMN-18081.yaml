---
schema_version: "1.0.0"
ticket_id: "OMN-18081"
title: "API-key creation UI: make the user-entered label visible, and say a key can carry one"
summary: >
  OCC companion for OmniNode-ai/omniweb PR #364, binding AC1, AC2 and AC3. Bound by hand (union-resolve),
  the same way OCC#8760 was bound for omninode_infra#1266 and OCC#8787 for omninode_infra#1274: OMN-18073
  records that autobind is not minting companions in this window, and on this PR the occ-companion-effect
  job failed outright with "the lane declares security_protocol=SASL_PLAINTEXT / sasl_mechanism=SCRAM-SHA-256,
  but KAFKA_SASL_USERNAME and/or KAFKA_SASL_PASSWORD are not set in this job's environment" (run 34355977342),
  so no companion could be published and #364 has no receipt to cite. Cause of the ticket: the operator
  reported from the dev dashboard that "users cannot actually add in a label for the key anymore". The
  important finding is that NOTHING HAD BEEN REMOVED — read-only at origin/dev acec8de the label input
  still rendered, resolveKeyLabel still returned a typed label verbatim, action-logic forwarded it, api-client
  POSTed it as {name}, and the gateway's ApiKeyCreateBody requires and STORES that name (1-128, ^[\w\s\-\.]+$),
  so no omninode_infra change was needed and none was made. The operator-visible outcome was produced
  by three compounding things, none of them a removal - both mints write a name with NO PROMPT AT ALL
  (the gateway's at sign-up via DEFAULT_SIGNUP_API_KEY_NAME, and the panel's own first-visit backfill),
  the input sat behind a "New API key" toggle with nothing hinting it existed, and the page header read
  "you never type one in", which is true of the SECRET but reads as a statement about the whole panel.
  #364 opens the create form permanently so the label is reachable without first guessing that a button
  conceals it, scopes the header's claim to the credential and states the label is the customer's to give
  or leave blank, and captions an auto-named key as auto-named. The caption is the ticket's stated alternative
  to a rename - the gateway exposes NO PATCH /v1/api-keys/{id} (routers/api_keys.py carries post, get
  and delete only), so a rename is not buildable and is out of scope on the ticket. Both protections OMN-18011
  bought are preserved and now pinned rather than left to inspection: the submit control is still never
  gated on the label being non-empty (AC1 of that ticket, operator ruling 2026-09-06), and validateKeyLabel
  still refuses an onxk_-prefixed label, which is the mistake that put a 17-character key fragment in
  key d0554098's stored name. The PR closes the gap AC3 names - no test pinned that a label input renders
  AT ALL, so a regression deleting it outright would have gone green - with a new suite wired as CI Gate
  4l, RED at acec8de (10 of 17 fail) and GREEN at the pushed head. AC4 (the staging readback on dev.app
  after delivery) is deliberately NOT bound here: it cannot be proven before this merges and reaches onex-dev.
  Rule 24 is stated rather than implied - the .201 compose dev lane runs no dashboard, so there is no
  lab surface for this change, no lab pass is claimed, and the staging readback is the proof. A second
  commit on the same PR bumps the pnpm sharp override off the vulnerable exact pin 0.35.0 to '>=0.35.4
  <0.36'; that is unrelated to the label fix but was blocking it and every other open PR in the repo,
  and it is evidenced separately below. No credential was rotated, re-issued, revoked or minted, no secret
  value was read, printed or decoded, and no cluster object was mutated (rule 22).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #364 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #364 on OmniNode-ai/omniweb, derived from the PR's own diff: the suite the
      PR adds, which renders the real ApiKeyManager through renderToStaticMarkup and asserts that a
      label input is present on FIRST PAINT with no interaction, that the panel and page copy state a
      label is possible, and that both OMN-18011 protections still hold (the submit control is never
      gated on an empty label; an onxk_-prefixed label is still refused). Every source-scanning
      assertion carries a positive control. Executed at cwd `${OMNI_HOME}/omniweb`.
    command: |-
      pnpm test:api-key-label-affordance
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniweb-pr-364"
    description: >
      PR #364 on OmniNode-ai/omniweb — the always-open create form, the header and panel copy that state
      a label is possible, the auto-named caption, and the new label-affordance suite wired as CI Gate
      4l. Bound by hand (union-resolve) because the occ-companion-effect job could not publish a companion
      for this ticket (OMN-18073).
    source: "generated"
    binds_ac:
      - "AC1"
      - "AC2"
      - "AC3"
    checks:
      - check_type: "command"
        check_value: "gh pr view 364 --repo OmniNode-ai/omniweb --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniweb/pulls/364/files --paginate --jq '.[].filename' | grep -cFx 'tests/api-key-label-affordance.test.tsx'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #364 on OmniNode-ai/omniweb — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    binds_ac:
      - "AC1"
      - "AC2"
      - "AC3"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omniweb-pr-364"
    checks:
      - check_type: "test_passes"
        check_value: |-
          pnpm test:api-key-label-affordance
        cwd: "${OMNI_HOME}/omniweb"
  - id: "occ-self-bind-pr-8800"
    description: |-
      OCC self-binding receipt for PR #8800, the in-repo evidence PR carrying this contract.
      Binds OMN-18081 to the OCC PR by pr_number so eligibility can resolve a PASS receipt
      for the evidence PR itself. Appended, not edited: both existing entries are untouched,
      so their contract_entry_sha256 values stay valid (OMN-13888). No binds_ac — this is an
      infrastructure self-binding, not evidence for any acceptance criterion.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 8800 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "dod-OmniNode-ai-omninode_infra-pr-1280"
    description: >
      PR #1280 on OmniNode-ai/omninode_infra — the server half of the operator's 2026-09-09T~14:27Z clarification
      ("it should be for the label for the API key so that people can mint more than one API key"). The
      ticket's scope item 3 asked this repo to be READ rather than assumed: is there a per-tenant key
      cap, a unique (tenant_id, name) constraint refusing a duplicate label, or auto-mint logic that revokes
      or replaces an incumbent? Three suspects, one real. There is NO per-tenant cap — MAX_API_KEY_LIMIT
      is a pagination bound enforced in list_api_keys, not in the mint — and nothing on the create path
      revokes: _ensure_default_api_key returns early when the tenant holds any key and create_api_key's
      body is a single INSERT. So a key with a DISTINCT label was never blocked, which is the headline
      intent and already worked. The third was real: uq_tenant_api_keys_tenant_id_name_active (PARTIAL
      on is_active, so a revoked key does not hold its name hostage) correctly refuses a second ACTIVE
      key under the same name, but that refusal raised ApiKeyValidationError, routers/api_keys.py caught
      nothing, and there is no add_exception_handler for any ApiKeyError in docker/onex-api — so the route
      answered a bare 500, which omniweb's apiFetch renders as its no-detail fallback "API error 500".
      #1280 adds ApiKeyNameConflictError, deliberately a SUBCLASS of ApiKeyValidationError so provision.py's
      sign-up mint keeps treating a concurrent same-name race as an idempotent no-op (a sibling type would
      have converted that no-op into a 500 AT SIGN-UP), and translates it through one shared helper used
      by BOTH create routes into a typed 409 carrying a new RESOURCE_CONFLICT refusal code. Only the collision
      is translated: an invalid tenant_id arrives as the same base type and must not be answered "pick
      another label". The 409 is declared per-route rather than added to REFUSAL_RESPONSES, because a
      conflict is reachable only where a route has an incumbent to collide with. Bound by hand (union-resolve)
      onto this same-ticket companion because autobind is not minting companions in this window (OMN-18073);
      appended, so every existing entry's contract_entry_sha256 stays valid (OMN-13888). Rule 24 stated
      rather than implied: the .201 compose dev lane runs no onex-api and no dashboard, so there is no
      lab surface for this path, no lab pass is claimed, and the staging readback on onex-dev is the proof.
      No credential was rotated, re-issued, revoked or minted, no secret value was read or printed, and
      no cluster object was mutated (rule 22).
    source: "generated"
    binds_ac:
      - "AC1"
      - "AC3"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1280 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "test_passes"
        check_value: |-
          python3 -m pytest tests/test_omn_18081_duplicate_key_label_refusal.py -q
        cwd: "${OMNI_HOME}/omni_worktrees/OMN-18081/omninode_infra/docker/onex-api"
  - id: "occ-self-bind-pr-8805"
    description: >-
      OCC self-binding receipt for PR #8805, the in-repo evidence PR carrying the server half of this
      ticket. Binds OMN-18081 to that OCC PR by pr_number so eligibility can resolve a PASS receipt for
      the evidence PR itself. A second self-bind is needed because OCC#8800 merged before the server half
      was written, so the two halves of OMN-18081 arrive on two evidence PRs rather than one; occ-self-bind-pr-8800
      stays untouched above and keeps its own per-entry hash. Appended, not edited (OMN-13888). No binds_ac
      — this is an infrastructure self-binding, not evidence for any acceptance criterion.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8805 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
