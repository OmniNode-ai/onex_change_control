---
schema_version: "1.0.0"
ticket_id: "OMN-18056"
title: "Autobind OCC evidence for OMN-18056"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2417.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2417 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2417 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: "uv run pytest tests/unit/nodes/node_dod_verify/test_omn_18056_ac_binding_surfaced.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2417"
    description: "PR #2417 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/tests/unit/nodes/node_dod_verify/test_omn_18056_ac_binding_surfaced.py?ref=62a932e152abb9edaf22a45e42e65efa703e62aa --jq '.content' | base64 -d | grep -c 'def _contract'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2417/files --paginate --jq '.[].filename' | grep -cFx 'src/omnimarket/nodes/node_dod_verify/models/model_dod_verify_state.py'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2417 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2417/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2417 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_dod_verify/test_omn_18056_ac_binding_surfaced.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3347"
    description: "PR #3347 on OmniNode-ai/omnibase_infra — the AC-binding gate and the re-draw, wired
      inside the single all-verified block."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3347/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3347/files --paginate --jq '.[].filename' | grep -cFx 'tests/unit/nodes/node_evidence_autoclose_sweep_effect/_ac_binding_support.py'
  - id: "dod-omn-18056-infra-behavior-proof"
    description: "PR #3347 — diff-derived behavior proof: the gate's own suite, including the behavioural
      neutered-guard control the AST gate cannot express."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/ -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-OmniNode-ai-omnibase_core-pr-1666"
    description: "PR #1666 on OmniNode-ai/omnibase_core - binds_ac on both governance models, so the criterion-to-check
      relation becomes declarable."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_core/pulls/1666/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_core/models/contracts/ticket/model_dod_evidence_item.py'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_core/pulls/1666/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_core/models/ticket/model_contract_dod_item.py'
  - id: "dod-omn-18056-core-behavior-proof"
    description: "PR #1666 - diff-derived behavior proof: the model suite that pins the new field on both
      owners."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/models/contracts/test_model_dod_evidence_item.py -q
        cwd: "${OMNI_HOME}/omnibase_core"
  - id: "occ-self-bind-pr-8753"
    description: >-
      OCC self-binding receipt for PR #8753, the in-repo evidence PR carrying this contract. Binds OMN-18056
      to the OCC PR by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8753 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "occ-self-bind-pr-8729"
    description: "Binds OMN-18056 to OCC companion PR #8729 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8729 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omn-18056-occ-gate-behavior-proof"
    description: >-
      OCC PR #8769 — diff-derived behavior proof. The two OCC gates that refused a declared AC binding
      are exercised through their real entrypoints (`_validate_dod_item`, `validate_file`), each acceptance
      case paired with a positive control: a malformed `binds_ac` entry and an unknown field must still
      be refused after the change, or the gate was widened rather than fixed.
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/test_omn_18056_binds_ac_occ_gate.py -q
  - id: "dod-omn-18056-occ-corpus-unmoved"
    description: >-
      OCC PR #8769 — the change moves no existing count. The corpus-wide contract-yaml gate still validates
      every contract, run against the whole corpus rather than a sample.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          uv run validate-yaml contracts/OMN-*.yaml | grep -Eq '^\[OK\] All [0-9]+ file\(s\) valid$'
  - id: "dod-omn-18056-occ-orphan-ratchet-unmoved"
    description: >-
      OCC PR #8769 — the OMN-13888 orphan ratchet reports the frozen baseline exactly, before and after
      the change. Declared local_done_gate on a MEASURED runner limit, not a preference: the whole-corpus
      scan of drift/dod_receipts takes 39.3 s locally and exceeded the hosted gate's 60 s per-check ceiling
      on run 34321493136 (07:01:16Z to 07:02:16Z, "Command timed out after 60s"). The enforcing surface
      is the repo's own required "Orphan Corpus Ratchet (OMN-13888)" job, which takes 3m11s for exactly
      this reason and is green on this PR; this item records the local readback rather than duplicating
      that job inside a 60 s box it cannot fit in.
    source: "generated"
    execution_scope: "local_done_gate"
    checks:
      - check_type: "command"
        check_value: |-
          uv run python scripts/validation/check_receipt_hardening.py --orphan-corpus | grep -Fq 'Corpus matches the frozen orphan baseline exactly.'
  - id: "occ-self-bind-pr-8769"
    description: >-
      OCC self-binding receipt for PR #8769, the in-repo change carrying this contract append. Binds OMN-18056
      to the OCC PR by pr_number so eligibility can resolve a PASS receipt for the PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8769 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
