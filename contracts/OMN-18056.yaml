---
schema_version: "1.0.0"
ticket_id: "OMN-18056"
title: "Autobind OCC evidence for OMN-18056"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2417.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2417 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2417 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: "uv run pytest tests/unit/nodes/node_dod_verify/test_omn_18056_ac_binding_surfaced.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2417"
    description: "PR #2417 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/tests/unit/nodes/node_dod_verify/test_omn_18056_ac_binding_surfaced.py?ref=62a932e152abb9edaf22a45e42e65efa703e62aa --jq '.content' | base64 -d | grep -c 'def _contract'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2417/files --paginate --jq '.[].filename' | grep -cFx 'src/omnimarket/nodes/node_dod_verify/models/model_dod_verify_state.py'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2417 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2417/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2417 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_dod_verify/test_omn_18056_ac_binding_surfaced.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3347"
    description: "PR #3347 on OmniNode-ai/omnibase_infra — the AC-binding gate and the re-draw, wired
      inside the single all-verified block."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3347/files --paginate --jq '.[].filename' | grep -cFx 'src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3347/files --paginate --jq '.[].filename' | grep -cFx 'tests/unit/nodes/node_evidence_autoclose_sweep_effect/_ac_binding_support.py'
  - id: "dod-omn-18056-infra-behavior-proof"
    description: "PR #3347 — diff-derived behavior proof: the gate's own suite, including the behavioural
      neutered-guard control the AST gate cannot express."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_evidence_autoclose_sweep_effect/ -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-8729"
    description: "Binds OMN-18056 to OCC companion PR #8729 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8729 --repo OmniNode-ai/onex_change_control --json number,state"
