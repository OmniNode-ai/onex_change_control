---
schema_version: "1.0.0"
ticket_id: "OMN-11939"
title: "Dispatch worker knowledge context enrichment"
summary: >
  Bind central evidence for omnimarket PR #838, which adds opt-in knowledge_context_level support to node_dispatch_worker
  and records KB context evidence in dispatch worker results.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-838"
    description: "Bind OMN-11939 evidence to omnimarket PR #838."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11939/dod-omnimarket-pr-838/command.yaml
          && grep -q '^pr_number: 838$' drift/dod_receipts/OMN-11939/dod-omnimarket-pr-838/command.yaml"
  - id: "dod-dispatch-worker-local-validation"
    description: "Focused dispatch worker tests, strict mypy, ruff, and leaked-literals pass for the current
      omnimarket PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11939/dod-dispatch-worker-local-validation/command.yaml
          && grep -q '51 passed' drift/dod_receipts/OMN-11939/dod-dispatch-worker-local-validation/command.yaml
          && grep -q 'Success: no issues found' drift/dod_receipts/OMN-11939/dod-dispatch-worker-local-validation/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy gate evidence: dispatch worker contract/model/handler changes are locally validated
      and require no live runtime deployment before merge."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11939/dod-deploy-readiness/command.yaml
          && grep -qi 'deploy' drift/dod_receipts/OMN-11939/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the central OMN-11939 contract, deploy evidence, and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11939/dod-occ-pr/command.yaml"
