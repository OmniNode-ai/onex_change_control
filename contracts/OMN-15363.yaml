---
schema_version: "1.0.0"
ticket_id: "OMN-15363"
title: "Autobind OCC evidence for OMN-15363"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2589.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2589 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2589"
    description: "PR #2589 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2589 --repo OmniNode-ai/omnibase_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 2589 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2589 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2589 --repo OmniNode-ai/omnibase_infra --name-only | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'"
  - id: "dod-deploy-assessment-ghapi-head-076a9bf8"
    description: >-
      Append-only falsifiable deploy-scope proof for omnibase_infra PR #2589. The immutable product head
      must wire the lab analytics consumers to role_omnidash and must provision that role as non-BYPASSRLS.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-deploy-assessment"
    checks:
      - check_type: "command"
        check_value: |-
          body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.dev-lane.yml?ref=076a9bf861a8e701030176a0fec3154486d28e7e' --jq '.content' | base64 -d)" && printf '%s\n' "$body" | grep -q 'postgresql://role_omnidash:' && printf '%s\n' "$body" | grep -q 'ROLE_OMNIDASH_PASSWORD'
      - check_type: "command"
        check_value: |-
          body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/096_grant_role_omnidash_omnidash_analytics.sql?ref=076a9bf861a8e701030176a0fec3154486d28e7e' --jq '.content' | base64 -d)" && printf '%s\n' "$body" | grep -q 'NOBYPASSRLS' && printf '%s\n' "$body" | grep -q 'REVOKE DELETE, TRUNCATE, REFERENCES, TRIGGER'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_infra-pr-2589"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-5713"
    description: "Binds OMN-15363 to OCC companion PR #5713 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5713 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-5719"
    description: "Binds the OMN-15363 deploy evidence repair to OCC companion PR #5719."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5719 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
