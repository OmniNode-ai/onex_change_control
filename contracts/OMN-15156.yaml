---
schema_version: "1.0.0"
ticket_id: "OMN-15156"
title: "Autobind OCC evidence for OMN-15156"
summary: "OCC Evidence-Source autobind companion for omnimarket PR #1897."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1897 product diff scope present"
    command: "gh pr view 1897 --repo OmniNode-ai/omnimarket --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1897"
    description: "PR #1897 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1897 --repo OmniNode-ai/omnimarket --json number,state,headRefName"
  - id: "dod-OmniNode-ai-omnimarket-pr-1897-ci"
    description: "PR #1897 on OmniNode-ai/omnimarket — product diff scope check."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1897 --repo OmniNode-ai/omnimarket --json files"
  - id: "occ-self-bind-pr-4914"
    description: "OCC companion PR #4914 — self-bind for OMN-15156."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4914 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
  - id: "dod-deploy-omn15156-hermetic-symbol-check"
    description: >-
      Compute-only ticket: OMN-15156 threads an optional caller-supplied backend_id pin through LocalDelegationDispatchPort.dispatch()
      -> _resolve_initial_backend -> resolve_delegation_backend (OmniNode-ai/omnimarket#1897). No new
      bus topic, container, or live runtime deploy surface is introduced — the change is port-signature-scoped.
      Per the deploy-gate falsifiability guidance (OMN-14505) for compute-only/no-live-deploy tickets,
      this is a hermetic GitHub-content check (OMN-14051 exemption): it reads the actual file content
      at PR #1897's real head commit and asserts the class the PR introduces is genuinely present. It
      is NOT self-referential (does not read onex_change_control's own dod_receipts/contracts tree) and
      goes RED if the line is ever removed. Pinned to PR #1897's own head commit (501580287b2495f6117b82b8089bc3a7b71aa656)
      rather than @dev because this repo's own Contract Compliance Check EXECUTES the command at authoring
      time — pinning to @dev here would 404 until after #1897 merges. Follow-up: once #1897 merges, re-pin
      to the merged squash-commit SHA on omnimarket@dev per reference_never_pin_a_feature_branch_head.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_delegate_skill_orchestrator/ports/port_local_delegation_dispatch.py?ref=501580287b2495f6117b82b8089bc3a7b71aa656
          --jq .content | base64 -d | grep -q 'return resolve_delegation_backend(task_type, backend_id=backend_id)'"
  - id: "occ-self-bind-pr-4918"
    description: "OCC companion PR #4918 — self-bind for OMN-15156 (deploy-gate evidence companion)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4918 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
