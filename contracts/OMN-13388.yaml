---
schema_version: "1.0.0"
ticket_id: "OMN-13388"
title: "OMN-13388 — Bypass-token blocklist enforcement (pre-commit + CI + grep)"
summary: >-
  Phase 4.3 of OMN-13129. Implements the 9-token bypass blocklist as mechanical enforcement (CLAUDE.md
  Rule 5) in omnibase_core. Added: validator (bypass_token_blocklist.py), finding model (model_bypass_token_finding.py),
  67-test suite (all 9 tokens, failing + passing cases), pre-commit hook entry in .pre-commit-hooks.yaml,
  and reusable CI workflow (.github/workflows/validator-bypass-token-blocklist.yml). Applies to backend
  and UI contract edits. emergency_bypass.enabled is always false (schema-layer enforcement). omnibase_core
  PR #1291.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: >-
      67 tests green: uv run pytest tests/validators/test_bypass_token_blocklist.py -q → 67 passed. All
      9 tokens caught in failing cases; clean patch passes in passing cases; case-insensitive detection;
      suppression annotation; file-type filter; CLI integration. omnibase_core PR #1291 commit 3c436fa02.
  - id: "dod-mypy"
    description: >-
      mypy --strict passes on both new source files: bypass_token_blocklist.py and model_bypass_token_finding.py.
      Success: no issues found in 2 source files. omnibase_core PR #1291 commit 3c436fa02.
  - id: "dod-precommit"
    description: >-
      Pre-commit on all 5 new/modified files: all hooks pass. Only pre-existing failure (validate-deterministic-skill-routing:
      18 SKILL.md gaps in omniclaude) confirmed present on dev HEAD before this PR's changes. omnibase_core
      PR #1291 commit 3c436fa02.
  - id: "dod-occ-pr"
    description: "OCC receipt PR (this PR) carrying this contract and the receipts above."
