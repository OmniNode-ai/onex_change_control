---
schema_version: "1.0.0"
ticket_id: "OMN-13388"
title: "Bypass-token blocklist enforcement (pre-commit + CI + repo grep)"
summary: >-
  Phase 4.3 of OMN-13129. Implements the 9-token bypass blocklist as mechanical enforcement (CLAUDE.md
  Rule 5) in omnibase_core. Adds the validator (bypass_token_blocklist.py), the finding model (model_bypass_token_finding.py),
  a 67-test suite (all 9 tokens rejected, clean patch passes), a pre-commit hook entry in .pre-commit-hooks.yaml,
  and a reusable CI workflow (.github/workflows/validator-bypass-token-blocklist.yml) with a repo-wide
  grep mode. Applies to backend and UI contract edits. emergency_bypass.enabled is always false. omnibase_core
  PR #1291.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: >-
      67 tests green for tests/validators/test_bypass_token_blocklist.py — all 9 bypass tokens caught
      in failing cases, clean patch passes, case-insensitive detection, suppression annotation, file-type
      filter, and CLI integration. Proven by the feature PR #1291 receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13388/dod-unit-tests/command.yaml"
          && grep -q '^ticket_id: "OMN-13388"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13388/dod-unit-tests/command.yaml"
          && grep -q '^pr_number: 1291$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13388/dod-unit-tests/command.yaml"
          && grep -q '67 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13388/dod-unit-tests/command.yaml"
  - id: "dod-mypy"
    description: >-
      mypy --strict passes on both new source files (bypass_token_blocklist.py and model_bypass_token_finding.py).
      Proven by the feature PR #1291 receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13388/dod-mypy/command.yaml"
          && grep -q '^pr_number: 1291$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13388/dod-mypy/command.yaml"
          && grep -q 'no issues found' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13388/dod-mypy/command.yaml"
  - id: "dod-precommit"
    description: >-
      pre-commit passes on all 5 new/modified files in the feature PR. Proven by the feature PR #1291
      receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13388/dod-precommit/command.yaml"
          && grep -q '^pr_number: 1291$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13388/dod-precommit/command.yaml"
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR carrying the OMN-13388 contract + receipts used by the feature
      PR #1291 Receipt Gate via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13388/dod-occ-pr/command.yaml"
          && grep -q '^ticket_id: "OMN-13388"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13388/dod-occ-pr/command.yaml"
          && grep -q '^evidence_item_id: "dod-occ-pr"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13388/dod-occ-pr/command.yaml"
