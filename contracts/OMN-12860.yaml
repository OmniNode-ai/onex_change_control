---
schema_version: "1.0.0"
ticket_id: "OMN-12860"
title: "Infra lane overlay renders logical secret resolver config"
summary: >-
  Extends omnibase_infra runtime policy with dev and stability-test logical LLM secret references and
  concrete secret source descriptors. The policy renderer emits a typed ModelSecretResolverConfig payload/path,
  and the runtime entrypoint materializes that config under /app/data/delegation before runtime startup.
  Bifrost route authority remains on logical refs such as llm.openrouter.api_key; environment variables
  are concrete local secret source descriptors only.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-source-pr-1924"
    description: >-
      omnibase_infra PR #1924 carries the runtime policy, compose, entrypoint, and typed renderer changes
      for OMN-12860.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12860/dod-source-pr-1924/command.yaml"
  - id: "dod-source-pr-495"
    description: >-
      omninode_infra PR #495 carries the onex-dev k8s ConfigMap and runtime Deployment wiring required
      for OMN-12860 parity with the contract-rendered runtime secret resolver overlay.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12860/dod-source-pr-495/command.yaml"
  - id: "dod-focused-verification"
    description: >-
      Focused local renderer, runtime policy, env-anchor, integration policy, stability compose render,
      ruff, and whitespace checks passed for the changed infra surfaces.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12860/dod-focused-verification/command.yaml"
  - id: "dod-k8s-configmap-verification"
    description: >-
      Focused local k8s verification passed for the onex-dev runtime ConfigMap, Infisical-backed secret
      env refs, manifest YAML, and cross-repo env parity against the omnibase_infra compose anchor.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12860/dod-k8s-configmap-verification/command.yaml"
  - id: "dod-stability-compose-render"
    description: >-
      Stability-test docker compose render proves runtime services receive ONEX_SECRET_RESOLVER_CONFIG_PATH
      and logical secret resolver JSON from contract-rendered runtime policy values.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12860/dod-stability-compose-render/command.yaml"
  - id: "dod-policy-sync"
    description: >-
      docker/runtime-policy.env was regenerated from contracts/services/runtime_policy.contract.yaml and
      checked for sync.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12860/dod-policy-sync/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment records that dev/stability runtime must not be declared fixed until post-merge
      redeploy captures runtime identity and logical OpenRouter secret-ref resolution evidence without
      logging secret values.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12860/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-2387"
    description: >-
      onex_change_control PR #2387 carries the central OMN-12860 contract and receipt evidence paired
      with omnibase_infra PR #1924 and omninode_infra PR #495.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12860/dod-occ-pr-2387/command.yaml"
          && grep -q '^pr_number: 2387$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12860/dod-occ-pr-2387/command.yaml"
