---
schema_version: 1.0.0
ticket_id: OMN-9862
summary: Add Pydantic output models for change-aware test selection (ModelTestSelection, EnumFullSuiteReason)
  with full_suite_reason / smart-mode / matrix-length validation invariants in scripts/ci/test_selection_models.py
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'omnibase_core PR #927 for OMN-9862 CI checks pass (canonical implementation lives in
      omnibase_core scripts/ci/test_selection_models.py)'
    command: gh pr checks 927 --repo OmniNode-ai/omnibase_core --watch
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: "full_suite_reason required when is_full_suite=True: ModelTestSelection rejects payloads
      where is_full_suite=True without full_suite_reason via @model_validator(mode='after') (1 unit test
      test_full_suite_selection_serializes_with_reason in tests/unit/scripts/ci/test_test_selection_models.py)
      — receipt at drift/dod_receipts/OMN-9862/dod-001/"
    source: generated
    checks:
      - check_type: test_passes
        check_value: uv run pytest tests/unit/scripts/ci/test_test_selection_models.py::test_full_suite_selection_serializes_with_reason
          -q
  - id: dod-002
    description: "full_suite_reason forbidden when is_full_suite=False: ModelTestSelection rejects payloads
      where is_full_suite=False with a full_suite_reason set (1 unit test test_smart_selection_disallows_full_suite_reason
      in tests/unit/scripts/ci/test_test_selection_models.py) — receipt at drift/dod_receipts/OMN-9862/dod-002/"
    source: generated
    checks:
      - check_type: test_passes
        check_value: uv run pytest tests/unit/scripts/ci/test_test_selection_models.py::test_smart_selection_disallows_full_suite_reason
          -q
  - id: dod-003
    description: "matrix length must equal split_count: ModelTestSelection rejects payloads where len(matrix)
      != split_count via @model_validator(mode='after') (1 unit test test_matrix_length_matches_split_count
      in tests/unit/scripts/ci/test_test_selection_models.py) — receipt at drift/dod_receipts/OMN-9862/dod-003/"
    source: generated
    checks:
      - check_type: test_passes
        check_value: uv run pytest tests/unit/scripts/ci/test_test_selection_models.py::test_matrix_length_matches_split_count
          -q
