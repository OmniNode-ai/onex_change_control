---
schema_version: "1.0.0"
ticket_id: "OMN-15468"
title: "Bind OCC evidence for runtime_dispatch terminal event ingestion"
summary: >
  OCC contract for OmniNode-ai/omnibase_infra PR #2560. The product PR teaches local ingress route discovery
  to include runtime_dispatch.terminal_events so Pattern B waits subscribe to both success and failure
  terminals.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2560 CI checks green"
    command: "gh pr checks 2560 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2560"
    description: "PR #2560 on OmniNode-ai/omnibase_infra includes runtime_dispatch.terminal_events route
      discovery."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "python -c 'import base64, subprocess, sys; body = base64.b64decode(subprocess.check_output([\"gh\",
          \"api\", \"repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/runtime_local_ingress.py?ref=182cef9dd02fc7cc46ad66e4420b876861ae6af5\",
          \"--jq\", \".content\"])).decode(); sys.exit(0 if \"def _terminal_event_topics_from_declaration\"
          in body and \"runtime_dispatch.get(\\\"terminal_events\\\")\" in body else 1)'"
  - id: "occ-self-bind-pr-5579"
    description: "Binds OMN-15468 to OCC companion PR #5579 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5579 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2565"
    description: "PR #2565 corrects the unsourced '24 of the 51' terminal-declaration figure in _extract_terminal_events
      to provenance-stamped counts. Probes the product source at the exact PR head 712ccb070e93bb8de192d466b349c0e7afd6e4b8:
      the PROVENANCE stamp and the corpus sha must be PRESENT and the retracted figure ABSENT. Goes RED
      at the parent ref 6a0949fd55c7f54042849507bb42e2cab3adcc8d (executed both ways by the evidence author,
      2026-07-30)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/runtime_local_ingress.py?ref=712ccb070e93bb8de192d466b349c0e7afd6e4b8' --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF 'PROVENANCE — every number above is a measurement' && printf '%s' "$body" | grep -qF 'aea0c33dd89fb82fdca33aac7149992a21c46d43' && ! printf '%s' "$body" | grep -qF '24 of those 51 contracts'
  - id: "dod-omn-15468-pr-2565-test-comment-provenance"
    description: "The same correction on the second changed file. Probes tests/unit/runtime/test_runtime_local_ingress.py
      at PR #2565's exact head: the corpus sha and the corrected '30 of those 51' framing PRESENT, the
      retracted '24 of the 51' ABSENT. Goes RED at the parent ref."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/tests/unit/runtime/test_runtime_local_ingress.py?ref=712ccb070e93bb8de192d466b349c0e7afd6e4b8' --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF 'omnimarket@aea0c33dd89fb82fdca33aac7149992a21c46d43' && printf '%s' "$body" | grep -qF '30 of those 51 declare NO top-level' && ! printf '%s' "$body" | grep -qF '24 of the 51 contracts declare NO top-level'
  - id: "dod-omn-15468-pr-2565-scope-two-files"
    description: "Bounds the blast radius asserted by this evidence: PR #2565 changes EXACTLY the two
      files probed above and nothing else. Goes RED if a third file is pushed onto the branch."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          files="$(gh api repos/OmniNode-ai/omnibase_infra/pulls/2565/files --jq '.[].filename')" && printf '%s\n' "$files" | grep -qx 'src/omnibase_infra/runtime/runtime_local_ingress.py' && printf '%s\n' "$files" | grep -qx 'tests/unit/runtime/test_runtime_local_ingress.py' && test "$(printf '%s\n' "$files" | grep -c .)" -eq 2
  - id: "dod-omn-15468-pr-2565-head-binding"
    description: "Rule B literal binding: pins this evidence to PR #2565 specifically, at the head the
      content probes above read, on base dev. Goes RED on a force-push to a different head or a retarget."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 2565 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid,baseRefName --jq '.headRefOid == "712ccb070e93bb8de192d466b349c0e7afd6e4b8" and .baseRefName == "dev"' | grep -qx true
  - id: "occ-self-bind-pr-5585"
    description: "Binds OMN-15468 to OCC companion PR #5585 (self-bind), the independently authored evidence
      companion for omnibase_infra#2565."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5585 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "occ-self-bind-pr-5657"
    description: "Binds OMN-15468 to OCC companion PR #5657 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5657 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-5825"
    description: "Binds OMN-15468 to OCC companion PR #5825 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5825 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2606"
    description: "PR #2606 on OmniNode-ai/omnibase_infra routes a failed terminal verdict to the contract's
      declared failure terminal at head 072f57b836261c9cce9896c6e4ffd5dc58fc42ef."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 2606 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid,baseRefName --jq '.number == 2606 and .headRefOid == "072f57b836261c9cce9896c6e4ffd5dc58fc42ef" and .baseRefName == "dev"' | grep -qx true
  - id: "occ-self-bind-pr-5834"
    description: "Binds OMN-15468 to OCC companion repair PR #5834 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5834 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "occ-self-bind-pr-5842"
    description: "Binds OMN-15468 to OCC companion repair PR #5842 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 5842 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2790-verifier"
    description: >
      PR #2790 on OmniNode-ai/omnibase_infra -- payload-verdict downgrade fix in `_status_for_terminal_topic`
      (service_pattern_b_broker.py). Per the adversarial-receipts rule (implementer cannot self-author),
      this entry is VERIFIER-authored -- not by the build lane that made the change -- per this ticket's
      history of 3 prior merged-but-not-live-verified cycles, which is why a differential proof rather
      than a single-head content probe. Fetches the actual file content at BOTH the PR's merge-base (b99e1a9a90)
      and its head (d255bb7d91) via the GitHub contents API and asserts the new 3rd `payload: object =
      None` parameter on `_status_for_terminal_topic` is ABSENT at merge-base and PRESENT at head -- a
      real RED-before/GREEN-after differential on the function signature itself, not a single-point existence
      check. Independently confirmed by reading the PR diff directly (not asserted from its own description):
      the change threads `terminal.payload` through to a new payload-aware verdict read (`resolve_terminal_verdict`),
      backed by a new test file with an explicit backward-compatibility test for the 2-arg call shape.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          base_body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/service_pattern_b_broker.py?ref=b99e1a9a90dcf889ef4e77d02b52845b85bb3a8c"
          --jq .content | base64 -d)"; head_body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/service_pattern_b_broker.py?ref=d255bb7d91026c8ab15b52bc70f71da22d2a5e34"
          --jq .content | base64 -d)"; ! printf '%s' "$base_body" | grep -qF 'payload: object = None'
          && printf '%s' "$head_body" | grep -qF 'payload: object = None' && echo "OMN-15468-PR-2790-DIFFERENTIAL-PROBE-PASS:
          _status_for_terminal_topic's new payload parameter is absent at merge-base and present at head"
  - id: "occ-self-bind-pr-6705"
    description: "Binds OMN-15468 to OCC companion PR #6705 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6705 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3225"
    description: "PR #3225 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/contract_terminal_events.py?ref=0ae87c3711440e1438976c7f4f5c7b8207b43833 --jq '.content' | base64 -d | grep -c 'def declared_failure_terminal_topics'
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3225-ci"
    description: "PR #3225 on OmniNode-ai/omnibase_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 3225 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "occ-self-bind-pr-8326"
    description: "OCC companion PR #8326 — self-bind for OMN-15468 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8326 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-infra-3225-kernel-applier-differential"
    description: >
      omnibase_infra#3225 -- the AC2 residual #2606 and #2790 left open. Those two landed the failure-verdict
      guard and the payload-verdict status downgrade, but every applier built OUTSIDE handler_wiring lost
      both contract-derived routing inputs, and service_kernel hand-registered an applier for node_delegate_skill_orchestrator
      by NAME that took precedence over the contract-derived wiring -- so on the .201 dev lane the guard
      was structurally inert and 18 of the trailing 25 records on delegate-skill-completed.v1 carried
      status="failed" with a typed terminal_failure_cause. A REAL content differential, not a PR-state
      surrogate: it fetches service_kernel.py at BOTH the merge-base 724f01205ab1b70b39ab31d3744f76951da97d37
      and the PR head 0ae87c3711440e1438976c7f4f5c7b8207b43833 and asserts build_static_result_applier
      and failure_terminal_topics are ABSENT at base and PRESENT at head, while a direct DispatchResultApplier(
      construction is PRESENT at base (4 occurrences) and ABSENT at head. Executed both ways; this is
      the ticket's AC4 RED-before demonstration. Greps files rather than a pipe so an early-exiting grep
      cannot SIGPIPE the producer into a spurious exit 141.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/service_kernel.py?ref=724f01205ab1b70b39ab31d3744f76951da97d37' --jq .content | base64 -d > /tmp/omn15468_sk_base.py && gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/service_kernel.py?ref=0ae87c3711440e1438976c7f4f5c7b8207b43833' --jq .content | base64 -d > /tmp/omn15468_sk_head.py && test -s /tmp/omn15468_sk_base.py && test -s /tmp/omn15468_sk_head.py && test "$(grep -cF 'build_static_result_applier' /tmp/omn15468_sk_base.py)" -eq 0 && test "$(grep -cF 'build_static_result_applier' /tmp/omn15468_sk_head.py)" -gt 0 && test "$(grep -cF 'failure_terminal_topics' /tmp/omn15468_sk_base.py)" -eq 0 && test "$(grep -cF 'failure_terminal_topics' /tmp/omn15468_sk_head.py)" -gt 0 && test "$(grep -cF 'DispatchResultApplier(' /tmp/omn15468_sk_base.py)" -gt 0 && test "$(grep -cF 'DispatchResultApplier(' /tmp/omn15468_sk_head.py)" -eq 0
  - id: "dod-infra-3225-enforcement-gate"
    description: >
      Enforcement, not detection. The validator that makes the defect class unreachable -- no direct DispatchResultApplier(...)
      construction under src/ outside the factory module -- must exist at the PR head AND be wired as
      BOTH a pre-commit hook and a ci.yml step in the SAME change. Probes all three files at the head
      SHA. A validator that ships without its two wirings is advisory and gets ignored. Greps files rather
      than a pipe for the same SIGPIPE reason as the entry above.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/validators/contract_result_applier_construction.py?ref=0ae87c3711440e1438976c7f4f5c7b8207b43833' --jq .content | base64 -d > /tmp/omn15468_validator.py && gh api 'repos/OmniNode-ai/omnibase_infra/contents/.pre-commit-config.yaml?ref=0ae87c3711440e1438976c7f4f5c7b8207b43833' --jq .content | base64 -d > /tmp/omn15468_precommit.yaml && gh api 'repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/ci.yml?ref=0ae87c3711440e1438976c7f4f5c7b8207b43833' --jq .content | base64 -d > /tmp/omn15468_ci.yml && grep -qF 'def scan_source' /tmp/omn15468_validator.py && grep -qF 'id: contract-result-applier-construction' /tmp/omn15468_precommit.yaml && grep -qF 'omnibase_infra.validators.contract_result_applier_construction' /tmp/omn15468_ci.yml
  - id: "dod-omn-15468-pr-3225-behaviour-focused-suite"
    description: >
      BEHAVIOUR proof, executed rather than asserted. The focused suite drives the real factory, the real
      DispatchResultApplier, a real ModelEventEnvelope JSON round-trip and the real _status_for_terminal_topic
      over a fixture contract shaped exactly like node_delegate_skill_orchestrator. It asserts a map-miss
      failure verdict lands on the FAILURE terminal, that a success verdict still lands on completed (the
      same-contract positive control -- the guard corrects only the false-success direction), that a declared
      failure CLASS routes by class identity, that the re-routed record reads "failed" after the wire
      so ok reflects the verdict, and that build_static_result_applier REFUSES to default the failure-terminal
      answer. It also runs the enforcement gate over the real source tree with a positive control proving
      the scanner can find a violation. The suite lives in omnibase_infra, so it cannot be a hermetic
      command here (OMN-14051): the run is recorded in a committed receipt and the check reads that receipt,
      while a second probe pins the suite's actual content at the PR head so the receipt cannot outlive
      the tests it names. Recorded result: 9 passed at the merged tip 4346814ab.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-15468/dod-omn-15468-pr-3225-behaviour-focused-suite/command.yaml && grep -q 'evidence_item_id: "dod-omn-15468-pr-3225-behaviour-focused-suite"' drift/dod_receipts/OMN-15468/dod-omn-15468-pr-3225-behaviour-focused-suite/command.yaml && gh api 'repos/OmniNode-ai/omnibase_infra/contents/tests/unit/runtime/test_omn15468_kernel_applier_failure_terminal.py?ref=0ae87c3711440e1438976c7f4f5c7b8207b43833' --jq .content | base64 -d > /tmp/omn15468_suite.py && grep -qF 'def test_map_miss_failure_verdict_lands_on_the_failure_terminal' /tmp/omn15468_suite.py && grep -qF 'def test_success_verdict_still_lands_on_the_success_terminal' /tmp/omn15468_suite.py && grep -qF 'def test_failure_terminal_record_reads_as_failed_after_the_wire' /tmp/omn15468_suite.py && grep -qF 'def test_validator_positive_control' /tmp/omn15468_suite.py
  - id: "dod-15468-3225-live-lane-failure-routing"
    description: >
      The live readback AC2 is actually about, on the surface the defect was measured on: the .201 dev
      lane (compose project omnibase-infra, broker container omnibase-infra-redpanda), read-only rpk with
      no consumer group and no mutation. A CI runner has no route to that host, so the probe was run out
      of band and recorded in a committed receipt; this check reads the receipt. Three assertions were
      made, in order, each fail-closed. (1) ATTRIBUTION -- the lane's omninode-runtime image carries revision
      4346814ab3c0, the merge commit of omnibase_infra#3225; a readback off an unpatched lane proves nothing
      about the fix. (2) THE CLAIM -- among records on onex.evt.omnimarket.delegate-skill-completed.v1
      timestamped at or after the deploy (2026-09-06T03:22:07Z), ZERO carry a failure verdict; before
      the deploy that same query returned 18 of the trailing 25. (3) POSITIVE CONTROL -- over the SAME
      window the declared failure terminal carried 13 records the SAME classifier reads as failure verdicts,
      so an empty window or a classifier that never fires would have failed the probe rather than reporting
      a clean zero. An empty result is not evidence of absence. The receipt also records the STATED LIMIT:
      the success terminal took zero records of any kind in that window, so the zero rests on the positive
      control and on the focused suite's same-contract success case rather than on a live same-contract
      success, which could not be produced for a reason unrelated to this fix (OMN-17979).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-15468/dod-15468-3225-live-lane-failure-routing/command.yaml && grep -q 'evidence_item_id: "dod-15468-3225-live-lane-failure-routing"' drift/dod_receipts/OMN-15468/dod-15468-3225-live-lane-failure-routing/command.yaml && grep -qF '4346814ab3c0' drift/dod_receipts/OMN-15468/dod-15468-3225-live-lane-failure-routing/command.yaml && grep -qF 'post-deploy completed records: 0 with failure verdict: 0' drift/dod_receipts/OMN-15468/dod-15468-3225-live-lane-failure-routing/command.yaml && grep -qF 'post-deploy failed-terminal records: 13 classified as failure verdict: 13' drift/dod_receipts/OMN-15468/dod-15468-3225-live-lane-failure-routing/command.yaml
  - id: "dod-15468-supersedes-occ-self-bind-6705"
    description: >
      SUPERSESSION, add-only, for the merged entry occ-self-bind-pr-6705. That entry asserts head-branch
      state on jonah/omn-15468-verifier-companion, a branch whose PR read MERGED 2026-08-19T04:21:07Z
      at a35f50cde7ea40217ec12b5ef0f8e9d18bb8a12d. Head-branch check runs are deleted along with the branch,
      so the entry fails for a reason that is not a product defect and can never become true again --
      it was the single failed row in the 2026-09-05T20:54Z dod_verify run. Merged entries are immutable
      (the OCC append-only gate), so it is not edited: this net-new entry asserts the durable terminal
      fact in its place. Goes RED if PR #6705 is reverted, reopened or renumbered.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 6705 --repo OmniNode-ai/onex_change_control --json number,state,mergeCommit --jq '.number == 6705 and .state == "MERGED" and (.mergeCommit.oid | startswith("a35f50cde7ea40217ec12b5ef0f8e9d18bb8a12d"))' | grep -qx true
  - id: "occ-self-bind-pr-8342"
    description: >
      OCC self-binding entry for PR #8342, the in-repo evidence PR carrying the four omnibase_infra#3225
      evidence items and the add-only supersession of occ-self-bind-pr-6705. Binds OMN-15468 to this OCC
      PR by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself (OMN-14650).
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 8342 --repo OmniNode-ai/onex_change_control --json number,state,headRefName --jq '.number == 8342 and .headRefName == "jonah/omn-15468-kernel-applier-companion"' | grep -qx true
  - id: "dod-15468-3225-behavior-proof"
    description: >
      The BEHAVIOUR item this contract lacked (OMN-17943 class). Every other entry here reads content
      at a pinned ref, reads a PR's state, or greps a receipt -- under the OMN-16821 proof-class taxonomy
      all three are MERGE_STATE or SURROGATE, statements about GitHub or about bytes, never an execution
      of the claimed behaviour -- so behavior_proving_count sat at 0 and no automated Done flip could
      be released. This one runs the ticket's own focused suite against the product source: a map-miss
      failure verdict must land on the declared FAILURE terminal, a success verdict must still land on
      completed (the same-contract positive control), a declared failure CLASS must route by class identity,
      the re-routed record must read "failed" after a real ModelEventEnvelope wire round-trip, build_static_result_applier
      must REFUSE to default the failure-terminal answer, and the enforcement scanner must find zero direct
      DispatchResultApplier constructions in the real source tree while its own positive control proves
      it can still find one. Recorded result at the merge commit of omnibase_infra#3225 (4346814ab3c0c1f591cb15c62270848dba28d6eb):
      9 passed. execution_scope is local_done_gate (OMN-15392) because the suite lives in omnibase_infra:
      the hosted gate checks out one product repo and correctly refuses to reroute a command into a directory
      it does not have, so the local Done gate owns this item and the hosted gate records it NOT-EVALUATED
      rather than guessing.
    source: "manual"
    execution_scope: "local_done_gate"
    checks:
      - check_type: "test_passes"
        check_value: |-
          repos/OmniNode-ai/omnibase_infra/commits/4346814ab3c0c1f591cb15c62270848dba28d6eb
          uv run pytest tests/unit/runtime/test_omn15468_kernel_applier_failure_terminal.py -q
  - id: "dod-15468-6705-merged-terminal-fact"
    description: >
      Contract-level supersession of occ-self-bind-pr-6705, which asserts head-branch state on jonah/omn-15468-verifier-companion
      -- a branch deleted when its PR merged 2026-08-19T04:21:07Z. dod_verify derives a ::pr-live-state
      probe from that entry's literal `gh pr view 6705`, and that probe reports "required checks not green
      (occ-preflight / eligibility)" against a branch whose check runs no longer exist. It is unfixable
      by re-running anything and it is not a product defect, so it held the whole ticket's verdict at
      FAILED. A receipt-level supersede cannot cure it because the live-state binding is derived from
      the CONTRACT entry, not the receipt; only the supersedes_dod_evidence marker retires the entry so
      its checks are not executed and no ::pr-live-state check is appended. This entry asserts the same
      durable fact by a probe that names no PR: the merge commit exists in onex_change_control.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-6705"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/commits/a35f50cde7ea40217ec12b5ef0f8e9d18bb8a12d --jq .sha > /tmp/omn15468_6705_merge.txt && grep -qx a35f50cde7ea40217ec12b5ef0f8e9d18bb8a12d /tmp/omn15468_6705_merge.txt
  - id: "dod-15468-6705-ancestry-in-dev"
    description: >
      Contract-level supersession of dod-15468-supersedes-occ-self-bind-6705, the previous attempt at
      the repair above. That entry asserted the right fact but did it with `gh pr view 6705`, so dod_verify
      derived the SAME ::pr-live-state probe from it and it failed for the SAME reason as the entry it
      was meant to replace -- an add-only correction that inherited the defect it was correcting. Retired
      here in favour of a probe that names no PR and asserts something strictly stronger than existence:
      the merge commit of onex_change_control#6705 is an ANCESTOR of dev, so the evidence it carried is
      actually in the shipped history rather than merely reachable.
    source: "manual"
    evidence_artifact: "supersedes_dod_evidence:dod-15468-supersedes-occ-self-bind-6705"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/compare/a35f50cde7ea40217ec12b5ef0f8e9d18bb8a12d...dev --jq .status > /tmp/omn15468_6705_ancestry.txt && grep -qx ahead /tmp/omn15468_6705_ancestry.txt
  - id: "occ-self-bind-pr-8366"
    description: >
      OCC self-binding entry for PR #8366, the in-repo evidence PR carrying the behaviour proof and the
      two contract-level supersessions. Binds OMN-15468 to this OCC PR by pr_number so eligibility can
      resolve a PASS receipt for the evidence PR itself (OMN-14650). Asserts the head branch as well as
      the number, so a renumbered or retargeted PR goes RED rather than passing on the number alone.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: |-
          gh pr view 8366 --repo OmniNode-ai/onex_change_control --json number,state,headRefName --jq '.number == 8366 and .headRefName == "jonah/omn-15468-behavior-proof-and-supersede"' | grep -qx true
