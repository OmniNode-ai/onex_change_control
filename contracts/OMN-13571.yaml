---
schema_version: "1.0.0"
ticket_id: "OMN-13571"
title: "Doc-sanitization cleanup: remove OMN-XXXX Linear ticket references from public-repo docs"
summary: >-
  Per-repo documentation cleanup that removes Linear ticket references (OMN-XXXX) from tracked documentation
  files in the public OmniNode repos. onex_change_control is EXEMPT from this target because it is the
  record-keeping repo and intentionally retains ticket references. Driven by deterministic git grep over
  tracked files only. Removal scope across the other public repos is TOTAL 3,812 OMN occurrences in 416
  files, plus 48 tracked paths whose filename or a parent directory contains OMN-XXXX — those need renaming
  plus inbound-link fixes. Strategy options recorded in the audit INDEX: strip-token-only vs context-aware
  rewrite vs hybrid. This contract is the central Receipt-Gate Evidence-Source that the per-repo cleanup
  PRs cite so their verify/verify Receipt Gate resolves against an OCC-main ancestor. Documentation-only
  cleanup; no code, no topic schema, no envelope, no contract API change, no deploy.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "docs"
    description: >-
      The sanitization sweep that scoped this cleanup is recorded under the audit directory in omni_home:
      docs/audits/2026-06-24-public-repo-doc-sanitization-sweep/ (INDEX.md plus 14 per-repo reports).
      The INDEX records the deterministic git-grep ground truth for OMN-XXXX occurrences per repo (removal-scope
      TOTAL 3,812 across 416 files, 48 OMN-named paths; onex_change_control 5,279 occurrences EXEMPT)
      and the agreed rewrite strategy options.
    command: "ls docs/audits/2026-06-24-public-repo-doc-sanitization-sweep/"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-evidence-source"
    description: >-
      This OCC contract is the central Receipt-Gate Evidence-Source for the OMN-13571 OMN-XXXX doc-sanitization
      cleanup (epic OMN-13567). The per-repo cleanup PRs cite Evidence-Source: OCC#<this-pr> (post-merge
      OCC-main SHA) so their verify/verify Receipt Gate resolves against an OCC-main ancestor. The cleanup
      is documentation-only (OMN-XXXX token removal / file renames in tracked docs of non-exempt public
      repos); no code, no deploy, no contract API change.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13571/dod-occ-evidence-source/command.yaml"
          && grep -q '^ticket_id: "OMN-13571"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13571/dod-occ-evidence-source/command.yaml"
