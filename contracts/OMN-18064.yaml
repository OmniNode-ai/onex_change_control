---
schema_version: "1.0.0"
ticket_id: "OMN-18064"
title: "Autobind OCC evidence for OMN-18064"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #1260.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1260 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1260 on OmniNode-ai/omninode_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omninode_infra`.
    command: |-
      uv run pytest tests/k8s/test_deploy_onex_staging_delivered_digest_carrier_omn18064.py tests/k8s/test_deploy_onex_staging_workflow.py tests/scripts/test_evaluate_staging_green_bar.py tests/scripts/test_staging_green_bar_leg3_real_runs.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1260"
    description: "PR #1260 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1260 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1260/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/deploy-onex-staging.yml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #1260 on OmniNode-ai/omninode_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1260"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/k8s/test_deploy_onex_staging_delivered_digest_carrier_omn18064.py tests/k8s/test_deploy_onex_staging_workflow.py tests/scripts/test_evaluate_staging_green_bar.py tests/scripts/test_staging_green_bar_leg3_real_runs.py -q
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1263"
    description: >
      PR #1263 on OmniNode-ai/omninode_infra — the follow-up that pins the leg-3 RED-before proof to commit
      331f26ba instead of the moving origin/dev ref. Bound by hand (union-resolve): the autobind had already
      minted this ticket's companion for PR #1260, so a second command for the same ticket produced no
      companion and PR #1263 had no receipt to cite.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1263 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/scripts/test_staging_green_bar_leg3_real_runs.py -q
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "occ-self-bind-pr-8749"
    description: >-
      OCC self-binding receipt for PR #8749, the in-repo evidence PR carrying this contract. Binds OMN-18064
      to the OCC PR by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8749 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "occ-self-bind-pr-8742"
    description: "Binds OMN-18064 to OCC companion PR #8742 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8742 --repo OmniNode-ai/onex_change_control --json number,state"
