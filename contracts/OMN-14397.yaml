---
schema_version: "1.0.0"
ticket_id: "OMN-14397"
title: "fix(OMN-14397): onex delegate correlation_id + hard SIGALRM timeout backstop"
summary: >-
  omnibase_infra PR #2265 mints a fresh correlation_id per onex delegate invocation and adds a SIGALRM-based
  hard timeout backstop so a hung Kafka-bus response listener stuck in non-cooperative blocking I/O no
  longer outlives its own --timeout and requires a manual kill (observed on .201). Round-2 remediation
  (commit 3e5e86d8) closes a defect found in independent verification: the backstop's DelegateTimeoutExceededError
  now subclasses BaseException instead of RuntimeError so it cannot be silently swallowed by run_receipt_mode's
  own broad except Exception around the exact call that hangs.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2265-head-3e5e86d8"
    description: >-
      Independent adversarial verification of omnibase_infra PR #2265 at head 3e5e86d890cc624860906f0fd32fdf5e23589084
      (round 2, post-remediation). Round 1 found a real DEFECT: DelegateTimeoutExceededError(RuntimeError)
      was silently swallowed by run_receipt_mode's broad except Exception around the exact call that hangs
      (receipt_mode.py:509-526), so the documented clear-stderr / exit-1 contract never fired for the
      real hang scenario. Round 2 remediation (BaseException base class) was independently reproduced:
      reverting the base class back to RuntimeError produced the exact predicted RED signature (empty
      stderr, the accidental-exit-code fingerprint); restoring BaseException produced clean GREEN. Empirically
      confirmed no BaseException escape path exists anywhere on the call path (cli_delegate.py, receipt_mode.py,
      and the venv-resolved omnibase_core runtime_local.py all audited for bare/broad except clauses),
      that finally-block cleanup (signal.alarm(0) + handler restore) runs under BaseException unwind,
      and that issubclass(DelegateTimeoutExceededError, Exception) is False. Also independently reproduced
      the round-1 behavioral RED for both original fixes (correlation_id payload write, SIGALRM backstop)
      via targeted reverts. Full tests/unit/cli/ suite: 335 passed, 15 pre-existing unrelated skips (missing
      omnimarket module). ruff check, ruff format --check, and mypy clean on both touched files.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14397/dod-omnibase-infra-pr-2265-head-3e5e86d8/command.yaml
          && grep -q '^pr_number: 2265$' drift/dod_receipts/OMN-14397/dod-omnibase-infra-pr-2265-head-3e5e86d8/command.yaml
          && grep -q '3e5e86d890cc624860906f0fd32fdf5e23589084' drift/dod_receipts/OMN-14397/dod-omnibase-infra-pr-2265-head-3e5e86d8/command.yaml
  - id: "dod-deploy-assessment-head-3e5e86d8"
    description: >-
      Deploy-surface assessment for omnibase_infra PR #2265 head 3e5e86d890cc624860906f0fd32fdf5e23589084:
      the diff is limited to a single CLI module (src/omnibase_infra/cli/cli_delegate.py) and its focused
      unit test (tests/unit/cli/test_cli_delegate.py). It does not register a new service deployment,
      mutate runtime configuration, perform a deploy or restart, change topics, add a migration, or touch
      .201.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14397/dod-deploy-assessment-head-3e5e86d8/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14397/dod-deploy-assessment-head-3e5e86d8/command.yaml
  - id: "dod-occ-self-commit"
    description: >-
      OCC self-binding: this onex_change_control branch carries the OMN-14397 central contract and PASS
      receipts for omnibase_infra PR #2265 head 3e5e86d890cc624860906f0fd32fdf5e23589084. The receipt
      binds the first evidence commit for occ-preflight eligibility before the OCC PR number exists.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14397/dod-occ-self-commit/command.yaml && grep
          -q 'jonah/omn-14397-occ-pr2265' drift/dod_receipts/OMN-14397/dod-occ-self-commit/command.yaml
  - id: "dod-omnibase-infra-pr-2265-head-2aaf65f0"
    description: >-
      Merge-sweep repair verification for omnibase_infra PR #2265 at head 2aaf65f09ea9503ae824f5cebe0da08fde473276.
      After OCC#3973 unblocked receipt gates, the rerun batch surfaced a real skill-node-mapping-sync
      failure: skill_mapping.yaml still mapped auto_merge to the legacy node_auto_merge_effect even though
      OMN-14151 deliberately deregistered that catalog entry in omnimarket and replaced the direct arm
      path with the merge-queue governor. The product repair removes the stale auto_merge skill mapping
      and its registry expectation instead of silently retargeting to the governor with incompatible payload
      semantics. Local proof: the exact failed CI selector passed with OMNIMARKET_SRC pointed at the canonical
      sibling omnimarket checkout; the full tests/unit/cli/test_cli_skill.py file passed (31 passed, 15
      pre-existing omnimarket import skips); ruff check/format on the Python test file passed; skill_mapping.yaml
      parsed as YAML; git diff --check passed; and the product pre-commit suite passed with OMNI_HOME
      and OMNIMARKET_SRC exported so the node migration sync hook used the same sibling catalog as CI.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14397/dod-omnibase-infra-pr-2265-head-2aaf65f0/command.yaml
          && grep -q '^pr_number: 2265$' drift/dod_receipts/OMN-14397/dod-omnibase-infra-pr-2265-head-2aaf65f0/command.yaml
          && grep -q '2aaf65f09ea9503ae824f5cebe0da08fde473276' drift/dod_receipts/OMN-14397/dod-omnibase-infra-pr-2265-head-2aaf65f0/command.yaml
  - id: "dod-deploy-assessment-head-2aaf65f0"
    description: >-
      Deploy-surface assessment for omnibase_infra PR #2265 head 2aaf65f09ea9503ae824f5cebe0da08fde473276:
      the repair changes only src/omnibase_infra/cli/skill_mapping.yaml and tests/unit/cli/test_cli_skill.py
      to remove a stale CLI skill mapping to a deregistered omnimarket node. It does not register a new
      service deployment, mutate runtime configuration, perform a deploy or restart, change topics, add
      a migration, or touch .201.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14397/dod-deploy-assessment-head-2aaf65f0/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14397/dod-deploy-assessment-head-2aaf65f0/command.yaml
