---
schema_version: "1.0.0"
ticket_id: "OMN-13076"
title: "feat(omnimarket): projection contract for baselines.quality (NC-03)"
summary: >
  Creates node_projection_baselines_quality in omnimarket — a reducer node that consumes onex.evt.omnibase-infra.baselines-computed.v1
  and projects quality signal aggregates into the baselines_quality_snapshots table. The table is exposed
  via the projection API as onex.snapshot.projection.baselines.quality.v1, resolving the missing snapshot
  topic declared by the omnidash quality-baseline-panel component registry. Aggregates: patterns_compared,
  patterns_recommended, high_confidence_count, medium_confidence_count, low_confidence_count, quality_score
  (weighted avg: high=1.0/medium=0.5/low=0.25 per comparison), recommend_rate (patterns_recommended /
  max(1, patterns_compared)).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >
      11 unit tests covering: snapshot row fields, confidence tier counts, quality_score weighted average,
      recommend_rate ratio, patterns counts, empty event zero-values, upsert idempotency, contract subscribe
      topic, contract projection_api snapshot topic, contract node_type, and handle() shim delegation.
    command: "uv run pytest tests/nodes/node_projection_baselines_quality/ -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-projection-baselines-quality"
    description: >
      omnimarket PR implementing node_projection_baselines_quality: contract.yaml, handler_projection_baselines_quality.py,
      migrations/001_create_baselines_quality_snapshots.sql, metadata.yaml, node.py, tests/nodes/node_projection_baselines_quality/test_projection_baselines_quality.py,
      tests/test_golden_chain_projection_baselines_quality.py. 11/11 unit tests + 4/4 golden-chain tests
      pass; ruff format+check, mypy --strict, pre-commit all clean; dogfood inventory count updated 333->334.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/nodes/node_projection_baselines_quality/ -v --tb=short 2>&1
          | grep '11 passed'"
  - id: "dod-occ-pr-self"
    description: >
      Self-binding OCC eligibility receipt for the dev-targeted evidence PR. This onex_change_control
      PR carries the OMN-13076 contract onto dev, satisfying the receipt-to-PR binding required by validator_occ_merge_eligibility.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^status: \"?PASS\"?$' drift/dod_receipts/OMN-13076/dod-occ-pr-self/command.yaml
          && grep -Eq '^pr_number: [0-9]+$' drift/dod_receipts/OMN-13076/dod-occ-pr-self/command.yaml"
