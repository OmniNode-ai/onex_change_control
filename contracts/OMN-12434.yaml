---
schema_version: "1.0.0"
ticket_id: "OMN-12434"
title: "Surface SEA model registry drift"
summary: >
  Fixes the self-extending agent model registry drift by aligning served model identifiers with the live
  runtime model endpoints and classifying endpoint mismatches as configuration drift.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "onex-self-extending-agent PR #166 carries the registry drift fix and focused validation."
    command: "See drift/dod_receipts/OMN-12434/dod-sea-pr-166/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carrying this contract binds the central evidence publication."
    command: "See drift/dod_receipts/OMN-12434/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-166"
    description: "onex-self-extending-agent PR #166 updates the model registry drift handling and tests."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12434/dod-sea-pr-166/command.yaml
          && grep -q '^pr_number: 166$' drift/dod_receipts/OMN-12434/dod-sea-pr-166/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control publishes the OMN-12434 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12434/dod-occ-pr/command.yaml"
