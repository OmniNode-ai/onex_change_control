---
schema_version: "1.0.0"
ticket_id: "OMN-12434"
title: "Surface SEA model registry drift"
summary: >
  Corrects the self-extending agent model registry drift by aligning served model identifiers with the
  live vLLM runtime endpoints on .201 (schema 1.0.0 -> 2.0.0, registry renamed entropy_experiment_models
  -> onex_model_registry, IDs corrected to Qwen3.6-35B-A3B / Qwen3.6-27B-MTP-IQ4_XS.gguf / deepseek-v4-flash).
  15/15 unit tests pass. Replaces closed PR #166 (orphaned by rebase).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "onex-self-extending-agent PR #183 carries the registry correction and 15/15 passing
      tests."
    command: "See drift/dod_receipts/OMN-12434/dod-sea-pr-183/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carrying this contract binds the central evidence publication."
    command: "See drift/dod_receipts/OMN-12434/dod-occ-pr-self-183/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-sea-pr-183"
    description: >-
      onex-self-extending-agent PR #183 corrects model registry served_model_id values to match live vLLM
      endpoints; 15/15 unit tests pass; lint/typecheck/security all green.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12434/dod-sea-pr-183/command.yaml && grep -q
          '^pr_number: 183$' drift/dod_receipts/OMN-12434/dod-sea-pr-183/command.yaml && grep -q '^commit_sha:
          "7abd4b7b7a6d9ee423b9aeaef868a9f29ba0ed43"$' drift/dod_receipts/OMN-12434/dod-sea-pr-183/command.yaml
  - id: "dod-occ-pr-self-183"
    description: "OCC PR carrying this OMN-12434 contract and receipts (self-binding)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12434/dod-occ-pr-self-183/command.yaml
