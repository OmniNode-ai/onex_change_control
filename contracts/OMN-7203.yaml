---
schema_version: "1.0.0"
ticket_id: OMN-7203
title: 'Task 2: Run AST crawl on remaining repos'
summary: "Task 2: Run AST crawl on remaining repos"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against main branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view --json state -q .state"
  - id: "dod-002"
    description: "PR merged to main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view --json mergedAt -q .mergedAt"
  - id: "dod-003"
    description: "Tests pass in CI"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr checks --watch"
  - id: "dod-004"
    description: "Pre-commit hooks clean"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "pre-commit run --all-files"
  - id: "dod-occ-pr-724"
    description: "OCC PR #724 self-binding receipt exists for the OMN-7203 evidence PR"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7203/dod-occ-pr-724/command.yaml\"
          && grep -q '^evidence_item_id: dod-occ-pr-724$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7203/dod-occ-pr-724/command.yaml\"
          && grep -q '^ticket_id: OMN-7203$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7203/dod-occ-pr-724/command.yaml\"
          && grep -q '^pr_number: 724$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7203/dod-occ-pr-724/command.yaml\"
          && gh pr view 724 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path' |
          grep -Fx 'contracts/OMN-7203.yaml' && gh pr view 724 --repo OmniNode-ai/onex_change_control
          --json files --jq '.files[].path' | grep -Fx 'drift/dod_receipts/OMN-7203/dod-occ-pr-724/command.yaml'"
