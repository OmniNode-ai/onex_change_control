---
schema_version: "1.0.0"
ticket_id: OMN-10793
title: "fix(OMN-10793): golden chain + projection — tokens_to_compliance + compliance_attempts"
summary: >
  Wave 2 / Task 6 of the tokens-to-compliance epic. Extends the delegation golden chain's expected_fields
  with tokens_to_compliance and compliance_attempts and threads them through HandlerProjectionDelegation
  so values from the ModelTaskDelegatedEvent payload land in the delegation_events row instead of being
  dropped by the model's extra="ignore" gate. omniclaude's sqlite_adapter already wires both columns into
  the INSERT/UPSERT statement (OMN-10789), so Task 6 is one omnimarket PR with no omniclaude change needed.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-unit-tests
    description: "All 22 tests across handler_projection_delegation and golden_chain_sweep_registry pass
      on omnimarket branch."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10793/dod-unit-tests/command.yaml
          && grep -q '^ticket_id: OMN-10793$' drift/dod_receipts/OMN-10793/dod-unit-tests/command.yaml"
  - id: dod-occ-pr
    description: "Self-binding receipt for this OCC PR."
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10793/dod-occ-pr/command.yaml &&
          grep -q '^ticket_id: OMN-10793$' drift/dod_receipts/OMN-10793/dod-occ-pr/command.yaml"
