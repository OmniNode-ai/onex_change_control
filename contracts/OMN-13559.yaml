---
schema_version: "1.0.0"
ticket_id: "OMN-13559"
title: "refactor(OMN-13559): env→contract+overlay migration Wave 1 — omnibase_core Postgres doctor-probe
  endpoint"
summary: >-
  OCC evidence for epic OMN-13556 Wave 1 (endpoints, OVERLAY seam) in omnibase_core. The source PR migrates
  the Postgres doctor probe (src/omnibase_core/doctor/checks/check_postgres.py) off direct os.environ["POSTGRES_HOST"]
  / os.environ["POSTGRES_PORT"] reads. The endpoint now resolves through a contract-declared reference
  (${env.POSTGRES_HOST} / ${env.POSTGRES_PORT}) expanded by a new core-resident sanctioned overlay surface
  (omnibase_core.overlays.contract_env_ref.expand_contract_env_refs, the core-layer mirror of omnibase_infra.runtime.overlay.contract_env_ref
  — core cannot import infra under the compat→core→spi→infra layering), wrapped in a frozen Pydantic config
  model (ModelPostgresProbeConfig.from_overlay) that fails CLOSED with a ValueError when the active overlay
  does not bind the var (no silent localhost default). Adds resolution-equivalence tests proving the overlay
  resolves the same dev/stability/prod value the prior env read returned, plus fail-closed coverage. Coherent
  subset of the 13 Wave-1 endpoint candidates: the KAFKA_BOOTSTRAP_SERVERS / DB-connection mega-units
  and the ${env_prefix} dynamic-key constructors are deferred to later w1 increments; LLM_*_URL is Bifrost-owned
  (OMN-12803/12815) and not touched.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13559.yaml consumed
      by the omnibase_core feature-PR Receipt Gate via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13559\"$' contracts/OMN-13559.yaml"
  - id: "dod-omnibase-core-pr"
    description: >-
      Binds the omnibase_core feature PR: Postgres doctor-probe endpoint resolves via the sanctioned core-resident
      overlay boundary (${env.POSTGRES_HOST}/${env.POSTGRES_PORT}), fail-closed, no direct os.environ
      read. url-authority + contract-config-compliance + no-env-fallbacks gates PASS; resolution-equivalence
      + fail-closed tests added and green.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13559\"$' contracts/OMN-13559.yaml"
