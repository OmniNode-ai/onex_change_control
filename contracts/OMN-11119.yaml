---
schema_version: "1.0.0"
ticket_id: OMN-11119
title: "fix(ci): extend check-name validator to handle workflow_call composite check names"
summary: >
  Extends validate-check-names.py in omnibase_core to detect workflow_call jobs (uses: references) and
  validate that required-checks.yaml gate check_names use the composite form <caller-job-key> / <callee-job-name>
  that GitHub generates for reusable-workflow check runs. Binds omnibase_core PR #1094.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Implementation PR #1094 open against omnibase_core main with 7 new unit tests"
    command: "gh pr view 1094 --repo OmniNode-ai/omnibase_core"
  - kind: "manual"
    description: "Validator correctly detects workflow_call composite names and emits WORKFLOW_CALL_COMPOSITE_NAME_MISMATCH"
    command: "gh pr view 1094 --repo OmniNode-ai/omnibase_core --json headRefOid,state"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ci-pass"
    description: "Implementation PR #1094 is open for OMN-11119 against omnibase_core main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11119/dod-ci-pass/command.yaml &&
          grep -q '^ticket_id: OMN-11119$' drift/dod_receipts/OMN-11119/dod-ci-pass/command.yaml && grep
          -q '^pr_number: 1094$' drift/dod_receipts/OMN-11119/dod-ci-pass/command.yaml"
  - id: "dod-occ-pr-1089"
    description: "onex_change_control PR #1089 carries the OMN-11119 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11119/dod-occ-pr-1089/command.yaml
          && grep -q '^pr_number: 1089$' drift/dod_receipts/OMN-11119/dod-occ-pr-1089/command.yaml"
