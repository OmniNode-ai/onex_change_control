---
schema_version: "1.0.0"
ticket_id: "OMN-15051"
title: "Autobind OCC evidence for OMN-15051"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1888."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1888 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1888"
    description: "PR #1888 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1888-ci"
    description: "PR #1888 on OmniNode-ai/omnimarket — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4766"
    description: "OCC companion PR #4766 — self-bind for OMN-15051 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omn15051-pr1888-deploy"
    description: "PR #1888 deploy disposition — runtime-path change (node_delegation_orchestrator handler
      + contract.yaml removing the dead-end baseline-comparison-request producer) carries an explicit
      post-merge dev deploy validation bar."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1888 --repo OmniNode-ai/omnimarket --json body,files --jq 'if ((.body
          | test(\"Post-merge dev deploy validation\")) and (.body | test(\"node_delegation_orchestrator\"))
          and any(.files[].path; . == \"src/omnimarket/nodes/node_delegation_orchestrator/handlers/handler_delegation_workflow.py\"))
          then \"deploy disposition verified for OMN-15051 delegation orchestrator dead-end producer removal\"
          else error(\"OMN-15051 deploy disposition missing PR body bar or runtime path\") end'"
  - id: "occ-self-bind-pr-4783"
    description: "OCC companion PR #4783 — self-bind for OMN-15051 deploy disposition."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4783 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
  - id: "dod-omnimarket-pr-1888-deploy-gate"
    description: >-
      deploy-gate (OMN-8912) falsifiability evidence for PR #1888. OMN-15051 removes a dead-end producer:
      the ModelBaselineIntent event onto onex.cmd.*.baseline-comparison-request.v1 had zero Kafka consumers
      (contract-topic-graph ORPHANED_PRODUCER). The fix deletes models/model_baseline_intent.py outright
      and drops the import/emission from handler_delegation_workflow.py. No new runtime surface, container,
      or topic is introduced, so no live redeploy is required. The check below is a live, falsifiable
      probe (gh api against GitHub content, not a self-referential read of this repo's own dod_receipts/contract
      tree): it confirms the deleted model file is genuinely ABSENT at PR #1888's pinned head SHA (3ad56132ad3b547fb9cfe6042b6d67587178c069)
      -- gh api 404s on a deleted path, so the negated check exits 0 only when the file is truly gone.
      It goes RED (non-zero) if the file still exists at that SHA.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "! gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_delegation_orchestrator/models/model_baseline_intent.py?ref=3ad56132ad3b547fb9cfe6042b6d67587178c069"
