---
schema_version: "1.0.0"
ticket_id: "OMN-7777"
title: "Remove dead DUAL_PUBLISH_LEGACY_TOPICS migration flag from omniclaude"
summary: >
  Audit and remove the DUAL_PUBLISH_LEGACY_TOPICS migration flag introduced during OMN-2367 topic convention
  rename. Flag defaulted False, legacy topic omninode.intelligence.code-analysis.requested.v1 has zero
  consumers across all repos. Removes: dual_publish_legacy_topics settings field, TOPIC_REQUEST_LEGACY
  constant, dual-publish conditional branch in request_code_analysis(), 3 test classes covering removed
  code, and CLAUDE.md env var table row.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "manual"
    description: "omniclaude PR #1632 carries the dead flag removal at the recorded head SHA."
    command: "gh pr view 1632 --repo OmniNode-ai/omniclaude --json number,url,headRefOid,headRefName,title,state,baseRefName"
  - kind: "ci"
    description: "Pre-commit and mypy pass at the omniclaude PR head."
    command: "gh pr checks 1632 --repo OmniNode-ai/omniclaude"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1632-open"
    description: "omniclaude PR #1632 carries the dead flag removal branch at the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7777/dod-pr-1632-open/command.yaml"
          && grep -q '^pr_number: 1632$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7777/dod-pr-1632-open/command.yaml"
          && grep -q '^commit_sha: "98d3c738a0e54e3db870dd05566614e363c399b5"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7777/dod-pr-1632-open/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-7777 contract evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-7777/dod-occ-self/command.yaml"
