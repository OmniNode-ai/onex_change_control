---
schema_version: "1.0.0"
ticket_id: "OMN-10490"
title: "Contract-driven projection API topic discovery"
summary: "Replace the hardcoded omnimarket projection API topic whitelist with contract-driven discovery
  from projection node contracts, including explicit exposure metadata, immutable startup topic maps,
  and degraded responses for absent backing tables."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "topics"
evidence_requirements:
  - kind: "manual"
    description: "Focused projection discovery and projection API unit tests pass on omnimarket PR #516"
    command: "uv run pytest tests/unit/projection/ tests/test_projection_api_server.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused projection discovery and projection API unit tests pass on omnimarket PR #516"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10490/dod-001/command.yaml\"
          && grep -q '^evidence_item_id: \"dod-001\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10490/dod-001/command.yaml\"
          && grep -q '^ticket_id: \"OMN-10490\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10490/dod-001/command.yaml\"
          && grep -q '^pr_number: 516$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10490/dod-001/command.yaml\""
