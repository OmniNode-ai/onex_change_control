---
schema_version: "1.0.0"
ticket_id: "OMN-11501"
title: "Unit tests for ToolRegistry and tool functions"
summary: >-
  onex-self-extending-agent PR #81 adds unit tests that verify ToolRegistry behavior and the register_generated_tool
  / invoke_generated_tool ADK tool functions.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: tests
    description: "All unit tests in PR #81 pass."
    command: "uv run pytest tests/unit -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-81"
    description: "onex-self-extending-agent PR #81 carries the unit test suite."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11501/dod-product-pr-81/command.yaml"
          && grep -q '^pr_number: 81$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11501/dod-product-pr-81/command.yaml"
  - id: "dod-unit-tests"
    description: "Full unit test suite passes."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11501/dod-unit-tests/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11501 evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11501/dod-occ-self/command.yaml"
