---
schema_version: "1.0.0"
ticket_id: "OMN-12445"
title: "fix(OMN-12445): unique CI concurrency group per merge_group batch commit"
summary: >-
  Fixes the omnibase_core dev merge queue deadlock where merge_group CI runs shared the same concurrency
  group by base ref and cancelled queue entries ahead of them before CI Summary could report. The workflow
  now keys merge_group concurrency on github.ref, the unique gh-readonly-queue ref for each batch commit.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1295"
    description: >-
      omnibase_core PR #1295 carries the workflow-only fix in .github/workflows/ci.yml, changing the CI
      merge_group concurrency key from the shared base ref to github.ref so each merge-queue batch commit
      has an independent CI run.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12445/dod-core-pr-1295/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      The onex_change_control evidence PR carries the OMN-12445 contract and PASS receipts that unblock
      the core merge-queue concurrency fix.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-12445/dod-occ-pr-self/command.yaml
