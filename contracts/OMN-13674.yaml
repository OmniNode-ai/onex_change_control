---
schema_version: "1.0.0"
ticket_id: "OMN-13674"
title: "Epic: Market-skill full integration coverage — waves 1/2/4/6/8/9 state coverage"
summary: >-
  omnimarket PR #1560 brings the 24 nodes in WS-5 waves 1/2/4/6/8/9 (child tickets OMN-13675/OMN-13676/OMN-13678/OMN-13680/OMN-13682/OMN-13683)
  under the strengthened full-declared-state-coverage DoD, now mechanically enforceable via the AST-hardened
  state-coverage gate (OMN-13816 / omnimarket #1556). For each node it adds a per-node contract-declared
  state-coverage regression test (tests/integration/node_<x>/ test_<x>_state_coverage.py) that pins every
  previously-baselined declared output state — the publish topics the runtime auto-emits and the output-class
  fields the projection consumes — to its literal declared value, so a silent contract rename or removal
  now fails at test time instead of only at a live runtime/projection boundary. It then removes those
  42 (node, state) pairs from scripts/validation/ state_coverage_baseline.txt so the hardened gate strict-enforces
  those nodes going forward (baseline shrinks 637 -> 595 non-comment entries). Tests-only change: no handler,
  model, contract, topic, schema, or migration source is modified.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >-
      The 24 new per-node contract-declared state-coverage tests pass in-process (contract.yaml load +
      literal-pin assertions), no external services.
    command: "uv run pytest tests/integration/ -k state_coverage -v"
  - kind: "ci"
    description: >-
      Hardened state-coverage gate passes in strict changed-node mode for all 24 touched nodes.
    command: "uv run python scripts/validate_state_coverage.py --check-changed origin/dev --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1560"
    description: >-
      omnimarket PR #1560 carries the waves 1/2/4/6/8/9 state-coverage suite for OMN-13674 at head commit
      ba072a441e9db0ce6125d453549e281aa5b71668. Focused suite (tests/integration -k state_coverage) =
      42 passed; hardened gate strict changed-mode = PASS 24 / FAIL 0; baseline 637 -> 595.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1560/command.yaml
          && grep -q '^pr_number: 1560$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1560/command.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: PR #1560 changes tests/ + scripts/validation/ state_coverage_baseline.txt only
      -- no handler/runtime behavior change, no topic, schema, migration, or .201/.200 runtime mutation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-13674/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC eligibility receipt for this onex_change_control PR, which carries this contract
      and receipts onto main so downstream omnimarket PR #1560 can cite Evidence-Source: OCC#<this-pr-number>.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-occ-self/command.yaml"
  - id: "dod-omnimarket-pr-1567"
    description: >-
      omnimarket PR #1567 carries the merge_sweep_pr_lifecycle_compute cluster (cov wave A) state-coverage
      + full-COMPUTE bus-driven coverage suite for OMN-13674 at head commit ba072a441e9db0ce6125d453549e281aa5b71668's
      sibling ebf311a06feb1e5de97765fe4a0725a3af8a4e00: three compute nodes (node_pr_lifecycle_triage_compute,
      node_merge_sweep_compute, node_pr_lifecycle_inventory_compute) driven over EventBusInmemory via
      LocalRuntimeBusAdapter. Full non-kafka suite = 11372 passed; state-coverage gate strict changed-mode
      = PASS 3 / FAIL 0.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1567/command.yaml
          && grep -q '^pr_number: 1567$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1567/command.yaml"
  - id: "dod-omnimarket-pr-1565"
    description: >-
      omnimarket PR #1565 carries the merge_sweep_pr_lifecycle_reducers cluster (cov wave A) declared-state
      coverage suite for OMN-13674: three REDUCER nodes (node_pr_lifecycle_state_reducer, node_merge_sweep_state_reducer,
      node_pr_review_fsm_reducer) driven over EventBusInmemory via LocalRuntimeBusAdapter, asserting typed
      projection / terminal-event payloads by the literal declared FSM state name. Focused suite = 40
      passed; state-coverage gate strict changed-mode = PASS 3 / FAIL 0; baseline shrinks by 19 (node,
      state) pairs. Merged as omnimarket commit 88599b4a90a2427c8acf55451b5b5986438f91f2.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1565/command.yaml
          && grep -q '^pr_number: 1565$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1565/command.yaml"
  - id: "dod-omnimarket-pr-1570"
    description: >-
      omnimarket PR #1570 carries the session_phase_fsm cluster (cov wave B) declared-state coverage suite
      for OMN-13674: four nodes across three archetypes (node_session_phase_orchestrator ORCHESTRATOR,
      node_session_phase_dispatcher EFFECT, node_session_phase_evaluator COMPUTE, node_session_phase_reducer
      REDUCER) driven over EventBusInmemory via LocalRuntimeBusAdapter / drive_round_trip. Every declared
      route/verdict/event class reached with a negative control per node. Focused suites = 36 passed;
      full non-kafka suite = 11481 passed. Head commit b499bcc0c9168f2ed85dff81b68773d388fadb6f.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1570/command.yaml
          && grep -q '^pr_number: 1570$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1570/command.yaml"
  - id: "dod-omnimarket-pr-1571"
    description: >-
      omnimarket PR #1571 carries the merge_sweep_pipeline cluster (cov wave B) declared-route ORCHESTRATOR
      coverage suite for OMN-13674: node_merge_sweep_triage_orchestrator driven over EventBusInmemory
      via LocalRuntimeBusAdapter with constructor-injected gh-subprocess resolvers. Every declared route/decision-table
      branch is exercised, including emitted-command routes, SKIP gates, resolution-failure edges, CI-rerun
      variants, fan-out accounting, terminal-event round-trip, and a negative control. New suite = 27
      passed; full non-kafka suite = 11472 passed; state-coverage gate strict changed-mode = PASS 1 /
      FAIL 0.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1571/command.yaml
          && grep -q '^pr_number: 1571$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1571/command.yaml"
  - id: "dod-omnimarket-pr-1572"
    description: >-
      omnimarket PR #1572 carries the pr_lifecycle_pipeline cluster (cov wave B, archetype orchestrator)
      declared-state coverage suite for OMN-13674: node_pr_lifecycle_orchestrator driven end-to-end over
      EventBusInmemory via LocalRuntimeBusAdapter (start command -> terminal ModelPrLifecycleResult on
      the declared completed topic; every FSM transition asserted off the phase-transition topic). Covers
      every declared FSM state, every transition edge incl every * -> FAILED, every sub-handler route,
      the correct terminal-event final_state per path (COMPLETE / FAILED / NOT_DONE), the verify-gate
      negative control, and the org-wide done-gate downgrade, plus a state_coverage contract pin. 23 passed;
      full non-kafka suite = 11468 passed. Head commit adf46213fbc1d55bc498f383fbaf583e3545e904.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1572/command.yaml
          && grep -q '^pr_number: 1572$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1572/command.yaml"
  - id: "dod-omnimarket-pr-1575"
    description: >-
      omnimarket PR #1575 carries the wave-sweep-audit-compute cluster (cov wave C, archetype compute)
      full declared-state COMPUTE coverage suite for OMN-13674: four compute nodes (node_changelog_audit_compute,
      node_ticket_plan_compute, node_contract_drift_compute, node_checkpoint_compute) driven over EventBusInmemory
      via LocalRuntimeBusAdapter (typed request on the declared command topic -> terminal typed result
      on the declared completed topic). Every declared output/verdict class reached, every mode/flag branch
      exercised, a negative control per node, and constructor-injected I/O boundaries (state_dir / OMNI_HOME)
      -- no monkeypatching. 25 passed; full non-kafka suite = 11568 passed. Head commit 287147ec090139d101b074922eb38f4dc4610019.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1575/command.yaml
          && grep -q '^pr_number: 1575$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1575/command.yaml"
  - id: "dod-omnimarket-pr-1574"
    description: >-
      omnimarket PR #1574 carries the wave-semantic-antipattern-subsystem cluster (cov wave C) declared-state
      coverage suite for OMN-13674: four nodes across three archetypes (node_semantic_antipattern_validator_orchestrator
      ORCHESTRATOR, node_semantic_antipattern_classifier_compute COMPUTE, node_antipattern_match_effect
      EFFECT, node_antipattern_index_effect EFFECT) driven over EventBusInmemory via LocalRuntimeBusAdapter.
      The orchestrator's single declared route emits one match command over the bus with both correlation-id
      branches asserted; the compute folds every declared verdict class with a known-bad negative control;
      each effect exercises every outcome at the constructor-injected I/O boundary (success, each failure
      mode, idempotency, gate-blocked/no-output) plus a per-node state_coverage contract pin. New suite
      = 56 passed; full non-kafka suite = 11598 passed, 52 skipped. Merged to dev as omnimarket PR #1574.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1574/command.yaml
          && grep -q '^pr_number: 1574$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1574/command.yaml"
  - id: "dod-omnimarket-pr-1576"
    description: >-
      omnimarket PR #1576 carries the wave-kb-context-knowledge cluster (cov wave C) declared-state coverage
      suite for OMN-13674: four nodes across two archetypes (node_candidate_pool_compute and node_context_bundle_generator_compute
      COMPUTE; node_kb_repowise_index_effect and node_kb_adr_publisher EFFECT) driven over EventBusInmemory
      via LocalRuntimeBusAdapter. Every declared output field / verdict class and every I/O-boundary outcome
      is reached with a negative control per node. The two effect handlers gain a behavior-preserving
      constructor-injected subprocess runner seam so the I/O boundary is exercised without monkeypatching
      subprocess. New suite = 27 passed; full non-kafka suite = 11571 passed. Head commit d237e9d9dfb9e82a6e43172587416fff576010ce.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1576/command.yaml
          && grep -q '^pr_number: 1576$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1576/command.yaml"
  - id: "dod-omnimarket-pr-1577"
    description: >-
      omnimarket PR #1577 carries the side-effect-alert-render-discovery cluster (cov wave D, archetype
      effect) full declared-state EFFECT coverage suite for OMN-13674: three effect nodes (node_env_sync_alert_effect,
      node_demo_renderer_effect, node_swarm_fleet_discovery_effect) driven end-to-end over EventBusInmemory
      via LocalRuntimeBusAdapter (typed request on the declared command topic -> terminal typed result
      on the declared completed topic). Every declared output field / topic state is asserted off the
      terminal event and every outcome at the injected I/O boundary is reached: success, each failure
      mode, idempotency, and gate-blocked/no-output. I/O boundaries are constructor-injected (_MockLinearAdapter,
      http_get_fn) -- subprocess/asyncpg/httpx are never monkeypatched. No src/handler change was needed.
      New suite = 23 passed; full non-kafka suite = 11676 passed, 52 skipped (the 5 failures are pre-existing
      live-postgres + unrelated registry drift, identical on clean origin/dev). state-coverage gate strict
      changed-mode = PASS 3 / FAIL 0. Head commit 128f6723f070a7aa5e4678dcf58453b6fe9c95ad.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1577/command.yaml
          && grep -q '^pr_number: 1577$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1577/command.yaml"
  - id: "dod-omnimarket-pr-1578"
    description: >-
      omnimarket PR #1578 carries the wave-D-projection-correctness-verification cluster (cov wave D)
      declared-state coverage suite for OMN-13674: four nodes across two archetypes (node_output_schema_registry_compute,
      node_handler_correctness_gate, node_projection_replay_check_compute COMPUTE; node_projection_delegation_inference_response
      REDUCER) driven over EventBusInmemory via LocalRuntimeBusAdapter. Every declared output field /
      verdict class is reached with a negative control per node; the reducer folds the declared event
      and asserts the materialized singleton projection state via a constructor-injected InmemoryDatabaseAdapter
      (no production handler change, no monkeypatch). Also fixes a substring-collision false-FAIL in scripts/validate_state_coverage.py
      (_get_changed_nodes exact-segment match) with a regression test. New suite = 23 passed; gate strict
      changed-mode = PASS 4 / FAIL 0. Head commit 0a497801d59a12cd94efb9ffac07bcd1fe3e42fd.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1578/command.yaml
          && grep -q '^pr_number: 1578$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1578/command.yaml"
