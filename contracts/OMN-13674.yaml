---
schema_version: "1.0.0"
ticket_id: "OMN-13674"
title: "RT-10 (Train 2): drop stale omnibase-core git override on OCC main"
summary: >-
  onex_change_control#4041 (hotfix -> main) drops the stale [tool.uv.sources] omnibase-core git override
  (rev f325e759 == core v0.44.0) on OCC main and repins omnibase-core to PyPI >=0.46.5,<0.47.0, matching
  dev (carried forward from dev PR #3521 / OMN-13878). Config-only: pyproject.toml + uv.lock only, no
  src/ change. This contract carries the in-tree OCC DoD evidence for the OCC-self hotfix->main PR; authored
  by the independent verifier (not the implementer) per no-self-authored-evidence.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: >-
      #4041 CI proves compatibility with core 0.46.7: Tests (both, 3.13), Type Check (mypy), and CodeQL
      all GREEN.
    command: "gh pr checks 4041 --repo OmniNode-ai/onex_change_control"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-main-pr-4041"
    description: >-
      RT-10 (Train 2). Product-observing check against onex_change_control#4041 (hotfix -> main) head
      66e95d18a7955148e7febeb078ad08622aeeb5a8. The PR drops the stale [tool.uv.sources] omnibase-core
      git override (rev f325e759 == core v0.44.0) on OCC main and repins omnibase-core to PyPI >=0.46.5,<0.47.0,
      matching dev (carried forward from dev PR #3521 / OMN-13878). Config-only: pyproject.toml + uv.lock
      only, no src/ change. This check reads both changed files at the PR head and asserts the pyproject
      pin is >=0.46.5,<0.47.0 and that uv.lock resolves omnibase-core to the 0.46.7 PyPI wheel (the git
      override is gone). Compatibility proven on #4041 CI: Tests (3.13), Type Check (mypy), and CodeQL
      all GREEN against core 0.46.7.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          pyproject="$(mktemp)" && lock="$(mktemp)" && gh api "repos/OmniNode-ai/onex_change_control/contents/pyproject.toml?ref=66e95d18a7955148e7febeb078ad08622aeeb5a8"
          --jq .content | base64 -d > "$pyproject" && gh api "repos/OmniNode-ai/onex_change_control/contents/uv.lock?ref=66e95d18a7955148e7febeb078ad08622aeeb5a8"
          --jq .content | base64 -d > "$lock" && grep -F -- 'omnibase-core>=0.46.5,<0.47.0' "$pyproject"
          && grep -F -- 'omnibase_core-0.46.7-py3-none-any.whl' "$lock"
