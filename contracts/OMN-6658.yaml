---
schema_version: "1.0.0"
ticket_id: "OMN-6658"
title: "Justify omnibase_core type suppressions"
summary: >-
  Bind central OCC evidence for omnibase_core PR #1137, which documents and justifies existing core type
  suppressions across CLI, node, service, validation, and model boundaries.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "omnibase_core PR #1137 CI and receipt gates pass."
    command: "gh pr checks 1137 --repo OmniNode-ai/omnibase_core --watch"
  - kind: "manual"
    description: "Central OCC evidence PR carries this contract and PASS receipts."
    command: "gh pr view <occ-pr> --repo OmniNode-ai/onex_change_control"
dod_evidence:
  - id: "dod-omnibase-core-pr-1137"
    description: "Bind OMN-6658 evidence to omnibase_core PR #1137 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6658/dod-omnibase-core-pr-1137/command.yaml\"
          && grep -q '^pr_number: 1137$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6658/dod-omnibase-core-pr-1137/command.yaml\"
          && grep -q '^commit_sha: \"5d781fbbef71cc88209e185d1ba8587ff1e6e60d\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6658/dod-omnibase-core-pr-1137/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Runtime path touch requires CI and receipt-gate validation but no live deploy before
      merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6658/dod-deploy-assessment/command.yaml\"
          && grep -q 'deploy gate' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6658/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind OMN-6658 central evidence to this onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6658/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number: 1378$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-6658/dod-occ-pr/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
