---
schema_version: "1.0.0"
ticket_id: "OMN-11813"
title: "node_swarm_aggregator_compute pure compute node"
summary: >
  COMPUTE node node_swarm_aggregator_compute that rehomes ResultAggregator logic from SEA into omnimarket.
  Concatenates subtask outputs by wave/decomposition order. For synthesis mode, accepts pre-fetched LLM
  output as explicit synthesis_output input — never calls an LLM directly. Subscribes to onex.cmd.omnimarket.swarm-aggregate.v1
  and publishes onex.evt.omnimarket.swarm-aggregation-completed.v1.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-787"
    description: "omnimarket PR #787 implements node_swarm_aggregator_compute with 13 unit tests"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11813/dod-omnimarket-pr-787/command.yaml\"
          && grep -q '^pr_number: 787$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11813/dod-omnimarket-pr-787/command.yaml\""
  - id: "dod-occ-pr"
    description: "OCC PR binds this contract to onex_change_control as the evidence source"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11813/dod-occ-pr/command.yaml\""
  - id: "dod-deploy"
    description: "Deploy assessment — new COMPUTE node, pure and side-effect-free, no live deploy required
      for initial merge; node is invoked on-demand via contract topic"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11813/dod-deploy/command.yaml\""
