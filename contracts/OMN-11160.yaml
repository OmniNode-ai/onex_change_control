---
schema_version: "1.0.0"
ticket_id: "OMN-11160"
title: "Flip omni standards mypy workflow to blocking in omnibase_core"
summary: >
  Central OCC evidence for the OMN-11160 slice of omnibase_core PR #1106, which makes the standards mypy
  job blocking and fixes strict typing issues required for that gate.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Strict mypy passes across src/."
    command: "uv run mypy src/ --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-standards-mypy-blocking"
    description: "Strict mypy passes after the standards workflow is made blocking."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11160/dod-standards-mypy-blocking/command.yaml\"
          && grep -q 'Success: no issues found in 3790 source files' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11160/dod-standards-mypy-blocking/command.yaml\""
