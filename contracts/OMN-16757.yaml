---
schema_version: "1.0.0"
ticket_id: "OMN-16757"
title: "Autobind OCC evidence for OMN-16757"
summary: "OCC Evidence-Source autobind companion (OMN-13317 F1) for PR #1026."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1026 product diff scope present"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - kind: "ci"
    description: "DoD leg 2 — UNMET as of 2026-08-28. `deploy-onex-prod.yml` has never completed green
      end-to-end. The command below exits 0 only once at least one dispatch in the last 100 runs carries
      conclusion=success; it exits 1 today (zero successes across the full recorded history). Recorded
      here rather than in dod_evidence on purpose: the receipt gate requires a PASS receipt on disk for
      EVERY dod_evidence item, so declaring an honestly-red leg there would wedge the OMN-16757 receipt
      gate for every future PR that cites this ticket. The Done flip is withheld by the unchecked Definition-of-Done
      checkbox in the Linear description, which the OMN-16736 AC-coverage guard reads before any flip
      path. Clearing it needs an operator dispatch under a live prod-promotion grant."
    command: "gh run list --repo OmniNode-ai/omninode_infra --workflow deploy-onex-prod.yml --limit 100
      --json conclusion --jq '[.[] | select(.conclusion == \"success\")] | length' | grep -qxv 0"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1026"
    description: "PR #1026 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1026 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1026-ci"
    description: "PR #1026 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1026 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1026"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7284"
    description: "OCC companion PR #7284 — self-bind for OMN-16757 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7284 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1028"
    description: "PR #1028 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1028 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1028-ci"
    description: "PR #1028 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1028 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-7308"
    description: "OCC companion PR #7308 — self-bind for OMN-16757 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7308 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-7310"
    description: "Binds OMN-16757 to OCC companion PR #7310 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7310 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1030"
    description: "PR #1030 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1030 --repo OmniNode-ai/omninode_infra --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1030-ci"
    description: "PR #1030 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1030 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-7312"
    description: "OCC companion PR #7312 — self-bind for OMN-16757 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7312 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OMN-16757-ac1-prod-roster-scoped"
    description: "DoD leg 1 — the post-deploy gate's verdict reflects only workloads the prod overlay
      ships. Executes the OMN-16757 roster-split suite that landed with omninode_infra#1026: it parses
      scripts/post-deploy-verify.sh, asserts VERIFY_DEPLOYMENTS_COMMON / VERIFY_DEPLOYMENTS_DEV_ONLY and
      the ENVIRONMENT combiner resolve prod to COMMON only (no omnimarket-projection-* deployment and
      no omnimarket-projection-api service assertion on prod), and derives the promote-to-prod job timeout
      budget from the workflow itself. Falsifiable: reverting the roster split or shrinking the job cap
      turns this red."
    source: "manual"
    execution_scope: "local_done_gate"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/k8s/test_post_deploy_verify_env_roster.py -q"
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "dod-OMN-16757-ac3-gate-timeout-rollback"
    description: "DoD leg 3 — deploy-onex-prod.yml no longer carries a step timeout smaller than the work
      it schedules, plus the OMN-16757 conditional-rollback fix from omninode_infra#1029. Executes the
      workflow seam suite, which extracts the real `run:` bodies from the parsed workflow and runs them
      under bash against a stub kubectl: the post-deploy gate step timeout must cover deployments x ROLLOUT_TIMEOUT,
      and `rollout undo deployment/onex-api` must fire only when this run actually mutated onex-api (onex_api_mutation
      == updated), never on `created` or unset. Falsifiable: restoring the unconditional rollback or the
      5-minute gate timeout turns this red."
    source: "manual"
    execution_scope: "local_done_gate"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/k8s/test_deploy_onex_prod_workflow.py -q"
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "occ-self-bind-pr-7355"
    description: "OCC self-binding receipt for PR #7355, the in-repo evidence PR carrying this contract.
      Binds OMN-16757 to the OCC PR by pr_number so occ-preflight eligibility can resolve a PASS receipt
      for the evidence PR itself (OMN-14650)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7355 --repo OmniNode-ai/onex_change_control --json number,state,headRefName"
