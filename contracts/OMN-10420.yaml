---
schema_version: '1.0.0'
ticket_id: OMN-10420
title: "feat(OMN-10420): ticket-identity binding in receipt_gate.py"
summary: >
  Add ticket-identity binding to receipt_gate.py so that a code PR's evidence is provably about the same
  ticket the PR claims to address. Enforces invariant I3 from the gate-collapse-fix plan.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: ci
    description: All receipt_gate unit tests pass including the 5 required identity-binding cases
    command: >
      uv run pytest tests/unit/validation/test_receipt_gate_identity_binding.py -v --tb=short
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: Five identity-binding failure cases pass (missing Evidence-Ticket, PR title mismatch,
      branch mismatch, contract ticket_id mismatch, receipt ticket_id mismatch)
    source: generated
    checks:
      - check_type: command
        check_value: >
          test -f drift/dod_receipts/OMN-10420/dod-001/command.yaml && grep -Fq 'status: PASS' drift/dod_receipts/OMN-10420/dod-001/command.yaml
          && grep -Fq 'commit_sha: 2c7970c0b01c2984be961544ec0a129e17bc9006' drift/dod_receipts/OMN-10420/dod-001/command.yaml
          && grep -Fq 'pr_number: 1019' drift/dod_receipts/OMN-10420/dod-001/command.yaml && grep -Fq
          '9 passed' drift/dod_receipts/OMN-10420/dod-001/command.yaml
  - id: dod-002
    description: No regressions in existing receipt_gate tests (adversarial, skip-token, workflow shape,
      CLI)
    source: generated
    checks:
      - check_type: command
        check_value: >
          test -f drift/dod_receipts/OMN-10420/dod-002/command.yaml && grep -Fq 'status: PASS' drift/dod_receipts/OMN-10420/dod-002/command.yaml
          && grep -Fq 'commit_sha: 2c7970c0b01c2984be961544ec0a129e17bc9006' drift/dod_receipts/OMN-10420/dod-002/command.yaml
          && grep -Fq 'pr_number: 1019' drift/dod_receipts/OMN-10420/dod-002/command.yaml && grep -Fq
          '68 passed' drift/dod_receipts/OMN-10420/dod-002/command.yaml
  - id: dod-003
    description: EVIDENCE_SOURCE_PATTERN and EVIDENCE_TICKET_PATTERN exported in __all__
    source: generated
    checks:
      - check_type: command
        check_value: >
          test -f drift/dod_receipts/OMN-10420/dod-003/command.yaml && grep -Fq 'EVIDENCE_SOURCE_PATTERN'
          drift/dod_receipts/OMN-10420/dod-003/command.yaml && grep -Fq 'EVIDENCE_TICKET_PATTERN' drift/dod_receipts/OMN-10420/dod-003/command.yaml
          && grep -Fq 'rg.__all__' drift/dod_receipts/OMN-10420/dod-003/command.yaml && grep -Fq 'probe_stdout:
          |' drift/dod_receipts/OMN-10420/dod-003/command.yaml && grep -Fq 'ok' drift/dod_receipts/OMN-10420/dod-003/command.yaml
  - id: dod-004
    description: CLI accepts --evidence-ticket and --branch-name flags
    source: generated
    checks:
      - check_type: command
        check_value: >
          test -f drift/dod_receipts/OMN-10420/dod-004/command.yaml && grep -Fq -- '--evidence-ticket'
          drift/dod_receipts/OMN-10420/dod-004/command.yaml && grep -Fq -- '--branch-name' drift/dod_receipts/OMN-10420/dod-004/command.yaml
