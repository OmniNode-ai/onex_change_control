---
schema_version: "1.0.0"
ticket_id: "OMN-11078"
title: "PostToolUse Aislop Blocking Hook - Compiled Regex Engine"
summary: >
  Central OCC evidence for omniclaude PR #1591. Adds the focused Python PostToolUse aislop gate engine
  and direct tests for high-severity blocking patterns without wiring hook wrappers, hooks.json, or default-mask
  changes owned by adjacent tickets.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Direct PostToolUse aislop gate tests pass with timing assertion."
    command: "See drift/dod_receipts/OMN-11078/dod-focused-tests/command.yaml"
  - kind: "ci"
    description: "Ruff passes for the new gate engine and direct tests."
    command: "See drift/dod_receipts/OMN-11078/dod-lint/command.yaml"
  - kind: "ci"
    description: "Local commit and push hooks pass for the branch."
    command: "See drift/dod_receipts/OMN-11078/dod-local-git-hooks/command.yaml"
  - kind: "ci"
    description: "Central OCC PR carries the OMN-11078 contract and receipt bundle."
    command: "See drift/dod_receipts/OMN-11078/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Direct PostToolUse aislop gate tests passed, including <100ms timing assertion."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '11 passed' drift/dod_receipts/OMN-11078/dod-focused-tests/command.yaml"
  - id: "dod-lint"
    description: "Ruff check passed for the new gate engine and direct tests."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'All checks passed' drift/dod_receipts/OMN-11078/dod-lint/command.yaml"
  - id: "dod-local-git-hooks"
    description: "Pre-commit and pre-push hook suites passed while committing and pushing omniclaude PR
      #1591."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11078/dod-local-git-hooks/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR #1066 carries the central OMN-11078 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11078/dod-occ-pr/command.yaml &&
          grep -q '^pr_number: 1066$' drift/dod_receipts/OMN-11078/dod-occ-pr/command.yaml"
