---
schema_version: "1.0.0"
ticket_id: "OMN-8723"
title: "fix(OMN-8723): gate golden_chain probe to GATED when consumers idle"
summary: >
  omnimarket PR #749 adds an idle-gate mode to node_golden_chain_sweep so empty projection tables caused
  by an idle dispatch loop produce GATED instead of a blocking TIMEOUT. Real field-level failures still
  fail, and mixed gated plus failed chains remain blocking.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused golden_chain idle-gate tests pass for omnimarket PR #749."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^status: \"?PASS\"?$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8723/dod-focused-tests/command.yaml\"
          && grep -q '^commit_sha: \"46902ee493399bee7680d587cfe542a5b50d1457\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8723/dod-focused-tests/command.yaml\""
  - id: "dod-deploy-assessment"
    description: >
      Runtime node path change is deploy-gated. The PR changes golden-chain probe classification only;
      no live deploy, runtime restart, .201 mutation, docker exec, or rpk topic produce was performed
      pre-merge. Runtime pickup should occur through the normal post-merge deployment path.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -Eq '^status: \"?PASS\"?$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8723/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8723/dod-deploy-assessment/command.yaml\"
          && grep -q 'deploy-gated' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-8723/dod-deploy-assessment/command.yaml\""
