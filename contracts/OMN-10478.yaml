---
schema_version: "1.0.0"
ticket_id: OMN-10478
title: "Add ab-compare orchestrator node"
summary: "Add the A/B compare orchestrator node that resolves model registry entries, skips models missing
  required keys or endpoints, dispatches inference through the effect node, and returns sorted comparison
  results with costs and token counts."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "events"
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Focused ab-compare orchestrator unit tests pass"
    command: "uv run pytest tests/unit/nodes/node_ab_compare_orchestrator/test_handler_ab_compare_orchestrator.py
      -q"
  - kind: "manual"
    description: "Node metadata dependency scope validates orchestrator imports"
    command: "uv run python scripts/ci/check_node_metadata_dependencies.py"
  - kind: "manual"
    description: "Node drift gate validates changed orchestrator package registration"
    command: "python scripts/validate_node_drift.py --check-changed origin/main --strict"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused ab-compare orchestrator unit tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10478/dod-001/command.yaml"
  - id: "dod-002"
    description: "Node metadata dependency scope validates orchestrator imports"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10478/dod-002/command.yaml"
  - id: "dod-003"
    description: "Node drift gate validates changed orchestrator package registration"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10478/dod-003/command.yaml"
