---
schema_version: 1.0.0
ticket_id: OMN-14700
title: Autobind OCC evidence for OMN-14700
summary: 'OCC contract bound by node_pr_lifecycle_fix_effect Evidence-Source autobind (OMN-13317 F1) when
  OmniNode-ai/omnibase_core PR #1452 carried a product-SHA Evidence-Source or was missing an OCC contract,
  and failed the gate. #magic___^_^___line '
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: 'PR #1452 product diff scope present'
    command: PRODUCT_PR_NUMBER=1452; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr diff ${PRODUCT_PR_NUMBER}
      --repo ${PRODUCT_REPO} --name-only | grep -q .
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-OmniNode-ai-omnibase_core-pr-1452
    description: "PR #1452 on OmniNode-ai/omnibase_core — Evidence-Source autobind."
    source: generated
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=1452; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr view ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --json number,state
  - id: dod-OmniNode-ai-omnibase_core-pr-1452-ci
    description: "PR #1452 on OmniNode-ai/omnibase_core — product diff scope (OMN-14425 substance check;
      OMN-14650: no longer gated on source-PR CI being green)."
    source: generated
    checks:
      - check_type: command
        check_value: PRODUCT_PR_NUMBER=1452; PRODUCT_REPO=OmniNode-ai/omnibase_core; gh pr diff ${PRODUCT_PR_NUMBER}
          --repo ${PRODUCT_REPO} --name-only | grep -q .
  - id: occ-self-bind-pr-4291
    description: "OCC companion PR #4291 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14700
      (OMN-14650)."
    source: generated
    checks:
      - check_type: command
        check_value: PR_NUMBER=4291; REPO=OmniNode-ai/onex_change_control; gh pr view ${PR_NUMBER} --repo
          ${REPO} --json number,state
  - id: dod-OmniNode-ai-omnibase_core-pr-1452-deploy-scope
    description: 'PR #1452 on OmniNode-ai/omnibase_core touches the node_test_selector_compute runtime
      path; deploy-scope evidence for OMN-14700.'
    source: manual
    checks:
      - check_type: command
        check_value: DEPLOY_SCOPE_ASSERTION=deploy; PRODUCT_PR_NUMBER=1452; PRODUCT_REPO=OmniNode-ai/omnibase_core;
          gh pr diff ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --name-only | grep -q ^src/omnibase_core/nodes/node_test_selector_compute/
