---
schema_version: "1.0.0"
ticket_id: "OMN-12385"
title: "Focused unit coverage for Kafka error paths, diagnostics checks, and publisher loop"
summary: >
  Bind central evidence for the OMN-12385 test-only PRs: omnibase_infra PR #1783 adds focused EventBusKafka
  publish/subscribe error-path unit tests, and omnimarket PR #990 adds platform diagnostics dimension
  checks plus KafkaPublisherLoop unit tests. These PRs add coverage only and do not change production
  behavior.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-1783"
    description: "omnibase_infra PR #1783 adds 15 focused EventBusKafka error-path unit tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12385/dod-omnibase-infra-pr-1783/command.yaml\"
          && grep -q '^pr_number: 1783$' \"drift/dod_receipts/OMN-12385/dod-omnibase-infra-pr-1783/command.yaml\""
  - id: "dod-omnimarket-pr-990"
    description: "omnimarket PR #990 adds 48 focused diagnostics and publisher-loop unit tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12385/dod-omnimarket-pr-990/command.yaml\"
          && grep -q '^pr_number: 990$' \"drift/dod_receipts/OMN-12385/dod-omnimarket-pr-990/command.yaml\""
  - id: "dod-focused-tests"
    description: "The focused OMN-12385 unit tests pass locally for both repos."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12385/dod-focused-tests/command.yaml\"
          && grep -q '15 passed' \"drift/dod_receipts/OMN-12385/dod-focused-tests/command.yaml\" && grep
          -q '48 new unit tests' \"drift/dod_receipts/OMN-12385/dod-focused-tests/command.yaml\""
  - id: "dod-occ-followup-branch"
    description: "This OCC follow-up branch carries the OMN-12385 contract and receipts and is bound to
      the first evidence commit in this PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12385/dod-occ-followup-branch/command.yaml\"
          && grep -q '^commit_sha: \"3d9cdb8fb8f9e4310c697fbf5b0df0207a924a43\"' \"drift/dod_receipts/OMN-12385/dod-occ-followup-branch/command.yaml\""
