---
schema_version: "1.0.0"
ticket_id: "OMN-10592"
title: "feat: omnimarket_quickstart onboarding policy"
summary: >
  Adds three omnimarket-specific steps to the canonical onboarding graph (install_omnimarket, validate_omnimarket_config,
  register_omnimarket_node) and a new omnimarket_quickstart policy targeting omnimarket_node_registered.
  7 unit tests verify policy loads, capabilities resolve, and steps are present.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-policy-file-exists"
    description: "omnimarket_quickstart.yaml exists and loads via load_builtin_policies()."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10592/dod-policy-file-exists/command.yaml"
  - id: "dod-unit-tests"
    description: "7 unit tests in test_omnimarket_quickstart_policy.py pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10592/dod-unit-tests/command.yaml"
