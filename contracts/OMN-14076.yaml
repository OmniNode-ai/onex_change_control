---
schema_version: "1.0.0"
ticket_id: "OMN-14076"
title: "ci: remove merge_group triggers from omnibase_core non-required workflows"
summary: >-
  Canary reduction for omnibase_core merge queue load: PR #1392 removes the top-level
  merge_group trigger from 22 workflows that do not publish required status-check
  contexts on dev. The pull_request and push triggers remain intact, and the 10
  required-check carrier workflows keep merge_group coverage.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-core-pr-1392"
    description: >-
      Consuming-PR binding: omnibase_core PR #1392 at head
      5f73722cb696058bf0e5256ebfc707a264af1a52 removes only non-required
      merge_group workflow triggers for OMN-14076.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14076/dod-omnibase-core-pr-1392/command.yaml"
  - id: "dod-workflow-trigger-audit"
    description: >-
      Workflow audit proof: after the PR branch edit, the only remaining top-level
      merge_group triggers in omnibase_core are the 10 required status-check
      carrier workflows.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14076/dod-workflow-trigger-audit/command.yaml"
  - id: "dod-local-validation"
    description: >-
      Local validation proof for the workflow-only change: git diff whitespace
      validation is clean and actionlint was run against the changed workflows,
      with only pre-existing shellcheck notes in untouched validator-todo-marker
      script content.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14076/dod-local-validation/command.yaml"
  - id: "dod-occ-pr-3661"
    description: >-
      Self-binding OCC promotion receipt: onex_change_control PR #3661 carries
      the OMN-14076 central contract and receipts that unblock omnibase_core
      PR #1392 receipt-gate validation.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14076/dod-occ-pr-3661/command.yaml"
