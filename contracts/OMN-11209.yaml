---
schema_version: "1.0.0"
ticket_id: OMN-11209
title: "feat(OMN-11209): update scaffold templates with doctrine compliance fields"
summary: >
  Updates cli_new.py CONTRACT_TEMPLATE and HANDLER_TEMPLATE to emit descriptor.runtime_profiles, event_bus
  topic stubs, and data_provenance guidance. Adds projection/ Jinja2 template set with full projection_contract
  stub. Binds omnibase_core PR #1103. Part of epic OMN-11188.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "Implementation PR #1103 open against omnibase_core main."
    command: "gh pr view 1103 --repo OmniNode-ai/omnibase_core --json url,state,baseRefName,headRefName,headRefOid"
  - kind: "manual"
    description: "OCC PR carries the OMN-11209 contract and ci-pass receipt."
    command: "gh pr view <OCC-PR-NUM> --repo OmniNode-ai/onex_change_control --json number,state"
  - kind: "manual"
    description: "No live deploy is required for pure CLI scaffolding template changes."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11209/dod-deploy-assessment/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-ci-pass"
    description: "Implementation PR #1103 is open for OMN-11209 against omnibase_core main."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11209/dod-ci-pass/command.yaml &&
          grep -q '^ticket_id: OMN-11209$' drift/dod_receipts/OMN-11209/dod-ci-pass/command.yaml && grep
          -q '^pr_number: 1103$' drift/dod_receipts/OMN-11209/dod-ci-pass/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy, runtime restart, .201 mutation, or event-bus write was performed (pure
      CLI scaffolding template change)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11209/dod-deploy-assessment/command.yaml
          && grep -q 'No live deploy' drift/dod_receipts/OMN-11209/dod-deploy-assessment/command.yaml"
  - id: "dod-occ-pr-1119"
    description: "OCC PR #1119 self-binds OMN-11209 central contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11209/dod-occ-pr-1119/command.yaml
          && grep -q '^pr_number: 1119$' drift/dod_receipts/OMN-11209/dod-occ-pr-1119/command.yaml"
