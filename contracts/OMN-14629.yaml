---
schema_version: "1.0.0"
ticket_id: "OMN-14629"
title: "refactor(OMN-14629): flip core canon-shape rank1 nodes to definition-B"
summary: >-
  Central OCC evidence for omnibase_core PR #1442. The product PR converts three rank-1 canon-shape nodes
  from legacy definition-A forms to typed definition-B handle(request) shapes, preserving equivalence
  through committed adequacy receipts, equivalence replay artifacts, and real LocalRuntimeBusAdapter cross-boundary
  regression coverage. The PR also includes a test-only scanner repair that marks the intentionally synthetic
  credential fixture as allowlisted for Detect Secrets.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1442-product-head"
    description: >-
      Product-observing verification for omnibase_core PR #1442 at head 6cb3e2d9971e87b020873cb79cfc6f0740a512a5.
      The proof reads PR metadata and changed files from GitHub at that exact head, verifies the PR targets
      dev, and checks the load-bearing definition-B seams: backend secret discipline, contract resolve,
      and routing authority check handlers expose typed handle(request) forms; adequacy and equivalence
      artifacts exist for all three nodes; and the real LocalRuntimeBusAdapter cross-boundary regression
      file is present.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1442; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=6cb3e2d9971e87b020873cb79cfc6f0740a512a5;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,baseRefName,files,title
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
          > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fxq dev "$d/pr.txt" && grep -Fq
          'refactor(OMN-14629): flip 3 of 6 core canon-shape nodes to def-B' "$d/pr.txt" && for p in src/omnibase_core/validation/validator_backend_secret_discipline.py
          src/omnibase_core/nodes/node_contract_resolve_compute/handler.py src/omnibase_core/nodes/node_routing_authority_check_compute/handler.py
          tests/unit/runtime/test_canon_shape_rank1_cross_boundary.py scripts/ci/adequacy_receipts/omnibase_core.nodes.node_backend_secret_discipline_compute.json
          scripts/ci/adequacy_receipts/omnibase_core.nodes.node_backend_secret_discipline_compute.equivalence.json
          scripts/ci/adequacy_receipts/omnibase_core.nodes.node_contract_resolve_compute.json scripts/ci/adequacy_receipts/omnibase_core.nodes.node_contract_resolve_compute.equivalence.json
          scripts/ci/adequacy_receipts/omnibase_core.nodes.node_routing_authority_check_compute.json scripts/ci/adequacy_receipts/omnibase_core.nodes.node_routing_authority_check_compute.equivalence.json;
          do grep -Fq "$p" "$d/pr.txt" && mkdir -p "$d/$(dirname "$p")" && gh api "repos/OmniNode-ai/omnibase_core/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "$d/$p"; done && grep -Fq 'def handle(' "$d/src/omnibase_core/validation/validator_backend_secret_discipline.py"
          && grep -Fq 'request: ModelBackendSecretDisciplineInput' "$d/src/omnibase_core/validation/validator_backend_secret_discipline.py"
          && grep -Fq 'def handle(' "$d/src/omnibase_core/nodes/node_contract_resolve_compute/handler.py"
          && grep -Fq 'input_model: ModelContractResolveInput' "$d/src/omnibase_core/nodes/node_contract_resolve_compute/handler.py"
          && grep -Fq 'def handle(' "$d/src/omnibase_core/nodes/node_routing_authority_check_compute/handler.py"
          && grep -Fq 'request: ModelRoutingAuthorityCheckInput' "$d/src/omnibase_core/nodes/node_routing_authority_check_compute/handler.py"
          && grep -Fq 'LocalRuntimeBusAdapter' "$d/tests/unit/runtime/test_canon_shape_rank1_cross_boundary.py"
          && grep -Fq 'pragma: allowlist secret' "$d/tests/unit/runtime/test_canon_shape_rank1_cross_boundary.py"
          && grep -Fq 'node_backend_secret_discipline_compute' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_backend_secret_discipline_compute.json"
          && grep -Fq 'node_contract_resolve_compute' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_contract_resolve_compute.json"
          && grep -Fq 'node_routing_authority_check_compute' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_routing_authority_check_compute.json"
          && grep -Fq 'selected_input_hashes' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_backend_secret_discipline_compute.equivalence.json"
          && grep -Fq 'selected_input_hashes' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_contract_resolve_compute.equivalence.json"
          && grep -Fq 'selected_input_hashes' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_routing_authority_check_compute.equivalence.json"
  - id: "dod-core-pr-1442-local-verification"
    description: >-
      Verification evidence for omnibase_core PR #1442 at head 6cb3e2d9971e87b020873cb79cfc6f0740a512a5.
      The product PR body records the full local node, validation, runtime, and canary sweep, canonical-handler-shape
      full ratchet output, RED/GREEN cross-boundary proof, formatter/linter/type checks, and pre-commit
      proof. The foreground merge controller additionally reran the Detect Secrets workflow command locally
      after the fixture allowlist patch and reran the focused rank1 test set with PYTHONPATH pinned to
      the product worktree: 66 passed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1442; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=6cb3e2d9971e87b020873cb79cfc6f0740a512a5;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,body --jq '.headRefOid
          == "'$PRODUCT_HEAD'" and (.body | contains("5868 passed, 5 skipped")) and (.body | contains("scripts/ci/canonical_handler_shape.py
          --full")) and (.body | contains("new=0 unproven_flips=0 warn=3")) and (.body | contains("Cross-boundary
          test verified RED against pre-flip code, GREEN after")) and (.body | contains("ruff format --check"))
          and (.body | contains("pre-commit run --files <touched>"))' | grep -qx true
  - id: "dod-core-pr-1442-deploy-scope"
    description: >-
      Deploy-surface assessment for omnibase_core PR #1442. The diff is Python handler, validator, test,
      and adequacy receipt content only. It does not touch Dockerfiles, compose manifests, deploy scripts,
      migrations, runtime service definitions, or infrastructure configuration. No live deploy, restart,
      docker exec, rpk topic produce, database mutation, or .201 mutation was performed or required by
      this OCC evidence lane.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1442; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=6cb3e2d9971e87b020873cb79cfc6f0740a512a5;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid --jq '.headRefOid ==
          "'$PRODUCT_HEAD'"' | grep -qx true && ! gh pr diff "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO"
          --name-only | grep -qE '(^|/)(Dockerfile|docker-compose[^/]*|compose[^/]*|deploy|migrations)(/|$)'
  - id: "dod-occ-self-bind-pr-4170"
    description: >-
      OCC companion self-binding for onex_change_control PR #4170. Verifies the PR is open on the expected
      OMN-14629 evidence branch and that the initial evidence commit 16706ddfd8c28d71f1573a3b246d0b6528e89b99
      is an ancestor of the current PR head.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/4170 --jq '.head.ref == "jonah/omn-14629-occ-core-1442"
          and .state == "open"' | grep -qx true && gh api repos/OmniNode-ai/onex_change_control/pulls/4170/commits
          --paginate --jq '.[].sha' | grep -Fx 16706ddfd8c28d71f1573a3b246d0b6528e89b99
