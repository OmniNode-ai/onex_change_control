---
schema_version: "1.0.0"
ticket_id: "OMN-14629"
title: "refactor(OMN-14629): flip core canon-shape rank1 nodes to definition-B"
summary: >-
  Central OCC evidence for omnibase_core PR #1442. The product PR converts three rank-1 canon-shape nodes
  from legacy definition-A forms to typed definition-B handle(request) shapes, preserving equivalence
  through committed adequacy receipts, equivalence replay artifacts, and real LocalRuntimeBusAdapter cross-boundary
  regression coverage. The PR also includes a test-only scanner repair that marks the intentionally synthetic
  credential fixture as allowlisted for Detect Secrets.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
  - events
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1442-product-head"
    description: >-
      Product-observing verification for omnibase_core PR #1442 at head 6cb3e2d9971e87b020873cb79cfc6f0740a512a5.
      The proof reads PR metadata and changed files from GitHub at that exact head, verifies the PR targets
      dev, and checks the load-bearing definition-B seams: backend secret discipline, contract resolve,
      and routing authority check handlers expose typed handle(request) forms; adequacy and equivalence
      artifacts exist for all three nodes; and the real LocalRuntimeBusAdapter cross-boundary regression
      file is present.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1442; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=6cb3e2d9971e87b020873cb79cfc6f0740a512a5;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,baseRefName,files,title
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
          > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fxq dev "$d/pr.txt" && grep -Fq
          'refactor(OMN-14629): flip 3 of 6 core canon-shape nodes to def-B' "$d/pr.txt" && for p in src/omnibase_core/validation/validator_backend_secret_discipline.py
          src/omnibase_core/nodes/node_contract_resolve_compute/handler.py src/omnibase_core/nodes/node_routing_authority_check_compute/handler.py
          tests/unit/runtime/test_canon_shape_rank1_cross_boundary.py scripts/ci/adequacy_receipts/omnibase_core.nodes.node_backend_secret_discipline_compute.json
          scripts/ci/adequacy_receipts/omnibase_core.nodes.node_backend_secret_discipline_compute.equivalence.json
          scripts/ci/adequacy_receipts/omnibase_core.nodes.node_contract_resolve_compute.json scripts/ci/adequacy_receipts/omnibase_core.nodes.node_contract_resolve_compute.equivalence.json
          scripts/ci/adequacy_receipts/omnibase_core.nodes.node_routing_authority_check_compute.json scripts/ci/adequacy_receipts/omnibase_core.nodes.node_routing_authority_check_compute.equivalence.json;
          do grep -Fq "$p" "$d/pr.txt" && mkdir -p "$d/$(dirname "$p")" && gh api "repos/OmniNode-ai/omnibase_core/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "$d/$p"; done && grep -Fq 'def handle(' "$d/src/omnibase_core/validation/validator_backend_secret_discipline.py"
          && grep -Fq 'request: ModelBackendSecretDisciplineInput' "$d/src/omnibase_core/validation/validator_backend_secret_discipline.py"
          && grep -Fq 'def handle(' "$d/src/omnibase_core/nodes/node_contract_resolve_compute/handler.py"
          && grep -Fq 'input_model: ModelContractResolveInput' "$d/src/omnibase_core/nodes/node_contract_resolve_compute/handler.py"
          && grep -Fq 'def handle(' "$d/src/omnibase_core/nodes/node_routing_authority_check_compute/handler.py"
          && grep -Fq 'request: ModelRoutingAuthorityCheckInput' "$d/src/omnibase_core/nodes/node_routing_authority_check_compute/handler.py"
          && grep -Fq 'LocalRuntimeBusAdapter' "$d/tests/unit/runtime/test_canon_shape_rank1_cross_boundary.py"
          && grep -Fq 'pragma: allowlist secret' "$d/tests/unit/runtime/test_canon_shape_rank1_cross_boundary.py"
          && grep -Fq 'node_backend_secret_discipline_compute' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_backend_secret_discipline_compute.json"
          && grep -Fq 'node_contract_resolve_compute' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_contract_resolve_compute.json"
          && grep -Fq 'node_routing_authority_check_compute' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_routing_authority_check_compute.json"
          && grep -Fq 'selected_input_hashes' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_backend_secret_discipline_compute.equivalence.json"
          && grep -Fq 'selected_input_hashes' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_contract_resolve_compute.equivalence.json"
          && grep -Fq 'selected_input_hashes' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_routing_authority_check_compute.equivalence.json"
  - id: "dod-core-pr-1442-local-verification"
    description: >-
      Verification evidence for omnibase_core PR #1442 at head 6cb3e2d9971e87b020873cb79cfc6f0740a512a5.
      The product PR body records the full local node, validation, runtime, and canary sweep, canonical-handler-shape
      full ratchet output, RED/GREEN cross-boundary proof, formatter/linter/type checks, and pre-commit
      proof. The foreground merge controller additionally reran the Detect Secrets workflow command locally
      after the fixture allowlist patch and reran the focused rank1 test set with PYTHONPATH pinned to
      the product worktree: 66 passed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1442; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=6cb3e2d9971e87b020873cb79cfc6f0740a512a5;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,body --jq '.headRefOid
          == "'$PRODUCT_HEAD'" and (.body | contains("5868 passed, 5 skipped")) and (.body | contains("scripts/ci/canonical_handler_shape.py
          --full")) and (.body | contains("new=0 unproven_flips=0 warn=3")) and (.body | contains("Cross-boundary
          test verified RED against pre-flip code, GREEN after")) and (.body | contains("ruff format --check"))
          and (.body | contains("pre-commit run --files <touched>"))' | grep -qx true
  - id: "dod-core-pr-1442-deploy-scope"
    description: >-
      Deploy-surface assessment for omnibase_core PR #1442. The diff is Python handler, validator, test,
      and adequacy receipt content only. It does not touch Dockerfiles, compose manifests, deploy scripts,
      migrations, runtime service definitions, or infrastructure configuration. No live deploy, restart,
      docker exec, rpk topic produce, database mutation, or .201 mutation was performed or required by
      this OCC evidence lane.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1442; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=6cb3e2d9971e87b020873cb79cfc6f0740a512a5;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid --jq '.headRefOid ==
          "'$PRODUCT_HEAD'"' | grep -qx true && ! gh pr diff "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO"
          --name-only | grep -qE '(^|/)(Dockerfile|docker-compose[^/]*|compose[^/]*|deploy|migrations)(/|$)'
  - id: "dod-occ-self-bind-pr-4170"
    description: >-
      OCC companion self-binding for onex_change_control PR #4170. Verifies the PR is open on the expected
      OMN-14629 evidence branch and that the initial evidence commit 16706ddfd8c28d71f1573a3b246d0b6528e89b99
      is an ancestor of the current PR head.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/4170 --jq '.head.ref == "jonah/omn-14629-occ-core-1442"
          and .state == "open"' | grep -qx true && gh api repos/OmniNode-ai/onex_change_control/pulls/4170/commits
          --paginate --jq '.[].sha' | grep -Fx 16706ddfd8c28d71f1573a3b246d0b6528e89b99
  - id: "dod-core-pr-1442-head-e757d225-product-head"
    description: >-
      Product-observing verification for refreshed omnibase_core PR #1442 head e757d225be13b284349f42cfd8adefddc9bf32c9
      after the merge-sweep repair updated the live canonical-shape test expectation to match the intended
      backend-secret-discipline definition-B conversion.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1442; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=e757d225be13b284349f42cfd8adefddc9bf32c9;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,baseRefName,files,title
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
          > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fxq dev "$d/pr.txt" && grep -Fq
          'refactor(OMN-14629): flip 3 of 6 core canon-shape nodes to def-B' "$d/pr.txt" && for p in src/omnibase_core/validation/validator_backend_secret_discipline.py
          src/omnibase_core/nodes/node_contract_resolve_compute/handler.py src/omnibase_core/nodes/node_routing_authority_check_compute/handler.py
          tests/unit/runtime/test_canon_shape_rank1_cross_boundary.py tests/unit/scripts/ci/test_canonical_handler_shape.py;
          do grep -Fq "$p" "$d/pr.txt" && mkdir -p "$d/$(dirname "$p")" && gh api "repos/OmniNode-ai/omnibase_core/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "$d/$p"; done && grep -Fq 'def handle(' "$d/src/omnibase_core/validation/validator_backend_secret_discipline.py"
          && grep -Fq 'request: ModelBackendSecretDisciplineInput' "$d/src/omnibase_core/validation/validator_backend_secret_discipline.py"
          && grep -Fq 'def handle(' "$d/src/omnibase_core/nodes/node_contract_resolve_compute/handler.py"
          && grep -Fq 'input_model: ModelContractResolveInput' "$d/src/omnibase_core/nodes/node_contract_resolve_compute/handler.py"
          && grep -Fq 'def handle(' "$d/src/omnibase_core/nodes/node_routing_authority_check_compute/handler.py"
          && grep -Fq 'request: ModelRoutingAuthorityCheckInput' "$d/src/omnibase_core/nodes/node_routing_authority_check_compute/handler.py"
          && grep -Fq 'LocalRuntimeBusAdapter' "$d/tests/unit/runtime/test_canon_shape_rank1_cross_boundary.py"
          && grep -Fq 'assert backend.is_canonical and backend.category == "canonical"' "$d/tests/unit/scripts/ci/test_canonical_handler_shape.py"
    evidence_artifact: supersedes_dod_evidence:dod-core-pr-1442-product-head
  - id: "dod-core-pr-1442-head-e757d225-local-verification"
    description: >-
      Local verification proof for refreshed omnibase_core PR #1442 head e757d225be13b284349f42cfd8adefddc9bf32c9.
      The product PR body records the rank1 validation sweep and the merge controller reran the focused
      failing live-shape test, the full canonical-handler-shape ratchet, and focused pre-commit after
      the test expectation repair.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1442; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=e757d225be13b284349f42cfd8adefddc9bf32c9;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,body --jq '.headRefOid
          == "'$PRODUCT_HEAD'" and (.body | contains("5868 passed, 5 skipped")) and (.body | contains("scripts/ci/canonical_handler_shape.py
          --full")) and (.body | contains("new=0 unproven_flips=0 warn=3"))' | grep -qx true
    evidence_artifact: supersedes_dod_evidence:dod-core-pr-1442-local-verification
  - id: "dod-core-pr-1442-head-e757d225-deploy-scope"
    description: >-
      Deploy-surface assessment for refreshed omnibase_core PR #1442 head e757d225be13b284349f42cfd8adefddc9bf32c9.
      The follow-up repair is a test expectation update only and does not touch Dockerfiles, compose manifests,
      deploy scripts, migrations, runtime service definitions, or infrastructure configuration.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1442; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=e757d225be13b284349f42cfd8adefddc9bf32c9;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid --jq '.headRefOid ==
          "'$PRODUCT_HEAD'"' | grep -qx true && ! gh pr diff "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO"
          --name-only | grep -qE '(^|/)(Dockerfile|docker-compose[^/]*|compose[^/]*|deploy|migrations)(/|$)'
    evidence_artifact: supersedes_dod_evidence:dod-core-pr-1442-deploy-scope
  - id: "dod-occ-self-bind-pr-4172"
    description: >-
      OCC companion self-binding for refreshed onex_change_control PR #4172. Verifies the PR is open on
      the expected OMN-14629 refresh branch and contains the initial evidence commit ed1d7cc96fb7a258a337a9398ffcf03a84fa12c7.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/4172 --jq '.head.ref == "jonah/omn-14629-occ-core-1442-head-e757d225"
          and .state == "open"' | grep -qx true && gh api repos/OmniNode-ai/onex_change_control/pulls/4172/commits
          --paginate --jq '.[].sha' | grep -Fx ed1d7cc96fb7a258a337a9398ffcf03a84fa12c7
    evidence_artifact: supersedes_dod_evidence:dod-occ-self-bind-pr-4170
  - id: "dod-core-pr-1443-head-e44e25d7-product-head"
    description: >-
      Product-observing verification for omnibase_core PR #1443 at head e44e25d7b852976f6ccb0db4438d558a030c3d91.
      The proof reads PR metadata and changed files from GitHub at that exact head, verifies the PR targets
      dev, and checks the rank2 definition-B seams: compliance scan, report reducer, and evidence effect
      handlers expose typed handle(request) forms; adequacy/equivalence artifacts exist for all three
      nodes; the canonical-handler baseline is empty after rank1 plus rank2; and the real LocalRuntimeBusAdapter
      cross-boundary regression is present.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1443; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=e44e25d7b852976f6ccb0db4438d558a030c3d91;
          d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,baseRefName,files,title
          --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
          > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fxq dev "$d/pr.txt" && grep -Fq
          'refactor(OMN-14629): flip remaining 3 core canon-shape nodes to def-B' "$d/pr.txt" && for p
          in src/omnibase_core/nodes/node_compliance_scan_compute/handler.py src/omnibase_core/nodes/node_compliance_report_reducer/handler.py
          src/omnibase_core/nodes/node_compliance_evidence_effect/handler.py scripts/ci/canonical_handler_shape_baseline.py
          tests/unit/runtime/test_canon_shape_rank2_cross_boundary.py tests/unit/scripts/ci/test_canonical_handler_shape.py
          scripts/ci/adequacy_receipts/omnibase_core.nodes.node_compliance_scan_compute.json scripts/ci/adequacy_receipts/omnibase_core.nodes.node_compliance_scan_compute.equivalence.json
          scripts/ci/adequacy_receipts/omnibase_core.nodes.node_compliance_report_reducer.json scripts/ci/adequacy_receipts/omnibase_core.nodes.node_compliance_report_reducer.equivalence.json
          scripts/ci/adequacy_receipts/omnibase_core.nodes.node_compliance_evidence_effect.json scripts/ci/adequacy_receipts/omnibase_core.nodes.node_compliance_evidence_effect.equivalence.json;
          do grep -Fq "$p" "$d/pr.txt" && mkdir -p "$d/$(dirname "$p")" && gh api "repos/OmniNode-ai/omnibase_core/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "$d/$p"; done && grep -Fq 'def handle(' "$d/src/omnibase_core/nodes/node_compliance_scan_compute/handler.py"
          && grep -Fq 'ModelComplianceScanRequest' "$d/src/omnibase_core/nodes/node_compliance_scan_compute/handler.py"
          && grep -Fq 'def handle(' "$d/src/omnibase_core/nodes/node_compliance_report_reducer/handler.py"
          && grep -Fq 'ModelComplianceReportReduceRequest' "$d/src/omnibase_core/nodes/node_compliance_report_reducer/handler.py"
          && grep -Fq 'def handle(' "$d/src/omnibase_core/nodes/node_compliance_evidence_effect/handler.py"
          && grep -Fq 'ModelEvidenceReportState' "$d/src/omnibase_core/nodes/node_compliance_evidence_effect/handler.py"
          && grep -Fq 'NON_CANONICAL: tuple[str, ...] = ()' "$d/scripts/ci/canonical_handler_shape_baseline.py"
          && grep -Fq 'LocalRuntimeBusAdapter' "$d/tests/unit/runtime/test_canon_shape_rank2_cross_boundary.py"
          && grep -Fq 'current_non_canonical(findings) == []' "$d/tests/unit/scripts/ci/test_canonical_handler_shape.py"
          && grep -Fq 'selected_input_hashes' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_compliance_scan_compute.equivalence.json"
          && grep -Fq 'selected_input_hashes' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_compliance_report_reducer.equivalence.json"
          && grep -Fq 'selected_input_hashes' "$d/scripts/ci/adequacy_receipts/omnibase_core.nodes.node_compliance_evidence_effect.equivalence.json"
  - id: "dod-core-pr-1443-head-e44e25d7-local-verification"
    description: >-
      Local verification proof for omnibase_core PR #1443 head e44e25d7b852976f6ccb0db4438d558a030c3d91
      after rebasing onto the merged rank1 branch. The product PR body records the focused 99-test suite,
      the full canonical-handler-shape ratchet with zero warnings and zero unproven flips, and focused
      pre-commit proof.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1443; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=e44e25d7b852976f6ccb0db4438d558a030c3d91;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,body --jq '.headRefOid
          == "'$PRODUCT_HEAD'" and (.body | contains("Merge-sweep rebase verification")) and (.body |
          contains("99 passed")) and (.body | contains("new=0 unproven_flips=0 warn=0")) and (.body |
          contains("pre-commit run --files scripts/ci/canonical_handler_shape_baseline.py tests/unit/scripts/ci/test_canonical_handler_shape.py"))'
          | grep -qx true
  - id: "dod-core-pr-1443-head-e44e25d7-deploy-scope"
    description: >-
      Deploy-surface assessment for omnibase_core PR #1443 head e44e25d7b852976f6ccb0db4438d558a030c3d91.
      The rank2 diff is Python handler/model/test content plus adequacy receipts and contract YAML for
      core nodes; it does not touch Dockerfiles, compose manifests, deploy scripts, migrations, runtime
      service definitions, or infrastructure configuration.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=1443; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=e44e25d7b852976f6ccb0db4438d558a030c3d91;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid --jq '.headRefOid ==
          "'$PRODUCT_HEAD'"' | grep -qx true && ! gh pr diff "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO"
          --name-only | grep -qE '(^|/)(Dockerfile|docker-compose[^/]*|compose[^/]*|deploy|migrations)(/|$)'
