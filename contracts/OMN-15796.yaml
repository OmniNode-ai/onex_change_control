---
schema_version: "1.0.0"
ticket_id: "OMN-15796"
title: "Autobind OCC evidence for OMN-15796"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omninode_infra PR #1020
  and OmniNode-ai/omnibase_infra PR #2912.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1020 and PR #2912 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1020"
    description: "PR #1020 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1020 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: "gh pr view 1020 --repo OmniNode-ai/omninode_infra --json files"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2912"
    description: "PR #2912 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/ci/test_dev_candidate_delivery.py?ref=c472ac971a160f5ffbc1d3a10da6a3baa873ed74 --jq '.content' | base64 -d | grep -c 'def _load'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/ci/test_dev_candidate_delivery.py?ref=c472ac971a160f5ffbc1d3a10da6a3baa873ed74 --jq '.content' | base64 -d | grep -c 'def _load'
  - id: "dod-occ-evidence-admissibility-validator"
    description: "Hosted OCC evidence admissibility validator (OMN-15247)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1020"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_evidence_admissibility.py -q"
  - id: "occ-self-bind-pr-7215"
    description: "Binds OMN-15796 to OCC companion PR #7215 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7215 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-7220"
    description: "Binds OMN-15796 to OCC companion PR #7220 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 7220 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omn15796-live-delivery-readback-20260908"
    description: |-
      OMN-15796 AC (a)/(b)/(c) plus the proof clause, proven live on onex-dev (EC2 i-06169517a92b45f86) on 2026-09-08. An omnibase_infra dev merge fires deliver-dev-candidate-to-staging.yml on push, which builds the runtime candidate and the migrate bundle from that same commit and announces the pair to omninode_infra by repository_dispatch; deploy-onex-staging.yml applies both digests at deploy time, runs the migrations, pins and stamps the runtime plane, and verifies built == deployed == running. Proven twice, on a commit with no migration and on a commit that adds one.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh run view 34176437357 --repo OmniNode-ai/omninode_infra --json jobs --jq '[.jobs[]|select(.name=="deploy-staging")|.steps[]|select(.number==3 or .number==19 or .number==33 or .number==34 or .number==43 or .number==44 or .number==45)|"\(.number):\(.conclusion)"]|join(" ")'
  - id: "occ-self-bind-pr-8628"
    description: >-
      OCC self-binding receipt for PR #8628, the in-repo evidence PR carrying this contract. Binds OMN-15796
      to the OCC PR by pr_number so eligibility can resolve a PASS receipt for the evidence PR itself.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8628 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
