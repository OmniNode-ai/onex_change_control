---
schema_version: "1.0.0"
ticket_id: "OMN-14779"
title: "B12 to B6 canary DDL/model seam fingerprint"
summary: >-
  OCC Evidence-Source companion for omnibase_infra PR #2402. The product PR adds a packaged seam fingerprint
  for the B12 delivery replay canary landing table and a unit test that parses the real DDL and verifies
  the packaged fingerprint, preserving the repo layering that prevents omnibase_infra from importing omnimarket
  directly.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #2402 is open at the expected product head."
    command: "gh pr view 2402 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid,headRefName"
  - kind: "ci"
    description: "omnibase_infra PR #2402 diff carries the seam artifact, DDL seam test, and mechanical
      node migration vendor-sync files only."
    command: "gh pr view 2402 --repo OmniNode-ai/omnibase_infra --json files"
  - kind: "tests"
    description: "The new seam test is present in the product PR diff."
    command: "gh pr diff 2402 --repo OmniNode-ai/omnibase_infra --name-only"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase-infra-pr-2402-head"
    description: "Binds omnibase_infra PR #2402 at head 44188dd270fe9e4331785f939303d59c43c75f62."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2402 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid,headRefName"
  - id: "dod-omnibase-infra-pr-2402-diff-scope"
    description: >-
      PR #2402 product diff adds src/omnibase_infra/contracts/canary/model_replay_projection_seam.json
      and tests/unit/docker/test_canary_ddl_seam_fingerprint.py, plus three mechanical node migration
      vendor-sync files already mirrored by omnibase_infra#2401. This evidence authorizes no live runtime
      deploy, .201 mutation, docker exec, DB migration apply, or omnimarket model-side follow-up.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 2402 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-omnibase-infra-pr-2402-seam-test"
    description: >-
      The product PR contains the DDL seam fingerprint test path that parses the real canary DDL and asserts
      parity with the packaged seam JSON. Hosted product CI already ran the shadow tests green at the
      product head before this OCC companion; this receipt binds the proof surface without re-authoring
      product code.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr diff 2402 --repo OmniNode-ai/omnibase_infra --name-only"
