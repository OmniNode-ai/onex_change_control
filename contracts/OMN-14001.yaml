---
schema_version: "1.0.0"
ticket_id: "OMN-14001"
title: "feat(OMN-14001): close first platform learning loop via delegation ROI routing"
summary: >-
  omnimarket PR #1629 closes the first platform learning loop by making stored context ROI outcomes influence
  live delegation routing. The implementation keeps the routing reducer pure by resolving the ROI overlay
  at the I/O boundary, then passing that in-memory overlay through initial resolution and escalation hops.
  The change is fail-open when projection reads are unavailable and falls back to statically routable
  tiers when ROI suppression would otherwise dead-end routing.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: >-
      Focused omnimarket unit coverage proves ROI overlay aggregation, fail-open behavior, reducer tier
      flips, dispatch-port wiring, and the capture-to-read-to-decision capstone.
    command: "uv run pytest tests/unit/delegation/test_roi_overlay_routing_omn14001.py -v"
  - kind: "docs"
    description: "Evidence document records the closed learning loop proof and live-lane caveat."
    command: "test -f docs/evidence/M5-capstone-dogfood/2026-07-06-learning-loop-closed-OMN-14001.md"
  - kind: "manual"
    description: "Deploy-scope proof is bound by the stored PASS receipt, not by live SSH from CI."
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14001/dod-deploy-scope/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1629"
    description: >-
      Consuming-PR binding: omnimarket PR #1629 at head a5925dabdacf12c90fdbd755a58b89208530bae2 carries
      the ROI overlay reader, pure reducer threading, dispatch-port integration, and focused unit tests
      for OMN-14001.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14001/dod-omnimarket-pr-1629/command.yaml\""
  - id: "dod-learning-loop-proof"
    description: >-
      Proof binding: the omnimarket PR body and evidence doc record the canonical capture-to-read-to-decision
      test, while explicitly noting that no live .201 readback was performed because context_roi_scores
      is absent on live dev/stability lanes.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14001/dod-learning-loop-proof/command.yaml\""
  - id: "dod-deploy-scope"
    description: >-
      Deploy-scope binding: this runtime-path delegation routing change rides the normal post-merge dev
      and stability redeploy cadence; no out-of-band production deploy is in scope. The .201 dev-lane
      runtime containers were observed healthy for post-merge validation.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14001/dod-deploy-scope/command.yaml\""
  - id: "dod-occ-self"
    description: >-
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-14001 central contract and
      PASS receipts consumed by omnimarket PR #1629 via its Evidence-Source line.
    source: "generated"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-14001/dod-occ-self/command.yaml\""
