---
schema_version: "1.0.0"
ticket_id: "OMN-15011"
title: "RSD emission-side provenance stamp"
summary: "OCC companion evidence for omnimarket PR #1877, which emits RSD provenance stamps for generated
  nodes."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "PR #1877 product diff scope and CI evidence are present."
    command: "gh pr checks 1877 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1877"
    description: "PR #1877 on OmniNode-ai/omnimarket -- RSD provenance emitter diff is present."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1877 --repo OmniNode-ai/omnimarket --json number,state,files"
  - id: "dod-OmniNode-ai-omnimarket-pr-1877-ci"
    description: "PR #1877 on OmniNode-ai/omnimarket -- CI evidence binding for the RSD provenance emitter."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' $CONTRACT_REPO_DIR/drift/dod_receipts/OMN-15011/dod-OmniNode-ai-omnimarket-pr-1877-ci/command.yaml"
  - id: "occ-self-bind-pr-4731"
    description: "OCC companion PR #4731 -- self-bind for OMN-15011 omnimarket#1877 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4731 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-omnimarket-pr-1877-deploy-gate-disposition"
    description: "PR #1877 deploy-gate disposition -- runtime handler path touched and post-merge dev
      redeploy validation bar is explicit in the PR body."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1877 --repo OmniNode-ai/omnimarket --json body,files --jq 'if ((.body
          | test(\"Post-merge dev redeploy\")) and any(.files[].path; . == \"src/omnimarket/nodes/node_hybrid_codegen_orchestrator/handlers/handler_hybrid_codegen_orchestrator.py\"))
          then \"deploy gate disposition verified: runtime handler path touched; PR body carries post-merge
          dev redeploy validation bar\" else error(\"deploy gate disposition missing runtime path or PR
          body deploy bar\") end'"
