---
schema_version: '1.0.0'
ticket_id: OMN-10041
summary: 'feat(governance): make Receipt-Gate + CodeRabbit Thread Check required status checks across
  11 repos'
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: All 11 repos probed; eligible repos enrolled as required status checks
    command: 'bash -c ''for r in omniclaude omnibase_core omnibase_infra omnibase_spi omnidash omniintelligence
      omnimemory omninode_infra omniweb onex_change_control omnibase_compat; do gh api repos/OmniNode-ai/$r/branches/main/protection
      --jq .required_status_checks.contexts; done'''
  - kind: docs
    description: Governance doc captures final state and follow-ups
    command: test -f docs/governance/2026-04-27-required-gates-rollout.md
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: omnibase_core requires both 'verify / verify' and 'gate / CodeRabbit Thread Check'
    source: generated
    checks:
      - check_type: command
        check_value: 'gh api repos/OmniNode-ai/omnibase_core/branches/main/protection --jq ''.required_status_checks.contexts
          | contains(["verify / verify", "gate / CodeRabbit Thread Check"])'' | grep -qx true'
  - id: dod-002
    description: omnibase_infra requires both 'verify / verify' and 'gate / CodeRabbit Thread Check'
    source: generated
    checks:
      - check_type: command
        check_value: 'gh api repos/OmniNode-ai/omnibase_infra/branches/main/protection --jq ''.required_status_checks.contexts
          | contains(["verify / verify", "gate / CodeRabbit Thread Check"])'' | grep -qx true'
  - id: dod-003
    description: omniclaude requires 'gate / CodeRabbit Thread Check'
    source: generated
    checks:
      - check_type: command
        check_value: 'gh api repos/OmniNode-ai/omniclaude/branches/main/protection --jq ''.required_status_checks.contexts
          | contains(["gate / CodeRabbit Thread Check"])'' | grep -qx true'
  - id: dod-004
    description: omnibase_spi requires 'gate / CodeRabbit Thread Check'
    source: generated
    checks:
      - check_type: command
        check_value: 'gh api repos/OmniNode-ai/omnibase_spi/branches/main/protection --jq ''.required_status_checks.contexts
          | contains(["gate / CodeRabbit Thread Check"])'' | grep -qx true'
  - id: dod-005
    description: omnimemory requires 'gate / CodeRabbit Thread Check'
    source: generated
    checks:
      - check_type: command
        check_value: 'gh api repos/OmniNode-ai/omnimemory/branches/main/protection --jq ''.required_status_checks.contexts
          | contains(["gate / CodeRabbit Thread Check"])'' | grep -qx true'
  - id: dod-006
    description: onex_change_control requires 'gate / CodeRabbit Thread Check' (already present pre-rollout,
      asserted for drift protection)
    source: generated
    checks:
      - check_type: command
        check_value: 'gh api repos/OmniNode-ai/onex_change_control/branches/main/protection --jq ''.required_status_checks.contexts
          | contains(["gate / CodeRabbit Thread Check"])'' | grep -qx true'
  - id: dod-007
    description: omnibase_compat requires 'gate / CodeRabbit Thread Check'
    source: generated
    checks:
      - check_type: command
        check_value: 'gh api repos/OmniNode-ai/omnibase_compat/branches/main/protection --jq ''.required_status_checks.contexts
          | contains(["gate / CodeRabbit Thread Check"])'' | grep -qx true'
  - id: dod-008
    description: Governance rollout doc present in this PR
    source: generated
    checks:
      - check_type: file_exists
        check_value: docs/governance/2026-04-27-required-gates-rollout.md
  - id: dod-009
    description: PR cites this ticket
    source: generated
    checks:
      - check_type: command
        check_value: 'gh pr view ${PR_NUMBER} --repo ${REPO} --json title,body --jq ''.title + " " + (.body
          // "")'' | grep -q ''${TICKET_ID}'''
