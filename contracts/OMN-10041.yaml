---
schema_version: '1.0.0'
ticket_id: OMN-10041
summary: 'feat(governance): make Receipt-Gate + CodeRabbit Thread Check required status checks across
  11 repos'
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: Committed rollout baseline captures live probes and final required status-check enrollment
    command: "grep -F '## Apply (after) — verified 2026-04-27' docs/tracking/2026-04-27-omn-10041-required-gates-baseline.md
      && grep -F '| omnibase_compat | N | **Y** | `gate / CodeRabbit Thread Check` |' docs/tracking/2026-04-27-omn-10041-required-gates-baseline.md"
  - kind: docs
    description: Governance doc captures final state and follow-ups
    command: test -f docs/governance/2026-04-27-required-gates-rollout.md
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: omnibase_core requires both 'verify / verify' and 'gate / CodeRabbit Thread Check'
    source: generated
    checks:
      - check_type: command
        check_value: "grep -F '| omnibase_core | **Y** | **Y** | `verify / verify`, `gate / CodeRabbit
          Thread Check` |' docs/tracking/2026-04-27-omn-10041-required-gates-baseline.md"
  - id: dod-002
    description: omnibase_infra requires both 'verify / verify' and 'gate / CodeRabbit Thread Check'
    source: generated
    checks:
      - check_type: command
        check_value: "grep -F '| omnibase_infra | **Y** | **Y** | `verify / verify`, `gate / CodeRabbit
          Thread Check` |' docs/tracking/2026-04-27-omn-10041-required-gates-baseline.md"
  - id: dod-003
    description: omniclaude requires 'gate / CodeRabbit Thread Check'
    source: generated
    checks:
      - check_type: command
        check_value: "grep -F '| omniclaude | N | **Y** | `gate / CodeRabbit Thread Check` |' docs/tracking/2026-04-27-omn-10041-required-gates-baseline.md"
  - id: dod-004
    description: omnibase_spi requires 'gate / CodeRabbit Thread Check'
    source: generated
    checks:
      - check_type: command
        check_value: "grep -F '| omnibase_spi | N | **Y** | `gate / CodeRabbit Thread Check` |' docs/tracking/2026-04-27-omn-10041-required-gates-baseline.md"
  - id: dod-005
    description: omnimemory requires 'gate / CodeRabbit Thread Check'
    source: generated
    checks:
      - check_type: command
        check_value: "grep -F '| omnimemory | N | **Y** | `gate / CodeRabbit Thread Check` |' docs/tracking/2026-04-27-omn-10041-required-gates-baseline.md"
  - id: dod-006
    description: onex_change_control requires 'gate / CodeRabbit Thread Check' (already present pre-rollout,
      asserted for drift protection)
    source: generated
    checks:
      - check_type: command
        check_value: "grep -F '| onex_change_control | N | **Y** (already) |' docs/tracking/2026-04-27-omn-10041-required-gates-baseline.md"
  - id: dod-007
    description: omnibase_compat requires 'gate / CodeRabbit Thread Check'
    source: generated
    checks:
      - check_type: command
        check_value: "grep -F '| omnibase_compat | N | **Y** | `gate / CodeRabbit Thread Check` |' docs/tracking/2026-04-27-omn-10041-required-gates-baseline.md"
  - id: dod-008
    description: Governance rollout doc present in this PR
    source: generated
    checks:
      - check_type: file_exists
        check_value: docs/governance/2026-04-27-required-gates-rollout.md
  - id: dod-009
    description: PR cites this ticket
    source: generated
    checks:
      - check_type: command
        check_value: 'gh pr view ${PR_NUMBER} --repo ${REPO} --json title,body --jq ''.title + " " + (.body
          // "")'' | grep -q ''${TICKET_ID}'''
