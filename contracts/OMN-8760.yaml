---
schema_version: "1.0.0"
ticket_id: OMN-8760
title: "Implement node_bus_audit_compute"
summary: "Promote node_bus_audit_compute from an explicit stub to a deterministic event-bus registry and
  contract wiring audit node."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
  - "events"
  - "topics"
evidence_requirements:
  - kind: "manual"
    description: "Bus audit golden-chain and explicit-stub tests pass"
    command: "uv run pytest tests/test_golden_chain_bus_audit_compute.py tests/test_golden_chain_new_stub_nodes.py
      -q"
  - kind: "manual"
    description: "Touched bus audit paths pass lint"
    command: "uv run ruff check src/omnimarket/nodes/node_bus_audit_compute tests/test_golden_chain_bus_audit_compute.py
      tests/test_golden_chain_new_stub_nodes.py"
  - kind: "manual"
    description: "Bus audit CLI emits JSON and exits zero against the repo"
    command: "uv run python -m omnimarket.nodes.node_bus_audit_compute --dry-run --json --failures-only"
  - kind: "manual"
    description: "Post-merge deploy smoke imports the bus audit CLI in the runtime container"
    command: "docker exec omninode-runtime python -m omnimarket.nodes.node_bus_audit_compute --help"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Bus audit golden-chain and explicit-stub tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_golden_chain_bus_audit_compute.py tests/test_golden_chain_new_stub_nodes.py
          -q"
  - id: "dod-lint"
    description: "Touched bus audit paths pass lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_bus_audit_compute tests/test_golden_chain_bus_audit_compute.py
          tests/test_golden_chain_new_stub_nodes.py"
  - id: "dod-cli"
    description: "Bus audit CLI emits JSON and exits zero against the repo"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run python -m omnimarket.nodes.node_bus_audit_compute --dry-run --json --failures-only"
  - id: "dod-deploy"
    description: "Post-merge deploy smoke imports the bus audit CLI in the runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec omninode-runtime python -m omnimarket.nodes.node_bus_audit_compute --help"
