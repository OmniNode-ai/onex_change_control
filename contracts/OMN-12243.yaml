---
schema_version: "1.0.0"
ticket_id: "OMN-12243"
title: "Install main-target-guard on release/control repositories"
summary: >
  Install the OMN-12243 main-target guard workflow so PRs targeting main are rejected unless they are
  dev-to-main promotion PRs with a promotion receipt, or hotfix PRs with OCC evidence and a backmerge
  reference.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-main-target-guard-bootstrap-prs"
    description: "Bootstrap PRs add main-target-guard workflow to the release/control repository set."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12243/dod-main-target-guard-bootstrap-prs/command.yaml"
  - id: "dod-omnibase-infra-pr-1768"
    description: "omnibase_infra#1768 installs the main-target guard workflow with scoped read-only permissions."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12243/dod-omnibase-infra-pr-1768/command.yaml"
  - id: "dod-omniclaude-pr-1684"
    description: "omniclaude#1684 installs the main-target guard workflow with scoped read-only permissions."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12243/dod-omniclaude-pr-1684/command.yaml"
  - id: "dod-omnimarket-pr-931"
    description: "omnimarket#931 installs the main-target guard workflow with scoped read-only permissions."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12243/dod-omnimarket-pr-931/command.yaml"
