---
schema_version: "1.0.0"
ticket_id: "OMN-11075"
title: "Wait for scored delegation terminal payload in delegate-skill node"
summary: >
  Central OCC evidence for omnimarket PR #660. The delegate-skill orchestrator now routes runtime dispatch
  directly to the delegation request topic and waits on the scored onex.evt.omnibase-infra.delegation-completed/failed
  terminal events. ModelDelegateSkillResponse exposes quality_score, total tokens, tokens_to_compliance,
  compliance_attempts, and cost_savings_usd estimation. Contract dependency topics now reference the actual
  omnibase-infra delegation topics.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused delegate-skill orchestrator + delegate adapter + cross-repo contract dep tests
      pass."
    command: "See drift/dod_receipts/OMN-11075/dod-tests/command.yaml"
  - kind: "ci"
    description: "Central OCC PR carries the OMN-11075 contract and receipt bundle."
    command: "See drift/dod_receipts/OMN-11075/dod-occ-pr-1052/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Focused delegate-skill, delegate adapter, and cross-repo contract dep tests pass."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q 'status: PASS' drift/dod_receipts/OMN-11075/dod-tests/command.yaml && grep
          -q 'pr_number: 660' drift/dod_receipts/OMN-11075/dod-tests/command.yaml"
  - id: "dod-occ-pr-1052"
    description: "onex_change_control PR #1052 carries the central OMN-11075 contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11075/dod-occ-pr-1052/command.yaml
          && grep -q '^pr_number: 1052$' drift/dod_receipts/OMN-11075/dod-occ-pr-1052/command.yaml"
