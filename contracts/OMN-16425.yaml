---
schema_version: "1.0.0"
ticket_id: "OMN-16425"
title: "test_prepush_hook_host_identity_guard.py subprocess inherits PREPUSH_ALLOW_LOCAL_FULL_SUITE, causing
  recursive full-suite spawns"
summary: >-
  Manually-authored OCC contract for OmniNode-ai/omnibase_core PR #1580, which fixes OMN-16425 as a bonus
  fix alongside its primary OMN-16432 change (both landed in the same PR after OMN-16425's env-leak recursion
  was reproduced live three times while pushing OMN-16432 through the governed pre-push gate). PR #1580's
  own autobind companion (OCC#6973) carries OMN-16432's evidence; this contract covers OMN-16425 specifically.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_core-pr-1580-omn16425-fix"
    description: >-
      PR #1580 adds env.pop("PREPUSH_ALLOW_LOCAL_FULL_SUITE", None) to test_guard_refuses_full_suite_escalation_on_non_200_host
      in tests/scripts/test_prepush_hook_host_identity_guard.py, stripping the leaky var from the subprocess
      env it builds before spawning the real pre-push hook script -- the exact OMN-16425 defect (PREPUSH_ALLOW_LOCAL_FULL_SUITE
      leaking from the outer git push's ambient env into this test's subprocess, causing the hook to take
      the DEGRADED-HOST-OVERRIDE branch instead of refusing and recursively launch another full-suite
      pytest run). Content-bound: reads the fixed file at the PR head SHA through the GitHub API, a repo
      this OCC PR does not author.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api 'repos/OmniNode-ai/omnibase_core/contents/tests/scripts/test_prepush_hook_host_identity_guard.py?ref=40103bbde511e5f4cb8f78f14401efcdb38d8a6e'
          --jq '.content' | base64 -d | grep -F 'env.pop("PREPUSH_ALLOW_LOCAL_FULL_SUITE", None)'
  - id: "occ-self-bind-pr-6973"
    description: "Binds OMN-16425 to OCC companion PR #6973 (self-bind, shared with OMN-16432 -- both
      tickets are closed by the same product PR #1580)."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "gh pr view 6973 --repo OmniNode-ai/onex_change_control --json number,state"
