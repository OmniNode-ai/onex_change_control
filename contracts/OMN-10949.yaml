---
schema_version: "1.0.0"
ticket_id: "OMN-10949"
title: "Project delegation cost fields"
summary: "Binds omnimarket PR #616, which adds cost fields to the task-delegated projection model and
  row output."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Downstream PR #616 reports golden-chain projection coverage for delegation cost fields."
    command: "gh pr view 616 --repo OmniNode-ai/omnimarket --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-616"
    description: "omnimarket PR #616 carries the OMN-10949 projection cost-field implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10949/dod-omnimarket-pr-616/command.yaml
          && grep -q '^pr_number: 616$' drift/dod_receipts/OMN-10949/dod-omnimarket-pr-616/command.yaml"
  - id: "dod-omnibase-infra-pr-1588"
    description: "omnibase_infra PR #1588 adds pricing_manifest_version to ModelTaskDelegatedEvent."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10949/dod-omnibase-infra-pr-1588/command.yaml
          && grep -q '^pr_number: 1588$' drift/dod_receipts/OMN-10949/dod-omnibase-infra-pr-1588/command.yaml"
  - id: "dod-omnimarket-pr-622"
    description: "omnimarket PR #622 persists pricing_manifest_version in delegation_events projection."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10949/dod-omnimarket-pr-622/command.yaml
          && grep -q '^pr_number: 622$' drift/dod_receipts/OMN-10949/dod-omnimarket-pr-622/command.yaml"
  - id: "deploy-evidence-pricing-version"
    description: "pricing_manifest_version is a schema-only additive field — no deploy step required.
      Event consumers apply extra='ignore' so old events default to version 0 without restart."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q 'pricing_manifest_version' drift/dod_receipts/OMN-10949/dod-omnibase-infra-pr-1588/command.yaml
          && echo 'deploy: no runtime restart needed — additive field with backward-compatible default'"
