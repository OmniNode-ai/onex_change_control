---
schema_version: "1.0.0"
ticket_id: "OMN-12322"
title: "High-risk OmniMarket orchestration handlers"
summary: >
  Bind central evidence for omnimarket PR #954, which replaces runtime-routable high-risk skill-to-node
  stubs with bounded deterministic handler slices and adapter-gated live side effects.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - topics
  - protocols
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-954"
    description: "Bind OMN-12322 evidence to omnimarket PR #954."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12322/dod-omnimarket-pr-954/command.yaml
          && grep -q '^pr_number: 954$' drift/dod_receipts/OMN-12322/dod-omnimarket-pr-954/command.yaml"
  - id: "dod-focused-quality-gates"
    description: "Focused ruff, mypy, and pytest gates passed for the high-risk handler slice."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12322/dod-focused-quality-gates/command.yaml
          && grep -q '174 passed' drift/dod_receipts/OMN-12322/dod-focused-quality-gates/command.yaml"
  - id: "dod-deploy-readiness"
    description: "Deploy readiness evidence for runtime handler changes with no live side effects in default
      paths."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12322/dod-deploy-readiness/command.yaml
          && grep -q 'deploy readiness: live side effects require injected adapters' drift/dod_receipts/OMN-12322/dod-deploy-readiness/command.yaml"
  - id: "dod-occ-pr-1804"
    description: "Bind OMN-12322 central evidence to onex_change_control PR #1804."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12322/dod-occ-pr-1804/command.yaml
          && grep -q '^pr_number: 1804$' drift/dod_receipts/OMN-12322/dod-occ-pr-1804/command.yaml"
