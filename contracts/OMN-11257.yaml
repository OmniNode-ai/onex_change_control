---
schema_version: "1.0.0"
ticket_id: "OMN-11257"
title: "feat(OMN-11241): Wave 4 Task 12 — Demo script rework — use real cross-system pipeline"
summary: >
  DeploymentClient (SCP+SSH). Rework demo.py with deploy step and --progressive mode (3 runs showing learning).
  Record deployment events in chain. No hardcoded paths.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "DeploymentClient mockable via subprocess. --progressive runs 3 tasks showing learning
      improvement."
    command: "gh pr view 24 --repo OmniNode-ai/onex-self-extending-agent --json number,state"
  - kind: "ci"
    description: "PR #24 open with CI passing"
    command: "gh pr view 24 --repo OmniNode-ai/onex-self-extending-agent --json number,state,statusCheckRollup"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-24"
    description: "onex-self-extending-agent PR #24 — demo.py rework with real cross-system pipeline. DeploymentClient
      via SCP+SSH, --progressive mode showing 3-run learning improvement, deployment events recorded in
      chain."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11257/dod-pr-24/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carrying this contract and binding receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11257/dod-occ-pr/command.yaml"
