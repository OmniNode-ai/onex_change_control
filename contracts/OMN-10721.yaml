---
schema_version: "1.0.0"
ticket_id: "OMN-10721"
title: "Track omniintelligence mako dependabot bump"
summary: >
  Binds omniintelligence Dependabot PR #642, which bumps mako from 1.3.11 to 1.3.12, to central OCC evidence
  so Receipt Gate can verify the dependency maintenance update without bypassing branch protection.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-642-head"
    description: "omniintelligence PR #642 carries the mako dependency bump head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10721/dod-pr-642-head/command.yaml\""
  - id: "dod-ci-642-green"
    description: "All non-receipt CI checks on omniintelligence PR #642 are passing."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10721/dod-ci-642-green/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10721/dod-occ-pr/command.yaml\""
