---
schema_version: "1.0.0"
ticket_id: "OMN-12677"
title: "Delegation routing reducer gates on secret-reference presence only"
summary: >-
  Central evidence for the routing reducer change that stops resolving backend.api_key_env values. The
  reducer checks only whether the declared environment reference is present and carries the reference
  forward.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-routing-presence-tests"
    description: "Focused omnimarket routing tests prove api_key_env presence is enough and absent refs
      remain unroutable."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12677/dod-routing-presence-tests/command.yaml"
