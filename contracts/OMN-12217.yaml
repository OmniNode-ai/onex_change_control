---
schema_version: "1.0.0"
ticket_id: "OMN-12217"
title: "Create node_observability_sink_effect — fix bus bypass"
summary: >
  OCC evidence for omnimarket PR #913. The change adds node_observability_sink_effect (EFFECT_GENERIC)
  to omnimarket to fix the architectural violation where the observability skill made direct
  ActionLogger + Postgres writes bypassing node dispatch. Contract-only stub with
  node_not_implemented: true and 14 unit tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-golden-chain"
    description: "14 unit tests pass for node_observability_sink_effect on omnimarket PR #913 head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12217/dod-golden-chain/command.yaml && grep -q '14 passed' drift/dod_receipts/OMN-12217/dod-golden-chain/command.yaml"
  - id: "dod-deploy-assessment"
    description: "No live deploy or runtime restart is required before merge for this contract + handler-stub slice."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12217/dod-deploy-assessment/command.yaml && grep -q 'no live deploy or runtime restart required before merge' drift/dod_receipts/OMN-12217/dod-deploy-assessment/command.yaml"
