---
schema_version: "1.0.0"
ticket_id: "OMN-12403"
title: "Migrate node_dod_verify _build_receipt() to ModelDodReceipt"
summary: >
  node_dod_verify/__main__.py::_build_receipt() emitted a legacy receipt dict with timestamp, git_sha,
  and result keys that the DoD completion guard rejects. This migrates _build_receipt() to emit ModelDodReceipt
  (OMN-9792) matching the reference implementation in omniclaude dod_evidence_runner.write_evidence_receipt().
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-974"
    description: "omnimarket PR #974 migrates _build_receipt() to ModelDodReceipt and updates tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12403/dod-omnimarket-pr-974/command.yaml\"
          && grep -q '^pr_number: 974$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12403/dod-omnimarket-pr-974/command.yaml\""
  - id: "dod-deploy-assessment"
    description: >
      Schema-only change to receipt output: no Docker container, runtime process, or Kafka topic is modified.
      The fix changes the JSON keys written to dod_report.json on disk. No service restart is required.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12403/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-1823"
    description: "onex_change_control PR #1823 carries the OMN-12403 central contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12403/dod-occ-pr-1823/command.yaml\"
          && grep -q '^pr_number: 1823$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12403/dod-occ-pr-1823/command.yaml\""
