---
schema_version: "1.0.0"
ticket_id: "OMN-13579"
title: "Onboard ungoverned public repos into the validator spec (omnibase, omnicursor, onex-self-extending-agent,
  knowledge-base)"
summary: >-
  Adds four previously ungoverned public repos (omnibase, omnicursor, onex-self-extending-agent, knowledge-base)
  to known_repos in architecture-handshakes/validator-requirements.yaml so universal validators (applies_to_repos:
  "all") apply automatically and Python-specific validators are scoped per-repo based on language and
  content. schema_version bumped 1.3.0 → 1.4.0. Three new tests added to test_validator_requirements_spec.py
  to lock the contract.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-spec-change"
    description: >-
      Each onboarded repo appears in known_repos (spec version 1.4.0). Universal validators (applies_to_repos:
      "all") cover all four repos automatically once they are in known_repos. Python-specific validators
      (stub-implementations, enum-governance, naming-conventions, mypy-type-check, ruff-format-check,
      bandit-security, pydantic-patterns, single-class-per-file) apply to omnicursor + onex-self-extending-agent
      + knowledge-base (all have src/ + pyproject.toml). no-hardcoded-topics applies to omnicursor and
      onex-self-extending-agent (hardcoded topic literals in source). doc-content-scan applies to all
      four repos (docs/ exist). omnibase (meta/registry, no Python src) is governed by universal validators
      only — intentionally excluded from Python validators with rationale in inline comments.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13579/dod-spec-change/command.yaml
  - id: "dod-tests-green"
    description: >-
      Three new tests pass: test_new_public_repos_in_known_repos, test_new_public_repos_covered_by_universal_validators,
      test_doc_content_scan_applies_to_knowledge_base. All 328 validation tests pass on the PR branch.
      ruff + pre-commit green.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13579/dod-tests-green/command.yaml
  - id: "dod-occ-pr-self"
    description: >-
      Self-binding receipt for this OCC contract. The receipt binds onex_change_control PR #3120 by pr_number
      so OCC preflight and the OCC PR Receipt Gate can resolve a PASS receipt for the evidence carrier.
      The feature PR omnibase_core#1338 cites this OCC evidence PR as its downstream Receipt-Gate source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13579/dod-occ-pr-self/command.yaml
