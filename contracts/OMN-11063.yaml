---
schema_version: "1.0.0"
ticket_id: OMN-11063
title: "Dep-health baseline phased CI rollout"
summary: "Repair dep-health baseline rollout for omnimarket PR #698 by normalizing the baseline, aligning
  CI and the standalone dep-health gate on delta-mode, and binding the PR to central receipt evidence."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - protocols
  - public_api
evidence_requirements:
  - kind: tests
    description: "Focused dep-health workflow and script tests pass."
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-11063/omnimarket-698-polish
      && uv run pytest tests/validation/test_dep_health_precommit_hook.py tests/ci/test_dep_health_workflow_yaml.py
      tests/ci/test_run_dep_health_sweep_script.py -q"
  - kind: manual
    description: "Leaked literal gate and dep-health delta gate pass locally."
    command: "cd ${OMNI_HOME:-/Volumes/PRO-G40/Code/omni_home}/omni_worktrees/OMN-11063/omnimarket-698-polish
      && bash scripts/validation/check_leaked_literals.sh blocking all && uv run python scripts/ci/run_dep_health_sweep.py
      --repo-roots src/ --severity-threshold MAJOR --baseline-path .onex_state/dep_health_baseline.json
      --delta-mode"
  - kind: ci
    description: "omnimarket#698 carries the OMN-11063 dep-health delta gate repair."
    command: "gh pr checks 698 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-local-validation
    description: "Focused dep-health tests, leaked literal gate, ruff, hook, and delta gate pass locally."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11063/dod-local-validation/command.yaml
          && grep -q '^ticket_id: OMN-11063$' drift/dod_receipts/OMN-11063/dod-local-validation/command.yaml"
  - id: dod-omnimarket-pr-698
    description: "omnimarket PR #698 is bound to OMN-11063 for dep-health phased rollout repair."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11063/dod-omnimarket-pr-698/command.yaml
          && grep -q '^ticket_id: OMN-11063$' drift/dod_receipts/OMN-11063/dod-omnimarket-pr-698/command.yaml
          && grep -q '^pr_number: 698$' drift/dod_receipts/OMN-11063/dod-omnimarket-pr-698/command.yaml"
  - id: dod-occ-pr-1217
    description: "onex_change_control PR #1217 self-binds the OMN-11063 omnimarket#698 receipt repair."
    source: manual
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11063/dod-occ-pr-1217/command.yaml
          && grep -q '^ticket_id: OMN-11063$' drift/dod_receipts/OMN-11063/dod-occ-pr-1217/command.yaml
          && grep -q '^pr_number: 1217$' drift/dod_receipts/OMN-11063/dod-occ-pr-1217/command.yaml"
