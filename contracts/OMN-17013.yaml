---
schema_version: "1.0.0"
ticket_id: "OMN-17013"
title: "Autobind OCC evidence for OMN-17013"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_compat PR #199.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #199 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #199 on OmniNode-ai/omnibase_compat, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_compat`.
    command: |-
      uv run pytest src/omnibase_compat/tests/env/test_wheel_packaging.py tests/unit/contracts/delegation/terminal_v2/test_model_delegation_terminal_transport.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_compat-pr-199"
    description: "PR #199 on OmniNode-ai/omnibase_compat — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_compat/contents/src/omnibase_compat/contracts/delegation/terminal_v2/model_delegation_terminal_transport.py?ref=82bf9531a853bc3e170bc44842a23c4b40915ac3 --jq '.content' | base64 -d | grep -c 'class EnumDelegationRoutingDisposition'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_compat/pulls/199/files --paginate --jq '.[].filename' | grep -cFx 'CHANGELOG.md'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #199 on OmniNode-ai/omnibase_compat — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest src/omnibase_compat/tests/env/test_wheel_packaging.py tests/unit/contracts/delegation/terminal_v2/test_model_delegation_terminal_transport.py -q
        cwd: "${OMNI_HOME}/omnibase_compat"
  - id: "occ-self-bind-pr-8001"
    description: "Binds OMN-17013 to OCC companion PR #8001 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8001 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omnibase_compat-pr-199-final-head"
    description: >-
      Append-only correction for the historical PR #199 evidence item. The old item pinned the pre-merge
      compat commit 82bf9531; this successor binds one durable predicate to immutable final product head
      5304ca43 and proves the terminal-v2 routing/unrouted models plus strict backend-ref validator there.
      Historical evidence and receipts remain untouched.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omnibase_compat-pr-199"
    checks:
      - check_type: "command"
        check_value: >-
          content="$(gh api 'repos/OmniNode-ai/omnibase_compat/contents/src/omnibase_compat/contracts/delegation/terminal_v2/model_delegation_terminal_transport.py?ref=5304ca43c962622e5b6919b181ebc5c0998a68c5'
          --jq '.content' | base64 -d)" && printf '%s\n' "$content" | grep -Fq 'class EnumDelegationRoutingDisposition'
          && printf '%s\n' "$content" | grep -Fq 'class EnumDelegationUnroutedReason' && printf '%s\n'
          "$content" | grep -Fq 'def validate_backend_ref' && printf '%s\n' "$content" | grep -Fq 'backend_ref:'
          && printf '%s\n' "$content" | grep -Fq 'terminal_outcome:' && printf '%s\n' 'final-head terminal-v2
          content symbols present'
  - id: "dod-compat-v2-final-head-validation"
    description: >-
      Append-only correction for the historical diff-derived behavior proof. The old receipt was recorded
      against compat commit 82bf9531 before PR #199 merged; this successor binds immutable product merge
      fcc826e0, verifies that PR #199's final head is 5304ca43, and records final-head CI, contract-validation,
      precommit-parity, release-dry-run, and post-merge CI runs. The original check and receipt remain
      immutable.
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-diff-derived-behavior-proof"
    checks:
      - check_type: "command"
        check_value: >-
          test "$(gh pr view 199 --repo OmniNode-ai/omnibase_compat --json headRefOid,mergeCommit,state
          --jq '.headRefOid + " " + .mergeCommit.oid + " " + .state')" = "5304ca43c962622e5b6919b181ebc5c0998a68c5
          fcc826e0e6f9de09c3f9969f80f77793fe923ee3 MERGED" && expected=5304ca43c962622e5b6919b181ebc5c0998a68c5;
          for run in 33570202504 33570202566 33570202594 33570202582; do test "$(gh run view "$run" --repo
          OmniNode-ai/omnibase_compat --json headSha,conclusion --jq '.headSha + " " + .conclusion')"
          = "$expected success" || exit 1; done; test "$(gh run view 33570540807 --repo OmniNode-ai/omnibase_compat
          --json headSha,conclusion --jq '.headSha + " " + .conclusion')" = "fcc826e0e6f9de09c3f9969f80f77793fe923ee3
          success"
  - id: "occ-self-bind-pr-8075"
    description: >-
      Append-only self-bind for this OMN-17013 OCC correction PR. Binds the immutable published OCC head
      before CI re-evaluates the corrected receipt corpus, and requires the local receipt-hardening validator
      that checks the contract and receipt hashes fail closed.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: >-
          git merge-base --is-ancestor 587beac8cf796de453e8c91ffd34b239e2af44d5 HEAD && state="$(gh pr
          view 8075 --repo OmniNode-ai/onex_change_control --json state --jq '.state')" && test "$state"
          = "OPEN" && printf 'ancestor=587beac8cf796de453e8c91ffd34b239e2af44d5 state=%s\n' "$state" &&
          uv run python scripts/validation/check_receipt_hardening.py
  - id: "occ-self-bind-8075-merged"
    description: "Supersedes the frozen open-state self-bind for OCC PR #8075 after its external merge."
    source: "manual"
    status: "verified"
    evidence_artifact: "supersedes_dod_evidence:occ-self-bind-pr-8075"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/pulls/8075 --jq 'if (.number == 8075 and .state == "closed" and .merged == true and .merge_commit_sha == "2652e124fb7f57975325dfa9d1dd4da399a89200") then "true" else error("unexpected #8075 state") end'
  - id: "occ-self-bind-pr-8094"
    description: "Self-binds OMN-17013 to draft OCC carrier PR #8094 using stable carrier identity, base,
      and required C1 file facts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/onex_change_control/pulls/8094 --jq 'if (.number == 8094 and .state == "open" and .base.ref == "dev") then "true" else error("unexpected carrier PR state") end' | grep -x true && gh api --paginate repos/OmniNode-ai/onex_change_control/pulls/8094/files --jq '[.[].filename] | if ((index("contracts/OMN-17013.yaml") != null) and (index("drift/dod_receipts/OMN-17013/occ-self-bind-8075-merged/command.yaml") != null) and (index("drift/dod_receipts/OMN-17013/occ-self-bind-pr-8075/command.supersede.0001.yaml") != null)) then "true" else error("required C1 file absent") end' | grep -x true
