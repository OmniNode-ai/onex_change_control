---
schema_version: "1.0.0"
ticket_id: "OMN-13773"
title: "P0: omniclaude plugin deploys from stale main — SubagentStop transcript_path fix (OMN-12719) stranded
  on dev"
summary: >-
  Central OCC contract + receipts for omniclaude hotfix PR #1828. Commit 83a48c904 (fix(OMN-12719): handle
  newer Claude Code SubagentStop payload shapes, PR #1737) merged to omniclaude dev on 2026-06-08 but was
  never promoted to main. The deployed onex plugin tracks main, so the deployed SubagentStop hook lacked
  the transcript_path/agent_transcript_path JSONL branch and blocked every subagent stop regardless of
  report content. This ticket cherry-picks 83a48c904 onto main via a hotfix/* PR (no new logic, verbatim
  dev commit).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
evidence_requirements:
  - kind: "tests"
    description: >-
      omniclaude SubagentStop claim verifier regression tests pass on the cherry-picked main branch.
    command: "cd omniclaude && uv run pytest tests/hooks/test_subagent_claim_verifier.py -v"
  - kind: "ci"
    description: "omniclaude PR #1828 required CI gates must pass before merge."
    command: "gh pr checks 1828 --repo OmniNode-ai/omniclaude"
dod_evidence:
  - id: "dod-omniclaude-pr-1828"
    description: >-
      omniclaude PR #1828 (hotfix/omn-13773-transcript-path-main) carries the cherry-picked OMN-12719
      SubagentStop transcript_path fix onto main.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13773/dod-omniclaude-pr-1828/command.yaml &&
          grep -q '^pr_number: 1828$' drift/dod_receipts/OMN-13773/dod-omniclaude-pr-1828/command.yaml
  - id: "dod-regression-tests-pass"
    description: >-
      tests/hooks/test_subagent_claim_verifier.py (21 tests, including transcript_path/agent_transcript_path/last_assistant_message
      payload-shape coverage) passes on the cherry-picked branch.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-13773/dod-regression-tests-pass/command.yaml
