---
schema_version: "1.0.0"
ticket_id: "OMN-12998"
title: "Instruction-eval aggregate projection"
summary: >-
  Central evidence for omnimarket PR #1414, which adds node_projection_instruction_eval. The node consumes
  instruction-eval-result events and materializes the instruction_eval_aggregate_snapshots read model
  used by the InstructionEvalHeatmap projection API. The product PR also normalizes projection_api.topic
  to the canonical event-shaped snapshot topic so architectural topic checks pass.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
  - "topics"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-1414"
    description: >-
      omnimarket PR #1414 carries the node_projection_instruction_eval contract, handler, row builder,
      model, migration, pyproject entry point, runtime dogfood count update, and projection/golden-chain
      tests.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json state,headRefOid,baseRefName"
  - id: "dod-projection-tests"
    description: >-
      Focused product tests prove the contract projection topic, write table, row builder, UPSERT path,
      null pass_rate behavior, and golden-chain projection flow.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12998/dod-projection-tests/command.yaml"
  - id: "dod-occ-evidence-source"
    description: >-
      This OCC contract is the central Receipt-Gate Evidence-Source for OMN-12998 and is cited by omnimarket
      PR #1414.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f contracts/OMN-12998.yaml"
  - id: "dod-deploy-assessment"
    description: >-
      Deploy assessment: omnimarket PR #1414 adds a runtime projection node and database migration. The
      PR is code, contract, and migration evidence only; post-merge rollout follows the normal deployment
      lane.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12998/dod-deploy-assessment/command.yaml"
