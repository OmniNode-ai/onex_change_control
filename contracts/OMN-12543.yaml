---
schema_version: "1.0.0"
ticket_id: "OMN-12543"
title: "ci: add persistent uv/wheel cache for self-hosted runner dependency installs"
summary: >
  Stop forcing cold uv installs across required omnibase_infra CI jobs so self-hosted runners can reuse
  dependency cache under merge-queue fanout.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Workflow resilience tests prove required install paths do not disable uv cache."
    command: "See drift/dod_receipts/OMN-12543/dod-cache-workflow-tests/command.yaml"
  - kind: "manual"
    description: "Workflow scan confirms no remaining .github cold-install patterns targeted by this fix."
    command: "See drift/dod_receipts/OMN-12543/dod-cache-workflow-scan/command.yaml"
  - kind: "ci"
    description: "omnibase_infra PR #1818 carries the persistent uv cache workflow changes."
    command: "See drift/dod_receipts/OMN-12543/dod-infra-pr-1818/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-cache-workflow-tests"
    description: "Focused CI workflow resilience tests and ruff checks pass for the PR head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12543/dod-cache-workflow-tests/command.yaml
          && grep -q '7 passed' drift/dod_receipts/OMN-12543/dod-cache-workflow-tests/command.yaml"
  - id: "dod-cache-workflow-scan"
    description: "Workflow scan finds no remaining disabled uv cache or uv sync --no-cache patterns in
      .github."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12543/dod-cache-workflow-scan/command.yaml
          && grep -q 'no workflow hits' drift/dod_receipts/OMN-12543/dod-cache-workflow-scan/command.yaml"
  - id: "dod-infra-pr-1818"
    description: "omnibase_infra PR #1818 is open at the persistent uv cache commit."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12543/dod-infra-pr-1818/command.yaml
          && grep -q '^pr_number: 1818$' drift/dod_receipts/OMN-12543/dod-infra-pr-1818/command.yaml"
