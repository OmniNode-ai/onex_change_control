---
schema_version: "1.0.0"
ticket_id: OMN-18601
title: Central OCC evidence for the OMN-18601 prod-promotion MANIFEST grant staged on onex_change_control@main
summary: >-
  Central OCC evidence for the first `target_kind: manifest` entry ever written to grants/prod_promotion_grants.yaml
  — the authorization for the first governed production Keycloak reconcile. The entry authorizes exactly
  one deploy-onex-prod.yml dispatch at ref refs/heads/main, applying k8s/keycloak-seed/overlays/prod into
  namespace onex-prod on the public cluster, which renders to three objects and nothing else. This contract
  lives on the PR branch itself because occ-preflight pins OCC evidence at the PR head SHA for onex_change_control's
  own PRs — the same shape OCC#7418, OCC#7899 and OCC#8571 used on this base, and it is deliberately SEPARATE
  from and smaller than the OMN-18601 contract on this repo's dev lane, which covers the feature work
  in omninode_infra rather than this registry change. NOTHING ABOUT PRODUCTION IS MUTATED BY MERGING THIS.
  The merge adds an authorization, not a workload; no dispatch, deploy, rollback or restart happens, and
  onex-prod keeps running exactly what it runs now. One premise is deliberately unmet at staging time
  and is disclosed rather than hidden — promote-manifest resolves a staging-apply receipt recorded AT
  the promoted commit and accepts it for at most 24h, and no such receipt exists at 8279e2ca yet, so a
  dispatch today would still be refused. Every field in the entry was RECOMPUTED at manifest_ref 8279e2ca
  using that checkout's own scripts/manifest_promotion_render.py rather than carried forward from the
  earlier draft at e746a665; manifest_ref is the only field that moved.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: manual
    description: >-
      Prod-promotion grant registry state and producer-side schema validation for the single OMN-18601
      manifest entry: the registry carries the entry and is therefore no longer at rest.
    command: >-
      grep -Fq 'manifest_ref: 8279e2ca8b2681a630905bae3ce4470a6a289ebb' grants/prod_promotion_grants.yaml
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-prod-promotion-grant-values-omn18601"
    description: >-
      STAGED STATE. The prod-promotion grant registry carries the OMN-18601 manifest entry and every one
      of its six target fields verbatim — grant_id grant-90f8e729-c65c-4693-9bc0-b0df390f2d2e, target_kind
      manifest, manifest_path k8s/keycloak-seed/overlays/prod, manifest_subtree k8s/keycloak-seed, manifest_ref
      8279e2ca8b2681a630905bae3ce4470a6a289ebb and rendered_digest sha256:33b54afe55e78fc3943d4ef67182f14935c73d7f9df9bd4f619e3acf981c5227
      — and the file is no longer at `entries: []`. Falsifiable in the useful direction, which is the
      half that makes it a measurement: the identical six-grep chain FAILS on onex_change_control@main
      at 3311f0bb1, where the registry is still at rest and none of the six strings exists. A chain that
      was silently broken — a mistyped grant id, a grep that never matched — would have failed on BOTH
      trees; it did not. READ FROM THE RECEIPT, NOT THE LIVE REGISTRY, and the reason is the revocation
      this PR carries. The six- grep chain above is a measurement of registry STATE, and OCC#10302 removes
      the entry and returns the file to `entries: []`, so the live form cannot hold at this PR's head
      and would invert into a false failure. The chain itself is unchanged and is preserved verbatim in
      the receipt minted at a94f7353 while the entry was staged, together with its main-tree positive
      control; this check now reads that receipt, which is the same shape dod-grant-subtree-bound-omn18601
      uses for a fact that was true at one specific commit. What is asserted is therefore narrower and
      stated plainly: the staged state WAS measured, at a94f7353, not that it holds now -- it deliberately
      does not.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-prod-promotion-grant-values-omn18601/command.yaml
  - id: "dod-prod-promotion-grant-validation-omn18601"
    description: >-
      The repo's own grant-schema suite passes against the STAGED registry, including the OMN-18579 manifest-kind
      cases that this entry is the first real exercise of — that a manifest grant requires all four manifest
      fields and forbids image_digest, that manifest_path must lie inside manifest_subtree segment-wise,
      and the OMN-13424 at-rest invariant refusing any expired entry. 44 tests pass across tests/test_prod_promotion_grants.py
      and tests/test_prod_promotion_grants_manifest_kind.py. Stated plainly so the suite is not over-read
      this is a SCHEMA gate, not an authorization gate. It proves the staged entry is WELL-FORMED and
      of the kind it claims to be. It does not prove the promotion is warranted — the authority for that
      is the CODEOWNERS merge — and it does not prove the promotion would SUCCEED, which depends on the
      staging-apply receipt premise that is openly unmet at staging time. Executed hermetically from the
      repo checkout, no network.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          uv run pytest tests/test_prod_promotion_grants.py tests/test_prod_promotion_grants_manifest_kind.py
          -q
  - id: "dod-grant-author-identity-omn18601"
    description: >-
      ANTI-SELF-APPROVAL, on the only surface this repo controls. The entry names approved_by jonahgabriel,
      a real GitHub login rather than a placeholder, because the consumer (omninode_infra's scripts/validate_prod_promotion_grant.py)
      enforces approved_by != requested_by against github.actor at dispatch time; a synthetic placeholder
      would compare unequal to EVERY dispatcher and would silently disable the only mechanical anti-self-approval
      check on this path. This PR is authored by jake-b-omni, who is also the intended dispatcher, so
      approved_by differs from both the author and the dispatcher and both refusals — the authoring-time
      one in this repo and the dispatch-time one in omninode_infra — are satisfied on their own terms
      rather than by assertion. Note what this item does NOT claim: no file anywhere proves a human said
      the words behind approved_by. The CODEOWNERS-gated merge is that act, and it is the reason this
      PR must be landed by a person. READ FROM THE RECEIPT, NOT THE LIVE REGISTRY, for the same reason
      as the values item: OCC#10302 removes the entry, so `grep -Fq 'approved_by: jonahgabriel'` has nothing
      left to match and would invert into a false failure. The anti-self-approval fact is historical --
      it was true of the entry that authorized run 35392020746 -- and it is recorded in the receipt minted
      at a94f7353, which carries the grep pair and the `gh api user --jq .login` probe that established
      the author identity. This check reads that receipt. The dispatch-time refusal in omninode_infra
      is unaffected either way; it reads the registry at dispatch, and there is no longer an entry to
      dispatch against.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-grant-author-identity-omn18601/command.yaml
  - id: "dod-grant-subtree-bound-omn18601"
    description: >-
      THE BLAST-RADIUS ITEM, recomputed at manifest_ref rather than trusted. Rendering k8s/keycloak-seed/overlays/prod
      from a checkout of omninode_infra at 8279e2ca with that repository's own scripts/manifest_promotion_render.py
      yields exactly three objects — the ConfigMap seed-keycloak-desired-clients-4888bkdm6h, the Job seed-keycloak-clients
      and the InfisicalSecret keycloak-reconcile-credentials — digesting to sha256:33b54afe55e78fc3943d4ef67182f14935c73d7f9df9bd4f619e3acf981c5227
      with derive_namespace() resolving to onex-prod. subtree_violations over the six-file kustomization
      tree returns [] against the declared bound k8s/keycloak-seed. THE POSITIVE CONTROL is the same call
      against the tighter bound k8s/keycloak-seed/overlays/prod, which returns 2 violations, both files
      under base/ — that is what makes the empty result a measurement rather than a silently broken probe,
      and it is also why manifest_subtree is DECLARED wider than manifest_path: the overlay names ../../base
      as a resource, so a bound of the overlay path alone would refuse the very tree it authorizes. A
      SECOND CONTROL pins that the promotion moved the ref and not the render: the identical render at
      the pre-promotion commit e746a665 digests to the same value, because omninode_infra#1556 changed
      no file under k8s/keycloak-seed. CROSS-REPO by construction, which is why the hosted check_value
      is the receipt-grep form — the renderer lives in the PRIVATE omninode_infra repo, which the OCC
      runner has no checkout of and no token for, so a direct check_value would fail for lack of read
      access rather than for lack of evidence. The receipt carries all four readings verbatim.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-grant-subtree-bound-omn18601/command.yaml
  - id: "occ-self-bind-pr-10246"
    description: >-
      Self-bind for onex_change_control#10246, the PR that stages this entry. Binds this receipt set to
      the pull request that introduces it, so the evidence and the registry change are one reviewable
      unit rather than two things that happen to agree.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/occ-self-bind-pr-10246/command.yaml
  - id: dod-omn18601-grant-revoked
    description: 'REVOKED STATE, and the reason it is a removal rather than a consumed marker. grants/prod_promotion_grants.yaml
      is back to `entries: []` at rest: grant-90f8e729-c65c-4693-9bc0-b0df390f2d2e no longer authorizes
      anything. It was consumed in substance by deploy-onex-prod.yml run 35392020746, which passed every
      premise the gate enforces and applied all three objects into onex-prod with a live readback, after
      which the WORKLOAD failed: onex-prod enforces the restricted pod-security profile, the seed Job''s
      container declared neither allowPrivilegeEscalation: false nor capabilities.drop: [ALL], and the
      Job reached DeadlineExceeded with zero pods admitted. Two independent reasons make removal correct
      rather than a consumed marker, either sufficient alone. The entry pins rendered_digest sha256:33b54afe
      at manifest_ref 8279e2ca, a tree production cannot admit, and omninode_infra#1563 moves that render
      to sha256:976ef4de, so the entry could only ever re-authorize the broken manifest. And it expires
      2026-09-19T16:39:04Z, where the OMN-13424 at-rest invariant FAILS on any expired entry left in the
      file, turning every subsequent pull request to this repository red rather than merely the next promotion;
      a consumed marker neutralizes the authorization but does not clear that trap, while removal does
      both. FALSIFIABLE IN THE USEFUL DIRECTION: the check asserts `entries: []` and therefore FAILS on
      onex_change_control@main at 6c54c74f8, where the entry is still live. Note the check deliberately
      does NOT grep for the absence of the grant id, because the revocation header above names it on purpose
      and such a check would match the comment and report a false failure. READ FROM THE RECEIPT, NOT
      THE LIVE REGISTRY, and the reason is the mirror image of why the two staged-state items above read
      from theirs. `entries: []` is an assertion about registry STATE, and OCC#10454 stages the replacement
      grant-aeae215d, so the live form inverts into a false failure the moment a new authorization is
      written -- which is the registry working, not a regression. What this item records is unchanged
      and remains true: grant-90f8e729 WAS removed, measured at 6c54c74f8 with a control that inverts,
      and it has not come back. The check now reads the receipt minted when that removal happened. Every
      state-asserting item on this contract has now been converted at the first moment its state changed,
      which is the lifecycle this pattern exists to survive.'
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-18601/dod-omn18601-grant-revoked/command.yaml'
  - id: "dod-omn18601-psa-admissible-at-main"
    description: >-
      THE QUESTION THE 20:32Z DISPATCH NEVER ASKED, asked here before the next one. Rendering k8s/keycloak-seed/overlays/prod
      from a checkout of omninode_infra at main 8279e2ca with the two files of b4eac797 applied yields
      three objects, and the one workload among them, Job/seed-keycloak-clients, satisfies every requirement
      the restricted pod-security profile enforces on a container: allowPrivilegeEscalation false, capabilities.drop
      containing ALL, runAsNonRoot true and a seccompProfile of RuntimeDefault. FALSIFIABLE IN THE USEFUL
      DIRECTION, which is the half that makes it a measurement rather than an assertion: the identical
      checker run against main 8279e2ca UNMODIFIED REFUSES the same Job and names both missing fields,
      exiting 1. A checker that was silently broken would have admitted BOTH trees; it did not. Stated
      narrowly on purpose, this proves the rendered pod template is ADMISSIBLE, not that the Job will
      succeed. Whether the seeding logic then does its work is a separate question that only a real run
      answers, and the gap between an overlay that APPLIES and a workload that RUNS is OMN-18764. CROSS-REPO
      by construction: the render is performed in an omninode_infra checkout, which the Contract Compliance
      Check runner has neither, so the hosted check reads this receipt rather than re-running the probe.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-psa-admissible-at-main/command.yaml
  - id: "dod-omn18601-psa-narrow-hotfix-parity"
    description: >-
      THE HOTFIX CARRIES THE FIX AND NOTHING ELSE. omninode_infra's hotfix/omn-18601-keycloak-psa branch
      is cut from main 8279e2ca and applies exactly two files from b4eac797, the squash of omninode_infra#1563
      that already merged to dev: k8s/keycloak-seed/base/seed-keycloak-clients-job.yaml and its test tests/k8s/test_keycloak_seed_restricted_psa_omn18601.py.
      Measured rather than asserted, in both directions. The whole of k8s/keycloak-seed differs between
      main and dev by that one file and 37 insertions, so nothing else in the promoted subtree is outstanding;
      and after the two files are applied, a diff of that subtree and the test against origin/dev is EMPTY,
      so the branch carries dev's bytes exactly rather than a reconstruction of them. This is why a narrow
      hotfix is the correct shape here rather than a promotion of all of dev, which .github/workflows/main-target-guard.yml
      admits as a first-class path for a hotfix/* head carrying hotfix-evidence and a backmerge link.
      What it deliberately does NOT do is carry the other twelve merges on dev, including the hook-topic
      grant mirror whose drift blocks omninode_infra#1565; that drift is real, is dev-only, and is deferred
      by this route rather than fixed by it.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-psa-narrow-hotfix-parity/command.yaml
  - id: "dod-omn18601-psa-test-suite"
    description: >-
      THE FIX CARRIES ITS OWN REGRESSION TEST, and the test runs green on the hotfix tree. tests/k8s/test_keycloak_seed_restricted_psa_omn18601.py
      is 325 lines introduced by the same commit as the manifest change, and all 7 of its cases pass against
      the branch. The test is what keeps the property after this ticket closes: a future edit that drops
      allowPrivilegeEscalation or the capabilities drop from the base Job fails in omninode_infra's own
      CI rather than at a production admission controller ten minutes into a promotion, which is where
      it failed on 2026-09-18. CROSS-REPO by construction, as above: the suite lives in omninode_infra
      and cannot execute in the OCC checkout, so the hosted check reads this receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-psa-test-suite/command.yaml
  - id: "dod-omn18601-grant2-values"
    description: >-
      STAGED STATE of the replacement grant. The registry carries grant-aeae215d-d796-4141-a0a9-7b5fd0c22055
      and every one of its six target fields verbatim — target_kind manifest, manifest_path k8s/keycloak-seed/overlays/prod,
      manifest_subtree k8s/keycloak-seed, manifest_ref ad45e2cc1a8bbd0c4524e62783ee8fd84532e00c and rendered_digest
      sha256:976ef4de626144f156a6a227c3b881066e07273476a5f19f00b96e6e454fad06 — and the file is no longer
      at rest. FALSIFIABLE IN THE USEFUL DIRECTION: the identical chain FAILS on onex_change_control@main
      at 16a3db814, where the registry is `entries: []` and none of the six strings exists, so a chain
      broken by a mistyped id or a grep that never matches would have failed on BOTH trees. THIS ITEM
      ASSERTS STATE AND WILL INVERT WHEN THE ENTRY IS PRUNED, which is a lifecycle fact rather than a
      defect and is recorded here so the next reader does not have to rediscover it: the PR that prunes
      this entry must re-point this check at its own receipt, the way OCC#10302 re-pointed dod-prod-promotion-grant-values-omn18601
      when it revoked grant-90f8e729. The measurement and its control live in the receipt either way.
      THAT MOMENT ARRIVED, in OCC#10471: grant-aeae215d is superseded by grant-823154a7 because omninode_infra#1583
      moved main ad45e2cc -> b828ec9e, so the six-grep chain has nothing left to match and this check
      now reads its receipt. The prediction above was written before the event and is left standing rather
      than tidied away, because a contract that forecast its own inversion and then survived it is the
      evidence that the pattern is understood rather than merely described.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-grant2-values/command.yaml
  - id: "dod-omn18601-grant2-schema"
    description: >-
      The repo's own grant-schema suite passes against the STAGED registry: 44 tests across tests/test_prod_promotion_grants.py
      and tests/test_prod_promotion_grants_manifest_kind.py, covering the OMN-18579 manifest-kind rules
      this entry exercises — a manifest grant requires all four manifest fields and must NOT carry image_digest,
      manifest_path must lie inside manifest_subtree segment-wise — plus the OMN-13424 at-rest invariant
      that refuses any expired entry. The suite caught a real omission while this entry was being authored:
      `reason` is a COMMON required field, and the entry was drafted without it. Stated plainly so the
      suite is not over-read: this is a SCHEMA gate, not an authorization gate and not a proof the promotion
      will succeed. It proves the entry is WELL-FORMED and of the kind it claims. The authority is the
      CODEOWNERS-gated merge; the premise that the promotion can succeed is the two items below.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          uv run pytest tests/test_prod_promotion_grants.py tests/test_prod_promotion_grants_manifest_kind.py
          -q
  - id: "dod-omn18601-grant2-admissible"
    description: >-
      THE CHECK WHOSE ABSENCE COST THE LAST DISPATCH, run before this one is authorized. Rendering k8s/keycloak-seed/overlays/prod
      from a checkout of omninode_infra at the grant's own manifest_ref ad45e2cc yields exactly three
      objects — ConfigMap seed-keycloak-desired-clients-4888bkdm6h, Job seed-keycloak-clients and InfisicalSecret
      keycloak-reconcile-credentials, all in namespace onex-prod — and the one workload among them satisfies
      every requirement PodSecurity `restricted` enforces per container: allowPrivilegeEscalation false,
      capabilities.drop containing ALL, runAsNonRoot true, seccompProfile RuntimeDefault. FALSIFIABLE
      IN THE USEFUL DIRECTION: the identical checker REFUSES the same Job at 8279e2ca, the ref the revoked
      grant named, printing both absent field names and exiting 1. The digest recomputed at ad45e2cc with
      omninode_infra's own scripts/manifest_promotion_render.py equals the rendered_digest this entry
      declares, so identity and admissibility were measured on the same bytes. CROSS-REPO by construction
      — the render needs an omninode_infra checkout the OCC runner does not have — so the hosted check
      reads this receipt.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-grant2-admissible/command.yaml
  - id: "dod-omn18601-grant2-premise-green"
    description: >-
      THE STABILITY PREMISE IS MET AT AUTHORING TIME, which the revoked grant's was not. promote-manifest
      resolves a staging-apply receipt recorded AT the promoted commit and accepts it for at most 24 hours;
      deploy-onex-staging run 35458357503, dispatched at ref main, recorded verdict=GREEN with sha=ad45e2cc1a8bbd0c4524e62783ee8fd84532e00c
      and the applied mapping k8s/keycloak-seed/overlays/dev-system=k8s/keycloak-seed, so resolve_staging_apply
      BINDS the subtree rather than reading SUBTREE_UNBOUND. Fresh until 2026-09-20T17:38:11Z. WHAT THIS
      PREMISE DOES NOT PROVE, named rather than glossed: staging enforces no pod-security profile on any
      of its namespaces, so a GREEN apply there says the overlay APPLIED, not that the Job RAN — it went
      green for the revoked grant's manifest too, which production then refused. That is OMN-18764 and
      it is open; the admissibility item above is what covers the gap for this dispatch, by hand, because
      the gate cannot yet do it. CROSS-REPO by construction, as above.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-grant2-premise-green/command.yaml
  - id: "dod-omn18601-grant2-author-identity"
    description: >-
      ANTI-SELF-APPROVAL, on the only surface this repo controls. The entry names approved_by jonahgabriel,
      a real GitHub login rather than a placeholder, because omninode_infra's scripts/validate_prod_promotion_grant.py
      enforces approved_by != requested_by against github.actor at dispatch time; a synthetic value would
      compare unequal to EVERY dispatcher and would silently disable the only mechanical anti-self-approval
      check on this path. This PR is authored by jake-b-omni, who is also the intended dispatcher, so
      approved_by differs from both and each refusal is satisfied on its own terms. What this does NOT
      claim: no file proves a human said the words behind approved_by. The CODEOWNERS-gated merge is that
      act, and because grants/prod_promotion_grants.yaml is owned by @OmniNode-ai/platform-leads and GitHub
      refuses an author's own approval, the only person who can land this is jonahgabriel. Like the values
      item, this asserts STAGED state and inverts when the entry is pruned; the pruning PR re-points it
      at its receipt. RE-POINTED IN OCC#10471, and for a SHARPER reason than the values item, which is
      worth stating because the two failure modes are not equally visible. The values chain names a grant
      id and therefore INVERTS when that entry goes — a loud, unmissable red. This chain names only `approved_by:
      jonahgabriel`, and the replacement entry grant-823154a7 carries the same approver, so after the
      prune it would have kept PASSING while silently measuring a DIFFERENT grant than the one this item
      describes. A check that passes for the wrong reason is worse than one that fails for the right one:
      the red gets fixed, the false green gets trusted. Reading the receipt binds the claim back to the
      entry it was actually made about.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-grant2-author-identity/command.yaml
  - id: "dod-omn18601-prod-job-guard-asymmetry"
    description: >-
      THE DEFECT NAMED AS A MEASURED ASYMMETRY, not as a theory about why a run failed. deploy-onex-prod.yml
      run 35465514637 passed every premise the manifest arm enforces — grant match, rendered_digest match,
      subtree bound, staging-apply receipt bound to the promoted commit ad45e2cc — and then failed IN
      THE APPLY, with two of three objects landing and Job/seed-keycloak-clients rejected on `spec.template
      ... field is immutable`. A batch/v1 Job's pod template cannot be updated, so the inert Job left
      in onex-prod by the 2026-09-18 reconcile, uid d612f3af created 2026-09-18T21:56:46Z with zero pods
      ever admitted, was an unremovable obstacle to applying the CORRECTED manifest over it. The cause
      is a workflow asymmetry and it is counted rather than asserted: deploy-onex-staging.yml carries
      14 references to the OMN-15812 pre-apply Job guard and deploy-onex-prod.yml carried 0. That is why
      the same bytes applied cleanly on dev-system and were refused in production, and it is a SECOND
      premise defect on this path, alongside OMN-18764's apply-versus-run gap. CROSS-REPO: both workflows
      live in omninode_infra.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-prod-job-guard-asymmetry/command.yaml
  - id: "dod-omn18601-prod-job-guard-detects"
    description: >-
      THE PORTED GUARD FIRES ON THE REAL CONFLICT, measured against LIVE onex-prod rather than reasoned
      about. Rendering k8s/keycloak-seed/overlays/prod at the guard branch and server-dry-run-applying
      the Job it declares into onex-prod returns `field is immutable`, which is verbatim the literal the
      guard's IMMUTABLE_CONFLICT_RE matches, and the match count is 1. FALSIFIABLE IN THE USEFUL DIRECTION,
      which is what makes it a measurement: the SAME server dry-run against the ConfigMap in the same
      namespace SUCCEEDS, reporting serverside-applied (server dry run), so the mechanism rejects the
      immutable Job specifically rather than rejecting everything handed to it. A dry-run that failed
      on both would have proven nothing about immutability. Non-mutating by construction — a server-side
      dry-run persists nothing and this probe issued no delete. WHAT IS NOT EXERCISED, stated rather than
      implied: the DELETE half, because running it would mutate production. Its correctness rests on the
      port being byte-equivalent in that branch to the staging guard that has run this path since OMN-15812,
      and on the detection half proven here deciding when it runs at all.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-prod-job-guard-detects/command.yaml
  - id: "dod-omn18601-prod-job-guard-placed-and-bounded"
    description: >-
      WHERE IT SITS AND WHAT IT REFUSES TO DO, both asserted by parsing the workflow rather than by reading
      it. The guard is step 14 of promote-manifest, strictly between `Enforce prod-promotion grant` at
      13 and `Apply the approved manifest overlay` at 15 — after the gate, so it can never delete anything
      on an unauthorized dispatch, and before the apply, which is the thing it unblocks. Its executable
      body contains no cluster-wide `get jobs` sweep and does carry an explicit JOBS allowlist, which
      is the safety property the staging original states in its own comment: a recreate re-runs the Job
      from scratch, so membership requires established idempotency, and seed-keycloak-clients.py's docstring
      guarantees a re-run against an already-correct realm produces all op=unchanged and exits 0. Two
      deliberate departures from the staging port, both fail-closed: the namespace is DERIVED from the
      rendered objects because manifest_path varies, and a render whose Jobs disagree about their namespace
      refuses rather than picking one; and a Job that conflicts but is NOT on the allowlist fails the
      job closed naming itself, rather than being deleted (re-running unvetted work) or skipped (reproducing
      this outage on a Job nobody examined).
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-prod-job-guard-placed-and-bounded/command.yaml
  - id: "dod-omn18601-grant3-values"
    description: >-
      STAGED STATE of grant-823154a7-18ab-4d54-8d3c-3a2e859d1f87, and the ONE field that moved. The registry
      carries the entry with target_kind manifest, manifest_path k8s/keycloak-seed/overlays/prod, manifest_subtree
      k8s/keycloak-seed, manifest_ref b828ec9e65f873149c97d94cbefa9133b9574e5d and rendered_digest sha256:976ef4de626144f156a6a227c3b881066e07273476a5f19f00b96e6e454fad06,
      and the superseded grant-aeae215d is GONE. Only manifest_ref differs from its predecessor: the digest
      was RECOMPUTED at b828ec9e with omninode_infra's own scripts/manifest_promotion_render.py rather
      than carried forward, and it came back equal, because omninode_infra#1583 edited a workflow and
      touched nothing under k8s/keycloak-seed. FALSIFIABLE IN THE USEFUL DIRECTION: the same chain FAILS
      on onex_change_control@main at a7b185cb2, where grant-aeae215d is present and grant-823154a7 is
      not, so the chain distinguishes the two entries rather than merely finding any entry. STATE-ASSERTING,
      AND THE LIFECYCLE IS KNOWN: this inverts when the entry is pruned and the pruning PR re-points it
      at its receipt, which is now the third occurrence of that conversion on this contract and the reason
      a per-grant-cycle contract would serve this ticket family better than one that grows.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -Fq 'grant-823154a7-18ab-4d54-8d3c-3a2e859d1f87' grants/prod_promotion_grants.yaml && grep
          -Fq 'manifest_ref: b828ec9e65f873149c97d94cbefa9133b9574e5d' grants/prod_promotion_grants.yaml
          && grep -Fq 'rendered_digest: sha256:976ef4de626144f156a6a227c3b881066e07273476a5f19f00b96e6e454fad06'
          grants/prod_promotion_grants.yaml && grep -Fq 'manifest_subtree: k8s/keycloak-seed' grants/prod_promotion_grants.yaml
          && ! grep -Fq 'grant-aeae215d-d796-4141-a0a9-7b5fd0c22055' grants/prod_promotion_grants.yaml
  - id: "dod-omn18601-grant3-both-defects-covered"
    description: >-
      THE FIRST COMMIT WHERE BOTH PREMISE DEFECTS ON THIS PATH ARE COVERED, which is the whole reason
      this grant names b828ec9e rather than ad45e2cc. Defect one, ADMISSIBILITY: the render of k8s/keycloak-seed/overlays/prod
      at b828ec9e yields three objects whose one workload satisfies every requirement PodSecurity `restricted`
      enforces, and the identical checker REFUSES the same Job at 8279e2ca naming both absent fields.
      Defect two, IMMUTABILITY: deploy-onex-prod.yml at b828ec9e carries 17 references to the OMN-15812
      pre-apply Job guard where the same file at ad45e2cc carried 0, and a live probe against onex-prod
      confirmed the guard's detection fires — the server dry-run of the rendered Job returns the immutable-field
      error the guard matches, while the same dry-run on the ConfigMap succeeds. Run 35465514637 is the
      proof that covering only the first was insufficient: it passed every identity premise, applied two
      of three objects, and could not apply the Job. WHAT REMAINS UNCOVERED, stated rather than implied:
      OMN-18764. The staging-apply premise proves an overlay APPLIED, not that a Job RAN, so the receipt
      this grant depends on would go GREEN either way; the admissibility measurement stands in for it
      by hand, and the guard's DELETE branch is unexercised because exercising it would mutate production.
      CROSS-REPO by construction.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-grant3-both-defects-covered/command.yaml
  - id: "dod-omn18601-grant3-premise-green"
    description: >-
      THE STABILITY PREMISE, MEASURED AT THE NEW REF rather than inherited from the previous cycle. promote-manifest
      resolves a staging-apply receipt recorded AT the promoted commit and accepts it for at most 24 hours,
      and the previous cycle's receipt was recorded at ad45e2cc, which b828ec9e supersedes — so a receipt
      had to be produced afresh rather than reused. deploy-onex-staging.yml was dispatched at ref main
      after omninode_infra#1583 merged; the receipt's own recorded sha, its verdict and the applied subtree
      mapping are in this item's receipt, along with the run id. resolve_staging_apply binds on the receipt's
      recorded sha rather than the run's head sha, which is load-bearing because deploy-onex-staging is
      also triggered by workflow_run, where GitHub attributes the run to the default branch while the
      receipt records the triggering commit. CROSS-REPO by construction.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-grant3-premise-green/command.yaml
  - id: "dod-omn18601-grant3-author-identity"
    description: >-
      ANTI-SELF-APPROVAL for this cycle, written as a RECEIPT read from the start rather than as a live
      grep, and the reason is the lesson the grant2 item recorded on its way out. A live `grep -Fq 'approved_by:
      jonahgabriel'` does not invert when an entry is replaced, because every entry on this path carries
      the same approver — it keeps PASSING while silently measuring whichever entry happens to be present.
      A check that passes for the wrong reason is worse than one that fails for the right one, so this
      item reads the receipt that binds the claim to grant-823154a7 specifically. The fact itself is unchanged
      and mechanical: approved_by jonahgabriel differs from the PR author and from the intended dispatcher,
      both jake-b-omni, so the authoring-time refusal here and the dispatch-time refusal in omninode_infra
      are each satisfied on their own terms. What it does NOT claim: no file proves a human said the words
      behind approved_by. The CODEOWNERS merge is that act, and platform-leads ownership plus GitHub's
      refusal of an author's own approval means jonahgabriel alone can land it.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-grant3-author-identity/command.yaml
