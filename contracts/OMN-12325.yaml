---
schema_version: "1.0.0"
ticket_id: "OMN-12325"
title: "Fix bus-bypass and observability skill nodes so market skills use runtime truth"
summary: >
  Central OCC evidence for omnimarket PR #952. The change routes recall, observability-sink,
  dep-cascade-dedup, and adversarial-pipeline skill surfaces through the Codex runtime request
  adapter and makes the observability sink handler contract-routed with typed runtime adapters.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
  - "protocols"
  - "public_api"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-952"
    description: "omnimarket PR #952 merged the OMN-12325 runtime-truth skill and observability handler slice."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12325/dod-omnimarket-pr-952/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused omnimarket regression tests prove observability handler behavior and skill shim runtime routing."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12325/dod-focused-tests/command.yaml"
  - id: "dod-boundary-validators"
    description: "Ruff, mypy, handler inventory, skill boundary, environment fallback, overlay, node drift, and topic lint validators passed."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12325/dod-boundary-validators/command.yaml"
  - id: "dod-runtime-dogfood"
    description: "Dogfood runtime requests compiled OMN-12325 skill nodes and executed observability-sink through local runtime truth."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12325/dod-runtime-dogfood/command.yaml"
