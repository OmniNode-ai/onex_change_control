---
schema_version: "1.0.0"
ticket_id: "OMN-12663"
title: "feat(OMN-12663): add omnibase-infra delegation topics and widen delegation tier Literal"
summary: >-
  Central OCC evidence for omnibase_core PR #1192. The PR adds the omnibase-infra delegation topics
  required by the WS-B1 omnimarket repoint path and widens ModelDelegationBackendConfig.tier to cover
  in-use routing tier values while preserving a typed Literal contract.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "events"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-core-pr-1192-ci-green"
    description: >-
      omnibase_core PR #1192 has green non-receipt CI, including the split test matrix, Tests Gate,
      CI Summary, deploy-gate, architecture, CodeRabbit, and standards checks.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12663/dod-core-pr-1192-ci-green/command.yaml"
          && grep -q '^pr_number: 1192$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12663/dod-core-pr-1192-ci-green/command.yaml"
  - id: "dod-core-pr-1192-scope"
    description: >-
      omnibase_core PR #1192 is scoped to TopicBase delegation topics, bifrost delegation tier typing,
      version metadata, and focused unit tests for those surfaces.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12663/dod-core-pr-1192-scope/command.yaml"
          && grep -q 'src/omnibase_core/topics.py' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12663/dod-core-pr-1192-scope/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-12663 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12663/dod-occ-pr-self/command.yaml"
          && grep -q '^pr_number: 2137$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12663/dod-occ-pr-self/command.yaml"
