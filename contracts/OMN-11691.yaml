---
schema_version: "1.0.0"
ticket_id: "OMN-11691"
title: "fix: update model_registry.yaml and all references from gemini-2.0-flash to gemini-2.5-flash"
summary: >
  Updates gemini-2.0-flash to gemini-2.5-flash across all contract surfaces in onex-self-extending-agent:
  model_registry.yaml (served_model_id), cost_pricing.yaml (3 provider entries), agent.py (hardcoded model
  string), and docs/DEMO_GUIDE.md. All test assertions updated to match. Ships with OMN-11690 in PR #127.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests-pass"
    description: "883 unit tests pass with no -k filter after model ID update"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11690/dod-unit-tests-pass/command.yaml\""
  - id: "dod-sea-pr-127"
    description: "onex-self-extending-agent PR #127 implements all fixes"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11690/dod-sea-pr-127/command.yaml\"
          && grep -q '^pr_number: 127$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11690/dod-sea-pr-127/command.yaml\""
