---
schema_version: "1.0.0"
ticket_id: OMN-10124
title: "Add GzipRotatingFileHandler and _configure_logging to node_emit_daemon"
summary: "Add GzipRotatingFileHandler and _configure_logging to node_emit_daemon"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR checks pass including typecheck and test"
    command: "gh pr checks 428 --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "Unit tests for RotatingFileHandler and gzip rotation pass"
    command: "uv run pytest tests/unit/nodes/node_emit_daemon/test_logging.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Unit tests for GzipRotatingFileHandler pass (4 tests: handler wiring, gzip integrity,
      root-logger isolation, defaults)"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/nodes/node_emit_daemon/test_logging.py -v"
  - id: "dod-002"
    description: "mypy strict passes on __main__.py"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run mypy src/omnimarket/nodes/node_emit_daemon/__main__.py --strict"
  - id: "dod-003"
    description: "Post-merge deploy smoke: emit daemon module imports cleanly in deployed runtime container"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_emit_daemon.__main__
          import _configure_logging; print('deploy smoke ok')\""
