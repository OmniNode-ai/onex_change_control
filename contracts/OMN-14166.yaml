---
schema_version: "1.0.0"
ticket_id: "OMN-14166"
title: "fix(OMN-14166): keep omniweb Keycloak client confidential"
summary: >-
  Central OCC evidence for omninode_infra PR #580 and omnibase_infra PR #2241. The downstream PRs align
  the omniweb Keycloak desired-state surfaces so the reconciler preserves the live confidential client
  contract: publicClient false, KEYCLOAK_CLIENT_SECRET binding, and PKCE S256 metadata. This evidence
  PR performs no live deploy, runtime restart, branch-protection mutation, or bypass.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omninode-infra-pr-580"
    description: >-
      omninode_infra PR #580 is open against dev at head 97e307f2ddeacf4fa51ce1071ae3e93081cd382f and
      corrects the repo-local omniweb Keycloak desired-state surfaces to confidential client semantics.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14166/dod-omninode-infra-pr-580/command.yaml
          && grep -q '^pr_number: 580$' drift/dod_receipts/OMN-14166/dod-omninode-infra-pr-580/command.yaml
          && grep -q '^commit_sha: 97e307f2ddeacf4fa51ce1071ae3e93081cd382f$' drift/dod_receipts/OMN-14166/dod-omninode-infra-pr-580/command.yaml
  - id: "dod-omnibase-infra-pr-2241"
    description: >-
      omnibase_infra PR #2241 is open against dev at head 8612dce493561fb907b82460f8389280275a53ec and
      aligns the canonical desired-clients manifest with omninode_infra PR #580 so the parity check has
      a single source of truth.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14166/dod-omnibase-infra-pr-2241/command.yaml
          && grep -q '^pr_number: 2241$' drift/dod_receipts/OMN-14166/dod-omnibase-infra-pr-2241/command.yaml
          && grep -q '^commit_sha: 8612dce493561fb907b82460f8389280275a53ec$' drift/dod_receipts/OMN-14166/dod-omnibase-infra-pr-2241/command.yaml
  - id: "dod-parity-proof"
    description: >-
      The canonical omnibase_infra desired-clients manifest and the omninode_infra k8s desired-clients
      manifest match for the omniweb confidential-client fields.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14166/dod-parity-proof/command.yaml && grep
          -qi 'desired-clients-match' drift/dod_receipts/OMN-14166/dod-parity-proof/command.yaml
  - id: "dod-no-live-deploy"
    description: >-
      Scope evidence: this is static GitOps desired-state correction plus central evidence only. The OCC
      PR performs no live deploy, no runtime restart, no branch-protection change, and no bypass.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14166/dod-no-live-deploy/command.yaml && grep
          -qi 'no live deploy' drift/dod_receipts/OMN-14166/dod-no-live-deploy/command.yaml
