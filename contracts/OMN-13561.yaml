---
schema_version: "1.0.0"
ticket_id: "OMN-13561"
title: "refactor(OMN-13561): env→contract+overlay migration Wave 1 — omniintelligence endpoint env-read
  cleanup"
summary: >-
  OCC evidence for epic OMN-13556 Wave 1 (endpoints, OVERLAY seam) in omniintelligence. The source PR
  removes three scattered os.environ endpoint references from node_navigation_retriever_effect docstrings/Field
  descriptions (LLM_EMBEDDING_URL, QDRANT_URL) that the url-authority gate flagged as env-url-read — these
  were never live reads (the node is a pure declarative shell; endpoints are caller-injected via the input
  model). A paired omnibase_core PR burns down the corresponding three url-authority baseline entries
  (215 -> 212). The genuine live integration-endpoint migrations (QDRANT_URL in the gmail evaluator handler,
  MEMGRAPH_URI in dispatch_handler_graph_storage) are deferred — blocked on omnibase_infra contract_env_ref
  (OMN-13247) which is unreleased (latest v0.38.3 lacks it; omniintelligence pins <0.38.0). LLM_*_URL
  is Bifrost-owned (OMN-12803/12815).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-occ-pr"
    description: >-
      Self-binding receipt for this OCC PR, which carries the central contracts/OMN-13561.yaml consumed
      by the omniintelligence and omnibase_core feature-PR Receipt Gates via Evidence-Source.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13561\"$' contracts/OMN-13561.yaml"
  - id: "dod-omniintelligence-pr"
    description: >-
      Binds the omniintelligence feature PR (#734): navigation-retriever endpoint env-read doc cleanup.
      url-authority gate PASSED with 0 new violations; 3 grandfathered env-url-read findings removed.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13561\"$' contracts/OMN-13561.yaml"
  - id: "dod-omnibase-core-pr"
    description: >-
      Binds the omnibase_core feature PR (#1324): url-authority baseline burn-down (215 -> 212), removing
      the 3 omniintelligence grandfathered entries; shrink-only invariant holds.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -c '^ticket_id: \"OMN-13561\"$' contracts/OMN-13561.yaml"
