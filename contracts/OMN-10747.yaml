---
schema_version: "1.0.0"
ticket_id: "OMN-10747"
title: "Inject NodeLlmEvalHarness via DI in NodeOverseerBenchmarker"
summary: >
  Central OCC evidence for omnimarket PR #611. NodeOverseerBenchmarker.__init__ now accepts an injectable
  harness: NodeLlmEvalHarness | None = None parameter; default-None preserves prior behavior (construct
  from client or FakeLlmClient). Pure constructor signature change inside an offline benchmarking node
  — no new topology, no migration, no Kafka topic changes, no runtime restart required.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "Full omnimarket unit test suite passes, including the two new DI tests in tests/test_benchmarking_harness.py"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10747/dod-unit-tests/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10747/dod-unit-tests/command.yaml && grep -q 'tests pass' drift/dod_receipts/OMN-10747/dod-unit-tests/command.yaml"
  - id: "dod-deploy"
    description: >
      Deploy verification: change is a constructor-signature addition (new optional harness param, default-None
      preserves prior behavior) inside the offline NodeOverseerBenchmarker node. No handler routing change,
      no migration, no new runtime service, no Kafka topic change. Running runtime on .201 continues to
      operate identically; no restart required.
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10747/dod-deploy/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10747/dod-deploy/command.yaml && grep -q 'constructor signature only'
          drift/dod_receipts/OMN-10747/dod-deploy/command.yaml"
  - id: "dod-occ-pr"
    description: "Self-binding receipt for this OCC evidence PR."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f drift/dod_receipts/OMN-10747/dod-occ-pr/command.yaml && grep -q '^status:.*PASS'
          drift/dod_receipts/OMN-10747/dod-occ-pr/command.yaml"
