---
schema_version: "1.0.0"
ticket_id: "OMN-18066"
title: "Staging-green bar leg 2 scores the delegation migration the plane applied (0037), not the superseded
  0033"
summary: >
  OCC companion for OmniNode-ai/omninode_infra PR #1264. Bound by hand (union-resolve): the OMN-18066
  companion-effect command published on omninode_infra run 34304452838 with publish_reason no_evidence_source_citation
  and no companion PR was minted, so #1264 had no receipt to cite. Leg 2 of the staging-green bar was
  hard-keyed on migration 0033, which is superseded three times over (0033 -> 0034 -> 0036 -> 0037), has
  never applied on onex-dev, and is fenced out of every release; the leg now resolves the newest id in
  the supersession chain that the plane's stamped omnibase_infra revision also declares, and requires
  that revision to agree with the migrate bundle the scored deploy run itself resolved.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1264 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1264 on OmniNode-ai/omninode_infra, derived from the PR's own diff: the bar test files it adds or changes, including the new real-revision module that drives vendored omnibase_infra bytes at 3dfe23e5e and the positive-control revision 0a84ef522. Executed at cwd `${OMNI_HOME}/omninode_infra`.
    command: |-
      uv run pytest tests/scripts/test_staging_green_bar_leg2_real_revision.py tests/scripts/test_collect_staging_green_facts.py tests/scripts/test_evaluate_staging_green_bar.py tests/scripts/test_staging_green_bar_leg3_real_runs.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1264"
    description: >
      PR #1264 on OmniNode-ai/omninode_infra — the leg 2 re-key. Bound by hand (union-resolve) because
      the autobind published no companion for this ticket.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1264 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1264/files --paginate --jq '.[].filename' | grep -cFx 'scripts/collect_staging_green_facts.py'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #1264 on OmniNode-ai/omninode_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1264"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/scripts/test_staging_green_bar_leg2_real_revision.py tests/scripts/test_collect_staging_green_facts.py tests/scripts/test_evaluate_staging_green_bar.py tests/scripts/test_staging_green_bar_leg3_real_runs.py -q
        cwd: "${OMNI_HOME}/omninode_infra"
