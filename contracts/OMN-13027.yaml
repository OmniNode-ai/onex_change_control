---
schema_version: "1.0.0"
ticket_id: "OMN-13027"
title: "A-8: Dev-baseline ratchet — scheduled full-suite on dev publishes machine-readable baseline; PR
  check fails any NEW failure repo-wide"
summary: >-
  Retro A-8 (PROCESS_FAILURE_RETRO.md §3.A). Scheduled full-suite on dev publishes config/validation/test-failure-baseline.yaml;
  new failure clusters auto-file Urgent Linear tickets; PR gate (test-failure-ratchet CI job) fails any
  failure cluster not in the baseline; entries expire at 7 days.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-2008"
    description: >-
      omnibase_infra PR #2008 ships the publisher script, ratchet script, baseline YAML, dev-baseline-publisher
      scheduled workflow, test-failure-ratchet CI gate, and 22 unit tests covering all paths.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13027/dod-infra-pr-2008/command.yaml"
          && grep -q '^pr_number: 2008$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13027/dod-infra-pr-2008/command.yaml"
  - id: "dod-occ-pr-self"
    description: "onex_change_control PR carrying the OMN-13027 contract and evidence receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-13027/dod-occ-pr-self/command.yaml"
