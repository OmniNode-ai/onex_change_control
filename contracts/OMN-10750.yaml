---
schema_version: "1.0.0"
ticket_id: "OMN-10750"
title: "DI refactor: HandlerBuildLoopOrchestrator injects HandlerBuildLoop + consolidates HandlerOverseerVerifier"
summary: >
  HandlerBuildLoopOrchestrator gets two fixes: (1) fsm: HandlerBuildLoop | None injectable param — previously
  always constructed unconditionally with no injection path; (2) HandlerOverseerVerifier split-identity
  resolved — overseer_verifier param now wires directly to self._overseer_verifier (the single used reference).
  Dead _advisory_overseer attribute and unused ProtocolOverseerVerifier import removed. Part of OMN-10841
  DI Wave 2 (Bucket A #5,#6).
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests-pass"
    description: "7 injection-path unit tests in test_di_injection_build_loop_orchestrator.py verify both
      injection paths and consolidated overseer reference."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10750/dod-unit-tests-pass/command.yaml\""
  - id: "dod-omnimarket-pr-690"
    description: "omnimarket PR #690 open with CI green at commit c821a0d5a1b20b18260e00be1d5a4d2da2a64cf1."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10750/dod-omnimarket-pr-690/command.yaml\"
          && grep -q '^pr_number: 690$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10750/dod-omnimarket-pr-690/command.yaml\""
