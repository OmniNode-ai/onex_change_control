---
schema_version: "1.0.0"
ticket_id: "OMN-11330"
title: "Wire validation through ticket_contract pipeline in onex-self-extending-agent"
summary: >-
  Bind OCC evidence for onex-self-extending-agent PR #48 (OMN-11330), which adds ModelDelegationValidationResult
  and validate_generation_with_contract(), chaining validate_generation() into the ticket_contract readiness
  gate with strict write isolation under a run-id-scoped subdirectory of the caller's ticket_dir. Receipts
  reference the observed downstream PR head and the OCC self-binding PR.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Downstream PR test suite passes: validate_generation_with_contract chains basic + readiness
      gates and isolates writes under ticket_dir."
    command: "gh pr checks 48 --repo OmniNode-ai/onex-self-extending-agent"
dod_evidence:
  - id: "dod-tests"
    description: "Bind OMN-11330 evidence to onex-self-extending-agent PR #48 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11330/dod-tests/command.yaml\"
          && grep -q '^pr_number: 48$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11330/dod-tests/command.yaml\"
          && grep -q '^commit_sha: \"31d6c90e4a3895f20690be139240ca820132708f\"$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11330/dod-tests/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying the OMN-11330 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11330/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
