---
schema_version: "1.0.0"
ticket_id: "OMN-12381"
title: "Retire superseded omnimarket handler exports"
summary: >
  Central OCC evidence for omnimarket PR #981. The PR removes the superseded node_build_loop_orchestrator
  handler_orchestrator.py draft, preserves the cross-node reach-in allowlist to avoid widening scope,
  and annotates dynamic consumers that were proven live by the dep-health sweep.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-981"
    description: "omnimarket PR #981 carries the OMN-12381 dead-export removal."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12381/dod-omnimarket-pr-981/command.yaml"
  - id: "dod-dead-export-proof"
    description: "The removed handler file is absent and no contract, package export, or source import
      references the superseded handler_orchestrator module."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12381/dod-dead-export-proof/command.yaml"
  - id: "dod-ci-green-excluding-occ"
    description: "All product CI gates other than the expected Receipt Gate and Deploy Gate evidence failures
      are green for omnimarket PR #981."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12381/dod-ci-green-excluding-occ/command.yaml"
  - id: "dod-deploy-gate"
    description: "Runtime-path changes in omnimarket PR #981 have an explicit deploy DoD receipt for post-merge
      runtime validation."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12381/dod-deploy-gate/command.yaml
          && grep -qi deploy drift/dod_receipts/OMN-12381/dod-deploy-gate/command.yaml"
