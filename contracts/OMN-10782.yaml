---
schema_version: "1.0.0"
ticket_id: "OMN-10782"
title: "feat(OMN-10782): interactive executor — drives onboarding flow via adapter + reducer"
summary: >
  Adds InteractiveExecutor that composes TransitionReducer with ProtocolInputAdapter to drive interactive
  onboarding policies from start to terminal step. Pure except for adapter I/O — never writes files. Produces
  ModelInteractiveResult with env dict, step results, and provenance metadata.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr"
    description: "omnibase_infra PR #1555 carries the OMN-10782 implementation."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10782/dod-product-pr/command.yaml\""
  - id: "dod-local-tests"
    description: "Unit and integration tests pass for interactive executor."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10782/dod-local-tests/command.yaml\""
  - id: "dod-occ-contract"
    description: "OCC contract and receipts committed for OMN-10782."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10782/dod-occ-contract/command.yaml\""
  - id: "dod-occ-pr-901"
    description: "Self-binding receipt for onex_change_control PR #901."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10782/dod-occ-pr-901/command.yaml\""
