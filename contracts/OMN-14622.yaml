---
schema_version: "1.0.0"
ticket_id: "OMN-14622"
title: "Autobind OCC evidence for OMN-14622"
summary: >
  OCC contract bound by node_pr_lifecycle_fix_effect Evidence-Source autobind (OMN-13317 F1) when OmniNode-ai/omnimarket
  PR #1764 carried a product-SHA Evidence-Source or was missing an OCC contract, and failed the gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1764 product diff scope present"
    command: "PRODUCT_PR_NUMBER=\"${PRODUCT_PR_NUMBER:-1764}\"; PRODUCT_REPO=\"${PRODUCT_REPO:-OmniNode-ai/omnimarket}\";
      gh pr diff ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --name-only | grep -q ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-1764"
    description: "PR #1764 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=\"${PRODUCT_PR_NUMBER:-1764}\"; PRODUCT_REPO=\"${PRODUCT_REPO:-OmniNode-ai/omnimarket}\";
          gh pr view ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --json number,state"
  - id: "dod-OmniNode-ai-omnimarket-pr-1764-ci"
    description: "PR #1764 on OmniNode-ai/omnimarket — product diff scope (OMN-14425 substance check;
      OMN-14650: no longer gated on source-PR CI being green)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=\"${PRODUCT_PR_NUMBER:-1764}\"; PRODUCT_REPO=\"${PRODUCT_REPO:-OmniNode-ai/omnimarket}\";
          gh pr diff ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --name-only | grep -q ."
  - id: "occ-self-bind-pr-4235"
    description: "OCC companion PR #4235 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14622
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1764-deploy"
    description: "Deploy-readiness assessment for omnimarket PR #1764 runtime-path edits. No live deploy,
      restart, docker exec, rpk topic produce, or .201 mutation was performed by this OCC PR; post-merge
      validation remains in the normal dev deploy pipeline."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14622/dod-omnimarket-pr-1764-deploy/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14622/dod-omnimarket-pr-1764-deploy/command.yaml"
  - id: "occ-self-bind-pr-4236"
    description: "OCC follow-up PR #4236 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14622
      deploy assessment."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1764-head-dcf3d35c"
    description: "Final-head product PR #1764 evidence for OmniNode-ai/omnimarket after OMN-14622 canonical
      effect seam repair."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=\"${PRODUCT_PR_NUMBER:-1764}\"; PRODUCT_REPO=\"${PRODUCT_REPO:-OmniNode-ai/omnimarket}\";
          gh pr view ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --json number,state,headRefName"
  - id: "dod-omnimarket-pr-1764-ci-head-dcf3d35c"
    description: "Final-head product diff scope for OmniNode-ai/omnimarket PR #1764 after OMN-14622 canonical
      effect seam repair."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=\"${PRODUCT_PR_NUMBER:-1764}\"; PRODUCT_REPO=\"${PRODUCT_REPO:-OmniNode-ai/omnimarket}\";
          gh pr diff ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --name-only | grep -q ."
  - id: "occ-self-bind-pr-4237"
    description: "OCC companion PR #4237 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14622
      final-head evidence."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omnimarket-pr-1764-head-62c63500"
    description: "Final-head product PR #1764 evidence for OmniNode-ai/omnimarket after OMN-14622 routable
      OCC companion effect contract repair."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=\"${PRODUCT_PR_NUMBER:-1764}\"; PRODUCT_REPO=\"${PRODUCT_REPO:-OmniNode-ai/omnimarket}\";
          gh pr view ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --json number,state,headRefName"
  - id: "dod-omnimarket-pr-1764-ci-head-62c63500"
    description: "Final-head product diff scope for OmniNode-ai/omnimarket PR #1764 after OMN-14622 routable
      OCC companion effect contract repair."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "PRODUCT_PR_NUMBER=\"${PRODUCT_PR_NUMBER:-1764}\"; PRODUCT_REPO=\"${PRODUCT_REPO:-OmniNode-ai/omnimarket}\";
          gh pr diff ${PRODUCT_PR_NUMBER} --repo ${PRODUCT_REPO} --name-only | grep -q ."
  - id: "occ-self-bind-pr-4238"
    description: "OCC companion PR #4238 on OmniNode-ai/onex_change_control — self-binding proof for OMN-14622
      (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
