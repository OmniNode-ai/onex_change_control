---
schema_version: "1.0.0"
ticket_id: OMN-10066
title: "OCC re-exports core ModelTicketContract + ModelGoldenPath (Part B of OMN-9582)"
summary: "OCC re-exports core ModelTicketContract + ModelGoldenPath (Part B of OMN-9582)"
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: Full OCC test suite passes after re-export
    command: uv run pytest tests/ -v
  - kind: ci
    description: CI passes on merge PR
    command: gh pr checks
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: PR opened against main branch
    source: generated
    checks:
      - check_type: command
        check_value: "gh pr view {pr} --repo {repo} --json state -q .state | grep -q OPEN"
  - id: dod-002
    description: CI checks pass on PR
    source: generated
    checks:
      - check_type: command
        check_value: >-
          gh pr checks {pr} --repo {repo} --json name,state -q 'map(select(.state=="FAILURE")) | length
          == 0'
  - id: dod-003
    description: Tests pass in CI
    source: generated
    checks:
      - check_type: command
        check_value: >-
          gh pr checks {pr} --repo {repo} --json name,state -q 'map(select(.name|test("Tests"))) | map(select(.state=="SUCCESS"))
          | length > 0'
  - id: dod-004
    description: Pre-commit hooks clean (verified locally before push)
    source: manual
    checks:
      - check_type: command
        check_value: >-
          gh pr checks {pr} --repo {repo} --json name,state -q 'map(select(.name|test("Pre-commit")))
          | map(select(.state=="SUCCESS")) | length > 0'
  - id: dod-005
    description: Contract/schema artifact file exists
    source: generated
    checks:
      - check_type: command
        check_value: "test -f contracts/OMN-10066.yaml"
