---
schema_version: "1.0.0"
ticket_id: OMN-10066
summary: "Extend core ModelDodCheck with check_type+check_value fields (Part A of OMN-9582)"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: tests
    description: ModelDodCheck merged-fields tests pass (7 cases)
    command: uv run pytest tests/unit/models/contracts/ticket/test_model_dod_check_merged_fields.py -v
  - kind: ci
    description: omnibase_core PR 956 CI green
    command: gh pr checks 956 --repo OmniNode-ai/omnibase_core
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: ModelDodCheck merged-fields unit tests pass (7 cases)
    source: manual
    checks:
      - check_type: command
        check_value: uv run pytest tests/unit/models/contracts/ticket/test_model_dod_check_merged_fields.py
          -v
  - id: dod-002
    description: Full contracts + ticket model tests pass with no regressions (3265 passed)
    source: manual
    checks:
      - check_type: command
        check_value: gh pr checks 956 --repo OmniNode-ai/omnibase_core
  - id: dod-003
    description: Lint, format, and mypy pass with zero new errors
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 956 --repo OmniNode-ai/omnibase_core --json headRefOid --jq '.headRefOid'
