---
schema_version: 1.0.0
ticket_id: OMN-14209
title: "fix(OMN-14209): extend prod-grant gate to k3s deploy-onex-prod.yml"
summary: >-
  omninode_infra PR #583 extends the OMN-13418 production-promotion grant gate to the k3s deploy-onex-prod.yml
  manual promotion workflow. The workflow's GitHub Environment protection is branch-policy-only, so the
  PR adds a standalone grant validator and inserts runtime-image and onex-api-image grant checks before
  the first prod-mutating kubectl apply step. The validator reads grants from onex_change_control main
  via CROSS_REPO_PAT, enforces approved_by != requested_by, unexpired/unconsumed grant state, runtime_lane
  prod, and image_digest equality, and allows only a target-correct kubeconfig-based unhealthy-runtime
  waiver.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - public_api
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: >-
      Independent verification of omninode_infra PR #583 at head b144aa4439b2d8ddf0f06111a908377f44953d0f.
      Focused workflow and validator tests passed 45/45, covering grant resolution, anti-self-approval,
      expiration/consumed failure modes, runtime/onex-api gate placement before prod apply, CROSS_REPO_PAT
      wiring, and the target-correct kubeconfig health probe that never uses a public hostname.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-001/command.yaml && grep -q '^pr_number:
          583$' drift/dod_receipts/OMN-14209/dod-001/command.yaml && grep -q 'b144aa4439b2d8ddf0f06111a908377f44953d0f'
          drift/dod_receipts/OMN-14209/dod-001/command.yaml
  - id: dod-deploy-assessment
    description: >-
      Deploy-surface assessment for PR #583. The PR changes the deployment workflow and a standalone validation
      script plus tests only; it does not perform a live deploy, restart, kubectl apply, AWS mutation,
      .201 mutation, or branch-protection/CODEOWNERS admin change. The new gate is fail-closed before
      the first prod-mutating step and therefore reduces deploy risk without mutating runtime state during
      PR validation.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-deploy-assessment/command.yaml &&
          grep -qi 'no live deploy' drift/dod_receipts/OMN-14209/dod-deploy-assessment/command.yaml
  - id: dod-occ-self
    description: >-
      OCC companion self-binding for omninode_infra #583. This onex_change_control PR carries the central
      OMN-14209 contract and PASS receipts consumed by #583 through its Evidence-Source line. Self-binds
      via the first commit SHA so occ-preflight eligibility does not require predicting the OCC PR number.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-occ-self/command.yaml
  - id: dod-001-head-correction
    description: >-
      Add-only correction for omninode_infra PR #583 current head b144aa417b2a2f7fdf15f5c748ace1e0b0e213dd.
      The original dod-001 evidence text carried a transcribed SHA that did not match the live product
      PR head. This evidence item preserves append-only OCC history while binding the verified 45-test
      product proof to the actual product commit.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-001-head-correction/command.yaml &&
          grep -q '^pr_number: 583$' drift/dod_receipts/OMN-14209/dod-001-head-correction/command.yaml
          && grep -q 'b144aa417b2a2f7fdf15f5c748ace1e0b0e213dd' drift/dod_receipts/OMN-14209/dod-001-head-correction/command.yaml
  - id: dod-occ-correction-self
    description: >-
      Self-binding for the add-only OMN-14209 OCC correction PR #3959. This PR appends corrected evidence
      for omninode_infra #583's actual head without mutating the already-merged OCC #3958 files.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-occ-correction-self/command.yaml &&
          grep -q '^pr_number: 3959$' drift/dod_receipts/OMN-14209/dod-occ-correction-self/command.yaml
  - id: dod-main-hotfix-pr-586-head-c5a3aef
    description: >-
      Foreground Codex verification of omninode_infra main hotfix PR #586 at head c5a3aeffd6f39ab4ae5399a743f38cd893cea8e0.
      The hotfix preserves the original #584 four-file aws/cluster-dev Terraform guard repair on a policy-valid
      hotfix/* branch: managed data-plane auto tfvars are enabled, MSK prevent_destroy guard count is
      7, RDS prevent_destroy guard count is 9, aws_profile defaults to null, and git diff --check passes.
      Terraform CLI was unavailable in the Codex environment; the source PR body reports terraform validate
      and live-state plan proof.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-main-hotfix-pr-586-head-c5a3aef/command.yaml
          && grep -q '^pr_number: 586$' drift/dod_receipts/OMN-14209/dod-main-hotfix-pr-586-head-c5a3aef/command.yaml
          && grep -q 'c5a3aeffd6f39ab4ae5399a743f38cd893cea8e0' drift/dod_receipts/OMN-14209/dod-main-hotfix-pr-586-head-c5a3aef/command.yaml
  - id: dod-backmerge-pr-585-head-c5a3aef
    description: >-
      Backmerge reference for the OMN-14209 main hotfix. omninode_infra PR #585 targets dev from the same
      hotfix/omn-14209-cluster-dev-destroy-guards head c5a3aeffd6f39ab4ae5399a743f38cd893cea8e0 so the
      main-target hotfix guard has a real backmerge PR reference while preserving the exact main hotfix
      commit.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-backmerge-pr-585-head-c5a3aef/command.yaml
          && grep -q '^pr_number: 585$' drift/dod_receipts/OMN-14209/dod-backmerge-pr-585-head-c5a3aef/command.yaml
          && grep -q 'c5a3aeffd6f39ab4ae5399a743f38cd893cea8e0' drift/dod_receipts/OMN-14209/dod-backmerge-pr-585-head-c5a3aef/command.yaml
  - id: dod-main-hotfix-deploy-assessment-head-c5a3aef
    description: >-
      Deploy-surface assessment for omninode_infra PR #586 head c5a3aeffd6f39ab4ae5399a743f38cd893cea8e0:
      the diff is limited to aws/cluster-dev Terraform guard and auto-loaded tfvars repair. The validation
      performed here does not apply Terraform, mutate AWS, deploy, restart services, mutate .201, or change
      branch protection/admin settings.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-main-hotfix-deploy-assessment-head-c5a3aef/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14209/dod-main-hotfix-deploy-assessment-head-c5a3aef/command.yaml
  - id: dod-occ-main-hotfix-self
    description: >-
      Self-binding for the add-only OMN-14209 OCC companion that appends evidence for omninode_infra #586
      and backmerge #585. The self receipt binds the initial OCC evidence commit to the actual OCC PR
      number before downstream receipt gates consume it.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-occ-main-hotfix-self/command.yaml
          && grep -Eq '^[[:space:]]*commit_sha: [0-9a-f]{40}$' drift/dod_receipts/OMN-14209/dod-occ-main-hotfix-self/command.yaml
  - id: dod-main-hotfix-pr-586-head-d83bb24
    description: >-
      Add-only refresh for omninode_infra PR #586 at head d83bb240c50d080bd13efa1d7fbfe52e54909ceb. This
      head preserves the aws/cluster-dev destroy-guard hotfix and adds the required k8s/onex-dev/jobs/desired-clients.json
      parity sync to match the canonical omnibase_infra copy. Local Codex checks passed: git diff --check,
      desired-clients SHA equality/diff clean, MSK prevent_destroy count 7, RDS prevent_destroy count
      9, managed data-plane auto tfvars true, and aws_profile default null. Terraform CLI was unavailable
      in the Codex environment.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-main-hotfix-pr-586-head-d83bb24/command.yaml
          && grep -q '^pr_number: 586$' drift/dod_receipts/OMN-14209/dod-main-hotfix-pr-586-head-d83bb24/command.yaml
          && grep -q 'd83bb240c50d080bd13efa1d7fbfe52e54909ceb' drift/dod_receipts/OMN-14209/dod-main-hotfix-pr-586-head-d83bb24/command.yaml
  - id: dod-backmerge-pr-585-head-d83bb24
    description: >-
      Add-only refresh for omninode_infra PR #585 backmerge reference at head d83bb240c50d080bd13efa1d7fbfe52e54909ceb.
      The backmerge reference continues to point at the same hotfix branch used by main hotfix PR #586
      after the desired-clients parity repair.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-backmerge-pr-585-head-d83bb24/command.yaml
          && grep -q '^pr_number: 585$' drift/dod_receipts/OMN-14209/dod-backmerge-pr-585-head-d83bb24/command.yaml
          && grep -q 'd83bb240c50d080bd13efa1d7fbfe52e54909ceb' drift/dod_receipts/OMN-14209/dod-backmerge-pr-585-head-d83bb24/command.yaml
  - id: dod-main-hotfix-deploy-assessment-head-d83bb24
    description: >-
      Deploy-surface refresh for omninode_infra PR #586 head d83bb240c50d080bd13efa1d7fbfe52e54909ceb:
      the diff now includes the aws/cluster-dev Terraform guard repair plus the desired-clients parity
      sync required by CI. Validation performed here did not apply Terraform, mutate AWS, deploy, restart
      services, mutate .201, or change branch protection/admin settings.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-main-hotfix-deploy-assessment-head-d83bb24/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14209/dod-main-hotfix-deploy-assessment-head-d83bb24/command.yaml
  - id: dod-main-hotfix-pr-586-head-65d7ecb
    description: >-
      Add-only refresh for omninode_infra PR #586 at head 65d7ecbfc3830654ddb8fd2b4e205414f2edb0d1. This
      head preserves the aws/cluster-dev destroy-guard hotfix, keeps desired-clients parity with omnibase_infra,
      and adds the reviewed .secrets.baseline false-positive entry required by the detect-secrets pre-commit
      hook for the secretEnv environment-variable name. Local Codex checks passed: git diff --check, desired-clients
      SHA equality, targeted detect-secrets pre-commit, MSK prevent_destroy count 7, RDS prevent_destroy
      count 9, managed data-plane auto tfvars true, and aws_profile default null. Full pre-commit passed
      non-Terraform hooks; local terraform_validate failed only on provider install timeouts.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-main-hotfix-pr-586-head-65d7ecb/command.yaml
          && grep -q '^pr_number: 586$' drift/dod_receipts/OMN-14209/dod-main-hotfix-pr-586-head-65d7ecb/command.yaml
          && grep -q '65d7ecbfc3830654ddb8fd2b4e205414f2edb0d1' drift/dod_receipts/OMN-14209/dod-main-hotfix-pr-586-head-65d7ecb/command.yaml
  - id: dod-backmerge-pr-585-head-65d7ecb
    description: >-
      Add-only refresh for omninode_infra PR #585 backmerge reference at head 65d7ecbfc3830654ddb8fd2b4e205414f2edb0d1.
      The backmerge reference continues to point at the same hotfix branch used by main hotfix PR #586
      after the detect-secrets baseline repair.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-backmerge-pr-585-head-65d7ecb/command.yaml
          && grep -q '^pr_number: 585$' drift/dod_receipts/OMN-14209/dod-backmerge-pr-585-head-65d7ecb/command.yaml
          && grep -q '65d7ecbfc3830654ddb8fd2b4e205414f2edb0d1' drift/dod_receipts/OMN-14209/dod-backmerge-pr-585-head-65d7ecb/command.yaml
  - id: dod-main-hotfix-deploy-assessment-head-65d7ecb
    description: >-
      Deploy-surface refresh for omninode_infra PR #586 head 65d7ecbfc3830654ddb8fd2b4e205414f2edb0d1:
      the diff includes the aws/cluster-dev Terraform guard repair, desired-clients parity sync, and the
      .secrets.baseline false-positive entry for the environment-variable-name scanner finding. Validation
      performed here did not apply Terraform, mutate AWS, deploy, restart services, mutate .201, or change
      branch protection/admin settings.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-main-hotfix-deploy-assessment-head-65d7ecb/command.yaml
          && grep -qi 'no live deploy' drift/dod_receipts/OMN-14209/dod-main-hotfix-deploy-assessment-head-65d7ecb/command.yaml
  - id: dod-occ-main-hotfix-65d7ecb-self
    description: >-
      Self-binding for onex_change_control PR #3970, which appends OMN-14209 evidence for omninode_infra
      #586 head 65d7ecbfc3830654ddb8fd2b4e205414f2edb0d1 after the detect-secrets baseline repair. This
      companion does not change product code, deploy, restart services, mutate AWS, mutate .201, or change
      branch protection/admin settings.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-occ-main-hotfix-65d7ecb-self/command.yaml
          && grep -q '^pr_number: 3970$' drift/dod_receipts/OMN-14209/dod-occ-main-hotfix-65d7ecb-self/command.yaml
          && grep -q '6043591fef3585505bfd9064ad8e47d33ee8ae40' drift/dod_receipts/OMN-14209/dod-occ-main-hotfix-65d7ecb-self/command.yaml
  - id: dod-occ-main-hotfix-refresh-self
    description: >-
      Self-binding for the add-only OMN-14209 OCC refresh that appends d83bb24 evidence for omninode_infra
      #586 and #585 after the required desired-clients parity repair.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-occ-main-hotfix-refresh-self/command.yaml
          && grep -Eq '^[[:space:]]*commit_sha: [0-9a-f]{40}$' drift/dod_receipts/OMN-14209/dod-occ-main-hotfix-refresh-self/command.yaml
  - id: dod-occ-stale-receipt-repair-self
    description: >-
      Self-binding for onex_change_control PR #3967, which appends only receipt supersession sidecars
      to rebind stale OMN-14209 whole-file receipt hashes exposed by the #586/#585 main-hotfix refresh.
      The repair does not change product code, deploy, restart services, mutate AWS, mutate .201, or change
      branch protection/admin settings.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14209/dod-occ-stale-receipt-repair-self/command.yaml
          && grep -q '^pr_number: 3967$' drift/dod_receipts/OMN-14209/dod-occ-stale-receipt-repair-self/command.yaml
          && grep -q '898e067ccc19a7eb6a94c777b744f3a1c78a8af6' drift/dod_receipts/OMN-14209/dod-occ-stale-receipt-repair-self/command.yaml
