---
schema_version: "1.0.0"
ticket_id: "OMN-16844"
title: "Autobind OCC evidence for OMN-16844"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #2965.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2965 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2965 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: "uv run pytest tests/unit/event_bus/test_topic_provisioner_rf_policy_omn15395.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-2965"
    description: "PR #2965 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/unit/event_bus/test_topic_provisioner_rf_policy_omn15395.py?ref=568e5f465f68666e12fa702fc0a499cd0535a91a --jq '.content' | base64 -d | grep -c 'def test_batch_creation_records_the_clamped_creation_spec'
      - check_type: "command"
        check_value: "gh pr view 2965 --repo OmniNode-ai/omnibase_infra --json files"
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2965 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/event_bus/test_topic_provisioner_rf_policy_omn15395.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
