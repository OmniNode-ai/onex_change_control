---
schema_version: "1.0.0"
ticket_id: "OMN-10089"
summary: "Add a Keycloak readiness healthcheck to docker-compose"
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused compose YAML assertion proves the Keycloak healthcheck shape"
    command: "cd ../omnibase_infra && uv run python -c 'from pathlib import Path; import yaml; data=yaml.safe_load(Path(\"docker/docker-compose.infra.yml\").read_text());
      svc=data[\"services\"][\"keycloak\"]; hc=svc[\"healthcheck\"]; test=\" \".join(hc[\"test\"]); assert
      svc[\"environment\"][\"KC_HEALTH_ENABLED\"] == \"true\"; assert hc[\"interval\"] == \"15s\"; assert
      hc[\"start_period\"] == \"60s\"; assert hc[\"retries\"] == 10; assert \"bash -ec\" in test; assert
      \"health/ready\" in test; assert \"localhost/9000\" in test; assert \"curl\" not in test; print(\"keycloak
      healthcheck validated\")'"
  - kind: "ci"
    description: "omnibase_infra PR checks pass for the Keycloak compose healthcheck"
    command: "gh pr checks 1433 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "The docker-compose Keycloak service has a management-port readiness healthcheck"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "find drift/dod_receipts/OMN-10089/dod-001 -type f -name '*.yaml' | grep -q ."
