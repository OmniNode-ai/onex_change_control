---
schema_version: "1.0.0"
ticket_id: OMN-10089
title: "Wire seed-keycloak-clients reconciler into Makefile (local install)"
summary: "Add the seed-keycloak orchestration wrapper, make Keycloak default-on in local compose, and
  keep stability-test compose isolation intact."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "Focused stability compose render test proves default-on Keycloak remains lane-isolated"
    command: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10089/dod-001/command.yaml"
  - kind: "ci"
    description: "omnibase_infra PR checks pass for the seed-keycloak orchestration PR"
    command: "gh pr checks 1500 --repo OmniNode-ai/omnibase_infra"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Focused stability compose render test passes with default-on Keycloak isolated"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10089/dod-001/command.yaml && grep
          -q '^ticket_id: OMN-10089$' drift/dod_receipts/OMN-10089/dod-001/command.yaml && grep -q '^evidence_item_id:
          dod-001$' drift/dod_receipts/OMN-10089/dod-001/command.yaml"
