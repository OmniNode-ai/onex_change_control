---
schema_version: 1.0.0
ticket_id: OMN-14747
title: Evidence for OMN-14747 RuntimeDispatch single core runtime over transport DI
summary: Central OCC evidence for omnibase_core#1466. The product PR adds RuntimeDispatch, envelope fan-out
  stamping, prefix commit behavior, and regression tests at exact product head d2e8ecfc8cf0c8b6b32079e89b2591b6c4db27ec.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - events
  - protocols
evidence_requirements:
  - kind: ci
    description: 'PR #1466 product head is exact for OMN-14747 evidence'
    command: 'PRODUCT_PR_NUMBER=1466; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=d2e8ecfc8cf0c8b6b32079e89b2591b6c4db27ec;
      : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid --jq
      .headRefOid)" = "$PRODUCT_HEAD"'
  - kind: ci
    description: 'PR #1466 omnibase_core CI completed successfully at exact product head'
    command: 'PRODUCT_RUN_ID=29627490823; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=d2e8ecfc8cf0c8b6b32079e89b2591b6c4db27ec;
      : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
      = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
      = "$PRODUCT_HEAD"'
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-omnibase_core-pr-1466
    description: 'PR #1466 on OmniNode-ai/omnibase_core exists and is bound to OMN-14747 at exact product
      head d2e8ecfc8cf0c8b6b32079e89b2591b6c4db27ec.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1466; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=d2e8ecfc8cf0c8b6b32079e89b2591b6c4db27ec;
          : ${PR_NUMBER}; test "$(gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json headRefOid
          --jq .headRefOid)" = "$PRODUCT_HEAD"'
  - id: dod-omnibase_core-pr-1466-diff
    description: 'PR #1466 on OmniNode-ai/omnibase_core has a non-empty reviewed product diff for OMN-14747.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1466; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER};
          gh pr diff $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --name-only | grep -q .'
  - id: dod-omnibase_core-pr-1466-ci
    description: 'The omnibase_core CI workflow completed successfully for PR #1466 at exact product head
      d2e8ecfc8cf0c8b6b32079e89b2591b6c4db27ec before OCC evidence was minted.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_RUN_ID=29627490823; PRODUCT_REPO=OmniNode-ai/omnibase_core; PRODUCT_HEAD=d2e8ecfc8cf0c8b6b32079e89b2591b6c4db27ec;
          : ${PR_NUMBER}; test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .conclusion)"
          = success && test "$(gh api repos/${PRODUCT_REPO}/actions/runs/${PRODUCT_RUN_ID} --jq .head_sha)"
          = "$PRODUCT_HEAD"'
  - id: dod-omnibase_core-pr-1466-local-verification
    description: 'Product PR body records local verification for OMN-14747: ruff format/check clean, mypy
      clean for changed src, pre-commit all 111 hooks Passed/Skipped with 0 Failed, and 26 new tests plus
      the full runtime suite green.'
    source: generated
    checks:
      - check_type: command
        check_value: 'PRODUCT_PR_NUMBER=1466; PRODUCT_REPO=OmniNode-ai/omnibase_core; : ${PR_NUMBER};
          gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json body --jq .body | grep -q "all 111
          hooks Passed/Skipped, 0 Failed" && gh pr view $PRODUCT_PR_NUMBER --repo $PRODUCT_REPO --json
          body --jq .body | grep -q "26 new tests + full runtime suite (309) green"'
  - id: dod-omnibase_core-pr-1466-deploy
    description: Deploy-readiness assessment for OMN-14747. The PR adds a net-new RuntimeDispatch implementation
      and tests but is not wired into service_kernel or production runtime paths; no deploy, restart,
      .201, or production mutation was performed by this OCC evidence lane.
    source: generated
    checks:
      - check_type: command
        check_value: 'grep -q ''^status: PASS$'' drift/dod_receipts/OMN-14747/dod-omnibase_core-pr-1466-deploy/command.yaml
          && grep -qi ''no deploy, restart, .201, or production mutation'' drift/dod_receipts/OMN-14747/dod-omnibase_core-pr-1466-deploy/command.yaml'
