---
schema_version: "1.0.0"
ticket_id: "OMN-12443"
title: "Repair stability Pattern B broker terminal wait for session_orchestrator"
summary: >
  Align the omnimarket Codex Pattern B adapter defaults with the active stability runtime broker topics
  so runbook foreground requests publish to the consumed omnimarket Pattern B bridge without manual topic
  overrides.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnimarket PR #996 carries the OMN-12443 default-topic fix at the recorded head SHA."
    command: "gh pr view 996 --repo OmniNode-ai/omnimarket --json number,url,state,headRefOid,headRefName,title,baseRefName"
  - kind: "tests"
    description: "Focused Codex runtime client regression suite passes locally at the recorded head SHA."
    command: "uv run pytest tests/test_codex_runtime_client.py -q"
  - kind: "tests"
    description: "Strict omnimarket mypy passes at the recorded head SHA."
    command: "uv run mypy src/omnimarket/ --strict"
  - kind: "manual"
    description: "Stability runtime session_orchestrator dry-run succeeds without Pattern B topic env
      overrides."
    command: "env -u PYTHONPATH uv run python scripts/run_codex_runtime_request.py --command-name session_orchestrator
      --payload '<payload>' --timeout-ms 300000 --target-runtime-address runtime://omninode-pc/stability-test/main"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-pr-996"
    description: "omnimarket PR #996 is open against dev at the recorded OMN-12443 default-topic fix head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12443/dod-omnimarket-pr-996/command.yaml" &&
          grep -q '^pr_number: 996$' "drift/dod_receipts/OMN-12443/dod-omnimarket-pr-996/command.yaml"
          && grep -q '^commit_sha: "d7aa22f41564ac257f368215cb358f89c1df3b45"$' "drift/dod_receipts/OMN-12443/dod-omnimarket-pr-996/command.yaml"
  - id: "dod-runtime-client-tests"
    description: "Focused Codex runtime client regression tests pass at the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12443/dod-runtime-client-tests/command.yaml"
          && grep -q '^commit_sha: "d7aa22f41564ac257f368215cb358f89c1df3b45"$' "drift/dod_receipts/OMN-12443/dod-runtime-client-tests/command.yaml"
  - id: "dod-strict-mypy"
    description: "Strict omnimarket mypy passes at the recorded head."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12443/dod-strict-mypy/command.yaml" && grep
          -q '^commit_sha: "d7aa22f41564ac257f368215cb358f89c1df3b45"$' "drift/dod_receipts/OMN-12443/dod-strict-mypy/command.yaml"
  - id: "dod-stability-dogfood"
    description: "Stability runtime session_orchestrator dry-run succeeds with default Pattern B adapter
      topics."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12443/dod-stability-dogfood/command.yaml" &&
          grep -q '^commit_sha: "d7aa22f41564ac257f368215cb358f89c1df3b45"$' "drift/dod_receipts/OMN-12443/dod-stability-dogfood/command.yaml"
  - id: "dod-occ-pr-1891"
    description: "onex_change_control PR #1891 carries the central OMN-12443 contract and receipt evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "drift/dod_receipts/OMN-12443/dod-occ-pr-1891/command.yaml" && grep
          -q '^pr_number: 1891$' "drift/dod_receipts/OMN-12443/dod-occ-pr-1891/command.yaml" && grep -q
          '^commit_sha: "0a103ac911135234c424d093c93df3a44612e85b"$' "drift/dod_receipts/OMN-12443/dod-occ-pr-1891/command.yaml"
