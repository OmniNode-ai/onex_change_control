---
schema_version: "1.0.0"
ticket_id: "OMN-11010"
title: "Quality: exclude generated and fixture migrations from migration-conflicts scan"
summary: "Updates the migration-conflicts scanner to ignore generated dependency, cache, agent-worktree,
  and repo-local fixture migration copies while preserving explicit fixture-root test scans."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Focused migration-conflicts regression tests cover generated tree and fixture exclusions."
    command: "uv run pytest tests/unit/scripts/test_check_migration_conflicts.py -q"
  - kind: "manual"
    description: "Suppressions-aware cross-repo scan no longer reports generated or fixture migration
      noise."
    command: "uv run check-migration-conflicts --repos-root /Volumes/PRO-G40/Code/omni_home --repos omnibase_infra
      omniclaude omniintelligence omnimarket onex_change_control --suppressions-file /Volumes/PRO-G40/Code/omni_home/omni_worktrees/OMN-11010/onex_change_control/migration_conflict_suppressions.yaml
      --warn-only"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-focused-tests"
    description: "Focused migration-conflicts unit tests pass with generated-tree and fixture exclusion
      coverage."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11010/dod-focused-tests/command.yaml"
  - id: "dod-generated-fixture-exclusions"
    description: "Cross-repo migration scan excludes generated and fixture copies, leaving only real unsuppressed
      owner decisions."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11010/dod-generated-fixture-exclusions/command.yaml
          && grep -q 'node_service_registry' drift/dod_receipts/OMN-11010/dod-generated-fixture-exclusions/command.yaml"
