---
schema_version: "1.0.0"
ticket_id: OMN-10394
title: "Implement JSON handler"
summary: "Add the shared JSON output handler for typed market CLI reports."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "JSON output handler tests pass"
    command: "uv run pytest tests/unit/cli/output/handlers/test_handler_json.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "JSON output handler tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/cli/output/handlers/test_handler_json.py -q"
  - id: "dod-002"
    description: "No deploy required; JSON rendering is local CLI presentation logic"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "echo 'no deploy required for OMN-10394 - local CLI renderer only'"
