---
schema_version: "1.0.0"
ticket_id: "OMN-9161"
title: "Propagate session and run identity into session orchestrator dispatch env"
summary: >
  Binds omnimarket PR #586, which adds _build_dispatch_env to HandlerSessionOrchestrator for propagating
  ONEX_SESSION_ID, ONEX_RUN_ID, and ONEX_CORRELATION_PREFIX into dispatched subprocess environments. Also
  fixes pre-existing hardcoded Kafka topic constants by loading topic names from the node's contract.yaml
  at startup.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-market-pr-586"
    description: "omnimarket PR #586 carries the OMN-9161 HandlerSessionOrchestrator changes."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 586 --repo OmniNode-ai/omnimarket --json state -q .state"
  - id: "dod-dispatch-env-tests"
    description: "Unit tests for _build_dispatch_env helper pass in omnimarket."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 586 --repo OmniNode-ai/omnimarket --json statusCheckRollup -q '.statusCheckRollup[]
          | select(.name==\"test\") | .conclusion'"
  - id: "dod-occ-pr"
    description: "Bind this OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view --repo OmniNode-ai/onex_change_control --head jonah/omn-9159-9161-9162-occ-contracts
          --json state -q .state"
