---
schema_version: "1.0.0"
ticket_id: "OMN-9161"
title: "Propagate session and run identity into session orchestrator dispatch env"
summary: >
  Binds omnimarket PR #586, which adds dispatch environment propagation for ONEX_SESSION_ID, ONEX_RUN_ID,
  and ONEX_CORRELATION_PREFIX in HandlerSessionOrchestrator. The follow-up fix keeps session orchestrator
  Kafka topic constants contract-derived so the handler does not carry hardcoded topic literals.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-586-head"
    description: "omnimarket PR #586 carries the OMN-9161 implementation at the recorded head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9161/dod-pr-586-head/command.yaml\""
  - id: "dod-dispatch-env-tests"
    description: "Focused session orchestrator dispatch env and consumer tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9161/dod-dispatch-env-tests/command.yaml\""
  - id: "dod-ruff"
    description: "Ruff passes on changed session orchestrator handler and focused tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9161/dod-ruff/command.yaml\""
  - id: "dod-arch-topic-lint"
    description: "Changed Python source no longer contains hardcoded onex topic literals."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9161/dod-arch-topic-lint/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9161/dod-occ-pr/command.yaml\""
