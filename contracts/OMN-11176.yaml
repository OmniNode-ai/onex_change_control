---
schema_version: "1.0.0"
ticket_id: "OMN-11176"
title: "Route onex setup orchestration through bus"
summary: >
  Refactor scripts/onex-setup.py to publish a typed ModelEventEnvelope to the ONEX event bus (onex.cmd.omnibase-infra.setup-orchestration-start.v1)
  instead of invoking setup orchestration logic directly. Adds dry-run support and tests.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: "ENRICHED"
evidence_requirements:
  - kind: "ci"
    description: "omnibase_infra PR #1697 CI checks pass."
    command: "gh pr checks 1697 --repo OmniNode-ai/omnibase_infra --watch"
  - kind: "tests"
    description: "Unit tests for onex-setup.py CLI and bus publishing pass."
    command: "uv run pytest tests/unit/scripts/test_onex_setup_cli.py -v"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-pr-1697-open"
    description: "omnibase_infra PR #1697 carries the bus-routed onex setup orchestration branch."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11176/dod-pr-1697-open/command.yaml"
          && grep -q '^pr_number: 1697$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11176/dod-pr-1697-open/command.yaml"
          && grep -q '^commit_sha: "d8259e47ac18e90a6b050a384ec5b38e9cec7006"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11176/dod-pr-1697-open/command.yaml"
  - id: "dod-occ-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11176 contract evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11176/dod-occ-self/command.yaml"
