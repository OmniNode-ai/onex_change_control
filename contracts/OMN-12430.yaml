---
schema_version: "1.0.0"
ticket_id: OMN-12430
title: "Remove dead omniclaude CircuitBreaker and safe_execute_operation utilities"
summary: >
  Remove the confirmed-unused CircuitBreaker and safe_execute_operation symbols from omniclaude's error_handling
  utility module and remove their lazy import table entries. The live pattern-tracking logger and error-policy
  symbols remain in place.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omniclaude PR #1706 carries only the intended two-file dead-code removal."
    command: "gh pr view 1706 --repo OmniNode-ai/omniclaude --json files,headRefOid"
  - kind: "manual"
    description: "Reachability grep finds no remaining CircuitBreaker or safe_execute_operation references
      outside deleted definitions."
    command: "grep -rn --include='*.py' -wE 'safe_execute_operation|CircuitBreaker' ."
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omniclaude-pr-1706"
    description: "omniclaude PR #1706 is open at the current OMN-12430 dead-code removal head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12430/dod-omniclaude-pr-1706/command.yaml\"
          && grep -q '^pr_number: 1706$' \"drift/dod_receipts/OMN-12430/dod-omniclaude-pr-1706/command.yaml\"
          && grep -q '^commit_sha: \"c29acd6d00d466a696d6a382ed5f80d81f9589d3\"$' \"drift/dod_receipts/OMN-12430/dod-omniclaude-pr-1706/command.yaml\""
  - id: "dod-reachability-grep"
    description: "The removed symbols have no remaining Python references in the omniclaude PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"drift/dod_receipts/OMN-12430/dod-reachability-grep/command.yaml\"
          && grep -q '^pr_number: 1706$' \"drift/dod_receipts/OMN-12430/dod-reachability-grep/command.yaml\""
