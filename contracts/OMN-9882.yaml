---
schema_version: "1.0.0"
ticket_id: OMN-9882
title: "Add Codex runtime shims for bus-audit and gap"
summary: "Extend the Codex ONEX plugin surface with dispatch-only shims for node_bus_audit_compute and
  node_gap_compute."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "manual"
    description: "Codex plugin shim and installer tests pass"
    command: "uv run pytest tests/test_codex_plugin_shims.py tests/test_install_codex_skills.py -q"
  - kind: "manual"
    description: "Touched Codex shim tests pass lint"
    command: "uv run ruff check tests/test_codex_plugin_shims.py"
  - kind: "manual"
    description: "Bus audit Codex runtime request compiles without broker publish"
    command: "env -u PYTHONPATH uv run python scripts/run_codex_runtime_request.py --command-name bus_audit_compute
      --payload '{\"dry_run\":true,\"failures_only\":true}' --timeout-ms 30000 --compile-only"
  - kind: "manual"
    description: "Gap Codex runtime request compiles without broker publish"
    command: "env -u PYTHONPATH uv run python scripts/run_codex_runtime_request.py --command-name gap_compute
      --payload '{\"subcommand\":\"detect\",\"dry_run\":true,\"skip_infra_probes\":true}' --timeout-ms
      30000 --compile-only"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-tests"
    description: "Codex plugin shim and installer tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_codex_plugin_shims.py tests/test_install_codex_skills.py
          -q"
  - id: "dod-lint"
    description: "Touched Codex shim tests pass lint"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check tests/test_codex_plugin_shims.py"
  - id: "dod-bus-audit-compile"
    description: "Bus audit Codex runtime request compiles without broker publish"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run python scripts/run_codex_runtime_request.py --command-name
          bus_audit_compute --payload '{\"dry_run\":true,\"failures_only\":true}' --timeout-ms 30000 --compile-only"
  - id: "dod-gap-compile"
    description: "Gap Codex runtime request compiles without broker publish"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "env -u PYTHONPATH uv run python scripts/run_codex_runtime_request.py --command-name
          gap_compute --payload '{\"subcommand\":\"detect\",\"dry_run\":true,\"skip_infra_probes\":true}'
          --timeout-ms 30000 --compile-only"
