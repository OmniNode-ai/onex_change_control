---
schema_version: "1.0.0"
ticket_id: "OMN-12521"
title: "SEA->zero: make __main__ freestanding IO contract-native; dashboard_server retired"
summary: >-
  Remove the two freestanding imperative-IO violations the OCC scanner (--scan-freestanding, OMN-12515)
  flags in onex-self-extending-agent src/__main__.py: the line-33 hardcoded onex.evt topic literal (now
  loaded from src/contracts/pipeline_topics.yaml) and the line-840 subprocess git rev-parse shell-out
  (now resolved from the contract-declared provenance channel src/contracts/evidence_provenance.yaml).
  dashboard_server.py was already retired by SEA PR #157, so the catalog's hand_rolled_daemon entry for
  it is stale and needs no conversion. Scope is __main__.py only; eval_runner/consumer/mcp_client/deployer
  belong to sibling clusters.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "--scan-freestanding no longer flags src/__main__.py (before: hardcoded_topic L33 + subprocess_network
      L840; after: clean)."
    command: "See drift/dod_receipts/OMN-12521/dod-scanner-clean/command.yaml"
  - kind: "tests"
    description: "SEA unit suite (the CI test target) passes for the changed module; 4 new tests cover
      the two contract-native helpers."
    command: "See drift/dod_receipts/OMN-12521/dod-unit-suite/command.yaml"
  - kind: "ci"
    description: "mypy --strict, ruff, and pre-commit pass on the SEA change."
    command: "See drift/dod_receipts/OMN-12521/dod-static-checks/command.yaml"
  - kind: "manual"
    description: "onex_change_control PR carrying this contract + receipts binds the central evidence
      publication for SEA PR #178."
    command: "See drift/dod_receipts/OMN-12521/dod-occ-pr/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-scanner-clean"
    description: "--scan-freestanding flags src/__main__.py BEFORE (hardcoded_topic L33, subprocess_network
      L840) and reports it CLEAN AFTER the contract-native refactor in SEA PR #178."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12521/dod-scanner-clean/command.yaml"
  - id: "dod-unit-suite"
    description: "SEA unit suite (tests/unit, the ci.yml test target) passes for the changed module; 4
      new tests cover _load_pipeline_topics and _resolve_evidence_commit_sha."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12521/dod-unit-suite/command.yaml"
  - id: "dod-static-checks"
    description: "mypy --strict (151 files, no issues), ruff check, ruff format, and pre-commit pass on
      the SEA change."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12521/dod-static-checks/command.yaml"
  - id: "dod-occ-pr"
    description: "onex_change_control PR publishes the OMN-12521 central contract and receipts."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12521/dod-occ-pr/command.yaml"
