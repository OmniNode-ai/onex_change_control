---
schema_version: "1.0.0"
ticket_id: "OMN-10604"
title: "Delegation routing production"
summary: "Bind omniclaude PR #1533 response-text extraction, PR #1536 delegation architecture guard, PR
  #1618 delegate skill merge-conflict regression evidence, and PR #1619 delegation daemon classifier-cache
  regression evidence."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Delegation runner unit tests pass with packaged config parsing and generated_text extraction."
    command: "uv run pytest tests/delegation/test_runner.py -q"
  - kind: "ci"
    description: "omniclaude PR #1533 checks are green."
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omniclaude"
  - kind: "ci"
    description: "omniclaude PR #1536 checks prove direct adapter imports are blocked in delegation business
      logic."
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omniclaude"
  - kind: "tests"
    description: "omniclaude PR #1618 delegate skill contract-resolution regression tests pass."
    command: "uv run pytest tests/unit/hooks/lib/test_delegate_contract_resolution.py -q"
  - kind: "ci"
    description: "onex_change_control PR #1183 binds the OMN-10604 PR #1618 receipt."
    command: "gh pr checks 1183 --repo OmniNode-ai/onex_change_control"
  - kind: "tests"
    description: "omniclaude PR #1619 delegation daemon classifier-cache regression tests pass."
    command: "uv run pytest tests/unit/hooks/lib/test_delegation_daemon.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Delegation runner unit tests pass after parsing packaged bifrost_delegation.yaml locally."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "if [ \"$REPO\" = \"OmniNode-ai/onex_change_control\" ]; then grep -q '^status: PASS$'
          \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10604/dod-001/command.yaml\"; else uv run pytest
          tests/delegation/test_runner.py -q; fi"
  - id: "dod-002"
    description: "Bind OMN-10604 evidence to omniclaude PR #1533 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omniclaude --json headRefOid,title"
  - id: "dod-003"
    description: "Bind OMN-10604 architecture-guard evidence to omniclaude PR #1536 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omniclaude --json headRefOid,title"
  - id: "dod-004"
    description: "Bind OMN-10604 delegate skill merge-conflict regression evidence to omniclaude PR #1618
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo OmniNode-ai/omniclaude --json headRefOid,title"
  - id: "dod-occ-pr-1183"
    description: "OCC PR #1183 carries the OMN-10604 central contract and PR #1618 receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10604/dod-occ-pr-1183/command.yaml\"
          && grep -q '^pr_number: 1183$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10604/dod-occ-pr-1183/command.yaml\""
  - id: "dod-005"
    description: "Bind OMN-10604 delegation daemon classifier-cache regression evidence to omniclaude
      PR #1619 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/unit/hooks/lib/test_delegation_daemon.py -q"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-10604 central contract and receipt evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10604/dod-occ-pr/command.yaml\"
          && grep -q '^pr_number: 793$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10604/dod-occ-pr/command.yaml\""
