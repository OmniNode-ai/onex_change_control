---
schema_version: "1.0.0"
ticket_id: "OMN-15227"
title: "A2 (omnimemory): def-B handle() on HandlerMemoryRetrieval + repoint node_memory_retrieval_effect
  routing"
summary: >-
  OCC evidence companion for OMN-15227 (slice A2 of OMN-15027). Adds the canonical definition-B entrypoint
  handle(request) -> response to HandlerMemoryRetrieval and repoints all three dispatchable operations
  (search, search_text, search_graph) in src/omnimemory/nodes/node_memory_retrieval_effect/contract.yaml
  off HandlerQdrantMock / HandlerDbMock / HandlerGraphMock and onto HandlerMemoryRetrieval, which is what
  the runtime actually dispatches to.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "public_api"
evidence_requirements:
  - kind: "ci"
    description: "Product PR diff scope present on OmniNode-ai/omnimemory"
    command: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omn-15227-red-to-green"
    description: >-
      RED-to-GREEN on the real tree: the new contract-target and entrypoint tests fail 14/16 against unmodified
      dev (HandlerMemoryRetrieval has no handle; all three declared Mock targets fail both the dispatch-entrypoint
      and boot-resolvability predicates) and pass 16/16 after the change.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnimemory/contents/tests/nodes/node_memory_retrieval_effect/test_dispatch_entrypoint_contract.py?ref=f45a4f3ec7e464248bdc10d88fb14d781fc55a8f'
          --jq .content | base64 -d | grep -q 'def has_dispatch_entrypoint' && gh api 'repos/OmniNode-ai/omnimemory/contents/src/omnimemory/nodes/node_memory_retrieval_effect/handlers/handler_memory_retrieval.py?ref=f45a4f3ec7e464248bdc10d88fb14d781fc55a8f'
          --jq .content | base64 -d | grep -q 'async def handle'"
  - id: "dod-omn-15227-full-gates-200"
    description: >-
      Full local gates on .200 per rule 11a — ruff format --check, ruff check, mypy --strict, pyright,
      pre-commit run --all-files, full pytest suite.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 416 --repo OmniNode-ai/omnimemory --json files,state"
  - id: "dod-omn-15227-seam"
    description: >-
      Seam deliverable for the downstream omnimarket slice (A3) — all three operations route to name HandlerMemoryRetrieval,
      module omnimemory.nodes.node_memory_retrieval_effect.handlers.handler_memory_retrieval.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh api 'repos/OmniNode-ai/omnimemory/contents/src/omnimemory/nodes/node_memory_retrieval_effect/contract.yaml?ref=f45a4f3ec7e464248bdc10d88fb14d781fc55a8f'
          --jq .content | base64 -d | python3 -c \"import sys,yaml; d=yaml.safe_load(sys.stdin); t={(e['handler']['module'],e['handler']['name'])
          for e in d['handler_routing']['handlers'] if e['operation'] in {'search','search_text','search_graph'}};
          sys.exit(0 if t=={('omnimemory.nodes.node_memory_retrieval_effect.handlers.handler_memory_retrieval','HandlerMemoryRetrieval')}
          else 1)\""
  - id: "dod-OmniNode-ai-omnimemory-pr-416"
    description: "Product PR #416 on OmniNode-ai/omnimemory — OMN-15227 slice A2."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "occ-self-bind-pr-5088"
    description: "OCC companion PR #5088 — self-bind for OMN-15227 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
