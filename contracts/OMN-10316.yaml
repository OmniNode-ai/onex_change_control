---
schema_version: 1.0.0
ticket_id: OMN-10316
title: Wire validate-docs.yml reusable workflow as required CI gate across all 10 repos
summary: Add docs-validate.yml caller workflow to 9 repos (omnibase_compat, omnibase_infra, omnibase_spi,
  omnimarket, omniintelligence, omnimemory, onex_change_control, omniclaude, omnidash) calling OmniNode-ai/omnibase_core/.github/workflows/validate-docs.yml@main.
  Update .markdown-link-check.json to exclude omni_worktrees/** per OMN-9607 pattern.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: FULL
evidence_requirements:
  - kind: ci
    description: docs-validate caller workflow present in each repo
    command: ls .github/workflows/docs-validate.yml
  - kind: ci
    description: dod receipts present at canonical path
    command: ls drift/dod_receipts/OMN-10316/
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: pre-commit run --all-files passed in all 9 repo worktrees
    source: manual
    checks:
      - check_type: command
        check_value: pre-commit run --all-files
  - id: dod-002
    description: docs-validate / call CI check passed on omnibase_compat PR 79
    source: manual
    checks:
      - check_type: command
        check_value: gh pr checks 79 --repo OmniNode-ai/omnibase_compat | grep "call / validate-docs"
