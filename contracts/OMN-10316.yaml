---
schema_version: 1.0.0
ticket_id: OMN-10316
title: Wire validate-docs.yml reusable workflow as required CI gate across all 10 repos
summary: Add docs-validate.yml caller workflow to 9 repos (omnibase_compat, omnibase_infra, omnibase_spi,
  omnimarket, omniintelligence, omnimemory, onex_change_control, omniclaude, omnidash) calling OmniNode-ai/omnibase_core/.github/workflows/validate-docs.yml@main.
  Update .markdown-link-check.json to exclude omni_worktrees/** per OMN-9607 pattern.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: FULL
evidence_requirements:
  - kind: ci
    description: pre-commit run --all-files passes in all 9 repo worktrees
    command: pre-commit run --all-files
  - kind: ci
    description: docs-validate / call CI check passes on each PR
    command: gh pr checks <pr> --repo OmniNode-ai/<repo>
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: pre-commit run --all-files passes in all 9 repo worktrees
    source: manual
    checks:
      - check_type: command
        check_value: pre-commit run --all-files
  - id: dod-002
    description: docs-validate / call CI check passes on each PR
    source: manual
    checks:
      - check_type: command
        check_value: gh pr checks <pr> --repo OmniNode-ai/<repo> | grep "call / validate-docs"
