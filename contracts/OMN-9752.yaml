---
schema_version: "1.0.0"
ticket_id: "OMN-9752"
title: "Batch-validate node contract node_type values via model_validate"
summary: >
  Adds scripts/batch_validate_node_contracts.py and wires it into omnibase_infra validation so every nodes/*/contract.yaml
  file is batch-validated through ModelNodeTypeProbe.model_validate(). The PR also hardens validator runtime
  handling by rejecting non-directory CLI inputs and preserving import failures as runtime errors instead
  of contract validation failures.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-batch-validator-tests"
    description: "Focused batch validator unit tests pass on omnibase_infra PR #1533 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9752/dod-batch-validator-tests/command.yaml\""
  - id: "dod-batch-validator-lint"
    description: "Focused ruff check and format check pass for the batch validator script and tests."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9752/dod-batch-validator-lint/command.yaml\""
  - id: "dod-precommit"
    description: "Pre-commit hooks pass for the OMN-9752 fix commit."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9752/dod-precommit/command.yaml\""
  - id: "dod-pr-1533-head"
    description: "Bind OMN-9752 evidence to omnibase_infra PR #1533 head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-9752/dod-pr-1533-head/command.yaml\""
