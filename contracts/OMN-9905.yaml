---
schema_version: "1.0.0"
ticket_id: OMN-9905
title: "Build overlay loader and merge semantics for OmniNode plugin scope"
summary: "Bind omnibase_infra#1503 to central OCC evidence for the plugin scope overlay loader and merge
  semantics."
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - "runtime"
  - "contracts"
evidence_requirements:
  - kind: "ci"
    description: "Overlay loader implementation PR is open against omnibase_infra main"
    command: "gh pr view 1503 --repo OmniNode-ai/omnibase_infra"
  - kind: "manual"
    description: "Implementation PR documents overlay loader test and type-check evidence"
    command: "gh pr view 1503 --repo OmniNode-ai/omnibase_infra --json body"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Implementation PR is open for OMN-9905 against omnibase_infra main"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9905/dod-001/command.yaml && grep
          -q '^pr_number: 1503$' drift/dod_receipts/OMN-9905/dod-001/command.yaml"
  - id: "dod-002"
    description: "Implementation PR records overlay loader tests, mypy, and ruff evidence"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9905/dod-002/command.yaml && grep
          -q '18 unit tests' drift/dod_receipts/OMN-9905/dod-002/command.yaml && grep -q 'mypy src/omnibase_infra/scope/'
          drift/dod_receipts/OMN-9905/dod-002/command.yaml"
  - id: "dod-occ-validation"
    description: "OCC evidence contract validates and binds this evidence PR"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-9905/dod-occ-validation/command.yaml
          && grep -q '^pr_number: 718$' drift/dod_receipts/OMN-9905/dod-occ-validation/command.yaml"
