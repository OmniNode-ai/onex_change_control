---
schema_version: "1.0.0"
ticket_id: OMN-10460
title: "Cost Event Publication Daemon"
summary: "Implements a background daemon node that reads Kafka cost events and publishes them to downstream
  consumers, replacing the inline plugin_emit_daemon with a proper event-bus-driven omnimarket node."
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "topics"
evidence_requirements:
  - kind: tests
    description: "26 unit tests pass covering publisher, Kafka adapter, and serialization paths"
    command: "cd ${OMNI_HOME:-/Users/jonah/Code/omni_home}/omni_worktrees/OMN-10460/omnimarket && env
      -u PYTHONPATH uv run pytest tests/unit/nodes/node_cost_event_publisher -q"
  - kind: ci
    description: "Integration test against live Kafka passes (producer → consumer round-trip)"
    command: "cd ${OMNI_HOME:-/Users/jonah/Code/omni_home}/omni_worktrees/OMN-10460/omnimarket && env
      -u PYTHONPATH uv run pytest tests/integration/test_cost_event_kafka_roundtrip.py -q"
  - kind: ci
    description: "CodeRabbit review passed with no unresolved threads"
    command: "gh pr view 491 --repo OmniNode-ai/omnimarket --json reviews,reviewDecision"
  - kind: ci
    description: "Hostile reviewer passed — no blocking findings"
    command: "gh pr checks 491 --repo OmniNode-ai/omnimarket"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "26 unit tests pass covering publisher, Kafka adapter, and serialization paths"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10460/dod-001/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10460/dod-001/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10460/dod-001/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-002
    description: "Integration test against live Kafka passes (producer → consumer round-trip)"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10460/dod-002/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10460/dod-002/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10460/dod-002/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-003
    description: "CodeRabbit review passed with no unresolved threads"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10460/dod-003/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10460/dod-003/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10460/dod-003/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
  - id: dod-004
    description: "Hostile reviewer passed — no blocking findings"
    source: generated
    checks:
      - check_type: command
        check_value: "if test -f drift/dod_receipts/OMN-10460/dod-004/command.yaml; then grep -q '^status:
          PASS$' drift/dod_receipts/OMN-10460/dod-004/command.yaml; else gh api repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-10460/dod-004/command.yaml
          --jq .content | tr -d '\n' | base64 -d | grep -q '^status: PASS$'; fi"
