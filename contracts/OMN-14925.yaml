---
schema_version: "1.0.0"
ticket_id: "OMN-14925"
title: "Align omnibase_core pre-push naming hook with CI canonical validator"
summary: >
  omnibase_core PR #1480 repoints the local pre-push class naming hook to the same baseline-aware validator
  used by the required CI naming context, removing the pre-existing 29-violation local-only wedge without
  weakening the CI gate.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "manual"
    description: "omnibase_core PR #1480 is open at the recorded head and changes only .pre-commit-config.yaml."
    command: "gh pr view 1480 --repo OmniNode-ai/omnibase_core --json headRefOid,files,body"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnibase_core-pr-1480-hookfix-head"
    description: >
      Binds omnibase_core PR #1480 at head 2a9022c1575f68bc728e8c0366be0e57b7778c3b. The PR carries the
      OMN-14925 pre-push naming-hook alignment needed to unblock hooked pushes without bypassing pre-push.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14925/dod-omnibase_core-pr-1480-hookfix-head/command.yaml
          && grep -q '2a9022c1575f68bc728e8c0366be0e57b7778c3b' drift/dod_receipts/OMN-14925/dod-omnibase_core-pr-1480-hookfix-head/command.yaml
  - id: "dod-omnibase_core-pr-1480-product-diff-scope"
    description: >
      PR #1480 product diff scope is limited to .pre-commit-config.yaml and repoints validate-naming-conventions
      to scripts/validate_class_naming.py, matching the required CI naming-conventions gate baseline semantics.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-14925/dod-omnibase_core-pr-1480-product-diff-scope/command.yaml
          && grep -q '.pre-commit-config.yaml' drift/dod_receipts/OMN-14925/dod-omnibase_core-pr-1480-product-diff-scope/command.yaml
  - id: "occ-self-bind-pr-4597"
    description: >
      Self-binding OCC eligibility receipt for onex_change_control PR #4597, which carries OMN-14925 central
      evidence for omnibase_core PR #1480.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 4597 --repo OmniNode-ai/onex_change_control --json number,state,headRefName,headRefOid
