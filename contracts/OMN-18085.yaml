---
schema_version: "1.0.0"
ticket_id: "OMN-18085"
title: "omniweb and omnidash dev rollouts are pinned to maxSurge 0 so neither waits on CPU the staging
  node does not have"
summary: >
  OCC companion for OmniNode-ai/omninode_infra PR #1282. Bound by hand (union-resolve) because the autobind
  producer mints nothing today (OMN-18073), the same reason OCC#8760, OCC#8782, OCC#8792 and OCC#8798
  were hand-minted in this window. k8s/onex-dev/omniweb/deployment.yaml declares no strategy, so it inherits
  the Kubernetes default RollingUpdate maxSurge 25% / maxUnavailable 25%. At replicas 2 those percentages
  resolve to maxSurge 1 (fractions round UP) and maxUnavailable 0 (fractions round DOWN), so the Deployment
  is REQUIRED to create a third pod and FORBIDDEN from terminating either running pod first - it can only
  progress by finding 100m of unallocated CPU. The dev-system staging node has almost none. Measured read-only
  over SSM against EC2 i-06169517a92b45f86 at 2026-09-09T16:40Z, node ip-10-40-204-249 is a single 4-CPU
  k3s control-plane with 4000m allocatable, 3880m requested (97%), 120m free, and 2564m actually used
  (64% per kubectl top node) - Insufficient cpu is about REQUESTS against allocatable, never about usage,
  so at 64% use the node was not busy, it was fully committed. Live instance on the rollout this ticket
  was filed for (omniweb#366, merge 7b28a4d087ebd0a4076229697b653939c83fa75f): ReplicaSet omniweb-7dd966bbd9
  rev 168 was created at 16:15:18Z and its surge replica went Pending with "0/1 nodes are available: 1
  Insufficient cpu"; at about 16:33:15Z omnimarket-projection-api finished ITS own rollout and scaled
  ReplicaSet d4c5fcfc5 from 1 to 0, releasing exactly the 100m omniweb needed; omniweb reached Progressing/NewReplicaSetAvailable
  2/2 ready at 16:33:23Z. The rollout was unblocked by an unrelated Deployment, roughly 18 minutes of
  a staging deploy spent waiting on a coin flip, and deploy-onex-staging.yml rolls several 2-replica Deployments
  on the same push so the 120m headroom is a race rather than a guarantee. Stated plainly and recorded
  rather than implied - the rollout SELF-CLEARED at 16:33:23Z, before this change existed; both serving
  pods already carry sha256:1b98a56c64a39261fe0ea5e5e3683f209b172b44f79cbfd0799d502aab747cf5, whose ECR
  tag is sha-7b28a4d087ebd0a4076229697b653939c83fa75f, so this PR does not rescue that rollout, it removes
  the deadlock so the next one does not depend on a coin flip. Cause class is the surge strategy needing
  headroom the node does not reliably have, with the 97% request commitment as the standing condition;
  it is explicitly NOT leaked objects, since all 11 non-current omniweb ReplicaSets are scaled to 0 and
  every Completed Job pod contributes 0m - the positive control being that the per-namespace sum of Running
  and Pending pod requests is 3880m, exactly matching the node's own Allocated resources line. Trimming
  requests was measured and REJECTED: the two pods dominating actual CPU are UNDER-requested, not over
  (omninode-runtime requests 300m and uses 916m, 3.05x; omninode-runtime-effects requests 100m and uses
  692m, 6.92x), so cutting requests would degrade placement quality while leaving a strategy that is wrong
  on its own terms; the genuinely idle 100m requests (three qdrant StatefulSets, kreuzberg, omnidash)
  live in k8s/data-plane and the shared k8s/onex-dev base, outside this dev-only overlay. The fix is maxSurge
  0 / maxUnavailable 1 in the DEV OVERLAY only, which inverts the order so a replacement pod lands in
  the slot a terminating pod frees, needing zero additional headroom, with one of two replicas serving
  throughout. It is in k8s/onex-dev-overlay-dev and not the base because the base is applied to the production-facing
  cluster too (the onex-dev NAMESPACE owns the public app/api/dash hostnames), where maxSurge 0 would
  make every public omniweb deploy drop to a single replica on a cluster that has room to surge properly;
  this overlay is applied by one workflow only, deploy-onex-staging.yml against i-06169517a92b45f86, and
  deploy-onex-prod.yml applies k8s/onex-prod and never this directory. The change cannot revert omniweb's
  image - the digest pin in k8s/onex-dev/omniweb/kustomization.yaml is a floor, and the workflow step
  "Freeze omniweb on its live verified digest" re-pins the ephemeral checkout to the live digest before
  the apply, anchoring on the digest key, so this patch touching only spec.strategy is orthogonal. Lab-first
  is stated honestly rather than claimed: the k8s/onex-lab overlay DOES NOT carry omniweb - it bases on
  ../onex-dev/runtime and ../onex-dev/api only and names omniweb among the seven planes deliberately left
  out, with a matching entry in parity_exclusions.yaml - so no lab surface renders this Deployment and
  no lab pass covers this change. Read-only throughout: no kubectl set, patch, apply, scale or delete
  against any cluster, the public cluster i-0e596e8b557e27785 untouched, no instance resize, no credential
  rotated, re-issued, revoked or minted, and no secret value read, printed or decoded. AC1, AC2 and AC3
  are live-cluster readbacks that are not derivable from this diff; they are recorded on the Linear ticket
  after the staging deploy and are deliberately NOT claimed by any evidence item here. UNION-RESOLVE APPEND
  for OmniNode-ai/omninode_infra PR #1284, the omnidash half of the same ticket, hand-bound for the same
  OMN-18073 reason. One correction to the paragraph above is recorded rather than glossed: PR #1282 stated
  omnidash "has the identical 25%/25%-on-a-full-node shape", which is right about the effect and wrong
  about the source. omnidash does NOT inherit the Kubernetes default - k8s/onex-dev/omnidash/deployment.yaml
  declares maxSurge 1 / maxUnavailable 0 EXPLICITLY, read back live at 2026-09-09T18:07Z as {"rollingUpdate":{"maxSurge":1,"maxUnavailable":0},"type":"RollingUpdate"}
  at replicas 2. Different source, identical arithmetic, identical deadlock: the Deployment must create
  a third pod and may not terminate either running pod first, so it can only progress by finding 100m
  of unallocated CPU on a node with 120m free, and the omnidash pod's effective CPU request is max(init
  10m, sum(containers) 100m) = 100m, the same figure as one omniweb replica. Node arithmetic re-measured
  read-only over SSM against i-06169517a92b45f86 at 2026-09-09T18:07Z with --comment omnidash-rollout-strategy:
  4000m allocatable, 3880m requested (97%), 120m free, omnidash 2 pods Running since 2026-09-02T13:22Z,
  0 Pending cluster-wide. Stated plainly rather than implied: omnidash is NOT currently broken - this
  is a latent deadlock removed before it fires, not an incident repair. The fix is the same one, in the
  same dev overlay, for the same blast-radius reason, and it does not conflict with the omnidash PodDisruptionBudget
  (k8s/onex-dev/omnidash/pdb.yaml, minAvailable 1): a PDB constrains the Eviction API, while a Deployment
  rolling update scales the old ReplicaSet down directly rather than evicting, and maxUnavailable 1 on
  2 replicas leaves 1 pod serving, which is what the budget asks for anyway. Enumeration rather than a
  vague residual: omnidash and omniweb are the ONLY replicas-2 Deployments the k8s/onex-dev-overlay-dev
  render produces, and both are now covered; the four replicas-1 Deployments with the same surge-requiring
  shape (omnimarket-projection-api and umami explicitly, memgraph-lab and pgweb by inherited default)
  are deliberately left alone, because at one replica maxUnavailable 1 is a full outage of that workload
  for the rollout rather than a degraded-but- serving pair. AC6, AC7 and AC8 were added to the Linear
  ticket for the omnidash half before this contract bound to them; AC7 is a live-cluster readback that
  is not derivable from this diff and is deliberately NOT claimed by any evidence item here. UNION-RESOLVE
  APPEND for OmniNode-ai/omninode_infra PR #1285, the node-sizing half of the same ticket, hand-bound
  for the same OMN-18073 reason. #1282 and #1284 remove the surge REQUIREMENT from a rollout; #1285 removes
  the CONDITION that made it unsatisfiable. The dev-system staging instance i-06169517a92b45f86 was resized
  in place from t3a.xlarge (4 vCPU / 16 GiB) to t3a.2xlarge (8 vCPU / 32 GiB) on 2026-09-09 under the
  operator consent row at omni_home docs/tracking/ROLLING_WORK_LEDGER.md:5463, and #1285 changes only
  the declaration so terraform stops disagreeing with the live instance. AC2 is bound on LIVE READBACK,
  not on the diff: over SSM with --comment staging-resize-8cpu, allocatable CPU 4000m -> 8000m and memory
  16212040Ki -> 32572104Ki with requests UNCHANGED at 3880m, so utilisation moved 97% -> 48% and free
  headroom 120m -> 4120m against the 100m one omniweb replica requests. A correction is recorded rather
  than glossed: #1285's first commit claimed a terraform apply would trip aws_autoscaling_group.k3s_system's
  instance_refresh and REPLACE the node; the PR's own plan disproves it - it updates aws_launch_template.k3s_system
  in place, never lists the ASG, and reports 0 to destroy - because launch_template.version is the literal
  $Latest, so the ASG resource is unchanged and the refresh has no trigger. What survives: an apply only
  writes a new launch template version effective on the NEXT launch, so no apply could have resized the
  running instance; suspending the ASG's processes before the stop WAS mandatory, since min=max=desired=1
  with an EC2 health check would have read the stopped instance as unhealthy and replaced it; and the
  node is stateful, carrying the k3s control plane and local-path PersistentVolumes for postgres, valkey,
  qdrant and Keycloak, so any real replacement destroys data. Recorded but NOT fixed here: the launch
  template's $Latest still declares t3a.large, so a replacement today would still return the cluster at
  half size until aws/cluster-dev is applied. AC1, AC3, AC4, AC5, AC6, AC7 and AC8 are NOT claimed by
  any #1285 evidence item. UNION-RESOLVE APPEND for omninode_infra #1286: its brief's premise is REFUTED
  on readback - an unchanged apply is already a no-op. Requests 300m to 1000m and 100m to 800m; startup
  budget 1800s to 3600s. This field is at its cap; the account is in the #1286 evidence items.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1282 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1282 on OmniNode-ai/omninode_infra, derived from the PR's own diff: the overlay digest-pin gate that owns this patch file, manifest env hygiene over the rendered overlay, and the orphan-Deployment detector that reads the same overlay. Executed at cwd `${OMNI_HOME}/omninode_infra`.
    command: |-
      uv run pytest tests/k8s/test_omniweb_overlay_digest_pin.py tests/k8s/test_manifest_env_hygiene.py tests/k8s/test_omn_17693_orphan_deployment_detection.py -q
  - kind: "ci"
    description: "PR #1284 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1284 on OmniNode-ai/omninode_infra, the omnidash half, derived from the PR's own diff: manifest env hygiene and the orphan-Deployment detector over the rendered overlay, the two omnidash-owning suites, the onex-lab overlay parity gate, and the overlay regression guard. Executed at cwd `${OMNI_HOME}/omni_worktrees/OMN-18085/omninode_infra-omnidash`.
    command: |-
      uv run pytest tests/k8s/test_manifest_env_hygiene.py tests/k8s/test_omnidash_runtime_edge_image.py tests/k8s/test_omnidash_rds_tls.py tests/k8s/test_omn_17693_orphan_deployment_detection.py tests/k8s/test_onex_lab_overlay_omn17533.py tests/k8s/test_omn_16350_migration_gate_and_overlay_regression_guard.py tests/k8s/test_omn_17250_public_rollout_stale_condition.py tests/k8s/test_deploy_onex_staging_rollout_wait_writer_roster.py -q
  - kind: "ci"
    description: "PR #1285 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1285 on OmniNode-ai/omninode_infra, the node-sizing half, derived from the PR's own diff. The diff is one terraform variable declaration in aws/cluster-dev, so the proof that fits it is the terraform toolchain over that root rather than a pytest selection: formatting and configuration validity. Executed at cwd `${OMNI_HOME}/omni_worktrees/OMN-18085/omninode_infra-resize/aws/cluster-dev`. The stronger proof is the repository's own `plan (cluster-dev)` PR check, which renders the real plan against live state and passed on both commits of this PR.
    command: |-
      terraform fmt -check -recursive . && terraform init -backend=false -input=false && terraform validate
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1282"
    description: >
      PR #1282 on OmniNode-ai/omninode_infra - the dev-overlay strategy pin (maxSurge 0 / maxUnavailable
      1) on the omniweb Deployment, carrying the measured node arithmetic and the rejected request-trim
      alternative in the patch file's own rationale block. Bound by hand (union-resolve) because the autobind
      producer mints nothing today (OMN-18073).
    source: "generated"
    binds_ac:
      - "AC4"
      - "AC5"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1282 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1282/files --paginate --jq '.[].filename' | grep -cFx 'k8s/onex-dev-overlay-dev/patches/omniweb-deployment.yaml'
  - id: "dod-deploy-assessment"
    description: >
      Deploy-scope DoD so PR #1282 clears the deploy-gate. The diff touches one kustomize overlay patch
      that shapes a Deployment applied to the staging cluster. It mutates no cluster: the change is landed
      through the overlay only, per CLAUDE.md rule 13, and the entire diagnosis behind it was taken read-only
      over SSM with get / describe / top. The blast radius is provably the staging cluster, since only
      deploy-onex-staging.yml applies k8s/onex-dev-overlay-dev and deploy-onex-prod.yml applies k8s/onex-prod.
    source: "generated"
    binds_ac:
      - "AC5"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1282/files --paginate --jq '.[].filename' | grep -ciE '^k8s/'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #1282 on OmniNode-ai/omninode_infra - diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1282"
    binds_ac:
      - "AC4"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/k8s/test_omniweb_overlay_digest_pin.py tests/k8s/test_manifest_env_hygiene.py tests/k8s/test_omn_17693_orphan_deployment_detection.py -q
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1284"
    description: >
      PR #1284 on OmniNode-ai/omninode_infra - the omnidash half. Adds k8s/onex-dev-overlay-dev/patches/omnidash-deployment.yaml
      pinning the omnidash rollout to maxSurge 0 / maxUnavailable 1, plus the kustomization.yaml entry
      registering it. Carries the re-measured node arithmetic, the correction that omnidash declares maxSurge
      1 / maxUnavailable 0 explicitly rather than inheriting the 25%/25% default, the PodDisruptionBudget
      non-interaction, and the full enumeration of every Deployment the overlay renders with its disposition.
      Bound by hand (union-resolve) because the autobind producer mints nothing today (OMN-18073).
    source: "generated"
    binds_ac:
      - "AC6"
      - "AC8"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1284 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1284/files --paginate --jq '.[].filename' | grep -cFx 'k8s/onex-dev-overlay-dev/patches/omnidash-deployment.yaml'
  - id: "dod-deploy-assessment-pr-1284"
    description: >
      Deploy-scope DoD so PR #1284 clears the deploy-gate. The diff touches one new kustomize overlay
      patch and the kustomization entry that registers it, together shaping a Deployment applied to the
      staging cluster. It mutates no cluster: the change is landed through the overlay only, per CLAUDE.md
      rule 13, and the entire diagnosis behind it was taken read-only over SSM with get and describe.
      The blast radius is provably the staging cluster, since only deploy-onex-staging.yml applies k8s/onex-dev-overlay-dev
      and deploy-onex-prod.yml applies k8s/onex-prod.
    source: "generated"
    binds_ac:
      - "AC5"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1284/files --paginate --jq '.[].filename' | grep -ciE '^k8s/'
  - id: "dod-occ-diff-derived-behavior-proof-pr-1284"
    description: "PR #1284 on OmniNode-ai/omninode_infra - diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1284"
    binds_ac:
      - "AC6"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/k8s/test_manifest_env_hygiene.py tests/k8s/test_omnidash_runtime_edge_image.py tests/k8s/test_omnidash_rds_tls.py tests/k8s/test_omn_17693_orphan_deployment_detection.py tests/k8s/test_onex_lab_overlay_omn17533.py tests/k8s/test_omn_16350_migration_gate_and_overlay_regression_guard.py tests/k8s/test_omn_17250_public_rollout_stale_condition.py tests/k8s/test_deploy_onex_staging_rollout_wait_writer_roster.py -q
        cwd: "${OMNI_HOME}/omni_worktrees/OMN-18085/omninode_infra-omnidash"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1285"
    description: >
      PR #1285 on OmniNode-ai/omninode_infra - the node-sizing half. Changes one declaration, aws/cluster-dev/variables.tf
      instance_type_system, from t3a.large to t3a.2xlarge so the terraform root stops disagreeing with
      the live dev-system staging instance i-06169517a92b45f86, which was resized in place from t3a.xlarge
      to t3a.2xlarge on 2026-09-09 under the operator consent row at omni_home docs/tracking/ROLLING_WORK_LEDGER.md:5463.
      Binds AC2 on the live readback, not on the diff: allocatable CPU 4000m -> 8000m with requests unchanged
      at 3880m, so free request headroom moved 120m -> 4120m against the 100m one omniweb replica requests.
      The PR runs no terraform apply.
    source: "generated"
    binds_ac:
      - "AC2"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1285 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1285/files --paginate --jq '.[].filename' | grep -cFx 'aws/cluster-dev/variables.tf'
  - id: "dod-deploy-assessment-pr-1285"
    description: >
      Deploy-scope DoD so PR #1285 clears the deploy-gate. The diff touches exactly one file, aws/cluster-dev/variables.tf,
      and no kubernetes manifest, no overlay and no workload. It mutates nothing by merging: a terraform
      declaration takes effect only when someone applies the root, and this PR runs no apply. The live
      instance was already resized out of band under operator consent before this PR opened, so merging
      changes no running system. Stated rather than implied, because it is the residual: applying aws/cluster-dev
      is NOT a no-op - its plan reports 3 to add and 11 to change across MSK, RDS, SNS and SSM - so the
      apply remains a separate decision that this PR neither takes nor authorises.
    source: "generated"
    binds_ac: []
    checks:
      - check_type: "command"
        check_value: |-
          body="$(gh api repos/OmniNode-ai/omninode_infra/pulls/1285/files --paginate --jq '.[].filename')" \
            && printf '%s\n' "$body" | grep -qxF 'aws/cluster-dev/variables.tf' \
            && ! printf '%s\n' "$body" | grep -qE '^k8s/'
  - id: "dod-occ-diff-derived-behavior-proof-pr-1285"
    description: "PR #1285 on OmniNode-ai/omninode_infra - diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1285"
    binds_ac: []
    checks:
      - check_type: "test_passes"
        check_value: |-
          terraform fmt -check -recursive . && terraform init -backend=false -input=false && terraform validate
        cwd: "${OMNI_HOME}/omni_worktrees/OMN-18085/omninode_infra-resize/aws/cluster-dev"
  - id: "occ-self-bind-pr-8827"
    description: >-
      OCC self-binding receipt for PR #8827, the in-repo evidence PR carrying the union-resolve APPEND
      that binds OMN-18085 to omninode_infra#1285. Binds by pr_number so eligibility can resolve a PASS
      receipt for the evidence PR itself. Hand-minted: the autobind producer mints nothing today (OMN-18073).
      It declares no binds_ac because a pull-request-visibility probe proves no acceptance criterion.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8827 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "occ-self-bind-pr-8811"
    description: >-
      OCC self-binding receipt for PR #8811, the in-repo evidence PR carrying this contract. Binds OMN-18085
      to the OCC companion by pr_number so eligibility can resolve a PASS receipt for the evidence PR
      itself. Hand-minted: the autobind producer mints nothing today (OMN-18073). It declares no binds_ac
      because a pull-request-visibility probe proves no acceptance criterion.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8811 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "occ-self-bind-pr-8816"
    description: >-
      OCC self-binding receipt for PR #8816, the in-repo evidence PR carrying the union-resolve APPEND
      that binds OMN-18085 to omninode_infra#1284. Binds by pr_number so eligibility can resolve a PASS
      receipt for the evidence PR itself. Hand-minted: the autobind producer mints nothing today (OMN-18073).
      It declares no binds_ac because a pull-request-visibility probe proves no acceptance criterion.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8816 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "dod-OmniNode-ai-omninode_infra-pr-1286"
    description: >
      PR #1286 on OmniNode-ai/omninode_infra - the runtime-sizing half. Raises requests.cpu to 1000m on
      omninode-runtime and 800m on omninode-runtime-effects in the dev overlay, widens the startup budget
      1800s -> 3600s on both while holding the OMN-17372 periodSeconds x failureThreshold == progressDeadlineSeconds
      invariant, and lifts readiness failureThreshold 3 -> 6 to match the worker profile. Carries the
      steady-state measurement table, the enumerated 400m surge reserve (one surge pod each for the only
      five Deployments in onex-dev that can surge at all, every strategy read live), and the refusal of
      its own brief's premise. Bound by hand (union-resolve) because the autobind producer mints nothing
      today (OMN-18073).
    source: "generated"
    binds_ac:
      - "AC10"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1286 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1286/files --paginate --jq '.[].filename' | grep -cFx 'k8s/onex-dev-overlay-dev/patches/omninode-runtime-startup-readiness.yaml'
  - id: "dod-deploy-assessment-pr-1286"
    description: >
      Deploy-scope DoD so PR #1286 clears the deploy-gate. The diff touches two kustomize overlay patches
      that shape Deployments applied to the staging cluster, plus the guard test asserting the rendered
      result. It mutates no cluster: the change is landed through the overlay only, per CLAUDE.md rule
      13, and the diagnosis behind it was taken read-only over SSM with get, describe and top. Blast radius
      is provably the staging cluster, since only deploy-onex-staging.yml applies k8s/onex-dev-overlay-dev
      and deploy-onex-prod.yml applies k8s/onex-prod, which this diff does not touch.
    source: "generated"
    binds_ac:
      - "AC5"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1286/files --paginate --jq '.[].filename' | grep -ciE '^k8s/'
  - id: "dod-occ-diff-derived-behavior-proof-pr-1286"
    description: >
      PR #1286 on OmniNode-ai/omninode_infra - diff-derived behavior proof (OMN-16434). The behavior this
      diff changes is what the overlay RENDERS, so the proof is the re-baselined OMN-17372 gate, which
      asserts the RENDERED overlay rather than the patch files, plus the adjacent suites reading the same
      render. Two assertions in it are new and RED on the parent: the effects CPU request (parent 100m),
      and that the startup budget exceeds the 1919s boot that completed on 2026-09-09 (parent budget 1800s).
      The second exists because the pre-existing budget test only checks that the startup and rollout
      budgets AGREE with each other and cannot notice that both are too small. What this proves and what
      it does not is stated rather than implied: it proves the rendered requests and probe budgets are
      the intended values and that the request sum fits the node budget RECORDED in the gate; it does
      not prove that budget was read live, which is AC12's live half and is recorded on the Linear ticket.
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1286"
    binds_ac:
      - "AC10"
      - "AC11"
      - "AC12"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/k8s/test_runtime_startup_probe_and_cpu_omn17372.py tests/k8s/test_runtime_readiness_gate.py tests/k8s/test_manifest_env_hygiene.py tests/k8s/test_gateway_attach_runtime_config.py tests/k8s/test_omn18067_runtime_plane_store_sourced_bindings.py tests/k8s/test_omn_17693_orphan_deployment_detection.py tests/k8s/test_onex_lab_overlay_omn17533.py -q
        cwd: "${OMNI_HOME}/omni_worktrees/OMN-18085/omninode_infra-apply-restart"
  - id: "occ-self-bind-pr-8826"
    description: >-
      OCC self-binding receipt for PR #8826, the in-repo evidence PR carrying the union-resolve APPEND
      that binds OMN-18085 to omninode_infra#1286. Binds by pr_number so eligibility can resolve a PASS
      receipt for the evidence PR itself. Hand-minted: the autobind producer mints nothing today (OMN-18073).
      It declares no binds_ac because a pull-request-visibility probe proves no acceptance criterion.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh pr view 8826 --repo OmniNode-ai/onex_change_control --json number,state,headRefName
  - id: "dod-OmniNode-ai-omninode_infra-pr-1286-ci"
    description: "PR #1286 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1286 --repo OmniNode-ai/omninode_infra --json files"
  - id: "occ-self-bind-pr-8837"
    description: "OCC companion PR #8837 — self-bind for OMN-18085 (OMN-14650)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8837 --repo OmniNode-ai/onex_change_control --json number,state"
