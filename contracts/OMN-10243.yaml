---
schema_version: "1.0.0"
ticket_id: OMN-10243
title: "feat(omnibase_core): create core/enums/governance/ namespace + migrate 27 governance enums"
summary: "feat(omnibase_core): create core/enums/governance/ namespace + migrate 27 governance enums (Task
  3 of OMN-9582 Wave 2)"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: 26 TDD import tests all pass green
    command: uv run pytest tests/unit/enums/governance/ -v
  - kind: tests
    description: mypy strict zero errors on governance namespace
    command: uv run mypy src/omnibase_core/enums/governance/ --strict
  - kind: tests
    description: pre-commit all hooks pass
    command: pre-commit run --all-files
  - kind: ci
    description: CI pipeline green on PR 961
    command: gh pr checks 961 --repo OmniNode-ai/omnibase_core
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: 26 TDD import tests pass green — all 27 governance enum classes importable from omnibase_core.enums.governance
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 961 --repo OmniNode-ai/omnibase_core --json state,baseRefName --jq 'select(.state=="OPEN"
          and .baseRefName=="main") | "ok"'
  - id: dod-002
    description: mypy --strict clean on 26 source files in governance namespace
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 961 --repo OmniNode-ai/omnibase_core --json headRefOid --jq '.headRefOid'
  - id: dod-003
    description: All pre-commit hooks pass including mypy, pyright, naming conventions, SPDX headers
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 961 --repo OmniNode-ai/omnibase_core --json headRefOid --jq '.headRefOid'
