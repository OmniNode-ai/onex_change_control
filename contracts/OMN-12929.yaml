---
schema_version: "1.0.0"
ticket_id: "OMN-12929"
title: "OCC evidence for omnimemory Redis 8 dependency update"
summary: >
  Central OCC evidence for omnimemory PR #361. The product change replaces the stale Dependabot Redis
  8 PR with a ticket-bound branch, updates the direct redis dependency to >=8,<9, aligns omnibase-infra
  to 0.38.3 so the resolver accepts Redis 8, and refreshes uv.lock with the resulting minimal dependency
  metadata changes.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-product-pr-361"
    description: "omnimemory PR #361 carries the OMN-12929 Redis 8 replacement branch and targets dev."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12929/dod-product-pr-361/command.yaml
          && grep -q '^pr_number: 361$' drift/dod_receipts/OMN-12929/dod-product-pr-361/command.yaml"
  - id: "dod-dependency-resolution"
    description: "Frozen dependency resolution succeeds with Redis 8 and omnibase-infra 0.38.3."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12929/dod-dependency-resolution/command.yaml
          && grep -q 'redis 8.0.0' drift/dod_receipts/OMN-12929/dod-dependency-resolution/command.yaml"
  - id: "dod-static-validation"
    description: "Ruff and pyright validation passed on the product PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12929/dod-static-validation/command.yaml
          && grep -q '0 errors' drift/dod_receipts/OMN-12929/dod-static-validation/command.yaml"
  - id: "dod-focused-tests"
    description: "Focused adapter and health-manager tests passed with Redis 8 installed."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12929/dod-focused-tests/command.yaml
          && grep -q '124 passed' drift/dod_receipts/OMN-12929/dod-focused-tests/command.yaml"
  - id: "dod-occ-pr"
    description: "OCC PR carries the OMN-12929 evidence-ticket contract and self-binding central receipt."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12929/dod-occ-pr/command.yaml &&
          grep -q '^pr_number: 2441$' drift/dod_receipts/OMN-12929/dod-occ-pr/command.yaml"
