---
schema_version: "1.0.0"
ticket_id: "OMN-10753"
title: "Wire CLAUDE_CODE_SESSION_ID into emit daemon as canonical session ID"
summary: >
  Replace fragmented SESSION_ID / CLAUDE_SESSION_ID / ONEX_SESSION_ID env var reads in omniclaude hook
  scripts with CLAUDE_CODE_SESSION_ID (Claude Code's native UUID v4 env var). Inject session_id into each
  event payload at the emit_client_wrapper call site; emit daemon preserves the payload value and falls
  back to env only when absent.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnimarket-tests"
    description: "Emit daemon session_id injection unit tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10753/dod-omnimarket-tests/command.yaml\""
  - id: "dod-omniclaude-tests"
    description: "Emit client session_id injection unit tests pass."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10753/dod-omniclaude-tests/command.yaml\""
  - id: "dod-ruff-check"
    description: "Changed files pass ruff check."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10753/dod-ruff-check/command.yaml\""
  - id: "dod-env-var-registry"
    description: "CLAUDE_CODE_SESSION_ID declared in omniclaude env var registry allowlist."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10753/dod-env-var-registry/command.yaml\""
  - id: "dod-occ-pr"
    description: "Bind this central OCC evidence update to its onex_change_control PR."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-10753/dod-occ-pr/command.yaml\""
