---
schema_version: "1.0.0"
ticket_id: "OMN-11579"
title: "Delete duplicate src/ pricing contract test, backfill missing tests"
summary: >-
  Bind omnibase_compat PR #102 evidence for deleting the duplicate src/omnibase_compat/tests pricing contract
  test and backfilling the two tests that were unique to the deleted copy.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "Pricing contract tests pass after duplicate deletion and backfill in omnibase_compat."
    command: "uv run pytest tests/ -q"
dod_evidence:
  - id: "dod-compat-pr-102"
    description: "omnibase_compat PR #102 carries the duplicate deletion and test backfill at the observed
      head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11579/dod-compat-pr-102/command.yaml"
          && grep -q '^pr_number: 102$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11579/dod-compat-pr-102/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-11579 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11579/dod-occ-pr-self/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
