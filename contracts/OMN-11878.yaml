---
schema_version: "1.0.0"
ticket_id: OMN-11878
title: "Classify imperative guard baseline debt and bind every allowlisted violation to a real Linear
  ticket"
summary: >-
  Replace every '# migration pending' placeholder ticket in allowlists/*.yaml (342 entries across omnibase_infra,
  omniclaude, omniintelligence, omnimarket) with the real per-repo remediation-epic OMN-#### ticket. Add
  ModelAllowlistedHandler (new interface-surface model, ticket field pattern ^OMN-\d+$) and validate_allowlist_tickets(),
  wired into the existing check-imperative-contracts CLI (already a required CI gate + pre-commit hook
  across all consuming repos) so a future allowlist entry with a placeholder or malformed ticket fails
  closed.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "PR opened against dev branch"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --json state -q .state --repo ${REPO}"
  - id: "dod-002"
    description: "No '# migration pending' placeholder remains in any allowlists/*.yaml"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "! grep -rn 'migration pending' allowlists/"
  - id: "dod-003"
    description: "New ticket-format model landed in the product"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "test -f src/onex_change_control/models/model_allowlisted_handler.py"
  - id: "dod-004"
    description: "Imperative contract guard self-check runs green on the baselined repo"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run check-imperative-contracts --repo-root . --allowlists-dir allowlists --scan-freestanding"
  - id: "dod-005"
    description: "Ticket contract artifact validates against ModelTicketContract"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "uv run validate-yaml contracts/OMN-11878.yaml"
