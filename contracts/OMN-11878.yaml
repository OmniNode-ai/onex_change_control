---
schema_version: "1.0.0"
ticket_id: OMN-11878
title: "Classify imperative guard baseline debt and bind every allowlisted violation to a real Linear
  ticket"
summary: >-
  Replace every '# migration pending' placeholder ticket in allowlists/*.yaml with the real per-repo remediation
  epic ticket, add a fail-closed ModelAllowlistedHandler ticket-format validator, and wire that validator
  into the existing imperative-contract guard so future baseline debt cannot be recorded without a real
  OMN ticket.
is_seam_ticket: true
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "CI passes on merge PR"
    command: "gh pr checks"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: >-
      Product PR #4163 is open against dev at the reviewed OMN-11878 head and carries the expected allowlist,
      validator, contract, and test files.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=4163; PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=99d0d971b3add6c831afd930fe114d5f0949e13b;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json state,baseRefName,headRefOid,files,title
          --jq '.state == "OPEN" and .baseRefName == "dev" and .headRefOid == "'$PRODUCT_HEAD'" and (.title
          | contains("OMN-11878")) and ((.files | map(.path)) | index("contracts/OMN-11878.yaml") != null)
          and ((.files | map(.path)) | index("src/onex_change_control/models/model_allowlisted_handler.py")
          != null) and ((.files | map(.path)) | index("src/onex_change_control/scripts/check_imperative_contracts.py")
          != null)' | grep -qx true
  - id: "dod-002"
    description: >-
      The reviewed #4163 head removes all '# migration pending' placeholders from the four allowlist files
      changed by OMN-11878.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=99d0d971b3add6c831afd930fe114d5f0949e13b;
          d="$(mktemp -d)" && for p in allowlists/omnibase_infra.yaml allowlists/omniclaude.yaml allowlists/omniintelligence.yaml
          allowlists/omnimarket.yaml; do mkdir -p "$d/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "$d/$p"; done && ! grep -R "migration pending" "$d"
  - id: "dod-003"
    description: >-
      The reviewed #4163 head contains the fail-closed ModelAllowlistedHandler contract model with an
      explicit OMN-ticket pattern and extra-forbid model configuration.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=99d0d971b3add6c831afd930fe114d5f0949e13b;
          gh api "repos/${PRODUCT_REPO}/contents/src/onex_change_control/models/model_allowlisted_handler.py?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode | grep -Fq 'class ModelAllowlistedHandler(BaseModel)' && gh
          api "repos/${PRODUCT_REPO}/contents/src/onex_change_control/models/model_allowlisted_handler.py?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode | grep -Fq 'extra="forbid"' && gh api "repos/${PRODUCT_REPO}/contents/src/onex_change_control/models/model_allowlisted_handler.py?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode | grep -Fq 'pattern=r"^OMN-\d+$"'
  - id: "dod-004"
    description: >-
      The exact imperative-contract guard self-check runs green on a detached temporary worktree of the
      reviewed #4163 head.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_BRANCH=jonah/omn-11878-classify-imperative-guard-baseline-debt-and-bind-every; PRODUCT_HEAD=99d0d971b3add6c831afd930fe114d5f0949e13b;
          d="$(mktemp -d)" && gh repo clone OmniNode-ai/onex_change_control "$d/wt" -- --depth 1 --branch
          "$PRODUCT_BRANCH" && test "$(git -C "$d/wt" rev-parse HEAD)" = "$PRODUCT_HEAD" && cd "$d/wt"
          && uv run check-imperative-contracts --repo-root . --allowlists-dir allowlists --scan-freestanding
  - id: "dod-005"
    description: >-
      The OMN-11878 ticket contract artifact on the reviewed #4163 head validates against ModelTicketContract.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_BRANCH=jonah/omn-11878-classify-imperative-guard-baseline-debt-and-bind-every; PRODUCT_HEAD=99d0d971b3add6c831afd930fe114d5f0949e13b;
          d="$(mktemp -d)" && gh repo clone OmniNode-ai/onex_change_control "$d/wt" -- --depth 1 --branch
          "$PRODUCT_BRANCH" && test "$(git -C "$d/wt" rev-parse HEAD)" = "$PRODUCT_HEAD" && cd "$d/wt"
          && uv run validate-yaml contracts/OMN-11878.yaml
  - id: "dod-occ-self-bind-pr-4175"
    description: >-
      OCC companion self-binding for onex_change_control PR #4175. Verifies the PR is open on the expected
      OMN-11878 evidence branch and contains the initial evidence commit c89dcebf4711eef031da22f83baf928684d9946e.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/4175 --jq '.head.ref == "jonah/omn-11878-occ-4163-evidence"
          and .state == "open"' | grep -qx true && gh api repos/OmniNode-ai/onex_change_control/pulls/4175/commits
          --paginate --jq '.[].sha' | grep -Fx c89dcebf4711eef031da22f83baf928684d9946e
  - id: "dod-001-head-efcf732f"
    evidence_artifact: "supersedes_dod_evidence:dod-001"
    description: >-
      Current-head product PR proof after #4163 merged dev and resolved contracts/OMN-11878.yaml to the
      central OCC#4175 evidence-bearing contract.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=4163; PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=efcf732f7e3d61e9cfee1ec8dadda84d9ca4f941;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json state,baseRefName,headRefOid,files,title
          --jq '.state == "OPEN" and .baseRefName == "dev" and .headRefOid == "'$PRODUCT_HEAD'" and (.title
          | contains("OMN-11878")) and ((.files | map(.path)) | index("src/onex_change_control/models/model_allowlisted_handler.py")
          != null) and ((.files | map(.path)) | index("src/onex_change_control/scripts/check_imperative_contracts.py")
          != null)' | grep -qx true
  - id: "dod-002-head-efcf732f"
    evidence_artifact: "supersedes_dod_evidence:dod-002"
    description: >-
      Current-head allowlist proof after #4163 advanced to efcf732f7e3d61e9cfee1ec8dadda84d9ca4f941. The
      four changed allowlist files still contain no legacy placeholder ticket text.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=efcf732f7e3d61e9cfee1ec8dadda84d9ca4f941;
          d="$(mktemp -d)" && for p in allowlists/omnibase_infra.yaml allowlists/omniclaude.yaml allowlists/omniintelligence.yaml
          allowlists/omnimarket.yaml; do mkdir -p "$d/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "$d/$p"; done && ! grep -R "migration pending" "$d"
  - id: "dod-003-head-efcf732f"
    evidence_artifact: "supersedes_dod_evidence:dod-003"
    description: >-
      Current-head model proof after #4163 advanced to efcf732f7e3d61e9cfee1ec8dadda84d9ca4f941. The fail-closed
      allowlisted-handler model remains present with extra-forbid config and an OMN ticket pattern.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=efcf732f7e3d61e9cfee1ec8dadda84d9ca4f941;
          gh api "repos/${PRODUCT_REPO}/contents/src/onex_change_control/models/model_allowlisted_handler.py?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode | grep -Fq 'class ModelAllowlistedHandler(BaseModel)' && gh
          api "repos/${PRODUCT_REPO}/contents/src/onex_change_control/models/model_allowlisted_handler.py?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode | grep -Fq 'extra="forbid"' && gh api "repos/${PRODUCT_REPO}/contents/src/onex_change_control/models/model_allowlisted_handler.py?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode | grep -Fq 'pattern=r"^OMN-\d+$"'
  - id: "dod-004-head-efcf732f"
    evidence_artifact: "supersedes_dod_evidence:dod-004"
    description: >-
      Current-head imperative-contract guard self-check after #4163 advanced to efcf732f7e3d61e9cfee1ec8dadda84d9ca4f941.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_BRANCH=jonah/omn-11878-classify-imperative-guard-baseline-debt-and-bind-every; PRODUCT_HEAD=efcf732f7e3d61e9cfee1ec8dadda84d9ca4f941;
          d="$(mktemp -d)" && gh repo clone OmniNode-ai/onex_change_control "$d/wt" -- --depth 1 --branch
          "$PRODUCT_BRANCH" && test "$(git -C "$d/wt" rev-parse HEAD)" = "$PRODUCT_HEAD" && cd "$d/wt"
          && uv run check-imperative-contracts --repo-root . --allowlists-dir allowlists --scan-freestanding
  - id: "dod-005-head-efcf732f"
    evidence_artifact: "supersedes_dod_evidence:dod-005"
    description: >-
      Current-head ticket-contract validation after #4163 advanced to efcf732f7e3d61e9cfee1ec8dadda84d9ca4f941.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_BRANCH=jonah/omn-11878-classify-imperative-guard-baseline-debt-and-bind-every; PRODUCT_HEAD=efcf732f7e3d61e9cfee1ec8dadda84d9ca4f941;
          d="$(mktemp -d)" && gh repo clone OmniNode-ai/onex_change_control "$d/wt" -- --depth 1 --branch
          "$PRODUCT_BRANCH" && test "$(git -C "$d/wt" rev-parse HEAD)" = "$PRODUCT_HEAD" && cd "$d/wt"
          && uv run validate-yaml contracts/OMN-11878.yaml
  - id: "dod-occ-self-bind-pr-4177"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-self-bind-pr-4175"
    description: >-
      OCC companion self-binding for onex_change_control PR #4177. Verifies the PR is open on the expected
      OMN-11878 current-head refresh branch and contains the initial refresh commit 8e8b944be2d54c4ea9039ac97bb164d02e7721a9.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/4177 --jq '.head.ref == "jonah/omn-11878-occ-4163-head-efcf"
          and .state == "open"' | grep -qx true && gh api repos/OmniNode-ai/onex_change_control/pulls/4177/commits
          --paginate --jq '.[].sha' | grep -Fx 8e8b944be2d54c4ea9039ac97bb164d02e7721a9
  - id: "dod-occ-merged-pr-4177"
    evidence_artifact: "supersedes_dod_evidence:dod-occ-self-bind-pr-4177"
    description: >-
      OCC companion merged-state proof for onex_change_control PR #4177 after it landed as the current-head
      evidence source for product PR #4163.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/4177 --jq '.head.ref == "jonah/omn-11878-occ-4163-head-efcf"
          and .head.sha == "d25fe5624012e7b66ce995ab31563ed01c06558b" and .state == "closed" and .merged_at
          == "2026-07-15T01:55:50Z" and .merge_commit_sha == "d4d64640f3904e16c81707699aba70e8704f0579"'
          | grep -qx true
  - id: "dod-occ-self-bind-pr-4180"
    description: >-
      OCC companion self-binding for onex_change_control PR #4180. Verifies the PR is on the expected
      OMN-11878 merged-proof branch, remains bound after merge, and contains the initial merged-proof
      commit d0ee33d3933ff1e35a38e81b0a8fffe785937de5.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/4180 --jq '.head.ref == "jonah/omn-11878-occ-4163-selfbind-merged"
          and ((.state == "open") or (.state == "closed" and .merged_at != null))' | grep -qx true &&
          gh api repos/OmniNode-ai/onex_change_control/pulls/4180/commits --paginate --jq '.[].sha' |
          grep -Fx d0ee33d3933ff1e35a38e81b0a8fffe785937de5
  - id: "dod-001-head-6c829d83"
    evidence_artifact: "supersedes_dod_evidence:dod-001-head-efcf732f"
    description: >-
      Current-head product PR proof after #4163 refreshed onto dev containing OCC#4180 evidence.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=4163; PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=6c829d83c82afd6c2e5ec9b50aa060ac5ef6935c;
          gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json state,baseRefName,headRefOid,files,title
          --jq '.state == "OPEN" and .baseRefName == "dev" and .headRefOid == "'$PRODUCT_HEAD'" and (.title
          | contains("OMN-11878")) and ((.files | map(.path)) | index("src/onex_change_control/models/model_allowlisted_handler.py")
          != null) and ((.files | map(.path)) | index("src/onex_change_control/scripts/check_imperative_contracts.py")
          != null)' | grep -qx true
  - id: "dod-002-head-6c829d83"
    evidence_artifact: "supersedes_dod_evidence:dod-002-head-efcf732f"
    description: >-
      Current-head allowlist proof after #4163 advanced to 6c829d83c82afd6c2e5ec9b50aa060ac5ef6935c.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=6c829d83c82afd6c2e5ec9b50aa060ac5ef6935c;
          d="$(mktemp -d)" && for p in allowlists/omnibase_infra.yaml allowlists/omniclaude.yaml allowlists/omniintelligence.yaml
          allowlists/omnimarket.yaml; do mkdir -p "$d/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode > "$d/$p"; done && ! grep -R "migration pending" "$d"
  - id: "dod-003-head-6c829d83"
    evidence_artifact: "supersedes_dod_evidence:dod-003-head-efcf732f"
    description: >-
      Current-head model proof after #4163 advanced to 6c829d83c82afd6c2e5ec9b50aa060ac5ef6935c.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD=6c829d83c82afd6c2e5ec9b50aa060ac5ef6935c;
          gh api "repos/${PRODUCT_REPO}/contents/src/onex_change_control/models/model_allowlisted_handler.py?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode | grep -Fq 'class ModelAllowlistedHandler(BaseModel)' && gh
          api "repos/${PRODUCT_REPO}/contents/src/onex_change_control/models/model_allowlisted_handler.py?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode | grep -Fq 'extra="forbid"' && gh api "repos/${PRODUCT_REPO}/contents/src/onex_change_control/models/model_allowlisted_handler.py?ref=${PRODUCT_HEAD}"
          --jq .content | base64 --decode | grep -Fq 'pattern=r"^OMN-\d+$"'
  - id: "dod-004-head-6c829d83"
    evidence_artifact: "supersedes_dod_evidence:dod-004-head-efcf732f"
    description: >-
      Current-head imperative-contract guard self-check after #4163 advanced to 6c829d83c82afd6c2e5ec9b50aa060ac5ef6935c.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_BRANCH=jonah/omn-11878-classify-imperative-guard-baseline-debt-and-bind-every; PRODUCT_HEAD=6c829d83c82afd6c2e5ec9b50aa060ac5ef6935c;
          d="$(mktemp -d)" && gh repo clone OmniNode-ai/onex_change_control "$d/wt" -- --depth 1 --branch
          "$PRODUCT_BRANCH" && test "$(git -C "$d/wt" rev-parse HEAD)" = "$PRODUCT_HEAD" && cd "$d/wt"
          && uv run check-imperative-contracts --repo-root . --allowlists-dir allowlists --scan-freestanding
  - id: "dod-005-head-6c829d83"
    evidence_artifact: "supersedes_dod_evidence:dod-005-head-efcf732f"
    description: >-
      Current-head ticket-contract validation after #4163 advanced to 6c829d83c82afd6c2e5ec9b50aa060ac5ef6935c.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_BRANCH=jonah/omn-11878-classify-imperative-guard-baseline-debt-and-bind-every; PRODUCT_HEAD=6c829d83c82afd6c2e5ec9b50aa060ac5ef6935c;
          d="$(mktemp -d)" && gh repo clone OmniNode-ai/onex_change_control "$d/wt" -- --depth 1 --branch
          "$PRODUCT_BRANCH" && test "$(git -C "$d/wt" rev-parse HEAD)" = "$PRODUCT_HEAD" && cd "$d/wt"
          && uv run validate-yaml contracts/OMN-11878.yaml
  - id: "dod-occ-self-bind-pr-4182"
    description: >-
      OCC companion self-binding for onex_change_control PR #4182. Verifies the PR is on the expected
      OMN-11878 6c829d83 evidence branch, remains bound after merge, and contains the initial evidence
      commit c54ba64d0692994e3cda1c4a31db87bd1532285c.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          gh api repos/OmniNode-ai/onex_change_control/pulls/4182 --jq '.head.ref == "jonah/omn-11878-occ-4163-head-6c829d83"
          and ((.state == "open") or (.state == "closed" and .merged_at != null))' | grep -qx true &&
          gh api repos/OmniNode-ai/onex_change_control/pulls/4182/commits --paginate --jq '.[].sha' |
          grep -Fx c54ba64d0692994e3cda1c4a31db87bd1532285c
  - id: "dod-001-head-current-pr"
    evidence_artifact: "supersedes_dod_evidence:dod-001-head-6c829d83"
    description: >-
      Current product PR proof after #4163 merged dev evidence into its own branch. The check derives
      the live PR head before validation so this self-referential OCC PR can append evidence without invalidating
      the proof with a new evidence-only commit SHA.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=4163; PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_HEAD="$(gh pr
          view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid --jq .headRefOid)"; gh pr
          view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json state,baseRefName,headRefOid,files,title
          --jq '.state == "OPEN" and .baseRefName == "dev" and .headRefOid == "'$PRODUCT_HEAD'" and (.title
          | contains("OMN-11878")) and ((.files | map(.path)) | index("src/onex_change_control/models/model_allowlisted_handler.py")
          != null) and ((.files | map(.path)) | index("src/onex_change_control/scripts/check_imperative_contracts.py")
          != null)' | grep -qx true
  - id: "dod-004-head-current-pr"
    evidence_artifact: "supersedes_dod_evidence:dod-004-head-6c829d83"
    description: >-
      Current imperative-contract guard proof after #4163 merged dev evidence into its own branch. The
      command derives the live PR head before cloning so the self-referential evidence commit does not
      stale the proof it adds.
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          PRODUCT_PR_NUMBER=4163; PRODUCT_REPO=OmniNode-ai/onex_change_control; PRODUCT_BRANCH=jonah/omn-11878-classify-imperative-guard-baseline-debt-and-bind-every;
          PRODUCT_HEAD="$(gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid --jq
          .headRefOid)"; d="$(mktemp -d)" && gh repo clone "$PRODUCT_REPO" "$d/wt" -- --depth 1 --branch
          "$PRODUCT_BRANCH" && test "$(git -C "$d/wt" rev-parse HEAD)" = "$PRODUCT_HEAD" && cd "$d/wt"
          && uv run check-imperative-contracts --repo-root . --allowlists-dir allowlists --scan-freestanding
