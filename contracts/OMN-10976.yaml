---
schema_version: "1.0.0"
ticket_id: "OMN-10976"
title: "Flip omnidash contract.yaml default from sqlite to postgres"
summary: "Binds omnidash PR #88, which flips the omnidash contract.yaml default reader from sqlite to
  postgres, making postgres the canonical projection source for all environments."
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnidash PR #88 passes CI with contract.yaml default flipped to postgres."
    command: "gh pr view 88 --repo OmniNode-ai/omnidash --json state,headRefOid"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-omnidash-pr-88"
    description: "omnidash PR #88 flips the omnidash contract.yaml default reader from sqlite to postgres."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10976/dod-omnidash-pr-88/command.yaml
          && grep -q '^pr_number: 88$' drift/dod_receipts/OMN-10976/dod-omnidash-pr-88/command.yaml"
  - id: "dod-occ-pr-1009"
    description: "onex_change_control PR #1009 carries the OMN-10976 contract and downstream receipt bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-10976/dod-occ-pr-1009/command.yaml
          && grep -q '^pr_number: 1009$' drift/dod_receipts/OMN-10976/dod-occ-pr-1009/command.yaml"
