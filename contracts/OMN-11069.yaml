---
schema_version: "1.0.0"
ticket_id: "OMN-11069"
title: "CI anti-regression gate for os.environ reads"
summary: >-
  Central OCC evidence for omnibase_infra PR #1610. Adds scripts/check-env-reads.sh with --staged (pre-commit)
  and --base (CI) modes to block new os.environ/os.getenv reads outside approved overlay modules, wired
  into .pre-commit-config.yaml.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "tests"
    description: "12 unit tests covering all 5 blocked patterns and all 5 allowlist paths pass."
    command: "See drift/dod_receipts/OMN-11069/dod-unit-tests/command.yaml"
  - kind: "manual"
    description: "pre-commit run check-env-reads --all-files passes on existing codebase."
    command: "See drift/dod_receipts/OMN-11069/dod-pre-commit-passes/command.yaml"
  - kind: "manual"
    description: "omnibase_infra PR #1610 carries the check-env-reads.sh script and pre-commit hook."
    command: "See drift/dod_receipts/OMN-11069/dod-infra-pr-1610/command.yaml"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-unit-tests"
    description: "12 unit tests pass"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11069/dod-unit-tests/command.yaml
          && grep -q '12 passed' drift/dod_receipts/OMN-11069/dod-unit-tests/command.yaml"
  - id: "dod-pre-commit-passes"
    description: "pre-commit hook passes on existing codebase"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11069/dod-pre-commit-passes/command.yaml"
  - id: "dod-infra-pr-1610"
    description: "omnibase_infra PR #1610 exists"
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-11069/dod-infra-pr-1610/command.yaml"
