---
schema_version: "1.0.0"
ticket_id: "OMN-11069"
title: "feat(OMN-11069): Contract-overlay configuration — replace .env with contract overlays"
summary: >-
  Wire the existing overlay infrastructure into runtime boot and onboarding so that contracts + overlays
  replace .env files as the single configuration source of truth. Implements ModelOverlayFile, ModelOverlayResolutionManifest
  (omnibase_core), OverlayFileLoader, OverlayConfigResolver, OverlayWriter, boot_overlay.py, and CI os.environ
  anti-regression gate (omnibase_infra).
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - public_api
evidence_requirements:
  - kind: "tests"
    description: "OverlayConfigResolver unit tests pass — resolves keys, raises RequiredConfigMissingError,
      apply_to_environment returns injection result."
    command: >-
      uv run pytest tests/unit/runtime/overlay/test_overlay_config_resolver.py -v
  - kind: "tests"
    description: "OverlayFileLoader unit tests pass — loads valid YAML, rejects invalid schema and bad
      permissions."
    command: >-
      uv run pytest tests/unit/runtime/overlay/test_overlay_file_loader.py -v
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-overlay-config-resolver-task4"
    description: "OverlayConfigResolver + ModelOverlayResolutionResult + ModelOverlayEnvInjectionResult
      implemented with 12 unit tests passing (PR #1612)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          test -f drift/dod_receipts/OMN-11069/dod-overlay-config-resolver-task4/command.yaml && grep
          -q '^status: PASS$' drift/dod_receipts/OMN-11069/dod-overlay-config-resolver-task4/command.yaml
          && grep -q 'pr_number: 1612' drift/dod_receipts/OMN-11069/dod-overlay-config-resolver-task4/command.yaml
  - id: "dod-overlay-config-resolver-deploy"
    description: "OverlayConfigResolver module importable from the runtime image after deploy (PR #1612)."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          docker exec omninode-runtime python -c "from omnibase_infra.runtime.overlay.overlay_config_resolver
          import OverlayConfigResolver; print('deploy ok')"
  - id: "dod-occ-pr-1050"
    description: "onex_change_control PR #1050 carries the OMN-11069 central contract and receipts."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' drift/dod_receipts/OMN-11069/dod-occ-pr-1050/command.yaml && grep -q
          '^pr_number: 1050$' drift/dod_receipts/OMN-11069/dod-occ-pr-1050/command.yaml
