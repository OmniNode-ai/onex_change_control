---
schema_version: "1.0.0"
ticket_id: "OMN-11069"
title: "Contract-overlay configuration: replace .env with contract overlays"
summary: >-
  Epic: eliminate all .env file dependencies by wiring the overlay infrastructure into runtime boot and
  onboarding, making contracts + overlays the single source of truth for all configuration. Wave 1 implements
  ModelOverlayFile and ModelOverlayResolutionManifest in omnibase_core (OMN-11070).
is_seam_ticket: false
interface_change: false
interfaces_touched: []
contract_completeness: FULL
evidence_requirements:
  - kind: tests
    description: "Wave 1 unit tests pass: ModelOverlayFile and ModelOverlayResolutionManifest"
    command: >-
      uv run pytest tests/unit/models/overlay/ -v
  - kind: ci
    description: "CI pipeline green on omnibase_core PR #1082"
    command: "gh pr checks 1082 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-core-pr-1082
    description: "omnibase_core PR #1082 — Wave 1 ModelOverlayFile + ModelOverlayResolutionManifest"
    source: generated
    checks:
      - check_type: command
        check_value: >-
          test -f "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11070/dod-core-pr-1082/command.yaml" && grep
          -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11070/dod-core-pr-1082/command.yaml"
          && grep -q '^pr_number: 1082$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11070/dod-core-pr-1082/command.yaml"
