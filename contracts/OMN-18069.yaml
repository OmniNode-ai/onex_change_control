---
schema_version: "1.0.0"
ticket_id: "OMN-18069"
title: "Autobind OCC evidence for OMN-18069"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnimarket PR #2423.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #2423 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #2423 on OmniNode-ai/omnimarket, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnimarket`.
    command: |-
      uv run pytest tests/unit/nodes/node_pr_lifecycle_fix_effect/test_autobind_loud_outcome_omn_18069.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnimarket-pr-2423"
    description: "PR #2423 on OmniNode-ai/omnimarket — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/src/omnimarket/github_app_auth.py?ref=8da0203a108d733a8038268e648762df9b984323 --jq '.content' | base64 -d | grep -c 'class GitHubAppCredentialMalformedError'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2423/files --paginate --jq '.[].filename' | grep -cFx 'src/omnimarket/github_app_auth.py'
  - id: "dod-deploy-assessment"
    description: "Deploy-scope DoD so PR #2423 clears the deploy-gate (F-05)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2423/files --paginate --jq '.[].filename' | grep -ciE 'nodes/|handlers/|runtime/|services/|docker|monitor_logs|deploy'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #2423 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/nodes/node_pr_lifecycle_fix_effect/test_autobind_loud_outcome_omn_18069.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3353"
    description: >
      PR #3353 on OmniNode-ai/omnibase_infra — the companion-merged gate reads the autobind producer's
      terminal outcome instead of polling its 1500-second deadline. Bound BY HAND (union-resolve onto
      this ticket's existing contract) because the autobind emitter is the very surface this ticket repairs:
      its dispatch for #3353 was consumed on the .201 dev lane at 2026-09-09T06:43:58Z and failed, exactly
      as it did for the 37 dispatches that opened the ticket. omnibase_infra has no second OCC producer,
      so nothing was minted. Every entry that preceded this one is byte-identical.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/scripts/ci/check_occ_companion_merged.py?ref=be2ac4723ac31f595a4a11b7885edca8c5050bc3 --jq '.content' | base64 -d | grep -c 'AUTOBIND_OUTCOME_CHECK_NAME'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3353/files --paginate --jq '.[].filename' | grep -cFx 'scripts/ci/check_occ_companion_merged.py'
  - id: "dod-occ-diff-derived-behavior-proof-pr-3353"
    description: "PR #3353 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/ci/test_check_occ_companion_merged.py -q
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1277"
    description: >
      PR #1277 on OmniNode-ai/omninode_infra — the OMN-18069 scope-item-4 fan-out: this repo's vendored
      copy of scripts/ci/check_occ_companion_merged.py now reads the autobind producer's terminal outcome
      check-run instead of polling its 1500-second deadline. Bound BY HAND (union-resolve onto this ticket's
      existing contract) for the same reason every entry above it was: the autobind emitter is the very
      surface this ticket repairs, so nothing was minted. Every entry that preceded this one is byte-identical.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/contents/scripts/ci/check_occ_companion_merged.py?ref=3add0b1e7ec4a75a00fef8e2135f8364de54572e --jq '.content' | base64 -d | grep -c 'AUTOBIND_OUTCOME_CHECK_NAME'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1277/files --paginate --jq '.[].filename' | grep -cFx 'scripts/ci/check_occ_companion_merged.py'
  - id: "dod-occ-diff-derived-behavior-proof-pr-1277"
    description: "PR #1277 on OmniNode-ai/omninode_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/scripts/test_check_occ_companion_merged.py -q
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "dod-OmniNode-ai-omniclaude-pr-2129"
    description: >
      PR #2129 on OmniNode-ai/omniclaude — the OMN-18069 scope-item-4 fan-out: this repo's vendored copy
      of scripts/ci/check_occ_companion_merged.py now reads the autobind producer's terminal outcome check-run
      instead of polling its 1500-second deadline. Bound BY HAND (union-resolve onto this ticket's existing
      contract) for the same reason every entry above it was: the autobind emitter is the very surface
      this ticket repairs, so nothing was minted. Every entry that preceded this one is byte-identical.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniclaude/contents/scripts/ci/check_occ_companion_merged.py?ref=accf8734b6372345af40996d2ba59d661afd1de3 --jq '.content' | base64 -d | grep -c 'AUTOBIND_OUTCOME_CHECK_NAME'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omniclaude/pulls/2129/files --paginate --jq '.[].filename' | grep -cFx 'scripts/ci/check_occ_companion_merged.py'
  - id: "dod-occ-diff-derived-behavior-proof-pr-2129"
    description: "PR #2129 on OmniNode-ai/omniclaude — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/ci/test_check_occ_companion_merged.py -q
        cwd: "${OMNI_HOME}/omniclaude"
  - id: "dod-OmniNode-ai-omnimarket-pr-2429"
    description: >
      PR #2429 on OmniNode-ai/omnimarket — the OMN-18069 scope-item-4 fan-out: this repo's vendored copy
      of scripts/ci/check_occ_companion_merged.py now reads the autobind producer's terminal outcome check-run
      instead of polling its 1500-second deadline. Bound BY HAND (union-resolve onto this ticket's existing
      contract) for the same reason every entry above it was: the autobind emitter is the very surface
      this ticket repairs, so nothing was minted. Every entry that preceded this one is byte-identical.
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/contents/scripts/ci/check_occ_companion_merged.py?ref=def6e2f5cc794fb7481b2394ba996faaeb99c927 --jq '.content' | base64 -d | grep -c 'AUTOBIND_OUTCOME_CHECK_NAME'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnimarket/pulls/2429/files --paginate --jq '.[].filename' | grep -cFx 'scripts/ci/check_occ_companion_merged.py'
  - id: "dod-occ-diff-derived-behavior-proof-pr-2429"
    description: "PR #2429 on OmniNode-ai/omnimarket — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest tests/unit/scripts/ci/test_check_occ_companion_merged.py -q
        cwd: "${OMNI_HOME}/omnimarket"
  - id: "occ-self-bind-pr-8764"
    description: "Binds OMN-18069 to OCC companion PR #8764 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8764 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "occ-self-bind-pr-8772"
    description: "Binds OMN-18069 to OCC companion PR #8772 (self-bind, union-resolve for omnibase_infra#3353)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 8772 --repo OmniNode-ai/onex_change_control --json number,state"
