---
schema_version: "1.0.0"
ticket_id: OMN-12425
title: "feat(OMN-12425): proactive OCC delegated-repair path"
summary: >
  Extends OccContractAdapter in omnimarket node_pr_lifecycle_fix_effect with a proactive OCC delegated-repair
  path: dry_run/mutate modes, idempotency key (ticket_id, evidence_item_id, repo, pr_head_sha, contract_sha256),
  mandatory receipt fields (contract_sha256, pr_head_sha, source_repo, runner, verifier), and verifier
  != runner self-attestation rejection.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: ci
    description: >
      Unit tests green: gap-detection, receipt fields, dry-run no-mutation, mutate full-path, idempotent
      rerun no-op, verifier==runner rejection
    command: "uv run pytest tests/unit/nodes/node_pr_lifecycle_fix_effect/test_occ_proactive_repair.py
      tests/unit/nodes/node_pr_lifecycle_fix_effect/test_adapter_occ_contract.py -q --repo OmniNode-ai/omnimarket"
  - kind: ci
    description: >
      adapter_occ_contract.py extended with _compute_contract_sha256, _build_idempotency_key, detect_occ_gap,
      mode/runner/verifier params, and updated _RECEIPT_TEMPLATE with mandatory fields
    command: "grep -q 'contract_sha256' src/omnimarket/nodes/node_pr_lifecycle_fix_effect/handlers/adapter_occ_contract.py"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-unit-tests-green
    description: >
      79+ unit tests pass covering gap-detection, dry-run, mutate, idempotency, verifier enforcement,
      and all mandatory receipt fields. Evidence in drift/dod_receipts/OMN-12425/dod-unit-tests-green/command.yaml.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12425/dod-unit-tests-green/command.yaml"
  - id: dod-receipt-fields-complete
    description: >
      _RECEIPT_TEMPLATE includes contract_sha256, pr_head_sha, source_repo, runner, verifier, probe_command,
      probe_stdout, run_timestamp, commit_sha. Evidence in drift/dod_receipts/OMN-12425/dod-receipt-fields-complete/command.yaml.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12425/dod-receipt-fields-complete/command.yaml"
  - id: dod-idempotency-key
    description: >
      _build_idempotency_key() computes 5-tuple key; identical re-run is no-op. Evidence in drift/dod_receipts/OMN-12425/dod-idempotency-key/command.yaml.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12425/dod-idempotency-key/command.yaml"
  - id: dod-self-attestation-rejected
    description: >
      verifier == runner raises ValueError (self-attestation rejected). Evidence in drift/dod_receipts/OMN-12425/dod-self-attestation-rejected/command.yaml.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12425/dod-self-attestation-rejected/command.yaml"
  - id: dod-deploy-dry-run-safe
    description: >
      OccContractAdapter dry-run mode is verified via unit tests: no git/network calls are made in dry_run
      mode. deploy-gate evidence: adapter has no new daemon or service process; it is called on-demand
      from within node_pr_lifecycle_fix_effect. Evidence in drift/dod_receipts/OMN-12425/dod-deploy-dry-run-safe/command.yaml.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12425/dod-deploy-dry-run-safe/command.yaml"
  - id: dod-occ-self
    description: >
      Self-binding OCC receipt: this onex_change_control PR carries the OMN-12425 contract and all PASS
      DoD receipts required by OCC eligibility and downstream Receipt Gate resolution.
    source: generated
    checks:
      - check_type: command
        check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-12425/dod-occ-self/command.yaml"
