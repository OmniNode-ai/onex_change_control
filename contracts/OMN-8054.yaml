---
schema_version: '1.0.0'
ticket_id: OMN-8054
title: 'Task 7: Task shape feature extractor'
summary: 'Pure deterministic extraction of ModelTaskShapeFeatures from git diff stats, file listings, and ledger novelty scores. Used by routing engine to select models.'
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: All four DoD test cases pass in omnimarket PR 528
    command: uv run pytest tests/test_task_shape_extractor.py -v
  - kind: ci
    description: CI pipeline green on the omnimarket PR
    command: gh pr checks 528 --repo OmniNode-ai/omnimarket
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-001
    description: handler_task_shape_extractor.py present in omnimarket PR 528 diff
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 528 --repo OmniNode-ai/omnimarket | grep -q 'handler_task_shape_extractor'
  - id: dod-002
    description: test_task_shape_extractor.py present in omnimarket PR 528 diff with all four DoD test cases
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 528 --repo OmniNode-ai/omnimarket | grep -q 'test_extracts_diff_size_from_git_diff'
  - id: dod-003
    description: Implementation is a pure function with no LLM calls
    source: manual
    checks:
      - check_type: command
        check_value: gh pr diff 528 --repo OmniNode-ai/omnimarket | grep -qv 'openai\|anthropic'
  - id: dod-004
    description: omnimarket PR 528 open against main branch
    source: manual
    checks:
      - check_type: command
        check_value: gh pr view 528 --repo OmniNode-ai/omnimarket --json state,baseRefName --jq 'select(.state=="OPEN" and .baseRefName=="main") | "ok"' | grep -q ok
