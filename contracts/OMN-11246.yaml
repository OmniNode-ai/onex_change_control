---
schema_version: 1.0.0
ticket_id: OMN-11246
title: Dynamic topic subscription after dispatch engine freeze
summary: Dynamic handler materialization can register dispatchers and routes after the dispatch engine
  has frozen when explicitly authorized.
is_seam_ticket: true
interface_change: true
interfaces_touched:
  - protocols
  - events
contract_completeness: ENRICHED
evidence_requirements:
  - kind: tests
    description: Product PR  #1653 added unit and integration coverage for post-freeze dynamic registration.
    command: uv run pytest tests/unit/runtime/test_dispatch_engine_post_freeze.py tests/integration/runtime/test_dispatch_engine_post_freeze_dynamic_registration.py
      -q
emergency_bypass:
  enabled: false
  justification: ''
  follow_up_ticket_id: ''
dod_evidence:
  - id: dod-dynamic-registration-integration
    description: Product verification evidence is captured as a canonical PASS receipt.
    source: manual
    status: verified
    checks:
      - check_type: command
        check_value: >
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11246/dod-dynamic-registration-integration/command.yaml"
          && grep -q '^pr_number: 1653$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11246/dod-dynamic-registration-integration/command.yaml"
          && grep -q '^verifier: agent$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11246/dod-dynamic-registration-integration/command.yaml"
