---
schema_version: "1.0.0"
ticket_id: "OMN-12566"
title: "Bounded Docker network janitor + subnet-pool alerting"
summary: >-
  Bind central evidence for omnibase_infra PR #1826, which adds a bounded declared-ownership Docker network
  janitor plus subnet-pool exhaustion alerting on the existing .201 runner-health ops surface, and hardens
  the runtime-boot compose teardown so per-run networks never leak.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-infra-pr-1826"
    description: "omnibase_infra PR #1826 carries the OMN-12566 janitor + alerting work."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12566/dod-infra-pr-1826/command.yaml"
          && grep -q '^pr_number: 1826$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12566/dod-infra-pr-1826/command.yaml"
  - id: "dod-local-gates"
    description: "Local gates green: TDD janitor suite, full unit suite, mypy --strict, pre-commit."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12566/dod-local-gates/command.yaml"
          && grep -q '20823 passed' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12566/dod-local-gates/command.yaml"
  - id: "dod-occ-pr-self"
    description: "Self-binding receipt for the OCC PR carrying OMN-12566 central evidence."
    source: "manual"
    status: "verified"
    checks:
      - check_type: "command"
        check_value: >-
          grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12566/dod-occ-pr-self/command.yaml"
          && grep -q '^ticket_id: "OMN-12566"$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-12566/dod-occ-pr-self/command.yaml"
