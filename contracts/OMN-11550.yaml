---
schema_version: "1.0.0"
ticket_id: "OMN-11550"
title: "Add contract validation compute node"
summary: >-
  Bind central OCC evidence for omnibase_infra PR #1711, which adds node_contract_validate_compute as
  the runtime boundary for contract validation and includes top-level integration coverage.
is_seam_ticket: false
interface_change: true
interfaces_touched:
  - "protocols"
  - "public_api"
evidence_requirements:
  - kind: "tests"
    description: "omnibase_infra PR #1711 CI and receipt gates pass for contract validation compute."
    command: "gh pr checks 1711 --repo OmniNode-ai/omnibase_infra"
  - kind: "manual"
    description: "Central OCC evidence PR carries this contract and PASS receipts."
    command: "gh pr view <occ-pr> --repo OmniNode-ai/onex_change_control"
dod_evidence:
  - id: "dod-omnibase-infra-pr-1711"
    description: "Bind OMN-11550 evidence to omnibase_infra PR #1711 at the observed PR head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11550/dod-omnibase-infra-pr-1711/command.yaml\"
          && grep -q '^pr_number: 1711$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11550/dod-omnibase-infra-pr-1711/command.yaml\""
  - id: "dod-deploy-assessment"
    description: "Runtime node addition requires CI validation but no live deploy before merge."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11550/dod-deploy-assessment/command.yaml\"
          && grep -q 'No live deploy' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11550/dod-deploy-assessment/command.yaml\""
  - id: "dod-occ-pr-self"
    description: "Bind OMN-11550 central evidence to the OCC PR that carries this bundle."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "grep -q '^status: PASS$' \"$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-11550/dod-occ-pr-self/command.yaml\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
