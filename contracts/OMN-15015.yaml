---
schema_version: "1.0.0"
ticket_id: "OMN-15015"
title: "Autobind OCC evidence for OMN-15015"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omniclaude PR #1929.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1929 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omniclaude-pr-1929"
    description: "PR #1929 on OmniNode-ai/omniclaude — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json files"
  - id: "occ-self-bind-pr-4710"
    description: "Binds OMN-15015 to OCC companion PR #4710 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view ${PR_NUMBER} --repo ${REPO} --json number,state"
  - id: "dod-omniclaude-1929-r2-head"
    description: "PR #1929 on OmniNode-ai/omniclaude — exact repaired head after stale-run race refresh."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1929 --repo OmniNode-ai/omniclaude --json number,state,headRefOid,headRefName"
  - id: "dod-omniclaude-1929-r2-files"
    description: "PR #1929 on OmniNode-ai/omniclaude — OCC caller workflow diff scope."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1929 --repo OmniNode-ai/omniclaude --json files"
  - id: "dod-omniclaude-1929-r2-status"
    description: "PR #1929 on OmniNode-ai/omniclaude — hosted status readback for exact head."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1929 --repo OmniNode-ai/omniclaude --json headRefOid,mergeStateStatus,statusCheckRollup
          --jq 'if .headRefOid == \"4f5f717a907ed199f8c5b311726db44643fe7d73\" and ([.statusCheckRollup[]?
          | select(.conclusion == \"FAILURE\")] | length) == 0 then \"omniclaude#1929 exact head has no
          failed hosted checks\" else error(\"omniclaude#1929 exact head is stale or has failed hosted
          checks\") end'"
  - id: "occ-self-bind-pr-4714"
    description: "OCC companion PR #4714 — self-bind for OMN-15015 r2 evidence."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "gh pr view 4714 --repo OmniNode-ai/onex_change_control --json number,state,headRefOid"
