---
schema_version: "1.0.0"
ticket_id: OMN-10275
title: "Remove production default no-op merge adapter fallback"
summary: "Fail closed when PR lifecycle merge runs non-dry-run without an injected GitHub adapter"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "omnimarket PR checks pass"
    command: "gh pr checks ${PR_NUMBER} --repo OmniNode-ai/omnimarket --watch"
  - kind: "manual"
    description: "Post-merge runtime smoke confirms merge handler imports"
    command: "docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c \"from omnimarket.nodes.node_pr_lifecycle_merge_effect.handlers.handler_pr_lifecycle_merge
      import HandlerPrLifecycleMerge; print(HandlerPrLifecycleMerge.__name__)\""
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-001"
    description: "Merge handler fails closed without an adapter outside dry-run"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_golden_chain_pr_lifecycle_merge_effect.py::TestPrLifecycleMergeEffectGoldenChain::test_non_dry_run_without_adapter_fails_closed
          -q"
  - id: "dod-002"
    description: "Queue-manager merge handler and orchestrator regression tests pass"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run pytest tests/test_golden_chain_pr_lifecycle_merge_effect.py tests/unit/nodes/node_pr_lifecycle_merge_effect/test_orchestrator_wires_live_merge_adapter.py
          -q"
  - id: "dod-003"
    description: "Touched merge fallback cleanup passes lint, typecheck, and deploy-readiness smoke"
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "uv run ruff check src/omnimarket/nodes/node_pr_lifecycle_merge_effect/handlers/handler_pr_lifecycle_merge.py
          tests/test_golden_chain_pr_lifecycle_merge_effect.py tests/unit/nodes/node_pr_lifecycle_merge_effect/test_orchestrator_wires_live_merge_adapter.py
          && uv run mypy src/omnimarket/nodes/node_pr_lifecycle_merge_effect/handlers/handler_pr_lifecycle_merge.py
          && uv run python -c \"from omnimarket.nodes.node_pr_lifecycle_merge_effect.handlers.handler_pr_lifecycle_merge
          import HandlerPrLifecycleMerge; print('deploy readiness smoke', HandlerPrLifecycleMerge.__name__)\""
