---
schema_version: "1.0.0"
ticket_id: "OMN-19175"
title: "Hand-authored OCC evidence for OMN-19175"
summary: |-
  OCC contract hand-authored under OMN-15247 for OmniNode-ai/omninode_infra PR #1610, the C11 verdict
  probe. No autobind companion exists for that pull request: occ-autobind and occ-companion-effect both
  published their commands successfully at 2026-09-22T15:21Z (event ids 31068cfc-ec51-4ae8-ae22-ff7733bcd18b
  and 35988bc1-ed4e-4832-8a1d-9d0b7f291ef9) and no companion was minted, while the same runtime minted
  OCC#10801, #10802 and #10803 for three other pull requests in the same window. That is the OMN-19166
  defect class, and it is recorded here rather than worked around.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #1610 CI checks"
    command: "gh pr checks 1610 --repo OmniNode-ai/omninode_infra"
  - kind: "tests"
    description: |-
      The behavior proof is the C11 registration test, which is RED-derivable: reverting
      tools/beta_board/beta_board_data.py to the merge base while holding the rest of the head makes
      tests/scripts/test_m4_negative_paths_verdict_omn19175.py::test_probes_names_this_workflow_for_c11 fail.
      Executed in both directions before this contract was authored; see the receipt.
    command: "uv run pytest tests/scripts/test_m4_negative_paths_verdict_omn19175.py -q"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omninode_infra-pr-1610"
    description: "PR #1610 on OmniNode-ai/omninode_infra — hand-authored evidence (OMN-15247); no autobind
      companion exists or was minted for this PR. Content-bound: the C11 PROBES tuple naming m4-negative-paths-verdict.yml
      is present at the PR head and absent at the merge base, and the registration test that asserts it
      fails when that line is reverted."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api "repos/OmniNode-ai/omninode_infra/contents/tools/beta_board/beta_board_data.py?ref=205f4e616d78a6ffbf87292a751be43c92857321" --jq '.content' | base64 -d | grep -cF '("m4-negative-paths-verdict","OmniNode-ai/omninode_infra","m4-negative-paths-verdict.yml","C11")'
      - check_type: "command"
        check_value: "uv run pytest tests/scripts/test_m4_negative_paths_verdict_omn19175.py -q"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1610-ci"
    description: "PR #1610 on OmniNode-ai/omninode_infra — product diff scope check (OMN-14425)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1610 --repo OmniNode-ai/omninode_infra --json files"
