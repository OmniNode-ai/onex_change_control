---
schema_version: "1.0.0"
ticket_id: "OMN-13662"
evidence_item_id: "dod-deploy-assessment"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13662/dod-deploy-assessment/command.yaml
  && grep -q '^pr_number: 1462$' drift/dod_receipts/OMN-13662/dod-deploy-assessment/command.yaml && grep
  -qi 'no live deploy' drift/dod_receipts/OMN-13662/dod-deploy-assessment/command.yaml"
contract_sha256: "sha256:e86f48bc303134bd9f4eb81aa7c1ba2f2ad4fa4170dd77208b20700e03a77bd1"
status: PASS
run_timestamp: "2026-06-27T06:05:00Z"
commit_sha: "45406e3cbca9e89162c1e697cd1e1aefc7248672"
branch: "jonah/omn-13662-not-tier-routed"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-13662/omnimarket"
pr_number: 1462
runner: "claude-code"
verifier: "claude-code-review"
probe_command: "git -C omnimarket diff --stat origin/dev...HEAD"
probe_stdout: |
  .../nodes/node_projection_delegation/contract.yaml |   7 +
  ...elegation_tier_distribution_not_tier_routed.sql | 204 ++++++++++++
  tests/test_projection_api_server.py                |  22 +-
  ...ection_delegation_tier_distribution_omn13662.py | 365 +++++++++++++++++++++
  tests/unit/projection/test_projection_discovery.py |   5 +-
  5 files changed, 601 insertions(+), 2 deletions(-)
actual_output: >-
  PASS: no live deploy, runtime restart, topic mutation, or .201 mutation is performed for OMN-13662 in
  PR #1462. The change is a projection VIEW DDL addition (node-owned migration 0021 CREATE OR REPLACE
  VIEW projection_delegation_model_routing) plus an additive model-routing.v1 read-surface exposure (by_tier
  column in node_projection_delegation/contract.yaml) plus new unit tests; the cost_tier_name column it
  reads already exists (migration 0018), so no schema migration runs. No new node, no terminal/wire DTO
  field, no Dockerfile / compose / topic surface touched; dev lane only. This receipt does NOT claim the
  classification is already rendering live -- the live dev-lane psql proof (apply the view via CREATE
  OR REPLACE VIEW, then SELECT by_tier FROM projection_delegation_model_routing to show delegate-skill
  rows classified not_tier_routed and absent from the tier-% denominator for a known correlation_id of
  each kind, plus the projection-API exposure returning the classification) is a SEPARATE serialized Stage-4
  step after this OCC pairing merges. Because the rule lives in a CREATE OR REPLACE VIEW, that live proof
  needs only a psql apply -- no node redeploy and no T1/OMN-13660 dependency. Static proof; runner claude-code
  != verifier claude-code-review.
exit_code: 0
