---
schema_version: "1.0.0"
ticket_id: "OMN-13662"
evidence_item_id: "dod-omnimarket-pr-1462"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13662/dod-omnimarket-pr-1462/command.yaml
  && grep -q '^pr_number: 1462$' drift/dod_receipts/OMN-13662/dod-omnimarket-pr-1462/command.yaml && grep
  -q '10 passed' drift/dod_receipts/OMN-13662/dod-omnimarket-pr-1462/command.yaml"
contract_sha256: "sha256:eff09a591e705942dda55627aad99f3dc0c09595481d0f1bb34331e41a07fc3e"
status: PASS
run_timestamp: "2026-06-27T06:05:00Z"
commit_sha: "45406e3cbca9e89162c1e697cd1e1aefc7248672"
branch: "jonah/omn-13662-not-tier-routed"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-13662/omnimarket"
pr_number: 1462
runner: "claude-code"
verifier: "claude-code-review"
probe_command: "uv run pytest tests/test_projection_delegation_tier_distribution_omn13662.py -q"
probe_stdout: |
  ..........s                                                              [100%]
  10 passed, 1 skipped in 0.09s
actual_output: >-
  PASS: omnimarket PR #1462 encodes the not_tier_routed projection rule in node-owned migration 0021 (CREATE
  OR REPLACE VIEW projection_delegation_model_routing, by_tier jsonb appended at the END of the SELECT
  for replace-compatibility) and declares by_tier in the model-routing.v1 projection_api exposure (columns
  + json_columns, verified in node_projection_delegation/contract.yaml). The classification CASE maps
  empty cost_tier_name -> 'not_tier_routed'; by_tier carries total_tasks (inclusive), tier_routed_total
  (denominator = rows with non-empty cost_tier_name), not_tier_routed_count (excluded bucket), and tiers[]
  with pct_of_tier_routed gated on tier_routed_total > 0 so the not_tier_routed bucket gets pct 0 and
  the tier-routed pcts sum to 1.0. The focused test module test_projection_delegation_tier_distribution_omn13662.py
  was re-run on the PR head (45406e3c) and 10 passed, 1 skipped (the 1 skip is the real-Postgres view-DDL
  integration test, which self-skips without a DB). The tests prove, over a mixed fixture (2x local +
  1x claude LLM-tiered rows + 2x delegate-skill empty-tier rows), that tier-% sums to 1.0 over LLM rows
  only, totals include all rows, and not_tier_routed is surfaced + counted. Verified via gh pr checks
  2026-06-27: the only failing checks on #1462 are deploy-gate + occ-preflight + verify (this OCC pairing
  satisfies them); all other checks (Tests Split 1/2 + 2/2, Golden Chain Suite, E2E, SEA E2E, lint, typecheck,
  contract-validation, node-drift-gate, Runtime Sweep, Hostile Reviewer) are green. Static proof; runner
  claude-code != verifier claude-code-review.
exit_code: 0
