---
schema_version: "1.0.0"
ticket_id: "OMN-15724"
evidence_item_id: "dod-occ-guard-cache-activation-scoped"
check_type: "command"
check_value: |-
  python3 -c "
  import sys, yaml
  j = yaml.safe_load(open('.github/workflows/ci.yml'))['jobs']
  targets = ['validate-prod-promotion-grants', 'check-bot-authored-authz-guard']
  cache_host = 'omninode-pc.tail75df5e.ts.net:3141'
  bad = []
  for name in targets:
      env = j[name].get('env', {})
      idx = str(env.get('UV_DEFAULT_INDEX', ''))
      pip = str(env.get('PIP_INDEX_URL', ''))
      if cache_host not in idx or cache_host not in pip:
          bad.append(name)
  needs = j['ci-summary']['needs']
  other_scoped = [k for k in needs if k not in targets and cache_host in str(j.get(k, {}).get('env', {}))]
  sys.exit('scope violation: missing=%s leaked_to=%s' % (bad, other_scoped)) if (bad or other_scoped) else print('true')
  "
contract_sha256: "1fb52c4d2c4acb7df0565083b3fa01933f0b515abea9ad035164462f07e07702"
contract_entry_sha256: "sha256:4d324286c8ce6e11c1cfe5ee144875367ba1b224fe0ecf007803a493250c077a"
status: PASS
run_timestamp: "2026-08-09T18:46:16Z"
commit_sha: "8a598efb125bed2a9492f167ab5ce9ef39ccab16"
runner: "sonnet-betasprintD2"
verifier: "sonnet/betasprint-laneD2"
probe_command: |-
  python3 -c "
  import sys, yaml
  j = yaml.safe_load(open('.github/workflows/ci.yml'))['jobs']
  targets = ['validate-prod-promotion-grants', 'check-bot-authored-authz-guard']
  cache_host = 'omninode-pc.tail75df5e.ts.net:3141'
  bad = []
  for name in targets:
      env = j[name].get('env', {})
      idx = str(env.get('UV_DEFAULT_INDEX', ''))
      pip = str(env.get('PIP_INDEX_URL', ''))
      if cache_host not in idx or cache_host not in pip:
          bad.append(name)
  needs = j['ci-summary']['needs']
  other_scoped = [k for k in needs if k not in targets and cache_host in str(j.get(k, {}).get('env', {}))]
  sys.exit('scope violation: missing=%s leaked_to=%s' % (bad, other_scoped)) if (bad or other_scoped) else print('true')
  "
probe_stdout: "true"
actual_output: |
  PASS -- both target jobs carry job-level env with the OMN-14027 devpi cache host
  (omninode-pc.tail75df5e.ts.net:3141) in UV_DEFAULT_INDEX and PIP_INDEX_URL, and no
  other ci-summary needs-list member carries that host string -- confirming the
  activation is scoped to exactly these 2 jobs, not fleet- or repo-wide. Executed
  directly against the committed .github/workflows/ci.yml at f2e8c1a99, exit 0.
exit_code: 0
pr_number: 6267
branch: "jonahgabriel/omn-15724-guard-job-cost"
