---
schema_version: "1.0.0"
ticket_id: "OMN-14229"
evidence_item_id: "dod-omnibase_core-pr-1411"
check_type: "command"
check_value: "cd omnibase_core && env -u PYTHONPATH uv run pytest tests/unit/utils/test_fsm_executor.py::TestFSMPriorityGuardFallthrough"
status: PASS
run_timestamp: "2026-07-10T00:45:00Z"
commit_sha: 89b41118f142c978cc95444efd25278a327b8e8b
runner: "omn-14229-independent-verifier"
verifier: "occ-evidence-lane"
probe_command: >-
  cd /Users/jonah/Code/omni_home/omni_worktrees/OMN-14229/omnibase_core && env -u PYTHONPATH uv run pytest
  tests/unit/utils/test_fsm_executor.py::TestFSMPriorityGuardFallthrough
probe_stdout: |
  collected 7 items
  tests/unit/utils/test_fsm_executor.py .......                            [100%]
  7 passed
actual_output: >-
  PASS: omnibase_core PR #1411 wires the documented ModelFSMStateTransition.priority / guard-fallthrough
  semantics into util_fsm_executor._select_transition (replacing _find_transition): (from_state, trigger)
  candidates are ordered by priority DESCENDING with declaration order as the stable tiebreak, guards
  are evaluated in that order, the first eligible candidate wins, an unconditional transition is a default
  fallthrough edge, exact-state matches preempt wildcard, and when nothing is eligible the engine fails
  CLOSED with the existing guard_rejection result (stays in-state, success=False) rather than silently
  taking a false-guard transition. Independent reproduction with PYTHONPATH cleared (omnibase_core.__file__
  resolved to the worktree src at omni_worktrees/OMN-14229/omnibase_core, not the canonical clone): TestFSMPriorityGuardFallthrough
  7 passed; full test_fsm_executor.py 52 passed; FSM/reducer/mixin regression (test_mixin_fsm_execution,
  test_mixin_contract_state_reducer, test_reducer_integration, test_node_reducer_concurrency, test_node_reducer_determinism,
  test_node_reducer_build_fsm_context, test_fsm_analysis, test_fsm_handler_drift) 110 passed, 1 xpassed.
  Independent anti-gaming checks passed: priority-DESC selection isolated from declaration order (higher-priority
  edge declared SECOND wins), guard-truth fallthrough (higher-priority guard rejected, not silently taken),
  default edge genuinely advances state, fail-closed genuinely stays in-state with guard_rejection. Zero-regression:
  the ModelFSMSubcontract structural-uniqueness validator forbids duplicate (from_state, trigger, priority)
  so same-trigger candidate priorities are always distinct; the single standalone FSM contract YAML has
  no duplicate (from_state, trigger) group so its selection is byte-identical; the named production reducers
  do not use the FSM engine path. mypy clean; ruff check + ruff format --check clean; pre-commit on changed
  files exit 0.
exit_code: 0
pr_number: 1411
branch: "jonah/omn-14229-fsm-priority-guard-fallthrough"
contract_sha256: "sha256:74b5a4c644af9073810711a8bab1414e7a04dfffb8630fd38e80a008be2cebfe"
