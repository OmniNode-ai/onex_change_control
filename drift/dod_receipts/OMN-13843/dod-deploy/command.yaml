---
schema_version: "1.0.0"
ticket_id: "OMN-13843"
evidence_item_id: "dod-deploy"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13843/dod-deploy/command.yaml"
status: PASS
contract_sha256: "sha256:25de85294affd2aeb04320e0e5921451d58a4517aa25b7d661cd71a3f7cd3b4c"
run_timestamp: "2026-07-02T14:00:00Z"
commit_sha: "1f3a86e2"
runner: "local-occ-evidence"
verifier: "jonah"
probe_command: "n/a — deploy-not-required determination for a secret-resolution wiring fix"
probe_stdout: |
  No live prod deploy required. The change corrects how the verification_receipt_generator handler
  resolves the GITHUB_TOKEN (loop-safe resolver). The skill resolves via editable-venv entry-point node
  discovery and is exercised in-process by CI + the regression test; a future runtime refresh picks up
  the new code with no additional deploy action for this ticket.
actual_output: >-
  PASS: no live deploy required. The fix wires _resolve_github_token to resolve_api_key_loop_safe so the
  sync handler resolves the token from within a running event loop; correctness is proven by the full
  omnimarket suite and the RuntimeLocal + real-CI-verify regression tests, not by a runtime redeploy. No
  raw prod mutation performed.
exit_code: 0
branch: "jonah/omn-13843-verification-receipt-generator-fix"
pr_number: 1573
