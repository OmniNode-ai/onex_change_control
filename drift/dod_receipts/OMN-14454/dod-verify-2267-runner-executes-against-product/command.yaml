---
schema_version: "1.0.0"
ticket_id: "OMN-14454"
evidence_item_id: "dod-verify-2267-runner-executes-against-product"
check_type: "command"
check_value: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/ci.yml?ref=d5da8826a2cc7e5af0f442c0ae2e5c3cd27c1e1c"
  --jq .content | base64 -d | grep -c -- 'run_contract_compliance_check.py')" -ge 1
status: PASS
run_timestamp: "2026-07-12T08:20:00Z"
commit_sha: "d5da8826a2cc7e5af0f442c0ae2e5c3cd27c1e1c"
runner: "occ-queue-runner"
verifier: "occ-queue-verifier"
pr_number: 2267
probe_command: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/ci.yml?ref=d5da8826a2cc7e5af0f442c0ae2e5c3cd27c1e1c"
  --jq .content | base64 -d | grep -c -- 'run_contract_compliance_check.py')" -ge 1   # then re-run VERBATIM
  at omnibase_infra dev (b22a2a2e), where ci.yml EXISTS but is WRONG, and at a bogus SHA
probe_stdout: |
  # bound check_value exit codes, run verbatim:
  #   ref=d5da8826 (omnibase_infra#2267 head) -> exit 0   GREEN
  #   ref=b22a2a2e (infra dev)                 -> exit 1   RED   <- artifact EXISTS but is WRONG
  #   ref=000...0  (bogus SHA)                  -> exit 1   RED   <- not vacuous
exit_code: 0
actual_output: >-
  PASS: omnibase_infra's ci.yml at #2267's head INVOKES run_contract_compliance_check.py. The job previously
  ran 'uv run validate-yaml $CONTRACTS', which never executed a single check_value -- it only parsed OCC's
  YAML, so a green 'Contract Compliance Check' meant exactly 'OCC's ~6,900 files are valid YAML' and nothing
  about the PR. The DoD gate was dead platform-wide for ~3 months behind that green check. RED PROVEN
  against a ref where the artifact EXISTS BUT IS WRONG, not merely absent: infra dev HAS a ci.yml and
  HAS a contract-compliance job -- it just still runs the dead executor -> exit 1. Exit 1 at a bogus SHA.
  This check is a gh api content read and therefore workspace-INDEPENDENT: it observes the product no
  matter which tree the runner is pointed at.
branch: "jonah/omn-14454-occ-contract"
contract_sha256: "sha256:11b161b8a0768473c6fd566936bcc5a8078a67f5481e52955455aa535b5dc348"
contract_entry_sha256: "sha256:45e3995cfa70a9d0243fe5011391640d5aa18b8da3360a68590c3d3c9a417472"
