---
schema_version: "1.0.0"
ticket_id: "OMN-14454"
evidence_item_id: "dod-verify-2267-gate-cannot-suppress-tests"
check_type: "command"
check_value: >-
  C=$(gh api "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/ci.yml?ref=d5da8826a2cc7e5af0f442c0ae2e5c3cd27c1e1c"
  --jq .content 2>/dev/null | base64 -d 2>/dev/null); N=$(printf '%s' "$C" | grep -cE '^[[:space:]]+needs:
  \['); X=$(printf '%s' "$C" | grep -E '^[[:space:]]+needs: \[' | grep -c 'contract-compliance'); test
  "$N" -gt 0 && test "$X" -eq 0
status: PASS
run_timestamp: "2026-07-12T08:20:00Z"
commit_sha: "d5da8826a2cc7e5af0f442c0ae2e5c3cd27c1e1c"
runner: "occ-queue-runner"
verifier: "occ-queue-verifier"
pr_number: 2267
probe_command: >-
  C=$(gh api "repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/ci.yml?ref=d5da8826a2cc7e5af0f442c0ae2e5c3cd27c1e1c"
  --jq .content 2>/dev/null | base64 -d 2>/dev/null); N=$(printf '%s' "$C" | grep -cE '^[[:space:]]+needs:
  \['); X=$(printf '%s' "$C" | grep -E '^[[:space:]]+needs: \[' | grep -c 'contract-compliance'); test
  "$N" -gt 0 && test "$X" -eq 0   # then re-run VERBATIM at omnibase_infra dev (b22a2a2e), where ci.yml
  EXISTS but is WRONG, and at a bogus SHA
probe_stdout: |
  # bound check_value exit codes, run verbatim:
  #   ref=d5da8826 (omnibase_infra#2267 head) -> exit 0   GREEN
  #   ref=b22a2a2e (infra dev)                 -> exit 1   RED   <- artifact EXISTS but is WRONG
  #   ref=000...0  (bogus SHA)                  -> exit 1   RED   <- not vacuous
exit_code: 0
actual_output: >-
  PASS: contract-compliance appears in NO needs: list at #2267's head, while ci.yml still HAS needs: lists.
  It was a hard needs: predecessor of detect-changes and test-parallel, so a BLOCK did not fail one job
  -- it SKIPPED THE ENTIRE TEST PIPELINE. Turning the runner on without this would let a single circular
  contract silently un-run the test suite: hardening the evidence layer by disabling the test layer. Merge-blocking
  and test-suppressing are two different powers; a gate gets only the first. Enforcement is not weakened,
  and that is STRUCTURAL rather than asserted: ci-summary is a fail-closed poller with NO needs: -- it
  reads the run's job conclusions directly -- and 'Contract Compliance Check' is already in its gating
  list (ci_summary_gate.py:100), so a BLOCK still fails CI Summary, the required context. THE NAIVE FORM
  OF THIS CHECK IS A VACUOUS GREEN AND WAS REJECTED: 'count coupled needs: lines, require zero' PASSES
  against any ref with no ci.yml at all, since zero lines trivially contain zero couplings. This form
  requires needs: lists to EXIST (N > 0) and none to couple contract-compliance. RED PROVEN at infra dev,
  where ci.yml exists and couples it in 2 needs: lists -> exit 1; and at a bogus SHA (N == 0) -> exit
  1, so the check is not vacuous.
branch: "jonah/omn-14454-occ-contract"
contract_sha256: "sha256:11b161b8a0768473c6fd566936bcc5a8078a67f5481e52955455aa535b5dc348"
contract_entry_sha256: "sha256:c985bb3363b9d4bb30b3b86c2179e0cce7da79d089c01884f9cf34738ccc1439"
