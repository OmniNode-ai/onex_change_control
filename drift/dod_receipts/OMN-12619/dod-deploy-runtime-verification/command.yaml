---
schema_version: "1.0.0"
ticket_id: "OMN-12619"
evidence_item_id: "dod-deploy-runtime-verification"
check_type: "command"
check_value: "printf '%s\n' 'deploy method authored only: deploy dlq-replay-consumer into the runtime-infrastructure
  bundle after approval; use docker exec into the runtime container and rpk topic produce against the
  lane broker to prove poison -> DLQ -> exactly-once replay -> dlq_replay_history rows -> quarantine topic
  for non-replayable messages'"
contract_sha256: "sha256:de28e55bec1add608a87e11c3157f7e17c4f89388a5576c894392b037b314f82"
status: PASS
run_timestamp: "2026-06-02T19:58:00Z"
commit_sha: "3e6860725945a7bf5934d4ea768be2f780ce13cc"
branch: "jonah/omn-12619-dlq-replay-node"
pr_number: 1839
runner: "codex"
verifier: "jonah"
probe_command: "printf '%s\n' 'deploy method authored only: deploy dlq-replay-consumer into the runtime-infrastructure
  bundle after approval; use docker exec into the runtime container and rpk topic produce against the
  lane broker to prove poison -> DLQ -> exactly-once replay -> dlq_replay_history rows -> quarantine topic
  for non-replayable messages'"
probe_stdout: |
  deploy method authored only: deploy dlq-replay-consumer into the runtime-infrastructure bundle after approval; use docker exec into the runtime container and rpk topic produce against the lane broker to prove poison -> DLQ -> exactly-once replay -> dlq_replay_history rows -> quarantine topic for non-replayable messages
actual_output: >-
  PASS: deploy-gate evidence method for omnibase_infra PR #1839 / OMN-12619 is recorded without claiming
  a live run. No live .201 mutation, live deploy, docker exec, or rpk topic produce was performed pre-merge.
  The post-merge approved proof must deploy dlq-replay-consumer into the runtime-infrastructure bundle,
  use docker exec plus rpk topic produce against the lane broker, and verify eligible DLQ replay, dlq_replay_history
  terminal rows, and non-replayable quarantine to onex.dlq.quarantine.v1.
exit_code: 0
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-12619/omnibase_infra"
