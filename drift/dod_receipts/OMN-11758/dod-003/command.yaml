---
schema_version: 1.0.0
ticket_id: OMN-11758
evidence_item_id: dod-003
check_type: command
check_value: grep -q 'AccessDeniedException' drift/dod_receipts/OMN-11758/dod-003/command.yaml
status: PASS
observed_at: '2026-05-23T11:31:00Z'
probe_command: classify remaining blockers after local code fix boundary
probe_stdout: |
  `omnibase_core` downstream pin-bump is blocked by secret scope/config, not a flaky run: GitHub rejects workflow-file updates because `CROSS_REPO_PAT` lacks `workflow` scope, and the manifest still targets `omninode_infra`.
  `omniweb` deploy is blocked by AWS IAM, not a flaky run: `omniweb-ci` lacks `ssm:SendCommand` on EC2 instance `i-0e596e8b557e27785`.
  `omnimarket` and `omniintelligence` were rerun after their checkout-auth/cancelled-run boundaries were recorded.
actual_output: PASS - remaining blockers are classified on OMN-11758 and no prod/runtime lane mutation
  was attempted.
