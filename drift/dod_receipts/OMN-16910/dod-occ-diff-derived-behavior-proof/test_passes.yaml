---
schema_version: "1.0.0"
ticket_id: "OMN-16910"
evidence_item_id: "dod-occ-diff-derived-behavior-proof"
check_type: "test_passes"
check_value: "uv run pytest tests/scripts/test_check_occ_companion_merged.py -q"
contract_sha256: "sha256:072cced17026912f77e2b8c351c609e0b5ee2836a11cf2b4987a6a04f2387a32"
contract_entry_sha256: "sha256:386b47872ab2dbfe681275d51dcc8024b6d9f93d4895fdecbae24360bf1fd094"
status: PASS
run_timestamp: "2026-08-29T01:05:00Z"
commit_sha: "ffae92d2ba2d0ab950f3e14ef833fba0b303ecdc"
runner: "build-OMN-16910"
verifier: "live-command-readback"
probe_command: >-
  gh api repos/OmniNode-ai/omninode_infra/commits/ffae92d2ba2d0ab950f3e14ef833fba0b303ecdc --jq .sha &&
  cd "$OMNI_HOME/omni_worktrees/OMN-16910/omninode_infra" && git rev-parse HEAD && uv run --frozen pytest
  tests/scripts/test_check_occ_companion_merged.py -q
probe_stdout: |
  ffae92d2ba2d0ab950f3e14ef833fba0b303ecdc
  ffae92d2ba2d0ab950f3e14ef833fba0b303ecdc
  ........................                                                 [100%]
  24 passed in 1.28s
actual_output: >-
  PASS: 24/24 green, exit code 0, on omninode_infra PR #1056 at head ffae92d2ba2d0ab950f3e14ef833fba0b303ecdc
  (worktree HEAD read back against the live GitHub commit before the run, so the run is provably at the
  PR head). Executed in $OMNI_HOME/omni_worktrees/OMN-16910/omninode_infra rather than the contract's
  declared cwd $OMNI_HOME/omninode_infra, because the canonical clone tracks dev and does not contain
  tests/scripts/conftest.py or the TestAmbientEnvIsolation guard — both are added by this PR. Same repo,
  same command, at the commit under test. RED-first control, run against dev tip 4cc1071 before the fix:
  GITHUB_EVENT_NAME=push gives 2 failed on tests/scripts/test_check_occ_companion_merged.py::TestMainEntrypoint
  while env -u GITHUB_EVENT_NAME gives 2 passed, so the suite is non-vacuous and the defect is reproduced
  rather than inferred. After the fix the same file is 24 passed under BOTH environments (GITHUB_EVENT_NAME=push
  GH_REPO=bogus/repo PR_NUMBER=999, and env -u GITHUB_EVENT_NAME), which is the trigger-independence the
  ticket asks for. The new TestAmbientEnvIsolation guard was additionally proven non-vacuous by reverting
  the fix in place (conftest.py moved aside, both delenv lines stripped) and re-running only the guard:
  it fails and reports the two original TestMainEntrypoint failures. This receipt supplies the test_passes
  check the contract declares for this item; the sibling command.yaml emitted by autobind records a command-type
  receipt and does not by itself discharge the declared test_passes proof. Same autobind shape mismatch
  already recorded on OMN-15989, OMN-16764 and OMN-16902, tracked as OMN-16859 — hand-authored here rather
  than fixed inline, since the fix is the declaration side and a fix lane is concurrent.
exit_code: 0
pr_number: 1056
branch: "auto/omninode-ai-omninode_infra-pr-1056-occ-autobind"
