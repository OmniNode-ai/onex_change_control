---
schema_version: "1.0.0"
ticket_id: "OMN-14353"
evidence_item_id: "dod-omnibase_core-pr-1425"
check_type: "command"
check_value: "cd omnibase_core && env -u PYTHONPATH uv run pytest tests/unit/canary/ -q"
status: PASS
run_timestamp: "2026-07-11T02:05:00Z"
commit_sha: 2980603249274ba0d56d4e041a29b1f58337975b
runner: "omn-14353-independent-verifier"
verifier: "occ-evidence-lane"
probe_command: >-
  cd /Users/jonah/Code/omni_home/omni_worktrees/OMN-14353/omnibase_core && env -u PYTHONPATH uv run pytest
  tests/unit/canary/ -v; grep the canary test for handler-source mutation; mutate compare_output to return
  [] and re-run the discrimination test; run an independent two-way mask harness against compute_golden;
  env -u PYTHONPATH uv run ruff check; env -u PYTHONPATH uv run mypy --strict scripts/ci/compute_golden.py
probe_stdout: |
  8 passed in 1.89s (3 determinism, 3 record-then-replay, 1 discrimination, 1 volatile-mask)
  hermetic grep: canary test perturbs golden["output"][...] dicts only; no monkeypatch/setattr/patch/write of handler source
  mutation compare_output -> []: test_comparator_discriminates_a_regression FAILED "comparator MUST catch the regressed verdict"; reverted
  two-way mask harness: masked-field perturb -> [] GREEN; real-field perturb -> ['passed: True != False'] RED; real+masked perturb -> flags only the real field (mask not blind); PASS
  ruff check: All checks passed!
  re-bind: verified at head 2f9a72c55; head force-pushed to 2980603249 (single primitive-only commit, adequacy receipt split out); primitive content byte-identical except the one-line mypy annotation below
  mypy --strict compute_golden.py at head 2980603249: clean (the earlier no-any-return at line 42 is now annotated masked: dict[str, Any])
  PR #1425 tracked files: compute_golden.py + tests/unit/canary/* (primitive-only at this head)
actual_output: >-
  PASS: the compute-golden equivalence oracle primitive is real and genuinely discriminates. compute_golden.py
  is a pure typed input->output serializer + symmetric masked comparator, documented as complementary
  to (not duplicating) golden_chain.record_fixture. The canary is hermetic (perturbs recorded output dicts,
  never handler source) and the two-way mask discrimination holds three ways (committed regression test
  RED on a non-masked change, committed mask test GREEN on a masked change, independent harness confirms
  a real+masked perturb flags only the real field). Neutering the comparator drove the discrimination
  test RED, proving non-vacuity; determinism and replay-equivalence assertions are real. 8/8 tests pass,
  ruff clean. Re-bound to head 2980603249 (the branch was force-pushed to a single primitive-only commit
  after the adequacy receipt was split out); at this head mypy --strict on compute_golden.py is clean
  (the earlier no-any-return nit at line 42 is resolved). Worktree left clean of verification residue.
exit_code: 0
pr_number: 1425
branch: "jonah/omn-14353-fixture-recorder-canary"
contract_sha256: "sha256:aca933903036bd48e510841740d7ca3ae8374f1c1032a471c39c21c5787d9e23"
