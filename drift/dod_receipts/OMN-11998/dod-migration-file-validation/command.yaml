---
schema_version: "1.0.0"
ticket_id: "OMN-11998"
evidence_item_id: "dod-migration-file-validation"
check_type: "command"
check_value: "test -f docker/migrations/forward/082_swarm_runs.sql && rg -n 'CREATE TABLE IF NOT EXISTS
  swarm_runs|run_id[[:space:]]+TEXT PRIMARY KEY|models_used[[:space:]]+TEXT\\[\\]|created_at[[:space:]]+TIMESTAMPTZ
  NOT NULL DEFAULT NOW\\(\\)' docker/migrations/forward/082_swarm_runs.sql"
contract_sha256: "sha256:5c95dd884d581dbdd2530cbda3ca2bd92a429a233cbaf2896ce5e302caf4bbd5"
status: PASS
run_timestamp: "2026-05-25T14:40:00Z"
commit_sha: "d5458cc85d7d199a54e97b12a149646dba206361"
runner: "codex-cli"
verifier: "codex"
probe_command: "test -f docker/migrations/forward/082_swarm_runs.sql && rg -n 'CREATE TABLE IF NOT EXISTS
  swarm_runs|run_id[[:space:]]+TEXT PRIMARY KEY|models_used[[:space:]]+TEXT\\[\\]|created_at[[:space:]]+TIMESTAMPTZ
  NOT NULL DEFAULT NOW\\(\\)' docker/migrations/forward/082_swarm_runs.sql"
probe_stdout: |
  3:CREATE TABLE IF NOT EXISTS swarm_runs (
  4:  run_id            TEXT PRIMARY KEY,
  12:  models_used       TEXT[] DEFAULT '{}',
  32:  created_at                    TIMESTAMPTZ NOT NULL DEFAULT NOW()
actual_output: "PASS: forward migration 082_swarm_runs.sql creates the swarm_runs projection table with
  primary key, model array, and created_at timestamp fields."
exit_code: 0
pr_number: 1737
branch: "jonah/omn-11998-swarm-runs-migration"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-11998/omnibase_infra"
