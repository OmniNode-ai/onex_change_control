---
schema_version: "1.0.0"
ticket_id: "OMN-14898"
evidence_item_id: "dod-omnimarket-pr-1864-deploy-gate-code-only-hold"
check_type: "command"
check_value: "gh pr view 1864 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -F 'Proof class:
  **code-only**' && gh pr view 1864 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -F 'deployment-owned,
  operator-gated' && gh pr view 1864 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -F 'NOT
  claimed here'"
contract_entry_sha256: "sha256:2563ecd98b088e95e3e72bf5f136727d2b10bc2574efe1a93028d30da0695506"
status: PASS
run_timestamp: "2026-07-23T16:26:00Z"
commit_sha: "3aa7d3541a40d5451319f960c218eb7166dbc768"
runner: "codex-merge-controller"
verifier: "occ-evidence-source-autobind"
probe_command: "gh pr view 1864 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -F 'Proof
  class: **code-only**' && gh pr view 1864 --repo OmniNode-ai/omnimarket --json body --jq .body | grep
  -F 'deployment-owned, operator-gated' && gh pr view 1864 --repo OmniNode-ai/omnimarket --json body --jq
  .body | grep -F 'NOT claimed here'"
probe_stdout: |
  - Proof class: **code-only**. Live-readback proof against stability-test is gated on the Daniyal-owned infra handoff above and is NOT claimed here.
  Converting the omnimarket projection writer's runtime DB connection from the `postgres` SUPERUSER to a non-superuser, `NOBYPASSRLS` role (so `FORCE ROW LEVEL SECURITY` actually binds writes, not just `app_dashboard` reads) is a live infra/role/Secrets-Manager change — migration 094's own comment flags `app_dashboard` password/LOGIN attach as "deployment-owned, operator-gated." This PR's fail-closed application-level check is defense-in-depth regardless of connection role; it does not depend on or block that infra change, and does not invent or touch any live credential.
  - Proof class: **code-only**. Live-readback proof against stability-test is gated on the Daniyal-owned infra handoff above and is NOT claimed here.
actual_output: "PASS: omnimarket PR #1864 explicitly documents code-only proof class and no live deploy/readback
  claim."
exit_code: 0
pr_number: 1864
branch: "jonah/omn-14898-writer-tenant-fail-closed-rejection"
