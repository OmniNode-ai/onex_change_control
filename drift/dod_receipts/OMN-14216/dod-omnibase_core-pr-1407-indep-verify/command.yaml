---
schema_version: "1.0.0"
ticket_id: "OMN-14216"
evidence_item_id: "dod-omnibase_core-pr-1407-indep-verify"
check_type: "command"
check_value: "cd $OMNI_HOME/omni_worktrees/OMN-14216-verify/omnibase_core && env -u PYTHONPATH uv run
  lint-imports"
status: PASS
run_timestamp: "2026-07-09T21:20:00Z"
commit_sha: bb19d723b329fd96f5a0c049346280df6d68adba
runner: "omn-14216-independent-verifier"
verifier: "occ-evidence-lane"
probe_command: >-
  cd $OMNI_HOME/omni_worktrees/OMN-14216-verify/omnibase_core && rm -rf .import_linter_cache .grimp_cache
  && env -u PYTHONPATH uv run lint-imports
probe_stdout: |
  Analyzed 4078 files, 10327 dependencies.
  Foundation primitives must not import higher layers (direct) KEPT
  Models must not import behavioral/orchestration layers (direct) KEPT
  Services and validation must not import nodes (direct) KEPT
  Contracts: 3 kept, 0 broken.
actual_output: >-
  PASS: an independent verifier (not the implementing agent) reproduced the import-linter oracle against
  omnibase_core PR #1407 head bb19d723 in an isolated worktree with the canonical-clone PYTHONPATH cleared,
  so grimp analyzed the PR-branch source. lint-imports kept all 3 contracts. Negative tests confirmed
  the gate genuinely gates: a forbidden enums->nodes, models->services, and services->nodes edge each
  made the corresponding contract BROKEN (exit 1); removing any one of the 7 frozen ignore families made
  exactly one contract BROKEN (each ignore is load-bearing, no padding); a bogus unmatched ignore errors
  (self-cleaning ratchet active, unmatched_ignore_imports_alerting=ERROR). mypy clean (4079 files); test_no_circular_imports
  20 passed. The lint-imports CI job is wired into the quality-gate needs + gate condition, and the pre-commit
  hook is present.
exit_code: 0
pr_number: 1407
branch: "jonah/omn-14216-import-linter-oracle"
contract_sha256: "sha256:fa558fb567db1f5deb100d04ef5ade5e815442d2050e6db4de4d80901ce49842"
