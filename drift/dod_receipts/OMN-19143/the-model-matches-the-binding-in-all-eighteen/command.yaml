---
schema_version: 1.0.0
ticket_id: OMN-19143
evidence_item_id: the-model-matches-the-binding-in-all-eighteen
check_type: command
check_value: |-
  uv run python - <<'PY'
  import pathlib, sys, yaml
  from onex_change_control.validation.ac_criteria import canonical_ac_label

  REQ = "req-transcribed-acceptance-criteria"
  bad: list[str] = []
  seen = 0
  for path in sorted(pathlib.Path("contracts").glob("OMN-*.yaml")):
      data = yaml.safe_load(path.read_text(encoding="utf-8"))
      if not isinstance(data, dict):
          continue
      reqs = [r for r in (data.get("requirements") or []) if isinstance(r, dict)]
      if not any(r.get("id") == REQ for r in reqs):
          continue
      seen += 1
      declared = {
          canonical_ac_label(str(a.get("id")))
          for r in reqs
          for a in (r.get("acceptance") or [])
          if isinstance(a, dict)
      }
      bound = {
          canonical_ac_label(str(e))
          for it in (data.get("dod_evidence") or [])
          if isinstance(it, dict)
          for e in (it.get("binds_ac") or [])
      }
      if not declared or declared != bound:
          bad.append(f"{path.name}: declared={sorted(declared)} binds_ac={sorted(bound)}")
  print(f"{seen} contract(s) carry the transcribed block; {len(bad)} mismatched")
  for line in bad:
      print("MISMATCH", line)
  sys.exit(1 if bad or seen < 18 else 0)
  PY
status: PASS
run_timestamp: '2026-09-22T18:34:46Z'
commit_sha: f379cab82a2f10800f4aaef2453953a8c073894b
contract_entry_sha256: sha256:2c74502e9a51d21e96288634a0f2cfa83fa77a6bfb9db6fd5d5e5bb0d4f1d1bf
runner: |-
  github-actions: Contract Compliance Check, job 106881547359 of run 35767770226, onex_change_control#10759 at f379cab82a -- the job that executed this contract's dod_evidence checks and reported 3/3 PASS, 0 WARN, 0 BLOCK at 18:33:09Z; the identical command was then re-run at the identical commit by the verifier named below
verifier: claude-opus5-lane-dd-occ-reds
probe_command: |-
  uv run python - <<'PY'
  import pathlib, sys, yaml
  from onex_change_control.validation.ac_criteria import canonical_ac_label

  REQ = "req-transcribed-acceptance-criteria"
  bad: list[str] = []
  seen = 0
  for path in sorted(pathlib.Path("contracts").glob("OMN-*.yaml")):
      data = yaml.safe_load(path.read_text(encoding="utf-8"))
      if not isinstance(data, dict):
          continue
      reqs = [r for r in (data.get("requirements") or []) if isinstance(r, dict)]
      if not any(r.get("id") == REQ for r in reqs):
          continue
      seen += 1
      declared = {
          canonical_ac_label(str(a.get("id")))
          for r in reqs
          for a in (r.get("acceptance") or [])
          if isinstance(a, dict)
      }
      bound = {
          canonical_ac_label(str(e))
          for it in (data.get("dod_evidence") or [])
          if isinstance(it, dict)
          for e in (it.get("binds_ac") or [])
      }
      if not declared or declared != bound:
          bad.append(f"{path.name}: declared={sorted(declared)} binds_ac={sorted(bound)}")
  print(f"{seen} contract(s) carry the transcribed block; {len(bad)} mismatched")
  for line in bad:
      print("MISMATCH", line)
  sys.exit(1 if bad or seen < 18 else 0)
  PY
exit_code: 0
pr_number: 10759
probe_stdout: |
  18 contract(s) carry the transcribed block; 0 mismatched
actual_output: |-
  RUNNER AND VERIFIER ARE TWO PARTIES. The runner is the Contract Compliance
  Check job in CI, which executed all three of this contract's checks against
  f379cab82a and reported 3/3 PASS with 0 WARN and 0 BLOCK; its per-check
  `[+] command: Command succeeded` lines were read, not inferred from the
  check-run conclusion. The verifier is this lane, which is neither the lane that
  authored the batch nor the lane that certified its previous revision.

  MEASURED at this commit: 18 contract(s) carry the transcribed block; 0
  mismatched. The floor moved from 25 to 18 in the same commit, because the batch
  was narrowed: seven of the original twenty-five contracts -- OMN-16290,
  OMN-16493, OMN-16535, OMN-17083, OMN-17295, OMN-17304, OMN-17372 -- carry 32
  pre-existing bindings the OMN-18577 proof-class rule refuses, and they were
  restored to their merge-base bytes rather than repaired inside a backfill.

  NEGATIVE CONTROL, RUN, on a copy of the corpus outside the worktree: renaming
  one acceptance id in contracts/OMN-10856.yaml from AC1 to AC9 makes the same
  command exit 1 and print `MISMATCH OMN-10856.yaml: declared=[...AC9] binds_ac=
  [AC1...]`. The check therefore discriminates rather than passing on any input.
  The floor half is controlled too: with fewer than 18 contracts carrying the
  block the command exits 1 on `seen < 18` with no mismatch printed.
