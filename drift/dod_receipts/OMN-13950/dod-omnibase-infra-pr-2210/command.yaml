---
schema_version: "1.0.0"
ticket_id: "OMN-13950"
evidence_item_id: "dod-omnibase-infra-pr-2210"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13950/dod-omnibase-infra-pr-2210/command.yaml"
status: PASS
contract_sha256: "sha256:76308f34808cbc2d62776bbe7a5af856b4a3ce2192b02023ee04eeb2c861a978"
run_timestamp: "2026-07-05T08:11:05Z"
commit_sha: "de40afb91b01f5130645154cb8852593a6991178"
runner: "claude-opus-4-8"
verifier: "claude-opus-4-8-receipt-review-omn-13950"
probe_command: "env -u PYTHONPATH uv run pytest tests/ci/test_ci_workflow_resilience.py -q"
probe_stdout: |
  28 passed. docker-build job structure + integration-test step guards intact. actionlint on
  docker-build.yml shows no structural findings; pre-commit clean on the changed workflow.
  Follow-up runtime smoke stabilization passed focused CI workflow structural tests and pre-commit.
actual_output: >-
  PASS: omnibase_infra PR #2210 switches the docker-build job's buildx to the docker-container driver
  and adds a shared GHCR registry layer cache (cache-from always; cache-to on main push only, mode=max)
  so a cold runner imports unchanged layers instead of re-running the full torch + uv-sync install. It
  also gates the informational continue-on-error docker-integration-tests job off pull_request events
  (post-merge push + workflow_dispatch only), removing an up-to-30-min per-PR self-hosted runner hold
  with no loss of merge gating. The container-driver + registry-cache path is exercised by the PR's own
  docker-build run. Follow-up CI stabilization skips the advisory runtime-boot-smoke reusable compose
  job on pull_request events after repeated runner-network failures where compose services were healthy
  but unreachable from the runner; CI Summary already treats this smoke as advisory and still excludes
  it from hard gating.
exit_code: 0
branch: "jonah/omn-13950-docker-build-optimization"
pr_number: 2210
