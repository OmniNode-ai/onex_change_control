---
schema_version: "1.0.0"
ticket_id: "OMN-18139"
evidence_item_id: "dod-omn18139-ac4-casting-pairing-gate"
check_type: "command"
check_value: |-
  gh api "repos/OmniNode-ai/omnimarket/contents/tests/test_omn18139_tenant_guc_call_site_ratchet.py?ref=02abec5ad75d40dad2b9255ee6473b27b96cbb6a" --jq '.content' | base64 -d | grep -c 'class TestNoUntenantedCallTouchesACastingRelation'
contract_entry_sha256: "sha256:71d3e80f41cfebe62893fb485a45ddb09e3ec0b19e41be46761f09c75ea987ba"
status: PASS
run_timestamp: "2026-09-10T14:15:35Z"
commit_sha: "02abec5ad75d40dad2b9255ee6473b27b96cbb6a"
runner: "delegation-tenant-guc-fix"
verifier: "occ-merged-sha-content-probe"
probe_command: |-
  gh pr view 2445 --repo OmniNode-ai/omnimarket --json state,mergeCommit --jq '[.state,.mergeCommit.oid]|join(" ")'
probe_stdout: |
  MERGED 02abec5ad75d40dad2b9255ee6473b27b96cbb6a
actual_output: |
  PASS. Three declared checks executed against the merged squash commit.

    check 1  the repo-wide pairing gate class present at the merged sha .......... 1
    check 2  the gate's declared blind spot present in the same module ........... 1
    check 3  NEGATIVE CONTROL, module absent at squash parent .................... 0

  Check 3 is a zero whose POSITIVE CONTROL is checks 1 and 2, the same probe shape against
  the same path at the merged sha returning 1 and 1.

  The enumeration this binds: 24 untenanted adapter calls remain in the package at the merged
  sha and none names a relation whose app.tenant_id policy casts to uuid. Exactly two
  relations in the repo carry a casting policy, delegation_events and capability_scores, and
  the set is derived from the migrations rather than hand-kept, so a migration that converts a
  relation an untenanted call already touches turns the gate red.

  Check 2 is deliberate and is why this item does not overclaim: the repo-wide gate matches
  relation NAMES in source text and is blind to a relation interpolated from a variable, which
  is what both fixed sites were. It would not have caught this ticket's own defect. The
  module-scoped gate covers that case. The blind spot is asserted to be documented rather than
  left implicit.

  Binds AC4 only.
exit_code: 0
pr_number: 2445
branch: "jonah/omn-18139-ac-bind"
