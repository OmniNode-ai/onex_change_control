---
schema_version: "1.0.0"
ticket_id: "OMN-18139"
evidence_item_id: "dod-omn18139-ac2-tenant-uuid-never-slug"
check_type: "command"
check_value: |-
  gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_projection_delegation/handlers/handler_delegation.py?ref=02abec5ad75d40dad2b9255ee6473b27b96cbb6a" --jq '.content' | base64 -d | grep -c 'self._last_delegation_write_tenant = tenant'
contract_entry_sha256: "sha256:2ad2c80213a995f1d4ea25e229b7be1d2344fb3cb4a229e58aa2ce870da50a47"
status: PASS
run_timestamp: "2026-09-10T14:15:35Z"
commit_sha: "02abec5ad75d40dad2b9255ee6473b27b96cbb6a"
runner: "delegation-tenant-guc-fix"
verifier: "occ-merged-sha-content-probe"
probe_command: |-
  gh pr view 2445 --repo OmniNode-ai/omnimarket --json state,mergeCommit --jq '[.state,.mergeCommit.oid]|join(" ")'
probe_stdout: |
  MERGED 02abec5ad75d40dad2b9255ee6473b27b96cbb6a
actual_output: |
  PASS. Three declared checks executed against the merged squash commit.

    check 1  the write records the tenant it resolved, at the merged sha ......... 1
    check 2  keyword-only, no-default tenant on the republish signature .......... 1
    check 3  NEGATIVE CONTROL, the recorded-tenant attribute absent at parent .... 0

  Check 3 is a zero and its POSITIVE CONTROL is check 1: the same grep for the same string
  against the same file at the merged sha returns 1. So the zero is the attribute genuinely
  not existing at the parent.

  What check 2 buys beyond review: with the parameter keyword-only and default-less, a caller
  that omits the tenant is a TypeError at the call site rather than a silent bind to the
  adapter's table-less house-slug fallback. That is the property, and it is asserted rather
  than described.

  Binds AC2 only.
exit_code: 0
pr_number: 2445
branch: "jonah/omn-18139-ac-bind"
