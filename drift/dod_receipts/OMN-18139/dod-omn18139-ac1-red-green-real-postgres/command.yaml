---
schema_version: "1.0.0"
ticket_id: "OMN-18139"
evidence_item_id: "dod-omn18139-ac1-red-green-real-postgres"
check_type: "command"
check_value: |-
  gh api "repos/OmniNode-ai/omnimarket/contents/tests/test_omn18139_real_postgres_tenant_guc_representation.py?ref=02abec5ad75d40dad2b9255ee6473b27b96cbb6a" --jq '.content' | base64 -d | grep -c 'invalid input syntax for type uuid'
contract_entry_sha256: "sha256:dd568c120e3920d518ce1855f758193594f3b8bf0e7e2daf25a81a802ac2cade"
status: PASS
run_timestamp: "2026-09-10T14:15:35Z"
commit_sha: "02abec5ad75d40dad2b9255ee6473b27b96cbb6a"
runner: "delegation-tenant-guc-fix"
verifier: "occ-merged-sha-content-probe"
probe_command: |-
  gh pr view 2445 --repo OmniNode-ai/omnimarket --json state,mergeCommit --jq '[.state,.mergeCommit.oid]|join(" ")'
probe_stdout: |
  MERGED 02abec5ad75d40dad2b9255ee6473b27b96cbb6a
actual_output: |
  PASS. All four declared checks executed against the MERGED squash commit
  02abec5ad75d40dad2b9255ee6473b27b96cbb6a (the product branch is deleted, so a head-pinned
  probe would not resolve).

    check 1  the live failure string present in the module at the merged sha ..... 2
    check 2  the role-owned schema construction, which is what makes RLS bind .... 1
    check 3  NEGATIVE CONTROL, module absent at squash parent
             f36822c59b48d8a36114891223f1041391b6ef82 ........................... 0
    check 4  product change MERGED at that exact sha ............................ 1

  Check 3 is a zero and carries its POSITIVE CONTROL as a sibling: checks 1 and 2 are the
  same probe shape against the same paths at the merged sha and return 2 and 1. The zero is
  a measured absence at the parent, not a pattern that matches nothing anywhere.

  THE BEHAVIOURAL PROOF, which these content probes do not themselves carry. Executed on a
  real Postgres 2026-09-10 with the module's own disposable role-owned schema. At the squash
  parent the behavioural test raises asyncpg.exceptions.InvalidTextRepresentationError,
  'invalid input syntax for type uuid: "omninode"', which is the live dead-letter record's
  failure string byte-for-byte (offset 266, 2026-09-10T10:21:18.312Z). At head all thirteen
  tests pass, verified twice: against a populated database and against a freshly created one
  reproducing the CI shape.

  HONEST LIMIT: the OCC receipt runner executed this module in CI as '7 passed, 5 skipped'.
  The five real-Postgres tests skip where no database is provisioned, so that runner receipt
  is a real PASS of the declared command and does NOT carry the behavioural half. The
  behavioural half is the lab execution described above.

  Binds AC1 only. AC5, the live staging readback, is NOT claimed here and is not met at the
  time of writing.
exit_code: 0
pr_number: 2445
branch: "jonah/omn-18139-ac-bind"
