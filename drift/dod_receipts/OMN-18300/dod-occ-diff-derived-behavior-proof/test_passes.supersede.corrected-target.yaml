---
schema_version: 1.0.0
ticket_id: OMN-18300
evidence_item_id: dod-occ-diff-derived-behavior-proof
check_type: test_passes
supersedes: drift/dod_receipts/OMN-18300/dod-occ-diff-derived-behavior-proof/test_passes.yaml
superseder: lane delegation-complexity-ladder
tombstone: false
created_at: '2026-09-13T15:07:41Z'
reason: |-
  THE SUPERSEDED RECEIPT IS A PASS THAT PROVES NOTHING, AND IT MUST NOT BE THE
  BASIS OF THIS PR'S ELIGIBILITY.

  It records the declared check passing at omnimarket commit
  bece5648fad2ac0d11c5aeaf628783be3bb04621. That commit is what made it pass. It
  repointed four benchmark fixtures from importing `subject` -- the name the
  trace generator writes the DEFECTIVE copy of a function to -- at the committed
  subject modules instead. With that change the fixtures import a working
  function and pass trivially. They also stop being able to reproduce the failure
  trace the diagnosis rung is built on, which is the behaviour the check exists to
  prove. A green obtained by removing the subject's ability to fail is the exact
  shape of evidence this gate family exists to refuse.

  The immediately preceding receipt, against the unmodified fixtures, is the
  honest one: four collection errors, exit 2, ModuleNotFoundError on `subject`.
  That is what the declared command does to files that are fixtures rather than a
  suite.

  Both receipts are now moot. The fixtures are renamed to `_proof.py` in the
  product repo so the name cannot invite the reading again, and the contract entry
  in this same companion now declares the PR's real test directory, whose
  test_every_diagnosis_trace_regenerates_from_its_committed_fixtures runs each
  fixture against its committed mutant and asserts the committed trace still comes
  back. That is the behaviour proof, and it has to be earned at the current head
  rather than inherited from a commit that has been superseded.

  This record names the BASE receipt in `supersedes`, not the PASS it is
  correcting: supersession chains fail closed by design, so the base is the only
  valid anchor. The PASS being corrected is
  test_passes.supersede.34762495114.yaml, named here in prose so the chain is
  readable without being asserted as a link.
replacement:
  schema_version: 1.0.0
  ticket_id: OMN-18300
  evidence_item_id: dod-occ-diff-derived-behavior-proof
  check_type: test_passes
  check_value: |-
    uv run pytest benchmarks/delegation_ladder/tests/ -q
    # evidence item: dod-occ-diff-derived-behavior-proof
    # PR binding: gh api repos/OmniNode-ai/omnimarket/pulls/2516 --jq .head.sha
  status: PENDING
  run_timestamp: '2026-09-13T15:07:41Z'
  commit_sha: 44a47ee1e4179c16e2edf69b1a365282061e7746
  pr_number: 2516
  branch: jonah/omn-18300-delegation-complexity-ladder
  runner: occ-receipt-runner
  verifier: lane delegation-complexity-ladder
  exit_code: 0
  actual_output: |-
    PENDING BY DESIGN. The corrected command is owed a real execution by
    occ-receipt-runner in the product repo's CI at head 44a47ee1e4179c16e2edf69b1a365282061e7746, in a real
    checkout. It is deliberately not asserted here: this lane is the one that
    corrected the target, and a lane marking its own corrected check PASS without
    executing it in the product checkout would repeat, in a different way, the
    mistake this record supersedes.
  probe_command: |-
    gh api repos/OmniNode-ai/omnimarket/commits/44a47ee1e4179c16e2edf69b1a365282061e7746 --jq .sha
  probe_stdout: |-
    {"note":"the renamed fixtures and the corrected contract target are both at this head"}
