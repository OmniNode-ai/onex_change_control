---
schema_version: 1.0.0
ticket_id: OMN-15484
evidence_item_id: dod-OmniNode-ai-omnibase-infra-pr-2570-attempt1
check_type: command
check_value: |-
  out="$(gh api 'repos/OmniNode-ai/omnibase_infra/actions/runs/30565261108/attempts/1/jobs' --jq '.jobs[]|select(.name=="merge-hold-gate / evaluate")|.conclusion')" && printf '%s\n' "$out" | grep -qx success
contract_entry_sha256: sha256:6b48df918b2d193d8bb536a1818177211cbcb13ae845971e5dd7e704e1d5960f
status: PASS
run_timestamp: '2026-07-30T20:39:29Z'
commit_sha: 00cf1ad5cfc79357646838dbace5a2c04076b0bc
runner: omn-15484-repin-verifier-cleanup-lane
verifier: executed-probe
probe_command: bash -o pipefail -c 'out="$(gh api ''repos/OmniNode-ai/omnibase_infra/actions/runs/30565261108/attempts/1/jobs''
  --jq ''.jobs[]|select(.name=="merge-hold-gate / evaluate")|.conclusion'')" && printf ''%s\n'' "$out"
  | grep -qx success'
probe_stdout: 'exit:0 (3/3 consecutive GREEN under bash -o pipefail -c; 1/1 under sh -c)'
actual_output: >-
  PASS: leg 1 of the append-only replacement for dod-OmniNode-ai-omnibase-infra-pr-2570, executed live
  against the GitHub Actions API. attempts/1/jobs reports 'merge-hold-gate / evaluate' -> success on omnibase_infra
  run 30565261108. The superseded entry's leg 1 asserted the same fact through the attempt-less /actions/runs/<id>/jobs
  view, which reports only the LATEST attempt; that run is now attempt 2, so the superseded pair was decaying
  evidence even where it still happened to pass. A completed attempt's job records are immutable -- a
  re-run mints attempt N+1 and never rewrites attempt N -- so this binding cannot be flipped by any later
  retry. RED proven (exit 1) under three independent mutations: repointed at attempts/2, a nonexistent
  run id (30565261109), and a nonexistent job name.
exit_code: 0
pr_number: 5663
branch: jonah/omn-15484-supersede-attempt-pinned-red-evidence
