---
schema_version: "1.0.0"
ticket_id: "OMN-13356"
evidence_item_id: "dod-deploy-assessment"
check_type: "command"
check_value: "uv run python scripts/ci/run_runtime_sweep.py  # deploy-equivalent static proof for a node-add;
  NO lane mutation"
status: PASS
run_timestamp: "2026-06-20T16:12:00Z"
commit_sha: "6e25724db16be6ae9e129b251d9768d44293816b"
runner: "claude-omn13356-tool-reuse-worker"
verifier: "claude-receipt-review"
contract_sha256: "sha256:f86d2c9b8b41fadd76ea568b36591cfc75e5bf9c2fb2dd8abec72dddbab321b2"
branch: "jonahgabriel/omn-13356-node-generation-tool-reuse-router-route-a-later-similar"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-13356/omnimarket"
pr_number: 1311
probe_command: "uv run python scripts/ci/run_runtime_sweep.py"
probe_stdout: |
  runtime_sweep: 314/314 entry points OK
actual_output: |
  Deploy assessment: NON-DEPLOYABLE / no-runtime-mutation. PR #1311 adds a new pure
  COMPUTE node only (node_tool_reuse_matcher_compute). It wires no consumer into a
  running runtime and mutates no lane. The node is descriptor.purity=pure with
  validation_contract.llm_allowed=false and performs no I/O beyond an injected
  ProtocolGeneratedToolRegistry query, so there is no deployable side effect to assess
  on a live lane. The orchestrator integration (node_generation_consumer invoking the
  matcher as an upstream gate) and the durable registry projection (consuming
  node-registration events to UPSERT the registry) are explicitly scoped as follow-ups
  and are not part of this PR.
  No prod / judge / stability / dev lane was started, restarted, deployed, or mutated;
  no docker compose, no rpk produce, no psql write was run for this change. The
  deploy-equivalent proof for a node-add is the docker-free runtime sweep, which
  statically verifies every node contract + entry point: it reports 314/314 entry
  points OK, including node_tool_reuse_matcher_compute (the new entry point registered
  in pyproject.toml [project.entry-points."onex.nodes"]). mypy --strict and the
  golden-chain coverage gate further bound the node's behavior deterministically.
  Verifier (claude-receipt-review, distinct from runner) re-ran the sweep and confirmed
  314/314; probe_stdout above is the verifier's captured output.
exit_code: 0
duration_ms: 0
