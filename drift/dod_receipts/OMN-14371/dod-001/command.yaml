---
schema_version: "1.0.0"
ticket_id: "OMN-14371"
evidence_item_id: "dod-001"
check_type: "command"
check_value: >-
  grep -q '^status: PASS$' drift/dod_receipts/OMN-14371/dod-001/command.yaml && grep -q '^pr_number: 1714$'
  drift/dod_receipts/OMN-14371/dod-001/command.yaml && grep -q 'c4af019a4c42e1f07be03410b16bd6c97e553206'
  drift/dod_receipts/OMN-14371/dod-001/command.yaml
status: PASS
run_timestamp: "2026-07-11T09:25:00Z"
commit_sha: c4af019a4c42e1f07be03410b16bd6c97e553206
runner: "build-canary"
verifier: "verify-canary"
pr_number: 1714
probe_command: >-
  In an independent worktree off #1714 head c4af019a: (1) env -u PYTHONPATH uv run pytest test_projection_replay_check_golden_equivalence.py;
  (2) replay all 7 committed goldens through the LEGACY check() from origin/dev via scripts.ci.compute_golden.compare_output;
  (3) classify the node with omnibase_core/scripts/ci/canonical_handler_shape.py repointed at the #1714
  src and the committed baseline; (4) regenerate the ModelAdequacyReceipt with scripts.ci.adequacy_receipt.build_receipt
  over the 7 goldens and diff branch_coverage_pct vs the committed receipt; (5) drive canonical_handler_shape.verify_flip_receipt
  with the LEGACY baseline (target treated as a real flip), plus a tampered-receipt negative control (pct
  lowered to 10.0, waiver removed); (6) env -u PYTHONPATH uv run pytest the full node unit + bus-coverage
  integration suites.
probe_stdout: |
  equivalence(new handle): 8 passed — 7 goldens replay byte-equivalent through handle() (compare_output == [] for all 7) + fixture-count guard == 7
  equivalence(legacy check, origin/dev): ALL 7 goldens ZERO-DIFF through the legacy check() method -> the goldens are genuine pre-rename recordings, not fabricated to match handle()
  volatile_mask == [] on all 7 goldens -> full-field structural comparison (no masking); compare_output is a real recursive diff_paths, not a no-op
  classifier(#1714 src): omnimarket.nodes.node_projection_replay_check_compute -> is_canonical=True category=canonical detail=magic:request (legacy classified is_canonical=False detail="no handle(); op-methods=['check']")
  baseline diff = exactly 1 deletion / 0 insertions (this node removed); node_hybrid_codegen_orchestrator new_non_canonical is byte-identical in legacy and PR-head (pre-existing OMN-14372 gap), neither added nor removed by #1714
  adequacy regen: branch_coverage_pct=80.0 selected_count=2 candidate_count=7 meets_target=True (pct independently reproduced via coverage.py; committed selected inputs map to real goldens mixed_batch + single_occurrence)
  handler_module_sha256 recorded == live file: sha256:e9cd9365eb1c2f8ffef4e6645473daf51d4337589f013ee72e9d8e1462b2bf18
  verify_flip_receipt(LEGACY baseline, committed receipts): ok=True reason=recomputed-meets-target+equivalence-pass; target in proven_flips, unproven_flips empty
  tamper negative-control (branch_coverage_pct -> 10.0, waiver removed): verify_flip_receipt ok=False reason="adequacy: recomputed below target (10.0 < 80.0) and no waiver" -> gate is not a rubber stamp
  node suite: 32 passed (17 legacy unit via handle() + 8 golden-equivalence + 7 bus-coverage integration driving HandlerProjectionReplayCheck() directly, shim retired) + 5 adequacy_receipt unit tests
actual_output: >-
  PASS: independent re-derivation at product head c4af019a4c42e1f07be03410b16bd6c97e553206 confirms the
  check()->handle() rename is behavior-preserving and the canonical-shape flip is real, scoped, and proof-backed.
  All 7 disk goldens replay zero-diff through BOTH the new handle() and the legacy check() under an empty
  volatile mask (genuine recordings, full-field compare). The codemod is method-name-only (signature byte-identical,
  contract.yaml untouched); the dead _ReplayCheckBusHandler shim was removed and the 7 bus-coverage tests
  now drive the real handler directly. The node flips to category=canonical/detail=magic:request; the
  baseline diff removes exactly this one node (1 deletion, 0 insertions) and the pre-existing node_hybrid_codegen_orchestrator
  gap (OMN-14372) is byte-identical in legacy and PR-head, untouched. The adequacy receipt binds to the
  live handler sha256, its 80.0% branch coverage reproduces independently via coverage.py, the canonical-shape
  gate re-derives the verdict and accepts the flip as proven-flip, and a tampered receipt is rejected.
  runner (build-canary) and verifier (verify-canary) are distinct agent identities, so this receipt is
  an independent PASS.
exit_code: 0
branch: "jonah/omn-14371-canary-projection-replay"
contract_sha256: "sha256:eb039d6b6af75dcc096d6e08a35637915c643a51793bc9523d421ed6de8d02a8"
