---
schema_version: "1.0.0"
ticket_id: OMN-7835
evidence_item_id: dod-001
check_type: command
check_value: "gh pr view 1021 --repo OmniNode-ai/omnibase_core --json state --jq '.state'"
status: PASS
run_timestamp: 2026-05-01T12:00:00+00:00
commit_sha: 368f4e7e
runner: omn-7835-local-runner
verifier: jonahgabriel
probe_command: git -C "$OMNI_HOME/omni_worktrees/OMN-7835/omnibase_core" diff HEAD~1 -- src/omnibase_core/enums/enum_llm_provider.py
probe_stdout: |
  -    def requires_api_key(self) -> bool:
  -        """Check if this provider requires an API key."""
  -        return self in {self.CLAUDE, self.OPENAI, self.GEMINI, self.ANTHROPIC}
  +    def requires_api_key(self) -> bool:
  +        """Check if this provider requires an API key.
  +
  +        Per OMN-7467/OMN-7835, all frontier providers use SSO/OAuth.
  +        Only GLM (Zhipu AI) requires an explicit API key, but GLM is
  +        not represented in this enum. All members here use SSO/OAuth.
  +        """
  +        return False
actual_output: >-
  PASS: requires_api_key() returns False for all EnumLlmProvider members. ModelAgentConfig.api_key changed
  from required str to optional str | None = Field(default=None). 15/15 enum tests pass, mypy strict clean.
exit_code: 0
pr_number: 1021
