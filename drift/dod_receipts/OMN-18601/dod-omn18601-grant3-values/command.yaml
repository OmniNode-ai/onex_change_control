---
schema_version: "1.0.0"
ticket_id: OMN-18601
evidence_item_id: dod-omn18601-grant3-values
check_type: command
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-grant3-values/command.yaml"
contract_sha256: "sha256:1a290068b1c1ed7661260d3681882ed10bc5b2a97232b54f07d3d2da4331d0ca"
contract_entry_sha256: "sha256:7cee44c30b932692a1b60a60b9bb697841a23537f7984f1b2c0e459bca9d74ae"
status: PASS
run_timestamp: '2026-09-21T08:36:00Z'
commit_sha: c130a1a3b450a5bf8e66563a43e14ce75f7d4e74
pr_number: 10647
runner: omn18945-18946-gates-0425
verifier: grep
probe_command: |-
  # The item's own chain, run against BOTH trees. <file> = (1) the registry this PR produces,
  # (2) onex_change_control@main a7b185cb2, where grant-aeae215d is still the live entry.
  for v in grant-823154a7-... 'manifest_ref: b828ec9e...' 'rendered_digest: sha256:976ef4de...' \
           'manifest_subtree: k8s/keycloak-seed'; do grep -Fq "$v" <file>; done
  ! grep -Fq 'grant-aeae215d-...' <file>     # the superseded entry must be GONE
  #
  # And the digest, RECOMPUTED at the new ref rather than carried forward, in a checkout of
  # repos/OmniNode-ai/omninode_infra detached at b828ec9e:
  python3 -c "... manifest_promotion_render.rendered_digest(load_documents(kubectl kustomize ...))"
probe_stdout: |-
  # staged tree vs onex_change_control@main a7b185cb2
    grant-823154a7-18ab-...        staged exit=0   main exit=1
    manifest_ref: b828ec9e...      staged exit=0   main exit=1
    rendered_digest: sha256:976... staged exit=0   main exit=0   <- SHARED, see below
    manifest_subtree: k8s/...      staged exit=0   main exit=0   <- SHARED, see below
    ! grant-aeae215d-...           staged exit=0   main exit=1

  # the digest recomputed at b828ec9e
  objects: 3
  rendered_digest: sha256:976ef4de626144f156a6a227c3b881066e07273476a5f19f00b96e6e454fad06
  guard present at this sha: 17 references to OMN-15812   (was 0 at ad45e2cc)
actual_output: |-
  PASS. The registry carries grant-823154a7-18ab-4d54-8d3c-3a2e859d1f87 naming manifest_ref
  b828ec9e65f873149c97d94cbefa9133b9574e5d, and the superseded grant-aeae215d is gone.

  WHAT THE CONTROL DOES AND DOES NOT SEPARATE, said precisely because two of the five greps match on BOTH
  trees. rendered_digest and manifest_subtree are IDENTICAL between the two entries, so those two clauses
  cannot distinguish them and are not claimed to: they assert the fields are present and correct, nothing
  more. The discriminating clauses are the grant id, the manifest_ref, and the negated absence of the old
  id, and all three invert between the trees. A chain whose every clause matched on main would have proven
  only that the file mentions some grant.

  ONLY ONE FIELD MOVED, and it was recomputed rather than assumed. manifest_ref goes ad45e2cc -> b828ec9e
  because omninode_infra#1583 moved main; rendered_digest was recomputed at b828ec9e with that repository's
  own scripts/manifest_promotion_render.py and came back EQUAL to the previous cycle's value, which is the
  expected result and worth checking rather than asserting: #1583 edited .github/workflows/deploy-onex-prod.yml
  and touched nothing under k8s/keycloak-seed. path, subtree, lane, approver and digest are unchanged.

  WHY THE REF HAD TO MOVE AT ALL. The gate reads manifest_ref from its own checkout with `git rev-parse
  HEAD`, so an entry naming ad45e2cc would be refused the moment main advanced past it — regardless of
  whether the tree it names still renders correctly.
  CHECK CONVERTED FROM A STRING CHAIN TO A STRUCTURAL PARSE, after the string form FAILED on this PR's
  own first CI run. Its last clause was `! grep -Fq 'grant-aeae215d…'`, asserting the superseded entry was
  gone — and the registry's supersession header names that id ON PURPOSE, so the absence-grep matched a
  COMMENT and reported a false failure. Measured rather than guessed: the old id appears twice in the file,
  at lines 672 and 712, both comment lines, while `entries` contains exactly one entry, the new grant.

  THE WARNING WAS ALREADY IN THIS FILE. dod-omn18601-grant-revoked's description says the check
  "deliberately does NOT grep for the absence of the grant id, because the revocation header above names it
  on purpose and such a check would match the comment and report a false failure -- the shape OCC#8670 used
  is unsafe here for exactly that reason." That text was read earlier in this same session and the unsafe
  shape was written anyway, one item away from it.

  WHAT THE REPLACEMENT ASSERTS, and why it is stronger rather than merely different. It parses the registry
  and tests a property of the `entries` LIST: exactly one entry, whose grant_id, manifest_ref,
  rendered_digest, manifest_subtree, runtime_lane and target_kind all match. Comments cannot affect it, and
  it additionally pins the entry COUNT, which the string chain never did — a second, unrelated entry could
  have satisfied every grep in the old form.

  RE-MINTED 2026-09-21 by lane omn18945-18946-gates-0425 in OCC#10647. EVERY WORD ABOVE IS THE ORIGINAL
  EVIDENCE, authored by runner occ-grant3-staging-lane and verifier occ-grant3-staging-verifier at
  commit a7b185cb2 on 2026-09-19T20:26:10Z, and is preserved verbatim. Nothing recorded there is
  withdrawn or re-judged here.

  WHAT CHANGED AND WHY. The item's check asserted a TRANSIENT state of the live registry -- that
  grant-823154a7 is present with those six field values. That grant expired unconsumed at
  2026-09-20T20:23:15Z and OCC#10647 prunes it, so the assertion can never hold again and its failure
  turned every main-targeting pull request in this repository red. The check is re-pointed at this
  frozen receipt, which is what OCC#10302 did to dod-omn18601-grant2-values when the previous grant on
  this same ticket was pruned. Re-pointing mutates the dod_evidence entry, which is why the per-entry
  hash had to be re-minted and the probe re-run: sha256:2aa12152... -> sha256:7cee44c3..., the value
  occ-preflight itself recomputed and named.

  THE NEW PROBE, run at re-mint time: `grep -q '^status: PASS$' <this file>` -> exit 0.

  THE HONEST LIMIT, stated because the new check is self-referential. It asserts only that this receipt
  records a PASS. The substance -- the six field values, the two clauses that do and do not discriminate
  between the trees, and the digest recomputed at b828ec9e -- is the original evidence above, verified
  when the grant was live. What the check no longer asserts is that the grant is still live, which is
  correct, because it is not.
