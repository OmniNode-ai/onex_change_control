---
schema_version: "1.0.0"
ticket_id: OMN-18601
evidence_item_id: dod-omn18601-grant2-values
check_type: command
check_value: |-
  grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-omn18601-grant2-values/command.yaml
contract_sha256: "sha256:b3c70360826c66540efcbd174f37aa5c16bd1a068e00b464353aa5409e6448c4"
contract_entry_sha256: "sha256:ddb9653bb1d1c8ac9c2703a9c9f079fe01f05b3997b99a2f43cc658cc7007a7e"
status: PASS
run_timestamp: '2026-09-19T17:52:40Z'
commit_sha: 16a3db81400f7d7f1096ebdb46c56a90f8b14883
pr_number: 10454
runner: occ-grant2-staging-lane
verifier: occ-grant2-staging-verifier
probe_command: |-
  # The item's own six-grep chain plus the at-rest check, run against BOTH trees.
  # <file> = (1) the staged registry this PR produces, (2) onex_change_control@main 16a3db814
  for v in grant-aeae215d-... 'target_kind: manifest' 'manifest_path: ...' \
           'manifest_subtree: ...' 'manifest_ref: ad45e2cc...' 'rendered_digest: sha256:976ef4de...'; do
    grep -Fq "$v" <file>; done
  grep -Eq '^entries: \[\]$' <file>
probe_stdout: |-
  # Each of the six target-field strings, staged tree vs main.
    grant-aeae215d-d796-...     staged exit=0   main exit=1
    target_kind: manifest       staged exit=0   main exit=1
    manifest_path: k8s/...      staged exit=0   main exit=1
    manifest_subtree: k8s/...   staged exit=0   main exit=1
    manifest_ref: ad45e2cc...   staged exit=0   main exit=1
    rendered_digest: sha256:976ef4de...  staged exit=0   main exit=1

  # The at-rest check, which must invert between the two trees.
    main   'entries: []'  exit=0   POSITIVE CONTROL: main is at rest after OCC#10302
    staged 'entries: []'  exit=1   the replacement entry is present
actual_output: |-
  PASS, and the control is the half that makes it a measurement. All six target-field strings are present
  in the staged registry and absent from onex_change_control@main at 16a3db814, and the at-rest assertion
  inverts in the same direction. A chain broken by a mistyped grant id or a grep that never matched would
  have exited non-zero on BOTH trees; it did not.

  WHAT IS ADDED: exactly one entry, grant-aeae215d-d796-4141-a0a9-7b5fd0c22055, target_kind manifest,
  authorizing k8s/keycloak-seed/overlays/prod at omninode_infra ad45e2cc into onex-prod. No image_digest
  field is present, because the two kinds' target fields are mutually exclusive.

  HOW IT DIFFERS FROM THE REVOKED grant-90f8e729, in exactly two fields: manifest_ref moves 8279e2ca ->
  ad45e2cc and rendered_digest moves sha256:33b54afe -> sha256:976ef4de. Both moves trace to one file,
  k8s/keycloak-seed/base/seed-keycloak-clients-job.yaml, which now carries the securityContext production
  requires. The path, the subtree, the lane and the approver are unchanged.

  CHECK RE-POINTED AT THIS RECEIPT BY OCC#10471, with the measurement above unchanged. grant-aeae215d is
  superseded by grant-823154a7 because omninode_infra#1583 moved main ad45e2cc -> b828ec9e, and the gate
  reads manifest_ref from its own checkout, so the old ref would be refused on staleness alone. The
  six-grep chain therefore has nothing left to match. What this records is unchanged and still exact: the
  staged state of grant-aeae215d WAS measured, at the commit named above, with its main-tree control
  inverting in the same direction.

  WHY THE ENTRY WAS REMOVED RATHER THAN MARKED consumed. It was consumed in substance by run 35465514637,
  which passed every identity premise, applied two of three objects, and could not apply the Job because
  a batch/v1 pod template is immutable and onex-prod still held the inert Job from 2026-09-18. Leaving the
  entry would also have tripped the OMN-13424 at-rest invariant at its 2026-09-20T17:44:56Z expiry.
