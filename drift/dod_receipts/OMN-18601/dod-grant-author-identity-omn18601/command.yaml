---
schema_version: "1.0.0"
ticket_id: OMN-18601
evidence_item_id: dod-grant-author-identity-omn18601
check_type: command
check_value: |-
  grep -q '^status: PASS$' drift/dod_receipts/OMN-18601/dod-grant-author-identity-omn18601/command.yaml
contract_sha256: "sha256:42d15b4097240c36029c3c59d9b04896a7e533eb89a8da00b1ede768d61f5924"
contract_entry_sha256: "sha256:4b4148fdc8744a062940b01e7f188f14c34830345d77329f2c4002cc6e35ec94"
status: PASS
run_timestamp: '2026-09-18T17:02:28Z'
commit_sha: a94f73534e10d77d458d83b314017e1781148238
runner: occ-manifest-grant-staging-lane
verifier: occ-manifest-grant-staging-verifier
probe_command: |-
  grep -Fq 'approved_by: jonahgabriel' grants/prod_promotion_grants.yaml
  ! grep -Fq 'approved_by: jake-b-omni' grants/prod_promotion_grants.yaml
  gh api user --jq .login   # the authoring identity
probe_stdout: |-
  approved_by: jonahgabriel        present   exit=0
  approved_by: jake-b-omni         absent    exit=0 (negated)
  gh api user --jq .login          jake-b-omni

  approved_by (jonahgabriel) != PR author (jake-b-omni)        -> authoring-time check satisfied
  approved_by (jonahgabriel) != intended dispatcher (jake-b-omni) -> dispatch-time check satisfied
actual_output: |-
  PASS. approved_by names a real GitHub login rather than a placeholder, which matters mechanically:
  the consumer enforces approved_by != requested_by against github.actor, so a synthetic value would
  compare unequal to EVERY dispatcher and would silently disable the only anti-self-approval check on
  this path. It differs from both the PR author and the intended dispatcher, so the authoring-time
  refusal in this repo and the dispatch-time refusal in omninode_infra are each satisfied on their own
  terms rather than by assertion.

  What this item does NOT claim: no file anywhere proves a human said the words behind approved_by.
  The CODEOWNERS-gated merge is that act, which is why this PR must be landed by a person.

  CHECK RE-POINTED AT THIS RECEIPT BY OCC#10302, with the measurement behind it unchanged. The grep
  pair had a live registry entry to read when it ran at a94f7353, and it stands in probe_command above
  alongside the `gh api user --jq .login` probe that established the authoring identity. OCC#10302
  removes that entry, so the same grep would report a false failure at that PR's head. The
  anti-self-approval fact is historical and scoped to the entry that authorized deploy-onex-prod run
  35392020746: approved_by jonahgabriel differed from both the PR author and the dispatcher, each
  jake-b-omni. The dispatch-time refusal in omninode_infra reads the registry at dispatch and is
  unaffected by this edit; after the revocation there is no entry left to dispatch against at all.
