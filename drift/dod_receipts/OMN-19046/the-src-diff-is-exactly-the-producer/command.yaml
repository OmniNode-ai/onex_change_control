---
schema_version: 1.0.0
ticket_id: OMN-19046
evidence_item_id: the-src-diff-is-exactly-the-producer
check_type: command
check_value: |-
  test "$(git diff --name-only origin/dev...HEAD -- src/ scripts/ .github/ | sort | paste -sd, -)" = "src/onex_change_control/scripts/backfill_ac_requirements.py,src/onex_change_control/serialization/ac_requirements.py"
status: PASS
run_timestamp: '2026-09-22T13:08:12Z'
commit_sha: 4b976479aec29c9337395d13a493839e2c07d14e
contract_entry_sha256: sha256:4fb1b049de025c8010f44cdae6f85ad65eacf77d581bfa97920f99a9984e6ff0
runner: 'onex_change_control.scripts.contract_compliance_check --pr 10756 --repo OmniNode-ai/onex_change_control,
  run against this head: it executed every dod_evidence check this contract declares and reported 6/7
  PASS, 1 WARN (the superseded item), 0 BLOCK; the identical check_value was then re-run on its own by
  the verifier named below'
verifier: claude-opus5-lane-omn19038-second-actor-2
probe_command: |-
  git diff --name-only origin/dev...HEAD -- src/ scripts/ .github/ | sort | paste -sd, -
exit_code: 0
pr_number: 10756
probe_stdout: |
  src/onex_change_control/scripts/backfill_ac_requirements.py,src/onex_change_control/serialization/ac_requirements.py
actual_output: |-
  RUNNER AND VERIFIER ARE TWO PARTIES, as above.

  check_value is an equality assertion and prints nothing on success, which would
  leave this receipt with an empty stdout that the receipt-hardening gate
  correctly refuses as indistinguishable from a probe that never ran. The
  probe_command above is the left-hand side of that same equality on its own, so
  the stdout recorded here is the exact string the assertion compares. The bare
  check_value was ALSO run at this same head and exited 0.

  MEASURED: the sorted set of paths this branch changes under src/, scripts/ and
  .github/ is exactly the two producer modules, and the assertion comparing it to
  that literal pair exits 0.

  NEGATIVE CONTROLS, BOTH RUN. Removing either expected path from the comparison
  string makes the same command exit 1, and adding a third path makes it exit 1.
  The equality is doing the work, not a substring match, which is the failure a
  count-based version of this check would have had.

  WHY THIS ITEM EXISTS AT ALL, restated so the receipt is readable without the
  contract: it supersedes no-source-file-is-touched-by-this-change, whose claim
  that this change touches no source file is false at this head and whose check
  exits 1. That item was written for the earlier change on this ticket and was
  true of it. The blast-radius half of its claim survives and is what this item
  asserts; the privileged-path half does not survive and is not claimed.
