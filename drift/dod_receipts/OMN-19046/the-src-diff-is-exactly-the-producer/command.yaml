---
schema_version: 1.0.0
ticket_id: OMN-19046
evidence_item_id: the-src-diff-is-exactly-the-producer
check_type: command
check_value: |-
  test "$(gh api repos/OmniNode-ai/onex_change_control/pulls/10756/files --paginate --jq '.[].filename' | grep -E '^(src|scripts|[.]github)/' | sort | paste -sd, -)" = "src/onex_change_control/scripts/backfill_ac_requirements.py,src/onex_change_control/serialization/ac_requirements.py"
status: PASS
run_timestamp: '2026-09-22T13:08:12Z'
commit_sha: 441cce0f0e0ea5ec0b72a2cf09056ae105d7d7f6
contract_entry_sha256: sha256:e1763f82ccf7d4c687f0d60d7e4db2f3e3dd63fadca43714a1e4e53c23e96196
runner: 'onex_change_control.scripts.contract_compliance_check --pr 10756 --repo OmniNode-ai/onex_change_control,
  run against this head: it executed every dod_evidence check this contract declares and reported 6/7
  PASS, 1 WARN (the superseded item), 0 BLOCK; the identical check_value was then re-run on its own by
  the verifier named below'
verifier: claude-opus5-lane-omn19038-second-actor-2
probe_command: |-
  gh api repos/OmniNode-ai/onex_change_control/pulls/10756/files --paginate --jq '.[].filename' | grep -E '^(src|scripts|[.]github)/' | sort | paste -sd, -
exit_code: 0
pr_number: 10756
probe_stdout: |
  src/onex_change_control/scripts/backfill_ac_requirements.py,src/onex_change_control/serialization/ac_requirements.py
actual_output: |-
  RUNNER AND VERIFIER ARE TWO PARTIES. The runner is the contract-compliance
  runner, which executed every check this contract declares at this head and
  reported 6/7 PASS, 1 WARN (the superseded item), 0 BLOCK. The verifier is the
  certifying second actor, which re-ran the identical command on its own.

  check_value is an equality assertion and prints nothing on success, which would
  leave this receipt with an empty stdout that the receipt-hardening gate
  correctly refuses as indistinguishable from a probe that never ran. The
  probe_command above is the left-hand side of that same equality on its own, so
  the stdout recorded here is the exact string the assertion compares. The bare
  check_value was ALSO run and exited 0.

  MEASURED: the pull request's changed-file list, filtered to src/, scripts/ and
  .github/ and sorted, is exactly the two producer modules, and the assertion
  comparing it to that literal pair exits 0.

  NEGATIVE CONTROLS, BOTH RUN AGAINST THIS FORM. Removing either expected path
  from the comparison string makes the same command exit 1; adding a third path
  makes it exit 1. The equality is doing the work, not a substring match.

  WHY THE LIST COMES FROM THE FORGE, and why this receipt supersedes an earlier
  one of mine rather than standing alone. The first form of this check read the
  same set with `git diff --name-only origin/dev...HEAD`. It is green locally and
  in CI it prints `fatal: origin/dev...HEAD: no merge base` and exits 1, because
  the runner's checkout is shallow and that merge base does not exist there. It
  was not measuring the property; it was erroring, and the exit code looked like
  a verdict. The item this one supersedes carries the same latent defect, so its
  red in CI was never evidence about the source diff either. The forge's
  pull-request file list needs no history depth and cannot be emptied by clone
  depth.

  ON commit_sha. It names the pushed parent commit rather than the commit
  carrying the contract edit, because that edit is what re-hashed this entry and
  could not be pushed before the gate it feeds accepted a receipt for it. The
  distinction costs this receipt nothing: the check reads the pull request's own
  file list from the forge and does not read the working tree at all, so its
  result is identical at either commit, and it was measured at both.
