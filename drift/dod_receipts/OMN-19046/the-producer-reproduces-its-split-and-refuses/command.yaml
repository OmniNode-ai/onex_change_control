---
schema_version: 1.0.0
ticket_id: OMN-19046
evidence_item_id: the-producer-reproduces-its-split-and-refuses
check_type: command
check_value: |-
  uv run pytest tests/test_omn_19046_ac_requirements_backfill.py::TestItRefusesRatherThanGuess tests/test_omn_19046_ac_requirements_backfill.py::TestItIsIdempotentThroughTheReader tests/test_omn_19046_ac_requirements_backfill.py::TestItDoesNotOverwriteAnAuthoredModel -q
status: PASS
run_timestamp: '2026-09-22T13:08:12Z'
commit_sha: 4b976479aec29c9337395d13a493839e2c07d14e
contract_entry_sha256: sha256:94df17a52ac4967a32d771de8197c1223f1a5ccceb8bd5cb3a88ed39f1b6250a
runner: 'onex_change_control.scripts.contract_compliance_check --pr 10756 --repo OmniNode-ai/onex_change_control,
  run against this head: it executed every dod_evidence check this contract declares and reported 6/7
  PASS, 1 WARN (the superseded item), 0 BLOCK; the identical check_value was then re-run on its own by
  the verifier named below'
verifier: claude-opus5-lane-omn19038-second-actor-2
probe_command: |-
  uv run pytest tests/test_omn_19046_ac_requirements_backfill.py::TestItRefusesRatherThanGuess tests/test_omn_19046_ac_requirements_backfill.py::TestItIsIdempotentThroughTheReader tests/test_omn_19046_ac_requirements_backfill.py::TestItDoesNotOverwriteAnAuthoredModel -q
exit_code: 0
pr_number: 10756
probe_stdout: |
  ...........                                                              [100%]
  11 passed in 0.02s
actual_output: |-
  RUNNER AND VERIFIER ARE TWO PARTIES. The runner is the contract-compliance
  runner, which executed this item's check against this head as part of
  evaluating the whole contract. The verifier is the certifying second actor,
  which is NOT the lane that built the producer, and which re-ran the identical
  command on its own and separately reproduced the corpus measurement the item
  describes.

  THE COMMITTED CHECK: 11 passed, exit 0. It executes the producer's refusal,
  idempotence and no-overwrite behaviour over the fixtures in this same change.

  POSITIVE CONTROL, RUN, because eleven green tests prove nothing until one of
  them can go red. The three refusal raise sites in
  src/onex_change_control/serialization/ac_requirements.py were replaced with a
  no-op in the working tree and the same selection re-run: 4 failed, 7 passed,
  each failure naming the rule it lost -- the bound label the body lacks, the two
  criteria collapsing to one label, the pin that arbitrates a duplicate, and the
  stale pin. The tree was then restored from HEAD with a path-scoped checkout.

  A SECOND, WEAKER CONTROL IS RECORDED BECAUSE IT DID NOT FIRE, and a control
  that fails to fire is worth more in the record than out of it: renaming the
  rule-name constant alone left all tests green, because the assertions import
  that same constant rather than spelling the rule string. The rule NAMES are
  therefore pinned only self-referentially; the rule BEHAVIOUR is pinned for
  real, which is what the first control shows.

  THE CORPUS MEASUREMENT BEHIND THE CLAIM, run by the verifier against ticket
  bodies fetched from the tracker on 2026-09-22 for every bound contract at this
  head: 361 changed, 4 unchanged, 8 refused, reproducing the producing lane's
  reported split exactly under that lane's own file selection. Two surrounding
  figures did not reproduce and are corrected in the item description rather than
  here: the denominator is 373 files carrying a binds_ac key, not 374, and only
  370 of those carry a non-empty one, so under the stricter reading the split
  reads 361 fill, 1 agree, 8 refuse. Each of the 8 refusals was corroborated
  through the repository's own criterion reader and hash function rather than
  through the producer's planner.
