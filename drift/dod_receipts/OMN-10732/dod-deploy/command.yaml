---
schema_version: "1.0.0"
ticket_id: OMN-10732
evidence_item_id: dod-deploy
check_type: command
check_value: "echo 'no deploy required: Pydantic model field defaults evaluated at construction time,
  not at module import or service startup'"
status: PASS
run_timestamp: "2026-05-09T15:15:00Z"
commit_sha: "7640293823b3abcc20ff6f33e99b27758b9f91f3"
runner: local-macos
verifier: jonahgabriel
probe_command: "echo 'no deploy required: Pydantic model field defaults evaluated at construction time,
  not at module import or service startup'"
probe_stdout: |
  no deploy required: Pydantic model field defaults evaluated at construction time, not at module import or service startup
actual_output: "PASS: No runtime deploy required. This change modifies Pydantic model field defaults (replacing
  os.environ.get(KEY, 'localhost') with os.environ[KEY]) and annotates 7 bind-address defaults with #
  fallback-ok. Pydantic default_factory lambdas execute at object construction time — the running services
  (omninode-runtime, agent-actions-consumer, etc.) always pass explicit values from environment variables
  at construction time; they do not rely on the field defaults. No container rebuild, Docker image push,
  or service restart is needed. The validator script and pre-commit hook changes take effect on next pre-commit
  install, which is a developer-side operation."
exit_code: 0
pr_number: 1543
branch: jonah/omn-10732-fallback-sweep-infra
