---
schema_version: "1.0.0"
ticket_id: "OMN-16843"
evidence_item_id: "dod-omn16843-compose-internal-dsn"
check_type: "command"
check_value: |-
  body="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.infra.yml?ref=47f3758ba0b10023a03211455b32ae7b6c6bd9df" --jq '.content' | base64 -d)" && grep -qF 'OMNINODE_INTERNAL_DB_URL: "postgresql://omninode_runtime:' <<<"$body"
contract_sha256: "sha256:375a93bc424b5530769273e4758fddd160b2d45b7ea2e537a09c5f9db713d209"
contract_entry_sha256: "sha256:530d64bdb9efb00a78cae97bce9ec4ee1b271e61f40860aefbf36ba9fa588d85"
status: PASS
run_timestamp: "2026-08-28T15:53:59.100806+00:00"
commit_sha: "47f3758ba0b10023a03211455b32ae7b6c6bd9df"
runner: "human-operated bash run in the OMN-16843 onex_change_control worktree"
verifier: "omn16843-compose-lane-internal-dsn lane"
probe_command: |-
  # The commit_sha above resolves in the PRODUCT repo, OmniNode-ai/omnibase_infra
  # (PR #2968 head), not in onex_change_control:
  #   gh api repos/OmniNode-ai/omnibase_infra/commits/47f3758ba0b10023a03211455b32ae7b6c6bd9df
  #
  # The declared check uses `grep -q`, which is silent by design. This probe is
  # the INDEPENDENT read: it prints the matching line(s) at the fix SHA and then
  # re-runs the same grep at the parent SHA ccaff4181 as a RED control, so the
  # receipt carries evidence the predicate discriminates rather than a bare exit
  # code that a always-true predicate would also produce.
  gh api "repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.infra.yml?ref=47f3758ba0b10023a03211455b32ae7b6c6bd9df" --jq '.content' | base64 -d | grep -nF 'OMNINODE_INTERNAL_DB_URL: "postgresql://omninode_runtime:' | head -2; echo "MATCHES_AT_FIX=$?"; gh api "repos/OmniNode-ai/omnibase_infra/contents/docker/docker-compose.infra.yml?ref=ccaff4181" --jq '.content' | base64 -d | grep -cF 'OMNINODE_INTERNAL_DB_URL: "postgresql://omninode_runtime:'; echo "PARENT_GREP_EXIT=$?"
probe_stdout: |
  297:  OMNINODE_INTERNAL_DB_URL: "postgresql://omninode_runtime:${OMNINODE_RUNTIME_PASSWORD:?OMNINODE_RUNTIME_PASSWORD must be set — the internal projections connect to omnidash_analytics as the non-BYPASSRLS omninode_runtime principal (OMN-16843), never as the postgres superuser}@postgres:5432/omnidash_analytics"
  MATCHES_AT_FIX=0
  0
  PARENT_GREP_EXIT=1
actual_output: |
  PASS: the declared check_value was executed verbatim under
  `bash -o pipefail -c` - the same interpreter
  onex_change_control/src/onex_change_control/scripts/contract_compliance_check.py
  uses - and exited 0.

  What it proves: the base compose file's x-runtime-env anchor now declares OMNINODE_INTERNAL_DB_URL, so the five runtime services the dev, stability-test and prod lanes layer on top of it resolve the third DSN of the omninode_runtime_service binding instead of two of three.

  Falsifiability, proven by execution rather than asserted. The SAME check_value
  with only the ref swapped to the parent commit ccaff4181 exits
  1 (RED). Before this fix OMNINODE_INTERNAL_DB_URL appeared in
  ZERO omnibase_infra compose files, so the predicate genuinely discriminates
  between the pre-fix and post-fix trees rather than passing unconditionally.

  Why this leg is a repository read and not a live .201 probe. The first draft of
  this contract expressed the deploy-scope legs as `docker exec` / `docker logs` /
  `curl` probes against the dev lane. Contract Compliance Check executes every
  check_value on a hosted GitHub runner, where no such container or endpoint
  exists, so all four returned BLOCK (0/4 PASS, run 33176979377). A check a
  runner cannot execute is not evidence. The wiring being fixed here is a
  DECLARATION in deploy config, so the honest CI-executable predicate is a read
  of that declaration at the exact commit under review. The runtime-attach
  behaviour those live probes were reaching for is proven separately by the AC5
  unit gate bound in this same contract, which asserts the invariant the runtime
  depends on, and by the lane refresh that picks this commit up.
exit_code: 0
pr_number: 2968
branch: "jonah/omn-16843-occ-companion"
