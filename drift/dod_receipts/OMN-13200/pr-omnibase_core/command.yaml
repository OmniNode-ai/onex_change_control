---
schema_version: "1.0.0"
ticket_id: "OMN-13200"
evidence_item_id: "pr-omnibase_core"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13200/pr-omnibase_core/command.yaml"
status: PASS
run_timestamp: "2026-06-17T14:40:00-00:00"
commit_sha: "3be251f80c6880c01d81f325fbee7830b1d7d844"
contract_sha256: "sha256:66c8c47802b2f7a6062f89b223bad8560e8871b8fcd910a68b5fd6203d3cdad6"
branch: "jonah/omn-13200-version-bump-pin"
pr_number: 2698
runner: "omn-13200-impl"
verifier: "receipt-gate-local"
probe_command: "grep -rniE 'ModelOnexEnvelope|model_onex_envelope' src/ tests/ | wc -l ; grep -E '^version'
  pyproject.toml ; grep -E '^version = \"0.45.0\"' uv.lock ; uv run pytest tests/unit/validation/test_envelope_validator.py
  tests/unit/mixins/test_mixin_intent_publisher.py -q ; uv run mypy src/omnibase_core ; gh pr view 1250
  --repo OmniNode-ai/omnibase_core --json number,state,mergeCommit"
probe_stdout: |
  # grep -rniE 'ModelOnexEnvelope|model_onex_envelope' src/ tests/ | wc -l  ->  0
  #   ZERO ModelOnexEnvelope references remain in omnibase_core src + tests. The class was deleted
  #   in B4 (PR #1250, merged to dev at e9bb51aacac98c6b9ae89f5aa26b2668e6508404). This B6 phase
  #   does not touch source — it is a pure version-string + lockfile + changelog bump.
  # grep -E '^version' pyproject.toml  ->  version = "0.45.0"
  #   Bumped 0.44.2 -> 0.45.0 (minor bump, pre-1.0 semver convention for breaking public-API deletion).
  # grep -E '^version = "0.45.0"' uv.lock  ->  matched (uv lock regenerated: omnibase-core 0.44.2 -> 0.45.0)
  # uv run pytest test_envelope_validator.py test_mixin_intent_publisher.py -q  ->  67 passed in 7.58s
  # uv run mypy src/omnibase_core  ->  Success: no issues found in 3864 source files
  #   (CI-canonical strict gate per ci.yml line 119 + repo CLAUDE.md; uses pyproject [tool.mypy]
  #   strict=true with the pydantic plugin. The 3 errors seen under a bare `mypy src/ --strict`
  #   invocation are in untouched pre-existing files contract_loader.py / cli_install.py, reproduce
  #   on clean origin/dev, and are NOT in this diff — same baseline the B4 receipt documented.)
  # gh pr view 1250 --repo OmniNode-ai/omnibase_core  ->  number=1250 state=MERGED
  #   mergeCommit=e9bb51aacac98c6b9ae89f5aa26b2668e6508404 (the base of the B6 bump branch).
actual_output: >-
  PASS: omnibase_core version is bumped from 0.44.2 to 0.45.0 (B6, the final phase of the envelope-dedup
  workstream). This is a minor bump under the repo's pre-1.0 semver convention: a breaking public-API
  deletion (ModelOnexEnvelope removed in B4 / PR #1250) maps to a minor version increment, matching the
  CHANGELOG bump history where minor bumps carry breaking/feature changes and patch bumps are release-promotion
  chores. The bump touches exactly three files in the impl PR (branch jonah/omn-13200-version-bump-pin,
  base dev, head 3be251f80c6880c01d81f325fbee7830b1d7d844): pyproject.toml version 0.44.2 -> 0.45.0 (the
  single source of truth — __version__ in src/omnibase_core/__init__.py reads it dynamically via version("omnibase-core"),
  and the IMPORT_COMPATIBILITY_MATRIX doc is historical v0.4.0, not version-derived, so no matrix hand-edit
  is required), uv.lock regenerated so its self-reference reads 0.45.0, and a CHANGELOG.md entry under
  Breaking Changes. grep -rniE 'ModelOnexEnvelope|model_onex_envelope' over src/ + tests/ returns 0 —
  the class is fully gone (deleted in B4). Gates green: ruff check --fix src/ tests/ (All checks passed);
  the CI-canonical mypy gate `uv run mypy src/omnibase_core` returns Success: no issues found in 3864
  source files (the 3 errors that surface only under a bare `mypy src/ --strict` are pre-existing in untouched
  contract_loader.py / cli_install.py, reproduce on clean origin/dev, and are not in this diff — the same
  baseline the OMN-13196 / B4 receipt documented); tests/unit/validation/test_envelope_validator.py +
  tests/unit/mixins/test_mixin_intent_publisher.py 67 passed; pre-commit run against the staged files
  passes (the deterministic-skill-routing hook passes against an omniclaude@main snapshot pinned via DETERMINISTIC_SKILL_ROOT
  — the known sibling-worktree false-positive workaround — and the exception-handling hook receives zero
  staged src .py files since the bump is non-code, so it is a no-op on commit). Predecessor B4 (OMN-13196,
  PR #1250) is MERGED to dev (e9bb51aacac98c6b9ae89f5aa26b2668e6508404, the base of this branch). The
  version bump propagates the breaking-deletion signal to dependents and triggers cross-repo pin-update
  bot PRs; B4 + B5 already removed every live ModelOnexEnvelope reference across omnibase_infra / omnibase_spi
  / omnimarket / omniintelligence so the pin bumps stay green. This receipt is the grep-target consumed
  by the check_value in contracts/OMN-13200.yaml (contract bytes hash to sha256:66c8c47802b2f7a6062f89b223bad8560e8871b8fcd910a68b5fd6203d3cdad6).
exit_code: 0
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-13200/onex_change_control"
