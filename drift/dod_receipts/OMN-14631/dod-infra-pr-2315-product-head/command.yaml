---
schema_version: "1.0.0"
ticket_id: "OMN-14631"
evidence_item_id: "dod-infra-pr-2315-product-head"
check_type: "command"
check_value: >-
  PRODUCT_PR_NUMBER=2315; PRODUCT_REPO=OmniNode-ai/omnibase_infra; PRODUCT_HEAD=0ef19799d107bb4dc55e28c558c54effb74cd453;
  d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,baseRefName,files,title
  --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
  > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fxq dev "$d/pr.txt" && grep -Fq 'fix(OMN-14631):
  close proof gap that let workspace builds ship stale config content' "$d/pr.txt" && for p in docker/Dockerfile.runtime
  scripts/runtime_build/compute_workspace_provenance.py tests/scripts/test_compute_workspace_provenance_content_parity.py;
  do grep -Fq "$p" "$d/pr.txt" && mkdir -p "$d/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
  --jq .content | base64 --decode > "$d/$p"; done && grep -Fq 'def _content_parity_diff' "$d/scripts/runtime_build/compute_workspace_provenance.py"
  && grep -Fq 'importlib.util.find_spec' "$d/scripts/runtime_build/compute_workspace_provenance.py" &&
  grep -Fq 'path.relative_to(root)' "$d/scripts/runtime_build/compute_workspace_provenance.py" && grep
  -Fq 'content_diff_files' "$d/scripts/runtime_build/compute_workspace_provenance.py" && grep -Fq 'sharing=locked'
  "$d/docker/Dockerfile.runtime" && grep -Fq 'test_tracked_files_root_nested_under_dot_venv_ancestor_is_still_hashed'
  "$d/tests/scripts/test_compute_workspace_provenance_content_parity.py" && grep -Fq 'test_main_fails_closed_on_real_drift_when_installed_under_dot_venv_ancestor'
  "$d/tests/scripts/test_compute_workspace_provenance_content_parity.py"
contract_entry_sha256: "sha256:0f5d006688d460de63b226817360c6e607bcb4e5a4c37b0f3a98e0dcc3bda36a"
status: PASS
run_timestamp: "2026-07-15T03:24:00Z"
commit_sha: "0ef19799d107bb4dc55e28c558c54effb74cd453"
runner: "codex-merge-controller"
verifier: "codex-product-pr-state"
pr_number: 2315
probe_command: "gh pr view 2315 --repo OmniNode-ai/omnibase_infra plus content checks for the three changed
  files."
probe_stdout: |
  omnibase_infra#2315 is open against dev at 0ef19799d107bb4dc55e28c558c54effb74cd453 and includes the
  runtime provenance script, Dockerfile runtime cache-mount changes, and focused content-parity tests with
  the expected OMN-14631/OMN-14635 implementation markers.
actual_output: "PASS: product head, file surface, and content markers match OMN-14631 evidence expectations."
exit_code: 0
branch: "jonah/omn-14631-workspace-build-config-content-parity-gate"
