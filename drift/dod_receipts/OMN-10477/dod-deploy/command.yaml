---
schema_version: "1.0.0"
ticket_id: "OMN-10477"
evidence_item_id: "dod-deploy"
check_type: "command"
check_value: "printf '%s\n' 'post-merge deploy smoke: docker exec ${RUNTIME_CONTAINER:-omninode-runtime}
  python -c \"import pathlib; p=pathlib.Path('/app/src/omnimarket/nodes/node_ab_compare_orchestrator/contract.yaml');
  print(p.exists())\"'"
status: PASS
run_timestamp: "2026-05-01T22:23:07+00:00"
commit_sha: "b38040a"
runner: "codex-pr-sweep-omn-10477"
verifier: "jonahgabriel"
probe_command: "printf '%s\n' 'post-merge deploy smoke: docker exec ${RUNTIME_CONTAINER:-omninode-runtime}
  python -c \"import pathlib; p=pathlib.Path('/app/src/omnimarket/nodes/node_ab_compare_orchestrator/contract.yaml');
  print(p.exists())\"'"
probe_stdout: |
  post-merge deploy smoke: docker exec ${RUNTIME_CONTAINER:-omninode-runtime} python -c "import pathlib; p=pathlib.Path('/app/src/omnimarket/nodes/node_ab_compare_orchestrator/contract.yaml'); print(p.exists())"
actual_output: "Recorded deploy-gate posture without runtime mutation: this PR adds contract/config YAML
  only. Post-merge smoke must use docker exec after merge to verify the contract file is present in runtime."
exit_code: 0
pr_number: 508
