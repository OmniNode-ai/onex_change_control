---
schema_version: "1.0.0"
ticket_id: "OMN-14394"
evidence_item_id: "dod-verify-2266-product-observing-repair"
check_type: "command"
check_value: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_pr_state_projection_compute/handlers/handler_pr_state_projection.py?ref=daa28e5cd1af226af79b3641de1637915510539a"
  --jq .content | base64 -d | grep -c 'is_draft')" -ge 1
status: PASS
run_timestamp: "2026-07-12T07:10:00Z"
commit_sha: "daa28e5cd1af226af79b3641de1637915510539a"
runner: "occ-queue-runner"
verifier: "occ-queue-verifier"
pr_number: 2266
probe_command: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_pr_state_projection_compute/handlers/handler_pr_state_projection.py?ref=daa28e5cd1af226af79b3641de1637915510539a"
  --jq .content | base64 -d | grep -c 'is_draft')" -ge 1   # then re-run verbatim with ref=dev and ref=<bogus
  SHA> as RED controls
probe_stdout: |
  # bound check_value, run verbatim:
  #   ref=daa28e5c (omnibase_infra#2266 head) -> exit 0   GREEN ('is_draft' present)
  #   ref=dev            (omnibase_infra dev)            -> exit 1   RED   ('is_draft' absent)
  #   ref=000...0        (bogus SHA)                     -> exit 1   RED
exit_code: 0
actual_output: >-
  PASS: OMN-14436 REPAIR. Every pre-existing entry in OMN-14394's contract is INERT -- it greps this repo's
  own receipt store. That is a PLATFORM WIRING DEFECT, not an authoring failure: the DoD runner executed
  check_values with --workspace pointed at the onex_change_control clone rather than the product checkout,
  so the receipt was the only file on disk and NO other kind of check could have run. The author wrote
  exactly what the tooling accepted. We fix the debt we created; we do not invoice it to whoever is holding
  it. This entry adds the product-observing evidence that was impossible to write before. It reads omnibase_infra#2266's
  source at its exact head SHA via the GitHub contents API and asserts 'is_draft'. OMN-14394 closes an
  is_draft SEAM GAP: the field existed on one side and was dropped on the other, so the projection silently
  lost it. Asserting is_draft in the HANDLER -- the consuming side of the seam -- is what proves the gap
  is closed; the model alone would not. RED DIRECTION PROVEN by execution: the symbol occurs at #2266's
  head and ZERO times at omnibase_infra dev, so the identical command exits 1 at dev and at a bogus SHA.
  It exits 0 only where the code exists. APPEND-ONLY: no existing dod_evidence entry and no merged receipt
  was modified.
branch: "jonah/omn-14436-repair-inert-contracts"
contract_sha256: "sha256:9c7de7ab33ae539b24a15b283d13fbf0e3c5c98ecc0988ae6e328d63d7116668"
contract_entry_sha256: "sha256:f01d54e5351c9cdf54bbf2d30333d1a541f6760b788b643eee7297aa7f187ba3"
