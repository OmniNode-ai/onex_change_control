---
schema_version: 1.0.0
ticket_id: OMN-15413
evidence_item_id: dod-omn15413-ac2-idempotent-and-history-preserving
check_type: command
check_value: gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/run-forward-migrations.sh?ref=dev'
  --jq .content | base64 -d | grep -F 'ON CONFLICT (migration_id) DO NOTHING;' > /dev/null && gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/run-forward-migrations.sh?ref=dev'
  --jq .content | base64 -d | grep -F '(already applied)' > /dev/null && gh api 'repos/OmniNode-ai/omnibase_infra/contents/tests/integration/migrations/test_application_migration_ledger_omn15413.py?ref=dev'
  --jq .content | base64 -d | grep -F 'def test_real_runner_imports_sanitized_legacy_history_twice_on_postgresql_16('
  > /dev/null && gh api 'repos/OmniNode-ai/omnibase_infra/contents/tests/integration/migrations/test_application_migration_ledger_omn15413.py?ref=dev'
  --jq .content | base64 -d | grep -F 'def test_real_runner_fresh_install_twice_on_postgresql_16(' > /dev/null
contract_entry_sha256: sha256:ca38880a2b4c7a9568fc4fff62b460328d9f3df93e2f8b662594c0050f3847b2
status: PASS
run_timestamp: '2026-08-02T14:56:45Z'
commit_sha: 529f99909e1c144bc90bcf9a5f2980bb47e69175
runner: occ-integrity-0802
verifier: occ-integrity-0802/append-only-supersession
probe_command: gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/run-forward-migrations.sh?ref=dev'
  --jq .content | base64 -d | grep -F 'ON CONFLICT (migration_id) DO NOTHING;' > /dev/null && gh api 'repos/OmniNode-ai/omnibase_infra/contents/scripts/run-forward-migrations.sh?ref=dev'
  --jq .content | base64 -d | grep -F '(already applied)' > /dev/null && gh api 'repos/OmniNode-ai/omnibase_infra/contents/tests/integration/migrations/test_application_migration_ledger_omn15413.py?ref=dev'
  --jq .content | base64 -d | grep -F 'def test_real_runner_imports_sanitized_legacy_history_twice_on_postgresql_16('
  > /dev/null && gh api 'repos/OmniNode-ai/omnibase_infra/contents/tests/integration/migrations/test_application_migration_ledger_omn15413.py?ref=dev'
  --jq .content | base64 -d | grep -F 'def test_real_runner_fresh_install_twice_on_postgresql_16(' > /dev/null
probe_stdout: '(no stdout: every leg terminates in `> /dev/null`; the load-bearing signal is the exit
  status, recorded in exit_code)'
actual_output: 'PASS, exit 0, re-run live with the committed check_value executed verbatim. All four conjuncts
  resolved at ?ref=dev. ''ON CONFLICT (migration_id) DO NOTHING;'' and the ''(already applied)'' short-circuit
  are present in scripts/run-forward-migrations.sh — DO NOTHING rather than DO UPDATE is simultaneously
  the idempotency mechanism and the history-preservation mechanism, since an import can never overwrite
  an applied row. Both twice-apply suites ''def test_real_runner_imports_sanitized_legacy_history_twice_on_postgresql_16(''
  and ''def test_real_runner_fresh_install_twice_on_postgresql_16('' are declared in the OMN-15413 integration
  module. SCOPE: conjuncts 3 and 4 prove those test functions are PRESENT on the mainline, not that they
  PASS today — dod_verify runs check_values with cwd set to the onex_change_control checkout, so no omnibase_infra-relative
  pytest can resolve from this executor. Conjuncts 1 and 2 pin the shipped mechanism itself. FALSIFIABILITY
  DEMONSTRATED with two RED controls: pinning ''ON CONFLICT (migration_id) DO UPDATE;'' — the history-overwriting
  shape bullet 2 forbids — exits 1, and renaming the twice-import test to a ''_ONCE_'' variant exits 1.
  FAIL-CLOSED CONFIRMED: a non-existent ref exits 1.'
exit_code: 0
pr_number: 5975
branch: jonah/omn-15413-evidence-integrity
no_source_change: true
