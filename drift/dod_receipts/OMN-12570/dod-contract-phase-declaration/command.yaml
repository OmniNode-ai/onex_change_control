---
schema_version: "1.0.0"
ticket_id: "OMN-12570"
evidence_item_id: "dod-contract-phase-declaration"
check_type: "command"
check_value: "grep -q 'post_merge_tail' src/omnimarket/nodes/node_pr_lifecycle_orchestrator/contract.yaml
  && grep -q 'transitions_recorded: true' src/omnimarket/nodes/node_pr_lifecycle_orchestrator/contract.yaml
  && grep -q 'POST_MERGE_TAIL' src/omnimarket/nodes/node_pr_lifecycle_orchestrator/contract.yaml"
status: PASS
run_timestamp: "2026-06-01T23:14:00Z"
commit_sha: "7aa77e645e445d7fe317e6de3f6efe6b0a1bb566"
contract_sha256: "sha256:c406928b4d031407167c00580f563662c3eac29c738fb266b5e5e0a6989dbd8e"
branch: "jonah/omn-12570-orchestrator-phase-separation"
pr_number: 1014
runner: "claude-code"
verifier: "jonah"
probe_command: "grep -q 'post_merge_tail' src/omnimarket/nodes/node_pr_lifecycle_orchestrator/contract.yaml
  && grep -q 'transitions_recorded: true' src/omnimarket/nodes/node_pr_lifecycle_orchestrator/contract.yaml
  && grep -q 'POST_MERGE_TAIL' src/omnimarket/nodes/node_pr_lifecycle_orchestrator/contract.yaml && echo
  'PASS: all three present'"
probe_stdout: |
  PASS: all three present
  The orchestrator contract.yaml declares the phase vocabulary (inventory,
  triage, branch_checks, merge_group, post_merge_tail, terminal),
  transitions_recorded: true with a durable transition_table
  (pr_lifecycle_ledger_transitions), the POST_MERGE_TAIL FSM state, and the
  MERGING -> POST_MERGE_TAIL transition — keeping the contract the source of
  truth for the phase-separated state machine.
actual_output: "PASS: contract declares the branch_checks/merge_group/post_merge_tail phases, the recorded
  transition table, and the POST_MERGE_TAIL state."
exit_code: 0
working_dir: "/Users/jonah/Code/omni_worktrees/OMN-12570/omnimarket"
