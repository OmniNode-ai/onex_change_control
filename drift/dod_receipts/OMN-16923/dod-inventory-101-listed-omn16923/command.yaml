---
schema_version: 1.0.0
ticket_id: OMN-16923
evidence_item_id: dod-inventory-101-listed-omn16923
check_type: command
check_value: |-
  uv run python -c 'import yaml; inv = yaml.safe_load(open("src/onex_change_control/boundaries/migration_inventory.yaml")); sets = [s for db in inv["databases"].values() for s in db.get("migration_sets", []) if s.get("source_repo") == "omnibase_infra" and s.get("directory") == "docker/migrations/forward"]; rows = [m for s in sets for m in s["migrations"] if m.get("file") == "101_converge_savings_estimates_to_node_declared_shape.sql"]; assert len(rows) == 1, rows; assert rows[0].get("tables"), rows[0]; print("PASS: migration 101 listed once in the omnibase_infra forward set with tables=%s" % rows[0]["tables"])'
contract_sha256: sha256:bce53ca2cc6bc331350a07026ac1a299279567b40e64279123e134f4927ad247
contract_entry_sha256: sha256:0fcd45ed0cf9a4225c3dc7df95d7542d545835a2661456ce67fb6d8aa38855c2
status: PASS
run_timestamp: '2026-08-29T12:39:34Z'
commit_sha: 9cb810ae8217ab52bc16bfce2cc001d262aa8376
runner: occ16923-inventory-sync-lane
verifier: occ16923-inventory-sync-verifier
probe_command: |-
  uv run python -c 'import yaml; inv = yaml.safe_load(open("src/onex_change_control/boundaries/migration_inventory.yaml")); sets = [s for db in inv["databases"].values() for s in db.get("migration_sets", []) if s.get("source_repo") == "omnibase_infra" and s.get("directory") == "docker/migrations/forward"]; rows = [m for s in sets for m in s["migrations"] if m.get("file") == "101_converge_savings_estimates_to_node_declared_shape.sql"]; assert len(rows) == 1, rows; assert rows[0].get("tables"), rows[0]; print("PASS: migration 101 listed once in the omnibase_infra forward set with tables=%s" % rows[0]["tables"])'
probe_stdout: |
  PASS: migration 101 listed once in the omnibase_infra forward set with tables=['savings_estimates']
actual_output: |-
  PASS: the OCC cross-repo migration inventory now lists 101_converge_savings_estimates_to_node_declared_shape.sql exactly once in the omnibase_infra docker/migrations/forward set, carrying tables=['savings_estimates'] — the sole relation the migration's ALTER TABLE / CREATE TABLE IF NOT EXISTS statements target. The tables field is asserted non-empty because check_migration_inventory raises MISSING_METADATA as a WARNING when it is absent, so a file-only row clears the blocking error while leaving a warning behind. CROSS-REPO EXECUTION, recorded here because the hermetic probe above deliberately does not re-prove it: `uv run check-migration-inventory --repos-root /tmp/repos` against a real omnibase_infra checkout returns exit 0 with zero ERROR lines on this branch, and exit 1 with '[ERROR] UNLISTED_FILE: omnibase_infra: omnibase_infra/docker/migrations/forward/101_converge_savings_estimates_to_node_declared_shape.sql exists on disk but not in inventory' against the same tree with this row removed. The check therefore separates the fixed tree from the broken one rather than passing everywhere.
exit_code: 0
branch: hotfix/omn-16923-occ-inventory-101-savings-estimates
pr_number: 7558
