---
schema_version: "1.0.0"
ticket_id: "OMN-16434"
evidence_item_id: "dod-omn-16434-diff-derived-behavior-proof"
check_type: "test_passes"
check_value: "uv run pytest tests/unit/nodes/node_occ_companion_compute/test_autobind_diff_derived_behavior_proof_omn_16434.py
  -q"
contract_entry_sha256: "sha256:3d0d016004b7b7e78619ca2dcd615d3a96abbcd4ff7bcbf88c5a5e9a18319aeb"
status: PASS
run_timestamp: "2026-08-28T08:35:00+00:00"
commit_sha: "a9fafd1894b6741287d701615fd296c7c2c053f8"
runner: "claude-code-omn16434-autobind-mint-fix-lane"
verifier: "claude-code-omn16434-red-control-reverify"
probe_command: >-
  gh api repos/OmniNode-ai/omnimarket/commits/a9fafd1894b6741287d701615fd296c7c2c053f8 --jq .sha && cd
  "$OMNI_HOME/omni_worktrees/OMN-16434/omnimarket" && git rev-parse HEAD && uv run pytest tests/unit/nodes/node_occ_companion_compute/test_autobind_diff_derived_behavior_proof_omn_16434.py
  -q -p no:randomly
probe_stdout: |
  a9fafd1894b6741287d701615fd296c7c2c053f8
  a9fafd1894b6741287d701615fd296c7c2c053f8
  17 passed, 3 warnings in 0.64s
actual_output: >-
  PASS: the 17 assertions in omnimarket's OMN-16434 suite executed against the producer at a9fafd189 and
  exited 0. They drive the REAL compute_companion_plan seam, not a fixture: a PR whose changed_files carry
  a pytest target now yields behavior_proving_count >= 1 (computed by the real omnimarket.nodes.node_dod_verify.services.check_proof_class.classify_item_checks),
  the minted item names that exact target at cwd ${OMNI_HOME}/<product repo>, and the FOREIGN_SUITE_DENYLIST
  value "uv run pytest tests/test_evidence_admissibility.py -q" is absent from the contract. RED CONTROL
  EXECUTED IN THE SAME CLONE, not asserted: with the two producer modules reverted to origin/dev (git
  checkout origin/dev -- src/omnimarket/nodes/node_pr_lifecycle_fix_effect/handlers/occ_evidence_stamp.py
  src/omnimarket/nodes/node_occ_companion_compute/handlers/handler_occ_companion_compute.py) the same
  command exits 2 with "ImportError: cannot import name BEHAVIOR_PROOF_EVIDENCE_ID" and collects zero
  tests; the modules were restored with git checkout HEAD -- and the worktree verified clean. A SECOND
  red leg is internal to the suite and runs on every invocation: the same producer, same PR, with the
  test file removed from changed_files mints NO behavior item and stays at behavior_proving_count 0. BEHAVIOR
  proof class. HONEST SCOPE: the declared check_value's cwd resolves against $OMNI_HOME/omnimarket, where
  this suite exists only after omnimarket#2180 merges; hosted contract compliance does not execute a test_passes
  check_value (it asserts the PR's CI), so the recorded probe above is the run that actually happened,
  in the PR worktree, and is the durable evidence.
exit_code: 0
branch: "jonah/omn-16434-autobind-diff-derived-behavior-proof"
