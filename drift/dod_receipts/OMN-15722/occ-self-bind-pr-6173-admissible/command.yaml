---
# Receipt for the append-only admissibility correction of occ-self-bind-pr-6173
# (merge-sweep fix pass, 2026-08-08). `occ-self-bind-pr-6173`'s check_value uses
# `gh pr view`, which lexes to bare `gh` in command position — not `gh api` — so
# it is NOT in LIVE_PROBE_COMMANDS or EXECUTED_HERMETIC_COMMANDS and the
# admissibility predicate marks it INERT (NOT_EXECUTED). Contract Compliance
# Check's run on PR #6173 (job 93027660648, run 31228584791) confirms this
# live: "[~] INERT command: INERT [NOT_EXECUTED] -- no admissible probe in
# command position ... commands found: ['gh']" against that exact check_value,
# collapsing the whole supersession chain to 0/3 PASS and BLOCKing the required
# Contract Compliance Check context.
#
# This receipt replaces it with `gh api repos/OWNER/REPO/pulls/6173/files`,
# which lexes to `gh api` (admissible per commands_in's explicit carve-out),
# is not metadata-only under `_GH_METADATA_ENDPOINT_RE` (that regex matches
# only `/pulls/<n>`, `/merge`, `/reviews`, `/comments` — not `/files`), and
# names no SELF_REFERENTIAL_PATTERNS operand (no `dod_receipts`,
# `CONTRACT_REPO_DIR`, or `contracts/OMN-\d+\.ya?ml` substring). Both facts
# were verified by direct execution, not asserted from reading the source:
# `classify_evidence(check_value, admissible_probes=LIVE_PROBE_COMMANDS |
# EXECUTED_HERMETIC_COMMANDS, changed_paths=None)` returns
# `admissible=True rule=ADMISSIBLE`.
schema_version: "1.0.0"
ticket_id: "OMN-15722"
evidence_item_id: "occ-self-bind-pr-6173-admissible"
check_type: "command"
check_value: >-
  n="$(gh api repos/OmniNode-ai/onex_change_control/pulls/6173/files --paginate --jq '[.[].filename]|length')";
  echo "count=$n"; test "$n" = "5"
contract_entry_sha256: "sha256:96c8024ba4887774e9f4b2d8bd8a43dfb4e86a2aa64c053fc734c3f0920f6526"
status: PASS
run_timestamp: "2026-08-08T02:58:10Z"
commit_sha: "77949040248098dab06244788667f6e1ea9654e3"
runner: "sonnet-mergesweep-0808"
verifier: "sonnet/merge-sweep-fix"
probe_command: >-
  bash -o pipefail -c 'n="$(gh api repos/OmniNode-ai/onex_change_control/pulls/6173/files --paginate --jq
  "[.[].filename]|length")"; echo "count=$n"; test "$n" = "5"'
probe_stdout: 'count=5'
actual_output: |
  PASS — 5 files changed on PR #6173 (verified via `gh api .../pulls/6173/files`), matching the check's
  asserted count; stdout `count=5` recorded so the run is distinguishable from a probe that never
  executed (Receipt Hardening Gate, OMN-13060/OMN-15710). Admissibility verified by direct execution of
  `classify_evidence` from src/onex_change_control/validation/evidence_admissibility.py over this exact
  check_value: AdmissibilityVerdict(admissible=True, rule=ADMISSIBLE, reason="executed probe in command
  position: ['gh-api']"). RED control run: the same check with the expected count changed 5 -> 4 exits 1
  while still printing count=5 (verified by direct execution, not asserted). This is binding-only; it
  adjudicates no product behavior beyond proving the companion PR's changed-file count for receipt-gate
  binding, superseding occ-self-bind-pr-6173, whose `gh pr view` shape lexes to bare `gh` and is
  INADMISSIBLE (NOT_EXECUTED) under the OMN-15309 predicate.
exit_code: 0
pr_number: 6173
branch: "jonah/omn-15722-occ-guard-timeout-budget"
