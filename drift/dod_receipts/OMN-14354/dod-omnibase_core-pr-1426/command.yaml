---
schema_version: "1.0.0"
ticket_id: "OMN-14354"
evidence_item_id: "dod-omnibase_core-pr-1426"
check_type: "command"
check_value: "cd omnibase_core && env -u PYTHONPATH uv run pytest tests/unit/canary/test_adequacy_receipt.py
  -q"
status: PASS
run_timestamp: "2026-07-11T02:35:00Z"
commit_sha: 0eac0aeee9c7982ab050d70ae11b5b688ae3eb6b
runner: "omn-14354-independent-verifier"
verifier: "occ-evidence-lane"
probe_command: >-
  cd /Users/jonah/Code/omni_home/omni_worktrees/OMN-14354/omnibase_core && env -u PYTHONPATH uv run pytest
  tests/unit/canary/test_adequacy_receipt.py -v; run a standalone forge harness against adequacy_receipt.py
  (4 canonical waiver cases + the meets_target-lie forge + scrub + stale-hash); env -u PYTHONPATH uv run
  ruff check; env -u PYTHONPATH uv run mypy --strict scripts/ci/adequacy_receipt.py
probe_stdout: |
  re-bind: verified at head 54b7bb37 (had an open forge finding); head advanced to 0eac0aee with the fix ("reject receipts whose meets_target lies about the coverage numbers"); re-verified at 0eac0aee
  11 passed in 1.89s (was 9; +2 committed forge tests)
  waiver invariant: below-target+no-waiver REJECTED; above-target+waiver REJECTED; above+no-waiver accepted; below+waiver accepted
  degenerate guard: meets_target=True + selected_count==0 REJECTED
  FORGE (meets_target=True, pct=10, target=80, selected_count=1, no waiver): REJECTED (finding CLOSED - _check now raises unless meets_target == (pct >= target))
  MIRROR forge (pct>=target, meets_target=False): REJECTED
  scrub: redacts api_key/password/token at all depths; keeps non-sensitive; PASS
  stale-hash: one-byte handler change -> different sha256 -> stale receipt detectable; PASS
  ruff check: All checks passed!
  mypy --strict adequacy_receipt.py: Success no issues
actual_output: >-
  PASS on all five stated criteria for the coverage-guided adequacy receipt. The fail-closed uncovered-waiver
  invariant rejects a below-target receipt with no waiver and rejects a waiver when the target is met;
  the new hardening guard rejects a degenerate zero-input pass; the deny-by-default scrub redacts sensitive
  values at every depth; provenance (recorded_at, per-input canonical hash, handler_module_sha256) is
  present; the handler hash is byte-sensitive so a stale receipt is detectable by the consuming gate;
  and the 11 tests genuinely exercise all of this (not tautological). ruff and mypy --strict are clean.
  Re-bound to head 0eac0aeee9c7982ab050d70ae11b5b688ae3eb6b: the earlier MEDIUM finding is CLOSED. _check
  now derives the verdict — it raises unless meets_target == (branch_coverage_pct >= coverage_target)
  — so the forge that previously slipped through (pct=10, target=80, meets_target=True, no waiver) is
  now REJECTED at construction, as is the mirror (pct>=target with meets_target=False). Both forge cases
  are committed as regression tests. The fail-closed uncovered-waiver is now a clean, non-bypassable invariant
  on the receipt data itself, not just the honest build_receipt path.
exit_code: 0
pr_number: 1426
branch: "jonah/omn-14354-adequacy-receipt"
contract_sha256: "sha256:ca55d361f6f8de05a8b36e5e375f07199cdf9e2008e764b7f13164582fe493ff"
