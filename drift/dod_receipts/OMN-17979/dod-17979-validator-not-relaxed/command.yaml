---
schema_version: 1.0.0
ticket_id: OMN-17979
evidence_item_id: dod-17979-validator-not-relaxed
check_type: command
check_value: body="$(gh api 'repos/OmniNode-ai/omnimarket/contents/src/omnimarket/models/delegation/wire/model_delegate_skill_response.py?ref=b050a030d0f8ea6c4abaeda0eb86f39ccc57d9a8'
  --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF 'quality-failed response at or above required_quality_bar
  must' && printf '%s' "$body" | grep -qF 'quality_gate_passed response cannot be below required_quality_bar'
  && printf '%s' "$body" | grep -qF 'quality_gate_passed response cannot carry failed_acceptance_criteria'
  && printf '%s' "$body" | grep -qF 'successful delegation cannot carry terminal_failure_cause' && printf
  '%s' "$body" | grep -qF 'def _ladder_records_an_acceptance'
status: PASS
run_timestamp: '2026-09-06T15:05:00Z'
commit_sha: b050a030d0f8ea6c4abaeda0eb86f39ccc57d9a8
runner: omn17979-delegate-skill-response-wire-model
verifier: onex-dev-lane-broker-readback
probe_command: 'body="$(gh api ''repos/OmniNode-ai/omnimarket/contents/src/omnimarket/models/delegation/wire/model_delegate_skill_response.py?ref=b050a030d0f8ea6c4abaeda0eb86f39ccc57d9a8''
  --jq .content | base64 -d)" && printf ''%s'' "$body" | grep -qF ''quality-failed response at or above
  required_quality_bar must'' && printf ''%s'' "$body" | grep -qF ''quality_gate_passed response cannot
  be below required_quality_bar'' && printf ''%s'' "$body" | grep -qF ''quality_gate_passed response cannot
  carry failed_acceptance_criteria'' && printf ''%s'' "$body" | grep -qF ''successful delegation cannot
  carry terminal_failure_cause'' && printf ''%s'' "$body" | grep -qF ''def _ladder_records_an_acceptance''
  && echo "PASS: all four validator clauses and _ladder_records_an_acceptance present at b050a030d0f8"'
probe_stdout: 'PASS: all four validator clauses and _ladder_records_an_acceptance present at b050a030d0f8'
actual_output: 'PASS (exit 0, probe re-run with an emitting terminal so the stdout is observable): every
  clause of the ModelDelegateSkillResponse model validator is still present verbatim at PR #2332 head
  b050a030d0f8ea6c4abaeda0eb86f39ccc57d9a8 -- including the clause that refused this defect -- and the
  new producer-side guard _ladder_records_an_acceptance is present. The contract''s own check_value ends
  in grep -qF and is silent on success, so the probe recorded here appends a terminal echo to give the
  run an observable stdout; the clause chain in front of it is byte-identical to the contract''s, and
  any softened or removed clause short-circuits it to exit 1 with no echo.'
exit_code: 0
duration_ms: 1400
pr_number: 8391
branch: jonah/omn-17979-occ-companion
contract_sha256: sha256:1b9d33e3c97a851ee5990d55687e6dbe4749dc18cf106869815413fd2a7f63be
contract_entry_sha256: sha256:557dba3474a558af1d094c38c7e6ce71e1758eb38e2bad7b642559e7855e0c97
