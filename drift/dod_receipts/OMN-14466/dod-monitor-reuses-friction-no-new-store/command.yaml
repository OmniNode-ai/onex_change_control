---
schema_version: "1.0.0"
ticket_id: "OMN-14466"
evidence_item_id: "dod-monitor-reuses-friction-no-new-store"
check_type: "command"
check_value: >-
  S=$(gh api "repos/OmniNode-ai/omnibase_infra/contents/scripts/release_drift_monitor.py?ref=cfe726f88e91c350164e71a3af13823187f38ea9"
  --jq .content 2>/dev/null | base64 -d 2>/dev/null); test "$(printf '%s' "$S" | grep -c 'onex_state/friction')"
  -ge 1 && test "$(printf '%s' "$S" | grep -c 'node_friction_triage')" -ge 1
status: PASS
run_timestamp: "2026-07-12T18:32:00Z"
commit_sha: "cfe726f88e91c350164e71a3af13823187f38ea9"
runner: "occ-dod-runner"
verifier: "rt1-builder"
pr_number: 2272
probe_command: >-
  # verbatim check_value re-run at #2272 head cfe726f8 and branch-point parent e811995d, plus an # independent-verifier
  trace of the friction path itself (see actual_output).
probe_stdout: |
  # bound check_value exit codes, run verbatim by rt1-builder (RT-7 independent verifier):
  #   ref=cfe726f8 (#2272 head) -> friction=1 triage=1 -> exit 0  GREEN
  #   ref=e811995d (parent)     -> friction=0 triage=0 -> exit 1  RED  <- shell absent, non-vacuous
exit_code: 0
actual_output: >-
  PASS: release_drift_monitor.py at #2272's head writes into the EXISTING .onex_state/friction registry
  and routes through node_friction_triage -- it builds no new store. The independent verifier confirmed
  this is a real, wired, durable substrate, not a stub: node_friction_triage_orchestrator exists in omnimarket
  with a contract.yaml, a handler, and Linear routing, and node_env_sync_alert_effect is the platform's
  existing scheduled-drift -> friction -> Linear precedent the monitor mirrors. An emit-to-temp run wrote
  13 well-formed friction YAMLs (event_type: release_deploy_drift, drift_signature, severity, code, source).
  This is the design-deviation scrutinised on assignment: the builder deliberately did NOT feed pr_state
  (OMN-14375/14374) because release drift is not PR-scoped and pr_state's schema is wrong for it -- reusing
  friction is an honest, defensible "no new store" call. RED PROVEN at parent e811995d where the shell
  is absent (both counts 0 -> exit 1).
branch: "jonah/omn-14466-rt7-occ-companion"
contract_sha256: "sha256:8b89b26e876e26a10b11d24a15ebb24185cc0da30be84b01c2cc94eb0a9b7003"
contract_entry_sha256: "sha256:baabe6edbc854453eb54c2c29419173a1617e30b881712b42aa2a00967fffbeb"
