---
schema_version: "1.0.0"
ticket_id: "OMN-14340"
evidence_item_id: "dod-omnibase_core-pr-1423"
check_type: "command"
check_value: "cd omnibase_core && env -u PYTHONPATH uv run python scripts/ci/check_import_ratchet.py &&
  env -u PYTHONPATH uv run pytest tests/unit/scripts/ci/test_check_import_ratchet.py -q"
status: PASS
run_timestamp: "2026-07-10T22:13:00Z"
commit_sha: 8bc59f6f2eeaf35a570c65935121a59dbfb23fc9
runner: "omn-14340-independent-verifier"
verifier: "occ-evidence-lane"
probe_command: >-
  cd /Users/jonah/Code/omni_home/omni_worktrees/OMN-14340/omnibase_core && env -u PYTHONPATH uv run python
  scripts/ci/check_import_ratchet.py; for each of protocols->models / mixins / protocols / utils, create
  a real throwaway importer, run the checker CLI, capture exit code, remove; mutate find_violations to
  return {} and run the tests; regenerate the baseline via --update and diff vs committed; env -u PYTHONPATH
  uv run pytest tests/unit/scripts/ci/test_check_import_ratchet.py -q; env -u PYTHONPATH uv run mypy --strict
  scripts/ci/check_import_ratchet.py scripts/ci/import_ratchet_baseline.py
probe_stdout: |
  baseline: Import-layering growth ratchet OK — protocols->models=65 (frozen max 65, hard-fail), hub_inbound={'utils': 548, 'mixins': 17, 'protocols': 63}; exit 0
  inject protocols->models edge: FAILED, "+ omnibase_core.protocols.protocol_zzz_probe -> omnibase_core.models.primitives.model_semver"; EXIT 1
  inject mixins hub importer: FAILED, "hub 'mixins': + omnibase_core.zzz_probe_mixins"; EXIT 1
  inject protocols hub importer: FAILED, "hub 'protocols': + omnibase_core.zzz_probe_protocols"; EXIT 1
  inject utils hub importer: WARNING (non-blocking) "hub 'utils': + omnibase_core.zzz_probe_utils"; ratchet OK; EXIT 0 (utils 548->549)
  mutation find_violations()->{}: 3 failed, 9 passed (test_ratchet_flags_new_protocols_models_edge, hard-fail-hub[mixins], hard-fail-hub[protocols]); reverted -> 12 passed
  baseline --update regen: byte-identical to committed import_ratchet_baseline.py
  ratchet tests: 12 passed; lint-imports: 4 kept, 0 broken; mypy --strict: Success 2 files; ruff: All checks passed
  worktree left clean after every injection/mutation
actual_output: >-
  PASS: the import-layering growth ratchet bites and cannot rot. Frozen at 65 protocols->models edges;
  live injection of a real protocols->models edge, a mixins importer, and a protocols importer each EXITs
  1 with a precise report, while a utils importer only WARNs (EXIT 0) — the tiered enforcement (mixins/protocols
  hard-fail, utils warn-only) works exactly as the operator tuning specifies. Neutering find_violations()
  drives 3 self-check tests RED, so the ratchet cannot silently degrade to a no-op. The committed baseline
  is byte-identical to a fresh grimp regeneration (honest, not hand-loosened). Wired as a blocking Import
  Layering Oracle CI step + import-ratchet pre-commit hook. 12 tests pass; lint-imports 4/0; mypy --strict
  and ruff clean. Retirement rides the RSD canonical rewrite. No runtime or deploy scope.
exit_code: 0
pr_number: 1423
branch: "jonah/omn-14340-protocols-models-ratchet-hub-growth"
contract_sha256: "sha256:0330fc644d9c8f2c936612112f937e5eaad3c6e2286f065de111d560a0239a37"
