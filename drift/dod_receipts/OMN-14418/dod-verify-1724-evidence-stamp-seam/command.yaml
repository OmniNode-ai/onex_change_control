---
schema_version: "1.0.0"
ticket_id: "OMN-14418"
evidence_item_id: "dod-verify-1724-evidence-stamp-seam"
check_type: "command"
check_value: >-
  test "$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_pr_lifecycle_fix_effect/handlers/occ_evidence_stamp.py?ref=dbfc5d6bc72b0bddd83c3d6313ac03fff6165d28"
  --jq .content | base64 -d | grep -c "CONTRACT_ENTRY_SHA_LINE_RE")" -ge 1
status: PASS
run_timestamp: "2026-07-12T05:13:05Z"
commit_sha: "dbfc5d6bc72b0bddd83c3d6313ac03fff6165d28"
runner: "occ-queue-runner"
verifier: "occ-queue-verifier"
pr_number: 1724
probe_command: >-
  gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_pr_lifecycle_fix_effect/handlers/occ_evidence_stamp.py?ref=dbfc5d6bc72b0bddd83c3d6313ac03fff6165d28"
  --jq .content | base64 -d | grep -c "CONTRACT_ENTRY_SHA_LINE_RE"  # then the same grep against the SAME
  file at omnimarket dev HEAD 5c689429 (RED control), plus a bare-existence probe of the file at both
  refs to show why existence alone was rejected as a check
probe_stdout: |
  # symbol count, occ_evidence_stamp.py:
  #   ref=dbfc5d6b (#1724 head) -> 3   CONTRACT_ENTRY_SHA_LINE_RE present
  #   ref=5c689429 (dev HEAD)   -> 0   absent (#1724 unmerged)
  # why a bare-existence check was REJECTED — the file already exists at BOTH refs, so existence
  # does not discriminate and would be a rubber stamp:
  #   gh api .../occ_evidence_stamp.py?ref=5c689429 --jq .sha -> e3e8d58a93a6b3d34d53f2e739f68fa11d7c9af1
  #   gh api .../occ_evidence_stamp.py?ref=dbfc5d6b --jq .sha -> (present, different blob)
  # bound check_value exit codes, run verbatim:
  #   ref=dbfc5d6b (#1724 head) -> exit 0   GREEN
  #   ref=5c689429 (dev HEAD)   -> exit 1   RED
exit_code: 0
actual_output: >-
  PASS: reads the actual source of occ_evidence_stamp.py — the pure rendering seam OMN-14418 carves out
  of the emitter — at #1724's exact head commit SHA, and confirms it carries CONTRACT_ENTRY_SHA_LINE_RE,
  the regex that recognizes and rewrites the per-entry hash line in a generated receipt. That regex is
  the concrete mechanism by which the emitter stamps contract_entry_sha256, so asserting it is a direct
  read of the fix rather than a restatement of the PR's own claim. A bare existence check on this path
  was considered and REJECTED as non-falsifiable: occ_evidence_stamp.py already exists on omnimarket dev
  (#1724 extends it rather than creating it), so `--jq .sha` would return a hash at BOTH refs and the
  check would pass whether or not #1724's work is present. The symbol is what discriminates. RED DIRECTION
  PROVEN by execution: CONTRACT_ENTRY_SHA_LINE_RE occurs 3 times in that file at #1724's head SHA and
  ZERO times in the same file at omnimarket dev HEAD (5c6894294e3d207556017273d534d6dd0eee51d7), so the
  bound command exits 0 at #1724's head and exits 1 at dev. Like its sibling entry this is a content read
  pinned to a SHA, not a CI-outcome check, because #1724's CI is red BECAUSE this contract is missing
  — a CI-keyed check would be circular.
branch: "jonah/omn-14418-occ-companion"
contract_sha256: "sha256:328107bc211c798674987d606528667ecb4c20a487b1428bb937281a9e3916bc"
contract_entry_sha256: "sha256:602aa4d4267889fe917a70d0b65d256dc4cb907ee11c0b3b29d550949c41d666"
