---
schema_version: 1.0.0
ticket_id: OMN-12805
evidence_item_id: dod-001
check_type: command
check_value: "grep -q 'PASS' drift/dod_receipts/OMN-12805/dod-001/command.yaml"
status: PASS
run_timestamp: '2026-06-22T00:00:00Z'
commit_sha: 76d4714942a998e51fb8a2d8730a2a6de1de70ab
runner: claude-code
verifier: claude-code-review
contract_sha256: sha256:38e2466e8cc1ef8a244bde281dc142b116477c7147758aa1bfa8239828bd823e
probe_command: >-
  rg -n '_OPENROUTER_BASE_URL|GLM.*base.*URL|LLM_CODER_URL' src/omnimarket/config/settings.py src/omnimarket/nodes/node_swarm_fleet_discovery_effect/handlers/handler_swarm_fleet_discovery.py
  src/omnimarket/nodes/node_build_loop_orchestrator/assemble_live.py ; echo "regex-exit:$?"   # exit 1
  == zero hits == PASS # omnimarket OMN-12805 worktree @ 76d4714
probe_stdout: |
  === dod-001 probe: zero regex hits in the three code-literal files ===
  regex-exit:1 (1 = zero hits = PASS)
actual_output: >-
  PASS: the DoD regex returns ZERO hits across the three named code-literal sites (config/settings.py,
  node_swarm_fleet_discovery_effect/handlers/handler_swarm_fleet_discovery.py, node_build_loop_orchestrator/assemble_live.py).
  Each affected call site resolves its URL through the routing authority — OpenRouter base URL from OPENROUTER_BASE_URL
  config (ValueError when unset) and the local coder URL/model from ModelPolicyLoader over model_policy.yaml
  — and fails closed when unconfigured, proven by test_openrouter_base_url_unset_fails_closed and the
  golden-chain fail-closed test. Verifier (claude-code-review) is distinct from runner (claude-code).
