---
schema_version: "1.0.0"
ticket_id: "OMN-13159"
evidence_item_id: "dod-delegation-deploy-assessment"
check_type: "command"
check_value: >-
  Deploy assessment: confirm the delegation complete-URL fix performs no live deploy, restart, topic mutation,
  or .201 mutation; the fix takes effect on the next dev-lane redeploy that bakes omnimarket origin/dev
  HEAD.
status: PASS
run_timestamp: "2026-06-15T16:10:00Z"
contract_sha256: "sha256:7673be65dcb1b6881d663da3ada6e837ad6ca03ed342682a485bff48b5cf033b"
commit_sha: "c823b4fad44a6c4fa71d48bc083ee94e873cd2a4"
branch: "jonah/omn-13159-delegation-complete-urls"
runner: "worktree-agent-omn-13159-delegation-complete-urls"
verifier: "receipt-gate-local"
probe_command: >-
  git -C omni_worktrees/OMN-13159/omnimarket diff --stat origin/dev...HEAD ; git -C omni_worktrees/OMN-13159/omnibase_infra
  diff --stat origin/dev...HEAD
probe_stdout: |
  $ git -C omni_worktrees/OMN-13159/omnimarket diff --stat origin/dev...HEAD
  10 files changed, 240 insertions(+), 54 deletions(-)
  # scripts/ci, src/omnimarket/configs/bifrost_delegation.yaml,
  # src/omnimarket/models/delegation/wire/model_bifrost_delegation_config.py,
  # src/omnimarket/nodes/.../port_direct_curl_dispatch.py, tests/...
  # No Dockerfile, no docker/migrations/*, no DDL, no deploy script touched.

  $ git -C omni_worktrees/OMN-13159/omnibase_infra diff --stat origin/dev...HEAD
  3 files changed, 18 insertions(+), 16 deletions(-)
  # src/omnibase_infra/runtime/render_bifrost_delegation_contract.py + 2 tests.
  # No Dockerfile, no docker/migrations/*, no DDL, no deploy script touched.
actual_output: >-
  PASS: No live deploy. Both PRs are source/test only. The DirectCurl port fix (omnimarket #1228) takes
  effect on the next dev-lane redeploy that bakes omnimarket origin/dev HEAD; the renderer rename (omnibase_infra
  #1989) takes effect when the deployed contract is re-rendered at the next container boot. Neither PR
  performs a live deploy, restart, topic mutation, or .201 mutation. The bus runtime path was already
  verbatim-correct (OMN-12815) and was NOT the source of the 404. Do not claim deployed from these PRs.
exit_code: 0
working_dir: "/Users/jonah/Code/omni_home"
