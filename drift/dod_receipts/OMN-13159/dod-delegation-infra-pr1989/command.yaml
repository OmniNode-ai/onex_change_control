---
schema_version: "1.0.0"
ticket_id: "OMN-13159"
evidence_item_id: "dod-delegation-infra-pr1989"
check_type: "command"
check_value: >-
  cd omni_worktrees/OMN-13159/omnibase_infra && uv run ruff check src/ tests/ && uv run ruff format --check
  src/ tests/ && uv run mypy src/omnibase_infra/runtime/render_bifrost_delegation_contract.py --strict
  && uv run pytest tests/unit/runtime/test_render_bifrost_delegation_contract.py tests/integration/test_volume_config_drift_provenance_e2e.py
  -v
status: PASS
run_timestamp: "2026-06-15T16:05:00Z"
contract_sha256: "sha256:7673be65dcb1b6881d663da3ada6e837ad6ca03ed342682a485bff48b5cf033b"
commit_sha: "cbce7cb0c08524af7311acec39f6dbbec701dc5e"
branch: "jonah/omn-13159-delegation-complete-urls"
pr_number: 1989
runner: "worktree-agent-omn-13159-delegation-complete-urls"
verifier: "receipt-gate-local"
probe_command: >-
  gh pr view 1989 --repo OmniNode-ai/omnibase_infra --json number,url,headRefOid,headRefName,baseRefName,state
  ; uv run ruff check src/ tests/ ; uv run ruff format --check src/ tests/ ; uv run mypy src/omnibase_infra/runtime/render_bifrost_delegation_contract.py
  --strict ; uv run pytest tests/unit/runtime/test_render_bifrost_delegation_contract.py tests/integration/test_volume_config_drift_provenance_e2e.py
  -v ; uv run pytest tests/unit/ -m "not slow"   (broad unit suite, NO -k filter)
probe_stdout: |
  $ gh pr view 1989 --repo OmniNode-ai/omnibase_infra --json number,url,headRefOid,headRefName,baseRefName,state
  number=1989
  url=https://github.com/OmniNode-ai/omnibase_infra/pull/1989
  baseRefName=dev
  headRefName=jonah/omn-13159-delegation-complete-urls
  headRefOid=cbce7cb0c08524af7311acec39f6dbbec701dc5e
  state=OPEN

  $ uv run ruff check src/ tests/
  All checks passed!

  $ uv run ruff format --check src/ tests/
  4112 files left unchanged

  $ uv run mypy src/omnibase_infra/runtime/render_bifrost_delegation_contract.py --strict
  Success: no issues found in 1 source file

  $ uv run pytest tests/unit/runtime/test_render_bifrost_delegation_contract.py tests/integration/test_volume_config_drift_provenance_e2e.py -v
  25 passed in 0.36s

  $ uv run pytest tests/unit/ -m "not slow"   # broad unit suite, NO -k filter
  1 failed, 20477 passed, 15 skipped, 14 deselected, 8629 warnings in 239.91s
  # the 1 failure is tests/unit/verification/test_cli.py::TestCLIMain::test_registration_only
  # (asserts a CLI exit code in (1,2); got 0). PROVENANCE CHECK: stashed this change, re-ran on
  # the clean dev base a96fb355f -> identical failure. Pre-existing on dev; the verification CLI
  # imports nothing this PR touches. NOT introduced by this change.
actual_output: >-
  PASS: omnibase_infra PR #1989 (branch jonah/omn-13159-delegation-complete-urls, base=dev, head cbce7cb0c08524af7311acec39f6dbbec701dc5e)
  renames the bifrost render-hint field base_url_env -> endpoint_url_env in render_bifrost_delegation_contract
  (env value materialized into endpoint_url verbatim; no base+path concat) and updates its tests. ruff
  + ruff format clean; mypy --strict clean on the changed module; changed-area tests 25 passed; broad
  unit suite 20477 passed / 15 skipped with 1 pre-existing-on-dev failure (verification CLI exit code)
  proven identical on the clean base with this change stashed, outside this change's blast radius. SCOPE:
  render-hint rename only — NO DDL, NO migration, NO live deploy, NO restart, NO .201 mutation by this
  PR. The full integration suite needs the .201 Kafka/PG stack and is run by CI against real infra, not
  locally.
exit_code: 0
working_dir: "/Users/jonah/Code/omni_home"
