---
schema_version: "1.0.0"
ticket_id: "OMN-16109"
evidence_item_id: "dod-inventory-lists-tenant-event-outbox-migration"
check_type: "command"
check_value: >-
  body="$(cat src/onex_change_control/boundaries/migration_inventory.yaml)" && printf '%s' "$body" | grep
  -qF '20260816_tenant_event_outbox.sql' && printf '%s' "$body" | grep -A1 -F '20260816_tenant_event_outbox.sql'
  | grep -qF 'tables: [tenant_event_outbox]'
status: PASS
run_timestamp: "2026-08-17T02:25:18Z"
commit_sha: "37282fbed767918ce3f563cf513e8e456f8cb891"
runner: "occ-migration-inventory-fix"
verifier: "team-lead"
probe_command: >-
  grep -n '20260816_tenant_event_outbox.sql\|tenant_event_outbox\]' src/onex_change_control/boundaries/migration_inventory.yaml
probe_stdout: |
  579:          - file: "20260816_tenant_event_outbox.sql"
  580:            tables: [tenant_event_outbox]
exit_code: 0
actual_output: >-
  PASS: the OCC boundary inventory now lists 20260816_tenant_event_outbox.sql under the omninode_cloud/omninode_infra
  migration set with tables: [tenant_event_outbox], matching the migration file's own DDL (a single CREATE
  TABLE plus a supporting partial index on that same table). Red->green proof against a CI-shaped repos-root
  built from freshly-pulled/cloned peer default-branch trees (omnibase_infra, omnidash-archived, omniintelligence,
  omnimemory, omninode_infra, omniclaude, omniweb): check-migration-inventory exited 1 with the exact
  CI error (`[ERROR] UNLISTED_FILE: omninode_cloud: omninode_infra/db/migrations/20260816_tenant_event_outbox.sql
  exists on disk but not in inventory`) against the pre-fix inventory, and exited 0 with "Migration inventory:
  all files accounted for." against the fixed inventory. Falsifiability of this receipt's own check_value
  was proven in both directions: it exits 1 against the pre-fix inventory (the entry is genuinely absent)
  and exits 0 against the fixed inventory. The same repos-root was swept for other drift across all six
  database sections in the inventory (omnibase_infra, omniintelligence, omnidash_analytics, omnimemory,
  omninode_cloud, omniclaude, omniweb); none was found -- this was the only unlisted-file gap.
branch: "jonah/omn-16109-migration-inventory-tenant-event-outbox"
contract_entry_sha256: "sha256:2cd807cb23d14fa363371ddf66e68dcaf35195df72d3189b44a1adfd79bde3e0"
