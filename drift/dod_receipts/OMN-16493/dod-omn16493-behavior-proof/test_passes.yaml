---
schema_version: 1.0.0
ticket_id: OMN-16493
evidence_item_id: dod-omn16493-behavior-proof
check_type: test_passes
check_value: |
  OMN15683_REQUIRE_PG=1 OMNIBASE_INFRA_DB_URL= POSTGRES_HOST= PATH="/usr/lib/postgresql/16/bin:/opt/homebrew/opt/postgresql@16/bin:$PATH" uv run pytest tests/integration/migrations/test_omn15683_mixed_representation_conversion.py -q -k "red_control or successor_converts or policy_and_force_rls or slug_rows_resolve or already_canonical"
contract_sha256: sha256:368c1df98f1b9e3e21681c78045217a871645b44e17304c14da4915c7927b297
contract_entry_sha256: sha256:ecaab3f3c5d2e57859e846e57de12c7baabb02de960cf5b355fbd4481411e1d3
status: PASS
run_timestamp: '2026-09-10T03:45:26Z'
commit_sha: 3dfe23e5e5e41f6b0c8a7501e0a6883e2dbd0a42
runner: claude-local-worktree
verifier: omn16493-ac6-readback-close
probe_command: |
  gh api repos/OmniNode-ai/omnibase_infra/commits/3dfe23e5e5e41f6b0c8a7501e0a6883e2dbd0a42 --jq .sha && cd "$OMNI_HOME/omni_worktrees/OMN-16493/omnibase_infra" && OMN15683_REQUIRE_PG=1 OMNIBASE_INFRA_DB_URL= POSTGRES_HOST= PATH="/usr/lib/postgresql/16/bin:/opt/homebrew/opt/postgresql@16/bin:$PATH" uv run pytest tests/integration/migrations/test_omn15683_mixed_representation_conversion.py -q -k "red_control or successor_converts or policy_and_force_rls or slug_rows_resolve or already_canonical"
probe_stdout: |
  3dfe23e5e5e41f6b0c8a7501e0a6883e2dbd0a42
  6 passed, 6 deselected in 3.72s
actual_output: |
  PASS. AC2 / AC3 / AC4 behaviour proof -- the migration itself, EXECUTED, answering the closer's
  gap_no_behavior_proof hold on sweep run 34431989659 (5 verified, 0 failed, behavior_proving_count 0).

  WHY THIS ITEM EXISTS. Every other item on this contract is a content read at a pinned ref. That is
  not laziness: it is the form the OCC compliance runner's own source names as the ONLY admissible
  hermetic shape for a fact it cannot probe live, and it is what the runner forced -- the first
  revision of the AC1 checks read the migrate Job log through the Actions API and BLOCKED on
  cross-repo token scope. A content read can never classify as BEHAVIOR. This item is the executable
  half.

  WHAT WAS RUN, and against what.
  tests/integration/migrations/test_omn15683_mixed_representation_conversion.py applies the REAL
  vendored 0037 bytes through the same `psql -v ON_ERROR_STOP=1 -f <file>` invocation the deploy-time
  runner uses, against a real PostgreSQL 16 cluster seeded to the onex-dev ROW CENSUS and -- since
  0037 -- to its OWNERSHIP AND GRANT TOPOLOGY: delegation_events owned by one role, tenant_registry_mirror
  by another whose ACL does not name the first. The census carries the identities this ticket names:
  beta-business-proof 91c74442-1233-4c97-b191-911a10346fdf as 151 slug rows PLUS 21 already-stamped
  canonical-UUID rows (the mixture), and the house tenant omninode 820272f9-4aaf-5add-a2df-0af942852ab2
  as 43 slug rows.

  THE SIX SELECTED TESTS, criterion by criterion.
    AC2  test_successor_converts_the_mixed_column        -- the column ends as uuid
         test_policy_and_force_rls_are_restored_as_uuid  -- FORCE RLS back on, policy restated
                                                            comparing uuid to uuid (the ::uuid cast)
    AC3  test_slug_rows_resolve_to_their_mirror_uuid     -- every slug row lands on its mirror uuid
         test_already_canonical_rows_pass_through_unchanged -- a stamped row resolves TO ITSELF by
                                                            registry resolution, not by a bypass
                                                            branch; the two populations collapse onto
                                                            one identity with per-tenant row
                                                            arithmetic asserted. This is the
                                                            no-row-is-stranded claim EXECUTED rather
                                                            than counted.
    AC4  the same slug test asserts, for the house tenant specifically, that after the apply no
         `omninode` row holds any value other than 820272f9-4aaf-5add-a2df-0af942852ab2.

  TWO RED CONTROLS, so the selection discriminates rather than merely passing.
    test_red_control_predecessor_aborts_on_the_mixed_column      -- 0034 ABORTS on this data: it
        resolves on m.tenant_slug alone and has no branch for a row already holding the canonical UUID.
    test_red_control_0036_aborts_on_the_cross_owner_mirror_read  -- 0036 ABORTS on the grant split with
        `permission denied for table tenant_registry_mirror`, the exact failure measured live on
        staging deploy run 34281092205.
  A harness that could not tell 0037 from its two retired predecessors would fail both of these.

  SCOPE OF THE CLAIM, stated rather than implied. This proves the conversion SEMANTICS the criteria
  describe. It does NOT prove the onex-dev apply -- that is AC1's governed-Job evidence and the live
  readback -- which is why AC1 and AC5 are deliberately NOT bound here. The distinction is not
  pedantic on this ticket: 0036's lab proof was GREEN while onex-dev was not, because the
  reconstruction reproduced DATA and not OWNERSHIP. 0037's harness was fixed in the same change to
  seed the grant topology, and the second RED control above is that fix being exercised.

  EXECUTION AND BUDGET. Run 2026-09-10 in a dedicated omnibase_infra worktree at origin/dev, pinned by
  reading the merged 0037 sha first: 6 passed, 6 deselected, 3.72s of pytest and 8.4s wall including
  interpreter start, against the runner's 30s per-check budget. The full twelve-test module also
  passes (6.8s pytest, 11.9s wall); the focused six is bound to leave headroom, per the ruling.

  THE COMMAND PINS ITS OWN ENVIRONMENT so the result can be neither vacuous nor accidental.
    OMN15683_REQUIRE_PG=1   turns EVERY skip path in the suite into a hard failure, so a runner with no
                            Postgres toolchain fails loudly instead of exiting 0 with all tests skipped.
    OMNIBASE_INFRA_DB_URL=  blanked, and POSTGRES_HOST= blanked, so the suite takes its hermetic
                            initdb-and-throwaway-cluster path deterministically rather than binding to
                            whatever ambient database a runner happens to carry.
    PATH=                   names the two places a PostgreSQL 16 toolchain actually lives -- a hosted
                            Linux runner and a developer Mac -- because initdb and pg_ctl ship off-PATH
                            on both.

  DISPOSITION ON EACH GATE, measured rather than assumed. With OMNI_HOME unset (the hosted OCC
  runner's state) the compliance gate returns NOT-EVALUATED for this check and 0 BLOCK, the declared
  behaviour for a cwd it cannot resolve. With OMNI_HOME set to a local canonical clone the check FAILS,
  because that clone's venv deliberately carries omnimarket for the dod_verify path and the OMN-15620
  purity gate exits before pytest -- a local-clone artifact, not a statement about this evidence.
exit_code: 0
pr_number: 3340
branch: dev
