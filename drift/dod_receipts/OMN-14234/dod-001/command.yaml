---
schema_version: "1.0.0"
ticket_id: "OMN-14234"
evidence_item_id: "dod-001"
check_type: "command"
check_value: >-
  independent retry-local best-of-N behavior verification (both paths) + delegation regression at product
  head d757236f235427c6f8ae493830486f8ae19ebb0f
status: PASS
run_timestamp: "2026-07-10T19:40:00Z"
commit_sha: "d757236f235427c6f8ae493830486f8ae19ebb0f"
runner: "retry-local-builder"
verifier: "build-verifier"
pr_number: 1694
probe_command: >-
  env -u PYTHONPATH uv run pytest tests/unit/delegation/test_retry_local_omn14234.py tests/unit/nodes/node_delegate_skill_orchestrator/test_local_dispatch_retry_local_omn14234.py
  -q; then env -u PYTHONPATH uv run pytest tests/unit/delegation/ tests/unit/nodes/node_delegate_skill_orchestrator/
  -q
probe_stdout: |
  omnimarket.__file__ = .../omni_worktrees/OMN-14234-verify/omnimarket/src/omnimarket/__init__.py
  new retry-local suites (bus FSM + CLI port): 7 passed in 0.63s
  bus-FSM budget-exhausted-then-escalate: i1/i2 min_tier_name == "local", escalation_count == 0 across
    both retries, then routing min_tier_name == "cheap_cloud" with escalation_count == 1
  bus-FSM retry-then-pass: escalation_count == 0, terminal cumulative_attempt_cost == 0.0
  bus-FSM paid-tier gate-fail: local_retry_count == 0, escalation_count == 1 (no retry)
  CLI port budget-exhausted: effect.calls == ["local", "local", "local", "cheap_cloud"], escalation_count == 1
  CLI port paid gate-fail: effect.calls == ["cheap_cloud", "claude"] (no retry)
  full delegation + delegate-skill dirs: 680 passed, 2 skipped in 65.36s
actual_output: >-
  PASS: independent verification at product head d757236f235427c6f8ae493830486f8ae19ebb0f by build-verifier
  (distinct from the implementer retry-local-builder, satisfying the receipt-honesty runner!=verifier
  gate). In a clean worktree (source path confirmed to resolve to the PR head, not ambient), both new
  retry-local suites pass and prove N local drafts run BEFORE any escalation off the free tier on BOTH
  paths: the bus FSM re-routes to the SAME free tier (min_tier_name pinned to "local") with escalation_count
  staying 0 across the retries and incrementing exactly once only when the per-tier budget is exhausted;
  the CLI port issues effect.calls == [local, local, local, cheap_cloud]. Fail-closed is proven: a paid
  tier is never retried (local_retry_count == 0, exactly one call per tier). Cost/provider gating keys
  off the typed tier cost model (EnumTierCostType.FREE_LOCAL via is_free_tier) and the terminal payload
  (cumulative_attempt_cost / actual_cost_usd), never exit_code. The new GATE_EVALUATED -> ROUTED FSM edge
  is contract-declared. The full delegation + delegate-skill test dirs pass (680 passed, 2 skipped) -
  no regression in existing escalation mechanics.
exit_code: 0
branch: "jonah/omn-14234-retry-local"
contract_sha256: "sha256:d033b06544b5640c19597572c43293c2ea38b7723e61179be6423748980f6c84"
