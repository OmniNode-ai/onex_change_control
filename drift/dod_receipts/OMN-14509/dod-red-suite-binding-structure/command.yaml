---
schema_version: "1.0.0"
ticket_id: "OMN-14509"
evidence_item_id: "dod-red-suite-binding-structure"
check_type: "command"
check_value: >-
  F=$(gh api "repos/OmniNode-ai/omnibase_infra/contents/tests/integration/test_state_io_outbox_seam_red.py?ref=455ca16a57037f9c1360951f1d875d846f36c326"
  --jq .content | base64 -d); R=$(printf '%s' "$F" | grep -c 'def test_redelivery_of_same_input_resumes_publish_from_row_without_rerunning_handler');
  G=$(printf '%s' "$F" | grep -c 'def test_e1_guard_causation_and_envelope_ids_must_differ'); S=$(printf
  '%s' "$F" | grep -c '"strict": True, "raises": AssertionError'); X=$(printf '%s' "$F" | grep -c '@pytest.mark.xfail');
  C=$(printf '%s' "$F" | grep -c 'causation_envelope_id'); test "$R" -ge 1 && test "$G" -ge 1 && test
  "$S" -ge 1 && test "$X" -ge 6 && test "$C" -ge 1
contract_entry_sha256: "sha256:94191db5f27dc475199f8bfdf7c22ec3bfd8da9b004594a44f0a33ab76862d8e"
status: PASS
run_timestamp: "2026-07-13T01:34:50Z"
commit_sha: "455ca16a57037f9c1360951f1d875d846f36c326"
runner: "seam-test-author"
verifier: "seam-test-author-non-implementer"
pr_number: 2284
probe_command: >-
  gh pr view 2284 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid
probe_stdout: |
  {"headRefOid":"455ca16a57037f9c1360951f1d875d846f36c326","number":2284,"state":"OPEN"}
actual_output: >-
  PASS at head 455ca16a57037f9c1360951f1d875d846f36c326. resume-test(§11.11)=1, e1-meta-guard=1, strict+raises=AssertionError
  xfail kw=1, xfail markers=6, causation_envelope_id refs=4. RED proven against two EXISTS-but-WRONG mutations
  of this same head content (not merely absence): (1) stripping the raises=AssertionError constraint --
  a file that still exists with 6 xfails but whose REDs are now VACUOUS -- drops the strict-kw count to
  0 -> exit 1; (2) deleting the §11.11 resume test drops its count to 0 -> exit 1. The suite's own runtime
  RED was observed separately with --runxfail: 6 failed, 2 passed, each on its named AssertionError (e.g.
  "assert 0 == 3" for the crash-recovery case; "assert UUID('d7032b8a-...') != UUID('d7032b8a-...')" for
  the id collision -- both FSM legs computed the identical UUID).
exit_code: 0
branch: "jonah/omn-14509-red-suite-companion"
