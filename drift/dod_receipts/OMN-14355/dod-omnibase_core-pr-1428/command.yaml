---
schema_version: "1.0.0"
ticket_id: "OMN-14355"
evidence_item_id: "dod-omnibase_core-pr-1428"
check_type: "command"
check_value: "cd omnibase_core && env -u PYTHONPATH uv run pytest tests/unit/scripts/ci/test_canonical_handler_shape.py
  -q"
status: PASS
run_timestamp: "2026-07-11T03:15:53Z"
commit_sha: 050e55d200fda1d9a852f74c534deec3a92cd098
runner: "claude-opus-4-8"
verifier: "canon-shape-gate-independent-verify"
probe_command: >-
  cd /Users/jonah/Code/omni_home/omni_worktrees/OMN-14355/omnibase_core-occverify (detached at PR head)
  && env -u PYTHONPATH uv run pytest tests/unit/scripts/ci/test_canonical_handler_shape.py -q; env -u
  PYTHONPATH uv run python scripts/ci/canonical_handler_shape.py --full; env -u PYTHONPATH uv run python
  scripts/ci/canonical_handler_shape.py --scope changed; env -u PYTHONPATH uv run ruff check scripts/ci/canonical_handler_shape.py
  scripts/ci/canonical_handler_shape_baseline.py tests/unit/scripts/ci/test_canonical_handler_shape.py;
  git diff --stat origin/dev...HEAD
probe_stdout: |
  33 passed in 1.40s
  --full: Canonical handler-shape ratchet WARNING (OMN-14355, full) -- non-blocking: 6 baselined non-canonical
  node(s). Canonical handler-shape ratchet OK (full) -- checked 8 node(s); new=0 unproven_flips=0 warn=6. exit=0
  --scope changed: Canonical handler-shape ratchet WARNING (OMN-14355, full (escalated: gate/baseline/receipt
  changed (scripts/ci/adequacy_receipts/README.md))) -- checked 8 node(s); new=0 unproven_flips=0 warn=6. exit=0
  ruff check: All checks passed!
  git diff --stat origin/dev...HEAD: 7 files changed, 1387 insertions(+), 1 deletion(-) -- ci.yml, .pre-commit-config.yaml,
  pyproject.toml, scripts/ci/adequacy_receipts/README.md, scripts/ci/canonical_handler_shape.py,
  scripts/ci/canonical_handler_shape_baseline.py, tests/unit/scripts/ci/test_canonical_handler_shape.py
  (scripts + CI-config + tests only -- no runtime node-handler path touched)
actual_output: >-
  PASS. Independently re-verified (not self-authored by the implementer) against omnibase_core PR #1428
  head 050e55d200fda1d9a852f74c534deec3a92cd098 in an isolated detached worktree with PYTHONPATH cleared.
  All 33 unit tests in tests/unit/scripts/ci/test_canonical_handler_shape.py pass. The ratchet gate script
  itself was executed directly (not merely trusted from the PR body): --full reports 8 nodes checked,
  new=0, unproven_flips=0, warn=6, exit 0, matching the claimed canary classification (2 canonical / 6
  baselined non-canonical); --scope changed correctly fail-closed-escalates to full when a file the classifier
  resolves through (scripts/ci/adequacy_receipts/README.md) changed, exit 0. ruff clean on all three changed
  Python files. git diff --stat against origin/dev confirms the PR touches only scripts, CI config, and
  tests -- no runtime node-handler path -- consistent with Deploy Gate reporting SKIPPED (deploy-neutral)
  on the live code PR, so no dod-deploy-assessment evidence is required. `gh pr checks 1428` independently
  confirms Code Quality (the CI job running the ratchet gate) is SUCCESS; the ~40 other red checks on
  the PR are the occ-preflight/receipt-honesty/Canonical-Inference-Gate cascade this OCC companion resolves,
  not a defect in the gate under test.
exit_code: 0
pr_number: 1428
branch: "jonah/omn-14355-canonical-shape-ratchet-gate"
contract_sha256: "sha256:ffcb398c5539015cbd4636f7659bac50165bf4b10e294379e3e62104ce887320"
