---
schema_version: "1.0.0"
ticket_id: OMN-12775
evidence_item_id: dod-omnimarket-pr-1113
check_type: command
check_value: >-
  gh pr diff 1113 --repo OmniNode-ai/omnimarket | grep -q 'compute_generation_proof_fields' && gh pr diff
  1113 --repo OmniNode-ai/omnimarket | grep -q 'output_payload_sha256' && gh pr diff 1113 --repo OmniNode-ai/omnimarket
  | grep -q 'resolved_endpoint' && gh pr diff 1113 --repo OmniNode-ai/omnimarket | grep -q 'projection_owner'
status: PASS
run_timestamp: "2026-06-08T16:55:00Z"
commit_sha: aeb40e43547fcaaa89e7ae28bc878e3fbd0cc3f9
runner: omnimarket-a3-agent
verifier: occ-receipt-verifier
probe_command: >-
  gh pr diff 1113 --repo OmniNode-ai/omnimarket | grep -E 'output_payload_sha256|compute_generation_proof_fields|projection_owner|resolved_endpoint'
probe_stdout: |
  +  proof fields off the generation_events row (output_payload_sha256,
  +  contract_sha256, handler_sha256, routing_source, resolved_endpoint,
  +  projection_owner) but nothing produced them, so the canonical projection API
  +  (compute_generation_proof_fields) so they cannot drift; the SHA256 fields are
  +  routing_source + resolved_endpoint resolved verbatim from the routing authority;
  +        self._resolved_endpoint: str = ""
  +            self._resolved_endpoint = resolved.endpoint_url
  +            resolved_endpoint=self._resolved_endpoint,
  +    resolved_endpoint: str = Field(
  +        - output_payload_sha256
  +        - resolved_endpoint
  +        - projection_owner
actual_output: >-
  PASS: omnimarket PR #1113 head aeb40e43547fcaaa89e7ae28bc878e3fbd0cc3f9 populates the six generation_events
  proof fields in the write path via the shared compute_generation_proof_fields helper (used by both HandlerProjectionDelegation
  and DelegationProjectionRunner), captures resolved_endpoint verbatim from the routing authority on the
  benchmark, and lists the six proof columns in the projection_api exposure (canonical read path). Independent
  verifier (occ-receipt-verifier) ran the probe against the pushed PR diff; runner (omnimarket-a3-agent)
  authored the change.
exit_code: 0
pr_number: 1113
branch: jonah/omn-12775-generation-events-proof-fields
working_dir: /Users/jonah/Code/omni_home/omni_worktrees/OMN-12775-closeloop/omnimarket
contract_sha256: "sha256:2341f26f7f0489e8ecd2d65880e77992e8ba3ecae72654fd19d634d9a52111e4"
