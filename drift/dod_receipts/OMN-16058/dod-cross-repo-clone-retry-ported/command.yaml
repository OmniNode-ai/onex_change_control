---
schema_version: "1.0.0"
ticket_id: "OMN-16058"
evidence_item_id: "dod-cross-repo-clone-retry-ported"
check_type: "command"
check_value: "gh api \"repos/OmniNode-ai/onex_change_control/contents/.github/workflows/ci.yml?ref=5d60a3f74b4e0f76fbac8a85dbc6e7e52ee2a0c6\"
  --jq '.content' | base64 -d | grep -c 'for attempt in 1 2 3; do' | grep -cx 3"
contract_sha256: "sha256:ca9488c18dff08a0ea15d74c6e5375f01633d4db24ae015475ea7496512a785b"
contract_entry_sha256: "sha256:936ba887c67308fa0fa3ffd408502060349c75b3803fad918f7196c631e123ba"
status: PASS
run_timestamp: "2026-08-17T07:44:38Z"
commit_sha: "8c2f1d960260bcc3bb723fec69c94351bfa14cc2"
runner: "occ-receipts-pair-6500-rebind-mint"
verifier: "occ-receipts-pair-6500-rebind-verify"
probe_command: "gh api \"repos/OmniNode-ai/onex_change_control/contents/.github/workflows/ci.yml?ref=5d60a3f74b4e0f76fbac8a85dbc6e7e52ee2a0c6\"
  --jq '.content' | base64 -d | grep -c 'for attempt in 1 2 3; do' | grep -cx 3"
probe_stdout: |
  # Root cause, verbatim from the two failing required jobs on OCC#6491:
  Kafka Boundary Parity (job 94736873447):
    fatal: unable to access 'https://github.com/OmniNode-ai/omnidash.git/': Empty reply from server
    ##[error]Process completed with exit code 128
  Migration Conflict Check (job 94736873369):
    error: RPC failed; curl 56 GnuTLS recv error (-9): Error decoding the received TLS packet.
    fatal: early EOF / fatal: fetch-pack: invalid index-pack output
    ##[error]Process completed with exit code 128

  # Port fidelity (independent verifier; origin/dev 0cc01e4ef vs HEAD):
  dev  L563-587 sha=ab77f680997273c8 (retry) | L629-639 sha=cd0ae621d493793f (no retry) | L671-695 sha=ab77f680997273c8 (retry)
  HEAD L269-293 sha=ab77f680997273c8 (retry) | L333-343 sha=cd0ae621d493793f (no retry) | L370-394 sha=ab77f680997273c8 (retry)
  omnidash site: dev L736-756 vs HEAD L421-441 -> empty diff, sha 034b2475b65cc69510c6c9f0f9a9cf2813e9f71c
  git diff origin/main HEAD -- .github/workflows/ci.yml -> exactly 3 hunks, no other edits

  # Live CI on GitHub runners, run 31794732236:
  Migration Conflict Check  PASS   (previously exit 128)
  Kafka Boundary Parity     PASS   (previously exit 128)

  # Re-mint 2026-08-16 (occ-clear-lane): check_value replaced with an executable pinned-ref probe
  # (the prior value was prose describing a hand-run method, not a command). Live run: counts the
  # three 'for attempt in 1 2 3; do' retry loops in ci.yml at the port's merge commit (5d60a3f7):
  # stdout 1, exit 0.

  # Rebind 2026-08-17 (OMN-16058 occ#6500/#6501 pair): this receipt was minted while #6500 and
  # #6501 ("main-side unwedge", branch hotfix/occ-db-boundary-exception-review, merged) carried
  # identical registry content, and was left bound to #6501 (pr_number 6501, commit_sha
  # ae4bfe842c5cdf02b47f708a06e167bdc38cb9d3, branch hotfix/occ-db-boundary-exception-review) —
  # onex_change_control's own occ-preflight/eligibility validates its in-tree receipts against
  # THIS PR's number/commit (invariant I3), so it reported pr_ticket_mismatch on #6500 even
  # though Contract Compliance Check (job 95312265233, run 31967125436, success) and CI Summary
  # (job 95314555683, run 31967125436, success) both passed on the same head. Re-ran the
  # underlying probe live against #6500's actual head 8c2f1d960260bcc3bb723fec69c94351bfa14cc2:
  # identical result (stdout 1, exit 0) -- the check itself never regressed, only the PR binding
  # was stale. pr_number/commit_sha/branch rebound below to #6500's own identity; contract_sha256
  # and contract_entry_sha256 are unchanged (the contract file itself was not touched).
actual_output: "PASS: the exit-128 failures were unretried cross-repo clone egress failures, not the fetch-depth
  or missing-ref problem first hypothesised — both died inside the clone step before any checked-out ref
  was used. dev already carried retry wrappers that main never received because main has been wedged since
  2026-07-27, so they are ported verbatim rather than reimplemented; an independent verifier confirmed
  every helper block is byte-identical to dev by sha256, including the deliberately unretried one. Three
  sites are covered: migration-conflicts, kafka-boundary-parity, and the omnidash clone in cross-repo-null-contract.
  The best-effort migration-inventory helper is correctly left unretried, matching dev, because its call
  site is 'clone_omni_repo ... || true' with stderr suppressed. The retry cannot mask a real outage: after
  3 attempts it emits ::error:: and returns 1, which bash -e propagates to a job failure. Both previously-128
  jobs pass in live CI."
exit_code: 0
pr_number: 6500
branch: "jonah/occ-db-boundary-exception-review-dev"
