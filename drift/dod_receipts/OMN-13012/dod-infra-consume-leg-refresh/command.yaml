---
schema_version: "1.0.0"
ticket_id: "OMN-13012"
evidence_item_id: "dod-infra-consume-leg-refresh"
check_type: "command"
check_value: "uv run pytest tests/integration/test_terminal_consumer_concurrent_assign_race.py -q && uv
  run mypy src/ --strict && uv run pytest tests/unit/runtime/ -q -n auto"
status: PASS
run_timestamp: "2026-06-13T05:10:00Z"
commit_sha: "4feb364feb6cbdcae932184eb8b5e3078947d98f"
branch: "jonah/omn-13012-infra-consume-leg"
pr_number: 1969
contract_sha256: "sha256:b75f2267063a3c4df1a3dd65ea5e70d14e0966d8c5749f4b886afdb4c61f122f"
runner: "omn-13012-infra-impl"
verifier: "receipt-gate-local"
probe_command: "uv run pytest tests/integration/test_terminal_consumer_concurrent_assign_race.py -q (RED
  before fix = bare TimeoutError, the live empty-message wait-failed signature; GREEN after fix)"
probe_stdout: |
  --- RED (pre-fix, infra-red-run-02.txt) ---
  src/omnibase_infra/runtime/service_terminal_event_consumer.py:203: in open
      self._submit(
  E   TimeoutError
  FAILED tests/integration/test_terminal_consumer_concurrent_assign_race.py::test_both_terminal_consumers_assign_when_metadata_is_slow
  FAILED tests/integration/test_terminal_consumer_concurrent_assign_race.py::test_no_terminal_consumer_thread_leak_across_repeated_opens
  2 failed in 60.80s
  --- GREEN (post-fix, infra-green-run-01.txt) ---
  2 passed in 0.64s
  --- runtime unit regression sweep (infra-unit-runtime.txt) ---
  4880 passed, 5 skipped in 27.96s
  --- mypy --strict ---
  Success: no issues found in 2435 source files
actual_output: >-
  PASS. The defect is proven RED-then-GREEN through the REAL runtime consume leg, not a handler-isolation
  test: the new integration test drives the REAL TerminalEventConsumer (the object wired as event_consumer
  in handler_wiring) through the REAL open_direct_terminal_consumer / poll_direct_terminal_consumer path,
  with AIOKafkaConsumer monkeypatched (the same harness shape as test_pattern_b_broker_terminal_waiter)
  to a fake that faithfully models aiokafka 0.13.0 AIOKafkaClient.set_topics future + metadata-latency
  semantics. RED: _assign_direct_terminal_partitions re-called set_topics([same_topic]) each loop iteration,
  which aiokafka treats as a no-op refresh, so a consumer whose first metadata fetch had not surfaced
  partitions burned the 30s assign cap and raised a bare TimeoutError — the exact empty-message 'terminal-event
  consumer: wait failed' observed live (only the FAILED topic ever subscribed). GREEN: the assign loop
  now registers the topic once and awaits its metadata future, then forces a fresh fetch via force_metadata_update
  on each miss; both ephemeral terminal consumers reliably assign and the correlated COMPLETED terminal
  is delivered to wait(). The bare TimeoutError is replaced with a diagnostic message. No topic / envelope
  / contract API change. The omnimarket handler is unchanged (the prior two-strike diagnosis proved it
  correct).
exit_code: 0
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-13012-infra/omnibase_infra"
