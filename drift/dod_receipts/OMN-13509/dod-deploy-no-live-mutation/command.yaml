---
schema_version: "1.0.0"
ticket_id: "OMN-13509"
evidence_item_id: "dod-deploy-no-live-mutation"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13509/dod-deploy-no-live-mutation/command.yaml"
contract_sha256: "sha256:67c69dd210805b0273ac2b86fad84bb3a027224fb11ee008a2c242dcd1eb4ec5"
status: PASS
run_timestamp: "2026-06-23T12:50:00Z"
commit_sha: "afb4b88da34368e4183f6147c6ef078e244c8c9b"
branch: "jonah/omn-13509-pin-hygiene-gate"
pr_number: 1360
runner: "claude-omn-13509-sea-dogfood-runner"
verifier: "claude-omn-13509-receipt-review"
probe_command: "cd ${OMNI_HOME}/omni_worktrees/OMN-13509/omnimarket && git diff --name-only origin/dev...HEAD
  -- 'src/**' | grep -vE 'validator_corpora|__init__' || echo 'no runtime-behavior file changed (corpus
  DATA + driver only)'"
probe_stdout: |
  no runtime-behavior file changed (corpus DATA + driver only)
actual_output: >-
  PASS (no live deploy mutation): the OMN-13509 PRs add a PR-time pin-hygiene validation gate (a pure
  COMPUTE node run over the in-memory event bus at pre-commit time + a required CI gate), the generation
  acceptance corpus + driver script, and the OCC contract/receipts. The only omnimarket src/ change under
  a nodes/ path is the acceptance-corpus DATA file (validator_corpora/corpus_pin_hygiene.py) and its __init__
  registration — DATA consumed by the generation driver, not runtime node behavior. No runtime node deploy,
  no live-broker Kafka topic, no migration, no image-build input, no .201 mutation, no live deploy. The
  two new pin-hygiene topic constants are pre-commit / in-memory transport names, not live-broker topics.
exit_code: 0
