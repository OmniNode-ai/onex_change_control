---
schema_version: 1.0.0
ticket_id: OMN-15320
evidence_item_id: dod-omn15320-ac2-rationale-cites-cold-pull
check_type: command
check_value: |
  gh api "repos/OmniNode-ai/omninode_infra/contents/.github/workflows/deploy-onex-staging.yml?ref=f157b36aa5b05f89edf644a2284ae81f73356fbd" --jq '.content' | base64 -d | grep -c '783,619,615-byte runtime image cold pull'
contract_sha256: "sha256:451d3ffbe7c79415781f88ade613c6397bf6ed2249265dcfdd18cd9ab5efd20b"
contract_entry_sha256: sha256:2c38a32f243b62417636f1532c2c2182a73d17ea7ca37e7f76458a370449d9be
status: PASS
run_timestamp: '2026-09-10T02:15:00Z'
commit_sha: f157b36aa5b05f89edf644a2284ae81f73356fbd
runner: claude-local-worktree
verifier: omn15320-ac1-bind
probe_command: |
  gh api "repos/OmniNode-ai/omninode_infra/contents/.github/workflows/deploy-onex-staging.yml?ref=f157b36aa5b05f89edf644a2284ae81f73356fbd" --jq '.content' | base64 -d | grep -c '783,619,615-byte runtime image cold pull'
probe_stdout: |
  1
actual_output: |
  PASS. AC2, re-executable at the merged squash sha f157b36aa5b05f89edf644a2284ae81f73356fbd
  (OmniNode-ai/omninode_infra#1271). All four command checks on this evidence item were executed
  against that sha and every one exited 0:

    1. rationale block cites the cold-pull measurement
       ('783,619,615-byte runtime image cold pull')                 -> 1
    2. governed value is the live one ('DEPLOY_TIMEOUT_SECONDS: "1500"') -> 1
    3. the falsified estimate the ticket names ('30-60s for cold pull')  -> 0 hits, assertion passes
    4. gh pr view 1271 --json state,mergeCommit
       -> 'MERGED f157b36aa5b05f89edf644a2284ae81f73356fbd'

  NEGATIVE CONTROL at the pre-change parent 1cac47437ef58aff8af08b0a8b9f7dc95221dd2f: the
  cold-pull citation returns 0 and the falsified '30-60s for cold pull' estimate returns 1 —
  exactly inverted. The patterns therefore discriminate between the pre- and post-fix file rather
  than matching everywhere, so check 3's zero is a real absence and not a failed fetch.

  What AC2 asks: the wait budget is not shorter than the manifest's own startup allowance, and if a
  fixed number is kept its comment cites the cold-pull measurement it is based on rather than the
  '~30-60s for cold pull' rationale the founding run falsified. #1271 changes no timeout value, no
  manifest field and no deploy step — the diff is the rationale comment block plus the pytest
  assertions in tests/k8s/test_runtime_readiness_gate.py that stop the correction silently
  regressing. The governed 1500 was already raised by OMN-14974; only the prose was stale.

  This item supersedes dod-OmniNode-ai-omninode_infra-pr-1271, which binds the same PR but predates
  the binds_ac field and so declares no criterion. No claim in that item is withdrawn.
exit_code: 0
pr_number: 1271
branch: dev
