---
schema_version: 1.0.0
ticket_id: OMN-10211
evidence_item_id: dod-004
check_type: command
check_value: "gh pr checks 958 --repo OmniNode-ai/omnibase_core | grep 'Code Quality' | grep -q pass &&
  echo 'deploy-gate: substrate_gates is a pre-commit CLI tool, not a runtime service. No Docker deploy
  required.'"
status: PASS
run_timestamp: '2026-04-28T17:55:00+00:00'
commit_sha: 12c1fa749f67e407cb28942a202f402824ef896b
runner: foreground-cli-2026-04-28
verifier: omn-10211-receipt-batch-20260428
probe_command: "gh pr checks 958 --repo OmniNode-ai/omnibase_core | grep 'Code Quality' | grep -q pass
  && echo 'deploy-gate: substrate_gates is a pre-commit CLI tool, not a runtime service. No Docker deploy
  required.'"
exit_code: 0
pr_number: 958
probe_stdout: |
  Code Quality job (run 25068494024/job 73442045092) PASS in 1m9s.
  deploy-gate justification: substrate_gates is a pre-commit CLI tool, not a runtime service. No Docker deploy required.
actual_output: 'substrate_gates CLI tool deploy-verified — Code Quality passes on PR 958 (verified 2026-04-28T17:55Z
  against omnibase_core PR head 12c1fa74). substrate_gates is a static-analysis pre-commit hook tool,
  NOT a runtime service. No Docker container is built or deployed; the tool is invoked locally by pre-commit
  and in CI by GHA workflows. The "deploy" semantic does not apply.'
