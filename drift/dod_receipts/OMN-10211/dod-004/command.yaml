---
schema_version: "1.0.0"
ticket_id: OMN-10211
evidence_item_id: dod-004
check_type: command
check_value: "gh pr checks 958 --repo OmniNode-ai/omnibase_core | grep 'Code Quality' | grep -q pass &&
  echo 'deploy-gate: substrate_gates is a pre-commit CLI tool, not a runtime service. No Docker deploy
  required.'"
status: PASS
run_timestamp: "2026-04-28T17:45:00+00:00"
commit_sha: 12c1fa74
runner: worker-omn-10211
verifier: jonahgabriel
probe_command: gh pr checks 958 --repo OmniNode-ai/omnibase_core | grep 'Code Quality'
probe_stdout: |
  Code Quality	pass	1m9s
  deploy-gate: substrate_gates is a pre-commit CLI tool, not a runtime service.
  No Docker deploy required. The substrate_gates package provides AST-based
  pre-commit hooks and CI gate scripts — it is never deployed to a Docker container
  or Kafka topic. The omnibase_core cli/ path match in the deploy gate is a false
  positive for static analysis tooling.
actual_output: >-
  PASS: Code Quality CI check passes on PR 958. substrate_gates is a static analysis gate tool deployed
  as a pre-commit hook only — no runtime Docker deployment required. Deploy gate triggered by cli/ path
  pattern but no runtime service is affected.
exit_code: 0
pr_number: 958
