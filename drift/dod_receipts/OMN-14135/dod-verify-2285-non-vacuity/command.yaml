---
schema_version: 1.0.0
ticket_id: OMN-14135
evidence_item_id: dod-verify-2285-non-vacuity
check_type: command
check_value: |-
  v="$(mktemp)" && g="$(mktemp)" && gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/validators/handler_dispatch_entrypoint.py?ref=29c92ef28da5aa753c00388d64a2722382458a86" --jq .content | base64 -d > "$v" && gh api "repos/OmniNode-ai/omnibase_infra/contents/tests/unit/runtime/auto_wiring/test_handler_dispatch_entrypoint_coverage_omn14489.py?ref=29c92ef28da5aa753c00388d64a2722382458a86" --jq .content | base64 -d > "$g" && grep -F -- 'MIN_EXPECTED_DECLARED_HANDLERS' "$v" && grep -F -- 'vacuity guard' "$v" && grep -F -- 'def test_entrypoint_predicate_discriminates' "$g" && grep -F -- 'from omnibase_infra.validators.handler_dispatch_entrypoint import' "$g" && grep -F -- 'def test_validator_cli_passes_on_current_tree' "$g"
status: PASS
run_timestamp: '2026-07-13T03:15:00Z'
commit_sha: '0000000'
runner: claude-opus-4-8
verifier: occ-interim-manual-verify
pr_number: 2285
probe_command: |-
  gh api .../validator + gate test ?ref=29c92ef2 | base64 -d | grep -F <symbols>
probe_stdout: |
  validator @ 29c92ef2: MIN_EXPECTED_DECLARED_HANDLERS floor + vacuity guard -> a collapsed
  contract scan returns 1 instead of reporting success over an empty set.
  gate test: test_entrypoint_predicate_discriminates proves a submit()-only class (the
  HandlerA2ATask shape) is REJECTED while def-B handle / handle_async are ACCEPTED.
  gate test imports the validator module and drives main() via
  test_validator_cli_passes_on_current_tree - it exercises THE ARTIFACT THAT RUNS. exit 0.
exit_code: 0
actual_output: |-
  PASS: the gate cannot pass vacuously — an empty scan fails closed, the predicate is proven to discriminate, and the test drives the shipped validator rather than a re-implemented copy.
branch: jonah/omn-14135-occ-infra-2285-dispatch-entrypoint-gate
contract_entry_sha256: sha256:33c6a8bd8302b8b1406ae2bef7134bc1258f1049ca525df585572796854c8b39
