---
schema_version: "1.0.0"
ticket_id: "OMN-12744"
evidence_item_id: "dod-tailscale-share-config"
check_type: "command"
check_value: "Tailscale machine-share scoped to demo ports + app-layer auth configured — pending user
  approval"
contract_sha256: "TBD-pending-occ-pr-merge"
status: PENDING
run_timestamp: "2026-06-06T19:00:00Z"
commit_sha: "TBD-pending-config"
branch: "jonahgabriel/demo-evidence-bundle-7-tickets"
runner: "claude-code"
verifier: "occ-preflight-local"
probe_command: "tailscale share status && curl -s http://localhost:3001/health"
probe_stdout: |
  PENDING: Tailscale share not yet configured (requires user approval). Receipt scaffolded; update to PASS with tailscale share output + app auth config evidence.
actual_output: >-
  PENDING: dod-tailscale-share-config receipt scaffolded. Blocked on user approval (Operating Rule #2).
  Complete when Tailscale share is configured + app auth is verified.
exit_code: 0
pr_number: 0
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/demo-evidence/onex_change_control"
