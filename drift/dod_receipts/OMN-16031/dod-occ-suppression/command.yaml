---
schema_version: "1.0.0"
ticket_id: "OMN-16031"
evidence_item_id: "dod-occ-suppression"
check_type: "command"
check_value: |-
  gh api 'repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_baselines_quality/002_realign_quality_snapshots_to_producer_schema.sql?ref=dev' --jq .content | base64 -d | grep -c 'DROP TABLE IF EXISTS baselines_quality_snapshots'
contract_sha256: "sha256:d7d6833bf02c9666fa951804095aafc1a283f24619d75f03b93fa4cb8ecbd54c"
contract_entry_sha256: "sha256:4b81017514ab84d55186046550d56bdb6f7f3e7e2fef01818411641f87979d5a"
status: PASS
run_timestamp: "2026-08-13T22:52:00Z"
commit_sha: "48486f04367c3b4303ad5686b426ea2822e7a43b"
runner: "worktree-agent-omn-16031-occ-suppression"
verifier: "github-api-contents-readback"
probe_command: >-
  gh api 'repos/OmniNode-ai/omnibase_infra/contents/docker/migrations/forward/nodes/node_projection_baselines_quality/002_realign_quality_snapshots_to_producer_schema.sql?ref=dev'
  --jq .content | base64 -d | grep -c 'DROP TABLE IF EXISTS baselines_quality_snapshots'
probe_stdout: |
  1
actual_output: >-
  PASS: the suppression premise is proven on a surface this PR does not author. In omnibase_infra@dev,
  each of the two baselines projection nodes ships both a 001_create_*.sql that CREATEs the table and
  a 002_realign_*_to_producer_schema.sql that DROPs and recreates the same table against the real producer
  event schema — four independent gh-api content readbacks, all returning 1, each falsifiable (the same
  DROP probe against 001 returns 0 and exits 1). That is why the checker reports one table name with two
  disjoint column sets: intentional forward schema evolution held in append-only node migrations, not
  a duplicate table and not a cross-repo overlap. baselines_quality_snapshots and baselines_roi_snapshots
  were the only two unsuppressed conflicts blocking the OCC Migration Conflict Check; adding the two schema_evolution
  entries takes the gate command from "Found 2 migration conflict(s)" exit 1 to "No migration conflicts
  found, Suppressed 43 known conflict(s)" exit 0 against the same five-repo clone set the CI job builds.
  The 35 MISSING_COLUMN findings and the 6 SCHEMA_AMBIGUOUS tables are unchanged --warn-columns warnings
  (non-fatal) and were never the blocker. Verifier github-api-contents-readback is distinct from the worktree
  runner.
exit_code: 0
pr_number: 6452
branch: "jonah/omn-16031-suppress-baselines-snapshot-name-conflicts"
