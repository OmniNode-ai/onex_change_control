---
schema_version: "1.0.0"
ticket_id: "OMN-13572"
evidence_item_id: "dod-other-repos-green"
check_type: "command"
check_value: "for repo in <11 repos>; do diff <(consumer --spec dev) <(consumer --spec branch); done"
status: PASS
run_timestamp: "2026-06-24T18:30:00Z"
commit_sha: "1e93c7a32031cbf0dddb85f086ffca0a9c0e284c"
runner: "claude-omn-13572-builder"
verifier: "claude-receipt-review"
contract_sha256: "sha256:411422fde31125af7041841a26d5c202b9087c1231476f07dccb655106be07ac"
branch: "jonah/omn-13572-doc-content-scan-gate"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-13572/omnibase_core"
probe_command: "for repo in omniclaude omnibase_compat omnibase_spi omnibase_infra omnidash omnimarket
  omniintelligence omnimemory omninode_infra omniweb onex_change_control; do uv run python -m omnibase_core.validation.validator_requirements_consumer
  --repo $repo --repo-root <canonical_clone> --spec <dev-spec> > dev.txt; uv run python -m omnibase_core.validation.validator_requirements_consumer
  --repo $repo --repo-root <canonical_clone> --spec <branch-spec> > branch.txt; diff -q dev.txt branch.txt;
  done"
probe_stdout: |
  omniclaude: IDENTICAL gap set (dev spec == branch spec) — GREEN
  omnibase_compat: IDENTICAL gap set (dev spec == branch spec) — GREEN
  omnibase_spi: IDENTICAL gap set (dev spec == branch spec) — GREEN
  omnibase_infra: IDENTICAL gap set (dev spec == branch spec) — GREEN
  omnidash: IDENTICAL gap set (dev spec == branch spec) — GREEN
  omnimarket: IDENTICAL gap set (dev spec == branch spec) — GREEN
  omniintelligence: IDENTICAL gap set (dev spec == branch spec) — GREEN
  omnimemory: IDENTICAL gap set (dev spec == branch spec) — GREEN
  omninode_infra: IDENTICAL gap set (dev spec == branch spec) — GREEN
  omniweb: IDENTICAL gap set (dev spec == branch spec) — GREEN
  onex_change_control: IDENTICAL gap set (dev spec == branch spec) — GREEN
actual_output: |
  All 11 other governed repos produce a byte-identical validator-requirements gap set against the
  canonical origin/dev spec and this branch's spec. The spec change is purely additive: the new
  doc-content-scan validator is scoped applies_to_repos: [omnibase_core] (pilot), so _applies_to()
  returns False for every other repo and the consumer never checks them for the hook/keyword. Their
  validate-validator-requirements handshake gate is therefore unaffected. omnibase_core's own consumer
  stays baseline-clean (12 accepted gap(s)) because both the pre-commit hook and the CI job are wired.
  Fleet rollout to the 11 repos is OMN-13576.
exit_code: 0
duration_ms: 9000
