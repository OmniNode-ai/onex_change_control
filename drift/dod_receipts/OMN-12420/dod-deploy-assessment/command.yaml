---
schema_version: "1.0.0"
ticket_id: OMN-12420
evidence_item_id: dod-deploy-assessment
check_type: command
check_value: "git diff --name-only main..HEAD -- src/omnimarket/nodes/node_dod_verify"
contract_sha256: "sha256:9a692de27384b9001840acc59c6b916dc30f728feeb05b2a36e2b931c6592b4f"
status: PASS
run_timestamp: "2026-05-29T12:20:00Z"
commit_sha: 4d45458b086a8935d734b313802294df871de74d
runner: node-dod-verify-fix
verifier: automated
probe_command: "git diff --name-only main..HEAD -- src/omnimarket/nodes/node_dod_verify"
probe_stdout: |
  src/omnimarket/nodes/node_dod_verify/contract.yaml
  src/omnimarket/nodes/node_dod_verify/models/model_dod_verify_start_command.py
actual_output: >
  PASS (deploy assessment). This change touches a runtime path (node_dod_verify contract.yaml + ModelDodVerifyStartCommand)
  and DOES require a stability-test runtime redeploy to take effect on the deployed container. The deployed
  runtime currently runs omnimarket 0.4.2 (the broken source), so the deployed Kafka path (onex run-node)
  still times out until redeploy. The fix is proven against the fixed source: (1) the runtime dispatch
  callback, built with the contract's event_model, unwraps the wrapped envelope, validates ModelDodVerifyStartCommand
  with no ValidationError, and returns a ModelDispatchResult whose ModelDodVerifyState the runtime DispatchResultApplier
  publishes to onex.evt.omnimarket.dod-verify-completed.v1 (dod-001); (2) the CLI writes a guard-valid
  ModelDodReceipt dod_report.json (dod-002). Redeploy of the stability-test runtime is the documented
  follow-up, per OMN-12420 acceptance ("After merge + redeploy, generate receipts ... and move them to
  Done"). It is a live deploy and requires explicit user approval.
exit_code: 0
pr_number: 988
