---
schema_version: "1.0.0"
ticket_id: "OMN-14409"
evidence_item_id: "dod-verify-3990-kill-switch-present"
check_type: "command"
check_value: >-
  test "$(gh api "repos/OmniNode-ai/onex_change_control/contents/scripts/validation/check_contract_substance_floor.py?ref=41c0a3a20d20d70fe5be5f34884017a7a388524f"
  --jq .content | base64 -d | grep -c "GATE_SELF_REFERENTIAL")" -ge 1
status: PASS
run_timestamp: "2026-07-12T06:05:00Z"
commit_sha: "41c0a3a20d20d70fe5be5f34884017a7a388524f"
runner: "occ-queue-runner"
verifier: "occ-queue-verifier"
pr_number: 3990
probe_command: >-
  gh api "repos/OmniNode-ai/onex_change_control/contents/scripts/validation/check_contract_substance_floor.py?ref=41c0a3a20d20d70fe5be5f34884017a7a388524f"
  --jq .content | base64 -d | grep -n "GATE_SELF_REFERENTIAL"  # then the bound check_value re-run verbatim
  at OCC dev HEAD 5a27c309 and at a bogus all-zero SHA, as RED controls
probe_stdout: |
  # #3990 head 41c0a3a2 — GATE_SELF_REFERENTIAL occurs 5x, incl. the switch itself:
  246:GATE_SELF_REFERENTIAL = False
  109:# receipt>`), which GATE_SELF_REFERENTIAL deliberately leaves ungated. A partial,
  186:#     — the circular class, which GATE_SELF_REFERENTIAL deliberately leaves
  # RED control — the file does not exist on OCC dev at all:
  #   gh api .../check_contract_substance_floor.py?ref=5a27c309 -> HTTP 404 Not Found
  # bound check_value exit codes, run verbatim:
  #   ref=41c0a3a2 (#3990 head) -> exit 0   GREEN
  #   ref=5a27c309 (OCC dev)    -> exit 1   RED (404, symbol absent)
  #   ref=000...0  (bogus SHA)  -> exit 1   RED
exit_code: 0
actual_output: >-
  PASS: reads the actual source of scripts/validation/check_contract_substance_floor.py at #3990's exact
  head commit SHA and confirms it carries GATE_SELF_REFERENTIAL — the kill-switch that decides whether
  the circular self-grep class (a check_value that greps its own receipt for "status: PASS") is gated
  outright or merely derived as L0. That switch is the load-bearing control surface of OMN-14409, so asserting
  it is a direct read of the change, not a restatement of the PR's claim. This observes the PRODUCT tree,
  not this receipt: the command reads no file in the OCC checkout, so it cannot self-certify the way a
  grep of its own status line does. It is deliberately a content read pinned to a SHA rather than a CI-outcome
  check, because #3990's CI is red precisely BECAUSE this contract is missing (occ-preflight -> reason=missing_contract)
  — a CI-keyed check would be circular and could never go green. RED DIRECTION PROVEN by execution, not
  asserted: the file does not exist on OCC dev at all (contents API returns HTTP 404 at dev HEAD 5a27c309f2978afd2d2617321801c3d230795093),
  so the identical command exits 1 there, and also exits 1 against a bogus all-zero SHA. It exits 0 only
  at a ref where #3990's code genuinely exists.
branch: "jonah/omn-14409-occ-companion-evidence"
contract_sha256: "sha256:fc8e2bf3866de4f5e5f04ceb3b8f671ad0e6e6febdd078f6c6146f9d948cdcb5"
contract_entry_sha256: "sha256:3507dd5b16e97189c0d2d7d0d4868e5ba2d470f6f232f082010c85872ee58caa"
