---
schema_version: "1.0.0"
ticket_id: "OMN-16114"
evidence_item_id: "dod-OmniNode-ai-omnibase_infra-pr-2768-ci"
check_type: "command"
check_value: "files=\"$(gh api repos/OmniNode-ai/omnibase_infra/pulls/2768/files --jq '[.[].filename]')\"
  && printf '%s' \"$files\" | grep -qxF '[\"docker/runners/runner-job-started.sh\",\"tests/scripts/test_runner_job_started_mirror_rewrite.py\"]'"
contract_sha256: "sha256:2b283e8963062e484952a243de361564d9394905ba4ac7867cd357844e48d37e"
contract_entry_sha256: "sha256:e5ead0ff1112cce0927c6dbd1db2309f5bb866dd80b7ecc08d684ab56d48d15d"
status: PASS
run_timestamp: "2026-08-17T10:14:46Z"
commit_sha: "f18eee68f0933eb3206f909c4d8079e952647bb3"
runner: "occ-companion-2768-mint"
verifier: "occ-companion-2768-verify"
probe_command: "files=\"$(gh api repos/OmniNode-ai/omnibase_infra/pulls/2768/files --jq '[.[].filename]')\"
  && printf '%s' \"$files\" | grep -qxF '[\"docker/runners/runner-job-started.sh\",\"tests/scripts/test_runner_job_started_mirror_rewrite.py\"]'"
probe_stdout: |
  ["docker/runners/runner-job-started.sh","tests/scripts/test_runner_job_started_mirror_rewrite.py"]
actual_output: "PASS: product-diff-scope for OMN-16114 from OmniNode-ai/omnibase_infra#2768. gh api's
  /pulls/2768/files endpoint (the actual diff, not PR/issue metadata) returns exactly the mechanism file
  and its paired test file. RED control verified: the same pipeline against a fabricated single-file list
  fails the exact-match grep."
exit_code: 0
pr_number: 2768
branch: "jonah/omn-16114-occ-companion-2768"

# Fix 2026-08-17 (post-push, OCC#6595's own Contract Corpus Ratchets check, OMN-15411 Rule E): the
# original check_value piped an iterating jq projection (--jq '[.[].filename]', unbounded output by
# construction) directly into `grep -qxF`, the measured SIGPIPE-fragile shape. Rewritten per the
# documented repair idiom (occ#5496/#5523 precedent): the producer's output is captured into a shell
# variable first, then a printf of that variable is piped into grep. Re-verified live: still returns
# exit 0 with the real evidence, still RED against a fabricated file list. Admissibility and Rule E
# classification re-checked directly via the same scripts the CI job runs, both clean. See the
# sibling dod-OmniNode-ai-omnibase_infra-pr-2768 receipt for the fuller root-cause note.
