---
schema_version: "1.0.0"
ticket_id: "OMN-15281"
evidence_item_id: "dod-omn15281-verify-env-driven-rds-content"
check_type: "command"
check_value: >-
  grep -qx 'ENV_DRIVEN_DB_HOST_COUNT=6' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
  && grep -qx 'MANIFEST_RENDER_ERROR_DEFINED=1' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
  && grep -qx 'RDS_FAILFAST_PATTERNS=2' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
  && grep -qx 'NEW_TEST_MODULES_PASS=20/20' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
  && grep -qx 'FULL_SUITE_ON_PR_716=446 passed' docs/evidence/OMN-15282-b1-verify/lane-b1-verification-probes.md
status: PASS
contract_sha256: "sha256:fb3db25d7c41f07dbdd241f654217bb247c148cb105f50962972e0458e42f88b"
contract_entry_sha256: "sha256:51170bbaa007eb37148e550e094586be35beefdbf2ba31ef7722066dbb038e85"
run_timestamp: "2026-07-28T04:10:00Z"
commit_sha: "a467cc69d49eaf510d2618fa8c35c7e8ac9d8b4f"
branch: "jonah/omn-15282-b1-occ-verify"
pr_number: null
runner: "claude-b1-migrate-verify-0728"
verifier: "shell-readback-b1-migrate-verify-0728"
probe_command: >-
  gh pr diff 716 --repo OmniNode-ai/omninode_infra > /tmp/pr716_verify.diff; grep -c 'DB_HOST="${DB_HOST:?DB_HOST
  not set}"' /tmp/pr716_verify.diff; grep -c 'class ManifestRenderError' /tmp/pr716_verify.diff; grep
  -c 'resolved to nothing -- refusing to fall back' /tmp/pr716_verify.diff; ssh stickybeatz-studio: uv
  run pytest tests/scripts/test_render_migration_manifest_for_target.py tests/scripts/test_run_migrations_rds_target.py
  -v; ssh stickybeatz-studio: uv run pytest tests/ -q --tb=short
probe_stdout: |
  ENV_DRIVEN_DB_HOST_COUNT=6
  MANIFEST_RENDER_ERROR_DEFINED=1
  RDS_FAILFAST_PATTERNS=2
  NEW_TEST_MODULES_PASS: 20 passed in 1.57s
  FULL_SUITE: 446 passed in 46.54s
actual_output: >-
  PASS: PR #716's diff at head a467cc69 makes all 6 manifests' DB_HOST env-driven with fail-fast defaults,
  defines a dedicated ManifestRenderError, and names the SSM fail-fast refusal-to-fallback twice (host
  + port). Independently re-run on stickybeatz-studio (.200): both new test modules pass 20/20 and the
  full repo-root suite passes 446/446 at the PR head. schema-consistency checker and shellcheck on run-migrations.sh
  both clean. PR #716 was OPEN (not merged) at verification time; gh pr checks showed every job pass,
  mergeStateStatus=CLEAN, mergeable=MERGEABLE. Provenance disclosed plainly: runner and verifier are distinct
  identities per the receipt schema but are NOT independent parties -- both are this single adversarial-verification
  session (same disclosure pattern as OCC#5184 / OMN-15266). This session IS independent of the original
  implementer session that built OMN-15281 (fresh git fetch, PR state read via gh not local git, tests
  re-run in a detached clone on .200 rather than trusting the implementer's self-report). A further independent
  re-run by a separate party is still owed for full non-self-attestation rigor. PR #716 is unmerged as
  of this receipt; this receipt attests to the PR-head state, not a merge commit -- re-verify after merge
  if the merge is not a clean fast-forward/no-op.
exit_code: 0
working_dir: /Users/jonah/Code/omni_home/omni_worktrees/OMN-15282-occ-verify-b1
