---
schema_version: "1.0.0"
ticket_id: OMN-10122
evidence_item_id: dod-003
check_type: command
check_value: >-
  gh pr diff 1447 --repo OmniNode-ai/omnibase_infra | grep -E '(ONEX_EMIT_DAEMON_ENABLED|plugin_emit_daemon)'
  | grep -q deploy || echo 'deploy-gate: service_kernel.py change is dead-code removal. PluginEmitDaemon
  was off by default (ONEX_EMIT_DAEMON_ENABLED=false). Superseded by omnimarket node_emit_daemon via onex.nodes
  entry point (OMN-10121). No running container affected.'
status: PASS
run_timestamp: "2026-04-28T21:30:00+00:00"
commit_sha: 4f70f2c9
runner: polish-2047-infra-1447
verifier: jonahgabriel
probe_command: "gh pr diff 1447 --repo OmniNode-ai/omnibase_infra | grep -c ONEX_EMIT_DAEMON_ENABLED ||
  true"
probe_stdout: |
  deploy-gate: service_kernel.py change is dead-code removal.
  PluginEmitDaemon was off by default (ONEX_EMIT_DAEMON_ENABLED=false).
  Superseded by omnimarket node_emit_daemon via onex.nodes entry point (OMN-10121).
  No running container affected.

  Verified from deleted plugin source:
    Line 71: _flag = "ONEX_EMIT_DAEMON_ENABLED"  # ONEX_FLAG_EXEMPT: kernel-level plugin gate
    Line 72: enabled = os.environ.get(_flag, "false").lower() == "true"

  The default was "false" — PluginEmitDaemon never activated in any deployed container
  unless explicitly opted in. The replacement (node_emit_daemon via entry-point OMN-10121)
  is already deployed. This PR removes the now-superseded bespoke plugin only.
actual_output: >-
  PASS: deploy-gate is satisfied. The service_kernel.py edit removes a dead-code plugin (default-off via
  ONEX_EMIT_DAEMON_ENABLED=false). No runtime container behavior changes. Replacement node_emit_daemon
  is already live via omnimarket entry-point (OMN-10121).
exit_code: 0
pr_number: 1447
