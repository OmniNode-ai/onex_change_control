---
schema_version: "1.0.0"
ticket_id: "OMN-13120"
evidence_item_id: "dod-deploy-assessment"
check_type: "command"
check_value: "echo 'deploy-gate: OMN-13120 CI/pre-commit tooling-only change; no runtime/schema/topic/container/.201
  mutation'"
contract_sha256: "sha256:6c936252314201189be9e4ef99c5581c27e52c37bc6b6c940aeaacff06f501a8"
status: PASS
run_timestamp: "2026-06-14T20:30:00Z"
commit_sha: "45ec4632f1e72ced45214b30ef982c2ce9f254b0"
pr_number: 2628
runner: "worktree-agent-omn-13120-venv-skew-gate"
verifier: "receipt-gate-local"
probe_command: >-
  git diff --stat origin/dev..HEAD on omniclaude PR #1759 — confirms only the new validator, the pre-commit
  wrapper, the CI workflow, the .pre-commit-config.yaml registration, and the new test changed; no src/
  runtime/handler code, no docker/, no migrations/, no contract topic/schema files, no .201 mutation.
probe_stdout: |
  $ git diff --stat origin/dev..jonah/omn-13120-venv-skew-gate
   .github/workflows/daemon-venv-skew-gate.yml  |  59 ++++++
   .pre-commit-config.yaml                      |  15 ++
   .pre-commit-hooks/check-daemon-venv-skew.sh  |  40 ++++
   scripts/check_daemon_venv_skew.py            | 277 +++++++++++++++++++++++++++
   tests/scripts/test_check_daemon_venv_skew.py | 261 +++++++++++++++++++++++++
   5 files changed, 652 insertions(+)

  # No src/omniclaude/ runtime/handler code, no docker/, no migrations/, no *.contract.yaml
  # topic/schema files, no scripts/deploy* touched. No new Kafka topic, no DB schema, no
  # container image change, no API surface change.
actual_output: >-
  PASS (deploy_pending = false-by-design). OMN-13120 is a CI/pre-commit tooling change only: a new stdlib-only
  validator (scripts/check_daemon_venv_skew.py), a pre-commit wrapper, a required CI status check workflow,
  the .pre-commit-config.yaml registration, and unit tests. No runtime/handler code, no new Kafka topics,
  no schema/migration changes, no container image changes, no API surface changes, no live .201 mutation
  was performed. The live daemon venv rebuild via repair-plugin-venv.sh is a local runtime action on the
  developer machine (Rule 11), not a server deploy. No standalone live deploy is required for this PR.
  Not claiming a live deploy was performed.
