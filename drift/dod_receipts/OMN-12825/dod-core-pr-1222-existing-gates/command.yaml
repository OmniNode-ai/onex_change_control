---
schema_version: "1.0.0"
ticket_id: "OMN-12825"
evidence_item_id: "dod-core-pr-1222-existing-gates"
check_type: "command"
check_value: "assert Receipt Gate and Deploy Gate workflows already trigger on pull_request + merge_group
  on omnibase_core"
status: PASS
run_timestamp: "2026-06-08T18:02:58Z"
contract_sha256: "sha256:792ca0c41eaee0a536a2ed5108f7c0556874cc47e132131d1c5518ab63c91f69"
commit_sha: "a65a5371a555886a68600fbe28ef3e973045a914"
branch: "jonah/omn-12825-gateprep-honesty"
pr_number: 1222
runner: "claude"
verifier: "codex"
probe_command: >-
  cd omni_worktrees/OMN-12825-gateprep/omnibase_core && for f in call-receipt-gate.yml deploy-gate.yml;
  do python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/'+sys.argv[1])); t=list(d[True].keys());
  print(sys.argv[1], 'triggers:', t); assert 'pull_request' in t and 'merge_group' in t; print('  BOTH
  present: OK')" "$f"; done
probe_stdout: |
  call-receipt-gate.yml triggers: ['pull_request', 'merge_group']
    BOTH present: OK
  deploy-gate.yml triggers: ['pull_request', 'merge_group']
    BOTH present: OK
actual_output: |
  PASS: the pre-existing evidence gates on omnibase_core are already queue-safe. Receipt Gate
  (call-receipt-gate.yml; live check-run context 'verify / verify') and Deploy Gate (deploy-gate.yml;
  live check-run context 'deploy-gate / deploy-gate') both declare triggers on pull_request AND
  merge_group. Confirmed against live PR #1221 check-runs, which surface 'verify / verify' and
  'deploy-gate / deploy-gate'. No workflow change was needed for these two; requiring them later
  cannot wedge the merge queue. Note: occ-preflight.yml ('DoD Evidence / eligibility') is
  workflow_call-only with no caller in omnibase_core, so 'occ-preflight / eligibility' does NOT
  currently emit on omnibase_core PRs and is intentionally NOT listed as ready-to-require here.
exit_code: 0
