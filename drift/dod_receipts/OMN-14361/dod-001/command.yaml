---
schema_version: 1.0.0
ticket_id: OMN-14361
evidence_item_id: dod-001
check_type: command
check_value: >-
  env -u PYTHONPATH uv run pytest tests/unit/runtime/test_dispatch_parity_staleness_strip.py -v && python3
  adversarial_gate_neutering_probe.py  # 9 real corpus drifts vs the REAL committed baseline-selection-v2.json,
  each asserted to still trip; version+clock-only delta asserted suppressed && git diff origin/dev...HEAD
  -- tests/fixtures/dispatch_parity/baseline-selection-v2.json
status: PASS
run_timestamp: "2026-07-11T05:05:00Z"
commit_sha: 76e57e3e2db60ffaf07ec303a75ffa995704ee5b
runner: claude-opus-4-8
verifier: omn14361-independent-verify
pr_number: 2256
probe_command: >-
  cd omni_worktrees/OMN-14361/wt2256 && env -u PYTHONPATH .venv/bin/python -m pytest tests/unit/runtime/test_dispatch_parity_staleness_strip.py
  -v; then extract the byte-for-byte strip_volatile_header_fields helper from the PR harness.py and drive
  the exact workflow comparison (json.dumps(strip(x), sort_keys=True)) over the REAL committed baseline
  with nine mutated copies (route.handler_id, route.contract_name, routes-shrink, probe value, dispatcher
  drop, header.corpus, header.required_missing_packages, header.exclusions[].installed, and a combined
  real-route-drift + version-churn) plus a version+clock-only control.
probe_stdout: |
  unit suite = 8 passed
  gate-neutering probe: control identical -> stale=False; version+clock-only delta -> stale=False (suppressed)
  9/9 real corpus drifts -> stale=True (each still trips); combo (real route drift + version churn) -> stale=True
  baseline regen diff = 4 content lines = generated_at_utc + omnibase-core 0.46.5->0.46.7 ONLY (no corpus lines)
actual_output: >-
  PASS: omnibase_infra PR #2256 head 76e57e3e2db60ffaf07ec303a75ffa995704ee5b makes the dispatch-parity-gate
  staleness step strip generated_at_utc + installed_package_versions via a single-source-of-truth harness
  helper applied to BOTH committed and live snapshots, so a routine tracked-package release no longer
  re-breaks dev's frozen baseline. The gate is NOT neutered: an adversarial probe against the real committed
  baseline confirmed every genuine corpus drift (route/dispatcher/probe tuple, corpus count, required_missing_packages,
  exclusions[].installed) still trips, while only the version+clock churn is suppressed. The 8-test unit
  suite passes and the regenerated baseline diff is exactly the two volatile lines. The parity integration
  test's local 4/10 failure is a broader-local-venv package-closure artifact (co-installed omnimarket/omniclaude
  nodes) reproduced identically on origin/dev without this PR; the in-closure integration proof is the
  CI dispatch-parity-gate job on #2256 itself.
exit_code: 0
branch: jonah/omn-14361-dispatch-parity-version-metadata
contract_sha256: sha256:b231e10f2dfab1eeef49025c5ab4af403227a7b26187e8c0a378ed838be341e1
