---
schema_version: "1.0.0"
ticket_id: "OMN-17557"
evidence_item_id: "dod-omn17557-ac3-store-carried-binding"
check_type: "command"
check_value: |
  hw="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=188f37fc327b3fb5eb54e39fccd631b06bf0261b" --jq '.content' | base64 -d)"; test -n "$hw" || exit 1; sr="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/secret_resolver.py?ref=188f37fc327b3fb5eb54e39fccd631b06bf0261b" --jq '.content' | base64 -d)"; test -n "$sr" || exit 1; a=$(printf '%s\n' "$hw" | grep -c 'async def build_lane_infisical_handler'); b=$(printf '%s\n' "$sr" | grep -c 'def config_declares_infisical_source'); test "$a" -ge 1 && test "$b" -ge 1
contract_sha256: "sha256:7aa05a96962f75b65074b375f7ecfb244c6ad16eaeb77ee65e6772bf7f91b6b8"
contract_entry_sha256: "sha256:323e1e77852658df792a78b5ab13be714d21be2aa637988dde0e83a1b1be38bc"
status: "PASS"
run_timestamp: "2026-09-10T06:15:56Z"
commit_sha: "188f37fc327b3fb5eb54e39fccd631b06bf0261b"
runner: "claude-local-worktree"
verifier: "omn17557-closure"
probe_command: |
  hw="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=188f37fc327b3fb5eb54e39fccd631b06bf0261b" --jq '.content' | base64 -d)"; test -n "$hw" || exit 1; sr="$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/secret_resolver.py?ref=188f37fc327b3fb5eb54e39fccd631b06bf0261b" --jq '.content' | base64 -d)"; test -n "$sr" || exit 1; a=$(printf '%s\n' "$hw" | grep -c 'async def build_lane_infisical_handler'); b=$(printf '%s\n' "$sr" | grep -c 'def config_declares_infisical_source'); test "$a" -ge 1 && test "$b" -ge 1
probe_stdout: |
  check[0] exit=0 stdout=(empty)
  check[1] exit=0 stdout=(empty)
actual_output: |
  PASS. AC3, half two: the store-carried binding resolution.

  Positive reading at the merged squash of OmniNode-ai/omnibase_infra#3304,
  188f37fc327b3fb5eb54e39fccd631b06bf0261b (merged 2026-09-08T02:36:37Z): both files fetched
  non-empty and both named seams present -- `async def build_lane_infisical_handler` in
  src/omnibase_infra/runtime/auto_wiring/handler_wiring.py and
  `def config_declares_infisical_source` in src/omnibase_infra/runtime/secret_resolver.py.

  DISCRIMINATING CONTROL at that commit's parent 00a821b1886f8f61df7b8baf58ba5c0ff608b505: both
  files fetched non-empty and NEITHER definition exists. So the positive reading is the merge.

  Why these two seams are the criterion and not a surrogate for it: before this change
  SecretResolver.from_container treated an absent HandlerInfisical as graceful degradation, and
  nothing ever registered one on a runtime process, so every store-carried logical name resolved
  to None behind a single "Infisical handler not configured" warning. That is the condition that
  left the onex-dev tenant_projection binding unresolvable and made all eight contracts refuse to
  wire with "Projection handler requires topology bindings with configured DSNs:
  tenant_projection". build_lane_infisical_handler is the construction seam that removes it, and
  config_declares_infisical_source is what decides when an identity is required.

  What this proves and what it does not: it proves the resolution path exists in merged code. The
  executed behaviour proof for it is the sibling item dod-omn17557-ac3-behavior-proof.
exit_code: 0
pr_number: 3304
branch: "dev"
