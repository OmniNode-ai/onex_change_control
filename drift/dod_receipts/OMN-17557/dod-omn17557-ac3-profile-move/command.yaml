---
schema_version: "1.0.0"
ticket_id: "OMN-17557"
evidence_item_id: "dod-omn17557-ac3-profile-move"
check_type: "command"
check_value: |
  hits=0; files=0; for c in node_canary_score_reducer node_hook_event_capture node_projection_context_roi node_projection_cost_summary node_projection_delegation_inference_response node_projection_dep_health node_projection_pattern_learning node_projection_routing_decision; do body="$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/$c/contract.yaml?ref=0fedcab33c5ea34f98027c5c65cd2fcd0cc78e52" --jq '.content' | base64 -d)"; test -n "$body" || exit 1; files=$((files+1)); n=$(printf '%s\n' "$body" | grep -cE '^[[:space:]]{0,2}runtime_profiles: \[tenant-projection\]$'); hits=$((hits+n)); done; test "$files" = "8" && test "$hits" = "8"
contract_sha256: "sha256:7aa05a96962f75b65074b375f7ecfb244c6ad16eaeb77ee65e6772bf7f91b6b8"
contract_entry_sha256: "sha256:885da392510074a4fa15d236d1019fcd71eafb4b086037df85064791a4594849"
status: "PASS"
run_timestamp: "2026-09-10T06:15:52Z"
commit_sha: "0fedcab33c5ea34f98027c5c65cd2fcd0cc78e52"
runner: "claude-local-worktree"
verifier: "omn17557-closure"
probe_command: |
  hits=0; files=0; for c in node_canary_score_reducer node_hook_event_capture node_projection_context_roi node_projection_cost_summary node_projection_delegation_inference_response node_projection_dep_health node_projection_pattern_learning node_projection_routing_decision; do body="$(gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/$c/contract.yaml?ref=0fedcab33c5ea34f98027c5c65cd2fcd0cc78e52" --jq '.content' | base64 -d)"; test -n "$body" || exit 1; files=$((files+1)); n=$(printf '%s\n' "$body" | grep -cE '^[[:space:]]{0,2}runtime_profiles: \[tenant-projection\]$'); hits=$((hits+n)); done; test "$files" = "8" && test "$hits" = "8"
probe_stdout: |
  check[0] exit=0 stdout=(empty)
  check[1] exit=0 stdout=(empty)
actual_output: |
  PASS. AC3, half one: the profile move.

  Positive reading at the OMN-17641 merged squash 0fedcab33c5ea34f98027c5c65cd2fcd0cc78e52
  (OmniNode-ai/omnimarket#2277, merged 2026-09-03T05:40:16Z): all EIGHT tenant-domain
  contract.yaml files fetched non-empty, and the profile line
  `runtime_profiles: [tenant-projection]` occurs EIGHT times across them -- exactly one per
  contract. The loop aborts on an empty body, so a fetch failure cannot count as compliance.

  DISCRIMINATING CONTROL at that commit's parent 115bc775f12b8fc20ca6547e5d1fd9a14c4b700b: the
  same eight files are fetched non-empty (files=8, so the control is a real read and not an
  empty sweep) and the line occurs ZERO times. The positive reading is therefore the merge and
  not a property the tree always had.

  The line is matched with up to two leading spaces because the declaration sits at the document
  root in six contracts and under `descriptor:` in node_canary_score_reducer and
  node_hook_event_capture. Both placements are live and the consolidated writer wires all eight;
  the check accepts either rather than asserting a placement the tree does not have. Measured
  per-file at the merged sha: six root-level, two under descriptor, eight total.

  What this proves and what it does not: it proves the eight contracts are assigned to the
  tenant-projection profile in merged code, which is what makes filter_manifest_for_runtime_profile
  drop them from `main` and `effects`. It is not a statement about any running pod.
exit_code: 0
pr_number: 2277
branch: "dev"
