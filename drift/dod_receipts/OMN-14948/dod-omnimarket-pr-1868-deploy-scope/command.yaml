---
schema_version: "1.0.0"
ticket_id: "OMN-14948"
evidence_item_id: "dod-omnimarket-pr-1868-deploy-scope"
check_type: "command"
check_value: "gh pr view 1868 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -F 'No AWS/IAM/network/secret/DNS
  mutation' && gh pr view 1868 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -F 'no deploy,
  no runtime restart' && gh pr view 1868 --repo OmniNode-ai/omnimarket --json body --jq .body | grep -F
  'abort action' && test 'deploy scope acknowledged; no live deploy performed by PR #1868' = 'deploy scope
  acknowledged; no live deploy performed by PR #1868'"
contract_entry_sha256: "sha256:5cb9dc4f7666ad54d0dc96e0bafbd8342b3aca64cbba7a3b488a92c0fcddda7d"
status: PASS
run_timestamp: "2026-07-24T01:04:00Z"
commit_sha: "ba647183a5026d62c6e42f04826a66ba022bab11"
runner: "codex-merge-controller"
verifier: "occ-evidence-source-autobind"
probe_command: "gh pr view 1868 --repo OmniNode-ai/omnimarket --json body --jq .body | grep deploy-scope
  strings"
probe_stdout: |
  No AWS/IAM/network/secret/DNS mutation, no ECR push, no topic apply, no database provision, no deploy, no runtime restart, no staging connection.
  abort action's live execution is OMN-14736 and out of this PR's scope.
  deploy scope acknowledged; no live deploy performed by PR #1868
actual_output: "PASS: omnimarket PR #1868 carries deploy-scope evidence: typed threshold wiring only;
  no live deploy performed."
exit_code: 0
pr_number: 1868
branch: "jonah/omn-14948-wire-b10-thresholds"
