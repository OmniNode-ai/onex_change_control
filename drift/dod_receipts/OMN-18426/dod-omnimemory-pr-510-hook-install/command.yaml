---
schema_version: "1.0.0"
ticket_id: "OMN-18426"
evidence_item_id: "dod-omnimemory-pr-510-hook-install"
check_type: "command"
check_value: |-
  gh api repos/OmniNode-ai/omnimemory/commits/21c7a051fb2867766cbdb9d56bea5a4ce11f826f --jq .sha
  uv run python - <<'PY'
  from pathlib import Path
  import yaml
  cfg = yaml.safe_load(Path('.pre-commit-config.yaml').read_text())
  install_types = cfg.get('default_install_hook_types')
  default_stages = cfg.get('default_stages')
  strip_hooks = [
      hook
      for repo in cfg.get('repos', [])
      for hook in repo.get('hooks', [])
      if hook.get('id') == 'strip-ai-coauthor-trailer'
  ]
  assert install_types == ['pre-commit', 'pre-push', 'commit-msg'], install_types
  assert default_stages == ['pre-commit'], default_stages
  assert len(strip_hooks) == 1, strip_hooks
  assert strip_hooks[0].get('stages') == ['commit-msg'], strip_hooks[0]
  print('PASS default_install_hook_types installs commit-msg and strip hook remains commit-msg-only')
  PY
contract_sha256: "sha256:bc38b2f767fd531f90db52e10aee70bd868896381162cb8fc520dcb9c824200e"
contract_entry_sha256: "sha256:628a51cd5988beafbfd57c03cf74874fc1d8f05a502f0ee55e353766d008777f"
status: PASS
run_timestamp: "2026-09-17T13:34:00Z"
commit_sha: "21c7a051fb2867766cbdb9d56bea5a4ce11f826f"
runner: "codex-merge-sweep"
verifier: "uv run python"
probe_command: |-
  gh api repos/OmniNode-ai/omnimemory/commits/21c7a051fb2867766cbdb9d56bea5a4ce11f826f --jq .sha
  uv run python - <<'PY'
  from pathlib import Path
  import yaml
  cfg = yaml.safe_load(Path('.pre-commit-config.yaml').read_text())
  install_types = cfg.get('default_install_hook_types')
  default_stages = cfg.get('default_stages')
  strip_hooks = [
      hook
      for repo in cfg.get('repos', [])
      for hook in repo.get('hooks', [])
      if hook.get('id') == 'strip-ai-coauthor-trailer'
  ]
  assert install_types == ['pre-commit', 'pre-push', 'commit-msg'], install_types
  assert default_stages == ['pre-commit'], default_stages
  assert len(strip_hooks) == 1, strip_hooks
  assert strip_hooks[0].get('stages') == ['commit-msg'], strip_hooks[0]
  print('PASS default_install_hook_types installs commit-msg and strip hook remains commit-msg-only')
  PY
probe_stdout: |
  21c7a051fb2867766cbdb9d56bea5a4ce11f826f
  PASS default_install_hook_types installs commit-msg and strip hook remains commit-msg-only
actual_output: >-
  PASS: OmniNode-ai/omnimemory#510 config sets default_install_hook_types to [pre-commit, pre-push, commit-msg],
  leaves default_stages as [pre-commit], and keeps strip-ai-coauthor-trailer scoped to commit-msg. The
  command was executed in the OmniNode-ai/omnimemory PR #510 worktree at commit 21c7a051fb2867766cbdb9d56bea5a4ce11f826f.
exit_code: 0
pr_number: 510
branch: "jonah/omn-18426-omnimemory-510-companion-dev"
