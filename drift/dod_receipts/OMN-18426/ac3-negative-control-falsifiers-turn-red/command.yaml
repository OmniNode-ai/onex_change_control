---
schema_version: 1.0.0
ticket_id: OMN-18426
evidence_item_id: ac3-negative-control-falsifiers-turn-red
check_type: command
check_value: |-
  python3 - <<'PY'
  """AC3 negative control: both falsifiers the criterion names must turn the proof red."""
  import re
  import shutil
  import subprocess
  import sys
  import tempfile
  from pathlib import Path

  TEST = Path("tests/unit/test_strip_ai_coauthor_trailer_hook.py")
  CONFIG = Path(".pre-commit-config.yaml")
  MUTANTS = {
      "an entry that strips nothing": "true",
      "an entry that strips every line mentioning Claude": "perl -i -ne 'print unless /claude/i'",
  }

  results = []
  for label, entry in MUTANTS.items():
      with tempfile.TemporaryDirectory() as work:
          root = Path(work)
          (root / "tests" / "unit").mkdir(parents=True)
          shutil.copy(TEST, root / "tests" / "unit" / TEST.name)
          original = CONFIG.read_text()
          mutated = re.sub(r"entry: perl -i -ne .*\n", "entry: " + entry + "\n", original, count=1)
          if mutated == original:
              sys.exit("CONTROL BROKEN: the mutation did not apply for " + label)
          (root / CONFIG.name).write_text(mutated)
          run = subprocess.run(
              ["uv", "run", "pytest", str(root / "tests" / "unit" / TEST.name), "-q", "-p", "no:cacheprovider"],
              capture_output=True, text=True,
          )
          tail = run.stdout.strip().splitlines()[-1] if run.stdout.strip() else run.stderr.strip()[-200:]
          if " failed" not in tail:
              sys.exit("CONTROL BROKEN: " + label + " did not turn the proof red: " + tail)
          if "error" in tail:
              sys.exit("CONTROL BROKEN: " + label + " errored instead of failing: " + tail)
          results.append((label, tail))

  print("PASS both falsifiers named in AC3 turn the behaviour test red")
  for label, tail in results:
      print("  " + label + ": " + tail)
  PY
contract_sha256: sha256:20888f457a6054b9e3328654936d8edede5fe04de348ff03973a2de24df1774b
contract_entry_sha256: sha256:629e9541d059e7eb0dc186aee23e22c88e5cd00705469fe419eb733589a4964b
status: PASS
run_timestamp: '2026-09-18T01:38:27Z'
commit_sha: fc73fa33cc9cef8a3005f1a8ed74288a7a8322ab
runner: manual-closure
verifier: uv run pytest
probe_command: |-
  python3 - <<'PY'
  """AC3 negative control: both falsifiers the criterion names must turn the proof red."""
  import re
  import shutil
  import subprocess
  import sys
  import tempfile
  from pathlib import Path

  TEST = Path("tests/unit/test_strip_ai_coauthor_trailer_hook.py")
  CONFIG = Path(".pre-commit-config.yaml")
  MUTANTS = {
      "an entry that strips nothing": "true",
      "an entry that strips every line mentioning Claude": "perl -i -ne 'print unless /claude/i'",
  }

  results = []
  for label, entry in MUTANTS.items():
      with tempfile.TemporaryDirectory() as work:
          root = Path(work)
          (root / "tests" / "unit").mkdir(parents=True)
          shutil.copy(TEST, root / "tests" / "unit" / TEST.name)
          original = CONFIG.read_text()
          mutated = re.sub(r"entry: perl -i -ne .*\n", "entry: " + entry + "\n", original, count=1)
          if mutated == original:
              sys.exit("CONTROL BROKEN: the mutation did not apply for " + label)
          (root / CONFIG.name).write_text(mutated)
          run = subprocess.run(
              ["uv", "run", "pytest", str(root / "tests" / "unit" / TEST.name), "-q", "-p", "no:cacheprovider"],
              capture_output=True, text=True,
          )
          tail = run.stdout.strip().splitlines()[-1] if run.stdout.strip() else run.stderr.strip()[-200:]
          if " failed" not in tail:
              sys.exit("CONTROL BROKEN: " + label + " did not turn the proof red: " + tail)
          if "error" in tail:
              sys.exit("CONTROL BROKEN: " + label + " errored instead of failing: " + tail)
          results.append((label, tail))

  print("PASS both falsifiers named in AC3 turn the behaviour test red")
  for label, tail in results:
      print("  " + label + ": " + tail)
  PY
probe_stdout: |
  PASS both falsifiers named in AC3 turn the behaviour test red
    an entry that strips nothing: 2 failed, 3 passed, 4 warnings in 0.20s
    an entry that strips every line mentioning Claude: 2 failed, 3 passed, 4 warnings in 0.20s
actual_output: 'PASS: both AC3 falsifiers turn the behaviour test red (pytest failures, not errors).'
exit_code: 0
pr_number: null
branch: lakshman/omn-18426-closure-evidence
