---
schema_version: "1.0.0"
ticket_id: "OMN-14505"
evidence_item_id: "dod-matcher-rejects-4067"
check_type: "command"
check_value: >-
  Fetch validate_pr_deploy_required.py from OmniNode-ai/omniclaude at the pinned PR-head SHA c69a565cb8681107c780791be366ef61c61c49f4,
  execute it under DEPLOY_GATE_FALSIFIABILITY=enforce, and assert has_deploy_evidence() REJECTS the exact
  #4067 check_value, REJECTS the quoted-keyword embed trap, and ACCEPTS a genuine docker-exec probe. Executable
  form in contracts/OMN-14505.yaml.
status: PASS
run_timestamp: "2026-07-13T01:37:43Z"
commit_sha: "c69a565cb8681107c780791be366ef61c61c49f4"
runner: "claude-opus-4-8"
verifier: "occ-interim-manual-verify"
probe_command: >-
  d="$(mktemp -d)" && gh api "repos/OmniNode-ai/omniclaude/contents/.github/actions/deploy-gate/validate_pr_deploy_required.py?ref=c69a565cb8681107c780791be366ef61c61c49f4"
  --jq .content | base64 -d > "$d/v.py" && DEPLOY_GATE_FALSIFIABILITY=enforce python3 -c '<assert block>'
  "$d"
probe_stdout: |
  === GREEN at omniclaude#1896 head c69a565cb (POST-FIX) ===
  has_deploy_evidence(#4067 vacuous value) = False   expect False
  has_deploy_evidence(real docker exec)    = True    expect True
  has_deploy_evidence(embed trap)          = False   expect False
  PASS: #4067 REJECTED, embed-trap REJECTED, real docker-exec probe ACCEPTED
  exit_code=0

  === RED against origin/dev (PRE-FIX) — same probe, same assertions ===
  has_deploy_evidence(#4067 vacuous value) = True    expect False   <-- WRONG ANSWER
  has_deploy_evidence(real docker exec)    = True    expect True
  has_deploy_evidence(embed trap)          = True    expect False   <-- WRONG ANSWER
  AssertionError: FIX ABSENT/VACUOUS: #4067's check_value still satisfies deploy-gate
  exit_code=1
exit_code: 0
actual_output: >-
  PASS at omniclaude#1896 head c69a565cb: has_deploy_evidence() REJECTS the exact check_value that closed
  the REQUIRED deploy-gate on omnibase_infra#2279 via OCC #4067 (dod-deploy-assessment-head-f973c737);
  REJECTS the quoted-keyword embed trap (echo 'docker exec' && grep -q PASS my_own_receipt), which a merely-stricter
  keyword list would still accept; and still ACCEPTS a genuine docker-exec probe, so there is no false
  negative that could wedge the 4 consuming repos. #magic___^_^___line FALSIFIABILITY — this evidence
  is RED-against-EXISTS-but-WRONG, not red-against-absence. The probe targets has_deploy_evidence(), which
  EXISTS in BOTH the pre-fix and post-fix matcher, so its red is a WRONG ANSWER from a pre-existing function,
  not a missing symbol. Running the SAME probe against origin/dev (pre-fix) prints "has_deploy_evidence(#4067
  vacuous value) = True" and exits 1 with "AssertionError: FIX ABSENT/VACUOUS"; against the PR head it
  prints False and exits 0 (both runs captured verbatim in probe_stdout above). A check asserting that
  classify_check_value is merely ABSENT on dev would have been a green-on-absence and would have proven
  nothing — that is precisely the defect class this ticket repairs, so this receipt refuses to reproduce
  it. The probe reads no file this PR authored and greps back no receipt.
branch: "jonah/omn-14505-occ-deploy-gate-falsifiability"
contract_entry_sha256: "sha256:bff1e831a88125b9bb534696c1b3261915dd05063ff0df5581bc76aa39c986dc"
