---
schema_version: "1.0.0"
ticket_id: "OMN-17163"
evidence_item_id: "dod-omn17163-monitor-reaches-the-broker-pr-3367"
check_type: "command"
check_value: |-
  test "$(gh api repos/OmniNode-ai/omnibase_infra/actions/runs/34387621476/attempts/1/jobs --jq '[.jobs[] | select(.run_attempt == 1) | .conclusion] | join(",")')" = "failure"
contract_sha256: "sha256:941a5afc45c73dcb051198545d8f2e4cf6f53d49a4b75bd5fd6a082138dedb4c"
contract_entry_sha256: "sha256:f4f26b4febd61ee85b0fffd70598b45597e400ed8d003211aa119c00f1b1cbae"
status: PASS
run_timestamp: "2026-09-09T18:58:16+00:00"
commit_sha: "1eef12c79886a3a34070dad80411890d5f170fe1"
runner: "omn17163-3367-companion"
verifier: "occ-union-resolve-same-ticket-companion"
probe_command: |-
  gh api repos/OmniNode-ai/omnibase_infra/actions/runs/34387621476/attempts/1/jobs --jq '.jobs[] | [.name,.conclusion,(.run_attempt|tostring)]|join("  |  ")'
probe_stdout: |
  DLQ Depth Monitor (read-only)  |  failure  |  1
actual_output: |-
  PASS -- AC2, and this is the RED HALF OF THE PAIR, which is the whole point of the criterion. Attempt 1
  of run 34387621476, on the IDENTICAL head 1eef12c79886a3a34070dad80411890d5f170fe1 and the identical
  lane data as the green run above, concluded failure with alert_triggered true because three sinks
  exceeded their contract-pinned bound.

  AC2 says a run is red ONLY when a sink exceeds its bound -- i.e. red carries information again. The two
  runs together are what prove it, and neither alone would: 34387621476 shows red is REACHABLE on a real
  breach, and 34387859397 shows that the same measurement under suppress_alert_exit=true is GREEN, so the
  red is the alert policy firing rather than the probe being broken. For this workflow's entire prior
  existence red meant only that the probe could not connect.

  Zero-vacuity control on the discriminator, taken in this lane: an all-time status=success query on
  dlq-depth-monitor.yml returns total_count 1, and that one success IS 34387859397 at this head. The
  identical filter returns 1397 for runtime-rebuild-trigger.yml, so the 1 is a real count and not a
  broken query. Rule 16 observed throughout -- no stderr was suppressed on any probe in this lane, and
  the one empty result encountered (`gh run view --log-failed` returning 0 bytes) was NOT read as an
  absence: it was re-taken through the jobs-logs API, which returned 67,947 bytes and the actual refusal
  message.
exit_code: 0
pr_number: 3367
branch: "jonah/omn-17163-occ-companion-3367"
