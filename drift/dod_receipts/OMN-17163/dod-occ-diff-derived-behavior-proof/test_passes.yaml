---
schema_version: "1.0.0"
ticket_id: "OMN-17163"
evidence_item_id: "dod-occ-diff-derived-behavior-proof"
check_type: "test_passes"
check_value: |-
  uv run pytest tests/ci/test_dlq_and_baselines_lane_broker_omn17163.py tests/unit/nodes/node_dlq_depth_monitor_effect/ tests/unit/scripts/test_run_baselines_batch_compute.py -q
contract_sha256: "sha256:dd38172fb22e70d032f63ed521253274067481a1d789c6ee5add3b8a8c49b787"
contract_entry_sha256: "sha256:02c205088c4aaa9f8ee05f61ad6e97e0e85ffab6bba5c4ad12fec2ed30a1cc6d"
status: PASS
run_timestamp: "2026-09-09T14:06:24+00:00"
commit_sha: "183e4a1cdefccd0bccd0b99d739bfa0bb971d3f6"
runner: "omn17163-dlq-monitor-lane-broker"
verifier: "occ-union-resolve-same-ticket-companion"
probe_command: |-
  gh api repos/OmniNode-ai/omnibase_infra/commits/183e4a1cdefccd0bccd0b99d739bfa0bb971d3f6 --jq .sha \
    && (cd "${OMNI_HOME}/omni_worktrees/OMN-17163/omnibase_infra" \
        && uv run pytest tests/ci/test_dlq_and_baselines_lane_broker_omn17163.py tests/unit/nodes/node_dlq_depth_monitor_effect/ tests/unit/scripts/test_run_baselines_batch_compute.py -q -p no:cacheprovider)
probe_stdout: |
  183e4a1cdefccd0bccd0b99d739bfa0bb971d3f6
  ...............................................                          [100%]
  47 passed in 12.60s
actual_output: |-
  PASS: the pushed commit resolves on OmniNode-ai/omnibase_infra and 47 tests pass at it.

  The zero is not vacuous, and the RED control was taken in this lane rather than asserted. The new
  guard's predicates were run, comment-stripped exactly as the test strips them, against the pre-change
  files read from origin/dev. All five pins fail there and pass here:

    workflow                  host-local literal | bootstrap assignment | env sourcing | routing var | runner pinned
    dlq-depth-monitor.yml     True               | True                 | True         | True        | False
    baselines-scheduler.yml   True               | True                 | True         | True        | False

  Every zero-result assertion in the file carries its own positive control, including a live one that
  asserts chain-canary.yml -- the reference implementation these two were brought into line with --
  satisfies the same pins, so a predicate that is merely too strict fails loudly instead of passing
  vacuously. The comment stripper is separately controlled in both directions: a commented copy of the
  retired line does not match, an uncommented one does.

  Worth recording because it was found by running the test rather than reasoning about it: the guard's
  FIRST run failed both workflows on their own postmortem comments, which quote the literal that was
  removed. That is the CLAUDE.md rule 15 shape in miniature -- prose that merely mentions a trigger
  firing the gate -- and it is why the matcher scans executable lines only.

  The node-side assertions pin ORDER, not just the raised error: with no broker declared the aiokafka
  reader is never instantiated, proven by a patched constructor that records its argument and raises;
  that assertion carries its own positive control (with a broker declared, the same run DOES reach
  construction and records the address), so a monkeypatch that silently missed its target cannot read
  as a pass.

  Separately, the governed pre-push escalated to a full-suite run and placed it on lab host h201.3
  (omninode-pc): 27,362 tests green on this same commit in 289s, suite log sha256
  ddf9813a8dbebc0aa50d442f3af03e240e7d6bfaba14145be86b616611f988e7. Nothing broad ran on the launching
  host (rule 21).

  Honest limit, stated rather than implied (rule 24): none of this proves lane REACHABILITY. That half
  is only provable by a dispatched run of these workflows on the deploy runner against the .201 dev
  lane, which requires the workflow files to be on dev first. This receipt is the tree-level proof; the
  reachability proof is the dispatched run that follows the merge.
exit_code: 0
pr_number: 3362
