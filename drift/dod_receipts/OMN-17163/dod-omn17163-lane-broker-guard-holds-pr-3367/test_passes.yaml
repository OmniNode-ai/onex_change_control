---
schema_version: "1.0.0"
ticket_id: "OMN-17163"
evidence_item_id: "dod-omn17163-lane-broker-guard-holds-pr-3367"
check_type: "test_passes"
check_value: |-
  uv run pytest tests/ci/test_dlq_and_baselines_lane_broker_omn17163.py -q
contract_sha256: "sha256:941a5afc45c73dcb051198545d8f2e4cf6f53d49a4b75bd5fd6a082138dedb4c"
contract_entry_sha256: "sha256:61bd289af668526ecedf6ec59fc38bfdc74cbb8b5c979728f700747a7006a232"
status: PASS
run_timestamp: "2026-09-09T18:58:16+00:00"
commit_sha: "1eef12c79886a3a34070dad80411890d5f170fe1"
runner: "omn17163-3367-companion"
verifier: "occ-union-resolve-same-ticket-companion"
probe_command: |-
  gh api repos/OmniNode-ai/omnibase_infra/commits/1eef12c79886a3a34070dad80411890d5f170fe1 --jq .sha \
    && (cd "${OMNI_HOME}/omni_worktrees/OMN-17163/omnibase_infra-3367" \
        && python -m pytest tests/ci/test_dlq_and_baselines_lane_broker_omn17163.py -q -p no:cacheprovider)
probe_stdout: |
  1eef12c79886a3a34070dad80411890d5f170fe1
  .....................                                                    [100%]
  21 passed in 0.19s
actual_output: |-
  PASS -- AC3. The pushed head resolves on OmniNode-ai/omnibase_infra and the PR-time guard passes at it:
  21 tests, run file-scoped in a detached worktree cut at that exact sha.

  WHAT THIS ITEM DOES AND DOES NOT CLAIM, said plainly because the distinction is the honest part. AC3 is
  a STANDING PROPERTY of the workflow file -- that it no longer silently depends on a repo variable which
  can be repointed without failing it closed. #3362 established that property; #3367 edits the same file
  and must not regress it. #3367 adds NO test of its own (its diff is exactly one workflow file, read as
  a full file listing, not sampled), so this is the applicable EXISTING guard, not a new one. Binding AC3
  to a regression proof of the property, rather than inventing a fresh check, is the accurate description
  of what was run.

  THE RED CONTROL WAS TAKEN IN THIS LANE, NOT ASSERTED. The same test file, byte-unmodified, was run
  against a tree assembled from the PRE-#3362 sources at 1e5145885f8b5d28b6e266f8b4c958c92ab96be8 -- the
  two workflows, chain-canary.yml and the publisher script, laid out under a scratch repo root so the
  test's own parents[2] resolution finds them:

    at 1eef12c79 (this head):   21 passed
    at 1e5145885 (pre-#3362):   17 failed, 4 passed

  The 17 failures span every runs-on pin, both host-local-literal assertions, the bootstrap-assignment
  ban, the routing-variable ban, the fail-closed lane loader and the credentials-as-environment rule.
  Without that control, 21 green would be indistinguishable from a test that asserts nothing.

  The test additionally carries its OWN live positive control on chain-canary.yml, the reference
  implementation both probes were brought into line with, so a predicate that is merely too strict fails
  loudly instead of passing vacuously.

  Provenance note, recorded rather than hidden: the run used the sibling OMN-17163 worktree's interpreter
  rather than a fresh uv sync in the detached worktree. That is sound here and was checked rather than
  assumed -- this test file imports only re, pathlib, pytest and yaml, and imports no omnibase_infra
  package at all, so no environment-dependent code path is exercised. Rule 21 observed: the selection is
  a single file bounded to well under a second, so nothing broad ran on the launching host.
exit_code: 0
pr_number: 3367
branch: "jonah/omn-17163-occ-companion-3367"
