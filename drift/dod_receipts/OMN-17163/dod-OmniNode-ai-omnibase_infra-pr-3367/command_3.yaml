---
schema_version: "1.0.0"
ticket_id: "OMN-17163"
evidence_item_id: "dod-OmniNode-ai-omnibase_infra-pr-3367"
check_type: "command"
check_value: |-
  gh api repos/OmniNode-ai/omnibase_infra/pulls/3367/files --paginate --jq '.[] | select(.filename==".github/workflows/dlq-depth-monitor.yml") | .patch' | grep -cE '^\+\s*ONEX_ALLOW_OMNIMARKET_DRIFT:\s*"1"'
contract_sha256: "sha256:941a5afc45c73dcb051198545d8f2e4cf6f53d49a4b75bd5fd6a082138dedb4c"
contract_entry_sha256: "sha256:6f80f79f6024d82e38742e0c6f1148d95388c265de5b9ee0887f423b2ffab49f"
status: PASS
run_timestamp: "2026-09-09T18:58:16+00:00"
commit_sha: "1eef12c79886a3a34070dad80411890d5f170fe1"
runner: "omn17163-3367-companion"
verifier: "occ-union-resolve-same-ticket-companion"
probe_command: |-
  gh api repos/OmniNode-ai/omnibase_infra/pulls/3367/files --paginate --jq '.[] | select(.filename==".github/workflows/dlq-depth-monitor.yml") | .patch' | grep -cE '^\+\s*ONEX_ALLOW_OMNIMARKET_DRIFT:\s*"1"'
probe_stdout: |
  1
actual_output: |-
  PASS: the drift-guard override is an ADDED line in the diff, not merely present in the file. Controlled
  in three directions in this lane rather than asserted:
    - NEGATIVE: the same anchored grep against the pre-#3362 file read from
      1e5145885f8b5d28b6e266f8b4c958c92ab96be8 returns 0. The line is genuinely new.
    - POSITIVE: the same grep against .github/workflows/chain-canary.yml at this head returns 1.
      chain-canary is the reference implementation already carrying the identical override on the identical
      runner for the identical reason, so this is a convergence onto an existing sanctioned pattern rather
      than a novel escape hatch.
    - SCOPE: the same patch grepped for '^[-+]\s*runs-on' returns 0. #3367 changes no runner selector,
      which is what keeps it clear of OMN-16682.
  Honest limit, recorded because the guard says it out loud: the guard's own refusal message states that
  results obtained under the override are "NOT evidence". That warning is about omnimarket-PROVIDED nodes
  being unresolvable in a drifted interpreter. It does not apply to this result, and the run proves it
  rather than arguing it -- the green run's runtime_identity block reports omnimarket source "absent"
  while node_dlq_depth_monitor_effect resolved and executed, because that node is provided by
  omnibase_infra itself. The guarded surface is genuinely unused here.
exit_code: 0
pr_number: 3367
branch: "jonah/omn-17163-occ-companion-3367"
