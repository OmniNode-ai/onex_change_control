---
schema_version: "1.0.0"
ticket_id: "OMN-13649"
evidence_item_id: "dod-deploy-assessment"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13649/dod-deploy-assessment/command.yaml
  && grep -q '^pr_number: 1458$' drift/dod_receipts/OMN-13649/dod-deploy-assessment/command.yaml && grep
  -qi 'no live deploy' drift/dod_receipts/OMN-13649/dod-deploy-assessment/command.yaml"
contract_sha256: "sha256:750858a87b2fc8ad9bdeef75d3a88aa1f21417ac4cd30e103cb6dcf795f5ea08"
status: PASS
run_timestamp: "2026-06-27T02:35:00Z"
commit_sha: "33b083d4287753cddd97eadf4e38f75c645493f0"
branch: "jonah/omn-13649-carry-tier-to-projection"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-13649/omnimarket"
pr_number: 1458
runner: "claude-code"
verifier: "claude-code-review"
probe_command: "git -C omnimarket diff --stat origin/dev...HEAD"
probe_stdout: |
  pyproject.toml + uv.lock pin bump to the omnibase_core cost_tier_name rev; handler-logic plumb in
  node_delegation_orchestrator/handlers/handler_delegation_workflow.py (terminal carry) +
  node_projection_delegation/handlers/handler_projection_delegation.py (converter); additive read-surface
  exposure columns in node_projection_delegation/contract.yaml (decisions.v1 + correlation-trace.v1); new
  unit tests. No deploy/runtime/topic/.201 surface touched; the delegation_events cost_tier_name +
  cost_tier_type columns already exist (migration 0018), so no migration runs.
actual_output: >-
  PASS: no live deploy, runtime restart, topic mutation, or .201 mutation is performed for OMN-13649 in
  PR #1458. Pure handler-logic plumb (terminal carry + projection converter) plus an additive projection-API
  read-surface exposure (decisions.v1 / correlation-trace.v1 columns) plus a dependency pin bump; dev
  lane only. This receipt does NOT claim the tier is already rendering live -- the live dev-lane proof
  (psql delegation_events shows a real cost_tier_name for a known correlation_id, and GET /projection/onex.snapshot.projection.delegation.decisions.v1
  returns the tier) is a SEPARATE serialized step after the cross-repo stack (core #1349 -> dev, omnimarket
  #1458 re-pin -> dev) lands and the projection redeploys. Static proof; runner claude-code != verifier
  claude-code-review.
exit_code: 0
