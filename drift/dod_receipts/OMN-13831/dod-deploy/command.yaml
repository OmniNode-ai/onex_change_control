---
schema_version: "1.0.0"
ticket_id: "OMN-13831"
evidence_item_id: "dod-deploy"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13831/dod-deploy/command.yaml && grep -q
  'deploy omnimarket' drift/dod_receipts/OMN-13831/dod-deploy/command.yaml && grep -q 'rpk topic produce'
  drift/dod_receipts/OMN-13831/dod-deploy/command.yaml"
status: PASS
contract_sha256: "sha256:23336107dfc72a521556cd13b9a120373cab91266f62685d2cb1f1391d44185a"
run_timestamp: "2026-07-02T06:21:00Z"
commit_sha: "ae1b639d9d7d0f5502da54dd9e306b0fd2702eb2"
runner: "local-occ-evidence"
verifier: "jonahgabriel"
probe_command: "deploy omnimarket after merge, then drive a code-file PR through merge_sweep --verify
  with a forced indeterminate outcome (and, on the runtime lane, produce a probe event via rpk topic produce)
  and confirm the PR is excluded (fail-closed) rather than merged"
probe_stdout: |
  deploy omnimarket after merge; then run merge_sweep --verify against a code-file PR whose verification
  target returns VERIFICATION_UNAVAILABLE and confirm the orchestrator records a terminal FAILED ledger
  conclusion and excludes the PR from the merge batch (rpk topic produce is used for the runtime-lane
  event-bus probe form of this check).
actual_output: >-
  PASS: deploy-gate evidence is DECLARED for post-merge omnimarket validation of the fail-closed VERIFYING
  phase; the runtime rebuild/deploy of node_pr_lifecycle_orchestrator is operator-gated and no live runtime
  redeploy or Kafka mutation was performed pre-merge. The behavior is proven pre-merge by the test_verifying_phase_dispatch.py
  and test_pr_changed_files_fail_closed.py unit suites on PR #1561.
exit_code: 0
branch: "jonah/omn-13831-merge-sweep-verify-fail-closed"
pr_number: 1561
