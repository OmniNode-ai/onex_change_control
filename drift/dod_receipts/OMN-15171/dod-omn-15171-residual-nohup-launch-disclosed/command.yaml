---
# Executed PASS receipt for one dod_evidence item of contracts/OMN-15171.yaml.
#
# PROVENANCE / SEPARATION. This receipt was produced by the
# omn15171-receipt-0730 reconciliation lane, which is NEITHER the implementer of
# the receipted work (steel#224 was built and opened by the fable-steel-0726
# session, subagent OMN-15171-builder, 2026-07-27T00:35Z/01:10Z) NOR the author
# of contracts/OMN-15171.yaml. Implementer != contract author != receipt author,
# so feedback_no_self_authored_evidence is satisfied rather than waived.
#
# EXECUTION. Every check_value of this item was run VERBATIM through
# `bash -o pipefail -c "<check_value>"` from the onex_change_control repo root on
# the .200 gate host, against the exact committed bytes of this PR. Each leg's
# PRODUCER stage was then re-run with the terminal assertion stripped, so the
# observed count is recorded rather than only an exit code -- an exit code alone
# cannot distinguish "asserted 1, saw 1" from "asserted 1, saw 1 by accident".
#
# BINDING. contract_entry_sha256 (OMN-13888) binds this receipt to the canonical
# JSON of its OWN dod_evidence item plus the immutable header, so appending a
# later item to the contract leaves it valid. yamlfmt reflowed the contract
# before hashing, and the per-entry hash is parse-based, so the value is stable
# across reflow.
schema_version: 1.0.0
ticket_id: OMN-15171
evidence_item_id: dod-omn-15171-residual-nohup-launch-disclosed
check_type: command
check_value: gh pr view 229 --repo jonahgabriel/steel_onslaught --json state,mergeCommit --jq '.state
  + " " + (.mergeCommit.oid // "none")' | grep -cFx 'MERGED 375c98015463b794c46dde59fca91c5805d19312'
  | grep -qx 1
contract_entry_sha256: sha256:508d3635566ae7a280db6bcd454a9b1ca0a6bf557f2cb1267a9d9db1afd59b0f
status: PASS
run_timestamp: '2026-07-30T06:51:41Z'
commit_sha: cf04fea53b4c08d885fe920017acf1217f0d3e8b
runner: stickybeatz-studio-gate-host
verifier: executed-probe
probe_command: 'for each of the 4 check_value(s) in dod_evidence item dod-omn-15171-residual-nohup-launch-disclosed:
  bash -o pipefail -c "<check_value>", run from the onex_change_control repo root on the .200 gate host
  (Stickybeatz-Studio.local) against the committed bytes of this PR'
probe_stdout: |
  leg 1  exit=0  assert=1  observed=1  (283 ms)
  leg 2  exit=0  assert=1  observed=1  (252 ms)
  leg 3  exit=0  assert=1  observed=1  (222 ms)
  leg 4  exit=0  assert=1  observed=1  (227 ms)

  `exit` is the verbatim status of `bash -o pipefail -c "<check_value>"`; `observed` is the count emitted by the same pipeline with its terminal assertion stripped, so the asserted and measured values are both on the record.
actual_output: |
  PASS -- 4/4 check(s) executed and green.

  FOUR legs, mixed binding by design, all GREEN. This is the item adversarial
  review F1 hit, and the fourth leg is that finding's fix.

  THE DEFECT F1 FOUND. The item previously bound only PINNED refs (`gh pr view 229`
  plus `?ref=375c98015463b794c46dde59fca91c5805d19312`) while claiming that
  deleting the disclosure would drive it RED. That claim was FALSE: content at an
  immutable commit SHA is frozen forever, so a pinned read cannot observe a later
  deletion on main. Someone removing the `## 5. Launch discipline:` section from
  main would have left all three legs GREEN in perpetuity while the residual
  resumed being silently inherited -- the exact outcome the item claims to prevent.

  THE FIX, AND ITS PROOF. Leg 4 reads the SAME runbook path with NO `?ref=` pin, so
  it resolves the repository default branch. Proven by readback rather than
  asserted, on the gate host:
      default_branch                                 = main
      live main HEAD                                 = 61ac5220872d625c9c88be24964c563df41c7c62
      run_display_salience_battery.py @ pinned 491c52a7 blob = 1bb9ed8f9fa26c1f6af3323052d7f96221094ef7
      run_display_salience_battery.py @ NO ref       blob = 9dc71debaf7180bde9f417fc72ef20d5322fba26
      run_display_salience_battery.py @ ?ref=main    blob = 9dc71debaf7180bde9f417fc72ef20d5322fba26
  The unpinned form returns the SAME blob as ?ref=main and a DIFFERENT blob from
  the pinned form, so the no-ref shape demonstrably tracks main and pinned reads
  demonstrably do not.

  WHY LEGS 1-3 STAY PINNED. They are the historical provenance anchor -- they prove
  the disclosure LANDED in steel#229 at that exact commit, a fact later history
  cannot rewrite. Only leg 4 makes the disclosure a LIVE invariant. Retiring the
  pinned legs in favour of the live one would trade provenance for liveness; the
  item needs both.

  RED CONTROLS, executed on the gate host, each driving leg 4 RED:
      * marker mutated to '## 5. Launch discipline REMOVED:' on live main -> exit 1
      * path mutated to a runbook that does not exist on main             -> exit 1
      * expected count mutated from 1 to 2 at the true marker             -> exit 1
  So leg 4 fails on deletion of the section, on removal of the file, and on any
  drift in occurrence count -- which is what F1 asked for. Leg 1 was falsified too:
  the #229 state+SHA comparison with the last hex digit altered (...19313) exits 1.

  RESIDUAL ITSELF, unchanged and not papered over: OS-level `nohup ... &` was NOT
  eliminated. The accepted OMN-15172 run launched this driver under `nohup ... &`
  from `chain_prominent.sh`, an uncommitted per-corner chaining wrapper. Closing
  that (a committed, supervised launch surface) is org-side work open under
  OMN-15265, not a gap in steel#224.
exit_code: 0
duration_ms: 984
pr_number: 5567
branch: jonah/omn-15171-occ-receipt-contract
working_dir: /Users/jonah/Code/omni_home/omni_worktrees/OMN-15171/onex_change_control
