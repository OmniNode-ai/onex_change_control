---
# Executed PASS receipt for one dod_evidence item of contracts/OMN-15171.yaml.
#
# PROVENANCE / SEPARATION. This receipt was produced by the
# omn15171-receipt-0730 reconciliation lane, which is NEITHER the implementer of
# the receipted work (steel#224 was built and opened by the fable-steel-0726
# session, subagent OMN-15171-builder, 2026-07-27T00:35Z/01:10Z) NOR the author
# of contracts/OMN-15171.yaml. Implementer != contract author != receipt author,
# so feedback_no_self_authored_evidence is satisfied rather than waived.
#
# EXECUTION. Every check_value of this item was run VERBATIM through
# `bash -o pipefail -c "<check_value>"` from the onex_change_control repo root on
# the .200 gate host, against the exact committed bytes of this PR. Each leg's
# PRODUCER stage was then re-run with the terminal assertion stripped, so the
# observed count is recorded rather than only an exit code -- an exit code alone
# cannot distinguish "asserted 1, saw 1" from "asserted 1, saw 1 by accident".
#
# BINDING. contract_entry_sha256 (OMN-13888) binds this receipt to the canonical
# JSON of its OWN dod_evidence item plus the immutable header, so appending a
# later item to the contract leaves it valid. yamlfmt reflowed the contract
# before hashing, and the per-entry hash is parse-based, so the value is stable
# across reflow.
schema_version: 1.0.0
ticket_id: OMN-15171
evidence_item_id: dod-omn-15171-other-arms-untouched
check_type: command
check_value: gh api 'repos/jonahgabriel/steel_onslaught/commits/491c52a76d90324e557f7638b7fb7312ca54057a'
  --jq '.files[].filename' | grep -cE '^(mypy\.ini|scripts/run_display_salience_battery\.py|tests/cli/test_display_salience_battery_driver\.py)$'
  | grep -qx 3
contract_entry_sha256: sha256:173b21b7283a203c8f79a3aeefe175e3143e70646fe2bc0c72ad00035689339a
status: PASS
run_timestamp: '2026-07-30T06:51:41Z'
commit_sha: cf04fea53b4c08d885fe920017acf1217f0d3e8b
runner: stickybeatz-studio-gate-host
verifier: executed-probe
probe_command: 'for each of the 3 check_value(s) in dod_evidence item dod-omn-15171-other-arms-untouched:
  bash -o pipefail -c "<check_value>", run from the onex_change_control repo root on the .200 gate host
  (Stickybeatz-Studio.local) against the committed bytes of this PR'
probe_stdout: |
  leg 1  exit=0  assert=3  observed=3  (316 ms)
  leg 2  exit=0  assert=3  observed=3  (280 ms)
  leg 3  exit=0  assert=1  observed=1  (227 ms)

  `exit` is the verbatim status of `bash -o pipefail -c "<check_value>"`; `observed` is the count emitted by the same pipeline with its terminal assertion stripped, so the asserted and measured values are both on the record.
actual_output: |
  PASS -- 3/3 check(s) executed and green.

  All three legs GREEN. The merge commit's file list is pinned as a CLOSED SET:
  3 matches against the anchored whitelist and 3 filenames in total, plus
  `class OpenAICompatibleClient` retained at the merge SHA for the non-migrated
  arms.

  CORRECTED RATIONALE (adversarial review F2 -- the contract description carried a
  wrong explanation of which leg catches what, and it is fixed in this same push).
  The two legs are complementary in a specific direction each:
    * leg 2 ALONE catches an ADDITION -- a fourth touched file returns 4 and exits 1;
    * leg 1 ALONE catches a SUBSTITUTION -- swapping a whitelisted path for some
      other path keeps the total at 3 (leg 2 stays GREEN) while dropping the
      whitelist count to 2.
  The earlier claim that the fourth-file case is caught by "neither leg alone" was
  false. Both legs are load-bearing; only the stated reason was wrong.

  RED CONTROLS, executed on the gate host, one per direction:
    * ADDITION direction -- leg 2 with its expected count mutated from 3 to 4 exits
      1 (observed 3);
    * SUBSTITUTION direction -- leg 1 with the whitelist narrowed from three paths
      to two, holding the assertion at 3, exits 1 (observed 2), which is exactly
      the count a swapped-out whitelisted path would produce.
exit_code: 0
duration_ms: 823
pr_number: 5567
branch: jonah/omn-15171-occ-receipt-contract
working_dir: /Users/jonah/Code/omni_home/omni_worktrees/OMN-15171/onex_change_control
