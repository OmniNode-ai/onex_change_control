---
schema_version: "1.0.0"
ticket_id: "OMN-15374"
evidence_item_id: "dod-15374-apply-role-unchanged-superseded"
check_type: "command"
check_value: >-
  receipt="drift/dod_receipts/OMN-15374/dod-15374-apply-role-unchanged/command.yaml" && grep -q '^status:
  PASS$' "$receipt" && grep -A1 '^probe_stdout: |$' "$receipt" | tail -n1 | grep -qx '  0' && grep -q
  'apply-capable role unchanged binding PASS' "$receipt" && printf 'apply-capable role unchanged binding
  field-name repair PASS\n'
contract_entry_sha256: "sha256:48d54cab416b96360e52d97234fd786eb9bc66e01b6674de820c715de422da6c"
status: PASS
run_timestamp: "2026-08-04T19:42:06Z"
commit_sha: "a3573aae2d54d412843ffaf9c2c7ad81985b1b37"
runner: "claude-code-agent:occ-stale-refresh-0804"
verifier: "claude-code-agent:opus-refresh-checker-0804"
probe_command: >-
  receipt="drift/dod_receipts/OMN-15374/dod-15374-apply-role-unchanged/command.yaml" && grep -q '^status:
  PASS$' "$receipt" && grep -A1 '^probe_stdout: |$' "$receipt" | tail -n1 | grep -qx '  0' && grep -q
  'apply-capable role unchanged binding PASS' "$receipt" && printf 'apply-capable role unchanged binding
  field-name repair PASS\n'
probe_stdout: |
  apply-capable role unchanged binding field-name repair PASS
actual_output: |
  apply-capable role unchanged binding field-name repair PASS
exit_code: 0
