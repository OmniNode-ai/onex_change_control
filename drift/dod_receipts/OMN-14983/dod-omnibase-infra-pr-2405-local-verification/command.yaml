---
schema_version: "1.0.0"
ticket_id: "OMN-14983"
evidence_item_id: "dod-omnibase-infra-pr-2405-local-verification"
check_type: "command"
check_value: "gh pr view 2405 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep -F 'getent
  group 1001' && gh pr view 2405 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep -F 'shellcheck'
  && gh pr view 2405 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep -F 'pre-commit run
  --files <4 changed files>'"
contract_entry_sha256: "sha256:10a0e8ea12e04785b3988b94f70335dde5754e80bfb262b174c01acabe85f0d2"
status: PASS
run_timestamp: "2026-07-23T22:24:00Z"
commit_sha: "046b3075b36faad9753d4e04781be2efae38d725"
runner: "codex-merge-controller"
verifier: "occ-evidence-source-autobind"
probe_command: "gh pr view 2405 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep -F 'getent
  group 1001' && gh pr view 2405 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep -F 'shellcheck'
  && gh pr view 2405 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep -F 'pre-commit run
  --files <4 changed files>'"
probe_stdout: |
  - Root cause confirmed live: `getent group 1001` on `.201`/`omninode-pc` -> `daniyal:x:1001:`; host operator env file confirmed `-rw-------` owned `jonah:jonah` (uid/gid 1000).
  - `shellcheck` clean on all 3 edited shell scripts (zero findings).
  - `pre-commit run --files <4 changed files>` -- full local hook suite green, including `ONEX Node Migration Vendor Sync Check` (no migration drift introduced) and `Shell hygiene (shellcheck, OMN-14761)`.
actual_output: "PASS: PR #2405 body records live root-cause proof and local validation gates."
exit_code: 0
pr_number: 2405
branch: "jonah/omn-14983-deploy-runner-operator-env-perms"
