---
schema_version: "1.0.0"
ticket_id: "OMN-13415"
evidence_item_id: "dod-omnibase-infra-pr-2105"
check_type: "command"
check_value: "uv run pytest tests/scripts/test_check_deployed_migration_tree_sync.py -q"
status: PASS
run_timestamp: "2026-06-24T20:40:00Z"
commit_sha: "5bb8b7f394625b397cf198d7cc6ba5ba5946af79"
runner: "claude-omn-13415-builder"
verifier: "claude-receipt-review"
contract_sha256: "sha256:cc883e70be11362152bdf299cf012cd5f78159d8ae965b4ceed3688185d1075a"
branch: "jonah/omn-13415-deployed-migration-sync-gate"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-13415/omnibase_infra"
pr_number: 2105
probe_command: "uv run pytest tests/scripts/test_check_deployed_migration_tree_sync.py -q"
probe_stdout: |
  .........                                                                [100%]
  9 passed in 2.07s
actual_output: |
  9 passed: the deployed-migration-tree sync gate covers in-sync-passes (byte-identical -> exit 0),
  stale-fails (the OMN-13415 footgun: a deployed file with stale bytes -> exit 1), missing-fails (a
  migration present in the clone but absent from the deployed tree, i.e. the missing-0018/0019 case
  -> exit 1), extra-leftover-fails (a stale leftover absent from the clone @ ref -> exit 1),
  nested-node-migration drift (docker/migrations/forward/nodes/<node>/ compared too -> exit 1), and
  bad-ref / absent-deployed-tree / non-git-clone config errors (-> exit 2, fails closed), plus a live
  subprocess smoke run. The existing test_deploy_runtime_build_context.py stays 7/7 green. ruff +
  mypy --strict clean; bash -n + actionlint clean; pre-commit all-hooks green. The diff touches no
  docker/migrations/forward/ files; the ONEX Node Migration Vendor Sync drift over
  node_projection_cost_by_repo / node_projection_traces is pre-existing on origin/dev.
exit_code: 0
duration_ms: 2070
