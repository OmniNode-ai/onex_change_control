---
schema_version: 1.0.0
ticket_id: OMN-14542
evidence_item_id: dod-verify-1739-required-census-fails-closed
check_type: command
check_value: >-
  f="$(mktemp)" && gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_contract_sweep/handlers/handler_contract_sweep.py?ref=a0dc1746b95a038e4aa7a51dc96e973b26a55abb"
  --jq .content | base64 -d > "$f" && grep -Fq -- 'repos: list[str] = Field(' "$f" && grep -Fq -- 'min_length=1'
  "$f" && grep -Fq -- 'env_omni_home = os.environ["OMNI_HOME"]' "$f" && grep -Fq -- 'except KeyError:'
  "$f" && grep -Fq -- 'scanned_count: int = Field(' "$f" && grep -Fq -- 'missing_repos: list[str] = Field('
  "$f" && grep -Fq -- 'scope_error: str = Field(' "$f" && grep -Fq -- 'if missing_repos:' "$f" && grep
  -Fq -- 'if contracts_checked == 0:' "$f" && grep -Fq -- 'status=EnumSweepStatus.ERROR,' "$f"
contract_sha256: sha256:7789691b51874994002e0e623328a31141dbaf11f0134c25655a5fecb3c2ea9a
contract_entry_sha256: sha256:0ee874ff999ef2b70a1dc9ce1f183c22d6c0ac5e42ea339a52e0614af229cd49
status: PASS
run_timestamp: '2026-07-13T05:37:59Z'
commit_sha: a0dc1746b95a038e4aa7a51dc96e973b26a55abb
runner: claude-sonnet-5
verifier: occ-interim-manual-verify
pr_number: 1739
probe_command: 'gh api .../handler_contract_sweep.py?ref=a0dc1746b95a038e4aa7a51dc96e973b26a55abb -- read
  PR head source directly'
probe_stdout: |
  Read handler_contract_sweep.py from omnimarket PR#1739 head a0dc1746b95a038e4aa7a51dc96e973b26a55abb.
  Confirmed: ContractSweepRequest.repos is declared Field(..., min_length=1) — required, non-empty,
  no default_factory=list; OMNI_HOME is read via os.environ["OMNI_HOME"] inside a try/except
  KeyError, with no Path(__file__).parents fallback surviving; ContractSweepResult declares
  scanned_count, status, missing_repos, and scope_error fields; and the handler returns
  EnumSweepStatus.ERROR both when missing_repos is non-empty (a requested repo absent on disk,
  even alongside other valid repos) and when contracts_checked == 0 — closing the fail-open path
  where an unresolvable or partially-narrowed scope reported the same silent PASS as a genuinely
  healthy one.
exit_code: 0
actual_output: |
  CHECK1_PASS -- all ten substring assertions matched against the live PR head file.
branch: jonah/omn-14542-occ-companion-contract-sweep
