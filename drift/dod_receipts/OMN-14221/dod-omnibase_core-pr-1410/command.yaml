---
schema_version: "1.0.0"
ticket_id: "OMN-14221"
evidence_item_id: "dod-omnibase_core-pr-1410"
check_type: "command"
check_value: "cd omnibase_core && env -u PYTHONPATH uv run lint-imports"
status: PASS
run_timestamp: "2026-07-09T23:29:38Z"
commit_sha: beb1a3429973e908bed358a9ef1065dc147350b2
runner: "omn-14221-independent-verifier"
verifier: "occ-evidence-lane"
probe_command: >-
  cd /Users/jonah/Code/omni_home/omni_worktrees/OMN-14221/omnibase_core && env -u PYTHONPATH uv run lint-imports
probe_stdout: |
  Analyzed 4078 files, 10326 dependencies.
  Foundation primitives must not import higher layers (direct) KEPT
  Models must not import behavioral/orchestration layers (direct) KEPT
  Services and validation must not import nodes (direct) KEPT
  Protocols (seam) must not import domain/behavioral layers (direct) KEPT
  Contracts: 4 kept, 0 broken.
actual_output: >-
  PASS: omnibase_core PR #1410 retires the frozen constants->models import-layering back-edge by changing
  the two boundary-validation raises in constants/constants_handler_capabilities.py's get_capabilities_by_node_kind
  from ModelOnexError to the built-in ValueError (matching the function docstring Raises: ValueError and
  CLAUDE.md error-handling doctrine), folding error_code/context into the message with the # error-ok:
  boundary marker, dropping the now-unused ModelOnexError import, and retiring the .importlinter ignore
  line. Independent reproduction (PYTHONPATH cleared; grimp built the worktree src at file:///Users/jonah/Code/omni_home/omni_worktrees/OMN-14221/omnibase_core)
  kept all 4 contracts (Analyzed 4078 files, 10326 dependencies; 4 kept, 0 broken). Negative control:
  restoring the old ModelOnexError import with the ignore line still removed broke core-foundation-no-upward
  (3 kept, 1 broken: constants.constants_handler_capabilities -> models.errors.model_onex_error l.43),
  proving the edge is genuinely gone, not wildcard-masked. grep 'from omnibase_core.models' under src/omnibase_core/constants/
  returns nothing. ValueError correctness: ModelOnexError derives directly from Exception (MRO Exception,
  BaseException, object), issubclass(ModelOnexError, ValueError) is False, so the flipped test assertion
  pytest.raises(ValueError) is a genuine flip. mypy Success (1 source file); 68 tests passed; ruff clean;
  pre-commit changed files 0 failed (import-linter Passed, ONEX Error Raising Validation Passed; marker-stripped
  negative control exit 1 / 2 violations proves the # error-ok: markers are required).
exit_code: 0
pr_number: 1410
branch: "jonah/omn-14221-retire-constants-models-backedge"
contract_sha256: "sha256:41acc84bf27568d7a2641f333fcfa8fb6d8731b0e45585be4b6bf7c0ab6527c3"
