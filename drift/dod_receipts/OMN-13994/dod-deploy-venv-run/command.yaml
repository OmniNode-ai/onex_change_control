---
schema_version: "1.0.0"
ticket_id: OMN-13994
evidence_item_id: dod-deploy-venv-run
check_type: command
check_value: "ONEX_CC_REPO_PATH=/onex_change_control OMNI_HOME=$OMNI_HOME env -u PYTHONPATH uv run onex
  skill integration_sweep --timeout 150"
status: PASS
run_timestamp: "2026-07-05T22:46:00Z"
commit_sha: "e2c4b117e99e801432084ef1baffcb9b8330e6e0"
runner: "claude-opus-4-8"
verifier: "claude-opus-4-8-verify-omn-13994"
probe_command: "cd $OMNI_HOME/omnibase_infra && env -u PYTHONPATH uv pip install --python .venv/bin/python
  --no-deps $OMNI_HOME/omni_worktrees/OMN-13994/omnimarket && env -u PYTHONPATH ONEX_CC_REPO_PATH=/onex_change_control
  OMNI_HOME=$OMNI_HOME uv run onex skill integration_sweep --timeout 150"
exit_code: 0
contract_sha256: "sha256:c4e5c6de92de22fe50cd1c07972d6b57b81c64ca28db1ed3020df17e38fef7ba"
branch: "jonah/omn-13994-onex-cc-repo-path-registry-fallback"
working_dir: "/Users/jonah/Code/omni_home/omnibase_infra"  # local-path-ok: receipt evidence for OMN-13994
probe_stdout: "status=success exit_code=0 terminal status=recorded artifact_written=true; resolved artifact_root
  fell back to $OMNI_HOME/onex_change_control"
actual_output: >
  PASS: co-installed the OMN-13994 PR-branch omnimarket (head e2c4b117, carrying the registry-aware ONEX_CC_REPO_PATH
  fallback) into the canonical omnibase_infra .venv as a site-packages install (the deploy surface). Ran
  'onex skill integration_sweep' from omnibase_infra with a STALE container-style ONEX_CC_REPO_PATH=/onex_change_control
  (which has no contracts/ dir on this host) and OMNI_HOME set. The handler's _resolve_artifact_root detected
  the stale path lacked contracts/ and fell back to $OMNI_HOME/onex_change_control, so the sweep ran to
  completion: status=success, exit_code=0, terminal status=recorded, artifact_written=true (drift/integration/2026-07-05.yaml),
  GITHUB_CI surface probe pass. Pre-fix, the same stale ONEX_CC_REPO_PATH raised RuntimeError('...does
  not exist or lacks a contracts/ directory') before the sweep could run. The .201 RUNTIME_HEALTH/CONTAINER_HEALTH
  probes fail (operator is traveling; the 192.168.86.201 LAN host is not routable off-network) — that
  is an environment fact, not a regression, and does not gate the fallback proof. Fail-fast (CLAUDE.md
  rule #8) is preserved: OMNI_HOME is read via os.environ[...] (KeyError when unset) and a fallback that
  itself lacks contracts/ still raises.
