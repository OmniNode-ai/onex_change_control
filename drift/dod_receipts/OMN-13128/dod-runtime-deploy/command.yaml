---
schema_version: "1.0.0"
ticket_id: "OMN-13128"
evidence_item_id: "dod-runtime-deploy"
check_type: "command"
check_value: "PRE-DEPLOY proof gate (repro RED->GREEN + local verify); live acceptance is the deferred
  operator-gated 'rpk topic produce onex.cmd.omnimarket.context-roi-run-requested.v1' K>=10 multi-cell
  stability REBUILD-6 re-probe"
status: PASS
run_timestamp: "2026-06-13T15:50:00Z"
commit_sha: "5ce878788e6798bb82adce4c558f554a97c65c92"
runner: "omn-13128-impl"
verifier: "receipt-gate-local"
probe_command: "git stash push -- src/omnibase_infra/runtime/service_pattern_b_broker.py; uv run pytest
  tests/integration/test_terminal_consumer_subscription_flip_wedge.py -q; git stash pop; uv run pytest
  tests/integration/test_terminal_consumer_subscription_flip_wedge.py -q; uv run mypy src/omnibase_infra/runtime/service_pattern_b_broker.py
  --strict"
probe_stdout: |
  RED (source reverted to single consumer assigned both topics): the subscription flip drops every COMPLETED terminal; the K>=10 x 2-cell x 2-arm matrix burns its per-arm window every trial and exceeds the run timeout — wedge reproduced.
  GREEN (fix restored, one independent consumer per terminal topic): "1 passed in 2.31s" — every trial correlates, both cells/arms advance, COMPLETED HWM climbs to 40, FAILED HWM stays 0.
  mypy --strict: Success: no issues found in 1 source file. ruff clean. tests/unit/runtime + repro + #1970 battery: 4882 passed / 5 skipped.
actual_output: "PASS (pre-deploy proof gate): the subscription-flip repro is RED-then-GREEN gated on the
  source fix and local verify is clean, so the fix is authorized to roll into a gated stability rebuild.
  The full live K>=10 multi-cell re-probe on the stability lane is the operator-gated acceptance tracked
  on this contract's description and is not part of this pre-deploy gate."
exit_code: 0
contract_sha256: "sha256:a94d2b8519ad34ca15bb38d94c6f20190d659a01ffc830e17230db8cca3e2308"
pr_number: 2605
branch: "jonah/omn-13128-occ-deploy-item"
