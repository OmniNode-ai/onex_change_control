---
schema_version: "1.0.0"
ticket_id: "OMN-14375"
evidence_item_id: "dod-verify-2262-product-observing-repair"
check_type: "command"
check_value: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/enums/generated/enum_omnibase_infra_topic.py?ref=b134fc473aa7a4e32d7c99a67fb4a6d88fe91fdd"
  --jq .content | base64 -d | grep -c 'PR_STATE')" -ge 1
status: PASS
run_timestamp: "2026-07-12T07:10:00Z"
commit_sha: "b134fc473aa7a4e32d7c99a67fb4a6d88fe91fdd"
runner: "occ-queue-runner"
verifier: "occ-queue-verifier"
pr_number: 2262
probe_command: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/enums/generated/enum_omnibase_infra_topic.py?ref=b134fc473aa7a4e32d7c99a67fb4a6d88fe91fdd"
  --jq .content | base64 -d | grep -c 'PR_STATE')" -ge 1   # then re-run verbatim with ref=dev and ref=<bogus
  SHA> as RED controls
probe_stdout: |
  # bound check_value, run verbatim:
  #   ref=b134fc47 (omnibase_infra#2262 head) -> exit 0   GREEN ('PR_STATE' present)
  #   ref=dev            (omnibase_infra dev)            -> exit 1   RED   ('PR_STATE' absent)
  #   ref=000...0        (bogus SHA)                     -> exit 1   RED
exit_code: 0
actual_output: >-
  PASS: OMN-14436 REPAIR. Every pre-existing entry in OMN-14375's contract is INERT -- it greps this repo's
  own receipt store. That is a PLATFORM WIRING DEFECT, not an authoring failure: the DoD runner executed
  check_values with --workspace pointed at the onex_change_control clone rather than the product checkout,
  so the receipt was the only file on disk and NO other kind of check could have run. The author wrote
  exactly what the tooling accepted. We fix the debt we created; we do not invoice it to whoever is holding
  it. This entry adds the product-observing evidence that was impossible to write before. It reads omnibase_infra#2262's
  source at its exact head SHA via the GitHub contents API and asserts 'PR_STATE'. OMN-14375 ships a pr_state
  projection that consumes a GitHub-state fan-in topic. If the topic is not registered in the generated
  enum, nothing can publish to it and the projection is dead on arrival. The topic registration is the
  load-bearing fact, not the migration file's existence. RED DIRECTION PROVEN by execution: the symbol
  occurs at #2262's head and ZERO times at omnibase_infra dev, so the identical command exits 1 at dev
  and at a bogus SHA. It exits 0 only where the code exists. APPEND-ONLY: no existing dod_evidence entry
  and no merged receipt was modified.
branch: "jonah/omn-14436-repair-inert-contracts"
contract_sha256: "sha256:9ff196578c71d70ccee7d69483418c3b7831c530dcaed598b9d205af85d88ee6"
contract_entry_sha256: "sha256:71f58f473777b6f7a51574bcc30644ab5e9b23f2007d420f15dbd0cd88133046"
