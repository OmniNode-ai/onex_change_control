---
schema_version: "1.0.0"
ticket_id: "OMN-15418"
evidence_item_id: "dod-deploy-assessment-falsifiable"
check_type: "command"
supersedes: "drift/dod_receipts/OMN-15418/dod-deploy-assessment-falsifiable/command.yaml"
reason: |-
  Honesty correction, add-only. The superseded receipt (merged in OCC #5937 at 622c78f631bdc4110064dd41c10158430eb4ea04) states two things that are false. (1) FACT — its actual_output says "#2548 changed 130 files and none is a compose file". #2548 ADDED two compose files, docker/application-acl-proof/compose.yml and docker/domain-adapter-proof/compose.yml, both status "added", re-verified 2026-08-02 against the live 130-path list. The true, narrower, load-bearing half is that docker/docker-compose.e2e.yml — the single path the dead generated dod-deploy-assessment probe greps for — is absent from those 130 paths. (2) FRAMING — its actual_output says "RED-proven in the same pass ... The check therefore discriminates", and the entry it binds is named "-falsifiable", together presenting the probe as escaping the OMN-14443 unfalsifiable class. It does not. The probe greps file content at an immutable 40-hex commit pin, so at that pin it exits 0 unconditionally and can never go RED there. The rc=1-at-the-first-parent result proves content DISCRIMINATION, which is real but strictly weaker than falsifiability at the probe's own pin; the superseded prose conflated the two. The merged base receipt is immutable under validator_occ_append_only and is left byte-for-byte intact; this net-new higher-token record carries the corrected text and re-binds the key. The check is UNCHANGED and re-run live — the defect was the claim about the probe, never the probe, so the bar is not weakened. At the contract level this entry is separately retired add-only by dod-deploy-assessment-merged-tree-attestation (OCC #5945).
superseder: "chain-head-0802-honesty/occ-attestation-probe"
created_at: "2026-08-02T06:10:50Z"
replacement:
  schema_version: "1.0.0"
  ticket_id: "OMN-15418"
  evidence_item_id: "dod-deploy-assessment-falsifiable"
  check_type: "command"
  check_value: |-
    gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/validation/application_relation_ownership.py?ref=3863623e4844dc9b0db69ceb4e77942737fbca32' --jq .content | base64 -d | grep -F 'def validate_application_relation_ownership(' > /dev/null && gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/validation/application_relation_ownership.py?ref=3863623e4844dc9b0db69ceb4e77942737fbca32' --jq .content | base64 -d | grep -F 'code=EnumApplicationRelationViolation.DUPLICATE_OWNER,' > /dev/null
  contract_entry_sha256: "sha256:3cf486a88d53ed838a9ca8d56b0306f45dac3305730e83f7ea49b1b412cc52d0"
  status: PASS
  run_timestamp: "2026-08-02T06:10:50Z"
  commit_sha: "3863623e4844dc9b0db69ceb4e77942737fbca32"
  runner: "chain-head-0802-honesty"
  verifier: "chain-head-0802-honesty/occ-attestation-probe"
  probe_command: |-
    gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/validation/application_relation_ownership.py?ref=3863623e4844dc9b0db69ceb4e77942737fbca32' --jq .content | base64 -d | grep -F 'def validate_application_relation_ownership('; gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/validation/application_relation_ownership.py?ref=3863623e4844dc9b0db69ceb4e77942737fbca32' --jq .content | base64 -d | grep -F 'code=EnumApplicationRelationViolation.DUPLICATE_OWNER,'
  probe_stdout: |
    def validate_application_relation_ownership(
                    code=EnumApplicationRelationViolation.DUPLICATE_OWNER,
                        code=EnumApplicationRelationViolation.DUPLICATE_OWNER,
                        code=EnumApplicationRelationViolation.DUPLICATE_OWNER,
  actual_output: |
    PASS under bash, exit 0, re-run live 2026-08-02T06:10:50Z with the committed check_value executed verbatim. SCOPE — this is a POINT-IN-TIME ATTESTATION against an immutable commit pin. It makes NO ongoing-falsifiability claim and it does not claim the check escapes the OMN-14443 unfalsifiable class; both claims appear in the receipt this record supersedes and both are withdrawn here. WHAT IT ATTESTS: the merged tree at PR OmniNode-ai/omnibase_infra#2548's squash merge commit 3863623e4844dc9b0db69ceb4e77942737fbca32 carries the OMN-15418 deliverable — the application-relation ownership validator definition and the exactly-one-owner enforcement call site — both in src/omnibase_infra/validation/application_relation_ownership.py. WHAT IT DOES NOT ATTEST: the probe greps content at a 40-hex commit pin, so at that pin it exits 0 unconditionally; it is frozen-true there and cannot go RED there. DISCRIMINATION, NOT FALSIFIABILITY: the probed file was ADDED by #2548 (858 additions, status "added") and returns HTTP 404 at that merge commit's first parent beebd1d636b9182b5ba80c7b68daf5ecfc7e76d3, so the identical probe repointed at the parent exits 1 (re-verified 2026-08-02). That proves the probe reads real bytes rather than passing vacuously; it does NOT prove the probe can go RED at its own pin. CORRECTED FACT: the superseded receipt says "#2548 changed 130 files and none is a compose file". False — #2548 ADDED docker/application-acl-proof/compose.yml and docker/domain-adapter-proof/compose.yml, both status "added". The accurate, narrower, load-bearing statement is that docker/docker-compose.e2e.yml, the single path the dead generated dod-deploy-assessment probe greps for, is absent from those 130 paths, which is why that probe exits 1 permanently. The merged base receipt is unedited and stays verbatim for audit. Executor shape: first shlex token is gh, zero command substitution, both legs terminate in grep -F redirected to /dev/null on fully drained input so neither meets the base64 -d SIGPIPE tier-1 producer hazard that grep -q would introduce; the ?ref= pin is a 40-hex commit SHA, not a deletable branch ref.
  exit_code: 0
  pr_number: 2548
  branch: "jonah/omn-15418-honesty-supersession"
  no_source_change: true
