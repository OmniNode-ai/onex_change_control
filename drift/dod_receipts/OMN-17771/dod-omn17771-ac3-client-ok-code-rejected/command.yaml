---
schema_version: "1.0.0"
ticket_id: "OMN-17771"
evidence_item_id: "dod-omn17771-ac3-client-ok-code-rejected"
check_type: "command"
check_value: |
  guide="$(gh api 'repos/OmniNode-ai/knowledge-base-internal/contents/guides/customer-getting-started.md?ref=055da140d8b2e772464ee7a50802aee851d29b4b' --jq '.content' | base64 -d)" && test -n "$guide" && printf '%s' "$guide" | grep -qF 'grant_type=authorization_code' && body="$(curl -sS -X POST "https://dev.auth.omninode.ai/realms/omninode/protocol/openid-connect/token" -H "Content-Type: application/x-www-form-urlencoded" -d "grant_type=authorization_code" -d "code=deliberately-invalid-code" -d "redirect_uri=https://dev.app.omninode.ai" -d "code_verifier=occ-ac3-deterministic-verifier-0001" -d "client_id=onex-customer")" && printf '%s' "$body" | grep -qF '"error":"invalid_grant"'
contract_sha256: "sha256:bd5a01cff0f6b55998fbfe3fd6139061a4a4e4ea30c0611d1ed10c1de6ea538c"
contract_entry_sha256: "sha256:064c16ec8ca4ac5ecbdc844d992e76b2ef2b26339edd47c62acab8162263cdf7"
status: "PASS"
run_timestamp: '2026-09-10T02:53:11Z'
commit_sha: "055da140d8b2e772464ee7a50802aee851d29b4b"
runner: "claude-local-worktree"
verifier: "omn17771-guides-onex-customer"
probe_command: |
  guide="$(gh api 'repos/OmniNode-ai/knowledge-base-internal/contents/guides/customer-getting-started.md?ref=055da140d8b2e772464ee7a50802aee851d29b4b' --jq '.content' | base64 -d)" && test -n "$guide" && printf '%s' "$guide" | grep -qF 'grant_type=authorization_code' && body="$(curl -sS -X POST "https://dev.auth.omninode.ai/realms/omninode/protocol/openid-connect/token" -H "Content-Type: application/x-www-form-urlencoded" -d "grant_type=authorization_code" -d "code=deliberately-invalid-code" -d "redirect_uri=https://dev.app.omninode.ai" -d "code_verifier=occ-ac3-deterministic-verifier-0001" -d "client_id=onex-customer")" && printf '%s' "$body" | grep -qF '"error":"invalid_grant"'
probe_stdout: |
  exit=0 stdout=(empty)
  exit=0 stdout=(empty)
actual_output: |
  PASS on the criterion as its own text words it. AC3. POST to the token endpoint with
  client_id=onex-customer and a deliberately invalid code returns invalid_grant, Code not
  valid - the client was ACCEPTED and the CODE was rejected. POSITIVE CONTROL: the identical
  request with a client that does not exist returns invalid_client, so the two outcomes
  discriminate client-rejection from code-rejection, which is exactly the discriminator the
  criterion names. NOT CLAIMED, stated rather than softened: the scope-correction note
  restates AC3 more strictly, as the documented command producing a usable token end to end.
  That was NOT exercised. The realm sets verifyEmail true, and a correct-password sign-in
  returns HTTP 302 back to execution=VERIFY_EMAIL until the emailed link is clicked; the
  disposable account registered through the public customer path sits on a reserved invalid
  domain with no readable inbox, so the link was unreachable. That is a limitation of the
  lane, not a defect in the guide or the client.
exit_code: 0
pr_number: 331
branch: "main"
