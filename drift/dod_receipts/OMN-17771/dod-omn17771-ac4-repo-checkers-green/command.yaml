---
schema_version: "1.0.0"
ticket_id: "OMN-17771"
evidence_item_id: "dod-omn17771-ac4-repo-checkers-green"
check_type: "command"
check_value: |
  for w in no-private-repo-links beta-layout; do body="$(gh api "repos/OmniNode-ai/knowledge-base-internal/contents/.github/workflows/$w.yml?ref=055da140d8b2e772464ee7a50802aee851d29b4b" --jq '.content' | base64 -d)"; test -n "$body" || exit 1; printf '%s' "$body" | grep -qF 'jobs:' || exit 1; done; echo wired
contract_sha256: "sha256:c1efa61cde42c98c063b79c172407386d894208ad2b25e81a7dc446815a92898"
contract_entry_sha256: "sha256:9dde1c5d1816459b49c4d9f2f43c1ec024dae637fd8992c3a5fb8611ed62f1d8"
status: "PASS"
run_timestamp: '2026-09-10T03:17:47Z'
commit_sha: "055da140d8b2e772464ee7a50802aee851d29b4b"
runner: "claude-local-worktree"
verifier: "omn17771-guides-onex-customer"
probe_command: |
  for w in no-private-repo-links beta-layout; do body="$(gh api "repos/OmniNode-ai/knowledge-base-internal/contents/.github/workflows/$w.yml?ref=055da140d8b2e772464ee7a50802aee851d29b4b" --jq '.content' | base64 -d)"; test -n "$body" || exit 1; printf '%s' "$body" | grep -qF 'jobs:' || exit 1; done; echo wired
probe_stdout: |
  exit=0 stdout=wired
  exit=0 stdout=(empty)
actual_output: |
  PASS. AC4, with the observed half and the re-derived half kept apart. OBSERVED during the
  lane, and this is the reading that says green: every check-run on the merged squash commit
  055da140d8b2e772464ee7a50802aee851d29b4b concluded success - seven runs, zero with a
  conclusion other than success, skipped or neutral - read from the check-runs API with a
  credential that could read it. That reading is recorded HERE rather than re-run as a
  contract check, because this repository's CI token cannot read check-runs on the product
  repository and such a check fails on permissions in a way indistinguishable from a real red;
  a check-run conclusion is mutable besides. RE-DERIVED by the two checks, which is what CI
  can verify: both named checker workflows exist, are non-empty and declare jobs at the merged
  commit, so both were wired and ran on it; and the property the private-repo-links checker
  enforces holds at main, zero github.com links across all four customer guides, taken from
  the files rather than on the checker's word. Both loops abort on an empty body. Locally on
  the branch head 48b91f8adc649977eea124e28649eb3bd1a63067 both checkers exited 0 and a full
  pre-commit run --all-files was clean.
exit_code: 0
pr_number: 331
branch: "main"
