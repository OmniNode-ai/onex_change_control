---
schema_version: 1.0.0
ticket_id: OMN-15192
evidence_item_id: dod-omn-15192-b3-accepted-deviation-census
check_type: command
check_value: STATS="$(gh api --paginate 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A2026-07-29T03%3A01%3A23Z..2026-07-30T08%3A00%3A00Z&per_page=100'
  --jq '.items[]|[.title,.created_at,(.closed_at // "9999-99-99")]|@tsv' | awk -F'\t' '$1 ~ /OCC companion
  for OmniNode-ai\/[a-z_]+#[0-9]+$/ { t=$1; sub(/.* /,x,t); n[t]++; st[t,n[t]]=$2; en[t,n[t]]=$3; tot++
  } END { for (k in n) { if (n[k]<2) continue; dups++; hit=0; for (i=1;i<=n[k];i++) for (j=i+1;j<=n[k];j++)
  if (st[k,i] < en[k,j] && st[k,j] < en[k,i]) hit=1; races+=hit } print tot+0, dups+0, races+0 }')" &&
  TOT="${STATS%% *}" && REST="${STATS#* }" && DUPS="${REST%% *}" && RACES="${REST##* }" && [ "$TOT" -ge
  60 ] && [ "$DUPS" -eq 5 ] && [ "$RACES" -eq 2 ]
contract_entry_sha256: sha256:cd89bd532ba7c4335d738908e201cdf79973de96cbaf5e9cd73b286fc496dc59
status: PASS
run_timestamp: '2026-07-30T08:35:08Z'
commit_sha: cb660871e68991508289a8f4c927c586d2a2b875
runner: fable-resume-0730-b3-honest-repair
verifier: executed-probe
probe_command: STATS="$(gh api --paginate 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A2026-07-29T03%3A01%3A23Z..2026-07-30T08%3A00%3A00Z&per_page=100'
  --jq '.items[]|[.title,.created_at,(.closed_at // "9999-99-99")]|@tsv' | awk -F'\t' '$1 ~ /OCC companion
  for OmniNode-ai\/[a-z_]+#[0-9]+$/ { t=$1; sub(/.* /,x,t); n[t]++; st[t,n[t]]=$2; en[t,n[t]]=$3; tot++
  } END { for (k in n) { if (n[k]<2) continue; dups++; hit=0; for (i=1;i<=n[k];i++) for (j=i+1;j<=n[k];j++)
  if (st[k,i] < en[k,j] && st[k,j] < en[k,i]) hit=1; races+=hit } print tot+0, dups+0, races+0 }')" &&
  TOT="${STATS%% *}" && REST="${STATS#* }" && DUPS="${REST%% *}" && RACES="${REST##* }" && [ "$TOT" -ge
  60 ] && [ "$DUPS" -eq 5 ] && [ "$RACES" -eq 2 ] && COUNT="$(gh api 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%2Fomnibase_infra%232536%22'
  --jq .total_count)" && [ "$COUNT" -eq 2 ] && COUNT="$(gh api 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%2Fomnibase_infra%232537%22'
  --jq .total_count)" && [ "$COUNT" -eq 2 ]
probe_stdout: all 3 check(s) executed twice 2026-07-30T08:35Z, every exit code 0 on both runs
actual_output: 'PASS: 3/3 checks executed twice under bash -o pipefail -c (2026-07-30T08:35:04Z and 08:35:08Z),
  every exit code 0 on both runs, against the live GitHub search surface of onex_change_control. Measured
  counters: tot=67 dups=5 races=2; the two races are omnibase_infra#2536 and #2537. Negative controls
  executed in the opposite direction: the same pipeline with the race assertion inverted to -eq 0 exited
  1; the same pipeline pointed at OmniNode-ai/no_such_repo_xyz exited 1 (fail-closed: HTTP 422, counters
  resolve to 0, corpus control fails); the per-PR leg shape run against omnibase_infra#2543 (a single-companion
  PR) exited 1. This value was REWORKED after the first push: Contract Compliance Check run 30526689447
  job 90819147757 classified the earlier revision INERT [NOT_FALSIFIABLE], so it was never executed in
  CI; the nested function-call paren inside the awk if-condition made evidence_admissibility.commands_in
  lose every command in the value. The reworked value classifies ADMISSIBLE with commands [gh, gh-api,
  awk, [ ]. This item RECORDS the two accepted historical deviations and does NOT assert that acceptance
  bullet 3 is met.'
exit_code: 0
pr_number: 5586
branch: jonah/omn-15192-b3-accepted-deviation-and-forward-race-guard
