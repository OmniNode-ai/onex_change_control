---
schema_version: 1.0.0
ticket_id: OMN-15192
evidence_item_id: dod-omn-15192-b3-forward-zero-companion-race
check_type: command
check_value: STATS="$(gh api --paginate 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T05%3A00%3A00Z&per_page=100'
  --jq '.items[]|[.title,.created_at,.closed_at]|@tsv' | awk -F'\t' -v S=9999-99-99 '$1 ~ /OCC companion
  for OmniNode-ai\/[a-z_]+#[0-9]+$/ { t=$1; sub(/.* /,x,t); e=$3; if (length(e)==0) e=S; n[t]++; st[t,n[t]]=$2;
  en[t,n[t]]=e; tot++ } END { for (k in n) { if (n[k]<2) continue; dups++; hit=0; for (i=1;i<=n[k];i++)
  for (j=i+1;j<=n[k];j++) if (st[k,i] < en[k,j] && st[k,j] < en[k,i]) hit=1; races+=hit } print tot+0,
  dups+0, races+0 }')" && TOT="${STATS%% *}" && REST="${STATS#* }" && DUPS="${REST%% *}" && RACES="${REST##*
  }" && [ "$TOT" -ge 40 ] && [ "$DUPS" -ge 1 ] && [ "$RACES" -eq 0 ]
contract_entry_sha256: sha256:02d9bfded0c545eaf7b6f5b9ee7ddafbc81acaf4019d31eb4544db08c2f96ea1
status: PASS
run_timestamp: '2026-07-30T08:16:05Z'
commit_sha: b6a7a92ad6cf105b9f02d20dc2c13efb7adf966b
runner: fable-resume-0730-b3-honest-repair
verifier: executed-probe
probe_command: STATS="$(gh api --paginate 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T05%3A00%3A00Z&per_page=100'
  --jq '.items[]|[.title,.created_at,.closed_at]|@tsv' | awk -F'\t' -v S=9999-99-99 '$1 ~ /OCC companion
  for OmniNode-ai\/[a-z_]+#[0-9]+$/ { t=$1; sub(/.* /,x,t); e=$3; if (length(e)==0) e=S; n[t]++; st[t,n[t]]=$2;
  en[t,n[t]]=e; tot++ } END { for (k in n) { if (n[k]<2) continue; dups++; hit=0; for (i=1;i<=n[k];i++)
  for (j=i+1;j<=n[k];j++) if (st[k,i] < en[k,j] && st[k,j] < en[k,i]) hit=1; races+=hit } print tot+0,
  dups+0, races+0 }')" && TOT="${STATS%% *}" && REST="${STATS#* }" && DUPS="${REST%% *}" && RACES="${REST##*
  }" && [ "$TOT" -ge 40 ] && [ "$DUPS" -ge 1 ] && [ "$RACES" -eq 0 ] && REPOS="$(gh api --paginate 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T05%3A00%3A00Z&per_page=100'
  --jq '.items[].title' | awk -F'OmniNode-ai/' 'NF>1 { split($2,a,/#/); print a[1] }' | sort -u | wc -l
  | tr -d ' ')" && [ "$REPOS" -ge 5 ]
probe_stdout: all 2 check(s) executed twice 2026-07-30T08:16Z, every exit code 0 on both runs
actual_output: 'PASS: 2/2 checks executed twice under bash -o pipefail -c (2026-07-30T08:16:01Z and 08:16:05Z),
  every exit code 0 on both runs, against the live GitHub search surface of onex_change_control. Measured
  counters at execution: tot=57 dups=3 races=0 across 6 distinct product repos. Falsifiability executed,
  not asserted: widening this exact check window back to created>2026-07-29T03:01:23Z re-admits omnibase_infra#2536/#2537,
  yields tot=67 dups=5 races=2 and exits 1; the duplicate counter demonstrably sees omnimarket#1949 (dups
  goes 3 -> 5 when the window widens), and a cumulative companions-per-PR assertion over the shipped window
  exits 1 because of it; a non-existent repo exits 1 fail-closed.'
exit_code: 0
pr_number: 5586
branch: jonah/omn-15192-b3-accepted-deviation-and-forward-race-guard
