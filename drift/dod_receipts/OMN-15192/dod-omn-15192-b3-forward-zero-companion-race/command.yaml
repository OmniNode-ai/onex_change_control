---
schema_version: 1.0.0
ticket_id: OMN-15192
evidence_item_id: dod-omn-15192-b3-forward-zero-companion-race
check_type: command
check_value: STATS="$(gh api --paginate 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T05%3A00%3A00Z&per_page=100'
  --jq '.items[]|[.title,.created_at,(.closed_at // "9999-99-99")]|@tsv' | awk -F'\t' '$1 ~ /OCC companion
  for OmniNode-ai\/[a-z_]+#[0-9]+$/ { t=$1; sub(/.* /,x,t); n[t]++; st[t,n[t]]=$2; en[t,n[t]]=$3; tot++
  } END { for (k in n) { if (n[k]<2) continue; dups++; hit=0; for (i=1;i<=n[k];i++) for (j=i+1;j<=n[k];j++)
  if (st[k,i] < en[k,j] && st[k,j] < en[k,i]) hit=1; races+=hit } print tot+0, dups+0, races+0 }')" &&
  TOT="${STATS%% *}" && REST="${STATS#* }" && DUPS="${REST%% *}" && RACES="${REST##* }" && [ "$TOT" -ge
  40 ] && [ "$DUPS" -ge 1 ] && [ "$RACES" -eq 0 ]
contract_entry_sha256: sha256:746c06c6eaaf5154657cf826b0bded7ad1c768f5187f7809ae95ab7f47ac79d3
status: PASS
run_timestamp: '2026-07-30T08:35:08Z'
commit_sha: cb660871e68991508289a8f4c927c586d2a2b875
runner: fable-resume-0730-b3-honest-repair
verifier: executed-probe
probe_command: STATS="$(gh api --paginate 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T05%3A00%3A00Z&per_page=100'
  --jq '.items[]|[.title,.created_at,(.closed_at // "9999-99-99")]|@tsv' | awk -F'\t' '$1 ~ /OCC companion
  for OmniNode-ai\/[a-z_]+#[0-9]+$/ { t=$1; sub(/.* /,x,t); n[t]++; st[t,n[t]]=$2; en[t,n[t]]=$3; tot++
  } END { for (k in n) { if (n[k]<2) continue; dups++; hit=0; for (i=1;i<=n[k];i++) for (j=i+1;j<=n[k];j++)
  if (st[k,i] < en[k,j] && st[k,j] < en[k,i]) hit=1; races+=hit } print tot+0, dups+0, races+0 }')" &&
  TOT="${STATS%% *}" && REST="${STATS#* }" && DUPS="${REST%% *}" && RACES="${REST##* }" && [ "$TOT" -ge
  40 ] && [ "$DUPS" -ge 1 ] && [ "$RACES" -eq 0 ] && REPOS="$(gh api --paginate 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T05%3A00%3A00Z&per_page=100'
  --jq '.items[].title' | awk -F'OmniNode-ai/' 'NF>1 { split($2,a,/#/); print a[1] }' | sort -u | wc -l
  | tr -d ' ')" && [ "$REPOS" -ge 5 ]
probe_stdout: all 2 check(s) executed twice 2026-07-30T08:35Z, every exit code 0 on both runs
actual_output: 'PASS: 2/2 checks executed twice under bash -o pipefail -c (2026-07-30T08:35:04Z and 08:35:08Z),
  every exit code 0 on both runs, against the live GitHub search surface of onex_change_control. Measured
  counters: tot=57 dups=3 races=0 across 6 distinct product repos. Falsifiability executed, not asserted:
  widening this exact window back to created>2026-07-29T03:01:23Z re-admits omnibase_infra#2536/#2537,
  yields tot=67 dups=5 races=2 and exits 1; the duplicate counter demonstrably sees omnimarket#1949 (dups
  3 -> 5 on widening) and a cumulative companions-per-PR assertion over the shipped window exits 1 because
  of it; a non-existent repo exits 1 fail-closed. The simultaneously-open path, which live data cannot
  exercise today because every companion in both windows is closed, was proven on a 4-row synthetic fixture
  through the identical jq+awk pipeline: two null-closed_at companions for foo#1 and a sequential pair
  for bar#2 yield tot=4 dups=2 races=1 racelist=[OmniNode-ai/foo#1] -- it fires on the simultaneously-open
  pair and not on the sequential one. Same INERT-in-CI rework as the census item; now ADMISSIBLE.'
exit_code: 0
pr_number: 5586
branch: jonah/omn-15192-b3-accepted-deviation-and-forward-race-guard
