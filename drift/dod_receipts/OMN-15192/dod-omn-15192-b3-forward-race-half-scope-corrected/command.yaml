---
schema_version: 1.0.0
ticket_id: OMN-15192
evidence_item_id: dod-omn-15192-b3-forward-race-half-scope-corrected
check_type: command
check_value: STATS="$(gh api --paginate 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T05%3A00%3A00Z&per_page=100'
  --jq '.items[]|[.title,.created_at,(.closed_at // "9999-99-99")]|@tsv' | awk -F'\t' '$1 ~ /OCC companion
  for OmniNode-ai\/[a-z_]+#[0-9]+$/ { t=$1; sub(/.* /,x,t); n[t]++; st[t,n[t]]=$2; en[t,n[t]]=$3; tot++
  } END { for (k in n) { if (n[k]<2) continue; dups++; hit=0; for (i=1;i<=n[k];i++) for (j=i+1;j<=n[k];j++)
  if (st[k,i] < en[k,j] && st[k,j] < en[k,i]) hit=1; races+=hit } print tot+0, dups+0, races+0 }')" &&
  TOT="${STATS%% *}" && REST="${STATS#* }" && DUPS="${REST%% *}" && RACES="${REST##* }" && [ "$TOT" -ge
  40 ] && [ "$DUPS" -ge 1 ] && [ "$RACES" -eq 0 ]
contract_entry_sha256: sha256:8714c005ef7010dc71ec8af2ced5a2fca1392c24d29685e3497def7da03d367c
status: PASS
run_timestamp: '2026-07-30T09:23:54Z'
commit_sha: 6a22bc7066f238f7e1de11b256813db03bfaffa1
runner: fable-resume-0730-b3-scope-correction
verifier: executed-probe
probe_command: STATS="$(gh api --paginate 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T05%3A00%3A00Z&per_page=100'
  --jq '.items[]|[.title,.created_at,(.closed_at // "9999-99-99")]|@tsv' | awk -F'\t' '$1 ~ /OCC companion
  for OmniNode-ai\/[a-z_]+#[0-9]+$/ { t=$1; sub(/.* /,x,t); n[t]++; st[t,n[t]]=$2; en[t,n[t]]=$3; tot++
  } END { for (k in n) { if (n[k]<2) continue; dups++; hit=0; for (i=1;i<=n[k];i++) for (j=i+1;j<=n[k];j++)
  if (st[k,i] < en[k,j] && st[k,j] < en[k,i]) hit=1; races+=hit } print tot+0, dups+0, races+0 }')" &&
  TOT="${STATS%% *}" && REST="${STATS#* }" && DUPS="${REST%% *}" && RACES="${REST##* }" && [ "$TOT" -ge
  40 ] && [ "$DUPS" -ge 1 ] && [ "$RACES" -eq 0 ] && REPOS="$(gh api --paginate 'search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T05%3A00%3A00Z&per_page=100'
  --jq '.items[].title' | awk -F'OmniNode-ai/' 'NF>1 { split($2,a,/#/); print a[1] }' | sort -u | wc -l
  | tr -d ' ')" && [ "$REPOS" -ge 5 ]
probe_stdout: all 2 check(s) executed twice 2026-07-30T09:23:16Z and 09:23:54Z, every exit code 0 on both
  runs
actual_output: 'PASS: 2/2 checks executed twice under bash -o pipefail -c (2026-07-30T09:23:16Z and 09:23:54Z)
  exactly as parsed from the committed contract, exit 0 on every run, against the live GitHub search surface
  of onex_change_control. Measured counters: tot=57 dups=3 races=0 across 6 distinct product repos. APPEND-ONLY
  REPLACEMENT, not an edit: OCC#5586 merged at 2026-07-30T09:17:08Z (14c5ecde) mid-round, so dod-omn-15192-b3-forward-zero-companion-race
  and its receipt are merged evidence; this item supersedes it via evidence_artifact rather than editing
  it, and both check values are BYTE-IDENTICAL to the superseded item (verified by exact string comparison,
  not by inspection) so no assertion is weakened, narrowed or dropped -- only the description is corrected.
  SCOPE, which is what the correction is for: this item asserts the RACE HALF of acceptance bullet 3 and
  nothing else. The cumulative ''no two companions on the same PR'' clause is RED over this same window
  (dups=3: omnibase_infra#2551, #2562, omnimarket#1949, each SEQUENTIAL head-refresh re-mints, each adjudicated
  an accepted deviation under a standing rule that every future member needs its own adjudication) and
  the ''no competing force-push to auto/*'' clause is RED at far larger volume (force_push_events=50 competing_events=48
  competing_prs=34 actors=[jonahgabriel:48 onexbot-occ-writer[bot]:2] over the 54 machine-minted companions
  in the same window). Both are recorded as R32 BULLET 3 UNMET evidence_requirements with executed commands
  and real output, each executed twice with identical results; the historical half of the force-push clause
  remains the four force-pushes (OCC#5419 x2, #5420 x1, #5425 x1, all jonahgabriel) recorded on dod-omn-15192-b3-one-open-machine-mint.
  A green run on THIS item is therefore not a green bullet 3. Falsifiability, executed and unchanged from
  the superseded item: widening this exact window back to created>2026-07-29T03:01:23Z re-admits omnibase_infra#2536/#2537,
  yields tot=67 dups=5 races=2 and exits 1; the duplicate counter demonstrably sees omnimarket#1949 (dups
  3 -> 5 on widening); a non-existent repo exits 1 fail-closed; and the simultaneously-open path, which
  live data cannot exercise today because every companion in both windows is closed, was proven on a 4-row
  synthetic fixture through the identical jq+awk pipeline yielding tot=4 dups=2 races=1 racelist=[OmniNode-ai/foo#1]
  -- firing on the simultaneously-open pair and not on the sequential one. classify_evidence_item returns
  ADMISSIBLE for both values (not INERT: the awk nested-paren lexer defect filed as OMN-15480 does not
  recur here).'
exit_code: 0
pr_number: 5588
branch: jonah/omn-15192-b3-forward-scope-correction
