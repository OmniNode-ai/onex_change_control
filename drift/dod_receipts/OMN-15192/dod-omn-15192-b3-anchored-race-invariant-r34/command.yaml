---
schema_version: 1.0.0
ticket_id: OMN-15192
evidence_item_id: dod-omn-15192-b3-anchored-race-invariant-r34
check_type: command
check_value: 'gh api --paginate ''search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T03%3A01%3A01Z&per_page=100''
  --jq ''.items[]|[.title,.created_at,(.closed_at // "9999-99-99")]|@tsv'' | awk -F''\t'' ''$1 ~ /OCC
  companion for OmniNode-ai\/[A-Za-z0-9._-]+#[0-9]+$/ { t=$1; sub(/.* /,x,t); n[t]++; st[t,n[t]]=$2; en[t,n[t]]=$3
  } END { for (k in n) { if (n[k]<2) continue; hit=0; for (i=1;i<=n[k];i++) for (j=i+1;j<=n[k];j++) if
  (st[k,i] < en[k,j] && st[k,j] < en[k,i]) hit=1; if (hit) print k } }'' | sort | paste -sd, - | awk ''BEGIN
  { n = 0; v = "" } { n++; v = $0 } END { exit (n == 1 && v == "OmniNode-ai/omnibase_infra#2536,OmniNode-ai/omnibase_infra#2537")
  ? 0 : 1 }'''
contract_entry_sha256: sha256:db0cc45731005008f90e8897301e0c6e3175dc033ed122b5d8b76ba38fd3ff71
status: PASS
run_timestamp: '2026-07-30T22:08:03Z'
commit_sha: 5a454694229b87e91d23fa6e2e8b02f09a0135f9
runner: fable-b3-repair-r34
verifier: executed-omnimarket-evidencecollector-collect
probe_command: 'gh api --paginate ''search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T03%3A01%3A01Z&per_page=100''
  --jq ''.items[]|[.title,.created_at,(.closed_at // "9999-99-99")]|@tsv'' | awk -F''\t'' ''$1 ~ /OCC
  companion for OmniNode-ai\/[A-Za-z0-9._-]+#[0-9]+$/ { t=$1; sub(/.* /,x,t); n[t]++; st[t,n[t]]=$2; en[t,n[t]]=$3
  } END { for (k in n) { if (n[k]<2) continue; hit=0; for (i=1;i<=n[k];i++) for (j=i+1;j<=n[k];j++) if
  (st[k,i] < en[k,j] && st[k,j] < en[k,i]) hit=1; if (hit) print k } }'' | sort | paste -sd, - | awk ''BEGIN
  { n = 0; v = "" } { n++; v = $0 } END { exit (n == 1 && v == "OmniNode-ai/omnibase_infra#2536,OmniNode-ai/omnibase_infra#2537")
  ? 0 : 1 }'' && gh api --paginate ''search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T03%3A01%3A01Z&per_page=100''
  --jq ''.items[].title'' | awk ''$0 ~ /OCC companion for OmniNode-ai\/[A-Za-z0-9._-]+#[0-9]+$/ { c++
  } END { exit (c+0 >= 90) ? 0 : 1 }'' && gh api --paginate ''search/issues?q=repo%3AOmniNode-ai%2Fonex_change_control+is%3Apr+in%3Atitle+%22OCC+companion+for+OmniNode-ai%22+created%3A%3E2026-07-29T03%3A01%3A01Z&per_page=100''
  --jq ''.items[].title'' | awk -F''OmniNode-ai/'' ''NF>1 { split($2,a,/#/); print a[1] }'' | sort -u
  | awk ''END { exit (NR >= 5) ? 0 : 1 }'''
probe_stdout: '3/3 legs executed by EvidenceCollector; status verified on each. Live measurement: 100
  post-anchor companions parsed; race list = exactly OmniNode-ai/omnibase_infra#2536,OmniNode-ai/omnibase_infra#2537;
  distinct product repos = 8.'
actual_output: 'PASS: 3/3 legs verified through the real runner. HOW THIS VERDICT WAS OBTAINED — the R33
  mistake, not repeated: every result below comes from invoking the REAL runner, `EvidenceCollector().collect()`
  from omnimarket/src/omnimarket/nodes/node_dod_verify/services/evidence_collector.py, so the OMN-15382
  pre-execution shape guard AND the `bash -o pipefail -c` execution path are both in the loop. R33 validated
  with `bash -o pipefail -c ''<value>''` — a SURROGATE that runs AFTER the rejecting stage and therefore
  could not observe that all three force-push legs were being rejected before any shell ran. No bash surrogate
  was used anywhere in this receipt. DEFECT REPAIRED (silent-miss channel): the superseded R33 leg filtered
  companion titles with /OCC companion for OmniNode-ai\/[a-z_]+#[0-9]+$/, narrower than the GitHub repo-name
  charset [A-Za-z0-9._-], so every hyphenated, digit-bearing or uppercase product repo was dropped from
  the corpus BEFORE the pairwise interval comparison — a FAIL-OPEN filter on a fail-closed criterion.
  PROVEN: a seeded post-anchor race on OmniNode-ai/omnidash-archived#77 (two companions, overlapping [created_at,
  closed_at] intervals) leaves the R33 narrow form GREEN. LATENT TODAY, DISCLOSED: live 2026-07-30 the
  widened regex matches exactly the same 100 titles as the narrow one, so the pinned race set is byte-identical
  before and after — this closes a channel, it does not change a verdict. FALSIFIABILITY MATRIX, EXECUTED
  (not claimed): producer stage swapped for `cat <fixture>`, every downstream stage byte-identical to
  the committed check_value, each scenario run as a real dod_evidence command check through collect().
  21 of 21 scenarios across the three R34 items landed on their expected status. (a) R33 narrow form +
  seeded hyphenated race -> verified, the defect reproduced; (b) R34 widened form, same seeded input ->
  failed; (c) seed removed -> verified; (d) pinned history erased (#2536 rows dropped) -> failed; (e)
  empty fetch on the >=90 corpus floor -> failed and on the >=5 repo-span control -> failed (fail-CLOSED
  both). ALSO REPAIRED: the R33 race legs passed the OMN-15382 shape guard only ACCIDENTALLY — the same
  shlex desync occurs (the --jq filter''s "9999-99-99" closes the outer quote of RL="$(...)") but token
  1 landed on && and token 2 on [, both of which the guard skips or allowlists, so no real command name
  was ever inspected. The R34 shape makes gh the first token. Leg 3 (repo-span) is carried byte-for-byte:
  its extraction never used the narrow regex.'
exit_code: 0
pr_number: 5673
branch: jonah/omn-15192-b3-r34-guard-parseable-and-repo-charset
