---
schema_version: 1.0.0
ticket_id: OMN-14548
evidence_item_id: dod-forbid-verify-independent-review
check_type: command
check_value: 'v="$(mktemp)" && gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/event_bus/mixin_kafka_dlq.py?ref=1379fbb452204e788db8c48c0612abe63f1f70a0"
  --jq .content | base64 -d > "$v" && grep -F -- ''async def _publish_raw_to_dlq('' "$v" && grep -F --
  ''original_topic: str,'' "$v" && grep -F -- ''raw_msg: object,'' "$v" && grep -F -- ''error: Exception,''
  "$v" && grep -F -- ''correlation_id: UUID,'' "$v" && grep -F -- ''failure_type: str,'' "$v" && grep
  -F -- ''consumer_group: str,'' "$v" && grep -F -- ''dlq_topic: str | None = None,'' "$v"'
status: PASS
run_timestamp: '2026-07-13T06:52:00Z'
commit_sha: 3b3b2aba04ef4f3c2da501131d2eca6039caf7b2
runner: claude-sonnet-5
verifier: forbid-verify-independent-review
probe_command: 'v="$(mktemp)" && gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/event_bus/mixin_kafka_dlq.py?ref=1379fbb452204e788db8c48c0612abe63f1f70a0"
  --jq .content | base64 -d > "$v" && grep -F -- ''async def _publish_raw_to_dlq('' "$v" && grep -F --
  ''original_topic: str,'' "$v" && grep -F -- ''raw_msg: object,'' "$v" && grep -F -- ''error: Exception,''
  "$v" && grep -F -- ''correlation_id: UUID,'' "$v" && grep -F -- ''failure_type: str,'' "$v" && grep
  -F -- ''consumer_group: str,'' "$v" && grep -F -- ''dlq_topic: str | None = None,'' "$v"'
probe_stdout: |-
  async def _publish_raw_to_dlq(
  original_topic: str,
  raw_msg: object,
  error: Exception,
  correlation_id: UUID,
  failure_type: str,
  consumer_group: str,
  dlq_topic: str | None = None,
  exit=0
exit_code: 0
actual_output: 'PASS: independently re-verifies the exact seam-match claim forbid-verify made in cross-agent
  review (verbatim quote in the contract summary/description): the real EventBusKafka._publish_raw_to_dlq
  signature at event_bus/mixin_kafka_dlq.py:560-570 (pinned at omnibase_infra dev SHA 1379fbb452204e788db8c48c0612abe63f1f70a0)
  matches #2288''s call field-for-field. This is not a self-authored claim -- forbid-verify (a separate
  teammate) performed the original independent review and re-ran all 7 (now 13) tests themselves before
  reporting the finding; this receipt re-confirms the static signature half of that finding is reproducible
  against the real repo, not asserted from memory.'
branch: jonah/omn-14548-occ-boundary-dlq
contract_entry_sha256: sha256:a36943b394b4f11ba072693835c40506b7d591f91e8b209abd272fc16f80f9ea
pr_number: 2288
