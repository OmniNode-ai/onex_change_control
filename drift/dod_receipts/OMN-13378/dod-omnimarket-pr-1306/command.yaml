---
schema_version: "1.0.0"
ticket_id: "OMN-13378"
evidence_item_id: "dod-omnimarket-pr-1306"
check_type: "command"
check_value: "uv run pytest tests/integration/e2e_probe/test_leg_probe.py::TestE2EGEN01GenerationLeg::test_gen01_projection_api_read
  tests/integration/e2e_probe/test_leg_probe.py::TestE2EGEN01GenerationLeg::test_gen01_status_report -m
  e2e -q"
status: PASS
run_timestamp: "2026-06-20T15:30:00Z"
commit_sha: "68266ffb282c9ad229ef454c18e55b6b930baa18"
runner: "claude-gen01-probe-worker"
verifier: "claude-receipt-review"
contract_sha256: "sha256:9083fa53c096b6ddf0c5361c6a9cbdf78365eeff7e5e6f3a55941fc3de2d242f"
branch: "jonah/omn-13378-gen01-probe-topic-mismatch"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-13378/omnimarket"
pr_number: 1306
probe_command: "OMN_ALLOW_LIVE_E2E_PROBE=true ONEX_E2E_LANE=dev uv run pytest tests/integration/e2e_probe/test_leg_probe.py::TestE2EGEN01GenerationLeg::test_gen01_projection_api_read
  -m e2e -v"
probe_stdout: |
  tests/integration/e2e_probe/test_leg_probe.py::TestE2EGEN01GenerationLeg::test_gen01_projection_api_read PASSED [100%]
  1 passed in 0.27s
actual_output: |
  GEN-01 fix repoints _PROJECTION_API_TOPIC_GENERATION from the unexposed
  onex.snapshot.projection.generation.events.v1 (declared by no producer or
  reducer; HTTP 404) to the exposed terminal-event topic
  onex.evt.omnimarket.node-generation-completed.v1 that node_projection_delegation
  materializes into the generation_events table and the projection API serves at
  GET /projection/<event-topic>. _build_generation_payload is stripped to
  declared-fields-only (correlation_id + task_description); the undeclared probe
  fields (node_name_hint, requested_by, timestamp, probe, ticket) are removed
  because ModelNodeGenerationRequest is frozen with extra=forbid and rejects the
  command on strict validation before any terminal event or projection row.
  Deterministic model proof: the new payload validates (max_attempts default=10);
  the old payload is rejected by extra=forbid citing all five forbidden fields.
  test_gen01_projection_api_read passes against the live dev projection.
  Regression: 230 passed / 1 skipped for generation-consumer model/request tests
  (-m "not kafka and not e2e"). ruff format + ruff check clean; mypy --strict on
  src/omnimarket clean (Success, 2393 files); pre-commit --files passes (0
  failures). The removed datetime/UTC import leaves the file import-clean (full
  probe file collects 9 skipped with the live flag off).
exit_code: 0
duration_ms: 270
