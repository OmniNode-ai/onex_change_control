---
schema_version: "1.0.0"
ticket_id: "OMN-17352"
evidence_item_id: "write-path-hashes-and-never-inserts-plaintext"
check_type: "command"
check_value: |-
  gh api 'repos/OmniNode-ai/omninode_infra/contents/docker/onex-api/auth_api_keys.py?ref=618d99a6cf423b2c7e0994564dcc74bc4b7e5b60' --jq '.content' | base64 -d | grep -qF 'hashlib.sha256(raw_key.encode("utf-8")).hexdigest()'
contract_sha256: "sha256:9dfd88c8b90d613fb7a8b0c40a383c47e76e87cee62b6a2d7b2b14374b9cd9d3"
contract_entry_sha256: "sha256:edf5047ffce0eb5257833252e8bec47002db28eac309d9cd22b3ddd7b70adb97"
status: PASS
run_timestamp: "2026-09-17T15:02:00Z"
commit_sha: "618d99a6cf423b2c7e0994564dcc74bc4b7e5b60"
runner: "m4-stalled-carriers-triage-1415"
verifier: "github-api-readback"
probe_command: "both command checks of this evidence item, executed verbatim before commit, plus a negative
  control using the same grep shape against a plaintext-column insert that is not in the file"
probe_stdout: |-
  hostedA PASS   _hash_key is hashlib.sha256(raw_key.encode("utf-8")).hexdigest()   (line 174)
  hostedB PASS   INSERT INTO tenant_api_keys (tenant_id, name, key_hash)            (line 333)
  control correctly FAILED   same shape, '(tenant_id, name, key_hash, plaintext_key)', absent from the file
actual_output: |-
  PASS. Read at omninode_infra 618d99a6cf423b2c7e0994564dcc74bc4b7e5b60,
  docker/onex-api/auth_api_keys.py. This is the hosted half and it answers the residual both store
  reads disclosed but could not settle from the stored bytes: the digest is specifically SHA-256.
  The file contains exactly ONE INSERT INTO tenant_api_keys and it names three columns -- tenant_id,
  name, key_hash. plaintext_key exists only as a local (line 318) returned to the caller in the
  response model (line 386) and is never a bound parameter of an insert. Both checks are fail-closed:
  a failed content fetch yields an empty body and grep -qF exits non-zero. Offered as the hosted,
  falsifiable, outside-its-own-diff check the substance floor requires, NOT as the ticket's proof
  surface, which the ticket is explicit is a store read rather than a code reading.
exit_code: 0
pr_number: 10022
branch: "jonah/omn-17352-occ-contract"
