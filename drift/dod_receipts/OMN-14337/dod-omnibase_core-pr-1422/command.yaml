---
schema_version: "1.0.0"
ticket_id: "OMN-14337"
evidence_item_id: "dod-omnibase_core-pr-1422"
check_type: "command"
check_value: "cd omnibase_core && env -u PYTHONPATH uv run lint-imports"
status: PASS
run_timestamp: "2026-07-10T21:57:00Z"
commit_sha: de147202880d4cae20979f99fe229c693702a117
runner: "omn-14337-independent-verifier"
verifier: "occ-evidence-lane"
probe_command: >-
  cd /Users/jonah/Code/omni_home/omni_worktrees/OMN-14337/omnibase_core && env -u PYTHONPATH uv run lint-imports;
  # negative control (throwaway types file imports ModelSemVer) -> BROKEN, then remove -> restored; #
  ProtocolSemVer runtime probe: ModelSemVer(1,2,3) exposes major/minor/patch/prerelease/build/to_string/is_prerelease;
  # widening: grep consumers of TypedDictActionValidationStatistics / TypedDictAnalyticsSummaryData; #
  mypy: git diff --name-only origin/dev...HEAD -- 'src/**/*.py' | xargs env -u PYTHONPATH uv run mypy
  --strict; # tests: env -u PYTHONPATH uv run pytest tests/unit/core/test_type_constraints.py tests/unit/test_no_circular_imports.py
  tests/unit/test_smoke_high_dependency_exports.py -q
probe_stdout: |
  Analyzed 4074 files, 10323 dependencies.
  Foundation primitives must not import higher layers (direct) KEPT
  Models must not import behavioral/orchestration layers (direct) KEPT
  Services and validation must not import nodes (direct) KEPT
  Protocols (seam) must not import domain/behavioral layers (direct) KEPT
  Contracts: 4 kept, 0 broken.
  negative-control (throwaway types imports ModelSemVer): Foundation primitives must not import higher layers BROKEN; 3 kept, 1 broken (4075 files, 10324 deps); restored 4 kept/0 broken after removal
  ProtocolSemVer probe: major=1 minor=2 patch=3 prerelease=None build=None to_string()='1.2.3' is_prerelease()=False; accepts ModelSemVer as ProtocolSemVer
  widening consumers: model_node_action_validator (recent_validations producer-write), model_metadata_analytics_summary (errors/performance producer-write); no structured attr read; mypy --strict clean on both
  mypy --strict (19 changed files): Success: no issues found in 19 source files
  targeted tests: 153 passed in 8.75s
  ruff check src/ tests/: All checks passed!
actual_output: >-
  PASS: omnibase_core PR #1422 retires 20/24 types->models import-layering back-edges (ProtocolSemVer
  repoint of 9 ModelSemVer edges; object/Sequence[object] widening of 8 immovable domain-model edges;
  dead ModelBase re-export removal), narrowing the 2 types->models wildcard ignores to 4 real residual
  pins tracked in follow-up OMN-14339. lint-imports 4 kept/0 broken with unmatched_ignore_imports_alerting=ERROR
  proves both the 20 severed edges and the 4 residuals' reality. Negative control confirmed the pins do
  not wildcard-mask new edges. ProtocolSemVer is structurally satisfied by ModelSemVer with zero behavior
  change. The 3 widened fields are producer-only pass-through (no structured consumer read; mypy --strict
  clean on both consumers). Removed ModelBase re-exports have zero facade importers. 153 targeted tests
  pass; mypy --strict clean on 19 changed files; ruff clean.
exit_code: 0
pr_number: 1422
branch: "jonah/omn-14337-retire-types-models-back-edge"
contract_sha256: "sha256:9dae2c58e2dc68c1fecb2122c4893fc340649ee30835efb0e311c3a841ffbd47"
