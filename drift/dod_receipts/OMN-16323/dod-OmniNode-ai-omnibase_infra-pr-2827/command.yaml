---
schema_version: "1.0.0"
ticket_id: "OMN-16323"
evidence_item_id: "dod-OmniNode-ai-omnibase_infra-pr-2827"
check_type: "command"
check_value: |-
  body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/release.yml?ref=fdc8758fefc620dcc9b9c85a1a9d2ffa7d5243ac' --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF -- 'TAG="$RELEASE_TAG"' && printf '%s' "$body" | grep -qF -- 'echo "tag=$RELEASE_TAG" >> $GITHUB_OUTPUT' && printf '%s' "$body" | grep -qF -- 'release tag contains unsupported characters' && ! printf '%s' "$body" | grep -qF -- 'TAG="${{ inputs.tag }}"' && ! printf '%s' "$body" | grep -qF -- 'echo "tag=${{ inputs.tag }}" >> $GITHUB_OUTPUT' && ! printf '%s' "$body" | grep -qF -- 'TAG="${{ steps.tag.outputs.tag }}"'
contract_sha256: "sha256:d98988630fab1c5f55992d332c64815d64f0c3ceedaeb758b6ea57a967d30922"
contract_entry_sha256: "sha256:38b9255119ab5fc4829b7f0d6d055d22855c1e7622ea6f21dfa0485d5bc6ecf6"
status: PASS
run_timestamp: "2026-08-21T06:59:31Z"
commit_sha: "fdc8758fefc620dcc9b9c85a1a9d2ffa7d5243ac"
runner: "infra-red-clusters"
verifier: "gh-cli-github-api"
probe_command: |-
  body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/.github/workflows/release.yml?ref=fdc8758fefc620dcc9b9c85a1a9d2ffa7d5243ac' --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF -- 'TAG="$RELEASE_TAG"' && printf '%s' "$body" | grep -qF -- 'echo "tag=$RELEASE_TAG" >> $GITHUB_OUTPUT' && printf '%s' "$body" | grep -qF -- 'release tag contains unsupported characters' && ! printf '%s' "$body" | grep -qF -- 'TAG="${{ inputs.tag }}"' && ! printf '%s' "$body" | grep -qF -- 'echo "tag=${{ inputs.tag }}" >> $GITHUB_OUTPUT' && ! printf '%s' "$body" | grep -qF -- 'TAG="${{ steps.tag.outputs.tag }}"'
probe_stdout: |
  exit=0
actual_output: >-
  PASS: sink-closure probe GREEN at omnibase_infra fdc8758fefc620dcc9b9c85a1a9d2ffa7d5243ac -- all three
  safe shell reads present (TAG="$RELEASE_TAG" twice, echo "tag=$RELEASE_TAG" >> $GITHUB_OUTPUT) plus
  the charset guard, AND all three pre-fix interpolated sinks absent. RED at merge-base 063cf6ecff69b21d3f2896e404b7d5f2644e2ed8
  (exit 1), where TAG="${ inputs.tag }" is still interpolated directly into the run: block. Probe shape
  supplied by lane omn16289-tail on the PR; it asserts the security property rather than the presence
  of a single guard, so substituting the weaker env:-only half-fix would still fail it.
exit_code: 0
pr_number: 2827
branch: "jonah/omn-16323-release-tag-injection"
