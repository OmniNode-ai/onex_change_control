---
schema_version: "1.0.0"
ticket_id: "OMN-14985"
evidence_item_id: "dod-omnibase-core-pr-1487-body-evidence"
check_type: "command"
check_value: "gh pr view 1487 --repo OmniNode-ai/omnibase_core --json body --jq .body | grep -F '74 passed,
  0 failed' && gh pr view 1487 --repo OmniNode-ai/omnibase_core --json body --jq .body | grep -F 'ruff
  format' && gh pr view 1487 --repo OmniNode-ai/omnibase_core --json body --jq .body | grep -F 'pre-commit
  run --files <3 files>' && gh pr view 1487 --repo OmniNode-ai/omnibase_core --json body --jq .body |
  grep -F 'Governed pre-push selector' && gh pr view 1487 --repo OmniNode-ai/omnibase_core --json body
  --jq .body | grep -F 'Full single-process `tests/` suite proof bar'"
contract_entry_sha256: "sha256:7cdd84d72a5bbdb096fcc4412ae2b14797732271d95bcc2bccd76bd53831103c"
status: PASS
run_timestamp: "2026-07-23T23:08:40Z"
commit_sha: "eabc68ea1a01628c7440ceb44844eadd4b3c2c8a"
runner: "codex-merge-controller"
verifier: "occ-evidence-source-autobind"
probe_command: "gh pr view 1487 --repo OmniNode-ai/omnibase_core --json body --jq .body | grep -F '74
  passed, 0 failed' && gh pr view 1487 --repo OmniNode-ai/omnibase_core --json body --jq .body | grep
  -F 'ruff format' && gh pr view 1487 --repo OmniNode-ai/omnibase_core --json body --jq .body | grep -F
  'pre-commit run --files <3 files>' && gh pr view 1487 --repo OmniNode-ai/omnibase_core --json body --jq
  .body | grep -F 'Governed pre-push selector' && gh pr view 1487 --repo OmniNode-ai/omnibase_core --json
  body --jq .body | grep -F 'Full single-process `tests/` suite proof bar'"
probe_stdout: |
  - Targeted single-process run (the 3 changed test files, 74 tests): **74 passed, 0 failed** (`uv run pytest -p no:xdist tests/unit/models/contracts/test_model_fast_imports.py tests/unit/models/contracts/test_model_lazy_imports.py tests/unit/test_omn_14944_sys_modules_purge_regression.py`), including the new guard test.
  - `ruff format` + `ruff check --fix`: clean on all 3 changed files.
  - `pre-commit run --files <3 files>` (full default-stage battery, ~90 hooks): **all Passed / Skipped (no files to check)**, zero failures.
  - Governed pre-push selector (`scripts/ci/detect_test_paths.py`) dry-run against this diff selects `tests/unit/models/` + the regression test file (not a full-suite escalation) -- consistent with a test-only diff.
  - **Full single-process `tests/` suite proof bar (the load-bearing OMN-14944-unblock check) could not be completed in-session**: the shared .200 build host had 5+ concurrent unrelated pytest runs from other live sessions consuming all CPU during this work (confirmed via `ps aux`), driving single-process suite progress to ~2-3% per 15-20 minutes of wall time. Both a full-suite run and the governed-selector-scoped subset run were left running in the background, logged to `/tmp/omn14985_full_suite.log` and `/tmp/omn14985_selector_scope.log` on stickybeatz-studio, for follow-up readback. No red tests were observed in either log before this PR was opened.
actual_output: "PASS: PR #1487 body records targeted test, lint, pre-commit, governed-selector, and full-suite
  caveat evidence."
exit_code: 0
pr_number: 1487
branch: "jonah/omn-14985-close-sys-modules-residual"
