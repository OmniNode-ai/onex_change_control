---
schema_version: "1.0.0"
ticket_id: "OMN-14358"
evidence_item_id: "dod-omnibase_core-pr-1427"
check_type: "command"
check_value: "cd omnibase_core && env -u PYTHONPATH uv run pytest tests/unit/canary/test_effect_golden.py
  -q"
status: PASS
run_timestamp: "2026-07-11T02:37:00Z"
commit_sha: 5453be738251124f0ea1638dab83ed984ee813ea
runner: "omn-14358-independent-verifier"
verifier: "occ-evidence-lane"
probe_command: >-
  cd /Users/jonah/Code/omni_home/omni_worktrees/OMN-14358/omnibase_core && env -u PYTHONPATH uv run pytest
  tests/unit/canary/test_effect_golden.py -v; grep the module + test for real Mock usage; run an independent
  discrimination probe (suppress topic / suppress file / conjunctive partial) as a throwaway pytest, then
  remove it; env -u PYTHONPATH uv run ruff check; env -u PYTHONPATH uv run mypy --strict scripts/ci/effect_golden.py
probe_stdout: |
  7 passed in 3.61s
  RecordingEventSink: plain class appending to captured list; only MagicMock refs are docstrings (no Mock/mocker instantiation)
  committed test_readback_CATCHES_a_suppressed_effect: node with event_bus=None -> execute() returns Path (no throw) -> readback ok=False, topic in missing_effects
  independent probe (4/4): baseline all-present ok=True; suppress-topic ok=False+topic missing; suppress-file(empty) ok=False+files_ok=False; conjunctive 2-topic-1-pub ok=False, only missing topic flagged
  contract-driven parse_publish_topics via Pydantic; volatile mask run_id/timestamp/correlation_id; compare catches payload change
  ruff check: All checks passed!
  mypy --strict effect_golden.py: 1 error no-any-return at line 142 (non-enforced; scripts/ci/ not in mypy CI scope)
  throwaway probe removed; worktree clean
actual_output: >-
  PASS on all stated criteria for the effect_golden readback oracle. RecordingEventSink is a real observable
  sink (not a MagicMock); readback_effects is conjunctive over declared topics AND declared file globs
  and returns a typed ModelEffectReadback with missing_effects; discrimination is proven both by the committed
  test that drives the real EFFECT node with event_bus=None (execute returns without throwing yet readback
  fails on the missing topic) and by an independent probe covering topic-suppression, file-suppression,
  and conjunctive-partial cases; topics are contract-driven; the volatile mask excludes run_id/timestamp/correlation_id;
  and compare catches a real payload change. 7 tests pass, non-tautological; ruff clean. One LOW, non-blocking
  finding: mypy --strict reports a no-any-return at effect_golden.py:142 (the same _apply_mask nit that
  was annotated away in compute_golden but copied unfixed here); no CI gate type-checks scripts/ci/, so
  it does not block CI and a one-line annotation resolves it.
exit_code: 0
pr_number: 1427
branch: "jonah/omn-14358-effect-golden"
contract_sha256: "sha256:9b4aae3a3abcbb5636af3ac73b56925c9499c85207727190bf357f05e05b088f"
