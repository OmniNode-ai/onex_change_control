---
schema_version: "1.0.0"
ticket_id: "OMN-13841"
evidence_item_id: "dod-deploy"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13841/dod-deploy/command.yaml"
status: PASS
run_timestamp: "2026-07-02T12:25:00Z"
commit_sha: "c503808b"
runner: "local-occ-evidence"
verifier: "jonah"
probe_command: "n/a — deploy-not-required determination for a pure FSM handler return-type fix"
probe_stdout: |
  No live prod deploy required. build_loop / design_to_plan are pure FSM
  handlers (no I/O). The skills resolve via editable-venv entry-point node
  discovery and are exercised in-process by CI + the regression test.
actual_output: >-
  PASS: no live deploy required. The change corrects HandlerBuildLoop.handle() and HandlerDesignToPlan.handle()
  to return typed BaseModel results instead of a tuple; the fix is proven by the full omnimarket suite
  and the real-adapter ONEX_CORE_095 regression test, not by a runtime redeploy. No raw prod mutation
  performed.
exit_code: 0
branch: "jonah/omn-13841-onex-core-095-tuple-return"
pr_number: 1568
