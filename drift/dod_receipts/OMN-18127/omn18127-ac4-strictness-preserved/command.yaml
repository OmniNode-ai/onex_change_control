---
actual_output: "PASS. Executed at the head sha bf1290be41c16dcd2da2e7a193dc1facd6fc4433 (OmniNode-ai/omnimarket#2442).\n\nCommand
  checks on this evidence item, each executed and each exit 0:\n\n  1. the checker declares and enforces
  the models that must stay strict\n     -> 3   (exit 0)\n  2. the workflow declares no skip, force or
  advisory switch\n     -> 1   (exit 0)\n\nAC4. Both command checks executed against the head sha and
  both exited 0.\n\nThe criterion is that the consumer's extra=forbid strictness is untouched. Nothing
  in this change\nmodifies it; the publisher-side model keeps extra=forbid and its own suite still refuses
  a\ngenuinely unknown lane key, which I verified independently by running a red-first test class\nagainst
  omnibase_infra#3387 head 2f5f3d22d: 11 of 11 pass, including two positive controls where\nan unknown
  lane key and an unknown nested key are both still refused.\n\nThis item goes further, and that is the
  part worth reading. The gate is a RE-RUN of the\nconsumer's strictness, so if extra=forbid were ever
  relaxed to extra=allow every overlay would\nvalidate here and this check would report green forever
  -- green most confidently at the exact\nmoment the contract stopped being enforced. That is a worse
  failure than the one the gate exists\nto catch, because it is silent. So the checker asserts the strictness
  directly against the\nFETCHED SOURCE using ast, which also means a runtime mutation cannot satisfy it,
  and refuses a\nrelaxed consumer while naming the models that lost it. TestRefusesASilentlyGreenConsumer
  pins\nboth halves, including a case proving the relaxed consumer WOULD otherwise have validated the\noverlay
  -- so the refusal is the assertion doing work rather than an incidental red.\n\nThe second grep pins
  the absence of an escape hatch: a gate with an override is a suggestion.\n\nNEGATIVE CONTROL, run at
  the parent commit 103c81baf1e1a70a8d1fca0ec5ff9b7df94f7b73: all three\nfiles this item greps are ABSENT
  there, so every pattern above returns nothing at the parent and\nmatches only at the head. The counts
  are therefore discriminating rather than patterns that would\nmatch anywhere, which is what makes a
  non-zero count evidence instead of a successful fetch.\n"
branch: jonah/omn-18127-overlay-parity-gate
check_type: command
check_value: gh api "repos/OmniNode-ai/omnimarket/contents/scripts/ci/check_ci_bus_overlay_parity.py?ref=bf1290be41c16dcd2da2e7a193dc1facd6fc4433"
  --jq '.content' | base64 -d | grep -c '_REQUIRED_STRICT_MODELS'
commit_sha: bf1290be41c16dcd2da2e7a193dc1facd6fc4433
contract_entry_sha256: sha256:8c4607e272fa4efab8d51e20283ba2f874688c9b7c272c5e6e5efcc50d4481f1
evidence_item_id: omn18127-ac4-strictness-preserved
exit_code: 0
pr_number: 2442
probe_command: gh api "repos/OmniNode-ai/omnimarket/contents/scripts/ci/check_ci_bus_overlay_parity.py?ref=bf1290be41c16dcd2da2e7a193dc1facd6fc4433"
  --jq '.content' | base64 -d | grep -c '_REQUIRED_STRICT_MODELS'
probe_stdout: |
  3
run_timestamp: '2026-09-10T07:01:08Z'
runner: claude-local-worktree
schema_version: 1.0.0
status: PASS
ticket_id: OMN-18127
verifier: trigger-overlay-ledger-readback-key
