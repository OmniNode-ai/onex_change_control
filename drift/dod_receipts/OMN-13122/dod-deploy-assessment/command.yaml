---
schema_version: "1.0.0"
ticket_id: "OMN-13122"
evidence_item_id: "dod-deploy-assessment"
check_type: "command"
check_value: "grep -q 'No live deploy' drift/dod_receipts/OMN-13122/dod-deploy-assessment/command.yaml"
status: PASS
run_timestamp: "2026-06-13T22:32:27Z"
commit_sha: "eac8a3ee01148e697da1526cceed9475736fea77"
runner: "worktree-agent-omn-13122"
verifier: "receipt-gate-local"
probe_command: "git diff --name-only origin/dev...HEAD"
probe_stdout: |
  .onex_state/dep_health_baseline.json
  docker-compose.projection.yml
  pyproject.toml
  src/omnimarket/nodes/node_projection_routing_decision/__init__.py
  src/omnimarket/nodes/node_projection_routing_decision/contract.yaml
  src/omnimarket/nodes/node_projection_routing_decision/handlers/__init__.py
  src/omnimarket/nodes/node_projection_routing_decision/handlers/handler_projection_routing_decision.py
  src/omnimarket/nodes/node_projection_routing_decision/handlers/handler_routing_decision.py
  src/omnimarket/nodes/node_projection_routing_decision/metadata.yaml
  src/omnimarket/nodes/node_projection_routing_decision/tests/__init__.py
  tests/test_golden_chain_projection_routing_decision.py
  tests/test_market_node_runtime_dogfood.py
actual_output: >-
  PASS: deploy_pending = true. No live deploy, restart, topic mutation, or .201 mutation was performed.
  The dev lane still runs the prior build that does not include node_projection_routing_decision; this
  node lives only on the PR branch and in CI. The RoutingDecisionProjectionRunner consumer (docker-compose.projection.yml
  service projection-routing-decision) must be deployed for the registration golden chain (onex.evt.omniclaude.routing-decision.v1
  -> agent_routing_decisions) to be owned by a canonical node at runtime; that runtime ownership is not
  established by this PR. agent_routing_decisions DDL is owned by omnibase_infra migration 021 (021_create_agent_routing_decisions_table.sql)
  — this node is a non-owner WRITER and ships no CREATE TABLE migration (metadata.yaml declares duplicate_migration_policy:
  forbid_cross_repo_create_table). This PR ships code + wiring only. The consumer lands on dev via a coordinated
  dev redeploy folded with OMN-13121 / OMN-13124, and routing-decision row materialization in agent_routing_decisions
  becomes observable on the dev lane only after that batched rebuild. HONESTY: this receipt explicitly
  does not claim the node is deployed and does not claim agent_routing_decisions rows are materializing
  in the live table. It asserts only that the code, contract wiring, and tests are correct locally; live
  confirmation remains a separate step under the coordinated redeploy.
exit_code: 0
contract_sha256: "sha256:f1ef3458c220715de82566da46c9cc5d6e64e6d2ffe930f0f07c870018eb1d9d"
pr_number: 1211
branch: "jonah/omn-13122-registration-projection-node"
