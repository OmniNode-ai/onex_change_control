---
schema_version: "1.0.0"
ticket_id: "OMN-14376"
evidence_item_id: "dod-verify-2261-product-observing-repair"
check_type: "command"
check_value: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/cli/cli_delegate.py?ref=c22182edad34454c843e35a7c14175081cc8e8c4"
  --jq .content | base64 -d | grep -c 'resolve_default_bus')" -ge 1
status: PASS
run_timestamp: "2026-07-12T07:10:00Z"
commit_sha: "c22182edad34454c843e35a7c14175081cc8e8c4"
runner: "occ-queue-runner"
verifier: "occ-queue-verifier"
pr_number: 2261
probe_command: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/cli/cli_delegate.py?ref=c22182edad34454c843e35a7c14175081cc8e8c4"
  --jq .content | base64 -d | grep -c 'resolve_default_bus')" -ge 1   # then re-run verbatim with ref=dev
  and ref=<bogus SHA> as RED controls
probe_stdout: |
  # bound check_value, run verbatim:
  #   ref=c22182ed (omnibase_infra#2261 head) -> exit 0   GREEN ('resolve_default_bus' present)
  #   ref=dev            (omnibase_infra dev)            -> exit 1   RED   ('resolve_default_bus' absent)
  #   ref=000...0        (bogus SHA)                     -> exit 1   RED
exit_code: 0
actual_output: >-
  PASS: OMN-14436 REPAIR. Every pre-existing entry in OMN-14376's contract is INERT -- it greps this repo's
  own receipt store. That is a PLATFORM WIRING DEFECT, not an authoring failure: the DoD runner executed
  check_values with --workspace pointed at the onex_change_control clone rather than the product checkout,
  so the receipt was the only file on disk and NO other kind of check could have run. The author wrote
  exactly what the tooling accepted. We fix the debt we created; we do not invoice it to whoever is holding
  it. This entry adds the product-observing evidence that was impossible to write before. It reads omnibase_infra#2261's
  source at its exact head SHA via the GitHub contents API and asserts 'resolve_default_bus'. OMN-14376
  IS the flip from a hardcoded DEFAULT_BUS=inmemory to the system-configured bus. resolve_default_bus
  is the mechanism that performs the resolution, so asserting it asserts the fix rather than restating
  the PR title. RED DIRECTION PROVEN by execution: the symbol occurs at #2261's head and ZERO times at
  omnibase_infra dev, so the identical command exits 1 at dev and at a bogus SHA. It exits 0 only where
  the code exists. APPEND-ONLY: no existing dod_evidence entry and no merged receipt was modified.
branch: "jonah/omn-14436-repair-inert-contracts"
contract_sha256: "sha256:b287051bed5a8265d19ab9143d2b29c0638a0c0b230a93e0bb75d94a7f69e585"
contract_entry_sha256: "sha256:509fce2fd9b7a2598f3a10edda007bb33200d9b36b5f26b0490ff0288396e8b1"
