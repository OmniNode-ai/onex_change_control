---
schema_version: 1.0.0
ticket_id: OMN-13558
evidence_item_id: dod-otel
check_type: command
check_value: "grep -q 'PASS' drift/dod_receipts/OMN-13558/dod-otel/command.yaml"
status: PASS
run_timestamp: '2026-06-24T00:00:00Z'
commit_sha: 4fa1576e69ab613ce8c57f6e4cac9d4fd59aea74
runner: claude-code
verifier: claude-code-review
contract_sha256: "sha256:5896d3a9af2778b09c5dd647b5fef92c7468b0479990dadb49a1513f0af1f45a"
probe_command: 'uv run pytest tests/unit/runtime/test_tracing_contract_descriptor.py tests/unit/runtime/test_tracing.py
  -q'
probe_stdout: |
  tests/unit/runtime/test_tracing_contract_descriptor.py ......... (resolution-equivalence: dev/stability/prod lane values + whitespace-strip parity + empty-when-unset opt-in + malformed-contract raises + end-to-end configure_tracing()==False when unset)
  tests/unit/runtime/test_tracing.py ....... (existing tracing suite unaffected; endpoint now overlay-resolved)
  16 passed in 0.57s
actual_output: >-
  PASS: the migrated overlay seam resolves descriptor.otel_exporter_otlp_endpoint through expand_contract_env_refs
  identically to the pre-migration os.environ.get("OTEL_EXPORTER_OTLP_ENDPOINT", "") read for every representative
  dev/stability-test/prod lane OTLP endpoint, including whitespace-strip parity. Unlike the fail-closed
  QDRANT/GRAPH endpoints, OTEL is opt-in (OMN-3811): an unset endpoint resolves to "" so configure_tracing()
  takes its existing "empty => disabled" branch (proven end-to-end by test_configure_tracing_disabled_when_endpoint_unset);
  the descriptor raises ValueError only on a structurally malformed contract (missing descriptor mapping
  / non-string field). OTEL_SERVICE_NAME / OTEL_TRACES_EXPORTER remain direct reads (operator-local span-naming
  / toggle knobs, not endpoints — Wave-2 config). 16 tests passed (9 new descriptor + 7 existing tracing);
  ruff + mypy --strict clean on the changed files; the check-env-reads.sh gate exits 0 (no new os.environ
  reads added — this migration REMOVES the endpoint env read).
exit_code: 0
branch: jonah/omn-13558-w1d-otel-overlay
working_dir: /Users/jonah/omni_worktrees/OMN-13558/omnibase_infra
pr_number: 2104
