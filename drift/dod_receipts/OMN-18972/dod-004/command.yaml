---
schema_version: 1.0.0
ticket_id: OMN-18972
evidence_item_id: dod-004
check_type: command
check_value: |-
  uv run python -c "import re; from datetime import UTC, datetime; from scripts.ci.ci_summary_gate import EXTERNAL_SWEEP_EXCLUSIONS as E, validate_sweep_exclusions as v, active_sweep_exclusions as a; assert E, 'registry is empty'; assert v(E) == [], v(E); assert a(E, now=datetime.now(UTC))[1] == (), a(E, now=datetime.now(UTC))[1]; assert all(re.fullmatch(r'OMN-\d+', x.ticket) for x in E.values()), sorted(E); print('OK', len(E), 'entries')"
status: PASS
run_timestamp: '2026-09-21T11:55:47Z'
commit_sha: dd447cd1d77baf15a741e0b01aa9c8786727ea1c
branch: jonah/omn-18972-umbrella-external-default-deny
runner: omn18943-umbrella-default-deny-0320
verifier: codex-delegation-plan-review-1835
probe_command: |-
  git -C $OMNI_HOME/onex_change_control worktree add --detach $OMNI_HOME/omni_worktrees/OMN-18972-verify/occ-pr10642-v2 dd447cd1d77baf15a741e0b01aa9c8786727ea1c && cd $OMNI_HOME/omni_worktrees/OMN-18972-verify/occ-pr10642-v2 && uv run python -c "import re; from datetime import UTC, datetime; from scripts.ci.ci_summary_gate import EXTERNAL_SWEEP_EXCLUSIONS as E, validate_sweep_exclusions as v, active_sweep_exclusions as a; assert E, 'registry is empty'; assert v(E) == [], v(E); assert a(E, now=datetime.now(UTC))[1] == (), a(E, now=datetime.now(UTC))[1]; assert all(re.fullmatch(r'OMN-\d+', x.ticket) for x in E.values()), sorted(E); print('OK', len(E), 'entries')"
probe_stdout: |
  OK 6 entries
actual_output: |-
  PASS, exit 0, output "OK 6 entries". Verified by a second actor in a detached worktree at the committed head, not the runner's checkout. The check_value was extracted from the contract at this head and run verbatim rather than retyped.
  Positive control re-derived independently: emptying the registry in the disposable worktree makes the probe refuse at exit 1 with "AssertionError: registry is empty", so the non-emptiness clause is load-bearing and the item cannot pass vacuously, which was the defect the runner's own negative control caught in the earlier name-set form. The real tree was never touched and git status was clean after restore.
  The six entries were read back and each carries a well-formed ticket: 'tests+coverage (shadow)', 'PEP 604 Type Union Check (UP007)', 'occ-autobind', 'occ-companion-effect' at OMN-18972, plus the two App-written rows this change adds, 'occ-autobind / outcome' and 'occ-companion-effect / mint status', at OMN-18939.
  SCOPE: this item asserts the registry is non-empty, validates, is unexpired and references real tickets. It deliberately no longer asserts a specific name set, so it does not detect a wrongly ADDED exclusion, only an empty, malformed, expired or badly-referenced one. A future entry that should not be there would pass this item. That gap is being closed mechanically under OMN-19011, which requires every entry to cite a live ticket naming the context it excludes and diffs the registry against the base branch so an added entry is checked rather than shaped.
exit_code: 0
pr_number: 10642
contract_sha256: sha256:f2b4e5f0b1c6709e1a1689b0d00ba3b82a9a5ad637f676518ebfea5fed8933ea
contract_entry_sha256: sha256:c5d444bcc5c04b5a3dc7347f62d8f569a90a8680fc206dc52b684f793c7ed912
