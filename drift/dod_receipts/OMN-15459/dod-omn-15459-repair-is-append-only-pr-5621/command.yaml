---
# Authored by the independent adversarial verifier for lane
# next-wave-0730/evidence-rebind-safety, NOT by the implementer of #5621.
#
# This is the item that would go RED if the "repair" had been done the way the
# OMN-15485 producer defect does it -- by rewriting a merged immutable receipt
# in place. GitHub reports such a file with status "modified", so it is
# excluded by the `select(.status == "added")` filter and the count falls below
# 8.
schema_version: "1.0.0"
ticket_id: "OMN-15459"
evidence_item_id: "dod-omn-15459-repair-is-append-only-pr-5621"
check_type: "command"
check_value: "gh api repos/OmniNode-ai/onex_change_control/pulls/5621/files --paginate --jq '[.[] | select(.filename
  | endswith(\"/command.supersede.15459.yaml\")) | select(.status == \"added\") | select(.deletions ==
  0)] | length' | grep -cxF '8' | grep -qx 1"
contract_entry_sha256: "sha256:d508c35b2ad96503fd7d7d3244a7832b0cd8c2c94b5bb9c6cfc247a12c009740"
status: PASS
run_timestamp: "2026-07-30T17:32:00Z"
commit_sha: "35558f9327cd6e2cce58e60b55a32037ae5119fe"
runner: "next-wave-0730/evidence-rebind-safety-verifier"
verifier: "executed-probe"
probe_command: "bash -o pipefail -c \"gh api repos/OmniNode-ai/onex_change_control/pulls/5621/files --paginate
  --jq '[.[] | select(.filename | endswith(\\\"/command.supersede.15459.yaml\\\")) | select(.status ==
  \\\"added\\\") | select(.deletions == 0)] | length' | grep -cxF '8' | grep -qx 1\""
probe_stdout: |
  8

  (the `jq` length; `grep -cxF '8'` then emits 1 and `grep -qx 1` consumes it,
  exit 0)
actual_output: |
  PASS -- exit 0. #5621 contributes exactly 8 files named
  `command.supersede.15459.yaml`, every one with GitHub file status "added" and
  `deletions == 0`.

  WHAT THIS DISCRIMINATES. Three distinct failure modes go RED here rather than
  passing quietly:
    * an in-place rewrite of a merged immutable receipt (the OMN-15485 class)
      reports status "modified", is filtered out, count drops to 7 or lower;
    * a repair that also deleted lines from a merged record reports
      `deletions > 0`, is filtered out, same effect;
    * repairing only some of the cohort leaves the count short of 8.

  RED CONTROL, EXECUTED: same command asserting 9 -> exit 1.

  CORROBORATION FROM A SECOND, INDEPENDENT SURFACE (recorded because a single
  API-shaped assertion is thin on its own): the whole-PR diffstat read from the
  same API is 13 files, +3311/-9, and the only 9 deleted lines are in
  `scripts/validation/check_receipt_hardening.py` (6), `.pre-commit-config.yaml`
  (2) and `.github/workflows/ci.yml` (1) -- all authoring surfaces, none under
  `drift/dod_receipts/`. So no merged receipt byte was touched by this PR at all,
  which is the stronger statement this item's own check only samples.

  SHAPE NOTES:
    * OMN-15382 Rule B -- 5621 pinned literally, no `${PR_NUMBER}` placeholder.
    * OMN-15411 Rule E -- drain-then-assert throughout; no early-exiting
      consumer on an unbounded pipe.
    * Hosted-runnable: `gh api` only.
exit_code: 0
duration_ms: 833
pr_number: 5621
branch: "jonah/omn-15459-occ-companion-verifier"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-15459-companion/onex_change_control"
