---
# Authored by the independent adversarial verifier for lane
# next-wave-0730/evidence-rebind-safety, NOT by the implementer of #5621.
#
# Bound to the PULL REQUEST DIFF API rather than to a commit SHA on the feature
# branch. That is deliberate: onex_change_control#5621 must be updated with
# `dev` before it can merge (this companion is what puts contracts/OMN-15459.yaml
# into its in-tree checkout, which OCC invariant I3 requires), so its head SHA
# is expected to move. A check pinned to today's head would decay exactly the
# way the ?ref=05e105cf pins in OCC#5534 decayed.
schema_version: "1.0.0"
ticket_id: "OMN-15459"
evidence_item_id: "dod-omn-15459-s1-s2-rules-shipped-pr-5621"
check_type: "command"
check_value: "gh api repos/OmniNode-ai/onex_change_control/pulls/5621/files --paginate --jq '[.[] | select(.filename
  == \"scripts/validation/check_receipt_hardening.py\") | .patch] | join(\"\")' | grep -cE '^\\+.*(DISTINCTNESS
  = \"S1\"|FAMILY_BINDING = \"S2\")' | grep -qx 2"
contract_entry_sha256: "sha256:8753a75ec0d6b2e4674b1a01890aaa2eb7db5d4fc5817a7895762c1cd95d86f7"
status: PASS
run_timestamp: "2026-07-30T17:31:00Z"
commit_sha: "35558f9327cd6e2cce58e60b55a32037ae5119fe"
runner: "next-wave-0730/evidence-rebind-safety-verifier"
verifier: "executed-probe"
probe_command: "bash -o pipefail -c \"gh api repos/OmniNode-ai/onex_change_control/pulls/5621/files --paginate
  --jq '[.[] | select(.filename == \\\"scripts/validation/check_receipt_hardening.py\\\") | .patch] |
  join(\\\"\\\")' | grep -cE '^\\+.*(DISTINCTNESS = \\\"S1\\\"|FAMILY_BINDING = \\\"S2\\\")' | grep -qx
  2\""
probe_stdout: |
  2

  (the intermediate grep -cE count; `grep -qx 2` consumes it and emits nothing,
  exit 0)
actual_output: |
  PASS -- exit 0. Exactly two ADDED lines in #5621's diff of
  `scripts/validation/check_receipt_hardening.py` declare the two rule
  constants: `DISTINCTNESS = "S1"` and `FAMILY_BINDING = "S2"`.

  WHY THE `^+` ANCHOR IS LOAD-BEARING. Matching the constants anywhere in the
  file would pass just as happily if they had always been there; matching only
  ADDED lines proves #5621 is where they enter the tree. The pre-state is
  independently checkable and was checked: `git show
  origin/dev:scripts/validation/check_receipt_hardening.py` is 417 lines and
  contains the string `check_value` ZERO times, so on `dev` nothing in that
  gate can reason about a replacement's check at all -- which is the root hole
  OMN-15459 names.

  RED CONTROL, EXECUTED: same command with the assertion changed to `grep -qx 3`
  -> exit 1. The count is asserted, not printed, so a diff that added only one
  of the two rules would go RED rather than silently passing.

  SCOPE, STATED HONESTLY -- this check proves the two rule constants are
  INTRODUCED by this PR. It does not by itself prove they are ENFORCED; that is
  the separate CI-wiring surface (the unconditional `supersession-binding-ratchet`
  job plus the strict success-only check in `ci-summary`), which is proven by
  the job's own live green run and by the `--check-supersession-wiring`
  anti-removal anchor, not by this item. Claiming otherwise here would be the
  narrated-proof defect (OMN-15391) this ticket's own family exists to catch.

  SHAPE NOTES:
    * OMN-15382 Rule B -- 5621 is pinned as a literal. `${PR_NUMBER}` is
      pre-substituted by the compliance runner with ITS OWN value, so a
      placeholder here would silently re-check whatever PR happened to be under
      evaluation.
    * OMN-15411 Rule E -- drain-then-assert; `grep -cE` consumes the producer in
      full, no SIGPIPE under `bash -o pipefail`.
    * Hosted-runnable: `gh api` only.
exit_code: 0
duration_ms: 906
pr_number: 5621
branch: "jonah/omn-15459-occ-companion-verifier"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-15459-companion/onex_change_control"
