---
# Self-bind receipt for onex_change_control#5633 — this companion PR itself.
#
# WHY A SECOND SELF-BIND EXISTS. Two PRs cite OMN-15459: #5621 (the gate and the
# repair) and #5633 (this contract + its receipts). `occ-preflight / eligibility`
# requires a PASS receipt binding the ticket to THE PR UNDER EVALUATION, so one
# self-bind cannot cover both. This was not assumed — it was observed live on
# this PR at head 7c5a172e1fcb5f22c3b5f67180747324a2da8e23, which returned:
#
#   {"reason":"pr_ticket_mismatch","missing_contracts":[],
#    "detail":"no PASS receipt for one or more tickets binds to PR #5633 or one
#              of its commit SHAs: OMN-15459",
#    "contract_hashes":{"OMN-15459":"sha256:e85118653de7d4b3fa25fe6c16c3f51b62302b0e8f2e3ed95955a555ce1cb034"}}
#
# That readback is also the empirical proof that the contract this companion
# adds is what clears #5621's failure mode: the reason moved OFF
# `missing_contract` (the code #5621 returns) the moment the contract landed in
# an in-tree checkout, and `missing_contracts` is now empty.
schema_version: "1.0.0"
ticket_id: "OMN-15459"
evidence_item_id: "occ-self-bind-pr-5633"
check_type: "command"
check_value: "gh pr view 5633 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
  | grep -cxF 'contracts/OMN-15459.yaml' | grep -qx 1"
contract_entry_sha256: "sha256:4369a100088e23d1e82f424bbec18f717515ec7dca648ee26faed9448d8ae13d"
status: PASS
run_timestamp: "2026-07-30T17:52:00Z"
commit_sha: "7c5a172e1fcb5f22c3b5f67180747324a2da8e23"
runner: "next-wave-0730/evidence-rebind-safety-verifier"
verifier: "executed-probe"
probe_command: "bash -o pipefail -c \"gh pr view 5633 --repo OmniNode-ai/onex_change_control --json files
  --jq '.files[].path' | grep -cxF 'contracts/OMN-15459.yaml' | grep -qx 1\""
probe_stdout: |
  1

  (the intermediate grep -cxF count; `grep -qx 1` consumes it, exit 0)
actual_output: |
  PASS -- exit 0. PR 5633's own file list contains `contracts/OMN-15459.yaml`
  exactly once.

  RED CONTROL, EXECUTED: same command against a path this PR does not carry
  (`contracts/OMN-99999.yaml`) -> count 0, exit 1. So the binding asserts a
  specific delivered artifact, not merely that the PR exists.

  APPEND-ONLY NOTE. This entry was appended to contracts/OMN-15459.yaml after
  the first four were already pushed. The per-entry hashes of all four prior
  entries were re-computed after the append and are BYTE-UNCHANGED
  (e84f1804.., 8753a75e.., d508c35b.., 2aa744ec..), which is the OMN-13888
  property the whole receipt store depends on: appending a new dod_evidence
  item must not invalidate receipts already bound to earlier ones. Verified,
  not assumed.

  SUBSTANCE-FLOOR HONESTY: a BINDING item, L0 under OMN-14409 by design. It
  contributes nothing to the substance floor; the three substantive items in
  this contract carry that.

  ADMISSIBILITY CAVEAT, stated rather than glossed: unlike
  `occ-self-bind-pr-5621`, this operand IS a `contracts/` path, which
  SELF_REFERENTIAL_PATTERNS denies. This item is therefore a binding stamp
  only and must not be read as correctness evidence — the honest version of
  the label, per the OCC#5568 -> OCC#5582 correction (OMN-15352). No stronger
  admissible form exists for a companion asserting its own delivered contract;
  that structural gap is owned by OMN-15309 and is not claimed as closed here.

  SHAPE NOTES:
    * OMN-15382 Rule B -- 5633 pinned literally, no `${PR_NUMBER}` placeholder.
    * OMN-15411 Rule E -- drain-then-assert, no SIGPIPE under `bash -o pipefail`.
    * Hosted-runnable: `gh pr view` only.
exit_code: 0
duration_ms: 702
pr_number: 5633
branch: "jonah/omn-15459-occ-companion-verifier"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-15459-companion/onex_change_control"
