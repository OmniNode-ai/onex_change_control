---
# Self-bind receipt for onex_change_control#5621, the carrier PR for OMN-15459.
#
# `occ-preflight / eligibility` needs SOME PASS receipt for every cited ticket
# to bind to the PR under evaluation or one of its commit SHAs. Both channels
# are supplied: pr_number 5621, and commit_sha 35558f9327cd6e2cce58e60b55a32037ae5119fe,
# a real commit of that PR (its head at the time this receipt was authored).
#
# Authored by the independent adversarial verifier for the lane, NOT by the
# implementer (feedback_no_self_authored_evidence). #5621 is a feature PR in
# onex_change_control itself, so the OMN-15334 self-companion guard suppresses
# the machine autobind for it and no bot companion exists or will exist.
schema_version: "1.0.0"
ticket_id: "OMN-15459"
evidence_item_id: "occ-self-bind-pr-5621"
check_type: "command"
check_value: "gh pr view 5621 --repo OmniNode-ai/onex_change_control --json files --jq '.files[].path'
  | grep -cxF 'scripts/validation/check_receipt_hardening.py' | grep -qx 1"
contract_entry_sha256: "sha256:2aa744ec13d1908fd04a979b976edda6dedae675707764201901c3786711026c"
status: PASS
run_timestamp: "2026-07-30T17:33:00Z"
commit_sha: "35558f9327cd6e2cce58e60b55a32037ae5119fe"
runner: "next-wave-0730/evidence-rebind-safety-verifier"
verifier: "executed-probe"
probe_command: "bash -o pipefail -c \"gh pr view 5621 --repo OmniNode-ai/onex_change_control --json files
  --jq '.files[].path' | grep -cxF 'scripts/validation/check_receipt_hardening.py' | grep -qx 1\""
probe_stdout: |
  1

  (the intermediate grep -cxF count; `grep -qx 1` consumes it, exit 0)
actual_output: |
  PASS -- exit 0. PR 5621's own file list contains
  `scripts/validation/check_receipt_hardening.py` exactly once. That file is
  the single owner of the supersession leg this ticket names, so the binding
  ties the ticket to the PR through the artifact the ticket is about rather
  than through a bare existence probe.

  DELIBERATE NON-ASSERTIONS, each for a recorded reason:
    * Does NOT assert `.state == "OPEN"`. Per OMN-15386 that predicate is
      permanently false after merge and produces a non-convergent rebind loop.
      The file-list binding stays true for the life of the PR.
    * Does NOT assert the presence of `contracts/OMN-15459.yaml` in #5621. It
      is not there, and that absence is the defect this companion exists to
      repair: OCC invariant I3 (onex_change_control/CLAUDE.md, "In-repo trap")
      requires an OCC PR to carry its own contract + receipts, and #5621 shipped
      without one. The implementer could not supply it without authoring its own
      evidence, so the verifier does.

  SUBSTANCE-FLOOR HONESTY: this item is a BINDING item. It derives to L0 under
  OMN-14409 and deliberately contributes nothing to the substance floor -- the
  three substantive items in this contract carry that. Recording that here so
  the item is not later mistaken for correctness evidence.

  ADMISSIBILITY: `gh pr view` is a live third-party probe in command position
  and no operand matches SELF_REFERENTIAL_PATTERNS -- the operand is a product
  source path, not a contracts/ or drift/dod_receipts/ path. This is the same
  trap that made the OCC#5568 self-bind claim false (see OMN-15352 /
  OCC#5582); it is avoided here by binding to the source file rather than to
  the evidence tree.

  SHAPE NOTES:
    * OMN-15382 Rule B -- 5621 pinned literally; `${PR_NUMBER}` would be
      pre-substituted by the runner with its own value.
    * OMN-15411 Rule E -- drain-then-assert, no SIGPIPE under `bash -o pipefail`.
exit_code: 0
duration_ms: 688
pr_number: 5621
branch: "jonah/omn-15459-occ-companion-verifier"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-15459-companion/onex_change_control"
