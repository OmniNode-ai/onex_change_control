---
# Authored by the independent adversarial verifier for lane
# next-wave-0730/evidence-rebind-safety, NOT by the implementer of
# onex_change_control#5621 (feedback_no_self_authored_evidence). The
# implementer deliberately shipped #5621 without a contract for exactly this
# reason; this companion supplies it.
#
# Pin discipline: the merged bytes are read at 34c8dacc4cab7bf0e48a616fdb9820aefb09434d,
# the dev SQUASH commit for OCC#5534 -- a durable ref. The mis-paired records
# this receipt indicts pin ?ref=05e105cf..., the head of a feature branch that
# now 404s and is GC-eligible; reproducing that mistake in the evidence that
# names it would be self-defeating (reference_never_pin_a_feature_branch_head).
schema_version: "1.0.0"
ticket_id: "OMN-15459"
evidence_item_id: "dod-omn-15459-wrong-item-rebind-real-at-occ5534"
check_type: "command"
check_value: "gh api 'repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-15395/dod-occ-evidence-admissibility-validator/command.supersede.2552.yaml?ref=34c8dacc4cab7bf0e48a616fdb9820aefb09434d'
  --jq '.content' | base64 -d | grep -cF 'create_kafka_topics.py' | grep -qx 1"
contract_entry_sha256: "sha256:e84f180405f6af55d20be6ce366c4c62ac9d97d7eb13ef728bfb11799ecfaac7"
status: PASS
run_timestamp: "2026-07-30T17:30:00Z"
commit_sha: "34c8dacc4cab7bf0e48a616fdb9820aefb09434d"
runner: "next-wave-0730/evidence-rebind-safety-verifier"
verifier: "executed-probe"
probe_command: "bash -o pipefail -c \"gh api 'repos/OmniNode-ai/onex_change_control/contents/drift/dod_receipts/OMN-15395/dod-occ-evidence-admissibility-validator/command.supersede.2552.yaml?ref=34c8dacc4cab7bf0e48a616fdb9820aefb09434d'
  --jq '.content' | base64 -d | grep -cF 'create_kafka_topics.py' | grep -qx 1\""
probe_stdout: |
  1

  (the intermediate grep -cF count; the trailing `grep -qx 1` consumes it and
  emits nothing, exit 0)
actual_output: |
  PASS -- exit 0. The supersession that OCC#5534 minted for
  `dod-occ-evidence-admissibility-validator` names `create_kafka_topics.py`
  exactly once, read from the immutable merge commit `34c8dacc`.

  WHY THAT IS THE DEFECT, not a stylistic complaint. That item's own declared
  bar in contracts/OMN-15395.yaml is `uv run pytest
  tests/test_evidence_admissibility.py -q`. The replacement runs a grep of a
  Kafka-topic provisioning script. The two share no artifact family, so the
  item now authoritatively attests something it never declared, and the suite
  it does declare goes unrun. Seven sibling items in the same cohort carry the
  byte-identical replacement.

  RED CONTROL, EXECUTED (not reasoned about):
    same command, same merged bytes, anchor swapped to the *repaired* family
    `test_evidence_admissibility.py`  ->  grep -cF emits 0, `grep -qx 1` exits 1.
  So this check discriminates the wrong-item binding from the right one at the
  same ref -- which is precisely the pairing S2 was built to see and which
  every prior gate (OMN-14505 predicate, lint_contract_check_values,
  contract_compliance_check) is structurally blind to, because each reasons
  about a check in isolation.

  SHAPE NOTES:
    * OMN-15411 Rule E -- drain-then-assert. `grep -cF` consumes its producer
      in full and emits one line, so there is no early-exiting consumer on an
      unbounded pipe and no SIGPIPE 141 to be promoted under `bash -o pipefail`.
    * Hosted-runnable: `gh api` only. No LAN dependency, no ssh, no docker, no
      local filesystem operand, no `uv`.
    * Durable ref: a squash commit on `dev`, not a feature-branch head.
exit_code: 0
duration_ms: 741
pr_number: 5621
branch: "jonah/omn-15459-occ-companion-verifier"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-15459-companion/onex_change_control"
