---
schema_version: "1.0.0"
ticket_id: "OMN-17228"
evidence_item_id: "dod-omn17228-ac2-red-first-real-postgres"
check_type: "command"
check_value: |-
  gh api "repos/OmniNode-ai/omnimarket/contents/tests/test_omn17228_real_postgres_drifted_default_write_path.py?ref=8dfb7bbf9506497e10f763cb821bb107ccc47958" --jq '.content' | base64 -d | grep -c 'ALTER COLUMN {column} DROP DEFAULT'
contract_entry_sha256: "sha256:4f102c3c7283420f2d10357701d62a0a956c4c74eda22a7abbdcf10b20ce0cc1"
status: PASS
run_timestamp: "2026-09-10T10:14:18.6N+00:00"
commit_sha: "8dfb7bbf9506497e10f763cb821bb107ccc47958"
runner: "staging-dlq-corpus-triage"
verifier: "staging-dlq-corpus-triage"
probe_command: |-
  gh pr view 2443 --repo OmniNode-ai/omnimarket --json state,mergeCommit --jq '[.state,.mergeCommit.oid]|join(" ")'
probe_stdout: |
  MERGED 8dfb7bbf9506497e10f763cb821bb107ccc47958
actual_output: |
  PASS. All four declared checks of this item were executed against the MERGED squash commit
  8dfb7bbf9506497e10f763cb821bb107ccc47958 (the product branch is deleted, so a head-pinned
  probe would not resolve).

    check 1  drifted-fixture marker at the merged sha .................. 1
    check 2  the four insert-only columns at the merged sha ........... 1
    check 3  NEGATIVE CONTROL, bound string at squash parent
             71c6ad952947035532000893b1bb895b86564bd6 ................. 0
    check 4  product change MERGED at that exact sha .................. 1

  Check 3 is a zero, so it carries its own positive control: the SAME grep shape
  against the SAME file at the merged sha returns 1. The zero is therefore a
  measured absence at the parent, not a pattern that matches nothing anywhere.

  What this item binds, and what it does not: AC2 only, the RED-first test through
  the real dispatch path. The RED half was measured against REAL Postgres on the
  .201 compose dev lane 2026-09-10 -- at the parent the three behavioural tests
  raise asyncpg.exceptions.NotNullViolationError, 'null value in column
  "task_type" of relation "delegation_events" violates not-null constraint', which
  is the live dead-letter record's failure_reason byte-for-byte; all five pass at
  head. DoD bullets 1, 3 and 4 are NOT claimed and are NOT met. The ticket stays
  open.
exit_code: 0
pr_number: 2443
branch: "jonah/omn-17228-ac2-bind"
