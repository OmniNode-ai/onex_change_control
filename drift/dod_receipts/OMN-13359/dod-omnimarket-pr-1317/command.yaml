---
schema_version: "1.0.0"
ticket_id: "OMN-13359"
evidence_item_id: "dod-omnimarket-pr-1317"
check_type: "command"
check_value: "uv run pytest tests/unit/nodes/node_generation_consumer/ -m 'not kafka' -q"
status: PASS
run_timestamp: "2026-06-20T18:24:32Z"
commit_sha: "16ae7badccb8be318435a80c4884cf56cdeb26fc"
runner: "claude-omn13359-ladder-worker"
verifier: "claude-receipt-review"
contract_sha256: "sha256:5e8f33e400ad83b3aed69e7864bc6332836363b33a51c7d1461e9cd1b2885989"
branch: "jonah/omn-13359-generation-rides-routing-ladder"
working_dir: "/$OMNI_HOME/omni_worktrees/OMN-13359/omnimarket"
pr_number: 1317
probe_command: "uv run pytest tests/unit/nodes/node_generation_consumer/ -m 'not kafka' -q"
probe_stdout: |
  234 passed in 12.25s
actual_output: |
  omnimarket PR #1317 wires node_generation_consumer onto the EXISTING delegation routing
  ladder. The handler now carries a ModelActiveRoute that tracks the model/endpoint the run
  is routing to: attempt #1 rides the contract-declared starting tier (unchanged first-attempt
  behavior), and each quality-gate (contract-validation) failure advances the active route to
  the next tier via the routing authority (node_delegation_routing_reducer.delta) so the NEXT
  attempt actually calls the escalated model. A single authority surface
  (_resolve_escalation_decision) backs both the escalation proof event and the route
  advancement, so the proof records exactly the tier the next attempt rides. routing_tiers.yaml
  and task_class_contracts.v1.yaml were NOT edited — the ladder is consumed only through the
  delta / next_eligible_tier / tier_for_backend / tier_max_retries authority.

  Added tests/unit/nodes/node_generation_consumer/test_generation_rides_ladder_omn13359.py (5
  tests) proving the behavior through the REAL dispatch path (the non-injected _call_llm branch
  that builds a real ModelLlmInferenceRequest and resolves the endpoint from the routing
  authority against a hermetic bifrost fixture): local-first attempt #1, escalation to a cloud
  tier on QG fail (different model AND endpoint), quality-gate stop on first pass, proof matches
  the active route, benchmark records the escalated tier.

  Verifier (claude-receipt-review, distinct from runner) re-ran the generation-consumer unit
  suite: 234 passed (229 existing + 5 new). Strict mypy on the changed handler + test is clean;
  ruff clean; all pre-commit gates pass. The broader generation/routing/delegation/escalation
  selection across the tree reports 1590 passed, 51 skipped.
exit_code: 0
duration_ms: 12250
