---
schema_version: "1.0.0"
ticket_id: "OMN-13359"
evidence_item_id: "dod-deploy-assessment"
check_type: "command"
check_value: "uv run python scripts/ci/run_runtime_sweep.py  # deploy-equivalent static proof for a handler
  change; NO lane mutation"
status: PASS
run_timestamp: "2026-06-20T18:24:32Z"
commit_sha: "16ae7badccb8be318435a80c4884cf56cdeb26fc"
runner: "claude-omn13359-ladder-worker"
verifier: "claude-receipt-review"
contract_sha256: "sha256:5e8f33e400ad83b3aed69e7864bc6332836363b33a51c7d1461e9cd1b2885989"
branch: "jonah/omn-13359-generation-rides-routing-ladder"
working_dir: "/$OMNI_HOME/omni_worktrees/OMN-13359/omnimarket"
pr_number: 1317
probe_command: "uv run python scripts/ci/run_runtime_sweep.py"
probe_stdout: |
  runtime_sweep: 316/316 entry points OK
actual_output: |
  Deploy assessment: NON-DEPLOYABLE-IN-THIS-PR / no-runtime-mutation. PR #1317 modifies the
  node_generation_consumer handler's model-selection path so generation rides the existing
  delegation routing ladder. It wires no new consumer into a running runtime and mutates no lane.
  No prod / judge / stability / dev lane was started, restarted, deployed, or otherwise mutated;
  no docker compose, no rpk produce, no psql write was run for this change.

  The deploy-equivalent proof for this handler change is the docker-free runtime sweep, which
  statically verifies every node contract + entry point: it reports 316/316 entry points OK.
  The new behavior is bound by the real-dispatch-path tests
  (test_generation_rides_ladder_omn13359.py: 5 tests, exercising the routing authority's
  delta/next_eligible_tier for real) and strict mypy.

  Live runtime adoption — rebuilding the effects-lane image so the DEPLOYED generation consumer
  rides the ladder — is a separate deploy step, intentionally NOT part of this no-merge PR. This
  assessment asserts only that no live mutation occurred and that the static deploy-equivalent
  proof passes. Verifier (claude-receipt-review, distinct from runner) re-ran the runtime sweep
  and confirmed 316/316; probe_stdout above is the verifier's captured output.
exit_code: 0
duration_ms: 0
