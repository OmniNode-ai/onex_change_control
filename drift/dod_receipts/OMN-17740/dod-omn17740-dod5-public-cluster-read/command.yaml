---
schema_version: 1.0.0
ticket_id: OMN-17740
evidence_item_id: dod-omn17740-dod5-public-cluster-read
check_type: command
check_value: |
  env AWS_PAGER= aws ssm get-command-invocation --region us-east-1 --command-id 97eba021-aea7-4d85-a2cb-6bc376a17a10 --instance-id i-0e596e8b557e27785 --query 'StandardOutputContent' --output text | grep -c 'KEY_COUNT: 5' | grep -x '1'
contract_sha256: c18b45ed0f84547d63f728d628255b3f5327dc2ecdd352b50b830fc6e73eb1fa
contract_entry_sha256: sha256:50d749645c88b1f77a7767740c322e37584e412bc7500e216f227f1bd4d2b1b2
status: PASS
run_timestamp: '2026-09-10T15:20:00Z'
commit_sha: b564a6c650987a8f834b1aecc7cb11a6d1dfa667
runner: claude-local-worktree
verifier: omn17740-dod3-dod5
probe_command: |
  env AWS_PAGER= aws ssm get-command-invocation --region us-east-1 --command-id 97eba021-aea7-4d85-a2cb-6bc376a17a10 --instance-id i-0e596e8b557e27785 --query 'StandardOutputContent' --output text | grep -c 'KEY_COUNT: 5' | grep -x '1'
probe_stdout: |
  1
actual_output: |
  PASS. DoD 5 -- the other cluster read, read-only, on i-0e596e8b557e27785. All three
  command checks run and every one exits 0:
    1. 97eba021 'KEY_COUNT: 5' -> exactly 1. The managed Secret onex-dev/omniweb-credentials
       carries exactly five keys, matching OMN-16526's stated criterion.
    2. 97eba021 'Last reconcile synced 5 secrets' -> 1, from the CR's own condition message.
    3. 97eba021 'PROBE_PRESENT_ON_PUBLIC_CLUSTER: NONE (expected)' -> 1.
  Check 3 is a control on this lane's own blast radius rather than on the criterion: the
  DoD 3 probe key was written only to the dev-system store, so a sweep of every Secret in
  every namespace on THIS instance must find it nowhere. It does.
  VERDICT: OMN-16526 is genuinely fixed on its own surface. This ticket's break was a
  distinct regression on a different cluster, not a contradiction of that closure.
  HONEST LIMIT, carried in the contract entry and repeated here so the receipt does not
  overstate what it proves: this is a CONDITION read, not a subsequent-sync proof. Those
  conditions last transitioned 2026-08-30T21:06:33Z and do not re-stamp on an unchanged
  sync, so this readback alone cannot separate a live controller from one that stopped and
  left True frozen -- exactly the failure shape DoD 4's gate was built to catch. The
  stronger property needs that instance's operator log window, outside this lane's scope.
  ALSO SEEN, not acted on and owned by neither ticket: dev/omnidash-credentials
  LoadedInfisicalToken=False since 2026-08-30T19:48:58Z ('authentication failed for strategy
  [UNIVERSAL_MACHINE_IDENTITY]'), and onex-prod/umami-infisical-sync ReadyToSyncSecrets=False
  since 2026-08-19T13:16:44Z.
  Nothing on this instance was mutated.
exit_code: 0
pr_number: 8955
branch: jonah/omn-17740-dod3-dod5-bind
