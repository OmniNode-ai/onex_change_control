---
schema_version: 1.0.0
ticket_id: OMN-17740
evidence_item_id: dod-omn17740-dod2-crs-true-subsequent-sync
check_type: command
check_value: |
  env AWS_PAGER= aws ssm get-command-invocation --region us-east-1 --command-id 9633f23e-b92c-4aac-8f50-f12594382094 --instance-id i-06169517a92b45f86 --query 'StandardOutputContent' --output text | grep -c 'READY=True LOADED=True' | grep -qx '6'
contract_sha256: "sha256:e6909f26f54175e6fecbe6c02a88f841f1d22d1fc3e1f5518231b5112189034e"
contract_entry_sha256: sha256:702f41a8a07290294ed1b0ed84c47ad0fa6f6bbb20154c6a57c07c6cdb1852be
status: PASS
run_timestamp: '2026-09-10T12:30:00Z'
commit_sha: a58f35de667c03acb6a5613baa693fcdf7c53e00
runner: claude-local-worktree
verifier: omn17740-dod-remainder
probe_command: |
  env AWS_PAGER= aws ssm get-command-invocation --region us-east-1 --command-id 9633f23e-b92c-4aac-8f50-f12594382094 --instance-id i-06169517a92b45f86 --query 'StandardOutputContent' --output text | grep -c 'READY=True LOADED=True' | grep -qx '6'
probe_stdout: |
  6
actual_output: |
  PASS. DoD 2 -- every InfisicalSecret CR reports ReadyToSyncSecrets=True, re-read live.
  Both command checks on this item were executed against SSM invocation
  9633f23e-b92c-4aac-8f50-f12594382094 (i-06169517a92b45f86, read-only, 2026-09-10T10:41Z) and both
  exited 0:
    1. exactly 6 lines match 'READY=True LOADED=True' -> 6. The five CRs in onex-dev plus one in
       dev, True on BOTH conditions rather than only the one the criterion names.
    2. 'AUTHFAIL COUNT=0 WINDOW_LINES=1170' -> 1.
  A True condition alone was NOT accepted as proof. The same readback carries a per-CR LAST_SUCCESS
  inside the preceding 60 s for all six, which is what distinguishes a live controller from one that
  stopped reconciling and left its conditions frozen at True -- the shape that recorded success
  fourteen hours before the 2026-08-30 failure.
  POSITIVE CONTROL on the zero: WINDOW_LINES=1170 is the operator log window's own line count, so
  the zero auth failures is a reading rather than an empty probe. A count of nothing drawn from a log
  read that returned nothing is itself refused by the gate built under DoD 4.
  Supersedes dod-omn17740-cr-conditions-live-readback, which binds this criterion from the
  2026-09-08 readback but predates binds_ac and so declares no criterion. No claim in that item is
  withdrawn.
exit_code: 0
pr_number: 1300
branch: dev
