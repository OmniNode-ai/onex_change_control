---
schema_version: "1.0.0"
ticket_id: "OMN-13118"
evidence_item_id: "dod-tierb-longlived-correlator"
check_type: "command"
check_value: "uv run pytest tests/integration/test_terminal_consumer_longlived_correlator.py tests/unit/runtime/auto_wiring/test_event_consumer_injection.py
  -q"
status: PASS
run_timestamp: "2026-06-13T19:05:00Z"
commit_sha: "d084eb82e1320efc800090caeaa66fb21680dbe5"
runner: "omn-13118-tierb-impl"
verifier: "receipt-gate-local"
probe_command: "git stash push -- src/omnibase_infra/runtime/service_terminal_event_consumer.py; uv run
  pytest tests/integration/test_terminal_consumer_longlived_correlator.py::test_correlator_reads_terminal_published_in_open_wait_gap
  -q; git stash pop; uv run pytest tests/integration/test_terminal_consumer_longlived_correlator.py tests/unit/runtime/auto_wiring/test_event_consumer_injection.py
  -q"
probe_stdout: |
  RED (source reverted to per-trial substrate via git stash):
    AttributeError: 'TerminalConsumerSession' object has no attribute 'register'
    1 failed in 0.42s
  GREEN (long-lived correlator restored):
    test_terminal_consumer_longlived_correlator.py ... 3 passed
    test_event_consumer_injection.py ... 2 passed
    5 passed in 32.57s
actual_output: >-
  PASS: the NEW real-dispatch repro test_terminal_consumer_longlived_correlator drives the REAL TerminalEventConsumer.open()/register()/wait()
  through the long-lived LongLivedTerminalCorrelator the way HandlerContextRoiRunner._run_trial does —
  open BOTH terminal topics, register the cid before publish, race the wait — across K=10 x 2 cells x
  2 arms (40 trials). It asserts exactly ONE consumer is built for the WHOLE battery (the structural property
  the five per-trial fixes could not establish), the COMPLETED HWM climbs to 40, the partition-less FAILED
  topic stays at HWM 0, every trial correlates the gap-landed COMPLETED terminal, and both cells/arms
  advance with zero degenerate rows. RED genuineness verified by git-stashing ONLY src/omnibase_infra/runtime/service_terminal_event_consumer.py
  (the old per-trial substrate has no register()/correlator API) and re-running. test_event_consumer_injection
  rewritten to the new correlator seam exercises the real _prepare_handler_wiring -> make_terminal_event_consumer
  path. NOT the acceptance gate — acceptance is the LIVE K>=10 stability-lane reprobe (per the plan).
exit_code: 0
pr_number: 1974
branch: "jonah/omn-13118-tierb-occ-receipt"
contract_sha256: "sha256:45d5d12d6961e2a45377c312e3bbe65e2a47dbac287a29cd3152cecf3e6728a3"
