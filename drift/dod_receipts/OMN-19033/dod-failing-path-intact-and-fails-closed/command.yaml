---
schema_version: 1.0.0
ticket_id: OMN-19033
evidence_item_id: dod-failing-path-intact-and-fails-closed
check_type: command
check_value: uv run pytest tests/test_prod_promotion_grants_required_context.py::TestTheFailingPathIsIntact
  -q
contract_sha256: sha256:bddcc631eea9900048e510a18f6a544a0fd4f9179e8235ac0d03265a19a9e8dc
contract_entry_sha256: sha256:943aa15b4cc4ba4053c7befae972f1cc38eb81108e23bf05ee853677bbc4f1c5
status: PASS
run_timestamp: '2026-09-21T15:26:00Z'
commit_sha: c298c17857d71ef4311ea4b3c0cb923a220cbb94
pr_number: 10682
runner: claude-opus-occ-omn19033-lane
verifier: ci-job-contract-compliance-check
probe_command: uv run pytest tests/test_prod_promotion_grants_required_context.py::TestTheFailingPathIsIntact
  -q
probe_stdout: |-
  .........                                                                [100%]
  9 passed in 0.70s
actual_output: |-
  PASS. Nine assertions, each running the workflow's OWN inline validator rather than a copy: the
  heredoc is located through the PARSED workflow YAML, written to a file and executed as a separate
  process, so a change to the surrounding workflow shape cannot make this item read something else.

  THE FIVE RED SHAPES, each exit 1 under enforcement:

      malformed entry           grant_id, image_digest and expires_at<=created_at all reported
      expired entry             reported as EXPIRED with its own timestamp
      unparseable YAML          refused at load
      top level is a list       refused as not a mapping
      unexpected top-level key  refused

  FAILING CLOSED IS ASSERTED, NOT ASSUMED. With `GRANT_VALIDATION_ENFORCE` absent entirely, the
  malformed anchor still exits 1. That is the single most important assertion in this item: the whole
  change turns on a flag, and a flag whose default is permissive converts every path the `scope` step
  does not reach into a silent pass. The default is enforcement, so an unreachable or misspelled
  scope output validates for real.

  PAIRED POSITIVE CONTROL, without which the five reds above prove nothing. A valid `entries: []`
  anchor is run in all three modes -- enforce, advisory, and flag absent -- and exits 0 with PASS in
  each. A validator that refused everything would satisfy every red assertion in this item and be
  worthless; this is the control that rules it out.

  MEASURED AGAINST THE LIVE ANCHOR TOO. The registry as it stands on `main` today carries one entry
  whose `expires_at` is `2026-09-20T20:23:15Z`. Run through the same extracted validator under
  enforcement it exits 1 and names that timestamp. Under advisory it exits 0 and annotates. That is
  the exact pair of behaviours this change depends on, measured on the real file rather than a
  fixture.
