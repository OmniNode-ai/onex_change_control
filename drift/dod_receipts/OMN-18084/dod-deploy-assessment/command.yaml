---
schema_version: "1.0.0"
ticket_id: "OMN-18084"
evidence_item_id: "dod-deploy-assessment"
check_type: "command"
check_value: |-
  gh api repos/OmniNode-ai/omnibase_infra/pulls/3365/files --paginate --jq '.[].filename' | grep -ciE 'runtime/|nodes/|event_bus/'
contract_sha256: "sha256:d007aaee4c32683e9aebf051ab0e83bd497906837cb0f33740208ff3e2b37e41"
contract_entry_sha256: "sha256:6ca05928f5d0c7b258bab43fe10fd30366a592b285d5d3eb076431cf5c717654"
status: PASS
run_timestamp: "2026-09-09T17:52:38+00:00"
commit_sha: "e13932a406b5bc5ef34fefc210896238ce9a1909"
runner: "dlq-loop-fix"
verifier: "occ-union-resolve-same-ticket-companion"
probe_command: |-
  gh api repos/OmniNode-ai/omnibase_infra/pulls/3365/files --paginate --jq '.[].filename' | grep -ciE 'runtime/|nodes/|event_bus/'
probe_stdout: |
  9
actual_output: |-
  PASS: 9 of the 11 changed files sit on a deployed runtime surface - the auto-wiring consume
  boundary, the subcontract wiring dead-letter path, the bus-forwarder gateway delivery service,
  the DLQ replay handler and its tests, plus the topic resolver and the Kafka DLQ mixin.

  Deploy assessment, stated rather than implied. Every source change is a guard that REMOVES a
  publish, plus one mutex that serialises a lifecycle that was already shared. The single
  operator-visible behaviour change is that a handler failure on a record consumed FROM a
  dead-letter sink is now recorded as a structured log line naming the topic instead of producing
  a new dead-letter record; the positive control in the AC1 module pins that an ordinary topic
  still produces exactly one dead-letter write to a DIFFERENT topic, so the boundary's reason to
  exist is intact.

  No lane, container, topic, consumer group, service, retention setting, cluster, credential or
  routing variable was mutated by this change or by the lane that authored it. The fix reaches the
  .201 dev lane only when the deploy agent is restarted; this lane neither performed nor requested
  that, and says so in the PR body rather than asserting a lab proof it does not have.
exit_code: 0
pr_number: 3365
