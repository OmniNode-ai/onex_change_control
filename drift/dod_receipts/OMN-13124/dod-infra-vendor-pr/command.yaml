---
schema_version: "1.0.0"
ticket_id: "OMN-13124"
evidence_item_id: "dod-infra-vendor-pr"
check_type: "command"
check_value: "OMNI_HOME=$OMNI_HOME bash scripts/sync-node-migrations.sh --check"
status: PASS
run_timestamp: "2026-06-13T00:00:00Z"
commit_sha: "e664c023e557ab018a27425e5872f671f842c11d"
runner: "worktree-agent-omn-13124-durable"
verifier: "receipt-gate-local"
probe_command: "bash scripts/sync-node-migrations.sh --check && (move vendored copy; --check expect exit
  1) && psql -f 0000_create_pattern_learning_artifacts.sql (throwaway pg14) && uv run --frozen python
  scripts/validate.py migration_freeze && uv run --frozen python scripts/validation/validate_migration_sequence.py"
probe_stdout: |
  $ OMNI_HOME=… bash scripts/sync-node-migrations.sh
  [sync-node-migrations]   vendored node_omnigate_projection/0000_create_gate_projection_tables.sql
  [sync-node-migrations]   vendored node_projection_pattern_learning/0000_create_pattern_learning_artifacts.sql
  [sync-node-migrations] done: 2 file(s) updated.

  $ OMNI_HOME=… bash scripts/sync-node-migrations.sh --check
  [sync-node-migrations] check: in sync.   (exit 0)

  # gate proof — remove the vendored copy, --check must fail:
  $ mv docker/migrations/forward/nodes/node_projection_pattern_learning /tmp && bash scripts/sync-node-migrations.sh --check
  [sync-node-migrations] DRIFT: node_projection_pattern_learning/0000_create_pattern_learning_artifacts.sql
  [sync-node-migrations] node migration vendor tree is OUT OF SYNC with omnimarket.   (exit 1)

  # SQL applies to omnidash_analytics on a throwaway postgres 14, idempotent:
  $ psql -d omnidash_analytics -f .../0000_create_pattern_learning_artifacts.sql   -> CREATE TABLE … (exit 0)
  $ \d pattern_learning_artifacts  -> pattern_id uuid not null; correlation_id text; composite_score numeric(10,6)
  $ re-apply (IF NOT EXISTS)  -> RE-APPLY CLEAN (idempotent)

  $ uv run --frozen python scripts/validate.py migration_freeze            -> exit 0
  $ uv run --frozen python scripts/validation/validate_migration_sequence.py -> exit 0

  $ pre-commit run onex-check-node-migration-sync --all-files
  ONEX Node Migration Vendor Sync Check....................................Passed
actual_output: >-
  PASS: omnibase_infra PR (branch jonah/omn-13124-vendor-pattern-learning-migration, base=dev, head e664c023e557ab018a27425e5872f671f842c11d)
  makes the golden-chain pattern_learning chain DURABLE. It vendors node_projection_pattern_learning/0000_create_pattern_learning_artifacts.sql
  (+ node_omnigate_projection) into docker/migrations/forward/nodes/ via sync-node-migrations.sh, so the
  forward-migration runner (which applies forward/nodes/ to NODE_POSTGRES_DB=omnidash_analytics) recreates
  pattern_learning_artifacts on every clean redeploy — closing the run12 Finding where the table existed
  only via a live hot-fix. ENFORCEMENT: the onex-check-node-migration-sync pre-commit hook is flipped
  from a files:-scoped trigger (which never fired for an omnimarket-only node migration — the gap that
  let #1210 merge un-vendored) to always_run, and a new node-migration-sync.yml GHA gate re-runs --check
  against the omnimarket dev tip on every infra PR + merge_group. PROVES: vendored tree byte-identical
  to source; --check passes in sync and fails (exit 1) when a vendored copy is removed; the SQL applies
  cleanly + idempotently to omnidash_analytics on postgres 14; freeze/sequence validators exit 0 (namespaced
  node: identity, no flat-sequence collision). SCOPE: code + migration vendoring + CI/pre-commit gate
  only. NO live deploy, NO restart, NO .201 mutation. deploy_pending = true: the table becomes durably
  present on the dev lane only after the next clean redeploy applies the now-committed vendored migration.
  HONESTY: this receipt does not claim the table is live in omnidash_analytics via this PR — it claims
  the repo now recreates it deterministically on redeploy.
exit_code: 0
contract_sha256: "sha256:1aec4a90e6e1eeec39d3dce3d258954583a2da0c7ae0a245d99c68d11607fb9f"
pr_number: 1978
branch: "jonah/omn-13124-vendor-pattern-learning-migration"
