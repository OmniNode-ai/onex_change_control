---
schema_version: "1.0.0"
ticket_id: OMN-15468
evidence_item_id: "dod-15468-behavior-proof-worktree"
check_type: test_passes
check_value: "uv run pytest tests/unit/runtime/test_omn15468_kernel_applier_failure_terminal.py -q"
status: PASS
run_timestamp: "2026-09-06T10:40:00Z"
contract_entry_sha256: "sha256:daa8b268a56f5e31638c9f58246850e16484fd77d987a307c452394971ef8280"
commit_sha: "4346814ab3c0c1f591cb15c62270848dba28d6eb"
runner: "claude:omn15468-failure-verdict-topic-fix"
verifier: omn15468-live-lane-verifier
pr_number: 3225
probe_command: |
  # Executed in the ticket's omnibase_infra worktree, whose HEAD is the merge commit of #3225 at
  # repos/OmniNode-ai/omnibase_infra/commits/4346814ab3c0c1f591cb15c62270848dba28d6eb
  git log --oneline -1
  uv run pytest tests/unit/runtime/test_omn15468_kernel_applier_failure_terminal.py -q
probe_stdout: |
  4346814ab fix(OMN-15468): build every dispatch-result applier through its contract factory (#3225)
  .........                                                                [100%]
  9 passed in 3.08s
actual_output: |
  PASS: 9 passed at the merged tip 4346814ab.

  This is the ticket's BEHAVIOUR proof -- the proof class every other entry on this contract lacks.
  The others read content at a pinned ref, read a PR's state, or grep a receipt; under the
  OMN-16821 taxonomy each is MERGE_STATE or SURROGATE, a statement about GitHub or about bytes.
  This one executes the claimed behaviour through the real factory, the real DispatchResultApplier,
  a real ModelEventEnvelope JSON round-trip and the real _status_for_terminal_topic, over a fixture
  contract shaped exactly like node_delegate_skill_orchestrator:

    test_map_miss_failure_verdict_lands_on_the_failure_terminal   the defect, corrected
    test_success_verdict_still_lands_on_the_success_terminal      same-contract positive control
    test_declared_failure_class_routes_by_class_identity          class routing stays primary
    test_failure_terminal_record_reads_as_failed_after_the_wire   ok now reflects the verdict
    test_static_factory_requires_the_failure_terminal_answer      no silent empty default
    test_no_direct_applier_construction_in_src                    enforcement gate, real tree
    test_validator_positive_control                               that scanner can find a violation
    test_factory_derives_both_contract_routing_inputs
    test_declared_failure_terminal_topics_reads_the_contract

  Locality note, recorded rather than implied: this runs in the ticket's worktree, not the
  canonical clone. The canonical omnibase_infra venv cannot host it -- `onex skill dod_verify`
  requires omnimarket installed there, and the OMN-15620 venv-purity gate refuses any pytest run in
  a venv carrying an onex.nodes provider uv.lock does not declare. Both were confirmed in this
  session: `uv sync` removed omnimarket and the suite ran; re-installing omnimarket via
  scripts/install-node-skill-package.sh restored dispatch and the purity gate refused pytest again.
exit_code: 0
