---
schema_version: "1.0.0"
ticket_id: "OMN-15468"
evidence_item_id: "dod-OmniNode-ai-omnibase_infra-pr-2565"
check_type: "command"
check_value: |-
  body="$(gh api 'repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/runtime_local_ingress.py?ref=712ccb070e93bb8de192d466b349c0e7afd6e4b8' --jq .content | base64 -d)" && printf '%s' "$body" | grep -qF 'PROVENANCE — every number above is a measurement' && printf '%s' "$body" | grep -qF 'aea0c33dd89fb82fdca33aac7149992a21c46d43' && ! printf '%s' "$body" | grep -qF '24 of those 51 contracts'
contract_entry_sha256: "sha256:0bc68969215474e800e75d41cf582d6a41712b7f967738d5799500c3e9dab392"
status: PASS
run_timestamp: "2026-07-30T08:04:06+00:00"
commit_sha: "712ccb070e93bb8de192d466b349c0e7afd6e4b8"
runner: "bash-5.3.9 -o pipefail on stickybeatz-studio (.200 gate host)"
verifier: "occ-companion-author-fable-resume-0730"
probe_command: "gh pr view 2565 --repo OmniNode-ai/omnibase_infra --json number,state,headRefOid,baseRefName,headRefName"
probe_stdout: |
  {"baseRefName":"dev","headRefName":"jonah/omn-15468-terminal-count-provenance","headRefOid":"712ccb070e93bb8de192d466b349c0e7afd6e4b8","number":2565,"state":"OPEN"}
actual_output: |-
  PASS (exit 0 on the .200 gate host under bash -o pipefail). At PR #2565's exact head
  712ccb070e93bb8de192d466b349c0e7afd6e4b8, src/omnibase_infra/runtime/runtime_local_ingress.py carries the
  PROVENANCE stamp and the corpus sha aea0c33dd89fb82fdca33aac7149992a21c46d43, and no longer carries the
  retracted "24 of those 51 contracts" figure. FALSIFIABILITY PROVEN, not asserted -- the identical command
  with ref= swapped to the merge-base 6a0949fd55c7f54042849507bb42e2cab3adcc8d exits 1, so this check goes
  RED against the unfixed parent.

  INDEPENDENT VERIFICATION BY THIS EVIDENCE AUTHOR (not taken from the implementer's report). (1) Scope --
  stated PER FILE, because the non-vacuity control does NOT apply uniformly across the two changed files.
  Both files parse to an AST identical to the merge-base with docstrings stripped, so the diff carries zero
  executable change. src/omnibase_infra/runtime/runtime_local_ingress.py: stripped EQUAL / unstripped
  DIFFERS -- the stripping control is VALID there and the equality is non-vacuous. tests/unit/runtime/
  test_runtime_local_ingress.py: its diff is `#` comments ONLY, and comments never enter a Python AST, so
  stripped and unstripped are BOTH EQUAL -- the stripping control is a no-op and NOT APPLICABLE to this
  file. That does not weaken the conclusion, it strengthens it: nothing in that file's diff is AST-visible
  at all. AMENDED 2026-07-30 by the same evidence author -- this receipt as first pushed asserted the
  "unstripped DIFFERS" control held for BOTH files, which is FALSE for the test file; that sentence was a
  false attestation and is retracted here. Re-run recipe: ast.dump(ast.parse(src), include_attributes=False)
  over each file's blob at 6a0949fd55c7f54042849507bb42e2cab3adcc8d vs
  712ccb070e93bb8de192d466b349c0e7afd6e4b8, with and without docstring-Expr stripping; observed
  runtime_local_ingress.py {stripped: EQUAL, unstripped: DIFFERS} and test_runtime_local_ingress.py
  {stripped: EQUAL, unstripped: EQUAL}. Only actual_output prose changed in this amendment -- check_value,
  contract_entry_sha256, status, exit_code, and every probe field are byte-unchanged.
  (2) Figures: the 51/30/21/17/13 counts were re-derived from scratch against the raw corpus of 384
  src/omnimarket/nodes/*/contract.yaml blobs read out of git at omnimarket@aea0c33dd89fb82fdca33aac7149992a21c46d43,
  using an independent port of the shipped _terminal_event_topics_from_declaration / _safe_optional_string /
  _select_command_topic helpers. Every figure reproduced exactly; 51 = 21 + 30 and 30 = 17 + 13 hold; all 51
  carry an explicit `failure` key and at least one runtime_dispatch topic absent from the top level; and the
  retracted 24 matches none of the counts on this axis.
exit_code: 0
pr_number: 2565
branch: "jonah/omn-15468-terminal-count-provenance"
evidence_class: "backend"
diff_attestations:
  - RECEIPTS_UNMODIFIED
