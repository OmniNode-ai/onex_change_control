---
schema_version: "1.0.0"
ticket_id: "OMN-14428"
evidence_item_id: "dod-verify-source-pr-ci-status"
check_type: "command"
check_value: >-
  test "$(gh api repos/OmniNode-ai/onex_change_control/commits/b60544ffe09918055337d4b6248cbe366458897d/check-runs
  --jq '([.check_runs[] | select(.name != "verify / verify" and .name != "occ-preflight / eligibility")
  | .conclusion] | index("failure")) // "none"')" = "none"
status: PASS
run_timestamp: "2026-07-12T04:13:27Z"
commit_sha: "b60544ffe09918055337d4b6248cbe366458897d"
runner: "fix-occ-pin"
verifier: "verify-occ-pin"
pr_number: 3995
probe_command: >-
  gh api repos/OmniNode-ai/onex_change_control/commits/b60544ffe09918055337d4b6248cbe366458897d/check-runs
  --jq '[.check_runs[] | select(.name != "verify / verify" and .name != "occ-preflight / eligibility")
  | .conclusion] | unique'  # then the same query re-run against OmniNode-ai/omnibase_infra#2264's head
  SHA (an independently reported 32-failing-check PR) as a known-red control; separately, a supplementary
  merge-status probe: gh api repos/OmniNode-ai/onex_change_control/pulls/3995 --jq .merged, re-run against
  the merged control OmniNode-ai/onex_change_control#3994
probe_stdout: |
  ["SKIPPED","SUCCESS"]
  # control (OmniNode-ai/omnibase_infra#2264 head SHA 26e1c3e915a98832519ad9f2a35534915bb6c071):
  ["FAILURE","SKIPPED","SUCCESS"]
  # supplementary merge-status probe (gh api repos/OmniNode-ai/onex_change_control/pulls/3995 --jq .merged):
  false
  # merge-status control (OmniNode-ai/onex_change_control#3994, a merged companion PR):
  true
exit_code: 0
actual_output: >-
  PASS: queried GitHub's check-runs API directly for #3995's actual head commit SHA and confirmed no check
  carries a failure conclusion, excluding only verify/verify and occ-preflight/eligibility — the two checks
  that fail solely because this companion's own contract was absent from OCC evidence, the documented
  missing_contract circularity this companion exists to resolve, not a defect in #3995's substantive changes.
  Bidirectionally proven against a real control: the identical check-runs query run against omnibase_infra#2264's
  head SHA (independently reported to carry 32 failing checks) correctly surfaces a FAILURE conclusion
  in the result set, and the check_value correctly fails closed against it. This closes the gap the team
  lead identified in OCC#4002, where a content-existence-only check let a red source PR (omnibase_infra#2264,
  open with 32 failing checks) get laundered into merged evidence — this check instead reads the actual
  CI conclusions, not merely that a file exists at some SHA. Supplementary: a separate product-observing
  command reading #3995's raw merge status (gh api repos/OmniNode-ai/onex_change_control/pulls/3995 --jq
  .merged) correctly reports false, since #3995 remained unmerged at verification time, and correctly
  reports true against a merged control (onex_change_control#3994) using the identical command form —
  proving both directions of discrimination. That command is recorded here rather than as its own dod_evidence
  entry: binding a dod_evidence item to "the PR this companion unblocks is already merged" can never carry
  a PASS receipt before that merge happens, which drove occ-preflight/eligibility (validator_occ_merge_eligibility.py
  line 279, requiring EnumReceiptStatus.PASS on every bound receipt with no exemption) to a real, executed
  FAIL on this exact contract in an earlier revision of this companion — a genuine deadlock, not a hypothetical,
  since this companion must itself pass that same required gate to land and unblock #3995.
branch: "jonah/omn-14428-occ-behavioral-evidence"
contract_sha256: "sha256:d9247026c854cb83e6025068e1c04c5751d8b479a3a778c6d1c3acef765b0d73"
contract_entry_sha256: "sha256:99c138889f95366662a694c5fdd480dd2e1768f3f3e1e9594263752dee884af3"
