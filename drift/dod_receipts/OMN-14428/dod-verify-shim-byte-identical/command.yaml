---
schema_version: "1.0.0"
ticket_id: "OMN-14428"
evidence_item_id: "dod-verify-shim-byte-identical"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-14428/dod-verify-shim-byte-identical/command.yaml
  && grep -q 'Total dod_evidence entries checked: 27699' drift/dod_receipts/OMN-14428/dod-verify-shim-byte-identical/command.yaml
  && grep -q 'Mismatches: 0' drift/dod_receipts/OMN-14428/dod-verify-shim-byte-identical/command.yaml"
status: PASS
run_timestamp: "2026-07-12T03:13:09Z"
commit_sha: "b60544ffe09918055337d4b6248cbe366458897d"
runner: "fix-occ-pin"
verifier: "verify-occ-pin"
pr_number: 3995
probe_command: >-
  env -u PYTHONPATH uv run python compare_shim.py  # reimplements the exact deleted shim function from
  the #3995 diff and diffs it against omnibase_core.validation.validator_receipt_gate.compute_contract_entry_sha256
  over every dod_evidence entry in every contracts/*.yaml file on #3995's branch, run from the pinned
  0.46.7 venv (confirmed omnibase_core.__file__ resolves to site-packages, not a source checkout)
probe_stdout: |
  Total dod_evidence entries checked: 27699
  Mismatches: 0
  Errors: 0
actual_output: >-
  PASS: reimplemented the exact try/except fallback compute_contract_entry_sha256 deleted by #3995 (copied
  verbatim from the pre-#3995 diff, including its header={ticket_id, schema_version} + json.dumps(sort_keys=True,
  separators=(",", ":")) canonicalization) and ran it side by side with core's real function from the
  pinned 0.46.7 wheel, over every id in every dod_evidence list across every contracts/*.yaml file on
  #3995's branch: 27699 entries, 0 mismatches, 0 errors. The implementer's PR body claims 27696 — the
  3-entry delta is explained by ordinary corpus growth (contracts/*.yaml receiving new dod_evidence appends)
  between the implementer's run and this one, not a discrepancy in the claim. The shim never diverged
  from core's canonicalization at any point covered by the current corpus, so deleting it does not silently
  change the hash bound by any existing receipt.
exit_code: 0
branch: "jonah/omn-14428-occ-behavioral-evidence"
contract_sha256: "sha256:e4d61abcf399ce928891eb2e20bbdc99b2470d30c8f086b4714666ef036c74c4"
contract_entry_sha256: "sha256:c1e747d83aa2203e55212e5381c27792baf1a7527adfda2c4037d5a8313ff79b"
