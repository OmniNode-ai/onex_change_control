---
schema_version: 1.0.0
ticket_id: OMN-15283
evidence_item_id: dod-omn15283-catalog-parity-at-2e2108f4
check_type: command
check_value: 'C=$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/topics/managed_staging_topic_checker.py?ref=2e2108f499349c59f851d465185ae90cd08c7980"
  --jq .content | base64 -d) && Y=$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/topics/managed_staging_canary_catalog_namespace.yaml?ref=2e2108f499349c59f851d465185ae90cd08c7980"
  --jq .content | base64 -d) && printf ''%s'' "$C" | python3 -c ''import ast,sys; t=ast.parse(sys.stdin.read());
  names={n.id for n in ast.walk(t) if isinstance(n,ast.Name)}; assert "build_canary_catalog_from_candidate"
  in names, "checker does not build the catalog via build_canary_catalog_from_candidate"; assert "load_canary_namespace"
  in names, "checker does not load the namespace YAML via load_canary_namespace"; doc={id(n.value) for
  n in ast.walk(t) if isinstance(n,ast.Expr) and isinstance(n.value,ast.Constant)}; bad=[n.value for n
  in ast.walk(t) if isinstance(n,ast.Constant) and isinstance(n.value,str) and id(n) not in doc and n.value.startswith("onex.")];
  assert not bad, "checker hardcodes catalog/topic literals instead of deriving them: %r" % (bad,)'' &&
  printf ''%s\n'' "$Y" | grep -q ''^epoch: "mstg1"$'' && printf ''%s\n'' "$Y" | grep -q ''^topic_prefix:
  "onex.mstg1."$'' && printf ''%s\n'' "$Y" | grep -q ''^group_prefix: "onex.mstg1."$'''
contract_entry_sha256: sha256:e243b6619205b74d35a8b6a923418b19556f4d59ffa3e4f71d7d7960a4e6dbb2
status: PASS
run_timestamp: '2026-07-28T10:01:21Z'
commit_sha: 7a02d6b8bb61d52352aae534db32cd4d6c9fea91
branch: jonah/omn-15283-occ-content-bound
runner: fable-overnight-0728-omn15283-occ-content-bound
verifier: fable-overnight-0728-omn15283-occ-content-bound-readback
probe_command: |
  GREEN: the check_value above, executed verbatim as read from the committed contract.
  RED:   the same check_value with the merged squash SHA replaced by the PR merge-base
         d85226172b90197f501f5d71bededf4f249cd607, executed identically.
probe_stdout: |
  GREEN @ merged squash SHA -> exit=0
  RED   @ merge-base d8522617 ->
  gh: Not Found (HTTP 404)
  base64: stdin: (null): error decoding base64 input stream
  exit=1
actual_output: |
  PASS -- at omnibase_infra 2e2108f4 managed_staging_topic_checker.py references both
  build_canary_catalog_from_candidate and load_canary_namespace and declares no non-docstring
  string literal beginning 'onex.', and managed_staging_canary_catalog_namespace.yaml at the same
  SHA declares epoch mstg1 / topic_prefix onex.mstg1. / group_prefix onex.mstg1. The checker
  therefore inherits the catalog from the topic-contract YAML rather than restating it. exit 0.
exit_code: 0
