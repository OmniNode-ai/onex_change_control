---
schema_version: 1.0.0
ticket_id: OMN-14538
evidence_item_id: dod-verify-1735-surface-fail-blocks
check_type: command
check_value: >-
  f="$(mktemp)" && gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_integration_sweep_orchestrator/handlers/handler_integration_sweep_orchestrator.py?ref=e2f4b06abf7bb60f807b07bdba08a314e7bcf65e"
  --jq .content | base64 -d > "$f" && grep -Fq -- 'surface_fail_count: int' "$f" && grep -Fq -- '_SURFACE_NON_SUCCESS_STATUSES
  = frozenset({"fail", "error"})' "$f" && grep -Fq -- 'if entry.get("status") in _SURFACE_NON_SUCCESS_STATUSES'
  "$f" && grep -Fq -- 'stale_count or surface_fail_count' "$f"
contract_sha256: sha256:767682b736298cb1ac51fb311a47583ea3dffa3060b6aecda3120ecc0fd2ccf6
contract_entry_sha256: sha256:ecd4fc96ad70b7906a5e43a9bb32ccce5682adc56cd0c67cfc353b43a921b6de
status: PASS
run_timestamp: '2026-07-13T05:12:02Z'
commit_sha: e2f4b06abf7bb60f807b07bdba08a314e7bcf65e
runner: claude-sonnet-5
verifier: occ-interim-manual-verify
pr_number: 1735
probe_command: >-
  f="$(mktemp)" && gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_integration_sweep_orchestrator/handlers/handler_integration_sweep_orchestrator.py?ref=e2f4b06abf7bb60f807b07bdba08a314e7bcf65e"
  --jq .content | base64 -d > "$f" && grep -Fq -- 'surface_fail_count: int' "$f" && grep -Fq -- '_SURFACE_NON_SUCCESS_STATUSES
  = frozenset({"fail", "error"})' "$f" && grep -Fq -- 'if entry.get("status") in _SURFACE_NON_SUCCESS_STATUSES'
  "$f" && grep -Fq -- 'stale_count or surface_fail_count' "$f" && echo "CHECK1 PASS"
probe_stdout: |
  CHECK1 PASS
exit_code: 0
actual_output: |
  Read handler_integration_sweep_orchestrator.py directly from the omnimarket PR #1735 head via the
  GitHub Contents API (not the local clone) and confirmed the new surface_fail_count parameter, the
  _SURFACE_NON_SUCCESS_STATUSES frozenset, the surface_results scan that populates it, and the
  (stale_count or surface_fail_count) routing condition are all present at the pinned commit.
branch: jonah/omn-14538-occ-companion
