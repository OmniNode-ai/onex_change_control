---
schema_version: "1.0.0"
ticket_id: "OMN-13196"
evidence_item_id: "pr-omnibase_core"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13196/pr-omnibase_core/command.yaml"
status: PASS
run_timestamp: "2026-06-17T14:07:18-00:00"
commit_sha: "a77ef5ecb2e7bb7b7f6761fb7468f5741e9cde23"
contract_sha256: "sha256:c14c39c283212fe1d3c2594030f27f4a3e4702fa8ad138078f8b2bd379cf314b"
branch: "jonah/omn-13196-delete-modelonexenvelope"
pr_number: 2692
runner: "omn-13196-impl"
verifier: "receipt-gate-local"
probe_command: "grep -rniE 'ModelOnexEnvelope|model_onex_envelope' src/ tests/ | wc -l ; gh pr view 1250
  --repo OmniNode-ai/omnibase_core --json number,headRefName,headRefOid,state"
probe_stdout: |
  # grep -rniE 'ModelOnexEnvelope|model_onex_envelope' src/ tests/ | wc -l  ->  0
  #   ZERO ModelOnexEnvelope references remain in omnibase_core src + tests after this phase.
  #   Deleted: src/omnibase_core/models/core/model_onex_envelope.py (the class) + its 2 exports
  #   in models/core/__init__.py; deleted the 4 ModelOnexEnvelope-specific test files
  #   (test_model_onex_envelope.py, test_model_onex_envelope_source_node_id.py,
  #   test_model_onex_envelope_chunking.py, tests/performance/test_source_node_id_overhead.py).
  #   Retargeted tests/unit/validation/test_envelope_validator.py to a frozen Pydantic stub
  #   (_ValidatorEnvelope) since EnvelopeValidator is duck-typed and ModelEventEnvelope[T] lacks
  #   the dispatch-routing fields. Retargeted stale docstring/comment refs in
  #   model_envelope_metadata.py / model_chunked_envelope.py / type_serializable_value.py and the
  #   mixin_metadata.yaml `requires` entry to ModelEventEnvelope.
  number=1250
  headRefName=jonah/omn-13196-delete-modelonexenvelope
  headRefOid=a77ef5ecb2e7bb7b7f6761fb7468f5741e9cde23
  state=OPEN
actual_output: >-
  PASS: ModelOnexEnvelope is deleted from omnibase_core src + tests (impl PR #1250, head a77ef5ecb2e7bb7b7f6761fb7468f5741e9cde23,
  base dev). grep -rniE 'ModelOnexEnvelope|model_onex_envelope' over src/ + tests/ returns 0 — zero references
  remain. The class file src/omnibase_core/models/core/model_onex_envelope.py and its import + __all__
  export in models/core/__init__.py are removed. Four ModelOnexEnvelope-specific test files are deleted
  because they exercised the class's own fields (source_node_id, chunking, operation) which have no ModelEventEnvelope
  equivalent: tests/unit/models/test_model_onex_envelope.py, tests/unit/models/test_model_onex_envelope_source_node_id.py,
  tests/unit/models/chunking/test_model_onex_envelope_chunking.py, and the perf benchmark tests/performance/test_source_node_id_overhead.py.
  tests/unit/validation/test_envelope_validator.py is retargeted: the surviving EnvelopeValidator reads
  every field via getattr(envelope, ..., None) (duck-typed), and the canonical ModelEventEnvelope[T] does
  not carry the dispatch-routing fields the validator reads (source_node/operation/target_node), so the
  ModelOnexEnvelope fixture is replaced with a minimal frozen Pydantic stub (_ValidatorEnvelope) exposing
  exactly those attributes — 40/40 validator tests pass. Stale docstring/comment references to the deleted
  class in model_envelope_metadata.py, model_chunked_envelope.py, type_serializable_value.py, and the
  mixin_metadata.yaml MixinIntentPublisher `requires` entry (which pointed at the already-wrong models.events.model_onex_envelope
  path) are retargeted to ModelEventEnvelope. Gates green: ruff format + ruff check --fix (All checks
  passed); mypy src/ --strict introduces zero NEW errors (the 3 pre-existing errors in untouched contract_loader.py
  / cli_install.py reproduce on clean origin/dev and are not in this diff); tests/unit/validation/test_envelope_validator.py
  40 passed; FULL pytest tests/ -n 8 (no -k) 41576 passed, 65 skipped, 12 xfailed, 43 xpassed. The 21
  full-suite failures are full-suite-only xdist ordering flakes (RegistryNode forward-ref model_rebuild()
  contamination in tests/unit/navigation/* and DI-singleton contamination in test_protocol_auditor / test_model_action_registry)
  — all 21 PASS in isolation (48/48 serial; the navigation subset also passes 46/46 under its own -n 8
  run on clean dev), and none reference envelopes or any file in this diff. pre-commit run --all-files:
  all hooks pass (DETERMINISTIC_SKILL_ROOT pinned to an omniclaude@main snapshot for CI parity — a known
  sibling-worktree false-positive when pointed at the live clone with incomplete SKILL.md state). Predecessor
  B3 (OMN-13194, PR #1248) is MERGED to dev (b7ed3f0f0845f7ccd26f84473841b8061afb7d98, the base of this
  branch). This receipt is the grep-target consumed by the check_value in contracts/OMN-13196.yaml (contract
  bytes hash to sha256:c14c39c283212fe1d3c2594030f27f4a3e4702fa8ad138078f8b2bd379cf314b).
exit_code: 0
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-13196/onex_change_control"
