---
schema_version: "1.0.0"
ticket_id: "OMN-15669"
evidence_item_id: "dod-omn15669-gate-self-applies"
check_type: "command"
check_value: "uv run check-contract-shape-v1 --changed-files contracts/v1/OMN-15669.yaml"
contract_entry_sha256: "sha256:6c8d2da59ee3c1d76b7efb50537c5c1fca6fae37947a6fd13075ecac05395e1e"
status: FAIL
run_timestamp: "2026-08-02T21:01:29Z"
commit_sha: "f18be44578d4216300d40d3a56df2fd74ac05e34"
runner: "fable-resume-0802/occ-5976-receipts"
verifier: "non-implementer-probe-200-gate-host"
probe_command: |-
  uv run check-contract-shape-v1 --changed-files contracts/v1/OMN-15669.yaml
probe_stdout: |-
  contract shape v1: 1 finding(s) across 1 in-scope contract(s):

    [identity_not_evaluated] contracts/v1/OMN-15669.yaml: the P2 ticket-identity leg did not run: no PR body was supplied and no caller declared the exclusion. Pass --pr-body-file to prove three-way identity, or --skip-identity to state on the record that this invocation is a strict subset of the CI gate. A silent subset is how a DoD check ends up weaker than the gate it certifies

  This gate has no reviewer and no allowlist. Fix the shape (schemas/occ_contract_v1.schema.yaml is the only authority).

  exit=1
actual_output: |-
  FAIL, exit 1. This receipt records the true live state and deliberately does NOT
  claim green. The check_value declared by contracts/OMN-15669.yaml (the file the OCC
  eligibility gate resolves) is not satisfiable as written outside a pull_request
  context: it passes neither --pr-body-file nor --skip-identity, so the gate's own P2
  identity leg reports identity_not_evaluated and exits 1 by design. The defect is in
  the declared check_value, not in the gate and not in the contract's shape.

  Discriminating control run in the same session, same host, same sha: the SAME binary
  with the exclusion declared --
    $ uv run check-contract-shape-v1 --skip-identity --changed-files contracts/v1/OMN-15669.yaml
    contract shape v1 OK: 1 in-scope contract(s) conform
    exit=0
  -- returns zero findings. That is the form contracts/v1/OMN-15669.yaml declares for
  the same evidence item, and the form the CI job runs (with --pr-body-file supplied,
  .github/workflows/ci.yml:1760-1847). So the shape leg itself is GREEN; only the
  root-contract check_value string is wrong. Repairing it is an implementer edit to
  contracts/OMN-15669.yaml (which re-mints this entry's contract_entry_sha256 and
  therefore this receipt) and is out of scope for a non-implementer evidence author.

  Consequence, stated plainly so it is not mistaken for closure: occ-preflight moves
  from reason=missing_receipt to reason=nonpass_receipt on this one key; the PR is
  still not eligible, and must not be. Re-executed live by a NON-IMPLEMENTER lane
  (feedback_no_self_authored_evidence) on the .200 gate host (stickybeatz-studio via
  MagicDNS; LAN 192.168.86.200 unroutable from this Mac), worktree
  OMN-15669-receipts, HEAD f18be44578d4216300d40d3a56df2fd74ac05e34.
exit_code: 1
pr_number: 5976
branch: "jonah/omn-15669-canonical-contract-shape-v1"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-15669-receipts/onex_change_control"
