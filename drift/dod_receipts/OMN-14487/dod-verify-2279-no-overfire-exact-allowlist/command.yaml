---
schema_version: "1.0.0"
ticket_id: "OMN-14487"
evidence_item_id: "dod-verify-2279-no-overfire-exact-allowlist"
check_type: "command"
check_value: >-
  hw="$(mktemp)" && gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/runtime/auto_wiring/handler_wiring.py?ref=f973c737d1383f31bf3e6146a0ad893801230696"
  --jq .content | base64 -d > "$hw" && grep -F -- 'str(key).endswith(("_json", "_jsonb"))' "$hw" && grep
  -F -- 'str(key) in _JSONB_LIST_COLUMNS' "$hw" && grep -F -- 'models_used' "$hw"
status: PASS
run_timestamp: "2026-07-13T00:05:00Z"
commit_sha: "0000000"
runner: "claude-opus-4-8"
verifier: "occ-interim-manual-verify"
pr_number: 2279
probe_command: >-
  cd omni_worktrees/OMN-14487-jsonb-verify/omnibase_infra && env -u PYTHONPATH uv run --frozen pytest
  -k text_array_list_is_not_serialized
probe_stdout: |
  handler_wiring.py @ f973c737 wraps a list in Json ONLY when key endswith(('_json','_jsonb')) OR key in
  _JSONB_LIST_COLUMNS (exact-name allowlist). A genuine text[] column (swarm_runs.models_used) is preserved.
  Verifier: text[]-preservation test passes (1 passed); adding recent_responses cannot affect a differently-named column.
exit_code: 0
actual_output: >-
  PASS: the adapter uses an exact-name allowlist + _json/_jsonb suffix, so the fix is surgically scoped
  and does not over-fire on genuine text[] columns (models_used preserved; text[]-preservation test passes).
branch: "jonah/omn-14487-occ-infra-2279-jsonb-adapt"
contract_entry_sha256: "sha256:fab8d2fb34b0a4f012e65f26decc43d120fa3826c351864197729d35a95bce56"
