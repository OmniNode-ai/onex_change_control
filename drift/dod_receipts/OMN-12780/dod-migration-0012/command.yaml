---
schema_version: "1.0.0"
ticket_id: "OMN-12780"
evidence_item_id: "dod-migration-0012"
check_type: "command"
check_value: "cat src/omnimarket/nodes/node_projection_delegation/migrations/0012_generation_output_columns.sql"
status: PASS
run_timestamp: "2026-06-07T21:00:00Z"
commit_sha: "eb743e9858ce13a1085c196cf3aa901c7df10da1"
branch: "jonah/omn-12780-persist-expose-output"
runner: "codex"
verifier: "jonah"
probe_command: "cat src/omnimarket/nodes/node_projection_delegation/migrations/0012_generation_output_columns.sql"
probe_stdout: |
  -- OMN-12780 (Wave 1C): persist the generated output from node-generation-completed events.
  --
  -- The terminal event model (node_generation_consumer/models/model_generation.py)
  -- carries contract_yaml and handler_source but migration 0008 did not include them,
  -- so _project_generation_completed was silently dropping the output.
  --
  -- Provenance captured per-row for evidence packets:
  --   - migration id: 0012
  --   - source commit: tracked by git history on this file
  --   - applied lane: recorded by the migration runner (dev/stability-test/prod)
  -- SHA256 of each generated output is computed at query time via:
  --   encode(sha256(contract_yaml::bytea), 'hex') and
  --   encode(sha256(handler_source::bytea), 'hex')
  -- so large fields are verifiable without visual render (rev 3.1 acceptance).
  --
  -- Applied only (never reversed); DO NOT apply to live lanes — Wave 3 deploy is user-gated.

  ALTER TABLE generation_events
      ADD COLUMN IF NOT EXISTS contract_yaml TEXT NOT NULL DEFAULT '',
      ADD COLUMN IF NOT EXISTS handler_source TEXT NOT NULL DEFAULT '';
actual_output: >-
  Migration 0012 authored with contract_yaml and handler_source columns. ADD COLUMN IF NOT EXISTS makes
  it idempotent. Cites OMN-12780 in header. Not applied to live lanes — Wave 3 deploy is user-gated.
exit_code: 0
contract_sha256: "sha256:pending-occ-pr"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-12780/omnimarket"
notes: >-
  IF NOT EXISTS confirmed in migration SQL. contract_yaml and handler_source both present. Migration id
  0012 follows the sequence after 0011_delegation_event_projection_versions.sql.
