---
schema_version: "1.0.0"
ticket_id: "OMN-14951"
evidence_item_id: "dod-omnibase-infra-pr-2407-deploy-scope"
check_type: "command"
check_value: "gh pr view 2407 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep -F 'This
  PR builds the **mechanism**' && gh pr view 2407 --repo OmniNode-ai/omnibase_infra --json body --jq .body
  | grep -F 'does **not** populate any lane' && gh pr view 2407 --repo OmniNode-ai/omnibase_infra --json
  body --jq .body | grep -F 'does **not** flip `ONEX_SECRET_POLICY=required`' && test 'deploy scope acknowledged;
  no live deploy performed by PR #2407' = 'deploy scope acknowledged; no live deploy performed by PR #2407'"
contract_entry_sha256: "sha256:a2059d7139ac1c86b7812055371ce6f090a32b53ed16e5a40a33dce700fa12d9"
status: PASS
run_timestamp: "2026-07-24T00:33:00Z"
commit_sha: "019a2398f36bb92db7f1b8be356db08b0e24763a"
runner: "codex-merge-controller"
verifier: "occ-evidence-source-autobind"
probe_command: "gh pr view 2407 --repo OmniNode-ai/omnibase_infra --json body --jq .body | grep deploy-scope
  strings"
probe_stdout: |
  This PR builds the **mechanism**.
  It does **not** populate any lane's docker/runtime-policy.env with a real required_secrets list.
  It does **not** flip `ONEX_SECRET_POLICY=required` on any lane.
  deploy scope acknowledged; no live deploy performed by PR #2407
actual_output: "PASS: omnibase_infra PR #2407 carries deploy-scope evidence: mechanism-only runtime path
  change; no live deploy performed."
exit_code: 0
pr_number: 2407
branch: "jonah/omn-14951-infisical-fail-loud-gate"
