---
schema_version: "1.0.0"
ticket_id: "OMN-16702"
evidence_item_id: "dod-onex-public-tree-on-main-omn16702"
check_type: "command"
check_value: "gh api \"repos/OmniNode-ai/omninode_infra/contents/k8s/onex-public?ref=main\" --jq '.[].name'
  | grep -qx 'omnidash'"
contract_sha256: "sha256:09a4c272d5e65cb2a759f30b2a0e2e67e62a4716d9d7165bbb26b19549e62877"
contract_entry_sha256: "sha256:5eaec5fc9a67aa43c75903b872ec3d0c11532f0f7bd7f873b46a6a48b63b9d08"
status: PASS
run_timestamp: "2026-08-28T21:23:00Z"
commit_sha: "539d64a53f1ebf4ff4661f61dd6c4600da66f031"
runner: "occ7464-grant-prep-lane"
verifier: "occ7464-grant-prep-verifier"
probe_command: "gh api \"repos/OmniNode-ai/omninode_infra/contents/k8s/onex-public?ref=main\" --jq '.[].name'"
probe_stdout: |
  README.md
  api
  kustomization.yaml
  omnidash
  runtime
  umami
actual_output: "PASS: the k8s/onex-public manifest tree exists on omninode_infra `main` — the ONLY branch
  deploy-onex-prod.yml accepts a dispatch from (its 'Enforce main-only production dispatch' step refuses
  any GITHUB_REF that is not refs/heads/main). The omnidash subtree is present, which is the workload
  whose session-store cutover is the entire point of tonight's Step 1. Corroborated locally out of band:
  `git rev-parse origin/main:k8s/onex-public` and `origin/dev:k8s/onex-public` both return tree SHA 04c2fe17e6a7478271e1358b78ee9afda6aa544b,
  so the tree the grant authorizes an apply of is byte-identical on both branches and the dispatch cannot
  silently apply a different tree than the one reviewed. The check reads the GitHub contents API rather
  than a filesystem path, so it is hosted-runnable without a second checkout and cannot resolve against
  the wrong tree the way an ${OMNINODE_INFRA}-rooted command did on OCC#7418. It exits non-zero on any
  ref where the directory is absent."
exit_code: 0
pr_number: 7464
branch: "hotfix/omn-16702-onex-public-apply-grant"
