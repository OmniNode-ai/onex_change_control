---
schema_version: "1.0.0"
ticket_id: "OMN-14467"
evidence_item_id: "dod-2271-shared-failclosed-util-exists"
check_type: "command"
check_value: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/utils/util_producer_effect_assertion.py?ref=f304e13b64e011ee3ae081349178c4f7fd4f99a3"
  --jq .content 2>/dev/null | base64 -d 2>/dev/null | grep -cE 'class ProducerZeroOutputError|def require_producer_preconditions|def
  assert_producer_emitted')" -ge 3
status: PASS
run_timestamp: "2026-07-12T18:31:00Z"
commit_sha: "f304e13b64e011ee3ae081349178c4f7fd4f99a3"
runner: "occ-queue-runner"
verifier: "rt6-builder"
pr_number: 2271
probe_command: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/utils/util_producer_effect_assertion.py?ref=f304e13b64e011ee3ae081349178c4f7fd4f99a3"
  --jq .content 2>/dev/null | base64 -d 2>/dev/null | grep -cE 'class ProducerZeroOutputError|def require_producer_preconditions|def
  assert_producer_emitted')" -ge 3   # then re-run VERBATIM at omnibase_infra dev (e811995d), where the
  file does not exist, and at a bogus SHA
probe_stdout: |
  # bound check_value exit codes, run verbatim (verifier, 2026-07-12):
  #   ref=f304e13b (omnibase_infra#2271 head) -> exit 0   GREEN  (3 defining lines present)
  #   ref=e811995d (infra dev)                 -> exit 1   RED    <- file absent pre-#2271 -> count 0
  #   ref=000...0  (bogus SHA)                  -> exit 1   RED    <- not vacuous (positive >=3 check)
exit_code: 0
actual_output: >-
  PASS: the shared, pure fail-closed effect-assertion util src/omnibase_infra/utils/util_producer_effect_assertion.py
  exists at #2271's head and defines all three primitives the invariant is built on -- class ProducerZeroOutputError,
  def require_producer_preconditions, def assert_producer_emitted. The module is pure (no I/O, no env
  reads, no topic literals): callers resolve their own preconditions/emit counts and translate ProducerZeroOutputError
  into a non-zero exit at their boundary, so the deploy trigger, publish step, pin cascade, and OCC publisher
  can share one "producing zero fails closed" invariant. RED PROVEN at omnibase_infra dev (e811995d):
  the file does NOT exist there, so the gh-api content read returns nothing -> grep count 0 -> exit 1.
  Exit 1 at a bogus SHA. A positive >=3 presence check, so it cannot pass vacuously. Workspace-INDEPENDENT
  content read.
branch: "jonah/omn-14467-occ-companion"
contract_entry_sha256: "sha256:1501589eea209fc41f99488e7a6c9550a1e657a20df472812f61715416e51247"
