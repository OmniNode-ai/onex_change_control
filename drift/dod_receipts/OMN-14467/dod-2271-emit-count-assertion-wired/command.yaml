---
schema_version: "1.0.0"
ticket_id: "OMN-14467"
evidence_item_id: "dod-2271-emit-count-assertion-wired"
check_type: "command"
check_value: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/scripts/trigger_rebuild_on_merge.py?ref=f304e13b64e011ee3ae081349178c4f7fd4f99a3"
  --jq .content 2>/dev/null | base64 -d 2>/dev/null | grep -c 'assert_producer_emitted')" -ge 1
status: PASS
run_timestamp: "2026-07-12T18:31:00Z"
commit_sha: "f304e13b64e011ee3ae081349178c4f7fd4f99a3"
runner: "occ-queue-runner"
verifier: "rt6-builder"
pr_number: 2271
probe_command: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/scripts/trigger_rebuild_on_merge.py?ref=f304e13b64e011ee3ae081349178c4f7fd4f99a3"
  --jq .content 2>/dev/null | base64 -d 2>/dev/null | grep -c 'assert_producer_emitted')" -ge 1 # then
  re-run VERBATIM at omnibase_infra dev (e811995d) and at a bogus SHA
probe_stdout: |
  # bound check_value exit codes, run verbatim (verifier, 2026-07-12):
  #   ref=f304e13b (omnibase_infra#2271 head) -> exit 0   GREEN
  #   ref=e811995d (infra dev)                 -> exit 1   RED    <- EXISTS but WRONG (symbol absent pre-#2271)
  #   ref=000...0  (bogus SHA)                  -> exit 1   RED    <- not vacuous (positive >=1 check)
exit_code: 0
actual_output: >-
  PASS: at #2271's head trigger_rebuild_on_merge.py calls assert_producer_emitted after the publish, so
  a completed publish that delivered zero commands fails closed instead of reporting success -- "produced
  N>0, and here it is" is the only completion signal for a producer. publish_redeploy_start_event now
  returns the delivered count (1 on success) and main() asserts it. RED PROVEN at omnibase_infra dev (e811995d):
  the file EXISTS but never calls assert_producer_emitted (the symbol does not exist pre-#2271) -> grep
  count 0 -> exit 1. Exit 1 at a bogus SHA. A positive presence check (>=1), so it cannot pass on empty/absent
  content -- not vacuous. gh-api content read, workspace-INDEPENDENT.
branch: "jonah/omn-14467-occ-companion"
contract_entry_sha256: "sha256:a96bb143db68da7833cead43091df67c026c7fb2dfcbd77b4ef0b0c8813a9ba2"
