---
schema_version: "1.0.0"
ticket_id: "OMN-14467"
evidence_item_id: "dod-2271-acceptance-policy-tests-exist"
check_type: "command"
check_value: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/tests/scripts/test_trigger_producer_effect_assertion.py?ref=f304e13b64e011ee3ae081349178c4f7fd4f99a3"
  --jq .content 2>/dev/null | base64 -d 2>/dev/null | grep -cE 'def test_runtime_change_with_broker_unset_fails_closed|def
  test_producer_has_no_silent_skip_path')" -ge 2
status: PASS
run_timestamp: "2026-07-12T18:31:00Z"
commit_sha: "f304e13b64e011ee3ae081349178c4f7fd4f99a3"
runner: "occ-queue-runner"
verifier: "rt6-builder"
pr_number: 2271
probe_command: >-
  test "$(gh api "repos/OmniNode-ai/omnibase_infra/contents/tests/scripts/test_trigger_producer_effect_assertion.py?ref=f304e13b64e011ee3ae081349178c4f7fd4f99a3"
  --jq .content 2>/dev/null | base64 -d 2>/dev/null | grep -cE 'def test_runtime_change_with_broker_unset_fails_closed|def
  test_producer_has_no_silent_skip_path')" -ge 2   # then re-run VERBATIM at omnibase_infra dev (e811995d),
  where the file does not exist, and at a bogus SHA
probe_stdout: |
  # bound check_value exit codes, run verbatim (verifier, 2026-07-12):
  #   ref=f304e13b (omnibase_infra#2271 head) -> exit 0   GREEN  (both test defs present)
  #   ref=e811995d (infra dev)                 -> exit 1   RED    <- test file absent pre-#2271 -> count 0
  #   ref=000...0  (bogus SHA)                  -> exit 1   RED    <- not vacuous (positive >=2 check)
exit_code: 0
actual_output: >-
  PASS: tests/scripts/test_trigger_producer_effect_assertion.py at #2271's head carries BOTH the RED-on-zero
  acceptance test (test_runtime_change_with_broker_unset_fails_closed -- the exists-but-WRONG regression:
  producer ran, emitted zero, must exit 1) and the Rule-5 anti-regression policy gate (test_producer_has_no_silent_skip_path
  -- no producer may reintroduce a print-then-exit-0 silent skip). The verifier ran these tests: 25 pass
  across the RT-5 test files. RED PROVEN at omnibase_infra dev (e811995d): the test file does NOT exist
  there -> grep count 0 -> exit 1. Exit 1 at a bogus SHA. SCOPE CAVEAT (does not weaken this receipt;
  tracked as a separate follow-up ticket): these tests live under tests/scripts/, which the change-aware
  selector (detect_test_paths.py; scripts reverse_deps is empty and tests/scripts is not in the unit self-select
  bucket) does not select for scripts/ changes, and no dedicated paths-scoped workflow runs them -- so
  they execute at the dev->main full-suite boundary but not on the dev PR. This receipt proves the acceptance
  + anti-regression tests LANDED and encode the correct invariants, NOT that they are enforced in-PR on
  the dev path. Workspace-INDEPENDENT content read.
branch: "jonah/omn-14467-occ-companion"
contract_entry_sha256: "sha256:c29108b2d33f3a89bb0e1445bc86099ff6976072e6623af481d8f59f8f44c0dc"
