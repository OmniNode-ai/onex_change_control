---
schema_version: "1.0.0"
ticket_id: "OMN-16130"
evidence_item_id: "dod-security-scan-concurrency-and-inventory-fix"
check_type: "command"
check_value: >-
  scan_body="$(cat .github/workflows/security-scan.yml)" && printf '%s' "$scan_body" | grep -qF 'cancel-in-progress:
  true' && inv_body="$(cat src/onex_change_control/boundaries/migration_inventory.yaml)" && printf '%s'
  "$inv_body" | grep -A1 -F '20260815_gateway_sessions_terminal_states.sql' | grep -qF 'tables: [gateway_session_events,
  gateway_sessions, migrations_log]' && printf '%s' "$inv_body" | grep -A1 -F '20260816_tenant_event_outbox.sql'
  | grep -qF 'tables: [tenant_event_outbox]'
status: PASS
run_timestamp: "2026-08-17T14:52:02Z"
commit_sha: "39860c63391d454cb3e61a46c216fef444a1481a"
runner: "grant-6599-drive"
verifier: "team-lead"
probe_command: >-
  grep -n 'cancel-in-progress: true' .github/workflows/security-scan.yml; grep -n '20260815_gateway_sessions_terminal_states.sql\|20260816_tenant_event_outbox.sql\|tables:
  \[gateway_session_events\|tables: \[tenant_event_outbox\]' src/onex_change_control/boundaries/migration_inventory.yaml
probe_stdout: |
  25:  cancel-in-progress: true
  577:          - file: "20260815_gateway_sessions_terminal_states.sql"
  578:            tables: [gateway_session_events, gateway_sessions, migrations_log]
  579:          - file: "20260816_tenant_event_outbox.sql"
  580:            tables: [tenant_event_outbox]
exit_code: 0
actual_output: >-
  PASS: security-scan.yml carries the concurrency group's cancel-in-progress: true line, and migration_inventory.yaml
  lists both 20260815_gateway_sessions_terminal_states.sql and 20260816_tenant_event_outbox.sql with the
  correct tables: lines. Red->green proof: the check_value command run standalone against the pre-fix
  worktree state (concurrency block absent, both inventory entries absent) exits non-zero, and exits 0
  against this fixed state. check-migration-inventory --repos-root $OMNI_HOME also exits 0 against this
  state (only the pre-existing, unrelated omnidash-archived degraded-validation WARNING remains).
pr_number: 6614
branch: "hotfix/omn-16130-security-scan-inventory-fix"
contract_entry_sha256: "sha256:55cebfd2a1f52770c6085430b1b94fffd2150e4a1008678053eebd4f4b859480"
