---
schema_version: "1.0.0"
ticket_id: "OMN-14494"
evidence_item_id: "dod-omnimarket-pr-1767-product-head"
check_type: "command"
check_value: >-
  PRODUCT_PR_NUMBER=1767; PRODUCT_REPO=OmniNode-ai/omnimarket; PRODUCT_HEAD=8f8c8225f32823edbc6e686adf50a87011c87f73;
  d="$(mktemp -d)" && gh pr view "$PRODUCT_PR_NUMBER" --repo "$PRODUCT_REPO" --json headRefOid,baseRefName,files,title
  --jq '.headRefOid + "\n" + .baseRefName + "\n" + .title + "\n" + ((.files | map(.path)) | join("\n"))'
  > "$d/pr.txt" && grep -Fq "$PRODUCT_HEAD" "$d/pr.txt" && grep -Fxq dev "$d/pr.txt" && grep -Fq 'fix(OMN-14494):
  declare dlq_topics on node_projection_delegation_inference_response' "$d/pr.txt" && for p in src/omnimarket/nodes/node_projection_delegation_inference_response/contract.yaml
  tests/nodes/node_projection_delegation_inference_response/test_node_projection_delegation_inference_response_contract.py;
  do grep -Fq "$p" "$d/pr.txt" && mkdir -p "$d/$(dirname "$p")" && gh api "repos/${PRODUCT_REPO}/contents/${p}?ref=${PRODUCT_HEAD}"
  --jq .content | base64 --decode > "$d/$p"; done && grep -Fq 'dlq_topics:' "$d/src/omnimarket/nodes/node_projection_delegation_inference_response/contract.yaml"
  && grep -Fq 'onex.dlq.omnimarket.projection-delegation-inference-response-malformed.v1' "$d/src/omnimarket/nodes/node_projection_delegation_inference_response/contract.yaml"
  && grep -Fq 'test_contract_declares_dlq_topic' "$d/tests/nodes/node_projection_delegation_inference_response/test_node_projection_delegation_inference_response_contract.py"
  && grep -Fq 'test_dlq_topic_follows_onex_dlq_naming_convention' "$d/tests/nodes/node_projection_delegation_inference_response/test_node_projection_delegation_inference_response_contract.py"
contract_entry_sha256: "sha256:a429ec5083d3fc8ea920ae0d560dea8816f81e1ec5859c3ec258264763672672"
status: PASS
run_timestamp: "2026-07-15T03:50:00Z"
commit_sha: "8f8c8225f32823edbc6e686adf50a87011c87f73"
runner: "codex-merge-controller"
verifier: "codex-product-pr-state"
pr_number: 1767
probe_command: "gh pr view 1767 --repo OmniNode-ai/omnimarket plus content checks for the contract and
  test file."
probe_stdout: |
  omnimarket#1767 is open against dev at 8f8c8225f32823edbc6e686adf50a87011c87f73 and includes the
  node_projection_delegation_inference_response contract/test files with the expected DLQ topic declaration
  and naming-convention tests.
actual_output: "PASS: market product head, file surface, and DLQ markers match OMN-14494 expectations."
exit_code: 0
branch: "jonah/omn-14494-delegation-inference-response-dlq-topic"
