---
schema_version: "1.0.0"
ticket_id: "OMN-18078"
evidence_item_id: "dod-check-execution-history-behavior"
check_type: "test_passes"
check_value: |-
  bash -euo pipefail <<'CHECK'
  expected_sha="f5c88d78dd77291100dcc3b73405d255532cb60d"
  actual_sha="$(gh api "repos/OmniNode-ai/omnimarket/commits/$expected_sha" --jq .sha)"
  test "$actual_sha" = "$expected_sha"
  target_root="$(mktemp -d "${TMPDIR:-/tmp}/omn-18078-proof.XXXXXX")"
  target="$target_root/product"
  cleanup() {
    if test -e "$target/.git"; then
      git worktree remove --force "$target"
    fi
    rmdir "$target_root"
  }
  trap cleanup EXIT HUP INT TERM
  git worktree add --detach "$target" "$actual_sha"
  (
    cd "$target"
    test "$(git rev-parse HEAD)" = "$actual_sha"
    env -u PYTHONPATH -u VIRTUAL_ENV UV_CACHE_DIR=/tmp/omn-18078-occ-uv-cache uv run --locked pytest tests/test_golden_chain_pr_lifecycle_inventory_compute.py -q
  )
  CHECK
contract_entry_sha256: "sha256:63887b53a6bb5aeba076504761b35978aaade330aa9b511ff50193674c438d46"
status: "PASS"
run_timestamp: "2026-09-09T09:29:44Z"
commit_sha: "f5c88d78dd77291100dcc3b73405d255532cb60d"
runner: "codex-omn-18078-detached-remote-proof"
verifier: "remote-bound-local-detached-focused-test"
probe_command: |-
  bash -euo pipefail <<'CHECK'
  expected_sha="f5c88d78dd77291100dcc3b73405d255532cb60d"
  actual_sha="$(gh api "repos/OmniNode-ai/omnimarket/commits/$expected_sha" --jq .sha)"
  test "$actual_sha" = "$expected_sha"
  target_root="$(mktemp -d "${TMPDIR:-/tmp}/omn-18078-proof.XXXXXX")"
  target="$target_root/product"
  cleanup() {
    if test -e "$target/.git"; then
      git worktree remove --force "$target"
    fi
    rmdir "$target_root"
  }
  trap cleanup EXIT HUP INT TERM
  git worktree add --detach "$target" "$actual_sha"
  (
    cd "$target"
    test "$(git rev-parse HEAD)" = "$actual_sha"
    env -u PYTHONPATH -u VIRTUAL_ENV UV_CACHE_DIR=/tmp/omn-18078-occ-uv-cache uv run --locked pytest tests/test_golden_chain_pr_lifecycle_inventory_compute.py -q
  )
  CHECK
probe_stdout: |-
  Preparing worktree (detached HEAD f5c88d78)
  HEAD is now at f5c88d78 feat(OMN-18078): record immutable CI check executions
  ........................................................................ [ 94%]
  ....                                                                     [100%]
  76 passed in 2.28s
actual_output: "PASS: the remote-bound detached checkout at f5c88d78 passed all 76 focused lifecycle inventory
  tests."
exit_code: 0
duration_ms: 110000
pr_number: 2428
branch: "jonah/omn-18078-record-ci-check-executions"
working_dir: null
