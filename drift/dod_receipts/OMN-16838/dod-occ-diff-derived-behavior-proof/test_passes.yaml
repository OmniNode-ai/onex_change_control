---
schema_version: "1.0.0"
ticket_id: "OMN-16838"
evidence_item_id: "dod-occ-diff-derived-behavior-proof"
check_type: "test_passes"
check_value: |-
  uv run pytest tests/k8s/test_deploy_onex_prod_grant_gate.py tests/k8s/test_omniweb_public_surface_drift_report.py tests/scripts/test_plan_omniweb_public_promotion.py -q
contract_sha256: "sha256:9e13d6b122f8f960f76a73995be3375473d5be6a6c5c49e670cfdfd190158520"
contract_entry_sha256: "sha256:316a670a45ee68613a45317c888d07fa9bdc357c63b1092b30b6e9dee2bd9e32"
status: PASS
run_timestamp: "2026-08-28T12:37:26+00:00"
commit_sha: "7170da05da9434de0ce94954a7fa95b47b542896"
runner: "human-operated pytest run in the OMN-16838 worktree"
verifier: "omn16838-option-a lane"
probe_command: |-
  # The checked-out product repo for this run is OmniNode-ai/omninode_infra, NOT
  # onex_change_control, so commit_sha below resolves there:
  #   gh api repos/OmniNode-ai/omninode_infra/commits/7170da05da9434de0ce94954a7fa95b47b542896
  uv run pytest tests/k8s/test_deploy_onex_prod_grant_gate.py tests/k8s/test_omniweb_public_surface_drift_report.py tests/scripts/test_plan_omniweb_public_promotion.py -q
probe_stdout: |
  ...........................................                              [100%]
  43 passed in 0.62s
actual_output: |
  PASS: 43 passed, 0 failed, exit 0. The declared behaviour proof was executed
  at the PR head commit 7170da05da9434de0ce94954a7fa95b47b542896, and its exit
  status was captured explicitly rather than read off a pipeline (a piped tail
  reports the tail's status, not pytest's).

  Coverage of the three suites, all RED before the implementation landed:
    * tests/scripts/test_plan_omniweb_public_promotion.py (18) — the promotion
      planner: digest-addressed pin required, public hostnames read from the
      Ingress, grant refusal, self-approval refusal, UNKNOWN-health fail-closed,
      live-digest no-change branch, and the CLI exit codes. RED before the
      module existed (ModuleNotFoundError).
    * tests/k8s/test_deploy_onex_prod_grant_gate.py (17) — the OMN-14209 gate
      wiring plus the new OMN-16838 omniweb gate: present, unconditional,
      ordered before every apply, probe-sourced inputs, CROSS_REPO_PAT wired,
      `-m` invocation form. 8/8 new assertions RED before the workflow edit.
    * tests/k8s/test_omniweb_public_surface_drift_report.py (8) — the drift
      report job: depends on the push job, names the pin file and the public
      hostnames, writes the step summary, contains no cluster-mutating verb,
      and does not fail the build. 8/8 RED before the workflow edit.

  Why this receipt was authored by hand rather than minted by the autobind.

  contracts/OMN-16838.yaml declares this evidence item with
  `check_type: "test_passes"`, but node_occ_companion_compute emitted only
  `dod-occ-diff-derived-behavior-proof/command.yaml`. The eligibility validator
  keys receipts as `<ticket>:<evidence_item>:<check_type>` and reads
  `<evidence_item>/<check_type>.yaml`, so it looked for
  `OMN-16838:dod-occ-diff-derived-behavior-proof:test_passes`, found only the
  `:command` sibling, and returned reason=missing_receipt (omninode_infra#1040
  run 33171453109). The autobind receipt's own `actual_output` says the product
  test run "executes the declared check" while its probe is a `gh pr view` —
  i.e. it asserts the tests pass without having run them.

  This file supplies the declared check actually executed, with its real stdout
  and its real exit status. Both hashes were recomputed independently rather
  than copied from the sibling receipt:
  `compute_contract_entry_sha256(contract, "dod-occ-diff-derived-behavior-proof")`
  from omnibase_core.validation.validator_receipt_gate returns
  sha256:316a670a45ee68613a45317c888d07fa9bdc357c63b1092b30b6e9dee2bd9e32, and
  sha256 of the contract file returns
  sha256:9e13d6b122f8f960f76a73995be3375473d5be6a6c5c49e670cfdfd190158520.
  The entry hash is per evidence item, not per check type, so it is identical
  across the `command` and `test_passes` receipts for this item by construction.

  The `command` sibling is left in place: nothing here supersedes it, and
  deleting an autobind artefact to make a gate pass would be the wrong move.

  The autobind check_type mismatch is a defect in the companion generator, not
  in this ticket's work. It is recorded on OMN-16838 so it can be ticketed
  against the generator rather than re-patched by hand on the next PR.
exit_code: 0
pr_number: 1040
branch: "auto/omninode-ai-omninode_infra-pr-1040-occ-autobind"
