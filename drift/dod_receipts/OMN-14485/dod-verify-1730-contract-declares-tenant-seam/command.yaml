---
schema_version: "1.0.0"
ticket_id: "OMN-14485"
evidence_item_id: "dod-verify-1730-contract-declares-tenant-seam"
check_type: "command"
check_value: >-
  t="$(mktemp)" && gh api "repos/OmniNode-ai/omnimarket/contents/src/omnimarket/nodes/node_delegate_skill_orchestrator/contract.yaml?ref=bf6290eb3c06686c6a4071ac67e95eaacd250fcb"
  --jq .content | base64 -d > "$t" && grep -F -- 'declare the tenant seam on the RESPONSE/terminal side'
  "$t" && grep -F -- 'delegation_events.tenant_id' "$t"
status: PASS
run_timestamp: "2026-07-12T23:20:00Z"
commit_sha: "0000000"
runner: "claude-opus-4-8"
verifier: "occ-interim-manual-verify"
pr_number: 1730
probe_command: >-
  gh api .../node_delegate_skill_orchestrator/contract.yaml?ref=bf6290eb | base64 -d | grep -nE 'outputs:|tenant_id'
probe_stdout: |
  contract.yaml @ bf6290eb declares the response/terminal seam: an 'outputs:' block with tenant_id (line 73-74),
  plus the request input tenant_id (line 126) and terminal_event delegate-skill-completed.v1 — 4-of-4 by contract.
  Head moved 6128bbf8 -> bf6290eb (docs commit; only contract.yaml changed; code + test unchanged, suite still 4 passed).
exit_code: 0
actual_output: >-
  PASS: the node contract now declares tenant_id on the RESPONSE/terminal side (outputs.tenant_id), not
  only the request inputs; the tenant seam is 4-of-4 by contract and cannot be silently dropped by a regeneration.
branch: "jonah/omn-14485-occ-omnimarket-1730-tenant-carry"
contract_entry_sha256: "sha256:ce28b51aa0ea6463a2203271b5c3e6ba0e3db841bf8567e6353f8c71bad3f0d6"
