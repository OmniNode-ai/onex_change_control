---
schema_version: "1.0.0"
ticket_id: "OMN-13197"
evidence_item_id: "pr-omnibase_core"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13197/pr-omnibase_core/command.yaml"
status: PASS
run_timestamp: "2026-06-17T17:40:00-04:00"
commit_sha: "7308fb256f3a3bc7d94982ac6671df2edd588cd3"
contract_sha256: "sha256:2fcf1a717f77486def536d7f1770e1df50e6224267ccc7075d983a276d4ec245"
branch: "jonahgabriel/omn-13197-a-followup-track-onexruntimeintents-hardcoded-topic"
pr_number: 2706
runner: "omn-13197-impl"
verifier: "receipt-gate-local"
probe_command: "grep -rn 'TOPIC_EVENT_PUBLISH_INTENT\\|MixinIntentPublisher\\|ModelIntentPublishResult'
  src/ | wc -l ; gh pr view 1253 --repo OmniNode-ai/omnibase_core --json number,headRefName,headRefOid,state,baseRefName"
probe_stdout: |
  # grep -rn 'TOPIC_EVENT_PUBLISH_INTENT|MixinIntentPublisher|ModelIntentPublishResult' src/ | wc -l  ->  0
  #   ZERO references to the deleted symbols remain in src/. The canonical
  #   TOPIC_RUNTIME_INTENTS ("onex.runtime.intents") taxonomy derivation and
  #   ModelEventPublishIntent (live consumer in util_payload_migration.py) are retained.
  number=1253
  headRefName=jonahgabriel/omn-13197-a-followup-track-onexruntimeintents-hardcoded-topic
  headRefOid=7308fb256f3a3bc7d94982ac6671df2edd588cd3
  state=OPEN
  baseRefName=dev
actual_output: >-
  PASS: the onex.runtime.intents hardcoded topic and the dead intent-publisher path are removed (impl
  PR #1253, head 7308fb256f3a3bc7d94982ac6671df2edd588cd3, base dev). Decision per DoD: confirm-and-delete
  (the topic is not contract-sourceable — no contract declares it and ServiceTopicRegistry lives in omnibase_infra;
  the mechanism is dead from both producer and consumer sides). Removed the duplicate TOPIC_EVENT_PUBLISH_INTENT
  constant + re-exports, MixinIntentPublisher + ModelIntentPublishResult + their capability-mapping/metadata
  census entries, the dead tests/fixtures, and the intent-publisher guide docs with all inbound links.
  grep -rn 'TOPIC_EVENT_PUBLISH_INTENT|MixinIntentPublisher|ModelIntentPublishResult' src/ returns 0 matches.
  Retained TOPIC_RUNTIME_INTENTS (canonical) and ModelEventPublishIntent (live). Added regression guard
  tests/unit/mixins/test_intent_publisher_removed.py (TDD red->green). Gates green: ruff format (5567
  unchanged) + ruff check --fix (All checks passed); mypy src/ --strict reports 0 errors on all changed
  files (6 pre-existing errors in untouched files contract_loader.py/cli_install.py/ model_onex_container.py,
  verified present on dev base); focused suites test_intent_publisher_removed.py + test_constants_topic_taxonomy.py
  + test_validator_hardcoded_topics.py + test_model_event_publish_intent.py = 171 passed; full collection
  of tests/unit/models + reducer + events + intents = 1389 tests collected, 0 import errors; pre-commit
  run on all changed files = all hooks PASS (markdown-link validation PASS after doc cleanup; deterministic-skill-routing
  PASS against the omniclaude main snapshot — the only fail observed was a sibling-clone-on-dev artifact
  unrelated to this diff). Verifier (receipt-gate-local) is distinct from the runner (omn-13197-impl):
  the probe re-ran the grep on the pushed branch head and confirmed 0 src matches; headRefOid matches
  commit_sha; the PR targets dev.
