---
schema_version: "1.0.0"
ticket_id: "OMN-14436"
evidence_item_id: "dod-verify-14436-digest-binding-in-source"
check_type: "command"
check_value: >-
  test "$(gh api "repos/OmniNode-ai/onex_change_control/contents/scripts/ci/run_contract_compliance_check.py?ref=7266a4f1b2fff2bf5a362a60bb1f099dd94d1835"
  --jq .content | base64 -d | grep -c 'def _contract_digest')" -ge 1
status: PASS
run_timestamp: "2026-07-12T08:00:00Z"
commit_sha: "7266a4f1b2fff2bf5a362a60bb1f099dd94d1835"
runner: "occ-queue-runner"
verifier: "occ-queue-verifier"
probe_command: >-
  gh api "repos/OmniNode-ai/onex_change_control/contents/scripts/ci/run_contract_compliance_check.py?ref=7266a4f1b2fff2bf5a362a60bb1f099dd94d1835"
  --jq .content | base64 -d | grep -n 'def _contract_digest'   # then the IDENTICAL command at the pre-fix
  parent f6088719 and at a bogus SHA, as RED controls
probe_stdout: |
  # #4022 merge commit 7266a4f1 -- the shipped runner:
  def _contract_digest(contract_path: Path) -> str:
  # RED control -- pre-fix parent f6088719: 0 occurrences (the function does not exist)
  # bound check_value exit codes:
  #   ref=7266a4f1 (merge)  -> exit 0   GREEN
  #   ref=f6088719 (parent) -> exit 1   RED
  #   ref=000...0  (bogus)  -> exit 1   RED
exit_code: 0
actual_output: >-
  PASS: asserts the shipped runner carries _contract_digest -- the function binding the grandfather to
  a contract's CONTENT HASH rather than its ticket id. NOTHING ELSE IN THIS CONTRACT GUARDS THIS PROPERTY:
  dod-ratchet-tests (pytest), dod-strict-mypy and dod-ruff would ALL still pass if the grandfather reverted
  to ticket-keyed. That version is an AMNESTY, not a ratchet -- a fresh circular dod_evidence entry appended
  under an old ticket id would inherit that ticket's exemption forever. A green suite proving the code
  COMPILES while the invariant the code exists to ENFORCE quietly evaporates is precisely the failure
  class OMN-14436 was opened to kill, and this contract had reproduced it. RED PROVEN across three refs:
  exit 0 at the merge commit, exit 1 at its pre-fix parent (symbol absent), exit 1 at a bogus SHA.
branch: "jonah/omn-14436-assert-ratchet-invariant"
contract_sha256: "sha256:2384cf3a7e6736bf0f9726a16a6442df12dba012f2e07a302407145e4d415d55"
contract_entry_sha256: "sha256:053c7d7c574d1d6de39c72b3a5a89adb654d1d26c4d48fa3d4a48d0ee89453a9"
