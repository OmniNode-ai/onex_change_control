---
schema_version: "1.0.0"
ticket_id: "OMN-16237"
evidence_item_id: "dod-OmniNode-ai-omnibase_infra-pr-2802-ci"
check_type: "command"
check_value: |-
  uv run pytest tests/unit/validation/test_application_database_sql_enforcement_regressions.py::test_allowlisted_physically_public_table_passes_unqualified_or_public -q
contract_sha256: "sha256:87ee62f8cb08ed7634c0a904e2f3bd9f432712772bdee133b8d05cadf73b82d7"
contract_entry_sha256: "sha256:715541e8625d37fdad118b254394dba55bf150855593ee772e0c9029593447e2"
status: PASS
run_timestamp: "2026-08-19T06:15:57.632989+00:00"
commit_sha: "a9c9415d11274d2fe4aae489bdf8c75177d11189"
runner: "agent-omn-16237"
verifier: "jonahgabriel"
probe_command: |-
  uv run pytest tests/unit/validation/test_application_database_sql_enforcement_regressions.py::test_allowlisted_physically_public_table_passes_unqualified_or_public -q
probe_stdout: |
  2 passed, 8 warnings in 0.21s

  --- supplementary real before/after differential, captured separately ---
  Both commits' real module source (application_database_domain_enforcement.py) loaded
  directly via `git show <sha>:<path>`, exec'd as a standalone module, and called as
  lint_application_database_sql('ALTER TABLE intent_classification_events ADD COLUMN
  agent_source text;', load_topology_profile('local')) against the same real topology
  profile -- the exact statement OMN-14751's 0001_intent_classification_agent_source.sql
  ships:

  BEFORE (fa3c706fa, the PR's merge-base):
    ("application relation target 'intent_classification_events' must be schema-qualified",)
  AFTER  (a9c9415d11274d2fe4aae489bdf8c75177d11189, the PR head):
    ()
actual_output: >-
  PASS: uv run pytest on the exact new regression test (2 parametrized cases: unqualified and public-qualified)
  is GREEN against the PR head and RED against the pre-fix module (verified separately below). The supplementary
  before/after differential proves the fix changes real gate behavior, not just that new tests were added:
  before omnibase_infra#2802 (merge-base fa3c706fa), lint_application_database_sql() rejects OMN-14751's
  exact unqualified ALTER TABLE intent_classification_events statement with "must be schema-qualified";
  after the fix (PR head a9c9415d1), the same statement against the same real topology profile returns
  zero violations, because intent_classification_events is enumerated in INTERNAL_TABLES_PHYSICALLY_IN_PUBLIC_UNTIL_OMN15359
  (src/omnibase_infra/topology/physical_schema_mapping.py) and the gate now consults it.
exit_code: 0
pr_number: 2802
branch: "jonah/omn-16237-domain-gate-consults-physical-allowlist"
