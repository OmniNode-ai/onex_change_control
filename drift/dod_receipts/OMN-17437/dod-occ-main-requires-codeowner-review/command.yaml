---
schema_version: "1.0.0"
ticket_id: "OMN-17437"
evidence_item_id: "dod-occ-main-requires-codeowner-review"
check_type: "command"
check_value: >-
  test -f evidence/OMN-17437/occ-main-protection-before.json && test -f evidence/OMN-17437/occ-main-protection-after.json
  && uv run pytest tests/ci/test_occ_main_protection_evidence.py -q --no-cov -p no:cacheprovider
contract_sha256: "sha256:dfd3eb97f6e97e325ca89a9199227b333c5696145ea0cce160b5d5166ac1ddce"
contract_entry_sha256: "sha256:a3e1ba550b061ee4ec069cdf63a6b3115b70610fa2bbe1c942282b495b25ed26"
status: PASS
run_timestamp: "2026-09-01T09:26:00Z"
commit_sha: "2227d178dd3d1fc229f042895890f9dd9b647d65"
runner: "omn17437-occ-branch-protection-put"
verifier: "github-branch-protection-api-readback"
probe_command: >-
  gh api graphql -f query='{repository(owner:"OmniNode-ai",name:"onex_change_control"){branchProtectionRules(first:20){nodes{pattern
  requiresApprovingReviews requiredApprovingReviewCount requiresCodeOwnerReviews dismissesStaleReviews
  requireLastPushApproval}}}}' --jq '.data.repository.branchProtectionRules.nodes[] | select(.pattern=="main")';
  uv run pytest tests/ci/test_occ_main_protection_evidence.py -q --no-cov -p no:cacheprovider
probe_stdout: |
  {"dismissesStaleReviews":true,"pattern":"main","requireLastPushApproval":false,"requiredApprovingReviewCount":0,"requiresApprovingReviews":true,"requiresCodeOwnerReviews":true}
  4 passed in 0.01s
exit_code: 0
actual_output: >-
  PASS, in two layers. LIVE LAYER (operator-scope, first probe_stdout line): onex_change_control@main
  branch protection reports requiresApprovingReviews=true, requiresCodeOwnerReviews=true and dismissesStaleReviews=true,
  so the CODEOWNERS entry `grants/prod_promotion_grants.yaml @OmniNode-ai/platform-leads` is now an enforced
  merge requirement rather than a mere auto-request. The identical query run before the change returned
  requiresApprovingReviews=false, requiresCodeOwnerReviews=false, requiredApprovingReviewCount=null, and
  the REST representation carried no required_pull_request_reviews object at all. RECORDED LAYER (the
  check_value, which is what CI re-runs): the raw REST readbacks captured either side of the change are
  committed at evidence/OMN-17437/occ-main-protection-{before,after}.json, and tests/ci/test_occ_main_protection_evidence.py
  asserts over them that the BEFORE state had no review requirement, that the AFTER state enables code-owner
  review without the branch-wide controls, and that the change was strictly ADDITIVE — all 23 required
  status-check contexts (sorted set equality), strict=false, enforce_admins, required_linear_history,
  required_conversation_resolution, allow_force_pushes, allow_deletions, block_creations, lock_branch
  and allow_fork_syncing all round-tripped unchanged. That additivity assertion is the one that most needs
  mechanical proof, because PUT /repos/:owner/:repo/branches/:branch/protection replaces the WHOLE protection
  resource and silently drops every control the request body omits; the PUT body was therefore generated
  by round-tripping the captured readback rather than hand-written. Red->green proof for both legs, run
  locally: substituting the BEFORE readback as the AFTER file fails with "AFTER readback has no required_pull_request_reviews",
  and deleting one status-check context from the AFTER readback fails with "required status-check contexts
  changed; the PUT was not additive. before=23 after=22". WHY THE CHECK_VALUE READS A RECORDING RATHER
  THAN THE LIVE SETTING: it cannot read the live setting, and no CI check in this repo can. Both repository.branchProtectionRules
  and repos/:owner/:repo/branches/main/protection require admin scope; the workflow GITHUB_TOKEN does
  not have it and a live query from CI returns FORBIDDEN — observed directly, since the first revision
  of this entry used the live query and failed the Contract Compliance Check on run 33488797921 with {"data":{"repository":null},"errors":[{"type":"FORBIDDEN","path":["repository","branchProtectionRules"]}]}.
  The live claim is therefore evidenced by the operator-scope probe above, not by CI. SCOPE CHOICE, MEASURED
  NOT ASSUMED: requiredApprovingReviewCount is 0 and require_last_push_approval is false, because both
  are branch-WIDE and gate every PR into main regardless of code ownership. This was established experimentally,
  not reasoned: with require_last_push_approval=true, PR #7939 — which touches no CODEOWNERS-covered path
  — sat mergeStateStatus=BLOCKED with all 38 checks green and reviewDecision="" and `gh pr merge` refused
  with "the base branch policy prohibits the merge"; setting that single field false, with nothing else
  changed, flipped the same PR to mergeStateStatus=CLEAN. So require_code_owner_reviews with count 0 gates
  the CODEOWNERS-covered paths without freezing routine non-governance traffic on main. The complementary
  positive proof — a PR touching grants/prod_promotion_grants.yaml reporting reviewDecision REVIEW_REQUIRED
  — is sequenced after this PR lands, because running it earlier means opening a grant-registry PR while
  the leg-2 auto-merge hole is still open.
pr_number: 7939
branch: "hotfix/omn-17437-occ-main-grant-codeowners-gate"
