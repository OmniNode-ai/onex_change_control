---
schema_version: "1.0.0"
ticket_id: "OMN-17437"
evidence_item_id: "dod-occ-main-requires-codeowner-review"
check_type: "command"
check_value: >-
  test -f evidence/OMN-17437/occ-main-protection-before.json && test -f evidence/OMN-17437/occ-main-protection-after.json
  && uv run pytest tests/ci/test_occ_main_protection_evidence.py -q --no-cov -p no:cacheprovider
contract_sha256: "sha256:a5a658185508919ad8a1669ea0fbd755cc3001d60285935c4a8dfc5660cded91"
contract_entry_sha256: "sha256:9949da6135a6ba41b331949743fe46d4c4f872bb20c8ddf3e3934f7215b307f4"
status: PASS
run_timestamp: "2026-09-01T08:55:19Z"
commit_sha: "bc3c64ce941ff852ffbf54b7fdf29c8ad7ef2623"
runner: "omn17437-occ-branch-protection-put"
verifier: "github-branch-protection-api-readback"
probe_command: >-
  gh api graphql -f query='{repository(owner:"OmniNode-ai",name:"onex_change_control"){branchProtectionRules(first:20){nodes{pattern
  requiresApprovingReviews requiredApprovingReviewCount requiresCodeOwnerReviews dismissesStaleReviews
  requireLastPushApproval}}}}' --jq '.data.repository.branchProtectionRules.nodes[] | select(.pattern=="main")';
  uv run pytest tests/ci/test_occ_main_protection_evidence.py -q --no-cov -p no:cacheprovider
probe_stdout: |
  {"dismissesStaleReviews":true,"pattern":"main","requireLastPushApproval":true,"requiredApprovingReviewCount":0,"requiresApprovingReviews":true,"requiresCodeOwnerReviews":true}
  4 passed in 0.03s
exit_code: 0
actual_output: >-
  PASS, in two layers. LIVE LAYER (operator-scope, the first probe_stdout line): a GraphQL readback of
  onex_change_control@main branch protection reports requiresApprovingReviews=true, requiresCodeOwnerReviews=true,
  dismissesStaleReviews=true and requireLastPushApproval=true, so the CODEOWNERS entry `grants/prod_promotion_grants.yaml
  @OmniNode-ai/platform-leads` is now an enforced merge requirement rather than an auto-request. The same
  query run before the change returned requiresApprovingReviews=false, requiresCodeOwnerReviews=false,
  requiredApprovingReviewCount=null. RECORDED LAYER (the check_value, which is what CI re-runs): the raw
  REST readbacks captured immediately before and after the PUT are committed at evidence/OMN-17437/occ-main-protection-{before,after}.json,
  and tests/ci/test_occ_main_protection_evidence.py asserts over them that the BEFORE state had no required_pull_request_reviews
  object at all, that the AFTER state enables all three review controls, and that the change was strictly
  ADDITIVE — all 23 required status-check contexts (sorted set equality), strict=false, enforce_admins,
  required_linear_history, required_conversation_resolution, allow_force_pushes, allow_deletions, block_creations,
  lock_branch and allow_fork_syncing round-tripped unchanged. That additivity assertion is the one that
  most needs mechanical proof, because PUT /repos/:owner/:repo/branches/:branch/protection replaces the
  WHOLE protection resource and silently drops every control the request body omits; the PUT body was
  therefore generated by round-tripping the captured BEFORE object rather than hand-written. Red->green
  proof for both legs of that test, run locally: substituting the BEFORE readback as the AFTER file fails
  with "AFTER readback has no required_pull_request_reviews", and deleting one status-check context from
  the AFTER readback fails with "required status-check contexts changed; the PUT was not additive. before=23
  after=22". WHY THE CHECK_VALUE READS A RECORDING RATHER THAN THE LIVE SETTING: it cannot read the live
  setting, and no CI check in this repo can. Both repository.branchProtectionRules and repos/:owner/:repo/branches/main/protection
  require admin scope; the workflow GITHUB_TOKEN does not have it and a live query from CI returns FORBIDDEN
  — observed directly, as the first revision of this entry used the live query and failed the Contract
  Compliance Check on run 33488797921 with {"data":{"repository":null},"errors":[{"type":"FORBIDDEN","path":["repository","branchProtectionRules"]}]}.
  The live claim is evidenced here by the operator-scope probe above, not by CI. KNOWN DIVERGENCE FROM
  AC-1: requiredApprovingReviewCount is 0, not >= 1, because a count of 1 is branch-wide and would freeze
  routine non-governance traffic on main that no code owner owns. The behavioural probe of count-0 enforcement
  (a PR touching grants/prod_promotion_grants.yaml reporting reviewDecision REVIEW_REQUIRED) is sequenced
  after this PR lands, since running it earlier means opening a grant-registry PR while the leg-2 auto-merge
  hole is still open.
pr_number: 7939
branch: "hotfix/omn-17437-occ-main-grant-codeowners-gate"
