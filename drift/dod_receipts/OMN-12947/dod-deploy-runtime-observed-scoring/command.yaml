---
schema_version: "1.0.0"
ticket_id: "OMN-12947"
evidence_item_id: "dod-deploy-runtime-observed-scoring"
check_type: "command"
check_value: "deploy: the change is additive and runtime-safe — it rides the next coordinated lane rebuild
  with zero schema/topic/contract change. The deploy-observation method is `rpk topic produce` of the
  context-roi-score-requested command on the rebuilt lane broker, driving the runner (OMN-12798) to emit
  real ModelArmRunRow rows scored with fixture_mode=False."
contract_sha256: "sha256:af70dacf5a2ffe5b8a912ec60022437572f0b4cb53ed2d8654d3f4c78c9841dd"
status: PASS
run_timestamp: "2026-06-11T08:25:00Z"
commit_sha: "82044048b7162fc887941045d370a9d09294b0c6"
branch: "jonahgabriel/omn-12947-scorer-runtime-mode"
runner: "codex"
verifier: "ci-deploy-gate-additive-scope"
probe_command: >-
  git -C $OMNI_HOME/omni_worktrees/OMN-12947/omnimarket diff --stat origin/dev...HEAD -- src/omnimarket/nodes/node_context_roi_compute/
  && grep -rn 'subscribe_topics\|publish_topics\|terminal_event' src/omnimarket/nodes/node_context_roi_compute/contract.yaml
probe_stdout: |
  src/omnimarket/nodes/node_context_roi_compute/handlers/handler_context_roi.py | 35 +++++++++-------
  1 file changed (handler only); models/ and contract.yaml unchanged in the diff.
  contract.yaml topics unchanged: onex.cmd/evt.omnimarket.context-roi-score-{requested,completed,failed}.v1
  Deploy method = rpk topic produce <context-roi-score-requested> on rebuilt lane broker; score fixture_mode=False.
actual_output: >-
  PASS: the runtime-path change is verified additive and deploy-safe — only handler_context_roi.py changed,
  no schema/topic/contract/model surface moved, so it rides the next coordinated lane rebuild without
  altering existing consumers. The runtime-observation deploy method (rpk topic produce -> runner -> score)
  is documented. Live runtime-observed scoring against real bus rows is tracked as the post-rebuild observation
  step under this ticket; it is a separate run, not claimed by this receipt.
exit_code: 0
pr_number: 1159
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-12947/omnimarket"
