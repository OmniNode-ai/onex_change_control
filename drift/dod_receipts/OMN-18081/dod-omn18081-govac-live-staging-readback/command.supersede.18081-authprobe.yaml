---
schema_version: 1.0.0
ticket_id: OMN-18081
evidence_item_id: dod-omn18081-govac-live-staging-readback
check_type: command
supersedes: drift/dod_receipts/OMN-18081/dod-omn18081-govac-live-staging-readback/command.yaml
reason: |-
  Repair, not a re-bind of a different bar. The base receipt (run_timestamp 2026-09-10T09:03:18Z, minted
  on onex_change_control#8899, merged squash 45bea46860b1314fe656b61cd60e33dd16d5349c) carries a
  probe_command/probe_stdout pair that does not match its own actual_output: probe_command is a Playwright
  invocation and probe_stdout is an UNAUTHENTICATED proxy-origin trace of the login page (a 307 redirect
  from /app/api-keys to /login, then providers/session reads on the login page) - it never authenticates
  and never calls /v1/api-keys at all. actual_output, unchanged and correct, describes an AUTHENTICATED
  mint/409/revoke probe: two distinct-label keys created and listed (AC1c), a duplicate label refused as
  a typed 409 (AC3c server half), no cap and no replace-on-mint (AC4c live corroboration), and a disposable
  key minted then revoked. The mismatch was introduced in commit faa1eac74eb7ec5a121fb6fb1758d03cfc2d7260
  ("fix(OMN-18081): record playwright staging readback") on a branch that squash-merged as #8899, most
  likely a probe_command/probe_stdout overwrite during a session interruption that left actual_output from
  an earlier, correct edit untouched. Confirmed byte-identical between that commit's blob and the merged
  file on origin/dev, so the defect is live on dev, not a local artifact.
  The real, authenticated probe that actual_output describes was independently run and recorded by lane
  omn18081-apikey-probe (ledger docs/tracking/ROLLING_WORK_LEDGER.md:5972) and posted verbatim as Linear
  comment 7f1a811b-6095-42a3-9996-712a6ce25222 on OMN-18081. This record's probe_command/probe_stdout below
  are that lane's own SSM invocation and its step/result table, reproduced from the comment so probe_command
  and probe_stdout finally agree with actual_output. No secret value is carried: the disposable key's value
  was never printed anywhere (prefix onxk_, length 48, sha256-12 d52e3cbfad99); the tenant's stored account
  password is likewise represented only by length (40) and sha256-12 (fc485f1269bb), byte-identical to the
  2026-09-09 record, which is itself the proof nothing was rotated or re-issued. The revoke cited
  docs/tracking/ROLLING_WORK_LEDGER.md:5845 item (e) as ROTATION-CONSENT on its own command line per
  OMN-17957; that same row (item e) authorizes the live probe this record re-states, and item (g) of the
  same row authorized OMN-18081's hand-close while this defect was still open. This is a receipt-level
  repair under OMN-15459 (net-new command.supersede.*.yaml, base receipt untouched) - the item this record
  binds is the SAME item the base receipt bound, dod-omn18081-govac-live-staging-readback, not a different
  one, so OMN-15459's S1 distinctness rule does not apply (single item, no cohort). S2 family binding is
  satisfied because the replacement's check_value is byte-identical to this item's own declared check in
  contracts/OMN-18081.yaml, and evidence_item_id names the item directly. pr_number below is the PRODUCT
  PR this probe exercises (OmniNode-ai/omniweb#364, the omniweb half of OMN-18081, merged squash
  1816b5b4adcb8ef86627facea914dcbff52de80a) rather than the OCC carrier PR, per this ticket's own binds_ac
  convention on its non-govac items (dod-OmniNode-ai-omniweb-pr-364 uses pr_number 364, not the OCC PR
  that carried it). No credential was rotated, re-issued or revoked by this lane; no secret value was
  read, printed or decoded; no cluster object was mutated (rule 22) - this record only re-transcribes a
  probe already run and already authorized.
superseder: occ-18081-receipt-supersede
created_at: '2026-09-10T11:21:18Z'
replacement:
  schema_version: 1.0.0
  ticket_id: OMN-18081
  evidence_item_id: dod-omn18081-govac-live-staging-readback
  check_type: command
  check_value: |-
    grep -q '^status: PASS$' "$CONTRACT_REPO_DIR/drift/dod_receipts/OMN-18081/dod-omn18081-govac-live-staging-readback/command.yaml"
  status: PASS
  run_timestamp: '2026-09-10T06:08:25Z'
  commit_sha: 1816b5b4adcb8ef86627facea914dcbff52de80a
  runner: omn18081-apikey-probe
  verifier: occ-18081-receipt-supersede-nonimplementer
  evidence_class: ui_dashboard
  probe_command: |-
    aws ssm send-command --instance-ids i-06169517a92b45f86 --document-name AWS-RunShellScript \
      --comment OMN-18081 --parameters commands=["<store-read the beta-synthetic-2 dev-store password>
      <mint a token via the omnidash-spa direct grant as beta-synthetic-2>
      <GET /v1/api-keys baseline>
      <POST /v1/api-keys name=omn18081-probe-a>
      <GET /v1/api-keys>
      <POST /v1/api-keys the same label again>
      <GET /v1/api-keys with the new key>
      <DELETE /v1/api-keys/{id}>
      <GET /v1/api-keys with the revoked key>
      <GET /v1/api-keys with the bearer token, control>"]
    # command-id 3383e21b-516d-41d6-8733-b10f43c19f94
    aws ssm get-command-invocation --instance-id i-06169517a92b45f86 --command-id 3383e21b-516d-41d6-8733-b10f43c19f94
  probe_stdout: |-
    step 0 store read -> 200, password len 40, sha256-12 fc485f1269bb
    step 1 token via omnidash-spa direct grant -> 200, tenant_slug beta-synthetic-2, aud [onex-api, account], iss dev.auth.omninode.ai/realms/omninode
    step 2 GET /v1/api-keys baseline -> 200, 1 row, default, active
    step 3 POST /v1/api-keys name=omn18081-probe-a -> 200, id 7fde6348-28c2-4990-8ec8-24991eda0c1c
    step 4 GET /v1/api-keys -> 200, 2 rows, both active, distinct names [omn18081-probe-a, default]
    step 5 POST the same label again -> 409 detail="That label is already in use by one of your active API keys. Choose a different label, or revoke the existing key first." code=resource.conflict
    step 6 GET /v1/api-keys with the new key -> 200 (the key authenticates, positive control for step 8)
    step 7 DELETE /v1/api-keys/7fde6348-... -> 200 ok=true already_revoked=false
    step 8 GET /v1/api-keys with the revoked key -> 401 detail=Unauthorized code=auth.credential.invalid
    step 9 same call with the bearer token, control -> 200, probe row now is_active=false
  actual_output: 'PASS: the deployed surface admits a second key under a distinct label and lists both
    active (AC1c); refuses a duplicate label as a typed 409 carrying resource.conflict and a customer-readable
    sentence (AC3c server half); minted the second key with no cap refusing it and without revoking or
    replacing the incumbent default (AC4c, live corroboration). The disposable key was revoked and no
    longer authenticates - 401 auth.credential.invalid - with the bearer control returning 200 on the
    same call, so the 401 is the credential and not the endpoint. Its value was never printed. The tenant
    is back to exactly one active key. Deployed digests, recorded alongside the merged shas as AC5c requires:
    omniweb sha256:1d0038879995c1282328e944ebb0a3a9d14ea18d65b1a98e58e1d08e54413c2a (ECR tags sha-e128dd39b8e51f03d0f0f73474eba04b4dce720a
    and latest; e128dd39 has the merged squash 1816b5b4adcb8ef86627facea914dcbff52de80a as an ancestor),
    onex-api sha256:5c90adf53c5bc1c619f1b74912594e0c20db4d3bfdd7f622e5dbaf06443b2e75 (ECR tags dev-sha-5785b43,
    sha-5785b43, 5785b433bbd0de23f1f40aa747474606ed257bfd - the merged squash itself). This record supersedes
    the base receipt of the same item, whose probe_command/probe_stdout were an unauthenticated Playwright
    login-page trace that never called /v1/api-keys - the probe_command/probe_stdout above are the actual
    authenticated probe this actual_output has always described. Rule 24: the .201 compose dev lane runs
    no dashboard and no onex-api, so there is no lab surface for either half and no lab pass is claimed.'
  exit_code: 0
  pr_number: 364
  branch: jonah/omn-18081-receipt-supersede
  contract_sha256: sha256:54d67aa0dc623688838f9b00a7d296384f726c1fd35094603498421910c2ef77
  contract_entry_sha256: sha256:69dd1b10a28c5d60c851c738f22d39802e9369522662601f86ff4937e2827de2
