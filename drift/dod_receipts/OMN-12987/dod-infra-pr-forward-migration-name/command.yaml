---
schema_version: "1.0.0"
ticket_id: "OMN-12987"
evidence_item_id: "dod-infra-pr-forward-migration-name"
check_type: "command"
check_value: "bash -n scripts/deploy-runtime.sh && targeted compose/catalog/runtime-lane tests"
status: PASS
run_timestamp: "2026-06-14T06:03:26Z"
commit_sha: "caebc477df021cbe0ab01a99d4ddf0feec8ee372"
runner: "codex-merge-sweep-20260614"
verifier: "receipt-gate-local"
probe_command: "omnibase_infra PR #1981 validation: bash -n scripts/deploy-runtime.sh; targeted stability
  runtime lane, migration manifest, deploy-agent compose generation, and catalog/compose sync checks"
probe_stdout: |
  PR #1981 project-namespaces the forward-migration container to stop cross-lane collisions:
    - docker/catalog/services/forward-migration.yaml is the source of truth and now emits
      container_name: omnibase-infra-forward-migration for the base compose project.
    - docker/docker-compose.infra.yml is regenerated/in sync with the catalog output.
    - scripts/deploy-runtime.sh derives ${compose_project}-forward-migration and
      ${compose_project}-postgres for the migration wait/projection-table probe instead of using
      lane-blind hard-coded dev names.
    - tests cover the stability-test runtime lane, migration manifest expectations, deploy-agent
      compose generation, and catalog/compose sync.
actual_output: >-
  PASS: omnibase_infra PR #1981 (head caebc477df021cbe0ab01a99d4ddf0feec8ee372) removes the non-namespaced
  forward-migration container collision that blocked the OMN-12987 stability-lane build-context re-prove
  after the docker build succeeded. The change is contract/source-of-truth aligned through the service
  catalog and generated compose file, and deploy-runtime.sh now targets the active compose project for
  migration wait/projection-table probes. No live dev/prod/judge mutation is claimed by this receipt.
exit_code: 0
contract_sha256: "sha256:868dcd40a879cce47d028283ac5225791739eaffaa154d3e128f3b4ae4030091"
pr_number: 1981
branch: "jonah/omn-12987-forward-migration-container-name"
