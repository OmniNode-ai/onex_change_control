---
schema_version: "1.0.0"
ticket_id: "OMN-15772"
evidence_item_id: "dod-omn-15772-pr-6635-fail-closed-reassert"
check_type: "command"
check_value: >-
  body="$(gh api 'repos/OmniNode-ai/onex_change_control/contents/.github/workflows/ci.yml?ref=a1f5325fb9cc1dafcc36fe1b5fc76c8e54eb6453'
  --jq .content | base64 -d)" && failed_clone_needle="$(printf '%s' '::error::Failed to clone $'; printf
  '%s' '{'; printf '%s' 'repo} after 3 attempts')" && grep -qF 'rm -rf /tmp/repos' <<< "$body" && grep
  -qF 'rm -rf "${target}"' <<< "$body" && grep -qF "$failed_clone_needle" <<< "$body" && grep -qF 'clone_omni_repo
  "$repo" "/tmp/repos/$repo"' <<< "$body"
contract_sha256: "sha256:365c9f1baa401156c4a9526188bbb17bb2f7274183e115f675038c8fb9d662f1"
contract_entry_sha256: "sha256:cbf758834ead6714e711d01031684e8ed21e1764591bc88ce6a2b530860bf375"
status: PASS
run_timestamp: "2026-08-29T02:05:26Z"
commit_sha: "4c48f4b40bf84199d57a0e6addfdc3796f7f0990"
runner: "local-worktree-omn-15772"
verifier: "bash-pipefail-static-assertion"
probe_command: >-
  body="$(gh api 'repos/OmniNode-ai/onex_change_control/contents/.github/workflows/ci.yml?ref=a1f5325fb9cc1dafcc36fe1b5fc76c8e54eb6453'
  --jq .content | base64 -d)" && failed_clone_needle="$(printf '%s' '::error::Failed to clone $'; printf
  '%s' '{'; printf '%s' 'repo} after 3 attempts')" && grep -qF 'rm -rf /tmp/repos' <<< "$body" && grep
  -qF 'rm -rf "${target}"' <<< "$body" && grep -qF "$failed_clone_needle" <<< "$body" && grep -qF 'clone_omni_repo
  "$repo" "/tmp/repos/$repo"' <<< "$body"
probe_stdout: |
  exit=0
actual_output: >-
  PASS: the re-asserted probe is GREEN under `bash -o pipefail -c` against merged commit a1f5325fb9cc1dafcc36fe1b5fc76c8e54eb6453,
  read through the GitHub contents API. The superseded item's content claim is therefore intact and unweakened
  -- what was retired is its SHELL FORM, which was a latent exit 141: `printf | grep -qF` under pipefail
  dies of SIGPIPE when grep -q exits on its first match, so the chain reported failure while every assertion
  passed. Measured directly: the superseded item's own check_value returns 0 under `bash -c` and 141 under
  `bash -o pipefail -c`. Falsifiability is preserved exactly: the same probe pointed at merge-base c3a008a83991a09ceb4e50b55e57425b82698165
  returns exit 1, because that tree lacks the /tmp/repos cleanup and still wraps the clone loop in `||
  true`. The merged item and its merged receipt were left byte-untouched; both still bind (their contract_entry_sha256
  values were re-verified against the amended contract and are unchanged).
exit_code: 0
