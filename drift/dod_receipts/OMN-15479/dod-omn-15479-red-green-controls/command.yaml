---
schema_version: "1.0.0"
ticket_id: "OMN-15479"
evidence_item_id: "dod-omn-15479-red-green-controls"
check_type: "command"
check_value: |-
  uv run pytest tests/unit/scripts/test_yamlfmt_contamination_gate.py -q -p no:randomly
contract_sha256: "sha256:c9e9c7d709c4f8386a6151e142d2edae03f4699aaf66809efaa6a512826babca"
contract_entry_sha256: "sha256:55f770e6e8a0b2fe6ccea4b7cc932b8f8eeaed7b375278fa9a763cb24fe16586"
status: PASS
run_timestamp: "2026-07-30T09:33:33.600000+00:00"
commit_sha: "8ed4bfc4421a6fae292557dd00117470da2ee16c"
runner: "bash -o pipefail -c (stickybeatz-studio, .200 gate host)"
verifier: "stickybeatz-studio-gate-host"
probe_command: |-
  uv run pytest tests/unit/scripts/test_yamlfmt_contamination_gate.py -q -p no:randomly
probe_stdout: |
  ..........................................................               [100%]
  58 passed in 15.74s

actual_output: |
  PASS (exit 0 on the .200 gate host under bash -o pipefail). 58 passed, 0 failed.

  These controls prove the gate FIRES rather than that it exists. Defect shapes go RED:
  the yamlfmt marker reaching a parsed value, and a folded scalar carrying an internal
  newline across all three chomping variants and the more-indented-continuation form.
  Proven-safe shapes stay GREEN and are pinned as negative controls: literal block scalars
  including a line above max_line_length, single-paragraph folded scalars, and quoted or
  plain multi-line scalars.

  The ratchet is exercised in all three directions (new path, grown count, stale entry),
  and the wiring anchor is mutated against six removal vectors: job deleted, job made
  conditional with if:, job given a needs: chain, job steps gutted, job dropped from
  ci-summary needs:, and the strict success-only check removed.

  Two controls close known false-green classes. Per OMN-15070 a hook whose files: regex
  matches nothing is byte-indistinguishable from a passing hook, so the regex is compiled
  and matched against real corpus paths drawn from the live baseline rather than invented
  ones. And the scanning hook is asserted to be declared BEFORE the yamlfmt hook, because
  the corruption is a fixed point that never self-heals -- running after yamlfmt would mean
  objecting to evidence the formatter had already destroyed.

  The gate is also asserted to be read-only: it never rewrites a scanned file, since
  formatter mutation is the defect and not the remedy.

exit_code: 0
