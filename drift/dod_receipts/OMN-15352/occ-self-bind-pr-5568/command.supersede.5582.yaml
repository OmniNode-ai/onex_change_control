---
# Append-only relabel of the occ-self-bind-pr-5568 receipt (OCC PR #5582).
#
# The base receipt's bytes are NOT touched: this is the sanctioned
# command.supersede.<pr>.yaml channel, so the original remains auditable and the
# per-entry hash binding (sha256:ff193f35...7da6c879) is unchanged.
#
# NOTHING about the probe changes. Same check_value, same execution, same exit
# code, same pr_number, same commit_sha, same run_timestamp, same runner. The
# ONLY correction is the receipt's ADMISSIBILITY sentence, which stated a
# conclusion the cited predicate refuses. Executing
# onex_change_control.validation.evidence_admissibility.classify_evidence over
# this exact check_value, with the executing-runner probe set
# (LIVE_PROBE_COMMANDS | EXECUTED_HERMETIC_COMMANDS), returns
# admissible=False rule=INSIDE_OWN_DIFF - identically with changed_paths=None
# and changed_paths=[], because SELF_REFERENTIAL_PATTERNS denies the operand
# `contracts/OMN-15352.yaml` first and unconditionally.
schema_version: 1.0.0
ticket_id: OMN-15352
evidence_item_id: occ-self-bind-pr-5568
check_type: command
supersedes: drift/dod_receipts/OMN-15352/occ-self-bind-pr-5568/command.yaml
reason: 'Relabels a false ADMISSIBILITY claim raised as Finding 1 by the adversarial review of OCC PR
  #5568. That review landed after #5568 merged (2026-07-30T05:11:55Z), so the claim could not be amended
  in place. The claim is falsified by executing the predicate it cites: the correct verdict is INADMISSIBLE
  / INSIDE_OWN_DIFF. Probe, exit code and PR binding are unchanged - only the admissibility label is corrected.'
superseder: fable-ac4-followup-0730/omn15352-receipt-followup
created_at: '2026-07-30T07:30:00Z'
replacement:
  schema_version: "1.0.0"
  ticket_id: "OMN-15352"
  evidence_item_id: "occ-self-bind-pr-5568"
  check_type: "command"
  check_value: "files=\"$(gh api repos/OmniNode-ai/onex_change_control/pulls/5568/files --paginate --jq
    '.[].filename')\" && printf '%s\\n' \"$files\" | grep -qx 'contracts/OMN-15352.yaml'"
  contract_entry_sha256: "sha256:ff193f352ee4ff6cdeae97c1d9523d2913f37dacccec177ad472853e7da6c879"
  status: PASS
  run_timestamp: "2026-07-30T05:06:00Z"
  commit_sha: "572bd7800c0c353691bf653494189d2d1937b0a7"
  runner: "fable-ac4-receipt-0730/omn15352-ac4-durable-receipt"
  verifier: "executed-probe"
  probe_command: "sh -c 'files=\"$(gh api repos/OmniNode-ai/onex_change_control/pulls/5568/files --paginate
    --jq \".[].filename\")\" && printf \"%s\\n\" \"$files\" | grep -qx \"contracts/OMN-15352.yaml\"'"
  probe_stdout: |
    exit=0 (grep -qx emits no stdout on match; exit 0 confirms PR #5568's own file
    list, as reported by the GitHub API, contains the literal path
    contracts/OMN-15352.yaml)
  actual_output: |
    PASS — PR #5568 provably touches contracts/OMN-15352.yaml.

    RED CONTROL: the same command with the asserted path swapped for
    contracts/OMN-99999-does-not-exist.yaml exits 1. So this is a falsifiable diff
    assertion about the PR, not a bare existence probe that would pass for any PR.

    SHAPE NOTES (both deliberate, both mechanically checked by hooks that run on this file):
      * OMN-15411 Rule E — the producer is buffered into `files` and replayed through
        `printf` rather than piped straight into `grep -q`. An unbounded producer piped
        into an early-exiting `grep -q` gets SIGPIPE (141), and under the dod_verify
        runner's `bash -o pipefail` that 141 becomes the pipeline's exit status: a false
        RED on evidence that is actually present.
      * OMN-15382 Rule B — 5568 is pinned literally. The bare ${PR_NUMBER} placeholder
        is pre-substituted by the runner with ITS OWN value before `sh -c` runs, so an
        item whose id embeds pr-5568 while its check_value says ${PR_NUMBER} silently
        re-checks whatever PR the runner happens to be evaluating.

    ADMISSIBILITY — CORRECTED 2026-07-30 (OCC PR #5582, review Finding 1). This receipt
    originally concluded that the check is "admissible under the OMN-14505/OMN-15309
    predicate". That conclusion is RETRACTED: it is false, and the predicate it cited is
    what falsifies it.

      classify_evidence(<this check_value>,
                        admissible_probes=LIVE_PROBE_COMMANDS | EXECUTED_HERMETIC_COMMANDS,
                        changed_paths=None) -> admissible=False, rule=INSIDE_OWN_DIFF
      classify_evidence(... , changed_paths=[])   -> admissible=False, rule=INSIDE_OWN_DIFF
      reason: self-referential ... (matched 'contracts/OMN-\d+\.ya?ml')

    CORRECT LABEL: INADMISSIBLE / INSIDE_OWN_DIFF. The refusal comes from the DENY half
    of the predicate (evidence_admissibility.SELF_REFERENTIAL_PATTERNS), which is applied
    FIRST and UNCONDITIONALLY; the grep operand contracts/OMN-15352.yaml is what trips it.
    The original sentence reasoned only about the ALLOW half — `gh api` being in
    LIVE_PROBE_COMMANDS, in command position, against a /files endpoint that is not
    metadata-only. Each of those sub-clauses is individually TRUE. The conclusion drawn
    from them is not, because the DENY half is never reached in that reasoning. The same
    author applied the DENY half correctly one item earlier in the same change, for the
    seal's `dod_receipts` operand.

    WHAT DOES NOT CHANGE: the probe still ran, still exits 0, and still binds this ticket
    to PR #5568 for occ-preflight eligibility — eligibility reads pr_number and commit_sha,
    not the admissibility class. Inadmissible checks are demoted to WARN in
    contract_compliance_check, and node_dod_verify does not apply the predicate at all.
    The correction is to the LABEL, and it is made because a false admissibility claim in
    a durable governance artifact is exactly the defect class the authoring change existed
    to repair.

    WHY NO STRONGER SHAPE WAS SUBSTITUTED HERE: every path this PR touches is either the
    OCC receipt tree (`dod_receipts`) or a ticket contract file, and both are denied
    unconditionally by the same tuple — so no path-membership assertion about an OCC
    companion's own diff can ever be admissible, even read from the GitHub API at an
    immutable ref. The admissible alternative (asserting the PR's file COUNT) is strictly
    weaker: it passes for any PR with the same number of files, proven by execution
    against OCC #5479. That structural gap is owned by OMN-15309; it is named here rather
    than papered over.
  exit_code: 0
  duration_ms: 640
  pr_number: 5568
  branch: "jonah/omn-15352-ac4-durable-receipt"
  working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-15352-ac4-receipt/onex_change_control"
