---
# Receipt for the retraction of a false ADMISSIBILITY claim in the merged
# receipt drift/dod_receipts/OMN-15352/occ-self-bind-pr-5568/command.yaml
# (adversarial-review Finding 1, raised against OCC PR #5568 and delivered
# after that PR merged at 2026-07-30T05:11:55Z, so it could not be amended).
#
# Both check_values were executed verbatim through `bash -o pipefail -c` — the
# same invocation shape contract_compliance_check._check_command and
# node_dod_verify use — from the OCC repo root, and both were also falsified
# deliberately. A check never seen to go RED is not evidence that it can
# (feedback_prove_red_against_exists_but_wrong).
schema_version: "1.0.0"
ticket_id: "OMN-15352"
evidence_item_id: "dod-omn-15352-selfbind-admissibility-correction"
check_type: "command"
check_value: "test \"$(gh api repos/OmniNode-ai/onex_change_control/pulls/5568/files --paginate --jq '[.[].filename]|length')\"
  = \"4\""
contract_entry_sha256: "sha256:38a95b9b55c9927d3be732ed51191aab4548a5cd868032223956cc20147b4db3"
status: PASS
run_timestamp: "2026-07-30T07:20:00Z"
commit_sha: "b8183314c745905224edba1190971f9eb9c914c8"
runner: "fable-ac4-followup-0730/omn15352-receipt-followup"
verifier: "executed-probe"
probe_command: "for each of the 2 check_values in dod_evidence item dod-omn-15352-selfbind-admissibility-correction:
  bash -o pipefail -c \"<check_value>\", run from the OCC repo root; plus a direct execution of onex_change_control.validation.evidence_admissibility.classify_evidence
  over the check_value of the merged receipt under correction"
probe_stdout: |
  check 1  exit=0  stdout=''   # test "$(gh api .../pulls/5568/files --paginate --jq '[.[].filename]|length')" = "4"
  check 2  exit=0  stdout=''   # gh api .../contents/src/onex_change_control/validation/evidence_admissibility.py?ref=45eaca8d5 | base64 -d | grep -c SELF_REFERENTIAL_PATTERNS  -> 4

  classify_evidence(<merged occ-self-bind-pr-5568 check_value>,
                    admissible_probes=LIVE_PROBE_COMMANDS | EXECUTED_HERMETIC_COMMANDS,
                    changed_paths=None) -> admissible=False rule=INSIDE_OWN_DIFF
  classify_evidence(... , changed_paths=[])                      -> admissible=False rule=INSIDE_OWN_DIFF
  reason: "self-referential: reads back the receipt/contract tree the evidence author writes in the
           same PR ... (matched 'contracts/OMN-\\d+\\.ya?ml')"
actual_output: |
  PASS — 2/2 checks executed green; both classify ADMISSIBLE; 2/3 RED controls
  fired and the one that did NOT fire is recorded below as a measured weakness
  rather than omitted.

  WHAT IS BEING RETRACTED. The merged receipt
  drift/dod_receipts/OMN-15352/occ-self-bind-pr-5568/command.yaml states, verbatim:

    "ADMISSIBILITY: admissible under the OMN-14505/OMN-15309 predicate — `gh api`
     is in LIVE_PROBE_COMMANDS and appears in command position inside the
     substitution, and `/pulls/<n>/files` returns the actual diff so it does NOT
     match the metadata-only refusal (_GH_METADATA_ENDPOINT_RE covers
     /pulls|issues/<n> and the merge/reviews/comments suffixes, not /files)."

  That conclusion is FALSE, and the predicate it cites is what falsifies it.
  Executing classify_evidence over that exact check_value returns
  admissible=False, rule=INSIDE_OWN_DIFF — the same result with changed_paths=None
  and with changed_paths=[], because the refusal comes from the DENY half
  (SELF_REFERENTIAL_PATTERNS), which is applied first and unconditionally. The
  operand `contracts/OMN-15352.yaml` inside the grep is what trips it.

  Every sub-clause of the retracted sentence is individually true. The conclusion
  is false because the sentence reasons only about the ALLOW half and stops. The
  same author applied the DENY half CORRECTLY one item earlier, for the seal's
  `dod_receipts` operand ("the predicate DENIES first and unconditionally").
  Same tuple, adjacent element.

  CORRECT LABEL: INADMISSIBLE / INSIDE_OWN_DIFF.

  BLAST RADIUS, STATED HONESTLY AND NOT INFLATED: nil. The earlier
  occ-self-bind-pr-5479 shape is inadmissible too (NOT_EXECUTED), so this is not
  a regression; inadmissible checks are demoted to WARN in
  contract_compliance_check; node_dod_verify never applies the predicate at all.
  It is corrected because a false admissibility claim inside a change whose whole
  thesis is honest admissibility labelling is exactly the defect class that
  change existed to repair.

  RED CONTROLS:
    RED-1  check 1 with the expected count changed 4 -> 3            : exit 1.
    RED-2  check 2 grepping a marker absent from the module
           (SELF_REFERENTIAL_PATTERNS_ABSENT) -> count 0 != 4        : exit 1.
    RED-3 (DID NOT FIRE — recorded, not hidden) check 1 with the PR number
           changed 5568 -> 5479                                      : exit 0.
           OCC PR #5479 also touches exactly 4 files, so the count assertion
           passes for a DIFFERENT PR.

  WHAT RED-3 MEANS, AND WHY IT IS IN THIS RECEIPT. The admissible replacement
  shape is measurably WEAKER than the inadmissible original: the original
  asserted a specific path is in this PR's diff (falsifiable per-PR); the
  replacement asserts only a file COUNT (falsifiable per-count). This correction
  therefore makes the LABEL true — it does not make the evidence stronger, and no
  claim is made here that it does. The reason no stronger admissible form exists
  is the DENY half itself: every path this PR touches lives under the OCC receipt
  tree or is a ticket contract file, so any path-membership assertion about this
  PR's own diff is denied unconditionally — even when read from the GitHub API at
  an immutable ref, which is a third-party surface by every other measure. That
  structural gap is the subject of OMN-15309 and is NOT claimed as closed here.

  RELABEL CHANNEL: the effective receipt is corrected through the sanctioned
  append-only supersession file beside the original
  (drift/dod_receipts/OMN-15352/occ-self-bind-pr-5568/command.supersede.<pr>.yaml),
  which leaves the original bytes byte-intact and carries a replacement whose
  ADMISSIBILITY sentence states the executed verdict. Confirmed mechanically:
  the per-entry hashes of all six pre-existing dod_evidence entries are unchanged
  after this PR's append (occ-self-bind-pr-5568 stays
  sha256:ff193f352ee4ff6cdeae97c1d9523d2913f37dacccec177ad472853e7da6c879,
  dod-omn-15352-ac4-dev-lane-live-readback stays sha256:83975a78...cb62b63e,
  dod-omn-15352-ac4-live-readback-hosted-seal stays sha256:5b44992a...441cfc9e),
  so every prior merged receipt stays valid.

  SCOPE: this is proof-surface hygiene on a ticket that is already Done
  (flipped 2026-07-30T05:40:36Z on adjudicated double-run dod_verify evidence).
  Nothing here reopens that acceptance, and no status was changed.
exit_code: 0
duration_ms: 975
branch: "jonah/omn-15352-ac4-followup-admissibility"
working_dir: "/Users/jonah/Code/omni_home/omni_worktrees/OMN-15352-ac4-followup/onex_change_control"
