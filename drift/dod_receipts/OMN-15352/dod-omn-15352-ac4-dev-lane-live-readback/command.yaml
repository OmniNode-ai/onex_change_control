---
# Durable AC4 receipt for OMN-15352 — `.201` dev-lane registry reconciliation.
#
# WHY THIS FILE EXISTS. Before this receipt, AC4's only durable home was a Linear
# comment. `contracts/OMN-15352.yaml` probed `.201` nowhere, so a `dod_verify`
# green on this ticket was vacuous with respect to AC4 — the absence of a red was
# the absence of a probe, not a pass. That was stated on the ticket three times by
# three independent parties (comments `2dcf691b` item 4, `4d85c99e` "AC4 is
# falsified by live readback", `ad4b23cb` "What this lane did NOT prove" item 2)
# and again by the AC4 lane's adversarial verifier ("CONTRACT-BINDING RESIDUAL IS
# REAL AND MATERIAL ... A dod_verify rerun WILL go green without proving AC1, AC2,
# or AC4"). This receipt makes the readback durable in the governance tree; the
# append-only supersession that binds it by sha256 is
# `dod-omn-15352-ac4-live-readback-hosted-seal` in the same contract.
#
# PROVENANCE OF EVERY FACT BELOW — cited, not invented:
#   * the deploy + first readback: lane `fable-fanout-0730/omn15352-ac4-dev-lane`,
#     ledger claim 2026-07-30T02:15Z, terminal 02:42Z, Linear comment `ad4b23cb`;
#   * independent reproduction: that lane's adversarial verifier (verifier !=
#     implementer), which re-probed `ssh omni-201-ts` / `gh` / Linear / `git`;
#   * this receipt's own re-probe: 2026-07-30T04:34Z, the `probe_command` and
#     `probe_stdout` recorded verbatim below. Every hash here is the FULL 64-hex
#     digest read from the live host, not the truncated form quoted in Linear.
schema_version: "1.0.0"
ticket_id: "OMN-15352"
evidence_item_id: "dod-omn-15352-ac4-dev-lane-live-readback"
check_type: "command"
check_value: "ssh omni-201-ts 'sha256sum /tmp/ac4/registry.json.before /home/jonah/.omnibase/infra/registry.json
  && grep -E \"git_sha|deployed_at|parent_command\" /home/jonah/.omnibase/infra/registry.json && for c
  in omninode-runtime omninode-runtime-effects omnimarket-projection-api; do printf \"%s \" \"$c\"; docker
  exec \"$c\" grep infra_vcs_ref /app/build-provenance.json; done && git -C /data/omninode/omni_home/omnibase_infra
  rev-parse origin/dev'"
contract_entry_sha256: "sha256:83975a7866cca9aec96e7f44b85664cf76f909f5e68852c10ccf2fb9cb62b63e"
status: PASS
run_timestamp: "2026-07-30T04:34:07Z"
commit_sha: "f420d5fa26f704c75c3854ba2efc788d36175d13"
# runner = the lane that executed the probe whose verbatim output is recorded below
# (2026-07-30T04:34:07Z). The DEPLOY that produced the AFTER state was executed
# earlier, at 02:16:34Z, by `fable-fanout-0730/omn15352-ac4-dev-lane` — named in
# actual_output rather than here so this field stays literally true about the
# recorded probe. verifier = the independent adversarial verifier of that deploy
# lane (verifier != runner, verifier != implementer), which reproduced the same
# registry before/after digests, the three-way provenance agreement and the
# ancestry result against the same live surfaces before this receipt was authored.
runner: "fable-ac4-receipt-0730/omn15352-ac4-durable-receipt"
verifier: "omn15352-ac4-adversarial-verifier"
probe_command: "ssh omni-201-ts 'sha256sum /tmp/ac4/registry.json.before /home/jonah/.omnibase/infra/registry.json
  && grep -E \"git_sha|deployed_at|parent_command\" /home/jonah/.omnibase/infra/registry.json && for c
  in omninode-runtime omninode-runtime-effects omnimarket-projection-api; do printf \"%s \" \"$c\"; docker
  exec \"$c\" grep infra_vcs_ref /app/build-provenance.json; done && git -C /data/omninode/omni_home/omnibase_infra
  rev-parse origin/dev'"
probe_stdout: |
  fcc344836dcbf0cbd65caf74a4f76ceb260dac86d2feddd6efca87f179e45d0e  /tmp/ac4/registry.json.before
  3f5bfc1c574898bd7af4e3da45174ec52ea10d542b39e0dfcdbd5dd84475fd1f  /home/jonah/.omnibase/infra/registry.json
    "git_sha": "f420d5fa26f7",
    "deployed_at": "2026-07-30T02:27:37Z",
        "parent_command": "bash scripts/deploy-runtime.sh --execute --cold --force",
  omninode-runtime   "infra_vcs_ref": "f420d5fa26f7",
  omninode-runtime-effects   "infra_vcs_ref": "f420d5fa26f7",
  omnimarket-projection-api   "infra_vcs_ref": "f420d5fa26f7",
  f420d5fa26f704c75c3854ba2efc788d36175d13
actual_output: |
  PASS — AC4 (.201 dev-lane registry.json reconciled to the actually-running revision).

  HOST / ACCESS. `omninode-pc` via Tailscale MagicDNS `omni-201-ts` (100.109.203.94).
  Raw LAN `ssh jonah@192.168.86.201` is CONNECTION REFUSED — recorded because two
  independent lanes hit it (comments `a258d14d`, `ad4b23cb`). # onex-allow-internal-ip

  REGISTRY READBACK — BEFORE vs AFTER, full digests.
    file            : /home/jonah/.omnibase/infra/registry.json
    before snapshot : /tmp/ac4/registry.json.before  (captured 2026-07-29T22:14 EDT by the deploy lane)
    BEFORE  git_sha     = 7964da738bc2
            deployed_at = 2026-07-29T15:22:05Z
            sha256      = fcc344836dcbf0cbd65caf74a4f76ceb260dac86d2feddd6efca87f179e45d0e
    AFTER   git_sha     = f420d5fa26f7
            deployed_at = 2026-07-30T02:27:37Z
            sha256      = 3f5bfc1c574898bd7af4e3da45174ec52ea10d542b39e0dfcdbd5dd84475fd1f
    unchanged across the deploy: active_version 0.38.4, compose_project omnibase-infra,
            profile runtime, deploy_path /home/jonah/.omnibase/infra/deployed/0.38.4

  ATTRIBUTION RECORD (registry.json .attribution, AFTER state).
    parent_command   = bash scripts/deploy-runtime.sh --execute --cold --force
    invoking_command = deploy-runtime.sh --execute --cold --force
    lane             = dev
    compose_project  = omnibase-infra
    mode             = execute
    result           = ALLOW
    grant_guard      = verdict NOT_APPLICABLE, applies false, live_grants [] (dev lane needs no prod grant)
    ts_utc           = 2026-07-30T02:16:34.084851Z
    record_path      = /home/jonah/.omnibase/infra/deploy-attribution/20260730T021634.084851Z-dev-allow.json
    identity         = jonah@omninode-pc (uid 1000)
    mechanism_ticket = OMN-15218
  The BEFORE state carried the SAME parent_command with ts_utc 2026-07-29T15:21:59.719855Z,
  i.e. the pre-state was itself a `--cold --force` deploy (lane R29), not a partial write.

  THREE-WAY AGREEMENT — registry == deploy source == running containers.
    registry.json git_sha                            : f420d5fa26f7
    deploy-source HEAD (origin/dev, .201 clone)      : f420d5fa26f704c75c3854ba2efc788d36175d13
    /app/build-provenance.json infra_vcs_ref
      omninode-runtime                               : f420d5fa26f7
      omninode-runtime-effects                       : f420d5fa26f7
      omnimarket-projection-api                      : f420d5fa26f7
    build_source workspace, build_time 2026-07-30T02:16:40Z, sibling proofs 4/4 status=verified
  NOTE the container is `omnimarket-projection-api`, not `omninode-projection-api`
  (named here because the verifier had to correct that in the lane report).

  MERGED-AND-DEPLOYED — the defect condition AC4 actually closes.
    git merge-base --is-ancestor b6df2d16169a71394a087f268f2be6d72d99551e f420d5fa26f704c75c3854ba2efc788d36175d13 -> 0  (fix IS in the deployed revision)
    git merge-base --is-ancestor b6df2d16169a71394a087f268f2be6d72d99551e 7964da738bc2                             -> 1  (fix was NOT in the pre-state)
  `b6df2d16...` is the squash-merge commit of omnibase_infra PR #2541 on `dev`
  (MERGED 2026-07-29T19:11:27Z). The lane was last deployed 15:22:05Z, ~3h49m
  BEFORE that merge — so the pre-state was merged-not-deployed, exactly the
  `feedback_merged_is_not_deployed_verify_blocker_root_cause` class.

  ORDERING PROOF — the FIXED script performed this write (log `.201:/tmp/ac4/bringup.log`,
  2923 lines, sha256 3d11a57eadf287da3642bfc3a17cf76bd38d38187f2abf5032fb97af49312077):
     122 [deploy] Snapshotted pre-build :latest image ids for 8 service(s) (rollback safety).
     124 [deploy] === Build Images ===
    2850 [deploy] === Deploy Readback (RT-6, fail-closed) [OMN-14469] ===
    2891 [deploy] === Write Registry ===
  Pre-fix run quoted in the ticket description had Write Registry at 123 and Build
  Images at 133 — the ordering is INVERTED in the artifact that actually ran, which
  is what commit-on-success means. RT-6 readback passed for 8/8 in-scope services,
  each `revision f420d5fa26f7 matches intended f420d5fa26f7`.

  SCOPE — WHAT THIS RECEIPT DOES NOT PROVE (do not let a green here imply it):
    * AC1 / AC2 (RED-first test driving the real deploy-runtime.sh failure path,
      asserting registry.json byte-identical and :latest unchanged after an injected
      post-write failure) are NOT proven here. They are carried by
      tests/scripts/test_deploy_runtime_registry_commit_on_success.py in
      https://github.com/OmniNode-ai/omnibase_infra/pull/2541 . The AC4 lane
      deliberately injected no failure into a live keep-alive lane, and the RED
      re-execution is still owed by an independent party.
    * registry.json stores only the 12-char abbreviated git_sha, so the file alone
      cannot distinguish f420d5fa26f7 from another commit sharing that prefix. The
      full 40-hex source HEAD is recorded above and re-derivable from `origin/dev`.
    * One evidence-hygiene correction to the lane report, recorded rather than
      dropped: `omnibase-infra-postgres` has RestartCount=1 (Created
      2026-07-29T13:07:35Z, ~13h before this deploy, Health=healthy), so the lane
      report's "all running containers healthy, RestartCount=0" was overstated for
      1 of 17 containers. Pre-existing dep container, not a masked regression.
exit_code: 0
duration_ms: 2810
branch: "jonah/omn-15352-ac4-durable-receipt"
working_dir: "/data/omninode/omni_home/omnibase_infra"
# evidence_class deliberately UNSET. `runtime_ops` (OMN-14168) is the no-PR /
# no-source-change hatch that WAIVES the merged-PR requirement, and this receipt
# does not need that waiver: the fix it reads back is omnibase_infra PR #2541,
# MERGED 2026-07-29T19:11:27Z as b6df2d16169a71394a087f268f2be6d72d99551e. Claiming
# the runtime-ops class here would smuggle in a waiver the evidence does not require
# (and would additionally require no_source_change=true + an empty pr_number, both
# false for this ticket). Left unclassified on purpose.
