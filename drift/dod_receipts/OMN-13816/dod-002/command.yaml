---
schema_version: "1.0.0"
ticket_id: "OMN-13816"
evidence_item_id: "dod-002"
check_type: "command"
check_value: "python scripts/validate_state_coverage.py --check-all (old-vs-new parity + wave zero-divergence)"
contract_sha256: "sha256:a3594f4ae88ab0ea04874b0b2cd025144b9e5312cc833b33c0f6f01999ee0813"
status: PASS
run_timestamp: "2026-07-02T03:25:00Z"
commit_sha: "f54d95ed006cabacf49d4958b407f9f4c498b9e0"
branch: "jonah/state-coverage-gate-ast-harden"
pr_number: 1555
runner: "claude-local-omn-13816"
verifier: "claude-receipt-review-omn-13816"
probe_command: "cd $OMNI_HOME/omni_worktrees/OMN-scg-harden/omnimarket && python scripts/validate_state_coverage.py --check-all --json (old logic vs new logic on same HEAD) + per-node old-vs-new diff on Wave 3/5/7/10 nodes"
probe_stdout: |
  BEFORE (old logic, same HEAD): {'total': 346, 'failed': 0, 'warned': 250, 'passed': 96}
  AFTER  (new logic v2)        : {'total': 346, 'failed': 0, 'warned': 249, 'passed': 96}
  NEW failing nodes (pre-grandfather): ['node_pr_review_fsm_reducer']  # review state, was a false-positive
                                       # import-path substring match; grandfathered into baseline.
  Total old-vs-new divergences across all Wave 3/5/7/10 nodes (strict+nonstrict): 0
  AST bad-form rejection: assert "active" -> False; "active" == "active" -> False;
    x.overall_status == x.overall_status -> False; docstring-only -> False; comment-only -> False;
    import-path .review -> False
  AST genuine-form retention: assert result.state == "active" -> True;
    assert result.overall_status == "healthy" -> True; enum in for-loop -> True; assertEqual -> True
actual_output: >-
  PASS: full-run `--check-all` introduces 0 new FAIL vs the pre-change baseline after the single exposed
  false-positive (node_pr_review_fsm_reducer `review`) is grandfathered alongside its 8 already-baselined
  sibling states. Old-vs-new coverage classification diverges on zero of the Wave 3/5/7/10 nodes in both
  strict and non-strict modes. The three canonical vacuous forms are rejected; genuine assertions stay
  covered.
exit_code: 0
