---
schema_version: "1.0.0"
ticket_id: "OMN-13674"
evidence_item_id: "dod-omnimarket-pr-1566"
check_type: "command"
check_value: "grep -q '^status: PASS$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1566/command.yaml
  && grep -q '^pr_number: 1566$' drift/dod_receipts/OMN-13674/dod-omnimarket-pr-1566/command.yaml"
status: PASS
run_timestamp: "2026-07-02T12:55:00Z"
commit_sha: "888ae198cbbfb41d5d82811cce9bdedc64824d0f"
branch: "jonah/omn-13674-cov-A-merge_sweep_pr_lifecycle_orchestrators"
pr_number: 1566
runner: "claude-omn-13674-cov-A"
verifier: "claude-receipt-review"
contract_sha256: "sha256:99db5e48cb9cd88fc8ec205f7745c12d8f91927efabc8f42978c510cbf8e00db"
probe_command: "uv run pytest tests/integration/test_pr_lifecycle_orchestrator_fsm_state_coverage.py tests/integration/test_merge_sweep_triage_orchestrator_route_coverage.py
  -p no:randomly -q"
probe_stdout: |
  31 passed
actual_output: |
  omnimarket PR #1566 is the OMN-13674 cov-A declared-state coverage PR for the
  merge_sweep_pr_lifecycle_orchestrators cluster (ORCHESTRATOR archetype). It adds
  two bus-driven coverage suites over the canonical in-memory bus (EventBusInmemory
  via integration_event_bus + LocalRuntimeBusAdapter):
  (1) node_pr_lifecycle_orchestrator FSM coverage — 12 scenarios read
  phase-transition events off the bus to prove every declared FSM state (IDLE,
  INVENTORYING, TRIAGING, VERIFYING, MERGING, POST_MERGE_TAIL, FIXING, COMPLETE,
  FAILED) is entered and every declared transition edge (18) fires, including every
  failure->FAILED edge; a completeness test asserts the scenario-edge union equals
  the contract FSM exactly. Terminal-event payload asserted per path. Error edges
  injected by fault at the phase-entry call (subclass injection; never
  subprocess/asyncpg monkeypatch).
  (2) node_merge_sweep_triage_orchestrator route coverage — every declared output
  command class reached (AutoMergeArm, Rebase x2, CiRerun x2 modes, ThreadReply,
  ConflictHunk, CiFix, PrPolishStart); every SKIP gate has a negative control (no
  command); a bus round-trip lands the terminal ModelHandlerOutput on the contract
  terminal_event topic. gh boundary replaced by subclass override of _resolve_*;
  no subprocess monkeypatch.
  node_merge_sweep does not exist as a node in omnimarket, so it has no state space
  and is excluded. Local proof: the two suites = 31 passed; ruff format/check clean;
  full suite (uv run pytest tests/, no -k) = 11319 passed, 59 skipped; the 6
  failures + 1 error are pre-existing infra/env-dependent tests (live Postgres x4,
  live Kafka x2, event-registry drift for artifact-captured/tool-output-captured),
  none touching the added files; pre-commit on both files = all hooks Passed.
exit_code: 0
duration_ms: 630
working_dir: "/Users/jonah/Code/omni_home/.claude/worktrees/wf_e1d248a6-c27-2/omnimarket-mkt"
