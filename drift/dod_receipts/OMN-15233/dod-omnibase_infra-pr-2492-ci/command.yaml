---
schema_version: "1.0.0"
ticket_id: "OMN-15233"
evidence_item_id: "dod-omnibase_infra-pr-2492-ci"
check_type: "command"
check_value: |-
  gh api "repos/OmniNode-ai/omnibase_infra/commits/5bfb419f1be51504d0955e1ebbc9c9d0425dd8b4/check-runs?per_page=100" --jq '.check_runs[] | select(.name=="runner-image-build-smoke") | .conclusion' | grep -qx success
contract_sha256: "sha256:a4008038f3a085470f072ef208328759f7ebed00ebb9269f15b22956147065df"
status: PASS
run_timestamp: "2026-07-27T13:05:00Z"
commit_sha: "5bfb419f1be51504d0955e1ebbc9c9d0425dd8b4"
runner: "claude-omn15233-implementer"
verifier: "github-actions-ci"
probe_command: |-
  gh api "repos/OmniNode-ai/omnibase_infra/commits/5bfb419f1be51504d0955e1ebbc9c9d0425dd8b4/check-runs?per_page=100" --jq '.check_runs[] | select(.name=="runner-image-build-smoke" or .name=="shell-hygiene") | "\(.name)=\(.conclusion)"'
probe_stdout: |
  runner-image-build-smoke=success
  shell-hygiene=success
actual_output: >-
  PASS on the substantive gate set, with the occ-preflight chain honestly enumerated as the only red.
  GitHub Actions re-ran the omnibase_infra gates against a clean checkout of head 5bfb419f on hardware
  unrelated to the authoring host - an independent verifier, which is why this receipt is signed by github-actions-ci
  rather than by the agent that wrote the change. 55 checks SUCCESS, 38 SKIPPED. Runner Image Build Smoke
  passed, which is the load-bearing one for this change: it builds the runner image that contains the
  edited docker/runners/healthcheck.sh and docker/runners/entrypoint.sh. receipt-honesty, Shell Hygiene
  Gate (OMN-14761), Required-Check Skip-Vector Guard, Dispatcher Route Coverage, Canonical Inference Gate,
  Duplication Sweep, Handler Contract Compliance, Topic Drift, Topic Enum Drift, Topic Naming Lint, Dep
  Provenance Gate, Integration Test Coverage, Integration Test Removal Gate, Attest Source Hash and the
  shadow lint plus tests-and-coverage jobs all passed. The 5 distinct red checks are a single cascade
  with one root cause and are expected in this state: occ-preflight/eligibility fails with reason=pr_ticket_mismatch
  because no PASS receipt bound to PR #2492 existed on OCC at the moment that run executed - this very
  receipt is that binding, and it lands on OCC PR #5103 in the same commit. call-reject-skip-token/occ-preflight
  is the same job reached through a second reusable-workflow path. CI Summary is fail-closed on any red
  gating job, Hostile Review Gate short-circuits on occ-preflight, and verify/verify is the receipt gate
  reading the same OCC state. All five clear once this OCC companion lands on onex_change_control dev;
  none of them reports on the runner-healthcheck code itself. No agent merged anything.
exit_code: 0
branch: "jonah/omn-15233-runner-healthcheck-recalibration"
pr_number: 2492
