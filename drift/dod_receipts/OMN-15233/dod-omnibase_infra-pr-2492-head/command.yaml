---
schema_version: "1.0.0"
ticket_id: "OMN-15233"
evidence_item_id: "dod-omnibase_infra-pr-2492-head"
check_type: "command"
check_value: "gh api repos/OmniNode-ai/omnibase_infra/pulls/2492 --jq .head.sha"
contract_sha256: "sha256:38ca3d6ae2c9465fdb20074a58eec96210597baf887ad173ca237fb1ec3dcf8a"
status: PASS
run_timestamp: "2026-07-27T13:05:00Z"
commit_sha: "5bfb419f1be51504d0955e1ebbc9c9d0425dd8b4"
runner: "claude-omn15233-implementer"
verifier: "github-api-readback"
probe_command: "gh api repos/OmniNode-ai/omnibase_infra/pulls/2492 --jq '{number,head_sha:.head.sha,state,base:.base.ref}'"
probe_stdout: |
  {"base":"dev","head_sha":"5bfb419f1be51504d0955e1ebbc9c9d0425dd8b4","number":2492,"state":"open"}
actual_output: >-
  PASS: omnibase_infra PR #2492 is open against dev at head 5bfb419f and carries the OMN-15233 fix. docker/runners/healthcheck.sh
  raises RUNNER_HEALTH_MAX_DIAG_AGE_SECONDS from 900 to 4500s with an inline justification against the
  ~50-minute idle OAuth-refresh _diag cadence (the 900s default flagged an idle runner for ~35 of every
  50 minutes by arithmetic alone), adds a process-topology layer that fails on duplicate Runner.Listener
  processes or any listener with PPID 1, and adds a RATE-based crash-loop layer counting Runner_*.log
  files touched inside a 60-minute window and failing above 6 starts per hour - explicitly not cumulative,
  because a cumulative count grows with container uptime and would red-line every long-lived healthy container
  forever. docker/runners/entrypoint.sh adds _reap_orphaned_listeners, which kills any listener surviving
  from a previous incarnation (TERM, escalating to KILL after LISTENER_REAP_TIMEOUT_SECONDS) and confirms
  it is gone before every run.sh spawn; spawn-without-reap is what manufactures TaskAgentSessionConflictException.
  docs/runbooks/runner-fleet-listener-liveness.md records the interim operating rule that the GitHub runner
  registry is cross-checked before any restart sweep. RED-then-GREEN proof: with docker/runners/{healthcheck,entrypoint}.sh
  reverted to origin/dev and the new tests in place, 9 failed and 3 passed; with the change applied, 12
  passed. Local gates on .200 (ssh stickybeatz-studio, patch-transfer verified by matching diff sha256
  and git diff --cached --stat): ruff check clean, ruff format clean over 4525 files, mypy clean over
  2615 files, pre-commit clean on every changed file, uv run pytest tests/ci/ 962 passed and 1 skipped
  (the governed selector picked tests/ci/ for this changeset), and the integration liveness test passed.
  No live-fleet mutation, no lane mutation, no container recreate.
exit_code: 0
branch: "jonah/omn-15233-runner-healthcheck-recalibration"
pr_number: 2492
