---
schema_version: "1.0.0"
ticket_id: OMN-17863
evidence_item_id: "dod-omn17863-audit-verdict-behavior-proof"
check_type: test_passes
check_value: "pnpm test:audit-verdict"
status: PASS
run_timestamp: "2026-09-07T10:58:46Z"
contract_entry_sha256: "sha256:d581de338c454461ecf479303264c7a436aa4c206d2be9b8172653a2539b064c"
commit_sha: "acec8de716b96e566a68fc4a51f194d6c43e8c01"
runner: "claude:omn17863-audit-verdict-behavior-proof"
verifier: "node_dod_verify (omnimarket 3182b4d72f11)"
pr_number: 8381
duration_ms: 8183
probe_command: |
  # Adjudicated by node_dod_verify itself, not by a bare pnpm run in the canonical clone.
  # The runner stages a lock-exact omniweb tree keyed by (project root, lockfile bytes),
  # populates its own node_modules with `pnpm install --frozen-lockfile` under the pnpm
  # its packageManager field pins (11.5.3, via corepack), and runs the check INSIDE that
  # stage -- so the verdict is not resolved from whatever the last install on this host
  # left behind. Product head under test:
  # repos/OmniNode-ai/omniweb/commits/acec8de716b96e566a68fc4a51f194d6c43e8c01
  OCC_GOVERNANCE_REF=omn17863probe onex skill dod_verify OMN-17863 \
    | jq '.result.handler_result.checks[]
          | select(.evidence_id == "dod-omn17863-audit-verdict-behavior-proof")'
probe_stdout: |
  {
    "evidence_id": "dod-omn17863-audit-verdict-behavior-proof",
    "status": "verified",
    "message": "OK (8183ms): PASS transport timeout on a non-zero exit classifies as REGISTRY_UNREACHABLE\nPASS REGISTRY_UNREACHABLE still fails the gate (UNKNOWN fails closed)\nPASS the same output on stderr rather than stdout cla",
    "unverifiable_cause": null,
    "unbindable_derived_overlay": false,
    "proof_class": "indeterminate",
    "product_clones": [
      {
        "repo_root": "/Users/lakshmanpatel/Desktop/OmniNodeWork2026-27/omnibase/repos/omniweb",
        "freshness": "fresh",
        "head_sha": "acec8de716b96e566a68fc4a51f194d6c43e8c01",
        "upstream_ref": "origin/dev",
        "behind_count": 0,
        "detail": null
      }
    ]
  }
actual_output: |
  PASS: status=verified, OK (8183ms), 16/16, at omniweb acec8de716b96e566a68fc4a51f194d6c43e8c01.

  Three fields above are recorded rather than smoothed over.

  `product_clones[0].repo_root` is this host's OMNI_HOME path, kept verbatim because probe_stdout is
  captured output and editing it is the defect this contract's own gates exist to catch. It pins
  nothing: check_value is `pnpm test:audit-verdict` with no path in it, and the item's cwd is the
  `${OMNI_HOME}/omniweb` token, which every runner resolves for itself.

  `message` is truncated at 200 characters by the collector, not by me. The full suite output,
  captured from the same staged tree, is 16 PASS lines then `16/16 passed`. The three visible ones
  are the transport-vs-finding core: a transport timeout on a non-zero exit classifies as
  REGISTRY_UNREACHABLE; REGISTRY_UNREACHABLE still fails the gate because UNKNOWN fails closed; and
  the same output on stderr rather than stdout classifies identically.

  `proof_class` reads `indeterminate`, not `behavior`, and the run's `behavior_proving_count` is 0.
  That is a finding about the sweep's classifier, not a hedge on this check, and it is left visible
  here rather than omitted: this item does execute the claimed behaviour, so OMN-16106 may still
  post `gap_no_behavior_proof` against a contract that now carries a real behaviour proof.

  WHAT IT PROVES. scripts/ci/pnpm-audit-verdict.ts is the transport-vs-finding classifier this
  ticket is about -- the same target ci.yml Gate 8 runs. The suite pins it against real captured
  output from four live pnpm-audit runs and asserts the invariant directly rather than by example:
  for every exit code and every output in the corpus, a non-zero child exit produces a non-zero gate
  exit. Every other item on this contract reads a PR's state or a file's content at a pinned ref --
  MERGE_STATE or SURROGATE under the OMN-16821 taxonomy. This one runs the code.

  The guard was watched failing before it was trusted. Letting a non-zero child exit reach the CLEAN
  branch takes the suite from 16/16 to 5/16, failing on the invariant itself with
  'exit 1 with output "" produced gateExitCode 0'. The classifier was then restored byte-identical
  and the suite is 16/16 again.

  WHY THIS RECEIPT IS HAND-AUTHORED. No producer mints it. occ-receipt-runner.yml lives only in
  omnimarket, triggers on a product-repo pull_request and carries no workflow_call; omniweb has no
  receipt runner at all. The autoclose sweep reads origin/dev, where this item does not exist -- it
  concluded success 11 times after the Node/pnpm toolchain landed (omnimarket#2330,
  omnibase_infra#3240) and minted nothing for this item, because it is not failing to produce it, it
  is not attempting it. Jonah ruled the remaining step mine rather than a platform gap
  (2026-09-06T15:16Z): "it needs a receipt, which is now producible because the check is
  executable." So the execution here is the real adjudication path, not a hand-rolled substitute.
  Generalising the runner past the omnimarket pilot is OMN-16859 AC3b and stays open; this receipt
  does not close it and does not pretend to.
exit_code: 0
