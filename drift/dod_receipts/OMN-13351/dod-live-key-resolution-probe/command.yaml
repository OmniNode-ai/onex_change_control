---
schema_version: "1.0.0"
ticket_id: "OMN-13351"
evidence_item_id: "dod-live-key-resolution-probe"
check_type: "command"
check_value: >-
  grep -q '^status: PASS$' drift/dod_receipts/OMN-13351/dod-live-key-resolution-probe/command.yaml
contract_sha256: "sha256:9a4bfa47a5e3ca0dc2608ae729e8d0b27d4979af0b4e04009a748e133a461179"
status: PASS
run_timestamp: "2026-06-19T18:30:00Z"
commit_sha: "1cb0b2809111c4186558d6edf0dd70cf68d0a11a"
branch: "jonah/omn-13351-repoint-ceiling-gemini"
pr_number: 1287
runner: "claude-code-subagent"
verifier: "stability-test-runtime-in-container"
probe_command: >-
  ssh jonah@192.168.86.201 'docker exec -i omninode-stability-test-runtime /app/.venv/bin/python - <<PY
  ... from omnimarket.inference.secret_store_resolver import resolve_api_key; for each candidate ref:
  resolve_api_key(ref, required=False) ... PY' ; then a 1-token POST to the AI Studio endpoint per model
probe_stdout: |
  llm.anthropic.api_key -> None (NOT resolvable)
  llm.openai.api_key -> None (NOT resolvable)
  llm.gemini.api_key -> RESOLVED len=39
  llm.openrouter.api_key -> None (NOT resolvable)
  llm.glm.api_key -> RESOLVED len=49
  llm.vertex.access_token -> None (NOT resolvable)
  -- model probe (resolved gemini key, 1 token):
  gemini-2.5-pro -> HTTPError 429 (exceeded current quota)
  gemini-2.5-flash -> HTTP 200 echo=gemini-2.5-flash
  gemini-2.5-flash-lite -> HTTP 200 echo=gemini-2.5-flash-lite
  gemini-2.0-flash -> HTTPError 429
actual_output: >-
  PASS (live key-resolution probe): Inside omninode-stability-test-runtime, resolve_api_key(ref, required=False)
  was called for every candidate ceiling secret_ref, mirroring the 2026-06-19 reprove §2d method. llm.anthropic.api_key
  and llm.openai.api_key resolve to None (the dead ceiling, confirming the HARD FACT); llm.gemini.api_key
  RESOLVES (len=39) and authenticates (post-auth 200/429, never 401/403); llm.openrouter.api_key does
  NOT resolve; llm.glm.api_key RESOLVES (len=49, the cheap_cloud tier); llm.vertex.access_token does NOT
  resolve. A 1-token model probe with the resolved Gemini key returned HTTP 200 for gemini-2.5-flash (echo
  gemini-2.5-flash) and HTTP 429 quota-exhausted for gemini-2.5-pro. This proves the ceiling must be repointed
  off Anthropic and justifies pinning gemini-2.5-flash (the most capable Gemini model that responds 200
  on the resolved key). The verifier path (in-container resolution + independent HTTP model probe) is
  distinct from the routing reducer's escalation runner. No key VALUE was recorded — only resolved/None
  and length.
exit_code: 0
