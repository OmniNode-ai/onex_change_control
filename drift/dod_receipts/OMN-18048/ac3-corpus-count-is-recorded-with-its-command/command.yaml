---
schema_version: "1.0.0"
ticket_id: "OMN-18048"
evidence_item_id: "ac3-corpus-count-is-recorded-with-its-command"
check_type: "command"
check_value: |-
  c=$(gh api "repos/OmniNode-ai/omnibase_infra/contents/src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py?ref=dev" --jq ".content" | base64 -d | tr "\n" " " | tr -s " "); case "$c" in *"Measured (OMN-18048 AC3, 110 ticket descriptions read at full text): **14.5%** carry an AC-looking heading this set does not recognise, across **12 distinct spellings**"*) exit 0 ;; *) exit 1 ;; esac
contract_sha256: "sha256:b0afd1fda85681de139c6a6cf7fb4b3073804b2d57f447f4ce3b519c6d2bac8b"
contract_entry_sha256: "sha256:88318058ea9dec53ee8b3f82f9db37d04a9be7b3be7debad942dc98d28c80aa3"
status: PASS
run_timestamp: "2026-09-10T19:18:49.927380+00:00"
commit_sha: "0a704957c200ad2c7874e62e50515c419f24a286"
runner: "manual"
verifier: "collab-asks-2026-09-10-lane"
probe_command: |-
  The check_value above, run verbatim under `set -o pipefail`, against omnibase_infra dev head 0a704957c200ad2c7874e62e50515c419f24a286.
probe_stdout: |
  (no stdout by construction: the check asserts via `case` on a shell variable and signals only through
  its exit status. The matched text, recovered separately with `grep -oE` for the record, is:)
  Measured (OMN-18048 AC3, 110 ticket descriptions read at full text): **14.5%** carry an AC-looking heading this set does not recognise, across **12 distinct spellings**
actual_output: |-
  PASS. AC3's corpus measurement is recorded on merged omnibase_infra dev at
  src/omnibase_infra/nodes/node_evidence_autoclose_sweep_effect/handlers/handler_evidence_autoclose_sweep.py,
  and all three figures assert together: corpus size 110, rate 14.5%, 12 distinct spellings.

  Controls run before this receipt was written, so the pass is not a wildcard match:
    positive, the check verbatim, 8 consecutive runs .................. exit 0 on 8 of 8
    negative, corpus size replaced with 999 .......................... exit 1
    negative, rate replaced with 99.9% ............................... exit 1
    negative, spelling count replaced with 7 ......................... exit 1

  Each of the three figures is independently load-bearing: changing any one alone turns the check red.
  stderr is not suppressed anywhere in the check, so an API or decode failure surfaces as a failure
  rather than as an empty match that would read as success.

  Recorded during authoring: an earlier draft of this check piped into `grep -q`. Under
  `set -o pipefail` that form returned 141 intermittently (2 of 13 runs) because grep exits on first
  match and SIGPIPEs the upstream base64/tr. That is the OMN-16535 SIGPIPE false-red class. The
  `case` form consumes the whole stream and was stable across 8 of 8 runs; the flaky form was
  discarded rather than shipped.
exit_code: 0
