---
schema_version: "1.0.0"
ticket_id: "OMN-18085"
evidence_item_id: "dod-occ-diff-derived-behavior-proof-pr-1285"
check_type: "test_passes"
check_value: |-
  terraform fmt -check -recursive . && terraform init -backend=false -input=false && terraform validate
contract_sha256: "sha256:bd4ef298ef66ccbbb9caa69c850a70b00ff6b09a887b040114db0ef9973d8594"
contract_entry_sha256: "sha256:caf2ae258cc1d02fee929e14bd158469ba1db0c64a235085010338b28915facf"
status: PASS
run_timestamp: "2026-09-09T19:41:10+00:00"
commit_sha: "0746b49b34e3cdb6d68af538d049e2d48e6fb48c"
runner: "staging-resize-8cpu"
verifier: "occ-union-resolve-same-ticket-companion"
probe_command: |-
  gh api repos/OmniNode-ai/omninode_infra/commits/0746b49b34e3cdb6d68af538d049e2d48e6fb48c --jq .sha \
    && terraform fmt -check -recursive . \
    && terraform init -backend=false -input=false && terraform validate
probe_stdout: |
  0746b49b34e3cdb6d68af538d049e2d48e6fb48c
  Success! The configuration is valid.
actual_output: |-
  PASS: terraform fmt -check -recursive exited 0 (no file requires reformatting) and
  terraform validate reported "Success! The configuration is valid." over aws/cluster-dev,
  run at ${OMNI_HOME}/omni_worktrees/OMN-18085/omninode_infra-resize/aws/cluster-dev.

  The diff is a single terraform variable declaration, so the toolchain over that root is the proof
  that fits it; a pytest selection would prove nothing about this change. The same two checks also
  passed inside the repository's pre-commit hook on both commits.

  The stronger proof is the repository's own `plan (cluster-dev)` PR check, which renders a real
  plan against live AWS state. It passed on both commits of this PR, and its output is what
  corrected this PR's own reasoning: the plan updates aws_launch_template.k3s_system in place, does
  NOT list aws_autoscaling_group.k3s_system among its changes at all, and reports 0 to destroy.
  The first commit had asserted that an apply would trip that ASG's instance_refresh block and
  replace the node; the plan disproves it, because launch_template.version is the literal string
  $Latest, so the ASG resource is unchanged and the refresh has no trigger. The second commit
  corrects the claim rather than leaving it standing.
exit_code: 0
pr_number: 1285
branch: "jonah/omn-18085-resize-dev-system-node"
