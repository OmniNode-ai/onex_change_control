---
schema_version: "1.0.0"
ticket_id: "OMN-18085"
evidence_item_id: "dod-deploy-assessment-pr-1285"
check_type: "command"
check_value: |-
  gh api repos/OmniNode-ai/omninode_infra/pulls/1285/files --paginate --jq '.[].filename' | grep -qxF 'aws/cluster-dev/variables.tf' \
    && ! gh api repos/OmniNode-ai/omninode_infra/pulls/1285/files --paginate --jq '.[].filename' | grep -qE '^k8s/'
contract_sha256: "sha256:bd4ef298ef66ccbbb9caa69c850a70b00ff6b09a887b040114db0ef9973d8594"
contract_entry_sha256: "sha256:c6642ad483ee33c99dd6fc77a33739c54b04e07b33ad118a7d24669009b17ebe"
status: PASS
run_timestamp: "2026-09-09T19:41:10+00:00"
commit_sha: "0746b49b34e3cdb6d68af538d049e2d48e6fb48c"
runner: "staging-resize-8cpu"
verifier: "occ-union-resolve-same-ticket-companion"
probe_command: |-
  gh api repos/OmniNode-ai/omninode_infra/pulls/1285/files --paginate --jq '.[].filename' | grep -qxF 'aws/cluster-dev/variables.tf' \
    && ! gh api repos/OmniNode-ai/omninode_infra/pulls/1285/files --paginate --jq '.[].filename' | grep -qE '^k8s/'
probe_stdout: |
  aws/cluster-dev/variables.tf
actual_output: |-
  PASS, fail-closed: the diff CONTAINS aws/cluster-dev/variables.tf (asserted positively with
  grep -qxF) and contains NO file under k8s/ (asserted with a negated grep -qE, so an empty or failed
  fetch fails the check rather than passing it). The whole diff is that single file.

  Merging this PR mutates no running system. A terraform declaration takes effect only when someone
  applies the root, and this PR runs no apply. The live instance was already resized out of band,
  in place and under operator consent, before this PR opened - so the PR is the declaration
  catching up to reality rather than an instruction to change it.

  Recorded as the residual rather than left implied: applying aws/cluster-dev is NOT a no-op. Its
  plan on this PR reports 3 to add and 11 to change, spanning MSK, RDS, SNS and SSM as well as the
  three launch templates. That breadth is why the apply is a separate decision, which this PR
  neither takes nor authorises.

  Blast radius of the apply that this declaration would eventually feed, stated for the reviewer:
  aws/cluster-dev is the dev-system cluster root. The public cluster instance
  i-0e596e8b557e27785 and prod are in different roots and were untouched by any part of this work.
exit_code: 0
pr_number: 1285
branch: "jonah/omn-18085-resize-dev-system-node"
