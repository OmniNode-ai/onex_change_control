---
# Migration Conflict Suppressions
#
# This file lists known, intentional migration conflicts that should not
# cause CI to fail. Each entry documents WHY the conflict exists and which
# repos are involved.
#
# Categories:
#   cross_repo_shared_schema: Table DDL is intentionally duplicated across repos
#     because each repo deploys its own migrations independently. The schemas
#     are identical (EXACT_DUPLICATE).
#   omnidash_read_model: omnidash maintains its own read-model projection tables
#     with a different schema than the upstream source-of-truth tables
#     (NAME_CONFLICT). This is intentional — omnidash analytics tables are
#     denormalized projections, not the canonical schema.
#   intra_repo_duplicate: The same table appears in multiple migration files
#     within the same repo (e.g., test fixtures, legacy numbered migrations).
#   schema_evolution: Table was redefined with additional columns in a later
#     migration within the same repo (intentional schema evolution).

suppressions:
  # ── EXACT_DUPLICATE: Cross-repo shared schema ──────────────────────────
  # These tables have identical DDL across repos. Each repo owns its own
  # migration files for independent deployment.

  - table: skill_executions
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniclaude, omniintelligence, omnimemory]
    note: "Shared ONEX table deployed independently per repo"

  - table: agent_routing_decisions
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omnimarket]
    note: "infra 021 creates agent_routing_decisions in omnibase_infra DB; omnimarket node_projection_routing_decision
      0021 creates the projection copy in omnidash_analytics (contract database: omnidash_analytics).
      Identical DDL, different DBs (OMN-13150). Consolidation tracked under OMN-13127."

  - table: fsm_state
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "FSM state table shared between infra and intelligence"

  - table: fsm_state_history
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "FSM state history shared between infra and intelligence"

  - table: workflow_executions
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Workflow executions shared between infra and intelligence"

  - table: domain_taxonomy
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Domain taxonomy shared between infra and intelligence"

  - table: learned_patterns
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Learned patterns shared between infra and intelligence"

  - table: pattern_disable_events
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Pattern disable events shared between infra and intelligence"

  - table: pattern_lifecycle_transitions
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omnidash, omniintelligence]
    note: "Pattern lifecycle transitions shared across repos"

  - table: routing_feedback_scores
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Routing feedback scores shared between infra and intelligence"

  - table: db_metadata
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "DB metadata shared between infra and intelligence"

  - table: review_findings
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Review findings shared between infra and intelligence"

  - table: review_fixes
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Review fixes shared between infra and intelligence"

  - table: finding_fix_pairs
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Finding-fix pairs shared between infra and intelligence"

  - table: pattern_candidates
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Pattern candidates shared between infra and intelligence"

  - table: pattern_lifecycle
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Pattern lifecycle shared between infra and intelligence"

  - table: workflow_steps
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Workflow steps shared between infra and intelligence"

  - table: plan_reviewer_model_accuracy
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Plan reviewer model accuracy shared between infra and intelligence"

  - table: plan_reviewer_strategy_runs
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Plan reviewer strategy runs shared between infra and intelligence"

  - table: failure_streaks
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Debug intelligence table shared between infra and intelligence"

  - table: ci_failure_events
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Debug intelligence table shared between infra and intelligence"

  - table: debug_trigger_records
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Debug intelligence table shared between infra and intelligence"

  - table: debug_fix_records
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniintelligence]
    note: "Debug intelligence table shared between infra and intelligence"

  - table: event_ledger
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniclaude, omniintelligence, omnimemory]
    note: "Core ONEX event ledger deployed independently per repo"

  - table: validation_event_ledger
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniclaude, omniintelligence, omnimemory]
    note: "Validation event ledger deployed independently per repo"

  - table: decision_store
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniclaude, omniintelligence, omnimemory]
    note: "Decision store deployed independently per repo"

  - table: decision_conflicts
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniclaude, omniintelligence, omnimemory]
    note: "Decision conflicts deployed independently per repo"

  - table: manifest_injection_lifecycle
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omnibase_infra, omniclaude, omniintelligence, omnimemory]
    note: "Manifest injection lifecycle deployed independently per repo"

  - table: objective_variant_registry
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omniclaude, omniintelligence]
    note: "Objective variant registry shared between claude and intelligence"

  - table: objective_variant_evaluations
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omniclaude, omniintelligence]
    note: "Objective variant evaluations shared between claude and intelligence"

  - table: objective_variant_win_counts
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omniclaude, omniintelligence]
    note: "Objective variant win counts shared between claude and intelligence"

  - table: policy_state_audit
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omniclaude, omniintelligence]
    note: "Policy state audit shared between claude and intelligence"

  - table: policy_state_processed_events
    conflict_type: exact_duplicate
    reason: cross_repo_shared_schema
    repos: [omniclaude, omniintelligence]
    note: "Policy state processed events shared between claude and intelligence"

  # ── NAME_CONFLICT: Omnidash read-model projections ─────────────────────
  # omnidash_analytics has its own denormalized projection tables that
  # intentionally differ from the upstream source-of-truth schema.

  - table: llm_cost_aggregates
    conflict_type: name_conflict
    reason: omnidash_read_model
    repos: [omnibase_infra, omnidash]
    note: "omnidash projection table with denormalized columns for analytics"

  - table: llm_routing_decisions
    conflict_type: name_conflict
    reason: omnidash_read_model
    repos: [omnibase_infra, omnidash, omniintelligence]
    note: "omnidash projection with simplified routing decision columns"

  - table: objective_evaluations
    conflict_type: name_conflict
    reason: omnidash_read_model
    repos: [omnibase_infra, omnidash, omniintelligence]
    note: "omnidash projection adds agent_name column"

  - table: baselines_comparisons
    conflict_type: name_conflict
    reason: omnidash_read_model
    repos: [omnibase_infra, omniclaude, omnidash, omniintelligence, omnimemory]
    note: "omnidash ROI projection with different comparison columns"

  - table: baselines_trend
    conflict_type: name_conflict
    reason: omnidash_read_model
    repos: [omnibase_infra, omniclaude, omnidash, omniintelligence, omnimemory]
    note: "omnidash ROI trend projection with different aggregation columns"

  - table: baselines_breakdown
    conflict_type: name_conflict
    reason: omnidash_read_model
    repos: [omnibase_infra, omniclaude, omnidash, omniintelligence, omnimemory]
    note: "omnidash ROI breakdown projection with different columns"

  - table: policy_state
    conflict_type: name_conflict
    reason: omnidash_read_model
    repos: [omniclaude, omnidash, omniintelligence]
    note: "omnidash adds monitoring columns (confidence, reliability, alerts)"

  - table: pattern_injections
    conflict_type: name_conflict
    reason: omnidash_read_model
    repos: [omnibase_infra, omnidash, omniintelligence]
    note: "omnidash projection adds run_id column"

  - table: pattern_measured_attributions
    conflict_type: name_conflict
    reason: omnidash_read_model
    repos: [omnibase_infra, omnidash, omniintelligence]
    note: "omnidash projection with slightly different columns"

  - table: llm_call_metrics
    conflict_type: name_conflict
    reason: schema_evolution
    repos: [omnibase_infra, omniclaude]
    note: "Runtime telemetry and delegation migrations intentionally carry different metrics columns"

  - table: savings_estimates
    conflict_type: name_conflict
    reason: schema_evolution
    repos: [omnibase_infra, omniclaude]
    note: "Runtime savings analytics and delegation migrations intentionally carry different estimate
      columns"

  - table: delegation_events
    conflict_type: name_conflict
    reason: schema_evolution
    repos: [omnibase_infra, omniclaude]
    note: "omnibase_infra 078_create_delegation_events.sql is the Postgres projection consumed by HandlerProjectionDelegation
      from onex.evt.omniclaude.task-delegated.v1 (22 cols, UUID PK). omniclaude 001_create_delegation_tables.sql
      is the local SQLite hook-side table for client capture (15 cols, INTEGER PK). Different databases,
      different access patterns — kept distinct by design (OMN-10526, OMN-10604)."

  - table: session_outcomes
    conflict_type: name_conflict
    reason: schema_evolution
    repos: [omnibase_infra]
    note: "Legacy 063 session outcome projection and marketplace node 0021 session outcome migration are
      both present during OMN-12898 migration materialization."

  - table: llm_delegation_daily_projection
    conflict_type: name_conflict
    reason: schema_evolution
    repos: [omnibase_infra]
    note: "Runtime delegation aggregate and marketplace node projection keep distinct column sets during
      OMN-12898 materialization."

  - table: evidence_dashboard_projection
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [omnibase_infra]
    note: "Marketplace evidence dashboard node migration duplicates the legacy infra projection DDL during
      OMN-12898 materialization."

  - table: evidence_correlation_trace_projection
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [omnibase_infra]
    note: "Marketplace evidence dashboard node migration duplicates the legacy infra projection DDL during
      OMN-12898 materialization."

  - table: evidence_readiness_aggregate_projection
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [omnibase_infra]
    note: "Marketplace evidence dashboard node migration duplicates the legacy infra projection DDL during
      OMN-12898 materialization."

  # ── NAME_CONFLICT: Schema evolution within repo ────────────────────────

  - table: agent_actions
    conflict_type: name_conflict
    reason: schema_evolution
    repos: [omnibase_infra, omniintelligence]
    note: "Table redefined in migration 020 with expanded columns (action_name, agent_name, etc.)"

  - table: code_entities
    conflict_type: name_conflict
    reason: schema_evolution
    repos: [omniintelligence]
    note: "Table redefined in later migration with expanded columns (enrichment, quality dimensions)"

  - table: code_relationships
    conflict_type: name_conflict
    reason: schema_evolution
    repos: [omniintelligence]
    note: "Table redefined in later migration alongside code_entities expansion"

  # ── EXACT_DUPLICATE / NAME_CONFLICT: Intra-repo test fixtures ─────────
  # onex_change_control test fixtures intentionally define conflicting tables
  # to exercise the conflict detection logic itself.

  - table: users
    conflict_type: name_conflict
    reason: intra_repo_duplicate
    repos: [onex_change_control]
    note: "Test fixture tables for migration conflict detection tests"

  - table: foo
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [onex_change_control]
    note: "Test fixture table for migration conflict detection tests"

  - table: sessions
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [onex_change_control]
    note: "Test fixture table for migration conflict detection tests"

  # ── EXACT_DUPLICATE: Intra-repo (omniclaude) ──────────────────────────

  - table: claude_session_snapshots
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [omniclaude]
    note: "Duplicate migration files within omniclaude"

  - table: claude_session_prompts
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [omniclaude]
    note: "Duplicate migration files within omniclaude"

  - table: claude_session_tools
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [omniclaude]
    note: "Duplicate migration files within omniclaude"

  - table: claude_session_event_idempotency
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [omniclaude]
    note: "Duplicate migration files within omniclaude"

  - table: hook_events
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [omniclaude]
    note: "Duplicate hook event migration files within omniclaude"

  # ── EXACT_DUPLICATE: Intra-repo (omniintelligence) ────────────────────

  - table: review_calibration_runs
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [omniintelligence]
    note: "Duplicate review calibration migration files within omniintelligence"

  - table: review_calibration_model_scores
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [omniintelligence]
    note: "Duplicate review calibration migration files within omniintelligence"

  - table: calibration_event_outbox
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [omniintelligence]
    note: "Duplicate review calibration migration files within omniintelligence"

  # ── EXACT_DUPLICATE: Intra-repo (omnibase_infra) ──────────────────────

  - table: swarm_runs
    conflict_type: exact_duplicate
    reason: intra_repo_duplicate
    repos: [omnibase_infra]
    note: "082_swarm_runs.sql targets the flat infra DB (POSTGRES_DB); nodes/node_projection_swarm/0001_create_swarm_runs.sql
      targets the projection DB (NODE_POSTGRES_DB) so the projection API can read swarm_runs without DEGRADED
      status. Identical DDL, different target databases. (OMN-13084)"
