## Summary

Prose that is not a criterion, and names no check.

## Acceptance criteria

* **AC1 — the 1th thing the change must do.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac1.py -q` asserts it, red before the change and green after.
* **AC2 — the 2th thing the change must do.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac2.py -q` asserts it, red before the change and green after.
* **AC3 — the 3th thing the change must do.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac3.py -q` asserts it, red before the change and green after.
* **AC4 — the 4th thing the change must do.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac4.py -q` asserts it, red before the change and green after.
* **AC5 — the 5th thing the change must do.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac5.py -q` asserts it, red before the change and green after.
* **AC6 — the 6th thing the change must do.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac6.py -q` asserts it, red before the change and green after.
* **AC7 — the 7th thing the change must do.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac7.py -q` asserts it, red before the change and green after.

## Out of scope

Judging whether the bound check proves the criterion.
