## Summary

Prose that is not a criterion, and names no check.

## Acceptance criteria

* **AC1 — the thing this criterion requires.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac1.py -q` asserts it, red before the change and green after.
* **AC2 — the thing this criterion requires.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac2.py -q` asserts it, red before the change and green after.
* **AC2b — the thing this criterion requires.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac2b.py -q` asserts it, red before the change and green after.
* **AC2d — the thing this criterion requires.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac2d.py -q` asserts it, red before the change and green after.
* **AC2c — the thing this criterion requires.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac2c.py -q` asserts it, red before the change and green after.
* **AC2e — the thing this criterion requires.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac2e.py -q` asserts it, red before the change and green after.
* **AC2f — the thing this criterion requires.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac2f.py -q` asserts it, red before the change and green after.
* **AC2g — the thing this criterion requires.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac2g.py -q` asserts it, red before the change and green after.
* **AC3 — the thing this criterion requires.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac3.py -q` asserts it, red before the change and green after.
* **AC4 — the thing this criterion requires.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac4.py -q` asserts it, red before the change and green after.
* **AC5 — the thing this criterion requires.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac5.py -q` asserts it, red before the change and green after.
* **AC6 — the thing this criterion requires.** Prose restating it at length so the item is not a bare label. — falsifier: `uv run pytest tests/test_ac6.py -q` asserts it, red before the change and green after.

## Out of scope

Judging the check.
