---
schema_version: "1.0.0"
ticket_id: "OMN-18291"
title: "Autobind OCC evidence for OMN-18291"
summary: >
  OCC contract authored by node_occ_companion_compute (OMN-14285) for OmniNode-ai/omnibase_infra PR #3478
  and OmniNode-ai/omninode_infra PR #1414.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: "ci"
    description: "PR #3478 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #3478 on OmniNode-ai/omnibase_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-diff-derived-behavior-proof`, executed at cwd `${OMNI_HOME}/omnibase_infra`.
    command: "uv run pytest tests/ci/test_runner_routing_audit.py -q"
  - kind: "ci"
    description: "PR #1414 CI checks green"
    command: "gh pr checks ${PR_NUMBER} --repo ${REPO}"
  - kind: "tests"
    description: |-
      Behavior proof for PR #1414 on OmniNode-ai/omninode_infra, derived from the PR's own diff: the test target(s) it adds or changes. Bound as dod_evidence item `dod-occ-proof-node-1414`, executed at cwd `${OMNI_HOME}/omninode_infra`.
    command: |-
      uv run pytest .github/scripts/test_check_no_hardcoded_aws_ci_identifiers.py tests/aws/test_ci_runner_scaler.py tests/aws/test_ci_runner_scaler_iam.py -q
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "dod-OmniNode-ai-omnibase_infra-pr-3478"
    description: "PR #3478 on OmniNode-ai/omnibase_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/contents/tests/ci/test_runner_routing_audit.py?ref=432095bed6e384941a3d67084319e510803d2b64 --jq '.content' | base64 -d | grep -c 'def test_the_live_policy_declares_every_scoped_variable'
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omnibase_infra/pulls/3478/files --paginate --jq '.[].filename' | grep -cFx '.github/workflows/application-acl-postgres16-proof.yml'
  - id: "dod-occ-diff-derived-behavior-proof"
    description: "PR #3478 on OmniNode-ai/omnibase_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    checks:
      - check_type: "test_passes"
        check_value: "uv run pytest tests/ci/test_runner_routing_audit.py -q"
        cwd: "${OMNI_HOME}/omnibase_infra"
  - id: "occ-self-bind-pr-9324"
    description: "Binds OMN-18291 to OCC companion PR #9324 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9324 --repo OmniNode-ai/onex_change_control --json number,state"
  - id: "dod-OmniNode-ai-omninode_infra-pr-1414"
    description: "PR #1414 on OmniNode-ai/omninode_infra — Evidence-Source autobind."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 1414 --repo OmniNode-ai/omninode_infra --json number,state"
      - check_type: "command"
        check_value: |-
          gh api repos/OmniNode-ai/omninode_infra/pulls/1414/files --paginate --jq '.[].filename' | grep -cFx '.github/scripts/check_no_hardcoded_aws_ci_identifiers.py'
  - id: "dod-occ-proof-node-1414"
    description: "PR #1414 on OmniNode-ai/omninode_infra — diff-derived behavior proof (OMN-16434)."
    source: "generated"
    evidence_artifact: "supersedes_dod_evidence:dod-OmniNode-ai-omninode_infra-pr-1414"
    checks:
      - check_type: "test_passes"
        check_value: |-
          uv run pytest .github/scripts/test_check_no_hardcoded_aws_ci_identifiers.py tests/aws/test_ci_runner_scaler.py tests/aws/test_ci_runner_scaler_iam.py -q
        cwd: "${OMNI_HOME}/omninode_infra"
  - id: "occ-self-bind-pr-9327"
    description: "Binds OMN-18291 to OCC companion PR #9327 (self-bind)."
    source: "generated"
    checks:
      - check_type: "command"
        check_value: "gh pr view 9327 --repo OmniNode-ai/onex_change_control --json number,state"
