schema_version: "1.0.0"
ticket_id: "OMN-000"
summary: "<one-line summary>"

# If true, this ticket touches cross-repo interfaces and must be drift-controlled.
is_seam_ticket: false

# If false, interfaces_touched must be explicitly empty (or include an explicit "none" convention once standardized).
interface_change: false

interfaces_touched: []

evidence_requirements:
  - kind: "tests|docs|ci|benchmark|manual"
    description: "<what evidence must exist>"
    command: "<how to reproduce, if applicable>"

emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
