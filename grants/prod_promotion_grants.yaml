---
# prod_promotion_grants.yaml — un-forgeable trust anchor for prod-promotion approvals
#
# OMN-13418 Phase 2a: This file is the canonical grant registry for production
# promotion approvals. It is protected by a dedicated CODEOWNERS rule requiring
# @OmniNode-ai/platform-leads approval on any PR that touches this file.
#
# Schema per entry:
#   grant_id:           unique UUID identifying this grant (format: "grant-<uuid4>")
#   runtime_lane:       target runtime lane being promoted to (e.g. "prod")
#   image_digest:       sha256 digest of the image being promoted (format: "sha256:<hex>")
#   promotion_batch_id: batch identifier grouping related promotions (format: "batch-<uuid4>")
#   approved_by:        GitHub login of the platform-leads approver (CODEOWNERS-enforced)
#   expires_at:         ISO-8601 UTC datetime after which the grant is invalid
#   created_at:         ISO-8601 UTC datetime when the grant was created
#   reason:             human-readable justification for the promotion
#
# Optional single-use lifecycle markers (OMN-13424; absent == not consumed):
#   consumed:                    bool — once a promotion using this grant reaches a
#                                terminal deploy state, mark true (or remove the entry)
#   consumed_at:                 ISO-8601 UTC datetime the grant was consumed
#   consumed_by_correlation_id:  UUID of the redeploy run that consumed it
#   The resolver treats a consumed grant as ABSENT — no replay of a spent grant.
#
# Invariants enforced by the dedicated validation workflow
# (.github/workflows/validate-prod-promotion-grants.yml):
#   - File must parse as valid YAML
#   - Top-level key must be "entries" (list)
#   - Each entry must have all required fields present; only the OMN-13424
#     consumed* lifecycle markers are tolerated as extra fields
#   - grant_id must match pattern: grant-[0-9a-f-]{36}
#   - image_digest must match pattern: sha256:[0-9a-f]{64}
#   - expires_at and created_at must be ISO-8601 UTC datetimes
#   - expires_at must be after created_at
#   - NO EXPIRED ENTRY may remain (OMN-13424): a grant whose expires_at is in the
#     past FAILs the check — it must be pruned so at rest the file is entries: []
#
# To add a grant:
#   1. Open a PR against onex_change_control with a new entry below.
#   2. CODEOWNERS (@OmniNode-ai/platform-leads) must review and approve.
#   3. The approver's GitHub login becomes the approved_by value.
#   4. The approver MUST differ from the PR author (enforced by CODEOWNERS gate).
#
# This file intentionally starts empty. Entries are added only through the
# CODEOWNERS-gated approval process — never by automated agents or CI.
#
# GRANT REQUEST (OMN-13418) — PREP ONLY, opened by requester `jonahgabriel` on
# 2026-07-25 as background-lane prep. This PR is NOT to be merged by any agent.
# It becomes effective ONLY when a human operator lands it on
# `onex_change_control@main` on explicit operator word — that merge event is
# the anti-self-issue anchor (an agent cannot complete a CODEOWNERS-gated
# merge to `main`). `expires_at` below is a PROPOSAL; the human approver may
# shorten it at merge time.
# ---------------------------------------------------------------------------
entries:
  - grant_id: grant-68c191b0-98a8-4f10-aef9-8a5479fbf230
    runtime_lane: prod
    image_digest: sha256:bb3bab3742518b22d985e314b7bbcab1cf7b07e796b253047a78d4fa3bf2ce29
    promotion_batch_id: batch-b551aa00-7268-42b7-a193-b9156e6d39dc
    approved_by: jonahgabriel
    expires_at: "2026-07-28T23:59:00Z"
    created_at: "2026-07-27T02:52:43Z"
    reason: >-
      Promote main-built, stability-proven omnibase_infra runtime (git 5bb0c25d9b71f97796957e63ffb75d4855714ef7,
      HEAD of `main` as of this grant — the merge commit of PR OmniNode-ai/omnibase_infra#2481, the operator-ruled
      dev-wins bridge that carries all fifteen OMN-15181 prod-bootstrap fixes and lands PROMOTED lineage,
      not a dev-only build) from the .201 stability-test lane to the .201 prod lane. Proof: release-train-lab.yml
      run OmniNode-ai/omnibase_infra#30227238420 (workflow_dispatch, execute=true) SUCCESS; tag lab/stability/20260727T002130Z-5bb0c25d9b71
      cut at that same main HEAD; REFRESH_EXIT: 0; health-gate result PASS (exit 0); overall run result
      "PASS"; receipt 20260727T002208Z-5bb0c25d9b71.json; ancestry check omnibase_infra 87ec5b3165ce ->
      5bb0c25d9b71 confirmed forward-progress OK (no regression). Live docker readback corroborates the
      build: container omninode-stability-test-runtime StartedAt 2026-07-27T00:26:44Z, revision label
      5bb0c25d9b71, and its `.Image` digest equals this run's own receipt `post_image_id` byte-for-byte.
      Candidate runtime digest sha256:bb3bab3742518b22d985e314b7bbcab1cf7b07e796b253047a78d4fa3bf2ce29.
      Companion images built from the same git rev under this same promotion_batch_id: runtime-effects
      sha256:b94b0386b74225cc50baebc59eb70c944400a149806be7a7efe3a31e3f583b9e, projection-api sha256:17b1f7c55c985fa3f0f602e9c041e15cbd5ac4e68ed6fca2cc2d29877bfb3565.
      There is deliberately NO runtime-worker digest in this batch: live `docker ps` against the `omnibase-infra-prod`
      compose project confirms prod runs no runtime-worker service, so that image is excluded from this
      grant, not omitted by oversight. Rollback targets = current live prod omnibase_infra, per container
      (containers have drifted independently, so each rolls back to its OWN anchor, not a single shared
      digest), git rev db3ae8527b5f, version 0.38.3, all healthy (28085/28086 both return HTTP 200): runtime
      sha256:d68b8972202cc09a427a372a25de923cbbb549533eacf307e1cda8d22a5a31c9, runtime-effects sha256:cb3a1c9f7c808950ccd8e75c24f4b19d4147a7ed299ca6c3ec9871bd274778a3,
      projection-api sha256:15811f51886ccaa22196b09b129f89cdb9504065684cb9cd94c3c1f03c0a3a8e. This grant
      SUPERSEDES grant-6dbeae94-0dac-420b-94d4-66f86c433500 (unconsumed, expires 2026-07-28T23:59:00Z,
      pins the now-superseded digest sha256:ddb296f83ef58357707b6c2e2729ff94d24dbe9ba38071c374bde8a6540d516f
      from a dev-only stability build); it is verified INERT this session — the resolver matches lane+digest+batch
      so today's candidate resolves ABSENT against it, and a second independent gate resolves the stability
      digest LIVE off the running container, never off the grant file — and is removed from this file
      in the same PR rather than left alongside the new entry, since two simultaneous live prod grants
      for the same lane is exactly the ambiguity the single-entry-at-rest design guards against. DISCLOSURE
      1 (discovery_errors, not softened): the new artifact reports status=DEGRADED, contracts=296, errors=4
      on the discovery_errors dimension. Four omnimarket/omnimemory nodes (node_code_embedding_effect,
      node_code_enrichment_effect, node_intent_storage_effect, node_memory_retrieval_effect) remain on
      the flat handler_routing schema the OMN-14141 fail-closed loader rejects. PROVEN ZERO CAPABILITY
      LOSS: all 8 declared topics have high-watermark AND log-start-offset 0 (never produced to, ever).
      Prod's current errors=0 is a FALSE CLEAN — same omnimarket 0.4.3 and byte-identical contract.yaml
      on both lanes; only the engine differs (0.38.3's loader silently accepted what 0.38.4 rejects).
      Expect prod to report errors=4 after promotion; that is newly-surfaced pre-existing debt tracked
      by OMN-15027, not a regression caused by this promotion. DISCLOSURE 2 (node_bus_forwarder_effect
      dormancy, not softened): node_bus_forwarder_effect goes dormant under 0.38.4's cloud-mirroring gate
      (commit fc0cd9f2 / OMN-13809, merged 2026-07-01; prod's current rev is its ANCESTOR, so this is
      a stale-promotion gap, not a regression). Investigated and settled on OMN-15225: the forwarder was
      NEVER capable of mirroring in production — ServiceGatewayForwarder is instantiated nowhere in production
      code, the handler payload model cannot validate the real topic payload, and the raise was swallowed
      by a dispatch catch-all that commits offsets anyway. Both affected topics show log-start-offset
      == high-watermark with no segment writes since 2026-07-11 — zero traffic to lose. ONEX_GATEWAY_CLOUD_MIRRORING_ENABLED
      must NOT be set on prod as part of this promotion. requested_by = PR author jonahgabriel (a GitHub
      fact; there is deliberately no requested_by YAML field). approved_by/self-approval is valid per
      OMN-14814 (sole CODEOWNER, no dual-control requirement — the anti-self-issue anchor is the `@main`
      landing event, not a second approver). No raw `.201` SSH/docker retag/recreate path is used or implied
      by this PR — all promotion routes through the gated `node_redeploy_orchestrator` -> prod-promotion-gate
      -> deploy-agent path. NO PROD MUTATION HAS OCCURRED OR IS AUTHORIZED BY THIS PR — this PR only lands
      the trust-anchor grant entry; the actual prod promotion is a separate, explicit, subsequent operator-approved
      redeploy action.
  - grant_id: grant-6cea9b85-db18-409b-a994-c11f80744fc6
    runtime_lane: prod
    image_digest: sha256:b94b0386b74225cc50baebc59eb70c944400a149806be7a7efe3a31e3f583b9e
    promotion_batch_id: batch-b551aa00-7268-42b7-a193-b9156e6d39dc
    approved_by: jonahgabriel
    expires_at: "2026-07-28T23:59:00Z"
    created_at: "2026-07-27T03:42:07Z"
    reason: >-
      OMN-15181 companion grant, sibling to grant-68c191b0-98a8-4f10-aef9-8a5479fbf230 under the SAME
      promotion_batch_id batch-b551aa00-7268-42b7-a193-b9156e6d39dc. REQUIRED for the promotion to complete:
      the prod-promotion grant resolver's _matches_key (omnimarket src/omnimarket/nodes/node_prod_promotion_grant_resolver_effect/grant_resolver.py)
      requires an EXACT single-digest match on runtime_lane + image_digest + promotion_batch_id, and a
      prod promotion sends ONE signed ModelRebuildRequested PER SERVICE, each carrying that service's
      OWN digest — the deploy-agent's assert_prod_request_has_stability_digest independently resolves
      the stability digest per service. grant-68c191b0 covers only the omninode-runtime digest; without
      this entry the omninode-runtime-effects command resolves ABSENT and is rejected fail-closed, leaving
      prod running a split 0.38.4-runtime / 0.38.3-runtime-effects pair. Service: omninode-runtime-effects.
      Digest sha256:b94b0386b74225cc50baebc59eb70c944400a149806be7a7efe3a31e3f583b9e, built from the same
      source rev as the sibling entry: omnibase_infra `main` HEAD 5bb0c25d9b71f97796957e63ffb75d4855714ef7
      (merge of PR OmniNode-ai/omnibase_infra#2481). Proof: release-train-lab.yml run OmniNode-ai/omnibase_infra#30227238420
      (workflow_dispatch, execute=true) SUCCESS; tag lab/stability/20260727T002130Z-5bb0c25d9b71 cut at
      that same main HEAD; REFRESH_EXIT: 0; health-gate result PASS (exit 0); overall run result "PASS".
      Live docker-inspect corroboration on the .201 stability-test lane: container omninode-stability-test-runtime-effects
      StartedAt 2026-07-27T00:26:44Z, revision label 5bb0c25d9b71, `.Image` digest equals this run's own
      receipt value for the effects service byte-for-byte. Rollback anchor for THIS service specifically
      (containers have drifted independently — each service rolls back to its own anchor, never a shared
      digest): sha256:cb3a1c9f7c808950ccd8e75c24f4b19d4147a7ed299ca6c3ec9871bd274778a3, git rev db3ae8527b5f,
      version 0.38.3, healthy (28086 returns HTTP 200) on live prod omnibase-infra-prod-omninode-runtime-effects
      prior to this promotion. Batches with sibling entry grant-68c191b0-98a8-4f10-aef9-8a5479fbf230 (omninode-runtime,
      same batch id, same source rev, same proof run). Standing disclosures, restated verbatim from the
      sibling entry, not softened: (1) post-promotion prod is expected to report discovery_errors=4 (status=DEGRADED,
      contracts=296) from four omnimarket/omnimemory nodes still on the flat handler_routing schema the
      OMN-14141 fail-closed loader rejects — PROVEN ZERO CAPABILITY LOSS (all 8 declared topics show high-watermark
      == log-start-offset == 0, never produced to), newly-surfaced pre-existing debt owned by OMN-15027,
      not a regression; (2) node_bus_forwarder_effect goes dormant under 0.38.4's cloud-mirroring gate
      (commit fc0cd9f2 / OMN-13809), settled on OMN-15225 (forwarder was never capable of mirroring in
      prod — zero traffic to lose) — ONEX_GATEWAY_CLOUD_MIRRORING_ENABLED must NOT be set on prod as part
      of this promotion. No raw `.201` SSH/docker retag/recreate path is used or implied by this PR —
      all promotion routes through the gated `node_redeploy_orchestrator` -> prod-promotion-gate -> deploy-agent
      path. NO PROD MUTATION HAS OCCURRED OR IS AUTHORIZED BY THIS PR — this PR only lands the trust-anchor
      grant entry; the actual prod promotion is a separate, explicit, subsequent operator-approved redeploy
      action.
  - grant_id: grant-80624116-63eb-4cff-b52b-6fea7d6f5d60
    runtime_lane: prod
    image_digest: sha256:17b1f7c55c985fa3f0f602e9c041e15cbd5ac4e68ed6fca2cc2d29877bfb3565
    promotion_batch_id: batch-b551aa00-7268-42b7-a193-b9156e6d39dc
    approved_by: jonahgabriel
    expires_at: "2026-07-28T23:59:00Z"
    created_at: "2026-07-27T03:42:07Z"
    reason: >-
      OMN-15181 companion grant, sibling to grant-68c191b0-98a8-4f10-aef9-8a5479fbf230 under the SAME
      promotion_batch_id batch-b551aa00-7268-42b7-a193-b9156e6d39dc. AUTHORIZED BUT OPTIONAL: unlike the
      runtime and runtime-effects entries, this entry is not required for the in-flight promotion to complete
      — it exists so all three prod services CAN move together rather than leaving projection-api drifted
      on the old digest, if the operator elects to promote it in the same pass. A grant is permission,
      not an action; this entry authorizes but does not compel promoting projection-api. Service: omninode-projection-api
      (prod runs omnimarket-prod-projection-api). Digest sha256:17b1f7c55c985fa3f0f602e9c041e15cbd5ac4e68ed6fca2cc2d29877bfb3565,
      built from the same source rev as the sibling entries: omnibase_infra `main` HEAD 5bb0c25d9b71f97796957e63ffb75d4855714ef7
      (merge of PR OmniNode-ai/omnibase_infra#2481). Proof: release-train-lab.yml run OmniNode-ai/omnibase_infra#30227238420
      (workflow_dispatch, execute=true) SUCCESS; tag lab/stability/20260727T002130Z-5bb0c25d9b71 cut at
      that same main HEAD; REFRESH_EXIT: 0; health-gate result PASS (exit 0); overall run result "PASS".
      Live docker-inspect corroboration on the .201 stability-test lane: container omninode-stability-test-projection-api
      StartedAt 2026-07-27T00:26:44Z, revision label 5bb0c25d9b71, `.Image` digest equals this run's own
      receipt value for the projection-api service byte-for-byte. Rollback anchor for THIS service specifically
      (containers have drifted independently — each service rolls back to its own anchor, never a shared
      digest): sha256:15811f51886ccaa22196b09b129f89cdb9504065684cb9cd94c3c1f03c0a3a8e, git rev db3ae8527b5f,
      version 0.38.3, healthy on live omnimarket-prod-projection-api prior to this promotion. Batches
      with sibling entries grant-68c191b0-98a8-4f10-aef9-8a5479fbf230 (omninode-runtime) and grant-6cea9b85-db18-409b-a994-c11f80744fc6
      (omninode-runtime-effects), same batch id, same source rev, same proof run. Standing disclosures,
      restated verbatim from the sibling entries, not softened: (1) post-promotion prod is expected to
      report discovery_errors=4 (status=DEGRADED, contracts=296) from four omnimarket/omnimemory nodes
      still on the flat handler_routing schema the OMN-14141 fail-closed loader rejects — PROVEN ZERO
      CAPABILITY LOSS (all 8 declared topics show high-watermark == log-start-offset == 0, never produced
      to), newly-surfaced pre-existing debt owned by OMN-15027, not a regression; (2) node_bus_forwarder_effect
      goes dormant under 0.38.4's cloud-mirroring gate (commit fc0cd9f2 / OMN-13809), settled on OMN-15225
      (forwarder was never capable of mirroring in prod — zero traffic to lose) — ONEX_GATEWAY_CLOUD_MIRRORING_ENABLED
      must NOT be set on prod as part of this promotion. No raw `.201` SSH/docker retag/recreate path
      is used or implied by this PR — all promotion routes through the gated `node_redeploy_orchestrator`
      -> prod-promotion-gate -> deploy-agent path. NO PROD MUTATION HAS OCCURRED OR IS AUTHORIZED BY THIS
      PR — this PR only lands the trust-anchor grant entry; the actual prod promotion is a separate, explicit,
      subsequent operator-approved redeploy action.
